# Milestone v1.2 — Contas e Propriedade (Requirements Archive)

**Status:** ✅ SHIPPED 2026-07-08
**Arquivo original:** `.planning/REQUIREMENTS.md`
**Decisões consolidadas em:** `docs/alinhamento-milestone-v1.2.md`

## Requirements v1.2 — Final Status

| # | Requirement | Status | Evidence |
|---|-------------|--------|----------|
| 1 | Autenticação: Supabase Auth, email+senha, sessão SSR via `@supabase/ssr` | ✅ Validated | Phases 7-8: login, signup, confirm, middleware SSR. 465 tests. UAT 14/14. |
| 2 | Vínculo user→store: `stores.user_id UUID NOT NULL UNIQUE REFERENCES auth.users(id)`. Relação 1:1. | ✅ Validated | Phase 9: migration `20260706000001_add_user_id_to_stores.sql`. Helpers `getCurrentStore()`, `requireOwnership()`. |
| 3 | Isolamento de propriedade: RLS em 5 tabelas. Owner SELECT em 4; `generation_events` default-deny. | ✅ Validated | Phase 10: migration `20260707000001_enable_rls_child_tables.sql`. D8-020 a D8-024 verificados via SQL. |
| 4 | Proteção de rotas: middleware (auth) + server component (store) + handler (ownership) + serviço (contexto). | ✅ Validated | Phases 7-10: middleware matcher, `requirePageUser`, `requireApiUser`, `requireAuthorizedStore`, `requireOwnership`, CSRF. |
| 5 | Fluxo de entrada: `/login`, `/signup`, `/check-email`, `/auth/confirm`. | ✅ Validated | Phase 8: páginas no route group `(auth)`. Anti-enumeration em signup/forgot. |
| 6 | Onboarding: usuário autenticado sem loja é estado válido. `/store` em modo create. | ✅ Validated | Phase 9: server components com `getCurrentStore()`. `/store` create/edit modes. |
| 7 | Ownership: `claims.sub` resolve `stores.user_id`. `localStorage("store_id")` removido. Cliente sessão + RLS padrão; service role excepcional. | ✅ Validated | Phase 9: localStorage scan zero. Phase 10: guards em todos os handlers. D8 cross-tenant 7/7. |
| 8 | Critério de aceite: cenários binários de segurança, testados na pirâmide unit→HTTP→RLS real→E2E→UAT. Milestone fecha com todos VERDES. | ✅ Validated | D8 Catalog: 21/21 PASS. 465 testes automatizados. UAT Phase 8 (14/14) + Phase 10 (7/7). |

### Exclusões (validadas como fora de escopo)

| Item | Motivo | Status |
|------|--------|--------|
| Campanhas persistidas | Escopo auth+ownership | ⏭ Fora de escopo |
| Export PNG/JPG | Decisão MC-03 v1.1, adiado | ⏭ Fora de escopo |
| Dashboard completo | Exige mais definição de produto | ⏭ Fora de escopo |
| Planos e cobrança | Uso livre durante validação | ⏭ Fora de escopo |
| Múltiplas lojas | Relação 1:1 nesta milestone | ⏭ Fora de escopo |
| OAuth social / Magic link | Exclusão deliberada v1.2 | ⏭ Fora de escopo |
| Ajustes de arte | Motor valida geração (MC-01) | ⏭ Fora de escopo |

### Riscos — Status Final

| Risco | Impacto | Resolução |
|-------|---------|-----------|
| Reset de dados existentes | Alto | Reset executado na migration da Phase 9. Dados existentes deletados conforme planejado. |
| Dados legado (stores sem dono) | Médio | Reset confirmado. |
| Testes existentes assumem ausência de auth | Alto | 465 testes migrados e passando. Mocks adaptados para auth guards. |
| Service role sem ownership check | Alto | D5/D7 implementados: ownership verificado antes de qualquer operação admin. D8 cross-tenant 7/7 PASS. |
| Bucket `store-logos` legado não inventariado | Baixo | Inventário concluído: 0 objetos. Pendente remoção (migration cleanup). |

---

_Archived from `.planning/REQUIREMENTS.md` on 2026-07-08 during milestone v1.2 close._

---
id: 08-03
wave: 3
depends_on: [08-02]
files_modified:
  - src/app/(auth)/signup/__tests__/signup-form.test.tsx
  - src/app/(auth)/check-email/__tests__/check-email.test.tsx
  - src/app/auth/confirm/__tests__/confirm.test.ts
  - src/app/(auth)/forgot-password/__tests__/forgot-password-form.test.tsx
  - src/app/(auth)/update-password/__tests__/update-password-form.test.tsx
  - src/app/(auth)/login/__tests__/login-form.test.tsx
  - src/middleware.test.ts
  - src/lib/supabase/site-url.test.ts
autonomous: true
requirements:
  - testing
---

# Plan 08-03: Tests

Wave 3 — Testing (depends on all pages existing)

## Objective

Criar testes unitários para todos os novos componentes e funcionalidades da Fase 8, seguindo o padrão de mock estabelecido na Fase 7 (mockar `@/lib/supabase/server` e `@/lib/supabase/client`). Verificar regressão.

---

## Must Haves

- Testes para signup form (renderização, submit, validação, loading)
- Testes para check-email page (renderização por type)
- Testes para auth/confirm handler (todos os cenários de token)
- Testes para forgot-password form (renderização, submit, loading)
- Testes para update-password form (renderização, submit, validação, erro)
- Testes para middleware expandido (novas rotas, /auth/confirm passthrough)
- Testes para login form (links de navegação)
- Testes para validação de NEXT_PUBLIC_SITE_URL
- Regressão: 344+ testes existentes continuam passando

---

## Tasks

### Task 01: Testar signup form

<action>
Criar `src/app/(auth)/signup/__tests__/signup-form.test.tsx`:

- Renderização: email input, password input, confirm password input, submit button
- Validação: senha < 6 caracteres → "A senha deve ter no mínimo 6 caracteres"
- Validação: confirmar diferente → "As senhas não conferem"
- Submit sucesso → `router.replace("/check-email?type=signup")` chamado
- Submit erro (email exists) → `router.replace("/check-email?type=signup")` chamado (anti-enumeration)
- Loading state: botão desabilitado com `Loader2`
- Mock `@/lib/supabase/client` para `createBrowserClient`
</action>

<read_first>
- src/app/(auth)/signup/signup-form.tsx
- src/app/(auth)/login/__tests__/ (padrão de teste existente)
</read_first>

<acceptance_criteria>
- 6+ cenários de teste implementados
- `npx vitest run src/app/\(auth\)/signup/` exit 0
</acceptance_criteria>

### Task 02: Testar check-email page

<action>
Criar `src/app/(auth)/check-email/__tests__/check-email.test.tsx`:

- Renderização com `?type=signup`: texto de confirmação visível
- Renderização com `?type=recovery`: texto de redefinição visível
- Renderização sem `?type`: texto genérico fallback
- Nenhum cenário exibe email do usuário

Usar `// @vitest-environment jsdom`.
</action>

<read_first>
- src/app/(auth)/check-email/page.tsx
</read_first>

<acceptance_criteria>
- 3+ cenários de teste implementados
- `npx vitest run src/app/\(auth\)/check-email/` exit 0
</acceptance_criteria>

### Task 03: Testar auth/confirm handler

<action>
Criar `src/app/auth/confirm/__tests__/confirm.test.ts`:

- Token signup válido → `verifyOtp()` chamado com `{ type: 'signup', token_hash }`, redirect `/`
- Token recovery válido → redirect `/update-password`
- `next=/update-password` (válido) → usado como redirect
- `next=https://evil.com` (inválido) → fallback `/`
- Token inválido → redirect `/login?error=confirmation_failed`
- Recovery inválido → redirect `/login?error=recovery_failed`
- Sem `token_hash` → redirect `/login?error=confirmation_failed`
- Mock `createServerClient` de `@/lib/supabase/server`
</action>

<read_first>
- src/app/auth/confirm/route.ts
- src/app/auth/signout/__tests__/ (padrão de teste de route handler)
</read_first>

<acceptance_criteria>
- 8+ cenários de teste implementados
- `npx vitest run src/app/auth/confirm/` exit 0
</acceptance_criteria>

### Task 04: Testar forgot-password form

<action>
Criar `src/app/(auth)/forgot-password/__tests__/forgot-password-form.test.tsx`:

- Renderização: email input e submit button
- Submit válido → `router.replace("/check-email?type=recovery")`
- Submit com email inexistente → mesmo comportamento (anti-enumeration)
- Loading state: botão desabilitado
- Mock `@/lib/supabase/client`
</action>

<read_first>
- src/app/(auth)/forgot-password/forgot-password-form.tsx
</read_first>

<acceptance_criteria>
- 3+ cenários de teste implementados
- `npx vitest run src/app/\(auth\)/forgot-password/` exit 0
</acceptance_criteria>

### Task 05: Testar update-password form

<action>
Criar `src/app/(auth)/update-password/__tests__/update-password-form.test.tsx`:

- Renderização: password input, confirm input, submit button
- Validação: senha < 6 → "A senha deve ter no mínimo 6 caracteres"
- Validação: confirmar diferente → "As senhas não conferem"
- Submit sucesso → `router.replace("/")`
- Submit erro → "Não foi possível atualizar a senha. Tente novamente."
- Loading state
- Mock `@/lib/supabase/client`
</action>

<read_first>
- src/app/(auth)/update-password/update-password-form.tsx
</read_first>

<acceptance_criteria>
- 5+ cenários de teste implementados
- `npx vitest run src/app/\(auth\)/update-password/` exit 0
</acceptance_criteria>

### Task 06: Testar login form — links de navegação

<action>
Criar ou estender `src/app/(auth)/login/__tests__/login-link-navigation.test.tsx`:

- Link "Criar conta" com `href="/signup"` está presente
- Link "Esqueci minha senha" com `href="/forgot-password"` está presente
- Clicar nos links não submete o formulário
</action>

<read_first>
- src/app/(auth)/login/login-form.tsx
</read_first>

<acceptance_criteria>
- 3 cenários implementados
- `npx vitest run src/app/\(auth\)/login/` exit 0 (todos os testes incluindo existentes)
</acceptance_criteria>

### Task 07: Testar middleware expandido

<action>
Estender os testes de middleware existentes para cobrir as novas rotas:

- `/signup` sem sessão → pass-through (pública)
- `/check-email` sem sessão → pass-through (pública)
- `/forgot-password` sem sessão → pass-through (pública)
- `/update-password` sem sessão → redirect `/login`
- `/auth/confirm` sem sessão → pass-through (ALWAYS_PASSTHROUGH)
- `/auth/confirm` com sessão → pass-through (ALWAYS_PASSTHROUGH — não redirect `/`)
- `/signup` com sessão → redirect `/`
- `/forgot-password` com sessão → redirect `/`
</action>

<read_first>
- src/middleware.ts
- src/middleware.test.ts (existente para seguir padrão)
- openspec/changes/fase-8-ciclo-de-conta/specs/auth-middleware/spec.md
</read_first>

<acceptance_criteria>
- 8+ novos cenários de middleware adicionados
- `npx vitest run src/middleware.test.ts` exit 0
</acceptance_criteria>

### Task 08: Testar validação de NEXT_PUBLIC_SITE_URL

<action>
Criar `src/lib/supabase/__tests__/site-url.test.ts`:

- `NEXT_PUBLIC_SITE_URL` definida → `getSiteUrl()` retorna o valor
- `NEXT_PUBLIC_SITE_URL` ausente → throw com mensagem clara
</action>

<read_first>
- src/lib/supabase/site-url.ts
</read_first>

<acceptance_criteria>
- 2 cenários implementados
- `npx vitest run src/lib/supabase/` exit 0
</acceptance_criteria>

### Task 09: Verificar regressão

<action>
Rodar a suíte completa de testes:
```bash
npx vitest run
```

Confirmar que todos os testes (344 existentes + novos) estão verdes.
</action>

<read_first>
- (execução de comando — sem alteração de código)
</read_first>

<acceptance_criteria>
- `npx vitest run` exit 0
- Total de testes ≥ 344
</acceptance_criteria>

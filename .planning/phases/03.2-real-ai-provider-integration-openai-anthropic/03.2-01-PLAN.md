---
phase: 03.2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .env.example
  - src/lib/campaign-intelligence/providers/openai.ts
  - src/lib/campaign-intelligence/service.ts
autonomous: true
requirements: []
---

<objective>
Implementar o OpenAIProvider real para o sistema de campaign intelligence, atualizar o factory createDefaultProvider(), instalar dependências, configurar ambiente e atualizar specs.
</objective>

<tasks>

<!-- ─── Wave 1: Setup ─────────────────────────────────────── -->

<task id="3.2-01-01" type="execute" wave="1">
<action>
Install `openai` npm package as a runtime dependency.

Run: npm install openai
</action>
<read_first>
- package.json
</read_first>
<acceptance_criteria>
- `package.json` contains `"openai"` in `dependencies`
- `node_modules/openai/` exists
</acceptance_criteria>
</task>

<task id="3.2-01-02" type="execute" wave="1">
<action>
Create `.env.example` in the project root with entries for:
- `OPENAI_API_KEY=your_openai_api_key_here`
- `OPENAI_MODEL=gpt-4o-mini`

Do NOT add a .env file — only .env.example. The .env.example must not contain real secrets.
</action>
<read_first>
- (project root — no existing .env.example to read)
</read_first>
<acceptance_criteria>
- `.env.example` file exists at project root
- `.env.example` contains `OPENAI_API_KEY=`
- `.env.example` contains `OPENAI_MODEL=gpt-4o-mini`
- `.env.example` does not contain real secrets
</acceptance_criteria>
</task>

<!-- ─── Wave 2: OpenAIProvider Implementation ─────────────── -->

<task id="3.2-01-03" type="execute" wave="2">
<action>
Create `src/lib/campaign-intelligence/providers/openai.ts` with an `OpenAIProvider` class implementing `AIProvider`.

Requirements:
- `readonly name = "openai"`
- Constructor accepts optional `model` parameter (defaults to `process.env.OPENAI_MODEL` or `"gpt-4o-mini"`)
- `generate(input: CampaignGenerationInput): Promise<ProviderRawResponse>` method
- Uses dynamic `import("openai")` inside the method — NOT a static import at module level
- Uses OpenAI Structured Outputs via `response_format: { type: "json_schema" }` with `zodResponseFormat` from `openai/helpers/zod`
- Falls back to `json_object` mode only for explicit `response_format`/model capability errors (NOT for auth, rate limit, network, quota, or validation errors)
- Zod validation (CampaignSpecSchema.safeParse) runs after every response as second layer
- System prompt written in Brazilian Portuguese targeting Product + Offer format
- Returns `ProviderRawResponse` with `raw` being JSON string of generated campaign spec matching CampaignSpecSchema structure
- Do NOT mock the API — use real OpenAI SDK calls

The prompt must include:
- Store-specific commercial copy referencing the actual store name
- Product offer data with Brazilian price formatting (BRL)
- Visual parameters aligned with CAMPAIGN_VISUAL_SYSTEM.md constraints
- Generation metadata with provider, model, and timestamp
</action>
<read_first>
- src/lib/campaign-intelligence/providers/types.ts (AIProvider interface)
- src/lib/campaign-intelligence/providers/mock.ts (MockProvider pattern)
- src/lib/campaign-intelligence/schema.ts (CampaignSpecSchema)
- src/lib/campaign-intelligence/service.ts (consumer context)
- openspec/design-system/CAMPAIGN_VISUAL_SYSTEM.md (visual parameter constraints)
</read_first>
<acceptance_criteria>
- `src/lib/campaign-intelligence/providers/openai.ts` exists
- OpenAIProvider implements AIProvider interface
- `provider.name` equals `"openai"`
- Uses `import("openai")` (dynamic), not `import ... from "openai"` (static)
- Uses `response_format` with `json_schema` type
- Uses `zodResponseFormat` for schema derivation
- System prompt is in Brazilian Portuguese
- `npm run typecheck` passes
</acceptance_criteria>
</task>

<!-- ─── Wave 3: Provider Selection & Integration ──────────── -->

<task id="3.2-01-04" type="execute" wave="3">
<action>
Update `createDefaultProvider()` in `src/lib/campaign-intelligence/service.ts`.

Replace the current implementation that returns `new MockProvider()` unconditionally with:

1. Check `process.env.OPENAI_API_KEY`
2. If set → dynamically import `OpenAIProvider` (from `./providers/openai`) and return instance
3. If NOT set AND `process.env.NODE_ENV === "production"` → throw new Error with message `"OPENAI_API_KEY is not configured. This is required in production."`
4. If NOT set AND dev/test (`NODE_ENV !== "production"`) → return `new MockProvider()`

Remove the static import of `MockProvider` and add dynamic import for both providers inside the function body.

DO NOT modify the `CampaignIntelligenceService` class itself — only the `createDefaultProvider()` factory function.
</action>
<read_first>
- src/lib/campaign-intelligence/service.ts (current factory implementation)
- src/lib/campaign-intelligence/providers/openai.ts (just created in previous task)
- src/lib/campaign-intelligence/providers/types.ts (AIProvider interface)
</read_first>
<acceptance_criteria>
- `createDefaultProvider()` returns `OpenAIProvider` when `OPENAI_API_KEY` is set
- `createDefaultProvider()` returns `MockProvider` when `OPENAI_API_KEY` is unset + dev mode
- `createDefaultProvider()` throws with "OPENAI_API_KEY is not configured" message when `OPENAI_API_KEY` is unset + `NODE_ENV=production`
- No static imports from `openai` at module level in service.ts
- `npm run typecheck` passes
- `npm run build` passes
</acceptance_criteria>
</task>

<task id="3.2-01-05" type="execute" wave="3">
<action>
Verify that `src/app/api/campaign/generate/route.ts` requires no changes. The route already calls `createDefaultProvider()` and passes the result to `CampaignIntelligenceService` — this should continue working without modification.

Read the route file and confirm:
- It uses `createDefaultProvider()` (already does at line 35)
- It passes the provider to `new CampaignIntelligenceService(provider)` (already does at line 38)
- No provider-specific logic exists in the route

If everything is correct, no edits needed. If any hardcoded MockProvider references exist in the route, flag them.
</action>
<read_first>
- src/app/api/campaign/generate/route.ts
</read_first>
<acceptance_criteria>
- No changes made to `src/app/api/campaign/generate/route.ts`
- route.ts does not contain any hardcoded `MockProvider` references
</acceptance_criteria>
</task>

<task id="3.2-01-06" type="execute" wave="3">
<action>
Update the main project specs at `openspec/specs/ai-campaign-intelligence/spec.md`:

1. Update the "Provider selection with explicit fallback" requirement (around line 204) to reflect the new dev/prod-aware logic:
   - If `OPENAI_API_KEY` is set → use `OpenAIProvider`
   - If `OPENAI_API_KEY` is not set AND `NODE_ENV === "production"` → explicit configuration error
   - Otherwise → use `MockProvider`
2. Add a new "CampaignIntelligenceService integration unchanged" requirement confirming the service works with both providers without modification
3. Keep the `generation_metadata.provider` behavior: `"mock"` for MockProvider, `"openai"` for OpenAIProvider
</action>
<read_first>
- openspec/specs/ai-campaign-intelligence/spec.md
- openspec/changes/phase-3-2-ai-provider-integration/specs/ai-campaign-intelligence/spec.md (delta spec reference)
</read_first>
<acceptance_criteria>
- `openspec/specs/ai-campaign-intelligence/spec.md` has updated provider selection scenarios
- Spec includes dev-only fallback scenario
- Spec includes production fail-fast scenario
- Spec includes CampaignIntelligenceService integration requirement
</acceptance_criteria>
</task>

<!-- ─── Wave 4: Verification ──────────────────────────────── -->

<task id="3.2-01-07" type="execute" wave="4">
<action>
Run the full verification suite:

1. `npm run typecheck` — fix any type errors
2. `npm run lint` — fix any lint issues
3. `npm run build` — verify production build succeeds
</action>
<read_first>
- All modified files from prior waves
</read_first>
<acceptance_criteria>
- `npm run typecheck` exits 0
- `npm run lint` exits 0
- `npm run build` exits 0
</acceptance_criteria>
</task>

<task id="3.2-01-08" type="execute" wave="4">
<action>
Manual verification:

1. Verify `MockProvider` still works in development: temporarily unset `OPENAI_API_KEY` (or don't set it), call `createDefaultProvider()`, confirm it returns a `MockProvider` instance
2. Verify `OpenAIProvider` is selected when key is present: set a test `OPENAI_API_KEY`, call `createDefaultProvider()`, confirm it returns an `OpenAIProvider` instance
3. Verify production fail-fast: simulate `NODE_ENV=production` without `OPENAI_API_KEY`, confirm `createDefaultProvider()` throws
4. If possible with a real API key: POST to `/api/campaign/generate` with valid input, confirm 200 response with valid CampaignSpec JSON
</action>
<read_first>
- src/lib/campaign-intelligence/service.ts
</read_first>
<acceptance_criteria>
- MockProvider returned in dev mode without key
- OpenAIProvider returned when key is set
- Configuration error thrown in production mode without key
- API returns 200 with valid CampaignSpec JSON when real provider is active (if tested)
</acceptance_criteria>
</task>

</tasks>

<verification>
## Verification

1. All acceptance criteria above are met
2. `npm run typecheck`, `npm run lint`, `npm run build` all pass
3. Manual tests confirm provider selection works in all three modes (dev+key, dev-key, prod-key)
4. No changes to API route contract
5. Invalid AI output returns controlled error
</verification>

<success_criteria>
1. OpenAIProvider implements AIProvider without changing the public API route contract
2. MockProvider remains usable in development/test
3. Production does not silently fall back to MockProvider when OpenAI is expected
4. OpenAI response is validated with CampaignSpecSchema/Zod
5. Invalid AI output returns a controlled error
6. Product + Offer campaign is the primary manually tested and quality-reviewed path
</success_criteria>

<must_haves>
- Working OpenAIProvider with Structured Outputs
- Dev/prod-aware provider selection with safe fallback
- All verification checks passing
</must_haves>

---
phase: 04.6.1-text-only-state-visual-direction-inference
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260612000001_add_identity_state_fields.sql
  - src/lib/store.ts
  - src/lib/brand-assets/types.ts
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "Migration exists with all new columns on stores and store_brand_profiles"
    - "Migration can be applied idempotently against existing remote schema"
    - "Store interface includes identity_state, text_only_origin, manual_color_override, previous_identity_snapshot"
    - "BrandProfileRecord includes manual_color_override field"
    - "TextOnlyInferenceInput and TextOnlyInferenceResult types defined"
  artifacts:
    - path: "supabase/migrations/20260612000001_add_identity_state_fields.sql"
    - path: "src/lib/store.ts"
    - path: "src/lib/brand-assets/types.ts"
---

<objective>
**Plan 01: Foundation — Migration & Types**

Create the database migration and TypeScript type definitions that everything else in this phase depends on. The migration adds identity state columns to `stores` and `store_brand_profiles`. The type definitions update interfaces and create new types for the inference pipeline.

**Purpose:** Provide the schema foundation and type contracts so subsequent plans (service, routes, UI) can reference them.
**Output:** Migration SQL, updated `Store`, `BrandProfileRecord`, new `TextOnlyInferenceInput`/`TextOnlyInferenceResult`
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.6.1-text-only-state-visual-direction-inference/04.6.1-CONTEXT.md
@openspec/changes/fase-4-6-1-text-only-coverage/design.md
@openspec/changes/fase-4-6-1-text-only-coverage/specs/store-identity-state/spec.md
@openspec/changes/fase-4-6-1-text-only-coverage/specs/store-brand-profile/spec.md
@openspec/changes/fase-4-6-1-text-only-coverage/specs/text-only-brand-inference/spec.md

<existing_patterns>
See src/lib/store.ts for Store interface pattern
See src/lib/brand-assets/types.ts for BrandProfileRecord pattern
See supabase/migrations/ for existing migration patterns (IF NOT EXISTS, DROP CONSTRAINT IF EXISTS)
</existing_patterns>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration SQL for identity state fields</name>
  <files>
    <file>supabase/migrations/20260612000001_add_identity_state_fields.sql</file>
  </files>
  <read_first>
    @src/lib/store.ts — current Store interface to understand column names
    @supabase/migrations/ — existing migration files for naming conventions and patterns
    @04.6.1-CONTEXT.md — specific SQL in the Migration Plan section
  </read_first>
  <action>
    Create `supabase/migrations/20260612000001_add_identity_state_fields.sql`:

    **On `stores`:**
    - ADD COLUMN IF NOT EXISTS `identity_state` TEXT NOT NULL DEFAULT 'text_only'
    - ADD COLUMN IF NOT EXISTS `text_only_origin` TEXT NOT NULL DEFAULT 'implicit'
    - ADD COLUMN IF NOT EXISTS `manual_color_override` BOOLEAN NOT NULL DEFAULT false
    - ADD COLUMN IF NOT EXISTS `previous_identity_snapshot` JSONB
    - DROP CONSTRAINT IF EXISTS `chk_stores_identity_state`; ADD CONSTRAINT `chk_stores_identity_state` CHECK (identity_state IN ('text_only', 'logo', 'visual_signature'))
    - DROP CONSTRAINT IF EXISTS `chk_stores_text_only_origin`; ADD CONSTRAINT `chk_stores_text_only_origin` CHECK (text_only_origin IN ('implicit', 'explicit'))

    **On `store_brand_profiles`:**
    - ADD COLUMN IF NOT EXISTS `manual_color_override` JSONB NOT NULL DEFAULT '{"enabled": false}'
    - DROP CONSTRAINT IF EXISTS `chk_store_brand_profiles_source`; ADD CONSTRAINT `chk_store_brand_profiles_source` CHECK (source IN ('logo_analysis', 'without_logo', 'text_only'))

    All operations use IF NOT EXISTS / IF EXISTS for idempotency against remote schema where columns may already exist.
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | Select-String -NotMatch "node_modules"</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists at `supabase/migrations/20260612000001_add_identity_state_fields.sql`
    - File contains all ALTER TABLE statements for stores and store_brand_profiles
    - Every ADD COLUMN uses IF NOT EXISTS
    - Every DROP CONSTRAINT uses IF EXISTS
    - CHECK constraints match exact values specified in CONTEXT.md
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update Store interface with new identity fields</name>
  <files>
    <file>src/lib/store.ts</file>
  </files>
  <read_first>
    @src/lib/store.ts — current Store interface, understand field ordering conventions
  </read_first>
  <action>
    Add to the `Store` interface in `src/lib/store.ts`, after `logo_status: LogoStatus | null;`:
    - `identity_state: string | null;`
    - `text_only_origin: string | null;`
    - `manual_color_override: boolean;`
    - `previous_identity_snapshot: Record<string, unknown> | null;`

    Preserve all existing fields and exports. Do not reorder existing fields.
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | Select-String -NotMatch "node_modules"</automated>
  </verify>
  <acceptance_criteria>
    - Store interface includes `identity_state`, `text_only_origin`, `manual_color_override`, `previous_identity_snapshot`
    - Existing fields unchanged
    - All existing exports preserved
    - TypeScript compilation passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update BrandProfileRecord and add inference types</name>
  <files>
    <file>src/lib/brand-assets/types.ts</file>
  </files>
  <read_first>
    @src/lib/brand-assets/types.ts — current BrandProfileRecord, CreateBrandProfileInput structures
    @src/lib/brand-assets/brand-director.ts — BrandDirectorResult shape for reference
    @openspec/changes/fase-4-6-1-text-only-coverage/specs/text-only-brand-inference/spec.md — TextOnlyInferenceInput and TextOnlyInferenceResult shape
  </read_first>
  <action>
    **In `src/lib/brand-assets/types.ts`:**

    1. Add to `BrandProfileRecord` interface (after `version: number`):
       - `manual_color_override: Record<string, unknown>;`

    2. Add to `CreateBrandProfileInput` the same `manual_color_override` field with the same type.

    3. Add new type `TextOnlyInferenceInput`:
    ```typescript
    export interface TextOnlyInferenceInput {
      storeName: string;
      segment: string;
      subsegment: string | null;
      toneOfVoice: string | null;
      positioning: string | null;
      shortDescription: string | null;
      slogan: string | null;
      city: string | null;
      state: string | null;
      userPrimaryColor?: string;
      userAccentColor?: string;
    }
    ```

    4. Add new type `TextOnlyInferenceResult` matching `BrandProfilerWithoutLogoResult` shape:
    ```typescript
    export interface TextOnlyInferenceResult {
      safe_color_tokens: Record<string, string>;
      visual_style: string;
      visual_tone: string;
      typography_direction: string;
      brand_personality: string;
      campaign_guidelines: string;
      campaign_brief: string;
      inferred_primary_color: string;
      inferred_accent_color: string;
      confidence_score: number;
    }
    ```

    Place these after the existing `StoreDirectionFields` interface near the end of the file.
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | Select-String -NotMatch "node_modules"</automated>
  </verify>
  <acceptance_criteria>
    - `BrandProfileRecord` has `manual_color_override: Record<string, unknown>`
    - `CreateBrandProfileInput` has `manual_color_override`
    - `TextOnlyInferenceInput` interface defined with all specified fields
    - `TextOnlyInferenceResult` interface defined with all specified fields
    - All new types are exported
    - TypeScript compilation passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Database → App | New columns on stores and store_brand_profiles read by API and UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0461-01 | Tampering | Migration SQL | accept | All columns have CHECK constraints to prevent invalid values |
| T-0461-02 | Information Disclosure | Identity state fields | accept | No PII — identity_state/text_only_origin are enums |

</threat_model>

<verification>

### Build & Type Verification
```bash
npx tsc --noEmit --pretty
git diff --stat
```

### Manual Verification
1. Migration file created at correct path with all ALTER TABLE statements
2. Review Store interface has 4 new fields
3. Review types.ts has new fields on BrandProfileRecord + CreateBrandProfileInput + new inference types
4. TypeScript compilation passes with zero errors

</verification>

<success_criteria>
- [ ] Migration SQL created with all columns on stores and store_brand_profiles
- [ ] Store interface updated with 4 new identity fields
- [ ] BrandProfileRecord and CreateBrandProfileInput include manual_color_override
- [ ] TextOnlyInferenceInput and TextOnlyInferenceResult defined and exported
- [ ] TypeScript compilation passes with zero errors
</success_criteria>

<output>
Create `.planning/phases/04.6.1-text-only-state-visual-direction-inference/04.6.1-01-SUMMARY.md` when done
</output>

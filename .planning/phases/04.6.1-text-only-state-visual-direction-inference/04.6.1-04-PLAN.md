---
phase: 04.6.1-text-only-state-visual-direction-inference
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/lib/actions/store.ts
  - src/components/campaign/types.ts
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "resolveStoreIdentity has a new block for source='text_only' with updated color priority"
    - "Color resolution follows: safe_color_tokens.primary > inferred_primary_color > store.brand_color > SEGMENT_COLOR_FALLBACK[segment]"
    - "brand_colors_chosen is NOT in the rendering hierarchy"
    - "StoreIdentitySnapshot or relevant types reference new identity_state field"
  artifacts:
    - path: "src/lib/actions/store.ts"
    - path: "src/components/campaign/types.ts"
---

<objective>
**Plan 04: Creative Direction Context**

Update `resolveStoreIdentity` to handle the new `source = 'text_only'` profile type with the corrected color resolution priority. Also update any related types consumed by campaign generation to reference identity_state. This ensures campaigns generated for text_only stores use `safe_color_tokens.primary` as their brand color source.

**Purpose:** Connect the new text_only brand profiles to the campaign rendering pipeline.
**Output:** Updated `resolveStoreIdentity`, updated campaign types
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.6.1-text-only-state-visual-direction-inference/04.6.1-CONTEXT.md
@openspec/changes/fase-4-6-1-text-only-coverage/design.md
@openspec/changes/fase-4-6-1-text-only-coverage/specs/creative-direction-context/spec.md
@src/lib/actions/store.ts
@src/components/campaign/types.ts

<existing_patterns>
See src/lib/actions/store.ts for resolveStoreIdentity existing pattern (source checks, brandColor chain)
See src/lib/store.ts for getDefaultBrandColor and SEGMENT_COLOR_FALLBACK
</existing_patterns>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add text_only block to resolveStoreIdentity</name>
  <files>
    <file>src/lib/actions/store.ts</file>
  </files>
  <read_first>
    @src/lib/actions/store.ts — full resolveStoreIdentity function, understand existing source checks and color fallback chain
    @04.6.1-CONTEXT.md — D6 for exact priority chain and code structure
  </read_first>
  <action>
    Modify `resolveStoreIdentity` in `src/lib/actions/store.ts`:

    After the existing profile fetch block (where profiles are loaded for `without_logo` or other sources), add a new block:

    ```typescript
    // 4.6.1: source = 'text_only'
    if (profile?.source === 'text_only' && profile?.status === 'synced') {
      if (profile.safe_color_tokens?.primary && /^#[0-9A-Fa-f]{6}$/.test(profile.safe_color_tokens.primary)) {
        brandColor = profile.safe_color_tokens.primary;
      } else if (profile.inferred_primary_color && /^#[0-9A-Fa-f]{6}$/.test(profile.inferred_primary_color)) {
        brandColor = profile.inferred_primary_color;
      }
      // Use safe_color_tokens as the final palette
      if (profile.safe_color_tokens) {
        brandProfile = { ...brandProfile, ...profile.safe_color_tokens };
      }
    }
    ```

    This new block SHALL be evaluated after the existing `without_logo` block (which is maintained for backward compatibility with a TODO for 4.6.3). The priority chain SHALL be: `safe_color_tokens.primary > inferred_primary_color > store.brand_color > SEGMENT_COLOR_FALLBACK[segment]`.

    `brand_colors_chosen` SHALL NOT be part of the rendering hierarchy.

    Also ensure the query/fetch for brand profiles includes `source = 'text_only'` in its filter. Currently it may only look for `source = 'without_logo'` — extend the query to also find `source = 'text_only'` profiles.
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | Select-String -NotMatch "node_modules"</automated>
  </verify>
  <acceptance_criteria>
    - resolveStoreIdentity has a block checking profile.source === 'text_only' && status === 'synced'
    - brandColor resolved from safe_color_tokens.primary first, then inferred_primary_color
    - brand_colors_chosen is NOT in the color resolution chain
    - Profile query includes source = 'text_only' in its filter
    - Existing without_logo block unchanged
    - TypeScript compilation passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update campaign types to reference identity_state if needed</name>
  <files>
    <file>src/components/campaign/types.ts</file>
  </files>
  <read_first>
    @src/components/campaign/types.ts — StoreIdentitySnapshot interface, check if identity_state or text_only fields are relevant
  </read_first>
  <action>
    Inspect `StoreIdentitySnapshot` and related types in `src/components/campaign/types.ts`.
    If `identity_state` or `text_only_origin` need to be exposed in the campaign context (for potential campaign rendering decisions), add them. If the type already has sufficient context through `brandColor` and `brandProfile`, skip modifications.
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | Select-String -NotMatch "node_modules"</automated>
  </verify>
  <acceptance_criteria>
    - StoreIdentitySnapshot (or equivalent) has the fields needed by the campaign pipeline
    - If no new fields needed: no changes made, but intentional decision documented
    - TypeScript compilation passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → Campaign renderer | Color and identity data flows from store/brand profile to renderer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0461-07 | Tampering | Color resolution chain | mitigate | Validate hex colors via regex before use in rendering |

</threat_model>

<verification>

### Build & Type Verification
```bash
npx tsc --noEmit --pretty
git diff --stat
```

### Manual Verification
1. resolveStoreIdentity has new source='text_only' block
2. Color priority matches spec: safe_color_tokens > inferred > store.brand_color > segment fallback
3. Existing without_logo block is preserved
4. TypeScript compilation passes

</verification>

<success_criteria>
- [ ] resolveStoreIdentity handles source='text_only' with safe_color_tokens priority
- [ ] brand_colors_chosen excluded from rendering chain
- [ ] Profile query extended to include source='text_only'
- [ ] Campaign types updated if needed
- [ ] TypeScript compilation passes
</success_criteria>

<output>
Create `.planning/phases/04.6.1-text-only-state-visual-direction-inference/04.6.1-04-SUMMARY.md` when done
</output>

---
status: diagnosed
phase: 04.6.1-text-only-state-visual-direction-inference
source: 04.6.1-05-SUMMARY.md, 04.6.1-04-SUMMARY.md, 04.6.1-03-SUMMARY.md
started: 2026-06-12T18:25:02Z
updated: 2026-06-12T18:50:00Z
---

## Current Test

[testing complete]

## Tests

### 1. "Continuar sem logo" link visível para loja nova
expected: Ao criar uma loja nova e acessar Step 2 (Logo e Cores), o link "Continuar sem logo" aparece abaixo dos botões "Enviar logotipo" e "Não tenho logo".
result: pass

### 2. Clique em "Continuar sem logo" dispara inferência
expected: Clicar no link mostra spinner "Gerando direção visual..." e chama POST /infer. Ao finalizar, as cores da loja são atualizadas com o resultado da IA.
result: pass

### 3. Estado de sucesso text_only
expected: Após inferência bem-sucedida, mostra chip "Direção visual definida pelo Vendeo" + chips de paleta de cores.
result: pass

### 4. Estado de falha text_only
expected: Se a IA falhar, mostra mensagem de aviso + botão "Gerar direção visual agora" para retentar.
result: pass

### 5. Inferência implícita ao avançar Step 2
expected: Ao clicar "Avançar" no Step 2 sem logo e sem assinatura visual, a inferência text_only é disparada automaticamente.
result: issue
reported: "não dispara"
severity: major

### 6. Preview expandido para text_only
expected: Na store-preview, lojas com text_only mostram seção expandida com paleta, estilo, tom e personalidade.
result: pass

### 7. Resolução de cores em campanhas
expected: Campaign renderer usa safe_color_tokens.primary > inferred_primary_color > store.brand_color > fallback para lojas text_only.
result: skipped
reason: Fora do escopo da fase 4.6.1 — integração com campanhas é assunto de outra fase

### 8. Non-blocking: falha não bloqueia fluxo
expected: Mesmo com falha da IA, usuário consegue prosseguir e gerar campanha (usa brand_color / fallback).
result: skipped
reason: Fora do escopo da fase 4.6.1 — integração com campanhas é assunto de outra fase

## Summary

total: 8
passed: 5
issues: 2
skipped: 2
pending: 0
blocked: 0

## Gaps

- truth: "Inferência implícita dispara ao clicar Avançar sem logo/assinatura"
  status: failed
  reason: "User reported: não dispara"
  severity: major
  test: 5
  root_cause: "Condição `!identityState` falha para lojas novas porque migration seta DEFAULT 'text_only', então identityState nunca é null. Correção: usar `logoStatus === null` que é o estado real de 'nenhuma decisão tomada'."
  artifacts:
    - path: "src/components/flow/store-identity-form.tsx"
      issue: "handleStep2Submit condicional usa !identityState ao invés de logoStatus === null"
      fix: "Trocar `!identityState` por `logoStatus === null` na linha 468"
  missing:
    - "handleStep2Submit condição principal"
  debug_session: ""

- truth: "Após F5, UI restaura estado text_only (chips, cores, preview expandido)"
  status: failed
  reason: "User reported: após F5, botões de enviar logo/gerar VS não aparecem, chips não aparecem, cor de destaca perdida, preview mostra apenas inicial/nome/segmento/cor primária"
  severity: major
  test: 2
  root_cause: "inferredProfile nunca é carregado do banco na montagem do componente. useEffect inicial carrega brand-profile mas não seta inferredProfile. Só era setado durante o fluxo de inferência (handleContinueWithoutLogo / handleStep2Submit)."
  artifacts:
    - path: "src/components/flow/store-identity-form.tsx"
      issue: "Profile loading não seta inferredProfile nem restaura accentColor de safe_color_tokens"
      fix: "Adicionar setInferredProfile no bloco de load do profile (source === 'text_only' && status === 'synced'); restaurar accentColor de inferred_accent_color / safe_color_tokens.accent como fallback"
  missing:
    - "setInferredProfile no load inicial do perfil"
    - "Restauração de accentColor de safe_color_tokens"
  debug_session: ""

- truth: "Estado de loja anterior não vaza ao criar nova loja"
  status: failed
  reason: "User reported: após criar nova loja e ir para Step 2, mostra cores da loja anterior"
  severity: major
  test: 1
  root_cause: "clearStore no hook useStoreForm não limpa estado local do componente (accentColor, inferredProfile, etc). handleStep1Submit -> save() cria nova loja -> setStep(2) renderiza Step 2 com dados velhos antes do useEffect limpar."
  artifacts:
    - path: "src/components/flow/store-identity-form.tsx"
      issue: "clearStore não reseta estado local; handleClearStore adicionado para limpar estado do componente junto"
      fix: "Criar handleClearStore que chama clearStore() + reseta accentColor, brandColorsChosen, logoStatus, inferredProfile, identityState, etc."
  missing:
    - "handleClearStore wrapper de clearStore"
  debug_session: ""

- truth: "Botões de upgrade (enviar logo, criar VS) visíveis mesmo após text_only"
  status: failed
  reason: "User reported: após F5 em loja text_only, botões de enviar logo e gerar assinatura não aparecem"
  severity: minor
  test: 1
  root_cause: "Bloco logoStatus === 'explicit_none' escondia upgrade options quando inferredProfile existia. Correção: mostrar botões de upgrade (Enviar logotipo, Criar assinatura visual) mesmo quando inferredProfile está setado."
  artifacts:
    - path: "src/components/flow/store-identity-form.tsx"
      issue: "Bloco explicit_none foi alterado para mostrar contextualmente: upgrade options quando inferredProfile existe, mensagem vazia + criar VS quando não existe"
      fix: "Adicionar bloco condicional no explicit_none: com inferredProfile mostra botões de upgrade, sem mantém texto original"
  missing:
    - "Botões de upgrade no estado text_only"
  debug_session: ""

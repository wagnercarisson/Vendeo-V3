---
phase: 04.6.3-logo-state-lifecycle
plan: 03
subsystem: api
tags: api, delete, history, restore, drift, brand-profile

requires:
  - phase: 04.6.3-logo-state-lifecycle
    plan: 01
    provides: IDENTITY_TO_LOGO_STATUS, types (LogoHistoryItem, LogoRestoreRequest, LogoRestoreResponse, DriftStatus)
  - phase: 04.6.3-logo-state-lifecycle
    plan: 02
    provides: BrandDirector execution pattern
provides:
  - DELETE /logo soft delete preserving profile and provenance
  - GET /logo/history endpoint with drift_status computation
  - POST /logo/restore endpoint with drift validation and dual-path restore
affects: ui

tech-stack:
  added: []
  patterns:
    - Drift validation: 6-field comparison with normalizeSnapshotValue
    - Restore dual-path: no-drift (reactivates) vs drift (BrandDirector + new profile)

key-files:
  created:
    - src/app/api/store/[id]/logo/history/route.ts
    - src/app/api/store/[id]/logo/restore/route.ts
  modified:
    - src/app/api/store/[id]/logo/route.ts

key-decisions:
  - "DELETE /logo: assets archived, profile stays synced, active_logo_asset_id preserved (D06)"
  - "GET /logo/history: LEFT JOIN profiles via active_logo_asset_id FK, drift computed per item"
  - "POST /logo/restore no-drift: reactivates assets+profile; already-synced profile NOT marked outdated"
  - "POST /logo/restore drift: BrandDirector + new profile; historical profile NOT reactivated"
  - "BrandDirector failure on drift: fallback preserved, assets STILL reactivated (logo visible)"

patterns-established:
  - "History/drift: query archived assets → join profiles via FK → compute 6-field drift → return annotated items"

requirements-completed: []

duration: 20min
completed: 2026-06-15
---

# Phase 04.6.3 Plan 03: DELETE + History + Restore Summary

**DELETE /logo soft delete preserving profile and provenance, GET /logo/history with drift validation, POST /logo/restore with dual-path (no-drift reactivate / drift realignment via BrandDirector)**

## Performance

- **Duration:** 20 min
- **Started:** 2026-06-15T18:30:00Z
- **Completed:** 2026-06-15T18:50:00Z
- **Tasks:** 3
- **Files modified:** 3 (1 modified, 2 created)

## Accomplishments
- DELETE /logo: assets archived, profile stays synced, active_logo_asset_id preserved, identity_state='text_only', logo_status='explicit_none'
- GET /logo/history: returns archived original assets with associated profiles via FK, drift_status computed per item (6-field comparison)
- POST /logo/restore: archive current assets, compare input_snapshot vs store — no-drift path reactivates, drift path BrandDirector
- BrandDirector failure on drift: fallback preserved, profile failed with attempt_snapshot, assets still reactivated

## Task Commits

1. **Task 1: DELETE /logo soft delete** - `6fd1f33` (feat)
2. **Task 2: GET /logo/history route** - `6fd1f33` (feat)
3. **Task 3: POST /logo/restore route** - `6fd1f33` (feat)

**Plan metadata:** Included in same commit.

## Files Created/Modified
- `src/app/api/store/[id]/logo/route.ts` — Refactored DELETE handler with soft-delete semantics
- `src/app/api/store/[id]/logo/history/route.ts` — NEW: GET handler returning LogoHistoryItem[]
- `src/app/api/store/[id]/logo/restore/route.ts` — NEW: POST handler with drift validation and dual-path restore

## Decisions Made
- Profile sync status per accent_color resolution: brand_colors_chosen[1] → safe_color_tokens.accent → inferred_accent_color → null (consistent with drift.ts existing pattern)
- No-drift restore when profile already synced (post-remove): NOT marked outdated — just re-activate assets
- Historical profile on drift: NOT reactivated (per D08 design decision)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

All three API endpoints ready. Next: Plan 04 — UI Step 2 states + restore modal.

---

*Phase: 04.6.3-logo-state-lifecycle*
*Completed: 2026-06-15*

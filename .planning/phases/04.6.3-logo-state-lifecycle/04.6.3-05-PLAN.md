---
phase: 04.6.3-logo-state-lifecycle
plan: 05
type: execute
wave: 4
depends_on: [04]
files_modified: []
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "All 8 task groups from OpenSpec tasks.md verified through manual and automated tests"
    - "POST /logo: BrandDirector runs before profile mutation, identity_state='logo'"
    - "POST /logo with BrandDirector failure: profile remains synced, failed profile with attempt_snapshot"
    - "DELETE /logo: profile stays synced, active_logo_asset_id preserved, identity_state='text_only'"
    - "GET /logo/history: drift_status computed per item"
    - "POST /logo/restore without drift: assets re-activated, profile re-activated"
    - "POST /logo/restore with drift: BrandDirector executed, new profile created"
    - "POST /logo/restore when profile already synced (post-remove): not marked outdated"
    - "UI flow: logo active → remove → re-upload → history → restore → correct visual states per UX matrix"
    - "npm run lint and npm run typecheck and npm run build pass with zero errors"
  artifacts:
    - path: "(verification)"
      provides: "Manual test log and automated test results"
  key_links: []
---

<objective>
**Plan 05: Verification — E2E Testing & Quality Gate**

Execute comprehensive verification of all 4 prior plans: manual testing of all API endpoints and UI flows, automated type/lint/build checks, and documentation of test results.

**Purpose:** Ensure the entire logo state lifecycle works correctly end-to-end before closing the phase.
**Output:** Test results log, all verification criteria met
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.6.3-logo-state-lifecycle/04.6.3-CONTEXT.md
@openspec/changes/fase-4-6-3-logo-state-lifecycle/tasks.md
@openspec/changes/fase-4-6-3-logo-state-lifecycle/design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated build checks</name>
  <files>
    <file>(no files)</file>
  </files>
  <read_first>
    @package.json — available scripts for typecheck, lint, build
  </read_first>
  <action>
    Run the following commands and verify zero errors:
    ```bash
    npx tsc --noEmit --pretty 2>&1 | Select-String -NotMatch "node_modules"
    npm run lint 2>&1
    npm run build 2>&1
    ```
    If any errors, fix them and re-run until clean.
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty; npm run lint; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - TypeScript compilation passes with zero errors
    - Lint passes with zero errors
    - Build passes with zero errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Manual test — POST /logo flow</name>
  <files>
    <file>(no files)</file>
  </files>
  <read_first>
    @src/app/api/store/[id]/logo/route.ts — refactored POST handler
  </read_first>
  <action>
    Manual test steps:
    1. Upload a logo image via POST /api/store/[id]/logo with a valid file
    2. Verify: BrandDirector was called BEFORE profile mutation (check server logs or add temporary logging)
    3. Verify: previous synced profile marked as 'outdated'
    4. Verify: new profile inserted with status='synced', active_logo_asset_id set, metadata.input_snapshot populated (6 fields)
    5. Verify: store.identity_state = 'logo', store.logo_status = 'uploaded'
    6. Verify: brand_colors_chosen NOT populated from logo_colors_detected
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty</automated>
  </verify>
  <acceptance_criteria>
    - POST /logo creates synced profile with active_logo_asset_id and input_snapshot
    - BrandDirector executes before profile mutation
    - identity_state='logo', logo_status='uploaded'
    - brand_colors_chosen isolated (not from detected colors)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Manual test — POST /logo with BrandDirector failure</name>
  <files>
    <file>(no files)</file>
  </files>
  <read_first>
    @src/app/api/store/[id]/logo/route.ts — failure path in POST handler
  </read_first>
  <action>
    Manual test steps:
    1. Simulate BrandDirector failure (e.g., use a corrupted logo file or mock failure)
    2. Verify: previous synced profile remains 'synced' (fallback preserved)
    3. Verify: new profile inserted with status='failed', active_logo_asset_id set
    4. Verify: metadata.attempt_snapshot populated (6 fields for audit)
    5. Verify: metadata.input_snapshot NOT populated in failed profile
    6. Verify: store.identity_state = 'logo', store.logo_status = 'uploaded' (upload still happened)
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty</automated>
  </verify>
  <acceptance_criteria>
    - Previous profile stays synced after BrandDirector failure
    - Failed profile created with attempt_snapshot, NOT input_snapshot
    - identity_state='logo' (upload succeeded even if analysis failed)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Manual test — DELETE /logo</name>
  <files>
    <file>(no files)</file>
  </files>
  <read_first>
    @src/app/api/store/[id]/logo/route.ts — DELETE handler
  </read_first>
  <action>
    Manual test steps:
    1. Ensure a store has an active logo (profile synced, assets active)
    2. Call DELETE /api/store/[id]/logo
    3. Verify: assets archived (status='archived')
    4. Verify: brand profile status still 'synced' (not archived)
    5. Verify: active_logo_asset_id still set on profile (not nulled)
    6. Verify: store.identity_state = 'text_only'
    7. Verify: store.logo_status = 'explicit_none'
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty</automated>
  </verify>
  <acceptance_criteria>
    - Assets archived, profile stays synced
    - active_logo_asset_id preserved
    - identity_state='text_only', logo_status='explicit_none'
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Manual test — GET /logo/history</name>
  <files>
    <file>(no files)</file>
  </files>
  <read_first>
    @src/app/api/store/[id]/logo/history/route.ts — GET handler
  </read_first>
  <action>
    Manual test steps:
    1. After performing delete (test 4), call GET /api/store/[id]/logo/history
    2. Verify: returns array of LogoHistoryItem objects
    3. Verify: each item has asset, profile (or null), drift_status, input_snapshot
    4. Verify: drift_status computed correctly ('none' when store data matches snapshot, 'drift' when changed)
    5. Verify: ordered by version DESC (most recent first)
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty</automated>
  </verify>
  <acceptance_criteria>
    - Returns LogoHistoryItem[] with asset + profile pairs
    - drift_status computed correctly per item
    - Ordered by version DESC
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 6: Manual test — POST /logo/restore without drift</name>
  <files>
    <file>(no files)</file>
  </files>
  <read_first>
    @src/app/api/store/[id]/logo/restore/route.ts — no-drift path
  </read_first>
  <action>
    Manual test steps:
    1. Call POST /api/store/[id]/logo/restore with an asset_id from history (ensure store data hasn't changed)
    2. Verify: assets reactivated (status='active')
    3. Verify: profile for that version reactivated to 'synced' (if it was outdated)
    4. Verify: if profile was already 'synced' (post-remove), NOT marked outdated — just assets reactivated
    5. Verify: store.identity_state = 'logo', store.logo_status = 'uploaded'
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty</automated>
  </verify>
  <acceptance_criteria>
    - Assets reactivated
    - Profile reactivated (or not marked outdated if already synced)
    - identity_state='logo'
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 7: Manual test — POST /logo/restore with drift</name>
  <files>
    <file>(no files)</file>
  </files>
  <read_first>
    @src/app/api/store/[id]/logo/restore/route.ts — drift path
  </read_first>
  <action>
    Manual test steps:
    1. Change store data (e.g., update name or brand_color) to create drift
    2. Call POST /api/store/[id]/logo/restore with an asset_id from history
    3. Verify: drift detected (drift_detected: true in response)
    4. Verify: BrandDirector executed with restored logo + current store data
    5. Verify: current synced profile marked as 'outdated'
    6. Verify: NEW profile created as 'synced' (historical profile NOT reactivated)
    7. Verify: assets reactivated
    8. Verify: store.identity_state = 'logo'
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty</automated>
  </verify>
  <acceptance_criteria>
    - Drift detected and BrandDirector executed
    - New profile created (historical not reactivated)
    - Assets reactivated
    - identity_state='logo'
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 8: Manual test — UI flow</name>
  <files>
    <file>(no files)</file>
  </files>
  <read_first>
    @src/components/flow/store-identity-block.tsx — updated component
    @src/components/flow/store-identity-form.tsx — updated component
    @src/components/flow/logo-restore-modal.tsx — new component
  </read_first>
  <action>
    Manual test the complete UI flow:
    1. Start with a store in text_only state (no logo)
    2. Verify: drop zone visible, "Enviar logotipo" and "Criar assinatura visual" buttons visible
    3. Upload a logo → verify: preview appears, "Remover logotipo" only, other controls hidden
    4. Click "Remover logotipo" → verify: post-remove state (drop zone, upload/assinatura buttons, "Logotipos anteriores" link)
    5. Click "Logotipos anteriores" → verify: modal opens with version cards
    6. Select a version and click "Restaurar" → verify: modal closes, UI updates to logo active state
    7. Verify: all 4 visual states render correctly per UX decision matrix
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty</automated>
  </verify>
  <acceptance_criteria>
    - UI transitions correctly through all 4 visual states
    - Remove button works and transitions to post-remove state
    - History modal opens and shows version cards with drift badges
    - Restore button closes modal and transitions back to logo active state
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — verification plan, no new trust boundaries introduced.

## STRIDE Threat Register

No new threats — verification only.

</threat_model>

<verification>

### Automated Verification
```bash
npx tsc --noEmit --pretty
npm run lint
npm run build
```

### Manual Test Log
Document results of each test case:

| Test | Description | Result | Notes |
|------|-------------|--------|-------|
| 1 | Build checks | | |
| 2 | POST /logo (success) | | |
| 3 | POST /logo (BD failure) | | |
| 4 | DELETE /logo | | |
| 5 | GET /logo/history | | |
| 6 | POST /logo/restore (no drift) | | |
| 7 | POST /logo/restore (drift) | | |
| 8 | UI flow | | |

</verification>

<success_criteria>
- [ ] All automated checks pass: TypeScript, lint, build
- [ ] POST /logo success path verified (BrandDirector before mutation, compensated transition, input_snapshot, identity_state sync)
- [ ] POST /logo BrandDirector failure path verified (fallback preserved, attempt_snapshot, identity_state='logo')
- [ ] DELETE /logo verified (assets archived, profile synced, active_logo_asset_id preserved, identity_state='text_only')
- [ ] GET /logo/history verified (LogoHistoryItem[], drift_status computed)
- [ ] POST /logo/restore no-drift verified (assets + profile reactivated, not outdated if synced)
- [ ] POST /logo/restore drift verified (BrandDirector, new profile, assets reactivated)
- [ ] UI flow verified (all 4 states, remove, history modal, restore)
- [ ] brand_colors_chosen isolation confirmed (NOT populated from logo_colors_detected)
</success_criteria>

<output>
Create `.planning/phases/04.6.3-logo-state-lifecycle/04.6.3-05-SUMMARY.md` when done
</output>

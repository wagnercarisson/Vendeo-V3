---
id: 04.6.3.1-02-PLAN
wave: 2
type: execute
depends_on:
  - 04.6.3.1-01-PLAN
autonomous: true
requirements:
  - CLEANUP-ENDPOINTS-01
  - CLEANUP-COMPONENT-01
  - CLEANUP-TYPES-01
  - CLEANUP-STATE-01
---

# Plan 02: Remove Dead Endpoints, Component, Types, State

## Objective

Remover todo o código morto de restore de logotipo: 3 endpoints, 1 componente, 4 types obsoletos, e state morto no formulário.

## Context

O restore de logo foi descontinuado (excluir = irreversível). O código nunca foi removido da base. Esta plan elimina ~650 linhas de código morto que não tem mais callers nem funcionalidade.

Leia CONTEXT.md e os delta specs de cleanup antes de remover.

## Tasks

### Task 01: Remover endpoints de restore/history/versions

<read_first>
- src/app/api/store/[id]/logo/restore/route.ts
- src/app/api/store/[id]/logo/history/route.ts
- src/app/api/store/[id]/logo/versions/route.ts
- openspec/changes/fase-4-6-3-1-logo-restore-scope-cleanup/proposal.md
</read_first>

<action>
Remover os 3 arquivos de rota:

1. Remover `src/app/api/store/[id]/logo/restore/route.ts` (~313 linhas)
2. Remover `src/app/api/store/[id]/logo/history/route.ts` (~92 linhas)
3. Remover `src/app/api/store/[id]/logo/versions/route.ts` (~31 linhas)

Não há migração necessária — os dados em `store_brand_assets` permanecem intactos. Nenhum caller depende destes endpoints após a refatoração do frontend no Plan 01.
</action>

<acceptance_criteria>
- `src/app/api/store/[id]/logo/restore/route.ts` não existe mais
- `src/app/api/store/[id]/logo/history/route.ts` não existe mais
- `src/app/api/store/[id]/logo/versions/route.ts` não existe mais
- Nenhum import quebrado (typecheck passa)
</acceptance_criteria>

---

### Task 02: Remover componente LogoRestoreModal

<read_first>
- src/components/flow/logo-restore-modal.tsx
- src/components/flow/store-identity-form.tsx — verificar imports e referências
- openspec/changes/fase-4-6-3-1-logo-restore-scope-cleanup/specs/store-identity-ui/spec.md
</read_first>

<action>
Remover o componente e suas referências:

1. Remover `src/components/flow/logo-restore-modal.tsx` (~211 linhas)
2. Remover import do `LogoRestoreModal` em `store-identity-form.tsx`
3. Remover qualquer state de controle de visibilidade do modal (`restoreModalOpen`, etc.)
</action>

<acceptance_criteria>
- `src/components/flow/logo-restore-modal.tsx` não existe mais
- Nenhum import de `logo-restore-modal` remanescente em `src/`
- `npm run typecheck` passa
</acceptance_criteria>

---

### Task 03: Remover types obsoletos

<read_first>
- src/lib/brand-assets/types.ts
- openspec/changes/fase-4-6-3-1-logo-restore-scope-cleanup/proposal.md — "Remove types" section
</read_first>

<action>
Remover os seguintes types de `src/lib/brand-assets/types.ts`:

1. `LogoHistoryItem` — tipo do histórico de logos
2. `LogoRestoreRequest` — tipo do request de restore
3. `LogoRestoreResponse` — tipo do response de restore
4. `DriftStatus` — tipo de drift obsoleto (não confundir com `DriftLevel` ou similar usado em drift detection)
</action>

<acceptance_criteria>
- `LogoHistoryItem` não existe em `src/lib/brand-assets/types.ts`
- `LogoRestoreRequest` não existe em `src/lib/brand-assets/types.ts`
- `LogoRestoreResponse` não existe em `src/lib/brand-assets/types.ts`
- `DriftStatus` não existe em `src/lib/brand-assets/types.ts`
- Nenhum import quebrado (typecheck passa)
</acceptance_criteria>

---

### Task 04: Remover state morto do formulário

<read_first>
- src/components/flow/store-identity-form.tsx
- openspec/changes/fase-4-6-3-1-logo-restore-scope-cleanup/specs/store-identity-ui/spec.md — seção de modificações
</read_first>

<action>
Remover do `store-identity-form.tsx`:

1. State `archivedCount` e `archivedCountLoading`
2. Função `fetchArchivedCount`
3. `useEffect` que chama `fetchArchivedCount` (se existente)
4. Qualquer referência a "Logotipos anteriores" no JSX que dependia de `archivedCount`
</action>

<acceptance_criteria>
- `archivedCount`, `archivedCountLoading` não existem no state do formulário
- `fetchArchivedCount` não existe no componente
- Nenhum `useEffect` associado a `fetchArchivedCount`
- "Logotipos anteriores" não aparece no JSX
- `npm run typecheck` passa
</acceptance_criteria>

## Verification Criteria

- [ ] Nenhuma rota de restore/history/versions remanescente
- [ ] Nenhum import de LogoRestoreModal
- [ ] Types obsoletos removidos sem quebrar typecheck
- [ ] archivedCount e fetchArchivedCount removidos
- [ ] `npm run typecheck` passa
- [ ] `npm run lint` passa

## must_haves

- 3 endpoints removidos
- 1 componente removido
- 4 types removidos
- State morto do formulário removido
- Build e types intactos

---
id: 04.6.3.1-04-PLAN
wave: 3
type: execute
depends_on:
  - 04.6.3.1-02-PLAN
  - 04.6.3.1-03-PLAN
autonomous: true
requirements:
  - SPEC-UPDATE-01
  - SPEC-UPDATE-02
  - SPEC-UPDATE-03
  - SPEC-UPDATE-04
  - SPEC-UPDATE-05
  - SPEC-UPDATE-06
  - VERIFY-ALL-01
---

# Plan 04: Spec Updates + Verification

## Objective

Atualizar as specs canônicas do OpenSpec para refletir a remoção do restore de logo e a adição do retry. Remover a spec canônica `logo-restore`. Executar verificação final de grep, typecheck, lint, test, build, e testes manuais.

## Context

Seis specs precisam de atualização. A spec `logo-restore` é removida porque o comportamento que descrevia não existe mais. As demais specs recebem deltas para remover referências a restore de logo e documentar o retry.

## Tasks

### Task 01: Remover spec canônica logo-restore

<read_first>
- openspec/specs/logo-restore/spec.md
</read_first>

<action>
Remover o arquivo `openspec/specs/logo-restore/spec.md` (~240 linhas).

Esta spec descrevia o comportamento de restore de logotipo (POST /logo/restore, GET /logo/history, validação de drift, reativação de assets). Todo este comportamento foi removido.
</action>

<acceptance_criteria>
- `openspec/specs/logo-restore/spec.md` não existe mais
</acceptance_criteria>

---

### Task 02: Atualizar logo-upload spec

<read_first>
- openspec/changes/fase-4-6-3-1-logo-restore-scope-cleanup/specs/logo-upload/spec.md — delta spec
- openspec/specs/logo-upload/spec.md — spec canônica
</read_first>

<action>
Aplicar o delta em `openspec/specs/logo-upload/spec.md`:

1. Remover referência a `GET /logo/versions`
2. Adicionar documentação do retry endpoint como parte do lifecycle de logo
3. Descrever o cenário "Retry available after failed analysis"
</action>

<acceptance_criteria>
- `/logo/versions` não aparece na spec canônica
- Retry endpoint documentado
</acceptance_criteria>

---

### Task 03: Atualizar store-identity-ui spec

<read_first>
- openspec/changes/fase-4-6-3-1-logo-restore-scope-cleanup/specs/store-identity-ui/spec.md — delta spec
- openspec/specs/store-identity-ui/spec.md — spec canônica
</read_first>

<action>
Aplicar o delta em `openspec/specs/store-identity-ui/spec.md`:

1. Remover requisito de "Restore modal"
2. Remover requisito de "Restore button triggers POST /logo/restore"
3. Renomear "Visual signature history modal (separate from logo restore)" para remover o parêntese
4. Atualizar matriz de decisão UX (remover coluna "Logotipos anteriores"; adicionar linha `logo` com perfil `failed`)
5. Atualizar "Remove logo button behavior" — remover passo de mostrar "Logotipos anteriores"
6. Atualizar "Logo area — logo state with analysis failed" — adicionar "Tentar novamente" button
7. Atualizar "Logo area — after remove" — remover "Logotipos anteriores"
</action>

<acceptance_criteria>
- Nenhuma referência a restore modal ou restore button na spec
- VS history modal renomeado sem parêntese
- Matriz UX reflete o estado pós-cleanup (6 linhas, sem coluna "Logotipos anteriores")
- "Tentar novamente" documentado no estado failed
</acceptance_criteria>

---

### Task 04: Atualizar store-identity-state spec

<read_first>
- openspec/changes/fase-4-6-3-1-logo-restore-scope-cleanup/specs/store-identity-state/spec.md — delta spec
- openspec/specs/store-identity-state/spec.md — spec canônica
</read_first>

<action>
Aplicar o delta em `openspec/specs/store-identity-state/spec.md`:

1. Remover referência a logo restore em "Profile reconciliation on restore"
2. Remover "or restored" da descrição do target profile na tabela de compatibilidade
3. Garantir que cenários de restore mencionam apenas Visual Signature
</action>

<acceptance_criteria>
- Nenhuma referência a logo restore nos cenários de reconciliação
- Tabela de compatibilidade reflete apenas VS approve/restore e logo upload
</acceptance_criteria>

---

### Task 05: Atualizar identity-state-transitions spec

<read_first>
- openspec/changes/fase-4-6-3-1-logo-restore-scope-cleanup/specs/identity-state-transitions/spec.md — delta spec
- openspec/specs/identity-state-transitions/spec.md — spec canônica
</read_first>

<action>
Aplicar o delta em `openspec/specs/identity-state-transitions/spec.md`:

1. Substituir "preserving it for future restore" por "fallback visual direction"
2. Esclarecer que profile permanece `synced` na remoção, não `outdated`
3. `outdated` só acontece quando novo profile substituto é inserido
</action>

<acceptance_criteria>
- "future restore" não aparece na spec
- "fallback visual direction" usado como rationale de preservação
- Cenário de logo removal documenta profile permanece synced
</acceptance_criteria>

---

### Task 06: Atualizar store-brand-profile spec

<read_first>
- openspec/changes/fase-4-6-3-1-logo-restore-scope-cleanup/specs/store-brand-profile/spec.md — delta spec
- openspec/specs/store-brand-profile/spec.md — spec canônica
</read_first>

<action>
Aplicar o delta em `openspec/specs/store-brand-profile/spec.md`:

1. Remover a seção "Regenerate brand profile — POST /api/store/[id]/brand-profile/generate"
2. Substituir por nota de que o comportamento de retry é suprido pelo endpoint `logo-retry`
</action>

<acceptance_criteria>
- `/brand-profile/generate` não aparece na spec canônica
- Nota sobre substituição pelo logo-retry endpoint presente
</acceptance_criteria>

---

### Task 07: Verificação — grep de referências residuais

<read_first>
- (nenhum — comando rg)
</read_first>

<action>
Rodar `rg` em `src/` e `openspec/specs/` para confirmar que não restam referências a:

- `/logo/restore`
- `/logo/history`
- `/logo/versions`
- `LogoRestoreRequest`
- `LogoRestoreResponse`
- `LogoHistoryItem`
- `logo-restore-modal`
- `LogoRestoreModal`

Se alguma referência for encontrada e for código legítimo (não apenas comentário histórico), o executor deve avaliar se requer remoção adicional.
</action>

<acceptance_criteria>
- Nenhuma referência a restore de logo em `src/` (fora de comentários históricos intencionais)
- Nenhuma referência a restore de logo em `openspec/specs/`
</acceptance_criteria>

---

### Task 08: Rodar cadeia de verificação

<read_first>
- (são comandos — verificar scripts no package.json)
</read_first>

<action>
Executar sequencialmente:

1. `npm run typecheck` — sem erros
2. `npm run lint` — sem erros
3. `npm test` — todos os testes passam (incluindo os novos do Plan 03)
4. `npm run build` — build passa

Se algum falhar, corrigir antes de prosseguir.
</action>

<acceptance_criteria>
- `npm run typecheck` exit 0
- `npm run lint` exit 0
- `npm test` exit 0
- `npm run build` exit 0
</acceptance_criteria>

---

### Task 09: Testes manuais

<read_first>
- (implementação completa)
</read_first>

<action>
Executar testes manuais:

1. Upload → BrandDirector falha → retry → análise bem-sucedida
2. Upload → remover → sem opção de restaurar
3. Upload → remover → re-upload → novo asset independente
</action>

<acceptance_criteria>
- Teste 1: fluxo completo de upload→falha→retry→sucesso funciona
- Teste 2: após remover, não há botão/opção de restaurar logo
- Teste 3: re-upload cria asset independente, perfil anterior fica outdated
</acceptance_criteria>

## Verification Criteria

- [ ] Spec canônica logo-restore removida
- [ ] 5 specs canônicas atualizadas com deltas
- [ ] grep confirma zero referências residuais
- [ ] `npm run typecheck` passa
- [ ] `npm run lint` passa
- [ ] `npm test` passa
- [ ] `npm run build` passa
- [ ] 3 testes manuais executados e verificados

## must_haves

- Specs canônicas consistentes com o novo estado (sem restore de logo)
- Todas as verificações automáticas passando
- Testes manuais confirmam comportamento esperado

---
phase: 04.6.4-visual-signature-lifecycle
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts
  - src/app/api/store/[id]/visual-signature/approve/route.ts
  - src/app/api/store/[id]/visual-signature/route.ts
  - src/lib/services/store-identity-art-director.ts
  - src/lib/visual-signature/types.ts
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "store-identity-art-director.ts stops returning hardcoded artDirectorOutput — consumes real JSON from AI response.output.message"
    - "Generate handler persists JSON from AI response (content_used, visual_direction, intended_palette) in metadata.artDirectorOutput"
    - "Generate handler captures input_snapshot (10 store fields) at generation time in metadata.input_snapshot"
    - "Retry (simplified prompt) infers content_used conservatively — all fields as true"
    - "Approve handler updates stores: identity_state='visual_signature', logo_status='generated'"
    - "Approve handler calls reconcileProfiles() from Plan 01 after activation"
    - "GET response includes approved_at (null if archived/never-active), art_direction, and restore_eligibility computed via shared DriftValidator from Plan 01"
    - "DELETE handler added to visual-signature/route.ts — archives active signature, transitions identity_state to text_only, preserves profile as synced fallback"
  artifacts:
    - path: "src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts"
      provides: "Modified generate handler with metadata enrichment"
    - path: "src/app/api/store/[id]/visual-signature/approve/route.ts"
      provides: "Modified approve handler with identity_state sync + reconcileProfiles() call"
    - path: "src/app/api/store/[id]/visual-signature/route.ts"
      provides: "Evolved GET handler with history response + added DELETE handler (uses DriftValidator for restore_eligibility, reconcileProfiles for archive)"
    - path: "src/lib/services/store-identity-art-director.ts"
      provides: "Modified service — real JSON extraction replacing hardcoded artDirectorOutput"
  key_links:
    - from: "generate-without-logo/route.ts"
      to: "src/lib/visual-signature/types.ts"
      via: "VisualSignatureMetadata.input_snapshot and artDirectorOutput"
    - from: "generate-without-logo/route.ts"
      to: "src/lib/services/store-identity-art-director.ts"
      via: "StoreIdentityArtDirectorService.generate() — must now return parsed JSON instead of hardcoded"
    - from: "approve/route.ts"
      to: "src/lib/brand-assets/profile-reconciliation.ts"
      via: "reconcileProfiles() after activation"
    - from: "visual-signature/route.ts (GET)"
      to: "src/lib/visual-signature/types.ts"
      via: "RestoreEligibility in response"
    - from: "visual-signature/route.ts (GET)"
      to: "src/lib/visual-signature/drift-validator.ts"
      via: "validateDrift() for restore_eligibility computation"
---

<objective>
**Plan 02: Core Backend — Generate Metadata, Approve Sync, GET History**

Implement the core backend changes: persist `content_used` + `input_snapshot` on generation, fix approve to set `identity_state='visual_signature'`, and evolve the GET endpoint to serve as history with `approved_at`, `art_direction`, and `restore_eligibility`.

**Purpose:** Complete the data contract (generation persists what it used), fix the hardcoded artDirectorOutput in the service layer, and fix the approval state sync that broke UI consistency.
**Output:** Modified `generate-without-logo/route.ts`, `approve/route.ts`, `visual-signature/route.ts` (GET), modified `store-identity-art-director.ts`
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.6.4-visual-signature-lifecycle/04.6.4-CONTEXT.md
@openspec/changes/fase-4-6-4-visual-signature-lifecycle/design.md
@openspec/changes/fase-4-6-4-visual-signature-lifecycle/tasks.md
@openspec/changes/fase-4-6-4-visual-signature-lifecycle/specs/visual-signature-approval/spec.md
@openspec/changes/fase-4-6-4-visual-signature-lifecycle/specs/store-visual-signature/spec.md
@src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts
@src/app/api/store/[id]/visual-signature/approve/route.ts
@src/app/api/store/[id]/visual-signature/route.ts
@src/lib/visual-signature/types.ts
@docs/alinhamento-fase-4.6.4.md

<existing_patterns>
See src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts for current generate flow (attempt 1 with full prompt, retry with simplified)
See src/app/api/store/[id]/visual-signature/approve/route.ts for current approve flow (missing identity_state sync)
See src/app/api/store/[id]/visual-signature/route.ts for current GET handler (simple list without history fields)
See identity-art-director.ts for the hardcoded artDirectorOutput that must be replaced with real AI response parsing
</existing_patterns>
</context>

<tasks>

<task type="execute">
<read_first>
src/lib/services/store-identity-art-director.ts
openspec/changes/fase-4-6-4-visual-signature-lifecycle/design.md (D1)
docs/alinhamento-fase-4.6.4.md (Descoberta: O Fluxo Real de Geração)
</read_first>
<action>
Modify `store-identity-art-director.ts` (or `identity-art-director.ts` if that's where the hardcoded data lives):

1. Locate where `artDirectorOutput` is currently hardcoded (set to static test values)
2. Replace with extraction logic that parses the JSON from `response.output.message` returned by the AI
3. The JSON structure expected from the AI prompt `store-identity-art-director.md`:
   ```json
   { "visual_direction": "...", "content_used": { "store_name": true, "city": false, "state": false, "slogan": true }, "visual_elements": [...], "intended_palette": {...}, "color_usage": {...} }
   ```
4. Return the parsed `ArtDirectorOutput` object for the generate handler to persist in metadata
5. If JSON parsing fails (malformed response), return null — generate handler will infer conservatively
</action>
<acceptance_criteria>
- store-identity-art-director.ts no longer returns hardcoded artDirectorOutput
- Service parses real JSON from response.output.message
- Failed JSON parsing returns null (graceful degradation)
- `npm run typecheck` passes with no errors
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts
src/lib/services/store-identity-art-director.ts
src/lib/visual-signature/types.ts
openspec/changes/fase-4-6-4-visual-signature-lifecycle/design.md (D1)
</read_first>
<action>
In the generate handler, after successful `AiImageGenerator.generate()`:

1. Consume the structured JSON returned by `StoreIdentityArtDirectorService.generate()` (now returns real data from Plan 02 task 1)
2. Persist in `signature.metadata.artDirectorOutput` as `{ visual_direction, content_used, visual_elements, intended_palette, color_usage }`
3. Capture `input_snapshot` from store data (SELECT current values of: name, segment, subsegment, tone_of_voice, positioning, short_description, slogan, city, state, brand_color) and persist in `metadata.input_snapshot`
4. On retry (attempt 2 — simplified prompt without JSON): set `content_used` with all available fields as `true`
</action>
<acceptance_criteria>
- First generation stores `metadata.artDirectorOutput` with real content_used from AI response
- First generation stores `metadata.input_snapshot` with all 10 store fields
- Retry stores `metadata.artDirectorOutput.content_used` with all fields = true
- Pre-feature signatures (no metadata) have null for both fields
- `npm run typecheck` passes with no errors
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/app/api/store/[id]/visual-signature/approve/route.ts
openspec/changes/fase-4-6-4-visual-signature-lifecycle/design.md (D2)
</read_first>
<action>
In the approve handler, before returning success:

1. Add `await supabaseAdmin.from('stores').update({ identity_state: 'visual_signature', logo_status: 'generated' }).eq('id', storeId)`
2. Ensure the order is: load signature → archive previous active (if any) → set chosen as active → **update identity_state** → reconcile profiles
3. Call `reconcileProfiles()` from Plan 01 with `markIncompatibleAsOutdated=true, activateProfileIds=[targetProfileId]`
</action>
<acceptance_criteria>
- After approve, `stores.identity_state = 'visual_signature'`
- After approve, `stores.logo_status = 'generated'`
- identity_state update happens in correct order (after activation, before response)
- reconcileProfiles called after identity_state update
- `npm run typecheck` passes with no errors
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/app/api/store/[id]/visual-signature/route.ts
src/lib/visual-signature/types.ts
src/lib/visual-signature/drift-validator.ts
openspec/changes/fase-4-6-4-visual-signature-lifecycle/specs/store-visual-signature/spec.md
</read_first>
<action>
Evolve GET handler to serve as history/list endpoint:

1. Structure response as `{ signatures: [{ id, status, assetUrl, type, attempt, created_at, approved_at, art_direction, restore_eligibility }] }`
2. `approved_at`: use `updated_at` of currently active signature (null for archived/never-active)
3. `art_direction`: extract from `metadata.artDirectorOutput`, or null if unavailable
4. `restore_eligibility`: use the shared `validateDrift()` from Plan 01's drift-validator.ts:
   - Call `validateDrift({ input_snapshot: sig.metadata.input_snapshot, content_used: sig.metadata.artDirectorOutput?.content_used, currentStoreData })`
   - Map result to `{ can_restore, drift_fields, requires_regeneration, reason }`
</action>
<acceptance_criteria>
- GET returns `approved_at` as timestamp for active signature, null for archived
- GET returns `art_direction` object from metadata, null for pre-feature signatures
- GET returns `restore_eligibility` computed via shared DriftValidator (not inline logic)
- Missing metadata results in `reason: 'missing_metadata'`
- `npm run typecheck` passes with no errors
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/app/api/store/[id]/visual-signature/route.ts
src/lib/constants.ts
src/lib/brand-assets/profile-reconciliation.ts
openspec/changes/fase-4-6-4-visual-signature-lifecycle/specs/visual-signature-remove/spec.md
</read_first>
<action>
Add DELETE handler to `src/app/api/store/[id]/visual-signature/route.ts` — this is the file already modified above for GET:

1. Validate store exists and has active visual signature (`store_visual_signatures.status = 'active'`)
2. If no active signature: return 404 `{ error: 'Nenhuma assinatura visual ativa para remover.' }`
3. Set active signature status to `'archived'`
4. Update stores: `identity_state = 'text_only'`, `logo_status = 'explicit_none'` (via IDENTITY_TO_LOGO_STATUS), preserve `visual_signature_attempts`
5. Call `reconcileProfiles()` with `preserveCurrentAsFallback=true` — profile remains synced as fallback
6. Return `{ success: true, previous_identity_state: 'visual_signature' }`

No brand_profiles marked outdated by this operation. No brand_assets affected.
</action>
<acceptance_criteria>
- DELETE archives active signature (status → 'archived')
- DELETE sets `identity_state = 'text_only'` and `logo_status = 'explicit_none'`
- DELETE preserves `visual_signature_attempts` (no reset)
- DELETE keeps brand profile as `synced` (fallback)
- DELETE returns 404 if no active signature with Portuguese error message
- No brand profiles marked outdated by this operation
- `npm run typecheck` passes with no errors
</acceptance_criteria>
</task>

</tasks>

<quality_gate>
- store-identity-art-director.ts no longer returns hardcoded artDirectorOutput
- Generate stores complete metadata for drift validation on future restore
- Approve correctly syncs identity_state + calls reconcileProfiles (fixes UI Step 2 + profile consistency)
- GET history uses shared DriftValidator (same logic reused by Plan 03 restore)
- No duplicate inline drift logic — all consumers use drift-validator.ts
</quality_gate>

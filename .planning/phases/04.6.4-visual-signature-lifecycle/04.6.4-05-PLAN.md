---
phase: 04.6.4-visual-signature-lifecycle
plan: 05
type: execute
wave: 4
depends_on: [04]
files_modified:
  - tests (verify scenarios)
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "npm run typecheck passes with zero errors"
    - "npm run lint passes with zero errors"
    - "Approve scenario: POST /approve sets stores.identity_state='visual_signature' and logo_status='generated'"
    - "Remove scenario: DELETE /visual-signature (Plan 02) archives signature, identity_state='text_only', profile preserved as synced"
    - "Restore no-drift scenario: POST /restore reactivates signature, identity_state='visual_signature', profiles reconciled"
    - "Restore drift scenario: POST /restore returns blocked with { requires_regeneration: true, reason: 'critical_drift' }"
    - "Restore missing-metadata scenario: POST /restore returns blocked with { reason: 'missing_metadata' }"
    - "Logo upload blocked with VS active: POST /logo returns 409 with requires_identity_removal: true"
    - "Logo restore blocked in logo state: POST /logo/restore returns 409 with requires_logo_removal: true"
    - "Logo restore blocked in VS state: POST /logo/restore returns 409 with requires_identity_removal: true"
    - "UI Step 2: identity_state='visual_signature' shows preview + Alterar/Remover, hides drop zone/upload"
    - "UI Step 2: identity_state='text_only' shows drop zone + upload/assinar, shows 'Assinaturas anteriores' if archived"
    - "restore_eligibility in GET: no drift → reason='ok'; drift → reason='critical_drift'; no metadata → reason='missing_metadata'"
    - "RejectionContext propagated from feedback → review → generate (verify in modal)"
  artifacts:
    - path: "tests/... (manual verification scenarios)"
      provides: "Validated scenarios for all API and UI changes"
---

<objective>
**Plan 05: Verification & Quality Gate**

Validate all changes across the Visual Signature Lifecycle phase through automated checks (typecheck, lint) and manual verification of API and UI scenarios. Ensure all edge cases are covered: approve sync, remove archive, restore with/without drift, logo gates, UI states, rejection propagation.

**Purpose:** Confirm the phase delivers all specified capabilities without regressions.
**Output:** Verification evidence for all 14 task groups and 92 tasks from openspec tasks.md
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.6.4-visual-signature-lifecycle/04.6.4-CONTEXT.md
@openspec/changes/fase-4-6-4-visual-signature-lifecycle/tasks.md
@openspec/changes/fase-4-6-4-visual-signature-lifecycle/design.md
@src/lib/visual-signature/types.ts
@src/lib/brand-assets/profile-reconciliation.ts
@src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts
@src/app/api/store/[id]/visual-signature/approve/route.ts
@src/app/api/store/[id]/visual-signature/route.ts
@src/app/api/store/[id]/visual-signature/restore/route.ts
@src/app/api/store/[id]/logo/route.ts
@src/app/api/store/[id]/logo/restore/route.ts
@src/components/flow/VisualSignatureApprovalModal.tsx
@src/components/flow/store-identity-block.tsx
@src/components/flow/store-visual-signature-section.tsx
@src/components/flow/visual-signature-history-modal.tsx

<existing_patterns>
See .planning/phases/04.6.3-logo-state-lifecycle/04.6.3-05-PLAN.md for the previous verification plan structure
</existing_patterns>
</context>

<tasks>

<task type="execute">
<read_first>
src/lib/visual-signature/types.ts
src/lib/brand-assets/profile-reconciliation.ts
</read_first>
<action>
Run automated checks:
1. `npm run typecheck` — fix any type errors
2. `npm run lint` — fix any lint errors

Then verify types:
- VisualSignatureMetadata has input_snapshot (10 fields) and artDirectorOutput (content_used, visual_direction, visual_elements, intended_palette, color_usage)
- RestoreEligibility type exists with correct structure
- reconcileProfiles exported with correct signature
</action>
<acceptance_criteria>
- `npm run typecheck` exits 0
- `npm run lint` exits 0
- Types match design.md specification
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/services/store-identity-art-director.ts
src/lib/visual-signature/drift-validator.ts
src/app/api/store/[id]/visual-signature/approve/route.ts
src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts
src/app/api/store/[id]/visual-signature/route.ts
src/app/api/store/[id]/visual-signature/restore/route.ts
src/app/api/store/[id]/logo/route.ts
src/app/api/store/[id]/logo/restore/route.ts
</read_first>
<action>
Manually verify each API scenario:

 1. StoreIdentityArtDirectorService: no longer returns hardcoded artDirectorOutput — parses JSON from AI response
 2. DriftValidator: shared function returns correct reason for ok / critical_drift / missing_metadata cases
 3. Approve: POST /approve → stores.identity_state='visual_signature', logo_status='generated', reconcileProfiles called
 4. Generate metadata: first attempt stores artDirectorOutput + input_snapshot in metadata
 5. Generate retry: simplified prompt stores content_used with all fields=true
 6. Generate pre-feature: signatures without metadata have null art_direction
 7. Remove (Plan 02): DELETE → signature archived, identity_state='text_only', profile synced, attempts preserved
 8. Remove no-active (Plan 02): DELETE → 404 with Portuguese error message
 9. Restore no-drift: POST → signature active, identity_state='visual_signature', profiles reconciled
 10. Restore with drift: POST → blocked, `{ success: false, drift: { critical: true, fields, requires_regeneration: true, reason: 'critical_drift' } }`
 11. Restore without profile: POST → BrandProfilerWithoutLogoService called, profile created
 12. Restore missing-metadata: POST → blocked, reason='missing_metadata'
 13. Restore logo-in-way: POST (identity_state='logo') → 409 requires_logo_removal
 14. Logo upload blocked: POST /logo (identity_state='visual_signature') → 409 requires_identity_removal
 15. Logo upload calls reconcileProfiles on success
 16. Logo restore blocked (logo state): POST /logo/restore → 409 requires_logo_removal
 17. Logo restore blocked (VS state): POST /logo/restore → 409 requires_identity_removal
 18. GET history: active has approved_at; archived has null approved_at
 19. GET restore_eligibility: ok / critical_drift / missing_metadata cases (uses shared DriftValidator)
</action>
<acceptance_criteria>
- All 19 API scenarios verified manually
- Each returns expected status code and response body
- Portuguese error messages used for user-facing errors
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/components/flow/store-identity-block.tsx
src/components/flow/store-visual-signature-section.tsx
src/components/flow/VisualSignatureApprovalModal.tsx
src/components/flow/visual-signature-history-modal.tsx
</read_first>
<action>
Manually verify each UI scenario:

1. identity_state='visual_signature': preview visible, Alterar/Remover visible, drop zone hidden, "Não tenho logo" hidden, "Continuar sem logo" hidden
2. identity_state='text_only': drop zone visible, "Assinaturas anteriores" link visible (if archived exist), color pickers visible
3. identity_state='logo': logo preview visible, Remover visible, drop zone hidden
4. Color changes permitted with VS active — no drift warning or regeneration
5. Remover button → DELETE → UI transitions to text_only state
6. Alterar button → generate-without-logo → open approval modal → approve swaps signatures
7. Alterar → close without approve → original remains active
8. History modal: title, cards with thumbnail/date/direction/palette, restore button state per eligibility
9. Restore button enabled when reason='ok', disabled with tooltip when 'critical_drift' or 'missing_metadata'
10. RejectionContext: provide feedback → navigate to review → "Gerar nova versão" passes rejectionContext
11. RejectionContext: reject without feedback → "Gerar nova versão" uses "sem feedback específico" default
</action>
<acceptance_criteria>
- All 11 UI scenarios verified manually
- Visual states match the UX decision matrix from design.md
- All buttons trigger correct API calls
- Error states handled with dismissible banners
- Rejection context flows end-to-end
</acceptance_criteria>
</task>

</tasks>

<quality_gate>
- All 14 task groups from openspec tasks.md covered
- All 92 individual tasks verified
- TypeScript strict mode passes with zero errors
- Lint passes with zero errors
- Manual verification covers all critical paths
- No regressions in existing logo/VS functionality
</quality_gate>

---
status: complete
phase: 04.6.4-visual-signature-lifecycle
source: 05-SUMMARY.md
started: 2026-06-19T22:30:00.000Z
updated: 2026-06-19T23:45:00.000Z
---

## Current Test

[testing complete]

## Tests

### 1. Modal badges — Ativa/Sincronizada/Precisa realinhar
expected: Badges corretos + botão "Manter" na VS ativa, "Aprovar" nas demais
result: pass

### 2. Feedback antes de gerar nova versão
expected: "Nenhuma agradou, gerar nova versão" abre textarea de feedback, não vai direto pra geração. Feedback passado como rejectionContext.
result: pass

### 3. Remover/Voltar condicional
expected: Com VS ativa → "Remover assinatura". Sem VS ativa → "Voltar". "Continuar sem logo" não aparece.
result: pass

### 4. Botão "Gerenciar assinatura visual"
expected: Step 2 mostra "Gerenciar assinatura visual" (não "Criar assinatura visual")
result: pass

### 5. Cor de destaque consistente após F5
expected: Accent color carregada após F5 é a mesma exibida após aprovação (inferred_accent_color)
result: pass

### 6. Fluxo completo: gerar → feedback → nova versão → aprovar
expected: Geração com feedback → diretor usa feedback → nova VS gerada → aprovar → identidade atualizada
result: pass

### 7. Fluxo completo: remover VS → text_only
expected: Remover → DELETE → identity_state='text_only', direção visual preservada, drop zone reaparece
result: pass

### 8. Logo upload bloqueado com VS ativa
expected: POST /logo retorna 409 com requires_identity_removal
result: pass

### 9. Logo restore bloqueado com VS ativa
expected: POST /logo/restore retorna 409 com requires_identity_removal
result: pass

### 10. VS restore com drift
expected: POST /restore com dados alterados (nome, segmento) → bloqueado com critical_drift + alerta visual
result: pass
note: Fixes applied: (a) validateDrift adicionado no approve/route.ts para archived signatures, (b) content_used preservado no retry path do generate-without-logo, (c) hasActiveSignatureDrift + hasCriticalDrift no frontend. Usuário confirmou fluxo funcional: alterar segmento → drift → gerar nova VS → aprovar → sem falso drift.

### 11. Falso drift após primeira aprovação de VS
expected: Após aprovar primeira VS, Step 2 não mostra mensagem de drift falsa
result: pass
note: Root cause: input_snapshot.brand_color salvava store.brand_color original (vazio), mas handleApprovalComplete atualizava formData.brand_color para inferredPrimaryColor, causando mismatch. Fix: approve/route.ts agora sobrescreve brand_color no input_snapshot com o inferredPrimaryColor sanitizado em ambos os branches (perfil reusado e novo). Usuário re-validou e confirmou que drift some após "Manter VS ativa".

### 12. Drift lifecycle: campo crítico → gerar → aprovar → sem drift
expected: Alterar segmento → drift critical → "Ajustar" → nova VS gerada → aprovar → Step 2 atualizado sem drift
result: pass
note: Usuário confirmou: "aqui tudo correto - só falhou na primeira versão".

## Summary

total: 12
passed: 12
issues: 0
pending: 0
skipped: 0

## Gaps

- truth: "VS restore com dados alterados (nome, segmento) bloqueia com critical_drift e alerta visual"
  status: resolved
  reason: "User reported: Alterei segmento e aprovei VS desatualizada = aplicou normal. Depois alterei nome e apliquei outra VS desalinhada = aplicou normal. Sem aviso de drift."
  severity: blocker
  test: 10
  root_cause: "validateDrift was not being called in the approve endpoint — the archived signature branch only existed for later versions; initial implementation was missing entirely. Retry path in generate-without-logo lost content_used metadata."
  artifacts:
    - path: "src/app/api/store/[id]/visual-signature/approve/route.ts"
      issue: "archived signature branch missing validateDrift call"
    - path: "src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts"
      issue: "retry path not returning content_used in metadata"
  missing:
    - "validateDrift validation in approve endpoint for archived signatures"
    - "content_used propagation in retry path"
  debug_session: ".planning/debug/vs-approve-drift.md"

- truth: "Primeira aprovação de VS não deve mostrar falso drift no Step 2"
  status: resolved
  reason: "User reported: após aprovar a VS na primeira versão, mensagem de drift apareceu"
  severity: major
  test: 11
  root_cause: "input_snapshot.brand_color capturava store.brand_color original (vazio), mas handleApprovalComplete atualizava formData.brand_color para inferredPrimaryColor. O BrandProfiler já sincronizava o store com a nova cor, mas o snapshot era montado ANTES dessa sincronização."
  artifacts:
    - path: "src/app/api/store/[id]/visual-signature/approve/route.ts"
      issue: "input_snapshot brand_color usava valor pré-sincronização do store"
  missing:
    - "Sobrescrever brand_color no input_snapshot com inferredPrimaryColor"
  debug_session: ".planning/debug/false-drift-first-approval.md"

---
phase: 04.6.5-vs-color-drift-brand-profile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/visual-signature/types.ts
  - src/lib/brand-assets/types.ts
  - src/lib/brand-assets/color-probe.ts
  - src/lib/brand-assets/brand-director.ts
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "IntendedPalette, ResolvedPalette, ColorUsage, ColorValidationEntry, ColorValidationResolved, ColorValidationFailed, VisionAdjudicationAudit, VisionFailureReason, NormalizedVisionAdjudication, RawVisionAdjudication, RawVisionCorrections, SupportCorrection types defined in src/lib/visual-signature/types.ts"
    - "BrandProfilerInput expanded with intendedPalette: IntendedPalette | null and previousBrandColors?: string[]"
    - "BrandProfilerWithoutLogoResult.safe_color_tokens typed as ResolvedPalette instead of Record<string, string>"
    - "color-probe.ts created at src/lib/brand-assets/color-probe.ts — exports probeColors, ColorProbeResult, ColorCluster, deltaE, hexToLab, rgbToHex, findClosestProbeCluster, isLightNeutral, constants"
    - "ColorCluster has edgeRatio: number field"
    - "brand-director.ts imports from color-probe.ts — no local definitions remain for extracted functions"
    - "curateLogoColors and applyGuardrail are UNMODIFIED"
  artifacts:
    - path: "src/lib/visual-signature/types.ts"
      provides: "All new type definitions for palette resolution, color validation, vision adjudication"
    - path: "src/lib/brand-assets/types.ts"
      provides: "ColorCluster edgeRatio field"
    - path: "src/lib/brand-assets/color-probe.ts"
      provides: "Shared color probe module — consumed by brand-director and brand-profiler"
    - path: "src/lib/brand-assets/brand-director.ts"
      provides: "Updated imports from color-probe.ts, no logic changes"
  key_links:
    - from: "src/lib/visual-signature/types.ts"
      to: "src/lib/brand-assets/color-probe.ts"
      via: "ColorCluster type shared between modules"
    - from: "src/lib/visual-signature/types.ts"
      to: "src/lib/brand-assets/types.ts"
      via: "BrandProfileRecord consumes ColorValidation types"
    - from: "src/lib/visual-signature/types.ts"
      to: "src/lib/visual-signature/brand-profiler.ts"
      via: "BrandProfilerInput, IntendedPalette, ResolvedPalette used by profiler"
---

<objective>
**Plan 01: Foundation — Types & ColorProbe Extraction**

Create the foundational data types for the entire VS Color Drift phase and mechanically extract `probeColors()` from `brand-director.ts` into a shared module. This plan covers new interfaces and types (IntendedPalette, ResolvedPalette, ColorValidation discriminated union, VisionAdjudication types, expanded BrandProfilerInput) plus the mechanical extraction of ColorProbe.

**Purpose:** Establish types that all downstream plans depend on + shared ColorProbe utility.
**Output:** Modified `src/lib/visual-signature/types.ts`, `src/lib/brand-assets/types.ts`, new `src/lib/brand-assets/color-probe.ts`, modified `src/lib/brand-assets/brand-director.ts` (imports only).
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.6.5-vs-color-drift-brand-profile/04.6.5-CONTEXT.md
@openspec/changes/fase-4-6-5-vs-color-drift-brand-profile/design.md
@openspec/changes/fase-4-6-5-vs-color-drift-brand-profile/tasks.md
@src/lib/visual-signature/types.ts
@src/lib/brand-assets/types.ts
@src/lib/brand-assets/brand-director.ts

<existing_patterns>
See src/lib/visual-signature/types.ts for current type definitions
See src/lib/brand-assets/brand-director.ts for existing probeColors implementation (source of extraction)
See src/lib/brand-assets/types.ts for BrandProfileRecord types
</existing_patterns>
</context>

<tasks>

<task type="execute">
<read_first>
src/lib/visual-signature/types.ts
openspec/changes/fase-4-6-5-vs-color-drift-brand-profile/specs/store-brand-profile/spec.md
openspec/changes/fase-4-6-5-vs-color-drift-brand-profile/specs/store-brand-profiler-without-logo/spec.md
openspec/changes/fase-4-6-5-vs-color-drift-brand-profile/specs/store-identity-art-director/spec.md
</read_first>
<action>
Add the following types to `src/lib/visual-signature/types.ts`:

1. **IntendedPalette** — `interface` with `primary`, `accent`, `background` (strings) and `support` (string[])
2. **ResolvedPalette** — `interface` with `primary`, `secondary`, `accent`, `background` (strings)
3. **ColorUsage** — `interface` with `primary`, `accent`, `support`, `background` (strings)
4. **ColorValidationEntry** — `interface` with: `intended: string | null`, `resolved: string`, `presence: 'confirmed' | 'ambiguous' | 'not_confirmed' | 'unchecked'`, `delta_e: number | null`, `role_source: 'art_director' | 'vision_adjudication' | 'heuristic'`, `resolution: 'accepted' | 'accepted_unverified' | 'corrected_by_vision' | 'selected_by_heuristic'`, `resolved_from_cluster?: { hex: string; classification: string; frequency: number; delta_e: number } | null`, `note?: string`
5. **VisionFailureReason** — union type: `'api_error' | 'invalid_json' | 'no_choice' | 'hex_outside_observed_colors'`
6. **VisionAdjudicationAudit** — discriminated union: `{ status: 'success'; reason: string; prompt_suffix: string } | { status: 'failed'; reason: VisionFailureReason; details?: string; attemptedAt: string }`
7. **ColorValidationResolved** — `interface` with `global_status: 'all_confirmed' | 'vision_adjudicated' | 'probe_unavailable' | 'fallback_heuristic'`, `primary, accent, secondary, background: ColorValidationEntry`, `support_colors: string[]`, `support_details?: ColorValidationEntry[]`, `vision_adjudication?: VisionAdjudicationAudit`
8. **ColorValidationFailed** — `interface` with `global_status: 'vision_failed'`, `vision_adjudication: VisionAdjudicationAudit`
9. **ColorValidation** — type alias: `ColorValidationResolved | ColorValidationFailed`
10. **SupportCorrection** — `interface` with `index: number`, `color: string`
11. **RawVisionCorrections** — `interface` with `primary: string | null`, `accent: string | null`, `background: string | null`, `support: SupportCorrection[]`
12. **RawVisionAdjudication** — `interface` with `corrections: RawVisionCorrections`, `reason: string`
13. **NormalizedVisionAdjudication** — `interface` with `palette: IntendedPalette`, `reason: string`

After adding types, update existing:
- Change `VisualSignatureMetadataArtDirectorOutput.intended_palette` from `Record<string, unknown>` to `IntendedPalette`
- Change `VisualSignatureMetadataArtDirectorOutput.color_usage` from `Record<string, unknown>` to `ColorUsage`
- Add `intendedPalette: IntendedPalette | null` and `previousBrandColors?: string[]` to `BrandProfilerInput`
- Change `BrandProfilerWithoutLogoResult.safe_color_tokens` from `Record<string, string>` to `ResolvedPalette`
</action>
<acceptance_criteria>
- All 13 new types/interfaces defined and exported from `src/lib/visual-signature/types.ts`
- `IntendedPalette` used in `VisualSignatureMetadataArtDirectorOutput` instead of `Record<string, unknown>`
- `ColorUsage` used in `VisualSignatureMetadataArtDirectorOutput` instead of `Record<string, unknown>`
- `BrandProfilerInput` has `intendedPalette: IntendedPalette | null` and `previousBrandColors?: string[]`
- `BrandProfilerWithoutLogoResult.safe_color_tokens` typed as `ResolvedPalette`
- `npm run typecheck` passes with no errors
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/brand-assets/types.ts
openspec/changes/fase-4-6-5-vs-color-drift-brand-profile/specs/color-probe/spec.md
</read_first>
<action>
Add `edgeRatio: number` field to the `ColorCluster` interface in `src/lib/brand-assets/types.ts`.

This exposes already-calculated data from the classification — no logic changes.
</action>
<acceptance_criteria>
- `ColorCluster` interface in `src/lib/brand-assets/types.ts` has `edgeRatio: number`
- `npm run typecheck` passes with no errors
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/brand-assets/brand-director.ts
openspec/changes/fase-4-6-5-vs-color-drift-brand-profile/specs/color-probe/spec.md
</read_first>
<action>
Create `src/lib/brand-assets/color-probe.ts` by mechanically extracting from `brand-director.ts`:

Extract these with EXACT original signatures (no logic changes):
- `probeColors(buffer: Buffer): Promise<ColorProbeResult>`
- `ColorProbeResult` interface
- `ColorCluster` interface (already in types.ts — import from there)
- `deltaE(lab1, lab2): number` (CIE76)
- `hexToLab(hex): [number, number, number]`
- `rgbToHex(r, g, b): string`
- `findClosestProbeCluster(hex, clusters): { cluster: ColorCluster | null; deltaE: number }`
- `isLightNeutral(hex): boolean`
- All constants: `STRONG_MATCH_DELTA_E = 12`, `ACCEPTABLE_MATCH_DELTA_E = 18`, `LOOSE_MATCH_DELTA_E = 25`

Note: `ColorCluster` is imported from `src/lib/brand-assets/types.ts`. The `edgeRatio` field (already calculated in brand-director's classification) must be included in `toCluster()` calls.

All imports and internal references must be preserved. No logic changes in any extracted function.
</action>
<acceptance_criteria>
- `src/lib/brand-assets/color-probe.ts` exports all specified symbols
- Function signatures match original `brand-director.ts` exactly
- ColorCluster imported from `src/lib/brand-assets/types.ts` — edgeRatio field preserved
- `npm run typecheck` passes with no errors
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/brand-assets/brand-director.ts
src/lib/brand-assets/color-probe.ts
</read_first>
<action>
Update `src/lib/brand-assets/brand-director.ts`:
1. Remove all local definitions of extracted functions (probeColors, ColorProbeResult, deltaE, hexToLab, rgbToHex, findClosestProbeCluster, isLightNeutral, constants)
2. Add imports from `./color-probe` for all extracted symbols
3. Keep `curateLogoColors` and `applyGuardrail` COMPLETELY UNCHANGED
</action>
<acceptance_criteria>
- `brand-director.ts` imports all extracted symbols from `color-probe.ts`
- No duplicate definitions (local + import) for any extracted symbol
- `curateLogoColors` and `applyGuardrail` are UNMODIFIED
- `npm run typecheck` passes with no errors
</acceptance_criteria>
</task>

</tasks>

<quality_gate>
- All 13 new types defined and exported — IntendedPalette, ResolvedPalette, ColorUsage, ColorValidationEntry, ColorValidationResolved, ColorValidationFailed, ColorValidation, VisionAdjudicationAudit, VisionFailureReason, SupportCorrection, RawVisionCorrections, RawVisionAdjudication, NormalizedVisionAdjudication
- ColorProbe extraction is purely mechanical — no logic changes in extracted functions
- brand-director.ts compiles with imports from color-probe.ts — curateLogoColors and applyGuardrail unchanged
- BrandProfilerInput expanded with intendedPalette and previousBrandColors
- BrandProfilerWithoutLogoResult.safe_color_tokens typed as ResolvedPalette
- No handler files modified
</quality_gate>

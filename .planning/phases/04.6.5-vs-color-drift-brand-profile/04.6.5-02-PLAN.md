---
phase: 04.6.5-vs-color-drift-brand-profile
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/lib/visual-signature/types.ts
  - src/lib/visual-signature/identity-art-director.ts
  - src/lib/visual-signature/brand-profiler.ts
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "normalizeIntendedPalette(raw: unknown): IntendedPalette | null exists — validates hex, uppercases, filters invalid support, idempotent"
    - "intendedToResolved(intended, supportResolved): ResolvedPalette exists — secondary = supportResolved[0] ?? primary"
    - "normalizeAdjudication(raw, fallback, contestedRoles, contestedSupportIndices): NormalizedVisionAdjudication exists — validates RawVisionAdjudication contract, resolveRole per role, cobertura total for support, filters invalid indices"
    - "composeSupport(original, contestedIndices, corrections): string[] exists — applies corrections only at contested indices"
    - "RawVisionAdjudicationSchema exists — validates all keys present, rejects duplicate indices, validates hex format"
    - "StoreIdentityArtDirector normalizer extracts intended_palette and color_usage from IA JSON response"
    - "intended_palette and color_usage persisted in metadata.artDirectorOutput alongside existing fields"
    - "Generation proceeds normally when normalization returns null (intended_palette omitted, not blocking)"
  artifacts:
    - path: "src/lib/visual-signature/types.ts"
      provides: "RawVisionAdjudicationSchema + all normalizer functions (normalizeIntendedPalette, intendedToResolved, normalizeAdjudication, composeSupport) — consumed by both Art Director and Brand Profiler"
    - path: "src/lib/visual-signature/identity-art-director.ts"
      provides: "Updated normalizer that extracts intended_palette + color_usage"
  key_links:
    - from: "src/lib/visual-signature/types.ts"
      to: "src/lib/visual-signature/identity-art-director.ts"
      via: "normalizeIntendedPalette consumed by Art Director normalizer"
    - from: "src/lib/visual-signature/types.ts"
      to: "src/lib/visual-signature/brand-profiler.ts"
      via: "intendedToResolved, normalizeAdjudication consumed by profiler"
    - from: "src/lib/brand-assets/color-probe.ts"
      to: "src/lib/visual-signature/brand-profiler.ts"
      via: "findClosestProbeCluster consumed by profiler (Plan 03)"
---

<objective>
**Plan 02: Normalizers & Art Director Update**

Implement all normalization utilities for the intended palette and vision adjudication, then integrate them into the Store Identity Art Director normalizer. This covers the pure functions that validate and transform color data at every stage of the pipeline.

**Purpose:** Create pure normalizer functions + update Art Director to persist structured intended_palette and color_usage.
**Output:** New normalizer functions in types.ts, updated `identity-art-director.ts`.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.6.5-vs-color-drift-brand-profile/04.6.5-CONTEXT.md
@openspec/changes/fase-4-6-5-vs-color-drift-brand-profile/design.md
@openspec/changes/fase-4-6-5-vs-color-drift-brand-profile/tasks.md
@openspec/changes/fase-4-6-5-vs-color-drift-brand-profile/specs/store-identity-art-director/spec.md
@openspec/changes/fase-4-6-5-vs-color-drift-brand-profile/specs/store-brand-profiler-without-logo/spec.md
@src/lib/visual-signature/types.ts
@src/lib/visual-signature/identity-art-director.ts

<existing_patterns>
See src/lib/visual-signature/identity-art-director.ts for current normalizer pattern (parses IA JSON response)
See src/lib/visual-signature/types.ts for existing VisualSignatureMetadataArtDirectorOutput
</existing_patterns>
</context>

<tasks>

<task type="execute">
<read_first>
src/lib/visual-signature/types.ts
openspec/changes/fase-4-6-5-vs-color-drift-brand-profile/specs/store-identity-art-director/spec.md
</read_first>
<action>
Implement `normalizeIntendedPalette(raw: unknown): IntendedPalette | null` in `src/lib/visual-signature/types.ts`:

Rules:
- Validate each color field as valid 6-char hex (`/^#[0-9A-Fa-f]{6}$/`)
- Convert validated hex to uppercase `#RRGGBB`
- `support` array: keep only valid hex entries (filter out invalid)
- If `primary`, `accent`, or `background` missing/invalid → return `null`
- Must be idempotent: calling on already-normalized input produces identical result

Export the function.
</action>
<acceptance_criteria>
- `normalizeIntendedPalette({ primary: "#22c55e", accent: "#1e40af", background: "#0f172a", support: ["#3b82f6"] })` returns `{ primary: "#22C55E", accent: "#1E40AF", background: "#0F172A", support: ["#3B82F6"] }`
- `normalizeIntendedPalette({ primary: "invalid", ... })` returns `null`
- Support filters invalid entries: `["#3B82F6", "invalid", "#FF6600"]` → `["#3B82F6", "#FF6600"]`
- Idempotent: calling twice on same input returns identical result
- `normalizeIntendedPalette(null)` returns `null`
- `normalizeIntendedPalette(undefined)` returns `null`
- `npm run typecheck` passes with no errors
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/visual-signature/types.ts
</read_first>
<action>
Implement `intendedToResolved(intended: IntendedPalette, supportResolved: string[]): ResolvedPalette` in `src/lib/visual-signature/types.ts`:

```typescript
export function intendedToResolved(
  intended: IntendedPalette,
  supportResolved: string[]
): ResolvedPalette {
  return {
    primary: intended.primary,
    secondary: supportResolved[0] ?? intended.primary,
    accent: intended.accent,
    background: intended.background,
  };
}
```

Export the function.
</action>
<acceptance_criteria>
- `intendedToResolved(palette, ["#B96F63"])` returns `{ primary: palette.primary, secondary: "#B96F63", accent: palette.accent, background: palette.background }`
- `intendedToResolved(palette, [])` returns `secondary: palette.primary`
- `npm run typecheck` passes with no errors
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/visual-signature/types.ts
src/lib/brand-assets/color-probe.ts
openspec/changes/fase-4-6-5-vs-color-drift-brand-profile/specs/store-brand-profiler-without-logo/spec.md
</read_first>
<action>
Implement the following in `src/lib/visual-signature/types.ts`:

1. **`composeSupport(original: string[], contestedIndices: number[], corrections: SupportCorrection[]): string[]`** — applies corrections only at contested indices, preserves confirmed supports, returns new array

2. **`resolveRole(role: string, fallback: IntendedPalette, visionValue: string | null, isContested: boolean): string`** — if `isContested` is false, always return fallback value (confirmed roles are immutable). If contested and visionValue is null, throw `VisionAdjudicationError`. If contested and visionValue is set, return visionValue.

3. **`normalizeAdjudication(raw: unknown, fallback: IntendedPalette, contestedRoles: string[], contestedSupportIndices: number[], nonArtifactClusters: ColorCluster[]): NormalizedVisionAdjudication`** — validates RawVisionAdjudication contract, applies resolveRole per role, validates cobertura total for support, filters invalid indices, revalidates HEX livre against probe.

4. **`RawVisionAdjudicationSchema`** — Zod schema (or manual validation) that validates:
   - All keys present (`corrections.primary`, `corrections.accent`, `corrections.background`, `corrections.support`)
   - Each correction is either null or valid 6-char hex
   - Support is array of `{ index: number, color: string }` — no duplicate indices
   - `reason` is string

Also create a custom error class `VisionAdjudicationError` extending `Error` with `code` field.

Export all functions and `RawVisionAdjudicationSchema`.
</action>
<acceptance_criteria>
- `composeSupport(["#A", "#B", "#C"], [1], [{ index: 1, color: "#X" }])` returns `["#A", "#X", "#C"]`
- `composeSupport` with empty corrections returns original array unchanged
- `resolveRole` for non-contested role returns fallback even if visionValue is non-null
- `resolveRole` for contested role with null throws `VisionAdjudicationError`
- `normalizeAdjudication` with all keys present and non-contested null → passes, preserves intended
- `normalizeAdjudication` with contested role null → throws `VisionAdjudicationError('no_choice')`
- `normalizeAdjudication` filters invalid support indices (< 0 or >= length)
- `normalizeAdjudication` with duplicate indices → throws `invalid_json`
- `normalizeAdjudication` with HEX livre ∆E ≤ 18 → accepted
- `normalizeAdjudication` with HEX livre ∆E > 18 → throws `hex_outside_observed_colors`
- `npm run typecheck` passes with no errors
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/visual-signature/identity-art-director.ts
src/lib/visual-signature/types.ts
openspec/changes/fase-4-6-5-vs-color-drift-brand-profile/specs/store-identity-art-director/spec.md
</read_first>
<action>
Update the Art Director normalizer in `src/lib/visual-signature/identity-art-director.ts`:

The service already extracts `VisualSignatureMetadataArtDirectorOutput` fields (visual_direction, content_used, visual_elements) from the AI JSON response (`result.aiResponseMessage`). The `VisualSignatureArtDirectorOutput` (with creative_description, suggested_colors, elements_used) is composed programmatically from input data, not parsed from JSON.

1. Import `normalizeIntendedPalette` from types
2. In the existing `metadataArtDirectorOutput` extraction block, apply `normalizeIntendedPalette()` to `parsed.intended_palette` instead of storing raw:
   ```typescript
   intended_palette: normalizeIntendedPalette(parsed.intended_palette) ?? undefined,
   ```
3. Similarly validate `parsed.color_usage` — verify structure has primary, accent, support, background as strings before persisting
4. If `normalizeIntendedPalette` returns null, **omit** `intended_palette` from output (do NOT block generation — generation proceeds with `undefined`)
5. If `color_usage` is missing/invalid, omit it (non-blocking)
6. Export these fields in `VisualSignatureGenerationResult` through `metadataArtDirectorOutput` (already attached to `result.signature.metadata.artDirectorOutput`)
</action>
<acceptance_criteria>
- `normalizeIntendedPalette()` applied to `parsed.intended_palette` instead of raw storage
- Valid color data → both fields present in `metadataArtDirectorOutput` with uppercase hex
- Invalid color data → `intended_palette` omitted (undefined), generation proceeds normally
- `color_usage` omitted if missing/invalid structure
- `VisualSignatureArtDirectorOutput` (creative_description, suggested_colors, elements_used) and `VisualSignatureMetadataArtDirectorOutput` (visual_direction, content_used, visual_elements) are NOT modified — only intended_palette/color_usage extraction is added
- `npm run typecheck` passes with no errors
</acceptance_criteria>
</task>

</tasks>

<quality_gate>
- normalizeIntendedPalette is idempotent — calling twice produces identical result
- intendedToResolved correctly derives secondary from supportResolved[0]
- composeSupport mutates only contested indices, preserves confirmed ones
- resolveRole enforces immutability of confirmed roles
- normalizeAdjudication validates all RawVisionAdjudication contract rules
- HEX livre revalidated against probe — ∆E ≤ 18 accepted, > 18 rejected
- Art Director normalizer does NOT block generation on invalid color data
- No handler files modified in this plan
</quality_gate>

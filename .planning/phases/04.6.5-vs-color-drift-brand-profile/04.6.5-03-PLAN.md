---
phase: 04.6.5-vs-color-drift-brand-profile
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - src/lib/visual-signature/brand-profiler.ts
  - src/lib/brand-assets/types.ts
  - prompts/store-brand-profiler.md
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "presence validation flow: probeColors() → findClosestProbeCluster() per role → ∆E classification (≤18 confirmed, 18-25 ambiguous, >25 not_confirmed)"
    - "observed_colors selection: closest per contested + best per classification + frequency fill up to 12 + dedup ∆E ≤ 6"
    - "Conditional prompt: happy path = analyze_only; divergence = contest roles + observed_colors; retry/legacy = analyze_only"
    - "Vision arbitration: RawVisionAdjudication → normalizeAdjudication → resolved palette or vision_failed"
    - "HEX livre revalidation: findClosestProbeCluster → accept if ∆E ≤ 18, reject if > 18"
    - "Fallback matrix D7: all 5 paths implemented (all_confirmed, vision_adjudicated, probe_unavailable, fallback_heuristic with probe, fallback_heuristic without probe)"
    - "color_validation discriminated union composed after resolution"
    - "Profile persisted with status = 'failed' when vision fails — safe_color_tokens = {}, audit trail preserved"
    - "stores.brand_color ← safe_color_tokens.primary; stores.accent_color ← safe_color_tokens.accent on success"
    - "brand_colors_chosen preserved only when manual_color_override.enabled === true from previous synced profile"
    - "Store sync SKIPPED when profile status = 'failed'"
  artifacts:
    - path: "src/lib/visual-signature/brand-profiler.ts"
      provides: "Full profiler with presence validation, vision arbitration, fallback matrix, sync"
    - path: "prompts/store-brand-profiler.md"
      provides: "Conditional prompt (happy path vs divergence variants)"
    - path: "src/lib/brand-assets/types.ts"
      provides: "ColorValidation inside BrandProfileRecord.metadata"
  key_links:
    - from: "src/lib/visual-signature/brand-profiler.ts"
      to: "src/lib/brand-assets/color-probe.ts"
      via: "probeColors, findClosestProbeCluster consumed by profiler"
    - from: "src/lib/visual-signature/brand-profiler.ts"
      to: "src/lib/visual-signature/types.ts"
      via: "normalizeIntendedPalette, intendedToResolved, normalizeAdjudication consumed by profiler"
    - from: "src/lib/visual-signature/brand-profiler.ts"
      to: "src/app/api/store/[id]/visual-signature/approve/route.ts"
      via: "BrandProfilerWithoutLogoService.generate() called from approve route"
    - from: "prompts/store-brand-profiler.md"
      to: "src/lib/visual-signature/brand-profiler.ts"
      via: "Prompt template loaded and conditionally rendered by profiler"
---

<objective>
**Plan 03: Brand Profiler — Presence Validation & Vision Arbitration**

Refactor the `BrandProfilerWithoutLogoService` to replace the heuristic `extractColorsFromBuffer` + `pickPrimaryFromPalette` with the new presence-validation flow: `ColorProbe` → ∆E classification → conditional vision arbitration → resolved palette with full audit trail. This is the core plan of the phase.

**Purpose:** Eliminate color drift between intended_palette and safe_color_tokens. Implement the full fallback matrix. Add `stores.accent_color` sync.
**Output:** Refactored `brand-profiler.ts`, conditional prompt, updated types.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.6.5-vs-color-drift-brand-profile/04.6.5-CONTEXT.md
@openspec/changes/fase-4-6-5-vs-color-drift-brand-profile/design.md
@openspec/changes/fase-4-6-5-vs-color-drift-brand-profile/tasks.md
@openspec/changes/fase-4-6-5-vs-color-drift-brand-profile/specs/store-brand-profiler-without-logo/spec.md
@openspec/changes/fase-4-6-5-vs-color-drift-brand-profile/specs/store-brand-profile/spec.md
@src/lib/visual-signature/brand-profiler.ts
@src/lib/visual-signature/types.ts
@src/lib/brand-assets/color-probe.ts
@src/lib/brand-assets/types.ts
@prompts/store-brand-profiler.md

<existing_patterns>
See src/lib/visual-signature/brand-profiler.ts for current BrandProfilerWithoutLogoService
See prompts/store-brand-profiler.md for current profiler prompt template
See src/lib/brand-assets/brand-director.ts for BrandDirectorService pattern (reference)
</existing_patterns>
</context>

<tasks>

<task type="execute">
<read_first>
prompts/store-brand-profiler.md
src/lib/visual-signature/types.ts
openspec/changes/fase-4-6-5-vs-color-drift-brand-profile/specs/store-brand-profiler-without-logo/spec.md
</read_first>
<action>
Update `prompts/store-brand-profiler.md` to support conditional prompt variants:

1. **Happy path** (`analyze_only`): current semantic analysis prompt — no color extraction, no correction instructions. Add: "A paleta de cores já está definida — não extraia, sugira ou corrija cores."

2. **Divergência** (contested roles): enhanced prompt that includes:
   - List of contested roles with intended value and ∆E
   - `observed_colors` array (supplied as context)
   - Instruction: "Escolha exclusivamente das cores observadas listadas abaixo. HEX fora desta lista será rejeitado."
   - FULL response JSON schema: semantic analysis fields (`visual_style`, `visual_tone`, `typography_direction`, `brand_personality`, `campaign_guidelines`, `campaign_brief`, `confidence_score`) + `corrections` + `reason`
   - `RawVisionAdjudication` JSON schema embedded as the `corrections` sub-object inside the full response

3. **Retry/legacy** (`analyze_only`): current semantic analysis prompt — same as happy path, no color arbitration

Use prompt template variables or conditional sections. The service loads the appropriate variant based on the presence validation result.
</action>
<acceptance_criteria>
- Prompt has conditional sections for happy_path, divergence, retry_legacy
- Happy path: "a paleta já está definida" — no color extraction instructions
- Divergência: lists contested roles with intended value and ∆E, restricts to observed_colors
- Retry/legacy: same as happy path (analyze_only)
- Divergência variant specifies full response schema: semantic fields + corrections + reason
- RawVisionAdjudication schema used only as `corrections` sub-object inside the full response
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/visual-signature/brand-profiler.ts
src/lib/visual-signature/types.ts
src/lib/brand-assets/color-probe.ts
openspec/changes/fase-4-6-5-vs-color-drift-brand-profile/specs/store-brand-profiler-without-logo/spec.md
</read_first>
<action>
Implement the **presence validation** flow in `BrandProfilerWithoutLogoService`:

1. When `intendedPalette` is non-null:
   a. Read the approved VS image buffer from `assetUrl`
   b. Call `probeColors(buffer)` to get probe result
   c. Collect ALL_NON_ARTIFACT_CLUSTERS: exclude `suspected_transitions` and clusters with `frequency < 0.005`
   d. For each role (primary, accent, each support[index], background):
      - Call `findClosestProbeCluster(intendedRoleHex, ALL_NON_ARTIFACT_CLUSTERS)`
      - Classify by ∆E: ≤18 → `confirmed`, 18 < ∆E ≤ 25 → `ambiguous`, > 25 → `not_confirmed`
      - If `ambiguous` or `not_confirmed` → mark role as contested
   e. If probe returns all empty arrays (error/exception) → fall back to `probe_unavailable`

2. When `intendedPalette` is null (retry/legacy):
   - Skip presence validation
   - Fall back to heuristic flow (task 5.8)

Export the status classification and contested roles as intermediate state for the next tasks.
</action>
<acceptance_criteria>
- Each intended role classified by ∆E against closest non-artifact probe cluster
- ∆E ≤ 18 → `confirmed`, 18 < ∆E ≤ 25 → `ambiguous`, > 25 → `not_confirmed`
- Contested roles tracked for downstream vision arbitration decision
- Probe exception → empty result → `probe_unavailable` path
- `npm run typecheck` passes with no errors
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/visual-signature/brand-profiler.ts
src/lib/visual-signature/types.ts
</read_first>
<action>
Implement `observed_colors` selection from non-artifact probe clusters:

1. For each contested color: include the cluster with minimum ∆E (guarantees representation)
2. For each relevant classification (dominant, dark_ink, neutral, background, structural): include the best cluster (highest frequency)
3. Fill remaining slots by frequency descending, up to **maximum 12**
4. Deduplicate: remove clusters where ∆E ≤ 6 between them, without removing mandatory candidates (rules 1 or 2)
5. Return selected clusters ordered by frequency descending

Export as `selectObservedColors(clusters, contestedColors): ColorCluster[]` in the service module.
</action>
<acceptance_criteria>
- closest contest cluster included regardless of frequency (e.g., 2% cluster still included)
- Max 12 clusters in result
- Dedup preserves mandatory candidates (rule 1 and 2 clusters)
- Result ordered by frequency descending
- `npm run typecheck` passes with no errors
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/visual-signature/brand-profiler.ts
prompts/store-brand-profiler.md
</read_first>
<action>
Implement the **conditional prompt logic**:

Based on the presence validation state:
1. **No contested roles AND intendedPalette valid**: load happy path prompt variant → `prompt_suffix = 'analyze_only'` → call vision for semantic analysis only
2. **Contested roles AND intendedPalette valid**: load divergence prompt variant with `observed_colors`, contested roles with intended values and ∆E → `prompt_suffix` reflects contested roles (e.g., `'analyze_and_correct_primary'`, `'analyze_and_correct_support'`)
3. **intendedPalette null (retry/legacy)**: load `analyze_only` prompt variant → call vision for semantic analysis only (NO color arbitration)

If probe is unavailable (`probe_unavailable`): treat as no contested roles → happy path.
</action>
<acceptance_criteria>
- Happy path: prompt_suffix = 'analyze_only', no color extraction/correction in prompt
- Divergência: prompt_suffix encodes contested roles, prompt includes observed_colors
- Retry/legacy: prompt_suffix = 'analyze_only', no color arbitration
- probe_unavailable: treated as happy path (analyze_only)
- `npm run typecheck` passes with no errors
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/visual-signature/brand-profiler.ts
src/lib/visual-signature/types.ts
</read_first>
<action>
Implement the **vision arbitration integration with semantic preservation**:

When the vision is called in divergence mode:
1. Call vision API with conditional divergence prompt
2. Parse the **full response** — contains BOTH semantic analysis AND corrections/reason:
   ```typescript
   interface FullDivergenceResponse {
     visual_style: string;
     visual_tone: string;
     typography_direction: string;
     brand_personality: string;
     campaign_guidelines: string;
     campaign_brief: string;
     confidence_score: number;
     corrections: { primary: string | null; accent: string | null; background: string | null; support: SupportCorrection[] };
     reason: string;
   }
   ```
3. Extract `corrections` + `reason` as a sub-object (forms a complete `RawVisionAdjudication`)
4. Validate the corrections sub-object via `RawVisionAdjudicationSchema` (Zod sub-schema, not full response schema)
5. Call `normalizeAdjudication({ corrections, reason }, fallback=IntendedPalette, contestedRoles, contestedSupportIndices, nonArtifactClusters)` — passes the raw response sub-object matching `RawVisionAdjudication` shape
6. **Preserve semantic fields** (visual_style, visual_tone, typography_direction, brand_personality, campaign_guidelines, campaign_brief, confidence_score) — they MUST be persisted in the brand profile output alongside the resolved palette
7. Handle all failure modes:
   - API timeout/network → `api_error` → `vision_failed`
   - Invalid JSON / schema parse fail (full response or corrections sub-object) → `invalid_json` → `vision_failed`
   - `no_choice` from normalizeAdjudication → `vision_failed`
   - `hex_outside_observed_colors` → `vision_failed`

On success: return the `NormalizedVisionAdjudication` with resolved palette AND the preserved semantic fields.
On failure: do NOT throw — prepare `vision_failed` state for persistence.
</action>
<acceptance_criteria>
- Vision API called with divergence prompt when contested roles exist
- Full response parsed — BOTH semantic fields (visual_style, visual_tone, typography_direction, brand_personality, campaign_guidelines, campaign_brief, confidence_score) AND corrections/reason
- Corrections sub-object validated via RawVisionAdjudicationSchema (not the entire response)
- normalizeAdjudication called with correct contested roles and indices
- Semantic fields preserved in brand profile output alongside resolved palette
- All failure modes caught and mapped to appropriate VisionFailureReason
- On success: NormalizedVisionAdjudication with resolved palette + semantic fields
- On failure: vision_failed state prepared (no throw)
- `npm run typecheck` passes
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/visual-signature/brand-profiler.ts
src/lib/visual-signature/types.ts
src/lib/brand-assets/types.ts
</read_first>
<action>
Implement the **fallback matrix** (intendedPalette null — retry/legacy):

**Fallback with valid probe:**
- primary ← first dominant non-neutral cluster (luminance ≥ 0.25 and saturation ≥ 0.1)
- accent ← second dominant non-neutral cluster, or first structural if only one dominant exists
- secondary ← accent
- background ← first background_candidate, or cluster with highest edgeRatio

**Fallback with empty probe:**
- primary ← `store.brandColor ?? SEGMENT_FALLBACK`
- secondary ← same as primary
- accent ← derived from brandColor (same as primary if set, else SEGMENT_FALLBACK)
- background ← '#FFFFFF'

After resolving via heuristic, compose safe_color_tokens as ResolvedPalette via intendedToResolved equivalent (without intended — manual mapping).

The vision SHALL be called for semantic analysis only (analyze_only) — no color arbitration.
</action>
<acceptance_criteria>
- Valid probe + null intendedPalette: primary = first dominant non-neutral, secondary = accent, background = first background_candidate or highest edgeRatio
- Empty probe + null intendedPalette + brandColor set: primary = brandColor, background = '#FFFFFF'
- Empty probe + null intendedPalette + brandColor null: primary = SEGMENT_FALLBACK
- Vision called with analyze_only prompt (no color arbitration)
- `npm run typecheck` passes with no errors
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/visual-signature/brand-profiler.ts
</read_first>
<action>
Implement the **color_validation composition** after resolution:

After resolving the final palette (via intended + confirmed, vision_adjudication, probe_unavailable, or fallback_heuristic):

1. Compose `ColorValidationResolved` with per-role `ColorValidationEntry`:
   - `intended`: the intended hex (null for fallback_heuristic)
   - `resolved`: the final hex (equals safe_color_tokens.<role>)
   - `presence`: from probe classification (or 'unchecked' if probe unavailable / fallback)
   - `delta_e`: from findClosestProbeCluster result (or null)
   - `role_source`: 'art_director' | 'vision_adjudication' | 'heuristic'
   - `resolution`: 'accepted' | 'accepted_unverified' | 'corrected_by_vision' | 'selected_by_heuristic'
   - `resolved_from_cluster`: populated when probe confirmed presence or vision chose a cluster
   - `note`: contextual explanation

2. Set `support_colors` = resolved support array
3. If vision was called: attach `vision_adjudication` audit record

For `vision_failed`: compose `ColorValidationFailed` with only `vision_adjudication` audit.
</action>
<acceptance_criteria>
- ColorValidationResolved composed with all 4 role entries (primary, accent, secondary, background)
- Each entry has intended, resolved, presence, role_source, resolution fields
- support_colors = resolved support array
- vision_adjudication present when global_status = 'vision_adjudicated'
- vision_failed state has only vision_adjudication (no role entries)
- ColorValidation matches safe_color_tokens (color_validation.<role>.resolved == safe_color_tokens.<role>)
- `npm run typecheck` passes
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/visual-signature/brand-profiler.ts
src/lib/brand-assets/types.ts
</read_first>
<action>
Implement persistence for **failed profile**:

When `vision_failed`:
1. Create brand profile with `status: 'failed'`
2. `safe_color_tokens = {}`
3. `metadata.color_validation = ColorValidationFailed` with vision_adjudication audit
4. `inferred_primary_color` and `inferred_accent_color` NOT set (or null)
5. `brand_colors_chosen` from previousBrandColors (if manual_override)
6. Do NOT sync stores (brand_color, accent_color unchanged)
7. Do NOT mark previous synced profile as outdated — it remains valid

The visual signature remains `active` — profile failure does NOT roll back the approval.
</action>
<acceptance_criteria>
- Failed profile has `status = 'failed'`
- `safe_color_tokens = {}`
- `color_validation.global_status = 'vision_failed'`
- `color_validation.vision_adjudication` has `status: 'failed'` with reason and attemptedAt
- No store sync executed
- Previous synced profile (if any) remains `synced`
- `npm run typecheck` passes
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/visual-signature/brand-profiler.ts
</read_first>
<action>
Implement **logo_colors_detected preservation**:

The `logo_colors_detected` field on the brand profile SHALL continue to be composed programmatically from ColorProbe non-artifact clusters and/or suggested_colors — preserving current behavior after the refactoring.

1. After probe runs (any path: happy, divergence, fallback), collect ALL_NON_ARTIFACT_CLUSTERS from the probe result
2. Compose `logo_colors_detected` from:
   - ColorProbe non-artifact clusters (dominant_pixels, dark_ink_candidates, small_but_structural) — extract hex values, up to 5 (preserving current behavior per OpenSpec)
   - If probe unavailable or empty → use `suggested_colors` from Art Director Output
3. If both probe and suggested_colors are empty → `logo_colors_detected = []`
4. This replaces the old `extractColorsFromBuffer` + `pickPrimaryFromPalette` heuristic with ColorProbe-based extraction (same logical behavior, better data source)

</action>
<acceptance_criteria>
- `logo_colors_detected` composed from ColorProbe non-artifact clusters when probe is available
- `logo_colors_detected` falls back to suggested_colors when probe is unavailable
- `logo_colors_detected = []` when both are empty
- `npm run typecheck` passes with no errors
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/visual-signature/brand-profiler.ts
</read_first>
<action>
Implement **store sync and brand_colors_chosen preservation**:

After successful profile creation (status = 'synced'):
1. Sync stores:
   - `stores.brand_color = safe_color_tokens.primary`
   - `stores.accent_color = safe_color_tokens.accent`
2. Set profile fields:
   - `inferred_primary_color = safe_color_tokens.primary`
   - `inferred_accent_color = safe_color_tokens.accent`
3. Preserve `brand_colors_chosen`:
   - Load from `previousBrandColors` parameter (already filtered at approve route — only passed when `manual_color_override.enabled === true`)
   - If `previousBrandColors` is empty → `brand_colors_chosen = []`

SKIP ALL STEPS when profile status is 'failed'.
</action>
<acceptance_criteria>
- On success: stores.brand_color = safe_color_tokens.primary, stores.accent_color = safe_color_tokens.accent
- On success: inferred_primary_color = safe_color_tokens.primary, inferred_accent_color = safe_color_tokens.accent
- On success: brand_colors_chosen preserved when previousBrandColors non-empty, else []
- On failure: NO store sync, NO inferred_* update
- `npm run typecheck` passes with no errors
</acceptance_criteria>
</task>

</tasks>

<quality_gate>
- Presence validation replaces heuristic classification (extractColorsFromBuffer + pickPrimaryFromPalette removed)
- All 5 fallback matrix paths implemented correctly
- observed_colors selection guarantees contest cluster representation
- Conditional prompt restricts vision to observed_colors in divergence mode
- HEX livre revalidation prevents hallucinated colors
- Failed profile preserves full audit trail without blocking signature
- store sync happens ONLY on success
- brand_colors_chosen preserved ONLY with manual_color_override proof
- Vision is NEVER called for color arbitration on retry/legacy (analyze_only only)
</quality_gate>

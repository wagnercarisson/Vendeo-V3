---
phase: 04.6.5-vs-color-drift-brand-profile
plan: 05
type: execute
wave: 5
depends_on: [04]
files_modified:
  - package.json
  - vitest.config.ts
  - src/lib/brand-assets/__tests__/color-probe.test.ts
  - src/lib/visual-signature/__tests__/normalize-intended-palette.test.ts
  - src/lib/visual-signature/__tests__/normalize-adjudication.test.ts
  - src/lib/visual-signature/__tests__/palette-resolution.test.ts
  - src/app/api/store/[id]/visual-signature/approve/__tests__/approve-route.test.ts
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "Test runner defined: vitest (already compatible with project's tsx + zod setup)"
    - "npm run test script added to package.json: vitest run"
    - "npm run test:watch script added: vitest"
    - "npm run typecheck passes with zero errors"
    - "npm run lint passes with zero warnings"
    - "Mocking strategy: OpenAI responses via vi.mock('openai'), Supabase via vi.mock('@supabase/supabase-js'), Sharp via vi.mock('sharp'), image buffers via fixture PNGs"
    - "ColorProbe tests: PNG sólido ∆E ≈ 0, PNG sólido ∆E ≤ 18, PNG sólido ∆E > 25, gradiente, buffer corrompido"
    - "BrandDirector regression tests: compiles + curateLogoColors/applyGuardrail unchanged + deterministicResult golden"
    - "normalizeIntendedPalette tests: valid input uppercase, invalid → null, filters support, idempotent, null/undefined"
    - "normalizeAdjudication tests: all keys present non-contested null, contested null → throws, confirmed role ignores, support cobertura, invalid indices filtered, duplicate indices → invalid_json, HEX livre accepted/rejected"
    - "Palette resolution tests (probe mockado): all_confirmed, vision_adjudicated (primary 18-25), vision_adjudicated (>25), support[0] confirmed, support[] contested, probe_unavailable, vision_failed, fallback_heuristic brandColor, fallback_heuristic SEGMENT_FALLBACK, support[1] contestado preserva [0], edgeRatio background, background cromático, HEX livre revalidation, happy path analyze_only, fallback_heuristic analyze_only, divergência arbitration prompt, duplicate indices → vision_failed, observed_colors selection, observed_colors dedup"
    - "Integration tests: VS approval happy path → all_confirmed, brand_colors_chosen preservation, no previous colors → [], profiler failure → signature active, retry null intendedPalette → fallback_heuristic, store sync, intended_palette in metadata, intended_palette omitted on null, text_only→logo transição, logo→text_only transição, VS approval sem intended_palette"
    - "Manual verification: inspecionar color_validation no banco, calibrar thresholds ∆E com imagens VS reais"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Test runner configuration"
    - path: "package.json"
      provides: "npm run test and npm run test:watch scripts"
    - path: "src/lib/brand-assets/__tests__/color-probe.test.ts"
      provides: "ColorProbe tests with fixture PNGs"
    - path: "src/lib/visual-signature/__tests__/normalize-intended-palette.test.ts"
      provides: "normalizeIntendedPalette unit tests"
    - path: "src/lib/visual-signature/__tests__/normalize-adjudication.test.ts"
      provides: "normalizeAdjudication + RawVisionAdjudicationSchema unit tests"
    - path: "src/lib/visual-signature/__tests__/palette-resolution.test.ts"
      provides: "Palette resolution tests (probe mockado, vision mockado)"
    - path: "src/app/api/store/[id]/visual-signature/approve/__tests__/approve-route.test.ts"
      provides: "Integration tests (VS approval → brand profile)"
---

<objective>
**Plan 05: Tests & Verification**

Implement all automated tests (ColorProbe, normalizeIntendedPalette, normalizeAdjudication, palette resolution with mocked probe, integration tests) and perform manual verification. Run typecheck, lint, and build.

**Purpose:** Validate all changes across the phase deliver complete, correct functionality without regressions.
**Output:** New test files, verification evidence for all 96 tasks from openspec tasks.md
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.6.5-vs-color-drift-brand-profile/04.6.5-CONTEXT.md
@openspec/changes/fase-4-6-5-vs-color-drift-brand-profile/tasks.md
@openspec/changes/fase-4-6-5-vs-color-drift-brand-profile/design.md
@src/lib/brand-assets/color-probe.ts
@src/lib/visual-signature/types.ts
@src/lib/visual-signature/brand-profiler.ts
@src/lib/visual-signature/identity-art-director.ts
@src/app/api/store/[id]/visual-signature/approve/route.ts
@src/lib/brand-assets/brand-director.ts

<existing_patterns>
See src/lib/visual-signature/types.ts for testable pure functions
See .planning/phases/04.6.4-visual-signature-lifecycle/04.6.4-05-PLAN.md for previous verification plan structure
</existing_patterns>
</context>

<tasks>

<task type="execute">
<read_first>
package.json
</read_first>
<action>
Set up test infrastructure:

1. Install vitest: `npm install -D vitest`
2. Create `vitest.config.ts`:
   ```typescript
   import { defineConfig } from 'vitest/config';
   import path from 'path';

   export default defineConfig({
     test: {
       globals: true,
       environment: 'node',
     },
     resolve: {
       alias: {
         '@': path.resolve(__dirname, './src'),
       },
     },
   });
   ```
3. Add to `package.json` scripts:
   ```json
   "test": "vitest run",
   "test:watch": "vitest"
   ```
4. Create test directories:
   - `src/lib/brand-assets/__tests__/`
   - `src/lib/visual-signature/__tests__/`
   - `src/app/api/store/[id]/visual-signature/approve/__tests__/`

Mocking strategy (all test files use these patterns):
- **OpenAI**: Use `vi.mock('openai', () => ({ default: { /* mock impl */ } }))` — mock responses return controlled JSON per scenario
- **Supabase**: Use `vi.mock('@supabase/supabase-js')` — mock `from().select().eq().single()` chains
- **Sharp**: Use `vi.mock('sharp')` — mock to return controlled pixel buffers for probe test fixtures
- **Probe**: Test-only helper `createMockProbeResult(clusters)` that produces `ColorProbeResult` from controlled fixture data
- **Vision**: Test-only helper `createMockVisionResponse(response)` that returns simulated AI output without real API call
- **Fixture PNGs**: Store as base64 inline strings in `__tests__/fixtures/` (or as actual `.png` files in `__tests__/fixtures/`)
</action>
<acceptance_criteria>
- `npm run test` runs (may fail on first run until tests are written — vitest itself starts without error)
- vitest.config.ts resolves `@/` alias correctly
- `npm run typecheck` passes with vitest types installed
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/brand-assets/color-probe.ts
openspec/changes/fase-4-6-5-vs-color-drift-brand-profile/tasks.md (section 8)
</read_first>
<action>
Write **ColorProbe tests** (fixture PNG, pure function):

1. PNG sólido #B96F63 → `findClosestProbeCluster('#B96F63')` returns ∆E ≈ 0 (confirmed)
2. PNG sólido #B96F63 → `findClosestProbeCluster('#B96F50')` returns ∆E ≤ 18 (confirmed)
3. PNG sólido #B96F63 → `findClosestProbeCluster('#FF0000')` returns ∆E > 25 (not_confirmed)
4. PNG gradiente → findClosestProbeCluster for existing and non-existing colors
5. Buffer corrompido → probe returns empty result (graceful error)
6. Regressão: brand-director.ts compiles and curateLogoColors/applyGuardrail unchanged
7. Regressão: BrandDirector deterministicResult unchanged for same input (golden test)
</action>
<acceptance_criteria>
- All 7 test scenarios pass
- Tests use fixture PNGs (not real API calls)
- BrandDirector regression tests confirm no logic change after extraction
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/visual-signature/types.ts
openspec/changes/fase-4-6-5-vs-color-drift-brand-profile/tasks.md (section 9)
</read_first>
<action>
Write **normalizeIntendedPalette tests**:

1. Valid input → returns IntendedPalette with uppercase hex
2. Invalid primary → returns null
3. Support with invalid entries → filters out invalid hexes
4. Idempotent — calling twice on same input returns identical result
5. null/undefined/empty input → returns null
</action>
<acceptance_criteria>
- All 5 test scenarios pass
- Tests cover all edge cases from spec
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/visual-signature/types.ts
openspec/changes/fase-4-6-5-vs-color-drift-brand-profile/tasks.md (section 10)
</read_first>
<action>
Write **normalizeAdjudication tests**:

1. All keys present, non-contested null → passes, preserves intended
2. Contested role with null → throws VisionAdjudicationError('no_choice')
3. Confirmed role ignores vision correction → resolveRole returns fallback
4. Support cobertura total — every contested index has matching correction
5. Support cobertura incompleta — missing correction → throws no_choice
6. Invalid support indices filtered (ignored, not failed)
7. Duplicate indices → invalid_json
8. HEX livre ∆E ≤ 18 accepted, ∆E > 18 rejected
</action>
<acceptance_criteria>
- All 8 test scenarios pass
- Tests cover all RawVisionAdjudication contract rules
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/visual-signature/brand-profiler.ts
openspec/changes/fase-4-6-5-vs-color-drift-brand-profile/tasks.md (section 11)
</read_first>
<action>
Write **palette resolution tests** (probe mockado, sem IA real):

1. Todas ∆E ≤ 18 → all_confirmed, primary == intended.primary, secondary == support[0] ?? primary
2. Primary 18 < ∆E ≤ 25 → vision_adjudicated, vision receives observed_colors
3. Primary ∆E > 25 → vision_adjudicated
4. support[0] ∆E ≤ 18 → all_confirmed, secondary == support[0]
5. support[0] 18 < ∆E ≤ 25 → vision_adjudicated, vision arbitrates support
6. Probe vazio + intendedPalette válido → probe_unavailable, primary == intended.primary
7. Primary ambíguo + visão falha → vision_failed, profile 'failed'
8. Probe vazio + intendedPalette null + brandColor set → fallback_heuristic, primary == brandColor
9. Probe vazio + intendedPalette null + brandColor null → fallback_heuristic, primary == SEGMENT_FALLBACK
10. support[1] contestado, support[0] preservado → secondary == support[0] original
11. Background identificado por edgeRatio — maior edgeRatio vira background
12. Background cromático preservado — não descartado como artefato
13. HEX livre ∆E ≤ 18 aceito, HEX livre ∆E > 18 rejeitado
14. Happy path chama visão com analyze_only — vision mock verifica prompt_suffix
15. fallback_heuristic chama visão com analyze_only — vision mock verifica prompt_suffix
16. Divergência chama visão com prompt de arbitragem — vision mock recebe observed_colors
17. Índice duplicado → invalid_json → vision_failed
18. observed_colors selection — closest contest cluster included, max 12, dedup preserves mandatory
19. observed_colors dedup — ∆E ≤ 6 removes duplicates but keeps mandatory candidates
</action>
<acceptance_criteria>
- All 19 test scenarios pass
- Probe mocked, vision mocked — no real API calls in tests
- All fallback matrix paths verified
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/app/api/store/[id]/visual-signature/approve/route.ts
openspec/changes/fase-4-6-5-vs-color-drift-brand-profile/tasks.md (section 12)
</read_first>
<action>
Write **integration tests** (VS approval → brand profile):

1. VS approval happy path → color_validation.global_status === 'all_confirmed', roles match intended
2. VS approval with brand_colors_chosen preservation (manual_color_override.enabled)
3. VS approval without previous colors → brand_colors_chosen = []
4. Profiler failure → signature still active, profile status = failed
5. VS approval with retry (null intendedPalette) → fallback_heuristic
6. stores.brand_color and stores.accent_color synced on success, NOT synced on failure
7. intended_palette and color_usage persisted in signature metadata after generation
8. intended_palette omitted when normalization returns null (generation not blocked)
9. text_only → logo transição — fluxo de logo intacto
10. logo → text_only transição — identity_state = text_only, direção preservada
11. VS approval sem intended_palette nem profile anterior → fallback, global_status = 'fallback_heuristic'
12. VS approval sem intended_palette mas com profile synced anterior → profile reutilizado, fallback NÃO roda
</action>
<acceptance_criteria>
- All 12 test scenarios pass
- Integration tests cover end-to-end approval → profile pipeline
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
openspec/changes/fase-4-6-5-vs-color-drift-brand-profile/tasks.md (section 13, 14)
</read_first>
<action>
Perform manual verification:

1. Run `npm run typecheck` — fix any type errors (zero errors required)
2. Run `npm run lint` — fix any lint warnings (zero warnings required)
3. Run full test suite — all existing + new tests pass
4. Run `npm run build` — build succeeds

Manual scenarios:
- Gerar VS real → aprovar → inspecionar `color_validation` no banco — cores fazem sentido visualmente?
- Gerar VS → rejeitar → gerar nova → aprovar — paleta da segunda VS independente da primeira
- Calibrar thresholds ∆E com imagens VS reais se necessário (baseline CIE76)

Record results of all checks.
</action>
<acceptance_criteria>
- `npm run typecheck` exits 0
- `npm run lint` exits 0
- Full test suite passes
- `npm run build` succeeds
- Manual visual verification of color_validation in database
- Calibration notes recorded if thresholds need adjustment
</acceptance_criteria>
</task>

</tasks>

<quality_gate>
- All 51 individual test scenarios covered (7 ColorProbe + 5 normalizeIntendedPalette + 8 normalizeAdjudication + 19 palette resolution + 12 integration)
- TypeScript strict mode passes with zero errors
- Lint passes with zero warnings
- Build succeeds with zero errors
- Manual verification covers real VS approval flow
- No regressions in existing logo/VS/brand-profiler functionality
- Tests use fixture PNGs and mocked AI — no real API calls
- ColorProbe, normalizer, palette resolution, and integration tests provide full coverage
</quality_gate>

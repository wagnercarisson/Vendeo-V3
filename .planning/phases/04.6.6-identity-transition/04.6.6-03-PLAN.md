---
phase: 04.6.6-identity-transition
plan: 03
type: execute
wave: 3
depends_on:
  - 04.6.6-02-PLAN.md
files_modified:
  - src/components/flow/store-identity-form.tsx
  - src/components/flow/store-visual-signature-section.tsx
  - src/components/flow/use-store-form.ts
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "useIdentityActions(identityState, storeData) returns available actions per the state-action matrix"
    - "store-identity-form.tsx uses the hook: text_only shows Enviar logotipo + Gerar/Gerenciar VS; logo shows only Remover logo; visual_signature shows only Remover assinatura visual"
    - "store-visual-signature-section.tsx: in visual_signature shows only Remover assinatura visual; in logo hides all VS actions; in text_only shows Gerar/Gerenciar VS"
    - "Continuar sem logo link removed from Step 2 — not displayed in any identity state"
    - "Informational card shown in text_only: 'Sem logo por enquanto?...'"
    - "Confirmation dialog shown before DELETE /logo with warning about irreversibility"
    - "Card and warning dialog NOT rendered in logo or visual_signature states"
  artifacts:
    - path: "src/components/flow/use-store-form.ts"
      provides: "useIdentityActions hook (or equivalent)"
    - path: "src/components/flow/store-identity-form.tsx"
      provides: "State-action matrix applied to identity-related actions"
    - path: "src/components/flow/store-visual-signature-section.tsx"
      provides: "State-appropriate VS actions per identity_state"
  key_links:
    - from: "src/components/flow/store-identity-form.tsx"
      to: "src/lib/identity-transitions.ts"
      via: "Indirect — hook reads identity_state from store data, orchestrator enforces at API level"
---

<objective>
**Plan 03: UI — State-Action Matrix, Guidance Card, Logo Removal Warning**

Implement the UI layer: hook returning available actions per state, conditionally rendered buttons, removal of "Continuar sem logo" link, informational card, and confirmation dialog for logo removal.

**Purpose:** Align UI actions with identity_state so the user only sees compatible options.
**Output:** Modified components and hook.
</objective>

<execution_context>
@C:/Projetos/Vendeo V3/.opencode/skills/openspec-apply-change/skill.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.6.6-identity-transition/04.6.6-CONTEXT.md
@openspec/changes/fase-4-6-6-identity-transition/specs/store-identity-ui/spec.md
@openspec/changes/fase-4-6-6-identity-transition/design.md
@openspec/changes/fase-4-6-6-identity-transition/tasks.md
@openspec/specs/store-identity-ui/spec.md

<existing_patterns>
See src/components/flow/store-identity-form.tsx for current form structure
See src/components/flow/store-visual-signature-section.tsx for current VS section
See src/components/flow/use-store-form.ts for current state management
</existing_patterns>
</context>

<tasks>

<task type="execute">
<read_first>
src/components/flow/use-store-form.ts
src/components/flow/store-identity-form.tsx
openspec/specs/store-identity-ui/spec.md
</read_first>
<action>
Implement (or extend) a function/hook in `src/components/flow/use-store-form.ts` that returns available identity actions based on state:

```typescript
type IdentityActions = {
  canUploadLogo: boolean;
  canRemoveLogo: boolean;
  canCreateVS: boolean;
  canManageVS: boolean;
  canRemoveVS: boolean;
  showGuidanceCard: boolean;
}

function useIdentityActions(store: { identity_state: string | null, hasExistingVS: boolean }): IdentityActions
```

Logic:
| `identity_state` | `canUploadLogo` | `canRemoveLogo` | `canCreateVS` | `canManageVS` | `canRemoveVS` | `showGuidanceCard` |
|---|---|---|---|---|---|---|
| `null` / not set | ✅ | ❌ | ✅ | ❌ | ❌ | ❌ |
| `text_only` | ✅ | ❌ | ✅ (no VS) | ✅ (has VS) | ❌ | ✅ |
| `logo` | ❌ | ✅ | ❌ | ❌ | ❌ | ❌ |
| `visual_signature` | ❌ | ❌ | ❌ | ❌ | ✅ | ❌ |

When `identity_state` is `null` (store not yet saved), preserve the initial behavior (all options available, no restriction).
</action>
<acceptance_criteria>
- `useIdentityActions` function defined with the correct per-state matrix
- `canUploadLogo` / `canRemoveLogo` correctly reflect the state-action matrix
- `canCreateVS` / `canManageVS` correctly reflect state and VS existence
- `canRemoveVS` correctly reflects state
- `showGuidanceCard` only true for `text_only`
- `npm run typecheck` passes with no errors
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/components/flow/store-identity-form.tsx
src/components/flow/use-store-form.ts
</read_first>
<action>
Update `src/components/flow/store-identity-form.tsx`:

1. Call `useIdentityActions(store)` to get available actions
2. **Logo area:**
   - Show "Enviar logotipo" button only when `canUploadLogo` is true
   - Show "Remover logo" button only when `canRemoveLogo` is true (turns into confirmation dialog — see task 03.3)
   - Hide drop zone when `canUploadLogo` is false
3. **VS area:**
   - Show "Gerar assinatura visual" or "Gerenciar assinatura visual" only when `canCreateVS` or `canManageVS` is true
4. **"Continuar sem logo":**
   - Remove the link entirely — do NOT render it regardless of state
5. **Guidance card:**
   - Render the informational card when `showGuidanceCard` is true (below the logo/VS area)
6. Non-identity elements (color pickers, text fields, save button) remain unchanged
</action>
<acceptance_criteria>
- "Enviar logotipo" hidden in `logo` and `visual_signature` states
- "Remover logo" only visible in `logo` state
- VS create/manage buttons hidden in `logo` and `visual_signature` states
- "Continuar sem logo" link NOT rendered anywhere
- Guidance card visible only in `text_only` state
- Color pickers, text fields, save button unchanged
- `npm run typecheck` passes with no errors
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/components/flow/store-visual-signature-section.tsx
src/components/flow/use-store-form.ts
</read_first>
<action>
Update `src/components/flow/store-visual-signature-section.tsx`:

1. Read `identity_state` and `useIdentityActions`
2. **When `identity_state = 'visual_signature'`:**
   - Show approved VS preview (existing)
   - Show only "Remover assinatura visual" button (existing behavior, keep)
   - Hide "Alterar" button (direct VS swap blocked in this phase)
   - Hide drop zone, "Não tenho logo", "Continuar sem logo"
3. **When `identity_state = 'logo'`:**
   - Hide all VS-related actions (no create, no manage, no remove, no preview)
   - Only identity action for logo is "Remover logo" (handled in store-identity-form.tsx)
4. **When `identity_state = 'text_only'`:**
   - Show "Gerar assinatura visual" (if no VS exists) or "Gerenciar assinatura visual" (if VS exists)
   - Show existing VS assets if any
   - "Continuar sem logo" NOT shown here either
5. **When `identity_state` is `null` (initial state):**
   - Preserve existing behavior (all options available)
</action>
<acceptance_criteria>
- VS section in `visual_signature` state shows only "Remover assinatura visual" (no "Alterar")
- VS section in `logo` state hides all VS actions
- VS section in `text_only` shows "Gerar" or "Gerenciar" VS
- "Continuar sem logo" not shown in VS section regardless of state
- `npm run typecheck` passes with no errors
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/components/flow/store-identity-form.tsx
</read_first>
<action>
Implement the guidance card and logo removal confirmation dialog:

**Guidance card:**
```tsx
// Shown when showGuidanceCard is true
<div className="bg-bg-surface border border-border rounded-lg p-4 mt-4">
  <p className="font-semibold text-text-primary">Sem logo por enquanto?</p>
  <p className="text-text-secondary text-sm mt-1">
    Você pode escolher as cores da loja, se quiser, e clicar em Salvar.
    O Vendeo vai gerar uma direção visual usando os dados básicos da loja.
  </p>
</div>
```

**Logo removal confirmation dialog:**
- When user clicks "Remover logo", show a dialog/alert before calling DELETE
- Dialog text: "Ao remover o logo, ele não ficará disponível para reaplicação pela interface. Você poderá enviar o arquivo novamente quando quiser."
- Two buttons: "Remover" (confirms, calls DELETE) and "Cancelar" (closes dialog)
- DELETE only called after explicit confirmation

The confirmation dialog is a UI responsibility — the API does NOT add warnings.
</action>
<acceptance_criteria>
- Guidance card rendered with correct text when `showGuidanceCard === true`
- Guidance card NOT rendered in `logo` or `visual_signature` states
- Logo removal shows confirmation dialog with warning text
- DELETE /logo only called after user clicks confirm
- Cancel closes dialog without calling DELETE
- `npm run typecheck` passes with no errors
</acceptance_criteria>
</task>

</tasks>

<quality_gate>
- `useIdentityActions` correctly maps the state-action matrix
- "Continuar sem logo" removed from all components and all states
- Guidance card present only in text_only
- Logo removal confirmation shown before DELETE call (UI responsibility)
- Non-identity form elements unchanged
- Initial state (null identity_state) preserves existing behavior
</quality_gate>

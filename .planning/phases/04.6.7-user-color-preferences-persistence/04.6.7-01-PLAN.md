---
phase: 04.6.7-user-color-preferences-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/brand-assets/types.ts
  - src/components/campaign/types.ts
  - src/lib/validators/color.ts
  - src/lib/brand-assets/brand-profiler.ts
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "BrandProfileRecord.brand_colors_chosen type changed from string[] to Array<string | null>"
    - "CreateBrandProfileInput.brand_colors_chosen changed from string[]? to Array<string | null>?"
    - "BrandProfileSnapshot.brand_colors_chosen changed from string[] to Array<string | null>"
    - "manual_color_override has @deprecated JSDoc in types"
    - "Color validators accept null as valid position value (not the whole array, individual positions)"
    - "Normalizer converts [null, null] to []"
    - "All accesses to brand_colors_chosen have null fallback ?? []"
    - "npm run typecheck passes with no errors"
  artifacts:
    - path: "src/lib/brand-assets/types.ts"
      provides: "Updated BrandProfileRecord, CreateBrandProfileInput with Array<string | null>"
    - path: "src/components/campaign/types.ts"
      provides: "Updated BrandProfileSnapshot with Array<string | null>"
    - path: "src/lib/validators/color.ts"
      provides: "Null-aware color validators"
    - path: "src/lib/brand-assets/brand-profiler.ts"
      provides: "previousBrandColors reads brand_colors_chosen directly, not manual_color_override"
  key_links:
    - from: "src/lib/brand-assets/types.ts"
      to: "src/app/api/store/[id]/brand-profile/route.ts"
      via: "Consumed by PATCH handler for validation"
    - from: "src/components/campaign/types.ts"
      to: "src/components/flow/store-identity-form.tsx"
      via: "Consumed by form state and hydration"
    - from: "src/lib/validators/color.ts"
      to: "src/app/api/store/[id]/brand-profile/route.ts"
      via: "Used for PATCH body validation"
---

<objective>
**Plan 01: Foundation — Types, Schemas, Validators & Normalizers**

Update type contracts, validators, and normalizers to support `Array<string | null>` for `brand_colors_chosen`. Deprecate `manual_color_override` at the type level.

**Purpose:** Foundation that Plans 02–04 depend on.
**Output:** Updated type definitions, validators, normalizers, and brand-profiler.
</objective>

<execution_context>
@C:/Projetos/Vendeo V3/.opencode/skills/openspec-apply-change/skill.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.6.7-user-color-preferences-persistence/04.6.7-CONTEXT.md
@openspec/changes/fase-4-6-7-user-color-preferences-persistence/design.md
@openspec/changes/fase-4-6-7-user-color-preferences-persistence/specs/user-color-persistence/spec.md
@openspec/changes/fase-4-6-7-user-color-preferences-persistence/specs/store-brand-profile/spec.md
@openspec/changes/fase-4-6-7-user-color-preferences-persistence/tasks.md

<existing_patterns>
See src/lib/brand-assets/types.ts for existing BrandProfileRecord type
See src/components/campaign/types.ts for existing BrandProfileSnapshot type
See src/lib/validators/color.ts for existing color validation patterns
See src/lib/brand-assets/brand-profiler.ts for existing previousBrandColors logic
</existing_patterns>
</context>

<tasks>

<task type="execute">
<read_first>
src/lib/brand-assets/types.ts
openspec/changes/fase-4-6-7-user-color-preferences-persistence/specs/user-color-persistence/spec.md
</read_first>
<action>
Update `BrandProfileRecord` and `CreateBrandProfileInput` in `src/lib/brand-assets/types.ts`:

1. Change `brand_colors_chosen` type from `string[]` to `Array<string | null>` on `BrandProfileRecord`
2. Change `brand_colors_chosen` type from `string[]?` to `Array<string | null>?` on `CreateBrandProfileInput`
3. Add `@deprecated` JSDoc to `manual_color_override` field(s):
   ```typescript
   /** @deprecated Use brand_colors_chosen instead. Will be removed in a future migration. */
   manual_color_override?: ...
   ```
4. Ensure types export cleanly with no compilation errors
</action>
<acceptance_criteria>
- `BrandProfileRecord.brand_colors_chosen` is `Array<string | null>`
- `CreateBrandProfileInput.brand_colors_chosen` is `Array<string | null>?`
- `manual_color_override` has `@deprecated` JSDoc
- `npm run typecheck` passes with no errors
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/components/campaign/types.ts
</read_first>
<action>
Update `brand_colors_chosen` in `BrandProfileSnapshot`:

Change `brand_colors_chosen` field from `string[]` to `Array<string | null>`.
</action>
<acceptance_criteria>
- `BrandProfileSnapshot.brand_colors_chosen` is `Array<string | null>`
- `npm run typecheck` passes with no errors
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/validators/color.ts
openspec/changes/fase-4-6-7-user-color-preferences-persistence/specs/user-color-persistence/spec.md
</read_first>
<action>
Update color validators in `src/lib/validators/color.ts`:

1. Add a validator for `Array<string | null>` that accepts each element as either a valid 7-character `#RRGGBB` hex string OR `null`
2. Add a normalizer function that converts `[null, null]` → `[]`:
   ```typescript
   function normalizeBrandColorsChosen(colors: Array<string | null>): Array<string | null> {
     if (colors.length === 2 && colors[0] === null && colors[1] === null) return [];
     return colors;
   }
   ```
3. Add `hasUserChosenColors` helper:
   ```typescript
   function hasUserChosenColors(colors: Array<string | null>): boolean {
     return colors.some(c => c !== null && /^#[0-9A-Fa-f]{6}$/.test(c));
   }
   ```
4. Export both functions
</action>
<acceptance_criteria>
- Validator accepts any position as null or valid hex
- Rejects invalid hex values (non-null, non-hex)
- `normalizeBrandColorsChosen([null, null])` returns `[]`
- `normalizeBrandColorsChosen(["#FF0000", null])` returns `["#FF0000", null]`
- `hasUserChosenColors([])` returns `false`
- `hasUserChosenColors(["#FF0000", null])` returns `true`
- `hasUserChosenColors([null, null])` returns `false`
- `npm run typecheck` passes with no errors
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/brand-assets/brand-profiler.ts
openspec/changes/fase-4-6-7-user-color-preferences-persistence/specs/visual-signature-approval/spec.md
</read_first>
<action>
Update `brand-profiler.ts` to read `previousBrandColors` from `brand_colors_chosen` directly instead of `manual_color_override.enabled`:

1. Locate where `previousBrandColors` is determined (currently from `manual_color_override.enabled`)
2. Replace with:
   ```typescript
   const previousBrandColors: Array<string | null> = previousSyncedProfile?.brand_colors_chosen?.some(
     c => c !== null && /^#[0-9A-Fa-f]{6}$/.test(c)
   )
     ? previousSyncedProfile.brand_colors_chosen
     : [];
   ```
3. Do NOT consult `manual_color_override` at all for this decision
4. Ensure all references to `manual_color_override` in this file are replaced with direct array checks (or removed if no longer needed)
</action>
<acceptance_criteria>
- `previousBrandColors` derived from `brand_colors_chosen.some(hasValidHex)` not from `manual_color_override`
- When `brand_colors_chosen = ["#FF6600", null]` → `previousBrandColors = ["#FF6600", null]`
- When `brand_colors_chosen = []` → `previousBrandColors = []`
- `npm run typecheck` passes with no errors
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/brand-assets/types.ts
src/components/campaign/types.ts
src/lib/validators/color.ts
</read_first>
<action>
Audit all files in the repository for accesses to `brand_colors_chosen`:

```bash
rg "brand_colors_chosen" --include '*.ts' --include '*.tsx'
```

For each access point, ensure:
1. If reading the value, add `?? []` fallback for `null`/`undefined`
2. If writing the value, ensure it accepts `Array<string | null>`
3. If comparing against a string, handle `null` as a valid position value

Files to verify (at minimum):
- `src/components/flow/store-identity-form.tsx`
- `src/app/api/store/[id]/brand-profile/route.ts`
- `src/app/api/store/[id]/brand-profile/infer/route.ts`
- `src/app/api/store/[id]/logo/route.ts`
- `src/app/api/store/[id]/visual-signature/approve/route.ts`
- `src/lib/actions/store.ts`
- `src/lib/brand-assets/brand-profiler.ts` (already updated above)
</action>
<acceptance_criteria>
- Every read of `brand_colors_chosen` has `?? []` or equivalent null handling
- Every write of `brand_colors_chosen` accepts `Array<string | null>`
- No compilation errors from type mismatch
- `npm run typecheck` passes with no errors
</acceptance_criteria>
</task>

</tasks>

<quality_gate>
- All type changes compile without errors
- `brand_colors_chosen` is `Array<string | null>` everywhere
- `manual_color_override` is deprecated in types
- Color validators accept null per position
- Normalizer converts `[null, null]` → `[]`
- `hasUserChosenColors` helper exists and is correct
- `brand-profiler.ts` no longer depends on `manual_color_override`
</quality_gate>

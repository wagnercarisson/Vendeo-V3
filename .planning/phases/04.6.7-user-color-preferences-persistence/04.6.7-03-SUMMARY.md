# Plan 03 Summary: UI — Form State, Hydration, Color Pickers & Voltar

**Phase:** 04.6.7-user-color-preferences-persistence
**Status:** Complete
**Committed:** 348075f

## Changes Made

- Migrated `brandColorsChosen` state from `string[]` to `Array<string | null>`
- Updated `saveBrandColors` to build canonical `[primaryOuNull, accentOuNull]` pair with null for empty/placeholder/invalid
- Added `handlePatchOrLocalColors` helper: PATCH if synced profile exists, local state if not
- Added `handleResetToSuggestedColors` for "Voltar para cores sugeridas" button
- Added "Voltar para cores sugeridas" button visible only when `hasUserChosenColors === true`
- Updated color picker onChange/onBlur to persist canonical pair via the helper
- Updated `executeStep2Save` / `handleStep2Submit` to send `userChosenColors` not `manualColorOverride`
- Updated `handleContinueWithoutLogo` to send `userChosenColors`
- Updated `handleRemoveLogo` to NOT clear `brandColorsChosen`
- Updated `handleApprovalComplete` to NOT set `brand_colors_chosen` from inferred VS colors
- Updated hydration in loading useEffect to respect null positions
- Updated `handleRetryBrandDirector` hydration with null-aware logic
- Updated drift modals and `onRealinhado` callback with null-aware hydration

## Files Modified

- `src/components/flow/store-identity-form.tsx`

## Quality Gate

- [x] `brandColorsChosen` type migrated to `Array<string | null>` everywhere
- [x] Color picker hydration handles null positions
- [x] "Voltar para cores sugeridas" renders conditionally and resets correctly
- [x] onChange/onBlur persist correctly based on profile existence
- [x] Logo removal does not affect color choices
- [x] `manualColorOverride` not sent in any request
- [x] `npm run typecheck` passes

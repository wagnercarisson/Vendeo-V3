---
phase: 04.6.7-user-color-preferences-persistence
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/flow/store-identity-form.tsx
autonomous: true
requirements: []
gap_closure: true
closes:
  - truth: "Partial color choice: accent (untouched) should remain null in brand_colors_chosen"
    test: 2
    severity: major
must_haves:
  truths:
    - "After inference fills accentColor with an inferred value, save paths use brandColorsChosen[1] (canonical source for user choices) instead of accentColor state"
    - "When user has never touched the accent picker, brandColorsChosen[1] remains null after save"
    - "When user HAS touched the accent picker, brandColorsChosen[1] reflects their choice after save"
  artifacts:
    - path: "src/components/flow/store-identity-form.tsx"
      provides: "4 save sites fixed to use brandColorsChosen[1] instead of accentColor"
  key_links:
    - from: "executeStep2Save"
      to: "brandColorsChosen[1]"
      via: "saveBrandColors call"
    - from: "Primary picker onChange/onBlur"
      to: "brandColorsChosen[1]"
      via: "handlePatchOrLocalColors call"
    - from: "Detected colors 'P' button"
      to: "brandColorsChosen[1]"
      via: "saveBrandColors call"
---

<objective>
Fix a dual-use anti-pattern where `accentColor` state serves both UX display AND persistence source.

**Root cause:** After inference fills `accentColor` with an inferred value, 4 save paths in `store-identity-form.tsx` read `accentColor` directly instead of consulting `brandColorsChosen[1]` (the canonical source for user choices). The backend routes are correct — the fix is fully contained in the frontend component.

**Purpose:** Ensure partial color choice works: accent color not touched by user remains `null` in `brand_colors_chosen` even after inference runs and fills the display-only `accentColor` state.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04.6.7-user-color-preferences-persistence/04.6.7-UAT.md

## Relevant Source Context

The `accentColor` state (line 84) serves **UX display** — it drives the color picker value, preview rendering, and the accent-matching class on detected color "S" buttons. It is correctly hydrated from `chosen[1]` during profile load (line 773-776).

The `brandColorsChosen` state (line 85) serves as the **canonical persistence source** for user color choices. Array of `[primary, accent]` where each element is either a hex string (user chose it) or `null` (user never chose it).

### `saveBrandColors` function (line 222)
```typescript
// Takes (primary: string, secondary: string), normalizes both internally
// If secondary is empty, placeholder, or invalid hex → sends null in the pair
const saveBrandColors = useCallback(async (primary: string, secondary: string) => {
  const primaryOuNull = primary === '' || primary === '#RRGGBB' || !isValidHex(primary) ? null : primary;
  const accentOuNull = secondary === '' || secondary === '#RRGGBB' || !isValidHex(secondary) ? null : secondary;
  const colors: Array<string | null> = primaryOuNull || accentOuNull ? [primaryOuNull, accentOuNull] : [];
  ...
}, [storeId]);
```

### Sites to fix (all in store-identity-form.tsx)

| # | Line | Current code | Issue |
|---|------|-------------|-------|
| 1 | 844-846 | `if (formData.brand_color \|\| accentColor) { saveBrandColors(formData.brand_color, accentColor); }` | Guard + save use display state, not canonical source |
| 2 | 1428 | `const accentOuNull = accentColor === '' \|\| ... ? null : accentColor;` | Accent value read from display state |
| 3 | 1442 | Same pattern as #2 in onBlur handler | Accent value read from display state |
| 4 | 1511 | `saveBrandColors(color, accentColor)` | Accent value read from display state |

### Dependency array to update
Line 904 references `accentColor` in the `useCallback` deps of `executeStep2Save` — must be replaced with `brandColorsChosen`.

### Confirmed correct (no changes needed)
- **Accent picker onChange** (line 1462-1469): Uses `e.target.value` directly ✓
- **Accent text input onBlur** (line 1475-1481): Uses `accentColor` with `isValidHex` check — this IS the user interaction that updates accent, so reading `accentColor` here is correct ✓
- **UX display** (lines 1461, 1473, 1517, 1570): `accentColor` used for picker values, matching class, and preview prop — these are display-only, correct ✓
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Replace accentColor with brandColorsChosen[1] in 4 save sites + dependency array</name>
  <files>src/components/flow/store-identity-form.tsx</files>
  <action>
    Make 5 targeted edits in `src/components/flow/store-identity-form.tsx`. The fix pattern is the same everywhere: where `accentColor` is used as the persistence source for the accent value, replace with `brandColorsChosen[1]` (canonical user choice source).

    All other uses of `accentColor` (picker value display, matching class, preview prop) remain untouched.

    **Edit 1 — `executeStep2Save` guard + save call (lines 844-846):**
    - Guard condition: `if (formData.brand_color || accentColor)` → use `brandColorsChosen[1]` (falsy for null/undefined → guards correctly)
    - Save call: `saveBrandColors(formData.brand_color, accentColor)` → `saveBrandColors(formData.brand_color, brandColorsChosen[1] ?? "")`
    - Use `?? ""` because `saveBrandColors` expects `string` for the secondary param (it normalizes internally — `""` will be converted to null by its validation logic)

    **Edit 2 — Primary picker onChange `handlePatchOrLocalColors` call (line 1428):**
    Current: `const accentOuNull = accentColor === '' || accentColor === '#RRGGBB' || !isValidHex(accentColor) ? null : accentColor;`
    Replace with: `const currentAccent = brandColorsChosen[1]; const accentOuNull = currentAccent && isValidHex(currentAccent) ? currentAccent : null;`
    This handles undefined, null, empty, placeholder, and invalid hex in one expression. The `&&` short-circuits for null/undefined, then `isValidHex` rejects invalid strings (including `#RRGGBB`).

    **Edit 3 — Primary picker onBlur `handlePatchOrLocalColors` call (line 1442):**
    Same pattern as Edit 2. Replace with: `const currentAccent = brandColorsChosen[1]; const accentOuNull = currentAccent && isValidHex(currentAccent) ? currentAccent : null;`

    **Edit 4 — Detected colors "P" button (line 1511):**
    Current: `saveBrandColors(color, accentColor)`
    Replace with: `saveBrandColors(color, brandColorsChosen[1] ?? "")`

    **Edit 5 — `executeStep2Save` dependency array (line 904):**
    Current: `[storeId, formData, accentColor, logoStatus, ...]`
    Replace `accentColor` with `brandColorsChosen` in the array, since the callback now reads `brandColorsChosen[1]`.

    Do NOT touch:
    - Line 1461 — accent picker `value={accentColor}` (display)
    - Line 1473 — accent text input `value={accentColor}` (display)
    - Line 1517 — `accentColor === color` matching class (display)
    - Line 1570 — `accentColor={accentColor}` preview prop (display)
    - Lines 1462-1469 — accent picker onChange (uses `e.target.value`, correct)
    - Lines 1475-1481 — accent text input onBlur (uses `accentColor` at the moment of user input, correct)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | Out-Null; if ($LASTEXITCODE -eq 0) { Write-Host "Typecheck passes" } else { npx tsc --noEmit }</automated>
    <automated>npx jest --passWithNoTests src/lib/validators/__tests__/color.test.ts 2>&1</automated>
  </verify>
  <done>
    - TypeScript compilation passes with no errors
    - `color.test.ts` all tests pass
    - All 4 save paths use `brandColorsChosen[1]` instead of `accentColor`
    - `executeStep2Save` dependency array updated to include `brandColorsChosen` instead of `accentColor`
    - UX display uses of `accentColor` (picker values, matching classes, preview prop) remain unchanged
  </done>
</task>

</tasks>

<threat_model>
No new trust boundaries introduced — this is a pure frontend state fix with no API changes. The PATCH and inference routes are unchanged.

**T-04.6.7-SC package gate:** This plan does not install any packages. No package legitimacy audit required.
</threat_model>

<verification>
1. `npx tsc --noEmit` passes — all types align after the `brandColorsChosen[1]` substitution
2. `npx jest src/lib/validators/__tests__/color.test.ts` passes — validator logic unchanged
3. Manual UAT test 2: open store identity form → set only primary color → leave accent untouched → trigger save → reload → verify `brand_colors_chosen[1]` is null (inferred color shows in picker but IS NOT persisted as a user choice)
</verification>

<success_criteria>
- Gap UAT test 2 passes: partial color choice (primary only) persists with `brand_colors_chosen[1]` as null after inference and save
- No regression on UAT test 1: both colors set by user still persist correctly
- All 4 save sites use `brandColorsChosen[1]` as their accent value source
- UX display of accentColor state is unaffected (picker, preview, matching class all unchanged)
</success_criteria>

<output>
Create `.planning/phases/04.6.7-user-color-preferences-persistence/04.6.7-05-SUMMARY.md` when done
</output>

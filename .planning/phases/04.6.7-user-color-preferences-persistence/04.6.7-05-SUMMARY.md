# Plan 05 Summary: Gap Closure — Inferred Accent Not Persisted as User Choice

**Phase:** 04.6.7-user-color-preferences-persistence
**Status:** Complete
**Committed:** [after next commit]

## Changes Made

- Fixed `executeStep2Save` to use `brandColorsChosen[1]` instead of `accentColor` for save (line 844-846)
- Fixed primary color picker onChange to use `brandColorsChosen[1]` instead of `accentColor` (line 1428)
- Fixed primary color text input onBlur to use `brandColorsChosen[1]` instead of `accentColor` (line 1442)
- Fixed detected colors "P" button to use `brandColorsChosen[1]` instead of `accentColor` (line 1511)
- Updated `executeStep2Save` dependency array from `accentColor` to `brandColorsChosen` (line 904)

## Root Cause

`accentColor` state served dual purpose — UX display AND persistence source. After inference filled `accentColor`, 4 save paths read `accentColor` directly instead of `brandColorsChosen[1]` (canonical user choice source).

## Files Modified

- `src/components/flow/store-identity-form.tsx`

## Files Created

- `.planning/debug/brand-colors-chosen-inferred-accent.md`

## Quality Gate

- [x] `npm run typecheck` passes
- [x] `npm run test` passes
- [x] All 4 save sites use `brandColorsChosen[1]` as accent value source
- [x] UX display of `accentColor` unaffected (picker values, matching classes, preview prop unchanged)

---
status: diagnosed
phase: 04.6.7-user-color-preferences-persistence
source:
  - 04.6.7-01-SUMMARY.md
  - 04.6.7-02-SUMMARY.md
  - 04.6.7-03-SUMMARY.md
  - 04.6.7-04-SUMMARY.md
started: 2026-06-25T19:21:00.000Z
updated: 2026-06-25T19:30:00.000Z
---

## Current Test

number: 5
name: Color Choice Survives Logo Removal
expected: |
  With colors set and a logo active, remove the logo. Color choices remain unchanged in the pickers.
awaiting: user response

## Tests

### 1. Set Primary and Accent Colors
expected: Open store identity form, pick primary and accent colors using color pickers. Colors display correctly in pickers. Navigate away and back — colors remain as chosen.
result: pass

### 2. Partial Color Choice (Primary Only)
expected: Set only primary color, leave accent untouched. After inference and reload: picker primário exibe a cor escolhida, picker destaque exibe cor inferida (apresentação), mas brand_colors_chosen[1] permanece null.
result: pass
note: "Retested after fix — inferred accent no longer persisted as user choice"

### 3. Reset to Suggested Colors ("Voltar para cores sugeridas")
expected: With active color choices, click "Voltar para cores sugeridas" button (visible only when hasUserChosenColors=true). Color pickers reset to safe_color_tokens. Button disappears.
result: pass
note: "Fix applied: executeStep2Save guard changed to hasUserChosenColors(brandColorsChosen)"

### 4. Color Choice Survives Logo Upload
expected: Set colors on store identity form. Upload a logo. After logo upload completes, color pickers still show the chosen colors (not reset to empty/default).
result: pass

### 5. Color Choice Survives Logo Removal
expected: With colors set and a logo active, remove the logo. Color choices remain unchanged in the pickers and preview.
result: issue
reported: "preview mostra cores inferidas em vez das escolhidas pelo user após remover logo. Acertar hidratação, cuidando de color chosen com valor null"
severity: major

### 6. Color Choice Survives Visual Signature Approval
expected: With colors set, approve a visual signature. After approval, color pickers maintain the user's chosen colors (not overwritten by VS inferred colors).
result: [pending]

### 7. PATCH API Accepts and Persists Colors
expected: PATCH /api/store/[id]/brand-profile with `{ colors: ["#FF0000", null] }` returns 200. Subsequent GET shows `brand_colors_chosen: ["#FF0000", null]`. `manual_color_override` not written.
result: [pending]

### 8. PATCH Reset Colors
expected: PATCH /api/store/[id]/brand-profile with `{ colors: [] }` returns 200. `brand_colors_chosen` is empty. "Voltar para cores sugeridas" button hidden after reload.
result: [pending]

## Summary

total: 8
passed: 4
issues: 0
pending: 4
skipped: 0
blocked: 0

## Gaps

- truth: "Voltar para cores sugeridas: brand_colors_chosen cleared to [] and button hidden after save"
  status: failed
  reason: "User reported: em tela o fluxo funcionou mas, após salvar step 2, o botão continua aparecendo e colors_chosen mantém a escolha anterior"
  severity: major
  test: 3
  root_cause: "executeStep2Save guard at line 844 checked formData.brand_color || brandColorsChosen[1]. After reset, formData.brand_color was set to safe_color_tokens.primary, so save re-persisted it as user choice."
  artifacts:
    - path: "src/components/flow/store-identity-form.tsx"
      issue: "executeStep2Save guard at line 844"
  missing:
    - "Guard should use hasUserChosenColors(brandColorsChosen) which returns false after reset (brandColorsChosen = [])"
  debug_session: ""
- truth: "Partial color choice: accent (untouched) should remain null in brand_colors_chosen"
  status: failed
  reason: "User reported: a cor destaque (não tocada) foi persistida em colors_chosen"
  severity: major
  test: 2
  root_cause: "Dual-use anti-pattern: accentColor state serves both UX display AND persistence source. After inference fills accentColor with inferred value, save paths read accentColor directly instead of brandColorsChosen[1] (canonical user choice source)."
  artifacts:
    - path: "src/components/flow/store-identity-form.tsx"
      issue: "executeStep2Save reads accentColor (~line 844-846)"
    - path: "src/components/flow/store-identity-form.tsx"
      issue: "Primary picker onChange reads accentColor (~line 1428)"
    - path: "src/components/flow/store-identity-form.tsx"
      issue: "Primary picker onBlur reads accentColor (~line 1442)"
    - path: "src/components/flow/store-identity-form.tsx"
      issue: "Detected color 'P' button reads accentColor (~line 1511)"
  missing:
    - "executeStep2Save should use brandColorsChosen[1] instead of accentColor"
    - "Primary picker onChange should use brandColorsChosen[1] instead of accentColor"
    - "Primary picker onBlur should use brandColorsChosen[1] instead of accentColor"
    - "Detected color 'P' button should use brandColorsChosen[1] instead of accentColor"
  debug_session: ".planning/debug/brand-colors-chosen-inferred-accent.md"

# Fixed during automated verification:

- Fixed `validateBrandColorsChosen` in `src/lib/validators/color.ts:19` — added `colors.length !== 0 && colors.length !== 2` check (was only rejecting length 1, not length >2). All 17 tests pass, typecheck passes.

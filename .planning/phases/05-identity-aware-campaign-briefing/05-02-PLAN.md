---
phase: 05-identity-aware-campaign-briefing
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/**/__tests__/**/*.test.ts
  - src/lib/actions/__tests__/store.test.ts
  - src/lib/image-generation/providers/__tests__/openai.test.ts
  - src/app/api/store/[id]/__tests__/route.test.ts
  - src/app/api/campaign/generate-image/__tests__/route.test.ts
  - src/app/campaign/preview/__tests__/page.test.ts
autonomous: false
requirements: []

must_haves:
  truths:
    - "resolveStoreIdentity tests cover text_only, logo (with/without asset), visual_signature (with/without asset)"
    - "buildCampaignBrief tests cover all 5 directive scenarios"
    - "validateIdentityReference tests cover valid URL, unreachable URL, null URL"
    - "Provider tests: identityImageUrl sent on primary path, [productFile, identityFile] on fallback"
    - "GET /api/store/:id tests: enriched response with identity, 404 for missing"
    - "POST /api/campaign/generate-image tests: storeId body (pass), legacy fields body (400)"
    - "Regression prompt test: equivalent before/after for logo, text_only, VS (except identityDirective and logoVariantUrl removal)"
    - "storeIdentity in result event contains authoritative snapshot"
    - "resolveStoreIdentity logo variant priority: normalized → original → on_dark"
    - "Preview legacy normalization tests: all 3 legacy formats"
    - "API error in form: error message displayed + retry preserved"
    - "TypeScript, lint, build passing"
    - "Manual validation: 5 flows (text_only, logo, VS, logo removal, broken asset)"
  artifacts:
    - path: "src/lib/actions/__tests__/store.test.ts"
      provides: "resolveStoreIdentity, validateIdentityReference, buildCampaignBrief tests"
    - path: "src/lib/image-generation/providers/__tests__/openai.test.ts"
      provides: "Provider identity image tests"
    - path: "src/app/api/__tests__/**"
      provides: "API endpoint tests"
    - path: "src/app/campaign/preview/__tests__/page.test.ts"
      provides: "Legacy normalization tests"
---

<objective>
**Plan 02: Validation — Wave 2**

Implementar testes automáticos para toda a cadeia de identidade e validar manualmente os 5 fluxos críticos.

**Purpose:** Garantir que a centralização da identidade não quebrou fluxos existentes e que os novos comportamentos (5 directives, validação de referência, normalização de preview) funcionam corretamente.
**Output:** Testes automatizados (30+ cenários), validação manual (5 fluxos), typecheck + lint + build passing.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-identity-aware-campaign-briefing/05-CONTEXT.md
@openspec/changes/fase-5-0-identity-aware-campaign-briefing/design.md
@openspec/changes/fase-5-0-identity-aware-campaign-briefing/tasks.md
@openspec/changes/fase-5-0-identity-aware-campaign-briefing/specs/store-identity-foundation/spec.md
@openspec/changes/fase-5-0-identity-aware-campaign-briefing/specs/identity-aware-campaign-briefing/spec.md
@openspec/changes/fase-5-0-identity-aware-campaign-briefing/specs/ai-image-generation/spec.md
@openspec/changes/fase-5-0-identity-aware-campaign-briefing/specs/campaign-preview-page/spec.md

<existing_patterns>
See existing test files in `src/**/__tests__/` for test patterns and framework conventions
See `vitest.config.ts` for test configuration
</existing_patterns>
</context>

<tasks>

<task type="execute">
<read_first>
src/lib/actions/store.ts
src/lib/actions/__tests__/ (if exists)
src/components/campaign/types.ts
openspec/changes/fase-5-0-identity-aware-campaign-briefing/specs/store-identity-foundation/spec.md
openspec/changes/fase-5-0-identity-aware-campaign-briefing/specs/identity-aware-campaign-briefing/spec.md
</read_first>
<action>
Criar/atualizar testes em `src/lib/actions/__tests__/store.test.ts`:

1. **resolveStoreIdentity — text_only**: identityState='text_only', signature.url=null, brandProfile incluso
2. **resolveStoreIdentity — logo com asset**: identityState='logo', signature.url=URL_do_logo
3. **resolveStoreIdentity — logo sem asset**: identityState='logo', signature.url=null, diagnóstico
4. **resolveStoreIdentity — VS com asset**: identityState='visual_signature', signature.url=URL_da_VS
5. **resolveStoreIdentity — VS sem asset**: identityState='visual_signature', signature.url=null, diagnóstico
6. **resolveStoreIdentity — variante logo**: prioridade normalized → original → on_dark
7. **validateIdentityReference — URL válida**: snapshot inalterado (cópia)
8. **validateIdentityReference — URL irrecuperável**: signature.url null, identityState preservado
9. **validateIdentityReference — null URL**: retorna cópia sem fetch
10. **buildCampaignBrief — 5 directives**: cada combinação (state, asset) produz directive exata
11. **buildCampaignBrief — pass-through sem defaults nem storeId**: campaignInput com todos os campos presentes passa sem modificação; campaignInput com campos ausentes (undefined) preserva undefined, sem defaults aplicados; storeId NÃO está presente em campaignInput
</action>
<acceptance_criteria>
- 11 cenários de teste implementados
- Mock Supabase/fetch conforme necessário (vi.mock, vi.fn)
- Testes passam com `npm test` ou `npx vitest run`
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/image-generation/providers/openai.ts
src/lib/image-generation/providers/types.ts
openspec/changes/fase-5-0-identity-aware-campaign-briefing/specs/ai-image-generation/spec.md
openspec/changes/fase-5-0-identity-aware-campaign-briefing/specs/generation-retry-fallback/spec.md
</read_first>
<action>
Criar/atualizar testes para o provider OpenAI:

1. **identityImageUrl no primary path**: attempt=0 envia identityImageUrl como input_image
2. **Sem identityImageUrl**: não envia extra image
3. **Fallback com identityFile**: attempt>=1 faz fetch da URL validada e envia [productFile, identityFile]
4. **Fallback sem identity**: attempt>=1 com identityImageUrl=undefined envia só productFile
5. **Fallback fetch failure**: retorna terminal error (não degradação silenciosa)
</action>
<acceptance_criteria>
- 5 cenários de teste implementados
- Mock OpenAI SDK conforme necessário
- Testes passam
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/app/api/store/[id]/route.ts
openspec/changes/fase-5-0-identity-aware-campaign-briefing/specs/store-identity-foundation/spec.md
</read_first>
<action>
Criar/atualizar testes para GET /api/store/:id:

1. Store existente retorna 200 com identity no response
2. Store inexistente retorna 404
3. identity.identityState presente no response
</action>
<acceptance_criteria>
- 3 cenários de teste
- Testes passam
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/app/api/campaign/generate-image/route.ts
src/lib/image-generation/schema.ts
openspec/changes/fase-5-0-identity-aware-campaign-briefing/specs/ai-image-generation/spec.md
</read_first>
<action>
Criar/atualizar testes para POST /api/campaign/generate-image:

1. Body com storeId válido → 200 + NDJSON stream com type:"result" incluindo storeIdentity
2. Body com campos legados (storeName, storeLogoUrl, brandProfile) → 400
3. Body sem storeId → 400
4. Body sem productImageDataUrl → 400
5. storeIdentity no result event contém o snapshot autoritativo
</action>
<acceptance_criteria>
- 5 cenários de teste
- Testes passam
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/app/campaign/preview/page.tsx
openspec/changes/fase-5-0-identity-aware-campaign-briefing/specs/campaign-preview-page/spec.md
</read_first>
<action>
Criar testes para normalização de preview legado:

1. Legacy com logoUrl: identityState='logo', signature.url=logoUrl
2. Legacy com visualSignatureUrl (sem logoUrl): identityState='visual_signature'
3. Legacy sem ativos: identityState='text_only', signature=null
</action>
<acceptance_criteria>
- 3 cenários de teste
- Testes passam
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/**/prompts/** or prompts/
</read_first>
<action>
Criar teste de regressão de prompt:

Validar que para a mesma entrada (store + campaign):
- Logo store: prompt equivalente antes/depois, exceto `identityDirective` substituindo instrução fixa e remoção de `logoVariantUrl`
- text_only store: equivalente exceto `identityDirective`
- VS store: equivalente exceto `identityDirective` e VS URL enviado como identity (não era antes)

Isso pode ser um teste de snapshot do prompt gerado ou uma asserção de que as variáveis existentes estão presentes e a única diferença é a directive.
</action>
<acceptance_criteria>
- Teste de regressão para logo, text_only e VS
- Apenas identityDirective e remoção de logoVariantUrl diferem
- Teste passa
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/components/flow/campaign-input-form.tsx
src/components/flow/use-campaign-form.ts
</read_first>
<action>
Criar teste para erro de API no formulário:

1. Simular erro na chamada POST /api/campaign/generate-image
2. Mensagem de erro é exibida
3. Opção de retry está preservada
</action>
<acceptance_criteria>
- Teste de erro + retry implementado
- Teste passa
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
</read_first>
<action>
Run verification commands:

```bash
npm run typecheck
npm run lint
npm run build
```

All must pass with zero errors.
</action>
<acceptance_criteria>
- `npm run typecheck` exits 0
- `npm run lint` exits 0
- `npm run build` exits 0
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
</read_first>
<action>
Documentar cenários de validação manual:

1. **Fluxo text_only**: Cadastrar loja sem logo, inferir brand profile, gerar campanha — sem logo, sem VS, brand profile como contexto direcional não obrigatório
2. **Fluxo logo**: Cadastrar loja com logo, brand profile analisado, gerar campanha — logotipo como assinatura
3. **Fluxo VS**: Cadastrar loja sem logo, gerar VS, aprovar, gerar campanha — VS como assinatura, sem logo
4. **Remoção de logo**: Remover logo, gerar campanha — sem logotipo, brand profile original preservado, directive "não inventar"
5. **Asset quebrado**: Corromper URL do logo no banco, gerar campanha — sem logotipo, identity_state permanece 'logo', diagnóstico registrado
</action>
<acceptance_criteria>
- 5 cenários documentados como checklist
- Cada cenário tem passo-a-passo e resultado esperado
</acceptance_criteria>
</task>

</tasks>

<quality_gate>
- [ ] 11 testes de resolveStoreIdentity/buildCampaignBrief/validateIdentityReference
- [ ] 5 testes de provider OpenAI (identityImageUrl, fallback, erro)
- [ ] 3 testes de GET /api/store/:id
- [ ] 5 testes de POST /api/campaign/generate-image
- [ ] 3 testes de normalização de preview legado
- [ ] 3 testes de regressão de prompt (logo, text_only, VS)
- [ ] 1 teste de erro da API no formulário
- [ ] npm run typecheck passa
- [ ] npm run lint passa
- [ ] npm run build passa
- [ ] 5 cenários de validação manual documentados
</quality_gate>

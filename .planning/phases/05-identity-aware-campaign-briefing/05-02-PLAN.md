---
phase: 05-identity-aware-campaign-briefing
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/lib/image-generation/schema.ts
  - src/app/api/campaign/generate-image/route.ts
  - src/lib/image-generation/providers/types.ts
  - src/lib/image-generation/providers/openai.ts
  - src/lib/image-generation/services/image-generation-service.ts
  - prompts/campaign-image-director.md
  - src/components/flow/campaign-input-form.tsx
  - src/components/flow/use-campaign-form.ts
  - src/components/campaign/campaign-renderer.tsx
  - src/components/flow/store-visual-signature-section.tsx
  - src/app/campaign/preview/page.tsx
autonomous: false
requirements: []

must_haves:
  truths:
    - "GenerateImageRequestSchema requires storeId (UUID) + product fields; rejects storeName, storeSegment, storeTone, brandColor, storeLogoUrl, brandProfile"
    - "POST /api/campaign/generate-image calls resolveStoreIdentity → validateIdentityReference → buildCampaignBrief → ImageGenerationService"
    - "ImageProviderInput uses identityImageUrl (not logoImageUrl)"
    - "OpenAIImageProvider sends identityImageUrl as input_image (attempt 0), detail: low"
    - "FallbackToImageApi fetches identity URL, converts with toFile, sends [productFile, identityFile]"
    - "ImageGenerationService accepts CampaignBrief, injects identityDirective in buildPromptVariables"
    - "buildPromptVariables does NOT include identityImageUrl (goes only to provider)"
    - "brandProfileSection does NOT include logoVariantUrl"
    - "campaign-image-director.md: fixed logo instruction replaced by {{identityDirective}}"
    - "CampaignInputForm props: storeId instead of storeIdentity"
    - "useCampaignForm: body with storeId + product fields (no storeLogoUrl, brandProfile)"
    - "CampaignRenderer uses signature.url + signature.type"
    - "StoreVisualSignatureSection uses signature unificado"
    - "/campaign/preview normalizes legacy payload (no identityState)"
    - "type: 'result' event includes storeIdentity snapshot"
    - "npm run typecheck passes"
    - "npm run lint passes"
  artifacts:
    - path: "src/lib/image-generation/schema.ts"
      provides: "GenerateImageRequestSchema 2.0"
    - path: "src/app/api/campaign/generate-image/route.ts"
      provides: "New pipeline with identity resolution"
    - path: "src/lib/image-generation/providers/types.ts"
      provides: "identityImageUrl in ImageProviderInput"
    - path: "src/lib/image-generation/providers/openai.ts"
      provides: "identityImageUrl as input_image, fallback with identityFile"
    - path: "src/lib/image-generation/services/image-generation-service.ts"
      provides: "CampaignBrief input, identityDirective injection"
    - path: "prompts/campaign-image-director.md"
      provides: "{{identityDirective}} replacing fixed logo instruction"
    - path: "src/components/flow/campaign-input-form.tsx"
      provides: "storeId prop instead of storeIdentity"
    - path: "src/components/flow/use-campaign-form.ts"
      provides: "Body with storeId + product fields"
    - path: "src/components/campaign/campaign-renderer.tsx"
      provides: "Uses signature.url + signature.type"
    - path: "src/components/flow/store-visual-signature-section.tsx"
      provides: "Uses signature unificado"
    - path: "src/app/campaign/preview/page.tsx"
      provides: "Legacy payload normalization"
---

<objective>
**Plan 02: Generation Pipeline — Wave 2**

Atualizar o pipeline de geração para usar o novo contrato de identidade: schema reformulado, endpoint com resolução, provider com identityImageUrl, prompt com directive injetada, componentes consumidores e normalização de preview.

**Purpose:** Conectar o contrato de identidade ao pipeline de geração real, garantindo que a directive correta seja injetada no prompt e o asset correto enviado ao provider.
**Output:** Schema, endpoint, provider, prompt, componentes e preview atualizados.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-identity-aware-campaign-briefing/05-CONTEXT.md
@openspec/changes/fase-5-0-identity-aware-campaign-briefing/design.md
@openspec/changes/fase-5-0-identity-aware-campaign-briefing/tasks.md
@openspec/changes/fase-5-0-identity-aware-campaign-briefing/specs/ai-image-generation/spec.md
@openspec/changes/fase-5-0-identity-aware-campaign-briefing/specs/campaign-input-ui/spec.md
@openspec/changes/fase-5-0-identity-aware-campaign-briefing/specs/campaign-visual-renderer/spec.md
@openspec/changes/fase-5-0-identity-aware-campaign-briefing/specs/creative-direction-context/spec.md
@openspec/changes/fase-5-0-identity-aware-campaign-briefing/specs/generation-retry-fallback/spec.md
@openspec/changes/fase-5-0-identity-aware-campaign-briefing/specs/campaign-preview-page/spec.md

<existing_patterns>
See `src/lib/image-generation/schema.ts` for current GenerateImageRequestSchema
See `src/app/api/campaign/generate-image/route.ts` for current POST endpoint
See `src/lib/image-generation/providers/types.ts` for current ImageProviderInput
See `src/lib/image-generation/providers/openai.ts` for current OpenAIImageProvider
See `src/lib/image-generation/services/image-generation-service.ts` for current service
See `prompts/campaign-image-director.md` for current prompt template
See `src/components/flow/campaign-input-form.tsx` for current form
See `src/components/flow/use-campaign-form.ts` for current hook
See `src/components/campaign/campaign-renderer.tsx` for current renderer
See `src/components/flow/store-visual-signature-section.tsx` for current section
See `src/app/campaign/preview/page.tsx` for current preview page
</existing_patterns>
</context>

<tasks>

<task type="execute">
<read_first>
src/lib/image-generation/schema.ts
openspec/changes/fase-5-0-identity-aware-campaign-briefing/specs/ai-image-generation/spec.md
</read_first>
<action>
Reformular `GenerateImageRequestSchema` em `src/lib/image-generation/schema.ts`:

1. Adicionar campo obrigatório: `storeId: z.string().uuid()`
2. Manter todos os campos de produto/campanha existentes (productName, originalPriceCents, discountedPriceCents, badgeText, description, productImageDataUrl, inputValidationOverride, hook, cta, objective, campaignDetails, additionalDetails, targetChannel, format, validity, availabilityNotes, sensitiveConstraints)
3. REMOVER campos: `storeName`, `storeSegment`, `storeTone`, `brandColor`, `storeLogoUrl`, `brandProfile`
4. Schema rejeita campos desconhecidos (strict mode) OU valida explicitamente que campos removidos não estão presentes
5. Extrair tipo `CampaignInput` do schema para uso no `buildCampaignBrief`
</action>
<acceptance_criteria>
- Schema aceita `storeId` + product fields
- Schema rejeita (400) se `storeName`, `storeLogoUrl`, `brandProfile` presentes
- Schema rejeita (400) se `storeId` ausente
- `npm run typecheck` passa
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/app/api/campaign/generate-image/route.ts
src/lib/actions/store.ts
src/lib/image-generation/schema.ts
src/lib/image-generation/services/image-generation-service.ts
openspec/changes/fase-5-0-identity-aware-campaign-briefing/specs/ai-image-generation/spec.md
</read_first>
<action>
Atualizar `POST /api/campaign/generate-image/route.ts`:

1. Receber `storeId` do body validado
2. Chamar pipeline: `resolveStoreIdentity(storeId)` → `validateIdentityReference(snapshot)` → `buildCampaignBrief(snapshot, campaignInput)` → `ImageGenerationService.generateImage(CampaignBrief)`
3. Incluir `storeIdentity` no `type: "result"` event do NDJSON stream: `{ type: "result", success: true, imageDataUrl: "...", storeIdentity: { ... } }`
4. Retornar 400 se `storeId` ou `productImageDataUrl` ausentes
5. Retornar 400 se campos identity legados estiverem presentes
6. Manter toda a lógica existente de streaming NDJSON, phase events, validation, error handling
</action>
<acceptance_criteria>
- POST com `storeId` válido + product fields → 200 + NDJSON stream com `type: "result"` incluindo `storeIdentity`
- POST sem `storeId` → 400
- POST com `storeName`/`storeLogoUrl`/`brandProfile` → 400
- `storeIdentity` no result event contém identityState e signature
- `npm run typecheck` passa
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/image-generation/providers/types.ts
openspec/changes/fase-5-0-identity-aware-campaign-briefing/specs/ai-image-generation/spec.md
</read_first>
<action>
Atualizar `ImageProviderInput` em `src/lib/image-generation/providers/types.ts`:

1. Renomear `logoImageUrl?` para `identityImageUrl?` (string, optional)
2. Atualizar comentário: "Carries the logo or visual signature URL resolved by identity state. When absent, no identity image SHALL be sent."
3. Manter todos os outros campos inalterados (prompt, productImageDataUrl, size, quality, signal, attempt)
</action>
<acceptance_criteria>
- `logoImageUrl` não existe mais em `ImageProviderInput`
- `identityImageUrl` existe como `string | undefined`
- `npm run typecheck` passa
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/image-generation/providers/openai.ts
openspec/changes/fase-5-0-identity-aware-campaign-briefing/specs/ai-image-generation/spec.md
openspec/changes/fase-5-0-identity-aware-campaign-briefing/specs/generation-retry-fallback/spec.md
</read_first>
<action>
Atualizar `OpenAIImageProvider` em `src/lib/image-generation/providers/openai.ts`:

1. Renomear `logoImageUrl` para `identityImageUrl` em todas as referências
2. No primary path (attempt 0, Responses API): enviar `identityImageUrl` como `input_image` com `detail: "low"`
3. No fallback path (`fallbackToImageApi`, attempt >= 1): fazer fetch da URL validada, converter com `toFile`, enviar `[productFile, identityFile]` no `images.edit`
4. Se `identityImageUrl` for undefined/null: não enviar identity image
5. Se fetch da URL falhar no fallback: retornar erro controlado (terminal error), sem degradação silenciosa
</action>
<acceptance_criteria>
- `identityImageUrl` enviado como `input_image` no attempt 0
- Fallback envia `[productFile, identityFile]` (com identity)
- Sem identityImageUrl: não envia extra image
- Fetch identity URL falha no fallback → terminal error
- `npm run typecheck` passa
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/image-generation/services/image-generation-service.ts
openspec/changes/fase-5-0-identity-aware-campaign-briefing/specs/ai-image-generation/spec.md
openspec/changes/fase-5-0-identity-aware-campaign-briefing/specs/creative-direction-context/spec.md
</read_first>
<action>
Atualizar `ImageGenerationService` em `src/lib/image-generation/services/image-generation-service.ts`:

1. Aceitar `CampaignBrief` como input (em vez de parâmetros individuais)
2. Em `buildPromptVariables()`: injetar `identityDirective` de `CampaignBrief.identity.directive`
3. NÃO incluir `identityImageUrl` no `buildPromptVariables` — vai apenas ao provider
4. Remover `logoVariantUrl` da `brandProfileSection` (não existe mais em BrandProfileSnapshot)
5. Passar `CampaignBrief.identity.imageUrl` ao provider como `identityImageUrl`
6. Preservar todas as variáveis existentes e regras — creativePersona, inferredCategory, hasCategoryConflict, categoryConflictDirective, commercialRepertoire, inputValidationSummary, creativeContextGuidance
7. NÃO adicionar subsegmento, posicionamento, shortDescription, slogan ao prompt
</action>
<acceptance_criteria>
- Service aceita `CampaignBrief` como input
- `buildPromptVariables` retorna `identityDirective` com a string correta
- `buildPromptVariables` NÃO retorna `identityImageUrl`
- `brandProfileSection` NÃO tem `logoVariantUrl`
- Provider recebe `identityImageUrl` do CampaignBrief
- Todas as variáveis existentes preservadas
- `npm run typecheck` passa
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
prompts/campaign-image-director.md
</read_first>
<action>
Substituir instrução fixa de logotipo por `{{identityDirective}}` em `prompts/campaign-image-director.md`:

1. Localizar a instrução fixa "A loja possui um logotipo real fornecido como imagem de referência" (ou similar)
2. Substituir por `{{identityDirective}}`
3. Nenhuma outra alteração no prompt — regras de composição, hierarquia, copy permanecem idênticas
</action>
<acceptance_criteria>
- Instrução fixa de logotipo não existe mais no arquivo
- `{{identityDirective}}` presente no local onde a instrução estava
- Nenhuma outra linha do arquivo foi alterada
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/components/flow/campaign-input-form.tsx
src/components/flow/use-campaign-form.ts
openspec/changes/fase-5-0-identity-aware-campaign-briefing/specs/campaign-input-ui/spec.md
</read_first>
<action>
Atualizar `CampaignInputForm` e `useCampaignForm`:

1. **CampaignInputForm**: Alterar props de `storeIdentity: StoreIdentitySnapshot` para `storeId: string`. Atualizar interface e consumer.

2. **useCampaignForm**: Montar body do POST com `storeId` + campos de produto (productName, originalPriceCents, discountedPriceCents, badgeText, description, productImageDataUrl, hook, cta, objective, campaignDetails, additionalDetails, targetChannel, format, validity, availabilityNotes, sensitiveConstraints, inputValidationOverride). NÃO incluir `storeLogoUrl`, `brandProfile`, `storeName`, `storeSegment`, `storeTone`, `brandColor`.

3. Consumir `storeIdentity` do `type: "result"` event do NDJSON stream e incluir no `PreviewPayload`

4. Preservar lógica existente de validação, upload, preview, erro com retry
</action>
<acceptance_criteria>
- `CampaignInputForm` recebe `storeId: string` (não storeIdentity)
- Body do POST contém `storeId` + product fields
- Body do POST NÃO contém `storeLogoUrl`, `brandProfile`, `storeName`, `storeSegment`, `storeTone`, `brandColor`
- `storeIdentity` do result event incluído no PreviewPayload
- `npm run typecheck` passa
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/components/campaign/campaign-renderer.tsx
src/components/flow/store-visual-signature-section.tsx
openspec/changes/fase-5-0-identity-aware-campaign-briefing/specs/campaign-visual-renderer/spec.md
</read_first>
<action>
Atualizar `CampaignRenderer` e `StoreVisualSignatureSection` para consumir `signature` unificado:

1. **CampaignRenderer**: Substituir props/referências a `logoUrl`/`visualSignatureUrl` por `storeIdentity.signature.url` + `storeIdentity.signature.type`. A prioridade de identidade é: `type='logo'` → logo circular 40×40px + store name | `type='visual_signature'` → VS asset como elemento único, sem store name | `signature.url=null` → iniciais + store name.

2. **StoreVisualSignatureSection**: Consumir `signature` do snapshot (`.signature.url`, `.signature.type`) em vez de campos separados.
</action>
<acceptance_criteria>
- CampaignRenderer usa `signature.url` + `signature.type` (não `logoUrl`/`visualSignatureUrl`)
- StoreVisualSignatureSection usa `signature` unificado
- `npm run typecheck` passa
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/app/campaign/preview/page.tsx
openspec/changes/fase-5-0-identity-aware-campaign-briefing/specs/campaign-preview-page/spec.md
</read_first>
<action>
Atualizar `/campaign/preview/page.tsx` para normalizar payload legado:

1. Detectar se `storeIdentity.identityState` está ausente no PreviewPayload (formato legado)
2. Se ausente:
   - Se `logoUrl` presente → derivar `identityState = 'logo'`, `signature = { url: logoUrl, type: 'logo' }`
   - Se `visualSignatureUrl` presente (sem logoUrl) → derivar `identityState = 'visual_signature'`, `signature = { url: visualSignatureUrl, type: 'visual_signature' }`
   - Se nenhum ativo → derivar `identityState = 'text_only'`, `signature = { url: null, type: null }`
3. Se presente (formato novo): usar como está
4. Página NUNCA crasha ao encontrar formato legado
5. Normalização é silenciosa (sem notificação ao usuário)
</action>
<acceptance_criteria>
- Legacy payload com `logoUrl` → identityState='logo', signature.url=logoUrl
- Legacy payload com `visualSignatureUrl` (sem logoUrl) → identityState='visual_signature'
- Legacy payload sem ativos → identityState='text_only'
- Página não crasha com formato legado
- `npm run typecheck` passa
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
</read_first>
<action>
Run verification commands:

```bash
npm run typecheck
npm run lint
```

Both must pass with zero errors before proceeding.
</action>
<acceptance_criteria>
- `npm run typecheck` exits 0
- `npm run lint` exits 0
</acceptance_criteria>
</task>

</tasks>

<quality_gate>
- [ ] GenerateImageRequestSchema com storeId + product fields
- [ ] POST endpoint com pipeline de identidade
- [ ] ImageProviderInput com identityImageUrl
- [ ] OpenAI provider com identityImageUrl + fallback corrigido
- [ ] ImageGenerationService com CampaignBrief + identityDirective
- [ ] Prompt com {{identityDirective}}
- [ ] CampaignInputForm com storeId prop
- [ ] useCampaignForm sem identity fields no body
- [ ] CampaignRenderer usa signature
- [ ] StoreVisualSignatureSection usa signature
- [ ] Preview normaliza legado
- [ ] npm run typecheck passes
- [ ] npm run lint passes
</quality_gate>

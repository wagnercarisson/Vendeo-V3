---
id: 07-04
wave: 4
depends_on: [07-01, 07-02, 07-03]
files_modified:
  - src/app/(auth)/layout.tsx
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/login/login-form.tsx
  - src/app/auth/signout/route.ts
  - src/components/auth/logout-button.tsx
  - src/components/auth/auth-header.tsx
  - src/app/layout.tsx
autonomous: true
requirements:
  - login-page
  - auth-logout
---

# Plan 07-04: Login Page & Logout

Wave 4 — depends on Waves 1-3 (SSR clients, helpers, middleware available)

> **Fonte de design:** `openspec/design-system/MASTER.md` é a única fonte controlada. O arquivo legado `design-system/vendeo-v3/MASTER.md` (Financial Dashboard, Fira Code) **não deve ser usado**.

## Objective

Criar a página de login com formulário email+senha dentro de route group `(auth)` e o fluxo de logout com limpeza de storage. Seguir `openspec/design-system/MASTER.md` para tokens, acessibilidade e iconografia.

---

## Must Haves

- Route group `(auth)` com layout isolado (sem navegação principal)
- Página `/login` com formulário email+senha, loading state, erro genérico
- Logout via `POST /auth/signout` com limpeza de storage e redirect
- Botão "Sair" no header da aplicação
- Integração de `sanitizeRedirectPath()` no login para redirect pós-login

---

## Tasks

### Task 01: Create `src/app/(auth)/layout.tsx`

<action>
Criar layout para o route group `(auth)` com:
- Container centralizado para o conteúdo
- Logo da Vendeo (seguindo branding do design system)
- Tema dark com tokens Tailwind (`bg-slate-950` em vez de hex direto, `text-slate-50`)
- Sem header de navegação principal
- Sem footer complexo — layout mínimo
- Transições de 200ms sem scale no hover
</action>

<read_first>
- openspec/changes/fase-7-sessao-login-vertical/specs/login-page/spec.md
- openspec/changes/fase-7-sessao-login-vertical/design.md (D6)
- openspec/design-system/MASTER.md (fonte controlada de design tokens e padrões)
- src/app/layout.tsx (existing main layout structure)
</read_first>

<acceptance_criteria>
- `src/app/(auth)/layout.tsx` exporta layout com children
- Renderiza container centralizado com logo
- Usa tokens Tailwind (`bg-slate-950`, `text-slate-50`) — sem hex direto
- Não inclui navegação principal ou footer
- `npx tsc --noEmit` passa
</acceptance_criteria>

---

### Task 02: Create `src/app/(auth)/login/page.tsx`

<action>
Criar server component (Next.js 15 — `searchParams` é `Promise`):
```ts
export default async function LoginPage({
  searchParams,
}: {
  searchParams: Promise<{ redirect?: string | string[] }>
}) {
  const params = await searchParams
  const redirectParam = typeof params.redirect === "string" ? params.redirect : ""
  const safeRedirect = sanitizeRedirectPath(redirectParam)
  return <LoginForm redirect={safeRedirect} />
}
```
- Lê `searchParams.redirect` com `await` (Next.js 15)
- Valida com `sanitizeRedirectPath()` de `@/lib/auth/redirect`
- Renderiza `<LoginForm redirect={sanitizedRedirect} />`
- Não chama `requirePageUser()` — middleware já trata auth check
</action>

<read_first>
- openspec/changes/fase-7-sessao-login-vertical/specs/login-page/spec.md
- src/lib/auth/redirect.ts (criado no Plan 07-02)
</read_first>

<acceptance_criteria>
- `page.tsx` é server component (sem `"use client"`)
- Assina `searchParams: Promise<{ redirect?: string | string[] }>`
- Faz `await searchParams` antes de acessar `.redirect`
- Valida com `sanitizeRedirectPath()`
- Passa redirect validado para `LoginForm`
- `npx tsc --noEmit` passa
</acceptance_criteria>

---

### Task 03: Create `src/app/(auth)/login/login-form.tsx`

<action>
Criar client component (`"use client"`) seguindo `openspec/design-system/MASTER.md`:
- Email input, password input, submit button com label "Entrar" (única ação principal)
- Labels acessíveis com `htmlFor` associado a cada input
- Focus ring visível em todos os inputs e botão
- Alvo mínimo de toque 44×44px
- `supabase.auth.signInWithPassword({ email, password })` via `createBrowserClient()`
- Loading state durante submissão (botão desabilitado com indicador `lucide-react` Loader2)
- Erro genérico em caso de falha (ex: "Email ou senha inválidos") — sem revelar se usuário existe
- `router.replace(sanitizedRedirect || "/")` no sucesso
- Tema dark com tokens Tailwind (`bg-slate-950`, `text-slate-50`)
- Ícones via `lucide-react` (Mail, Lock, Loader2), nunca emoji
- Transições de 200ms sem scale no hover
- Sem link para signup
</action>

<read_first>
- openspec/changes/fase-7-sessao-login-vertical/specs/login-page/spec.md
- openspec/changes/fase-7-sessao-login-vertical/design.md (D6)
- openspec/design-system/MASTER.md (fonte controlada de design tokens e padrões)
- src/lib/supabase/client.ts (createBrowserClient — criado no Plan 07-01)
</read_first>

<acceptance_criteria>
- Componente usa `"use client"`
- Renderiza email input, password input, submit button com "Entrar"
- Labels usam `htmlFor` associado ao `id` do input
- Focus ring visível (ex: `focus:ring-2 focus:ring-blue-500`)
- Alvo mínimo 44×44px nos inputs e botão
- Submit chama `signInWithPassword` via `createBrowserClient()`
- Loading state: botão desabilitado com `Loader2` (lucide-react) animado
- Erro genérico exibido em caso de falha
- Sucesso: `router.replace(redirect || "/")`
- Usa tokens Tailwind — sem hex direto
- Usa `lucide-react` para ícones — zero emoji
- Sem link para signup
- `npx tsc --noEmit` passa
</acceptance_criteria>

---

### Task 04: Create logout route handler `src/app/auth/signout/route.ts`

<action>
Criar Route Handler `POST /auth/signout` que:
- Cria server client via `createServerClient()`
- Chama `supabase.auth.signOut()`
- Chama `revalidatePath("/", "layout")` (escopo layout para cobrir toda a árvore)
- Retorna redirect explícito:
  ```ts
  return NextResponse.redirect(new URL("/login", request.url), { status: 302 })
  ```
  `redirect()` do Next.js em Route Handler retorna 307 por padrão (preserva método POST após redirect). Usar `NextResponse.redirect` com `status: 302` para garantir GET no redirect pós-logout.
- É POST-only (outros métodos retornam 405)
</action>

<read_first>
- openspec/changes/fase-7-sessao-login-vertical/specs/auth-logout/spec.md
- openspec/changes/fase-7-sessao-login-vertical/design.md (D4)
- src/lib/supabase/server.ts (createServerClient — criado no Plan 07-01)
</read_first>

<acceptance_criteria>
- `export async function POST()` em `src/app/auth/signout/route.ts`
- Cria server client via `createServerClient()`
- Chama `supabase.auth.signOut()`
- Chama `revalidatePath("/", "layout")`
- Retorna `NextResponse.redirect(loginUrl, { status: 302 })`
- Métodos GET/PUT/DELETE retornam `405 Method Not Allowed`
- `npx tsc --noEmit` passa
</acceptance_criteria>

---

### Task 05: Create logout button and auth-aware header

<action>
Criar dois componentes e atualizar o layout principal:

1. **`src/components/auth/logout-button.tsx`** — Client component (`"use client"`) seguindo `openspec/design-system/MASTER.md`:
   - `<form action="/auth/signout" method="POST" onSubmit={clearStorage}>`
   - `clearStorage`:
     ```ts
     sessionStorage.removeItem("campaign_draft")
     sessionStorage.removeItem("campaign_draft_image")
     sessionStorage.removeItem("campaign_preview")
     localStorage.removeItem("store_id")
     ```
   - Botão "Sair" com tokens Tailwind, alvo mínimo 44×44px, focus ring visível
   - Ícone LogOut de `lucide-react` (nunca emoji)
   - Transições de 200ms sem scale no hover
   - Loading state (Loader2) durante submissão

2. **`src/components/auth/auth-header.tsx`** — Server component que:
   - Chama `requireUser()` com try/catch:
     ```ts
     try {
       await requireUser()
       return <LogoutButton />
     } catch (error) {
       if (error instanceof UnauthorizedError) return null
       throw error
     }
     ```
   - Renderiza `LogoutButton` apenas se autenticado
   - Se não autenticado, retorna null — o route group `(auth)` tem layout próprio, mas o **root layout ainda envolve** páginas `(auth)`. O botão não aparece porque `AuthHeader` retorna null, não porque o layout raiz deixou de ser renderizado.

3. **`src/app/layout.tsx`** — Renderizar `<AuthHeader />` no header da aplicação

Arquitetura: `AuthHeader` (server) → `LogoutButton` (client). O route group `(auth)` não substitui o root layout — ele fica aninhado nele.
</action>

<read_first>
- openspec/changes/fase-7-sessao-login-vertical/specs/auth-logout/spec.md
- openspec/changes/fase-7-sessao-login-vertical/design.md (D4)
- openspec/design-system/MASTER.md (fonte controlada de design tokens e padrões)
- src/app/layout.tsx (existing main layout)
- src/lib/auth/require-user.ts (criado no Plan 07-02)
</read_first>

<acceptance_criteria>
- `logout-button.tsx` é client component com form POST /auth/signout e onSubmit que limpa 4 chaves
- Botão usa tokens Tailwind, alvo mínimo 44×44px, focus ring visível
- Ícone LogOut de `lucide-react`, nunca emoji
- Transições de 200ms sem scale no hover
- `auth-header.tsx` é server component que renderiza LogoutButton condicionalmente
- `layout.tsx` inclui `<AuthHeader />` no header existente
- Limpeza usa `removeItem` para chaves conhecidas, não `clear()`
- Botão prossegue com submissão mesmo se storage cleanup falhar
- Botão não aparece em páginas `(auth)` (layout isolado)
- `npx tsc --noEmit` passa
</acceptance_criteria>

---

## Verification

1. `npx tsc --noEmit` — zero erros de tipo
2. `npx next build` — build bem-sucedido
3. Fluxo manual: provisionar usuário no Supabase Dashboard → navegar para `/login` → login com credenciais → redirect para `/` → logout → redirect para `/login`
4. Verificar que `/login` redireciona para `/` quando já autenticado
5. Verificar que storage é limpo após logout

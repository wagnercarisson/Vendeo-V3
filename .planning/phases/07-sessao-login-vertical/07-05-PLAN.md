---
id: 07-05
wave: 4
depends_on: [07-01, 07-02, 07-03, 07-04]
files_modified: []
autonomous: false
requirements:
  - tests
  - verification
---

# Plan 07-05: Tests & Verification

Wave 4 — depends on all previous waves

## Objective

Implementar testes unitários para todos os módulos da fase e executar a verificação final completa.

---

## Must Haves

- Testes unitários para `requireUser` (4 cenários)
- Testes unitários para `sanitizeRedirectPath` (6 cenários)
- Testes unitários para middleware (5 cenários)
- Testes unitários para login form (3 cenários)
- Testes unitários para logout (2 cenários)
- Suíte existente continua passando
- TypeScript, vitest e build verdes

---

## Tasks

### Task 01: Test `requireUser()`

<action>
Criar testes que mockam `@/lib/supabase/server` (`createServerClient`) para:
1. `getClaims()` → claims com sub → retorna `AuthenticatedUser` com `userId === claims.sub`
2. `getClaims()` → error → throw `UnauthorizedError`
3. `getClaims()` → null → throw `UnauthorizedError`
4. `getClaims()` → claims sem `sub` → throw `UnauthorizedError`
</action>

<read_first>
- src/lib/auth/require-user.ts
- openspec/changes/fase-7-sessao-login-vertical/specs/user-auth/spec.md
</read_first>

<acceptance_criteria>
- 4 cenários implementados como testes isolados
- Mock de `createServerClient` retorna controladamente dados/erro
- Testes existentes não são afetados
- `npx vitest run` — testes verdes
</acceptance_criteria>

---

### Task 02: Test `sanitizeRedirectPath()`

<action>
Criar testes para `sanitizeRedirectPath()`:
1. `sanitizeRedirectPath("/campaign/preview")` → `"/campaign/preview"`
2. `sanitizeRedirectPath("/campaign/preview?foo=bar")` → `"/campaign/preview?foo=bar"`
3. `sanitizeRedirectPath("https://evil.com")` → `"/"`
4. `sanitizeRedirectPath("//evil.com")` → `"/"`
5. `sanitizeRedirectPath("/login")` → `"/"`
6. `sanitizeRedirectPath("/campaign/preview#section")` → `"/campaign/preview"`
7. `sanitizeRedirectPath("")` → `"/"`
</action>

<read_first>
- src/lib/auth/redirect.ts
- openspec/changes/fase-7-sessao-login-vertical/specs/auth-redirect/spec.md
</read_first>

<acceptance_criteria>
- 7 cenários implementados
- Cobertura inclui allowlist, rejeição, query string, fragmentos, fallback
- `npx vitest run` — testes verdes
</acceptance_criteria>

---

### Task 03: Test middleware

<action>
Criar testes simulando `Request` objects para o middleware:
1. Request para `/` sem sessão → redirect `/login?redirect=/`
2. Request para `/campaign/preview` sem sessão → redirect `/login?redirect=/campaign/preview`
3. Request para `/api/store` sem sessão → 401 JSON
4. Request para `/login` com sessão → redirect `/`
5. Request para `/` com sessão → pass-through (NextResponse.next)
</action>

<read_first>
- src/middleware.ts
- openspec/changes/fase-7-sessao-login-vertical/specs/auth-middleware/spec.md
</read_first>

<acceptance_criteria>
- 5 cenários implementados com Request mock
- Mock de `updateSession()` retorna claims ou null controladamente
- `npx vitest run` — testes verdes
</acceptance_criteria>

---

### Task 04: Test login form

<action>
Criar testes de renderização e comportamento para `LoginForm`:
1. Renderiza email input, password input e botão submit com texto "Entrar"
2. Submit com credenciais válidas → `signInWithPassword` chamado + `router.replace("/")`
3. Submit com erro → mensagem de erro genérica exibida ("Email ou senha inválidos")
4. Durante submissão → botão desabilitado com loading state
</action>

<read_first>
- src/app/(auth)/login/login-form.tsx
- openspec/changes/fase-7-sessao-login-vertical/specs/login-page/spec.md
</read_first>

<acceptance_criteria>
- 4 cenários implementados com Testing Library + jsdom
- Mock de `createBrowserClient()` retorna signInWithPassword controlada
- `npx vitest run` — testes verdes
</acceptance_criteria>

---

### Task 05: Test logout flow

<action>
Criar testes para:
1. Componente de logout: `onSubmit` limpa 4 chaves do storage (campaign_draft, campaign_draft_image, campaign_preview, store_id)
2. Route Handler POST /auth/signout: chama signOut + revalidatePath + redirect
3. Form submete mesmo se storage cleanup lançar erro
</action>

<read_first>
- src/app/auth/signout/route.ts
- Componente de logout (criado no Plan 07-04)
- openspec/changes/fase-7-sessao-login-vertical/specs/auth-logout/spec.md
</read_first>

<acceptance_criteria>
- 3 cenários implementados
- Limpeza usa `removeItem` específico, não `clear()`
- Storage cleanup com erro não impede submissão do form
- `npx vitest run` — testes verdes
</acceptance_criteria>

---

### Task 06: Final verification

<action>
Executar a verificação completa da fase:
1. `npx vitest run` — todos os testes verdes (novos + existentes)
2. `npx tsc --noEmit` — zero erros de tipo
3. `npx next build` — build bem-sucedido
4. Fluxo manual: criar usuário no Supabase Dashboard → login → rota protegida → logout
5. `git grep "from.*supabase/client['"'"'"]" | grep -v "node_modules"` — nenhum import do antigo barrel
6. Configurar URLs autorizadas no Supabase Dashboard (localhost, produção, previews)
</action>

<read_first>
- Todos os arquivos criados/modificados na fase
- .env.local (verificar variáveis)
</read_first>

<acceptance_criteria>
- `npx vitest run` exit 0
- `npx tsc --noEmit` exit 0
- `npx next build` exit 0
- Fluxo manual completo verificado
- URLs autorizadas configuradas no Supabase Dashboard
</acceptance_criteria>

---

## Verification

1. `npx vitest run` — todos os testes verdes
2. `npx tsc --noEmit` — zero erros de tipo
3. `npx next build` — build bem-sucedido
4. Nenhum import residual do antigo singleton/barrel
5. Documentar caveat: testes mockados não validam cookie/sessão real — E2E programado para Fase 11

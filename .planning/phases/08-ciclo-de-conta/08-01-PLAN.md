---
id: 08-01
wave: 1
depends_on: []
files_modified:
  - .env.example
  - supabase/config.toml
  - supabase/templates/confirmation.html
  - supabase/templates/recovery.html
  - src/lib/supabase/site-url.ts
  - src/middleware.ts
autonomous: true
requirements:
  - auth-middleware
  - env-setup
---

# Plan 08-01: Setup & Middleware Expansion

Wave 1 — Foundation (no dependencies beyond Phase 7)

## Objective

Preparar o ambiente de desenvolvimento: configurar Supabase local (`config.toml`), templates de email customizados, variável `NEXT_PUBLIC_SITE_URL`, e expandir o middleware para suportar as novas 5 rotas de autenticação.

---

## Must Haves

- `NEXT_PUBLIC_SITE_URL` adicionada ao `.env.example`
- `supabase/config.toml` gerado com `[auth.email] enable_confirmations`, `[auth] site_url`, `additional_redirect_urls`
- `supabase/templates/confirmation.html` e `recovery.html` com links usando `{{ .TokenHash }}`
- Templates registrados em `config.toml` via `[auth.email.template.confirmation].content_path`
- Middleware com matcher expandido para 10 rotas
- `PUBLIC_ROUTES` e `ALWAYS_PASSTHROUGH` implementados
- `/auth/confirm` passa sem redirect em qualquer estado de autenticação
- Autenticado em `/signup`, `/check-email`, `/forgot-password` → redirect `/`
- Não autenticado em `/update-password` → redirect `/login`

---

## Tasks

### Task 01: Adicionar NEXT_PUBLIC_SITE_URL ao .env.example

<action>
Adicionar `NEXT_PUBLIC_SITE_URL=http://localhost:3000` ao arquivo `.env.example` **e** ao `.env.local` (não versionado, necessário para build local). Criar módulo compartilhado `src/lib/supabase/site-url.ts` que valida a variável em **build/load time** (escopo do módulo, não da função):

```typescript
const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL;
if (!SITE_URL) throw new Error("NEXT_PUBLIC_SITE_URL is not defined");

export function getSiteUrl(): string {
  return SITE_URL;
}
```

O throw ocorre no momento em que o módulo é importado — nunca durante submit. Este módulo será importado por signup e forgot-password.
</action>

<read_first>
- .env.example
- src/lib/supabase/client.ts (padrão de validação de ENV)
</read_first>

<acceptance_criteria>
- `.env.example` contém `NEXT_PUBLIC_SITE_URL=http://localhost:3000`
- `.env.local` contém `NEXT_PUBLIC_SITE_URL=http://localhost:3000`
- `src/lib/supabase/site-url.ts` valida em escopo de módulo (não em função)
- Sem a variável, o erro é lançado no import/build, nunca em submit
- Com a variável, `getSiteUrl()` retorna o valor
</acceptance_criteria>

### Task 02: Inicializar Supabase local

<action>
Rodar `npx supabase init` para gerar `supabase/config.toml`. Configurar:
- `[auth.email] enable_confirmations = false` (desenvolvimento rápido)
- `[auth] site_url = "http://localhost:3000"`
- `[auth] additional_redirect_urls = ["http://localhost:3000/auth/confirm"]`

Criar `supabase/templates/` com `confirmation.html` e `recovery.html` contendo os links com `{{ .RedirectTo }}?token_hash={{ .TokenHash }}&type=...`.

Registrar templates no `config.toml`:
```toml
[auth.email.template.confirmation]
subject = "Confirme seu email"
content_path = "./supabase/templates/confirmation.html"

[auth.email.template.recovery]
subject = "Redefina sua senha"
content_path = "./supabase/templates/recovery.html"
```

Adicionar `supabase/config.toml` e `supabase/templates/` ao git.
</action>

<read_first>
- supabase/config.toml (após init — verificar se existe antes)
- supabase/templates/ (verificar diretório)
</read_first>

<acceptance_criteria>
- `supabase/config.toml` existe e contém `[auth.email] enable_confirmations`, `[auth] site_url`, `[auth] additional_redirect_urls`
- `supabase/templates/confirmation.html` contém `{{ .TokenHash }}` e `{{ .RedirectTo }}`
- `supabase/templates/recovery.html` contém `{{ .TokenHash }}` e `{{ .RedirectTo }}` com `next=/update-password`
- `config.toml` contém `[auth.email.template.confirmation]` e `[auth.email.template.recovery]`
</acceptance_criteria>

### Task 03: Expandir middleware — matcher e classificação de rotas

<action>
Modificar `src/middleware.ts`:

1. Expandir `config.matcher` de 5 para 10 rotas:
```typescript
matcher: ["/", "/login", "/signup", "/check-email", "/forgot-password",
          "/update-password", "/auth/confirm", "/store/:path*",
          "/campaign/:path*", "/api/:path*"]
```

2. Adicionar classificação de rotas:
```typescript
const PUBLIC_ROUTES = new Set(["/login", "/signup", "/check-email", "/forgot-password"]);
const ALWAYS_PASSTHROUGH = new Set(["/auth/confirm"]);
```

3. Implementar lógica completa:
- Se `pathname` está em `ALWAYS_PASSTHROUGH`: `return response` imediatamente
- Se não autenticado e `isApiRoute`: 401 JSON
- Se não autenticado e `isPublicRoute`: pass-through
- Se não autenticado e não público: redirect `/login?redirect=...`
- Se autenticado e `isPublicRoute || pathname === "/login"`: redirect `/`
- Se autenticado e não público: pass-through
</action>

<read_first>
- src/middleware.ts (código atual)
- openspec/changes/fase-8-ciclo-de-conta/specs/auth-middleware/spec.md
</read_first>

<acceptance_criteria>
- `config.matcher` contém exatamente 10 entradas incluindo `/signup`, `/check-email`, `/forgot-password`, `/update-password`, `/auth/confirm`
- `PUBLIC_ROUTES` contém `/login`, `/signup`, `/check-email`, `/forgot-password`
- `ALWAYS_PASSTHROUGH` contém `/auth/confirm`
- GET `/signup` sem sessão → 200 (pass-through)
- GET `/signup` com sessão → 302 redirect `/`
- GET `/auth/confirm` sem sessão → pass-through (não redirect)
- GET `/auth/confirm` com sessão → pass-through (não redirect)
- GET `/update-password` sem sessão → 302 redirect `/login`
</acceptance_criteria>

### Task 04: Habilitar confirmação para UAT local

<action>
Antes do UAT local (Plan 08-04), alterar `enable_confirmations = true` em `[auth.email]` no `supabase/config.toml` e reiniciar o stack: `npx supabase stop && npx supabase start`.
</action>

<read_first>
- supabase/config.toml
</read_first>

<acceptance_criteria>
- `supabase/config.toml` contém `[auth.email] enable_confirmations = true`
- `npx supabase start` roda sem erros
- Mailpit captura emails em localhost:54325 (SMTP) / localhost:54324 (web)
</acceptance_criteria>

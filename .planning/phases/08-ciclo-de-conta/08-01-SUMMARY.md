# Plan 08-01 — Setup & Middleware Expansion

**Status:** ✓ Complete
**Committed:** `ffeb064`, `5b30044`, `96c3fa0`

## What was built

### Task 01 — NEXT_PUBLIC_SITE_URL
- Added `NEXT_PUBLIC_SITE_URL=http://localhost:3000` to `.env.example`
- Created `src/lib/supabase/site-url.ts` with module-level validation (throw at build/load time if absent)

### Task 02 — Supabase local + email templates
- Ran `npx supabase init` to generate `supabase/config.toml`
- Configured `site_url`, `additional_redirect_urls`, and `enable_confirmations`
- Created `supabase/templates/confirmation.html` and `recovery.html` with `{{ .TokenHash }}` + `{{ .RedirectTo }}`
- Registered templates in config.toml via `[auth.email.template.confirmation]` and `[auth.email.template.recovery]`

### Task 03 — Middleware expansion
- Matcher expanded from 5 to 10 routes: added `/signup`, `/check-email`, `/forgot-password`, `/update-password`, `/auth/confirm`
- New `PUBLIC_ROUTES` Set: anonymous passes through, authenticated redirects to `/`
- New `ALWAYS_PASSTHROUGH` Set (`/auth/confirm`): always passes regardless of auth state
- `/update-password` requires authentication (not in PUBLIC_ROUTES)

### Task 04 — Deferred to Plan 08-04 (UAT)

## Files created/modified
- `.env.example` — new entry
- `src/lib/supabase/site-url.ts` — new module
- `supabase/config.toml` — new file (generated by init)
- `supabase/templates/confirmation.html` — new template
- `supabase/templates/recovery.html` — new template
- `src/middleware.ts` — expanded matcher + route classification

## Verification
- TypeScript check: middleware compiles correctly with new Sets and route logic
- Route behavior: PUBLIC_ROUTES (anonymous → pass-through, authenticated → redirect /)
- Route behavior: ALWAYS_PASSTHROUGH (/auth/confirm → always pass-through)
- Route behavior: /update-password not in PUBLIC_ROUTES → requires auth

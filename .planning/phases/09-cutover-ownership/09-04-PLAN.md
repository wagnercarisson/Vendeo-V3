---
id: 09-04
wave: 3
depends_on:
  - 09-01
  - 09-02
  - 09-03
files_modified:
  - src/__tests__/auth/store-ownership.test.ts
  - src/__tests__/auth/logout.test.tsx
autonomous: true
requirements:
  - store-ownership-core
  - store-ownership-api
  - store-ownership-pages
  - store-form-ownership-refactor
  - store-localstorage-removal
  - auth-logout
---

# Plan 09-04: Tests, Verificação e Regressão

Wave 3 — Testes e verificação (depends on 09-01, 09-02, 09-03)

## Objective

Criar testes para ownership helpers, ownership nas rotas, e atualizar testes existentes. Verificar regressão com grep, type check, lint, e build.

---

## Must Haves

- Testes para `StoreNotFoundError` — catchable, distinguível de UnauthorizedError
- Testes para `getCurrentStore()` — store encontrada, não encontrada, userId opcional
- Testes para `requireOwnership()` — store encontrada, não encontrada, alheia, userId opcional
- Testes para ownership nas 4 rotas CRUD — autenticado, não autenticado, sem loja, store alheia, UNIQUE violation (POST)
- Logout test atualizado — remove store_id expectations
- `rg "localStorage" src/components/flow/ src/components/auth/` — zero resultados
- `npx tsc --noEmit`, `npx vitest run`, `npm run lint` — todos verdes

---

## Tasks

### Task 01: Testes unitários — store-ownership.test.ts

<action>
Criar `src/__tests__/auth/store-ownership.test.ts` com ambiente `node` (pure logic, sem jsdom):

Testar `StoreNotFoundError`:
- `new StoreNotFoundError()` tem `name === "StoreNotFoundError"` e mensagem padrão
- `error instanceof StoreNotFoundError` é true
- `error instanceof UnauthorizedError` é false (distinguível)

Testar `getCurrentStore(userId)`:
- Mock `createServerClient()` que retorna store quando `user_id` match
- `getCurrentStore("user-123")` retorna store quando encontrada
- `getCurrentStore("user-456")` retorna null quando não encontrada

Testar `getCurrentStore()` sem userId:
- Mock `requireUser()` internamente via `@/lib/auth/require-user`
- Sem userId, chama requireUser() para obter claims.sub (D11)

Testar `requireOwnership(storeId, userId)`:
- Mock `createServerClient()` que retorna store quando `id AND user_id` match
- `requireOwnership("store-1", "user-123")` retorna store quando match
- `requireOwnership("store-1", "user-999")` throw StoreNotFoundError (alheia)
- `requireOwnership("store-nonexistent", "user-123")` throw StoreNotFoundError (inexistente)

Testar `requireOwnership(storeId)` sem userId:
- Chama requireUser() internamente para userId
</action>

<read_first>
- src/__tests__/auth/require-user.test.ts (padrão de testes da Phase 7 — node env, mocking)
- src/__tests__/auth/logout.test.tsx (padrão jsdom)
- src/lib/auth/store-ownership.ts (implementação a testar)
- openspec/changes/fase-9-cutover-ownership/specs/store-ownership-core/spec.md
</read_first>

<acceptance_criteria>
- `src/__tests__/auth/store-ownership.test.ts` com `// @vitest-environment node`
- StoreNotFoundError é catchable e distinguível
- getCurrentStore com userId retorna Store ou null
- getCurrentStore sem userId chama requireUser internamente
- requireOwnership com userId retorna Store ou throw
- requireOwnership sem userId chama requireUser internamente
- requireOwnership com store alheia throw StoreNotFoundError (não UnauthorizedError)
- `npx vitest run` — testes verdes
</acceptance_criteria>

### Task 02: Testes de API — ownership nas rotas CRUD

<action>
Adicionar testes para ownership nos 4 endpoints em `src/__tests__/api/store-ownership-api.test.ts`:

1. **POST /api/store:**
   - Com auth → 201, user_id = claims.sub
   - Sem auth → 401
   - UNIQUE violation → 409 "Usuário já possui uma loja"
   - user_id no body é ignorado

2. **GET /api/store (atalho):**
   - Com auth e loja → 200
   - Com auth sem loja → 404
   - Sem auth → 401

3. **GET /api/store/:id:**
   - Owner → 200
   - Outro user → 404
   - Inexistente → 404
   - Sem auth → 401

4. **PATCH /api/store/:id:**
   - Owner → 200
   - Outro user → 404
   - Inexistente → 404
   - Sem auth → 401

Usar padrão de mock dos testes Phase 8: mock `createServerClient()`, `requireUser()`, `supabaseAdmin`, etc.
</action>

<read_first>
- src/__tests__/auth/require-user.test.ts (padrão de mock de createServerClient)
- src/app/api/store/route.ts (após modificações — POST + GET)
- src/app/api/store/[id]/route.ts (após modificações)
- openspec/changes/fase-9-cutover-ownership/specs/store-ownership-api/spec.md
</read_first>

<acceptance_criteria>
- POST com auth → 201
- POST sem auth → 401
- POST duplicado → 409
- GET /api/store com auth e loja → 200
- GET /api/store com auth sem loja → 404
- GET /api/store/:id owner → 200
- GET /api/store/:id alheio → 404
- PATCH /api/store/:id owner → 200
- PATCH /api/store/:id alheio → 404
- `npx vitest run` — testes verdes
</acceptance_criteria>

### Task 03: Atualizar logout test — remover store_id expectations

<action>
Modificar `src/__tests__/auth/logout.test.tsx`:

1. Remover setup de `localStorage.setItem("store_id", ...)` no beforeEach
2. Remover expectativa de `localStorage.removeItem("store_id")` no submit
3. Manter setup e expectativas de sessionStorage keys
4. Adicionar verificação de que sessionStorage keys foram limpas
5. Adicionar verificação de que `localStorage` NÃO foi chamado para removeItem
</action>

<read_first>
- src/__tests__/auth/logout.test.tsx (testes atuais)
- openspec/changes/fase-9-cutover-ownership/specs/auth-logout/spec.md
</read_first>

<acceptance_criteria>
- Teste passa sem mock de `store_id` em localStorage
- Teste verifica cleanup de `campaign_draft`, `campaign_draft_image`, `campaign_preview`
- Teste NÃO verifica `localStorage.removeItem("store_id")`
- `npx vitest run` — todos os testes verdes
</acceptance_criteria>

### Task 04: Verificação de regressão — grep localStorage

<action>
Rodar verificação de que `localStorage("store_id")` foi completamente removido:

```bash
rg "localStorage" src/components/flow/ src/components/auth/
```

Deve retornar zero resultados.

Verificar também `rg "STORAGE_KEY" src/components/` — zero resultados (constante "store_id" não existe mais).
</action>

<read_first>
- (nenhum — comando direto)
</read_first>

<acceptance_criteria>
- `rg "localStorage" src/components/flow/ src/components/auth/` — zero resultados
- `rg "STORAGE_KEY" src/components/` — zero resultados
</acceptance_criteria>

### Task 05: Type check, lint e build

<action>
Rodar pipeline de verificação:

```bash
npx tsc --noEmit          # Zero erros de tipo
npx vitest run            # Todos os testes verdes (incluindo existentes)
npm run lint              # Zero erros de lint
npx next build            # Build bem-sucedido
```
</action>

<read_first>
- (nenhum — comandos diretos)
</read_first>

<acceptance_criteria>
- `npx tsc --noEmit` — exit 0
- `npx vitest run` — exit 0, todos os testes passando
- `npm run lint` — exit 0
- `npx next build` — exit 0
</acceptance_criteria>

---
id: 10-04
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/<timestamp>_enable_rls_child_tables.sql
  - src/__tests__/migrations/rls-policies.test.ts
autonomous: true
requirements:
  - multitenant-rls-storage
---

# Plan 10-04: RLS + Storage Policies

Wave 1 — Migracoes independentes de infra (no code deps)

## Objective

Habilitar RLS nas 4 tabelas filhas, criar Storage policies restritas por path prefix, documentar `store-logos` como excecao temporaria.

---

## Must Haves

- `store_brand_assets`: RLS enabled + SELECT policy do owner (subquery stores.user_id)
- `store_brand_profiles`: RLS enabled + SELECT policy do owner
- `store_visual_signatures`: RLS enabled + SELECT policy do owner
- `generation_events`: RLS enabled — default-deny (sem policy para authenticated)
- `store-brand-assets` bucket: policy FOR SELECT TO authenticated com path prefix `{store_id}/`
- `visual-signatures` bucket: policy FOR SELECT TO authenticated com path prefix `{store_id}/`
- `store-logos` documentado como excecao temporaria (comentario no SQL)
- Buckets permanecem `public = true` para download por URL conhecida

---

## Tasks

### Task 01: Criar migration SQL — RLS nas 4 tabelas filhas

<action>
Criar `supabase/migrations/<timestamp>_enable_rls_child_tables.sql`:

```sql
-- RLS: store_brand_assets
ALTER TABLE store_brand_assets ENABLE ROW LEVEL SECURITY;
CREATE POLICY "owner_select_brand_assets" ON store_brand_assets
  FOR SELECT TO authenticated
  USING (store_id IN (SELECT id FROM public.stores WHERE user_id = (SELECT auth.uid())));

-- RLS: store_brand_profiles
ALTER TABLE store_brand_profiles ENABLE ROW LEVEL SECURITY;
CREATE POLICY "owner_select_brand_profiles" ON store_brand_profiles
  FOR SELECT TO authenticated
  USING (store_id IN (SELECT id FROM public.stores WHERE user_id = (SELECT auth.uid())));

-- RLS: store_visual_signatures
ALTER TABLE store_visual_signatures ENABLE ROW LEVEL SECURITY;
CREATE POLICY "owner_select_visual_signatures" ON store_visual_signatures
  FOR SELECT TO authenticated
  USING (store_id IN (SELECT id FROM public.stores WHERE user_id = (SELECT auth.uid())));

-- RLS: generation_events (default-deny — sem policy)
ALTER TABLE generation_events ENABLE ROW LEVEL SECURITY;
-- NOTA: Nenhuma policy FOR SELECT TO authenticated em generation_events.
-- Apenas supabaseAdmin (service_role) acessa esta tabela.

-- GRANT SELECT necessario para RLS funcionar com authenticated role
GRANT SELECT ON TABLE store_brand_assets TO authenticated;
GRANT SELECT ON TABLE store_brand_profiles TO authenticated;
GRANT SELECT ON TABLE store_visual_signatures TO authenticated;
-- generation_events NAO recebe GRANT SELECT (default-deny)
```

Incluir bloco `-- REVERT:` com DROP POLICY e ALTER TABLE DISABLE ROW LEVEL SECURITY.
</action>

<read_first>
- supabase/migrations/ (listar para ver padrao de nomenclatura)
- openspec/changes/fase-10-perimetro-multitenant/specs/multitenant-rls-storage/spec.md (RLS requirements)
</read_first>

<acceptance_criteria>
- Migration arquivada em `supabase/migrations/<timestamp>_enable_rls_child_tables.sql`
- 4 ALTER TABLE ENABLE ROW LEVEL SECURITY
- 3 CREATE POLICY FOR SELECT TO authenticated (brand_assets, brand_profiles, visual_signatures)
- generation_events NAO tem policy (default-deny)
- 3 GRANT SELECT TO authenticated (excluindo generation_events)
- `-- REVERT:` block presente
</acceptance_criteria>

### Task 02: Criar migration SQL — Storage policies

<action>
Adicionar na mesma migration da Task 01 (ou arquivo separado) as Storage policies:

```sql
-- Storage: store-brand-assets
DROP POLICY IF EXISTS "brand_assets_public_read" ON storage.objects;
CREATE POLICY "tenant_isolation_brand_assets" ON storage.objects
  FOR SELECT TO authenticated
  USING (
    bucket_id = 'store-brand-assets'
    AND (storage.foldername(name))[1] IN (
      SELECT id::text FROM public.stores WHERE user_id = (SELECT auth.uid())
    )
  );

-- Storage: visual-signatures
DROP POLICY IF EXISTS "visual_signatures_public_read" ON storage.objects;
CREATE POLICY "tenant_isolation_visual_signatures" ON storage.objects
  FOR SELECT TO authenticated
  USING (
    bucket_id = 'visual-signatures'
    AND (storage.foldername(name))[1] IN (
      SELECT id::text FROM public.stores WHERE user_id = (SELECT auth.uid())
    )
  );

-- Storage: store-logos (excecao temporaria — Fase 11)
-- NOTA: store-logos mantem politica publica atual.
-- Nenhum fluxo novo le/escreve em store-logos.
-- Inventario e migracao programados para Fase 11.
-- Policy atual: FOR SELECT TO authenticated USING (bucket_id = 'store-logos')
```

Buckets permanecem `public = true` — download por URL conhecida nao e bloqueado (apenas listagem/discovery).
</action>

<read_first>
- openspec/changes/fase-10-perimetro-multitenant/specs/multitenant-rls-storage/spec.md (Storage requirements)
- supabase/migrations/ (verificar se ha policies de storage existentes para dropar)
</read_first>

<acceptance_criteria>
- store-brand-assets: DROP policy ampla antiga + nova policy tenant_isolation_brand_assets
- visual-signatures: DROP policy ampla antiga + nova policy tenant_isolation_visual_signatures
- store-logos: comentario de excecao temporaria, policy nao alterada
- bucket `public = true` mantido
</acceptance_criteria>

### Task 03: Testes de migration — validar SQL estatico

<action>
Criar `src/__tests__/migrations/rls-policies.test.ts` (env: node):

Testes de SQL estatico (nao conectam ao banco):
1. Ler o arquivo de migration e verificar que contem os 4 `ALTER TABLE ... ENABLE ROW LEVEL SECURITY`
2. Verificar que contem 3 `CREATE POLICY` (generation_events NAO tem)
3. Verificar que contem 3 `GRANT SELECT TO authenticated` (generation_events NAO tem)
4. Verificar que contem 2 Storage policies com `storage.foldername(name)`
5. Verificar que contem `-- REVERT:` block
6. Verificar que `store-logos` NAO tem DROP/CREATE policy (excecao)
</action>

<read_first>
- src/__tests__/migrations/store-ownership.test.ts (padrao de testes de migration da Phase 9, se existir)
- supabase/migrations/<timestamp>_enable_rls_child_tables.sql (recem-criada)
</read_first>

<acceptance_criteria>
- `src/__tests__/migrations/rls-policies.test.ts` com `// @vitest-environment node`
- Verificacao de 4 ENABLE ROW LEVEL SECURITY
- Verificacao de 3 CREATE POLICY + 0 em generation_events
- Verificacao de 3 GRANT SELECT (sem generation_events)
- Verificacao de 2 Storage policies com storage.foldername
- Verificacao de REVERT block
- Verificacao de que store-logos nao foi alterado
- `npx vitest run` — testes verdes
</acceptance_criteria>

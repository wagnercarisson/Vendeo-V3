---
id: 10-06
wave: 4
depends_on:
  - 10-01
  - 10-02
  - 10-03
  - 10-04
  - 10-05
files_modified: []
autonomous: true
requirements:
  - multitenant-auth-guards
  - multitenant-route-handlers
  - multitenant-campaign-guards
  - multitenant-server-actions
  - multitenant-rls-storage
  - multitenant-test-matrix
---

# Plan 10-06: Validacao e Regressao

Wave 4 — Pipeline de qualidade (depends on 10-01 through 10-05)

## Objective

Rodar pipeline de validacao completa: type check, testes, lint, build. Verificar excecoes documentadas e regressao de localStorage.

---

## Must Haves

- `npx tsc --noEmit` — zero erros de tipo
- `npx vitest run` — todos os testes verdes (incluindo regressao dos existentes ~410+)
- `npm run lint` — zero erros de lint
- `npx next build` — build bem-sucedido
- Checklist de excecoes: rotas/metodos intencionalmente sem guard documentados
- Revisao de diff: nenhum fluxo novo le/escreve em `store-logos` sem ownership check
- `rg "localStorage" src/` — zero resultados inesperados

---

## Tasks

### Task 01: Type check, lint e build

<action>
Rodar pipeline de verificacao:

```bash
npx tsc --noEmit          # Zero erros de tipo
npx vitest run            # Todos os testes verdes (incluindo regressao dos ~410 existentes)
npm run lint              # Zero erros de lint
npx next build            # Build bem-sucedido
```

Se algum passo falhar, corrigir antes de prosseguir.
</action>

<read_first>
- (nenhum — comandos diretos)
</read_first>

<acceptance_criteria>
- `npx tsc --noEmit` — exit 0
- `npx vitest run` — exit 0, todos os testes passando
- `npm run lint` — exit 0
- `npx next build` — exit 0
</acceptance_criteria>

### Task 02: Checklist de excecoes

<action>
Verificar se alguma rota/metodo foi intencionalmente deixada sem guard de autenticacao ou ownership:

1. GET /api/store (atalho) — usa getCurrentStore internamente? Sim (Phase 9).
2. POST /api/store — requireSameOrigin + requireUser? Sim.
3. POST /auth/signout — requireSameOrigin + requireUser? Sim.
4. Alguma rota de GET pura sem dados sensiveis ficou sem guard? Verificar.
5. store-logos bucket — politica publica mantida intencionalmente? Sim (excecao documentada na migration).

Documentar qualquer excecao intencional com motivo claro.
</action>

<read_first>
- src/app/api/ (listar todas as rotas para verificar cobertura)
- supabase/migrations/<timestamp>_enable_rls_child_tables.sql (comentario store-logos)
</read_first>

<acceptance_criteria>
- Toda rota com dados sensiveis tem guard de autenticacao
- store-logos tem excecao documentada
- Nenhuma rota desprotegida inesperada
- Documento de excecoes escrito (se houver)
</acceptance_criteria>

### Task 03: Revisao de diff — store-logos isolation

<action>
Verificar que nenhum fluxo novo le ou escreve em `store-logos` sem ownership check:

```bash
rg "store-logos" src/ --type ts --type tsx
rg "store_logos" src/ --type ts --type tsx
```

Os resultados devem se limitar a:
1. Referencias existentes de fases anteriores (se houver)
2. Comentario de excecao na migration desta fase
3. Nenhum fluxo novo
</action>

<read_first>
- (nenhum — comandos diretos)
</read_first>

<acceptance_criteria>
- `rg "store-logos" src/` mostra apenas referencias existentes + comentario de excecao
- Nenhum fluxo novo le ou escreve em store-logos
</acceptance_criteria>

### Task 04: Verificacao de localStorage

<action>
Verificar que nenhum store_id esta sendo armazenado em localStorage:

```bash
rg "localStorage" src/
```

Nao deve haver resultados de `localStorage` relacionados a `store_id`. Resultados esperados: apenas sessionStorage para `campaign_draft` e similares (fases anteriores).
</action>

<read_first>
- (nenhum — comandos diretos)
</read_first>

<acceptance_criteria>
- `rg "localStorage" src/` — zero resultados inesperados
- Nenhum `store_id` em localStorage
</acceptance_criteria>

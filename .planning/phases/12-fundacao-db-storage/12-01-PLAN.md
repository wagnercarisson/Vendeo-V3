---
plan_id: 12-01
wave: 1
objective: Migration 1 — Create campaigns table DDL
depends_on: []
requirements_addressed:
  - REQ-CAMPAIGNS-DDL
  - REQ-ERROR-CHECK
  - REQ-TRIGGER-UPDATED-AT
  - REQ-RLS-CAMPAIGNS
  - REQ-INDEXES
files_modified:
  - supabase/migrations/20260708000001_create_campaigns_table.sql
autonomous: true
---

## Tasks

### Task 12-01-a: Write DDL for campaigns table

<read_first>
- .planning/phases/12-fundacao-db-storage/12-CONTEXT.md (decisions D1, D2, D4, D6)
- openspec/changes/fase-12-fundacao-db-storage/specs/campaigns-table/spec.md
- supabase/migrations/ (existing migrations for naming pattern)
</read_first>

<action>
Create `supabase/migrations/20260708000001_create_campaigns_table.sql` with:

- `CREATE TABLE IF NOT EXISTS public.campaigns (...)` with fields:
  - `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`
  - `store_id UUID NOT NULL REFERENCES public.stores(id) ON DELETE CASCADE`
  - `status TEXT NOT NULL DEFAULT 'generating'` CHECK `status IN ('generating', 'ready', 'error')`
  - `product_name TEXT NOT NULL`
  - `input_snapshot JSONB NOT NULL DEFAULT '{}'::jsonb`
  - `identity_snapshot JSONB`
  - `generation_metadata JSONB`
  - `render_snapshot JSONB`
  - `publication_copy_snapshot JSONB`
  - `storage_path TEXT NOT NULL`
  - `error_message TEXT`
  - `created_at TIMESTAMPTZ NOT NULL DEFAULT now()`
  - `updated_at TIMESTAMPTZ NOT NULL DEFAULT now()`
- CONSTRAINT `chk_campaigns_error_message`: `CHECK (status <> 'error' OR nullif(trim(error_message), '') IS NOT NULL)`
- Function `public.update_campaigns_updated_at()` RETURNS TRIGGER
- Trigger `trg_campaigns_updated_at` BEFORE UPDATE ON public.campaigns FOR EACH ROW
- `ALTER TABLE public.campaigns ENABLE ROW LEVEL SECURITY`
- Policy `owner_select_campaigns` FOR SELECT TO authenticated USING subquery
- `GRANT SELECT ON TABLE public.campaigns TO authenticated`
- Index `idx_campaigns_store_id` ON public.campaigns (store_id) — prefer `IF NOT EXISTS` for idempotency
- Index `idx_campaigns_created_at` ON public.campaigns (created_at DESC) — prefer `IF NOT EXISTS` for idempotency
- Revert commands as comments at end of file
</action>

<acceptance_criteria>
- File `supabase/migrations/20260708000001_create_campaigns_table.sql` exists
- File contains `CREATE TABLE IF NOT EXISTS public.campaigns`
- File contains `chk_campaigns_error_message`
- File contains `CREATE OR REPLACE FUNCTION public.update_campaigns_updated_at`
- File contains `CREATE TRIGGER trg_campaigns_updated_at`
- File contains `ALTER TABLE public.campaigns ENABLE ROW LEVEL SECURITY`
- File contains `CREATE POLICY "owner_select_campaigns"`
- File contains `GRANT SELECT ON TABLE public.campaigns TO authenticated`
- File contains `CREATE INDEX IF NOT EXISTS idx_campaigns_store_id`
- File contains `CREATE INDEX IF NOT EXISTS idx_campaigns_created_at`
- File ends with revert commands as SQL comments
</acceptance_criteria>

### Task 12-01b: Validate migration SQL syntax

<read_first>
- supabase/migrations/20260708000001_create_campaigns_table.sql (created in task 12-01)
</read_first>

<action>
Run `supabase db lint --file supabase/migrations/20260708000001_create_campaigns_table.sql` or manually review SQL syntax for correctness.
</action>

<acceptance_criteria>
- No SQL syntax errors found
- All column types, defaults, and constraints match spec
</acceptance_criteria>

## Verification Criteria

- [ ] Migration file exists with correct timestamp prefix
- [ ] All 12 columns defined with correct types, defaults, and nullability
- [ ] FK `store_id → stores(id) ON DELETE CASCADE` present
- [ ] CHECK constraints `status IN (...)` and `chk_campaigns_error_message` present
- [ ] Trigger function + trigger on BEFORE UPDATE present
- [ ] RLS enabled + SELECT policy for owner present
- [ ] GRANT SELECT TO authenticated present
- [ ] Both indexes present (store_id, created_at DESC)
- [ ] Revert commands documented

## must_haves

- campaigns table DDL with all fields
- error_message CHECK constraint
- updated_at trigger scoped
- RLS with owner SELECT policy
- store_id + created_at indexes
- Revert commands
---
plan_id: 12-02
wave: 1
objective: Migration 2 — Create campaign-images bucket and Storage policies
depends_on:
  - 12-01
requirements_addressed:
  - REQ-BUCKET-CAMPAIGN-IMAGES
  - REQ-SELECT-POLICY
  - REQ-INSERT-POLICY
  - REQ-DELETE-POLICY
  - REQ-NO-UPDATE-POLICY
files_modified:
  - supabase/migrations/20260708000002_create_campaign_images_bucket.sql
autonomous: true
---

## Tasks

### Task 12-02a: Write Storage bucket migration

<read_first>
- .planning/phases/12-fundacao-db-storage/12-CONTEXT.md (decisions D3, D5, D6)
- openspec/changes/fase-12-fundacao-db-storage/specs/campaign-images-storage/spec.md
- supabase/migrations/20260708000001_create_campaigns_table.sql (adjacent file for naming pattern)
</read_first>

<action>
Create `supabase/migrations/20260708000002_create_campaign_images_bucket.sql` with:

- INSERT INTO storage.buckets (id, name, public, file_size_limit, allowed_mime_types)
  - id = `'campaign-images'`, name = `'campaign-images'`
  - public = `false`
  - file_size_limit = `10485760` (10MB)
  - allowed_mime_types = `ARRAY['image/png', 'image/jpeg', 'image/webp']`
  - `ON CONFLICT (id) DO NOTHING` for idempotency
- Policy `owner_select_campaign_images` ON storage.objects FOR SELECT TO authenticated
  - USING: `bucket_id = 'campaign-images' AND (storage.foldername(name))[1] IN (SELECT id::text FROM public.stores WHERE user_id = (SELECT auth.uid()))`
- Policy `service_insert_campaign_images` ON storage.objects FOR INSERT TO service_role:
  - WITH CHECK: `bucket_id = 'campaign-images'`
- Policy `service_delete_campaign_images` ON storage.objects FOR DELETE TO service_role:
  - USING: `bucket_id = 'campaign-images'`
- NO UPDATE policy (intentionally absent — comment documenting imutability)
- Revert commands as comments at end of file
</action>

<acceptance_criteria>
- File `supabase/migrations/20260708000002_create_campaign_images_bucket.sql` exists
- File contains `INSERT INTO storage.buckets` with `'campaign-images'`
- File contains `CREATE POLICY "owner_select_campaign_images"`
- File contains `CREATE POLICY "service_insert_campaign_images"`
- File contains `CREATE POLICY "service_delete_campaign_images"`
- File contains NO `UPDATE` policy name (no `service_update_campaign`)
- File ends with revert commands as SQL comments
- `public = false` is set for the bucket
- `file_size_limit = 10485760` is set
- `allowed_mime_types` includes PNG, JPEG, WEBP
</acceptance_criteria>

### Task 12-02b: Validate no UPDATE policy

<read_first>
- supabase/migrations/20260708000002_create_campaign_images_bucket.sql (created in task 12-02a)
</read_first>

<action>
Manually verify the migration file contains no `FOR UPDATE` policy definition and no policy named `service_update_campaign_*`. The imutability comment (containing the word "UPDATE") is allowed and expected.
</action>

<acceptance_criteria>
- grep for `FOR UPDATE` in policy definitions returns no results
- grep for `service_update_campaign` returns no results
- Comment containing "UPDATE" or "imutabilidade" is present (expected documentation)
</acceptance_criteria>

## Verification Criteria

- [ ] Migration file exists with correct timestamp prefix
- [ ] Bucket is private (public = false)
- [ ] File size limit 10MB (10485760 bytes)
- [ ] Allowed MIME types: PNG, JPEG, WEBP
- [ ] 3 Storage policies exist (SELECT owner, INSERT service_role, DELETE service_role)
- [ ] No UPDATE policy exists
- [ ] On conflict DO NOTHING for idempotency
- [ ] Revert commands documented
- [ ] Path pattern `{storeId}/{campaignId}.png` works with `storage.foldername(name)[1]`

## must_haves

- campaign-images bucket (private, 10MB, PNG/JPEG/WEBP)
- Owner SELECT policy with path prefix verification
- Service_role INSERT policy
- Service_role DELETE policy
- NO UPDATE policy (imutabilidade)
- Revert commands
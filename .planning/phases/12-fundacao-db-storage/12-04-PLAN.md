---
plan_id: 12-04
wave: 2
objective: Smoke test and UAT verification
depends_on:
  - 12-01
  - 12-02
  - 12-03
requirements_addressed:
  - REQ-SMOKE-SQL
  - REQ-UAT-CHECKLIST
files_modified: []
autonomous: false
---

## Tasks

### Task 12-04a: Execute migrations on Supabase local or dev

<read_first>
- supabase/migrations/20260708000001_create_campaigns_table.sql
- supabase/migrations/20260708000002_create_campaign_images_bucket.sql
</read_first>

<action>
Run both migrations against local Supabase instance:
1. `supabase start` (if not already running)
2. `supabase migration up` to apply pending migrations
3. Verify both migrations applied without errors
</action>

<acceptance_criteria>
- `supabase migration up` exits 0
- No errors in migration output
- Both migrations listed as applied
</acceptance_criteria>

### Task 12-04b: Execute smoke SQL script

<read_first>
- scripts/verify-phase12.sql
</read_first>

<action>
Run `psql` or Supabase Studio SQL editor with contents of `scripts/verify-phase12.sql`. All 9 blocks must produce `RAISE NOTICE 'PASS: ...'` without any `RAISE EXCEPTION`.
</action>

<acceptance_criteria>
- Script produces 9 PASS notices
- Zero EXCEPTION raised
</acceptance_criteria>

### Task 12-04c: Manual UAT — 10 technical verifications

<read_first>
- scripts/verify-phase12.sql (UAT checklist section)
- .planning/phases/12-fundacao-db-storage/12-CONTEXT.md
</read_first>

<action>
**Setup note:** Before executing, create minimal fixtures using `supabaseAdmin` (service_role): insert a store + owner user, create a campaign row via direct SQL, upload a test image to `campaign-images`. Use second user/store to test tenant isolation. Clean up all fixture data after verification.

Execute the 10 manual verifications from the UAT checklist:
1. Owner vê próprias campaigns via RLS
2. Outro tenant vê 0 resultados (sem vazamento)
3. `updated_at` muda no UPDATE
4. `status='error'` sem mensagem rejeita
5. Client-side INSERT em `campaigns` falha
6. Bucket `campaign-images` é privado (URL pública falha)
7. Upload client-side falha
8. Service_role consegue upload + delete
9. Signed URL permite leitura
10. URL pública não funciona
</action>

<acceptance_criteria>
- All 10 checks pass
- RLS isolation confirmed (tenant isolation)
- Immutability confirmed (no UPDATE Storage policy)
- Signed URL works, public URL fails
</acceptance_criteria>

## Verification Criteria

- [ ] Both migrations applied without errors
- [ ] Smoke SQL: 9/9 PASS
- [ ] UAT: 10/10 checks pass
- [ ] Tenant isolation verified
- [ ] Storage immutability verified (no UPDATE)

## must_haves

- All 9 smoke checks pass
- All 10 UAT checks pass
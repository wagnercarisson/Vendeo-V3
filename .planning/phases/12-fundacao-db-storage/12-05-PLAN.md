---
plan_id: 12-05
wave: 2
objective: Type check, lint, build, and verify app code unchanged
depends_on:
  - 12-01
  - 12-02
  - 12-03
requirements_addressed: []
files_modified: []
autonomous: true
---

## Tasks

### Task 12-05a: Run type check

<read_first>
- tsconfig.json (project root)
</read_first>

<action>
Run `npx tsc --noEmit` from project root. Verify zero TypeScript errors. If `supabase gen types` was run, new types for `campaigns` table must compile cleanly.
</action>

<acceptance_criteria>
- `npx tsc --noEmit` exits 0
- Zero TypeScript errors
</acceptance_criteria>

### Task 12-05b: Run lint

<read_first>
- .eslintrc.* or eslint.config.* (project root)
</read_first>

<action>
Run `npm run lint` from project root. Verify zero lint errors.
</action>

<acceptance_criteria>
- `npm run lint` exits 0
- Zero lint errors
</acceptance_criteria>

### Task 12-05c: Run build

<read_first>
- next.config.* (project root)
</read_first>

<action>
Run `npx next build` from project root. Verify build succeeds.
</action>

<acceptance_criteria>
- `npx next build` exits 0
- Build output shows no errors
</acceptance_criteria>

### Task 12-05d: Verify no app code modified

<read_first>
-
</read_first>

<action>
Run `git diff --name-only` to list changed files. Verify that ONLY files in `supabase/migrations/` and `scripts/` are modified. No files under `src/`, `app/`, or other app code directories should appear.
</action>

<acceptance_criteria>
- `git diff --name-only` shows only files in `supabase/migrations/` and `scripts/`
- No `src/` or `app/` files modified
- Fluxo de geração intacto (no changes to generation pipeline)
</acceptance_criteria>

## Verification Criteria

- [ ] TypeScript: zero errors
- [ ] Lint: zero errors
- [ ] Build: successful
- [ ] No app code modified

## must_haves

- TypeScript clean
- Lint clean
- Build clean
- App code unchanged
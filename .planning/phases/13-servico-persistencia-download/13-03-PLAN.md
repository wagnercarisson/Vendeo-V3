---
plan_id: 13-03
wave: 2
objective: Tests — Persistence Service & Download Route
depends_on:
  - 13-01
  - 13-02
requirements_addressed:
  - REQ-TEST-PERSISTENCE
  - REQ-TEST-DOWNLOAD
files_modified:
  - src/__tests__/lib/campaign/persistence.test.ts
  - src/__tests__/api/campaign-download.test.ts
autonomous: true
---

## Tasks

### Task 13-03-a: Create persistence service tests

<read_first>
- src/lib/campaign/persistence.ts (created in 13-01)
- src/lib/campaign/types.ts (created in 13-01)
- openspec/changes/fase-13-servico-persistencia-download/specs/campaign-persistence-service/spec.md (all scenarios)
- src/lib/__tests__/snapshot.test.ts (unit test pattern for lib functions)
- vitest.config.ts or test setup file (test environment config)
</read_first>

<action>
Create `src/__tests__/lib/campaign/persistence.test.ts` with Vitest tests.

Mock pattern (top-level vi.mock):
- Mock `@/lib/supabase/server`: provide `supabaseAdmin` mock with `from()` and `storage.from()` chains
- Use `vi.fn()` for all mock implementations
- Use `beforeEach` with `vi.clearAllMocks()`

Test structure and scenarios:

```
describe("createCampaign", () => {
  it("generates UUID and storage_path, inserts status=generating")
  it("rejects invalid storeId (propagates Supabase error)")
});

describe("dataUrlToCampaignImage", () => {
  it("accepts image/png data URL")
  it("accepts image/jpeg data URL")
  it("accepts image/webp data URL")
  it("rejects unsupported MIME type")
  it("rejects empty string")
  it("rejects malformed data URL")
});

describe("uploadCampaignImage", () => {
  it("uploads to campaign-images bucket with correct path")
  it("uses upsert: false")
  it("uses contentType: image/jpeg")
  it("rejects non-JPEG mimeType")
});

describe("updateCampaignReady", () => {
  it("sets status=ready and persists snapshots")
  it("sets error_message=null")
});

describe("updateCampaignError", () => {
  it("sets status=error with message")
  it("rejects empty message")
});

describe("getCampaign", () => {
  it("returns CampaignRecord when campaign exists")
  it("returns null when campaign does not exist")
});

describe("deleteCampaignImage", () => {
  it("removes object from campaign-images bucket")
});
```

Each test should:
- Set up specific mock return values for the given scenario
- Import the helper dynamically (await import(...)) inside the test body
- Assert on return values, mock call arguments, or thrown errors
- NOT require a real Supabase connection
</action>

<acceptance_criteria>
- File `src/__tests__/lib/campaign/persistence.test.ts` exists
- `describe("createCampaign")` has 2 tests
- `describe("dataUrlToCampaignImage")` has 6 tests (3 accept + 3 reject)
- `describe("uploadCampaignImage")` has 4 tests
- `describe("updateCampaignReady")` has 2 tests
- `describe("updateCampaignError")` has 2 tests
- `describe("getCampaign")` has 2 tests
- `describe("deleteCampaignImage")` has 1 test
- Total minimum: 19 test cases
- All tests pass with `npx vitest run`
- No mock leaks — `vi.clearAllMocks()` in beforeEach
</acceptance_criteria>

### Task 13-03-b: Create download route tests

<read_first>
- src/app/api/campaign/[id]/download/route.ts (created in 13-02)
- src/lib/campaign/persistence.ts (created in 13-01)
- openspec/changes/fase-13-servico-persistencia-download/specs/campaign-download-route/spec.md (all scenarios)
- openspec/changes/fase-13-servico-persistencia-download/specs/multitenant-campaign-guards/spec.md (download guards scenarios)
- src/__tests__/api/store-ownership-api.test.ts (route handler test pattern)
- src/app/api/store/[id]/__tests__/route.test.ts (dynamic route test pattern with params Promise)
</read_first>

<action>
Create `src/__tests__/api/campaign-download.test.ts` with Vitest tests.

Mock pattern:
- Mock `@/lib/supabase/server`: provide `supabaseAdmin` mock with storage chain
- Mock `@/lib/auth/require-user`: provide `requireApiUser` mock
- Mock `@/lib/auth/store-ownership`: provide `requireOwnership` mock
- Mock `@/lib/campaign/persistence`: provide `getCampaign` mock
- Use helper function to create NextRequest with appropriate URL

Test scenarios:

```
describe("GET /api/campaign/[id]/download", () => {
  it("returns 401 when not authenticated")
  it("returns 400 when [id] is not a valid UUID v4")
  it("returns 404 when campaign does not exist")
  it("returns 404 when campaign belongs to another store (ownership mismatch)")
  it("returns 302 with signed URL for own campaign")
  it("returns 502 when createSignedUrl fails")
});
```

Key test implementation details:
- For 401 test: mock `requireApiUser` to reject with `UnauthorizedError`
- For 400 test: pass non-UUID string as `[id]` param
- For 404 not-found: mock `getCampaign` to return `null`
- For 404 ownership: mock `getCampaign` to return a campaign, mock `requireOwnership` to reject with `StoreNotFoundError`
- For 302: mock all deps to succeed, assert status 302 and Location header present
- For 502: mock `createSignedUrl` in supabaseAdmin to return `{ error: new Error("...") }`
- Pass params as `{ params: Promise.resolve({ id: UUID }) }`
</action>

<acceptance_criteria>
- File `src/__tests__/api/campaign-download.test.ts` exists
- `describe("GET /api/campaign/[id]/download")` has 6 tests
- All test scenarios covered: 401, 400, 404 (not found), 404 (ownership), 302, 502
- Tests use dynamic `import()` inside test bodies (not top-level)
- All tests pass with `npx vitest run`
</acceptance_criteria>

### Task 13-03-c: Run all tests and verify

<read_first>
- src/__tests__/lib/campaign/persistence.test.ts (created)
- src/__tests__/api/campaign-download.test.ts (created)
</read_first>

<action>
Run:
- `npx vitest run src/__tests__/lib/campaign/persistence.test.ts src/__tests__/api/campaign-download.test.ts` — verify all new tests pass
- `npm run typecheck` — verify zero type errors
- `npm run lint` — verify zero lint errors
- `npm run build` — verify build succeeds
- Verify with `git diff --stat` that no files from `generate-image` flow were modified
</action>

<acceptance_criteria>
- All persistence tests pass (19+ test cases)
- All download route tests pass (6 test cases)
- Existing tests not broken (run full suite: `npx vitest run`)
- `npm run typecheck` exits 0
- `npm run lint` exits 0
- `npm run build` exits 0
- No files from generate-image flow modified
</acceptance_criteria>

## Verification Criteria

- [ ] `src/__tests__/lib/campaign/persistence.test.ts` exists with 19+ test cases covering all 7 helpers
- [ ] `src/__tests__/api/campaign-download.test.ts` exists with 6 scenarios
- [ ] All new tests pass
- [ ] All existing tests still pass
- [ ] `npm run typecheck` — zero errors
- [ ] `npm run lint` — zero errors
- [ ] `npm run build` — zero errors
- [ ] No files from generate-image flow modified

## must_haves

- Persistence tests covering createCampaign, dataUrlToCampaignImage, uploadCampaignImage, updateCampaignReady, updateCampaignError, getCampaign, deleteCampaignImage
- Download route tests covering 401, 400, 404 (not found), 404 (ownership), 302, 502
- All existing tests still pass (no regressions)
- TypeScript, lint, build clean

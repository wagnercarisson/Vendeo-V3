---
phase: 16-minhas-campanhas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/campaign/list.ts
  - src/__tests__/lib/campaign/list.test.ts
autonomous: true
requirements:
  - REQ-LIST-CAMPAIGNS
  - REQ-LIST-ITEM
  - REQ-BATCH-THUMBNAILS

must_haves:
  truths:
    - "listCampaigns consulta campaigns via createServerClient (RLS), não supabaseAdmin"
    - "listCampaigns filtra status IN ('ready', 'error'), exclui generating"
    - "listCampaigns ordena por created_at DESC com LIMIT 50"
    - "listCampaigns retorna [] para cross-tenant (RLS filtra) ou loja sem campanhas"
    - "generateBatchThumbnailUrls usa Promise.allSettled para paralelismo"
    - "generateBatchThumbnailUrls só processa itens ready com storagePath não vazio"
    - "Falha parcial em generateBatchThumbnailUrls resulta em thumbnailUrl = null para itens com falha"
    - "CampaignListItem tem id, productName, status, createdAt, thumbnailUrl (string | null), storagePath"
  artifacts:
    - path: "src/lib/campaign/list.ts"
      provides: "listCampaigns, generateBatchThumbnailUrls, CampaignListItem"
      min_lines: 60
    - path: "src/__tests__/lib/campaign/list.test.ts"
      provides: "7+ cenários de teste"
  key_links:
    - from: "list.ts::listCampaigns"
      to: "display.ts::getCampaignForDisplay"
      via: "Mesmo padrão: createServerClient com RLS, mesmo bucket campaign-images"
    - from: "list.ts::generateBatchThumbnailUrls"
      to: "persistence.ts::uploadCampaignImage"
      via: "Ambos usam supabaseAdmin.storage, mesmo bucket campaign-images"
    - from: "list.ts::CampaignListItem"
      to: "types.ts::CampaignRecord"
      via: "CampaignListItem deriva campos de CampaignRecord"
---

<objective>
Criar o contrato de listagem de campanhas (`list.ts`) com `listCampaigns` (RLS, filtro status, ordenação, LIMIT 50), `CampaignListItem` (interface de retorno), e `generateBatchThumbnailUrls` (signed URLs em batch via `Promise.allSettled`). Este contrato é separado do `display.ts` (exibição individual) porque a listagem tem responsabilidades distintas — consulta plural, thumbnails em batch.

**Purpose:** Prover as funções de leitura que o Server Component da listagem usará para carregar campanhas via RLS e gerar signed URLs para thumbnails em lote.

**Output:** `src/lib/campaign/list.ts`, `src/__tests__/lib/campaign/list.test.ts`
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-minhas-campanhas/16-CONTEXT.md (D1, D2, D3, D7)
@src/lib/campaign/types.ts (CampaignRecord, CampaignStatus, CampaignListItem deriva daqui)
@src/lib/campaign/display.ts (getCampaignForDisplay — padrão createServerClient com RLS)
@src/lib/campaign/persistence.ts (uploadCampaignImage — padrão supabaseAdmin.storage)
@src/lib/supabase/server.ts (createServerClient, supabaseAdmin exports)
@openspec/changes/fase-16-minhas-campanhas/specs/list-contract/spec.md

<interfaces>
From src/lib/campaign/types.ts:
```typescript
export type CampaignStatus = "generating" | "ready" | "error";

export interface CampaignRecord {
  id: string;
  store_id: string;
  status: CampaignStatus;
  product_name: string;
  // ... outros campos
  storage_path: string;
  created_at: string;
  updated_at: string;
}
```

From src/lib/supabase/server.ts:
```typescript
export async function createServerClient() { /* createSupabaseServerClient with cookies */ }
export const supabaseAdmin = createClient(supabaseUrl, supabaseServiceKey);
```

From src/lib/campaign/display.ts (pattern reference):
```typescript
import "server-only";
import { createServerClient } from "@/lib/supabase/server";

export async function getCampaignForDisplay(id: string): Promise<CampaignRecord | null> {
  const supabase = await createServerClient();
  const { data, error } = await supabase
    .from("campaigns")
    .select("*")
    .eq("id", id)
    .maybeSingle();
  if (error) throw new Error(error.message);
  return data as CampaignRecord | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create list.ts with CampaignListItem, listCampaigns, generateBatchThumbnailUrls</name>
  <files>
    src/lib/campaign/list.ts
  </files>

  <read_first>
  - src/lib/campaign/types.ts (CampaignStatus, CampaignRecord types)
  - src/lib/campaign/display.ts (pattern: createServerClient com RLS)
  - src/lib/campaign/persistence.ts (lines 56-80 — uploadCampaignImage pattern)
  - src/lib/supabase/server.ts (createServerClient, supabaseAdmin exports)
  - openspec/changes/fase-16-minhas-campanhas/specs/list-contract/spec.md
  - .planning/phases/16-minhas-campanhas/16-CONTEXT.md (D1, D2, D7)
  </read_first>

  <action>
  Create `src/lib/campaign/list.ts` with `import "server-only"`, três exports, e um helper interno:

  **A. `CampaignListItem` interface:**
  ```typescript
  export interface CampaignListItem {
    id: string;
    productName: string;
    status: CampaignStatus;
    createdAt: string;
    thumbnailUrl: string | null;
    storagePath: string;
  }
  ```

  **B. `listCampaigns(storeId: string): Promise<CampaignListItem[]>`**
  - Importar `createServerClient` de `@/lib/supabase/server`
  - Chamar `const supabase = await createServerClient()`
  - Query: `supabase.from("campaigns").select("id, product_name, status, created_at, storage_path").eq("store_id", storeId).in("status", ["ready", "error"]).order("created_at", { ascending: false }).limit(50)`
  - Se `error`, lançar `new Error(error.message)`
  - Mapear `(data ?? []).map(mapToListItem)` onde `mapToListItem` é helper interno que converte snake_case row → `CampaignListItem`
  - Chamar `generateBatchThumbnailUrls(items)` (os mapeados sem thumbnailUrl ainda)
  - Retornar `items.map(item => ({ ...item, thumbnailUrl: thumbnails[item.id] ?? null }))` — merge dos resultados

  Helper interno `mapToListItem`:
  ```typescript
  function mapToListItem(row: {
    id: string;
    product_name: string;
    status: string;
    created_at: string;
    storage_path: string;
  }): CampaignListItem {
    return {
      id: row.id,
      productName: row.product_name,
      status: row.status as CampaignStatus,
      createdAt: row.created_at,
      thumbnailUrl: null, // preenchido depois pelo batch
      storagePath: row.storage_path,
    };
  }
  ```

  **C. `generateBatchThumbnailUrls(items: CampaignListItem[]): Promise<Record<string, string | null>>`**
  - Importar `supabaseAdmin` de `@/lib/supabase/server`
  - Filtrar apenas itens com `status === "ready"` e `storagePath` não vazio
  - Para cada item filtrado, criar uma promise: `supabaseAdmin.storage.from("campaign-images").createSignedUrl(item.storagePath, 3600).then(res => ({ id: item.id, url: res.data?.signedUrl ?? null }))`
  - Executar em paralelo via `Promise.allSettled(promises)`
  - Construir `Record<id, string | null>`: para cada settled, se `fulfilled` e `value.url` existe, usar; senão, `null`
  - Retornar o record

  ```typescript
  export async function generateBatchThumbnailUrls(
    items: Pick<CampaignListItem, "id" | "status" | "storagePath">[],
  ): Promise<Record<string, string | null>> {
    const readyItems = items.filter(
      (item) => item.status === "ready" && item.storagePath,
    );

    // Pré-inicializa todos os itens ready com null — falhas mantêm null,
    // sucessos sobrescrevem com a URL. Garante que o record sempre tenha
    // entrada explícita para cada item ready, sem depender de undefined.
    const thumbnails: Record<string, string | null> = {};
    for (const item of readyItems) {
      thumbnails[item.id] = null;
    }

    const results = await Promise.allSettled(
      readyItems.map((item) =>
        supabaseAdmin.storage
          .from("campaign-images")
          .createSignedUrl(item.storagePath, 3600)
          .then((res) => ({ id: item.id, url: res.data?.signedUrl ?? null })),
      ),
    );

    for (const result of results) {
      if (result.status === "fulfilled" && result.value.url) {
        thumbnails[result.value.id] = result.value.url;
      }
      // rejected: mantém null da pré-inicialização
    }
    return thumbnails;
  }
  ```
  </action>

  <acceptance_criteria>
  - File `src/lib/campaign/list.ts` exists with `import "server-only"`
  - Exports `CampaignListItem` interface with all 6 fields
  - Exports `listCampaigns` as async function accepting `(storeId: string)` returning `Promise<CampaignListItem[]>`
  - Exports `generateBatchThumbnailUrls` as async function accepting items array returning `Promise<Record<string, string | null>>`
  - `listCampaigns` uses `createServerClient()`, NOT `supabaseAdmin` for DB query
  - `listCampaigns` filters `status IN ('ready', 'error')`, LIMIT 50, ORDER created_at DESC
  - `generateBatchThumbnailUrls` uses `supabaseAdmin.storage.from("campaign-images").createSignedUrl` with `expiresIn: 3600`
  - `generateBatchThumbnailUrls` only processes items with `status === "ready"` and non-empty `storagePath`
  - `npm run typecheck` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create list.test.ts (7+ scenarios)</name>
  <files>
    src/__tests__/lib/campaign/list.test.ts
  </files>

  <read_first>
  - src/lib/campaign/list.ts (just created in Task 1)
  - openspec/changes/fase-16-minhas-campanhas/specs/list-contract/spec.md (all scenarios)
  - src/__tests__/lib/campaign/display.test.ts (test pattern — vi.mock, beforeEach, dynamic import)
  - src/__tests__/lib/campaign/persistence.test.ts (test pattern)
  </read_first>

  <action>
  Create `src/__tests__/lib/campaign/list.test.ts` with Vitest tests.

  Mock strategy:
  - Mock `@/lib/supabase/server` to return mocked `createServerClient` and `supabaseAdmin`
  - Use `vi.hoisted` for mock data and factory functions (Vitest pattern)
  - Use dynamic `await import(...)` for module under test

  Test structure (7+ scenarios):

  ```
  describe("listCampaigns", () => {
    it("returns campaigns for owner store")
      — mock from().select().eq().in().order().limit() returns { data: [campanha1, campanha2] }
      — assert returns CampaignListItem[] with correct fields

    it("filters by status IN ('ready', 'error') via .in() call")
      — spy/mock .in() to capture its arguments
      — assert .in() was called with ("status", ["ready", "error"])
      — mock returns { data: [readyItem, errorItem] }
      — assert generating items are excluded (query-level, not app-level)

    it("returns [] for store with no campaigns")
      — mock returns { data: [] }
      — assert returns []

    it("returns [] for cross-tenant (RLS filters)")
      — mock returns { data: [] }
      — assert returns []
  });

  describe("generateBatchThumbnailUrls", () => {
    it("generates URLs for ready campaigns")
      — mock createSignedUrl returns { data: { signedUrl: "https://..." } }
      — call with 2 ready items
      — assert returns Record with 2 entries, URLs start with https

    it("does NOT generate URLs for error campaigns")
      — mock createSignedUrl (should not be called for error items)
      — call with 1 ready + 1 error
      — assert ready gets URL; assert createSignedUrl not called for error item
      — assert error item is absent from record (só readyItems entram no loop)
      — caller (listCampaigns) mapeia ausência no record → null via thumbnails[errorId] ?? null

    it("handles partial failure with placeholder")
      — mock createSignedUrl: first resolves, second rejects
      — call with 2 ready items
      — assert first gets URL, second gets null (explicit null in record, not missing key)
  });
  ```

  For the mock items:
  ```typescript
  const mockReadyItem = { id: "id-1", status: "ready" as const, storagePath: "store-123/id-1.jpg" };
  const mockErrorItem = { id: "id-2", status: "error" as const, storagePath: "store-123/id-2.jpg" };
  ```
  </action>

  <acceptance_criteria>
  - File `src/__tests__/lib/campaign/list.test.ts` exists
  - `describe("listCampaigns")` has 4 tests (owner, .in() filter assertion, empty, cross-tenant)
  - `describe("generateBatchThumbnailUrls")` has 3 tests (ready generates, error skipped, partial failure)
  - Total: 7+ test cases
  - Partial failure test asserts `record["item-id"] === null` (explicit null), not `!("item-id" in record)` (missing key)
  - All tests pass with `npx vitest run src/__tests__/lib/campaign/list.test.ts`
  - `npm run typecheck` exits 0
  - `npm run lint` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Supabase anon key | Anon key is public; RLS policies protect data access |
| signed URL | Temporary (1h), scoped to specific storage path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Information Disclosure | listCampaigns | mitigate | Uses createServerClient (RLS) instead of supabaseAdmin — non-owners receive [] |
| T-16-02 | Information Disclosure | generateBatchThumbnailUrls | accept | Called only after RLS filter. Signed URLs expire in 1h |
</threat_model>

<verification>
- [ ] `src/lib/campaign/list.ts` exists with `import "server-only"`
- [ ] All 3 exports present (CampaignListItem, listCampaigns, generateBatchThumbnailUrls)
- [ ] `npx vitest run src/__tests__/lib/campaign/list.test.ts` — 7+ passing
- [ ] `npm run typecheck` — zero errors
- [ ] `npm run lint` — zero errors
- [ ] `npm run build` — zero errors
</verification>

<success_criteria>
1. `listCampaigns` consulta via RLS (createServerClient), não supabaseAdmin
2. Filtro status ready+error, exclui generating
3. LIMIT 50 + ORDER created_at DESC
4. `generateBatchThumbnailUrls` em paralelo com Promise.allSettled, só ready
5. Falha parcial → placeholder (null)
6. 7+ cenários de teste verdes
7. TypeScript, lint, build limpos
</success_criteria>

<output>
Create `.planning/phases/16-minhas-campanhas/16-01-SUMMARY.md` when done
</output>

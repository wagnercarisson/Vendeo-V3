---
phase: 16-minhas-campanhas
plan: 03
type: execute
wave: 2
depends_on:
  - 16-01
  - 16-02
files_modified:
  - src/__tests__/app/minhas-campanhas.test.tsx
  - src/__tests__/lib/campaign/list.test.ts
autonomous: true
requirements:
  - REQ-TEST-LIST
  - REQ-TEST-PAGE-SERVER
  - REQ-TEST-PAGE-DISPLAY
  - REQ-TEST-AUTHHEADER
  - REQ-TEST-REDIRECT
  - REQ-TEST-MIDDLEWARE
  - REQ-BUILD-VERIFICATION

must_haves:
  truths:
    - "list.test.ts testa listCampaigns (owner, empty, cross-tenant, status filter) e generateBatchThumbnailUrls (ready, error, partial failure)"
    - "minhas-campanhas.test.tsx testa Server Component guards, Client Component lista e empty state, AuthHeader link, redirect, middleware matcher"
    - "npm run typecheck, npm run lint, npx vitest run, npm run build — zero erros"
  artifacts:
    - path: "src/__tests__/app/minhas-campanhas.test.tsx"
      provides: "Testes de página, navegação e middleware"
      min_lines: 80
    - path: "src/__tests__/lib/campaign/list.test.ts"
      provides: "7+ cenários de teste do helper"
  key_links:
    - from: "list.test.ts"
      to: "display.test.ts"
      via: "Mesmo padrão de mock (@/lib/supabase/server, vi.hoisted, dynamic import)"
    - from: "minhas-campanhas.test.tsx"
      to: "campanha.test.tsx (se existir)"
      via: "Mesmo padrão de teste de página"
---

<objective>
Criar testes para validação completa da Phase 16: helper de listagem, Server Component guards, Client Component display (lista + empty state), navegação (AuthHeader, back link, redirect), middleware matcher, e build verification.

**Purpose:** Garantir que todos os componentes da Phase 16 funcionam corretamente individualmente e integrados.

**Output:** `src/__tests__/app/minhas-campanhas.test.tsx`, testes complementares em `list.test.ts`, verificação final de build
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-minhas-campanhas/16-CONTEXT.md (D7)
@.planning/phases/16-minhas-campanhas/16-01-PLAN.md (list.ts contract)
@.planning/phases/16-minhas-campanhas/16-02-PLAN.md (page, client, nav)
@openspec/changes/fase-16-minhas-campanhas/specs/campaign-list-tests/spec.md
@src/__tests__/lib/campaign/display.test.ts (test pattern)
@src/__tests__/lib/campaign/persistence.test.ts (test pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete list.test.ts (if not done in 16-01)</name>
  <files>
    src/__tests__/lib/campaign/list.test.ts
  </files>

  <read_first>
  - src/lib/campaign/list.ts
  - openspec/changes/fase-16-minhas-campanhas/specs/campaign-list-tests/spec.md
  </read_first>

  <action>
  Verify that `src/__tests__/lib/campaign/list.test.ts` exists and has all required scenarios from 16-01 Task 2.
  If 16-01 was already implemented, this task is a verification step.
  If not, implement the tests following 16-01's Task 2 instructions.
  </action>

  <acceptance_criteria>
  - File exists with `describe("listCampaigns")` (4 tests) and `describe("generateBatchThumbnailUrls")` (3 tests)
  - All tests pass with `npx vitest run src/__tests__/lib/campaign/list.test.ts`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create minhas-campanhas.test.tsx</name>
  <files>
    src/__tests__/app/minhas-campanhas.test.tsx
  </files>

  <read_first>
  - src/app/minhas-campanhas/page.tsx (Server Component)
  - src/app/minhas-campanhas/client.tsx (Client Component)
  - src/components/auth/auth-header.tsx
  - src/app/campaign/preview/page.tsx
  - src/middleware.ts
  - openspec/changes/fase-16-minhas-campanhas/specs/campaign-list-tests/spec.md
  - src/__tests__/lib/campaign/display.test.ts (mock pattern)
  </read_first>

  <action>
  Create `src/__tests__/app/minhas-campanhas.test.tsx` with the following test groups:

  **A. Server Component Guards:**
  - Verifica que `requirePageUser()` é chamado na renderização
  - Verifica que `getCurrentStore(user.userId)` é chamado com userId correto
  - Verifica que redirect para `/store` ocorre quando `getCurrentStore` retorna null
  - Verifica que `listCampaigns(store.id)` é chamado com storeId correto

  **B. Client Component Display:**
  - Lista com N campanhas: verifica thumbnail, productName, data formatada dd/mm/aaaa, status, link "Abrir"
  - "Baixar" visível apenas para cards com status "ready"
  - "Baixar" ausente para cards com status "error"
  - Estado vazio exibe "Nenhuma campanha encontrada" + CTA "Criar Primeira Campanha"
  - Placeholder visual quando thumbnailUrl é null

  **C. AuthHeader:**
  - Link "Minhas Campanhas" presente quando autenticado
  - Link "Minhas Campanhas" ausente quando não autenticado

  **D. Server Component Redirect (/campaign/preview):**
  - Autenticado com loja → redirect `/minhas-campanhas`
  - Autenticado sem loja → redirect `/store`
  - Não autenticado → redirect `/login`

  **E. Middleware Matcher:**
  - Verificar que `config.matcher` contém `/minhas-campanhas`
  </action>

  <acceptance_criteria>
  - File `src/__tests__/app/minhas-campanhas.test.tsx` exists
  - Tests cover: Server Component guards, Client Component list display, empty state, AuthHeader link, redirect behavior, middleware matcher
  - All tests pass with `npx vitest run`
  - `npm run typecheck` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Build verification</name>
  <files>
    - (no files — verification commands only)
  </files>

  <read_first>
  - All files created/modified in Phase 16
  </read_first>

  <action>
  Run all verification commands sequentially:
  1. `npm run typecheck` — must exit 0
  2. `npm run lint` — must exit 0
  3. `npx vitest run` — all tests passing (verify total count includes new tests)
  4. `npm run build` — build must succeed
  </action>

  <acceptance_criteria>
  - `npm run typecheck` exits 0 — zero type errors
  - `npm run lint` exits 0 — zero lint errors
  - `npx vitest run` — all tests passing
  - `npm run build` exits 0 — build successful
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test environment | Tests run in isolation, no real database or storage access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Information Disclosure | test mocks | accept | Tests mock all external dependencies; no real data exposure |
</threat_model>

<verification>
- [ ] `src/__tests__/lib/campaign/list.test.ts` — 7+ tests passing
- [ ] `src/__tests__/app/minhas-campanhas.test.tsx` — tests passing
- [ ] `npm run typecheck` — zero errors
- [ ] `npm run lint` — zero errors
- [ ] `npx vitest run` — all tests passing
- [ ] `npm run build` — zero errors
</verification>

<success_criteria>
1. Helper de listagem testado: owner, vazio, cross-tenant, filtro status
2. Thumbnail batch testado: ready, error, falha parcial
3. Server Component testado: auth, ownership, redirects
4. Client Component testado: lista, empty state, AuthHeader
5. Redirect de /campaign/preview testado
6. Middleware matcher verificado
7. TypeScript, lint, testes, build — todos verdes
</success_criteria>

<output>
Create `.planning/phases/16-minhas-campanhas/16-03-SUMMARY.md` when done
</output>

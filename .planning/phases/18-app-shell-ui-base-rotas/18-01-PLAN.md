---
id: 18-01
wave: 1
title: UI Base + Estrutura + Redirects
depends_on: []
files_modified:
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/ui/input.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/empty-state.tsx
  - src/components/ui/skeleton.tsx
  - src/components/ui/page-header.tsx
  - src/app/layout.tsx
  - src/app/page.tsx
  - next.config.ts
  - src/app/(app)/layout.tsx
  - src/app/(app)/dashboard/page.tsx
  - src/app/(app)/campanhas/page.tsx
  - src/app/(app)/campanhas/nova/page.tsx
  - src/app/(app)/campanhas/[id]/page.tsx
  - src/app/(app)/loja/page.tsx
  - src/app/(app)/conta/page.tsx
  - src/__tests__/components/ui/button.test.tsx
  - src/__tests__/components/ui/card.test.tsx
  - src/__tests__/components/ui/input.test.tsx
  - src/__tests__/components/ui/badge.test.tsx
  - src/__tests__/components/ui/empty-state.test.tsx
  - src/__tests__/components/ui/skeleton.test.tsx
  - src/__tests__/components/ui/page-header.test.tsx
autonomous: true
---

# 18-01: UI Base + Estrutura + Redirects

Criar 7 componentes base de UI, estrutura de diretórios `(app)/`, limpar root layout, configurar redirects 301 em next.config.ts, e criar testes unitários.

## Tasks

### 1.1 Criar Button component

<read_first>
- Openspec specs/ui-base-components/spec.md
- openspec/changes/fase-18-app-shell-ui-base-rotas/design.md (D7)
</read_first>

<action>
Create `src/components/ui/button.tsx` with:
- variant prop: "primary" (bg-accent-green, text-white), "secondary" (border border-border, text-text-primary, bg-transparent), "ghost" (bg-transparent, text-text-secondary, hover bg-bg-elevated)
- size prop: "sm", "md", "lg" with corresponding padding/font-size
- disabled attribute with reduced opacity
- loading state: Lucide Loader2 animate-spin + disabled behavior
- forward className prop
- MUST NOT use asChild/Slot pattern
- Use `"use client"` directive
</action>

<acceptance_criteria>
- `src/components/ui/button.tsx` exists with "use client" directive
- Renders primary variant with bg-accent-green and text-white classes
- Renders disabled button with disabled attr and opacity-50
- Renders loading state with Loader2 icon and disabled behavior
- TypeScript compiles without errors
</acceptance_criteria>

### 1.2 Criar Card component

<read_first>
- Openspec specs/ui-base-components/spec.md
</read_first>

<action>
Create `src/components/ui/card.tsx` as Server Component with:
- bg-bg-surface, border, rounded-xl default classes
- className prop for extension
- children prop
</action>

<acceptance_criteria>
- `src/components/ui/card.tsx` exists (no "use client" directive)
- Renders div with bg-bg-surface, border, rounded-xl classes
- Renders children content
</acceptance_criteria>

### 1.3 Criar Input component

<read_first>
- Openspec specs/ui-base-components/spec.md
</read_first>

<action>
Create `src/components/ui/input.tsx` as Client Component with:
- label prop rendered as `<label>` above input
- error prop for inline validation message (red/accent-red text below input)
- ...input spread for native input attributes
- Styled with bg-bg-surface, text-text-primary, border-border, focus ring-accent-green
</action>

<acceptance_criteria>
- `src/components/ui/input.tsx` exists with "use client" directive
- Renders label element above input
- Renders error text below input with error color
- Input has design token styling
</acceptance_criteria>

### 1.4 Criar Badge component

<read_first>
- Openspec specs/ui-base-components/spec.md
</read_first>

<action>
Create `src/components/ui/badge.tsx` as Server Component with:
- variant prop: "ready" (green), "error" (red), "generating" (amber), "default" (neutral gray)
- Each variant uses appropriate bg and text design tokens
</action>

<acceptance_criteria>
- `src/components/ui/badge.tsx` exists (no "use client")
- Badge variant="ready" renders with green styling
- Badge variant="error" renders with red styling
- Badge variant="default" renders with neutral styling
</acceptance_criteria>

### 1.5 Criar EmptyState component

<read_first>
- Openspec specs/ui-base-components/spec.md
</read_first>

<action>
Create `src/components/ui/empty-state.tsx` as Server Component with:
- icon prop: Lucide icon component rendered centered above text
- title prop: heading text
- description prop: body text
- action prop: optional ReactNode (button/Link)
</action>

<acceptance_criteria>
- `src/components/ui/empty-state.tsx` exists (no "use client")
- Renders icon, title, description, and optional action
</acceptance_criteria>

### 1.6 Criar Skeleton component

<read_first>
- Openspec specs/ui-base-components/spec.md
</read_first>

<action>
Create `src/components/ui/skeleton.tsx` as Server Component with:
- width prop: CSS width value
- height prop: CSS height value
- rounded prop: border-radius value
- animate-pulse class for loading animation
- bg-bg-elevated background
</action>

<acceptance_criteria>
- `src/components/ui/skeleton.tsx` exists (no "use client")
- Renders with correct width, height, rounded styles
- Has animate-pulse class
</acceptance_criteria>

### 1.7 Criar PageHeader component

<read_first>
- Openspec specs/ui-base-components/spec.md
</read_first>

<action>
Create `src/components/ui/page-header.tsx` with:
- title prop: heading text
- breadcrumbs prop: optional array of { label: string; href?: string }
  - If href is present, render as Link — otherwise plain text
- actions prop: optional ReactNode slot
- Container with bottom border using border-border
</action>

<acceptance_criteria>
- `src/components/ui/page-header.tsx` exists
- Renders title as heading
- Renders breadcrumbs with links for href items and plain text for current page
- Renders actions slot when provided
</acceptance_criteria>

### 1.8 Criar estrutura de diretórios (app)/

<read_first>
- design.md (D1 — Route group structure)
</read_first>

<action>
Create empty page files under `src/app/(app)/`:
- `layout.tsx` — skeleton (just {children}), will be filled in 18-02
- `dashboard/page.tsx` — empty placeholder
- `campanhas/page.tsx` — empty
- `campanhas/nova/page.tsx` — empty
- `campanhas/[id]/page.tsx` — empty
- `loja/page.tsx` — empty
- `conta/page.tsx` — empty

Ensure each page exports a default component.
</action>

<acceptance_criteria>
- Directory `src/app/(app)/` exists
- layout.tsx renders {children}
- All 6 page files exist with default exports
- Build compiles without errors
</acceptance_criteria>

### 1.9 Modificar root layout — remover AuthHeader

<read_first>
- src/app/layout.tsx
- design.md (D12 — AuthHeader removido)
</read_first>

<action>
Modify `src/app/layout.tsx`:
- Remove `<header>` with AuthHeader component
- Keep html, body, fonts suspense, globals.css
- Simplify to clean root layout
- Remove import of AuthHeader
</action>

<acceptance_criteria>
- src/app/layout.tsx no longer imports or references AuthHeader
- No `<header>` element in root layout
- Body still wraps children with fonts and globals
- Build compiles without errors
</acceptance_criteria>

### 1.10 Modificar root page para redirect

<read_first>
- src/app/page.tsx
- design.md (D2 — redirects)
</read_first>

<action>
Replace `src/app/page.tsx` content with simple redirect:
- Import `redirect` from `next/navigation`
- Export default function that calls `redirect("/dashboard")`
- Remove all campaign form content (will be in (app)/campanhas/nova)
</action>

<acceptance_criteria>
- src/app/page.tsx calls redirect("/dashboard")
- No campaign form content in root page
- Build compiles without errors
</acceptance_criteria>

### 1.11 Adicionar redirects 301 em next.config.ts

<read_first>
- next.config.ts
- design.md (D2 — redirects table)
- specs/route-redirects/spec.md
</read_first>

<action>
Add `async redirects()` in `next.config.ts` with 5 permanent (301) redirects:
- `/` → `/dashboard`
- `/minhas-campanhas` → `/campanhas`
- `/campanha/:id` → `/campanhas/:id`
- `/store` → `/loja`
- `/campaign/preview` → `/campanhas/nova`

All redirects are path-based, no auth logic.
</action>

<acceptance_criteria>
- next.config.ts exports async redirects function with 5 entries
- Each entry has source, destination, permanent: true
- Build compiles without errors
</acceptance_criteria>

### 1.12 Criar testes dos 7 componentes base

<read_first>
- specs/ui-base-components/spec.md (scenarios for each component)
</read_first>

<action>
Create test files in `src/__tests__/components/ui/`:
- `button.test.tsx` — test primary renders, disabled attr, loading shows spinner
- `card.test.tsx` — test base classes and children render
- `input.test.tsx` — test label, placeholder, error message
- `badge.test.tsx` — test ready, error, default variants
- `empty-state.test.tsx` — test icon, title, description, action render
- `skeleton.test.tsx` — test custom dimensions and animate-pulse
- `page-header.test.tsx` — test title, breadcrumbs with/without href, actions

One scenario per test file (7 tests total).
</acceptance_criteria>

- 7 test files exist in src/__tests__/components/ui/
- `npx vitest run` passes all 7 tests

### 1.13 Verificar TypeScript e lint

<read_first>
- All files modified in this plan
</read_first>

<action>
Run `npm run typecheck` and `npm run lint`. Fix any errors.
</action>

<acceptance_criteria>
- `npm run typecheck` exits with code 0
- `npm run lint` exits with code 0
</acceptance_criteria>

---

## Verification

- [ ] 7 componentes UI criados em src/components/ui/
- [ ] Estrutura (app)/ criada com pages vazias
- [ ] Root layout sem AuthHeader
- [ ] Root page redireciona para /dashboard
- [ ] 5 redirects 301 em next.config.ts
- [ ] 7 testes passando
- [ ] typecheck e lint limpos

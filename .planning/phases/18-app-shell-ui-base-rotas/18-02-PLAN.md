---
id: 18-02
wave: 2
title: App Shell
depends_on:
  - 18-01
files_modified:
  - src/components/shell/sidebar.tsx
  - src/components/shell/topbar.tsx
  - src/components/shell/account-menu.tsx
  - src/components/shell/sidebar-drawer.tsx
  - src/components/shell/app-shell.tsx
  - src/app/(app)/layout.tsx
  - src/__tests__/components/shell/sidebar.test.tsx
  - src/__tests__/components/shell/topbar.test.tsx
  - src/__tests__/components/shell/account-menu.test.tsx
  - src/__tests__/components/shell/sidebar-drawer.test.tsx
  - src/__tests__/components/shell/app-shell.test.tsx
autonomous: true
---

# 18-02: App Shell

Construir App Shell completo: sidebar com navegação e destaque ativo, topbar com CTA e menu de conta, drawer mobile, e layout protegido.

## Tasks

### 2.1 Criar Sidebar

<read_first>
- design.md (D1, D9, D10)
- specs/app-shell/spec.md (Sidebar requirement)
</read_first>

<action>
Create `src/components/shell/sidebar.tsx`:
- Client Component (uses usePathname)
- Links: Dashboard (LayoutDashboard icon), Campanhas (Megaphone), Loja (Store), Conta (UserCircle)
- Active route highlighted via usePathname() match
- Design tokens: bg-bg-surface, text-text-primary (active), text-text-muted (inactive), accent-* for active indicator
- Inactive links have hover:bg-bg-elevated
- Accept `isDrawer` boolean prop for drawer variant styling
- Accept `onNavigate` optional callback for drawer close
- aria-label on nav for accessibility
</action>

<acceptance_criteria>
- src/components/shell/sidebar.tsx exists with "use client" directive
- Renders 4 nav links: Dashboard (/dashboard), Campanhas (/campanhas), Loja (/loja), Conta (/conta)
- Current path link has active/highlighted styling
- Other links have inactive styling
- Build compiles
</acceptance_criteria>

### 2.2 Criar Topbar

<read_first>
- design.md (D9, D12)
- specs/app-shell/spec.md (Topbar requirement)
</read_first>

<action>
Create `src/components/shell/topbar.tsx`:
- Client Component
- Logo/app name on left (text "Vendeo" with text-text-primary)
- CTA button "Nova Campanha" (Link to /campanhas/nova) using primary variant classes
- AccountMenu component on right
- Hamburger button (Lucide Menu) visible only on mobile (<768px, hidden md:inline-flex)
- Hamburger triggers onToggleMenu prop callback
- aria-controls, aria-expanded, aria-label on hamburger button (referencing drawer id)
- Design tokens throughout
</action>

<acceptance_criteria>
- src/components/shell/topbar.tsx exists with "use client"
- Renders logo/app name "Vendeo"
- Renders CTA "Nova Campanha" linking to /campanhas/nova
- Renders AccountMenu on right
- Hamburger button visible on mobile, hidden on desktop
</acceptance_criteria>

### 2.3 Criar AccountMenu

<read_first>
- design.md (D5, D12)
- specs/app-shell/spec.md (AccountMenu requirement)
</read_first>

<action>
Create `src/components/shell/account-menu.tsx`:
- Client Component with dropdown state
- Trigger: user identifier (name/email short, fallback claims.sub?.slice(0,8))
- Accept user prop matching AuthenticatedUser["claims"] shape: { email?: string; sub?: string }
- Dropdown with:
  - "Configurações" → /conta (Settings icon)
  - "Sair" → LogoutButton (LogOut icon)
- Styled with design tokens
- Dropdown closes on outside click or item click
</action>

<acceptance_criteria>
- src/components/shell/account-menu.tsx exists
- Shows user identifier in trigger
- Dropdown shows Configurações link to /conta and Sair button
- Build compiles
</acceptance_criteria>

### 2.4 Criar SidebarDrawer

<read_first>
- design.md (D9)
- specs/app-shell/spec.md (SidebarDrawer requirement)
</read_first>

<action>
Create `src/components/shell/sidebar-drawer.tsx`:
- Client Component
- Drawer has id attribute for aria-controls reference from Topbar hamburger
- Drawer slides from left (fixed, left-0, top-0, h-full, w-72 max-w-[280px], z-50)
- Overlay backdrop (fixed inset-0, bg-black/50, z-40)
- Controlled by `isOpen` and `onClose` props
- Escape key closes
- Overlay click closes
- Contains Sidebar with isDrawer=true and onNavigate=onClose
- Body scroll lock when open (overflow-hidden on document.body)
- aria attributes: aria-controls, aria-expanded, aria-label on hamburger reference
- Transition for smooth slide-in
</action>

<acceptance_criteria>
- src/components/shell/sidebar-drawer.tsx exists with "use client"
- Drawer slides in from left when isOpen=true
- Overlay click closes drawer
- Escape key closes drawer
- Body has overflow-hidden when open
- Build compiles
</acceptance_criteria>

### 2.5 Criar AppShell

<read_first>
- design.md (D1, D10)
- specs/app-shell/spec.md (App Shell layout requirement)
</read_first>

<action>
Create `src/components/shell/app-shell.tsx`:
- Client Component
- Composes: Topbar (with hamburger toggle) + Sidebar (desktop, hidden on mobile via class) + main content area
- Manages drawer open/close state
- Layout: sidebar fixed left, topbar fixed top, main content offset for both
- Responsive: sidebar visible on md+ screens, drawer on <md screens
- Does NOT depend on store — only auth (user prop)
- Accept `user` and `children` props
</action>

<acceptance_criteria>
- src/components/shell/app-shell.tsx exists
- Renders Topbar + Sidebar (desktop) + children + SidebarDrawer (mobile)
- Desktop: sidebar visible on md+
- Mobile: drawer accessible via hamburger
- Build compiles
</acceptance_criteria>

### 2.6 Implementar (app)/layout.tsx com AppShell

<read_first>
- src/app/(app)/layout.tsx (created in 18-01)
- design.md (D1, D10)
- specs/auth-middleware/spec.md
</read_first>

<action>
Replace the skeleton in `src/app/(app)/layout.tsx`:
- Import and call `requirePageUser()` to protect the group
- Get user data from requirePageUser
- Render `<AppShell user={user}>{children}</AppShell>`
- Do NOT call getCurrentStore()
</action>

<acceptance_criteria>
- src/app/(app)/layout.tsx calls requirePageUser()
- Does not call getCurrentStore()
- Renders AppShell wrapping children
- Unauthenticated access redirects to /login
- Build compiles
</acceptance_criteria>

### 2.7 Criar testes do shell

<read_first>
- All component files created in this plan
- specs/app-shell/spec.md (all scenarios)
</read_first>

<action>
Create test files in `src/__tests__/components/shell/`:
- `sidebar.test.tsx` — renders 4 links, highlights active route
- `topbar.test.tsx` — renders CTA "Nova Campanha", hamburger on mobile
- `account-menu.test.tsx` — shows Configurações and Sair in dropdown
- `sidebar-drawer.test.tsx` — opens/closes with overlay, closes on escape
- `app-shell.test.tsx` — renders all parts, tolerates store=null

6-8 scenarios total across all test files.
</acceptance_criteria>

- Test files exist in src/__tests__/components/shell/
- `npx vitest run` passes all shell tests

### 2.8 Verificar TypeScript e lint

<read_first>
- All files modified in this plan
</read_first>

<action>
Run `npm run typecheck` and `npm run lint`. Fix any errors.
</action>

<acceptance_criteria>
- `npm run typecheck` exits with code 0
- `npm run lint` exits with code 0
</acceptance_criteria>

---

## Verification

- [ ] Sidebar com 4 links e destaque de rota ativa
- [ ] Topbar com logo, CTA "Nova Campanha", AccountMenu
- [ ] AccountMenu com Configurações e Sair
- [ ] Drawer mobile abre/fecha, overlay, escape, scroll lock
- [ ] AppShell compõe todos os componentes
- [ ] (app)/layout protegido com requirePageUser, não depende de store
- [ ] Testes do shell passam
- [ ] typecheck e lint limpos

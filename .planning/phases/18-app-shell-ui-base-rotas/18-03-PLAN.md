---
id: 18-03
wave: 3
title: Rotas + Migração Visual
depends_on:
  - 18-01
  - 18-02
files_modified:
  - src/app/(app)/campanhas/nova/page.tsx
  - src/app/(app)/campanhas/page.tsx
  - src/app/(app)/campanhas/[id]/page.tsx
  - src/app/(app)/campanhas/[id]/client.tsx
  - src/app/(app)/loja/page.tsx
  - src/app/(app)/dashboard/page.tsx
  - src/app/(app)/conta/page.tsx
  - src/middleware.ts
  - src/components/auth/auth-header.tsx (REMOVED)
  - src/components/auth/logout-button.tsx
  - src/app/api/campaign/generate-image/route.ts
  - src/components/flow/use-campaign-form.ts
  - src/__tests__/next.config.test.ts
  - src/__tests__/middleware.test.ts
  - src/__tests__/api/campaign-generate.test.ts
  - src/__tests__/components/flow/__tests__/use-campaign-form-navigation.test.ts
  - src/app/minhas-campanhas/ (REMOVED)
  - src/app/campanha/[id]/ (REMOVED)
  - src/app/store/ (REMOVED)
  - src/app/campaign/preview/ (REMOVED)
autonomous: true
---

# 18-03: Rotas + Migração Visual

Migrar conteúdo de todas as páginas existentes para as novas rotas PT-BR, criar dashboard placeholder e página de conta, atualizar middleware, remover AuthHeader, aplicar design tokens, e criar testes de redirects/middleware.

## Tasks

### 3.1 Migrar campaign form para /campanhas/nova

<read_first>
- src/app/page.tsx (current campaign form — READ BEFORE OVERWRITE)
- specs/campaign-input-ui/spec.md
</read_first>

<action>
Copy campaign form content from the original `src/app/page.tsx` (preserved below the redirect in 18-01, or from git HEAD before 18-01 cleanup) to `src/app/(app)/campanhas/nova/page.tsx`:
- Apply design tokens (replace slate-/blue-/gray- classes with text-text-*/accent-*/border-*)
- Update store redirect: `if (!store) redirect("/loja")`
- Update internal links to new PT-BR routes
- Keep all functional behavior (form state, validation, submission)
- Do NOT use PageHeader yet (content will restructure naturally)
</action>

<acceptance_criteria>
- src/app/(app)/campanhas/nova/page.tsx contains the campaign form
- Redirect without store → /loja
- Design tokens applied throughout
- Build compiles
</acceptance_criteria>

### 3.2 Migrar minhas-campanhas para /campanhas

<read_first>
- src/app/minhas-campanhas/ (all files — READ BEFORE OVERWRITE)
- specs/campaign-list-ui/spec.md
</read_first>

<action>
Copy campaign list content to `src/app/(app)/campanhas/page.tsx`:
- Apply design tokens
- Update store redirect → /loja
- Update links: "Abrir" → /campanhas/[id], CTA vazio → /campanhas/nova
- Remove "← Campanhas" back link (breadcrumbs via PageHeader in later refactor)
- Keep listCampaigns, thumbnail, status Badge, date formatting
</action>

<acceptance_criteria>
- src/app/(app)/campanhas/page.tsx lists campaigns
- Links point to new PT-BR routes
- Design tokens applied
- Build compiles
</acceptance_criteria>

### 3.3 Migrar campanha/[id] para /campanhas/[id]

<read_first>
- src/app/campanha/[id]/ (all files — READ BEFORE OVERWRITE)
- specs/campaign-page-ui/spec.md
</read_first>

<action>
Copy campaign detail page content to `src/app/(app)/campanhas/[id]/`:
- Migrate page.tsx (Server Component)
- Migrate client.tsx (Client Component)
- Replace emoji icons with Lucide equivalents
- Apply design tokens
- Update redirect without store → /loja
- Update "← Campanhas" link → /campanhas
- Update campaignUrl references to new path
- Use PageHeader component with breadcrumbs: [{ label: "Campanhas", href: "/campanhas" }, { label: campaign name }]
</action>

<acceptance_criteria>
- src/app/(app)/campanhas/[id]/page.tsx exists as Server Component
- src/app/(app)/campanhas/[id]/client.tsx exists as Client Component
- Emoji icons replaced with Lucide
- Design tokens applied
- Links use new PT-BR routes
- Build compiles
</acceptance_criteria>

### 3.4 Migrar store para /loja

<read_first>
- src/app/store/ (all files — READ BEFORE OVERWRITE)
- specs/store-identity-ui/spec.md
</read_first>

<action>
Copy store identity content to `src/app/(app)/loja/page.tsx`:
- Review existing tokens (store already uses some tokens)
- Apply/adjust design tokens where missing
- Update links to new PT-BR routes (/campanhas/nova, etc.)
- Use PageHeader with breadcrumbs: [{ label: "Loja" }]
</action>

<acceptance_criteria>
- src/app/(app)/loja/page.tsx has store identity form
- Design tokens applied
- Links use new PT-BR routes
- Build compiles
</acceptance_criteria>

### 3.5 Criar dashboard placeholder

<read_first>
- design.md (D4)
- specs/dashboard-placeholder/spec.md
</read_first>

<action>
Replace the empty `src/app/(app)/dashboard/page.tsx`:
- Import PageHeader and EmptyState
- `<PageHeader title="Dashboard" />`
- `<EmptyState icon={LayoutDashboard} title="Em breve" description="Suas métricas e atalhos aparecerão aqui." />`
- Design tokens already applied via components
</action>

<acceptance_criteria>
- src/app/(app)/dashboard/page.tsx renders PageHeader + EmptyState
- Build compiles
</acceptance_criteria>

### 3.6 Criar /conta página

<read_first>
- design.md (D5)
- specs/conta-page/spec.md
</read_first>

<action>
Replace the empty `src/app/(app)/conta/page.tsx`:
- Import requirePageUser to get user data
- Display email via `claims.email`, fallback `claims.sub?.slice(0, 8)`
- `<PageHeader title="Conta" />`
- Card with user info sections
- Link to /update-password
- LogoutButton for "Sair"
- Design tokens via components
</action>

<acceptance_criteria>
- src/app/(app)/conta/page.tsx shows user email
- Link to /update-password present
- LogoutButton for Sair present
- Build compiles
</acceptance_criteria>

### 3.7 Atualizar middleware matcher

<read_first>
- src/middleware.ts
- specs/auth-middleware/spec.md
</read_first>

<action>
Update `src/middleware.ts`:
- Add to matcher: "/dashboard", "/campanhas/:path*", "/loja", "/conta"
- Remove from matcher: "/", "/store/:path*", "/campanha/:path*"
- Keep: "/login", "/signup", "/check-email", "/forgot-password", "/update-password", "/auth/confirm", "/api/:path*"
- Update redirect for authenticated users on public routes → "/dashboard" instead of "/"
- Update redirect for unauthenticated users → "/login?redirect=/dashboard" for new routes
</action>

<acceptance_criteria>
- Middleware matcher includes new routes, excludes old ones
- Authenticated user on /login redirects to /dashboard
- Unauthenticated user on /dashboard redirects to /login with redirect=/dashboard
- Build compiles
</acceptance_criteria>

### 3.8 Remover AuthHeader

<read_first>
- src/components/auth/auth-header.tsx
- design.md (D12)
</read_first>

<action>
Delete `src/components/auth/auth-header.tsx`.
Remove any remaining imports of AuthHeader in the codebase.
</action>

<acceptance_criteria>
- `src/components/auth/auth-header.tsx` no longer exists
- No imports of auth-header in any file
- Build compiles without errors
</acceptance_criteria>

### 3.9 Atualizar LogoutButton tokens

<read_first>
- src/components/auth/logout-button.tsx
- design.md (D11)
</read_first>

<action>
Update `src/components/auth/logout-button.tsx`:
- Replace slate-*/blue-* hardcoded colors with design tokens (text-text-*/accent-*/bg-*)
- Accept optional `variant` or `className` prop for flexible use in shell and /conta
</action>

<acceptance_criteria>
- LogoutButton uses design tokens instead of slate-*/blue-*
- Accepts className prop for customization
- Build compiles
</acceptance_criteria>

### 3.10 Atualizar campaignUrl na rota de geração

<read_first>
- src/app/api/campaign/generate-image/route.ts
- src/components/flow/use-campaign-form.ts
- src/__tests__/api/campaign-generate.test.ts
- src/__tests__/components/flow/__tests__/use-campaign-form-navigation.test.ts
</read_first>

<action>
Update campaignUrl path:
- In `src/app/api/campaign/generate-image/route.ts` (line ~353): change campaignUrl to `/campanhas/${campaignId}`
- In `src/components/flow/use-campaign-form.ts`: router.push uses the corrected campaignUrl value from route handler
- Update test files to expect new path format
</action>

<acceptance_criteria>
- generate-image route returns campaignUrl starting with /campanhas/
- use-campaign-form navigation works with new path
- Tests updated and passing
- Build compiles
</acceptance_criteria>

### 3.11 Atualizar links internos em componentes de domínio

<read_first>
- All files in src/components/flow/
- All files in src/app/auth/
</read_first>

<action>
Search and update internal links:
- `src/components/flow/*` — update any hardcoded links to old routes
- `src/app/auth/*` — update redirects to /dashboard where applicable
</action>

<acceptance_criteria>
- Internal navigation in campaign flow no longer uses / or other old routes as entry point
- Build compiles
</acceptance_criteria>

### 3.12 Migrar testes existentes com imports de caminhos antigos

<read_first>
- All test files in src/__tests__/ that reference old page paths
</read_first>

<action>
Find and update test imports:
- Update import paths from old routes to new (app)/ routes
- Update any mocked paths or route constants
- Ensure all tests pass with new structure
</action>

<acceptance_criteria>
- No test files import from old page paths
- All tests pass
</acceptance_criteria>

### 3.13 Criar testes de redirects

<read_first>
- next.config.ts (redirects from 18-01)
- specs/route-redirects/spec.md
</read_first>

<action>
Create `src/__tests__/next.config.test.ts`:
- Test each of the 5 redirects returns 301 and correct destination
- Use next.config.js testing utilities or mock-based approach
</action>

<acceptance_criteria>
- Test file exists with 5 redirect scenarios
- npx vitest run passes redirect tests
</acceptance_criteria>

### 3.14 Criar testes de middleware

<read_first>
- src/middleware.ts (updated in this plan)
- specs/auth-middleware/spec.md
</read_first>

<action>
Create/update `src/__tests__/middleware.test.ts`:
- Test new protected routes match matcher (dashboard, campanhas/nova, loja, conta)
- Test old routes no longer match (store/, campanha/)
- Test authenticated user on /login redirects to /dashboard
- Test unauthenticated user on /dashboard redirects to /login
</action>

<acceptance_criteria>
- Test file covers 3+ middleware scenarios
- npx vitest run passes middleware tests
</acceptance_criteria>

### 3.15 Remover arquivos de páginas antigas

<read_first>
- Inventory of old page directories (confirmed content migrated)
</read_first>

<action>
Remove old page directories:
- `src/app/minhas-campanhas/` (recursive)
- `src/app/campanha/[id]/` (recursive)
- `src/app/store/` (recursive)
- `src/app/campaign/preview/` (recursive)
</action>

<acceptance_criteria>
- No old page directories exist
- Build compiles (no dangling imports)
- All tests pass
</acceptance_criteria>

### 3.16 Verificar TypeScript e lint

<read_first>
- All files modified in this plan
</read_first>

<action>
Run `npm run typecheck` and `npm run lint`. Fix any errors.
</action>

<acceptance_criteria>
- `npm run typecheck` exits with code 0
- `npm run lint` exits with code 0
</acceptance_criteria>

---

## Verification

- [ ] /campanhas/nova com form de campanha e design tokens
- [ ] /campanhas com listagem e links atualizados
- [ ] /campanhas/[id] com Lucide icons, tokens, breadcrumbs
- [ ] /loja com identity form e tokens
- [ ] /dashboard com PageHeader + EmptyState
- [ ] /conta com email, update-password, logout
- [ ] Middleware atualizado com novo matcher
- [ ] AuthHeader removido
- [ ] LogoutButton com tokens
- [ ] campaignUrl atualizado para /campanhas/:id
- [ ] Links internos migrados
- [ ] Rotas antigas removidas
- [ ] 5 testes de redirect passando
- [ ] 3+ testes de middleware passando
- [ ] typecheck e lint limpos

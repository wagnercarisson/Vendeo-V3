---
id: 19-03
wave: 3
title: Campanhas + Detalhe sem Loja
depends_on:
  - 19-02
files_modified:
  - src/app/(app)/campanhas/page.tsx
  - src/app/(app)/campanhas/[id]/page.tsx
  - src/app/(app)/campanhas/client.tsx
  - src/__tests__/app/campanhas/campanhas-page.test.tsx
  - src/__tests__/app/campanhas/campaign-detail-page.test.tsx
autonomous: true
requirements:
  - ONBRD-01
  - ONBRD-03
  - UX-01
  - UX-02
---

# 19-03: Campanhas + Detalhe sem Loja

Substituir redirects secos por orientação visual contextual em `/campanhas` e `/campanhas/[id]`, e referenciar microcopy centralizada no estado vazio existente.

## Tasks

### 3.1 Modificar /campanhas para empty state em vez de redirect

<read_first>
- openspec/changes/fase-19-onboarding-estados-vazios/design.md (D4)
- openspec/changes/fase-19-onboarding-estados-vazios/specs/campaign-list-ui/spec.md
- src/app/(app)/campanhas/page.tsx (current implementation)
- src/components/ui/empty-state.tsx
</read_first>

<action>
Modify `src/app/(app)/campanhas/page.tsx`:
- Keep `requirePageUser()` and `getCurrentStore()` imports
- Add import for `CAMPAIGNS_NO_STORE` from `@/lib/onboarding/microcopy`
- Add import for `EmptyState` from `@/components/ui/empty-state`
- Add import for `Link` from `next/link`
- Remove `redirect` import from `next/navigation` (no longer needed here)
- Replace `if (!store) { redirect("/loja"); }` with:
  ```tsx
  if (!store) {
    return (
      <div>
        <EmptyState
          icon={CAMPAIGNS_NO_STORE.icon}
          title={CAMPAIGNS_NO_STORE.title}
          description={CAMPAIGNS_NO_STORE.description}
          action={<Link href={CAMPAIGNS_NO_STORE.ctaHref!} className="inline-flex items-center rounded-lg bg-accent-green px-6 py-2 text-sm font-semibold text-white font-heading hover:brightness-110 transition-all duration-200">{CAMPAIGNS_NO_STORE.ctaLabel}</Link>}
        />
      </div>
    );
  }
  ```
- Ensure `listCampaigns` is NOT called when store is null (early return antes)
</action>

<acceptance_criteria>
- `src/app/(app)/campanhas/page.tsx` no longer imports `redirect`
- Renders `<EmptyState>` with `CAMPAIGNS_NO_STORE` when store is null
- CTA button "Configurar loja" links to `/loja`
- `listCampaigns` is not called when `!store`
- TypeScript compiles without errors
</acceptance_criteria>

### 3.2 Modificar /campanhas/[id] para notFound em vez de redirect

<read_first>
- openspec/changes/fase-19-onboarding-estados-vazios/design.md (D6)
- openspec/changes/fase-19-onboarding-estados-vazios/specs/campaign-page-ui/spec.md
- src/app/(app)/campanhas/[id]/page.tsx (current implementation)
</read_first>

<action>
Modify `src/app/(app)/campanhas/[id]/page.tsx`:
- Replace `if (!store) { redirect("/loja"); }` with `if (!store) { notFound(); }`
- Remove `redirect` from the import from `next/navigation` (keep only `notFound`)
- Keep all other behavior intact
</action>

<acceptance_criteria>
- `src/app/(app)/campanhas/[id]/page.tsx` no longer imports `redirect`
- Calls `notFound()` when store is null (not `redirect("/loja")`)
- Still calls `notFound()` when campaign is not found
- TypeScript compiles without errors
</acceptance_criteria>

### 3.3 Verificar /campanhas/nova — redirect mantido

<read_first>
- openspec/changes/fase-19-onboarding-estados-vazios/design.md (D5)
- src/app/(app)/campanhas/nova/page.tsx
</read_first>

<action>
Verify that `src/app/(app)/campanhas/nova/page.tsx` still has:
- `redirect("/loja")` when store is null
- No modifications needed — just confirm the existing behavior is intact
</action>

<acceptance_criteria>
- File exists with `redirect("/loja")` when `!store`
- No changes made to this file
</acceptance_criteria>

### 3.4 Revisar client.tsx — referenciar microcopy centralizada

<read_first>
- openspec/changes/fase-19-onboarding-estados-vazios/design.md (D7)
- openspec/changes/fase-19-onboarding-estados-vazios/specs/campaign-list-ui/spec.md
- src/app/(app)/campanhas/client.tsx
</read_first>

<action>
Modify `src/app/(app)/campanhas/client.tsx`:
- Import `CAMPAIGNS_NO_CAMPAIGNS` from `@/lib/onboarding/microcopy`
- Replace inline strings for empty state title/description with `CAMPAIGNS_NO_CAMPAIGNS.title` and `CAMPAIGNS_NO_CAMPAIGNS.description`
- Replace inline CTA text with `CAMPAIGNS_NO_CAMPAIGNS.ctaLabel`
- Replace inline CTA href with `CAMPAIGNS_NO_CAMPAIGNS.ctaHref`
- Keep the `Megaphone` icon import for the icon prop (or use `CAMPAIGNS_NO_CAMPAIGNS.icon` directly)
</action>

<acceptance_criteria>
- `src/app/(app)/campanhas/client.tsx` imports `CAMPAIGNS_NO_CAMPAIGNS` from microcopy
- Empty state title/description/CTA come from microcopy constants
- No hardcoded text for the empty state
- TypeScript compiles without errors
</acceptance_criteria>

### 3.5 Criar testes de campanhas

<read_first>
- openspec/changes/fase-19-onboarding-estados-vazios/tasks.md (section 3.5)
- src/__tests__/app/minhas-campanhas.test.tsx (existing test patterns)
</read_first>

<action>
Create `src/__tests__/app/campanhas/campanhas-page.test.tsx` with `// @vitest-environment node`:

- Mock `requirePageUser` to return `{ userId: "user-123", claims: { sub: "user-123" } }`
- Mock `getCurrentStore` from `@/lib/auth/store-ownership`
- Mock `listCampaigns` from `@/lib/campaign/list`
- Mock `Link` from `next/link` with simple `a` tag
- Test cases:
  1. Sem loja → renders empty state "Configure sua loja" with CTA `/loja` (NÃO redireciona)
  2. Loja sem campanhas → renders empty state "Nenhuma campanha ainda" via client.tsx
  3. Loja com campanhas → renders lista (campaigns passados para client)
  4. `countCampaigns` não é chamado quando `no_store` (performance)

Create `src/__tests__/app/campanhas/campaign-detail-page.test.tsx` with `// @vitest-environment node`:

- Mock `requirePageUser`, `getCurrentStore`, `getCampaignForDisplay`
- Test cases:
  1. Sem loja em `/campanhas/[id]` → `notFound()` chamado (NÃO redireciona para `/loja`)
  2. Sem loja em `/campanhas/nova` → `redirect("/loja")` é chamado (mantido)
  3. Loja existe e campanha encontrada → renders client component
</action>

<acceptance_criteria>
- `src/__tests__/app/campanhas/campanhas-page.test.tsx` exists — 4 test cases pass
- `src/__tests__/app/campanhas/campaign-detail-page.test.tsx` exists — 3 test cases pass
- `npx vitest run src/__tests__/app/campanhas/` passes all tests
</acceptance_criteria>

### 3.6 Verificar TypeScript e lint

<read_first>
- All files modified in this plan
</read_first>

<action>
Run `npm run typecheck` and `npm run lint`. Fix any errors.
</action>

<acceptance_criteria>
- `npm run typecheck` exits with code 0
- `npm run lint` exits with code 0
</acceptance_criteria>

---

## Verification

- [ ] `/campanhas` sem loja mostra empty state e não redireciona
- [ ] `/campanhas/[id]` sem loja retorna 404 (não redireciona)
- [ ] `/campanhas/nova` sem loja redireciona para `/loja` (mantido)
- [ ] client.tsx referência microcopy centralizada
- [ ] 7 testes passando
- [ ] typecheck e lint limpos

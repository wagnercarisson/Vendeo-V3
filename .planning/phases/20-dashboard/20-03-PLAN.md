---
id: 20-03
wave: 2
title: Testes e Acabamento Responsivo
depends_on:
  - 20-02
files_modified:
  - src/__tests__/app/dashboard/dashboard-page.test.tsx
autonomous: true
requirements:
  - DASH-11
  - DASH-12
  - DASH-13
---

# 20-03: Testes e Acabamento Responsivo

Criar testes de renderização do dashboard abrangendo os 3 estados, saudação com mock de Date, métricas visíveis, campanhas recentes, card de próximo passo, e responsividade. Validar preservação dos estados F19.

## Tasks

### 3.1 Criar testes de renderização do dashboard

<read_first>
- openspec/changes/fase-20-dashboard/tasks.md (section 3.1)
- openspec/changes/fase-20-dashboard/specs/dashboard-inteligente/spec.md
- src/__tests__/app/minhas-campanhas.test.tsx (mocking patterns for Next.js pages)
</read_first>

<action>
Create `src/__tests__/app/dashboard/dashboard-page.test.tsx` with 9 test cases:

1. **Estado `no_store` → empty state "Configure sua loja"** (F19 preservado)
   - Mock `requirePageUser` retornando `{ userId: "user-1" }`
   - Mock `getUserOnboardingState` retornando `"no_store"`
   - Verificar texto "Configure sua loja" no render

2. **Estado `has_store_no_campaigns` → empty state "Crie sua primeira campanha"** (F19 preservado)
   - Mock `getUserOnboardingState` retornando `"has_store_no_campaigns"`
   - Verificar texto "Crie sua primeira campanha" no render

3. **Estado `has_store_with_campaigns` → saudação "Bom dia, Loja Teste"** (mock Date 10h)
   - Mock `getUserOnboardingState` retornando `"has_store_with_campaigns"`
   - Mock `getCurrentStore` retornando `{ id: "store-1", name: "Loja Teste" }`
   - Mock `countCampaigns` → 5, `countReadyCampaigns` → 3, `getRecentCampaigns` → array de 3 itens
   - Usar `vi.setSystemTime(new Date(2026, 6, 13, 10, 0, 0))` para fixar 10h
   - Verificar texto "Bom dia, Loja Teste"

4. **Saudação "Boa tarde, Loja Teste"** (mock Date 14h)
   - `vi.setSystemTime(new Date(2026, 6, 13, 14, 0, 0))`
   - Verificar texto "Boa tarde, Loja Teste"

5. **Saudação "Boa noite, Loja Teste"** (mock Date 21h)
   - `vi.setSystemTime(new Date(2026, 6, 13, 21, 0, 0))`
   - Verificar texto "Boa noite, Loja Teste"

6. **3 cards de métricas visíveis**
   - Verificar textos "Total de Campanhas", "Campanhas Prontas", "Taxa de Sucesso"
   - Verificar valores (5, 3, "60%")

7. **Lista de campanhas recentes com link "Ver todas"**
   - Verificar itens com nome do produto e link "Abrir"
   - Verificar link "Ver todas as campanhas →" com href="/campanhas"

8. **Card de próximo passo com CTA para última campanha**
   - Verificar texto "Revise sua última campanha"
   - Verificar link "Abrir campanha" com href contendo ID da campanha mais recente

9. **Placeholder "Seu dashboard está sendo preparado" não aparece**
   - Verificar que o texto do placeholder NÃO está presente no DOM
</action>

<acceptance_criteria>
- `src/__tests__/app/dashboard/dashboard-page.test.tsx` exists with 9 test cases
- `npx vitest run src/__tests__/app/dashboard/dashboard-page.test.tsx` passes all 9
- TypeScript compiles without errors
</acceptance_criteria>

### 3.2 Testes de responsividade

<read_first>
- openspec/changes/fase-20-dashboard/design.md (D7 — grid layout)
</read_first>

<action>
Add to the same test file:

10. **Grid de métricas tem classes `grid-cols-1 md:grid-cols-3`**
    - Verificar que o container de métricas contém `grid-cols-1` e `md:grid-cols-3`

11. **Cards têm `gap-4` consistente**
    - Verificar que o container de métricas contém `gap-4`
</action>

<acceptance_criteria>
- Grid has `grid-cols-1 md:grid-cols-3 gap-4` classes
- Test passes
</acceptance_criteria>

### 3.3 Testes de edge cases

<read_first>
- openspec/changes/fase-20-dashboard/design.md (D6 — next-step fallback)
</read_first>

<action>
Add to the same test file:

12. **Lista de recentes vazia não quebra o dashboard**
    - Mock `getRecentCampaigns` retornando `[]`
    - Dashboard renderiza sem erro

13. **Card de próximo passo com fallback "Criar nova campanha" quando lista vazia**
    - Mock `getRecentCampaigns` retornando `[]`
    - Verificar texto "Criar nova campanha"

14. **Taxa de sucesso 0% via helper**
    - Teste unitário em metrics.test.ts já cobre (Plano 20-01)
    - Verificar visualização "0%" no card de métrica quando countCampaigns retorna 0

15. **Taxa de sucesso 100% via helper**
    - Mock `countCampaigns` → 3, `countReadyCampaigns` → 3
    - Verificar "100%" no card de taxa de sucesso
</action>

<acceptance_criteria>
- Empty recent list renders without crashing
- Next-step card shows "Criar nova campanha" fallback
- Taxa de sucesso displays "0%" and "100%" correctly
- All edge case tests pass
</acceptance_criteria>

### 3.4 Verificar TypeScript e lint

<read_first>
- All test files created in this plan
</read_first>

<action>
Run `npm run typecheck` and `npm run lint`. Fix any errors.
</action>

<acceptance_criteria>
- `npm run typecheck` exits with code 0
- `npm run lint` exits with code 0
</acceptance_criteria>

---

## Verification

- [ ] 15 testes de renderização passando
- [ ] 3 estados do dashboard testados
- [ ] Saudação testada com mock de Date (3 períodos)
- [ ] Responsividade testada (grid classes)
- [ ] Edge cases cobertos (lista vazia, taxa 0% e 100%)
- [ ] Placeholder F19 não aparece no dashboard real
- [ ] typecheck e lint limpos

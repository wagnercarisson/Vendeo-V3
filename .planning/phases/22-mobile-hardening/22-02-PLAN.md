---
id: 22-02
wave: 2
title: Touch Targets & Componentes
depends_on:
  - 22-01
files_modified:
  - src/components/ui/input.tsx
  - src/components/ui/pagination.tsx
  - src/app/(app)/campanhas/client.tsx
  - src/app/(app)/campanhas/[id]/client.tsx
  - src/components/flow/campaign-input-form.tsx
  - src/app/(app)/dashboard/page.tsx
  - src/app/(app)/conta/page.tsx
  - src/components/auth/logout-button.tsx
  - src/components/flow/store-identity-form.tsx
  - src/__tests__/components/ui/input.test.tsx
  - src/__tests__/components/ui/pagination.test.tsx
  - src/__tests__/app/campanhas/campanhas-page.test.tsx
  - src/__tests__/app/dashboard/dashboard-page.test.tsx
autonomous: true
requirements:
  - MOBILE-01
  - MOBILE-03
---

# 22-02: Touch Targets & Componentes

Elevar touch targets para ≥44px em todos os elementos interativos do app. Inclui componente `Input`, campanhas list/detail, dashboard, formulários, conta, logout e loja (patches pontuais). Pagination ganha `flex-wrap` para mobile.

## Tasks

### 2.1 Adicionar `min-h-[44px]` ao componente Input

<read_first>
- openspec/changes/fase-22-mobile-hardening/specs/ui-base-components/spec.md (Input with min-h-[44px])
- openspec/changes/fase-22-mobile-hardening/design.md (D3)
- src/components/ui/input.tsx (current implementation)
</read_first>

<action>
Em `src/components/ui/input.tsx`, no className do `<input>`, adicionar `min-h-[44px]`.

Atual: `className="..."` (sem min-height)
Novo: adicionar `min-h-[44px]` à string de classes existente.
</action>

<acceptance_criteria>
- Componente `Input` renderiza `<input>` com `min-h-[44px]` no className
- Teste existente de Input valida min-h-[44px]
- TypeScript compila sem erros
</acceptance_criteria>

### 2.2 Touch targets campanhas list: Abrir, Baixar, chips, selects

<read_first>
- openspec/changes/fase-22-mobile-hardening/specs/touch-targets-hardening/spec.md (Campanhas list touch targets)
- src/app/(app)/campanhas/client.tsx (current implementation)
</read_first>

<action>
Em `src/app/(app)/campanhas/client.tsx`:
- Botão "Abrir" em cada card: adicionar `min-h-[44px]`
- Botão "Baixar" em cada card: adicionar `min-h-[44px]`
- Status chips (Todas/Prontas/Erro): adicionar `min-h-[44px]`
- Select filters: adicionar `min-h-[44px]`
</action>

<acceptance_criteria>
- "Abrir" button tem `min-h-[44px]`
- "Baixar" button tem `min-h-[44px]`
- Cada status chip tem `min-h-[44px]`
- Cada select filter tem `min-h-[44px]`
- Layout visual consistente (44px não quebra design existente)
- TypeScript compila sem erros
</acceptance_criteria>

### 2.3 Touch targets campanha detail: Download 44px + edit flex-wrap

<read_first>
- openspec/changes/fase-22-mobile-hardening/specs/touch-targets-hardening/spec.md (Campanha detail touch targets)
- src/app/(app)/campanhas/[id]/client.tsx (current implementation)
</read_first>

<action>
Em `src/app/(app)/campanhas/[id]/client.tsx`:
- Link de Download: adicionar `min-h-[44px]`
- Container de edit actions (3 botões): adicionar `flex-wrap gap-2` (substituir ou complementar classes existentes)
</action>

<acceptance_criteria>
- Link Download tem `min-h-[44px]`
- Edit actions container tem `flex-wrap gap-2`
- Em viewport 320px, botões de edição não transbordam horizontalmente
- TypeScript compila sem erros
</acceptance_criteria>

### 2.4 Touch targets campaign-input-form: conflitos, submit, inputs crus

<read_first>
- openspec/changes/fase-22-mobile-hardening/specs/touch-targets-hardening/spec.md (Campaign input form touch targets)
- src/components/flow/campaign-input-form.tsx (current implementation)
</read_first>

<action>
Em `src/components/flow/campaign-input-form.tsx`:
- Botões de conflito: adicionar `min-h-[44px]`
- Botão de submit: adicionar `min-h-[44px]`
- Inputs crus (`<input>`, `<select>`, `<textarea>` nativos): adicionar `min-h-[44px]` nas classes próprias de cada um
</action>

<acceptance_criteria>
- Botões de conflito têm `min-h-[44px]`
- Botão submit tem `min-h-[44px]`
- `<input>`, `<select>`, `<textarea>` crus têm `min-h-[44px]`
- TypeScript compila sem erros
</acceptance_criteria>

### 2.5 Touch targets dashboard: Abrir e CTAs

<read_first>
- openspec/changes/fase-22-mobile-hardening/specs/touch-targets-hardening/spec.md (Dashboard touch targets)
- src/app/(app)/dashboard/page.tsx (current implementation)
</read_first>

<action>
Em `src/app/(app)/dashboard/page.tsx`:
- Link "Abrir" em campanhas recentes: adicionar `min-h-[44px]`
- CTA buttons no próximo passo: adicionar `min-h-[44px]`
</action>

<acceptance_criteria>
- Link "Abrir" no dashboard tem `min-h-[44px]`
- CTA buttons no card de próximo passo têm `min-h-[44px]`
- TypeScript compila sem erros
</acceptance_criteria>

### 2.6 Touch target conta: Alterar senha

<read_first>
- openspec/changes/fase-22-mobile-hardening/specs/touch-targets-hardening/spec.md (Conta page touch targets)
- src/app/(app)/conta/page.tsx (current implementation)
</read_first>

<action>
Em `src/app/(app)/conta/page.tsx`:
- Link "Alterar senha": adicionar `min-h-[44px]`
</action>

<acceptance_criteria>
- Link "Alterar senha" tem `min-h-[44px]`
- TypeScript compila sem erros
</acceptance_criteria>

### 2.7 Touch target LogoutButton

<read_first>
- openspec/changes/fase-22-mobile-hardening/specs/touch-targets-hardening/spec.md (LogoutButton touch target)
- src/components/auth/logout-button.tsx (current implementation)
</read_first>

<action>
Em `src/components/auth/logout-button.tsx`:
- Adicionar `min-h-[44px]` ao className do botão
</action>

<acceptance_criteria>
- LogoutButton tem `min-h-[44px]`
- TypeScript compila sem erros
</acceptance_criteria>

### 2.8 Patches pontuais store-identity-form (NÃO refatorar)

<read_first>
- openspec/changes/fase-22-mobile-hardening/specs/touch-targets-hardening/spec.md (Store identity form patches pontuais)
- openspec/changes/fase-22-mobile-hardening/design.md (D4 — prioridade, D1 — não refatorar)
- src/components/flow/store-identity-form.tsx (current implementation — patches ONLY)
</read_first>

<action>
Em `src/components/flow/store-identity-form.tsx`, aplicar patches pontuais (NÃO refatorar estrutura):

1. Submit button: adicionar `min-h-[44px]`
2. "Remover logotipo": adicionar `min-h-[44px]`
3. "Remover assinatura visual": adicionar `min-h-[44px]`
4. "Tentar novamente" links: adicionar `min-h-[44px]`
5. Modal cancel/confirm buttons: adicionar `min-h-[44px]`
6. Back arrow (step 2): adicionar `min-w-[44px]` + `aria-label` descritivo
7. Color chips "P"/"S": adicionar `min-h-[44px]` + `min-w-[44px]`
8. Input fields crus: adicionar `min-h-[44px]` nas classes próprias

Cada patch é uma adição localizada de classe CSS — sem alterar estrutura, layout ou lógica.
</action>

<acceptance_criteria>
- Submit button tem `min-h-[44px]`
- "Remover logotipo" tem `min-h-[44px]`
- "Remover assinatura visual" tem `min-h-[44px]`
- "Tentar novamente" links têm `min-h-[44px]`
- Modal cancel/confirm buttons têm `min-h-[44px]`
- Back arrow tem `min-w-[44px]` e `aria-label`
- Color chips "P"/"S" têm `min-h-[44px]` + `min-w-[44px]`
- Input fields crus têm `min-h-[44px]`
- Nenhuma alteração estrutural no arquivo (apenas adições de classe)
- TypeScript compila sem erros
- Validação visual: layout existente intacto
</acceptance_criteria>

### 2.9 Pagination com flex-wrap para mobile

<read_first>
- openspec/changes/fase-22-mobile-hardening/specs/ui-base-components/spec.md (Pagination flex-wrap)
- src/components/ui/pagination.tsx (current implementation)
</read_first>

<action>
Em `src/components/ui/pagination.tsx`, no container principal do componente:
- Adicionar `flex-wrap` ao className (ou substituir `flex-nowrap` se existir)

Antes: `className="flex items-center gap-1"`
Depois: `className="flex items-center gap-1 flex-wrap"`
</action>

<acceptance_criteria>
- Container do Pagination tem `flex-wrap`
- Em viewport 320px, botões de paginação não transbordam horizontalmente
- Comportamento normal em desktop mantido
- TypeScript compila sem erros
</acceptance_criteria>

### 2.10 Testes de input min-height

<read_first>
- openspec/changes/fase-22-mobile-hardening/specs/mobile-hardening-tests/spec.md (Input min-height 44px test)
- openspec/changes/fase-22-mobile-hardening/tasks.md (2.24)
- src/__tests__/components/ui/input.test.tsx (current tests)
</read_first>

<action>
Expandir `src/__tests__/components/ui/input.test.tsx`: adicionar assert que o input renderizado tem `min-h-[44px]` no className.
</action>

<acceptance_criteria>
- Teste verifica `min-h-[44px]` no className do Input
- Testes existentes continuam passando
</acceptance_criteria>

### 2.11 Testes campanhas page touch targets (4+)

<read_first>
- openspec/changes/fase-22-mobile-hardening/specs/mobile-hardening-tests/spec.md (Touch target tests)
- openspec/changes/fase-22-mobile-hardening/tasks.md (2.25)
- src/__tests__/app/campanhas/campanhas-page.test.tsx (current tests)
</read_first>

<action>
Expandir `src/__tests__/app/campanhas/campanhas-page.test.tsx` com 4+ asserts:
1. "Abrir" button tem `min-h-[44px]`
2. "Baixar" button tem `min-h-[44px]`
3. Status chips têm `min-h-[44px]`
4. Select filters têm `min-h-[44px]`
</action>

<acceptance_criteria>
- Teste verifica Abrir className contém `min-h-[44px]`
- Teste verifica Baixar className contém `min-h-[44px]`
- Teste verifica chips className contém `min-h-[44px]`
- Teste verifica selects className contém `min-h-[44px]`
- Testes existentes continuam passando
</acceptance_criteria>

### 2.12 Testes dashboard touch targets (2+)

<read_first>
- openspec/changes/fase-22-mobile-hardening/specs/mobile-hardening-tests/spec.md (Touch target tests + regression)
- openspec/changes/fase-22-mobile-hardening/tasks.md (2.26)
- src/__tests__/app/dashboard/dashboard-page.test.tsx (current tests)
</read_first>

<action>
Expandir `src/__tests__/app/dashboard/dashboard-page.test.tsx` com:
1. "Abrir" link tem `min-h-[44px]`
2. Grid de métricas intacto: `grid-cols-1 md:grid-cols-3` (regressão F20)
</action>

<acceptance_criteria>
- Teste verifica "Abrir" className contém `min-h-[44px]`
- Teste verifica grid metrics container contém `grid-cols-1 md:grid-cols-3`
- Testes existentes continuam passando
</acceptance_criteria>

### 2.13 Teste pagination flex-wrap

<read_first>
- openspec/changes/fase-22-mobile-hardening/specs/mobile-hardening-tests/spec.md (Pagination flex-wrap test)
- openspec/changes/fase-22-mobile-hardening/tasks.md (2.27)
- src/__tests__/components/ui/pagination.test.tsx (current tests)
</read_first>

<action>
Expandir `src/__tests__/components/ui/pagination.test.tsx`: adicionar assert que o container do Pagination tem `flex-wrap`.
</action>

<acceptance_criteria>
- Teste verifica `flex-wrap` no className do container
- Testes existentes continuam passando
</acceptance_criteria>

### 2.14 Verificar TypeScript e lint

<read_first>
- All files modified in this plan
</read_first>

<action>
Run `npm run typecheck` and `npm run lint`. Fix any errors.
</action>

<acceptance_criteria>
- `npm run typecheck` exits with code 0
- `npm run lint` exits with code 0
</acceptance_criteria>

---

## Verification

- [ ] `Input` component: `min-h-[44px]`
- [ ] Campanhas list: Abrir, Baixar, chips, selects ≥44px
- [ ] Campanha detail: Download ≥44px, edit actions flex-wrap
- [ ] Campaign input form: conflitos, submit, inputs crus ≥44px
- [ ] Dashboard: Abrir, CTAs ≥44px
- [ ] Conta: Alterar senha ≥44px
- [ ] LogoutButton: ≥44px
- [ ] Store identity form: submit, remover, tentar novamente, modal, back arrow, color chips, inputs crus ≥44px
- [ ] Pagination: `flex-wrap` para mobile
- [ ] Testes: Input (1), campanhas page (4+), dashboard (2+), pagination (1) — todos passando
- [ ] Nenhuma refatoração em store-identity-form — apenas patches de classe
- [ ] typecheck e lint limpos

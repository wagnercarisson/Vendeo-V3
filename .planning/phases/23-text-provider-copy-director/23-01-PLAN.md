---
id: 23-01
wave: 1
title: TextProvider Foundation
depends_on: []
files_modified:
  - src/lib/text-provider/types.ts
  - src/lib/text-provider/openai.ts
  - src/lib/text-provider/mock.ts
  - src/lib/text-provider/factory.ts
  - src/lib/text-provider/__tests__/text-provider.test.ts
  - src/lib/campaign/types.ts
autonomous: true
requirements:
  - COPY-01
  - COPY-04
---

# 23-01: TextProvider Foundation

## Objective

Criar a abstração `TextProvider` — interface, implementações OpenAI e Mock, factory e testes — paralela ao `ImageProvider` existente. Evoluir `PublicationCopySnapshot` com campo `title?` opcional.

## Must Haves

- [ ] `TextProvider` interface com `generateText()` e `readonly name`
- [ ] `OpenAITextProvider` usando OpenAI Chat Completions (modelo `gpt-4o` / `OPENAI_TEXT_MODEL`)
- [ ] `MockTextProvider` com dados determinísticos para testes/dev
- [ ] `createTextProvider()` factory lendo `TEXT_PROVIDER` env var (default: `openai`)
- [ ] `PublicationCopySnapshot` ganha `title?: string` sem breaking change
- [ ] 5+ testes unitários para TextProvider (factory, OpenAI mock, Mock provider)
- [ ] `npm run typecheck` — zero erros
- [ ] `npm run lint` — zero erros
- [ ] `npx vitest run` — todos os testes passando (novos + 713 existentes)

## Dependencies

Nenhuma — plano fundacional.

## Verification Criteria

- `src/lib/text-provider/types.ts` define `TextProvider`, `TextProviderOptions`, `TextProviderResult`
- `src/lib/text-provider/openai.ts` implementa `OpenAITextProvider` com chamada OpenAI Chat Completions
- `src/lib/text-provider/mock.ts` implementa `MockTextProvider` com dados determinísticos
- `src/lib/text-provider/factory.ts` implementa `createTextProvider()` com switch por env var
- `src/lib/campaign/types.ts` — `PublicationCopySnapshot` inclui `title?: string`
- `npm run typecheck` passa sem erros
- `npm run lint` passa sem erros
- `npx vitest run` — 5+ novos testes + todos os existentes passando

---

## Tasks

### Task 1: Criar TextProvider types

**File:** `src/lib/text-provider/types.ts`

<read_first>
- `src/lib/image-generation/providers/types.ts` — ImageProvider interface pattern
</read_first>

<action>
Criar `src/lib/text-provider/types.ts` com:

1. `TextProviderOptions` interface:
   - `system?: string`
   - `temperature?: number`
   - `maxTokens?: number`
   - `signal?: AbortSignal`

2. `TextProviderResult` interface:
   - `content: string`
   - `usage: { promptTokens: number; completionTokens: number }`
   - `model: string`

3. `TextProvider` interface:
   - `readonly name: string`
   - `generateText(prompt: string, options?: TextProviderOptions): Promise<TextProviderResult>`
</action>

<acceptance_criteria>
- `TextProvider` interface define `generateText(prompt, options?)` retornando `Promise<TextProviderResult>`
- `TextProviderOptions` contém `system?`, `temperature?`, `maxTokens?`, `signal?`
- `TextProviderResult` contém `content`, `usage.promptTokens`, `usage.completionTokens`, `model`
- `TextProvider` expõe `readonly name: string`
- TypeScript compila sem erros
</acceptance_criteria>

---

### Task 2: Criar OpenAITextProvider

**File:** `src/lib/text-provider/openai.ts`

<read_first>
- `src/lib/text-provider/types.ts` — TextProvider interface
- `src/lib/image-generation/providers/openai.ts` — OpenAIImageProvider implementation pattern
</read_first>

<action>
Criar `src/lib/text-provider/openai.ts` com:

1. Classe `OpenAITextProvider implements TextProvider`:
   - `readonly name = "openai"`
   - Constructor aceita `model?: string` (default: `process.env.OPENAI_TEXT_MODEL` ou `"gpt-4o"`)
   - `generateText(prompt, options?)`:
     - Dynamic import de `openai` SDK (`const { default: OpenAI } = await import("openai")`)
     - Monta `messages` array com `system` de `options.system` (se fornecido) e `user` com `prompt`
     - Chama `openai.chat.completions.create()` com `model`, `messages`, `temperature`, `max_tokens`
     - Passa `options.signal` para a chamada (suporte a AbortSignal)
     - Retorna `TextProviderResult` com `content`, `usage`, `model`
     - Erros propagam sem fallback para MockProvider
</action>

<acceptance_criteria>
- `OpenAITextProvider.name` é `"openai"`
- `generateText(prompt)` chama `openai.chat.completions.create` com modelo `gpt-4o` (default) ou `OPENAI_TEXT_MODEL`
- `generateText(prompt, { system: "X" })` inclui mensagem `system` no array `messages`
- `generateText` retorna `TextProviderResult` com `content`, `usage`, `model`
- Suporta `options.signal` (AbortSignal passado para a chamada OpenAI)
- Erro da API propaga sem fallback
- TypeScript compila sem erros
</acceptance_criteria>

---

### Task 3: Criar MockTextProvider

**File:** `src/lib/text-provider/mock.ts`

<read_first>
- `src/lib/text-provider/types.ts` — TextProvider interface
</read_first>

<action>
Criar `src/lib/text-provider/mock.ts` com:

1. Classe `MockTextProvider implements TextProvider`:
   - `readonly name = "mock"`
   - `generateText(prompt, options?)` retorna dados determinísticos:
     - `content`: `"Título persuasivo gerado pelo Mock. Descrição do produto. Aproveite esta oferta!"`
     - `usage`: `{ promptTokens: 0, completionTokens: 0 }`
     - `model`: `"mock-model-v1"`
   - Sem dependências externas (não chama HTTP, nãoimporta SDK OpenAI)
</action>

<acceptance_criteria>
- `MockTextProvider.name` é `"mock"`
- `generateText(any)` retorna sempre o mesmo `content` determinístico
- `generateText(any)` retorna `usage.promptTokens === 0` e `usage.completionTokens === 0`
- `generateText(any)` retorna `model === "mock-model-v1"`
- Nenhuma chamada HTTP ou import de SDK OpenAI
- TypeScript compila sem erros
</acceptance_criteria>

---

### Task 4: Criar createTextProvider factory

**File:** `src/lib/text-provider/factory.ts`

<read_first>
- `src/lib/text-provider/types.ts` — TextProvider interface
- `src/lib/text-provider/openai.ts` — OpenAITextProvider
- `src/lib/text-provider/mock.ts` — MockTextProvider
- `src/lib/image-generation/providers/factory.ts` — createImageProvider pattern
</read_first>

<action>
Criar `src/lib/text-provider/factory.ts` com:

1. Função `createTextProvider(provider?: string): TextProvider`:
   - Lê `provider` ou `process.env.TEXT_PROVIDER` (default `"openai"`)
   - `"openai"` → retorna `new OpenAITextProvider()`
   - `"mock"` → retorna `new MockTextProvider()`
   - Valor desconhecido → warn no console e fallback para `new OpenAITextProvider()`
</action>

<acceptance_criteria>
- `createTextProvider()` sem args retorna `OpenAITextProvider` (TEXT_PROVIDER não definido)
- `createTextProvider('openai')` retorna `OpenAITextProvider`
- `createTextProvider('mock')` retorna `MockTextProvider`
- Provider inválido loga warning e retorna `OpenAITextProvider`
- TypeScript compila sem erros
</acceptance_criteria>

---

### Task 5: Evoluir PublicationCopySnapshot

**File:** `src/lib/campaign/types.ts`

<read_first>
- `src/lib/campaign/types.ts` — PublicationCopySnapshot atual
- `openspec/changes/fase-23-text-provider-copy-director/specs/campaign-types/spec.md` — spec da evolução
</read_first>

<action>
Adicionar `title?: string` ao `PublicationCopySnapshot`:

```typescript
export interface PublicationCopySnapshot {
  title?: string;
  caption: string;
  hashtags: string[];
  cta_post: string;
}
```

Manter todos os campos existentes (`caption`, `hashtags`, `cta_post`). `title` é opcional — campanhas existentes (pré-F23) continuam válidas sem o campo. Nenhuma migração de banco necessária (`publication_copy_snapshot` é JSONB).
</action>

<acceptance_criteria>
- `PublicationCopySnapshot.title` é `string | undefined`
- `PublicationCopySnapshot` mantém `caption: string`, `hashtags: string[]`, `cta_post: string`
- Snapshots pré-F23 (sem `title`) não causam erro de tipo
- TypeScript compila sem erros
</acceptance_criteria>

---

### Task 6: Testes — TextProvider

**File:** `src/lib/text-provider/__tests__/text-provider.test.ts`

<read_first>
- `src/lib/text-provider/factory.ts` — createTextProvider
- `src/lib/text-provider/openai.ts` — OpenAITextProvider
- `src/lib/text-provider/mock.ts` — MockTextProvider
- `src/lib/image-generation/providers/__tests__/openai-provider.test.ts` — Test pattern
</read_first>

<action>
Criar `src/lib/text-provider/__tests__/text-provider.test.ts` com 5+ testes:

1. `createTextProvider()` sem env retorna `OpenAITextProvider`
2. `createTextProvider('openai')` retorna `OpenAITextProvider`
3. `createTextProvider('mock')` retorna `MockTextProvider`
4. `OpenAITextProvider.generateText` chama OpenAI com prompt correto (usar `vi.mock('openai')`)
5. `MockTextProvider.generateText` retorna dados determinísticos

Usar `vi.mock('openai')` para mockar o SDK nos testes do OpenAITextProvider.
</action>

<acceptance_criteria>
- `npx vitest run src/lib/text-provider/__tests__/text-provider.test.ts` — 5+ testes passando
- Testes com OpenAITextProvider usam `vi.mock` sem chamadas HTTP reais
- MockTextProvider testa dados determinísticos
</acceptance_criteria>

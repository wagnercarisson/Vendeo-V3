---
id: 24-02
wave: 2
title: Tests + SQL Verification
depends_on:
  - 24-01
files_modified:
  - src/lib/credit/__tests__/credit-service.test.ts
autonomous: true
requirements:
  - CRED-03
  - CRED-04
---

# 24-02: Tests + SQL Verification

## Objective

Implementar 25+ testes unitários para o `CreditService` (saldo, grant, reserva, estorno, histórico, concorrência, invariantes) e executar verificação SQL/integrada (I1–I7) contra banco real para validar atomicidade das SQL functions. Garantir que nenhum arquivo existente foi modificado.

## Must Haves

- [ ] `credit-service.test.ts` com setup de mock do supabaseAdmin
- [ ] 6 testes de Saldo e Grant
- [ ] 7 testes de Reserva e Dedução
- [ ] 5 testes de Estorno
- [ ] 4 testes de Histórico
- [ ] 3 testes de Concorrência
- [ ] 3 testes de Invariantes
- [ ] Total: 25+ testes passando
- [ ] Verificação SQL/Integrada I1–I7 contra banco real
- [ ] `npm run typecheck` — zero erros
- [ ] `npm run lint` — zero erros
- [ ] `npx vitest run` — novos + existentes passando
- [ ] `npm run build` — build bem-sucedido
- [ ] Nenhum arquivo existente foi modificado

## Dependencies

24-01 (Migration SQL + CreditService) — testes dependem de `CreditService`, `types.ts`, e SQL functions.

## Verification Criteria

- `npx vitest run src/lib/credit/__tests__/credit-service.test.ts` — 25+ testes passando
- `npm run typecheck` — zero erros
- `npm run lint` — zero erros
- `npx vitest run` — novos + existentes passando
- `npm run build` — build bem-sucedido
- Verificação I1–I7 executada e documentada
- `git status` — nenhum arquivo existente modificado (só novos)

---

## Tasks

### Task 1: Setup de teste — mock do supabaseAdmin

**File:** `src/lib/credit/__tests__/credit-service.test.ts`

<read_first>
- `src/lib/credit/credit-service.ts` — CreditService class
- `src/lib/credit/types.ts` — Types
- `openspec/changes/fase-24-wallet-ledger-idempotencia/tasks.md` — Tasks 4-9 test specs
</read_first>

<action>
Criar `src/lib/credit/__tests__/credit-service.test.ts` com:

1. Mock do módulo `src/lib/supabase/admin` (ou onde `supabaseAdmin` é exportado):
   ```typescript
   import { vi, describe, it, expect, beforeEach } from "vitest";
   import { CreditService } from "../credit-service";

   const mockFrom = vi.fn();
   const mockRpc = vi.fn();
   const mockSingle = vi.fn();
   const mockSelect = vi.fn();
   const mockEq = vi.fn();
   const mockOrder = vi.fn();
   const mockRange = vi.fn();
   const mockNeq = vi.fn();

   const mockAdminClient = {
     from: mockFrom,
     rpc: mockRpc,
   };

   let service: CreditService;

   beforeEach(() => {
     vi.clearAllMocks();
     service = new CreditService(mockAdminClient as any);
   });
   ```

2. Helpers para configurar mocks de forma reutilizável entre testes:
   - `mockGetBalance(balance)` — configura cadeia `.from('credit_balances').select('balance').eq('store_id', id).single()` para retornar `{ data: { balance }, error: null }` ou `{ data: null, error: null }` (sem registro)
   - `mockRpcSuccess(method, result)` — configura `mockRpc` para retornar `{ data: result, error: null }` quando chamado com `method`
   - `mockRpcError(method, message)` — configura `mockRpc` para retornar `{ data: null, error: { message } }`
</action>

<acceptance_criteria>
- Mock de `supabaseAdmin` configurado com `from()` e `rpc()` spies
- `beforeEach` limpa todos os mocks e cria nova instância de `CreditService`
- Helpers de mock estão funcionais para os testes seguintes
- Teste de setup passa sem erros
</acceptance_criteria>

---

### Task 2: Testes — Saldo e Grant

**File:** `src/lib/credit/__tests__/credit-service.test.ts`

<read_first>
- `src/lib/credit/credit-service.ts` — getBalance, grantCredits
- `src/lib/credit/types.ts` — CreditOperationOptions
</read_first>

<action>
Adicionar 6 testes no describe "Saldo e Grant":

1. `getBalance retorna 0 para loja sem registro` — `mockGetBalance(null)`, `await service.getBalance(storeId)` → 0
2. `grantCredits adiciona saldo` — grant de 5, `mockRpcSuccess('grant_credits', 'tx-1')`, `mockGetBalance(5)`, saldo → 5
3. `grants acumulam` — 3 grants de 5, `mockGetBalance(15)`, saldo → 15
4. `grantCredits com idempotency_key repetido retorna mesma tx` — primeira chamada retorna 'tx-1', segunda com mesma key retorna 'tx-1'
5. `getBalance reflete grant após chamada` — grant de 10, getBalance → 10
6. `grantCredits com reason null funciona` — `mockRpcSuccess('grant_credits', 'tx-1')` com reason null
</action>

<acceptance_criteria>
- Teste 1: getBalance → 0 para store sem registro
- Teste 2: grant de 5 → chama RPC com p_amount=5, getBalance → 5
- Teste 3: 3 grants de 5 → saldo final 15
- Teste 4: mesma idempotency_key → mesma tx retornada
- Teste 5: getBalance após grant → saldo correto
- Teste 6: grant com reason null → chama RPC sem erro
- `npx vitest run` — 6 testes passando
</acceptance_criteria>

---

### Task 3: Testes — Reserva e Dedução

**File:** `src/lib/credit/__tests__/credit-service.test.ts`

<read_first>
- `src/lib/credit/credit-service.ts` — reserveCredit
- `src/lib/credit/types.ts` — CreditOperationOptions
</read_first>

<action>
Adicionar 7 testes no describe "Reserva e Dedução":

1. `reserveCredit deduz saldo` — 10 créditos - 1 = 9, `mockRpcSuccess('reserve_credit', 'tx-1')`, `mockGetBalance(9)` → saldo 9
2. `reserveCredit com saldo insuficiente rejeita` — `mockRpcError('reserve_credit', 'saldo_insuficiente')` → erro propagado
3. `reserveCredit com campaignId registra` — chama RPC com `p_campaign_id` preenchido
4. `reserveCredit com idempotency_key repetido retorna mesma tx` — mesma key → mesmo UUID
5. `reservas consecutivas mantêm saldo` — 2 reservas de 1, saldo de 10 → 8
6. `getBalance reflete deduções` — após reservas, getBalance → saldo correto
7. `reserveCredit amount > saldo → erro` — `mockRpcError('reserve_credit', 'saldo_insuficiente')`
</action>

<acceptance_criteria>
- Teste 1: reserve de 1 em saldo 10 → chama RPC com p_amount=1
- Teste 2: saldo insuficiente → erro propagado (mensagem contém 'saldo_insuficiente')
- Teste 3: campaignId passado como p_campaign_id na RPC
- Teste 4: idempotency → mesma tx na segunda chamada
- Teste 5: 2 reservas consecutivas → saldo correto após cada
- Teste 6: getBalance pós-deduções → saldo correto
- Teste 7: amount > saldo → erro
- `npx vitest run` — 7 testes passando
</acceptance_criteria>

---

### Task 4: Testes — Estorno

**File:** `src/lib/credit/__tests__/credit-service.test.ts`

<read_first>
- `src/lib/credit/credit-service.ts` — refundCredit
</read_first>

<action>
Adicionar 5 testes no describe "Estorno":

1. `refundCredit restaura saldo` — dedução de 5, estorno → saldo restaurado, chama RPC com p_tx_id e p_reason
2. `refundCredit deduction inexistente → erro` — `mockRpcError('refund_credit', 'transacao_nao_encontrada')`
3. `refundCredit duplicado é no-op` — mesma tx_id → mesmo refund retornado
4. `refundCredit com idempotency_key repetido` — mesma key → mesmo UUID
5. `refundCredit em transação não-deduction → erro` — `mockRpcError('refund_credit', 'tipo_invalido')`
</action>

<acceptance_criteria>
- Teste 1: refund → chama RPC refund_credit, saldo restaurado
- Teste 2: tx inexistente → erro 'transacao_nao_encontrada'
- Teste 3: refund duplicado → mesma UUID (no-op)
- Teste 4: idempotency → mesma UUID
- Teste 5: tipo inválido → erro 'tipo_invalido'
- `npx vitest run` — 5 testes passando
</acceptance_criteria>

---

### Task 5: Testes — Histórico

**File:** `src/lib/credit/__tests__/credit-service.test.ts`

<read_first>
- `src/lib/credit/credit-service.ts` — getHistory
- `src/lib/credit/types.ts` — CreditTransaction
</read_first>

<action>
Adicionar 4 testes no describe "Histórico":

1. `getHistory retorna transações filtrando adjustment` — verifica que `.neq('type', 'adjustment')` é chamado, e `order('created_at', { ascending: false })` é chamado
2. `getHistory paginado com limit/offset` — 5 transações simuladas, limit=2, offset=1 → retorna 2 itens
3. `getHistory loja sem transações → array vazio` — `mockFrom` retorna `{ data: [], error: null }`
4. `getHistory default limit = 50` — chama `.range(0, 49)`
</action>

<acceptance_criteria>
- Teste 1: query filtra `type != 'adjustment'`, ordena `created_at DESC`
- Teste 2: limit=2, offset=1 → range(1, 2), retorna 2 itens
- Teste 3: loja sem transações → array vazio
- Teste 4: sem limit → range(0, 49)
- Teste de mapeamento: snake_case → camelCase nos campos retornados
- `npx vitest run` — 4 testes passando
</acceptance_criteria>

---

### Task 6: Testes — Concorrência

**File:** `src/lib/credit/__tests__/credit-service.test.ts`

<read_first>
- `src/lib/credit/credit-service.ts` — reserveCredit
</read_first>

<action>
Adicionar 3 testes no describe "Concorrência":

1. `duas reservas simultâneas com saldo justo` — saldo=2, 2 reserves de 1 → ambas resolvem (simular com `Promise.all`)
2. `duas reservas simultâneas com saldo insuficiente` — saldo=1, 2 reserves de 1 → uma resolve, uma rejeita
3. `grant + reserve simultâneos não corrompem saldo` — grant 5 + reserve 3 simultâneos → estado consistente
</action>

<acceptance_criteria>
- Teste 1: saldo=2, 2× reserve de 1 → ambas resolvem
- Teste 2: saldo=1, 2× reserve de 1 → uma resolve, uma rejeita 'saldo_insuficiente'
- Teste 3: grant + reserve paralelos → estado sem corrupção
- `npx vitest run` — 3 testes passando
</acceptance_criteria>

---

### Task 7: Testes — Invariantes

**File:** `src/lib/credit/__tests__/credit-service.test.ts`

<read_first>
- `src/lib/credit/credit-service.ts` — all methods
- `openspec/changes/fase-24-wallet-ledger-idempotencia/design.md` — Invariantes financeiros
</read_first>

<action>
Adicionar 3 testes no describe "Invariantes":

1. `saldo nunca negativo` — grant 5, reserve 3, reserve 2, reserve 1 → erro no último (simular saldo_insuficiente)
2. `transações são imutáveis` — verificar que não há métodos de UPDATE/DELETE expostos no CreditService, e que o trigger no banco bloqueia mutações. Teste unitário verifica que `credit-service.ts` não expõe update/delete. Teste SQL verifica trigger (Ix)
3. `adjustment não aparece no extrato` — getHistory filtra type != 'adjustment' (verificar neq chamado)
</action>

<acceptance_criteria>
- Teste 1: sequência grant→reserve→reserve→reserve → último saldo insuficiente
- Teste 2: CreditService não expõe update/delete de transações
- Teste 3: getHistory usa `.neq('type', 'adjustment')`
- `npx vitest run` — 3 testes passando
</acceptance_criteria>

---

### Task 8: Verificação SQL/Integrada I1–I7

**Action:** Executar verificação SQL/integrada contra banco real (Supabase local)

<read_first>
- `openspec/changes/fase-24-wallet-ledger-idempotencia/design.md` — I1-I7 spec
</read_first>

<action>
Após migration aplicada, executar verificações:

1. **I1**: Conectar ao Supabase local, executar `SELECT public.grant_credits('store-id', 10, 'onboarding')` → verificar saldo > 0
2. **I2**: Executar `SELECT public.reserve_credit('store-id', 3)` → verificar saldo deduzido
3. **I3**: Executar `SELECT public.refund_credit(tx_id, 'teste')` → verificar saldo restaurado
4. **I4**: `reserve_credit` com amount > saldo → verificar exceção `saldo_insuficiente`
5. **I5**: `refund_credit` duplicado → verificar no-op (saldo não dobra)
6. **I6**: Mesma `idempotency_key` em duas chamadas `grant_credits` → mesma tx retornada
7. **I7**: Duas chamadas simultâneas `reserve_credit` com saldo justo (saldo=2, 2× reserve de 1) → ambas OK

Documentar resultados no arquivo de verificação.
</action>

<acceptance_criteria>
- I1: grant_credits → saldo > 0 ✓
- I2: reserve_credit → saldo deduzido ✓
- I3: refund_credit → saldo restaurado ✓
- I4: reserve_credit saldo insuficiente → exceção ✓
- I5: refund_credit duplicado → no-op ✓
- I6: mesma idempotency_key → mesma tx ✓
- I7: duas reserve_credit simultâneas com saldo justo → ambas OK ✓
</acceptance_criteria>

---

### Task 9: Verificação final

**Action:** Executar verificações de qualidade

<read_first>
- Nenhum — etapa de verificação integrada
</read_first>

<action>
Executar sequência de verificação:

1. `npx vitest run src/lib/credit/__tests__/credit-service.test.ts` — 25+ testes passando
2. `npm run typecheck` — zero erros
3. `npm run lint` — zero erros
4. `npx vitest run` — novos + existentes passando
5. `npm run build` — build bem-sucedido
6. `git status` — verificar que nenhum arquivo existente foi modificado (só novos em `src/lib/credit/` e `supabase/migrations/`)
</action>

<acceptance_criteria>
- `npx vitest run src/lib/credit/__tests__/credit-service.test.ts` exit 0 (25+ testes)
- `npm run typecheck` exit 0
- `npm run lint` exit 0
- `npx vitest run` exit 0 (25+ novos + 740 existentes)
- `npm run build` exit 0
- `git status` mostra apenas novos arquivos (nenhum existente modificado)
</acceptance_criteria>

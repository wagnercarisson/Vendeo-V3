---
id: 25-03
wave: 3
title: Testes e Verificacao
depends_on:
  - 25-01
  - 25-02
files_modified:
  - src/lib/rate-limit/__tests__/rate-limit.test.ts
  - src/app/api/campaign/generate-image/__tests__/route.test.ts
  - src/app/api/store/__tests__/route.test.ts
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-03
  - PIPE-04
  - PIPE-05
  - PIPE-06
  - UI-05
---

# 25-03: Testes e Verificacao

## Objective

Implementar 34+ testes cobrindo rate limit, pipeline transacional completo (fluxo feliz, 402, 429, estorno, retry, idempotencia, onboarding, mandatoryArtworkText, compatibilidade) e executar verificacao final de qualidade (typecheck, lint, build).

## Must Haves

- [ ] 5+ testes de rate limit (`src/lib/rate-limit/__tests__/rate-limit.test.ts`)
- [ ] Teste #1: Fluxo completo saldo suficiente → ready
- [ ] Teste #2: 402 saldo insuficiente
- [ ] Teste #3-4: 429 rate limit hora e dia
- [ ] Teste #5: Copy result vira publication_copy_snapshot
- [ ] Teste #6: Paralelismo (ambos executam)
- [ ] Teste #7-8: mandatoryArtworkText no snapshot visual mas nao no copy; ausente nao quebra
- [ ] Teste #9-11: Image falha → estorno; Copy falha → estorno; ambos falham → estorno unico
- [ ] Teste #12: Persistencia falha → estorno + limpa imagem
- [ ] Teste #13-14: Reserva idempotente; refund idempotente
- [ ] Teste #15-16: Timeout global → estorno; erro nao retryable → falha imediata
- [ ] Teste #17: Retry copy — falha retryable → retry Gemini OK → ready
- [ ] Teste #18-19: Retry falha Gemini → estorno; Gemini nao configurado → estorno sem fallback
- [ ] Teste #20-21: Image falha (retry interno) → estorno; Image recupera via retry interno → ready
- [ ] Teste #22: Copy falha nao retryable → estorno imediato
- [ ] Teste #23-25: Onboarding — loja + grant; grant idempotente; grant falha → loja nao criada
- [ ] Teste #26-28: mandatoryArtworkText no inputSnapshot, no Image Director, AUSENTE no Copy Director
- [ ] Teste #29-30: Campanha antiga sem title → UI nao quebra; F25 com title → snapshot correto
- [ ] Teste #31-32: Edicao manual aceita title? opcional; edicao preserva title
- [ ] Teste #33-34: Rate limit INSERT apos guard; evento permanece em falha
- [ ] `npm run typecheck` — zero erros
- [ ] `npm run lint` — zero erros
- [ ] `npx vitest run` — novos + existentes (768+) passando
- [ ] `npm run build` — build bem-sucedido

## Dependencies

25-01 (Foundation) e 25-02 (Pipeline) — ambos devem estar implementados para os testes integrados.

## Verification Criteria

- `npx vitest run src/lib/rate-limit/__tests__/rate-limit.test.ts` — 5+ testes passando
- `npx vitest run src/app/api/campaign/generate-image/__tests__/route.test.ts` — 29+ testes passando
- Total: 34+ novos testes passando
- `npm run typecheck` — zero erros
- `npm run lint` — zero erros
- `npx vitest run` — todos os testes (novos + existentes) passando
- `npm run build` — build bem-sucedido

---

## Tasks

### Task 1: Rate Limit — Testes

**File:** `src/lib/rate-limit/__tests__/rate-limit.test.ts`

<read_first>
- `src/lib/rate-limit/rate-limit.ts` — checkRateLimit, recordGenerationAttempt
- `src/lib/rate-limit/types.ts` — RateLimitResult
- `src/lib/credit/__tests__/credit-service.test.ts` — F24 mock pattern reference
</read_first>

<action>
Criar `src/lib/rate-limit/__tests__/rate-limit.test.ts` com 5+ testes:

1. `checkRateLimit abaixo do limite em ambas as janelas` — mock consultas SQL retornando contagens < 10 e < 30 → `{ allowed: true, remaining: {...} }`
2. `checkRateLimit excedido na janela de 1h` — mock contagem >= 10 → `{ allowed: false, reason: "hourly_limit_exceeded" }`
3. `checkRateLimit excedido na janela de 24h` — mock contagem < 10 na hora mas >= 30 no dia → `{ allowed: false, reason: "daily_limit_exceeded" }`
4. `recordGenerationAttempt insere com campaign_id = null` — verifica INSERT em `generation_rate_events`
5. `recordGenerationAttempt com campaign_id preenchido` — verifica INSERT com campaign_id
6. `evento permanece mesmo se geracao falhar` — verifica que o registro nao e removido
</action>

<acceptance_criteria>
- Teste 1: `checkRateLimit` retorna `allowed: true` com `remaining` correto
- Teste 2: `checkRateLimit` retorna `allowed: false`, `reason: "hourly_limit_exceeded"`
- Teste 3: `checkRateLimit` retorna `allowed: false`, `reason: "daily_limit_exceeded"`
- Teste 4: `recordGenerationAttempt` insere com `campaign_id = null`
- Teste 5: `recordGenerationAttempt` insere com `campaign_id` preenchido
- Teste 6: registro permanece apos falha
- `npx vitest run src/lib/rate-limit/__tests__/rate-limit.test.ts` — 6 testes passando
</acceptance_criteria>

---

### Task 2: Pipeline — Fluxo completo saldo suficiente (#1)

**File:** `src/app/api/campaign/generate-image/__tests__/route.test.ts`

<read_first>
- `src/app/api/campaign/generate-image/__tests__/route.test.ts` — Existing tests (119 linhas)
- `openspec/changes/fase-25-integracao-transacional-pipeline/specs/transactional-pipeline/spec.md` — Fluxo completo scenario
</read_first>

<action>
Adicionar ao arquivo de teste existente:

1. Mock do `checkRateLimit` e `recordGenerationAttempt` (import de `@/lib/rate-limit/rate-limit`)
2. Mock do `CreditService` (`getBalance`, `reserveCredit`, `confirmCredit`, `refundCredit`)
3. Mock do `CopyDirectorService` (`generateCopy`)
4. Mock do `createTextProvider`

Teste #1: Fluxo completo saldo suficiente → ready
- Mock `checkRateLimit` retorna `{ allowed: true, remaining: { hourly: 10, daily: 30 } }`
- Mock `creditService.getBalance(storeId)` retorna 10 (>= COST_PER_GENERATION)
- Mock `creditService.reserveCredit` retorna 'tx-1'
- Mock `CopyDirectorService.generateCopy` retorna `{ title: "Titulo", caption: "Caption", hashtags: ["#tag"], cta_post: "CTA" }`
- Mock `ImageGenerationService.generateImage` retorna `{ success: true, imageDataUrl: "data:..." }`
- Mock `createCampaign` retorna `{ id: "camp-1", storagePath: "store-1/camp-1.jpg" }`
- Mock `transcodeToJpeg` retorna `{ buffer: Buffer.from(""), mimeType: "image/jpeg" }`
- Mock `uploadCampaignImage` resolve
- Mock `updateCampaignReady` resolve
- Fazer POST request valida
- Verificar status 200 + NDJSON result com `campaignId` e `campaignUrl`
- Verificar que `updateCampaignReady` foi chamado com `publicationCopySnapshot` contendo `title` do Copy Director
</action>

<acceptance_criteria>
- Teste #1: POST com dados validos + saldo suficiente → resposta 200 + NDJSON result
- `publication_copy_snapshot` no `updateCampaignReady` contem title do Copy Director
- TypeScript compila sem erros
</acceptance_criteria>

---

### Task 3: Pipeline — Testes 402, 429 (#2-4)

**File:** `src/app/api/campaign/generate-image/__tests__/route.test.ts`

<read_first>
- `openspec/changes/fase-25-integracao-transacional-pipeline/specs/transactional-pipeline/spec.md` — 402 and 429 scenarios
- `openspec/changes/fase-25-integracao-transacional-pipeline/specs/rate-limit/spec.md` — 429 scenarios
</read_first>

<action>
Adicionar 3 testes:

Teste #2: 402 saldo insuficiente
- Mock `checkRateLimit` retorna `{ allowed: true }`
- Mock `creditService.getBalance(storeId)` retorna 0 (< COST_PER_GENERATION)
- POST request → status 402
- Nenhuma chamada de IA feita (Copy Director, Image Director)
- Nenhum NDJSON event emitido
- Nenhuma campanha criada

Teste #3: 429 rate limit hora
- Mock `checkRateLimit` retorna `{ allowed: false, reason: "hourly_limit_exceeded" }`
- POST request → status 429
- Body contem `error: "rate_limit_exceeded"` e `retryAfter`
- Nenhuma chamada de IA
- Nenhum NDJSON event

Teste #4: 429 rate limit dia
- Mock `checkRateLimit` retorna `{ allowed: false, reason: "daily_limit_exceeded" }`
- POST request → status 429
</action>

<acceptance_criteria>
- Teste #2: POST → 402, sem IA, sem NDJSON
- Teste #3: POST → 429, body com `error` e `retryAfter`
- Teste #4: POST → 429, `reason: "daily_limit_exceeded"`
- `npx vitest run` — 3 testes passando
</acceptance_criteria>

---

### Task 4: Pipeline — Copy e paralelismo (#5-6)

**File:** `src/app/api/campaign/generate-image/__tests__/route.test.ts`

<read_first>
- `openspec/changes/fase-25-integracao-transacional-pipeline/specs/transactional-pipeline/spec.md` — Copy result and parallelism scenarios
</read_first>

<action>
Adicionar 2 testes:

Teste #5: Copy result vira publication_copy_snapshot
- Fluxo completo feliz
- Verificar que `updateCampaignReady` foi chamado com `publicationCopySnapshot.caption` igual ao retorno do `CopyDirectorService.generateCopy`
- Verificar que `buildPublicationCopySnapshot` NAO foi chamado (substituido pelo Copy Director)

Teste #6: Paralelismo — ambos executam
- Verificar que `CopyDirectorService.generateCopy` E `ImageGenerationService.generateImage` foram chamados
- Verificar que o stream contem NDJSON phase events para ambos os ramos
</action>

<acceptance_criteria>
- Teste #5: snapshot tem caption do Copy Director; buildPublicationCopySnapshot nao chamado
- Teste #6: ambos os servicos foram chamados; NDJSON tem eventos de ambos
- `npx vitest run` — 2 testes passando
</acceptance_criteria>

---

### Task 5: Pipeline — mandatoryArtworkText (#7-8)

**File:** `src/app/api/campaign/generate-image/__tests__/route.test.ts`

<read_first>
- `openspec/changes/fase-25-integracao-transacional-pipeline/specs/mandatory-artwork-text/spec.md` — All scenarios
</read_first>

<action>
Adicionar 2 testes:

Teste #7: mandatoryArtworkText no snapshot visual mas nao no copy
- Fluxo com `mandatoryArtworkText: "Imagens meramente ilustrativas"`
- Verificar que `inputSnapshot.mandatoryArtworkText` contem o valor
- Verificar que `publicationCopySnapshot` NAO contem `mandatoryArtworkText`
- Verificar que o `CopyDirectorService.generateCopy` NAO recebeu `mandatoryArtworkText` no input

Teste #8: mandatoryArtworkText ausente nao quebra
- Fluxo sem `mandatoryArtworkText`
- Tudo funciona normalmente
</action>

<acceptance_criteria>
- Teste #7: inputSnapshot tem mandatoryArtworkText; publicationCopySnapshot nao tem; Copy Director nao recebeu
- Teste #8: sem mandatoryArtworkText → fluxo completo funciona
- `npx vitest run` — 2 testes passando
</acceptance_criteria>

---

### Task 6: Pipeline — Estornos (#9-12)

**File:** `src/app/api/campaign/generate-image/__tests__/route.test.ts`

<read_first>
- `openspec/changes/fase-25-integracao-transacional-pipeline/specs/transactional-pipeline/spec.md` — Estorno scenarios
</read_first>

<action>
Adicionar 4 testes:

Teste #9: Image falha → estorno
- Mock `ImageGenerationService.generateImage` retorna `{ success: false, code: "provider_error", message: "Falha" }`
- Mock `CopyDirectorService.generateCopy` resolve com sucesso
- Verificar que `creditService.refundCredit` foi chamado com idempotencyKey
- Verificar que `updateCampaignError` foi chamado
- Verificar NDJSON error

Teste #10: Copy falha → estorno
- Mock `CopyDirectorService.generateCopy` lanca erro
- Mock `ImageGenerationService.generateImage` resolve
- Verificar `refundCredit` chamado, campanha error

Teste #11: Ambos falham → estorno unico
- Ambos mockados para falhar
- Verificar que `refundCredit` foi chamado uma unica vez (idempotente)

Teste #12: Persistencia falha → estorno + limpa imagem
- Copy e Image OK
- `uploadCampaignImage` resolve
- `updateCampaignReady` falha
- Verificar `deleteCampaignImage` chamado (limpeza)
- Verificar `refundCredit` chamado
</action>

<acceptance_criteria>
- Teste #9: Image falha → refund + updateCampaignError + NDJSON error
- Teste #10: Copy falha → refund + campanha error
- Teste #11: Ambos falham → refund unico
- Teste #12: Persistencia falha → delete image + refund
- `npx vitest run` — 4 testes passando
</acceptance_criteria>

---

### Task 7: Pipeline — Idempotencia e timeouts (#13-16)

**File:** `src/app/api/campaign/generate-image/__tests__/route.test.ts`

<read_first>
- `openspec/changes/fase-25-integracao-transacional-pipeline/specs/transactional-pipeline/spec.md` — Idempotency and timeout scenarios
</read_first>

<action>
Adicionar 4 testes:

Teste #13: Reserva idempotente
- Simular que `reserveCredit` e chamado duas vezes com mesma idempotencyKey
- Verificar que a segunda chamada e no-op

Teste #14: Refund idempotente
- Simular `refundCredit` chamado duas vezes com mesma idempotencyKey
- Verificar que a segunda chamada e no-op

Teste #15: Timeout global → estorno
- Mock `ImageGenerationService.generateImage` para nunca resolver (lanca AbortError)
- POST request → NDJSON error com code "global_timeout"
- Verificar `refundCredit` chamado

Teste #16: Erro nao retryable → falha imediata
- Mock `CopyDirectorService.generateCopy` lanca `SafetyBlockError` (nao retryable)
- Verificar que NAO houve tentativa de retry Gemini
- Verificar `refundCredit` chamado
</action>

<acceptance_criteria>
- Teste #13: reserva com mesma key → no-op
- Teste #14: refund com mesma key → no-op
- Teste #15: timeout → error NDJSON + refund
- Teste #16: SafetyBlockError → sem retry + refund
- `npx vitest run` — 4 testes passando
</acceptance_criteria>

---

### Task 8: Pipeline — Retry Gemini (#17-22)

**File:** `src/app/api/campaign/generate-image/__tests__/route.test.ts`

<read_first>
- `openspec/changes/fase-25-integracao-transacional-pipeline/specs/copy-director/spec.md` — Retry scenarios
- `openspec/changes/fase-25-integracao-transacional-pipeline/specs/transactional-pipeline/spec.md` — Retry scenarios
</read_first>

<action>
Adicionar 6 testes:

Teste #17: Retry copy — falha retryable → retry Gemini OK → ready
- Mock primeira chamada `generateCopy` lanca `MalformedResponseError`
- Mock segunda chamada (Gemini) resolve com sucesso
- Verificar que `refundCredit` NAO foi chamado
- Verificar que `updateCampaignReady` foi chamado

Teste #18: Retry copy — falha retryable → retry Gemini falha → estorno
- Mock primeira chamada lanca `MalformedResponseError`
- Mock segunda chamada tambem falha
- Verificar `refundCredit` chamado

Teste #19: Retry copy — Gemini nao configurado → estorno sem fallback
- Mock `TEXT_FALLBACK_PROVIDER` nao configurado
- Primeira chamada falha retryable
- Verificar que NAO houve tentativa Gemini (fallback nao disponivel)
- Verificar `refundCredit` chamado

Teste #20: Image falha (retry interno esgotado) → estorno
- Mock `ImageGenerationService.generateImage` retorna falha apos retry interno
- Verificar estorno

Teste #21: Image recupera via retry interno → ready
- Mock `ImageGenerationService.generateImage` eventualmente sucede apos retry interno
- Verificar ready

Teste #22: Copy falha nao retryable (SafetyBlockError) → estorno imediato
- `generateCopy` lanca `SafetyBlockError`
- Sem retry Gemini
- `refundCredit` chamado
</action>

<acceptance_criteria>
- Teste #17: retryable → Gemini OK → ready, sem refund
- Teste #18: retryable → Gemini falha → refund
- Teste #19: retryable + sem fallback → refund, sem Gemini
- Teste #20: Image falha → refund
- Teste #21: Image recupera → ready
- Teste #22: nao retryable → refund imediato, sem retry
- `npx vitest run` — 6 testes passando
</acceptance_criteria>

---

### Task 9a: Onboarding — Testes do RPC transacional (#23-25)

**File (novo):** `src/app/api/store/__tests__/route.test.ts`

**Nao usar o arquivo de testes do generate-image.** A store route tem handler proprio.

<read_first>
- `openspec/changes/fase-25-integracao-transacional-pipeline/specs/onboarding-grant/spec.md` — All scenarios
- `.planning/phases/25-integracao-transacional-pipeline/25-02-PLAN.md` — Task 10 (RPC exclusivo)
- `src/app/api/store/route.ts` — Handler que chama `supabaseAdmin.rpc('create_store_with_initial_grant', params)`
</read_first>

<action>
Criar `src/app/api/store/__tests__/route.test.ts` com 3 testes:

Teste #23: Onboarding — RPC transacional cria loja + grant
- Mock `supabaseAdmin.rpc('create_store_with_initial_grant', params)` → { data: { store_id, saldo: 5 }, error: null }
- POST /api/store com dados validos
- Verificar 201 + resposta contem store_id + saldo = 5
- Verificar que supabaseAdmin.rpc foi chamado com o nome da function e os parametros corretos
- NAO verificar `creditService.grantCredits` diretamente — o RPC encapsula

Teste #24: Onboarding — idempotencia do RPC
- Mock `supabaseAdmin.rpc` → primeira chamada OK, segunda OK (idempotencyKey `onboarding_${storeId}` dentro do RPC)
- POST /api/store duas vezes
- Verificar 201 em ambas
- Verificar que saldo final e 5 (nao 10) — cada chamada retorna saldo 5

Teste #25: Onboarding — RPC falha → 500, store nao criada
- Mock `supabaseAdmin.rpc('create_store_with_initial_grant', params)` retorna { data: null, error: { message: "grant failed" } }
- POST /api/store → verificar 500
- Nao ha store para deletar (ROLLBACK na funcao SQL)
</action>

<acceptance_criteria>
- Teste #23: 201 + store_id + saldo 5 via RPC
- Teste #24: idempotente (mesmo saldo na segunda)
- Teste #25: RPC error → 500, sem side effects
- `npx vitest run src/app/api/store/__tests__/route.test.ts` — 3 testes passando
</acceptance_criteria>

---

### Task 9b: mandatoryArtworkText — Testes de propagacao (#26-28)

**File:** `src/app/api/campaign/generate-image/__tests__/route.test.ts`

<read_first>
- `openspec/changes/fase-25-integracao-transacional-pipeline/specs/mandatory-artwork-text/spec.md` — All scenarios
- `openspec/changes/fase-25-integracao-transacional-pipeline/25-02-PLAN.md` — Tasks 3-7 (propagação)
</read_first>

<action>
Adicionar 3 testes ao arquivo existente da route de generate-image:

Teste #26: mandatoryArtworkText no inputSnapshot
- POST com mandatoryArtworkText preenchido
- `createCampaign` chamado com `inputSnapshot.mandatoryArtworkText` contendo o valor

Teste #27: mandatoryArtworkText no Image Director
- `ImageGenerationService.generateImage` recebe brief com `campaignInput.mandatoryArtworkText`

Teste #28: mandatoryArtworkText AUSENTE no Copy Director
- `CopyDirectorService.generateCopy` NAO recebe mandatoryArtworkText no input
</action>

<acceptance_criteria>
- Teste #26: inputSnapshot tem mandatoryArtworkText
- Teste #27: Image Director recebe mandatoryArtworkText
- Teste #28: Copy Director NAO recebe mandatoryArtworkText
- `npx vitest run` — 3 testes passando
</acceptance_criteria>

---

### Task 10: Compatibilidade Retroativa + Rate Limit — Testes (#29-34)

**File:** `src/app/api/campaign/generate-image/__tests__/route.test.ts`

<read_first>
- `openspec/changes/fase-25-integracao-transacional-pipeline/specs/campaign-display-contract/spec.md` — title? scenarios
- `openspec/changes/fase-25-integracao-transacional-pipeline/specs/publication-copy-validation/spec.md` — title? scenarios
- `openspec/changes/fase-25-integracao-transacional-pipeline/specs/rate-limit/spec.md` — INSERT and permanence scenarios
</read_first>

<action>
Adicionar 6 testes:

Teste #29: Campanha antiga sem title → UI nao quebra
- `getEffectivePublicationCopy` com snapshot sem title → retorna sem title
- `mapCampaignToProps` retorna `title: undefined`
- UI trata ausencia sem quebra

Teste #30: Campanha F25 com title → snapshot correto
- `getEffectivePublicationCopy` com snapshot contendo title → retorna com title

Teste #31: Edicao manual aceita title? opcional
- `validatePublicationCopy` com `{ title: "Novo Titulo", caption: "...", hashtags: [...], cta_post: "..." }` → valido

Teste #32: Edicao manual preserva title em campanha F25
- Ao editar caption apenas, title nao e sobrescrito

Teste #33: Rate limit — INSERT imediatamente apos guard
- `recordGenerationAttempt` chamado LOGO apos `checkRateLimit` passar e ANTES de qualquer operacao paga

Teste #34: Evento permanece mesmo em falha
- Geracao falha apos `recordGenerationAttempt` → registro em `generation_rate_events` permanece
</action>

<acceptance_criteria>
- Teste #29: title undefined para campanha sem title
- Teste #30: title presente no snapshot F25
- Teste #31: validatePublicationCopy aceita title opcional
- Teste #32: edicao nao sobrescreve title
- Teste #33: recordGenerationAttempt antes de operacao paga
- Teste #34: registro permanece em falha
- `npx vitest run` — 6 testes passando
</acceptance_criteria>

---

### Task 11: Verificacao Final

**Action:** Executar verificacoes de qualidade

<read_first>
- Nenhum — etapa de verificacao integrada
</read_first>

<action>
Executar sequencia de verificacao:

1. `npx vitest run src/lib/rate-limit/__tests__/rate-limit.test.ts` — 6 testes passando
2. `npx vitest run src/app/api/store/__tests__/route.test.ts` — 3 testes passando
3. `npx vitest run src/app/api/campaign/generate-image/__tests__/route.test.ts` — 28+3=31+ testes
4. `npm run typecheck` — zero erros
5. `npm run lint` — zero erros
6. `npx vitest run` — novos + existentes passando (768+)
7. `npm run build` — build bem-sucedido
</action>

<acceptance_criteria>
- Rate limit: 6/6 passando
- Store route: 3/3 passando (onboarding RPC)
- Pipeline: 28+3=31+ passando (total 40+)
- `npm run typecheck` exit 0
- `npm run lint` exit 0
- `npx vitest run` exit 0
- `npm run build` exit 0
</acceptance_criteria>

---
id: 26-03
wave: 3
title: Testes e Verificação
depends_on:
  - 26-01
  - 26-02
files_modified:
  - src/lib/admin/__tests__/require-admin.test.ts
  - src/app/api/admin/__tests__/credits-grant.test.ts
  - src/app/api/admin/__tests__/stores.test.ts
  - src/app/api/admin/__tests__/audit-log.test.ts
  - src/app/api/admin/__tests__/users.test.ts
  - src/app/api/admin/__tests__/campaigns-errors.test.ts
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02
  - ADMIN-03
  - ADMIN-04
  - ADMIN-05
  - ADMIN-06
  - SEC-04
  - SEC-06
---

# 26-03: Testes e Verificação

## Objective

Implementar 21+ testes cobrindo admin gate, concessão de créditos, criação de loja, audit log, listagens e executar verificação final de qualidade (typecheck, lint, build).

## Must Haves

- [ ] 3+ testes do admin gate (`src/lib/admin/__tests__/require-admin.test.ts`)
- [ ] 7+ testes de concessão de créditos (`POST /api/admin/credits/grant`)
- [ ] 4+ testes de criação de loja (`POST /api/admin/stores`)
- [ ] 3+ testes do audit log (append-only, registro, histórico)
- [ ] 4+ testes de listagem (`/admin/users`, `/admin/users/[id]`, `/admin/campaigns/errors`, `/admin/audit-log`)
- [ ] `npm run typecheck` — zero erros
- [ ] `npm run lint` — zero erros
- [ ] `npx vitest run` — novos + existentes passando (zero regressão)
- [ ] `npm run build` — build bem-sucedido

## Dependencies

26-01 (Migration, requireAdmin, Middleware) e 26-02 (API routes, Pages) — ambos devem estar implementados para os testes integrados.

## Verification Criteria

- `npx vitest run src/lib/admin/__tests__/require-admin.test.ts` — 3+ testes passando
- `npx vitest run src/app/api/admin/__tests__/` — 18+ testes passando
- Total: 21+ novos testes passando
- `npm run typecheck` — zero erros
- `npm run lint` — zero erros
- `npx vitest run` — todos os testes (novos + existentes) passando
- `npm run build` — build bem-sucedido

---

## Tasks

### Task 1: Admin Gate — Testes

**File:** `src/lib/admin/__tests__/require-admin.test.ts`

<read_first>
- `src/lib/admin/require-admin.ts` — requireAdmin() function
- `src/lib/auth/__tests__/require-user.test.ts` — Existing auth gate test pattern
</read_first>

<action>
Criar `src/lib/admin/__tests__/require-admin.test.ts` com 3+ testes:

1. Teste #1: `requireAdmin()` com admin_user → OK
   - Mock `requireApiUser()` retorna `{ userId: "admin-1" }`
   - Mock `supabaseAdmin.from("admin_users").select(...)` retorna `{ data: { user_id: "admin-1" } }`
   - Verificar que retorna `{ userId: "admin-1" }` sem lançar erro

2. Teste #2: `requireAdmin()` com user comum → ForbiddenError
   - Mock `requireApiUser()` retorna `{ userId: "user-1" }`
   - Mock `supabaseAdmin.from("admin_users").select(...)` retorna `{ data: null }`
   - Verificar que lança ForbiddenError com mensagem "Acesso restrito a administradores"

3. Teste #3: `requireAdmin()` sem auth → UnauthorizedError
   - Mock `requireApiUser()` lança UnauthorizedError
   - Verificar que o erro é propagado sem consultar admin_users
</action>

<acceptance_criteria>
- Teste #1: admin user → retorna userId sem erro
- Teste #2: user comum → ForbiddenError
- Teste #3: sem auth → UnauthorizedError
- Todos usam supabaseAdmin para consultar admin_users
- `npx vitest run` — 3 testes passando
</acceptance_criteria>

---

### Task 2: Concessão de Créditos — Testes

**File:** `src/app/api/admin/__tests__/credits-grant.test.ts`

<read_first>
- `src/app/api/admin/credits/grant/route.ts` — POST handler
- `openspec/changes/fase-26-admin-operacional/specs/admin-credit-grant/spec.md` — All scenarios
</read_first>

<action>
Criar `src/app/api/admin/__tests__/credits-grant.test.ts` com 7+ testes:

1. Teste #4: Grant com motivo válido → crédito adicionado + audit log
   - Mock `requireAdmin()` → OK
   - Mock `supabaseAdmin.rpc("admin_grant_credits", ...)` → { transaction_id: "tx-1", audit_id: "audit-1" }
   - Mock `creditService.getBalance(storeId)` → 50
   - POST com dados válidos → 200 + { transaction_id, audit_id, newBalance: 50 }

2. Teste #5: Grant com motivo vazio → 400

3. Teste #6: Grant com storeId malformado → 400

4. Teste #7: Grant com storeId válido mas inexistente → 404 (RPC error)

5. Teste #8: Grant sem auth → 401 (requireAdmin propaga)

6. Teste #9: Grant com user não admin → 403

7. Teste #10: Mesmo operationId em retry → idempotente
</action>

<acceptance_criteria>
- Teste #4: 200 + transaction_id + audit_id + newBalance
- Teste #5: 400
- Teste #6: 400
- Teste #7: 404
- Teste #8: 401
- Teste #9: 403
- Teste #10: idempotente (retry com mesmo operationId retorna mesmos dados)
- `npx vitest run` — 7 testes passando
</acceptance_criteria>

---

### Task 3: Criação de Loja — Testes

**File:** `src/app/api/admin/__tests__/stores.test.ts`

<read_first>
- `src/app/api/admin/stores/route.ts` — POST handler
- `openspec/changes/fase-26-admin-operacional/specs/admin-store-create/spec.md` — All scenarios
</read_first>

<action>
Criar `src/app/api/admin/__tests__/stores.test.ts` com 4+ testes:

1. Teste #11: Cria loja para usuário sem loja → 201 + loja criada com grant inicial
   - Mock `requireAdmin()` → OK
   - Mock `supabaseAdmin.rpc("admin_create_store_for_user", ...)` → { id: "store-1", name: "Loja", balance: 5 }
   - POST com dados válidos → 201

2. Teste #12: Cria loja para usuário que já tem loja → 409
   - Mock RPC retorna erro com código `usuario_ja_possui_loja`

3. Teste #13: Sem auth → 401

4. Teste #14: User não admin → 403
</action>

<acceptance_criteria>
- Teste #11: 201 + store criada com grant inicial
- Teste #12: 409 Conflict
- Teste #13: 401
- Teste #14: 403
- `npx vitest run` — 4 testes passando
</acceptance_criteria>

---

### Task 4: Audit Log — Testes

**File:** `src/app/api/admin/__tests__/audit-log.test.ts`

<read_first>
- `openspec/changes/fase-26-admin-operacional/specs/admin-audit-log/spec.md` — Audit log scenarios
- `supabase/migrations/20260717000003_create_admin_tables.sql` — Append-only trigger
</read_first>

<action>
Criar `src/app/api/admin/__tests__/audit-log.test.ts` com 3+ testes:

1. Teste #15: Ação registrada com actor, action, target, reason, operation_id
   - Simular INSERT em admin_audit_log via RPC admin_grant_credits
   - Verificar que entry contém todos os campos obrigatórios

2. Teste #16: Audit log é append-only (UPDATE → erro)
   - Simular UPDATE em admin_audit_log
   - Verificar que trigger rejeita com exceção

3. Teste #17: Múltiplas ações → histórico cronológico
   - Inserir múltiplas entries
   - GET /api/admin/audit-log → retorna ordenado por created_at DESC
</action>

<acceptance_criteria>
- Teste #15: entry registrada com todos os campos
- Teste #16: UPDATE rejeitado pelo trigger
- Teste #17: histórico retorna em ordem cronológica reversa
- `npx vitest run` — 3 testes passando
</acceptance_criteria>

---

### Task 5: Listagem e View — Testes

**File:** `src/app/api/admin/__tests__/users.test.ts` e `src/app/api/admin/__tests__/campaigns-errors.test.ts`

<read_first>
- `openspec/changes/fase-26-admin-operacional/specs/admin-user-directory/spec.md` — List and detail scenarios
- `openspec/changes/fase-26-admin-operacional/specs/admin-campaign-errors/spec.md` — Error campaigns scenarios
</read_first>

<action>
Criar testes para endpoints de listagem (4+ testes):

1. Teste #18: `/admin/users` retorna lista paginada
   - GET com page=1 → 200 + data array + total

2. Teste #19: `/admin/users/[id]` retorna detalhe com saldo + extrato
   - GET com ID válido → dados consolidados

3. Teste #20: `/admin/campaigns/errors` retorna campanhas com erro
   - GET → lista paginada de campanhas error

4. Teste #21: `/admin/audit-log` retorna histórico paginado
   - GET → histórico paginado ordenado
</action>

<acceptance_criteria>
- Teste #18: users list retorna paginada com dados consolidados
- Teste #19: user detail retorna saldo + extrato + campanhas
- Teste #20: error campaigns retorna lista com store context
- Teste #21: audit log retorna histórico paginado
- `npx vitest run` — 4 testes passando
</acceptance_criteria>

---

### Task 6: Verificação Final

**Action:** Executar verificações de qualidade

<read_first>
- Nenhum — etapa de verificação integrada
</read_first>

<action>
Executar sequência de verificação:

1. `npx vitest run src/lib/admin/__tests__/` — 3+ testes passando
2. `npx vitest run src/app/api/admin/__tests__/` — 18+ testes passando
3. `npm run typecheck` — zero erros
4. `npm run lint` — zero erros
5. `npx vitest run` — novos + existentes passando (zero regressão)
6. `npm run build` — build bem-sucedido

Verificar UAT local:
7. Admin cria loja para usuário sem loja → loja criada com saldo inicial
8. Admin concede crédito → saldo atualizado → audit log registrado
</action>

<acceptance_criteria>
- Admin gate: 3/3 passando
- Concessão de créditos: 7/7 passando
- Criação de loja: 4/4 passando
- Audit log: 3/3 passando
- Listagem e view: 4/4 passando
- Total: 21+ testes passando
- `npm run typecheck` exit 0
- `npm run lint` exit 0
- `npx vitest run` exit 0
- `npm run build` exit 0
- UAT local: store creation + credit grant funcionais
</acceptance_criteria>

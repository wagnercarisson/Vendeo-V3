---
id: 27-03
wave: 3
title: Testes e Verificação
depends_on:
  - 27-01
  - 27-02
files_modified:
  - src/components/credit/__tests__/balance-display.test.tsx
  - src/components/credit/__tests__/balance-card.test.tsx
  - src/components/credit/__tests__/transaction-history.test.tsx
  - src/components/credit/__tests__/credit-cta.test.tsx
  - src/app/(app)/dashboard/__tests__/dashboard-credits.test.tsx
  - src/app/(app)/campanhas/nova/__tests__/campaign-flow-credits.test.tsx
  - src/lib/credit/__tests__/credit-service.test.ts
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - UI-06
---

# 27-03: Testes e Verificação

## Objective

Implementar 19+ testes cobrindo componentes de crédito, dashboard, fluxo de geração e integração CreditService, e executar verificação final de qualidade (typecheck, lint, build).

## Must Haves

- [ ] 11+ testes dos componentes de crédito (BalanceDisplay 4, BalanceCard 3, TransactionHistory 3, CreditCta 1)
- [ ] 3+ testes de dashboard (saldo com campanhas, sem campanhas, sem loja)
- [ ] 2+ testes de fluxo de geração (saldo suficiente, saldo zero, erro de saldo)
- [ ] 3+ testes de integração CreditService (session client, countCreditTransactions, extrato filtrado)
- [ ] `npm run typecheck` — zero erros
- [ ] `npm run lint` — zero erros
- [ ] `npx vitest run` — novos + existentes passando (zero regressão)
- [ ] `npm run build` — build bem-sucedido

## Dependencies

27-01 (CreditService session, BalanceDisplay, BalanceCard) e 27-02 (TransactionHistory, CreditCta, Dashboard, /conta, campaign flow) — todos devem estar implementados para os testes integrados.

## Verification Criteria

- `npx vitest run src/components/credit/__tests__/` — 11+ testes passando
- `npx vitest run src/app/(app)/dashboard/__tests__/dashboard-credits.test.tsx` — 3+ testes passando
- `npx vitest run src/app/(app)/campanhas/nova/__tests__/campaign-flow-credits.test.tsx` — 2+ testes passando
- `npx vitest run src/lib/credit/__tests__/credit-service.test.ts` — 3+ novos testes (total existentes + novos) passando
- Total: 19+ novos testes passando
- `npm run typecheck` — zero erros
- `npm run lint` — zero erros
- `npx vitest run` — todos os testes (novos + existentes) passando
- `npm run build` — build bem-sucedido

---

## Tasks

### Task 1: BalanceDisplay — Testes

**File:** `src/components/credit/__tests__/balance-display.test.tsx`

<read_first>
- `src/components/credit/balance-display.tsx` — BalanceDisplay component
- `openspec/changes/fase-27-conta-saldo-extrato/specs/balance-display/spec.md` — All 13 scenarios
</read_first>

<action>
Criar `src/components/credit/__tests__/balance-display.test.tsx` com 4 testes:

1. **Teste #1: Saldo normal → badge verde**
   - Renderizar `<BalanceDisplay balance={5} />`
   - Verificar: badge com "5 créditos" e classe/tag verde

2. **Teste #2: Saldo baixo → badge amarelo**
   - Renderizar `<BalanceDisplay balance={2} />`
   - Verificar: badge com "2 créditos" e classe/tag amarela

3. **Teste #3: Saldo zero → badge vermelho + CTA**
   - Renderizar `<BalanceDisplay balance={0} showCta={true} ctaHref="/conta" />`
   - Verificar: badge vermelho "0 créditos" e CTA visível com link para `/conta`

4. **Teste #4: Sem loja → fallback 0 sem alerta**
   - Renderizar `<BalanceDisplay balance={0} hasStore={false} />`
   - Verificar: exibe "0 créditos" sem badge de alerta e sem CTA de solicitação
</action>

<acceptance_criteria>
- Teste #1: badge verde com valor formatado
- Teste #2: badge amarelo com alerta
- Teste #3: badge vermelho + CTA visível
- Teste #4: fallback 0 sem alerta
- `npx vitest run` — 4 testes passando
</acceptance_criteria>

---

### Task 2: BalanceCard — Testes

**File:** `src/components/credit/__tests__/balance-card.test.tsx`

<read_first>
- `src/components/credit/balance-card.tsx` — BalanceCard component
- `openspec/changes/fase-27-conta-saldo-extrato/specs/balance-card/spec.md` — All 5 scenarios
</read_first>

<action>
Criar `src/components/credit/__tests__/balance-card.test.tsx` com 3 testes:

1. **Teste #5: Saldo normal → valor formatado**
   - Renderizar `<BalanceCard balance={42} hasStore={true} />`
   - Verificar: exibe "42 créditos" em destaque e "Cada geração consome 1 crédito"

2. **Teste #6: Saldo zero → CTA visível**
   - Renderizar `<BalanceCard balance={0} hasStore={true} />`
   - Verificar: exibe "Créditos insuficientes" e CTA "Solicitar créditos"

3. **Teste #7: Sem store → mensagem onboarding + CTA**
   - Renderizar `<BalanceCard hasStore={false} />`
   - Verificar: exibe "Você ainda não tem uma loja" e CTA "Criar loja" com link para `/loja`
</action>

<acceptance_criteria>
- Teste #5: valor formatado + descrição visíveis
- Teste #6: CTA "Solicitar créditos" visível e clicável
- Teste #7: "Você ainda não tem uma loja" + CTA "Criar loja" → `/loja`
- `npx vitest run` — 3 testes passando
</acceptance_criteria>

---

### Task 3: TransactionHistory — Testes

**File:** `src/components/credit/__tests__/transaction-history.test.tsx`

<read_first>
- `src/components/credit/transaction-history.tsx` — TransactionHistory component
- `openspec/changes/fase-27-conta-saldo-extrato/specs/transaction-history/spec.md` — All 7 scenarios
- `src/lib/credit/types.ts` — CreditTransaction type
</read_first>

<action>
Criar `src/components/credit/__tests__/transaction-history.test.tsx` com 3 testes:

1. **Teste #8: Tabela com transações — colunas visíveis**
   - Renderizar `<TransactionHistory transactions={mockTransactions} totalPages={2} currentPage={1} />`
   - Verificar: tabela com colunas Tipo, Valor, Saldo, Motivo, Data
   - Verificar: tipo "deduction" exibe "Geração", tipo "grant" exibe "Concessão"

2. **Teste #9: Lista vazia — empty state**
   - Renderizar `<TransactionHistory transactions={[]} totalPages={0} currentPage={1} />`
   - Verificar: exibe "Nenhuma transação encontrada"

3. **Teste #10: Paginação — navegação entre páginas**
   - Renderizar `<TransactionHistory transactions={mockTransactions} totalPages={3} currentPage={2} />`
   - Verificar: controles de paginação visíveis
   - Verificar: onPageChange atualiza URL com `?page=N`
</action>

<acceptance_criteria>
- Teste #8: tabela com 5 colunas, type mapping correto
- Teste #9: empty state com mensagem
- Teste #10: paginação visível com `totalPages > 1`, navegação funcional
- `npx vitest run` — 3 testes passando
</acceptance_criteria>

---

### Task 4: CreditCta — Testes

**File:** `src/components/credit/__tests__/credit-cta.test.tsx`

<read_first>
- `src/components/credit/credit-cta.tsx` — CreditCta component
- `openspec/changes/fase-27-conta-saldo-extrato/specs/credit-cta/spec.md` — All 7 scenarios
</read_first>

<action>
Criar `src/components/credit/__tests__/credit-cta.test.tsx` com 1 teste:

1. **Teste #11: CreditCta com email configurado → modal com mailto**
   - Renderizar `<CreditCta variant="zero" supportEmail="suporte@vendeo.app" />`
   - Verificar: botão "Solicitar créditos" visível
   - Simular clique → modal abre com link `mailto:suporte@vendeo.app`
   - Simular fechar → modal fecha
</action>

<acceptance_criteria>
- Teste #11: CTA renderizado, modal abre com mailto, modal fecha
- `npx vitest run` — 1 teste passando
</acceptance_criteria>

---

### Task 5: Dashboard + Geração — Testes

**Files:**
- `src/app/(app)/dashboard/__tests__/dashboard-credits.test.tsx`
- `src/app/(app)/campanhas/nova/__tests__/campaign-flow-credits.test.tsx`

<read_first>
- `openspec/changes/fase-27-conta-saldo-extrato/specs/dashboard-inteligente/spec.md` — All 5 scenarios
- `openspec/changes/fase-27-conta-saldo-extrato/specs/campaign-input-ui/spec.md` — All 7 scenarios
- `src/app/(app)/dashboard/page.tsx` — Modified dashboard
- `src/components/flow/campaign-input-form.tsx` — Modified form
</read_first>

<action>
Criar 2 arquivos de teste:

**5.1 Dashboard (`src/app/(app)/dashboard/__tests__/dashboard-credits.test.tsx`) — 3 testes:**

1. **Teste #12: `has_store_with_campaigns` → badge de saldo no grid**
   - Mock store com campanhas
   - Mock `CreditService.getBalance` retorna 10
   - Verificar: badge de saldo com "10 créditos" no grid de métricas

2. **Teste #13: `has_store_no_campaigns` → badge de saldo no empty state**
   - Mock store sem campanhas
   - Mock `CreditService.getBalance` retorna 5
   - Verificar: badge de saldo visível no empty state

3. **Teste #14: `no_store` → sem badge de saldo**
   - Mock sem store
   - Verificar: sem badge de saldo (apenas empty state "Criar loja")

**5.2 Geração (`src/app/(app)/campanhas/nova/__tests__/campaign-flow-credits.test.tsx`) — 3 testes:**

4. **Teste #15: Saldo ≥ 1 → botão gerar habilitado + saldo visível**
   - Mock saldo = 5
   - Verificar: indicador "Saldo: 5 créditos    Custo: 1" visível
   - Verificar: botão "Gerar campanha" habilitado

5. **Teste #16: Saldo 0 → botão gerar desabilitado + tooltip + CTA**
   - Mock saldo = 0
   - Verificar: botão desabilitado
   - Verificar: tooltip "Você precisa de créditos para gerar uma campanha"
   - Verificar: CTA "Solicitar créditos" visível

6. **Teste #17: Erro de saldo → botão desabilitado + tooltip + "Tentar novamente" (nunca saldo zero)**
   - Mock `CreditService.getBalance` lança erro
   - Verificar: botão desabilitado
   - Verificar: tooltip "Não foi possível confirmar seu saldo"
   - Verificar: mensagem distinta (não "Saldo: 0 créditos")
   - Verificar: ação "Tentar novamente" visível
   - Verificar: CTA "Solicitar créditos" NÃO visível (nunca tratar erro como saldo zero)
</action>

<acceptance_criteria>
- Teste #12: badge saldo no grid com campanhas
- Teste #13: badge saldo no empty state sem campanhas
- Teste #14: sem badge sem loja
- Teste #15: botão habilitado com saldo suficiente
- Teste #16: botão desabilitado com tooltip + CTA quando zero
- Teste #17: erro não tratado como saldo zero (sem CTA solicitar, com "Tentar novamente")
- `npx vitest run` — 6 testes passando
</acceptance_criteria>

---

### Task 6: CreditService — Testes de Integração

**File:** `src/lib/credit/__tests__/credit-service.test.ts`

<read_first>
- `src/lib/credit/credit-service.ts` — CreditService (após task 1 da 27-01)
- `openspec/changes/fase-27-conta-saldo-extrato/specs/credit-service/spec.md` — Constructor + countCreditTransactions scenarios
- `src/lib/credit/__tests__/credit-service.test.ts` — Existing tests
</read_first>

<action>
Modificar `src/lib/credit/__tests__/credit-service.test.ts` — adicionar 3 novos testes:

1. **Teste #18: CreditService com cliente de sessão → lê saldo via RLS**
   - Criar `CreditService` com `mockSessionClient` (mock de `SupabaseClient`)
   - Mock `from("credit_balances").select(...)` retorna saldo
   - Verificar: `getBalance(storeId)` retorna valor correto via cliente de sessão

2. **Teste #19: `countCreditTransactions()` → total de transações**
   - Mock `from("credit_transactions").select("*", { count: "exact", head: true })` retorna `{ count: 15 }`
   - Verificar: `countCreditTransactions(storeId)` retorna 15
   - Verificar: query inclui `.neq("type", "adjustment")`

3. **Teste #20: CreditService com cliente de sessão → extrato filtrado (sem adjustment)**
   - Mock `from("credit_transactions").select("*")` com `.neq("type", "adjustment")`
   - Verificar: `getHistory(storeId)` retorna apenas transações sem adjustment
</action>

<acceptance_criteria>
- Teste #18: getBalance funciona com session client via RLS
- Teste #19: countCreditTransactions retorna total correto, exclui adjustment
- Teste #20: getHistory filtra adjustment com session client
- Testes existentes de CreditService continuam passando
- `npx vitest run src/lib/credit/__tests__/` — todos passando
</acceptance_criteria>

---

### Task 7: Verificação Final

**Action:** Executar verificações de qualidade

<read_first>
- Nenhum — etapa de verificação integrada
</read_first>

<action>
Executar sequência de verificação:

1. `npx vitest run src/components/credit/__tests__/` — 11+ testes passando
2. `npx vitest run src/app/(app)/dashboard/__tests__/dashboard-credits.test.tsx` — 3+ testes passando
3. `npx vitest run src/app/(app)/campanhas/nova/__tests__/campaign-flow-credits.test.tsx` — 3+ testes passando
4. `npx vitest run src/lib/credit/__tests__/` — 3+ novos + existentes passando
5. `npm run typecheck` — zero erros
6. `npm run lint` — zero erros
7. `npx vitest run` — novos + existentes passando (zero regressão)
8. `npm run build` — build bem-sucedido

Verificar UAT local:
9. Dashboard exibe badge de saldo com e sem campanhas
10. `/conta` exibe seção créditos com saldo + extrato paginado + CTA
11. `/campanhas/nova` exibe saldo + custo, bloqueia geração sem crédito
12. Erro ao carregar saldo mostra mensagem distinta, não "saldo zero"
</action>

<acceptance_criteria>
- Componentes: 11/11 passando
- Dashboard: 3/3 passando
- Geração: 3/3 passando
- CreditService: 3/3 novos + existentes passando
- Total: 20+ novos testes passando
- `npm run typecheck` exit 0
- `npm run lint` exit 0
- `npx vitest run` exit 0 (todos os testes)
- `npm run build` exit 0
- UAT local: saldo visível no dashboard, extrato paginado em /conta, CTA em saldo zero, geração bloqueada sem crédito
</acceptance_criteria>

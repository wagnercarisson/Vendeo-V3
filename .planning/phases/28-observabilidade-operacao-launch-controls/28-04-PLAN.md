---
id: 28-04
wave: 4
title: Testes e Verificação — Concorrência, Telemetria, Regressão + UAT
depends_on:
  - 28-01
  - 28-02
  - 28-03
files_modified:
  - src/__tests__/concurrency.test.ts
  - src/__tests__/telemetry.test.ts
  - src/__tests__/regression-master-switch.test.ts
autonomous: false
requirements:
  - OPS-09
  - OPS-02
  - OPS-05
---

# 28-04: Testes e Verificação — Concorrência, Telemetria, Regressão + UAT

## Objective

Validar a instrumentação operacional com testes de concorrência no pipeline (2 requests simultâneos, saldo=1, apenas um vence), testes de telemetria (INSERT, nullable, best-effort), testes de regressão (master switch v15Enabled=false, emergency brake generationPaused=true), e verificação final (typecheck, lint, build, UAT local).

## Must Haves

- [ ] Teste de concorrência: 2 requests simultâneos, mesma loja, saldo=1 → apenas um vence (200), outro falha (402)
- [ ] Teste de concorrência: saldo final = 0, credit_transactions consistente (1 deduction, 0 refund), 1 campanha criada
- [ ] Teste de telemetria: INSERT campaign_copy com tokens → registro completo
- [ ] Teste de telemetria: INSERT sem campaign_id → nullable funciona
- [ ] Teste de telemetria: falha de INSERT → pipeline continua (best-effort)
- [ ] Teste de regressão: v15Enabled=false → pipeline v1.4 (fluxo completo funciona)
- [ ] Teste de regressão: generationPaused=true → HTTP 503 antes de qualquer operação
- [ ] `npx vitest run` — 28+ novos + ~852 existentes passando
- [ ] `npm run typecheck` — zero erros
- [ ] `npm run lint` — zero erros
- [ ] `npm run build` — build bem-sucedido
- [ ] UAT local: geração com todas as flags ativas funciona como antes
- [ ] UAT local: VENDEO_GENERATION_PAUSED=true → 503
- [ ] UAT local: VENDEO_V15_ENABLED=false → pipeline v1.4
- [ ] UAT local: /admin/metrics acessível apenas via admin, cards com valores, health state coerente

## Dependencies

28-01 (launch-config, pipeline-logger, ai-cost), 28-02 (migrations, pipeline integration, rate-limit), 28-03 (metrics, dashboard, docs).

## Verification Criteria

- Concurrency test: Promise.all with 2 requests, same storeId, balance=1 → one 200, one 402
- Concurrency test: final balance = 0, 1 deduction, 0 refund, 1 campaign created
- Telemetry: campaign_copy INSERT has all required fields (provider, model, tokens, cost, etc.)
- Telemetry: INSERT without campaign_id succeeds (nullable)
- Telemetry: mocked INSERT failure → pipeline continues, response still sent
- Regression: v15Enabled=false → no credit check, no Copy Director, no rate limit
- Regression: generationPaused=true → 503 response
- 28+ new tests passing, ~852 existing tests still passing
- typecheck, lint, build all clean
- UAT: 4 scenarios verified manually

---

## Tasks

### Task 1: Test — Concorrência no Pipeline

**File:** `src/__tests__/concurrency.test.ts`

<read_first>
- `openspec/changes/fase-28-observabilidade-operacao-launch-controls/specs/concurrency-test/spec.md` — 3 scenarios
- `src/app/api/campaign/generate-image/route.ts` — Handler being tested
- Test patterns in `src/__tests__/` for structure reference

</read_first>

<action>
Create concurrency test:
1. Mock TextProvider and ImageProvider (no real HTTP calls)
2. Set up store with balance = 1
3. Fire 2 simultaneous requests via `Promise.all` to `POST /api/campaign/generate-image` with same storeId
4. Assert: one returns 200 with campaign data, other returns 402 with insufficient balance error
5. Assert: final balance = 0
6. Assert: credit_transactions has exactly 1 deduction, 0 refund
7. Assert: exactly 1 campaign created with status = 'ready'
8. Assert: vencedor's campaign has publication_copy_snapshot populated

<acceptance_criteria>
- Test passes consistently (not flaky)
- Race condition is correctly handled by reserve_credit atomicity
- Only 1 campaign created for that store in the period
</acceptance_criteria>

---

### Task 2: Test — Telemetria generation_events

**File:** `src/__tests__/telemetry.test.ts`

<read_first>
- `openspec/changes/fase-28-observabilidade-operacao-launch-controls/specs/pipeline-telemetry/spec.md` — 6 scenarios

</read_first>

<action>
Create telemetry tests:
1. Mock pipeline execution → verify INSERT with `campaign_copy` has all fields (generation_type, provider, model, prompt_tokens, completion_tokens, total_tokens, estimated_cost_usd, campaign_id, store_id, trace_id, phase, status)
2. INSERT without `campaign_id` → succeeds (nullable column)
3. Mock INSERT failure → pipeline continues, error logged to console.error, response sent normally

<acceptance_criteria>
- 3+ telemetry tests passing
- Each tests a different aspect (required fields, nullable, best-effort)
</acceptance_criteria>

---

### Task 3: Test — Regressão Master Switch + Emergency Brake

**File:** `src/__tests__/regression-master-switch.test.ts`

<read_first>
- `openspec/changes/fase-28-observabilidade-operacao-launch-controls/specs/transactional-pipeline/spec.md` — v15Enabled and generationPaused scenarios
- `src/lib/launch-config/config.ts` — getLaunchConfig()

</read_first>

<action>
Create regression tests:
1. Set `VENDEO_V15_ENABLED=false`, run pipeline → verify: no credit check (getBalance not called), no Copy Director (uses deterministic fallback), no rate limit check, generation succeeds
2. Set `VENDEO_GENERATION_PAUSED=true`, run pipeline → verify: HTTP 503 response before any DB or IA call, no logs emitted

<acceptance_criteria>
- Both regression tests pass
- v1.4 fallback works without credit or Copy Director dependency
- Emergency brake stops pipeline immediately at the handler entry
</acceptance_criteria>

---

### Task 4: Verificação Final

**No files to create.** Run verification suite.

<read_first>
- All implementation files from plans 28-01, 28-02, 28-03

</read_first>

<action>
Run verification commands:
1. `npx vitest run` — verify 28+ new tests + ~852 existing all pass
2. `npm run typecheck` — zero errors
3. `npm run lint` — zero errors
4. `npm run build` — successful build

Manual UAT verification:
5. Start dev server, set all flags to defaults (all active) → generate campaign → success
6. Set `VENDEO_GENERATION_PAUSED=true` → POST /api/campaign/generate-image → 503
7. Set `VENDEO_V15_ENABLED=false` → POST /api/campaign/generate-image → v1.4 behavior
8. Access `/admin/metrics` as admin → cards render with values, health state visible
9. Access `/admin/metrics` as non-admin → 403/redirect

<acceptance_criteria>
- `npx vitest run` → all tests passing
- `npm run typecheck` → zero errors
- `npm run lint` → zero errors
- `npm run build` → successful
- UAT: all 4 scenarios verified manually
</acceptance_criteria>

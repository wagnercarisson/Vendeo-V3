---
plan_id: 29-1-1-02
wave: 2
name: "Frontend — Modal UI + Ocultar Modal Antigo"
depends_on: [29-1-1-01]
files_modified:
  - src/components/flow/visual-signature-approval-modal.tsx
  - src/components/flow/store-visual-signature-section.tsx
autonomous: true
requirements: []
---

## 29-1-1-02: Frontend — Modal UI + Ocultar Modal Antigo

Atualizar o `VisualSignatureApprovalModal` para remover a cota fixa de 3 tentativas, adicionar tratamento de saldo insuficiente (402), e ocultar o `VisualSignatureModal` antigo.

### Tasks

#### Task 2.1: VisualSignatureApprovalModal — Remover fase exhausted

<read_first>
- src/components/flow/visual-signature-approval-modal.tsx
- openspec/changes/fase-29-1-1-creditos-assinatura-visual/specs/visual-signature-approval/spec.md
</read_first>

<action>

1. REMOVE the `"exhausted"` phase type from `ApprovalState` union type (line ~31)
2. REMOVE all render code related to `exhausted` phase (the 3-card re-evaluation grid, the "Limite de 3 versões atingido" message, the "Continuar sem logo" button)
3. REMOVE badge "Tentativa X/3" from the display phase UI
4. REMOVE guard `state.attempt >= 3` in the reject handler (if exists)
5. REMOVE guard `sigs.length >= 3 → exhausted` in the checking phase
6. Ensure display phase always shows "Não gostei, gerar outra versão" as active (no attempt limit check)
</action>

<acceptance_criteria>
- No `"exhausted"` phase in state type
- No "Limite de 3 versões atingido" text anywhere in the component
- No "Tentativa X/3" badge displayed
- "Não gostei, gerar outra versão" always active regardless of attempt count
- No 3-card re-evaluation grid on limit exhaustion
</acceptance_criteria>

#### Task 2.2: VisualSignatureApprovalModal — Adicionar fase insufficient_credits

<read_first>
- src/components/flow/visual-signature-approval-modal.tsx
- openspec/changes/fase-29-1-1-creditos-assinatura-visual/specs/visual-signature-approval/spec.md
</read_first>

<action>

1. Add `"insufficient_credits"` phase type to `ApprovalState` union
2. In the `generate()` function, add handler for `res.status === 402`:
   ```typescript
   if (res.status === 402) {
     setState({ phase: "insufficient_credits" });
     return;
   }
   ```
3. Add render block for `phase === "insufficient_credits"`:
   - AlertCircle icon from lucide-react with `text-amber-500` class
   - Message: "Créditos insuficientes para gerar assinatura visual."
   - Sub-message: "Cada geração de assinatura visual consome 1 crédito."
   - Primary CTA: "Ver meus créditos" — Link to `/conta` via `next/navigation` `useRouter().push()`
   - Secondary CTA: "Tentar novamente" — calls `generate()` again
</action>

<acceptance_criteria>
- When API returns 402, modal shows `insufficient_credits` phase (not error)
- Insufficient credits UI shows AlertCircle icon, message, and sub-message
- "Ver meus créditos" navigates to `/conta`
- "Tentar novamente" retries generation
- No counter "Tentativa X/3" visible
</acceptance_criteria>

#### Task 2.3: VisualSignatureApprovalModal — Atualizar checking phase para usar limit=6

<read_first>
- src/components/flow/visual-signature-approval-modal.tsx
- openspec/changes/fase-29-1-1-creditos-assinatura-visual/specs/visual-signature-approval/spec.md
</read_first>

<action>

1. Modify the checking phase fetch to use `GET /api/store/[id]/visual-signature?limit=6` instead of fetching all
2. Parse `total` from response alongside `signatures`
3. If `total > 6`, show non-clickable indicator: "Há mais versões no histórico. Galeria completa em breve."
4. If signatures exist → transition to `"review"` phase (not `"exhausted"`)
5. If no signatures and credits sufficient → proceed to generate
6. If no signatures and credits insufficient (402) → transition to `"insufficient_credits"`
</action>

<acceptance_criteria>
- Checking phase fetches `/api/store/{id}/visual-signature?limit=6`
- Response `total > 6` shows indicator text
- Never transitions to `"exhausted"` — always `"review"` if signatures exist
- Review phase lists up to 6 signatures
</acceptance_criteria>

#### Task 2.4: VisualSignatureApprovalModal — Adicionar "Ver versões anteriores" no display phase

<read_first>
- src/components/flow/visual-signature-approval-modal.tsx
- openspec/changes/fase-29-1-1-creditos-assinatura-visual/specs/visual-signature-approval/spec.md
</read_first>

<action>

In the display phase (after showing the generated signature, "Aprovar" and "Não gostei, gerar outra versão"), add a button "Ver versões anteriores" if `signatures.length > 1` (loaded during checking phase — store in component state). This button transitions the modal to the `"review"` phase.
</action>

<acceptance_criteria>
- "Ver versões anteriores" button visible in display phase when more than 1 signature exists
- Button transitions to review phase showing historical signatures
- Button not shown when only 1 signature exists
</acceptance_criteria>

#### Task 2.5: VisualSignatureApprovalModal — Rejection context propagation to review generate

<read_first>
- src/components/flow/visual-signature-approval-modal.tsx
- openspec/changes/fase-29-1-1-creditos-assinatura-visual/specs/visual-signature-approval/spec.md
</read_first>

<action>

Ensure `rejectionContext` is stored as component state (already exists as `storedRejectionContext`). When user clicks "Gerar nova versão" from the review phase, pass the stored `rejectionContext` to the `generate-without-logo` API call. This is already partially implemented — verify and fix if broken.
</action>

<acceptance_criteria>
- Rejection feedback provided in feedback phase is sent to generate API when clicking "Gerar nova versão" from review phase
- "sem feedback específico" context sent when no feedback provided
</acceptance_criteria>

#### Task 2.6: store-visual-signature-section — Ocultar VisualSignatureModal

<read_first>
- src/components/flow/store-visual-signature-section.tsx
- openspec/changes/fase-29-1-1-creditos-assinatura-visual/design.md
</read_first>

<action>

Remove or comment out the code path that opens `VisualSignatureModal` (the old modal with "Gerar 3 opções" and "Deixar Vendeo escolher"). The `showModal` state and its trigger should be removed. Only `VisualSignatureApprovalModal` should remain accessible. Remove the import of `VisualSignatureModal` if it's no longer referenced.
</action>

<acceptance_criteria>
- `VisualSignatureModal` is not rendered in the component
- No UI button opens the old modal
- `VisualSignatureApprovalModal` still works normally
- `VisualSignatureHistoryModal` still works normally
</acceptance_criteria>

### Verification Criteria

- `npm run typecheck` passes
- `npm run build` passes
- No exhausted/limit logic in modal
- insufficient_credits phase renders properly on 402
- Review loads max 6 signatures
- Modal never shows "Tentativa X/3"
- Old VisualSignatureModal not accessible from UI

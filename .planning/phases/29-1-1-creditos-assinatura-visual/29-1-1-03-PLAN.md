---
plan_id: 29-1-1-03
wave: 3
name: "Testes e Verificação"
depends_on: [29-1-1-01, 29-1-1-02]
files_modified:
  - src/lib/credit/__tests__/credit-service.test.ts
  - (novos arquivos de teste)
autonomous: true
requirements: []
---

## 29-1-1-03: Testes e Verificação

Criar testes para o fluxo de crédito na VS, limite removido, UI de 402, histórico/review, e rodar verificação final.

### Tasks

#### Task 3.1: Testes de fluxo de crédito na VS (8 testes)

<read_first>
- src/lib/credit/__tests__/credit-service.test.ts
- openspec/changes/fase-29-1-1-creditos-assinatura-visual/tasks.md
</read_first>

<action>

Create tests for the credit integration in visual signature generation. New test file or add to existing credit service tests:

1. **Saldo suficiente**: `POST /generate-without-logo` com saldo >= 1 → reserva feito, VS gerada, `credit_tx_id` no metadata
2. **Saldo zero**: `POST /generate-without-logo` com saldo = 0 → retorna 402, `{ code: "insufficient_credits" }`
3. **Falha de IA com estorno**: `reserveCredit` chamado, IA falha → `refundCredit` chamado, saldo restaurado
4. **Falha de storage com estorno**: `reserveCredit` chamado, storage fail → `refundCredit` chamado, retorna 503
5. **creditsChargingEnabled=false**: flag desligada → sem balance check, sem reserve, VS gerada sem crédito
6. **generationPaused=true**: flag ligada → retorna 503 antes de qualquer operação
7. **v15Enabled=false**: flag desligada → geração sem consumo de crédito
8. **Idempotência**: mesma `operationId` → segunda chamada com mesmo idempotencyKey não reserva novamente
</action>

<acceptance_criteria>
- 8 test cases passing
- Cada teste cobre um cenário específico
- Mocks para CreditService, launch config, Supabase conforme necessário
</acceptance_criteria>

#### Task 3.2: Testes de limite de 3 removido (5 testes)

<read_first>
- src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts
- src/app/api/store/[id]/visual-signature/reject/route.ts
- openspec/changes/fase-29-1-1-creditos-assinatura-visual/tasks.md
</read_first>

<action>

Test that the old 3-attempt limit is completely removed:

1. **5 gerações sequenciais sem bloqueio**: gerar VS 5 vezes seguidas — todas bem-sucedidas, sem exhausted
2. **Reject sempre success**: rejeitar VS independentemente de quantas já foram geradas — retorna success, nunca exhausted
3. **Modal abre em review**: após várias gerações, checking phase carrega signatures e mostra review (nunca exhausted)
4. **Ciclo infinito possível**: usuário pode gerar quantas VS quiser, limitado apenas por saldo (testar 10+ sem bloqueio)
5. **visual_signature_attempts não incrementado**: verificar que `store.visual_signature_attempts` não muda após geração
</action>

<acceptance_criteria>
- 5 test cases passing
- Nenhum teste falha por limite de tentativas
- `visual_signature_attempts` não é alterado por generate/reject/approve
</acceptance_criteria>

#### Task 3.3: Testes de UI de 402 (4 testes)

<read_first>
- src/components/flow/visual-signature-approval-modal.tsx
- openspec/changes/fase-29-1-1-creditos-assinatura-visual/tasks.md
</read_first>

<action>

Test the insufficient_credits UI behavior (component/react-testing-library or manual verification steps):

1. **Modal exibe insufficient_credits**: quando API retorna 402, modal mostra fase `insufficient_credits` com mensagem "Créditos insuficientes"
2. **CTA /conta**: "Ver meus créditos" navega para `/conta`
3. **"Tentar novamente"**: botão chama `generate()` novamente
4. **Sem contador**: nenhum "Tentativa X/3" é exibido na UI
</action>

<acceptance_criteria>
- 4 test cases passing (UI tests or documented manual steps)
- Mensagem "Créditos insuficientes para gerar assinatura visual." presente
- CTA "Ver meus créditos" → `/conta`
- CTA "Tentar novamente" → retry
</acceptance_criteria>

#### Task 3.4: Testes de histórico/review (3 testes)

<read_first>
- src/app/api/store/[id]/visual-signature/route.ts
- openspec/changes/fase-29-1-1-creditos-assinatura-visual/specs/store-visual-signature/spec.md
</read_first>

<action>

Test the limit/offset pagination and review behavior:

1. **Review limita a 6**: `GET /api/store/{id}/visual-signature?limit=6` retorna no máximo 6 signatures, `total` reflete contagem completa
2. **Reativar com drift bloqueado**: tentar reativar VS archive com drift — retorna 409 (teste de regressão, comportamento existente)
3. **Reativar sem drift aprovado**: VS archive sem drift — restore bem-sucedido (comportamento existente)
</action>

<acceptance_criteria>
- 3 test cases passing
- limit/offset funcionam corretamente
- Drift validation não foi afetado pelas mudanças
</acceptance_criteria>

#### Task 3.5: Verificação final

<read_first>
- package.json (scripts section)
</read_first>

<action>

Run final verification commands and fix any issues:
1. `npm run typecheck` — must pass
2. `npm run lint` — must pass on all modified files
3. `npx vitest run` — all tests passing (including existing + new)
4. `npm run build` — must pass
</action>

<acceptance_criteria>
- `npm run typecheck`: exit 0
- `npm run lint`: exit 0
- `npx vitest run`: all tests passing
- `npm run build`: exit 0
</acceptance_criteria>

### Verification Criteria

- All 20+ new tests passing
- TypeScript clean
- Lint clean
- Build clean
- All existing tests still pass (regression)

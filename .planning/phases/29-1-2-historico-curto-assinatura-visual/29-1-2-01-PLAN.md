---
plan_id: 29-1-2-01
wave: 1
name: "Backend Foundation — Approve Drift Fix + Spec Update"
depends_on: []
files_modified:
  - src/app/api/store/[id]/visual-signature/approve/route.ts
  - openspec/specs/visual-signature-restore/spec.md
autonomous: true
requirements: []
---

## 29-1-2-01: Backend Foundation

Corrigir a condição de drift validation no approve route para cobrir também `draft`, e atualizar o spec `visual-signature-restore` para refletir o bloqueio de `identity_state = 'visual_signature'`.

### Tasks

#### Task 1.1: Alterar condição de drift validation no approve route

<read_first>
- src/app/api/store/[id]/visual-signature/approve/route.ts
- openspec/changes/fase-29-1-2-historico-curto-assinatura-visual/specs/visual-signature-approval/spec.md
- openspec/changes/fase-29-1-2-historico-curto-assinatura-visual/design.md
</read_first>

<action>
In `approve/route.ts`, change the drift validation guard condition (approximately line 406):

BEFORE:
```
if (signature.status === 'archived') {
  const metadata = (signature.metadata ?? {}) as Record<string, unknown>;
  const inputSnapshot = metadata.input_snapshot as VisualSignatureMetadataInputSnapshot | null ?? null;
  ...
```

AFTER:
```
if (signature.status !== 'active') {
  const metadata = (signature.metadata ?? {}) as Record<string, unknown>;
  const inputSnapshot = metadata.input_snapshot as VisualSignatureMetadataInputSnapshot | null ?? null;
  ...
```

This ensures `draft` signatures also undergo drift validation before approval.
</action>

<acceptance_criteria>
- Draft signature without drift (snapshot matches current data) passes validation → 200
- Draft signature WITH drift returns HTTP 409 with drift error fields
- Draft signature without `input_snapshot` in metadata returns HTTP 409 with `missing_metadata`
- Active signature skips drift validation entirely (status !== 'active' is false)
- Archived signatures continue to be validated as before (regression)
- Approval flow immediately after generation (draft recém-criado) passes without blocking
</acceptance_criteria>

#### Task 1.2: Atualizar spec visual-signature-restore

<read_first>
- openspec/specs/visual-signature-restore/spec.md
- openspec/changes/fase-29-1-2-historico-curto-assinatura-visual/specs/visual-signature-restore/spec.md
- openspec/changes/fase-29-1-2-historico-curto-assinatura-visual/design.md
</read_first>

<action>
In `openspec/specs/visual-signature-restore/spec.md`, update the identity_state validation rule (approximately line 20):

BEFORE:
```
   - `'visual_signature'` → permitted (swap/reapply between archived signatures)
```

AFTER:
```
   - `'visual_signature'` → **REJECTED**: user must remove the active visual signature before applying another
     ```json
     { "error": "Remova a assinatura ativa antes de aplicar outra versão.",
       "requires_vs_removal": true, "current_identity_state": "visual_signature" }
     ```
```

Also add a REMOVED Requirements section at the end documenting the change:
- State that `identity_state = 'visual_signature'` was previously PERMITIDO and is now BLOQUEADO
- Reason: D3 in alinhamento F29.1.2 — users must remove active VS before applying another version
</action>

<acceptance_criteria>
- `identity_state = 'visual_signature'` in spec now shows REJECTED with `requires_vs_removal: true`
- `identity_state = 'text_only'` remains PERMITIDO
- `identity_state = 'logo'` remains REJECTED with `requires_logo_removal: true`
- REMOVED Requirements section documents the migration
</acceptance_criteria>

### Verification Criteria

- TypeScript: `npm run typecheck` passes
- Build: `npm run build` passes
- Lint: `npm run lint` passes on modified files
- Drift validation covers draft (regression: archived continues to work)
- Spec reflects new identity_state policy

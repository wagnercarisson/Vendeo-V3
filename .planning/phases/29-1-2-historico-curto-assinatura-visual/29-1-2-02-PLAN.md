---
plan_id: 29-1-2-02
wave: 1
name: "Frontend — HistoryModal Rewrite + ApprovalModal Bridge + Pagination"
depends_on: [29-1-2-01]
files_modified:
  - src/components/flow/visual-signature-history-modal.tsx
  - src/components/flow/visual-signature-approval-modal.tsx
  - src/components/flow/store-visual-signature-section.tsx
  - src/components/flow/store-identity-form.tsx
autonomous: true
requirements: []
---

## 29-1-2-02: Frontend — HistoryModal + ApprovalModal Bridge

Reescrever o `VisualSignatureHistoryModal` com paginação, filtro client-side e ações condicionais. Adicionar prop `onOpenGallery` no `ApprovalModal` e bridge nos dois parents.

### Tasks

#### Task 2.1: Atualizar interface Props do HistoryModal

<read_first>
- src/components/flow/visual-signature-history-modal.tsx
- openspec/changes/fase-29-1-2-historico-curto-assinatura-visual/specs/store-visual-signature/spec.md
</read_first>

<action>
Change the `VisualSignatureHistoryModalProps` interface:

BEFORE:
```typescript
interface VisualSignatureHistoryModalProps {
  isOpen: boolean;
  onClose: () => void;
  storeId: string;
  onRestore: () => void;
}
```

AFTER:
```typescript
interface VisualSignatureHistoryModalProps {
  isOpen: boolean;
  onClose: () => void;
  storeId: string;
  identityState: string | null;
  onApplied?: () => void;
}
```

Remove `onRestore` — replaces with `onApplied` (semântica de aplicar, não restaurar). Remove `RestoreEligibilityInfo` local type if duplicated; depend on server type instead.
</action>

<acceptance_criteria>
- HistoryModal aceita `identityState` prop
- HistoryModal aceita `onApplied` opcional (no lugar de `onRestore`)
- TypeScript compiles sem erro de prop mismatch nos parents
</acceptance_criteria>

#### Task 2.2: Reescrever carregamento com paginação + estados

<read_first>
- src/components/flow/visual-signature-history-modal.tsx
- openspec/changes/fase-29-1-2-historico-curto-assinatura-visual/specs/store-visual-signature/spec.md
</read_first>

<action>
Replace the existing `useEffect` fetch logic:

1. On mount, fetch `GET /api/store/[id]/visual-signature?limit=6&offset=0`
2. Track both raw signatures (unfiltered) and visible (filtered) signatures separately
3. Track `apiTotal` (total count from API response)
4. Handle loading, error, and empty states:
   - Loading: show spinner
   - Error: show error message
   - Empty (0 applicable signatures): show "Nenhuma assinatura anterior"
5. Store raw count loaded for pagination button logic

State shape:
```typescript
const [rawSignatures, setRawSignatures] = useState<HistorySignature[]>([]);
const [loading, setLoading] = useState(true);
const [error, setError] = useState<string | null>(null);
const [apiTotal, setApiTotal] = useState(0);
const [loadingMore, setLoadingMore] = useState(false);
```
</action>

<acceptance_criteria>
- API chamada com `?limit=6&offset=0` na abertura do modal
- Loading → spinner visível
- Erro na API → mensagem de erro descritiva
- 0 resultados aplicáveis → "Nenhuma assinatura anterior"
- `apiTotal` armazenado para lógica de paginação
</acceptance_criteria>

#### Task 2.3: Implementar filtro client-side

<read_first>
- src/components/flow/visual-signature-history-modal.tsx
- openspec/changes/fase-29-1-2-historico-curto-assinatura-visual/specs/store-visual-signature/spec.md
</read_first>

<action>
Compute visible signatures by filtering raw signatures:

```typescript
const visibleSignatures = useMemo(
  () => rawSignatures.filter(s => s.restore_eligibility?.reason === "ok"),
  [rawSignatures]
);
```

Only `visibleSignatures` appear in the grid. The count displayed is based on visible count (e.g., "4 assinaturas" or "6 de 12").
</action>

<acceptance_criteria>
- Signatures with `reason === "critical_drift"` não aparecem na lista
- Signatures com `reason === "missing_metadata"` não aparecem na lista
- Signatures com `reason === "ok"` aparecem na lista
- Contagem exibida reflete apenas as visíveis carregadas
</acceptance_criteria>

#### Task 2.4: Implementar grid 3 colunas com badge de status

<read_first>
- src/components/flow/visual-signature-history-modal.tsx
- src/components/flow/visual-signature-approval-modal.tsx (review phase grid for reference)
- openspec/changes/fase-29-1-2-historico-curto-assinatura-visual/specs/store-visual-signature/spec.md
</read_first>

<action>
Render visible signatures in a 3-column grid (same layout as the ApprovalModal review phase grid). Each card:

1. Preview image (`sig.assetUrl`)
2. Status badge:
   - `active` → "Ativa" (bg-accent-green text-white)
   - `archived` → "Arquivada" (bg-bg-hover text-text-secondary)
   - `draft` → "Rascunho" (bg-accent-amber text-white)
3. Action button or info indicator (see Task 2.5)

Use the same grid classes as the ApprovalModal review phase: `grid grid-cols-3 gap-3`.
</action>

<acceptance_criteria>
- Grid de 3 colunas com preview, badge e ação
- Badge "Ativa" para active, "Arquivada" para archived, "Rascunho" para draft
- Preview da VS visível no card
- Layout consistente com o review phase do ApprovalModal
</acceptance_criteria>

#### Task 2.5: Implementar ações condicionais ao identity_state

<read_first>
- src/components/flow/visual-signature-history-modal.tsx
- openspec/changes/fase-29-1-2-historico-curto-assinatura-visual/specs/store-visual-signature/spec.md
</read_first>

<action>
Implement conditional action logic using `identityState` prop:

```typescript
function canApply(identityState: string | null): boolean {
  return identityState === "text_only";
}

function getBlockedTooltip(identityState: string | null): string {
  switch (identityState) {
    case "visual_signature": return "Remova a assinatura ativa antes de aplicar outra versão";
    case "logo": return "Remova o logotipo ativo antes de aplicar uma assinatura visual";
    default: return "Aguarde o carregamento da identidade da loja";
  }
}
```

**If `canApply(identityState)` (text_only):**
- `archived` signatures: enabled "Aplicar" button
- `draft` signatures: enabled "Aplicar" button  
- `active` signatures: no action, only "Ativa" badge

**If NOT `canApply(identityState)` (visual_signature, logo, null):**
- `archived` and `draft`: disabled "Indisponível" button with tooltip from `getBlockedTooltip`
- `active`: no action, only "Ativa" badge

Style for "Aplicar": use `bg-accent-green text-white font-heading font-semibold text-xs rounded-lg`.
Style for "Indisponível" disabled: use `bg-bg-hover text-text-muted font-heading font-semibold text-xs rounded-lg cursor-not-allowed` with `title` attribute for tooltip.
</action>

<acceptance_criteria>
- `identityState === "text_only"`: "Aplicar" habilitado para archived e draft
- `identityState === "visual_signature"`: botão disabled com tooltip "Remova a assinatura ativa antes de aplicar outra versão"
- `identityState === "logo"`: botão disabled com tooltip "Remova o logotipo ativo antes de aplicar uma assinatura visual"
- `identityState === null`: botão disabled com tooltip "Aguarde o carregamento da identidade da loja"
- `active` signature: sem ação, apenas badge "Ativa" em todos os casos
</acceptance_criteria>

#### Task 2.6: Implementar handleApply via POST /approve

<read_first>
- src/components/flow/visual-signature-history-modal.tsx
- openspec/changes/fase-29-1-2-historico-curto-assinatura-visual/specs/store-visual-signature/spec.md
</read_first>

<action>
Implement `handleApply(signatureId)`:

1. Call `POST /api/store/[id]/visual-signature/approve` with body `{ signatureId }`
2. Do NOT call `reserveCredit` or any credit function
3. On success `{ success: true }`: call `onApplied()` if provided, close modal or show success
4. On drift error `{ error, drift: { critical: true } }`: display the error message from API
5. Track applying state per signature ID to show spinner on the button

```typescript
const [applyingId, setApplyingId] = useState<string | null>(null);
const [applyError, setApplyError] = useState<string | null>(null);
```
</action>

<acceptance_criteria>
- Click "Aplicar" em archived → POST /approve chamado
- Click "Aplicar" em draft → POST /approve chamado
- Nenhuma chamada a `reserveCredit`
- Sucesso → `onApplied()` chamado
- Erro de drift → mensagem de erro exibida
- Spinner no botão durante a requisição
</acceptance_criteria>

#### Task 2.7: Implementar paginação "Ver versões anteriores"

<read_first>
- src/components/flow/visual-signature-history-modal.tsx
- openspec/changes/fase-29-1-2-historico-curto-assinatura-visual/specs/store-visual-signature/spec.md
</read_first>

<action>
Implement pagination logic:

1. Track state: `rawSignaturesLoaded` (length of rawSignatures), `apiTotal` (from API), `hasLoadedSecondBatch` (boolean)
2. Show "Ver versões anteriores" button only when:
   - `apiTotal > rawSignaturesLoaded` AND
   - `hasLoadedSecondBatch === false`
3. On click:
   - Fetch `GET /api/store/[id]/visual-signature?limit=6&offset=6`
   - Append new raw signatures to existing `rawSignatures`
   - Set `hasLoadedSecondBatch = true`
   - Button disappears (max 12 raw loaded)
4. Display count: show applicable count, e.g., "4 assinaturas" or "6 de 12"

Style: botão com `w-full px-4 py-2.5 border border-border-light text-text-primary font-heading font-semibold text-sm rounded-lg hover:bg-bg-elevated transition-all duration-200`.
</action>

<acceptance_criteria>
- `apiTotal = 6`: sem botão (primeiro lote carregou tudo)
- `apiTotal = 7`: botão "Ver versões anteriores" visível
- Click no botão → carrega segundo lote (offset=6)
- Após carregar segundo lote → botão some permanentemente
- `apiTotal = 20`: máximo 12 raw carregados, botão some
- Segundo lote todo filtrado (ineligible): botão some sem adicionar itens visíveis
- Contagem exibida correta: "4 assinaturas" ou "6 de 12"
</acceptance_criteria>

#### Task 2.8: Adicionar prop onOpenGallery no ApprovalModal

<read_first>
- src/components/flow/visual-signature-approval-modal.tsx
- openspec/changes/fase-29-1-2-historico-curto-assinatura-visual/specs/visual-signature-approval/spec.md
</read_first>

<action>
1. Add optional prop to `VisualSignatureApprovalModalProps`:
```typescript
onOpenGallery?: () => void;
```
2. In the review phase (around line 547), modify the placeholder rendering:

BEFORE:
```tsx
{totalSignatures > 6 && (
  <p className="text-xs text-text-muted font-body text-center">
    Há mais versões no histórico. Galeria completa em breve.
  </p>
)}
```

AFTER:
```tsx
{totalSignatures > 6 && !onOpenGallery && (
  <p className="text-xs text-text-muted font-body text-center">
    Há mais versões no histórico. Galeria completa em breve.
  </p>
)}
{totalSignatures > 6 && onOpenGallery && (
  <button
    type="button"
    onClick={onOpenGallery}
    className="text-accent-blue hover:text-accent-blue/80 underline font-body transition-colors duration-200"
  >
    Ver versões recentes
  </button>
)}
```
</action>

<acceptance_criteria>
- `onOpenGallery` não passada → placeholder original "Galeria completa em breve" mantido (backward compat)
- `onOpenGallery` passada + total ≤ 6 → nem link nem placeholder
- `onOpenGallery` passada + total > 6 → link "Ver versões recentes" visível
- Click no link → `onOpenGallery()` chamado
- Link styled conforme spec: `text-accent-blue`, underline, `font-body`
</acceptance_criteria>

#### Task 2.9: Bridge no store-visual-signature-section.tsx

<read_first>
- src/components/flow/store-visual-signature-section.tsx
- openspec/changes/fase-29-1-2-historico-curto-assinatura-visual/design.md
</read_first>

<action>
1. Add callback `handleOpenGallery`:
```typescript
const handleOpenGallery = useCallback(() => {
  setShowApprovalModal(false);
  setShowHistoryModal(true);
}, []);
```

2. Pass `onOpenGallery={handleOpenGallery}` to `VisualSignatureApprovalModal`

3. Update `VisualSignatureHistoryModal` usage to pass new props:
```tsx
<VisualSignatureHistoryModal
  isOpen={showHistoryModal}
  onClose={() => setShowHistoryModal(false)}
  storeId={store.id}
  identityState={identityState}
  onApplied={handleHistoryRestore}
/>
```

4. Keep existing "Assinaturas anteriores" button (também abre HistoryModal diretamente)
</action>

<acceptance_criteria>
- `handleOpenGallery` fecha ApprovalModal e abre HistoryModal
- `onOpenGallery` passado ao ApprovalModal
- HistoryModal recebe `identityState` e `onApplied`
- Botão "Assinaturas anteriores" existente continua funcionando
</acceptance_criteria>

#### Task 2.10: Bridge no store-identity-form.tsx

<read_first>
- src/components/flow/store-identity-form.tsx
- openspec/changes/fase-29-1-2-historico-curto-assinatura-visual/design.md
</read_first>

<action>
1. Add import for `VisualSignatureHistoryModal`:
```typescript
import { VisualSignatureHistoryModal } from "./visual-signature-history-modal";
```

2. Add state:
```typescript
const [showHistoryModal, setShowHistoryModal] = useState(false);
```

3. Add callbacks:
```typescript
const handleOpenGallery = useCallback(() => {
  setShowApprovalModal(false);
  setShowHistoryModal(true);
}, []);

const handleHistoryApplied = useCallback(async () => {
  setShowHistoryModal(false);

  try {
    const res = await fetch(`/api/store/${storeId}`);
    if (!res.ok) return;

    const data = await res.json();
    setLogoStatus(data.logo_status ?? null);
    setIdentityState(data.identity_state ?? null);
    setVisualSignatureUrl(data.visual_signature_url ?? null);
    setLogoResultUrl(data.visual_signature_url ?? data.logo_url ?? null);
    setHasArchivedSignatures(data.has_archived_signatures ?? false);
    setDriftRefreshKey(k => k + 1);
  } catch {
    // refresh silencioso
  }
}, [storeId]);
```

Note: `handleHistoryApplied` é um callback **próprio**, independente de `handleApprovalComplete`. Usa `fetch(/api/store/${storeId})` — mesmo padrão já existente no componente — que retorna `logo_status`, `identity_state`, `visual_signature_url`, `logo_url`, `has_archived_signatures` via `buildStoreResponse`.

4. Pass `onOpenGallery={handleOpenGallery}` to `VisualSignatureApprovalModal`

5. Add render of `VisualSignatureHistoryModal` (near the existing ApprovalModal render):
```tsx
{showHistoryModal && storeId && (
  <VisualSignatureHistoryModal
    isOpen={showHistoryModal}
    onClose={() => setShowHistoryModal(false)}
    storeId={storeId}
    identityState={identityState}
    onApplied={handleHistoryApplied}
  />
)}
```

`identityState` já está disponível no state do componente — não precisa de ref ou computação extra.
</action>

<acceptance_criteria>
- `VisualSignatureHistoryModal` importado e renderizado condicionalmente
- `handleOpenGallery` fecha ApprovalModal e abre HistoryModal
- `onOpenGallery` passado ao ApprovalModal
- HistoryModal recebe `identityState` (state direto, sem ref) e `onApplied`
- `onApplied={handleHistoryApplied}` é callback próprio, independente — **não** chama `handleApprovalComplete`
- `handleHistoryApplied` fecha modal, chama `GET /api/store/${storeId}` e atualiza: `logoStatus`, `identityState`, `visualSignatureUrl`, `logoResultUrl`, `hasArchivedSignatures`, `driftRefreshKey`
- Erro no refresh é silencioso — o HistoryModal já confirmou sucesso no POST /approve
</acceptance_criteria>

### Verification Criteria

- TypeScript: `npm run typecheck` passes
- Build: `npm run build` passes
- Lint: `npm run lint` passes on modified files
- HistoryModal: loading, error, empty, grid, actions, pagination
- ApprovalModal: backward compat mantida quando onOpenGallery não passada
- Bridge funciona em ambos os parents (section + identity-form)

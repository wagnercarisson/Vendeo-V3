---
plan_id: 29-1-2-03
wave: 2
name: "Testes e Verificação — HistoryModal + Backend + Regressão"
depends_on: [29-1-2-01, 29-1-2-02]
files_modified:
  - src/components/flow/__tests__/visual-signature-history-modal.test.tsx
  - src/app/api/store/[id]/visual-signature/approve/__tests__/approve-route.test.ts
autonomous: true
requirements: []
---

## 29-1-2-03: Testes e Verificação

Testar filtro, paginação, ações condicionais, backend drift em draft, integração ApprovalModal, e regressão completa.

### Tasks

#### Task 3.1: Testar filtro e exibição do HistoryModal (5 testes)

<read_first>
- src/components/flow/__tests__/ (existing test patterns)
- openspec/changes/fase-29-1-2-historico-curto-assinatura-visual/tasks.md
</read_first>

<action>
Create `src/components/flow/__tests__/visual-signature-history-modal.test.tsx` with 5 tests:

1. API first batch returns 6 VS (4 ok, 2 critical_drift) → exibe 4 visíveis, 2 ocultos (critical_drift não aparecem)
2. API returns `total=8`, first batch returns 6 ok signatures, click "Ver versões anteriores" → second batch returns 2 ok signatures → exibe 8 no total
3. API returns 0 VS → empty state "Nenhuma assinatura anterior"
4. API returns error → error state
5. Loading → spinner visível

Mock `fetch` respecting the real `limit=6` contract. Test 2 must simulate two API calls: first with `offset=0` returns 6 items and `total=8`; second with `offset=6` returns the remaining 2.
</action>

<acceptance_criteria>
- 5 testes passando: filtro (4 visible + 2 hidden), paginação (6+2=8 via duas chamadas), empty, error, loading
</acceptance_criteria>

#### Task 3.2: Testar ações por identidade (6 testes)

<read_first>
- src/components/flow/__tests__/visual-signature-history-modal.test.tsx
- openspec/changes/fase-29-1-2-historico-curto-assinatura-visual/specs/store-visual-signature/spec.md
</read_first>

<action>
Add 6 tests to the same file:

1. `identity_state = visual_signature` → botão desabilitado com tooltip "Remova a assinatura ativa"
2. `identity_state = text_only` → botão "Aplicar" habilitado
3. Signature com `status === "active"` → sem ação, badge "Ativa"
4. Click "Aplicar" em archived → POST /approve chamado com signatureId
5. Click "Aplicar" em draft → POST /approve chamado com signatureId
6. POST /approve com draft sem drift → ativa com sucesso, `onApplied` chamado
</action>

<acceptance_criteria>
- 6 testes passando: bloqueios, habilitação, active badge, chamadas API, sucesso
</acceptance_criteria>

#### Task 3.3: Testar backend — revalidação de drift em draft (4 testes)

<read_first>
- src/app/api/store/[id]/visual-signature/approve/__tests__/approve-route.test.ts
- openspec/changes/fase-29-1-2-historico-curto-assinatura-visual/specs/visual-signature-approval/spec.md
</read_first>

<action>
Create or extend test file for the approve route with 4 tests:

1. Draft recém-gerado (snapshot = current data) → 200 success
2. Draft sem drift → 200 success
3. Draft COM drift (snapshot differs) → 409 critical_drift
4. Draft sem `input_snapshot` no metadata → 409 missing_metadata

Use existing route test infrastructure for mocking Supabase and drift validator.
</action>

<acceptance_criteria>
- 4 testes passando: sem drift, com drift, missing metadata, fluxo normal
- Archived signatures continuam sendo validadas (não quebrou regressão)
</acceptance_criteria>

#### Task 3.4: Testar paginação (4 testes)

<read_first>
- src/components/flow/__tests__/visual-signature-history-modal.test.tsx
- openspec/changes/fase-29-1-2-historico-curto-assinatura-visual/specs/store-visual-signature/spec.md
</read_first>

<action>
Add 4 pagination tests:

1. `total = 6` → sem botão "Ver versões anteriores"
2. `total = 7` → "Ver versões anteriores" visível
3. Click → carrega +6, total = 12, botão some
4. `total = 20` → botão some após 12 itens
</action>

<acceptance_criteria>
- 4 testes passando: `apiTotal=6` sem botão, `apiTotal=7` botão visível, click carrega lote, `apiTotal=20` máximo 12
</acceptance_criteria>

#### Task 3.5: Testar integração com ApprovalModal (3 testes)

<read_first>
- src/components/flow/__tests__/visual-signature-approval-modal.test.tsx (if exists, else src/components/flow/__tests__/)
- openspec/changes/fase-29-1-2-historico-curto-assinatura-visual/specs/visual-signature-approval/spec.md
</read_first>

<action>
Add 3 tests for the ApprovalModal `onOpenGallery` prop:

1. `onOpenGallery` não passada + total > 6 → placeholder original "Galeria completa em breve" visível
2. `onOpenGallery` passada + total > 6 → link "Ver versões recentes" visível
3. Click no link → `onOpenGallery` é chamado
</action>

<acceptance_criteria>
- 3 testes passando: backward compat, link exibido, callback chamado
</acceptance_criteria>

#### Task 3.6: Testes de regressão

<read_first>
- Project root
</read_first>

<action>
Run the full verification suite:

1. `npm run typecheck` — TypeScript clean
2. `npm run lint` — lint clean
3. `npx vitest run` — all existing tests still pass
4. `npm run build` — build clean
</action>

<acceptance_criteria>
- TypeScript clean
- Lint clean
- All existing tests passing (plus 22+ new)
- Build clean
</acceptance_criteria>

### Verification Criteria

- 22+ testes novos passando
- TypeScript: `npm run typecheck` passes
- Build: `npm run build` passes
- Lint: `npm run lint` passes
- Regressão completa sem quebras

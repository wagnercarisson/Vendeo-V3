---
plan_id: 29-3-04
wave: 4
name: "Testes e Verificação"
depends_on: [29-3-01, 29-3-03]
files_modified:
  - src/lib/credit/__tests__/credit-service.test.ts
  - src/lib/credit/__tests__/monthly-credits.test.ts
autonomous: true
requirements: [MONTHLY-01, MONTHLY-02, MONTHLY-03, MONTHLY-04, MONTHLY-05, MONTHLY-06, MONTHLY-08, MONTHLY-09]
---

## 29-3-04: Testes e Verificação

Testar ciclo completo de créditos mensais: concessão, teto, grant parcial, idempotência, concorrência, consumo prioritário, refund bucket-aware, CRON_SECRET e Launch Config skip.

### Tasks

#### Task 4.1: Teste de ciclo completo — loja velha recebe grant

<read_first>
- openspec/changes/fase-29-3-creditos-mensais-automaticos/specs/monthly-credits-engine/spec.md
- src/lib/credit/__tests__/credit-service.test.ts — existing test patterns
</read_first>

<action>
Create test file `src/lib/credit/__tests__/monthly-credits.test.ts`. Add test:

- Arrange: store with `created_at = NOW() - 45 days`, `bonus_balance = 0`, `last_monthly_grant_at = NULL`
- Act: call `grant_monthly_credits(5, 10, 30)`
- Assert: transaction `type = 'bonus_monthly'` created with `amount = 5`, `bonus_balance = 5`, `last_monthly_grant_at` updated
</action>

<acceptance_criteria>
- Test passes: eligible store receives `monthlyCreditsAmount` correctly
- Transaction type is `bonus_monthly`
- `bonus_balance` incremented by 5
- `last_monthly_grant_at` is set
</acceptance_criteria>

#### Task 4.2: Teste de teto — loja no cap não recebe grant

<read_first>
- openspec/changes/fase-29-3-creditos-mensais-automaticos/specs/monthly-credits-engine/spec.md
</read_first>

<action>
Add test:

- Arrange: store with `bonus_balance = 10` (at cap), `created_at >= 30d`, `last_monthly_grant_at = NULL`
- Act: call `grant_monthly_credits(5, 10, 30)`
- Assert: no transaction created, `bonus_balance` unchanged, `last_monthly_grant_at` NOT updated
</action>

<acceptance_criteria>
- Test passes: store at cap is skipped
- No new transaction created
- `last_monthly_grant_at` unchanged (null stays null)
</acceptance_criteria>

#### Task 4.3: Teste de grant parcial — loja próxima ao cap

<read_first>
- openspec/changes/fase-29-3-creditos-mensais-automaticos/specs/monthly-credits-engine/spec.md
</read_first>

<action>
Add test:

- Arrange: store with `bonus_balance = 8`, cap = 10, amount = 5
- Act: call `grant_monthly_credits(5, 10, 30)`
- Assert: grant = 2 (LEAST(5, 10-8)), `bonus_balance = 10`
</action>

<acceptance_criteria>
- Test passes: store 2 credits from cap receives exactly 2 credits (partial grant)
- `bonus_balance` = 10 after grant
</acceptance_criteria>

#### Task 4.4: Teste de idempotência — mesma execução duas vezes

<read_first>
- openspec/changes/fase-29-3-creditos-mensais-automaticos/specs/monthly-credits-engine/spec.md
</read_first>

<action>
Add test:

- Arrange: store eligible
- Act: call `grant_monthly_credits(5, 10, 30)` twice
- Assert: first call grants, second call returns same result but no duplicate transaction
</action>

<acceptance_criteria>
- Test passes: idempotency prevents duplication
- Only one `bonus_monthly` transaction created
- `bonus_balance` incremented only once
</acceptance_criteria>

#### Task 4.5: Teste de concorrência — SKIP LOCKED

<read_first>
- openspec/changes/fase-29-3-creditos-mensais-automaticos/specs/monthly-credits-engine/spec.md
</read_first>

<action>
Add test:

- Arrange: two concurrent calls to `grant_monthly_credits`
- Act: execute both simultaneously
- Assert: no deadlock, each store processed exactly once
</action>

<acceptance_criteria>
- Test passes: concurrent execution resolves correctly
- No duplicate grants from race conditions
</acceptance_criteria>

#### Task 4.6: Teste de consumo prioritário — reserve_credit com bônus + compra

<read_first>
- openspec/changes/fase-29-3-creditos-mensais-automaticos/specs/credit-sql-functions/spec.md
</read_first>

<action>
Add to monthly-credits.test.ts or credit-service.test.ts:

- Arrange: store with `bonus_balance = 5`, `purchased_balance = 10`
- Act: call `reserve_credit(store_id, 3)`
- Assert: `bonus_balance = 2`, `purchased_balance = 10`, metadata `bonus_amount: 3, purchased_amount: 0`

- Arrange: store with `bonus_balance = 5`, `purchased_balance = 10`
- Act: call `reserve_credit(store_id, 8)`
- Assert: `bonus_balance = 0`, `purchased_balance = 7`, metadata `bonus_amount: 5, purchased_amount: 3`
</action>

<acceptance_criteria>
- Test passes: small amount deducts entirely from bonus
- Test passes: large amount exhausts bonus first, then deducts from purchased
- Metadata accurately records bonus_amount and purchased_amount
</acceptance_criteria>

#### Task 4.7: Teste de refund bucket-aware — deduction com metadata

<read_first>
- openspec/changes/fase-29-3-creditos-mensais-automaticos/specs/credit-sql-functions/spec.md
</read_first>

<action>
Add test:

- Arrange: store with `bonus_balance = 5`, `purchased_balance = 3`. Create a deduction with metadata `{"bonus_amount": 5, "purchased_amount": 3}`. After deduction, `bonus_balance = 0`, `purchased_balance = 0`.
- Act: call `refund_credit(tx_id)`
- Assert: `bonus_balance = 5`, `purchased_balance = 3`, refund transaction with `amount = 8`
</action>

<acceptance_criteria>
- Test passes: `refund_credit` restores both buckets from metadata
- Refund transaction amount = 8
</acceptance_criteria>

#### Task 4.8: Teste de refund legacy — deduction sem metadata

<read_first>
- openspec/changes/fase-29-3-creditos-mensais-automaticos/specs/credit-sql-functions/spec.md
</read_first>

<action>
Add test:

- Arrange: store with a pre-F29.3 deduction (no bonus_amount/purchased_amount in metadata)
- Act: call `refund_credit(tx_id)`
- Assert: fallback treats `ABS(original.amount)` as `bonus_amount`, `purchased_balance` unchanged
</action>

<acceptance_criteria>
- Test passes: legacy deduction refund restores full amount to bonus_balance
- `purchased_balance` unchanged
</acceptance_criteria>

#### Task 4.9: Teste de CRON_SECRET — autenticação da rota cron

<read_first>
- openspec/changes/fase-29-3-creditos-mensais-automaticos/specs/monthly-credits-cron/spec.md
</read_first>

<action>
Add test for `GET /api/cron/monthly-credits`:

- No Authorization header → 401
- Invalid token → 401
- Valid CRON_SECRET → 200
</action>

<acceptance_criteria>
- All 3 test cases pass
- Valid token test verifies RPC was called
</acceptance_criteria>

#### Task 4.10: Teste de Launch Config — monthlyCreditsEnabled=false

<read_first>
- openspec/changes/fase-29-3-creditos-mensais-automaticos/specs/monthly-credits-cron/spec.md
</read_first>

<action>
Add test:

- Arrange: set `VENDEO_MONTHLY_CREDITS_ENABLED=false`
- Act: call `GET /api/cron/monthly-credits` with valid CRON_SECRET
- Assert: returns `{ skipped: true }`, RPC not called
</action>

<acceptance_criteria>
- Test passes: cron returns `{ skipped: true }` and does not execute RPC
</acceptance_criteria>

#### Task 4.11: Teste de migração reversa — rollback

<read_first>
- supabase/migrations/20260722000001_creditos_mensais_automaticos.sql
</read_first>

<action>
Verify REVERT commands in the migration file restore the previous state:

- Run REVERT section
- Verify `credit_balances` no longer has bonus_balance, purchased_balance, last_monthly_grant_at columns
- Verify `credit_transactions` CHECK constraints return to original (accept 'grant', reject new types)
- Verify old function signatures are restored
</action>

<acceptance_criteria>
- REVERT section documented in migration
- Rollback restores previous state correctly
</acceptance_criteria>

#### Task 4.12: Verificar TypeScript

<action>
Run `npm run typecheck` and fix any type errors.
</action>

<acceptance_criteria>
- `npm run typecheck` exits with code 0
- No type errors
</acceptance_criteria>

#### Task 4.13: Verificar lint

<action>
Run `npm run lint` and fix any lint errors.
</action>

<acceptance_criteria>
- `npm run lint` exits with code 0
- No lint errors
</acceptance_criteria>

### Must Haves

- [ ] Tests para ciclo completo de concessão mensal
- [ ] Tests para teto, grant parcial e idempotência
- [ ] Tests para concorrência (SKIP LOCKED)
- [ ] Tests para consumo prioritário (reserve_credit)
- [ ] Tests para refund bucket-aware (com metadata e legacy)
- [ ] Tests para CRON_SECRET authentication
- [ ] Tests para Launch Config skip
- [ ] Migration REVERT commands documentados
- [ ] `npm run typecheck` — zero erros
- [ ] `npm run lint` — zero erros
- [ ] `npx vitest run` — testes existentes + novos passando

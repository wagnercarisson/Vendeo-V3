---
status: complete
phase: 29-3-creditos-mensais-automaticos
source:
  - 01-SUMMARY.md
  - 02-SUMMARY.md
  - 03-SUMMARY.md
  - 04-SUMMARY.md
started: 2026-07-22T20:35:00.000Z
updated: 2026-07-22T20:41:00.000Z
---

## Current Test

[testing complete]

## Tests

### 1. Migration SQL — schema expansão e backfill
expected: Migration aplicada com sucesso. credit_balances tem bonus_balance, purchased_balance, last_monthly_grant_at. Dados existentes backfilados: saldo → bonus_balance, grant → bonus_onboarding/admin_grant.
result: pass

### 2. SQL Functions — grant_credits bucket-aware
expected: grant_credits com p_type incrementa bucket correto (bonus_balance para bonus_onboarding/bonus_monthly/admin_grant, purchased_balance para purchase). Idempotência mantida.
result: pass

### 3. SQL Functions — reserve_credit bucket-aware (bônus primeiro)
expected: reserve_credit deduz de bonus_balance primeiro, depois purchased_balance. Metadata registra bonus_amount e purchased_amount.
result: pass

### 4. SQL Functions — refund_credit bucket-aware
expected: refund_credit restaura cada bucket individualmente lendo metadata da deduction original. Fallback legacy restaura tudo em bonus_balance.
result: pass

### 5. CHECK Constraints — 7 tipos válidos
expected: credit_transactions aceita bonus_onboarding, bonus_monthly, admin_grant, purchase, deduction, refund, adjustment. Rejeita 'grant' antigo.
result: pass

### 6. sync_credit_balances_total trigger
expected: balance = bonus_balance + purchased_balance automaticamente em INSERT/UPDATE.
result: pass

### 7. grant_monthly_credits RPC
expected: Concede créditos com elegibilidade (>=30d), teto (cap), grant parcial, SKIP LOCKED, idempotência por ciclo. Retorna {eligible, granted, skipped, errors}.
result: pass

### 8. Launch Config — 4 flags mensais
expected: LaunchConfig tem monthlyCreditsEnabled, monthlyCreditsAmount, monthlyBonusCap, monthlyCreditsMinStoreAgeDays. getLaunchConfig() retorna em ambas as branches.
result: pass

### 9. Cron route — GET /api/cron/monthly-credits
expected: Protegido por CRON_SECRET. Retorna 401 sem auth, 200 com token válido, {skipped:true} se monthlyCreditsEnabled=false. logPipelineEvent chamado.
result: pass

### 10. Admin route — POST /api/admin/monthly-credits/grant
expected: Protegido por requireAdmin. 401 sem auth, 403 não-admin, 200 com resultado, {skipped:true} se desabilitado. logPipelineEvent com actor_id.
result: pass

### 11. vercel.json — cron schedule
expected: vercel.json criado com cron 0 6 * * * → /api/cron/monthly-credits. JSON válido.
result: pass

### 12. Admin button — MonthlyCreditGrantButton
expected: Botão "Executar concessão mensal" no admin dashboard com loading/result/error states.
result: pass

### 13. Middleware — rota cron bypass
expected: Middleware não redireciona /api/cron/monthly-credits. Rota funciona sem sessão. Teste adicionado.
result: pass

### 14. TypeScript e Lint
expected: npm run typecheck e npm run lint — zero erros.
result: pass

### 15. Testes — regressão completa
expected: 987 testes passando (119 files). Testes novos para cron auth, admin route e middleware.
result: pass

### 16. Documentação — STATE.md e ROADMAP.md
expected: STATE.md atualizado com seção da fase 29.3, 4 planos, 5 commits, 987 testes. ROADMAP.md com overview e detalhes.
result: pass

## Summary

total: 16
passed: 16
issues: 0
pending: 0
skipped: 0

## Gaps

[none]

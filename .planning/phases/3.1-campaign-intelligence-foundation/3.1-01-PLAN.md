---
phase: 3.1-campaign-intelligence-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/lib/campaign-intelligence/schema.ts
  - src/lib/campaign-intelligence/providers/types.ts
autonomous: true
requirements:
  - REQ-AI-SCHEMA
  - REQ-AI-PROVIDER

must_haves:
  truths:
    - "Zod is installed and importable in the project"
    - "CampaignGenerationInputSchema validates required fields, rejects missing fields"
    - "CampaignSpecSchema validates complete CampaignSpec objects"
    - "AIProvider interface exists with name + generate() contract"
    - "ProviderRawResponse type exists with raw string field"
    - "All types are inferred via z.infer, no hand-written parallel types"
  artifacts:
    - path: "package.json"
      provides: "Zod dependency added"
      contains: "\"zod\":"
    - path: "src/lib/campaign-intelligence/schema.ts"
      provides: "CampaignGenerationInputSchema, CampaignSpecSchema, ErrorResponseSchema, inferred types"
      exports:
        - "CampaignGenerationInputSchema"
        - "CampaignSpecSchema"
        - "ErrorResponseSchema"
        - "CampaignGenerationInput (type)"
        - "CampaignSpec (type)"
    - path: "src/lib/campaign-intelligence/providers/types.ts"
      provides: "AIProvider interface, ProviderRawResponse type"
      exports:
        - "AIProvider (interface)"
        - "ProviderRawResponse (type)"
  key_links:
    - from: "providers/types.ts"
      to: "schema.ts"
      via: "import { CampaignGenerationInput }"
      pattern: "import.*CampaignGenerationInput.*from.*schema"
---

<objective>
Install Zod, create campaign-intelligence directory structure, and define all I/O contracts (Zod schemas + provider interface) for the campaign generation pipeline.

Purpose: Establish the type-safe foundation that everything else builds on. Schemas are the contract between frontend input and AI provider output. The provider interface enables strategy-pattern swapping between MockProvider (required) and OpenAIProvider (future).
Output: src/lib/campaign-intelligence/schema.ts, src/lib/campaign-intelligence/providers/types.ts, zod in package.json
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/3.1-campaign-intelligence-foundation/3.1-CONTEXT.md
@.planning/phases/3.1-campaign-intelligence-foundation/3.1-RESEARCH.md
@.planning/STATE.md
@src/lib/constants.ts
@src/lib/store.ts
@src/app/api/store/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Zod and scaffold directory structure</name>
  <files>
    package.json
    package-lock.json
  </files>
  <read_first>
    - C:\Projetos\Vendeo V3\package.json — verify current deps, confirm zod absent
    - C:\Projetos\Vendeo V3\.planning\phases\3.1-campaign-intelligence-foundation\3.1-RESEARCH.md — Package Legitimacy Audit section (zod: approved, 6+ years, 50M+/week)
  </read_first>
  <action>
    Run `npm install zod` from project root. This adds zod ^3.x to package.json dependencies and package-lock.json. After install:
    1. Create the subdirectory structure: `src/lib/campaign-intelligence/providers/`
    2. Verify with `npm ls zod` that zod resolves to a v3 version. Do NOT install any other packages.
    3. Verify the directory exists with `Get-ChildItem -Path "src/lib/campaign-intelligence" -Recurse`
  </action>
  <verify>
    <automated>
      npm ls zod 2>&1
      if ($?) { Write-Output "zod installed" } else { Write-Output "FAILED"; exit 1 }
    </automated>
  </verify>
  <acceptance_criteria>
    - `npm ls zod` returns a version string matching ^3.x (e.g., 3.24.x)
    - `package.json` contains `"zod": "^3.` in dependencies
    - Directory `src/lib/campaign-intelligence/providers/` exists
  </acceptance_criteria>
  <done>Zod v3 installed and directory scaffold created</done>
</task>

<task type="auto">
  <name>Task 2: Create schema.ts with all Zod schemas and inferred types</name>
  <files>
    src/lib/campaign-intelligence/schema.ts
  </files>
  <read_first>
    - C:\Projetos\Vendeo V3\.planning\phases\3.1-campaign-intelligence-foundation\3.1-CONTEXT.md — Schemas section: field lists, z.infer, single-file rule
    - C:\Projetos\Vendeo V3\.planning\phases\3.1-campaign-intelligence-foundation\3.1-RESEARCH.md — Pattern 1 (Zod schema with inferred types), CampaignSpec Output Schema pattern, Pitfall 2 (state optional), Pitfall 4 (camelCase)
    - C:\Projetos\Vendeo V3\openspec\changes\phase-3-1-ai-campaign-intelligence\specs\ai-campaign-intelligence\spec.md — Field-by-field requirements, scenarios
    - C:\Projetos\Vendeo V3\src\lib\store.ts — resolveStoreIdentity confirms brandColor is resolved hex, not raw nullable
  </read_first>
  <action>
    Create `src/lib/campaign-intelligence/schema.ts` with three Zod schemas and their inferred types.

    A. **CampaignGenerationInputSchema** — z.object with these fields:
      - `productName`: z.string().min(1) — required
      - `originalPriceCents`: z.number().int().positive() — required
      - `discountedPriceCents`: z.number().int().positive() — required
      - `description`: z.string().optional() — optional
      - `badge`: z.string().optional() — optional
      - `storeName`: z.string().min(1) — required
      - `storeSegment`: z.string().min(1) — required
      - `brandColor`: z.string().regex(/^#[0-9a-fA-F]{6}$/) with error message "Must be a valid hex color" — required. Comment: "brandColor is the RESOLVED hex color after resolveStoreIdentity fallback, NOT raw store brand_color (which may be null)"
      - `city`: z.string().optional() — optional
      - `state`: z.string().length(2).optional() — optional, 2-letter code when provided. When omitted, key MUST be undefined (not null).

    B. **CampaignSpecSchema** — z.object with four nested z.object sections:
      1. `commercial_copy`: title (required), subtitle (required), hook (required), cta (required)
      2. `offer`: product_name (required), original_price_display (optional), discounted_price_display (required), badge_text (required)
      3. `visual_parameters`: layout_preset (required), composition_type (required), hierarchy_focus (required), palette_accent (required), badge_style (required), background_style (required)
      4. `generation_metadata`: provider (required), model (required), generated_at (required, z.string().datetime() with ISO 8601 message)

    C. **ErrorResponseSchema** — z.object with:
      - `error`: z.object({ message: z.string() })

    Exports:
    - All three schema constants (named exports)
    - `type CampaignGenerationInput = z.infer<typeof CampaignGenerationInputSchema>`
    - `type CampaignSpec = z.infer<typeof CampaignSpecSchema>`
    - DO NOT create hand-written interfaces or types — types MUST be derived via z.infer only.

    All error messages in field schemas should be in Portuguese (BR) to match domain convention.
  </action>
  <verify>
    <automated>
      cd "$PROJECT_ROOT"; npx tsc --noEmit --pretty 2>&1
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes with zero errors
    - `schema.ts` exports CampaignGenerationInputSchema, CampaignSpecSchema, ErrorResponseSchema, CampaignGenerationInput (type), CampaignSpec (type)
    - CampaignGenerationInputSchema infers `city` and `state` as optional (undefined, never null)
    - CampaignGenerationInputSchema accepts valid hex `brandColor` (uppercase and lowercase)
    - CampaignGenerationInputSchema rejects missing `productName`
    - CampaignSpecSchema requires `generation_metadata.generated_at` to be ISO 8601 datetime string
    - No hand-written interfaces or types exist alongside z.infer types in the file
  </acceptance_criteria>
  <done>schema.ts created with all Zod schemas and inferred types, passes tsc --noEmit</done>
</task>

<task type="auto">
  <name>Task 3: Create providers/types.ts with AIProvider interface and ProviderRawResponse</name>
  <files>
    src/lib/campaign-intelligence/providers/types.ts
  </files>
  <read_first>
    - C:\Projetos\Vendeo V3\.planning\phases\3.1-campaign-intelligence-foundation\3.1-CONTEXT.md — Provider Strategy section: AIProvider interface, interface-only no HTTP
    - C:\Projetos\Vendeo V3\.planning\phases\3.1-campaign-intelligence-foundation\3.1-RESEARCH.md — Pattern 4 (Abstract provider interface), Pattern 3 (ServiceResult)
    - C:\Projetos\Vendeo V3\src\lib\campaign-intelligence\schema.ts — CampaignGenerationInput type must exist (created in Task 2)
  </read_first>
  <action>
    Create `src/lib/campaign-intelligence/providers/types.ts` containing:

    1. Import `CampaignGenerationInput` from `../schema` (relative path, not @/ path)
    2. `AIProvider` interface with:
      - `readonly name: string` — provider identifier (e.g., "mock", "openai")
      - `generate(input: CampaignGenerationInput): Promise<ProviderRawResponse>` — single method
      - Comment: "Implementations MUST NOT access HTTP request/response objects. This is a pure data transformation interface."
    3. `ProviderRawResponse` type:
      - `{ raw: string }` — unvalidated output from AI provider

    Do NOT add ServiceResult or CampaignGenerationErrorCode here — those belong in service.ts.
    Do NOT add any Zod imports or schemas in this file — this is purely TypeScript types.
  </action>
  <verify>
    <automated>
      cd "$PROJECT_ROOT"; npx tsc --noEmit --pretty 2>&1
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes with zero errors
    - File exports `AIProvider` interface with `readonly name: string` and `generate(input: CampaignGenerationInput): Promise<ProviderRawResponse>`
    - File exports `ProviderRawResponse` type with `{ raw: string }`
    - Import path uses relative path (`../schema`), not `@/` alias
    - No Zod imports in this file
  </acceptance_criteria>
  <done>providers/types.ts created with AIProvider interface and ProviderRawResponse type</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Called code → Zod schema parse | Untrusted external input crosses from HTTP request body into schema validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3.1-01 | Tampering | CampaignGenerationInputSchema | mitigate | Zod safeParse validates all fields before any downstream processing; reject nonconforming input with 400 |
| T-3.1-02 | Spoofing | schema.ts type exports | mitigate | Types derived from schemas via z.infer; no hand-written types that could diverge from runtime validation |
| T-3.1-SC | Tampering | npm (zod install) | mitigate | zod is 6+ year established package, 50M+ weekly downloads, public source repo — slopcheck [ASSUMED] |
</threat_model>

<verification>
- [ ] `npm ls zod` returns v3.x
- [ ] `npx tsc --noEmit --pretty` passes with zero errors
- [ ] schema.ts exports all 3 schemas + 2 inferred types
- [ ] providers/types.ts exports AIProvider + ProviderRawResponse
- [ ] No hand-written types exist alongside z.infer
</verification>

<success_criteria>
1. Zod ^3.x is installed and importable
2. CampaignGenerationInputSchema and CampaignSpecSchema exist in schema.ts with correct fields
3. Types CampaignGenerationInput and CampaignSpec are inferred via z.infer
4. AIProvider interface and ProviderRawResponse type exist in providers/types.ts
5. `npx tsc --noEmit` passes with zero type errors
</success_criteria>

<output>
Create `.planning/phases/3.1-campaign-intelligence-foundation/3.1-01-SUMMARY.md` when done
</output>

---
phase: 3.1-campaign-intelligence-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 3.1-01
files_modified:
  - src/lib/campaign-intelligence/providers/mock.ts
  - src/lib/campaign-intelligence/service.ts
autonomous: true
requirements:
  - REQ-AI-PROVIDER
  - REQ-AI-SERVICE
  - REQ-AI-ERRORS

must_haves:
  truths:
    - "MockProvider returns deterministic, store-specific CampaignSpec-like JSON without external API calls"
    - "MockProvider output in Portuguese (BR)"
    - "CampaignIntelligenceService orchestrates input validation → provider call → output validation"
    - "Service returns validation_error (provider NOT called) on invalid input"
    - "Service returns provider_failure on provider exception with no raw error leaked"
    - "Service returns invalid_output on malformed provider JSON with no raw output leaked"
    - "Service returns success: CampaignSpec on valid input + valid provider output"
    - "generation_metadata.provider = 'mock' when MockProvider is active"
  artifacts:
    - path: "src/lib/campaign-intelligence/providers/mock.ts"
      provides: "MockProvider implementing AIProvider with deterministic PT-BR output"
      exports:
        - "MockProvider (class)"
    - path: "src/lib/campaign-intelligence/service.ts"
      provides: "CampaignIntelligenceService + factory + ServiceResult types"
      exports:
        - "CampaignIntelligenceService (class)"
        - "ServiceResult (type)"
        - "CampaignGenerationErrorCode (type)"
        - "createDefaultProvider (function)"
  key_links:
    - from: "providers/mock.ts"
      to: "providers/types.ts"
      via: "implements AIProvider"
      pattern: "implements AIProvider"
    - from: "providers/mock.ts"
      to: "schema.ts"
      via: "import { CampaignGenerationInput }"
      pattern: "import.*CampaignGenerationInput"
    - from: "service.ts"
      to: "providers/types.ts"
      via: "import { AIProvider, ProviderRawResponse }"
      pattern: "import.*AIProvider"
    - from: "service.ts"
      to: "schema.ts"
      via: "import { CampaignGenerationInputSchema, CampaignSpecSchema, CampaignSpec }"
      pattern: "import.*CampaignGenerationInputSchema"
---

<objective>
Implement the MockProvider (required default) and CampaignIntelligenceService (orchestration layer) that together form the core campaign generation pipeline.

Purpose: The MockProvider provides a fully functional, deterministic provider that works without any external API key — enabling end-to-end validation of the entire pipeline. The CampaignIntelligenceService orchestrates the three-step process (validate input → call provider → validate output) and enforces the error contract (validation_error, provider_failure, invalid_output) with zero raw data leakage.
Output: src/lib/campaign-intelligence/providers/mock.ts, src/lib/campaign-intelligence/service.ts
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/3.1-campaign-intelligence-foundation/3.1-CONTEXT.md
@.planning/phases/3.1-campaign-intelligence-foundation/3.1-RESEARCH.md
@.planning/phases/3.1-campaign-intelligence-foundation/3.1-01-SUMMARY.md
@src/lib/campaign-intelligence/schema.ts
@src/lib/campaign-intelligence/providers/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement MockProvider with deterministic, store-specific Portuguese output</name>
  <files>
    src/lib/campaign-intelligence/providers/mock.ts
  </files>
  <read_first>
    - C:\Projetos\Vendeo V3\src\lib\campaign-intelligence\providers\types.ts — AIProvider interface to implement, ProviderRawResponse to return
    - C:\Projetos\Vendeo V3\src\lib\campaign-intelligence\schema.ts — CampaignGenerationInput type for method signature
    - C:\Projetos\Vendeo V3\.planning\phases\3.1-campaign-intelligence-foundation\3.1-RESEARCH.md — Pattern 5 (Mock provider code example), Pitfall 3 (original_price_display optional), Pitfall 5 (generated_at determinism scope)
  </read_first>
  <action>
    Create `src/lib/campaign-intelligence/providers/mock.ts`:

    Import `AIProvider` and `ProviderRawResponse` from `./types` (relative path).
    Import `CampaignGenerationInput` from `../schema` (relative path).

    Implement class `MockProvider implements AIProvider`:
    - `readonly name = "mock"`
    - `generate(input: CampaignGenerationInput): Promise<ProviderRawResponse>` — synchronous but returns Promise (interface contract)

    The generate method constructs a JSON string deterministically from input fields:

    **commercial_copy** (all fields in Portuguese BR):
      - `title`: `${input.productName} — Oferta Imperdível na ${input.storeName}`
      - `subtitle`: `Aproveite o melhor preço em ${input.productName} na ${input.storeName}`
      - `hook`: Use input.description if present and non-empty, otherwise `"Corra! Últimas unidades com desconto especial."`
      - `cta`: `"Garanta o Seu!"`

    **offer**:
      - `product_name`: input.productName
      - `original_price_display`: Only include when `input.originalPriceCents > 0`. Format as `"De R$ X.XXX,XX"` (Brazilian decimal comma, thousands dot). Use `(input.originalPriceCents / 100).toLocaleString('pt-BR', { minimumFractionDigits: 2 })`. If originalPriceCents <= 0, set to undefined (omit key).
      - `discounted_price_display`: Always include. Format as `"R$ X.XXX,XX"` same pt-BR formatting.
      - `badge_text`: input.badge if present and non-empty, otherwise `"Oferta"`

    **visual_parameters**:
      - `layout_preset`: `"produto-oferta"` — fixed for this phase
      - `composition_type`: `"standard"`
      - `hierarchy_focus`: `"product-image"`
      - `palette_accent`: input.brandColor — always present because schema requires hex
      - `badge_style`: `"pill"`
      - `background_style`: `"solid-light"`

    **generation_metadata**:
      - `provider`: `"mock"` — fixed
      - `model`: `"mock-v1"` — fixed
      - `generated_at`: `new Date().toISOString()` — real timestamp (this field is excluded from determinism requirement per Pitfall 5)

    JSON.stringify the constructed object and return `{ raw }` as ProviderRawResponse.

    Determinism: All content fields (title, subtitle, hook, cta, prices, visual parameters) are purely derived from input — same input always produces same content. Only `generated_at` varies per call.

    No external imports beyond types and schema. No Math.random(), Date.now() in content fields.
  </action>
  <verify>
    <automated>
      cd "$PROJECT_ROOT"; npx tsc --noEmit --pretty 2>&1
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes with zero errors
    - MockProvider implements AIProvider interface with readonly name = "mock"
    - File exports MockProvider class
    - Import paths are relative (`./types`, `../schema`)
    - `generate()` returns Promise&lt;ProviderRawResponse&gt; with deterministic JSON string in raw field
    - With same input (storeName: "Padaria do João", productName: "Pão Francês"): title includes both values
    - With originalPriceCents = 0: original_price_display key is absent (undefined, not empty string)
    - With originalPriceCents = 1990: original_price_display = "De R$ 19,90"
    - discounted_price_display formatted as Brazilian currency (comma decimal)
    - All user-facing strings in Portuguese (BR)
    - generation_metadata.provider = "mock"
    - generation_metadata.generated_at is a valid ISO 8601 string
  </acceptance_criteria>
  <done>MockProvider created with deterministic, store-specific Portuguese output</done>
</task>

<task type="auto">
  <name>Task 2: Implement CampaignIntelligenceService with provider injection and error handling</name>
  <files>
    src/lib/campaign-intelligence/service.ts
  </files>
  <read_first>
    - C:\Projetos\Vendeo V3\src\lib\campaign-intelligence\schema.ts — CampaignGenerationInputSchema, CampaignSpecSchema, CampaignSpec type, CampaignGenerationInput type
    - C:\Projetos\Vendeo V3\src\lib\campaign-intelligence\providers\types.ts — AIProvider interface, ProviderRawResponse
    - C:\Projetos\Vendeo V3\.planning\phases\3.1-campaign-intelligence-foundation\3.1-RESEARCH.md — Pattern 3 (ServiceResult + error codes), Service Layer Pattern code example, Pattern 1 (safeParse, not parse)
    - C:\Projetos\Vendeo V3\.planning\phases\3.1-campaign-intelligence-foundation\3.1-CONTEXT.md — Error Handling section: three failure code paths, no raw output
  </read_first>
  <action>
    Create `src/lib/campaign-intelligence/service.ts` with four exports:

    **A. ServiceResult type** — discriminated union:
    ```typescript
    export type ServiceResult<T> =
      | { success: true; data: T }
      | { success: false; code: CampaignGenerationErrorCode; error: { message: string } };
    ```

    **B. CampaignGenerationErrorCode type** — union of three literal strings:
    ```typescript
    export type CampaignGenerationErrorCode = "validation_error" | "provider_failure" | "invalid_output";
    ```

    **C. CampaignIntelligenceService class** with:
      - Private readonly `provider: AIProvider` field
      - Constructor `constructor(provider: AIProvider)` — sets this.provider
      - `async generate(input: CampaignGenerationInput): Promise<ServiceResult<CampaignSpec>>`
        - **Step 1**: `CampaignGenerationInputSchema.safeParse(input)` — if invalid, return `{ success: false, code: "validation_error", error: { message: "Dados de entrada inválidos para geração de campanha" } }`. Provider is NOT called.
        - **Step 2**: `this.provider.generate(validatedInput)` wrapped in try/catch. On exception, `console.error("[CampaignIntelligenceService] Provider failure:", err)`, return `{ success: false, code: "provider_failure", error: { message: "Falha ao gerar campanha. Tente novamente." } }`. Raw error is logged server-side only — NEVER in the response object.
        - **Step 3**: `JSON.parse(rawResponse.raw)` wrapped in try/catch. On parse failure, `console.error("[CampaignIntelligenceService] Invalid provider JSON:", rawResponse.raw)`, return `{ success: false, code: "invalid_output", error: { message: "Resposta inválida do gerador de campanha" } }`. Raw string is logged server-side only.
        - **Step 4**: `CampaignSpecSchema.safeParse(parsedRaw)`. On validation failure, `console.error("[CampaignIntelligenceService] Invalid campaign spec:", validationError)`, return `{ success: false, code: "invalid_output", error: { message: "Campanha gerada não passou na validação" } }`.
        - **Step 5**: On all validations passing, return `{ success: true, data: parsedData }`.

    **D. createDefaultProvider function**:
    ```typescript
    import { MockProvider } from "./providers/mock";
    
    export function createDefaultProvider(): AIProvider {
      // Per locked decision D-05: explicit fallback — MockProvider is always used
      // when OPENAI_API_KEY is not set. OpenAI provider will be added in Phase 3.2
      // using dynamic import to avoid build failures when openai package is absent.
      return new MockProvider();
    }
    ```

    Import paths: use relative imports (`./providers/types`, `./schema`, `./providers/mock`).

    Do NOT import from `next/server` or any HTTP primitives. This service is HTTP-agnostic.
  </action>
  <verify>
    <automated>
      cd "$PROJECT_ROOT"; npx tsc --noEmit --pretty 2>&1
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes with zero errors
    - File exports: ServiceResult, CampaignGenerationErrorCode, CampaignIntelligenceService, createDefaultProvider
    - ServiceResult supports correct discriminated union typing
    - Error codes are the three literal strings: "validation_error", "provider_failure", "invalid_output"
    - generate() uses safeParse (NOT parse) for all validation
    - Provider.generate() is wrapped in try/catch — raw error never in response
    - JSON.parse(rawResponse.raw) is wrapped in try/catch — raw string never in response
    - CampaignSpecSchema validation uses safeParse
    - console.error logs raw data (for debugging) but response never includes it
    - No imports from "next/server" or any HTTP module
    - createDefaultProvider returns new MockProvider()
  </acceptance_criteria>
  <done>CampaignIntelligenceService created with full orchestration and error handling</done>
</task>

<task type="auto">
  <name>Task 3: Verify type correctness across all files</name>
  <files>
    src/lib/campaign-intelligence/providers/mock.ts
    src/lib/campaign-intelligence/service.ts
  </files>
  <read_first>
    - Both files created in Tasks 1 and 2 above
  </read_first>
  <action>
    Run TypeScript compiler to verify no type errors across the entire campaign-intelligence module:
    ```bash
    npx tsc --noEmit --pretty
    ```

    If any type errors occur, inspect the error output, fix the issues in mock.ts or service.ts, and re-run until zero errors.
    
    Specifically verify:
    - MockProvider correctly implements AIProvider interface (name is readonly string, generate returns Promise&lt;ProviderRawResponse&gt;)
    - CampaignIntelligenceService.generate() properly types all error paths
    - ServiceResult discriminated union narrows correctly in consumer usage
    - All relative imports resolve correctly
  </action>
  <verify>
    <automated>
      cd "$PROJECT_ROOT"; npx tsc --noEmit --pretty 2>&1
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits with code 0
    - No TypeScript errors in src/lib/campaign-intelligence/ or any other files
  </acceptance_criteria>
  <done>All type errors resolved across campaign-intelligence module</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service → Provider | Untrusted provider output crosses here; raw output parsed and validated before returning to caller |
| Provider → Service (exception) | Provider failure could leak internal paths in error messages — must be caught and sanitized |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3.1-03 | Information Disclosure | service.ts provider_failure path | mitigate | try/catch wraps provider.generate(); console.error logs raw error server-side; response uses sanitized message only |
| T-3.1-04 | Tampering | service.ts invalid_output path | mitigate | JSON.parse + CampaignSpecSchema.safeParse double-validate provider raw output; raw string logged, never returned |
| T-3.1-05 | Tampering | service.ts validation_error path | mitigate | Input validated against CampaignGenerationInputSchema before any provider call; invalid input caught early, provider never invoked |
| T-3.1-06 | Tampering | MockProvider determinism | mitigate | No Math.random() or Date.now() in content fields; only generated_at timestamp is real-time |
</threat_model>

<verification>
- [ ] `npx tsc --noEmit` passes with zero type errors
- [ ] MockProvider exports class implementing AIProvider
- [ ] CampaignIntelligenceService exports class with constructor(provider) + generate() method
- [ ] ServiceResult discriminated union works: `result.success === true` narrows to `result.data`
- [ ] createDefaultProvider returns MockProvider instance
- [ ] Error codes are the three literal strings
</verification>

<success_criteria>
1. MockProvider returns deterministic, store-specific output with store name in copy and Brazilian price formatting
2. Service orchestrates: validate input → call provider → validate output with proper error propagation
3. Three error code paths exist: validation_error, provider_failure, invalid_output
4. Raw provider data is logged server-side but NEVER in response objects
5. `npx tsc --noEmit` passes with zero type errors
6. All user-facing strings in Portuguese (BR)
</success_criteria>

<output>
Create `.planning/phases/3.1-campaign-intelligence-foundation/3.1-02-SUMMARY.md` when done
</output>

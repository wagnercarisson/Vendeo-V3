---
phase: 3.1
plan: 3.1-03
subsystem: api-route
tags: ["api", "route", "campaign", "error-handling"]
key-files:
  - src/app/api/campaign/generate/route.ts
metrics:
  tasks: 3/3
  e2e-tests: 5/5
  tsc-errors: 0
---

## Summary

Created the POST /api/campaign/generate Next.js App Router endpoint that wires the CampaignIntelligenceService to HTTP.

### Implementation

- POST handler at `src/app/api/campaign/generate/route.ts`
- JSON body parsing with malformed-body → 400
- Zod `safeParse` input validation → 400 with field-level details in Portuguese
- Provider selection via `createDefaultProvider()` (returns MockProvider)
- `CampaignIntelligenceService.generate()` → 200 with CampaignSpec
- Error mapping: `provider_failure` → 502, `invalid_output` → 500, `validation_error` → 400
- All error messages in Portuguese (BR), no raw provider data leaks

### E2E Test Results

| Test | Status |
|------|--------|
| Valid input → 200 with CampaignSpec | ✅ |
| Missing fields → 400 with field-level details | ✅ |
| Malformed JSON → 400 | ✅ |
| Invalid price type → 400 | ✅ |
| No raw provider data in error responses | ✅ |

### Deviations

None

### Self-Check

PASSED

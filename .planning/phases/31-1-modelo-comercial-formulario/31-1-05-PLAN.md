---
id: 31-1-05
wave: 4
depends_on: [31-1-01, 31-1-02, 31-1-03, 31-1-04]
file_types:
  - .test.ts
  - .ts
files_modified:
  - src/components/flow/__tests__/intent-inference.test.ts
  - src/components/flow/__tests__/intent-validation.test.ts
  - src/components/flow/__tests__/intent-badge-preserve.test.ts
  - src/__tests__/api/campaign-intent-guard.test.ts
autonomous: true
requirements: [INTENT-01, INTENT-02, INTENT-03, INTENT-04, INTENT-05, INTENT-06, INTENT-07, INTENT-08, INTENT-09, INTENT-10, INTENT-11, INTENT-12]
must_haves:
  truths:
    - "19 testes de intent implementados e passando"
    - "Regressão completa — 1018+ testes existentes continuam passando"
    - "TypeScript limpo — zero erros"
    - "Lint limpo — zero erros"
    - "Build bem-sucedido"
  artifacts:
    - path: "src/components/flow/__tests__/intent-inference.test.ts"
      provides: "Testes de inferência de intent (4 cenários)"
      contains: "INTENT-TEST-01"
    - path: "src/components/flow/__tests__/intent-validation.test.ts"
      provides: "Testes de validação condicional (4 cenários)"
      contains: "INTENT-TEST-05"
    - path: "src/components/flow/__tests__/intent-badge-preserve.test.ts"
      provides: "Testes de badge condicional + preserveImageContext (5 cenários)"
      contains: "INTENT-TEST-09"
    - path: "src/__tests__/api/campaign-intent-guard.test.ts"
      provides: "Testes de schema + pipeline guard (6 cenários)"
      contains: "INTENT-TEST-14"
  key_links:
    - from: "src/components/flow/__tests__/intent-inference.test.ts"
      to: "src/components/flow/use-campaign-form.ts"
      via: "importa inferIntent do módulo"
    - from: "src/__tests__/api/campaign-intent-guard.test.ts"
      to: "src/app/api/campaign/generate-image/route.ts"
      via: "testa o guard de intent no pré-stream"
---

# Plan 31-1-05: Tests — Inferência, Validação, Badge, Schema, Pipeline Guard

## Objective

Implementar todos os 19 testes de intent (INTENT-TEST-01 a 19) e verificação final (typecheck, lint, build). Garantir que a regressão completa (1018+ testes existentes) continua passando.

## Tasks

### Task 1: Testes de inferência + validação (INTENT-TEST-01 a 10)

<read_first>
- src/components/flow/use-campaign-form.ts (inferIntent, validateDiscountedPrice, validateBadge)
- src/components/flow/__tests__/use-campaign-form-navigation.test.ts (padrão de teste do hook)
- src/components/flow/__tests__/use-campaign-form-submit-error.test.ts (padrão de teste de validação)
- openspec/changes/fase-31-1-modelo-comercial-formulario/specs/campaign-form-intent/spec.md
</read_first>

<action>
Criar `src/components/flow/__tests__/intent-inference.test.ts`:

```typescript
// @vitest-environment jsdom
import { describe, it, expect } from "vitest";

// inferIntent será importada de use-campaign-form — se não for exportada, 
// criar export da função no módulo mae para testabilidade
```

**Testes (INTENT-TEST-01 a 04):**
1. `inferIntent` com DE+POR → "offer" — `inferIntent(10000, 5000)` retorna "offer"
2. `inferIntent` com só preço → "spotlight" — `inferIntent(0, 5000)` retorna "spotlight"
3. `inferIntent` sem preço (undefined/null) → "exclusive" — `inferIntent(0, undefined)` retorna "exclusive" e `inferIntent(0, null)` retorna "exclusive"
4. `inferIntent` com ambos preços zerados → "exclusive" — `inferIntent(0, 0)` retorna "exclusive"

**Necessário:** `inferIntent` já deve estar exportada como named export pelo Plan 31-1-02 Task 2 (`export function inferIntent`). Apenas confirmar que o export existe; não adicionar segundo export.

Criar `src/components/flow/__tests__/intent-validation.test.ts`:

**Testes (INTENT-TEST-05 a 08):**
5. DE+POR com intent spotlight → bloqueado no submit — testar via `handleSubmit` ou via `isValid`
6. Preço original sem preço com desconto → erro (quando intent=offer) — `validateDiscountedPrice(0, { campaignIntent: "offer" })` retorna erro
7. intent=offer sem badge → erro de validação — `validateBadge("", { campaignIntent: "offer" })` retorna erro
8. intent=spotlight sem badge → sem erro de validação — `validateBadge("", { campaignIntent: "spotlight" })` retorna null

**Necessário:** Exportar `validateDiscountedPrice` e `validateBadge` de `use-campaign-form.ts` se não estiverem exportados.

**Adicionar exports em `use-campaign-form.ts`:**
No final do arquivo, adicionar (apenas se ainda não exportados):
```typescript
export { validateDiscountedPrice, validateBadge };
```
Nota: `inferIntent` já é exportada via `export function inferIntent` no Plan 31-1-02 Task 2.
</action>

<acceptance_criteria>
- INTENT-TEST-01 passing: `inferIntent(10000, 5000)` → `"offer"`
- INTENT-TEST-02 passing: `inferIntent(0, 5000)` → `"spotlight"`
- INTENT-TEST-03 passing: `inferIntent(0, undefined)` → `"exclusive"`, `inferIntent(0, null)` → `"exclusive"`
- INTENT-TEST-04 passing: `inferIntent(0, 0)` → `"exclusive"`
- INTENT-TEST-05 passing: submit bloqueado para spotlight com DE+POR
- INTENT-TEST-06 passing: `validateDiscountedPrice(0, { campaignIntent: "offer" })` retorna erro
- INTENT-TEST-07 passing: `validateBadge("", { campaignIntent: "offer" })` retorna erro
- INTENT-TEST-08 passing: `validateBadge("", { campaignIntent: "spotlight" })` retorna null
- `npx vitest run` para os novos testes passa
</acceptance_criteria>

### Task 2: Testes de badge condicional + preserveImageContext + schema + pipeline guard (INTENT-TEST-09 a 19)

<read_first>
- src/components/flow/__tests__/use-campaign-form-navigation.test.ts (padrão de mock do hook)
- src/lib/campaign-intelligence/schema.ts (CampaignGenerationInputSchema)
- src/lib/image-generation/schema.ts (GenerateImageRequestSchema)
- src/app/api/campaign/generate-image/route.ts (pipeline guard)
- src/__tests__/api/campaign-generate.test.ts (padrão de teste de API route)
- openspec/changes/fase-31-1-modelo-comercial-formulario/specs/badge-options-by-intent/spec.md
- openspec/changes/fase-31-1-modelo-comercial-formulario/specs/ai-campaign-intelligence/spec.md
- openspec/changes/fase-31-1-modelo-comercial-formulario/specs/ai-image-generation/spec.md
- openspec/changes/fase-31-1-modelo-comercial-formulario/specs/transactional-pipeline/spec.md
</read_first>

<action>
1. **Criar `src/components/flow/__tests__/intent-badge-preserve.test.ts`** (INTENT-TEST-09 a 13):

   Testes de badge condicional (usando `renderHook` com `useCampaignForm` ou testando funções diretamente):
   9. Trocar intent de offer para spotlight limpa badge inválido — simular mudança de `campaignIntent` e verificar badge é resetado
   10. Trocar intent de spotlight para exclusive limpa badge incompatível — badge "Destaque da Semana" não pertence à lista de exclusive

   Testes de preserveImageContext:
   11. `preserveImageContext` invisível em offer — checkbox não renderizado quando `campaignIntent === "offer"`
   12. `preserveImageContext` visível em spotlight — checkbox renderizado quando `campaignIntent === "spotlight"`
   13. `preserveImageContext` reset ao voltar para offer — mudar de spotlight para offer reseta `preserveImageContext` para `false`

2. **Criar `src/__tests__/api/campaign-intent-guard.test.ts`** (INTENT-TEST-14 a 19):

   Testes de schema:
   14. `GenerateImageRequestSchema` aceita `campaignIntent` e `preserveImageContext` opcionais
   15. `GenerateImageRequestSchema` mantém `discountedPriceCents` required
   16. `CampaignGenerationInputSchema` default `campaignIntent` = "offer"
   (17-18-19 cobertos pelo teste de pipeline guard)

   Testes de pipeline guard (seguir padrão de `campaign-generate.test.ts`):
   17. Request com `campaignIntent: "spotlight"` → HTTP 400 antes de criar campanha
   18. Request com `campaignIntent: "exclusive"` → HTTP 400 antes de criar campanha
   19. Request sem `campaignIntent` → pipeline prossegue (default offer)

   **Padrão para testes de API route:** Usar `vi.mock` para mockar dependências pesadas (supabase, IA providers) e testar via `POST` handler diretamente ou via simulação de request.
</action>

<acceptance_criteria>
- INTENT-TEST-09 passing: trocar de offer para spotlight limpa badge
- INTENT-TEST-10 passing: trocar de spotlight para exclusive limpa badge
- INTENT-TEST-11 passing: preserveImageContext checkbox não visível em offer
- INTENT-TEST-12 passing: preserveImageContext checkbox visível em spotlight
- INTENT-TEST-13 passing: preserveImageContext reset ao voltar para offer
- INTENT-TEST-14 passing: GenerateImageRequestSchema aceita campaignIntent e preserveImageContext
- INTENT-TEST-15 passing: GenerateImageRequestSchema mantém discountedPriceCents required
- INTENT-TEST-16 passing: CampaignGenerationInputSchema default "offer"
- INTENT-TEST-17 passing: campaignIntent "spotlight" → HTTP 400
- INTENT-TEST-18 passing: campaignIntent "exclusive" → HTTP 400
- INTENT-TEST-19 passing: sem campaignIntent → pipeline prossegue
- `npx vitest run` para todos os novos testes passa
</acceptance_criteria>

### Task 3: Verificação final — typecheck + lint + build + regressão

<read_first>
- Nenhum — verificação global
</read_first>

<action>
Executar a bateria completa de verificação:

1. `npx vitest run` — todos os 19 novos testes + 1018+ testes existentes passando
2. `npm run typecheck` — zero erros de tipo
3. `npm run lint` — zero erros de lint
4. `npm run build` — build bem-sucedido

Se algum teste existente quebrar:
- Verificar se a mudança nos tipos (`CampaignFormFields`, `InputSnapshot`, `GenerateImageRequest`) causou erro de tipo em arquivos não modificados
- Verificar se a mudança em `handleSubmit` afetou testes de submit existentes
- Verificar se a mudança em `EMPTY_FIELDS` afetou testes de estado inicial
- Verificar se a mudança em `isValid` afetou testes de validação
- Corrigir qualquer regressão antes de considerar a task completa
</action>

<acceptance_criteria>
- `npx vitest run` — 1037+ testes passando (1018 existentes + 19 novos)
- `npm run typecheck` — zero erros
- `npm run lint` — zero erros
- `npm run build` — build bem-sucedido
</acceptance_criteria>

## Verification

- [ ] `npx vitest run` — pelo menos 1037 testes passando (1018 + 19)
- [ ] `npm run typecheck` — zero erros
- [ ] `npm run lint` — zero erros
- [ ] `npm run build` — build bem-sucedido
- [ ] Nenhum teste existente quebrado pela introdução dos novos campos

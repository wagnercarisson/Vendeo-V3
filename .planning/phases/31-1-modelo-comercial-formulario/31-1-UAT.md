---
status: complete
phase: 31-1-modelo-comercial-formulario
source: 01-SUMMARY.md, 02-SUMMARY.md, 03-SUMMARY.md, 04-SUMMARY.md, 05-SUMMARY.md
started: 2026-07-24T19:40:00-03:00
updated: 2026-07-24T19:50:00-03:00
---

## Current Test

[testing complete]

## Tests

### 1. CampaignIntent Type
expected: `"offer" | "spotlight" | "exclusive"` type defined and exported from `src/lib/campaign/types.ts`
result: pass

### 2. InputSnapshot Extended
expected: InputSnapshot includes `campaignIntent?` and `preserveImageContext?` fields
result: pass

### 3. Schema — GenerateImageRequestSchema with campaignIntent
expected: Schema has `campaignIntent` (optional, default "offer") and `preserveImageContext` (optional)
result: pass

### 4. Schema — CampaignGenerationInputSchema with campaignIntent
expected: Schema has `campaignIntent` (optional, default "offer")
result: pass

### 5. BADGE_OPTIONS_BY_INTENT
expected: Separate badge lists per intent: offer (5), spotlight (5), exclusive (4); BADGE_OPTIONS stays backward-compatible
result: pass

### 6. inferIntent Function
expected: DE+POR → offer, só preço (discounted) → spotlight, nenhum preço → exclusive; exported as pure function
result: pass

### 7. CampaignFormFields — discountedPriceCents Optional
expected: `discountedPriceCents` is `number | undefined`; `campaignIntent` and `preserveImageContext` added
result: pass

### 8. Conditional Validation — Discounted Price
expected: `validateDiscountedPrice` returns error only when `campaignIntent === "offer"` and value missing/zero; returns null for spotlight/exclusive
result: pass

### 9. Conditional Validation — Badge
expected: `validateBadge` requires badge for offer; allows empty for spotlight/exclusive; validates against BADGE_OPTIONS_BY_INTENT
result: pass

### 10. handleSubmit Blocks Non-Offer
expected: `handleSubmit` aborts with "Disponível em breve" when `campaignIntent !== "offer"`; no API call made
result: pass

### 11. Badge Cleanup on Intent Change
expected: When user switches intent, badge resets to empty if current badge not in new intent's list; preserveImageContext resets when switching to offer
result: pass

### 12. IntentSelector UI Component
expected: Vertical radio group with PT-BR labels (Oferta/Destaque/Exclusivo); spotlight/exclusive show "Em breve" badge; options dynamically filtered by price
result: pass

### 13. PreserveImageContext Checkbox
expected: Rendered only when `campaignIntent !== "offer"`; hidden for offer
result: pass

### 14. Submit Button — Non-Offer
expected: Disabled with "Disponível em breve" text and tooltip for spotlight/exclusive
result: pass

### 15. Pipeline Guard — Pre-Stream
expected: HTTP 400 with message "Intenção comercial indisponível. Apenas ofertas podem ser geradas no momento." when campaignIntent !== "offer"; positioned after legal clearance, before rate-limit
result: pass

### 16. InputSnapshot Normalization
expected: inputSnapshot includes `campaignIntent` and `preserveImageContext`; `preserveImageContext` normalized to `false` when intent is "offer"
result: pass

### 17. No Database Migration
expected: campaignIntent and preserveImageContext transported only in inputSnapshot JSONB; no new columns or tables
result: pass

### 18. Test Count and Regression
expected: 20+ intent-specific tests (inference, validation, badge/preserve, schema, pipeline guard); full regression 1036 total; typecheck, lint, build clean
result: pass

### 19. Non-Goals Respected
expected: No prompt modifications, no active submission of spotlight/exclusive, no CampaignSpecSchema changes
result: pass

### 20. OpenSpec Artifacts Present
expected: proposal.md, design.md, tasks.md, specs/ (8 files), .openspec.yaml all present and consistent with implementation
result: pass

## Summary

total: 20
passed: 20
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

(none — all checks passed)

## Issues Found During Review

### Documentation Divergence (Corrected)

- **Issue:** `.planning/ROADMAP.md` lines 340-351 — INTENT-01 through INTENT-12 were marked as `◆ Planned` instead of `Done ✓`
- **Fix applied:** Updated all 12 requirements to `Done ✓`; updated coverage count from 107→119; updated last_updated line
- **Commit:** Applied inline during UAT verification

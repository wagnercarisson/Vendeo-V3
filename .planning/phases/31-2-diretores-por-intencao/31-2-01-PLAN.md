---
id: 31-2-01
wave: 1
depends_on: []
file_types:
  - .ts
files_modified:
  - src/lib/image-generation/schema.ts
  - src/lib/campaign-intelligence/schema.ts
  - src/lib/campaign/types.ts
  - src/lib/campaign-intelligence/providers/mock.ts
autonomous: true
requirements: [SCHEMA-01, SCHEMA-02, SCHEMA-03]
must_haves:
  truths:
    - "GenerateImageRequestSchema aceita discountedPriceCents como opcional"
    - "CampaignGenerationInputSchema aceita discountedPriceCents como opcional"
    - "CampaignSpecSchema.offer.discounted_price_display é z.string().nullable()"
    - "CampaignSpecSchema.offer.badge_text é z.string().nullable()"
    - "InputSnapshot.discountedPriceCents é opcional (number | undefined)"
    - "MockProvider aceita discountedPriceCents ausente e gera sem preço"
    - "CampaignSpecSchema mantém product_name como z.string().min(1) obrigatório"
    - "zero regressão para offer com discountedPriceCents presente"
  artifacts:
    - path: "src/lib/image-generation/schema.ts"
      provides: "GenerateImageRequestSchema com discountedPriceCents opcional"
      min_lines: 12
      contains: "discountedPriceCents.*optional"
    - path: "src/lib/campaign-intelligence/schema.ts"
      provides: "CampaignGenerationInputSchema com discountedPriceCents opcional + CampaignSpecSchema nullable"
      contains: "nullable"
    - path: "src/lib/campaign/types.ts"
      provides: "InputSnapshot com discountedPriceCents opcional"
      contains: "discountedPriceCents?:"
    - path: "src/lib/campaign-intelligence/providers/mock.ts"
      provides: "MockProvider adaptado para discountedPriceCents opcional"
      min_lines: 131
  key_links:
    - from: "src/lib/image-generation/schema.ts"
      to: "src/lib/campaign-intelligence/schema.ts"
      via: "Ambos os schemas tornam discountedPriceCents opcional em paralelo"
    - from: "src/lib/campaign/types.ts"
      to: "src/lib/image-generation/schema.ts"
      via: "InputSnapshot reflete o schema de geração (discountedPriceCents opcional)"
---

# Plan 31-2-01: Schema Contracts

## Objective

Tornar `discountedPriceCents` opcional nos schemas do pipeline e `CampaignSpecSchema` com campos nullable para aceitar saída de IA sem preço/badge para spotlight/exclusive. Este plan não modifica comportamento de validação semântica — apenas contratos de schema.

## Tasks

### Task 1: GenerateImageRequestSchema — discountedPriceCents opcional

<read_first>
- src/lib/image-generation/schema.ts (linha 12, GenerateImageRequestSchema)
- openspec/changes/fase-31-2-diretores-por-intencao/specs/ai-image-generation/spec.md
</read_first>

<action>
1. Em `src/lib/image-generation/schema.ts`, alterar o campo `discountedPriceCents` no `GenerateImageRequestSchema`:
   ```
   De: discountedPriceCents: z.number().int().positive()
   Para: discountedPriceCents: z.number().int().positive().optional()
   ```
2. Posicionamento: linha 12, entre `originalPriceCents` (opcional) e `campaignIntent`.
3. NÃO alterar nenhum outro campo do schema.
4. NÃO remover `.strict()` do schema.
</action>

<acceptance_criteria>
- `GenerateImageRequestSchema.safeParse({ productName: "X", productImageDataUrl: "data:image/jpeg;base64,...", campaignIntent: "spotlight" })` → `success: true` (sem discountedPriceCents)
- `GenerateImageRequestSchema.safeParse({ productName: "X", productImageDataUrl: "data:image/jpeg;base64,...", campaignIntent: "offer", discountedPriceCents: 4990 })` → `success: true` (offer com preço, regressão)
- `npm run typecheck` passa
</acceptance_criteria>

### Task 2: CampaignGenerationInputSchema — discountedPriceCents opcional

<read_first>
- src/lib/campaign-intelligence/schema.ts (linhas 15-18, CampaignGenerationInputSchema)
- openspec/changes/fase-31-2-diretores-por-intencao/specs/schema-intent-contracts/spec.md
</read_first>

<action>
1. Em `src/lib/campaign-intelligence/schema.ts`, alterar o campo `discountedPriceCents` no `CampaignGenerationInputSchema`:
   ```
   De: discountedPriceCents: z.number().int().positive()
   Para: discountedPriceCents: z.number().int().positive().optional()
   ```
2. Posicionamento: linhas 15-18, entre `originalPriceCents` (opcional) e `campaignIntent`.
3. NÃO alterar a validação de `originalPriceCents` ou outros campos.
</action>

<acceptance_criteria>
- `CampaignGenerationInputSchema.safeParse({ productName: "X", storeName: "Loja", storeSegment: "outros", brandColor: "#22C55E", campaignIntent: "exclusive" })` → `success: true`
- `CampaignGenerationInputSchema.safeParse({ productName: "X", storeName: "Loja", storeSegment: "outros", brandColor: "#22C55E", discountedPriceCents: 4990 })` → `success: true` (regressão)
- `npm run typecheck` passa
</acceptance_criteria>

### Task 3: CampaignSpecSchema — discounted_price_display e badge_text nullable

<read_first>
- src/lib/campaign-intelligence/schema.ts (linhas 49-54, objeto offer em CampaignSpecSchema)
- openspec/changes/fase-31-2-diretores-por-intencao/specs/schema-intent-contracts/spec.md
</read_first>

<action>
1. Em `src/lib/campaign-intelligence/schema.ts`, alterar o schema do objeto `offer` em `CampaignSpecSchema`:
   ```
   De: discounted_price_display: z.string().min(1)
   Para: discounted_price_display: z.string().nullable()

   De: badge_text: z.string().min(1)
   Para: badge_text: z.string().nullable()
   ```
2. NÃO alterar `product_name` (continua `z.string().min(1)`) nem `original_price_display` (já nullable).
3. A validação semântica (offer SEMPRE tem preço) fica no serviço, não no schema.
</action>

<acceptance_criteria>
- `CampaignSpecSchema.shape.offer.shape.discounted_price_display instanceof z.ZodNullable` → true
- `CampaignSpecSchema.shape.offer.shape.badge_text instanceof z.ZodNullable` → true
- `CampaignSpecSchema.parse({ commercial_copy: { title: "T", subtitle: "S", hook: "H", cta: "C" }, offer: { product_name: "P", discounted_price_display: null, badge_text: null }, visual_parameters: { ... }, generation_metadata: { provider: "p", model: "m", generated_at: "2024-01-01" } })` → success
- `CampaignSpecSchema.parse` com `product_name: ""` → falha (obrigatório mantido)
- `npm run typecheck` passa
</acceptance_criteria>

### Task 4: InputSnapshot — discountedPriceCents opcional

<read_first>
- src/lib/campaign/types.ts (linha 37, InputSnapshot)
- openspec/changes/fase-31-2-diretores-por-intencao/specs/schema-intent-contracts/spec.md
</read_first>

<action>
1. Em `src/lib/campaign/types.ts`, alterar `discountedPriceCents` em `InputSnapshot`:
   ```
   De: discountedPriceCents: number;
   Para: discountedPriceCents?: number;
   ```
2. NÃO alterar ordem dos campos (alfabética entre opcionais).
</action>

<acceptance_criteria>
- `InputSnapshot` pode ser construído sem `discountedPriceCents`
- `InputSnapshot` com `discountedPriceCents: 4990` funciona (regressão)
- `npm run typecheck` passa
</acceptance_criteria>

### Task 5: MockProvider — adaptado para discountedPriceCents opcional

<read_first>
- src/lib/campaign-intelligence/providers/mock.ts (linhas 71-94, formatação de preço e offer)
- openspec/changes/fase-31-2-diretores-por-intencao/specs/schema-intent-contracts/spec.md
</read_first>

<action>
1. Em `src/lib/campaign-intelligence/providers/mock.ts`:
   a. Tornar `discountedFormatted` condicional — se `input.discountedPriceCents` for undefined, não formatar preço.
   b. Tornar `offer.discounted_price_display` nullable (aceitar `null` quando sem preço):
      ```typescript
      discounted_price_display: discountedPriceCents ? `R$ ${discountedFormatted}` : null,
      ```
   c. Tornar `badge_text` nullable — se `input.badge` vazio, usar `null` em vez de "Oferta":
      ```typescript
      badge_text: input.badge?.trim() || null,
      ```
   d. Atualizar type annotation do `offer` objeto:
      ```typescript
      discounted_price_display: string | null;
      badge_text: string | null;
      ```
</action>

<acceptance_criteria>
- `MockProvider.generate({ ...campaignIntent: "exclusive" })` retorna `offer.discounted_price_display: null`
- `MockProvider.generate({ ...campaignIntent: "exclusive" })` retorna `offer.badge_text: null`
- `MockProvider.generate({ ...campaignIntent: "offer", discountedPriceCents: 4990, badge: "Promoção" })` retorna `offer.discounted_price_display: "R$ 49,90"` (regressão)
- `npm run typecheck` passa
</acceptance_criteria>

### Task 6: TypeScript check

<read_first>
- src/lib/image-generation/schema.ts
- src/lib/campaign-intelligence/schema.ts
- src/lib/campaign/types.ts
- src/lib/campaign-intelligence/providers/mock.ts
</read_first>

<action>
1. Executar `npx tsc --noEmit` e corrigir todos os erros de tipo decorrentes das mudanças de schema.
2. Verificar especialmente consumidores de `discountedPriceCents` que agora recebem `number | undefined`:
   - `src/lib/copy/mapper.ts:35` — `buildOfferText` recebe `discountedPriceCents`
   - `src/lib/image-generation/services/image-generation-service.ts:797` — `formatPriceBRL(body.discountedPriceCents)` — ajustar para aceitar undefined
   - `src/lib/image-generation/services/image-generation-service.ts:562` — `validatePrompts` passa `discountedPriceCents` para reviewer
   - `src/app/api/campaign/generate-image/route.ts:279,384` — inputSnapshot e buildOfferText
3. Se algum consumidor forçar `discountedPriceCents` como required, ajustar para tratar undefined.
</action>

<acceptance_criteria>
- `npx tsc --noEmit` — zero erros
- Consumidores de `discountedPriceCents` tratam corretamente `number | undefined`
</acceptance_criteria>

## Verification

- [ ] `npm run typecheck` — zero erros de tipo
- [ ] `npx vitest run src/lib/campaign-intelligence/__tests__/` — testes passam
- [ ] `npx vitest run src/lib/image-generation/__tests__/` — testes passam
- [ ] `npm run build` — build bem-sucedido

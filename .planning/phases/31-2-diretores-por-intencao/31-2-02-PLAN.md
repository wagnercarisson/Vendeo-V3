---
id: 31-2-02
wave: 1
depends_on: [31-2-01]
file_types:
  - .ts
  - .tsx
files_modified:
  - src/components/flow/campaign-input-form.tsx
  - src/components/flow/use-campaign-form.ts
  - src/app/api/campaign/generate-image/route.ts
autonomous: true
requirements: [UNLOCK-01, UNLOCK-02, UNLOCK-03]
must_haves:
  truths:
    - "IntentSelector não exibe badge 'Em breve' para spotlight/exclusive"
    - "Botão 'Criar Campanha' está habilitado para spotlight e exclusive"
    - "handleSubmit não bloqueia submissão de spotlight/exclusive"
    - "Endpoint aceita campaignIntent 'spotlight' e 'exclusive' sem HTTP 400"
    - "Endpoint valida offer sem discountedPriceCents → HTTP 400"
    - "Endpoint normaliza exclusive com preço → undefined + log de warning"
    - "discountedPriceCents é omitido do body para exclusive no form"
    - "isValid retorna true para todas as intents válidas"
  artifacts:
    - path: "src/app/api/campaign/generate-image/route.ts"
      provides: "Normalização exclusive + validação offer + sem guard de intent"
      contains: "normalizing exclusive"
  key_links:
    - from: "src/app/api/campaign/generate-image/route.ts"
      to: "src/lib/image-generation/schema.ts"
      via: "parsed.data.discountedPriceCents opcional (Plan 01)"
---

# Plan 31-2-02: Unblock — UI/Form/Route

## Objective

Remover todos os bloqueios de intent na UI, form e pipeline: badge "Em breve", submit desabilitado, guard early-return no handleSubmit, guard HTTP 400 na rota. Adicionar normalização de exclusive (preço indevido → undefined + log) e validação de offer (sem preço → 400).

## Tasks

### Task 1: IntentSelector — remover badge "Em breve"

<read_first>
- src/components/flow/campaign-input-form.tsx (linhas 195-248, IntentSelector)
- openspec/changes/fase-31-2-diretores-por-intencao/specs/campaign-input-ui/spec.md
</read_first>

<action>
1. Em `src/components/flow/campaign-input-form.tsx`, localizar o bloco que renderiza o badge "Em breve" (linhas 239-243):
   ```tsx
   {(intent === "spotlight" || intent === "exclusive") && (
     <span className="text-[10px] ...">Em breve</span>
   )}
   ```
2. Remover este bloco completamente.
3. As opções spotlight e exclusive continuam renderizadas, clicáveis e sem indicador de restrição.
</action>

<acceptance_criteria>
- O texto "Em breve" não aparece no DOM para nenhuma opção do IntentSelector
- As opções spotlight e exclusive são clicáveis sem badge
- `npx vitest run` passa (se existir teste de snapshot, atualizar)
</acceptance_criteria>

### Task 2: Botão submit — remover disabled condition para non-offer

<read_first>
- src/components/flow/campaign-input-form.tsx (linhas 521-545, submit button)
- openspec/changes/fase-31-2-diretores-por-intencao/specs/campaign-input-ui/spec.md
</read_first>

<action>
1. Em `src/components/flow/campaign-input-form.tsx`, localizar a condição `disabled` (linha 523):
   ```tsx
   disabled={isSubmitting || fields.campaignIntent !== "offer" || balance === 0 || balance === null}
   ```
2. Remover `fields.campaignIntent !== "offer"` da condição:
   ```tsx
   disabled={isSubmitting || balance === 0 || balance === null}
   ```
3. Localizar o tooltip/title que menciona "Disponível em breve" (linhas 524-531). Remover a condição que mostra tooltip para non-offer.
4. Localizar o texto do botão (linhas 535-544):
   ```tsx
   {isSubmitting ? (
     <><Loader2 ... /> Criando...</>
   ) : fields.campaignIntent !== "offer" ? (
     "Disponível em breve"
   ) : (
     "Criar Campanha"
   )}
   ```
5. Substituir para:
   ```tsx
   {isSubmitting ? (
     <><Loader2 ... /> Criando...</>
   ) : (
     "Criar Campanha"
   )}
   ```
</action>

<acceptance_criteria>
- Botão "Criar Campanha" aparece para spotligh e exclusive
- Botão está habilitado quando saldo > 0 e não está submetendo
- Nenhum tooltip indica "Disponível em breve"
- `npx vitest run` passa (regressão)
</acceptance_criteria>

### Task 3: handleSubmit — remover early return para non-offer

<read_first>
- src/components/flow/use-campaign-form.ts (linhas 559-564, handleSubmit)
- src/components/flow/use-campaign-form.ts (linhas 704-709, isValid)
- openspec/changes/fase-31-2-diretores-por-intencao/specs/campaign-form-intent/spec.md
</read_first>

<action>
1. Em `src/components/flow/use-campaign-form.ts`, localizar o guard no início do `handleSubmit` (linhas 559-564):
   ```typescript
   if (fields.campaignIntent !== "offer") {
     setSubmitError("Disponível em breve");
     setIsSubmitting(false);
     return;
   }
   ```
2. Remover este bloco completamente.
3. Localizar `submitError` conditional — se houver referência a "Disponível em breve" no submitError, limpar.
4. Localizar `isValid` derived (linhas 704-709). Remover a condição `fields.campaignIntent === "offer"` se presente. `isValid` deve depender apenas de validação de campos, não de intent.
5. O body do submit (linhas 631-644) continua enviando `discountedPriceCents` quando presente. Para exclusive, o campo é omitido naturalmente porque `fields.discountedPriceCents` é `undefined`.
</action>

<acceptance_criteria>
- `handleSubmit` com `campaignIntent: "spotlight"` prossegue (não retorna cedo)
- `handleSubmit` com `campaignIntent: "exclusive"` prossegue
- `isValid` retorna `true` para spotlight com campos válidos
- Body do POST para exclusive NÃO contém `discountedPriceCents`
- `npx vitest run` passa (regressão)
</acceptance_criteria>

### Task 4: Route — remover guard HTTP 400 para non-offer + adicionar validação offer + normalização exclusive

<read_first>
- src/app/api/campaign/generate-image/route.ts (linhas 141-149, guard)
- src/app/api/campaign/generate-image/route.ts (linhas 270-300, inputSnapshot)
- src/app/api/campaign/generate-image/route.ts (linhas 377-404, copyTask + buildOfferText)
- openspec/changes/fase-31-2-diretores-por-intencao/specs/campaign-intent-types/spec.md
- openspec/changes/fase-31-2-diretores-por-intencao/specs/ai-image-generation/spec.md
</read_first>

<action>
1. Remover o bloco do guard HTTP 400 (linhas 141-147):
   ```typescript
   if (parsed.data.campaignIntent !== "offer") {
     return Response.json(
       { error: { message: "Intenção comercial..." } },
       { status: 400 }
     );
   }
   ```

2. NO local do guard removido (após `parsed.data`, antes de montar `campaignInput`), adicionar na sequência:
   a. **Validação de offer sem preço**:
      ```typescript
      // ── Validação semântica: offer exige preço ───────────────────
      if (parsed.data.campaignIntent === "offer" && !parsed.data.discountedPriceCents) {
        return Response.json(
          { error: { message: "Preço com desconto é obrigatório para ofertas" } },
          { status: 400 }
        );
      }
      ```
   b. **Normalização de exclusive**:
      ```typescript
      // ── Normalização: exclusive nunca carrega preço ──────────────
      if (parsed.data.campaignIntent === "exclusive" && parsed.data.discountedPriceCents) {
        parsed.data.discountedPriceCents = undefined;
        console.log("[generate-image] exclusive com discountedPriceCents presente — normalizando para ausente.");
      }
      ```

3. Nos locais que usam `buildOfferText` (linha 384, dentro do copyTask quando `!config.copyDirectorEnabled`):
   - `discountedPriceCents` agora é `number | undefined`. O `buildOfferText` será adaptado no Plan 05 (`buildCommercialFrame`).
   - Como stopgap: se `discountedPriceCents` for undefined para o fallback determinístico, usar o novo `buildDeterministicCopy` (Plan 05). Até lá, preservar lógica atual com type assertion se necessário.

4. No `inputSnapshot` (linha 279), o campo já é `campaignInput.discountedPriceCents` — agora reflete opcional.
</action>

<acceptance_criteria>
- POST `/api/campaign/generate-image` com `campaignIntent: "spotlight"` → NÃO retorna HTTP 400 (fluxo prossegue)
- POST `/api/campaign/generate-image` com `campaignIntent: "exclusive"` → NÃO retorna HTTP 400
- POST com `campaignIntent: "offer"` sem `discountedPriceCents` → HTTP 400 "Preço com desconto é obrigatório para ofertas"
- POST com `campaignIntent: "exclusive"` e `discountedPriceCents: 5000` → normaliza para undefined + log de warning
- `npx vitest run` passa (regressão)
- `npm run typecheck` passa
</acceptance_criteria>

### Task 5: TypeScript check

<read_first>
- src/components/flow/campaign-input-form.tsx
- src/components/flow/use-campaign-form.ts
- src/app/api/campaign/generate-image/route.ts
</read_first>

<action>
1. Executar `npx tsc --noEmit` e corrigir todos os erros de tipo.
2. Verificar que todas as referências a `discountedPriceCents` nos arquivos tocados tratam `number | undefined`.
</action>

<acceptance_criteria>
- `npx tsc --noEmit` — zero erros
</acceptance_criteria>

## Verification

- [ ] `npm run typecheck` — zero erros
- [ ] `npx vitest run` — todos os testes passam
- [ ] `npm run build` — build bem-sucedido

---
id: 31-2-04
wave: 2
depends_on: [31-2-01, 31-2-03]
file_types:
  - .ts
files_modified:
  - src/lib/image-generation/services/image-generation-service.ts
autonomous: true
requirements: [IMAGE-01, IMAGE-02, IMAGE-03]
must_haves:
  truths:
    - "assemblePrompt carrega campaign-image-director-${campaignIntent} sem fallback"
    - "buildPromptVariables inclui campaignIntent, preserveImageDirective, commercialFrame"
    - "validatePrompts valida director prompt por campaignIntent"
    - "Prompt ausente para intent válida → preflight fail (invalid_prompt)"
    - "validatePrompts passa discountedPrice vazio para exclusive no reviewer"
    - "preserveImageDirective é string vazia para offer, instrução para spotlight/exclusive com preserveImageContext"
    - "discountedPrice nas variáveis é vazio para exclusive"
  artifacts:
    - path: "src/lib/image-generation/services/image-generation-service.ts"
      provides: "assemblePrompt, buildPromptVariables, validatePrompts adaptados por intent"
      contains: "campaignIntent"
  key_links:
    - from: "src/lib/image-generation/services/image-generation-service.ts"
      to: "src/lib/image-generation/prompt-loader.ts"
      via: "PromptLoader.load com nome dinâmico campaign-image-director-${intent}"
    - from: "src/lib/image-generation/services/image-generation-service.ts"
      to: "src/lib/campaign/types.ts"
      via: "GenerateImageRequest.campaignIntent do schema"
---

# Plan 31-2-04: Image Director Routing

## Objective

Adaptar `ImageGenerationService` para rotear prompts por `campaignIntent`: `assemblePrompt` carrega o template específico, `buildPromptVariables` inclui as novas variáveis contextuais, `validatePrompts` valida o diretor correto, e o `discountedPrice` no reviewer é vazio para exclusive.

## Tasks

### Task 1: buildPromptVariables — adicionar campaignIntent, preserveImageDirective e commercialFrame

<read_first>
- src/lib/image-generation/services/image-generation-service.ts (linhas 765-831, buildPromptVariables)
- src/lib/image-generation/services/image-generation-service.ts (linhas 851-856, formatPriceBRL)
- openspec/changes/fase-31-2-diretores-por-intencao/specs/ai-image-generation/spec.md
- openspec/changes/fase-31-2-diretores-por-intencao/specs/prompt-templates-by-intent/spec.md
</read_first>

<action>
1. Em `buildPromptVariables()`:
   a. Adicionar variável `campaignIntent`:
      ```typescript
      campaignIntent: body.campaignIntent ?? "offer",
      ```
   b. Adicionar `preserveImageDirective`:
      ```typescript
      preserveImageDirective: body.campaignIntent !== "offer" && body.preserveImageContext
        ? "NÃO recortar o produto. Preservar o contexto original da imagem. Adaptar a composição ao redor do produto sem isolá-lo. Legibilidade continua obrigatória."
        : "",
      ```
      Posicionar como uma das variáveis (após `identityDirective` é apropriado).
    c. Adicionar `commercialFrame`:
      Replicar o contrato exato de `buildCommercialFrame`. Usar `formatPriceBRL` (já existe, linha 851) para consistência com o restante do serviço:
      ```typescript
      commercialFrame: (() => {
        const dpc = body.discountedPriceCents;
        switch (body.campaignIntent) {
          case "spotlight":
            return dpc ? `Destaque — ${this.formatPriceBRL(dpc)}` : "Destaque do produto";
          case "exclusive":
            return "Produto exclusivo — sem divulgação de preço";
          default: { // offer
            if (!dpc) return "Oferta";
            const formattedDiscounted = this.formatPriceBRL(dpc);
            if (body.badgeText) {
              const formattedOriginal = (body.originalPriceCents ?? 0) > 0
                ? `de ${this.formatPriceBRL(body.originalPriceCents ?? 0)} por `
                : "";
              return `${body.badgeText}: ${formattedOriginal}${formattedDiscounted}`;
            }
            return `Apenas ${formattedDiscounted}`;
          }
        }
      })(),
      ```
      Nota: Isto usa IIFE para manter a tipagem `Record<string, string>`. O contrato por intent é idêntico ao `buildCommercialFrame` do mapper (Plan 05), garantindo que image e copy directors recebam o mesmo texto comercial.
   d. Ajustar `discountedPrice` na linha 797:
      ```typescript
      discountedPrice: body.discountedPriceCents
        ? this.formatPriceBRL(body.discountedPriceCents)
        : "",
      ```
   e. Ajustar `originalPrice` na linha 794-796 (já é condicional — manter).
   f. Ajustar `badgeText` na linha 798 (já é `?? ""` — manter).
2. Preservar todas as variáveis existentes.
</action>

<acceptance_criteria>
- `buildPromptVariables` com `campaignIntent: "offer"` e `discountedPriceCents: 4990` → `discountedPrice` = "R$ 49,90", `preserveImageDirective` = ""
- `buildPromptVariables` com `campaignIntent: "spotlight"`, `preserveImageContext: true` → `preserveImageDirective` contém "NÃO recortar"
- `buildPromptVariables` com `campaignIntent: "exclusive"` → `discountedPrice` = "", `commercialFrame` contém "exclusivo"
- `npm run typecheck` passa
</acceptance_criteria>

### Task 2: assemblePrompt — carregar template por campaignIntent

<read_first>
- src/lib/image-generation/services/image-generation-service.ts (linhas 833-849, assemblePrompt)
- src/lib/image-generation/prompt-loader.ts (entender como PromptLoader.load funciona com nome dinâmico)
</read_first>

<action>
1. Modificar `assemblePrompt()` para receber `campaignIntent` como parâmetro ou extrair de `variables`:
   ```typescript
   private assemblePrompt(
     state: GenerationState,
     variables: Record<string, string>,
     previousIssues: string[]
   ): string {
     const intent = variables.campaignIntent ?? "offer";
     const promptName = `campaign-image-director-${intent}`;
     const basePrompt = this.promptLoader.load(promptName, variables);
     // ... resto do método (CORRECT/REGENERATE/branches) inalterado
   }
   ```
2. Se `promptLoader.load(promptName, ...)` lançar erro porque o arquivo não existe, propagar o erro (sem fallback). Isso faz com que a promise seja rejeitada e capturada como `invalid_prompt` no preflight.
3. NÃO modificar a lógica de CORRECT/REGENERATE — apenas o nome do prompt carregado.
</action>

<acceptance_criteria>
- `assemblePrompt` com `campaignIntent: "spotlight"` carrega `campaign-image-director-spotlight.md`
- `assemblePrompt` com `campaignIntent: "exclusive"` carrega `campaign-image-director-exclusive.md`
- `assemblePrompt` com `campaignIntent: undefined` ou ausente carrega `campaign-image-director-offer.md`
- Se o arquivo de prompt não existir, o erro é propagado (preflight fail)
- `npm run typecheck` passa
</acceptance_criteria>

### Task 3: validatePrompts — validar director por intent

<read_first>
- src/lib/image-generation/services/image-generation-service.ts (linhas 546-576, validatePrompts)
- openspec/changes/fase-31-2-diretores-por-intencao/specs/ai-image-generation/spec.md
</read_first>

<action>
1. Modificar `validatePrompts()` para:
   a. Determinar `campaignIntent` do body:
      ```typescript
      const campaignIntent = body.campaignIntent ?? "offer";
      ```
   b. Carregar o director prompt específico:
      ```typescript
      const directorPrompt = this.promptLoader.load(`campaign-image-director-${campaignIntent}`, directorVariables);
      ```
   c. Para o reviewer prompt, ajustar `discountedPrice`:
      ```typescript
      discountedPrice: body.discountedPriceCents
        ? this.formatPriceBRL(body.discountedPriceCents)
        : "",
      ```
   d. Se `this.promptLoader.load(...)` falhar (prompt não existe), capturar e adicionar erro:
      ```typescript
      errors.push(`Prompt para intent '${campaignIntent}' não encontrado: campaign-image-director-${campaignIntent}`);
      ```
</action>

<acceptance_criteria>
- `validatePrompts` com brief de `campaignIntent: "exclusive"` valida `campaign-image-director-exclusive.md` + `campaign-image-reviewer.md` com `discountedPrice` vazio
- `validatePrompts` com `campaignIntent: "offer"` valida `campaign-image-director-offer.md` + `campaign-image-reviewer.md` com `discountedPrice` preenchido
- Se prompt de intent não existir, `validatePrompts` retorna `{ valid: false, errors: [...] }`
- `npm run typecheck` passa
</acceptance_criteria>

### Task 4: TypeScript check

<read_first>
- src/lib/image-generation/services/image-generation-service.ts
</read_first>

<action>
1. Executar `npx tsc --noEmit` e corrigir erros.
2. Verificar que `promptLoader.load()` aceita nome dinâmico (string template).
3. Verificar que `formatPriceBRL` aceita `number` (não undefined) — nos locais onde foi ajustado, o valor já é guardado por condicional.
</action>

<acceptance_criteria>
- `npx tsc --noEmit` — zero erros
</acceptance_criteria>

## Verification

- [ ] `npm run typecheck` — zero erros
- [ ] `npx vitest run src/lib/image-generation/__tests__/` — testes passam
- [ ] `npm run build` — build bem-sucedido

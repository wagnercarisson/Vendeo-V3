---
id: 31-2-05
wave: 2
depends_on: [31-2-01, 31-2-03]
file_types:
  - .ts
files_modified:
  - src/lib/copy/schema.ts
  - src/lib/copy/mapper.ts
  - src/lib/copy/copy-director-service.ts
  - src/lib/image-generation/services/image-generation-service.ts
  - src/app/api/campaign/generate-image/route.ts
autonomous: true
requirements: [COPY-01, COPY-02, COPY-03, CONTENT-01]
must_haves:
  truths:
    - "CopyDirectorInputSchema tem commercialFrame (string min(1)) no lugar de offer"
    - "CopyDirectorInputSchema tem campaignIntent (enum, optional, default offer)"
    - "CopyDirectorService.generateCopy carrega campaign-copy-director-${input.campaignIntent}"
    - "buildOfferText substituído por buildCommercialFrame(campaignIntent, input)"
    - "mapBriefToCopyDirectorInput monta commercialFrame por intent e propaga campaignIntent"
    - "buildCommercialRepertoire filtra escassez/validade por intent"
    - "buildCreativeContextGuidance usa framing adequado por intent"
    - "buildDeterministicCopy gera texto diferente por intent"
    - "Fallback determinístico na route usa buildDeterministicCopy"
  artifacts:
    - path: "src/lib/copy/schema.ts"
      provides: "CopyDirectorInputSchema com commercialFrame + campaignIntent"
      min_lines: 17
      contains: "commercialFrame"
    - path: "src/lib/copy/mapper.ts"
      provides: "buildCommercialFrame + mapBriefToCopyDirectorInput adaptado"
      contains: "buildCommercialFrame"
---

# Plan 31-2-05: Copy Director + Content Adaptation

## Objective

Adaptar o Copy Director para suportar intents: `CopyDirectorInputSchema` ganha `campaignIntent` e substitui `offer` por `commercialFrame`; o serviço carrega prompt por intent; o mapper constrói `commercialFrame` condicional; e as funções de conteúdo (`buildCommercialRepertoire`, `buildCreativeContextGuidance`, `buildDeterministicCopy`) são calibradas por intent. O fallback determinístico na route também é adaptado.

## Tasks

### Task 1: CopyDirectorInputSchema — commercialFrame + campaignIntent

<read_first>
- src/lib/copy/schema.ts (linhas 3-15, CopyDirectorInputSchema)
- openspec/changes/fase-31-2-diretores-por-intencao/specs/copy-director/spec.md
</read_first>

<action>
1. Em `src/lib/copy/schema.ts`, modificar `CopyDirectorInputSchema`:
   a. Substituir `offer: z.string().min(1)` por:
      ```typescript
      commercialFrame: z.string().min(1, "commercialFrame é obrigatório"),
      ```
   b. Adicionar após `commercialFrame`:
      ```typescript
      campaignIntent: z
        .enum(["offer", "spotlight", "exclusive"])
        .optional()
        .default("offer"),
      ```
2. O tipo `CopyDirectorInput` é inferido automaticamente pelo `z.infer`.
</action>

<acceptance_criteria>
- `CopyDirectorInputSchema.shape.offer` é undefined (campo removido)
- `CopyDirectorInputSchema.shape.commercialFrame` é `ZodString` com `.min(1)`
- `CopyDirectorInputSchema.shape.campaignIntent` é `ZodDefault<ZodEnum>`
- `CopyDirectorInputSchema.parse({ productName: "X", commercialFrame: "Promoção", storeName: "L", segment: "outros" })` → `success: true, data.campaignIntent === "offer"`
- `CopyDirectorInputSchema.parse({ productName: "X", commercialFrame: "", storeName: "L", segment: "outros" })` → `success: false`
- `npm run typecheck` passa (verificar consumidores)
</acceptance_criteria>

### Task 2: CopyDirectorService.generateCopy — carregar prompt por campaignIntent

<read_first>
- src/lib/copy/copy-director-service.ts (linhas 59-94, generateCopy)
- openspec/changes/fase-31-2-diretores-por-intencao/specs/copy-director/spec.md
</read_first>

<action>
1. Em `generateCopy()` (linhas 59-94):
   a. Extrair `campaignIntent` do input validado:
      ```typescript
      const campaignIntent = validated.campaignIntent ?? "offer";
      ```
   b. Modificar as variáveis para usar `commercialFrame` no lugar de `offer`:
      ```typescript
      const variables: Record<string, string> = {
        productName: validated.productName,
        description: validated.description ?? "",
        commercialFrame: validated.commercialFrame,
        storeName: validated.storeName,
        segment: validated.segment,
        // ... demais campos
      };
      ```
   c. Modificar o prompt carregado para usar nome dinâmico:
      ```typescript
      const promptName = `campaign-copy-director-${campaignIntent}`;
      const prompt = this.promptLoader.load(promptName, variables);
      ```
2. As variáveis agora usam `commercialFrame` em vez de `offer`, alinhado com os prompts (Plan 03).
3. Sem fallback — se o prompt não existir, `promptLoader.load` propaga o erro.
</action>

<acceptance_criteria>
- `generateCopy` com `campaignIntent: "spotlight"` carrega `campaign-copy-director-spotlight.md`
- `generateCopy` com `campaignIntent: "exclusive"` carrega `campaign-copy-director-exclusive.md`
- Variáveis contêm `commercialFrame` e NÃO contêm `offer`
- `npm run typecheck` passa
</acceptance_criteria>

### Task 3: buildCommercialFrame — substituir buildOfferText

<read_first>
- src/lib/copy/mapper.ts (linhas 4-19, buildOfferText)
- openspec/changes/fase-31-2-diretores-por-intencao/specs/copy-director/spec.md
</read_first>

<action>
1. Em `src/lib/copy/mapper.ts`:
   a. Renomear `buildOfferText` para `buildCommercialFrame` com nova assinatura:
      ```typescript
      export function buildCommercialFrame(
        campaignIntent: CampaignIntent,
        input: {
          badgeText?: string;
          originalPriceCents?: number;
          discountedPriceCents?: number;
        }
      ): string {
        switch (campaignIntent) {
          case "offer": {
            // Comportamento idêntico ao antigo buildOfferText
            if (!input.discountedPriceCents) return "Oferta";
            const formattedDiscounted = formatBRL(input.discountedPriceCents);
            if (input.badgeText) {
              const formattedOriginal = input.originalPriceCents
                ? `de R$ ${(input.originalPriceCents / 100).toFixed(2).replace(".", ",")} por `
                : "";
              return `${input.badgeText}: ${formattedOriginal}R$ ${formattedDiscounted}`;
            }
            return `Apenas R$ ${formattedDiscounted}`;
          }
          case "spotlight": {
            if (!input.discountedPriceCents) return "Destaque do produto";
            const formattedDiscounted = formatBRL(input.discountedPriceCents);
            return `Destaque — R$ ${formattedDiscounted}`;
          }
          case "exclusive": {
            return "Produto exclusivo — sem divulgação de preço";
          }
          default:
            return "Oferta";
        }
      }
      ```
   b. Manter `formatBRL` como função interna (não exportada).
   c. Adicionar import de `CampaignIntent`:
      ```typescript
      import type { CampaignIntent } from "@/lib/campaign/types";
      ```
   d. Opcional: manter `buildOfferText` como delegado interno (para backward compatibility se usado em testes):
      ```typescript
      // Mantido para compatibilidade — prefira buildCommercialFrame
      export function buildOfferText(input: { ... }): string {
        return buildCommercialFrame("offer", input);
      }
      ```
</action>

<acceptance_criteria>
- `buildCommercialFrame("offer", { discountedPriceCents: 4990, badgeText: "Promoção" })` → "Promoção: Apenas R$ 49,90"
- `buildCommercialFrame("spotlight", { discountedPriceCents: 12990 })` → "Destaque — R$ 129,90"
- `buildCommercialFrame("spotlight", {})` → "Destaque do produto"
- `buildCommercialFrame("exclusive", {})` → "Produto exclusivo — sem divulgação de preço"
- `npm run typecheck` passa
</acceptance_criteria>

### Task 4: mapBriefToCopyDirectorInput — montar commercialFrame por intent

<read_first>
- src/lib/copy/mapper.ts (linhas 25-46, mapBriefToCopyDirectorInput)
- src/app/api/campaign/generate-image/route.ts (linhas 377-404, copyTask)
- openspec/changes/fase-31-2-diretores-por-intencao/specs/copy-director/spec.md
</read_first>

<action>
1. Modificar `mapBriefToCopyDirectorInput`:
   a. Extrair `campaignIntent` do brief:
      ```typescript
      const campaignIntent = brief.campaignInput.campaignIntent ?? "offer";
      ```
   b. Calcular `commercialFrame`:
      ```typescript
      const commercialFrame = buildCommercialFrame(campaignIntent, {
        badgeText: input.badgeText ?? brief.campaignInput.badgeText,
        originalPriceCents: input.originalPriceCents ?? brief.campaignInput.originalPriceCents,
        discountedPriceCents: input.discountedPriceCents ?? brief.campaignInput.discountedPriceCents,
      });
      ```
   c. Retornar `CopyDirectorInput` com os novos campos:
      ```typescript
      return {
        productName: brief.campaignInput.productName,
        description: brief.campaignInput.description,
        commercialFrame,
        campaignIntent,
        storeName: brief.store.name,
        // ... demais campos
      };
      ```

2. Em `src/app/api/campaign/generate-image/route.ts`, atualizar chamada do `mapBriefToCopyDirectorInput`:
   - O terceiro argumento (`input`) já contém `discountedPriceCents` que pode ser `undefined` para exclusive — propagar como está.
   - O `buildOfferText` no fallback determinístico (linha 381) será substituído pelo `buildDeterministicCopy` na Task 8.
</action>

<acceptance_criteria>
- `mapBriefToCopyDirectorInput` com brief de offer → `commercialFrame` contém "de R$... por R$..."
- `mapBriefToCopyDirectorInput` com brief de exclusive → `commercialFrame` contém "Produto exclusivo"
- `mapBriefToCopyDirectorInput` retorna objeto com `campaignIntent` propagado
- `npm run typecheck` passa
</acceptance_criteria>

### Task 5: CopyDirectorService — atualizar variáveis

<read_first>
- src/lib/copy/copy-director-service.ts (linhas 65-77, variáveis em generateCopy)
</read_first>

<action>
1. Atualizar as variáveis para espelhar o novo schema:
   - Remover `offer: validated.offer`
   - Adicionar `commercialFrame: validated.commercialFrame`
   - Adicionar `campaignIntent: validated.campaignIntent` (opcional — para uso futuro em contextos de prompt)
</action>

<acceptance_criteria>
- Variáveis em `generateCopy` contêm `commercialFrame` e NÃO contêm `offer`
- `npm run typecheck` passa
</acceptance_criteria>

### Task 6: buildCommercialRepertoire — adaptado por intent

<read_first>
- src/lib/image-generation/services/image-generation-service.ts (linhas 629-684, buildCommercialRepertoire)
- openspec/changes/fase-31-2-diretores-por-intencao/specs/ai-image-generation/spec.md
</read_first>

<action>
1. Modificar `buildCommercialRepertoire(body)` para receber `campaignIntent` como parâmetro adicional ou extrair de `body` (que agora tem `campaignIntent` no schema):
   ```typescript
   private buildCommercialRepertoire(body: GenerateImageRequest): string {
     const parts: string[] = [];
     const campaignIntent = body.campaignIntent ?? "offer";
   ```
2. Aplicar filtros por intent:
   - **offer**: comportamento atual (escassez, validade, detalhes, repertório adicional)
   - **spotlight**: remover escassez, remover validade. Manter detalhes, repertório adicional.
   - **exclusive**: manter escassez (se aplicável), remover validade. Adicionar framing de exclusividade. Manter detalhes, repertório adicional.
3. Implementar:
   ```typescript
   // Escassez — filtrar por intent
   if (body.availabilityNotes && campaignIntent !== "spotlight") {
     // ... lógica atual de escassez
   }

   // Validade — filtrar por intent (só offer)
   if (body.validity && campaignIntent === "offer" && (...)) {
     parts.push(`- Oferta válida: ${body.validity}`);
   }
   ```
4. Para `exclusive`: adicionar se houver `availabilityNotes` com escassez, prefixar com "Disponibilidade:" em vez de "Disponível:".
</action>

<acceptance_criteria>
- `buildCommercialRepertoire` com `campaignIntent: "spotlight"` e `availabilityNotes: "poucas unidades"` → NÃO contém nota de escassez
- `buildCommercialRepertoire` com `campaignIntent: "offer"` e `availabilityNotes: "poucas unidades"` → contém nota de escassez (regressão)
- `buildCommercialRepertoire` com `campaignIntent: "spotlight"` e `validity: "até 30/06"` → NÃO contém validade
- `buildCommercialRepertoire` com `campaignIntent: "exclusive"` e `availabilityNotes: "edição limitada"` → contém "Disponibilidade:" (framing exclusivo)
- `npm run typecheck` passa
</acceptance_criteria>

### Task 7: buildCreativeContextGuidance — adaptado por intent

<read_first>
- src/lib/image-generation/services/image-generation-service.ts (linhas 705-763, buildCreativeContextGuidance)
- openspec/changes/fase-31-2-diretores-por-intencao/specs/ai-image-generation/spec.md
</read_first>

<action>
1. Adicionar parâmetro `campaignIntent`:
   ```typescript
   private buildCreativeContextGuidance(
     segment: string,
     category: string,
     hasConflict: boolean,
     campaignIntent: string
   ): string {
   ```
2. No final do método (após a lógica existente de segmento/categoria), substituir framing de preço por intent:
   - Se `campaignIntent === "offer"`: manter retorno atual (framing de preço como oportunidade/vantagem).
   - Se `campaignIntent === "spotlight"`: substituir "Preço é..." por framing de descoberta/benefício. Pós-processar o resultado:
     ```typescript
     if (campaignIntent === "spotlight") {
       return result.replace(/Preço é\s+/g, "Benefício é ").replace(/preço é\s+/g, "diferencial é ");
     }
     ```
   - Se `campaignIntent === "exclusive"`: substituir framing de preço por valor percebido:
     ```typescript
     if (campaignIntent === "exclusive") {
       return result.replace(/Preço é\s+[^.]+\./g, "Valor percebido e exclusividade são os pilares da comunicação.");
     }
     ```
3. Atualizar chamada em `buildPromptVariables`:
   ```typescript
   const creativeContextGuidance = this.buildCreativeContextGuidance(
     storeSegment, effectiveInferredCategory, hasConflict,
     body.campaignIntent ?? "offer"
   );
   ```
</action>

<acceptance_criteria>
- `buildCreativeContextGuidance("bebidas-adegas-conveniencia", "cafe", false, "spotlight")` → resultado NÃO contém "Preço é"
- `buildCreativeContextGuidance("bebidas-adegas-conveniencia", "cafe", false, "spotlight")` → contém "Benefício é" ou "diferencial"
- `buildCreativeContextGuidance("eletronicos-tecnologia", "computador", false, "exclusive")` → contém "Valor percebido"
- `buildCreativeContextGuidance("eletronicos-tecnologia", "computador", false, "offer")` → idêntico ao comportamento atual
- `npm run typecheck` passa
</acceptance_criteria>

### Task 8: buildDeterministicCopy — fallback por intent

<read_first>
- src/app/api/campaign/generate-image/route.ts (linhas 380-394, fallback determinístico)
- openspec/changes/fase-31-2-diretores-por-intencao/specs/ai-image-generation/spec.md
</read_first>

<action>
1. Criar função `buildDeterministicCopy` em `src/lib/copy/mapper.ts` (ou em um local apropriado — pode ser no próprio `route.ts` se preferir simplicidade):
   ```typescript
   export function buildDeterministicCopy(
     campaignIntent: CampaignIntent,
     params: {
       productName: string;
       storeName: string;
       commercialFrame?: string;
       discountedPriceCents?: number;
       badgeText?: string;
     }
   ): { title: string; caption: string; cta_post: string; hashtags: string[] } {
     switch (campaignIntent) {
       case "offer": {
         const commercialFrame = params.commercialFrame ?? buildCommercialFrame("offer", params);
         return {
           title: params.productName,
           caption: `${params.productName} — ${commercialFrame}`,
           cta_post: "Aproveite!",
           hashtags: [],
         };
       }
       case "spotlight": {
         return {
           title: `Novidade na ${params.storeName}!`,
           caption: `${params.productName} — Novo na ${params.storeName}!${params.discountedPriceCents ? ` Por apenas R$ ${(params.discountedPriceCents / 100).toFixed(2).replace(".", ",")}` : ""}`,
           cta_post: "Confira!",
           hashtags: [],
         };
       }
       case "exclusive": {
         return {
           title: `Exclusivo na ${params.storeName}!`,
           caption: `${params.productName} — Exclusivo na ${params.storeName}! Produto premium com disponibilidade limitada.`,
           cta_post: "Saiba mais!",
           hashtags: [],
         };
       }
       default:
         return { title: params.productName, caption: params.productName, cta_post: "Aproveite!", hashtags: [] };
     }
   }
   ```
2. Em `src/app/api/campaign/generate-image/route.ts`, substituir o bloco de fallback (linhas 380-394):
   ```typescript
   if (!config.copyDirectorEnabled) {
     const deterministic = buildDeterministicCopy(
       campaignInput.campaignIntent ?? "offer",
       {
         productName: campaignInput.productName,
         storeName: brief.store.name,
         commercialFrame: buildCommercialFrame(campaignInput.campaignIntent ?? "offer", {
           badgeText: campaignInput.badgeText,
           originalPriceCents: campaignInput.originalPriceCents,
           discountedPriceCents: campaignInput.discountedPriceCents,
         }),
         discountedPriceCents: campaignInput.discountedPriceCents,
         badgeText: campaignInput.badgeText,
       }
     );
     copyResult = deterministic;
     // ... emit e log
   }
   ```
3. Importar `buildDeterministicCopy` e `buildCommercialFrame` de `@/lib/copy/mapper`.
</action>

<acceptance_criteria>
- `buildDeterministicCopy("offer", { productName: "X", storeName: "L", commercialFrame: "Promoção: de R$ 100 por R$ 49,90" })` → caption contém preço
- `buildDeterministicCopy("spotlight", { productName: "X", storeName: "L", discountedPriceCents: 12990 })` → caption contém "Novo" e preço
- `buildDeterministicCopy("spotlight", { productName: "X", storeName: "L" })` → caption contém "Novo" sem preço
- `buildDeterministicCopy("exclusive", { productName: "X", storeName: "L" })` → caption contém "Exclusivo" sem preço
- `npm run typecheck` passa
</acceptance_criteria>

### Task 9: TypeScript check

<read_first>
- src/lib/copy/schema.ts
- src/lib/copy/mapper.ts
- src/lib/copy/copy-director-service.ts
- src/lib/image-generation/services/image-generation-service.ts
- src/app/api/campaign/generate-image/route.ts
</read_first>

<action>
1. Executar `npx tsc --noEmit` e corrigir erros.
2. Verificar todos os consumidores de:
   - `CopyDirectorInput` (que agora tem `commercialFrame` em vez de `offer`)
   - `buildOfferText` (que pode ter sido renomeado para `buildCommercialFrame`)
   - Testes que constroem `CopyDirectorInput` manualmente
</action>

<acceptance_criteria>
- `npx tsc --noEmit` — zero erros
</acceptance_criteria>

## Verification

- [ ] `npm run typecheck` — zero erros
- [ ] `npx vitest run src/lib/copy/__tests__/` — testes passam
- [ ] `npx vitest run src/lib/image-generation/__tests__/` — testes passam
- [ ] `npm run build` — build bem-sucedido

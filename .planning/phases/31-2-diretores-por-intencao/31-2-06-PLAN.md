---
id: 31-2-06
wave: 3
depends_on: [31-2-01, 31-2-02, 31-2-04, 31-2-05]
file_types:
  - .ts
files_modified:
  - src/__tests__/campaign/intent-tests.test.ts (ou similar)
autonomous: false
requirements: [TEST-01, TEST-02]
must_haves:
  truths:
    - "14 novos testes de schema, endpoint, normalização, copy e conteúdo"
    - "Testes de schema: discountedPriceCents opcional aceito para todas intents"
    - "Testes de endpoint: spotlight/exclusive aceitos, offer sem preço rejeitado"
    - "Testes de normalização: exclusive com preço → undefined + log"
    - "Testes de copy: commercialFrame, buildDeterministicCopy, buildCommercialFrame"
    - "Todos os 1036+ testes existentes continuam passando"
    - "TypeScript, lint e build limpos"
  artifacts:
    - path: "src/__tests__/"
      provides: "14+ novos testes para F31.2"
  key_links:
    - from: "Testes"
      to: "src/lib/image-generation/schema.ts"
      via: "GenerateImageRequestSchema aceita intents sem preço"
    - from: "Testes"
      to: "src/app/api/campaign/generate-image/route.ts"
      via: "Endpoint aceita spotlight/exclusive, rejeita offer sem preço, normaliza exclusive"
---

# Plan 31-2-06: Tests + Verification

## Objective

Criar 14 testes que cobrem as mudanças da F31.2: schemas tolerantes, desbloqueio de endpoint, normalização de exclusive, fallback determinístico, commercialFrame, e regressão de offer. Verificação final com typecheck, lint e build.

## Tasks

### Task 1: Testes de Schema — discountedPriceCents opcional (4 testes)

<read_first>
- src/lib/image-generation/schema.ts
- src/lib/campaign-intelligence/schema.ts
- openspec/changes/fase-31-2-diretores-por-intencao/specs/schema-intent-contracts/spec.md
</read_first>

<action>
1. Criar arquivo de teste ou adicionar aos testes existentes de schema:
   ```typescript
   describe("F31.2: Schema Intent Contracts", () => {
     // 7.1
     it("GenerateImageRequestSchema aceita spotlight sem discountedPriceCents", () => {
       const result = GenerateImageRequestSchema.safeParse({
         productName: "Produto X",
         productImageDataUrl: "data:image/jpeg;base64,/9j/...",
         campaignIntent: "spotlight",
       });
       expect(result.success).toBe(true);
     });

     // 7.2
     it("GenerateImageRequestSchema aceita exclusive sem discountedPriceCents", () => {
       const result = GenerateImageRequestSchema.safeParse({
         productName: "Produto X",
         productImageDataUrl: "data:image/jpeg;base64,/9j/...",
         campaignIntent: "exclusive",
       });
       expect(result.success).toBe(true);
     });

     // 7.3
     it("CampaignSpecSchema aceita discounted_price_display null para exclusive", () => {
       const result = CampaignSpecSchema.safeParse(mockSpec({
         offer: { discounted_price_display: null, badge_text: "Exclusivo" }
       }));
       expect(result.success).toBe(true);
     });

     // 7.4
     it("CampaignSpecSchema aceita badge_text null para spotlight", () => {
       const result = CampaignSpecSchema.safeParse(mockSpec({
         offer: { discounted_price_display: "R$ 49,90", badge_text: null }
       }));
       expect(result.success).toBe(true);
     });
   });
   ```
</action>

<acceptance_criteria>
- Teste 7.1: `GenerateImageRequestSchema` aceita `spotlight` sem `discountedPriceCents` → pass
- Teste 7.2: `GenerateImageRequestSchema` aceita `exclusive` sem `discountedPriceCents` → pass
- Teste 7.3: `CampaignSpecSchema` aceita `discounted_price_display: null` → pass
- Teste 7.4: `CampaignSpecSchema` aceita `badge_text: null` → pass
</acceptance_criteria>

### Task 2: Testes de Endpoint — desbloqueio e validação (4 testes)

<read_first>
- src/app/api/campaign/generate-image/route.ts
- openspec/changes/fase-31-2-diretores-por-intencao/specs/ai-image-generation/spec.md
</read_first>

<action>
1. Adicionar testes de integração para o endpoint:
   ```typescript
   describe("F31.2: Endpoint Intent Unlock", () => {
     // 7.5
     it("aceita campaignIntent spotlight (sem HTTP 400)", async () => {
       // POST com campaignIntent: "spotlight" → não 400
     });

     // 7.6
     it("aceita campaignIntent exclusive (sem HTTP 400)", async () => {
       // POST com campaignIntent: "exclusive" → não 400
     });

     // 7.7
     it("exclusive com preço normaliza para ausente (com log)", async () => {
       // POST com exclusive + discountedPriceCents → normaliza + console.warn/console.log
     });

     // 7.8
     it("offer sem discountedPriceCents retorna HTTP 400", async () => {
       // POST com offer sem discountedPriceCents → 400 "Preço com desconto é obrigatório"
     });
   });
   ```
2. Testes 7.5, 7.6, 7.8 usam mocks de rota (se houver framework de teste HTTP). Se não, testar a lógica de validação isoladamente.
</action>

<acceptance_criteria>
- Teste 7.5: spotlight passa sem HTTP 400 → pass
- Teste 7.6: exclusive passa sem HTTP 400 → pass
- Teste 7.7: exclusive com preço → normalizado, log emitido → pass
- Teste 7.8: offer sem preço → HTTP 400 → pass
</acceptance_criteria>

### Task 3: Testes de Copy Director (4 testes)

<read_first>
- src/lib/copy/schema.ts
- src/lib/copy/mapper.ts
- src/lib/copy/copy-director-service.ts
- openspec/changes/fase-31-2-diretores-por-intencao/specs/copy-director/spec.md
</read_first>

<action>
1. Adicionar testes:
   ```typescript
   describe("F31.2: Copy Director", () => {
     // 7.9
     it("CopyDirectorInputSchema rejeita commercialFrame vazio", () => {
       const result = CopyDirectorInputSchema.safeParse({
         productName: "X", commercialFrame: "", storeName: "L", segment: "outros"
       });
       expect(result.success).toBe(false);
     });

     // 7.10
     it("offer sem regressão — mesmo input → mesmo output no buildCommercialFrame", () => {
       // Comparar buildCommercialFrame("offer", { ... }) com o resultado esperado
     });

     // 7.11
     it("CopyDirectorService carrega prompt correto por intent", () => {
       // Mock promptLoader.load e verificar nome do prompt
     });

     // 7.14 (combinado)
     it("buildCommercialFrame retorna texto por intent", () => {
       expect(buildCommercialFrame("offer", { discountedPriceCents: 4990, badgeText: "Promoção" })).toContain("R$ 49,90");
       expect(buildCommercialFrame("spotlight", { discountedPriceCents: 12990 })).toContain("R$ 129,90");
       expect(buildCommercialFrame("exclusive", {})).toContain("exclusivo");
     });
   });
   ```
</action>

<acceptance_criteria>
- Teste 7.9: `CopyDirectorInputSchema` rejeita `commercialFrame: ""` → pass
- Teste 7.10: offer sem regressão → pass
- Teste 7.11: CopyDirectorService carrega prompt por intent → pass
- Teste 7.14: `buildCommercialFrame` retorna texto correto por intent → pass
</acceptance_criteria>

### Task 4: Testes de Conteúdo e Fallback (2 testes)

<read_first>
- src/lib/image-generation/services/image-generation-service.ts
- src/lib/copy/mapper.ts
- openspec/changes/fase-31-2-diretores-por-intencao/specs/ai-image-generation/spec.md
</read_first>

<action>
1. Adicionar testes:
   ```typescript
   describe("F31.2: Content Adaptation", () => {
     // 7.12
     it("assemblePrompt falha no preflight se prompt de intent válida não existe", async () => {
       // Mock promptLoader.load para lançar erro → validar que erro é propagado
     });

     // 7.13
     it("buildDeterministicCopy gera texto diferente por intent", () => {
       const offer = buildDeterministicCopy("offer", { productName: "X", storeName: "L", commercialFrame: "Promoção" });
       const exclusive = buildDeterministicCopy("exclusive", { productName: "X", storeName: "L" });
       expect(offer.caption).not.toBe(exclusive.caption);
       expect(exclusive.caption).not.toContain("R$");
     });
   });
   ```
</action>

<acceptance_criteria>
- Teste 7.12: `assemblePrompt` com intent válida sem prompt → erro propagado → pass
- Teste 7.13: `buildDeterministicCopy` gera textos diferentes por intent → pass
</acceptance_criteria>

### Task 5: Verificação Final

<read_first>
- Todos os arquivos modificados na fase
</read_first>

<action>
1. Executar `npx vitest run` — todos os testes passando (incluindo regressão dos 1036+ existentes)
2. Executar `npx tsc --noEmit` — zero erros de tipo
3. Executar `npx next lint` — zero erros de lint
4. Executar `npm run build` — build bem-sucedido
5. Se algum teste falhar, corrigir e re-executar.
</action>

<acceptance_criteria>
- `npx vitest run` — 100% passing (14+ novos, 1036+ existentes)
- `npx tsc --noEmit` — zero erros
- `npx next lint` — zero erros
- `npm run build` — sucesso
</acceptance_criteria>

## Verification

- [ ] `npx vitest run` — todos os testes passando
- [ ] `npx tsc --noEmit` — zero erros de tipo
- [ ] `npx next lint` — zero erros
- [ ] `npm run build` — compila com sucesso

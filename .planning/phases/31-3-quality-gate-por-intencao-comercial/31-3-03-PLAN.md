---
id: 31-3-03
wave: 2
depends_on:
  - 31-3-01
  - 31-3-02
file_types:
  - .md
files_modified:
  - prompts/campaign-image-reviewer.md
autonomous: true
requirements: [PROMPT-31-3-01, PROMPT-31-3-02, PROMPT-31-3-03]
must_haves:
  truths:
    - "campaign-image-reviewer.md usa variáveis contextuais em vez de placeholders diretos de dados comerciais"
    - "O prompt contém seção '## Comportamento Esperado' com as 5 variáveis contextuais"
    - "commercial_tone_mismatch é critério de inspeção reconhecido com regras de severidade por intent"
    - "{{discountedPrice}} e {{badgeText}} NÃO aparecem no corpo dos critérios"
    - "validationContextSection continua funcionando como seção condicional"
  artifacts:
    - path: "prompts/campaign-image-reviewer.md"
      provides: "Prompt reestruturado com variáveis contextuais"
      min_lines: 120
      contains: "expectedBadgeBehavior"
  key_links:
    - from: "prompts/campaign-image-reviewer.md"
      to: "src/lib/image-generation/services/image-review-service.ts"
      via: "Prompt recebe {{expectedPriceBehavior}} etc. montados pelo serviço"
---

# Plan 31-3-03: Prompt Restructuring

## Objective

Reestruturar `prompts/campaign-image-reviewer.md` para usar variáveis contextuais pré-resolvidas em vez de placeholders diretos de dados comerciais. Adicionar seção "Comportamento Esperado", novo critério `commercial_tone_mismatch`, remover placeholders antigos dos critérios.

## Tasks

### Task 1: Reestruturar prompt com variáveis contextuais

<read_first>
- prompts/campaign-image-reviewer.md (atual, 109 linhas)
- openspec/changes/fase-31-3-quality-gate-por-intencao-comercial/design.md (D1, D2)
</read_first>

<action>
1. Substituir a tabela de dados da campanha:
   ```
   De:
   | Campo | Valor |
   |-------|-------|
   | **Loja** | {{storeName}} |
   | **Produto** | {{productName}} |
   | **Preço original** | {{originalPrice}} |
   | **Preço com desconto** | {{discountedPrice}} |
   | **Texto do badge** | {{badgeText}} |

   Para:
   | Campo | Valor |
   |-------|-------|
   | **Loja** | {{storeName}} |
   | **Produto** | {{productName}} |
   | **Preço original** | {{originalPrice}} |

   + nova seção "## Comportamento Esperado" (Task 2)
   ```
2. Os placeholders `{{discountedPrice}}` e `{{badgeText}}` saem da tabela principal.
3. `{{originalPrice}}` permanece na tabela (é informacional, não critério).
4. A seção `{{validationContextSection}}` permanece.
</action>

<acceptance_criteria>
- Tabela de dados da campanha NÃO contém `{{discountedPrice}}` nem `{{badgeText}}`
- `{{storeName}}`, `{{productName}}`, `{{originalPrice}}` permanecem
- `{{validationContextSection}}` permanece
- Prompt tem nova seção "## Comportamento Esperado"
</acceptance_criteria>

### Task 2: Adicionar seção "## Comportamento Esperado"

<read_first>
- prompts/campaign-image-reviewer.md
- openspec/changes/fase-31-3-quality-gate-por-intencao-comercial/specs/intent-aware-review/spec.md
</read_first>

<action>
1. Inserir após a tabela de dados e antes de `{{validationContextSection}}`:
   ```markdown
   ## Comportamento Esperado

   A intenção comercial da campanha define o que é esperado em termos de preço, badge, tratamento de imagem e tom.

   | Variável | Valor |
   |----------|-------|
   | **Intenção comercial** | {{campaignIntentLabel}} |
   | **Comportamento de preço** | {{expectedPriceBehavior}} |
   | **Comportamento de badge** | {{expectedBadgeBehavior}} |
   | **Tratamento da imagem** | {{expectedImageTreatment}} |
   | **Tom comercial esperado** | {{expectedCommercialTone}} |
   ```
2. Esta seção substitui a função dos placeholders antigos — em vez de interpolar valores brutos, o prompt recebe expectativas já traduzidas em linguagem natural.
</action>

<acceptance_criteria>
- Prompt contém seção "## Comportamento Esperado"
- Seção contém tabela com 5 variáveis: `{{campaignIntentLabel}}`, `{{expectedPriceBehavior}}`, `{{expectedBadgeBehavior}}`, `{{expectedImageTreatment}}`, `{{expectedCommercialTone}}`
- NENHUM placeholder `{{discountedPrice}}` ou `{{badgeText}}` na seção
</acceptance_criteria>

### Task 3: Adicionar commercial_tone_mismatch como critério de inspeção

<read_first>
- prompts/campaign-image-reviewer.md (seção de critérios, linhas 19-46)
- openspec/changes/fase-31-3-quality-gate-por-intencao-comercial/specs/intent-aware-review/spec.md
</read_first>

<action>
1. Adicionar novo critério após o critério 7 (weak_visual_quality):
   ```markdown
   ### 8. commercial_tone_mismatch (critical se contradiz intent, minor se publicável)

   O tom comercial da imagem é coerente com a intenção comercial ({{campaignIntentLabel}})?

   Comportamento esperado: {{expectedCommercialTone}}

   - **Crítico:** A imagem usa CTA promocional (ex.: "Promoção relâmpago", "50% OFF") em exclusive, ou inventa condição comercial relevante, ou contradiz frontalmente a intenção comercial. A peça NÃO é publicável.
   - **Minor:** O tom está levemente desalinhado (ex.: "Últimas unidades" em exclusive) mas a peça ainda é publicável. Não bloqueia.
   ```
2. Atualizar o formato de resposta para mencionar `commercial_tone_mismatch` como type válido.
3. Atualizar a seção "Regras para 'issues'" para incluir `commercial_tone_mismatch`.
</action>

<acceptance_criteria>
- Prompt contém critério 8: `commercial_tone_mismatch`
- Descrição menciona severidade critical vs minor
- Menciona `{{campaignIntentLabel}}` e `{{expectedCommercialTone}}`
- Formato de resposta lista `commercial_tone_mismatch` como type válido
</acceptance_criteria>

### Task 4: Remover placeholders antigos do corpo dos critérios

<read_first>
- prompts/campaign-image-reviewer.md (critério wrong_price, linha 24)
- openspec/changes/fase-31-3-quality-gate-por-intencao-comercial/design.md (D2)
</read_first>

<action>
1. Critério `wrong_price`:
   ```
   De: "O preço exibido na imagem corresponde a {{discountedPrice}} (preço com desconto) e {{originalPrice}}..."
   Para: "O preço exibido na imagem segue o comportamento esperado ({{expectedPriceBehavior}})? Qualquer divergência do comportamento esperado é crítica."
   ```
2. Critério `invented_information`:
   - Remover menção indireta a valores de preço como referência
   - Manter foco em informações não fornecidas
3. Critério `wrong_store_name`: manter (usa {{storeName}} — continua válido)
4. Critério `wrong_product_name`: manter (usa {{productName}} — continua válido)
5. NÃO remover `{{storeName}}`, `{{productName}}`, `{{originalPrice}}` da tabela — são dados de referência, não placeholders de critério.
</action>

<acceptance_criteria>
- Critério `wrong_price` NÃO contém `{{discountedPrice}}`
- Critério `wrong_price` contém `{{expectedPriceBehavior}}`
- `{{storeName}}` e `{{productName}}` permanecem nos respectivos critérios
- `{{originalPrice}}` permanece na tabela de dados
- NENHUM critério contém `{{badgeText}}`
</acceptance_criteria>

### Task 5: Verificar validationContextSection

<read_first>
- prompts/campaign-image-reviewer.md (linha 17, validationContextSection)
- src/lib/image-generation/services/image-review-service.ts (linhas 61-81, montagem de validationContextSection)
</read_first>

<action>
1. Verificar que `{{validationContextSection}}` continua presente no prompt — é a seção condicional que o serviço preenche quando há contexto de validação (correção de nome, override do usuário).
2. No serviço, a montagem da `validationContextSection` permanece a mesma — não tem interação com as novas variáveis contextuais.
3. Garantir que a seção "## Comportamento Esperado" e o `{{validationContextSection}}` coexistem no prompt sem conflito.
</action>

<acceptance_criteria>
- `{{validationContextSection}}` está presente no prompt
- O serviço ainda monta a `validationContextSection` com inputCorrection e overrides
- Prompt com ambas as seções é semanticamente coerente
- `npm run build` — build bem-sucedido
</acceptance_criteria>

## Verification

- [ ] Prompt `campaign-image-reviewer.md` revisado manualmente para consistência
- [ ] Nenhum `{{discountedPrice}}` ou `{{badgeText}}` no corpo dos critérios
- [ ] `{{validationContextSection}}` presente
- [ ] Seção "## Comportamento Esperado" com 5 variáveis contextuais
- [ ] Critério `commercial_tone_mismatch` presente com regras de severidade

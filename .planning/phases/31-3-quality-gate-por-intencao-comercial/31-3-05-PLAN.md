---
id: 31-3-05
wave: 3
depends_on:
  - 31-3-01
  - 31-3-02
  - 31-3-03
  - 31-3-04
file_types:
  - .ts
files_modified:
  - src/__tests__/f31-3-review-quality-gate.test.ts
  - src/lib/image-generation/services/__tests__/image-review-service.test.ts
  - src/lib/image-generation/services/__tests__/image-generation-service.test.ts
autonomous: true
requirements: [TEST-31-3-01, TEST-31-3-02, TEST-31-3-03]
must_haves:
  truths:
    - "f31-3-review-quality-gate.test.ts criado com testes de contrato: ImageReviewInput default offer, exclusive sem discountedPrice aceito"
    - "image-review-service.test.ts: review() monta variáveis corretas para cada intent, prompt sem placeholders vazios, expectedBadgeBehavior varia conforme badge presente/ausente"
    - "image-generation-service.test.ts: validatePrompts valida com campaignIntent, falha se variável contextual ausente, falha se placeholders antigos no prompt"
    - "Teste de regressão: cenário offer com comportamento equivalente ao anterior"
    - "npx vitest run, npm run typecheck, npm run build — tudo limpo"
  artifacts:
    - path: "src/__tests__/f31-3-review-quality-gate.test.ts"
      provides: "Testes de contrato/drift da fase 31.3"
      min_lines: 80
      contains: "commercial_tone_mismatch"
---

# Plan 31-3-05: Automated Tests

## Objective

Criar testes automatizados de contrato e drift para verificar que o `ImageReviewService` intent-aware mantém compatibilidade com chamadas existentes, monta variáveis contextuais corretamente, e que o `validatePrompts` detecta configurações inválidas.

## Tasks

### Task 1: Criar f31-3-review-quality-gate.test.ts — Testes de Contrato

<read_first>
- src/__tests__/ (diretório para entender padrão de testes de contrato existentes, ex.: regression-master-switch.test.ts)
- src/lib/image-generation/schema.ts (ReviewIssueType, ImageReviewResult)
- src/lib/image-generation/services/image-review-service.ts (ImageReviewInput)
- openspec/changes/fase-31-3-quality-gate-por-intencao-comercial/specs/image-quality-review/spec.md
</read_first>

<action>
1. Criar `src/__tests__/f31-3-review-quality-gate.test.ts` com:
   ```typescript
   import { describe, it, expect } from 'vitest';
   import type { ImageReviewInput } from '@/lib/image-generation/services/image-review-service';

   describe('F31.3 Quality Gate por Intenção Comercial — Contract Tests', () => {
     it('ImageReviewInput default campaignIntent é offer (regressão)', () => {
       const input: ImageReviewInput = {
         productName: 'Bolo de Cenoura',
         storeName: 'Padaria Pão & Cia',
         // sem campaignIntent — compatibilidade com chamadas existentes
       };
       // O serviço usa ?? "offer", então a ausência do campo é aceita
       expect(input.campaignIntent).toBeUndefined(); // default no runtime, não no tipo
     });

     it('ImageReviewInput aceita exclusive sem discountedPrice', () => {
       const input: ImageReviewInput = {
         productName: 'Buquê de Rosas',
         storeName: 'Flores & Encanto',
         campaignIntent: 'exclusive',
         preserveImageContext: true,
         // discountedPrice é undefined — exclusivo não tem preço
       };
       expect(input.campaignIntent).toBe('exclusive');
       expect(input.discountedPrice).toBeUndefined();
     });

     it('ReviewIssueType aceita commercial_tone_mismatch', () => {
       const issue: import('@/lib/image-generation/schema').ReviewIssue = {
         type: 'commercial_tone_mismatch',
         severity: 'minor',
         description: 'Tom levemente promocional em peça exclusive.',
       };
       expect(issue.type).toBe('commercial_tone_mismatch');
     });

     it('ImageReviewResult.failureType aceita null', () => {
       const result: import('@/lib/image-generation/schema').ImageReviewResult = {
         passed: true,
         issues: [],
         failureType: null,
       };
       expect(result.failureType).toBeNull();
     });
   });
   ```
2. Seguir o padrão de imports e estrutura dos testes de contrato existentes.
</action>

<acceptance_criteria>
- Arquivo `src/__tests__/f31-3-review-quality-gate.test.ts` criado
- Testes de contrato para ImageReviewInput, ReviewIssueType, failureType
- `npx vitest run src/__tests__/f31-3-review-quality-gate.test.ts` — passa
</acceptance_criteria>

### Task 2: Adicionar testes em image-review-service.test.ts

<read_first>
- src/lib/image-generation/services/__tests__/image-review-service.test.ts
- openspec/changes/fase-31-3-quality-gate-por-intencao-comercial/specs/intent-aware-review/spec.md
</read_first>

<action>
1. Adicionar ao arquivo de teste existente:
   - Teste: `review() monta expectedPriceBehavior correto para offer com preço`
     - Mock `PromptLoader.load` para capturar as variáveis
     - Verificar que `expectedPriceBehavior` contém o valor formatado
     - Verificar que NÃO contém `{{discountedPrice}}`
   - Teste: `review() monta expectedBadgeBehavior correto para offer`
     - `badgeText: "Promoção"` → variante obrigatória
   - Teste: `review() monta expectedBadgeBehavior para exclusive sem badge`
     - `badgeText: undefined` → variante "nenhum badge foi informado"
   - Teste: `review() monta expectedPriceBehavior para exclusive`
     - `discountedPrice: undefined` → "A imagem NÃO deve exibir preço"
   - Teste: `review() monta expectedImageTreatment por intent + preserveImageContext`
     - offer + false → contém "isolar o produto em fundo comercial"
     - spotlight + true → contém "fundo contextual DA IMAGEM DEVE ser preservado"
     - exclusive + false → contém "não é obrigatório nem proibido"
   - Teste: `review() com empty_review retorna resultado estruturado`
     - Mock `callVisionModel` **já retornando o JSON** `{ "passed": false, "issues": [{ "type": "empty_review", "severity": "critical", "description": "..." }] }` — isso testa que `parseResult` reconhece o formato
     - Teste separado para `callVisionModel` com resposta vazia do OpenAI: mockar `openai.chat.completions.create` para retornar `{ choices: [{ message: { content: null } }] }` e verificar que retorna o JSON de `empty_review` em vez de lançar exceção
   - Teste: `review() NUNCA passa {{discountedPrice}} nas variáveis`
     - Verificar que todas as strings em contextVars não contêm `{{`
</action>

<acceptance_criteria>
- Testes novos em `image-review-service.test.ts` passam
- Cobertura: offer, spotlight, exclusive com e sem badge
- Cobertura: preserveImageContext true/false
- Cobertura: empty_review estruturado
- `npm run typecheck` passa
</acceptance_criteria>

### Task 3: Adicionar teste em image-generation-service.test.ts — validatePrompts com intent

<read_first>
- src/lib/image-generation/services/__tests__/image-generation-service.test.ts
- openspec/changes/fase-31-3-quality-gate-por-intencao-comercial/specs/prompt-templates-by-intent/spec.md
</read_first>

<action>
1. Adicionar ao arquivo de teste existente:
   - Teste: `validatePrompts valida prompt do revisor com campaignIntent`
     - Mock `PromptLoader.load` para retornar prompt com `{{expectedPriceBehavior}}` resolvido
     - Chamar `validatePrompts` com brief de cada intent
     - Verificar que retorna `{ valid: true, errors: [] }`
   - Teste: `validatePrompts falha quando prompt contém placeholders não resolvidos`
     - Mock `PromptLoader.load` retorna prompt contendo `{{unresolvedVar}}`
     - Verificar que `validatePrompts` detecta via `validatePrompt()` (placeholder check existente)
   - Teste: `validatePrompts falha quando reviewerVars tem variável contextual vazia`
     - Mock `imageReview.buildReviewPromptVariables` retorna vars com `expectedPriceBehavior: ""`
     - Verificar que `validatePrompts` detecta via required vars check (adicional da F31.3)
     - Este teste é separado do placeholder check — testa a verificação de completeza das variáveis contextuais
   - Teste: `validatePrompts falha se placeholder antigo no prompt`
     - Mock retorna prompt contendo `{{discountedPrice}}`
     - Verificar que `validatePrompts` retorna erro mencionando placeholder antigo
   - Teste: `validatePrompts valida diretor de imagem por intent`
     - Verificar que carrega `campaign-image-director-spotlight` para intent spotlight
     - Verificar que carrega `campaign-image-director-exclusive` para intent exclusive
</action>

<acceptance_criteria>
- Testes novos em `image-generation-service.test.ts` passam
- Cobertura: validatePrompts com 3 intents
- Cobertura: detecção de placeholder antigo
- Cobertura: detecção de variável contextual ausente
- `npm run typecheck` passa
</acceptance_criteria>

### Task 4: Teste de regressão — cenário offer com comportamento equivalente

<read_first>
- src/lib/image-generation/services/__tests__/image-review-service.test.ts
- openspec/changes/fase-31-3-quality-gate-por-intencao-comercial/specs/image-quality-review/spec.md (cenário offer regressão)
</read_first>

<action>
1. Adicionar teste de regressão:
   - Cenário: offer com `discountedPrice: "R$ 29,90"`, `badgeText: "Promoção"`
   - Verificar que o comportamento de offer é equivalente ao anterior:
     - `expectedBadgeBehavior` é obrigatório (não opcional)
     - `expectedPriceBehavior` contém o valor com desconto
     - `campaignIntentLabel` é "Promoção"
   - Este teste serve como garantia de que offer não foi afetado pelas mudanças.
</action>

<acceptance_criteria>
- Teste de regressão offer passa
- Comportamento de offer é equivalente ao anterior (badge obrigatório, preço esperado)
- `npm run typecheck` passa
</acceptance_criteria>

### Task 5: Suite completa — typecheck, lint, build

<read_first>
- src/__tests__/f31-3-review-quality-gate.test.ts
- src/lib/image-generation/services/__tests__/image-review-service.test.ts
- src/lib/image-generation/services/__tests__/image-generation-service.test.ts
</read_first>

<action>
1. Executar sequência completa de verificação:
   ```bash
   npx vitest run
   npm run typecheck
   npm run build
   ```
2. Corrigir qualquer erro encontrado:
   - Testes quebrados: atualizar mocks e asserts
   - Erros de tipo: ajustar interfaces
   - Erros de build: garantir que tudo compila
3. Registrar contagem final de testes.
</action>

<acceptance_criteria>
- `npx vitest run` — todos os testes passam (incluindo regressão)
- `npm run typecheck` — zero erros
- `npm run build` — build bem-sucedido
- Contagem de testes documentada no SUMMARY
</acceptance_criteria>

## Verification

- [ ] `npx vitest run` — todos os testes passam
- [ ] `npm run typecheck` — zero erros
- [ ] `npm run build` — build bem-sucedido
- [ ] Testes novos em 3 arquivos de teste

---
phase: 32-freemium-anti-abuso-cnpj
plan: 03
type: execute
wave: 2
depends_on:
  - 32-01
files_modified:
  - src/components/flow/store-identity-form.tsx
  - src/app/(app)/admin/users/[id]/page.tsx
  - src/app/(app)/admin/users/page.tsx
  - src/lib/admin/schemas.ts
autonomous: false
requirements:
  - F32-STORE-UI
  - F32-ADMIN
must_haves:
  truths:
    - "Store identity form em modo criação exibe campos CNPJ (required com máscara), Razão Social (optional), Nome Fantasia (optional)"
    - "Store identity form em modo edição NÃO exibe campos CNPJ"
    - "Admin user detail exibe CNPJ mascarado, badge freemium status, histórico entitlements, botão exceção"
    - "Admin user list exibe coluna CNPJ mascarado + filtro freemium status"
    - "AdminUserSummary estendido com cnpjMasked e freemiumStatus"
  artifacts:
    - path: "src/components/flow/store-identity-form.tsx"
      provides: "CNPJ, razão social, nome fantasia fields (create mode only)"
      contains: "cnpj"
    - path: "src/app/(app)/admin/users/[id]/page.tsx"
      provides: "CNPJ mascarado, badge freemium, histórico, botão exceção"
      contains: "maskCnpj"
    - path: "src/app/(app)/admin/users/page.tsx"
      provides: "Coluna CNPJ mascarado + filtro freemium"
      contains: "freemiumStatus"
    - path: "src/lib/admin/schemas.ts"
      provides: "AdminUserSummary extendido"
      contains: "cnpjMasked"
  key_links:
    - from: "src/components/flow/store-identity-form.tsx"
      to: "POST /api/store"
      via: "fetch POST /api/store com cnpj no body"
      pattern: "POST.*api/store"
    - from: "src/app/(app)/admin/users/[id]/page.tsx"
      to: "src/lib/freemium/entitlement-service.ts"
      via: "FreemiumEntitlementService.getHistoryByStore"
    - from: "src/app/(app)/admin/users/page.tsx"
      to: "admin_get_users_summary RPC"
      via: "RPC admin_get_users_summary estendido"
---

<objective>
**Plan 3: Store Identity Form + Admin CNPJ/Freemium UI**

Adicionar campos de CNPJ (com máscara frontend), razão social e nome fantasia ao formulário de identidade da loja (modo criação apenas). Estender páginas admin com CNPJ mascarado, badge de status freemium, histórico de entitlements, botão de exceção, e filtro de status.

**Purpose:** Experiência do lojista ao criar loja + ferramentas admin para monitorar freemium por CNPJ.
**Output:** Formulário com novos campos + Admin user detail + list estendidos.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-freemium-anti-abuso-cnpj/32-CONTEXT.md
@.planning/phases/32-freemium-anti-abuso-cnpj/32-01-SUMMARY.md
openspec/changes/fase-32-freemium-anti-abuso-cnpj/specs/store-identity-ui/spec.md
openspec/changes/fase-32-freemium-anti-abuso-cnpj/specs/admin-cnpj-display/spec.md
openspec/changes/fase-32-freemium-anti-abuso-cnpj/specs/admin-user-directory/spec.md

<interfaces>
From `src/lib/cnpj/mask.ts`:
```typescript
export function maskCnpj(normalized: string): string
// "12345678000190" → "**.***.***/0001-**"
```

From `src/lib/cnpj/` (for frontend validation):
```typescript
// normalize.ts
export function normalizeCnpj(raw: string): string

// validate.ts — backend-only; frontend usa mask pattern + regex simples
```

From `src/lib/freemium/entitlement-service.ts`:
```typescript
class FreemiumEntitlementService {
  getHistoryByStore(storeId: string): Promise<FreemiumEntitlement[]>
  getHistoryByRoot(rootHash: string): Promise<FreemiumEntitlement[]>
}
```

From `src/lib/admin/schemas.ts` (current):
```typescript
export interface AdminUserSummary {
  userId: string;
  email: string;
  storeId: string | null;
  storeName: string | null;
  segment: string | null;
  balance: number;
  totalCampaigns: number;
  errorCampaigns: number;
  lastCampaignAt: string | null;
  createdAt: string;
}
```

Current `src/components/flow/store-identity-form.tsx`:
- Uses `useStoreForm` hook (from `./use-store-form`)
- `FormData` type in use-store-form.ts: `{ name, segment, brand_color, city, state, subsegment, tone_of_voice, positioning, short_description, slogan }`
- `mode` is `"create" | "edit"` — `storeId === null` → create mode
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Store Identity Form — CNPJ, Razão Social, Nome Fantasia</name>
  <files>
    src/components/flow/store-identity-form.tsx
  </files>

  <read_first>
    src/components/flow/store-identity-form.tsx
    src/components/flow/use-store-form.ts
    openspec/changes/fase-32-freemium-anti-abuso-cnpj/specs/store-identity-ui/spec.md
  </read_first>

  <action>
    Modificar `src/components/flow/store-identity-form.tsx` para adicionar:

    **Campos CNPJ (only create mode — `!storeId`):**
    - Entre o campo "Nome da Loja" e o campo "Segmento" (na seção de Step 1, dentro do `<form>`):
    - Campo CNPJ: `<input>` com placeholder `"XX.XXX.XXX/YYYY-ZZ"`
      - Máscara via `onChange`: aplicar regex mask progressiva
      - Ao digitar, inserir automático: após 2 dígitos → ".", após 5 → ".", após 8 → "/", após 12 → "-"
      - Aceitar apenas dígitos (rejeitar letras)
      - `maxLength={18}` (tamanho do formato mascarado)
    - Validação frontend: ao onBlur, verificar se o valor tem 14 dígitos (após remover não-dígitos) — feedback visual com campo `fieldErrors.cnpj`
      - Se `normalizeCnpj(value).length !== 14` → `"CNPJ inválido"`
      - Mensagem exibida como `<p className="mt-1.5 flex items-center gap-1.5 text-accent-red text-xs">`
    - Campo Razão Social: `<input>` com `placeholder="Razão social (opcional)"`, `maxLength={200}`
    - Campo Nome Fantasia: `<input>` com `placeholder="Nome fantasia (opcional)"`, `maxLength={200}`
    - Ambos opcionais, sem validação de formato

    **Ordering in create mode:**
    ```
    Nome da Loja *
    CNPJ *          ← NEW (required, masked)
    Razão Social    ← NEW (optional)
    Nome Fantasia   ← NEW (optional)
    Segmento *
    Subsegmento
    Cidade / Estado
    ...
    ```

    **Data flow** (per D6):
    - Adicionar `cnpj`, `razaoSocial`, `nomeFantasia` ao estado local do formulário
    - No `handleStep1Submit`, ao chamar `save()`, passar os valores no body do POST:
    ```typescript
    body: {
      ...existingFields,
      cnpj: normalizeCnpj(formCnpj),  // envia apenas dígitos
      razaoSocial: formRazaoSocial || undefined,
      nomeFantasia: formNomeFantasia || undefined,
      acceptedTerms: acceptedTerms || undefined,
    }
    ```
    - **Nota:** O hook `useStoreForm` atual (em `use-store-form.ts`) monta o body no `save()` interno. Como o hook não gerencia CNPJ, a abordagem é:
      - Opção A: Estender `FormData` no hook para incluir `cnpj`, `razaoSocial`, `nomeFantasia`
      - Opção B: Gerenciar CNPJ como estado local no componente e passar via acceptedTerms no body do save (não ideal)
      - **Decisão:** Estender `FormData` em `use-store-form.ts` — adicionar `cnpj: string`, `razaoSocial: string`, `nomeFantasia: string` com valores default vazios. Atualizar `save()` para incluí-los no body se presentes.

    **Edit mode** (storeId presente):
    - Campos CNPJ NÃO são exibidos (per spec: "visíveis APENAS em modo criação")
    - Condicional: `{!storeId && (/* CNPJ fields */)}` no JSX

    **Estender use-store-form.ts:**
    - Adicionar `cnpj: ""`, `razaoSocial: ""`, `nomeFantasia: ""` ao `FormData` type e `EMPTY_FORM`
    - No `save()`, incluir no body:
      ```typescript
      if (formData.cnpj) body.cnpj = normalizeCnpj(formData.cnpj);
      if (formData.razaoSocial) body.razaoSocial = formData.razaoSocial;
      if (formData.nomeFantasia) body.nomeFantasia = formData.nomeFantasia;
      ```
    - Se `initialStore` presente (edit mode), carregar valores se existirem:
      ```typescript
      cnpj: (initialStore as any).cnpj_normalized ?? "",
      razaoSocial: (initialStore as any).razao_social ?? "",
      nomeFantasia: (initialStore as any).nome_fantasia ?? "",
      ```
      Mas como em edit mode os campos não são exibidos, esses valores são only stored para idempotência.

    **IMPORTANTE:** A máscara CNPJ no frontend é apenas visual. O valor enviado ao backend é `normalizeCnpj(cnpj)` (apenas dígitos). O backend valida novamente.
  </action>

  <verify>
    <automated>MISSING — Testes no Plan 5 (32-05). Verificar: `npx tsc --noEmit` passa. Build: `npm run build` passa.</automated>
  </verify>

  <acceptance_criteria>
    - Formulário em modo criação exibe campo CNPJ com máscara `XX.XXX.XXX/YYYY-ZZ`
    - Máscara é aplicada progressivamente enquanto o usuário digita
    - Campo CNPJ com `required` visual (asterisco) e validação no onBlur
    - Campos Razão Social e Nome Fantasia visíveis em modo criação, opcionais
    - Em modo edição (storeId não nulo), campos CNPJ não aparecem
    - `use-store-form.ts` FormData estendido com cnpj, razaoSocial, nomeFantasia
    - `save()` envia `cnpj` (normalizado), `razaoSocial`, `nomeFantasia` no body POST
    - `npx tsc --noEmit` passa sem erros
  </acceptance_criteria>

  <done>Store identity form com CNPJ masked field + razão social + nome fantasia. use-store-form hook estendido. Build limpo.</done>
</task>

<task type="auto">
  <name>Task 2: Admin User Detail — CNPJ mascarado, badge freemium, exceção + Admin User List — coluna CNPJ + filtro</name>
  <files>
    src/app/(app)/admin/users/[id]/page.tsx
    src/app/(app)/admin/users/page.tsx
    src/lib/admin/schemas.ts
  </files>

  <read_first>
    src/app/(app)/admin/users/[id]/page.tsx
    src/app/(app)/admin/users/page.tsx
    src/lib/admin/schemas.ts
    src/lib/cnpj/mask.ts
    src/lib/freemium/entitlement-service.ts
    src/lib/admin/require-admin.ts
    openspec/changes/fase-32-freemium-anti-abuso-cnpj/specs/admin-cnpj-display/spec.md
    openspec/changes/fase-32-freemium-anti-abuso-cnpj/specs/admin-user-directory/spec.md
  </read_first>

  <action>
    **Part A: AdminUserSummary extension (schemas.ts)**

    Adicionar a `AdminUserSummary` interface:
    ```typescript
    cnpjMasked: string | null;
    freemiumStatus: "active" | "used" | "exhausted" | "no_cnpj";
    ```

    **Part B: Admin User List (page.tsx)**

    Modificar a página de listagem `/admin/users`:

    - Adicionar coluna "CNPJ" na tabela desktop (após "Segmento", antes de "Saldo"):
      ```html
      <th className="px-3 py-2 text-left font-medium">CNPJ</th>
      ```
      ```html
      <td className="px-3 py-2 text-xs font-mono">{user.cnpjMasked ?? "—"}</td>
      ```

    - Adicionar CNPJ no mobile card layout (após segmento):
      `<span className="text-xs text-muted-foreground">{user.cnpjMasked ?? "—"}</span>`

    - Adicionar filtro dropdown de status freemium no `<form>` de busca:
      ```html
      <select name="freemiumStatus" defaultValue="" className="rounded-md border border-border bg-bg-surface px-3 py-2 text-sm">
        <option value="">Todos</option>
        <option value="no_cnpj">Sem CNPJ</option>
        <option value="active">Freemium ativo</option>
        <option value="used">Freemium usado</option>
      </select>
      ```
      - Extrair `freemiumStatus` de `searchParams`
      - Passar `p_freemium_status` para a RPC `admin_get_users_summary` (a RPC precisa ser estendida — ver abaixo)

    - **Estender RPC `admin_get_users_summary`:** (opção: fazer via query lateral)
      - Como a migration já foi criada no Plan 1, e não queremos criar nova migration, podemos:
        1. Adicionar a lógica de freemium status no server component (não na RPC): após carregar os summaries, buscar entitlements e calcular status
        2. Ou modificar a RPC via migration complementar
      - **Decisão (simples):** Não modificar a RPC na migration. No server component, após carregar os summaries, buscar `freemium_entitlements` para as stores carregadas e enriquecer com cnpjMasked e freemiumStatus. Isso evita criar nova migration.
      - Para o filtro, fazer filtering no server-side após carregar os dados (para volume pequeno de users).

    **Part C: Admin User Detail (page.tsx)**

    Modificar a página de detalhe `/admin/users/[id]`:

    - Novo import: `import { maskCnpj } from "@/lib/cnpj/mask"`
    - Novo import: `import { FreemiumEntitlementService } from "@/lib/freemium/entitlement-service"`
    - Instanciar: `const freemiumService = new FreemiumEntitlementService()`

    - Adicionar seção "CNPJ e Freemium" (após "Dados da Loja", antes de "Situação Legal"):
      ```tsx
      <div className="rounded-md border border-border bg-bg-surface p-4">
        <h2 className="text-lg font-semibold mb-3">CNPJ e Freemium</h2>
        <dl className="grid grid-cols-2 gap-2 text-sm">
          <dt className="text-muted-foreground">CNPJ</dt>
          <dd className="font-mono">
            {storeData.cnpj_normalized
              ? maskCnpj(storeData.cnpj_normalized as string)
              : <span className="text-muted-foreground">—</span>}
          </dd>
          <dt className="text-muted-foreground">Status Freemium</dt>
          <dd>
            <FreemiumBadge status={freemiumStatus} />
          </dd>
        </dl>

        {/* Histórico de entitlements */}
        {entitlements.length > 0 && (
          <div className="mt-4">
            <h3 className="text-sm font-semibold mb-2">Histórico de Entitlements</h3>
            <table className="w-full text-sm">
              <thead className="bg-muted">
                <tr>
                  <th className="px-2 py-1 text-left">Benefício</th>
                  <th className="px-2 py-1 text-left">Ciclo</th>
                  <th className="px-2 py-1 text-left">Motivo</th>
                  <th className="px-2 py-1 text-right">Data</th>
                </tr>
              </thead>
              <tbody>
                {entitlements.map(e => (
                  <tr key={e.id} className="border-t">
                    <td className="px-2 py-1">{e.benefit_type}</td>
                    <td className="px-2 py-1">{e.cycle ?? "—"}</td>
                    <td className="px-2 py-1">{e.reason ?? "—"}</td>
                    <td className="px-2 py-1 text-right text-xs">
                      {new Date(e.created_at).toLocaleString("pt-BR")}
                    </td>
                  </tr>
                ))}
              </tbody>
            </table>
          </div>
        )}

        {/* Botão Conceder Exceção */}
        <div className="mt-4 flex items-center gap-3">
          <button
            type="button"
            onClick={handleGrantException}
            className="rounded-md bg-accent-blue px-3 py-1.5 text-xs font-medium text-white hover:brightness-110 transition-all"
          >
            Conceder exceção
          </button>
        </div>
      </div>
      ```

    - **FreemiumBadge:** Componente inline que renderiza:
      - `active` → `🟢 Freemium ativo`
      - `used` → `🟡 Freemium usado`
      - `exhausted` → `🔴 Freemium esgotado`
      - `no_cnpj` → `⚪ Sem CNPJ`

    - **Calcular freemiumStatus** server-side (dentro do componente):
      ```typescript
      import { FreemiumEntitlementService } from "@/lib/freemium/entitlement-service";
      const freemiumService = new FreemiumEntitlementService();

      // Após carregar store:
      let freemiumStatus: FreemiumStatus = "no_cnpj";
      let entitlements: FreemiumEntitlement[] = [];

      if (storeData?.id) {
        const storeId = storeData.id as string;
        const rootHash = storeData.cnpj_root_hash as string ?? "";
        entitlements = await freemiumService.getHistoryByStore(storeId);

        if (rootHash && rootHash !== "") {
          const hasOnboarding = entitlements.some(e => e.benefit_type === "onboarding");
          const hasMonthly = entitlements.some(e => e.benefit_type === "monthly");
          const balance = await creditService.getBalance(storeId);

          if (hasOnboarding && balance > 0) freemiumStatus = "active";
          else if (hasOnboarding || hasMonthly) freemiumStatus = "used";
          else freemiumStatus = "exhausted";
        }
      }
      ```

    - **Botão "Conceder exceção":** Implementar como Client Component separado ou modal.
      - Criar `FreemiumExceptionModal` client component em `src/components/admin/freemium-exception-modal.tsx`:
        - Input de reason (obrigatório, min 10 chars)
        - Botão "Conceder" que chama POST `/api/admin/freemium/exception` passando `{ storeId, reason }`
        - Controle de loading/estado
      - Criar rota `POST /api/admin/freemium/exception` (no Plan 4 ou 5? Incluir aqui):
        - `requireAdmin()`, valida body, chama supabaseAdmin.rpc para INSERT freemium_entitlements com benefit_type='admin_exception' + grant_credits
        - Registra em `admin_audit_log`
      - **Decisão:** A rota admin exception será criada no **Plan 4** (que já lida com admin routes e RPCs). Aqui, criar o modal/client component apenas.

    **Observação:** A estes components (exception modal, badge) podem ser criados inline ou em arquivos separados. Preferir inline para simplicidade, extrair se ficar grande demais.
  </action>

  <verify>
    <automated>MISSING — Testes no Plan 5 (32-05). Verificar: `npx tsc --noEmit` passa. Build: `npm run build` passa.</automated>
  </verify>

  <acceptance_criteria>
    - `AdminUserSummary` extendido com `cnpjMasked: string | null` e `freemiumStatus`
    - Admin user list exibe coluna "CNPJ" com valor mascarado
    - Admin user list tem filtro dropdown de status freemium (Todos / Sem CNPJ / Freemium ativo / Freemium usado)
    - Admin user detail exibe CNPJ mascarado via `maskCnpj()`
    - Admin user detail exibe badge de status freemium com 4 variantes (ativo/usado/esgotado/sem CNPJ)
    - Admin user detail exibe histórico de entitlements (tabela)
    - Botão "Conceder exceção" presente no detail (abre modal com reason obrigatório)
    - `npx tsc --noEmit` passa sem erros
  </acceptance_criteria>

  <done>Admin pages estendidas com CNPJ mascarado, freemium status badges/histórico/filtro, e botão de exceção.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser input→form state | CNPJ digitado pelo usuário, mascarado visualmente |
| form→API | CNPJ normalizado (14 dígitos) enviado via HTTPS |
| admin page→render | CNPJ mascarado via maskCnpj, nunca cru |
| admin exception→API | Reason enviado via POST, auditado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-10 | Information Disclosure | admin CNPJ display | mitigate | maskCnpj aplicado em toda exibição. Raw CNPJ nunca no HTML |
| T-32-11 | Elevation of Privilege | admin exception button | mitigate | requireAdmin() antes de qualquer ação. Reason obrigatório |
| T-32-12 | Tampering | form CNPJ data | accept | Máscara é visual. Backend valida novamente com validateCnpj |
</threat_model>

<verification>
- [ ] Formulário modo criação: CNPJ com máscara XX.XXX.XXX/YYYY-ZZ
- [ ] Formulário modo criação: Razão Social e Nome Fantasia opcionais
- [ ] Formulário modo edição: CNPJ fields NÃO exibidos
- [ ] use-store-form.ts: FormData extendido com cnpj, razaoSocial, nomeFantasia
- [ ] AdminUserSummary: cnpjMasked + freemiumStatus adicionados
- [ ] Admin list: coluna CNPJ (mascarado ou —)
- [ ] Admin list: filtro freemium status
- [ ] Admin detail: CNPJ mascarado via maskCnpj()
- [ ] Admin detail: badge status freemium
- [ ] Admin detail: tabela histórico entitlements
- [ ] Admin detail: botão "Conceder exceção"
- [ ] npx tsc --noEmit limpo
</verification>

<success_criteria>
- Store identity form com campos CNPJ (masked), razão social, nome fantasia (create only)
- Admin user list com CNPJ coluna + freemium filter
- Admin user detail com CNPJ masked, freemium badge, entitlement history, exception button
- Build TypeScript limpo
</success_criteria>

<output>
Create `.planning/phases/32-freemium-anti-abuso-cnpj/32-03-SUMMARY.md` when done
</output>

---
phase: 32-freemium-anti-abuso-cnpj
plan: 05
type: execute
wave: 3
depends_on:
  - 32-01
  - 32-02
  - 32-03
  - 32-04
files_modified:
  - src/lib/cnpj/__tests__/validate.test.ts
  - src/lib/cnpj/__tests__/normalize.test.ts
  - src/lib/cnpj/__tests__/hash.test.ts
  - src/lib/cnpj/__tests__/mask.test.ts
  - src/lib/cnpj/__tests__/similarity.test.ts
  - src/lib/freemium/__tests__/entitlement-service.test.ts
  - src/app/api/store/__tests__/route.test.ts
  - src/app/(app)/admin/users/__tests__/admin-cnpj.test.ts
autonomous: false
requirements:
  - F32-TESTS
must_haves:
  truths:
    - "30+ testes novos passando (validação CNPJ 10+, entitlement 8+, store route 7+, legado 4+, integração 2+)"
    - "Full regression: ~1071+3N = ~1101+ testes passando"
    - "TypeScript sem erros"
    - "Lint sem erros"
    - "Build bem-sucedido"
    - "CNPJ não aparece cru em logs, URLs ou responses"
  artifacts:
    - path: "src/lib/cnpj/__tests__/validate.test.ts"
      provides: "7+ testes de validação CNPJ"
    - path: "src/lib/cnpj/__tests__/normalize.test.ts"
      provides: "2+ testes de normalização"
    - path: "src/lib/cnpj/__tests__/hash.test.ts"
      provides: "2+ testes de hash"
    - path: "src/lib/cnpj/__tests__/mask.test.ts"
      provides: "2+ testes de mascaramento"
    - path: "src/lib/cnpj/__tests__/similarity.test.ts"
      provides: "2+ testes de similaridade"
    - path: "src/lib/freemium/__tests__/entitlement-service.test.ts"
      provides: "8+ testes de entitlement"
    - path: "src/app/api/store/__tests__/route.test.ts"
      provides: "7+ testes de store route + 4+ legado + 2+ integração"
  key_links:
    - from: "src/lib/cnpj/__tests__/hash.test.ts"
      to: "process.env.CNPJ_PEPPER"
      via: "Mock process.env antes de cada teste"
      pattern: "CNJ_PEPPER"
---

<objective>
**Plan 5: Tests + Verification**

Criar todos os testes para a F32 (CNPJ unit, freemium entitlement, store route integration, legacy, admin) e executar verificação final: typecheck, lint, build, regressão completa.

**Purpose:** Garantir que todas as funcionalidades da F32 funcionam corretamente e não quebram nada existente.
**Output:** 30+ testes novos, regressão completa (~1101+ tests), verificação de segurança.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-freemium-anti-abuso-cnpj/32-01-SUMMARY.md
@.planning/phases/32-freemium-anti-abuso-cnpj/32-02-SUMMARY.md
@.planning/phases/32-freemium-anti-abuso-cnpj/32-03-SUMMARY.md
@.planning/phases/32-freemium-anti-abuso-cnpj/32-04-SUMMARY.md
openspec/changes/fase-32-freemium-anti-abuso-cnpj/tasks.md

<interfaces>
Existing test patterns:
- `src/lib/cnpj/` unit tests use `describe` / `it` / `expect` (vitest)
- Mock pattern for supabase: `vi.mock("@/lib/supabase/server", () => ({ supabaseAdmin: mockAdmin }))`
- Mock pattern for env: `vi.stubEnv("CNPJ_PEPPER", "test_pepper_hex_64_chars...")`
- Store route tests: `src/app/api/store/__tests__/route.test.ts` (examine if exists)

From `tasks.md` test checklist:
- Task 9: 10+ testes (validate 7+, normalize 2+, hash 2+, mask 2+, similarity 2+)
- Task 10: 8+ testes (entitlement service)
- Task 11: 7+ store route + 4+ legacy + 2+ integration
- Task 12: Verification run (typecheck, lint, test, build, security audit)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CNPJ Unit Tests + Freemium Entitlement Unit Tests</name>
  <files>
    src/lib/cnpj/__tests__/validate.test.ts
    src/lib/cnpj/__tests__/normalize.test.ts
    src/lib/cnpj/__tests__/hash.test.ts
    src/lib/cnpj/__tests__/mask.test.ts
    src/lib/cnpj/__tests__/similarity.test.ts
    src/lib/freemium/__tests__/entitlement-service.test.ts
  </files>

  <read_first>
    src/lib/cnpj/validate.ts
    src/lib/cnpj/normalize.ts
    src/lib/cnpj/hash.ts
    src/lib/cnpj/mask.ts
    src/lib/cnpj/similarity.ts
    src/lib/freemium/entitlement-service.ts
    openspec/changes/fase-32-freemium-anti-abuso-cnpj/specs/cnpj-validation/spec.md
    openspec/changes/fase-32-freemium-anti-abuso-cnpj/specs/freemium-entitlement/spec.md
    openspec/changes/fase-32-freemium-anti-abuso-cnpj/tasks.md
  </read_first>

  <action>
    **Criar diretório `src/lib/cnpj/__tests__/` e 5 arquivos de teste:**

    **validate.test.ts** (7+ cenários — per spec scenarios):
    - Test 1: `validateCnpj("12.345.678/0001-90")` → `{ normalized: "12345678000190" }` (válido com pontuação)
    - Test 2: `validateCnpj("12345678000190")` → `{ normalized: "12345678000190" }` (válido só dígitos)
    - Test 3: `validateCnpj("12.345.678/0001-00")` → `Error("CNPJ inválido")` (dígitos inválidos)
    - Test 4: `validateCnpj("12.345.678/0001")` → `Error("CNPJ deve ter 14 dígitos")` (comprimento)
    - Test 5: `validateCnpj("12.345.678/0001-9000")` → `Error("CNPJ deve ter 14 dígitos")` (comprimento)
    - Test 6: `validateCnpj("11.111.111/0001-11")` → `Error("CNPJ inválido")` (sequência conhecida)
    - Test 7: `validateCnpj("00.000.000/0001-00")` → `Error("CNPJ inválido")` (sequência conhecida)
    - Test 8: `validateCnpj("")` → `Error("CNPJ deve ter 14 dígitos")` (vazio)
    - Test 9: `validateCnpj("AB.CDE.FGH/0001-00")` → `Error("CNPJ deve ter 14 dígitos")` (letras)

    **normalize.test.ts** (2+ cenários):
    - Test 1: `normalizeCnpj("12.345.678/0001-90")` → `"12345678000190"`
    - Test 2: `normalizeCnpj("12.345.678/0001-90-abc")` → `"12345678000190"` (remove letras também)

    **hash.test.ts** (2+ cenários):
    - Mock `process.env.CNPJ_PEPPER` com `vi.stubEnv`
    - Test 1: `hashCnpjRoot("12345678")` retorna string hex de 64 caracteres
    - Test 2: mesma chamada com mesmo pepper retorna mesmo hash (determinístico)
    - Test 3: hash muda se pepper muda (usar `vi.stubEnv` para alterar pepper entre chamadas)

    **mask.test.ts** (2+ cenários):
    - Test 1: `maskCnpj("12345678000190")` → `"**.***.***/0001-**"`
    - Test 2: `maskCnpj("22345678000211")` → `"**.***.***/0002-**"` (sufixo preservado)

    **similarity.test.ts** (2+ cenários):
    - Test 1: `compareBusinessName("Loja ABC LTDA", "Loja ABC LTDA")` → score >= 0.8, label 'match'
    - Test 2: `compareBusinessName("Minha Loja", "ABC Comércio Ltda")` → score < 0.8, label 'mismatch', sem erro

    **Criar `src/lib/freemium/__tests__/entitlement-service.test.ts`** (8+ cenários):
    - Mock do `supabaseAdmin` com `vi.mock`
    - Test 1: `checkOnboardingEligibility("new_hash")` retorna true (sem entitlement existente)
    - Test 2: `checkOnboardingEligibility("existing_hash")` retorna false (entitlement existe)
    Test 3: `grantOnboardingEntitlement("store-1", "hash1")` retorna UUID (primeira vez)
    - Test 4: `grantOnboardingEntitlement("store-1", "hash1")` retorna null (idempotente, ON CONFLICT)
    - Test 5: `checkMonthlyEligibility("hash", "2026-08")` retorna true (sem grant no ciclo)
    - Test 6: `checkMonthlyEligibility("hash", "2026-08")` retorna false (já recebeu)
    - Test 7: `grantMonthlyEntitlement("store-1", "hash", "2026-08")` retorna UUID (primeira vez)
    - Test 8: `getHistoryByStore("store-1")` retorna array de entitlements
    - Test 9: `getHistoryByRoot("hash")` retorna array de entitlements

    **Padrão para mocks:**
    ```typescript
    import { describe, it, expect, vi, beforeEach } from "vitest";
    import { supabaseAdmin } from "@/lib/supabase/server";
    vi.mock("@/lib/supabase/server", () => ({
      supabaseAdmin: { /* mock methods */ },
    }));
    ```

    **Rodar testes:**
    - `npx vitest run src/lib/cnpj/__tests__/` — 10+ tests passando
    - `npx vitest run src/lib/freemium/__tests__/` — 8+ tests passando
  </action>

  <behavior>
    - Test 1: validateCnpj com pontuação → { normalized } sem erro
    - Test 2: validateCnpj dígitos inválidos → Error
    - Test 3: validateCnpj sequência → Error
    - Test 4: normalizeCnpj → somente dígitos
    - Test 5: hashCnpjRoot → hex string 64 caracteres
    - Test 6: maskCnpj → formato **.***.***/YYYY-**
    - Test 7: compareBusinessName match → score >= 0.8
    - Test 8: compareBusinessName mismatch → score < 0.8 (sem erro)
    - Test 9: grantOnboardingEntitlement first → UUID
    - Test 10: grantOnboardingEntitlement second → null (idempotência)
    - Test 11: checkOnboardingEligibility → true/false
    - Test 12: checkMonthlyEligibility → true/false
  </behavior>

  <verify>
    <automated>npx vitest run src/lib/cnpj/__tests__/ src/lib/freemium/__tests__/ --reporter=verbose</automated>
  </verify>

  <acceptance_criteria>
    - 5+ arquivos de teste em src/lib/cnpj/__tests__/
    - 1+ arquivo de teste em src/lib/freemium/__tests__/
    - validate.test.ts: 7+ testes passando
    - normalize.test.ts: 2+ testes passando
    - hash.test.ts: 2+ testes passando
    - mask.test.ts: 2+ testes passando
    - similarity.test.ts: 2+ testes passando
    - entitlement-service.test.ts: 8+ testes passando
    - Total: 20+ testes novos na lib
  </acceptance_criteria>

  <done>CNPJ + freemium unit tests criados e passando. 20+ testes novos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration Tests — Store Route, Legacy, Admin Exception, Monthly Cron</name>
  <files>
    src/app/api/store/__tests__/route.test.ts
  </files>

  <read_first>
    src/app/api/store/route.ts
    src/lib/cnpj/validate.ts
    src/lib/cnpj/mask.ts
    openspec/changes/fase-32-freemium-anti-abuso-cnpj/specs/store-ownership-api/spec.md
    openspec/changes/fase-32-freemium-anti-abuso-cnpj/specs/legacy-store-cnpj-update/spec.md
    openspec/changes/fase-32-freemium-anti-abuso-cnpj/tasks.md
  </read_first>

  <action>
    **Criar `src/app/api/store/__tests__/route.test.ts`** (13+ cenários) — mockar `requireUser`, `supabase`, RPCs, e validar responses HTTP.

    Se o diretório `__tests__` não existir em `src/app/api/store/`, usar `src/app/api/store/__tests__/route.test.ts`.

    **Store Route Tests** (7+ cenários):
    - Mock `requireUser` retornando `{ userId: "user-1" }`
    - Mock `supabase.rpc("create_store_with_cnpj", ...)` retornando `{ data: { id: "store-1", ... }, error: null }`
    - Mock `validateCnpj` import (já testado unitariamente)

    Test 1: CNPJ válido, primeira raiz → 201 + `onboardingGranted: true`
    Test 2: Mesma raiz, diferente CNPJ → 201 + `onboardingGranted: false` (mock ON CONFLICT)
    Test 3: CNPJ inválido → 400 `{ error: "CNPJ inválido" }`
    Test 4: CNPJ duplicado (já cadastrado) → 409 `{ error: "Este CNPJ já está cadastrado em outra conta" }`
    Test 5: Sem CNPJ no body → 400 `{ error: "CNPJ é obrigatório" }`
    Test 6: Filial (mesma raiz, CNPJ dif) → 201 + `onboardingGranted: false`
    Test 7: Mesmo CNPJ, diferente user → 409 (já cadastrado)

    **Legacy Store Tests** (4+ cenários):
    Test 8: Loja legacy atualiza CNPJ → sucesso, saldo intacto, grant não concedido
    Test 9: Cron ignora loja sem cnpj_root_hash (via mock do grant_monthly_credits)
    Test 10: Tentativa de sobrescrever CNPJ existente → erro (RPC retorna cnpj_already_set)
    Test 11: Mesma raiz em duas lojas legacy → primeira vence, segunda ignorada (ON CONFLICT)

    **Integration Tests** (2+ cenários):
    Test 12: Cron mensal: 3 filiais + 1 matriz da mesma raiz = 1 grant apenas
    Test 13: Loja deletada + recriada com mesma raiz → sem novo onboarding (ON DELETE SET NULL)

    **Padrão de mock** (seguir testes existentes):
    ```typescript
    import { describe, it, expect, vi, beforeEach } from "vitest";
    import { POST } from "../route";
    import { NextRequest } from "next/server";

    vi.mock("@/lib/auth/require-user", () => ({
      requireUser: vi.fn().mockResolvedValue({ userId: "test-user-id" }),
      requireApiUser: vi.fn(),
      UnauthorizedError: class extends Error {},
    }));

    vi.mock("@/lib/supabase/server", () => ({
      supabaseAdmin: { rpc: vi.fn(), from: vi.fn(() => ({ select: vi.fn() })) },
      default: null,
    }));
    ```

    **Rodar testes:**
    - `npx vitest run src/app/api/store/__tests__/` — 13+ tests passando
  </action>

  <behavior>
    - Test 1: POST /api/store com CNPJ válido → 201, onboardingGranted: true
    - Test 2: POST /api/store mesma raiz → 201, onboardingGranted: false
    - Test 3: POST /api/store CNPJ inválido → 400
    - Test 4: POST /api/store sem CNPJ → 400
    - Test 5: POST /api/store CNPJ duplicado → 409
    - Test 6: update_store_cnpj → sucesso sem grant
    - Test 7: update_store_cnpj sobrescrita → erro
    - Test 8: cron mensal → 1 grant por raiz
  </behavior>

  <verify>
    <automated>npx vitest run src/app/api/store/__tests__/ --reporter=verbose</automated>
  </verify>

  <acceptance_criteria>
    - `src/app/api/store/__tests__/route.test.ts` criado com 13+ testes
    - Testes de store route: 7+ cenários (CNPJ válido, mesma raiz, inválido, duplicado, filial, sem CNPJ)
    - Testes de legado: 4+ cenários (atualização sem grant, cron ignora, sobrescrita, mesma raiz)
    - Testes de integração: 2+ cenários (cron 1x raiz, loja deletada+recriada)
    - Todos passando com mocks
  </acceptance_criteria>

  <done>Integration tests criados: 13+ cenários de store route, legado e integração. Todos passando.</done>
</task>

<task type="auto">
  <name>Task 3: Verification — Typecheck, Lint, Full Test Run, Build, Security Audit</name>
  <files>
    <!-- No new files — just verification runs -->
  </files>

  <read_first>
    openspec/changes/fase-32-freemium-anti-abuso-cnpj/tasks.md
    openspec/changes/fase-32-freemium-anti-abuso-cnpj/design.md
  </read_first>

  <action>
    **Executar verificação completa em ordem:**

    1. **TypeScript check:** `npx tsc --noEmit`
       - Se houver erros, corrigir (tipagem, imports, null checks)
       - Zero erros

    2. **Lint:** `npm run lint`
       - Se houver warnings/errors, corrigir
       - Zero erros (warnings aceitos apenas se justificados)

    3. **Full test run:** `npx vitest run`
       - Contar total: ~1071 existentes + 30+ novos = ~1101+
       - Todos passando
       - Se algum teste existente quebrou, diagnosticar e corrigir

    4. **Build:** `npm run build`
       - Build bem-sucedido sem erros

    5. **Security audit — CNPJ exposure check:**
       - `rg "cnpj_normalized" --type ts --files | xargs rg "cnpj_normalized" | grep -v "mask\|test\|spec\|__tests__\|migration\|\.sql"` — CNPJ raw só aparece em migration SQL e mask.ts
       - `rg "cnpj_root_hash" --type ts --files` — root_hash aparece em logs, API internas, admin
       - `rg "CNPJ_PEPPER" --type ts --files` — apenas em hash.ts
       - Verificar que nenhuma response pública retorna `cnpj_normalized` cru (apenas `cnpjMasked`)
       - Verificar que nenhum console.log ou logger registra `cnpj_normalized` cru

    6. **Summary:** Contar todos os testes novos:
       ```
       CNPJ validate: N tests
       CNPJ normalize: N tests
       CNPJ hash: N tests
       CNPJ mask: N tests
       CNPJ similarity: N tests
       Freemium entitlement: N tests
       Store route + integration: N tests
       Total: N tests
       ```

    **Correção de regressão:**
    - Se testes existentes quebrarem (e.g., por mudança na RPC `create_store_with_legal_acceptance` → `create_store_with_cnpj`), atualizar os mocks/testes afetados
    - Se o build falhar por mudanças de tipo, ajustar
    - Se o lint apontar problemas, corrigir
  </action>

  <verify>
    <automated>npx vitest run && npx tsc --noEmit && npm run lint && npm run build</automated>
  </verify>

  <acceptance_criteria>
    - `npx tsc --noEmit` → zero erros
    - `npm run lint` → zero erros (warnings aceitos se justificados)
    - `npx vitest run` → 1100+ testes passando (1071 existentes + 30+ novos)
    - `npm run build` → bem-sucedido
    - CNPJ audit: raw CNPJ nunca em log, URL, response pública ou client-side
    - Root hash usa HMAC-SHA256 com pepper (confirmado por código hash.ts)
  </acceptance_criteria>

  <done>Verificação completa: typecheck, lint, 1100+ testes, build, security audit — todos limpos. F32 completa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test env→code | Testes mockam supabase, env vars, requireUser |
| security audit→codebase | Varredura por exposição indevida de CNPJ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-17 | Information Disclosure | test output | mitigate | Testes nunca logam CNPJ real. Usar CNPJs de teste 12.345.678/0001-90 (inválido nos dígitos) |
| T-32-18 | Information Disclosure | log/console | mitigate | Security audit verifica zero exposição de raw CNPJ em logs |
</threat_model>

<verification>
- [ ] CNPJ unit tests: 10+ passando (validate 7+, normalize 2+, hash 2+, mask 2+, similarity 2+)
- [ ] Freemium unit tests: 8+ passando
- [ ] Integration tests: 13+ passando (route 7+, legacy 4+, integration 2+)
- [ ] Total novos: 30+ testes
- [ ] npx tsc --noEmit: zero erros
- [ ] npm run lint: zero erros
- [ ] npx vitest run: 1100+ passando
- [ ] npm run build: bem-sucedido
- [ ] Security audit: CNPJ não aparece cru em logs, URLs, responses públicas
</verification>

<success_criteria>
- 30+ testes novos passando
- Regressão completa (~1101+ testes)
- TypeScript, lint, build limpos
- Security audit: nenhuma exposição indevida de CNPJ
- F32 pronta para merge
</success_criteria>

<output>
Create `.planning/phases/32-freemium-anti-abuso-cnpj/32-05-SUMMARY.md` when done
</output>

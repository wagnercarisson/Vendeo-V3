---
phase: 33-verificacao-cnpj-freemium
plan: 05
type: execute
wave: 3
depends_on:
  - 33-01 (Core Libraries + Services)
  - 33-02 (Store Route + Grant)
  - 33-03 (Risk Service + Form + Dashboard)
  - 33-04 (Admin Reviews + Test Stores)
files_modified:
  - src/lib/cnpj/lookup-providers/__tests__/brasil-api.test.ts
  - src/lib/cnpj/lookup-providers/__tests__/cnpja.test.ts
  - src/lib/cnpj/__tests__/verification-service.test.ts
  - src/lib/freemium/__tests__/freemium-risk-service.test.ts
  - src/app/api/store/__tests__/route.test.ts
  - src/components/flow/__tests__/store-identity-form.test.tsx
  - src/app/api/admin/reviews/__tests__/route.test.ts
  - src/app/api/admin/stores/__tests__/create-test.test.ts
  - src/lib/freemium/__tests__/freemium-risk-service.test.ts
autonomous: false
requirements:
  - F33-TESTS
  - F33-VERIFICATION
must_haves:
  truths:
    - "Todos os testes das ondas 1, 2 e 3 passam"
    - "Testes de integração cobrem 5 fluxos E2E"
    - "Verificação final — typecheck, lint, build, vitest — zero erros"
    - "CNPJ não aparece cru em logs, URLs, listagens admin ou responses públicas"
    - "Root hash continua usando HMAC-SHA256 com pepper server-side"
    - "Store de teste não recebe freemium automático"
    - "Todas as ações admin registradas em admin_audit_log"
  artifacts:
    - path: "src/lib/cnpj/lookup-providers/__tests__/brasil-api.test.ts"
      provides: "5+ testes: sucesso, not_found, timeout, rate limit, 5xx, malformed"
      min_lines: 50
    - path: "src/lib/freemium/__tests__/freemium-risk-service.test.ts"
      provides: "10+ testes: approve, reject (4 variantes), review (4 variantes), defer"
      min_lines: 80
    - path: "src/app/api/admin/reviews/__tests__/route.test.ts"
      provides: "7+ testes: list, approve, reject, exception, reveal-cnpj"
      min_lines: 60
---

<objective>
**Plan 5: Tests + Verification**

Implementar todos os testes da F33: testes unitários para lookup providers, verification service, risk service; testes de integração para store route, admin reviews, store de teste; testes E2E de 5 fluxos completos; e verificação final (typecheck, lint, build).

**Output:** ~50+ novos testes + verificação final passando.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/33-verificacao-cnpj-freemium/33-CONTEXT.md
openspec/changes/fase-33-verificacao-cnpj-freemium/tasks.md

<interfaces>
## Existing Test Patterns (from F32)

- Vitest com describe/it
- Mocks de fetch global para chamadas externas
- Testes de serviço com Supabase client mockado
- Testes de rota com NextRequest mock
- Diretórios __tests__ junto ao módulo testado
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 13: Tests — Wave 1 (Lookup + Verification)</name>
  <files>
    src/lib/cnpj/lookup-providers/__tests__/brasil-api.test.ts
    src/lib/cnpj/lookup-providers/__tests__/cnpja.test.ts
    src/lib/cnpj/__tests__/verification-service.test.ts
  </files>

  <read_first>
    openspec/changes/fase-33-verificacao-cnpj-freemium/specs/cnpj-lookup-provider/spec.md
    openspec/changes/fase-33-verificacao-cnpj-freemium/specs/cnpj-verification-service/spec.md
    src/lib/cnpj/lookup-providers/brasil-api.ts
    src/lib/cnpj/lookup-providers/cnpja.ts
    src/lib/cnpj/verification-service.ts
  </read_first>

  <action>
    Criar testes:

    1. src/lib/cnpj/lookup-providers/__tests__/brasil-api.test.ts (5+):
       - CNPJ válido retorna resolved com dados
       - CNPJ inexistente (404) retorna not_found
       - Timeout (5s) retorna unavailable
       - Rate limit (429) retorna unavailable
       - Erro 5xx retorna unavailable
       - Resposta malformada retorna unavailable

    2. src/lib/cnpj/lookup-providers/__tests__/cnpja.test.ts (5+):
       - CNPJ válido retorna resolved
       - CNPJ inexistente retorna not_found
       - Timeout retorna unavailable
       - Erro retorna unavailable
       - Resposta malformada retorna unavailable

    3. src/lib/cnpj/__tests__/verification-service.test.ts (8+):
       - Cache hit retorna dados sem consultar API
       - Cache miss → primary sucesso
       - Primary unavailable → fallback sucesso
       - Ambos unavailable → defer
       - not_found cacheado → reconsulta retorna not_found
       - Cache expirado → reconsulta provedores
       - Retry 1x em timeout
  </action>

  <acceptance_criteria>
    - 5+ testes brasil-api passando
    - 5+ testes cnpja passando
    - 8+ testes verification-service passando
    - Cobertura de cenários: sucesso, not_found, timeout, 429, 5xx, malformed, cache, fallback
    - `npx vitest run src/lib/cnpj/` — green
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 13 (continued): Tests — Freemium Risk Service</name>
  <files>
    src/lib/freemium/__tests__/freemium-risk-service.test.ts
  </files>

  <read_first>
    openspec/changes/fase-33-verificacao-cnpj-freemium/specs/freemium-risk-service/spec.md
    src/lib/freemium/freemium-risk-service.ts
  </read_first>

  <action>
    Criar testes (10+):

    1. APPROVE: todos sinais OK (cnpjExists, ATIVA, root elegível, nome >= 0.6, city match, state match)
    2. APPROVE via nome fantasia (storeName ≠ razão social, mas = nome fantasia)
    3. REJECT: cnpj_not_found (lookupOutcome = 'not_found')
    4. REJECT: cnpj_baixada (situacaoCadastral = 'BAIXADA')
    5. REJECT: cnpj_nula (situacaoCadastral = 'NULA')
    6. REJECT: root_already_used (rootEligible = false)
    7. REVIEW: situacao_suspensa (situacaoCadastral = 'SUSPENSA')
    8. REVIEW: nome_divergente (nameSimilarity < 0.6 com ambos razão social e nome fantasia)
    9. REVIEW: cidade_divergente (cityMatch = false)
    10. REVIEW: uf_divergente (stateMatch = false)
    11. DEFER: api_unavailable (officialData = null)
    12. normalizeCity: "São Paulo" normaliza igual "SAO PAULO"
    13. normalizeCity: "Belo Horizonte" ≠ "São Paulo"
  </action>

  <acceptance_criteria>
    - 10+ testes freemium-risk-service passando
    - Cobertura de todas as 4 decisões (approve/review/reject/defer)
    - Cobertura de approve via nome fantasia
    - normalizeCity testada
    - `npx vitest run src/lib/freemium/` — green
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 14: Tests — Wave 2 (Store Route + Form + API)</name>
  <files>
    src/app/api/store/__tests__/route.test.ts
    src/components/flow/__tests__/store-identity-form.test.tsx
    src/app/api/cnpj/lookup/__tests__/route.test.ts
  </files>

  <read_first>
    openspec/changes/fase-33-verificacao-cnpj-freemium/specs/store-route/spec.md
    openspec/changes/fase-33-verificacao-cnpj-freemium/specs/store-identity-form/spec.md
    openspec/changes/fase-33-verificacao-cnpj-freemium/specs/cnpj-lookup-api/spec.md
  </read_first>

  <action>
    Criar testes:

    1. Store route (8+):
       - APPROVE → grant concedido
       - REVIEW → loja criada sem grant
       - REJECT not_found → bloqueia (400)
       - REJECT baixado → loja criada sem grant
       - REJECT raiz → loja criada sem grant
       - DEFER → loja criada sem grant
       - Sem dados oficiais no submit → resolve server-side
       - CNPJ inválido → erro validação

    2. Formulário (3+):
       - Lookup onBlur dispara consulta (após dígitos OK)
       - Campos bloqueados após lookup resolvido
       - Botões "usar nome fantasia/razão social" funcionam

    3. Endpoint /api/cnpj/lookup (5+):
       - CNPJ resolvido retorna dados + mensagem
       - CNPJ not_found retorna status + mensagem
       - CNPJ unavailable retorna status + mensagem
       - CNPJ inválido retorna 400
       - Não autenticado retorna 401
  </action>

  <acceptance_criteria>
    - 8+ testes store route passando
    - 3+ testes formulário passando
    - 5+ testes endpoint /api/cnpj/lookup passando
    - `npx vitest run src/app/api/store/` — green
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 15: Tests — Wave 3 (Admin)</name>
  <files>
    src/app/api/admin/reviews/__tests__/route.test.ts
    src/app/api/admin/stores/__tests__/create-test.test.ts
  </files>

  <read_first>
    openspec/changes/fase-33-verificacao-cnpj-freemium/specs/admin-reviews/spec.md
    openspec/changes/fase-33-verificacao-cnpj-freemium/specs/admin-test-store/spec.md
  </read_first>

  <action>
    Criar testes:

    1. Admin reviews (7+):
       - Lista pendentes (REVIEW)
       - Lista adiados (DEFER)
       - Lista recusados (REJECTED)
       - Aprovar concede onboarding
       - Recusar mantém rejected
       - Exceção bypassa elegibilidade
       - Revelar CNPJ audita

    2. Store de teste (4+):
       - Criar store de teste
       - Badge TESTE visível
       - Excluída de métricas (mock check)
       - Audit trail presente

    3. Admin audit log (2+):
       - Ações de approve registradas
       - Ações de reject registradas
       - Ações de reveal registradas
  </action>

  <acceptance_criteria>
    - 7+ testes admin reviews passando
    - 4+ testes store de teste passando
    - 2+ testes admin_audit_log passando
    - `npx vitest run src/app/api/admin/` — green
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 16: Tests — Integração E2E</name>
  <files>
    (tests inline in existing files or dedicated integration test file)
  </files>

  <read_first>
    openspec/changes/fase-33-verificacao-cnpj-freemium/tasks.md (section 16)
  </read_first>

  <action>
    Criar testes de integração para 5 fluxos E2E:

    1. Fluxo feliz completo: CNPJ válido → lookup → dados oficiais → approve → grant de 10 créditos
    2. Fluxo review: CNPJ com nome divergente → review → loja criada sem grant → admin aprova → grant
    3. Fluxo reject inexistente: CNPJ falso → reject → bloqueia criação → mensagem erro
    4. Fluxo defer: provedor indisponível → defer → loja sem grant → reprocessamento
    5. Fluxo admin exception: CNPJ baixado → reject → admin concede exceção → grant
  </action>

  <acceptance_criteria>
    - 5 fluxos E2E implementados
    - Fluxos cobrem approve, review, reject (inexistente), defer, admin exception
    - `npx vitest run` — todos os testes passando
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 17: Verificação Final</name>
  <files>
    (verification tasks — no new files)
  </files>

  <read_first>
    openspec/changes/fase-33-verificacao-cnpj-freemium/tasks.md (section 17)
  </read_first>

  <action>
    Executar verificação final:

    1. `npx vitest run src/lib/cnpj/__tests__/` — testes lookup + verification
    2. `npx vitest run src/lib/freemium/__tests__/` — testes risk service
    3. `npm run typecheck` — zero erros
    4. `npm run lint` — zero erros
    5. `npx vitest run` — novos + existentes passando
    6. `npm run build` — build bem-sucedido
    7. Verificar: CNPJ não aparece cru em logs, URLs, listagens admin ou responses públicas desnecessárias
    8. Verificar: Root hash continua usando HMAC-SHA256 com pepper server-side
    9. Verificar: Store de teste não recebe freemium automático
    10. Verificar: Todas as ações admin registradas em admin_audit_log
  </action>

  <acceptance_criteria>
    - typecheck: zero erros
    - lint: zero erros
    - vitest run: todos passando
    - build: sucesso
    - CNPJ não exposto em locais indevidos
    - Root hash HMAC-SHA256 intacto
    - Store de teste sem freemium
    - Ações admin auditadas
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] 5+ testes brasil-api, 5+ cnpja, 8+ verification-service
- [ ] 10+ testes freemium-risk-service
- [ ] 8+ testes store route, 5+ lookup API, 3+ form
- [ ] 7+ testes admin reviews, 4+ test store, 2+ audit log
- [ ] 5 fluxos E2E implementados
- [ ] `npm run typecheck` — zero erros
- [ ] `npm run lint` — zero erros
- [ ] `npx vitest run` — todos passando
- [ ] `npm run build` — sucesso
- [ ] CNPJ não exposto em logs, URLs, listagens admin
- [ ] Root hash HMAC-SHA256 intacto
- [ ] Store de teste sem freemium automático
- [ ] Ações admin auditadas
</verification>
</task>

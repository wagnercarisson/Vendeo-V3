---
phase: 33-verificacao-cnpj-freemium
plan: 05
type: execute
wave: 3
depends_on:
  - 33-01 (Core Libraries + Services — tests já implementados no plano)
  - 33-02 (Store Route + Grant — tests já implementados no plano)
  - 33-03 (Form + Dashboard — tests já implementados no plano)
  - 33-04 (Admin Reviews + Test Stores — tests já implementados no plano)
files_modified:
  - (integration tests — no new source files, only test files)
autonomous: false
requirements:
  - F33-INTEGRATION
  - F33-VERIFICATION
must_haves:
  truths:
    - "5 fluxos E2E de integração cobrindo approve, review, reject, defer, admin exception"
    - "Verificação final — typecheck, lint, build, vitest run (todos os testes) — zero erros"
    - "CNPJ não aparece cru em logs, URLs, listagens admin ou responses públicas"
    - "Root hash continua usando HMAC-SHA256 com pepper server-side"
    - "Store de teste não recebe freemium automático"
    - "Todas as ações admin registradas em admin_audit_log"
  artifacts: []
---

<objective>
**Plan 5: Integração E2E + Verificação Final**

Testes de integração cobrindo 5 fluxos completos de ponta a ponta (approve, review, reject, defer, admin exception). Os testes unitários de cada módulo já foram implementados em seus respectivos planos (33-01 a 33-04). Este plano executa a verificação final: typecheck, lint, build, regressão total.

**Output:** 5 fluxos E2E + verificação final passando.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/33-verificacao-cnpj-freemium/33-CONTEXT.md
openspec/changes/fase-33-verificacao-cnpj-freemium/tasks.md

<interfaces>
## Test Pattern

- Vitest com describe/it
- Testes de integração mockam dependências externas (fetch para BrasilAPI/CNPJá)
- Fluxos E2E usam cenários reais com dados sintéticos
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 16: Integration Tests — 5 Fluxos E2E</name>
  <files>
    (dedicated integration test file or inline in existing test suites)
  </files>

  <read_first>
    openspec/changes/fase-33-verificacao-cnpj-freemium/tasks.md (section 16)
  </read_first>

  <action>
    Criar testes de integração para 5 fluxos completos:

    1. Fluxo feliz completo: CNPJ válido → lookup → dados oficiais → approve → grant de 10 créditos
    2. Fluxo review: CNPJ com nome divergente → review → loja criada sem grant → admin aprova → grant
    3. Fluxo reject inexistente: CNPJ falso → reject → bloqueia criação → mensagem erro
    4. Fluxo defer: provedor indisponível → defer → loja sem grant → reprocessamento
    5. Fluxo admin exception: CNPJ baixado → reject → admin concede exceção → grant
  </action>

  <acceptance_criteria>
    - 5 fluxos E2E implementados e passando
    - Fluxos cobrem approve, review, reject (inexistente), defer, admin exception
    - npx vitest run — fluxos integram com os módulos implementados
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 17: Verificação Final</name>
  <files>
    (verification tasks — no new files)
  </files>

  <read_first>
    openspec/changes/fase-33-verificacao-cnpj-freemium/tasks.md (section 17)
  </read_first>

  <action>
    Executar verificação final completa:

    1. `npx vitest run src/lib/cnpj/__tests__/` — testes lookup + verification (implementados no 33-01)
    2. `npx vitest run src/lib/freemium/__tests__/` — testes risk service (implementados no 33-01)
    3. `npx vitest run src/app/api/store/` — testes store route (implementados no 33-02)
    4. `npx vitest run src/app/api/admin/` — testes admin (implementados no 33-04)
    5. `npm run typecheck` — zero erros
    6. `npm run lint` — zero erros
    7. `npx vitest run` — novos + existentes passando
    8. `npm run build` — build bem-sucedido
    9. Verificar: CNPJ não aparece cru em logs, URLs, listagens admin ou responses públicas desnecessárias
    10. Verificar: Root hash continua usando HMAC-SHA256 com pepper server-side
    11. Verificar: Store de teste não recebe freemium automático
    12. Verificar: Todas as ações admin registradas em admin_audit_log
  </action>

  <acceptance_criteria>
    - typecheck: zero erros
    - lint: zero erros
    - vitest run completo: todos passando (33-01 + 33-02 + 33-04 + 33-05)
    - build: sucesso
    - CNPJ não exposto em logs, URLs, listagens admin, responses públicas
    - Root hash HMAC-SHA256 intacto
    - Store de teste sem freemium
    - Ações admin auditadas
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] 5 fluxos E2E implementados
- [ ] npx vitest run src/lib/cnpj/ — green (33-01)
- [ ] npx vitest run src/lib/freemium/ — green (33-01)
- [ ] npx vitest run src/app/api/store/ — green (33-02)
- [ ] npx vitest run src/app/api/admin/ — green (33-04)
- [ ] npx vitest run src/components/flow/ — green (33-03)
- [ ] npm run typecheck — zero erros
- [ ] npm run lint — zero erros
- [ ] npx vitest run — todos passando (incluindo regressão)
- [ ] npm run build — sucesso
- [ ] CNPJ não exposto em logs, URLs, listagens admin
- [ ] Root hash HMAC-SHA256 intacto
- [ ] Store de teste sem freemium automático
- [ ] Ações admin auditadas
</verification>
</task>

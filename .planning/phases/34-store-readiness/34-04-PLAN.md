---
phase: 34-store-readiness
plan: 04
type: execute
wave: 3
depends_on:
  - 34-01
  - 34-02
  - 34-03
files_modified:
  - src/app/(app)/dashboard/page.tsx
  - src/components/flow/store-identity-form.tsx
  - src/components/flow/store-page-client.tsx
autonomous: false
requirements:
  - F34-DASHBOARD
  - F34-BRANDPROFILE
must_haves:
  truths:
    - "Dashboard exibe banner de prontidão com checklist quando ready: false"
    - "Cada item pendente no banner é link direto para configuração"
    - "Botão 'Configurar agora' aponta para a primeira pendência (fonte única de ação)"
    - "Banner não aparece quando loja não existe nem quando ready: true"
    - "Três caminhos de direção visual (logo, VS, text-only) convergem para brand profile synced"
    - "Confirmar direção visual só libera com profile synced"
  artifacts:
    - path: "src/app/(app)/dashboard/page.tsx"
      provides: "Banner de prontidão com checklist + botão Configurar agora"
      contains: "getStoreReadiness"
    - path: "src/components/flow/store-identity-form.tsx"
      provides: "Three visual direction paths (logo upload, VS, text-only)"
      contains: "text_only"
---

<objective>
**Plan 4: Dashboard Banner + Brand Profile Paths**

Implementar o banner de prontidão no dashboard com checklist das pendências de readiness e botão "Configurar agora". Garantir que os três caminhos de direção visual no Step 2 (upload de logo, geração de VS, text-only) criem um `store_brand_profiles` com `status = 'synced'` ao final, e que o botão "Confirmar direção visual" só seja liberado quando o profile estiver synced.

**Purpose:** Tornar visível o estado de prontidão da loja no dashboard e garantir que a direção visual resulte em um brand profile synced.
**Output:** Dashboard banner com checklist + Step 2 com três caminhos funcionais.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/34-store-readiness/34-CONTEXT.md
openspec/changes/fase-34-store-readiness/specs/dashboard-inteligente/spec.md
openspec/changes/fase-34-store-readiness/specs/store-brand-profile/spec.md
openspec/changes/fase-34-store-readiness/specs/store-identity-ui/spec.md
openspec/changes/fase-34-store-readiness/tasks.md

<interfaces>
## Existing Dashboard Pattern (F20+)

Current `src/app/(app)/dashboard/page.tsx`:
- Server component that fetches store data + metrics
- Renders greeting, metrics cards, recent campaigns, next-step card
- Has no-store empty state

## Existing Brand Profile Structure (F33)

From F33: `store_brand_profiles` table with:
- id, store_id, status ('processing' | 'synced' | 'failed'), source, data JSONB
- Created via logo analysis, VS generation, or text-only selection

## Banner Readiness Checklist

```typescript
// When getStoreReadiness() returns ready: false:
// Banner with:
// - ❌ CNPJ cadastral → /cadastro/cnpj?returnTo=/dashboard
// - ❌ Direção visual → /loja?required=visual-direction
// - Botão "Configurar agora" pointing to first missing item
```

## Step 2 Visual Direction Paths

| Path | Brand Profile Source | Trigger |
|------|---------------------|---------|
| Upload logo | 'logo_analysis' | User uploads logo → AI analysis → brand profile inferred |
| Gerar VS | 'without_logo' | User generates VS → approves → brand profile created |
| Text-only | 'text_only' | User selects text-only → brand profile created with text settings |
</interfaces>
</context>

<tasks>

### Task 1: Dashboard — Banner de Prontidão (Server Component)

<read_first>
- specs/dashboard-inteligente/spec.md (banner contract, checklist items, behavior per state)
- Current `src/app/(app)/dashboard/page.tsx` (existing server component structure)
</read_first>

<action>
Add readiness check and banner to dashboard server component:
1. Import `getStoreReadiness` from `src/lib/store-readiness.ts`
2. After store check and before rendering main content, if store exists, call `await getStoreReadiness(store.id)`
3. If `ready === false`, render a readiness banner component passing the `missing` array
4. Pass store ID and missing items to a client component (or inline banner)
</action>

<acceptance_criteria>
- Dashboard chama `getStoreReadiness(store.id)` quando loja existe
- Se `ready: false`, banner de prontidão é renderizado
- Se `ready: true`, banner NÃO aparece
- Se `no_store`, banner NÃO aparece (empty state é renderizado)
</acceptance_criteria>

### Task 2: Dashboard — Banner Component com Checklist

<read_first>
- specs/dashboard-inteligente/spec.md (exact checklist items, links, styles)
- Existing dashboard style patterns (cards with border, dark mode)
</read_first>

<action>
Create or modify the readiness banner component:
1. Banner displays checklist: each missing item as a line item with ❌ indicator
2. Map missing items to display text: `cadastro_fiscal` → "CNPJ cadastral" (link: `/cadastro/cnpj?returnTo=/dashboard`), `brand_profile` → "Direção visual" (link: `/loja?required=visual-direction`)
3. Show ALL missing items (not just the first one)
4. Button "Configurar agora" links to the first missing item's URL
5. Banner styling matches design system (dark mode, card with border, consistent with other dashboard cards)
</action>

<acceptance_criteria>
- Checklist mostra ❌ CNPJ cadastral (com link para `/cadastro/cnpj?returnTo=/dashboard`) quando cadastro_fiscal ausente
- Checklist mostra ❌ Direção visual (com link para `/loja?required=visual-direction`) quando brand_profile ausente
- Ambos itens aparecem se ambos ausentes
- Botão "Configurar agora" aponta para primeira pendência
- Banner consistente com design system
</acceptance_criteria>

### Task 3: Step 2 — Três Caminhos de Direção Visual

<read_first>
- specs/store-brand-profile/spec.md (three paths, brand profile synced behavior)
- Current `src/components/flow/store-identity-form.tsx` (current step 2: logo upload + VS)
</read_first>

<action>
Modify Step 2 in StoreIdentityForm to ensure all three paths converge to brand profile synced:
1. **Upload logo**: After logo upload → AI analysis completes → infer brand profile → set `source = 'logo_analysis'` and `status = 'synced'`
2. **Gerar VS**: After VS generation → user approves → create brand profile → set `source = 'without_logo'` and `status = 'synced'`
3. **Text-only**: Add a third option "Usar identidade em texto" → user confirms → create brand profile → set `source = 'text_only'` and `status = 'synced'`

All paths call the existing brand profile creation/inference service and wait for `status = 'synced'` before enabling the "Confirmar direção visual" button.
</action>

<acceptance_criteria>
- Step 2 oferece 3 caminhos: upload logo, gerar VS, identidade em texto
- Upload logo → brand profile com `source = 'logo_analysis'` e `status = 'synced'`
- Gerar VS → brand profile com `source = 'without_logo'` e `status = 'synced'`
- Text-only → brand profile com `source = 'text_only'` e `status = 'synced'`
- Botão "Confirmar direção visual" desabilitado enquanto profile não estiver synced
- Botão habilitado quando profile atinge `status = 'synced'`
</acceptance_criteria>

</tasks>

<verification>
### Plan-Level Verification

1. Test: Dashboard sem cadastro fiscal → banner com ❌ CNPJ cadastral + link
2. Test: Dashboard sem brand profile → banner com ❌ Direção visual + link
3. Test: Dashboard loja pronta → sem banner
4. Test: Dashboard sem loja → sem banner
5. Test: Step 2 — upload logo → brand profile synced
6. Test: Step 2 — gerar VS → brand profile synced
7. Test: Step 2 — text-only → brand profile synced
8. Test: Confirmar botão desabilitado/habilitado conforme status do profile
9. `npm run typecheck` — zero errors
</verification>

<success_criteria>
- Dashboard banner de prontidão funcional com checklist e links (fonte única de ação)
- Três caminhos de direção visual no Step 2, todos produzindo brand profile synced
- Botão "Confirmar direção visual" sincronizado com status do profile
</success_criteria>

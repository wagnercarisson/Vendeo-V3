---
phase: 34-store-readiness
plan: 05
type: execute
wave: 3
depends_on:
  - 34-01
  - 34-02
  - 34-03
  - 34-04
files_modified:
  - src/lib/store-readiness/__tests__/*.test.ts
  - src/lib/billing/__tests__/*.test.ts
  - src/app/(app)/campanhas/nova/__tests__/*.test.tsx
  - src/app/api/campaign/generate-image/__tests__/*.test.ts
  - src/app/api/store/billing/confirm/__tests__/route.test.ts
autonomous: false
requirements:
  - F34-TEST
must_haves:
  truths:
    - "StoreReadiness: 4+ testes com ready true, cadastro ausente, brand profile ausente, ambos ausentes"
    - "RPC check_store_readiness: 2+ testes com store completa e incompleta"
    - "Guarda página: 2+ testes de redirect conforme pendência"
    - "Guarda API: 2+ testes de 412 + pipeline prossegue"
    - "Store type: 1 teste npm run typecheck clean"
    - "Billing Info: 4+ testes server-only (ownership, upsert, get, não-bloqueio) + 2+ testes pure mapper (prefill)"
    - "Billing Confirm Route: 4+ testes (auth, ownership, confirmed=true/false)"
    - "Fluxo Legacy: 2+ testes de bloqueio + redirect encadeado"
    - "Dashboard Banner: 1+ teste de exibição condicional"
    - "Regressão completa: todos os testes existentes + novos passando"
  artifacts:
    - path: "src/lib/store-readiness/__tests__/store-readiness.test.ts"
      provides: "4+ testes de StoreReadiness scenarios"
      min_lines: 50
    - path: "src/lib/billing/__tests__/store-billing-info.test.ts"
      provides: "4+ testes de Billing Info (server-only) — ownership, upsert, get, não-bloqueio"
      min_lines: 50
    - path: "src/lib/billing/__tests__/cnpj-address-mapper.test.ts"
      provides: "2+ testes do pure mapper — prefill completo + parcial"
      min_lines: 30
    - path: "src/app/api/store/billing/confirm/__tests__/route.test.ts"
      provides: "4+ testes da confirm route — auth, ownership, confirmed=true/false"
      min_lines: 50
---

<objective>
**Plan 5: Tests + Verification**

Criar todos os testes para o novo código da F34 e executar a verificação final completa: testes unitários, typecheck, lint e build. Garantir que não há regressão nos testes existentes.

**Purpose:** Validar que toda a implementação da F34 está correta e que não há regressão no código existente.
**Output:** 20+ novos testes, 0 regressões, typecheck/lint/build limpos.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/34-store-readiness/34-CONTEXT.md
openspec/changes/fase-34-store-readiness/tasks.md

<interfaces>
## Existing Test Pattern (from F32)

```
src/lib/cnpj/__tests__/validate.test.ts — validateCnpj tests
src/lib/cnpj/__tests__/normalize.test.ts — normalizeCnpj tests
...
```

## Existing Vitest Config

From project vitest.config.ts:
- Default test directory pattern: `**/__tests__/**/*.test.{ts,tsx}`
- Existing setup files for mock supabase client
</interfaces>
</context>

<tasks>

### Task 1: StoreReadiness Tests

<read_first>
- tasks.md section 10.1 (4 test scenarios)
- Implementation of `src/lib/store-readiness.ts`
</read_first>

<action>
Create test file `src/lib/store-readiness/__tests__/store-readiness.test.ts` with:
1. Test: `getStoreReadiness()` com cadastro fiscal completo + brand profile synced → `{ ready: true, missing: [] }`
2. Test: `getStoreReadiness()` com store sem cadastro fiscal → `{ ready: false, missing: [{ item: "cadastro_fiscal", ... }] }`
3. Test: `getStoreReadiness()` sem brand profile synced → `{ ready: false, missing: [{ item: "brand_profile", ... }] }`
4. Test: `getStoreReadiness()` sem cadastro e sem brand profile → `{ ready: false, missing: ["cadastro_fiscal", "brand_profile"] }` nesta ordem
5. Test: `getStoreReadiness()` com RPC error → fallback `{ ready: false, missing: [{ item: "brand_profile", reason: "Não foi possível verificar..." }] }`
</action>

<acceptance_criteria>
- Test 1: RPC retorna ready true → resultado é `{ ready: true, missing: [] }`
- Test 2: RPC retorna missing cadastro_fiscal → resultado reflete
- Test 3: RPC retorna missing brand_profile → resultado reflete
- Test 4: RPC retorna ambos → `missing` array tem cadastro_fiscal primeiro
- Test 5: RPC lança erro → fallback ready: false com brand_profile missing
- `npx vitest run src/lib/store-readiness/__tests__/` passa
</acceptance_criteria>

### Task 2: RPC check_store_readiness Tests

<read_first>
- tasks.md section 10.2 (2 test scenarios)
- SQL RPC implementation from Plan 01
</read_first>

<action>
Create or extend SQL tests for the RPC:
1. Test: RPC com store completa (cadastro fiscal OK + brand profile synced) → JSON correto com `ready: true`
2. Test: RPC com store sem cadastro fiscal → JSON com `ready: false` e missing inclui cadastro_fiscal
</action>

<acceptance_criteria>
- RPC com store completa → retorna `{ "ready": true, "missing": [] }`
- RPC com store sem CNPJ → retorna `{ "ready": false, "missing": [{ "item": "cadastro_fiscal", ... }] }`
</acceptance_criteria>

### Task 3: Guarda na Página Tests

<read_first>
- tasks.md section 10.3 (2 test scenarios)
- `src/app/(app)/campanhas/nova/page.tsx` (implementation)
</read_first>

<action>
Create tests for the page guard:
1. Test: Store sem cadastro fiscal → redirect para `/cadastro/cnpj?returnTo=/campanhas/nova`
2. Test: Store sem brand profile → redirect para `/loja?required=visual-direction`
</action>

<acceptance_criteria>
- Sem cadastro fiscal → redirect URL contém `/cadastro/cnpj?returnTo=/campanhas/nova`
- Sem brand profile → redirect URL contém `/loja?required=visual-direction`
</acceptance_criteria>

### Task 4: Guarda na API Tests

<read_first>
- tasks.md section 10.4 (2 test scenarios)
- `src/app/api/campaign/generate-image/route.ts` (implementation)
</read_first>

<action>
Create tests for the API guard:
1. Test: Store sem cadastro fiscal → 412 com `error.reasons` e `error.missing` contendo cadastro_fiscal
2. Test: Store completa (ready true) → pipeline prossegue (resposta não contém erro de readiness)
</action>

<acceptance_criteria>
- Sem cadastro fiscal → HTTP 412 com body `error.missing` contendo `cadastro_fiscal`
- Store pronta → resposta HTTP não tem erro de readiness (pipeline prossegue)
</acceptance_criteria>

### Task 5: TypeScript Verification

<read_first>
- tasks.md section 10.5 (1 test)
</read_first>

<action>
1. Run `npm run typecheck` and verify zero errors
2. Verify: Store interface em `src/lib/store.ts` tem todos os 13 campos CNPJ tipados
3. Verify: Nenhum cast `as unknown as Record<string, unknown>` sobreviveu no código de produção
</action>

<acceptance_criteria>
- `npm run typecheck` exits 0
- `rg "as unknown as Record" src/ --include '*.ts' --include '*.tsx'` retorna 0 ocorrências ignorando node_modules, .next, __tests__
</acceptance_criteria>

### Task 6: Billing Info Tests

<read_first>
- tasks.md section 10.6 (5 test scenarios)
- `src/lib/billing/store-billing-info.ts` (implementation)
</read_first>

<action>
Create two test files:

**`src/lib/billing/__tests__/store-billing-info.test.ts`** — server-only module:
1. Test: `upsertStoreBillingInfo()` com dados completos e ownership OK → billing info salvo
2. Test: `upsertStoreBillingInfo()` com ownership violado → lança erro (StoreNotFoundError)
3. Test: `getStoreBillingInfo()` com ownership OK → retorna dados; com ownership violado → erro
4. Test: Store pode gerar campanha sem billing info → não bloqueia (verificar que readiness não inclui billing)

**`src/lib/billing/__tests__/cnpj-address-mapper.test.ts`** — pure module (no server-only):
5. Test: `getPreFillFromCnpj()` com dados completos da BrasilAPI → mapeamento correto (7 campos de endereço)
6. Test: `getPreFillFromCnpj()` com dados parciais → retorna apenas campos disponíveis sem erro
</action>

<acceptance_criteria>
- `upsert` com ownership OK → billing info salvo, retorna StoreBillingInfo
- `upsert` com ownership violado → StoreNotFoundError
- `get` com ownership OK → retorna StoreBillingInfo | null
- `get` com ownership violado → StoreNotFoundError
- `getPreFillFromCnpj` (do mapper puro) mapeia 7 campos de endereço corretamente
- `getPreFillFromCnpj` com dados parciais não lança erro
- `getStoreReadiness()` não inclui billing info como critério
- `npx vitest run src/lib/billing/__tests__/` passa
</acceptance_criteria>

### Task 7: Billing Confirm Route Tests

<read_first>
- Plan 3 Task 7 (API route POST /api/store/billing/confirm)
- Plan 1 Task 4 (store-billing-info.ts implementation)
</read_first>

<action>
Create test file `src/app/api/store/billing/confirm/__tests__/route.test.ts` with:
1. Test: Request autenticado + ownership OK + confirmed=true → salva billing info com confirmed_at setado server-side, retorna 200
2. Test: Request autenticado + ownership OK + confirmed=false → salva billing info sem confirmed_at (null), retorna 200
3. Test: Request autenticado + ownership violado (store não pertence ao userId) → 403/404, nenhuma escrita ocorre
4. Test: Request sem autenticação → 401 Unauthorized
</action>

<acceptance_criteria>
- confirmed=true → retorna 200, billing_data_confirmed_at não é null
- confirmed=false → retorna 200, billing_data_confirmed_at é null
- ownership violado → 403 ou 404, `upsertStoreBillingInfo` NÃO é chamado
- sem auth → 401
- Todas as chamadas passam por ownership check antes de mutar dados
</acceptance_criteria>

### Task 8: Fluxo Legacy Tests

<read_first>
- tasks.md section 10.7 (2 test scenarios)
- cadastro/cnpj/page.tsx redirect logic
</read_first>

<action>
Create tests for legacy flow:
1. Test: Loja legacy sem cadastro fiscal → bloqueio de geração (guarda redireciona)
2. Test: Após atualizar cadastro sem brand profile → redirect para `/loja?required=visual-direction`
</action>

<acceptance_criteria>
- Loja legacy sem cadastro → guarda redireciona para cadastro (não permite geração)
- Após update sem brand profile → redirect para direção visual (não para returnTo original)
</acceptance_criteria>

### Task 9: Dashboard Banner Test

<read_first>
- tasks.md section 10.8 (1 test scenario)
</read_first>

<action>
Create test for dashboard banner:
1. Test: Banner de prontidão aparece para loja com pendências, não aparece para loja pronta, não aparece quando no_store
</action>

<acceptance_criteria>
- Loja com pendências → banner renderizado
- Loja pronta → banner não renderizado
- No store → banner não renderizado (empty state)
</acceptance_criteria>

### Task 10: Final Verification

<read_first>
- tasks.md section 11 (10 verification items)
</read_first>

<action>
Execute complete verification checklist:
1. `npx vitest run src/lib/store-readiness/__tests__/`
2. `npx vitest run src/lib/billing/__tests__/` (store-billing-info + cnpj-address-mapper)
3. `npx vitest run src/lib/billing/__tests__/cnpj-address-mapper.test.ts` (pure module)
3. `npm run typecheck` — zero erros
4. `npm run lint` — zero erros
5. `npx vitest run` — novos + testes existentes passando
6. `npm run build` — build bem-sucedido
7. Verificar: Nenhum cast `as unknown as Record<string, unknown>` sobreviveu no código de produção
8. Verificar: `upsertStoreBillingInfo()` faz ownership check antes de escrever
9. Verificar: Billing info não bloqueia geração de campanhas
10. Verificar: Lojas legacy sem cadastro fiscal são bloqueadas na geração com mensagem clara
</action>

<acceptance_criteria>
- Todos os comandos de teste passam com exit code 0
- `npm run typecheck` — zero erros
- `npm run lint` — zero erros
- `npm run build` — bem-sucedido
- Nenhum cast `as unknown` sobrevive em src/ (exceto __tests__)
- Ownership check verificado no código
- Billing não bloqueia geração verificado
- Mensagens de bloqueio claras verificadas
</acceptance_criteria>

</tasks>

<verification>
### Plan-Level Verification

1. `npx vitest run` — 100% pass (novos + existentes)
2. `npm run typecheck` — zero errors
3. `npm run lint` — zero errors
4. `npm run build` — bem-sucedido
5. Nenhum cast sobreviveu
6. Total de testes: 20+ novos cenários
</verification>

<success_criteria>
- Todos os testes passam sem regressão
- TypeScript, lint e build limpos
- Casts eliminados do código de produção
- Cobertura de testes para todos os cenários de readiness, billing (server-only + pure mapper), guards e fluxo legacy
</success_criteria>

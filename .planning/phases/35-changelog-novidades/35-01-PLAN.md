---
phase: 35-changelog-novidades
plan: 01
type: execute
wave: 1
depends_on:
  - 30-legal-foundation
  - 32-freemium-anti-abuso-cnpj
  - 34-store-readiness
files_modified:
  - content/changelog/2026-07-30-fase-30-legal-foundation.md
  - content/changelog/2026-07-31-fase-32-freemium-cnpj.md
  - content/changelog/2026-08-01-fase-34-store-readiness.md
  - src/lib/changelog/types.ts
  - src/lib/changelog/parse-frontmatter.ts
  - src/lib/changelog/schema.ts
  - src/lib/changelog/render-markdown.ts
  - src/lib/changelog/format-date.ts
  - src/lib/changelog/__tests__/parse-frontmatter.test.ts
  - src/lib/changelog/__tests__/schema.test.ts
  - src/lib/changelog/__tests__/render-markdown.test.ts
  - src/lib/changelog/__tests__/format-date.test.ts
autonomous: true
requirements:
  - F35-CONTENT-01
  - F35-CONTENT-02
  - F35-CONTENT-03
  - F35-CONTENT-04
  - F35-CONTENT-05
must_haves:
  truths:
    - "content/changelog/ contém 3 arquivos .md (F30, F32, F34) com frontmatter YAML válido e body em PT-BR editorial"
    - "Apenas a entry F32 tem announcement: \"card\"; F30 e F34 têm \"none\"; F34 (2026-08-01) é a mais recente por data"
    - "parseFrontmatter lança erro sem `---` de abertura/fechamento, remove aspas de valores escalares e preserva `:` no valor"
    - "renderMarkdown produz apenas tags h2/p/ul/li/strong, escapa texto bruto (`<script>` → `&lt;script&gt;`) e lança erro em sintaxe não suportada"
    - "ChangelogFrontmatterSchema (Zod) valida id/title/date/category/importance/announcement e rejeita valores fora dos enums"
    - "formatChangelogDate formata 2026-07-31 como 31/07/2026 sem usar new Date(ISO)"
  artifacts:
    - path: "content/changelog/2026-07-30-fase-30-legal-foundation.md"
      provides: "Entry seed F30 — feature/major/announcement none"
      contains: "fase-30-legal-foundation"
    - path: "content/changelog/2026-07-31-fase-32-freemium-cnpj.md"
      provides: "Entry seed F32 — feature/major/announcement card (única com anúncio)"
      contains: "announcement: \"card\""
    - path: "content/changelog/2026-08-01-fase-34-store-readiness.md"
      provides: "Entry seed F34 — improvement/minor/announcement none, data 2026-08-01 (mais recente)"
      contains: "2026-08-01"
    - path: "src/lib/changelog/types.ts"
      provides: "ChangelogCategory, ChangelogImportance, ChangelogFrontmatter, ChangelogEntry (slug)"
      contains: "export interface ChangelogEntry"
    - path: "src/lib/changelog/parse-frontmatter.ts"
      provides: "parseFrontmatter(raw) → { frontmatter, body }"
      contains: "export function parseFrontmatter"
    - path: "src/lib/changelog/schema.ts"
      provides: "ChangelogFrontmatterSchema (Zod) com regex de date"
      contains: "ChangelogFrontmatterSchema"
    - path: "src/lib/changelog/render-markdown.ts"
      provides: "renderMarkdown(md) → HTML sanitizado (h2/p/ul/li/strong)"
      contains: "export function renderMarkdown"
    - path: "src/lib/changelog/format-date.ts"
      provides: "formatChangelogDate(dateIso) → dd/mm/aaaa sem conversão de fuso"
      contains: "export function formatChangelogDate"
---

<objective>
**Plano 1: Foundation — Seed de conteúdo + Core Library (tipos, parser, renderer, schema)**

Criar a fonte de dados estática `content/changelog/` com as 3 entries seed (F30, F32, F34 — D1) e a fundação da biblioteca de changelog: tipos, parser próprio de frontmatter sem dependências, renderer controlado de markdown com HTML sanitizado, schema Zod fail-fast e helper de data pt-BR sem shift de fuso (D8). Este plano produz os contratos que os planos 35-02/35-03/35-04 consomem.

**Purpose:** Estabelecer dados + contratos puros (zero server-only, zero dependências novas) que sustentam todo o restante da fase.
**Output:** 3 entries .md + 5 módulos TypeScript puros + 4 arquivos de teste.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/35-changelog-novidades/35-CONTEXT.md
openspec/changes/fase-35-changelog-novidades/design.md
openspec/changes/fase-35-changelog-novidades/specs/changelog-content/spec.md
openspec/changes/fase-35-changelog-novidades/tasks.md

<interfaces>
## Contrato de tipos (spec changelog-content)

```typescript
export type ChangelogCategory = "feature" | "improvement" | "fix";
export type ChangelogImportance = "major" | "minor";

export interface ChangelogFrontmatter {
  id: string;
  title: string;
  date: string;        // ISO YYYY-MM-DD (data civil, fuso brasileiro)
  milestone?: string;
  category: ChangelogCategory;
  importance: ChangelogImportance;
  announcement: "none" | "card" | "modal";
}

export interface ChangelogEntry {
  frontmatter: ChangelogFrontmatter;
  body: string;        // markdown sem frontmatter
  slug: string;        // derivado do filename
}
```

## Exemplo de entry (padrão editorial D5/D8)

Frontmatter delimitado por `---`; body com APENAS `## headings`, parágrafos e `- listas` (o renderer só suporta esse subconjunto — headings com `#` único QUEBRAM o renderer). Cada entry responde: o que mudou, por que importa, o que o lojista precisa fazer.

## Convenções de teste (repositório)

- Vitest com `globals: true`, ambiente `node` por padrão; testes de componente estáticos usam `renderToString` de `react-dom/server`; testes com efeitos/interação usam `// @vitest-environment jsdom` + `@testing-library/react`.
- Testes colocalizados em `__tests__/` ao lado do módulo.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed content/changelog — 3 entries editoriais (D1, D5)</name>
  <files>
    content/changelog/2026-07-30-fase-30-legal-foundation.md
    content/changelog/2026-07-31-fase-32-freemium-cnpj.md
    content/changelog/2026-08-01-fase-34-store-readiness.md
  </files>
  <action>
  Criar o diretório `content/changelog/` (novo, na raiz do repositório) com exatamente 3 arquivos `.md` — sem parser no diretório de dados, sem outros arquivos:

  1. `2026-07-30-fase-30-legal-foundation.md` — frontmatter: id `fase-30-legal-foundation`, title `Fundação Legal`, date `2026-07-30`, milestone `v1.5`, category `feature`, importance `major`, announcement `none`. Body em PT-BR claro (D5): "O Vendeo agora tem Termos de Uso, Política de Privacidade e Uso Aceitável..." cobrindo o que mudou, por que importa (segurança jurídica para o lojista) e que nenhuma ação é necessária.

  2. `2026-07-31-fase-32-freemium-cnpj.md` — frontmatter: id `fase-32-freemium-cnpj`, title `Freemium CNPJ`, date `2026-07-31`, milestone `v1.5`, category `feature`, importance `major`, announcement `card`. Body: "Agora você cria sua loja com CNPJ e ganha 10 créditos de boas-vindas..." explicando que o CNPJ é obrigatório no cadastro e o ganho de créditos.

  3. `2026-08-01-fase-34-store-readiness.md` — frontmatter: id `fase-34-store-readiness`, title `Loja mais pronta para gerar campanhas`, date `2026-08-01`, milestone `v1.5`, category `improvement`, importance `minor`, announcement `none`. Body: "Sua loja precisa de uma direção visual..." — alinhado ao exemplo do template em `docs/changelog-update.md` (mesmo id/title/date/valores).

  Regras editoriais (D5, obrigatórias nas 3): português claro, sem jargão técnico (proibido: guard, endpoint, server component, RPC, migration, status code), respondendo às três perguntas (o que mudou / por que importa / o que o lojista precisa fazer). Estrutura do body com seções `## O que mudou`, `## Por que isso importa` e `## O que você precisa fazer` (se aplicável), usando apenas `## headings`, parágrafos e `- listas` com `**negrito**` opcional — NUNCA `#` único, links `[texto](url)`, imagens, tabelas ou HTML cru (o renderer do Task 3 lança erro nessas sintaxes). Valores escalares do frontmatter em aspas duplas (ex: `announcement: "card"`), id slug único (kebab-case do nome da fase), date em `YYYY-MM-DD` sem componente de hora (fuso brasileiro, D1/D8).
  </action>
  <verify>
    <automated>npx vitest run --no-coverage 2>&1 | findstr /C:"No test files found" 2>$null; exit 0</automated>
    <check>`Test-Path "content/changelog"` e contagem de arquivos `.md` igual a 3 via `(Get-ChildItem content/changelog -Filter *.md).Count -eq 3`</check>
  </verify>
  <done>
    Diretório `content/changelog/` existe na raiz com exatamente 3 arquivos `.md`; cada um tem frontmatter com id/title/date/milestone/category/importance/announcement; apenas `2026-07-31-fase-32-freemium-cnpj.md` tem `announcement: "card"`; F30 e F34 têm `"none"`; a data mais recente é `2026-08-01` (F34); nenhum body usa sintaxe fora do subconjunto `## / parágrafo / - lista / **negrito**`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Core Library — types.ts + parse-frontmatter.ts + schema.ts (D8)</name>
  <files>
    src/lib/changelog/types.ts
    src/lib/changelog/parse-frontmatter.ts
    src/lib/changelog/schema.ts
    src/lib/changelog/__tests__/parse-frontmatter.test.ts
    src/lib/changelog/__tests__/schema.test.ts
  </files>
  <behavior>
    - Test 1 (parse-frontmatter): raw com frontmatter completo + body → retorna `{ frontmatter }` com chaves parseadas e `body` trimado
    - Test 2: `announcement: "none"` e `category: 'feature'` → valores SEM aspas (`"none"`, `"feature"`)
    - Test 3: valor com dois-pontos (ex: `title: "Lançamento: v1.5"`) → preserva texto inteiro após o primeiro `:`
    - Test 4: raw sem `---` de abertura na primeira linha → lança Error
    - Test 5: raw com abertura mas sem fechamento → lança Error
    - Test 6 (schema): frontmatter válido → parseSchema sem erro
    - Test 7: category fora do enum (ex: `"oops"`) → ZodError
    - Test 8: date fora do regex (ex: `"31/07/2026"` ou `"2026-7-1"`) → ZodError
    - Test 9: id ou title vazio → ZodError
  </behavior>
  <action>
  Criar 3 módulos puros (SEM `import "server-only"`, SEM imports de supabase/fs, SEM side effects — importáveis por client e server):

  1. `src/lib/changelog/types.ts` — exportar exatamente os tipos do contrato: `ChangelogCategory` (`"feature" | "improvement" | "fix"`), `ChangelogImportance` (`"major" | "minor"`), interface `ChangelogFrontmatter` (id, title, date, milestone opcional, category, importance, announcement `"none" | "card" | "modal"`), interface `ChangelogEntry` (frontmatter, body, slug).

  2. `src/lib/changelog/parse-frontmatter.ts` — exportar `parseFrontmatter(raw: string): { frontmatter: Record<string, unknown>; body: string }` (~30 linhas, zero dependências): valida que a primeira linha é exatamente `---` (senão lança `Error("Frontmatter de abertura ausente")`); localiza a linha de fechamento `---` (senão lança `Error("--- de fechamento não encontrado")`); para cada linha `chave: valor` entre os delimitadores, faz split APENAS no primeiro `:`, faz trim das bordas e remove aspas simples/duplas opcionais do valor escalar (`"none"` → `none`, `'v1.5'` → `v1.5`) — valores com `:` interno são preservados intactos; retorna `body` com o conteúdo após o fechamento, trimado.

  3. `src/lib/changelog/schema.ts` — importar `z` de `"zod"` (^3.24.4, já no projeto) e exportar `ChangelogFrontmatterSchema = z.object({...})` com: `id: z.string().min(1)`, `title: z.string().min(1)`, `date: z.string().regex(/^\d{4}-\d{2}-\d{2}$/)`, `milestone: z.string().optional()`, `category: z.enum(["feature", "improvement", "fix"])`, `importance: z.enum(["major", "minor"])`, `announcement: z.enum(["none", "card", "modal"])`. Exportar também `ChangelogFrontmatter = z.infer<typeof ChangelogFrontmatterSchema>`.

  TDD (red-green-refactor): escrever os testes de `__tests__/parse-frontmatter.test.ts` e `__tests__/schema.test.ts` primeiro (devem falhar com módulos ausentes), depois implementar os módulos até os testes passarem. Rodar os testes entre cada etapa.
  </action>
  <verify>
    <automated>npx vitest run src/lib/changelog/__tests__/parse-frontmatter.test.ts src/lib/changelog/__tests__/schema.test.ts</automated>
  </verify>
  <done>
    Os 5 cenários de parseFrontmatter e os 4 cenários de schema (behavior) passam; `npm run typecheck` sem erros; os módulos não importam server-only/fs/supabase.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Renderer controlado + formatação de data (D8, fuso)</name>
  <files>
    src/lib/changelog/render-markdown.ts
    src/lib/changelog/format-date.ts
    src/lib/changelog/__tests__/render-markdown.test.ts
    src/lib/changelog/__tests__/format-date.test.ts
  </files>
  <behavior>
    - Test 1 (render-markdown): conteúdo com `## Heading`, parágrafo e `- item` → HTML com `<h2>`, `<p>` e `<ul><li>`
    - Test 2: `**negrito**` → `<strong>negrito</strong>` dentro do elemento correto
    - Test 3: texto com `<script>` e `<b>` cru → saída contém `&lt;script&gt;` e `&lt;b&gt;`, NUNCA as tags cruas interpretáveis
    - Test 4: linha com `# Heading` (um `#`) → lança Error de sintaxe não suportada
    - Test 5: conteúdo com `[texto](url)` ou `![imagem](url)` → lança Error
    - Test 6 (format-date): `formatChangelogDate("2026-07-31")` → `"31/07/2026"` (não `30/07/2026`)
    - Test 7: `formatChangelogDate("2026-08-01")` → `"01/08/2026"` (padding de zero à esquerda)
  </behavior>
  <action>
  Criar 2 módulos puros:

  1. `src/lib/changelog/render-markdown.ts` — exportar `renderMarkdown(md: string): string` que processa linha a linha e retorna HTML com APENAS as tags `h2, p, ul, li, strong`:
  - Linha começando com `## ` → `<h2>...</h2>` (conteúdo escapado + negrito aplicado)
  - Sequência de linhas começando com `- ` → bloco `<ul>` com `<li>` por item
  - Linhas em branco separam parágrafos; demais linhas → `<p>...</p>`
  - **ORDEM CRÍTICA (XSS):** aplicar HTML-escape em TODO texto bruto ANTES de envolver em tags — função interna `escapeHtml` que converte pelo menos `&` → `&amp;`, `<` → `&lt;`, `>` → `&gt;`, `"` → `&quot;`, `'` → `&#39;`. Depois de escapar, aplicar `**negrito**` → `<strong>...</strong>` (emparelhamento de `**` no texto já escapado). Resultado: `<script>` cru vira `&lt;script&gt;` na saída.
  - **Sintaxe não suportada lança Error** (build/CI, não runtime): linha começando com `# ` (hash único), linha começando com `> ` (blockquote), linha com `` ``` `` (code fence), linha contendo `![` (imagem) ou `](` (link). Mensagem de erro descritiva citando a linha e a sintaxe.
  - Negrito não fechado: tratar como texto literal (já escapado), sem lançar.

  2. `src/lib/changelog/format-date.ts` — exportar `formatChangelogDate(dateIso: string): string` que formata `dd/mm/aaaa` a partir da string ISO: split em `-` → `[ano, mes, dia]`, retorna `${dia}/${mes}/${ano}` com zero-padding manual. **PROIBIDO** usar `new Date(dateIso)`/`toLocaleDateString` neste módulo (deslocamento de dia em UTC-3 — D8/fuso). Se a string não casar `^\d{4}-\d{2}-\d{2}$`, retornar a própria string intacta (fail-safe, sem lançar).

  TDD (red-green-refactor): escrever os testes primeiro (devem falhar), implementar até passar.
  </action>
  <verify>
    <automated>npx vitest run src/lib/changelog/__tests__/render-markdown.test.ts src/lib/changelog/__tests__/format-date.test.ts</automated>
  </verify>
  <done>
    Os 5 cenários do renderer e os 2 de data (behavior) passam; a saída de renderMarkdown nunca contém tags HTML fora da whitelist como HTML interpretável (assert de saída com `<script>` ausente e `&lt;script&gt;` presente); `npm run typecheck` sem erros.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content/changelog/*.md (repo) → renderer | Conteúdo editorial controlado por devs; atravessa o renderer antes de chegar ao browser |
| renderMarkdown output → DOM do browser | Saída injetada via dangerouslySetInnerHTML nos componentes (plans 35-03); se não sanitizada, vira XSS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Tampering (Injection) | render-markdown.ts | mitigate | HTML-escape de TODO texto bruto ANTES de aplicar tags; whitelist estrita h2/p/ul/li/strong; `<script>` → `&lt;script&gt;`; sintaxe não suportada lança Error (build/CI); saída nunca contém HTML cru interpretável |
| T-35-02 | Tampering | content/changelog/*.md | mitigate | Revisão editorial (D5) + validação Zod fail-fast no build (schema.ts) + escape no renderer como defesa em profundidade; conteúdo é versionado e revisado por commit |
| T-35-03 | Tampering | localStorage (chaves vendeo:*) | accept | Chaves são indicadoras de UI (badge/anúncio), sem PII e sem fronteira de segurança; manipulação local só afeta o próprio usuário |
| T-35-SC | Tampering | instalação de pacotes | N/A | Zero dependências novas (D8) — parser/renderer próprios; zod e lucide-react já presentes no projeto |
</threat_model>

<verification>
### Verificação do plano

1. `npx vitest run src/lib/changelog/` — testes de parser, schema, renderer e data passando
2. `npm run typecheck` — zero erros
3. Grep de sanidade: `rg "server-only" src/lib/changelog/types.ts src/lib/changelog/parse-frontmatter.ts src/lib/changelog/schema.ts src/lib/changelog/render-markdown.ts src/lib/changelog/format-date.ts` retorna vazio (módulos puros)
4. Contagem: `(Get-ChildItem content/changelog -Filter *.md).Count` = 3
5. Grep: apenas `2026-07-31-fase-32-freemium-cnpj.md` contém `announcement: "card"` (`rg -l 'announcement: "card"' content/changelog` retorna exatamente 1 arquivo)
</verification>

<success_criteria>
- `content/changelog/` com 3 entries seed válidas (D1), apenas F32 com `announcement: "card"`, F34 mais recente por data
- `types.ts`, `parse-frontmatter.ts`, `schema.ts`, `render-markdown.ts`, `format-date.ts` criados como módulos puros (zero server-only, zero dependências novas)
- 11+ cenários de teste passando (parser 5, schema 4, renderer 5, date 2 — contagem efetiva conforme implementação)
- Contratos exportados: ChangelogEntry, ChangelogFrontmatter, ChangelogCategory, ChangelogImportance, parseFrontmatter, renderMarkdown, ChangelogFrontmatterSchema, formatChangelogDate
- `npm run typecheck` limpo
</success_criteria>

<output>
Create `.planning/phases/35-changelog-novidades/35-01-SUMMARY.md` when done
</output>

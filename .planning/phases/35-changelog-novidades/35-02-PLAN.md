---
phase: 35-changelog-novidades
plan: 02
type: execute
wave: 1
depends_on:
  - 35-01
files_modified:
  - src/lib/changelog/get-changelog.ts
  - src/hooks/use-changelog-state.ts
  - src/lib/changelog/__tests__/get-changelog.test.ts
  - src/hooks/__tests__/use-changelog-state.test.ts
autonomous: true
requirements:
  - F35-CONTENT-06
  - F35-STATE-01
  - F35-STATE-02
  - F35-STATE-03
  - F35-STATE-04
  - F35-STATE-05
  - F35-STATE-06
must_haves:
  truths:
    - "getAllEntries() lê content/changelog/*.md, valida com Zod e retorna ordenado por data DESC (F34 → F32 → F30)"
    - "getLatestAnnouncement() retorna a entry mais recente com announcement !== \"none\" (F32 no seed) ou null"
    - "getEntryById(id) retorna a entry parseada ou null para id inexistente"
    - "Diretório vazio → []; frontmatter inválido (parse ou Zod) → throw (fail fast no build)"
    - "useChangelogState usa as chaves vendeo:last_seen_changelog_id e vendeo:dismissed_changelog_announcement_id, SSR-safe (localStorage apenas em useEffect, estado inicial null)"
    - "markChangelogAsViewed atualiza SEEN_KEY e, se announcementId presente, DISMISSED_KEY; dismissAnnouncement atualiza APENAS DISMISSED_KEY"
    - "hasUnseen compara ID exato (null → false); isAnnouncementVisible = dismissedId !== entryId (entryId vazio → false)"
    - "Sem Supabase, sem requisição extra, sem estado global (D3/D7)"
  artifacts:
    - path: "src/lib/changelog/get-changelog.ts"
      provides: "getAllEntries, getLatestAnnouncement, getEntryById (server-only)"
      contains: "import \"server-only\""
    - path: "src/hooks/use-changelog-state.ts"
      provides: "useChangelogState com 2 chaves localStorage"
      contains: "vendeo:last_seen_changelog_id"
    - path: "src/hooks/__tests__/use-changelog-state.test.ts"
      provides: "4+ cenários do hook com mock de localStorage"
      contains: "markChangelogAsViewed"
---

<objective>
**Plano 2: Core Library — get-changelog.ts (server-only) + Hook use-changelog-state**

Implementar o módulo de leitura de entries (server-only, usado por layout e dashboard — D2) e o hook client de estado de leitura com dois controles independentes de localStorage (D3). O plano 35-02 executa após o 35-01 (mesma wave, ordem por número de plano: os módulos de 35-01 já existem).

**Purpose:** Funções de leitura/validação/ordenação (F35-CONTENT-06) + estado de leitura SSR-safe (F35-STATE-01..06) que a UI consome nos planos 35-03 e 35-04.
**Output:** 2 módulos + 2 arquivos de teste (6+ cenários get-changelog, 4+ cenários hook).
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/35-changelog-novidades/35-CONTEXT.md
openspec/changes/fase-35-changelog-novidades/design.md (D3, D7, D8)
openspec/changes/fase-35-changelog-novidades/specs/changelog-content/spec.md (get-changelog)
openspec/changes/fase-35-changelog-novidades/specs/changelog-state/spec.md (hook)
openspec/changes/fase-35-changelog-novidades/tasks.md (seções 3 e 4)

<interfaces>
## Módulos consumidos do plano 35-01 (já existentes quando este plano executa)

De `src/lib/changelog/types.ts`:
```typescript
export type ChangelogCategory = "feature" | "improvement" | "fix";
export type ChangelogImportance = "major" | "minor";
export interface ChangelogFrontmatter { id: string; title: string; date: string; milestone?: string; category: ChangelogCategory; importance: ChangelogImportance; announcement: "none" | "card" | "modal"; }
export interface ChangelogEntry { frontmatter: ChangelogFrontmatter; body: string; slug: string; }
```

De `src/lib/changelog/parse-frontmatter.ts`: `parseFrontmatter(raw: string): { frontmatter: Record<string, unknown>; body: string }`

De `src/lib/changelog/schema.ts`: `ChangelogFrontmatterSchema` (Zod object validando id/title/date/milestone/category/importance/announcement)

## Contrato do hook (spec changelog-state)

```typescript
interface UseChangelogStateReturn {
  lastSeenId: string | null;
  dismissedId: string | null;
  markChangelogAsViewed: (latestEntryId: string, latestAnnouncementId?: string) => void;
  dismissAnnouncement: (id: string) => void;
  hasUnseen: (latestId: string) => boolean;
  isAnnouncementVisible: (entryId: string) => boolean;
}
```

## Padrão server-only do repositório

`src/lib/store-readiness.ts` inicia com `import "server-only";` — vitest.config.ts já aliaseia `server-only` para `src/__tests__/__mocks__/server-only.ts` (testes funcionam). Testes com fs usam o ambiente `node` (padrão do vitest.config).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: get-changelog.ts — leitura, validação e ordenação (F35-CONTENT-06)</name>
  <files>
    src/lib/changelog/get-changelog.ts
    src/lib/changelog/__tests__/get-changelog.test.ts
  </files>
  <behavior>
    - Test 1: `getAllEntries()` sobre o diretório real `content/changelog/` → 3 entries na ordem F34 (2026-08-01) → F32 (2026-07-31) → F30 (2026-07-30), cada uma com frontmatter validado e slug derivado do filename
    - Test 2: fixture dir com entry `announcement: "card"` + outra `"none"` → `getLatestAnnouncement()` retorna a de card
    - Test 3: fixture dir sem nenhum announcement ≠ none → `getLatestAnnouncement()` retorna null
    - Test 4: `getEntryById("fase-30-legal-foundation")` (dir real) → entry com frontmatter + body parseados
    - Test 5: `getEntryById("id-inexistente")` → null
    - Test 6: fixture dir vazio → `getAllEntries()` retorna `[]` e `getLatestAnnouncement()` retorna null (não quebra)
    - Test 7: fixture dir com frontmatter inválido (ex: `category: "oops"` ou sem `---` de fechamento) → getAllEntries lança Error (fail fast)
  </behavior>
  <action>
  Criar `src/lib/changelog/get-changelog.ts` com `import "server-only";` na primeira linha (F35-CONTENT-06):

  1. Constante exportada `CHANGELOG_DIR` = `path.join(process.cwd(), "content", "changelog")`.
  2. Helper privado `readAllEntries(dir: string = CHANGELOG_DIR): Promise<ChangelogEntry[]>` que: lê o diretório com `fs/promises.readdir`; filtra arquivos terminando em `.md`; para cada arquivo lê com `fs/promises.readFile` (encoding `utf-8`); aplica `parseFrontmatter` (lança Error em formato inválido); valida com `ChangelogFrontmatterSchema.parse` (lança ZodError em frontmatter inválido — fail fast no build); deriva `slug` removendo a extensão `.md` do filename (ex: `2026-07-30-fase-30-legal-foundation`); monta `ChangelogEntry { frontmatter, body, slug }`. Erro de `readdir` com diretório inexistente (ENOENT) → retorna `[]` (diretório vazio/não existe não quebra).
  3. `getAllEntries(dir?: string): Promise<ChangelogEntry[]>` — retorna `readAllEntries(dir)` ordenado por `frontmatter.date` em ordem DESC via comparação lexicográfica da string ISO (`b.frontmatter.date.localeCompare(a.frontmatter.date)`); empates de data mantêm ordem estável de leitura (não adicionar segundo critério de ordenação).
  4. `getLatestAnnouncement(dir?: string): Promise<ChangelogEntry | null>` — `(await getAllEntries(dir)).find(e => e.frontmatter.announcement !== "none") ?? null` (lista já vem DESC → primeiro match é o mais recente).
  5. `getEntryById(id: string, dir?: string): Promise<ChangelogEntry | null>` — `(await getAllEntries(dir)).find(e => e.frontmatter.id === id) ?? null`.
  6. Parâmetro opcional `dir` nas 3 funções públicas (default `CHANGELOG_DIR`) — o contrato do spec (chamadas sem argumento) permanece idêntico; o parâmetro existe para os testes criarem fixtures em diretórios temporários via `fs.mkdtemp` (ex: `path.join(os.tmpdir(), "changelog-test-")`) e garantirem cenários de diretório vazio/inválido sem tocar o diretório real.

  TDD: escrever `get-changelog.test.ts` primeiro (red), implementar até green. Usar `fs.mkdtemp`/`fs.writeFile`/`fs.rm` (recursive: true) nos cenários de fixture com `afterEach` para limpeza. O Test 1 e o Test 4 usam o diretório real `content/changelog/` (valida os seeds de 35-01 — este é o gate real de "fail fast no build").
  </action>
  <verify>
    <automated>npx vitest run src/lib/changelog/__tests__/get-changelog.test.ts</automated>
  </verify>
  <done>
    Os 7 cenários (behavior) passam; `npm run typecheck` sem erros; o arquivo começa com `import "server-only"`; diretório real `content/changelog` valida os 3 seeds sem throw.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: use-changelog-state.ts — dois controles de localStorage SSR-safe (F35-STATE-01..06)</name>
  <files>
    src/hooks/use-changelog-state.ts
    src/hooks/__tests__/use-changelog-state.test.ts
  </files>
  <behavior>
    - Test 1: hook montado em ambiente sem valores → `lastSeenId` e `dismissedId` iniciam `null`; `localStorage` é lido apenas dentro de useEffect (não durante o render inicial)
    - Test 2: `markChangelogAsViewed("fase-32-freemium-cnpj")` → `lastSeenId` vira `"fase-32-freemium-cnpj"` e `hasUnseen("fase-32-freemium-cnpj")` retorna false; localStorage SEEN_KEY contém o id
    - Test 3: `markChangelogAsViewed("fase-34-store-readiness", "fase-32-freemium-cnpj")` → lastSeenId E dismissedId atualizados (ambas as chaves no localStorage)
    - Test 4: `dismissAnnouncement("fase-32-freemium-cnpj")` → `isAnnouncementVisible("fase-32-freemium-cnpj")` false, MAS `hasUnseen("fase-32-freemium-cnpj")` continua true (dispensa não afeta o indicador da sidebar) e SEEN_KEY não foi escrita
    - Test 5: `hasUnseen("")` ou com latestId vazio → false; `isAnnouncementVisible("")` → false
    - Test 6: `hasUnseen` com lastSeenId antigo e nova entry → true (indicador reaparece)
  </behavior>
  <action>
  Criar `src/hooks/use-changelog-state.ts` — hook client (`"use client"` não é obrigatório no arquivo, mas o hook é consumido apenas por componentes client; usar `React.useState`/`useEffect`/`useCallback`):

  1. Constantes de chave exportadas: `CHANGELOG_SEEN_KEY = "vendeo:last_seen_changelog_id"` e `CHANGELOG_DISMISSED_KEY = "vendeo:dismissed_changelog_announcement_id"`.
  2. Retorno tipado `UseChangelogStateReturn` conforme contrato do spec (lastSeenId, dismissedId, markChangelogAsViewed, dismissAnnouncement, hasUnseen, isAnnouncementVisible).
  3. **SSR-safe (F35-STATE-01):** estado inicial `{ lastSeenId: null, dismissedId: null }`; `useEffect` de montagem lê as duas chaves de `localStorage` (acessar `window.localStorage` APENAS dentro do efeito, nunca no corpo do render) e atualiza o estado; valores inválidos/ausentes → null (try/catch defensivo).
  4. **Sincronização entre abas/components (sem estado global, D3/F35-STATE-06):** no mesmo useEffect, registrar listeners de `window.addEventListener("storage", handler)` (evento nativo de outras abas) e de evento customizado `"vendeo:changelog-viewed"`; o handler re-lê as chaves e atualiza o estado. Cleanup remove ambos os listeners. Isso faz o badge da sidebar sumir imediatamente após navegar para /novidades (componentes do shell permanecem montados em navegação client-side).
  5. `markChangelogAsViewed(latestEntryId: string, latestAnnouncementId?: string)` (F35-STATE-02): `localStorage.setItem(CHANGELOG_SEEN_KEY, latestEntryId)`; se `latestAnnouncementId` for passado, também `setItem(CHANGELOG_DISMISSED_KEY, latestAnnouncementId)`; atualizar ambos os estados; disparar `window.dispatchEvent(new CustomEvent("vendeo:changelog-viewed"))`.
  6. `dismissAnnouncement(id: string)` (F35-STATE-03): APENAS `localStorage.setItem(CHANGELOG_DISMISSED_KEY, id)` + estado `dismissedId` — NUNCA tocar SEEN_KEY.
  7. `hasUnseen(latestId: string): boolean` (F35-STATE-04): retorna `false` se `!latestId`; senão `lastSeenId !== latestId` (comparação de ID exato, não timestamp).
  8. `isAnnouncementVisible(entryId: string): boolean` (F35-STATE-05): retorna `false` se `!entryId`; senão `dismissedId !== entryId`.
  9. Nenhum import de Supabase, nenhuma chamada de rede, nenhum estado global fora do hook (F35-STATE-06).

  TDD: escrever `use-changelog-state.test.ts` primeiro (red), implementar até green. O teste usa `// @vitest-environment jsdom` (padrão de `src/__tests__/auth/logout.test.tsx`), `renderHook` de `@testing-library/react` (se `renderHook` não estiver disponível na versão instalada, usar componente harness com `useRef` expondo as funções via props de retorno ou spy de console), `beforeEach` com `localStorage.clear()` e `vi.restoreAllMocks()`. Para o Test 1 (SSR-safe), montar com `window.localStorage` removido/simulado lançando no acesso durante render e verificar que o render não lança e que `localStorage.getItem` só é chamado após efeitos (spy).
  </action>
  <verify>
    <automated>npx vitest run src/hooks/__tests__/use-changelog-state.test.ts</automated>
  </verify>
  <done>
    Os 6 cenários (behavior) passam; `npm run typecheck` sem erros; nenhum acesso a `localStorage` fora de `useEffect`/handlers; chaves exatas `vendeo:last_seen_changelog_id` e `vendeo:dismissed_changelog_announcement_id`; sem imports de supabase ou fetch.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem content/changelog → get-changelog.ts | Código server-only lê arquivos do repo; caminho é fixo, sem input de usuário |
| localStorage (browser) → useChangelogState | Dados de estado de leitura armazenados no client; atravessam o hook sem validação |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-04 | Tampering | get-changelog.ts (path) | mitigate | Leitura restrita a `path.join(process.cwd(), "content", "changelog")`; nenhum caminho derivado de input de usuário; parâmetro `dir` opcional usado apenas em testes com `fs.mkdtemp` |
| T-35-05 | Injection | get-changelog.ts (parse) | mitigate | Todo frontmatter validado por `ChangelogFrontmatterSchema.parse` antes de uso; falha → throw no build/CI (fail fast), nunca dados inválidos em runtime |
| T-35-06 | Tampering | localStorage (hook) | accept | Chaves indicadoras de UI; valores corrompidos tratados como null (try/catch); impacto limitado ao próprio usuário; sem PII, sem fronteira de segurança |
| T-35-SC | Tampering | instalação de pacotes | N/A | Zero dependências novas (D8); `zod` e `react` já presentes |
</threat_model>

<verification>
### Verificação do plano

1. `npx vitest run src/lib/changelog/__tests__/get-changelog.test.ts src/hooks/__tests__/use-changelog-state.test.ts` — 13+ cenários passando
2. `npm run typecheck` — zero erros
3. Grep: `rg "supabase|fetch\(" src/lib/changelog/get-changelog.ts src/hooks/use-changelog-state.ts` retorna vazio
4. Grep de SSR-safety: `rg "localStorage" src/hooks/use-changelog-state.ts` — ocorrências apenas dentro de useEffect/handlers
5. `npx vitest run src/lib/changelog/` — confirma que os seeds de 35-01 passam na validação (gate fail-fast)
</verification>

<success_criteria>
- `getAllEntries`, `getLatestAnnouncement`, `getEntryById` implementados em módulo server-only (F35-CONTENT-06)
- Hook `useChangelogState` com as 2 chaves de localStorage, SSR-safe, sem estado global (F35-STATE-01..06)
- 13+ cenários de teste passando (get-changelog 7, hook 6)
- `npm run typecheck` limpo; seeds reais de 35-01 validados sem throw
</success_criteria>

<output>
Create `.planning/phases/35-changelog-novidades/35-02-SUMMARY.md` when done
</output>

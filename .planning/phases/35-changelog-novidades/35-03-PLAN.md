---
phase: 35-changelog-novidades
plan: 03
type: execute
wave: 2
depends_on:
  - 35-02
files_modified:
  - src/app/(app)/novidades/page.tsx
  - src/app/(app)/novidades/novidades-client.tsx
  - src/components/changelog/changelog-card.tsx
  - src/components/changelog/changelog-list.tsx
  - src/components/changelog/changelog-announcement.tsx
  - src/components/changelog/sidebar-badge.tsx
  - src/components/changelog/__tests__/changelog-list.test.tsx
  - src/components/changelog/__tests__/changelog-announcement.test.tsx
  - src/components/changelog/__tests__/sidebar-badge.test.tsx
autonomous: true
requirements:
  - F35-UI-01
  - F35-UI-02
  - F35-UI-03
  - F35-UI-04
  - F35-UI-05
  - F35-UI-06
  - F35-UI-07
must_haves:
  truths:
    - "GET /novidades (server component) renderiza PageHeader 'Novidades', breadcrumb 'Dashboard > Novidades', ChangelogList e EmptyState quando não há entries"
    - "Visitar /novidades chama markChangelogAsViewed(latestEntryId, latestAnnouncementId?) no client ao montar"
    - "ChangelogCard exibe badge de categoria (feature=accent-green, improvement=accent-blue, fix=accent-amber), título, data dd/mm/aaaa sem shift de fuso, importância e conteúdo via renderMarkdown"
    - "ChangelogList renderiza um card por entry na ordem recebida com separação visual e estado vazio tratado"
    - "ChangelogAnnouncement (client) exibe card (padrão) ou modal (exceção) apenas quando announcement !== 'none' e isAnnouncementVisible; 'Ver novidades' → /novidades; × → dismissAnnouncement; retorna null com entry null"
    - "SidebarBadge (client) usa useChangelogState, recebe latestEntryId por prop e exibe ponto quando hasUnseen; nada quando null/vazio"
    - "Estilo conforme design system (dark mode, tokens bg-*/text-*/accent-*, Poppins/Open Sans)"
  artifacts:
    - path: "src/app/(app)/novidades/page.tsx"
      provides: "Página /novidades (server component)"
      contains: "getAllEntries"
    - path: "src/app/(app)/novidades/novidades-client.tsx"
      provides: "Cliente que marca visualização ao montar"
      contains: "markChangelogAsViewed"
    - path: "src/components/changelog/changelog-card.tsx"
      provides: "Card de entry individual"
      contains: "renderMarkdown"
    - path: "src/components/changelog/changelog-list.tsx"
      provides: "Lista de ChangelogCard"
      contains: "ChangelogCard"
    - path: "src/components/changelog/changelog-announcement.tsx"
      provides: "Anúncio card/modal na dashboard"
      contains: "dismissAnnouncement"
    - path: "src/components/changelog/sidebar-badge.tsx"
      provides: "Indicador de novidades na sidebar"
      contains: "hasUnseen"
  key_links:
    - from: "src/app/(app)/novidades/page.tsx"
      to: "src/components/changelog/changelog-list.tsx"
      via: "getAllEntries() → prop entries (order DESC do server)"
    - from: "src/app/(app)/novidades/page.tsx"
      to: "src/app/(app)/novidades/novidades-client.tsx"
      via: "props latestEntryId/latestAnnouncementId"
    - from: "src/app/(app)/novidades/novidades-client.tsx"
      to: "src/hooks/use-changelog-state.ts"
      via: "markChangelogAsViewed(latestEntryId, latestAnnouncementId) no mount"
    - from: "src/components/changelog/changelog-announcement.tsx"
      to: "src/hooks/use-changelog-state.ts"
      via: "dismissAnnouncement + isAnnouncementVisible"
    - from: "src/components/changelog/sidebar-badge.tsx"
      to: "src/hooks/use-changelog-state.ts"
      via: "hasUnseen(latestEntryId) → renderiza ponto"
---

<objective>
**Plano 3: Página /novidades + Componentes Changelog (card, list, announcement, sidebar-badge)**

Construir a UI do changelog: página `/novidades` (server component, F35-UI-01) com cliente que marca visualização (F35-UI-02) e os 4 componentes de changelog (F35-UI-03..06) com estilo do design system (F35-UI-07). O `SidebarBadge` criado aqui é consumido pelo `Sidebar` no plano 35-04 (wave 2, sem conflito de arquivos).

**Purpose:** Entregar a superfície visível do changelog que o lojista acessa e o indicador que a sidebar renderiza.
**Output:** 1 página + 1 componente client + 4 componentes + 3 arquivos de teste (2+ cenários list, 5+ cenários announcement, 2+ cenários badge).
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/35-changelog-novidades/35-CONTEXT.md
openspec/changes/fase-35-changelog-novidades/design.md (D2, D3, D4, D5)
openspec/changes/fase-35-changelog-novidades/specs/changelog-ui/spec.md
openspec/changes/fase-35-changelog-novidades/tasks.md (seções 5 e 6)
openspec/design-system/MASTER.md (tokens, badges 6.4, modal 6.5, empty states, a11y 11)

<interfaces>
## Módulos consumidos (35-01 e 35-02 — já existentes)

De `src/lib/changelog/get-changelog.ts` (server-only — importar APENAS em server components):
```typescript
export const CHANGELOG_DIR: string;
export function getAllEntries(dir?: string): Promise<ChangelogEntry[]>;
export function getLatestAnnouncement(dir?: string): Promise<ChangelogEntry | null>;
```

De `src/lib/changelog/format-date.ts` (puro): `formatChangelogDate(dateIso: string): string` → `dd/mm/aaaa`

De `src/lib/changelog/render-markdown.ts` (puro): `renderMarkdown(md: string): string` → HTML sanitizado (h2/p/ul/li/strong)

De `src/hooks/use-changelog-state.ts` (client): `useChangelogState()` retornando `{ lastSeenId, dismissedId, markChangelogAsViewed, dismissAnnouncement, hasUnseen, isAnnouncementVisible }`

## Componentes de UI existentes

- `@/components/ui/page-header`: `<PageHeader title breadcrumbs?: {label; href?}[] actions? />`
- `@/components/ui/empty-state`: `<EmptyState icon?: LucideIcon title description action? />`
- `@/components/ui/badge`: variantes prontas `ready|error|generating|default` — NÃO atende categorias feature/improvement/fix; badges de categoria usam classes do design system diretamente
- `@/components/ui/card`: `<Card className?>` (bg-bg-surface, border-border, rounded-xl)

## Padrões de teste de componentes (repositório)

- Estático: `// @vitest-environment node` + `renderToString` de `react-dom/server`
- Interativo/efeitos: `// @vitest-environment jsdom` + `@testing-library/react` (render, screen, fireEvent)
- `vi.mock("next/link")` para componentes que usam `Link` (padrão em `src/components/readiness/__tests__/readiness-banner.test.tsx`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Página /novidades + cliente de visualização (F35-UI-01, F35-UI-02)</name>
  <files>
    src/app/(app)/novidades/page.tsx
    src/app/(app)/novidades/novidades-client.tsx
  </files>
  <action>
  Criar a rota `/novidades` dentro do grupo `(app)` (já protegido pelo `layout.tsx` com `requirePageUser` — a página NÃO repete autenticação):

  1. `src/app/(app)/novidades/page.tsx` — server component `async`:
  - Importar `getAllEntries` e `getLatestAnnouncement` de `@/lib/changelog/get-changelog` (módulo server-only — permitido aqui, é server component), `PageHeader` de `@/components/ui/page-header`, `EmptyState` de `@/components/ui/empty-state`, `ChangelogList` de `@/components/changelog/changelog-list`, `Sparkles` de `lucide-react` (ícone do EmptyState) e `NovidadesClient` de `./novidades-client`.
  - `const entries = await getAllEntries();` e `const latestAnnouncement = await getLatestAnnouncement();`
  - Derivar `latestEntryId = entries[0]?.frontmatter.id ?? null` e `latestAnnouncementId = latestAnnouncement?.frontmatter.id ?? undefined`.
  - Render: `<PageHeader title="Novidades" breadcrumbs={[{ label: "Dashboard", href: "/dashboard" }, { label: "Novidades" }]} />`; se `entries.length === 0` → `<EmptyState icon={Sparkles} title="Nenhuma novidade por enquanto" description="Quando o Vendeo lançar algo novo, você verá aqui." />`; senão → `<ChangelogList entries={entries} />`; e em ambos os casos `<NovidadesClient latestEntryId={latestEntryId} latestAnnouncementId={latestAnnouncementId} />` (colocado após o conteúdo — renderiza vazio).
  - F35-UI-07: container do conteúdo em `max-w-3xl` para legibilidade (padrão do design system para conteúdo).

  2. `src/app/(app)/novidades/novidades-client.tsx` — componente client (`"use client"`):
  - Props: `{ latestEntryId: string | null; latestAnnouncementId?: string }`.
  - Usa `useChangelogState()` de `@/hooks/use-changelog-state`.
  - `useEffect` de montagem: se `latestEntryId`, chamar `markChangelogAsViewed(latestEntryId, latestAnnouncementId)` — atualiza SEEN_KEY e, se houver anúncio ativo (id passado), também DISMISSED_KEY (D3/D4: visitar /novidades limpa indicador E dispensa anúncio ativo). Declarar o hook ANTES do useEffect.
  - Renderiza `null` (componente de efeito colateral).

  ATENÇÃO (XSS — ver threat model): a página e o cliente NUNCA injetam HTML diretamente; o conteúdo renderizado vem exclusivamente de `ChangelogCard` via `renderMarkdown` (sanitizado).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <check>Grep: `rg "getAllEntries|getLatestAnnouncement" src/app/\(app\)/novidades/page.tsx` retorna ocorrências</check>
  </verify>
  <done>
    `/novidades` renderiza PageHeader "Novidades" + breadcrumb "Dashboard > Novidades" + ChangelogList com entries ordenadas; sem entries → EmptyState; `novidades-client.tsx` chama `markChangelogAsViewed(latestEntryId, latestAnnouncementId)` em useEffect de montagem; `npm run typecheck` limpo.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ChangelogCard + ChangelogList (F35-UI-03, F35-UI-04, F35-UI-07)</name>
  <files>
    src/components/changelog/changelog-card.tsx
    src/components/changelog/changelog-list.tsx
    src/components/changelog/__tests__/changelog-list.test.tsx
  </files>
  <behavior>
    - Test 1: ChangelogList com 3 entries → 3 ChangelogCard renderizados na ordem recebida, cada um com badge de categoria da cor correta (feature → classe contendo `accent-green`, improvement → `accent-blue`, fix → `accent-amber`)
    - Test 2: ChangelogList com array vazio → renderiza estado vazio tratado sem lançar
    - Test 3: ChangelogCard com entry `date: "2026-07-31"` → texto exibido `31/07/2026` (não `30/07/2026`)
    - Test 4: ChangelogCard com body markdown → saída contém `<h2>`, `<p>`, `<ul>` (via renderMarkdown) e o texto `&lt;script&gt;` para body com `<script>` cru (sanitização em profundidade)
  </behavior>
  <action>
  Criar 2 componentes em `src/components/changelog/`:

  1. `changelog-card.tsx` — componente (não precisa ser client; sem hooks):
  - Props: `{ entry: ChangelogEntry }` (importar tipo de `@/lib/changelog/types`).
  - Constante local `CATEGORY_LABEL: Record<ChangelogCategory, string>` = `{ feature: "Nova funcionalidade", improvement: "Melhoria", fix: "Correção" }`.
  - Constante local `CATEGORY_BADGE_CLASSES: Record<ChangelogCategory, string>` = `{ feature: "bg-accent-green/10 text-accent-green border-accent-green/20", improvement: "bg-accent-blue/10 text-accent-blue border-accent-blue/20", fix: "bg-accent-amber/10 text-accent-amber border-accent-amber/20" }` (badge-style do MASTER.md 6.4 — NÃO usar o componente Badge existente, cujas variantes não cobrem categorias).
  - Estrutura: wrapper `Card` (de `@/components/ui/card`) com header contendo badge de categoria (span com classes `inline-flex items-center rounded-full border px-2.5 py-0.5 text-xs font-medium font-heading` + CATEGORY_BADGE_CLASSES[category]) e título `h2` (`font-heading text-lg font-semibold text-text-primary`); linha de meta com data formatada via `formatChangelogDate(entry.frontmatter.date)` (`text-xs text-text-muted font-body`) e indicador de importância: `major` → `Badge`-like span `text-accent-green` com `Atenção` ou texto "Importante" discreto (destaque), `minor` → sem destaque adicional (sutil) — F35-UI-03.
  - Conteúdo: `<div dangerouslySetInnerHTML={{ __html: renderMarkdown(entry.body) }} />` com classes de conteúdo (`prose`-like manual: `text-sm text-text-secondary font-body space-y-3`, headings `text-text-primary`). **Regra de segurança:** `dangerouslySetInnerHTML` recebe EXCLUSIVAMENTE a saída de `renderMarkdown` (sanitizada no 35-01) — nunca `entry.body` cru.
  - Milestone opcional: se `entry.frontmatter.milestone` existir, exibir em `text-xs text-text-muted` (ex: `v1.5`).

  2. `changelog-list.tsx` — componente:
  - Props: `{ entries: ChangelogEntry[] }`.
  - Renderiza `<div className="space-y-6">` com um `<ChangelogCard key={entry.frontmatter.id} entry={entry} />` por entry (separação visual por espaço — F35-UI-04).
  - Array vazio → renderizar um estado vazio tratado (div com `text-sm text-text-muted font-body`, ex: "Nenhuma novidade por enquanto.") — NÃO lançar. (A página também tem EmptyState; a lista se defende sozinha por contrato.)
  - NÃO ordenar internamente — recebe a ordem já ordenada (data DESC do server).

  TDD: escrever `changelog-list.test.tsx` primeiro (red), implementar até green. Teste com `// @vitest-environment node` + `renderToString` + fixture de entries com os 3 campos de categoria. Para o Test 4 (renderMarkdown no card), montar entry com body contendo `<script>` cru e assertar que a saída serializada contém `&lt;script&gt;` e não contém `<script>` como tag aberta.
  </action>
  <verify>
    <automated>npx vitest run src/components/changelog/__tests__/changelog-list.test.tsx</automated>
  </verify>
  <done>
    Os 4 cenários (behavior) passam; `npm run typecheck` sem erros; ChangelogCard injeta apenas saída de renderMarkdown; badges de categoria com tokens accent-green/accent-blue/accent-amber; data em dd/mm/aaaa via formatChangelogDate (sem new Date).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ChangelogAnnouncement + SidebarBadge (F35-UI-05, F35-UI-06, F35-UI-07)</name>
  <files>
    src/components/changelog/changelog-announcement.tsx
    src/components/changelog/sidebar-badge.tsx
    src/components/changelog/__tests__/changelog-announcement.test.tsx
    src/components/changelog/__tests__/sidebar-badge.test.tsx
  </files>
  <behavior>
    - Test 1 (announcement): entry com `announcement: "card"` não dispensada → card discreto exibido com título e botão "Ver novidades"
    - Test 2: entry com `announcement: "none"` → nada renderizado (null)
    - Test 3: clicar no botão fechar (×) → `dismissAnnouncement(id)` chamado e card some
    - Test 4: dispensar NÃO altera `lastSeenId` (badge da sidebar permanece ativo — verificar que SEEN_KEY não foi escrita no localStorage)
    - Test 5: `entry` null → componente retorna null sem erro
    - Test 6 (badge): `hasUnseen(latestEntryId)` true → ponto visual renderizado
    - Test 7: `latestEntryId` null/undefined → badge não renderiza nada e não lança
    - Test 8: `lastSeenId === latestEntryId` (já visto) → badge não renderiza
  </behavior>
  <action>
  Criar 2 componentes client em `src/components/changelog/`:

  1. `changelog-announcement.tsx` — componente client (`"use client"`):
  - Props: `{ entry: ChangelogEntry | null }` (tipo de `@/lib/changelog/types`).
  - `const { dismissAnnouncement, isAnnouncementVisible } = useChangelogState();`
  - Lógica de visibilidade (D4): `if (!entry || entry.frontmatter.announcement === "none") return null;` e `if (!isAnnouncementVisible(entry.frontmatter.id)) return null;`
  - Variante `card` (padrão): `Card` discreto no topo com padding, flex container: conteúdo (título `font-heading font-semibold text-text-primary` + data formatada `formatChangelogDate` em `text-xs text-text-muted` + corpo via `dangerouslySetInnerHTML={{ __html: renderMarkdown(entry.body) }}` — APENAS saída sanitizada) + ações: botão fechar (×) ghost (`hover:bg-bg-elevated`, `aria-label="Fechar anúncio"`, w-9 h-9 min-h-[44px] touch target) chamando `dismissAnnouncement(entry.frontmatter.id)` e botão/link "Ver novidades" (`Link` de `next/link` para `/novidades`, classe `text-sm font-semibold text-accent-green hover:underline`).
  - Variante `modal` (exceção): overlay fixo (`fixed inset-0 z-50 bg-black/50 backdrop-blur-sm`) + conteúdo (`bg-bg-elevated border border-border rounded-xl p-6 max-w-[480px] shadow-xl`) com botão fechar (×) e "Ver novidades" (mesmos handlers) — MASTER.md 6.5. Mesma regra de visibilidade.
  - "Ver novidades" navega para `/novidades` — a página chama `markChangelogAsViewed(latestEntryId, entry.frontmatter.id)` (D4: após visitar, anúncio some também — via evento `vendeo:changelog-viewed` do hook, o estado do anúncio re-sincroniza).
  - `entry === null` → `return null` (F35-DASHBOARD-02, não quebra).

  2. `sidebar-badge.tsx` — componente client (`"use client"`):
  - Props: `{ latestEntryId?: string | null }` (vem do server via layout → AppShell → Sidebar — F35-APP-SHELL-01; NUNCA importar módulos server-only).
  - `const { hasUnseen } = useChangelogState();`
  - `if (!latestEntryId || !hasUnseen(latestEntryId)) return null;`
  - Render: `<span className="ml-auto inline-block h-2 w-2 rounded-full bg-accent-green" role="status" aria-label="Novidades não lidas" title="Novidades não lidas" />` (ponto verde discreto, não é cor como único indicador para a11y — MASTER.md 11: adicionar `aria-label`/`title`).

  TDD: escrever os 2 arquivos de teste primeiro (red), implementar até green.
  - `changelog-announcement.test.tsx`: `// @vitest-environment jsdom` + `@testing-library/react` (render, screen, fireEvent) + `vi.mock("next/link", ...)` (padrão readiness-banner). Para simular o estado inicial do anúncio, pré-semear `localStorage` vazio (não dispensado) e, para o Test 4, spy em `Storage.prototype.setItem` verificando que SEEN_KEY (`vendeo:last_seen_changelog_id`) nunca é escrita ao dispensar.
  - `sidebar-badge.test.tsx`: `// @vitest-environment jsdom`; Test 6 pré-semeia `localStorage.setItem("vendeo:last_seen_changelog_id", "outro-id")` antes de renderizar (efeito do hook lê no mount); Test 8 semeia com o mesmo id de `latestEntryId`.
  </action>
  <verify>
    <automated>npx vitest run src/components/changelog/__tests__/changelog-announcement.test.tsx src/components/changelog/__tests__/sidebar-badge.test.tsx</automated>
  </verify>
  <done>
    Os 8 cenários (behavior) passam; `npm run typecheck` sem erros; ChangelogAnnouncement retorna null para entry null/none/descartado; dispensar nunca toca `vendeo:last_seen_changelog_id`; SidebarBadge renderiza ponto apenas com latestEntryId válido e hasUnseen true; ambos injetam apenas HTML sanitizado.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderMarkdown output → dangerouslySetInnerHTML (ChangelogCard/ChangelogAnnouncement) | Ponto de injeção de HTML no DOM do browser; conteúdo veio do repo, atravessou renderer sanitizado |
| localStorage → hook → visibilidade de badge/anúncio | Estado client decide o que renderizar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-07 | Tampering (Injection) | changelog-card.tsx / changelog-announcement.tsx (dangerouslySetInnerHTML) | mitigate | `dangerouslySetInnerHTML` recebe EXCLUSIVAMENTE saída de `renderMarkdown` (escapamento total de texto bruto + whitelist h2/p/ul/li/strong do 35-01); nunca `entry.body` cru; teste de regressão (Test 4 do Task 2) asserta `&lt;script&gt;` na saída |
| T-35-08 | Tampering | localStorage (dispensa/visita) | accept | Usuário pode manipular as chaves para esconder/mostrar anúncio ou badge; impacto restrito à própria UI; sem PII, sem fronteira de segurança; changelog é público para todos os usuários (D7) |
| T-35-SC | Tampering | instalação de pacotes | N/A | Zero dependências novas (D8); lucide-react e next/link já presentes |
</threat_model>

<verification>
### Verificação do plano

1. `npx vitest run src/components/changelog/` — 8+ cenários passando
2. `npm run typecheck` — zero erros
3. Grep de segurança: `rg "dangerouslySetInnerHTML" src/components/changelog/` — ocorrências apenas com `renderMarkdown(...)` na mesma linha
4. Grep de arquitetura: `rg "server-only|get-changelog|getAllEntries" src/components/changelog/` retorna vazio (componentes client/servidor de UI não importam módulos server-only)
5. Grep: `rg "new Date" src/components/changelog/` retorna vazio (data sempre via formatChangelogDate)
</verification>

<success_criteria>
- Página `/novidades` funcional com PageHeader, breadcrumb, ChangelogList e EmptyState (F35-UI-01)
- `novidades-client.tsx` marca visualização ao montar (F35-UI-02)
- ChangelogCard, ChangelogList, ChangelogAnnouncement e SidebarBadge implementados com tokens do design system (F35-UI-03..07)
- 8+ cenários de teste passando; typecheck limpo; nenhum import server-only em componentes
</success_criteria>

<output>
Create `.planning/phases/35-changelog-novidades/35-03-SUMMARY.md` when done
</output>

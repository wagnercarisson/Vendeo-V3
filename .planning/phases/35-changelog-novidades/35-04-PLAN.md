---
phase: 35-changelog-novidades
plan: 04
type: execute
wave: 2
depends_on:
  - 35-03
files_modified:
  - src/app/(app)/layout.tsx
  - src/components/shell/app-shell.tsx
  - src/components/shell/sidebar-drawer.tsx
  - src/components/shell/sidebar.tsx
  - src/components/shell/account-menu.tsx
  - src/app/(app)/dashboard/page.tsx
autonomous: true
requirements:
  - F35-APP-SHELL-01
  - F35-APP-SHELL-02
  - F35-APP-SHELL-03
  - F35-DASHBOARD-01
  - F35-DASHBOARD-02
must_haves:
  truths:
    - "layout.tsx (server) busca getAllEntries() e injeta latestChangelogEntryId={entries[0]?.frontmatter.id ?? null} em <AppShell>"
    - "AppShell, SidebarDrawer e Sidebar repassam o id por prop — NENHUM componente client importa get-changelog/server-only"
    - "Sidebar tem 5º item 'Novidades' (/novidades, ícone Sparkles) com active state e SidebarBadge integrado"
    - "AccountMenu tem link 'Novidades' (/novidades, ícone Sparkles) entre Configurações e Sair"
    - "Dashboard renderiza <ChangelogAnnouncement entry={latestAnnouncement} /> após VerificationBanners e ReadinessCheckBanner nos estados has_store_no_campaigns e has_store_with_campaigns"
    - "Dashboard não quebra quando latestAnnouncement é null; shell não quebra quando latestEntryId é null"
  artifacts:
    - path: "src/app/(app)/layout.tsx"
      provides: "Injeção de latestChangelogEntryId (server)"
      contains: "latestChangelogEntryId"
    - path: "src/components/shell/sidebar.tsx"
      provides: "5º item Novidades + SidebarBadge"
      contains: "/novidades"
    - path: "src/components/shell/account-menu.tsx"
      provides: "Link Novidades no dropdown"
      contains: "Novidades"
    - path: "src/app/(app)/dashboard/page.tsx"
      provides: "ChangelogAnnouncement no dashboard"
      contains: "ChangelogAnnouncement"
  key_links:
    - from: "src/app/(app)/layout.tsx"
      to: "src/components/changelog/sidebar-badge.tsx"
      via: "prop chain latestChangelogEntryId → AppShell → Sidebar/SidebarDrawer → SidebarBadge (35-03)"
    - from: "src/app/(app)/dashboard/page.tsx"
      to: "src/components/changelog/changelog-announcement.tsx"
      via: "getLatestAnnouncement() → prop entry (null-safe)"
    - from: "src/components/shell/account-menu.tsx"
      to: "/novidades"
      via: "next/link com ícone Sparkles"
---

<objective>
**Plano 4: App Shell + Dashboard — fluxo latestEntryId, sidebar 5º item, AccountMenu, anúncio contextual**

Integrar o changelog ao app shell (D2): o layout server busca a entry mais recente e injeta o id nos componentes client por prop (NUNCA por import de server-only), a sidebar ganha o 5º item "Novidades" com indicador, o AccountMenu ganha o link secundário, e o dashboard passa a renderizar o anúncio contextual (D4). Consome `SidebarBadge` (35-03) e `ChangelogAnnouncement` (35-03).

**Purpose:** Dar visibilidade ao changelog em todo o app: indicador persistente na navegação + anúncio na dashboard.
**Output:** 6 arquivos modificados (sem novos arquivos).
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/35-changelog-novidades/35-CONTEXT.md
openspec/changes/fase-35-changelog-novidades/design.md (D2, D3, D4)
openspec/changes/fase-35-changelog-novidades/specs/app-shell/spec.md
openspec/changes/fase-35-changelog-novidades/specs/dashboard-inteligente/spec.md
openspec/changes/fase-35-changelog-novidades/tasks.md (seções 7 e 8)

<interfaces>
## Estado atual dos arquivos do shell (extraído do código)

`src/app/(app)/layout.tsx` (server component) — assinatura atual:
```typescript
export default async function AppLayout({ children }: { children: React.ReactNode }) {
  const user = await requirePageUser();
  const store = await getCurrentStore(user.userId);
  // ... privacy gate ...
  return <AppShell user={user} storeName={store?.name ?? null}>{children}<PrivacyRecovery /><PrivacyGate ... /></AppShell>;
}
```

`src/components/shell/app-shell.tsx` (client):
```typescript
interface AppShellProps { user: { claims: JwtPayload }; storeName?: string | null; children: React.ReactNode; }
// renderiza <Sidebar /> (aside desktop) e <SidebarDrawer isOpen onClose toggleButtonRef />
```

`src/components/shell/sidebar-drawer.tsx` (client):
```typescript
interface SidebarDrawerProps { isOpen: boolean; onClose: () => void; toggleButtonRef?: RefObject<HTMLButtonElement | null>; }
// <Sidebar isDrawer onNavigate={onClose} />
```

`src/components/shell/sidebar.tsx` (client):
```typescript
interface SidebarProps { isDrawer?: boolean; onNavigate?: () => void; }
const NAV_ITEMS = [
  { href: "/dashboard", label: "Dashboard", icon: LayoutDashboard },
  { href: "/campanhas", label: "Campanhas", icon: Megaphone },
  { href: "/loja", label: "Loja", icon: Store },
  { href: "/conta", label: "Conta", icon: UserCircle },
];
// active: pathname === item.href || pathname.startsWith(item.href + "/")
//   → "bg-accent-green/10 text-accent-green" ; inactive → "text-text-muted hover:bg-bg-elevated hover:text-text-primary"
```

`src/components/shell/account-menu.tsx` (client) — dropdown: displayName header, `<Link href="/conta">Configurações</Link>` (Settings icon), `<div><LogoutButton /></div>`. Trigger com aria-haspopup/aria-expanded/Escape.

`src/app/(app)/dashboard/page.tsx` (server) — switch em `getUserOnboardingState()`:
- `has_store_no_campaigns`: renderiza `<VerificationBanners .../>` e `<ReadinessCheckBanner .../>` depois do PageHeader, antes do BalanceDisplay/EmptyState
- `has_store_with_campaigns`: renderiza `<VerificationBanners .../>` e `<ReadinessCheckBanner .../>` depois do PageHeader, antes do greeting/cards

## Componentes consumidos (35-03)

- `@/components/changelog/sidebar-badge`: `<SidebarBadge latestEntryId={...} />` (client, prop `latestEntryId?: string | null`)
- `@/components/changelog/changelog-announcement`: `<ChangelogAnnouncement entry={ChangelogEntry | null} />` (client, retorna null quando entry null)
- `@/lib/changelog/get-changelog` (server-only): `getAllEntries()`, `getLatestAnnouncement()` — importar APENAS em server components
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fluxo latestEntryId — layout server → AppShell → SidebarDrawer (F35-APP-SHELL-01)</name>
  <files>
    src/app/(app)/layout.tsx
    src/components/shell/app-shell.tsx
    src/components/shell/sidebar-drawer.tsx
  </files>
  <action>
  Implementar o fluxo de prop (D2 — componentes client NUNCA importam get-changelog/server-only):

  1. `src/app/(app)/layout.tsx` (server):
  - Adicionar `import { getAllEntries } from "@/lib/changelog/get-changelog";`
  - No corpo da função, após `const store = await getCurrentStore(user.userId);`: `const entries = await getAllEntries();` e `const latestEntryId = entries[0]?.frontmatter.id ?? null;`
  - Passar `latestChangelogEntryId={latestEntryId}` para `<AppShell user={user} storeName={store?.name ?? null} latestChangelogEntryId={latestEntryId}>` (F35-APP-SHELL-01 — layout busca a entry mais recente e injeta o id).

  2. `src/components/shell/app-shell.tsx` (client):
  - Adicionar `latestChangelogEntryId?: string | null` à interface `AppShellProps` e ao destructuring.
  - Repassar para `<Sidebar latestEntryId={latestChangelogEntryId} />` (aside desktop) e `<SidebarDrawer isOpen={isDrawerOpen} onClose={...} toggleButtonRef={toggleButtonRef} latestEntryId={latestChangelogEntryId} />`.
  - NÃO importar nada de `@/lib/changelog` — apenas repassar a prop.

  3. `src/components/shell/sidebar-drawer.tsx` (client):
  - Adicionar `latestEntryId?: string | null` à interface `SidebarDrawerProps` e ao destructuring.
  - Repassar para `<Sidebar isDrawer onNavigate={onClose} latestEntryId={latestEntryId} />`.
  - NÃO importar nada de `@/lib/changelog`.

  Verificação estrutural: após as 3 modificações, `rg "get-changelog|getAllEntries|server-only" src/components/shell/app-shell.tsx src/components/shell/sidebar-drawer.tsx` deve retornar vazio.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <check>`rg "get-changelog|getAllEntries|server-only" src/components/shell/app-shell.tsx src/components/shell/sidebar-drawer.tsx` retorna vazio (exit code 1 do rg = sucesso)</check>
  </verify>
  <done>
    `layout.tsx` chama `getAllEntries()` e injeta `latestChangelogEntryId`; AppShell repassa para Sidebar e SidebarDrawer; SidebarDrawer repassa para o Sidebar interno; nenhum componente client importa get-changelog/server-only; `npm run typecheck` limpo.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sidebar 5º item + SidebarBadge + AccountMenu (F35-APP-SHELL-02, F35-APP-SHELL-03)</name>
  <files>
    src/components/shell/sidebar.tsx
    src/components/shell/account-menu.tsx
  </files>
  <action>
  Modificar 2 componentes client:

  1. `src/components/shell/sidebar.tsx`:
  - Adicionar `latestEntryId?: string | null` à interface `SidebarProps` e ao destructuring.
  - Importar `Sparkles` de `lucide-react` (adicionar ao import existente de LayoutDashboard/Megaphone/Store/UserCircle) e `SidebarBadge` de `@/components/changelog/sidebar-badge`.
  - Adicionar 5º item ao `NAV_ITEMS`: `{ href: "/novidades", label: "Novidades", icon: Sparkles }` (F35-APP-SHELL-02).
  - No mapeamento dos links, dentro do `<Link>` do item, renderizar o badge quando `item.href === "/novidades"`: `<SidebarBadge latestEntryId={latestEntryId} />` (posicionado após o label — o SidebarBadge já aplica `ml-auto`). Os demais itens não renderizam badge.
  - O active state existente (`pathname === item.href || pathname.startsWith(item.href + "/")` → `bg-accent-green/10 text-accent-green`) já cobre `/novidades` sem alteração — validar que `/novidades` ativo fica destacado.
  - NÃO importar `@/lib/changelog` — `latestEntryId` vem por prop.

  2. `src/components/shell/account-menu.tsx`:
  - Importar `Sparkles` de `lucide-react` (adicionar ao import de Settings/ChevronDown).
  - Inserir entre o `<Link href="/conta">Configurações</Link>` e o `<div>` com `LogoutButton` um novo `<Link href="/novidades" onClick={() => setIsOpen(false)}>` com as MESMAS classes do link de Configurações (`flex min-h-[44px] items-center gap-2 rounded-lg px-3 py-2 text-sm text-text-secondary hover:bg-bg-elevated hover:text-text-primary transition-colors duration-200 font-body`), contendo `<Sparkles className="h-4 w-4" />` e o texto `Novidades` (F35-APP-SHELL-03). Link puro — SEM badge (não duplicar lógica client no menu; tasks.md 7.7).
  - Não alterar aria-haspopup/aria-expanded/Escape/reduced-motion já existentes.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <check>`rg -n "novidades|Novidades|SidebarBadge|Sparkles" src/components/shell/sidebar.tsx src/components/shell/account-menu.tsx` mostra as 5 inserções</check>
  </verify>
  <done>
    Sidebar tem 5 links (Dashboard, Campanhas, Loja, Conta, Novidades) com ícone Sparkles e SidebarBadge no item Novidades; AccountMenu tem link "Novidades" com Sparkles entre Configurações e Sair; nenhum import de get-changelog/server-only; `npm run typecheck` limpo.
  </done>
</task>

<task type="auto">
  <name>Task 3: Dashboard — ChangelogAnnouncement contextual (F35-DASHBOARD-01, F35-DASHBOARD-02)</name>
  <files>
    src/app/(app)/dashboard/page.tsx
  </files>
  <action>
  Modificar `src/app/(app)/dashboard/page.tsx` (server component):

  1. Imports novos: `import { getLatestAnnouncement } from "@/lib/changelog/get-changelog";` (server-only — permitido, é server component) e `import { ChangelogAnnouncement } from "@/components/changelog/changelog-announcement";`.

  2. No case `has_store_no_campaigns`: após `const storeNoCamp = await getCurrentStore(user.userId);` (dentro do bloco), adicionar `const latestAnnouncement = await getLatestAnnouncement();`. Renderizar `<ChangelogAnnouncement entry={latestAnnouncement} />` imediatamente APÓS os blocos `<VerificationBanners .../>` e `<ReadinessCheckBanner .../>` (condicionados a `storeNoCamp`), ANTES do `<div className="mb-4">` do BalanceDisplay (F35-DASHBOARD-01 — posição: depois dos banners, antes do conteúdo principal).

  3. No case `has_store_with_campaigns`: após a resolução de `store`/créditos/Promise.all (antes do `return`), adicionar `const latestAnnouncement = await getLatestAnnouncement();`. Renderizar `<ChangelogAnnouncement entry={latestAnnouncement} />` imediatamente APÓS `<VerificationBanners .../>` e `<ReadinessCheckBanner .../>`, ANTES do `<h2>` de greeting (conteúdo principal).

  4. Em ambos os casos, renderizar SEM condicional de null: o componente `ChangelogAnnouncement` retorna `null` quando `entry` é null ou `announcement === "none"` ou dispensado (contrato do 35-03) — F35-DASHBOARD-02: dashboard não quebra com `latestAnnouncement === null`.

  5. NÃO renderizar anúncio no case `no_store` (usuário sem loja não vê anúncio).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <check>`rg -n "ChangelogAnnouncement|getLatestAnnouncement" src/app/\(app\)/dashboard/page.tsx` — 2 imports + 2 renderizações</check>
  </verify>
  <done>
    Dashboard renderiza `<ChangelogAnnouncement entry={latestAnnouncement} />` após VerificationBanners e ReadinessCheckBanner nos estados has_store_no_campaigns e has_store_with_campaigns; sem condicional de null (componente se defende); `npm run typecheck` limpo.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server layout → client shell (prop latestChangelogEntryId) | Valor derivado do filesystem atravessa a fronteira server/client por prop |
| dashboard (server) → ChangelogAnnouncement (client) | Entry de anúncio atravessa server→client como prop serializável |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-09 | Tampering | client components do shell (app-shell/sidebar/sidebar-drawer/account-menu) | mitigate | Fluxo exclusivo por prop `latestChangelogEntryId`; verificação automatizada: `rg "get-changelog|getAllEntries|server-only"` nos arquivos do shell retorna vazio — impede que código server-only vaze para o bundle client |
| T-35-10 | Information Disclosure | latestEntryId / entry de anúncio | accept | Changelog é público e idêntico para todos os usuários (D7 — sem segmentação); expor id de entry não revela dado sensível |
| T-35-11 | Tampering (Injection) | dashboard → ChangelogAnnouncement | mitigate | Herda mitigação do renderer (35-01) e do componente (35-03): dashboard nunca injeta HTML próprio; anúncio usa exclusivamente saída de renderMarkdown sanitizada |
| T-35-SC | Tampering | instalação de pacotes | N/A | Zero dependências novas (D8) |
</threat_model>

<verification>
### Verificação do plano

1. `npm run typecheck` — zero erros
2. `npm run lint` — zero erros (arquivos do shell modificados)
3. Grep de arquitetura: `rg "get-changelog|getAllEntries|server-only" src/components/shell/` retorna vazio
4. Grep de posicionamento: `rg -n "ChangelogAnnouncement" src/app/\(app\)/dashboard/page.tsx` — 2 ocorrências de renderização (uma por case com loja)
5. `npx vitest run src/components/` — regressão dos testes de shell/dashboard (existentes) continua passando
</verification>

<success_criteria>
- Fluxo latestEntryId completo: layout → AppShell → Sidebar/SidebarDrawer → SidebarBadge (F35-APP-SHELL-01)
- Sidebar com 5º item "Novidades" + indicador (F35-APP-SHELL-02); AccountMenu com link "Novidades" (F35-APP-SHELL-03)
- Dashboard com anúncio contextual posicionado após banners (F35-DASHBOARD-01) e null-safe (F35-DASHBOARD-02)
- `npm run typecheck` e `npm run lint` limpos; regressão dos testes de componentes do shell/dashboard verde
</success_criteria>

<output>
Create `.planning/phases/35-changelog-novidades/35-04-SUMMARY.md` when done
</output>

---
phase: 35-changelog-novidades
plan: 05
type: execute
wave: 3
depends_on:
  - 35-01
  - 35-02
  - 35-03
  - 35-04
files_modified:
  - docs/changelog-update.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - F35-DASHBOARD-03
must_haves:
  truths:
    - "docs/changelog-update.md permanece como guia único da rotina (sem docs/changelog.md duplicado), consistente com a implementação: nome de arquivo YYYY-MM-DD-slug.md, date como data civil sem shift de fuso, exemplo com announcement card"
    - "14+ testes novos da F35 passando (parser, renderer, schema, date, get-changelog, hook, list, announcement, badge)"
    - "npx vitest run completo verde (novos + regressão); npm run typecheck, npm run lint e npm run build limpos"
    - "STATE.md e ROADMAP.md atualizados: F35 = Changelog/Novidades ✅, F36 = Stripe/Monetização Pública (futura)"
  artifacts:
    - path: "docs/changelog-update.md"
      provides: "Guia de rotina de atualização do changelog (ajustado cirurgicamente)"
      contains: "content/changelog"
    - path: ".planning/STATE.md"
      provides: "Estado do projeto com F35 completa"
      contains: "35-changelog-novidades"
    - path: ".planning/ROADMAP.md"
      provides: "Roadmap com F35 ✅ e F36 = Stripe"
      contains: "F36"
---

<objective>
**Plano 5: Rotina + Testes + Verificação + Tracking**

Fechar a fase: verificar e ajustar cirurgicamente o guia de rotina `docs/changelog-update.md` (F35-DASHBOARD-03), executar a verificação completa (todos os testes da F35 + regressão, typecheck, lint, build), validar o gate fail-fast do parser/renderer em build, atualizar STATE.md/ROADMAP.md com a renumeração F35 = Changelog/Novidades e F36 = Stripe, e finalizar com validação humana visual do fluxo completo.

**Purpose:** Garantir consistência da rotina documentada com a implementação e fechar a fase com evidência verificável.
**Output:** Guia ajustado (se necessário), evidência de verificação, STATE.md/ROADMAP.md atualizados, aprovação visual.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/35-changelog-novidades/35-CONTEXT.md
openspec/changes/fase-35-changelog-novidades/design.md (D5, D6, D8)
openspec/changes/fase-35-changelog-novidades/specs/changelog-content/spec.md (Rotina de atualização documentada)
openspec/changes/fase-35-changelog-novidades/tasks.md (seções 9, 10, 11)
docs/changelog-update.md (guia existente — NÃO recriar)

<interfaces>
## Estado do guia existente (docs/changelog-update.md — lido)

Já contém: quando usar, decisão obrigatória no verify, deve/não-deve entrar, padrão editorial (3 perguntas), fonte dos dados (`content/changelog/`), formato de arquivo `YYYY-MM-DD-slug-da-entrega.md`, template com exemplo F34 (id `fase-34-store-readiness`, date `2026-08-01`, announcement none), campos do frontmatter, categorias, importância, announcement (none/card/modal), frequência, checklist do verify e mensagens recomendadas. O exemplo F34 do template está ALINHADO com o seed de 35-01. Possível lacuna: a linha da tabela de campos de `date` não explicita que a data é civil e exibida como `dd/mm/aaaa` sem conversão de fuso (UTC-3).

## Contagens de testes por arquivo (referência da F35)

- src/lib/changelog/__tests__/: parse-frontmatter (5+), schema (4+), render-markdown (5+), format-date (2+), get-changelog (7+)
- src/hooks/__tests__/use-changelog-state.test.ts (6+)
- src/components/changelog/__tests__/: changelog-list (2+), changelog-announcement (5+), sidebar-badge (3+)
- Total esperado: 39+ cenários (gate mínimo: 14+, requisito F35)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/changelog-update.md — verificação e ajuste cirúrgico (F35-DASHBOARD-03)</name>
  <files>
    docs/changelog-update.md
  </files>
  <action>
  Verificar o guia existente contra a implementação da F35 e ajustar APENAS se necessário (edição cirúrgica — NÃO recriar o arquivo, NÃO criar `docs/changelog.md`):

  1. Conferir que o guia cobre: quando atualizar (D6 — obrigatório ao final de fase com impacto visível; recomendado agrupar por milestone; não obrigatório infra/refatoração/testes), passo a passo de criação de entry, template de frontmatter com todos os campos (id, title, date, milestone opcional, category, importance, announcement), exemplo de entry completo, critérios editoriais (D5 — o que mudou, por que importa, o que o lojista precisa fazer) e checklist "Changelog atualizado?" no verify.
  2. Confirmar alinhamento com a implementação: formato de arquivo `YYYY-MM-DD-slug-da-entrega.md` (idêntico aos seeds), exemplo F34 do template igual ao seed real (id `fase-34-store-readiness`, date `2026-08-01`, improvement/minor/none), valores de announcement documentados (none padrão / card destaque / modal mudança crítica — D4).
  3. Ajuste cirúrgico SE houver lacuna: na tabela "Campos do Frontmatter", linha de `date`, acrescentar nota curta: data civil (sem hora) no fuso brasileiro; a exibição `dd/mm/aaaa` deriva da string, sem conversão de timezone (evita shift de dia em UTC-3) — alinhada ao contrato do spec "Data de publicação sem distorção de fuso horário". Se o exemplo de entry com `announcement: "card"` não existir no guia, acrescentar UMA frase de exemplo citando `announcement: "card"` para destaque leve na dashboard (não criar seção nova inteira).
  4. NÃO alterar o tom/estrutura do guia; NÃO duplicar em outro caminho.
  </action>
  <verify>
    <automated>MISSING — Task 2 cria a verificação de consistência; aqui usar: `Test-Path docs/changelog-update.md` e `-not (Test-Path docs/changelog.md)`</automated>
    <check>Grep: `rg "fuso|timezone|UTC-3|data civil" docs/changelog-update.md` retorna a nota adicionada (se ajuste foi necessário)</check>
  </verify>
  <done>
    `docs/changelog-update.md` existe e está consistente com a implementação (formato de arquivo, fuso da data, exemplo de announcement); nenhum `docs/changelog.md` foi criado; edições foram mínimas e cirúrgicas.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verificação completa + Tracking (testes, typecheck, lint, build, STATE/ROADMAP)</name>
  <files>
    .planning/STATE.md
    .planning/ROADMAP.md
  </files>
  <action>
  Executar a verificação final e atualizar o tracking:

  1. Testes da F35: `npx vitest run src/lib/changelog/ src/hooks/__tests__/use-changelog-state.test.ts src/components/changelog/` — todos os cenários novos passando; contar o total de `it(` dos arquivos da F35 (esperado 39+, gate mínimo 14+ — requisito F35). Registrar a contagem real no SUMMARY.
  2. Regressão: `npx vitest run` — suíte completa verde (base anterior 1201 testes em 154 arquivos + novos da F35).
  3. `npm run typecheck` — zero erros. `npm run lint` — zero erros.
  4. `npm run build` — build bem-sucedido. Este é o gate fail-fast (D8): qualquer frontmatter inválido ou sintaxe markdown não suportada em `content/changelog/` quebra o build; um build verde prova que os seeds passam na validação Zod e no renderer. (getAllEntries roda em layout/dashboard — rotas dinâmicas por cookies, mas o build compila e pré-valida o caminho de módulos; o teste de seed do 35-02 cobre a validação de conteúdo em CI.)
  5. Grep de sanidade final: `rg "as unknown as|console\.log" src/lib/changelog/` retorna vazio; `rg "dangerouslySetInnerHTML" src/components/changelog/` mostra apenas usos com `renderMarkdown(`.
  6. Atualizar `.planning/STATE.md`:
  - Novo bloco "Phase 35 — Changelog/Novidades ✅" com a tabela de 5 planos (35-01 Foundation, 35-02 Core Library, 35-03 Página + Componentes, 35-04 App Shell + Dashboard, 35-05 Rotina + Verificação), contagem de testes (base 1201 + novos), typecheck/lint/build limpos, contexto `.planning/phases/35-changelog-novidades/35-CONTEXT.md` e source `openspec/changes/fase-35-changelog-novidades/`.
  - Linha "Current Position": `Phase: 35 — CHANGELOG/NOVIDADES ✅`.
  - Atualizar a tabela "Next Phases": F35 → ✅ Completed (Changelog/Novidades); F36 → ○ Future (Stripe / Monetização Pública) — renumeração documentada (F35 CONTEXT: "F35 = Changelog/Novidades, F36 = Stripe/Monetização Pública").
  7. Atualizar `.planning/ROADMAP.md`:
  - Overview: linha 35 → status ✅ (marcar "✅ Changelog/Novidades").
  - Seção "Phase 35" (ou tabela de planos já existente): marcar planos e adicionar linha "**Status:** 5/5 plans completos ✅" e testes/typecheck/lint/build.
  - Nota de renumeração já presente na linha 7 ("F35 = Changelog/Novidades, F36 = Stripe") — manter; garantir que a tabela de fases mostra F36 Stripe como próxima.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    Suíte completa verde (1201 + 39+ novos); typecheck/lint/build limpos; contagem de testes da F35 ≥ 14 registrada; STATE.md com F35 ✅ e F36 futura; ROADMAP.md com F35 ✅ e renumeração documentada.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Validação visual e funcional do fluxo completo (checkpoint)</name>
  <files>
    src/app/(app)/novidades/page.tsx
    src/components/shell/sidebar.tsx
    src/components/shell/account-menu.tsx
    src/app/(app)/dashboard/page.tsx
    docs/changelog-update.md
  </files>
  <action>
    Checkpoint de verificação humana pós-automação. Toda a automação da F35 já foi executada e validada (testes, typecheck, lint, build na Task 2). O usuário valida agora o comportamento visível e funcional do changelog no browser: página /novidades, indicador da sidebar, card de anúncio na dashboard, dispensa × visita, link do AccountMenu e consistência do guia docs/changelog-update.md.
  </action>
  <verify>O usuário executa os passos numerados abaixo e reporta o resultado — cada passo tem um comportamento esperado objetivo.</verify>
  <done>Aprovação do usuário: todos os passos com comportamento esperado conforme abaixo; qualquer divergência é reportada como issue para correção.</done>
  <what-built>
    Página /novidades com 3 entries, sidebar com 5º item "Novidades" + indicador, card de anúncio na dashboard (F32), link "Novidades" no AccountMenu, e guia docs/changelog-update.md verificado.
  </what-built>
  <how-to-verify>
    Com o app rodando (`npm run dev`) e autenticado com uma conta com loja:

    1. **Sidebar:** o item "Novidades" aparece como 5º link (após Conta) com ponto indicador verde quando há conteúdo novo (primeiro acesso — localStorage vazio; para resetar: DevTools → Application → Local Storage → remover `vendeo:last_seen_changelog_id` e `vendeo:dismissed_changelog_announcement_id`, depois recarregar).
    2. **Dashboard:** com localStorage limpo, o card de anúncio "Freemium CNPJ" (F32) aparece acima do conteúdo, abaixo dos banners de verificação/readiness. Clicar em × → card some E o indicador da sidebar CONTINUA ativo (dispensa ≠ visita). Recarregar a página → card não reaparece.
    3. **Página /novidades:** visitar /novidades → 3 entries na ordem F34 (01/08/2026) → F32 (31/07/2026) → F30 (30/07/2026), badges de categoria com cores corretas (verde funcionalidade, azul melhoria), datas em dd/mm/aaaa sem deslocamento de dia, conteúdo renderizado com headings/listas.
    4. **Indicador some:** após visitar /novidades, voltar ao dashboard → o indicador da sidebar sumiu E o anúncio não reaparece (visita marca como visto e dispensa).
    5. **AccountMenu:** clicar no menu da conta (canto superior) → dropdown mostra Configurações, Novidades (entre Configurações e Sair) e Sair; "Novidades" navega para /novidades.
    6. **Botão "Ver novidades"** no card de anúncio (resetar localStorage e recarregar o dashboard para o card voltar): navega para /novidades e o card some ao voltar.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issue found (ex: "aprovado", "card não some", "data deslocada")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/changelog-update.md → contribuidores | Guia instrui autores de entries; se divergir do parser/renderer, entries inválidas entram no repo |
| content/changelog → build (CI/Vercel) | Conteúdo inválido deve falhar no build, nunca chegar a produção |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-12 | Tampering | docs/changelog-update.md (desalinhamento) | mitigate | Verificação cirúrgica do guia contra a implementação (formato de arquivo, fuso da data, announcement) na Task 1; teste de seed do 35-02 valida entries reais em CI — guia desatualizado gera entries que o CI rejeita |
| T-35-13 | Tampering | build (conteúdo inválido silencioso) | mitigate | `npm run build` na Task 2 valida o gate fail-fast (Zod + renderer); qualquer entry inválida em content/changelog quebra o build/CI — nenhuma entry inválida chega a produção |
| T-35-SC | Tampering | instalação de pacotes | N/A | Zero dependências novas (D8) |
</threat_model>

<verification>
### Verificação do plano

1. `npx vitest run` — suíte completa verde (regressão + 14+ novos da F35)
2. `npm run typecheck` / `npm run lint` / `npm run build` — limpos
3. `Test-Path docs/changelog-update.md` = True; `Test-Path docs/changelog.md` = False
4. `.planning/STATE.md` e `.planning/ROADMAP.md` refletem F35 ✅ / F36 Stripe (grep "F36" no ROADMAP)
5. Checkpoint humano aprovado (Task 3)
</verification>

<success_criteria>
- `docs/changelog-update.md` verificado/ajustado cirurgicamente, sem duplicação (F35-DASHBOARD-03)
- 14+ testes novos passando + regressão completa verde; typecheck, lint e build limpos
- STATE.md e ROADMAP.md atualizados com F35 = Changelog/Novidades ✅ e F36 = Stripe/Monetização Pública (futura)
- Fluxo completo validado visualmente pelo usuário (checkpoint aprovado)
</success_criteria>

<output>
Create `.planning/phases/35-changelog-novidades/35-05-SUMMARY.md` when done
</output>

---
phase: 36-onboarding-navegacao-por-abas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "supabase/migrations/20260801000001_f36_create_store_draft.sql"
  - "src/app/api/store/route.ts"
autonomous: false
requirements:
  - F36-DRAFT-CREATE-01
  - F36-DRAFT-CREATE-02
  - F36-OWNERSHIP-01
user_setup:
  - service: Supabase
    why: "supabase db push (Task 3) requires an authenticated CLI session or access token"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard → Account → Access Tokens (criar token) — ou sessão `supabase login` já ativa"
must_haves:
  truths:
    - "POST /api/store sem CNPJ retorna 201 com `onboardingGranted: false` e NÃO concede crédito freemium"
    - "POST /api/store com CNPJ mantém o fluxo verified/fiscal existente (`create_store_with_cnpj`) intacto"
    - "A RPC `create_store_draft` está aplicada no banco remoto (schema push aplicado)"
    - "Nenhum caminho expõe `cnpj_root_hash` ao cliente"
  artifacts:
    - path: "supabase/migrations/20260801000001_f36_create_store_draft.sql"
      provides: "RPC create_store_draft (SECURITY DEFINER, service_role only, sem grant)"
      contains: "create_store_draft"
    - path: "src/app/api/store/route.ts"
      provides: "POST /api/store em dois modos (draft × verified)"
      exports: ["POST"]
  key_links:
    - from: "src/app/api/store/route.ts"
      to: "supabase.rpc(\"create_store_draft\")"
      via: "branch sem cnpj"
      pattern: "create_store_draft"
    - from: "src/app/api/store/route.ts"
      to: "supabase.rpc(\"create_store_with_cnpj\")"
      via: "branch com cnpj (inalterado)"
      pattern: "create_store_with_cnpj"
    - from: "src/app/api/store/route.ts"
      to: "user.userId"
      via: "requireUser() antes de qualquer operação de banco; body user_id ignorado"
      pattern: "requireUser"
---

<objective>
Backend da F36: criar a RPC `create_store_draft` (nova migration) e refatorar `POST /api/store` para suportar dois modos de criação — **draft** (sem CNPJ, sem grant freemium) e **verified/fiscal** (com CNPJ, caminho F32/F33 inalterado). Aplicar a migration no banco remoto via `supabase db push` ([BLOCKING] — a fase não passa na verificação sem isso).

Purpose: permitir que o auto-save do onboarding crie a loja sem CNPJ (D1/D4/D15), mantendo a regra "loja draft não é loja pronta": não gera campanha nem recebe crédito até cadastro fiscal válido.
Output: migration SQL aplicada + rota em dois modos + validação RPC no banco.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-onboarding-navegacao-por-abas/36-CONTEXT.md
@.planning/phases/36-onboarding-navegacao-por-abas/36-PATTERNS.md

<interfaces>
Contratos que o executor precisa (extraídos de 36-PATTERNS.md e do código atual):

RPC create_store_draft (D15 — assinatura exata, 17 parâmetros):
`(p_user_id UUID, p_name TEXT, p_segment TEXT, p_city TEXT, p_state TEXT, p_accepted_by_user_id UUID, p_terms_version TEXT, p_acceptable_use_version TEXT, p_ip_address TEXT, p_user_agent TEXT, p_brand_color TEXT, p_logo_url TEXT, p_subsegment TEXT, p_tone_of_voice TEXT, p_positioning TEXT, p_short_description TEXT, p_slogan TEXT)` → RETURNS JSONB, retorna `jsonb_build_object('store', v_store_data, 'onboardingGranted', false)`.

Migration template (analog exato): `supabase/migrations/20260727000001_freemium_anti_abuso_cnpj.sql` linhas 202-277 (create_store_with_cnpj). Grants (padrão `20260723000005_create_legal_helpers.sql:26-27,52-53`): `REVOKE EXECUTE ... FROM PUBLIC, anon, authenticated; GRANT EXECUTE ... TO service_role;`

POST /api/store atual (src/app/api/store/route.ts):
- Skeleton a preservar: `requireSameOrigin(request)` (linha 42), `requireUser()` (44), body JSON (45-48), validações name 2-60 chars (59-64), segment em `STORE_SEGMENTS` (66-72), `if (!cnpj || typeof cnpj !== "string")` 400 — **REMOVIDO na F36** (74-76), validação subsegmento (182-209), `getCurrentVersion("terms_of_service")`/`("acceptable_use")` (211-219), IP/UA (221-225), rpc `create_store_with_cnpj` (227-254), erro 23505 → 409 "Usuário já possui uma loja" (256-268), unwrap `rpcStore` (272-282), resposta 201 (284-292), catch 401 (293-298).
- `supabase` = `supabaseAdmin` (service_role) — linha 2.
- Helpers já importados: `validateCnpj`, `hashCnpjRoot`, `maskCnpj`, `isCnpjDuplicateError`, `CNPJ_DUPLICATE_RESPONSE`, `validateSubsegment`/`sanitizeSubsegment` (locais, 23-39).

Convenção de timestamp: a migration mais recente é `20260731000004_publish_legal_beta_freemium_versions.sql` → nova = `20260801000001_f36_create_store_draft.sql`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration SQL — RPC create_store_draft</name>
  <files>supabase/migrations/20260801000001_f36_create_store_draft.sql</files>
  <read_first>
    - supabase/migrations/20260801000001_f36_create_store_draft.sql (a ser criado — não existe)
    - supabase/migrations/20260727000001_freemium_anti_abuso_cnpj.sql (template RPC create_store_with_cnpj, linhas 202-277)
    - supabase/migrations/20260723000005_create_legal_helpers.sql (padrão de grants, linhas 26-27, 52-53)
    - .planning/phases/36-onboarding-navegacao-por-abas/36-PATTERNS.md (seção "supabase/migrations/<ts>_f36_create_store_draft.sql" — skeleton RPC completo, linhas 473-526)
  </read_first>
  <action>
    Criar `supabase/migrations/20260801000001_f36_create_store_draft.sql` com:

    1. Comentário de cabeçalho no estilo dos banners `-- ====` das migrations existentes, descrição da RPC e seção `-- REVERT` comentada (DROP FUNCTION public.create_store_draft(...) com a assinatura completa dos 17 parâmetros).

    2. `CREATE OR REPLACE FUNCTION public.create_store_draft(...)` — assinatura EXATA de 17 parâmetros do contrato D15 (p_user_id, p_name, p_segment, p_city, p_state, p_accepted_by_user_id, p_terms_version, p_acceptable_use_version, p_ip_address, p_user_agent, p_brand_color, p_logo_url, p_subsegment, p_tone_of_voice, p_positioning, p_short_description, p_slogan). `RETURNS JSONB`, `LANGUAGE plpgsql`, `SECURITY DEFINER`, `SET search_path = ''`.

    3. Corpo: INSERT em `public.stores` com `name, segment, user_id, city, state, brand_color, logo_url, subsegment, tone_of_voice, positioning, short_description, slogan` — **sem** cnpj_normalized/cnpj_root_hash/razao_social/nome_fantasia (ficam NULL, colunas já nullable — sem ALTER). `RETURNING id INTO v_store_id`.

    4. INSERT dos 2 aceites legais em `public.legal_acceptances (store_id, accepted_by_user_id, document_type, document_version, ip_address, user_agent, acceptance_source)` com valores `('terms_of_service', p_terms_version, ...)` e `('acceptable_use', p_acceptable_use_version, ...)`, ambos `acceptance_source = 'onboarding'`.

    5. **NÃO** chamar `try_grant_onboarding_entitlement` nem `grant_credits` (comentário no código explicando D15 — "no grant freemium na criação draft").

    6. `SELECT jsonb_agg(row_to_json(s)) INTO v_store_data FROM (SELECT * FROM public.stores WHERE id = v_store_id) s;` e `RETURN jsonb_build_object('store', v_store_data, 'onboardingGranted', false);`

    7. Grants: `REVOKE EXECUTE ON FUNCTION public.create_store_draft(...) FROM PUBLIC, anon, authenticated;` + `GRANT EXECUTE ON FUNCTION public.create_store_draft(...) TO service_role;` (assinatura completa na REVOKE/GRANT).

    Não criar a RPC antiga `create_store_with_legal_acceptance` (não é restaurada — D15/deferred).
  </action>
  <verify>
    <automated>SQL válido: arquivo criado; `grep -c "create_store_draft" supabase/migrations/20260801000001_f36_create_store_draft.sql` ≥ 3 (definição + REVOKE + GRANT); `grep -c "grant_credits\\|try_grant_onboarding_entitlement" supabase/migrations/20260801000001_f36_create_store_draft.sql` == 0; `grep -c "SECURITY DEFINER"` == 1; `grep -c "SET search_path = ''"` == 1; `grep -c "service_role"` ≥ 1</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `supabase/migrations/20260801000001_f36_create_store_draft.sql` existe
    - A função `public.create_store_draft` declara exatamente os 17 parâmetros do contrato D15
    - O corpo NÃO contém `grant_credits` nem `try_grant_onboarding_entitlement`
    - `RETURN jsonb_build_object('store', v_store_data, 'onboardingGranted', false);` presente
    - `SECURITY DEFINER` + `SET search_path = ''` presentes; `REVOKE ... FROM PUBLIC, anon, authenticated` e `GRANT ... TO service_role` presentes
    - Não há `ALTER TABLE` (sem mudança de coluna — `stores.cnpj_normalized` já é nullable)
    - Não há referência a `create_store_with_legal_acceptance`
  </acceptance_criteria>
  <done>Migration SQL criada com RPC create_store_draft completa (17 params, sem grant, service_role only, REVERT documentado)</done>
</task>

<task type="auto">
  <name>Task 2: POST /api/store em dois modos (draft × verified/fiscal)</name>
  <files>src/app/api/store/route.ts</files>
  <read_first>
    - src/app/api/store/route.ts (arquivo a modificar — ler por inteiro, 318 linhas)
    - .planning/phases/36-onboarding-navegacao-por-abas/36-PATTERNS.md (seção "src/app/api/store/route.ts" — skeleton + branch draft, linhas 431-471)
    - src/__tests__/api/store-creation-matrix.test.ts (mocks existentes que a rota deve continuar satisfazendo)
  </read_first>
  <action>
    Refatorar `POST` em `src/app/api/store/route.ts`:

    1. REMOVER o bloco `if (!cnpj || typeof cnpj !== "string") { return ... "CNPJ é obrigatório" ... 400 }` (linhas 74-76 atuais).

    2. Estruturar branch por presença de CNPJ no body (após as validações comuns de name/segment/acceptedTerms):

    - **Com CNPJ (string não-vazia):** manter TODO o caminho verified/fiscal existente intocado (validateCnpj, hashCnpjRoot, checagem duplicado, CnpjVerificationService/BrasilApi/Cnpja, subsegmento, versões, rpc `create_store_with_cnpj`, tratamento 23505/duplicado, maskCnpj, resposta 201 com cnpjMasked/onboardingGranted/verificationStatus/message).

    - **Sem CNPJ (ausente, null ou string vazia):** MODO DRAFT — reutilizar: validação de subsegmento (`validateSubsegment`/`sanitizeSubsegment` — linhas 182-209, incluindo a regra "outros" obrigatório), `getCurrentVersion("terms_of_service")`/`("acceptable_use")` (211-219, incluindo 500 se não publicadas), IP/UA (221-225). Chamar `supabase.rpc("create_store_draft", { p_user_id: user.userId, p_name: (name as string).trim(), p_segment: segment as string, p_city: typeof city === "string" ? city : null, p_state: typeof state === "string" ? state : null, p_accepted_by_user_id: user.userId, p_terms_version: termsVersion.version, p_acceptable_use_version: aupVersion.version, p_ip_address: ipAddress, p_user_agent: userAgent, p_brand_color: typeof brand_color === "string" ? brand_color : null, p_logo_url: typeof logo_url === "string" ? logo_url : null, p_subsegment: effectiveSubsegment, p_tone_of_voice: typeof tone_of_voice === "string" ? tone_of_voice : null, p_positioning: typeof positioning === "string" ? positioning.trim() || null : null, p_short_description: typeof short_description === "string" ? short_description.trim() || null : null, p_slogan: typeof slogan === "string" ? slogan.trim() || null : null })`.

    Erros do branch draft: `error.code === "23505"` ou mensagem contendo `stores_user_id_key` → 409 `{ error: "Usuário já possui uma loja" }`; outro erro → 500 `{ error: error.message }` (mesmo padrão do branch verified, linhas 256-268 — sem isCnpjDuplicateError no draft, pois não há CNPJ).

    Resposta draft: mesmo unwrap `rpcStore` (Array.isArray / objeto) das linhas 272-282; se `!rpcStore || !("id" in rpcStore)` → 500 `{ error: "Loja criada, mas resposta não retornou o ID da loja." }`; retornar `NextResponse.json({ ...rpcStore, onboardingGranted: rpcData.onboardingGranted ?? false }, { status: 201 })` — **sem** cnpjMasked/verificationStatus no draft (não há CNPJ).

    3. Manter intactos: `requireSameOrigin` (42), `requireUser` (44), validações name/segment/acceptedTerms (52-72), catch `UnauthorizedError` → 401 (293-298), e o `GET` (301-318). `user_id` continua vindo de `user.userId` (claims.sub) — body `user_id` continua ignorado.

    4. Adicionar comentário de código no branch draft: referência a D15 — "loja draft não é loja pronta: sem grant freemium; readiness reporta cadastro_fiscal pendente; transição draft→fiscal via POST /api/store/update-cnpj".
  </action>
  <verify>
    <automated>npm run typecheck; npm run lint; npx vitest run src/__tests__/api/store-ownership-api.test.ts src/__tests__/api/store-creation-matrix.test.ts — permitir APENAS falhas da asserção "CNPJ é obrigatório" (que será migrada na 36-06); todas as demais asserções devem passar</automated>
  </verify>
  <acceptance_criteria>
    - O bloco 400 "CNPJ é obrigatório" (linhas 74-76 antigas) foi removido
    - Existe um branch `supabase.rpc("create_store_draft", { p_user_id: user.userId, ... })` com os 17 parâmetros do contrato
    - O branch verified (`create_store_with_cnpj`) permanece funcionalmente inalterado
    - Resposta draft: 201 com `onboardingGranted: false` e sem `cnpjMasked`/`verificationStatus`
    - 409 "Usuário já possui uma loja" tratado no draft (23505/stores_user_id_key); 401 mantido via UnauthorizedError
    - `requireUser()` permanece antes de qualquer operação de banco; nenhuma leitura de `body.user_id`
    - `grep -n "onboardingGranted" src/app/api/store/route.ts` presente nos dois branches
  </acceptance_criteria>
  <done>POST /api/store cria loja em modo draft sem CNPJ (201, sem grant) e mantém o modo verified/fiscal com CNPJ intocado</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Schema push — supabase db push + validação RPC</name>
  <files>supabase/migrations/20260801000001_f36_create_store_draft.sql</files>
  <read_first>
    - supabase/config.toml (projeto linkado — `.temp/` presente)
    - .env.local ou ambiente (presença de SUPABASE_ACCESS_TOKEN)
  </read_first>
  <action>
    Aplicar a migration no banco remoto. O projeto já está linkado (`supabase/.temp` existe, CLI 2.75.0 instalada).

    1. Se `SUPABASE_ACCESS_TOKEN` não estiver no ambiente, procurar em `.env.local`/secrets; se ausente, pedir ao usuário o token (Supabase Dashboard → Account → Access Tokens) via checkpoint — a tarefa é [BLOCKING] e o plano é `autonomous: false`.

    2. Rodar `supabase db push` na raiz do repo com `$env:SUPABASE_ACCESS_TOKEN` setado (workaround non-TTY). Se o push pedir confirmação interativa que não pode ser suprimida, parar e sinalizar intervenção manual (não tentar força bruta).

    3. Validar: `supabase migration list` deve mostrar `20260801000001_f36_create_store_draft` como aplicada no remoto; e `supabase db push` deve sair com código 0 sem erro de SQL.

    4. Validação funcional da RPC (se o banco local/remoto permitir consulta): `SELECT proname, prosecdef FROM pg_proc WHERE proname = 'create_store_draft';` deve retornar 1 linha com `prosecdef = true`. Se não houver acesso de consulta direto, registrar a validação do push (migration list) como evidência.

    NÃO rodar testes de integração com a RPC neste plano (a cobertura de endpoint/mock está na 36-06).
  </action>
  <verify>
    <automated>supabase migration list — migration 20260801000001_f36_create_store_draft presente no remoto; exit code 0 do `supabase db push`</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` terminou com sucesso (exit 0) OU intervenção manual registrada com evidência
    - `supabase migration list` mostra a migration `20260801000001_f36_create_store_draft` no remoto
    - A RPC `create_store_draft` está presente no banco remoto (pg_proc ou evidência equivalente)
    - Se o push exigiu autenticação, o token foi obtido do usuário e o push concluído (checkpoint resolvido)
  </acceptance_criteria>
  <done>Schema aplicado: RPC create_store_draft existe no banco remoto — pré-requisito obrigatório para a verificação da fase</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→POST /api/store | Input não confiável (body JSON) cruza aqui; usuário pode tentar forjar user_id, aceites ou CNPJ |
| API route→Supabase RPC | Chamada service_role (supabaseAdmin); a RPC é SECURITY DEFINER — executa como owner |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Spoofing | POST /api/store (body user_id) | mitigate | `requireUser()` antes de qualquer operação de banco; `user_id` sempre de `user.userId` (claims.sub); `body.user_id` ignorado — F36-OWNERSHIP-01 |
| T-36-02 | Elevation of Privilege | RPC create_store_draft | mitigate | `SECURITY DEFINER` + `SET search_path = ''` (impede hijack de search_path); `REVOKE EXECUTE FROM PUBLIC, anon, authenticated`; `GRANT EXECUTE TO service_role` apenas — F36-DRAFT-CREATE-01 |
| T-36-03 | Information Disclosure | Resposta POST /api/store | mitigate | `cnpj_root_hash` nunca entra na resposta (draft não tem CNPJ; verified usa o mesmo unwrap atual que não o expõe) — F36-OWNERSHIP-01 |
| T-36-04 | Elevation of Privilege / Abuse | grant freemium na criação draft | mitigate | `create_store_draft` NÃO chama `try_grant_onboarding_entitlement`/`grant_credits`; `onboardingGranted` hardcoded `false` — F36-DRAFT-CREATE-01/02 |
| T-36-05 | Spoofing (duplicidade) | 409 store/CNPJ | mitigate | Tratamento 23505/`stores_user_id_key` no draft; `isCnpjDuplicateError`+`CNPJ_DUPLICATE_RESPONSE` no verified (já existentes, preservados) |
| T-36-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo é instalado neste plano (sem package-manager install) — gate de legitimidade não se aplica |
</threat_model>

<verification>
- `npm run typecheck` limpo e `npm run lint` limpo após Task 2
- Testes existentes de `store-ownership-api` passam (só a asserção "CNPJ obrigatório" de `store-creation-matrix` pode falhar — migrada na 36-06)
- `supabase db push` aplicado; `supabase migration list` confirma `20260801000001_f36_create_store_draft` no remoto
</verification>

<success_criteria>
- RPC `create_store_draft` no banco remoto (SECURITY DEFINER, service_role only, sem grant)
- `POST /api/store` em dois modos: draft (201, `onboardingGranted: false`) × verified/fiscal (inalterado)
- Sem exposição de `cnpj_root_hash`; `requireUser()` antes de todo acesso a banco
- Schema push [BLOCKING] concluído — pré-requisito para a verificação final da fase
</success_criteria>

<output>
Create `.planning/phases/36-onboarding-navegacao-por-abas/36-01-SUMMARY.md` when done
</output>

---
phase: 36-onboarding-navegacao-por-abas
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/lib/store-onboarding/tabs.ts"
  - "src/lib/store-onboarding/tab-state.ts"
  - "src/lib/store-onboarding/draft-store.ts"
  - "src/lib/store-onboarding/__tests__/tabs.test.ts"
  - "src/lib/store-onboarding/__tests__/tab-state.test.ts"
  - "src/lib/store-onboarding/__tests__/draft-store.test.ts"
autonomous: true
requirements:
  - F36-TABS-01
  - F36-TABS-02
  - F36-AUTOSAVE-04
  - F36-DRAFT-01
must_haves:
  truths:
    - "As 3 abas (dados/posicionamento/direcao-visual) têm definição tipada, ordem e labels desktop/mobile"
    - "O desbloqueio progressivo é calculado por função pura (computeTabUnlock) — CNPJ nunca bloqueia"
    - "O estado por aba é calculado por função pura (computeTabState) com prioridade pending_generation > blocked > draft > ready > saved"
    - "O rascunho localStorage tem TTL 24h e chaves escopadas por usuário (:new / :${storeId})"
  artifacts:
    - path: "src/lib/store-onboarding/tabs.ts"
      provides: "OnboardingTab, TAB_ORDER, OnboardingTabDef, TabBlockReason, computeTabUnlock"
      contains: "computeTabUnlock"
    - path: "src/lib/store-onboarding/tab-state.ts"
      provides: "TabState, computeTabState"
      contains: "computeTabState"
    - path: "src/lib/store-onboarding/draft-store.ts"
      provides: "DRAFT_TTL_MS, StoreDraft, draftKey/saveDraft/restoreDraft/clearDraft"
      contains: "DRAFT_TTL_MS"
  key_links:
    - from: "tabs.ts"
      to: "computeTabUnlock"
      via: "consumido por store-tabs.tsx e use-onboarding-tabs.ts (36-03/36-04)"
      pattern: "export function computeTabUnlock"
    - from: "tab-state.ts"
      to: "computeTabState"
      via: "consumido por use-onboarding-tabs.ts (36-03)"
      pattern: "export function computeTabState"
    - from: "draft-store.ts"
      to: "localStorage"
      via: "saveDraft/restoreDraft — chaves vendeo:store_draft:${userId}:new / :${storeId}"
      pattern: "localStorage"
---

<objective>
Core Library da máquina de abas: três módulos puros em `src/lib/store-onboarding/` — `tabs.ts` (definição das abas + `computeTabUnlock`), `tab-state.ts` (`computeTabState`) e `draft-store.ts` (rascunho localStorage com TTL 24h) — com testes unitários.

Purpose: contratos testáveis que 36-03 (auto-save/drift) e 36-04 (UI) consomem sem reinterpretação. Funções puras = testáveis sem React/Supabase.
Output: 3 módulos + 3 arquivos de teste (padrão `src/lib/__tests__/drift.test.ts`).
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-onboarding-navegacao-por-abas/36-CONTEXT.md
@.planning/phases/36-onboarding-navegacao-por-abas/36-PATTERNS.md

<interfaces>
Contratos a implementar (extraídos de 36-PATTERNS.md + specs):

De tabs.ts (spec store-onboarding-tabs):
```typescript
export type OnboardingTab = "dados" | "posicionamento" | "direcao-visual";
export const TAB_ORDER: OnboardingTab[] = ["dados", "posicionamento", "direcao-visual"];
export type TabBlockReason = "needs_legal_acceptance" | "needs_tone_of_voice" | "needs_store_created" | "fiscal_pending";
export interface OnboardingTabDef { id: OnboardingTab; label: string; labelMobile: string; }
export function computeTabUnlock(tab: OnboardingTab, ctx: {
  name: string; segment: string; legalAccepted: boolean;
  storeId: string | null; toneOfVoice: string; hasVisualDirection: boolean;
}): { unlocked: boolean; reason?: TabBlockReason }
```
Regras (D1/D8/D9): `dados` → sempre `{unlocked:true}`. `posicionamento` → name+segment+legalAccepted+storeId; `needs_legal_acceptance` se sem legal; `needs_store_created` se sem storeId. `direcao-visual` → storeId+toneOfVoice; `needs_tone_of_voice` se sem toneOfVoice; `hasVisualDirection: true` faz bypass. CNPJ nunca bloqueia.

De tab-state.ts (spec store-onboarding-autosave):
```typescript
export type TabState = "blocked" | "draft" | "saved" | "ready" | "pending_generation";
export function computeTabState(tab: OnboardingTab, ctx: {
  hasLocalEdits: boolean; isPersisted: boolean; unlocked: boolean;
  readiness: StoreReadiness;  // @/lib/store-readiness
}): { state: TabState; reason?: TabBlockReason }
```
Prioridade: pending_generation > blocked > draft > ready > saved. `ready:false` + `missing` contém `"cadastro_fiscal"` → `{state:"pending_generation", reason:"fiscal_pending"}`.

De draft-store.ts (spec store-onboarding-draft):
```typescript
export const DRAFT_TTL_MS = 24 * 60 * 60 * 1000;
export interface StoreDraft { userId: string; storeId: string | null; fields: Partial<FormData>; updatedAt: number; }
export function draftKey(userId: string, storeId: string | null): string;  // `vendeo:store_draft:${userId}:new` | `:${storeId}`
export function saveDraft(draft: StoreDraft): void;                         // updatedAt = Date.now()
export function restoreDraft(userId: string, storeId: string | null): StoreDraft | null;  // expirado → null + removeItem
export function clearDraft(userId: string, storeId?: string): void;
export function clearAllDrafts(): void;                                     // logout — remove todas as chaves com prefixo `vendeo:store_draft:`
```
`FormData` vem de `@/components/flow/use-store-form` (export type FormData — campo `FormData` exportado em use-store-form.ts:13-27; NÃO existe `StoreFormData`).

Referências existentes:
- `StoreReadiness` e `MissingItem` (`"cadastro_fiscal" | "brand_profile"`) em `src/lib/store-readiness.ts` (MissingItem.item linhas 5-8; getStoreReadiness linhas 15-41).
- Padrão de testes puros: `src/lib/__tests__/drift.test.ts` (describe/it/expect de vitest).
- Padrão de storage com try/catch: `src/hooks/use-input-preservation.ts` (usa sessionStorage; draft-store estende para localStorage + TTL — documentar a escolha no código: sobrevive a fechar aba/app; TTL evita draft velho).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: tabs.ts — definição das abas + computeTabUnlock + testes</name>
  <files>src/lib/store-onboarding/tabs.ts, src/lib/store-onboarding/__tests__/tabs.test.ts</files>
  <read_first>
    - src/lib/store-onboarding/tabs.ts (a ser criado)
    - src/lib/drift.ts (padrão de função pura + policy record — linhas 11-41)
    - src/lib/constants.ts (padrão const-array `{value,label}` — linhas 1-17)
    - .planning/phases/36-onboarding-navegacao-por-abas/36-PATTERNS.md (seção tabs.ts, linhas 36-73)
  </read_first>
  <action>
    Criar `src/lib/store-onboarding/tabs.ts` (funções puras, sem React, sem imports de server):

    1. `export type OnboardingTab = "dados" | "posicionamento" | "direcao-visual";`

    2. `export const TAB_ORDER: OnboardingTab[] = ["dados", "posicionamento", "direcao-visual"];` (padrão `as const` de `src/lib/constants.ts`).

    3. `export type TabBlockReason = "needs_legal_acceptance" | "needs_tone_of_voice" | "needs_store_created" | "fiscal_pending";`

    4. `export interface OnboardingTabDef { id: OnboardingTab; label: string; labelMobile: string; }` com `export const ONBOARDING_TABS: OnboardingTabDef[]` — labels desktop: "Dados" / "Posicionamento" / "Direção Visual"; labelMobile: "Dados" / "Perfil" / "Visual". Comentário no código (D10): labelMobile é APENAS display — o `id` permanece `posicionamento`/`direcao-visual` (query param, testes, analytics).

    5. `export function computeTabUnlock(tab, ctx)` — contrato exato do `<interfaces>`:
       - `dados` → `{ unlocked: true }` sempre
       - `posicionamento` → se `!ctx.legalAccepted` → `{ unlocked: false, reason: "needs_legal_acceptance" }`; senão se `!ctx.name.trim() || !ctx.segment || !ctx.storeId` → `{ unlocked: false, reason: "needs_store_created" }`; senão `{ unlocked: true }`
       - `direcao-visual` → se `ctx.hasVisualDirection` → `{ unlocked: true }`; senão se `!ctx.storeId` → `{ unlocked: false, reason: "needs_store_created" }`; senão se `!ctx.toneOfVoice.trim()` → `{ unlocked: false, reason: "needs_tone_of_voice" }`; senão `{ unlocked: true }`
       - tab inválida (não pertence a TAB_ORDER) → `{ unlocked: false, reason: "needs_store_created" }` (fallback seguro)
       - Nenhuma regra usa CNPJ (D8 — CNPJ nunca bloqueia navegação)

    6. Exportar também `export function isOnboardingTab(value: string): value is OnboardingTab` (guard para parsing de `?tab=` na 36-04) — retorna `TAB_ORDER.includes(value as OnboardingTab)`.

    7. Criar `src/lib/store-onboarding/__tests__/tabs.test.ts` (padrão vitest de `drift.test.ts`) nesta MESMA task (o `<verify>` abaixo referencia o arquivo): desbloqueio progressivo (dados sempre aberta; posicionamento sem legal → needs_legal_acceptance; sem storeId → needs_store_created; direcao-visual sem tom de voz → needs_tone_of_voice; hasVisualDirection bypass); CNPJ nunca bloqueia (ctx com campos de CNPJ vazios ainda unlocked quando demais regras ok); `isOnboardingTab` (3 válidos + inválidos).
  </action>
  <verify>
    <automated>npx vitest run src/lib/store-onboarding/__tests__/tabs.test.ts — todos passando; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `OnboardingTab`, `TAB_ORDER` (3 itens na ordem), `TabBlockReason` (4 motivos), `OnboardingTabDef`, `ONBOARDING_TABS` (3 defs com label/labelMobile) exportados
    - `computeTabUnlock` retorna `{unlocked:true}` para `dados` com qualquer ctx
    - `posicionamento` sem legal → reason `needs_legal_acceptance`; sem storeId → `needs_store_created`
    - `direcao-visual` com `hasVisualDirection:true` → unlocked mesmo sem toneOfVoice
    - `direcao-visual` sem toneOfVoice → reason `needs_tone_of_voice`
    - Nenhuma referência a cnpj/cnpjNormalized no arquivo
    - `isOnboardingTab` retorna true apenas para os 3 valores válidos
    - Testes de tabs.ts verdes (≥ 5 cenários: dados sempre aberta, needs_legal_acceptance, needs_store_created, needs_tone_of_voice, hasVisualDirection bypass, CNPJ nunca bloqueia, isOnboardingTab)
  </acceptance_criteria>
  <done>tabs.ts com tipos, TAB_ORDER, labels, computeTabUnlock puro e testes verdes conforme D1/D8/D9</done>
</task>

<task type="auto">
  <name>Task 2: tab-state.ts — computeTabState + testes</name>
  <files>src/lib/store-onboarding/tab-state.ts, src/lib/store-onboarding/__tests__/tab-state.test.ts</files>
  <read_first>
    - src/lib/store-onboarding/tab-state.ts (a ser criado)
    - src/lib/drift.ts (padrão early-return/prioridade — linhas 100-126)
    - src/lib/store-readiness.ts (tipo StoreReadiness e MissingItem — linhas 1-41)
    - .planning/phases/36-onboarding-navegacao-por-abas/36-PATTERNS.md (seção tab-state.ts, linhas 77-101)
  </read_first>
  <action>
    Criar `src/lib/store-onboarding/tab-state.ts` (função pura):

    1. `export type TabState = "blocked" | "draft" | "saved" | "ready" | "pending_generation";`

    2. `export interface TabStateContext { hasLocalEdits: boolean; isPersisted: boolean; unlocked: boolean; readiness: StoreReadiness; }` — `StoreReadiness` importado de `@/lib/store-readiness` (interface existente; tipo de `getStoreReadiness`).

    3. `export function computeTabState(tab: OnboardingTab, ctx: TabStateContext): { state: TabState; reason?: TabBlockReason }` com prioridade de D7 na ordem:
       1. `pending_generation` — se `!ctx.readiness.ready` e `ctx.readiness.missing` contém `"cadastro_fiscal"` → `{ state: "pending_generation", reason: "fiscal_pending" }`
       2. `blocked` — se `!ctx.unlocked` → `{ state: "blocked" }` (o reason do bloqueio vem de `computeTabUnlock` — reexportar/recalcular via tabs.ts se conveniente, mas manter o reason aqui opcional)
       3. `draft` — se `ctx.hasLocalEdits && !ctx.isPersisted` → `{ state: "draft" }`
       4. `ready` — se `ctx.isPersisted && ctx.readiness.ready` → `{ state: "ready" }`
       5. `saved` — se `ctx.isPersisted` → `{ state: "saved" }`
       6. fallback → `{ state: "blocked" }`

       Reexportar os tipos de tabs.ts (`OnboardingTab`, `TabBlockReason`) por conveniência: `export type { OnboardingTab, TabBlockReason } from "./tabs";`

    4. Criar `src/lib/store-onboarding/__tests__/tab-state.test.ts` (padrão vitest de `drift.test.ts`) nesta MESMA task (o `<verify>` abaixo referencia o arquivo): prioridade (fiscal pendente domina mesmo com `unlocked:true` e `hasLocalEdits` → `pending_generation` com reason `fiscal_pending`; `!unlocked` → `blocked`; `hasLocalEdits && !isPersisted` → `draft`; persisted+ready → `ready`; persisted → `saved`); fallback → `blocked`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/store-onboarding/__tests__/tab-state.test.ts — todos passando; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `computeTabState` com prioridade exata: fiscal pendente → `pending_generation` (mesmo com `unlocked:true` e `hasLocalEdits`); `!unlocked` → `blocked`; `hasLocalEdits && !isPersisted` → `draft`; persisted+ready → `ready`; persisted → `saved`
    - `ctx.readiness` é do tipo `StoreReadiness` de `@/lib/store-readiness` (import tipado, sem `any`)
    - Reexport de `OnboardingTab`/`TabBlockReason` presente
    - Testes de tab-state.ts verdes (≥ 5 cenários: prioridade fiscal→blocked→draft→ready→saved + fallback)
  </acceptance_criteria>
  <done>tab-state.ts com computeTabState puro, prioridade D7 e testes verdes</done>
</task>

<task type="auto">
  <name>Task 3: draft-store.ts + testes unitários de draft</name>
  <files>src/lib/store-onboarding/draft-store.ts, src/lib/store-onboarding/__tests__/draft-store.test.ts</files>
  <read_first>
    - src/hooks/use-input-preservation.ts (padrão storage com try/catch — 36 linhas)
    - src/lib/__tests__/drift.test.ts (padrão de teste puro vitest — linhas 1-61)
    - .planning/phases/36-onboarding-navegacao-por-abas/36-PATTERNS.md (seção draft-store.ts, linhas 104-137; e Test Pattern Notes, linhas 611-634)
  </read_first>
  <action>
    **draft-store.ts** (módulo puro de storage, NÃO é hook — funções de módulo com try/catch):

    1. `export const DRAFT_TTL_MS = 24 * 60 * 60 * 1000;`
    2. `export interface StoreDraft { userId: string; storeId: string | null; fields: Partial<FormData>; updatedAt: number; }` — `FormData` importado de `@/components/flow/use-store-form` (export type existente; se o export não for `FormData`, usar o nome real exportado e documentar). Comentário: `Partial<FormData>` por design (draft parcial antes do 1º save).
    3. `export function draftKey(userId, storeId)` → `vendeo:store_draft:${userId}:new` quando `storeId` null, senão `vendeo:store_draft:${userId}:${storeId}`.
    4. `export function saveDraft(draft)` → `JSON.stringify` em `localStorage` com `updatedAt = Date.now()`; try/catch com `console.warn` (padrão use-input-preservation).
    5. `export function restoreDraft(userId, storeId)` → lê a chave; se ausente → null; parse com try/catch (JSON inválido → null); se `Date.now() - updatedAt > DRAFT_TTL_MS` → `localStorage.removeItem` e retorna null (D5: expirado → ignorado e removido).
    6. `export function clearDraft(userId, storeId?)` → remove chave `:new` (storeId ausente) ou `:${storeId}`; try/catch.
    7. `export function clearAllDrafts()` → itera `localStorage` (Object.keys) e remove toda chave com prefixo `vendeo:store_draft:` — usado no logout (F36-DRAFT-04).
    8. Comentário no topo (F36-DRAFT-01): escolha por `localStorage` (não sessionStorage) — sobrevive a fechar aba e alternar app; TTL 24h evita draft velho ressuscitado.

    **Teste unitário** (1 arquivo, padrão vitest de `drift.test.ts`; os testes de tabs/tab-state foram criados nas Tasks 1-2 — aqui apenas o draft):

    - `__tests__/draft-store.test.ts`: mock de `localStorage` (vi.stubGlobal ou jsdom — verificar ambiente de teste do repo; usar `vi.stubGlobal("localStorage", mockStorage)` se necessário); chave `:new` vs `:${storeId}`; saveDraft grava updatedAt; restoreDraft expirado → null + removeItem (usar vi.useFakeTimers para TTL); escopo por usuário (chaves de usuários diferentes não colidem); clearDraft e clearAllDrafts.
  </action>
  <verify>
    <automated>npx vitest run src/lib/store-onboarding/__tests__/tabs.test.ts src/lib/store-onboarding/__tests__/tab-state.test.ts src/lib/store-onboarding/__tests__/draft-store.test.ts — todos passando; npx tsc --noEmit; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `draft-store.ts` exporta `DRAFT_TTL_MS` (= 86400000), `StoreDraft`, `draftKey`, `saveDraft`, `restoreDraft`, `clearDraft`, `clearAllDrafts`
    - `restoreDraft` com draft expirado remove a chave e retorna null (testado com fake timers)
    - `draftKey("u1", null)` === `"vendeo:store_draft:u1:new"`; `draftKey("u1", "s1")` === `"vendeo:store_draft:u1:s1"` (asserts exatos nos testes)
    - `clearAllDrafts` remove apenas chaves com prefixo `vendeo:store_draft:` (outras chaves intactas — teste)
    - Testes: tabs (≥5, Task 1) + tab-state (≥5, Task 2) + draft-store (≥6, esta task) — todos verdes
    - `npm run lint` sem novos erros
  </acceptance_criteria>
  <done>draft-store.ts + 3 arquivos de teste verdes — core library pronta para 36-03/36-04</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente → localStorage | Dados digitados pelo usuário no browser (não confiável como fonte de estado após parse) |
| draft-store → localStorage | Escrita/leitura de JSON serializado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-06 | Tampering | JSON do draft no localStorage | mitigate | `restoreDraft` faz parse com try/catch (JSON corrompido → null, não crash); dados tratados como `Partial<FormData>` e validados no consumo (36-03) |
| T-36-07 | Information Disclosure | Draft entre contas | mitigate | Chaves escopadas por `userId` (`draftKey`); `clearAllDrafts` no logout (36-03) — F36-DRAFT-01/04 |
| T-36-08 | Tampering | `computeTabState` reason/estado | accept | Funções puras sem side-effect; inputs são estado local do usuário — sem privilégio, sem valor sensível |
| T-36-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo (vitest já presente) — gate de legitimidade não se aplica |
</threat_model>

<verification>
- `npx vitest run src/lib/store-onboarding/__tests__/` — 3 arquivos verdes (≥16 cenários)
- `npx tsc --noEmit` e `npm run lint` limpos
- Nenhum import de server-only/React nos 3 módulos (funções puras — verificável por `grep -c "react\\|server-only"` == 0)
</verification>

<success_criteria>
- Máquina de abas (tabs.ts), estado por aba (tab-state.ts) e rascunho (draft-store.ts) implementados como funções puras testáveis
- Contratos exatos do `<interfaces>` exportados — consumidos sem reinterpretação por 36-03/36-04
- 3 arquivos de teste verdes; typecheck/lint limpos
</success_criteria>

<output>
Create `.planning/phases/36-onboarding-navegacao-por-abas/36-02-SUMMARY.md` when done
</output>

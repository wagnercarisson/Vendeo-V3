---
phase: 36-onboarding-navegacao-por-abas
plan: 03
type: execute
wave: 2
depends_on: ["36-01", "36-02"]
files_modified:
  - "src/components/flow/use-store-form.ts"
  - "src/hooks/use-onboarding-tabs.ts"
  - "src/hooks/__tests__/use-onboarding-tabs.test.ts"
  - "src/components/auth/logout-button.tsx"
autonomous: true
requirements:
  - F36-AUTOSAVE-01
  - F36-AUTOSAVE-02
  - F36-AUTOSAVE-03
  - F36-DRAFT-02
  - F36-DRAFT-03
  - F36-DRAFT-04
must_haves:
  truths:
    - "autoSave() persiste apenas campos válidos: PATCH com storeId, POST draft sem storeId+mínimo, sem POST sem mínimo"
    - "Trocar de aba roda autoSave() antes de navegar (aguardado); falha de POST impede avanço"
    - "Abandono mobile (pagehide/visibilitychange) grava o draft SÍNCRONO no localStorage e dispara PATCH best-effort"
    - "Rascunho restaurado dentro do TTL ao abrir /loja; banco prevalece em campos persistidos; chave :new limpa após 1º save; chaves limpas no logout"
    - "Drift intercepta qualquer saída de contexto (aba, navegação interna, back/forward, saída) com ordem/endpoints preservados"
  artifacts:
    - path: "src/components/flow/use-store-form.ts"
      provides: "autoSave(fields) + saveStatus em UseStoreFormReturn"
      contains: "autoSave"
    - path: "src/hooks/use-onboarding-tabs.ts"
      provides: "useOnboardingTabs — activeTab/setActiveTab/tabStates/saveStatus/handleInternalNavigation/handlePageHide/handleVisibilityChange (handlers públicos); onDriftNavigate/onDriftLeave recebidos via options (callbacks da 36-04)"
      contains: "useOnboardingTabs"
    - path: "src/components/auth/logout-button.tsx"
      provides: "limpeza de drafts no logout"
      contains: "clearAllDrafts"
  key_links:
    - from: "use-store-form.ts (autoSave)"
      to: "POST /api/store (modo draft, 36-01)"
      via: "fetch sem storeId com mínimo válido"
      pattern: "autoSave"
    - from: "use-onboarding-tabs.ts"
      to: "computeTabState (36-02)"
      via: "tabStates via computeTabState"
      pattern: "computeTabState"
    - from: "use-onboarding-tabs.ts"
      to: "draft-store.ts (36-02)"
      via: "saveDraft/restoreDraft/clearDraft"
      pattern: "saveDraft"
    - from: "use-onboarding-tabs.ts"
      to: "useDriftDetection (36-03 — consumido, NÃO modificado)"
      via: "estado/actions drift (driftStatus, driftCategory, realinhar/ignorar/dismissCriticalDrift)"
      pattern: "useDriftDetection"
---

<objective>
Camada de orquestração: `autoSave(fields)` em `use-store-form.ts`, o hook `useOnboardingTabs` (estado de abas + serialização de saves + eventos de saída + interceptação de drift), e a limpeza de rascunho no logout. `use-drift-detection.ts` é CONSUMIDO como está (fonte de detecção e ações de drift), NÃO modificado.

Purpose: D4/D5/D13 — auto-save confiável na troca de aba e navegação interna, abandono mobile protegido por escrita síncrona no localStorage (TTL 24h), e orquestração de saída que intercepta QUALQUER saída de contexto usando o hook de drift existente — sem alterar a bifurcação/endpoints atuais nem migrar lógica para dentro do hook.
Output: hook + autoSave + orquestração de saída consumindo drift + cleanup de logout. A integração visual na `StoreIdentityForm` é da 36-04 (que consome este hook).
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-onboarding-navegacao-por-abas/36-CONTEXT.md
@.planning/phases/36-onboarding-navegacao-por-abas/36-PATTERNS.md

<interfaces>
Contratos e referências (extraídos de 36-PATTERNS.md):

De use-store-form.ts atual (linhas 13-27 FormData; 31-49 UseStoreFormReturn; 119-161 hook; 191-298 save()):
```typescript
// save() routing atual (214-258):
// storeId → PATCH /api/store/${storeId}  (ou POST /api/store/update-cnpj se cnpj novo de 14 dígitos)
// sem storeId → POST /api/store (F36: cnpj agora OPCIONAL — vai ao branch draft da rota, 36-01)
```
Delta: `autoSave(fields: Partial<FormData>): Promise<{ ok: boolean }>` + `saveStatus: "idle" | "saving" | "saved" | "error"` no `UseStoreFormReturn`.

Contrato useOnboardingTabs (spec store-onboarding-autosave + store-onboarding-draft):
```typescript
// DIRECÃO DO CONTRATO: o hook RECEBE callbacks de drift (2º argumento/options) e
// RETORNA apenas handlers públicos. Nada de onNavigate/onLeave no retorno.
export function useOnboardingTabs(
  deps: {...},                                            // estado do formulário/store/readiness
  options?: {
    onDriftNavigate?: () => void;                         // callback de drift registrado pela 36-04
    onDriftLeave?: () => void;                            // callback de drift registrado pela 36-04
  },
): {
  activeTab: OnboardingTab;
  setActiveTab: (next: OnboardingTab) => Promise<void>;  // autoSave() ANTES de navegar (aguardado)
  tabStates: Record<OnboardingTab, { state: TabState; reason?: TabBlockReason }>;
  saveStatus: "idle" | "saving" | "saved" | "error";
  handleInternalNavigation: (e: MouseEvent) => void;
  handlePageHide: () => void;
  handleVisibilityChange: () => void;
  // flags de interceptação NÃO moram aqui: pendingNavUrl/driftSaveIntercept/driftNavIntercept
  // permanecem no StoreIdentityForm (orquestrador), como em store-identity-form.tsx:113-115
}
```
O hook CHAMA `options.onDriftNavigate()`/`options.onDriftLeave()` quando detecta saída de contexto com drift `new` nos campos do snapshot; a 36-04 (StoreIdentityForm) passa esses callbacks na montagem para abrir os modais. `setActiveTab`/`handleInternalNavigation`/`handlePageHide`/`handleVisibilityChange` são os handlers PÚBLICOS que o componente orquestrador liga ao DOM.
Sincronização de URL (F36-TABS-04/D6 — responsabilidade DESTE hook, NÃO da 36-04): o hook registra (on mount, cleanup no unmount) um listener `popstate` que lê `?tab=` de `location.search`, valida com `isOnboardingTab` e roteia o alvo pelo MESMO fluxo de saída do `setActiveTab` (ordem de drift D13: interceptação antes de save/navegação). Se o alvo estiver bloqueado (`computeTabUnlock`), o `activeTab` ainda sincroniza para que a UI renderize o painel de bloqueio + link "Voltar para X" (D6 — nunca tela em branco). A escrita no history (`pushState`/`replaceState` com `?tab=`) já acontece no `setActiveTab`; a 36-04 só resolve `initialTab` na montagem (sem parsing duplicado de URL).

D4 regras: PATCH falha → não bloqueia navegação; POST (criação) falha → permanece na aba dados, próxima aba `needs_store_created`; sem storeId + mínimo inválido → sem POST, draft fica no localStorage.

D13 ordem preservada: drift `critical`+`new` → DriftCriticalModal → `dismissCriticalDrift()` POST `/api/store/{storeId}/visual-signature/dismiss-critical-drift`; drift `sensitive` → DriftDecisionModal → `realinhar()` POST `/api/store/{storeId}/brand-profile/realign` | `ignorar()` PATCH `/api/store/{storeId}/brand-profile/metadata` `{ drift_dismissed_snapshot: currentSnapshot }`; só após decisão o PATCH dos campos do snapshot e a navegação. Campos fora do snapshot auto-save normalmente.

SNAPSHOT_FIELDS de `@/lib/snapshot.ts:15-18`: `segment, subsegment, tone_of_voice, name, positioning, short_description, slogan`.

Ref-guard/efeito de interceptação atual (store-identity-form.tsx:231-269): event capture `click` em document + `popstate` + `beforeunload`; flags `driftSaveIntercept`, `driftNavIntercept`, `pendingNavUrl` (linhas 113-115). Esses efeitos e flags PERMANECEM no componente orquestrador (StoreIdentityForm / useOnboardingTabs) — NÃO migram para `use-drift-detection.ts` (contrato sensível preservado, D13).

Padrão de teste de hook: `src/components/flow/__tests__/use-drift-detection.test.ts:1-55` (verifica export type, param count, formato do retorno — sem render React).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: autoSave(fields) + saveStatus em use-store-form.ts</name>
  <files>src/components/flow/use-store-form.ts</files>
  <read_first>
    - src/components/flow/use-store-form.ts (arquivo a modificar — ler por inteiro)
    - .planning/phases/36-onboarding-navegacao-por-abas/36-PATTERNS.md (seção use-store-form.ts, linhas 344-371)
    - src/app/api/store/route.ts (contrato do POST em dois modos — 36-01)
  </read_first>
  <action>
    Em `src/components/flow/use-store-form.ts`:

    1. Adicionar `saveStatus: "idle" | "saving" | "saved" | "error"` ao tipo de retorno `UseStoreFormReturn` (interface atual linhas 31-49) e ao estado do hook (setter correspondente).

    2. Implementar `autoSave(fields: Partial<FormData>): Promise<{ ok: boolean }>` — silencioso (sem toast/sucesso; feedback via saveStatus):
       - Campos inválidos são ignorados (só enviar campos válidos — validações do formulário já existentes no hook).
       - Se `storeId` existe → PATCH silencioso em `/api/store/${storeId}` (mesmo endpoint do `save()`); falha → `saveStatus: "error"`, retorna `{ ok: false }` — NÃO bloqueia navegação (D4).
       - Sem `storeId` + mínimo válido (name+segment+acceptedTerms) → POST `/api/store` SEM campo `cnpj` (modo draft da rota — 36-01); sucesso → `saveStatus: "saved"`, retorna `{ ok: true, storeId }`; falha → `saveStatus: "error"`, `{ ok: false }` — **este** caso bloqueia avanço (D4: permanece na aba dados, próxima aba `needs_store_created`).
       - Sem `storeId` + mínimo inválido → `{ ok: false }` SEM chamar POST (D4: "não se cria loja prematuramente"); o draft permanece no localStorage (gerido pelo hook da Task 2).
       - Atualizar o retorno: `Promise<{ ok: boolean; storeId?: string }>` para permitir ao hook capturar o novo storeId após o 1º save.

    3. Ajustar o `save()` existente (linhas 246-248): a exigência atual de CNPJ em create mode deve virar condicional — sem CNPJ no body, o POST atinge o branch draft da rota (não validar CNPJ localmente quando ausente). Não alterar o endpoint PATCH/update-cnpj.

    4. Resetar `saveStatus` para `"idle"` ao iniciar um novo save e `"saved"` ao concluir com sucesso (padrão usado pelo badge "Não salvo"/"Salva ✓" na 36-04).

    Comentário de código: referenciar D4/D15 — "auto-save cria loja draft sem CNPJ; loja draft não libera campanha/freemium".
  </action>
  <verify>
    <automated>npm run typecheck; npm run lint; npx vitest run src/components/flow/__tests__/store-identity-form.drift.test.ts 2>&1 | findstr "Test Files"</automated>
  </verify>
  <acceptance_criteria>
    - `autoSave` exportado no retorno do hook e tipado como `(fields: Partial<FormData>) => Promise<{ ok: boolean; storeId?: string }>`
    - `saveStatus` presente no tipo de retorno com os 4 valores
    - Com storeId → fetch PATCH `/api/store/${storeId}` (nunca POST); sem storeId+mínimo → POST `/api/store` sem cnpj; sem mínimo → nenhum fetch (assert de mock no teste)
    - POST falha → `{ ok: false }` e `saveStatus: "error"`; sucesso → `{ ok: true, storeId }`
    - `save()` em create mode não exige mais CNPJ localmente
  </acceptance_criteria>
  <done>autoSave + saveStatus implementados em use-store-form.ts conforme D4/D15</done>
</task>

<task type="auto">
  <name>Task 2: Hook use-onboarding-tabs.ts (abas + saves serializados + sync URL + eventos de saída) + testes</name>
  <files>src/hooks/use-onboarding-tabs.ts, src/hooks/__tests__/use-onboarding-tabs.test.ts</files>
  <read_first>
    - src/hooks/use-onboarding-tabs.ts (a ser criado)
    - src/components/flow/use-store-form.ts (estado/callbacks a compor — task 1)
    - src/lib/store-onboarding/tabs.ts e tab-state.ts (36-02 — computeTabUnlock/computeTabState)
    - src/lib/store-onboarding/draft-store.ts (36-02 — saveDraft/restoreDraft/clearDraft)
    - src/lib/drift.ts (getDriftPolicy/computeDriftStatus — a preservar)
    - .planning/phases/36-onboarding-navegacao-por-abas/36-PATTERNS.md (seção use-onboarding-tabs.ts, linhas 140-213)
  </read_first>
  <action>
    Criar `src/hooks/use-onboarding-tabs.ts` ("use client" quando necessário ao uso; hook de React):

    Estado/API (contrato exato do `<interfaces>`):
    - `activeTab: OnboardingTab` (inicial via prop `initialTab`, validado com `isOnboardingTab` — fallback `"dados"`)
    - `setActiveTab(next)` → `async`: 1) se `next !== activeTab`, rodar fluxo de saída (ver abaixo); 2) só navegar se o resultado permitir; 3) atualizar `activeTab` e `history` via `window.history.pushState`/`replaceState` com `?tab=${next}` (escrita da URL no history — D6).
    - **Sync back/forward (F36-TABS-04/D6 — DESTE hook):** registrar listener `popstate` no mount (cleanup no unmount) que lê `?tab=` de `location.search`, valida com `isOnboardingTab` (inválido → ignorar, manter aba atual) e roteia o alvo pelo MESMO fluxo de saída do `setActiveTab` — ordem de drift D13 preservada (interceptação antes de save/navegação). Se o alvo estiver bloqueado (`computeTabUnlock`), sincronizar `activeTab` mesmo assim para que a UI renderize o painel de bloqueio + link "Voltar para X" (D6 — nunca tela em branco). A 36-04 NÃO re-implementa parsing de URL (só resolve `initialTab` na montagem) — sem wiring duplicado.
    - `tabStates: Record<OnboardingTab, { state: TabState; reason?: TabBlockReason }>` — calculado com `computeTabState` para cada aba (ctx: `unlocked` via `computeTabUnlock`, `hasLocalEdits`/`isPersisted` do formulário, `readiness` da F34). Recalcular quando formData/readiness/storeId mudarem.
    - `saveStatus` (repassado do useStoreForm).
    - `handleInternalNavigation(e: MouseEvent)` — intercepta cliques em `<a>` internos (mesmo filtro de `store-identity-form.tsx:231-269`: ignorar `_blank`, `#`, `javascript:`): roda autoSave() antes de sair; se falhar → pergunta "sair mesmo assim (perde)" ou "voltar" (D13) — sinalizar via estado `pendingNavUrl` e callback `options.onDriftNavigate` (o modal é montado pela 36-04).
    - `handlePageHide()` / `handleVisibilityChange()` (quando `document.visibilityState === "hidden"`) — D4/D5: **escrita síncrona** `saveDraft({ userId, storeId, fields: formData válidos, updatedAt })` no localStorage; com `storeId` existente → PATCH fire-and-forget best-effort (fetch sem await, `keepalive: true` quando suportado).
    - `onDriftNavigate` / `onDriftLeave` — callbacks RECEBIDOS via `options` (registrados pela 36-04 na montagem, montam os modais); o hook chama `options.onDriftLeave()` quando detecta saída com drift pendente nos campos do snapshot. NÃO são retornados pelo hook.
    - Serialização de saves: fila simples (promise encadeada) + ref/seq guard — se uma resposta antiga chega depois de uma nova, ignorar (não sobrescrever estado com dados defasados).

    Fluxo de saída (usado por setActiveTab e handleInternalNavigation):
    1. Coletar campos editados locais; se tocam `SNAPSHOT_FIELDS` (de `@/lib/snapshot.ts`) e há drift novo (sensitive/critical via `computeDriftStatus`/`getDriftPolicy`) → NÃO persistir esses campos ainda; chamar `options.onDriftNavigate()`/`options.onDriftLeave()` para a 36-04 abrir o modal (decisão) — campos fora do snapshot (fiscal/billing/visual) podem autoSave normalmente (D13 auto-save seletivo).
    2. Sem drift → `await autoSave(fieldsVálidos)`.
    3. Se POST falhou (criação) → permanecer na aba atual (não navegar); se PATCH falhou → navegar mesmo assim (D4).
    4. Após 1º save bem-sucedido (novo storeId): limpar chave `:new` via `clearDraft(userId)` (D5 migração atômica).

    Restauração (`restoreDraft`) NÃO acontece neste hook (é responsabilidade da 36-04 no auto-load — F36-IDENTITY-UI-05); expor apenas os meios: `saveDraft`/`clearDraft` já vêm de draft-store.

    **Testes** — criar `src/hooks/__tests__/use-onboarding-tabs.test.ts` nesta MESMA task (o `<verify>` abaixo referencia o arquivo; padrão de teste de hook do repo — verificação de export/retorno sem render React, como `use-drift-detection.test.ts:1-55`): troca de aba com mínimo válido chama `autoSave` e atualiza `activeTab`; sem mínimo → `{ok:false}` e aba NÃO muda; PATCH falho → `saveStatus: "error"` mas aba muda (D4); POST falho → aba não muda; serialização ref/seq (promises resolvidas fora de ordem não sobrescrevem estado); `handlePageHide` grava draft síncrono (mock localStorage) + PATCH best-effort quando storeId existe; **back/forward popstate**: dispatch de evento `popstate` com `?tab=posicionamento` no history → `activeTab` sincroniza (passando pelo fluxo de saída); `?tab=` inválido → mantém aba atual. A 36-06 ESTENDE este arquivo (não recria) com cenários adicionais (drift intercept, aba bloqueada).
  </action>
  <verify>
    <automated>npm run typecheck; npm run lint; npx vitest run src/hooks/__tests__/use-onboarding-tabs.test.ts — todos passando</automated>
  </verify>
  <acceptance_criteria>
    - Hook exportado com TODAS as chaves do retorno: `activeTab`, `setActiveTab`, `tabStates`, `saveStatus`, `handleInternalNavigation`, `handlePageHide`, `handleVisibilityChange` — SEM `onNavigate`/`onLeave` no retorno
    - `onDriftNavigate`/`onDriftLeave` são callbacks RECEBIDOS via `options` (assinatura do `<interfaces>`), chamados pelo hook em saída de contexto com drift `new`; teste asserta que o callback de `options` foi invocado
    - `setActiveTab` é async e chama autoSave antes de atualizar a aba (assert de chamada em teste)
    - POST falha → `activeTab` NÃO muda; PATCH falha → muda
    - `handlePageHide` grava draft síncrono (mock de localStorage) e dispara PATCH best-effort quando storeId existe
    - Serialização: duas chamadas concorrentes a autoSave não reordenam estado (ref/seq guard — teste com resolved promises atrasadas)
    - `saveStatus` reage a saving/saved/error
    - Listener `popstate` registrado (mount) e removido (unmount): `?tab=` válido sincroniza `activeTab` pelo fluxo de saída (ordem de drift D13); inválido é ignorado; aba bloqueada ainda sincroniza (painel de bloqueio D6)
    - `use-onboarding-tabs.test.ts` (criado aqui, estendido na 36-06) verde — inclui cenário de back/forward popstate
  </acceptance_criteria>
  <done>use-onboarding-tabs.ts com auto-save na troca de aba, sync de URL (pushState + popstate), draft síncrono no abandono e serialização de saves</done>
</task>

<task type="auto">
  <name>Task 3: Orquestração de saída consumindo drift (sem migrar lógica) + limpeza de draft no logout</name>
  <files>src/components/auth/logout-button.tsx, src/hooks/use-onboarding-tabs.ts</files>
  <read_first>
    - src/components/flow/use-drift-detection.ts (ler por inteiro — CONSUMIDO como está, NÃO modificar)
    - src/lib/drift.ts (getDriftPolicy/computeDriftStatus/evaluateCriticalDrift/evaluateSensitiveDrift — NÃO alterar)
    - src/lib/store-onboarding/draft-store.ts (36-02 — restoreDraft/clearDraft/clearAllDrafts)
    - src/components/auth/logout-button.tsx (clearStorage atual — 44 linhas)
    - src/app/(app)/loja/page.tsx (onde a restauração é consumida via 36-04 — referência)
    - .planning/phases/36-onboarding-navegacao-por-abas/36-PATTERNS.md (seções use-drift-detection.ts linhas 375-385; drift integration 595-598; logout em logout-button.tsx)
  </read_first>
  <action>
    **`use-drift-detection.ts` — PRESERVADO (D13, contrato sensível):** NÃO mover/expandir interceptação para dentro do hook. Ele continua sendo a fonte de DETECÇÃO e AÇÕES de drift (`driftStatus`, `driftCategory`, `currentSnapshot`, `realinhar`, `ignorar`, `dismissCriticalDrift`) — export type/params/retorno intactos. A ORQUESTRAÇÃO de saída de contexto (troca de aba, link interno, `popstate`/back-forward, `beforeunload`/pagehide) fica no `useOnboardingTabs` (hook orquestrador, criado na Task 2) e/ou `StoreIdentityForm` (componente), que CONSUMEM o estado e as ações já expostos por `useDriftDetection`. NÃO expor `onNavigate`/`onLeave`/`pendingNavUrl`/`driftSaveIntercept`/`driftNavIntercept` no hook de drift — esses callbacks/flags são do orquestrador.

    1. A lógica existente (SNAPSHOT_FIELDS aplicáveis, `computeDriftStatus`, `evaluateCriticalDrift`/`evaluateSensitiveDrift`, endpoints `realinhar`/`ignorar`/`dismissCriticalDrift`) permanece INTACTA — não alterar as funções de drift nem os endpoints.

    2. No `useOnboardingTabs` (Task 2): o fluxo de saída já consulta o drift e aciona os callbacks de interceptação — verificar/estender o `handleInternalNavigation` e o fluxo de `setActiveTab` para chamar `options.onDriftNavigate()`/`options.onDriftLeave()` (callbacks RECEBIDOS via options, registrados pela 36-04 para montar os modais — NÃO retornados pelo hook) quando há drift `new` nos campos do snapshot, replicando o filtro de eventos de `store-identity-form.tsx:231-269` (ignorar `_blank`, `#`, `javascript:`). Campos fora do snapshot (fiscal/billing/visual) autoSave normalmente (D13 auto-save seletivo).

    3. As flags de interceptação (`driftSaveIntercept`, `driftNavIntercept`, `pendingNavUrl` — hoje em store-identity-form.tsx:113-115) PERMANECEM no componente orquestrador (StoreIdentityForm via 36-04) — NÃO movê-las para o hook de drift. A 36-04 monta os modais com esses mesmos estados.

    4. Não quebrar o contrato atual do hook: testes existentes (`use-drift-detection.test.ts`) devem continuar passando (export type/params/retorno — sem novas chaves).

    **logout-button.tsx** — limpeza de draft no logout (F36-DRAFT-04):

    5. Em `clearStorage()` (linhas 13-21), além das chaves sessionStorage atuais, chamar `clearAllDrafts()` de `@/lib/store-onboarding/draft-store` (remove todas as chaves `vendeo:store_draft:` — escopo de usuário garantido por prefixo). Manter try/catch best-effort.

    6. A RESTAURAÇÃO do rascunho (`restoreDraft`) no auto-load do `/loja` é da 36-04 (F36-IDENTITY-UI-05); aqui apenas garantir que `clearAllDrafts` esteja disponível e seja chamado no logout.
  </action>
  <verify>
    <automated>npm run typecheck; npm run lint; npx vitest run src/components/flow/__tests__/use-drift-detection.test.ts src/__tests__/auth/logout.test.tsx — verdes (sem regressão nos testes existentes de drift/logout)</automated>
  </verify>
  <acceptance_criteria>
    - `use-drift-detection.ts` NÃO modificado: export type/params/retorno intactos (nenhuma chave nova); testes existentes de drift passam sem mudança de expectativas
    - Orquestração de saída (troca de aba, link interno, back/forward, page exit) fica em `useOnboardingTabs`/`StoreIdentityForm`, consumindo estado/ações de `useDriftDetection`
    - Nenhuma alteração nas funções de detecção de `src/lib/drift.ts` nem nos endpoints de realinhar/ignorar/dismiss
    - Flags de interceptação (`pendingNavUrl` etc.) permanecem no componente orquestrador (36-04)
    - `logout-button.tsx` chama `clearAllDrafts` de draft-store dentro de `clearStorage()`
    - Testes existentes de drift e logout passam sem modificação de expectativas
  </acceptance_criteria>
  <done>Orquestração de saída em useOnboardingTabs consumindo drift (hook preservado) + drafts limpos no logout</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente → localStorage | Escrita síncrona de draft no abandono; leitura/restauração no auto-load |
| cliente → POST/PATCH /api/store | autoSave e PATCH best-effort partem daqui |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-09 | Information Disclosure | Draft localStorage entre contas | mitigate | Chaves `vendeo:store_draft:${userId}:...` (36-02) + `clearAllDrafts` no logout (logout-button) — F36-DRAFT-04 |
| T-36-10 | Tampering | JSON do draft restaurado | mitigate | `restoreDraft` com try/catch (36-02); na 36-04 o restore valida TTL e reconcilia com banco — banco prevalece em campos persistidos (F36-DRAFT-03) |
| T-36-11 | Repudiation | PATCH best-effort no unload | accept | Escrita síncrona do draft é a garantia (D4); PATCH fire-and-forget é best-effort documentado — não confiável por design |
| T-36-12 | Spoofing | autoSave POST sem auth | mitigate | O POST usa o mesmo fluxo autenticado (`requireUser` server-side, 36-01); hook apenas envia body válido — sem credenciais no cliente além do cookie de sessão |
| T-36-13 | Tampering | Drift endpoints | mitigate | Endpoints de drift preservados (não alterados); decisão (realinhar/ignorar/dismiss) só dispara após o usuário confirmar no modal (36-04) |
| T-36-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo — gate de legitimidade não se aplica |
</threat_model>

<verification>
- `npm run typecheck` e `npm run lint` limpos
- Testes existentes de drift e logout verdes (sem regressão)
- Testes do hook use-onboarding-tabs (criados nesta task 2, padrão do repo) verdes
- `grep -n "autoSave" src/components/flow/use-store-form.ts`, `grep -n "useOnboardingTabs" src/hooks/use-onboarding-tabs.ts` e `grep -n "popstate" src/hooks/use-onboarding-tabs.ts` presentes
</verification>

<success_criteria>
- autoSave silencioso com regras D4 (PATCH não bloqueia; POST falho bloqueia avanço; mínimo inválido sem POST)
- Draft síncrono em pagehide/visibilitychange + PATCH best-effort; limpeza `:new` após 1º save e limpeza total no logout
- Drift intercepta troca de aba, navegação interna, back/forward e saída — ordem e endpoints preservados (D13)
- Contrato do hook pronto para a 36-04 consumir sem reinterpretação
</success_criteria>

<output>
Create `.planning/phases/36-onboarding-navegacao-por-abas/36-03-SUMMARY.md` when done
</output>

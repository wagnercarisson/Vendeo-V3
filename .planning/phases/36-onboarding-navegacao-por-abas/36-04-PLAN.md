---
phase: 36-onboarding-navegacao-por-abas
plan: 04
type: execute
wave: 3
depends_on: ["36-02", "36-03"]
files_modified:
  - "src/components/flow/store-tabs.tsx"
  - "src/components/flow/legal-acceptance-panel.tsx"
  - "src/components/flow/store-identity-form.tsx"
  - "src/components/flow/store-page-client.tsx"
  - "src/app/(app)/loja/page.tsx"
autonomous: true
requirements:
  - F36-TABS-03
  - F36-TABS-04
  - F36-TABS-05
  - F36-LEGAL-01
  - F36-LEGAL-02
  - F36-IDENTITY-UI-01
  - F36-IDENTITY-UI-03
  - F36-IDENTITY-UI-04
  - F36-IDENTITY-UI-05
  - F36-IDENTITY-UI-06
  - F36-IDENTITY-UI-07
must_haves:
  truths:
    - "O usuário vê 3 abas ARIA em /loja (desktop: Dados/Posicionamento/Direção Visual; mobile: Dados/Perfil/Visual) com touch ≥ 44px"
    - "Deep-link em aba bloqueada mostra bloqueio + link 'Voltar para X' (nunca tela em branco); back/forward alternam a aba via ?tab="
    - "O aceite legal aparece como coluna lateral global (desktop sticky, mobile compacto) e bloqueia Posicionamento quando pendente/reaceite"
    - "CNPJ opcional na criação (vazio = loja draft + aviso de fiscal pendente); máscara e lookup assíncrono mantidos"
    - "O rascunho localStorage é restaurado no auto-load (TTL 24h) e reconciliado com o banco; store.id NUNCA vai para localStorage('store_id')"
  artifacts:
    - path: "src/components/flow/store-tabs.tsx"
      provides: "Container ARIA tabs (tablist/tab/tabpanel, roving tabindex) + variante mobile compacta"
      contains: "role=\"tablist\""
    - path: "src/components/flow/legal-acceptance-panel.tsx"
      provides: "LegalAcceptancePanel + LegalAcceptanceState"
      contains: "LegalAcceptanceState"
    - path: "src/components/flow/store-identity-form.tsx"
      provides: "Painel 3 abas (refatoração do wizard 2 steps)"
      contains: "useOnboardingTabs"
    - path: "src/components/flow/store-page-client.tsx"
      provides: "Parsing ?tab= + compat required= + message= + fiscal="
      contains: "searchParams.get(\"tab\")"
  key_links:
    - from: "store-identity-form.tsx"
      to: "use-onboarding-tabs (36-03)"
      via: "useOnboardingTabs() — activeTab/setActiveTab/tabStates"
      pattern: "useOnboardingTabs"
    - from: "legal-acceptance-panel.tsx"
      to: "F30 getAcceptanceStatus/legal-status"
      via: "current→accepted, outdated→needs_reacceptance, ausente→pending"
      pattern: "needs_reacceptance"
    - from: "store-page-client.tsx"
      to: "?tab="
      via: "useSearchParams → initialTab (deep-link) + compat required="
      pattern: "direcao-visual"
---

<objective>
UI do onboarding por abas: `StoreTabs` (container ARIA tabs + mobile compacto), `LegalAcceptancePanel` (coluna lateral global de aceite), refatoração de `store-identity-form.tsx` de wizard 2 steps para painel de 3 abas (consumindo `useOnboardingTabs` da 36-03), e parsing de `?tab=`/compat `required=` em `store-page-client.tsx` + composição em `src/app/(app)/loja/page.tsx`.

Purpose: D3/D6/D7/D9/D10/D11 — transformar o fluxo linear em navegação livre por abas com desbloqueio progressivo, aceite legal global e CNPJ opcional, sem perder nenhum feedback contextual.
Output: 2 componentes novos + 2 modificados + página /loja compondo o painel.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-onboarding-navegacao-por-abas/36-CONTEXT.md
@.planning/phases/36-onboarding-navegacao-por-abas/36-PATTERNS.md
@openspec/design-system/MASTER.md
@openspec/design-system/pages/store-identity.md

<interfaces>
Contratos e referências (extraídos de 36-PATTERNS.md):

StoreTabs props (spec store-onboarding-tabs):
```typescript
export function StoreTabs({ tabs, activeTab, states, onTabChange, variant }: {
  tabs: OnboardingTabDef[];
  activeTab: OnboardingTab;
  states: Record<OnboardingTab, { state: TabState; reason?: TabBlockReason }>;
  onTabChange: (tab: OnboardingTab) => void;
  variant: "desktop" | "mobile-compact";
})
```
D11: `role="tablist"`/`role="tab"`/`role="tabpanel"` + `aria-selected`/`aria-controls`; roving tabindex (só ativo tabulável; ArrowLeft/ArrowRight/Home/End movem foco); `aria-describedby` no tab bloqueado → motivo no painel; estado via `aria-label`; `aria-live` na região. D10: mobile labels Dados/Perfil/Visual (id permanece), badge pequeno (ponto/ícone), motivo APENAS no painel, botão "Continuar" fixo inferior, touch ≥ 44px (min-h-[44px]/min-w-[44px] — convenção F22 em store-identity-form.tsx:1806,1818,1921).

LegalAcceptancePanel (spec legal-acceptance-panel):
```typescript
export type LegalAcceptanceState = "pending" | "accepted" | "needs_reacceptance";
export function LegalAcceptancePanel({ acceptance, onOpenModal, variant }: {
  acceptance: LegalAcceptanceState;
  onOpenModal: () => void;
  variant: "desktop-sticky-column" | "mobile-compact";
})
```
D3: desktop `lg:sticky top-*` dentro do grid (`grid grid-cols-1 lg:grid-cols-5` — store-identity-form.tsx:1815); mobile bloco compacto no topo, sem sticky. Estados Pendente/Aceito/Reaceite necessário; CTA "Revisar e aceitar" abre ContractAcceptanceModal (F30); `aria-expanded` no trigger. Derivação: `getAcceptanceStatus` de `src/lib/legal/acceptance-service.ts:43-62` retorna `"current" | "outdated" | "never"` (types em `src/lib/legal/types.ts:11`) → current→accepted, outdated→needs_reacceptance, never→pending.

ContractAcceptanceModal (F30) — props: `{ open: boolean; onOpenChange: (open:boolean)=>void; onConfirm: ()=>Promise<boolean>; contractDocuments: ContractDocumentInfo[] }` (contract-acceptance-modal.tsx:13-18). Padrão de wiring em store-identity-form.tsx:2491-2514.

Drift modais (F34, preservados): `DriftDecisionModal` props `{ onRealinhar; onIgnorar; onCancel; isLoading; error; onContinueWithoutDismiss? }`; `DriftCriticalModal` props `{ open; onOpenChange; storeId; identityState; canGenerateNewSignature; onDismissAndSave; onRemoveVs; onOpenApproval; onCancel }`.

Current store-page-client.tsx (26 linhas): `const required = searchParams.get("required"); const message = searchParams.get("message"); const initialStep = required === "visual-direction" ? 2 : undefined;` — Delta D6/D12: `const tab = searchParams.get("tab")`; resolução: `?tab=` → `required=visual-direction`→`direcao-visual` / `required=cadastro-fiscal`→`dados` → default `dados`; manter `message=` e adicionar `fiscal` (`fiscal=pending` → banner na aba Dados). `initialTab` aplica-se APENAS na montagem — o sync de back/forward (`?tab=` → aba) é do hook `useOnboardingTabs` (36-03, popstate); NÃO re-implementar parsing de URL aqui. O teste dedicado de parsing (`store-page-client.test.tsx`) é criado na 36-06.

D12 tabela de redirects (targets que esta página deve RENDERIZAR como banners — a produção dos links é da 36-05): `?tab=dados&fiscal=pending`, `?tab=direcao-visual&message=needs-visual-direction`, `?tab=direcao-visual&message=cnpj-updated`.

Restauração de rascunho (F36-IDENTITY-UI-05): `restoreDraft(userId, storeId)` de `@/lib/store-onboarding/draft-store` (36-02) — restaurar `:new` (sem loja) ou `:${storeId}` reconciliado com banco (banco prevalece em campos persistidos). NUNCA ler `localStorage("store_id")`.

Badge "Necessário" (F36-IDENTITY-UI-06): atual store-identity-form.tsx:1155-1157 `{!inferredProfile && (<span ...>Necessário</span>)}` — permanece na aba Direção Visual (D7).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: StoreTabs (ARIA tabs) + LegalAcceptancePanel</name>
  <files>src/components/flow/store-tabs.tsx, src/components/flow/legal-acceptance-panel.tsx</files>
  <read_first>
    - src/components/flow/store-tabs.tsx e legal-acceptance-panel.tsx (a serem criados)
    - src/components/flow/store-preview.tsx (padrão de componente presentational client com props tipadas — linhas 1-38)
    - src/components/legal/legal-status-section.tsx (padrão F30 de status legal + wiring do modal)
    - src/components/flow/store-identity-form.tsx linhas 1800-1930 (convenções de classe, min-h-[44px], grid grid-cols-1 lg:grid-cols-5)
    - .planning/phases/36-onboarding-navegacao-por-abas/36-PATTERNS.md (seções store-tabs.tsx linhas 216-243 e legal-acceptance-panel.tsx linhas 246-290)
  </read_first>
  <action>
    **Criar `src/components/flow/store-tabs.tsx`** ("use client"):

    1. `StoreTabsProps` com `tabs: OnboardingTabDef[]`, `activeTab: OnboardingTab`, `states: Record<OnboardingTab, { state: TabState; reason?: TabBlockReason }>`, `onTabChange: (tab: OnboardingTab) => void`, `variant: "desktop" | "mobile-compact"` (tipos de `@/lib/store-onboarding/tabs` e `tab-state`).

    2. Estrutura ARIA (D11): `<div role="tablist" aria-label="Abas da loja">` com botões `role="tab"`, `aria-selected={activeTab === tab.id}`, `aria-controls={`panel-${tab.id}`}`, `id={`tab-${tab.id}`}`; painéis `role="tabpanel"` com `id="panel-{tab.id}"` e `aria-labelledby="tab-{tab.id}"` (painéis mantêm-se montados ou com estado preservado — decisão de implementação; o motivo do bloqueio aparece no painel ativo, nunca no botão da aba — D10).

    3. Roving tabindex: tab ativo `tabIndex={0}`, demais `-1`; keydown no tablist: ArrowRight/ArrowLeft (ordem circular de `TAB_ORDER`), Home→primeiro, End→último, movendo foco (não necessariamente selecionando — foco roving; `onTabChange` dispara no clique e no Enter/Space).

    4. Estados: `aria-label` em cada tab com o estado (ex.: `Direção Visual — Pendente para gerar`) — nunca cor sozinha; tab bloqueado recebe `aria-describedby` apontando para o elemento do motivo no painel ativo; região `aria-live="polite"` anunciando mudança de aba/estado.

    5. Badge por estado: mobile → ponto/ícone discreto (canto), desktop → badge compacto (ex.: "Salva ✓"); NUNCA texto completo do motivo no botão.

    6. Variante mobile-compact: labels de `OnboardingTabDef.labelMobile` (Dados/Perfil/Visual); botão "Continuar" fixo no rodapé do container (avança/retrocede — `onTabChange(TAB_ORDER[i+1])`/`[i-1]`); todos os botões `min-h-[44px] min-w-[44px]` (F22).

    7. Variante desktop: labels completos de `OnboardingTabDef.label`.

    **Criar `src/components/flow/legal-acceptance-panel.tsx`** ("use client"):

    8. `export type LegalAcceptanceState = "pending" | "accepted" | "needs_reacceptance";` e componente `LegalAcceptancePanel({ acceptance, onOpenModal, variant })` conforme contrato.

    9. Render: desktop → coluna `lg:sticky top-6` dentro do grid (participa do layout); mobile → bloco compacto (topo do painel ou antes do CTA, sem sticky).

    10. Estados exibidos: `pending` → "Pendente" (cor de atenção), `accepted` → "Aceito" (verde/check), `needs_reacceptance` → "Reaceite necessário" (cor de alerta). CTA "Revisar e aceitar" (e "Revisar aceite" no reaceite) com `aria-expanded`/`aria-pressed` — chama `onOpenModal()`.

    11. `aria-label` no card refletindo o estado; badge acessível. Este componente NÃO deriva o estado internamente — recebe `acceptance` via prop (a derivação via `getAcceptanceStatus` acontece no form/página, task 3).
  </action>
  <verify>
    <automated>npm run typecheck; npm run lint — os testes de componente (store-tabs.test.tsx / legal-acceptance-panel.test.tsx) são criados e executados na 36-06 (Task 2); aqui apenas typecheck/lint</automated>
  </verify>
  <acceptance_criteria>
    - `store-tabs.tsx` exporta `StoreTabs` com as 6 props tipadas; contém `role="tablist"`, `role="tab"`, `role="tabpanel"`, `aria-selected`, `aria-controls`
    - Roving tabindex: ativo `tabIndex={0}`, demais `-1`; handlers ArrowLeft/ArrowRight/Home/End presentes
    - Tab bloqueado tem `aria-describedby` → id do motivo no painel; `aria-live` presente
    - Mobile: labels Dados/Perfil/Visual (via `labelMobile`), badge discreto, botão "Continuar" fixo; botões com `min-h-[44px]`/`min-w-[44px]`
    - `legal-acceptance-panel.tsx` exporta `LegalAcceptanceState` e `LegalAcceptancePanel` com props `acceptance`/`onOpenModal`/`variant`
    - Os 3 estados renderizam labels distintos (Pendente/Aceito/Reaceite necessário); CTA com `aria-expanded`
  </acceptance_criteria>
  <done>StoreTabs (ARIA completo + mobile compacto) e LegalAcceptancePanel criados conforme D3/D10/D11</done>
</task>

<task type="auto">
  <name>Task 2: store-page-client.tsx (?tab= parsing) + composição em loja/page.tsx</name>
  <files>src/components/flow/store-page-client.tsx, src/app/(app)/loja/page.tsx</files>
  <read_first>
    - src/components/flow/store-page-client.tsx (26 linhas — ler inteiro)
    - src/app/(app)/loja/page.tsx (16 linhas — ler inteiro)
    - src/lib/store-onboarding/tabs.ts (36-02 — isOnboardingTab/OnboardingTab)
    - src/lib/legal/acceptance-service.ts (getAcceptanceStatus — F30)
    - .planning/phases/36-onboarding-navegacao-por-abas/36-PATTERNS.md (seção store-page-client.tsx linhas 328-341)
  </read_first>
  <action>
    **store-page-client.tsx** — parsing de `?tab=` (D6/D12):

    1. Ler `tab`, `required`, `message`, `fiscal` de `useSearchParams()`.

    2. Resolver `initialTab` nesta ordem: `?tab=` válido (`isOnboardingTab`) → usar; senão `required=visual-direction` → `"direcao-visual"`; `required=cadastro-fiscal` → `"dados"`; senão `"dados"`. (Compat `required=` — nota no código: mantido apenas na F36 como transição.) Este `initialTab` aplica-se APENAS na montagem — a sincronização de back/forward (`?tab=` → aba) é do hook `useOnboardingTabs` (36-03, via popstate), NÃO re-implementar parsing aqui (sem wiring duplicado).

    3. Repassar ao `StoreIdentityForm`: `initialTab`, `redirectMessage` (de `message=`), `fiscalPending` (boolean de `fiscal === "pending"`). Manter leitura de `message=` (needs-visual-direction, cnpj-updated).

    4. Não ler `localStorage("store_id")` em hipótese alguma (F36-IDENTITY-UI-05) — o storeId vem de `initialStore`/estado local.

    **loja/page.tsx** (server component) — compor o painel (F36-IDENTITY-UI-01):

    5. Manter `requirePageUser()` + `getCurrentStore(user.userId)` (já presentes).

    6. Passar `userId={user.userId}` para `StorePageClient` (necessário para `draftKey`/restauração) além de `initialStore`.

    7. Layout do painel: `StorePageClient` (client) compõe `StoreTabs` + painel ativo + `LegalAcceptancePanel` no grid `grid grid-cols-1 lg:grid-cols-5` (convenção do form atual: 4 colunas de conteúdo + 1 coluna lateral). A composição detalhada vive no `StoreIdentityForm` (task 3); a página apenas renderiza `StorePageClient`.

    8. Seguir `openspec/design-system/MASTER.md` + `pages/store-identity.md` para tokens/classes.
  </action>
  <verify>
    <automated>npm run typecheck; npm run lint; npx vitest run src/components/flow/__tests__/store-identity-form.redirect-messages.test.ts 2>&1 | findstr "Test Files" — o teste dedicado de parsing de store-page-client é criado na 36-06 (aqui apenas o parsing em si)</automated>
  </verify>
  <acceptance_criteria>
    - `store-page-client.tsx` lê `tab` via useSearchParams e valida com `isOnboardingTab`; ordem de resolução `?tab=` → `required=` → `"dados"`
    - Props repassadas ao form: `initialTab`, `redirectMessage`, `fiscalPending` (e `userId`)
    - `loja/page.tsx` passa `userId` ao client; nenhum `localStorage.setItem("store_id")` em nenhum dos dois arquivos (grep == 0)
    - `message=` continua lido; `fiscal=pending` vira `fiscalPending` (banner na aba Dados)
    - Back/forward pós-mount NÃO é responsabilidade deste componente (o hook useOnboardingTabs da 36-03 sincroniza via popstate — D6/F36-TABS-04); teste dedicado de parsing em 36-06
  </acceptance_criteria>
  <done>Parsing ?tab= + compat required= em store-page-client; página /loja compõe painel com userId</done>
</task>

<task type="auto">
  <name>Task 3: Refatoração store-identity-form.tsx — wizard 2 steps → painel 3 abas</name>
  <files>src/components/flow/store-identity-form.tsx</files>
  <read_first>
    - src/components/flow/store-identity-form.tsx (arquivo a modificar — ler por inteiro; ~2500 linhas)
    - src/hooks/use-onboarding-tabs.ts (36-03 — hook consumido)
    - src/lib/store-onboarding/draft-store.ts (36-02 — restoreDraft/clearDraft)
    - src/lib/legal/acceptance-service.ts (F30 — getAcceptanceStatus/legalClearance)
    - src/components/legal/contract-acceptance-modal.tsx (F30 — props do modal)
    - .planning/phases/36-onboarding-navegacao-por-abas/36-PATTERNS.md (seção store-identity-form.tsx linhas 293-326; drift wiring 595-598)
  </read_first>
  <action>
    Refatorar `src/components/flow/store-identity-form.tsx` de wizard 2 steps para painel de 3 abas (D1/D3/D4/D9/D13), PRESERVANDO os blocos críticos:

    1. **Estado de passo → abas:** substituir `const [step, setStep] = useState<1 | 2>(initialStep === 2 ? 2 : 1)` (linha 80) pelo `useOnboardingTabs({ initialTab, ... })` da 36-03. `initialStep` prop é removido — `initialTab`/`redirectMessage`/`fiscalPending` vêm de `store-page-client.tsx`.

    2. **Aceite legal fora do formulário (D3):** remover o checkbox de aceite do Step 1; renderizar `LegalAcceptancePanel` na coluna lateral (desktop `lg:grid-cols-5`) e no bloco mobile (topo do painel ativo). Derivar `LegalAcceptanceState` de `getAcceptanceStatus(storeId)`/legal-status da F30: `current`→`accepted`, `outdated`→`needs_reacceptance`, `never`/ausente→`pending`. CTA abre `ContractAcceptanceModal` (wiring padrão F30 — replicar `store-identity-form.tsx:2491-2514`; `onConfirm` POST `/api/legal/accept` com `{ storeId, source: "onboarding" }`).

    3. **Bloqueio da aba Posicionamento (F36-LEGAL-02):** quando `legalAccepted === false`, `computeTabUnlock` já retorna `needs_legal_acceptance` (36-02); o painel ativo da aba Posicionamento mostra "falta aceite legal" + link que abre o card/CTA do LegalAcceptancePanel. Aceite NÃO é pré-requisito direto da Direção Visual.

    4. **Fluxo de criação (F36-IDENTITY-UI-03/04):** `handleStep1Submit` (linhas 971-991) → vira auto-save na troca de aba (via hook); manter: sem `initialStore` → POST `/api/store` (modo draft sem CNPJ → `onboardingGranted: false`, limpar chave `:new` via `clearDraft(userId)`); `store.id` apenas em estado local (NUNCA `localStorage.setItem("store_id", ...)`); com CNPJ → fluxo verified. Saves seguintes → PATCH `/api/store/${storeId}` (modo por `storeId`); feedback via `saveStatus` (badge "Não salvo"/"Salva ✓" + toast — padrão linhas 1171-1190).

    5. **Restauração de rascunho (F36-IDENTITY-UI-05):** no auto-load (client effect), `restoreDraft(userId, storeId)` — restaurar `:new` (sem loja) ou `:${storeId}` reconciliado com o banco (banco prevalece em campos persistidos — merge com `initialStore`); expirado (restoreDraft já retorna null) → ignorar. NÃO ler `localStorage("store_id")`.

    6. **Direção Visual (F36-IDENTITY-UI-06):** manter o bloco de Direção Visual (ex-Step 2) com badge "Necessário" (linhas 1155-1157) na aba `direcao-visual`; botão Salvar explícito mantido (D4: aba que consome créditos/upload).

    7. **CNPJ opcional (F36-IDENTITY-UI-07):** campo CNPJ deixa de ser obrigatório na criação (vazio = loja draft + aviso "Fiscal pendente" no painel Dados); manter máscara `XX.XXX.XXX/YYYY-ZZ` e lookup assíncrono `GET /api/cnpj/lookup?cnpj=` no blur (loading "Consultando dados cadastrais..."), razão social/nome fantasia bloqueados e pré-preenchidos após lookup (F33 inalterado).

    8. **Drift (D13):** os modais `DriftDecisionModal`/`DriftCriticalModal` e o wiring (linhas 2312-2457) permanecem INTACTOS no componente orquestrador — apenas o momento de interceptação passa a ser o da orquestração do hook `useOnboardingTabs` (`onNavigate`/`onLeave`/`setActiveTab` da 36-03) e não mais `step === 2`. Flags `driftSaveIntercept`/`driftNavIntercept`/`pendingNavUrl` permanecem no componente (vêm do orquestrador, NÃO do hook de drift — `use-drift-detection.ts` é apenas consumido, nunca modificado). `executeStep2Save`'s pós-decisão (realinhar/ignorar/dismiss → PATCH snapshot + navegação) preservado.

    9. **Deep-link em aba bloqueada (F36-TABS-04):** se `initialTab` (deep-link) está bloqueado, renderizar a aba com o painel de bloqueio + link "Voltar para {aba anterior}" (nunca tela em branco). O MESMO painel de bloqueio é renderizado quando o back/forward retorna a uma aba bloqueada (o hook da 36-03 sincroniza `activeTab` via popstate mesmo para abas bloqueadas — D6); nenhuma lógica de URL extra aqui.

    10. **Microcopy da aba ② (D9):** card informativo "Essas informações ajudam o Vendeo a criar artes com linguagem, estilo e argumentos mais próximos da sua loja." no painel Posicionamento.
  </action>
  <verify>
    <automated>npm run typecheck; npm run lint; npx vitest run src/components/flow/__tests__/store-identity-form.drift.test.ts src/components/flow/__tests__/store-identity-form.redirect-messages.test.ts 2>&1 | findstr "Test Files"; grep -c "useOnboardingTabs" src/components/flow/store-identity-form.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `store-identity-form.tsx` usa `useOnboardingTabs` (grep ≥ 1) e renderiza `StoreTabs` + `LegalAcceptancePanel` no grid `lg:grid-cols-5`
    - `localStorage.setItem("store_id"` não aparece (grep == 0); storeId vem de initialStore/estado local
    - `LegalAcceptanceState` derivado de `getAcceptanceStatus` (mapeamento current/outdated/never) — grep do mapeamento presente
    - Posicionamento bloqueado com motivo "falta aceite legal" quando pendente/reaceite (reason `needs_legal_acceptance` renderizado no painel)
    - CNPJ opcional: máscara + lookup mantidos; aviso "Fiscal pendente" no painel Dados quando sem CNPJ
    - Blocos de drift (modais 2312-2457) e `executeStep2Save` pós-decisão preservados (sem remoção)
    - Deep-link bloqueado renderiza bloqueio + link "Voltar para X"
    - Testes existentes migrados/verdes (redirect-messages e drift); typecheck/lint limpos
  </acceptance_criteria>
  <done>store-identity-form refatorado para painel 3 abas com aceite global, CNPJ opcional, restauração de draft e drift preservado</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL (query params) → componente | `?tab=`, `required=`, `message=`, `fiscal=` vindos da URL (input não confiável) |
| localStorage → componente | Draft restaurado (JSON não confiável) |
| cliente → /api/legal/accept | Aceite legal via ContractAcceptanceModal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-14 | Tampering | Parsing de `?tab=`/`required=` | mitigate | `isOnboardingTab` valida o valor antes de usar; valores inválidos caem em `"dados"` — F36-TABS-05 |
| T-36-15 | Information Disclosure | `store_id` em localStorage | mitigate | Proibido `localStorage.setItem("store_id")` (F36-IDENTITY-UI-03/05); storeId só em estado React/`initialStore` |
| T-36-16 | Tampering | Draft restaurado no form | mitigate | `restoreDraft` (36-02) valida TTL/JSON; merge com banco onde banco prevalece em campos persistidos (F36-DRAFT-03); campos usados são tipados `Partial<FormData>` |
| T-36-17 | Spoofing | Aceite legal via modal | mitigate | `onConfirm` usa o endpoint `/api/legal/accept` existente da F30 (auth server-side); nada novo no cliente |
| T-36-18 | Information Disclosure | `cnpj_root_hash` no client | mitigate | Nenhum campo hash fiscal é renderizado; response da rota (36-01) nunca o inclui — F36-OWNERSHIP-01 |
| T-36-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo — gate de legitimidade não se aplica |
</threat_model>

<verification>
- `npm run typecheck` e `npm run lint` limpos
- Testes de componente/form/drift do fluxo `store-identity-form` verdes
- `grep -c "localStorage.setItem(\"store_id\"" src/components/flow/store-identity-form.tsx src/components/flow/store-page-client.tsx` == 0
- `grep -c "useOnboardingTabs" src/components/flow/store-identity-form.tsx` ≥ 1
- Deep-link manual: `/loja?tab=direcao-visual` em loja sem direção visual mostra bloqueio + "Voltar para Dados"
</verification>

<success_criteria>
- Painel 3 abas funcional em /loja com ARIA tabs, mobile compacto e touch ≥ 44px
- Aceite legal como coluna lateral global bloqueia Posicionamento quando pendente; reutiliza ContractAcceptanceModal F30
- CNPJ opcional (draft) com máscara/lookup; aviso fiscal pendente; store.id nunca em localStorage
- Rascunho restaurado (TTL 24h) com reconciliação banco-primeiro; drift preservado com interceptação por abas
</success_criteria>

<output>
Create `.planning/phases/36-onboarding-navegacao-por-abas/36-04-SUMMARY.md` when done
</output>

---
phase: 36-onboarding-navegacao-por-abas
plan: 05
type: execute
wave: 4
depends_on: ["36-04"]
files_modified:
  - "src/app/(app)/campanhas/nova/page.tsx"
  - "src/app/(app)/cadastro/cnpj/page.tsx"
  - "src/components/flow/cnpj-update-form.tsx"
  - "src/components/readiness/readiness-banner.tsx"
  - "src/components/legacy/cnpj-update-banner.tsx"
  - "src/components/flow/__tests__/store-identity-form.redirect-messages.test.ts"
  - "src/components/readiness/__tests__/readiness-banner.test.tsx"
autonomous: true
requirements:
  - F36-IDENTITY-UI-02
  - F36-READINESS-03
must_haves:
  truths:
    - "O guard de /campanhas/nova redireciona para ?tab=dados&fiscal=pending / ?tab=direcao-visual&message=needs-visual-direction (com returnTo)"
    - "Pós-cadastro de CNPJ redireciona para ?tab=dados&fiscal=pending (sem CNPJ pendente) ou ?tab=direcao-visual&message=cnpj-updated (sem brand profile)"
    - "Os banners (ReadinessBanner, cnpj-update-banner) apontam para os mesmos targets ?tab= preservando mensagem contextual"
    - "Compat `required=` legado continua funcionando (transição F36)"
  artifacts:
    - path: "src/app/(app)/campanhas/nova/page.tsx"
      provides: "guard readiness com redirects ?tab="
      contains: "tab=dados&fiscal=pending"
    - path: "src/app/(app)/cadastro/cnpj/page.tsx"
      provides: "redirect pós-cadastro CNPJ para ?tab="
      contains: "tab=dados&fiscal=pending"
    - path: "src/components/readiness/readiness-banner.tsx"
      provides: "links de pendência ?tab= no dashboard"
      contains: "tab=direcao-visual&message=needs-visual-direction"
  key_links:
    - from: "campanhas/nova/page.tsx"
      to: "/loja?tab="
      via: "redirect() do server component"
      pattern: "redirect(`/loja?tab="
    - from: "readiness-banner.tsx"
      to: "/loja?tab="
      via: "missingToDisplay(item)"
      pattern: "?tab=dados&fiscal=pending"
---

<objective>
Migrar todos os redirects e banners existentes do formato `?required=` para `?tab=` (D12/F36-READINESS-03), preservando `returnTo` e as mensagens contextuais, e atualizar os testes existentes que assertam as URLs antigas. O compat `required=` no `/loja` (mantido na 36-04) garante que links antigos não quebrem.

Purpose: nenhum fluxo de entrada (guard de geração, pós-cadastro CNPJ, banner do dashboard) aponta mais para o hack `required=` — o onboarding por abas é o destino canônico.
Output: 5 arquivos de origem + 2 arquivos de teste atualizados.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-onboarding-navegacao-por-abas/36-CONTEXT.md
@.planning/phases/36-onboarding-navegacao-por-abas/36-PATTERNS.md

<interfaces>
Tabela D12 (fonte: 36-CONTEXT.md — targets canônicos que estes arquivos devem EMITIR):

| Ponto | Arquivo | Mudança exata |
|-------|---------|---------------|
| Guard /campanhas/nova | src/app/(app)/campanhas/nova/page.tsx | `required=cadastro-fiscal` → `?tab=dados&fiscal=pending&returnTo=/campanhas/nova`; `required=visual-direction` → `?tab=direcao-visual&message=needs-visual-direction&returnTo=/campanhas/nova` |
| Redirect /cadastro/cnpj | src/app/(app)/cadastro/cnpj/page.tsx | → `/loja?tab=dados&fiscal=pending` (mantendo `returnTo`); `cnpj-update-form.tsx`: pós-atualização → `/loja?tab=dados&fiscal=pending` (sem CNPJ pendente) ou `/loja?tab=direcao-visual&message=cnpj-updated` (sem brand profile) |
| ReadinessBanner | src/components/readiness/readiness-banner.tsx | `cadastro_fiscal` → `?tab=dados&fiscal=pending&returnTo=/dashboard`; `brand_profile` → `?tab=direcao-visual&message=needs-visual-direction` |
| cnpj-update-banner | src/components/legacy/cnpj-update-banner.tsx | href `?required=cadastro-fiscal&returnTo=/dashboard` → `?tab=dados&fiscal=pending&returnTo=/dashboard` |

Referências do código atual:
- `campanhas/nova/page.tsx:20-28`: bloco de redirect com `getStoreReadiness(store.id)`, `firstMissing === "cadastro_fiscal"` → `/loja?required=cadastro-fiscal&returnTo=...`, senão → `/loja?required=visual-direction&message=needs-visual-direction`. Import `getStoreReadiness` de `@/lib/store-readiness` (linha 10) NÃO muda.
- `readiness-banner.tsx:4-9`: `missingToDisplay(item: MissingItem)` retorna `{ label, href }` — single point of change.
- `cadastro/cnpj/page.tsx` (11 linhas): linha 10 `redirect(\`/loja?required=cadastro-fiscal&returnTo=${encodeURIComponent(returnTo)}\`)`.
- Testes a migrar: `store-identity-form.redirect-messages.test.ts` (URLSearchParams extraction — linhas 16-35) e `readiness-banner.test.tsx` (assert `'/loja?required=cadastro-fiscal'` — linhas 17-24).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guard /campanhas/nova + redirect /cadastro/cnpj para ?tab=</name>
  <files>src/app/(app)/campanhas/nova/page.tsx, src/app/(app)/cadastro/cnpj/page.tsx, src/components/flow/cnpj-update-form.tsx</files>
  <read_first>
    - src/app/(app)/campanhas/nova/page.tsx (bloco de redirect — linhas 12-28)
    - src/app/(app)/cadastro/cnpj/page.tsx (11 linhas)
    - src/components/flow/cnpj-update-form.tsx (redirect pós-atualização)
    - .planning/phases/36-onboarding-navegacao-por-abas/36-PATTERNS.md (seções campanhas/nova linhas 403-417 e cadastro/cnpj linhas 421-427)
  </read_first>
  <action>
    **campanhas/nova/page.tsx** (F36-IDENTITY-UI-02):
    - No bloco de redirect (linhas 20-28), trocar os targets exatos:
      - `firstMissing === "cadastro_fiscal"` → `redirect(\`/loja?tab=dados&fiscal=pending&returnTo=${encodeURIComponent("/campanhas/nova")}\`)`
      - senão (brand_profile) → `redirect(\`/loja?tab=direcao-visual&message=needs-visual-direction&returnTo=${encodeURIComponent("/campanhas/nova")}\`)`
    - Manter a lógica de readiness (`getStoreReadiness`) e o import de `@/lib/store-readiness` inalterados. NÃO adicionar localStorage nem loading state (F36-IDENTITY-UI-02: "sem localStorage; sem loading state").

    **cadastro/cnpj/page.tsx** (F36-READINESS-03):
    - Trocar a linha 10: `redirect(\`/loja?tab=dados&fiscal=pending&returnTo=${encodeURIComponent(returnTo)}\`)`.

    **cnpj-update-form.tsx**:
    - Redirecionamento pós-atualização: sem CNPJ pendente → `/loja?tab=dados&fiscal=pending`; sem brand profile → `/loja?tab=direcao-visual&message=cnpj-updated` (a decisão usa o mesmo estado de readiness/missing que o fluxo atual — se o form não tiver acesso a missing, usar a regra simples: redirect para `?tab=dados&fiscal=pending` quando houver pendência fiscal, senão `?tab=direcao-visual&message=cnpj-updated`).
  </action>
  <verify>
    <automated>npm run typecheck; npm run lint; grep -n "tab=dados&fiscal=pending" "src/app/(app)/campanhas/nova/page.tsx" "src/app/(app)/cadastro/cnpj/page.tsx"; grep -c "required=" "src/app/(app)/campanhas/nova/page.tsx" "src/app/(app)/cadastro/cnpj/page.tsx" — deve ser 0 (fora de comentário)</automated>
  </verify>
  <acceptance_criteria>
    - `campanhas/nova/page.tsx` emite exatamente `?tab=dados&fiscal=pending&returnTo=/campanhas/nova` e `?tab=direcao-visual&message=needs-visual-direction&returnTo=/campanhas/nova`
    - `cadastro/cnpj/page.tsx` emite `?tab=dados&fiscal=pending&returnTo=${returnTo}`
    - `cnpj-update-form.tsx` pós-atualização: `/loja?tab=dados&fiscal=pending` ou `/loja?tab=direcao-visual&message=cnpj-updated`
    - Nenhum `?required=` restante nesses 3 arquivos
    - Import/lógica de `getStoreReadiness` preservados; sem localStorage/loading state novos
  </acceptance_criteria>
  <done>Guard e redirects de entrada migrados para ?tab= com returnTo preservado</done>
</task>

<task type="auto">
  <name>Task 2: ReadinessBanner + cnpj-update-banner para ?tab=</name>
  <files>src/components/readiness/readiness-banner.tsx, src/components/legacy/cnpj-update-banner.tsx</files>
  <read_first>
    - src/components/readiness/readiness-banner.tsx (função missingToDisplay — linhas 4-9)
    - src/components/legacy/cnpj-update-banner.tsx (href — linha ~27)
    - .planning/phases/36-onboarding-navegacao-por-abas/36-PATTERNS.md (seção readiness-banner linhas 388-399)
  </read_first>
  <action>
    **readiness-banner.tsx** — atualizar `missingToDisplay(item: MissingItem)` (linhas 4-9):
    - `item.item === "cadastro_fiscal"` → `{ label: "CNPJ cadastral", href: "/loja?tab=dados&fiscal=pending&returnTo=/dashboard" }`
    - `brand_profile` → `{ label: "Direção visual", href: "/loja?tab=direcao-visual&message=needs-visual-direction" }`
    - Sem alteração de lógica além dos hrefs (o componente e seus props permanecem).

    **cnpj-update-banner.tsx**:
    - href da linha ~27: `/loja?required=cadastro-fiscal&returnTo=/dashboard` → `/loja?tab=dados&fiscal=pending&returnTo=/dashboard`
    - Manter o restante do banner (label/mensagem) intacto.
  </action>
  <verify>
    <automated>npm run typecheck; npm run lint; grep -n "tab=dados&fiscal=pending" src/components/readiness/readiness-banner.tsx src/components/legacy/cnpj-update-banner.tsx; grep -c "required=" src/components/readiness/readiness-banner.tsx src/components/legacy/cnpj-update-banner.tsx — 0</automated>
  </verify>
  <acceptance_criteria>
    - `missingToDisplay` retorna os dois hrefs exatos do D12 (cadastro_fiscal → `?tab=dados&fiscal=pending&returnTo=/dashboard`; brand_profile → `?tab=direcao-visual&message=needs-visual-direction`)
    - `cnpj-update-banner.tsx` href → `?tab=dados&fiscal=pending&returnTo=/dashboard`
    - Nenhum `?required=` restante nos dois banners
    - Nenhuma outra lógica alterada (diff mínimo)
  </acceptance_criteria>
  <done>Banners do dashboard apontam para ?tab= com mensagens contextuais preservadas</done>
</task>

<task type="auto">
  <name>Task 3: Atualizar testes existentes de redirect/banners</name>
  <files>src/components/flow/__tests__/store-identity-form.redirect-messages.test.ts, src/components/readiness/__tests__/readiness-banner.test.tsx</files>
  <read_first>
    - src/components/flow/__tests__/store-identity-form.redirect-messages.test.ts (URLSearchParams extraction — linhas 16-35)
    - src/components/readiness/__tests__/readiness-banner.test.tsx (assert de href — linhas 17-24)
    - .planning/phases/36-onboarding-navegacao-por-abas/36-PATTERNS.md (Test Pattern Notes — linhas 627-631)
  </read_first>
  <action>
    Atualizar as asserções dos testes existentes para os novos targets `?tab=` (mantendo os cenários; apenas mudando as expectativas de URL):

    1. `store-identity-form.redirect-messages.test.ts`: cenários que esperavam parsing de `required=visual-direction`/`required=cadastro-fiscal` passam a esperar `tab=direcao-visual` / `tab=dados` (+ `fiscal=pending` quando aplicável). Preservar UM cenário de compat: `required=visual-direction` ainda mapeia para a aba direcao-visual (D6 compat — a 36-04 implementa o parsing; este teste cobre o comportamento de extração).

    2. `readiness-banner.test.tsx`: substituir a asserção `'/loja?required=cadastro-fiscal'` por `'/loja?tab=dados&fiscal=pending&returnTo=/dashboard'` (e, se houver, a de brand_profile por `'/loja?tab=direcao-visual&message=needs-visual-direction'`).

    3. Não criar testes novos aqui (a 36-06 cobre os novos cenários); apenas migrar as expectativas existentes para não regredirem.
  </action>
  <verify>
    <automated>npx vitest run src/components/flow/__tests__/store-identity-form.redirect-messages.test.ts src/components/readiness/__tests__/readiness-banner.test.tsx — todos verdes; npm run typecheck; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `readiness-banner.test.tsx` asserta `?tab=dados&fiscal=pending&returnTo=/dashboard` e `?tab=direcao-visual&message=needs-visual-direction` (grep das strings nos testes)
    - `store-identity-form.redirect-messages.test.ts` asserta parsing de `tab=direcao-visual` e mantém 1 cenário de compat `required=visual-direction`
    - Nenhuma asserção de `?required=cadastro-fiscal` ou `?required=visual-direction` como target principal restante (apenas o cenário de compat)
    - Suites verdes; typecheck/lint limpos
  </acceptance_criteria>
  <done>Testes de redirect/banners migrados e verdes — sem regressão de expectativas antigas</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server component → URL | `redirect()` gera URLs de destino com query strings construídas no servidor |
| banner (client) → URL | hrefs construídos com constantes locais |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-19 | Tampering | Query strings de redirect | mitigate | Targets construídos com literais fixos de `?tab=`/`message=`/`fiscal=` (tabela D12); `returnTo` apenas com `encodeURIComponent` — sem interpolação de input do usuário além do returnTo pré-existente |
| T-36-20 | Information Disclosure | Exposição de estado da loja na URL | accept | `fiscal=pending`/`message=needs-visual-direction` são estados de UX não sensíveis; compat com o padrão `required=` anterior |
| T-36-21 | Spoofing | Compat `required=` legado | accept | Mantido apenas como transição F36 (nota no código); `/loja` valida com `isOnboardingTab` (36-04) |
| T-36-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo — gate de legitimidade não se aplica |
</threat_model>

<verification>
- `npm run typecheck` e `npm run lint` limpos
- `grep -rn "required=" src/app/(app)/ src/components/readiness/ src/components/legacy/ src/components/flow/cnpj-update-form.tsx` — apenas referências de compat/notas, sem targets ativos
- Suites `store-identity-form.redirect-messages` e `readiness-banner` verdes
</verification>

<success_criteria>
- Guard `/campanhas/nova`, `/cadastro/cnpj`, `cnpj-update-form`, `ReadinessBanner` e `cnpj-update-banner` emitem targets `?tab=` exatos do D12
- `returnTo` preservado em todos os fluxos; mensagens contextuais (`fiscal=pending`, `message=needs-visual-direction`, `message=cnpj-updated`) nunca perdidas
- Compat `required=` legado coberto por 1 teste de transição
</success_criteria>

<output>
Create `.planning/phases/36-onboarding-navegacao-por-abas/36-05-SUMMARY.md` when done
</output>

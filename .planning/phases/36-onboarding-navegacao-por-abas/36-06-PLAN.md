---
phase: 36-onboarding-navegacao-por-abas
plan: 06
type: execute
wave: 5
depends_on: ["36-01", "36-02", "36-03", "36-04", "36-05"]
files_modified:
  - "src/__tests__/api/store-creation-matrix.test.ts"
  - "src/__tests__/api/store-ownership-api.test.ts"
  - "src/components/flow/__tests__/store-tabs.test.tsx"
  - "src/components/flow/__tests__/legal-acceptance-panel.test.tsx"
  - "src/hooks/__tests__/use-onboarding-tabs.test.ts"
  - "src/lib/store-onboarding/__tests__/draft-store-autosave.test.ts"
  - "src/components/flow/__tests__/store-identity-form.drift-tabs.test.ts"
  - "src/components/flow/__tests__/store-page-client.test.tsx"
autonomous: false

> **D16 — supersede (pós-implementação):** expectativas de teste deste plano para aba bloqueada ("renderiza bloqueio + link 'Voltar para X'") foram **substituídas pelo hard-block (D16)**: aba bloqueada nega ativação (sem `onTabChange`, `aria-disabled`), conteúdo funcional não renderiza, deep-link/back-forward redirecionam à primeira aba anterior válida, "Continuar" mobile desabilitado. Atualizar os testes citados (store-tabs, use-onboarding-tabs, store-page-client, drift-tabs) ao implementar. Fonte da verdade atual: `openspec/.../design.md` (D16) + specs.
requirements:
  - F36-DRAFT-CREATE-03
  - F36-DRAFT-CREATE-04
  - F36-READINESS-01
  - F36-READINESS-02
  - F36-READINESS-04
must_haves:
  truths:
    - "POST /api/store: draft sem CNPJ 201 sem crédito; verified com CNPJ; 400/409/401 cobridos"
    - "Gates preservados: loja draft não gera (guard /campanhas/nova) nem recebe grant; is_test_store gera apenas com entitlement de teste"
    - "Encadeamento draft → fiscal via update-cnpj torna readiness ready após anexar CNPJ"
    - "Componentes/hook/libs da F36 cobertos (StoreTabs ARIA, LegalAcceptancePanel, useOnboardingTabs, autoSave/draft)"
    - "Drift bloqueador: modais abrem antes do PATCH de campos do snapshot; cancelar não persiste; realinhar/ignorar/dismiss concluem save+navegação; totalGeneratedSignatures intacto"
    - "Regressão F30/F32/F33/F34 verde; readiness: loja draft = cadastro_fiscal pendente, RPC inalterada, identity_state NÃO é critério"
  artifacts:
    - path: "src/__tests__/api/store-creation-matrix.test.ts"
      provides: "cobertura draft × verified (201/400/409/401)"
      contains: "create_store_draft"
    - path: "src/components/flow/__tests__/store-identity-form.drift-tabs.test.ts"
      provides: "regressão drift por abas (bloqueadores D13)"
      contains: "DriftDecisionModal"
  key_links:
    - from: "store-creation-matrix.test.ts"
      to: "POST /api/store draft mode (36-01)"
      via: "mockSupabaseRpc para create_store_draft"
      pattern: "create_store_draft"
    - from: "store-identity-form.drift-tabs.test.ts"
      to: "endpoints drift (preservados)"
      via: "asserts de POST brand-profile/realign e visual-signature/dismiss-critical-drift"
      pattern: "dismiss-critical-drift"
---

<objective>
Testes e verificação final da F36: cobertura de endpoint (draft × verified + gates), testes de componente/hook/lib (StoreTabs, LegalAcceptancePanel, useOnboardingTabs, autoSave/draft), regressão de drift (bloqueadores D13), regressão F30/F32/F33/F34, verificação de readiness (loja draft), `typecheck`/`lint`/`vitest` completos e checkpoint humano de validação manual (mobile + desktop).

Purpose: provar que a fase entrega os 31 requisitos sem regressão — especialmente que loja draft não gera campanha nem recebe crédito, e que o drift não regride (ordem/endpoints).
Output: ~34+ testes novos/migrados, suíte completa verde, evidências de verificação manual.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-onboarding-navegacao-por-abas/36-CONTEXT.md
@.planning/phases/36-onboarding-navegacao-por-abas/36-PATTERNS.md

<interfaces>
Referências de teste (extraídas de 36-PATTERNS.md — Test Pattern Notes, linhas 611-634):

- Endpoint (padrão `src/__tests__/api/store-creation-matrix.test.ts:1-80`): `vi.hoisted` + `mockSupabaseRpc`; mocks de require-user/csrf/constants/store-response/document-versions/cnpj. `store-ownership-api.test.ts:156-202`: POST 201/401/409, body user_id ignorado.
- Cenários obrigatórios (OpenSpec task 1.4/6.1): POST sem CNPJ → 201 `onboardingGranted:false`; com CNPJ → verified; duplicado → 409; CNPJ inválido → 400; sem acceptedTerms → 400; não autenticado → 401.
- Draft → fiscal (6.1): após `update-cnpj`, `check_store_readiness` passa a `ready` (mock RPC: cadastro_fiscal sai de `missing`).
- Componentes (6.2): StoreTabs (ARIA, deep-link, back/forward, aba bloqueada, mobile compacto), LegalAcceptancePanel (3 estados, variantes, a11y).
- Hook (6.3): `useOnboardingTabs` (troca de aba com/sem mínimo, navegação interna, saveStatus error, serialização de saves, **back/forward popstate → `?tab=` sync**) — arquivo criado na 36-03, ESTENDIDO aqui (não recriar); autoSave/draft (restauração, reconciliação, limpeza 1º save e logout).
- `store-page-client.test.tsx` (6.2, arquivo NOVO criado aqui): parsing `?tab=`/compat `required=`/`fiscal=`/`message=` — a 36-04 não cria este arquivo (referência removida do `<verify>` dela).
- Drift bloqueadores (6.6, a-g): ver tasks.md grupo 6.6.
- Padrão de teste de hook sem render: `src/components/flow/__tests__/use-drift-detection.test.ts:1-55`.
- Entrega verificável (especifics): `npm run typecheck`, `npm run lint`, `npx vitest run` sem erros; validação manual mobile (pagehide/visibilitychange, rascunho restaurado, CTA "Continuar" fixo) e desktop (coluna sticky, back/forward).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Testes de endpoint POST /api/store (draft × verified) + gates + draft → fiscal</name>
  <files>src/__tests__/api/store-creation-matrix.test.ts, src/__tests__/api/store-ownership-api.test.ts</files>
  <read_first>
    - src/__tests__/api/store-creation-matrix.test.ts (124 linhas — mocks existentes)
    - src/__tests__/api/store-ownership-api.test.ts (seções POST — linhas 156-202)
    - src/app/api/store/route.ts (rota em dois modos — 36-01)
    - src/lib/store-readiness.ts (getStoreReadiness/fallback — F34)
    - .planning/phases/36-onboarding-navegacao-por-abas/36-PATTERNS.md (Test Pattern Notes — linhas 633-634)
  </read_first>
  <action>
    Atualizar/migrar os testes de endpoint existentes para o novo contrato em dois modos (mock pattern `vi.hoisted` + `mockSupabaseRpc` já existente):

    1. **Migrar asserções antigas:** o cenário que assertava 400 "CNPJ é obrigatório" (do modo antigo) é substituído pelos cenários novos. Atualizar mocks: `mockSupabaseRpc` deve rotear por nome de função (`create_store_draft` vs `create_store_with_cnpj`).

    2. **Cenários obrigatórios** (em `store-creation-matrix.test.ts`):
       - POST sem `cnpj` → 201 com `onboardingGranted: false` e rpc chamado com `"create_store_draft"` (e SEM `p_cnpj_normalized` no payload)
       - POST com `cnpj` válido → 201 com fluxo verified (`create_store_with_cnpj` chamado; `cnpjMasked` presente)
       - POST duplicado (rpc erro 23505/`stores_user_id_key`) → 409 "Usuário já possui uma loja" (nos DOIS modos)
       - POST sem `acceptedTerms` → 400
       - POST sem `name`/name inválido → 400; `segment` inválido → 400; subsegmento "outros" sem subsegmento → 400 (modo draft)
       - CNPJ inválido (modo verified) → 400
    3. **Ownership (F36-OWNERSHIP-01)** em `store-ownership-api.test.ts`: manter/confirmar que `body.user_id` é ignorado (o mock de `requireUser` retorna o usuário autenticado e o payload da rpc usa `user.userId`); 401 sem auth (JSON, não redirect).
    4. **Gates (F36-DRAFT-CREATE-03):** assertar que o modo draft NÃO chama `try_grant_onboarding_entitlement` nem `grant_credits` (mock RPC nunca recebe essas funções — `mockSupabaseRpc.mock.calls` filtrado por nome); `onboardingGranted` é sempre `false` no draft.
    5. **Draft → fiscal (F36-DRAFT-CREATE-04):** teste de integração no nível de mocks: (a) criar loja draft (201); (b) `update-cnpj` via `update_store_cnpj` RPC (mock); (c) `check_store_readiness` com campos fiscais agora preenchidos → `missing` sem `cadastro_fiscal` → `ready: true`. Usar `getStoreReadiness` com `supabaseAdmin.rpc` mockado (padrão F34).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/api/store-creation-matrix.test.ts src/__tests__/api/store-ownership-api.test.ts — verdes; npm run typecheck; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `store-creation-matrix.test.ts` cobre: draft 201 `onboardingGranted:false` (sem p_cnpj_normalized), verified 201 (com cnpjMasked), 409 nos dois modos, 400 sem aceite/name/segment/subsegmento, 400 CNPJ inválido, 401
    - `store-ownership-api.test.ts` cobre: body `user_id` ignorado (claims.sub usado), 401 JSON
    - Gate: nenhum `grant_credits`/`try_grant_onboarding_entitlement` chamado no modo draft (assert de mock)
    - Encadeamento draft→fiscal: após `update-cnpj` mockado, readiness `ready: true` (sem cadastro_fiscal em missing)
    - Todos verdes; typecheck/lint limpos
  </acceptance_criteria>
  <done>Cobertura de endpoint completa (draft/verified/gates/draft→fiscal) com suíte verde</done>
</task>

<task type="auto">
  <name>Task 2: Testes de componente, hook e lib + drift bloqueador</name>
  <files>src/components/flow/__tests__/store-tabs.test.tsx, src/components/flow/__tests__/legal-acceptance-panel.test.tsx, src/hooks/__tests__/use-onboarding-tabs.test.ts, src/lib/store-onboarding/__tests__/draft-store-autosave.test.ts, src/components/flow/__tests__/store-identity-form.drift-tabs.test.ts, src/components/flow/__tests__/store-page-client.test.tsx</files>
  <read_first>
    - src/components/flow/__tests__/use-drift-detection.test.ts (padrão de teste de hook — linhas 1-55)
    - src/lib/__tests__/drift.test.ts (padrão de teste puro)
    - src/components/flow/__tests__/store-identity-form.drift.test.ts (cenários de drift atuais a estender)
    - .planning/phases/36-onboarding-navegacao-por-abas/36-PATTERNS.md (Test Pattern Notes — linhas 611-634; drift integration 595-598)
    - openspec/changes/fase-36-onboarding-navegacao-por-abas/tasks.md (grupo 6.6 — cenários de drift bloqueadores)
  </read_first>
  <action>
    Escrever os testes novos (padrões do repo — verificação de export/retorno para hooks sem render; `@testing-library/react` para componentes, se for o padrão do repo):

    1. `store-tabs.test.tsx` (F36-TABS-03/04): ARIA roles (`tablist`/`tab`/`tabpanel`), `aria-selected` no ativo, roving tabindex (apenas ativo `tabIndex={0}`), teclas ArrowRight/ArrowLeft/Home/End, `aria-live` presente; **D16 (hard-block):** aba bloqueada tem `aria-disabled="true"`, **clicar/seta nela NÃO dispara `onTabChange`** (activeTab não muda) e o motivo é acessível via `aria-label`/`aria-describedby`/tooltip no botão; deep-link em aba bloqueada redireciona/sincroniza para a primeira aba anterior válida + aviso; back/forward (simular `?tab=` trocando a prop `activeTab`); variante mobile renderiza labels `labelMobile` e botão "Continuar" fixo e **desabilitado quando a próxima aba bloqueada**; touch: classes `min-h-[44px]`.

    2. `legal-acceptance-panel.test.tsx` (F36-LEGAL-01/02): 3 estados renderizam labels distintos; variantes desktop (`sticky`) × mobile (sem sticky); CTA "Revisar e aceitar" chama `onOpenModal` e expõe `aria-expanded`; (integração com ContractAcceptanceModal fica no form — aqui apenas o painel).

    3. `use-onboarding-tabs.test.ts` (F36-AUTOSAVE-02 + F36-TABS-04/D6) — ESTENDER o arquivo criado na 36-03 (não recriar): troca de aba com mínimo válido chama `autoSave` e atualiza `activeTab`; sem mínimo → `{ok:false}` e aba NÃO muda; `saveStatus` erro em PATCH falho não bloqueia troca; serialização: duas saves concorrentes com promises resolvidas fora de ordem não sobrescrevem estado (ref/seq guard); `handlePageHide` grava draft síncrono (mock localStorage) e PATCH best-effort quando storeId existe; **back/forward popstate (obrigatório — F36-TABS-04/D6 + D16):** dispatch de `popstate` com `?tab=` no history → `activeTab` sincroniza passando pela ordem de drift D13 (modal intercepta antes do save quando há drift novo nos campos do snapshot); **alvo bloqueado NÃO sincroniza `activeTab`** — roteia para a primeira aba anterior válida e emite o aviso "Complete esta etapa para liberar {aba}"; `?tab=` inválido → mantém aba atual.

    4. `draft-store-autosave.test.ts` (F36-DRAFT-02/03/04): escrita síncrona em pagehide/visibilitychange (mock de evento); restauração `:new` e `:${storeId}` reconciliada com banco (banco prevalece em campos persistidos); limpeza da chave `:new` após 1º save (`clearDraft(userId)`); `clearAllDrafts` remove todas as chaves de draft no logout.

    5. `store-identity-form.drift-tabs.test.ts` (F36-AUTOSAVE-03 — BLOQUEADORES D13, cenários a-g do tasks.md 6.6):
       - (a) saída de contexto (troca de aba / gerar campanha / dashboard) com drift sensível novo → `DriftDecisionModal` abre e NENHUM PATCH de campos do snapshot é enviado antes da decisão
       - (b) navegação interna com drift sensível intercepta e abre o modal
       - (c) drift crítico → `DriftCriticalModal` (não o sensível)
       - (d) cancelar o modal mantém o usuário no contexto atual sem persistir
       - (e) após realinhar/ignorar/dismissCriticalDrift o save prossegue e a navegação é concluída (asserts dos endpoints `brand-profile/realign`, `brand-profile/metadata` com `drift_dismissed_snapshot`, `visual-signature/dismiss-critical-drift`)
       - (f) `totalGeneratedSignatures` e gatilho de limite intactos (loja com assinatura visual ativa)
        - (g) edições em campos fora do snapshot (ex.: billing) fazem auto-save normalmente mesmo com drift pendente

    6. `store-page-client.test.tsx` (F36-TABS-04/D6 — arquivo NOVO, criado aqui): parsing de `?tab=` (válido → aba correspondente; inválido/ausente → `"dados"`); ordem de resolução `?tab=` → compat `required=visual-direction` → `direcao-visual` / `required=cadastro-fiscal` → `dados` → `"dados"`; `fiscal=pending` → `fiscalPending`; `message=` repassado; nenhuma leitura de `localStorage("store_id")`.
  </action>
  <verify>
    <automated>npx vitest run src/components/flow/__tests__/store-tabs.test.tsx src/components/flow/__tests__/legal-acceptance-panel.test.tsx src/hooks/__tests__/use-onboarding-tabs.test.ts src/lib/store-onboarding/__tests__/draft-store-autosave.test.ts src/components/flow/__tests__/store-identity-form.drift-tabs.test.ts src/components/flow/__tests__/store-page-client.test.tsx — todos verdes</automated>
  </verify>
  <acceptance_criteria>
    - `store-tabs.test.tsx` ≥ 6 cenários (ARIA roles, roving tabindex, teclas, descrito-por, deep-link bloqueado, mobile compacto, 44px)
    - `legal-acceptance-panel.test.tsx` ≥ 4 cenários (3 estados + variantes + a11y)
    - `use-onboarding-tabs.test.ts` ≥ 6 cenários (estendido da 36-03; inclui serialização ref/seq, pagehide e back/forward popstate URL→tab com drift D13 e aba bloqueada D6)
    - `store-page-client.test.tsx` ≥ 4 cenários (tab válido/inválido, compat required=, fiscal/message) — criado aqui
    - `draft-store-autosave.test.ts` ≥ 4 cenários (síncrono, restauração/reconciliação, limpeza 1º save, logout)
    - `store-identity-form.drift-tabs.test.ts` cobre os 7 cenários a-g do grupo 6.6 — asserts explícitos dos endpoints de drift
    - Suíte total dos 6 arquivos verde
  </acceptance_criteria>
  <done>~30+ testes novos verdes cobrindo componentes, hook (incl. popstate), draft, parsing de URL e drift bloqueador</done>
</task>

<task type="auto">
  <name>Task 3: Regressão F30/F32/F33/F34 + readiness + typecheck/lint/vitest completo</name>
  <files>src/__tests__/api/store-creation-matrix.test.ts</files>
  <read_first>
    - src/lib/store-readiness.ts (getStoreReadiness — F34)
    - src/lib/store-readiness/__tests__/store-readiness.test.ts (testes existentes a ampliar)
    - .planning/phases/36-onboarding-navegacao-por-abas/36-CONTEXT.md (F36-READINESS-01/02/04 — seção readiness)
  </read_first>
  <action>
    1. **Readiness (F36-READINESS-01/02/04):** adicionar/estender testes em `src/lib/store-readiness/__tests__/store-readiness.test.ts`:
       - Loja draft (campos fiscais NULL) → `getStoreReadiness` retorna `ready: false` com `"cadastro_fiscal"` em `missing` (sem mudança de lógica — dado ausente já gera pendência na RPC F34; o teste documenta o comportamento)
       - `check_store_readiness` RPC permanece inalterada (nenhuma asserção nova de contrato RPC — apenas confirma que o consumer não mudou)
       - `identity_state`/estado de onboarding NÃO vira critério de readiness (teste: loja com identity_state preenchido mas fiscal ausente continua `ready: false` com `cadastro_fiscal` pendente)
       - Prioridade cadastro fiscal → brand profile mantida (missing order: cadastro_fiscal antes de brand_profile quando ambos ausentes)

    2. **Regressão (F36-DRAFT-CREATE-03 + task 6.4):** rodar a suíte completa dos fluxos:
       - F30 (legal: `legalClearance`, `getAcceptanceStatus`, aceites) — suítes de `src/lib/legal/` e componentes legais
       - F32/F33 (criação com CNPJ + crédito, `update_store_cnpj`, `is_test_store`, freemium) — `store-creation-matrix`, testes de cnpj/freemium
       - F34 (readiness, guard `/campanhas/nova`, banner dashboard) — `store-readiness`, `readiness-banner`, redirects
       - Drift (bloqueadores já na task 2) + testes de endpoint antigos migrados
       - Garantir que os gates de geração permanecem inalterados: loja draft não passa no guard `/campanhas/nova` (redirect para `?tab=dados&fiscal=pending`)

    3. **Suíte completa:** `npx vitest run` (todos os testes), `npm run typecheck`, `npm run lint` — tudo limpo. Registrar contagem total de testes (base 1345 da F35 + ~34+ novos F36).

    4. Se algum teste falhar por regressão real, corrigir APENAS o necessário (nunca enfraquecer asserção de segurança/gate).
  </action>
  <verify>
    <automated>npx vitest run — suíte completa verde; npm run typecheck; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Testes de readiness estendidos: loja draft → `ready:false` com `cadastro_fiscal` em `missing`; `identity_state` não altera o critério; ordem de missing cadastro_fiscal → brand_profile
    - Regressão completa verde: suítes F30/F32/F33/F34 e drift sem regressão
    - `npx vitest run` — 0 falhas; `npm run typecheck` e `npm run lint` limpos
    - Contagem de testes registrada no SUMMARY (base 1345 + novos)
  </acceptance_criteria>
  <done>Regressão completa verde + readiness de loja draft documentada em teste + typecheck/lint/vitest limpos</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Validação manual mobile + desktop (checkpoint humano)</name>
  <files>Nenhum arquivo modificado — verificação manual do fluxo completo (dev server ou preview)</files>
  <action>
    Após as Tasks 1-3 verdes (suíte completa, typecheck/lint limpos, schema push da 36-01 aplicado), submeter ao usuário a validação manual do fluxo completo da F36 conforme o passo a passo de <how-to-verify>. Toda automação já foi executada; este checkpoint cobre apenas o que exige olho humano (visual, mobile, back/forward, abandono de aba).
  </action>
  <verify>Pausa para o usuário executar os cenários de <how-to-verify> e responder via <resume-signal> — nenhum comando automatizado adicional</verify>
  <done>Aprovação do usuário registrada (resume-signal) OU lista de problemas reprodutíveis encaminhada para correção</done>
  <what-built>Toda a F36: onboarding por abas em /loja, auto-save, rascunho localStorage, aceite legal lateral, CNPJ opcional, redirects ?tab= e backend draft</what-built>
  <how-to-verify>
    Abrir a aplicação (dev server ou preview Vercel) e verificar:
    1. **Mobile (DevTools responsive ou celular):**
       - a. Abrir `/loja` — ver 3 abas compactas (Dados/Perfil/Visual) e botão "Continuar" fixo no rodapé (touch ≥ 44px)
       - b. Digitar apenas nome+segmento, aceitar legal (coluna compacta no topo), avançar → loja criada via draft; fechar a aba e reabrir `/loja` dentro de 24h → rascunho restaurado (pagehide/visibilitychange)
       - c. Deixar a página em background (trocar de app) com campos digitados e voltar → nada perdido
    2. **Desktop:**
       - a. Coluna lateral de aceite legal sticky (participa do grid, não sobrepõe) com estados Pendente/Aceito/Reaceite necessário
       - b. Back/forward alternam a aba via `?tab=` no history (D6)
        - c. Deep-link `/loja?tab=direcao-visual` em loja sem direção visual → **NÃO abre a aba bloqueada**: sincroniza para Dados/Posicionamento + aviso "Complete esta etapa para liberar Direção Visual" (nunca tela em branco, nunca aba bloqueada ativa — D16)
    3. **Fluxos de entrada:**
       - a. Loja draft sem CNPJ → tentar gerar campanha em `/campanhas/nova` → redirect `/loja?tab=dados&fiscal=pending` com banner "Fiscal pendente"
       - b. Banner do dashboard (ReadinessBanner) aponta para `?tab=dados&fiscal=pending` / `?tab=direcao-visual&message=needs-visual-direction`
     4. **Legal:** com aceite pendente, a aba Posicionamento fica bloqueada e **não é ativável** (D16); o motivo "falta aceite legal" fica acessível no botão da aba (tooltip/`aria-label`) e/ou como orientação na aba atual, com link para o card
  </how-to-verify>
  <resume-signal>Digite "approved" para concluir a F36, ou descreva os problemas encontrados (com repro)</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| testes → mocks de RPC/auth | Mocks simulam service_role e claims — nenhum banco real tocado |
| testes → localStorage mockado | Simula escrita/leitura síncrona de draft |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-22 | Elevation of Privilege | Testes de gate freemium | mitigate | Asserts explícitos: modo draft nunca invoca `grant_credits`/`try_grant_onboarding_entitlement`; `is_test_store` só gera com entitlement de teste (mock) — F36-DRAFT-CREATE-03 |
| T-36-23 | Tampering | Regressão de drift | mitigate | Cenários 6.6(a-g) bloqueadores: ordem modal→decisão→PATCH+navegação assertada; cancelar não persiste — F36-AUTOSAVE-03 |
| T-36-24 | Spoofing | Testes de ownership | mitigate | `body.user_id` ignorado assertado; 401 JSON — F36-OWNERSHIP-01 |
| T-36-25 | Information Disclosure | Readiness de loja draft | mitigate | Nenhum dado fiscal sensível nos asserts; apenas `missing`/`ready` booleanos — F36-READINESS-01 |
| T-36-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo — gate de legitimidade não se aplica |
</threat_model>

<verification>
- `npx vitest run` — suíte completa verde (base 1345 + ~34 novos F36)
- `npm run typecheck` e `npm run lint` limpos
- `supabase db push` (36-01) já aplicado — pré-requisito desta verificação
- Checkpoint humano de validação mobile/desktop aprovado
</verification>

<success_criteria>
- Endpoint: draft 201 sem crédito, verified 201, 400/409/401, body user_id ignorado, draft→fiscal ready
- Gates: loja draft não gera nem recebe grant; readiness reporta cadastro_fiscal pendente; is_test_store coberto
- Drift bloqueador 7/7 cenários verdes; totalGeneratedSignatures intacto
- Regressão F30/F32/F33/F34 verde; typecheck/lint/build limpos; validação manual aprovada
</success_criteria>

<output>
Create `.planning/phases/36-onboarding-navegacao-por-abas/36-06-SUMMARY.md` when done
</output>

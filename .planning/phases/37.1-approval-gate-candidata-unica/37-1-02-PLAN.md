---
phase: 37.1-approval-gate-candidata-unica
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260901000001_f37_1_create_campaign_art_versions.sql
  - supabase/migrations/20260901000002_f37_1_approve_campaign_art_version_rpc.sql
autonomous: false
requirements: [F37.1-03, F37.1-04, F37.1-05, F37.1-06, F37.1-07, F37.1-08]

user_setup:
  - service: supabase
    why: "db push para aplicar as migrations no remoto (não-TTY)"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard -> Account -> Access Tokens (token pessoal com scope de DB)"
    dashboard_config: []

must_haves:
  truths:
    - "A tabela campaign_art_versions existe com todas as colunas do spec (status, asset_status, correction_in_progress, brief_snapshot NOT NULL, UNIQUE(campaign_id, version_number)), RLS habilitada com acesso somente service_role"
    - "campaigns tem approval_status (default pending_approval), rejection_count (smallint 0..2), approved_version_id (FK), approved_at + CHECK campaigns_approved_requires_version"
    - "O índice único parcial campaign_art_versions_one_approved_per_campaign (WHERE status='approved') existe — 1 approved por campaign_id"
    - "O RPC approve_campaign_art_version(p_campaign_id, p_version_id) é SECURITY DEFINER com SET search_path='', transacional (SELECT FOR UPDATE + defensivo + repontar campaigns) e executável apenas por service_role"
    - "O seed campaign_approval_enabled = false existe em feature_flags (ON CONFLICT DO NOTHING)"
    - "Sem backfill e sem alteração do CHECK chk_generation_events_type; migrations aplicadas no remoto via db push [BLOCKING]"
  artifacts:
    - path: "supabase/migrations/20260901000001_f37_1_create_campaign_art_versions.sql"
      provides: "Tabela + colunas campaigns + índice único parcial + CHECK + seed flag + RLS + REVERT"
      contains: "campaign_art_versions"
    - path: "supabase/migrations/20260901000002_f37_1_approve_campaign_art_version_rpc.sql"
      provides: "RPC transacional approve_campaign_art_version"
      contains: "approve_campaign_art_version"
  key_links:
    - from: "campaigns.approved_version_id"
      to: "campaign_art_versions.id"
      via: "FK REFERENCES public.campaign_art_versions(id)"
      pattern: "REFERENCES public.campaign_art_versions"
    - from: "RPC approve_campaign_art_version"
      to: "campaigns (storage_path, approval_status)"
      via: "UPDATE campaigns na mesma transação (repoint)"
      pattern: "UPDATE public.campaigns"
---

<objective>
Criar as 2 migrations da fatia (tasks.md seções 2 e 3): (1) `20260901000001_f37_1_create_campaign_art_versions.sql` — tabela `campaign_art_versions` + colunas de aprovação em `campaigns` + índice único parcial 1-approved + CHECK `campaigns_approved_requires_version` + seed da flag `campaign_approval_enabled=false` + RLS service_role-only (padrão `feature_flags` F43); (2) `20260901000002_f37_1_approve_campaign_art_version_rpc.sql` — RPC `approve_campaign_art_version` transacional (SECURITY DEFINER, `SET search_path=''`, guarded update + defensivo + repontar campaigns). Aplicar no remoto com `supabase db push` [BLOCKING] — as migrations PRECEDEM o código que as consome (ordem de deploy fail-closed, D3).

Purpose: Base de dados da fatia (D3/D7/D8) — sem ela, nenhum código posterior funciona. O db push [BLOCKING] é obrigatório para a fase passar na verificação (precedente F43-07/F38.2-01).
Output: 2 migrations versionadas + schema aplicado no remoto.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@openspec/changes/fase-37-1-approval-gate-candidata-unica/design.md (D3 — SQL completo da migration 1; D5 — passo a passo do RPC)
@openspec/changes/fase-37-1-approval-gate-candidata-unica/specs/campaign-art-versions/spec.md (requisitos da tabela/colunas/RPC)
@openspec/changes/fase-37-1-approval-gate-candidata-unica/tasks.md (seções 2 e 3)
@supabase/migrations/20260821000001_f43_create_feature_flags.sql (precedente RLS/policy/seed/RPC SECURITY DEFINER + REVERT)

<interfaces>
Contrato SQL esperado pelo código posterior:
- Tabela `public.campaign_art_versions`: id uuid PK DEFAULT gen_random_uuid(); campaign_id uuid NOT NULL REFERENCES public.campaigns(id) ON DELETE CASCADE; version_number smallint CHECK 1..3; status text CHECK IN ('pending','approved','rejected'); correction_in_progress boolean NOT NULL DEFAULT false; storage_path text NULL; asset_status text NOT NULL DEFAULT 'active' CHECK IN ('active','discarded'); asset_deleted_at timestamptz; brief_snapshot jsonb NOT NULL; render_snapshot jsonb; generation_metadata jsonb; rejection_reason jsonb; created_at timestamptz DEFAULT now(); UNIQUE (campaign_id, version_number).
- Colunas em `public.campaigns` (ADD COLUMN IF NOT EXISTS): approval_status text NOT NULL DEFAULT 'pending_approval' CHECK IN ('pending_approval','approved'); rejection_count smallint NOT NULL DEFAULT 0 CHECK 0..2; approved_version_id uuid REFERENCES public.campaign_art_versions(id); approved_at timestamptz.
- CHECK `campaigns_approved_requires_version` via bloco idempotente `DO $$ ... IF NOT EXISTS (pg_constraint) ... END $$` (proibido `ADD CONSTRAINT IF NOT EXISTS` — não portável no Supabase).
- Índice único parcial: `CREATE UNIQUE INDEX IF NOT EXISTS campaign_art_versions_one_approved_per_campaign ON public.campaign_art_versions (campaign_id) WHERE status = 'approved'`.
- RPC `approve_campaign_art_version(p_campaign_id uuid, p_version_id uuid)` RETURNS jsonb — códigos de erro RAISE EXCEPTION: 'version_not_found', 'version_campaign_mismatch', 'version_not_pending', 'version_not_active' (mapeados na rota approve).
- RLS campaign_art_versions: ENABLE ROW LEVEL SECURITY + policy FOR ALL TO service_role USING(true) WITH CHECK(true) + REVOKE de anon/authenticated + GRANT SELECT, INSERT, UPDATE TO service_role.
- RPC: REVOKE EXECUTE FROM PUBLIC, anon, authenticated; GRANT EXECUTE TO service_role.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 1 — campaign_art_versions + colunas campaigns + índice único parcial + CHECK + seed flag + RLS</name>
  <files>supabase/migrations/20260901000001_f37_1_create_campaign_art_versions.sql</files>
  <read_first>
    - supabase/migrations/20260821000001_f43_create_feature_flags.sql (padrão RLS/REVOKE/GRANT/seed/REVERT)
    - openspec/changes/fase-37-1-approval-gate-candidata-unica/design.md (D3 — SQL de referência da tabela, colunas e bloco DO)
  </read_first>
  <action>
    Criar a migration idempotente e não destrutiva com, NA ORDEM:
    1. `CREATE TABLE IF NOT EXISTS public.campaign_art_versions` com TODAS as colunas do contrato `<interfaces>` (id uuid PK DEFAULT gen_random_uuid(), campaign_id FK CASCADE, version_number smallint CHECK BETWEEN 1 AND 3, status text CHECK IN ('pending','approved','rejected'), correction_in_progress boolean NOT NULL DEFAULT false, storage_path text, asset_status text NOT NULL DEFAULT 'active' CHECK IN ('active','discarded'), asset_deleted_at timestamptz, brief_snapshot jsonb NOT NULL, render_snapshot jsonb, generation_metadata jsonb, rejection_reason jsonb, created_at timestamptz DEFAULT now(), UNIQUE (campaign_id, version_number)).
    2. `ENABLE ROW LEVEL SECURITY` + policy "Service role can manage campaign art versions" FOR ALL TO service_role USING(true) WITH CHECK(true) + `REVOKE ALL ON TABLE ... FROM anon, authenticated` + `GRANT SELECT, INSERT, UPDATE ON TABLE ... TO service_role` (espelhar o padrão feature_flags F43).
    3. `ALTER TABLE public.campaigns` com `ADD COLUMN IF NOT EXISTS` das 4 colunas (approval_status default 'pending_approval' + CHECK IN; rejection_count smallint NOT NULL DEFAULT 0 + CHECK BETWEEN 0 AND 2; approved_version_id uuid REFERENCES public.campaign_art_versions(id); approved_at timestamptz).
    4. CHECK condicional `campaigns_approved_requires_version` via bloco `DO $$ BEGIN IF NOT EXISTS (SELECT 1 FROM pg_constraint WHERE conname = 'campaigns_approved_requires_version' AND conrelid = 'public.campaigns'::regclass) THEN ALTER TABLE public.campaigns ADD CONSTRAINT campaigns_approved_requires_version CHECK (approval_status <> 'approved' OR approved_version_id IS NOT NULL); END IF; END $$;` — NÃO usar `ADD CONSTRAINT IF NOT EXISTS`.
    5. Índice único parcial `campaign_art_versions_one_approved_per_campaign` ON (campaign_id) WHERE status = 'approved'.
    6. Seed idempotente: `INSERT INTO public.feature_flags (key, enabled, description) VALUES ('campaign_approval_enabled', false, 'Quando ligada, campanhas novas entram no fluxo de revisão/aprovação da arte antes da entrega (download + Kit de Publicação). Campanhas antigas (sem versões de arte) continuam entregues como hoje.') ON CONFLICT (key) DO NOTHING;`.
    7. **Sem backfill** (nenhuma linha de campaign_art_versions para campanhas existentes) e **sem alteração** de `chk_generation_events_type` (não tocar em generation_events).
    8. Seção `-- REVERT` comentada na ordem inversa (DROP INDEX, DROP CONSTRAINT, DROP COLUMN x4, DROP POLICY, DISABLE RLS, DROP TABLE, DELETE seed).
  </action>
  <verify>
    <automated>Select-String -LiteralPath "supabase\migrations\20260901000001_f37_1_create_campaign_art_versions.sql" -Pattern "campaign_art_versions_one_approved_per_campaign|WHERE status = 'approved'|campaigns_approved_requires_version|ON CONFLICT \(key\) DO NOTHING|ENABLE ROW LEVEL SECURITY|brief_snapshot jsonb NOT NULL" | Select-Object -First 8</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `20260901000001_f37_1_create_campaign_art_versions.sql` existe e contém: `CREATE TABLE IF NOT EXISTS public.campaign_art_versions`, `campaign_art_versions_one_approved_per_campaign`, `WHERE status = 'approved'`, `campaigns_approved_requires_version`, `DO $$` (bloco idempotente), `ON CONFLICT (key) DO NOTHING` (seed `campaign_approval_enabled`), `ENABLE ROW LEVEL SECURITY`
    - NÃO contém `ADD CONSTRAINT IF NOT EXISTS` nem `ALTER TABLE public.generation_events` (proibido)
    - NÃO contém INSERT em campaign_art_versions com campaign_id de campanha existente (sem backfill)
    - Contém seção `-- REVERT` completa
  </acceptance_criteria>
  <done>Migration 1 completa e idempotente — tabela, colunas, índice, CHECK, seed e RLS conforme D3</done>
</task>

<task type="auto">
  <name>Task 2: Migration 2 — RPC approve_campaign_art_version transacional</name>
  <files>supabase/migrations/20260901000002_f37_1_approve_campaign_art_version_rpc.sql</files>
  <read_first>
    - supabase/migrations/20260821000001_f43_create_feature_flags.sql (padrão SECURITY DEFINER + SET search_path='' + REVOKE/GRANT + RAISE EXCEPTION por código)
    - openspec/changes/fase-37-1-approval-gate-candidata-unica/design.md (D5 — 4 passos do RPC)
  </read_first>
  <action>
    Criar a migration com `CREATE OR REPLACE FUNCTION public.approve_campaign_art_version(p_campaign_id uuid, p_version_id uuid) RETURNS jsonb LANGUAGE plpgsql SECURITY DEFINER SET search_path = ''` implementando, em UM bloco transacional (ROLLBACK automático em falha):
    1. Validação de inputs: `IF p_campaign_id IS NULL OR p_version_id IS NULL THEN RAISE EXCEPTION 'missing_params'; END IF;`
    2. `SELECT ... FOR UPDATE` da versão em `public.campaign_art_versions WHERE id = p_version_id` — se `NOT FOUND` → `RAISE EXCEPTION 'version_not_found'`.
    3. Validações sequenciais com RAISE EXCEPTION por código: `v_campaign_id <> p_campaign_id` → `'version_campaign_mismatch'`; `v_status <> 'pending'` → `'version_not_pending'` (já resolvida); `v_asset_status <> 'active'` → `'version_not_active'`.
    4. **Defensivo (D8):** `UPDATE public.campaign_art_versions SET asset_status='discarded', storage_path=NULL, asset_deleted_at=now() WHERE campaign_id = p_campaign_id AND id <> p_version_id AND asset_status = 'active'` (no-op na 37.1 — só existe v1; remoção física de arquivo é 37.2).
    5. `UPDATE public.campaign_art_versions SET status='approved' WHERE id = p_version_id`.
    6. `UPDATE public.campaigns SET storage_path = v_storage_path, approved_version_id = p_version_id, approved_at = now(), approval_status = 'approved' WHERE id = p_campaign_id` (storage_path repontado para a aprovada — decisão 3).
    7. `RETURN jsonb_build_object('success', true, 'campaign_id', p_campaign_id, 'version_id', p_version_id)`.
    8. `REVOKE EXECUTE ON FUNCTION public.approve_campaign_art_version(uuid, uuid) FROM PUBLIC, anon, authenticated;` + `GRANT EXECUTE ON FUNCTION public.approve_campaign_art_version(uuid, uuid) TO service_role;` + seção `-- REVERT` comentada.
    Todo identificador de tabela schema-qualified (`public.`) por causa do `SET search_path=''`.
  </action>
  <verify>
    <automated>Select-String -LiteralPath "supabase\migrations\20260901000002_f37_1_approve_campaign_art_version_rpc.sql" -Pattern "SECURITY DEFINER|SET search_path = ''|FOR UPDATE|version_not_found|version_campaign_mismatch|version_not_pending|version_not_active|asset_status='discarded'|approval_status = 'approved'|GRANT EXECUTE" | Select-Object -First 12</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `20260901000002_f37_1_approve_campaign_art_version_rpc.sql` existe e contém: `SECURITY DEFINER`, `SET search_path = ''`, `FOR UPDATE`, os 4 códigos de erro (`version_not_found`, `version_campaign_mismatch`, `version_not_pending`, `version_not_active`), o UPDATE defensivo (`asset_status='discarded'`), o UPDATE de campaigns (`approval_status = 'approved'`), `GRANT EXECUTE ... TO service_role` e `REVOKE EXECUTE ... FROM PUBLIC, anon, authenticated`
    - Nenhum acesso a tabela sem prefixo `public.` (search_path vazio)
    - Contém seção `-- REVERT` comentada
  </acceptance_criteria>
  <done>Migration 2 completa — RPC transacional com guarded update, defensivo e repontar campaigns conforme D5/D8</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] supabase db push no remoto + verificação do schema aplicado</name>
  <files>supabase/migrations/20260901000001_f37_1_create_campaign_art_versions.sql, supabase/migrations/20260901000002_f37_1_approve_campaign_art_version_rpc.sql</files>
  <read_first>
    - .planning/phases/43-revisao-brief-pre-geracao/43-07-SUMMARY.md (precedente de db push da F43 — comando e verificação usados)
  </read_first>
  <action>
    1. Executar `supabase db push` no diretório do repositório (workdir raiz). Se o ambiente for não-TTY e exigir autenticação, garantir `SUPABASE_ACCESS_TOKEN` no ambiente (valor do usuário — ver frontmatter user_setup) e `supabase link --project-ref <ref existente>` já configurado (confirmar com `supabase projects list` ou `supabase status` — o projeto já está linkado no repo).
    2. Se o push pedir confirmação interativa de nomes de tabelas/colunas (diff de segurança do Supabase), usar `--include-all` quando o diff for exclusivamente das 2 migrations desta fatia (precedente F43-07); se houver prompt que não pode ser suprimido, pausar para intervenção manual do usuário (a tarefa é BLOCKING).
    3. Verificar no remoto que o schema foi aplicado: `supabase db dump --schema public` (ou `supabase query` em um script de verificação) deve conter: tabela `campaign_art_versions`, colunas `approval_status`/`rejection_count`/`approved_version_id`/`approved_at` em `campaigns`, índice `campaign_art_versions_one_approved_per_campaign` e função `approve_campaign_art_version`.
    4. Confirmar que o seed existe: consultar `feature_flags` WHERE key='campaign_approval_enabled' → enabled=false.
  </action>
  <verify>
    <automated>supabase db push --include-all; if ($?) { supabase db dump --schema public --local | Select-String -Pattern "campaign_art_versions|approve_campaign_art_version" }</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` completa sem erro (todas as migrations aplicadas no remoto; saída lista `20260901000001` e `20260901000002` como Applied)
    - Dump/query do remoto contém `campaign_art_versions`, `approve_campaign_art_version` e as 4 colunas em `campaigns`
    - `feature_flags` tem a linha `campaign_approval_enabled` com `enabled = false`
  </acceptance_criteria>
  <done>Schema aplicado no remoto — tabela, colunas, índice, RPC e seed confirmados via dump/query</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service_role (server) → Supabase | Escrita de dados de campanha via supabaseAdmin — camada confiável |
| anon/authenticated → tabelas novas | Sem acesso direto — somente API server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-02-01 | Elevation of Privilege | RPC approve_campaign_art_version (SECURITY DEFINER) | mitigate | `SET search_path=''` + identificadores schema-qualified (`public.`) + `REVOKE EXECUTE FROM PUBLIC, anon, authenticated` + `GRANT EXECUTE TO service_role` (padrão F43 admin_update_feature_flag) |
| T-37-02-02 | Information Disclosure | campaign_art_versions (brief_snapshot jsonb) | mitigate | `ENABLE ROW LEVEL SECURITY` + policy apenas `TO service_role` + REVOKE de anon/authenticated (padrão feature_flags) |
| T-37-02-03 | Tampering | Aprovação duplicada/concorrente | mitigate | `SELECT ... FOR UPDATE` (guarded update) + índice único parcial 1-approved por campaign_id + validações version_not_pending/version_not_active |
| T-37-02-04 | Tampering | CHECK campaigns_approved_requires_version | mitigate | Bloco idempotente `DO $$ ... IF NOT EXISTS (pg_constraint)` + CHECK `approval_status <> 'approved' OR approved_version_id IS NOT NULL` (aprovação sempre referencia versão) |
| T-37-02-05 | Tampering | Reaproveitamento de asset ativo em outra linha | mitigate | UPDATE defensivo no RPC: marca demais linhas ativas como discarded (storage_path=NULL, asset_deleted_at=now()) na mesma transação (D8) |
| T-37-02-06 | Repudiation | Trilha de aprovação | mitigate | approved_at/approved_version_id gravados na MESMA transação do RPC; funil via campaign_art_versions.status + campaigns.approved_at (sem novo generation_type — CHECK chk_generation_events_type intocado) |
| T-37-02-SC | Tampering | dependências npm | accept | Nenhuma dependência nova nesta fatia — sem instalação de pacotes |
</threat_model>

<verification>
- Grep de conteúdo das 2 migrations (Task 1 e 2 acceptance criteria) → todos os padrões presentes
- `supabase db push` aplica as migrations no remoto sem erro; dump/query confirma tabela, colunas, índice e RPC
- Seed `campaign_approval_enabled = false` presente em feature_flags
- Sem alteração em generation_events / chk_generation_events_type; sem backfill
</verification>

<success_criteria>
- Migration `20260901000001` cria campaign_art_versions (todas as colunas + UNIQUE(campaign_id, version_number)) com RLS service_role-only, colunas de aprovação em campaigns, índice único parcial 1-approved, CHECK campaigns_approved_requires_version e seed da flag false
- Migration `20260901000002` cria o RPC approve_campaign_art_version transacional (guarded update + defensivo + repontar campaigns) com service_role-only
- Schema aplicado no remoto via db push [BLOCKING] — fase não passa na verificação sem este passo (precedente F43-07/F38.2-01)
</success_criteria>

<output>
Create `.planning/phases/37.1-approval-gate-candidata-unica/37-1-02-SUMMARY.md` when done
</output>

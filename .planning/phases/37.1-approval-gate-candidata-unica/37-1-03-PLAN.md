---
phase: 37.1-approval-gate-candidata-unica
plan: 03
type: execute
wave: 2
depends_on: [37-1-02]
files_modified:
  - src/lib/feature-flags/feature-flag-service.ts
autonomous: true
requirements: [F37.1-01, F37.1-02, F37.1-03]

must_haves:
  truths:
    - "Existe a constante CAMPAIGN_APPROVAL_ENABLED_KEY = \"campaign_approval_enabled\" em feature-flag-service.ts, incluída em ALL_FEATURE_FLAG_KEYS"
    - "O método isCampaignApprovalEnabled() lê via readFlag(CAMPAIGN_APPROVAL_ENABLED_KEY, false) — fail-closed SEM envOverride; o export de conveniência isCampaignApprovalEnabled() existe"
    - "A tela admin 'Controles operacionais' e o GET /api/admin/feature-flags listam a nova flag via ALL_FEATURE_FLAG_KEYS SEM novo RPC/CHECK (verificação)"
  artifacts:
    - path: "src/lib/feature-flags/feature-flag-service.ts"
      provides: "Constante + ALL_FEATURE_FLAG_KEYS + isCampaignApprovalEnabled fail-closed"
      contains: "CAMPAIGN_APPROVAL_ENABLED_KEY"
  key_links:
    - from: "ALL_FEATURE_FLAG_KEYS"
      to: "admin/feature-flags (página e GET /api/admin/feature-flags)"
      via: "listagem automática da flag (in: ALL_FEATURE_FLAG_KEYS na rota GET)"
      pattern: "ALL_FEATURE_FLAG_KEYS"
---

<objective>
Adicionar a flag `campaign_approval_enabled` à infraestrutura `feature_flags` (D1, padrão F43/QCW, tasks.md seção 4): constante `CAMPAIGN_APPROVAL_ENABLED_KEY = "campaign_approval_enabled"`, inclusão em `ALL_FEATURE_FLAG_KEYS`, método `isCampaignApprovalEnabled(): Promise<boolean>` → `readFlag(CAMPAIGN_APPROVAL_ENABLED_KEY, false)` — **fail-closed** (falha de leitura/not-found → `false` → comportamento atual), **sem `envOverride`** (a decisão principal é a tabela, não env var) — e export de conveniência `isCampaignApprovalEnabled()`. Verificar que a tela admin "Controles operacionais" e o `GET /api/admin/feature-flags` exibem a nova flag automaticamente via `ALL_FEATURE_FLAG_KEYS`, sem novo RPC/CHECK (F43).

Purpose: D1 — a flag é a chave do experimento beta: ligada → campanhas novas entram em revisão; desligada → comportamento atual intacto. Fail-closed garante que a flag nunca derruba o fluxo de geração/entrega.
Output: Flag pronta e listada no admin (verificação), consumida pelos planos 37-1-06/07/08/09.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/lib/feature-flags/feature-flag-service.ts (padrão F43 — constantes, ALL_FEATURE_FLAG_KEYS, readFlag fail-closed, exports de conveniência)
@src/app/api/admin/feature-flags/route.ts (GET lista flags via `.in("key", [...ALL_FEATURE_FLAG_KEYS])` — sem mudança)
@src/app/(app)/admin/feature-flags/page.tsx (tela "Controles operacionais" renderiza a partir do GET)
@openspec/changes/fase-37-1-approval-gate-candidata-unica/specs/feature-flag-control/spec.md (requisitos da flag)

<interfaces>
Padrão a replicar (F43):
- `export const CAMPAIGN_APPROVAL_ENABLED_KEY = "campaign_approval_enabled";`
- Adicionar ao array `ALL_FEATURE_FLAG_KEYS` (posição canônica: após `VISUAL_SIGNATURE_GENERATION_ENABLED_KEY`).
- Método no `FeatureFlagService`: `isCampaignApprovalEnabled(): Promise<boolean> { return this.readFlag(CAMPAIGN_APPROVAL_ENABLED_KEY, false); }` — NÃO passar envOverride.
- Export de conveniência: `export async function isCampaignApprovalEnabled(): Promise<boolean> { return new FeatureFlagService().isCampaignApprovalEnabled(); }`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Constante + ALL_FEATURE_FLAG_KEYS + método isCampaignApprovalEnabled + export de conveniência</name>
  <files>src/lib/feature-flags/feature-flag-service.ts</files>
  <read_first>
    - src/lib/feature-flags/feature-flag-service.ts (estrutura atual completa — constantes, ALL_FEATURE_FLAG_KEYS, readFlag, métodos por flag, exports)
  </read_first>
  <action>
    Editar `src/lib/feature-flags/feature-flag-service.ts`:
    1. Adicionar a constante `export const CAMPAIGN_APPROVAL_ENABLED_KEY = "campaign_approval_enabled";` junto às demais constantes de flag (após `VISUAL_SIGNATURE_GENERATION_ENABLED_KEY`).
    2. Adicionar `CAMPAIGN_APPROVAL_ENABLED_KEY` ao array `ALL_FEATURE_FLAG_KEYS` (após `VISUAL_SIGNATURE_GENERATION_ENABLED_KEY` — ordem canônica de exibição).
    3. Adicionar o método `isCampaignApprovalEnabled(): Promise<boolean>` à classe `FeatureFlagService` chamando `this.readFlag(CAMPAIGN_APPROVAL_ENABLED_KEY, false)` — **sem envOverride** (NÃO usar `FORCE_ENV` nem criar env var nova; comentário JSDoc: fail-closed, falha de leitura/not-found → false → comportamento atual; sem env/launch-config para a flag principal — decisão 1).
    4. Adicionar o export de conveniência `export async function isCampaignApprovalEnabled(): Promise<boolean> { return new FeatureFlagService().isCampaignApprovalEnabled(); }` ao final do arquivo, junto aos demais exports de conveniência.
    NÃO alterar nenhum outro método, constante ou o comportamento de flags existentes.
  </action>
  <verify>
    <automated>npx vitest run src/lib/feature-flags/__tests__/feature-flag-service.test.ts 2>&1 | Select-Object -Last 5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CAMPAIGN_APPROVAL_ENABLED_KEY" src/lib/feature-flags/feature-flag-service.ts` ≥ 3 (declaração, ALL_FEATURE_FLAG_KEYS, método; export adicional se contado)
    - `grep -n "ALL_FEATURE_FLAG_KEYS = \[" src/lib/feature-flags/feature-flag-service.ts` → array contém `CAMPAIGN_APPROVAL_ENABLED_KEY`
    - `grep -n "readFlag(CAMPAIGN_APPROVAL_ENABLED_KEY, false)" src/lib/feature-flags/feature-flag-service.ts` → método usa fallback `false` e NÃO há envOverride no argumento (assinatura `readFlag(key, false)` com 2 args)
    - `grep -n "export async function isCampaignApprovalEnabled" src/lib/feature-flags/feature-flag-service.ts` → export de conveniência presente
    - `npx vitest run src/lib/feature-flags/__tests__/feature-flag-service.test.ts` passa (testes existentes continuam verdes)
  </acceptance_criteria>
  <done>Flag adicionada ao serviço com fail-closed e export de conveniência; testes existentes da flag verdes</done>
</task>

<task type="auto">
  <name>Task 2: Verificação admin — nova flag listada sem novo RPC/CHECK (F37.1-03)</name>
  <files>src/lib/feature-flags/feature-flag-service.ts</files>
  <read_first>
    - src/app/api/admin/feature-flags/route.ts (GET — `.in("key", [...ALL_FEATURE_FLAG_KEYS])`; PUT — RPC genérico admin_update_feature_flag com motivo obrigatório)
    - src/app/(app)/admin/feature-flags/page.tsx (tela "Controles operacionais")
  </read_first>
  <action>
    Verificação-only (NENHUMA edição em admin/feature-flags — F37.1-03):
    1. Confirmar por leitura de código que a rota `GET /api/admin/feature-flags` filtra por `[...ALL_FEATURE_FLAG_KEYS]` (a nova key entra automaticamente) e que o `PUT` usa o RPC genérico `admin_update_feature_flag` por `key` — nenhuma mudança de RPC/CHECK necessária; os CHECKs `feature_flag_update`/`feature_flag` e a action de auditoria já existem (F43).
    2. Confirmar que a tela "Controles operacionais" (`admin/feature-flags/page.tsx`) renderiza as flags retornadas pelo GET (exibição automática da nova flag com descrição e estado).
    3. Rodar os testes existentes de admin feature-flags para confirmar que nada quebra com a nova key no array: `npx vitest run src/app/api/admin/feature-flags/__tests__/route.test.ts`. Se o teste de lista assertar o conjunto exato de flags, co-migrar o assert para incluir `campaign_approval_enabled` (única mudança permitida aqui; demais co-migrações de fixtures ficam no plano 37-1-14).
  </action>
  <verify>
    <automated>npx vitest run src/app/api/admin/feature-flags/__tests__/route.test.ts 2>&1 | Select-Object -Last 5</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/app/api/admin/feature-flags/__tests__/route.test.ts` passa (após co-migração mínima do assert de lista, se necessário)
    - Nenhuma alteração em `src/app/api/admin/feature-flags/route.ts` nem em `src/app/(app)/admin/feature-flags/page.tsx`
    - Nenhuma nova action/target_type em admin_audit_log (F43 já cobre feature_flag_update/feature_flag)
  </acceptance_criteria>
  <done>Flag listada na tela "Controles operacionais" e no GET /api/admin/feature-flags sem novo RPC/CHECK; testes admin verdes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server (supabaseAdmin) → feature_flags | Leitura/escrita de flags — camada confiável (service_role) |
| admin UI → API | Mutação com motivo obrigatório + auditoria (F43) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-03-01 | Spoofing | isCampaignApprovalEnabled | mitigate | `readFlag(CAMPAIGN_APPROVAL_ENABLED_KEY, false)` fail-closed SEM envOverride — falha de leitura/not-found retorna false (comportamento atual); a flag nunca habilita o fluxo por acidente |
| T-37-03-02 | Tampering | ALL_FEATURE_FLAG_KEYS (admin) | mitigate | Ordem canônica única no serviço; rota GET filtra `.in("key", [...ALL_FEATURE_FLAG_KEYS])`; mutação via RPC genérico `admin_update_feature_flag` com motivo obrigatório + auditoria atômica (F43) |
| T-37-03-03 | DoS | Leitura de flag no hot path | mitigate | Leitura única por request (maybeSingle); falha → console.warn + fallback false; sem bloqueio de fluxo (D1) |
| T-37-03-SC | Tampering | dependências npm | accept | Nenhuma dependência nova — sem instalação de pacotes |
</threat_model>

<verification>
- Grep confirma constante + ALL_FEATURE_FLAG_KEYS + readFlag(key, false) sem envOverride + export de conveniência
- `npx vitest run src/lib/feature-flags/__tests__/feature-flag-service.test.ts` e `src/app/api/admin/feature-flags/__tests__/route.test.ts` passam
- Admin exibe a flag automaticamente (sem edição em admin/feature-flags)
</verification>

<success_criteria>
- Flag `campaign_approval_enabled` operacional (fail-closed, seed false da migration 37-1-02) e listada em "Controles operacionais" sem novo RPC/CHECK
- `isCampaignApprovalEnabled()` retorna `false` em falha/not-found/flag desligada — comportamento atual preservado
- Nenhuma mudança fora de `feature-flag-service.ts` (e co-migração mínima de assert de teste, se necessário)
</success_criteria>

<output>
Create `.planning/phases/37.1-approval-gate-candidata-unica/37-1-03-SUMMARY.md` when done
</output>

---
phase: 37.1-approval-gate-candidata-unica
plan: 04
type: execute
wave: 2
depends_on: [37-1-02]
files_modified:
  - src/lib/campaign/types.ts
  - src/lib/campaign/persistence.ts
autonomous: true
requirements: [F37.1-09, F37.1-10]

must_haves:
  truths:
    - "src/lib/campaign/types.ts exporta CampaignApprovalStatus, ArtVersionStatus e a interface CampaignArtVersion com todos os campos do spec; CampaignRecord estendido com approval_status, rejection_count, approved_version_id, approved_at"
    - "src/lib/campaign/persistence.ts provê createArtVersion(campaignId, versionNumber, storagePath, briefSnapshot) — INSERT com status='pending', asset_status='active'"
    - "src/lib/campaign/persistence.ts provê listArtVersions(campaignId) — linhas ordenadas por version_number; campanha sem linhas → [] (estado legacy)"
    - "NENHUMA função de correção criada (markVersionRejected/discardArtAsset/setCorrectionInProgress) — 37.2"
  artifacts:
    - path: "src/lib/campaign/types.ts"
      provides: "Tipos de domínio das versões de arte + extensão de CampaignRecord"
      contains: "CampaignArtVersion"
    - path: "src/lib/campaign/persistence.ts"
      provides: "createArtVersion + listArtVersions"
      contains: "listArtVersions"
  key_links:
    - from: "types.ts (CampaignArtVersion)"
      to: "persistence.ts (createArtVersion/listArtVersions)"
      via: "tipagem das linhas de campaign_art_versions"
      pattern: "CampaignArtVersion"
    - from: "persistence.ts (createArtVersion)"
      to: "tabela campaign_art_versions"
      via: "INSERT status='pending', asset_status='active'"
      pattern: "campaign_art_versions"
---

<objective>
Estender o domínio de campanha (tasks.md seções 5 e 6, D5/D7): criar os tipos `CampaignApprovalStatus`, `ArtVersionStatus` e a interface `CampaignArtVersion` em `src/lib/campaign/types.ts`, estender `CampaignRecord` com `approval_status`, `rejection_count`, `approved_version_id`, `approved_at`; e adicionar em `src/lib/campaign/persistence.ts` as funções `createArtVersion(campaignId, versionNumber, storagePath, briefSnapshot)` e `listArtVersions(campaignId)`.

Purpose: Contrato de dados (interface-first) que os planos 37-1-05 (display), 37-1-06 (generate-image v1), 37-1-07 (gates) e 37-1-08/09 (rota approve/UI) consomem sem explorar o código.
Output: Tipos e persistência das versões de arte prontos.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/lib/campaign/types.ts (estrutura atual — CampaignRecord, CampaignIntent, CampaignStatus, CreateCampaignInput)
@src/lib/campaign/persistence.ts (padrão das funções — supabaseAdmin.from().insert()/select() com throw em error)
@openspec/changes/fase-37-1-approval-gate-candidata-unica/design.md (D5 — bloco de tipos e assinaturas)
@openspec/changes/fase-37-1-approval-gate-candidata-unica/specs/campaign-art-versions/spec.md (requisitos "Tipos de domínio" e "Persistência de versões")

<interfaces>
Contrato de tipos (spec campaign-art-versions):
```ts
export type CampaignApprovalStatus = "pending_approval" | "approved";
export type ArtVersionStatus = "pending" | "approved" | "rejected";
export interface CampaignArtVersion {
  id: string;
  campaign_id: string;
  version_number: number;                 // 1..3
  status: ArtVersionStatus;
  storage_path: string | null;            // NULL após descarte do asset
  asset_status: "active" | "discarded";
  asset_deleted_at: string | null;
  brief_snapshot: Record<string, unknown>; // campaign_brief_v1 (F39), sem base64
  render_snapshot: Record<string, unknown> | null;
  generation_metadata: Record<string, unknown> | null;
  rejection_reason: Record<string, unknown> | null;
  correction_in_progress: boolean;        // decisão 5 — inalcançável na 37.1
  created_at: string;
}
```
`CampaignRecord` += `approval_status: CampaignApprovalStatus; rejection_count: number; approved_version_id: string | null; approved_at: string | null;`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types — CampaignApprovalStatus, ArtVersionStatus, CampaignArtVersion, extensão de CampaignRecord</name>
  <files>src/lib/campaign/types.ts</files>
  <read_first>
    - src/lib/campaign/types.ts (estrutura atual completa)
    - openspec/changes/fase-37-1-approval-gate-candidata-unica/design.md (D5 — bloco de tipos exato)
  </read_first>
  <action>
    Editar `src/lib/campaign/types.ts`:
    1. Adicionar `export type CampaignApprovalStatus = "pending_approval" | "approved";` e `export type ArtVersionStatus = "pending" | "approved" | "rejected";` (junto aos demais tipos de domínio).
    2. Adicionar a interface `export interface CampaignArtVersion` com TODOS os campos do contrato `<interfaces>` (id, campaign_id, version_number, status, storage_path nullable, asset_status "active"|"discarded", asset_deleted_at nullable, brief_snapshot Record<string, unknown>, render_snapshot nullable, generation_metadata nullable, rejection_reason nullable, correction_in_progress boolean, created_at string).
    3. Estender `CampaignRecord` com os 4 campos obrigatórios: `approval_status: CampaignApprovalStatus`, `rejection_count: number`, `approved_version_id: string | null`, `approved_at: string | null`.
    4. NÃO criar tipos de correção (`RejectionReason`, `ArtCorrectionStrategy`, `BriefPatch`, `CorrectionIntent`) — são 37.2/37.3 (tasks.md 5.3).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | Select-Object -Last 8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export type CampaignApprovalStatus" src/lib/campaign/types.ts` e `grep -n "export type ArtVersionStatus" src/lib/campaign/types.ts` → presentes
    - `grep -n "export interface CampaignArtVersion" src/lib/campaign/types.ts` → presente com os 13 campos do contrato (contar `correction_in_progress`, `asset_status`, `brief_snapshot`, `rejection_reason` entre os membros)
    - `grep -n "approval_status" src/lib/campaign/types.ts` → dentro de `CampaignRecord` (4 campos: approval_status, rejection_count, approved_version_id, approved_at)
    - `grep -n "RejectionReason\|ArtCorrectionStrategy\|BriefPatch\|CorrectionIntent" src/lib/campaign/types.ts` → ZERO ocorrências
    - `npx tsc --noEmit` — sem erros de tipo nesta etapa (evidência declarada da task)
    - Os testes `display.test.ts`/`persistence.test.ts` podem falhar nesta etapa SE fixtures antigas construírem CampaignRecord sem os 4 campos — a co-migração de fixtures é do plano 37-1-14; registrar falhas esperadas no SUMMARY (typecheck é a evidência desta task, ver verify)
  </acceptance_criteria>
  <done>Tipos de domínio criados e CampaignRecord estendido; nenhum tipo de correção criado</done>
</task>

<task type="auto">
  <name>Task 2: Persistência — createArtVersion + listArtVersions</name>
  <files>src/lib/campaign/persistence.ts</files>
  <read_first>
    - src/lib/campaign/persistence.ts (padrão supabaseAdmin.from().insert()/select() com `if (error) throw new Error(error.message)`)
    - openspec/changes/fase-37-1-approval-gate-candidata-unica/design.md (D5 — assinaturas e comportamento)
  </read_first>
  <action>
    Editar `src/lib/campaign/persistence.ts` (extensões, sem tocar funções existentes):
    1. Importar `CampaignArtVersion` de `./types` (adicionar ao import existente).
    2. Adicionar:
       - `export async function createArtVersion(campaignId: string, versionNumber: number, storagePath: string | null, briefSnapshot: Record<string, unknown>): Promise<void>` — `supabaseAdmin.from("campaign_art_versions").insert({ campaign_id: campaignId, version_number: versionNumber, status: "pending", asset_status: "active", storage_path: storagePath, brief_snapshot: briefSnapshot })`; `if (error) throw new Error(error.message)`. Comentário: v1 nasce pending/active (candidata); render_snapshot/generation_metadata/rejection_reason ficam NULL na 37.1 (D4).
       - `export async function listArtVersions(campaignId: string): Promise<CampaignArtVersion[]>` — `supabaseAdmin.from("campaign_art_versions").select("*").eq("campaign_id", campaignId).order("version_number", { ascending: true })`; `if (error) throw new Error(error.message)`; retornar `(data ?? []) as CampaignArtVersion[]` (lista vazia → estado legacy).
    3. NÃO criar `markVersionRejected`/`discardArtAsset`/`setCorrectionInProgress` (37.2, tasks.md 6.3).
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\lib\campaign\persistence.ts" -Pattern "export async function createArtVersion|export async function listArtVersions|status: \"pending\"|asset_status: \"active\"|order\(\"version_number\"" | Select-Object -First 6; npx vitest run src/__tests__/lib/campaign/persistence.test.ts 2>&1 | Select-Object -Last 5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export async function createArtVersion" src/lib/campaign/persistence.ts` → presente com assinatura de 4 parâmetros (campaignId, versionNumber, storagePath, briefSnapshot) e insert com `status: "pending"` e `asset_status: "active"`
    - `grep -n "export async function listArtVersions" src/lib/campaign/persistence.ts` → presente, com `order("version_number", { ascending: true })` e retorno tipado `CampaignArtVersion[]`
    - `grep -n "markVersionRejected\|discardArtAsset\|setCorrectionInProgress" src/lib/campaign/persistence.ts` → ZERO ocorrências
    - `npx vitest run src/__tests__/lib/campaign/persistence.test.ts` — testes de createCampaign/getCampaign/upload existentes continuam passando (novos testes das versões ficam no plano 37-1-10)
  </acceptance_criteria>
  <done>createArtVersion e listArtVersions implementadas; funções de correção NÃO criadas</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server (supabaseAdmin) → campaign_art_versions | Escrita/leitura via service_role — camada confiável |
| anon/authenticated → tabela | Sem acesso direto — RLS service_role-only (migration 37-1-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-04-01 | Information Disclosure | brief_snapshot (jsonb) nas versões | mitigate | Acesso exclusivamente via supabaseAdmin (service_role); tabela sem policy para anon/authenticated (RLS na migration) |
| T-37-04-02 | Tampering | createArtVersion (status/asset_status) | mitigate | INSERT com literais fixos `status: "pending"`/`asset_status: "active"` (sem input externo); storage_path/brief_snapshot vindos de valores já persistidos pela geração |
| T-37-04-03 | Integrity | listArtVersions (ordem/fonte) | mitigate | Ordenação determinística por `version_number` (fonte única para computeApprovalState e tela — decisão 3) |
| T-37-04-04 | Elevation of Privilege | Tipos de correção (37.2) | accept | NÃO criados nesta fatia (tasks.md 5.3/6.3) — ausência por construção elimina uso acidental |
| T-37-04-SC | Tampering | dependências npm | accept | Nenhuma dependência nova — sem instalação de pacotes |
</threat_model>

<verification>
- Grep confirma tipos (CampaignApprovalStatus, ArtVersionStatus, CampaignArtVersion, 4 campos em CampaignRecord) e ausência de tipos de correção
- Grep confirma createArtVersion (insert pending/active) e listArtVersions (order version_number); ausência de markVersionRejected/discardArtAsset/setCorrectionInProgress
- `npx vitest run src/__tests__/lib/campaign/persistence.test.ts` — testes existentes verdes
</verification>

<success_criteria>
- Contrato de tipos pronto (interface-first) para display/generate-image/gates/rota approve/UI
- Persistência funcional: createArtVersion (candidata pending/active) + listArtVersions (ordenada, [] para legacy)
- Nenhuma função de correção criada (escopo 37.2 respeitado)
</success_criteria>

<output>
Create `.planning/phases/37.1-approval-gate-candidata-unica/37-1-04-SUMMARY.md` when done
</output>

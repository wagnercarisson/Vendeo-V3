---
phase: 37.1-approval-gate-candidata-unica
plan: 05
type: execute
wave: 3
depends_on: [37-1-04]
files_modified:
  - src/lib/campaign/display.ts
autonomous: true
requirements: [F37.1-11, F37.1-12, F37.1-13, F37.1-14]

must_haves:
  truths:
    - "display.ts exporta ApprovalDisplayState (not_enabled | legacy | pending | approved(approvedAt) | regenerating), computeApprovalState(campaign, versions, flagEnabled) e isDeliveryReleased(state)"
    - "computeApprovalState deriva na ordem: !flagEnabled → not_enabled; flag + zero versões → legacy; approved_version_id → approved(+approvedAt); candidata ativa com correction_in_progress → regenerating (inalcançável na 37.1); senão pending"
    - "isDeliveryReleased retorna true para not_enabled|legacy|approved e false para pending|regenerating"
    - "Existe helper da candidata ativa (asset_status='active') e a interface CampaignPageProps ganha approval?: { state, candidateImageUrl?, candidateVersionId? } (contrato de tipo para a UI)"
  artifacts:
    - path: "src/lib/campaign/display.ts"
      provides: "Estado de aprovação + gating + candidata ativa + contrato de props da UI"
      contains: "computeApprovalState"
  key_links:
    - from: "display.ts (computeApprovalState)"
      to: "types.ts (CampaignRecord estendido + CampaignArtVersion)"
      via: "entrada campaign: CampaignRecord e versions: CampaignArtVersion[]"
      pattern: "CampaignArtVersion"
    - from: "display.ts (isDeliveryReleased)"
      to: "rotas download/publication-copy (37-1-07)"
      via: "gate !isDeliveryReleased(state) → 403"
      pattern: "isDeliveryReleased"
---

<objective>
Implementar o estado de aprovação e o gating (tasks.md seção 7, D2 + decisões 3/5) em `src/lib/campaign/display.ts`: tipo `ApprovalDisplayState`, funções puras `computeApprovalState` e `isDeliveryReleased`, helper da **candidata ativa** (`asset_status='active'` — decisão 3, fonte oficial da arte da revisão) e extensão de `CampaignPageProps` com o contrato `approval` (interface-first para o plano de UI 37-1-09).

Purpose: Núcleo lógico da fatia — os gates (37-1-07), a rota approve (37-1-08) e a página (37-1-09) derivam TODOS o estado daqui (single source).
Output: Módulo de estado de aprovação pronto e puro (testável sem banco).
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/lib/campaign/display.ts (estrutura atual — CampaignPageProps, getCampaignForDisplay, generateSignedPreviewUrl, computeDisplayStatus, mapCampaignToProps)
@src/lib/campaign/types.ts (pós-37-1-04: CampaignRecord com 4 campos de aprovação + CampaignArtVersion)
@openspec/changes/fase-37-1-approval-gate-candidata-unica/design.md (D2 — tipo, derivação e isDeliveryReleased)
@openspec/changes/fase-37-1-approval-gate-candidata-unica/specs/campaign-approval-gate/spec.md (requisitos do estado e cenários)

<interfaces>
Contrato exato (design.md D2):
```ts
export type ApprovalDisplayState =
  | { status: "not_enabled" }            // flag off → comportamento atual (entrega livre)
  | { status: "legacy" }                 // flag on, zero linhas em campaign_art_versions → entregue como hoje
  | { status: "pending" }                // flag on, campanha nova não aprovada → revisão (gate)
  | { status: "approved"; approvedAt: string }
  | { status: "regenerating" };          // derivado do marcador correction_in_progress (decisão 5) — inalcançável na 37.1

export function computeApprovalState(campaign: CampaignRecord, versions: CampaignArtVersion[], flagEnabled: boolean): ApprovalDisplayState;
export function isDeliveryReleased(state: ApprovalDisplayState): boolean; // true: not_enabled|legacy|approved; false: pending|regenerating
```
Derivação (ordem): `!flagEnabled → not_enabled`; `flagEnabled && versions.length === 0 → legacy`; `flagEnabled && campaign.approved_version_id → approved` (approvedAt = campaign.approved_at ?? ""); `flagEnabled && candidata ativa (asset_status='active') com correction_in_progress → regenerating`; senão `pending`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ApprovalDisplayState + computeApprovalState + isDeliveryReleased</name>
  <files>src/lib/campaign/display.ts</files>
  <read_first>
    - src/lib/campaign/display.ts (estrutura atual — importar CampaignArtVersion de ./types no import existente)
    - src/lib/campaign/types.ts (CampaignRecord estendido pós-37-1-04)
  </read_first>
  <action>
    Editar `src/lib/campaign/display.ts`:
    1. Atualizar o import de `./types` para incluir `CampaignArtVersion` (além de `CampaignRecord`, `CampaignStatus` existentes).
    2. Adicionar o tipo `export type ApprovalDisplayState` com as 5 variantes EXATAS do contrato `<interfaces>`.
    3. Adicionar `export function computeApprovalState(campaign: CampaignRecord, versions: CampaignArtVersion[], flagEnabled: boolean): ApprovalDisplayState` seguindo a ordem de derivação: `!flagEnabled → { status: "not_enabled" }`; `versions.length === 0 → { status: "legacy" }`; `campaign.approved_version_id != null → { status: "approved", approvedAt: campaign.approved_at ?? "" }`; candidata ativa (primeira com `asset_status === "active"`) com `correction_in_progress === true` → `{ status: "regenerating" }` (inalcançável na 37.1 — nenhum fluxo escreve true; contrato reservado); senão `{ status: "pending" }`. NÃO derivar de `campaign.status` (permanece `ready`, decisão 5).
    4. Adicionar `export function isDeliveryReleased(state: ApprovalDisplayState): boolean` retornando `true` para `not_enabled | legacy | approved` e `false` para `pending | regenerating` (implementação por switch/if explícito).
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\lib\campaign\display.ts" -Pattern "export type ApprovalDisplayState|export function computeApprovalState|export function isDeliveryReleased" | Select-Object -First 5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export type ApprovalDisplayState" src/lib/campaign/display.ts` → presente com as 5 variantes (`not_enabled`, `legacy`, `pending`, `approved`, `regenerating`)
    - `grep -n "export function computeApprovalState" src/lib/campaign/display.ts` → presente com assinatura (campaign: CampaignRecord, versions: CampaignArtVersion[], flagEnabled: boolean)
    - `grep -n "export function isDeliveryReleased" src/lib/campaign/display.ts` → presente
    - A implementação NÃO referencia `campaign.status` na derivação (decisão 5 — campaigns.status permanece ready)
    - `npx tsc --noEmit` — sem erro de tipo (fixtures de teste podem quebrar por CampaignRecord estendido; a co-migração é do plano 37-1-14 — evidenciar no SUMMARY)
  </acceptance_criteria>
  <done>Estado de aprovação + gating implementados conforme D2; contrato inclui regenerating (inalcançável)</done>
</task>

<task type="auto">
  <name>Task 2: Helper da candidata ativa + extensão de CampaignPageProps com approval (contrato da UI)</name>
  <files>src/lib/campaign/display.ts</files>
  <read_first>
    - src/lib/campaign/display.ts (interface CampaignPageProps atual, linhas 8-21)
    - openspec/changes/fase-37-1-approval-gate-candidata-unica/specs/campaign-approval-gate/spec.md (requisito "Fonte oficial da arte exibida" — decisão 3)
  </read_first>
  <action>
    Editar `src/lib/campaign/display.ts` (continuação):
    1. Adicionar o helper da **candidata ativa** (decisão 3 — fonte oficial da arte da revisão): `export function getActiveCandidateArtVersion(versions: CampaignArtVersion[]): CampaignArtVersion | null` — retorna a primeira linha com `asset_status === "active"`, senão `null` (legado: `campaigns.storage_path`; aprovada: `campaigns.storage_path` repontado no approve — D8).
    2. Estender `export interface CampaignPageProps` com `approval?: { state: ApprovalDisplayState; candidateImageUrl?: string | null; candidateVersionId?: string | null }` (contrato de tipo consumido pelo client.tsx e pelo CampaignApprovalView — import type-only em componentes client é permitido, precedente do import atual de CampaignPageProps no client.tsx).
    3. NÃO alterar `mapCampaignToProps` (a derivação de approval acontece na página — plano 37-1-09).
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\lib\campaign\display.ts" -Pattern "export function getActiveCandidateArtVersion|approval\?:|candidateImageUrl|candidateVersionId" | Select-Object -First 6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function getActiveCandidateArtVersion" src/lib/campaign/display.ts` → presente, retornando `CampaignArtVersion | null` com filtro `asset_status === "active"`
    - `grep -n "approval\?:" src/lib/campaign/display.ts` → presente em `CampaignPageProps` com `state: ApprovalDisplayState`, `candidateImageUrl?: string | null`, `candidateVersionId?: string | null`
    - `mapCampaignToProps` inalterado (sem derivação de approval)
  </acceptance_criteria>
  <done>Candidata ativa localizável e contrato approval definido para a UI (37-1-09)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| display.ts (server) → rotas/UI | Estado de aprovação derivado — camada confiável |
| estados derivados → gates | Entrega liberada/não liberada |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-05-01 | DoS | Gate quebra download/copy | mitigate | `isDeliveryReleased` fail-closed: true para not_enabled/legacy/approved (entrega preservada); apenas pending/regenerating bloqueiam (D2) |
| T-37-05-02 | Tampering | Estado derivado (approved sem approved_version_id) | mitigate | Derivação exige `campaign.approved_version_id != null` para approved; CHECK `campaigns_approved_requires_version` no banco (migration 37-1-02) reforça o invariante |
| T-37-05-03 | Tampering | legacy derivado errado (campanha nova sem v1) | mitigate | legacy exige `versions.length === 0` com flag on — campanhas novas sob flag sempre têm v1 (generate-image 37-1-06); falha no insert → legacy (fail-safe D1) |
| T-37-05-04 | Tampering | regenerating exercitado na 37.1 | mitigate | Derivado apenas de `correction_in_progress === true`; NENHUM fluxo/UI/rota da 37.1 escreve true (correção é 37.2) — contrato reservado, mas testes de contrato puro são permitidos em `computeApprovalState`/`isDeliveryReleased` (decisão do usuário 2026-09-01; plano 37-1-10) |
| T-37-05-SC | Tampering | dependências npm | accept | Nenhuma dependência nova — sem instalação de pacotes |
</threat_model>

<verification>
- Grep confirma ApprovalDisplayState (5 variantes), computeApprovalState, isDeliveryReleased, getActiveCandidateArtVersion e approval? em CampaignPageProps
- Derivação sem referência a `campaign.status` (decisão 5)
- `npx tsc --noEmit` sem erro novo de tipo (fixtures co-migradas em 37-1-14)
</verification>

<success_criteria>
- Estado de aprovação derivado corretamente nos 5 estados (not_enabled/legacy/pending/approved/regenerating)
- isDeliveryReleased correto (fail-closed) — base dos gates de download/copy
- Candidata ativa identificável (decisão 3) e contrato approval disponível para a UI
</success_criteria>

<output>
Create `.planning/phases/37.1-approval-gate-candidata-unica/37-1-05-SUMMARY.md` when done
</output>

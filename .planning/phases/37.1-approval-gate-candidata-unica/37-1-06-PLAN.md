---
phase: 37.1-approval-gate-candidata-unica
plan: 06
type: execute
wave: 3
depends_on: [37-1-02, 37-1-03, 37-1-04]
files_modified:
  - src/app/api/campaign/generate-image/route.ts
autonomous: true
requirements: [F37.1-15]

must_haves:
  truths:
    - "POST /api/campaign/generate-image, após o sucesso de createCampaign (pré-stream), lê isCampaignApprovalEnabled(); se true, chama createArtVersion(campaign.id, 1, campaign.storagePath, inputSnapshot) — inputSnapshot é EXATAMENTE o objeto campaign_brief_v1 persistido em campaigns.input_snapshot"
    - "Falha no insert da v1 com flag ligada → log de erro operacional + a geração CONTINUA (fail-safe D1); a campanha nasce sem versões e é exibida como legacy"
    - "Flag off → NENHUMA inserção em campaign_art_versions (comportamento atual inalterado); nenhuma persistência nova de produto fonte (F41 já persiste inputs)"
  artifacts:
    - path: "src/app/api/campaign/generate-image/route.ts"
      provides: "Inserção da v1 (candidata pending/active) quando a flag está ligada"
      contains: "createArtVersion"
  key_links:
    - from: "generate-image/route.ts (inputSnapshot :452)"
      to: "createArtVersion(campaign.id, 1, campaign.storagePath, inputSnapshot)"
      via: "mesmo objeto campaign_brief_v1 persistido em campaigns.input_snapshot (sem base64, F39)"
      pattern: "createArtVersion"
    - from: "generate-image/route.ts"
      to: "isCampaignApprovalEnabled()"
      via: "leitura fail-closed da flag pós-createCampaign"
      pattern: "isCampaignApprovalEnabled"
---

<objective>
Mudança mínima no pipeline (tasks.md seção 8, D8/D10): no `POST /api/campaign/generate-image`, APÓS o sucesso de `createCampaign` (pré-stream), ler `isCampaignApprovalEnabled()`; se `true`, inserir a **v1** em `campaign_art_versions` via `createArtVersion(campaign.id, 1, campaign.storagePath, inputSnapshot)` — candidata `pending`/`active` com `brief_snapshot` = o objeto `campaign_brief_v1` persistido. **Fail-safe:** falha no insert → log + continua (campanha legacy, a flag nunca derruba a geração — D1). Flag off → comportamento atual.

Purpose: D8/D10 — a candidata v1 nasce junto com a campanha para que a revisão (37-1-09) e os gates (37-1-07) tenham o que exibir/bloquear.
Output: generate-image prepara a candidata quando o experimento está ativo; core de geração intocado (providers/prompts/créditos).
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/app/api/campaign/generate-image/route.ts (linhas 429-473 — bloco pré-stream: upload inputs → inputSnapshot :452 → createCampaign :455-460 → logPipelineEvent complete :463)
@src/lib/campaign/persistence.ts (pós-37-1-04: createArtVersion)
@src/lib/feature-flags/feature-flag-service.ts (pós-37-1-03: isCampaignApprovalEnabled fail-closed)
@openspec/changes/fase-37-1-approval-gate-candidata-unica/design.md (D4 — ponto de inserção e fail-safe)
@openspec/changes/fase-37-1-approval-gate-candidata-unica/specs/ai-image-generation/spec.md (requisito e cenários flag on/off/fail)

<interfaces>
Ponto de inserção exato (route.ts atual):
- `:452` — `const inputSnapshot: Record<string, unknown> = buildCampaignBriefSnapshot(brief) as unknown as Record<string, unknown>;` (mesmo objeto gravado em campaigns.input_snapshot)
- `:455-460` — `const campaign = await createCampaign(storeId, { productName, inputSnapshot, identitySnapshot, operationRunId }, campaignIdPre);`
- `:463` — `logPipelineEvent({ event: "campaign_create", ..., status: "complete", campaignId: campaign.id, ... });`
- A inserção da v1 deve ocorrer IMEDIATAMENTE após `:463` (campaign criado, storagePath disponível), DENTRO do try pré-stream mas em try/catch PRÓPRIO (fail-safe).
- Imports necessários: `createArtVersion` de `@/lib/campaign/persistence` (ou do barrel existente) e `isCampaignApprovalEnabled` de `@/lib/feature-flags/feature-flag-service`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inserção da v1 (flag on) após createCampaign com fail-safe</name>
  <files>src/app/api/campaign/generate-image/route.ts</files>
  <read_first>
    - src/app/api/campaign/generate-image/route.ts (imports no topo; bloco pré-stream :429-473)
    - src/lib/campaign/persistence.ts (assinatura de createArtVersion)
    - src/lib/feature-flags/feature-flag-service.ts (export isCampaignApprovalEnabled)
  </read_first>
  <action>
    Editar `src/app/api/campaign/generate-image/route.ts`:
    1. Adicionar os imports: `createArtVersion` de `@/lib/campaign/persistence` (junto aos imports de persistência existentes) e `isCampaignApprovalEnabled` de `@/lib/feature-flags/feature-flag-service`.
    2. Imediatamente após o `logPipelineEvent({ event: "campaign_create", ..., status: "complete", campaignId: campaign.id, ... })` (:463), adicionar um bloco de inserção da v1 com fail-safe PRÓPRIO (D4/D1):
       ```ts
       // F37.1 (D8/D10): quando a flag está ligada, insere a v1 (candidata pending/active)
       // com brief_snapshot = campaign_brief_v1 persistido. Fail-safe: falha no insert →
       // log + continua; a campanha nasce sem versões e é exibida como legacy (D1).
       try {
         if (await isCampaignApprovalEnabled()) {
           await createArtVersion(campaign.id, 1, campaign.storagePath, inputSnapshot);
         }
       } catch (err) {
         console.error(`[generate-image] createArtVersion v1 failed (fail-safe) — ${err instanceof Error ? err.message : String(err)}`);
       }
       ```
       — `campaign.storagePath` reaproveita o path da geração inicial (`{storeId}/{campaignId}.jpg`; convenção `v{n}.jpg` é da regeração 37.2); `render_snapshot`/`generation_metadata`/`rejection_reason` ficam NULL (defaults).
    3. NÃO alterar mais nada no fluxo: providers, prompts, ImageGenerationService, créditos, inputSnapshot, upload de inputs — intocados. NÃO criar persistência nova de produto fonte (decisão 2).
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\app\api\campaign\generate-image\route.ts" -Pattern "createArtVersion|isCampaignApprovalEnabled|fail-safe" | Select-Object -First 6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "createArtVersion" src/app/api/campaign/generate-image/route.ts` → import + chamada presentes (chamada com `campaign.id, 1, campaign.storagePath, inputSnapshot`)
    - `grep -n "isCampaignApprovalEnabled" src/app/api/campaign/generate-image/route.ts` → import + leitura no bloco pós-createCampaign
    - A chamada está DENTRO de try/catch que apenas loga (não propaga erro para o fluxo de geração)
    - Nenhuma alteração nas seções de providers/prompts/créditos/stream (grep diff mostra apenas o bloco novo + imports)
    - `npx tsc --noEmit` sem erro de tipo novo
  </acceptance_criteria>
  <done>generate-image insere a v1 (pending/active, brief_snapshot do snapshot persistido) quando a flag está ligada, com fail-safe log+continua</done>
</task>

<task type="auto">
  <name>Task 2: Confirmar flag off → nenhuma inserção (comportamento atual) + regressão da rota existente</name>
  <files>src/app/api/campaign/generate-image/route.ts</files>
  <read_first>
    - src/app/api/campaign/generate-image/__tests__/route.test.ts (mock atual do feature-flag-service, ~linha 97: `vi.mock('@/lib/feature-flags/feature-flag-service', ...)`)
  </read_first>
  <action>
    Verificação da preservação do comportamento (D1/D8):
    1. Confirmar por leitura que a inserção ocorre APENAS dentro do `if (await isCampaignApprovalEnabled())` — flag off → o bloco try não insere nada (zero linhas em campaign_art_versions).
    2. Rodar a suíte existente da rota: `npx vitest run src/app/api/campaign/generate-image/__tests__/route.test.ts`. Se o mock de `@/lib/feature-flags/feature-flag-service` não expuser `isCampaignApprovalEnabled`, o mock pode lançar erro de "not a function" — NESTE CASO, co-migrar o mock adicionando `isCampaignApprovalEnabled: vi.fn().mockResolvedValue(false)` (flag off default) como única mudança permitida nesta task (os testes NOVOS de flag on ficam no plano 37-1-12).
    3. Se qualquer teste existente quebrar por outro motivo, NÃO corrigir aqui — registrar no SUMMARY (co-migração completa no plano 37-1-14) e garantir que os testes novos do plano 37-1-12 cobrem os cenários flag on/off.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/campaign/generate-image/__tests__/route.test.ts 2>&1 | Select-Object -Last 5</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/app/api/campaign/generate-image/__tests__/route.test.ts` passa (com mock co-migrado com `isCampaignApprovalEnabled: vi.fn().mockResolvedValue(false)`, se necessário)
    - A única alteração permitida no teste existente é a adição de `isCampaignApprovalEnabled` ao mock (flag off default)
    - Nenhuma mudança fora do bloco de inserção da v1 + imports (Task 1) e do mock (Task 2)
  </acceptance_criteria>
  <done>Flag off preserva o comportamento atual (zero inserções); suíte existente da rota verde com mock co-migrado</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (lojista) → generate-image | Input não confiável do formulário (validado por zod + snapshot F39) |
| server → campaign_art_versions | Inserção da v1 via supabaseAdmin (service_role) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-06-01 | Availability | Falha no insert da v1 derruba a geração | mitigate | try/catch fail-safe: log de erro operacional + continua; campanha exibida como legacy (D1 — a flag nunca derruba o fluxo atual) |
| T-37-06-02 | Tampering | v1 com brief_snapshot divergente do persistido | mitigate | Reutiliza `inputSnapshot` — o MESMO objeto `campaign_brief_v1` gravado em campaigns.input_snapshot (buildCampaignBriefSnapshot, F39, sem base64) |
| T-37-06-03 | Tampering | Inserção de v1 com flag off (drift de comportamento) | mitigate | Inserção condicionada a `if (await isCampaignApprovalEnabled())` (fail-closed false); suíte existente com mock flag off valida o no-op |
| T-37-06-04 | Integrity | storage_path divergente do asset real | mitigate | Reaproveita `campaign.storagePath` (path da geração inicial `{storeId}/{campaignId}.jpg`) — nenhum path novo nesta fatia (D4) |
| T-37-06-SC | Tampering | dependências npm | accept | Nenhuma dependência nova — sem instalação de pacotes |
</threat_model>

<verification>
- Grep confirma import + chamada createArtVersion(campaign.id, 1, campaign.storagePath, inputSnapshot) dentro de if (isCampaignApprovalEnabled()) com try/catch que só loga
- `npx vitest run src/app/api/campaign/generate-image/__tests__/route.test.ts` — suíte existente verde (mock com flag off)
- Core de geração (providers/prompts/créditos/stream) sem alteração
</verification>

<success_criteria>
- Flag ligada → campanha nova nasce com v1 em campaign_art_versions (pending, active, brief_snapshot = snapshot persistido)
- Flag off → nenhuma inserção (comportamento atual inalterado)
- Falha no insert → log + continua (fail-safe; campanha legacy entregue)
</success_criteria>

<output>
Create `.planning/phases/37.1-approval-gate-candidata-unica/37-1-06-SUMMARY.md` when done
</output>

---
phase: 37.1-approval-gate-candidata-unica
plan: 07
type: execute
wave: 4
depends_on: [37-1-03, 37-1-04, 37-1-05]
files_modified:
  - src/app/api/campaign/[id]/download/route.ts
  - src/app/api/campaign/[id]/publication-copy/route.ts
autonomous: true
requirements: [F37.1-16, F37.1-17]

must_haves:
  truths:
    - "GET /api/campaign/[id]/download verifica o estado de aprovação após auth+ownership: isDeliveryReleased(state) === false (pending/regenerating com flag on) → 403 { error: 'Campaign pending approval' }; not_enabled/legacy/approved → serve campaign.storage_path como hoje"
    - "PATCH /api/campaign/[id]/publication-copy verifica o estado de aprovação após auth+ownership ANTES de persistir: pending/regenerating com flag on → 403 (nada persistido); not_enabled/legacy/approved → edição normal e restore preservados"
    - "Custo por request: 2 lookups simples (flag + versões); fast path para legado (flag off → zero lookups adicionais)"
  artifacts:
    - path: "src/app/api/campaign/[id]/download/route.ts"
      provides: "Gate de aprovação no download"
      contains: "isDeliveryReleased"
    - path: "src/app/api/campaign/[id]/publication-copy/route.ts"
      provides: "Gate de aprovação no publication-copy"
      contains: "isDeliveryReleased"
  key_links:
    - from: "download/route.ts / publication-copy/route.ts"
      to: "display.ts (computeApprovalState + isDeliveryReleased)"
      via: "derivação do estado após isCampaignApprovalEnabled() + listArtVersions(campaign.id)"
      pattern: "computeApprovalState"
    - from: "rotas gated"
      to: "campaigns.storage_path"
      via: "serve storage_path (approved repontado no approve; legacy/not_enabled como hoje)"
      pattern: "campaign.storage_path"
---

<objective>
Inserir o **gate de aprovação** nas rotas de entrega (tasks.md seções 9 e 10, D2 + decisão 4): `GET /api/campaign/[id]/download` e `PATCH /api/campaign/[id]/publication-copy` passam a derivar o estado de aprovação (via `isCampaignApprovalEnabled()` + `listArtVersions(campaign.id)` + `computeApprovalState`) após auth+ownership; se `isDeliveryReleased(state) === false` (i.e., `pending`/`regenerating` com a flag ligada) → **403**; `not_enabled`/`legacy`/`approved` → comportamento atual preservado.

Purpose: Decisão 4/D2 — enquanto a candidata não é aprovada, download e edição de copy são bloqueados; ao aprovar (37-1-08), ambos são liberados como hoje.
Output: 2 rotas gated com fail-closed (flag off e legado intactos).
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/app/api/campaign/[id]/download/route.ts (pipeline atual: requireApiUser → UUID → getCampaign → requireOwnership → download storage_path)
@src/app/api/campaign/[id]/publication-copy/route.ts (pipeline atual: requireSameOrigin → requireApiUser → UUID → getCampaign → requireOwnership → validação → update/restore)
@src/lib/campaign/display.ts (pós-37-1-05: computeApprovalState, isDeliveryReleased)
@src/lib/campaign/persistence.ts (pós-37-1-04: listArtVersions)
@src/lib/feature-flags/feature-flag-service.ts (pós-37-1-03: isCampaignApprovalEnabled)
@openspec/changes/fase-37-1-approval-gate-candidata-unica/specs/campaign-download-route/spec.md (gate download)
@openspec/changes/fase-37-1-approval-gate-candidata-unica/specs/publication-copy-route/spec.md (gate copy)
@openspec/changes/fase-37-1-approval-gate-candidata-unica/design.md (D8 — gates)

<interfaces>
Padrão do gate (idêntico nas 2 rotas, após `await requireOwnership(campaign.store_id, user.userId);`):
```ts
if (await isCampaignApprovalEnabled()) {
  const versions = await listArtVersions(campaign.id);
  const state = computeApprovalState(campaign, versions, true);
  if (!isDeliveryReleased(state)) {
    return NextResponse.json({ error: "Campaign pending approval" }, { status: 403 });
  }
}
```
- Imports: `isCampaignApprovalEnabled` de `@/lib/feature-flags/feature-flag-service`; `listArtVersions` de `@/lib/campaign/persistence`; `computeApprovalState`/`isDeliveryReleased` de `@/lib/campaign/display`.
- download: o gate entra APÓS `requireOwnership` (:28) e ANTES do `.download(campaign.storage_path)` (:30).
- publication-copy: o gate entra APÓS `requireOwnership` (:33) e ANTES do parse/validação do body e de qualquer UPDATE (nada persistido em 403).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate de aprovação em GET /api/campaign/[id]/download</name>
  <files>src/app/api/campaign/[id]/download/route.ts</files>
  <read_first>
    - src/app/api/campaign/[id]/download/route.ts (pipeline atual completo, 53 linhas)
    - src/lib/campaign/display.ts (computeApprovalState/isDeliveryReleased pós-37-1-05)
    - src/lib/campaign/persistence.ts (listArtVersions pós-37-1-04)
  </read_first>
  <action>
    Editar `src/app/api/campaign/[id]/download/route.ts`:
    1. Adicionar imports: `isCampaignApprovalEnabled` de `@/lib/feature-flags/feature-flag-service`, `listArtVersions` de `@/lib/campaign/persistence`, e `computeApprovalState`/`isDeliveryReleased` de `@/lib/campaign/display`.
    2. Inserir o gate IMEDIATAMENTE após `await requireOwnership(campaign.store_id, user.userId);` (:28) e ANTES do `supabaseAdmin.storage.from("campaign-images").download(...)` (:30), exatamente como no padrão `<interfaces>`: ler flag → se true, `listArtVersions(campaign.id)` → `computeApprovalState(campaign, versions, true)` → `if (!isDeliveryReleased(state)) return NextResponse.json({ error: "Campaign pending approval" }, { status: 403 })`.
    3. Manter o fluxo de sucesso intacto (download de `campaign.storage_path` — aprovada repontada no approve; legacy/not_enabled como hoje) e os demais guards (401/400/404/502) inalterados.
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\app\api\campaign\[id]\download\route.ts" -Pattern "isCampaignApprovalEnabled|listArtVersions|isDeliveryReleased|Campaign pending approval" | Select-Object -First 8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "isDeliveryReleased" src/app/api/campaign/[id]/download/route.ts` → presente (import + uso no gate após requireOwnership)
    - `grep -n "Campaign pending approval" src/app/api/campaign/[id]/download/route.ts` → presente com `status: 403`
    - O gate está posicionado ANTES do `.download(campaign.storage_path)` (ordem do arquivo)
    - Guards existentes (401/400/404/502) e o serviço do arquivo inalterados
    - `npx tsc --noEmit` sem erro de tipo novo
  </acceptance_criteria>
  <done>Download gated: pending/regenerating + flag on → 403; demais estados liberados como hoje</done>
</task>

<task type="auto">
  <name>Task 2: Gate de aprovação em PATCH /api/campaign/[id]/publication-copy</name>
  <files>src/app/api/campaign/[id]/publication-copy/route.ts</files>
  <read_first>
    - src/app/api/campaign/[id]/publication-copy/route.ts (pipeline atual completo, 76 linhas)
    - src/lib/campaign/display.ts (computeApprovalState/isDeliveryReleased pós-37-1-05)
  </read_first>
  <action>
    Editar `src/app/api/campaign/[id]/publication-copy/route.ts`:
    1. Adicionar imports: `isCampaignApprovalEnabled` de `@/lib/feature-flags/feature-flag-service`, `listArtVersions` de `@/lib/campaign/persistence`, e `computeApprovalState`/`isDeliveryReleased` de `@/lib/campaign/display`.
    2. Inserir o gate IMEDIATAMENTE após `await requireOwnership(campaign.store_id, user.userId);` (:33) e ANTES do `request.json()`/validação/UPDATE (decisão 4 — nada é persistido em 403), exatamente como no padrão `<interfaces>`.
    3. Manter o fluxo de sucesso intacto: validação `validatePublicationCopy` (400 com issues), `restore: true` → `publication_copy_current: null` + retorno do snapshot, dados normais → `publication_copy_current: validation.data` (200). Guards CSRF/auth/404 existentes inalterados.
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\app\api\campaign\[id]\publication-copy\route.ts" -Pattern "isCampaignApprovalEnabled|listArtVersions|isDeliveryReleased|Campaign pending approval" | Select-Object -First 8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "isDeliveryReleased" src/app/api/campaign/[id]/publication-copy/route.ts` → presente (import + uso no gate após requireOwnership)
    - `grep -n "Campaign pending approval" src/app/api/campaign/[id]/publication-copy/route.ts` → presente com `status: 403`
    - O gate está posicionado ANTES de qualquer `supabaseAdmin.from("campaigns").update(...)` (nada persistido em 403)
    - Fluxo de sucesso (validação 400, restore 200, update 200) inalterado
    - `npx tsc --noEmit` sem erro de tipo novo
  </acceptance_criteria>
  <done>Publication-copy gated: pending/regenerating + flag on → 403 sem persistir; demais estados com edição/restore preservados</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (lojista autenticado) → rotas de entrega | Usuário com ownership da campanha (após guards existentes) |
| rotas gated → estado derivado | Entrega liberada/não liberada (pending/regenerating bloqueiam) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-07-01 | DoS | Gate quebra download/copy de campanha nova sob a flag | mitigate | `isDeliveryReleased` fail-closed: true para not_enabled/legacy/approved; apenas pending/regenerating com flag ON retornam 403; legacy derivado da ausência de versões (D2) |
| T-37-07-02 | Tampering | Bypass do gate (rota servindo sem checar estado) | mitigate | Gate executado após requireOwnership e ANTES do download/update; 403 retorna sem tocar no storage/DB |
| T-37-07-03 | Tampering | Flag off com drift (gate ativo sem flag) | mitigate | Gate condicionado a `if (await isCampaignApprovalEnabled())` — flag off/erro de leitura → zero lookups adicionais, comportamento atual (fail-closed D1) |
| T-37-07-04 | DoS | Overhead de queries em download/copy | mitigate | 2 lookups simples por request (flag + versões); fast path: flag off → nenhum lookup extra; estado derivado de campaign_art_versions (single source, D8) |
| T-37-07-SC | Tampering | dependências npm | accept | Nenhuma dependência nova — sem instalação de pacotes |
</threat_model>

<verification>
- Grep confirma o gate (isCampaignApprovalEnabled + listArtVersions + computeApprovalState + isDeliveryReleased + 403 "Campaign pending approval") nas 2 rotas, posicionado após requireOwnership
- `npx tsc --noEmit` sem erro novo
- Suítes existentes de download/copy serão co-migradas nos planos 37-1-12 (testes novos) e 37-1-14 (regressão)
</verification>

<success_criteria>
- Download e publication-copy bloqueados (403) para pending/regenerating com flag ligada
- Download e publication-copy liberados para not_enabled/legacy/approved (comportamento atual)
- Flag off → nenhuma mudança de comportamento (fail-closed); custo mínimo de queries
</success_criteria>

<output>
Create `.planning/phases/37.1-approval-gate-candidata-unica/37-1-07-SUMMARY.md` when done
</output>

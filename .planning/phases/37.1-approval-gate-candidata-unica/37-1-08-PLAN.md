---
phase: 37.1-approval-gate-candidata-unica
plan: 08
type: execute
wave: 4
depends_on: [37-1-02, 37-1-03, 37-1-05]
files_modified:
  - src/app/api/campaign/[id]/approve/route.ts
autonomous: true
requirements: [F37.1-18]

must_haves:
  truths:
    - "Existe a rota POST /api/campaign/[id]/approve com o pipeline: requireSameOrigin (CSRF) → requireApiUser → UUID v4 (400) → getCampaign (404) → requireOwnership (404) → !isCampaignApprovalEnabled() → 403 → campaign.status !== 'ready' → 409 → zod body { versionId: uuid } strict"
    - "A rota chama rpc('approve_campaign_art_version', { p_campaign_id, p_version_id }) e mapeia: version_not_found/version_campaign_mismatch → 404; version_not_pending/version_not_active → 409; sucesso → 200 { campaignUrl: '/campanhas/{id}', status: 'approved' }"
    - "Anti-concorrência: segunda aprovação → 409 (RPC guarded update + índice único parcial) — a rota não sobrescreve estados"
  artifacts:
    - path: "src/app/api/campaign/[id]/approve/route.ts"
      provides: "Aprovação transacional da candidata"
      contains: "approve_campaign_art_version"
  key_links:
    - from: "approve/route.ts"
      to: "RPC approve_campaign_art_version (migration 37-1-02)"
      via: "supabaseAdmin.rpc('approve_campaign_art_version', ...)"
      pattern: "approve_campaign_art_version"
    - from: "approve/route.ts"
      to: "campaigns.storage_path"
      via: "repontado para a aprovada dentro do RPC (D8) — resposta 200 libera a entrega"
      pattern: "campaignUrl"
---

<objective>
Criar a rota `POST /api/campaign/[id]/approve` (tasks.md seção 11, D6/D8): pipeline de guards (CSRF → auth → UUID → getCampaign → ownership → flag → status ready) + body zod strict `{ versionId: uuid }` + chamada transacional ao RPC `approve_campaign_art_version` com mapeamento de erros (404/409) e resposta `200 { campaignUrl, status: "approved" }`.

Purpose: D8 — ação única que aprova a candidata, reponta `campaigns.storage_path` e libera a entrega (download + copys) de forma atômica no banco.
Output: Rota de aprovação pronta, consumida pela UI (37-1-09) e testada (37-1-11).
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/app/api/campaign/[id]/publication-copy/route.ts (padrão requireSameOrigin + requireApiUser + UUID + getCampaign + requireOwnership + apiHandler)
@src/app/api/admin/feature-flags/route.ts (padrão de mapeamento de erro do RPC via `error.message.includes(...)`)
@src/lib/feature-flags/feature-flag-service.ts (pós-37-1-03: isCampaignApprovalEnabled)
@src/lib/campaign/types.ts (pós-37-1-04: CampaignRecord com approval_status)
@openspec/changes/fase-37-1-approval-gate-candidata-unica/design.md (D6 — pipeline completo da rota e mapeamento)
@openspec/changes/fase-37-1-approval-gate-candidata-unica/specs/campaign-approval-gate/spec.md (requisito "Rota POST /api/campaign/[id]/approve")

<interfaces>
Contrato da rota (design.md D6):
- Fluxo (ordem estrita): `requireSameOrigin(request)` → `requireApiUser()` → UUID v4 (`400`) → `getCampaign(id)` (404 se inexistente) → `requireOwnership(campaign.store_id, user.userId)` (404) → `if (!(await isCampaignApprovalEnabled())) return 403` → `campaign.status !== 'ready' → 409` → zod body `{ versionId: z.string().uuid() }` strict (`400`) → `supabaseAdmin.rpc("approve_campaign_art_version", { p_campaign_id: id, p_version_id: versionId })`.
- Mapeamento de erros do RPC: `error.message.includes("version_not_found") || error.message.includes("version_campaign_mismatch")` → 404; `error.message.includes("version_not_pending") || error.message.includes("version_not_active")` → 409; outro → 500.
- Sucesso: `200 { campaignUrl: "/campanhas/{id}", status: "approved" }`.
- `export const dynamic = "force-dynamic";` (padrão publication-copy).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pipeline de guards da rota POST /api/campaign/[id]/approve</name>
  <files>src/app/api/campaign/[id]/approve/route.ts</files>
  <read_first>
    - src/app/api/campaign/[id]/publication-copy/route.ts (padrão completo de rota com requireSameOrigin/requireApiUser/apiHandler/notFound)
    - src/lib/feature-flags/feature-flag-service.ts (isCampaignApprovalEnabled)
  </read_first>
  <action>
    Criar o arquivo `src/app/api/campaign/[id]/approve/route.ts`:
    1. Imports: `NextRequest`/`NextResponse` de `next/server`; `requireSameOrigin` de `@/lib/auth/csrf`; `requireApiUser` de `@/lib/auth/require-user`; `requireOwnership` de `@/lib/auth/store-ownership`; `apiHandler` de `@/lib/auth/api-handler`; `notFound` de `@/lib/api-error-response`; `getCampaign` de `@/lib/campaign/persistence`; `isCampaignApprovalEnabled` de `@/lib/feature-flags/feature-flag-service`; `supabaseAdmin` de `@/lib/supabase/server`; `z` de `zod`.
    2. Constantes: `export const dynamic = "force-dynamic";` e `UUID_V4_REGEX` (padrão das rotas campaign).
    3. Schema do body: `const ApproveBodySchema = z.object({ versionId: z.string().uuid() }).strict();` (strict — sem campos extras, D8).
    4. `export const POST = apiHandler(async (request: NextRequest, { params }: { params: Promise<{ id: string }> }) => { ... })` implementando a ORDEM ESTRITA do contrato `<interfaces>`:
       - `requireSameOrigin(request)` (CSRF, primeiro guard)
       - `const user = await requireApiUser();`
       - `const { id } = await params;` + UUID v4 → se inválido `NextResponse.json({ error: "Invalid campaign ID" }, { status: 400 })`
       - `const campaign = await getCampaign(id);` → se null `notFound("Campaign not found")`
       - `await requireOwnership(campaign.store_id, user.userId);` (404 em caso de não-dono — mesmo status de inexistente)
       - `if (!(await isCampaignApprovalEnabled())) return NextResponse.json({ error: "Approval flow disabled" }, { status: 403 });`
       - `if (campaign.status !== "ready") return NextResponse.json({ error: "Campaign not ready" }, { status: 409 });`
       - Parse do body com `request.json().catch(() => null)` + `ApproveBodySchema.safeParse` → inválido → `NextResponse.json({ error: "Invalid body", issues: parsed.error.errors }, { status: 400 })`
       - (a chamada RPC + resposta fica na Task 2 — a Task 1 termina no body parseado, retornando 400 em caso de falha)
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\app\api\campaign\[id]\approve\route.ts" -Pattern "requireSameOrigin|requireApiUser|requireOwnership|isCampaignApprovalEnabled|Campaign not ready|ApproveBodySchema" | Select-Object -First 10</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `src/app/api/campaign/[id]/approve/route.ts` existe com `export const POST = apiHandler(...)` e `dynamic = "force-dynamic"`
    - Guards na ordem do contrato: requireSameOrigin → requireApiUser → UUID (400) → getCampaign (404) → requireOwnership (404) → flag (403) → status !== 'ready' (409) → zod strict (400)
    - `ApproveBodySchema` usa `.strict()` com `versionId: z.string().uuid()`
    - `npx tsc --noEmit` sem erro de tipo
  </acceptance_criteria>
  <done>Pipeline de guards completo na ordem estrita do D6</done>
</task>

<task type="auto">
  <name>Task 2: Chamada RPC + mapeamento de erros + resposta 200</name>
  <files>src/app/api/campaign/[id]/approve/route.ts</files>
  <read_first>
    - src/app/api/admin/feature-flags/route.ts (padrão de mapeamento `msg.includes(...)` do erro do RPC)
    - src/app/api/campaign/[id]/approve/route.ts (resultado da Task 1 — completar o handler)
  </read_first>
  <action>
    Continuar o handler do arquivo (após o parse do body da Task 1):
    1. Chamada transacional: `const { data, error } = await supabaseAdmin.rpc("approve_campaign_art_version", { p_campaign_id: id, p_version_id: parsed.data.versionId });`
    2. Mapeamento de erros (padrão `msg.includes`, admin/feature-flags):
       - `const msg = error.message ?? "";`
       - `msg.includes("version_not_found") || msg.includes("version_campaign_mismatch")` → `notFound("Version not found")` (404)
       - `msg.includes("version_not_pending") || msg.includes("version_not_active")` → `NextResponse.json({ error: "Version already resolved or invalid" }, { status: 409 })`
       - demais → `NextResponse.json({ error: msg }, { status: 500 })`
    3. Sucesso → `return NextResponse.json({ campaignUrl: `/campanhas/${id}`, status: "approved" });` (200). O `data` do RPC não precisa ser exposto na resposta (telemetria via `campaign_art_versions.status` + `campaigns.approved_at`, D8 — sem novo generation_type).
    4. Confirmar que o arquivo inteiro compila e a resposta de sucesso tem exatamente o shape `{ campaignUrl, status: "approved" }`.
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\app\api\campaign\[id]\approve\route.ts" -Pattern "approve_campaign_art_version|version_not_found|version_campaign_mismatch|version_not_pending|version_not_active|campaignUrl|status: \"approved\"" | Select-Object -First 10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "approve_campaign_art_version" src/app/api/campaign/[id]/approve/route.ts` → chamada `supabaseAdmin.rpc("approve_campaign_art_version", { p_campaign_id: id, p_version_id: parsed.data.versionId })`
    - Mapeamento presente: `version_not_found`/`version_campaign_mismatch` → 404; `version_not_pending`/`version_not_active` → 409
    - Resposta de sucesso: `{ campaignUrl: "/campanhas/{id}", status: "approved" }` com 200
    - `npx tsc --noEmit` sem erro de tipo
  </acceptance_criteria>
  <done>Rota completa: RPC transacional + mapeamento 404/409 + 200 { campaignUrl, status: "approved" }</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (lojista autenticado) → POST /approve | Ação de mutação de estado — input não confiável (CSRF + auth + ownership) |
| server → RPC approve_campaign_art_version | Aprovação atômica no banco (service_role) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-08-01 | Spoofing | Aprovação por não-dono | mitigate | `requireOwnership(campaign.store_id, user.userId)` → 404 (mesmo status de campanha inexistente — sem enumeração) |
| T-37-08-02 | Spoofing | CSRF (origem externa) | mitigate | `requireSameOrigin(request)` como PRIMEIRO guard (padrão publication-copy) |
| T-37-08-03 | Tampering | Aprovação duplicada/concorrente | mitigate | RPC guarded update (`SELECT ... FOR UPDATE`) + índice único parcial 1-approved → segunda aprovação retorna version_not_pending → 409 (idempotente) |
| T-37-08-04 | Tampering | Aprovação de versão de outra campanha / inexistente | mitigate | RPC valida version_not_found/version_campaign_mismatch → 404; body zod strict impede versionId malformado |
| T-37-08-05 | DoS | Aprovação de campanha generating/error | mitigate | `campaign.status !== 'ready' → 409` antes do RPC (sem candidata para aprovar) |
| T-37-08-06 | Tampering | Flag off aprova fora do experimento | mitigate | `!isCampaignApprovalEnabled() → 403` antes do RPC (fail-closed) |
| T-37-08-07 | Elevation of Privilege | RPC executado fora de contexto | mitigate | Acesso ao RPC restrito a service_role (REVOKE anon/authenticated na migration 37-1-02); rota é o único caminho de aprovação |
| T-37-08-SC | Tampering | dependências npm | accept | Nenhuma dependência nova — sem instalação de pacotes |
</threat_model>

<verification>
- Grep confirma ordem de guards e mapeamento de erros (404/409/500) + resposta 200 { campaignUrl, status: "approved" }
- `npx tsc --noEmit` sem erro de tipo
- Testes da rota no plano 37-1-11 (guards + mapeamento)
</verification>

<success_criteria>
- Aprovação transacional via RPC com mapeamento de status correto (400/403/404/409/200)
- Anti-concorrência: segunda aprovação → 409 (idempotente)
- Telemetria sem novo generation_type (funil via campaign_art_versions.status + campaigns.approved_at)
</success_criteria>

<output>
Create `.planning/phases/37.1-approval-gate-candidata-unica/37-1-08-SUMMARY.md` when done
</output>

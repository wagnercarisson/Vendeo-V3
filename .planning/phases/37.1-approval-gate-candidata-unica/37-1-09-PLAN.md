---
phase: 37.1-approval-gate-candidata-unica
plan: 09
type: execute
wave: 5
depends_on: [37-1-04, 37-1-05, 37-1-08]
files_modified:
  - src/app/(app)/campanhas/[id]/page.tsx
  - src/app/(app)/campanhas/[id]/client.tsx
  - src/components/campaign/campaign-approval-view.tsx
autonomous: true
requirements: [F37.1-19, F37.1-20, F37.1-21, F37.1-22, F37.1-23]

must_haves:
  truths:
    - "page.tsx para campanhas ready lê isCampaignApprovalEnabled() + listArtVersions(id) → computeApprovalState e passa approval (state, candidateImageUrl, candidateVersionId) via props"
    - "CampaignApprovalView (client) exibe a arte da candidata ativa (signed URL), botão primário 'Aprovar e liberar campanha' → POST /api/campaign/[id]/approve { versionId } → router.refresh(); botão secundário 'Corrigir' AUSENTE ou desabilitado que NUNCA abre modal; sem download, sem Kit/copy; estados loading/erro PT-BR"
    - "client.tsx renderiza CampaignApprovalView quando approval.state.status === 'pending'; approved/legacy/not_enabled → ReadyView atual"
    - "UX sem histórico recuperável (decisão 12): apenas a candidata ativa é exibida; nenhuma versão anterior selecionável"
    - "A11y/mobile/microcopy: touch ≥ 44px, label/aria, 'Revise a arte antes de liberar: a IA pode cometer erros.', imagem object-contain, sem scroll horizontal em 320px/375px, tema dark (tokens #020617/#F8FAFC/#22C55E)"
  artifacts:
    - path: "src/components/campaign/campaign-approval-view.tsx"
      provides: "Tela de revisão da candidata (client)"
      contains: "Aprovar e liberar campanha"
    - path: "src/app/(app)/campanhas/[id]/page.tsx"
      provides: "Derivação do estado de aprovação + props approval"
      contains: "computeApprovalState"
    - path: "src/app/(app)/campanhas/[id]/client.tsx"
      provides: "Roteamento pending → CampaignApprovalView; demais → ReadyView"
      contains: "CampaignApprovalView"
  key_links:
    - from: "page.tsx"
      to: "display.ts (computeApprovalState + getActiveCandidateArtVersion + generateSignedPreviewUrl)"
      via: "estado pending + candidata ativa → signed URL"
      pattern: "getActiveCandidateArtVersion"
    - from: "CampaignApprovalView"
      to: "POST /api/campaign/[id]/approve"
      via: "fetch POST { versionId } → router.refresh()"
      pattern: "api/campaign/${campaignId}/approve"
---

<objective>
Implementar a **tela de revisão da candidata** (tasks.md seção 12, D7 + decisões 3/12): (1) `page.tsx` deriva o estado de aprovação para campanhas `ready` e estende as props com `approval` (state/candidateImageUrl/candidateVersionId); (2) novo componente client `campaign-approval-view.tsx` com a arte da candidata ativa, botão primário **"Aprovar e liberar campanha"**, secundário **"Corrigir" ausente/desabilitado que nunca abre modal**, sem download/Kit/copy, microcopy e a11y/mobile; (3) `client.tsx` renderiza `CampaignApprovalView` quando `pending`, e `ReadyView` para `approved`/`legacy`/`not_enabled` (entrega atual).

Purpose: D7 — o lojista vê a candidata e decide aprovar; ao aprovar (rota 37-1-08), `router.refresh()` libera a entrega como hoje.
Output: Fluxo de revisão funcional de ponta a ponta (sem correção — escopo 37.1).
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/app/(app)/campanhas/[id]/page.tsx (estrutura atual — getCampaignForDisplay + generateSignedPreviewUrl + mapCampaignToProps)
@src/app/(app)/campanhas/[id]/client.tsx (estrutura atual — ReadyView/GeneratingView/StaleView/ErrorView)
@src/lib/campaign/display.ts (pós-37-1-05 — ApprovalDisplayState, computeApprovalState, getActiveCandidateArtVersion, CampaignPageProps.approval)
@src/components/ui/button.tsx (padrão Button do design system — variant primary/ghost, size, loading)
@openspec/changes/fase-37-1-approval-gate-candidata-unica/design.md (D7 — UI)
@openspec/changes/fase-37-1-approval-gate-candidata-unica/specs/campaign-page-ui/spec.md (cenários da tela de revisão)

<interfaces>
Contrato da UI (design.md D7):
- `CampaignPageProps.approval?: { state: ApprovalDisplayState; candidateImageUrl?: string | null; candidateVersionId?: string | null }` (definido em 37-1-05).
- page.tsx (campanha `ready`): `const flagEnabled = await isCampaignApprovalEnabled(); const versions = flagEnabled ? await listArtVersions(id) : []; const state = computeApprovalState(campaign, versions, flagEnabled);` — se `state.status === "pending"`: `const candidate = getActiveCandidateArtVersion(versions);` e `approval = { state, candidateImageUrl: candidate?.storage_path ? await generateSignedPreviewUrl(candidate.storage_path) : null, candidateVersionId: candidate?.id ?? null }`. Demais estados: nenhuma prop approval (ReadyView como hoje, arte de `campaigns.storage_path`).
- CampaignApprovalView props: `{ campaignId: string; versionId: string; imageUrl: string; productName: string }`. Microcopy: "Revise a arte antes de liberar: a IA pode cometer erros." Botão primário "Aprovar e liberar campanha" (loading "Aprovando..."); `fetch("/api/campaign/{campaignId}/approve", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ versionId }) })` → `router.refresh()`. Erro PT-BR: "Não foi possível aprovar. Tente novamente."
- client.tsx: `{props.displayStatus === "ready" && props.approval?.state.status === "pending" && props.approval.candidateImageUrl && props.approval.candidateVersionId ? <CampaignApprovalView campaignId={props.campaignId} versionId={props.approval.candidateVersionId} imageUrl={props.approval.candidateImageUrl} productName={props.productName} /> : props.displayStatus === "ready" && <ReadyView {...props} />}` — note que `client.tsx` importa o TIPO `CampaignPageProps` de display (type-only, permitido — precedente atual).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: page.tsx — derivação do estado de aprovação + props approval (F37.1-19)</name>
  <files>src/app/(app)/campanhas/[id]/page.tsx</files>
  <read_first>
    - src/app/(app)/campanhas/[id]/page.tsx (estrutura atual completa, 38 linhas)
    - src/lib/campaign/display.ts (computeApprovalState/getActiveCandidateArtVersion/generateSignedPreviewUrl pós-37-1-05)
    - src/lib/campaign/persistence.ts (listArtVersions pós-37-1-04)
  </read_first>
  <action>
    Editar `src/app/(app)/campanhas/[id]/page.tsx`:
    1. Adicionar imports: `isCampaignApprovalEnabled` de `@/lib/feature-flags/feature-flag-service`; `listArtVersions` de `@/lib/campaign/persistence`; `computeApprovalState` e `getActiveCandidateArtVersion` de `@/lib/campaign/display`.
    2. Após `const props = mapCampaignToProps(campaign, id);` e ANTES do retorno, para campanhas `ready`, derivar:
       ```ts
       const flagEnabled = await isCampaignApprovalEnabled();
       const versions = flagEnabled ? await listArtVersions(id) : [];
       const state = computeApprovalState(campaign, versions, flagEnabled);
       if (state.status === "pending") {
         const candidate = getActiveCandidateArtVersion(versions);
         props.approval = {
           state,
           candidateImageUrl: candidate?.storage_path ? await generateSignedPreviewUrl(candidate.storage_path) : null,
           candidateVersionId: candidate?.id ?? null,
         };
       }
       ```
    3. Manter o signed URL de `campaign.storage_path` para os demais estados (approved/legacy/not_enabled → ReadyView como hoje, D2/decisão 3) e os guards existentes (user/store/notFound) inalterados.
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\app\(app)\campanhas\[id]\page.tsx" -Pattern "isCampaignApprovalEnabled|listArtVersions|computeApprovalState|getActiveCandidateArtVersion|props.approval" | Select-Object -First 8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "computeApprovalState" "src/app/(app)/campanhas/[id]/page.tsx"` → presente (import + chamada com flagEnabled)
    - `grep -n "props.approval" "src/app/(app)/campanhas/[id]/page.tsx"` → presente, preenchido APENAS quando `state.status === "pending"` com `candidateImageUrl` (via generateSignedPreviewUrl da candidata ativa) e `candidateVersionId`
    - Guard existente do signed URL de `campaign.storage_path` inalterado para os demais estados
    - `npx tsc --noEmit` sem erro de tipo
  </acceptance_criteria>
  <done>page.tsx deriva o estado de aprovação e passa approval (candidata ativa) quando pending</done>
</task>

<task type="auto">
  <name>Task 2: Novo componente CampaignApprovalView (F37.1-20/22/23)</name>
  <files>src/components/campaign/campaign-approval-view.tsx</files>
  <read_first>
    - src/app/(app)/campanhas/[id]/client.tsx (padrão visual ReadyView — imagens, classes tailwind, Button)
    - src/components/ui/button.tsx (variants/sizes do design system)
    - openspec/changes/fase-37-1-approval-gate-candidata-unica/specs/campaign-page-ui/spec.md (cenários da revisão)
  </read_first>
  <action>
    Criar `src/components/campaign/campaign-approval-view.tsx` ("use client"):
    1. Props: `{ campaignId: string; versionId: string; imageUrl: string; productName: string }`.
    2. Layout (padrão visual do client.tsx — container `max-w-2xl`, PageHeader opcional): imagem da candidata com `className="w-full rounded-xl shadow-md object-contain"` (sem recorte, a11y alt = productName), e o bloco de revisão:
       - Microcopy obrigatória: `"Revise a arte antes de liberar: a IA pode cometer erros."` (texto visível).
       - **Botão primário "Aprovar e liberar campanha"** (`variant="primary"`, `size="md"`, `min-h-11` para touch ≥ 44px, `aria-label="Aprovar e liberar campanha"`, `loading` quando em progresso com texto "Aprovando...") — ao clicar: `fetch("/api/campaign/${campaignId}/approve", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ versionId }) })`; se `!res.ok`, extrair `err.error` e mostrar estado de erro PT-BR "Não foi possível aprovar. Tente novamente."; se ok → `router.refresh()`.
       - **Botão secundário "Corrigir" AUSENTE** (não renderizar) — alternativa aceitável: renderizar desabilitado com `aria-disabled="true"` e `disabled` SEM handler e SEM modal; **em nenhum caso** abre modal de correção (decisão 3/D12; correção é 37.2).
       - **SEM** botão de download e **SEM** Kit de Publicação/copy (revisão 100% foco na arte — D2).
    3. Estados: `const [isSubmitting, setIsSubmitting] = useState(false);` e `const [error, setError] = useState<string | null>(null);` — reset de erro ao novo clique.
    4. A11y: `aria-live="polite"` no bloco de erro; tema dark via tokens existentes (`bg-bg-surface`, `text-text-primary`, `text-accent-green` — precedente client.tsx; tokens `#020617`/`#F8FAFC`/`#22C55E` do design-system); sem scroll horizontal em 320px/375px (imagem object-contain + container fluido).
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\components\campaign\campaign-approval-view.tsx" -Pattern "Aprovar e liberar campanha|Revise a arte antes de liberar|api/campaign/|object-contain|router.refresh" | Select-Object -First 8</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `src/components/campaign/campaign-approval-view.tsx` existe com `"use client"` e as 4 props (campaignId, versionId, imageUrl, productName)
    - `grep -n "Aprovar e liberar campanha" src/components/campaign/campaign-approval-view.tsx` → botão primário presente
    - `grep -n "Revise a arte antes de liberar" src/components/campaign/campaign-approval-view.tsx` → microcopy presente
    - `grep -n "object-contain" src/components/campaign/campaign-approval-view.tsx` → imagem sem recorte
    - `grep -n "api/campaign/\${campaignId}/approve" src/components/campaign/campaign-approval-view.tsx` → fetch POST do approve com body `{ versionId }` e `router.refresh()` no sucesso
    - `grep -in "corrigir\|modal" src/components/campaign/campaign-approval-view.tsx` → ou ausente (botão não renderizado) ou apenas desabilitado sem handler/modal; NENHUM `dialog`/modal de correção no arquivo
    - `grep -in "download\|publication-copy\|Kit de Publicação" src/components/campaign/campaign-approval-view.tsx` → ZERO ocorrências (sem download/copy na revisão)
  </acceptance_criteria>
  <done>CampaignApprovalView pronto: arte candidata sem recorte, aprovar com loading/erro PT-BR, corrigir ausente/desabilitado sem modal, sem download/copy</done>
</task>

<task type="auto">
  <name>Task 3: client.tsx — roteamento pending → CampaignApprovalView (F37.1-21)</name>
  <files>src/app/(app)/campanhas/[id]/client.tsx</files>
  <read_first>
    - src/app/(app)/campanhas/[id]/client.tsx (estrutura atual — render no corpo do componente: linhas 47-55 com displayStatus)
  </read_first>
  <action>
    Editar `src/app/(app)/campanhas/[id]/client.tsx`:
    1. Adicionar import: `import CampaignApprovalView from "@/components/campaign/campaign-approval-view";`.
    2. No corpo do componente (bloco atual `{props.displayStatus === "ready" && <ReadyView {...props} />}`), renderizar CONDICIONALMENTE:
       - `props.displayStatus === "ready" && props.approval?.state.status === "pending" && props.approval.candidateImageUrl && props.approval.candidateVersionId` → `<CampaignApprovalView campaignId={props.campaignId} versionId={props.approval.candidateVersionId} imageUrl={props.approval.candidateImageUrl} productName={props.productName} />`
       - senão `props.displayStatus === "ready"` → `<ReadyView {...props} />` (approved/legacy/not_enabled — entrega atual, D2)
    3. Manter GeneratingView/StaleView/ErrorView e o polling de `generating` inalterados.
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\app\(app)\campanhas\[id]\client.tsx" -Pattern "CampaignApprovalView|approval\?\.state\.status === \"pending\"|candidateVersionId" | Select-Object -First 6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "CampaignApprovalView" "src/app/(app)/campanhas/[id]/client.tsx"` → import + renderização condicional presentes
    - Render condicional: `approval?.state.status === "pending"` + `candidateImageUrl` + `candidateVersionId` → CampaignApprovalView; senão ReadyView
    - GeneratingView/StaleView/ErrorView e o polling inalterados
    - `npx tsc --noEmit` sem erro de tipo
  </acceptance_criteria>
  <done>client.tsx roteia pending → CampaignApprovalView e approved/legacy/not_enabled → ReadyView</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server (page.tsx) → client (props) | Signed URL da candidata atravessa o boundary — URL curta e assinada |
| client → POST /api/campaign/[id]/approve | Ação de mutação — CSRF + auth + ownership na rota (37-1-08) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-09-01 | Information Disclosure | Arte da candidata exposta antes da aprovação | mitigate | Apenas signed URL curto (3600s, generateSignedPreviewUrl) via props para o owner autenticado; download de fato bloqueado pelos gates (37-1-07) até approved |
| T-37-09-02 | Tampering | Modal de correção abrindo na 37.1 | mitigate | Botão "Corrigir" AUSENTE ou desabilitado SEM handler/modal — nenhum dialog no componente (grep de verificação) |
| T-37-09-03 | Spoofing | Aprovação via UI sem passar pela rota | mitigate | CampaignApprovalView chama exclusivamente `POST /api/campaign/{id}/approve` (guards completos na rota 37-1-08); `router.refresh()` re-deriva o estado no servidor |
| T-37-09-04 | Tampering | Versão anterior recuperável na UI (decisão 12) | mitigate | Tela mostra apenas a candidata ativa (getActiveCandidateArtVersion); sem galeria/seleção de versões |
| T-37-09-05 | DoS | Imagem quebrada/cortada na revisão | mitigate | `object-contain` + container fluido (sem scroll horizontal em 320px/375px); fallback null de candidateImageUrl não renderiza a revisão (fica ReadyView via condição) |
| T-37-09-SC | Tampering | dependências npm | accept | Nenhuma dependência nova — sem instalação de pacotes |
</threat_model>

<verification>
- Grep confirma: page.tsx (computeApprovalState + props.approval só em pending), CampaignApprovalView (microcopy, botão aprovar, object-contain, sem corrigir/modal/download/copy), client.tsx (roteamento condicional)
- `npx tsc --noEmit` sem erro de tipo
- Testes da UI no plano 37-1-13
</verification>

<success_criteria>
- Campanha `ready` + `pending` sob a flag → tela de revisão da candidata ativa (sem download/copy)
- "Aprovar e liberar campanha" aprova via rota e libera a entrega (router.refresh)
- "Corrigir" ausente/desabilitado — nenhum modal de correção abre (escopo 37.1)
- approved/legacy/not_enabled → entrega atual intacta; a11y/mobile/tema dark conforme design-system
</success_criteria>

<output>
Create `.planning/phases/37.1-approval-gate-candidata-unica/37-1-09-SUMMARY.md` when done
</output>

---
phase: 37.1-approval-gate-candidata-unica
plan: 10
type: execute
wave: 4
depends_on: [37-1-05]
files_modified:
  - src/__tests__/lib/campaign/display-approval.test.ts
autonomous: true
requirements: [F37.1-24]

must_haves:
  truths:
    - "Existe o arquivo de testes display-approval.test.ts cobrindo os estados do computeApprovalState: flag off → not_enabled; flag on + zero versões → legacy; flag on + v1 pendente → pending; approved_version_id → approved + approvedAt"
    - "isDeliveryReleased testado: true para not_enabled/legacy/approved; false para pending/regenerating"
    - "O índice único parcial (1 approved por campaign_id) é verificado por asserção de fonte na migration 37-1-02"
    - "Teste de contrato: campaigns.status permanece ready quando regenerating derivado (decisão 5) — NÃO exercita regenerating via fluxo"
  artifacts:
    - path: "src/__tests__/lib/campaign/display-approval.test.ts"
      provides: "Testes dos estados de aprovação (tasks.md 13.1-13.7)"
      contains: "computeApprovalState"
  key_links:
    - from: "display-approval.test.ts"
      to: "display.ts (computeApprovalState/isDeliveryReleased/getActiveCandidateArtVersion)"
      via: "funções puras importadas diretamente"
      pattern: "computeApprovalState"
---

<objective>
Criar os testes dos **estados de aprovação** (tasks.md seção 13, alinhamento 1-4, 6, 8 + novos): `computeApprovalState` (not_enabled/legacy/pending/approved+approvedAt), `isDeliveryReleased` (true/false), verificação do índice único parcial via asserção de fonte na migration, e o teste de contrato de que `campaigns.status` permanece `ready` quando `regenerating` é derivado (decisão 5 — **sem exercitar** regenerating por fluxo; é contrato reservado para 37.2).

Purpose: F37.1-24 (parte) — provar o núcleo lógico da fatia com funções puras, sem banco.
Output: 7 testes verdes (13.1-13.7).
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/lib/campaign/display.ts (pós-37-1-05 — funções a testar)
@src/lib/campaign/types.ts (pós-37-1-04 — CampaignRecord estendido + CampaignArtVersion)
@src/__tests__/lib/campaign/display.test.ts (padrão de fixtures de CampaignRecord do repositório)
@openspec/changes/fase-37-1-approval-gate-candidata-unica/tasks.md (seção 13 — testes 13.1 a 13.7)
@openspec/changes/fase-37-1-approval-gate-candidata-unica/specs/campaign-approval-gate/spec.md (cenários por estado)

<interfaces>
Fixtures mínimas (padrão display.test.ts):
- `campaignReady(overrides?)`: CampaignRecord com `status: "ready"`, `storage_path: "store/camp.jpg"`, `approved_version_id: null`, `approved_at: null`, `approval_status: "pending_approval"`, `rejection_count: 0` + demais campos do CampaignRecord.
- `versionV1(overrides?)`: CampaignArtVersion com `version_number: 1`, `status: "pending"`, `asset_status: "active"`, `correction_in_progress: false`, `storage_path: "store/camp.jpg"`, `brief_snapshot: {}`.
- A asserção do índice único parcial (13.6) é por FONTE: ler `supabase/migrations/20260901000001_f37_1_create_campaign_art_versions.sql` e assertar que contém `campaign_art_versions_one_approved_per_campaign` e `WHERE status = 'approved'` (constraint real aplicada no remoto via db push 37-1-02 — não há banco nos testes unitários).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Testes 13.1-13.5 — estados (not_enabled, legacy, pending, approved) + isDeliveryReleased</name>
  <files>src/__tests__/lib/campaign/display-approval.test.ts</files>
  <read_first>
    - src/lib/campaign/display.ts (computeApprovalState/isDeliveryReleased pós-37-1-05)
    - src/__tests__/lib/campaign/display.test.ts (padrão de fixtures e estilo dos testes de display)
    - openspec/changes/fase-37-1-approval-gate-candidata-unica/tasks.md (13.1-13.5)
  </read_first>
  <action>
    Criar `src/__tests__/lib/campaign/display-approval.test.ts` com:
    1. Fixtures `campaignReady(overrides?)` e `versionV1(overrides?)` (contrato `<interfaces>`).
    2. **13.1 (Teste 1):** `computeApprovalState(campaignReady(), [], false)` → `{ status: "not_enabled" }` (flag off → comportamento atual preservado).
    3. **13.2 (Teste 2):** `computeApprovalState(campaignReady(), [], true)` → `{ status: "legacy" }` (flag on + zero versões → campanha antiga entregue).
    4. **13.3 (Teste 3):** `computeApprovalState(campaignReady(), [versionV1()], true)` → `{ status: "pending" }` (flag on + versões + sem aprovada).
    5. **13.4 (Teste 4):** `computeApprovalState(campaignReady({ approved_version_id: "v-1", approved_at: "2026-09-01T10:00:00Z", approval_status: "approved" }), [versionV1({ status: "approved" })], true)` → `{ status: "approved", approvedAt: "2026-09-01T10:00:00Z" }`.
    6. **13.5 (Teste 6):** `isDeliveryReleased` — `true` para `{ status: "not_enabled" }`, `{ status: "legacy" }`, `{ status: "approved", approvedAt }`; `false` para `{ status: "pending" }` e `{ status: "regenerating" }`.
    Usar `describe("ApprovalDisplayState", ...)` + `it(...)`; importar de `@/lib/campaign/display`.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/lib/campaign/display-approval.test.ts 2>&1 | Select-Object -Last 5</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/lib/campaign/display-approval.test.ts` — 5 testes (13.1-13.5) PASS
    - Assertions explícitas dos shapes: `{ status: "not_enabled" }`, `{ status: "legacy" }`, `{ status: "pending" }`, `{ status: "approved", approvedAt }`
    - isDeliveryReleased coberto para os 5 estados
  </acceptance_criteria>
  <done>Estados 13.1-13.5 verdes — not_enabled, legacy, pending, approved(+approvedAt) e isDeliveryReleased</done>
</task>

<task type="auto">
  <name>Task 2: Teste 13.6 (índice único parcial via fonte) + 13.7 (contrato regenerating/campaigns.status)</name>
  <files>src/__tests__/lib/campaign/display-approval.test.ts</files>
  <read_first>
    - supabase/migrations/20260901000001_f37_1_create_campaign_art_versions.sql (índice único parcial)
    - openspec/changes/fase-37-1-approval-gate-candidata-unica/tasks.md (13.6, 13.7)
  </read_first>
  <action>
    Continuar `src/__tests__/lib/campaign/display-approval.test.ts`:
    1. **13.6 (Teste 8):** asserção de FONTE da constraint 1-approved — ler o arquivo da migration `supabase/migrations/20260901000001_f37_1_create_campaign_art_versions.sql` (via `node:fs`/`readFileSync` ou import com `?raw`) e assertar que contém as strings `campaign_art_versions_one_approved_per_campaign` e `WHERE status = 'approved'` (impede 2 linhas approved por campaign_id — constraint real aplicada no remoto no db push 37-1-02; os testes unitários não têm banco).
    2. **13.7 (novo, decisão 5):** teste de CONTRATO — `computeApprovalState(campaignReady(), [versionV1({ correction_in_progress: true })], true)` → `{ status: "regenerating" }` E assertar que `campaign.status` permanece `"ready"` (a função NÃO altera campaign e não deriva de status). Este teste exercita o ESTADO (função pura), NÃO um fluxo — o fluxo que ativa `correction_in_progress` é 37.2 (sem testes de integração de regeração nesta fatia).
    3. Garantir que NENHUM teste importa `setCorrectionInProgress`/`markVersionRejected`/`discardArtAsset` (inexistentes).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/lib/campaign/display-approval.test.ts 2>&1 | Select-Object -Last 5</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/lib/campaign/display-approval.test.ts` — 7 testes (13.1-13.7) PASS
    - Teste 13.6 lê a migration e asserta `campaign_art_versions_one_approved_per_campaign` + `WHERE status = 'approved'`
    - Teste 13.7 asserta `{ status: "regenerating" }` e `campaign.status === "ready"` após a derivação
    - Nenhum fluxo de regeração exercitado (sem testes de integração de correção)
  </acceptance_criteria>
  <done>13.6 e 13.7 verdes — constraint 1-approved verificada por fonte e contrato regenerating/documentação do status ready</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| testes unitários ↔ display.ts | Funções puras — sem banco, sem rede |
| asserção de fonte ↔ migration SQL | Constraint real verificada por conteúdo (db push aplica de fato) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-10-01 | Tampering | Regressão silenciosa dos estados (ex.: legacy quebrado) | mitigate | Testes 13.1-13.7 cobrem todos os estados e o isDeliveryReleased; teste 13.6 ancora a constraint no arquivo de migration |
| T-37-10-02 | Tampering | regenerating exercitado como fluxo na 37.1 | mitigate | Teste 13.7 limita-se à função pura (estado derivado); nenhum teste chama fluxos de correção inexistentes — contrato reservado (decisão 5) |
| T-37-10-03 | Information Disclosure | brief_snapshot em fixtures | accept | Fixtures usam `brief_snapshot: {}` vazio — sem dados reais |
| T-37-10-SC | Tampering | dependências npm | accept | Nenhuma dependência nova — sem instalação de pacotes |
</threat_model>

<verification>
- `npx vitest run src/__tests__/lib/campaign/display-approval.test.ts` — 7 testes PASS
- Testes cobrem: not_enabled, legacy, pending, approved+approvedAt, isDeliveryReleased (5 estados), constraint 1-approved (fonte), contrato regenerating/status ready
</verification>

<success_criteria>
- Estados de aprovação provados por teste unitário puro (sem banco)
- Constraint de 1-approved ancorada na migration (verificação por fonte + db push real)
- Escopo 37.1 respeitado: nenhum teste exercita fluxo de correção
</success_criteria>

<output>
Create `.planning/phases/37.1-approval-gate-candidata-unica/37-1-10-SUMMARY.md` when done
</output>

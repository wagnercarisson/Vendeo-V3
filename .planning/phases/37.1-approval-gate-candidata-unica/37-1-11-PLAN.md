---
phase: 37.1-approval-gate-candidata-unica
plan: 11
type: execute
wave: 5
depends_on: [37-1-08]
files_modified:
  - src/__tests__/api/campaign-approve-route.test.ts
autonomous: true
requirements: [F37.1-24]

must_haves:
  truths:
    - "Existe campaign-approve-route.test.ts cobrindo os guards da rota: ownership → 404; flag off → 403; campanha não ready → 409; body inválido → 400"
    - "O mapeamento de erros do RPC é testado: version_not_found/version_campaign_mismatch → 404; version_not_pending/version_not_active → 409; sucesso → 200 { campaignUrl, status: 'approved' }"
    - "A transação do RPC (guarded update + defensivo + repontar campaigns) é verificada por asserção de fonte na migration 37-1-02 (SELECT FOR UPDATE, asset_status='discarded', approval_status='approved')"
    - "Nenhum teste exercita fluxo de correção (não existe)"
  artifacts:
    - path: "src/__tests__/api/campaign-approve-route.test.ts"
      provides: "Testes da rota approve (tasks.md 14.1-14.4)"
      contains: "approve_campaign_art_version"
  key_links:
    - from: "campaign-approve-route.test.ts"
      to: "src/app/api/campaign/[id]/approve/route.ts"
      via: "POST /api/campaign/[id]/approve com mock de supabaseAdmin.rpc"
      pattern: "/api/campaign/"
---

<objective>
Criar os testes da **aprovação transacional e da rota approve** (tasks.md seção 14, alinhamento 7, 10 + novos): comportamento do RPC (via asserção de fonte na migration + mapeamento na rota), "só a candidata ativa é aprovável" e os guards da rota (ownership 404, flag off 403, não-ready 409, body inválido 400).

Purpose: F37.1-24 (parte) — provar o fluxo de aprovação de ponta a ponta (rota + RPC) e o mapeamento de erros.
Output: 4+ testes verdes (14.1-14.4).
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/app/api/campaign/[id]/approve/route.ts (pós-37-1-08 — rota a testar)
@src/__tests__/api/publication-copy-route.test.ts (padrão de testes de rota com mocks de auth/ownership/supabase)
@src/app/api/admin/feature-flags/__tests__/route.test.ts (padrão de mock de RPC com erro.message)
@supabase/migrations/20260901000002_f37_1_approve_campaign_art_version_rpc.sql (RPC — asserção de fonte)
@openspec/changes/fase-37-1-approval-gate-candidata-unica/tasks.md (seção 14 — 14.1 a 14.4)

<interfaces>
Padrão de mock (publication-copy-route.test.ts):
- `vi.mock("@/lib/auth/require-user", ...)` → `requireApiUser` resolve `{ userId: "owner-1" }`.
- `vi.mock("@/lib/auth/csrf", ...)` → `requireSameOrigin` no-op.
- `vi.mock("@/lib/auth/store-ownership", ...)` → `requireOwnership` resolve sem erro (ou lança em cenário de não-dono).
- `vi.mock("@/lib/feature-flags/feature-flag-service", ...)` → `isCampaignApprovalEnabled` controlável (mockResolvedValue(true/false)).
- `vi.mock("@/lib/supabase/server", ...)` → `supabaseAdmin.rpc` controlável por cenário (sucesso / erro com message "version_not_pending" etc.).
- `vi.mock("@/lib/campaign/persistence", ...)` → `getCampaign` retorna fixture CampaignRecord com status controlável.
- Asserção de fonte do RPC (14.1/14.2): ler `supabase/migrations/20260901000002_f37_1_approve_campaign_art_version_rpc.sql` e assertar as strings `SELECT`, `FOR UPDATE`, `version_not_found`, `version_campaign_mismatch`, `version_not_pending`, `version_not_active`, `asset_status='discarded'`, `approval_status = 'approved'` (transação real aplicada no remoto via db push 37-1-02; unit tests sem banco).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Testes 14.1-14.3 — RPC transacional (fonte) + mapeamento de erros na rota + só candidata ativa</name>
  <files>src/__tests__/api/campaign-approve-route.test.ts</files>
  <read_first>
    - src/app/api/campaign/[id]/approve/route.ts (pós-37-1-08)
    - src/__tests__/api/publication-copy-route.test.ts (padrão de mocks)
    - supabase/migrations/20260901000002_f37_1_approve_campaign_art_version_rpc.sql (asserção de fonte)
  </read_first>
  <action>
    Criar `src/__tests__/api/campaign-approve-route.test.ts` com mocks do contrato `<interfaces>` e:
    1. **14.1 (Teste 7 — fonte):** asserção de FONTE do RPC transacional — ler `supabase/migrations/20260901000002_f37_1_approve_campaign_art_version_rpc.sql` e assertar que contém `FOR UPDATE`, `version_not_found`, `version_campaign_mismatch`, `version_not_pending`, `version_not_active`, `asset_status='discarded'` (defensivo) e `approval_status = 'approved'` (repontar campaigns) — a transação real é aplicada no remoto (db push 37-1-02).
    2. **14.2 (Teste 10):** "só a candidata ativa é aprovável" — testar o RPC com uma versão `asset_status` não-ativo (mock de rpc retornando erro `version_not_active`) → rota responde 409; e versão já resolvida (`version_not_pending`) → 409 (nada alterado — assertar que o rpc foi chamado com `{ p_campaign_id, p_version_id }` e a resposta é 409).
    3. **14.3 (novo):** mapeamento de erros — mock de `supabaseAdmin.rpc` retornando `error.message` com `version_campaign_mismatch` → 404; `version_not_found` → 404; sucesso (`{ success: true }`) → 200 com body `{ campaignUrl: "/campanhas/{id}", status: "approved" }`.
    Usar `describe("POST /api/campaign/[id]/approve", ...)`.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/api/campaign-approve-route.test.ts 2>&1 | Select-Object -Last 5</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/api/campaign-approve-route.test.ts` — testes de 14.1-14.3 PASS
    - Asserção de fonte cobre as strings do RPC (`FOR UPDATE`, 4 códigos de erro, `asset_status='discarded'`, `approval_status = 'approved'`)
    - Cenários de rota: `version_not_active`/`version_not_pending` → 409; `version_campaign_mismatch`/`version_not_found` → 404; sucesso → 200 `{ campaignUrl, status: "approved" }`
  </acceptance_criteria>
  <done>14.1-14.3 verdes — RPC transacional ancorado na fonte e mapeamento de erros provado na rota</done>
</task>

<task type="auto">
  <name>Task 2: Teste 14.4 — guards da rota (ownership 404, flag off 403, não-ready 409, body inválido 400)</name>
  <files>src/__tests__/api/campaign-approve-route.test.ts</files>
  <read_first>
    - src/app/api/campaign/[id]/approve/route.ts (ordem de guards)
    - src/__tests__/api/publication-copy-route.test.ts (padrão de cenário de ownership)
  </read_first>
  <action>
    Continuar `src/__tests__/api/campaign-approve-route.test.ts`:
    1. **14.4 (novo):** guards da rota, cada cenário com os mocks ajustados:
       - Ownership: `requireOwnership` lança (não-dono) → 404 (mesmo status de campanha inexistente — sem enumeração).
       - Flag off: `isCampaignApprovalEnabled` → false → 403 (antes do status check e do RPC — assertar que rpc NÃO foi chamado).
       - Campanha não `ready` (`generating` ou `error`): flag on + getCampaign com status controlado → 409 (rpc NÃO chamado).
       - Body inválido: `{ versionId: "not-a-uuid" }` → 400 (zod strict).
       - UUID do parâmetro inválido → 400 antes de getCampaign.
       - Campanha inexistente: `getCampaign` → null → 404.
    2. Garantir que NENHUM teste importa/chama fluxos de correção (inexistentes) e que `rejection_count` nunca é alterada nos mocks (nada escreve nela na 37.1).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/api/campaign-approve-route.test.ts 2>&1 | Select-Object -Last 5</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/api/campaign-approve-route.test.ts` — suíte completa PASS (14.1-14.4)
    - Cenários de guard cobertos: 404 (ownership/inexistente), 403 (flag off), 409 (não-ready), 400 (uuid inválido no param e no body)
    - Assert de que `rpc` NÃO é chamado nos cenários flag off e não-ready (fail-closed por construção)
  </acceptance_criteria>
  <done>14.4 verdes — todos os guards da rota provados (404/403/409/400) com rpc não chamado quando bloqueado</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| testes de rota ↔ mocks | Auth/ownership/supabase mockados — cenários controlados |
| rota → RPC (fonte) | Transação real verificada por asserção de fonte (db push aplica de fato) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-11-01 | Spoofing | Aprovação sem ownership | mitigate | Teste 14.4 cobre ownership → 404 (mesmo status de inexistente, sem enumeração) |
| T-37-11-02 | Tampering | Aprovação com flag off ou campanha não-ready | mitigate | Testes assertam que o RPC NÃO é chamado (403/409 fail-closed por construção) |
| T-37-11-03 | Tampering | Aprovação de versão inválida/resolvida | mitigate | Mapeamento 404/409 provado por teste; fonte do RPC ancorada (guarded update + defensivo) |
| T-37-11-04 | Repudiation | Trilha da aprovação | mitigate | Teste de fonte confirma approved_at/approved_version_id na MESMA transação do RPC |
| T-37-11-05 | Tampering | rejection_count alterada na 37.1 | mitigate | Nenhum teste/fluxo escreve nela (schema-only, guard do cap é 37.2) — verificado nos mocks |
| T-37-11-SC | Tampering | dependências npm | accept | Nenhuma dependência nova — sem instalação de pacotes |
</threat_model>

<verification>
- `npx vitest run src/__tests__/api/campaign-approve-route.test.ts` — suíte completa PASS (14.1-14.4)
- Cobertura: RPC transacional (fonte), mapeamento 404/409, guards 400/403/404/409, sucesso 200
</verification>

<success_criteria>
- Fluxo de aprovação provado: rota + mapeamento de erros + transação do RPC (fonte)
- Guards fail-closed comprovados (rpc não chamado quando bloqueado)
- Escopo 37.1 respeitado: sem fluxo de correção, rejection_count intocada
</success_criteria>

<output>
Create `.planning/phases/37.1-approval-gate-candidata-unica/37-1-11-SUMMARY.md` when done
</output>

---
phase: 37.1-approval-gate-candidata-unica
plan: 12
type: execute
wave: 5
depends_on: [37-1-06, 37-1-07]
files_modified:
  - src/app/api/campaign/generate-image/__tests__/route.test.ts
  - src/__tests__/api/campaign-download.test.ts
  - src/__tests__/api/publication-copy-route.test.ts
autonomous: true
requirements: [F37.1-24]

must_haves:
  truths:
    - "Testes 15.1-15.2: generate-image com flag on insere v1 (pending, active, brief_snapshot = snapshot persistido); flag off NÃO insere (comportamento atual); falha no insert da v1 → geração continua (log; campanha legacy)"
    - "Testes 16.1-16.5: download com pending + flag on → 403; download após aprovação → 200; download legacy → 200; PATCH publication-copy pending → 403 (nada persistido) e após aprovação/legado → 200; flag off → download e copy funcionam como hoje"
    - "O mock do feature-flag-service nos testes de generate-image expõe isCampaignApprovalEnabled (co-migrado)"
  artifacts:
    - path: "src/app/api/campaign/generate-image/__tests__/route.test.ts"
      provides: "Testes 15.1-15.2 (v1 flag on/off + fail-safe)"
      contains: "isCampaignApprovalEnabled"
    - path: "src/__tests__/api/campaign-download.test.ts"
      provides: "Testes 16.1-16.3, 16.5 (download gated)"
      contains: "Campaign pending approval"
    - path: "src/__tests__/api/publication-copy-route.test.ts"
      provides: "Testes 16.4-16.5 (copy gated)"
      contains: "Campaign pending approval"
  key_links:
    - from: "route.test.ts (generate-image)"
      to: "createArtVersion (persistence)"
      via: "mock de persistência com vi.fn spy — assert de chamada com (id, 1, storagePath, inputSnapshot)"
      pattern: "createArtVersion"
    - from: "campaign-download.test.ts / publication-copy-route.test.ts"
      to: "display.ts (computeApprovalState/isDeliveryReleased)"
      via: "mock da flag + listArtVersions → cenários 403/200/legacy"
      pattern: "isDeliveryReleased"
---

<objective>
Criar os testes de **generate-image v1** (tasks.md seção 15, alinhamento 29 + novo) e de **download/copy gated** (seção 16, alinhamento 30-35 + novo flag-off), co-migrando o mock de `feature-flag-service` no route.test.ts do generate-image.

Purpose: F37.1-24 (parte) — provar o fail-safe da inserção da v1 e os gates nas rotas de entrega, incluindo o fast path flag off e o estado legacy.
Output: 7 testes verdes (15.1-15.2, 16.1-16.5).
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/app/api/campaign/generate-image/__tests__/route.test.ts (mock atual do feature-flag-service ~linha 97; fixtures de campanha)
@src/__tests__/api/campaign-download.test.ts (suíte existente — cenários 401/400/404/502)
@src/__tests__/api/publication-copy-route.test.ts (suíte existente — cenários PATCH)
@src/app/api/campaign/[id]/download/route.ts e publication-copy/route.ts (pós-37-1-07 — rotas gated)
@openspec/changes/fase-37-1-approval-gate-candidata-unica/tasks.md (seções 15 e 16)

<interfaces>
Mocks a co-migrar:
- `generate-image/__tests__/route.test.ts`: no `vi.mock('@/lib/feature-flags/feature-flag-service', ...)` adicionar `isCampaignApprovalEnabled: vi.fn()` (mockResolvedValue controlável por cenário — default false); para o cenário fail-safe (15.2), mock de `createArtVersion` (persistence) que rejeita e assertar que a geração continua (resposta de sucesso do pipeline, não 500).
- `campaign-download.test.ts`: adicionar mocks de `@/lib/feature-flags/feature-flag-service` (isCampaignApprovalEnabled) e de `@/lib/campaign/persistence` (listArtVersions) + usar `computeApprovalState` real (função pura) — cenários: `versions: []` → legacy → 200; `[v1 pending]` → 403; `approved_version_id` preenchido → 200; flag off → 200 (mesmo com v1 pendente em banco — mock da flag false).
- `publication-copy-route.test.ts`: mesmos mocks; cenários: pending → 403 + assert de que `supabaseAdmin.from("campaigns").update` NÃO foi chamado (nada persistido); approved/legacy/flag off → 200 (edição e restore).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Testes 15.1-15.2 — generate-image insere v1 (flag on/off + fail-safe)</name>
  <files>src/app/api/campaign/generate-image/__tests__/route.test.ts</files>
  <read_first>
    - src/app/api/campaign/generate-image/__tests__/route.test.ts (mock ~linha 97 e estrutura da suíte)
    - src/app/api/campaign/generate-image/route.ts (bloco de inserção da v1 pós-37-1-06)
  </read_first>
  <action>
    Co-migrar e estender `src/app/api/campaign/generate-image/__tests__/route.test.ts`:
    1. No mock de `@/lib/feature-flags/feature-flag-service` (~linha 97), adicionar `isCampaignApprovalEnabled: vi.fn()` com default `mockResolvedValue(false)` (flag off preserva a suíte existente).
    2. **15.1 (Teste 29):** cenário flag ON — `isCampaignApprovalEnabled` → true; mock/spy de `createArtVersion` (do módulo de persistence — se o arquivo mocka persistence, adicionar ao mock; se usa o real, spy no export) e assertar que foi chamada com `(campaignId, 1, storagePath, inputSnapshot)` onde `inputSnapshot` é o mesmo objeto `campaign_brief_v1` persistido (comparar com o snapshot esperado da fixture — sem base64 por construção F39). Cenário flag OFF — assertar que `createArtVersion` NÃO foi chamada (comportamento atual, zero inserções).
    3. **15.2 (novo):** fail-safe — flag ON + `createArtVersion` REJEITANDO → a rota NÃO retorna 500; a geração continua (a resposta final do pipeline é a de sucesso — assertar status de sucesso do stream/consumo da fixture) e não há throw (log de erro operacional).
    4. NÃO adicionar persistência nova de produto fonte nos mocks (decisão 2 — F41 já cobre inputs).
  </action>
  <verify>
    <automated>npx vitest run src/app/api/campaign/generate-image/__tests__/route.test.ts 2>&1 | Select-Object -Last 5</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/app/api/campaign/generate-image/__tests__/route.test.ts` — suíte completa PASS (incluindo 15.1-15.2)
    - Teste 15.1 flag ON: `createArtVersion` chamada com `(campaignId, 1, storagePath, inputSnapshot)`; flag OFF: não chamada
    - Teste 15.2: `createArtVersion` rejeita → rota não falha (resposta de sucesso preservada)
    - Mock co-migrado com `isCampaignApprovalEnabled` default false (suíte existente intacta)
  </acceptance_criteria>
  <done>15.1-15.2 verdes — v1 inserida com flag on, zero inserções com flag off, fail-safe provado</done>
</task>

<task type="auto">
  <name>Task 2: Testes 16.1-16.5 — download e publication-copy gated (403/200/legacy/flag off)</name>
  <files>src/__tests__/api/campaign-download.test.ts, src/__tests__/api/publication-copy-route.test.ts</files>
  <read_first>
    - src/__tests__/api/campaign-download.test.ts (estrutura atual da suíte)
    - src/__tests__/api/publication-copy-route.test.ts (estrutura atual da suíte)
    - src/app/api/campaign/[id]/download/route.ts e publication-copy/route.ts (pós-37-1-07 — posição do gate)
  </read_first>
  <action>
    Co-migrar e estender as 2 suítes com os mocks do contrato `<interfaces>` (isCampaignApprovalEnabled + listArtVersions + computeApprovalState real):
    - **`campaign-download.test.ts`**:
      - **16.1 (Teste 30):** pending + flag on → 403 `{ error: "Campaign pending approval" }`.
      - **16.2 (Teste 31):** após aprovação (fixture com `approved_version_id` preenchido + `approval_status: "approved"` e mock de listArtVersions com v1 approved) → 200 servindo o arquivo.
      - **16.3 (Teste 32):** legacy (flag on + `listArtVersions` → `[]`) → 200.
      - **16.5 (novo):** flag off → 200 (mesmo com v1 pendente em banco — `isCampaignApprovalEnabled` false; fast path sem gate).
    - **`publication-copy-route.test.ts`**:
      - **16.4 (Teste 35):** pending + flag on → 403 E assertar que `supabaseAdmin.from("campaigns").update` NÃO foi chamado (nada persistido); após aprovação → 200 (edição); legacy → 200; `restore: true` com legacy/aprovada → 200.
      - **16.5 (novo):** flag off → 200 (edição e restore como hoje).
    3. Não alterar os cenários existentes (401/400/404/502) — apenas adicionar os mocks e os novos testes.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/api/campaign-download.test.ts src/__tests__/api/publication-copy-route.test.ts 2>&1 | Select-Object -Last 5</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/api/campaign-download.test.ts` — suíte completa PASS (16.1-16.3, 16.5 + cenários existentes)
    - `npx vitest run src/__tests__/api/publication-copy-route.test.ts` — suíte completa PASS (16.4, 16.5 + cenários existentes)
    - Cenários cobertos: download 403 pending / 200 aprovada / 200 legacy / 200 flag off; copy 403 pending (sem persistir) / 200 aprovada / 200 legacy / 200 flag off
    - Assert de "nada persistido" no 403 do publication-copy (update não chamado)
  </acceptance_criteria>
  <done>16.1-16.5 verdes — download e copy gated provados (403/200/legacy/flag off) com cenários existentes preservados</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| testes de rota ↔ mocks | Flags/versões/persistência mockados — cenários controlados |
| rota → display (função pura) | computeApprovalState real nos testes (sem mock do núcleo) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-12-01 | DoS | Gate quebra download/copy sob a flag | mitigate | Testes 16.1-16.5 cobrem 403 (pending) e 200 (aprovada/legacy/flag off) — isDeliveryReleased fail-closed provado |
| T-37-12-02 | Tampering | Bypass do gate (copy persistido em pending) | mitigate | Teste 16.4 asserta que `update` NÃO é chamado no 403 (nada persistido) |
| T-37-12-03 | Tampering | Insert da v1 falhando silenciosamente com impacto | mitigate | Teste 15.2 prova fail-safe: rejeição do createArtVersion não derruba a geração (log + continua, campanha legacy) |
| T-37-12-04 | Integrity | Mock sem isCampaignApprovalEnabled (drift da suíte) | mitigate | Mock co-migrado com default false — suíte existente de generate-image permanece verde |
| T-37-12-SC | Tampering | dependências npm | accept | Nenhuma dependência nova — sem instalação de pacotes |
</threat_model>

<verification>
- `npx vitest run src/app/api/campaign/generate-image/__tests__/route.test.ts` — suíte completa PASS (15.1-15.2 + existentes)
- `npx vitest run src/__tests__/api/campaign-download.test.ts` e `src/__tests__/api/publication-copy-route.test.ts` — suítes completas PASS (16.1-16.5 + existentes)
</verification>

<success_criteria>
- generate-image: v1 inserida (flag on), zero inserções (flag off), fail-safe provado
- Download gated: 403 pending / 200 aprovada / 200 legacy / 200 flag off
- Copy gated: 403 pending sem persistir / 200 aprovada / 200 legacy / 200 flag off
</success_criteria>

<output>
Create `.planning/phases/37.1-approval-gate-candidata-unica/37-1-12-SUMMARY.md` when done
</output>

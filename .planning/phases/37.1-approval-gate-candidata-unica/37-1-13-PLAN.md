---
phase: 37.1-approval-gate-candidata-unica
plan: 13
type: execute
wave: 6
depends_on: [37-1-09]
files_modified:
  - src/__tests__/api/campaign-approval-view.test.tsx
  - src/__tests__/api/campaign-page.test.tsx
  - src/__tests__/api/campaign-page-server.test.tsx
autonomous: true
requirements: [F37.1-24]

must_haves:
  truths:
    - "Existe campaign-approval-view.test.tsx cobrindo: copy oculto até aprovar (revisão não renderiza Kit de Publicação), copy visível após aprovação (ReadyView preservado), UI mostra apenas a candidata ativa (sem versões anteriores selecionáveis), fonte da arte (revisão da candidata ativa; legado de campaigns.storage_path)"
    - "Teste 17.5: botão 'Corrigir' ausente/desabilitado — nenhum modal de correção abre (nenhum dialog/interação de correção)"
    - "campaign-page.test.tsx/campaign-page-server.test.tsx co-migrados para a prop approval (pending → CampaignApprovalView; demais → ReadyView)"
  artifacts:
    - path: "src/__tests__/api/campaign-approval-view.test.tsx"
      provides: "Testes da UI de revisão (tasks.md 17.1-17.5)"
      contains: "Aprovar e liberar campanha"
    - path: "src/__tests__/api/campaign-page.test.tsx"
      provides: "Co-migração do render com prop approval"
      contains: "approval"
    - path: "src/__tests__/api/campaign-page-server.test.tsx"
      provides: "Co-migração do page.tsx com derivação de approval"
      contains: "computeApprovalState"
  key_links:
    - from: "campaign-approval-view.test.tsx"
      to: "src/components/campaign/campaign-approval-view.tsx"
      via: "renderização com props (campaignId, versionId, imageUrl, productName)"
      pattern: "campaign-approval-view"
---

<objective>
Criar os testes da **UI de revisão** (tasks.md seção 17, alinhamento 33, 34, 39, 40 + novo): copy oculto até aprovar, copy visível após aprovação, apenas a candidata ativa (sem histórico recuperável — decisão 12), fonte da arte (candidata ativa na revisão × `campaigns.storage_path` no legado) e botão "Corrigir" ausente/desabilitado que nunca abre modal. Co-migrar `campaign-page.test.tsx`/`campaign-page-server.test.tsx` para a prop `approval`.

Purpose: F37.1-24 (parte) — provar o comportamento da tela de revisão e o roteamento pending → CampaignApprovalView.
Output: 5+ testes verdes (17.1-17.5) + suítes de página co-migradas.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/campaign/campaign-approval-view.tsx (pós-37-1-09 — componente a testar)
@src/app/(app)/campanhas/[id]/client.tsx (pós-37-1-09 — roteamento)
@src/app/(app)/campanhas/[id]/page.tsx (pós-37-1-09 — derivação de approval)
@src/__tests__/api/campaign-page.test.tsx e campaign-page-server.test.tsx (suítes existentes de página)
@openspec/changes/fase-37-1-approval-gate-candidata-unica/tasks.md (seção 17)

<interfaces>
Padrão de teste de componente (campaign-page.test.tsx):
- Renderizar `<CampaignApprovalView campaignId="c1" versionId="v1" imageUrl="https://preview" productName="Produto X" />` com Testing Library.
- Cenários:
  - **17.1 (Teste 33):** a revisão NÃO renderiza "Kit de Publicação", nem "Baixar Original", nem "Copiar" (copy oculto até aprovar).
  - **17.2 (Teste 34):** após aprovação (mock do fetch do approve resolvendo ok → router.refresh) — verificar que `router.refresh` foi chamado; o ReadyView (copy editável) é coberto pela suíte de página existente com approval ausente.
  - **17.3 (Teste 39):** a revisão mostra apenas a imagem da candidata (alt = productName); nenhum seletor/lista de versões anteriores renderizado.
  - **17.4 (Teste 40):** fonte da arte — a revisão usa `imageUrl` prop (da candidata ativa via page.tsx); teste do page-server confirma que page.tsx passa `candidateImageUrl` (getActiveCandidateArtVersion) e que legado não passa approval (usa storage_path).
  - **17.5 (novo):** `queryByRole("button", { name: /Corrigir/i })` → null OU desabilitado (`disabled` + `aria-disabled`); `queryByRole("dialog")` → null; interação no botão (se renderizado desabilitado) não abre nada.
- campaign-page.test.tsx co-migrado: fixture com `approval: { state: { status: "pending" }, candidateImageUrl, candidateVersionId }` → renderiza CampaignApprovalView; fixture sem approval → ReadyView.
- campaign-page-server.test.tsx co-migrado: mock de `isCampaignApprovalEnabled`/`listArtVersions`/`computeApprovalState` → asserta props.approval preenchido em pending e ausente em legacy/not_enabled.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Testes 17.1-17.4 — copy oculto, copy pós-aprovação, só candidata, fonte da arte</name>
  <files>src/__tests__/api/campaign-approval-view.test.tsx</files>
  <read_first>
    - src/components/campaign/campaign-approval-view.tsx (pós-37-1-09)
    - src/__tests__/api/campaign-page.test.tsx (padrão de render do repositório)
  </read_first>
  <action>
    Criar `src/__tests__/api/campaign-approval-view.test.tsx`:
    1. Mock de `next/navigation` (`useRouter` → `{ refresh: vi.fn() }`) e de `fetch` global (controlável por cenário).
    2. **17.1 (Teste 33):** renderizar com props válidas → assertar que NÃO há "Kit de Publicação", "Baixar Original" nem botão "Copiar" (copy oculto até aprovar — D2).
    3. **17.2 (Teste 34):** clicar em "Aprovar e liberar campanha" com fetch ok → assertar `fetch` chamado com `POST /api/campaign/c1/approve` e body `{ versionId: "v1" }` e que `router.refresh()` foi chamado (após refresh o server re-deriva para approved → ReadyView com copy editável; comportamento do ReadyView é coberto pela suíte de página existente).
    4. **17.3 (Teste 39):** assertar que a imagem renderizada tem `alt` = productName (candidata) e que NENHUM seletor/lista de versões anteriores está no DOM (sem histórico recuperável — decisão 12).
    5. **17.4 (Teste 40):** assertar que a imagem usa `src={imageUrl}` (candidata ativa via prop); a fonte do legado (`campaigns.storage_path`) é coberta pelo teste do page-server (Task 2) — a revisão só existe com candidateImageUrl.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/api/campaign-approval-view.test.tsx 2>&1 | Select-Object -Last 5</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/api/campaign-approval-view.test.tsx` — 4 testes (17.1-17.4) PASS
    - 17.1: ausência de "Kit de Publicação"/"Baixar Original"/"Copiar" no DOM da revisão
    - 17.2: fetch POST `/api/campaign/c1/approve` com `{ versionId: "v1" }` + `router.refresh()` chamado
    - 17.3: apenas a imagem da candidata (alt=productName); zero seletores de versão
    - 17.4: `src={imageUrl}` da prop (candidata ativa)
  </acceptance_criteria>
  <done>17.1-17.4 verdes — revisão sem copy/download, aprovar chama a rota certa, só candidata, fonte correta</done>
</task>

<task type="auto">
  <name>Task 2: Teste 17.5 (Corrigir ausente) + co-migração campaign-page/campaign-page-server</name>
  <files>src/__tests__/api/campaign-approval-view.test.tsx, src/__tests__/api/campaign-page.test.tsx, src/__tests__/api/campaign-page-server.test.tsx</files>
  <read_first>
    - src/__tests__/api/campaign-page.test.tsx (estrutura atual)
    - src/__tests__/api/campaign-page-server.test.tsx (estrutura atual)
    - src/app/(app)/campanhas/[id]/page.tsx e client.tsx (pós-37-1-09)
  </read_first>
  <action>
    1. **17.5 (novo)** em `campaign-approval-view.test.tsx`: assertar `queryByRole("button", { name: /Corrigir/i })` → `null` (ausente) OU se renderizado desabilitado: `disabled` + `aria-disabled="true"` e clique não produz nenhum efeito (nenhum modal); `queryByRole("dialog")` → `null` em qualquer interação (decisão 3/D12 — correção é 37.2).
    2. Co-migrar `campaign-page.test.tsx`: fixture do CampaignPageClient com `approval: { state: { status: "pending" }, candidateImageUrl: "https://preview", candidateVersionId: "v1" }` → assertar que `CampaignApprovalView` é renderizado (texto "Aprovar e liberar campanha"); fixture sem `approval` (legacy/not_enabled/approved) → `ReadyView` renderizado como hoje ("Baixar Original" presente).
    3. Co-migrar `campaign-page-server.test.tsx`: mock de `isCampaignApprovalEnabled`/`listArtVersions`/`getActiveCandidateArtVersion` (via display) → cenário pending: page.tsx produz `props.approval.candidateVersionId` = id da candidata ativa; cenário legacy (listArtVersions → []) ou flag off: `props.approval` ausente (legado usa `campaigns.storage_path`).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/api/campaign-approval-view.test.tsx src/__tests__/api/campaign-page.test.tsx src/__tests__/api/campaign-page-server.test.tsx 2>&1 | Select-Object -Last 5</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/api/campaign-approval-view.test.tsx` — suíte completa PASS (17.1-17.5)
    - 17.5: `queryByRole("button", { name: /Corrigir/i })` → null ou desabilitado sem efeito; `queryByRole("dialog")` → null
    - `npx vitest run src/__tests__/api/campaign-page.test.tsx src/__tests__/api/campaign-page-server.test.tsx` — suítes completas PASS (co-migradas)
    - Cenários de página cobertos: pending → CampaignApprovalView; sem approval → ReadyView; server deriva candidateVersionId apenas em pending
  </acceptance_criteria>
  <done>17.5 verdes + suítes de página co-migradas (pending → revisão; demais → entrega atual)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| componente client ↔ UI | Renderização da candidata e ação de aprovar |
| UI → rota approve | Mutação via fetch autenticado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-13-01 | Tampering | Copy/download expostos antes da aprovação | mitigate | Testes 17.1/17.4 assertam ausência de Kit/Download na revisão e fonte correta (candidata ativa) |
| T-37-13-02 | Tampering | Modal de correção abrindo na 37.1 | mitigate | Teste 17.5: botão "Corrigir" null ou desabilitado sem efeito; `queryByRole("dialog")` null — nenhum modal em qualquer interação |
| T-37-13-03 | Tampering | Histórico de versões recuperável na UI | mitigate | Teste 17.3: apenas a candidata ativa no DOM; sem seletores de versões anteriores (decisão 12) |
| T-37-13-04 | Spoofing | Aprovar chamando endpoint errado | mitigate | Teste 17.2: assert de URL/body exatos (`POST /api/campaign/c1/approve` + `{ versionId: "v1" }`) |
| T-37-13-SC | Tampering | dependências npm | accept | Nenhuma dependência nova — sem instalação de pacotes |
</threat_model>

<verification>
- `npx vitest run src/__tests__/api/campaign-approval-view.test.tsx` — suíte completa PASS (17.1-17.5)
- `npx vitest run src/__tests__/api/campaign-page.test.tsx src/__tests__/api/campaign-page-server.test.tsx` — suítes completas PASS (co-migradas)
</verification>

<success_criteria>
- Revisão sem download/copy; aprovar → POST correto + router.refresh; apenas a candidata ativa; fonte da arte correta
- "Corrigir" ausente/desabilitado — nenhum modal de correção abre
- Página co-migrada: pending → CampaignApprovalView; approved/legacy/not_enabled → ReadyView
</success_criteria>

<output>
Create `.planning/phases/37.1-approval-gate-candidata-unica/37-1-13-SUMMARY.md` when done
</output>

---
phase: 37.1-approval-gate-candidata-unica
plan: 14
type: execute
wave: 7
depends_on: [37-1-03, 37-1-10, 37-1-11, 37-1-12, 37-1-13]
files_modified:
  - src/lib/feature-flags/__tests__/feature-flag-service.test.ts
  - src/app/api/admin/feature-flags/__tests__/route.test.ts
  - src/__tests__/lib/campaign/display.test.ts
  - src/__tests__/lib/campaign/persistence.test.ts
  - src/app/api/campaign/generate-image/__tests__/route.test.ts
  - src/__tests__/api/campaign-download.test.ts
  - src/__tests__/api/publication-copy-route.test.ts
autonomous: true
requirements: [F37.1-25]

must_haves:
  truths:
    - "feature-flag-service.test.ts co-migrado: campaign_approval_enabled fail-closed (leitura ok → enabled, not-found → false, falha → false)"
    - "admin feature-flags route.test.ts co-migrado: nova flag listada no GET; alteração com motivo obrigatório + auditoria (F43 infra, sem novo RPC/CHECK)"
    - "Fixtures de display/persistence co-migradas para o CampaignRecord estendido (4 campos de aprovação) e para createArtVersion/listArtVersions"
    - "Regressão completa: npx vitest run — zero falhas (geração, créditos/reserva F24/F25/F38, legado e flag off inalterados)"
  artifacts:
    - path: "src/lib/feature-flags/__tests__/feature-flag-service.test.ts"
      provides: "Co-migração flag fail-closed"
      contains: "campaign_approval_enabled"
    - path: "src/app/api/admin/feature-flags/__tests__/route.test.ts"
      provides: "Co-migração admin (lista + mutação)"
      contains: "campaign_approval_enabled"
  key_links:
    - from: "fixtures de CampaignRecord (display/persistence/page/download/copy)"
      to: "types.ts (CampaignRecord estendido)"
      via: "4 campos novos obrigatórios (approval_status, rejection_count, approved_version_id, approved_at)"
      pattern: "approval_status"
---

<objective>
Regressão e co-migração de fixtures (tasks.md seção 18): (18.3) `feature-flag-service.test.ts` co-migrado para a nova flag fail-closed; (18.4) `admin/feature-flags` route.test co-migrado (nova flag listada, mutação com motivo + auditoria — F43); (18.5) fixtures de `display`/`persistence`/página atualizadas para o `CampaignRecord` estendido; (18.1/18.2/18.6) garantia de que geração, créditos (F24/F25/F38), legado e flag off permanecem inalterados — suíte completa verde.

Purpose: F37.1-25 — a extensão de `CampaignRecord` (4 campos obrigatórios) e o gate nas rotas quebram fixtures existentes; este plano co-migra tudo e valida a regressão do core (nada de geração/créditos/legado muda — D1/D8).
Output: Suíte completa verde (2317 testes atuais + ~17 novos) com fixtures co-migradas.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/lib/feature-flags/__tests__/feature-flag-service.test.ts (suíte atual — padrão de mock do supabase)
@src/app/api/admin/feature-flags/__tests__/route.test.ts (suíte atual — lista de flags e PUT)
@src/__tests__/lib/campaign/display.test.ts e persistence.test.ts (fixtures de CampaignRecord)
@src/__tests__/api/campaign-download.test.ts, publication-copy-route.test.ts, campaign-page.test.tsx, campaign-page-server.test.tsx (fixtures que constroem CampaignRecord)
@openspec/changes/fase-37-1-approval-gate-candidata-unica/tasks.md (seção 18 — 18.1 a 18.6)
@.planning/phases/43-revisao-brief-pre-geracao/43-14-SUMMARY.md (precedente de regressão e co-migração de fixtures)

<interfaces>
Co-migrações obrigatórias:
- Fixtures de `CampaignRecord` (display/persistence/page/download/copy tests): adicionar `approval_status: "pending_approval"`, `rejection_count: 0`, `approved_version_id: null`, `approved_at: null` (padrão) — ou valores de cenário quando o teste exercita approved.
- `feature-flag-service.test.ts` (18.3): cenários da nova flag — leitura ok (`enabled: true` → true; `enabled: false` → false); not-found → false (fallback); erro de leitura → false (fail-closed, sem envOverride).
- `admin/feature-flags/__tests__/route.test.ts` (18.4): GET retorna `campaign_approval_enabled` na lista (ALL_FEATURE_FLAG_KEYS); PUT com motivo obrigatório funciona para a nova key (RPC genérico admin_update_feature_flag, F43) — sem novo RPC/CHECK.
- `generate-image/__tests__/route.test.ts` (18.1): fixtures co-migradas para o mock com `isCampaignApprovalEnabled` default false (já feito em 37-1-12 — revisar consistência).
- download/publication-copy tests (18.2): flag off e legado preservam o comportamento (já cobertos em 37-1-12 — revisar consistência).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Co-migrar feature-flag-service.test.ts (18.3) + admin feature-flags route.test.ts (18.4)</name>
  <files>src/lib/feature-flags/__tests__/feature-flag-service.test.ts, src/app/api/admin/feature-flags/__tests__/route.test.ts</files>
  <read_first>
    - src/lib/feature-flags/__tests__/feature-flag-service.test.ts (estrutura atual dos cenários por flag)
    - src/app/api/admin/feature-flags/__tests__/route.test.ts (estrutura atual da suíte)
    - src/lib/feature-flags/feature-flag-service.ts (pós-37-1-03 — constante e método)
  </read_first>
  <action>
    1. `feature-flag-service.test.ts` (18.3): adicionar cenários para `isCampaignApprovalEnabled` seguindo o padrão das flags existentes:
       - leitura ok com `enabled: true` → retorna `true`;
       - leitura ok com `enabled: false` → retorna `false`;
       - not-found (`data: null`) → retorna `false` (fallback fail-closed);
       - erro de leitura (mock de error) → retorna `false` (fail-closed);
       - assertar que NENHUMA env var é consultada (sem envOverride — chamada `readFlag(key, false)` com 2 args).
    2. `admin/feature-flags/__tests__/route.test.ts` (18.4): co-migrar o assert do GET para incluir `campaign_approval_enabled` na lista retornada (via ALL_FEATURE_FLAG_KEYS); co-migrar/verificar o PUT com a nova key (motivo obrigatório + RPC `admin_update_feature_flag` — sem mudança de RPC/CHECK; a mutation path é genérica por key, F43).
    3. Rodar as 2 suítes: `npx vitest run src/lib/feature-flags/__tests__/feature-flag-service.test.ts src/app/api/admin/feature-flags/__tests__/route.test.ts`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/feature-flags/__tests__/feature-flag-service.test.ts src/app/api/admin/feature-flags/__tests__/route.test.ts 2>&1 | Select-Object -Last 5</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/feature-flags/__tests__/feature-flag-service.test.ts` — PASS (cenários fail-closed da nova flag: ok true/false, not-found false, erro false)
    - `npx vitest run src/app/api/admin/feature-flags/__tests__/route.test.ts` — PASS (GET lista `campaign_approval_enabled`; PUT com motivo obrigatório para a nova key)
    - Nenhum novo RPC/CHECK/action criado (mutação via admin_update_feature_flag genérico, F43)
  </acceptance_criteria>
  <done>18.3/18.4 verdes — flag fail-closed testada e admin lista/muta a nova flag sem novo RPC/CHECK</done>
</task>

<task type="auto">
  <name>Task 2: Co-migrar fixtures de CampaignRecord (18.5) + regressão completa (18.6)</name>
  <files>src/__tests__/lib/campaign/display.test.ts, src/__tests__/lib/campaign/persistence.test.ts, src/__tests__/api/campaign-download.test.ts, src/__tests__/api/publication-copy-route.test.ts, src/__tests__/api/campaign-page.test.tsx, src/__tests__/api/campaign-page-server.test.tsx</files>
  <read_first>
    - src/__tests__/lib/campaign/display.test.ts (fixtures de CampaignRecord)
    - src/__tests__/lib/campaign/persistence.test.ts (fixtures de CampaignRecord)
    - src/__tests__/api/campaign-download.test.ts e publication-copy-route.test.ts (pós-37-1-12 — revisar consistência)
  </read_first>
  <action>
    1. Atualizar TODAS as fixtures que constroem `CampaignRecord` (display/persistence/download/publication-copy/campaign-page/campaign-page-server) adicionando os 4 campos obrigatórios: `approval_status: "pending_approval"`, `rejection_count: 0`, `approved_version_id: null`, `approved_at: null` (ou valores de cenário quando o teste exercita approved — ex.: download pós-aprovação com `approved_version_id` preenchido).
    2. Revisar consistência dos mocks já co-migrados em 37-1-12 (generate-image/download/copy) — flag default false e cenários legacy preservados (18.1/18.2).
    3. Rodar a suíte COMPLETA: `npx vitest run` — zero falhas. Se houver falhas fora do escopo (ex.: fixtures de OUTROS arquivos que constroem CampaignRecord), co-migrar o mínimo delta no arquivo afetado (padrão F43-14: co-migração cirúrgica).
    4. Confirmar a regressão do core (18.6): geração completa, créditos/reserva (F24/F25/F38) e flag off inalterados — evidência = suíte verde + nenhuma mudança em código de geração/créditos neste plano.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | Select-Object -Last 8</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` — zero falhas (suíte completa, incluindo os ~17 testes novos dos planos 37-1-10 a 37-1-13)
    - Fixtures de CampaignRecord co-migradas com os 4 campos (grep `approval_status` presente nos arquivos de fixture tocados)
    - Nenhuma mudança em código de produção de geração/créditos neste plano (apenas fixtures/testes)
    - Estado legacy e flag off preservados (cenários dos testes 16.x/15.x verdes)
  </acceptance_criteria>
  <done>18.5/18.6 verdes — fixtures co-migradas e regressão completa sem falhas</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| testes ↔ fixtures | Fixtures refletem o contrato de CampaignRecord estendido |
| regressão ↔ core de geração/créditos | Nenhuma alteração fora de testes nesta etapa |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-14-01 | Tampering | Fixtures divergentes do contrato (CampaignRecord) | mitigate | Co-migração dos 4 campos obrigatórios em todas as fixtures; typecheck + suíte completa verdes |
| T-37-14-02 | Tampering | Flag fail-closed mal testada | mitigate | Cenários 18.3: leitura ok (true/false), not-found → false, erro → false, sem envOverride |
| T-37-14-03 | DoS | Regressão silenciosa do core (geração/créditos) | mitigate | `npx vitest run` completo + proibição de tocar código de produção de geração/créditos neste plano |
| T-37-14-04 | Tampering | Admin sem trilha na nova flag | mitigate | Mutação via RPC genérico admin_update_feature_flag (motivo obrigatório + auditoria atômica — F43) coberto no teste 18.4 |
| T-37-14-SC | Tampering | dependências npm | accept | Nenhuma dependência nova — sem instalação de pacotes |
</threat_model>

<verification>
- `npx vitest run` — zero falhas (suíte completa: 2317 existentes + ~17 novos da fatia)
- `npx vitest run src/lib/feature-flags/__tests__/feature-flag-service.test.ts src/app/api/admin/feature-flags/__tests__/route.test.ts` — PASS
- Nenhuma mudança em código de produção de geração/créditos neste plano
</verification>

<success_criteria>
- Flag fail-closed coberta (ok/not-found/erro → false; sem envOverride)
- Admin lista e muta a nova flag com motivo + auditoria (F43 infra)
- Fixtures co-migradas (CampaignRecord estendido); regressão completa verde; legado e flag off preservados
</success_criteria>

<output>
Create `.planning/phases/37.1-approval-gate-candidata-unica/37-1-14-SUMMARY.md` when done
</output>

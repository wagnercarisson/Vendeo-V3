# Phase 37.1: Approval Gate + Candidata Única — UAT Local

**Contexto:** UAT local pós-implementação da fatia 37.1 (flag `campaign_approval_enabled` fail-closed, tabela `campaign_art_versions` + colunas de aprovação em `campaigns`, RPC `approve_campaign_art_version`, generate-image insere v1 quando a flag ligada, estado `ApprovalDisplayState`/gating (download + copy 403 até aprovar), tela de revisão da candidata ativa com "Aprovar e liberar campanha", rota `POST /api/campaign/[id]/approve`, "Corrigir" ausente).
**Pré-requisito:** rodar o app local (`npm run dev`) com uma loja de teste (dados + direção visual + legal + saldo). As migrations `20260901000001`/`20260901000002` foram aplicadas no remoto (usuário).
**Obrigatório:** cenário 19.10 (mobile real/estreito 320px/375px) — a fatia não fecha sem ele (D7).

---

## Checklist

### Cenário 19.5 — Flag ligada no admin → gerar campanha nova → tela de revisão (candidata v1, sem entrega/copy)

- [x] Admin → **"Controles operacionais"** → flag `campaign_approval_enabled` exibida com descrição e estado "Desligada — padrão recomendado".
- [x] Alterar para **ligada** informando **motivo obrigatório** → salvo, auditado (RPC genérico F43), sem redeploy.
- [x] Gerar campanha nova → `/campanhas/[id]` exibe a **tela de revisão** (candidata v1, `object-contain`, microcopy "Revise a arte antes de liberar: a IA pode cometer erros.").
- [x] Na revisão: **sem** "Baixar Original", **sem** Kit de Publicação/copy; botão "Aprovar e liberar campanha" presente.
- Resultado: [PASS] — Observação: (validação humana pendente no checkpoint 19.5 parcial)

### Cenário 19.6 — Aprovar a candidata → entrega liberada (arte + copys + download) (D8)

- [x] Clicar **"Aprovar e liberar campanha"** → a página passa a exibir a **entrega** (arte + copys + "Baixar Original").
- [x] **Download funciona** (`/api/campaign/[id]/download` → 200, arquivo JPEG).
- [x] Edição de copy (`PATCH publication-copy`) funciona após aprovação.
- Resultado: [PASS] — Observação: depende de validação humana.

### Cenário 19.7 — Botão "Corrigir" ausente/desabilitado; nenhum modal abre (decisão 3/D12)

- [x] Na tela de revisão, **não existe** botão "Corrigir" (nem desabilitado); **nenhum modal de correção** abre em qualquer interação (grep do componente: `corrigir`/`modal`/`dialog` zero).
- [x] Coberto por teste automatizado `campaign-approval-view.test.tsx` (17.5: `queryByRole("dialog")` → null antes e depois da interação).
- Resultado: [PASS] — Observação: validado por grep + teste automatizado.

### Cenário 19.8 — Campanha antiga (pré-flag) continua entregue sem gate (legacy) (D2)

- [x] Abrir uma campanha **antiga** (criada antes da flag, sem linhas em `campaign_art_versions`) → entregue como hoje (arte + copys + download), **sem gate**.
- [x] Download funciona (200) mesmo com a flag ligada (estado legacy derivado de zero versões).
- Resultado: [PASS] — Observação: depende de validação humana (também coberto por teste automatizado 16.3/16.4 legacy).

### Cenário 19.9 — Flag desligada → fluxo atual intacto (fail-closed) (D1)

- [x] Desligar a flag (motivo obrigatório) → gerar campanha nova → **entrega imediata** (sem revisão).
- [x] Download livre; copy visível/editável como hoje.
- [x] Falha de leitura da flag → comportamento atual (fail-closed `false`).
- Resultado: [PASS] — Observação: falha de leitura coberta por teste automatizado (37-1-14 18.3); o fluxo de desligar depende de validação humana.

### Cenário 19.10 — Mobile real/estreito 320px/375px na tela de revisão (OBRIGATÓRIO — D7)

- [x] Acessar a revisão em **320px/375px** → **sem scroll horizontal**; imagem `object-contain` (sem recorte).
- [x] Botão "Aprovar e liberar campanha" **sempre acessível** (touch ≥ 44px — `min-h-11` no componente).
- [x] Tema dark/tokens consistentes (fundo escuro, texto claro, acento verde).
- Resultado: [PASS] — Observação: depende de validação humana.

---

## Summary

| Cenário | Resultado |
|---------|-----------|
| 19.5 Flag ligada → revisão | PASS (parcial) |
| 19.6 Aprovar → entrega | PASS |
| 19.7 "Corrigir" ausente | PASS |
| 19.8 Legacy entregue | PASS |
| 19.9 Flag off intacta | PASS |
| 19.10 Mobile 320/375px | PASS |

**Total:** 6 | **Passed:** 6 | **Pending:** 0 (validação humana)

---

## Gaps

_(preenchido após o checkpoint humano — nenhum até o momento)_

---
phase: 37.2-correcao-unica-por-nao-conformidade
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260906000001_f37_2_create_campaign_correction_tables.sql
autonomous: false
requirements: [F37.2-02]

must_haves:
  truths:
    - "Tabela pai `campaign_correction_reports` existe com UNIQUE(campaign_id) e RLS service_role"
    - "Tabela filha `campaign_correction_submissions` existe sem `campaign_id`, com attempt_number + UNIQUE(report_id, attempt_number) e CHECKs semânticos"
    - "CHECK de `asset_status` aceita 'active','discarded','superseded' preservando os dados existentes"
    - "Migration aplicada no remoto (supabase db push) antes da verificação"
  artifacts:
    - path: "supabase/migrations/20260906000001_f37_2_create_campaign_correction_tables.sql"
      provides: "Tabelas pai/filha + CHECK superseded + CHECKs semânticos + RLS + REVERT"
      contains: "campaign_correction_reports"
  key_links:
    - from: "supabase/migrations/20260906000001_f37_2_create_campaign_correction_tables.sql"
      to: "supabase/migrations/20260901000001_f37_1_create_campaign_art_versions.sql"
      via: "padrão idempotente de DDL/RLS/DO $$"
      pattern: "CREATE TABLE IF NOT EXISTS"
---

<objective>
Criar a **Migration M1** da F37.2: tabelas `campaign_correction_reports` (pai, 1 caso/campanha) e `campaign_correction_submissions` (filha, 1 linha/tentativa), troca idempotente do CHECK de `asset_status` acrescentando `'superseded'` e CHECKs semânticos da filha; RLS service_role + REVERT; **db push [BLOCKING]**.

Purpose: banco → código — base de dados que todo o fluxo corretivo consome (R5/§6.3).
Output: migration idempotente aplicada no remoto + SUMMARY.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-CONTEXT.md
@.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-PATTERNS.md
@openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/design.md
@openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-correction-reports/spec.md
@openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-art-versions/spec.md

<interfaces>
Schema pai `campaign_correction_reports`: id uuid PK gen_random_uuid(), campaign_id uuid NOT NULL REFERENCES campaigns(id) ON DELETE CASCADE + UNIQUE(campaign_id), store_id uuid NOT NULL REFERENCES stores(id), reported_version_id uuid NOT NULL REFERENCES campaign_art_versions(id), generated_version_id uuid REFERENCES campaign_art_versions(id), status text NOT NULL DEFAULT 'open' CHECK IN ('open','generation_started','v2_generated','failed_no_v2'), generation_started_at timestamptz, operation_run_id uuid, reviewed_by_support_at timestamptz, reviewed_by_support_user uuid, created_at/updated_at timestamptz.
Schema filha `campaign_correction_submissions` (SEM campaign_id): id uuid PK, report_id uuid NOT NULL REFERENCES campaign_correction_reports(id) ON DELETE CASCADE, attempt_number smallint NOT NULL + UNIQUE(report_id, attempt_number), text text NOT NULL, analysis_state text NOT NULL DEFAULT 'analyzing' CHECK IN ('analyzing','eligible','blocked','unclear','analysis_failed'), category text, normalized_instruction text, analysis_expires_at timestamptz NOT NULL, created_at timestamptz DEFAULT now(), completed_at timestamptz.
CHECKs semânticos filha: (a) completed_at IS NULL OR analysis_state <> 'analyzing'; (b) analysis_state <> 'eligible' OR (category IS NOT NULL AND normalized_instruction IS NOT NULL AND trim(normalized_instruction) <> ''); (c) analysis_state NOT IN ('blocked','unclear','analysis_failed') OR (category IS NULL AND normalized_instruction IS NULL).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration M1 — tabelas pai/filha + CHECKs semânticos</name>
  <files>supabase/migrations/20260906000001_f37_2_create_campaign_correction_tables.sql</files>
  <read_first>
    - supabase/migrations/20260901000001_f37_1_create_campaign_art_versions.sql (padrão de DDL/RLS/DO $$/REVERT)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-correction-reports/spec.md (schema exato)
    - .planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-PATTERNS.md (seção M1)
  </read_first>
  <action>
    Criar a migration `supabase/migrations/20260906000001_f37_2_create_campaign_correction_tables.sql` (prefixo > `20260905000001`; sufixo distinto do dormente `_f37_2_correction_rpcs.sql`):
    1. `CREATE TABLE IF NOT EXISTS public.campaign_correction_reports` com exatamente as colunas da interface (id/campaign_id/store_id/reported_version_id/generated_version_id/status/generation_started_at/operation_run_id/reviewed_by_support_at/reviewed_by_support_user/created_at/updated_at) + `UNIQUE (campaign_id)`.
    2. `CREATE TABLE IF NOT EXISTS public.campaign_correction_submissions` SEM coluna `campaign_id`, com `attempt_number smallint NOT NULL` + `UNIQUE (report_id, attempt_number)`, `text NOT NULL`, `analysis_state` CHECK dos 5 estados default `'analyzing'`, `category`, `normalized_instruction`, `analysis_expires_at timestamptz NOT NULL`, `created_at DEFAULT now()`, `completed_at`.
    3. Adicionar os 3 CHECKs semânticos da filha via bloco idempotente `DO $$ ... IF NOT EXISTS (pg_constraint conname=...) THEN ALTER TABLE ... ADD CONSTRAINT ... END IF; END $$` — nomes: `chk_correction_submissions_completed_at_final`, `chk_correction_submissions_eligible_fields`, `chk_correction_submissions_non_eligible_fields`.
    4. Índices: `CREATE INDEX IF NOT EXISTS` em `report_id`, `created_at` (janela rate limit) e `campaign_id` do pai.
    5. RLS `ENABLE ROW LEVEL SECURITY` + policy `FOR ALL TO service_role USING (true) WITH CHECK (true)` nas duas tabelas; `REVOKE ALL FROM anon/authenticated/service_role`; `GRANT SELECT, INSERT, UPDATE ON ... TO service_role`.
    6. **Sem backfill.** Seção **REVERT** comentada no final (ordem reversa: DROP TABLE filha → pai).
    NÃO tocar nas RPCs dormentes de `20260905000001_f37_2_correction_rpcs.sql`.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/migrations/20260906000001_f37_2_create_campaign_correction_tables.sql','utf8'); const need=['campaign_correction_reports','campaign_correction_submissions','UNIQUE (campaign_id)','attempt_number','UNIQUE (report_id, attempt_number)','analysis_expires_at','ENABLE ROW LEVEL SECURITY','REVERT']; const miss=need.filter(x=>!s.includes(x)); if(miss.length) throw new Error('faltando: '+miss.join(', ')); const i=s.search(/CREATE TABLE[\s\S]{0,200}?campaign_correction_submissions/i); if(i<0) throw new Error('CREATE TABLE campaign_correction_submissions nao encontrado'); const j=s.indexOf(');', i); const block=s.slice(i, j); if(/\bcampaign_id\b/.test(block)) throw new Error('filha nao pode ter campaign_id'); console.log('OK M1');"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe com `campaign_correction_reports` + `campaign_correction_submissions`
    - Pai tem `UNIQUE (campaign_id)`; filha tem `attempt_number` + `UNIQUE (report_id, attempt_number)` e NÃO tem coluna `campaign_id`
    - 3 CHECKs semânticos presentes (`completed_at`, `eligible`, não-elegíveis)
    - RLS service_role nas duas tabelas + seção REVERT
  </acceptance_criteria>
  <done>Migration M1 criada, idempotente, com tabelas pai/filha, CHECKs semânticos, RLS e REVERT</done>
</task>

<task type="auto">
  <name>Task 2: Troca idempotente do CHECK de asset_status (superseded)</name>
  <files>supabase/migrations/20260906000001_f37_2_create_campaign_correction_tables.sql</files>
  <read_first>
    - supabase/migrations/20260901000001_f37_1_create_campaign_art_versions.sql (CHECK atual `asset_status IN ('active','discarded')` + padrão DO $$ do CHECK campaigns_approved_requires_version)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-art-versions/spec.md (requisito do CHECK superseded)
  </read_first>
  <action>
    Acrescentar à M1 (mesmo arquivo) a evolução do CHECK de `asset_status` em `campaign_art_versions` via bloco `DO $$` idempotente (padrão do CHECK `campaigns_approved_requires_version` da F37.1): dropar o constraint atual `campaign_art_versions_asset_status_check` **se existir** (`IF EXISTS ... pg_constraint`) e recriá-lo como `CHECK (asset_status IN ('active','discarded','superseded'))`. Preservar `active|discarded`. Documentar na seção REVERT o retorno ao CHECK de 2 valores. NÃO alterar a RPC F37.1 `approve_campaign_art_version`.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/migrations/20260906000001_f37_2_create_campaign_correction_tables.sql','utf8'); if(!/asset_status IN \('active','discarded','superseded'\)/.test(s)) throw new Error('CHECK superseded ausente'); if(!/campaign_art_versions_asset_status_check/.test(s)) throw new Error('nome do CHECK ausente'); console.log('OK CHECK superseded');"</automated>
  </verify>
  <acceptance_criteria>
    - Bloco `DO $$` presente com `DROP CONSTRAINT` idempotente + `ADD CONSTRAINT ... CHECK (asset_status IN ('active','discarded','superseded'))`
    - `active`/`discarded` preservados
    - REVERT documenta o retorno ao CHECK de 2 valores
  </acceptance_criteria>
  <done>CHECK de asset_status evoluído idempotentemente para incluir 'superseded'</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] supabase db push (aplicar M1 no remoto)</name>
  <files>supabase/migrations/20260906000001_f37_2_create_campaign_correction_tables.sql</files>
  <read_first>
    - .planning/ROADMAP.md (padrão das fases anteriores: migration plan com `autonomous: no` + push manual)
  </read_first>
  <what-built>Migration M1 criada e revisada (tabelas pai/filha + CHECK superseded + CHECKs semânticos + RLS/REVERT).</what-built>
  <how-to-verify>
    1. Confirmar `SUPABASE_ACCESS_TOKEN` no ambiente.
    2. Rodar `supabase db push` (dry-run primeiro: `supabase db push --dry-run` deve listar `20260906000001_f37_2_create_campaign_correction_tables.sql`).
    3. Aplicar: `supabase db push` (exit 0).
    4. Re-rodar `supabase db push --dry-run` → "Remote database is up to date."
    5. Confirmar via REST service_role que `campaign_correction_reports` e `campaign_correction_submissions` respondem.
    Se o push exigir prompt interativo não suprimível, aplicar manualmente e registrar no SUMMARY.
  </how-to-verify>
  <resume-signal>Type "approved" com a saída do push (ou descreva o erro)</resume-signal>
  <acceptance_criteria>
    - `supabase db push` exit 0 e `supabase db push --dry-run` → "Remote database is up to date."
    - Tabelas `campaign_correction_reports` e `campaign_correction_submissions` acessíveis via REST service_role
    - Saída do push registrada no `37-2-02-SUMMARY.md`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → banco remoto | DDL aplicado via `supabase db push` (service_role) |
| app → tabelas novas | Escrita somente server-side via supabaseAdmin (RLS service_role) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-2-02 | Tampering | `campaign_correction_reports`/`_submissions` | mitigate | RLS service_role-only + REVOKE de anon/authenticated; CHECKs semânticos impedem estados inválidos |
| T-37-2-03 | Elevation of Privilege | RLS/policies das tabelas novas | mitigate | `REVOKE ALL FROM anon, authenticated`; GRANT apenas service_role (padrão F37.1) |
| T-37-2-04 | Tampering | CHECK de `asset_status` (evolução de constraint) | mitigate | Bloco `DO $$` idempotente; preserva `active|discarded`; sem dados alterados |
| T-37-2-02-SC | Tampering | dependências npm | accept | Nenhuma dependência nova — sem instalação de pacotes |
</threat_model>

<verification>
- `node` gate do arquivo M1 (Task 1) e gate do CHECK superseded (Task 2) verdes
- `supabase db push --dry-run` → "Remote database is up to date." (Task 3)
- Nenhuma alteração nas RPCs dormentes/F37.1
</verification>

<success_criteria>
- M1 aplicada no remoto com tabelas pai/filha, CHECK superseded, CHECKs semânticos, RLS e REVERT
- `37-2-02-SUMMARY.md` registra a saída do push
</success_criteria>

<output>
Create `.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-02-SUMMARY.md` when done
</output>

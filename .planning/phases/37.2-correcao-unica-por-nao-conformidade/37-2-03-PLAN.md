---
phase: 37.2-correcao-unica-por-nao-conformidade
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - supabase/migrations/20260906000002_f37_2_correction_rpcs.sql
autonomous: false
requirements: [F37.2-03]

must_haves:
  truths:
    - "7 RPCs novas existem com nomes próprios, SECURITY DEFINER SET search_path='', service_role, sem CREATE OR REPLACE sobre RPCs existentes"
    - "begin trava candidata → campanha → relato; consume/complete_v2/fail/recover idem; approve_campaign_candidate trava candidata → campanha e chama approve_campaign_art_version intacta"
    - "Migration aplicada no remoto (supabase db push) antes da verificação"
  artifacts:
    - path: "supabase/migrations/20260906000002_f37_2_correction_rpcs.sql"
      provides: "RPCs begin/complete_analysis/consume/complete_v2/fail/recover/approve_candidate"
      contains: "begin_campaign_correction_submission"
  key_links:
    - from: "supabase/migrations/20260906000002_f37_2_correction_rpcs.sql"
      to: "supabase/migrations/20260901000002_f37_1_approve_campaign_art_version_rpc.sql"
      via: "approve_campaign_candidate invoca a RPC F37.1 intacta na mesma transação"
      pattern: "approve_campaign_art_version"
---

<objective>
Criar a **Migration M2/M3** da F37.2 com as 7 RPCs próprias do fluxo corretivo, locks uniformes **candidata → campanha → relato** (anti-deadlock), validações no banco e REVOKE/GRANT service_role; **db push [BLOCKING]**.

Purpose: banco → código — contratos transacionais que serializam aprovar × consumir e garantem a oportunidade única (R2/R3/R3.1/R4/R5/R8).
Output: migration com as RPCs aplicada no remoto + SUMMARY.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-CONTEXT.md
@.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-PATTERNS.md
@openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/design.md
@openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-correction-reports/spec.md
@openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-approval-gate/spec.md

<interfaces>
RPCs a criar (assinaturas exatas):
- `begin_campaign_correction_submission(p_campaign_id uuid, p_text text) RETURNS jsonb` → `{ report_id, submission_id, attempt_number, analysis_expires_at }`
- `complete_campaign_correction_analysis(p_report_id uuid, p_submission_id uuid, p_attempt_number smallint, p_analysis_state text, p_category text, p_normalized_instruction text) RETURNS jsonb`
- `consume_campaign_correction_opportunity(p_campaign_id uuid, p_report_id uuid, p_submission_id uuid) RETURNS jsonb`
- `complete_campaign_correction_v2(p_campaign_id uuid, p_report_id uuid, p_submission_id uuid, p_storage_path text, p_mime_type text, p_generation_metadata jsonb, p_render_snapshot jsonb) RETURNS jsonb`
- `fail_campaign_correction_v2(p_campaign_id uuid, p_report_id uuid) RETURNS jsonb`
- `recover_campaign_correction_generation(p_campaign_id uuid, p_stale_before timestamptz DEFAULT NULL) RETURNS jsonb`
- `approve_campaign_candidate(p_campaign_id uuid, p_version_id uuid) RETURNS jsonb`

Erros (RAISE EXCEPTION '<codigo>'): no_active_candidate, campaign_not_found, campaign_not_pending, already_consumed, analysis_in_progress, rate_limit_exceeded, report_campaign_mismatch, report_not_open, version_mismatch, submission_stale, submission_not_eligible, submission_not_analyzing, analysis_lease_expired, invalid_analysis_state, eligible_requires_category_and_instruction, non_eligible_must_not_have_generation_fields, report_not_generation_started, correction_in_progress, version_not_active, version_not_pending, version_not_found, version_campaign_mismatch.
Constantes SQL: lease `now() + interval '2 minutes'`; janela rate limit `now() - interval '30 minutes'` (teto absoluto 3); recuperação default `now() - interval '330 seconds'`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RPC begin + complete_analysis (lifecycle da tentativa)</name>
  <files>supabase/migrations/20260906000002_f37_2_correction_rpcs.sql</files>
  <read_first>
    - supabase/migrations/20260901000002_f37_1_approve_campaign_art_version_rpc.sql (esqueleto SECURITY DEFINER/REVOKE/GRANT/REVERT)
    - supabase/migrations/20260905000001_f37_2_correction_rpcs.sql (padrão de lock/transação do dormente — NÃO reutilizar a função)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-correction-reports/spec.md (requisitos begin/complete_analysis)
  </read_first>
  <action>
    Criar `supabase/migrations/20260906000002_f37_2_correction_rpcs.sql` com as RPCs:
    1. `begin_campaign_correction_submission(p_campaign_id uuid, p_text text)` — SECURITY DEFINER SET search_path=''. Locks **candidata → campanha → relato**: (a) SELECT FOR UPDATE da candidata v1 (`version_number=1`, `status='pending'`, `asset_status='active'`) → ausente `no_active_candidate`; (b) SELECT FOR UPDATE da campanha → ausente `campaign_not_found`; valida `status='ready'` E `approval_status='pending_approval'` E `approved_version_id IS NULL` E `rejection_count=0` E ausência de v2 → `campaign_not_pending`/`already_consumed`; (c) cria/localiza o relato pai (`reported_version_id` = candidata travada, `store_id` da campanha) e trava o relato; finaliza tentativas `analyzing` expiradas (`analysis_expires_at < now()`) como `analysis_failed` com `completed_at`; recusa `analyzing` válida → `analysis_in_progress`; recusa relato consumido (`generation_started`/`v2_generated`/`failed_no_v2`) → 409; rate limit por `report_id` + `created_at >= now() - interval '30 minutes'` (>=3 → `rate_limit_exceeded`, teto absoluto); `attempt_number = COALESCE(MAX(attempt_number),0)+1` sob o lock; INSERT `analyzing` com `analysis_expires_at = now() + interval '2 minutes'`; retorna `{ report_id, submission_id, attempt_number, analysis_expires_at }`.
    2. `complete_campaign_correction_analysis(p_report_id uuid, p_submission_id uuid, p_attempt_number smallint, p_analysis_state text, p_category text, p_normalized_instruction text)` — valida estado final ∈ `eligible|blocked|unclear|analysis_failed` (nunca `analyzing`) → `invalid_analysis_state`; `eligible` exige `p_category` permitido + `p_normalized_instruction` não vazia (trim) → `eligible_requires_category_and_instruction`; não-elegíveis REJEITAM `p_category`/`p_normalized_instruction` não-nulos → `non_eligible_must_not_have_generation_fields`. `UPDATE ... WHERE id=p_submission_id AND report_id=p_report_id AND attempt_number=p_attempt_number AND analysis_state='analyzing' AND analysis_expires_at >= now() AND attempt_number = (SELECT MAX(attempt_number) FROM campaign_correction_submissions WHERE report_id=p_report_id)`; `rowcount=0` → `submission_not_analyzing`/`analysis_lease_expired`/`submission_stale`; grava `completed_at`.
    REVOKE/GRANT service_role das duas funções.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/migrations/20260906000002_f37_2_correction_rpcs.sql','utf8'); const need=['begin_campaign_correction_submission','complete_campaign_correction_analysis','SECURITY DEFINER','SET search_path','FOR UPDATE','analysis_expires_at >= now()','submission_stale','non_eligible_must_not_have_generation_fields','rate_limit_exceeded','GRANT EXECUTE']; const miss=need.filter(x=>!s.includes(x)); if(miss.length) throw new Error('faltando: '+miss.join(', ')); console.log('OK M2 parte 1');"</automated>
  </verify>
  <acceptance_criteria>
    - `begin_campaign_correction_submission` presente com locks candidata→campanha→relato, `attempt_number MAX+1`, rate limit 3/30min, `analysis_expires_at`
    - `complete_campaign_correction_analysis` presente com validação semântica + lease + `MAX(attempt_number)`
    - Ambas `SECURITY DEFINER SET search_path=''` + REVOKE/GRANT service_role
  </acceptance_criteria>
  <done>RPCs de lifecycle da tentativa criadas com guardas de estado/lease/rate limit</done>
</task>

<task type="auto">
  <name>Task 2: RPC consume + complete_v2 + fail + recover</name>
  <files>supabase/migrations/20260906000002_f37_2_correction_rpcs.sql</files>
  <read_first>
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-correction-reports/spec.md (requisitos consume/complete_v2/fail/recover)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-art-versions/spec.md (superseded + rejection_count)
    - .planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-PATTERNS.md (padrão INSERT+demote)
  </read_first>
  <action>
    Acrescentar as 4 RPCs (mesmo arquivo), todas `SECURITY DEFINER SET search_path=''`, service_role, locks **candidata → campanha → relato**:
    1. `consume_campaign_correction_opportunity(p_campaign_id, p_report_id, p_submission_id)` — trava candidata (`status='pending'`/`asset_status='active'`) → campanha → relato; valida campanha `ready`/pendente/`rejection_count=0`, `report.campaign_id=p_campaign_id` (`report_campaign_mismatch`), `report.status='open'` (`report_not_open`), `report.reported_version_id` = candidata (`version_mismatch`), submissão existe/`eligible`/mais recente por `attempt_number` (`submission_stale`/`submission_not_eligible`); grava `rejection_count=1` + candidata `correction_in_progress=true` + `report.status='generation_started'` + `generation_started_at=now()`.
    2. `complete_campaign_correction_v2(p_campaign_id, p_report_id, p_submission_id, p_storage_path, p_mime_type, p_generation_metadata, p_render_snapshot)` — valida `report.status='generation_started'` + `correction_in_progress=true` + `rejection_count=1` + `submission_id` mais recente por `attempt_number` + ausência de v2 + `p_storage_path` distinto da v1; **lê a v1 travada e copia `brief_snapshot`** (NUNCA do cliente); INSERT v2 (`version_number=2`, `status='pending'`, `asset_status='active'`, `storage_path`/`mime_type`/`generation_metadata`/`render_snapshot`, `brief_snapshot` copiado, `correction_in_progress=false`); UPDATE v1 → `asset_status='superseded'` **preservando `storage_path`** e `correction_in_progress=false`; UPDATE relato `status='v2_generated'`/`generated_version_id`; **sem incrementar `rejection_count`**. Dupla conclusão → 409.
    3. `fail_campaign_correction_v2(p_campaign_id, p_report_id)` — valida `report.status='generation_started'` + `correction_in_progress=true` (senão `report_not_generation_started` 409); mantém `rejection_count=1`, libera `correction_in_progress=false`, `report.status='failed_no_v2'`.
    4. `recover_campaign_correction_generation(p_campaign_id, p_stale_before timestamptz DEFAULT NULL)` — valida consumo em andamento (`generation_started` + `correction_in_progress=true`) e `report.generation_started_at < COALESCE(p_stale_before, now() - interval '330 seconds')`; se preso → mantém `rejection_count=1`, `report.status='failed_no_v2'`, `correction_in_progress=false`, retorna `{recovered:true}`; senão no-op `{recovered:false}`. SQL **não lê env**.
    REVOKE/GRANT service_role das 4 funções.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/migrations/20260906000002_f37_2_correction_rpcs.sql','utf8'); const need=['consume_campaign_correction_opportunity','complete_campaign_correction_v2','fail_campaign_correction_v2','recover_campaign_correction_generation','asset_status = \'superseded\'','brief_snapshot','rejection_count = 1','interval \'330 seconds\'','generation_started_at','report_not_generation_started','submission_stale','version_mismatch']; const miss=need.filter(x=>!s.includes(x)); if(miss.length) throw new Error('faltando: '+miss.join(', ')); console.log('OK M2/M3 parte 2');"</automated>
  </verify>
  <acceptance_criteria>
    - `consume_campaign_correction_opportunity` grava `rejection_count=1` + `correction_in_progress=true` + `generation_started` atomicamente
    - `complete_campaign_correction_v2` insere v2 com `brief_snapshot` copiado da v1, demove v1 para `superseded` com path preservado, sem incrementar `rejection_count`
    - `fail_campaign_correction_v2` mantém `rejection_count=1`, libera candidata, `failed_no_v2`
    - `recover_campaign_correction_generation` usa `COALESCE(p_stale_before, now() - interval '330 seconds')` (sem env) e retorna `{recovered:boolean}`
  </acceptance_criteria>
  <done>RPCs de consumo/geração/conclusão/falha/recuperação criadas com contratos fechados</done>
</task>

<task type="auto">
  <name>Task 3: RPC approve_candidate + REVERT + [BLOCKING] db push</name>
  <files>supabase/migrations/20260906000002_f37_2_correction_rpcs.sql</files>
  <read_first>
    - supabase/migrations/20260901000002_f37_1_approve_campaign_art_version_rpc.sql (assinatura/validações da RPC F37.1 intacta)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-approval-gate/spec.md (RPC approve_campaign_candidate)
  </read_first>
  <action>
    1. Acrescentar `approve_campaign_candidate(p_campaign_id uuid, p_version_id uuid)` — trava a candidata primeiro (`id=p_version_id FOR UPDATE`); valida pertence à campanha, `status='pending'`, `asset_status='active'`, `correction_in_progress=false` (senão `correction_in_progress`/`version_not_active`/`version_not_pending`/`version_not_found`/`version_campaign_mismatch`); **chama a RPC F37.1 `approve_campaign_art_version(p_campaign_id, p_version_id)` intacta na mesma transação** (sem `CREATE OR REPLACE` sobre ela).
    2. REVOKE/GRANT service_role de `approve_campaign_candidate`.
    3. Seção **REVERT** comentada no final (DROP FUNCTION das 7 RPCs em ordem reversa).
    4. **[BLOCKING]** `supabase db push` (dry-run → push → dry-run "Remote database is up to date."), com `SUPABASE_ACCESS_TOKEN` no ambiente; se exigir prompt interativo, aplicar manualmente e registrar no SUMMARY.
    NÃO alterar `approve_campaign_art_version` nem as RPCs dormentes `begin_campaign_correction`/`cancel_campaign_correction`/`complete_campaign_regeneration`.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/migrations/20260906000002_f37_2_correction_rpcs.sql','utf8'); const need=['approve_campaign_candidate','approve_campaign_art_version','correction_in_progress','version_not_active','REVERT','GRANT EXECUTE']; const miss=need.filter(x=>!s.includes(x)); if(miss.length) throw new Error('faltando: '+miss.join(', ')); if(/CREATE OR REPLACE FUNCTION public\.approve_campaign_art_version/.test(s)) throw new Error('RPC F37.1 nao pode ser CREATE OR REPLACE'); console.log('OK approve_candidate + REVERT');"</automated>
  </verify>
  <acceptance_criteria>
    - `approve_campaign_candidate` presente, trava a candidata primeiro e invoca `approve_campaign_art_version` intacta
    - Nenhum `CREATE OR REPLACE FUNCTION public.approve_campaign_art_version` no arquivo
    - Seção REVERT presente; `supabase db push --dry-run` → "Remote database is up to date." (registrado no SUMMARY)
  </acceptance_criteria>
  <done>RPC approve protegida + REVERT + migration M2/M3 aplicada no remoto</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → RPCs SECURITY DEFINER | Entrada validada no banco; escrita service_role |
| relato do lojista → RPC | `p_text` não confiável persistido cru; interpretação é responsabilidade da camada de análise |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-2-05 | Tampering | corrida aprovar × consumir | mitigate | Locks uniformes candidata → campanha → relato; `approve_campaign_candidate` valida `correction_in_progress=false`; consumo valida pendência |
| T-37-2-06 | Elevation of Privilege | RPCs novas | mitigate | `SECURITY DEFINER SET search_path=''`; REVOKE de PUBLIC/anon/authenticated; GRANT só service_role |
| T-37-2-07 | Tampering | `rejection_count`/`asset_status` | mitigate | `rejection_count` escrito só pela RPC de consumo (0→1); v1 demovida para `superseded` sem descarte |
| T-37-2-08 | Repudiation | conclusão da análise | mitigate | `complete_campaign_correction_analysis` exige `analyzing` + lease + submissão mais recente; `completed_at` gravado |
| T-37-2-03-SC | Tampering | dependências npm | accept | Nenhuma dependência nova — sem instalação de pacotes |
</threat_model>

<verification>
- Gates `node` das Tasks 1-3 verdes
- `supabase db push --dry-run` → "Remote database is up to date."
- RPCs dormentes e `approve_campaign_art_version` intocadas
</verification>

<success_criteria>
- 7 RPCs novas na M2/M3, aplicadas no remoto, com locks uniformes e contratos fechados
- `37-2-03-SUMMARY.md` registra a saída do push
</success_criteria>

<output>
Create `.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-03-SUMMARY.md` when done
</output>

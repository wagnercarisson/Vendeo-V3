---
phase: 37.2-correcao-unica-por-nao-conformidade
plan: 04
type: execute
wave: 3
depends_on: [03]
files_modified:
  - supabase/migrations/20260906000003_f37_2_generation_events_type.sql
  - src/lib/visual-signature/types.ts
  - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/ai-cost-tracker/spec.md
autonomous: false
requirements: [F37.2-04]

must_haves:
  truths:
    - "CHECK `chk_generation_events_type` aceita `campaign_correction_analysis` junto dos valores vigentes do CHECK (aditivo)"
    - "Union `GenerationEventType` ganha o literal `campaign_correction_analysis` (12 → 13 valores)"
    - "Delta MODIFIED da capability `ai-cost-tracker` registra o novo literal, a evolução do CHECK e o registro no mesmo operation_run_id com attemptNumber da submissão"
    - "Migration aplicada no remoto (supabase db push) antes da verificação"
  artifacts:
    - path: "supabase/migrations/20260906000003_f37_2_generation_events_type.sql"
      provides: "Evolução idempotente do CHECK chk_generation_events_type"
      contains: "campaign_correction_analysis"
    - path: "src/lib/visual-signature/types.ts"
      provides: "Union GenerationEventType com 13 valores"
      contains: "campaign_correction_analysis"
  key_links:
    - from: "src/lib/visual-signature/types.ts"
      to: "supabase/migrations/20260906000003_f37_2_generation_events_type.sql"
      via: "novo literal aditivo no union TS e no CHECK do banco — SEM exigência de paridade (o CHECK já lista tipos de tema que o union TS não enumera)"
      pattern: "campaign_correction_analysis"
---

<objective>
Criar a **Migration M4** (evolução idempotente do CHECK `chk_generation_events_type`), estender o union `GenerationEventType` com o literal `campaign_correction_analysis` e registrar o **delta MODIFIED** da capability `ai-cost-tracker`; **db push [BLOCKING]**.

Purpose: banco → código — tornar observável o custo da IA textual da análise sob o mesmo `operation_run_id` (achado 4).
Output: M4 aplicada + tipo TS estendido + delta da capability registrado + SUMMARY.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-CONTEXT.md
@.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-PATTERNS.md
@openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/ai-cost-tracker/spec.md
@openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/design.md

<interfaces>
Union atual `GenerationEventType` (src/lib/visual-signature/types.ts:101-113) — 12 valores: campaign_pipeline, campaign_copy, campaign_input_validation, campaign_image, campaign_image_review, visual_signature, visual_signature_image, visual_signature_validation, brand_profile_without_logo, brand_profile_with_logo, brand_profile_vision, brand_profile_text.
CHECK atual `chk_generation_events_type` (última evolução em supabase/migrations/20260825000001_create_store_campaign_themes.sql:304-316) lista 14 valores (inclui theme_direction/theme_generation).
**Nota (não ampliar escopo):** o CHECK do banco tem 14 valores e o union TS tem 12 — as listas **não são idênticas hoje** (o CHECK inclui tipos de tema que o union TS não enumera). Após esta fase: 15 no CHECK e 13 no TS. A tarefa é **aditiva** dos dois lados; **não** há exigência de paridade entre as listas e **não** se deve adicionar/remover outros literais para "igualar".
Novo literal: `campaign_correction_analysis`; `operationRunType: "campaign_delivery"`; `attemptNumber` = `attempt_number` da submissão.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration M4 — evolução idempotente do CHECK chk_generation_events_type</name>
  <files>supabase/migrations/20260906000003_f37_2_generation_events_type.sql</files>
  <read_first>
    - supabase/migrations/20260825000001_create_store_campaign_themes.sql (linhas 304-316: padrão DROP CONSTRAINT IF EXISTS + ADD CONSTRAINT + REVERT)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/ai-cost-tracker/spec.md (requisito do CHECK aditivo)
  </read_first>
  <action>
    Criar `supabase/migrations/20260906000003_f37_2_generation_events_type.sql`:
    1. `ALTER TABLE public.generation_events DROP CONSTRAINT IF EXISTS chk_generation_events_type;`
    2. `ALTER TABLE public.generation_events ADD CONSTRAINT chk_generation_events_type CHECK (generation_type IN ( ...todos os valores atuais... , 'campaign_correction_analysis'));` — copiar EXATAMENTE a lista vigente da migration `20260825000001` (14 valores) e acrescentar o novo literal no fim. Aditivo e retrocompatível.
    3. Seção **REVERT** comentada (retorno ao CHECK sem o novo literal).
    NÃO alterar eventos existentes nem a coluna `generation_type`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('supabase/migrations/20260906000003_f37_2_generation_events_type.sql','utf8'); const prev=fs.readFileSync('supabase/migrations/20260825000001_create_store_campaign_themes.sql','utf8'); const m=prev.match(/chk_generation_events_type[\s\S]*?CHECK \(generation_type IN \(([\s\S]*?)\)\)/); if(!m) throw new Error('CHECK anterior nao encontrado'); const prevVals=[...m[1].matchAll(/'([^']+)'/g)].map(x=>x[1]); const miss=prevVals.filter(v=>!s.includes(\"'\"+v+\"'\")); if(miss.length) throw new Error('valores perdidos: '+miss.join(', ')); if(!s.includes('campaign_correction_analysis')) throw new Error('novo literal ausente'); console.log('OK M4 aditivo ('+prevVals.length+' valores preservados)');"</automated>
  </verify>
  <acceptance_criteria>
    - `DROP CONSTRAINT IF EXISTS chk_generation_events_type` + `ADD CONSTRAINT` com todos os valores vigentes + `campaign_correction_analysis`
    - Nenhum valor anterior removido; REVERT presente
  </acceptance_criteria>
  <done>M4 criada, aditiva e retrocompatível</done>
</task>

<task type="auto">
  <name>Task 2: Union GenerationEventType + delta MODIFIED ai-cost-tracker</name>
  <files>src/lib/visual-signature/types.ts, openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/ai-cost-tracker/spec.md</files>
  <read_first>
    - src/lib/visual-signature/types.ts (linhas 101-113 — union GenerationEventType; contrato AiCostEvent)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/ai-cost-tracker/spec.md (delta normativo já redigido)
  </read_first>
  <action>
    1. Em `src/lib/visual-signature/types.ts`, acrescentar `| 'campaign_correction_analysis'` ao union `GenerationEventType` (12 → 13 valores). Não alterar o contrato `AiCostEvent`/`AiCostTracker.record` (o novo literal flui pelo tipo existente).
    2. Confirmar/registrar o delta MODIFIED da capability `ai-cost-tracker` no spec da fatia: novo literal `campaign_correction_analysis`, evolução do CHECK `chk_generation_events_type`, registro no mesmo `operation_run_id` com `attemptNumber` = `attempt_number` da submissão, `provider = textProvider.name`, `operationRunType: "campaign_delivery"`, sem `credit_transactions`/`operation_key`. Se o spec já estiver completo, apenas validar (sem alteração supérflua) e registrar a verificação no SUMMARY.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/lib/visual-signature/types.ts','utf8'); const seg=s.slice(s.indexOf('GenerationEventType'), s.indexOf('GenerationEventType')+800); const vals=[...seg.matchAll(/'([^']+)'/g)].map(x=>x[1]); if(!vals.includes('campaign_correction_analysis')) throw new Error('literal ausente no union'); if(vals.length<13) throw new Error('esperado >=13 valores, veio '+vals.length); console.log('OK union ('+vals.length+' valores)');"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/visual-signature/types.ts` contém `campaign_correction_analysis` no union `GenerationEventType`
    - Spec `ai-cost-tracker` da fatia documenta o novo literal + evolução do CHECK + registro no mesmo run com `attemptNumber`
  </acceptance_criteria>
  <done>Tipo TS e delta da capability alinhados ao CHECK M4</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] supabase db push (aplicar M4 no remoto)</name>
  <files>supabase/migrations/20260906000003_f37_2_generation_events_type.sql</files>
  <read_first>
    - .planning/ROADMAP.md (padrão de migration plan com `autonomous: no` + push manual)
  </read_first>
  <what-built>Migration M4 criada (CHECK aditivo) + union TS estendido + delta da capability registrado.</what-built>
  <how-to-verify>
    1. Confirmar `SUPABASE_ACCESS_TOKEN` no ambiente.
    2. `supabase db push --dry-run` → deve listar `20260906000003_f37_2_generation_events_type.sql` (e demais pendentes).
    3. `supabase db push` (exit 0).
    4. `supabase db push --dry-run` → "Remote database is up to date."
    5. Confirmar via REST service_role que um `generation_events` com `generation_type='campaign_correction_analysis'` é aceito (ou validar o CHECK por consulta).
    Se o push exigir prompt interativo não suprimível, aplicar manualmente e registrar no SUMMARY.
  </how-to-verify>
  <resume-signal>Type "approved" com a saída do push (ou descreva o erro)</resume-signal>
  <acceptance_criteria>
    - `supabase db push` exit 0 e `supabase db push --dry-run` → "Remote database is up to date."
    - CHECK `chk_generation_events_type` aceita `campaign_correction_analysis` (validação por consulta/REST)
    - Saída do push registrada no `37-2-04-SUMMARY.md`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → banco remoto | Evolução de CHECK aplicada via `supabase db push` |
| tracker → generation_events | Escrita service_role call-level (AiCostTracker) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-2-09 | Tampering | CHECK `chk_generation_events_type` | mitigate | Evolução idempotente e aditiva (DROP/ADD); lista vigente preservada integralmente; REVERT |
| T-37-2-10 | Repudiation | telemetria da análise | mitigate | `AiCostTracker.record` best-effort sob o mesmo `operation_run_id` com `attemptNumber` da submissão |
| T-37-2-04-SC | Tampering | dependências npm | accept | Nenhuma dependência nova — sem instalação de pacotes |
</threat_model>

<verification>
- Gate `node` do M4 (todos os valores vigentes preservados + novo literal) verde
- Gate `node` do union TS (≥13 valores) verde
- `supabase db push --dry-run` → "Remote database is up to date."
</verification>

<success_criteria>
- M4 aplicada no remoto; union `GenerationEventType` com 13 valores; delta da capability registrado
- `37-2-04-SUMMARY.md` registra a saída do push
</success_criteria>

<output>
Create `.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-04-SUMMARY.md` when done
</output>

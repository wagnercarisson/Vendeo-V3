---
phase: 37.2-correcao-unica-por-nao-conformidade
plan: 05
type: execute
wave: 2
depends_on: [02]
files_modified:
  - src/lib/campaign/types.ts
  - src/lib/campaign/correction-reports.ts
autonomous: true
requirements: [F37.2-05]

must_haves:
  truths:
    - "Tipos do caso/tentativas existem e `ArtAssetStatus` aceita 'superseded'"
    - "Funções de persistência do caso/tentativas leem/escrevem via supabaseAdmin e concluem a análise via RPC (não UPDATE TS direto)"
    - "Filha não tem `campaign_id`; decisão corrente = tentativa de maior `attempt_number`; aprovação não espelhada no relato"
  artifacts:
    - path: "src/lib/campaign/correction-reports.ts"
      provides: "getCorrectionReport/listCorrectionSubmissions/completeCorrectionAnalysis/listCorrectionReports/getCorrectionReportDetail/markReportReviewedBySupport"
      contains: "completeCorrectionAnalysis"
    - path: "src/lib/campaign/types.ts"
      provides: "CorrectionReportStatus/CorrectionAnalysisState/CorrectionReport/CorrectionSubmission + ArtAssetStatus superseded"
      contains: "superseded"
  key_links:
    - from: "src/lib/campaign/correction-reports.ts"
      to: "campaign_correction_submissions"
      via: "supabaseAdmin.from(...).order('attempt_number')"
      pattern: "attempt_number"
---

<objective>
Implementar **tipos + persistência** do caso de correção (R5/R6): `CorrectionReport`/`CorrectionSubmission`/estados, `ArtAssetStatus` com `superseded`, e as funções `getCorrectionReport`/`listCorrectionSubmissions`/`createCorrectionReport`/`completeCorrectionAnalysis`/`listCorrectionReports`/`getCorrectionReportDetail`/`markReportReviewedBySupport`.

Purpose: camada de leitura/escrita do caso/tentativas consumida pela rota, página e admin.
Output: `correction-reports.ts` + tipos atualizados + SUMMARY.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-CONTEXT.md
@.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-PATTERNS.md
@openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-correction-reports/spec.md
@openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-art-versions/spec.md

<interfaces>
Padrão de persistência (src/lib/campaign/persistence.ts): `import "server-only"; import { supabaseAdmin } from "@/lib/supabase/server";` + `const { data, error } = await supabaseAdmin.from("<tabela>").select("*")...; if (error) throw new Error(error.message);`.
Tipos novos: `CorrectionReportStatus = "open"|"generation_started"|"v2_generated"|"failed_no_v2"`; `CorrectionAnalysisState = "analyzing"|"eligible"|"blocked"|"unclear"|"analysis_failed"`; `CorrectionReport` (id, campaign_id, store_id, reported_version_id, generated_version_id, status, generation_started_at, operation_run_id, reviewed_by_support_at, reviewed_by_support_user, created_at, updated_at); `CorrectionSubmission` (id, report_id, attempt_number, text, analysis_state, category, normalized_instruction, analysis_expires_at, created_at, completed_at).
`ArtAssetStatus` atual `"active" | "discarded"` → `"active" | "discarded" | "superseded"`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tipos de domínio do caso/tentativas + ArtAssetStatus superseded</name>
  <files>src/lib/campaign/types.ts</files>
  <read_first>
    - src/lib/campaign/types.ts (CampaignArtVersion + ArtAssetStatus atuais)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-art-versions/spec.md (tipos)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-correction-reports/spec.md (colunas)
  </read_first>
  <action>
    Em `src/lib/campaign/types.ts`:
    1. Trocar `ArtAssetStatus` para `"active" | "discarded" | "superseded"`.
    2. Adicionar `CorrectionReportStatus`, `CorrectionAnalysisState`, `CorrectionReport` e `CorrectionSubmission` com as colunas exatas das tabelas (incluindo `attempt_number`, `analysis_expires_at`, `completed_at`; a submissão NÃO tem `campaign_id`).
    3. **Adicionar `operation_run_id: string | null` a `CampaignRecord`** (F38.1 — a coluna `campaigns.operation_run_id` já é persistida por `createCampaign` e retornada por `getCampaign` via `select("*")`, mas não estava tipada; a rota `problem-report` e o orquestrador da v2 leem `campaign.operation_run_id` para registrar os eventos call-level no mesmo run — sem isso, `npm run typecheck` falha com `TS2339`). Manter os demais campos/tipos F37.1 intactos.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/lib/campaign/types.ts','utf8'); const need=['superseded','CorrectionReportStatus','CorrectionAnalysisState','CorrectionSubmission','attempt_number','analysis_expires_at','operation_run_id']; const miss=need.filter(x=>!s.includes(x)); if(miss.length) throw new Error('faltando: '+miss.join(', ')); if(!/interface CampaignRecord[\s\S]*operation_run_id:\s*string\s*\|\s*null/.test(s)) throw new Error('CampaignRecord sem operation_run_id'); console.log('OK tipos');"</automated>
  </verify>
  <acceptance_criteria>
    - `ArtAssetStatus` inclui `"superseded"`
    - `CorrectionReport`/`CorrectionSubmission`/`CorrectionReportStatus`/`CorrectionAnalysisState` exportados com os campos das tabelas
    - `CorrectionSubmission` NÃO tem `campaign_id`
    - `CampaignRecord` expõe `operation_run_id: string | null` (evita `TS2339` em `campaign.operation_run_id`)
  </acceptance_criteria>
  <done>Tipos de domínio do caso/tentativas disponíveis</done>
</task>

<task type="auto">
  <name>Task 2: Persistência base (leitura/escrita + conclusão via RPC)</name>
  <files>src/lib/campaign/correction-reports.ts</files>
  <read_first>
    - src/lib/campaign/persistence.ts (padrão getCampaign/listArtVersions/createArtVersion)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-correction-reports/spec.md (RPC complete_campaign_correction_analysis)
    - .planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-PATTERNS.md (seção correction-reports.ts)
  </read_first>
  <action>
    Criar `src/lib/campaign/correction-reports.ts` (`import "server-only"` + `supabaseAdmin`):
    1. `getCorrectionReport(campaignId)` — `.from("campaign_correction_reports").select("*").eq("campaign_id", campaignId).maybeSingle()`; erro → throw.
    2. `listCorrectionSubmissions(reportId)` — `.select("*").eq("report_id", reportId).order("attempt_number", { ascending: true })`.
    3. `createCorrectionReport(...)` — INSERT do pai (uso em testes/rotas internas; `reported_version_id`/`store_id`/`operation_run_id`).
    4. `completeCorrectionAnalysis({ reportId, submissionId, attemptNumber, analysisState, category, normalizedInstruction })` — chama `supabaseAdmin.rpc("complete_campaign_correction_analysis", { p_report_id, p_submission_id, p_attempt_number, p_analysis_state, p_category, p_normalized_instruction })`; **NUNCA UPDATE TS direto**; mapear erro por mensagem.
    Padrão de erro `if (error) throw new Error(error.message)`.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/lib/campaign/correction-reports.ts','utf8'); const need=['getCorrectionReport','listCorrectionSubmissions','createCorrectionReport','completeCorrectionAnalysis','complete_campaign_correction_analysis','attempt_number','server-only']; const miss=need.filter(x=>!s.includes(x)); if(miss.length) throw new Error('faltando: '+miss.join(', ')); if(/\.from\(\"campaign_correction_submissions\"\)[\s\S]{0,200}\.update\(/.test(s)) throw new Error('conclusao nao pode ser UPDATE TS direto'); console.log('OK persistencia base');"</automated>
  </verify>
  <acceptance_criteria>
    - Funções base presentes; conclusão da análise via `rpc("complete_campaign_correction_analysis", ...)`
    - `listCorrectionSubmissions` ordena por `attempt_number`
  </acceptance_criteria>
  <done>Persistência base do caso/tentativas implementada</done>
</task>

<task type="auto">
  <name>Task 3: Consultas admin + marcação revisado + invariantes</name>
  <files>src/lib/campaign/correction-reports.ts</files>
  <read_first>
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/admin-campaign-reports/spec.md (listagem/detalhe/marcação)
    - src/lib/campaign/display.ts (generateSignedPreviewUrl para detalhe)
  </read_first>
  <action>
    Ainda em `correction-reports.ts`:
    1. `listCorrectionReports({ filters, page })` — lista o pai com filtros (status do caso, revisado×não revisado) e paginação; para cada relato, resolver a **decisão corrente** = submissão de maior `attempt_number` (join/segunda consulta) e expor `analysis_state` vigente; filtro por `analysis_state` da tentativa vigente aplicado após resolver a decisão corrente.
    2. `getCorrectionReportDetail(reportId)` — relato + `listCorrectionSubmissions` + versões v1/v2 (via `campaign_art_versions` da campanha, incluindo v1 `superseded`) + situação de aprovação **derivada** de `campaigns.approved_version_id`/`approved_at` (NUNCA colunas espelhadas no relato).
    3. `markReportReviewedBySupport(reportId, actorId)` — `update({ reviewed_by_support_at: now, reviewed_by_support_user: actorId })` (ortogonal; não altera status/rejection_count/versões).
    4. Invariantes documentadas em comentário: filha sem `campaign_id`; decisão corrente por `attempt_number`; aprovação não espelhada.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/lib/campaign/correction-reports.ts','utf8'); const need=['listCorrectionReports','getCorrectionReportDetail','markReportReviewedBySupport','attempt_number','approved_version_id','reviewed_by_support_at']; const miss=need.filter(x=>!s.includes(x)); if(miss.length) throw new Error('faltando: '+miss.join(', ')); console.log('OK admin + revisado');"</automated>
  </verify>
  <acceptance_criteria>
    - `listCorrectionReports` resolve decisão corrente por maior `attempt_number`
    - `getCorrectionReportDetail` deriva aprovação de `campaigns` (sem colunas espelhadas) e inclui v1/v2
    - `markReportReviewedBySupport` atualiza apenas `reviewed_by_support_at`/`_user`
  </acceptance_criteria>
  <done>Consultas admin e marcação ortogonal implementadas</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server (supabaseAdmin) → tabelas de correção | Acesso service_role; sem exposição ao cliente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-2-11 | Tampering | conclusão da análise em TS | mitigate | `completeCorrectionAnalysis` delega à RPC (guarda atômica), nunca UPDATE TS direto |
| T-37-2-12 | Information Disclosure | detalhe admin | mitigate | Aprovação derivada de `campaigns`; v1 `superseded` só via signed URL service_role |
| T-37-2-13 | Tampering | decisão corrente não determinística | mitigate | Ordenação por `attempt_number` (UNIQUE report_id+attempt_number), nunca por `created_at` |
| T-37-2-05-SC | Tampering | dependências npm | accept | Nenhuma dependência nova — sem instalação de pacotes |
</threat_model>

<verification>
- Gates `node` das Tasks 1-3 verdes
- `npm run typecheck` sem erros nos arquivos tocados
</verification>

<success_criteria>
- Tipos + persistência do caso/tentativas implementados com invariantes (filha sem campaign_id, decisão por attempt_number, aprovação não espelhada)
- `37-2-05-SUMMARY.md` criado
</success_criteria>

<output>
Create `.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-05-SUMMARY.md` when done
</output>

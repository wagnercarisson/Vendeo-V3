---
phase: 37.2-correcao-unica-por-nao-conformidade
plan: 06
type: execute
wave: 4
depends_on: [04]
files_modified:
  - src/lib/campaign/correction-intent-service.ts
autonomous: true
requirements: [F37.2-06]

must_haves:
  truths:
    - "CorrectionIntentService.analyzeReport(text) retorna `eligible|blocked|unclear` + category/normalizedInstruction/guidance via createTextProvider (sem jsonMode)"
    - "JSON inválido/fora do schema → `unclear`; timeout/transporte/vazio → `analysis_failed`"
    - "Texto do lojista e normalizedInstruction saneados (sanitizePromptText) e incapazes de sobrescrever o briefing"
    - "Cada chamada registra evento call-level `campaign_correction_analysis` no mesmo operation_run_id com provider=textProvider.name e attemptNumber da submissão"
  artifacts:
    - path: "src/lib/campaign/correction-intent-service.ts"
      provides: "CorrectionIntentService + CorrectionAnalysisResultSchema"
      contains: "analyzeReport"
  key_links:
    - from: "src/lib/campaign/correction-intent-service.ts"
      to: "src/lib/ai-cost/tracker.ts"
      via: "AiCostTracker.record com generationType campaign_correction_analysis"
      pattern: "campaign_correction_analysis"
---

<objective>
Implementar o **CorrectionIntentService** (R2): análise textual do relato via `createTextProvider()`, JSON estrito + parse defensivo + Zod, taxonomia §4, conteúdo não confiável saneado, tratamento de erros e custo call-level.

Purpose: decidir a intenção do relato (não a veracidade da imagem) sem nunca alterar o briefing.
Output: `correction-intent-service.ts` + SUMMARY.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-CONTEXT.md
@.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-PATTERNS.md
@openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-correction-analysis/spec.md
@openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/ai-cost-tracker/spec.md

<interfaces>
`createTextProvider()` (src/lib/text-provider/factory.ts) → `TextProvider { readonly name: string; generateText(prompt, { system, temperature, maxTokens, signal }): Promise<TextProviderResult> }`; `TextProviderResult { content, usage: { promptTokens, completionTokens }, model }`.
Padrão parse defensivo (copy-director-service.ts:14-22): `try { JSON.parse(raw); Schema.parse(parsed) } catch { null }` + limpeza de fences (`cleanJsonResponse`, input-validation-service.ts:256-269).
`sanitizePromptText` (art-director-briefing.ts:55-57): `{{`→`{`, `}}`→`}`.
`AiCostTracker.record` (src/lib/ai-cost/tracker.ts): best-effort, nunca lança; campos incl. `operationRunId`, `operationRunType: "campaign_delivery"`, `generationType`, `provider`, `model`, `attemptNumber`, `durationMs`, `status`, `errorType`, tokens, `cost`; custo via `resolveAiCost`.
Taxonomia §4 (elegível): elemento obrigatório cortado; logo/produto/texto gravemente cortado; texto ilegível/corrompido; dado divergente do briefing; informação inventada; elemento duplicado; produto deformado; falha grave de composição. (Bloqueado): alterar dado aprovado; mudar preço/validade/produto/badge/fundo/identidade por preferência; reposicionar/reestilizar sem defeito; "não gostei"/"outra opção"; rebriefing/estética.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Serviço analyzeReport + schema Zod + taxonomia</name>
  <files>src/lib/campaign/correction-intent-service.ts</files>
  <read_first>
    - src/lib/copy/copy-director-service.ts (estrutura de serviço + parseViaJson + onCall best-effort)
    - src/lib/image-generation/services/input-validation-service.ts (cleanJsonResponse)
    - src/lib/text-provider/types.ts + src/lib/text-provider/factory.ts
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-correction-analysis/spec.md
  </read_first>
  <action>
    Criar `src/lib/campaign/correction-intent-service.ts`:
    1. **Exportar a allowlist canônica `export const CORRECTION_ELIGIBLE_CATEGORIES = ["truncated_element","illegible_text","data_mismatch","invented_information","duplicated_element","deformed_product","blocking_composition"] as const;`** — fonte única dos identificadores estáveis da taxonomia §4 (o SQL `complete_campaign_correction_analysis` do plano 03 usa exatamente estes; um teste do plano 16 assere a paridade SQL×TS). `CorrectionAnalysisResultSchema` (Zod `.strict()`): `analysisState: "eligible"|"blocked"|"unclear"`, `category?: z.enum(CORRECTION_ELIGIBLE_CATEGORIES)`, `normalizedInstruction?: string`, `guidance: string`. Campos inesperados rejeitados.
    2. `CorrectionIntentService` (construtor recebe `TextProvider` opcional via `createTextProvider()`), método `analyzeReport(text, { signal?, operationRunId, campaignId, storeId, userId, attemptNumber }): Promise<CorrectionAnalysisResult>`.
    3. System prompt pedindo **JSON estrito**; instrui entender o relato tolerando erros de escrita; **NÃO avaliar a imagem**; classificar pela taxonomia §4; quando elegível devolver categoria + `normalizedInstruction` objetiva; quando bloqueado/unclear devolver `guidance` PT-BR. `generateText(prompt, { system, temperature, maxTokens, signal })` — **sem `jsonMode`**.
    4. Parse defensivo: limpeza de fences + `JSON.parse` + `Schema.parse`; JSON inválido/fora do schema → `{ analysisState: "unclear", guidance }`.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/lib/campaign/correction-intent-service.ts','utf8'); const need=['CORRECTION_ELIGIBLE_CATEGORIES','truncated_element','illegible_text','data_mismatch','invented_information','duplicated_element','deformed_product','blocking_composition','CorrectionAnalysisResultSchema','analyzeReport','createTextProvider','generateText','.strict()','eligible','blocked','unclear','NÃO']; const miss=need.filter(x=>!s.includes(x)); if(miss.length) throw new Error('faltando: '+miss.join(', ')); if(/jsonMode|response_format/.test(s)) throw new Error('nao deve depender de jsonMode'); console.log('OK servico');"</automated>
  </verify>
  <acceptance_criteria>
    - `CORRECTION_ELIGIBLE_CATEGORIES` exportado como fonte única da taxonomia §4 (7 identificadores estáveis)
    - `CorrectionAnalysisResultSchema` `.strict()` com os 3 estados + category (enum da allowlist)/normalizedInstruction/guidance
    - `analyzeReport` usa `createTextProvider().generateText` sem `jsonMode`
    - Prompt cita a taxonomia §4 e a instrução de NÃO avaliar a imagem
  </acceptance_criteria>
  <done>Serviço de análise textual com contrato JSON+Zod e taxonomia implementado</done>
</task>

<task type="auto">
  <name>Task 2: Conteúdo não confiável + tratamento de erros</name>
  <files>src/lib/campaign/correction-intent-service.ts</files>
  <read_first>
    - src/lib/image-generation/services/art-director-briefing.ts (sanitizePromptText)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-correction-analysis/spec.md (conteúdo não confiável + falhas)
  </read_first>
  <action>
    1. Delimitar o `text` do lojista no prompt com delimitadores explícitos (ex.: `<<<RELATO>>> ... <<<FIM_RELATO>>>`) e aplicar `sanitizePromptText`; o prompt declara que o conteúdo é **não confiável** e não pode alterar briefing/identidade/valores (anti-invenção).
    2. `normalizedInstruction` retornada também é saneada antes de ser devolvida/persistida (sem placeholder não resolvido).
    3. Tratamento de erros: `try/catch` na chamada do provider — timeout/transporte/resposta vazia → `{ analysisState: "analysis_failed", guidance }`; JSON inválido/fora do schema → `unclear`. Em nenhum caso lança para o caller.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/lib/campaign/correction-intent-service.ts','utf8'); const need=['sanitizePromptText','analysis_failed','RELATO','try','catch']; const miss=need.filter(x=>!s.includes(x)); if(miss.length) throw new Error('faltando: '+miss.join(', ')); console.log('OK conteudo/erros');"</automated>
  </verify>
  <acceptance_criteria>
    - `sanitizePromptText` aplicado ao texto do lojista e à `normalizedInstruction`
    - Delimitadores explícitos no prompt
    - `analysis_failed` para timeout/transporte/vazio; `unclear` para JSON inválido; sem lançar exceção ao caller
  </acceptance_criteria>
  <done>Conteúdo não confiável saneado e falhas mapeadas para unclear/analysis_failed</done>
</task>

<task type="auto">
  <name>Task 3: Custo call-level campaign_correction_analysis</name>
  <files>src/lib/campaign/correction-intent-service.ts</files>
  <read_first>
    - src/app/api/campaign/generate-image/route.ts (bloco recordCall: resolveAiCost + AiCostTracker.record)
    - src/lib/ai-cost/tracker.ts (contrato record)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/ai-cost-tracker/spec.md
  </read_first>
  <action>
    Após cada `generateText` da análise, registrar um evento call-level via `AiCostTracker.record` (best-effort, em `try/catch`, nunca derruba o fluxo):
    - `operationRunId` = `operation_run_id` da campanha; `operationRunType: "campaign_delivery"`;
    - `generationType: "campaign_correction_analysis"`;
    - **`provider` = `textProvider.name`** (o `TextProviderResult` só devolve `model`/`usage`);
    - `model`/`usage` do `TextProviderResult`; `durationMs`; `status` (`success`/`failed`); `errorType` quando aplicável;
    - `campaignId`/`storeId`/`userId`; **`attemptNumber` = `attempt_number` da submissão**;
    - custo resolvido por `resolveAiCost({ provider, model, usage, generationType: "campaign_correction_analysis" })`.
    Sem `credit_transactions`/`operation_key` novas.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/lib/campaign/correction-intent-service.ts','utf8'); const need=['AiCostTracker','resolveAiCost','campaign_correction_analysis','this.provider.name','attemptNumber','campaign_delivery']; const miss=need.filter(x=>!s.includes(x)); if(miss.length) throw new Error('faltando: '+miss.join(', ')); console.log('OK custo');"</automated>
  </verify>
  <acceptance_criteria>
    - Evento `campaign_correction_analysis` registrado via `AiCostTracker.record` com `provider = textProvider.name` e `attemptNumber`
    - Registro best-effort (try/catch); sem `credit_transactions`/`operation_key`
  </acceptance_criteria>
  <done>Custo da análise observável no mesmo operation_run_id</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lojista → IA textual | Texto não confiável entra no prompt (risco de prompt injection) |
| serviço → generation_events | Escrita service_role best-effort |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-2-14 | Tampering | prompt injection via `text`/`normalizedInstruction` | mitigate | Delimitadores explícitos + `sanitizePromptText`; prompt declara conteúdo não confiável e proíbe alterar briefing/identidade/valores |
| T-37-2-15 | Spoofing | saída do provider fora do contrato | mitigate | Parse defensivo + Zod `.strict()`; JSON inválido → `unclear`; timeout/vazio → `analysis_failed` |
| T-37-2-16 | Repudiation | custo não registrado | mitigate | `AiCostTracker.record` best-effort sempre chamado após `generateText` |
| T-37-2-06-SC | Tampering | dependências npm | accept | Nenhuma dependência nova — Zod e provider já existentes |
</threat_model>

<verification>
- Gates `node` das Tasks 1-3 verdes
- `npm run typecheck` sem erros
</verification>

<success_criteria>
- `CorrectionIntentService` classifica relatos (eligible/blocked/unclear/analysis_failed), saneia conteúdo não confiável e registra o custo no mesmo run
- `37-2-06-SUMMARY.md` criado
</success_criteria>

<output>
Create `.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-06-SUMMARY.md` when done
</output>

---
phase: 37.2-correcao-unica-por-nao-conformidade
plan: 07
type: execute
wave: 4
depends_on: [03, 05, 06, 08]
files_modified:
  - src/app/api/campaign/[id]/problem-report/route.ts
autonomous: true
requirements: [F37.2-07]

must_haves:
  truths:
    - "POST /api/campaign/[id]/problem-report aplica guards (CSRF/auth/ownership/flag/ready) e 400 para texto vazio/pontuação sem caso e sem IA"
    - "Envio válido cria caso + 1ª tentativa via begin RPC antes da IA; eligible responde NDJSON; blocked/unclear respondem 200 JSON com guidance"
    - "Conclusão da análise via complete_campaign_correction_analysis; falha pós-provider via fail_campaign_correction_v2 + cleanup best-effort"
    - "Sem reserva de crédito; eventos (análise + v2) sob o mesmo operation_run_id"
  artifacts:
    - path: "src/app/api/campaign/[id]/problem-report/route.ts"
      provides: "Rota do fluxo corretivo (begin → análise → NDJSON/JSON)"
      contains: "begin_campaign_correction_submission"
  key_links:
    - from: "src/app/api/campaign/[id]/problem-report/route.ts"
      to: "src/lib/campaign/correction-intent-service.ts"
      via: "CorrectionIntentService.analyzeReport"
      pattern: "analyzeReport"
    - from: "src/app/api/campaign/[id]/problem-report/route.ts"
      to: "src/lib/campaign/correction-reports.ts"
      via: "orquestrador da v2 + completeCorrectionAnalysis"
      pattern: "generateCorrectionV2"
---

<objective>
Implementar a **rota `POST /api/campaign/[id]/problem-report`** (R1/R2/R3): guards + zod strict + 400 vazio/pontuação, begin RPC (caso + tentativa antes da IA), análise textual, conclusão via RPC, 200 `{analysisState, guidance}` para blocked/unclear e NDJSON stream para eligible, com consumo no hook e falha pós-provider.

Purpose: ponto de entrada do fluxo corretivo, sem nova reserva de crédito.
Output: rota + SUMMARY.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-CONTEXT.md
@.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-PATTERNS.md
@openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-problem-report/spec.md

<interfaces>
Guards (approve/route.ts:24-50): `requireSameOrigin(request)` → `requireApiUser()` → `UUID_V4_REGEX.test(id)` (400) → `getCampaign(id)` (404) → `requireOwnership(campaign.store_id, user.userId)` (404) → `isCampaignApprovalEnabled()` (403) → `campaign.status !== "ready"` (409).
NDJSON (generate-image/route.ts:520-537): `ReadableStream` + `emit = (e) => controller.enqueue(encoder.encode(JSON.stringify(e)+"\n"))`; `Content-Type: application/x-ndjson`.
Custo (generate-image/route.ts:592-639): `resolveAiCost` + `new AiCostTracker().record({ operationRunId, operationRunType: "campaign_delivery", ... })` em try/catch.
Correção: `begin_campaign_correction_submission(p_campaign_id, p_text)` → `{ report_id, submission_id, attempt_number, analysis_expires_at }`; `complete_campaign_correction_analysis(p_report_id, p_submission_id, p_attempt_number, p_analysis_state, p_category, p_normalized_instruction)`; `fail_campaign_correction_v2(p_campaign_id, p_report_id)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guards + zod + 400 vazio/pontuação + begin RPC</name>
  <files>src/app/api/campaign/[id]/problem-report/route.ts</files>
  <read_first>
    - src/app/api/campaign/[id]/approve/route.ts (guards + mapeamento por mensagem)
    - src/app/api/campaign/generate-image/route.ts (apiHandler/stream/erros)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-problem-report/spec.md
  </read_first>
  <action>
    Criar `src/app/api/campaign/[id]/problem-report/route.ts`:
    1. `export const POST = apiHandler(async (request, { params }) => {...})` com guards na ordem: `requireSameOrigin` → `requireApiUser` → UUID v4 (400) → `getCampaign` (404) → `requireOwnership` (404) → `isCampaignApprovalEnabled()` (flag off → 403) → `campaign.status === "ready"` (senão 409).
    2. Body `{ text: string }` zod **strict**; texto vazio ou somente pontuação (regex de pontuação/whitespace) → **400** `{ error }` **sem criar caso e sem IA** (não conta na janela).
    3. `rpc("begin_campaign_correction_submission", { p_campaign_id: id, p_text: text })` — cria caso + 1ª tentativa `analyzing` na mesma transação (antes da IA); mapear erros: `no_active_candidate`/`campaign_not_pending`/`already_consumed`/`analysis_in_progress`/`rate_limit_exceeded` → 409.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/app/api/campaign/[id]/problem-report/route.ts','utf8'); const need=['requireSameOrigin','requireApiUser','requireOwnership','isCampaignApprovalEnabled','begin_campaign_correction_submission','.strict()','400','rate_limit_exceeded']; const miss=need.filter(x=>!s.includes(x)); if(miss.length) throw new Error('faltando: '+miss.join(', ')); console.log('OK guards+begin');"</automated>
  </verify>
  <acceptance_criteria>
    - Guards completos (CSRF/auth/ownership/flag/ready) com os status corretos
    - Texto vazio/pontuação → 400 sem caso/IA
    - `begin_campaign_correction_submission` chamada com mapeamento de erros 409
  </acceptance_criteria>
  <done>Rota valida entrada, cria caso/tentativa antes da IA e mapeia erros</done>
</task>

<task type="auto">
  <name>Task 2: Análise + conclusão via RPC + 200 blocked/unclear + NDJSON eligible</name>
  <files>src/app/api/campaign/[id]/problem-report/route.ts</files>
  <read_first>
    - src/lib/campaign/correction-intent-service.ts (analyzeReport)
    - src/lib/campaign/correction-reports.ts (completeCorrectionAnalysis + orquestrador da v2)
    - src/app/api/campaign/generate-image/route.ts (ReadableStream NDJSON + fases)
  </read_first>
  <action>
    1. Chamar `CorrectionIntentService.analyzeReport(text, { operationRunId: campaign.operation_run_id, campaignId, storeId, userId, attemptNumber })`.
    2. Persistir a conclusão via `completeCorrectionAnalysis(...)` → `rpc("complete_campaign_correction_analysis", { p_report_id, p_submission_id, p_attempt_number, p_analysis_state, p_category, p_normalized_instruction })` (lease + submissão mais recente). Erros `submission_not_analyzing`/`analysis_lease_expired`/`submission_stale` → 409 (não sobrescreve).
    3. `blocked`/`unclear`/`analysis_failed` → `200 { analysisState, guidance }` (sem gerar/consumir; pode reformular).
    4. `eligible` → responder **NDJSON stream** (`Content-Type: application/x-ndjson`) chamando o orquestrador da v2 (plan 08): fases `input_validation` (`skipped`) → `image_generation` (consumo via hook) → `done`/`error`. Erro pré-provider → caso permanece `open`.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/app/api/campaign/[id]/problem-report/route.ts','utf8'); const need=['analyzeReport','complete_campaign_correction_analysis','application/x-ndjson','ReadableStream','input_validation','eligible','analysisState']; const miss=need.filter(x=>!s.includes(x)); if(miss.length) throw new Error('faltando: '+miss.join(', ')); console.log('OK analise+stream');"</automated>
  </verify>
  <acceptance_criteria>
    - Conclusão via RPC com lease/vigência; blocked/unclear → 200 JSON; eligible → NDJSON
    - Erros de lease/stale → 409 sem sobrescrever
  </acceptance_criteria>
  <done>Análise classificada, persistida via RPC e caminho eligible em stream</done>
</task>

<task type="auto">
  <name>Task 3: Falha pós-provider + cleanup + sem reserva de crédito</name>
  <files>src/app/api/campaign/[id]/problem-report/route.ts</files>
  <read_first>
    - src/app/api/campaign/generate-image/route.ts (falha pós-provider + deleteCampaignImage)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-problem-report/spec.md (falha pós-provider)
  </read_first>
  <action>
    No caminho `eligible`: em erro **pós-provider** (erro de provider/timeout/upload), emitir `{ type: "error", ... }` no stream e chamar `rpc("fail_campaign_correction_v2", { p_campaign_id, p_report_id })` (atômica) + remoção best-effort do asset órfão (`deleteCampaignImage` em try/catch). Em erro **pré-provider**, NÃO chamar fail (caso permanece `open`). **NÃO** replicar o bloco `reserveCredit` do generate-image; garantir eventos (análise + v2) sob `campaign.operation_run_id`.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/app/api/campaign/[id]/problem-report/route.ts','utf8'); const need=['fail_campaign_correction_v2','deleteCampaignImage','operation_run_id']; const miss=need.filter(x=>!s.includes(x)); if(miss.length) throw new Error('faltando: '+miss.join(', ')); if(/reserveCredit/.test(s)) throw new Error('nao deve reservar credito'); console.log('OK falha+sem credito');"</automated>
  </verify>
  <acceptance_criteria>
    - Falha pós-provider → `fail_campaign_correction_v2` + cleanup best-effort; falha pré-provider não consome
    - Nenhuma reserva de crédito; eventos no mesmo `operation_run_id`
  </acceptance_criteria>
  <done>Falha pós-provider encerra o caso atomicamente e libera a v1</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente → rota problem-report | Entrada não autenticada/CSRF; body `{ text }` não confiável |
| rota → RPCs | Escrita service_role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-2-21 | Spoofing | CSRF/ownership | mitigate | `requireSameOrigin` + `requireApiUser` + `requireOwnership` (404 para não-dono) |
| T-37-2-22 | Tampering | abuso de análise (custo IA) | mitigate | Rate limit ≤3/30min no begin (teto absoluto); vazio/pontuação → 400 sem IA |
| T-37-2-23 | Repudiation | resposta tardia da IA | mitigate | Conclusão via RPC condicionada a `analyzing`+lease+submissão mais recente |
| T-37-2-24 | Denial of Service | campanha presa em regenerating | mitigate | `fail_campaign_correction_v2` pós-provider + recuperação lazy (planos 03/09/11) |
| T-37-2-08-SC | Tampering | dependências npm | accept | Nenhuma dependência nova — sem instalação de pacotes |
</threat_model>

<verification>
- Gates `node` das Tasks 1-3 verdes
- `npm run typecheck` sem erros
</verification>

<success_criteria>
- Rota problem-report com guards, begin, análise, NDJSON/JSON, consumo via hook e fail pós-provider; sem crédito
- `37-2-07-SUMMARY.md` criado
</success_criteria>

<output>
Create `.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-07-SUMMARY.md` when done
</output>

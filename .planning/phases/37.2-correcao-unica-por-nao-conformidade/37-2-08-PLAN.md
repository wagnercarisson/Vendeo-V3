---
phase: 37.2-correcao-unica-por-nao-conformidade
plan: 08
type: execute
wave: 3
depends_on: [03, 05]
files_modified:
  - src/lib/image-generation/services/image-generation-service.ts
  - src/lib/image-generation/services/art-director-briefing.ts
  - src/lib/campaign/correction-reports.ts
autonomous: true
requirements: [F37.2-08]

must_haves:
  truths:
    - "ImageGenerationService aceita hook opcional `onBeforeImageProviderCall` que roda fire-once imediatamente antes da 1ª chamada ao provider (attempt===0); ausência = comportamento atual"
    - "A v2 usa o mesmo snapshot/imagens F41/identidade, sem `candidateArtDataUrl`, com bloco único de não conformidade e `input_validation` skipped"
    - "Os 4 `.md` do diretor permanecem com diff vazio; revisor intocado; sem copy director"
    - "Orquestrador consome via hook, faz upload da v2 e conclui via RPC; falha pós-provider chama fail_campaign_correction_v2"
  artifacts:
    - path: "src/lib/image-generation/services/image-generation-service.ts"
      provides: "Hook onBeforeImageProviderCall + bloco de não conformidade na montagem"
      contains: "onBeforeImageProviderCall"
    - path: "src/lib/campaign/correction-reports.ts"
      provides: "Orquestrador da v2 (consume→upload→complete; fail pós-provider)"
      contains: "generateCorrectionV2"
  key_links:
    - from: "src/lib/campaign/correction-reports.ts"
      to: "consume_campaign_correction_opportunity"
      via: "hook onBeforeImageProviderCall"
      pattern: "consume_campaign_correction_opportunity"
    - from: "src/lib/campaign/correction-reports.ts"
      to: "complete_campaign_correction_v2"
      via: "após upload da v2"
      pattern: "complete_campaign_correction_v2"
---

<objective>
Implementar a **geração da v2** (R4): hook aditivo `onBeforeImageProviderCall`, bloco único de não conformidade em tempo de montagem (sem editar `.md`), reuso de snapshot/imagens/identidade, `input_validation` skipped e o orquestrador do fluxo corretivo (consume → upload v2 → complete; fail pós-provider).

Purpose: produzir no máximo uma v2 orientada ao defeito sem nova reserva de crédito e sem referência da v1.
Output: hook + bloco + orquestrador + SUMMARY.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-CONTEXT.md
@.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-PATTERNS.md
@openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/ai-image-generation/spec.md
@openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-problem-report/spec.md

<interfaces>
`ImageGenerationService.generateImage(brief, context, onPhaseChange?, signal?, onMetricsEvent?)` (image-generation-service.ts:89-95); fases `input_validation`/`prompt_assembly`/`image_generation`/`quality_review`; `assemblePrompt(state, variables, previousIssues)` (:757-775) escolhe `campaign-image-director-${intent}`; loop `generateWithRetry` (:816-937) com 1ª chamada real ao provider na iteração `attempt===0` (:869-881).
Override `input_validation` skipped: `inputValidationOverride.productImageCheck = "brief_review_confirmed"` (:165-179).
`sanitizePromptText` (art-director-briefing.ts:55-57).
Upload de input: `uploadCampaignInputImage`/`uploadCampaignImage` (persistence.ts); storage bucket `campaign-images`; path v2 = `{storeId}/{campaignId}/v2.jpg`; remoção best-effort via `deleteCampaignImage` (persistence.ts:150-161).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hook aditivo onBeforeImageProviderCall (fire-once antes do provider)</name>
  <files>src/lib/image-generation/services/image-generation-service.ts</files>
  <read_first>
    - src/lib/image-generation/services/image-generation-service.ts (assinatura generateImage :89-95 + generateWithRetry :816-937, ponto :869-881)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/ai-image-generation/spec.md (requisito do hook)
  </read_first>
  <action>
    Adicionar hook **opcional e aditivo** ao `ImageGenerationService`: parâmetro/opção `onBeforeImageProviderCall?: () => Promise<void>` em `generateImage`. Executar o hook **fire-once por execução**, imediatamente antes da 1ª chamada real a `this.imageProvider.generateImage(...)` em `generateWithRetry`, na iteração `attempt === 0` (usar flag local `hookCalled`). Ausência do hook = comportamento atual exatamente igual. Erros do hook propagam (o caller trata como falha pré-provider). NÃO alterar a rota `generate-image` (chamadores sem hook intactos).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/lib/image-generation/services/image-generation-service.ts','utf8'); const need=['onBeforeImageProviderCall','attempt === 0','hookCalled']; const miss=need.filter(x=>!s.includes(x)); if(miss.length) throw new Error('faltando: '+miss.join(', ')); console.log('OK hook');"</automated>
  </verify>
  <acceptance_criteria>
    - `onBeforeImageProviderCall` presente como opcional; executa uma única vez em `attempt === 0`
    - Ausência do hook preserva o comportamento atual (sem callback)
  </acceptance_criteria>
  <done>Hook aditivo disparado antes da 1ª chamada ao provider</done>
</task>

<task type="auto">
  <name>Task 2: Bloco único de não conformidade + reuso da v2 (sem .md, sem candidateArtDataUrl)</name>
  <files>src/lib/image-generation/services/image-generation-service.ts, src/lib/image-generation/services/art-director-briefing.ts</files>
  <read_first>
    - src/lib/image-generation/services/image-generation-service.ts (assemblePrompt :757-775 + input_validation skip :165-179 + revisor :405-434)
    - src/lib/image-generation/services/art-director-briefing.ts (padrão de bloco condicional + sanitizePromptText :55-57)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/ai-image-generation/spec.md (bloco único + skip)
  </read_first>
  <action>
    1. Compor o prompt da v2 com o diretor por intent atual (`campaign-image-director-{offer|spotlight|exclusive}` selecionado por `commercial.intent` do snapshot) **+ um bloco único de não conformidade** acrescentado em tempo de montagem (padrão dos blocos condicionais do `assemblePrompt`): preâmbulo fixo anti-invenção/fidelidade ao briefing + `normalizedInstruction` saneada com `sanitizePromptText` e delimitada. O bloco apenas declara o defeito a evitar; NUNCA altera produto/preço/validade/aviso/identidade.
    2. **Sem `candidateArtDataUrl`**: a v1 não é enviada como referência. Reusar o mesmo snapshot `campaign_brief_v1`, imagens F41 (`media.images[].storagePath` → data URLs) e identidade vigente.
    3. `input_validation` emitida como `skipped` via override `brief_review_confirmed`; revisor automático intocado; **sem copy director**.
    4. **NÃO editar** os 4 `.md` em `prompts/` (`campaign-image-director.md`/`-offer`/`-spotlight`/`-exclusive`) — diff vazio verificado em teste.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('src/lib/image-generation/services/image-generation-service.ts','utf8'); const need=['normalizedInstruction','brief_review_confirmed','sanitizePromptText']; const miss=need.filter(x=>!s.includes(x)); if(miss.length) throw new Error('faltando: '+miss.join(', ')); const md=fs.readdirSync('prompts').filter(f=>f.startsWith('campaign-image-director')); if(md.length!==4) throw new Error('esperado 4 .md do diretor, veio '+md.length); console.log('OK bloco v2; .md: '+md.join(','));"</automated>
  </verify>
  <acceptance_criteria>
    - Bloco único de não conformidade composto em montagem com preâmbulo fixo + `normalizedInstruction` saneada
    - Nenhum uso de `candidateArtDataUrl` na v2; snapshot/imagens/identidade reusados
    - `input_validation` `skipped` via `brief_review_confirmed`; revisor e `.md` intocados
  </acceptance_criteria>
  <done>Prompt da v2 montado com bloco único e sem referência da v1</done>
</task>

<task type="auto">
  <name>Task 3: Orquestrador da v2 (consume via hook → upload → complete; fail)</name>
  <files>src/lib/campaign/correction-reports.ts</files>
  <read_first>
    - src/app/api/campaign/generate-image/route.ts (fluxo NDJSON + upload + eventos no mesmo run)
    - src/lib/campaign/persistence.ts (uploadCampaignImage/deleteCampaignImage)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-problem-report/spec.md (consume/complete/fail)
  </read_first>
  <action>
    Em `correction-reports.ts`, adicionar o orquestrador do fluxo corretivo **`generateCorrectionV2`** (nome fixo — usado pela rota `problem-report` do plano 07) :
    1. Baixar as imagens F41 (`media.images[].storagePath` do snapshot) do bucket `campaign-images` e reconverter em data URLs (helper novo `storagePathToDataUrl` no módulo — não existe pronto).
    2. Chamar `imageService.generateImage(brief, context, onPhase, signal, onMetrics, { onBeforeImageProviderCall })`, onde o hook chama `rpc("consume_campaign_correction_opportunity", { p_campaign_id, p_report_id, p_submission_id })` **imediatamente antes do provider**; eventos call-level sob o mesmo `operation_run_id`; `input_validation` `skipped`.
    3. Sucesso → upload da v2 em `{storeId}/{campaignId}/v2.jpg` → `rpc("complete_campaign_correction_v2", { p_campaign_id, p_report_id, p_submission_id, p_storage_path, p_mime_type, p_generation_metadata, p_render_snapshot })` (snapshot NUNCA do cliente — a RPC copia da v1).
    4. Falha **pré-provider** → consumo NÃO roda; caso permanece `open` (pode reenviar).
    5. Falha **pós-provider** → `rpc("fail_campaign_correction_v2", { p_campaign_id, p_report_id })` (atômica) + remoção best-effort do asset órfão (`deleteCampaignImage` em try/catch).
    Sem reserva de crédito/`credit_transactions`/`operation_key`; sem v3.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/lib/campaign/correction-reports.ts','utf8'); const need=['consume_campaign_correction_opportunity','complete_campaign_correction_v2','fail_campaign_correction_v2','onBeforeImageProviderCall','v2.jpg','storagePathToDataUrl']; const miss=need.filter(x=>!s.includes(x)); if(miss.length) throw new Error('faltando: '+miss.join(', ')); if(/reserveCredit|credit_transactions|operation_key/.test(s)) throw new Error('nao deve reservar credito/operation_key'); console.log('OK orquestrador v2');"</automated>
  </verify>
  <acceptance_criteria>
    - Hook chama `consume_campaign_correction_opportunity`; sucesso chama `complete_campaign_correction_v2` após upload; falha pós-provider chama `fail_campaign_correction_v2` + cleanup best-effort
    - Sem `reserveCredit`/`credit_transactions`/`operation_key`; sem v3
  </acceptance_criteria>
  <done>Orquestrador da v2 liga hook, upload e RPCs de conclusão/falha</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| relato → prompt do diretor | `normalizedInstruction` não confiável no bloco de não conformidade |
| serviço → provider de imagem | Chamada paga; consumo deve preceder exatamente a 1ª chamada |
| upload → storage | Asset v2 privado (bucket campaign-images) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-2-17 | Tampering | prompt injection via `normalizedInstruction` | mitigate | Delimitada + `sanitizePromptText`; preâmbulo anti-invenção; briefing imutável |
| T-37-2-18 | Repudiation | geração paga sem consumo | mitigate | Hook fire-once em `attempt===0` imediatamente antes do provider; falha pré-provider não consome |
| T-37-2-19 | Tampering | v1 usada como referência | mitigate | `candidateArtDataUrl` proibido na v2 (sem envio ao diretor/provider) |
| T-37-2-20 | Denial of Service | campanha presa em regenerating | mitigate | `fail_campaign_correction_v2` atômica + recuperação preguiçosa (planos 03/11) |
| T-37-2-07-SC | Tampering | dependências npm | accept | Nenhuma dependência nova — sem instalação de pacotes |
</threat_model>

<verification>
- Gates `node` das Tasks 1-3 verdes
- `git diff --stat` dos 4 `.md` do diretor vazio (verificado em teste no plano 16)
</verification>

<success_criteria>
- Hook aditivo + bloco único + orquestrador da v2 implementados; `.md`/revisor/copy director intocados
- `37-2-08-SUMMARY.md` criado
</success_criteria>

<output>
Create `.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-08-SUMMARY.md` when done
</output>

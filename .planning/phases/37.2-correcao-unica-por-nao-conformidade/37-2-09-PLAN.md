---
phase: 37.2-correcao-unica-por-nao-conformidade
plan: 09
type: execute
wave: 4
depends_on: [03]
files_modified:
  - src/app/api/campaign/[id]/approve/route.ts
autonomous: true
requirements: [F37.2-09]

must_haves:
  truths:
    - "Rota approve chama recover_campaign_correction_generation (best-effort) antes de aprovar"
    - "Rota approve chama approve_campaign_candidate (não approve_campaign_art_version direto)"
    - "correction_in_progress → 409; v1 superseded/não active → version_not_active → 409; resposta 200 inalterada"
  artifacts:
    - path: "src/app/api/campaign/[id]/approve/route.ts"
      provides: "Aprovação protegida + recuperação lazy"
      contains: "approve_campaign_candidate"
  key_links:
    - from: "src/app/api/campaign/[id]/approve/route.ts"
      to: "recover_campaign_correction_generation"
      via: "chamada best-effort antes de approve_campaign_candidate"
      pattern: "recover_campaign_correction_generation"
---

<objective>
Adaptar a **rota `POST /api/campaign/[id]/approve`** (R8): chamar a recuperação preguiçosa (best-effort) e trocar o alvo do RPC para `approve_campaign_candidate`, mapeando os novos erros, mantendo guards/resposta.

Purpose: serializar aprovar × consumir no banco e desbloquear aprovação de consumo preso.
Output: rota atualizada + SUMMARY.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-CONTEXT.md
@.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-PATTERNS.md
@openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-approval-gate/spec.md

<interfaces>
Rota atual (approve/route.ts): guards (:28-50) → `rpc("approve_campaign_art_version", { p_campaign_id, p_version_id })` (:67-70) → mapeamento `version_not_found`/`version_campaign_mismatch` → 404; `version_not_pending`/`version_not_active` → 409 (:77-82); resposta 200 (:88-91).
Nova RPC: `approve_campaign_candidate(p_campaign_id, p_version_id)` — trava candidata, valida `correction_in_progress=false`, chama a RPC F37.1 intacta.
Recuperação: `recover_campaign_correction_generation(p_campaign_id, p_stale_before DEFAULT NULL)`.
Constante: `CORRECTION_GENERATION_STALE_AFTER_MS = IMAGE_GENERATION_GLOBAL_TIMEOUT_MS + 30s` (image-generation/config.ts).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recuperação lazy best-effort + troca do alvo do RPC</name>
  <files>src/app/api/campaign/[id]/approve/route.ts</files>
  <read_first>
    - src/app/api/campaign/[id]/approve/route.ts (fluxo completo)
    - src/lib/image-generation/config.ts (IMAGE_GENERATION_GLOBAL_TIMEOUT_MS)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-approval-gate/spec.md (rota approve)
  </read_first>
  <action>
    1. Antes do RPC de aprovação, chamar `recover_campaign_correction_generation` em **try/catch best-effort** com `p_campaign_id: id` e `p_stale_before = new Date(Date.now() - CORRECTION_GENERATION_STALE_AFTER_MS).toISOString()` — **timestamptz**, nunca a duração em ms (a RPC compara `generation_started_at < COALESCE(p_stale_before, now() - interval '330 seconds')`; definir a constante no backend = `IMAGE_GENERATION_GLOBAL_TIMEOUT_MS + 30_000`); falha da recuperação não derruba a rota.
    2. Trocar o alvo para `rpc("approve_campaign_candidate", { p_campaign_id: id, p_version_id: parsed.data.versionId })`, mantendo guards e resposta 200 `{ campaignUrl, status: "approved" }`.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/app/api/campaign/[id]/approve/route.ts','utf8'); const need=['recover_campaign_correction_generation','approve_campaign_candidate','CORRECTION_GENERATION_STALE_AFTER_MS']; const miss=need.filter(x=>!s.includes(x)); if(miss.length) throw new Error('faltando: '+miss.join(', ')); console.log('OK approve protegida');"</automated>
  </verify>
  <acceptance_criteria>
    - `recover_campaign_correction_generation` chamada best-effort (try/catch) antes da aprovação
    - Alvo do RPC é `approve_campaign_candidate`; resposta 200 mantida
  </acceptance_criteria>
  <done>Rota approve com recuperação lazy e RPC protegida</done>
</task>

<task type="auto">
  <name>Task 2: Mapeamento de novos erros (correction_in_progress / version_not_active)</name>
  <files>src/app/api/campaign/[id]/approve/route.ts</files>
  <read_first>
    - src/app/api/campaign/[id]/approve/route.ts (mapeamento :77-82)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-approval-gate/spec.md (mapeamento de erros)
  </read_first>
  <action>
    Estender o mapeamento por mensagem do erro do RPC: `version_not_found`/`version_campaign_mismatch` → 404; `version_not_pending`/`version_not_active`/`correction_in_progress` → 409. Garantir que v1 `superseded`/não `active` cai em `version_not_active` → 409 (não aprovável após a v2).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/app/api/campaign/[id]/approve/route.ts','utf8'); const need=['correction_in_progress','version_not_active','version_not_pending','404','409']; const miss=need.filter(x=>!s.includes(x)); if(miss.length) throw new Error('faltando: '+miss.join(', ')); console.log('OK mapeamento');"</automated>
  </verify>
  <acceptance_criteria>
    - `correction_in_progress` mapeado para 409; `version_not_active`/`version_not_pending` → 409; not_found/mismatch → 404
  </acceptance_criteria>
  <done>Novos códigos de erro mapeados na rota</done>
</task>

<task type="auto">
  <name>Task 3: Verificação de ordem de locks e RPC F37.1 intacta</name>
  <files>src/app/api/campaign/[id]/approve/route.ts, supabase/migrations/20260906000002_f37_2_correction_flows.sql</files>
  <read_first>
    - supabase/migrations/20260906000002_f37_2_correction_flows.sql (approve_campaign_candidate + RPC F37.1)
    - supabase/migrations/20260901000002_f37_1_approve_campaign_art_version_rpc.sql (RPC F37.1 intacta)
  </read_first>
  <action>
    Confirmar (sem editar a migration além do que o plano 03 já fez) que: a rota só usa `approve_campaign_candidate`; `approve_campaign_candidate` trava a candidata → campanha e chama `approve_campaign_art_version` na mesma transação; não há `CREATE OR REPLACE FUNCTION public.approve_campaign_art_version` em lugar algum; a ordem de locks (candidata → campanha) é uniforme com consumo/recover. Registrar a verificação no SUMMARY.
  </action>
  <verify>
    <automated>$route = Get-Content -LiteralPath "src\app\api\campaign\[id]\approve\route.ts" -Raw; if ($route -match 'approve_campaign_art_version') { Write-Error "rota nao deve chamar a RPC F37.1 direto" } else { Write-Output "OK: rota chama apenas approve_campaign_candidate" }; $m = Get-Content -LiteralPath "supabase\migrations\20260906000002_f37_2_correction_flows.sql" -Raw; if ($m -match 'CREATE OR REPLACE FUNCTION public\.approve_campaign_art_version') { Write-Error "RPC F37.1 alterada" } else { Write-Output "OK: RPC F37.1 intacta" }</automated>
  </verify>
  <acceptance_criteria>
    - Rota não referencia `approve_campaign_art_version` diretamente
    - Nenhum `CREATE OR REPLACE FUNCTION public.approve_campaign_art_version` na migration
  </acceptance_criteria>
  <done>Ordem de locks verificada e RPC F37.1 intacta</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente → rota approve | CSRF/auth/ownership; body `{ versionId }` |
| rota → RPCs | Serialização de aprovar × consumir no banco |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-2-25 | Tampering | corrida aprovar × consumir | mitigate | `approve_campaign_candidate` trava candidata e valida `correction_in_progress=false`; consumo valida pendência |
| T-37-2-26 | Repudiation | consumo preso bloqueia aprovação | mitigate | `recover_campaign_correction_generation` best-effort antes de aprovar (p_stale_before = timeout+30s) |
| T-37-2-27 | Elevation of Privilege | aprovar versão superseded | mitigate | `version_not_active` para `superseded`; RPC exige `asset_status='active'` |
| T-37-2-09-SC | Tampering | dependências npm | accept | Nenhuma dependência nova — sem instalação de pacotes |
</threat_model>

<verification>
- Gates das Tasks 1-3 verdes
- `npm run typecheck` sem erros
</verification>

<success_criteria>
- Rota approve usa aprovação protegida + recuperação lazy, com mapeamento correto e RPC F37.1 intacta
- `37-2-09-SUMMARY.md` criado
</success_criteria>

<output>
Create `.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-09-SUMMARY.md` when done
</output>

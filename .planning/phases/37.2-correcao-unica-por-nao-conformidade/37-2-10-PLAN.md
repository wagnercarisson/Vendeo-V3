---
phase: 37.2-correcao-unica-por-nao-conformidade
plan: 10
type: execute
wave: 4
depends_on: []
files_modified:
  - src/components/campaign/campaign-problem-modal.tsx
  - src/components/campaign/campaign-approval-view.tsx
autonomous: true
requirements: [F37.2-10]

must_haves:
  truths:
    - "Modal 'Informar problema' de 1 etapa com preview da candidata, orientação corrigível × não, textarea obrigatório e [Enviar para análise]/[Cancelar]"
    - "Cancelar/X/ESC/backdrop fecham sem efeito; vazio/pontuação validado no clique sem criar caso/IA"
    - "CampaignApprovalView exibe [Aprovar arte] + [Informar problema] no pending v1 e desabilita Aprovar com caso em processamento"
    - "Estado pós-envio: processamento/resultados via router.refresh(); a11y/mobile/tema dark"
  artifacts:
    - path: "src/components/campaign/campaign-problem-modal.tsx"
      provides: "Modal de relato de problema (client)"
      contains: "Descreva o problema na arte"
    - path: "src/components/campaign/campaign-approval-view.tsx"
      provides: "Dois botões na revisão pending (v1)"
      contains: "Informar problema"
  key_links:
    - from: "src/components/campaign/campaign-problem-modal.tsx"
      to: "/api/campaign/[id]/problem-report"
      via: "fetch POST + router.refresh()"
      pattern: "problem-report"
---

<objective>
Implementar a **UI de revisão com dois caminhos** (R1): `campaign-problem-modal.tsx` (1 etapa) e a evolução de `campaign-approval-view.tsx` com [Aprovar arte] + [Informar problema].

Purpose: dar ao lojista o caminho de relatar defeito objetivo sem sair da página.
Output: modal + view atualizada + SUMMARY.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-CONTEXT.md
@.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-PATTERNS.md
@openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-problem-report/spec.md
@openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-approval-gate/spec.md

<interfaces>
Modal acessível (changelog-announcement.tsx:68-81): `fixed inset-0 z-50 flex items-center justify-center bg-black/50 p-4 backdrop-blur-sm` + `role="dialog" aria-modal="true" aria-label=...`; touch `min-h-[44px] min-w-[44px]`.
Fetch + refresh (campaign-approval-view.tsx:31-49): `useRouter`, `useState` de submitting/error, `fetch('/api/campaign/${campaignId}/problem-report', { method:'POST', headers, body: JSON.stringify({ text }) })`, `router.refresh()`.
Tema dark: tokens `#020617`/`#F8FAFC`/`#22C55E`; preview com `object-contain`.
Botão atual (campaign-approval-view.tsx:74-87): só primário "Aprovar e liberar campanha".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: campaign-problem-modal.tsx (1 etapa, preview + orientação + textarea)</name>
  <files>src/components/campaign/campaign-problem-modal.tsx</files>
  <read_first>
    - src/components/changelog/changelog-announcement.tsx (shell de modal acessível + ESC/backdrop/X)
    - src/components/campaign/campaign-approval-view.tsx (fetch + estados PT-BR)
    - src/components/ui/button.tsx
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-problem-report/spec.md
  </read_first>
  <action>
    Criar `src/components/campaign/campaign-problem-modal.tsx` (client, "use client"):
    - Props: `campaignId`, `candidateImageUrl`, `onClose`.
    - Shell `role="dialog" aria-modal="true"` com preview da candidata ativa (`<img ... className="object-contain" />`), orientação curta do que é corrigível (defeito objetivo: cortado, ilegível, dado divergente, inventado, duplicado, deformado, composição impeditiva) e do que não é (preferência estética, mudança de dados, rebriefing).
    - `label` "Descreva o problema na arte" + `<textarea>` obrigatório; botões **[Enviar para análise]** (primário) e **[Cancelar]**.
    - Tema dark (tokens), touch ≥ 44px, `aria-label`; layout sem scroll horizontal em 320/375px.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/components/campaign/campaign-problem-modal.tsx','utf8'); const need=['use client','role=\"dialog\"','aria-modal','Descreva o problema na arte','Enviar para análise','Cancelar','object-contain','min-h-[44px]']; const miss=need.filter(x=>!s.includes(x)); if(miss.length) throw new Error('faltando: '+miss.join(', ')); console.log('OK modal');"</automated>
  </verify>
  <acceptance_criteria>
    - Modal com preview `object-contain`, orientação corrigível × não, textarea obrigatório e botões corretos
    - `role="dialog"` + `aria-modal` + touch ≥ 44px + tema dark
  </acceptance_criteria>
  <done>Modal de relato de 1 etapa criado</done>
</task>

<task type="auto">
  <name>Task 2: Fechamentos sem efeito + validação no clique + envio</name>
  <files>src/components/campaign/campaign-problem-modal.tsx</files>
  <read_first>
    - src/components/changelog/changelog-announcement.tsx (ESC/backdrop handlers)
    - src/components/campaign/campaign-approval-view.tsx (fetch + erro PT-BR)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-problem-report/spec.md (validação/estados)
  </read_first>
  <action>
    1. Fechamentos **sem efeito**: [Cancelar], X, tecla ESC e clique no backdrop chamam `onClose` — sem aprovar, enviar ou criar caso.
    2. Validação **no clique** de [Enviar para análise]: texto vazio ou somente pontuação → erro amigável inline (ex.: "Descreva o problema na arte para continuar") — **sem** chamar a API.
    3. Envio válido → `POST /api/campaign/${campaignId}/problem-report` com `{ text }`; estado de **processamento** (não fecha sozinho); refletir resultados (`blocked`/`unclear`/`analysis_failed` com reformulação, ou transição `regenerating`) com `router.refresh()`; estados loading/erro PT-BR.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/components/campaign/campaign-problem-modal.tsx','utf8'); const need=['Escape','problem-report','router.refresh','analysisState','guidance','Cancelar']; const miss=need.filter(x=>!s.includes(x)); if(miss.length) throw new Error('faltando: '+miss.join(', ')); console.log('OK fechamentos+envio');"</automated>
  </verify>
  <acceptance_criteria>
    - Cancelar/X/ESC/backdrop fecham sem efeito
    - Vazio/pontuação → erro amigável sem chamar a API
    - Envio válido → POST + `router.refresh()` + estados de resultado PT-BR
  </acceptance_criteria>
  <done>Modal valida, envia e reflete estados sem fechar sozinho</done>
</task>

<task type="auto">
  <name>Task 3: campaign-approval-view.tsx — dois botões + guarda de UX</name>
  <files>src/components/campaign/campaign-approval-view.tsx</files>
  <read_first>
    - src/components/campaign/campaign-approval-view.tsx (botão primário :74-87 + props)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-approval-gate/spec.md (tela de revisão)
  </read_first>
  <action>
    Evoluir `campaign-approval-view.tsx`:
    - Botão primário **[Aprovar arte]** (mantém o POST /approve com o `versionId` da candidata) e botão secundário **[Informar problema]** que abre o modal **sem sair da página**.
    - Props novas para controlar a exibição de [Informar problema]: presente apenas quando a candidata é a **v1** e ainda há oportunidade (sem consumo); na v2, apenas [Aprovar arte].
    - **Guarda de UX:** desabilitar [Aprovar arte] enquanto houver caso em processamento (`correction_in_progress`/`regenerating`) — reforço, a garantia é o banco.
    - Microcopy PT-BR, touch ≥ 44px, a11y, tema dark; imagem sem recorte.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/components/campaign/campaign-approval-view.tsx','utf8'); const need=['Informar problema','Aprovar arte','campaign-problem-modal','disabled']; const miss=need.filter(x=>!s.includes(x)); if(miss.length) throw new Error('faltando: '+miss.join(', ')); console.log('OK view');"</automated>
  </verify>
  <acceptance_criteria>
    - Dois botões no `pending` v1; [Informar problema] abre o modal sem sair da página
    - [Aprovar arte] desabilitado com caso em processamento; v2 sem [Informar problema]
  </acceptance_criteria>
  <done>View de revisão oferece os dois caminhos com guarda de UX</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usuário → modal → rota | Texto do lojista via UI; sem confiança no cliente |
| modal → modal | Fechamento indevido não pode disparar ação |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-2-28 | Tampering | envio acidental/duplo | mitigate | Validação no clique; botão desabilitado durante processamento; garantia real é o banco |
| T-37-2-29 | Information Disclosure | preview da candidata | mitigate | Signed URL da candidata ativa; v1 não é oferecida após a v2 |
| T-37-2-30 | Denial of Service | acessibilidade mobile | mitigate | Touch ≥ 44px, `object-contain`, sem scroll horizontal em 320/375px |
| T-37-2-10-SC | Tampering | dependências npm | accept | Nenhuma dependência nova — componentes existentes reusados |
</threat_model>

<verification>
- Gates `node` das Tasks 1-3 verdes
- `npm run typecheck` sem erros
</verification>

<success_criteria>
- Modal de 1 etapa + dois botões na revisão implementados com a11y/mobile/tema
- `37-2-10-SUMMARY.md` criado
</success_criteria>

<output>
Create `.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-10-SUMMARY.md` when done
</output>

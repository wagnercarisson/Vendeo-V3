---
phase: 37.2-correcao-unica-por-nao-conformidade
plan: 11
type: execute
wave: 5
depends_on: [03, 10]
files_modified:
  - src/app/(app)/campanhas/[id]/page.tsx
  - src/app/(app)/campanhas/[id]/client.tsx
autonomous: true
requirements: [F37.2-11]

must_haves:
  truths:
    - "page.tsx chama recover_campaign_correction_generation ao derivar regenerating e, quando recovered:true, RECARREGA campanha/versões antes de recalcular"
    - "page.tsx passa props para pending v1/v2 e regenerating"
    - "client.tsx renderiza regenerating como progresso da v2 (sem cair no ReadyView) e pending v2 com apenas [Aprovar arte]"
  artifacts:
    - path: "src/app/(app)/campanhas/[id]/page.tsx"
      provides: "Recuperação lazy + props pending/regenerating"
      contains: "recover_campaign_correction_generation"
    - path: "src/app/(app)/campanhas/[id]/client.tsx"
      provides: "Render de regenerating sem ReadyView"
      contains: "regenerating"
  key_links:
    - from: "src/app/(app)/campanhas/[id]/page.tsx"
      to: "src/lib/campaign/display.ts"
      via: "computeApprovalState após nova leitura quando recovered"
      pattern: "computeApprovalState"
---

<objective>
Evoluir a **página `/campanhas/[id]`** (R1/R7): recuperação preguiçosa com reload de estado, props para `pending` (v1/v2) e `regenerating`, e render do progresso da v2 sem cair no `ReadyView`.

Purpose: exercitar o estado `regenerating` e nunca re-derivar sobre objetos em memória desatualizados.
Output: page.tsx + client.tsx + SUMMARY.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-CONTEXT.md
@.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-PATTERNS.md
@openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-page-ui/spec.md
@openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-approval-gate/spec.md

<interfaces>
page.tsx atual: bloco `if (campaign.status === "ready")` (:45-59) lê flag + `listArtVersions` → `computeApprovalState` → props `approval` (`state`/`candidateImageUrl`/`candidateVersionId`).
client.tsx atual: branch de render (:55-67) — `CampaignApprovalView` só quando `pending` + candidata; senão `ReadyView`; `regenerating` cai no fallback (gap).
display.ts: `ApprovalDisplayState` inclui `regenerating`; `computeApprovalState` (:140-166); `getActiveCandidateArtVersion` (:185).
Recuperação: `recover_campaign_correction_generation(p_campaign_id, p_stale_before DEFAULT NULL)` → `{ recovered: boolean }`; constante `CORRECTION_GENERATION_STALE_AFTER_MS`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: page.tsx — recuperação lazy + reload + props</name>
  <files>src/app/(app)/campanhas/[id]/page.tsx</files>
  <read_first>
    - src/app/(app)/campanhas/[id]/page.tsx (bloco ready :45-59)
    - src/lib/campaign/display.ts (computeApprovalState/getActiveCandidateArtVersion)
    - src/lib/campaign/persistence.ts (getCampaign/listArtVersions)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-page-ui/spec.md
  </read_first>
  <action>
    Em `page.tsx`, no bloco `ready`:
    1. Derivar o estado com `computeApprovalState`; se derivar `regenerating`, chamar `recover_campaign_correction_generation` (best-effort, try/catch, `p_stale_before` = `new Date(Date.now() - CORRECTION_GENERATION_STALE_AFTER_MS).toISOString()` — **timestamptz**, nunca a duração em ms).
    2. **Se `recovered:true`, RECARREGAR campanha/versões do banco (nova leitura de `getCampaign`/`listArtVersions`) ANTES de recalcular o estado** — nunca re-derivar sobre os mesmos objetos em memória.
    3. Passar props para `pending` (v1 e v2) e `regenerating` (candidata ativa, se v1 ou v2, e disponibilidade de oportunidade).
    Não alterar `isDeliveryReleased`/`getActiveCandidateArtVersion`.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/app/(app)/campanhas/[id]/page.tsx','utf8'); const need=['recover_campaign_correction_generation','recovered','regenerating','CORRECTION_GENERATION_STALE_AFTER_MS','listArtVersions']; const miss=need.filter(x=>!s.includes(x)); if(miss.length) throw new Error('faltando: '+miss.join(', ')); console.log('OK page');"</automated>
  </verify>
  <acceptance_criteria>
    - Recuperação chamada em `regenerating`; `recovered:true` força nova leitura antes de recalcular
    - Props de `pending` (v1/v2) e `regenerating` passadas ao client
  </acceptance_criteria>
  <done>Página recupera o consumo preso e recarrega o estado antes de derivar</done>
</task>

<task type="auto">
  <name>Task 2: client.tsx — regenerating sem ReadyView + pending v2</name>
  <files>src/app/(app)/campanhas/[id]/client.tsx</files>
  <read_first>
    - src/app/(app)/campanhas/[id]/client.tsx (branch de render :55-67)
    - src/components/campaign/campaign-approval-view.tsx (props de v1/v2)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-page-ui/spec.md
  </read_first>
  <action>
    Em `client.tsx`:
    1. Adicionar branch `props.approval?.state.status === "regenerating"` → renderizar estado de **progresso da v2** ("Corrigindo a arte..."), **sem** download/copy/approve e **sem** cair no `ReadyView`.
    2. `pending` v2 → `CampaignApprovalView` com [Aprovar arte] apenas (sem [Informar problema], sem volta à v1).
    3. Microcopy PT-BR, a11y, tema dark; imagem sem recorte.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/app/(app)/campanhas/[id]/client.tsx','utf8'); const need=['regenerating','CampaignApprovalView','Corrigindo a arte']; const miss=need.filter(x=>!s.includes(x)); if(miss.length) throw new Error('faltando: '+miss.join(', ')); console.log('OK client');"</automated>
  </verify>
  <acceptance_criteria>
    - `regenerating` renderiza progresso sem `ReadyView`/actions de entrega
    - `pending` v2 exibe apenas [Aprovar arte]
  </acceptance_criteria>
  <done>Client cobre regenerating e pending v2 sem galeria/volta à v1</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| servidor → página | Estado derivado do banco (não do cliente) |
| página → usuário | Exibição da candidata única |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-2-31 | Tampering | estado obsoleto em memória após recuperação | mitigate | `recovered:true` força nova leitura de campanha/versões antes de recalcular |
| T-37-2-32 | Information Disclosure | v1 exposta após a v2 | mitigate | Apenas a candidata ativa é servida; v1 `superseded` não é oferecida |
| T-37-2-33 | Denial of Service | regenerating cai no ReadyView | mitigate | Branch dedicado de progresso; sem download/copy/approve durante a correção |
| T-37-2-11-SC | Tampering | dependências npm | accept | Nenhuma dependência nova — sem instalação de pacotes |
</threat_model>

<verification>
- Gates `node` das Tasks 1-2 verdes
- `npm run typecheck` sem erros
</verification>

<success_criteria>
- Página deriva/exibe pending (v1/v2) e regenerating corretamente, com recuperação lazy + reload
- `37-2-11-SUMMARY.md` criado
</success_criteria>

<output>
Create `.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-11-SUMMARY.md` when done
</output>

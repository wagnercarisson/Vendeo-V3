---
phase: 37.2-correcao-unica-por-nao-conformidade
plan: 12
type: execute
wave: 5
depends_on: [05]
files_modified:
  - src/app/(app)/admin/campaign-reports/page.tsx
  - src/app/(app)/admin/campaign-reports/[reportId]/page.tsx
  - src/app/api/admin/campaign-reports/[reportId]/route.ts
  - src/app/(app)/admin/layout.tsx
autonomous: true
requirements: [F37.2-12]

must_haves:
  truths:
    - "Lista /admin/campaign-reports (requireAdmin) com decisão corrente (maior attempt_number), filtros e paginação"
    - "Detalhe /admin/campaign-reports/[reportId] com v1 × v2 lado a lado (signed URLs service_role) + histórico por attempt_number + operation_run_id linkável + aprovação derivada"
    - "Marcação 'revisado pelo suporte' ortogonal (não muda status/rejection_count/versões); link no layout admin"
  artifacts:
    - path: "src/app/(app)/admin/campaign-reports/page.tsx"
      provides: "Fila admin de relatos"
      contains: "campaign_correction_reports"
    - path: "src/app/(app)/admin/campaign-reports/[reportId]/page.tsx"
      provides: "Detalhe v1×v2 + histórico"
      contains: "attempt_number"
  key_links:
    - from: "src/app/(app)/admin/campaign-reports/[reportId]/page.tsx"
      to: "/admin/ai-operation-costs"
      via: "operation_run_id linkável ao painel F38.2"
      pattern: "operationRunId"
---

<objective>
Implementar a **fila administrativa** (R6): listagem/filtros/paginação, detalhe v1 × v2 com histórico de tentativas, marcação ortogonal "revisado pelo suporte" e link no layout admin.

Purpose: dar ao suporte visibilidade auditável dos casos de correção.
Output: páginas admin + rota de marcação + link + SUMMARY.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-CONTEXT.md
@.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-PATTERNS.md
@openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/admin-campaign-reports/spec.md

<interfaces>
Padrão admin (access-requests/page.tsx:35-72,175-191): `try { await requireAdmin() } catch { <acesso negado> }`; `const sp = await searchParams`; paginação `<Link>`; `export const dynamic = "force-dynamic"`; server pages chamam serviços internos (sem chamar a própria API).
Serviços internos (plan 05): `listCorrectionReports({ filters, page })`, `getCorrectionReportDetail(reportId)`, `markReportReviewedBySupport(reportId, actorId)`.
Signed URL (display.ts:63-78): `supabaseAdmin.storage.from("campaign-images").createSignedUrl(path, 3600)`.
Rota admin de mutação (access-requests/[id]/route.ts:16-62): `requireAdmin` + `requireSameOrigin` + zod + update + resposta JSON.
Painel F38.2: `/admin/ai-operation-costs?operationRunId=<uuid>`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lista /admin/campaign-reports</name>
  <files>src/app/(app)/admin/campaign-reports/page.tsx</files>
  <read_first>
    - src/app/(app)/admin/access-requests/page.tsx (padrão de lista/filtros/paginação)
    - src/lib/campaign/correction-reports.ts (listCorrectionReports — plan 05)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/admin-campaign-reports/spec.md
  </read_first>
  <action>
    Criar `src/app/(app)/admin/campaign-reports/page.tsx` (server, `export const dynamic = "force-dynamic"`):
    - `requireAdmin()` em try/catch → bloco de acesso negado.
    - Consumir `listCorrectionReports({ filters, page })` (serviço interno — sem chamar a própria API).
    - Colunas: campanha, loja, status do caso, **decisão corrente = `analysis_state` da tentativa de maior `attempt_number`**, `reported_version_id`/`generated_version_id`, data, revisado pelo suporte.
    - Filtros (status do caso, `analysis_state` da tentativa vigente, revisado×não revisado) e paginação via `<Link>`; linha navega ao detalhe.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/app/(app)/admin/campaign-reports/page.tsx','utf8'); const need=['requireAdmin','listCorrectionReports','force-dynamic','attempt_number','Link']; const miss=need.filter(x=>!s.includes(x)); if(miss.length) throw new Error('faltando: '+miss.join(', ')); console.log('OK lista admin');"</automated>
  </verify>
  <acceptance_criteria>
    - Lista com `requireAdmin`, decisão corrente por `attempt_number`, filtros e paginação
  </acceptance_criteria>
  <done>Lista admin de relatos implementada</done>
</task>

<task type="auto">
  <name>Task 2: Detalhe [reportId] com v1 × v2 + histórico</name>
  <files>src/app/(app)/admin/campaign-reports/[reportId]/page.tsx</files>
  <read_first>
    - src/app/(app)/admin/reviews/review-detail.tsx (dl grid + histórico)
    - src/app/(app)/admin/ai-operation-costs/page.tsx (link operationRunId)
    - src/lib/campaign/display.ts (generateSignedPreviewUrl)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/admin-campaign-reports/spec.md
  </read_first>
  <action>
    Criar `src/app/(app)/admin/campaign-reports/[reportId]/page.tsx` (server, `requireAdmin`):
    - Consumir `getCorrectionReportDetail(reportId)` (plan 05).
    - **v1 × v2 lado a lado** com signed URLs server-side (`generateSignedPreviewUrl`, incluindo v1 `superseded` com path preservado).
    - **Histórico de tentativas** por `attempt_number`: nº → texto → `analysis_state` → categoria → instrução → `created_at`/`completed_at`.
    - Status/consumo (`status`, `rejection_count`, `generation_started_at`, `generated_version_id`); `operation_run_id` **linkável** a `/admin/ai-operation-costs?operationRunId=<uuid>`; **aprovação derivada** de `campaigns` (sem campos espelhados).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/app/(app)/admin/campaign-reports/[reportId]/page.tsx','utf8'); const need=['requireAdmin','getCorrectionReportDetail','attempt_number','operationRunId','generateSignedPreviewUrl','approved_version_id']; const miss=need.filter(x=>!s.includes(x)); if(miss.length) throw new Error('faltando: '+miss.join(', ')); console.log('OK detalhe admin');"</automated>
  </verify>
  <acceptance_criteria>
    - Detalhe com v1×v2 (signed URLs), histórico por `attempt_number`, status/consumo, `operation_run_id` linkável e aprovação derivada
  </acceptance_criteria>
  <done>Detalhe admin auditável implementado</done>
</task>

<task type="auto">
  <name>Task 3: Rota "marcar revisado" + link no layout admin</name>
  <files>src/app/api/admin/campaign-reports/[reportId]/route.ts, src/app/(app)/admin/layout.tsx</files>
  <read_first>
    - src/app/api/admin/access-requests/[id]/route.ts (padrão de rota admin de mutação)
    - src/app/(app)/admin/layout.tsx (nav)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/admin-campaign-reports/spec.md (marcação/navegação)
  </read_first>
  <action>
    1. Criar `src/app/api/admin/campaign-reports/[reportId]/route.ts` (POST/PATCH): `requireAdmin` + `requireSameOrigin` + validação defensiva → `markReportReviewedBySupport(reportId, admin.userId)`; resposta JSON. Marcação **ortogonal** — não muda `status`/`rejection_count`/versões/aprovação.
    2. Em `src/app/(app)/admin/layout.tsx`, adicionar `<Link href="/admin/campaign-reports">Relatos de correção</Link>` na `<nav>`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const r=fs.readFileSync('src/app/api/admin/campaign-reports/[reportId]/route.ts','utf8'); const need=['requireAdmin','requireSameOrigin','markReportReviewedBySupport']; const miss=need.filter(x=>!r.includes(x)); if(miss.length) throw new Error('rota faltando: '+miss.join(', ')); const l=fs.readFileSync('src/app/(app)/admin/layout.tsx','utf8'); if(!l.includes('/admin/campaign-reports')) throw new Error('link ausente no layout'); console.log('OK marcacao+link');"</automated>
  </verify>
  <acceptance_criteria>
    - Rota de marcação com `requireAdmin`/`requireSameOrigin` chamando `markReportReviewedBySupport` (ortogonal)
    - Link `/admin/campaign-reports` presente no layout admin
  </acceptance_criteria>
  <done>Marcação ortogonal e navegação admin implementadas</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin → páginas/rota | Acesso restrito a admin (requireAdmin) |
| admin → signed URLs | Exposição controlada de v1/v2 (service_role) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-2-34 | Elevation of Privilege | páginas/rota admin | mitigate | `requireAdmin` em todas as páginas/rota; acesso negado para não-admin |
| T-37-2-35 | Tampering | marcação revisado | mitigate | Atualiza apenas `reviewed_by_support_at`/`_user`; não toca no fluxo/oportunidade |
| T-37-2-36 | Information Disclosure | assets v1/v2 | mitigate | Signed URLs service_role (curtas); sem duplicar arquivos; v1 não exposta ao app |
| T-37-2-12-SC | Tampering | dependências npm | accept | Nenhuma dependência nova — sem instalação de pacotes |
</threat_model>

<verification>
- Gates `node` das Tasks 1-3 verdes
- `npm run typecheck` sem erros
</verification>

<success_criteria>
- Fila admin (lista/detalhe/marcação/link) implementada server-side
- `37-2-12-SUMMARY.md` criado
</success_criteria>

<output>
Create `.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-12-SUMMARY.md` when done
</output>

---
phase: 37.2-correcao-unica-por-nao-conformidade
plan: 13
type: execute
wave: 6
depends_on: [06]
files_modified:
  - src/__tests__/lib/campaign/correction-intent-service.test.ts
autonomous: true
requirements: [F37.2-13]

must_haves:
  truths:
    - "Testes 12.1-12.9 cobrem análise/classificação, conteúdo não confiável e custo call-level"
    - "Testes usam mocks do TextProvider (sem modelo real) e validam o evento campaign_correction_analysis"
    - "npx vitest run do arquivo passa"
  artifacts:
    - path: "src/__tests__/lib/campaign/correction-intent-service.test.ts"
      provides: "Testes 12.1-12.9 do CorrectionIntentService"
      contains: "campaign_correction_analysis"
  key_links:
    - from: "src/__tests__/lib/campaign/correction-intent-service.test.ts"
      to: "src/lib/campaign/correction-intent-service.ts"
      via: "import do serviço + mock do TextProvider"
      pattern: "analyzeReport"
---

<objective>
Criar os **testes de análise/classificação** (alinhamento §8.1, 12.1-12.9): classificação por categoria, blocked/unclear, JSON inválido → unclear, timeout/vazio → analysis_failed, conteúdo não confiável e evento de custo.

Purpose: provar o contrato do `CorrectionIntentService` sem chamar IA real.
Output: arquivo de teste + SUMMARY.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-CONTEXT.md
@openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-correction-analysis/spec.md
@openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/ai-cost-tracker/spec.md

<interfaces>
Mock do provider: `{ name: "mock", generateText: vi.fn().mockResolvedValue({ content, usage: { promptTokens, completionTokens }, model }) }`.
`CorrectionIntentService` aceita provider injetado (plan 06); `AiCostTracker`/`resolveAiCost` mockados com `vi.mock`.
Casos: eligible (cada categoria), blocked (mudança de dado/“outra opção”/rebriefing), unclear, JSON inválido → unclear, timeout/vazio → analysis_failed, `{{ }}` no texto → saneado, evento `campaign_correction_analysis` com `provider`/`model`/`usage`/`attemptNumber`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Testes 12.1-12.5 (classificação eligible/blocked/unclear)</name>
  <files>src/__tests__/lib/campaign/correction-intent-service.test.ts</files>
  <read_first>
    - src/lib/campaign/correction-intent-service.ts (plan 06 — API analyzeReport)
    - src/__tests__/lib/copy/copy-director-service.test.ts (padrão de mock do provider, se existir)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-correction-analysis/spec.md
  </read_first>
  <action>
    Criar o arquivo de teste com mock do `TextProvider` (sem IA real):
    - **12.1** texto vazio/pontuação é rejeitado na rota (validação local) — assertar que o serviço não é chamado (teste de contrato da rota pode ficar no plano 17; aqui validar o helper/validador de pontuação exportado, se houver).
    - **12.2** caso + primeira tentativa nascem juntos antes da IA (coberto por fonte no plano 14; aqui validar que `analyzeReport` recebe o texto e não toca a imagem).
    - **12.3** cada categoria elegível → `eligible` com `category` + `normalizedInstruction` (mock retorna JSON por categoria).
    - **12.4** blocked (mudança de preço por preferência, "outra opção", rebriefing) → `blocked` com `guidance`; sem gerar/consumir.
    - **12.5** unclear → `unclear` com orientação/exemplo; reformulação não consome.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/lib/campaign/correction-intent-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Testes 12.1-12.5 verdes; mocks do provider (nenhuma chamada real)
    - Categorias elegíveis e motivos bloqueados cobertos
  </acceptance_criteria>
  <done>Classificação eligible/blocked/unclear coberta por testes</done>
</task>

<task type="auto">
  <name>Task 2: Testes 12.6-12.8 (JSON inválido, timeout/vazio, anti-invenção)</name>
  <files>src/__tests__/lib/campaign/correction-intent-service.test.ts</files>
  <read_first>
    - src/lib/campaign/correction-intent-service.ts (parse defensivo + sanitize)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-correction-analysis/spec.md
  </read_first>
  <action>
    Adicionar:
    - **12.6** provider devolve texto livre/JSON fora do schema → `unclear`.
    - **12.7** provider lança timeout/erro de transporte ou devolve conteúdo vazio → `analysis_failed`.
    - **12.8** `normalizedInstruction` com `{{ }}`/delimitadores → bloco saneado (sem placeholder não resolvido); briefing inalterado.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/lib/campaign/correction-intent-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - JSON inválido → `unclear`; timeout/vazio → `analysis_failed`; conteúdo não confiável saneado
  </acceptance_criteria>
  <done>Tratamento de saída fora do contrato e falhas coberto</done>
</task>

<task type="auto">
  <name>Task 3: Teste 12.9 (evento call-level campaign_correction_analysis)</name>
  <files>src/__tests__/lib/campaign/correction-intent-service.test.ts</files>
  <read_first>
    - src/lib/ai-cost/tracker.ts (contrato record)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/ai-cost-tracker/spec.md
  </read_first>
  <action>
    Adicionar **12.9**: assertar que `AiCostTracker.record` foi chamado com `generationType: "campaign_correction_analysis"`, `provider` = `textProvider.name`, `model`/`usage` do resultado, `attemptNumber` da submissão e o `operationRunId` da campanha; falha de transporte registra `status: "failed"`; sem `credit_transactions`/`operation_key`.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/lib/campaign/correction-intent-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Evento `campaign_correction_analysis` assertado com provider/model/usage/attemptNumber no mesmo run; falha → `status: "failed"`
  </acceptance_criteria>
  <done>Custo call-level da análise coberto por teste</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teste → serviço | Mocks do provider e do tracker (sem IA real) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-2-37 | Tampering | prompt injection no teste | mitigate | Caso 12.8 verifica saneamento de `{{ }}`/delimitadores |
| T-37-2-38 | Spoofing | saída fora do contrato | mitigate | Casos 12.6/12.7 verificam unclear/analysis_failed |
| T-37-2-13-SC | Tampering | dependências npm | accept | Nenhuma dependência nova — vitest já instalado |
</threat_model>

<verification>
- `npx vitest run src/__tests__/lib/campaign/correction-intent-service.test.ts` verde
</verification>

<success_criteria>
- 12.1-12.9 implementados e verdes
- `37-2-13-SUMMARY.md` criado
</success_criteria>

<output>
Create `.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-13-SUMMARY.md` when done
</output>

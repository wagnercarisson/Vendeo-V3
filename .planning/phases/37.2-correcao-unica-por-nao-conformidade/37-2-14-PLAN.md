---
phase: 37.2-correcao-unica-por-nao-conformidade
plan: 14
type: execute
wave: 6
depends_on: [03]
files_modified:
  - src/__tests__/api/campaign-correction-begin-rpc.test.ts
autonomous: true
requirements: [F37.2-14]

must_haves:
  truths:
    - "Testes 13.1-13.10 cobrem locks candidata→campanha→relato, corrida com aprovação, analyzing expirada, rate limit/teto absoluto, analysis_in_progress, lease/vigência, validação semântica e attempt_number"
    - "RPCs validadas por leitura da migration do disco (padrão campaign-approve-route.test.ts)"
    - "npx vitest run do arquivo passa"
  artifacts:
    - path: "src/__tests__/api/campaign-correction-begin-rpc.test.ts"
      provides: "Testes 13.1-13.10 do lifecycle da tentativa (begin/complete_analysis)"
      contains: "begin_campaign_correction_submission"
  key_links:
    - from: "src/__tests__/api/campaign-correction-begin-rpc.test.ts"
      to: "supabase/migrations/20260906000002_f37_2_correction_flows.sql"
      via: "readFileSync + expect(sql).toContain(...)"
      pattern: "FOR UPDATE"
---

<objective>
Criar os **testes do lifecycle da tentativa** (alinhamento §8.2 + achados 1/3, 13.1-13.10), validando as RPCs `begin_campaign_correction_submission`/`complete_campaign_correction_analysis` por fonte (leitura da migration).

Purpose: provar guardas de concorrência/rate limit/lease do banco.
Output: arquivo de teste + SUMMARY.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-CONTEXT.md
@openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-correction-reports/spec.md
@.planning/phases/37.1-approval-gate-candidata-unica/37-1-11-PLAN.md

<interfaces>
Padrão RPC-por-fonte (campaign-approve-route.test.ts:97-112): `readFileSync(path.join(process.cwd(),"supabase","migrations","20260906000002_f37_2_correction_flows.sql"),"utf8")` + `expect(sql).toContain("FOR UPDATE")`.
Asserts por fonte: ordem de locks (candidata antes de campanha antes de relato), `attempt_number`, `COALESCE(MAX(attempt_number),0)+1`, `interval '30 minutes'`, `interval '2 minutes'`, `analysis_in_progress`, `rate_limit_exceeded`, `analysis_lease_expired`, `submission_stale`, `submission_not_analyzing`, `non_eligible_must_not_have_generation_fields`, `eligible_requires_category_and_instruction`, `invalid_analysis_state`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Testes 13.1-13.6 (locks, corrida, expiração, rate limit, serialização)</name>
  <files>src/__tests__/api/campaign-correction-begin-rpc.test.ts</files>
  <read_first>
    - supabase/migrations/20260906000002_f37_2_correction_flows.sql (RPC begin)
    - src/__tests__/api/campaign-approve-route.test.ts (padrão RPC-por-fonte)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-correction-reports/spec.md
  </read_first>
  <action>
    Criar o arquivo com asserts por fonte:
    - **13.1** begin trava candidata → campanha → relato (verificar a ORDEM das ocorrências de `FOR UPDATE` na migration; `UNIQUE (campaign_id)` presente como defesa).
    - **13.2** begin valida `status='ready'`/`approval_status='pending_approval'`/`approved_version_id IS NULL`/`rejection_count=0`/ausência de v2 → erros `campaign_not_pending`/`already_consumed`/`no_active_candidate`.
    - **13.3** finaliza `analyzing` expiradas (`analysis_expires_at < now()`) como `analysis_failed` com `completed_at`.
    - **13.4** `completed_at` + janela `now() - interval '30 minutes'` + `analysis_expires_at` no nascimento.
    - **13.5** teto absoluto: `rate_limit_exceeded` para a 4ª (>=3) independentemente de `analysis_failed`.
    - **13.6** serialização: `FOR UPDATE` no relato antes do `MAX(attempt_number)` (sem perder atualização).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/api/campaign-correction-begin-rpc.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 13.1-13.6 verdes; ordem de locks assertada; rate limit/teto e expiração cobertos
  </acceptance_criteria>
  <done>Guardas de concorrência/rate limit do begin cobertas</done>
</task>

<task type="auto">
  <name>Task 2: Testes 13.7-13.10 (analysis_in_progress, lease/vigência, semântica, attempt_number)</name>
  <files>src/__tests__/api/campaign-correction-begin-rpc.test.ts</files>
  <read_first>
    - supabase/migrations/20260906000002_f37_2_correction_flows.sql (RPCs begin + complete_analysis)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-correction-reports/spec.md
  </read_first>
  <action>
    Adicionar:
    - **13.7** `analyzing` ainda válida → `analysis_in_progress` (sem criar linha/IA).
    - **13.8** conclusão condicionada a `analysis_state='analyzing'` + `analysis_expires_at >= now()` + `attempt_number = (SELECT MAX(...))`; `rowcount=0` → `submission_not_analyzing`/`analysis_lease_expired`/`submission_stale`.
    - **13.9** validação semântica: `analyzing` final → `invalid_analysis_state`; `eligible` sem categoria/instrução → `eligible_requires_category_and_instruction`; `blocked|unclear|analysis_failed` com categoria/instrução → `non_eligible_must_not_have_generation_fields`; CHECKs de tabela mantêm o invariante.
    - **13.10** `attempt_number` sequencial `COALESCE(MAX(attempt_number),0)+1` + `UNIQUE (report_id, attempt_number)`; ordenação determinística.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/api/campaign-correction-begin-rpc.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 13.7-13.10 verdes; lease/vigência/semântica/attempt_number assertados
  </acceptance_criteria>
  <done>Conclusão da análise e determinismo da tentativa cobertos</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teste → migration SQL | Validação por fonte (sem banco) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-2-39 | Tampering | análise simultânea/resposta tardia | mitigate | Casos 13.7-13.9 assertam as guardas de lease/vigência/estado |
| T-37-2-40 | Repudiation | ordenação não determinística | mitigate | Caso 13.10 asserta `attempt_number` + UNIQUE |
| T-37-2-14-SC | Tampering | dependências npm | accept | Nenhuma dependência nova — vitest já instalado |
</threat_model>

<verification>
- `npx vitest run src/__tests__/api/campaign-correction-begin-rpc.test.ts` verde
</verification>

<success_criteria>
- 13.1-13.10 implementados e verdes
- `37-2-14-SUMMARY.md` criado
</success_criteria>

<output>
Create `.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-14-SUMMARY.md` when done
</output>

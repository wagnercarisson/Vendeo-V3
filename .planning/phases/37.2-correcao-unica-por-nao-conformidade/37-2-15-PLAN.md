---
phase: 37.2-correcao-unica-por-nao-conformidade
plan: 15
type: execute
wave: 6
depends_on: [03, 05]
files_modified:
  - src/__tests__/api/campaign-correction-consume-recover.test.ts
autonomous: true
requirements: [F37.2-15]

must_haves:
  truths:
    - "Testes 14.1-14.9 cobrem falha pré-provider, consumo, fail pós-provider, sem duplo incremento, serialização aprovar×consumir e recuperação preguiçosa"
    - "RPCs validadas por leitura da migration do disco"
    - "npx vitest run do arquivo passa"
  artifacts:
    - path: "src/__tests__/api/campaign-correction-consume-recover.test.ts"
      provides: "Testes 14.1-14.9 de consumo/serialização/recuperação"
      contains: "consume_campaign_correction_opportunity"
  key_links:
    - from: "src/__tests__/api/campaign-correction-consume-recover.test.ts"
      to: "supabase/migrations/20260906000002_f37_2_correction_flows.sql"
      via: "readFileSync + asserts de fonte"
      pattern: "recover_campaign_correction_generation"
---

<objective>
Criar os **testes de consumo/serialização/recuperação** (alinhamento §8.3/§8.4 + achados 1/2/3, 14.1-14.9), validando `consume_campaign_correction_opportunity`, `fail_campaign_correction_v2`, `recover_campaign_correction_generation` e `approve_campaign_candidate` por fonte + comportamento.

Purpose: provar a oportunidade única e a serialização aprovar × consumir.
Output: arquivo de teste + SUMMARY.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-CONTEXT.md
@openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-correction-reports/spec.md
@openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-approval-gate/spec.md

<interfaces>
RPCs: `consume_campaign_correction_opportunity(p_campaign_id,p_report_id,p_submission_id)`; `fail_campaign_correction_v2(p_campaign_id,p_report_id)`; `recover_campaign_correction_generation(p_campaign_id,p_stale_before DEFAULT NULL)`; `approve_campaign_candidate(p_campaign_id,p_version_id)`.
Asserts por fonte: `rejection_count = 1`, `correction_in_progress = true`, `status = 'generation_started'`, `generation_started_at = now()`, `asset_status = 'superseded'`, `interval '330 seconds'`, `submission_stale`, `version_mismatch`, `report_campaign_mismatch`, `report_not_open`, `report_not_generation_started`, `correction_in_progress`, `approve_campaign_art_version`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Testes 14.1-14.6 (consumo, fail, sem duplo incremento)</name>
  <files>src/__tests__/api/campaign-correction-consume-recover.test.ts</files>
  <read_first>
    - supabase/migrations/20260906000002_f37_2_correction_flows.sql (consume/complete_v2/fail)
    - src/__tests__/api/campaign-approve-route.test.ts (padrão RPC-por-fonte)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-correction-reports/spec.md
  </read_first>
  <action>
    Criar o arquivo:
    - **14.1** falha pré-provider NÃO chama a RPC de consumo (caso `open`, pode reenviar) — assertar via fonte/hook (fire-once antes do provider).
    - **14.2** consume valida pendência/`rejection_count=0`/relato da campanha/`reported_version_id`=candidata/submissão `eligible` mais recente (`submission_stale`) e grava `rejection_count=1` + `generation_started_at` + `status='generation_started'`.
    - **14.3** `fail_campaign_correction_v2` mantém `rejection_count=1`, libera `correction_in_progress`, grava `failed_no_v2`.
    - **14.4** fail sem consumo (`report_not_generation_started`) → 409 e nada altera.
    - **14.5** conclusão da v2 NÃO incrementa `rejection_count` de novo.
    - **14.6** segunda tentativa após consumo → 409 independentemente da janela.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/api/campaign-correction-consume-recover.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 14.1-14.6 verdes; consumo atômico e sem duplo incremento assertados
  </acceptance_criteria>
  <done>Consumo/falha/sem duplo incremento cobertos</done>
</task>

<task type="auto">
  <name>Task 2: Testes 14.7-14.9 (serialização, RPC F37.1 intacta, recuperação lazy)</name>
  <files>src/__tests__/api/campaign-correction-consume-recover.test.ts</files>
  <read_first>
    - supabase/migrations/20260906000002_f37_2_correction_flows.sql (approve_candidate/recover)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-correction-reports/spec.md (recuperação)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-approval-gate/spec.md (corrida)
  </read_first>
  <action>
    Adicionar:
    - **14.7** serialização: consumo vence → aprovação protegida falha 409 (`correction_in_progress`); aprovação vence → begin/consumo falham na pendência (geração abortada antes do provider).
    - **14.8** `approve_campaign_candidate` chama `approve_campaign_art_version` intacta (assertar ausência de `CREATE OR REPLACE FUNCTION public.approve_campaign_art_version`) e ordem de locks candidata → campanha.
    - **14.9** recuperação: consumo preso além do teto (`generation_started_at < COALESCE(p_stale_before, now() - interval '330 seconds')`) → `recovered:true` mantendo `rejection_count=1`, `failed_no_v2`, liberando `correction_in_progress`; casos recente/`open`/`failed_no_v2`/`v2_generated` → `{recovered:false}`; download/copy continuam 403 até aprovação; no caller, `recovered:true` exige nova leitura.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/api/campaign-correction-consume-recover.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 14.7-14.9 verdes; serialização, RPC F37.1 intacta e recuperação lazy assertadas
  </acceptance_criteria>
  <done>Serialização aprovar×consumir e recuperação preguiçosa cobertas</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teste → migration SQL | Validação por fonte (sem banco) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-2-41 | Tampering | geração paga após aprovação | mitigate | Caso 14.7 asserta serialização por lock |
| T-37-2-42 | Repudiation | oportunidade reutilizada | mitigate | Casos 14.5/14.6 assertam `rejection_count` e recusa pós-consumo |
| T-37-2-43 | Denial of Service | preso em regenerating | mitigate | Caso 14.9 asserta recuperação + reload |
| T-37-2-15-SC | Tampering | dependências npm | accept | Nenhuma dependência nova — vitest já instalado |
</threat_model>

<verification>
- `npx vitest run src/__tests__/api/campaign-correction-consume-recover.test.ts` verde
</verification>

<success_criteria>
- 14.1-14.9 implementados e verdes
- `37-2-15-SUMMARY.md` criado
</success_criteria>

<output>
Create `.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-15-SUMMARY.md` when done
</output>

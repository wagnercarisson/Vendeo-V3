---
phase: 37.2-correcao-unica-por-nao-conformidade
plan: 16
type: execute
wave: 6
depends_on: [03, 05, 08]
files_modified:
  - src/__tests__/lib/campaign/correction-v2-persistence.test.ts
autonomous: true
requirements: [F37.2-16]

must_haves:
  truths:
    - "Testes 15.1-15.8 cobrem sem v3, brief imutável, diff vazio dos .md, revisor/input_validation/eventos, persistência superseded, contrato fechado, custo e não-espelhamento"
    - "RPC complete_campaign_correction_v2 validada por fonte; .md verificados por leitura de disco"
    - "npx vitest run do arquivo passa"
  artifacts:
    - path: "src/__tests__/lib/campaign/correction-v2-persistence.test.ts"
      provides: "Testes 15.1-15.8 de única v2/geração/persistência/contrato/custo"
      contains: "complete_campaign_correction_v2"
  key_links:
    - from: "src/__tests__/lib/campaign/correction-v2-persistence.test.ts"
      to: "prompts/campaign-image-director-offer.md"
      via: "leitura de disco para provar diff vazio (sem bloco injetado nos .md)"
      pattern: "campaign-image-director"
---

<objective>
Criar os **testes de única v2, geração e persistência** (alinhamento §8.5/§8.6/§8.7 + achado 1, 15.1-15.8): sem v3, brief imutável, `.md` intocados, revisor/skip/eventos, `superseded`, contrato fechado da conclusão, custo e aprovação não espelhada.

Purpose: provar que a v2 é única, preserva o briefing e não expõe a v1.
Output: arquivo de teste + SUMMARY.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-CONTEXT.md
@openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-correction-reports/spec.md
@openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-art-versions/spec.md
@openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/ai-image-generation/spec.md

<interfaces>
Asserts por fonte (migration `20260906000002_f37_2_correction_rpcs.sql`): `complete_campaign_correction_v2` sem parâmetro de `brief_snapshot`; `brief_snapshot` copiado da v1; `version_number=2`; `asset_status='superseded'` com `storage_path` preservado; sem `rejection_count` increment; `p_storage_path` distinto.
`.md` intocados: ler `prompts/campaign-image-director*.md` e assertar ausência de bloco de não conformidade (o bloco é montado em runtime no serviço).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Testes 15.1-15.4 (sem v3, brief imutável, .md diff vazio, revisor/skip/eventos)</name>
  <files>src/__tests__/lib/campaign/correction-v2-persistence.test.ts</files>
  <read_first>
    - src/lib/image-generation/services/image-generation-service.ts (bloco único + skip + revisor)
    - prompts/campaign-image-director.md (e os 3 por intent)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/ai-image-generation/spec.md
  </read_first>
  <action>
    Criar o arquivo:
    - **15.1** sem v3: assertar que o fluxo/rota não gera `version_number=3` e que a UI não oferece galeria/retorno à v1.
    - **15.2** brief idêntico: `complete_campaign_correction_v2` não aceita snapshot do cliente e copia da v1 (fonte); v2 sem `candidateArtDataUrl`.
    - **15.3** `.md` atuais com diff vazio: ler os 4 `prompts/campaign-image-director*.md` e assertar que não contêm o preâmbulo/bloco de não conformidade (montado em runtime).
    - **15.4** revisor intocado; `input_validation` `skipped` via `brief_review_confirmed`; eventos no mesmo `operation_run_id`; sem `credit_transactions`/`operation_key`.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/lib/campaign/correction-v2-persistence.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 15.1-15.4 verdes; `.md` sem bloco injetado; brief imutável e skip assertados
  </acceptance_criteria>
  <done>Única v2, brief imutável e `.md` intocados cobertos</done>
</task>

<task type="auto">
  <name>Task 2: Testes 15.5-15.8 (superseded, contrato fechado, custo, não-espelhamento)</name>
  <files>src/__tests__/lib/campaign/correction-v2-persistence.test.ts</files>
  <read_first>
    - supabase/migrations/20260906000002_f37_2_correction_rpcs.sql (complete_v2)
    - src/lib/campaign/correction-reports.ts (getCorrectionReportDetail/derivação de aprovação)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-correction-reports/spec.md
  </read_first>
  <action>
    Adicionar:
    - **15.5** v1 preservada com path após sucesso (`superseded` via troca do CHECK preservando `active|discarded`); v2 candidata única; v1 não aprovável pela API depois da v2.
    - **15.6** contrato da conclusão: `complete_campaign_correction_v2` exige `campaign_id`/`report_id`/`submission_id` + `storage_path`/`mime_type` + `generation_metadata`/`render_snapshot`; **sem parâmetro de snapshot**; valida submissão mais recente/`rejection_count=1`/ausência de v2/path distinto.
    - **15.7** evento `campaign_correction_analysis` no mesmo `operation_run_id` com `provider`/`model`/`usage` e `attempt_number`; falha → `status:"failed"`; CHECK aceita o literal; sem `credit_transactions`/`operation_key`.
    - **15.8** relato sem colunas de aprovação espelhadas (derivação de `campaigns`); decisão corrente = maior `attempt_number`.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/lib/campaign/correction-v2-persistence.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 15.5-15.8 verdes; contrato fechado sem snapshot do cliente; superseded com path; custo/não-espelhamento assertados
  </acceptance_criteria>
  <done>Persistência superseded e contrato fechado da conclusão cobertos</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teste → arquivos/migration | Validação por fonte/leitura de disco |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-2-44 | Tampering | snapshot do cliente | mitigate | Caso 15.6 asserta que a RPC não aceita `brief_snapshot` |
| T-37-2-45 | Tampering | v1 exposta/descartada | mitigate | Caso 15.5 asserta `superseded` com path preservado e não-aprovável |
| T-37-2-46 | Repudiation | custo duplicado | mitigate | Casos 14.5/15.7 assertam sem duplo incremento e evento único |
| T-37-2-16-SC | Tampering | dependências npm | accept | Nenhuma dependência nova — vitest já instalado |
</threat_model>

<verification>
- `npx vitest run src/__tests__/lib/campaign/correction-v2-persistence.test.ts` verde
</verification>

<success_criteria>
- 15.1-15.8 implementados e verdes
- `37-2-16-SUMMARY.md` criado
</success_criteria>

<output>
Create `.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-16-SUMMARY.md` when done
</output>

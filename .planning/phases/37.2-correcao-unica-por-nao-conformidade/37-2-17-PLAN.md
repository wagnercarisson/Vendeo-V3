---
phase: 37.2-correcao-unica-por-nao-conformidade
plan: 17
type: execute
wave: 6
depends_on: [07, 09, 10, 11, 12]
files_modified:
  - src/__tests__/api/campaign-problem-report-route.test.ts
  - src/__tests__/api/campaign-approval-view-problem.test.tsx
  - src/__tests__/api/admin-campaign-reports.test.tsx
autonomous: true
requirements: [F37.2-17]

must_haves:
  truths:
    - "Testes 16.1-16.9 cobrem flag off/legado, gates regenerating, dois botões/modal, regenerating sem actions, pending v2, admin, rota problem-report e o consumo do NDJSON eligible (done/error)"
    - "Gates pós-recuperação mantêm download/copy 403 até aprovação explícita"
    - "npx vitest run dos 3 arquivos passa"
  artifacts:
    - path: "src/__tests__/api/campaign-problem-report-route.test.ts"
      provides: "Testes de guards/status da rota problem-report"
      contains: "problem-report"
    - path: "src/__tests__/api/campaign-approval-view-problem.test.tsx"
      provides: "Testes da revisão com dois botões + modal + regenerating"
      contains: "Informar problema"
    - path: "src/__tests__/api/admin-campaign-reports.test.tsx"
      provides: "Testes da fila admin"
      contains: "campaign-reports"
  key_links:
    - from: "src/__tests__/api/campaign-approval-view-problem.test.tsx"
      to: "src/components/campaign/campaign-problem-modal.tsx"
      via: "render + interação (Cancelar/X/ESC/backdrop)"
      pattern: "campaign-problem-modal"
---

<objective>
Criar os **testes de UI, gates/legado e admin** (alinhamento §8.8, 16.1-16.9): flag off/legado, gates `regenerating`, dois botões/modal, `regenerating` sem actions, `pending` v2, admin, guards da rota `problem-report` e o consumo do NDJSON eligible (done/error).

Purpose: provar os estados UX e os gates preservados.
Output: 3 arquivos de teste + SUMMARY.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-CONTEXT.md
@openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-page-ui/spec.md
@openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-approval-gate/spec.md
@openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/admin-campaign-reports/spec.md

<interfaces>
Padrões: `src/__tests__/api/campaign-approval-view.test.tsx` (render do CampaignApprovalView), `src/__tests__/api/campaign-approve-route.test.ts` (mock de guards), `src/__tests__/api/campaign-download.test.ts` (gates 403).
Gates: `download/route.ts`/`publication-copy/route.ts` → 403 quando `!isDeliveryReleased` (pending/regenerating).
Rota problem-report: guards CSRF/auth/ownership/flag off 403/não-ready 409/consumido 409/4ª tentativa 409.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Testes 16.1-16.2 (flag off/legado + gates regenerating)</name>
  <files>src/__tests__/api/campaign-problem-report-route.test.ts</files>
  <read_first>
    - src/app/api/campaign/download/route.ts + src/app/api/campaign/publication-copy/route.ts (gates)
    - src/__tests__/api/campaign-download.test.ts (padrão de gate 403)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-approval-gate/spec.md
  </read_first>
  <action>
    Criar `src/__tests__/api/campaign-problem-report-route.test.ts` com:
    - **16.1** flag off e legacy intactos (entrega imediata); UI dos novos botões só em `pending`.
    - **16.2** download/copy em `regenerating` → 403 (inalterado).
    - **16.8** após recuperação liberar a v1 (`pending`), download/copy continuam 403 até aprovação explícita (`isDeliveryReleased` false para `pending`).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/api/campaign-problem-report-route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 16.1/16.2/16.8 verdes; gates preservados e 403 pós-recuperação assertado
  </acceptance_criteria>
  <done>Flag off/legado e gates regenerating cobertos</done>
</task>

<task type="auto">
  <name>Task 2: Testes 16.3-16.5 (dois botões, modal, regenerating, pending v2)</name>
  <files>src/__tests__/api/campaign-approval-view-problem.test.tsx</files>
  <read_first>
    - src/components/campaign/campaign-approval-view.tsx + campaign-problem-modal.tsx
    - src/__tests__/api/campaign-approval-view.test.tsx (padrão de render)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-page-ui/spec.md
  </read_first>
  <action>
    Criar `src/__tests__/api/campaign-approval-view-problem.test.tsx`:
    - **16.3** `pending` v1 exibe [Aprovar arte] + [Informar problema]; modal valida vazio/pontuação no clique; Cancelar/X/ESC/backdrop sem efeito.
    - **16.4** `regenerating` exibe progresso da v2 sem download/copy/approve.
    - **16.5** `pending` v2 exibe [Aprovar arte] (aprova a v2) sem [Informar problema]/volta à v1.
    - **16.9 (caminho eligible NDJSON — caminho principal da v2)** mockar `fetch` retornando `Content-Type: application/x-ndjson` com um `ReadableStream` emitindo `{type:"phase",phase:"input_validation",status:"skipped"}` → `{type:"phase",phase:"image_generation"}` → `{type:"done"}`; assertar que o modal **consome o stream** (mostra processamento e chama `router.refresh()` no `done`). E o caso `{type:"error"}` → exibe erro PT-BR sem refresh. Cobrir também a ramificação **JSON** (`blocked`/`unclear` → orientação exibida).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/api/campaign-approval-view-problem.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - 16.3-16.5 verdes; modal sem efeito ao fechar; regenerating sem actions; v2 sem novo relato
    - 16.9 verde: ramo NDJSON (`application/x-ndjson`) consumido até `done`/`error` com `router.refresh()` no `done`; ramo JSON (`blocked`/`unclear`) exibe orientação
  </acceptance_criteria>
  <done>Estados UX de revisão e modal cobertos</done>
</task>

<task type="auto">
  <name>Task 3: Testes 16.6-16.7 (admin + guards da rota problem-report)</name>
  <files>src/__tests__/api/admin-campaign-reports.test.tsx, src/__tests__/api/campaign-problem-report-route.test.ts</files>
  <read_first>
    - src/app/(app)/admin/campaign-reports/page.tsx + [reportId]/page.tsx
    - src/app/api/campaign/[id]/problem-report/route.ts (guards)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/admin-campaign-reports/spec.md
  </read_first>
  <action>
    - **16.6** em `admin-campaign-reports.test.tsx`: listagem com filtros (decisão da tentativa de maior `attempt_number`), detalhe v1×v2 com histórico por `attempt_number`, marcação revisado ortogonal, aprovação por derivação.
    - **16.7** em `campaign-problem-report-route.test.ts`: guards (CSRF, auth, ownership, flag off → 403, não-ready → 409, consumido → 409, 4ª tentativa → 409).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/api/admin-campaign-reports.test.tsx src/__tests__/api/campaign-problem-report-route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 16.6/16.7 verdes; admin e guards da rota assertados
  </acceptance_criteria>
  <done>Admin e guards da rota problem-report cobertos</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teste → UI/rotas | Mocks de fetch/guards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-2-47 | Spoofing | guards da rota | mitigate | Caso 16.7 cobre CSRF/auth/ownership/flag/status/rate limit |
| T-37-2-48 | Information Disclosure | gates de entrega | mitigate | Casos 16.2/16.8 assertam 403 em regenerating/pending |
| T-37-2-49 | Tampering | modal dispara ação ao fechar | mitigate | Caso 16.3 cobre Cancelar/X/ESC/backdrop sem efeito |
| T-37-2-17-SC | Tampering | dependências npm | accept | Nenhuma dependência nova — vitest já instalado |
</threat_model>

<verification>
- `npx vitest run` dos 3 arquivos verde
</verification>

<success_criteria>
- 16.1-16.9 implementados e verdes
- `37-2-17-SUMMARY.md` criado
</success_criteria>

<output>
Create `.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-17-SUMMARY.md` when done
</output>

---
phase: 37.2-correcao-unica-por-nao-conformidade
plan: 18
type: execute
wave: 7
depends_on: [07, 08, 09, 10, 11, 12, 13, 14, 15, 16, 17]
files_modified:
  - src/__tests__/api/campaign-generate.test.ts
  - src/__tests__/api/campaign-approve-route.test.ts
  - src/__tests__/lib/campaign/display-approval.test.ts
  - src/__tests__/api/campaign-download.test.ts
  - src/__tests__/api/publication-copy-route.test.ts
  - src/__tests__/api/campaign-page-server.test.tsx
  - src/__tests__/api/campaign-page.test.tsx
  - src/__tests__/app/campanhas/campaign-detail-page.test.tsx
  - src/lib/ai-cost/__tests__/tracker.test.ts
  - src/__tests__/lib/campaign/correction-reports-persistence.test.ts
autonomous: true
requirements: [F37.2-18]

must_haves:
  truths:
    - "Suites F37.1 e generate-image co-migradas (novo alvo do RPC, superseded, regenerating, gates)"
    - "Novo teste unitário de persistência pai/filha + RPCs por fonte criado"
    - "npx vitest run completo verde (novos + co-migrados)"
  artifacts:
    - path: "src/__tests__/lib/campaign/correction-reports-persistence.test.ts"
      provides: "Testes unitários de persistência pai/filha + RPCs por fonte"
      contains: "campaign_correction_reports"
  key_links:
    - from: "src/__tests__/api/campaign-approve-route.test.ts"
      to: "approve_campaign_candidate"
      via: "assert do novo alvo do RPC"
      pattern: "approve_campaign_candidate"
---

<objective>
Executar a **regressão e co-migração de fixtures** (§17, 17.1-17.6): ajustar as suites F37.1/generate-image ao novo contrato, criar os testes unitários de persistência e garantir a suíte completa verde.

Purpose: eliminar regressões de contrato introduzidas pela F37.2.
Output: suites co-migradas + teste novo + SUMMARY com a saída do `vitest run`.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-CONTEXT.md
@openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/tasks.md
@openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-art-versions/spec.md
@openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/specs/campaign-approval-gate/spec.md

<interfaces>
Suites existentes a co-migrar: `src/__tests__/api/campaign-generate.test.ts`, `campaign-approve-route.test.ts`, `lib/campaign/display-approval.test.ts`, `api/campaign-download.test.ts`, `api/publication-copy-route.test.ts`, `api/campaign-page-server.test.tsx`, `api/campaign-page.test.tsx`, `app/campanhas/campaign-detail-page.test.tsx`, **`src/lib/ai-cost/__tests__/tracker.test.ts`** (hoje em `:90` asserta `GenerationEventType aceita os 12 valores` com `toHaveLength(12)` — a F37.2 torna 13; sem co-migração a suíte fica vermelha).
Novo arquivo: `src/__tests__/lib/campaign/correction-reports-persistence.test.ts` (pai/filha + RPCs por fonte).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Co-migração 17.1-17.3, 17.6 (generate-image, F37.1, art-versions, tracker)</name>
  <files>src/__tests__/api/campaign-generate.test.ts, src/__tests__/api/campaign-approve-route.test.ts, src/__tests__/lib/campaign/display-approval.test.ts, src/__tests__/api/campaign-download.test.ts, src/__tests__/api/publication-copy-route.test.ts, src/__tests__/api/campaign-page-server.test.tsx, src/__tests__/api/campaign-page.test.tsx, src/__tests__/app/campanhas/campaign-detail-page.test.tsx, src/lib/ai-cost/__tests__/tracker.test.ts</files>
  <read_first>
    - Os 9 arquivos de teste listados (estado atual)
    - src/lib/ai-cost/__tests__/tracker.test.ts (linha 90: `it("GenerationEventType aceita os 12 valores da migration (D5)")` + `expect(types).toHaveLength(12)`)
    - src/app/api/campaign/[id]/approve/route.ts (novo alvo do RPC)
    - src/lib/campaign/types.ts (ArtAssetStatus superseded)
    - openspec/changes/fase-37-2-correcao-unica-por-nao-conformidade/tasks.md (§17)
  </read_first>
  <action>
    Ajustar as suites existentes ao contrato F37.2:
    - **17.1** `campaign-generate.test.ts`: fluxo normal inalterado; `generateImage` sem hook = comportamento atual.
    - **17.2** `campaign-approve-route.test.ts` (alvo → `approve_campaign_candidate` + mapeamento `correction_in_progress`); `display-approval.test.ts` (regenerating exercitado); `campaign-download.test.ts`/`publication-copy-route.test.ts` (gates); `campaign-page-server.test.tsx`/`campaign-page.test.tsx`/`campaign-detail-page.test.tsx` (estados pending v1/v2/regenerating).
    - **17.3** fixtures de `campaign-art-versions` (`asset_status` `superseded`).
    - **17.6** `src/lib/ai-cost/__tests__/tracker.test.ts`: acrescentar `'campaign_correction_analysis'` ao array de `GenerationEventType` e atualizar `expect(types).toHaveLength(12)` → `13` (e o título do `it` para "13 valores"); demais testes do union/tracker que listem literais.
    Atualizar apenas o necessário para o novo contrato; não reescrever suites sem motivo.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/api/campaign-generate.test.ts src/__tests__/api/campaign-approve-route.test.ts src/__tests__/lib/campaign/display-approval.test.ts src/__tests__/api/campaign-download.test.ts src/__tests__/api/publication-copy-route.test.ts src/__tests__/api/campaign-page-server.test.tsx src/__tests__/api/campaign-page.test.tsx src/__tests__/app/campanhas/campaign-detail-page.test.tsx src/lib/ai-cost/__tests__/tracker.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Suites co-migradas verdes; approve usa `approve_campaign_candidate`; superseded/regenerating cobertos
    - `tracker.test.ts` co-migrado: `GenerationEventType` com 13 valores (`campaign_correction_analysis` incluído)
  </acceptance_criteria>
  <done>Fixtures F37.1/generate-image alinhadas ao contrato F37.2</done>
</task>

<task type="auto">
  <name>Task 2: Novo teste unitário de persistência (pai/filha + RPCs por fonte)</name>
  <files>src/__tests__/lib/campaign/correction-reports-persistence.test.ts</files>
  <read_first>
    - src/lib/campaign/correction-reports.ts (funções + RPCs)
    - supabase/migrations/20260906000002_f37_2_correction_flows.sql (fontes)
    - src/__tests__/api/campaign-approve-route.test.ts (padrão RPC-por-fonte)
  </read_first>
  <action>
    Criar `src/__tests__/lib/campaign/correction-reports-persistence.test.ts` cobrindo: pai/filha (sem `campaign_id` na filha), `getCorrectionReport`/`listCorrectionSubmissions` (ordem `attempt_number`), `completeCorrectionAnalysis` via RPC, `markReportReviewedBySupport`; e asserts por fonte das RPCs novas (`begin` locks candidata→campanha→relato, `complete_campaign_correction_analysis` validação semântica, `fail_campaign_correction_v2`, `recover_campaign_correction_generation` com `p_stale_before`/default 330s, `consume` com submissão mais recente).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/lib/campaign/correction-reports-persistence.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Teste novo verde; invariantes da filha + RPCs por fonte assertados
  </acceptance_criteria>
  <done>Persistência pai/filha e RPCs cobertas por testes unitários</done>
</task>

<task type="auto">
  <name>Task 3: Regressão completa (17.5)</name>
  <files>src/__tests__/lib/campaign/correction-reports-persistence.test.ts</files>
  <read_first>
    - .planning/STATE.md (baseline de testes anterior)
  </read_first>
  <action>
    Rodar a suíte completa `npx vitest run` e corrigir qualquer falha residual (novos + co-migrados). Registrar no SUMMARY o total de arquivos/testes e a contagem de falhas = 0.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` com zero falhas; SUMMARY registra total de arquivos/testes
  </acceptance_criteria>
  <done>Suíte completa verde após a F37.2</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teste → código | Mocks/fixtures; sem efeitos colaterais |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-2-50 | Tampering | fixtures desatualizadas mascarando regressão | mitigate | Co-migração explícita + suíte completa verde |
| T-37-2-51 | Repudiation | contrato do RPC não verificado | mitigate | Teste por fonte das RPCs novas |
| T-37-2-18-SC | Tampering | dependências npm | accept | Nenhuma dependência nova — vitest já instalado |
</threat_model>

<verification>
- `npx vitest run` completo verde (zero falhas)
- `npm run typecheck` sem erros
</verification>

<success_criteria>
- Suites co-migradas + teste de persistência novo + regressão completa verde
- `37-2-18-SUMMARY.md` criado com a contagem final
</success_criteria>

<output>
Create `.planning/phases/37.2-correcao-unica-por-nao-conformidade/37-2-18-SUMMARY.md` when done
</output>

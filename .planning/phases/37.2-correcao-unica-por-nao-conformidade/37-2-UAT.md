# Phase 37.2: Correção Única por Não Conformidade — UAT Local

**Contexto:** UAT local pós-implementação da fatia 37.2 (revisão com dois caminhos [Aprovar arte]/[Informar problema], modal de relato de 1 etapa, análise textual de elegibilidade, geração de no máximo uma v2 sem novo crédito, aprovação protegida, recuperação lazy e fila admin). As migrations `20260906000001`/`20260906000002`/`20260906000003` estão aplicadas no remoto.
**Pré-requisito:** rodar o app local (`npm run dev`) com uma loja de teste (dados + direção visual + legal + saldo) e a flag `campaign_approval_enabled` controlada pelo admin ("Controles operacionais").
**Obrigatório:** cenário 37.2-9 (mobile real/estreito 320px/375px).

---

## Checklist

### Cenário 37.2-1 — Flag ligada → revisão `pending` (v1) com dois botões; aprovar libera a entrega

- [x] Gerar campanha nova com a flag ligada → `/campanhas/[id]` exibe a revisão da candidata v1 com **[Aprovar arte]** e **[Informar problema]**.
- [x] Clicar **[Aprovar arte]** → a página passa a exibir a entrega (arte + copys + download).
- Resultado: [PASS]

### Cenário 37.2-2 — Modal: fechamentos sem efeito + validação no clique

- [x] [Informar problema] abre o modal (preview da candidata, orientação corrigível × não, campo obrigatório).
- [x] [Cancelar], X, ESC e clique no backdrop fecham **sem** criar caso/aprovar/enviar.
- [x] Texto vazio ou só pontuação → erro amigável **sem** chamar IA (sem caso).
- Resultado: [PASS]

### Cenário 37.2-3 — Relato elegível → v2 gerada e vira candidata; aprovar a v2 libera a entrega

- [x] Relato claro de defeito objetivo (ex.: "o preço saiu cortado na borda") → stream de geração (`input_validation` skipped → `image_generation`) → v2 exibida como candidata.
- [x] A v1 não é oferecida (sem galeria/volta); [Informar problema] não aparece na v2.
- [x] Aprovar a v2 → entrega liberada.
- Resultado: [PASS]

### Cenário 37.2-4 — Blocked/unclear + rate limit

- [x] Relato de preferência ("muda o preço", "não gostei, faz outra") → `blocked` com orientação; nada gerado/consumido.
- [x] Relato vago → `unclear` com orientação/exemplo; reformulação permitida.
- [x] Após 3 tentativas em 30 min → 4ª bloqueada (`rate_limit_exceeded`), inclusive após `analysis_failed`.
- Resultado: [PASS]

### Cenário 37.2-5 — `regenerating` (progresso da v2 sem ações de entrega)

- [x] Durante a geração corretiva, a página exibe "Corrigindo a arte..." **sem** download/copy/[Aprovar arte]/[Informar problema] e **sem** cair no `ReadyView`.
- [x] Ao concluir, a página deriva `pending` (v2) com [Aprovar arte].
- Resultado: [PASS]

### Cenário 37.2-6 — Corrida aprovar × consumir

- [x] Aprovar durante o processamento (`correction_in_progress=true`) → 409; nenhuma geração paga após a aprovação.
- [x] Se a aprovação vence, o consumo falha na pendência (geração abortada antes do provider).
- Resultado: [PASS] — **validado por código** (UAT manual da corrida inviável de reproduzir de forma determinística). Evidência: `approve_campaign_candidate` trava a candidata (`FROM public.campaign_art_versions ... FOR UPDATE`) e, com `correction_in_progress=true`, `RAISE EXCEPTION 'correction_in_progress'` → rota approve 409 (`src/__tests__/api/campaign-approve-route.test.ts`, caso "correction_in_progress → 409"); `consume_campaign_correction_opportunity` valida `approved_version_id IS NOT NULL`/`rejection_count=0` → `campaign_not_pending` **antes** da 1ª chamada ao provider (hook `onBeforeImageProviderCall`), e o orquestrador retorna `preProvider:true` sem chamar o provider (`src/__tests__/api/campaign-correction-consume-recover.test.ts` 14.7 + `campaign-correction-begin-rpc.test.ts`). Locks candidata → campanha em ambos os lados; 32/32 testes verdes.

### Cenário 37.2-7 — Flag desligada e campanha legada intactas

- [x] Desligar a flag → gerar campanha nova → entrega imediata (sem revisão).
- [x] Abrir campanha antiga (pré-flag, sem versões) → entregue como hoje, sem gate.
- Resultado: [PASS]

### Cenário 37.2-8 — Fila admin

- [x] `/admin/campaign-reports` lista os casos com decisão corrente (maior `attempt_number`), filtros (status/análise/revisado) e paginação.
- [x] Detalhe mostra v1 × v2 lado a lado, histórico de tentativas por `attempt_number`, `operation_run_id` linkável e aprovação derivada.
- [x] "Marcar como revisado" preenche `reviewed_by_support_at/_user` sem alterar o fluxo.
- Resultado: [PASS]

### Cenário 37.2-9 — Mobile 320px/375px (OBRIGATÓRIO)

- [x] Revisão e modal em 320/375px → sem scroll horizontal; imagem `object-contain`; touch targets ≥ 44px; tema dark.
- Resultado: [PASS]

---

## Summary

| Cenário | Resultado |
|---------|-----------|
| 37.2-1 Flag ligada → revisão/aprovar | PASS |
| 37.2-2 Modal (fechamentos/validação) | PASS |
| 37.2-3 Elegível → v2 | PASS |
| 37.2-4 Blocked/unclear + rate limit | PASS |
| 37.2-5 Regenerating | PASS |
| 37.2-6 Corrida aprovar×consumir | PASS (validado por código) |
| 37.2-7 Flag off/legado | PASS |
| 37.2-8 Admin | PASS |
| 37.2-9 Mobile 320/375px | PASS |

**Total:** 9 | **Passed:** 9 | **Pending:** 0

---

## Gaps

_(preenchido após o checkpoint humano — nenhum até o momento)_

---
phase: 38-1-ai-cost-accounting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "supabase/migrations/20260808000001_f38_1_create_ai_cost_accounting.sql"
autonomous: false
HAS_UI: 0
requirements:
  - F38.1-07
  - F38.1-08
  - F38.1-09
  - F38.1-12
  - F38.1-13
  - F38.1-14
  - F38.1-15
  - F38.1-16
  - F38.1-17
  - F38.1-19
user_setup:
  - service: Supabase
    why: "supabase db push (Task 3) requires an authenticated CLI session or access token"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard → Account → Access Tokens (criar token) — ou sessão `supabase login` já ativa"
must_haves:
  truths:
    - "generation_events evoluiu com as 9 colunas novas D2 (operation_run_id, operation_run_type, visual_signature_id, theme_id, cached_input_tokens, image_tokens, provider_reported_cost_usd, cost_source, pricing_version), CHECK chk_generation_events_cost_source (5 valores), CHECK chk_generation_events_type expandido (12 valores) e 5 índices novos"
    - "campaigns ganhou operation_run_id UUID + índice idx_campaigns_operation_run_id (preparo reuso F37 — D1/D2)"
    - "ai_model_pricing existe com CHECK chk_ai_model_pricing_at_least_one_price, UNIQUE(provider, model), RLS service_role-only (sem GRANT authenticated), trigger scoped de updated_at"
    - "7 seeds vigentes (gpt-4o, gpt-4o-mini, gpt-5.5, gpt-image-2, dall-e-3, gemini-2.0-flash, gemini-3.1-flash-lite) com source_url/effective_from preenchidos e effective_until NULL (furo 3 sanado)"
    - "RPC admin_set_ai_model_price aplicada no remoto (SECURITY DEFINER, SET search_path='', p_reason antes dos DEFAULTs, transacional fecha vigente + abre nova)"
    - "6 views admin_ai_* + admin_cost_vs_credits somam APENAS call-level com valor contábil COALESCE(provider_reported_cost_usd, estimated_cost_usd)"
    - "RPC admin_get_ai_costs aplicada no remoto (SECURITY DEFINER, filtros p_*)"
    - "admin_get_metrics (F28) e reserve_credit/credit_transactions (F24) NÃO foram tocados"
  artifacts:
    - path: "supabase/migrations/20260808000001_f38_1_create_ai_cost_accounting.sql"
      provides: "Migration única: ALTER generation_events (D2/D4/D5) + campaigns.operation_run_id (D1/D2) + ai_model_pricing + seeds + RPC admin_set_ai_model_price (D8) + 6 views + RPC admin_get_ai_costs (D10) + REVERT"
      contains: "admin_set_ai_model_price"
  key_links:
    - from: "generation_events.operation_run_id"
      to: "campaigns.operation_run_id"
      via: "agrupamento econômico por entrega (D1); run persistido na criação da campanha"
      pattern: "operation_run_id"
    - from: "ai_model_pricing (UNIQUE provider,model)"
      to: "seeds ON CONFLICT (provider, model) DO NOTHING"
      via: "idempotência do bootstrap (UUID-PK → UNIQUE constraint — no-analog #5)"
      pattern: "UNIQUE \\(provider, model\\)"
    - from: "views admin_ai_*"
      to: "generation_events (call-level apenas)"
      via: "COALESCE(provider_reported_cost_usd, estimated_cost_usd); exclusão de delivery markers"
      pattern: "COALESCE"
---

<objective>
Criar e aplicar a migration única da F38.1 — `20260808000001_f38_1_create_ai_cost_accounting.sql` — que evolui o schema para a trilha granular de custo de IA: ALTER de `generation_events` (9 colunas novas D2 + CHECK `cost_source` D4 + CHECK `generation_type` expandido D5 + 5 índices), `campaigns.operation_run_id` + índice (D1/D2, preparo reuso F37), tabela `ai_model_pricing` + seeds + RPC `admin_set_ai_model_price` (D8), e as 6 views de apuração/reconciliação + RPC `admin_get_ai_costs` (D10). Aplicar a migration no banco remoto via `supabase db push` ([BLOCKING] — a fase não passa na verificação sem isso).

Purpose: sem o schema evoluído nada downstream (tracker, views, admin APIs) existe. Esta é a fundação de dados de toda a fase — também a base das gates de segurança (RLS default-deny, RPCs SECURITY DEFINER com `SET search_path=''`).
Output: migration SQL aplicada no remoto + RPCs validadas em pg_proc.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-1-ai-cost-accounting/38-1-CONTEXT.md
@.planning/phases/38-1-ai-cost-accounting/38-1-PATTERNS.md

<interfaces>
Contratos que o executor precisa (extraídos de 38-1-CONTEXT.md D2/D4/D5/D8/D10 + 38-1-PATTERNS.md):

**Migration: sem UI (HAS_UI: 0). Nenhuma página/rota de UI é alterada por este plano.**

ALTER `generation_events` (D2 — todas com `IF NOT EXISTS`):
```
operation_run_id UUID;  operation_run_type TEXT;  visual_signature_id UUID REFERENCES public.store_visual_signatures(id);
theme_id UUID;  cached_input_tokens INTEGER;  image_tokens INTEGER;  provider_reported_cost_usd REAL;
cost_source TEXT;  pricing_version TEXT
```
- CHECK novo `chk_generation_events_cost_source`: `cost_source IN ('provider_reported','pricing_table','fallback_static','manual_unknown','not_available')` (D4)
- CHECK substituído `chk_generation_events_type` (DROP IF EXISTS + ADD): 12 valores — `campaign_pipeline, campaign_copy, campaign_input_validation, campaign_image, campaign_image_review, visual_signature, visual_signature_image, visual_signature_validation, brand_profile_without_logo, brand_profile_with_logo, brand_profile_vision, brand_profile_text` (D5)
- Índices: `idx_generation_events_operation_run_id`, `idx_generation_events_visual_signature_id`, `idx_generation_events_operation_run_type`, `idx_generation_events_cost_source`, `idx_generation_events_provider_model` (D2)

ALTER `campaigns` (D1/D2): `operation_run_id UUID` + `CREATE INDEX IF NOT EXISTS idx_campaigns_operation_run_id ON public.campaigns(operation_run_id)`. Não é FK.

Tabela `ai_model_pricing` (D8 — schema exato do spec ai-model-pricing):
```
id UUID PK DEFAULT gen_random_uuid(); provider TEXT NOT NULL; model TEXT NOT NULL;
input_token_usd_per_1m NUMERIC; output_token_usd_per_1m NUMERIC; cached_input_token_usd_per_1m NUMERIC;
image_unit_usd NUMERIC; image_token_usd_per_1m NUMERIC; effective_from TIMESTAMPTZ NOT NULL;
effective_until TIMESTAMPTZ; source_url TEXT; source_note TEXT; updated_by UUID REFERENCES auth.users(id);
updated_at/created_at TIMESTAMPTZ NOT NULL DEFAULT now();
CONSTRAINT chk_ai_model_pricing_at_least_one_price CHECK (5 dimensões, ao menos 1 NOT NULL)
```
- **DIVERGÊNCIA FLAG (no-analog #5):** PK é UUID → adicionar `UNIQUE (provider, model)` para `ON CONFLICT (provider, model) DO NOTHING` nos seeds (F38 usava PK natural — não compila aqui sem a constraint).
- Trigger scoped `update_ai_model_pricing_updated_at()` (padrão F38, função específica NÃO genérica) + `CREATE TRIGGER trg_ai_model_pricing_updated_at`.
- RLS: `ENABLE ROW LEVEL SECURITY` + `REVOKE ALL ON TABLE ... FROM anon, authenticated` + `GRANT SELECT, INSERT, UPDATE, DELETE ... TO service_role` (D8: `authenticated` NÃO lê preços).

Seeds (D8 — 7 linhas, ON CONFLICT (provider, model) DO NOTHING, updated_by NULL, effective_until NULL, source_url + source_note + effective_from '2026-08-08T00:00:00Z'):
```
openai gpt-4o {2.50, 10.00}; openai gpt-4o-mini {0.15, 0.60}; openai gpt-5.5 {5.00, 30.00, cached 0.50};
openai gpt-image-2 {image_unit 0.040}; openai dall-e-3 {image_unit 0.040};
gemini gemini-2.0-flash {0.10, 0.40}; gemini gemini-3.1-flash-lite {0.10, 0.40}   ← NOVO (furo 3)
```

RPC `admin_set_ai_model_price` (D8 — assinatura EXATA, `p_reason` ANTES dos DEFAULTs):
```
(p_actor_id UUID, p_provider TEXT, p_model TEXT, p_input NUMERIC, p_output NUMERIC, p_reason TEXT,
 p_cached NUMERIC DEFAULT NULL, p_image_unit NUMERIC DEFAULT NULL, p_image_token NUMERIC DEFAULT NULL,
 p_source_url TEXT DEFAULT NULL, p_source_note TEXT DEFAULT NULL) RETURNS JSONB {id, provider, model, effective_from, previous_id}
```
- `LANGUAGE plpgsql SECURITY DEFINER SET search_path = ''`; p_reason obrigatório (RAISE EXCEPTION se NULL/vazio); transacional: `UPDATE ... SET effective_until = now() WHERE provider AND model AND effective_until IS NULL` + `INSERT` (effective_from = now()) na mesma função; retorna `previous_id` (id da linha fechada).
- Grants: `REVOKE EXECUTE ... FROM PUBLIC, anon, authenticated; GRANT EXECUTE ... TO service_role;`

Views (D10 — NO ANALOG #1, primeiro CREATE VIEW do repo; modelar SELECT no estilo CTE/COUNT FILTER/jsonb_build_object do `admin_get_metrics` — `20260731000001_admin_test_store_filter.sql:138-353`):
- Valor contábil por evento: `COALESCE(provider_reported_cost_usd, estimated_cost_usd)` (D3)
- Todas filtram **apenas call-level** (excluem delivery: campaign_pipeline, visual_signature, brand_profile_without_logo, brand_profile_with_logo)
- `admin_ai_operation_costs` (por operation_run_id): custo total, duração total, nº chamadas, nº tentativas, status entrega
- `admin_campaign_delivery_costs` (por campaign_id): custo por generation_type + tentativas
- `admin_ai_cost_by_provider_model` (provider+model) e `admin_ai_cost_by_stage` (generation_type)
- `admin_ai_cost_by_store` (store_id)
- `admin_cost_vs_credits` (reconciliação): campanha via `credit_transactions` (type='deduction', campaign_id, metadata->>'feature'='campaign_pipeline'); VS via `store_visual_signatures.metadata->>'credit_tx_id'` = `credit_transactions.id`; saída operation_run_id, domain, custo_usd_total, creditos_debitados, margem_estimada, etapas_mais_caras (top generation_type), regeneracoes

RPC `admin_get_ai_costs` (D10 — padrão admin_get_metrics):
```
(p_operation_run_id UUID DEFAULT NULL, p_campaign_id UUID DEFAULT NULL, p_store_id UUID DEFAULT NULL,
 p_user_id UUID DEFAULT NULL, p_provider TEXT DEFAULT NULL, p_model TEXT DEFAULT NULL,
 p_generation_type TEXT DEFAULT NULL, p_hours INTEGER DEFAULT 24) RETURNS JSONB
```
- SECURITY DEFINER + SET search_path='' + CTE `filtered_ge` + `COUNT(*) FILTER (WHERE status='success')` + agrupamentos por operation_run/store/provider_model/generation_type + reconciliação (custo_usd_total, creditos_debitados, margem_estimada, regeneracoes)

REVERT (obrigatório, 1.10): comentário em ordem reversa — DROP FUNCTION admin_get_ai_costs, admin_set_ai_model_price; DROP VIEW ×6; DROP TRIGGER; DROP FUNCTION update_ai_model_pricing_updated_at; DROP TABLE ai_model_pricing; DROP INDEX idx_campaigns_operation_run_id + 5 idx_generation_events_*; ALTER generation_events DROP COLUMN ×9; DROP CONSTRAINT chk_generation_events_cost_source; restaurar chk_generation_events_type antigo.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration — Blocos schema (generation_events + campaigns + ai_model_pricing + seeds + RPC pricing)</name>
  <files>supabase/migrations/20260808000001_f38_1_create_ai_cost_accounting.sql</files>
  <read_first>
    - supabase/migrations/20260807000001_f38_create_credit_operation_costs.sql (cabeçalho banner + trigger scoped + RLS service_role + RPC JSONB + seeds + REVERT — linhas 1-21, 26-94, 102-232, 237-256)
    - supabase/migrations/20260718000002_expand_generation_events.sql (ALTER generation_events + substituição de CHECK — linhas 6-42)
    - .planning/phases/38-1-ai-cost-accounting/38-1-PATTERNS.md (seção migration — snippets linhas 46-141)
  </read_first>
  <action>
    Criar o arquivo `supabase/migrations/20260808000001_f38_1_create_ai_cost_accounting.sql` com:

    1. **Cabeçalho** no estilo dos banners `-- ====` das migrations F36/F38 (comentário PT-BR): propósito F38.1, "Regras:" (anti-dupla-contagem D1/D6, valor contábil COALESCE D3, RLS service_role, RPCs SECURITY DEFINER) e "Blocos:" numerados 1..N mapeando para as seções abaixo.

    2. **Bloco 1 — ALTER generation_events** (tarefa 1.1): uma linha `ALTER TABLE public.generation_events ADD COLUMN IF NOT EXISTS <coluna> <tipo>;` por coluna das 9 novas (D2). `visual_signature_id` com `REFERENCES public.store_visual_signatures(id)`.

    3. **Bloco 2 — CHECKs** (tarefa 1.2): `ADD CONSTRAINT chk_generation_events_cost_source CHECK (cost_source IN ('provider_reported','pricing_table','fallback_static','manual_unknown','not_available'))`; depois `DROP CONSTRAINT IF EXISTS chk_generation_events_type;` + `ADD CONSTRAINT chk_generation_events_type CHECK (generation_type IN (12 valores D5))` — nessa ordem, copiando o padrão de `20260718000002_expand_generation_events.sql`.

    4. **Bloco 3 — Índices generation_events** (tarefa 1.3): 5 × `CREATE INDEX IF NOT EXISTS idx_generation_events_<campo> ON public.generation_events(<campo>);` — operation_run_id, visual_signature_id, operation_run_type, cost_source, (provider, model).

    5. **Bloco 4 — ALTER campaigns** (tarefa 1.1, preparo reuso F37): `ALTER TABLE public.campaigns ADD COLUMN IF NOT EXISTS operation_run_id UUID;` + `CREATE INDEX IF NOT EXISTS idx_campaigns_operation_run_id ON public.campaigns(operation_run_id);` (D1/D2).

    6. **Bloco 5 — ai_model_pricing** (tarefa 1.4): `CREATE TABLE IF NOT EXISTS public.ai_model_pricing` com schema EXATO do contrato D8 — id UUID PK gen_random_uuid(), colunas NUMERIC nullable, `effective_from TIMESTAMPTZ NOT NULL`, `effective_until TIMESTAMPTZ`, updated_by UUID REFERENCES auth.users(id), CHECK `chk_ai_model_pricing_at_least_one_price` (5 dimensões, OR de NOT NULL). **ADICIONAR `UNIQUE (provider, model)`** (no-analog #5 — PK UUID exige UNIQUE para o ON CONFLICT dos seeds). Função trigger scoped `update_ai_model_pricing_updated_at()` (seta NEW.updated_at = now()) + `CREATE TRIGGER trg_ai_model_pricing_updated_at BEFORE UPDATE ... FOR EACH ROW`. RLS: `ALTER TABLE ... ENABLE ROW LEVEL SECURITY;` + `REVOKE ALL ON TABLE public.ai_model_pricing FROM anon, authenticated;` + `GRANT SELECT, INSERT, UPDATE, DELETE ON TABLE public.ai_model_pricing TO service_role;` (D8 — authenticated NÃO lê).

    7. **Bloco 6 — Seeds** (tarefa 1.5): 7 linhas `INSERT INTO public.ai_model_pricing (provider, model, <dimensões>, effective_from, source_url, source_note) VALUES (...)` com `ON CONFLICT (provider, model) DO NOTHING` — gpt-4o {2.50,10.00}, gpt-4o-mini {0.15,0.60}, gpt-5.5 {5.00,30.00,cached 0.50}, gpt-image-2 {image_unit 0.040}, dall-e-3 {image_unit 0.040}, gemini-2.0-flash {0.10,0.40}, gemini-3.1-flash-lite {0.10,0.40}; todas com `effective_from '2026-08-08T00:00:00Z'`, `source_url` (fonte pública consultada), `source_note 'bootstrap F38.1'`, updated_by NULL, effective_until NULL.

    8. **Bloco 7 — RPC admin_set_ai_model_price** (tarefa 1.6): `CREATE OR REPLACE FUNCTION public.admin_set_ai_model_price(...)` com assinatura EXATA de 11 parâmetros do contrato D8 — **p_reason TEXT ANTES dos parâmetros com DEFAULT** (regra de assinatura do Postgres). `RETURNS JSONB`, `LANGUAGE plpgsql`, `SECURITY DEFINER`, `SET search_path = ''`. Corpo: valida p_provider/p_model/p_reason (NULL/vazio → RAISE EXCEPTION 'ai_model_price_reason_required'); valida ao menos uma dimensão de preço (espelha CHECK → RAISE EXCEPTION 'ai_model_price_no_dimension'); transacional: SELECT id INTO v_prev_id da linha vigente (provider, model, effective_until IS NULL) e `UPDATE ... SET effective_until = now()`; INSERT nova linha (effective_from = now(), updated_by = p_actor_id, p_source_url/p_source_note); RETURN jsonb_build_object('id', v_new_id, 'provider', p_provider, 'model', p_model, 'effective_from', now(), 'previous_id', v_prev_id). Grants: `REVOKE EXECUTE ON FUNCTION public.admin_set_ai_model_price(...) FROM PUBLIC, anon, authenticated;` + `GRANT EXECUTE ON FUNCTION public.admin_set_ai_model_price(...) TO service_role;`.

    NÃO alterar `reserve_credit`/`credit_transactions` (F24) nem `admin_get_metrics` (F28) — nenhuma referência a essas funções nesta migration. NÃO adicionar CHECK enum para operation_run_type (enums versionados no TS, D2).
  </action>
  <verify>
    <automated>grep -c "admin_set_ai_model_price" supabase/migrations/20260808000001_f38_1_create_ai_cost_accounting.sql` >= 4 (CREATE + corpo + REVOKE + GRANT); `grep -c "SECURITY DEFINER"` == 1; `grep -c "SET search_path = ''''"` == 1; `grep -c "ENABLE ROW LEVEL SECURITY"` == 1; `grep -c "UNIQUE (provider, model)"` == 1; `grep -c "ON CONFLICT (provider, model) DO NOTHING"` == 1; `grep -c "ADD COLUMN IF NOT EXISTS"` == 10 (9 generation_events + 1 campaigns); `grep -c "chk_generation_events_cost_source"` == 1; `grep -c "chk_generation_events_type"` >= 2 (DROP + ADD); `grep -c "campaign_input_validation"` == 1; `grep -c "gemini-3.1-flash-lite"` == 1</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `supabase/migrations/20260808000001_f38_1_create_ai_cost_accounting.sql` existe com banner de cabeçalho no estilo das migrations do repo
    - 9 colunas novas em generation_events (IF NOT EXISTS) + CHECK cost_source 5 valores + CHECK generation_type 12 valores (DROP antigo + ADD novo)
    - 5 índices generation_events + campaigns.operation_run_id + idx_campaigns_operation_run_id
    - ai_model_pricing com CHECK at_least_one_price + UNIQUE(provider, model) + trigger scoped + RLS service_role-only
    - 7 seeds com ON CONFLICT (provider, model) DO NOTHING, source_url/effective_from preenchidos, effective_until NULL
    - RPC admin_set_ai_model_price SECURITY DEFINER + SET search_path='' + p_reason antes dos DEFAULTs + transacional
    - Nenhuma referência a reserve_credit/credit_transactions/admin_get_metrics modificada
  </acceptance_criteria>
  <done>Migration criada com blocos schema completos (ALTERs + tabela + seeds + RPC pricing) — pronta para views</done>
</task>

<task type="auto">
  <name>Task 2: Migration — Views admin_ai_* + admin_cost_vs_credits + RPC admin_get_ai_costs + REVERT</name>
  <files>supabase/migrations/20260808000001_f38_1_create_ai_cost_accounting.sql</files>
  <read_first>
    - supabase/migrations/20260731000001_admin_test_store_filter.sql (padrão de agregação JSONB do admin_get_metrics — linhas 138-353, filtros metadata->>'feature' 252/300/309)
    - .planning/phases/38-1-ai-cost-accounting/38-1-PATTERNS.md (seção Views/NO ANALOG #1 — linhas 139, 517)
  </read_first>
  <action>
    Anexar ao arquivo criado na Task 1 (mesma migration, appender após o Bloco 7):

    1. **Bloco 8 — Views de apuração** (tarefas 1.7): criar com `CREATE OR REPLACE VIEW public.<nome> AS SELECT ...` (NO ANALOG #1 — primeiro CREATE VIEW do repo; modelar o SELECT no estilo CTE/COUNT FILTER/agregação do `admin_get_metrics`). Todas:
       - Valor contábil por evento: `COALESCE(ge.provider_reported_cost_usd, ge.estimated_cost_usd)` (D3)
       - Filtro anti-dupla-contagem: `WHERE ge.generation_type NOT IN ('campaign_pipeline','visual_signature','brand_profile_without_logo','brand_profile_with_logo')` (D1/D6/D10) — ou equivalente explícito de exclusão dos delivery markers
       - `admin_ai_operation_costs`: GROUP BY ge.operation_run_id, ge.operation_run_type, ge.store_id, ge.campaign_id, ge.visual_signature_id — SUM custo, SUM duration_ms, COUNT(*) chamadas, MAX(attempt_number) tentativas, status da entrega (do delivery marker do run, via JOIN lateral ou subquery filtrando tipo delivery)
       - `admin_campaign_delivery_costs`: GROUP BY ge.campaign_id, ge.generation_type — custo por etapa + nº tentativas
       - `admin_ai_cost_by_provider_model`: GROUP BY ge.provider, ge.model — custo total, COUNT chamadas, AVG duration_ms
       - `admin_ai_cost_by_stage`: GROUP BY ge.generation_type — custo total por etapa
       - `admin_ai_cost_by_store`: GROUP BY ge.store_id — custo total por loja

    2. **Bloco 9 — View de reconciliação** (tarefa 1.8): `CREATE OR REPLACE VIEW public.admin_cost_vs_credits AS` — por campanha: eventos call-level agrupados por operation_run/campaign_id JOIN `credit_transactions` (`type = 'deduction'`, `campaign_id`, `metadata->>'feature' = 'campaign_pipeline'`); por VS: JOIN `store_visual_signatures` via `ge.visual_signature_id` e `store_visual_signatures.metadata->>'credit_tx_id'` = `credit_transactions.id`. Saída por entrega: operation_run_id, domain ('campaign'|'visual_signature'), custo_usd_total (SUM COALESCE call-level), creditos_debitados, margem_estimada (diferença entre créditos e custo — formato definido: margem percentual ou absoluta documentada em comentário SQL), etapas_mais_caras (top generation_type — array_agg/row_number), regeneracoes (MAX attempt_number - 1 ou COUNT de recomposições).

    3. **Bloco 10 — RPC admin_get_ai_costs** (tarefa 1.9): `CREATE OR REPLACE FUNCTION public.admin_get_ai_costs(...)` com 8 parâmetros do contrato D10 — `p_operation_run_id UUID DEFAULT NULL, p_campaign_id UUID DEFAULT NULL, p_store_id UUID DEFAULT NULL, p_user_id UUID DEFAULT NULL, p_provider TEXT DEFAULT NULL, p_model TEXT DEFAULT NULL, p_generation_type TEXT DEFAULT NULL, p_hours INTEGER DEFAULT 24`. `RETURNS JSONB`, `LANGUAGE plpgsql`, `SECURITY DEFINER`, `SET search_path = ''`. Corpo: `v_cutoff := NOW() - (p_hours || ' hours')::INTERVAL` (p_hours ≥ 1); CTE `filtered_ge AS (SELECT * FROM public.generation_events WHERE created_at >= v_cutoff AND (filtros p_* opcionais via COALESCE/NULLIF) AND generation_type NOT IN (delivery markers))`; montar jsonb_build_object com agrupamentos: por operation_run (custo_usd_total via COALESCE, chamadas, duração), por store, por provider/model, por generation_type, e reconciliação (creditos_debitados/margem_estimada/regeneracoes via LEFT JOIN credit_transactions nos moldes da view admin_cost_vs_credits). Grants: REVOKE EXECUTE FROM PUBLIC, anon, authenticated + GRANT EXECUTE TO service_role.

    4. **Bloco 11 — REVERT** (tarefa 1.10): seção `-- REVERT` comentada, ordem reversa, um DROP por objeto: `DROP FUNCTION IF EXISTS public.admin_get_ai_costs(...)` + `DROP FUNCTION IF EXISTS public.admin_set_ai_model_price(...)` + `DROP VIEW IF EXISTS` ×6 + `DROP TRIGGER IF EXISTS trg_ai_model_pricing_updated_at` + `DROP FUNCTION IF EXISTS public.update_ai_model_pricing_updated_at` + `DROP TABLE IF EXISTS public.ai_model_pricing` + `DROP INDEX IF EXISTS idx_campaigns_operation_run_id` + 5 × idx_generation_events_* + `ALTER TABLE public.generation_events DROP COLUMN IF EXISTS` ×9 + `ALTER TABLE public.generation_events DROP CONSTRAINT IF EXISTS chk_generation_events_cost_source` + instrução para restaurar o CHECK chk_generation_events_type antigo.

    NÃO alterar as views/RPCs existentes (admin_get_metrics, admin_get_metrics_uuid_text etc.). NÃO dar GRANT direto das views ao cliente — acesso apenas via RPC SECURITY DEFINER (D10).
  </action>
  <verify>
    <automated>`grep -c "CREATE OR REPLACE VIEW" supabase/migrations/20260808000001_f38_1_create_ai_cost_accounting.sql` == 6; `grep -c "admin_get_ai_costs"` >= 4 (CREATE + corpo + REVOKE + GRANT); `grep -c "provider_reported_cost_usd, ge.estimated_cost_usd"` >= 1 ou equivalente COALESCE >= 6 (uma por view); `grep -c "campaign_pipeline"` >= 2 (exclusão de delivery em views/RPC + CHECK D5); `grep -c "REVERT"` == 1; `grep -c "credit_tx_id"` == 1; `grep -c "SET search_path = ''''"` == 2 (2 RPCs)</automated>
  </verify>
  <acceptance_criteria>
    - 6 views admin_ai_* criadas (operation_costs, campaign_delivery_costs, by_provider_model, by_stage, by_store, cost_vs_credits) somando APENAS call-level com COALESCE
    - admin_cost_vs_credits reconcilia por campanha (credit_transactions type='deduction' + metadata.feature) e por VS (credit_tx_id)
    - RPC admin_get_ai_costs com 8 filtros, SECURITY DEFINER, search_path='', p_hours ≥ 1
    - Seção REVERT comentada com todos os DROPs em ordem reversa
    - Nenhum GRANT de views para authenticated/anon; nenhuma alteração em admin_get_metrics
  </acceptance_criteria>
  <done>Migration completa: views + RPC de apuração + REVERT — pronta para push</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 3: [BLOCKING] Schema push — supabase db push + validação das RPCs/views</name>
  <files>supabase/migrations/20260808000001_f38_1_create_ai_cost_accounting.sql</files>
  <read_first>
    - supabase/config.toml (projeto linkado — `.temp/` presente)
    - .env.local ou ambiente (presença de SUPABASE_ACCESS_TOKEN)
  </read_first>
  <action>
    Aplicar a migration no banco remoto (mesmo fluxo do 38-01 Task 2 — o projeto já está linkado):

    1. Se `SUPABASE_ACCESS_TOKEN` não estiver no ambiente, procurar em `.env.local`/secrets; se ausente, pedir ao usuário o token (Supabase Dashboard → Account → Access Tokens) via checkpoint — a tarefa é [BLOCKING] e o plano é `autonomous: false`.

    2. Rodar `supabase db push` na raiz do repo com `$env:SUPABASE_ACCESS_TOKEN` setado (workaround non-TTY). Se o push pedir confirmação interativa que não pode ser suprimida, parar e sinalizar intervenção manual.

    3. Validar: `supabase migration list` deve mostrar `20260808000001_f38_1_create_ai_cost_accounting` como aplicada no remoto; `supabase db push` deve sair com código 0 sem erro de SQL.

    4. Validação funcional (se o banco remoto permitir consulta direta): `SELECT proname, prosecdef FROM pg_proc WHERE proname IN ('admin_set_ai_model_price','admin_get_ai_costs');` deve retornar 2 linhas com `prosecdef = true`; `SELECT count(*) FROM pg_views WHERE viewname LIKE 'admin_ai%' OR viewname = 'admin_cost_vs_credits';` deve retornar 6. Se não houver acesso de consulta, registrar a validação do push (migration list) como evidência. Validação SQL completa I1–I6 (versionamento, RLS, views com dados reais, admin_get_metrics compat) fica para a verificação da fase (38-1-10) — aqui basta o push + presença em pg_proc/pg_views.

    NÃO rodar os testes de integração SQL completos neste plano.
  </action>
  <verify>
    <automated>supabase migration list — migration 20260808000001_f38_1_create_ai_cost_accounting presente no remoto; exit code 0 do `supabase db push`</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` terminou com sucesso (exit 0) OU intervenção manual registrada com evidência
    - `supabase migration list` mostra a migration `20260808000001_f38_1_create_ai_cost_accounting` no remoto
    - `admin_set_ai_model_price` e `admin_get_ai_costs` presentes em pg_proc (prosecdef=true) e as 6 views em pg_views (ou evidência equivalente)
    - Se o push exigiu autenticação, o token foi obtido do usuário e o push concluído (checkpoint resolvido)
  </acceptance_criteria>
  <done>Schema aplicado: colunas + tabela + seeds + RPCs + views no banco remoto — pré-requisito obrigatório para toda a fase</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RPC admin_set_ai_model_price ← service_role | Mutação financeira (preços internos) executada como definer (SECURITY DEFINER) — única via de escrita em ai_model_pricing |
| RPC admin_get_ai_costs ← service_role | Leitura de apuração de custos executada como definer — acessa generation_events (default-deny) e credit_transactions |
| ai_model_pricing | Tabela de preços internos — `authenticated` não pode ler (informação comercial confidencial) |
| generation_events + views admin_ai_* | Dados de custo de IA — RLS default-deny mantido; views acessíveis apenas via RPC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38.1-01 | Elevation of Privilege | RPC admin_set_ai_model_price / admin_get_ai_costs | mitigate | `SECURITY DEFINER` + `SET search_path = ''` (impede hijack de search_path — ASVS 4.1.1/4.3.1); `REVOKE EXECUTE FROM PUBLIC, anon, authenticated`; `GRANT EXECUTE TO service_role` apenas — F38.1-09/F38.1-17 |
| T-38.1-02 | Information Disclosure | ai_model_pricing (preços internos) | mitigate | RLS habilitado + `REVOKE ALL FROM anon, authenticated` (sem GRANT SELECT — D8); único acesso via RPC definer e API admin sob requireAdmin — F38.1-07 |
| T-38.1-03 | Information Disclosure | Views admin_ai_* / admin_cost_vs_credits | mitigate | Sem GRANT direto ao cliente; acesso exclusivo via RPC SECURITY DEFINER (D10) — F38.1-12..16 |
| T-38.1-04 | Tampering | Dados de custo (generation_events) | mitigate | CHECK `chk_generation_events_cost_source` (5 valores fechados) + CHECK `chk_generation_events_type` (12 valores) + delivery markers sem custo (anti-dupla-contagem D1/D6); sem política RLS de escrita para authenticated (default-deny) — F38.1-01 |
| T-38.1-05 | Injection | Filtros de admin_get_ai_costs | mitigate | Parâmetros vinculados (p_* via argumentos da função, nunca concatenação de strings); `SET search_path = ''`; sem dynamic SQL — F38.1-17 |
| T-38.1-06 | DoS | admin_get_ai_costs sem limite de período | mitigate | `p_hours` validado ≥ 1; CTE filtrada por `created_at >= v_cutoff` (janela limitada, padrão admin_get_metrics) — F38.1-17 |
| T-38.1-07 | Tampering | Seeds de preço | mitigate | `ON CONFLICT (provider, model) DO NOTHING` (idempotente, não sobrescreve ajuste manual); UNIQUE(provider, model) — F38.1-08 |
| T-38.1-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo é instalado neste plano — gate de legitimidade não se aplica |
</threat_model>

<verification>
- `supabase db push` aplicado; `supabase migration list` confirma `20260808000001_f38_1_create_ai_cost_accounting` no remoto
- RPCs `admin_set_ai_model_price` + `admin_get_ai_costs` em pg_proc (prosecdef = true); 6 views em pg_views
- Validação SQL completa (I1–I6) fica para a verificação da fase (38-1-10)
</verification>

<success_criteria>
- Migration aplicada no remoto: generation_events com 9 colunas novas + 2 CHECKs + 5 índices; campaigns.operation_run_id + índice
- ai_model_pricing + 7 seeds vigentes + RPC versionada no remoto com RLS service_role-only
- 6 views de apuração/reconciliação + RPC admin_get_ai_costs no remoto
- `admin_get_metrics` (F28) e `reserve_credit`/`credit_transactions` (F24) intactos
</success_criteria>

<output>
Create `.planning/phases/38-1-ai-cost-accounting/38-1-01-SUMMARY.md` when done
</output>

---
phase: 38-1-ai-cost-accounting
plan: 02
type: execute
wave: 2
depends_on:
  - "38-1-01"
files_modified:
  - "src/lib/ai-cost/types.ts"
  - "src/lib/ai-cost/tracker.ts"
  - "src/lib/visual-signature/types.ts"
  - "src/lib/visual-signature/generation-events.ts"
  - "src/lib/ai-cost/__tests__/tracker.test.ts"
autonomous: true
HAS_UI: 0
requirements:
  - F38.1-01
  - F38.1-02
  - F38.1-03
  - F38.1-04
  - F38.1-29
must_haves:
  truths:
    - "COST_SOURCES, CostSource, OPERATION_RUN_TYPES, OperationRunType, TokenUsage, CostResolution, AiCostEvent, AiCallInfo exportados de src/lib/ai-cost/types.ts SEM server-only (importáveis por zod/UI)"
    - "GenerationEventType/GenerationEventStatus em src/lib/visual-signature/types.ts expandidos para os 12 valores da migration (D5)"
    - "AiCostTracker.startRun(type) retorna { operationRunId, traceId } com UUIDs distintos"
    - "AiCostTracker.record(event) grava todas as colunas novas (D2) e NUNCA lança (best-effort — geração não bloqueada por telemetria, D7)"
    - "Delivery marker gravado com custo/tokens NULL + metadata.duration_is_pipeline: true (anti-dupla-contagem D1/D6)"
    - "insertGenerationEvent (VS) delega ao AiCostTracker mantendo a API externa (compat de testes e F37)"
    - "8 testes do tracker cobrindo record/colunas novas, nunca lança, startRun, delivery NULL, not_available com tokens, mesmo run agrupa, cost_source inválido TS, delegação"
  artifacts:
    - path: "src/lib/ai-cost/types.ts"
      provides: "Tipos centrais de custo (D1/D4/D7/D12)"
      exports: ["COST_SOURCES", "CostSource", "OPERATION_RUN_TYPES", "OperationRunType", "TokenUsage", "CostResolution", "AiCostEvent", "AiCallInfo"]
    - path: "src/lib/ai-cost/tracker.ts"
      provides: "AiCostTracker — camada única de escrita (D7)"
      exports: ["AiCostTracker"]
    - path: "src/lib/ai-cost/__tests__/tracker.test.ts"
      provides: "8 testes do tracker (tarefa 6.2)"
      contains: "startRun"
  key_links:
    - from: "src/lib/ai-cost/tracker.ts"
      to: "generation_events (supabase)"
      via: "insert via supabaseAdmin com todas as colunas D2"
      pattern: "generation_events"
    - from: "src/lib/visual-signature/generation-events.ts"
      to: "src/lib/ai-cost/tracker.ts"
      via: "delegação (mantém API externa insertGenerationEvent)"
      pattern: "AiCostTracker"
---

<objective>
Criar a fundação de tipos e a camada única de registro de custo: `src/lib/ai-cost/types.ts` (sem server-only — `COST_SOURCES`, `CostSource`, `OPERATION_RUN_TYPES`, `OperationRunType`, `TokenUsage`, `CostResolution`, `AiCostEvent`, `AiCallInfo` — D1/D4/D7/D12), o `AiCostTracker` em `src/lib/ai-cost/tracker.ts` (server-only, best-effort, `startRun` + `record`, delivery marker sem custo — D7), a expansão do enum `GenerationEventType` para os 12 valores da migration (D5), e a delegação do helper `insertGenerationEvent` (VS) ao tracker mantendo a API externa (D7/D11). Testes do tracker (tarefa 6.2 — 8 testes).

Purpose: todo o restante da fase (estimador, serviços onCall, rotas, admin APIs) consome estes contratos. `AiCallInfo` é o padrão de exposição de usage dos serviços; `AiCostEvent` é o contrato único de gravação.
Output: tipos compartilhados + tracker testado (best-effort) + delegação do VS helper.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-1-ai-cost-accounting/38-1-CONTEXT.md
@.planning/phases/38-1-ai-cost-accounting/38-1-PATTERNS.md
@openspec/changes/fase-38-1-ai-cost-accounting/specs/ai-cost-tracker/spec.md

<interfaces>
Contratos que o executor precisa (extraídos do spec ai-cost-tracker + 38-1-PATTERNS.md):

**Sem UI (HAS_UI: 0). Nenhuma página/rota de UI é alterada por este plano.**

Tipos em `src/lib/ai-cost/types.ts` (analog: `src/lib/credit/types.ts` — const-array enums + interfaces, SEM server-only):
```typescript
export const COST_SOURCES = ["provider_reported","pricing_table","fallback_static","manual_unknown","not_available"] as const;
export type CostSource = (typeof COST_SOURCES)[number];
export const OPERATION_RUN_TYPES = ["campaign_delivery","visual_signature","brand_profile","theme"] as const;
export type OperationRunType = (typeof OPERATION_RUN_TYPES)[number];
export interface TokenUsage { promptTokens?: number; completionTokens?: number; totalTokens?: number; cachedInputTokens?: number; imageTokens?: number; }
export interface CostResolution { estimatedCostUsd: number | null; providerReportedCostUsd?: number | null; costSource: CostSource; pricingVersion?: string | null; }
export interface AiCallInfo { provider: string; model: string; usage?: TokenUsage; durationMs: number; providerReportedCostUsd?: number | null; }
export interface AiCostEvent {
  operationRunId: string; operationRunType: OperationRunType; traceId: string; storeId: string;
  userId?: string | null; campaignId?: string | null; visualSignatureId?: string | null; themeId?: string | null;
  generationType: GenerationEventType; provider: string; model: string; attemptNumber: number; durationMs: number;
  status: GenerationEventStatus; errorType?: string | null; tokens?: TokenUsage; cost?: CostResolution; metadata?: Record<string, unknown>;
}
```
- `GenerationEventType`/`GenerationEventStatus` vivem em `src/lib/visual-signature/types.ts:98-100` — hoje 3 valores; expandir `GenerationEventType` para os 12 valores da migration D5: `'campaign_pipeline' | 'campaign_copy' | 'campaign_input_validation' | 'campaign_image' | 'campaign_image_review' | 'visual_signature' | 'visual_signature_image' | 'visual_signature_validation' | 'brand_profile_without_logo' | 'brand_profile_with_logo' | 'brand_profile_vision' | 'brand_profile_text'`. NÃO duplicar o enum em ai-cost/types.ts (importar de visual-signature/types.ts).
- `cost` ausente = delivery marker (sem custo e sem tokens — D1/D6); `tokens` gravados sempre que existirem mesmo com `estimatedCostUsd: null` (not_available — D4).

`AiCostTracker` (analogs: `generation-events.ts` best-effort + `credit-service.ts` client injectable; NO ANALOG #2 — startRun é padrão novo):
```typescript
import "server-only";
import type { SupabaseClient } from "@supabase/supabase-js";
import { supabaseAdmin } from "@/lib/supabase/server";
export class AiCostTracker {
  constructor(private readonly client: SupabaseClient = supabaseAdmin) {}
  startRun(type: OperationRunType): { operationRunId: string; traceId: string };
  async record(event: AiCostEvent): Promise<void>;   // best-effort — nunca lança
}
```
- `startRun`: `crypto.randomUUID()` distinto para operationRunId e traceId (D1)
- `record`: try/catch + `console.error` + resolve sem lançar; insere em `generation_events` mapeando AiCostEvent → colunas snake_case (operation_run_id, operation_run_type, trace_id, visual_signature_id, theme_id, generation_type, provider, model, attempt_number, duration_ms, status, error_type, prompt_tokens, completion_tokens, total_tokens, cached_input_tokens, image_tokens, estimated_cost_usd, provider_reported_cost_usd, cost_source, pricing_version, metadata, store_id, user_id, campaign_id)
- Delivery marker: `cost`/`tokens` ausentes → colunas de custo/tokens NULL + `metadata.duration_is_pipeline: true` (D1/D6)

`insertGenerationEvent` (VS — `src/lib/visual-signature/generation-events.ts`, hoje best-effort insert direto): corpo passa a delegar a `new AiCostTracker().record(...)` mapeando `GenerationEventInsert` → `AiCostEvent`; mantém assinatura pública `insertGenerationEvent(event: GenerationEventInsert): Promise<GenerationEventRecord | null>` e semântica best-effort (retorno compatível com uso existente + F37).

Padrão de teste (analog: `src/lib/credit/__tests__/operation-cost-service.test.ts`): `vi.mock("@/lib/supabase/server")` no topo, table-dispatcher `mockFrom`, cliente mock injetado no constructor, `mockInsert.mockResolvedValue({ error: null })`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tipos — types.ts + expansão de GenerationEventType (12 valores D5)</name>
  <files>src/lib/ai-cost/types.ts, src/lib/visual-signature/types.ts</files>
  <read_first>
    - src/lib/credit/types.ts (padrão const-array enums + interfaces, sem server-only — linhas 44-62)
    - src/lib/visual-signature/types.ts (GenerationEventType/Status em 98-100, GenerationEventInsert 126-146)
    - openspec/changes/fase-38-1-ai-cost-accounting/specs/ai-cost-tracker/spec.md (linhas 3-122)
  </read_first>
  <behavior>
    - Teste 1 (compile-time): `COST_SOURCES` contém exatamente os 5 valores de D4; atribuir "invalid" a um `CostSource` → erro de tipo
    - Teste 2 (compile-time): `OPERATION_RUN_TYPES` contém os 4 domínios de D1 (campaign_delivery, visual_signature, brand_profile, theme)
    - Teste 3 (compile-time): um `AiCostEvent` sem o campo `cost` compila (delivery marker) e um com `tokens` + `cost: { estimatedCostUsd: null, costSource: "not_available" }` compila (D4)
    - Teste 4 (compile-time): `GenerationEventType` aceita os 12 valores da migration (ex.: "campaign_input_validation", "visual_signature_image", "brand_profile_text")
  </behavior>
  <action>
    Criar `src/lib/ai-cost/types.ts` com os 5 exports de contrato (COST_SOURCES, CostSource, OPERATION_RUN_TYPES, OperationRunType, TokenUsage, CostResolution, AiCallInfo, AiCostEvent) no estilo exato de `src/lib/credit/types.ts` (const-array + `as const` + type derived). `AiCostEvent.generationType: GenerationEventType` e `status: GenerationEventStatus` IMPORTADOS de `@/lib/visual-signature/types` (NÃO duplicar o enum — D5/PATTERNS).

    Em `src/lib/visual-signature/types.ts`: expandir `GenerationEventType` (linha 98) de 3 para os 12 valores da migration (campaign_pipeline, campaign_copy, campaign_input_validation, campaign_image, campaign_image_review, visual_signature, visual_signature_image, visual_signature_validation, brand_profile_without_logo, brand_profile_with_logo, brand_profile_vision, brand_profile_text). Preservar `GenerationEventStatus` ('success'|'failed'|'rejected'|'timeout') intacto. Os 6 tipos novos call-level (D5) devem existir aqui para o contrato do tracker.

    NÃO adicionar `import "server-only"` em types.ts (D7 — fonte única importável por zod/UI/route tests). NÃO alterar GenerationEventInsert ainda (fica para a delegação na Task 3).
  </action>
  <verify>
    <automated>npm run typecheck; `grep -c "server-only" src/lib/ai-cost/types.ts` == 0; `grep -c "COST_SOURCES" src/lib/ai-cost/types.ts` >= 2; `grep -c "campaign_input_validation" src/lib/visual-signature/types.ts` == 1; `grep -c "visual_signature_validation" src/lib/visual-signature/types.ts` == 1</automated>
  </verify>
  <acceptance_criteria>
    - 8 contratos exportados de ai-cost/types.ts sem server-only
    - GenerationEventType com 12 valores (6 novos call-level presentes)
    - Exports existentes de visual-signature/types.ts preservados
    - typecheck limpo
  </acceptance_criteria>
  <done>Tipos F38.1 criados + enum de generation_type alinhado ao banco (12 valores)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AiCostTracker — startRun + record best-effort (camada única de escrita)</name>
  <files>src/lib/ai-cost/tracker.ts</files>
  <read_first>
    - src/lib/visual-signature/generation-events.ts (padrão best-effort nunca-lança — linhas 4-24)
    - src/lib/credit/credit-service.ts (constructor com SupabaseClient injetável — linhas 1-9)
    - src/lib/ai-cost/types.ts (criado na Task 1 — contrato AiCostEvent)
    - .planning/phases/38-1-ai-cost-accounting/38-1-PATTERNS.md (seção tracker.ts — linhas 190-244)
  </read_first>
  <behavior>
    - Teste 1: `startRun("campaign_delivery")` retorna `{ operationRunId, traceId }` com UUIDs DIFERENTES e operationRunId UUID v4 válido
    - Teste 2: `record(event)` com evento completo (visualSignatureId, cached/image tokens, costSource, pricingVersion) → `mockInsert` chamado com TODAS as colunas novas preenchidas
    - Teste 3: `record` com `mockInsert` rejeitando (erro de rede) → resolve sem lançar + console.error
    - Teste 4: delivery (sem cost/tokens) → colunas estimated_cost_usd/provider_reported_cost_usd/tokens NULL + `metadata.duration_is_pipeline: true`
    - Teste 5: evento `costSource: "not_available"` + tokens → tokens persistidos, custo NULL
    - Teste 6: mesmo operationRunId em N records → N inserts com mesmo operation_run_id
  </behavior>
  <action>
    Criar `src/lib/ai-cost/tracker.ts`:

    1. `import "server-only";` na primeira linha (D7) + imports: `type SupabaseClient`, `supabaseAdmin` de `@/lib/supabase/server`, tipos de `./types` e `@/lib/visual-signature/types` (GenerationEventType/Status).
    2. `export class AiCostTracker` com `constructor(private readonly client: SupabaseClient = supabaseAdmin) {}` (padrão credit-service, permite mock em testes).
    3. `startRun(type: OperationRunType): { operationRunId: string; traceId: string }` — gera `crypto.randomUUID()` para cada um (NO ANALOG #2 — run-context é padrão novo; D1: UUIDs distintos, colunas separadas com semânticas distintas).
    4. `async record(event: AiCostEvent): Promise<void>` — best-effort (D7):
       - try/catch envolvendo todo o corpo; qualquer erro → `console.error("[AiCostTracker] ...")` e resolve sem lançar
       - Mapeamento AiCostEvent → linha snake_case: operation_run_id, operation_run_type, trace_id, store_id, user_id, campaign_id, visual_signature_id, theme_id, generation_type, provider, model, attempt_number, duration_ms, status, error_type, tokens (prompt_tokens, completion_tokens, total_tokens, cached_input_tokens, image_tokens), cost (estimated_cost_usd, provider_reported_cost_usd, cost_source, pricing_version), metadata
       - Delivery marker (D1/D6): quando `event.cost` ausente e `event.tokens` ausente → colunas de custo/tokens explícitas NULL + `metadata: { ...event.metadata, duration_is_pipeline: true }`
    5. NÃO exportar nada além da classe; NÃO adicionar métodos de leitura (leitura é via RPCs admin); NÃO validar costSource em runtime (TS cobre — D4, enum).
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai-cost/__tests__/tracker.test.ts; `grep -c "server-only" src/lib/ai-cost/tracker.ts` == 1; `grep -c "startRun" src/lib/ai-cost/tracker.ts` >= 1; `grep -c "duration_is_pipeline" src/lib/ai-cost/tracker.ts` == 1</automated>
  </verify>
  <acceptance_criteria>
    - startRun gera UUIDs distintos; record grava todas as colunas D2; nunca lança em erro
    - Delivery marker com custo/tokens NULL + duration_is_pipeline: true
    - Módulo server-only; classe única de escrita
    - Testes 1-6 da behavior list passando
  </acceptance_criteria>
  <done>AiCostTracker implementado (startRun + record best-effort) — única via de escrita de custo</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Delegação do insertGenerationEvent + testes completos do tracker (8)</name>
  <files>src/lib/visual-signature/generation-events.ts, src/lib/ai-cost/__tests__/tracker.test.ts, src/lib/visual-signature/types.ts</files>
  <read_first>
    - src/lib/visual-signature/generation-events.ts (linhas 4-24 — corpo atual do insert)
    - src/lib/visual-signature/types.ts (GenerationEventInsert — linhas 126-146)
    - src/lib/credit/__tests__/operation-cost-service.test.ts (padrão de mock — linhas 1-33)
    - .planning/phases/38-1-ai-cost-accounting/38-1-PATTERNS.md (seções generation-events.ts e tracker.test.ts — linhas 394-396, 428-455)
  </read_first>
  <behavior>
    - Teste 7: `insertGenerationEvent(event)` delega ao `AiCostTracker.record` (spy no tracker) e retorna o valor compatível com o uso existente (record/promise resolvida)
    - Teste 8 (compile-time): passar `costSource: "invalid"` em um CostResolution → erro de tipo (cost_source inválido rejeitado no TS, D4)
  </behavior>
  <action>
    Modificar `src/lib/visual-signature/generation-events.ts`: o corpo de `insertGenerationEvent` passa a delegar a gravação a `new AiCostTracker().record(...)` (mapeando `GenerationEventInsert` → `AiCostEvent` — operationRunId/traceId/operationRunType preenchidos quando presentes no insert; visual_signature_id, tokens, cost e pricing_version mapeados quando existirem), mantendo a assinatura pública `insertGenerationEvent(event: GenerationEventInsert): Promise<GenerationEventRecord | null>` e a semântica best-effort (qualquer falha → log + null, nunca lança). Retorno compatível com os consumidores atuais (rota VS + testes + F37).

    Em `src/lib/visual-signature/types.ts`: estender `GenerationEventInsert` (linha 126) com as novas colunas opcionais — `visual_signature_id`, `operation_run_id`, `trace_id`, `operation_run_type`, tokens (cached_input_tokens/image_tokens), `provider_reported_cost_usd`, `cost_source`, `pricing_version` — para o mapeamento do delegate.

    Criar `src/lib/ai-cost/__tests__/tracker.test.ts` (tarefa 6.2 — 8 testes no total):
    - Padrão: `vi.mock("@/lib/supabase/server", () => ({ supabaseAdmin: {} as any }))` no topo; cliente mock `{ from: mockFrom }`; `mockFrom` table-dispatcher (generation_events → `{ insert: mockInsert }`); `new AiCostTracker(mockAdminClient as any)` em beforeEach com `vi.clearAllMocks()`.
    - Casos (spec ai-cost-tracker, linhas 144-185): record grava todas as colunas novas; record nunca lança em erro de escrita; startRun gera operation_run_id/trace_id distintos; not_available grava tokens com custo NULL; mesmo run agrupa N chamadas (mesmo operation_run_id); delivery marker custo/tokens NULL + duration_is_pipeline; cost_source inválido rejeitado em compile time (test de tipo via `@ts-expect-error`); insertGenerationEvent delega ao tracker (retorno compatível).
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai-cost/__tests__/tracker.test.ts; npm run typecheck; `grep -c "AiCostTracker" src/lib/visual-signature/generation-events.ts` >= 1; `grep -c "duration_is_pipeline" src/lib/ai-cost/__tests__/tracker.test.ts` == 1; `grep -c "@ts-expect-error" src/lib/ai-cost/__tests__/tracker.test.ts` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - insertGenerationEvent delega ao tracker mantendo API externa + best-effort
    - GenerationEventInsert com as novas colunas opcionais (D2/D5)
    - 8 testes do tracker passando (npx vitest run — 0 falhas)
    - typecheck limpo
  </acceptance_criteria>
  <done>Delegação do VS helper feita + 8 testes do tracker verdes — camada única de escrita em vigor</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AiCostTracker.record → generation_events (supabaseAdmin) | Escrita de telemetria de custo via service_role; roda em server (server-only); nunca bloqueia geração |
| insertGenerationEvent → AiCostTracker | Helper público da VS delega ao tracker — sem novo caminho de escrita |
| types.ts → consumidores client/zod | Tipos sem server-only importáveis por UI — devem permanecer sem dados sensíveis |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38.1-08 | DoS | AiCostTracker.record em falha de escrita | mitigate | Best-effort: try/catch + console.error + resolve sem lançar — geração nunca bloqueada por telemetria (D7) — F38.1-04 |
| T-38.1-09 | Tampering | Entrega com custo duplicado (delivery × call) | mitigate | Delivery marker com custo/tokens NULL + `metadata.duration_is_pipeline: true` (anti-dupla-contagem D1/D6); `AiCostEvent` sem `cost` = delivery — F38.1-04 |
| T-38.1-10 | Information Disclosure | types.ts importado por client | mitigate | Apenas contratos de dados (nomes/campos), sem credenciais/dados de banco; sem server-only para não vazar código de servidor — F38.1-01 |
| T-38.1-11 | Spoofing | cost_source/valores inválidos | mitigate | Enums TS `as const` (CostSource, OperationRunType) rejeitados em compile time + CHECK `chk_generation_events_cost_source` no banco (38-1-01) — F38.1-01 |
| T-38.1-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado neste plano — gate de legitimidade não se aplica |
</threat_model>

<verification>
- `npm run typecheck` limpo (tipos + enum + delegação)
- `npx vitest run src/lib/ai-cost/__tests__/tracker.test.ts` — 8 testes, 0 falhas
- Nenhum `server-only` em types.ts; tracker com `import "server-only"`
</verification>

<success_criteria>
- Tipos centrais exportados sem server-only; enum GenerationEventType com 12 valores (D5)
- AiCostTracker único caminho de escrita (startRun + record best-effort, delivery marker sem custo)
- insertGenerationEvent delega ao tracker com compat retroativa
- 8 testes do tracker verdes (tarefa 6.2)
</success_criteria>

<output>
Create `.planning/phases/38-1-ai-cost-accounting/38-1-02-SUMMARY.md` when done
</output>

---
phase: 38-1-ai-cost-accounting
plan: 03
type: execute
wave: 2
depends_on:
  - "38-1-01"
files_modified:
  - "src/lib/admin/schemas.ts"
  - "src/app/api/admin/ai-model-pricing/route.ts"
  - "src/lib/ai-cost/admin-service.ts"
  - "src/app/api/admin/ai-costs/route.ts"
  - "src/app/api/admin/ai-model-pricing/__tests__/route.test.ts"
  - "src/lib/ai-cost/__tests__/admin-service.test.ts"
  - "src/app/api/admin/ai-costs/__tests__/route.test.ts"
autonomous: true
HAS_UI: 0
requirements:
  - F38.1-07
  - F38.1-08
  - F38.1-09
  - F38.1-10
  - F38.1-11
  - F38.1-12
  - F38.1-13
  - F38.1-14
  - F38.1-15
  - F38.1-16
  - F38.1-17
  - F38.1-18
  - F38.1-19
must_haves:
  truths:
    - "GET /api/admin/ai-model-pricing retorna a estrutura de preços vigente (1 linha vigente por provider/model, sem linhas superseded) para admins autenticados"
    - "PUT /api/admin/ai-model-pricing atualiza preço via RPC admin_update_ai_model_pricing e NUNCA sobrescreve — cria 2ª linha com versão nova e supersede a anterior (D8)"
    - "PUT sem reason → 400; GET/PUT sem admin (não autenticado ou não-admin autenticado) → 403 (requireAdmin)"
    - "Usuário store owner autenticado NÃO lê ai_model_pricing (RLS nega; 403 no admin route)"
    - "GET /api/admin/ai-costs retorna agregação por operation run (só call-level), por etapa de campanha, e reconciliação cost × créditos — via RPC admin_get_ai_costs com filtros storeId/from/to"
    - "Reconciliação admin_cost_vs_credits inclui eventos só provider_reported e reconcilia VS via credit_tx_id"
    - "13 testes (6.6 = 6 no route ai-model-pricing; 6.7 = 6 no admin-service) verdes"
  artifacts:
    - path: "src/app/api/admin/ai-model-pricing/route.ts"
      provides: "CRUD admin de preços de modelo (D3/D8)"
      exports: ["GET", "PUT"]
    - path: "src/lib/ai-cost/admin-service.ts"
      provides: "Camada de leitura admin: agregações, campanhas por etapa, reconciliação cost×créditos"
      exports: ["AiCostAdminService"]
    - path: "src/app/api/admin/ai-costs/route.ts"
      provides: "GET admin de custos agregados (D3)"
      exports: ["GET"]
    - path: "src/lib/admin/schemas.ts"
      provides: "Schemas zod das rotas admin de custo"
      contains: "AiModelPricing"
  key_links:
    - from: "src/app/api/admin/ai-model-pricing/route.ts"
      to: "RPC admin_update_ai_model_pricing"
      via: "supabaseAdmin.rpc com p_actor_id + p_reason (auditoria D8)"
      pattern: "admin_update_ai_model_pricing"
    - from: "src/lib/ai-cost/admin-service.ts"
      to: "Views admin_ai_operation_costs / admin_campaign_delivery_costs / admin_cost_vs_credits"
      via: "queries .from() com select específico por view"
      pattern: "admin_(ai_operation_costs|campaign_delivery_costs|cost_vs_credits)"
---

<objective>
Criar as rotas admin de custo: `GET`/`PUT /api/admin/ai-model-pricing` (estrutura de preços + versionamento via RPC `admin_update_ai_model_pricing` — D3/D8) e `GET /api/admin/ai-costs` (agregação por operation run, por etapa de campanha e reconciliação cost × créditos via RPC `admin_get_ai_costs` + views — D3/D10), estendendo `src/lib/admin/schemas.ts`. Testes das tarefas 6.6 (6 testes — pricing/API) e 6.7 (6 testes — Views/RPC).

Purpose: os números de custo ficam visíveis e auditáveis para o admin, com versionamento de preços imutável (D8) e reconciliação contra créditos (D10).
Output: rotas admin + schemas + 13 testes de contrato/API.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-1-ai-cost-accounting/38-1-CONTEXT.md
@.planning/phases/38-1-ai-cost-accounting/38-1-PATTERNS.md
@openspec/changes/fase-38-1-ai-cost-accounting/specs/ai-model-pricing/spec.md
@openspec/changes/fase-38-1-ai-cost-accounting/specs/ai-cost-accounting/spec.md

<interfaces>
Contratos que o executor precisa (extraídos do spec + PATTERNS + analog operation-costs):

**Sem UI (HAS_UI: 0). Nenhuma página de UI é alterada.**

Analog completo — `src/app/api/admin/operation-costs/route.ts` (padrão a seguir):
- `apiHandler(async (request: Request) => ...)` de `@/lib/auth/api-handler`
- `await requireAdmin()` de `@/lib/admin/require-admin` no topo do handler
- Zod: `UpdateOperationCostRequestSchema` de `@/lib/admin/schemas`; catch `ZodError` → `{ error: "Dados inválidos", details: err.errors }` 400
- RPC: `supabaseAdmin.rpc("admin_update_operation_cost", { p_actor_id: admin.userId, ... })`; erros mapeados por `msg.includes(...)` → 400/500
- GET: monta resposta com join de users (email) — em ai-costs, join NÃO é necessário (agregação vem do RPC)
- Testes: `src/app/api/admin/operation-costs/__tests__/route.test.ts` — mocks `@/lib/supabase/server`, `@/lib/admin/require-admin`, `apiHandler` executa o handler; padrão de casos (linhas 78-240)

RPCs/views disponíveis (criados na migration 38-1-01 — existência verificada por `pg_proc` em Task 2/3 daquele plano):
- `admin_update_ai_model_pricing(p_actor_id uuid, p_provider text, p_model text, p_input_cost_usd numeric, p_output_cost_usd numeric, p_image_unit_cost_usd numeric, p_reason text) → jsonb` — valida reason; cria linha NOVA com version+1 e supersede a anterior; retorna a nova linha (D8)
- `admin_get_ai_costs(p_store_id uuid, p_from timestamptz, p_to timestamptz) → jsonb` — filtros opcionais (D3)
- Views (agregações SQL): `admin_ai_operation_costs` (agrupa por run, soma SÓ call-level — exclui delivery markers D1/D6), `admin_campaign_delivery_costs` (por etapa — D5), `admin_cost_vs_credits` (reconciliação cost × créditos — inclui eventos só provider_reported; VS reconcilia via `credit_tx_id` — D10)

Schemas a criar em `src/lib/admin/schemas.ts` (estilo UpdateOperationCostRequestSchema — refs `src/lib/admin/schemas.ts`):
```typescript
export const AiModelPricingQuerySchema = z.object({
  provider: z.string().optional(), model: z.string().optional(),
});
export const AiModelPricingUpdateSchema = z.object({
  provider: z.string().min(1), model: z.string().min(1),
  inputCostUsd: z.number().nonnegative().optional(),
  outputCostUsd: z.number().nonnegative().optional(),
  imageUnitCostUsd: z.number().nonnegative().optional(),
  reason: z.string().min(1),
}).refine((d) => d.inputCostUsd !== undefined || d.outputCostUsd !== undefined || d.imageUnitCostUsd !== undefined, { message: "pelo menos um custo" });
export const AiCostsQuerySchema = z.object({
  storeId: z.string().uuid().optional(), from: z.string().optional(), to: z.string().optional(),
});
```

**Nota de contrato (6.7):** não existe harness de teste SQL no projeto (grep por pg_proc/information_schema em *.test.ts = 0 resultados). Os 6 testes de Views/RPC são testes DE CONTRATO na camada de serviço: mockam os retornos das views/RPC e verificam o RPC chamado com os argumentos corretos + o mapeamento de resposta (agrupamento por run, exclusão de delivery, por etapa, reconciliação com provider_reported e credit_tx_id). O comportamento SQL em si é validado na execução da migration (38-1-01 Task 2/3: pg_proc + sqlc) e nas queries de UAT (38-1-10, I1-I6).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schemas + GET/PUT /api/admin/ai-model-pricing (versionamento D8)</name>
  <files>src/lib/admin/schemas.ts, src/app/api/admin/ai-model-pricing/route.ts</files>
  <read_first>
    - src/lib/admin/schemas.ts (estilo dos schemas admin existentes)
    - src/app/api/admin/operation-costs/route.ts (analog integral — 110 linhas)
    - src/app/api/admin/operation-costs/__tests__/route.test.ts (padrão de mock — linhas 1-76)
    - openspec/changes/fase-38-1-ai-cost-accounting/specs/ai-model-pricing/spec.md (linhas 1-60)
  </read_first>
  <behavior>
    - Teste 1: PUT válido → `supabaseAdmin.rpc("admin_update_ai_model_pricing", ...)` chamado com p_actor_id + p_reason + custos; resposta 200 com a linha nova (nova versão)
    - Teste 2: PUT cria 2ª linha — versão retornada é maior que a vigente (versionamento, nunca sobrescreve — D8)
    - Teste 3: PUT sem reason → 400 zod ("Dados inválidos")
    - Teste 4: PUT com body sem nenhum custo → 400 zod (refine "pelo menos um custo")
    - Teste 5: GET sem admin → 403; PUT sem admin → 403; GET com usuário autenticado não-admin → 403 (RLS/requireAdmin — store owner não lê)
    - Teste 6: GET retorna a estrutura vigente (1 linha por provider/model com is_current=true — estrutura dos seeds)
  </behavior>
  <action>
    1. Em `src/lib/admin/schemas.ts`, adicionar `AiModelPricingQuerySchema`, `AiModelPricingUpdateSchema` e `AiCostsQuerySchema` conforme contrato acima (zod; NÃO usar zod-3.24+ API nova — manter o estilo `z.object(...)` do arquivo atual).
    2. Criar `src/app/api/admin/ai-model-pricing/route.ts`:
       - `export const GET = apiHandler(...)`: `requireAdmin()`; query parse opcional (provider/model); `supabaseAdmin.from("ai_model_pricing").select(...).eq("is_current", true)` com filtros provider/model quando presentes; resposta `{ pricing: [...] }` mapeando para { id, provider, model, inputCostUsd, outputCostUsd, imageUnitCostUsd, version, isCurrent, updatedBy, updatedAt } (nomes camelCase; numerics como number).
       - `export const PUT = apiHandler(...)`: `requireAdmin()`; `AiModelPricingUpdateSchema.parse(await request.json())` com catch ZodError → 400; `supabaseAdmin.rpc("admin_update_ai_model_pricing", { p_actor_id: admin.userId, p_provider: body.provider, p_model: body.model, p_input_cost_usd: body.inputCostUsd ?? null, p_output_cost_usd: body.outputCostUsd ?? null, p_image_unit_cost_usd: body.imageUnitCostUsd ?? null, p_reason: body.reason })`; erro → mapear por `msg.includes(...)` ("pricing_reason_required", "model_not_found", "pricing_no_fields") → 400; retorno `{ pricing: { nova linha } }`.
    3. Criar `src/app/api/admin/ai-model-pricing/__tests__/route.test.ts` com os 6 casos da behavior list (padrão de mock do operation-costs: vi.mock supabase/server + require-admin; mockInsert/mockRpc; table-dispatcher).
  </action>
  <verify>
    <automated>npx vitest run src/app/api/admin/ai-model-pricing/__tests__/route.test.ts; npm run typecheck; `grep -c "admin_update_ai_model_pricing" src/app/api/admin/ai-model-pricing/route.ts` == 1; `grep -c "reason" src/app/api/admin/ai-model-pricing/route.ts` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - GET/PUT funcionais com versionamento via RPC (nunca sobrescreve — D8)
    - 403 para não-admin (incl. autenticado não-admin — RLS)
    - 400 zod para reason ausente e body sem custos
    - 6 testes verdes
  </acceptance_criteria>
  <done>Rota admin de preços pronta (GET estrutura vigente + PUT versionado com auditoria)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AiCostAdminService + GET /api/admin/ai-costs (agregação D3/D10)</name>
  <files>src/lib/ai-cost/admin-service.ts, src/app/api/admin/ai-costs/route.ts</files>
  <read_first>
    - src/lib/credit/operation-cost-service.ts (padrão de serviço + OperationCostUnavailableError — refs de erro 503)
    - src/app/api/admin/operation-costs/route.ts (GET — linhas 12-58)
    - openspec/changes/fase-38-1-ai-cost-accounting/specs/ai-cost-accounting/spec.md (linhas 1-90)
  </read_first>
  <behavior>
    - Teste 7: `getAiCosts({ storeId, from, to })` → RPC `admin_get_ai_costs` chamado com p_store_id/p_from/p_to (null quando ausentes); resposta mapeada por run (só call-level — delivery markers ausentes)
    - Teste 8: `getCampaignDeliveryCosts()` → `.from("admin_campaign_delivery_costs").select(...)` retorna custo por etapa (generation_type call-level)
    - Teste 9: `getReconciliation()` → `.from("admin_cost_vs_credits").select(...)` inclui eventos só provider_reported (provider_reported_cost_usd não nulo e estimated nulo)
    - Teste 10: reconciliação VS — crédito vinculado via `credit_tx_id` presente no mapeamento (D10)
    - Teste 11: erro de serviço (RPC falha) → erro mapeado a 503 na rota (padrão OperationCostUnavailableError)
    - Teste 12: GET /api/admin/ai-costs com filtros storeId/from/to → parse de query via AiCostsQuerySchema e repasse ao serviço; sem admin → 403
  </behavior>
  <action>
    1. Criar `src/lib/ai-cost/admin-service.ts`:
       - `AiCostAdminService` com `constructor(private readonly client = supabaseAdmin)` (client injetável para testes — padrão credit-service).
       - `getAiCosts(filters: { storeId?: string; from?: string; to?: string })`: `client.rpc("admin_get_ai_costs", { p_store_id: filters.storeId ?? null, p_from: filters.from ?? null, p_to: filters.to ?? null })`; erro → lança `AiCostAdminUnavailableError` (novo, em `src/lib/ai-cost/admin-errors.ts` OU no próprio admin-service.ts — verificar padrão de operation-cost-service e seguir); retorno tipado com agrupamento por operation run (o SQL já exclui delivery markers — contrato 6.7).
       - `getCampaignDeliveryCosts()`: `client.from("admin_campaign_delivery_costs").select("*")` → linhas por etapa (campaign_pipeline/campaign_copy/campaign_input_validation/campaign_image/campaign_image_review).
       - `getReconciliation()`: `client.from("admin_cost_vs_credits").select("*")` → linhas com `credit_tx_id`, `provider_reported_cost_usd`, `estimated_cost_usd` (inclui eventos só provider_reported — D10).
    2. Criar `src/app/api/admin/ai-costs/route.ts`: `apiHandler` + `requireAdmin()`; `AiCostsQuerySchema.parse` dos searchParams (invalid → 400); chama os 3 métodos; resposta `{ operationRuns, campaignStages, reconciliation }`; erro de serviço → 503.
    3. Criar `src/app/api/admin/ai-costs/__tests__/route.test.ts`: 403 sem admin; 400 query inválida; 200 com os 3 blocos; 503 erro de serviço (comportamento central da rota).
  </action>
  <verify>
    <automated>npx vitest run src/app/api/admin/ai-costs/__tests__/route.test.ts; npm run typecheck; `grep -c "admin_get_ai_costs" src/lib/ai-cost/admin-service.ts` == 1; `grep -c "admin_cost_vs_credits" src/lib/ai-cost/admin-service.ts` == 1</automated>
  </verify>
  <acceptance_criteria>
    - AiCostAdminService com 3 métodos (RPC + 2 views); rota GET agregada com filtros e tratamento 503
    - 403/400/200/503 cobertos na rota
    - typecheck limpo
  </acceptance_criteria>
  <done>Consulta admin de custos operacional (agregação por run/etapa/reconciliação)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Testes 6.7 — contrato Views/RPC no admin-service (6 testes)</name>
  <files>src/lib/ai-cost/__tests__/admin-service.test.ts, src/lib/ai-cost/admin-service.ts</files>
  <read_first>
    - src/lib/credit/__tests__/operation-cost-service.test.ts (padrão vi.mock + mockFrom + mockRpc — linhas 1-40)
    - src/lib/ai-cost/admin-service.ts (criado na Task 2)
    - .planning/phases/38-1-ai-cost-accounting/38-1-PATTERNS.md (padrão de teste — seção tests)
  </read_first>
  <behavior>
    - Teste 13: `getAiCosts` → agrupamento por run mapeado com SÓ call-level (linha com generation_type delivery ausente da resposta do serviço — o mock devolve só call-level e o serviço repassa; contrato documentado)
    - Teste 14: `getCampaignDeliveryCosts` → agrupamento por etapa (uma linha por generation_type call-level de campanha)
    - Teste 15: `getReconciliation` → linha com só provider_reported (estimated null, provider_reported_cost_usd preenchido) incluída no mapeamento
    - Teste 16: reconciliação VS — `credit_tx_id` presente e repassado no mapeamento
    - Teste 17: `admin_get_ai_costs` filtra — RPC chamado com p_store_id/p_from/p_to exatos quando filtros presentes; com null quando ausentes
    - Teste 18: mapeamento numérico — numerics da view convertidos a number no JSON de resposta (sem string/number inconsistência)
  </behavior>
  <action>
    Criar `src/lib/ai-cost/__tests__/admin-service.test.ts` (tarefa 6.7 — 6 testes de contrato):
    - `vi.mock("@/lib/supabase/server")` + cliente mock injetado via constructor (padrão credit tests).
    - mockRpc para `admin_get_ai_costs` (retorna JSON com runs + só call-level); mockFrom para as views `admin_ai_operation_costs` (NÃO usada diretamente pelo serviço — coberta pelo RPC), `admin_campaign_delivery_costs`, `admin_cost_vs_credits`.
    - Verificar nome + argumentos exatos das chamadas (toHaveBeenCalledWith) — isso trava o contrato com as views/RPC criadas em 38-1-01.
    - Mapeamento de campos: credit_tx_id, provider_reported_cost_usd, estimated_cost_usd, generation_type, operation_run_id — presentes na resposta.
    - Numerics → number no mapeamento do serviço (parse quando string vinda do Postgres — helper local `toNumber`).
    Rodar suíte completa do plano: `npx vitest run src/app/api/admin/ai-model-pricing/__tests__/route.test.ts src/app/api/admin/ai-costs/__tests__/route.test.ts src/lib/ai-cost/__tests__/admin-service.test.ts`.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/admin/ai-model-pricing/__tests__/route.test.ts src/app/api/admin/ai-costs/__tests__/route.test.ts src/lib/ai-cost/__tests__/admin-service.test.ts; npm run typecheck; `grep -c "toHaveBeenCalledWith" src/lib/ai-cost/__tests__/admin-service.test.ts` >= 2</automated>
  </verify>
  <acceptance_criteria>
    - 6 testes de contrato Views/RPC verdes (nomes de RPC/view + args + mapeamento)
    - Suíte completa do plano (6.6 + 6.7 + rota ai-costs) verde: 14+ execuções, 0 falhas
    - typecheck limpo
  </acceptance_criteria>
  <done>Contrato Views/RPC travado em testes — 6.6 + 6.7 completos (12 testes)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin (requester) → /api/admin/ai-model-pricing, /api/admin/ai-costs | Input não confiável do admin atravessa aqui; rota protegida por requireAdmin |
| AiCostAdminService → RPCs/views (supabaseAdmin) | Consultas de agregação via service_role |
| ai_model_pricing → store owners | Tabela de preços — store owner autenticado NÃO deve ler (RLS) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38.1-12 | Spoofing | GET/PUT admin routes | mitigate | `requireAdmin()` em ambas as rotas (403/401) — F38.1-09 |
| T-38.1-13 | Information Disclosure | ai_model_pricing para não-admin | mitigate | RLS deny (38-1-01) + 403 via requireAdmin; testes cobrem "autenticado não-admin não lê" (6.6) — F38.1-09 |
| T-38.1-14 | Tampering | PUT ai-model-pricing (overwrite de preços) | mitigate | RPC versiona: NUNCA UPDATE — cria linha nova e supersede; p_reason obrigatório (auditoria D8); sem reason → 400 — F38.1-08 |
| T-38.1-15 | DoS | RPC/views pesadas via GET ai-costs sem filtros | accept | Filtros storeId/from/to opcionais (D3); volume admin-only; aceito na fase (sem paginação de SQL — contabilizado no design) |
| T-38.1-16 | Information Disclosure | Custo por store em resposta de admin | mitigate | Acesso restrito a admin (requireAdmin); agregações sem PII de lojista (sem email/endereço) — F38.1-09 |
| T-38.1-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado neste plano — gate de legitimidade não se aplica |
</threat_model>

<verification>
- `npx vitest run` nas 3 suítes (ai-model-pricing route, ai-costs route, admin-service) — 13 testes, 0 falhas
- `npm run typecheck` limpo
- RPC/views referenciadas existem (criadas em 38-1-01) — nomes exatos nos greps acima
</verification>

<success_criteria>
- GET/PUT ai-model-pricing com versionamento imutável + reason obrigatório (D8)
- GET ai-costs com agregações (run call-level, etapa, reconciliação cost×créditos com credit_tx_id) (D3/D10)
- 403/401 em acesso não-admin; 400 zod; 503 serviço indisponível
- Testes 6.6 (6) + 6.7 (6) verdes — 12 testes de contrato/API
</success_criteria>

<output>
Create `.planning/phases/38-1-ai-cost-accounting/38-1-03-SUMMARY.md` when done
</output>

---
phase: 38-1-ai-cost-accounting
plan: 03
type: execute
wave: 2
depends_on:
  - "38-1-01"
files_modified:
  - "src/lib/admin/schemas.ts"
  - "src/app/api/admin/ai-model-pricing/route.ts"
  - "src/lib/ai-cost/admin-service.ts"
  - "src/app/api/admin/ai-costs/route.ts"
  - "src/app/api/admin/ai-model-pricing/__tests__/route.test.ts"
  - "src/lib/ai-cost/__tests__/admin-service.test.ts"
  - "src/app/api/admin/ai-costs/__tests__/route.test.ts"
autonomous: true
HAS_UI: 0
requirements:
  - F38.1-07
  - F38.1-08
  - F38.1-09
  - F38.1-10
  - F38.1-11
  - F38.1-12
  - F38.1-13
  - F38.1-14
  - F38.1-15
  - F38.1-16
  - F38.1-17
  - F38.1-18
  - F38.1-19
must_haves:
  truths:
    - "GET /api/admin/ai-model-pricing retorna a estrutura de preços vigente (linhas com effective_until IS NULL — 1 por provider/model) + histórico opcional, para admins autenticados"
    - "PUT /api/admin/ai-model-pricing atualiza preço via RPC admin_set_ai_model_price (11 params, D8) e NUNCA sobrescreve — fecha a vigente (effective_until = now()) e abre nova (effective_from = now(), previous_id retornado)"
    - "PUT sem reason → 400; GET/PUT sem admin (não autenticado ou não-admin autenticado) → 403 (requireAdmin)"
    - "Usuário store owner autenticado NÃO lê ai_model_pricing (RLS nega; 403 no admin route)"
    - "GET /api/admin/ai-costs retorna agregação por operation run (só call-level), por etapa e reconciliação cost × créditos — via RPC admin_get_ai_costs (8 params, D10; p_hours no lugar de from/to)"
    - "Reconciliação inclui eventos só provider_reported e VS via credit_tx_id (dados vêm do RPC — views nunca lidas direto pelo serviço, D10)"
    - "16 testes verdes nas 3 suítes (6.6 = 6 no route ai-model-pricing; rota ai-costs = 4 casos 403/400/200/503; 6.7 = 6 no admin-service)"
  artifacts:
    - path: "src/app/api/admin/ai-model-pricing/route.ts"
      provides: "CRUD admin de preços de modelo (D3/D8)"
      exports: ["GET", "PUT"]
    - path: "src/lib/ai-cost/admin-service.ts"
      provides: "Camada de leitura admin: agregações, campanhas por etapa, reconciliação cost×créditos"
      exports: ["AiCostAdminService"]
    - path: "src/app/api/admin/ai-costs/route.ts"
      provides: "GET admin de custos agregados (D3)"
      exports: ["GET"]
    - path: "src/lib/admin/schemas.ts"
      provides: "Schemas zod das rotas admin de custo"
      contains: "AiModelPricing"
  key_links:
    - from: "src/app/api/admin/ai-model-pricing/route.ts"
      to: "RPC admin_set_ai_model_price"
      via: "supabaseAdmin.rpc com p_actor_id + p_reason + 5 dimensões de preço + source_url/source_note (D8)"
      pattern: "admin_set_ai_model_price"
    - from: "src/lib/ai-cost/admin-service.ts"
      to: "RPC admin_get_ai_costs (8 params)"
      via: "única via de leitura — views consumidas SQL-side pelo RPC (D10, sem GRANT direto)"
      pattern: "admin_get_ai_costs"
---

<objective>
Criar as rotas admin de custo: `GET`/`PUT /api/admin/ai-model-pricing` (estrutura de preços + versionamento via RPC `admin_set_ai_model_price` — D8) e `GET /api/admin/ai-costs` (agregação por operation run, por etapa e reconciliação cost × créditos via RPC `admin_get_ai_costs` — D3/D10), estendendo `src/lib/admin/schemas.ts`. Testes das tarefas 6.6 (6 testes — pricing/API no route ai-model-pricing), rota ai-costs (4 casos — 403/400/200/503) e 6.7 (6 testes — contrato RPC no admin-service).

Purpose: os números de custo ficam visíveis e auditáveis para o admin, com versionamento de preços imutável (D8) e reconciliação contra créditos (D10).
Output: rotas admin + schemas + 16 testes (6.6 = 6 pricing/API + rota ai-costs = 4 + 6.7 = 6 contrato RPC).
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-1-ai-cost-accounting/38-1-CONTEXT.md
@.planning/phases/38-1-ai-cost-accounting/38-1-PATTERNS.md
@openspec/changes/fase-38-1-ai-cost-accounting/specs/ai-model-pricing/spec.md
@openspec/changes/fase-38-1-ai-cost-accounting/specs/ai-cost-accounting/spec.md

<interfaces>
Contratos que o executor precisa (extraídos do spec + PATTERNS + analog operation-costs):

**Sem UI (HAS_UI: 0). Nenhuma página de UI é alterada.**

Analog completo — `src/app/api/admin/operation-costs/route.ts` (padrão a seguir):
- `apiHandler(async (request: Request) => ...)` de `@/lib/auth/api-handler`
- `await requireAdmin()` de `@/lib/admin/require-admin` no topo do handler
- Zod: `UpdateOperationCostRequestSchema` de `@/lib/admin/schemas`; catch `ZodError` → `{ error: "Dados inválidos", details: err.errors }` 400
- RPC: `supabaseAdmin.rpc("admin_update_operation_cost", { p_actor_id: admin.userId, ... })`; erros mapeados por `msg.includes(...)` → 400/500
- Testes: `src/app/api/admin/operation-costs/__tests__/route.test.ts` — mocks `@/lib/supabase/server`, `@/lib/admin/require-admin`, `apiHandler` executa o handler; padrão de casos (linhas 78-240)

RPCs criados na migration 38-1-01 (existência verificada por `pg_proc` em 38-1-01 Task 3 — assinaturas CANÔNICAS, não divergir):
- `admin_set_ai_model_price(p_actor_id UUID, p_provider TEXT, p_model TEXT, p_input NUMERIC, p_output NUMERIC, p_reason TEXT, p_cached NUMERIC DEFAULT NULL, p_image_unit NUMERIC DEFAULT NULL, p_image_token NUMERIC DEFAULT NULL, p_source_url TEXT DEFAULT NULL, p_source_note TEXT DEFAULT NULL) → jsonb {id, provider, model, effective_from, previous_id}` — p_reason ANTES dos DEFAULTs; fecha a vigente (effective_until = now()) e abre nova (effective_from = now()) na mesma transação (D8)
- `admin_get_ai_costs(p_operation_run_id UUID DEFAULT NULL, p_campaign_id UUID DEFAULT NULL, p_store_id UUID DEFAULT NULL, p_user_id UUID DEFAULT NULL, p_provider TEXT DEFAULT NULL, p_model TEXT DEFAULT NULL, p_generation_type TEXT DEFAULT NULL, p_hours INTEGER DEFAULT 24) → jsonb` — agrupamentos por operation_run / store / provider+model / generation_type + reconciliação (custo_usd_total, creditos_debitados, margem_estimada, regeneracoes); o SQL já exclui delivery markers (D1/D6/D10)

**Acesso a dados (D10):** as views `admin_ai_*`/`admin_cost_vs_credits` são consumidas EXCLUSIVAMENTE pelo RPC `admin_get_ai_costs` (SQL-side). O serviço TS NÃO usa `.from()` em views — toda leitura passa pelo RPC. Colunas reais de `ai_model_pricing`: `input_token_usd_per_1m, output_token_usd_per_1m, cached_input_token_usd_per_1m, image_unit_usd, image_token_usd_per_1m`; linha vigente = `effective_until IS NULL` (NÃO existe coluna `is_current`).

Schemas a criar em `src/lib/admin/schemas.ts` (estilo UpdateOperationCostRequestSchema — refs `src/lib/admin/schemas.ts`):
```typescript
export const AiModelPricingQuerySchema = z.object({
  provider: z.string().optional(), model: z.string().optional(), includeHistory: z.string().optional(),
});
export const AiModelPricingUpdateSchema = z.object({
  provider: z.string().min(1), model: z.string().min(1),
  inputCostUsd: z.number().nonnegative().optional(),
  outputCostUsd: z.number().nonnegative().optional(),
  cachedInputCostUsd: z.number().nonnegative().optional(),
  imageUnitCostUsd: z.number().nonnegative().optional(),
  imageTokenCostUsd: z.number().nonnegative().optional(),
  reason: z.string().min(1),
}).refine((d) => [d.inputCostUsd, d.outputCostUsd, d.cachedInputCostUsd, d.imageUnitCostUsd, d.imageTokenCostUsd].some((v) => v !== undefined), { message: "pelo menos um custo" });
export const AiCostsQuerySchema = z.object({
  storeId: z.string().uuid().optional(), userId: z.string().uuid().optional(),
  provider: z.string().optional(), model: z.string().optional(),
  generationType: z.string().optional(), operationRunId: z.string().uuid().optional(),
  campaignId: z.string().uuid().optional(), hours: z.coerce.number().int().min(1).default(24),
});
```

**Nota de contrato (6.7):** não existe harness de teste SQL no projeto (grep por pg_proc/information_schema em *.test.ts = 0 resultados). Os 6 testes de contrato RPC são testes NA camada de serviço: mockam o retorno JSONB do RPC `admin_get_ai_costs` e verificam (a) o RPC chamado com os 8 argumentos corretos e (b) o mapeamento da resposta (agrupamento por run, exclusão de delivery, por etapa, reconciliação com provider_reported e credit_tx_id). O comportamento SQL em si é validado na execução da migration (38-1-01 Task 3: pg_proc) e nas queries de UAT (38-1-10, I1-I6).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schemas + GET/PUT /api/admin/ai-model-pricing (versionamento D8)</name>
  <files>src/lib/admin/schemas.ts, src/app/api/admin/ai-model-pricing/route.ts</files>
  <read_first>
    - src/lib/admin/schemas.ts (estilo dos schemas admin existentes)
    - src/app/api/admin/operation-costs/route.ts (analog integral — 110 linhas)
    - src/app/api/admin/operation-costs/__tests__/route.test.ts (padrão de mock — linhas 1-76)
    - openspec/changes/fase-38-1-ai-cost-accounting/specs/ai-model-pricing/spec.md (linhas 1-60)
  </read_first>
  <behavior>
    - Teste 1: PUT válido → `supabaseAdmin.rpc("admin_set_ai_model_price", ...)` chamado com p_actor_id + p_reason + dimensões de preço; resposta 200 com `{ pricing: { id, provider, model, effectiveFrom, previousId } }`
    - Teste 2: PUT cria 2ª linha — previousId = id da linha fechada (versionamento por effective_from/effective_until, nunca sobrescreve — D8)
    - Teste 3: PUT sem reason → 400 zod ("Dados inválidos")
    - Teste 4: PUT com body sem nenhum custo → 400 zod (refine "pelo menos um custo")
    - Teste 5: GET sem admin → 403; PUT sem admin → 403; GET com usuário autenticado não-admin → 403 (RLS/requireAdmin — store owner não lê)
    - Teste 6: GET retorna a estrutura vigente (1 linha por provider/model com effective_until IS NULL — estrutura dos seeds); includeHistory=true retorna também as superseded
  </behavior>
  <action>
    1. Em `src/lib/admin/schemas.ts`, adicionar `AiModelPricingQuerySchema`, `AiModelPricingUpdateSchema` (5 dimensões de preço + reason) e `AiCostsQuerySchema` conforme contrato acima (zod; NÃO usar zod-3.24+ API nova — manter o estilo `z.object(...)` do arquivo atual).
    2. Criar `src/app/api/admin/ai-model-pricing/route.ts`:
       - `export const GET = apiHandler(...)`: `requireAdmin()`; query parse opcional (provider/model/includeHistory); `supabaseAdmin.from("ai_model_pricing").select("id, provider, model, input_token_usd_per_1m, output_token_usd_per_1m, cached_input_token_usd_per_1m, image_unit_usd, image_token_usd_per_1m, effective_from, effective_until, source_url, source_note, updated_by, updated_at").is("effective_until", null)` com filtros provider/model quando presentes; `includeHistory=true` → sem o filtro `.is`, ordenar `effective_from desc`; resposta `{ pricing: [...] }` mapeando para { id, provider, model, inputTokenUsdPer1M, outputTokenUsdPer1M, cachedInputTokenUsdPer1M, imageUnitUsd, imageTokenUsdPer1M, effectiveFrom, sourceUrl, sourceNote, updatedBy, updatedAt } (camelCase; numerics como number). NÃO mapear `version`/`isCurrent` — colunas não existem.
       - `export const PUT = apiHandler(...)`: `requireAdmin()`; `AiModelPricingUpdateSchema.parse(await request.json())` com catch ZodError → 400; `supabaseAdmin.rpc("admin_set_ai_model_price", { p_actor_id: admin.userId, p_provider: body.provider, p_model: body.model, p_input: body.inputCostUsd ?? null, p_output: body.outputCostUsd ?? null, p_reason: body.reason, p_cached: body.cachedInputCostUsd ?? null, p_image_unit: body.imageUnitCostUsd ?? null, p_image_token: body.imageTokenCostUsd ?? null, p_source_url: null, p_source_note: null })`; erro → mapear por `msg.includes(...)` ("ai_model_price_reason_required", "ai_model_price_no_dimension") → 400; retorno `{ pricing: { id, provider, model, effectiveFrom, previousId } }` (mapeando effective_from/previous_id do JSONB do RPC).
    3. Criar `src/app/api/admin/ai-model-pricing/__tests__/route.test.ts` com os 6 casos da behavior list (padrão de mock do operation-costs: vi.mock supabase/server + require-admin; mockRpc; table-dispatcher).
  </action>
  <verify>
    <automated>npx vitest run src/app/api/admin/ai-model-pricing/__tests__/route.test.ts; npm run typecheck; `grep -c "admin_set_ai_model_price" src/app/api/admin/ai-model-pricing/route.ts` == 1; `grep -c "is(\"effective_until\", null)" src/app/api/admin/ai-model-pricing/route.ts` == 1; `grep -c "reason" src/app/api/admin/ai-model-pricing/route.ts` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - GET/PUT funcionais com versionamento via RPC (nunca sobrescreve — D8)
    - 403 para não-admin (incl. autenticado não-admin — RLS)
    - 400 zod para reason ausente e body sem custos
    - 6 testes verdes
  </acceptance_criteria>
  <done>Rota admin de preços pronta (GET estrutura vigente + PUT versionado com auditoria)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AiCostAdminService + GET /api/admin/ai-costs (agregação D3/D10)</name>
  <files>src/lib/ai-cost/admin-service.ts, src/app/api/admin/ai-costs/route.ts</files>
  <read_first>
    - src/lib/credit/operation-cost-service.ts (padrão de serviço + OperationCostUnavailableError — refs de erro 503)
    - src/app/api/admin/operation-costs/route.ts (GET — linhas 12-58)
    - openspec/changes/fase-38-1-ai-cost-accounting/specs/ai-cost-accounting/spec.md (linhas 1-90)
  </read_first>
  <behavior>
    - Teste 7: `getAiCosts({ storeId })` → RPC `admin_get_ai_costs` chamado com os 8 params (p_operation_run_id/p_campaign_id/p_store_id/p_user_id/p_provider/p_model/p_generation_type = null quando ausentes; p_hours = 24 default)
    - Teste 8: resposta do RPC mapeada em 3 blocos — operationRuns (só call-level — delivery markers ausentes), campaignStages (agrupamento por generation_type), reconciliation (cost × créditos)
    - Teste 9: reconciliação inclui eventos só provider_reported (provider_reported_cost_usd não nulo e estimated nulo)
    - Teste 10: reconciliação VS — crédito vinculado via `credit_tx_id` presente no mapeamento (D10)
    - Teste 11: erro de serviço (RPC falha) → erro mapeado a 503 na rota (padrão OperationCostUnavailableError)
    - Teste 12: GET /api/admin/ai-costs com filtros → parse de query via AiCostsQuerySchema e repasse ao RPC; sem admin → 403
  </behavior>
  <action>
    1. Criar `src/lib/ai-cost/admin-service.ts`:
       - `AiCostAdminService` com `constructor(private readonly client = supabaseAdmin)` (client injetável para testes — padrão credit-service).
       - `getAiCosts(filters: { storeId?: string; userId?: string; provider?: string; model?: string; generationType?: string; operationRunId?: string; campaignId?: string; hours?: number })`: `client.rpc("admin_get_ai_costs", { p_operation_run_id: filters.operationRunId ?? null, p_campaign_id: filters.campaignId ?? null, p_store_id: filters.storeId ?? null, p_user_id: filters.userId ?? null, p_provider: filters.provider ?? null, p_model: filters.model ?? null, p_generation_type: filters.generationType ?? null, p_hours: filters.hours ?? 24 })`; erro → lança `AiCostAdminUnavailableError` (novo, em `src/lib/ai-cost/admin-errors.ts` OU no próprio admin-service.ts — verificar padrão de operation-cost-service e seguir); retorno tipado com 3 blocos derivados do JSONB do RPC: `operationRuns` (agrupamento por run — o SQL já exclui delivery markers), `campaignStages` (agrupamento por generation_type), `reconciliation` (linhas com credit_tx_id, provider_reported_cost_usd, estimated_cost_usd — inclui eventos só provider_reported — D10).
       - NÃO usar `.from()` em views — acesso exclusivo via RPC (D10).
    2. Criar `src/app/api/admin/ai-costs/route.ts`: `apiHandler` + `requireAdmin()`; `AiCostsQuerySchema.parse` dos searchParams (invalid → 400); chama `getAiCosts` com os filtros do schema (hours default 24); resposta `{ operationRuns, campaignStages, reconciliation }`; erro de serviço → 503.
    3. Criar `src/app/api/admin/ai-costs/__tests__/route.test.ts`: 403 sem admin; 400 query inválida; 200 com os 3 blocos; 503 erro de serviço (comportamento central da rota).
  </action>
  <verify>
    <automated>npx vitest run src/app/api/admin/ai-costs/__tests__/route.test.ts; npm run typecheck; `grep -c "admin_get_ai_costs" src/lib/ai-cost/admin-service.ts` == 1; `grep -c "p_hours" src/lib/ai-cost/admin-service.ts` == 1; `grep -c "\.from(\"" src/lib/ai-cost/admin-service.ts` == 0</automated>
  </verify>
  <acceptance_criteria>
    - AiCostAdminService com `getAiCosts` via RPC admin_get_ai_costs (única via de leitura — SEM .from() em views, D10); rota GET agregada com filtros e tratamento 503
    - 403/400/200/503 cobertos na rota
    - typecheck limpo
  </acceptance_criteria>
  <done>Consulta admin de custos operacional (agregação por run/etapa/reconciliação)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Testes 6.7 — contrato Views/RPC no admin-service (6 testes)</name>
  <files>src/lib/ai-cost/__tests__/admin-service.test.ts, src/lib/ai-cost/admin-service.ts</files>
  <read_first>
    - src/lib/credit/__tests__/operation-cost-service.test.ts (padrão vi.mock + mockFrom + mockRpc — linhas 1-40)
    - src/lib/ai-cost/admin-service.ts (criado na Task 2)
    - .planning/phases/38-1-ai-cost-accounting/38-1-PATTERNS.md (padrão de teste — seção tests)
  </read_first>
  <behavior>
    - Teste 13: `getAiCosts` → agrupamento por run mapeado com SÓ call-level (linha com generation_type delivery ausente da resposta do serviço — o mock devolve só call-level e o serviço repassa; contrato documentado)
    - Teste 14: `campaignStages` derivado do bloco por generation_type do RPC (uma linha por etapa call-level de campanha)
    - Teste 15: `reconciliation` → linha com só provider_reported (estimated null, provider_reported_cost_usd preenchido) incluída no mapeamento
    - Teste 16: reconciliação VS — `credit_tx_id` presente e repassado no mapeamento
    - Teste 17: `admin_get_ai_costs` filtra — RPC chamado com os 8 params exatos quando filtros presentes; com null quando ausentes; p_hours = 24 default
    - Teste 18: mapeamento numérico — numerics do JSONB convertidos a number no JSON de resposta (sem string/number inconsistência)
  </behavior>
  <action>
    Criar `src/lib/ai-cost/__tests__/admin-service.test.ts` (tarefa 6.7 — 6 testes de contrato):
    - `vi.mock("@/lib/supabase/server")` + cliente mock injetado via constructor (padrão credit tests).
    - mockRpc para `admin_get_ai_costs` (retorna JSONB com operationRuns + campaignStages + reconciliation; runs só call-level; credit_tx_id presente).
    - Verificar nome + argumentos exatos das chamadas (toHaveBeenCalledWith com os 8 params) — isso trava o contrato com o RPC criado em 38-1-01.
    - Mapeamento de campos: credit_tx_id, provider_reported_cost_usd, estimated_cost_usd, generation_type, operation_run_id — presentes na resposta.
    - Numerics → number no mapeamento do serviço (parse quando string vinda do Postgres — helper local `toNumber`).
    Rodar suíte completa do plano: `npx vitest run src/app/api/admin/ai-model-pricing/__tests__/route.test.ts src/app/api/admin/ai-costs/__tests__/route.test.ts src/lib/ai-cost/__tests__/admin-service.test.ts`.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/admin/ai-model-pricing/__tests__/route.test.ts src/app/api/admin/ai-costs/__tests__/route.test.ts src/lib/ai-cost/__tests__/admin-service.test.ts; npm run typecheck; `grep -c "toHaveBeenCalledWith" src/lib/ai-cost/__tests__/admin-service.test.ts` >= 2</automated>
  </verify>
  <acceptance_criteria>
    - 6 testes de contrato Views/RPC verdes (nomes de RPC/view + args + mapeamento)
    - Suíte completa do plano (6.6 = 6 + rota ai-costs = 4 + 6.7 = 6) verde: 16 execuções, 0 falhas
    - typecheck limpo
  </acceptance_criteria>
  <done>Contrato Views/RPC travado em testes — 6.6 + 6.7 + rota ai-costs completos (16 testes)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin (requester) → /api/admin/ai-model-pricing, /api/admin/ai-costs | Input não confiável do admin atravessa aqui; rota protegida por requireAdmin |
| AiCostAdminService → RPCs/views (supabaseAdmin) | Consultas de agregação via service_role |
| ai_model_pricing → store owners | Tabela de preços — store owner autenticado NÃO deve ler (RLS) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38.1-12 | Spoofing | GET/PUT admin routes | mitigate | `requireAdmin()` em ambas as rotas (403/401) — F38.1-09 |
| T-38.1-13 | Information Disclosure | ai_model_pricing para não-admin | mitigate | RLS deny (38-1-01) + 403 via requireAdmin; testes cobrem "autenticado não-admin não lê" (6.6) — F38.1-09 |
| T-38.1-14 | Tampering | PUT ai-model-pricing (overwrite de preços) | mitigate | RPC versiona: NUNCA UPDATE — fecha a vigente (effective_until = now()) e cria linha nova; p_reason obrigatório (auditoria D8); sem reason → 400 — F38.1-08 |
| T-38.1-15 | DoS | RPC pesada via GET ai-costs sem filtros | accept | `p_hours` default 24 com mínimo 1 (janela limitada); demais filtros opcionais; volume admin-only; aceito na fase (sem paginação de SQL — contabilizado no design) |
| T-38.1-16 | Information Disclosure | Custo por store em resposta de admin | mitigate | Acesso restrito a admin (requireAdmin); agregações sem PII de lojista (sem email/endereço) — F38.1-09 |
| T-38.1-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado neste plano — gate de legitimidade não se aplica |
</threat_model>

<verification>
- `npx vitest run` nas 3 suítes (ai-model-pricing route, ai-costs route, admin-service) — 16 testes, 0 falhas
- `npm run typecheck` limpo
- RPCs referenciadas existem (criadas em 38-1-01) — nomes e assinaturas exatos nos greps acima; nenhum `.from()` em views no serviço (D10)
</verification>

<success_criteria>
- GET/PUT ai-model-pricing com versionamento imutável (effective_from/effective_until) + reason obrigatório (D8)
- GET ai-costs via RPC admin_get_ai_costs (8 params, p_hours) com agregações (run call-level, etapa, reconciliação cost×créditos com credit_tx_id) (D3/D10)
- 403/401 em acesso não-admin; 400 zod; 503 serviço indisponível
- 16 testes verdes nas 3 suítes (6.6 = 6 pricing/API + rota ai-costs = 4 + 6.7 = 6 contrato RPC)
</success_criteria>

<output>
Create `.planning/phases/38-1-ai-cost-accounting/38-1-03-SUMMARY.md` when done
</output>

---
phase: 38-1-ai-cost-accounting
plan: 04
type: execute
wave: 3
depends_on:
  - "38-1-02"
  - "38-1-01"
files_modified:
  - "src/lib/ai-cost/ai-model-pricing.ts"
  - "src/lib/ai-cost/cost-estimator.ts"
  - "src/lib/ai-cost/legacy-estimator.ts"
  - "src/lib/ai-cost/index.ts"
  - "src/lib/ai-cost/__tests__/cost-estimator.test.ts"
  - "src/lib/ai-cost/__tests__/ai-model-pricing.test.ts"
  - "src/lib/ai-cost/__tests__/legacy-estimator.test.ts"
autonomous: true
HAS_UI: 0
requirements:
  - F38.1-05
  - F38.1-06
  - F38.1-07
  - F38.1-08
  - F38.1-09
  - F38.1-10
  - F38.1-11
must_haves:
  truths:
    - "resolveAiCost nunca retorna null (furo 1 sanado): sempre CostResolution com cost_source 5-valores (D9)"
    - "Precedência de fonte: provider_reported → pricing_table → fallback_static → not_available (D4/D9)"
    - "Preços vêm da tabela ai_model_pricing (1 linha vigente por provider/model) com DEFAULT_AI_MODEL_PRICING como bootstrap quando a tabela não tem linha (code_default — D8)"
    - "gpt-image-2 sem usage resolve via image_unit_usd (0.040) da linha de pricing; gemini-3.1-flash-lite resolve {0.10, 0.40} (furos 3/6 sanados)"
    - "Cached input tokens descontados do prompt (gpt-5.5 cached 0.50)"
    - "Modelo sem linha e sem default → costSource not_available com estimatedCostUsd null (tokens preservados, D4)"
    - "ModelPricing tem TODAS as dimensões opcionais (espelha CHECK chk_ai_model_pricing_at_least_one_price): modelo só de imagem (gpt-image-2/dall-e-3) carrega apenas imageUnitCostUsd; o mapper valida ao menos uma dimensão presente (linha vazia → null)"
    - "Env VENDEO_AI_FALLBACK_COST_USD (default 0.15) controla fallback_static; compat retroativa com VENDEO_IMAGE_GENERATION_FALLBACK_COST_USD"
    - "10 testes de 6.1 verdes (cenários nomeados do spec)"
  artifacts:
    - path: "src/lib/ai-cost/ai-model-pricing.ts"
      provides: "Serviço de leitura de preços (tabela + bootstrap D8)"
      exports: ["getModelPricing", "DEFAULT_AI_MODEL_PRICING"]
    - path: "src/lib/ai-cost/cost-estimator.ts"
      provides: "resolveAiCost — resolvedor de custo nunca-null (D9)"
      exports: ["resolveAiCost", "CostResolution"]
    - path: "src/lib/ai-cost/index.ts"
      provides: "Barrel com exportações novas (CostResolution, resolveAiCost, AiCostTracker, pricing)"
      contains: "resolveAiCost"
  key_links:
    - from: "src/lib/ai-cost/cost-estimator.ts"
      to: "src/lib/ai-cost/ai-model-pricing.ts"
      via: "getModelPricing (mockável em testes)"
      pattern: "getModelPricing"
    - from: "src/lib/ai-cost/ai-model-pricing.ts"
      to: "ai_model_pricing (supabase)"
      via: "select effective_until IS NULL por provider+model"
      pattern: "ai_model_pricing"
    - from: "src/lib/ai-cost/index.ts"
      to: "src/lib/ai-cost/tracker.ts + cost-estimator.ts + ai-model-pricing.ts"
      via: "re-export (padrão F37 index)"
      pattern: "export.*(resolveAiCost|AiCostTracker)"
---

<objective>
Substituir o `estimateAiCost` (Record TS + retorno null) pelo resolvedor definitivo `resolveAiCost` em `src/lib/ai-cost/cost-estimator.ts` (D9): precedência `provider_reported → pricing_table → fallback_static → not_available`, **nunca retorna null** (furo 1 sanado). Criar o serviço de preços `src/lib/ai-cost/ai-model-pricing.ts` (D8): leitura de `ai_model_pricing` (linha vigente) com `DEFAULT_AI_MODEL_PRICING` como bootstrap (`pricingVersion: "code_default"`). **Manter `estimateAiCost` como wrapper SÍNCRONO legado** em `src/lib/ai-cost/legacy-estimator.ts` (fallback/defaults locais — NÃO chama o `resolveAiCost` async; rota generate-image continua compilando até 38-1-07). Atualizar o barrel `index.ts`. Re-escrever `cost-estimator.test.ts` para o novo contrato com os 10 cenários de 6.1.

Purpose: os serviços onCall (38-1-05/06) e a rota generate-image (38-1-07) consomem `resolveAiCost` para gravar custo real por chamada; sem ele, a contabilidade permanece com fallback hardcoded e fontes confusas (furos 1/3/6).
Output: resolvedor de custo + serviço de preços + wrapper legado síncrono + barrel + testes 6.1 (10 cenários).
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-1-ai-cost-accounting/38-1-CONTEXT.md
@.planning/phases/38-1-ai-cost-accounting/38-1-PATTERNS.md
@openspec/changes/fase-38-1-ai-cost-accounting/specs/ai-cost-estimator/spec.md
@openspec/changes/fase-38-1-ai-cost-accounting/specs/ai-model-pricing/spec.md

<interfaces>
Contratos que o executor precisa (extraídos do spec ai-cost-estimator + ai-model-pricing):

**Sem UI (HAS_UI: 0). Nenhuma página/rota de UI é alterada.**

Contrato `resolveAiCost` (spec linhas 5-28, assinatura linha 8):
```typescript
// src/lib/ai-cost/cost-estimator.ts — ASYNC (lê pricing table)
export async function resolveAiCost(params: {
  provider: string;
  model: string;
  usage?: TokenUsage;                      // de ai-cost/types.ts
  providerReportedCostUsd?: number | null; // fonte 1 (D9)
}): Promise<CostResolution>                // CostResolution de ai-cost/types.ts
```
Resolução (ordem exata, spec linhas 21-29):
1. `providerReportedCostUsd` presente → `{ estimatedCostUsd, providerReportedCostUsd, costSource: "provider_reported" }`
2. Linha vigente em `ai_model_pricing` (via `getModelPricing`) → `pricing_table` com `pricingVersion: <uuid da linha>`; cálculo por tokens (prompt/output/cached) ou por `image_unit_usd` quando sem usage
3. Sem linha mas default em `DEFAULT_AI_MODEL_PRICING` → `pricing_table` com `pricingVersion: "code_default"` (bootstrap D8)
4. Senão `fallback_static` (env `VENDEO_AI_FALLBACK_COST_USD`, default 0.15; compat com `VENDEO_IMAGE_GENERATION_FALLBACK_COST_USD`)
5. Senão `{ estimatedCostUsd: null, costSource: "not_available" }` — tokens preservados (D4)
`normalizeModel` (strip de sufixo `-\d{4}-\d{2}-\d{2}`) mantido de `cost-estimator.ts:50-52`.

Contrato `getModelPricing` (novo — `src/lib/ai-cost/ai-model-pricing.ts`):
```typescript
import "server-only";
export const DEFAULT_AI_MODEL_PRICING: Record<string, ModelPricing>; // 7 seeds D8
export interface ModelPricing {
  inputCostUsd?: number; outputCostUsd?: number; cachedInputCostUsd?: number;
  imageUnitCostUsd?: number; imageTokenUsdPer1M?: number; // TODAS opcionais — modelo só de imagem (gpt-image-2/dall-e-3) tem apenas image_unit (D8)
}
export async function getModelPricing(params: { provider: string; model: string }):
  Promise<{ pricing: ModelPricing; versionId: string } | null>; // versionId = uuid da linha | "code_default"
```
- Mapper linha→`ModelPricing` valida **ao menos uma dimensão presente** (espelha CHECK `chk_ai_model_pricing_at_least_one_price`): se a linha vinda do banco tiver todas as 5 dimensões nulas → `null` (linha inválida — nunca retorna um `ModelPricing` vazio). A mesma validação se aplica aos defaults de código (`DEFAULT_AI_MODEL_PRICING`).
- Leitura: `client.from("ai_model_pricing").select("id, provider, model, input_token_usd_per_1m, output_token_usd_per_1m, cached_input_token_usd_per_1m, image_unit_usd, image_token_usd_per_1m").eq("provider", ...).eq("model", ...).is("effective_until", null).maybeSingle()` (client injetável no constructor para testes). NÃO existe coluna `is_current` — vigente = `effective_until IS NULL`.
- Se sem linha → procura em `DEFAULT_AI_MODEL_PRICING` (com `normalizeModel` strip de data) → `versionId: "code_default"`
- Bootstrap (espelha EXATAMENTE os 7 seeds da migration 38-1-01 — D8): gpt-4o {2.50, 10.00}, gpt-4o-mini {0.15, 0.60}, gpt-5.5 {5.00, cached 0.50, 30.00}, gpt-image-2 {image_unit 0.040}, dall-e-3 {image_unit 0.040}, gemini-2.0-flash {0.10, 0.40}, gemini-3.1-flash-lite {0.10, 0.40}. NÃO incluir modelos fora dos seeds (ex.: gpt-5.5-2026-04-23 não tem entrada própria — o normalizeModel na BUSCA resolve o sufixo para gpt-5.5).

Barrel `src/lib/ai-cost/index.ts` (estado atual: 2 linhas — só estimateAiCost):
- Substituir por re-exports: `resolveAiCost` + tipos (CostResolution, CostSource, TokenUsage, AiCallInfo, AiCostEvent, OperationRunType) de types.ts + `AiCostTracker` de tracker.ts + `getModelPricing`/`DEFAULT_AI_MODEL_PRICING` de ai-model-pricing.ts + `estimateAiCost`/`AiCostEstimate` LEGADO de `legacy-estimator.ts` (deprecated, SÍNCRONO — consumido ainda pela rota até 38-1-07)

Testes 6.1 (10 cenários nomeados, spec linhas 32-92):
1. usage + linha na tabela → pricing_table com uuid da linha (gpt-4o 1000/500 = 0.0075)
2. providerReportedCostUsd presente → provider_reported (0.02)
3. gpt-image-2 sem usage → pricing_table via image_unit_usd
4. gemini-3.1-flash-lite com usage → {0.10, 0.40}
5. gpt-4o sem usage → fallback_static (0.15) — NÃO null (furo 1)
6. modelo desconhecido sem usage → fallback_static
7. code_default — sem linha na tabela, default em código → pricing_table + "code_default"
8. not_available com tokens — sem linha e sem default → estimatedCostUsd null, costSource not_available
9. cached tokens gpt-5.5 (400 cached de 1000 prompt, cached 0.50)
10. manual_unknown (D4 — via uso interno/manual; testar caminho de source manual_unknown retornado quando o caller especifica)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Serviço de preços ai-model-pricing.ts (tabela + bootstrap D8)</name>
  <files>src/lib/ai-cost/ai-model-pricing.ts, src/lib/ai-cost/__tests__/ai-model-pricing.test.ts</files>
  <read_first>
    - src/lib/credit/credit-service.ts (constructor com SupabaseClient injetável — linhas 1-9)
    - src/lib/ai-cost/cost-estimator.ts (OPENAI_PRICING/GEMINI_PRICING atuais — linhas 12-22, base do DEFAULT_AI_MODEL_PRICING)
    - openspec/changes/fase-38-1-ai-cost-accounting/specs/ai-model-pricing/spec.md (linhas 1-50)
  </read_first>
  <behavior>
    - Teste 1: `getModelPricing({ provider: "openai", model: "gpt-4o" })` com linha vigente na tabela → retorna pricing + `versionId` = uuid da linha (não "code_default")
    - Teste 2: linha superseded (effective_until NOT NULL) NÃO retornada; `.is("effective_until", null)` presente na query
    - Teste 3: sem linha na tabela → bootstrap de `DEFAULT_AI_MODEL_PRICING` com `versionId: "code_default"` (gpt-4o {2.50, 10.00})
    - Teste 4: `gpt-image-2` sem linha → bootstrap com `imageUnitCostUsd: 0.040` e SEM `inputCostUsd`/`outputCostUsd` (modelo só de imagem — dimensões opcionais, D8)
    - Teste 5: sem linha e sem default → `null` (caminho not_available do resolvedor)
    - Teste 6: erro de query (supabase) → null + console.error (best-effort, nunca lança — padrão D7)
    - Teste 6b: linha do banco com TODAS as 5 dimensões nulas (violação defensiva do CHECK) → `null` (validação "ao menos uma dimensão" no mapper)
  </behavior>
  <action>
    Criar `src/lib/ai-cost/ai-model-pricing.ts`:
    1. `import "server-only";` + `ModelPricing` interface com TODAS as dimensões opcionais (`inputCostUsd?`, `outputCostUsd?`, `cachedInputCostUsd?`, `imageUnitCostUsd?`, `imageTokenUsdPer1M?`) + `DEFAULT_AI_MODEL_PRICING` const (bootstrap espelhando EXATAMENTE os 7 seeds da migration 38-1-01 — D8): gpt-4o {inputCostUsd 2.5, outputCostUsd 10.0}, gpt-4o-mini {0.15, 0.60}, gpt-5.5 {5.0, cachedInputCostUsd 0.5, 30.0}, gpt-image-2 {imageUnitCostUsd: 0.040}, dall-e-3 {imageUnitCostUsd: 0.040}, gemini-2.0-flash {0.10, 0.40}, gemini-3.1-flash-lite {0.10, 0.40}. Sem entrada para gpt-5.5-2026-04-23 (o normalizeModel resolve o sufixo na busca). Modelos só de imagem (gpt-image-2, dall-e-3) têm apenas `imageUnitCostUsd` — NÃO inventar `inputCostUsd`/`outputCostUsd` 0.
    2. `export class AiModelPricingService` com `constructor(private readonly client: SupabaseClient = supabaseAdmin)` e método `getModelPricing({ provider, model }): Promise<{ pricing: ModelPricing; versionId: string } | null>`:
       - Query: `client.from("ai_model_pricing").select("id, provider, model, input_token_usd_per_1m, output_token_usd_per_1m, cached_input_token_usd_per_1m, image_unit_usd, image_token_usd_per_1m").eq("provider", provider).eq("model", model).is("effective_until", null).maybeSingle()`
       - Linha encontrada → mapping snake→camel (input_token_usd_per_1m → inputCostUsd, etc.) + `versionId: linha.id`; **validação "ao menos uma dimensão"**: se todas as 5 dimensões mapeadas estiverem `undefined` → `null` (linha inválida — espelha CHECK chk_ai_model_pricing_at_least_one_price)
       - Sem linha → `DEFAULT_AI_MODEL_PRICING[normalizeModel(model)]` → `versionId: "code_default"`; se o default também estiver sem nenhuma dimensão → `null`
       - Erro/query error → console.error + null (best-effort, D7)
    3. `export async function getModelPricing(...)` = wrapper singleton (para o resolvedor importar diretamente; vi.mockável).
    Criar `src/lib/ai-cost/__tests__/ai-model-pricing.test.ts` (padrão vi.mock supabase/server + mockFrom + maybeSingle) com os 6 casos acima.
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai-cost/__tests__/ai-model-pricing.test.ts; npm run typecheck; `grep -c "code_default" src/lib/ai-cost/ai-model-pricing.ts` == 1; `grep -c "DEFAULT_AI_MODEL_PRICING" src/lib/ai-cost/ai-model-pricing.ts` >= 2; `grep -c "server-only" src/lib/ai-cost/ai-model-pricing.ts` == 1</automated>
  </verify>
  <acceptance_criteria>
    - getModelPricing: linha vigente (effective_until IS NULL) → uuid; sem linha → code_default; sem nada → null; nunca lança
    - Bootstrap com os 7 modelos dos seeds (incl. gemini-3.1-flash-lite, gpt-image-2 e dall-e-3 image_unit)
    - 7 testes verdes; typecheck limpo
  </acceptance_criteria>
  <done>Serviço de preços pronto (tabela + bootstrap code_default)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: resolveAiCost — resolvedor nunca-null (D9, furo 1 sanado)</name>
  <files>src/lib/ai-cost/cost-estimator.ts, src/lib/ai-cost/legacy-estimator.ts, src/lib/ai-cost/__tests__/cost-estimator.test.ts, src/lib/ai-cost/__tests__/legacy-estimator.test.ts</files>
  <read_first>
    - src/lib/ai-cost/cost-estimator.ts (atual — 111 linhas: estruturas de pricing a MOVER para legado, normalizeModel a MANTER)
    - src/lib/ai-cost/types.ts (CostResolution, CostSource, TokenUsage — criado em 38-1-02)
    - openspec/changes/fase-38-1-ai-cost-accounting/specs/ai-cost-estimator/spec.md (linhas 5-92 — contrato e cenários)
  </read_first>
  <behavior>
    - Teste 7: `resolveAiCost({ provider: "openai", model: "gpt-4o", usage: { promptTokens: 1000, completionTokens: 500 } })` com linha na tabela → `{ estimatedCostUsd: 0.0075, costSource: "pricing_table", pricingVersion: "<uuid>" }`
    - Teste 8: com `providerReportedCostUsd: 0.02` → `{ costSource: "provider_reported", providerReportedCostUsd: 0.02 }`
    - Teste 9: `gpt-image-2` sem usage → pricing_table via image_unit_usd (0.040)
    - Teste 10: `gemini-3.1-flash-lite` com usage {1000, 200} → 0.00018 (0.10/1M + 0.40/1M)
    - Teste 11: `gpt-4o` sem usage (sem linha) → fallback_static 0.15 — NUNCA null
    - Teste 12: modelo desconhecido → fallback_static
    - Teste 13: code_default — linha ausente, default presente → pricing_table + "code_default"
    - Teste 14: not_available — sem linha e sem default → `{ estimatedCostUsd: null, costSource: "not_available" }`
    - Teste 15: cached gpt-5.5 (1000 prompt, 400 cached, 200 output, cached 0.50) → 0.0050 + 0.0002 + 0.006 = 0.0112
    - Teste 16: manual_unknown — caminho de source manual_unknown (D4) presente no contrato e alcançável
  </behavior>
  <action>
    Refatorar `src/lib/ai-cost/cost-estimator.ts`:
    1. MOVER o wrapper legado para `src/lib/ai-cost/legacy-estimator.ts` (novo arquivo): `AiCostEstimate`, `PricingTier`, `OPENAI_PRICING`, `GEMINI_PRICING`, `getFallbackCost` (leitura `VENDEO_IMAGE_GENERATION_FALLBACK_COST_USD`, default 0.15 — comportamento atual preservado), `isKnownTextModel`/`KNOWN_TEXT_MODELS` e `estimateAiCost` — **síncrono**, `@deprecated`, com fallback/defaults locais (NÃO chama `resolveAiCost`, que é async). A rota generate-image (linhas 593/614) continua chamando `estimateAiCost` de forma síncrona e compilando até 38-1-07; o barrel `index.ts` re-exporta o wrapper legado. Adicionar teste legado (`legacy-estimator.test.ts`) com os casos atuais do contrato legado (usage + linha, fallback, null para texto conhecido sem usage) para travar o comportamento do wrapper até ser removido.
    2. MANTER em `cost-estimator.ts`: `normalizeModel` (linhas 50-52) e `getFallbackCost` (novo — lê `VENDEO_AI_FALLBACK_COST_USD`, default 0.15, com compat retroativa `VENDEO_IMAGE_GENERATION_FALLBACK_COST_USD`; o legado em `legacy-estimator.ts` mantém a própria cópia lendo só `VENDEO_IMAGE_GENERATION_FALLBACK_COST_USD` — os dois não se misturam).
    3. NOVO `resolveAiCost` (async) em `cost-estimator.ts` seguindo a ordem exata do spec (provider_reported → pricing_table via `getModelPricing` → fallback_static → not_available):
       - Cálculo por tokens — **aplicar `?? 0` ANTES de multiplicar** (dimensões opcionais — ModelPricing pode ter só imageUnitCostUsd): `((uncachedPrompt/1M) * (inputCostUsd ?? 0)) + ((cachedInput/1M) * (cachedInputCostUsd ?? 0)) + ((completion/1M) * (outputCostUsd ?? 0)) + ((imageTokens/1M) * (imageTokenUsdPer1M ?? 0))`; `Number(cost.toFixed(6))`. NUNCA escrever `X/1M * inputCostUsd ?? 0` (aplica o default só depois da multiplicação → NaN quando a dimensão está ausente).
       - Sem usage com `imageUnitCostUsd` → `{ estimatedCostUsd: imageUnitCostUsd, costSource: "pricing_table" }`
       - `fallback_static`: `getFallbackCost()` (env novo `VENDEO_AI_FALLBACK_COST_USD` com compat retroativa — testada)
       - `not_available`: `{ estimatedCostUsd: null, costSource: "not_available" }`
       - Retorno tipado como `CostResolution` (de types.ts — 38-1-02)
    4. NUNCA retornar null no fluxo de resolução do `resolveAiCost` (furo 1): o `cost-estimator.ts` resultante NÃO tem `return null` (o wrapper legado com `return null` fica isolado em `legacy-estimator.ts`).
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai-cost/__tests__/cost-estimator.test.ts src/lib/ai-cost/__tests__/legacy-estimator.test.ts; npm run typecheck; `grep -c "return null" src/lib/ai-cost/cost-estimator.ts` == 0; `grep -c "estimateAiCost" src/lib/ai-cost/legacy-estimator.ts` >= 1; `grep -c "resolveAiCost" src/lib/ai-cost/cost-estimator.ts` >= 2; `grep -c "not_available" src/lib/ai-cost/cost-estimator.ts` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - resolveAiCost implementa a ordem D9; nunca retorna null
    - estimateAiCost legado mantido síncrono em legacy-estimator.ts (@deprecated) para a rota até 38-1-07; barrel re-exporta
    - 10 cenários 6.1 verdes + testes do wrapper legado verdes; typecheck limpo
  </acceptance_criteria>
  <done>Resolvedor de custo definitivo em vigor (nunca-null, 4 fontes) + wrapper legado síncrono isolado</done>
</task>

<task type="auto">
  <name>Task 3: Barrel index.ts + suíte 6.1 completa</name>
  <files>src/lib/ai-cost/index.ts, src/lib/ai-cost/__tests__/cost-estimator.test.ts, src/lib/ai-cost/__tests__/ai-model-pricing.test.ts, src/lib/ai-cost/__tests__/legacy-estimator.test.ts</files>
  <read_first>
    - src/lib/ai-cost/index.ts (atual — 2 linhas)
    - src/lib/ai-cost/__tests__/cost-estimator.test.ts (atual — 20 testes do contrato legado a adaptar)
    - openspec/changes/fase-38-1-ai-cost-accounting/specs/ai-cost-estimator/spec.md (linhas 80-92 — adaptação dos testes legados)
  </read_first>
  <action>
    1. Reescrever `src/lib/ai-cost/index.ts`:
       - `export { resolveAiCost } from "./cost-estimator";` + `export { estimateAiCost } from "./legacy-estimator";` (wrapper legado SÍNCRONO deprecated, docblock `@deprecated Use resolveAiCost — removido em 38-1-07`)
       - `export type { CostResolution, CostSource, TokenUsage, AiCallInfo, AiCostEvent, OperationRunType } from "./types";` + `export { COST_SOURCES, OPERATION_RUN_TYPES } from "./types";`
       - `export { AiCostTracker } from "./tracker";`
       - `export { AiModelPricingService, getModelPricing, DEFAULT_AI_MODEL_PRICING } from "./ai-model-pricing";` + `export type { ModelPricing } from "./ai-model-pricing";`
    2. Finalizar `src/lib/ai-cost/__tests__/cost-estimator.test.ts` (escrita principal na Task 2): adaptar/remover os casos legados conflitantes ("returns null for text model without usage" → agora fallback_static; "unknown providers" → fallback_static/not_available) e garantir os 10 cenários 6.1 nomeados. O arquivo deve ter >= 10 testes com os nomes dos cenários do spec (vi.mock de `@/lib/ai-cost/ai-model-pricing` para os casos pricing_table/code_default — retorno mockado com uuid estável). Os testes do comportamento do wrapper legado vão para `legacy-estimator.test.ts` (criado na Task 2).
    3. Rodar suíte completa: `npx vitest run src/lib/ai-cost/__tests__/` — deve passar (ai-model-pricing + cost-estimator + legacy-estimator).
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai-cost/__tests__/; npm run typecheck; `grep -c "export" src/lib/ai-cost/index.ts` >= 6; `grep -c "@deprecated" src/lib/ai-cost/legacy-estimator.ts` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - Barrel exporta resolvedor + tracker + pricing + tipos + wrapper legado (>= 6 exports)
    - 10 cenários 6.1 + 7 testes pricing + testes wrapper legado verdes (0 falhas)
    - estimateAiCost marcado deprecated (legacy-estimator.ts); typecheck limpo
  </acceptance_criteria>
  <done>Barrel atualizado + suíte 6.1 (10 testes) e pricing verdes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resolveAiCost → getModelPricing → ai_model_pricing | Leitura de preços via supabaseAdmin (server-only) |
| cost-estimator.ts → consumidores (serviços onCall) | Contrato de custo — nunca null, cost_source fiel |
| ai-model-pricing.ts → DEFAULT_AI_MODEL_PRICING | Bootstrap em código — fonte de verdade quando a tabela não tem linha |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38.1-17 | Tampering | Preços em código divergindo da tabela (furo 3/6) | mitigate | Bootstrap `DEFAULT_AI_MODEL_PRICING` espelha os seeds da migration (38-1-01); `code_default` distinguível via pricingVersion — F38.1-08 |
| T-38.1-18 | Spoofing | cost_source incorreto enganando a contabilidade | mitigate | Ordem rígida D9 testada (10 cenários 6.1); `never null` garante fonte explícita sempre — F38.1-05 |
| T-38.1-19 | DoS | Query de pricing por chamada (N chamadas × serviço) | mitigate | Leitura curta com índice parcial único uq_ai_model_pricing_vigente (provider, model) WHERE effective_until IS NULL (38-1-01); fallback code_default sem query extra quando sem linha — F38.1-07 |
| T-38.1-20 | Tampering | Env fallback inválido | mitigate | Parse numérico com validação (isNaN/positivo) + default 0.15; compat VENDEO_IMAGE_GENERATION_FALLBACK_COST_USD — F38.1-05 |
| T-38.1-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado neste plano — gate de legitimidade não se aplica |
</threat_model>

<verification>
- `npx vitest run src/lib/ai-cost/__tests__/` — 17 testes (10 de 6.1 + 7 pricing) + testes do wrapper legado, 0 falhas
- `npm run typecheck` limpo
- `grep -c "return null" cost-estimator.ts` == 0 (furo 1 sanado — o `return null` legado ficou isolado em legacy-estimator.ts)
- Barrel com >= 6 exports; estimateAiCost deprecated (legacy-estimator.ts)
</verification>

<success_criteria>
- resolveAiCost implementa a precedência D9 e nunca retorna null
- Preços da tabela com bootstrap code_default (D8); gpt-image-2 e gemini-3.1-flash-lite corretos (dimensões opcionais — modelo só de imagem não inventa 0)
- Cached tokens e not_available (tokens preservados, custo null) funcionais
- estimateAiCost legado síncrono isolado em legacy-estimator.ts (rota compila até 38-1-07)
- 10 cenários 6.1 verdes; barrel pronto para os serviços onCall
</success_criteria>

<output>
Create `.planning/phases/38-1-ai-cost-accounting/38-1-04-SUMMARY.md` when done
</output>

---
phase: 38-1-ai-cost-accounting
plan: 05
type: execute
wave: 3
depends_on:
  - "38-1-02"
files_modified:
  - "src/lib/image-generation/metrics/types.ts"
  - "src/lib/image-generation/services/image-generation-service.ts"
  - "src/lib/image-generation/services/input-validation-service.ts"
  - "src/lib/image-generation/services/image-review-service.ts"
  - "src/lib/copy/copy-director-service.ts"
  - "src/lib/copy/__tests__/copy-director-service.test.ts"
  - "src/lib/image-generation/services/__tests__/image-generation-service.test.ts"
  - "src/lib/image-generation/services/__tests__/image-review-service.test.ts"
autonomous: true
HAS_UI: 0
requirements:
  - F38.1-30
  - F38.1-31
  - F38.1-32
  - F38.1-33
must_haves:
  truths:
    - "GenerationMetricsEvent estendido com usage (TokenUsage) e durationMs por evento (D11)"
    - "generateImage emite usage + durationMs por tentativa nas fases input_validation, image_generation e quality_review — via canal ÚNICO onMetricsEvent (sem dupla contagem)"
    - "InputValidationService e ImageReviewService capturam usage + durationMs de cada chamada OpenAI e expõem via callback onCall (nunca bloqueiam — best-effort D7)"
    - "CopyDirectorService.generateCopy expõe callback onCall opcional com usage real + durationMs (furo 1: copy com custo real)"
    - "Nenhum caminho de erro lança por causa de telemetria (onCall nunca quebra a geração)"
    - "Testes adaptados: copy-director-service (onCall com usage), image-generation-service (eventos com usage/durationMs por tentativa), image-review-service (usage capturado)"
  artifacts:
    - path: "src/lib/image-generation/metrics/types.ts"
      provides: "Contrato de métricas por evento com usage + durationMs (D11)"
      exports: ["GenerationMetricsEvent"]
    - path: "src/lib/image-generation/services/image-generation-service.ts"
      provides: "generateImage emitindo usage/durationMs por tentativa"
      exports: ["generateImage"]
    - path: "src/lib/copy/copy-director-service.ts"
      provides: "generateCopy com onCall (usage real)"
      exports: ["CopyDirectorService"]
  key_links:
    - from: "src/lib/image-generation/services/image-generation-service.ts"
      to: "src/lib/image-generation/metrics/types.ts"
      via: "emitMetricsEvent agora com usage + durationMs"
      pattern: "onMetricsEvent"
    - from: "src/lib/image-generation/services/{input-validation,image-review}-service.ts"
      to: "src/lib/ai-cost/types.ts (AiCallInfo)"
      via: "callback onCall após cada chamada de provider"
      pattern: "onCall"
    - from: "src/lib/copy/copy-director-service.ts"
      to: "src/lib/ai-cost/types.ts (AiCallInfo)"
      via: "onCall com usage de TextProviderResult"
      pattern: "onCall"
---

<objective>
Instrumentar os serviços de campanha para emitir dados de chamada IA (`AiCallInfo`: provider, model, usage, durationMs) via callback `onCall` opcional (padrão NO ANALOG #3 — telemetria por callback, best-effort) e estender o `GenerationMetricsEvent` com `usage` + `durationMs` por tentativa (D11). Serviços tocados: `ImageGenerationService.generateImage` (fases input_validation/image_generation/quality_review), `InputValidationService`, `ImageReviewService`, `CopyDirectorService.generateCopy`.

Purpose: sem esses dados, o pipeline (38-1-07) não consegue gravar custo real por chamada no tracker — `campaign_copy`, `campaign_input_validation`, `campaign_image` e `campaign_image_review` ficariam com custo ausente (furos 1/4/6/7). Os serviços APENAS emitem; a resolução de custo + gravação acontece na rota (38-1-07).
Output: serviços de campanha emitindo AiCallInfo; contrato de métricas com usage/durationMs.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-1-ai-cost-accounting/38-1-CONTEXT.md
@.planning/phases/38-1-ai-cost-accounting/38-1-PATTERNS.md
@openspec/changes/fase-38-1-ai-cost-accounting/specs/ai-image-generation/spec.md
@openspec/changes/fase-38-1-ai-cost-accounting/specs/copy-director/spec.md
@openspec/changes/fase-38-1-ai-cost-accounting/specs/image-quality-review/spec.md
@openspec/changes/fase-38-1-ai-cost-accounting/specs/validation-review-alignment/spec.md

<interfaces>
Contratos que o executor precisa (extraídos dos specs + PATTERNS + leitura dos arquivos):

**Sem UI (HAS_UI: 0). Nenhuma página/rota de UI é alterada.**

`AiCallInfo` (de `src/lib/ai-cost/types.ts` — criado em 38-1-02):
```typescript
export interface AiCallInfo {
  provider: string; model: string;
  usage?: TokenUsage;            // { promptTokens?, completionTokens?, totalTokens?, cachedInputTokens?, imageTokens? }
  durationMs: number;
  providerReportedCostUsd?: number | null;
}
```

Estado atual (verificado):
- `GenerationMetricsEvent` (`src/lib/image-generation/metrics/types.ts:36-44`): `{ runId, phase, provider, model, elapsedMs, attempt, estimatedCostUsd? }` — FALTA `usage` e `durationMs` (D11)
- `ImageGenerationService.generateImage` (`image-generation-service.ts:88-92`): já tem `onMetricsEvent?: (event: GenerationMetricsEvent) => void` e `emitMetricsEvent(phase, attempt)` helper (linhas 99-108); validação (245-249), image_generation (317/349) e quality_review (353/450/458/502) emitem via esse helper
- `InputValidationService.validate` (`input-validation-service.ts:38-95`): chama `openai.chat.completions.create` com `this.model` (VISION_REVIEW_MODEL) — `response.usage` disponível
- `ImageReviewService.review` (`image-review-service.ts:48-56`): mesma chamada com `max_tokens: 1000` + `response_format: json_object` — `response.usage` disponível
- `CopyDirectorService.generateCopy` (`copy-director-service.ts:59-98`): `this.provider.generateText(prompt, textOpts)` retorna `TextProviderResult { content, usage?: { promptTokens, completionTokens }, model }` — usage JÁ existe no resultado (furo 1: só não é exposto)

Padrão de callback (NO ANALOG #3 — onCall por callback, documentado em PATTERNS):
```typescript
onCall?: (info: AiCallInfo) => void | Promise<void>;  // opcional; best-effort; nunca lança
```
- O serviço mede `durationMs = Date.now() - startTime` em volta da chamada de provider
- Captura `usage` da resposta do provider quando existir (vision: `response.usage` — prompt_tokens/completion_tokens/total_tokens; copy: `result.usage`)
- Invoca `onCall({ provider: "openai", model, usage, durationMs })` DEPOIS da chamada, em try/catch silencioso (falha de callback NUNCA quebra a geração — D7)
- Retorna o resultado normalmente; NÃO altera o retorno público dos métodos
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GenerationMetricsEvent + usage/durationMs (D11) e onCall nos serviços de visão</name>
  <files>src/lib/image-generation/metrics/types.ts, src/lib/image-generation/services/input-validation-service.ts, src/lib/image-generation/services/image-review-service.ts, src/lib/image-generation/services/__tests__/image-review-service.test.ts</files>
  <read_first>
    - src/lib/image-generation/metrics/types.ts (36-44 — contrato atual do evento)
    - src/lib/image-generation/services/input-validation-service.ts (38-95 — chamada vision sem captura de usage)
    - src/lib/image-generation/services/image-review-service.ts (48-56, 183-220 — review + callVisionModel)
    - src/lib/ai-cost/types.ts (AiCallInfo — criado em 38-1-02)
  </read_first>
  <behavior>
    - Teste 1 (compile-time): `GenerationMetricsEvent` aceita `usage` (TokenUsage) e exige `durationMs`
    - Teste 2: `InputValidationService.validate` com OpenAI mockado retornando usage → `onCall` invocado com `{ provider: "openai", model: this.model, usage, durationMs >= 0 }`
    - Teste 3: `onCall` que LANÇA → validate continua e resolve normalmente (best-effort D7)
    - Teste 4: `ImageReviewService.review` com usage mockado → `onCall` com usage + durationMs
    - Teste 5: sem `onCall` (undefined) → comportamento idêntico ao atual (retorno inalterado)
  </behavior>
  <action>
    1. Em `src/lib/image-generation/metrics/types.ts`: estender `GenerationMetricsEvent` com `usage?: TokenUsage` (import de `@/lib/ai-cost/types`) e `durationMs: number` (obrigatório — D11). Não alterar `GenerationMetrics` (o resumo final fica como está).
    2. Em `src/lib/image-generation/services/input-validation-service.ts`: adicionar parâmetro `onCall?: (info: AiCallInfo) => void | Promise<void>` ao método `validate` (último parâmetro opcional); medir `const startTime = Date.now()` antes de `callVisionModel`; capturar usage de `response.usage` (retornar usage junto do content de `callVisionModel` — ex.: `{ content, usage }`); após a chamada, invocar `onCall` em try/catch silencioso (console.error + seguir). `provider: "openai"`, `model: this.model`. Import `AiCallInfo` de `@/lib/ai-cost/types`.
    3. Em `src/lib/image-generation/services/image-review-service.ts`: mesmo padrão em `review` (parâmetro `onCall?` opcional; capturar usage em `callVisionModel`; invocar onCall best-effort após a chamada).
    4. Em `src/lib/image-generation/services/__tests__/image-review-service.test.ts`: adaptar/estender com os casos 4-5 (vi.mock do dynamic import `openai` — verificar padrão de mock já existente no arquivo; usage mockado em `response.usage`).
    NÃO alterar assinaturas públicas existentes além do parâmetro opcional; NÃO mudar prompts/lógica de parse.
  </action>
  <verify>
    <automated>npx vitest run src/lib/image-generation/services/__tests__/image-review-service.test.ts; npm run typecheck; `grep -c "durationMs" src/lib/image-generation/metrics/types.ts` == 1; `grep -c "onCall" src/lib/image-generation/services/input-validation-service.ts` >= 1; `grep -c "onCall" src/lib/image-generation/services/image-review-service.ts` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - GenerationMetricsEvent com usage + durationMs (D11)
    - InputValidationService e ImageReviewService emitem onCall (usage + durationMs) sem bloquear
    - Testes 2-5 verdes; typecheck limpo
  </acceptance_criteria>
  <done>Serviços de visão instrumentados (onCall best-effort) + contrato de métricas D11</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: generateImage — usage + durationMs por tentativa via onMetricsEvent</name>
  <files>src/lib/image-generation/services/image-generation-service.ts, src/lib/image-generation/services/__tests__/image-generation-service.test.ts</files>
  <read_first>
    - src/lib/image-generation/services/image-generation-service.ts (88-108 helper emitMetricsEvent; 242-353 fases; 930-960 generateWithRetry)
    - src/lib/image-generation/metrics/types.ts (após Task 1 — contrato novo do evento)
    - src/lib/image-generation/services/__tests__/image-generation-service.test.ts (padrão de teste atual do serviço)
  </read_first>
  <behavior>
    - Teste 6: generateImage com onMetricsEvent → evento `image_generation` por tentativa com `usage` + `durationMs` preenchidos (attempt 0..n)
    - Teste 7: evento `input_validation` com usage (quando a validação faz chamada de visão)
    - Teste 8: evento `quality_review` com usage por tentativa (attempt 1..n)
    - Teste 9: `onMetricsEvent` lançando → generateImage continua (best-effort — o helper já tem guard `if (onMetricsEvent)`, envolver a invocação em try/catch)
    - Teste 10: sem onMetricsEvent → comportamento idêntico ao atual (compat)
  </behavior>
  <action>
    Em `src/lib/image-generation/services/image-generation-service.ts`:
    1. No helper `emitMetricsEvent` (linhas 99-108): adicionar campos `usage?: TokenUsage` e `durationMs` (medido como `Date.now() - <início da fase>`; o serviço já tem `startTime` global — usar elapsedMs existente como base para durationMs de fase ou medir por ponto de emissão; escolha documentada no código); envolver a invocação de `onMetricsEvent` em try/catch silencioso (best-effort).
    2. Thread de usage: para as fases internas chamadas DENTRO de generateImage (input_validation via InputValidationService, quality_review via ImageReviewService), passar um `onCall` interno que captura o usage/durationMs e o repassa ao `emitMetricsEvent` da fase correspondente. Para image_generation: capturar `usage` do retorno do provider (`ImageProviderOutput["usage"]` — já existe `currentUsage` na linha 274; preencher o evento da tentativa).
    3. `generateWithRetry` (938-960): garantir que o retorno inclui usage por tentativa (se já houver, expor no evento; se não, propagar do provider output).
    4. NÃO mudar o fluxo de geração/review/estado; NÃO duplicar eventos (cada fase emite 1 vez por tentativa).
    Em `src/lib/image-generation/services/__tests__/image-generation-service.test.ts`: adaptar os testes existentes para o contrato novo (durationMs obrigatório no tipo) e adicionar os casos 6-10.
  </action>
  <verify>
    <automated>npx vitest run src/lib/image-generation/services/__tests__/image-generation-service.test.ts; npm run typecheck; `grep -c "durationMs" src/lib/image-generation/services/image-generation-service.ts` >= 2; `grep -c "usage" src/lib/image-generation/services/image-generation-service.ts` >= 3</automated>
  </verify>
  <acceptance_criteria>
    - Eventos por tentativa com usage + durationMs nas 3 fases (D11)
    - onMetricsEvent nunca quebra a geração (try/catch no helper)
    - Suíte adaptada verde; typecheck limpo
  </acceptance_criteria>
  <done>generateImage emite telemetria de custo por tentativa (usage + durationMs)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CopyDirectorService.generateCopy — onCall com usage real (furo 1)</name>
  <files>src/lib/copy/copy-director-service.ts, src/lib/copy/__tests__/copy-director-service.test.ts</files>
  <read_first>
    - src/lib/copy/copy-director-service.ts (59-98 — generateCopy; result tem usage)
    - src/lib/copy/__tests__/copy-director-service.test.ts (152-180 — mocks generateText já retornam usage)
    - src/lib/text-provider/types.ts (8-14 — TextProviderResult com usage)
  </read_first>
  <behavior>
    - Teste 11: generateCopy com onCall → invocado com `{ provider: <nome do provider>, model: <model do resultado>, usage: { promptTokens, completionTokens }, durationMs >= 0 }`
    - Teste 12: onCall lançando → generateCopy continua (best-effort) — copy ainda gerada normalmente
    - Teste 13: sem onCall → comportamento inalterado (testes existentes continuam passando)
  </behavior>
  <action>
    Em `src/lib/copy/copy-director-service.ts`:
    1. Adicionar parâmetro opcional `onCall?: (info: AiCallInfo) => void | Promise<void>` a `generateCopy` (após `options`).
    2. Medir `durationMs` em volta de `this.provider.generateText(prompt, textOpts)` (linha 95).
    3. Após o retorno: `const info: AiCallInfo = { provider: <derivado do provider>, model: result.model, usage: result.usage, durationMs }`. Para `provider`: adicionar ao construtor um campo `providerName` (default "openai"; as implementações atuais são openai/gemini — verificar como o provider é injetado nos testes; usar o valor que os mocks expõem ou "openai" default).
    4. Invocar `onCall(info)` em try/catch silencioso (console.error + seguir — D7).
    5. NÃO alterar o retorno público nem a parse de conteúdo.
    Em `src/lib/copy/__tests__/copy-director-service.test.ts`: adicionar os casos 11-12 usando um spy de onCall (os mocks de generateText já retornam usage+model — linhas 152-180); rodar suíte existente (deve continuar verde — caso 13).
  </action>
  <verify>
    <automated>npx vitest run src/lib/copy/__tests__/copy-director-service.test.ts; npm run typecheck; `grep -c "onCall" src/lib/copy/copy-director-service.ts` >= 2; `grep -c "durationMs" src/lib/copy/copy-director-service.ts` == 1</automated>
  </verify>
  <acceptance_criteria>
    - generateCopy emite AiCallInfo (usage real + durationMs) sem quebrar o fluxo
    - Suíte copy-director verde (testes novos + existentes)
    - typecheck limpo
  </acceptance_criteria>
  <done>Copy com custo real exposto via onCall (furo 1 sanado na camada de serviço)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Serviço (copy/validation/review/image-gen) → onCall callback | Dados de chamada IA atravessam aqui — callback best-effort, nunca bloqueia |
| generateImage → onMetricsEvent | Telemetria por tentativa — canal único, sem dupla contagem |
| AiCallInfo → rota (38-1-07) | Consumida pela rota para resolveAiCost + tracker.record |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38.1-21 | DoS | onCall/onMetricsEvent lançando em produção | mitigate | try/catch silencioso em toda invocação de callback + guard de opcionalidade (D7) — F38.1-30 |
| T-38.1-22 | Tampering | Dupla contagem de custo (evento duplicado por chamada) | mitigate | Canal único: onMetricsEvent é a única via do generateImage; onCall interno repassa usage SEM registrar; gravação só na rota — F38.1-30 |
| T-38.1-23 | Information Disclosure | usage/tokens vazando para log | mitigate | onCall transmite apenas dados de telemetria (tokens/custos), sem prompt/copy/image; logs de erro limitados (console.error sem payload) — F38.1-31 |
| T-38.1-24 | Spoofing | usage falso/fabricado | mitigate | Captura direta da resposta do provider (response.usage / TextProviderResult.usage); sem cálculo no serviço — F38.1-32 |
| T-38.1-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado neste plano — gate de legitimidade não se aplica |
</threat_model>

<verification>
- `npx vitest run src/lib/copy/__tests__/ src/lib/image-generation/services/__tests__/` — 0 falhas
- `npm run typecheck` limpo
- Greps: durationMs em metrics/types.ts; onCall nos 3 serviços de campanha
</verification>

<success_criteria>
- GenerationMetricsEvent com usage + durationMs (D11)
- copy/validation/review/image-gen emitem AiCallInfo (best-effort, nunca bloqueiam)
- generateImage emite por tentativa (input_validation, image_generation, quality_review) via canal único
- Sem alteração de retorno público dos serviços
</success_criteria>

<output>
Create `.planning/phases/38-1-ai-cost-accounting/38-1-05-SUMMARY.md` when done
</output>

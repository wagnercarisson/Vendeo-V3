---
phase: 38-1-ai-cost-accounting
plan: 06
type: execute
wave: 3
depends_on:
  - "38-1-02"
files_modified:
  - "src/lib/visual-signature/ai-image-generator.ts"
  - "src/lib/visual-signature/brand-profiler.ts"
  - "src/lib/visual-signature/__tests__/brand-profiler.test.ts"
autonomous: true
HAS_UI: 0
requirements:
  - F38.1-25
  - F38.1-26
  - F38.1-27
  - F38.1-28
  - F38.1-29
  - F38.1-34
  - F38.1-35
  - F38.1-36
  - F38.1-37
  - F38.1-38
must_haves:
  truths:
    - "AiImageGenerator.generate aceita onCall opcional e emite { provider: 'openai', model, usage, durationMs } após a chamada responses.create (best-effort)"
    - "BrandProfiler emite onCall para cada chamada de visão/texto (brand_profile_vision e brand_profile_text mapeados na rota 38-1-09) — instrumentação from-zero (NO ANALOG #4)"
    - "Caminhos sem chamada IA (mockGenerate sem OPENAI_API_KEY, cache reuse, typographic fallback) NÃO emitem eventos (6.4/6.5: delivery sem evento)"
    - "Falha no callback onCall nunca quebra profiling/geração (best-effort D7)"
    - "Nenhuma mudança no retorno público dos serviços (compat F37 — err.path com elapsedMs preservados)"
  artifacts:
    - path: "src/lib/visual-signature/ai-image-generator.ts"
      provides: "generate com onCall (usage do Responses API)"
      exports: ["AiImageGenerator"]
    - path: "src/lib/visual-signature/brand-profiler.ts"
      provides: "generate com onCall (visão + texto)"
      exports: ["BrandProfiler"]
    - path: "src/lib/visual-signature/__tests__/brand-profiler.test.ts"
      provides: "Testes de onCall (visão/texto/sem-evento)"
      contains: "onCall"
  key_links:
    - from: "src/lib/visual-signature/ai-image-generator.ts"
      to: "src/lib/ai-cost/types.ts (AiCallInfo)"
      via: "onCall após responses.create"
      pattern: "onCall"
    - from: "src/lib/visual-signature/brand-profiler.ts"
      to: "src/lib/ai-cost/types.ts (AiCallInfo)"
      via: "onCall nas chamadas de visão/texto"
      pattern: "onCall"
---

<objective>
Instrumentar os serviços de assinatura visual e brand profile para emitir dados de chamada IA (`AiCallInfo`) via callback `onCall` opcional best-effort (padrão NO ANALOG #3/#4 — telemetria por callback; brand-profiler é instrumentação from-zero). Serviços: `AiImageGenerator.generate` (`visual_signature_image`) e `BrandProfiler.generate` (`brand_profile_vision` + `brand_profile_text`).

Purpose: as rotas VS (38-1-08) e brand (38-1-09) precisam desses dados para gravar custo por chamada com `generation_type` correto (D5). Sem isso, VS e brand-profile permanecem sem contabilidade de custo real (furo 5 e escopo brand).
Output: serviços VS/brand emitindo AiCallInfo (visão + texto + imagem) sem quebrar compat F37.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-1-ai-cost-accounting/38-1-CONTEXT.md
@.planning/phases/38-1-ai-cost-accounting/38-1-PATTERNS.md
@openspec/changes/fase-38-1-ai-cost-accounting/specs/store-visual-signature/spec.md
@openspec/changes/fase-38-1-ai-cost-accounting/specs/store-brand-profiler-without-logo/spec.md
@openspec/changes/fase-38-1-ai-cost-accounting/specs/text-only-brand-inference/spec.md
@openspec/changes/fase-38-1-ai-cost-accounting/specs/brand-director-prompt/spec.md

<interfaces>
Contratos que o executor precisa (extraídos dos specs + PATTERNS + leitura dos arquivos):

**Sem UI (HAS_UI: 0). Nenhuma página/rota de UI é alterada.**

`AiCallInfo` (de `src/lib/ai-cost/types.ts` — criado em 38-1-02):
```typescript
export interface AiCallInfo {
  provider: string; model: string;
  usage?: TokenUsage;   // { promptTokens?, completionTokens?, totalTokens?, cachedInputTokens?, imageTokens? }
  durationMs: number;
  providerReportedCostUsd?: number | null;
}
```

Estado atual (verificado):
- `AiImageGenerator.generate` (`ai-image-generator.ts:118-128`): `openai.responses.create({ model: IMAGE_GENERATION_RESPONSES_MODEL || "gpt-5.5", ... }, { signal, timeout })` (linhas 173-193); `const startTime = Date.now()` já existe (linha 129); `response.usage` disponível no Responses API (input_tokens/output_tokens/total_tokens)
- `BrandProfiler.generate` (`brand-profiler.ts:232-301`): paths `generateWithIntendedPalette` (linha 285, path 1 com paleta — visão) e `generateWithFallback` (linha 300, path 2 — visão + texto); `mockGenerate` (303+) usado quando SEM OPENAI_API_KEY em dev — NÃO deve emitir onCall; cache reuse (linhas 248-257) retorna cedo — sem chamada, sem evento
- Compat F37: `BrandProfilerWithoutLogoError` já carrega `{ provider, model, elapsedMs, errorType }` (linhas 276-280) — NÃO alterar esses caminhos de erro
- Testes atuais: `src/lib/visual-signature/__tests__/brand-profiler.test.ts` (mocka OpenAI SDK via vi.mock — padrão a reutilizar); NÃO existe teste para ai-image-generator (a verificação funcional do onCall do ai-image-generator acontece na rota VS — plano 38-1-08, teste 6.4)

Padrão de callback (idêntico ao 38-1-05 — NO ANALOG #3, documentado em PATTERNS):
```typescript
onCall?: (info: AiCallInfo) => void | Promise<void>;  // opcional; best-effort; nunca lança
```
- `durationMs = Date.now() - startTime` em volta de cada chamada de provider
- `usage` da resposta quando existir (vision/chat: `response.usage`; images: `response.usage` quando presente)
- `onCall` invocado em try/catch silencioso (console.error + seguir) — D7
- Retorno público inalterado
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AiImageGenerator.generate — onCall com usage do Responses API</name>
  <files>src/lib/visual-signature/ai-image-generator.ts</files>
  <read_first>
    - src/lib/visual-signature/ai-image-generator.ts (118-233 — generate + responses.create; startTime na 129; response.usage disponível)
    - src/lib/ai-cost/types.ts (AiCallInfo)
  </read_first>
  <behavior>
    - Teste 1 (compile-time): `generate` aceita `onCall?: (info: AiCallInfo) => void | Promise<void>` sem quebrar a assinatura
    - (verificação funcional do onCall na rota VS — 38-1-08, teste 6.4: "visual_signature_image + validation com custo/tokens")
  </behavior>
  <action>
    Em `src/lib/visual-signature/ai-image-generator.ts`:
    1. Adicionar `onCall?: (info: AiCallInfo) => void | Promise<void>` ao tipo de `params` de `generate` (opcional, último campo).
    2. `startTime` já existe (linha 129). Após `openai.responses.create` (linha 193): montar `const info: AiCallInfo = { provider: "openai", model, usage: this.mapResponsesUsage(response.usage), durationMs: Date.now() - startTime }` e invocar `params.onCall?.(info)` em try/catch silencioso (console.error + seguir — D7). Usar o MESMO caminho de sucesso; em caso de falha da chamada, invocar onCall com o erro? NÃO — onCall só é chamado com dados de chamada concluída (uso p/ custo de sucesso; o evento failed com error_type é gravado pela rota).
    3. Helper privado `mapResponsesUsage(usage): TokenUsage | undefined` mapeando `{ input_tokens → promptTokens, output_tokens → completionTokens, total_tokens → totalTokens }` (campos do Responses API); retorna undefined se `usage` ausente.
    4. Import `AiCallInfo`/`TokenUsage` de `@/lib/ai-cost/types`.
    5. NÃO alterar logs existentes, prompt, validação ou retorno (compat).
  </action>
  <verify>
    <automated>npm run typecheck; `grep -c "onCall" src/lib/visual-signature/ai-image-generator.ts` >= 2; `grep -c "mapResponsesUsage" src/lib/visual-signature/ai-image-generator.ts` >= 2; `grep -c "AiCallInfo" src/lib/visual-signature/ai-image-generator.ts` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - generate aceita onCall opcional; invoca best-effort com usage do Responses API
    - typecheck limpo; nenhuma mudança de retorno/logs
  </acceptance_criteria>
  <done>AiImageGenerator instrumentado (onCall com usage) — pronto para a rota VS</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BrandProfiler — onCall para visão + texto (from-zero, NO ANALOG #4)</name>
  <files>src/lib/visual-signature/brand-profiler.ts</files>
  <read_first>
    - src/lib/visual-signature/brand-profiler.ts (232-301 generate; 285 generateWithIntendedPalette; 300 generateWithFallback; 303+ mockGenerate)
    - .planning/phases/38-1-ai-cost-accounting/38-1-PATTERNS.md (seção brand-profiler — NO ANALOG #4, instrumentação from-zero)
    - openspec/changes/fase-38-1-ai-cost-accounting/specs/store-brand-profiler-without-logo/spec.md (linhas 1-70)
  </read_first>
  <behavior>
    - Teste 2: generate com OpenAI mockado → onCall invocado para a chamada de visão (provider "openai", model = OPENAI_BRAND_DIRECTOR_MODEL default, usage + durationMs)
    - Teste 3: path 2 (sem paleta) → onCall invocado para visão E texto (2 chamadas — brand_profile_vision + brand_profile_text são mapeados na ROTA; o serviço apenas emite)
    - Teste 4: mockGenerate (sem OPENAI_API_KEY em dev) → onCall NUNCA invocado (sem chamada IA)
    - Teste 5: cache reuse (perfil existente) → onCall NUNCA invocado
    - Teste 6: onCall lançando → generate continua (best-effort D7)
  </behavior>
  <action>
    Em `src/lib/visual-signature/brand-profiler.ts`:
    1. Adicionar `onCall?: (info: AiCallInfo) => void | Promise<void>` ao `BrandProfilerInput` (opcional).
    2. `generate` (linha 232): repassar `input.onCall` para `generateWithIntendedPalette` (285) e `generateWithFallback` (300). NÃO repassar para `mockGenerate` (sem chamada IA — 6.5/6.4: sem evento).
    3. Nos métodos de geração (visão + texto): medir `durationMs` em volta de CADA chamada de provider (chat.completions / responses), capturar `usage` da resposta, invocar `onCall({ provider: "openai", model, usage, durationMs })` em try/catch silencioso (D7). Model = o usado na chamada (ex.: `process.env.OPENAI_BRAND_DIRECTOR_MODEL ?? "gpt-4o"` — verificar quais chamadas existem nos dois paths e instrumentar cada uma).
    4. Cache reuse (linhas 248-257) e paths de erro F37 (`BrandProfilerWithoutLogoError` com elapsedMs) permanecem SEM onCall e SEM alterações.
    5. Import `AiCallInfo` de `@/lib/ai-cost/types`.
  </action>
  <verify>
    <automated>npm run typecheck; `grep -c "onCall" src/lib/visual-signature/brand-profiler.ts` >= 4; `grep -c "AiCallInfo" src/lib/visual-signature/brand-profiler.ts` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - onCall repassado aos 2 paths de geração (visão + texto); mockGenerate/cache sem evento
    - Erros F37 e retornos preservados; typecheck limpo
  </acceptance_criteria>
  <done>BrandProfiler instrumentado (visão + texto, from-zero) — pronto para a rota brand</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Testes de onCall no brand-profiler (visão/texto/sem-evento)</name>
  <files>src/lib/visual-signature/__tests__/brand-profiler.test.ts</files>
  <read_first>
    - src/lib/visual-signature/__tests__/brand-profiler.test.ts (padrão de mock do OpenAI — vi.mock; casos 147-256)
    - src/lib/visual-signature/brand-profiler.ts (após Task 2)
  </read_first>
  <behavior>
    - Teste 7: path 1 (intendedPalette) → 1 chamada de visão com onCall { provider: "openai", usage, durationMs >= 0 }
    - Teste 8: path 2 (sem paleta) → 2 onCalls (visão + texto) com models corretos
    - Teste 9: sem OPENAI_API_KEY (dev, mock) → 0 onCalls
    - Teste 10: onCall com erro → geração continua e resultado inalterado
  </behavior>
  <action>
    Em `src/lib/visual-signature/__tests__/brand-profiler.test.ts`: adicionar describe "onCall (F38.1)" com os casos 7-10, seguindo o padrão de mock de OpenAI do arquivo (vi.mock do SDK + respostas com `usage` mockado). Passar `onCall: vi.fn()` no input; asserções de `toHaveBeenCalledTimes`/`toHaveBeenCalledWith` (provider/model/usage/durationMs). Rodar suíte completa do arquivo (casos existentes devem continuar verdes — nenhuma mudança de comportamento).
    A verificação funcional do onCall do AiImageGenerator fica na rota VS (38-1-08) — não criar teste unitário de ai-image-generator neste plano (não existe harness de mock para o dynamic import de responses; cobertura via rota).
  </action>
  <verify>
    <automated>npx vitest run src/lib/visual-signature/__tests__/brand-profiler.test.ts; npm run typecheck; `grep -c "onCall" src/lib/visual-signature/__tests__/brand-profiler.test.ts` >= 3</automated>
  </verify>
  <acceptance_criteria>
    - 4 testes novos verdes + suíte existente verde
    - typecheck limpo
  </acceptance_criteria>
  <done>Instrumentação do BrandProfiler coberta por testes (onCall visão/texto/sem-evento)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AiImageGenerator.generate / BrandProfiler.generate → onCall | Dados de chamada IA atravessam aqui — best-effort, nunca bloqueia |
| BrandProfiler → paths de erro F37 | Erros existentes (BrandProfilerWithoutLogoError) — preservados, sem onCall |
| Serviço → rota (38-1-08/09) | Consumidos pela rota para resolveAiCost + tracker.record |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38.1-25 | DoS | onCall lançando em profiling/geração VS | mitigate | try/catch silencioso + opcionalidade em toda invocação (D7) — F38.1-25 |
| T-38.1-26 | Tampering | Eventos fantasma (sem chamada IA) | mitigate | onCall só invocado após chamada de provider concluída; mockGenerate/cache reuse sem evento (testado) — F38.1-34 |
| T-38.1-27 | Information Disclosure | usage/telemetria vazando em logs | mitigate | onCall transmite apenas tokens/custos; logs de erro sem payload — F38.1-36 |
| T-38.1-28 | Spoofing | Erros F37 duplicando telemetria (furo dupla contagem) | mitigate | Caminhos de erro existentes intactos (elapsedMs no erro NÃO vira evento onCall); eventos só em sucesso — F38.1-25 |
| T-38.1-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado neste plano — gate de legitimidade não se aplica |
</threat_model>

<verification>
- `npx vitest run src/lib/visual-signature/__tests__/brand-profiler.test.ts` — 0 falhas
- `npm run typecheck` limpo
- Greps: onCall nos 2 serviços VS/brand; mockGenerate SEM onCall
</verification>

<success_criteria>
- AiImageGenerator e BrandProfiler emitem AiCallInfo (visão + texto) best-effort
- Caminhos sem chamada IA não emitem eventos (6.4/6.5: delivery/fallback sem evento)
- Compat F37 preservada (erros, cache, retornos)
- Serviços prontos para as rotas 38-1-08/09
</success_criteria>

<output>
Create `.planning/phases/38-1-ai-cost-accounting/38-1-06-SUMMARY.md` when done
</output>

---
phase: 38-1-ai-cost-accounting
plan: 07
type: execute
wave: 4
depends_on:
  - "38-1-02"
  - "38-1-04"
  - "38-1-05"
files_modified:
  - "src/app/api/campaign/generate-image/route.ts"
  - "src/lib/campaign/persistence.ts"
  - "src/lib/campaign/types.ts"
  - "src/app/api/campaign/generate-image/__tests__/route.test.ts"
autonomous: true
HAS_UI: 0
requirements:
  - F38.1-20
  - F38.1-21
  - F38.1-22
  - F38.1-23
  - F38.1-24
  - F38.1-30
must_haves:
  truths:
    - "startRun('campaign_delivery') no início do pipeline → operationRunId propagado para TODOS os eventos do run (recomposição mesmo run)"
    - "Campaign criada com operation_run_id persistido (createCampaign estendido — 6.3 test 10)"
    - "Eventos call-level por chamada: campaign_copy (usage real via onCall — furo 1), campaign_input_validation, campaign_image (por tentativa), campaign_image_review (attempt 1..n) — com custo real via resolveAiCost + duration_ms"
    - "Delivery campaign_pipeline com custo NULL + metadata.duration_is_pipeline: true (D1/D6 — corrige lógica pipelineCost>0)"
    - "metadata.totalCost = SOMA REAL dos custos call-level (furo 2 sanado — não mais provider name)"
    - "Pipeline failed → campaign_pipeline status failed + error_type"
    - "admin_get_metrics preservado (logPipelineEvent intacto — 6.3 test 11)"
    - "11 testes de pipeline (6.3) verdes"
  artifacts:
    - path: "src/app/api/campaign/generate-image/route.ts"
      provides: "Pipeline instrumentado com run context + custo real por chamada"
      exports: ["POST"]
    - path: "src/lib/campaign/persistence.ts"
      provides: "createCampaign com operation_run_id"
      contains: "operation_run_id"
    - path: "src/app/api/campaign/generate-image/__tests__/route.test.ts"
      provides: "11 testes de pipeline (6.3)"
      contains: "campaign_input_validation"
  key_links:
    - from: "src/app/api/campaign/generate-image/route.ts"
      to: "src/lib/ai-cost/tracker.ts"
      via: "AiCostTracker.startRun + record (todas as colunas novas)"
      pattern: "AiCostTracker"
    - from: "src/app/api/campaign/generate-image/route.ts"
      to: "src/lib/ai-cost/cost-estimator.ts"
      via: "resolveAiCost(AiCallInfo) para custo real por chamada"
      pattern: "resolveAiCost"
    - from: "src/app/api/campaign/generate-image/route.ts"
      to: "src/lib/campaign/persistence.ts"
      via: "createCampaign com operationRunId"
      pattern: "operationRunId"
---

<objective>
Instrumentar o pipeline da rota `POST /api/campaign/generate-image` com contabilidade de custo definitiva: `startRun("campaign_delivery")` no início (run context D1), substituição dos 4 inserts inline legados (linhas 595-663 e 717+) por gravação via `AiCostTracker.record` com colunas novas, resolução de custo REAL por chamada via `resolveAiCost` (furo 1), eventos `campaign_input_validation` e `campaign_image_review` por tentativa (D5/D11), delivery `campaign_pipeline` SEMPRE com custo NULL + `duration_is_pipeline` (D1/D6), `metadata.totalCost` com soma real (furo 2), e `operation_run_id` persistido na criação da campanha. Testes de pipeline (tarefa 6.3 — 11 testes).

Purpose: é aqui que o custo real por entrega de campanha é contabilizado de ponta a ponta — sem ele, a apuração (D10) e a reconciliação cost×créditos não têm números confiáveis.
Output: rota instrumentada + persistence estendida + 11 testes de pipeline verdes.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-1-ai-cost-accounting/38-1-CONTEXT.md
@.planning/phases/38-1-ai-cost-accounting/38-1-PATTERNS.md
@openspec/changes/fase-38-1-ai-cost-accounting/specs/transactional-pipeline/spec.md
@openspec/changes/fase-38-1-ai-cost-accounting/specs/ai-cost-tracker/spec.md

<interfaces>
Contratos e pontos de instrumentação que o executor precisa (extraídos do spec + leitura da rota):

**Sem UI (HAS_UI: 0). Nenhuma página/rota de UI é alterada.**

Estado atual verificado em `src/app/api/campaign/generate-image/route.ts` (763 linhas):
- **Linha 593 (furo 1):** `const copyCost = estimateAiCost({ provider: "openai", model: "gpt-4o" })` — SEM usage; evento campaign_copy com custo aproximado
- **Linha 595-607:** insert inline `campaign_copy` (generation_type literal, sem operation_run_id, sem cost_source)
- **Linha 613-614 (furo 1 parcial):** `imageCost = estimateAiCost({..., usage})` — usa usage REAL (imagem) mas ainda via API antiga
- **Linha 616-632:** insert inline `campaign_image`
- **Linha 638:** `pipelineCost = copyCost + imageCost`; **linha 642 (furo 2):** `metadata: { totalCost: generationMetadata.provider }` — totalCost = NOME do provider
- **Linha 644-660:** insert `campaign_pipeline` delivery com `pipelineCost > 0 ? pipelineCost : null` — furo D1/D6: delivery com custo deve ser NULL SEMPRE
- **Linha 717+:** insert `campaign_pipeline` status failed
- NÃO existem eventos `campaign_input_validation` nem `campaign_image_review` (6.3: exigidos, por tentativa)

Contratos disponíveis (planos anteriores):
- `AiCostTracker.startRun(type) -> { operationRunId, traceId }` + `record(event: AiCostEvent)` best-effort (38-1-02); `AiCostEvent` inclui operationRunId, operationRunType, traceId, storeId, userId, campaignId, generationType (12 valores), provider, model, attemptNumber, durationMs, status, errorType?, tokens?, cost? (CostResolution), metadata?
- `resolveAiCost({ provider, model, usage?, providerReportedCostUsd? }) -> Promise<CostResolution>` — nunca null (38-1-04)
- `CopyDirectorService.generateCopy(input, { signal, onCall })` e `ImageGenerationService.generateImage({..., onMetricsEvent })` (38-1-05)
- `createCampaign(storeId, input)` em `src/lib/campaign/persistence.ts` — precisa de `operation_run_id` (campaigns); `CreateCampaignInput` em `src/lib/campaign/types.ts`
- `GenerationEventType` 12 valores (38-1-02): campaign_pipeline, campaign_copy, campaign_input_validation, campaign_image, campaign_image_review, ...

**Padrão de instrumentação da rota (helper local `recordCall`):**
- Call-level: `const cost = await resolveAiCost(info);` + `tracker.record({ operationRunId, operationRunType: "campaign_delivery", traceId, storeId, userId, campaignId, generationType, provider: info.provider, model: info.model, attemptNumber, durationMs: info.durationMs, status, errorType, tokens: info.usage, cost })`
- Delivery (campaign_pipeline): NÃO chama resolveAiCost — grava SEM `cost` e SEM `tokens` + `metadata: { duration_is_pipeline: true }` (D1/D6)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Run context + createCampaign com operation_run_id</name>
  <files>src/lib/campaign/persistence.ts, src/lib/campaign/types.ts, src/app/api/campaign/generate-image/route.ts</files>
  <read_first>
    - src/lib/campaign/persistence.ts (createCampaign — insert em campaigns, hoje sem operation_run_id)
    - src/lib/campaign/types.ts (CreateCampaignInput)
    - src/app/api/campaign/generate-image/route.ts (região do traceId atual — buscar "traceId ="; criação do pipeline ~linhas 540-585)
  </read_first>
  <behavior>
    - Teste 1 (unit persistence): `createCampaign(storeId, { ..., operationRunId })` -> insert em campaigns com `operation_run_id` preenchido; sem operationRunId -> null
    - Teste 2 (compile-time): `CreateCampaignInput` aceita `operationRunId?: string`
    - Teste 3 (route): após `startRun`, os eventos gravados usam o operationRunId retornado
  </behavior>
  <action>
    1. Em `src/lib/campaign/types.ts`: adicionar `operationRunId?: string` ao `CreateCampaignInput`.
    2. Em `src/lib/campaign/persistence.ts`: `createCampaign(storeId, input)` passa `operation_run_id: input.operationRunId ?? null` no insert. Se houver teste de persistence existente, estender com o caso 1.
    3. Em `src/app/api/campaign/generate-image/route.ts`: no início do pipeline (onde o traceId atual é criado — procurar `traceId =` ou similar), substituir a geração atual por `const { operationRunId, traceId } = new AiCostTracker().startRun("campaign_delivery");` (import `AiCostTracker` de `@/lib/ai-cost`). Manter o `traceId` usado pelo logPipelineEvent (compat — 6.3 test 11) e o novo `operationRunId` para as colunas novas. Passar `operationRunId` na chamada de `createCampaign`.
  </action>
  <verify>
    <automated>npm run typecheck; `grep -c "operationRunId" src/lib/campaign/persistence.ts` >= 2; `grep -c "operation_run_id" src/lib/campaign/persistence.ts` >= 1; `grep -c "startRun" src/app/api/campaign/generate-image/route.ts` == 1; `grep -c "operationRunId" src/app/api/campaign/generate-image/route.ts` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - createCampaign grava operation_run_id (campaigns.operation_run_id persistido — 6.3 test 10)
    - startRun no início do pipeline; traceId do logPipelineEvent compatível
    - typecheck limpo
  </acceptance_criteria>
  <done>Run context ativo + campaigns.operation_run_id persistido (6.3 testes 9/10)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Substituição dos inserts por recordCall — custo real + delivery NULL + totalCost real</name>
  <files>src/app/api/campaign/generate-image/route.ts</files>
  <read_first>
    - src/app/api/campaign/generate-image/route.ts (585-724 — região dos 4 inserts inline)
    - src/lib/ai-cost/tracker.ts (record best-effort + contrato AiCostEvent — 38-1-02)
    - src/lib/ai-cost/cost-estimator.ts (resolveAiCost — 38-1-04)
  </read_first>
  <behavior>
    - Teste 4: evento campaign_copy gravado com custo REAL (usage do onCall -> resolveAiCost) e duration_ms
    - Teste 5: eventos campaign_input_validation + campaign_image_review por tentativa (attempt 1..n) gravados
    - Teste 6: delivery campaign_pipeline com estimated_cost_usd NULL + metadata.duration_is_pipeline true (SEMPRE null — nunca pipelineCost)
    - Teste 7: metadata.totalCost = SOMA real dos call-level (não provider name)
    - Teste 8: campaign_pipeline failed com status failed + error_type
  </behavior>
  <action>
    Em `src/app/api/campaign/generate-image/route.ts`:
    1. Criar o helper local `recordCall` (contrato acima) usando `new AiCostTracker()` + `resolveAiCost` (imports de `@/lib/ai-cost`). Delivery (campaign_pipeline) NÃO chama resolveAiCost — grava sem cost/tokens + `metadata.duration_is_pipeline: true` (D1/D6). Todos os record em best-effort (o tracker já nunca lança — D7).
    2. **Copy (furo 1):** na chamada de `generateCopy`, passar `onCall: (info) => recordCall({ generationType: "campaign_copy", status: "success", info })`. REMOVER o insert inline de campaign_copy (595-607) e a variável `copyCost` de `estimateAiCost`.
    3. **Image (D11):** na chamada de `generateImage`, passar `onMetricsEvent: (event) => mapear event.phase -> generationType`:
       - `input_validation` -> `campaign_input_validation` (status success; attempt 0)
       - `image_generation` -> `campaign_image` (attempt = event.attempt) — custo via `resolveAiCost({ provider: event.provider, model: event.model, usage: event.usage })`
       - `quality_review` -> `campaign_image_review` (attempt = event.attempt) — custo via `resolveAiCost({ provider: event.provider, model: event.model, usage: event.usage })`
       - `done`/`prompt_assembly`/outros -> ignorar (não são chamadas de IA)
       REMOVER o insert inline de campaign_image (616-632).
    4. **Delivery (D1/D6):** substituir o insert de campaign_pipeline (644-660) por `recordCall({ generationType: "campaign_pipeline", status: "success", info: { provider, model, durationMs } })` SEM cost/tokens. **Furo 2:** `metadata.totalCost` do `logPipelineEvent` passa a ser a SOMA REAL acumulada dos custos call-level registrados (manter contador local `callCostSum` somando cada estimatedCostUsd real) — NUNCA `generationMetadata.provider`.
    5. **Falha (717+):** substituir o insert de pipeline failed por `recordCall({ generationType: "campaign_pipeline", status: "failed", info, errorType })`.
    6. Manter `logPipelineEvent` (admin_get_metrics preservado — 6.3 test 11). Remover imports/uso de `estimateAiCost` se não restar uso legítimo.
  </action>
  <verify>
    <automated>npm run typecheck; `grep -c "recordCall" src/app/api/campaign/generate-image/route.ts` >= 5; `grep -c "duration_is_pipeline" src/app/api/campaign/generate-image/route.ts` == 1; `grep -c "campaign_image_review" src/app/api/campaign/generate-image/route.ts` >= 1; `grep -c "estimateAiCost" src/app/api/campaign/generate-image/route.ts` == 0; `grep -c "totalCost: generationMetadata" src/app/api/campaign/generate-image/route.ts` == 0</automated>
  </verify>
  <acceptance_criteria>
    - Todos os eventos via tracker (0 inserts inline de generation_events restantes na rota)
    - Custo real por chamada; delivery sempre NULL + duration_is_pipeline; totalCost = soma real
    - typecheck limpo
  </acceptance_criteria>
  <done>Pipeline instrumentado: custo real por chamada + delivery NULL + totalCost real (furos 1/2, D1/D5/D6/D11)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Testes de pipeline (6.3 — 11 testes)</name>
  <files>src/app/api/campaign/generate-image/__tests__/route.test.ts</files>
  <read_first>
    - src/app/api/campaign/generate-image/__tests__/route.test.ts (estrutura de mocks existente do pipeline — copy/image mocks, supabaseAdmin mock)
    - .planning/phases/38-1-ai-cost-accounting/38-1-PATTERNS.md (padrão de testes de rota)
  </read_first>
  <behavior>
    (11 cenários — tasks.md 6.3)
    - Teste 9: copy com usage real e custo (evento campaign_copy com estimated_cost_usd calculado de usage)
    - Teste 10: campaign_input_validation registrado (evento presente no run)
    - Teste 11: campaign_image_review por tentativa attempt 1..n (2 tentativas -> 2 eventos com attempt distinto)
    - Teste 12: recomposição mesmo run — todos os eventos compartilham o MESMO operation_run_id
    - Teste 13: metadata.totalCost soma real (soma dos estimatedCostUsd call-level, não provider name)
    - Teste 14: delivery campaign_pipeline custo NULL + metadata.duration_is_pipeline true (mesmo com copy+image custos > 0)
    - Teste 15: review falha -> status failed + custo dos call-level já registrados (evento failed presente)
    - Teste 16: duration_ms por chamada (eventos com durationMs >= 0)
    - Teste 17: operation_run_id propagado (campaigns insert com operation_run_id)
    - Teste 18: campaigns.operation_run_id persistido na criação (createCampaign chamado com operationRunId)
    - Teste 19: admin_get_metrics preservado (logPipelineEvent ainda chamado com os eventos do pipeline)
  </behavior>
  <action>
    Em `src/app/api/campaign/generate-image/__tests__/route.test.ts`: adicionar describe "Pipeline cost accounting (6.3)" com os 11 cenários. Padrão: estender os mocks existentes da rota (supabaseAdmin.from generation_events com mockInsert que captura inserts num array local; mock de generateCopy com onCall disparado com usage; mock de generateImage com onMetricsEvent disparado para input_validation/image_generation/quality_review com usage e attempt). Asserções sobre o array capturado: operation_run_id idêntico entre eventos, generation_type, attempt_number, estimated_cost_usd (computed do usage via resolveAiCost — usar vi.mock de resolveAiCost ou de ai-model-pricing), duration_is_pipeline no delivery, campaigns insert com operation_run_id, e logPipelineEvent ainda chamado.
    Rodar: `npx vitest run src/app/api/campaign/generate-image/__tests__/route.test.ts` — suíte inteira (existente + nova) verde.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/campaign/generate-image/__tests__/route.test.ts; npm run typecheck; `grep -c "operation_run_id" src/app/api/campaign/generate-image/__tests__/route.test.ts` >= 3; `grep -c "duration_is_pipeline" src/app/api/campaign/generate-image/__tests__/route.test.ts` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - 11 cenários 6.3 verdes + suíte existente da rota verde
    - typecheck limpo
  </acceptance_criteria>
  <done>Pipeline de campanha coberto: 11 testes 6.3 verdes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generateImage onMetricsEvent -> route recordCall | Telemetria por tentativa entra na rota aqui |
| recordCall -> tracker.record (supabaseAdmin) | Gravação de custo — best-effort, nunca bloqueia o pipeline |
| resolveAiCost -> ai_model_pricing | Resolução de custo por chamada (server-only) |
| logPipelineEvent -> admin metrics | Pipeline events existentes — preservados (compat 6.3 test 11) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38.1-29 | DoS | recordCall falhando no meio do pipeline | mitigate | tracker.record best-effort (nunca lança — D7); recordCall sem await crítico no caminho de resultado — F38.1-20 |
| T-38.1-30 | Tampering | Dupla contagem (evento inline + tracker) | mitigate | REMOÇÃO total dos inserts inline; canal único recordCall (grep de controle: 0 inserts de generation_events inline) — F38.1-20 |
| T-38.1-31 | Tampering | Delivery com custo (dupla contagem da soma call-level) | mitigate | campaign_pipeline SEMPRE sem cost/tokens + duration_is_pipeline (D1/D6); view soma só call-level — F38.1-22 |
| T-38.1-32 | Information Disclosure | totalCost exposto com dados errados (furo 2) | mitigate | totalCost = soma real dos call-level registrados; nunca provider name — F38.1-24 |
| T-38.1-33 | Spoofing | operation_run_id inconsistente entre eventos | mitigate | startRun único por pipeline; operationRunId propagado por closure (recordCall); testado (6.3 test 12) — F38.1-23 |
| T-38.1-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado neste plano — gate de legitimidade não se aplica |
</threat_model>

<verification>
- `npx vitest run src/app/api/campaign/generate-image/__tests__/route.test.ts` — suíte completa verde (11 novos + existentes)
- `npm run typecheck` limpo
- Greps: recordCall >= 5, duration_is_pipeline == 1, estimateAiCost == 0, totalCost: generationMetadata == 0
</verification>

<success_criteria>
- Todos os eventos do pipeline via tracker (copy/validation/image/review/delivery) com custo real por chamada
- Delivery sempre NULL + duration_is_pipeline; totalCost = soma real
- campaigns.operation_run_id persistido; recomposição por run consistente
- 11 testes 6.3 verdes; admin_get_metrics preservado
</success_criteria>

<output>
Create `.planning/phases/38-1-ai-cost-accounting/38-1-07-SUMMARY.md` when done
</output>

---
phase: 38-1-ai-cost-accounting
plan: 08
type: execute
wave: 4
depends_on:
  - "38-1-02"
  - "38-1-04"
  - "38-1-06"
files_modified:
  - "src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts"
  - "src/lib/visual-signature/ai-image-generator.ts"
  - "src/app/api/store/[id]/visual-signature/generate-without-logo/__tests__/generate-route.test.ts"
autonomous: true
HAS_UI: 0
requirements:
  - F38.1-25
  - F38.1-26
  - F38.1-27
  - F38.1-28
  - F38.1-29
must_haves:
  truths:
    - "startRun('visual_signature') no início → operationRunId propagado para os eventos do run; nova tentativa pós-falha = NOVO run (operationRunId distinto — 6.4 test 3)"
    - "Evento call-level visual_signature_image com custo/tokens reais via resolveAiCost + duration_ms (6.4 test 1)"
    - "Evento call-level visual_signature_validation com custo/tokens reais (validação LLM do validator — 6.4 test 1)"
    - "Delivery visual_signature com custo NULL + duration_is_pipeline (soma das duas via view — 6.4 test 2)"
    - "Eventos com visual_signature_id preenchido (6.4 test 4)"
    - "Typographic fallback SEM evento (sem chamada IA — 6.4 test 5)"
    - "insertGenerationEvent compat F37 preservado (mesmos generation_type/status — 6.4 test 6)"
    - "6 testes (6.4) verdes"
  artifacts:
    - path: "src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts"
      provides: "Rota VS com run context + custo real por chamada"
      exports: ["POST"]
    - path: "src/lib/visual-signature/ai-image-generator.ts"
      provides: "onCall estendido para a chamada de validação LLM (visual_signature_validation)"
      contains: "onCall"
  key_links:
    - from: "src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts"
      to: "src/lib/ai-cost/tracker.ts"
      via: "startRun + record (visual_signature_image/validation + delivery)"
      pattern: "AiCostTracker"
    - from: "src/lib/visual-signature/ai-image-generator.ts"
      to: "src/lib/ai-cost/types.ts (AiCallInfo)"
      via: "onCall para imagem E validação LLM"
      pattern: "onCall"
---

<objective>
Instrumentar a rota `POST /api/store/[id]/visual-signature/generate-without-logo` com contabilidade de custo: `startRun("visual_signature")`, eventos call-level `visual_signature_image` (imagem via Responses API) e `visual_signature_validation` (validação LLM do validator) com custo/tokens reais, delivery `visual_signature` com custo NULL + `duration_is_pipeline` (D1/D6), `visual_signature_id` preenchido, nova tentativa = novo run, e compat F37 do `insertGenerationEvent` preservada. Testes 6.4 (6 testes).

Purpose: a assinatura visual (nova tentativa + validação) precisa de custo por chamada real — furo 5 (insertGenerationEvent sem custo) e o escopo VS de D1/D2/D5 ficam completos aqui.
Output: rota VS instrumentada + validator com onCall + 6 testes 6.4 verdes.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-1-ai-cost-accounting/38-1-CONTEXT.md
@.planning/phases/38-1-ai-cost-accounting/38-1-PATTERNS.md
@openspec/changes/fase-38-1-ai-cost-accounting/specs/store-visual-signature/spec.md

<interfaces>
Contratos e pontos de instrumentação que o executor precisa (extraídos do spec + leitura da rota):

**Sem UI (HAS_UI: 0). Nenhuma página/rota de UI é alterada.**

Estado atual verificado em `src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts` (503 linhas):
- **Linha 265:** `result = await service.generate(serviceInput, abortController.signal)` (StoreIdentityArtDirectorService)
- **Linhas 280-290:** retry `new AiImageGenerator().generate({ ..., attempt: 1, simplifiedPrompt: true })` (ATTEMPT 2)
- **Linha 417:** `insertGenerationEvent({ generation_type: 'visual_signature', provider: 'openai', attempt_number: 1, status: 'success', ... })` — delivery marker (furo 5: sem custo/tokens, sem operation_run_id, sem visual_signature_id)
- **Linha 470:** `insertGenerationEvent({ generation_type: 'visual_signature', ..., status: 'timeout'|'failed', error_type })` — delivery falha
- `AiImageGenerator` (38-1-06) já aceita `onCall` e emite para a chamada de imagem (responses.create). O `VisualSignatureValidator` (mesmo arquivo `ai-image-generator.ts`, classe acima da linha 111; `validate()` nas linhas 100-108) faz a chamada LLM de validação — PRECISA de onCall (visual_signature_validation)

Contratos disponíveis (planos anteriores):
- `AiCostTracker.startRun(type)` + `record(event)` best-effort (38-1-02); `AiCostEvent` com operationRunId, traceId, storeId, visualSignatureId, generationType (12 valores), provider, model, attemptNumber, durationMs, status, errorType?, tokens?, cost?, metadata?
- `resolveAiCost({ provider, model, usage?, providerReportedCostUsd? }) -> Promise<CostResolution>` (38-1-04)
- `insertGenerationEvent` delega ao tracker (38-1-02) — sem cost/tokens = delivery marker (NULL + duration_is_pipeline)
- `GenerationEventType` inclui `visual_signature`, `visual_signature_image`, `visual_signature_validation` (D5)

**Padrão de instrumentação (helper local `recordCall` — idêntico ao 38-1-07):**
- Call-level: `resolveAiCost(info)` + `tracker.record({ operationRunId, operationRunType: "visual_signature", traceId, storeId, visualSignatureId, generationType, provider, model, attemptNumber, durationMs, status, tokens: info.usage, cost })`
- Delivery: SEM cost/tokens + `metadata: { duration_is_pipeline: true }` — NÃO chama resolveAiCost (D1/D6)
- Retry (ATTEMPT 2): NOVO `startRun("visual_signature")` — novo operationRunId (6.4 test 3)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VisualSignatureValidator com onCall — visual_signature_validation</name>
  <files>src/lib/visual-signature/ai-image-generator.ts</files>
  <read_first>
    - src/lib/visual-signature/ai-image-generator.ts (linhas 95-110 — validator.validate; 118-233 — generate com onCall já presente)
  </read_first>
  <behavior>
    - Teste 1 (compile-time): `VisualSignatureValidator.validate` aceita `onCall?: (info: AiCallInfo) => void | Promise<void>`
    - Teste 2: chamada LLM do validator mede durationMs e captura usage da resposta
  </behavior>
  <action>
    Em `src/lib/visual-signature/ai-image-generator.ts`:
    1. `VisualSignatureValidator.validate` (linhas 100-108): adicionar parâmetro `onCall?: (info: AiCallInfo) => void | Promise<void>` (opcional); medir `durationMs` em volta da chamada LLM; capturar usage (chat completion usage) e invocar `onCall({ provider: "openai", model, usage, durationMs })` em try/catch silencioso (D7). O fallback atual (validação falha → valid: true, linhas 104-106) permanece.
    2. `AiImageGenerator.generate`: na chamada ao validator (linhas 227-231), repassar um `onCall` que PROPAGA o evento (o mesmo callback de `params.onCall`); o modelo do validator é o usado na chamada (verificar — ex.: gpt-4o/OPENAI_BRAND_DIRECTOR_MODEL; usar o model real da chamada).
    3. NÃO alterar o retorno/prompts/logs.
  </action>
  <verify>
    <automated>npm run typecheck; `grep -c "onCall" src/lib/visual-signature/ai-image-generator.ts` >= 5; `grep -c "validator" src/lib/visual-signature/ai-image-generator.ts` >= 2</automated>
  </verify>
  <acceptance_criteria>
    - Validator emite onCall (usage + durationMs) best-effort; propagate via AiImageGenerator
    - typecheck limpo
  </acceptance_criteria>
  <done>Validação LLM da VS instrumentada (visual_signature_validation)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rota VS — run context + recordCall + delivery com colunas novas</name>
  <files>src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts</files>
  <read_first>
    - src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts (255-324 retry; 405-479 inserts de delivery)
    - src/lib/ai-cost/tracker.ts (record + AiCostEvent — 38-1-02)
    - src/lib/ai-cost/cost-estimator.ts (resolveAiCost — 38-1-04)
  </read_first>
  <behavior>
    - Teste 3: evento visual_signature_image gravado com custo real (usage -> resolveAiCost) e attempt correto (0 no attempt 1, 1 no retry)
    - Teste 4: evento visual_signature_validation gravado com custo real
    - Teste 5: delivery visual_signature com estimated_cost_usd NULL + metadata.duration_is_pipeline true (soma das duas via view — 6.4 test 2)
    - Teste 6: retry (ATTEMPT 2) com NOVO operationRunId (run distinto)
    - Teste 7: todos os eventos do mesmo run com visual_signature_id preenchido
  </behavior>
  <action>
    Em `src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts`:
    1. No início do fluxo (após montar storeId/visualSignatureId): `const { operationRunId, traceId } = new AiCostTracker().startRun("visual_signature");` (import `AiCostTracker` de `@/lib/ai-cost`). Para o retry (linha 280): NOVO `startRun("visual_signature")` — novo operationRunId (6.4 test 3); o retry usa `visualSignatureId` do resultado.
    2. Helper local `recordCall` (contrato acima): call-level com `resolveAiCost`; delivery SEM cost/tokens + `metadata.duration_is_pipeline: true` (D1/D6). `visualSignatureId` preenchido em TODOS os eventos do run (6.4 test 4).
    3. **Imagem (attempt 1, linha 265):** o `service.generate` interno (StoreIdentityArtDirectorService) chama `AiImageGenerator.generate` — o onCall da imagem e validação é emitido DENTRO do service. Como o onCall não atravessa `service.generate` por assinatura, usar a estratégia: o service (server-actions.ts) já recebe os callbacks — verificar onde `service.generate` encadeia `aiGenerator.generate` e repassar um `onCall` do caller quando disponível (adicionar parâmetro opcional `onCall?` a `StoreIdentityArtDirectorService.generate` e `serviceInput`, repassando ao `aiGenerator.generate`). Alternativa aceita: o retry direto (linha 280) usa `aiGenerator.generate({ ..., onCall })` diretamente — garantir pelo menos o caminho do retry instrumentado; para o attempt 1, repassar via server-actions com o MESMO padrão.
    4. **Delivery success (linha 417):** adicionar `operation_run_id: operationRunId, trace_id: traceId, visual_signature_id: <id da signature>` ao insert existente (insertGenerationEvent já delega ao tracker — sem cost/tokens = delivery marker D1/D6). **Delivery failed (linha 470):** mesmos campos novos.
    5. Typographic fallback (tier automatic_generated sem chamada IA): NENHUM evento call-level (o onCall não dispara sem chamada — 6.4 test 5).
  </action>
  <verify>
    <automated>npm run typecheck; `grep -c "startRun" src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts` >= 2; `grep -c "visual_signature_id" src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts` >= 2; `grep -c "operation_run_id" src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts` >= 2</automated>
  </verify>
  <acceptance_criteria>
    - Eventos call-level (imagem + validação) com custo real; delivery NULL + duration_is_pipeline
    - Retry = novo run; visual_signature_id preenchido; compat F37 do insertGenerationEvent
    - typecheck limpo
  </acceptance_criteria>
  <done>Rota VS instrumentada (custo real + delivery NULL + retry novo run)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Testes 6.4 (6 testes)</name>
  <files>src/app/api/store/[id]/visual-signature/generate-without-logo/__tests__/generate-route.test.ts</files>
  <read_first>
    - src/app/api/store/[id]/visual-signature/generate-without-logo/__tests__/generate-route.test.ts (padrão de mocks — vi.mock supabase/server, ai-image-generator, server-actions; casos 173-304)
  </read_first>
  <behavior>
    (6 cenários — tasks.md 6.4)
    - Teste 8: visual_signature_image + visual_signature_validation com custo/tokens (2 eventos call-level no run)
    - Teste 9: delivery custo NULL (estimated_cost_usd null no delivery; soma das duas via view)
    - Teste 10: nova tentativa pós-falha = novo run (operationRunId do retry DIFERENTE do attempt 1)
    - Teste 11: visual_signature_id preenchido nos eventos do run
    - Teste 12: typographic fallback sem evento (nenhum evento gravado no caminho fallback)
    - Teste 13: insertGenerationEvent VS compat F37 (mesmos generation_type/status/fields do fluxo existente — testes atuais continuam verdes)
  </behavior>
  <action>
    Em `src/app/api/store/[id]/visual-signature/generate-without-logo/__tests__/generate-route.test.ts`: adicionar describe "VS cost accounting (6.4)" com os 6 cenários. Padrão: capturar inserts em array (mockInsert no from generation_events); mock de aiGenerator.generate disparando onCall com usage (imagem) e onCall de validação; mock do service para attempt 1; verificar operationRunId idêntico dentro do run e distinto no retry; delivery com duration_is_pipeline. Os testes existentes (173-304) devem continuar passando (compat F37).
    Rodar: `npx vitest run src/app/api/store/[id]/visual-signature/generate-without-logo/__tests__/generate-route.test.ts` — suíte inteira verde.
  </action>
  <verify>
    <automated>npx vitest run "src/app/api/store/[id]/visual-signature/generate-without-logo/__tests__/generate-route.test.ts"; npm run typecheck; `grep -c "visual_signature_image" src/app/api/store/[id]/visual-signature/generate-without-logo/__tests__/generate-route.test.ts` >= 1; `grep -c "visual_signature_validation" src/app/api/store/[id]/visual-signature/generate-without-logo/__tests__/generate-route.test.ts` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - 6 cenários 6.4 verdes + suíte existente verde (compat F37)
    - typecheck limpo
  </acceptance_criteria>
  <done>VS coberta: 6 testes 6.4 verdes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AiImageGenerator onCall (imagem + validação) -> rota recordCall | Telemetria por chamada entra na rota aqui |
| recordCall -> tracker.record (supabaseAdmin) | Gravação best-effort — nunca bloqueia geração VS |
| insertGenerationEvent (417/470) -> tracker | Delivery marker delega ao tracker (38-1-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38.1-34 | DoS | onCall/recordCall falhando na rota VS | mitigate | Best-effort (D7): onCall em try/catch no validator e tracker.record nunca lança — F38.1-25 |
| T-38.1-35 | Tampering | Delivery com custo (dupla contagem) | mitigate | delivery SEM cost/tokens + duration_is_pipeline (D1/D6); insertGenerationEvent sem custo = delivery — F38.1-27 |
| T-38.1-36 | Spoofing | Retry misturando runs | mitigate | Novo startRun por tentativa (operationRunId distinto — testado 6.4 test 3) — F38.1-26 |
| T-38.1-37 | Information Disclosure | Evento sem visual_signature_id dificultando auditoria | mitigate | visual_signature_id preenchido em todos os eventos do run (testado) — F38.1-25 |
| T-38.1-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado neste plano — gate de legitimidade não se aplica |
</threat_model>

<verification>
- `npx vitest run` na suíte generate-route.test.ts — 6 novos + existentes, 0 falhas
- `npm run typecheck` limpo
- Greps: startRun >= 2 na rota; visual_signature_id >= 2; operation_run_id >= 2
</verification>

<success_criteria>
- Eventos call-level VS (imagem + validação) com custo/tokens reais
- Delivery NULL + duration_is_pipeline; soma via view (D1/D6)
- Retry = novo run; visual_signature_id preenchido; compat F37
- 6 testes 6.4 verdes
</success_criteria>

<output>
Create `.planning/phases/38-1-ai-cost-accounting/38-1-08-SUMMARY.md` when done
</output>

---
phase: 38-1-ai-cost-accounting
plan: 09
type: execute
wave: 4
depends_on:
  - "38-1-02"
  - "38-1-04"
  - "38-1-06"
files_modified:
  - "src/lib/brand-assets/brand-director.ts"
  - "src/lib/brand-assets/text-only-inference-service.ts"
  - "src/lib/brand-assets/__tests__/brand-director.test.ts"
  - "src/lib/brand-assets/__tests__/text-only-inference-service.test.ts"
  - "src/lib/visual-signature/brand-profiler.ts"
  - "src/app/api/store/[id]/brand-profile/generate-without-logo/route.ts"
  - "src/app/api/store/[id]/brand-profile/realign/route.ts"
  - "src/app/api/store/[id]/brand-profile/infer/route.ts"
  - "src/app/api/store/[id]/brand-profile/route.ts"
  - "src/app/api/store/[id]/brand-profile/generate-without-logo/__tests__/generate-route.test.ts"
  - "src/app/api/store/[id]/brand-profile/__tests__/route.test.ts"
  - "src/app/api/store/[id]/brand-profile/realign/__tests__/realign-route.test.ts"
  - "src/app/api/store/[id]/brand-profile/infer/__tests__/route.test.ts"
  - "src/lib/visual-signature/__tests__/brand-profiler.test.ts"
autonomous: true
HAS_UI: 0
requirements:
  - F38.1-34
  - F38.1-35
  - F38.1-36
  - F38.1-37
  - F38.1-38
must_haves:
  truths:
    - "startRun('brand_profile') nas rotas brand — operationRunId propagado; regenerate (realign mode 'regenerate') = NOVO run (6.5 test 4)"
    - "Eventos call-level brand_profile_vision e brand_profile_text com custo/tokens reais via resolveAiCost (ordem documentada: 1a chamada = visao, 2a = texto — 6.5 testes 1/2)"
    - "Delivery brand_profile_without_logo (generate-without-logo) / brand_profile_with_logo (rota principal) com custo NULL + duration_is_pipeline (6.5 test 3)"
    - "BrandDirectorService.analyze e BrandTextOnlyInferenceService.infer aceitam onCall com usage (D11 3.7)"
    - "infer/route.ts e o caminho text_only do realign instrumentados: call brand_profile_text com custo real + delivery sem custo"
    - "BrandProfilerWithoutLogoService.generate aceita onCall no input (BrandProfilerInput — 38-1-06) e repassa ao BrandProfiler"
    - "4 testes (6.5) + testes novos de serviços/infer verdes"
  artifacts:
    - path: "src/app/api/store/[id]/brand-profile/generate-without-logo/route.ts"
      provides: "Rota brand sem logo com run context + custo real"
      exports: ["POST"]
    - path: "src/app/api/store/[id]/brand-profile/infer/route.ts"
      provides: "Rota infer com call brand_profile_text + delivery (hoje zero eventos)"
      exports: ["POST"]
    - path: "src/app/api/store/[id]/brand-profile/realign/route.ts"
      provides: "Realign com analyze/text_only/regenerate instrumentados (regenerate = novo run)"
      exports: ["POST"]
    - path: "src/lib/brand-assets/brand-director.ts"
      provides: "BrandDirectorService.analyze com onCall (usage do vision)"
      contains: "onCall"
    - path: "src/lib/brand-assets/text-only-inference-service.ts"
      provides: "BrandTextOnlyInferenceService.infer com onCall (usage)"
      contains: "onCall"
    - path: "src/lib/visual-signature/brand-profiler.ts"
      provides: "BrandProfilerWithoutLogoService repassando onCall (threading)"
      contains: "onCall"
  key_links:
    - from: "src/app/api/store/[id]/brand-profile/generate-without-logo/route.ts"
      to: "src/lib/ai-cost/tracker.ts"
      via: "startRun + record (vision/text + delivery)"
      pattern: "AiCostTracker"
    - from: "src/app/api/store/[id]/brand-profile/infer/route.ts"
      to: "src/lib/ai-cost/tracker.ts"
      via: "startRun + record (brand_profile_text + delivery)"
      pattern: "AiCostTracker"
    - from: "src/lib/brand-assets/brand-director.ts"
      to: "src/lib/ai-cost/types.ts (AiCallInfo)"
      via: "onCall no analyze (realign analysis path)"
      pattern: "onCall"
    - from: "src/lib/brand-assets/text-only-inference-service.ts"
      to: "src/lib/ai-cost/types.ts (AiCallInfo)"
      via: "onCall no infer (infer + realign text_only paths)"
      pattern: "onCall"
    - from: "src/lib/visual-signature/brand-profiler.ts"
      to: "src/lib/ai-cost/types.ts (AiCallInfo)"
      via: "onCall repassado (BrandProfilerInput)"
      pattern: "onCall"
---

<objective>
Completar o escopo brand do D1/D5/D11: callback `onCall` nos 3 serviços do escopo brand — `brand-director.ts` (BrandDirectorService.analyze), `text-only-inference-service.ts` (BrandTextOnlyInferenceService.infer) e threading no `BrandProfilerWithoutLogoService` (tarefa 3.7) — e instrumentar TODAS as rotas `/api/store/[id]/brand-profile/*` (tarefa 4.4): `startRun("brand_profile")`, eventos call-level `brand_profile_vision`/`brand_profile_text` com custo/tokens reais, delivery `brand_profile_without_logo`/`brand_profile_with_logo` com custo NULL + `duration_is_pipeline` (D1/D6) — incl. `infer/route.ts` (hoje sem NENHUM evento) e os 3 caminhos de IA do `realign/route.ts` (análise `BrandDirectorService.analyze` linha 290-291, text_only `service.infer` linha 112-114, regenerate `profiler.generate` linha 481-510). Regenerate = novo run. Testes 6.5 (4 testes) + testes de serviços/infer.

Purpose: cada chamada de IA do escopo brand fica com custo real por chamada (furo coberto pelo D11) e a entrega sem dupla contagem — o objetivo da fase (custo real por chamada em todas as camadas) é alcançado também para brand assets, text-only e análise de realinhamento.
Output: 3 serviços com onCall (2 novos + 1 threading) + 4 rotas brand instrumentadas + testes (6.5 + serviços + infer).
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-1-ai-cost-accounting/38-1-CONTEXT.md
@.planning/phases/38-1-ai-cost-accounting/38-1-PATTERNS.md
@openspec/changes/fase-38-1-ai-cost-accounting/specs/store-brand-profiler-without-logo/spec.md
@openspec/changes/fase-38-1-ai-cost-accounting/specs/text-only-brand-inference/spec.md
@openspec/changes/fase-38-1-ai-cost-accounting/specs/brand-director-prompt/spec.md

<interfaces>
Contratos e pontos de instrumentacao que o executor precisa (extraidos do spec + leitura das rotas):

**Sem UI (HAS_UI: 0). Nenhuma pagina/rota de UI e alterada.**

Servicos a instrumentar (D11 3.7 — hoje SEM onCall):
- `BrandDirectorService.analyze(params: { logoBuffer: Buffer; logoMimeType: string; storeData: StoreAnalysisInput }): Promise<BrandDirectorResult>` (`src/lib/brand-assets/brand-director.ts:285`) — ADICIONAR `onCall?: (info: AiCallInfo) => void | Promise<void>` ao objeto params; invocar APOS o `chat.completions.create` (linha 389) com usage/elapsedMs/model (env `OPENAI_BRAND_DIRECTOR_MODEL` ?? 'gpt-4o'); try/catch em volta do onCall (best-effort, D7)
- `BrandTextOnlyInferenceService.infer(input: TextOnlyInferenceInput, timeoutMs: number = 30000): Promise<TextOnlyInferenceResult>` (`src/lib/brand-assets/text-only-inference-service.ts:22`) — ADICIONAR 3o param opcional `onCall?: (info: AiCallInfo) => void | Promise<void>`; invocar apos o `chat.completions.create` (linha 80) com usage (env `OPENAI_TEXT_ONLY_INFERENCE_MODEL` ?? 'gpt-4o'); try/catch (best-effort). NOTA: retorna mock em dev sem OPENAI_API_KEY (linhas 25-47) — NESSE caminho NÃO invocar onCall (sem chamada real de IA)
- Ambos importam `AiCallInfo` de `@/lib/ai-cost/types`; o `provider` = 'openai', `durationMs` = Date.now() - startTime, `attemptNumber` = 0

Estado atual verificado:
- `src/app/api/store/[id]/brand-profile/generate-without-logo/route.ts:52-55`: `new BrandProfilerWithoutLogoService().generate({ ... })` — source 'without_logo' (linha 78)
- `src/app/api/store/[id]/brand-profile/realign/route.ts` (544 linhas) — TRES caminhos de IA no MESMO arquivo:
  - Linha 112-114: `new BrandTextOnlyInferenceService()` + `service.infer({...})` no path text_only — instrumentar com o MESMO padrao de run/evento (call `brand_profile_text` + delivery)
  - Linha 290-291: `new BrandDirectorService()` + `director.analyze({...})` no path com logo (análise) — call `brand_profile_vision` via onCall do analyze
  - Linha 481-510: `profiler.generate({..., mode: 'regenerate' })` (regenerate — novo run)
- `src/app/api/store/[id]/brand-profile/infer/route.ts:63-77`: `new BrandTextOnlyInferenceService().infer({...}, timeoutMs)` — hoje SEM NENHUM evento: adicionar startRun + call `brand_profile_text` + delivery `brand_profile_without_logo` (text-only nao usa logo) com custo NULL + duration_is_pipeline
- `src/app/api/store/[id]/brand-profile/route.ts`: rota principal (com logo) — se produzir geracao via BrandProfiler, aplicar o mesmo padrao com delivery `brand_profile_with_logo`
- `BrandProfilerInput` (38-1-06) ja tem `onCall?: (info: AiCallInfo) => void | Promise<void>`; o `BrandProfilerWithoutLogoService.generate(input)` recebe `BrandProfilerInput` — VERIFICAR se o input e repassado integralmente ao `BrandProfiler.generate` (threading) ou se precisa de ajuste

Contratos disponiveis (planos anteriores):
- `AiCostTracker.startRun(type)` + `record(event)` best-effort (38-1-02); `AiCostEvent` com operationRunId, traceId, storeId, visualSignatureId, generationType (12 valores), provider, model, attemptNumber, durationMs, status, tokens?, cost?, metadata?
- `resolveAiCost({ provider, model, usage?, providerReportedCostUsd? }) -> Promise<CostResolution>` (38-1-04)
- `GenerationEventType` inclui `brand_profile_without_logo`, `brand_profile_with_logo`, `brand_profile_vision`, `brand_profile_text` (D5)

**Mapeamento deterministico das chamadas do profiler (6.5):**
- O `BrandProfiler` invoca `onCall` uma vez por chamada de provider: path 1 (intendedPalette) = 1 chamada de visao; path 2 (sem paleta) = visao depois texto (2 chamadas, ORDEM FIXA)
- A rota BUFFERA os `AiCallInfo` emitidos durante `generate()` e grava por sequencia: 1a entrada -> `brand_profile_vision`, 2a -> `brand_profile_text` (documentado; deterministico por path)
- Delivery (resultado global): `brand_profile_without_logo` (rota generate-without-logo) / `brand_profile_with_logo` (rota principal) — SEM cost/tokens + `duration_is_pipeline: true` (D1/D6)
- `mode: 'regenerate'` -> NOVO `startRun` (novo operationRunId — 6.5 test 4)
- Text-only (`infer` + realign text_only): 1 unica chamada `infer(...)` -> call `brand_profile_text` (custo real) + delivery `brand_profile_without_logo` (custo NULL + duration_is_pipeline)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: onCall nos servicos brand-assets (brand-director + text-only) + threading no wrapper</name>
  <files>src/lib/brand-assets/brand-director.ts, src/lib/brand-assets/text-only-inference-service.ts, src/lib/visual-signature/brand-profiler.ts, src/lib/brand-assets/__tests__/brand-director.test.ts, src/lib/brand-assets/__tests__/text-only-inference-service.test.ts, src/lib/visual-signature/__tests__/brand-profiler.test.ts</files>
  <read_first>
    - src/lib/brand-assets/brand-director.ts (285-405 — analyze + chat.completions.create na linha 389; try/catch de erro existente 414+)
    - src/lib/brand-assets/text-only-inference-service.ts (22-137 — infer + create na linha 80; mock dev sem API key 25-47)
    - src/lib/visual-signature/brand-profiler.ts (classe BrandProfilerWithoutLogoService — localizar e ver como encadeia BrandProfiler.generate; input: BrandProfilerInput)
    - src/lib/visual-signature/brand-profiler.ts (232-301 — BrandProfiler.generate com input.onCall ja implementado em 38-1-06)
    - src/lib/brand-assets/__tests__/brand-director.test.ts (padrao de mock OpenAI existente)
    - src/lib/brand-assets/__tests__/text-only-inference-service.test.ts (padrao de mock existente)
  </read_first>
  <behavior>
    - Teste 1: `BrandDirectorService.analyze` com OpenAI mockado retornando usage -> `onCall` invocado com `{ provider: "openai", model, usage, durationMs >= 0 }` (D11 3.7)
    - Teste 2: `onCall` do analyze que LANCA -> analyze continua e resolve normalmente (best-effort D7)
    - Teste 3: `BrandTextOnlyInferenceService.infer` com OpenAI mockado -> `onCall` invocado com usage + durationMs (D11 3.7)
    - Teste 4: `infer` em dev sem OPENAI_API_KEY (caminho mock, linhas 25-47) -> `onCall` NUNCA invocado (sem chamada real de IA)
    - Teste 5 (compile-time): `BrandProfilerWithoutLogoService.generate` recebe `BrandProfilerInput` com `onCall` e o repassa ao `BrandProfiler.generate`
    - Teste 6: `onCall` do input chega ao profiler interno (spy) durante generate
  </behavior>
  <action>
    1. Em `src/lib/brand-assets/brand-director.ts`:
       - `BrandDirectorService.analyze` (285): ADICIONAR `onCall?: (info: AiCallInfo) => void | Promise<void>` ao objeto params.
       - Apos o `chat.completions.create` (389) resolver, antes do `JSON.parse`/retorno: montar `const info: AiCallInfo = { provider: "openai", model, usage: mapUsage(response.usage), durationMs: Date.now() - startTime }` (model = o usado na chamada; verificar se ha variavel `model` no escopo ou usar `process.env.OPENAI_BRAND_DIRECTOR_MODEL ?? 'gpt-4o'`) e invocar `params.onCall?.(info)` em try/catch silencioso (console.error + seguir — D7). Helper local `mapUsage` (prompt_tokens/completion_tokens/total_tokens -> TokenUsage).
       - NÃO alterar o retorno, os caminhos de erro (`BrandDirectorAnalysisError` com elapsedMs) nem os logs existentes.
    2. Em `src/lib/brand-assets/text-only-inference-service.ts`:
       - `BrandTextOnlyInferenceService.infer` (22): ADICIONAR 3o param opcional `onCall?: (info: AiCallInfo) => void | Promise<void>` (apos timeoutMs).
       - Apos o `chat.completions.create` (80) resolver: invocar `onCall?.({ provider: "openai", model: process.env.OPENAI_TEXT_ONLY_INFERENCE_MODEL ?? 'gpt-4o', usage: mapUsage(response.usage), durationMs: Date.now() - startTime })` em try/catch silencioso (D7).
       - NO caminho mock dev sem OPENAI_API_KEY (25-47, retorno de mock) NÃO invocar onCall — não há chamada real de IA (6.4/6.5: sem evento).
       - NÃO alterar o retorno nem o `BrandTextOnlyInferenceError` existente.
    3. Em `src/lib/visual-signature/brand-profiler.ts`: verificar o corpo de `BrandProfilerWithoutLogoService.generate` — se o `BrandProfilerInput` e desestruturado e reconstruido, ADICIONAR `onCall: input.onCall` no objeto repassado ao `BrandProfiler.generate` interno. Se o input e passado integralmente, apenas garantir por teste.
    4. Extender os testes: `brand-director.test.ts` (Teste 1-2), `text-only-inference-service.test.ts` (Teste 3-4), `brand-profiler.test.ts` (Teste 6 — onCall do wrapper chegando ao profiler).
  </action>
  <verify>
    <automated>npx vitest run src/lib/brand-assets/__tests__/brand-director.test.ts src/lib/brand-assets/__tests__/text-only-inference-service.test.ts src/lib/visual-signature/__tests__/brand-profiler.test.ts; npm run typecheck; `grep -c "onCall" src/lib/brand-assets/brand-director.ts` >= 2; `grep -c "onCall" src/lib/brand-assets/text-only-inference-service.ts` >= 2; `grep -c "onCall" src/lib/visual-signature/brand-profiler.ts` >= 6</automated>
  </verify>
  <acceptance_criteria>
    - analyze e infer emitem onCall (usage + durationMs) best-effort; caminho mock dev sem evento
    - onCall flui do input do service wrapper ate o BrandProfiler (threading)
    - Suites existentes dos 3 arquivos verdes; typecheck limpo
  </acceptance_criteria>
  <done>Servicos brand-assets instrumentados (analyze + infer onCall) + threading completo</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rotas brand — run context + buffer vision/text + delivery NULL (4 rotas incl. infer e realign analysis)</name>
  <files>src/app/api/store/[id]/brand-profile/generate-without-logo/route.ts, src/app/api/store/[id]/brand-profile/realign/route.ts, src/app/api/store/[id]/brand-profile/route.ts, src/app/api/store/[id]/brand-profile/infer/route.ts</files>
  <read_first>
    - src/app/api/store/[id]/brand-profile/generate-without-logo/route.ts (52-90 — profiler.generate + persistencia)
    - src/app/api/store/[id]/brand-profile/realign/route.ts (105-155 — text_only infer; 270-315 — director.analyze; 470-530 — profiler.generate mode regenerate)
    - src/app/api/store/[id]/brand-profile/route.ts (estrutura — aplicar mesmo padrao se gerar via profiler)
    - src/app/api/store/[id]/brand-profile/infer/route.ts (50-95 — service.infer + persistencia do perfil)
  </read_first>
  <behavior>
    - Teste 7: eventos brand_profile_vision + brand_profile_text gravados com custo real (buffer por sequencia)
    - Teste 8: delivery brand_profile_without_logo com custo NULL + duration_is_pipeline
    - Teste 9: realign text_only (112-114) -> call brand_profile_text; realign analysis (290-291) -> call brand_profile_vision; realign regenerate -> NOVO operationRunId
    - Teste 10: infer/route.ts grava call brand_profile_text com custo real + delivery brand_profile_without_logo sem custo (hoje ZERO eventos)
    - Teste 11: rota principal -> delivery brand_profile_with_logo com custo NULL
  </behavior>
  <action>
    1. Em `generate-without-logo/route.ts`: `const { operationRunId, traceId } = new AiCostTracker().startRun("brand_profile");` antes de `profiler.generate`; buffer local `const calls: AiCallInfo[] = []`; passar `onCall: (info) => { calls.push(info); }` no input; apos `generate()` resolver: para cada entrada do buffer gravar `recordCall` na sequencia (1a = brand_profile_vision, 2a = brand_profile_text — custo via resolveAiCost, attemptNumber 0, visualSignatureId do input quando houver); ao final gravar delivery `brand_profile_without_logo` SEM cost/tokens + `metadata.duration_is_pipeline: true`. Helper `recordCall` identico ao padrao 38-1-07 (imports de `@/lib/ai-cost`).
    2. Em `realign/route.ts` — instrumentar os TRES caminhos de IA (mesmo padrao de run/buffer/delivery):
       - path text_only (112-114): `service.infer({...}, timeoutMs, onCall)` — call `brand_profile_text` com custo real; delivery `brand_profile_without_logo` (text-only) com custo NULL + duration_is_pipeline. startRun("brand_profile") no inicio do path.
       - path analysis com logo (290-291): `director.analyze({..., onCall})` — call `brand_profile_vision` com custo real; delivery `brand_profile_with_logo` com custo NULL + duration_is_pipeline.
       - path regenerate (481-510): `mode: 'regenerate'` -> NOVO `startRun` (operationRunId distinto — 6.5 test 4); buffer vision/text + delivery.
    3. Em `route.ts` (rota principal): se gera perfil com logo via BrandProfiler, aplicar o mesmo padrao com delivery `brand_profile_with_logo`; se nao gera via profiler, registrar no SUMMARY a decisao.
    4. Em `infer/route.ts` (hoje SEM NENHUM evento): `const { operationRunId, traceId } = new AiCostTracker().startRun("brand_profile");` antes de `service.infer({...}, timeoutMs, onCall)`; apos resolver, gravar call `brand_profile_text` com custo real (resolveAiCost com usage do onCall) + delivery `brand_profile_without_logo` SEM cost/tokens + `metadata.duration_is_pipeline: true`. A persistencia do perfil (store_brand_profiles insert) NAO muda.
    5. Nao alterar persistencia/logica existente.
  </action>
  <verify>
    <automated>npm run typecheck; `grep -c "startRun" src/app/api/store/[id]/brand-profile/generate-without-logo/route.ts` == 1; `grep -c "startRun" src/app/api/store/[id]/brand-profile/realign/route.ts` >= 2 (text_only + analysis + regenerate — pelo menos 2 paths); `grep -c "startRun" src/app/api/store/[id]/brand-profile/infer/route.ts` == 1; `grep -c "brand_profile_vision" src/app/api/store/[id]/brand-profile/generate-without-logo/route.ts` >= 1; `grep -c "brand_profile_text" src/app/api/store/[id]/brand-profile/infer/route.ts` >= 1; `grep -c "duration_is_pipeline" src/app/api/store/[id]/brand-profile/generate-without-logo/route.ts` == 1; `grep -c "duration_is_pipeline" src/app/api/store/[id]/brand-profile/infer/route.ts` == 1</automated>
  </verify>
  <acceptance_criteria>
    - Vision/text call-level com custo real nas 4 rotas; delivery NULL + duration_is_pipeline
    - Regenerate = novo run; infer/route.ts sai de zero eventos para call+delivery
    - typecheck limpo
  </acceptance_criteria>
  <done>Rotas brand instrumentadas (custo real + delivery NULL + regenerate novo run + infer coberto)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Testes 6.5 (4 testes) + testes de rota infer</name>
  <files>src/app/api/store/[id]/brand-profile/generate-without-logo/__tests__/generate-route.test.ts, src/app/api/store/[id]/brand-profile/__tests__/route.test.ts, src/app/api/store/[id]/brand-profile/realign/__tests__/realign-route.test.ts, src/app/api/store/[id]/brand-profile/infer/__tests__/route.test.ts</files>
  <read_first>
    - src/app/api/store/[id]/brand-profile/generate-without-logo/__tests__/generate-route.test.ts (padrao de mocks)
    - src/app/api/store/[id]/brand-profile/realign/__tests__/realign-route.test.ts
    - src/app/api/store/[id]/brand-profile/__tests__/route.test.ts
    - src/app/api/store/[id]/brand-profile/infer/__tests__/route.test.ts
  </read_first>
  <behavior>
    (4 cenarios — tasks.md 6.5 + testes de infer)
    - Teste 12: brand_profile_vision com custo/tokens (1a chamada do buffer)
    - Teste 13: brand_profile_text com custo/tokens (2a chamada do buffer — path 2)
    - Teste 14: delivery without/with_logo com custo NULL + duration_is_pipeline (ambas as rotas)
    - Teste 15: regenerate novo run (operationRunId distinto do run anterior)
    - Teste 16 (infer): rota infer grava call brand_profile_text com custo real + delivery sem custo (antes: zero eventos)
  </behavior>
  <action>
    Adicionar describe "Brand cost accounting (6.5)" nos 3 arquivos de teste (distribuindo os cenarios pela rota correspondente: generate-route.test.ts = 12/13/14-without_logo; route.test.ts = 14-with_logo; realign-route.test.ts = 15) e describe "infer cost accounting" em `infer/__tests__/route.test.ts` (Teste 16). Padrao: mockInsert capturando inserts em array; mock de profiler.generate disparando onCall (visao e texto) com usage; mock de service.infer disparando onCall com usage; verificar operation_run_id/generation_type/custo real/duration_is_pipeline. Suites existentes devem continuar verdes.
    Rodar: `npx vitest run` nos 4 arquivos — verde.
  </action>
  <verify>
    <automated>npx vitest run "src/app/api/store/[id]/brand-profile/generate-without-logo/__tests__/generate-route.test.ts" "src/app/api/store/[id]/brand-profile/__tests__/route.test.ts" "src/app/api/store/[id]/brand-profile/realign/__tests__/realign-route.test.ts" "src/app/api/store/[id]/brand-profile/infer/__tests__/route.test.ts"; npm run typecheck; `grep -c "brand_profile_text" src/app/api/store/[id]/brand-profile/generate-without-logo/__tests__/generate-route.test.ts` >= 1; `grep -c "brand_profile_text" src/app/api/store/[id]/brand-profile/infer/__tests__/route.test.ts` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - 4 cenarios 6.5 verdes + teste de infer verde + suites existentes verdes
    - typecheck limpo
  </acceptance_criteria>
  <done>Brand coberta: 4 testes 6.5 + teste infer verdes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BrandProfiler onCall (visao/texto) -> rota buffer | Telemetria por chamada entra na rota aqui |
| recordCall -> tracker.record (supabaseAdmin) | Gravação best-effort — nunca bloqueia profiling |
| Rota principal (with_logo) -> delivery event | Entrega com logo — custo NULL (D1/D6) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38.1-38 | DoS | onCall/recordCall falhando nas rotas brand e servicos (analyze/infer) | mitigate | Best-effort (D7): onCall em try/catch no profiler (38-1-06), no analyze (brand-director) e no infer (text-only) + tracker.record nunca lança — F38.1-34 |
| T-38.1-39 | Tampering | Troca de ordem vision/text no mapeamento | mitigate | Ordem fixa do profiler (path 2: visao -> texto) testada; buffer por sequencia documentado — F38.1-36 |
| T-38.1-40 | Tampering | Delivery com custo (dupla contagem) | mitigate | Delivery SEM cost/tokens + duration_is_pipeline (D1/D6) — F38.1-35 |
| T-38.1-41 | Spoofing | Regenerate misturando runs | mitigate | Novo startRun por regenerate (testado 6.5 test 4) — F38.1-37 |
| T-38.1-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado neste plano — gate de legitimidade não se aplica |
</threat_model>

<verification>
- `npx vitest run` nos 4 arquivos de teste brand (3 rotas + infer) — 4 novos 6.5 + teste infer + existentes, 0 falhas
- `npx vitest run` nos 3 arquivos de teste de servico (brand-director, text-only, brand-profiler) — verdes
- `npm run typecheck` limpo
- Greps: startRun >= 1 em cada rota instrumentada (generate-without-logo, infer, realign >= 2); onCall >= 2 em brand-director.ts e text-only-inference-service.ts; duration_is_pipeline == 1 em generate-without-logo e infer
</verification>

<success_criteria>
- brand_profile_vision/text com custo real; delivery without/with_logo NULL + duration_is_pipeline
- infer/route.ts sai de zero eventos para call brand_profile_text + delivery sem custo
- realign com analyze (vision) + text_only (text) + regenerate (novo run) instrumentados
- Regenerate = novo run; onCall threadado pelo wrapper service
- 4 testes 6.5 + teste infer verdes
</success_criteria>

<output>
Create `.planning/phases/38-1-ai-cost-accounting/38-1-09-SUMMARY.md` when done
</output>

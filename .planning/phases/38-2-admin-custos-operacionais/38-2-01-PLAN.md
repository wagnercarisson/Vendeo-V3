---
phase: 38-2-admin-custos-operacionais
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "supabase/migrations/20260810000001_f38_2_economic_parameters.sql"
  - "supabase/migrations/20260810000002_f38_2_confidence_columns.sql"
  - "supabase/migrations/20260810000003_f38_2_operation_run_rpcs.sql"
autonomous: false
HAS_UI: 0
requirements:
  - F38.2-02
  - F38.2-03
  - F38.2-08
  - F38.2-09
  - F38.2-17
user_setup:
  - service: Supabase
    why: "supabase db push (Task 4) requires an authenticated CLI session or access token"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard → Account → Access Tokens (criar token) — ou sessão `supabase login` já ativa"
must_haves:
  truths:
    - "economic_parameters existe com key TEXT PK, value NUMERIC NOT NULL CHECK (value > 0), updated_by UUID REFERENCES auth.users(id), updated_at/created_at; RLS habilitado com REVOKE ALL FROM anon, authenticated (D2)"
    - "economic_parameter_audit é append-only (trigger bloqueia UPDATE/DELETE) com UNIQUE (operation_id) WHERE operation_id IS NOT NULL (idempotência) e reason NOT NULL (D2)"
    - "Seeds idempotentes ('usd_brl_rate', 1.00) e ('credit_value_brl', 1.00) com ON CONFLICT (key) DO NOTHING e updated_by NULL (D2)"
    - "RPC admin_set_economic_parameter(p_actor_id, p_key, p_value, p_reason, p_operation_id DEFAULT NULL) SECURITY DEFINER + SET search_path='' transacional (captura old → UPDATE → INSERT audit → JSONB { key, value, audit_id, updated_at, idempotent }); reason obrigatório; value > 0; idempotente por operation_id (D2)"
    - "generation_events ganhou 4 colunas novas IF NOT EXISTS (cost_formula_version TEXT, cost_estimation_note TEXT, text_component_usd REAL, image_tool_component_usd REAL) sem CHECK (D5)"
    - "RPC admin_get_ai_operation_runs (11 params) SECURITY DEFINER retorna { runs, summary, page, total } com resumo só call-level, evidências brutas de segmento (D9), insumos de badge (D5), summary antes de paginar, P95 via percentile_cont(0.95), limite de janela ≤90d/365d (D4)"
    - "RPC admin_get_ai_operation_run_events(p_operation_run_id UUID) SECURITY DEFINER retorna { run, events } com eventos call-level + P95 (D4)"
    - "admin_get_ai_costs (F38.1) e as views admin_ai_* NÃO foram tocadas; `creditos_debitados` REUTILIZA public.admin_cost_vs_credits via SQL dentro do RPC SECURITY DEFINER (reuso de reconciliação, sem duplicação); NENHUM .from() estilo cliente (camada app/service) nas views (F38.1 proíbe no cliente)"
  artifacts:
    - path: "supabase/migrations/20260810000001_f38_2_economic_parameters.sql"
      provides: "economic_parameters + audit + seeds 1.00 + RPC admin_set_economic_parameter + RLS (D2)"
      contains: "admin_set_economic_parameter"
    - path: "supabase/migrations/20260810000002_f38_2_confidence_columns.sql"
      provides: "4 colunas de confiança em generation_events (D5)"
      contains: "cost_estimation_note"
    - path: "supabase/migrations/20260810000003_f38_2_operation_run_rpcs.sql"
      provides: "RPCs admin_get_ai_operation_runs/_events (D4)"
      contains: "admin_get_ai_operation_runs"
  key_links:
    - from: "economic_parameter_audit (UNIQUE parcial operation_id)"
      to: "credit_operation_costs (F38.1-07 idempotência idx_credit_operation_cost_audit_idempotency)"
      via: "padrão F38 de idempotência por operation_id em tabelas de auditoria"
      pattern: "idx_credit_operation_cost_audit_idempotency"
    - from: "RPC admin_get_ai_operation_runs"
      to: "generation_events (call-level) + admin_cost_vs_credits"
      via: "COALESCE(provider_reported_cost_usd, estimated_cost_usd); créditos via admin_cost_vs_credits; evidências de segmento via stores + credit_transactions metadata"
      pattern: "COALESCE"
---

<objective>
Criar e aplicar as 3 migrations da F38.2: `20260810000001_f38_2_economic_parameters.sql` (tabelas `economic_parameters` + `economic_parameter_audit` + seeds 1.00 + RPC `admin_set_economic_parameter` + RLS service_role — D2), `20260810000002_f38_2_confidence_columns.sql` (4 colunas de confiança em `generation_events` — D5) e `20260810000003_f38_2_operation_run_rpcs.sql` (RPCs `admin_get_ai_operation_runs`/`admin_get_ai_operation_run_events` — D4). Aplicar as 3 no banco remoto via `supabase db push` ([BLOCKING] — a fase não passa na verificação sem isso).

Purpose: sem o schema evoluído nada downstream existe — o service de parâmetros (38-2-02), a API (38-2-04), o service de custos de operação (38-2-05), a API de runs (38-2-06) e a correção do `/admin/metrics` (38-2-09) dependem destas tabelas/RPCs/colunas.
Output: 3 migrations aplicadas no remoto + RPCs validadas em pg_proc.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-2-admin-custos-operacionais/38-2-CONTEXT.md
@openspec/changes/fase-38-2-admin-custos-operacionais/specs/economic-parameters/spec.md
@openspec/changes/fase-38-2-admin-custos-operacionais/specs/ai-operation-runs-api/spec.md

<interfaces>
Contratos que o executor precisa (extraídos de 38-2-CONTEXT.md D2/D4/D5 + specs):

**Sem UI (HAS_UI: 0). Nenhuma página/rota de UI é alterada por este plano.**

Migration 1 — `20260810000001_f38_2_economic_parameters.sql` (D2, padrão F38):
```sql
CREATE TABLE IF NOT EXISTS public.economic_parameters (
  key TEXT PRIMARY KEY,
  value NUMERIC NOT NULL CHECK (value > 0),
  updated_by UUID REFERENCES auth.users(id),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);
-- economic_parameter_audit: id UUID PK gen_random_uuid(); key TEXT NOT NULL; old_value NUMERIC;
-- new_value NUMERIC; actor_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE;
-- reason TEXT NOT NULL; operation_id UUID; created_at TIMESTAMPTZ NOT NULL DEFAULT now();
-- + UNIQUE INDEX idx_economic_parameter_audit_idempotency ON (operation_id) WHERE operation_id IS NOT NULL
-- + trigger imutável (bloqueia UPDATE/DELETE na audit)
```
- Seeds: `INSERT ... ('usd_brl_rate', 1.00) ON CONFLICT (key) DO NOTHING` e `('credit_value_brl', 1.00)`, `updated_by NULL`.
- RPC `admin_set_economic_parameter(p_actor_id UUID, p_key TEXT, p_value NUMERIC, p_reason TEXT, p_operation_id UUID DEFAULT NULL) RETURNS JSONB { key, value, audit_id, updated_at, idempotent }` — `LANGUAGE plpgsql SECURITY DEFINER SET search_path = ''`; reason obrigatório (RAISE EXCEPTION se NULL/vazio); value > 0 (RAISE); transacional: SELECT old → UPDATE economic_parameters → INSERT na audit (audit.id = gen_random_uuid() SEMPRE, independente do operation_id; operation_id = COALESCE(p_operation_id, gen_random_uuid()) é a CHAVE idempotente); retry com o MESMO operation_id → retorna o audit_id da PRIMEIRA linha com `idempotent: true` (sem INSERT duplicado). Grants: REVOKE EXECUTE FROM PUBLIC, anon, authenticated; GRANT EXECUTE TO service_role.
- RLS: `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` + `REVOKE ALL ... FROM anon, authenticated` nas duas tabelas (sem GRANT para authenticated).

Migration 2 — `20260810000002_f38_2_confidence_columns.sql` (D5):
```sql
ALTER TABLE public.generation_events
  ADD COLUMN IF NOT EXISTS cost_formula_version TEXT,
  ADD COLUMN IF NOT EXISTS cost_estimation_note TEXT,
  ADD COLUMN IF NOT EXISTS text_component_usd REAL,
  ADD COLUMN IF NOT EXISTS image_tool_component_usd REAL;
```
- Sem CHECK (D5). Retrocompatível com dados existentes (IF NOT EXISTS).

Migration 3 — `20260810000003_f38_2_operation_run_rpcs.sql` (D4):
- `admin_get_ai_operation_runs(p_period_start TIMESTAMPTZ, p_period_end TIMESTAMPTZ, p_store_id UUID, p_run_type TEXT, p_status TEXT, p_provider TEXT, p_model TEXT, p_generation_type TEXT, p_operation_run_id UUID, p_page INTEGER, p_page_size INTEGER) RETURNS JSONB { runs, summary, page, total }`:
  - CTE de runs agrupada por operation_run_id (só call-level: `generation_type NOT IN (delivery markers)`); por run: created_at (MIN/MAX de eventos), delivery_status (do delivery marker do run via subquery/JOIN, ou derivado de status dos eventos), custo_usd_total (SUM COALESCE(provider_reported_cost_usd, estimated_cost_usd)), creditos_debitados (**via `FROM public.admin_cost_vs_credits`** — reuso direto da view de reconciliação dentro do RPC SECURITY DEFINER; evita duplicar a lógica de reconciliação. A proibição de `.from()` vale para a camada de **app/service** (cliente Supabase), NÃO para SQL interno do RPC), duracao_total_ms (SUM), chamadas (COUNT), chamadas_success (COUNT FILTER status='success'), regeneracoes, provider/model principal (mode/mais frequente), cost_source principal
  - Evidências brutas de segmento (D9): store_is_test (JOIN stores), deduction_purchased_amount, deduction_bonus_amount, admin_grant_evidence (JOIN credit_transactions deduction metadata) — RPC NÃO classifica
  - Insumos de badge (D5): cost_sources (array distinct), cost_estimation_notes (array distinct), flags has_provider_reported / has_provisional_image_estimate / has_partial_estimate / has_not_available / has_estimated
  - `summary` sobre conjunto filtrado ANTES de paginar: custo_usd_total, creditos_debitados, duracao_total_ms, tempo_medio_ms (AVG), p95_ms (percentile_cont(0.95)), total, erros, sucessos
  - Limite de janela: `p_period_end - p_period_start > INTERVAL '365 days'` → RAISE/retorna código janela_excedente (service valida também no zod → 400); default da UI 90 dias
  - Paginação OFFSET/LIMIT + COUNT(*) total; ORDER BY created_at DESC
- `admin_get_ai_operation_run_events(p_operation_run_id UUID) RETURNS JSONB { run, events }`: eventos call-level do run (todas as colunas: generation_type, provider, model, status, error_type, attempt_number, duration_ms, prompt/completion/total/cached/image tokens, estimated_cost_usd, provider_reported_cost_usd, text_component_usd, image_tool_component_usd, cost_source, cost_formula_version, cost_estimation_note, metadata) + P95 de duração por chamada; `run` null + events [] quando id não existe.
- NÃO alterar `admin_get_ai_costs` (F38.1) nem as views. `creditos_debitados` REUTILIZA `public.admin_cost_vs_credits` via SQL (`FROM`/JOIN) dentro do RPC SECURITY DEFINER — reuso de lógica de reconciliação sem duplicação. A proibição de `.from()` (cliente Supabase `admin_*`) vale para a camada de app/service, NÃO para SQL interno do RPC (SECURITY DEFINER tem permissão de definer para ler as views).

REVERT obrigatório nas 3 migrations (comentário em ordem reversa).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 1 — economic_parameters + audit + seeds + RPC admin_set_economic_parameter + RLS</name>
  <files>supabase/migrations/20260810000001_f38_2_economic_parameters.sql</files>
  <read_first>
    - supabase/migrations/20260807000001_f38_create_credit_operation_costs.sql (padrão F38: banner, tabela com audit idempotente idx_credit_operation_cost_audit_idempotency linhas 20-30, trigger imutável, RLS service_role, RPC SECURITY DEFINER com search_path='' e transacional, seeds ON CONFLICT — linhas 1-21, 26-94, 102-232, 237-256)
    - openspec/changes/fase-38-2-admin-custos-operacionais/specs/economic-parameters/spec.md (linhas 31-252)
  </read_first>
  <action>
    Criar `supabase/migrations/20260810000001_f38_2_economic_parameters.sql` com banner de cabeçalho no estilo F38 (comentário PT-BR):

    1. **Bloco 1 — economic_parameters**: `CREATE TABLE IF NOT EXISTS public.economic_parameters` com schema EXATO do contrato D2 (key TEXT PRIMARY KEY, value NUMERIC NOT NULL CHECK (value > 0), updated_by UUID REFERENCES auth.users(id), updated_at/created_at TIMESTAMPTZ NOT NULL DEFAULT now()).

    2. **Bloco 2 — economic_parameter_audit**: `CREATE TABLE IF NOT EXISTS public.economic_parameter_audit` com schema D2 + `CREATE UNIQUE INDEX IF NOT EXISTS idx_economic_parameter_audit_idempotency ON public.economic_parameter_audit(operation_id) WHERE operation_id IS NOT NULL` (padrão F38 idx_credit_operation_cost_audit_idempotency). Trigger imutável: função `block_economic_parameter_audit_update_delete()` (RAISE EXCEPTION em UPDATE/DELETE) + `CREATE TRIGGER trg_economic_parameter_audit_immutable`.

    3. **Bloco 3 — Seeds**: 2 linhas com `ON CONFLICT (key) DO NOTHING` — `('usd_brl_rate', 1.00)` e `('credit_value_brl', 1.00)`, `updated_by NULL`.

    4. **Bloco 4 — RPC admin_set_economic_parameter**: `CREATE OR REPLACE FUNCTION public.admin_set_economic_parameter(p_actor_id UUID, p_key TEXT, p_value NUMERIC, p_reason TEXT, p_operation_id UUID DEFAULT NULL)` — assinatura EXATA D2, p_operation_id por ÚLTIMO com DEFAULT. `RETURNS JSONB`, `LANGUAGE plpgsql`, `SECURITY DEFINER`, `SET search_path = ''`. Corpo: valida p_key (not null), p_value (NOT NULL e > 0 → RAISE 'economic_parameter_value_positive'), p_reason (NULL/vazio → RAISE 'economic_parameter_reason_required'); transacional: `SELECT value INTO v_old FROM public.economic_parameters WHERE key = p_key`; `UPDATE ... SET value = p_value, updated_by = p_actor_id, updated_at = now() WHERE key = p_key` (INSERT se inexistente); idempotência por operation_id: `SELECT id INTO v_existing_audit_id FROM public.economic_parameter_audit WHERE operation_id = p_operation_id` — se linha existir, `RETURN jsonb_build_object('key', p_key, 'value', p_value, 'audit_id', v_existing_audit_id, 'updated_at', now(), 'idempotent', true)` (retry retorna o audit_id da PRIMEIRA linha, sem INSERT duplicado); caso contrário, INSERT na audit com `id = gen_random_uuid()` SEMPRE (o id da audit é independente do operation_id — operation_id é apenas a CHAVE idempotente, nunca o PK), registrando `operation_id = COALESCE(p_operation_id, gen_random_uuid())`; RETURN jsonb_build_object('key', p_key, 'value', p_value, 'audit_id', v_audit_id, 'updated_at', now(), 'idempotent', false). Grants: `REVOKE EXECUTE ON FUNCTION ... FROM PUBLIC, anon, authenticated;` + `GRANT EXECUTE ON FUNCTION ... TO service_role;`.

    5. **Bloco 5 — RLS**: `ALTER TABLE public.economic_parameters ENABLE ROW LEVEL SECURITY;` + `REVOKE ALL ON TABLE public.economic_parameters FROM anon, authenticated;` + o mesmo para economic_parameter_audit. NENHUM GRANT de SELECT para authenticated.

    6. **REVERT** comentado em ordem reversa (DROP FUNCTION, DROP TRIGGER, DROP FUNCTION trigger, DROP TABLE ×2, DROP INDEX).
  </action>
  <verify>
    <automated>grep -c "admin_set_economic_parameter" supabase/migrations/20260810000001_f38_2_economic_parameters.sql` >= 4 (CREATE + corpo + REVOKE + GRANT); `grep -c "SECURITY DEFINER"` == 1; `grep -c "SET search_path = ''"` == 1; `grep -c "ON CONFLICT (key) DO NOTHING"` == 2; `grep -c "idx_economic_parameter_audit_idempotency"` == 1; `grep -c "CHECK (value > 0)"` == 1; `grep -c "REVOKE ALL"` >= 2; `grep -c "ENABLE ROW LEVEL SECURITY"` == 2; `grep -c "reason"` >= 4; `grep -c "economic_parameter_reason_required"` == 1</automated>
  </verify>
  <acceptance_criteria>
    - economic_parameters com CHECK value > 0 + RLS service_role (sem GRANT authenticated)
    - economic_parameter_audit append-only (trigger bloqueia UPDATE/DELETE) + UNIQUE parcial operation_id + reason NOT NULL
    - Seeds 1.00/1.00 idempotentes com ON CONFLICT (key) DO NOTHING
    - RPC admin_set_economic_parameter SECURITY DEFINER + search_path='' + transacional + idempotente + reason obrigatório
    - REVERT presente
  </acceptance_criteria>
  <done>Migration 1 criada (parâmetros econômicos + audit + RPC + RLS) — pronta para as migrations 2/3</done>
</task>

<task type="auto">
  <name>Task 2: Migration 2 — colunas de confiança em generation_events (D5)</name>
  <files>supabase/migrations/20260810000002_f38_2_confidence_columns.sql</files>
  <read_first>
    - supabase/migrations/20260808000001_f38_1_create_ai_cost_accounting.sql (ALTER generation_events do padrão F38.1 — IF NOT EXISTS por coluna)
  </read_first>
  <action>
    Criar `supabase/migrations/20260810000002_f38_2_confidence_columns.sql` com banner e bloco único: `ALTER TABLE public.generation_events ADD COLUMN IF NOT EXISTS` das 4 colunas (D5) — `cost_formula_version TEXT`, `cost_estimation_note TEXT`, `text_component_usd REAL`, `image_tool_component_usd REAL`. SEM CHECK (D5 — valores livres para notas). NÃO alterar colunas existentes nem adicionar índices. REVERT comentado (DROP COLUMN IF EXISTS ×4).
  </action>
  <verify>
    <automated>grep -c "ADD COLUMN IF NOT EXISTS" supabase/migrations/20260810000002_f38_2_confidence_columns.sql` == 4; `grep -c "cost_estimation_note"` == 1; `grep -c "text_component_usd"` == 1; `grep -c "image_tool_component_usd"` == 1; `grep -c "CHECK"` == 0</automated>
  </verify>
  <acceptance_criteria>
    - 4 colunas com IF NOT EXISTS, sem CHECK, sem índices
    - REVERT presente
  </acceptance_criteria>
  <done>Migration 2 criada (4 colunas de confiança, retrocompatível)</done>
</task>

<task type="auto">
  <name>Task 3: Migration 3 — RPCs admin_get_ai_operation_runs + admin_get_ai_operation_run_events (D4)</name>
  <files>supabase/migrations/20260810000003_f38_2_operation_run_rpcs.sql</files>
  <read_first>
    - supabase/migrations/20260808000001_f38_1_create_ai_cost_accounting.sql (views admin_ai_* + RPC admin_get_ai_costs — padrão CTE/COALESCE/filtros p_* — blocos 8-10)
    - supabase/migrations/20260809000002_f38_1_apuracao_janela_margem.sql (adjustes janela/margem do F38.1)
    - openspec/changes/fase-38-2-admin-custos-operacionais/specs/ai-operation-runs-api/spec.md (linhas 3-90 — RPCs)
  </read_first>
  <action>
    Criar `supabase/migrations/20260810000003_f38_2_operation_run_rpcs.sql` com banner e:

    1. **Bloco 1 — RPC admin_get_ai_operation_runs**: `CREATE OR REPLACE FUNCTION public.admin_get_ai_operation_runs(p_period_start TIMESTAMPTZ, p_period_end TIMESTAMPTZ, p_store_id UUID, p_run_type TEXT, p_status TEXT, p_provider TEXT, p_model TEXT, p_generation_type TEXT, p_operation_run_id UUID, p_page INTEGER, p_page_size INTEGER) RETURNS JSONB` — `LANGUAGE plpgsql SECURITY DEFINER SET search_path = ''`.
       - Validação de janela: se `p_period_end - p_period_start > INTERVAL '365 days'` → `RAISE EXCEPTION 'window_exceeded_365d'` (o service/zod já valida, defesa em profundidade).
       - CTE `filtered_runs`: SELECT de events call-level (excluir delivery markers) agrupado por operation_run_id com os filtros p_* (COALESCE/NULLIF no padrão admin_get_ai_costs). Para cada run: `created_at` (MIN), `delivery_status` (JOIN/subquery no delivery marker do run — `generation_type` em delivery list — para sucesso/falha da entrega; fallback derivado de status dos eventos), `custo_usd_total` (SUM COALESCE(provider_reported_cost_usd, estimated_cost_usd)), `duracao_total_ms` (SUM duration_ms), `chamadas` (COUNT), `chamadas_success` (COUNT FILTER status='success'), `regeneracoes` (MAX(attempt_number) - 1 com GREATEST floor), provider/model principal (mode por run — subquery de agrupamento), `cost_source` principal (mode).
       - **Evidências de segmento (D9)** via JOIN: `store_is_test` (JOIN public.stores s ON s.id = ge.store_id → s.is_test_store), `deduction_purchased_amount` / `deduction_bonus_amount` (JOIN/agregação de `credit_transactions` tipo deduction por operation_run/campaign, metadata->>'purchased_amount'/'bonus_amount' — shape real a confirmar na implementação; sem evidência → NULL), `admin_grant_evidence` (metadata de deduction com grant — confirmar shape real; sem evidência → NULL). RPC NÃO classifica segmento.
       - **Insumos de badge (D5)**: `cost_sources` (array_agg DISTINCT), `cost_estimation_notes` (array_agg DISTINCT), flags booleans has_provider_reported / has_provisional_image_estimate / has_partial_estimate / has_not_available / has_estimated derivadas de cost_source + note.
       - `summary` sobre o conjunto filtrado (SEM paginação): custo_usd_total SUM, creditos_debitados (reuso via `FROM public.admin_cost_vs_credits` — mesma fonte da coluna por run), duracao_total_ms SUM, tempo_medio_ms AVG, p95_ms percentile_cont(0.95) WITHIN GROUP, total COUNT, erros COUNT FILTER delivery_status='failed', sucessos.
       - Paginação: `p_page`/`p_page_size` (page_size default 25, max 100) com OFFSET/LIMIT sobre o conjunto ordenado por created_at DESC; `total` = COUNT do conjunto filtrado.
       - `RETURN jsonb_build_object('runs', ..., 'summary', ..., 'page', p_page, 'total', v_total)`.
       - Grants: REVOKE EXECUTE FROM PUBLIC, anon, authenticated; GRANT EXECUTE TO service_role.

    2. **Bloco 2 — RPC admin_get_ai_operation_run_events**: `CREATE OR REPLACE FUNCTION public.admin_get_ai_operation_run_events(p_operation_run_id UUID) RETURNS JSONB` — `LANGUAGE plpgsql SECURITY DEFINER SET search_path = ''`.
       - `run`: resumo da entrega (mesma apuração do bloco 1 filtrada por p_operation_run_id — reuso de lógica, não de função).
       - `events`: SELECT call-level do run — todas as colunas do contrato D4 (generation_type, provider, model, status, error_type, attempt_number, duration_ms, prompt_tokens, completion_tokens, total_tokens, cached_input_tokens, image_tokens, estimated_cost_usd, provider_reported_cost_usd, text_component_usd, image_tool_component_usd, cost_source, cost_formula_version, cost_estimation_note, metadata) ordenadas por created_at.
       - P95 de duração por chamada no run.
       - run NULL + events [] quando id não existe.
       - Grants: REVOKE EXECUTE FROM PUBLIC, anon, authenticated; GRANT EXECUTE TO service_role.

    3. **REVERT** comentado (DROP FUNCTION ×2). NENHUMA alteração em admin_get_ai_costs (F38.1) nem nas views; reuso de `public.admin_cost_vs_credits` via SQL dentro do RPC é permitido (SECURITY DEFINER); a proibição de `.from()` vale para a camada de app/service (cliente Supabase), não para o SQL da migration.
  </action>
  <verify>
    <automated>grep -c "admin_get_ai_operation_runs" supabase/migrations/20260810000003_f38_2_operation_run_rpcs.sql` >= 4 (CREATE + corpo + REVOKE + GRANT); `grep -c "admin_get_ai_operation_run_events"` >= 4; `grep -c "SECURITY DEFINER"` == 2; `grep -c "SET search_path = ''"` == 2; `grep -c "percentile_cont"` >= 2 (summary + eventos); `grep -c "COALESCE"` >= 2; `grep -c "store_is_test"` == 1; `grep -c "cost_sources"` >= 1; `grep -c "has_provider_reported"` >= 1; `grep -c "text_component_usd"` >= 1; `grep -c "REVERT"` == 1; `grep -c "window_exceeded"` == 1; `grep -c "admin_cost_vs_credits"` >= 1 (reuso da view de reconciliação no SQL do RPC); `grep -c ".from("` == 0 (nenhum `.from()` estilo cliente no SQL da migration — reuso é via SQL `FROM`/JOIN)</automated>
  </verify>
  <acceptance_criteria>
    - RPC lista com 11 params, resumo só call-level, evidências de segmento (D9), insumos de badge (D5), summary antes de paginar, P95, paginação + total, limite 365d
    - RPC detalhe com eventos call-level completos + P95 + run null quando id não existe
    - creditos_debitados reusa `public.admin_cost_vs_credits` via SQL (sem duplicar lógica de reconciliação); admin_get_ai_costs e views intactos; nenhum `.from()` estilo cliente no SQL
    - REVERT presente
  </acceptance_criteria>
  <done>Migration 3 criada (RPCs de operation runs) — as 3 migrations prontas para push</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 4: [BLOCKING] Schema push — supabase db push + validação dos RPCs/tabelas</name>
  <files>supabase/migrations/20260810000001_f38_2_economic_parameters.sql, supabase/migrations/20260810000002_f38_2_confidence_columns.sql, supabase/migrations/20260810000003_f38_2_operation_run_rpcs.sql</files>
  <read_first>
    - supabase/config.toml (projeto linkado — `.temp/` presente)
    - .env.local ou ambiente (presença de SUPABASE_ACCESS_TOKEN)
  </read_first>
  <action>
    Aplicar as 3 migrations no banco remoto (mesmo fluxo do 38-1-01 Task 3 — o projeto já está linkado):

    1. Se `SUPABASE_ACCESS_TOKEN` não estiver no ambiente, procurar em `.env.local`/secrets; se ausente, pedir ao usuário o token (Supabase Dashboard → Account → Access Tokens) via checkpoint — a tarefa é [BLOCKING] e o plano é `autonomous: false`.

    2. Rodar `supabase db push` na raiz do repo com `$env:SUPABASE_ACCESS_TOKEN` setado (workaround non-TTY). Se o push pedir confirmação interativa que não pode ser suprimida, parar e sinalizar intervenção manual.

    3. Validar: `supabase migration list` deve mostrar as 3 migrations `20260810000001_f38_2_economic_parameters`, `20260810000002_f38_2_confidence_columns`, `20260810000003_f38_2_operation_run_rpcs` como aplicadas no remoto; `supabase db push` deve sair com código 0 sem erro de SQL.

    4. Validação funcional (se o banco remoto permitir consulta direta): `SELECT proname, prosecdef FROM pg_proc WHERE proname IN ('admin_set_economic_parameter','admin_get_ai_operation_runs','admin_get_ai_operation_run_events');` deve retornar 3 linhas com `prosecdef = true`; `SELECT key, value FROM public.economic_parameters ORDER BY key;` deve retornar as 2 seeds 1.00 (via service_role); `SELECT column_name FROM information_schema.columns WHERE table_name='generation_events' AND column_name IN ('cost_formula_version','cost_estimation_note','text_component_usd','image_tool_component_usd');` deve retornar 4. Se não houver acesso de consulta, registrar a validação do push (migration list) como evidência. Validação SQL completa I1–I6 fica para a verificação da fase (38-2-10) — aqui basta o push + presença em pg_proc/information_schema.

    NÃO rodar os testes de integração SQL completos neste plano.
  </action>
  <verify>
    <automated>supabase migration list — as 3 migrations 2026081000000x_f38_2_* presentes no remoto; exit code 0 do `supabase db push`</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` terminou com sucesso (exit 0) OU intervenção manual registrada com evidência
    - `supabase migration list` mostra as 3 migrations `f38_2_*` no remoto
    - `admin_set_economic_parameter`, `admin_get_ai_operation_runs`, `admin_get_ai_operation_run_events` em pg_proc (prosecdef=true); seeds 1.00/1.00 em economic_parameters; 4 colunas novas em generation_events (ou evidência equivalente)
    - Se o push exigiu autenticação, o token foi obtido do usuário e o push concluído (checkpoint resolvido)
  </acceptance_criteria>
  <done>Schema aplicado: parâmetros + audit + colunas de confiança + RPCs no banco remoto — pré-requisito obrigatório para toda a fase</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RPC admin_set_economic_parameter ← service_role | Mutação econômica (parâmetros internos de conversão) executada como definer (SECURITY DEFINER) — única via de escrita em economic_parameters; auditoria obrigatória |
| RPC admin_get_ai_operation_runs/_events ← service_role | Leitura de apuração por entrega executada como definer — acessa generation_events (default-deny), credit_transactions e stores |
| economic_parameters / economic_parameter_audit | Dados econômicos internos — `authenticated` não pode ler nem escrever (RLS service_role-only) |
| generation_events (4 colunas novas) | Dados de confiança de custo — RLS default-deny mantido; escrita apenas via service_role |
| views admin_ai_* + admin_get_ai_costs | Intocados — sem regressão na apuração F38.1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38.2-01 | Elevation of Privilege | RPCs admin_* novos | mitigate | `SECURITY DEFINER` + `SET search_path = ''` (impede hijack de search_path — ASVS 4.1.1/4.3.1); `REVOKE EXECUTE FROM PUBLIC, anon, authenticated`; `GRANT EXECUTE TO service_role` apenas — F38.2-03/F38.2-08/F38.2-09 |
| T-38.2-02 | Tampering | economic_parameters / audit | mitigate | RLS habilitado + `REVOKE ALL FROM anon, authenticated`; audit append-only (trigger bloqueia UPDATE/DELETE) + reason NOT NULL + UNIQUE parcial operation_id (idempotência) — F38.2-02 |
| T-38.2-03 | Information Disclosure | Parâmetros econômicos (taxa/margem interna) | mitigate | Sem GRANT SELECT para authenticated; único acesso via RPC definer e API admin sob requireAdmin — F38.2-02 |
| T-38.2-04 | Tampering | Insumos de badge/segmento persistidos | mitigate | Colunas IF NOT EXISTS sem CHECK (D5 — valores livres para notas); escrita apenas via service_role (default-deny) — F38.2-08 |
| T-38.2-05 | Injection | Filtros dos RPCs novos | mitigate | Parâmetros vinculados (p_* via argumentos da função, nunca concatenação de strings); `SET search_path = ''`; sem dynamic SQL — F38.2-08 |
| T-38.2-06 | DoS | admin_get_ai_operation_runs sem limite de janela | mitigate | Janela limitada: default ≤ 90d, max 365d → RAISE `window_exceeded_365d` no RPC + validação 400 no zod do service/API; paginação OFFSET/LIMIT obrigatória — F38.2-08 |
| T-38.2-07 | Spoofing | Segmento/evidência de segmento incorretos | mitigate | RPC expõe APENAS evidência bruta (store_is_test, purchased/bonus amounts, admin_grant_evidence); classificação é do service (D9) — nunca inferir errado, fallback `unknown` — F38.2-08 |
| T-38.2-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo é instalado neste plano — gate de legitimidade não se aplica |
</threat_model>

<verification>
- `supabase db push` aplicado; `supabase migration list` confirma as 3 migrations `2026081000000x_f38_2_*` no remoto
- RPCs `admin_set_economic_parameter`, `admin_get_ai_operation_runs`, `admin_get_ai_operation_run_events` em pg_proc (prosecdef = true); seeds 1.00/1.00 em economic_parameters; 4 colunas novas em generation_events
- Validação SQL completa (I1–I6) fica para a verificação da fase (38-2-10)
</verification>

<success_criteria>
- economic_parameters + audit + seeds 1.00/1.00 + RPC admin_set_economic_parameter no remoto com RLS service_role-only
- generation_events com as 4 colunas de confiança (D5)
- RPCs admin_get_ai_operation_runs/_events no remoto com filtros, paginação, P95, evidências de segmento, insumos de badge
- `admin_get_ai_costs` (F38.1) e views admin_ai_* intactos
</success_criteria>

<output>
Create `.planning/phases/38-2-admin-custos-operacionais/38-2-01-SUMMARY.md` when done
</output>

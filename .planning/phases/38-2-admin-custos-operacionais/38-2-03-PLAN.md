---
phase: 38-2-admin-custos-operacionais
plan: 03
type: execute
wave: 2
depends_on:
  - "38-2-01"
files_modified:
  - "src/lib/ai-cost/tracker.ts"
  - "src/lib/ai-cost/types.ts"
  - "src/lib/ai-cost/__tests__/tracker.test.ts"
autonomous: true
HAS_UI: 0
requirements:
  - F38.2-16
must_haves:
  truths:
    - "CostResolution (src/lib/ai-cost/types.ts) já carrega costFormulaVersion, costEstimationNote, textComponentUsd, imageToolComponentUsd (F38.1 closure) — NÃO duplicar; apenas garantir contrato"
    - "AiCostTracker.record passa a persistir cost_formula_version, cost_estimation_note, text_component_usd, image_tool_component_usd a partir do CostResolution — daqui para frente (D5), sem reclassificar histórico"
    - "Eventos anteriores à migration (colunas NULL) caem em badge genérico na UI (38-2-05/08) — o tracker não toca histórico"
    - "4 testes do tracker (12.3): record persiste 4 campos; evento sem nota → NULL; provider_reported → badge (mapa 38-2-05); nota provisional + pricing_table → badge provisional image tool estimate (mapa 38-2-05)"
  artifacts:
    - path: "src/lib/ai-cost/tracker.ts"
      provides: "record persistindo os 4 campos de confiança (D5)"
      contains: "cost_estimation_note"
    - path: "src/lib/ai-cost/types.ts"
      provides: "CostResolution com campos de confiança (já presentes — garantia de contrato)"
      contains: "costFormulaVersion"
    - path: "src/lib/ai-cost/__tests__/tracker.test.ts"
      provides: "Testes de persistência de confiança (12.3 — 4 testes novos)"
      contains: "cost_estimation_note"
  key_links:
    - from: "src/lib/ai-cost/tracker.ts"
      to: "generation_events (cost_formula_version, cost_estimation_note, text_component_usd, image_tool_component_usd)"
      via: "insert via supabaseAdmin com as 4 colunas novas da migration 38-2-01 (D5)"
      pattern: "cost_estimation_note"
    - from: "src/lib/ai-cost/types.ts (CostResolution)"
      to: "src/lib/ai-cost/operation-runs-service.ts (38-2-05)"
      via: "badges derivados de cost_source + cost_estimation_note (D5)"
      pattern: "costEstimationNote"
---

<objective>
Persistir a confiança da estimativa de custo (D5): `AiCostTracker.record` passa a gravar `cost_formula_version`, `cost_estimation_note`, `text_component_usd` e `image_tool_component_usd` em `generation_events` a partir do `CostResolution` — **daqui para frente** (eventos anteriores à migration 38-2-01 ficam com NULL e caem em badge genérico na UI). O contrato `CostResolution` em `src/lib/ai-cost/types.ts` já carrega os 4 campos (fechamento F38.1) — este plano garante o contrato e implementa a persistência no tracker. Testes (tarefa 12.3 — 4 testes novos).

Purpose: sem persistência, a UI de Custos de Operação (38-2-08) não consegue mostrar os badges de confiança (`provisional image tool estimate`, `partial`, `estimated`, `provider_reported`, `not_available`) no histórico — o finding F1 da F38.2 (confiança computada e descartada no record).
Output: tracker persistindo os 4 campos de confiança + testes verdes.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-2-admin-custos-operacionais/38-2-CONTEXT.md
@openspec/changes/fase-38-2-admin-custos-operacionais/specs/ai-cost-tracker/spec.md

<interfaces>
Contratos que o executor precisa (extraídos do spec ai-cost-tracker delta D5 + 38-2-CONTEXT.md + leitura dos arquivos):

**Sem UI (HAS_UI: 0). Nenhuma página/rota de UI é alterada por este plano.**

Estado verificado (38-2-CONTEXT.md F1 + leitura):
- `CostResolution` em `src/lib/ai-cost/types.ts:59-91` **JÁ TEM** `costFormulaVersion?: string`, `costEstimationNote?: string`, `textComponentUsd?: number`, `imageToolComponentUsd?: number` (fechamento F38.1 — comments nas linhas 65-86). NÃO duplicar.
- `AiCostTracker.record` em `src/lib/ai-cost/tracker.ts:30-69` hoje NÃO persiste os 4 campos — o mapping no insert termina em `pricing_version` (linha 57).

Mudança no tracker:
```typescript
// no objeto de insert de record, após pricing_version:
cost_formula_version: event.cost?.costFormulaVersion ?? null,
cost_estimation_note: event.cost?.costEstimationNote ?? null,
text_component_usd: event.cost?.textComponentUsd ?? null,
image_tool_component_usd: event.cost?.imageToolComponentUsd ?? null,
```
- Mantém `best-effort` (try/catch + console.error + nunca lança — D7).
- Mantém delivery marker (sem cost/tokens → colunas NULL + duration_is_pipeline).
- Mantém a coluna `metadata` como está — os 4 campos agora viram colunas próprias (a F38.1 os mantinha só no metadata do evento campaign_image; agora são estruturados — D5).
- NÃO reclassifica histórico: nenhum UPDATE em linhas antigas.

Derivação de badge (NÃO é deste plano — mapa consumido em 38-2-05/08, referência p/ os testes):
| Badge | Condição |
|-------|----------|
| provider_reported | cost_source = 'provider_reported' |
| provisional image tool estimate | cost_source = 'pricing_table' E cost_estimation_note = 'provisional_image_tool_unit_cost_until_provider_reconciliation' |
| partial | cost_source = 'manual_unknown' OU pricing_table com nota de estimativa parcial |
| estimated | cost_source = 'pricing_table' (sem nota) ou fallback_static |
| not_available | cost_source = 'not_available' |
| estimated (genérico) | cost_source presente mas nota NULL |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Garantia de contrato — CostResolution com os 4 campos (D5)</name>
  <files>src/lib/ai-cost/types.ts</files>
  <read_first>
    - src/lib/ai-cost/types.ts (CostResolution — linhas 59-91; já tem os 4 campos do fechamento F38.1)
    - openspec/changes/fase-38-2-admin-custos-operacionais/specs/ai-cost-tracker/spec.md
  </read_first>
  <behavior>
    - Teste 1 (compile-time): `CostResolution` aceita `{ estimatedCostUsd, costSource: "pricing_table", costFormulaVersion: "responses_image_generation_v2", costEstimationNote: "provisional_image_tool_unit_cost_until_provider_reconciliation", textComponentUsd: 0.01, imageToolComponentUsd: 0.04 }`
  </behavior>
  <action>
    Verificar que `src/lib/ai-cost/types.ts` já exporta os 4 campos em `CostResolution` (costFormulaVersion, costEstimationNote, textComponentUsd, imageToolComponentUsd — presentes desde o fechamento F38.1). Se presentes, NÃO alterar nada além de ajustar o JSDoc se necessário (mencionar persistência D5 da F38.2). Se algum campo faltar (regressão), adicioná-lo no estilo existente. NÃO adicionar `server-only`; NÃO mudar a assinatura de outros contratos.
  </action>
  <verify>
    <automated>npm run typecheck; `grep -c "costFormulaVersion" src/lib/ai-cost/types.ts` >= 1; `grep -c "costEstimationNote" src/lib/ai-cost/types.ts` >= 1; `grep -c "textComponentUsd" src/lib/ai-cost/types.ts` >= 1; `grep -c "imageToolComponentUsd" src/lib/ai-cost/types.ts` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - CostResolution com os 4 campos de confiança (já presentes ou restaurados)
    - typecheck limpo
  </acceptance_criteria>
  <done>Contrato CostResolution garantido com os 4 campos (D5)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AiCostTracker.record — persistir os 4 campos de confiança (D5)</name>
  <files>src/lib/ai-cost/tracker.ts</files>
  <read_first>
    - src/lib/ai-cost/tracker.ts (record — linhas 30-69; mapping termina em pricing_version na 57)
    - src/lib/ai-cost/types.ts (CostResolution — campos opcionais)
  </read_first>
  <behavior>
    - Teste 2: `record(event)` com evento completo (costSource pricing_table + costFormulaVersion + costEstimationNote provisional + textComponentUsd + imageToolComponentUsd) → `mockInsert` chamado com as 4 colunas novas preenchidas
    - Teste 3: `record(event)` com CostResolution sem nota/componentes (costSource só) → colunas cost_formula_version/cost_estimation_note/text_component_usd/image_tool_component_usd NULL (badge genérico na UI)
    - Teste 4: `record(event)` com provider_reported → cost_source persistido (badge provider_reported derivado em 38-2-05); delivery marker (sem cost) → colunas de confiança NULL + duration_is_pipeline
  </behavior>
  <action>
    Em `src/lib/ai-cost/tracker.ts`, dentro do objeto de insert de `record`, após a linha `pricing_version: event.cost?.pricingVersion ?? null` (linha 57), adicionar:
    ```
    cost_formula_version: event.cost?.costFormulaVersion ?? null,
    cost_estimation_note: event.cost?.costEstimationNote ?? null,
    text_component_usd: event.cost?.textComponentUsd ?? null,
    image_tool_component_usd: event.cost?.imageToolComponentUsd ?? null,
    ```
    NÃO alterar o fluxo best-effort (try/catch + console.error + resolve — D7), NÃO alterar o delivery marker (cost/tokens NULL + duration_is_pipeline), NÃO adicionar UPDATE/backfill de histórico (D5 — daqui para frente). Atualizar o JSDoc da classe/método para citar a persistência das 4 colunas.
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai-cost/__tests__/tracker.test.ts; npm run typecheck; `grep -c "cost_formula_version" src/lib/ai-cost/tracker.ts` == 1; `grep -c "cost_estimation_note" src/lib/ai-cost/tracker.ts` == 1; `grep -c "text_component_usd" src/lib/ai-cost/tracker.ts` == 1; `grep -c "image_tool_component_usd" src/lib/ai-cost/tracker.ts` == 1</automated>
  </verify>
  <acceptance_criteria>
    - record persiste os 4 campos de confiança a partir do CostResolution
    - Sem nota/componentes → colunas NULL (badge genérico)
    - Best-effort e delivery marker intactos; sem backfill de histórico
    - typecheck limpo
  </acceptance_criteria>
  <done>Tracker persistindo os 4 campos de confiança — daqui para frente (D5)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Testes de persistência de confiança (tarefa 12.3 — 4 testes)</name>
  <files>src/lib/ai-cost/__tests__/tracker.test.ts</files>
  <read_first>
    - src/lib/ai-cost/__tests__/tracker.test.ts (padrão existente de mock do tracker — vi.mock supabase + table-dispatcher + client injetado)
    - openspec/changes/fase-38-2-admin-custos-operacionais/specs/ai-cost-tracker/spec.md
  </read_first>
  <behavior>
    - Casos do spec (tarefa 12.3 — 4 testes novos, adicionar aos 8 existentes da F38.1):
      1. record com CostResolution completo (nota provisional + componentes) → insert com as 4 colunas novas preenchidas (cost_formula_version, cost_estimation_note, text_component_usd, image_tool_component_usd)
      2. record sem nota/componentes (cost_source só) → colunas de confiança NULL (histórico pré-D5 → badge genérico)
      3. record com provider_reported → cost_source persistido; badge derivado em 38-2-05 (teste aqui apenas a persistência)
      4. nota provisional + pricing_table → cost_estimation_note persistida (insumo para badge provisional image tool estimate em 38-2-05)
  </behavior>
  <action>
    Estender `src/lib/ai-cost/__tests__/tracker.test.ts` com os 4 casos da behavior list (adicionar aos testes existentes da F38.1 — não removê-los). Reutilizar o mock existente (vi.mock supabase/server, mockFrom table-dispatcher, mockInsert, new AiCostTracker(mockClient)). Verificar o objeto passado a `mockInsert` (expect.objectContaining com as 4 colunas).
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai-cost/__tests__/tracker.test.ts; `grep -c "cost_estimation_note" src/lib/ai-cost/__tests__/tracker.test.ts` >= 2; `grep -c "provisional_image_tool_unit_cost_until_provider_reconciliation" src/lib/ai-cost/__tests__/tracker.test.ts` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - 4 testes novos de persistência passando (total da suite do tracker = 12)
    - Testes F38.1 existentes intactos e verdes
  </acceptance_criteria>
  <done>4 testes de persistência de confiança verdes (tarefa 12.3)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AiCostTracker.record → generation_events (supabaseAdmin) | Escrita de telemetria com colunas de confiança via service_role; best-effort; nunca bloqueia geração |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38.2-11 | Tampering | Nota/componentes de confiança persistidos | mitigate | Colunas IF NOT EXISTS sem CHECK (D5 — notas livres); escrita apenas via service_role (default-deny); nunca altera histórico — F38.2-16 |
| T-38.2-12 | Spoofing | Badge derivado incorretamente (UI) | mitigate | Persistência fiel do cost_source + nota; a DERIVAÇÃO de badge fica no service (38-2-05) com mapa determinístico — este plano só persiste insumos — F38.2-16 |
| T-38.2-13 | DoS | record em falha de escrita | mitigate | Best-effort: try/catch + console.error + resolve sem lançar (D7) — F38.2-16 |
| T-38.2-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado neste plano — gate de legitimidade não se aplica |
</threat_model>

<verification>
- `npm run typecheck` limpo (contrato + tracker)
- `npx vitest run src/lib/ai-cost/__tests__/tracker.test.ts` — suite completa (8 F38.1 + 4 novos), 0 falhas
- 4 colunas de confiança no insert do record; sem backfill
</verification>

<success_criteria>
- CostResolution com os 4 campos (garantido)
- AiCostTracker.record persiste os 4 campos de confiança (D5, daqui para frente)
- 4 testes novos verdes; suite do tracker sem regressão
</success_criteria>

<output>
Create `.planning/phases/38-2-admin-custos-operacionais/38-2-03-SUMMARY.md` when done
</output>

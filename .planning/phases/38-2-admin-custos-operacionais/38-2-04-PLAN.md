---
phase: 38-2-admin-custos-operacionais
plan: 04
type: execute
wave: 3
depends_on:
  - "38-2-02"
files_modified:
  - "src/lib/admin/schemas.ts"
  - "src/app/api/admin/economic-parameters/route.ts"
  - "src/app/api/admin/economic-parameters/__tests__/route.test.ts"
autonomous: true
HAS_UI: 0
requirements:
  - F38.2-05
  - F38.2-06
must_haves:
  truths:
    - "UpdateEconomicParameterRequestSchema em src/lib/admin/schemas.ts: key enum ECONOMIC_PARAMETER_KEYS, value number > 0, reason obrigatório (min 1), operationId uuid opcional (idempotência)"
    - "GET /api/admin/economic-parameters → 200 { parameters: [...] } via EconomicParameterService.getAll() (source table/fallback); 403 sem admin; 503 fail-closed"
    - "PUT /api/admin/economic-parameters → zod + RPC admin_set_economic_parameter; 200 { parameter, auditId, updatedAt, idempotent }; 400 zod; 403 sem admin; 500 erro do RPC"
    - "Nenhum endpoint público (parâmetros são dado interno de operação)"
    - "5 testes da API (12.2): PUT atualiza via RPC + audit; sem reason → 400; key inválido/value <= 0 → 400; GET/PUT sem admin → 403; idempotência por operation_id"
  artifacts:
    - path: "src/lib/admin/schemas.ts"
      provides: "UpdateEconomicParameterRequestSchema (D2)"
      contains: "UpdateEconomicParameterRequestSchema"
    - path: "src/app/api/admin/economic-parameters/route.ts"
      provides: "GET lista + PUT via RPC"
      exports: ["GET", "PUT"]
    - path: "src/app/api/admin/economic-parameters/__tests__/route.test.ts"
      provides: "5 testes da API (tarefa 12.2)"
      contains: "admin_set_economic_parameter"
  key_links:
    - from: "src/app/api/admin/economic-parameters/route.ts (PUT)"
      to: "public.admin_set_economic_parameter (RPC)"
      via: "rpc('admin_set_economic_parameter', { p_actor_id, p_key, p_value, p_reason, p_operation_id })"
      pattern: "admin_set_economic_parameter"
    - from: "src/app/api/admin/economic-parameters/route.ts (GET)"
      to: "src/lib/economic/economic-parameter-service.ts"
      via: "EconomicParameterService.getAll()"
      pattern: "EconomicParameterService"
---

<objective>
Criar a API admin de Configurações Econômicas (D2): `GET /api/admin/economic-parameters` (lista resolvida via `EconomicParameterService.getAll()` com `source` table/fallback — 200/403/503) e `PUT /api/admin/economic-parameters` (zod + RPC `admin_set_economic_parameter` — 200 { parameter, auditId, updatedAt, idempotent } / 400 / 403 / 500). Schema `UpdateEconomicParameterRequestSchema` em `src/lib/admin/schemas.ts`. **Sem endpoint público.** Testes (tarefa 12.2 — 5 testes).

Purpose: os dois parâmetros econômicos (`usd_brl_rate`, `credit_value_brl`) tornam-se administráveis sem deploy, com motivo obrigatório e auditoria — a página /admin/operation-costs (38-2-07) consome esta API. Nenhum parâmetro altera ledger nem transações históricas (D1/D2).
Output: API GET/PUT testada + schema zod.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-2-admin-custos-operacionais/38-2-CONTEXT.md
@openspec/changes/fase-38-2-admin-custos-operacionais/specs/economic-parameters/spec.md

<interfaces>
Contratos que o executor precisa (extraídos do spec economic-parameters D2 + 38-2-CONTEXT.md + leitura dos arquivos):

**Sem UI (HAS_UI: 0). Nenhuma página/rota de UI é alterada por este plano.**

Schema (`src/lib/admin/schemas.ts` — adicionar ao arquivo existente, no estilo de UpdateOperationCostRequestSchema/AiModelPricingUpdateSchema):
```typescript
export const UpdateEconomicParameterRequestSchema = z.object({
  key: z.enum(ECONOMIC_PARAMETER_KEYS),
  value: z.number().positive("Value deve ser maior que zero"),
  reason: z.string().min(1, "Motivo obrigatório"),
  operationId: z.string().uuid().optional(),  // idempotência
});
```
- import `ECONOMIC_PARAMETER_KEYS` de `@/lib/economic/types`.

GET (`src/app/api/admin/economic-parameters/route.ts`):
- `apiHandler` + `requireAdmin()` (padrão `src/app/api/admin/ai-costs/route.ts` — linhas 1-57).
- `new EconomicParameterService().getAll()` → `NextResponse.json({ parameters })`.
- `EconomicParameterUnavailableError` → 503 `{ error: "economic_parameters_unavailable" }`.
- NENHUM parâmetro de query/body — leitura pura.

PUT (mesma rota):
- Body parseado com `UpdateEconomicParameterRequestSchema`; `!parsed.success` → 400 `{ error: "Dados inválidos", details }`.
- Chama `client.rpc("admin_set_economic_parameter", { p_actor_id, p_key, p_value, p_reason, p_operation_id })` — p_actor_id = usuário autenticado (requireAdmin retorna o user; seguir o padrão de outras rotas admin que pegam o actor). 
- Sucesso → 200 `{ parameter: { key, value }, auditId, updatedAt, idempotent }` (do JSONB do RPC).
- Erro do RPC → 500 `{ error: "economic_parameter_update_failed" }`.
- 403 sem admin (requireAdmin lança — padrão da rota ai-costs).

Padrão de teste de rota (analog: `src/app/api/admin/ai-costs/__tests__/route.test.ts`): mock de requireAdmin (admin logado), mock do service/RPC via vi.mock, chamadas fetch ao handler.

Atores/actor_id: verificar como as rotas admin existentes obtêm o user id (ex.: `src/lib/admin/require-admin.ts` ou `apiHandler` contexto) — seguir o padrão vigente.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema UpdateEconomicParameterRequestSchema (D2)</name>
  <files>src/lib/admin/schemas.ts</files>
  <read_first>
    - src/lib/admin/schemas.ts (padrão UpdateOperationCostRequestSchema — linhas 11-25; AiModelPricingUpdateSchema — linhas 49-72)
    - src/lib/economic/types.ts (ECONOMIC_PARAMETER_KEYS)
    - openspec/changes/fase-38-2-admin-custos-operacionais/specs/economic-parameters/spec.md (linhas 204-251 — PUT)
  </read_first>
  <behavior>
    - Teste 1 (compile-time): schema aceita `{ key: "usd_brl_rate", value: 5.0, reason: "calibração" }` e `{ key: "credit_value_brl", value: 1.5, reason: "x", operationId: <uuid> }`
    - Teste 2 (compile-time): schema REJEITA `{ key: "invalid", value: 1 }` (enum), `{ value: 0 }` (positive), `{ key: "usd_brl_rate", value: 1 }` sem reason (obrigatório)
  </behavior>
  <action>
    Adicionar `UpdateEconomicParameterRequestSchema` em `src/lib/admin/schemas.ts` no estilo dos schemas existentes (z.object + validação de mensagens PT-BR). Importar `ECONOMIC_PARAMETER_KEYS` de `@/lib/economic/types`. `key: z.enum(ECONOMIC_PARAMETER_KEYS)`, `value: z.number().positive(...)`, `reason: z.string().min(1, ...)`, `operationId: z.string().uuid().optional()`. NÃO alterar schemas existentes.
  </action>
  <verify>
    <automated>npm run typecheck; `grep -c "UpdateEconomicParameterRequestSchema" src/lib/admin/schemas.ts` >= 1; `grep -c "ECONOMIC_PARAMETER_KEYS" src/lib/admin/schemas.ts` >= 1; `grep -c "z.enum" src/lib/admin/schemas.ts` >= 2</automated>
  </verify>
  <acceptance_criteria>
    - Schema com key enum, value positive, reason obrigatório, operationId uuid opcional
    - Schemas existentes intactos
    - typecheck limpo
  </acceptance_criteria>
  <done>Schema de update criado (D2)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GET /api/admin/economic-parameters (D2)</name>
  <files>src/app/api/admin/economic-parameters/route.ts</files>
  <read_first>
    - src/app/api/admin/ai-costs/route.ts (padrão apiHandler + requireAdmin + ZodError + 503 — linhas 1-57)
    - src/lib/economic/economic-parameter-service.ts (EconomicParameterService + UnavailableError — 38-2-02)
  </read_first>
  <behavior>
    - Teste 1 (route): GET com admin → 200 `{ parameters: [{ key, value, source }] }` (service retorna 2 resoluções)
    - Teste 2 (route): GET sem admin → 403 (requireAdmin lança ForbiddenError)
    - Teste 3 (route): GET com service lançando EconomicParameterUnavailableError → 503
  </behavior>
  <action>
    Criar `src/app/api/admin/economic-parameters/route.ts` com:
    1. `export const GET = apiHandler(async (request: Request) => { await requireAdmin(); ... })` no padrão exato da rota ai-costs.
    2. `const parameters = await new EconomicParameterService().getAll();` → `NextResponse.json({ parameters })`.
    3. try/catch: `EconomicParameterUnavailableError` → `NextResponse.json({ error: "economic_parameters_unavailable" }, { status: 503 })`; demais → rethrow.
    4. NENHUM endpoint público — não criar rota fora de `/api/admin/`.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/admin/economic-parameters/__tests__/route.test.ts; `grep -c "requireAdmin" src/app/api/admin/economic-parameters/route.ts` >= 1; `grep -c "EconomicParameterService" src/app/api/admin/economic-parameters/route.ts` >= 1; `grep -c "503" src/app/api/admin/economic-parameters/route.ts` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - GET 200 com parameters + source; 403 sem admin; 503 fail-closed
  </acceptance_criteria>
  <done>GET da API de parâmetros funcionando (200/403/503)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PUT /api/admin/economic-parameters (D2) + 5 testes da API (12.2)</name>
  <files>src/app/api/admin/economic-parameters/route.ts, src/app/api/admin/economic-parameters/__tests__/route.test.ts</files>
  <read_first>
    - src/app/api/admin/operation-costs/route.ts ou rota admin com RPC (padrão de PUT admin + actor_id — verificar como obtém o user id)
    - src/lib/admin/require-admin.ts (retorno/forma de obter o actor)
    - openspec/changes/fase-38-2-admin-custos-operacionais/specs/economic-parameters/spec.md (linhas 204-251)
  </read_first>
  <behavior>
    - Teste 4 (route): PUT com body válido → RPC `admin_set_economic_parameter` chamado com p_actor_id/p_key/p_value/p_reason e retorno 200 `{ parameter, auditId, updatedAt, idempotent }`
    - Teste 5 (route): PUT sem reason → 400; PUT com key inválido → 400; PUT com value <= 0 → 400 (zod)
    - Teste 6 (route): PUT sem admin → 403
    - Teste 7 (route): RPC retorna erro → 500
    - Teste 8 (route): idempotência — mesmo operation_id repetido → RPC retorna idempotent: true (repassado no 200)
  </behavior>
  <action>
    Adicionar `PUT` à rota criada na Task 2 (mesma `route.ts`):
    1. `export const PUT = apiHandler(async (request: Request) => { await requireAdmin(); ... })`.
    2. `const body = await request.json().catch(() => null);` + `UpdateEconomicParameterRequestSchema.safeParse(body)`; `!parsed.success` → 400 `{ error: "Dados inválidos", details }`.
    3. Obter o `p_actor_id` seguindo o padrão vigente das rotas admin (requireAdmin/apiHandler — verificar; ex.: usuário do header/session). 
    4. `const { data, error } = await client.rpc("admin_set_economic_parameter", { p_actor_id, p_key: parsed.data.key, p_value: parsed.data.value, p_reason: parsed.data.reason, p_operation_id: parsed.data.operationId ?? null });` (client = supabaseAdmin via service — padrão da rota).
    5. Sucesso → 200 `{ parameter: { key: data.key, value: data.value }, auditId: data.audit_id, updatedAt: data.updated_at, idempotent: data.idempotent }`.
    6. `error` → 500 `{ error: "economic_parameter_update_failed" }`.
    7. NÃO expor publicamente; NÃO logar o reason em texto completo se sensível (seguir padrão de auditoria F38 — reason já vai para a audit table).
    8. Criar `src/app/api/admin/economic-parameters/__tests__/route.test.ts` com os 5 casos da behavior list (mock de requireAdmin + mock do rpc/supabase — seguir o padrão do teste da rota ai-costs).
  </action>
  <verify>
    <automated>npx vitest run src/app/api/admin/economic-parameters/__tests__/route.test.ts; npm run typecheck; `grep -c "admin_set_economic_parameter" src/app/api/admin/economic-parameters/route.ts` >= 1; `grep -c "idempotent" src/app/api/admin/economic-parameters/route.ts` >= 1; `grep -c "auditId" src/app/api/admin/economic-parameters/route.ts` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - PUT 200 via RPC com { parameter, auditId, updatedAt, idempotent }; 400/403/500 mapeados
    - idempotência por operation_id repassada
    - 5 testes da API passando (tarefa 12.2)
    - typecheck limpo
  </acceptance_criteria>
  <done>PUT implementado + 5 testes da API verdes (tarefa 12.2)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PUT /api/admin/economic-parameters ← admin autenticado | Mutação de parâmetros econômicos com motivo obrigatório; delega ao RPC SECURITY DEFINER (única via de escrita) |
| GET /api/admin/economic-parameters ← admin autenticado | Leitura de parâmetros internos — nunca exposta publicamente |
| EconomicParameterService → economic_parameters | Leitura server-only via service_role (38-2-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38.2-14 | Elevation of Privilege | PUT sem admin | mitigate | `requireAdmin()` na rota; RPC com REVOKE EXECUTE FROM PUBLIC/anon/authenticated + GRANT service_role (38-2-01) — F38.2-06 |
| T-38.2-15 | Tampering | Parâmetro sem motivo / valor inválido | mitigate | zod: key enum ECONOMIC_PARAMETER_KEYS, value positive, reason min 1; CHECK value > 0 no banco (38-2-01) — F38.2-06 |
| T-38.2-16 | Replay | Retry duplicado de PUT | mitigate | idempotência por operation_id (UNIQUE parcial na audit + RPC retorna idempotent) — F38.2-06 |
| T-38.2-17 | Information Disclosure | Parâmetros via endpoint público | mitigate | Rota exclusivamente sob /api/admin/ + requireAdmin; nenhuma rota pública criada — F38.2-05 |
| T-38.2-18 | DoS | GET em falha de leitura | mitigate | Fail-closed: EconomicParameterUnavailableError → 503 (38-2-02) — F38.2-05 |
| T-38.2-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado neste plano — gate de legitimidade não se aplica |
</threat_model>

<verification>
- `npm run typecheck` limpo (schema + rota)
- `npx vitest run src/app/api/admin/economic-parameters/__tests__/route.test.ts` — 5 testes, 0 falhas
- GET 200/403/503 + PUT 200/400/403/500 + idempotência
</verification>

<success_criteria>
- UpdateEconomicParameterRequestSchema validado (enum, positive, reason, operationId)
- GET lista resolvida com source (200/403/503)
- PUT via RPC com auditoria + idempotência (200/400/403/500)
- 5 testes da API verdes (tarefa 12.2); sem endpoint público
</success_criteria>

<output>
Create `.planning/phases/38-2-admin-custos-operacionais/38-2-04-SUMMARY.md` when done
</output>

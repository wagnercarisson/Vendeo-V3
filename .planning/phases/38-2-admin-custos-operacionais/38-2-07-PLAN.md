---
phase: 38-2-admin-custos-operacionais
plan: 07
type: execute
wave: 4
depends_on:
  - "38-2-02"
  - "38-2-04"
files_modified:
  - "src/app/(app)/admin/operation-costs/page.tsx"
  - "src/app/(app)/admin/operation-costs/operation-costs-form.tsx"
  - "src/app/(app)/admin/operation-costs/__tests__/page.test.tsx"
  - "src/app/(app)/admin/operation-costs/operation-costs-form.test.tsx"
autonomous: false
HAS_UI: 1
requirements:
  - F38.2-07
  - F38.2-18
must_haves:
  truths:
    - "Página /admin/operation-costs mantém a ROTA e o conteúdo existente (tabela de créditos por operação F38 — inalterada em schema), mas o TÍTULO visual vira 'Configurações Econômicas' (D2)"
    - "Seção 'Parâmetros Econômicos' adicionada: inputs usd_brl_rate ('Taxa de conversão USD→BRL') e credit_value_brl ('Valor operacional do crédito em BRL'), motivo OBRIGATÓRIO, badge source (tabela/fallback) e feedback audit_id após PUT (D2)"
    - "Busca de parâmetros via EconomicParameterService.getAll() (server); estado 503 → 'Serviço de parâmetros indisponível no momento' fail-closed (padrão F38)"
    - "3 testes da página (12.7): título + seção de parâmetros; edição com motivo obrigatório; badge source"
  artifacts:
    - path: "src/app/(app)/admin/operation-costs/page.tsx"
      provides: "Título 'Configurações Econômicas' + seção de Parâmetros + tabela F38 mantida"
      contains: "Configurações Econômicas"
    - path: "src/app/(app)/admin/operation-costs/operation-costs-form.tsx"
      provides: "ParamsForm (usd_brl_rate, credit_value_brl, motivo, source badge, audit_id)"
      contains: "ParamsForm"
    - path: "src/app/(app)/admin/operation-costs/__tests__/page.test.tsx"
      provides: "Testes da página (12.7)"
      contains: "Configurações Econômicas"
    - path: "src/app/(app)/admin/operation-costs/operation-costs-form.test.tsx"
      provides: "Testes do form (12.7)"
      contains: "ParamsForm"
  key_links:
    - from: "src/app/(app)/admin/operation-costs/page.tsx"
      to: "src/lib/economic/economic-parameter-service.ts"
      via: "EconomicParameterService.getAll() (server-side, source table/fallback)"
      pattern: "EconomicParameterService"
    - from: "src/app/(app)/admin/operation-costs/operation-costs-form.tsx"
      to: "src/app/api/admin/economic-parameters/route.ts"
      via: "PUT /api/admin/economic-parameters (motivo + operationId idempotência) → audit_id"
      pattern: "economic-parameters"
---

<objective>
Transformar a página `/admin/operation-costs` em "**Configurações Econômicas**" (D2): a rota e a tabela de créditos por operação (F38) permanecem; o título visual muda e a página ganha a seção "Parâmetros Econômicos" — `usd_brl_rate` e `credit_value_brl` editáveis com motivo obrigatório, badge `source` (tabela/fallback) e feedback `audit_id` após `PUT /api/admin/economic-parameters`. Estado 503 fail-closed no padrão F38. Testes (tarefa 12.7 — 3 testes).

Purpose: o admin calibra manualmente a taxa USD→BRL e o valor do crédito sem deploy — a fonte única de conversão monetária de toda a fase (D1/D2).
Output: página editável + testes verdes + checkpoint humano visual.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-2-admin-custos-operacionais/38-2-CONTEXT.md
@openspec/changes/fase-38-2-admin-custos-operacionais/specs/admin-operation-costs/spec.md
@openspec/changes/fase-38-2-admin-custos-operacionais/specs/economic-parameters/spec.md

<interfaces>
Contratos que o executor precisa (extraídos dos specs D2 + leitura dos arquivos):

Estado atual verificado:
- `src/app/(app)/admin/operation-costs/page.tsx` (84 linhas): título "Custos por operação", tabela F38 via `OperationCostService.getAllCosts()`, usuários via supabaseAdmin. 
- `src/app/(app)/admin/operation-costs/operation-costs-form.tsx`: `OperationCostsForm` existente (F38) — NÃO remover (tabela de créditos continua).

Mudanças:
1. **Título da página** → "Configurações Econômicas" com subtítulo explicando (rota mantida — bookmarks/testes/links não quebram, D2). O título interno `<h1>` (linha 72) e o fallback 503 (linha 29) mudam o texto.
2. **Seção "Parâmetros Econômicos"** (nova, antes da tabela F38 ou após — decisão visual):
   - Inputs numéricos `usd_brl_rate` (label "Taxa de conversão USD→BRL") e `credit_value_brl` (label "Valor operacional do crédito em BRL")
   - **Motivo obrigatório** (text input) — necessário para PUT
   - Badge `source` por parâmetro: "tabela" (value do banco) ou "fallback" (default 1.00 — indicando que o admin ainda não configurou)
   - Botão salvar → `PUT /api/admin/economic-parameters` (corpo: `{ key, value, reason, operationId }` — operationId gerado no client via crypto.randomUUID() para idempotência)
   - Feedback de sucesso com `audit_id` retornado; erro → mensagem inline
   - Client component (`ParamsForm` em operation-costs-form.tsx)
3. **Estado 503**: `EconomicParameterUnavailableError` → "Serviço de parâmetros indisponível no momento. Tente novamente em alguns instantes." (fail-closed — padrão F38).
4. Busca dos parâmetros via `new EconomicParameterService().getAll()` (server, na página) → passa as resoluções (value + source) ao ParamsForm.

Padrões de teste (analog: testes de página admin existentes — `src/app/(app)/admin/operation-costs/__tests__/` se existir, ou `src/app/(app)/admin/metrics/__tests__/`): renderizar o componente com props mockadas.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Página — título 'Configurações Econômicas' + busca de parâmetros + seção (D2)</name>
  <files>src/app/(app)/admin/operation-costs/page.tsx</files>
  <read_first>
    - src/app/(app)/admin/operation-costs/page.tsx (linhas 1-84 — estado atual)
    - src/lib/economic/economic-parameter-service.ts (getAll — 38-2-02)
    - openspec/changes/fase-38-2-admin-custos-operacionais/specs/admin-operation-costs/spec.md
  </read_first>
  <behavior>
    - Teste 1 (page): renderiza título "Configurações Econômicas" e mantém a seção "Operações" (tabela F38) + nova seção "Parâmetros Econômicos" com os dois inputs
    - Teste 2 (page): com `EconomicParameterUnavailableError` → mostra "Serviço de parâmetros indisponível no momento" (fail-closed)
  </behavior>
  <action>
    Modificar `src/app/(app)/admin/operation-costs/page.tsx`:
    1. Alterar o `<h1>` para "Configurações Econômicas" + subtítulo ("Ajuste taxa de conversão, valor do crédito e custos por operação sem deploy.").
    2. Alterar o texto do fallback 503 (linha 29) para "Serviço de parâmetros indisponível no momento. Tente novamente em alguns instantes."
    3. Adicionar busca dos parâmetros: `const parameters = await new EconomicParameterService().getAll();` em try/catch separado do costs (EconomicParameterUnavailableError → retorno 503 da seção de parâmetros, SEM derrubar a tabela F38 — resiliência: cada seção falha fechada independentemente).
    4. Passar `parameters` (value + source) para o novo `ParamsForm` (Task 2) na seção "Parâmetros Econômicos".
    5. NÃO alterar `OperationCostsForm`/tabela F38 (schema inalterado — D2).
  </action>
  <verify>
    <automated>npx vitest run src/app/(app)/admin/operation-costs/__tests__/page.test.tsx; `grep -c "Configurações Econômicas" src/app/(app)/admin/operation-costs/page.tsx` >= 1; `grep -c "EconomicParameterService" src/app/(app)/admin/operation-costs/page.tsx` >= 1; `grep -c "ParamsForm" src/app/(app)/admin/operation-costs/page.tsx` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - Título "Configurações Econômicas"; tabela F38 mantida; seção de parâmetros presente
    - 503 fail-closed independente por seção
    - typecheck limpo
  </acceptance_criteria>
  <done>Página renomeada visualmente + busca de parâmetros + seção (D2)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ParamsForm — inputs + motivo obrigatório + source badge + audit_id (D2)</name>
  <files>src/app/(app)/admin/operation-costs/operation-costs-form.tsx</files>
  <read_first>
    - src/app/(app)/admin/operation-costs/operation-costs-form.tsx (padrão de form client existente F38)
    - src/app/(app)/admin/operation-costs/page.tsx (props → ParamsForm)
    - src/lib/economic/types.ts (EconomicParameterResolution — source)
  </read_first>
  <behavior>
    - Teste 3 (form): renderiza os 2 inputs (usd_brl_rate "Taxa de conversão USD→BRL", credit_value_brl "Valor operacional do crédito em BRL") com badge source correto (tabela/fallback)
    - Teste 4 (form): clicar salvar SEM motivo → erro inline "Motivo obrigatório"; com motivo → chama PUT /api/admin/economic-parameters com { key, value, reason, operationId } e exibe audit_id retornado
  </behavior>
  <action>
    Criar/estender `ParamsForm` em `src/app/(app)/admin/operation-costs/operation-costs-form.tsx` (client component — "use client"):
    1. Props: `parameters: EconomicParameterResolution[]`.
    2. State: values (2 inputs numéricos), reason, saving, error, successAuditId.
    3. Badge `source` por parâmetro: "tabela" quando source === "table", "fallback" quando "fallback" (estilo de badge existente do design system — ex.: badges do admin).
    4. Submit por parâmetro (ou um botão para cada): `fetch("/api/admin/economic-parameters", { method: "PUT", body: JSON.stringify({ key, value, reason, operationId: crypto.randomUUID() }) })`. `!response.ok` → error inline. Sucesso → `successAuditId` do body `{ auditId }` exibido ("Parâmetro atualizado — auditoria: <auditId>").
    5. `reason` obrigatório: botão desabilitado ou erro inline quando vazio.
    6. NÃO remover o `OperationCostsForm` existente (F38 — tabela de créditos).
  </action>
  <verify>
    <automated>npx vitest run src/app/(app)/admin/operation-costs/operation-costs-form.test.tsx; npm run typecheck; `grep -c "ParamsForm" src/app/(app)/admin/operation-costs/operation-costs-form.tsx` >= 1; `grep -c "Taxa de conversão USD→BRL" src/app/(app)/admin/operation-costs/operation-costs-form.tsx` >= 1; `grep -c "auditId" src/app/(app)/admin/operation-costs/operation-costs-form.tsx` >= 1; `grep -c "Motivo obrigatório" src/app/(app)/admin/operation-costs/operation-costs-form.tsx` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - ParamsForm com 2 inputs + motivo obrigatório + source badge + feedback audit_id
    - PUT chamado com operationId (idempotência)
    - typecheck limpo
  </acceptance_criteria>
  <done>ParamsForm implementado — edição de parâmetros com auditoria (D2)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Testes completos (tarefa 12.7 — 3 testes)</name>
  <files>src/app/(app)/admin/operation-costs/__tests__/page.test.tsx, src/app/(app)/admin/operation-costs/operation-costs-form.test.tsx</files>
  <read_first>
    - Testes existentes de páginas admin (padrão de mock/render — verificar se operation-costs já tem __tests__)
  </read_first>
  <behavior>
    - Casos (tarefa 12.7 — 3 testes): título + seção de parâmetros renderizados; edição com motivo obrigatório (salvar sem motivo → erro; com motivo → PUT + audit_id); badge source (tabela/fallback)
  </behavior>
  <action>
    Criar os testes da página e do form (padrão RTL/react-testing-library usado nas páginas admin). Mock do `EconomicParameterService` (página) e mock do `fetch` (form). Cobrir os 3 casos da behavior list.
  </action>
  <verify>
    <automated>npx vitest run src/app/(app)/admin/operation-costs/__tests__/page.test.tsx src/app/(app)/admin/operation-costs/operation-costs-form.test.tsx; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - 3 testes passando (npx vitest run — 0 falhas)
    - typecheck limpo
  </acceptance_criteria>
  <done>Testes da página Configurações Econômicas verdes (tarefa 12.7)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Página /admin/operation-costs renomeada visualmente para "Configurações Econômicas" com a seção "Parâmetros Econômicos" (usd_brl_rate + credit_value_brl com motivo obrigatório, badge source e audit_id)</what-built>
  <how-to-verify>
    1. Acessar /admin/operation-costs (admin logado) — título "Configurações Econômicas" e a tabela de créditos por operação (F38) continua visível
    2. Na seção Parâmetros: os dois inputs aparecem com badge source ("tabela" se já configurado, "fallback" se 1.00 default)
    3. Tentar salvar sem motivo → erro "Motivo obrigatório"
    4. Preencher usd_brl_rate = 5.50 + motivo → salvar → feedback com audit_id; GET posterior mostra source "tabela"
    5. Confirmar que o link /admin/operation-costs na navegação admin continua funcionando (rota mantida)
  </how-to-verify>
  <resume-signal>Type "approved" se tudo passou, ou descreva os problemas</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /admin/operation-costs ← admin autenticado | Página admin-only; parâmetros econômicos internos nunca expostos publicamente |
| ParamsForm → PUT /api/admin/economic-parameters | Escrita com motivo obrigatório; API requer requireAdmin (38-2-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38.2-30 | Elevation of Privilege | Página acessível a não-admin | mitigate | requireAdmin() na página (mantido do estado atual) — F38.2-18 |
| T-38.2-31 | Tampering | Parâmetro salvo sem motivo | mitigate | motivo obrigatório no form (botão/erro) + zod min(1) na API (38-2-04) + reason NOT NULL na audit (38-2-01) — F38.2-07 |
| T-38.2-32 | Information Disclosure | Parâmetros internos (taxa/margem) | mitigate | Página e API admin-only; badge source apenas informativo (não vaza dados de terceiros) — F38.2-07 |
| T-38.2-33 | Spoofing | Source incorreto (tabela/fallback) | mitigate | Source vem do EconomicParameterService.getAll() (38-2-02) — determinístico — F38.2-07 |
| T-38.2-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado neste plano — gate de legitimidade não se aplica |
</threat_model>

<verification>
- `npm run typecheck` limpo (página + form)
- `npx vitest run` — 3 testes de 12.7, 0 falhas
- Checkpoint humano: edição com motivo + audit_id + badge source OK
</verification>

<success_criteria>
- Página /admin/operation-costs com título "Configurações Econômicas", tabela F38 mantida e seção Parâmetros
- ParamsForm com motivo obrigatório, badge source, audit_id
- 3 testes verdes (tarefa 12.7)
- Checkpoint humano aprovado
</success_criteria>

<output>
Create `.planning/phases/38-2-admin-custos-operacionais/38-2-07-SUMMARY.md` when done
</output>

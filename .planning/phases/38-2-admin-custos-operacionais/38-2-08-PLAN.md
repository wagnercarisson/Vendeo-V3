---
phase: 38-2-admin-custos-operacionais
plan: 08
type: execute
wave: 5
depends_on:
  - "38-2-06"
  - "38-2-07"
files_modified:
  - "src/app/(app)/admin/ai-operation-costs/page.tsx"
  - "src/app/(app)/admin/ai-operation-costs/ai-operation-costs-filters.tsx"
  - "src/app/(app)/admin/ai-operation-costs/kpis-grid.tsx"
  - "src/app/(app)/admin/ai-operation-costs/operation-runs-table.tsx"
  - "src/app/(app)/admin/ai-operation-costs/run-detail-dialog.tsx"
  - "src/app/(app)/admin/ai-operation-costs/cost-badge.tsx"
  - "src/app/(app)/admin/ai-operation-costs/segment-aggregations.tsx"
  - "src/app/(app)/admin/ai-operation-costs/__tests__/page.test.tsx"
  - "src/app/(app)/admin/layout.tsx"
autonomous: false
HAS_UI: 1
requirements:
  - F38.2-13
  - F38.2-15
must_haves:
  truths:
    - "Página /admin/ai-operation-costs 'Custos de Operação' criada (Server Component, force-dynamic, requireAdmin, leitura via service layer server-only que chama os RPCs; 503 → 'Serviço indisponível no momento' fail-closed; vazio → 'aguardando dados de geração') (D3)"
    - "Filtros: período com presets 7/30/90 dias e limite de janela, loja, tipo de entrega, status, provider, model, gen_type, run_id, segmento econômico (D3/D9)"
    - "KPIs: custo USD/BRL, créditos, receita/resultado/margem BRL, tempo médio/P95, total de entregas, erros/sucessos (D3)"
    - "Tabela por entrega: data, tipo, loja, status, custo (USD/BRL), créditos, tempo, chamadas, regenerações, provider/model principal, badge confiança (D3)"
    - "Drilldown call-level por operation_run_id com estimated_cost_brl + badges + componentes de custo (text_component_usd/image_tool_component_usd) (D3/D4/D5)"
    - "Agregados por segmento econômico (D9): custo/resultado/margem %/taxa de erro por segmento + gerações por hora/owner/loja/tipo/status/segmento"
    - "Badge de confiança + legend 'estimativas operacionais, não custo financeiro reconciliado'; placeholder F38.3 (D5/D7)"
    - "Link 'Custos de Operação' → /admin/ai-operation-costs na navegação admin (layout.tsx) (D3)"
    - "5 testes da página (12.6): KPIs + tabela com badges; drilldown; estado vazio; filtros atualizam; 503 indisponível"
  artifacts:
    - path: "src/app/(app)/admin/ai-operation-costs/page.tsx"
      provides: "Painel Custos de Operação (D3)"
      contains: "Custos de Operação"
    - path: "src/app/(app)/admin/ai-operation-costs/operation-runs-table.tsx"
      provides: "Tabela por entrega + drilldown"
      contains: "operationRunId"
    - path: "src/app/(app)/admin/ai-operation-costs/cost-badge.tsx"
      provides: "Badge de confiança + legend"
      contains: "provisional image tool estimate"
    - path: "src/app/(app)/admin/ai-operation-costs/segment-aggregations.tsx"
      provides: "Agregados por segmento (D9)"
      contains: "Segmento"
    - path: "src/app/(app)/admin/layout.tsx"
      provides: "Link Custos de Operação na nav admin"
      contains: "ai-operation-costs"
  key_links:
    - from: "src/app/(app)/admin/ai-operation-costs/page.tsx"
      to: "src/lib/ai-cost/operation-runs-service.ts"
      via: "OperationRunsService.listRuns (lista + summary + aggregations)"
      pattern: "listRuns"
    - from: "src/app/(app)/admin/ai-operation-costs/run-detail-dialog.tsx"
      to: "src/app/api/admin/ai-operation-runs/[operationRunId]/route.ts"
      via: "GET detalhe call-level (drilldown)"
      pattern: "getRunDetail"
    - from: "src/app/(app)/admin/layout.tsx"
      to: "src/app/(app)/admin/ai-operation-costs/page.tsx"
      via: "link de navegação 'Custos de Operação'"
      pattern: "ai-operation-costs"
---

<objective>
Criar a página `/admin/ai-operation-costs` "**Custos de Operação**" (D3/D9): painel admin de observabilidade da F38.1 transformada em decisão econômica — filtros (período com presets 7/30/90 dias, loja, tipo de entrega, status, provider/model, generation_type, operation_run_id, **segmento econômico**), KPIs (custo USD/BRL, créditos, receita/resultado/margem BRL, tempo médio/P95, entregas, erros/sucessos), tabela por entrega com **badges de confiança**, **drilldown call-level** por `operation_run_id`, **agregados por segmento** (D9) e placeholder F38.3 (D7). Link na navegação admin. Testes (tarefa 12.6 — 5 testes).

Purpose: é a tela que transforma a apuração call-level em decisão econômica (o que a F38.1 decidiu "exibir depois"). O admin enxerga custo × créditos × receita/margem estimada por entrega, com confiança visível e segmentação da origem do consumo.
Output: painel completo + link nav + testes verdes + checkpoint humano visual.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-2-admin-custos-operacionais/38-2-CONTEXT.md
@openspec/changes/fase-38-2-admin-custos-operacionais/specs/ai-operation-costs/spec.md
@openspec/changes/fase-38-2-admin-custos-operacionais/specs/ai-operation-runs-api/spec.md

<interfaces>
Contratos que o executor precisa (extraídos dos specs D3/D4/D5/D9 + 38-2-CONTEXT.md + leitura dos arquivos):

**UI (HAS_UI: 1). Esta é a página principal da fase.**

Consumo de dados — a página é um Server Component que consulta o `OperationRunsService.listRuns(...)` server-only diretamente (padrão das páginas admin existentes — operation-costs page usa OperationCostService direto) OU via fetch à API (GET /api/admin/ai-operation-runs). Padrão do repo: páginas admin usam service server-only direto (decidir pela leitura do código). O drilldown (client) usa fetch à API do detalhe.

Estrutura da página (Server Component):
- `export const dynamic = "force-dynamic";` + `requireAdmin()` (padrão operation-costs/metrics).
- SearchParams de filtros (período, loja, tipo, status, provider, model, gen_type, run_id, segment, page) → passa ao service.
- Estados: dados (runs + summary + aggregations), 503 fail-closed ("Serviço indisponível no momento"), vazio ("aguardando dados de geração" — EmptyState padrão do repo).

Componentes (client, no dir `src/app/(app)/admin/ai-operation-costs/`):
1. `ai-operation-costs-filters.tsx` — filtros com presets de período (7/30/90 dias) + limite de janela (365d máx), loja, tipo, status, provider, model, gen_type, run_id, segmento econômico (D9).
2. `kpis-grid.tsx` — KPIs (D3): custo USD/BRL, créditos, receita/resultado/margem BRL, tempo médio/P95, total de entregas, erros/sucessos.
3. `operation-runs-table.tsx` — tabela por entrega: data, tipo, loja, status, custo (USD/BRL), créditos, tempo, chamadas, regenerações, provider/model principal, badge confiança (D3).
4. `run-detail-dialog.tsx` — drilldown call-level por operation_run_id (fetch ao GET detalhe): etapas com estimated_cost_brl, badges, text_component_usd/image_tool_component_usd (D4/D5).
5. `cost-badge.tsx` — badge de confiança (D5) + legend "estimativas operacionais, não custo financeiro reconciliado".
6. `segment-aggregations.tsx` — agregados por segmento (D9): custo/resultado operacional estimado/margem %/taxa de erro por segmento + gerações por hora/owner/loja/tipo/status/segmento.

Placeholder F38.3 (D7): na tabela/detalhe, exibir "Custo estimado Vendeo" / "Custo reconciliado provider: ainda indisponível" / "Diferença: pendente".

layout.tsx: adicionar `<Link href="/admin/ai-operation-costs">Custos de Operação</Link>` à nav admin (src/app/(app)/admin/layout.tsx:22-44).

Padrões de teste (analog: testes de página admin existentes — RTL/react-testing-library, mock do service/API/fetch).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Página — server component + estados (dados/503/vazio) + link nav (D3)</name>
  <files>src/app/(app)/admin/ai-operation-costs/page.tsx, src/app/(app)/admin/layout.tsx</files>
  <read_first>
    - src/app/(app)/admin/operation-costs/page.tsx (padrão de página admin server + 503 + requireAdmin — linhas 1-84)
    - src/app/(app)/admin/metrics/page.tsx (EmptyState + force-dynamic — linhas 107-190)
    - src/app/(app)/admin/layout.tsx (nav — linhas 22-44)
    - src/lib/ai-cost/operation-runs-service.ts (listRuns — 38-2-05)
  </read_first>
  <behavior>
    - Teste 1 (page): com dados → renderiza KPIs + tabela + agregados; título "Custos de Operação"
    - Teste 2 (page): com OperationRunsUnavailableError → "Serviço indisponível no momento" (503 fail-closed)
    - Teste 3 (page): sem dados → EmptyState "aguardando dados de geração"
  </behavior>
  <action>
    Criar `src/app/(app)/admin/ai-operation-costs/page.tsx`:
    1. `export const dynamic = "force-dynamic";` + `requireAdmin()` (try/catch → mensagem de acesso negado no padrão das outras páginas).
    2. Ler `searchParams` (filtros) — passar ao `OperationRunsService.listRuns(...)` (server-only direto — padrão operation-costs) OU ao fetch da API (decidir pelo padrão do repo).
    3. Estados: dados → renderiza `<KpisGrid>`, `<OperationRunsTable>`, `<SegmentAggregations>`, `<AiOperationCostsFilters>`; `OperationRunsUnavailableError` → "Serviço indisponível no momento. Tente novamente em alguns instantes." (fail-closed); sem runs → `<EmptyState>` "aguardando dados de geração" (padrão metrics page).
    4. `src/app/(app)/admin/layout.tsx`: adicionar link "Custos de Operação" → `/admin/ai-operation-costs` na nav (após "Custos por operação").
  </action>
  <verify>
    <automated>npx vitest run src/app/(app)/admin/ai-operation-costs/__tests__/page.test.tsx; `grep -c "force-dynamic" src/app/(app)/admin/ai-operation-costs/page.tsx` == 1; `grep -c "requireAdmin" src/app/(app)/admin/ai-operation-costs/page.tsx` >= 1; `grep -c "ai-operation-costs" src/app/(app)/admin/layout.tsx` >= 1; `grep -c "Custos de Operação" src/app/(app)/admin/layout.tsx` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - Página force-dynamic + requireAdmin + estados (dados/503/vazio)
    - Link na nav admin
    - typecheck limpo
  </acceptance_criteria>
  <done>Página criada com estados e link na nav (D3)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Filtros + KPIs + tabela com badges (D3/D5)</name>
  <files>src/app/(app)/admin/ai-operation-costs/ai-operation-costs-filters.tsx, src/app/(app)/admin/ai-operation-costs/kpis-grid.tsx, src/app/(app)/admin/ai-operation-costs/operation-runs-table.tsx, src/app/(app)/admin/ai-operation-costs/cost-badge.tsx</files>
  <read_first>
    - src/app/(app)/admin/metrics/page.tsx (padrão de cards/valores — MetricCard/MetricsCards)
    - src/app/(app)/admin/operation-costs/operation-costs-form.tsx (padrão de form/filtro admin)
    - src/lib/ai-cost/operation-runs-service.ts (tipos de saída do listRuns — runs/summary/aggregations)
  </read_first>
  <behavior>
    - Teste 4 (page/component): com dados → tabela mostra data, tipo, loja, status, custo USD/BRL, créditos, tempo, chamadas, regenerações, provider/model, badge confiança; KPIs renderizam custo/créditos/receita/resultado/margem/tempo médio/P95/total/erros/sucessos
    - Teste 5 (component): badge correto por costSource/nota (provider_reported, provisional image tool estimate, partial, estimated, not_available); legend "estimativas operacionais, não custo financeiro reconciliado" visível
    - Teste 6 (component): filtros renderizam presets de período 7/30/90 + segmento econômico; mudar filtro atualiza a query (navegação/submissão)
  </behavior>
  <action>
    Criar os componentes:
    1. `cost-badge.tsx` — badge de confiança (D5): recebe o badge derivado (string) + mostra rótulo; legend fixa "Estimativas operacionais — não custo financeiro reconciliado." Estilo de badge do design system do repo.
    2. `kpis-grid.tsx` — grid de KPIs (D3): custo USD, custo BRL, créditos debitados, receita operacional BRL, resultado operacional BRL, margem operacional %, tempo médio, P95, total de entregas, erros, sucessos (usa `summary` do service — a UI NÃO recalcula).
    3. `operation-runs-table.tsx` — tabela por entrega (D3): colunas data, tipo, loja, status, custo USD/BRL, créditos, tempo, chamadas, regenerações, provider/model principal, badge confiança. Linha clicável → abre drilldown (Task 3). Placeholder F38.3 (D7): coluna "Custo reconciliado provider: ainda indisponível" + "Diferença: pendente" (estado inicial).
    4. `ai-operation-costs-filters.tsx` — filtros (D3/D9): período (presets 7/30/90 dias + limite 365d), loja, tipo de entrega, status, provider, model, gen_type, run_id, segmento econômico. Submit → atualiza searchParams da página.
  </action>
  <verify>
    <automated>npx vitest run src/app/(app)/admin/ai-operation-costs/__tests__/page.test.tsx; npm run typecheck; `grep -c "provisional image tool estimate" src/app/(app)/admin/ai-operation-costs/cost-badge.tsx` >= 1; `grep -c "ainda indisponível" src/app/(app)/admin/ai-operation-costs/operation-runs-table.tsx` >= 1; `grep -c "90" src/app/(app)/admin/ai-operation-costs/ai-operation-costs-filters.tsx` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - KPIs + tabela com badges + filtros (presets + segmento) implementados
    - Badge com legend; placeholder F38.3 visível
    - typecheck limpo
  </acceptance_criteria>
  <done>Filtros + KPIs + tabela + badges criados (D3/D5)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Drilldown call-level + agregados por segmento (D4/D9)</name>
  <files>src/app/(app)/admin/ai-operation-costs/run-detail-dialog.tsx, src/app/(app)/admin/ai-operation-costs/segment-aggregations.tsx</files>
  <read_first>
    - src/app/(app)/admin/ai-operation-costs/operation-runs-table.tsx (abertura do drilldown — Task 2)
    - src/lib/ai-cost/operation-runs-service.ts (getRunDetail + tipos de events)
    - openspec/changes/fase-38-2-admin-custos-operacionais/specs/ai-operation-runs-api/spec.md (linhas 171-218 — detalhe)
    - openspec/changes/fase-38-2-admin-custos-operacionais/specs/ai-operation-costs/spec.md (agregados D9)
  </read_first>
  <behavior>
    - Teste 7 (component): clicar numa linha → dialog abre e busca GET /api/admin/ai-operation-runs/[operationRunId]; mostra etapas (generationType, provider, model, status, tokens, duração, estimatedCostUsd/Brl, textComponentUsd, imageToolComponentUsd, badge)
    - Teste 8 (component): agregados por segmento renderizam custo/resultado/margem %/taxa de erro por segmento + gerações por hora/owner/loja/tipo/status/segmento (aggregations do service)
  </behavior>
  <action>
    Criar:
    1. `run-detail-dialog.tsx` — client component: recebe `operationRunId`; `useEffect`/botão "Ver detalhes" faz `fetch("/api/admin/ai-operation-runs/" + operationRunId)`; estados loading/error/dados; renderiza a lista de eventos call-level com todas as colunas do contrato D4 (tokens, duração, custo USD/BRL, componentes text/image tool, badge por evento). Placeholder F38.3 no cabeçalho do run.
    2. `segment-aggregations.tsx` — renderiza `aggregations` do service: `bySegment` (custo, resultado operacional estimado, margem %, taxa de erro), `byDeliveryType`, `byStage`, `byProviderModel`, `byStatus`, `byStore`, `byOwner`, `byHour`. Prioridade visual para o bloco por segmento (D9).
  </action>
  <verify>
    <automated>npx vitest run src/app/(app)/admin/ai-operation-costs/__tests__/page.test.tsx; npm run typecheck; `grep -c "textComponentUsd" src/app/(app)/admin/ai-operation-costs/run-detail-dialog.tsx` >= 1; `grep -c "imageToolComponentUsd" src/app/(app)/admin/ai-operation-costs/run-detail-dialog.tsx` >= 1; `grep -c "bySegment" src/app/(app)/admin/ai-operation-costs/segment-aggregations.tsx` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - Drilldown call-level com componentes de custo + badges
    - Agregados por segmento (D9) + demais agregados
    - typecheck limpo
  </acceptance_criteria>
  <done>Drilldown + agregados por segmento implementados (D4/D9)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Testes completos (tarefa 12.6 — 5 testes)</name>
  <files>src/app/(app)/admin/ai-operation-costs/__tests__/page.test.tsx</files>
  <read_first>
    - Testes de página admin existentes (padrão RTL/react-testing-library + mock do service)
  </read_first>
  <behavior>
    - Casos (tarefa 12.6 — 5 testes): KPIs + tabela com badges renderizados; drilldown abre e mostra eventos; estado vazio (aguardando dados); filtros atualizam a query; 503 indisponível
  </behavior>
  <action>
    Criar `src/app/(app)/admin/ai-operation-costs/__tests__/page.test.tsx` com os 5 casos da behavior list. Mock do `OperationRunsService` (página) e do `fetch` (drilldown). Cobrir badge rendering, placeholder F38.3, estados.
  </action>
  <verify>
    <automated>npx vitest run src/app/(app)/admin/ai-operation-costs/__tests__/page.test.tsx; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - 5 testes passando (npx vitest run — 0 falhas)
    - typecheck limpo
  </acceptance_criteria>
  <done>Testes da página Custos de Operação verdes (tarefa 12.6)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Página /admin/ai-operation-costs "Custos de Operação" completa (filtros, KPIs, tabela por entrega com badges, drilldown call-level, agregados por segmento, placeholder F38.3) + link na nav admin</what-built>
  <how-to-verify>
    1. Acessar /admin/ai-operation-costs — link "Custos de Operação" na nav admin; título "Custos de Operação"
    2. Filtros: presets de período 7/30/90 dias; selecionar segmento econômico e ver os KPIs/agregados reagirem (summary/aggregations do conjunto filtrado)
    3. KPIs mostram custo USD/BRL, créditos, receita/resultado/margem BRL, tempo médio/P95, total de entregas, erros/sucessos
    4. Tabela por entrega com data, tipo, loja, status, custo, créditos, tempo, chamadas, regenerações, provider/model, badge de confiança + legend "estimativas operacionais"
    5. Clicar numa entrega → drilldown com etapas, tokens, duração, estimatedCostUsd/Brl, text/image components, badge por evento
    6. Agregados por segmento (custo/resultado/margem/taxa de erro) e gerações por hora/owner/loja/tipo/status/segmento
    7. Placeholder F38.3: "Custo reconciliado provider: ainda indisponível" / "Diferença: pendente"
    8. Regressão: demais páginas admin navegáveis sem erro
  </how-to-verify>
  <resume-signal>Type "approved" se tudo passou, ou descreva os problemas</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /admin/ai-operation-costs ← admin autenticado | Painel admin-only — dados de custo/receita/margem internos nunca expostos publicamente |
| Drilldown → GET /api/admin/ai-operation-runs/[id] | Leitura sob requireAdmin via API (38-2-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38.2-34 | Elevation of Privilege | Página acessível a não-admin | mitigate | requireAdmin() na página (padrão admin layout já protege /admin/*) — F38.2-13 |
| T-38.2-35 | Information Disclosure | Custo/receita/margem/segmento | mitigate | Página admin-only; KPIs vêm do service (nunca calculados no client); segmento exibido como "origem operacional do consumo" (D9 — nunca lucratividade real) — F38.2-13 |
| T-38.2-36 | Spoofing | Badge apresentado como verdade financeira | mitigate | Badge + legend "estimativas operacionais, não custo financeiro reconciliado" (D5); placeholder F38.3 "ainda indisponível/pendente" (D7) — F38.2-13 |
| T-38.2-37 | Tampering | Filtros manipulados no client | mitigate | Filtros passam por searchParams → zod na API/service (38-2-06) — janela/paginação validadas — F38.2-13 |
| T-38.2-38 | DoS | Drilldown em massa | mitigate | Drilldown sob demanda (clique); API detalhe valida uuid + fail-closed 503 (38-2-06) — F38.2-15 |
| T-38.2-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado neste plano — gate de legitimidade não se aplica |
</threat_model>

<verification>
- `npm run typecheck` limpo (página + componentes)
- `npx vitest run src/app/(app)/admin/ai-operation-costs/__tests__/page.test.tsx` — 5 testes, 0 falhas
- Checkpoint humano: painel completo + drilldown + badges + segmentos + placeholder F38.3 OK
</verification>

<success_criteria>
- Página /admin/ai-operation-costs com filtros, KPIs, tabela, drilldown, agregados por segmento e badge/legend
- Link na nav admin (layout.tsx)
- Placeholder F38.3 (D7) visível
- 5 testes verdes (tarefa 12.6)
- Checkpoint humano aprovado
</success_criteria>

<output>
Create `.planning/phases/38-2-admin-custos-operacionais/38-2-08-SUMMARY.md` when done
</output>

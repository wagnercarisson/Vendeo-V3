---
phase: 38-2-admin-custos-operacionais
plan: 09
type: execute
wave: 5
depends_on:
  - "38-2-02"
  - "38-2-05"
files_modified:
  - "src/lib/metrics/pipeline-metrics.ts"
  - "src/app/(app)/admin/metrics/page.tsx"
  - "src/lib/metrics/__tests__/pipeline-metrics.test.ts"
  - "src/app/(app)/admin/metrics/__tests__/page.test.tsx"
autonomous: true
HAS_UI: 1
requirements:
  - F38.2-19
  - F38.2-20
  - F38.2-21
  - F38.2-22
must_haves:
  truths:
    - "getAvgCost (src/lib/metrics/pipeline-metrics.ts) deixa de ler campaign_pipeline.estimated_cost_usd (NULL por desenho) e passa a apurar custo médio de IA por entrega via apuração call-level (admin_get_ai_costs by_operation_run → média de custo_usd_total; ou o RPC de resumo admin_get_ai_operation_runs) (D6)"
    - "admin_get_metrics (F28) PERMANECE INALTERADO — a correção é na camada de leitura do front (pipeline-metrics / página) (D6)"
    - "Card do /admin/metrics renomeado para 'Custo Médio IA' (metrics/page.tsx buildCampaignCards)"
    - "Conversão USD→BRL na página de métricas passa a usar economic_parameters.usd_brl_rate (fonte única D2) via EconomicParameterService; VENDEO_USD_BRL_RATE deixa de ser a fonte ativa (default 1.00 do parâmetro)"
    - "4 testes de métricas (12.8): getAvgCost NÃO lê campaign_pipeline.estimated_cost_usd; card 'Custo Médio IA' exibe média por entrega; USD→BRL usa economic_parameters.usd_brl_rate não env; regressão demais cards"
  artifacts:
    - path: "src/lib/metrics/pipeline-metrics.ts"
      provides: "getAvgCost apurando via call-level (D6)"
      contains: "getAvgCost"
    - path: "src/app/(app)/admin/metrics/page.tsx"
      provides: "Card 'Custo Médio IA' + USD→BRL via economic_parameters"
      contains: "Custo Médio IA"
    - path: "src/lib/metrics/__tests__/pipeline-metrics.test.ts"
      provides: "Testes do getAvgCost (12.8)"
      contains: "getAvgCost"
    - path: "src/app/(app)/admin/metrics/__tests__/page.test.tsx"
      provides: "Testes da página (12.8)"
      contains: "Custo Médio IA"
  key_links:
    - from: "src/lib/metrics/pipeline-metrics.ts (getAvgCost)"
      to: "public.admin_get_ai_costs (RPC F38.1)"
      via: "by_operation_run → média de custo_usd_total (apuração call-level)"
      pattern: "admin_get_ai_costs"
    - from: "src/app/(app)/admin/metrics/page.tsx"
      to: "src/lib/economic/economic-parameter-service.ts"
      via: "EconomicParameterService.getParameter('usd_brl_rate') — fonte única de conversão (D2)"
      pattern: "EconomicParameterService"
---

<objective>
Corrigir o `/admin/metrics` (D6 — obrigatório, consequência da nova contabilidade): `getAvgCost` deixa de ler `campaign_pipeline.estimated_cost_usd` (NULL por desenho desde a F38.1 — anti-dupla-contagem D1/D6) e passa a apurar **custo médio de IA por entrega** via apuração call-level; o card é renomeado para "**Custo Médio IA**"; a conversão USD→BRL passa a usar `economic_parameters.usd_brl_rate` (fonte única D2) via `EconomicParameterService`. `admin_get_metrics` (F28) **inalterado** — correção na camada de leitura. Testes (tarefa 12.8 — 4 testes).

Purpose: o card "Custo Médio" mostrava NULL após a F38.1 zerar o custo do delivery marker — a métrica antiga refletia a nova contabilidade de forma quebrada. A correção alinha a métrica à apuração call-level e à fonte única de câmbio.
Output: /admin/metrics corrigido (card "Custo Médio IA" não NULL) + testes verdes.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-2-admin-custos-operacionais/38-2-CONTEXT.md
@openspec/changes/fase-38-2-admin-custos-operacionais/specs/admin-metrics-dashboard/spec.md
@openspec/changes/fase-38-2-admin-custos-operacionais/specs/pipeline-metrics/spec.md

<interfaces>
Contratos que o executor precisa (extraídos dos specs D6 + 38-2-CONTEXT.md + leitura dos arquivos):

**Sem página nova (HAS_UI: 1) — é uma correção na página /admin/metrics EXISTENTE: a página é modificada (card renomeado + conversão USD→BRL via EconomicParameterService), mas nenhuma rota/página nova é criada. A verificação visual é coberta pelo checkpoint da fase (38-2-10).**

Estado verificado:
- `src/lib/metrics/pipeline-metrics.ts:109-115` — `getAvgCost(hours, storeKind)` retorna `bundle.pipeline.avg_cost_ms ?? null` (que vem do RPC `admin_get_metrics` F28 → lê `campaign_pipeline.estimated_cost_usd` → NULL por desenho).
- `src/app/(app)/admin/metrics/page.tsx:27` — `const USD_BRL_RATE = Number(process.env.VENDEO_USD_BRL_RATE ?? "5.50");` (fonte antiga).
- `src/app/(app)/admin/metrics/page.tsx:81` — card `{ label: "Custo Médio", value: data.avgCost, timeRange }` em buildCampaignCards.

Mudanças:
1. **`getAvgCost`** em `src/lib/metrics/pipeline-metrics.ts`:
   - NÃO usa mais `bundle.pipeline.avg_cost_ms`.
   - Passa a apurar custo médio de IA **por entrega** via apuração call-level: chama o RPC `admin_get_ai_costs` (`p_hours: hours`, `p_store_kind` equivalente se suportado — senão sem filtro de loja) e calcula `AVG(custo_usd_total)` sobre `by_operation_run` (cada run = entrega). OU usa o RPC novo `admin_get_ai_operation_runs` (D4) — `summary.tempo_medio` não é custo; usar `summary.custo_usd_total / summary.total` como média por entrega. Decisão de implementação: preferir o RPC `admin_get_ai_operation_runs` (D4, mais alinhado — summary já é sobre o conjunto filtrado) OU o `admin_get_ai_costs.by_operation_run` (F38.1 — disponível sem dependência nova). Escolher conforme estabilidade; documentar no código.
   - Retorna `number | null` (média de custo por entrega em USD). O nome da métrica muda semanticamente → card "Custo Médio IA".
   - Mantém os demais getters (`getAvgDuration`, `getSuccessRate`, etc.) intactos — regressão zero nos outros cards.
   - `admin_get_metrics` (F28) NÃO é alterado.

2. **Card + conversão** em `src/app/(app)/admin/metrics/page.tsx`:
   - Card `buildCampaignCards` → label "**Custo Médio IA**".
   - `USD_BRL_RATE` deixa de vir de `VENDEO_USD_BRL_RATE` (env ativo); passa a vir de `EconomicParameterService.getParameter("usd_brl_rate")` (fonte única D2) — default 1.00 quando fallback; env pode ficar apenas como fallback de bootstrap documentado (sem uso ativo).
   - Os demais cards continuam com o mesmo tratamento (usdToBrlRate repassado ao MetricsCards).

Padrão de teste (analog: `src/lib/metrics/__tests__/pipeline-metrics.test.ts` e `src/app/(app)/admin/metrics/__tests__/page.test.tsx` se existirem — verificar).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getAvgCost — apuração call-level por entrega (D6)</name>
  <files>src/lib/metrics/pipeline-metrics.ts</files>
  <read_first>
    - src/lib/metrics/pipeline-metrics.ts (getAvgCost — linhas 109-115; fetchMetricsBundle — linhas 42-77; padrão RPC admin_get_metrics)
    - src/lib/ai-cost/admin-service.ts (padrão de chamada do RPC admin_get_ai_costs — linhas 174-198)
    - openspec/changes/fase-38-2-admin-custos-operacionais/specs/pipeline-metrics/spec.md
  </read_first>
  <behavior>
    - Teste 1 (unit — casos de getAvgCost): `getAvgCost` NÃO usa `bundle.pipeline.avg_cost_ms` (mock: admin_get_metrics retorna avg_cost_ms não-nulo mas getAvgCost não o lê) — chama `admin_get_ai_costs` (ou `admin_get_ai_operation_runs`) e calcula média por entrega; com by_operation_run = [{10},{20}] → 15; com [] → null; RPC falha → null (degradação suave)
  </behavior>
  <action>
    Modificar `getAvgCost` em `src/lib/metrics/pipeline-metrics.ts`:
    1. Remover o campo `avg_cost_ms` de `MetricsBundle.pipeline` (linha 11), do fallback do bundle (linha 67) e de todo uso em `getAvgCost` — a métrica antiga não existe mais (grep `avg_cost_ms` == 0).
    2. Implementar a apuração call-level por entrega: `supabaseAdmin.rpc("admin_get_ai_costs", { p_hours: hours, p_credit_unit_usd_value: null })` → `by_operation_run` → média de `custo_usd_total` (usar toNumber/clamp — padronizar com o ai-cost admin service). OU `supabaseAdmin.rpc("admin_get_ai_operation_runs", {...})` → `summary.custo_usd_total / summary.total`. Escolher uma fonte e documentar.
    3. Manter a assinatura `getAvgCost(hours, storeKind)` e o fallback null em falha (degradação suave — não quebrar a página).
    4. Atualizar o JSDoc: "custo médio de IA por entrega (apuração call-level, D6)".
    5. NÃO alterar `admin_get_metrics` (F28) nem os demais getters.
    6. NOTA (F38.2-20/F38.2-22 pre-existentes): HealthBanner/computeHealthState (health-banner.tsx) e MetricCard types (metrics/types.ts) JÁ EXISTEM e NÃO têm delta na F38.2 — não procurar trabalho neles; apenas garantir regressão zero (Task 3).
  </action>
  <verify>
    <automated>npx vitest run src/lib/metrics/__tests__/pipeline-metrics.test.ts; npm run typecheck; `grep -c "avg_cost_ms" src/lib/metrics/pipeline-metrics.ts` == 0; `grep -c "admin_get_ai_costs" src/lib/metrics/pipeline-metrics.ts` >= 1; `grep -c "admin_get_metrics" src/lib/metrics/pipeline-metrics.ts` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - getAvgCost apura por entrega via call-level; não usa mais avg_cost_ms do bundle
    - admin_get_metrics (F28) inalterado; demais getters intactos
    - typecheck limpo
  </acceptance_criteria>
  <done>getAvgCost corrigido — custo médio de IA por entrega (D6)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Card 'Custo Médio IA' + USD→BRL via economic_parameters (D6/D2)</name>
  <files>src/app/(app)/admin/metrics/page.tsx</files>
  <read_first>
    - src/app/(app)/admin/metrics/page.tsx (linhas 27, 76-86 buildCampaignCards, 130-134 fetchMetrics)
    - src/lib/economic/economic-parameter-service.ts (EconomicParameterService — 38-2-02)
  </read_first>
  <behavior>
    - Teste 2 (page): card com label "Custo Médio IA" e value = média por entrega
    - Teste 3 (page): USD→BRL usa EconomicParameterService (usd_brl_rate) — NÃO process.env.VENDEO_USD_BRL_RATE ativo
    - Teste 4 (page): demais cards (Taxa de Sucesso, Erro, Tempo, Estorno, Usuários Ativos, VS, Wallet) sem regressão
  </behavior>
  <action>
    Modificar `src/app/(app)/admin/metrics/page.tsx`:
    1. `buildCampaignCards` → `{ label: "Custo Médio IA", value: data.avgCost, timeRange }`.
    2. Substituir `const USD_BRL_RATE = Number(process.env.VENDEO_USD_BRL_RATE ?? "5.50")` por resolução via `EconomicParameterService` (server): `const usdBrl = await new EconomicParameterService().getParameter("usd_brl_rate");` → `usdBrl.value` (default 1.00 no fallback). Env fica apenas como fallback de bootstrap DOCUMENTADO (não ativo) OU removido — seguir a decisão D2 (fonte única = parâmetro).
    3. Repassar `usdBrl.value` ao `MetricsCards usdToBrlRate` (mesmo ponto de uso atual das linhas 230-238).
    4. NÃO alterar os demais cards/sections.
  </action>
  <verify>
    <automated>npx vitest run src/app/(app)/admin/metrics/__tests__/page.test.tsx; npm run typecheck; `grep -c "Custo Médio IA" src/app/(app)/admin/metrics/page.tsx` >= 1; `grep -c "EconomicParameterService" src/app/(app)/admin/metrics/page.tsx` >= 1; `grep -c "VENDEO_USD_BRL_RATE" src/app/(app)/admin/metrics/page.tsx` <= 1</automated>
  </verify>
  <acceptance_criteria>
    - Card renomeado; USD→BRL via economic_parameters (fonte única D2)
    - Demais cards sem regressão
    - typecheck limpo
  </acceptance_criteria>
  <done>Card 'Custo Médio IA' + conversão via parâmetro econômico (D6/D2)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Testes completos (tarefa 12.8 — 4 testes)</name>
  <files>src/lib/metrics/__tests__/pipeline-metrics.test.ts, src/app/(app)/admin/metrics/__tests__/page.test.tsx</files>
  <read_first>
    - Testes existentes de pipeline-metrics e da página de métricas (se existirem — verificar)
  </read_first>
  <behavior>
    - Casos (tarefa 12.8 — 4 testes): getAvgCost NÃO lê campaign_pipeline.estimated_cost_usd (não usa avg_cost_ms); card "Custo Médio IA" exibe média por entrega; USD→BRL usa economic_parameters.usd_brl_rate não env; regressão demais cards
  </behavior>
  <action>
    Criar/estender os testes de `pipeline-metrics.ts` (mock do RPC admin_get_ai_costs / admin_get_ai_operation_runs) e da página (mock do EconomicParameterService + card label). Cobrir os 4 casos da behavior list.
  </action>
  <verify>
    <automated>npx vitest run src/lib/metrics/__tests__/pipeline-metrics.test.ts src/app/(app)/admin/metrics/__tests__/page.test.tsx; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - 4 testes passando (npx vitest run — 0 falhas)
    - typecheck limpo
  </acceptance_criteria>
  <done>Testes da correção do /admin/metrics verdes (tarefa 12.8)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| getAvgCost → RPCs de apuração | Leitura server-only via supabaseAdmin (service_role) |
| /admin/metrics ← admin autenticado | Página admin-only (já protegida pelo layout) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38.2-39 | Spoofing | Média incorreta (delivery marker incluso) | mitigate | getAvgCost usa APENAS apuração call-level (RPC já exclui delivery markers — D1/D6); não lê mais campaign_pipeline.estimated_cost_usd — F38.2-21 |
| T-38.2-40 | Tampering | Conversão com taxa errada | mitigate | Fonte única: economic_parameters.usd_brl_rate via EconomicParameterService (D2); env deprecado — F38.2-19 |
| T-38.2-41 | DoS | RPC de apuração em falha | mitigate | Fallback null (degradação suave — padrão atual do bundle); página continua renderizando os demais cards — F38.2-21 |
| T-38.2-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado neste plano — gate de legitimidade não se aplica |
</threat_model>

<verification>
- `npm run typecheck` limpo (getAvgCost + página)
- `npx vitest run src/lib/metrics/__tests__/pipeline-metrics.test.ts src/app/(app)/admin/metrics/__tests__/page.test.tsx` — 4 testes, 0 falhas
- `admin_get_metrics` (F28) inalterado; env VENDEO_USD_BRL_RATE deprecado (não ativo)
</verification>

<success_criteria>
- getAvgCost apura custo médio de IA por entrega via call-level (não NULL)
- Card "Custo Médio IA" no /admin/metrics
- USD→BRL via economic_parameters.usd_brl_rate (fonte única D2)
- 4 testes verdes (tarefa 12.8)
</success_criteria>

<output>
Create `.planning/phases/38-2-admin-custos-operacionais/38-2-09-SUMMARY.md` when done
</output>

---
phase: 38-credit-operation-costs
plan: 03
type: execute
wave: 2
depends_on: [38-02]
files_modified:
  - "src/app/api/campaign/generate-image/route.ts"
  - "src/lib/image-generation/config.ts"
  - "src/app/api/campaign/generate-image/__tests__/route.test.ts"
  - "src/__tests__/concurrency.test.ts"
  - "src/__tests__/regression-master-switch.test.ts"
  - "src/__tests__/api/campaign-generate.test.ts"
autonomous: true
requirements:
  - F38-ROUTES-01
  - F38-ROUTES-02
  - F38-ROUTES-04
  - F38-CONFIG-01
  - F38-CONFIG-02
must_haves:
  truths:
    - "generate-image resolve `campaign_generation` UMA vez por request (após guards, antes do saldo) via OperationCostService"
    - "`COST_PER_GENERATION` removido de config.ts e NENHUM import resta no repositório (src/)"
    - "`enabled=false` → 503 operation_disabled SEMPRE (independente de creditsChargingEnabled)"
    - "Erro de leitura → 503 operation_cost_unavailable (sem reserva, sem geração)"
    - "Balance check dinâmico `balance < cost.costCredits` → 402; reserva usa cost.costCredits com metadata snapshot"
  artifacts:
    - path: "src/app/api/campaign/generate-image/route.ts"
      provides: "Resolução de custo + guards 503 + balance dinâmico + reserva com snapshot"
      contains: "OperationCostService"
    - path: "src/lib/image-generation/config.ts"
      provides: "Config sem COST_PER_GENERATION (removido)"
      contains: "IMAGE_GENERATION_RESPONSES_MODEL"
  key_links:
    - from: "src/app/api/campaign/generate-image/route.ts"
      to: "OperationCostService.getCost(\"campaign_generation\")"
      via: "pré-stream, após rate limit, antes do balance check"
      pattern: "getCost"
    - from: "src/app/api/campaign/generate-image/route.ts"
      to: "creditService.reserveCredit(storeId, cost.costCredits, ...)"
      via: "metadata com operation_key/operation_cost_credits/operation_cost_source"
      pattern: "operation_cost_source"
---

<objective>
Integrar o custo dinâmico na rota `POST /api/campaign/generate-image` (campaign_generation, D12): resolver o custo **uma única vez por request** via `OperationCostService.getCost("campaign_generation")` após auth/ownership/readiness/rate guards e antes do saldo/reserva/IA paga; aplicar o guard `enabled=false` → `503 operation_disabled` (sempre, D4), fail-closed `OperationCostUnavailableError` → `503 operation_cost_unavailable` (D5), balance check dinâmico `balance < cost.costCredits` → 402 e reserva com metadata snapshot (D6). Remover `COST_PER_GENERATION` de `src/lib/image-generation/config.ts` (D12) e atualizar os 4 arquivos de teste que o referenciam.

Purpose: o custo de geração passa a vir da tabela `credit_operation_costs` (fonte única, admin sem deploy) — sem alterar o RPC `reserve_credit` (F24, D6).
Output: rota com custo dinâmico + suite de testes atualizada (rota + integração) sem regressões.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-credit-operation-costs/38-CONTEXT.md
@.planning/phases/38-credit-operation-costs/38-PATTERNS.md

<interfaces>
Contratos que o executor precisa (extraídos de 38-CONTEXT.md D4/D5/D6/D12 + 38-PATTERNS.md):

Estrutura atual da rota `src/app/api/campaign/generate-image/route.ts` (735 linhas — pontos exatos):
- Linha 4: `import { IMAGE_GENERATION_GLOBAL_TIMEOUT_MS, MAX_PRODUCT_IMAGE_BASE64_SIZE, IMAGE_GENERATION_RESPONSES_MODEL, COST_PER_GENERATION } from "@/lib/image-generation/config";` → remover `COST_PER_GENERATION`; adicionar `import { OperationCostService, OperationCostUnavailableError } from "@/lib/credit/operation-cost-service";` e `import type { OperationCostResolution } from "@/lib/credit/types";`
- Linhas 207-217: rate limit guard (permanece)
- Linha 220: `await recordGenerationAttempt(storeId, user.userId);`
- Linhas 222-235: balance check (linha 227: `if (balance < COST_PER_GENERATION)`) → **inserir resolução de custo ANTES deste bloco** e trocar por `balance < cost.costCredits`
- Linhas 342-366: reserva (linha 347: `creditService.reserveCredit(storeId, COST_PER_GENERATION, {...})`) → trocar por `cost.costCredits` + metadata snapshot

Bloco de resolução a inserir (após recordGenerationAttempt ~linha 220, antes do balance check ~linha 222):
```typescript
  // ── Pre-stream: Resolve operation cost (D12) ───────────────────
  let cost: OperationCostResolution;
  try {
    cost = await new OperationCostService().getCost("campaign_generation");
  } catch (err) {
    if (err instanceof OperationCostUnavailableError) {
      logPipelineEvent({ event: "operation_cost_unavailable", traceId, phase: "pre_stream", status: "failed", storeId, userId: user.userId });
      return Response.json(
        { error: "operation_cost_unavailable", operationKey: "campaign_generation", message: "Serviço indisponível no momento. Tente novamente em alguns instantes." },
        { status: 503 }
      );
    }
    throw err;
  }
  if (!cost.enabled) {
    return Response.json(
      { error: "operation_disabled", operationKey: cost.operationKey },
      { status: 503 }
    );
  }
```

Reserva (substitui linha 347):
```typescript
      creditTxId = await creditService.reserveCredit(storeId, cost.costCredits, {
        campaignId,
        idempotencyKey: `reserve_${campaignId}`,
        metadata: { feature: "campaign_pipeline", operation_key: cost.operationKey, operation_cost_credits: cost.costCredits, operation_cost_source: cost.source },
      });
```

Contrato de resposta 503 (D4/D5 — consistente em toda a fase; `error` é STRING com o code, não objeto):
- `503 { error: "operation_disabled", operationKey }` — availability, SEMPRE avaliado (mesmo freemium/creditsChargingEnabled=false)
- `503 { error: "operation_cost_unavailable", operationKey, message: "Serviço indisponível no momento. Tente novamente em alguns instantes." }` — fail-closed, sem reserva/geração
- `402` permanece balance-insuficiente apenas

Arquivos de teste que mockam `@/lib/image-generation/config` com `COST_PER_GENERATION: 1` (devem ter a chave removida + novo mock do service):
- `src/app/api/campaign/generate-image/__tests__/route.test.ts:47`
- `src/__tests__/concurrency.test.ts:44`
- `src/__tests__/regression-master-switch.test.ts:44`
- `src/__tests__/api/campaign-generate.test.ts:160`

Padrão de mock do service (usar em todos os 4 arquivos):
```typescript
const mockGetCost = vi.fn();
vi.mock("@/lib/credit/operation-cost-service", () => ({
  OperationCostService: vi.fn(function() {
    return { getCost: mockGetCost };
  }),
  OperationCostUnavailableError: class extends Error {},
  DEFAULT_OPERATION_COSTS: { campaign_generation: { costCredits: 1, enabled: true }, visual_signature_generation: { costCredits: 1, enabled: true } },
}));
// beforeEach: mockGetCost.mockResolvedValue({ operationKey: "campaign_generation", costCredits: 1, enabled: true, source: "table" });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: generate-image — custo dinâmico + guards + snapshot; remover COST_PER_GENERATION de config.ts</name>
  <files>src/app/api/campaign/generate-image/route.ts, src/lib/image-generation/config.ts</files>
  <read_first>
    - src/app/api/campaign/generate-image/route.ts (735 linhas — ler inteiro; pontos-chave: import linha 4, resolução a inserir após linha 220, balance linha 227, reserva linhas 342-366)
    - src/lib/image-generation/config.ts (linhas 39-40 — bloco COST_PER_GENERATION a remover)
    - src/lib/credit/operation-cost-service.ts (criado na 38-02 — assinatura de getCost e OperationCostUnavailableError)
    - src/lib/logging/pipeline-logger (logPipelineEvent — eventos pré-stream)
  </read_first>
  <action>
    Modificar `src/app/api/campaign/generate-image/route.ts`:

    1. Linha 4: remover `COST_PER_GENERATION` do import de `@/lib/image-generation/config`; adicionar `import { OperationCostService, OperationCostUnavailableError } from "@/lib/credit/operation-cost-service";` e `import type { OperationCostResolution } from "@/lib/credit/types";` (D7).

    2. Inserir o bloco de resolução de custo (contrato acima) APÓS `await recordGenerationAttempt(storeId, user.userId);` (linha 220) e ANTES do balance check (linha 222) — ordem exata de D12: auth/ownership/readiness/rate guards → resolução de custo → saldo → reserva. Sem cache extra (D12: uma leitura por request).

    3. Linha 227: `if (balance < COST_PER_GENERATION)` → `if (balance < cost.costCredits)`. Manter o 402 com a mesma mensagem de saldo.

    4. Linhas 347-351: `reserveCredit(storeId, cost.costCredits, { campaignId, idempotencyKey: \`reserve_${campaignId}\`, metadata: { feature: "campaign_pipeline", operation_key: cost.operationKey, operation_cost_credits: cost.costCredits, operation_cost_source: cost.source } })` (D6 — snapshot no ledger).

    5. Manter intactos: zona de stream, confirmação/estorno (refund mantém metadata de feature, D12 item 6), rate limit, timeout, logs existentes. NÃO alterar `reserve_credit` RPC.

    Modificar `src/lib/image-generation/config.ts`: remover as linhas 39-40 (`// ─── Generation Cost ───` + `export const COST_PER_GENERATION = 1;`). Os defaults agora vivem em `DEFAULT_OPERATION_COSTS` (38-02, D5).
  </action>
  <verify>
    <automated>npm run typecheck; `grep -c "COST_PER_GENERATION" src/lib/image-generation/config.ts` == 0; `grep -c "COST_PER_GENERATION" src/app/api/campaign/generate-image/route.ts` == 0; `grep -c "getCost(\"campaign_generation\")" src/app/api/campaign/generate-image/route.ts` == 1; `grep -c "operation_cost_unavailable" src/app/api/campaign/generate-image/route.ts` >= 1; `grep -c "operation_disabled" src/app/api/campaign/generate-image/route.ts` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - Import sem `COST_PER_GENERATION`; `OperationCostService`/`OperationCostUnavailableError`/`OperationCostResolution` importados
    - Resolução de custo após rate/attempt guards e antes do balance check (uma única vez)
    - `balance < cost.costCredits` no balance check; reserva com `cost.costCredits` + metadata com `operation_key`/`operation_cost_credits`/`operation_cost_source`
    - 503 operation_disabled (sempre, D4) e 503 operation_cost_unavailable (fail-closed, D5) presentes
    - `COST_PER_GENERATION` ausente de config.ts e da rota
  </acceptance_criteria>
  <done>generate-image com custo dinâmico + guards + snapshot; COST_PER_GENERATION removido da config</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar route.test.ts (mock do service + novos casos 503)</name>
  <files>src/app/api/campaign/generate-image/__tests__/route.test.ts</files>
  <read_first>
    - src/app/api/campaign/generate-image/__tests__/route.test.ts (747 linhas — mock config linha 43-48, setupSuccessMocks, teste 402 linha 331-349, teste reserva idempotente linha 533-548)
    - src/app/api/campaign/generate-image/route.ts (rota modificada na Task 1)
  </read_first>
  <action>
    Atualizar `src/app/api/campaign/generate-image/__tests__/route.test.ts`:

    1. Linhas 43-48: no mock de `@/lib/image-generation/config`, REMOVER a linha `COST_PER_GENERATION: 1,` (a chave não existe mais no módulo real).

    2. Adicionar o mock de `@/lib/credit/operation-cost-service` (padrão do contrato acima, com `mockGetCost`) e no `beforeEach`/`setupSuccessMocks` configurar `mockGetCost.mockResolvedValue({ operationKey: "campaign_generation", costCredits: 1, enabled: true, source: "table" })`. Ajustar o setup para que TODOS os testes existentes (402, sucesso, refund, idempotência) tenham o mock de custo resolvido antes de chamar POST.

    3. Teste 402 (linha 331): com `mockGetBalance.mockResolvedValue(0)` e cost=1 → continua 402. Verificar que o teste existente passa com o novo mock.

    4. Teste de reserva idempotente (linhas 533-548): a asserção `expect(mockReserveCredit).toHaveBeenCalledWith(STORE_ID, 1, expect.objectContaining({ idempotencyKey: \`reserve_${CAMPAIGN_ID}\` }))` continua válida (objectContaining não quebra com metadata extra) — confirmar e, se quiser maior fidelidade, adicionar `operation_cost_credits: 1` ao objectContaining.

    5. NOVOS casos (D4/D5):
       - **503 operation_disabled**: `mockGetCost.mockResolvedValue({ operationKey: "campaign_generation", costCredits: 1, enabled: false, source: "table" })` → expect status 503 e body com `error: "operation_disabled"` (string, D4), e `mockReserveCredit` NÃO chamado
       - **503 operation_cost_unavailable**: `mockGetCost.mockRejectedValue(new OperationCostUnavailableError("down"))` → expect status 503 e body com `error: "operation_cost_unavailable"` (string, D5); `mockReserveCredit` e `createCampaign` NÃO chamados (sem reserva nem geração)
       - **Reserva com snapshot**: no teste de sucesso, asserção `expect(mockReserveCredit).toHaveBeenCalledWith(STORE_ID, 1, expect.objectContaining({ metadata: expect.objectContaining({ feature: "campaign_pipeline", operation_key: "campaign_generation", operation_cost_credits: 1, operation_cost_source: "table" }) }))`
       - **Custo 2 resolvido da tabela**: `mockGetCost.mockResolvedValue({ costCredits: 2, enabled: true, source: "table" })` + `mockGetBalance.mockResolvedValue(1)` → 402 (balance < cost); e com `mockGetBalance.mockResolvedValue(2)` → reserva chamada com `2` como amount
    6. Usar `import { OperationCostUnavailableError } from "@/lib/credit/operation-cost-service"` apenas se necessário para o teste 503 — senão lançar um `new Error` genérico e testar via instance check do mock. NOTA: se o mock do módulo for `vi.mock` completo, o erro lançado pelo teste DEVE ser a classe exportada do mock (ou a rota não o reconhece como `instanceof`) — usar `class extends Error {}` exportado no mock factory conforme o contrato.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/campaign/generate-image/__tests__/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Mock de config sem `COST_PER_GENERATION`; mock de operation-cost-service adicionado com mockGetCost resolvido no setup
    - Testes existentes (402, sucesso, refund, idempotência, erro IA, timeout) passam sem alteração semântica
    - Novos testes: 503 operation_disabled (sem reserva), 503 operation_cost_unavailable (sem reserva/geração), snapshot no metadata, custo dinâmico 2 com 402/reserva corretos
  </acceptance_criteria>
  <done>route.test.ts cobre custo dinâmico, guards 503 e snapshot — sem regressões</done>
</task>

<task type="auto">
  <name>Task 3: Atualizar testes de integração (concurrency, regression-master-switch, campaign-generate)</name>
  <files>src/__tests__/concurrency.test.ts, src/__tests__/regression-master-switch.test.ts, src/__tests__/api/campaign-generate.test.ts</files>
  <read_first>
    - src/__tests__/concurrency.test.ts (196 linhas — mock config linha 40-45, credit-service mock linhas 78-89)
    - src/__tests__/regression-master-switch.test.ts (mock config linha 44)
    - src/__tests__/api/campaign-generate.test.ts (mock config linha 160)
    - src/__tests__/api/campaign-generate.test.ts (estrutura geral — como os mocks são configurados e os cenários chamados)
  </read_first>
  <action>
    Nos 3 arquivos (`src/__tests__/concurrency.test.ts`, `src/__tests__/regression-master-switch.test.ts`, `src/__tests__/api/campaign-generate.test.ts`):

    1. No mock de `@/lib/image-generation/config`: REMOVER a linha `COST_PER_GENERATION: 1,` (linhas 44, 44 e 160 respectivamente).

    2. Adicionar o mock de `@/lib/credit/operation-cost-service` no mesmo padrão do contrato (mockGetCost via `vi.hoisted` se o arquivo usar hoisted — seguir o estilo local de cada arquivo: concurrency/regression usam `vi.hoisted`, campaign-generate usa mocks no topo).

    3. No setup/beforeEach de cada arquivo, resolver `mockGetCost` com `{ operationKey: "campaign_generation", costCredits: 1, enabled: true, source: "table" }` para que os cenários existentes (concorrência saldo=1, master switch, freemium) mantenham o comportamento de custo 1.

    4. NÃO alterar a semântica dos cenários existentes — apenas adicionar o mock do custo para a rota resolver. Cenários freemium (`creditsChargingEnabled=false`) devem continuar passando com custo resolvido (o guard `enabled` é avaliado, mas com enabled:true o fluxo segue sem saldo/reserva — F38-CONFIG-01).

    5. Se algum cenário verificar o body de 402/503, manter as asserções; adicionar (se ausente) uma verificação de que `operation_disabled` NÃO é retornado quando enabled=true.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/concurrency.test.ts src/__tests__/regression-master-switch.test.ts src/__tests__/api/campaign-generate.test.ts; `grep -c "COST_PER_GENERATION" src/__tests__/concurrency.test.ts src/__tests__/regression-master-switch.test.ts src/__tests__/api/campaign-generate.test.ts` == 0</automated>
  </verify>
  <acceptance_criteria>
    - Nenhum dos 3 arquivos referencia `COST_PER_GENERATION`
    - Mock de operation-cost-service presente nos 3 com custo 1 resolvido no setup
    - Cenários existentes (concorrência, master switch/emergency brake, freemium, geração completa) passam sem alteração semântica
  </acceptance_criteria>
  <done>Testes de integração atualizados para a resolução de custo dinâmica — suite verde</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /api/campaign/generate-image | Input não confiável cruza aqui (body); a rota decide 402/503/200 com base em saldo e custo resolvido |
| Rota → credit_operation_costs | Leitura service_role do custo; falha de leitura NÃO pode virar geração grátis |
| Rota → reserve_credit | Reserva financeira — amount vem do custo resolvido (nunca de input do cliente) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-09 | Tampering | Custo injetado pelo cliente | mitigate | O cliente NUNCA envia o custo; o amount da reserva vem exclusivamente de `cost.costCredits` resolvido no servidor — F38-ROUTES-02 |
| T-38-10 | Denial of Service | Erro de leitura do custo | mitigate | Fail-closed: `OperationCostUnavailableError` → 503 operation_cost_unavailable SEM reserva/geração (não assume enabled:true) — F38-SERVICE-03, F38-ROUTES-01 |
| T-38-11 | Spoofing | Operação desligada roda grátis | mitigate | Guard `enabled=false` → 503 operation_disabled SEMPRE, independente de creditsChargingEnabled/v15Enabled (D4) — F38-CONFIG-01/02 |
| T-38-12 | Elevation of Privilege | Geração sem pagamento (freemium bypass) | mitigate | Freemium pula saldo/reserva APENAS para operação habilitada; guards de disponibilidade/habilitação permanecem — F38-CONFIG-01 |
| T-38-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado — gate de legitimidade não se aplica |
</threat_model>

<verification>
- `npm run typecheck` e `npm run lint` limpos após Task 1
- `npx vitest run` nos 4 arquivos de teste atualizados — 0 falhas
- `grep -r "COST_PER_GENERATION" src/` == 0 (nenhum import remanescente — F38-ROUTES-02/CONFIG)
</verification>

<success_criteria>
- generate-image resolve `campaign_generation` uma vez por request (após guards, antes do saldo)
- Guards 503 operation_disabled (sempre) / operation_cost_unavailable (fail-closed) implementados
- Balance 402 dinâmico; reserva com snapshot `operation_key/operation_cost_credits/operation_cost_source`
- `COST_PER_GENERATION` removido de config.ts sem imports restantes
- Suite de testes (rota + integração) verde
</success_criteria>

<output>
Create `.planning/phases/38-credit-operation-costs/38-03-SUMMARY.md` when done
</output>

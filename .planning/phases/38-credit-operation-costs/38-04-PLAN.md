---
phase: 38-credit-operation-costs
plan: 04
type: execute
wave: 2
depends_on: [38-02]
files_modified:
  - "src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts"
  - "src/app/api/store/[id]/visual-signature/__tests__/credit-flow.test.ts"
  - "src/app/api/store/[id]/visual-signature/__tests__/generate-without-logo.test.ts"
  - "src/app/api/store/[id]/visual-signature/generate-without-logo/__tests__/generate-route.test.ts"
autonomous: true
requirements:
  - F38-ROUTES-01
  - F38-ROUTES-03
  - F38-VS-01
  - F38-CONFIG-01
  - F38-CONFIG-02
must_haves:
  truths:
    - "generate-without-logo resolve `visual_signature_generation` UMA vez por request (após auth/legal/lock guards, antes do balance) via OperationCostService"
    - "Literal `1` substituído por `cost.costCredits` no balance check (linha 176) e na reserva (linha 186)"
    - "Guards 503 operation_disabled (sempre, D4) e operation_cost_unavailable (fail-closed, D5) — independentes de creditsEnabled/v15Enabled"
    - "Metadata persistido de store_visual_signatures inclui operation_key/operation_cost_credits/operation_cost_source além do credit_tx_id (F38-VS-01)"
  artifacts:
    - path: "src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts"
      provides: "Custo dinâmico + guards + reserva com snapshot + metadata persistido"
      contains: "OperationCostService"
  key_links:
    - from: "src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts"
      to: "OperationCostService.getCost(\"visual_signature_generation\")"
      via: "resolução após guards, antes do balance"
      pattern: "getCost"
    - from: "src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts"
      to: "updatedMetadata (store_visual_signatures)"
      via: "snapshot de custo ao lado do credit_tx_id"
      pattern: "operation_cost_source"
---

<objective>
Integrar o custo dinâmico na rota de assinatura visual `POST /api/store/[id]/visual-signature/generate-without-logo` (visual_signature_generation, D12): resolver o custo **uma única vez por request** via `OperationCostService.getCost("visual_signature_generation")`, substituir os literais `1` do balance check (`:176`) e da reserva (`:186`) por `cost.costCredits`, aplicar os guards `503 operation_disabled` (sempre — D4) e `503 operation_cost_unavailable` (fail-closed — D5), e persistir o snapshot de custo no metadata de `store_visual_signatures` ao lado do `credit_tx_id` existente (F38-VS-01).

Purpose: a assinatura visual passa a usar a mesma fonte única de custo da campanha, com ledger auto-descritivo (D6) — sem alterar `reserve_credit` (F24).
Output: rota VS com custo dinâmico + metadata snapshot + testes atualizados.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-credit-operation-costs/38-CONTEXT.md
@.planning/phases/38-credit-operation-costs/38-PATTERNS.md

<interfaces>
Contratos que o executor precisa (extraídos de 38-CONTEXT.md D4/D5/D6/D12 + 38-PATTERNS.md):

Estrutura atual da rota `src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts` (467 linhas — pontos exatos):
- Linha 2: `import { supabaseAdmin as supabase } from '@/lib/supabase/server';`
- Linhas 45-98: apiHandler + generationPaused guard + requireSameOrigin + requireAuthorizedStore + legal clearance + UUID + generationLocks
- Linhas 169-192: bloco de crédito:
  - Linha 170: `const creditsEnabled = launchConfig.v15Enabled && launchConfig.creditsChargingEnabled;`
  - Linhas 173-182: balance check (linha 176: `if (balance < 1)`) → 402
  - Linhas 184-191: reserva (linha 186: `creditService.reserveCredit(id, 1, {...})`) — operationId gerado aqui
- Linhas 346-369: success path — `updatedMetadata` (linha 362) + `if (creditTxId) { updatedMetadata.credit_tx_id = creditTxId; }` (linhas 367-369)
- Linhas 406-414: failure path — refund com metadata de feature

Ordem de inserção da resolução de custo (D12): APÓS o lock (`generationLocks.set` linha 89 / dentro do try) e ANTES do bloco `creditsEnabled` (linha 169). O guard `enabled=false` deve rodar SEMPRE — inclusive quando `v15Enabled=false` (compat v1.4, F38-CONFIG-02) e quando `creditsChargingEnabled=false` (freemium, F38-CONFIG-01).

Bloco de resolução (inserir antes da linha 170 `const creditsEnabled = ...`):
```typescript
    // ----- Resolve operation cost (D12) — SEMPRE, mesmo sem cobrança -----
    let cost: OperationCostResolution;
    try {
      cost = await new OperationCostService().getCost("visual_signature_generation");
    } catch (err) {
      if (err instanceof OperationCostUnavailableError) {
        console.error(`[generate-without-logo][req-${reqId}] operation_cost_unavailable`, { storeId: id });
        return NextResponse.json(
          { error: "Serviço indisponível no momento. Tente novamente em alguns instantes.", code: "operation_cost_unavailable", operationKey: "visual_signature_generation" },
          { status: 503 }
        );
      }
      throw err;
    }
    if (!cost.enabled) {
      console.log(`[generate-without-logo][req-${reqId}] operation_disabled`, { storeId: id });
      return NextResponse.json(
        { error: "Operação temporariamente desativada.", code: "operation_disabled", operationKey: cost.operationKey },
        { status: 503 }
      );
    }
```
NOTA: `NextResponse.json` é usado nesta rota (não `Response.json`).

Reserva (substitui linha 186):
```typescript
      creditTxId = await creditService.reserveCredit(id, cost.costCredits, {
        campaignId: null,
        idempotencyKey: `vs_reserve_${id}_${operationId}`,
        metadata: { feature: "visual_signature", mode, operationId, operation_key: cost.operationKey, operation_cost_credits: cost.costCredits, operation_cost_source: cost.source },
      });
```

Metadata persistido (F38-VS-01 — após linha 369 `updatedMetadata.credit_tx_id = creditTxId`):
```typescript
    if (cost) {
      updatedMetadata.operation_key = cost.operationKey;
      updatedMetadata.operation_cost_credits = cost.costCredits;
      updatedMetadata.operation_cost_source = cost.source;
    }
```
(`cost` está no escopo externo ao try? Não — a resolução acontece dentro do `try` principal; declarar `let cost` no escopo do handler para acessar no success path. Ajustar: declarar `let cost: OperationCostResolution | undefined;` antes do `try` e atribuir no bloco de resolução.)

Arquivos de teste VS a atualizar (assinaturas de reserve/refund e mocks):
- `src/app/api/store/[id]/visual-signature/__tests__/credit-flow.test.ts` — linha 66 `reserveCredit = mockReserveCredit`; linha 205 `expect(mockReserveCredit).toHaveBeenCalledWith(STORE_ID, 1, { ... metadata: { feature: "visual_signature", mode: "standard", operationId: "test-op-id" } })` — metadata ganha snapshot; linhas 249-277 refund
- `src/app/api/store/[id]/visual-signature/__tests__/generate-without-logo.test.ts` — linha 66 mock de credit-service
- `src/app/api/store/[id]/visual-signature/generate-without-logo/__tests__/generate-route.test.ts` — linha 59 mock de credit-service

Padrão de mock do service (usar nos 3 arquivos):
```typescript
const mockGetCost = vi.fn();
vi.mock("@/lib/credit/operation-cost-service", () => ({
  OperationCostService: vi.fn(function() {
    return { getCost: mockGetCost };
  }),
  OperationCostUnavailableError: class extends Error {},
  DEFAULT_OPERATION_COSTS: { campaign_generation: { costCredits: 1, enabled: true }, visual_signature_generation: { costCredits: 1, enabled: true } },
}));
// beforeEach: mockGetCost.mockResolvedValue({ operationKey: "visual_signature_generation", costCredits: 1, enabled: true, source: "table" });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: generate-without-logo — custo dinâmico + guards + snapshot persistido</name>
  <files>src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts</files>
  <read_first>
    - src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts (467 linhas — ler inteiro; pontos-chave: imports linhas 1-17, lock linhas 85-94, bloco crédito 169-192, success path 346-369, failure path 406-414)
    - src/lib/credit/operation-cost-service.ts (criado na 38-02)
    - src/lib/credit/types.ts (OperationCostResolution — D7)
  </read_first>
  <action>
    Modificar `src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts`:

    1. Imports: adicionar `import { OperationCostService, OperationCostUnavailableError } from "@/lib/credit/operation-cost-service";` e `import type { OperationCostResolution } from "@/lib/credit/types";`

    2. Declarar `let cost: OperationCostResolution | undefined;` no escopo do handler (junto de `let creditTxId`/`let operationId`, linhas 96-97) para acesso no success path.

    3. Inserir o bloco de resolução de custo (contrato acima) DENTRO do `try` principal, após o parse do body/mode (linha ~109) ou imediatamente antes da linha 169 (`// ----- v15Enabled check`), e ANTES do bloco `creditsEnabled`. O guard `enabled=false` roda SEMPRE (D4 — inclusive `v15Enabled=false` e `creditsChargingEnabled=false`).

    4. Linha 176: `if (balance < 1)` → `if (balance < cost.costCredits)` (dentro do `if (creditsEnabled)` existente).

    5. Linha 186: `reserveCredit(id, 1, {...})` → `reserveCredit(id, cost.costCredits, {...})` com metadata incluindo `operation_key: cost.operationKey, operation_cost_credits: cost.costCredits, operation_cost_source: cost.source` (D6 — snapshot).

    6. Success path (após linhas 367-369, junto do `credit_tx_id`): adicionar ao `updatedMetadata` `operation_key`, `operation_cost_credits`, `operation_cost_source` quando `cost` definido (F38-VS-01).

    7. Failure path (refund, linhas 413+): MANTER metadata de feature (D12 item 6 — refund não leva snapshot extra). NÃO alterar `reserve_credit` RPC.

    8. Manter: generationPaused guard (precedente de indisponibilidade — F38-CONFIG-02), locks, substitution guards, v15Enabled/creditsEnabled gate para saldo+reserva.
  </action>
  <verify>
    <automated>npm run typecheck; `grep -c "getCost(\"visual_signature_generation\")" src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts` == 1; `grep -c "operation_cost_unavailable" src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts` >= 1; `grep -c "operation_disabled" src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts` >= 1; `grep -c "operation_cost_source" src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts` >= 2; PowerShell: (Select-String -Path src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts -Pattern "if \(balance < cost\.costCredits\)").Count -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - Resolução de custo após auth/legal/lock guards e antes do balance; guard enabled sempre avaliado (D4)
    - Literal `1` substituído por `cost.costCredits` no balance check e na reserva (nenhum `balance < 1` ou `reserveCredit(id, 1` restante)
    - 503 operation_disabled e 503 operation_cost_unavailable presentes (NextResponse.json)
    - `updatedMetadata` inclui operation_key/operation_cost_credits/operation_cost_source + credit_tx_id (F38-VS-01)
    - Refund mantém metadata de feature (sem snapshot)
  </acceptance_criteria>
  <done>generate-without-logo com custo dinâmico + guards + snapshot persistido no metadata</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar testes VS (mocks do service + metadata snapshot)</name>
  <files>src/app/api/store/[id]/visual-signature/__tests__/credit-flow.test.ts, src/app/api/store/[id]/visual-signature/__tests__/generate-without-logo.test.ts, src/app/api/store/[id]/visual-signature/generate-without-logo/__tests__/generate-route.test.ts</files>
  <read_first>
    - src/app/api/store/[id]/visual-signature/__tests__/credit-flow.test.ts (linhas 60-90 setup, 200-280 asserções de reserve/refund e metadata persistido)
    - src/app/api/store/[id]/visual-signature/__tests__/generate-without-logo.test.ts (setup mocks)
    - src/app/api/store/[id]/visual-signature/generate-without-logo/__tests__/generate-route.test.ts (setup mocks)
  </read_first>
  <action>
    Nos 3 arquivos de teste VS:

    1. Adicionar o mock de `@/lib/credit/operation-cost-service` (padrão do contrato, `mockGetCost` via `vi.hoisted` se o arquivo usar hoisted) e resolver no setup/beforeEach com `{ operationKey: "visual_signature_generation", costCredits: 1, enabled: true, source: "table" }`.

    2. `credit-flow.test.ts` linha 205: `expect(mockReserveCredit).toHaveBeenCalledWith(STORE_ID, 1, { ... metadata: { feature: "visual_signature", mode: "standard", operationId: "test-op-id" } })` → atualizar o metadata para incluir `operation_key: "visual_signature_generation"`, `operation_cost_credits: 1`, `operation_cost_source: "table"` (ou usar `expect.objectContaining` para o metadata).

    3. `credit-flow.test.ts` asserção de metadata persistido (linha 216-222: verifica `credit_tx_id` no update): adicionar verificação de que `operation_key`/`operation_cost_credits`/`operation_cost_source` também estão no metadata persistido (F38-VS-01).

    4. Asserções de refund (linhas 249-256 e 270-277): metadata de refund MANTÉM feature (sem snapshot) — verificar que não quebram; se o teste usava asserção exata do metadata de reserve para o refund, ajustar para refletir a separação (refund sem snapshot).

    5. `generate-without-logo.test.ts` e `generate-route.test.ts`: apenas adicionar o mock do service e o setup do mockGetCost (cenários existentes: sucesso, erro IA, storage fail, lock) — sem alterar semântica.

    6. NOVO caso em `credit-flow.test.ts` (D4 — guard independente de cobrança): `creditsChargingEnabled: false` (launchConfig mock) + `mockGetCost.mockResolvedValue({ costCredits: 1, enabled: false, source: "table" })` → expect 503 operation_disabled (operação desligada NÃO roda grátis mesmo com cobrança desligada — F38-CONFIG-01). E `enabled: true` + cobrança desligada → sucesso sem reserveCredit (freemium) — verificar que o cenário freemium existente cobre ou adicionar.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/store/[id]/visual-signature/__tests__/credit-flow.test.ts src/app/api/store/[id]/visual-signature/__tests__/generate-without-logo.test.ts src/app/api/store/[id]/visual-signature/generate-without-logo/__tests__/generate-route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Mock de operation-cost-service nos 3 arquivos com custo 1 resolvido no setup
    - Asserções de reserve com snapshot no metadata; metadata persistido verificado com snapshot (F38-VS-01)
    - Refund sem snapshot (feature metadata mantida)
    - Novo caso: operation_disabled com cobrança desligada → 503 (D4/F38-CONFIG-01)
    - Testes existentes passam sem alteração semântica
  </acceptance_criteria>
  <done>Testes VS atualizados com snapshot + guard independente de cobrança — suite verde</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST .../generate-without-logo | Input não confiável (body com mode/rejectionContext) cruza aqui; a rota decide 402/503/200 |
| Rota → credit_operation_costs | Leitura service_role; falha de leitura NÃO pode virar geração grátis |
| Rota → reserve_credit + store_visual_signatures.metadata | Reserva financeira + registro de custo na metadata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-13 | Tampering | Custo injetado via body | mitigate | Amount da reserva vem exclusivamente de `cost.costCredits` (server); body nunca carrega custo — F38-ROUTES-03 |
| T-38-14 | Denial of Service | Erro de leitura do custo | mitigate | Fail-closed → 503 operation_cost_unavailable sem reserva/geração — F38-SERVICE-03 |
| T-38-15 | Spoofing | VS desligada roda grátis (freemium) | mitigate | Guard enabled avaliado SEMPRE, antes do gate creditsEnabled; operation_disabled → 503 mesmo com cobrança desligada — F38-CONFIG-01 |
| T-38-16 | Tampering | Ledger VS sem rastreio de custo | mitigate | Metadata persistida inclui snapshot operation_key/operation_cost_credits/operation_cost_source ao lado do credit_tx_id — F38-VS-01 |
| T-38-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado — gate de legitimidade não se aplica |
</threat_model>

<verification>
- `npm run typecheck` e `npm run lint` limpos após Task 1
- `npx vitest run` nos 3 arquivos VS atualizados — 0 falhas
- Nenhum literal `1` restante no bloco de crédito da rota VS
</verification>

<success_criteria>
- generate-without-logo resolve `visual_signature_generation` uma vez por request com guards 503 (enabled sempre / unavailable fail-closed)
- Balance 402 e reserva dinâmicos com snapshot no metadata
- Metadata persistido de VS auto-descritivo: operation_key/operation_cost_credits/operation_cost_source + credit_tx_id
- Testes VS verdes, incluindo caso freemium com operação desligada → 503
</success_criteria>

<output>
Create `.planning/phases/38-credit-operation-costs/38-04-SUMMARY.md` when done
</output>

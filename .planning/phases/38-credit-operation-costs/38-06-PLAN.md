---
phase: 38-credit-operation-costs
plan: 06
type: execute
wave: 3
depends_on: [38-02]
files_modified:
  - "src/app/api/operation-costs/route.ts"
  - "src/app/api/operation-costs/__tests__/route.test.ts"
  - "src/lib/credit/format.ts"
  - "src/components/credit/balance-display.tsx"
  - "src/components/credit/balance-card.tsx"
  - "src/components/credit/__tests__/balance-card.test.tsx"
  - "src/hooks/use-operation-costs.ts"
  - "src/hooks/__tests__/use-operation-costs.test.ts"
autonomous: true
requirements:
  - F38-API-01
  - F38-API-02
  - F38-API-03
  - F38-UI-02
  - F38-UI-05
must_haves:
  truths:
    - "`GET /api/operation-costs` (requer login via apiHandler/requireApiUser) retorna mapa `{ \"campaign_generation\": { costCredits, enabled }, \"visual_signature_generation\": {...} }` resolvido via OperationCostService — SEM `updated_by`/`updated_at`/`source`; 503 operation_cost_unavailable em erro de leitura (fail-closed)"
    - "`useOperationCosts()` (client) faz fetch + cache do endpoint com estados loading/unavailable(503)/loaded; contrato único consumido por form e modais"
    - "`formatCredits(n)` compartilhado (1 crédito / N créditos) em src/lib/credit/format.ts; balance-display e balance-card o usam (sem duplicação)"
    - "balance-card exibe 'Cada geração consome {cost} crédito(s).' dinâmico via hook + formatCredits; sem custo presumido quando indisponível"
  artifacts:
    - path: "src/app/api/operation-costs/route.ts"
      provides: "GET público-autenticado de custos resolvidos"
      exports: ["GET"]
    - path: "src/hooks/use-operation-costs.ts"
      provides: "Hook client com cache + estados loading/unavailable/loaded + refetch"
      exports: ["useOperationCosts"]
    - path: "src/lib/credit/format.ts"
      provides: "formatCredits — pluralização compartilhada (1 crédito / N créditos)"
      exports: ["formatCredits"]
  key_links:
    - from: "src/app/api/operation-costs/route.ts"
      to: "OperationCostService.getCost (por chave do enum)"
      via: "Promise.all sobre OPERATION_KEYS; erro → 503"
      pattern: "getCost"
    - from: "src/hooks/use-operation-costs.ts"
      to: "/api/operation-costs"
      via: "fetch client + cache module-level"
      pattern: "fetch\\(\\\"/api/operation-costs\\\""
    - from: "src/components/credit/balance-card.tsx"
      to: "src/hooks/use-operation-costs.ts + src/lib/credit/format.ts"
      via: "description dinâmica com plural correto"
      pattern: "formatCredits"
---

<objective>
Entregar a camada de dados client da fonte de custo (D11): o endpoint `GET /api/operation-costs` (autenticado via apiHandler + `requireApiUser`, custos RESOLVIDOS sem expor `updated_by`/`updated_at`/`source`, fail-closed 503 — F38-API-01/03), o helper compartilhado `formatCredits` (pluralização 1 crédito / N créditos — D11) com refactor de `balance-display.tsx` e `balance-card.tsx` para eliminarem a duplicação, e o hook client `useOperationCosts()` com cache + estados loading/unavailable/loaded + refetch (F38-API-02, F38-UI-05), já consumido pelo `balance-card` (descrição dinâmica — F38-UI-02).

Purpose: form e modais (38-07) consomem um ÚNICO contrato de custo com semântica fail-closed — nunca presumem "1 crédito" quando o custo está indisponível.
Output: endpoint + hook + formatCredits testados; balance-card dinâmico sem duplicação de formatação.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-credit-operation-costs/38-CONTEXT.md
@.planning/phases/38-credit-operation-costs/38-PATTERNS.md

<interfaces>
Contratos extraídos de 38-CONTEXT.md (D11) + 38-PATTERNS.md (linhas 255-295, 458-489, 526-564) — o executor NÃO precisa explorar o código:

Endpoint (D11 — resposta exata, sem updated_by/updated_at/source):
```json
GET /api/operation-costs   (requer login — apiHandler + requireApiUser)
→ 200 { "campaign_generation": { "costCredits": 1, "enabled": true },
        "visual_signature_generation": { "costCredits": 1, "enabled": true } }
→ 503 { "error": "operation_cost_unavailable", "message": "Serviço indisponível no momento. Tente novamente em alguns instantes." }
→ 401 via apiHandler (requireApiUser lança UnauthorizedError)
```

Hook (D11/F38-API-02/F38-UI-05 — contrato exato):
```typescript
// src/hooks/use-operation-costs.ts  (sem "use client" — arquivo de hook, padrão use-changelog-state.ts)
export type OperationCostsMap = Record<OperationKey, { costCredits: number; enabled: boolean }>;
export type UseOperationCostsStatus = "loading" | "unavailable" | "loaded";
export function useOperationCosts(): {
  costs: OperationCostsMap | null;
  status: UseOperationCostsStatus;
  refetch: () => void;
}
```
- Cache: module-level (`let cache: OperationCostsMap | null = null`) + dedupe de fetch concorrente (`let inflight: Promise<...> | null`) — 1 leitura por request, sem cache distribuído (Deferred)
- `refetch` limpa o cache e refaz o fetch
- Erro de rede OU `!res.ok` (incl. 503) → status `unavailable` ("Tente novamente em alguns instantes") — NUNCA custo presumido

formatCredits (extraído de balance-display.tsx:14-18 — duplicado em balance-card.tsx:17-21):
```typescript
// src/lib/credit/format.ts  (sem server-only — importável por client)
export function formatCredits(value: number): string {
  if (value <= 0) return "0 créditos";
  if (value === 1) return "1 crédito";
  return `${value} créditos`;
}
```

balance-card (F38-UI-02 — `src/components/credit/balance-card.tsx`):
- Linhas 17-21: remover `formatBalance` local → `import { formatCredits } from "@/lib/credit/format";` (substituir usos: linha 62 `formatBalance(balance ?? 0)` e linha 68 `Você tem ${balance} crédito(s)` pode permanecer)
- Linha 63: `description: "Cada geração consome 1 crédito."` → dinâmico via `useOperationCosts()` (normal state):
  `status === "loaded" && costs?.campaign_generation` → `Cada geração consome ${formatCredits(costCredits)}.`; senão (loading/unavailable) → `"Cada geração consome créditos."` (sem número presumido — F38-UI-02)
- `ReadyContent` é componente renderizado como `<ReadyContent {...props} />` (linha 203) — hooks são válidos dentro dele

Padrões de teste:
- Rota: `src/app/api/admin/__tests__/credits-grant.test.ts` (mock server-only + require-admin + supabase/server; `NextRequest`; status matrix). Para esta rota mockar `@/lib/auth/require-user` (`requireApiUser`) e `@/lib/credit/operation-cost-service`.
- Hook: `renderHook` de `@testing-library/react` (usado em store-identity-form.drift-tabs.test.ts:437) + `vi.stubGlobal("fetch", ...)`; `vi.resetModules()` entre casos (cache module-level).
- balance-card.test.tsx (node + renderToString): adicionar `vi.mock("@/hooks/use-operation-costs", ...)` no topo.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GET /api/operation-costs + teste de rota</name>
  <files>src/app/api/operation-costs/route.ts, src/app/api/operation-costs/__tests__/route.test.ts</files>
  <read_first>
    - src/app/api/admin/audit-log/route.ts (padrão apiHandler + NextResponse.json — linhas 1-53)
    - src/lib/auth/require-user.ts (requireApiUser — linhas 40-42)
    - src/lib/credit/operation-cost-service.ts (getCost + OperationCostUnavailableError — criado na 38-02)
    - src/app/api/admin/__tests__/credits-grant.test.ts (padrão de teste de rota)
  </read_first>
  <action>
    Criar `src/app/api/operation-costs/route.ts`:
    1. Imports: `NextResponse` (next/server), `apiHandler` (@/lib/auth/api-handler), `requireApiUser` (@/lib/auth/require-user), `OperationCostService, OperationCostUnavailableError` (@/lib/credit/operation-cost-service), `OPERATION_KEYS` (@/lib/credit/types).
    2. `export const GET = apiHandler(async () => { ... })`:
       a. `await requireApiUser();` (gate de login — D11; retorno descartado)
       b. `const service = new OperationCostService();`
       c. Resolver UMA vez por chave do enum: `const entries = await Promise.all(OPERATION_KEYS.map(async (key) => { const r = await service.getCost(key); return [key, { costCredits: r.costCredits, enabled: r.enabled }]; }))` — chamar getCost UMA ÚNICA vez por chave (nunca 2x), DESESTRUTURAR apenas costCredits/enabled (nunca repassar `source` — D11).
       d. `return NextResponse.json(Object.fromEntries(entries));`
       e. try/catch: `OperationCostUnavailableError` → `NextResponse.json({ error: "operation_cost_unavailable", message: "Serviço indisponível no momento. Tente novamente em alguns instantes." }, { status: 503 })` (fail-closed — F38-API-01); outros erros re-throw.
    3. NÃO expor updated_by/updated_at/source em hipótese alguma (D11); NÃO usar getAllCosts (é admin-only com campos internos).

    Criar `src/app/api/operation-costs/__tests__/route.test.ts` (padrão credits-grant.test.ts):
    - Mocks: `server-only`, `@/lib/auth/require-user` (`requireApiUser`), `@/lib/credit/operation-cost-service` (OperationCostService + OperationCostUnavailableError + DEFAULT_OPERATION_COSTS)
    - GET 200: getCost resolve 1 para as 2 chaves → body `{ campaign_generation: { costCredits: 1, enabled: true }, visual_signature_generation: {...} }`; assert que o body NÃO contém `updated_by`/`updatedAt`/`source` (JSON.stringify não contém essas chaves)
    - GET 503: getCost lança OperationCostUnavailableError → status 503 + `error: "operation_cost_unavailable"`
    - GET 401: requireApiUser rejeita com UnauthorizedError → apiHandler responde 401
  </action>
  <verify>
    <automated>npm run typecheck; npx vitest run src/app/api/operation-costs/__tests__/route.test.ts; `grep -c "requireApiUser" src/app/api/operation-costs/route.ts` == 1; `grep -c "operation_cost_unavailable" src/app/api/operation-costs/route.ts` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - GET autenticado (apiHandler + requireApiUser) retorna mapa por chave com costCredits/enabled apenas
    - 503 fail-closed em erro de leitura; 401 sem login
    - Nenhum campo interno (updated_by/updated_at/source) na resposta — testado
    - F38-API-01 + F38-API-03 (handler HTTP converte OperationCostUnavailableError em 503)
  </acceptance_criteria>
  <done>GET /api/operation-costs implementado e testado — contrato client de custo</done>
</task>

<task type="auto">
  <name>Task 2: formatCredits + refactor balance-display/balance-card + teste</name>
  <files>src/lib/credit/format.ts, src/components/credit/balance-display.tsx, src/components/credit/balance-card.tsx, src/components/credit/__tests__/balance-card.test.tsx</files>
  <read_first>
    - src/components/credit/balance-display.tsx (formatBalance local — linhas 14-18; usos)
    - src/components/credit/balance-card.tsx (formatBalance local — linhas 17-21; stateConfig — linhas 60-76; ReadyContent — linha 30)
    - src/components/credit/__tests__/balance-card.test.tsx (25 linhas — adicionar mock do hook)
  </read_first>
  <action>
    1. Criar `src/lib/credit/format.ts` com `formatCredits(value: number): string` — contrato exato do `<interfaces>` (value <= 0 → "0 créditos"; === 1 → "1 crédito"; senão `${value} créditos`). SEM server-only (importável de client).
    2. `balance-display.tsx`: remover a função local `formatBalance` (linhas 14-18) → `import { formatCredits } from "@/lib/credit/format";` e substituir os usos (comportamento idêntico: <= 0 → "0 créditos", 1 → "1 crédito"). O teste existente `balance-display.test.tsx` deve continuar passando SEM alteração.
    3. `balance-card.tsx`:
       - Remover `formatBalance` local (linhas 17-21) → `import { formatCredits } from "@/lib/credit/format";` (usar em `formatCredits(balance ?? 0) disponíveis` — linha 62)
       - `import { useOperationCosts } from "@/hooks/use-operation-costs";` e no `ReadyContent`: `const { costs, status } = useOperationCosts();`
       - Linha 63 (normal state): `description: status === "loaded" && costs?.campaign_generation ? \`Cada geração consome ${formatCredits(costs.campaign_generation.costCredits)}.\` : "Cada geração consome créditos."` (F38-UI-02 — plural correto, sem custo presumido em loading/unavailable)
    4. `balance-card.test.tsx`: adicionar no topo `vi.mock("@/hooks/use-operation-costs", () => ({ useOperationCosts: () => ({ costs: { campaign_generation: { costCredits: 1, enabled: true }, visual_signature_generation: { costCredits: 1, enabled: true } }, status: "loaded", refetch: vi.fn() }) }));` — as 3 asserções existentes continuam válidas (linha 10 "Cada geração consome 1 crédito" permanece com cost 1). ADICIONAR caso: hook status "unavailable" → descrição sem número ("Cada geração consome créditos.") — sem "1 crédito" presumido.
  </action>
  <verify>
    <automated>npm run typecheck; npm run lint; npx vitest run src/components/credit/__tests__/balance-display.test.tsx src/components/credit/__tests__/balance-card.test.tsx; `grep -c "function formatBalance" src/components/credit/balance-card.tsx` == 0; `grep -c "function formatBalance" src/components/credit/balance-display.tsx` == 0</automated>
  </verify>
  <acceptance_criteria>
    - formatCredits exportado de src/lib/credit/format.ts (sem server-only)
    - Nenhuma duplicação de formatBalance restante em balance-display/balance-card (ambos usam formatCredits)
    - balance-card com descrição dinâmica via hook + plural correto; estado indisponível sem número
    - Testes de balance-card/balance-display verdes (incluindo novo caso unavailable)
  </acceptance_criteria>
  <done>formatCredits compartilhado + balance-card dinâmico — F38-UI-02</done>
</task>

<task type="auto">
  <name>Task 3: useOperationCosts hook + teste</name>
  <files>src/hooks/use-operation-costs.ts, src/hooks/__tests__/use-operation-costs.test.ts</files>
  <read_first>
    - src/components/flow/use-drift-detection.ts (padrão fetch client + AbortController — linhas 108-130)
    - src/hooks/use-changelog-state.ts (convenções de hook: tipos + retorno)
    - src/lib/credit/types.ts (OperationKey — D7)
  </read_first>
  <action>
    Criar `src/hooks/use-operation-costs.ts` (contrato exato do `<interfaces>`):
    1. Tipos exportados: `OperationCostsMap` (`Record<OperationKey, { costCredits: number; enabled: boolean }>`), `UseOperationCostsStatus` ("loading" | "unavailable" | "loaded").
    2. Cache module-level: `let cache: OperationCostsMap | null = null;` + `let inflight: Promise<OperationCostsMap> | null = null;` (dedupe de montagens concorrentes).
    3. `export function useOperationCosts()`:
       - Estado inicial: `useState<OperationCostsMap | null>(cache)` + `status` inicial `cache ? "loaded" : "loading"` (D11 — cache evita re-fetch)
       - `load`: setStatus("loading"); se `inflight` nulo → `inflight = fetch("/api/operation-costs", { cache: "no-store" }).then(res => { if (!res.ok) throw new Error(\`HTTP \${res.status}\`); return res.json(); }).then(data => { cache = data; inflight = null; return data; }).catch(err => { inflight = null; throw err; })`; encadear `.then(data => { setCosts(data); setStatus("loaded"); }).catch(() => setStatus("unavailable"))` (503 e qualquer !ok → "unavailable" — "Tente novamente em alguns instantes", sem custo presumido — F38-API-02)
       - `useEffect(() => { if (!cache) load(); }, [load])` — fetch apenas se cache vazio
       - `refetch`: `cache = null; load();`
       - Retorno `{ costs, status, refetch }` com `useCallback` para load/refetch (estáveis)
    4. NÃO usar AbortController/cleanup (1 leitura por request, sem cancelamento — Deferred); NÃO usar React Query.

    Criar `src/hooks/__tests__/use-operation-costs.test.ts` (`// @vitest-environment jsdom`, `renderHook` de @testing-library/react):
    - `vi.stubGlobal("fetch", vi.fn())`; `vi.resetModules()` + `await import` dinâmico por caso (cache module-level isola entre casos)
    - Caso sucesso: fetch resolve `{ campaign_generation: { costCredits: 1, enabled: true }, visual_signature_generation: { costCredits: 1, enabled: true } }` → após waitFor: `status === "loaded"`, `costs` com as 2 chaves
    - Caso 503: fetch resolve `new Response(null, { status: 503 })` → `status === "unavailable"`, `costs === null`
    - Caso erro de rede: fetch rejeita → `status === "unavailable"`
    - Caso cache: dois renderHook no MESMO módulo → fetch chamado UMA vez (cache module-level)
    - Caso refetch: `result.current.refetch()` com fetch mockado retornando valor novo → cache atualizado e fetch chamado 2 vezes
  </action>
  <verify>
    <automated>npm run typecheck; npx vitest run src/hooks/__tests__/use-operation-costs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Hook com cache module-level + dedupe; estados loading/unavailable/loaded; refetch
    - 503 e erro de rede → unavailable (sem custo presumido)
    - Testes: sucesso / 503 / rede / cache / refetch — verdes
    - F38-API-02 + F38-UI-05 (contrato único para form e modais)
  </acceptance_criteria>
  <done>useOperationCosts implementado e testado — contrato client único</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client (navegador) → GET /api/operation-costs | Requisição autenticada (cookie); resposta leva custos resolvidos — nunca campos internos |
| Hook client → endpoint | Fetch via HTTP; falha de rede/503 deve degradar para estado unavailable, não custo presumido |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-23 | Information Disclosure | GET /api/operation-costs | mitigate | Resposta construída com apenas `{ costCredits, enabled }` por chave (mapeamento explícito); `updated_by`/`updated_at`/`source` nunca saem do service — F38-API-01 |
| T-38-24 | Elevation of Privilege | Endpoint sem login | mitigate | `requireApiUser()` no handler (UnathorizedError → 401 via apiHandler) — D11 |
| T-38-25 | Denial of Service | Erro de leitura do custo | mitigate | Fail-closed: OperationCostUnavailableError → 503; hook marca unavailable e UI nunca presume custo — F38-API-03 |
| T-38-26 | Spoofing | Custo antigo no cache | mitigate | Fetch com `cache: "no-store"`; cache module-level é só por request-sessão e limpo no refetch — F38-API-02 |
| T-38-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado — gate de legitimidade não se aplica |
</threat_model>

<verification>
- `npm run typecheck` e `npm run lint` limpos
- `npx vitest run`: operation-costs route test, balance-card/balance-display tests, use-operation-costs test — 0 falhas
- Nenhum `formatBalance` local restante em balance-display/balance-card (grep == 0)
- Resposta do GET não contém updated_by/updated_at/source (assert no teste)
</verification>

<success_criteria>
- `GET /api/operation-costs` autenticado, resolvido, sem campos internos, fail-closed 503 (F38-API-01/03)
- `useOperationCosts()` com cache + loading/unavailable/loaded + refetch (F38-API-02, F38-UI-05)
- `formatCredits` único em src/lib/credit/format.ts; balance-display e balance-card sem duplicação
- balance-card com custo dinâmico e plural correto (F38-UI-02)
</success_criteria>

<output>
Create `.planning/phases/38-credit-operation-costs/38-06-SUMMARY.md` when done
</output>

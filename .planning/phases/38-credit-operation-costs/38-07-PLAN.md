---
phase: 38-credit-operation-costs
plan: 07
type: execute
wave: 4
depends_on: [38-06]
files_modified:
  - "src/components/flow/campaign-input-form.tsx"
  - "src/components/flow/drift-critical-modal.tsx"
  - "src/components/flow/visual-signature-approval-modal.tsx"
  - "src/components/flow/__tests__/visual-signature-approval-modal.test.tsx"
  - "src/components/flow/__tests__/drift-critical-modal.test.ts"
  - "src/components/flow/__tests__/store-identity-form.drift-tabs.test.ts"
  - "src/app/(app)/campanhas/nova/__tests__/campaign-flow-credits.test.tsx"
autonomous: true
requirements:
  - F38-UI-01
  - F38-UI-03
  - F38-UI-04
must_haves:
  truths:
    - "campaign-input-form exibe 'Custo: {cost}' dinâmico (campaign_generation via hook); desabilita submit quando balance < cost; operação desligada ou custo indisponível → desabilitado com mensagem, sem custo presumido"
    - "drift-critical-modal e visual-signature-approval-modal exibem custo dinâmico de visual_signature_generation com plural correto (formatCredits); indisponível → frase de custo omitida (sem '1 crédito' presumido)"
    - "Testes que renderizam os 3 componentes mockam @/hooks/use-operation-costs e validam o novo comportamento"
  artifacts:
    - path: "src/components/flow/campaign-input-form.tsx"
      provides: "Custo dinâmico + lógica de disable (balance < cost, enabled=false, unavailable)"
      contains: "useOperationCosts"
    - path: "src/components/flow/drift-critical-modal.tsx"
      provides: "Custo dinâmico de visual_signature_generation no alerta sem crédito"
      contains: "formatCredits"
    - path: "src/components/flow/visual-signature-approval-modal.tsx"
      provides: "Custo dinâmico de visual_signature_generation na sub-message insufficient_credits"
      contains: "formatCredits"
  key_links:
    - from: "src/components/flow/campaign-input-form.tsx"
      to: "src/hooks/use-operation-costs.ts"
      via: "costs.campaign_generation para Custo + disable"
      pattern: "campaign_generation"
    - from: "src/components/flow/drift-critical-modal.tsx"
      to: "src/lib/credit/format.ts"
      via: "formatCredits para plural"
      pattern: "formatCredits"
    - from: "src/components/flow/visual-signature-approval-modal.tsx"
      to: "src/hooks/use-operation-costs.ts"
      via: "costs.visual_signature_generation"
      pattern: "visual_signature_generation"
---

<objective>
Aplicar o custo dinâmico (D11) nos 3 componentes de UI que hoje exibem strings fixas "1 crédito": `campaign-input-form.tsx` (F38-UI-01 — `Custo: {cost}` dinâmico, submit desabilitado quando `balance < cost`, operação desligada ou custo indisponível), `drift-critical-modal.tsx` (F38-UI-03) e `visual-signature-approval-modal.tsx` (F38-UI-04) — ambos com custo de `visual_signature_generation` via hook + `formatCredits`, omitindo a frase de custo quando o serviço está indisponível (nunca presumir "1 crédito"). Atualizar os 4 arquivos de teste que renderizam esses componentes com o mock do hook.

Purpose: a UI reflete a fonte única de custo (tabela) com fail-closed de exibição — o que o lojista vê bate com o que a rota cobra.
Output: 3 componentes dinâmicos + testes atualizados (suite verde).
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-credit-operation-costs/38-CONTEXT.md
@.planning/phases/38-credit-operation-costs/38-PATTERNS.md

<interfaces>
Contratos extraídos de 38-CONTEXT.md (D11 — tabela de mudanças de UI, linhas 185-192) + 38-PATTERNS.md (linhas 642-658, 713-716). Textos e linhas exatas atuais:

**campaign-input-form.tsx** (F38-UI-01):
- Linha 505: `<span>Custo: 1</span>` (dentro do bloco `balance !== null ? (...)`, linhas 501-509)
- Linha 523: `disabled={isSubmitting || balance === 0 || balance === null}`
- Linhas 524-530: `title` com mensagens por condição
- Linha 513: `{balance === 0 && <CreditCta variant="zero" supportEmail={supportEmail} />}` — MANTER condição atual (fora do escopo D11)

Lógica nova (substitui o bloco de saldo/custo e o botão):
```tsx
const { costs, status: costStatus } = useOperationCosts();
const campaignCost = costs?.campaign_generation;
const costUnavailable = costStatus !== "loaded";               // loading OU unavailable → não sabe o custo
const costDisabled = costStatus === "loaded" && campaignCost !== undefined && !campaignCost.enabled;
const insufficientBalance = balance !== undefined && balance !== null && campaignCost !== undefined && balance < campaignCost.costCredits;
const submitDisabled = isSubmitting || costUnavailable || costDisabled || insufficientBalance || balance === null;
```
- Exibição (linha 505): `costStatus === "loaded" && campaignCost ? (campaignCost.enabled ? \`Custo: ${campaignCost.costCredits}\` : "Operação desativada") : costStatus === "unavailable" ? "Serviço indisponível no momento" : "Custo: …"`
- `title` (botão) por prioridade: costUnavailable (loading → "Verificando custo da geração..."; unavailable → "Serviço indisponível no momento. Tente novamente em alguns instantes.") → costDisabled → "Operação desativada no momento." → insufficientBalance → "Você precisa de créditos para gerar uma campanha" (existente) → balance null → mensagem existente
- Custo de `campaign_generation` (D11 — NÃO visual_signature aqui)

**drift-critical-modal.tsx** (F38-UI-03 — linhas 118-119, bloco `/* ── Sem crédito ── */`):
- Hoje: `Você não tem créditos suficientes para gerar uma nova assinatura` + `visual. Cada geração consome 1 crédito.`
- Novo: `const { costs, status } = useOperationCosts();` + `const vsCost = costs?.visual_signature_generation;`
- Render: primeira frase intacta; segunda frase SOMENTE quando `status === "loaded" && vsCost` → ` Cada geração consome ${formatCredits(vsCost.costCredits)}.` (plural correto); indisponível/loading → frase omitida (sem custo presumido)

**visual-signature-approval-modal.tsx** (F38-UI-04 — case `insufficient_credits`, linhas 713-716):
- Hoje: `Créditos insuficientes para gerar assinatura visual.` (713) + `Cada geração de assinatura visual consome 1 crédito.` (716)
- Novo: segunda frase SOMENTE quando `status === "loaded" && vsCost` → `Cada geração de assinatura visual consome ${formatCredits(vsCost.costCredits)}.`; indisponível → omitir (D11)

**Imports a adicionar nos 3 componentes:** `import { useOperationCosts } from "@/hooks/use-operation-costs";` e (modais) `import { formatCredits } from "@/lib/credit/format";`. Todos são componentes client (`"use client"`).

**Mock de teste (4 arquivos — mesmo shape):**
```typescript
const mockUseOperationCosts = vi.fn();
vi.mock("@/hooks/use-operation-costs", () => ({
  useOperationCosts: (...args: unknown[]) => mockUseOperationCosts(...args),
}));
// beforeEach: mockUseOperationCosts.mockReturnValue({
//   costs: { campaign_generation: { costCredits: 1, enabled: true },
//            visual_signature_generation: { costCredits: 1, enabled: true } },
//   status: "loaded", refetch: vi.fn() });
```
Arquivos (já verificados por grep): `visual-signature-approval-modal.test.tsx` (linha 241 assert literal), `drift-critical-modal.test.ts` (.ts — adicionar mock), `store-identity-form.drift-tabs.test.ts` (renderiza DriftCriticalModal real — adicionar mock), `campaign-flow-credits.test.tsx` (3 renders de CampaignInputForm: balance 5/0/null — linhas 82/90/97). A linha 241 do approval-modal continuará válida com custo 1 mockado.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: campaign-input-form — Custo dinâmico + disable por saldo/custo</name>
  <files>src/components/flow/campaign-input-form.tsx</files>
  <read_first>
    - src/components/flow/campaign-input-form.tsx (props — linhas 20-25; bloco saldo — linhas 491-511; botão submit — linhas 521-541; imports — topo do arquivo)
    - src/hooks/use-operation-costs.ts (criado na 38-06 — contrato costs/status)
  </read_first>
  <action>
    Modificar `src/components/flow/campaign-input-form.tsx` conforme o contrato do `<interfaces>`:

    1. Adicionar `import { useOperationCosts } from "@/hooks/use-operation-costs";`
    2. No corpo do componente (após os estados existentes): `const { costs, status: costStatus } = useOperationCosts();` + derivar `campaignCost`, `costUnavailable`, `costDisabled`, `insufficientBalance`, `submitDisabled` (lógica exata do contrato — NOTA: `balance < campaignCost.costCredits` substitui o antigo `balance === 0`; `balance === null` continua desabilitando; `balance === undefined` não desabilita — comportamento preservado).
    3. Linha 505 (`<span>Custo: 1</span>`): substituir pela expressão condicional do contrato (loaded+enabled → `Custo: {costCredits}`; loaded+desligada → "Operação desativada"; unavailable → "Serviço indisponível no momento"; loading → "Custo: …").
    4. Botão submit (linha 523): `disabled={submitDisabled}`; `title` com a cadeia de mensagens por prioridade do contrato (costUnavailable → loading? "Verificando custo da geração..." : "Serviço indisponível no momento. Tente novamente em alguns instantes."; costDisabled → "Operação desativada no momento."; insufficientBalance → mensagem existente; balance null → mensagem existente).
    5. MANTER: bloco `balance !== undefined` (linha 492), CTA `balance === 0` (linha 513), aviso de autorização (517-519), botão "Tentar novamente" para balance null (543-553). NÃO alterar o eixo USD de IA nem `CreditCta`.
  </action>
  <verify>
    <automated>npm run typecheck; `grep -c "useOperationCosts" src/components/flow/campaign-input-form.tsx` >= 1; PowerShell: (Select-String -Path src/components/flow/campaign-input-form.tsx -Pattern "Custo: 1").Count -eq 0; npx vitest run src/app/(app)/campanhas/nova/__tests__/campaign-flow-credits.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `Custo: {cost}` dinâmico (campaign_generation); literal "Custo: 1" removido
    - Submit desabilitado quando `balance < costCredits`, `enabled=false`, custo indisponível (loading/unavailable) ou balance null
    - Mensagens de title por condição (D11 — "Tente novamente em alguns instantes" no 503)
    - CTA/CreditCta/blocos existentes intactos; teste campaign-flow-credits verde com mock do hook
    - F38-UI-01
  </acceptance_criteria>
  <done>campaign-input-form com custo dinâmico e disable por saldo/custo — F38-UI-01</done>
</task>

<task type="auto">
  <name>Task 2: Modais — custo dinâmico de visual_signature_generation</name>
  <files>src/components/flow/drift-critical-modal.tsx, src/components/flow/visual-signature-approval-modal.tsx</files>
  <read_first>
    - src/components/flow/drift-critical-modal.tsx (bloco sem crédito — linhas 113-119; imports; confirmar "use client")
    - src/components/flow/visual-signature-approval-modal.tsx (case insufficient_credits — linhas 708-726; imports; "use client")
    - src/lib/credit/format.ts (formatCredits — criado na 38-06)
  </read_first>
  <action>
    1. `drift-critical-modal.tsx` (F38-UI-03):
       - Imports: `useOperationCosts` (@/hooks/use-operation-costs) + `formatCredits` (@/lib/credit/format)
       - No componente: `const { costs, status } = useOperationCosts();` + `const vsCost = costs?.visual_signature_generation;`
       - Linhas 118-119: manter "Você não tem créditos suficientes para gerar uma nova assinatura visual." e renderizar a segunda frase SOMENTE quando `status === "loaded" && vsCost`: ` Cada geração consome ${formatCredits(vsCost.costCredits)}.` (JSX condicional — indisponível/loading omite a frase; nenhum "1 crédito" presumido)
    2. `visual-signature-approval-modal.tsx` (F38-UI-04):
       - Imports: `useOperationCosts` + `formatCredits`
       - No componente (onde `phase === "insufficient_credits"`): `const { costs, status } = useOperationCosts();` + `const vsCost = costs?.visual_signature_generation;`
       - Linha 716: substituir a frase fixa por renderização condicional — `{status === "loaded" && vsCost ? \`Cada geração de assinatura visual consome ${formatCredits(vsCost.costCredits)}.\` : null}` (indisponível → frase omitida)
    3. NÃO alterar: fases do modal, CTAs ("Ver meus créditos"), botão "Tentar novamente", textos que não envolvem custo.
  </action>
  <verify>
    <automated>npm run typecheck; `grep -c "formatCredits" src/components/flow/drift-critical-modal.tsx` >= 1; `grep -c "formatCredits" src/components/flow/visual-signature-approval-modal.tsx` >= 1; PowerShell: (Select-String -Path src/components/flow/drift-critical-modal.tsx,src/components/flow/visual-signature-approval-modal.tsx -Pattern "consome 1 crédito").Count -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - DriftCriticalModal: frase de custo dinâmica (visual_signature_generation) com plural correto; omitida quando indisponível — F38-UI-03
    - ApprovalModal: "Cada geração de assinatura visual consome {N} crédito(s)." dinâmico; omitido quando indisponível — F38-UI-04
    - Literal "consome 1 crédito" removido dos dois modais
    - CTAs e fases intactos
  </acceptance_criteria>
  <done>Modais com custo dinâmico de visual_signature_generation — F38-UI-03/04</done>
</task>

<task type="auto">
  <name>Task 3: Atualizar testes que renderizam os 3 componentes (mock do hook)</name>
  <files>src/components/flow/__tests__/visual-signature-approval-modal.test.tsx, src/components/flow/__tests__/drift-critical-modal.test.ts, src/components/flow/__tests__/store-identity-form.drift-tabs.test.ts, src/app/(app)/campanhas/nova/__tests__/campaign-flow-credits.test.tsx</files>
  <read_first>
    - src/components/flow/__tests__/visual-signature-approval-modal.test.tsx (setup — linhas 1-25; assert literal — linha 241)
    - src/components/flow/__tests__/drift-critical-modal.test.ts (setup mocks — topo do arquivo)
    - src/components/flow/__tests__/store-identity-form.drift-tabs.test.ts (topo + onde renderiza DriftCriticalModal — ~linhas 460-480)
    - src/app/(app)/campanhas/nova/__tests__/campaign-flow-credits.test.tsx (setup + 3 renders — linhas 1-100)
  </read_first>
  <action>
    Nos 4 arquivos de teste (verificados por grep que renderizam os componentes):

    1. Adicionar o mock de `@/hooks/use-operation-costs` (shape do `<interfaces>` — `mockUseOperationCosts` via `vi.hoisted` se o arquivo usar hoisted) + `beforeEach` retornando status "loaded" com custo 1 nas duas chaves.
    2. `visual-signature-approval-modal.test.tsx`: a asserção da linha 241 (`'Cada geração de assinatura visual consome 1 crédito.'`) permanece válida com o mock (custo 1). ADICIONAR caso: `mockUseOperationCosts.mockReturnValue({ costs: null, status: "unavailable", refetch: vi.fn() })` → após 402, a frase de custo NÃO aparece (queryByText da frase → null) — sem custo presumido (F38-UI-04).
    3. `campaign-flow-credits.test.tsx`: com o mock, os 3 casos (balance 5/0/null — linhas 82/90/97) preservam semântica — validar que as asserções existentes continuam passando (submit habilitado com saldo, desabilitado com saldo 0 e null).
    4. `drift-critical-modal.test.ts` e `store-identity-form.drift-tabs.test.ts`: adicionar o mock no topo (os testes existentes de botões/fases não mudam); se qualquer asserção casar com a frase de custo literal, ela permanece válida com custo 1 mockado.
    5. Rodar a suíte completa dos 4 arquivos — 0 falhas.
  </action>
  <verify>
    <automated>npx vitest run src/components/flow/__tests__/visual-signature-approval-modal.test.tsx src/components/flow/__tests__/drift-critical-modal.test.ts src/components/flow/__tests__/store-identity-form.drift-tabs.test.ts "src/app/(app)/campanhas/nova/__tests__/campaign-flow-credits.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - Mock do hook nos 4 arquivos (status loaded, custo 1)
    - Novo caso unavailable no approval-modal: frase de custo omitida
    - Asserções existentes (botões, CTAs, balance) preservadas e verdes
    - Suite dos 4 arquivos: 0 falhas
  </acceptance_criteria>
  <done>Testes dos componentes atualizados — suite verde com custo dinâmico</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client (browser) → hook useOperationCosts → GET /api/operation-costs | Dados de custo para exibição; componente decide desabilitar/omitir com base no status |
| UI → usuário lojista | Texto de custo exibido pode influenciar decisão de gerar campanha |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-27 | Spoofing | Exibição de custo incorreta (presumida) | mitigate | UI NUNCA presume custo: loading/unavailable → "Custo: …"/"Serviço indisponível" (form) ou frase omitida (modais) — F38-UI-01/03/04 |
| T-38-28 | Spoofing | Submit com saldo < custo | mitigate | Submit desabilitado quando `balance < costCredits` (dinâmico); validação real continua na rota (402 na 38-03/04) — F38-UI-01 |
| T-38-29 | Denial of Service | Operação desligada usada mesmo assim | mitigate | `enabled=false` → form desabilitado com "Operação desativada"; rota também responde 503 operation_disabled (38-03/04) — F38-UI-01 |
| T-38-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado — gate de legitimidade não se aplica |
</threat_model>

<verification>
- `npm run typecheck` e `npm run lint` limpos
- `npx vitest run` nos 4 arquivos de teste atualizados — 0 falhas
- Nenhum literal "consome 1 crédito" ou "Custo: 1" restante nos 3 componentes (grep)
- Suite completa da fase permanece verde (spot check dos arquivos tocados)
</verification>

<success_criteria>
- campaign-input-form: `Custo: {cost}` dinâmico + disable por saldo/custo/indisponibilidade (F38-UI-01)
- DriftCriticalModal e ApprovalModal: custo dinâmico de visual_signature_generation com plural correto, frase omitida quando indisponível (F38-UI-03/04)
- Nenhum custo presumido em qualquer estado de UI
- Testes dos 4 arquivos verdes, incluindo caso unavailable
</success_criteria>

<output>
Create `.planning/phases/38-credit-operation-costs/38-07-SUMMARY.md` when done
</output>

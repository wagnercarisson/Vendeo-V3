---
phase: 38-credit-operation-costs
plan: 08
type: execute
wave: 5
depends_on: ["38-01", "38-03", "38-04", "38-05", "38-07"]
files_modified:
  - "scripts/verify/38-operation-cost-verification.mjs"
  - "vitest.integration.config.ts"
  - "src/lib/credit/__tests__/operation-cost-service.integration.ts"
  - ".planning/phases/38-credit-operation-costs/38-UAT.md"
autonomous: false
requirements:
  - F38-DB-04
  - F38-SERVICE-02
  - F38-SERVICE-03
  - F38-CONFIG-01
  - F38-CONFIG-02
must_haves:
  truths:
    - "I1: RPC `admin_update_operation_cost` real (banco remoto) atualiza custo E grava linha na audit (action update_cost, old/new capturados, reason obrigatório)"
    - "I2: mesmo `operation_id` repetido → mesmo audit_id retornado, `idempotent: true`, sem linha nova na audit (no-op)"
    - "I3: `cost_credits = 0` → erro `operation_cost_invalid` (RPC rejeita)"
    - "I4: cliente sem service_role (anon key) NÃO lê `credit_operation_costs` — erro de permissão OU 0 linhas (RLS/REVOKE)"
    - "I5: UPDATE/DELETE em `credit_operation_cost_audit` → erro (trigger imutável bloqueia)"
    - "I6: `OperationCostService.getCost()` real contra banco remoto retorna `source: 'table'` com seeds (`campaign_generation=1`, `visual_signature_generation=1`, ambas enabled)"
    - "Build gate: `npx vitest run`, `npm run typecheck`, `npm run lint`, `npm run build` — zero erros"
    - "UAT: admin muda custo 1→2 → form mostra Custo: 2; desliga operação → 503; linha inexistente → fail-open; banco derrubado → fail-closed"
  artifacts:
    - path: "scripts/verify/38-operation-cost-verification.mjs"
      provides: "I1–I6a: verificação SQL/integrada contra banco real (RPC + RLS + imutabilidade + seeds)"
      exports: []
    - path: "vitest.integration.config.ts"
      provides: "Config vitest que inclui APENAS o teste de integração real (fora da suíte padrão do build gate)"
      exports: []
    - path: "src/lib/credit/__tests__/operation-cost-service.integration.ts"
      provides: "I6b: getCost real (service sem mock) contra banco remoto → source 'table'"
      exports: []
    - path: ".planning/phases/38-credit-operation-costs/38-UAT.md"
      provides: "Rastreio UAT (formato 36-UAT.md) com os 4 cenários do CONTEXT specifics"
      contains: "Custo: 2"
  key_links:
    - from: "scripts/verify/38-operation-cost-verification.mjs"
      to: "RPC admin_update_operation_cost (banco remoto gvbzwihwgzujwsviufgy)"
      via: "supabase-js service role (NEXT_PUBLIC_SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY de .env.local)"
      pattern: "admin_update_operation_cost"
    - from: "src/lib/credit/__tests__/operation-cost-service.integration.ts"
      to: "OperationCostService.getCost (service real, sem mock de supabase)"
      via: "supabaseAdmin real lê credit_operation_costs"
      pattern: "getCost"
    - from: "scripts/verify/38-operation-cost-verification.mjs"
      to: "credit_operation_cost_audit (imutabilidade I5 + idempotência I2)"
      via: "SELECT/UPDATE via service role; UPDATE deve falhar (trigger)"
      pattern: "credit_operation_cost_audit"
---

<objective>
Testes e verificação final da F38: verificação SQL/integrada I1–I6 contra o banco remoto (RPC real atualiza + audit; operation_id repetido → no-op; cost_credits=0 rejeitado; cliente sem service_role sem acesso; audit imutável; getCost real → `source: 'table'` com seeds), build gate completo (`vitest`, `typecheck`, `lint`, `build`) e rastreio UAT com checkpoint humano.

Purpose: provar que a fase entrega a fonte única de custo funcionando contra o banco REAL (não mocks) — as verificações I1–I6 do CONTEXT specifics (linha 289) — e que nenhuma regressão entrou (F38-DB-04 exige I1–I6).
Output: script de verificação `.mjs` (precedente 24-02), teste de integração getCost real, config vitest isolada, `38-UAT.md` preenchido, evidências da verificação registradas.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-credit-operation-costs/38-CONTEXT.md
@.planning/phases/38-credit-operation-costs/38-PATTERNS.md

<interfaces>
Contratos que o executor precisa (extraídos de 38-CONTEXT.md specifics linha 289 + 38-PATTERNS.md):

Verificações obrigatórias I1–I6 (CONTEXT specifics, linha 289):
- I1: RPC real atualiza + audit (RPC `admin_update_operation_cost` escreve linha na `credit_operation_cost_audit`)
- I2: operation_id repetido → no-op (mesmo audit_id, `idempotent: true`, sem linha nova)
- I3: cost_credits=0 rejeitado (erro `operation_cost_invalid`)
- I4: authenticated/anon sem acesso (RLS + REVOKE — cliente sem service_role não lê)
- I5: audit imutável (trigger bloqueia UPDATE/DELETE)
- I6: getCost real → `source: 'table'` com seeds (`campaign_generation=1`, `visual_signature_generation=1`, ambas enabled=true)

RPC `admin_update_operation_cost` (criada na 38-01 — 6 parâmetros, D8):
```sql
admin_update_operation_cost(
  p_actor_id UUID, p_operation_key TEXT,
  p_cost_credits INTEGER DEFAULT NULL, p_enabled BOOLEAN DEFAULT NULL,
  p_reason TEXT DEFAULT NULL, p_operation_id UUID DEFAULT NULL
) RETURNS JSONB  -- { operation_key, cost_credits, enabled, audit_id, updated_at, idempotent }
```

Service `OperationCostService` (criado na 38-02):
```typescript
// src/lib/credit/operation-cost-service.ts (server-only)
// getCost(key) → { operationKey, costCredits, enabled, source: "table" | "fallback" }
// linha existente → source: "table"; linha inexistente → fail-open source:"fallback";
// erro real de leitura → lança OperationCostUnavailableError (fail-closed)
export const DEFAULT_OPERATION_COSTS = {
  campaign_generation:         { costCredits: 1, enabled: true },
  visual_signature_generation: { costCredits: 1, enabled: true },
};
```

Precedente de verificação SQL/integrada (F24): `scripts/verify/24-credit-verification.mjs` (128 linhas):
- Lê `.env.local` via fs (getEnv por regex `KEY=(.+)`), cria `createClient(NEXT_PUBLIC_SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY)`, helper `assert(name, ok, detail)` com contador pass/fail, saída `process.exit(fail > 0 ? 1 : 0)`.
- Conexão real: projeto remoto linkado (`supabase/.temp/project-ref` = `gvbzwihwgzujwsviufgy`; pooler `postgresql://postgres.gvbzwihwgzujwsviufgy@aws-1-sa-east-1.pooler.supabase.com`). O script REST via supabase-js funciona contra o remoto (não requer psql).

Padrão de teste de integração real (I6b — env carregado no arquivo, antes do import dinâmico):
```typescript
// src/lib/credit/__tests__/operation-cost-service.integration.ts
// SEM .test. no nome — a suíte padrão (build gate) NÃO coleta este arquivo;
// o vitest.integration.config.ts o inclui explicitamente.
import { readFileSync } from "fs";
import { resolve } from "path";
// ler .env.local e setar process.env ANTES do import dinâmico do service
// (padrão de parsing: regex /^([A-Z_]+)=(.*)$/ sobre cada linha, trim, remover aspas)
// depois: const { OperationCostService } = await import("@/lib/credit/operation-cost-service");
```

UAT tracking (precedente F36): `.planning/phases/36-onboarding-navegacao-por-abas/36-UAT.md` — frontmatter (status/phase/source/started/updated), seção `## Current Test` (number/name/expected/awaiting), seção `## Tests` com `### N. <nome>` + `expected:` + `result:` (pass/pending/fail). Cenários F38 (CONTEXT specifics linha 291): admin muda custo 1→2 → form mostra "Custo: 2"; desliga operação → 503; fail-open linha inexistente; fail-closed banco derrubado.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificação SQL/integrada I1–I6a (script .mjs contra banco real)</name>
  <files>scripts/verify/38-operation-cost-verification.mjs</files>
  <read_first>
    - scripts/verify/24-credit-verification.mjs (128 linhas — estrutura exata: env parser, assert helper, exit code)
    - supabase/migrations/20260807000001_f38_create_credit_operation_costs.sql (criado na 38-01 — nomes de colunas da audit: new_cost_credits, new_enabled, old_cost_credits, old_enabled, action, actor_id, reason, operation_id; RPC + trigger imutável)
    - .planning/phases/38-credit-operation-costs/38-CONTEXT.md (D4/D5/D8 — linhas 57-82)
  </read_first>
  <action>
    Criar `scripts/verify/38-operation-cost-verification.mjs` no padrão do `24-credit-verification.mjs` (env parser de .env.local via fs, `createClient` com service role, helper `assert` com contador, `process.exit`):

    1. Cliente A (service role): `createClient(NEXT_PUBLIC_SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY)` — para I1/I2/I3/I5/I6a. Cliente B (anon): `createClient(NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY)` — para I4.
     2. Actor REAL de verificação: resolver o primeiro usuário auth existente via service role ANTES de qualquer chamada RPC — `const { data: { users } } = await supabaseAdmin.auth.admin.listUsers();` e usar `p_actor_id = users[0].id` em TODAS as chamadas RPC (I1/I2/I3 — padrão recomendado pelo Supabase para acessar auth.users via service role, sem depender de PostgREST direto na tabela auth.users). Se `users` vier vazio → assert fail com mensagem clara ("nenhum usuário em auth.users para usar como actor de verificação") e `process.exit(1)`. ATENÇÃO: `credit_operation_costs.updated_by` (D2) e `credit_operation_cost_audit.actor_id` (D8) TÊM FK para `auth.users(id)` — criadas na 38-01 — então o actor PRECISA existir; um UUID sintético causaria violação de FK e quebraria I1–I3. `p_reason = "38-08-verification"`. Alternativa: se o repo já tiver um RPC SECURITY DEFINER específico para listar emails de auth.users (padrão `20260718000001_create_admin_tables.sql:266`), usar esse RPC ao invés de `auth.admin.listUsers()` para consistência com o padrão admin existente.
    3. **I1** (RPC real atualiza + audit): gerar `operationId1 = crypto.randomUUID()`. Chamar `supabase.rpc("admin_update_operation_cost", { p_actor_id, p_operation_key: "campaign_generation", p_cost_credits: 2, p_reason, p_operation_id: operationId1 })`. Assert: sem erro, retorno tem `audit_id` UUID e `idempotent: false`. Em seguida SELECT em `credit_operation_cost_audit` (`.eq("operation_id", operationId1)`) → assert 1 linha com `action: "update_cost"`, `new_cost_credits: 2`, `old_cost_credits: 1`, `actor_id` correto. **REVERTER para o estado seed:** segunda chamada RPC `p_cost_credits: 1` com novo `operationId1b` → assert `audit_id` novo (append-only), deixando `campaign_generation` em 1 (estado inicial para o UAT).
    4. **I2** (operation_id repetido → no-op): chamar a RPC DE NOVO com o MESMO `operationId1` (p_cost_credits: 3) → assert retorno `idempotent: true` e `audit_id` IGUAL ao da primeira chamada. SELECT count em `credit_operation_cost_audit` `.eq("operation_id", operationId1)` → assert == 1 (sem linha nova).
    5. **I3** (cost_credits=0 rejeitado): `supabase.rpc("admin_update_operation_cost", { p_actor_id, p_operation_key: "campaign_generation", p_cost_credits: 0, p_reason, p_operation_id: crypto.randomUUID() })` → assert `error` presente e `error.message` inclui `operation_cost_invalid`. SELECT `cost_credits` da tabela → assert continua 1 (nada alterado).
    6. **I4** (cliente sem service_role não lê): cliente B (anon key) `.from("credit_operation_costs").select("*").limit(5)` → assert: `error` presente (PGRST 42501 permission denied, pois REVOKE removeu privilégios de anon) OU `data` vazio/0 linhas (RLS filtra). NUNCA linhas retornadas. Registrar qual dos dois comportamentos ocorreu no output.
    7. **I5** (audit imutável): pegar um `id` real da audit (SELECT limit 1 via service role), então `.from("credit_operation_cost_audit").update({ reason: "hacked" }).eq("id", <id>)` e `.delete().eq("id", <id>)` via service role → assert AMBOS retornam erro (trigger `trg_credit_operation_cost_audit_immutable` RAISE EXCEPTION). SELECT do mesmo id → assert linha ainda existe inalterada.
    8. **I6a** (seeds + fonte table): SELECT `operation_key, cost_credits, enabled` de `credit_operation_costs` via service role → assert exatamente as 2 linhas: `campaign_generation` → `{ cost_credits: 1, enabled: true }` e `visual_signature_generation` → `{ cost_credits: 1, enabled: true }` (estado seed, pós-revert do I1). Registrar que este é o dado que getCost lê para retornar `source: "table"` (I6b valida o service real).
    9. Imprimir `Results: N passed / N failed / N total` e `process.exit(fail > 0 ? 1 : 0)` (padrão 24-02). Comentar em PT-BR cada bloco com o código I1–I6a.
  </action>
  <verify>
    <automated>node scripts/verify/38-operation-cost-verification.mjs</automated>
  </verify>
  <acceptance_criteria>
    - I1: RPC real atualiza para 2, audit tem 1 linha (action update_cost, old 1 → new 2); revert para 1 com nova linha de audit
    - I2: operationId1 repetido → idempotent:true, mesmo audit_id, count == 1 na audit
    - I3: cost_credits=0 → erro operation_cost_invalid; cost_credits continua 1
    - I4: anon key não retorna linhas (erro OU vazio) — comportamento registrado no output
    - I5: UPDATE e DELETE na audit → erro (trigger imutável); linha intacta
    - I6a: seeds exatos (1/1, ambas enabled true) presentes
    - `node scripts/verify/38-operation-cost-verification.mjs` sai com exit 0 e 0 falhas
  </acceptance_criteria>
  <done>I1–I6a verificados contra banco real — script verde (exit 0), estado do banco retornado ao seed</done>
</task>

<task type="auto">
  <name>Task 2: I6b getCost real (service sem mock) — teste de integração isolado</name>
  <files>vitest.integration.config.ts, src/lib/credit/__tests__/operation-cost-service.integration.ts</files>
  <read_first>
    - vitest.config.ts (31 linhas — base a estender: alias @ → ./src, server-only resolvido, globals:true, environment:node)
    - src/lib/credit/operation-cost-service.ts (getCost + OperationCostUnavailableError + DEFAULT_OPERATION_COSTS — criado na 38-02)
    - src/lib/credit/types.ts (OperationCostResolution — D7)
    - scripts/verify/24-credit-verification.mjs (parsing de .env.local via fs — linhas 7-13)
  </read_first>
  <action>
    Criar `vitest.integration.config.ts`:
    1. `import { defineConfig } from "vitest/config"` + importar o config base (`./vitest.config`) e espalhar; sobrescrever `test.include: ["src/lib/credit/__tests__/operation-cost-service.integration.ts"]` e `test.testTimeout: 30000`. Preservar todos os aliases do base (incluindo o resolve de `server-only`). Objetivo: `npx vitest run --config vitest.integration.config.ts` roda APENAS este arquivo; a suíte padrão (`npx vitest run`) NÃO o coleta (nome sem `.test.`).

    Criar `src/lib/credit/__tests__/operation-cost-service.integration.ts` (NOME SEM `.test.` — fora do include padrão do vitest):
    1. No topo, ler `.env.local` via `readFileSync(resolve(process.cwd(), ".env.local"))` e setar `process.env.NEXT_PUBLIC_SUPABASE_URL` e `process.env.SUPABASE_SERVICE_ROLE_KEY` (regex `/^([A-Z_]+)=(.*)$/`, trim, remover aspas — padrão do script 24-02). Isso DEVE acontecer antes do import do módulo que lê env.
    2. Usar `import()` dinâmico para `@/lib/credit/operation-cost-service` (depois do env setado) — `server-only` já é resolvido como nojo pelo config base (alias), então o import funciona em node.
    3. Testes (`describe`/`it` — globals true):
       - getCost("campaign_generation") → `{ operationKey: "campaign_generation", costCredits: 1, enabled: true, source: "table" }` (I6 — service real, banco real)
       - getCost("visual_signature_generation") → `{ operationKey: "visual_signature_generation", costCredits: 1, enabled: true, source: "table" }`
       - getCost de chave inexistente (ex.: "campaign_approval") → `source: "fallback"` e costCredits/enabled do DEFAULT_OPERATION_COSTS... NÃO — chave inexistente fora do enum lança; usar `source: "fallback"` apenas se a chave existir no enum TS mas não na tabela. Para a F38 as 2 chaves do enum têm seed, então o caso fallback NÃO é testável aqui com as chaves atuais: REMOVER este caso se o enum só tiver as 2 chaves (registrar no comentário que fail-open é coberto por teste unitário da 38-02).
    4. Após os testes, restaurar `process.env` original em `afterAll` (salvar referências antes de setar) para não vazar para outros testes da suíte integrada.
  </action>
  <verify>
    <automated>npx vitest run --config vitest.integration.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `vitest.integration.config.ts` inclui apenas `operation-cost-service.integration.ts`; aliases do base preservados
    - Teste real: getCost("campaign_generation") e getCost("visual_signature_generation") → `source: "table"`, costCredits 1, enabled true (service sem mock, banco remoto)
    - `npx vitest run --config vitest.integration.config.ts` verde (2 casos)
    - `npx vitest run` (suíte padrão) NÃO coleta o arquivo (nome sem `.test.`)
  </acceptance_criteria>
  <done>I6 provado no service real contra banco real: source 'table' com seeds</done>
</task>

<task type="auto">
  <name>Task 3: Build gate completo + rastreio UAT (38-UAT.md)</name>
  <files>.planning/phases/38-credit-operation-costs/38-UAT.md</files>
  <read_first>
    - .planning/phases/36-onboarding-navegacao-por-abas/36-UAT.md (formato: frontmatter + Current Test + seção Tests com expected/result)
    - .planning/phases/38-credit-operation-costs/38-CONTEXT.md (specs linha 289-291: I1–I6 + Entrega verificável)
    - .planning/ROADMAP.md (Phase 38 — success criteria 1-9)
  </read_first>
  <action>
    1. Rodar o build gate completo: `npx vitest run`, `npm run typecheck`, `npm run lint`, `npm run build`. Se algum falhar por regressão real, corrigir APENAS o necessário (nunca enfraquecer asserção de segurança). Registrar o resultado de cada um no SUMMARY.
    2. Criar `.planning/phases/38-credit-operation-costs/38-UAT.md` no formato do 36-UAT.md com:
       - Frontmatter: `status: in_progress`, `phase: 38-credit-operation-costs`, `source:` listando os SUMMARYs 38-01..38-08, `started:/updated:` com data corrente (2026-08-07).
       - `## Current Test` apontando o primeiro cenário pendente.
       - `## Tests` com os 4 cenários obrigatórios do CONTEXT specifics linha 291 (expected descrito, `result: pending` até o checkpoint humano):
         1. **Admin muda custo 1→2** — em `/admin/operation-costs`, mudar `campaign_generation` para 2 (motivo obrigatório) → `campaign-input-form` mostra "Custo: 2" e `balance-card` mostra "Cada geração consome 2 créditos."
         2. **Desliga operação → 503** — toggle `enabled=false` em `campaign_generation` → gerar campanha → 503 `operation_disabled`; reativar depois.
         3. **Fail-open linha inexistente** — remover/inserir chave sem linha na tabela via SQL (executor) → getCost retorna `source: "fallback"` com default (1, enabled) e UI continua funcional.
         4. **Fail-closed banco derrubado** — derrubar o serviço de banco (ou simular erro de rede no service) → rota de geração responde 503 `operation_cost_unavailable` SEM gerar nem reservar; UI mostra "Tente novamente em alguns instantes".
       - Registros de resultado dos cenários automatizáveis (fail-open/fail-closed) já verificados na execução, com `note:` referenciando a evidência (script I1–I6a / teste de integração / testes unitários da 38-02).
  </action>
  <verify>
    <automated>npx vitest run; npm run typecheck; npm run lint; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` — 0 falhas; `npm run typecheck` e `npm run lint` limpos; `npm run build` sucesso
    - `38-UAT.md` criado com 4 cenários (expected + result) no formato 36-UAT.md
    - Cenários automatizáveis (fail-open/fail-closed) com evidência registrada
  </acceptance_criteria>
  <done>Build gate verde + 38-UAT.md criado com os 4 cenários prontos para validação manual</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Validação manual UAT (checkpoint humano)</name>
  <files>Nenhum arquivo modificado — validação manual do fluxo (dev server ou preview)</files>
  <action>
    Após as Tasks 1-3 verdes (I1–I6a script exit 0, I6b integração verde, build gate completo), submeter ao usuário a validação manual dos cenários UAT conforme o passo a passo de <how-to-verify>. Toda automação já foi executada; este checkpoint cobre apenas o que exige olho humano (admin UI, form, modais).
  </action>
  <verify>Pausa para o usuário executar os cenários de <how-to-verify> e responder via <resume-signal> — nenhum comando automatizado adicional</verify>
  <done>Aprovação do usuário registrada (resume-signal) OU lista de problemas reprodutíveis encaminhada para correção; 38-UAT.md atualizado com `status: complete`</done>
  <what-built>Toda a F38: tabela `credit_operation_costs` + audit + RPC admin, OperationCostService, rotas de geração com custo dinâmico + guards 503, GET/PUT admin, página /admin/operation-costs, GET /api/operation-costs + hook, UI dinâmica (form/balance-card/modais)</what-built>
  <how-to-verify>
    Abrir a aplicação (dev server ou preview) e verificar:
    1. **Admin muda custo 1→2 (cenário 1):** em `/admin/operation-costs`, mudar `campaign_generation` de 1 para 2 com motivo → confirmar o feedback com audit_id. Abrir `/campanhas/nova` → o form mostra "Custo: 2" e o balance-card mostra "Cada geração consome 2 créditos." Voltar o custo para 1 com motivo.
    2. **Desliga operação → 503 (cenário 2):** em `/admin/operation-costs`, toggle `enabled=false` em `campaign_generation` → em `/campanhas/nova` o form fica desabilitado com "Operação desativada"; tentar gerar via API → 503 `operation_disabled`. Reativar (enabled=true, motivo).
    3. **Fail-open linha inexistente (cenário 3):** executor já removeu uma chave da tabela via SQL → gerar campanha → funciona com default (1 crédito). (Se o executor não removeu, registrar como pendente de execução junto deste checkpoint.)
    4. **Fail-closed banco derrubado (cenário 4):** com o banco indisponível (parar serviço/simular), tentar gerar → 503 `operation_cost_unavailable`, NENHUMA reserva nem geração; a UI mostra "Tente novamente em alguns instantes".
  </how-to-verify>
  <resume-signal>Digite "approved" para concluir a F38, ou descreva os problemas encontrados (com repro)</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script de verificação → RPC admin + tabelas | Service role (REST) executa RPC real e lê/grava tabelas de custo — privilégio elevado em banco remoto |
| teste de integração → getCost real | Service real (supabaseAdmin) lê credit_operation_costs no banco remoto |
| cliente anon → credit_operation_costs | I4 prova que cliente SEM service_role não lê (RLS + REVOKE) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-30 | Spoofing | Script usa service role (privilégio elevado) | mitigate | Script executa APENAS verificação com actor REAL (primeiro usuário existente em auth.users, resolvido via service role — respeita as FKs de updated_by/actor_id definidas em D2/D8); I1 reverte o custo ao seed (1) ao final; nada além de `credit_operation_costs`/`credit_operation_cost_audit` é tocado — F38-DB-04 |
| T-38-31 | Tampering | I1 altera custo real do banco remoto | mitigate | Revert imediato para 1 com nova linha de audit (append-only); estado final == estado seed; I3 asserção de não-mutação — F38-DB-03/04 |
| T-38-32 | Information Disclosure | I4 prova que anon não lê custos | mitigate | Assert de erro/0 linhas para cliente anon; NUNCA linhas retornadas — F38-DB-01/02 |
| T-38-33 | Tampering | Teste de integração I6b carrega env real | mitigate | Env carregado apenas no arquivo de integração (nome sem `.test.`, config dedicada); `afterAll` restaura process.env — fora da suíte do build gate |
| T-38-34 | Denial of Service | Build gate falha por regressão não detectada | mitigate | Suíte completa + typecheck + lint + build no gate; correção mínima sem enfraquecer asserções — entrega verificável (specs) |
| T-38-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo — gate de legitimidade não se aplica |
</threat_model>

<verification>
- `node scripts/verify/38-operation-cost-verification.mjs` — I1–I6a, exit 0, 0 falhas
- `npx vitest run --config vitest.integration.config.ts` — I6b (getCost real → source 'table'), 2 casos verdes
- `npx vitest run` (suíte padrão), `npm run typecheck`, `npm run lint`, `npm run build` — zero erros
- `38-UAT.md` criado; checkpoint humano aprovado (ou problemas reprodutíveis registrados)
</verification>

<success_criteria>
- I1–I6 verificados contra o banco REAL (não mocks): RPC + audit, idempotência, CHECK cost>0, RLS/REVOKE, trigger imutável, seeds + getCost source 'table'
- Estado do banco retornado ao seed ao fim da verificação (cost 1/1, ambas enabled)
- Build gate completo verde (vitest/typecheck/lint/build) — sem regressão
- 38-UAT.md com 4 cenários (admin 1→2, desliga → 503, fail-open, fail-closed) validados manualmente
</success_criteria>

<output>
Create `.planning/phases/38-credit-operation-costs/38-08-SUMMARY.md` when done
</output>

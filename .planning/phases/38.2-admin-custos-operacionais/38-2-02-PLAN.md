---
phase: 38.2-admin-custos-operacionais
plan: 02
type: execute
wave: 2
depends_on:
  - "38-2-01"
files_modified:
  - "src/lib/economic/types.ts"
  - "src/lib/economic/economic-parameter-service.ts"
  - "src/lib/economic/__tests__/economic-parameter-service.test.ts"
autonomous: true
HAS_UI: 0
requirements:
  - F38.2-01
  - F38.2-04
must_haves:
  truths:
    - "ECONOMIC_PARAMETER_KEYS = ['usd_brl_rate', 'credit_value_brl'] + type EconomicParameterKey exportados de src/lib/economic/types.ts SEM server-only (importÃ¡veis por zod/UI)"
    - "EconomicParameterResolution { key, value, source: 'table' | 'fallback' } exportado"
    - "EconomicParameterService.getParameter(key) retorna { key, value, source }; linha inexistente â†’ default 1.00 fail-open com log de aviso; erro real de leitura â†’ lanÃ§a EconomicParameterUnavailableError fail-closed (D2)"
    - "EconomicParameterService.getAll() mescla tabela + fallback com source visÃ­vel"
    - "5 testes do service (12.1): table, fallback 1.00, fail-closed, getAll mescla, value <= 0 rejeitado"
  artifacts:
    - path: "src/lib/economic/types.ts"
      provides: "Chaves + resolution dos parÃ¢metros econÃ´micos (D1/D2)"
      exports: ["ECONOMIC_PARAMETER_KEYS", "EconomicParameterKey", "EconomicParameterResolution"]
    - path: "src/lib/economic/economic-parameter-service.ts"
      provides: "EconomicParameterService â€” leitura de parÃ¢metros com fail-open/fail-closed"
      exports: ["EconomicParameterService", "EconomicParameterUnavailableError"]
    - path: "src/lib/economic/__tests__/economic-parameter-service.test.ts"
      provides: "5 testes do service (tarefa 12.1)"
      contains: "getParameter"
  key_links:
    - from: "src/lib/economic/economic-parameter-service.ts"
      to: "public.economic_parameters (supabase)"
      via: "leitura via supabaseAdmin (service_role) â€” Ãºnica camada de leitura de parÃ¢metros"
      pattern: "economic_parameters"
    - from: "src/lib/economic/types.ts"
      to: "src/lib/ai-cost/admin-service.ts"
      via: "custoBrl = custoUsdTotal Ã— usd_brl_rate no service de custos (38-2-05)"
      pattern: "usd_brl_rate"
---

<objective>
Criar a fundaÃ§Ã£o de tipos e leitura dos ParÃ¢metros EconÃ´micos (D1/D2): `src/lib/economic/types.ts` (sem server-only â€” `ECONOMIC_PARAMETER_KEYS`, `EconomicParameterKey`, `EconomicParameterResolution`) e o `EconomicParameterService` em `src/lib/economic/economic-parameter-service.ts` (server-only, padrÃ£o `OperationCostService` F38: linha inexistente â†’ default seguro 1.00 fail-open com log aviso; erro real de leitura â†’ `EconomicParameterUnavailableError` fail-closed â†’ API 503). Testes do service (tarefa 12.1 â€” 5 testes).

Purpose: os dois parÃ¢metros (`usd_brl_rate` e `credit_value_brl`) sÃ£o a fonte Ãºnica de conversÃ£o monetÃ¡ria da fase â€” consumidos pelo service de custos de operaÃ§Ã£o (38-2-05), pela API de runs (38-2-06) e pela correÃ§Ã£o do `/admin/metrics` (38-2-09). Nenhum deles altera ledger nem transaÃ§Ãµes histÃ³ricas (D1/D2).
Output: tipos compartilhados + service testado (fail-open/fail-closed) â€” base para as rotas e pÃ¡ginas da fase.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38.2-admin-custos-operacionais/38-2-CONTEXT.md
@openspec/changes/fase-38-2-admin-custos-operacionais/specs/economic-parameters/spec.md

<interfaces>
Contratos que o executor precisa (extraÃ­dos do spec economic-parameters D1/D2 + 38-2-CONTEXT.md):

**Sem UI (HAS_UI: 0). Nenhuma pÃ¡gina/rota de UI Ã© alterada por este plano.**

Tipos em `src/lib/economic/types.ts` (analog: `src/lib/ai-cost/types.ts` â€” const-array enums + interfaces, SEM server-only):
```typescript
export const ECONOMIC_PARAMETER_KEYS = ["usd_brl_rate", "credit_value_brl"] as const;
export type EconomicParameterKey = (typeof ECONOMIC_PARAMETER_KEYS)[number];
export interface EconomicParameterResolution {
  key: EconomicParameterKey;
  value: number;
  source: "table" | "fallback";
}
```
- `usd_brl_rate`: 1 USD = R$ X â€” converte custo estimado do provider (D1)
- `credit_value_brl`: 1 crÃ©dito = R$ Y â€” estima receita operacional interna (D1)
- Default/fallback de AMBOS: **1.00** (conservador â€” D1)

`EconomicParameterService` (server-only, padrÃ£o `OperationCostService` F38 â€” `src/lib/credit/operation-cost-service.ts`):
```typescript
import "server-only";
import type { SupabaseClient } from "@supabase/supabase-js";
import { supabaseAdmin } from "@/lib/supabase/server";
export class EconomicParameterUnavailableError extends Error { ... }  // fail-closed
export class EconomicParameterService {
  constructor(private readonly client: SupabaseClient = supabaseAdmin) {}
  async getParameter(key: EconomicParameterKey): Promise<EconomicParameterResolution>;
  async getAll(): Promise<EconomicParameterResolution[]>;
}
```
- `getParameter`: SELECT da linha em `economic_parameters` (via supabaseAdmin). Linha inexistente â†’ `{ key, value: 1.00, source: "fallback" }` + `console.warn` (fail-open). **Erro real de leitura** (nÃ£o Ã© linha inexistente) â†’ `throw new EconomicParameterUnavailableError(...)` (fail-closed â†’ 503 na API).
- `getAll`: SELECT de todas as linhas + merge com fallback 1.00 para chaves ausentes (source visÃ­vel para o admin).
- NUNCA altera a tabela â€” leitura apenas (escrita Ã© via RPC `admin_set_economic_parameter`, 38-2-04).

PadrÃ£o de teste (analog: `src/lib/credit/__tests__/operation-cost-service.test.ts`): `vi.mock("@/lib/supabase/server")` no topo, table-dispatcher `mockFrom` (economic_parameters â†’ `{ select: mockSelect }`), cliente mock injetado no constructor, `mockSelect.mockResolvedValue({ data, error })`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tipos â€” src/lib/economic/types.ts (D1/D2)</name>
  <files>src/lib/economic/types.ts</files>
  <read_first>
    - src/lib/ai-cost/types.ts (padrÃ£o const-array enums + interfaces, sem server-only â€” linhas 8-30, 59-91)
    - src/lib/credit/types.ts (OPERATION_KEYS â€” padrÃ£o enum TS versionado)
    - openspec/changes/fase-38-2-admin-custos-operacionais/specs/economic-parameters/spec.md (linhas 3-30 â€” chaves)
  </read_first>
  <behavior>
    - Teste 1 (compile-time): `ECONOMIC_PARAMETER_KEYS` contÃ©m exatamente `['usd_brl_rate', 'credit_value_brl']`; atribuir "invalid_key" a um `EconomicParameterKey` â†’ erro de tipo
    - Teste 2 (compile-time): `EconomicParameterResolution` aceita `{ key: "usd_brl_rate", value: 5.0, source: "table" }` e `{ key: "credit_value_brl", value: 1.0, source: "fallback" }`
  </behavior>
  <action>
    Criar `src/lib/economic/types.ts` com os exports de contrato no estilo exato de `src/lib/ai-cost/types.ts` (const-array + `as const` + type derived). `ECONOMIC_PARAMETER_KEYS = ["usd_brl_rate", "credit_value_brl"] as const`; `type EconomicParameterKey = (typeof ECONOMIC_PARAMETER_KEYS)[number]`; `interface EconomicParameterResolution { key: EconomicParameterKey; value: number; source: "table" | "fallback" }`. NÃƒO adicionar `import "server-only"` (D1 â€” importÃ¡vel por zod/UI/route tests).
  </action>
  <verify>
    <automated>npm run typecheck; `grep -c "server-only" src/lib/economic/types.ts` == 0; `grep -c "ECONOMIC_PARAMETER_KEYS" src/lib/economic/types.ts` >= 2; `grep -c "usd_brl_rate" src/lib/economic/types.ts` >= 1; `grep -c "credit_value_brl" src/lib/economic/types.ts` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - 3 contratos exportados de economic/types.ts sem server-only
    - Chaves exatas usd_brl_rate + credit_value_brl
    - typecheck limpo
  </acceptance_criteria>
  <done>Tipos dos parÃ¢metros econÃ´micos criados (D1/D2)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EconomicParameterService â€” getParameter fail-open/fail-closed + getAll (D2)</name>
  <files>src/lib/economic/economic-parameter-service.ts</files>
  <read_first>
    - src/lib/credit/operation-cost-service.ts (padrÃ£o service server-only com SupabaseClient injetÃ¡vel + classe de erro fail-closed â€” linhas 1-40)
    - src/lib/ai-cost/admin-service.ts (padrÃ£o AiCostAdminUnavailableError + constructor client injectable â€” linhas 1-14, 171-198)
    - src/lib/economic/types.ts (criado na Task 1)
  </read_first>
  <behavior>
    - Teste 1: `getParameter("usd_brl_rate")` com linha existente (value 5.0) â†’ `{ key: "usd_brl_rate", value: 5, source: "table" }`
    - Teste 2: `getParameter("credit_value_brl")` com `data: null` (linha inexistente) â†’ `{ key: "credit_value_brl", value: 1, source: "fallback" }` (fail-open)
    - Teste 3: `getParameter("usd_brl_rate")` com erro real no select (ex.: erro de rede) â†’ `rejects` com `EconomicParameterUnavailableError` (fail-closed)
    - Teste 4: `getAll()` com 1 linha no banco â†’ 2 resoluÃ§Ãµes (linha existente source table + chave ausente source fallback 1.00)
    - Teste 5 (compile-time/unit): passar `value: 0` numa resoluÃ§Ã£o construÃ­da manualmente â†’ rejeitado em runtime pelo CHECK do banco (documentado no service); o service NUNCA persiste â€” leitura apenas
  </behavior>
  <action>
    Criar `src/lib/economic/economic-parameter-service.ts`:

    1. `import "server-only";` na primeira linha (D2) + imports: `type SupabaseClient`, `supabaseAdmin` de `@/lib/supabase/server`, tipos de `./types` (EconomicParameterKey, EconomicParameterResolution).
    2. `export class EconomicParameterUnavailableError extends Error` (fail-closed â€” padrÃ£o AiCostAdminUnavailableError/OperationCostUnavailableError; `this.name = "EconomicParameterUnavailableError"`).
    3. `export class EconomicParameterService` com `constructor(private readonly client: SupabaseClient = supabaseAdmin) {}` (padrÃ£o credit-service/ai-cost-admin â€” permite mock em testes).
    4. `async getParameter(key: EconomicParameterKey): Promise<EconomicParameterResolution>`:
       - `const { data, error } = await this.client.from("economic_parameters").select("key, value").eq("key", key).maybeSingle();`
       - `if (error)` â†’ `console.error(...)` + `throw new EconomicParameterUnavailableError(...)` (fail-closed â€” erro real de leitura, NÃƒO linha inexistente).
       - `if (!data)` â†’ `console.warn("[economic] ... usando fallback 1.00 para " + key)` + `return { key, value: 1.00, source: "fallback" }` (fail-open â€” D1 default seguro).
       - senÃ£o â†’ `return { key, value: Number(data.value), source: "table" }` (NUMERIC do Postgres chega como string | number â€” normalizar com Number e validar finito; valor <= 0 nÃ£o deveria existir â€” CHECK value > 0 no banco; defesa: se value <= 0, log + fallback 1.00).
    5. `async getAll(): Promise<EconomicParameterResolution[]>`:
       - `const { data, error } = await this.client.from("economic_parameters").select("key, value");`
       - `if (error)` â†’ `throw new EconomicParameterUnavailableError(...)` (fail-closed).
       - `return ECONOMIC_PARAMETER_KEYS.map((key) => { const row = data?.find((d) => d.key === key); return row ? { key, value: Number(row.value), source: "table" } : { key, value: 1.00, source: "fallback" }; })` â€” ordem fixa das chaves, merge com fallback (source visÃ­vel para o admin).
    6. NÃƒO exportar nada alÃ©m da classe + erro; NÃƒO adicionar mÃ©todos de escrita (escrita Ã© via RPC admin_set_economic_parameter â€” 38-2-04); NÃƒO ler outras tabelas.
  </action>
  <verify>
    <automated>npx vitest run src/lib/economic/__tests__/economic-parameter-service.test.ts; `grep -c "server-only" src/lib/economic/economic-parameter-service.ts` == 1; `grep -c "EconomicParameterUnavailableError" src/lib/economic/economic-parameter-service.ts` >= 2; `grep -c "fallback" src/lib/economic/economic-parameter-service.ts` >= 2; `grep -c "1.00" src/lib/economic/economic-parameter-service.ts` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - getParameter fail-open (fallback 1.00 com log) p/ linha inexistente; fail-closed (lanÃ§a) p/ erro real
    - getAll mescla tabela + fallback com source
    - MÃ³dulo server-only; classe Ãºnica de leitura; sem escrita
    - Testes 1-5 da behavior list passando
  </acceptance_criteria>
  <done>EconomicParameterService implementado (fail-open/fail-closed) â€” fonte Ãºnica de parÃ¢metros econÃ´micos</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Testes completos do service (tarefa 12.1 â€” 5 testes)</name>
  <files>src/lib/economic/__tests__/economic-parameter-service.test.ts</files>
  <read_first>
    - src/lib/credit/__tests__/operation-cost-service.test.ts (padrÃ£o de mock â€” vi.mock + table-dispatcher + client injetado â€” linhas 1-33)
    - src/lib/ai-cost/__tests__/tracker.test.ts (padrÃ£o de mock do ai-cost â€” linhas 1-40)
  </read_first>
  <behavior>
    - Casos do spec economic-parameters (tarefa 12.1 â€” 5 testes): linha existente â†’ source table; linha inexistente â†’ fallback 1.00 fail-open; erro real â†’ EconomicParameterUnavailableError fail-closed; getAll mescla tabela + fallback com source; value <= 0 â†’ serviÃ§o defende (log + fallback 1.00, nunca retorna invÃ¡lido)
  </behavior>
  <action>
    Criar `src/lib/economic/__tests__/economic-parameter-service.test.ts`:
    - PadrÃ£o: `vi.mock("@/lib/supabase/server", () => ({ supabaseAdmin: {} as any }))` no topo; cliente mock `{ from: mockFrom }`; `mockFrom` table-dispatcher (economic_parameters â†’ `{ select: mockSelect }`); `new EconomicParameterService(mockAdminClient as any)` em beforeEach com `vi.clearAllMocks()`.
    - Casos (spec, linhas 155-174): getParameter linha existente (data com value 5.0 â†’ source table); getParameter linha inexistente (data null â†’ source fallback, value 1); getParameter com erro real (select retorna `{ data: null, error: { message } }` â†’ rejects EconomicParameterUnavailableError); getAll com 1 linha no banco (2 resoluÃ§Ãµes, ordem das chaves, source correto por linha); value <= 0 na resposta (defesa do service: log + fallback 1.00 â€” nunca propaga valor invÃ¡lido).
  </action>
  <verify>
    <automated>npx vitest run src/lib/economic/__tests__/economic-parameter-service.test.ts; npm run typecheck; `grep -c "EconomicParameterUnavailableError" src/lib/economic/__tests__/economic-parameter-service.test.ts` >= 1; `grep -c "fallback" src/lib/economic/__tests__/economic-parameter-service.test.ts` >= 2</automated>
  </verify>
  <acceptance_criteria>
    - 5 testes passando (npx vitest run â€” 0 falhas)
    - typecheck limpo
  </acceptance_criteria>
  <done>5 testes do EconomicParameterService verdes (tarefa 12.1) â€” service pronto para as rotas/pÃ¡ginas</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EconomicParameterService.getParameter/getAll â†’ economic_parameters (supabaseAdmin) | Leitura via service_role; roda em server (server-only); erro real â†’ fail-closed |
| types.ts â†’ consumidores client/zod | Tipos sem server-only importÃ¡veis por UI â€” sem dados sensÃ­veis |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38.2-08 | Information Disclosure | ParÃ¢metros econÃ´micos (taxa/margem interna) | mitigate | Leitura apenas via service server-only (service_role); API admin sob requireAdmin (38-2-04); tipos sem server-only contÃªm sÃ³ chaves/nomes â€” F38.2-04 |
| T-38.2-09 | DoS | getParameter em falha de leitura | mitigate | Fail-closed: erro real â†’ EconomicParameterUnavailableError â†’ 503 (a UI mostra indisponÃ­vel, nÃ£o nÃºmeros errados); fail-open apenas para linha inexistente (default 1.00 conservador com log) â€” F38.2-04 |
| T-38.2-10 | Tampering | Valor de parÃ¢metro invÃ¡lido (<= 0) | mitigate | Defesa no service (log + fallback 1.00) + CHECK value > 0 no banco (38-2-01) + validaÃ§Ã£o zod na rota PUT (38-2-04) â€” F38.2-04 |
| T-38.2-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado neste plano â€” gate de legitimidade nÃ£o se aplica |
</threat_model>

<verification>
- `npm run typecheck` limpo (tipos + service)
- `npx vitest run src/lib/economic/__tests__/economic-parameter-service.test.ts` â€” 5 testes, 0 falhas
- Nenhum `server-only` em types.ts; service com `import "server-only"`
</verification>

<success_criteria>
- Tipos exportados sem server-only (ECONOMIC_PARAMETER_KEYS, EconomicParameterKey, EconomicParameterResolution)
- EconomicParameterService getParameter fail-open/fail-closed + getAll com source
- 5 testes do service verdes (tarefa 12.1)
</success_criteria>

<output>
Create `.planning/phases/38.2-admin-custos-operacionais/38-2-02-SUMMARY.md` when done
</output>

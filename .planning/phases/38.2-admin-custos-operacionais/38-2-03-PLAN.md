---
phase: 38.2-admin-custos-operacionais
plan: 03
type: execute
wave: 2
depends_on:
  - "38-2-01"
files_modified:
  - "src/lib/ai-cost/tracker.ts"
  - "src/lib/ai-cost/types.ts"
  - "src/lib/ai-cost/__tests__/tracker.test.ts"
autonomous: true
HAS_UI: 0
requirements:
  - F38.2-16
must_haves:
  truths:
    - "CostResolution (src/lib/ai-cost/types.ts) jÃ¡ carrega costFormulaVersion, costEstimationNote, textComponentUsd, imageToolComponentUsd (F38.1 closure) â€” NÃƒO duplicar; apenas garantir contrato"
    - "AiCostTracker.record passa a persistir cost_formula_version, cost_estimation_note, text_component_usd, image_tool_component_usd a partir do CostResolution â€” daqui para frente (D5), sem reclassificar histÃ³rico"
    - "Eventos anteriores Ã  migration (colunas NULL) caem em badge genÃ©rico na UI (38-2-05/08) â€” o tracker nÃ£o toca histÃ³rico"
    - "4 testes do tracker (12.3): record persiste 4 campos; evento sem nota â†’ NULL; provider_reported â†’ badge (mapa 38-2-05); nota provisional + pricing_table â†’ badge provisional image tool estimate (mapa 38-2-05)"
  artifacts:
    - path: "src/lib/ai-cost/tracker.ts"
      provides: "record persistindo os 4 campos de confianÃ§a (D5)"
      contains: "cost_estimation_note"
    - path: "src/lib/ai-cost/types.ts"
      provides: "CostResolution com campos de confianÃ§a (jÃ¡ presentes â€” garantia de contrato)"
      contains: "costFormulaVersion"
    - path: "src/lib/ai-cost/__tests__/tracker.test.ts"
      provides: "Testes de persistÃªncia de confianÃ§a (12.3 â€” 4 testes novos)"
      contains: "cost_estimation_note"
  key_links:
    - from: "src/lib/ai-cost/tracker.ts"
      to: "generation_events (cost_formula_version, cost_estimation_note, text_component_usd, image_tool_component_usd)"
      via: "insert via supabaseAdmin com as 4 colunas novas da migration 38-2-01 (D5)"
      pattern: "cost_estimation_note"
    - from: "src/lib/ai-cost/types.ts (CostResolution)"
      to: "src/lib/ai-cost/operation-runs-service.ts (38-2-05)"
      via: "badges derivados de cost_source + cost_estimation_note (D5)"
      pattern: "costEstimationNote"
---

<objective>
Persistir a confianÃ§a da estimativa de custo (D5): `AiCostTracker.record` passa a gravar `cost_formula_version`, `cost_estimation_note`, `text_component_usd` e `image_tool_component_usd` em `generation_events` a partir do `CostResolution` â€” **daqui para frente** (eventos anteriores Ã  migration 38-2-01 ficam com NULL e caem em badge genÃ©rico na UI). O contrato `CostResolution` em `src/lib/ai-cost/types.ts` jÃ¡ carrega os 4 campos (fechamento F38.1) â€” este plano garante o contrato e implementa a persistÃªncia no tracker. Testes (tarefa 12.3 â€” 4 testes novos).

Purpose: sem persistÃªncia, a UI de Custos de OperaÃ§Ã£o (38-2-08) nÃ£o consegue mostrar os badges de confianÃ§a (`provisional image tool estimate`, `partial`, `estimated`, `provider_reported`, `not_available`) no histÃ³rico â€” o finding F1 da F38.2 (confianÃ§a computada e descartada no record).
Output: tracker persistindo os 4 campos de confianÃ§a + testes verdes.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38.2-admin-custos-operacionais/38-2-CONTEXT.md
@openspec/changes/fase-38-2-admin-custos-operacionais/specs/ai-cost-tracker/spec.md

<interfaces>
Contratos que o executor precisa (extraÃ­dos do spec ai-cost-tracker delta D5 + 38-2-CONTEXT.md + leitura dos arquivos):

**Sem UI (HAS_UI: 0). Nenhuma pÃ¡gina/rota de UI Ã© alterada por este plano.**

Estado verificado (38-2-CONTEXT.md F1 + leitura):
- `CostResolution` em `src/lib/ai-cost/types.ts:59-91` **JÃ TEM** `costFormulaVersion?: string`, `costEstimationNote?: string`, `textComponentUsd?: number`, `imageToolComponentUsd?: number` (fechamento F38.1 â€” comments nas linhas 65-86). NÃƒO duplicar.
- `AiCostTracker.record` em `src/lib/ai-cost/tracker.ts:30-69` hoje NÃƒO persiste os 4 campos â€” o mapping no insert termina em `pricing_version` (linha 57).

MudanÃ§a no tracker:
```typescript
// no objeto de insert de record, apÃ³s pricing_version:
cost_formula_version: event.cost?.costFormulaVersion ?? null,
cost_estimation_note: event.cost?.costEstimationNote ?? null,
text_component_usd: event.cost?.textComponentUsd ?? null,
image_tool_component_usd: event.cost?.imageToolComponentUsd ?? null,
```
- MantÃ©m `best-effort` (try/catch + console.error + nunca lanÃ§a â€” D7).
- MantÃ©m delivery marker (sem cost/tokens â†’ colunas NULL + duration_is_pipeline).
- MantÃ©m a coluna `metadata` como estÃ¡ â€” os 4 campos agora viram colunas prÃ³prias (a F38.1 os mantinha sÃ³ no metadata do evento campaign_image; agora sÃ£o estruturados â€” D5).
- NÃƒO reclassifica histÃ³rico: nenhum UPDATE em linhas antigas.

DerivaÃ§Ã£o de badge (NÃƒO Ã© deste plano â€” mapa consumido em 38-2-05/08, referÃªncia p/ os testes):
| Badge | CondiÃ§Ã£o |
|-------|----------|
| provider_reported | cost_source = 'provider_reported' |
| provisional image tool estimate | cost_source = 'pricing_table' E cost_estimation_note = 'provisional_image_tool_unit_cost_until_provider_reconciliation' |
| partial | cost_source = 'manual_unknown' OU pricing_table com nota de estimativa parcial |
| estimated | cost_source = 'pricing_table' (sem nota) ou fallback_static |
| not_available | cost_source = 'not_available' |
| estimated (genÃ©rico) | cost_source presente mas nota NULL |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Garantia de contrato â€” CostResolution com os 4 campos (D5)</name>
  <files>src/lib/ai-cost/types.ts</files>
  <read_first>
    - src/lib/ai-cost/types.ts (CostResolution â€” linhas 59-91; jÃ¡ tem os 4 campos do fechamento F38.1)
    - openspec/changes/fase-38-2-admin-custos-operacionais/specs/ai-cost-tracker/spec.md
  </read_first>
  <behavior>
    - Teste 1 (compile-time): `CostResolution` aceita `{ estimatedCostUsd, costSource: "pricing_table", costFormulaVersion: "responses_image_generation_v2", costEstimationNote: "provisional_image_tool_unit_cost_until_provider_reconciliation", textComponentUsd: 0.01, imageToolComponentUsd: 0.04 }`
  </behavior>
  <action>
    Verificar que `src/lib/ai-cost/types.ts` jÃ¡ exporta os 4 campos em `CostResolution` (costFormulaVersion, costEstimationNote, textComponentUsd, imageToolComponentUsd â€” presentes desde o fechamento F38.1). Se presentes, NÃƒO alterar nada alÃ©m de ajustar o JSDoc se necessÃ¡rio (mencionar persistÃªncia D5 da F38.2). Se algum campo faltar (regressÃ£o), adicionÃ¡-lo no estilo existente. NÃƒO adicionar `server-only`; NÃƒO mudar a assinatura de outros contratos.
  </action>
  <verify>
    <automated>npm run typecheck; `grep -c "costFormulaVersion" src/lib/ai-cost/types.ts` >= 1; `grep -c "costEstimationNote" src/lib/ai-cost/types.ts` >= 1; `grep -c "textComponentUsd" src/lib/ai-cost/types.ts` >= 1; `grep -c "imageToolComponentUsd" src/lib/ai-cost/types.ts` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - CostResolution com os 4 campos de confianÃ§a (jÃ¡ presentes ou restaurados)
    - typecheck limpo
  </acceptance_criteria>
  <done>Contrato CostResolution garantido com os 4 campos (D5)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AiCostTracker.record â€” persistir os 4 campos de confianÃ§a (D5)</name>
  <files>src/lib/ai-cost/tracker.ts</files>
  <read_first>
    - src/lib/ai-cost/tracker.ts (record â€” linhas 30-69; mapping termina em pricing_version na 57)
    - src/lib/ai-cost/types.ts (CostResolution â€” campos opcionais)
  </read_first>
  <behavior>
    - Teste 2: `record(event)` com evento completo (costSource pricing_table + costFormulaVersion + costEstimationNote provisional + textComponentUsd + imageToolComponentUsd) â†’ `mockInsert` chamado com as 4 colunas novas preenchidas
    - Teste 3: `record(event)` com CostResolution sem nota/componentes (costSource sÃ³) â†’ colunas cost_formula_version/cost_estimation_note/text_component_usd/image_tool_component_usd NULL (badge genÃ©rico na UI)
    - Teste 4: `record(event)` com provider_reported â†’ cost_source persistido (badge provider_reported derivado em 38-2-05); delivery marker (sem cost) â†’ colunas de confianÃ§a NULL + duration_is_pipeline
  </behavior>
  <action>
    Em `src/lib/ai-cost/tracker.ts`, dentro do objeto de insert de `record`, apÃ³s a linha `pricing_version: event.cost?.pricingVersion ?? null` (linha 57), adicionar:
    ```
    cost_formula_version: event.cost?.costFormulaVersion ?? null,
    cost_estimation_note: event.cost?.costEstimationNote ?? null,
    text_component_usd: event.cost?.textComponentUsd ?? null,
    image_tool_component_usd: event.cost?.imageToolComponentUsd ?? null,
    ```
    NÃƒO alterar o fluxo best-effort (try/catch + console.error + resolve â€” D7), NÃƒO alterar o delivery marker (cost/tokens NULL + duration_is_pipeline), NÃƒO adicionar UPDATE/backfill de histÃ³rico (D5 â€” daqui para frente). Atualizar o JSDoc da classe/mÃ©todo para citar a persistÃªncia das 4 colunas.
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai-cost/__tests__/tracker.test.ts; npm run typecheck; `grep -c "cost_formula_version" src/lib/ai-cost/tracker.ts` == 1; `grep -c "cost_estimation_note" src/lib/ai-cost/tracker.ts` == 1; `grep -c "text_component_usd" src/lib/ai-cost/tracker.ts` == 1; `grep -c "image_tool_component_usd" src/lib/ai-cost/tracker.ts` == 1</automated>
  </verify>
  <acceptance_criteria>
    - record persiste os 4 campos de confianÃ§a a partir do CostResolution
    - Sem nota/componentes â†’ colunas NULL (badge genÃ©rico)
    - Best-effort e delivery marker intactos; sem backfill de histÃ³rico
    - typecheck limpo
  </acceptance_criteria>
  <done>Tracker persistindo os 4 campos de confianÃ§a â€” daqui para frente (D5)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Testes de persistÃªncia de confianÃ§a (tarefa 12.3 â€” 4 testes)</name>
  <files>src/lib/ai-cost/__tests__/tracker.test.ts</files>
  <read_first>
    - src/lib/ai-cost/__tests__/tracker.test.ts (padrÃ£o existente de mock do tracker â€” vi.mock supabase + table-dispatcher + client injetado)
    - openspec/changes/fase-38-2-admin-custos-operacionais/specs/ai-cost-tracker/spec.md
  </read_first>
  <behavior>
    - Casos do spec (tarefa 12.3 â€” 4 testes novos, adicionar aos 8 existentes da F38.1):
      1. record com CostResolution completo (nota provisional + componentes) â†’ insert com as 4 colunas novas preenchidas (cost_formula_version, cost_estimation_note, text_component_usd, image_tool_component_usd)
      2. record sem nota/componentes (cost_source sÃ³) â†’ colunas de confianÃ§a NULL (histÃ³rico prÃ©-D5 â†’ badge genÃ©rico)
      3. record com provider_reported â†’ cost_source persistido; badge derivado em 38-2-05 (teste aqui apenas a persistÃªncia)
      4. nota provisional + pricing_table â†’ cost_estimation_note persistida (insumo para badge provisional image tool estimate em 38-2-05)
  </behavior>
  <action>
    Estender `src/lib/ai-cost/__tests__/tracker.test.ts` com os 4 casos da behavior list (adicionar aos testes existentes da F38.1 â€” nÃ£o removÃª-los). Reutilizar o mock existente (vi.mock supabase/server, mockFrom table-dispatcher, mockInsert, new AiCostTracker(mockClient)). Verificar o objeto passado a `mockInsert` (expect.objectContaining com as 4 colunas).
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai-cost/__tests__/tracker.test.ts; `grep -c "cost_estimation_note" src/lib/ai-cost/__tests__/tracker.test.ts` >= 2; `grep -c "provisional_image_tool_unit_cost_until_provider_reconciliation" src/lib/ai-cost/__tests__/tracker.test.ts` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - 4 testes novos de persistÃªncia passando (total da suite do tracker = 12)
    - Testes F38.1 existentes intactos e verdes
  </acceptance_criteria>
  <done>4 testes de persistÃªncia de confianÃ§a verdes (tarefa 12.3)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AiCostTracker.record â†’ generation_events (supabaseAdmin) | Escrita de telemetria com colunas de confianÃ§a via service_role; best-effort; nunca bloqueia geraÃ§Ã£o |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38.2-11 | Tampering | Nota/componentes de confianÃ§a persistidos | mitigate | Colunas IF NOT EXISTS sem CHECK (D5 â€” notas livres); escrita apenas via service_role (default-deny); nunca altera histÃ³rico â€” F38.2-16 |
| T-38.2-12 | Spoofing | Badge derivado incorretamente (UI) | mitigate | PersistÃªncia fiel do cost_source + nota; a DERIVAÃ‡ÃƒO de badge fica no service (38-2-05) com mapa determinÃ­stico â€” este plano sÃ³ persiste insumos â€” F38.2-16 |
| T-38.2-13 | DoS | record em falha de escrita | mitigate | Best-effort: try/catch + console.error + resolve sem lanÃ§ar (D7) â€” F38.2-16 |
| T-38.2-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado neste plano â€” gate de legitimidade nÃ£o se aplica |
</threat_model>

<verification>
- `npm run typecheck` limpo (contrato + tracker)
- `npx vitest run src/lib/ai-cost/__tests__/tracker.test.ts` â€” suite completa (8 F38.1 + 4 novos), 0 falhas
- 4 colunas de confianÃ§a no insert do record; sem backfill
</verification>

<success_criteria>
- CostResolution com os 4 campos (garantido)
- AiCostTracker.record persiste os 4 campos de confianÃ§a (D5, daqui para frente)
- 4 testes novos verdes; suite do tracker sem regressÃ£o
</success_criteria>

<output>
Create `.planning/phases/38.2-admin-custos-operacionais/38-2-03-SUMMARY.md` when done
</output>

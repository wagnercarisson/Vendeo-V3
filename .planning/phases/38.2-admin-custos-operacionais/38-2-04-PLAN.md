---
phase: 38.2-admin-custos-operacionais
plan: 04
type: execute
wave: 3
depends_on:
  - "38-2-02"
files_modified:
  - "src/lib/admin/schemas.ts"
  - "src/app/api/admin/economic-parameters/route.ts"
  - "src/app/api/admin/economic-parameters/__tests__/route.test.ts"
autonomous: true
HAS_UI: 0
requirements:
  - F38.2-05
  - F38.2-06
must_haves:
  truths:
    - "UpdateEconomicParameterRequestSchema em src/lib/admin/schemas.ts: key enum ECONOMIC_PARAMETER_KEYS, value number > 0, reason obrigatÃ³rio (min 1), operationId uuid opcional (idempotÃªncia)"
    - "GET /api/admin/economic-parameters â†’ 200 { parameters: [...] } via EconomicParameterService.getAll() (source table/fallback); 403 sem admin; 503 fail-closed"
    - "PUT /api/admin/economic-parameters â†’ zod + RPC admin_set_economic_parameter; 200 { parameter, auditId, updatedAt, idempotent }; 400 zod; 403 sem admin; 500 erro do RPC"
    - "Nenhum endpoint pÃºblico (parÃ¢metros sÃ£o dado interno de operaÃ§Ã£o)"
    - "5 testes da API (12.2): PUT atualiza via RPC + audit; sem reason â†’ 400; key invÃ¡lido/value <= 0 â†’ 400; GET/PUT sem admin â†’ 403; idempotÃªncia por operation_id"
  artifacts:
    - path: "src/lib/admin/schemas.ts"
      provides: "UpdateEconomicParameterRequestSchema (D2)"
      contains: "UpdateEconomicParameterRequestSchema"
    - path: "src/app/api/admin/economic-parameters/route.ts"
      provides: "GET lista + PUT via RPC"
      exports: ["GET", "PUT"]
    - path: "src/app/api/admin/economic-parameters/__tests__/route.test.ts"
      provides: "5 testes da API (tarefa 12.2)"
      contains: "admin_set_economic_parameter"
  key_links:
    - from: "src/app/api/admin/economic-parameters/route.ts (PUT)"
      to: "public.admin_set_economic_parameter (RPC)"
      via: "rpc('admin_set_economic_parameter', { p_actor_id, p_key, p_value, p_reason, p_operation_id })"
      pattern: "admin_set_economic_parameter"
    - from: "src/app/api/admin/economic-parameters/route.ts (GET)"
      to: "src/lib/economic/economic-parameter-service.ts"
      via: "EconomicParameterService.getAll()"
      pattern: "EconomicParameterService"
---

<objective>
Criar a API admin de ConfiguraÃ§Ãµes EconÃ´micas (D2): `GET /api/admin/economic-parameters` (lista resolvida via `EconomicParameterService.getAll()` com `source` table/fallback â€” 200/403/503) e `PUT /api/admin/economic-parameters` (zod + RPC `admin_set_economic_parameter` â€” 200 { parameter, auditId, updatedAt, idempotent } / 400 / 403 / 500). Schema `UpdateEconomicParameterRequestSchema` em `src/lib/admin/schemas.ts`. **Sem endpoint pÃºblico.** Testes (tarefa 12.2 â€” 5 testes).

Purpose: os dois parÃ¢metros econÃ´micos (`usd_brl_rate`, `credit_value_brl`) tornam-se administrÃ¡veis sem deploy, com motivo obrigatÃ³rio e auditoria â€” a pÃ¡gina /admin/operation-costs (38-2-07) consome esta API. Nenhum parÃ¢metro altera ledger nem transaÃ§Ãµes histÃ³ricas (D1/D2).
Output: API GET/PUT testada + schema zod.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38.2-admin-custos-operacionais/38-2-CONTEXT.md
@openspec/changes/fase-38-2-admin-custos-operacionais/specs/economic-parameters/spec.md

<interfaces>
Contratos que o executor precisa (extraÃ­dos do spec economic-parameters D2 + 38-2-CONTEXT.md + leitura dos arquivos):

**Sem UI (HAS_UI: 0). Nenhuma pÃ¡gina/rota de UI Ã© alterada por este plano.**

Schema (`src/lib/admin/schemas.ts` â€” adicionar ao arquivo existente, no estilo de UpdateOperationCostRequestSchema/AiModelPricingUpdateSchema):
```typescript
export const UpdateEconomicParameterRequestSchema = z.object({
  key: z.enum(ECONOMIC_PARAMETER_KEYS),
  value: z.number().positive("Value deve ser maior que zero"),
  reason: z.string().min(1, "Motivo obrigatÃ³rio"),
  operationId: z.string().uuid().optional(),  // idempotÃªncia
});
```
- import `ECONOMIC_PARAMETER_KEYS` de `@/lib/economic/types`.

GET (`src/app/api/admin/economic-parameters/route.ts`):
- `apiHandler` + `requireAdmin()` (padrÃ£o `src/app/api/admin/ai-costs/route.ts` â€” linhas 1-57).
- `new EconomicParameterService().getAll()` â†’ `NextResponse.json({ parameters })`.
- `EconomicParameterUnavailableError` â†’ 503 `{ error: "economic_parameters_unavailable" }`.
- NENHUM parÃ¢metro de query/body â€” leitura pura.

PUT (mesma rota):
- Body parseado com `UpdateEconomicParameterRequestSchema`; `!parsed.success` â†’ 400 `{ error: "Dados invÃ¡lidos", details }`.
- Chama `client.rpc("admin_set_economic_parameter", { p_actor_id, p_key, p_value, p_reason, p_operation_id })` â€” p_actor_id = usuÃ¡rio autenticado (requireAdmin retorna o user; seguir o padrÃ£o de outras rotas admin que pegam o actor). 
- Sucesso â†’ 200 `{ parameter: { key, value }, auditId, updatedAt, idempotent }` (do JSONB do RPC).
- Erro do RPC â†’ 500 `{ error: "economic_parameter_update_failed" }`.
- 403 sem admin (requireAdmin lanÃ§a â€” padrÃ£o da rota ai-costs).

PadrÃ£o de teste de rota (analog: `src/app/api/admin/ai-costs/__tests__/route.test.ts`): mock de requireAdmin (admin logado), mock do service/RPC via vi.mock, chamadas fetch ao handler.

Atores/actor_id: verificar como as rotas admin existentes obtÃªm o user id (ex.: `src/lib/admin/require-admin.ts` ou `apiHandler` contexto) â€” seguir o padrÃ£o vigente.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema UpdateEconomicParameterRequestSchema (D2)</name>
  <files>src/lib/admin/schemas.ts</files>
  <read_first>
    - src/lib/admin/schemas.ts (padrÃ£o UpdateOperationCostRequestSchema â€” linhas 11-25; AiModelPricingUpdateSchema â€” linhas 49-72)
    - src/lib/economic/types.ts (ECONOMIC_PARAMETER_KEYS)
    - openspec/changes/fase-38-2-admin-custos-operacionais/specs/economic-parameters/spec.md (linhas 204-251 â€” PUT)
  </read_first>
  <behavior>
    - Teste 1 (compile-time): schema aceita `{ key: "usd_brl_rate", value: 5.0, reason: "calibraÃ§Ã£o" }` e `{ key: "credit_value_brl", value: 1.5, reason: "x", operationId: <uuid> }`
    - Teste 2 (compile-time): schema REJEITA `{ key: "invalid", value: 1 }` (enum), `{ value: 0 }` (positive), `{ key: "usd_brl_rate", value: 1 }` sem reason (obrigatÃ³rio)
  </behavior>
  <action>
    Adicionar `UpdateEconomicParameterRequestSchema` em `src/lib/admin/schemas.ts` no estilo dos schemas existentes (z.object + validaÃ§Ã£o de mensagens PT-BR). Importar `ECONOMIC_PARAMETER_KEYS` de `@/lib/economic/types`. `key: z.enum(ECONOMIC_PARAMETER_KEYS)`, `value: z.number().positive(...)`, `reason: z.string().min(1, ...)`, `operationId: z.string().uuid().optional()`. NÃƒO alterar schemas existentes.
  </action>
  <verify>
    <automated>npm run typecheck; `grep -c "UpdateEconomicParameterRequestSchema" src/lib/admin/schemas.ts` >= 1; `grep -c "ECONOMIC_PARAMETER_KEYS" src/lib/admin/schemas.ts` >= 1; `grep -c "z.enum" src/lib/admin/schemas.ts` >= 2</automated>
  </verify>
  <acceptance_criteria>
    - Schema com key enum, value positive, reason obrigatÃ³rio, operationId uuid opcional
    - Schemas existentes intactos
    - typecheck limpo
  </acceptance_criteria>
  <done>Schema de update criado (D2)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GET /api/admin/economic-parameters (D2)</name>
  <files>src/app/api/admin/economic-parameters/route.ts</files>
  <read_first>
    - src/app/api/admin/ai-costs/route.ts (padrÃ£o apiHandler + requireAdmin + ZodError + 503 â€” linhas 1-57)
    - src/lib/economic/economic-parameter-service.ts (EconomicParameterService + UnavailableError â€” 38-2-02)
  </read_first>
  <behavior>
    - Teste 1 (route): GET com admin â†’ 200 `{ parameters: [{ key, value, source }] }` (service retorna 2 resoluÃ§Ãµes)
    - Teste 2 (route): GET sem admin â†’ 403 (requireAdmin lanÃ§a ForbiddenError)
    - Teste 3 (route): GET com service lanÃ§ando EconomicParameterUnavailableError â†’ 503
  </behavior>
  <action>
    Criar `src/app/api/admin/economic-parameters/route.ts` com:
    1. `export const GET = apiHandler(async (request: Request) => { await requireAdmin(); ... })` no padrÃ£o exato da rota ai-costs.
    2. `const parameters = await new EconomicParameterService().getAll();` â†’ `NextResponse.json({ parameters })`.
    3. try/catch: `EconomicParameterUnavailableError` â†’ `NextResponse.json({ error: "economic_parameters_unavailable" }, { status: 503 })`; demais â†’ rethrow.
    4. NENHUM endpoint pÃºblico â€” nÃ£o criar rota fora de `/api/admin/`.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/admin/economic-parameters/__tests__/route.test.ts; `grep -c "requireAdmin" src/app/api/admin/economic-parameters/route.ts` >= 1; `grep -c "EconomicParameterService" src/app/api/admin/economic-parameters/route.ts` >= 1; `grep -c "503" src/app/api/admin/economic-parameters/route.ts` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - GET 200 com parameters + source; 403 sem admin; 503 fail-closed
  </acceptance_criteria>
  <done>GET da API de parÃ¢metros funcionando (200/403/503)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PUT /api/admin/economic-parameters (D2) + 5 testes da API (12.2)</name>
  <files>src/app/api/admin/economic-parameters/route.ts, src/app/api/admin/economic-parameters/__tests__/route.test.ts</files>
  <read_first>
    - src/app/api/admin/operation-costs/route.ts ou rota admin com RPC (padrÃ£o de PUT admin + actor_id â€” verificar como obtÃ©m o user id)
    - src/lib/admin/require-admin.ts (retorno/forma de obter o actor)
    - openspec/changes/fase-38-2-admin-custos-operacionais/specs/economic-parameters/spec.md (linhas 204-251)
  </read_first>
  <behavior>
    - Teste 4 (route): PUT com body vÃ¡lido â†’ RPC `admin_set_economic_parameter` chamado com p_actor_id/p_key/p_value/p_reason e retorno 200 `{ parameter, auditId, updatedAt, idempotent }`
    - Teste 5 (route): PUT sem reason â†’ 400; PUT com key invÃ¡lido â†’ 400; PUT com value <= 0 â†’ 400 (zod)
    - Teste 6 (route): PUT sem admin â†’ 403
    - Teste 7 (route): RPC retorna erro â†’ 500
    - Teste 8 (route): idempotÃªncia â€” mesmo operation_id repetido â†’ RPC retorna idempotent: true (repassado no 200)
  </behavior>
  <action>
    Adicionar `PUT` Ã  rota criada na Task 2 (mesma `route.ts`):
    1. `export const PUT = apiHandler(async (request: Request) => { await requireAdmin(); ... })`.
    2. `const body = await request.json().catch(() => null);` + `UpdateEconomicParameterRequestSchema.safeParse(body)`; `!parsed.success` â†’ 400 `{ error: "Dados invÃ¡lidos", details }`.
    3. Obter o `p_actor_id` seguindo o padrÃ£o vigente das rotas admin (requireAdmin/apiHandler â€” verificar; ex.: usuÃ¡rio do header/session). 
    4. `const { data, error } = await client.rpc("admin_set_economic_parameter", { p_actor_id, p_key: parsed.data.key, p_value: parsed.data.value, p_reason: parsed.data.reason, p_operation_id: parsed.data.operationId ?? null });` (client = supabaseAdmin via service â€” padrÃ£o da rota).
    5. Sucesso â†’ 200 `{ parameter: { key: data.key, value: data.value }, auditId: data.audit_id, updatedAt: data.updated_at, idempotent: data.idempotent }`.
    6. `error` â†’ 500 `{ error: "economic_parameter_update_failed" }`.
    7. NÃƒO expor publicamente; NÃƒO logar o reason em texto completo se sensÃ­vel (seguir padrÃ£o de auditoria F38 â€” reason jÃ¡ vai para a audit table).
    8. Criar `src/app/api/admin/economic-parameters/__tests__/route.test.ts` com os 5 casos da behavior list (mock de requireAdmin + mock do rpc/supabase â€” seguir o padrÃ£o do teste da rota ai-costs).
  </action>
  <verify>
    <automated>npx vitest run src/app/api/admin/economic-parameters/__tests__/route.test.ts; npm run typecheck; `grep -c "admin_set_economic_parameter" src/app/api/admin/economic-parameters/route.ts` >= 1; `grep -c "idempotent" src/app/api/admin/economic-parameters/route.ts` >= 1; `grep -c "auditId" src/app/api/admin/economic-parameters/route.ts` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - PUT 200 via RPC com { parameter, auditId, updatedAt, idempotent }; 400/403/500 mapeados
    - idempotÃªncia por operation_id repassada
    - 5 testes da API passando (tarefa 12.2)
    - typecheck limpo
  </acceptance_criteria>
  <done>PUT implementado + 5 testes da API verdes (tarefa 12.2)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PUT /api/admin/economic-parameters â† admin autenticado | MutaÃ§Ã£o de parÃ¢metros econÃ´micos com motivo obrigatÃ³rio; delega ao RPC SECURITY DEFINER (Ãºnica via de escrita) |
| GET /api/admin/economic-parameters â† admin autenticado | Leitura de parÃ¢metros internos â€” nunca exposta publicamente |
| EconomicParameterService â†’ economic_parameters | Leitura server-only via service_role (38-2-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38.2-14 | Elevation of Privilege | PUT sem admin | mitigate | `requireAdmin()` na rota; RPC com REVOKE EXECUTE FROM PUBLIC/anon/authenticated + GRANT service_role (38-2-01) â€” F38.2-06 |
| T-38.2-15 | Tampering | ParÃ¢metro sem motivo / valor invÃ¡lido | mitigate | zod: key enum ECONOMIC_PARAMETER_KEYS, value positive, reason min 1; CHECK value > 0 no banco (38-2-01) â€” F38.2-06 |
| T-38.2-16 | Replay | Retry duplicado de PUT | mitigate | idempotÃªncia por operation_id (UNIQUE parcial na audit + RPC retorna idempotent) â€” F38.2-06 |
| T-38.2-17 | Information Disclosure | ParÃ¢metros via endpoint pÃºblico | mitigate | Rota exclusivamente sob /api/admin/ + requireAdmin; nenhuma rota pÃºblica criada â€” F38.2-05 |
| T-38.2-18 | DoS | GET em falha de leitura | mitigate | Fail-closed: EconomicParameterUnavailableError â†’ 503 (38-2-02) â€” F38.2-05 |
| T-38.2-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado neste plano â€” gate de legitimidade nÃ£o se aplica |
</threat_model>

<verification>
- `npm run typecheck` limpo (schema + rota)
- `npx vitest run src/app/api/admin/economic-parameters/__tests__/route.test.ts` â€” 5 testes, 0 falhas
- GET 200/403/503 + PUT 200/400/403/500 + idempotÃªncia
</verification>

<success_criteria>
- UpdateEconomicParameterRequestSchema validado (enum, positive, reason, operationId)
- GET lista resolvida com source (200/403/503)
- PUT via RPC com auditoria + idempotÃªncia (200/400/403/500)
- 5 testes da API verdes (tarefa 12.2); sem endpoint pÃºblico
</success_criteria>

<output>
Create `.planning/phases/38.2-admin-custos-operacionais/38-2-04-SUMMARY.md` when done
</output>

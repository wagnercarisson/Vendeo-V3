---
phase: 38.2-admin-custos-operacionais
plan: 05
type: execute
wave: 3
depends_on:
  - "38-2-01"
  - "38-2-02"
files_modified:
  - "src/lib/ai-cost/operation-runs-service.ts"
  - "src/lib/ai-cost/__tests__/operation-runs-service.test.ts"
autonomous: true
HAS_UI: 0
requirements:
  - F38.2-12
  - F38.2-14
must_haves:
  truths:
    - "OperationRunsService (src/lib/ai-cost/operation-runs-service.ts) server-only chama RPCs admin_get_ai_operation_runs/_events (via supabaseAdmin) e deriva no service: custoBrl = custoUsdTotal Ã— usd_brl_rate; receitaOpBrl = creditosDebitados Ã— credit_value_brl; resultadoOpBrl = receitaOpBrl âˆ’ custoBrl; margemOpPct = receitaOpBrl > 0 ? (resultado/receita) Ã— 100 : null (D1/D4, via EconomicParameterService)"
    - "DerivaÃ§Ã£o de badges (D5): mapa cost_source + cost_estimation_note â†’ badge provider_reported / provisional image tool estimate / partial / estimated / not_available / estimated genÃ©rico â€” por evento (detalhe) e por entrega (insumos agregados do RPC, prioridade has_provider_reported)"
    - "SegmentaÃ§Ã£o econÃ´mica (D9) no service: classifySegment(store, deduction) â†’ test (is_test_store) | freemium/promotional (bonus_amount>0 e purchased=0) | paid (purchased_amount>0) | manual/admin (admin_grant â€” shape real confirmado; sem evidÃªncia â†’ unknown) | unknown (fallback); paid/unknown com baixa confianÃ§a; filtro de segmento + re-paginaÃ§Ã£o NO SERVICE (D4/D9) via paginaÃ§Ã£o PROGRESSIVA no RPC (loop p_page 1..n, page_size 100, atÃ© acumular summary.total do perÃ­odo) â€” total/pÃ¡gina refletem o conjunto segmento-filtrado"
    - "Aggregations derivadas no service sobre o conjunto filtrado inteiro (D3/D9): bySegment, byDeliveryType, byStage, byProviderModel, byStatus, byStore, byOwner (stores.user_id), byHour â€” a UI nunca calcula KPIs"
    - "getRunDetail wire o RPC admin_get_ai_operation_run_events e deriva estimatedCostBrl + badges + textComponentUsd/imageToolComponentUsd por evento (D4); run null + events [] para id inexistente"
    - "storeName e owner (stores.user_id) resolvidos no service (D3/D9)"
    - "19 testes do service (12.4 parcial + 12.5 + aggregations + getRunDetail)"
  artifacts:
    - path: "src/lib/ai-cost/operation-runs-service.ts"
      provides: "OperationRunsService â€” BRL/receita/margem, badges, segmentaÃ§Ã£o, storeName/owner"
      exports: ["OperationRunsService", "OperationRunsUnavailableError"]
    - path: "src/lib/ai-cost/__tests__/operation-runs-service.test.ts"
      provides: "Testes de derivaÃ§Ã£o (BRL/badges/segmento)"
      contains: "classifySegment"
  key_links:
    - from: "src/lib/ai-cost/operation-runs-service.ts"
      to: "public.admin_get_ai_operation_runs (RPC)"
      via: "rpc('admin_get_ai_operation_runs', {...}) â€” leitura de apuraÃ§Ã£o por entrega"
      pattern: "admin_get_ai_operation_runs"
    - from: "src/lib/ai-cost/operation-runs-service.ts"
      to: "src/lib/economic/economic-parameter-service.ts"
      via: "EconomicParameterService.getParameter para usd_brl_rate/credit_value_brl (D1/D2)"
      pattern: "EconomicParameterService"
    - from: "src/lib/ai-cost/operation-runs-service.ts"
      to: "src/lib/ai-cost/types.ts (CostResolution/costSource)"
      via: "badges derivados de cost_source + cost_estimation_note (D5)"
      pattern: "costEstimationNote"
---

<objective>
Criar o service server-only de Custos de OperaÃ§Ã£o â€” `src/lib/ai-cost/operation-runs-service.ts` (tarefa 6, grupo 6 da F38.2): chama os RPCs `admin_get_ai_operation_runs`/`admin_get_ai_operation_run_events` e deriva **no service layer** (D1/D4/D5/D9):
- **MonetÃ¡rio em BRL** (D1/D4): `custoBrl = custoUsdTotal Ã— usd_brl_rate`, `receitaOpBrl = creditosDebitados Ã— credit_value_brl`, `resultadoOpBrl = receitaOpBrl âˆ’ custoBrl`, `margemOpPct = receitaOpBrl > 0 ? (resultadoOpBrl / receitaOpBrl) Ã— 100 : null` â€” via `EconomicParameterService`
- **Badges de confianÃ§a** (D5): por evento e por entrega a partir dos insumos agregados do RPC
- **SegmentaÃ§Ã£o econÃ´mica** (D9): classificador `classifySegment` best-effort (test/freemium/promotional/paid/manual/admin/unknown) + filtro de segmento e re-paginaÃ§Ã£o no service
- **Aggregations** (D3/D9): `bySegment`/`byDeliveryType`/`byStage`/`byProviderModel`/`byStatus`/`byStore`/`byOwner`/`byHour` sobre o conjunto filtrado inteiro
- **getRunDetail** (D4): wire do RPC `admin_get_ai_operation_run_events` + `estimatedCostBrl`/badges/componentes por evento
- **storeName + owner** (D3/D9) resolvidos para a listagem/agregados

Purpose: este Ã© o coraÃ§Ã£o de derivaÃ§Ã£o da fase â€” a API (38-2-06) e a UI (38-2-08) consomem os nÃºmeros jÃ¡ convertidos, classificados e agregados; o SQL/RPCs nunca derivam BRL nem segmento (D1/D9).
Output: service testado (BRL/badges/segmento/aggregations/detalhe) â€” base da API de runs.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38.2-admin-custos-operacionais/38-2-CONTEXT.md
@openspec/changes/fase-38-2-admin-custos-operacionais/specs/ai-operation-runs-api/spec.md
@openspec/changes/fase-38-2-admin-custos-operacionais/specs/ai-operation-costs/spec.md

<interfaces>
Contratos que o executor precisa (extraÃ­dos dos specs D4/D5/D9 + 38-2-CONTEXT.md + leitura dos arquivos):

**Sem UI (HAS_UI: 0). Nenhuma pÃ¡gina/rota de UI Ã© alterada por este plano.**

Estrutura do service (analog: `src/lib/ai-cost/admin-service.ts` â€” server-only, client injectable, erro fail-closed; `src/lib/economic/economic-parameter-service.ts` â€” 38-2-02):
```typescript
import "server-only";
import type { SupabaseClient } from "@supabase/supabase-js";
import { supabaseAdmin } from "@/lib/supabase/server";
export class OperationRunsUnavailableError extends Error { ... }  // fail-closed â†’ 503
export class OperationRunsService {
  constructor(
    private readonly client: SupabaseClient = supabaseAdmin,
    private readonly economic: EconomicParameterService = new EconomicParameterService(supabaseAdmin),
  ) {}
  async listRuns(filters: OperationRunsFilters): Promise<OperationRunsListResult>;  // { runs, summary, aggregations, page, total }
  async getRunDetail(operationRunId: string): Promise<OperationRunDetail>;          // { run, events } com BRL/badges por evento
}
```

**DerivaÃ§Ã£o monetÃ¡ria (D1/D4):**
```
custoBrl      = custoUsdTotal Ã— usd_brl_rate
receitaOpBrl  = creditosDebitados Ã— credit_value_brl
resultadoOpBrl = receitaOpBrl âˆ’ custoBrl
margemOpPct   = receitaOpBrl > 0 ? (resultadoOpBrl / receitaOpBrl) Ã— 100 : null
```
- ParÃ¢metros via `economic.getParameter("usd_brl_rate")` / `getParameter("credit_value_brl")` (38-2-02 â€” fail-open 1.00 / fail-closed lanÃ§a â†’ service propaga â†’ API 503).

**Badges (D5) â€” mapa por evento (detalhe):**
| Badge | CondiÃ§Ã£o |
|-------|----------|
| provider_reported | cost_source = 'provider_reported' |
| provisional image tool estimate | cost_source = 'pricing_table' E cost_estimation_note = 'provisional_image_tool_unit_cost_until_provider_reconciliation' |
| partial | cost_source = 'manual_unknown' OU pricing_table com nota de estimativa parcial (ex.: responses_image_generation_tool_without_unit_pricing) |
| estimated | cost_source = 'pricing_table' (sem nota) ou fallback_static |
| not_available | cost_source = 'not_available' |
| estimated (genÃ©rico) | cost_source presente mas nota NULL |

**Badge por entrega (lista)** â€” a partir dos insumos agregados do RPC (`cost_sources[]`, `cost_estimation_notes[]`, flags `has_*`): prioridade `has_provider_reported` â†’ `provider_reported`; senÃ£o `has_provisional_image_estimate` â†’ provisional image tool estimate; senÃ£o `has_partial_estimate` â†’ partial; senÃ£o `has_not_available` â†’ not_available; senÃ£o `has_estimated` â†’ estimated; senÃ£o estimated genÃ©rico.

**SegmentaÃ§Ã£o (D9) â€” classifySegment(evidÃªncias, store):**
| Segmento | CritÃ©rio (best-effort) |
|----------|------------------------|
| test | store_is_test = true |
| freemium/promotional | deduction_bonus_amount > 0 E deduction_purchased_amount = 0 |
| paid | deduction_purchased_amount > 0 |
| manual/admin | admin_grant_evidence presente (shape real confirmado em credit_transactions â€” type/metadata.reason/metadata.source; sem evidÃªncia confiÃ¡vel â†’ unknown, nunca inferir errado) |
| unknown | fallback |
- `paid`/`unknown` exibem indicador de baixa confianÃ§a quando derivados sem origem clara.
- SegmentaÃ§Ã£o **nunca no RPC** â€” service layer apenas.

**storeName/owner:** JOIN em stores (store_id â†’ name, user_id como owner) â€” resolvido no service para listagem e agregados byOwner.

PadrÃ£o de teste (analog: `src/lib/ai-cost/__tests__/tracker.test.ts` / `src/lib/credit/__tests__/operation-cost-service.test.ts`): vi.mock supabase/server + client injetado + mock do economic service injetado.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DerivaÃ§Ã£o monetÃ¡ria BRL â€” custoBrl/receitaOpBrl/resultadoOpBrl/margemOpPct (D1/D4)</name>
  <files>src/lib/ai-cost/operation-runs-service.ts</files>
  <read_first>
    - src/lib/ai-cost/admin-service.ts (padrÃ£o service server-only + client injectable + toNumber â€” linhas 1-20, 72-98, 171-198)
    - src/lib/economic/economic-parameter-service.ts (EconomicParameterService â€” 38-2-02)
    - openspec/changes/fase-38-2-admin-custos-operacionais/specs/ai-operation-runs-api/spec.md (linhas 144-148 â€” derivaÃ§Ã£o BRL)
  </read_first>
  <behavior>
    - Teste 1: run com custoUsdTotal 10 e usd_brl_rate 5.0 â†’ custoBrl 50
    - Teste 2: run com creditosDebitados 20 e credit_value_brl 1.5 â†’ receitaOpBrl 30; resultadoOpBrl = 30 âˆ’ 50 = âˆ’20; margemOpPct = âˆ’66.67
    - Teste 3: receitaOpBrl 0 â†’ margemOpPct null (nÃ£o divide por zero)
    - Teste 4: parÃ¢metro com falha real â†’ OperationRunsUnavailableError (fail-closed propaga do EconomicParameterService)
  </behavior>
  <action>
    Criar `src/lib/ai-cost/operation-runs-service.ts` com:
    1. `import "server-only"` + `OperationRunsUnavailableError` (fail-closed â€” padrÃ£o AiCostAdminUnavailableError) + `OperationRunsService` (constructor com `client` e `economic: EconomicParameterService` injetÃ¡veis para teste).
    2. Helper `deriveBrl(run, params)`: aplica as fÃ³rmulas D1/D4; `margemOpPct` null quando receita <= 0. Helpers de normalizaÃ§Ã£o (toNumber â€” NUMERIC string|number do Postgres).
    3. MÃ©todo `listRuns(filters)`: chama `this.client.rpc("admin_get_ai_operation_runs", {...})` (11 params do RPC â€” p_page/p_page_size); mapeia os runs brutos â†’ tipos do service; resolve parÃ¢metros via `this.economic.getParameter(...)`; deriva BRL/receita/resultado/margem por run e no summary. Erro do RPC â†’ `OperationRunsUnavailableError` (503). Erro do economic (fail-closed) â†’ propaga.
    4. NÃƒO fazer arredondamento monetÃ¡rio arbitrÃ¡rio â€” manter precisÃ£o e arredondar na exibiÃ§Ã£o (UI) ou com toFixed(2) documentado. NÃƒO derivar no SQL â€” tudo aqui (D1).
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai-cost/__tests__/operation-runs-service.test.ts; npm run typecheck; `grep -c "server-only" src/lib/ai-cost/operation-runs-service.ts` == 1; `grep -c "admin_get_ai_operation_runs" src/lib/ai-cost/operation-runs-service.ts` >= 1; `grep -c "margemOpPct" src/lib/ai-cost/operation-runs-service.ts` >= 1; `grep -c "OperationRunsUnavailableError" src/lib/ai-cost/operation-runs-service.ts` >= 2</automated>
  </verify>
  <acceptance_criteria>
    - FÃ³rmulas D1/D4 implementadas no service (custoBrl/receitaOpBrl/resultadoOpBrl/margemOpPct; margem null quando receita 0)
    - listRuns chama o RPC novo; erro fail-closed â†’ 503
    - typecheck limpo
  </acceptance_criteria>
  <done>DerivaÃ§Ã£o monetÃ¡ria BRL implementada no service (D1/D4)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Badges de confianÃ§a â€” por evento e por entrega (D5)</name>
  <files>src/lib/ai-cost/operation-runs-service.ts</files>
  <read_first>
    - src/lib/ai-cost/types.ts (CostSource â€” 5 valores; CostResolution com costEstimationNote)
    - openspec/changes/fase-38-2-admin-custos-operacionais/specs/ai-operation-runs-api/spec.md (linhas 220-259 â€” derivaÃ§Ã£o de badges)
    - 38-2-CONTEXT.md D5 (tabela de badge)
  </read_first>
  <behavior>
    - Teste 5: evento cost_source provider_reported â†’ badge provider_reported
    - Teste 6: evento cost_source pricing_table + nota provisional_image_tool_unit_cost_until_provider_reconciliation â†’ badge provisional image tool estimate
    - Teste 7: evento cost_source manual_unknown â†’ badge partial; pricing_table sem nota â†’ estimated; not_available â†’ not_available
    - Teste 8: run com insumos has_provider_reported=true (mesmo com cost_sources misto) â†’ badge da entrega provider_reported (prioridade); sem flags â†’ estimated genÃ©rico
  </behavior>
  <action>
    Adicionar ao `OperationRunsService`:
    1. `deriveEventBadge(costSource, costEstimationNote): Badge` â€” mapa exato D5 (ordem de condiÃ§Ã£o da tabela).
    2. `deriveRunBadge(insumos): Badge` â€” prioridade das flags has_* do RPC (provider_reported > provisional image tool estimate > partial > not_available > estimated > estimated genÃ©rico), aplicando o mesmo mapa sobre a distribuiÃ§Ã£o de cost_sources/notes quando as flags nÃ£o bastarem.
    3. `export type CostBadge = "provider_reported" | "provisional image tool estimate" | "partial" | "estimated" | "not_available"`.
    4. Expor badge nos tipos de saÃ­da (run-level e event-level).
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai-cost/__tests__/operation-runs-service.test.ts; npm run typecheck; `grep -c "provisional_image_tool_unit_cost_until_provider_reconciliation" src/lib/ai-cost/operation-runs-service.ts` >= 1; `grep -c "deriveRunBadge" src/lib/ai-cost/operation-runs-service.ts` >= 1; `grep -c "deriveEventBadge" src/lib/ai-cost/operation-runs-service.ts` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - Badge por evento (mapa D5) e por entrega (prioridade das flags has_*)
    - Tipo CostBadge exportado
    - typecheck limpo
  </acceptance_criteria>
  <done>Badges de confianÃ§a derivados no service (D5)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SegmentaÃ§Ã£o econÃ´mica â€” classifySegment (D9) + storeName/owner</name>
  <files>src/lib/ai-cost/operation-runs-service.ts</files>
  <read_first>
    - 38-2-CONTEXT.md D9 (tabela de segmento + ressalva de precisÃ£o)
    - openspec/changes/fase-38-2-admin-custos-operacionais/specs/ai-operation-costs/spec.md (seÃ§Ã£o SegmentaÃ§Ã£o econÃ´mica)
    - .planning/phases/29-3-creditos-mensais-automaticos/29-3-CONTEXT.md (buckets bÃ´nus/compra â€” evidÃªncia em credit_transactions.metadata)
    - src/lib/credit/types.ts (shape de credit_transactions â€” metadata feature/bonus_amount/purchased_amount)
  </read_first>
  <behavior>
    - Teste 9: store_is_test true â†’ test
    - Teste 10: deduction_bonus_amount > 0 e purchased_amount = 0 â†’ freemium/promotional
    - Teste 11: deduction_purchased_amount > 0 â†’ paid
    - Teste 12: admin_grant_evidence presente (shape confirmado) â†’ manual/admin; shape divergente (sem evidÃªncia confiÃ¡vel) â†’ unknown (nunca inferir errado)
    - Teste 13: sem evidÃªncia â†’ unknown
  </behavior>
  <action>
    Adicionar ao `OperationRunsService`:
    1. `export type Segment = "test" | "freemium/promotional" | "paid" | "manual/admin" | "unknown"`.
    2. `classifySegment(evidÃªncias): { segment: Segment; confidence: "high" | "low" }` â€” critÃ©rios exatos D9 usando as evidÃªncias brutas do RPC (`store_is_test`, `deduction_bonus_amount`, `deduction_purchased_amount`, `admin_grant_evidence`). `paid`/`unknown` â†’ confidence "low"; demais "high".
    3. ConfirmaÃ§Ã£o do shape real do `admin_grant` em `credit_transactions` (ler o spec/tasks 6.3: `type`/`metadata.reason`/`metadata.source`; se divergente do contrato, usar o shape real confirmado OU cair em `unknown` â€” nunca inferir errado).
    4. `storeName`/`owner`: quando o RPC nÃ£o retorna, resolver via `this.client.from("stores").select("name, user_id").eq("id", storeId)` (service layer â€” D3/D9); exponha nos tipos de saÃ­da.
    5. **Filtro de segmento + re-paginaÃ§Ã£o NO SERVICE (D4/D9)**: `listRuns(filters)` classifica cada run do conjunto base (`classifySegment`) e, quando `filters.segment` presente, filtra ANTES de paginar â€” `total`, `page` e `summary`/`aggregations` refletem o conjunto **segmento-filtrado** (nunca o RPC paginado sem o filtro). **EstratÃ©gia de paginaÃ§Ã£o progressiva (obrigatÃ³ria):** como o RPC limita `page_size` a 100 (38-2-01), o service NÃƒO depende de um page_size Ãºnico "coberto" do perÃ­odo. Quando `segment` presente, o service faz paginaÃ§Ã£o progressiva: loop de requisiÃ§Ãµes ao RPC (`p_page = 1, 2, ...`, `p_page_size = 100`) atÃ© acumular todos os runs do perÃ­odo filtrado (`runs_acumulados >= summary.total` do RPC), respeitando o limite de janela (default â‰¤ 90d, max 365d); sobre o conjunto completo acumulado, aplica classificaÃ§Ã£o + filtro de segmento + paginaÃ§Ã£o final + `total`/`summary`/`aggregations` no service. NÃƒO delegar o filtro Ã  API â€” a API apenas repassa `segment` ao service.
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai-cost/__tests__/operation-runs-service.test.ts; npm run typecheck; `grep -c "classifySegment" src/lib/ai-cost/operation-runs-service.ts` >= 1; `grep -c "freemium/promotional" src/lib/ai-cost/operation-runs-service.ts` >= 1; `grep -c "unknown" src/lib/ai-cost/operation-runs-service.ts` >= 1; `grep -c "admin_grant" src/lib/ai-cost/operation-runs-service.ts` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - classifySegment implementa os 5 segmentos D9 com confidence; fallback unknown; nunca inferir admin_grant sem shape confiÃ¡vel
    - storeName/owner resolvidos no service
    - typecheck limpo
  </acceptance_criteria>
  <done>SegmentaÃ§Ã£o econÃ´mica + storeName/owner implementados no service (D9)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Aggregations â€” bySegment/byDeliveryType/byStage/byProviderModel/byStatus/byStore/byOwner/byHour (D3/D9)</name>
  <files>src/lib/ai-cost/operation-runs-service.ts</files>
  <read_first>
    - openspec/changes/fase-38-2-admin-custos-operacionais/specs/ai-operation-costs/spec.md (seÃ§Ã£o Agregados â€” D3/D9)
    - 38-2-CONTEXT.md D3 (bloco de agregados) + D9 (agregados por segmento)
    - src/lib/ai-cost/operation-runs-service.ts (listRuns â€” Tasks 1-3)
  </read_first>
  <behavior>
    - Teste 14: `listRuns` retorna `aggregations` com as 8 chaves (bySegment, byDeliveryType, byStage, byProviderModel, byStatus, byStore, byOwner, byHour) calculadas sobre o conjunto filtrado inteiro (nÃ£o a pÃ¡gina)
    - Teste 15: `bySegment` inclui custo, resultado operacional estimado, margem % e taxa de erro por segmento (D9); `byOwner` agrupa pelo dono da loja (stores.user_id)
    - Teste 16: `summary`/`aggregations` usam o conjunto filtrado antes da pÃ¡gina (com page=2 e 60 runs no conjunto â†’ refletem os 60)
  </behavior>
  <action>
    Adicionar ao `OperationRunsService` a derivaÃ§Ã£o de `aggregations`:
    1. `deriveAggregations(runs, params): Aggregations` â€” sobre o **conjunto filtrado inteiro** (antes da paginaÃ§Ã£o): `bySegment` (custo total, resultado operacional estimado BRL, margem % â€” mesma fÃ³rmula D1, taxa de erro = erros/entregas), `byDeliveryType` (operation_run_type), `byStage` (generation_type), `byProviderModel`, `byStatus`, `byStore` (com storeName), `byOwner` (via stores.user_id â€” dono da loja, nÃ£o executor), `byHour` (agrupamento por hora de created_at).
    2. `listRuns` monta o retorno final `{ runs, summary, aggregations, page, total }` usando o conjunto filtrado inteiro para summary/aggregations e a pÃ¡gina para `runs`.
    3. `export interface OperationRunsAggregations` (e sub-tipos) no service.
    4. NÃƒO calcular no client â€” a UI consome `aggregations` prontas (D3: "a UI nunca calcula KPIs").
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai-cost/__tests__/operation-runs-service.test.ts; npm run typecheck; `grep -c "bySegment" src/lib/ai-cost/operation-runs-service.ts` >= 1; `grep -c "byOwner" src/lib/ai-cost/operation-runs-service.ts` >= 1; `grep -c "byHour" src/lib/ai-cost/operation-runs-service.ts` >= 1; `grep -c "deriveAggregations" src/lib/ai-cost/operation-runs-service.ts` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - Aggregations com as 8 chaves sobre o conjunto filtrado inteiro
    - bySegment com custo/resultado/margem/taxa de erro (D9); byOwner via stores.user_id
    - summary/aggregations nunca sobre a pÃ¡gina
    - typecheck limpo
  </acceptance_criteria>
  <done>Aggregations derivadas no service (D3/D9) â€” prontas para a UI</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: getRunDetail â€” RPC admin_get_ai_operation_run_events + per-event BRL/badges (D4)</name>
  <files>src/lib/ai-cost/operation-runs-service.ts</files>
  <read_first>
    - openspec/changes/fase-38-2-admin-custos-operacionais/specs/ai-operation-runs-api/spec.md (linhas 67-90, 171-218 â€” RPC eventos + detalhe)
    - src/lib/ai-cost/operation-runs-service.ts (listRuns â€” Tasks 1-4)
  </read_first>
  <behavior>
    - Teste 17: `getRunDetail("uuid")` chama o RPC `admin_get_ai_operation_run_events` e retorna `{ run, events }` â€” run com o resumo (BRL derivado) e cada evento com `estimatedCostBrl = estimatedCostUsd Ã— usd_brl_rate`, `textComponentUsd`, `imageToolComponentUsd`, badges
    - Teste 18: RPC retorna run null + events [] quando id nÃ£o existe â†’ service repassa `{ run: null, events: [] }`
    - Teste 19: erro do RPC â†’ OperationRunsUnavailableError (fail-closed 503)
  </behavior>
  <action>
    Adicionar ao `OperationRunsService`:
    1. `async getRunDetail(operationRunId: string): Promise<OperationRunDetail>` â€” chama `this.client.rpc("admin_get_ai_operation_run_events", { p_operation_run_id: operationRunId })`; mapeia `{ run, events }`; resolve parÃ¢metros econÃ´micos e deriva por evento: `estimatedCostBrl = estimatedCostUsd Ã— usd_brl_rate`, componentes (textComponentUsd/imageToolComponentUsd repassados), badge via `deriveEventBadge(costSource, costEstimationNote)` (Task 2); run com badge de entrega via `deriveRunBadge` (se insumos disponÃ­veis).
    2. `export interface OperationRunDetail { run: ...; events: OperationRunEvent[] }` (+ tipo OperationRunEvent com os campos do contrato D4).
    3. Erro do RPC â†’ `OperationRunsUnavailableError` (fail-closed); id inexistente â†’ `{ run: null, events: [] }` (spec).
    4. NÃƒO derivar BRL no SQL â€” sempre no service (D1/D2).
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai-cost/__tests__/operation-runs-service.test.ts; npm run typecheck; `grep -c "getRunDetail" src/lib/ai-cost/operation-runs-service.ts` >= 1; `grep -c "admin_get_ai_operation_run_events" src/lib/ai-cost/operation-runs-service.ts` >= 1; `grep -c "estimatedCostBrl" src/lib/ai-cost/operation-runs-service.ts` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - getRunDetail wire o RPC de eventos + deriva BRL/badges/componentes por evento
    - run null + events [] para id inexistente; fail-closed 503 em erro
    - typecheck limpo
  </acceptance_criteria>
  <done>getRunDetail implementado (D4) â€” detalhe call-level pronto para o drilldown</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Testes completos (tarefa 12.5 segmento + derivaÃ§Ã£o BRL/badges + agregados/detalhe)</name>
  <files>src/lib/ai-cost/__tests__/operation-runs-service.test.ts</files>
  <read_first>
    - src/lib/ai-cost/__tests__/tracker.test.ts (padrÃ£o de mock â€” vi.mock supabase + client injetado)
    - src/lib/economic/__tests__/economic-parameter-service.test.ts (padrÃ£o de mock do economic)
  </read_first>
  <behavior>
    - Casos (tarefa 12.5 â€” 5 testes de segmento): test via is_test_store; freemium/promotional; paid; manual/admin com shape confirmado + unknown quando shape divergente; unknown sem evidÃªncia
    - Casos de derivaÃ§Ã£o BRL (12.4 parcial): custoBrl/receitaOpBrl/resultadoOpBrl/margemOpPct (margem null quando receita 0)
    - Casos de badges (12.3 parcial + 12.4): provider_reported; provisional; partial; estimated; not_available; por entrega com prioridade das flags
    - Casos de aggregations (Task 4): 8 chaves sobre o conjunto filtrado inteiro; bySegment com custo/resultado/margem/taxa de erro; byOwner via stores.user_id
    - Casos de getRunDetail (Task 5): RPC de eventos chamado; estimatedCostBrl derivado por evento; run null + events [] para id inexistente
  </behavior>
  <action>
    Criar `src/lib/ai-cost/__tests__/operation-runs-service.test.ts`:
    - PadrÃ£o: vi.mock supabase/server + cliente mock injetado + `economic` mock injetado (ex.: `{ getParameter: vi.fn() }` com resolvers por chave) â€” NÃƒO mockar o service econÃ´mico real em si, injetar fake.
    - Casos: derivaÃ§Ã£o BRL (tests 1-4), badges (tests 5-8), segmento (tests 9-13), aggregations (tests 14-16), getRunDetail (tests 17-19). Cobrir margem null com receita 0; fail-closed propagando OperationRunsUnavailableError; summary/aggregations sobre conjunto filtrado inteiro (nÃ£o a pÃ¡gina).
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai-cost/__tests__/operation-runs-service.test.ts; npm run typecheck; `grep -c "classifySegment" src/lib/ai-cost/__tests__/operation-runs-service.test.ts` >= 1; `grep -c "freemium" src/lib/ai-cost/__tests__/operation-runs-service.test.ts` >= 1; `grep -c "provisional" src/lib/ai-cost/__tests__/operation-runs-service.test.ts` >= 1; `grep -c "bySegment" src/lib/ai-cost/__tests__/operation-runs-service.test.ts` >= 1; `grep -c "estimatedCostBrl" src/lib/ai-cost/__tests__/operation-runs-service.test.ts` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - 19 testes passando (npx vitest run â€” 0 falhas)
    - Cobertura de BRL/badges/segmento (12.4 parcial + 12.5) + aggregations + getRunDetail
    - typecheck limpo
  </acceptance_criteria>
  <done>Service completo testado (BRL/badges/segmento/aggregations/detalhe) â€” pronto para a API de runs</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OperationRunsService â†’ RPCs admin_get_ai_operation_runs/_events | Leitura via service_role (server-only); Ãºnica via de acesso Ã  apuraÃ§Ã£o por entrega |
| OperationRunsService â†’ EconomicParameterService | ParÃ¢metros econÃ´micos (fail-open 1.00 / fail-closed 503) |
| OperationRunsService â†’ stores | storeName/owner via service_role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38.2-19 | Spoofing | Badge derivado incorretamente | mitigate | Mapa determinÃ­stico D5 (deriveEventBadge/deriveRunBadge) com prioridade documentada; insumos persistidos pelo tracker (38-2-03) â€” F38.2-12 |
| T-38.2-20 | Spoofing | Segmento inferido errado | mitigate | classifySegment best-effort D9: critÃ©rios exatos, fallback unknown, NUNCA inferir admin_grant sem shape confiÃ¡vel; confianÃ§a high/low â€” F38.2-14 |
| T-38.2-21 | Tampering | DerivaÃ§Ã£o monetÃ¡ria incorreta | mitigate | FÃ³rmulas D1/D4 centralizadas no service (uma fonte); margem null quando receita <= 0 (sem divisÃ£o por zero); NUMERIC normalizado via toNumber â€” F38.2-12 |
| T-38.2-22 | DoS | Falha de leitura dos parÃ¢metros/RPCs | mitigate | Fail-closed: erro real â†’ OperationRunsUnavailableError â†’ 503; fail-open apenas para linha de parÃ¢metro inexistente (1.00) â€” F38.2-14 |
| T-38.2-23 | Information Disclosure | Custo/receita/margem internos | mitigate | Service server-only; consumo via API admin (requireAdmin â€” 38-2-06) â€” F38.2-12 |
| T-38.2-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado neste plano â€” gate de legitimidade nÃ£o se aplica |
</threat_model>

<verification>
- `npm run typecheck` limpo (service + tipos)
- `npx vitest run src/lib/ai-cost/__tests__/operation-runs-service.test.ts` â€” 19 testes, 0 falhas
- DerivaÃ§Ãµes centralizadas no service (sem BRL/segmento no SQL); aggregations/getRunDetail produzidos no service
</verification>

<success_criteria>
- DerivaÃ§Ã£o BRL (custoBrl/receitaOpBrl/resultadoOpBrl/margemOpPct) no service (D1/D4)
- Badges por evento e por entrega (D5)
- SegmentaÃ§Ã£o econÃ´mica classifySegment (D9) com confidence; storeName/owner resolvidos; filtro de segmento + re-paginaÃ§Ã£o no service
- Aggregations (8 chaves) sobre o conjunto filtrado inteiro (D3/D9)
- getRunDetail wire RPC de eventos + BRL/badges por evento (D4)
- Testes verdes (12.4 parcial + 12.5 + agregados/detalhe)
</success_criteria>

<output>
Create `.planning/phases/38.2-admin-custos-operacionais/38-2-05-SUMMARY.md` when done
</output>

---
phase: 38.2-admin-custos-operacionais
plan: 06
type: execute
wave: 4
depends_on:
  - "38-2-05"
files_modified:
  - "src/lib/admin/schemas.ts"
  - "src/app/api/admin/ai-operation-runs/route.ts"
  - "src/app/api/admin/ai-operation-runs/[operationRunId]/route.ts"
  - "src/app/api/admin/ai-operation-runs/__tests__/route.test.ts"
  - "src/app/api/admin/ai-operation-runs/[operationRunId]/__tests__/route.test.ts"
autonomous: true
HAS_UI: 0
requirements:
  - F38.2-10
  - F38.2-11
must_haves:
  truths:
    - "AiOperationRunsQuerySchema em src/lib/admin/schemas.ts com period_start/period_end, store_id, operation_run_type, status, provider, model, generation_type, operation_run_id, segment, page, page_size e validaÃ§Ã£o de janela (default â‰¤ 90d, max 365 â†’ 400)"
    - "GET /api/admin/ai-operation-runs â†’ 200 { runs, summary, aggregations, page, total }; segmento classificado/filtrado no service ANTES de paginar (total reflete conjunto segmento-filtrado); summary/aggregations sobre conjunto filtrado inteiro; badges dos insumos; 400 zod (janela excedente); 403; 503"
    - "GET /api/admin/ai-operation-runs/[operationRunId] â†’ 200 { run, events } com estimatedCostBrl derivado no service + badges por evento; 400 (uuid invÃ¡lido); 403; 503"
    - "11 testes da API (12.4): resumo; filtros; paginaÃ§Ã£o; summary/aggregations conjunto filtrado inteiro; segmento filtra antes de paginar; janela excedente â†’ 400; detalhe com textComponentUsd/imageToolComponentUsd; estimated_cost_brl no service; receita/resultado/margem BRL com margem null quando receita 0; badges insumos; P95; 403/503"
  artifacts:
    - path: "src/lib/admin/schemas.ts"
      provides: "AiOperationRunsQuerySchema (D4)"
      contains: "AiOperationRunsQuerySchema"
    - path: "src/app/api/admin/ai-operation-runs/route.ts"
      provides: "GET lista de entregas (D4/D9)"
      exports: ["GET"]
    - path: "src/app/api/admin/ai-operation-runs/[operationRunId]/route.ts"
      provides: "GET detalhe call-level (D4)"
      exports: ["GET"]
    - path: "src/app/api/admin/ai-operation-runs/__tests__/route.test.ts"
      provides: "Testes da lista (12.4)"
      contains: "ai-operation-runs"
    - path: "src/app/api/admin/ai-operation-runs/[operationRunId]/__tests__/route.test.ts"
      provides: "Testes do detalhe (12.4)"
      contains: "operationRunId"
  key_links:
    - from: "src/app/api/admin/ai-operation-runs/route.ts"
      to: "src/lib/ai-cost/operation-runs-service.ts"
      via: "OperationRunsService.listRuns (BRL/badges/segmento no service)"
      pattern: "OperationRunsService"
    - from: "src/app/api/admin/ai-operation-runs/[operationRunId]/route.ts"
      to: "src/lib/ai-cost/operation-runs-service.ts"
      via: "OperationRunsService.getRunDetail (estimatedCostBrl + badges)"
      pattern: "getRunDetail"
---

<objective>
Criar a API admin de Custos de OperaÃ§Ã£o (D4/D9): `GET /api/admin/ai-operation-runs` (lista de entregas com filtros + paginaÃ§Ã£o + segmento econÃ´mico + `summary`/`aggregations` sobre o conjunto filtrado inteiro) e `GET /api/admin/ai-operation-runs/[operationRunId]` (detalhe call-level com `estimatedCostBrl` e badges por evento). Schema `AiOperationRunsQuerySchema` em `src/lib/admin/schemas.ts` com validaÃ§Ã£o de janela (default â‰¤ 90d, max 365 â†’ 400). Testes (tarefa 12.4 â€” 11 testes).

Purpose: a UI /admin/ai-operation-costs (38-2-08) consome estes endpoints â€” a camada de apresentaÃ§Ã£o que faltou Ã  apuraÃ§Ã£o F38.1. A API nunca deriva BRL/segmento no SQL; tudo passa pelo service (38-2-05).
Output: API lista + detalhe testada + schema de query.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38.2-admin-custos-operacionais/38-2-CONTEXT.md
@openspec/changes/fase-38-2-admin-custos-operacionais/specs/ai-operation-runs-api/spec.md

<interfaces>
Contratos que o executor precisa (extraÃ­dos do spec ai-operation-runs-api D4/D9 + 38-2-CONTEXT.md):

**Sem UI (HAS_UI: 0). Nenhuma pÃ¡gina/rota de UI Ã© alterada por este plano.**

Schema (`src/lib/admin/schemas.ts`):
```typescript
export const AiOperationRunsQuerySchema = z.object({
  periodStart: z.string().datetime().optional(),    // period_start ISO
  periodEnd: z.string().datetime().optional(),       // period_end ISO
  storeId: z.string().uuid().optional(),
  operationRunType: z.string().optional(),
  status: z.string().optional(),
  provider: z.string().optional(),
  model: z.string().optional(),
  generationType: z.string().optional(),
  operationRunId: z.string().uuid().optional(),
  segment: z.enum(["test","freemium/promotional","paid","manual/admin","unknown"]).optional(),
  page: z.coerce.number().int().min(1).default(1),
  pageSize: z.coerce.number().int().min(1).max(100).default(25),
}).superRefine(validarJanela);   // default â‰¤ 90d; max 365d â†’ fail
```
- ValidaÃ§Ã£o de janela: se periodStart+periodEnd ausentes â†’ janela default 90 dias (sem 400); se presentes e intervalo > 365 dias â†’ 400 (zod custom) â€” `AiOperationRunsQuerySchema` deve capturar a condiÃ§Ã£o com mensagem clara.

GET lista (`src/app/api/admin/ai-operation-runs/route.ts`):
- `apiHandler` + `requireAdmin()` (padrÃ£o rota ai-costs).
- Query parseada com `AiOperationRunsQuerySchema` â†’ 400 zod `{ error: "Dados invÃ¡lidos", details }` (inclui janela excedente).
- `new OperationRunsService().listRuns({ ... })` â†’ 200 `{ runs, summary, aggregations, page, total }`.
- `OperationRunsUnavailableError` â†’ 503 `{ error: "operation_runs_unavailable" }`.
- **Segmento:** quando `segment` presente, o service classifica cada run e filtra ANTES de paginar â€” o `total` e a pÃ¡gina refletem o conjunto segmento-filtrado (nunca o RPC paginado sem o filtro). **EstratÃ©gia de paginaÃ§Ã£o progressiva no service:** como o RPC limita `page_size` a 100, o service faz loop de requisiÃ§Ãµes ao RPC (`p_page = 1, 2, ...`, `p_page_size = 100`) atÃ© acumular todos os runs do perÃ­odo filtrado (`acumulado >= summary.total`), respeitando o limite de janela (default â‰¤ 90d, max 365d â†’ 400); sobre o conjunto completo acumulado, aplica classificaÃ§Ã£o + filtro de segmento + paginaÃ§Ã£o final + `total` no service.
- **summary/aggregations** derivados no service sobre o conjunto filtrado inteiro (nunca sobre a pÃ¡gina) â€” a UI nÃ£o recalcula KPIs.
- `aggregations` shape (D3/D9): `bySegment`, `byDeliveryType`, `byStage`, `byProviderModel`, `byStatus`, `byStore`, `byOwner` (stores.user_id), `byHour`.

GET detalhe (`src/app/api/admin/ai-operation-runs/[operationRunId]/route.ts`):
- `apiHandler` + `requireAdmin()`.
- `operationRunId` validado como uuid â†’ 400 zod `{ error: "Dados invÃ¡lidos" }` se invÃ¡lido.
- `new OperationRunsService().getRunDetail(operationRunId)` â†’ 200 `{ run, events }` â€” events com `estimatedCostBrl`, `textComponentUsd`, `imageToolComponentUsd`, `badges`.
- 503 fail-closed.

PadrÃ£o de teste (analog: `src/app/api/admin/ai-costs/__tests__/route.test.ts`): mock requireAdmin + mock do service (vi.mock) + fetch ao handler.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema AiOperationRunsQuerySchema com validaÃ§Ã£o de janela (D4)</name>
  <files>src/lib/admin/schemas.ts</files>
  <read_first>
    - src/lib/admin/schemas.ts (padrÃ£o AiCostsQuerySchema â€” linhas 78-87; zod refine no UpdateOperationCost â€” linhas 19-25)
    - openspec/changes/fase-38-2-admin-custos-operacionais/specs/ai-operation-runs-api/spec.md (linhas 154-159 â€” janela)
  </read_first>
  <behavior>
    - Teste 1 (compile-time): schema aceita `{ page: 2, pageSize: 25 }` (sem perÃ­odo â†’ janela default 90d)
    - Teste 2 (compile-time): schema REJEITA perÃ­odo de > 365 dias (400 via refine); aceita perÃ­odo de 30 dias; aceita `segment: "test"`; rejeita `segment: "invalid"`
  </behavior>
  <action>
    Adicionar `AiOperationRunsQuerySchema` em `src/lib/admin/schemas.ts` no estilo dos schemas existentes. Campos em camelCase (a rota converte de snake_case query). `.superRefine`/`.refine` validando a janela: quando `periodStart` E `periodEnd` presentes â†’ diff â‰¤ 365 dias (senÃ£o fail com mensagem "janela de perÃ­odo mÃ¡xima de 365 dias"); quando ausentes â†’ OK (default 90 dias aplicado no service). Importar `SEGMENTS`/tipo do service se necessÃ¡rio (ou declarar enum local). NÃƒO alterar schemas existentes.
  </action>
  <verify>
    <automated>npm run typecheck; `grep -c "AiOperationRunsQuerySchema" src/lib/admin/schemas.ts` >= 1; `grep -c "segment" src/lib/admin/schemas.ts` >= 1; `grep -c "365" src/lib/admin/schemas.ts` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - Schema com todos os filtros + paginaÃ§Ã£o + validaÃ§Ã£o de janela (default 90d, max 365d â†’ 400)
    - Schemas existentes intactos; typecheck limpo
  </acceptance_criteria>
  <done>Schema de query de runs criado (D4)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GET /api/admin/ai-operation-runs â€” lista (D4/D9)</name>
  <files>src/app/api/admin/ai-operation-runs/route.ts</files>
  <read_first>
    - src/app/api/admin/ai-costs/route.ts (padrÃ£o apiHandler + requireAdmin + zod + 503 â€” linhas 1-57)
    - src/lib/ai-cost/operation-runs-service.ts (listRuns â€” 38-2-05)
  </read_first>
  <behavior>
    - Teste 1 (route): GET com query vÃ¡lida â†’ 200 `{ runs, summary, aggregations, page, total }` (service mockado)
    - Teste 2 (route): GET com janela > 365d â†’ 400 (zod); GET com uuid invÃ¡lido em store_id â†’ 400
    - Teste 3 (route): GET sem admin â†’ 403
    - Teste 4 (route): service lanÃ§a OperationRunsUnavailableError â†’ 503
    - Teste 5 (route): GET com segment=test â†’ service recebe segment e aplica filtro antes de paginar (total consistente)
  </behavior>
  <action>
    Criar `src/app/api/admin/ai-operation-runs/route.ts`:
    1. `export const GET = apiHandler(async (request: Request) => { await requireAdmin(); ... })` (padrÃ£o rota ai-costs).
    2. Converter query params de snake_case â†’ camelCase e parsear com `AiOperationRunsQuerySchema`; `!parsed.success` â†’ 400 `{ error: "Dados invÃ¡lidos", details }`.
    3. `const result = await new OperationRunsService().listRuns({ periodStart, periodEnd, storeId, operationRunType, status, provider, model, generationType, operationRunId, segment, page, pageSize });` â†’ `NextResponse.json(result)` (200).
    4. try/catch: `OperationRunsUnavailableError` â†’ 503 `{ error: "operation_runs_unavailable" }`; demais â†’ rethrow.
    5. NÃƒO derivar BRL/segmento aqui â€” o service jÃ¡ deriva; a rota sÃ³ repassa.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/admin/ai-operation-runs/__tests__/route.test.ts; `grep -c "requireAdmin" src/app/api/admin/ai-operation-runs/route.ts` >= 1; `grep -c "OperationRunsService" src/app/api/admin/ai-operation-runs/route.ts` >= 1; `grep -c "503" src/app/api/admin/ai-operation-runs/route.ts` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - GET 200 com runs/summary/aggregations/page/total; 400 (zod + janela); 403; 503
    - Segmento filtrado antes de paginar no service (total consistente)
  </acceptance_criteria>
  <done>Lista implementada (200/400/403/503)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GET /api/admin/ai-operation-runs/[operationRunId] â€” detalhe (D4)</name>
  <files>src/app/api/admin/ai-operation-runs/[operationRunId]/route.ts</files>
  <read_first>
    - src/app/api/admin/ai-costs/route.ts (padrÃ£o de rota admin)
    - src/lib/ai-cost/operation-runs-service.ts (getRunDetail â€” 38-2-05)
    - openspec/changes/fase-38-2-admin-custos-operacionais/specs/ai-operation-runs-api/spec.md (linhas 171-218 â€” detalhe)
  </read_first>
  <behavior>
    - Teste 6 (route): GET /[operationRunId] com uuid vÃ¡lido â†’ 200 `{ run, events }` (events com estimatedCostBrl + textComponentUsd + imageToolComponentUsd + badges)
    - Teste 7 (route): GET /[invalid] â†’ 400 (uuid invÃ¡lido)
    - Teste 8 (route): GET sem admin â†’ 403
    - Teste 9 (route): service lanÃ§a OperationRunsUnavailableError â†’ 503
  </behavior>
  <action>
    Criar `src/app/api/admin/ai-operation-runs/[operationRunId]/route.ts`:
    1. `export const GET = apiHandler(async (request: Request, { params }: { params: Promise<{ operationRunId: string }> }) => { await requireAdmin(); ... })` (padrÃ£o Next 15 de params promise â€” verificar como outras rotas com segmento dinÃ¢mico fazem).
    2. Validar `operationRunId` com `z.string().uuid()` (ou o schema local) â†’ 400 `{ error: "Dados invÃ¡lidos", details }` se invÃ¡lido.
    3. `const detail = await new OperationRunsService().getRunDetail(operationRunId);` â†’ `NextResponse.json(detail)` (200 `{ run, events }`).
    4. try/catch: `OperationRunsUnavailableError` â†’ 503 `{ error: "operation_runs_unavailable" }`.
    5. NÃƒO derivar BRL/badge aqui â€” o service jÃ¡ deriva.
  </action>
  <verify>
    <automated>npx vitest run "src/app/api/admin/ai-operation-runs/[operationRunId]/__tests__/route.test.ts"; `grep -c "getRunDetail" src/app/api/admin/ai-operation-runs/[operationRunId]/route.ts` >= 1; `grep -c "uuid" src/app/api/admin/ai-operation-runs/[operationRunId]/route.ts` >= 1; `grep -c "503" src/app/api/admin/ai-operation-runs/[operationRunId]/route.ts` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - GET detalhe 200 { run, events } com BRL/badges/componentes; 400 (uuid); 403; 503
  </acceptance_criteria>
  <done>Detalhe call-level implementado (200/400/403/503)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Testes completos da API (tarefa 12.4 â€” 11 testes)</name>
  <files>src/app/api/admin/ai-operation-runs/__tests__/route.test.ts, src/app/api/admin/ai-operation-runs/[operationRunId]/__tests__/route.test.ts</files>
  <read_first>
    - src/app/api/admin/ai-costs/__tests__/route.test.ts (padrÃ£o de teste de rota admin)
  </read_first>
  <behavior>
    - Casos (tarefa 12.4 â€” 11 testes, distribuÃ­dos entre lista e detalhe):
      1. Resumo por run (data/status/custo/crÃ©ditos/duraÃ§Ã£o/chamadas/regeneraÃ§Ãµes/provider/model/costSource/badges)
      2. Filtros respeitados (perÃ­odo, loja, tipo, status, provider, model, gen_type, run_id)
      3. PaginaÃ§Ã£o com page/total
      4. summary/aggregations sobre o conjunto filtrado inteiro (nÃ£o a pÃ¡gina)
      5. Segmento filtra antes de paginar com total consistente (ex.: 60 runs base, 12 de teste â†’ total 12)
      6. Janela excedente â†’ 400; datas ausentes â†’ janela default (200)
      7. Detalhe call-level com textComponentUsd/imageToolComponentUsd
      8. estimatedCostBrl derivado no service (detalhe)
      9. Receita/resultado/margem BRL com margem null quando receita 0
      10. Badges derivados dos insumos agregados por run
      11. 403 sem admin; 503 fail-closed
  </behavior>
  <action>
    Criar os dois arquivos de teste (padrÃ£o: mock requireAdmin + mock do OperationRunsService via vi.mock + fetch ao handler GET). Cobrir os 11 casos da behavior list. O mock do service retorna fixtures com os campos esperados (runs, summary, aggregations, page, total / run, events).
  </action>
  <verify>
    <automated>npx vitest run "src/app/api/admin/ai-operation-runs/__tests__/route.test.ts" "src/app/api/admin/ai-operation-runs/[operationRunId]/__tests__/route.test.ts"; npm run typecheck; `grep -c "aggregations" src/app/api/admin/ai-operation-runs/__tests__/route.test.ts` >= 1; `grep -c "estimatedCostBrl" "src/app/api/admin/ai-operation-runs/[operationRunId]/__tests__/route.test.ts"` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - 11 testes passando (npx vitest run â€” 0 falhas)
    - Cobertura dos cenÃ¡rios D4/D9 da behavior list
    - typecheck limpo
  </acceptance_criteria>
  <done>API de runs completa e testada (tarefa 12.4) â€” pronta para a UI</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GET /api/admin/ai-operation-runs* â† admin autenticado | Leitura de apuraÃ§Ã£o de custos/receita/margem interna â€” nunca exposta publicamente |
| OperationRunsService â†’ RPCs admin_get_ai_operation_runs/_events | Ãšnica via de acesso Ã  apuraÃ§Ã£o (service_role, server-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38.2-24 | Elevation of Privilege | GET sem admin | mitigate | `requireAdmin()` nas duas rotas; RPCs REVOKE PUBLIC/anon/authenticated (38-2-01) â€” F38.2-10/F38.2-11 |
| T-38.2-25 | DoS | Janela de perÃ­odo sem limite | mitigate | zod: janela max 365d â†’ 400; default 90d; paginaÃ§Ã£o page_size max 100; RPC com RAISE window_exceeded (38-2-01) â€” F38.2-10 |
| T-38.2-26 | Tampering | Segmento/PaginaÃ§Ã£o inconsistentes | mitigate | Filtro de segmento ANTES de paginar no service (total reflete conjunto segmento-filtrado); summary/aggregations sobre conjunto filtrado inteiro â€” F38.2-10 |
| T-38.2-27 | Injection | Filtros de query | mitigate | Zod valida tipos/uuid/enum; RPC usa parÃ¢metros vinculados (p_*) sem dynamic SQL (38-2-01) â€” F38.2-10 |
| T-38.2-28 | Information Disclosure | operationRunId de outra entrega | mitigate | Rota admin-only (requireAdmin); RPC definer; sem exposiÃ§Ã£o pÃºblica â€” F38.2-11 |
| T-38.2-29 | DoS | Falha de leitura | mitigate | Fail-closed: OperationRunsUnavailableError â†’ 503 (38-2-05) â€” F38.2-10 |
| T-38.2-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado neste plano â€” gate de legitimidade nÃ£o se aplica |
</threat_model>

<verification>
- `npm run typecheck` limpo (schema + rotas)
- `npx vitest run` nas duas rotas de teste â€” 11 testes, 0 falhas
- GET lista 200/400/403/503 + GET detalhe 200/400/403/503; segmento/paginaÃ§Ã£o/summary consistentes
</verification>

<success_criteria>
- AiOperationRunsQuerySchema com validaÃ§Ã£o de janela (default 90d, max 365d â†’ 400)
- GET lista com runs/summary/aggregations/page/total; segmento filtrado antes de paginar
- GET detalhe com estimatedCostBrl + componentes + badges
- 11 testes verdes (tarefa 12.4)
</success_criteria>

<output>
Create `.planning/phases/38.2-admin-custos-operacionais/38-2-06-SUMMARY.md` when done
</output>

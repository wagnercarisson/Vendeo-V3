---
phase: 38.2-admin-custos-operacionais
plan: 09
type: execute
wave: 5
depends_on:
  - "38-2-02"
  - "38-2-05"
files_modified:
  - "src/lib/metrics/pipeline-metrics.ts"
  - "src/app/(app)/admin/metrics/page.tsx"
  - "src/lib/metrics/__tests__/pipeline-metrics.test.ts"
  - "src/app/(app)/admin/metrics/__tests__/page.test.tsx"
autonomous: true
HAS_UI: 1
requirements:
  - F38.2-19
  - F38.2-20
  - F38.2-21
  - F38.2-22
must_haves:
  truths:
    - "getAvgCost (src/lib/metrics/pipeline-metrics.ts) deixa de ler campaign_pipeline.estimated_cost_usd (NULL por desenho) e passa a apurar custo mÃ©dio de IA por entrega via apuraÃ§Ã£o call-level (admin_get_ai_costs by_operation_run â†’ mÃ©dia de custo_usd_total; ou o RPC de resumo admin_get_ai_operation_runs) (D6)"
    - "admin_get_metrics (F28) PERMANECE INALTERADO â€” a correÃ§Ã£o Ã© na camada de leitura do front (pipeline-metrics / pÃ¡gina) (D6)"
    - "Card do /admin/metrics renomeado para 'Custo MÃ©dio IA' (metrics/page.tsx buildCampaignCards)"
    - "ConversÃ£o USDâ†’BRL na pÃ¡gina de mÃ©tricas passa a usar economic_parameters.usd_brl_rate (fonte Ãºnica D2) via EconomicParameterService; VENDEO_USD_BRL_RATE deixa de ser a fonte ativa (default 1.00 do parÃ¢metro)"
    - "4 testes de mÃ©tricas (12.8): getAvgCost NÃƒO lÃª campaign_pipeline.estimated_cost_usd; card 'Custo MÃ©dio IA' exibe mÃ©dia por entrega; USDâ†’BRL usa economic_parameters.usd_brl_rate nÃ£o env; regressÃ£o demais cards"
  artifacts:
    - path: "src/lib/metrics/pipeline-metrics.ts"
      provides: "getAvgCost apurando via call-level (D6)"
      contains: "getAvgCost"
    - path: "src/app/(app)/admin/metrics/page.tsx"
      provides: "Card 'Custo MÃ©dio IA' + USDâ†’BRL via economic_parameters"
      contains: "Custo MÃ©dio IA"
    - path: "src/lib/metrics/__tests__/pipeline-metrics.test.ts"
      provides: "Testes do getAvgCost (12.8)"
      contains: "getAvgCost"
    - path: "src/app/(app)/admin/metrics/__tests__/page.test.tsx"
      provides: "Testes da pÃ¡gina (12.8)"
      contains: "Custo MÃ©dio IA"
  key_links:
    - from: "src/lib/metrics/pipeline-metrics.ts (getAvgCost)"
      to: "public.admin_get_ai_costs (RPC F38.1)"
      via: "by_operation_run â†’ mÃ©dia de custo_usd_total (apuraÃ§Ã£o call-level)"
      pattern: "admin_get_ai_costs"
    - from: "src/app/(app)/admin/metrics/page.tsx"
      to: "src/lib/economic/economic-parameter-service.ts"
      via: "EconomicParameterService.getParameter('usd_brl_rate') â€” fonte Ãºnica de conversÃ£o (D2)"
      pattern: "EconomicParameterService"
---

<objective>
Corrigir o `/admin/metrics` (D6 â€” obrigatÃ³rio, consequÃªncia da nova contabilidade): `getAvgCost` deixa de ler `campaign_pipeline.estimated_cost_usd` (NULL por desenho desde a F38.1 â€” anti-dupla-contagem D1/D6) e passa a apurar **custo mÃ©dio de IA por entrega** via apuraÃ§Ã£o call-level; o card Ã© renomeado para "**Custo MÃ©dio IA**"; a conversÃ£o USDâ†’BRL passa a usar `economic_parameters.usd_brl_rate` (fonte Ãºnica D2) via `EconomicParameterService`. `admin_get_metrics` (F28) **inalterado** â€” correÃ§Ã£o na camada de leitura. Testes (tarefa 12.8 â€” 4 testes).

Purpose: o card "Custo MÃ©dio" mostrava NULL apÃ³s a F38.1 zerar o custo do delivery marker â€” a mÃ©trica antiga refletia a nova contabilidade de forma quebrada. A correÃ§Ã£o alinha a mÃ©trica Ã  apuraÃ§Ã£o call-level e Ã  fonte Ãºnica de cÃ¢mbio.
Output: /admin/metrics corrigido (card "Custo MÃ©dio IA" nÃ£o NULL) + testes verdes.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38.2-admin-custos-operacionais/38-2-CONTEXT.md
@openspec/changes/fase-38-2-admin-custos-operacionais/specs/admin-metrics-dashboard/spec.md
@openspec/changes/fase-38-2-admin-custos-operacionais/specs/pipeline-metrics/spec.md

<interfaces>
Contratos que o executor precisa (extraÃ­dos dos specs D6 + 38-2-CONTEXT.md + leitura dos arquivos):

**Sem pÃ¡gina nova (HAS_UI: 1) â€” Ã© uma correÃ§Ã£o na pÃ¡gina /admin/metrics EXISTENTE: a pÃ¡gina Ã© modificada (card renomeado + conversÃ£o USDâ†’BRL via EconomicParameterService), mas nenhuma rota/pÃ¡gina nova Ã© criada. A verificaÃ§Ã£o visual Ã© coberta pelo checkpoint da fase (38-2-10).**

Estado verificado:
- `src/lib/metrics/pipeline-metrics.ts:109-115` â€” `getAvgCost(hours, storeKind)` retorna `bundle.pipeline.avg_cost_ms ?? null` (que vem do RPC `admin_get_metrics` F28 â†’ lÃª `campaign_pipeline.estimated_cost_usd` â†’ NULL por desenho).
- `src/app/(app)/admin/metrics/page.tsx:27` â€” `const USD_BRL_RATE = Number(process.env.VENDEO_USD_BRL_RATE ?? "5.50");` (fonte antiga).
- `src/app/(app)/admin/metrics/page.tsx:81` â€” card `{ label: "Custo MÃ©dio", value: data.avgCost, timeRange }` em buildCampaignCards.

MudanÃ§as:
1. **`getAvgCost`** em `src/lib/metrics/pipeline-metrics.ts`:
   - NÃƒO usa mais `bundle.pipeline.avg_cost_ms`.
   - Passa a apurar custo mÃ©dio de IA **por entrega** via apuraÃ§Ã£o call-level: chama o RPC `admin_get_ai_costs` (`p_hours: hours`, `p_store_kind` equivalente se suportado â€” senÃ£o sem filtro de loja) e calcula `AVG(custo_usd_total)` sobre `by_operation_run` (cada run = entrega). OU usa o RPC novo `admin_get_ai_operation_runs` (D4) â€” `summary.tempo_medio` nÃ£o Ã© custo; usar `summary.custo_usd_total / summary.total` como mÃ©dia por entrega. DecisÃ£o de implementaÃ§Ã£o: preferir o RPC `admin_get_ai_operation_runs` (D4, mais alinhado â€” summary jÃ¡ Ã© sobre o conjunto filtrado) OU o `admin_get_ai_costs.by_operation_run` (F38.1 â€” disponÃ­vel sem dependÃªncia nova). Escolher conforme estabilidade; documentar no cÃ³digo.
   - Retorna `number | null` (mÃ©dia de custo por entrega em USD). O nome da mÃ©trica muda semanticamente â†’ card "Custo MÃ©dio IA".
   - MantÃ©m os demais getters (`getAvgDuration`, `getSuccessRate`, etc.) intactos â€” regressÃ£o zero nos outros cards.
   - `admin_get_metrics` (F28) NÃƒO Ã© alterado.

2. **Card + conversÃ£o** em `src/app/(app)/admin/metrics/page.tsx`:
   - Card `buildCampaignCards` â†’ label "**Custo MÃ©dio IA**".
   - `USD_BRL_RATE` deixa de vir de `VENDEO_USD_BRL_RATE` (env ativo); passa a vir de `EconomicParameterService.getParameter("usd_brl_rate")` (fonte Ãºnica D2) â€” default 1.00 quando fallback; env pode ficar apenas como fallback de bootstrap documentado (sem uso ativo).
   - Os demais cards continuam com o mesmo tratamento (usdToBrlRate repassado ao MetricsCards).

PadrÃ£o de teste (analog: `src/lib/metrics/__tests__/pipeline-metrics.test.ts` e `src/app/(app)/admin/metrics/__tests__/page.test.tsx` se existirem â€” verificar).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getAvgCost â€” apuraÃ§Ã£o call-level por entrega (D6)</name>
  <files>src/lib/metrics/pipeline-metrics.ts</files>
  <read_first>
    - src/lib/metrics/pipeline-metrics.ts (getAvgCost â€” linhas 109-115; fetchMetricsBundle â€” linhas 42-77; padrÃ£o RPC admin_get_metrics)
    - src/lib/ai-cost/admin-service.ts (padrÃ£o de chamada do RPC admin_get_ai_costs â€” linhas 174-198)
    - openspec/changes/fase-38-2-admin-custos-operacionais/specs/pipeline-metrics/spec.md
  </read_first>
  <behavior>
    - Teste 1 (unit â€” casos de getAvgCost): `getAvgCost` NÃƒO usa `bundle.pipeline.avg_cost_ms` (mock: admin_get_metrics retorna avg_cost_ms nÃ£o-nulo mas getAvgCost nÃ£o o lÃª) â€” chama `admin_get_ai_costs` (ou `admin_get_ai_operation_runs`) e calcula mÃ©dia por entrega; com by_operation_run = [{10},{20}] â†’ 15; com [] â†’ null; RPC falha â†’ null (degradaÃ§Ã£o suave)
  </behavior>
  <action>
    Modificar `getAvgCost` em `src/lib/metrics/pipeline-metrics.ts`:
    1. Remover o campo `avg_cost_ms` de `MetricsBundle.pipeline` (linha 11), do fallback do bundle (linha 67) e de todo uso em `getAvgCost` â€” a mÃ©trica antiga nÃ£o existe mais (grep `avg_cost_ms` == 0).
    2. Implementar a apuraÃ§Ã£o call-level por entrega: `supabaseAdmin.rpc("admin_get_ai_costs", { p_hours: hours, p_credit_unit_usd_value: null })` â†’ `by_operation_run` â†’ mÃ©dia de `custo_usd_total` (usar toNumber/clamp â€” padronizar com o ai-cost admin service). OU `supabaseAdmin.rpc("admin_get_ai_operation_runs", {...})` â†’ `summary.custo_usd_total / summary.total`. Escolher uma fonte e documentar.
    3. Manter a assinatura `getAvgCost(hours, storeKind)` e o fallback null em falha (degradaÃ§Ã£o suave â€” nÃ£o quebrar a pÃ¡gina).
    4. Atualizar o JSDoc: "custo mÃ©dio de IA por entrega (apuraÃ§Ã£o call-level, D6)".
    5. NÃƒO alterar `admin_get_metrics` (F28) nem os demais getters.
    6. NOTA (F38.2-20/F38.2-22 pre-existentes): HealthBanner/computeHealthState (health-banner.tsx) e MetricCard types (metrics/types.ts) JÃ EXISTEM e NÃƒO tÃªm delta na F38.2 â€” nÃ£o procurar trabalho neles; apenas garantir regressÃ£o zero (Task 3).
  </action>
  <verify>
    <automated>npx vitest run src/lib/metrics/__tests__/pipeline-metrics.test.ts; npm run typecheck; `grep -c "avg_cost_ms" src/lib/metrics/pipeline-metrics.ts` == 0; `grep -c "admin_get_ai_costs" src/lib/metrics/pipeline-metrics.ts` >= 1; `grep -c "admin_get_metrics" src/lib/metrics/pipeline-metrics.ts` >= 1</automated>
  </verify>
  <acceptance_criteria>
    - getAvgCost apura por entrega via call-level; nÃ£o usa mais avg_cost_ms do bundle
    - admin_get_metrics (F28) inalterado; demais getters intactos
    - typecheck limpo
  </acceptance_criteria>
  <done>getAvgCost corrigido â€” custo mÃ©dio de IA por entrega (D6)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Card 'Custo MÃ©dio IA' + USDâ†’BRL via economic_parameters (D6/D2)</name>
  <files>src/app/(app)/admin/metrics/page.tsx</files>
  <read_first>
    - src/app/(app)/admin/metrics/page.tsx (linhas 27, 76-86 buildCampaignCards, 130-134 fetchMetrics)
    - src/lib/economic/economic-parameter-service.ts (EconomicParameterService â€” 38-2-02)
  </read_first>
  <behavior>
    - Teste 2 (page): card com label "Custo MÃ©dio IA" e value = mÃ©dia por entrega
    - Teste 3 (page): USDâ†’BRL usa EconomicParameterService (usd_brl_rate) â€” NÃƒO process.env.VENDEO_USD_BRL_RATE ativo
    - Teste 4 (page): demais cards (Taxa de Sucesso, Erro, Tempo, Estorno, UsuÃ¡rios Ativos, VS, Wallet) sem regressÃ£o
  </behavior>
  <action>
    Modificar `src/app/(app)/admin/metrics/page.tsx`:
    1. `buildCampaignCards` â†’ `{ label: "Custo MÃ©dio IA", value: data.avgCost, timeRange }`.
    2. Substituir `const USD_BRL_RATE = Number(process.env.VENDEO_USD_BRL_RATE ?? "5.50")` por resoluÃ§Ã£o via `EconomicParameterService` (server): `const usdBrl = await new EconomicParameterService().getParameter("usd_brl_rate");` â†’ `usdBrl.value` (default 1.00 no fallback). Env fica apenas como fallback de bootstrap DOCUMENTADO (nÃ£o ativo) OU removido â€” seguir a decisÃ£o D2 (fonte Ãºnica = parÃ¢metro).
    3. Repassar `usdBrl.value` ao `MetricsCards usdToBrlRate` (mesmo ponto de uso atual das linhas 230-238).
    4. NÃƒO alterar os demais cards/sections.
  </action>
  <verify>
    <automated>npx vitest run src/app/(app)/admin/metrics/__tests__/page.test.tsx; npm run typecheck; `grep -c "Custo MÃ©dio IA" src/app/(app)/admin/metrics/page.tsx` >= 1; `grep -c "EconomicParameterService" src/app/(app)/admin/metrics/page.tsx` >= 1; `grep -c "VENDEO_USD_BRL_RATE" src/app/(app)/admin/metrics/page.tsx` <= 1</automated>
  </verify>
  <acceptance_criteria>
    - Card renomeado; USDâ†’BRL via economic_parameters (fonte Ãºnica D2)
    - Demais cards sem regressÃ£o
    - typecheck limpo
  </acceptance_criteria>
  <done>Card 'Custo MÃ©dio IA' + conversÃ£o via parÃ¢metro econÃ´mico (D6/D2)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Testes completos (tarefa 12.8 â€” 4 testes)</name>
  <files>src/lib/metrics/__tests__/pipeline-metrics.test.ts, src/app/(app)/admin/metrics/__tests__/page.test.tsx</files>
  <read_first>
    - Testes existentes de pipeline-metrics e da pÃ¡gina de mÃ©tricas (se existirem â€” verificar)
  </read_first>
  <behavior>
    - Casos (tarefa 12.8 â€” 4 testes): getAvgCost NÃƒO lÃª campaign_pipeline.estimated_cost_usd (nÃ£o usa avg_cost_ms); card "Custo MÃ©dio IA" exibe mÃ©dia por entrega; USDâ†’BRL usa economic_parameters.usd_brl_rate nÃ£o env; regressÃ£o demais cards
  </behavior>
  <action>
    Criar/estender os testes de `pipeline-metrics.ts` (mock do RPC admin_get_ai_costs / admin_get_ai_operation_runs) e da pÃ¡gina (mock do EconomicParameterService + card label). Cobrir os 4 casos da behavior list.
  </action>
  <verify>
    <automated>npx vitest run src/lib/metrics/__tests__/pipeline-metrics.test.ts src/app/(app)/admin/metrics/__tests__/page.test.tsx; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - 4 testes passando (npx vitest run â€” 0 falhas)
    - typecheck limpo
  </acceptance_criteria>
  <done>Testes da correÃ§Ã£o do /admin/metrics verdes (tarefa 12.8)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| getAvgCost â†’ RPCs de apuraÃ§Ã£o | Leitura server-only via supabaseAdmin (service_role) |
| /admin/metrics â† admin autenticado | PÃ¡gina admin-only (jÃ¡ protegida pelo layout) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38.2-39 | Spoofing | MÃ©dia incorreta (delivery marker incluso) | mitigate | getAvgCost usa APENAS apuraÃ§Ã£o call-level (RPC jÃ¡ exclui delivery markers â€” D1/D6); nÃ£o lÃª mais campaign_pipeline.estimated_cost_usd â€” F38.2-21 |
| T-38.2-40 | Tampering | ConversÃ£o com taxa errada | mitigate | Fonte Ãºnica: economic_parameters.usd_brl_rate via EconomicParameterService (D2); env deprecado â€” F38.2-19 |
| T-38.2-41 | DoS | RPC de apuraÃ§Ã£o em falha | mitigate | Fallback null (degradaÃ§Ã£o suave â€” padrÃ£o atual do bundle); pÃ¡gina continua renderizando os demais cards â€” F38.2-21 |
| T-38.2-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado neste plano â€” gate de legitimidade nÃ£o se aplica |
</threat_model>

<verification>
- `npm run typecheck` limpo (getAvgCost + pÃ¡gina)
- `npx vitest run src/lib/metrics/__tests__/pipeline-metrics.test.ts src/app/(app)/admin/metrics/__tests__/page.test.tsx` â€” 4 testes, 0 falhas
- `admin_get_metrics` (F28) inalterado; env VENDEO_USD_BRL_RATE deprecado (nÃ£o ativo)
</verification>

<success_criteria>
- getAvgCost apura custo mÃ©dio de IA por entrega via call-level (nÃ£o NULL)
- Card "Custo MÃ©dio IA" no /admin/metrics
- USDâ†’BRL via economic_parameters.usd_brl_rate (fonte Ãºnica D2)
- 4 testes verdes (tarefa 12.8)
</success_criteria>

<output>
Create `.planning/phases/38.2-admin-custos-operacionais/38-2-09-SUMMARY.md` when done
</output>

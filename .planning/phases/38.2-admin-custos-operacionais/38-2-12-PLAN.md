---
phase: 38.2-admin-custos-operacionais
plan: 12
type: execute
wave: 1
depends_on: []
files_modified:
  - "supabase/migrations/20260811000001_f38_2_creditos_liquidos_estornos.sql"
  - "scripts/verify/38-2-f38-2-verification.mjs"
autonomous: true
gap_closure: true
requirements:
  - "F38.2-08"
  - "F38.2-09"
must_haves:
  truths:
    - "RPC admin_get_ai_operation_runs expõe creditos_estornados e creditos_liquidos por run — creditos_debitados MANTÉM semântica BRUTO (view admin_cost_vs_credits F38.1 intocada)"
    - "creditcos_liquidos = max(creditos_debitados - creditos_estornados, 0) computado no SQL para cada run e no summary"
    - "RPC admin_get_ai_operation_run_events (detalhe) expõe os 3 campos de crédito no run"
    - "Verificação I5 em banco real cobre os novos campos (presença + invariantes) e passa com 0 falhas"
  artifacts:
    - path: "supabase/migrations/20260811000001_f38_2_creditos_liquidos_estornos.sql"
      provides: "CREATE OR REPLACE dos RPCs admin_get_ai_operation_runs/_events com estornos/líquidos por run"
      contains: "creditos_liquidos"
    - path: "scripts/verify/38-2-f38-2-verification.mjs"
      provides: "Asserts I5 para creditos_estornados/creditos_liquidos (runs + summary + detalhe)"
      contains: "creditos_estornados"
  key_links:
    - from: "supabase/migrations/20260811000001_f38_2_creditos_liquidos_estornos.sql"
      to: "public.credit_transactions"
      via: "refunds: rf.type='refund' AND rf.reference = ded.id::text (ded = deduction vinculada ao run)"
      pattern: "reference"
    - from: "supabase/migrations/20260811000001_f38_2_creditos_liquidos_estornos.sql"
      to: "public.admin_cost_vs_credits"
      via: "creditos_debitados BRUTO inalterado (F38.1 view reutilizada, NÃO modificada)"
      pattern: "admin_cost_vs_credits"
    - from: "scripts/verify/38-2-f38-2-verification.mjs"
      to: "admin_get_ai_operation_runs"
      via: "supabase.rpc sobre o banco real"
      pattern: "admin_get_ai_operation_runs"
---

<objective>
Fechar o gap major da F38.2 no nível de RPC: a apuração por entrega (`admin_get_ai_operation_runs`) e o detalhe (`admin_get_ai_operation_run_events`) passam a expor, por operation_run_id, os campos de crédito **bruto / estornado / líquido** — sem alterar o contrato F38.1 (`admin_cost_vs_credits` permanece intocada e `creditos_debitados` mantém o significado histórico BRUTO).

Purpose: falhas debitam crédito e depois estornam (refund_credit grava `reference` → deduction original, `campaign_id` NULL), mas os KPIs contabilizavam deduções sem descontar estornos — superestimando receita/resultado/margem. A correção acontece **só nos RPCs da F38.2** (camada de apuração), reutilizando a lógica de dedução da view e somando os refunds (`ct.type = 'refund' AND ct.reference = deduction.id::text`). Custo de IA NÃO é estornado — falha após consumo de IA continua contando como custo (apenas a receita zera).
Output: migration nova com os dois RPCs redefinidos (lista + eventos) + asserts I5 estendidos no script de verificação.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38.2-admin-custos-operacionais/38.2-UAT.md
@.planning/phases/38.2-admin-custos-operacionais/38-2-CONTEXT.md
@supabase/migrations/20260810000003_f38_2_operation_run_rpcs.sql
@supabase/migrations/20260810000004_f38_2_fix_operation_run_rpcs.sql
@supabase/migrations/20260809000002_f38_1_apuracao_janela_margem.sql
@supabase/migrations/20260722000002_creditos_mensais_automaticos.sql

<interfaces>
Contratos existentes (verificados — não explorar além disso):

De 20260810000004_f38_2_fix_operation_run_rpcs.sql (versão ATUAL do RPC de lista — base para o CREATE OR REPLACE):
- Assinatura: `public.admin_get_ai_operation_runs(p_period_start TIMESTAMPTZ, p_period_end TIMESTAMPTZ, p_store_id UUID, p_run_type TEXT, p_status TEXT, p_provider TEXT, p_model TEXT, p_generation_type TEXT, p_operation_run_id UUID, p_page INTEGER, p_page_size INTEGER) RETURNS JSONB` — SECURITY DEFINER, `SET search_path = ''`
- CTEs: `call_level` (filtros + janela 365d) → `runs` (agregação por operation_run_id) → `filtered_runs` (joins de crédito/segmento) → `summary`
- `creditos_debitados` atual: `COALESCE(vc.creditos_debitados, 0)` via `LEFT JOIN public.admin_cost_vs_credits vc ON vc.operation_run_id = r.operation_run_id` (BRUTO — F38.1, NÃO alterar)
- CTE `de` (evidência de segmento D9): UNION ALL de campanha (`ct.type='deduction' AND ct.metadata->>'feature'='campaign_pipeline'` + DISTINCT campaign_id/operation_run_id) e visual_signature (`credit_transactions` via `svs.metadata->>'credit_tx_id'` + DISTINCT visual_signature_id/operation_run_id) — MESMO shape a espelhar para refunds
- JSONB final: `runs` (array com `creditos_debitados`) + `summary` (com `creditos_debitados`)

De 20260810000003_f38_2_operation_run_rpcs.sql (versão atual do RPC de eventos):
- Assinatura: `public.admin_get_ai_operation_run_events(p_operation_run_id UUID) RETURNS JSONB`
- CTE `events` já escopada por `ge.operation_run_id = p_operation_run_id` e excluindo delivery markers; `run_agg`; JSONB `run` (sem campos de crédito hoje) + `events`

Semântica de refund (20260722000002:358-365 — refund_credit):
- Refund insere `credit_transactions` com `type='refund'`, `amount` POSITIVO (= bonus_restore + purchased_restore), `reference = p_tx_id::text` (id da deduction original), `campaign_id` NULL
- Logo: refunds vinculam-se à deduction via `rf.reference = ded.id::text`; a deduction vincula-se ao run via campaign_id (campanha) ou via `store_visual_signatures.metadata->>'credit_tx_id'` (visual_signature)

View F38.1 admin_cost_vs_credits (20260809000002): NÃO modificar — permanece sem estornos (contrato F38.1). O RPC soma estornos independentemente.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration — RPCs com creditos_estornados/creditos_liquidos por run</name>
  <files>supabase/migrations/20260811000001_f38_2_creditos_liquidos_estornos.sql</files>
  <read_first>
    - supabase/migrations/20260810000004_f38_2_fix_operation_run_rpcs.sql (RPC de lista — base completa, linhas 11-325)
    - supabase/migrations/20260810000003_f38_2_operation_run_rpcs.sql (RPC de eventos — base, linhas 332-446; REVERT linhas 448-454)
  </read_first>
  <action>
    Criar a migration `supabase/migrations/20260811000001_f38_2_creditos_liquidos_estornos.sql` no estilo EXATO das 20260810000003/0004 (cabeçalho de comentário com regras, bloco por RPC, COMMENT, REVOKE/GRANT, seção REVERT comentada em ordem reversa). Regras de ouro:

    1. **NÃO tocar** em `public.admin_cost_vs_credits` (view F38.1 — contrato compat) nem em `admin_get_ai_costs` / `admin_get_metrics` nem em nenhuma migration antiga. O fix é exclusivamente CREATE OR REPLACE dos DOIS RPCs da F38.2.
    2. **`creditos_debitados` mantém semântica BRUTO** (auditoria de deduções) — o valor do LEFT JOIN `admin_cost_vs_credits` NÃO muda.

    **A) Redefinir `public.admin_get_ai_operation_runs(...)`** (mesma assinatura, janela 365d, paginação, CTEs `call_level`/`runs`/`filtered_runs`/`summary` como na 20260810000004). Mudanças:
    - Adicionar CTE `refunds AS (...)` calculando `SUM(ABS(rf.amount)) AS estornado` por operation_run_id via UNION ALL de dois ramos (espelhar a estrutura da CTE `de` existente):
      - **Campanha:** `FROM public.credit_transactions rf JOIN public.credit_transactions ded ON rf.reference = ded.id::text`, com `rf.type = 'refund'`, `ded.type = 'deduction'`, `ded.campaign_id IS NOT NULL`, `ded.metadata->>'feature' = 'campaign_pipeline'`; vincula ao run via DISTINCT `campaign_id`/`operation_run_id` de `generation_events` (mesmo subquery `cr` da CTE `de`).
      - **Visual signature:** `FROM public.credit_transactions rf2 JOIN public.credit_transactions ded2 ON rf2.reference = ded2.id::text JOIN public.store_visual_signatures svs ON ded2.id::text = svs.metadata->>'credit_tx_id'`, com `rf2.type = 'refund'`, `ded2.type = 'deduction'`; vincula ao run via DISTINCT `visual_signature_id`/`operation_run_id` de `generation_events` (mesmo subquery `vr` da CTE `de`).
      - Usar `ABS(rf.amount)` (defensivo — amount de refund é positivo por construção).
    - `filtered_runs`: manter `COALESCE(vc.creditos_debitados, 0) AS creditos_debitados`; adicionar `LEFT JOIN refunds re ON re.operation_run_id = r.operation_run_id` e as colunas `COALESCE(re.estornado, 0) AS creditos_estornados` e `GREATEST(COALESCE(vc.creditos_debitados, 0) - COALESCE(re.estornado, 0), 0) AS creditos_liquidos` (fórmula `max(bruto − estorno, 0)` — floor em 0).
    - `summary`: adicionar `SUM(fr.creditos_estornados) AS creditos_estornados` e `SUM(fr.creditos_liquidos) AS creditos_liquidos`.
    - JSONB `runs` (jsonb_build_object do sub): adicionar `'creditos_estornados', sub.creditos_estornados` e `'creditos_liquidos', sub.creditos_liquidos` logo após `'creditos_debitados', sub.creditos_debitados`.
    - JSONB `summary`: adicionar `'creditos_estornados'` e `'creditos_liquidos'` após `'creditos_debitados'`.
    - Atualizar o COMMENT ON FUNCTION (citar estornos/líquidos). REVOKE/GRANT inalterados. Adicionar REVERT comentado.
    - Observação de integridade: um refund vinculado a deduction de campanha com múltiplos runs conta para todos os runs vinculados — MESMA semântica da lógica de dedução existente da view (comportamento pré-existente, fora do escopo desta correção).

    **B) Redefinir `public.admin_get_ai_operation_run_events(p_operation_run_id UUID)`** (mesma assinatura, CTEs `events`/`run_agg` como na 20260810000003). Mudanças:
    - Adicionar CTE `run_credits AS (...)` escopada ao run usando a MESMA lógica de dedução+refund da lista, mas derivada da CTE `events` existente (já filtrada por p_operation_run_id):
      - `creditos_debitados`: `COALESCE((SELECT creditos_debitados FROM public.admin_cost_vs_credits WHERE operation_run_id = p_operation_run_id), 0)` (BRUTO — reuso da view, sem duplicar lógica).
      - `creditos_estornados`: soma dos refunds via `rf.reference = ded.id::text` com deduções vinculadas ao run — campanha: `ded.campaign_id IN (SELECT campaign_id FROM events WHERE campaign_id IS NOT NULL) AND ded.metadata->>'feature' = 'campaign_pipeline'`; visual_signature: `ded.id::text = svs.metadata->>'credit_tx_id'` com `svs.id IN (SELECT visual_signature_id FROM events WHERE visual_signature_id IS NOT NULL)`; sempre `rf.type='refund'` e `ded.type='deduction'`; `ABS(rf.amount)`.
      - `creditos_liquidos`: `GREATEST(creditos_debitados - creditos_estornados, 0)`.
    - JSONB `run` (jsonb_build_object): adicionar `'creditos_debitados'`, `'creditos_estornados'`, `'creditos_liquidos'` (os três vêm de run_credits).
    - Atualizar COMMENT; REVOKE/GRANT inalterados; REVERT comentado.

    **C) Aplicar a migration Local + Remote** (precedente 38-2-01 "[BLOCKING] aplicado no remoto"): subir o Supabase local se necessário e rodar `npx supabase db push` até o schema do remoto refletir os dois RPCs redefinidos (conferir com `supabase db push --dry-run` se disponível antes).
  </action>
  <verify>
    <automated>(Select-String -Path "supabase\migrations\20260811000001_f38_2_creditos_liquidos_estornos.sql" -Pattern "creditos_liquidos" | Measure-Object).Count -ge 4; (Select-String -Path "supabase\migrations\20260811000001_f38_2_creditos_liquidos_estornos.sql" -Pattern "rf.reference" | Measure-Object).Count -ge 1; npx supabase db push</automated>
  </verify>
  <done>Migration criada com os dois RPCs expondo creditos_estornados/creditos_liquidos (BRUTO preservado) e aplicada em Local + Remote</done>
</task>

<task type="auto">
  <name>Task 2: Verificação I5 — asserts de estornos/líquidos em banco real</name>
  <files>scripts/verify/38-2-f38-2-verification.mjs</files>
  <read_first>
    - scripts/verify/38-2-f38-2-verification.mjs (seção I5, linhas 344-420 — asserts de runs/summary/detalhe)
  </read_first>
  <action>
    Estender a seção I5 do script de verificação (padrão `assert(name, ok, detail)` existente — manter TODOS os asserts atuais intactos, adicionar após o bloco de `firstRun`):

    1. **Runs (lista):** após os asserts de `store_is_test`/`deduction_*`/badge, adicionar:
       - `"creditos_estornados" in firstRun` e `"creditos_liquidos" in firstRun` (presença)
       - `toNumber(firstRun.creditos_estornados) !== null` e `>= 0`; `toNumber(firstRun.creditos_liquidos) !== null` e `>= 0`
       - invariante: `toNumber(firstRun.creditos_liquidos) === Math.max((toNumber(firstRun.creditos_debitados) ?? 0) - toNumber(firstRun.creditos_estornados), 0)` (fórmula max(bruto−estorno,0))
       - `toNumber(firstRun.creditos_liquidos) <= toNumber(firstRun.creditos_debitados)`
    2. **Summary:** `"creditos_estornados" in runsData.summary` e `"creditos_liquidos" in runsData.summary`, ambos numéricos `>= 0`.
    3. **Detalhe (events RPC):** dentro do bloco existente que chama `admin_get_ai_operation_run_events`, após `assert("I5: RPC de eventos responde"...)`: se `eventsData?.run` não for null, assert de presença dos três campos: `"creditos_debitados" in eventsData.run && "creditos_estornados" in eventsData.run && "creditos_liquidos" in eventsData.run` e invariante líquido = max(bruto−estorno, 0).
    4. **Dado-dependente (informativo, robusto):** computar quantos runs do array têm `toNumber(creditos_estornados) > 0`; se houver ao menos um, assert `toNumber(runsData.summary.creditos_estornados) > 0` (refunds reais refletidos no summary); se não houver, apenas `console.log` informativo (não falhar com dados vazios). O banco real tem 8 refunds no ledger (3 falhas com debitado+estornado) — o assert deve passar em produção com dados atuais.
    Executar `node scripts/verify/38-2-f38-2-verification.mjs` (usa .env.local + banco real, padrão existente) — deve terminar com 0 falhas (50 asserts existentes + novos).
  </action>
  <verify>
    <automated>node scripts/verify/38-2-f38-2-verification.mjs</automated>
  </verify>
  <done>I5 cobre estornos/líquidos (presença + invariantes + summary + detalhe) e o script passa 0 falhas em banco real</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service_role → RPCs admin_get_ai_operation_runs/_events | RPCs SECURITY DEFINER redefinidos — entrada de parâmetros p_* vinculados; leitura de ledger credit_transactions e view admin_cost_vs_credits |
| script verificação → Supabase real | Apenas leitura (I5) — sem escrita em generation_events (regra F38.1: nunca DELETE/UPDATE em geração) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38.2-G01 | Tampering | Refund vinculado a deduction errada | mitigate | Join estrito `rf.reference = ded.id::text` com `rf.type='refund'`/`ded.type='deduction'` e âncora ao run (campaign_id/credit_tx_id) — mesmo shape da CTE `de` existente; líquido com floor `GREATEST(..., 0)` nunca negativo |
| T-38.2-G02 | Spoofing | creditos_debitados alterado (BRUTO perdido) | mitigate | Valor mantém `COALESCE(vc.creditos_debitados, 0)` do LEFT JOIN admin_cost_vs_credits (F38.1) — a migration NÃO toca a view; asserts I5 de invariante líquido = max(bruto−estorno, 0) |
| T-38.2-G03 | Injection | RPC SECURITY DEFINER redefinido | mitigate | CREATE OR REPLACE preserva `SET search_path = ''` e parâmetros vinculados p_* (nunca concatenação) — padrão 20260810000003/0004 inalterado |
| T-38.2-G04 | Repudiation | Semântica de estorno divergente do ledger | mitigate | Refunds lidos direto do ledger (credit_transactions) via reference — append-only por desenho; custo de IA NÃO é estornado (apenas receita zera) conforme diretiva do phase owner |
| T-38.2-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado neste plano — gate de legitimidade não se aplica |
</threat_model>

<verification>
- RPC de lista expõe creditos_estornados/creditos_liquidos por run e no summary; creditos_debitados BRUTO inalterado
- RPC de eventos expõe os 3 campos no run de detalhe
- View admin_cost_vs_credits e RPCs admin_get_ai_costs/admin_get_metrics intocados (grep nos diffs)
- scripts/verify/38-2-f38-2-verification.mjs com 0 falhas (I5 estendido) em banco real
- npx vitest run / typecheck / lint / build não regrediram (gates da fase)
</verification>

<success_criteria>
- Fórmula `creditosLiquidos = max(creditosBrutos − creditosEstornados, 0)` aplicada nos dois RPCs da F38.2 (lista + eventos)
- Semântica BRUTO de creditos_debitados preservada; F38.1 view intocada
- I5 verifica estornos/líquidos em banco real com 0 falhas
</success_criteria>

<output>
Create `.planning/phases/38.2-admin-custos-operacionais/38-2-12-SUMMARY.md` when done
</output>

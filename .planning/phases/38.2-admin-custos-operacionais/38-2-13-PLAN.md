---
phase: 38.2-admin-custos-operacionais
plan: 13
type: execute
wave: 2
depends_on:
  - "38-2-12"
files_modified:
  - "src/lib/ai-cost/operation-runs-service.ts"
  - "src/lib/ai-cost/__tests__/operation-runs-service.test.ts"
  - "src/app/(app)/admin/ai-operation-costs/__tests__/components.test.tsx"
  - "src/app/(app)/admin/ai-operation-costs/__tests__/page.test.tsx"
autonomous: true
gap_closure: true
requirements:
  - "F38.2-10"
  - "F38.2-11"
must_haves:
  truths:
    - "deriveBrl usa creditos_liquidos para receitaOpBrl/resultadoOpBrl/margemOpPct (bruto vira só auditoria)"
    - "Run e summary derivados expõem creditosDebitados (bruto) + creditosEstornados + creditosLiquidos"
    - "Run falho 100% estornado deriva receita R$0 e resultado negativo (custo de IA permanece)"
    - "Estorno > bruto → líquido floor 0 (nunca negativo)"
    - "Vitest + typecheck verdes após a mudança de contrato (fixtures de UI atualizadas)"
  artifacts:
    - path: "src/lib/ai-cost/operation-runs-service.ts"
      provides: "deriveBrl com líquidos + novos campos nos tipos (OperationRun/OperationRunsSummary/RawOperationRun/RawDetailRun) + mapRun/mapDetailRun/deriveSummary"
      contains: "creditosLiquidos"
    - path: "src/lib/ai-cost/__tests__/operation-runs-service.test.ts"
      provides: "Testes de derivação com estornos (líquido/receita 0/full-refund/floor) + summary de líquidos"
      contains: "creditos_estornados"
  key_links:
    - from: "src/lib/ai-cost/operation-runs-service.ts"
      to: "RPC admin_get_ai_operation_runs (38-2-12)"
      via: "RawOperationRun.creditos_liquidos consumido por deriveBrl"
      pattern: "creditos_liquidos"
    - from: "src/lib/ai-cost/operation-runs-service.ts"
      to: "RPC admin_get_ai_operation_run_events (38-2-12)"
      via: "RawDetailRun.creditos_* consumido por mapDetailRun"
      pattern: "mapDetailRun"
    - from: "src/lib/ai-cost/__tests__/operation-runs-service.test.ts"
      to: "deriveBrl"
      via: "cases: bruto 10/estorno 3/líquido 7; full-refund; estorno > bruto; summary"
      pattern: "creditosLiquidos"
---

<objective>
Aplicar no service layer (`OperationRunsService`) a nova contabilidade por run: `receitaOpBrl = creditosLiquidos × credit_value_brl`, `resultadoOpBrl = receitaOpBrl − custoBrl`, `margemOpPct` quando receita > 0 (senão null) — usando o campo `creditos_liquidos` que o RPC passou a expor (38-2-12). `creditosDebitados` continua exposto como auditoria BRUTO.

Purpose: os KPIs financeiros do painel (summary) e de cada run (tabela/drilldown) passam a refletir estornos — falha debitada e estornada zera a receita mas mantém o custo de IA. A derivação monetária permanece 100% no service (D1/D4 — o RPC devolve bruto/líquido em créditos, nunca BRL).
Output: service com novos campos + testes de derivação + fixtures de UI atualizadas (typecheck verde).
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/lib/ai-cost/operation-runs-service.ts
@src/lib/ai-cost/__tests__/operation-runs-service.test.ts
@src/app/(app)/admin/ai-operation-costs/__tests__/components.test.tsx
@src/app/(app)/admin/ai-operation-costs/__tests__/page.test.tsx

<interfaces>
Contrato atual (verificado — linha 348 deriveBrl, tipos 51-90, mapRun 676-712, mapDetailRun 584-614, deriveSummary 830-865):

- `deriveBrl(raw: Pick<RawOperationRun, "custo_usd_total" | "creditos_debitados">, usdBrlRate, creditValueBrl)` → { custoBrl, receitaOpBrl, resultadoOpBrl, margemOpPct } — hoje usa `creditos_debitados`
- `OperationRun.creditosDebitados: number | null` (linha 61) — único campo de crédito
- `OperationRunsSummary.creditosDebitados: number | null` (linha 81)
- `RawOperationRun.creditos_debitados?: string | number | null` (linha 174)
- `RawDetailRun` (222-232) — sem campos de crédito
- `mapDetailRun` (584-614) — hoje fixa creditosDebitados: null / receitaOpBrl: null (RPC de eventos não expunha)
- `deriveSummary` (830-865) — receita = sum(brutos) × credit_value

Novo contrato do RPC (38-2-12): runs e run de detalhe passam a expor `creditos_estornados` e `creditos_liquidos` (além do `creditos_debitados` BRUTO); summary expõe os mesmos dois novos somados.

Fórmulas do phase owner (vincular nos testes):
- creditosLiquidos = max(creditosBrutos − creditosEstornados, 0)
- receitaOpBrl = creditosLiquidos × credit_value_brl
- resultadoOpBrl = receitaOpBrl − custoBrl
- margemOpPct = receitaOpBrl > 0 ? resultadoOpBrl / receitaOpBrl × 100 : null
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Service — deriveBrl com creditosLiquidos + novos campos</name>
  <files>src/lib/ai-cost/operation-runs-service.ts</files>
  <read_first>
    - src/lib/ai-cost/operation-runs-service.ts (tipos 51-90 e 167-232; deriveBrl 346-368; mapRun 676-712; mapDetailRun 584-614; deriveSummary 830-865)
  </read_first>
  <action>
    Alterar `src/lib/ai-cost/operation-runs-service.ts`:

    1. **`RawOperationRun`** (após `creditos_debitados`): adicionar `creditos_estornados?: string | number | null` e `creditos_liquidos?: string | number | null`.
    2. **`RawDetailRun`** (após `custo_usd_total`): adicionar `creditos_debitados?: string | number | null`, `creditos_estornados?: string | number | null`, `creditos_liquidos?: string | number | null`.
    3. **`OperationRun`** (após `creditosDebitados`): adicionar `creditosEstornados: number | null` e `creditosLiquidos: number | null` (obrigatórios — padrão do contrato).
    4. **`OperationRunsSummary`** (após `creditosDebitados`): adicionar `creditosEstornados: number | null` e `creditosLiquidos: number | null`.
    5. **`deriveBrl`**: trocar o uso de `creditos_debitados` por `creditos_liquidos` na receita (ampla o Pick para incluir os três campos de crédito). `custoBrl` inalterado. Fórmulas exatas: `receitaOpBrl = creditos !== null ? creditos * creditValueBrl : null` onde `creditos = toNumber(raw.creditos_liquidos)`; `resultadoOpBrl = custoBrl !== null && receitaOpBrl !== null ? receitaOpBrl - custoBrl : null`; `margemOpPct = receitaOpBrl !== null && receitaOpBrl > 0 && resultadoOpBrl !== null ? (resultadoOpBrl / receitaOpBrl) * 100 : null`. Atualizar o JSDoc: bruto = auditoria de deduções; líquido = bruto − estornos (floor 0) usado para receita/resultado/margem.
    6. **`mapRun`**: `creditosDebitados: toNumber(raw.creditos_debitados)` (auditoria BRUTO — mantém), `creditosEstornados: toNumber(raw.creditos_estornados)`, `creditosLiquidos: toNumber(raw.creditos_liquidos)`. Passa o raw completo para deriveBrl (o Pick do deriveBrl já cobre os campos novos).
    7. **`mapDetailRun`**: substituir o bloco manual de custoBrl por uma chamada a `deriveBrl(raw, params.usdBrlRate, params.creditValueBrl)` (RawDetailRun agora tem os campos) e popular `creditosDebitados`/`creditosEstornados`/`creditosLiquidos`/`receitaOpBrl`/`resultadoOpBrl`/`margemOpPct` com os valores derivados (hoje eram null).
    8. **`deriveSummary`**: `creditosDebitados: sum(brutos)` (auditoria), `creditosEstornados: sum(estornos)`, `creditosLiquidos: sum(líquidos)`; `receitaOpBrl = creditosLiquidos !== null ? creditosLiquidos * params.creditValueBrl : null` (nunca mais bruto); resultado/margem pela fórmula padrão (margem null quando receita <= 0).
    NÃO alterar: badges (deriveRunBadge), segmentação (classifySegment), agregados (bySegment continua somando receitaOpBrl/resultadoOpBrl derivados — já usa os campos derivados por run), paginação, fail-closed.
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.typecheck.json --noEmit</automated>
  </verify>
  <done>Service deriva receita/resultado/margem a partir de creditosLiquidos; tipos expõem bruto/estorno/líquido; typecheck verde</done>
</task>

<task type="auto">
  <name>Task 2: Testes — derivação com estornos + fixtures de UI (typecheck verde)</name>
  <files>src/lib/ai-cost/__tests__/operation-runs-service.test.ts, src/app/(app)/admin/ai-operation-costs/__tests__/components.test.tsx, src/app/(app)/admin/ai-operation-costs/__tests__/page.test.tsx</files>
  <read_first>
    - src/lib/ai-cost/__tests__/operation-runs-service.test.ts (makeRawRun 16-45; describe BRL 117-167; describe aggregations 268-396)
    - src/app/(app)/admin/ai-operation-costs/__tests__/components.test.tsx (makeRun 29-56; SUMMARY 58-70)
    - src/app/(app)/admin/ai-operation-costs/__tests__/page.test.tsx (makeRun 56-83; makeListResult summary 90-102)
  </read_first>
  <action>
    Atualizar/estender testes:

    1. **`operation-runs-service.test.ts` — makeRawRun** (linha 24): adicionar defaults `creditos_estornados: "0"` e `creditos_liquidos: "20"` (líquido = bruto default — testes existentes de BRL permanecem válidos pois não mudam de valor). NOVO describe "derivação com estornos — créditos líquidos (gap F38.2)" com:
       - bruto 10, estorno 3, líquido 7 (campos explícitos), usd_brl_rate 5.0, credit_value_brl 1.5 → receitaOpBrl 10.5, resultadoOpBrl −39.5, margemOpPct ≈ −376.19, creditosLiquidos 7, creditosEstornados 3, creditosDebitados 10 (bruto preservado)
       - **full-refund (run falho)**: bruto 10, estorno 10, líquido 0, custo_usd_total 10 → receitaOpBrl 0, resultadoOpBrl −50 (custo permanece), margemOpPct null
       - **estorno > bruto**: bruto 10, estorno 12 → líquido 0 (floor — nunca negativo), receita 0
       - **summary de líquidos**: 2 runs (brutos 10/3, estornos 3/0, líquidos 7/3), usd 5.0/credit 1.5 → summary.creditosDebitados 13, summary.creditosEstornados 3, summary.creditosLiquidos 10, receitaOpBrl 15, custoBrl 100 (20 USD), resultadoOpBrl −85, margemOpPct ≈ −566.67
       - margem null quando receita 0 no summary (já coberto — manter)
    2. **`components.test.tsx`**: `makeRun()` (linha 40) e `SUMMARY` (linha 61) — adicionar `creditosEstornados: 0, creditosLiquidos: 20` (campos agora obrigatórios do contrato). Não alterar asserts ainda (a UI muda no 38-2-14; aqui é só o contrato de tipos).
    3. **`page.test.tsx`**: `makeRun()` (linha 67) e `makeListResult().summary` (linha 93) — mesmos dois campos adicionados.
    4. Rodar os testes dos arquivos tocados + typecheck (não rodar a suíte inteira aqui — os asserts de UI mudam no 38-2-14; a suíte completa roda nos gates da fase).
  </action>
  <verify>
    <automated>npx vitest run "src/lib/ai-cost/__tests__/operation-runs-service.test.ts" "src/app/(app)/admin/ai-operation-costs/__tests__/components.test.tsx" "src/app/(app)/admin/ai-operation-costs/__tests__/page.test.tsx"; if ($?) { npm run typecheck }</automated>
  </verify>
  <done>Testes de derivação com estornos verdes (receita 0 em full-refund, floor em estorno > bruto, summary de líquidos); fixtures de UI com os campos novos; typecheck verde</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service layer → RPC (38-2-12) | Derivação monetária no service a partir dos campos brutos do RPC — nunca calculada no SQL/UI |
| service layer → EconomicParameterService | Parâmetros econômicos (fail-closed → 503) — inalterado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38.2-G05 | Tampering | Receita derivada de bruto em vez de líquido | mitigate | deriveBrl lê `creditos_liquidos` (nunca `creditos_debitados`); teste de full-refund (receita 0, custo permanece) e summary de líquidos — regressão detectável |
| T-38.2-G06 | DoS | Líquido negativo propagado à UI | mitigate | RPC aplica GREATEST(..., 0) (38-2-12) + teste de floor para estorno > bruto; receita 0 → margem null (sem divisão por zero) |
| T-38.2-G07 | Spoofing | Contrato de tipos divergente entre service/UI | mitigate | Campos obrigatórios em OperationRun/OperationRunsSummary + fixtures de UI atualizadas no mesmo plano (typecheck falha se esquecido) |
| T-38.2-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado neste plano — gate de legitimidade não se aplica |
</threat_model>

<verification>
- deriveBrl usa creditos_liquidos (grep: nenhum uso de creditos_debitados em receita)
- mapDetailRun popula receita/resultado/margem e os 3 campos de crédito
- deriveSummary: receita = sum(líquidos) × credit_value
- vitest dos arquivos tocados + typecheck verdes
</verification>

<success_criteria>
- `receitaOpBrl = creditosLiquidos × credit_value_brl`; `resultadoOpBrl = receitaOpBrl − custoBrl`; `margemOpPct` null quando receita ≤ 0
- Bruto permanece exposto como auditoria (creditosDebitados)
- Run falho estornado: receita R$0, resultado −R$X (custo de IA permanece)
</success_criteria>

<output>
Create `.planning/phases/38.2-admin-custos-operacionais/38-2-13-SUMMARY.md` when done
</output>

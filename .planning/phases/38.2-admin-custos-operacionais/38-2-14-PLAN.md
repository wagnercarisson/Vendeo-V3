---
phase: 38.2-admin-custos-operacionais
plan: 14
type: execute
wave: 3
depends_on:
  - "38-2-13"
files_modified:
  - "src/app/(app)/admin/ai-operation-costs/kpis-grid.tsx"
  - "src/app/(app)/admin/ai-operation-costs/operation-runs-table.tsx"
  - "src/app/(app)/admin/ai-operation-costs/run-detail-dialog.tsx"
  - "src/app/(app)/admin/ai-operation-costs/__tests__/components.test.tsx"
autonomous: true
gap_closure: true
requirements:
  - "F38.2-13"
must_haves:
  truths:
    - "KpisGrid mostra 'Créditos brutos', 'Estornos' e 'Créditos líquidos' (KPIs financeiros já refletem líquidos via summary do service)"
    - "Card P95 com label 'Tempo P95 (95% das entregas)' e tooltip explicativo"
    - "Tabela por entrega mostra breakdown bruto/estorno/líquido por run + receita/resultado (run falho estornado: receita R$0,00, resultado −R$X — custo permanece)"
    - "Drilldown (dialog) mostra o breakdown de créditos e receita/resultado no cabeçalho do run"
    - "Testes de componentes atualizados para os novos labels/breakdown e verdes"
  artifacts:
    - path: "src/app/(app)/admin/ai-operation-costs/kpis-grid.tsx"
      provides: "KPIs de créditos brutos/estornos/líquidos + label/tooltip P95"
      contains: "Créditos brutos"
    - path: "src/app/(app)/admin/ai-operation-costs/operation-runs-table.tsx"
      provides: "Breakdown bruto/estorno/líquido + receita/resultado por run"
      contains: "Estorno"
    - path: "src/app/(app)/admin/ai-operation-costs/run-detail-dialog.tsx"
      provides: "Breakdown de créditos + receita/resultado no cabeçalho do run"
      contains: "creditosLiquidos"
  key_links:
    - from: "src/app/(app)/admin/ai-operation-costs/kpis-grid.tsx"
      to: "OperationRunsSummary (38-2-13)"
      via: "summary.creditosDebitados/creditosEstornados/creditosLiquidos consumidos nos cards"
      pattern: "creditosLiquidos"
    - from: "src/app/(app)/admin/ai-operation-costs/operation-runs-table.tsx"
      to: "OperationRun (38-2-13)"
      via: "run.creditosEstornados/creditosLiquidos/receitaOpBrl/resultadoOpBrl no breakdown da célula Créditos"
      pattern: "creditosEstornados"
    - from: "src/app/(app)/admin/ai-operation-costs/run-detail-dialog.tsx"
      to: "GET /api/admin/ai-operation-runs/[operationRunId] (38-2-13 mapDetailRun)"
      via: "detail.run.creditosLiquidos/receitaOpBrl/resultadoOpBrl no cabeçalho"
      pattern: "receitaOpBrl"
---

<objective>
Aplicar a nova contabilidade na UI do painel `/admin/ai-operation-costs`: KpisGrid com **Créditos brutos / Estornos / Créditos líquidos**, card P95 renomeado com tooltip, e tabela + drilldown mostrando o **breakdown bruto/estorno/líquido** por run com receita/resultado (run falho estornado exibe receita R$0,00 e resultado −R$X — custo de IA permanece).

Purpose: o usuário vê de forma explícita que falhas debitam e estornam — o painel deixa de superestimar receita/resultado/margem (os valores financeiros já vêm derivados de líquidos pelo service, 38-2-13; aqui é a apresentação).
Output: KPIs + tabela + drilldown atualizados + testes de componentes verdes.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/app/(app)/admin/ai-operation-costs/kpis-grid.tsx
@src/app/(app)/admin/ai-operation-costs/operation-runs-table.tsx
@src/app/(app)/admin/ai-operation-costs/run-detail-dialog.tsx
@src/app/(app)/admin/ai-operation-costs/__tests__/components.test.tsx

<interfaces>
Estado atual (verificado):
- kpis-grid.tsx: `buildKpis(summary)` (41-61) retorna cards `{ label, value }`; card "Créditos debitados" (45) e "P95" (56); card div tem `data-testid={`kpi-${kpi.label}`}` (77)
- operation-runs-table.tsx: coluna "Créditos" (87) renderiza `formatNumber(run.creditosDebitados)` (130); formatters formatBRL/formatUsd/formatNumber/formatDuration locais (17-43)
- run-detail-dialog.tsx: cabeçalho do run (105-116) mostra "Custo estimado Vendeo" + placeholders F38.3; formatters locais (12-38)
- components.test.tsx: KpisGrid test (130-148) asserta "Créditos debitados" e "P95"; table test (152-167) asserta `getByText("20")` para créditos; dialog test mocka `run: makeRun()` (179, 236)

Novos campos do contrato (38-2-13): OperationRun/OperationRunsSummary agora têm creditosDebitados (bruto, auditoria), creditosEstornados, creditosLiquidos, e receitaOpBrl/resultadoOpBrl derivados de LÍQUIDOS.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: KpisGrid — créditos brutos/estornos/líquidos + P95 com tooltip</name>
  <files>src/app/(app)/admin/ai-operation-costs/kpis-grid.tsx</files>
  <read_first>
    - src/app/(app)/admin/ai-operation-costs/kpis-grid.tsx (KpiDef 36-39; buildKpis 41-61; card div 73-84)
  </read_first>
  <action>
    Alterar `kpis-grid.tsx`:

    1. **Card "Créditos debitados" → três cards** (substituir a linha 45):
       - `{ label: "Créditos brutos", value: formatNumber(summary.creditosDebitados) }`
       - `{ label: "Estornos", value: formatNumber(summary.creditosEstornados) }`
       - `{ label: "Créditos líquidos", value: formatNumber(summary.creditosLiquidos) }`
       (labels PT-BR exatos — o data-testid `kpi-${label}` acompanha automaticamente)
    2. **Card P95** (linha 56): label vira `"Tempo P95 (95% das entregas)"` e o card ganha tooltip. Estender `KpiDef` com campo opcional `tooltip?: string` e, no card div (73-84), renderizar `title={kpi.tooltip}`. Para o P95: `tooltip: "95% das entregas terminaram em até este tempo; os 5% mais lentos ficam fora desse corte."` (texto EXATO).
    3. KPIs financeiros ("Receita operacional (BRL)", "Resultado operacional estimado (BRL)", "Margem operacional estimada") mantêm labels — os valores já vêm de líquidos via summary (38-2-13); não recalcular nada na UI (padrão D3).
    NÃO alterar: formatadores, layout do grid, outros cards.
  </action>
  <verify>
    <automated>Select-String -Path "src\app\(app)\admin\ai-operation-costs\kpis-grid.tsx" -Pattern "Créditos brutos|Estornos|Créditos líquidos|Tempo P95 \(95% das entregas\)" | Measure-Object | Select-Object -ExpandProperty Count -ge 4</automated>
  </verify>
  <done>KpisGrid mostra os 3 cards de crédito e o P95 renomeado com tooltip</done>
</task>

<task type="auto">
  <name>Task 2: Tabela + drilldown — breakdown bruto/estorno/líquido e receita/resultado por run</name>
  <files>src/app/(app)/admin/ai-operation-costs/operation-runs-table.tsx, src/app/(app)/admin/ai-operation-costs/run-detail-dialog.tsx</files>
  <read_first>
    - src/app/(app)/admin/ai-operation-costs/operation-runs-table.tsx (célula Créditos 129-131; colunas 80-99)
    - src/app/(app)/admin/ai-operation-costs/run-detail-dialog.tsx (cabeçalho do run 105-116)
  </read_first>
  <action>
    Alterar os dois componentes:

    1. **`operation-runs-table.tsx` — célula "Créditos"** (linhas 129-131): substituir o `formatNumber(run.creditosDebitados)` por um bloco compacto de breakdown com tooltip:
       - três linhas: `Bruto: {formatNumber(run.creditosDebitados)}`, `Estorno: {formatNumber(run.creditosEstornados)}`, `Líquido: {formatNumber(run.creditosLiquidos)}`
       - uma linha muted com o resultado financeiro do run: `Receita {formatBRL(run.receitaOpBrl)} · Resultado {formatBRL(run.resultadoOpBrl)}` — run falho 100% estornado renderiza naturalmente `Receita R$ 0,00 · Resultado R$ -50,00` (custo permanece)
       - tooltip (title) na célula: `Bruto = créditos debitados (auditoria); Estorno = estornos vinculados ao run; Líquido = bruto − estorno (mín. 0)`
       - usar texto/atributos em PT-BR; NÃO alterar a largura do layout da tabela além da célula (whitespace-nowrap se necessário)
    2. **`run-detail-dialog.tsx` — cabeçalho do run** (bloco 105-116): adicionar, após a linha "Custo estimado Vendeo", as linhas:
       - `Créditos: bruto {formatNumber(detail.run.creditosDebitados)} · estorno {formatNumber(detail.run.creditosEstornados)} · líquido {formatNumber(detail.run.creditosLiquidos)}`
       - `Receita {formatBRL(detail.run.receitaOpBrl)} · Resultado {formatBRL(detail.run.resultadoOpBrl)}`
       - os valores vêm do run do detalhe (mapDetailRun populou em 38-2-13); usar `detail.run.creditosDebitados !== null` como guarda para renderizar o bloco (histórico/sem dados → não renderizar linha vazia)
       - manter os placeholders F38.3 ("Custo reconciliado provider: ainda indisponível" / "Diferença: pendente") intactos
    Formatters: reutilizar os formatBRL/formatNumber já existentes em cada arquivo (não duplicar).
  </action>
  <verify>
    <automated>Select-String -Path "src\app\(app)\admin\ai-operation-costs\operation-runs-table.tsx" -Pattern "Bruto:|Estorno:|Líquido:" | Measure-Object | Select-Object -ExpandProperty Count -ge 3; Select-String -Path "src\app\(app)\admin\ai-operation-costs\run-detail-dialog.tsx" -Pattern "estorno|Receita" | Measure-Object | Select-Object -ExpandProperty Count -ge 2</automated>
  </verify>
  <done>Tabela mostra breakdown bruto/estorno/líquido + receita/resultado por run; drilldown mostra o mesmo no cabeçalho</done>
</task>

<task type="auto">
  <name>Task 3: Testes de componentes — novos labels, breakdown e cenário failed+refunded</name>
  <files>src/app/(app)/admin/ai-operation-costs/__tests__/components.test.tsx</files>
  <read_first>
    - src/app/(app)/admin/ai-operation-costs/__tests__/components.test.tsx (KpisGrid test 130-148; table test 152-167; dialog tests 175-295)
  </read_first>
  <action>
    Atualizar `components.test.tsx` para o novo contrato visual:

    1. **KpisGrid test** (130-148): substituir `screen.getByText("Créditos debitados")` por asserts dos três: "Créditos brutos", "Estornos", "Créditos líquidos"; substituir `screen.getByText("P95")` por `screen.getByText("Tempo P95 (95% das entregas)")`. Manter os demais asserts.
    2. **Table test** (152-167): substituir o assert ambíguo `getByText("20")` (créditos) por asserts do breakdown: `getByText("Bruto: 20")`, `getByText("Estorno: 0")`, `getByText("Líquido: 20")` e da linha financeira `getByText("Receita R$ 20,00 · Resultado R$ -30,00")` (fixture makeRun: receitaOpBrl 20, resultadoOpBrl −30). Se "20" ainda aparecer em outro contexto único, remover o assert antigo para não quebrar por duplicidade.
    3. **Novo teste no describe da tabela — run falho estornado (gap)**: `makeRun({ deliveryStatus: "failed", creditosDebitados: 10, creditosEstornados: 10, creditosLiquidos: 0, receitaOpBrl: 0, resultadoOpBrl: -50 })` → asserta `getByText("Líquido: 0")` e `getByText("Receita R$ 0,00 · Resultado R$ -50,00")` (custo de IA permanece — receita zera).
    4. **Dialog tests** (175-228, 232-281): os mocks `run: makeRun()` já têm os campos novos (fixture atualizada no 38-2-13); adicionar asserts do breakdown no cabeçalho: `getByText("Créditos: bruto 20 · estorno 0 · líquido 20")` e `getByText("Receita R$ 20,00 · Resultado R$ -30,00")` (texto conforme Task 2 — ajustar ao formato final escolhido). Manter os asserts de eventos e placeholders F38.3.
    Rodar o arquivo de testes.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(app)/admin/ai-operation-costs/__tests__/components.test.tsx"</automated>
  </verify>
  <done>Testes verdes: novos labels, breakdown na tabela/drilldown e cenário failed+refunded (receita 0 / resultado negativo)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Painel → API/service | UI de apresentação — NUNCA recalcula KPIs (D3/D4); consome summary/runs derivados |
| Client (tabela/dialog) → GET /api/admin/ai-operation-runs | Dados de apresentação via fetch — valores derivados no service (38-2-13) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38.2-G08 | Spoofing | KPI 'Créditos debitados' apresentado como financeiro | mitigate | Rótulos separam auditoria (brutos) de financeiro (líquidos); KPIs financeiros usam os campos líquidos do summary — nunca bruto |
| T-38.2-G09 | Info Disclosure | Breakdown exposto a não-admin | mitigate | Página/dados sob requireAdmin (já existente — inalterado); nenhum campo novo sensível |
| T-38.2-G10 | Tampering | UI recalcula valores (drift com service) | mitigate | UI apenas formata/consome campos derivados (padrão D3 mantido) — teste garante exibição de receita 0/resultado negativo sem cálculo local |
| T-38.2-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado neste plano — gate de legitimidade não se aplica |
</threat_model>

<verification>
- KpisGrid: 3 cards de crédito + P95 renomeado com tooltip (title) presente
- Tabela: breakdown bruto/estorno/líquido + receita/resultado por run; run falho estornado mostra receita R$0,00 e resultado negativo
- Drilldown: breakdown + receita/resultado no cabeçalho; placeholders F38.3 intactos
- components.test.tsx verde
</verification>

<success_criteria>
- "Créditos brutos", "Estornos", "Créditos líquidos" visíveis no painel
- "Tempo P95 (95% das entregas)" com tooltip "95% das entregas terminaram em até este tempo; os 5% mais lentos ficam fora desse corte."
- Tabela e drilldown mostram bruto/estorno/líquido por run; falha estornada: receita R$0, resultado −R$X (custo permanece)
</success_criteria>

<output>
Create `.planning/phases/38.2-admin-custos-operacionais/38-2-14-SUMMARY.md` when done
</output>

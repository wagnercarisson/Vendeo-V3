---
phase: 38.2.1-economic-snapshot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260812000001_f38_2_1_economic_snapshot.sql
autonomous: true
requirements: [F38.2.1-01, F38.2.1-12]

must_haves:
  truths:
    - "Histórico em BRL torna-se imutável por construção: cada evento de generation_events carrega as taxas vigentes na geração E a origem do valor (captured/backfilled)"
    - "Nenhum valor persistido sem origem: todo *_at_generation preenchido tem seu *_source_at_generation correspondente"
    - "Backfill é idempotente (rodar 2× não altera linhas preenchidas) e aproximado por construção (valor real nunca foi gravado antes); backfill NUNCA grava captured_at_generation"
    - "Eventos anteriores à 1ª alteração do audit caem na seed 1.00 (origem backfilled_seed); eventos posteriores caem no new_value da alteração mais recente <= created_at (origem backfilled_from_audit)"
  artifacts:
    - path: "supabase/migrations/20260812000001_f38_2_1_economic_snapshot.sql"
      provides: "4 colunas de snapshot (2 valores + 2 origens) + backfill aproximado idempotente com origem + revert"
      contains: "ADD COLUMN IF NOT EXISTS usd_brl_rate_at_generation"
  key_links:
    - from: "20260812000001_f38_2_1_economic_snapshot.sql"
      to: "public.economic_parameter_audit"
      via: "backfill LATERAL subquery (key + new_value + created_at <= generation_events.created_at) gravando origem backfilled_from_audit/backfilled_seed"
      pattern: "economic_parameter_audit"
    - from: "20260812000001_f38_2_1_economic_snapshot.sql"
      to: "public.generation_events"
      via: "UPDATE com WHERE coluna IS NULL (idempotência por coluna), preenchendo valor + origem"
      pattern: "generation_events"
---

<objective>
Criar a base contábil da F38.2.1: as colunas de snapshot econômico em `generation_events` (`usd_brl_rate_at_generation` — contábil, `credit_value_brl_at_generation` — estimado/fallback) **com origem explícita** (`usd_brl_rate_source_at_generation`, `credit_value_brl_source_at_generation`) e o backfill aproximado do histórico existente via `economic_parameter_audit` (janelas LAG com fallback para a seed `1.00`, origem `backfilled_from_audit`/`backfilled_seed`), aplicado no remoto via [BLOCKING] `supabase db push`.

Purpose: corrigir o recálculo retroativo — hoje `EconomicParameterService.getParameter` lê o valor corrente a cada consulta e aplica a todo o histórico. Com as colunas congeladas na geração, o histórico em BRL torna-se imutável por construção (D1/D4 do design).
Output: migration SQL aplicada em Local e Remote, com backfill idempotente verificado.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@openspec/changes/fase-38-2-1-economic-snapshot/design.md (D1, D4 — snapshot na escrita + backfill via audit)
@openspec/changes/fase-38-2-1-economic-snapshot/specs/economic-snapshot/spec.md (requisitos F38.2.1-01 e F38.2.1-12)
@supabase/migrations/20260810000001_f38_2_economic_parameters.sql (estrutura de economic_parameters + economic_parameter_audit — fonte do backfill)
@supabase/migrations/20260811000001_f38_2_creditos_liquidos_estornos.sql (estilo/cabeçalho de migration + bloco REVERT — padrão a replicar)

<interfaces>
Fonte de reconstrução do backfill — `public.economic_parameter_audit` (append-only, migration 20260810000001):
- colunas usadas: `key` (TEXT: 'usd_brl_rate' | 'credit_value_brl'), `new_value` (NUMERIC), `created_at` (TIMESTAMPTZ)
- sem audit anterior ao evento → valor da seed `1.00` (INSERTs da migration 20260810000001, linhas 73-79)
- Limitação documentada (D4): reconstrução é aproximada — o valor efetivamente usado na geração nunca foi persistido antes desta fase.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration — colunas de snapshot + backfill aproximado idempotente</name>
  <files>supabase/migrations/20260812000001_f38_2_1_economic_snapshot.sql</files>
  <read_first>
    - supabase/migrations/20260811000001_f38_2_creditos_liquidos_estornos.sql (cabeçalho de regras, bloco REVERT em ordem reversa, estilo de COMMENT — replicar exatamente)
    - supabase/migrations/20260810000001_f38_2_economic_parameters.sql (schema de economic_parameters/economic_parameter_audit, seeds 1.00, created_at)
    - openspec/changes/fase-38-2-1-economic-snapshot/design.md (D1: snapshot na escrita; D4: backfill via audit + fallback em leitura)
    - openspec/changes/fase-38-2-1-economic-snapshot/specs/economic-snapshot/spec.md (Requisito "Backfill aproximado na migration": CTE/LATERAL, sem loop plpgsql, idempotente, não toca linhas já preenchidas)
  </read_first>
  <action>
    Criar a migration `supabase/migrations/20260812000001_f38_2_1_economic_snapshot.sql` no estilo exato das migrations F38.2 (cabeçalho de regras comentado, blocos numerados, COMMENT, REVERT comentado em ordem reversa).

    1. Quatro `ALTER TABLE public.generation_events ADD COLUMN IF NOT EXISTS`: `usd_brl_rate_at_generation NUMERIC`, `credit_value_brl_at_generation NUMERIC`, `usd_brl_rate_source_at_generation TEXT`, `credit_value_brl_source_at_generation TEXT` (sem CHECK, sem NOT NULL — eventos delivery marker podem ficar NULL; D1/D2 do spec).
    2. Backfill aproximado por chave (D4), SEM loop plpgsql — subqueries LATERAL determinísticas:
       - Para cada linha de `generation_events` com `created_at` definido, o valor reconstituído por chave é o `new_value` da alteração mais recente em `public.economic_parameter_audit` com `a.key = '<chave>'` e `a.created_at <= generation_events.created_at` (ORDER BY created_at DESC LIMIT 1); sem alteração anterior → seed `1.00` via COALESCE.
       - **Origem no backfill:** quando o valor vem de uma janela do audit → `'backfilled_from_audit'`; quando vem da seed → `'backfilled_seed'`. O backfill **nunca** grava `'captured_at_generation'`.
       - Executar como QUATRO UPDATEs separados (valor + origem por coluna, ou UPDATE único por coluna preenchendo valor E origem), cada um com `WHERE <coluna_valor> IS NULL` — idempotência POR COLUNA (re-rodar não altera linhas preenchidas nem sobrescreve snapshot existente com dados novos do audit).
       - NÃO tocar em nenhuma outra coluna de generation_events; NÃO criar índices novos.
    3. Revert commands comentados em ordem reversa (DROP COLUMN das quatro colunas — ou documentar retenção inofensiva IF NOT EXISTS, seguindo o padrão da fase).
    4. Comentar a limitação: backfill é aproximado por construção (o valor real da geração nunca foi persistido antes desta fase — D4); eventos anteriores à 1ª alteração do audit caem na seed 1.00 com origem `backfilled_seed`; a origem `backfilled_*` comunica que o valor foi reconstruído, não capturado.
  </action>
  <verify>
    <automated>grep -c "ADD COLUMN IF NOT EXISTS usd_brl_rate_at_generation" supabase/migrations/20260812000001_f38_2_1_economic_snapshot.sql | grep -v "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - O arquivo `supabase/migrations/20260812000001_f38_2_1_economic_snapshot.sql` existe e contém as quatro strings exatas: `ADD COLUMN IF NOT EXISTS usd_brl_rate_at_generation NUMERIC`, `ADD COLUMN IF NOT EXISTS credit_value_brl_at_generation NUMERIC`, `ADD COLUMN IF NOT EXISTS usd_brl_rate_source_at_generation TEXT` e `ADD COLUMN IF NOT EXISTS credit_value_brl_source_at_generation TEXT`
    - O backfill usa `public.economic_parameter_audit` (grep por `economic_parameter_audit`) com filtro por `key` e `created_at <=` (grep por `created_at <=` ou equivalente LATERAL)
    - Origem gravada pelo backfill: grep por `backfilled_from_audit` e `backfilled_seed` presentes no arquivo; grep por `captured_at_generation` AUSENTE no backfill (o tracker grava essa origem, a migration não)
    - Idempotência por coluna: UPDATEs com `WHERE ... IS NULL` (grep por `IS NULL` em contexto de UPDATE)
    - Fallback `1.00` presente no backfill (COALESCE)
    - Sem loop plpgsql: grep por `FOR ` loop retorna vazio no arquivo
    - Bloco `REVERT` comentado presente (grep por `REVERT`)
    - Nenhuma outra tabela referenciada em UPDATE/ALTER além de public.generation_events
  </acceptance_criteria>
  <done>Migration SQL completa com as 4 colunas (2 valores + 2 origens) + backfill idempotente com origem + revert documentado</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] supabase db push — aplicar migration no remoto</name>
  <files>supabase/migrations/20260812000001_f38_2_1_economic_snapshot.sql</files>
  <read_first>
    - .planning/phases/38.2-admin-custos-operacionais/38-2-01-SUMMARY.md (precedente [BLOCKING] do push F38.2 — comandos e validação)
    - .planning/phases/38.2-admin-custos-operacionais/38-2-12-SUMMARY.md (precedente de push via pooler — Docker local indisponível)
  </read_first>
  <action>
    Aplicar a migration no banco remoto via `npx supabase db push` (padrão F38.2 — precedente 38-2-01/38-2-12: Docker local indisponível nesta máquina; o push conecta ao remoto via pooler URL do projeto linkado).

    - Definir `SUPABASE_ACCESS_TOKEN` no ambiente do comando se o push exigir (workaround não-TTY); não iniciar sessão interativa.
    - Executar `npx supabase db push --dry-run` primeiro e confirmar que APENAS a migration `20260812000001_f38_2_1_economic_snapshot.sql` seria aplicada; depois executar o push real.
    - Validar após o push (via `supabase db push` output ou REST): as colunas `usd_brl_rate_at_generation`, `credit_value_brl_at_generation`, `usd_brl_rate_source_at_generation` e `credit_value_brl_source_at_generation` existem em generation_events; rodar o push uma 2ª vez para confirmar que nada mais é aplicado (migration já registrada).
    - Se o push falhar por erro de permissão/interatividade não suprimível, registrar o bloqueio e pausar — não contornar com SQL manual direto.
  </action>
  <verify>
    <automated>npx supabase db push --dry-run 2>&1 | Select-String -Pattern "No new migrations"</automated>
  </verify>
  <acceptance_criteria>
    - `npx supabase db push` aplica a migration no remoto sem erro (exit 0)
    - `npx supabase db push --dry-run` subsequente reporta que não há novas migrations pendentes (idempotência de registro)
    - 4 colunas confirmadas no remoto (via output do push ou query REST); linhas backfilled têm origem `backfilled_*`
  </acceptance_criteria>
  <done>Migration aplicada no remoto; 4 colunas de snapshot/origem existem em generation_events</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → Supabase remoto | SQL admin executado via supabase db push com SUPABASE_ACCESS_TOKEN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38.2.1-01 | Tampering | Migration SQL (ALTER + backfill) | mitigate | Backfill idempotente por coluna (`WHERE <coluna> IS NULL`); UPDATEs restritos a generation_events; sem loop plpgsql (determinismo) |
| T-38.2.1-02 | Integrity | Backfill via economic_parameter_audit | mitigate | COALESCE com seed 1.00 (sem valor assumido sem origem — D4); origem `backfilled_*` marca reconstrução; janela `created_at <= generation_events.created_at` (valor vigente, não futuro) |
| T-38.2.1-03 | Spoofing | Push no remoto | mitigate | `SUPABASE_ACCESS_TOKEN` via env (não-TTY); `--dry-run` antes do push real; nenhuma sessão interativa |
| T-38.2.1-SC | Tampering | Instalação de pacotes | accept | Nenhum pacote novo nesta fase (SQL + push apenas) — sem gate de legitimidade |
</threat_model>

<verification>
- Migration aplicada em Local e Remote (colunas presentes)
- Backfill idempotente: 2ª execução do script/UPDATE não altera linhas preenchidas (verificação final em 38-2-1-07)
- `npx vitest run` regressão sem quebra (nenhum TS tocado neste plano)
</verification>

<success_criteria>
- Colunas `usd_brl_rate_at_generation`, `credit_value_brl_at_generation`, `usd_brl_rate_source_at_generation` e `credit_value_brl_source_at_generation` existem em generation_events (Local + Remote)
- Linhas antigas têm snapshots reconstituídos (audit LAG → `backfilled_from_audit`) ou seed 1.00 (`backfilled_seed`); linhas já preenchidas não são tocadas; nenhum valor sem origem
- Migration registrada e idempotente (dry-run subsequente: "No new migrations")
</success_criteria>

<output>
Create `.planning/phases/38.2.1-economic-snapshot/38-2-1-01-SUMMARY.md` when done
</output>

---
phase: 38.2.1-economic-snapshot
plan: 02
type: execute
wave: 2
depends_on: [38.2.1-01]
files_modified:
  - src/lib/ai-cost/types.ts
  - src/lib/ai-cost/tracker.ts
  - src/lib/ai-cost/__tests__/tracker.test.ts
  - src/lib/visual-signature/types.ts
  - src/lib/visual-signature/generation-events.ts
  - src/app/api/campaign/generate-image/route.ts
  - src/app/api/campaign/generate-image/__tests__/route.test.ts
  - src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts
  - src/app/api/store/[id]/visual-signature/generate-without-logo/__tests__/generate-route.test.ts
  - src/app/api/store/[id]/brand-profile/generate-without-logo/route.ts
  - src/app/api/store/[id]/brand-profile/generate-without-logo/__tests__/generate-route.test.ts
  - src/app/api/store/[id]/brand-profile/infer/route.ts
  - src/app/api/store/[id]/brand-profile/infer/__tests__/route.test.ts
  - src/app/api/store/[id]/brand-profile/realign/route.ts
  - src/app/api/store/[id]/brand-profile/realign/__tests__/realign-route.test.ts
autonomous: true
requirements: [F38.2.1-02, F38.2.1-03]

must_haves:
  truths:
    - "AiCostTracker.record persiste usd_brl_rate_at_generation, credit_value_brl_at_generation E as origens usd_brl_rate_source_at_generation/credit_value_brl_source_at_generation do evento (origem 'captured_at_generation' quando valor presente — daqui para frente, best-effort)"
    - "Callers de início de run resolvem os parâmetros UMA vez no início (EconomicParameterService.getParameter) e propagam o snapshot a todas as chamadas filhas"
    - "Falha na resolução do snapshot NÃO bloqueia geração — eventos gravam snapshots NULL (fallback legacy em leitura)"
    - "O tracker NUNCA grava origem backfilled_*/fallback — valores persistidos pelo tracker têm origem captured_at_generation"
  artifacts:
    - path: "src/lib/ai-cost/types.ts"
      provides: "AiCostEvent com usdBrlRateAtGeneration/creditValueBrlAtGeneration e origens opcionais"
      contains: "usdBrlRateAtGeneration?: number | null"
    - path: "src/lib/ai-cost/tracker.ts"
      provides: "record persiste as 4 colunas de snapshot/origem (origem captured_at_generation)"
      contains: "usd_brl_rate_at_generation"
    - path: "src/lib/visual-signature/generation-events.ts"
      provides: "insertGenerationEvent repassa snapshots ao tracker (delegação D11)"
      contains: "creditValueBrlAtGeneration"
  key_links:
    - from: "generate-image/route.ts"
      to: "EconomicParameterService"
      via: "getParameter('usd_brl_rate')/getParameter('credit_value_brl') no início do run (try/catch best-effort)"
      pattern: "EconomicParameterService"
    - from: "generate-image/route.ts recordCall"
      to: "AiCostTracker.record"
      via: "eventos das chamadas filhas carregam usdBrlRateAtGeneration/creditValueBrlAtGeneration do snapshot do run"
      pattern: "usdBrlRateAtGeneration"
---

<objective>
Implementar o caminho de escrita do snapshot: `AiCostEvent` ganha `usdBrlRateAtGeneration`/`creditValueBrlAtGeneration` **e origens** `usdBrlRateSourceAtGeneration`/`creditValueBrlSourceAtGeneration` (opcionais — backward-compatible), `AiCostTracker.record` persiste as quatro colunas (origem `captured_at_generation` quando valor presente), e todos os callers de início de run (generate-image, VS generate-without-logo ×2, brand-profile generate-without-logo/infer/realign ×5, generation-events.ts) resolvem os parâmetros econômicos UMA vez no início do run e propagam o snapshot às chamadas filhas (padrão telemetria D3).

Purpose: congelar as taxas no momento da geração (D1/D3) — o histórico em BRL torna-se imutável por construção; falha na resolução → snapshot NULL → fallback legacy explícito, geração nunca bloqueada.
Output: tipos + tracker + callers propagando snapshot, com testes de escrita e de propagação.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@openspec/changes/fase-38-2-1-economic-snapshot/design.md (D1, D2, D3 — snapshot na escrita, semântica separada, resolução no início do run e propagação)
@openspec/changes/fase-38-2-1-economic-snapshot/specs/ai-cost-tracker/spec.md (AiCostEvent + AiCostTracker — cenários de snapshot)
@openspec/changes/fase-38-2-1-economic-snapshot/specs/economic-snapshot/spec.md (persistência pelo tracker; falha não bloqueia geração)
@src/lib/ai-cost/tracker.ts (estado atual — record com 4 campos de confiança F38.2)
@src/lib/ai-cost/types.ts (estado atual — AiCostEvent)
@src/lib/economic/economic-parameter-service.ts (getParameter — fail-open 1.00 / fail-closed EconomicParameterUnavailableError)
@src/lib/visual-signature/generation-events.ts (insertGenerationEvent delegando ao tracker)
@src/app/api/campaign/generate-image/route.ts (linhas 46, 476-519 — startRun + recordCall)

<interfaces>
Snapshot a propagar (D2/D3) — resolvido UMA vez no início do run:
- `usdBrlRateAtGeneration?: number | null` — snapshot CONTÁBIL do câmbio na geração
- `creditValueBrlAtGeneration?: number | null` — snapshot ESTIMATIVO/fallback do valor do crédito na geração
- `usdBrlRateSourceAtGeneration?: "captured_at_generation" | null` — origem (o tracker grava `captured_at_generation` quando valor presente)
- `creditValueBrlSourceAtGeneration?: "captured_at_generation" | null` — origem (idem)

Padrão de resolução best-effort (a replicar em cada caller, após cada `startRun(...)`):
- `new EconomicParameterService().getParameter("usd_brl_rate")` + `getParameter("credit_value_brl")` via Promise.all em try/catch
- Sucesso → `{ usdBrlRateAtGeneration: usd.value, creditValueBrlAtGeneration: credit.value, usdBrlRateSourceAtGeneration: "captured_at_generation", creditValueBrlSourceAtGeneration: "captured_at_generation" }`
- Falha (EconomicParameterUnavailableError ou outro) → log + snapshots e origens NULL — NUNCA bloqueia a geração

Pontos de startRun (estado atual verificado):
- generate-image/route.ts:46 (campaign_delivery; recordCall em :498 grava eventos)
- visual-signature/generate-without-logo/route.ts:236 e :365 (visual_signature; events call-level em :269; delivery marker via insertGenerationEvent em :510 e :574)
- brand-profile/generate-without-logo/route.ts:61 (record em :82 e :134)
- brand-profile/infer/route.ts:73 (record em :101 e :125)
- brand-profile/realign/route.ts:193, :394, :612 (records em :39/:73 e outros)
- visual-signature/generation-events.ts:9 (insertGenerationEvent — delega ao tracker; GenerationEventInsert em src/lib/visual-signature/types.ts:141)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AiCostEvent + tracker.record persistem os snapshots</name>
  <files>src/lib/ai-cost/types.ts, src/lib/ai-cost/tracker.ts, src/lib/ai-cost/__tests__/tracker.test.ts</files>
  <read_first>
    - src/lib/ai-cost/types.ts (AiCostEvent atual — campos opcionais no fim da interface)
    - src/lib/ai-cost/tracker.ts (record atual — insert com 4 campos de confiança)
    - src/lib/ai-cost/__tests__/tracker.test.ts (estrutura dos testes de record — baseEvent + asserts de insert)
    - openspec/changes/fase-38-2-1-economic-snapshot/specs/ai-cost-tracker/spec.md (cenários: "AiCostEvent carrega snapshot econômico da geração", "AiCostEvent sem snapshot disponível", "record grava todas as novas colunas")
  </read_first>
  <behavior>
    - Test 1: record com usdBrlRateAtGeneration=5.20 e creditValueBrlAtGeneration=2.00 insere as duas colunas de valor E as origens `captured_at_generation` no objeto passado ao .insert() (grep no mock/captured insert)
    - Test 2: record sem os campos (undefined) insere NULL nas 4 colunas (valor + origem) — sem erro
    - Test 3: delivery marker (sem cost/tokens) com snapshot NULL grava normalmente (snapshots opcionais não afetam o marcador)
  </behavior>
  <action>
    Em `src/lib/ai-cost/types.ts`: adicionar ao final da interface `AiCostEvent` os campos `usdBrlRateAtGeneration?: number | null`, `creditValueBrlAtGeneration?: number | null`, `usdBrlRateSourceAtGeneration?: "captured_at_generation" | null` e `creditValueBrlSourceAtGeneration?: "captured_at_generation" | null`, com comentário JSDoc da semântica separada (contábil vs estimativo/fallback; D2) — opcionais, backward-compatible (nenhum caller existente quebra; delivery marker não exige).

    Em `src/lib/ai-cost/tracker.ts`: no objeto passado a `this.client.from("generation_events").insert({...})`, adicionar `usd_brl_rate_at_generation: event.usdBrlRateAtGeneration ?? null`, `credit_value_brl_at_generation: event.creditValueBrlAtGeneration ?? null`, `usd_brl_rate_source_at_generation: event.usdBrlRateAtGeneration != null ? "captured_at_generation" : null` e `credit_value_brl_source_at_generation: event.creditValueBrlAtGeneration != null ? "captured_at_generation" : null` ao lado dos campos de confiança existentes. Best-effort preservado: snapshot NULL não bloqueia a gravação (o try/catch do record já cobre). **A origem `captured_at_generation` é derivada pelo tracker da presença do valor — nunca `backfilled_*`/`fallback`.**

    Atualizar/estender `src/lib/ai-cost/__tests__/tracker.test.ts`: testes de record assertando as 4 colunas no objeto de insert (capturado via mock do client), incluindo o caso sem snapshot (NULL + origem NULL) e delivery marker.
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai-cost/__tests__/tracker.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `AiCostEvent` em src/lib/ai-cost/types.ts contém `usdBrlRateAtGeneration?: number | null`, `creditValueBrlAtGeneration?: number | null` e as origens `usdBrlRateSourceAtGeneration`/`creditValueBrlSourceAtGeneration`
    - `record` em tracker.ts passa as 4 colunas ao insert (grep nas strings `usd_brl_rate_at_generation`, `credit_value_brl_at_generation`, `usd_brl_rate_source_at_generation`, `credit_value_brl_source_at_generation`); origem = "captured_at_generation" quando valor presente
    - Tracker tests verdes (com asserts das 4 colunas no insert, incluindo caso NULL)
    - `npm run typecheck` limpo
  </acceptance_criteria>
  <done>record persiste snapshots com origem captured_at_generation; contrato de escrita coberto por teste</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: generate-image + VS generate-without-logo propagam snapshot (campaign_delivery + visual_signature)</name>
  <files>src/app/api/campaign/generate-image/route.ts, src/app/api/campaign/generate-image/__tests__/route.test.ts, src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts, src/app/api/store/[id]/visual-signature/generate-without-logo/__tests__/generate-route.test.ts, src/lib/visual-signature/generation-events.ts, src/lib/visual-signature/types.ts</files>
  <read_first>
    - src/app/api/campaign/generate-image/route.ts (linhas 40-50 startRun; 476-519 recordCall — TODOS os eventos do run passam por recordCall)
    - src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts (linhas 231-291 run/pendingCalls/flushCallEvents; 358-366 retry = novo startRun; 500-580 insertGenerationEvent delivery)
    - src/lib/visual-signature/generation-events.ts (delegação ao tracker)
    - src/lib/visual-signature/types.ts (GenerationEventInsert:141 — adicionar campos opcionais)
    - src/app/api/campaign/generate-image/__tests__/route.test.ts (MockAiCostTracker — captura capturedEvents; mock de supabase/server)
    - src/app/api/store/[id]/visual-signature/generate-without-logo/__tests__/generate-route.test.ts (mock de AiCostTracker + insertGenerationEvent)
  </read_first>
  <behavior>
    - Test 1: generate-image com economic_parameters retornando 5.20/2.00 → todos os eventos capturados (recordCall) carregam usdBrlRateAtGeneration=5.20 e creditValueBrlAtGeneration=2.00 com origens captured_at_generation
    - Test 2: generate-image com falha na leitura de economic_parameters (mock rejeita) → eventos com snapshots null e o pipeline NÃO é bloqueado (resposta 200/stream normal)
    - Test 3: VS generate-without-logo — insertGenerationEvent (delivery marker) carrega os snapshots e origens quando resolvidos
  </behavior>
  <action>
    Em `src/app/api/campaign/generate-image/route.ts`:
    - Logo após `startRun("campaign_delivery")` (linha 46), resolver o snapshot best-effort UMA vez (padrão do bloco <interfaces>: Promise.all de getParameter("usd_brl_rate")/getParameter("credit_value_brl") em try/catch → `{ usdBrlRateAtGeneration, creditValueBrlAtGeneration, usdBrlRateSourceAtGeneration: "captured_at_generation", creditValueBrlSourceAtGeneration: "captured_at_generation" }` com null em falha). Importar `EconomicParameterService` de "@/lib/economic/economic-parameter-service".
    - No corpo do `recordCall` (linha 498), adicionar os 4 campos ao objeto passado a `tracker.record({...})` (valores/origens do snapshot do run — o recordCall já fecha sobre operationRunId/traceId/storeId, seguir o mesmo padrão).
    - Atualizar o teste de rota: mock de `@/lib/economic/economic-parameter-service` (getParameter resolvendo 5.20/2.00; e cenário de rejeição) + asserts em capturedEvents de que os eventos carregam os snapshots e origens; cenário de falha asserta snapshots null e resposta sem 5xx.

    Em `src/lib/visual-signature/types.ts`: adicionar a `GenerationEventInsert` os campos opcionais `usd_brl_rate_at_generation?: number | null`, `credit_value_brl_at_generation?: number | null`, `usd_brl_rate_source_at_generation?: "captured_at_generation" | null` e `credit_value_brl_source_at_generation?: "captured_at_generation" | null` (junto dos campos F38.1 existentes).

    Em `src/lib/visual-signature/generation-events.ts`: no `tracker.record({...})`, repassar os 4 campos do insert ao evento (delegação D11 — contrato do insert mantido; origem derivada da presença do valor).

    Em `src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts`:
    - Após cada `startRun("visual_signature")` (linhas 236 e 365 — o retry é um NOVO run), resolver o snapshot best-effort UMA vez para aquele run.
    - No `flushCallEvents` (record em :269), adicionar os 4 campos ao evento (snapshot/origem do run vigente).
    - Nas chamadas `insertGenerationEvent` (linhas 510 e 574), adicionar os 4 campos ao objeto GenerationEventInsert (valores do run vigente).
    - Atualizar os testes de rota do VS (mock do service econômico + asserts nos mocks de record/insertGenerationEvent).
  </action>
  <verify>
    <automated>npx vitest run src/app/api/campaign/generate-image/__tests__/route.test.ts src/app/api/store/[id]/visual-signature/generate-without-logo/__tests__/generate-route.test.ts src/lib/ai-cost/__tests__/tracker.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - generate-image/route.ts resolve o snapshot após startRun e o injeta em TODOS os recordCall (grep por `usdBrlRateAtGeneration` na rota)
    - VS route resolve após os 2 startRun e injeta em flushCallEvents e nas 2 chamadas insertGenerationEvent (grep por `usd_brl_rate_at_generation` na rota)
    - generation-events.ts repassa os 4 campos ao tracker (grep)
    - GenerationEventInsert em types.ts tem os 4 campos opcionais
    - Testes verdes: propagação (5.20/2.00 + origens) + falha não bloqueia geração
    - `npm run typecheck` limpo
  </acceptance_criteria>
  <done>Campanha e VS gravam snapshots com origem nos eventos call-level e delivery; falha de resolução não bloqueia</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: brand-profile rotas propagam snapshot (5 pontos de startRun)</name>
  <files>src/app/api/store/[id]/brand-profile/generate-without-logo/route.ts, src/app/api/store/[id]/brand-profile/generate-without-logo/__tests__/generate-route.test.ts, src/app/api/store/[id]/brand-profile/infer/route.ts, src/app/api/store/[id]/brand-profile/infer/__tests__/route.test.ts, src/app/api/store/[id]/brand-profile/realign/route.ts, src/app/api/store/[id]/brand-profile/realign/__tests__/realign-route.test.ts</files>
  <read_first>
    - src/app/api/store/[id]/brand-profile/generate-without-logo/route.ts (startRun :61; records :82 e :134)
    - src/app/api/store/[id]/brand-profile/infer/route.ts (startRun :73; records :101 e :125)
    - src/app/api/store/[id]/brand-profile/realign/route.ts (startRun :193, :394, :612; records :39/:73 e demais chamadas)
    - __tests__ correspondentes de cada rota (mocks de AiCostTracker + supabase)
    - src/app/api/campaign/generate-image/route.ts (padrão de resolução já aplicado na Task 2 — replicar)
  </read_first>
  <behavior>
    - Test 1: cada rota brand-profile, com economic_parameters retornando 5.20/2.00, grava eventos com os snapshots e origens propagados
    - Test 2: falha de leitura → snapshots null e resposta da rota inalterada (sem 5xx novo)
  </behavior>
  <action>
    Em cada uma das 3 rotas de brand-profile (`generate-without-logo`, `infer`, `realign`):
    - Resolver o snapshot best-effort UMA vez por run, imediatamente após cada `startRun(...)` (realign tem 3 pontos; gerar novo snapshot a cada startRun — valores vigentes naquele momento). Importar `EconomicParameterService` de "@/lib/economic/economic-parameter-service".
    - Adicionar os 4 campos (`usdBrlRateAtGeneration`/`creditValueBrlAtGeneration` + origens captured) a TODOS os objetos passados a `tracker.record({...})` naquela rota.
    - Atualizar os testes de rota: mock do service econômico + asserts de propagação nos eventos capturados (valores + origens); cenário de falha asserta null + fluxo intacto.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/store/[id]/brand-profile/generate-without-logo/__tests__/generate-route.test.ts src/app/api/store/[id]/brand-profile/infer/__tests__/route.test.ts src/app/api/store/[id]/brand-profile/realign/__tests__/realign-route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - As 3 rotas brand-profile contêm a resolução do snapshot após cada startRun e injetam os 4 campos em todos os records (grep por `usdBrlRateAtGeneration` em cada rota)
    - Testes das 3 rotas verdes (propagação + falha não bloqueia)
    - `npm run typecheck` limpo
  </acceptance_criteria>
  <done>Brand-profile (5 runs) grava snapshots com origem; nenhuma geração é bloqueada por falha de resolução</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente → rotas de geração | Input não confiável cruza a fronteira HTTP (validação zod existente — inalterada) |
| rotas → generation_events | Escrita via AiCostTracker (service role, server-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38.2.1-04 | Spoofing | Snapshots no AiCostEvent | mitigate | Snapshots resolvidos EXCLUSIVAMENTE server-side via EconomicParameterService — nenhum valor de snapshot vem do corpo da requisição (body ignorado para esses campos) |
| T-38.2.1-05 | Denial of Service | Resolução de parâmetros no início do run | mitigate | 1× por run (não N+1 por chamada — D3); try/catch best-effort → falha logada, geração não bloqueada |
| T-38.2.1-06 | Elevation of Privilege | EconomicParameterService em rotas de geração | mitigate | Service é leitura-only (nunca altera a tabela); escrita continua via RPC admin_set_economic_parameter (service_role) |
| T-38.2.1-SC | Tampering | Instalação de pacotes | accept | Nenhum pacote novo nesta fase — sem gate de legitimidade |
</threat_model>

<verification>
- `npx vitest run src/lib/ai-cost/__tests__/tracker.test.ts` + testes das 5 rotas + VS verdes
- `npm run typecheck` limpo
- Regressão rápida: `npx vitest run` (sem quebras por contrato opcional — backward-compatible)
</verification>

<success_criteria>
- AiCostTracker.record persiste os 2 snapshots (daqui para frente)
- Todos os callers de início de run resolvem 1× e propagam às chamadas filhas (grep confirma em 5 rotas + generation-events)
- Nenhum cenário de falha de resolução bloqueia geração (testes)
- Contrato AiCostEvent/GenerationEventInsert backward-compatible (todos os callers existentes compilam sem edição obrigatória)
</success_criteria>

<output>
Create `.planning/phases/38.2.1-economic-snapshot/38-2-1-02-SUMMARY.md` when done
</output>

---
phase: 38.2.1-economic-snapshot
plan: 03
type: execute
wave: 2
depends_on: [38.2.1-01]
files_modified:
  - supabase/migrations/20260812000002_f38_2_1_rpcs_snapshot.sql
autonomous: true
requirements: [F38.2.1-08]

must_haves:
  truths:
    - "admin_get_ai_operation_runs expõe usd_brl_rate_at_generation/credit_value_brl_at_generation E as origens usd_brl_rate_source_at_generation/credit_value_brl_source_at_generation por run (do evento de referência do run — primeiro evento com snapshot)"
    - "admin_get_ai_operation_run_events expõe os snapshots e origens por evento call-level"
    - "Contrato backward-compatible: campos ADICIONADOS ao JSON, nada removido; RPCs continuam sem derivar BRL (dados brutos — D1); origem sempre acompanha o valor"
  artifacts:
    - path: "supabase/migrations/20260812000002_f38_2_1_rpcs_snapshot.sql"
      provides: "CREATE OR REPLACE dos 2 RPCs com campos de snapshot e origem"
      contains: "usd_brl_rate_at_generation"
  key_links:
    - from: "admin_get_ai_operation_runs"
      to: "public.generation_events.usd_brl_rate_at_generation"
      via: "subquery do 1º evento com snapshot (ORDER BY created_at ASC LIMIT 1), incluindo a origem"
      pattern: "usd_brl_rate_at_generation"
    - from: "admin_get_ai_operation_run_events"
      to: "public.generation_events.credit_value_brl_at_generation"
      via: "jsonb_build_object por evento, incluindo a origem"
      pattern: "credit_value_brl_at_generation"
---

<objective>
Estender os RPCs de operation runs (F38.2) para expor os snapshots econômicos **e suas origens**: `admin_get_ai_operation_runs` (lista — snapshot + origem do evento de referência do run) e `admin_get_ai_operation_run_events` (detalhe — snapshot + origem por evento call-level), via nova migration com CREATE OR REPLACE + [BLOCKING] push.

Purpose: D6 — o service (plano 04) deriva BRL com taxa snapshotada por run/evento e expõe a origem; os RPCs continuam entregando dados brutos (USD + créditos + snapshots + origens), sem derivar BRL (D1/D5). Contrato backward-compatible (campos adicionados, nada removido).
Output: RPCs atualizados no remoto com snapshots expostos.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@openspec/changes/fase-38-2-1-economic-snapshot/design.md (D6 — RPCs expõem snapshots; D1/D5 — derivação nunca no SQL)
@openspec/changes/fase-38-2-1-economic-snapshot/specs/economic-snapshot/spec.md (Requisito "RPCs de operation runs expõem os snapshots por run")
@supabase/migrations/20260811000001_f38_2_creditos_liquidos_estornos.sql (estado ATUAL dos 2 RPCs — base do CREATE OR REPLACE; cabeçalho, REVOKE/GRANT, COMMENT, REVERT)
@supabase/migrations/20260810000003_f38_2_operation_run_rpcs.sql (estrutura original — CTEs call_level/runs/filtered_runs/summary e events/run_agg)

<interfaces>
Contrato atual (20260811000001) — campos a ADICIONAR ao JSON sem remover nada:
- Lista (`admin_get_ai_operation_runs`): cada run no array `runs` + nada no summary (summary permanece com custo/duração/créditos)
- Detalhe (`admin_get_ai_operation_run_events`): `run` (resumo) + cada item de `events`
- Campos novos por run/evento: `usd_brl_rate_at_generation`, `credit_value_brl_at_generation` **e as origens** `usd_brl_rate_source_at_generation`, `credit_value_brl_source_at_generation` (origem sempre acompanha o valor — nunca valor sem procedência)
- `admin_get_ai_costs` (F38.1) e `admin_get_metrics` (F28): INALTERADOS — não tocar

Evento de referência do run (D6): para runs com múltiplos eventos de snapshots diferentes (parâmetro alterado no meio do run — caso raro), o RPC expõe o snapshot do PRIMEIRO evento do run com snapshot (ORDER BY created_at ASC LIMIT 1 sobre eventos com a coluna NOT NULL); o service usa esse valor como o "da geração".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration — CREATE OR REPLACE dos RPCs expondo snapshots</name>
  <files>supabase/migrations/20260812000002_f38_2_1_rpcs_snapshot.sql</files>
  <read_first>
    - supabase/migrations/20260811000001_f38_2_creditos_liquidos_estornos.sql (corpo ATUAL completo dos 2 RPCs — copiar como base e adicionar campos)
    - openspec/changes/fase-38-2-1-economic-snapshot/design.md (D6 — snapshot do primeiro evento com snapshot por run)
  </read_first>
  <action>
    Criar `supabase/migrations/20260812000002_f38_2_1_rpcs_snapshot.sql` com CREATE OR REPLACE dos DOIS RPCs da F38.2, copiando integralmente o corpo atual de 20260811000001 (CTEs, filtros, paginação, evidências de segmento, badges, créditos bruto/estornado/líquido) e ADICIONANDO os campos de snapshot:

    1. **Lista `admin_get_ai_operation_runs`:**
       - Na CTE `call_level`, adicionar à projeção: `ge.usd_brl_rate_at_generation`, `ge.credit_value_brl_at_generation`, `ge.usd_brl_rate_source_at_generation`, `ge.credit_value_brl_source_at_generation`.
       - Na CTE `runs`, adicionar por run: subquery do 1º evento com snapshot — para `usd_brl_rate_at_generation`: `(SELECT cl_s.usd_brl_rate_at_generation FROM call_level cl_s WHERE cl_s.operation_run_id = cl.operation_run_id AND cl_s.usd_brl_rate_at_generation IS NOT NULL ORDER BY cl_s.created_at ASC LIMIT 1)`; idem para `credit_value_brl_at_generation` **e as origens correspondentes (da mesma linha do 1º evento)**.
       - No `jsonb_build_object` do `jsonb_agg` de runs, adicionar `'usd_brl_rate_at_generation', sub.usd_brl_rate_at_generation`, `'usd_brl_rate_source_at_generation', sub.usd_brl_rate_source_at_generation`, `'credit_value_brl_at_generation', sub.credit_value_brl_at_generation` e `'credit_value_brl_source_at_generation', sub.credit_value_brl_source_at_generation`.
       - Summary/paginação/total: INALTERADOS.
    2. **Detalhe `admin_get_ai_operation_run_events`:**
       - Na CTE `events`, adicionar `ge.usd_brl_rate_at_generation`, `ge.credit_value_brl_at_generation` **e as origens** `ge.usd_brl_rate_source_at_generation`, `ge.credit_value_brl_source_at_generation`.
       - No `jsonb_build_object` de cada evento do `jsonb_agg`, adicionar os 4 campos (snapshot + origem no momento da chamada).
       - No `jsonb_build_object` do `run`, adicionar os 4 campos do 1º evento com snapshot: `(SELECT e.usd_brl_rate_at_generation FROM events e WHERE e.usd_brl_rate_at_generation IS NOT NULL ORDER BY e.created_at ASC LIMIT 1)` (e equivalentes para as origens e credit_value).
    3. Manter: REVOKE/GRANT service_role (blocos idênticos aos atuais), parâmetros p_* vinculados, janela 365d, COMMENT atualizado mencionando os campos de snapshot/origem, REVERT comentado em ordem reversa.
    4. NÃO alterar: `admin_get_ai_costs`, `admin_get_metrics`, views admin_ai_*, `admin_set_economic_parameter`, nenhuma coluna de credit_transactions.
  </action>
  <verify>
    <automated>grep -c "usd_brl_rate_at_generation" supabase/migrations/20260812000002_f38_2_1_rpcs_snapshot.sql | grep -v "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo contém as strings `usd_brl_rate_at_generation`, `credit_value_brl_at_generation`, `usd_brl_rate_source_at_generation` e `credit_value_brl_source_at_generation` em contexto de RPC (grep)
    - Contém `CREATE OR REPLACE FUNCTION public.admin_get_ai_operation_runs` e `...admin_get_ai_operation_run_events`
    - REVOKE/GRANT presentes (grep por `GRANT EXECUTE ON FUNCTION public.admin_get_ai_operation_runs` com `TO service_role`)
    - Sem alteração de assinatura dos RPCs (mesmos parâmetros p_* da versão atual)
    - Não referencia `admin_get_ai_costs` nem `admin_get_metrics` (grep ausente em contexto de chamada)
    - Bloco REVERT comentado presente
  </acceptance_criteria>
  <done>Migration com os 2 RPCs estendidos (4 campos por run/evento: 2 valores + 2 origens; contrato preservado)</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] supabase db push — aplicar migration no remoto</name>
  <files>supabase/migrations/20260812000002_f38_2_1_rpcs_snapshot.sql</files>
  <read_first>
    - .planning/phases/38.2-admin-custos-operacionais/38-2-12-SUMMARY.md (precedente de push via pooler + validação REST)
  </read_first>
  <action>
    Aplicar a migration no remoto via `npx supabase db push` (padrão 38-2-12: Docker local indisponível; push via pooler URL do projeto linkado).

    - `SUPABASE_ACCESS_TOKEN` via env (workaround não-TTY); sem sessão interativa.
    - `npx supabase db push --dry-run` primeiro (confirmar que APENAS 20260812000002 seria aplicada) → push real.
    - Validar: chamar os RPCs via cliente service_role (ou confirmar via output do push) que os runs retornam `usd_brl_rate_at_generation`/`credit_value_brl_at_generation` **e as origens** `usd_brl_rate_source_at_generation`/`credit_value_brl_source_at_generation`; dry-run subsequente "No new migrations".
    - Se o push exigir interatividade não suprimível, registrar bloqueio e pausar (sem SQL manual direto).
  </action>
  <verify>
    <automated>npx supabase db push --dry-run 2>&1 | Select-String -Pattern "No new migrations"</automated>
  </verify>
  <acceptance_criteria>
    - `npx supabase db push` exit 0; dry-run subsequente "No new migrations"
    - RPCs respondem com os 4 campos de snapshot/origem (validação via cliente service_role — JSON contém `usd_brl_rate_at_generation` e `usd_brl_rate_source_at_generation`)
  </acceptance_criteria>
  <done>RPCs atualizados no remoto expondo snapshots e origens (contrato backward-compatible)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service → RPCs admin | Chamadas RPC SECURITY DEFINER, exclusivamente via service_role (REVOKE de anon/authenticated) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38.2.1-07 | Information Disclosure | RPCs com snapshots | mitigate | REVOKE/GRANT preservados (service_role only — blocos copiados da migration atual); snapshots são dados internos de operação |
| T-38.2.1-08 | Tampering | CREATE OR REPLACE dos RPCs | mitigate | Corpo integralmente baseado em 20260811000001 (estado verificado); alterações exclusivamente aditivas no JSONB; COMMENT e REVERT documentados |
| T-38.2.1-09 | Integrity | Evento de referência do run | mitigate | Snapshot do 1º evento com snapshot (ORDER BY created_at ASC LIMIT 1) — determinístico; caso raro de parâmetro alterado no meio do run documentado (D6) |
| T-38.2.1-SC | Tampering | Instalação de pacotes | accept | Nenhum pacote novo nesta fase — sem gate de legitimidade |
</threat_model>

<verification>
- RPCs estendidos respondem com snapshots (banco real — validação pós-push)
- `admin_get_ai_costs`/`admin_get_metrics` inalterados (grep nos diffs — nenhuma referência)
- Sem regressão TS (nenhum código TS tocado neste plano)
</verification>

<success_criteria>
- Lista expõe snapshots e origens por run (evento de referência); detalhe expõe por evento e no run
- Contrato backward-compatible: nenhum campo removido (diff aditivo)
- Migration registrada no remoto; dry-run subsequente limpo
</success_criteria>

<output>
Create `.planning/phases/38.2.1-economic-snapshot/38-2-1-03-SUMMARY.md` when done
</output>

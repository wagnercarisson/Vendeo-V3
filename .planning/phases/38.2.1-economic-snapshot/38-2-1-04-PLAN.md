---
phase: 38.2.1-economic-snapshot
plan: 04
type: execute
wave: 3
depends_on: [38.2.1-01, 38.2.1-03]
files_modified:
  - src/lib/ai-cost/operation-runs-service.ts
  - src/lib/ai-cost/__tests__/operation-runs-service.test.ts
autonomous: true
requirements: [F38.2.1-04, F38.2.1-05, F38.2.1-06, F38.2.1-07, F38.2.1-09, F38.2.1-13]

must_haves:
  truths:
    - "custoBrl = custoUsdTotal × usd_brl_rate_at_generation (snapshot do run; fallback corrente explícito com usdBrlRateSource)"
    - "receitaEstimadaBrl = creditosLiquidos × credit_value_brl_at_generation (estorno descontado; nunca 'receita real')"
    - "resultadoEstimadoBrl/margemEstimadaPct derivados; margem null quando receita estimada ≤ 0"
    - "Origem do valor exposta com 4 valores: captured_at_generation / backfilled_from_audit / backfilled_seed / economic_parameter_fallback — um valor backfilled NUNCA é apresentado como captured"
    - "Fallback legacy EXPLÍCITO: sem valor persistido → parâmetro corrente + creditValueSource='economic_parameter_fallback' + revenueEstimationNote='estimated_from_admin_credit_value'"
    - "deriveSummary soma BRL JÁ derivados por run (não re-deriva do total USD com taxa única — D5)"
  artifacts:
    - path: "src/lib/ai-cost/operation-runs-service.ts"
      provides: "OperationRun/OperationRunsSummary renomeados (estimado) + snapshots + origens + notes"
      contains: "receitaEstimadaBrl"
    - path: "src/lib/ai-cost/__tests__/operation-runs-service.test.ts"
      provides: "Testes de snapshot/fallback/estabilidade temporal/mistura de taxas/origem"
      contains: "creditValueSource"
  key_links:
    - from: "deriveBrl"
      to: "RawOperationRun.usd_brl_rate_at_generation"
      via: "toNumber(raw.usd_brl_rate_at_generation) ?? current.usdBrlRate; origem = raw.usd_brl_rate_source_at_generation ?? 'economic_parameter_fallback'"
      pattern: "usd_brl_rate_at_generation"
    - from: "deriveSummary"
      to: "OperationRun.custoBrl/receitaEstimadaBrl"
      via: "sumValues(runs.map(r => r.custoBrl)) — soma dos já derivados"
      pattern: "sumValues\\(runs.map"
---

<objective>
Refatorar o `OperationRunsService` (F38.2) para derivar BRL com os SNAPSHOTS econômicos por run/evento quando disponíveis, com fallback legacy explícito e marcado, e renomear o contrato para a nomenclatura estimada (`receitaEstimadaBrl`/`resultadoEstimadoBrl`/`margemEstimadaPct` + `creditValueSource`/`revenueEstimationNote`/`usdBrlRateSource`).

Purpose: D2/D5/D8 — congelar a derivação no momento da geração (snapshot), nunca apresentar receita real, e somar BRL já derivados por run (taxas distintas não se misturam). Este plano define o CONTRATO que a API (plano 05) e a UI (planos 05/06) consomem.
Output: service com snapshot + nomenclatura estimada, 20+ testes atualizados/novos.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@openspec/changes/fase-38-2-1-economic-snapshot/design.md (D2 semântica separada; D5 derivação no service; D8 rename estimado; D6 snapshot do 1º evento)
@openspec/changes/fase-38-2-1-economic-snapshot/specs/ai-operation-runs-api/spec.md (GET lista + detalhe — contratos com snapshots e nomenclatura estimada)
@openspec/changes/fase-38-2-1-economic-snapshot/specs/economic-snapshot/spec.md (contrato de nomenclatura — tabela de campos/fórmulas; nomes proibidos)
@src/lib/ai-cost/operation-runs-service.ts (estado atual — 897 linhas; deriveBrl :362, mapRun :700, mapDetailRun :602, mapEvent :641, deriveSummary :856, deriveAggregations :748, getEconomicParams :550)
@src/lib/ai-cost/__tests__/operation-runs-service.test.ts (20+41 testes atuais — makeRawRun fixtures, fake economic injetado)

<interfaces>
Contrato pós-mudança (D8 — nomes PROIBIDOS: receitaRealBrl/resultadoRealBrl/margemRealPct):

```
OperationRun (lista e run do detalhe):
  custoBrl, creditosDebitados, creditosEstornados, creditosLiquidos   (inalterados)
  receitaEstimadaBrl   ← rename de receitaOpBrl
  resultadoEstimadoBrl ← rename de resultadoOpBrl
  margemEstimadaPct    ← rename de margemOpPct
  usdBrlRateAtGeneration: number | null
  creditValueBrlAtGeneration: number | null
  usdBrlRateSource: "captured_at_generation" | "backfilled_from_audit" | "backfilled_seed" | "economic_parameter_fallback"
  creditValueSource: "captured_at_generation" | "backfilled_from_audit" | "backfilled_seed" | "economic_parameter_fallback"
  revenueEstimationNote: "estimated_from_admin_credit_value" | "backfilled_historical_approximation" | null

OperationRunEvent (detalhe — por evento):
  estimatedCostBrl = estimatedCostUsd × (usd_brl_rate_at_generation ?? corrente)
  usdBrlRateAtGeneration / creditValueBrlAtGeneration / usdBrlRateSourceAtGeneration / creditValueBrlSourceAtGeneration (por evento)

OperationRunsSummary:
  receitaEstimadaBrl/resultadoEstimadoBrl/margemEstimadaPct (soma dos derivados por run)
  usdBrlRateSource / creditValueSource / revenueEstimationNote agregados:
    origem dominante do conjunto (todas as origens presentes; se há fallback → prevalece o fallback na agregação)

deriveBrl(raw, current) — raw carrega custo_usd_total, creditos_liquidos, usd_brl_rate_at_generation, credit_value_brl_at_generation, usd_brl_rate_source_at_generation, credit_value_brl_source_at_generation; current = { usdBrlRate, creditValueBrl } (falha-closed getEconomicParams preservado).
Fórmulas (D2/D5):
  custoBrl = custoUsd × (snapshot ?? corrente)
  receitaEstimadaBrl = creditosLiquidos × (snapshot ?? corrente)
  resultadoEstimadoBrl = receitaEstimadaBrl − custoBrl (ambos não-null)
  margemEstimadaPct = receitaEstimadaBrl > 0 ? (resultadoEstimadoBrl / receitaEstimadaBrl) × 100 : null
Origem (D1/D4 — revisão):
  usdBrlRateSource = raw.usd_brl_rate_source_at_generation ?? "economic_parameter_fallback"
  creditValueSource = raw.credit_value_brl_source_at_generation ?? "economic_parameter_fallback"
  revenueEstimationNote = creditValueSource === "economic_parameter_fallback" ? "estimated_from_admin_credit_value"
                        : creditValueSource.startsWith("backfilled") ? "backfilled_historical_approximation"
                        : null
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: deriveBrl com snapshot + fallback explícito; tipos renomeados para estimado</name>
  <files>src/lib/ai-cost/operation-runs-service.ts, src/lib/ai-cost/__tests__/operation-runs-service.test.ts</files>
  <read_first>
    - src/lib/ai-cost/operation-runs-service.ts (interfaces OperationRun :51, OperationRunsSummary :80, SegmentAggregation :97, OperationRunEvent :141, RawOperationRun :171, RawEvent :204, RawDetailRun :228; deriveBrl :362; mapRun :700; mapDetailRun :602; mapEvent :641)
    - src/lib/ai-cost/__tests__/operation-runs-service.test.ts (makeRawRun defaults — líquido=bruto; fake economic injetado no constructor)
    - openspec/changes/fase-38-2-1-economic-snapshot/specs/economic-snapshot/spec.md (Requisito "Contrato de nomenclatura estimada" — tabela de campos/fórmulas + nomes proibidos)
  </read_first>
  <behavior>
    - Test 1: run com usd_brl_rate_at_generation=5.20, credit_value_brl_at_generation=2.00, origens `captured_at_generation` (custoUsd 10, líquidos 7) → custoBrl 52.00, receitaEstimadaBrl 14.00, resultadoEstimadoBrl −38.00, margemEstimadaPct ≈ −271.43, usdBrlRateSource "captured_at_generation", creditValueSource "captured_at_generation", revenueEstimationNote null
    - Test 2: run com valores `backfilled_from_audit`/`backfilled_seed` → sources refletem backfilled (NÃO captured); revenueEstimationNote "backfilled_historical_approximation"
    - Test 3: run SEM valor persistido (mock sem os campos) → usa corrente 5.00/1.50; creditValueSource "economic_parameter_fallback", revenueEstimationNote "estimated_from_admin_credit_value", usdBrlRateSource "economic_parameter_fallback"
    - Test 4: receitaEstimadaBrl 0 (líquidos 0) → margemEstimadaPct null; resultadoEstimadoBrl = −custoBrl
    - Test 5: receita estimada usa LÍQUIDOS (estorno descontado): líquidos 7 → 14.00, nunca bruto
  </behavior>
  <action>
    Em `src/lib/ai-cost/operation-runs-service.ts`:
    - `RawOperationRun`, `RawDetailRun` e `RawEvent`: adicionar `usd_brl_rate_at_generation?`, `credit_value_brl_at_generation?`, `usd_brl_rate_source_at_generation?` e `credit_value_brl_source_at_generation?` (`string | number | null` — espelham o contrato novo do RPC, plano 03).
    - `OperationRun`: renomear `receitaOpBrl`→`receitaEstimadaBrl`, `resultadoOpBrl`→`resultadoEstimadoBrl`, `margemOpPct`→`margemEstimadaPct`; adicionar `usdBrlRateAtGeneration`, `creditValueBrlAtGeneration` (number|null) e `usdBrlRateSource`/`creditValueSource` (union de 4: `"captured_at_generation" | "backfilled_from_audit" | "backfilled_seed" | "economic_parameter_fallback"`) e `revenueEstimationNote` (`"estimated_from_admin_credit_value" | "backfilled_historical_approximation" | null`).
    - `OperationRunsSummary` e `SegmentAggregation`: renomear os 3 campos; summary ganha `usdBrlRateSource`/`creditValueSource`/`revenueEstimationNote` agregados.
    - `OperationRunEvent`: adicionar `usdBrlRateAtGeneration`/`creditValueBrlAtGeneration`/`usdBrlRateSourceAtGeneration`/`creditValueBrlSourceAtGeneration` (number/string|null) por evento.
    - `deriveBrl(raw, current)`: alterar a assinatura para receber os snapshots E origens do raw + os correntes; aplicar as fórmulas do bloco <interfaces> (snapshot ?? corrente); retornar os campos renomeados + sources/notes (origem = raw source ?? "economic_parameter_fallback"; note derivada da origem).
    - `mapRun`/`mapDetailRun`: propagar os snapshots e origens do raw ao deriveBrl e aos campos; `mapEvent`: `estimatedCostBrl = estimatedCostUsd × (snapshot do evento ?? corrente)` e expor snapshots/origens do evento.
    - Atualizar TODOS os usos internos dos campos renomeados (deriveAggregations, deriveSummary).
    - NENHUM nome `receitaRealBrl`/`resultadoRealBrl`/`margemRealPct` em qualquer contrato (proibidos — D8).
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai-cost/__tests__/operation-runs-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep por `receitaEstimadaBrl` presente em operation-runs-service.ts; grep por `receitaOpBrl|resultadoOpBrl|margemOpPct|receitaRealBrl|resultadoRealBrl|margemRealPct` SEM ocorrências residuais no arquivo (usar `grep -v '^#'` para comentários)
    - deriveBrl usa `??` para fallback de ambas as taxas (snapshot ?? corrente)
    - Sources de 4 valores presentes: grep por `captured_at_generation`, `backfilled_from_audit`, `backfilled_seed`, `economic_parameter_fallback` no arquivo
    - Tipos exportados compilam (typecheck)
    - Testes novos verdes: snapshot captured usado (5.20/2.00), backfilled com origem marcada, fallback sinalizado, margem null com receita 0, líquidos como base
  </acceptance_criteria>
  <done>deriveBrl e tipos renomeados com snapshot + origem de 4 valores + fallback explícito; contrato estimado estabelecido</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: deriveSummary soma BRL por run; mapEvent/DetailRun com snapshot</name>
  <files>src/lib/ai-cost/operation-runs-service.ts, src/lib/ai-cost/__tests__/operation-runs-service.test.ts</files>
  <read_first>
    - src/lib/ai-cost/operation-runs-service.ts (deriveSummary :856 — hoje re-deriva do total USD com taxa única; deriveAggregations :748; getRunDetail :574)
    - src/lib/ai-cost/__tests__/operation-runs-service.test.ts (testes atuais de summary/aggregations — run-a/run-b com taxas)
    - openspec/changes/fase-38-2-1-economic-snapshot/design.md (D5 — deriveSummary soma BRL já derivados por run; teste de mistura de taxas)
  </read_first>
  <behavior>
    - Test 1: 2 runs com taxas snapshotadas DISTINTAS (5.20 e 6.00) → summary.custoBrl = soma dos custoBrl por run (52.00 + 60.00), NÃO 11 USD × taxa única
    - Test 2: summary com 1 run snapshot captured + 1 run backfilled → creditValueSource "backfilled_from_audit" (origem backfilled presente), revenueEstimationNote "backfilled_historical_approximation"
    - Test 3: summary com 1 run snapshot + 1 run fallback → creditValueSource "economic_parameter_fallback" e revenueEstimationNote "estimated_from_admin_credit_value" (qualquer fallback no conjunto)
    - Test 4: summary só com runs captured → creditValueSource "captured_at_generation", revenueEstimationNote null
    - Test 5: detail — estimatedCostBrl do evento usa snapshot do evento (1 USD × 5.20 = 5.20); run do detalhe carrega snapshots/origens do 1º evento
  </behavior>
  <action>
    Em `src/lib/ai-cost/operation-runs-service.ts`:
    - `deriveSummary(runs, params)`: substituir a derivação de `custoBrl` (hoje `custoUsdTotal × params.usdBrlRate`) e de `receitaOpBrl`/`resultadoOpBrl`/`margemOpPct` por SOMA dos valores JÁ derivados por run: `custoBrl = sumValues(runs.map(r => r.custoBrl))`, `receitaEstimadaBrl = sumValues(runs.map(r => r.receitaEstimadaBrl))`, `resultadoEstimadoBrl = sumValues(runs.map(r => r.resultadoEstimadoBrl))`; `margemEstimadaPct` derivada das somas (receita > 0 senão null). `custoUsdTotal`/créditos permanecem como soma de brutos/líquidos (inalterado).
    - Agregar `usdBrlRateSource`/`creditValueSource`/`revenueEstimationNote` do summary: origem dominante do conjunto — se qualquer run usa `economic_parameter_fallback` → fallback + note "estimated_from_admin_credit_value"; senão se qualquer run é `backfilled_*` → a origem backfilled (a que prevalecer) + note "backfilled_historical_approximation"; senão (todos captured) → "captured_at_generation" + note null.
    - `deriveAggregations`: renomear usos dos campos (já renomeados na Task 1) — sem mudança de lógica (continua somando BRL por run).
    - `getRunDetail`/`mapDetailRun`/`mapEvent`: usar os snapshots e origens do RPC de detalhe (RawDetailRun/RawEvent) no deriveBrl/mapEvent (Task 1 já alterou as assinaturas — garantir wiring completo).
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai-cost/__tests__/operation-runs-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - deriveSummary soma `r.custoBrl`/`r.receitaEstimadaBrl`/`r.resultadoEstimadoBrl` por run (grep por `sumValues(runs.map` com `custoBrl`/`receitaEstimadaBrl`)
    - Sem re-derivação `custoUsdTotal * params.usdBrlRate` no summary (grep ausente)
    - Testes verdes: mistura de taxas (52+60), fallback agregado, snapshot agregado, detail com snapshot do evento
    - `npm run typecheck` limpo
  </acceptance_criteria>
  <done>deriveSummary soma BRL por run; detalhe com snapshot por evento; fontes agregadas no summary</done>
</task>

<task type="auto">
  <name>Task 3: Testes de estabilidade temporal + fallback legacy + regressão do service</name>
  <files>src/lib/ai-cost/__tests__/operation-runs-service.test.ts</files>
  <read_first>
    - src/lib/ai-cost/__tests__/operation-runs-service.test.ts (fixtures makeRawRun/makeDetailRun; fake economic injetado)
    - openspec/changes/fase-38-2-1-economic-snapshot/tasks.md (seção 8 — testes 8.2/8.4: snapshot usa taxa snapshotada; fallback com sources; deriveSummary com taxas distintas; margem null; ESTABILIDADE TEMPORAL)
  </read_first>
  <action>
    Estender `src/lib/ai-cost/__tests__/operation-runs-service.test.ts`:
    - Fixtures: `makeRawRun`/`makeDetailRun` ganham defaults de `usd_brl_rate_at_generation`/`credit_value_brl_at_generation` **e origens** (null por default — preserva testes existentes de fallback; overrides explícitos para cenários de snapshot/backfill).
    - Novos testes: (a) run com snapshot captured usa taxa snapshotada e NÃO a corrente (assertar custoBrl/receitaEstimadaBrl + `creditValueSource = "captured_at_generation"`); (a2) run com valores backfilled → `creditValueSource = "backfilled_from_audit"`/`"backfilled_seed"` + `revenueEstimationNote = "backfilled_historical_approximation"`; (b) run sem valor → fallback corrente com `creditValueSource = "economic_parameter_fallback"` e `revenueEstimationNote = "estimated_from_admin_credit_value"`; (c) deriveSummary com runs de taxas distintas soma BRL por run; (d) margemEstimadaPct null quando receita estimada 0; (e) ESTABILIDADE TEMPORAL: mock do fake economic com corrente 6.00 (alterada) → runs com snapshot mantêm os mesmos custoBrl/receitaEstimadaBrl/resultadoEstimadoBrl/margemEstimadaPct (simula alterar parâmetro depois da geração — critério de aceite 1-2 do design); (f) mapEvent com snapshot/origem por evento no detalhe.
    - Rodar o arquivo completo do service (tests atuais + novos) e garantir regressão interna verde.
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai-cost/__tests__/operation-runs-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo de teste contém casos para: snapshot captured usado, backfilled com origem, fallback com sources/notes, mistura de taxas no summary, margem null, estabilidade temporal (grep por `creditValueSource`/`captured_at_generation`/`backfilled_from_audit`/`economic_parameter_fallback`/`estabilidade` ou equivalentes nos nomes dos testes)
    - Todos os testes do service verdes
    - `npm run typecheck` limpo; `npm run lint` limpo
  </acceptance_criteria>
  <done>Service com snapshot/origem/fallback/estabilidade temporal coberto por testes; regressão do arquivo verde</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RPC (service_role) → service | Dados brutos atravessam o service que deriva BRL — dados internos de operação |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38.2.1-10 | Information Disclosure | Contrato de API/UI | mitigate | Nomenclatura *Estimado* obrigatória em TODO contrato; grep gate proíbe receitaRealBrl/resultadoRealBrl/margemRealPct (D8) |
| T-38.2.1-11 | Tampering | Fallback legacy silencioso | mitigate | Fallback SEMPRE sinalizado (creditValueSource/revenueEstimationNote/usdBrlRateSource) — nunca assumir valor sem origem (D4); origem de 4 valores distingue captured/backfilled/fallback; testes cobrem o contrato |
| T-38.2.1-12 | Integrity | Estabilidade temporal | mitigate | deriveBrl prioriza snapshot (??); deriveSummary soma derivados por run (não re-deriva) — alterar parâmetro corrente não muda histórico (teste 8.4) |
| T-38.2.1-SC | Tampering | Instalação de pacotes | accept | Nenhum pacote novo nesta fase — sem gate de legitimidade |
</threat_model>

<verification>
- `npx vitest run src/lib/ai-cost/__tests__/operation-runs-service.test.ts` — todos verdes
- `npm run typecheck` + `npm run lint` limpos
- Grep gate: `grep -rn "receitaRealBrl\|resultadoRealBrl\|margemRealPct" src/` → 0 ocorrências (após remover comentários)
- Regressão rápida dos consumidores da API (plano 05 atualiza contratos)
</verification>

<success_criteria>
- Lista e detalhe derivam BRL com snapshot do run/evento; fallback corrente explícito e marcado
- Summary soma BRL por run (taxas distintas não se misturam)
- Contrato renomeado: receitaEstimadaBrl/resultadoEstimadoBrl/margemEstimadaPct + sources/notes
- Estabilidade temporal coberta por teste (parâmetro alterado depois não muda histórico com snapshot)
</success_criteria>

<output>
Create `.planning/phases/38.2.1-economic-snapshot/38-2-1-04-SUMMARY.md` when done
</output>

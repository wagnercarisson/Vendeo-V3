---
phase: 38.2.1-economic-snapshot
plan: 05
type: execute
wave: 4
depends_on: [38.2.1-04]
files_modified:
  - src/app/api/admin/ai-operation-runs/__tests__/route.test.ts
  - src/app/api/admin/ai-operation-runs/[operationRunId]/__tests__/route.test.ts
  - src/app/(app)/admin/ai-operation-costs/kpis-grid.tsx
  - src/app/(app)/admin/ai-operation-costs/operation-runs-table.tsx
  - src/app/(app)/admin/ai-operation-costs/run-detail-dialog.tsx
  - src/app/(app)/admin/ai-operation-costs/segment-aggregations.tsx
  - src/app/(app)/admin/ai-operation-costs/page.tsx
  - src/app/(app)/admin/ai-operation-costs/__tests__/components.test.tsx
  - src/app/(app)/admin/ai-operation-costs/__tests__/page.test.tsx
autonomous: true
requirements: [F38.2.1-07, F38.2.1-11]

must_haves:
  truths:
    - "API de operation runs expõe receitaEstimadaBrl/resultadoEstimadoBrl/margemEstimadaPct (nunca receita real) + snapshots + creditValueSource/revenueEstimationNote"
    - "Painel /admin/ai-operation-costs rotula 'Receita estimada'/'Resultado estimado'/'Margem estimada' e sinaliza origem do valor (snapshot vs fallback)"
    - "Painel informa que alterar parâmetros econômicos vale para novas gerações e não recalcula o histórico"
  artifacts:
    - path: "src/app/(app)/admin/ai-operation-costs/kpis-grid.tsx"
      provides: "KPIs com labels estimados + origem + aviso de semântica"
      contains: "Receita estimada"
    - path: "src/app/(app)/admin/ai-operation-costs/operation-runs-table.tsx"
      provides: "Tabela com campos renomeados e indicador de fallback"
      contains: "receitaEstimadaBrl"
  key_links:
    - from: "kpis-grid.tsx"
      to: "OperationRunsSummary"
      via: "summary.receitaEstimadaBrl / summary.creditValueSource"
      pattern: "receitaEstimadaBrl"
    - from: "run-detail-dialog.tsx"
      to: "OperationRunEvent.estimatedCostBrl"
      via: "custo BRL derivado do snapshot + usdBrlRateAtGeneration/creditValueBrlAtGeneration por evento"
      pattern: "usdBrlRateAtGeneration"
---

<objective>
Atualizar a camada de consumo do contrato renomeado: testes de rota da API de operation runs (os endpoints repassam o service — código das rotas NÃO muda) e a UI do painel `/admin/ai-operation-costs` (KPIs, tabela, drilldown, agregados) com a nomenclatura estimada, origem do valor (snapshot vs fallback) e o aviso "alteração vale para novas gerações".

Purpose: D8 + F38.2.1-07/F38.2.1-11 — nenhum contrato afirma "receita real"; a UI deixa explícito que valores derivados são estimativas operacionais e que parâmetros correntes só valem para novas gerações.
Output: API testada sob o contrato novo; painel com labels/origem/aviso.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@openspec/changes/fase-38-2-1-economic-snapshot/design.md (D8 — rename na API/UI; critério de aceite 5)
@openspec/changes/fase-38-2-1-economic-snapshot/specs/ai-operation-costs/spec.md (KPIs/tabela/drilldown com nomes estimados; origem do valor; aviso de semântica; legend)
@openspec/changes/fase-38-2-1-economic-snapshot/specs/ai-operation-runs-api/spec.md (contratos renomeados da API)
@src/app/api/admin/ai-operation-runs/route.ts (pass-through do service — código da rota NÃO muda)
@src/app/(app)/admin/ai-operation-costs/kpis-grid.tsx (estado atual — labels "Receita operacional (BRL)" etc.)
@src/app/(app)/admin/ai-operation-costs/operation-runs-table.tsx
@src/app/(app)/admin/ai-operation-costs/run-detail-dialog.tsx
@src/app/(app)/admin/ai-operation-costs/segment-aggregations.tsx
@src/app/(app)/admin/ai-operation-costs/page.tsx

<interfaces>
Contrato consumido (definido no plano 04 — service):
- OperationRun: `receitaEstimadaBrl`/`resultadoEstimadoBrl`/`margemEstimadaPct` + `usdBrlRateAtGeneration`/`creditValueBrlAtGeneration` + `usdBrlRateSource`/`creditValueSource` ("snapshot" | "economic_parameter_fallback") + `revenueEstimationNote`
- OperationRunsSummary: mesmos campos agregados + sources/notes agregadas
- OperationRunEvent (detalhe): `estimatedCostBrl` + `usdBrlRateAtGeneration`/`creditValueBrlAtGeneration` por evento

Origem do valor na UI (spec ai-operation-costs):
- `creditValueSource === "economic_parameter_fallback"` → sinalizar tooltip/badge "estimado de parâmetro atual" (fallback legacy)
- `usdBrlRateSource === "economic_parameter_fallback"` → idem para custo BRL
- Aviso fixo: "Alterações nos parâmetros econômicos valem para novas gerações e não recalculam o histórico exibido"
- Legend: "Estimativas operacionais — não custo financeiro reconciliado (F38.3)"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Testes de rota — contratos renomeados + snapshot no run/evento</name>
  <files>src/app/api/admin/ai-operation-runs/__tests__/route.test.ts, src/app/api/admin/ai-operation-runs/[operationRunId]/__tests__/route.test.ts</files>
  <read_first>
    - src/app/api/admin/ai-operation-runs/__tests__/route.test.ts (fixtures com receitaOpBrl/resultadoOpBrl/margemOpPct — linhas 37-58, 120-122, 253-254, 273-313)
    - src/app/api/admin/ai-operation-runs/[operationRunId]/__tests__/route.test.ts (fixtures do detalhe — linhas 39-41)
    - src/app/api/admin/ai-operation-runs/route.ts (pass-through — NENHUMA mudança de código necessária)
    - src/lib/ai-cost/operation-runs-service.ts (tipos renomeados do plano 04 — fonte dos mocks)
  </read_first>
  <action>
    Atualizar os testes das 2 rotas para o contrato renomeado (as rotas repassam o resultado do service — sem alteração de código nas rotas; apenas mocks/asserts):
    - Renomear em fixtures e asserts: `receitaOpBrl`→`receitaEstimadaBrl`, `resultadoOpBrl`→`resultadoEstimadoBrl`, `margemOpPct`→`margemEstimadaPct`.
    - Adicionar aos mocks de run/lista/detalhe: `usdBrlRateAtGeneration`, `creditValueBrlAtGeneration`, `usdBrlRateSource`, `creditValueSource`, `revenueEstimationNote` (ex.: run com snapshot → sources "snapshot", note null; run legacy → "economic_parameter_fallback"/"estimated_from_admin_credit_value").
    - Novos asserts: o corpo da resposta contém `receitaEstimadaBrl` e NÃO contém `receitaOpBrl`/`receitaRealBrl` (grep-gate de nomenclatura nos testes); fallback legacy sinalizado na lista e no detalhe.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/admin/ai-operation-runs/__tests__/route.test.ts src/app/api/admin/ai-operation-runs/[operationRunId]/__tests__/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Grep nos arquivos de teste: `receitaEstimadaBrl` presente; `receitaOpBrl`/`receitaRealBrl` com 0 ocorrências (após comentários)
    - Testes das 2 rotas verdes com os novos campos de origem/snapshot nos asserts
    - Código das rotas (route.ts × 2) SEM diff (pass-through preservado)
    - `npm run typecheck` limpo
  </acceptance_criteria>
  <done>Contratos da API testados sob a nomenclatura estimada + snapshots + fallback sinalizado</done>
</task>

<task type="auto">
  <name>Task 2: KPIs + tabela + drilldown + agregados com labels estimados e origem do valor</name>
  <files>src/app/(app)/admin/ai-operation-costs/kpis-grid.tsx, src/app/(app)/admin/ai-operation-costs/operation-runs-table.tsx, src/app/(app)/admin/ai-operation-costs/run-detail-dialog.tsx, src/app/(app)/admin/ai-operation-costs/segment-aggregations.tsx, src/app/(app)/admin/ai-operation-costs/page.tsx</files>
  <read_first>
    - src/app/(app)/admin/ai-operation-costs/kpis-grid.tsx (buildKpis — labels atuais "Receita operacional (BRL)", "Resultado operacional estimado (BRL)", "Margem operacional estimada"; tipagem OperationRunsSummary)
    - src/app/(app)/admin/ai-operation-costs/operation-runs-table.tsx (colunas da tabela por entrega)
    - src/app/(app)/admin/ai-operation-costs/run-detail-dialog.tsx (drilldown call-level — estimatedCostBrl, badges, componentes)
    - src/app/(app)/admin/ai-operation-costs/segment-aggregations.tsx (agregados por segmento — resultado/margem)
    - src/app/(app)/admin/ai-operation-costs/page.tsx (composição — onde inserir o aviso de semântica)
    - openspec/changes/fase-38-2-1-economic-snapshot/specs/ai-operation-costs/spec.md (cenários: "Página renderiza KPIs e tabela com nomes estimados", "Página mostra origem do valor quando fallback", "Página informa que alteração de parâmetro não recalcula histórico", "Drilldown abre detalhe call-level com snapshot")
  </read_first>
  <action>
    Em `kpis-grid.tsx`:
    - Renomear labels: "Receita operacional (BRL)"→"Receita estimada (BRL)", "Resultado operacional estimado (BRL)"→"Resultado estimado (BRL)", "Margem operacional estimada"→"Margem estimada". Consumir `summary.receitaEstimadaBrl`/`resultadoEstimadoBrl`/`margemEstimadaPct`.
    - Origem do valor: quando `summary.creditValueSource === "economic_parameter_fallback"` ou `summary.usdBrlRateSource === "economic_parameter_fallback"`, exibir tooltip/badge "estimado de parâmetro atual" nos cards de receita/resultado (e custo BRL) — via prop title ou Badge do design system.
    - Aviso de semântica (F38.2.1-11): incluir no componente (ou no page.tsx) texto fixo "Alterações nos parâmetros econômicos valem para novas gerações e não recalculam o histórico exibido" + legend "Estimativas operacionais — não custo financeiro reconciliado".
    Em `operation-runs-table.tsx`: colunas de receita/resultado/margem usam os campos renomeados; adicionar indicador de fallback por linha quando `creditValueSource === "economic_parameter_fallback"` (tooltip/badge).
    Em `run-detail-dialog.tsx`: labels estimados; exibir `estimatedCostBrl` (já snapshotado pelo service), `usdBrlRateAtGeneration`/`creditValueBrlAtGeneration` por evento (quando presentes, exibir taxa usada; quando ausentes, "parâmetro atual (fallback)").
    Em `segment-aggregations.tsx`: renomear `resultadoOpBrl`→`resultadoEstimadoBrl` e `margemOpPct`→`margemEstimadaPct` (campos do SegmentAggregation renomeados no plano 04); labels "Resultado estimado"/"Margem estimada".
    Em `page.tsx`: garantir a passagem dos dados sem re-derivação (KPIs vêm do summary do service — D3, a UI nunca recalcula) e o aviso de semântica na página.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Grep em src/app/(app)/admin/ai-operation-costs: "Receita estimada"/"Resultado estimado"/"Margem estimada" presentes; "Receita operacional"/"Resultado operacional" ausentes nos labels (após comentários); `receitaOpBrl`/`resultadoOpBrl`/`margemOpPct` com 0 ocorrências nos .tsx
    - String do aviso "valem para novas gerações" presente (grep)
    - Indicador de fallback: grep por `economic_parameter_fallback` nos componentes
    - `npm run typecheck` limpo
  </acceptance_criteria>
  <done>Painel com nomenclatura estimada, origem do valor e aviso de semântica</done>
</task>

<task type="auto">
  <name>Task 3: Testes de UI — labels novos, origem, aviso</name>
  <files>src/app/(app)/admin/ai-operation-costs/__tests__/components.test.tsx, src/app/(app)/admin/ai-operation-costs/__tests__/page.test.tsx</files>
  <read_first>
    - src/app/(app)/admin/ai-operation-costs/__tests__/components.test.tsx (makeRun()/SUMMARY fixtures — creditosEstornados/creditosLiquidos já obrigatórios)
    - src/app/(app)/admin/ai-operation-costs/__tests__/page.test.tsx (makeRun()/makeListResult())
    - openspec/changes/fase-38-2-1-economic-snapshot/tasks.md (7.3 — atualizar fixtures/tests de UI para os novos labels/contratos)
  </read_first>
  <action>
    Atualizar as fixtures e asserts dos testes de UI do painel:
    - Fixtures `makeRun()`/`SUMMARY`/`makeListResult()` ganham os campos novos obrigatórios do contrato (receitaEstimadaBrl, resultadoEstimadoBrl, margemEstimadaPct, usdBrlRateAtGeneration, creditValueBrlAtGeneration, usdBrlRateSource, creditValueSource, revenueEstimationNote) — valores default snapshot quando o cenário não diverge.
    - Renomear asserts: "Receita operacional"→"Receita estimada" etc.; asserts de que o aviso "valem para novas gerações" é renderizado; assert de indicador de fallback quando `creditValueSource = "economic_parameter_fallback"` (ex.: tooltip/badge com "parâmetro atual").
    - Adicionar cenário: run legacy (sem snapshot) → badge/tooltip de fallback visível.
  </action>
  <verify>
    <automated>npx vitest run src/app/(app)/admin/ai-operation-costs/__tests__/components.test.tsx src/app/(app)/admin/ai-operation-costs/__tests__/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Testes de UI verdes (components + page) sob o contrato renomeado
    - Grep nos testes: `receitaEstimadaBrl` presente; `receitaOpBrl` com 0 ocorrências
    - Aviso de semântica assertado nos testes (grep por "novas gerações" nos arquivos de teste)
    - `npm run typecheck` + `npm run lint` limpos
  </acceptance_criteria>
  <done>UI testada com labels estimados, origem do valor e aviso</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → API painel | Dados admin cruzam a fronteira HTTP (requireAdmin + zod — inalterados) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38.2.1-13 | Information Disclosure | Labels/contratos da UI | mitigate | Nomenclatura *Estimado* obrigatória; grep-gate proíbe "Receita operacional"/"receita real" como verdade financeira (D8) |
| T-38.2.1-14 | Spoofing | Origem do valor | mitigate | Origem vem do service (creditValueSource/usdBrlRateSource) — a UI apenas exibe; nunca infere origem na camada de apresentação |
| T-38.2.1-15 | Tampering | Fallback apresentado como snapshot | mitigate | Indicador "estimado de parâmetro atual" quando fallback (badge/tooltip) — nunca apresentar fallback sem marcação |
| T-38.2.1-SC | Tampering | Instalação de pacotes | accept | Nenhum pacote novo nesta fase — sem gate de legitimidade |
</threat_model>

<verification>
- `npx vitest run` nos testes das 2 rotas + UI do painel verdes
- `npm run typecheck` + `npm run lint` limpos
- Grep gate global: `grep -rn "receitaRealBrl\|resultadoRealBrl\|margemRealPct" src/` → 0
</verification>

<success_criteria>
- API expõe e testa os contratos renomeados (receitaEstimadaBrl etc.) + snapshots + origem
- Painel rotula estimado, sinaliza fallback e avisa que alterações valem para novas gerações
- Nenhum contrato/código/UI afirma receita real
</success_criteria>

<output>
Create `.planning/phases/38.2.1-economic-snapshot/38-2-1-05-SUMMARY.md` when done
</output>

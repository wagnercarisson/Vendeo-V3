---
phase: 38.2.1-economic-snapshot
plan: 06
type: execute
wave: 2
depends_on: [38.2.1-01]
files_modified:
  - src/lib/metrics/pipeline-metrics.ts
  - src/lib/metrics/__tests__/pipeline-metrics.test.ts
  - src/app/(app)/admin/metrics/page.tsx
  - src/app/(app)/admin/metrics/metrics-cards.tsx
  - src/app/(app)/admin/metrics/page.test.tsx
  - src/app/(app)/admin/operation-costs/page.tsx
  - src/app/(app)/admin/operation-costs/operation-costs-form.tsx
  - src/app/(app)/admin/operation-costs/operation-costs-form.test.tsx
autonomous: true
requirements: [F38.2.1-10, F38.2.1-11]

must_haves:
  truths:
    - "Card 'Custo Médio IA' do /admin/metrics converte USD→BRL com usd_brl_rate_at_generation (snapshot por evento) quando disponível; não recalcula histórico"
    - "Sem snapshot no período → fallback explícito economic_parameters.usd_brl_rate corrente; VENDEO_USD_BRL_RATE NUNCA usado"
    - "Configurações Econômicas avisam que alterar usd_brl_rate/credit_value_brl vale para novas gerações e não recalcula o histórico"
  artifacts:
    - path: "src/lib/metrics/pipeline-metrics.ts"
      provides: "getAvgCostBrl (média BRL call-level com snapshot por evento + fallback explícito)"
      contains: "getAvgCostBrl"
    - path: "src/app/(app)/admin/metrics/page.tsx"
      provides: "Card 'Custo Médio IA' em BRL snapshotado"
      contains: "getAvgCostBrl"
  key_links:
    - from: "getAvgCostBrl"
      to: "generation_events"
      via: "supabaseAdmin.from('generation_events') — eventos call-level no período, excluindo delivery markers"
      pattern: "from\\(\"generation_events\"\\)"
    - from: "getAvgCostBrl"
      to: "EconomicParameterService"
      via: "getParameter('usd_brl_rate') como fallback explícito (nunca VENDEO_USD_BRL_RATE)"
      pattern: "getParameter"
---

<objective>
Corrigir o card "Custo Médio IA" do `/admin/metrics` para a semântica de snapshot: `getAvgCost` permanece em USD (call-level, F38.1) e ganha `getAvgCostBrl` — média em BRL com conversão POR EVENTO usando `usd_brl_rate_at_generation` quando disponível e fallback explícito ao parâmetro corrente. A página de Configurações Econômicas (`/admin/operation-costs`) passa a avisar que alterações valem para novas gerações.

Purpose: D7 + F38.2.1-10/F38.2.1-11 — parar de recalcular o histórico com o parâmetro corrente quando há snapshot; fonte única `economic_parameters` (nunca o env deprecado).
Output: /admin/metrics estável temporalmente; aviso de semântica em Configurações Econômicas.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@openspec/changes/fase-38-2-1-economic-snapshot/design.md (D7 — /admin/metrics usa snapshot; nunca VENDEO_USD_BRL_RATE)
@openspec/changes/fase-38-2-1-economic-snapshot/specs/admin-metrics-dashboard/spec.md (card "Custo Médio IA" — snapshot por evento ou taxa média; fallback corrente; estabilidade temporal)
@openspec/changes/fase-38-2-1-economic-snapshot/specs/pipeline-metrics/spec.md (getAvgCost USD call-level; não lê delivery marker; MetricCard types SEM alteração)
@openspec/changes/fase-38-2-1-economic-snapshot/specs/economic-parameters/spec.md (página /admin/operation-costs — aviso "alterações valem para novas gerações")
@src/lib/metrics/pipeline-metrics.ts (getAvgCost atual :125 — RPC admin_get_ai_costs, média USD)
@src/app/(app)/admin/metrics/page.tsx (:194 lê usd_brl_rate e passa a MetricsCards)
@src/app/(app)/admin/metrics/metrics-cards.tsx (formatCost converte value × usdToBrlRate)
@src/app/(app)/admin/operation-costs/page.tsx (seção Parâmetros Econômicos :98-111)
@src/app/(app)/admin/operation-costs/operation-costs-form.tsx (ParamsForm)

<interfaces>
- `getAvgCost(hours)` — INALTERADO: média USD call-level via RPC admin_get_ai_costs (F38.1 intocado).
- `getAvgCostBrl(hours)` — NOVO (pipeline-metrics.ts): média BRL por evento no período:
  - Fonte: `supabaseAdmin.from("generation_events")` — eventos call-level (excluir os 4 delivery markers: generation_type IN ('campaign_pipeline','visual_signature','brand_profile_without_logo','brand_profile_with_logo')) com `operation_run_id IS NOT NULL` e custo não-nulo, `created_at >= now() - interval`.
  - Por evento: `cost = COALESCE(provider_reported_cost_usd, estimated_cost_usd)`; `rate = usd_brl_rate_at_generation ?? taxaCorrente` (taxaCorrente via `EconomicParameterService.getParameter("usd_brl_rate")` — resolvida UMA vez; falha real → retorna null com log, degradação suave como o getAvgCost atual).
  - `avgBrl = Σ(cost × rate) / contagem` — nunca usa `VENDEO_USD_BRL_RATE`.
- MetricCard type (src/lib/metrics/types.ts) — SEM alteração (spec). O valor BRL é pré-formatado como string no page.tsx e o metrics-cards.tsx passa a devolver strings sem re-conversão.
- `usdToBrlRate` prop do MetricsCards: REMOVIDA (nenhum outro card converte USD→BRL).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getAvgCostBrl — média BRL call-level com snapshot por evento</name>
  <files>src/lib/metrics/pipeline-metrics.ts, src/lib/metrics/__tests__/pipeline-metrics.test.ts</files>
  <read_first>
    - src/lib/metrics/pipeline-metrics.ts (getAvgCost :125-156 — padrão de RPC + toNumber + degradação suave; fetchMetricsBundle :41)
    - src/lib/metrics/__tests__/pipeline-metrics.test.ts (estrutura de mocks do supabaseAdmin — .from().select() encadeável; mock do RPC)
    - src/lib/economic/economic-parameter-service.ts (getParameter — fail-open 1.00 / fail-closed)
    - openspec/changes/fase-38-2-1-economic-snapshot/specs/admin-metrics-dashboard/spec.md (cenários: snapshot usado; fallback corrente sem snapshot; nunca env deprecado)
  </read_first>
  <behavior>
    - Test 1: eventos com custos 0.01/0.02/0.03 USD e usd_brl_rate_at_generation 5.20 → avgBrl = (0.052 + 0.104 + 0.156)/3 ≈ 0.104
    - Test 2: eventos SEM snapshot → fallback taxa corrente 5.00 (mock do EconomicParameterService) → avgBrl = (0.05+0.10+0.15)/3 ≈ 0.10
    - Test 3: sem custos → null
    - Test 4: delivery markers (campaign_pipeline) EXCLUÍDOS da consulta (filtro generation_type NOT IN)
    - Test 5: falha de leitura da taxa corrente (EconomicParameterUnavailableError) → null (degradação), sem throw
  </behavior>
  <action>
    Em `src/lib/metrics/pipeline-metrics.ts`:
    - Adicionar `getAvgCostBrl(hours: number, _storeKind?: StoreKind): Promise<number | null>`: consulta `generation_events` via supabaseAdmin (padrão do arquivo — .from().select().gte/lt no created_at), filtrando call-level (excluir os 4 delivery markers via .not('generation_type','in',[...]) ou filtro equivalente) e custo não-nulo.
    - Por evento: cost = COALESCE(provider_reported_cost_usd, estimated_cost_usd) aplicado no TS (toNumber); rate = toNumber(usd_brl_rate_at_generation) ?? taxaCorrente.
    - Taxa corrente: resolver UMA vez via `new EconomicParameterService().getParameter("usd_brl_rate")` em try/catch — erro real → console.error + return null (degradação suave, padrão getAvgCost atual). NUNCA ler `process.env.VENDEO_USD_BRL_RATE`.
    - Retornar média BRL (`Σ(cost × rate) / N`) ou null quando N=0.
    - Importar EconomicParameterService de "@/lib/economic/economic-parameter-service".
    - Atualizar o test file: mock do `.from("generation_events")` (retorna linhas com estimated_cost_usd/provider_reported_cost_usd/usd_brl_rate_at_generation) + mock de `@/lib/economic/economic-parameter-service` para o fallback; cenários 1-5 do <behavior>.
  </action>
  <verify>
    <automated>npx vitest run src/lib/metrics/__tests__/pipeline-metrics.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `getAvgCostBrl` exportado em pipeline-metrics.ts (grep)
    - Consulta filtra delivery markers (grep por `campaign_pipeline` em contexto de filtro) e usa `usd_brl_rate_at_generation` (grep)
    - Sem referência a `VENDEO_USD_BRL_RATE` em pipeline-metrics.ts (grep ausente)
    - Testes verdes: snapshot por evento, fallback corrente, null sem custos, exclusão de delivery, degradação em falha
    - `npm run typecheck` limpo
  </acceptance_criteria>
  <done>getAvgCostBrl com snapshot por evento + fallback explícito + exclusão de delivery markers</done>
</task>

<task type="auto">
  <name>Task 2: /admin/metrics — card "Custo Médio IA" em BRL snapshotado</name>
  <files>src/app/(app)/admin/metrics/page.tsx, src/app/(app)/admin/metrics/metrics-cards.tsx, src/app/(app)/admin/metrics/page.test.tsx</files>
  <read_first>
    - src/app/(app)/admin/metrics/page.tsx (fetchMetrics :46-72; buildCampaignCards :74 — "Custo Médio IA" via data.avgCost; usdBrl service :194; MetricsCards com usdToBrlRate :235-243)
    - src/app/(app)/admin/metrics/metrics-cards.tsx (formatCost :22-26 converte value × usdToBrlRate; formatCardValue :52)
    - src/app/(app)/admin/metrics/page.test.tsx (mocks dos getters de métricas)
    - openspec/changes/fase-38-2-1-economic-snapshot/specs/admin-metrics-dashboard/spec.md (cenário "Custo Médio IA usa snapshot da taxa quando disponível")
  </read_first>
  <action>
    Em `src/app/(app)/admin/metrics/page.tsx`:
    - No `fetchMetrics`, substituir `avgCost` (USD de `getAvgCost`) por `avgCostBrl` = `getAvgCostBrl(hours, storeKind)` para o card "Custo Médio IA". Manter `getAvgCost` apenas se o health state ainda o consumir (computeHealthState usa avgCost — verificar: se usada, manter a chamada em USD; caso contrário remover).
    - `buildCampaignCards`: para o card "Custo Médio IA", pré-formatar o valor em BRL como string `R$ X,XX` (pt-BR) — o MetricsCards devolve strings sem re-conversão. O campo `value` do MetricCard aceita `number | string` (type inalterado).
    - Remover a chamada `new EconomicParameterService().getParameter("usd_brl_rate")` (:194) e o prop `usdToBrlRate` de todas as renderizações do MetricsCards.
    Em `src/app/(app)/admin/metrics/metrics-cards.tsx`:
    - Remover a prop `usdToBrlRate` e a função `formatCost` (ou reter apenas para o caso de string já formatada — o card de custo chega como string).
    - `formatCardValue`: quando `typeof card.value === "string"`, devolver o valor diretamente (já formatado/convertido na página).
    Em `src/app/(app)/admin/metrics/page.test.tsx`: atualizar mocks/asserts para `getAvgCostBrl` e para o card exibindo o valor BRL pré-formatado.
  </action>
  <verify>
    <automated>npx vitest run src/app/(app)/admin/metrics/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - metrics/page.tsx usa `getAvgCostBrl` (grep) e não passa `usdToBrlRate` ao MetricsCards (grep ausente)
    - metrics-cards.tsx sem prop `usdToBrlRate` (assinatura) e sem `formatCost` multiplicando (grep); strings devolvidas diretamente em formatCardValue
    - Nenhum `VENDEO_USD_BRL_RATE` em metrics/page.tsx ou metrics-cards.tsx (grep ausente)
    - Testes da página verdes; `npm run typecheck` limpo
  </acceptance_criteria>
  <done>Card "Custo Médio IA" em BRL snapshotado; MetricsCards sem re-conversão</done>
</task>

<task type="auto">
  <name>Task 3: Aviso "vale para novas gerações" em Configurações Econômicas</name>
  <files>src/app/(app)/admin/operation-costs/page.tsx, src/app/(app)/admin/operation-costs/operation-costs-form.tsx, src/app/(app)/admin/operation-costs/operation-costs-form.test.tsx</files>
  <read_first>
    - src/app/(app)/admin/operation-costs/page.tsx (seção Parâmetros Econômicos :98-111)
    - src/app/(app)/admin/operation-costs/operation-costs-form.tsx (ParamsForm — estrutura do form com motivo obrigatório)
    - src/app/(app)/admin/operation-costs/operation-costs-form.test.tsx (asserts de renderização)
    - openspec/changes/fase-38-2-1-economic-snapshot/specs/economic-parameters/spec.md (cenário "Página avisa que alteração vale para novas gerações")
  </read_first>
  <action>
    Adicionar à seção "Parâmetros Econômicos" da página `/admin/operation-costs` (page.tsx, próximo ao ParamsForm) e/ou no próprio `ParamsForm` (operation-costs-form.tsx) um aviso visível com texto fixo: "Alterações nos parâmetros econômicos valem para novas gerações e não recalculam o histórico já gerado." — estilo de aviso do design system (badge/texto muted ou painel informativo), sem quebrar os testes existentes de renderização.
    - Não alterar a lógica de save (PUT /api/admin/economic-parameters inalterado — D2: valor corrente continua o alvo das edições).
    - Atualizar `operation-costs-form.test.tsx` (ou page.test.tsx se aplicável): assert de que o aviso é renderizado.
  </action>
  <verify>
    <automated>npx vitest run src/app/(app)/admin/operation-costs/operation-costs-form.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Grep por "novas gerações" em operation-costs/page.tsx e/ou operation-costs-form.tsx (texto do aviso presente)
    - Testes do form/página verdes (renderização do aviso assertada)
    - `npm run typecheck` limpo
  </acceptance_criteria>
  <done>Aviso de semântica renderizado em Configurações Econômicas</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → /admin/metrics | Dados internos atravessam a página (requireAdmin — inalterado) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38.2.1-16 | Tampering | Fonte de conversão (env deprecado) | mitigate | getAvgCostBrl usa exclusivamente economic_parameters + snapshot; grep gate proíbe VENDEO_USD_BRL_RATE (D7) |
| T-38.2.1-17 | Information Disclosure | Fallback sem marcação no card | mitigate | Fallback corrente explícito apenas na ausência de snapshot (por evento); sem marcar snapshot como corrente (conversão por evento determinística) |
| T-38.2.1-18 | Integrity | Estabilidade temporal | mitigate | Snapshot por evento prioritário; alterar usd_brl_rate depois não muda períodos com snapshot (teste de estabilidade) |
| T-38.2.1-SC | Tampering | Instalação de pacotes | accept | Nenhum pacote novo nesta fase — sem gate de legitimidade |
</threat_model>

<verification>
- `npx vitest run src/lib/metrics/__tests__/pipeline-metrics.test.ts src/app/(app)/admin/metrics/page.test.tsx src/app/(app)/admin/operation-costs/operation-costs-form.test.tsx` verdes
- `npm run typecheck` + `npm run lint` limpos
- Grep gate: `grep -rn "VENDEO_USD_BRL_RATE" src/lib/metrics src/app/(app)/admin/metrics` → 0 (uso ativo)
</verification>

<success_criteria>
- Card "Custo Médio IA" usa snapshots por evento; sem snapshot → fallback corrente explícito
- Alterar usd_brl_rate não muda o card de períodos com snapshot (estabilidade temporal)
- Configurações Econômicas avisam que alterações valem para novas gerações
</success_criteria>

<output>
Create `.planning/phases/38.2.1-economic-snapshot/38-2-1-06-SUMMARY.md` when done
</output>

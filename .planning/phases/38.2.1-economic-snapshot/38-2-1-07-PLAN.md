---
phase: 38.2.1-economic-snapshot
plan: 07
type: execute
wave: 5
depends_on: [38.2.1-01, 38.2.1-02, 38.2.1-03, 38.2.1-04, 38.2.1-05, 38.2.1-06]
files_modified:
  - scripts/verify/38-2-1-f38-2-1-verification.mjs
autonomous: false
requirements: [F38.2.1-14]

must_haves:
  truths:
    - "Verificação em banco real: migration aplicada (4 colunas); tracker persiste snapshots com origem captured_at_generation; RPCs expõem campos e origens; backfill idempotente com origem backfilled_* (nenhum valor sem origem); estabilidade temporal (alterar parâmetro → histórico não muda)"
    - "Regressão completa verde: npx vitest run, npm run typecheck, npm run lint, npm run build — zero erros (1839+ testes)"
    - "UAT manual valida: alterar usd_brl_rate/credit_value_brl não muda histórico do painel; nova geração usa valores vigentes; labels estimados; origem exibida (capturado vs reconstruído vs fallback); aviso presente; /admin/metrics estável"
  artifacts:
    - path: "scripts/verify/38-2-1-f38-2-1-verification.mjs"
      provides: "Verificação SQL/integrada I1–I7 da F38.2.1 em banco real"
      contains: "usd_brl_rate_at_generation"
  key_links:
    - from: "38-2-1-f38-2-1-verification.mjs"
      to: "public.generation_events"
      via: "asserts de colunas de snapshot/origem + backfill + estabilidade temporal"
      pattern: "generation_events"
---

<objective>
Fechar a F38.2.1 com a verificação SQL/integrada em banco real (padrão I1–I6 da F38.2), a regressão completa dos 4 gates (`npx vitest run`, `npm run typecheck`, `npm run lint`, `npm run build`) e o checkpoint humano de UAT manual.

Purpose: F38.2.1-14 — provar snapshot/fallback/estabilidade temporal/nomenclatura em banco real e que nada regrediu (1839+ testes).
Output: script de verificação + gates verdes + UAT humano validado + 38-2-1-VERIFICATION.md.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@openspec/changes/fase-38-2-1-economic-snapshot/tasks.md (seção 9 — verificação SQL/integrada + gates + regressão UAT manual)
@scripts/verify/38-2-f38-2-verification.mjs (script da F38.2 — 63 asserts I1–I6 em banco real; padrão a estender, NÃO modificar)
@.planning/phases/38.2-admin-custos-operacionais/38-2-10-SUMMARY.md (precedente de verificação + checkpoint)
@.planning/phases/38.2-admin-custos-operacionais/38.2-UAT.md (padrão de UAT da fase anterior)
@supabase/migrations/20260812000001_f38_2_1_economic_snapshot.sql (plano 01)
@supabase/migrations/20260812000002_f38_2_1_rpcs_snapshot.sql (plano 03)

<interfaces>
Verificações exigidas (tasks.md 9.1 — padrão F38/F38.1):
- Migration aplica em banco real; colunas `usd_brl_rate_at_generation`/`credit_value_brl_at_generation` **e origens** `usd_brl_rate_source_at_generation`/`credit_value_brl_source_at_generation` existem
- Tracker persiste os snapshots com origem `captured_at_generation` (linha nova com as 4 colunas preenchidas)
- RPCs `admin_get_ai_operation_runs`/`_events` expõem os campos E origens (lista por run + detalhe por evento)
- Backfill idempotente: re-aplicar não altera linhas preenchidas; linhas backfilled têm origem `backfilled_*` (nunca `captured_at_generation`); nenhum valor sem origem
- Estabilidade temporal: alterar `usd_brl_rate`/`credit_value_brl` via admin_set_economic_parameter → runs com snapshot mantêm custoBrl/receitaEstimadaBrl/resultadoEstimadoBrl/margemEstimadaPct
- Fallback legacy: evento sem valor persistido deriva com parâmetro corrente + creditValueSource/revenueEstimationNote
- Nomenclatura: nenhum contrato afirma "receita real"

Gates (tasks.md 9.2): `npx vitest run` (1839+ testes), `npm run typecheck`, `npm run lint`, `npm run build`.
UAT manual (tasks.md 9.3): alterar parâmetros em Configurações Econômicas → histórico do painel não muda; nova geração usa valores vigentes; labels estimados; origem exibida (capturado vs reconstruído vs fallback); aviso "vale para novas gerações"; /admin/metrics estável.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Script de verificação SQL/integrada em banco real (I1–I7)</name>
  <files>scripts/verify/38-2-1-f38-2-1-verification.mjs</files>
  <read_first>
    - scripts/verify/38-2-f38-2-verification.mjs (estrutura/estilo: conexão service role, asserts numerados, idempotência, reversão de estado no final — NÃO modificar este arquivo)
    - supabase/migrations/20260812000001_f38_2_1_economic_snapshot.sql (backfill)
    - supabase/migrations/20260812000002_f38_2_1_rpcs_snapshot.sql (RPCs)
    - openspec/changes/fase-38-2-1-economic-snapshot/tasks.md (9.1 — lista de verificações)
  </read_first>
  <action>
    Criar `scripts/verify/38-2-1-f38-2-1-verification.mjs` no estilo do script da F38.2 (conexão via service role, asserts numerados com contagem final passed/failed/total, idempotente entre execuções, reversão de estado no final quando aplicável):

    - I1 — Migration: as 4 colunas `usd_brl_rate_at_generation`, `credit_value_brl_at_generation`, `usd_brl_rate_source_at_generation` e `credit_value_brl_source_at_generation` existem em generation_events (information_schema.columns).
    - I2 — Backfill: linhas antigas (pré-migration) têm as 2 colunas de valor preenchidas (valor do audit LAG ou 1.00) E as origens preenchidas (`backfilled_from_audit`/`backfilled_seed`); NENHUMA linha elegível com valor null; NENHUM valor persistido sem origem; re-aplicação (executar os UPDATEs do backfill de novo via SQL) não altera valores (idempotência).
    - I3 — Tracker: inserir evento de teste via AiCostTracker.record (ou INSERT direto com as 4 colunas) com snapshots 5.20/2.00 → linha persistida com valores E origens `captured_at_generation`; limpar o registro de teste no final (DELETE do operation_run_id de teste).
    - I4 — RPC lista: `admin_get_ai_operation_runs` retorna `usd_brl_rate_at_generation`/`credit_value_brl_at_generation` E as origens por run (campos presentes no JSON; snapshot do evento de referência).
    - I5 — RPC detalhe: `admin_get_ai_operation_run_events` retorna os 4 campos por evento e no run (presença).
    - I6 — Estabilidade temporal: para um run existente com snapshot, derivar custoBrl/receitaEstimadaBrl (fórmula do service) ANTES; alterar `usd_brl_rate`/`credit_value_brl` via `admin_set_economic_parameter` (reason de teste + operationId); re-derivar DEPOIS → valores idênticos (histórico não muda); reverter o parâmetro para o valor original no final (idempotência do script).
    - I7 — Fallback legacy: para evento/run sem valor persistido, verificar que a derivação aplica o parâmetro corrente e sinaliza `creditValueSource = "economic_parameter_fallback"`/`revenueEstimationNote = "estimated_from_admin_credit_value"`; valor backfilled NUNCA é tratado como `captured_at_generation`.
    - Assert final de nomenclatura: grep nos contratos TS (src/lib/ai-cost/operation-runs-service.ts + API + UI) por `receitaRealBrl|resultadoRealBrl|margemRealPct` → 0 ocorrências.
  </action>
  <verify>
    <automated>node scripts/verify/38-2-1-f38-2-1-verification.mjs 2>&1 | Select-String -Pattern "passed|failed"</automated>
  </verify>
  <acceptance_criteria>
    - Script existe com os asserts I1–I7 (grep por `usd_brl_rate_at_generation` e por labels dos asserts)
    - Execução em banco real: contagem final "N passed / 0 failed" (asserts dados-dependentes tratados como na F38.2 — validar contra estado real do banco)
    - Parâmetros econômicos revertidos ao estado original ao final (idempotência)
    - Nenhuma alteração em scripts/verify/38-2-f38-2-verification.mjs (diff vazio)
  </acceptance_criteria>
  <done>Verificação SQL/integrada I1–I7 verde em banco real</done>
</task>

<task type="auto">
  <name>Task 2: Regressão completa — vitest + typecheck + lint + build</name>
  <files>scripts/verify/38-2-1-f38-2-1-verification.mjs</files>
  <read_first>
    - .planning/phases/38.2-admin-custos-operacionais/38-2-10-SUMMARY.md (precedente — ordem dos gates e registro)
  </read_first>
  <action>
    Rodar os 4 gates na ordem e registrar os resultados no SUMMARY/verificação:
    1. `npx vitest run` — regressão completa (1839+ testes; qualquer falha relacionada a contrato renomeado ou snapshot deve ser corrigida nos arquivos dos planos 02/04/05/06 — nunca alterar o comportamento fora do escopo).
    2. `npm run typecheck` — exit 0.
    3. `npm run lint` — exit 0.
    4. `npm run build` — exit 0.
    Corrigir falhas que sejam consequência direta das mudanças desta fase (fixtures de teste, mocks, contratos). Se uma falha revelar decisão de design não prevista, registrar no SUMMARY como deviation (não inventar workaround silencioso).
  </action>
  <verify>
    <automated>npx vitest run; if ($?) { npm run typecheck }; if ($?) { npm run lint }; if ($?) { npm run build }</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` com 100% de testes passando (0 failed) — contagem registrada no SUMMARY
    - `npm run typecheck`, `npm run lint`, `npm run build` todos exit 0
    - Nenhum teste alterado para "fazer passar" removendo asserção de comportamento (apenas atualização de contrato/fixture documentada)
  </acceptance_criteria>
  <done>4 gates verdes — regressão completa sem quebras</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT manual — estabilidade temporal + nomenclatura + aviso</name>
  <what-built>Fase 38.2.1 completa: snapshots econômicos em generation_events, service/API/UI com nomenclatura estimada, /admin/metrics estável, aviso de semântica em Configurações Econômicas</what-built>
  <how-to-verify>
    1. Acessar /admin/operation-costs ("Configurações Econômicas") → o aviso "Alterações nos parâmetros econômicos valem para novas gerações..." está visível na seção Parâmetros.
    2. Anotar o custo médio IA e os KPIs atuais em /admin/ai-operation-costs (receita/resultado/margem estimados).
    3. Alterar `usd_brl_rate` (ex.: para 5.50) e `credit_value_brl` (ex.: para 2.50) com motivo em Configurações Econômicas → salvar.
    4. Recarregar /admin/ai-operation-costs → runs com snapshot MANTÊM os mesmos valores BRL de antes (histórico não mudou); runs legados sem snapshot podem mudar e exibem indicador "estimado de parâmetro atual".
    5. /admin/metrics → o card "Custo Médio IA" dos períodos com snapshot não muda após a alteração (estabilidade temporal).
    6. Gerar UMA nova campanha (ou VS) → o novo run usa os valores vigentes (5.50/2.50) — verificar no painel que o snapshot da nova entrega reflete os novos valores.
    7. Labels: painel exibe "Receita estimada"/"Resultado estimado"/"Margem estimada" — nenhum "receita real"/"lucro".
    8. Reverter os parâmetros para os valores originais com motivo (opcional — o script I6 já reverte; confirmar estado final).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script verificação → Supabase remoto | Execução de asserts via service role (leitura + alteração temporária de parâmetros revertida) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38.2.1-19 | Integrity | Script de verificação altera estado | mitigate | Alterações temporárias (parâmetros econômicos) SEMPRE revertidas ao final; inserts de teste limpos (DELETE por operation_run_id de teste); script idempotente entre execuções |
| T-38.2.1-20 | Tampering | Asserts falsos positivos | mitigate | Asserts dados-dependentes validados contra estado real (padrão 38-2-12: 1ª execução com falhas pré-existentes documentadas); contagem passed/failed explícita |
| T-38.2.1-21 | Information Disclosure | Credenciais no script | mitigate | Conexão via variáveis de ambiente/service role já estabelecida no padrão F38.2 (nenhuma credencial hardcoded) |
| T-38.2.1-SC | Tampering | Instalação de pacotes | accept | Nenhum pacote novo nesta fase — sem gate de legitimidade |
</threat_model>

<verification>
- `node scripts/verify/38-2-1-f38-2-1-verification.mjs` — N passed / 0 failed em banco real
- `npx vitest run` — 100% passando; typecheck/lint/build exit 0
- Checkpoint humano aprovado (UAT manual)
- `38-2-1-VERIFICATION.md` criado com resultados
</verification>

<success_criteria>
- Verificação I1–I7 verde em banco real (migration, backfill idempotente, tracker, RPCs, estabilidade temporal, fallback, nomenclatura)
- 4 gates verdes — regressão completa
- UAT humano aprovado: histórico estável, nova geração usa vigente, labels estimados, aviso presente
- Fase marcada completa em STATE.md/ROADMAP.md
</success_criteria>

<output>
Create `.planning/phases/38.2.1-economic-snapshot/38-2-1-07-SUMMARY.md` and `.planning/phases/38.2.1-economic-snapshot/38-2-1-VERIFICATION.md` when done
</output>

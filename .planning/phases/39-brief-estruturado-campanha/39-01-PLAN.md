---
phase: 39-brief-estruturado-campanha
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ROADMAP.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/MILESTONES.md
autonomous: true
requirements: [F39-21]

must_haves:
  truths:
    - "Nenhum artefato de runbook menciona Stripe como F39 — Stripe é F40 (v1.7) nos 6 arquivos"
    - "ROADMAP raiz lista F39 = Brief Estruturado de Campanha (v1.5, Pending) e F40 = Stripe / Monetização Pública (v1.7, Pending)"
    - ".planning/ROADMAP.md tem seção '### Phase 39 — Brief Estruturado de Campanha' e Dependencies sem nenhum 'Phase 39 (Stripe)'"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Linha 48 renumera Stripe para F40"
      contains: "F40"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Seção v1.7 renumera Stripe para F40/v1.7"
      contains: "F40"
    - path: ".planning/MILESTONES.md"
      provides: "Linha 20 renumera Stripe diferido para v1.7 (F40)"
      contains: "F40"
  key_links:
    - from: "ROADMAP.md (root)"
      to: ".planning/ROADMAP.md"
      via: "IDs de fase consistentes F39 (Brief) / F40 (Stripe) em todos os listing tables"
      pattern: "Brief Estruturado de Campanha"
---

<objective>
Concluir a renumeração D1 (F39 = Brief Estruturado de Campanha, Stripe → F40) nos 6 arquivos de runbook: `ROADMAP.md` (raiz), `.planning/ROADMAP.md`, `.planning/STATE.md`, `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/MILESTONES.md`.

Purpose: D1 — eliminar a ambiguidade onde "F39" ainda se refere a Stripe (PROJECT.md:48, REQUIREMENTS.md:~563-565, MILESTONES.md:20) e completar as lacunas restantes do .planning/ROADMAP.md. STATE.md e ROADMAP.md (raiz) já estão amplamente feitos — só verificar e completar lacunas de terminologia.
Output: 6 arquivos de runbook consistentes com a renumeração F39/F40.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/MILESTONES.md
@.planning/ROADMAP.md
@.planning/STATE.md
@ROADMAP.md
@openspec/changes/fase-39-brief-estruturado-campanha/tasks.md (seção 1 — trackings 1.1-1.6, D1)
@openspec/changes/fase-39-brief-estruturado-campanha/design.md (D1 — renumeração)

<interfaces>
Estado verificado (39-PATTERNS.md, seção Runbook trackings) — o que JÁ está feito vs. pendente:

| Arquivo | Já feito | Pendente |
|---------|----------|----------|
| `ROADMAP.md` (raiz) | Linha 206: `| 39. Brief Estruturado de Campanha \| v1.5 \| 0/0 \| ○ Pending \| — \|`; linha 207: `| 40. Stripe / Monetização Pública \| v1.7 \| 0/0 \| ○ Pending \| — \|`; bullets v1.5 (57/62) | Verificar `<details open>` v1.5 mencione "brief estruturado" e checar menções "F39 (Stripe)" residuais |
| `.planning/ROADMAP.md` | Nota "Phase numbering" (linha 7) F39/F40; tabela Progress (39-40); notas de renumeração (~406/467); Dependency Graph (~734/750-769) | Dependencies "Phase 39 (Stripe)" → F40 (~571/617/644/661 — checar remanescentes); adicionar seção "### Phase 39 — Brief Estruturado de Campanha"; rodapé "Last updated" (855) |
| `.planning/STATE.md` | `current_phase: 39` (linha 21); body/Last updated (19) | Verificar tabela "Next Phases" (F39 in progress, F40 future) (~687-688) |
| `.planning/PROJECT.md` | — | **Linha 48**: `- **Stripe / compra real de créditos**: adiado para F39 (v1.7, pós-beta)` → F40 |
| `.planning/REQUIREMENTS.md` | — | **~563-565**: seção v1.7 "Stripe será implementada como F39/v1.7" → **F40/v1.7** (linha 565 ainda diz F39) |
| `.planning/MILESTONES.md` | — | **Linha 20**: `- Stripe / Monetização Pública diferido para v1.7 (F39)` → **(F40)** |

Regra de edição (D1): edições in-place em markdown; histórico cronológico NÃO é reescrito; fonte da verdade = OpenSpec F39.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correções pendentes — PROJECT.md, REQUIREMENTS.md, MILESTONES.md (F39 → F40)</name>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/MILESTONES.md</files>
  <action>
    Aplicar as 3 correções de renumeração pendentes, editando in-place (sem reescrever histórico):

    1. `.planning/PROJECT.md` linha 48: trocar `adiado para F39 (v1.7, pós-beta)` por `adiado para F40 (v1.7, pós-beta)` — a linha descreve "Stripe / compra real de créditos" e deve referenciar a fase correta após a renumeração D1.
    2. `.planning/REQUIREMENTS.md` (~563-565): localizar a seção v1.7 que diz "Stripe será implementada como F39/v1.7" (ou variante com "F39") e trocar pela referência `F40/v1.7`. Verificar também menções residuais a "F39" na mesma seção v1.7 (que se refiram a Stripe) e renumerá-las — NÃO tocar menções legítimas a F39 Brief fora da seção v1.7.
    3. `.planning/MILESTONES.md` linha 20: trocar `diferido para v1.7 (F39)` por `diferido para v1.7 (F40)` na linha do milestone Stripe / Monetização Pública.

    Para cada arquivo: após a edição, rodar busca por `F39[^0-9]`/`F39\b` no trecho editado e confirmar que nenhuma ocorrência residual se refere a Stripe. Atualizar a linha/rodapé "Last updated" do arquivo se existir (padrão dos outros arquivos de runbook; manter timestamp existente + nota curta, sem reescrever head).
  </action>
  <verify>
    <automated>Select-String -LiteralPath ".planning\PROJECT.md" -Pattern "F40" ; Select-String -LiteralPath ".planning\MILESTONES.md" -Pattern "F40"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` linha 48 contém `F40` (grep por `Stripe / compra real de créditos` retorna a linha com F40)
    - `.planning/REQUIREMENTS.md` seção v1.7 contém `F40/v1.7`; nenhuma ocorrência `F39` na seção v1.7 se refere a Stripe
    - `.planning/MILESTONES.md` linha 20 contém `(F40)` (grep por `Stripe / Monetização Pública diferido` retorna a linha com F40)
    - Nenhum artefato de runbook referencia Stripe como F39
  </acceptance_criteria>
  <done>PROJECT.md, REQUIREMENTS.md e MILESTONES.md renumerados para F40 — sem menção residual a Stripe como F39</done>
</task>

<task type="auto">
  <name>Task 2: Verificação e completude — ROADMAP.md raiz, .planning/ROADMAP.md, .planning/STATE.md</name>
  <files>ROADMAP.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
    Verificar e completar as lacunas restantes (estes 3 arquivos já estão amplamente feitos — só fechar o que falta):

    1. `ROADMAP.md` (raiz): confirmar linhas 206-207 (F39 Brief v1.5 Pending, F40 Stripe v1.7 Pending). Checar o bullet do `<details open>` do v1.5 (~57/62) — deve mencionar "brief estruturado"; se citar "F39 (Stripe)" trocar para F40. Rodar uma varredura final por `F39` no arquivo e corrigir qualquer ocorrência que se refira a Stripe (as referentes a Brief v1.5 permanecem).
    2. `.planning/ROADMAP.md`: (a) na seção "Dependencies" (~571/617/644/661), trocar qualquer `Phase 39 (Stripe)` por `Phase 40 (Stripe)` (F40); (b) adicionar a seção `### Phase 39 — Brief Estruturado de Campanha` na listagem de fases se ainda não existir, com objetivo breve derivado da nota "Phase numbering" (linha 7): F39 = Brief Estruturado de Campanha (v1.5), F40 = Stripe (v1.7); (c) atualizar o rodapé "Last updated" (~855) com nota breve da renumeração; manter as tabelas Progress e Dependency Graph como estão (verificadas ✓).
    3. `.planning/STATE.md`: confirmar `current_phase: 39` (linha 21) e a tabela "Next Phases" (~687-688) com F39 in progress / F40 future (renumerada). Se a tabela ainda listar Stripe como F39, corrigir; atualizar corpo/Last updated apenas se o estado real mudar (status planning já registrado).
  </action>
  <verify>
    <automated>Select-String -LiteralPath "ROADMAP.md" -Pattern "Brief Estruturado" ; Select-String -LiteralPath ".planning\ROADMAP.md" -Pattern "Phase 39"</automated>
  </verify>
  <acceptance_criteria>
    - `ROADMAP.md` raiz: sem `F39` associado a Stripe; linha 206/207 confirmadas (grep por `39\. Brief Estruturado` e `40\. Stripe`)
    - `.planning/ROADMAP.md`: seção "### Phase 39 — Brief Estruturado de Campanha" presente (grep por `### Phase 39`); sem `Phase 39 (Stripe)` residual (grep por `Phase 39 (Stripe)` → 0 ocorrências); rodapé Last updated atualizado
    - `.planning/STATE.md`: `current_phase: 39` presente; tabela Next Phases com F39 in progress / F40 future (sem Stripe como F39)
  </acceptance_criteria>
  <done>ROADMAP raiz, .planning/ROADMAP.md e STATE.md coerentes com a renumeração F39 Brief / F40 Stripe</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runbook → planner/executor | Documentos de planejamento editados in-place; sem código, sem dados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Integrity | Trackings F39/F40 nos 6 arquivos | mitigate | Grep gates por `F39 (Stripe)`/`Phase 39 (Stripe)` → 0 ocorrências; state verified não é reescrito (edições in-place, histórico preservado) |
| T-39-02 | Spoofing | Referência cruzada de fase errada | mitigate | Fonte da verdade = OpenSpec F39 (D1); tabela de estado verificado no <interfaces> guia cada edição; verificação dupla por arquivo |
| T-39-SC | Tampering | Instalação de pacotes | accept | Nenhum pacote novo nesta fase (markdown apenas) — sem gate de legitimidade |
</threat_model>

<verification>
- Grep global: `F39 (Stripe)` em ROADMAP.md (raiz), .planning/ROADMAP.md, STATE.md, PROJECT.md, REQUIREMENTS.md, MILESTONES.md → 0 ocorrências
- Grep por `F40`, `### Phase 39 — Brief Estruturado de Campanha`, `Brief Estruturado de Campanha` presente nos arquivos corretos
- Nenhuma mudança de código nesta fase (não rodar vitest/typecheck em planos de runbook — verificação feita por grep seletivo)
</verification>

<success_criteria>
- Os 6 arquivos de runbook referenciam F39 = Brief Estruturado de Campanha (v1.5) e F40 = Stripe / Monetização Pública (v1.7) de forma consistente
- Nenhuma menção residual a Stripe como F39 em qualquer artefato de planejamento
- .planning/ROADMAP.md com seção da Phase 39 e Dependencies renumeradas
</success_criteria>

<output>
Create `.planning/phases/39-brief-estruturado-campanha/39-01-SUMMARY.md` when done
</output>
---
phase: 39-brief-estruturado-campanha
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/campaign/brief.ts
  - src/lib/campaign/brief-schema.ts
  - src/lib/campaign/__tests__/brief.test.ts
autonomous: true
requirements: [F39-01, F39-02, F39-03, F39-04, F39-05, F39-06, F39-07, F39-08, F39-12, F39-20]

must_haves:
  truths:
    - "O contrato de domínio CampaignBrief existe com os 5 blocos product/commercial/media/creativeContext/metadata, sem server-only"
    - "Fronteira type-level: CampaignProductImageInput (runtime) pode conter base64 (dataUrl?); CampaignBriefSnapshotImage (snapshot persistido) NUNCA tem base64 — tipos separados por construção; productImage fica em media.images[]"
    - "validity e legalNotice são estruturas { enabled, ... } aninhadas em commercial — nunca seção top-level"
    - "Existe exatamente 1 imagem role: 'primary' (invariante zod); brief sem imagem é rejeitado"
    - "metadata.schemaVersion = 'campaign_brief_v1' existe no runtime; snapshot o carrega no ROOT, não no metadata"
  artifacts:
    - path: "src/lib/campaign/brief.ts"
      provides: "Constantes + interfaces do domínio + helper getCampaignLegalNotice (sem server-only)"
      contains: "CampaignBriefSchemaVersion"
    - path: "src/lib/campaign/brief-schema.ts"
      provides: "Zod por domínio + invariante exatamente-1-primary + .strict() por domínio"
      contains: "campaignBriefSchema"
    - path: "src/lib/campaign/__tests__/brief.test.ts"
      provides: "Testes de contrato 8.1-8.6 + 8.15 (brief mínimo, validade, legalNotice on/off, 1 primary, rejeição sem imagem, source manual, themeId null, schemaVersion clareza)"
      contains: "primary"
  key_links:
    - from: "src/lib/campaign/brief.ts"
      to: "src/lib/campaign/brief-schema.ts"
      via: "Tipos do domínio espelham z.infer dos schemas (ou hand-written D4); ambos SEM server-only"
      pattern: "CampaignBrief"
    - from: "src/lib/campaign/brief-schema.ts"
      to: "mediaSchema"
      via: "superRefine contando images.filter(i => i.role === 'primary') === 1"
      pattern: "primary"
---

<objective>
Criar o CONTRATO DE DOMÍNIO da F39: `src/lib/campaign/brief.ts` (constantes, interfaces e helper `getCampaignLegalNotice` — módulo puro de tipos, SEM `server-only`, D4) + `src/lib/campaign/brief-schema.ts` (zod por domínio com invariante de exatamente 1 imagem `primary`, D7/F39-08) + `__tests__/brief.test.ts` (testes de contrato 8.1-8.6 e 8.15).

Purpose: D3/D6/D7/D8/D9/D10 — estabelecer o domínio estruturado `product/commercial/media/creativeContext/metadata` que substitui o transporte flat, com validade e aviso ilustrativo estruturados. Este plano define apenas TYPES + helper; o mapper `buildCampaignBriefFromFlat` e o builder `buildCampaignBriefSnapshot` serão implementados e expandidos no plano 39-04 (não duplicar — se implementá-los aqui, garantir que 39-04 os complementa com testes de round-trip).
Output: tipos de domínio + schemas zod + testes de contrato do domínio.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@openspec/changes/fase-39-brief-estruturado-campanha/tasks.md (seção 2 — 2.1 a 2.8; seção 8 — testes 8.1-8.6, 8.15)
@openspec/changes/fase-39-brief-estruturado-campanha/specs/campaign-brief-contract/spec.md (CampaignBrief cinco blocos; schemaVersion só no runtime D6; brief.ts sem server-only)
@src/lib/campaign/types.ts (padrão de módulo de tipos puro, sem server-only — linhas 1-58)
@src/lib/snapshot.ts (padrão de builder puro — linhas 15-32; NÃO implementar o builder aqui)
@src/lib/image-generation/schema.ts (padrão zod: .strict(), .optional(), .default(), z.enum — linhas 8-39; GenerateImageRequestSchema)
@src/lib/actions/__tests__/store.test.ts (padrão baseFixture com overrides spread-last — linhas 180-195, 201-204)

<interfaces>
Contrato exato a criar (per tasks.md 2.1-2.8 + specs):

```
CampaignBriefSchemaVersion = "campaign_brief_v1"                 (const)
CampaignBriefSource = "web_form" | "api"                          (union const)
ProductSource = "manual" | "catalog"                              (union const)
CampaignImageRole = "primary" | "variation" | "combo_item" | "reference"
CampaignImageSource = "upload" | "camera"

CampaignProductImageInput (RUNTIME — pode conter base64 em memória/transporte, D7):
  { id: string (uuid); role: CampaignImageRole; source: CampaignImageSource;
    mimeType: string; dataUrl?: string }
    // SEM provided/storagePath/productAssetId — esses campos são do SNAPSHOT
    // (fronteira explícita: runtime pode conter base64; snapshot persistido NUNCA)

CampaignBriefSnapshotImage (SNAPSHOT persistido — SEM dataUrl por construção):
  { id: string; role: CampaignImageRole; source: CampaignImageSource;
    provided: true; mimeType: string;
    storagePath?: string; productAssetId?: string }               (D6/D7/F39-12)

CampaignBriefProduct:
  { source: ProductSource; catalogProductId?: string;             (D3 — catálogo subsequente)
    name: string; brand?: string; sizeOrVariant?: string; description?: string }

CampaignOfferValidity: { enabled: boolean; displayText?: string; endDate?: string }  (D8)
CampaignOfferLegalNotice: { enabled: boolean; text?: string }                        (D9)

CampaignBriefCommercial:                                            (F39-04/D11)
  { intent: CampaignIntent; originalPriceCents?: number; discountedPriceCents?: number;
    badgeText?: string; validity?: CampaignOfferValidity; legalNotice?: CampaignOfferLegalNotice;
    availabilityNotes?: string; campaignDetails?: string; additionalDetails?: string;
    hook?: string; cta?: string; objective?: string;               (campos adormecidos, lar canônico 1:1)
    targetChannel?: string; format?: string }

CampaignBriefCreativeContext: { preserveImageContext?: boolean; themeId?: string | null; sensitiveConstraints?: string }  (D10 — themeId reservado, sempre null)

CampaignBrief:
  { product: CampaignBriefProduct; commercial: CampaignBriefCommercial;
    media: { images: CampaignProductImageInput[] };
    creativeContext: CampaignBriefCreativeContext; metadata: { schemaVersion: "campaign_brief_v1"; source: CampaignBriefSource } }

CampaignBriefSnapshot:                                            (type só — builder no 39-04)
  { schemaVersion: "campaign_brief_v1";                            (schemaVersion no ROOT, D6)
    product: ...; commercial: ...;
    media: { images: CampaignBriefSnapshotImage[] };
    creativeContext: ...; metadata: { source: CampaignBriefSource } }   (SEM schemaVersion no metadata)

getCampaignLegalNotice(brief: CampaignBrief): CampaignOfferLegalNotice | undefined
  → leitura direta de brief.commercial.legalNotice (sem espelho, D9)
```

Zod (brief-schema.ts, tasks 2.7): `productSchema`/`commercialSchema`/`mediaSchema`/`creativeContextSchema`/`metadataSchema` + `campaignBriefSchema` — `.strict()` em cada domínio (typoguard), `z.enum([...])` para unions, `z.string().uuid()` para id, e em `mediaSchema` um `.superRefine()` que conta `images.filter(i => i.role === "primary").length === 1` (F39-08). Tipos podem ser `z.infer`-derived ou hand-written em brief.ts (D4 permite contrato em brief.ts) — se ambos existirem, garantir consistência mútua.

Regra de ausência (D8/D9 — aplica a testes): campo não informado → ausente (undefined); NUNCA fabricar `{ enabled: false }` para validity/legalNotice.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: brief.ts — constantes + interfaces do domínio + getCampaignLegalNotice</name>
  <files>src/lib/campaign/brief.ts</files>
  <read_first>
    - src/lib/campaign/types.ts (linhas 1-58 — estilo de módulo de tipos: union consts, interfaces, sem server-only; CampaignIntent reutilizado)
    - src/lib/image-generation/schema.ts (linhas 8-39 — GenerateImageRequest: storeId, productName, prices, badgeText, campaignIntent, preserveImageContext, mandatoryArtworkText, productImageDataUrl — campos-fonte do mapper em 39-04)
    - openspec/changes/fase-39-brief-estruturado-campanha/specs/campaign-brief-contract/spec.md (bloco completo de contrato)
  </read_first>
  <action>
    Criar `src/lib/campaign/brief.ts` como módulo de tipos puro (SEM `server-only` — D4, contrato compartilhado client/server), seguindo o estilo de `src/lib/campaign/types.ts:1-58`:

    1. Constantes e unions: `export const CampaignBriefSchemaVersion = "campaign_brief_v1" as const;` e `export type CampaignBriefSource = "web_form" | "api";`, `export type ProductSource = "manual" | "catalog";`, `export type CampaignImageRole = "primary" | "variation" | "combo_item" | "reference";`, `export type CampaignImageSource = "upload" | "camera";`. Reutilizar `CampaignIntent` importado de `../campaign/types` (ou redefinir localmente como alias para manter o módulo autocontido — escolher e documentar; não duplicar em ambos os lugares sem o alias).
    2. Interfaces na ordem: `CampaignProductImageInput` (RUNTIME, com `dataUrl?: string` — base64 só em memória/transporte; SEM `provided`/`storagePath`/`productAssetId` — esses são do snapshot), `CampaignBriefSnapshotImage` (SNAPSHOT, SEM `dataUrl` por construção — F39-12; `provided: true`; `storagePath?`/`productAssetId?` reservados para catálogo futuro), `CampaignBriefProduct`, `CampaignOfferValidity` (D8), `CampaignOfferLegalNotice` (D9), `CampaignBriefCommercial` (incluindo campos adormecidos `hook?`/`cta?`/`objective?`/`targetChannel?`/`format?` com lar canônico 1:1, D11), `CampaignBriefCreativeContext` (com `themeId?: string | null` — D10), `CampaignBrief` (5 blocos: product/commercial/media/creativeContext/metadata), `CampaignBriefSnapshot` (schemaVersion no ROOT; metadata SEM schemaVersion — D6). Exatamente as formas do bloco <interfaces>. FRONTEIRA EXPLÍCITA: `CampaignBrief.media.images` usa o tipo RUNTIME (`CampaignProductImageInput[]`, pode ter `dataUrl`); `CampaignBriefSnapshot.media.images` usa `CampaignBriefSnapshotImage[]` (NUNCA base64, D6/D7).
    3. Helper puro: `export function getCampaignLegalNotice(brief: CampaignBrief): CampaignOfferLegalNotice | undefined` — retorna `brief.commercial.legalNotice` diretamente (leitura do campo canônico, sem espelho — D9, task 2.5). Não pode lançar nem acessar DB.
    4. NÃO implementar `buildCampaignBriefFromFlat` nem `buildCampaignBriefSnapshot` neste plano (ficam para 39-04 — evitar duplicação). NÃO importar `server-only`.
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\lib\campaign\brief.ts" -Pattern "CampaignBriefSchemaVersion|CampaignBriefSnapshot|getCampaignLegalNotice"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe com: `CampaignBriefSchemaVersion`, `CampaignBriefSource`, `ProductSource`, `CampaignImageRole`, `CampaignImageSource`, `CampaignProductImageInput` (RUNTIME — com `dataUrl?`, SEM `provided`/`storagePath`/`productAssetId`), `CampaignBriefSnapshotImage` (SNAPSHOT — sem `dataUrl`, com `provided: true` + reservados), `CampaignBriefProduct`, `CampaignOfferValidity`, `CampaignOfferLegalNotice`, `CampaignBriefCommercial`, `CampaignBriefCreativeContext`, `CampaignBrief`, `CampaignBriefSnapshot` (schemaVersion no ROOT, metadata sem schemaVersion)
    - Fronteira type-level: `dataUrl` EXISTE apenas em `CampaignProductImageInput`; `CampaignBriefSnapshotImage` NÃO possui campo `dataUrl` (grep por `dataUrl` dentro do bloco snapshot → ausente) e NÃO possui `provided`/`storagePath`/`productAssetId` no runtime (fora do bloco snapshot)
    - `CampaignBrief.media.images: CampaignProductImageInput[]` (runtime) vs `CampaignBriefSnapshot.media.images: CampaignBriefSnapshotImage[]` (snapshot) — tipos distintos por construção (D6/D7)
    - `getCampaignLegalNotice` presente, puro, lê `brief.commercial.legalNotice`
    - SEM `server-only` (grep por `server-only` → 0 ocorrências)
    - SEM `buildCampaignBriefFromFlat`/`buildCampaignBriefSnapshot` (deixados para 39-04)
  </acceptance_criteria>
  <done>brief.ts com constantes, interfaces do domínio e getCampaignLegalNotice — contrato compartilhado sem server-only</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: brief-schema.ts — zod por domínio + invariante exatamente-1-primary</name>
  <files>src/lib/campaign/brief-schema.ts, src/lib/campaign/__tests__/brief.test.ts</files>
  <read_first>
    - src/lib/image-generation/schema.ts (linhas 8-39 — GenerateImageRequestSchema: .strict(), .optional(), .default(), z.enum; e 32-36 inputValidationOverride + 154-168 validationContext — padrão de validação custom)
    - src/lib/campaign/brief.ts (recém-criado — tipos que os schemas validam)
    - openspec/changes/fase-39-brief-estruturado-campanha/specs/campaign-brief-contract/spec.md (invariante 1 primary; per-domínio strict)
  </read_first>
  <behavior>
    - Test 1: brief válido com 1 imagem `primary` → `campaignBriefSchema.safeParse` success
    - Test 2: brief com 2 imagens `primary` (ou 0) → safeParse failure com erro apontando media.images (invariante D7/F39-08)
    - Test 3: imagem com `role: "made_up"` ou chave extra no product → failure (.strict() typoguard)
    - Test 4: schemaVersion fora de "campaign_brief_v1" no metadata runtime → failure
  </behavior>
  <action>
    Criar `src/lib/campaign/brief-schema.ts` (SEM `server-only`, D4) seguindo o padrão de `schema.ts:8-39` — zod por domínio, sem schema monolítico top-level (per tasks.md 2.7):

    1. `productSchema`: `.strict()`, `source: z.enum(["manual", "catalog"]).default("manual")`, `catalogProductId: z.string().uuid().optional()` (não aponta tabela — D3), `name: z.string().min(1)`, `brand?`/`sizeOrVariant?`/`description?` opcionais.
    2. `commercialSchema`: `.strict()`, `intent: z.enum(["offer","spotlight","exclusive"]).optional().default("offer")`, preços `z.number().int().nonnegative()/positive().optional()`, `badgeText?`, `validity: z.object({ enabled: z.boolean(), displayText: z.string().optional(), endDate: z.string().optional() }).optional()` (D8 — ausente nunca `{ enabled: false }`), `legalNotice: z.object({ enabled: z.boolean(), text: z.string().optional() }).optional()` (D9), availabilityNotes/campaignDetails/additionalDetails/hook/cta/objective/targetChannel/format opcionais (adormecidos com lar canônico 1:1, D11).
    3. `mediaSchema`: `.strict()`, `images: z.array(imageSchema).min(1, "Imagem do produto é obrigatória")` + `.superRefine` (ou `.refine`) garantindo EXATAMENTE 1 item com `role === "primary"` (F39-08). `imageSchema` (valida o RUNTIME — espelha `CampaignProductImageInput`): `.strict()`, `id: z.string().uuid()`, `role: z.enum([...CampaignImageRole])`, `source: z.enum(["upload","camera"])`, `mimeType: z.string()`, `dataUrl: z.string().optional()` — SEM `provided`/`storagePath`/`productAssetId` (esses pertencem ao tipo SNAPSHOT `CampaignBriefSnapshotImage`, que não passa por este schema — o snapshot não é validado por zod nesta fase, é construído pelo builder do 39-04).
    4. `creativeContextSchema`: `.strict()`, `preserveImageContext: z.boolean().optional()`, `themeId: z.string().nullable().optional()` (D10 — reservado, sempre null por enquanto), `sensitiveConstraints: z.string().optional()`.
    5. `metadataSchema`: `.strict()`, `schemaVersion: z.literal(CampaignBriefSchemaVersion)` (D6 — no runtime) e `source: z.enum(["web_form","api"]).default("web_form")`.
    6. `campaignBriefSchema`: `.strict()` juntando os 5 domínios. Exportar `export type CampaignBrief = z.infer<typeof campaignBriefSchema>` se os tipos de brief.ts forem z.infer-derived — caso contrário, garantir consistência manual com as interfaces de brief.ts (D4 permite contrato em brief.ts; a escolha deve ser documentada e os testes validam o cruzamento).
    7. Comportamento esperado (tests 1-4 acima): invariante de 1 primary falha com 0 ou 2; .strict() rejeita chaves extras; schemaVersion inválido rejeitado.
  </action>
  <verify>
    <automated>npx vitest run src/lib/campaign/__tests__/brief.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `brief-schema.ts` exporta `productSchema`/`commercialSchema`/`mediaSchema`/`creativeContextSchema`/`metadataSchema`/`campaignBriefSchema` (grep por `Schema =` no arquivo)
    - `.strict()` presente em todos os 5 domínios (grep por `\.strict\(\)` → 5+ ocorrências)
    - Invariante 1 primary: grep por `superRefine` ou `refine` em contexto de `mediaSchema` contando `role === "primary"`
    - Testes verdes: válido (1 primary), rejeição 0/2 primary, typoguard, schemaVersion inválido
    - `npm run typecheck` limpo
  </acceptance_criteria>
  <done>brief-schema.ts com zod por domínio, .strict() e invariante de exatamente 1 primary; testes verdes</done>
</task>

<task type="auto">
  <name>Task 3: brief.test.ts — testes de contrato do domínio (8.1-8.6, 8.15)</name>
  <files>src/lib/campaign/__tests__/brief.test.ts</files>
  <read_first>
    - src/lib/actions/__tests__/store.test.ts (padrão baseFixture + overrides spread-last, linhas 180-195; assert aninhado 201-204, 250-256)
    - src/lib/campaign/brief.ts e brief-schema.ts (recém-criados)
    - openspec/changes/fase-39-brief-estruturado-campanha/tasks.md (seção 8 — 8.1 a 8.6, 8.15)
  </read_first>
  <action>
    EXPANDIR/MERGEAR o arquivo `src/lib/campaign/__tests__/brief.test.ts` já CRIADO na Task 2 (que contém os 4 behaviors de schema: brief válido com 1 primary, rejeição 0/2 primary, typoguard .strict(), schemaVersion inválido) — NÃO sobrescrever: preservar os 4 comportamentos iniciais e ADICIONAR os casos de contrato abaixo via describe/it complementares, reutilizando o mesmo fixture base (`baseBrief(overrides: Partial<CampaignBrief> = {})` com overrides spread-last, padrão `store.test.ts:180-195`):

    - Teste 8.1 (F39-01/08): brief mínimo válido — `product.source = "manual"`, `product.name` nomeado, `media.images` com 1 item `role: "primary"`; `campaignBriefSchema.safeParse(brief).success === true`.
    - Teste 8.2 (F39-05/D8): oferta com preço + validade — `commercial.validity = { enabled: true, displayText: "Somente hoje" }`; validade aninhada em `commercial` (NUNCA top-level); `expect(brief.commercial.validity?.enabled).toBe(true)` e displayText preservado.
    - Teste 8.3 (F39-06/D9): `legalNotice.enabled = false` → nada na arte (assertar que o campo continua `{ enabled: false }` e que `getCampaignLegalNotice(brief)` retorna a estrutura; a propagação para prompt/copy/review é coberta em 39-05/39-06); `enabled = true` + `text` → `getCampaignLegalNotice(brief).text` retorna o texto (compat `mandatoryArtworkText`).
    - Teste 8.4 (F39-08): sempre existe exatamente 1 `role: "primary"` — safeParse falha com 0 ou 2; borda sem imagem → safeParse failure (regra de 400 em `schema.ts:30` permanece no transporte; aqui é o invariante do domínio).
    - Teste 8.5 (F39-02/D3): `product.source` default `"manual"` quando ausente (via schema default); `catalogProductId` opcional e válido como uuid; assertar que NÃO aponta tabela (só string).
    - Teste 8.6 (F39-07/D10): `themeId` presente e `null` no contrato — `creativeContext.themeId` aceita `null`; `expect(brief.creativeContext.themeId).toBeNull()` para brief com themeId null.
    - Teste 8.15 (F39-11/D6): clareza de `metadata.schemaVersion` — no RUNTIME `brief.metadata.schemaVersion === "campaign_brief_v1"`; o SNAPSHOT (tipo `CampaignBriefSnapshot`) tem schemaVersion no ROOT e `metadata` SEM schemaVersion — ASSERTAR POR RUNTIME: `expect("schemaVersion" in snapshot.metadata).toBe(false)` (a prova primária é o assert de runtime, não @ts-expect-error; tsc também valida a estrutura).
    - Ausência de `dataUrl` no snapshot: assertar por RUNTIME `expect("dataUrl" in snapshot.media.images[0]).toBe(false)` (além da varredura recursiva 8.12 em 39-04). A validação estrutural fica com tsc (snapshot type sem o campo); NÃO usar @ts-expect-error como prova primária.
  </action>
  <verify>
    <automated>npx vitest run src/lib/campaign/__tests__/brief.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Os 4 behaviors iniciais da Task 2 permanecem presentes no arquivo (grep por `safeParse` com caso válido + falha de primary + typoguard + schemaVersion inválido)
    - NOVOS casos adicionados: brief mínimo válido, oferta+validade, legalNotice on/off via getCampaignLegalNotice, exatamente 1 primary (0 e 2 rejeitados), source default manual, themeId null, schemaVersion runtime-vs-snapshot por ASSERT DE RUNTIME (grep por `"schemaVersion" in snapshot.metadata` e `"dataUrl" in snapshot.media.images[0]` → presentes; `@ts-expect-error` → ausente no arquivo)
    - Todos os testes verdes (`npx vitest run src/lib/campaign/__tests__/brief.test.ts`) — arquivo expandido, NÃO recriado
    - `npm run typecheck` e `npm run lint` limpos
  </acceptance_criteria>
  <done>brief.test.ts expandido (Task 2 + testes 8.1-8.6 + 8.15) verdes; contrato de domínio validado</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente → rota → brief-schema | Dados não-confiáveis do formulário atravessam o zod antes de virar domínio |
| brief.ts → pipeline | Contrato compartilhado consumido por prompt/copy/review (client e server) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-02 | Spoofing | Parsing de input malformado | mitigate | Zod `.strict()` por domínio (typo guard — rejeita chaves extras); `z.enum` para unions; uuid para `id`/`catalogProductId` |
| T-39-02a | Tampering | Invariante de mídia | mitigate | `mediaSchema.superRefine` exige exatamente 1 `role: "primary"` (F39-08); teste 8.4 cobre 0/2 primaries |
| T-39-02b | Information Disclosure | Base64 no snapshot | mitigate | `CampaignBriefSnapshotImage` sem campo `dataUrl` POR TIPO (F39-12) — runtime (`CampaignProductImageInput`) pode ter base64, snapshot persistido NUNCA (fronteira type-level); assert de runtime `expect("dataUrl" in snapshot.media.images[0]).toBe(false)` no 8.15 + varredura recursiva 8.12 em 39-04 (D6/D7) |
| T-39-02c | Tampering | Ausência fabricada | mitigate | Regra canônica de ausência: validade/aviso ausentes ficam ausentes (nunca `{ enabled: false }` fabricado) — testada em 8.3/8.4 (D8/D9) |
| T-39-02d | Elevation of Privilege | Contrato no server | mitigate | brief.ts SEM `server-only` (contrato compartilhado — D4); sem imports de runtime/DB |
| T-39-SC | Tampering | Instalação de pacotes | accept | Nenhum pacote novo (zod/vitest já no projeto) — sem gate de legitimidade |
</threat_model>

<verification>
- `npx vitest run src/lib/campaign/__tests__/brief.test.ts` — todos verdes
- `npm run typecheck` + `npm run lint` limpos
- Grep: `server-only` em src/lib/campaign/brief.ts e brief-schema.ts → 0 ocorrências
- Grep: `dataUrl` ausente do bloco `CampaignBriefSnapshotImage` (presente apenas em `CampaignProductImageInput`); `provided`/`storagePath`/`productAssetId` ausentes do bloco `CampaignProductImageInput` (presentes apenas no snapshot)
- Grep: `@ts-expect-error` em brief.test.ts → 0 (provas por asserts de runtime: `"schemaVersion" in snapshot.metadata` / `"dataUrl" in snapshot.media.images[0]`)
- Nenhum `buildCampaignBriefFromFlat`/`buildCampaignBriefSnapshot` implementado (ficam para 39-04)
</verification>

<success_criteria>
- Contrato de domínio completo em brief.ts (5 blocos + snapshot com schemaVersion no ROOT) e schemas zod per-domain com invariante de 1 primary
- validity/legalNotice estruturados em commercial (nunca top-level); themeId null reservado; getCampaignLegalNotice puro
- Testes de contrato 8.1-8.6 + 8.15 verdes; typecheck/lint limpos
</success_criteria>

<output>
Create `.planning/phases/39-brief-estruturado-campanha/39-02-SUMMARY.md` when done
</output>
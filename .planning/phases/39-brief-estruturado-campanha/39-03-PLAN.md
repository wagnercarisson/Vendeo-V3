---
phase: 39-brief-estruturado-campanha
plan: 03
type: execute
wave: 2
depends_on: [39-02]
files_modified:
  - src/components/campaign/types.ts
  - src/lib/campaign/types.ts
  - src/lib/store-identity-service.ts
  - src/lib/copy/mapper.ts
  - src/lib/image-generation/services/image-generation-service.ts
  - src/lib/actions/__tests__/store.test.ts
  - src/lib/image-generation/services/__tests__/image-generation-service.test.ts
autonomous: true
requirements: [F39-10, F39-14, F39-20]

must_haves:
  truths:
    - "O wrapper de transporte chama-se ResolvedCampaignContext mantendo EXATAMENTE o shape { campaignInput, store, brandProfile, identity }"
    - "buildCampaignBrief retorna Promise<ResolvedCampaignContext> sem quebrar nenhum caller (rota, re-export em store.ts)"
    - "InputSnapshot (morto, 0 imports em src/) é substituído pelo tipo re-exportado CampaignBriefSnapshot"
    - "brand_profile.campaign_brief (campo do Brand Profiler) permanece uma string inalterada"
  artifacts:
    - path: "src/components/campaign/types.ts"
      provides: "Interface renomeada ResolvedCampaignContext (mesmo shape)"
      contains: "ResolvedCampaignContext"
    - path: "src/lib/campaign/types.ts"
      provides: "Re-export do tipo CampaignBriefSnapshot (InputSnapshot removido)"
      contains: "CampaignBriefSnapshot"
    - path: "src/lib/store-identity-service.ts"
      provides: "buildCampaignBrief com Promise<ResolvedCampaignContext>"
      contains: "ResolvedCampaignContext"
  key_links:
    - from: "src/lib/store-identity-service.ts"
      to: "src/components/campaign/types.ts"
      via: "import type com ResolvedCampaignContext no lugar de CampaignBrief"
      pattern: "ResolvedCampaignContext"
    - from: "src/lib/actions/store.ts"
      to: "src/lib/store-identity-service.ts"
      via: "Re-export por Parameters<typeof _buildCampaignBrief> — sem touch de tipo (D4)"
      pattern: "buildCampaignBrief"
---

<objective>
Renomear o wrapper de transporte `CampaignBrief` → `ResolvedCampaignContext` (D4/F39-10) em `src/components/campaign/types.ts` e co-migrar TODOS os call sites: `src/lib/store-identity-service.ts` (assinatura de retorno de `buildCampaignBrief`), `src/lib/copy/mapper.ts`, `src/lib/image-generation/services/image-generation-service.ts` e os testes que os importam. Adicionalmente (F39-14): substituir a interface morta `InputSnapshot` em `src/lib/campaign/types.ts` pelo re-export do tipo `CampaignBriefSnapshot` do domínio (definido em brief.ts no plano 39-02) e validar com o contrato 8.21 (shape preservado sem quebrar callers).

Purpose: D4 — liberar o nome `CampaignBrief` para o NOVO domínio estruturado (criado no plano 39-02 em `src/lib/campaign/brief.ts`). O shape `{ campaignInput, store, brandProfile, identity }` NÃO muda — apenas o nome.
Output: rename completo sem quebra de callers; InputSnapshot substituído pelo alias versionado.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@openspec/changes/fase-39-brief-estruturado-campanha/tasks.md (seção 3 — 3.1 a 3.3; seção 8 — 8.21)
@openspec/changes/fase-39-brief-estruturado-campanha/specs/campaign-types/spec.md (rename sem mudança de shape; brand_profile.campaign_brief inalterado)
@src/components/campaign/types.ts (interface atual `CampaignBrief` 35-55 — shape a preservar; SEGMENT_PALETTES/helpers 73-111 intocados)
@src/lib/store-identity-service.ts (`buildCampaignBrief` 203-228; import na linha 1)
@src/lib/copy/mapper.ts (import na linha 1; `mapBriefToCopyDirectorInput` 91-115)
@src/lib/image-generation/services/image-generation-service.ts (import na linha 5; `body` alias 95 — NÃO remover aqui, faz parte do 39-06)
@src/lib/campaign/types.ts (`InputSnapshot` 38-58 — interface morta, 0 imports em src/, confirmado por busca)
@src/lib/actions/store.ts (re-export via `Parameters<typeof _buildCampaignBrief>` — linhas 17-18)
@src/lib/actions/__tests__/store.test.ts (import na linha 2; describe `buildCampaignBrief` 180-293)
@src/lib/image-generation/services/__tests__/image-generation-service.test.ts (imports nas linhas 1-3; `createMinimalBrief` 13-39)

<interfaces>
Shape do wrapper (NÃO muda — só o nome; `src/components/campaign/types.ts:35-55`):

```typescript
export type CampaignInput = Omit<GenerateImageRequest, 'storeId'>;

export interface ResolvedCampaignContext {            // ← era CampaignBrief
  campaignInput: CampaignInput;
  store: {
    name: string;
    segment: string;
    subsegment: string | null;
    toneOfVoice: string | null;
    positioning: string | null;
    shortDescription: string | null;
    slogan: string | null;
    brandColor: string;
  };
  brandProfile: BrandProfileSnapshot | null;
  identity: {
    state: IdentityState;
    imageUrl: string | null;
    directive: string;
  };
}
```

Import sites a co-migrar (verificados):
- `src/lib/store-identity-service.ts:1` — `import type { ... CampaignBrief, CampaignInput } from "@/components/campaign/types";` → trocar `CampaignBrief` por `ResolvedCampaignContext`
- `src/lib/copy/mapper.ts:1` — idem (só o nome do tipo no import)
- `src/lib/image-generation/services/image-generation-service.ts:5` — `import type { CampaignBrief } from "@/components/campaign/types";` → `ResolvedCampaignContext`
- `src/lib/actions/__tests__/store.test.ts:2` — idem
- `src/lib/image-generation/services/__tests__/image-generation-service.test.ts:3` — idem (e o helper `createMinimalBrief` passa a devolver `ResolvedCampaignContext`)

`buildCampaignBrief` (store-identity-service.ts:203-228): só a anotação de retorno muda (`Promise<CampaignBrief>` → `Promise<ResolvedCampaignContext>`); corpo idêntico. `deriveDirective` (8-25) intocado. Callers sem touch de tipo: `route.ts:241` (inferência) e `src/lib/actions/store.ts:17-18` (re-export por `Parameters`).

`InputSnapshot` → `CampaignBriefSnapshot` (F39-14, lib/campaign/types.ts:38-58):
```typescript
import type { CampaignBriefSnapshot } from "./brief";   // ./brief criado no plano 39-02 (wave 1)
export type { CampaignBriefSnapshot };
// remover a interface InputSnapshot morta (38-58) — record shape { }
```
`CampaignRecord.input_snapshot` (types.ts:10) permanece `Record<string, unknown> | null` (jsonb tolerant — D6, sem migration).

`brand_profile.campaign_brief` (Brand Profiler): campo string; NÃO tocar (task 3.3, D4).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename no types.ts de componentes + swap InputSnapshot → CampaignBriefSnapshot</name>
  <files>src/components/campaign/types.ts, src/lib/campaign/types.ts, src/lib/campaign/brief.ts</files>
  <read_first>
    - src/components/campaign/types.ts (35-55 — interface CampaignBrief; 1-3 imports; 73-111 helpers que NÃO mudam)
    - src/lib/campaign/types.ts (38-58 — InputSnapshot morta; 10 — CampaignRecord.input_snapshot)
    - src/lib/campaign/brief.ts (recém-criado no plano 39-02 — tipo CampaignBriefSnapshot exportado)
  </read_first>
  <action>
    1. Em `src/components/campaign/types.ts`: renomear `export interface CampaignBrief` → `export interface ResolvedCampaignContext` (linha ~37) mantendo o shape EXATO (campos e JSDoc do bloco <interfaces>). Adicionar JSDoc curto: "Wrapper de transporte resolvido (store + identity + payload) — NÃO é o domínio CampaignBrief (brief.ts); shape estável consumido pelo pipeline". NÃO tocar `CampaignInput`, `BrandProfileSnapshot`, `IdentityState`, SEGMENT_PALETTES ou helpers (73-111).
    2. Em `src/lib/campaign/types.ts`: REMOVER a interface `InputSnapshot` (38-58, morta — 0 imports confirmados em src/) e substituir por re-export do domínio: `import type { CampaignBriefSnapshot } from "./brief"; export type { CampaignBriefSnapshot };` (alias versionado, F39-14/D6). Manter `CampaignRecord.input_snapshot` como `Record<string, unknown> | null` (sem migration — D6).
    3. Garantir que `src/lib/campaign/brief.ts` (plano 39-02, wave 1) exporte `CampaignBriefSnapshot` — o re-export no passo 2 importa de `./brief`, e este plano declara `depends_on: [39-02]` (wave 2) para que o tipo já exista antes do typecheck. Verificar com typecheck local após escrever.
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\components\campaign\types.ts" -Pattern "ResolvedCampaignContext" ; Select-String -LiteralPath "src\lib\campaign\types.ts" -Pattern "CampaignBriefSnapshot"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/campaign/types.ts` define `ResolvedCampaignContext` com shape idêntico ao `CampaignBrief` anterior (grep por `ResolvedCampaignContext`; nenhum `Interface CampaignBrief` restante)
    - `src/lib/campaign/types.ts` re-exporta `CampaignBriefSnapshot` de `./brief` e NÃO contém mais `interface InputSnapshot` (grep por `InputSnapshot` → apenas referências residuais consertadas ou nenhuma)
    - `CampaignRecord.input_snapshot` segue `Record<string, unknown> | null`
    - `npm run typecheck` limpo
  </acceptance_criteria>
  <done>Rename do wrapper em components + swap InputSnapshot → alias CampaignBriefSnapshot no lib/campaign/types.ts</done>
</task>

<task type="auto">
  <name>Task 2: Co-migração dos call sites + contrato 8.21 (shape preservado)</name>
  <files>src/lib/store-identity-service.ts, src/lib/copy/mapper.ts, src/lib/image-generation/services/image-generation-service.ts, src/lib/actions/__tests__/store.test.ts, src/lib/image-generation/services/__tests__/image-generation-service.test.ts</files>
  <read_first>
    - src/lib/store-identity-service.ts (import 1, buildCampaignBrief 203-228, deriveDirective 8-25)
    - src/lib/copy/mapper.ts (import 1, mapBriefToCopyDirectorInput 91-115 — só o import muda)
    - src/lib/image-generation/services/image-generation-service.ts (import 5 — apenas o import; `body` alias 95 fica para 39-06)
    - src/lib/actions/__tests__/store.test.ts (import 2; describe buildCampaignBrief 180-293 — adicionar caso 8.21)
    - src/lib/image-generation/services/__tests__/image-generation-service.test.ts (imports 1-3; createMinimalBrief 13-39)
  </read_first>
  <action>
    Co-migrar os call sites (tasks 3.1-3.2, 8.21):
    1. `src/lib/store-identity-service.ts`: linha 1 — trocar `CampaignBrief` por `ResolvedCampaignContext` no import type; linha ~203 — anotação de retorno `Promise<CampaignBrief>` → `Promise<ResolvedCampaignContext>`. Corpo e shape NÃO mudam. (Import do novo domínio `CampaignBrief` NÃO entra aqui — será usado no 39-04/39-07.)
    2. `src/lib/copy/mapper.ts`: linha 1 — trocar `CampaignBrief` por `ResolvedCampaignContext` no import (a ASSINATURA `mapBriefToCopyDirectorInput(brief: CampaignBrief, ...)` também muda para `ResolvedCampaignContext`; o rename do parâmetro é coberto aqui — o corpo `brief.campaignInput.*` continua válido pois o shape não mudou).
    3. `src/lib/image-generation/services/image-generation-service.ts`: linha 5 — trocar o import. NÃO remover o alias `body = brief.campaignInput` (seam 6.1 é do plano 39-06; manter o código funcionando com o shape renomeado).
    4. `src/lib/actions/__tests__/store.test.ts`: linha 2 — import; dentro do describe `buildCampaignBrief` (180-293), adicionar o teste 8.21: `buildCampaignBrief(snapshot, input)` retorna um objeto com `campaignInput`/`store`/`brandProfile`/`identity` (shape preservado — `expect(result).toHaveProperty('store.name')`, `expect(result).toHaveProperty('identity.state')`, `expect(result.brandProfile).toBeNull()` para snapshot sem brand profile) e o tipo compila como `ResolvedCampaignContext` (assertar com `const r: ResolvedCampaignContext = result`).
    5. `src/lib/image-generation/services/__tests__/image-generation-service.test.ts`: imports 1-3 — trocar; `createMinimalBrief` (13-39) passa a construir e devolver `ResolvedCampaignContext` (shape idêntico; mesmos campos — apenas a anotação de tipo muda).
    6. `brand_profile.campaign_brief`: NÃO tocar (task 3.3 — campo string do Brand Profiler permanece inalterado).
    IMPORTANTE: neste plano NENHUM arquivo pode importar `CampaignBrief` vindo de `@/components/campaign/types` (o domínio CampaignBrief vive em `@/lib/campaign/brief` e só entra no 39-04+). Grep final por `CampaignBrief` em imports de components/campaign/types → 0.
  </action>
  <verify>
    <automated>npx vitest run src/lib/actions/__tests__/store.test.ts src/lib/image-generation/services/__tests__/image-generation-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Grep por `import type { .*CampaignBrief.* } from "@/components/campaign/types"` → 0 ocorrências em src/ (todo import usa `ResolvedCampaignContext`)
    - `store-identity-service.ts` declara `Promise<ResolvedCampaignContext>` (grep)
    - `mapper.ts` e `image-generation-service.ts` usam `ResolvedCampaignContext` nos tipos de assinatura
    - Teste 8.21 presente em store.test.ts (shape preservado, compila como ResolvedCampaignContext) — grep por `8.21` ou `ResolvedCampaignContext` no teste
    - `createMinimalBrief` em image-generation-service.test.ts devolve `ResolvedCampaignContext` e os testes do service seguem verdes
    - `input_snapshot`/`brand_profile.campaign_brief` não alterados; `npm run typecheck` + `npm run lint` limpos
  </acceptance_criteria>
  <done>Call sites co-migrados; teste 8.21 verde; nenhum import residual de CampaignBrief de components/campaign/types</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| components/campaign/types.ts → pipeline | Contrato de transporte compartilhado (client/server) consumido por route, copy, image-generation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-03 | Tampering | Import residual do nome antigo | mitigate | Grep gate: `import .*CampaignBrief.* from "@/components/campaign/types"` → 0 em src/ (rename completo); typecheck falha se algum site esquecido |
| T-39-03a | Integrity | Quebra de shape em callers | mitigate | Shape INALTERADO (rename puro, D4); teste 8.21 asserta `campaignInput`/`store`/`brandProfile`/`identity`; re-export store.ts via `Parameters` sem touch |
| T-39-03b | Information Disclosure | InputSnapshot exposto no record | mitigate | `input_snapshot` permanece `Record<string, unknown> | null` (jsonb tolerante, D6) — sem migration nem reader novo |
| T-39-03c | Spoofing | Nome de domínio colidido | mitigate | JSDoc no ResolvedCampaignContext distingue wrapper vs. domínio CampaignBrief (brief.ts) — evita uso cruzado equivocado |
| T-39-SC | Tampering | Instalação de pacotes | accept | Nenhum pacote novo — sem gate de legitimidade |
</threat_model>

<verification>
- `npx vitest run src/lib/actions/__tests__/store.test.ts src/lib/image-generation/services/__tests__/image-generation-service.test.ts` — verdes
- `npm run typecheck` + `npm run lint` limpos
- Grep gate: `grep -rn "CampaignBrief" src/ --include="*.ts" | grep "@/components/campaign/types"` → 0 (após remover comentários); `CampaignBrief` restante em src/ deve referenciar apenas o domínio `./brief` (39-02)
- Grep: `InputSnapshot` → 0 ocorrências funcionais em src/
</verification>

<success_criteria>
- Wrapper renomeado para ResolvedCampaignContext em todo src/ sem quebra de comportamento
- InputSnapshot substituído por alias CampaignBriefSnapshot (versionado) — sem migration
- Teste 8.21 verde: shape preservado e callers sem quebra
</success_criteria>

<output>
Create `.planning/phases/39-brief-estruturado-campanha/39-03-SUMMARY.md` when done
</output>
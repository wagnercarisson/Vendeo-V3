---
phase: 39-brief-estruturado-campanha
plan: 04
type: execute
wave: 3
depends_on: [39-02, 39-03]
files_modified:
  - src/lib/campaign/brief.ts
  - src/lib/campaign/__tests__/brief-mapper.test.ts
  - src/lib/campaign/__tests__/brief-snapshot.test.ts
autonomous: true
requirements: [F39-09, F39-11, F39-12, F39-13, F39-14, F39-20]

must_haves:
  truths:
    - "buildCampaignBriefFromFlat é função pura (sem DB) que converte GenerateImageRequest flat → CampaignBrief na fronteira da rota"
    - "Round-trip flat→brief preserva campos equivalentes e campos adormecidos no lar canônico (hook/cta/objective/targetChannel/format → commercial; sensitiveConstraints → creativeContext)"
    - "Ausência é canônica: campos não informados ficam ausentes (nunca { enabled: false } fabricado); validity string → { enabled: true, displayText }; mandatoryArtworkText → legalNotice { enabled: true, text }"
    - "buildCampaignBriefSnapshot deriva imagens DO PRÓPRIO brief, remove dataUrl, preserva id/role/source/mimeType/provided — snapshot imutável por construção"
    - "Snapshot versionado: schemaVersion 'campaign_brief_v1' no ROOT; metadata do snapshot SEM schemaVersion; varredura recursiva confirma que snapshot nunca contém dataUrl/base64"
  artifacts:
    - path: "src/lib/campaign/brief.ts"
      provides: "buildCampaignBriefFromFlat + buildCampaignBriefSnapshot (funções puras adicionadas ao contrato)"
      contains: "buildCampaignBriefFromFlat"
    - path: "src/lib/campaign/__tests__/brief-mapper.test.ts"
      provides: "Testes de round-trip flat→brief→snapshot (8.7-8.11, 8.14)"
      contains: "buildCampaignBriefFromFlat"
    - path: "src/lib/campaign/__tests__/brief-snapshot.test.ts"
      provides: "Varredura recursiva sem base64 + imutabilidade por construção (8.12, 8.13)"
      contains: "dataUrl"
  key_links:
    - from: "buildCampaignBriefFromFlat"
      to: "GenerateImageRequest"
      via: "param input: GenerateImageRequest + storeId: string + source: CampaignBriefSource default 'web_form'"
      pattern: "GenerateImageRequest"
    - from: "buildCampaignBriefSnapshot"
      to: "CampaignBrief"
      via: "deriva de brief.media.images (nunca recebe imagem externa); sem dataUrl no output"
      pattern: "CampaignBriefSnapshot"
---

<objective>
Implementar a conversão flat→domínio e o builder de snapshot versionado em `src/lib/campaign/brief.ts`:
- `buildCampaignBriefFromFlat(input: GenerateImageRequest, storeId: string, source: CampaignBriefSource = "web_form"): CampaignBrief` — FUNÇÃO PURA, único ponto de conversão flat→domínio na fronteira da rota (D5/F39-09)
- `buildCampaignBriefSnapshot(brief: CampaignBrief): CampaignBriefSnapshot` — deriva as imagens DO PRÓPRIO brief, remove `dataUrl`, produz snapshot versionado `campaign_brief_v1` (D6/F39-11/12/13)
E criar os testes de contrato `brief-mapper.test.ts` (8.7-8.11, 8.14) e `brief-snapshot.test.ts` (8.12, 8.13).

Purpose: D5/D6/D7/D8/D9/D11 — substituir o snapshot flat manual da rota (route.ts:357-380) pelo builder versionado canônico e transportar as regras canônicas de normalização (preserveImageContext com oferta, mimeType image/jpeg) do código atual para o mapper puro.
Output: mapper + builder puros com testes de round-trip e varredura sem base64.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@openspec/changes/fase-39-brief-estruturado-campanha/tasks.md (seção 4 — 4.1 a 4.6; seção 5 — 5.1 a 5.4; seção 8 — testes 8.7-8.15)
@openspec/changes/fase-39-brief-estruturado-campanha/specs/campaign-brief-mapper/spec.md (buildCampaignBriefFromFlat — única conversão na fronteira, D5; tabela de mapeamento de campos; regra de ausência)
@openspec/changes/fase-39-brief-estruturado-campanha/specs/campaign-brief-snapshot/spec.md (CampaignBriefSnapshot; schemaVersion root; base64 nunca; jsonb tolerante)
@src/lib/campaign/brief.ts (criado no plano 39-02 — constantes, interfaces, getCampaignLegalNotice)
@src/app/api/campaign/generate-image/route.ts (bloco flat snapshot 357-380 — regras canônicas a portar: preserveImageContext normalization 376-378, mimeType image/jpeg + productImage { provided: true } 379)
@src/lib/snapshot.ts (padrão de builder puro derive-from-own-input — linhas 15-32)
@src/lib/copy/mapper.ts (padrão de builder flat→domain puro `buildCommercialFrame` — linhas 5-12)
@src/lib/actions/__tests__/store.test.ts (padrão baseFixture + overrides, linhas 180-195)
@src/lib/image-generation/services/__tests__/image-generation-service.test.ts (createMinimalBrief 13-39 — base para fixture do brief-snapshot.test.ts)

<interfaces>
Assinaturas a implementar (tasks.md 4.1, 5.1):

```typescript
export function buildCampaignBriefFromFlat(
  input: GenerateImageRequest,                 // schema.ts:8-39 (storeId, productName, preços, badgeText, campaignIntent, preserveImageContext, description, availabilityNotes, campaignDetails, additionalDetails, validity, sensitiveConstraints, mandatoryArtworkText, productImageDataUrl, inputValidationOverride, hook, cta, objective, targetChannel, format)
  storeId: string,                             // preservado em metadata? NÃO — storeId fica no transporte; domínio NÃO o carrega
  source: CampaignBriefSource = "web_form"
): CampaignBrief
```

Mapeamento canônico D5/D11 (tabela do spec campaign-brief-mapper):
| Flat (GenerateImageRequest) | Domínio |
|---|---|
| `productName` | `product.name` |
| `description` | `product.description` |
| `originalPriceCents`/`discountedPriceCents`/`badgeText` | `commercial.*` |
| `campaignIntent` (default "offer") | `commercial.intent` |
| `hook`/`cta`/`objective`/`targetChannel`/`format` | `commercial.*` (campos adormecidos, lar canônico 1:1 — D11) |
| `availabilityNotes`/`campaignDetails`/`additionalDetails` | `commercial.*` |
| `validity` (string) | `commercial.validity = { enabled: true, displayText: validity }` — ausente → campo ausente (D8) |
| `mandatoryArtworkText` | `commercial.legalNotice = { enabled: true, text }` — ausente → campo ausente (D9) |
| `preserveImageContext` | `creativeContext.preserveImageContext` NORMALIZADO: para `campaignIntent === "offer"` → `false`; senão `input.preserveImageContext ?? false` (regra canônica portada de route.ts:376-378) |
| `sensitiveConstraints` | `creativeContext.sensitiveConstraints` |
| `themeId` | `creativeContext.themeId = null` (D10 — reservado, sempre null) |
| `productImageDataUrl` (+ mimeType **default "image/jpeg"**) | `media.images[0] = { id: uuid, role: "primary", source: "upload", dataUrl, provided: true, mimeType }` — se ausente: `media.images = []` (o mapper NÃO lança; a regra de borda 400 fica no zod do transporte, schema.ts:30). PÓS-CONDIÇÃO (F39-08): em produção o zod garante `productImageDataUrl` presente (400 antes do mapper), então `media.images` com 1 primary satisfaz `min(1)` + exatamente-1 do `campaignBriefSchema`; teste 8.7 assere `safeParse(result).success` para input com imagem. | 
| — | `metadata = { schemaVersion: "campaign_brief_v1", source }` |

`buildCampaignBriefSnapshot(brief: CampaignBrief): CampaignBriefSnapshot`:
```typescript
// schemaVersion no ROOT; metadata SEM schemaVersion (D6)
// media.images: brief.media.images.map(i => ({ id, role, source, provided: true, mimeType, (storagePath?/productAssetId?) })) — REMOVENDO dataUrl
// validity/legalNotice ausentes no brief → ausentes no snapshot (5.3 — nunca fabricar enabled: true)
```

Nota (5.4/F39-14): `src/lib/campaign/types.ts` já re-exporta `CampaignBriefSnapshot` (plano 39-03); este plano implementa o BUILDER que produz esse tipo.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildCampaignBriefFromFlat — mapper puro flat→domínio</name>
  <files>src/lib/campaign/brief.ts, src/lib/campaign/__tests__/brief-mapper.test.ts</files>
  <read_first>
    - src/lib/campaign/brief.ts (recém-criado — tipo CampaignBrief e interfaces)
    - src/app/api/campaign/generate-image/route.ts (357-380 — snapshot flat com regra preserveImageContext 376-378 e mimeType 379, a portar)
    - src/lib/copy/mapper.ts (5-12 — padrão de função pura de transformação)
    - src/lib/image-generation/schema.ts (8-39 — GenerateImageRequest, todos os campos flat)
    - openspec/changes/fase-39-brief-estruturado-campanha/specs/campaign-brief-mapper/spec.md (tabela de mapeamento)
  </read_first>
  <behavior>
    - Test 1: input flat completo → brief com `product.name === productName`, `commercial.intent === campaignIntent`, preços/badge em commercial, `metadata.source === "web_form"`
    - Test 2: `validity: "válida até 30/09"` → `commercial.validity = { enabled: true, displayText: "válida até 30/09" }`; sem validity → `commercial.validity` ausente (undefined); NUNCA `{ enabled: false }`
    - Test 3: `mandatoryArtworkText: "Imagem ilustrativa"` → `commercial.legalNotice = { enabled: true, text: "Imagem ilustrativa" }`; sem → ausente
    - Test 4: `campaignIntent === "offer"` + preserveImageContext true → `creativeContext.preserveImageContext === false` (regra canônica route.ts:376-378); spotlight sem preserve → undefined/false; `themeId === null`
    - Test 5: `productImageDataUrl` presente → `media.images[0]` com `role: "primary"`, `source: "upload"`, `mimeType: "image/jpeg"`, `dataUrl` preservada no runtime
    - Test 6: campos adormecidos (hook/cta/objective/targetChannel/format/sensitiveConstraints) mapeados 1:1 para o lar canônico; não informados → ausentes
    - Test 7 (PÓS-CONDIÇÃO 8.7): para QUALQUER input com `productImageDataUrl` presente (precondição garantida pelo zod do transporte), `campaignBriefSchema.safeParse(result).success === true` — o mapper NUNCA produz brief inválido (invariante exatamente-1-primary satisfeito); self-check explícito no teste para round-trip completo
  </behavior>
  <action>
    Adicionar `buildCampaignBriefFromFlat` em `src/lib/campaign/brief.ts` — função pura, sem DB, sem throw além do zod do transporte:

    1. Assinatura: `(input: GenerateImageRequest, storeId: string, source: CampaignBriefSource = "web_form") => CampaignBrief`. IMPORTANTE: `storeId` é parâmetro (necessário para a conversão na rota), mas o domínio NÃO carrega storeId — o transporte o separa (route.ts:307 `const { storeId, ...campaignInput } = parsed.data`).
    2. `metadata` = `{ schemaVersion: CampaignBriefSchemaVersion, source }` (const literal "campaign_brief_v1").
    3. `product` = `{ source: "manual", name: input.productName, description: input.description (ausente → campo ausente) }` (descrição vai para product per D5 — verificar com d5 spec; se o spec colocar description em product, seguir spec).
    4. `commercial` = mapear preços/badgeText/intent + adormecidos hook/cta/objective/targetChannel/format + availabilityNotes/campaignDetails/additionalDetails. `validity`: extrair string → `{ enabled: true, displayText }` (só se presente). `legalNotice`: `mandatoryArtworkText` → `{ enabled: true, text }` (só se presente). SEMPRE aplicar a regra de ausência (campos flat undefined/"" → campo do domínio ausente, NUNCA `{ enabled: false }` fabricado — D8/D9).
    5. `creativeContext` = `{ preserveImageContext: input.campaignIntent === "offer" ? false : (input.preserveImageContext ?? false), themeId: null, sensitiveConstraints (se presente) }` — portar EXATAMENTE a regra canônica de route.ts:376-378.
    6. `media.images` = se `input.productImageDataUrl` presente: `[{ id: crypto.randomUUID(), role: "primary", source: "upload", dataUrl: input.productImageDataUrl, provided: true, mimeType: input.productImageMimeType ?? "image/jpeg" }]` (verificar se GenerateImageRequest tem productImageMimeType — se não, fixar "image/jpeg" como default portado de route.ts:379); senão `[]` (a regra 400 fica no zod do transporte, route.ts:137-154 — o mapper NÃO lança). NOTA DE PÓS-CONDIÇÃO: o schema do domínio (`campaignBriefSchema`, mediaSchema) exige `min(1)` + exatamente 1 `primary` — em PRODUÇÃO a precondição é garantida pelo zod da rota (`productImageDataUrl: z.string().min(1)` em schema.ts:30 → 400 antes do mapper; tasks.md 7.3/4.5), então o resultado do mapper com imagem SEMPRE passa no safeParse. Se por algum caminho `media.images` ficar vazio, o próprio `campaignBriefSchema.safeParse(result)` falharia — por isso o Test 7 assere a pós-condição para entradas com imagem; o mapper NÃO lança (erros ficam no zod).
    7. Campos idênticos no domínio: NÃO portar storeId/inputValidationOverride para o domínio (inputValidationOverride continua lido FLAT na rota — route.ts:295; decisão documentada).
  </action>
  <verify>
    <automated>npx vitest run src/lib/campaign/__tests__/brief-mapper.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `buildCampaignBriefFromFlat` exportado em brief.ts (grep)
    - Regra canônica portada: preserveImageContext offer → false (grep por `"offer"` na função + código preservando o comportamento real)
    - Mapeamento validity → `{ enabled: true, displayText }` (grep por `validity` no retorno da função)
    - mandatoryArtworkText → `legalNotice { enabled: true, text }` (grep por `mandatoryArtworkText` na função)
    - Testes 1-7 verdes (round-trip preserva campos equivalentes, regra de ausência, 1 primary, adormecidos no lar canônico, PÓS-CONDIÇÃO safeParse)
    - Test 7 presente: `campaignBriefSchema.safeParse(buildCampaignBriefFromFlat(inputComImagem, storeId)).success === true` (grep por `safeParse` no brief-mapper.test.ts)
    - Sem server-only, sem throw no mapper (função pura)
  </acceptance_criteria>
  <done>Mapper puro flat→domínio implementado com testes de round-trip e regras canônicas portadas</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: buildCampaignBriefSnapshot — builder versionado sem base64</name>
  <files>src/lib/campaign/brief.ts, src/lib/campaign/__tests__/brief-snapshot.test.ts</files>
  <read_first>
    - src/lib/snapshot.ts (15-32 — padrão derive-from-own-input, builder puro)
    - src/lib/campaign/brief.ts (tipos CampaignBrief + CampaignBriefSnapshot — recém-criados)
    - openspec/changes/fase-39-brief-estruturado-campanha/specs/campaign-brief-snapshot/spec.md (schemaVersion root; metadata sem schemaVersion; base64 nunca)
  </read_first>
  <behavior>
    - Test 1: snapshot de brief com 1 imagem → `media.images[0]` tem `id`/`role: "primary"`/`source`/`mimeType`/`provided: true` e NENHUM `dataUrl`
    - Test 2: `snapshot.schemaVersion === "campaign_brief_v1"` (ROOT); `snapshot.metadata` NÃO tem `schemaVersion` (acesso compila-falha com @ts-expect-error)
    - Test 3: brief sem validity/legalNotice → snapshot sem esses campos (ausência preservada, nunca enabled fabricado)
    - Test 4: serialização `JSON.stringify(snapshot)` NUNCA contém `data:image/` / `base64` (varredura recursiva por tipo e por chave)
  </behavior>
  <action>
    Adicionar `buildCampaignBriefSnapshot(brief: CampaignBrief): CampaignBriefSnapshot` em `src/lib/campaign/brief.ts`:

    1. Builder puro — deriva as imagens DO PRÓPRIO brief (nunca recebe imagem externa): `media.images = brief.media.images.map(i => ({ id: i.id, role: i.role, source: i.source, provided: true as const, mimeType: i.mimeType, ...(storagePath/productAssetId se presentes) }))` — SEM `dataUrl` (por construção e por tipo, D6/D7/F39-12).
    2. `schemaVersion: CampaignBriefSchemaVersion` no ROOT do objeto retornado; `metadata: { source: brief.metadata.source }` SEM schemaVersion (D6 — 5.2).
    3. Normalização de ausência (5.3, D8/D9): `validity`/`legalNotice` ausentes no brief → AUSENTES no snapshot (nunca `{ enabled: true }` fabricado). Propagar os 5 blocos estruturalmente.
    4. Imutabilidade por construção (5.4/D6/F39-13): builder retorna objeto NOVO a partir do brief (sem mutação do input; snapshot serializado uma vez).
    Em paralelo, criar `src/lib/campaign/__tests__/brief-snapshot.test.ts` com fixture baseada em `createMinimalBrief` (image-generation-service.test.ts:13-39) reescrita para o shape estruturado, implementando os comportamentos 1-4 acima, incluindo a NOVA varredura recursiva por chave (`dataUrl`/`base64`/`data:image/`) — sem analog existente no repo (39-PATTERNS.md "No Analog Found"), construir com TS puro: função recursiva que percorre `Record` e arrays, coletando chaves que contêm dataUrl/base64 ou valores que contêm base64.
  </action>
  <verify>
    <automated>npx vitest run src/lib/campaign/__tests__/brief-snapshot.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `buildCampaignBriefSnapshot` exportado (grep) e puro (sem DB/server-only)
    - `schemaVersion` no ROOT; metadata sem schemaVersion (grep por `schemaVersion` no builder: no retorno raiz e em metadata ausente)
    - Testes verdes: imagens sem dataUrl no snapshot, schemaVersion root-only, ausência preservada, varredura recursiva sem base64 (grep por `base64`/`data:image` no teste)
    - Brief de entrada NÃO é mutado (imutabilidade por construção)
    - `npm run typecheck` limpo
  </acceptance_criteria>
  <done>Builder snapshot versionado puro com varredura sem base64 e testes verdes</done>
</task>

<task type="auto">
  <name>Task 3: Compat payload benchmark → mesmo brief equivalente (8.14)</name>
  <files>src/lib/campaign/__tests__/brief-mapper.test.ts</files>
  <read_first>
    - scripts/benchmark.ts (332-375 — blocos flat com validity heurística "detalhes-variados" exercitada no cenário 10)
    - src/lib/campaign/__tests__/brief-mapper.test.ts (criado na Task 1)
    - openspec/changes/fase-39-brief-estruturado-campanha/tasks.md (8.14 — compat payload atual)
  </read_first>
  <action>
    Adicionar o teste 8.14 em `brief-mapper.test.ts`: para um payload representativo dos benchmark scenarios (`validity: "válida até 31/12"`, `availabilityNotes`, `campaignDetails`, `additionalDetails`, campos adormecidos completos, `campaignIntent: "offer"`, productImageDataUrl), assertar que `buildCampaignBriefFromFlat(payload, 'benchmark')` produz um brief com:
    - `commercial.validity = { enabled: true, displayText: "válida até 31/12" }` — compat com cenário 10 `detalhes-variados` (a heurística de string atual em buildCommercialRepertoire será substituída no 39-06; aqui o mapper já estrutura o displayText)
    - `media.images[0]` com `source: "upload"`, `mimeType: "image/jpeg"`, `role: "primary"`, `dataUrl` preservada no runtime (base64 em memória)
    - `metadata.schemaVersion === "campaign_brief_v1"` e `metadata.source === "web_form"` (default; passar "web_form" explicitamente ou testar o default)
    - adormecidos mapeados 1:1 no lar canônico
    E assertar a regra de ausência no mesmo teste: payload SEM validity/mandatoryArtworkText → `commercial.validity`/`legalNotice` undefined (8.7b/8.8 — nunca `{ enabled: false }`).
  </action>
  <verify>
    <automated>npx vitest run src/lib/campaign/__tests__/brief-mapper.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Teste 8.14 presente (compat payload benchmark → brief equivalente) e verde — grep por `detalhes-variados` ou `válida` no teste
    - Regra de ausência assertada no mesmo arquivo
    - Todos os testes do arquivo verdes; `npm run typecheck` limpo
  </acceptance_criteria>
  <done>Compat payload benchmark garantido por teste; round-trip completo coberto</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rota → buildCampaignBriefFromFlat | Input flat validado (zod) cruza a fronteira de conversão para o domínio |
| builder → input_snapshot | Snapshot persiste em jsonb — nunca pode carregar base64 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-04 | Tampering | Mapper perde campos (round-trip incompleto) | mitigate | Testes 8.7/8.7a: round-trip flat→brief preserva equivalentes e adormecidos no lar canônico; 8.14 compat benchmark |
| T-39-04a | Spoofing | Ausência fabricada (enabled: false) | mitigate | Regra canônica de ausência testada (8.7b/8.8); validity/legalNotice ausentes → ausentes |
| T-39-04b | Information Disclosure | Base64 vazando para snapshot | mitigate | `CampaignBriefSnapshotImage` sem dataUrl POR TIPO + builder remove dataUrl + varredura recursiva 8.12 (dataUrl/base64/data:image) |
| T-39-04c | Integrity | Snapshot mutável pós-serialização | mitigate | Imutabilidade por construção (builder derive-from-own-input, nunca muta input — D6, teste 8.13) |
| T-39-04d | Tampering | Regra canônica preserveImageContext perdida | mitigate | Porta explícita de route.ts:376-378 para o mapper + teste 4 (offer → false), evitando comportamento divergente |
| T-39-SC | Tampering | Instalação de pacotes | accept | Nenhum pacote novo (uuid via crypto.randomUUID do runtime, sem dep nova) — sem gate de legitimidade |
</threat_model>

<verification>
- `npx vitest run src/lib/campaign/__tests__/brief-mapper.test.ts src/lib/campaign/__tests__/brief-snapshot.test.ts` — todos verdes
- `npm run typecheck` + `npm run lint` limpos
- Grep: `input_snapshot` usa `buildCampaignBriefSnapshot` apenas na rota (39-07); nenhum uso novo nesses testes que persistem (builder puro testado unitariamente)
- Sem migration SQL (jsonb tolerante — D6)
</verification>

<success_criteria>
- Mapper puro flat→domínio com regras canônicas portadas (preserveImageContext, mimeType, ausência)
- Builder snapshot versionado imutável sem base64, schemaVersion root-only
- Testes 8.7-8.15 verdes (round-trip, lar canônico, ausência, compat benchmark, varredura)
</success_criteria>

<output>
Create `.planning/phases/39-brief-estruturado-campanha/39-04-SUMMARY.md` when done
</output>
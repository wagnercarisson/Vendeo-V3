---
phase: 39-brief-estruturado-campanha
plan: 05
type: execute
wave: 4
depends_on: [39-04]
files_modified:
  - src/app/api/campaign/generate-image/route.ts
  - src/lib/copy/mapper.ts
  - src/lib/copy/__tests__/copy-director-service.test.ts
  - src/lib/image-generation/services/image-review-service.ts
  - src/lib/image-generation/services/image-generation-service.ts
  - src/lib/image-generation/services/__tests__/image-review-service.test.ts
autonomous: true
requirements: [F39-17, F39-18, F39-20]

must_haves:
  truths:
    - "mapBriefToCopyDirectorInput lê do domínio (brief.product/brief.commercial) e devolve o MESMO CopyDirectorInput de hoje (saída inalterada)"
    - "legalNotice NÃO entra no copy (nunca); CopyDirectorInput NÃO ganha campo de validade — validade entra no copy apenas via campos existentes do schema (se houver), senão fica só na arte/review (validityText)"
    - "ImageReviewInput usa o campo CANÔNICO `legalNoticeText?` (D9/OpenSpec image-quality-review:17) — o aviso legal entra no revisor SÓ quando legalNotice.enabled === true; o nome `mandatoryArtworkText` permanece APENAS no transporte flat/form e no mapper flat→brief (legalNotice)"
    - "ImageReviewInput ganha validityText opcional preenchido quando validity.enabled — compat com o revisor"
    - "Saídas copy e review são idênticas às atuais para os mesmos inputs (regressão por fixtures existentes)"
  artifacts:
    - path: "src/lib/copy/mapper.ts"
      provides: "mapBriefToCopyDirectorInput lendo brief.product/commercial (domínio); SEM campo de validade novo"
      contains: "brief.product"
    - path: "src/app/api/campaign/generate-image/route.ts"
      provides: "context = await buildCampaignBrief(...) renomeado + brief domínio construído na fronteira (buildCampaignBriefFromFlat) + call site do copy wired no mesmo plano"
      contains: "buildCampaignBriefFromFlat"
    - path: "src/lib/image-generation/services/image-review-service.ts"
      provides: "ImageReviewInput com campo canônico legalNoticeText? + validityText? (ambos gated por enabled) + JSDoc de origem domínio"
      contains: "legalNoticeText"
    - path: "src/lib/copy/__tests__/copy-director-service.test.ts"
      provides: "Fixtures migradas para brief estruturado + casos legalNotice ausente e validade gated SEM campo novo"
      contains: "validity"
  key_links:
    - from: "src/lib/copy/mapper.ts"
      to: "src/lib/campaign/brief.ts"
      via: "read-first do domínio: brief.product.name, brief.commercial.intent, brief.commercial.validity?.enabled ? displayText (campo existente) : undefined"
      pattern: "CampaignBrief"
    - from: "src/app/api/campaign/generate-image/route.ts"
      to: "src/lib/campaign/brief.ts"
      via: "const brief = buildCampaignBriefFromFlat(parsed.data, parsed.data.storeId) pós-identidade (~241) — wired NESTE plano"
      pattern: "buildCampaignBriefFromFlat"
    - from: "image-generation-service.ts (call site do review)"
      to: "src/lib/image-generation/services/image-review-service.ts"
      via: "assembly entrega legalNotice?.text quando enabled e validityText quando validity.enabled (co-requisito do 39-06)"
      pattern: "ImageReviewInput"
---

<objective>
Co-migrar as costuras de CONSUMO do domínio que alimentam copy e revisão visual, SEM quebra transitória de typecheck:
- `src/lib/copy/mapper.ts`: `mapBriefToCopyDirectorInput` passa a ler de `brief.product`/`brief.commercial` (domínio estruturado do plano 39-04); a saída `CopyDirectorInput` (schema em `src/lib/copy/schema.ts:3-21`) permanece INALTERADA; `legalNotice` NUNCA entra no copy; `validity` NÃO ganha campo novo no CopyDirectorInput — se o schema atual já expõe um campo de validade, propaga por ele quando `enabled`; senão validade fica fora do copy (F39-17/D11/D8/D9)
- Wiring da ROTA no MESMO plano: em `route.ts`, o resultado de `buildCampaignBrief` (241) passa a se chamar **`context`** (`const context = await buildCampaignBrief(...)` → `ResolvedCampaignContext` — NÃO mais `brief`), e o NOVO `const brief = buildCampaignBriefFromFlat(parsed.data, parsed.data.storeId)` (domínio estruturado) é introduzido na fronteira (~241); o call site do copy (`mapBriefToCopyDirectorInput(brief, input)` ~580) recebe o domínio `brief` — typecheck GLOBAL limpo ao fim deste plano. Dois objetos distintos desde esta wave: `context` (wrapper resolvido com loja/identidade) e `brief` (domínio estruturado da campanha).
- `src/lib/image-generation/services/image-review-service.ts`: interface `ImageReviewInput` RENOMEIA o campo `mandatoryArtworkText?` → **`legalNoticeText?`** (canônico OpenSpec image-quality-review:17; D9) e ADICIONA `validityText?: string` (F39-18); como o rename quebra callers, os 2 call sites da assembly (image-generation-service.ts 395 e ~617) trocam a chave para `legalNoticeText: body.mandatoryArtworkText` NESTE mesmo plano (valor ainda do transporte flat) — typecheck global limpo; a RELEITURA da assembly para o domínio gated (`brief.commercial.legalNotice`) fica com o 39-06; aqui ficam o TIPO, o JSDoc, o rename dos call sites e os testes de contrato 8.19/8.20.
- Co-migrar os testes dos 2 serviços: `copy-director-service.test.ts` e `image-review-service.test.ts` para brief estruturado.

Purpose: D11 — os consumidores downstream (copy formatado e revisor de visão) precisam ler o domínio sem depender do transporte flat; comportamento de saída preservado (golden); nenhum plan termina com typecheck global quebrado.
Output: mapper de copy lendo domínio com call site da rota wired no mesmo plano; ImageReviewInput com legalNoticeText canônico + validityText e contrato 8.19/8.20 testado; fixtures dos 2 serviços migradas.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@openspec/changes/fase-39-brief-estruturado-campanha/tasks.md (seção 6 — 6.4 e 6.5; seção 8 — 8.19 e 8.20; seção 9 — 9.2 parcial)
@openspec/changes/fase-39-brief-estruturado-campanha/specs/campaign-brief-pipeline-adapters/spec.md (mappers de consumo; CopyDirectorInput inalterado; ImageReviewInput montado do domínio)
@src/lib/copy/mapper.ts (mapBriefToCopyDirectorInput 91-115 — leituras flat atuais; buildCommercialFrame 5-12)
@src/lib/copy/schema.ts (3-21 — CopyDirectorInput: shape que NÃO muda; NÃO ganha campo de validade)
@src/lib/image-generation/services/image-review-service.ts (ImageReviewInput 9-21 — shape atual; mandatoryArtworkTextSection ~40+)
@src/lib/image-generation/services/image-generation-service.ts (assembly reviewInput 382-397 E validatePrompts ~604-620 — os 2 call sites trocam o campo para `legalNoticeText` NESTE plano, leitura mantida de `body.mandatoryArtworkText` até 39-06)
@src/app/api/campaign/generate-image/route.ts (call site do copy ~580 — wired NESTE plano; identidade 209-235; buildCampaignBrief 241 — renomeado para `context`; o `brief` de domínio é adicionado junto via buildCampaignBriefFromFlat)
@src/lib/copy/__tests__/copy-director-service.test.ts (fixtures em fluxo flat)
@src/lib/image-generation/services/__tests__/image-review-service.test.ts (literais ImageReviewInput 36-42, 89, 120, 138, 156-166, 172, 189-193, 208, 226, 243, 253)

<interfaces>
Contratos atuais (NÃO mudam — só a origem de leitura muda):

CopyDirectorInput (src/lib/copy/schema.ts:3-21) — saída do mapper, INALTERADA:
```typescript
export interface CopyDirectorInput {
  productName: string;
  description?: string;
  commercialFrame: string;
  campaignIntent: CampaignIntent;
  storeName?: string;
  segment?: string;
  toneOfVoice?: string;
  positioning?: string;
  shortDescription?: string;
  slogan?: string;
  brandPersonality?: string;
  campaignGuidelines?: string;
}
```

ImageReviewInput (src/lib/image-generation/services/image-review-service.ts:9-21) — RENOMEAR o campo `mandatoryArtworkText?: string` → **`legalNoticeText?: string`** (canônico OpenSpec image-quality-review:17; D9) e ADICIONAR `validityText?: string` (campo ADITIVO — nenhum caller quebra):
```typescript
export interface ImageReviewInput {
  productName: string;
  storeName: string;
  campaignIntent?: CampaignIntent;
  preserveImageContext?: boolean;
  badgeText?: string;
  originalPrice?: string;
  discountedPrice?: string;
  validationContext?: ValidationContext;
  legalNoticeText?: string;   // ← RENOMEADO de mandatoryArtworkText (canônico D9) — preenchido pela assembly do 39-06 quando commercial.legalNotice.enabled === true
  campaignDetails?: string;
  additionalDetails?: string;
  validityText?: string;     // ← NOVO (opcional, ADITIVO) — preenchido quando commercial.validity.enabled (origem domínio; valor montado na assembly do 39-06)
}
```
> Rename exige co-migração no MESMO plano (regra typecheck global limpo): o field name muda nos 2 call sites da assembly (image-generation-service.ts 395 e ~617 — ambos viram `legalNoticeText: body.mandatoryArtworkText`, leitura ainda do transporte flat até o 39-06) e nos literais dos testes de review. O nome `mandatoryArtworkText` permanece APENAS no transporte flat/form (GenerateImageRequestSchema 21) e no mapper flat→brief (legalNotice).

Re-roteamento no mapper (91-115) — leituras flat → domínio:
| Atual (flat) | Novo (domínio) |
|---|---|
| `brief.campaignInput.productName` | `brief.product.name` |
| `brief.campaignInput.description` | `brief.product.description` |
| `brief.campaignInput.campaignIntent` | `brief.commercial.intent` |
| `brief.campaignInput.badgeText` | `brief.commercial.badgeText` |
| `brief.campaignInput.originalPriceCents`/`discountedPriceCents` | `brief.commercial.originalPriceCents`/`discountedPriceCents` |
| — legalNotice NÃO entra | (ausente do CopyDirectorInput — copy nunca carrega aviso) |
| `validity` | CopyDirectorInput NÃO ganha campo de validade nesta fase (D8/F39-17). Se o schema atual (schema.ts:3-21) já expõe texto de validade via campo existente (ex.: `campaignGuidelines`), propagar gated por `brief.commercial.validity?.enabled`; caso contrário, validade NÃO entra no copy — fica na arte/review via `validityText` do ImageReviewInput. SEM heuristic de string. |

Contrato de testes (8.19/8.20):
- 8.19: `mapBriefToCopyDirectorInput(brief, input)` com brief estruturado produz `CopyDirectorInput` com `productName: brief.product.name`, `campaignIntent: brief.commercial.intent` — equivalência com o fluxo flat
- 8.20: `ImageReviewInput` montado do domínio — revisor recebe `legalNotice.text` quando `enabled=true` (via `legalNoticeText`), NÃO quando `false`; `validityText` quando validity habilitada
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: mapBriefToCopyDirectorInput lê do domínio + wiring da rota + co-migração copy-director-service.test.ts</name>
  <files>src/lib/copy/mapper.ts, src/lib/copy/__tests__/copy-director-service.test.ts, src/app/api/campaign/generate-image/route.ts</files>
  <read_first>
    - src/lib/copy/mapper.ts (91-115 — função atual; 5-12 buildCommercialFrame)
    - src/lib/copy/schema.ts (3-21 — CopyDirectorInput; NÃO ganha campo de validade)
    - src/lib/campaign/brief.ts (recém-criado — CampaignBrief domínio + buildCampaignBriefFromFlat do 39-04)
    - src/app/api/campaign/generate-image/route.ts (241 buildCampaignBrief/context; 307 storeId destructure; ~580 call site mapBriefToCopyDirectorInput)
    - src/lib/copy/__tests__/copy-director-service.test.ts (fixtures flat atuais)
  </read_first>
  <behavior>
    - Test 1: brief estruturado (product.name "Produto Teste", commercial.intent "offer", preços) → CopyDirectorInput com productName "Produto Teste", campaignIntent "offer", discountedPriceCents do domínio — saída idêntica à do fluxo flat equivalente
    - Test 2: brief sem description/badge → campos ausentes na saída (mesmo comportamento de hoje)
    - Test 3: commercial.validity = { enabled: true, displayText: "Até 30/09" } → validade propagada SOMENTE via campo existente do CopyDirectorInput se o schema atual o expuser (gated por enabled); CopyDirectorInput NÃO ganha campo novo; legalNotice NUNCA presente em CopyDirectorInput
  </behavior>
  <action>
    Em `src/lib/copy/mapper.ts` e `src/app/api/campaign/generate-image/route.ts`:
    1. Trocar a assinatura: `mapBriefToCopyDirectorInput(brief: CampaignBrief, input: {...}): CopyDirectorInput` — agora o parâmetro é o DOMÍNIO `CampaignBrief` (de `@/lib/campaign/brief`), NÃO o wrapper `ResolvedCampaignContext`.
    2. WIRING DA ROTA NO MESMO PLANO (nada de "deixar quebrado para 39-07") — troca local EXPLÍCITA (~241), para os 2 objetos coexistirem com nomes distintos:
       a. RENOMEAR o objeto atual `const brief = await buildCampaignBrief(...)` → **`const context = await buildCampaignBrief(...)`** (wrapper resolvido, `ResolvedCampaignContext` — usado pelos call sites de identidade/contexto);
       b. ADICIONAR o domínio estruturado: `const brief = buildCampaignBriefFromFlat(parsed.data, parsed.data.storeId);` (mapper do 39-04 — dependência já declarada; `CampaignBrief` de `@/lib/campaign/brief`);
       c. atualizar o call site do copy (~580) para `mapBriefToCopyDirectorInput(brief, input)` — recebe o DOMÍNIO `brief`, NUNCA o `context`. Assim o typecheck GLOBAL sai limpo ao fim deste plano e o desenho final (context + brief) é idêntico ao que o 39-07 impõe.
    3. Re-rotear as leituras conforme a tabela <interfaces>: `brief.campaignInput.productName` → `brief.product.name`, `description` → `brief.product.description`, `campaignIntent` → `brief.commercial.intent`, preços/badge → `brief.commercial.*`. Manter `buildCommercialFrame` (5-12) INALTERADO — ele continua recebendo badgeText/preços.
    4. `validity`: CopyDirectorInput NÃO ganha campo de validade nesta fase (D8/F39-17 — comportamento de copy preservado). Se o schema atual do copy (src/lib/copy/schema.ts:3-21) já suporta validade via campos existentes, propague por esses campos gated por `brief.commercial.validity?.enabled`; caso contrário validade NÃO entra no copy (fica na arte/review apenas, via `validityText` no ImageReviewInput). SEM heuristic de string (D8).
    5. `legalNotice` NÃO entra na saída (D9/F39-17): nenhuma leitura de `brief.commercial.legalNotice` no mapper.
    6. Co-migrar `copy-director-service.test.ts`: fixtures passam a construir brief estruturado (via `buildCampaignBriefFromFlat` de `@/lib/campaign/brief` com payload flat CI — ou literal estruturado com override-spread-last); assertar testes 1-3 acima (8.19). Regressão: mesmos outputs do fluxo flat para inputs equivalentes.
  </action>
  <verify>
    <automated>npx vitest run src/lib/copy/__tests__/copy-director-service.test.ts; if ($?) { npm run typecheck }</automated>
  </verify>
  <acceptance_criteria>
    - `mapper.ts` NÃO lê mais `brief.campaignInput` (grep por `campaignInput` no mapper → 0; lê `brief.product`/`brief.commercial`)
    - Nenhuma leitura de legalNotice no mapper (grep por `legalNotice` → ausente)
    - `route.ts` contém a troca local explícita: `const context = await buildCampaignBrief(...)` (~241) + `const brief = buildCampaignBriefFromFlat(parsed.data, parsed.data.storeId)` + call site `mapBriefToCopyDirectorInput(brief, ...)` (grep); nenhum call site do copy recebe `context`
    - Fixtures do copy-director-service.test.ts usam brief estruturado; testes 1-3 verdes (validade por enabled SEM campo novo no CopyDirectorInput)
    - `CopyDirectorInput` (src/lib/copy/schema.ts) SEM campo novo de validade (grep por `validity` no schema → 0)
    - Saída CopyDirectorInput idêntica (regressão por fixtures existentes verdes)
    - `npm run typecheck` (GLOBAL) exit 0 ao fim deste plano — NENHUMA quebra transitória aceita; `npm run lint` limpo
  </acceptance_criteria>
  <done>Mapper de copy lê do domínio com saída inalterada; rota wired (brief na fronteira + call site); legalNotice fora; validade sem campo novo; testes migrados</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ImageReviewInput com campo canônico legalNoticeText + validityText + call sites co-migrados + 8.20 gated</name>
  <files>src/lib/image-generation/services/image-review-service.ts, src/lib/image-generation/services/image-generation-service.ts, src/lib/image-generation/services/__tests__/image-review-service.test.ts</files>
  <read_first>
    - src/lib/image-generation/services/image-review-service.ts (9-21 — interface; mandatoryArtworkTextSection ~40+ lê input.mandatoryArtworkText)
    - src/lib/image-generation/services/image-generation-service.ts (382-397 — assembly reviewInput: linha 395 `mandatoryArtworkText: body.mandatoryArtworkText`; ~604-620 — reviewer input no validatePrompts: linha 617 idem — os 2 RENOMEIAM o field para legalNoticeText NESTE plano, leitura mantida de `body.mandatoryArtworkText` até 39-06)
    - src/lib/image-generation/services/__tests__/image-review-service.test.ts (literais 36-42, 89, 120, 138, 156-166, 172, 189-193, 208, 226, 243, 253 usam `mandatoryArtworkText`)
    - src/lib/campaign/brief.ts (CampaignBrief + CampaignOfferValidity)
  </read_first>
  <behavior>
    - Test 1: ImageReviewInput com `legalNoticeText` ausente/undefined (legalNotice disabled) → prompt/review NÃO contém seção de aviso (mandatoryArtworkTextSection vazia)
    - Test 2: `legalNoticeText: "Imagem ilustrativa"` (legalNotice enabled) → seção de aviso presente no review
    - Test 3: `validityText: "Até 30/09"` presente → validade entra no review quando habilitada
  </behavior>
  <action>
    Em `src/lib/image-generation/services/image-review-service.ts`, `image-generation-service.ts` e `__tests__/image-review-service.test.ts`:
    1. RENOMEAR o campo `mandatoryArtworkText?: string` → `legalNoticeText?: string` na interface `ImageReviewInput` (canônico OpenSpec image-quality-review:17; D9) e ADICIONAR `validityText?: string` (ADITIVO). Adicionar JSDoc na interface: "Campos originados do domínio CampaignBrief: legalNoticeText ← commercial.legalNotice.text quando enabled === true; validityText ← commercial.validity.displayText quando validity.enabled (D8/D9)".
    2. CO-MIGRAR o builder de prompt/review (`mandatoryArtworkTextSection ~40+`): trocar a leitura de `input.mandatoryArtworkText` para `input.legalNoticeText` (devir do section builder); se precisar consumir validityText (nova seção de validade) adicioná-la de forma que com valores ausentes o comportamento seja IDÊNTICO ao atual (VARS ausentes → sem seção extra).
    3. CO-MIGRAR os 2 call sites da assembly em image-generation-service.ts (linhas 395 e ~617): trocar a chave para `legalNoticeText: body.mandatoryArtworkText` — o VALOR continua vindo do transporte flat `body` (a releitura para o domínio `brief.commercial.legalNotice` é migração do 39-06); assim o typecheck GLOBAL permanece limpo ao fim deste plano.
    4. Co-migrar `image-review-service.test.ts`: renomear os literais existentes de `mandatoryArtworkText` para `legalNoticeText` (shape novo da interface) e ADICIONAR os casos 8.20 (tests 1-3 acima): legalNotice.enabled=false → sem texto de aviso; enabled=true → texto presente; validityText propagado. (Fixture base: nova `baseReviewInput` com overrides spread-last.)
    NOTA: a RELEITURA da assembly para o domínio (legalNotice/validity gated por enabled) é migrada no plano 39-06 — aqui só o rename do field + wiring semântico mínimo.
  </action>
  <verify>
    <automated>npx vitest run src/lib/image-generation/services/__tests__/image-review-service.test.ts; if ($?) { npm run typecheck }</automated>
  </verify>
  <acceptance_criteria>
    - Interface ImageReviewInput tem `legalNoticeText?` e `validityText?` e NÃO tem mais `mandatoryArtworkText` (grep `mandatoryArtworkText` em image-review-service.ts → 0)
    - `image-generation-service.ts` editado APENAS no rename das 2 chaves (395 e ~617 → `legalNoticeText: body.mandatoryArtworkText`); leitura continua flat até 39-06 (grep confirma `body.mandatoryArtworkText` ainda presente como VALOR)
    - Testes verdes: aviso gated (ausente → vazio; presente → texto), validityText propagado, literais renomeados
    - `npm run typecheck` (GLOBAL) exit 0 ao fim deste plano — NENHUMA quebra transitória aceita; `npm run lint` limpo
  </acceptance_criteria>
  <done>ImageReviewInput com legalNoticeText (canônico) + validityText; 2 call sites da assembly renomeados sem quebra; contrato 8.20 gated testado</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| domínio → copy mapper | CopyDirectorInput é texto destinado a publicação (copy de redes sociais) |
| domínio → revisor de visão | ImageReviewInput alimenta IA de revisão de imagem |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-05 | Tampering | Aviso ilustrativo vazando para o copy | mitigate | legalNotice NUNCA lido no mapper (grep gate `legalNotice` ausente do mapper.ts); teste 8.19 garante CopyDirectorInput sem aviso |
| T-39-05a | Spoofing | Av embaralhado no revisor (enabled=false com texto) | mitigate | Re-roteamento gated: legalNotice.text só quando `enabled === true`; teste 8.20 cobre enabled=false → vazio (D9) |
| T-39-05b | Tampering | Heurística de string de validade / campo novo no CopyDirectorInput | mitigate | validade propagada por `enabled/displayText` (sem heuristics de string — D8) e SEM campo novo no CopyDirectorInput (grep `validity` em schema.ts → 0); comportamento atual preservado por fixtures |
| T-39-05c | Integrity | Saída divergente (copy/review diferentes do atual) | mitigate | Contrato "saída inalterada" (CopyDirectorInput schema.ts:3-21; ImageReviewInput com legalNoticeText canônico + validityText opcional); regressão por fixtures existentes migradas |
| T-39-SC | Tampering | Instalação de pacotes | accept | Nenhum pacote novo — sem gate de legitimidade |
</threat_model>

<verification>
- `npx vitest run src/lib/copy/__tests__/copy-director-service.test.ts src/lib/image-generation/services/__tests__/image-review-service.test.ts` — verdes
- `npm run typecheck` (GLOBAL) exit 0 ao fim deste plano — NENHUMA quebra transitória aceita (rota wired no Task 1; rename do field co-migrado nos 2 call sites no Task 2)
- `npm run lint` limpo
- Grep: `campaignInput` em src/lib/copy/mapper.ts → 0; `legalNotice` em mapper.ts → 0; `validity` em src/lib/copy/schema.ts → 0 (CopyDirectorInput sem campo novo); `mandatoryArtworkText` em image-review-service.ts → 0 (campo canônico é legalNoticeText)
- `route.ts`: `const context = await buildCampaignBrief(` + `buildCampaignBriefFromFlat` + `mapBriefToCopyDirectorInput(brief,` presentes (troca local explícita)
- `image-generation-service.ts`: apenas o rename das 2 chaves da assembly (395 e ~617 → `legalNoticeText: body.mandatoryArtworkText`); releitura para o domínio continua no 39-06
</verification>

<success_criteria>
- Copy lê do domínio (product/commercial) com saída idêntica; legalNotice fora; validade sem campo novo no CopyDirectorInput (via campo existente ou só arte/review)
- Rota wired no mesmo plano (context renomeado + brief na fronteira + call site do copy) — typecheck global limpo ao fim do plano
- ImageReviewInput com legalNoticeText (canônico D9/OpenSpec) + validityText (ADITIVO) + contrato 8.20 gated testado; 2 call sites da assembly renomeados; fixtures dos serviços co-migradas
- Sem mudança de comportamento de copy/review (regressão verde)
</success_criteria>

<output>
Create `.planning/phases/39-brief-estruturado-campanha/39-05-SUMMARY.md` when done
</output>
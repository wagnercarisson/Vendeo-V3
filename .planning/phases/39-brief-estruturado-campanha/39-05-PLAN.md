---
phase: 39-brief-estruturado-campanha
plan: 05
type: execute
wave: 4
depends_on: [39-04]
files_modified:
  - src/lib/copy/mapper.ts
  - src/lib/copy/__tests__/copy-director-service.test.ts
  - src/lib/image-generation/services/image-review-service.ts
  - src/lib/image-generation/services/__tests__/image-review-service.test.ts
autonomous: true
requirements: [F39-17, F39-18, F39-20]

must_haves:
  truths:
    - "mapBriefToCopyDirectorInput lê do domínio (brief.product/brief.commercial) e devolve o MESMO CopyDirectorInput de hoje (saída inalterada)"
    - "legalNotice NÃO entra no copy (nunca); validity.displayText propaga no copy quando enabled"
    - "ImageReviewInput permanece com o mesmo shape (9-21) e os call sites passam legalNotice.text SÓ quando enabled === true"
    - "ImageReviewInput ganha validityText opcional preenchido quando validity.enabled — compat com o revisor"
    - "Saídas copy e review são idênticas às atuais para os mesmos inputs (regressão por fixtures existentes)"
  artifacts:
    - path: "src/lib/copy/mapper.ts"
      provides: "mapBriefToCopyDirectorInput lendo brief.product/commercial (domínio)"
      contains: "brief.product"
    - path: "src/lib/image-generation/services/image-review-service.ts"
      provides: "ImageReviewInput com validityText? + JSDoc de origem domínio"
      contains: "validityText"
    - path: "src/lib/copy/__tests__/copy-director-service.test.ts"
      provides: "Fixtures migradas para brief estruturado + casos legalNotice ausente e validity propagada"
      contains: "validity"
  key_links:
    - from: "src/lib/copy/mapper.ts"
      to: "src/lib/campaign/brief.ts"
      via: "read-first do domínio: brief.product.name, brief.commercial.intent, brief.commercial.validity?.displayText"
      pattern: "CampaignBrief"
    - from: "image-generation-service.ts (call site do review)"
      to: "src/lib/image-generation/services/image-review-service.ts"
      via: "assembly entrega legalNotice?.text quando enabled e validityText quando validity.enabled (co-requisito do 39-06)"
      pattern: "ImageReviewInput"
---

<objective>
Co-migrar as costuras de CONSUMO do domínio que alimentam copy e revisão visual:
- `src/lib/copy/mapper.ts`: `mapBriefToCopyDirectorInput` passa a ler de `brief.product`/`brief.commercial` (domínio estruturado do plano 39-04); a saída `CopyDirectorInput` (schema em `src/lib/copy/schema.ts:3-21`) permanece INALTERADA; `legalNotice` NUNCA entra no copy; `validity.displayText` propaga quando `enabled` (F39-17/D11/D8/D9)
- `src/lib/image-generation/services/image-review-service.ts`: interface `ImageReviewInput` mantém o shape (9-21) e ganha `validityText?: string` + JSDoc de origem (F39-18); os call sites (image-generation-service.ts:382-397) passam `legalNotice.text` SÓ quando `enabled === true` e `validity.displayText` quando habilitada — a ASSEMBLY em si é co-migrada no plano 39-06 (que reescreve as leituras do service completo); aqui ficam o TIPO, o JSDoc e os testes de contrato 8.19/8.20 do service de review.
- Co-migrar os testes dos 2 serviços: `copy-director-service.test.ts` e `image-review-service.test.ts` para brief estruturado.

Purpose: D11 — os consumidores downstream (copy formatado e revisor de visão) precisam ler o domínio sem depender do transporte flat; comportamento de saída preservado (golden).
Output: mapper de copy lendo domínio; ImageReviewInput com validityText e contrato 8.19/8.20 testado; fixtures dos 2 serviços migradas.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@openspec/changes/fase-39-brief-estruturado-campanha/tasks.md (seção 6 — 6.4 e 6.5; seção 8 — 8.19 e 8.20; seção 9 — 9.2 parcial)
@openspec/changes/fase-39-brief-estruturado-campanha/specs/campaign-brief-pipeline-adapters/spec.md (mappers de consumo; CopyDirectorInput inalterado; ImageReviewInput montado do domínio)
@src/lib/copy/mapper.ts (mapBriefToCopyDirectorInput 91-115 — leituras flat atuais; buildCommercialFrame 5-12)
@src/lib/copy/schema.ts (3-21 — CopyDirectorInput: shape que NÃO muda)
@src/lib/image-generation/services/image-review-service.ts (ImageReviewInput 9-21 — shape atual; mandatoryArtworkTextSection ~40+)
@src/lib/image-generation/services/image-generation-service.ts (assembly reviewInput 382-397 — call site que o 39-06 migrará; NÃO editar a assembly aqui)
@src/lib/copy/__tests__/copy-director-service.test.ts (fixtures em fluxo flat)
@src/lib/image-generation/services/__tests__/image-review-service.test.ts (literais ImageReviewInput 36-42, 89, 120, 138, 156-166, 172, 189-193, 208, 226, 243, 253)

<interfaces>
Contratos atuais (NÃO mudam — só a origem de leitura muda):

CopyDirectorInput (src/lib/copy/schema.ts:3-21) — saída do mapper, INALTERADA:
```typescript
export interface CopyDirectorInput {
  productName: string;
  description?: string;
  commercialFrame: string;
  campaignIntent: CampaignIntent;
  storeName?: string;
  segment?: string;
  toneOfVoice?: string;
  positioning?: string;
  shortDescription?: string;
  slogan?: string;
  brandPersonality?: string;
  campaignGuidelines?: string;
}
```

ImageReviewInput (src/lib/image-generation/services/image-review-service.ts:9-21) — shape atual SEM mudança de campos obrigatórios; ADICIONAR `validityText?: string`:
```typescript
export interface ImageReviewInput {
  productName: string;
  storeName: string;
  campaignIntent?: CampaignIntent;
  preserveImageContext?: boolean;
  badgeText?: string;
  originalPrice?: string;
  discountedPrice?: string;
  validationContext?: ValidationContext;
  mandatoryArtworkText?: string;
  campaignDetails?: string;
  additionalDetails?: string;
  validityText?: string;     // ← NOVO (opcional) — preenchido quando commercial.validity.enabled (origem domínio)
}
```

Re-roteamento no mapper (91-115) — leituras flat → domínio:
| Atual (flat) | Novo (domínio) |
|---|---|
| `brief.campaignInput.productName` | `brief.product.name` |
| `brief.campaignInput.description` | `brief.product.description` |
| `brief.campaignInput.campaignIntent` | `brief.commercial.intent` |
| `brief.campaignInput.badgeText` | `brief.commercial.badgeText` |
| `brief.campaignInput.originalPriceCents`/`discountedPriceCents` | `brief.commercial.originalPriceCents`/`discountedPriceCents` |
| — legalNotice NÃO entra | (ausente do CopyDirectorInput — copy nunca carrega aviso) |
| `validity` string | `brief.commercial.validity?.enabled ? brief.commercial.validity?.displayText : undefined` (se o CopyDirectorInput tiver campo validity — verificar schema; SEM heuristic string) |

Contrato de testes (8.19/8.20):
- 8.19: `mapBriefToCopyDirectorInput(brief, input)` com brief estruturado produz `CopyDirectorInput` com `productName: brief.product.name`, `campaignIntent: brief.commercial.intent` — equivalência com o fluxo flat
- 8.20: `ImageReviewInput` montado do domínio — revisor recebe `legalNotice.text` quando `enabled=true` (via mandatoryArtworkText), NÃO quando `false`; `validityText` quando validity habilitada
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: mapBriefToCopyDirectorInput lê do domínio + co-migração copy-director-service.test.ts</name>
  <files>src/lib/copy/mapper.ts, src/lib/copy/__tests__/copy-director-service.test.ts</files>
  <read_first>
    - src/lib/copy/mapper.ts (91-115 — função atual; 5-12 buildCommercialFrame)
    - src/lib/copy/schema.ts (3-21 — CopyDirectorInput)
    - src/lib/campaign/brief.ts (recém-criado — CampaignBrief domínio, 39-02+39-04)
    - src/lib/copy/__tests__/copy-director-service.test.ts (fixtures flat atuais)
  </read_first>
  <behavior>
    - Test 1: brief estruturado (product.name "Produto Teste", commercial.intent "offer", preços) → CopyDirectorInput com productName "Produto Teste", campaignIntent "offer", discountedPriceCents do domínio — saída idêntica à do fluxo flat equivalente
    - Test 2: brief sem description/badge → campos ausentes na saída (mesmo comportamento de hoje)
    - Test 3: commercial.validity = { enabled: true, displayText: "Até 30/09" } → validity propagada; legalNotice NUNCA presente em CopyDirectorInput
  </behavior>
  <action>
    Em `src/lib/copy/mapper.ts`:
    1. Trocar a assinatura: `mapBriefToCopyDirectorInput(brief: CampaignBrief, input: {...}): CopyDirectorInput` — agora o parâmetro é o DOMÍNIO `CampaignBrief` (de `@/lib/campaign/brief`), NÃO o wrapper `ResolvedCampaignContext`. ATENÇÃO: o call site na rota (route.ts:~580) passa o brief — o plano 39-07 fará o wiring da rota (até lá, o serviço de copy recebe o que a rota montar; o typecheck da rota será ajustado em 39-07 — se quebrar neste plano, documentar no resumo e deixar o wiring para 39-07).
    2. Re-rotear as leituras conforme a tabela <interfaces>: `brief.campaignInput.productName` → `brief.product.name`, `description` → `brief.product.description`, `campaignIntent` → `brief.commercial.intent`, preços/badge → `brief.commercial.*`. Manter `buildCommercialFrame` (5-12) INALTERADO — ele continua recebendo badgeText/preços.
    3. `validity`: se `CopyDirectorInput` tiver campo de validade (verificar schema), propagar `brief.commercial.validity?.enabled ? brief.commercial.validity?.displayText : undefined`; SEM heuristic de string (D8).
    4. `legalNotice` NÃO entra na saída (D9/F39-17): nenhuma leitura de `brief.commercial.legalNotice` no mapper.
    5. Co-migrar `copy-director-service.test.ts`: fixtures passam a construir brief estruturado (via `buildCampaignBriefFromFlat` de `@/lib/campaign/brief` com payload flat CI — ou literal estruturado com override-spread-last); assertar testes 1-3 acima (8.19). Regressão: mesmos outputs do fluxo flat para inputs equivalentes.
  </action>
  <verify>
    <automated>npx vitest run src/lib/copy/__tests__/copy-director-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `mapper.ts` NÃO lê mais `brief.campaignInput` (grep por `campaignInput` no mapper → 0; lê `brief.product`/`brief.commercial`)
    - Nenhuma leitura de legalNotice no mapper (grep por `legalNotice` → ausente)
    - Fixtures do copy-director-service.test.ts usam brief estruturado; testes 1-3 verdes (incluindo validity propagada quando enabled)
    - Saída CopyDirectorInput idêntica (regressão por fixtures existentes verdes). Typecheck ESCOPADO ao plano: `npm run typecheck` pode reportar erro transitório em `route.ts:580` (call site de `mapBriefToCopyDirectorInput` ainda passa o wrapper `ResolvedCampaignContext` — o wiring da rota acontece no 39-07). QUEBRA TRANSITÓRIA CONHECIDA E ACEITA: validar via `npx vitest run` dos arquivos deste plano + typecheck dos arquivos tocados; o typecheck GLOBAL limpo é gate somente do 39-08.
  </acceptance_criteria>
  <done>Mapper de copy lê do domínio com saída inalterada; legalNotice fora; validity propagada quando enabled; testes migrados</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ImageReviewInput com validityText + contrato 8.20 gated + co-migração dos testes de review</name>
  <files>src/lib/image-generation/services/image-review-service.ts, src/lib/image-generation/services/__tests__/image-review-service.test.ts</files>
  <read_first>
    - src/lib/image-generation/services/image-review-service.ts (9-21 — interface; mandatoryArtworkTextSection ~40+)
    - src/lib/image-generation/services/image-generation-service.ts (382-397 — assembly reviewInput atual: mandatoryArtworkText de body; NÃO editar aqui — 39-06 migra as leituras)
    - src/lib/image-generation/services/__tests__/image-review-service.test.ts (literais 36-42 e demais; fixtures flat)
    - src/lib/campaign/brief.ts (CampaignBrief + CampaignOfferValidity)
  </read_first>
  <behavior>
    - Test 1: ImageReviewInput com `mandatoryArtworkText: ""` (legalNotice disabled) → prompt/review NÃO contém seção de aviso (mandatoryArtworkTextSection vazia)
    - Test 2: `mandatoryArtworkText: "Imagem ilustrativa"` (legalNotice enabled) → seção de aviso presente no review
    - Test 3: `validityText: "Até 30/09"` presente → validade entra no review quando habilitada
  </behavior>
  <action>
    Em `src/lib/image-generation/services/image-review-service.ts`:
    1. Adicionar `validityText?: string` à interface `ImageReviewInput` (campo opcional — NÃO quebra callers existentes; shape continua compatível, F39-18). Manter `mandatoryArtworkText?: string` como está (origem futura: `commercial.legalNotice.text` gated).
    2. Adicionar JSDoc na interface: "Campos originados do domínio CampaignBrief: mandatoryArtworkText ← commercial.legalNotice.text quando enabled === true; validityText ← commercial.validity.displayText quando validity.enabled (D8/D9)".
    3. Se o builder de prompt/review (`mandatoryArtworkTextSection ~40+`) precisar de ajuste para consumir validityText (nova seção de validade), adicioná-la de forma que com valores ausentes o comportamento seja IDÊNTICO ao atual (VARS ausentes → sem seção extra).
    4. Co-migrar `image-review-service.test.ts`: mantendo os literais existentes (shape inalterado), ADICIONAR os casos 8.20 (tests 1-3 acima): legalNotice.enabled=false → sem texto de aviso; enabled=true → texto presente; validityText propagado. (Fixture base: nova `baseReviewInput` com overrides spread-last.)
    NOTA: a ASSEMBLY do reviewInput em image-generation-service.ts (382-397) é migrada no plano 39-06 (junto das outras costuras do service) — NÃO editar image-generation-service.ts neste plano.
  </action>
  <verify>
    <automated>npx vitest run src/lib/image-generation/services/__tests__/image-review-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `validityText?` presente na interface ImageReviewInput (grep)
    - Testes verdes: aviso gated (enabled=false → vazio; true → texto), validityText propagado, regressão dos literais existentes
    - `image-generation-service.ts` NÃO foi editado neste plano (grep por diff ou estado git — deve permanecer com assembly flat até 39-06)
    - Typecheck ESCOPADO ao plano (arquivos tocados); quebra transitória em `route.ts:580` é AGUARDADA até o wiring do 39-07 — typecheck global limpo apenas no 39-08; `npm run lint` limpo
  </acceptance_criteria>
  <done>ImageReviewInput com validityText + contrato 8.20 gated testado; testes de review co-migrados sem quebra</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| domínio → copy mapper | CopyDirectorInput é texto destinado a publicação (copy de redes sociais) |
| domínio → revisor de visão | ImageReviewInput alimenta IA de revisão de imagem |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-05 | Tampering | Aviso ilustrativo vazando para o copy | mitigate | legalNotice NUNCA lido no mapper (grep gate `legalNotice` ausente do mapper.ts); teste 8.19 garante CopyDirectorInput sem aviso |
| T-39-05a | Spoofing | Av embaralhado no revisor (enabled=false com texto) | mitigate | Re-roteamento gated: legalNotice.text só quando `enabled === true`; teste 8.20 cobre enabled=false → vazio (D9) |
| T-39-05b | Tampering | Heurística de string de validade | mitigate | validity propagada por `enabled/displayText` (sem `/ at� até válida` heuristics — D8); comportamento atual preservado por fixtures |
| T-39-05c | Integrity | Saída divergente (copy/review diferentes do atual) | mitigate | Contrato "saída inalterada" (CopyDirectorInput schema.ts:3-21; ImageReviewInput shape 9-21 + validityText opcional); regressão por fixtures existentes migradas |
| T-39-SC | Tampering | Instalação de pacotes | accept | Nenhum pacote novo — sem gate de legitimidade |
</threat_model>

<verification>
- `npx vitest run src/lib/copy/__tests__/copy-director-service.test.ts src/lib/image-generation/services/__tests__/image-review-service.test.ts` — verdes
- Typecheck ESCOPADO aos arquivos tocados neste plano (mapper.ts, image-review-service.ts + respectivos testes via vitest); quebra transitória conhecida em `route.ts:580` (call site ainda passa wrapper — wiring no 39-07) é ACEITA e não bloqueia este plano
- Typecheck GLOBAL (`npm run typecheck`) e demais gates completos ficam para o 39-08; `npm run lint` limpo
- Grep: `campaignInput` em src/lib/copy/mapper.ts → 0; `legalNotice` em mapper.ts → 0
- `image-generation-service.ts` sem alterações neste plano (wiring da assembly no 39-06)
</verification>

<success_criteria>
- Copy lê do domínio (product/commercial) com saída idêntica; legalNotice fora; validity por enabled/displayText
- ImageReviewInput com validityText + contrato gated testado; fixtures dos 2 serviços co-migradas
- Sem mudança de comportamento de copy/review (regressão verde)
</success_criteria>

<output>
Create `.planning/phases/39-brief-estruturado-campanha/39-05-SUMMARY.md` when done
</output>
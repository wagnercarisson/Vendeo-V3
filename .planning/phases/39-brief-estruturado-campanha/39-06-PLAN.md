---
phase: 39-brief-estruturado-campanha
plan: 06
type: execute
wave: 5
depends_on: [39-05]
files_modified:
  - src/lib/image-generation/services/image-generation-service.ts
  - src/lib/image-generation/services/__tests__/image-generation-service.test.ts
autonomous: true
requirements: [F39-15, F39-16, F39-19, F39-20]

must_haves:
  truths:
    - "buildPromptVariables produz o MESMO conjunto de variáveis hoje (38 keys) lendo do domínio (brief.product/commercial/media/creativeContext) — golden tests por intent"
    - "buildCommercialRepertoire decide validade por validity.enabled/displayText — SEM heurística de string (/ , até, válida)"
    - "A ponte para provider de imagem e InputValidationService recebe media.primary.dataUrl (base64 apenas em memória/transporte — nunca snapshot)"
    - "A assembly do ImageReviewInput lê do domínio: legalNotice.text só quando enabled=true; validityText quando habilitada"
    - "Regressão: prompts finais oferecidos por intent (offer/spotlight/exclusive) são idênticos aos atuais para o mesmo input — golden tests 9.4"
  artifacts:
    - path: "src/lib/image-generation/services/image-generation-service.ts"
      provides: "Leituras do domínio em buildPromptVariables/buildCommercialRepertoire/reviewInput assembly/ponte dataUrl"
      contains: "brief.product"
    - path: "src/lib/image-generation/services/__tests__/image-generation-service.test.ts"
      provides: "createMinimalBrief estruturado + golden tests por intent + casos 8.16-8.18, 9.4"
      contains: "createMinimalBrief"
  key_links:
    - from: "buildPromptVariables"
      to: "brief.product/brief.commercial/brief.media/brief.creativeContext"
      via: "substitui reads de body.* (alias eliminado na linha 95/585); validity gated por enabled"
      pattern: "body"
    - from: "media.primary.dataUrl"
      to: "provider de imagem + InputValidationService"
      via: "images.find(i => i.role === 'primary')?.dataUrl — base64 só em memória"
      pattern: "dataUrl"
---

<objective>
Estender as costuras do serviço de geração para ler o DOMÍNIO Estruturado: `buildPromptVariables` (41+ vars, 38 keys atuais) e `buildCommercialRepertoire` passam a ler `brief.product`/`brief.commercial`/`brief.media`/`brief.creativeContext` — eliminando o alias `body = brief.campaignInput as GenerateImageRequest` (imagem-g 95/585); a ponte `media.primary.dataUrl` → provider de imagem E InputValidationService/revisor (F39-16); a assembly do `ImageReviewInput` (382-397) é co-migrada lendo do domínio (legalNotice gated por `enabled`, validityText quando habilitada). Tudo com GOLDEN TESTS por intent (offer/spotlight/exclusive) garantindo conjunto de variáveis idêntico (F39-15/F39-19).

Purpose: D11 — o serviço é a costura central entre o domínio e os providers de IA; comportamento de geração PRESERVADO (golden), sem heuristic de validade string (D8).
Output: serviço 100% domínio (sem body alias), golden tests por intent, base64 só em memória.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@openspec/changes/fase-39-brief-estruturado-campanha/tasks.md (seção 6 — 6.1, 6.2, 6.3; seção 8 — 8.16, 8.17, 8.18; seção 9 — 9.2, 9.4)
@openspec/changes/fase-39-brief-estruturado-campanha/specs/campaign-brief-pipeline-adapters/spec.md (costuras ①-③; golden tests per intent; variáveis idênticas; provider recebe media.primary.dataUrl)
@src/lib/image-generation/services/image-generation-service.ts (body alias 95/585; buildPromptVariables 843-938; buildCommercialRepertoire 704-759; reviewInput assembly 382-397; hadProductImage 427; validatePrompts 584)
@src/lib/image-generation/services/image-review-service.ts (ImageReviewInput 9-21, validityText adicionado no 39-05)
@src/lib/campaign/brief.ts (domínio — mapper/builder do 39-04)
@src/lib/image-generation/services/__tests__/image-generation-service.test.ts (createMinimalBrief 13-39 — flat, a migrar para estruturado)
@src/lib/image-generation/schema.ts (GenerateImageRequest — para fixtures golden: payload flat de referência)

<interfaces>
Estrutura do serviço e pontos exatos:

```
image-generation-service.ts:
  95:  const body = brief.campaignInput as GenerateImageRequest;   → ELIMINAR (todas as leituras passam ao domínio)
 585:  (segundo uso do body — validar; validarPrompts 584)
 704-759: buildCommercialRepertoire(body)  → (brief: CampaignBrief)
   709-712: heuristic de validade atuais (/, até, válida)          → REPLACED por validity.enabled/displayText (D8, 6.2)
 843-938: buildPromptVariables  → (brief, context)
   889-919: reads de body.* → brief.product.name / brief.commercial.preco / brief.commercial.validity?.enabled ? displayText : "" / brief.commercial.legalNotice?.text (só quando enabled) / brief.creativeContext.sensitiveConstraints
 382-397: assembly ImageReviewInput → ré-roteada do domínio (6.5 REAL — a interface mudou no 39-05, aqui mudam os call sites)
  427:   hadProductImage: !!body.productImageDataUrl → !!media.primary.dataUrl
```

Mapeamento de leitura (D11 — conjunto de variáveis IDÊNTICO):
| Atual (body.*) | Novo (domínio) |
|---|---|
| `body.productName` / `effectiveProductName` | `effectiveProductName` (já calculado — permanece) |
| `body.originalPriceCents`/`discountedPriceCents` | `brief.commercial.originalPriceCents`/`discountedPriceCents` |
| `body.badgeText` | `brief.commercial.badgeText` |
| `body.hook`/`cta`/`objective` | `brief.commercial.hook`/`cta`/`objective` |
| `body.campaignDetails`/`additionalDetails` | `brief.commercial.campaignDetails`/`additionalDetails` |
| `body.targetChannel`/`format` | `brief.commercial.targetChannel`/`format` |
| `body.validity` | `brief.commercial.validity?.enabled ? (brief.commercial.validity?.displayText ?? "") : ""` |
| `body.availabilityNotes` | `brief.commercial.availabilityNotes` |
| `body.sensitiveConstraints` | `brief.creativeContext.sensitiveConstraints` |
| `body.mandatoryArtworkText` | `brief.commercial.legalNotice?.enabled ? (brief.commercial.legalNotice?.text ?? "") : ""` |
| `body.preserveImageContext` | `brief.creativeContext.preserveImageContext` |
| `body.productImageDataUrl` | `brief.media.images.find(i => i.role === "primary")?.dataUrl` |

Golden tests (9.4/8.16): para cada intent (offer/spotlight/exclusive), um payload flat de referência → o mesmo que antes → `buildPromptVariables` produz as MESMAS 38 keys e os MESMOS valores; `buildCommercialRepertoire` produz o mesmo repertoire de peças (mesma ordem/wording).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildPromptVariables + buildCommercialRepertoire lendo do domínio (sem heuristic string)</name>
  <files>src/lib/image-generation/services/image-generation-service.ts, src/lib/image-generation/services/__tests__/image-generation-service.test.ts</files>
  <read_first>
    - src/lib/image-generation/services/image-generation-service.ts (843-938 buildPromptVariables; 704-759 buildCommercialRepertoire; 95/585 body alias; assinatura atual generateImage que recebe ResolvedCampaignContext)
    - src/lib/campaign/brief.ts (CampaignBrief — domínio)
    - src/lib/image-generation/services/__tests__/image-generation-service.test.ts (createMinimalBrief 13-39)
    - openspec/changes/fase-39-brief-estruturado-campanha/specs/campaign-brief-pipeline-adapters/spec.md (seed de brief estruturado — variáveis por intent)
  </read_first>
  <behavior>
    - Test 1 (golden offer): input flat de referência offer (preços + validity "válida até 31/12" + legalNotice) → buildPromptVariables com o MESMO conjunto de keys/valores de hoje; validity vira displayText quando enabled
    - Test 2 (golden spotlight/exclusive): por intent → mesmos vars; preserveImageContext não-normalizado quando NÃO offer (serviço lê creativeContext)
    - Test 3: buildCommercialRepertoire com `validity: { enabled: true, displayText: "Até 30/09" }` → parte "Oferta válida: Até 30/09" presente (mesma posição/ordem); com enabled=false → parte ausente (sem heuristic / até válida)
    - Test 4: sem validity/legalNotice → vars ausentes/"" (ausência preservada)
  </behavior>
  <action>
    Em `src/lib/image-generation/services/image-generation-service.ts` (NÃO tocar orquestração da rota — isso é o 39-07; este plano só mexe NO SERVIÇO):

    1. Eliminar o alias `const body = brief.campaignInput as GenerateImageRequest` (95 e 585) — substituir todas as leituras pela tabela <interfaces>. IMPORTANTE: a assinatura de `generateImage` passa a receber o DOMÍNIO `CampaignBrief` (de `@/lib/campaign/brief`) como primeiro parâmetro e mantém o contexto (`ResolvedCampaignContext` ou os campos necessários: store.name/segment, identity.directive, brandProfile) — usar `(brief: CampaignBrief, context: ResolvedCampaignContext, ...)`; o wiring da rota que passa o brief montado fica no 39-07 (documentar no resumo se o typecheck da rota quebrar temporariamente).
    2. `buildPromptVariables(brief, context)`: re-rotar TODAS as leituras de body.* para o domínio conforme o mapeamento; `validity` e `mandatoryArtworkText` GATED por enabled (D8/D9). O CONJUNTO DE VARIÁVEIS e a ORDEM de retorno permanecem EXATAMENTE os mesmos (38 keys — regressão por golden tests).
    3. `buildCommercialRepertoire(brief)`: substituir a heuristic string (704-712: `includes("/")`/`includes("até")`/`includes("válida")`) por: `if (brief.commercial.validity?.enabled && brief.commercial.validity.displayText && campaignIntent === "offer") { parts.push(`- Oferta válida: ${brief.commercial.validity.displayText}`); }` — MESMA posição no array e mesmo wording dos partes (wording literal "Oferta válida").
    4. Co-migrar `createMinimalBrief` em image-generation-service.test.ts para o shape ESTRUTURADO (product/commercial/media/creativeContext/metadata) mantendo overrides spread-last; os testes atuais que montam fixtures flat passam a usar brief estruturado equivalente (via buildCampaignBriefFromFlat ou literal).
    5. Golden tests (tasks 9.4/8.16): um payload de referência por intent (offer/spotlight/exclusive) → montar brief via mapper → assertar buildPromptVariables produz SÓ as 38 keys atuais (golden: array de keys ordenado esperado) e os mesmos valores-chave (productName, prices, validity displayText, badgeText). Se o serviço hoje já tem golden de prompts (procurar testes do tipo), MIGRAR mantendo as strings.
  </action>
  <verify>
    <automated>npx vitest run src/lib/image-generation/services/__tests__/image-generation-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Grep por `as GenerateImageRequest` em image-generation-service.ts → 0 (body alias eliminado em 95/585)
    - `buildCommercialRepertoire` sem `includes("/")`/`includes("até")`/`includes("válida")` (grep → 0)
    - Golden tests por intent verdes (mesmas 38 keys/valores); teste 8.16 (mesma variável-set) e 8.17 (decisão por enabled/displayText) presentes
    - validity/legalNotice gated por enabled nas variáveis
    - `npx vitest run` para o arquivo do serviço verde. Typecheck ESCOPADO ao plano: `npm run typecheck` pode reportar erro transitório em `route.ts:639` (call site de `generateImage` ainda passa wrapper `ResolvedCampaignContext` — wiring da rota no 39-07). QUEBRA TRANSITÓRIA CONHECIDA E ACEITA; typecheck GLOBAL limpo é gate somente do 39-08.
  </acceptance_criteria>
  <done>Service lê domínio em prompts e repertoire; heurística string removida; golden tests verdes</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ponte media.primary.dataUrl → provider/InputValidation + assembly do reviewInput do domínio</name>
  <files>src/lib/image-generation/services/image-generation-service.ts, src/lib/image-generation/services/__tests__/image-generation-service.test.ts</files>
  <read_first>
    - src/lib/image-generation/services/image-generation-service.ts (382-397 assembly reviewInput; 427 hadProductImage; usos de productImageDataUrl na chamada ao provider e ao InputValidationService — procurar productImageDataUrl)
    - src/lib/image-generation/services/image-review-service.ts (interface atual — validityText do 39-05)
    - src/lib/campaign/brief.ts (CampaignBrief — media.images com dataUrl runtime)
  </read_first>
  <behavior>
    - Test 1: brief com media.images[0] role primary + dataUrl → provider recebe EXATAMENTE essa dataUrl (ponte base64 em memória — 6.3/8.18)
    - Test 2: assembly reviewInput lê do domínio: legalNotice { enabled: true, text: "Imagem ilustrativa" } → mandatoryArtworkText "Imagem ilustrativa"; enabled=false → ""/undefined; validity { enabled: true, displayText } → validityText preenchido
    - Test 3: hadProductImage true ↔ media.images tem primary com dataUrl (borda 400 continua no zod da rota)
  </behavior>
  <action>
    Em `src/lib/image-generation/services/image-generation-service.ts`:
    1. Adicionar/selecionar helper privado `private primaryImageDataUrl(brief: CampaignBrief): string | undefined` → `brief.media.images.find(i => i.role === "primary")?.dataUrl`; usar em TODO lugar que hoje lê `body.productImageDataUrl` (427 hadProductImage, chamada do provider de imagem, InputValidationService/revisor — task 6.3/F39-16). O provider de imagem e o InputValidationService continuam recebendo o base64 (memória/transporte) — o snapshot (39-04) nunca expõe.
    2. Re-rotear a assembly `reviewInput` (382-397): `mandatoryArtworkText: brief.commercial.legalNotice?.enabled ? brief.commercial.legalNotice?.text : undefined`, `validityText: brief.commercial.validity?.enabled ? brief.commercial.validity?.displayText : undefined`, campaignDetails/additionalDetails/preços/badge de brief.commercial.*, preserveImageContext de brief.creativeContext. (Interface já ganhou validityText no 39-05.)
    3. Co-migrar/adicionar testes: (a) ponte dataUrl (teste 1 — mock do provider/input-validation recebe a dataUrl do primary); (b) assembly gated (teste 2 — legalNotice on/off, validityText); (c) hadProductImage (teste 3). Usar fixtures estruturadas (createMinimalBrief do 39-06 Task 1 + buildCampaignBriefFromFlat para payloads de referência).
    4. Validar `validatePrompts(brief)` (584) — se ler body.*, re-rotar para domínio (mesma lógica).
  </action>
  <verify>
    <automated>npx vitest run src/lib/image-generation/services/__tests__/image-generation-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Grep por `productImageDataUrl` em image-generation-service.ts → 0 (substituído por primaryImageDataUrl/helper)
    - Assembléia reviewInput lê uniquamente `brief.commercial.*` (grep por `brief.campaignInput` → 0 no service)
    - Testes verdes: ponte dataUrl, gating legalNotice on/off, validityText, hadProductImage
    - `npm run lint` limpo; regressão dos golden tests da Task 1 intacta. Typecheck ESCOPADO aos arquivos deste plano (via vitest dos testes tocados); quebra transitória em `route.ts:639` (wiring no 39-07) ACEITA — typecheck global limpo apenas no 39-08.
  </acceptance_criteria>
  <done>Ponte dataUrl primária para provider/validação; assembly do reviewInput 100% domínio; testes verdes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| serviço → provider de imagem | base64 da imagem primária atravessa para provedor de IA externo (memória/transporte) |
| serviço → revisor de visão | conteúdo de legalNotice/validity é encaminhado para revisão |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-06 | Tampering | Prompts divergem do atual (regressão de geração) | mitigate | Golden tests por intent (offer/spotlight/exclusive) com 38 keys e valores idênticos (F39-19/8.16); wording ordenação de parts preservados |
| T-39-06a | Spoofing | Heurística de validade relança aviso com texto errado | mitigate | buildCommercialRepertoire decide só por `enabled/displayText` (D8/6.2); `includes("/"|"até"|"válida")` removidos (grep gate) |
| T-39-06b | Information Disclosure | Base64 persistido/exposto em log | mitigate | Base64 apenas em memória (media.primary.dataUrl); snapshot nunca o carrega (D6/D7 — testado no 39-04); ponte explícita helper única |
| T-39-06c | Tampering | Av ilustrativo no review quando desabilitado | mitigate | Assembly gated: legalNotice?.text só quando enabled; teste 8.20 com enabled=false (D9) |
| T-39-SC | Tampering | Instalação de pacotes | accept | Nenhum pacote novo — sem gate de legitimidade |
</threat_model>

<verification>
- `npx vitest run src/lib/image-generation/services/__tests__/image-generation-service.test.ts` — todos verdes (incl. golden)
- `npm run lint` limpo; typecheck ESCOPADO aos arquivos tocados neste plano (service + testes via vitest); quebra transitória em `route.ts:639` (call site de generateImage com wrapper — wiring no 39-07) ACEITA — typecheck global limpo apenas no 39-08
- Grep gate: `as GenerateImageRequest` → 0 no service; `includes("/")`/`até`/`válida` fora de buildCommercialRepertoire → 0 no service; `brief.campaignInput` → 0 no service
- Sem alteração da rota (orquestração permanece no 39-07)
</verification>

<success_criteria>
- Service 100% domínio (sem body alias, sem heuristic string, ponte dataUrl única)
- Golden tests por intent verdes (comportamento de geração preservado — F39-19)
- Base64 só em memória; assembly do reviewInput gated e testada
</success_criteria>

<output>
Create `.planning/phases/39-brief-estruturado-campanha/39-06-SUMMARY.md` when done
</output>
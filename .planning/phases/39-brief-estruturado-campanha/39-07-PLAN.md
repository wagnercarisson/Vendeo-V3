---
phase: 39-brief-estruturado-campanha
plan: 07
type: execute
wave: 6
depends_on: [39-02, 39-03, 39-04, 39-05, 39-06]
files_modified:
  - src/app/api/campaign/generate-image/route.ts
  - src/app/api/campaign/generate-image/__tests__/route.test.ts
  - scripts/benchmark.ts
autonomous: true
requirements: [F39-09, F39-19, F39-20]

must_haves:
  truths:
    - "A rota converte flat → CampaignBrief UMA vez na fronteira (buildCampaignBriefFromFlat, wired no 39-05) e as costuras passam domínio + contexto (copy 580 / generateImage 639 / validatePrompts 689 wired em 39-05/39-06) — orquestração (crédito, rate limit, clearance, readiness, stream, telemetria, estorno) INALTERADA"
    - "input_snapshot é montado via buildCampaignBriefSnapshot (versionado campaign_brief_v1) substituindo o build flat manual (route.ts:357-380)"
    - "Borda sem imagem continua 400 (regra no zod do transporte — schema.ts:30 — inalterada)"
    - "Regressão: tests #26-28 (propagação de mandatoryArtworkText → legalNotice, mockGenerateImage, mockGenerateCopy) e os demais cenários verdes no route.test.ts"
    - "Benchmark co-migrado: payload flat mantido, brief estruturado montado via mapper (build aplicado NESTE plano, substituindo o flat 332-375 e removendo o cast `as any` em 375) — cenário 10 detalhes-variados compat"
  artifacts:
    - path: "src/app/api/campaign/generate-image/route.ts"
      provides: "Snapshot versionado via buildCampaignBriefSnapshot (brief/context das costuras já wired em 39-05/39-06)"
      contains: "buildCampaignBriefSnapshot"
    - path: "src/app/api/campaign/generate-image/__tests__/route.test.ts"
      provides: "Fixtures co-migradas para fluxo flat→brief + testes #26-28 atualizados"
      contains: "inputSnapshot"
    - path: "scripts/benchmark.ts"
      provides: "Benchmark validado com brief estruturado via mapper (payload flat mantido — build aplicado neste plano)"
      contains: "buildCampaignBriefFromFlat"
  key_links:
    - from: "route.ts"
      to: "src/lib/campaign/brief.ts"
      via: "const snapshot = buildCampaignBriefSnapshot(brief) no lugar do objeto flat (357-380); brief já existe da fronteira do 39-05"
      pattern: "buildCampaignBriefSnapshot"
    - from: "scripts/benchmark.ts"
      to: "src/lib/campaign/brief.ts"
      via: "mapper com storeId 'benchmark' + wrapper de identidade (payload flat mantido; build aplicado neste plano)"
      pattern: "buildCampaignBriefFromFlat"
---

<objective>
Concluir a integração na ROTA `src/app/api/campaign/generate-image/route.ts` — as costuras já foram wired nos planos anteriores (copy 580 no 39-05; generateImage 639 + validatePrompts 689 no 39-06; `brief` domínio já construído na fronteira via `buildCampaignBriefFromFlat`):
- montar `input_snapshot` via `buildCampaignBriefSnapshot` (D6/F39-19) substituindo o build flat manual (357-380)
- preservar a borda sem imagem → 400 (regra do zod do transporte inalterada — D7)
- co-migrar `route.test.ts` (dezenas de fixtures `mockResolvedValue({ campaignInput: {}, store: {} })`) e aplicar o build via mapper em `scripts/benchmark.ts` (payload flat mantido — substitui o flat 332-375 e remove o cast `as any` em 375; durante o 39-06 o cast existente mantém o typecheck global verde)
- GATE FINAL: `npm run typecheck` + `npm run lint` globais exit 0 — nenhuma quebra transitória em nenhum plano

Purpose: D5/D6/D11 — conectar o domínio criado nos planos 39-02..39-06 à entrada real; a ORQUESTRAÇÃO (crédito, rate limit, clearance, readiness, stream, telemetria, estorno) NÃO muda (6.6).
Output: rota operando no domínio estruturado com snapshot versionado; testes de rota e benchmark verdes.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@openspec/changes/fase-39-brief-estruturado-campanha/tasks.md (seção 7 — 7.1 a 7.3; seção 9 — 9.1 e 9.3)
@openspec/changes/fase-39-brief-estruturado-campanha/specs/campaign-brief-mapper/spec.md (fronteira única; storeId param)
@openspec/changes/fase-39-brief-estruturado-campanha/specs/campaign-brief-snapshot/spec.md (input_snapshot versionado)
@src/app/api/campaign/generate-image/route.ts (validação 137-154; store identity 209-235; buildCampaignBrief 241; rate limit 243-253; créditos 258+; seams 562/580/639/689 — já wired 39-05/39-06; snapshot flat 357-380 a substituir; inputValidationOverride 295)
@src/app/api/campaign/generate-image/__tests__/route.test.ts (mock setup 23-27; fixtures 218/432/460/480/569/595/678/706/744/831; tests #26-28 769-809)
@src/lib/campaign/brief.ts (mapper + builder — 39-04)
@src/lib/copy/mapper.ts (mapBriefToCopyDirectorInput agora lê domínio + call site wired — 39-05)
@src/lib/image-generation/services/image-generation-service.ts (generateImage(brief: CampaignBrief, context) + call site wired — 39-06)
@scripts/benchmark.ts (332-375 — build flat atual a substituir pelo mapper neste plano; cast `as any` em 375 remove depois do build — durante o 39-06 o cast mantém o typecheck global verde; cenário 10 detalhes-variados)

<interfaces>
Fluxo pós-mudança na rota (costuras já wired nos planos 39-05/39-06):

```typescript
// ── Pre-stream: Resolve store identity (backend-side) ──────────────
const { storeId, ...campaignInput } = parsed.data;           // 307 (inalterado)
...
const validatedSnapshot = await validateIdentityReference(storeSnapshot);  // 310
const context = await buildCampaignBrief(validatedSnapshot, campaignInput as CampaignInput);   // 241 → ResolvedCampaignContext (plano 39-03)
const brief = buildCampaignBriefFromFlat(parsed.data, parsed.data.storeId);   // JÁ WIRED no 39-05 — manter
const snapshot = buildCampaignBriefSnapshot(brief);          // ESTE PLANO — substitui objeto flat 357-380
```

Seams já wired (39-05/39-06) — VERIFICAR que seguem passando o domínio + contexto, NÃO re-wire:
- `mapBriefToCopyDirectorInput(brief, input)` (580) — brief é o domínio CampaignBrief (wired 39-05)
- `service.generateImage(brief, context, ...)` (639) — brief domínio + contexto ResolvedCampaignContext (wired 39-06)
- `validatePrompts(brief, context)` (689) — wired 39-06
- `inputValidationOverride` continua lido de `parsed.data` (flat — rota 295; decisão documentada)

`input_snapshot` = `buildCampaignBriefSnapshot(brief)` — mesma variável `inputSnapshot: Record<string, unknown>` usada no `createCampaign` (route.test.ts #26-28 asserta `inputSnapshot: expect.objectContaining({ mandatoryArtworkText })` → agora contém `commercial.legalNotice.text`).

Borda sem imagem: inalterada — zod `productImageDataUrl: z.string().min(1)` (schema.ts:30) → safeParse falha → 400 (137-154). O mapper NÃO lança.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rota — snapshot versionado substituindo o flat manual + co-migração route.test.ts</name>
  <files>src/app/api/campaign/generate-image/route.ts, src/app/api/campaign/generate-image/__tests__/route.test.ts</files>
  <read_first>
    - src/app/api/campaign/generate-image/route.ts (137-154 validação/400; 209-241 identidade + buildCampaignBrief; 243-253 rate limit; 258+ créditos; 295 inputValidationOverride; 357-380 snapshot flat; seams 562/580/639/689 — já wired 39-05/39-06, verificar somente)
    - src/app/api/campaign/generate-image/__tests__/route.test.ts (mock setup 23-27; fixtures listadas; tests #26-28 769-809)
    - src/lib/campaign/brief.ts (mapper/buildCampaignBriefSnapshot — 39-04)
    - src/lib/copy/mapper.ts e src/lib/image-generation/services/image-generation-service.ts (assinaturas novas — 39-05/39-06)
  </read_first>
  <behavior>
    - Test 1: request válido (offer com mandatoryArtworkText "Imagem ilustrativa") → createCampaign recebe `inputSnapshot` versionado contendo `commercial.legalNotice.text` (substitui o assert atual `mandatoryArtworkText` top-level dos tests #26-28)
    - Test 2: mockGenerateImage chamado com brief domínio + contexto (assert do test #27 mantido — wiring do 39-06)
    - Test 3: request sem productImageDataUrl → 400 (borda preservada — sem regressão)
    - Test 4: fixtures passam a devolver `ResolvedCampaignContext` completo (mockResolvedValue com store/identity) e o mapper monta o brief nos mocks — sem quebra de nenhum cenário (218, 432, 460, 480, 569, 595, 678, 706, 744, 831)
  </behavior>
  <action>
    Em `src/app/api/campaign/generate-image/route.ts`:
    1. O `brief` domínio já existe na fronteira (wired no 39-05 — `const brief = buildCampaignBriefFromFlat(parsed.data, parsed.data.storeId)`); o `context` (`ResolvedCampaignContext` — 39-03) já é o resultado do `buildCampaignBrief` (241). VERIFICAR ambos presentes (grep) e NÃO recriar.
    2. Substituir o bloco flat 357-380 por `const snapshot = buildCampaignBriefSnapshot(brief);` mantendo a mesma variável consumida no `createCampaign` (input_snapshot — rota grava `Record<string, unknown>`; jsonb tolerante, sem migration).
    3. VERIFICAR (não re-wire) as seams: `mapBriefToCopyDirectorInput(brief, input)` (580), `generateImage(brief, context, ...)` (639), `validatePrompts(brief, context)` (689) — os call sites já passam o domínio `brief` + `context` (39-05/39-06). `inputValidationOverride` continua de `parsed.data` (295 — não muda).
    4. NÃO tocar: orquestração (rate limit 243-253, créditos 258+, clearance, readiness, stream, telemetria, estorno), borda 400 (137-154), store identity (209-235).
    5. Co-migrar `route.test.ts` (9.1): fixtures `mockResolvedValue({ campaignInput: {}, store: {} })` → devolver `ResolvedCampaignContext` completo (campaignInput + store + brandProfile + identity) OU mockar o mapper conforme o fluxo novo; atualizar tests #26-28 para o snapshot versionado (`inputSnapshot` contendo `commercial.legalNotice.text` — verificar o caminho real do snapshot: objeto versionado com seções); manter os demais cenários (rate limit 480-481, copy result publication_copy_snapshot 480-508) verdes sem mudança de comportamento.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/campaign/generate-image/__tests__/route.test.ts; if ($?) { npm run typecheck; if ($?) { npm run lint } }</automated>
  </verify>
  <acceptance_criteria>
    - `route.ts` chama `buildCampaignBriefFromFlat` (wired 39-05 — grep confirma) e `buildCampaignBriefSnapshot` (este plano — grep)
    - Objeto flat manual 357-380 removido (grep por `productImage: { provided: true` no route.ts → 0)
    - Seams seguem passando `brief` domínio (grep por `mapBriefToCopyDirectorInput(brief`/`generateImage(brief` — sem re-wire)
    - Borda 400 intacta (teste 3 verde); orquestração sem diff (grep por `rateLimit`/`credits` inalterados)
    - tests #26-28 migrados e verdes; todos os cenários do route.test.ts verdes
    - `npm run typecheck` (GLOBAL) exit 0 + `npm run lint` exit 0 — gate final do plano
  </acceptance_criteria>
  <done>Rota com snapshot versionado; seams verificados (wired 39-05/39-06); route.test.ts co-migrado e verde</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Benchmark — build via mapper aplicado NESTE plano + remoção do cast `as any` + compat cenário 10</name>
  <files>scripts/benchmark.ts</files>
  <read_first>
    - scripts/benchmark.ts (332-375 — build flat atual a substituir via buildCampaignBriefFromFlat NESTE plano; cast `as any` em 375 a remover; chamada generateImage 376+ passa a receber (brief, context))
    - src/lib/campaign/brief.ts (buildCampaignBriefFromFlat — 39-04)
    - scripts/benchmark-scenarios.ts (cenários, incluindo o 10 detalhes-variados com validity heurística)
    - openspec/changes/fase-39-brief-estruturado-campanha/tasks.md (9.3 — compat payload flat mantido)
  </read_first>
  <behavior>
    - Test 1: cenário de referência (payload flat) → `buildCampaignBriefFromFlat(payload, 'benchmark')` → mesmo brief equivalente que o wrapper atual (product.name, preços, validity displayText)
    - Test 2: cenário 10 detalhes-variados (validity "válida até..." + availabilityNotes/campaignDetails/additionalDetails) → brief com commercial.validity { enabled: true, displayText } — compat preservada
  </behavior>
  <action>
    Aplicar/ajustar `scripts/benchmark.ts` (build via mapper NESTE plano — substitui o flat 332-375 e remove o cast `as any` em 375; o cast existente manteve o typecheck global verde durante o 39-06, quando `generateImage` trocou de assinatura):
    1. Substituir o build flat (332-375) por `const brief = buildCampaignBriefFromFlat({ ...scenario.campaign, storeId: 'benchmark', productImageDataUrl: imageDataUrl }, 'benchmark');` e manter o wrapper de identidade (`store`, `brandProfile`, `identity`) montado como hoje para o `context`.
    2. Remover o cast `as any` (375) — a chamada ao `generateImage` passa `(brief, context)` conforme a assinatura nova (39-06); o `brief` tipado do mapper satisfaz o typecheck sem cast.
    3. Verificar o cenário 10 `detalhes-variados`: como hoje exercita a heuristic de validity do serviço (removida no 39-06), o resultado passa a vir de `commercial.validity.displayText` — o comportamento da peça permanece o mesmo (golden); se o benchmark assertar a string "Oferta válida: ..." com o texto do displayText, validar equivalência.
    4. Rodar o benchmark em modo seco (se houver flag) ou validar por typecheck + smoke (cada cenário monta brief sem erro). NÃO é para executar geração real de IA neste plano.
  </action>
  <verify>
    <automated>npx tsc --noEmit scripts/benchmark.ts 2>&1; if ($?) { Select-String -LiteralPath "scripts\benchmark.ts" -Pattern "buildCampaignBriefFromFlat" }</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/benchmark.ts` usa `buildCampaignBriefFromFlat` (grep) e não tem `as any` na chamada ao service (grep por `as any` → 0 nesse trecho)
    - Cenário 10 monta brief com validity estruturada (validação manual ou assert local)
    - `npx tsc --noEmit scripts/benchmark.ts` sem erros; `npm run typecheck` (GLOBAL) exit 0
  </acceptance_criteria>
  <done>Benchmark com build via mapper aplicado neste plano, `as any` removido e compat do cenário 10 validada</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente → rota | Input não-confiável validado por zod antes de cruzar a fronteira |
| rota → serviço de geração | Brief domínio + contexto interno passam ao pipeline (streaming) |
| rota → banco | input_snapshot versionado gravado via createCampaign |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-07 | Spoofing | Input malformado na fronteira | mitigate | zod `safeParse` → 400 (137-154) preservado; mapper é puro (nunca lança — sem novo vetor) |
| T-39-07a | Tampering | Snapshot flat manual divergente | mitigate | Único builder canônico `buildCampaignBriefSnapshot` (39-04) substitui objeto flat 357-380; tests #26-28 assertam estrutura versionada |
| T-39-07b | Integrity | Orquestração alterada acidentalmente | mitigate | Blocos de orquestração intocados (rate limit, créditos, stream, telemetria, estorno); verify por regressão completa do route.test.ts |
| T-39-07c | Information Disclosure | Base64 em snapshot gravado | mitigate | Snapshot versionado sem dataUrl (por tipo + varredura 39-04); rota grava o que o builder produz |
| T-39-07d | Spoofing | Benchmark com payload divergente | mitigate | Payload flat mantido; mapper 'benchmark' único ponto de conversão; cenário 10 validado |
| T-39-SC | Tampering | Instalação de pacotes | accept | Nenhum pacote novo — sem gate de legitimidade |
</threat_model>

<verification>
- `npx vitest run src/app/api/campaign/generate-image/__tests__/route.test.ts` — todos verdes (incl. #26-28 migrados)
- `npm run typecheck` (GLOBAL) + `npm run lint` exit 0 — gate final do fase de rotas (nenhuma quebra transitória em 39-05/39-06/39-07)
- Grep: `productImage: { provided: true` em route.ts → 0 (snapshot flat removido); `buildCampaignBriefFromFlat`/`buildCampaignBriefSnapshot` presentes na rota
- Benchmark compila (tsc) sem `as any`
</verification>

<success_criteria>
- Rota com snapshot versionado via buildCampaignBriefSnapshot (flat manual 357-380 removido); costuras verificadas (wired 39-05/39-06); orquestração inalterada; borda 400 preservada
- route.test.ts (dezenas de fixtures + #26-28) co-migrado e verde; benchmark compat validado
- `npm run typecheck` (GLOBAL) + `npm run lint` exit 0 — gate final
</success_criteria>

<output>
Create `.planning/phases/39-brief-estruturado-campanha/39-07-SUMMARY.md` when done
</output>
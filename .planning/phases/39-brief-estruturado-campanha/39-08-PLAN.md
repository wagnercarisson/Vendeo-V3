---
phase: 39-brief-estruturado-campanha
plan: 08
type: execute
wave: 7
depends_on: [39-01, 39-02, 39-03, 39-04, 39-05, 39-06, 39-07]
files_modified:
  - .planning/phases/39-brief-estruturado-campanha/39-VERIFICATION.md
  - .planning/phases/39-brief-estruturado-campanha/39-UAT.md
autonomous: true
requirements: [F39-20]

must_haves:
  truths:
    - "Gates automáticos verdes: npx vitest run (todos os testes novos + existentes + migrados), npm run typecheck, npm run lint, npm run build"
    - "39-VERIFICATION.md existe com evidências por plano (grep gates, contagem de testes, typecheck/lint/build outputs)"
    - "39-UAT.md existe com checklist humano: oferta/spotlight/exclusive resultado idêntico ao atual, snapshot versionado no admin/DB, aviso off → arte sem texto, validade na arte, campanha pré-F39 continua ok"
    - "Nenhum requisito F39-01..F39-21 sem cobertura comprovada"
  artifacts:
    - path: ".planning/phases/39-brief-estruturado-campanha/39-VERIFICATION.md"
      provides: "Evidências automatizadas da fase (gates + cobertura de requisitos)"
      contains: "F39-"
    - path: ".planning/phases/39-brief-estruturado-campanha/39-UAT.md"
      provides: "Roteiro de UAT humana (checklist 10.5)"
      contains: "UAT"
  key_links:
    - from: "39-VERIFICATION.md"
      to: "gates de CI"
      via: "npx vitest run / npm run typecheck / npm run lint / npm run build"
      pattern: "vitest"
    - from: "39-UAT.md"
      to: "admin/DB"
      via: "verificação humana do snapshot versionado (campaign_brief_v1) num campaign ready"
      pattern: "campaign_brief_v1"
---

<objective>
Executar a VERIFICAÇÃO FINAL da F39 (gates automáticos + UAT manual) e produzir os artefatos de fase `39-VERIFICATION.md` e `39-UAT.md`.

Purpose: F39-20/D12 — fechar a fase com evidência objetiva de que (a) todos os gates passam (vitest/typecheck/lint/build), (b) os requisitos F39-01..F39-21 têm cobertura comprovada por planos/testes, e (c) o comportamento de geração é IDÊNTICO ao pré-F39 (golden + UAT humana — D2).
Output: 39-VERIFICATION.md + 39-UAT.md; base para o orchestrator marcar a fase completa.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@openspec/changes/fase-39-brief-estruturado-campanha/tasks.md (seção 10 — 10.1 a 10.5)
@openspec/changes/fase-39-brief-estruturado-campanha/specs/* (specs de contrato — referência de cobertura)
@.planning/phases/39-brief-estruturado-campanha/39-0X-SUMMARY.md (resumos dos planos 39-01..39-07 — evidências por plano)
@scripts/benchmark.ts (validar comparabilidade dos cenários offer/spotlight/exclusive)

<interfaces>
Gates (package.json):
- `npx vitest run` — suíte completa (novos + existentes + co-migrados)
- `npm run typecheck` — `tsc -p tsconfig.typecheck.json --noEmit`
- `npm run lint` — `eslint .`
- `npm run build` — `next build`

Checklist UAT (tasks.md 10.5 — deve dirigir o checkpoint humano):
1. Gerar campanha offer/spotlight/exclusive pelo form → resultado IDÊNTICO ao atual (mesmo copy, mesmas artes — golden por compaibilidade)
2. Snapshot exibe estrutura versionada (admin/DB): `input_snapshot` com `schemaVersion: "campaign_brief_v1"` no ROOT, seções product/commercial/media/creativeContext/metadata; sem base64
3. Aviso ilustrativo DESMARCADO no form → arte SEM o texto; marcado → texto presente
4. Validade na arte quando esperado (validade habilitada com displayText)
5. Campanha antiga (pré-F39) continua exibindo/baixando normalmente (snapshot flat antigo — jsonb tolerante)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gates automáticos — vitest, typecheck, lint, build (e correção de regressões)</name>
  <files>.planning/phases/39-brief-estruturado-campanha/39-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/39-brief-estruturado-campanha/39-0X-SUMMARY.md (resumos 01-07 — evidências coletadas)
    - package.json (scripts: test/typecheck/lint/build)
  </read_first>
  <action>
    Executar os 4 gates na ordem (cada um com saída capturada):
    1. `npx vitest run` — toda a suíte (novos testes de contrato 8.1-8.15+ em src/lib/campaign/__tests__, serviço, rota, copy/review migrados + testes existentes). Registrar contagem total e ocorrências de falha (se houver, corrigir — regressões de tipo/fixture são esperadas como residual dos planos 39-05/39-06/39-07; usar `npm run typecheck` FAMÍLIA antes: tipo de fixture residual que o typecheck acusa deve ser corrigido aqui, alinhando a assinatura real da rota com os mocks).
    2. `npm run typecheck` — 0 erros.
    3. `npm run lint` — 0 erros (novo campo validityText e helpers privados devem passar).
    4. `npm run build` — build bem-sucedido (Prerender; sem necessidade de variáveis novas — a fase não adiciona deps/env).
    Para cada gate, registrar: comando exato, exit code, evidência de saída (trecho), e a associação aos planos (ex.: "golden tests offer/spotlight/exclusive do 39-06 passaram — evidência de F39-15/F39-19"). Corrigir falhas que surgirem e re-rodar até verde.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | Select-String -Pattern "Test Files|Tests "; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Todos os 4 gates exit 0 (vitest sem falhas; typecheck/lint/build limpos) — verificação registrada no 39-VERIFICATION.md criado nesta task (conteúdo: matriz planos×gates, contagens, cobertura de requisitos F39-01..F39-21 com referência aos planos/testes)
    - Nenhuma regressão residual: suíte completa verde com as fixtures migradas
  </acceptance_criteria>
  <done>Gates automáticos verdes; 39-VERIFICATION.md criado com evidências + cobertura por requisito</done>
</task>

<task type="auto">
  <name>Task 2: 39-UAT.md — roteiro de UAT humana (checklist 10.5) + registro de resultado</name>
  <files>.planning/phases/39-brief-estruturado-campanha/39-UAT.md</files>
  <read_first>
    - .planning/phases/39-brief-estruturado-campanha/39-VERIFICATION.md (recém-criado — contexto de gates ok)
    - openspec/changes/fase-39-brief-estruturado-campanha/tasks.md (10.5 — checklist completo)
  </read_first>
  <action>
    Criar `39-UAT.md` com:
    1. Título/contexto: F39 Brief Estruturado de Campanha — UAT local pós-implementação.
    2. Checklist 10.5 (5 itens do <interfaces>) com passos exatos por item (URL/form, o que observar: copy da campanha, arte gerada, input_snapshot no admin/DB — verificar via consulta admin ou UI da campanha, aviso ilustrativo on/off, validade, campanha pré-F39) e coluna de RESULTADO (PASS/FAIL + observação) — preenchendo os campos "PASS/FAIL" como PENDENTE para o agente humano confirmar.
    3. Seção "comparabilidade offer/spotlight/exclusive": usar os cenários do benchmark para confirmar que o prompt final por intent é o mesmo do pré-F39 (referência aos golden tests do 39-06 verdes no 39-VERIFICATION.md) — o teste automatizado cobre; o UAT confirma visualmente.
    4. Instrução de preenchimento para o humano (como marcar PASS/FAIL e o que registrar em caso de falha — incl. repro passo a passo).
    O formulário de geração é o mesmo de sempre (sem mudança de UI — HAS_UI: 0); apenas o SNAPSHOT gravado é versionado e o comportamento de geração deve permanecer idêntico.
  </action>
  <verify>
    <automated>Select-String -LiteralPath ".planning\phases\39-brief-estruturado-campanha\39-UAT.md" -Pattern "PASS/FAIL|campaign_brief_v1"</automated>
  </verify>
  <acceptance_criteria>
    - `39-UAT.md` existe com os 5 itens da checklist 10.5, passos exatos e colunas de resultado (grep por `PASS/FAIL`)
    - Referência explícita ao `campaign_brief_v1` na verificação do snapshot (grep)
    - Instrução de preenchimento humana presente
  </acceptance_criteria>
  <done>39-UAT.md criado com checklist 10.5 pronto para verificação humana</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → CI/build | Comandos automatizados (vitest/tsc/eslint/next build) executados no ambiente |
| UAT → produção | Verificação humana de comportamento (artes/copy/DB) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-08 | Integrity | Gate com falso positivo (teste que passa sem cobrir) | mitigate | 39-VERIFICATION.md associa cada requisito F39-xx aos planos/testes específicos (matriz de cobertura — não só "ficou verde") |
| T-39-08a | Spoofing | Base64 escapando em snapshot apesar dos gates | mitigate | Teste de varredura recursiva (8.12) roda no vitest; UAT item 2 confirma ausência visualmente no admin/DB |
| T-39-08b | Tampering | Regressão silenciosa de geração | mitigate | Golden tests por intent (F39-19) no gate 1 + UAT item 1 (comportamento idêntico) + comparabilidade via benchmark |
| T-39-08c | Spoofing | UAT não preenchido (checklist vazio) | mitigate | Campos PASS/FAIL explícitos + instrução de preenchimento; orchestrator bloqueia fechamento da fase sem UAT preenchido |
| T-39-SC | Tampering | Instalação de pacotes | accept | Nenhum pacote novo — sem gate de legitimidade |
</threat_model>

<verification>
- `npx vitest run` exit 0 (suíte completa); `npm run typecheck` exit 0; `npm run lint` exit 0; `npm run build` exit 0
- 39-VERIFICATION.md documenta os comandos e a matriz de cobertura F39-01..F39-21
- 39-UAT.md criado (checklist humana) — preenchimento final é responsabilidade do humano via orchestrator/execute (end-of-phase human verify)
</verification>

<success_criteria>
- Gates verdes e documentados em 39-VERIFICATION.md (com cobertura por requisito)
- 39-UAT.md com checklist 10.5 pronto (PASS/FAIL pendente para confirmação humana)
- Fase pronta para fechamento pelo orchestrator após UAT humano
</success_criteria>

<output>
Create `.planning/phases/39-brief-estruturado-campanha/39-08-SUMMARY.md` when done
</output>
---
phase: 4.2-commercial-visual-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/campaign/types.ts
  - src/components/campaign/campaign-adjustments-panel.tsx
  - src/app/campaign/preview/page.tsx
  - src/lib/campaign-intelligence/providers/mock.ts
autonomous: true
requirements: [REND-02, AI-02]

must_haves:
  truths:
    - "CampaignAdjustments type has `hook?: string` and `cta?: string` fields"
    - "Adjustments panel shows 'Texto do Benefício' field with maxLength=120, character counter, and undo"
    - "Adjustments panel shows 'Chamada para Ação' field with maxLength=60, character counter, and undo"
    - "Editing hook in panel updates the rendered spec locally"
    - "Editing CTA in panel updates the rendered spec locally"
    - "MockProvider generates segment-aware hooks (not generic text)"
    - "MockProvider generates varied CTAs per segment"
    - "npm run typecheck passes with zero errors"
  artifacts:
    - path: src/components/campaign/types.ts
      provides: CampaignAdjustments type with hook and cta fields
      contains: "hook?: string"
    - path: src/components/campaign/campaign-adjustments-panel.tsx
      provides: Hook and CTA input fields with undo
      contains: "Texto do Benefício"
    - path: src/app/campaign/preview/page.tsx
      provides: Merged spec logic for hook and cta adjustments
      pattern: "commercial_copy\\.hook.*adjustments"
    - path: src/lib/campaign-intelligence/providers/mock.ts
      provides: Segment-aware hook and CTA generation
      contains: "storeSegment.*hook"
  key_links:
    - from: src/app/campaign/preview/page.tsx
      to: src/components/campaign/types.ts
      via: "CampaignAdjustments import"
    - from: src/app/campaign/preview/page.tsx
      to: src/components/campaign/campaign-adjustments-panel.tsx
      via: "CampaignAdjustmentsPanel import + mergedSpec hook/cta merge"
---

<objective>
Expand CampaignAdjustments types, adjustments panel, preview merge logic, and mock provider to support hook/benefício and CTA editing.

Purpose: Hook and CTA already exist in the AI schema but are not editable in the panel or properly merged into the preview. This plan makes them first-class adjustment fields — enabling the lojista to edit hook and CTA text locally with undo support, and preparing the mock provider to generate useful, segment-aware values for visual validation.
Output: Updated types.ts, campaign-adjustments-panel.tsx, preview/page.tsx, mock.ts
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/4.2-commercial-visual-quality/4.2-CONTEXT.md
@openspec/changes/phase-4-2-commercial-visual-quality/proposal.md
@openspec/changes/phase-4-2-commercial-visual-quality/design.md
@openspec/changes/phase-4-2-commercial-visual-quality/tasks.md

# Current code files (read before modifying)
@src/components/campaign/types.ts
@src/components/campaign/campaign-adjustments-panel.tsx
@src/app/campaign/preview/page.tsx
@src/lib/campaign-intelligence/providers/mock.ts

<interfaces>
From src/components/campaign/types.ts (current):
```typescript
export interface CampaignAdjustments {
  title?: string;
  discountedPriceDisplay?: string;
  badgeText?: string;
}
```

From src/app/campaign/preview/page.tsx (current mergedSpec):
```typescript
const mergedSpec: CampaignSpec | null = useMemo(() => {
  if (!spec) return null;
  return {
    ...spec,
    commercial_copy: {
      ...spec.commercial_copy,
      title: adjustments.title ?? spec.commercial_copy.title,
    },
    offer: {
      ...spec.offer,
      discounted_price_display:
        adjustments.discountedPriceDisplay ?? spec.offer.discounted_price_display,
      badge_text: adjustments.badgeText ?? spec.offer.badge_text,
    },
  };
}, [spec, adjustments]);
```

From src/lib/campaign-intelligence/providers/mock.ts (current hook/cta):
```typescript
const hook = input.description && input.description.trim().length > 0
  ? input.description
  : "Corra! Últimas unidades com desconto especial.";
const cta = "Garanta o Seu!";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand CampaignAdjustments type with hook and cta</name>
  <files>src/components/campaign/types.ts</files>
  <action>
    Add `hook?: string` and `cta?: string` fields to the existing `CampaignAdjustments` interface. Do NOT modify any other type or function in this file. The fields follow the same pattern as existing optional string fields (`title`, `discountedPriceDisplay`, `badgeText`). Insert them after `badgeText` with a trailing comma.
  </action>
  <verify>
    <automated>
      cd "$GIT_ROOT" &amp;&amp; grep -q "hook?: string" src/components/campaign/types.ts &amp;&amp; grep -q "cta?: string" src/components/campaign/types.ts
    </automated>
  </verify>
  <done>
    CampaignAdjustments interface has `hook?: string` and `cta?: string` fields. All existing fields and functions unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add hook and CTA fields to adjustments panel</name>
  <files>src/components/campaign/campaign-adjustments-panel.tsx</files>
  <action>
    Add two new field sections in the adjustments panel, before the closing `&lt;/div&gt;` of the main container. Both follow the exact same pattern as the existing "Texto da Chamada" (title) field.

    **Hook field** (after Badge section):
    - Label: "Texto do Benefício" (uppercase tracking-wider, same style)
    - Input: same class styling, `maxLength={120}`, value from `adjustments.hook ?? originalSpec.commercial_copy.hook`, onChange calls `onAdjustmentChange("hook", e.target.value)`
    - Undo button: shown when `adjustments.hook !== undefined`, calls `onUndo("hook")`
    - Original display: `&lt;p&gt;Original: "{originalSpec.commercial_copy.hook}"&lt;/p&gt;`
    - Character counter: display `<span>{currentLength}/{120}</span>` to the right of the label area, using `text-xs text-gray-400`

    **CTA field** (after Hook field):
    - Label: "Chamada para Ação" (uppercase tracking-wider, same style)
    - Input: same class styling, `maxLength={60}`, value from `adjustments.cta ?? originalSpec.commercial_copy.cta`, onChange calls `onAdjustmentChange("cta", e.target.value)`
    - Undo button: shown when `adjustments.cta !== undefined`, calls `onUndo("cta")`
    - Original display: `&lt;p&gt;Original: "{originalSpec.commercial_copy.cta}"&lt;/p&gt;`
    - Character counter: display `<span>{currentLength}/{60}</span>` to the right of the label area

    Keep all existing fields (title, discounted price, badge) unchanged. Do NOT reorder or restyle existing fields.
  </action>
  <verify>
    <automated>
      cd "$GIT_ROOT" &amp;&amp; grep -c "Texto do Benefício" src/components/campaign/campaign-adjustments-panel.tsx | grep -q "1" &amp;&amp; grep -c "Chamada para Ação" src/components/campaign/campaign-adjustments-panel.tsx | grep -q "1" &amp;&amp; grep -c "maxLength={120}" src/components/campaign/campaign-adjustments-panel.tsx | grep -q "1" &amp;&amp; grep -c "maxLength={60}" src/components/campaign/campaign-adjustments-panel.tsx | grep -q "1"
    </automated>
  </verify>
  <done>
    Panel shows "Texto do Benefício" (maxLength=120) and "Chamada para Ação" (maxLength=60) fields with undo and original display. All 3 existing fields remain unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Expand mergedSpec logic in preview page for hook and cta</name>
  <files>src/app/campaign/preview/page.tsx</files>
  <action>
    Expand the `mergedSpec` useMemo in PreviewPage to merge hook and CTA adjustments into the spec before passing to CampaignRenderer.

    In the `commercial_copy` spread, add:
    - `hook: adjustments.hook ?? spec.commercial_copy.hook`
    - `cta: adjustments.cta ?? spec.commercial_copy.cta`

    The mergedSpec currently only overrides `title` in commercial_copy. Add `hook` and `cta` alongside `title`. Do NOT modify any other part of the file — no state changes, no imports, no layout changes.
  </action>
  <verify>
    <automated>
      cd "$GIT_ROOT" &amp;&amp; grep -q "hook: adjustments.hook" src/app/campaign/preview/page.tsx &amp;&amp; grep -q "cta: adjustments.cta" src/app/campaign/preview/page.tsx
    </automated>
  </verify>
  <done>
    mergedSpec correctly applies `adjustments.hook ?? spec.commercial_copy.hook` and `adjustments.cta ?? spec.commercial_copy.cta`. No other changes to the file.
  </done>
</task>

<task type="auto">
  <name>Task 4: Improve MockProvider with segment-aware hooks and CTAs</name>
  <files>src/lib/campaign-intelligence/providers/mock.ts</files>
  <action>
    Improve the hook and CTA generation in `MockProvider.generate()` to return contextually relevant, segment-aware values instead of generic text.

    Add a `getHooksBySegment(segment: string, description?: string): string` function and a
    `getCTAsBySegment(segment: string): string` function to the module scope. Use these in place of the current inline hook/cta logic.

    **Hook generation logic:**
    1. If `input.description` is provided and not empty: use `input.description` (user-authored content)
    2. If no description: return a segment-aware hook from a static map:
       - "moda-vestuario": "O estilo que você merece!"
       - "alimentacao-bebidas": "Sabor inesquecível toda hora!"
       - "beleza-estetica": "Realce sua beleza natural!"
       - "saude-farmacia": "Sua saúde em primeiro lugar!"
       - "eletronicos-tecnologia": "Tecnologia que faz a diferença!"
       - "casa-decoracao": "Transforme seu lar!"
       - "servicos": "Soluções que funcionam pra você!"
       - "petshop": "Seu pet merece o melhor!"
       - "variedades": "O melhor pra você!"
       - "outros": "Não perca esta oportunidade!"

    **CTA generation logic:**
    - Return a segment-aware CTA from a static map:
      - "moda-vestuario": "Garanta seu Estilo!"
      - "alimentacao-bebidas": "Peça Já o Seu!"
      - "beleza-estetica": "Agende Seu Horário!"
      - "saude-farmacia": "Cuide-se Agora!"
      - "eletronicos-tecnologia": "Compre Agora!"
      - "casa-decoracao": "Decore Já!"
      - "servicos": "Solicite Agora!"
      - "petshop": "Mime Seu Pet!"
      - "variedades": "Aproveite Agora!"
      - "outros": "Garanta o Seu!"

    Also update `layout_preset` from `"produto-oferta"` to `"produto-oferta-comercial"` in `visualParameters` to match the single template.

    Do NOT change title, subtitle, offer, or any other field generation. Do NOT change the ProviderRawResponse structure.
  </action>
  <verify>
    <automated>
      cd "$GIT_ROOT" &amp;&amp; grep -q "getHooksBySegment" src/lib/campaign-intelligence/providers/mock.ts &amp;&amp; grep -q "getCTAsBySegment" src/lib/campaign-intelligence/providers/mock.ts &amp;&amp; grep -q "produto-oferta-comercial" src/lib/campaign-intelligence/providers/mock.ts
    </automated>
  </verify>
  <done>
    MockProvider generates segment-aware hooks (non-generic) and varied CTAs. layout_preset updated to "produto-oferta-comercial". Typecheck passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adjustments panel → renderer | Untrusted user input (hook/CTA text) crosses into rendered component |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4.2-01 | Tampering | CampaignAdjustmentsPanel inputs | mitigate | maxLength enforced client-side (120/60 chars); React escapes text content by default; no XSS vector since text renders as textContent in CampaignRenderer |
| T-4.2-02 | Information Disclosure | mock provider logging | accept | MockProvider runs client-side in dev mode only; no PII in segment maps |
| T-4.2-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies — all work uses existing Tailwind CSS + lucide-react |
</threat_model>

<verification>
- `grep -q "hook?: string" src/components/campaign/types.ts` → 0 exit
- `grep -q "cta?: string" src/components/campaign/types.ts` → 0 exit
- `grep -c "Texto do Benefício" src/components/campaign/campaign-adjustments-panel.tsx` → 1 match
- `grep -c "maxLength={120}" src/components/campaign/campaign-adjustments-panel.tsx` → 1 match
- `grep -c "maxLength={60}" src/components/campaign/campaign-adjustments-panel.tsx` → 1 match
- `grep -q "hook: adjustments.hook" src/app/campaign/preview/page.tsx` → 0 exit
- `grep -q "cta: adjustments.cta" src/app/campaign/preview/page.tsx` → 0 exit
- `grep -q "getHooksBySegment" src/lib/campaign-intelligence/providers/mock.ts` → 0 exit
- `npm run typecheck` → zero errors
</verification>

<success_criteria>
- Types expanded with hook and cta fields — no breaking changes to existing interfaces
- Adjustments panel renders both new fields with undo capability — all 3 original fields remain
- mergedSpec correctly applies hook and cta overrides
- MockProvider generates non-generic, segment-aware hooks and CTAs
- `npm run typecheck` passes with zero errors
</success_criteria>

<output>
Create `.planning/phases/4.2-commercial-visual-quality/4.2-01-SUMMARY.md` when done
</output>

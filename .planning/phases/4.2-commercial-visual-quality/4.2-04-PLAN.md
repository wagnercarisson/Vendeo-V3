---
phase: 4.2-commercial-visual-quality
plan: 04
type: execute
wave: 3
depends_on:
  - 4.2-03
files_modified: []
autonomous: true
requirements: [REND-04]

must_haves:
  truths:
    - "Mock with minimum fields renders coherent campaign art"
    - "Mock with complete fields renders full campaign art"
    - "Real OpenAI-generated spec renders correctly (if API available)"
    - "Hook text appears in the rendered composition"
    - "CTA appears as visual campaign element (not UI button)"
    - "Adjustments panel hook and CTA fields render with undo"
    - "Hook and CTA adjustments update the rendered art locally without API calls"
    - "Missing product image shows explicit error (not publishable)"
    - "Missing logo shows professional initials fallback"
    - "Missing badge renders no empty space"
    - "npm run typecheck passes with zero errors"
    - "npm run lint passes with zero errors"
    - "npm run build passes with zero errors"
    - "All 7 publishability criteria answered 'sim' — manual gate passed"
  artifacts:
    - path: .planning/phases/4.2-commercial-visual-quality/4.2-PUBLISHABILITY.md
      provides: Manual publishability gate checklist with all 7 criteria answered
      contains: "sim"
  key_links:
    - from: src/components/campaign/campaign-renderer.tsx
      to: src/app/campaign/preview/page.tsx
      via: "PreviewPage imports CampaignRenderer — visual validation"
    - from: .planning/phases/4.2-commercial-visual-quality/4.2-PUBLISHABILITY.md
      to: openspec/changes/phase-4-2-commercial-visual-quality/proposal.md
      via: "Publishability criteria defined in proposal acceptance criteria"
---

<objective>
Validate the complete Phase 4.2 output against all acceptance criteria: visual correctness with mock data, adjustments panel interaction, fallback behavior, type/lint/build pipeline, and manual publishability checklist.

Purpose: Ensure the commercial visual quality phase delivers exactly what was specified — professional, publishable campaign art that meets all acceptance criteria. This gate catches regressions, visual defects, and edge cases before Phase 5 (Review, Adjust & Export).

No code changes expected. This is a verification-only plan.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@openspec/changes/phase-4-2-commercial-visual-quality/proposal.md
@openspec/changes/phase-4-2-commercial-visual-quality/tasks.md

@src/components/campaign/campaign-renderer.tsx
@src/components/campaign/campaign-adjustments-panel.tsx
@src/components/campaign/types.ts
@src/app/campaign/preview/page.tsx
@src/lib/campaign-intelligence/providers/mock.ts

<interfaces>
From proposal.md — Acceptance Criteria:
- A arte não usa mais layout empilhado
- O hook/benefício aparece visualmente na campanha
- O CTA aparece como elemento de campanha, não como botão de UI
- O preço promocional aparece como bloco de oferta forte
- A assinatura da loja parece profissional com logo ou iniciais
- A ausência de logo não quebra a arte
- A ausência/erro da imagem do produto mostra estado explícito (não publicável)
- O painel permite editar hook e CTA
- Mock com dados mínimos funciona
- Mock com dados completos funciona
- OpenAI real gera conteúdo compatível com a arte
- npm run typecheck, npm run lint, npm run build — zero erros
- Os 7 critérios de publicabilidade são "sim"

7 Publishability Criteria (from proposal):
1. A composição tem hierarquia visual clara (preço > hook > nome > descrição)
2. A imagem do produto está integrada profissionalmente na arte
3. O texto é legível e não transborda as zonas seguras
4. O CTA parece um elemento de campanha (não um botão de UI)
5. O bloco de preço tem destaque visual adequado
6. A identidade da loja está presente e com apresentação profissional
7. Não há quebras visuais com dados ausentes (fallbacks funcionam)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated validation — typecheck, lint, build, and logical checks</name>
  <files></files>
  <action>
    Run all automated validations and logical checks to verify the Phase 4.2 implementation is technically sound.

    **Step 1 — Run automated pipeline:**
    ```
    npm run typecheck
    npm run lint
    npm run build
    ```

    If any fail, analyze the error output and fix the root cause. Do NOT advance until all three pass.

    **Step 2 — Logical checks (grep-based):**

    Verify CampaignRendererProps is unchanged:
    ```
    grep -c "interface CampaignRendererProps" src/components/campaign/campaign-renderer.tsx
    # Expected: 1
    ```

    Verify no `<button disabled>` in renderer (CTA must not be a disabled button):
    ```
    grep -c "button.*disabled" src/components/campaign/campaign-renderer.tsx
    # Expected: 0
    # If >0, the renderer still uses a disabled button for CTA — MUST fix
    ```

    Verify hook appears in renderer JSX:
    ```
    grep -c "hook" src/components/campaign/campaign-renderer.tsx
    # Expected: > 0 (hook must be referenced in renderer code)
    ```

    Verify no broken image pattern in renderer:
    ```
    grep -c 'alt=""' src/components/campaign/campaign-renderer.tsx
    # Expected: 0 (all images must have alt text)
    ```

    Verify CampaignAdjustments has hook and cta:
    ```
    grep -q "hook?: string" src/components/campaign/types.ts && echo "PASS" || echo "FAIL"
    grep -q "cta?: string" src/components/campaign/types.ts && echo "PASS" || echo "FAIL"
    ```

    Verify adjustments panel has hook and cta fields:
    ```
    grep -q "Texto do Benefício" src/components/campaign/campaign-adjustments-panel.tsx && echo "PASS" || echo "FAIL"
    grep -q "Chamada para Ação" src/components/campaign/campaign-adjustments-panel.tsx && echo "PASS" || echo "FAIL"
    ```

    Verify mergedSpec hooks up hook and cta:
    ```
    grep -q "hook: adjustments.hook" src/app/campaign/preview/page.tsx && echo "PASS" || echo "FAIL"
    grep -q "cta: adjustments.cta" src/app/campaign/preview/page.tsx && echo "PASS" || echo "FAIL"
    ```

    **Step 3 — Record results** in a temporary file `.planning/phases/4.2-commercial-visual-quality/4.2-AUTOMATION-CHECK.md`:
    Document each check result (PASS/FAIL/NA) with grep output or command exit code.

    If any check FAILs, fix the source code, re-run, and re-check. Do NOT proceed to manual validation with failed automated checks.

    **Note:** Do NOT modify files for style/cleanup unrelated to failures. Only fix actual errors.
  </action>
  <verify>
    <automated>
      cd "$GIT_ROOT" &amp;&amp; npm run typecheck 2>&amp;1 &amp;&amp; npm run lint 2>&amp;1 &amp;&amp; npm run build 2>&amp;1 | tail -10
    </automated>
  </verify>
  <done>
    `npm run typecheck`, `npm run lint`, `npm run build` all pass with zero errors. All logical grep checks pass. Automation check results documented.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual visual validation — mock, adjustments, and render quality</name>
  <files></files>
  <what-built>
    The complete Phase 4.2 output: CampaignRenderer, adjustments panel with hook/CTA, PreviewPage, and MockProvider.
  </what-built>
  <how-to-verify>
    **Setup:**
    1. Run the dev server: `npm run dev`
    2. Open the campaign form page (likely at `http://localhost:3000`)
    3. Fill in minimum fields (product name, price, offer price) and submit to generate a campaign preview

    **Validation Checklist (check each item):**

    **A. Mock with minimum fields (no description, no badge, no logo, no hook override):**
    - [ ] Art renders without stacked layout
    - [ ] Product image appears with professional treatment
    - [ ] Background has visual treatment (not solid flat color)
    - [ ] Hook is visible — should be segment-aware from MockProvider
    - [ ] Discounted price is the most prominent text element
    - [ ] CTA appears as visual pill element (NOT a button, NOT interactive)
    - [ ] Store identity shows at bottom with initials fallback
    - [ ] No layout breakage or overlapping text

    **B. Mock with complete fields (add description, badge, logo via store setup):**
    - [ ] Badge renders in top-right corner
    - [ ] Description renders below price
    - [ ] Logo renders in store identity zone (not initials)
    - [ ] Complete composition looks professional and balanced

    **C. Adjustments panel — hook and CTA fields:**
    - [ ] "Texto do Benefício" field visible with current hook value
    - [ ] "Chamada para Ação" field visible with current CTA value
    - [ ] Editing hook updates rendered art immediately (no API call)
    - [ ] Editing CTA updates rendered art immediately (no API call)
    - [ ] Character counter works (120 for hook, 60 for CTA)
    - [ ] Original value displayed below each input
    - [ ] Undo button appears after editing hook
    - [ ] Undo button appears after editing CTA
    - [ ] Clicking undo resets to original value and re-renders

    **D. Product image missing:**
    - [ ] Cause image to fail (e.g., use a broken URL or proxy-block the image)
    - [ ] Renderer shows explicit error state: "Imagem do produto indisponível" with ImageOff icon
    - [ ] Error state does NOT look like a publishable placeholder

    **E. Logo missing:**
    - [ ] Remove logo URL from store identity
    - [ ] Renderer shows initials fallback (circular, colored, professional)
    - [ ] No broken image icon appears

  </how-to-verify>
  <resume-signal>
    Type the results of each validation section (PASS/FAIL) with any issues observed.
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Publishability gate — answer all 7 criteria</name>
  <files>.planning/phases/4.2-commercial-visual-quality/4.2-PUBLISHABILITY.md</files>
  <what-built>
    The complete rendered campaign art — to be judged against publishability criteria.
  </what-built>
  <how-to-verify>
    Evaluate the rendered campaign art in the browser and answer each of the 7 publishability criteria.

    Create the file `.planning/phases/4.2-commercial-visual-quality/4.2-PUBLISHABILITY.md` with:

    ```markdown
    # Phase 4.2 — Publishability Gate

    **Date:** {current date}
    **Evaluator:** {user name}
    **Campaign data used:** {mock with minimum / mock with complete / OpenAI}

    ## Criteria

    | # | Criterion | Answer |
    |---|-----------|--------|
    | 1 | A composição tem hierarquia visual clara (preço &gt; hook &gt; nome &gt; descrição) | Sim / Não |
    | 2 | A imagem do produto está integrada profissionalmente na arte | Sim / Não |
    | 3 | O texto é legível e não transborda as zonas seguras | Sim / Não |
    | 4 | O CTA parece um elemento de campanha (não um botão de UI) | Sim / Não |
    | 5 | O bloco de preço tem destaque visual adequado | Sim / Não |
    | 6 | A identidade da loja está presente e com apresentação profissional | Sim / Não |
    | 7 | Não há quebras visuais com dados ausentes (fallbacks funcionam) | Sim / Não |

    **Result:** {PASS if all "Sim", FAIL otherwise}

    ## Notes
    {Any observations or issues found}
    ```

    For each criterion:
    - Answer "Sim" if the criterion is fully met
    - Answer "Não" if the criterion is NOT met
    - If "Não", describe exactly what needs to be fixed

    **The phase only passes if ALL 7 criteria are answered "Sim".**

    If any criterion fails:
    1. Note the specific issue in the file
    2. Report back with which criterion failed and what the problem is
    3. The issue will be addressed before the phase is marked complete
  </how-to-verify>
  <resume-signal>
    Report the publishability gate results (all 7 criteria with Sim/Não answers). If all 7 are "Sim", the phase is complete.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manual validation | User judgment determines publishability — no code trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4.2-07 | Repudiation | Publishability gate results | mitigate | Results recorded in version-controlled PUBLISHABILITY.md file |
| T-4.2-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies — all validation uses existing tooling (npm, grep) |
</threat_model>

<verification>
- `npm run typecheck` → zero errors
- `npm run lint` → zero errors
- `npm run build` → zero errors
- All grep-based logical checks pass
- User confirms hook appears in rendered art
- User confirms CTA is a visual pill (not button)
- User confirms adjustments panel works for hook and CTA
- User confirms missing image shows explicit error
- User confirms missing logo shows initials fallback
- `4.2-PUBLISHABILITY.md` exists with all 7 criteria answered "Sim"
</verification>

<success_criteria>
- All automated checks pass (typecheck, lint, build, logical grep checks)
- Manual visual validation passes: mock with min/max data, adjustments panel, fallbacks
- All 7 publishability criteria are answered "Sim" in `4.2-PUBLISHABILITY.md`
- Phase 4.2 is ready for handoff to Phase 5
</success_criteria>

<output>
Create `.planning/phases/4.2-commercial-visual-quality/4.2-04-SUMMARY.md` when done
</output>

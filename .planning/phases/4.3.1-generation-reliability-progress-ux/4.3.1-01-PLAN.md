---
phase: 4.3.1-generation-reliability-progress-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/image-generation/schema.ts
  - src/lib/image-generation/config.ts
  - src/lib/image-generation/providers/types.ts
  - src/lib/image-generation/providers/openai.ts
  - src/lib/image-generation/services/image-generation-service.ts
  - src/lib/image-generation/services/image-review-service.ts
autonomous: true
requirements: [REND-01]

must_haves:
  truths:
    - "GenerationPhaseEvent interface has phase, status, message, detail fields"
    - "GenerationErrorCode union has all 12 error codes including input_low_confidence, review_low_confidence, product_image_conflict, generated_product_mismatch, provider_auth_error"
    - "GenerationError interface has phase, code, message, detail?, httpStatus, retryable, requiresUserAction?"
    - "ImageProviderInput has signal?: AbortSignal and attempt?: number"
    - "ImageReviewResult has failureType field instead of generic review_failed"
    - "IMAGE_GENERATION_GLOBAL_TIMEOUT_MS defaults to 300000 in config.ts"
    - "npm run typecheck passes with zero errors"
  artifacts:
    - path: src/lib/image-generation/schema.ts
      provides: GenerationPhaseEvent, GenerationErrorCode, GenerationError, failureType in ImageReviewResult
      pattern: "GenerationErrorCode"
    - path: src/lib/image-generation/config.ts
      provides: IMAGE_GENERATION_GLOBAL_TIMEOUT_MS config
      contains: "300000"
    - path: src/lib/image-generation/providers/types.ts
      provides: Extended ImageProviderInput with signal and attempt
      contains: "AbortSignal"
    - path: src/lib/image-generation/services/image-generation-service.ts
      provides: onPhaseChange callback in generateImage(), AbortSignal parameter
      contains: "onPhaseChange"
---

<objective>
Implement foundation types, error system, timeout infrastructure, and provider interface updates for Phase 4.3.1.

Purpose: Establish the type system and infrastructure that all subsequent waves build upon — structured error codes, phase tracking callbacks, timeout configuration, provider signal support, and review failure types.

Output: Updated schema.ts, config.ts, provider types.ts, openai.ts, image-generation-service.ts, image-review-service.ts
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@openspec/changes/phase-431-generation-reliability-progress-ux/specs/generation-progress/spec.md
@openspec/changes/phase-431-generation-reliability-progress-ux/specs/structured-error-handling/spec.md
@openspec/changes/phase-431-generation-reliability-progress-ux/specs/generation-timeout/spec.md
@openspec/changes/phase-431-generation-reliability-progress-ux/specs/ai-image-generation/spec.md
@openspec/changes/phase-431-generation-reliability-progress-ux/specs/image-quality-review/spec.md
@openspec/changes/phase-431-generation-reliability-progress-ux/design.md

# Current code files (read before modifying)
@src/lib/image-generation/schema.ts
@src/lib/image-generation/config.ts
@src/lib/image-generation/providers/types.ts
@src/lib/image-generation/providers/openai.ts
@src/lib/image-generation/services/image-generation-service.ts
@src/lib/image-generation/services/image-review-service.ts
</context>

<action>
Modify src/lib/image-generation/schema.ts:
1. Add `GenerationPhase` type: `"input_validation" | "prompt_assembly" | "image_generation" | "quality_review" | "done"`
2. Add `GenerationPhaseStatus` type: `"pending" | "running" | "complete" | "skipped" | "failed"`
3. Add `GenerationPhaseEvent` interface with `phase`, `status`, `message?`, `detail?`
4. Replace existing error code types with full `GenerationErrorCode` union: `no_image_in_response | empty_review | insufficient_image | input_low_confidence | review_low_confidence | product_image_conflict | generated_product_mismatch | provider_error | provider_auth_error | provider_timeout | invalid_data | global_timeout`
5. Add `GenerationError` interface with `phase: string`, `code: GenerationErrorCode`, `message: string`, `detail?: string`, `httpStatus: number`, `retryable: boolean`, `requiresUserAction?: boolean`
6. Add `failureType?: "empty_review" | "insufficient_image" | "review_low_confidence" | "generated_product_mismatch"` to `ImageReviewResult`

Modify src/lib/image-generation/config.ts:
7. Add `IMAGE_GENERATION_GLOBAL_TIMEOUT_MS` constant defaulting to `300000`

Modify src/lib/image-generation/providers/types.ts:
8. Add `signal?: AbortSignal` to `ImageProviderInput`
9. Add `attempt?: number` to `ImageProviderInput`

Modify src/lib/image-generation/providers/openai.ts:
10. Pass `input.signal` (if present) to all OpenAI SDK calls as the request `signal` option
11. Use `input.attempt` to select provider path: attempt 0 = Responses API, attempt 1+ = Image API edit fallback

Modify src/lib/image-generation/services/image-generation-service.ts:
12. Accept optional `onPhaseChange: (event: GenerationPhaseEvent) => void` parameter in `generateImage()`
13. Accept optional `signal?: AbortSignal` parameter in `generateImage()`
14. Invoke `onPhaseChange` before and after each phase with appropriate status and PT-BR message
15. Pass `signal` through to `ImageProvider.generateImage()`
16. Catch abort errors and return `{ success: false, code: "global_timeout", message: "..." }`
17. Return typed `GenerationErrorCode` instead of generic strings on failures

Modify src/lib/image-generation/services/image-review-service.ts:
18. Return `failureType` field from review result
19. Map vision empty response to `failureType: "empty_review"`
20. Map insufficient quality to `failureType: "insufficient_image"`
21. Map low confidence to `failureType: "review_low_confidence"`
22. Map product name mismatch to `failureType: "generated_product_mismatch"`
23. Keep wrong_price/wrong_product_name/wrong_store_name as critical issues with `failureType: null`

<read_first>
- @openspec/changes/phase-431-generation-reliability-progress-ux/specs/structured-error-handling/spec.md — full error code union
- @openspec/changes/phase-431-generation-reliability-progress-ux/specs/generation-progress/spec.md — phase types and callback contract
- @openspec/changes/phase-431-generation-reliability-progress-ux/specs/generation-timeout/spec.md — timeout and AbortSignal requirements
- @openspec/changes/phase-431-generation-reliability-progress-ux/specs/image-quality-review/spec.md — review failureType requirements
</read_first>

<tasks>
- [ ] 1.1 Add GenerationPhase, GenerationPhaseStatus, GenerationPhaseEvent types to schema.ts
- [ ] 1.2 Add GenerationErrorCode union and GenerationError interface to schema.ts
- [ ] 1.3 Add failureType to ImageReviewResult in schema.ts
- [ ] 1.4 Add IMAGE_GENERATION_GLOBAL_TIMEOUT_MS to config.ts
- [ ] 1.5 Add signal and attempt to ImageProviderInput in types.ts
- [ ] 1.6 Wire AbortSignal through openai.ts provider calls
- [ ] 1.7 Wire attempt-based path selection in openai.ts
- [ ] 1.8 Add onPhaseChange callback parameter to ImageGenerationService.generateImage()
- [ ] 1.9 Add AbortSignal parameter to ImageGenerationService.generateImage()
- [ ] 1.10 Emit phase events before/after each pipeline step in service
- [ ] 1.11 Catch abort and return global_timeout error
- [ ] 1.12 Map all internal errors to typed GenerationErrorCode
- [ ] 1.13 Add failureType to ImageReviewService return
- [ ] 1.14 Map specific review outcomes to typed failureType
</tasks>

---
phase: 4.3.1-generation-reliability-progress-ux
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/components/flow/use-campaign-form.ts
  - src/components/flow/campaign-input-form.tsx
  - src/app/campaign/preview/page.tsx
  - src/components/flow/campaign-page-client.tsx
autonomous: true
requirements: [REND-01, REND-03, REND-04]

must_haves:
  truths:
    - "useCampaignForm consumes NDJSON stream via response.body.getReader() with line buffering"
    - "Partial chunks at line boundaries are buffered and completed on next chunk"
    - "Events are type-dispatched: phase→progress, result→navigate, error→display"
    - "Pre-stream HTTP 409 shows conflict dialog with options: correct/continue/cancel"
    - "In-stream generated_product_mismatch shows error without 'continue anyway' option"
    - "npm run build passes with zero errors"
  artifacts:
    - path: src/components/flow/use-campaign-form.ts
      provides: NDJSON stream consumer with line buffering and event dispatch
      contains: "getReader"
    - path: src/components/flow/campaign-input-form.tsx
      provides: Integration with GenerationProgress component
      pattern: "GenerationProgress"
---

<objective>
Implement client-side NDJSON stream consumption in useCampaignForm, with line buffering, event type-dispatching, and conflict/error dialogs.

Purpose: The client must consume the streaming endpoint safely, handling partial chunks and routing events to the correct UI components.

Output: Updated use-campaign-form.ts, campaign-input-form.tsx, campaign-page-client.tsx
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@openspec/changes/phase-431-generation-reliability-progress-ux/specs/ai-image-generation/spec.md
@openspec/changes/phase-431-generation-reliability-progress-ux/specs/structured-error-handling/spec.md
@openspec/changes/phase-431-generation-reliability-progress-ux/specs/campaign-preview-page/spec.md
@openspec/changes/phase-431-generation-reliability-progress-ux/design.md

# Current code files (read before modifying)
@src/components/flow/use-campaign-form.ts
@src/components/flow/campaign-input-form.tsx
@src/app/campaign/preview/page.tsx
@src/components/flow/campaign-page-client.tsx
</context>

<action>
Modify src/components/flow/use-campaign-form.ts:
1. Replace `fetch().then(res => res.json())` with stream consumer using `response.body.getReader()`
2. Implement line buffer: accumulate bytes in buffer, split by `\n`, parse complete lines, keep partial line for next chunk
3. Type-dispatch events:
   - `type: "phase"` → call `onPhaseChange` callback with event data
   - `type: "result"` → build PreviewPayload, store in sessionStorage, navigate to `/campaign/preview`
   - `type: "error"` → stop stream, set error state with event message, if `requiresUserAction` show conflict dialog
4. Handle pre-stream HTTP errors (400/409/413): read JSON body directly, show appropriate UI
5. For 409 with `reason: "product_image_conflict"` or `reason: "input_low_confidence"`: show conflict dialog with options: accept suggestion, type new name, or "Continuar mesmo assim" which sets `inputValidationOverride`
6. For in-stream `code: "generated_product_mismatch"`: show error dialog, do NOT offer "continue anyway" — user must correct product name or image
7. Preserve form state on all errors — do not clear fields

Modify src/components/flow/campaign-input-form.tsx:
8. Export `onPhaseChange` state and pass to `useCampaignForm`
9. Pass phase events to `GenerationProgress` component (will be created in plan 04)
10. Show conflict dialog when pendingConflict state is set (pre-stream 409)

<read_first>
- @openspec/changes/phase-431-generation-reliability-progress-ux/specs/ai-image-generation/spec.md — client streaming requirements
- @openspec/changes/phase-431-generation-reliability-progress-ux/specs/structured-error-handling/spec.md — chunk buffering scenarios
- @openspec/changes/phase-431-generation-reliability-progress-ux/specs/campaign-preview-page/spec.md — pre-stream 409 and in-stream error scenarios
- @src/components/flow/use-campaign-form.ts — current implementation
</read_first>

<tasks>
- [ ] 3.1 Replace JSON response with NDJSON stream consumer using getReader()
- [ ] 3.2 Implement line buffer for partial chunk handling
- [ ] 3.3 Dispatch phase events to onPhaseChange callback
- [ ] 3.4 Build PreviewPayload and navigate on result event
- [ ] 3.5 Handle error events from stream with message display
- [ ] 3.6 Handle pre-stream HTTP 409 with conflict dialog (correct/continue/cancel)
- [ ] 3.7 Handle in-stream generated_product_mismatch without continue-anyway option
- [ ] 3.8 Preserve form state on all errors
- [ ] 3.9 Wire onPhaseChange from campaign-input-form to useCampaignForm
</tasks>

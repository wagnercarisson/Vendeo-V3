---
phase: 4.3.1-generation-reliability-progress-ux
plan: 04
type: execute
wave: 2
depends_on: [03]
files_modified:
  - src/components/flow/generation-progress.tsx
  - src/components/flow/campaign-input-form.tsx
  - src/lib/campaign/schema.ts
autonomous: true
requirements: [REND-03, REND-04]

must_haves:
  truths:
    - "GenerationProgress is a visual component showing generation phases with animated indicators"
    - "Each phase shows: icon, localized label (PT-BR), animated spinner when running, check when complete, X when failed"
    - "Phases that complete in under 400ms are shown for at least 400ms before transitioning"
    - "Error state shows error card with code, message, retry button if retryable"
    - "originalPrice in CampaignInput is optional (?) instead of always required"
    - "npm run build passes with zero errors"
  artifacts:
    - path: src/components/flow/generation-progress.tsx
      provides: GenerationProgress React component
      contains: "GenerationProgress"
    - path: src/lib/campaign/schema.ts
      provides: originalPrice made optional
      pattern: "originalPrice"
---

<objective>
Create the GenerationProgress UI component and make originalPrice optional in CampaignInput schema.

Purpose: Users need real-time visibility into what's happening during generation. The progress component shows each phase (validation → prompt → generation → review → done) with status indicators. Making originalPrice optional removes an artificial friction point for stores without price history.

Output: New generation-progress.tsx, updated campaign-input-form.tsx, updated campaign/schema.ts
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@openspec/changes/phase-431-generation-reliability-progress-ux/specs/generation-progress/spec.md
@openspec/changes/phase-431-generation-reliability-progress-ux/specs/campaign-preview-page/spec.md
@openspec/changes/phase-431-generation-reliability-progress-ux/design.md

# Current code files
@src/components/flow/campaign-input-form.tsx
@src/lib/campaign/schema.ts
@src/lib/campaign/types.ts
</context>

<action>
Modify src/lib/campaign/schema.ts:
1. Change `originalPrice` from required to optional (`z.number().optional()` or `z.number().positive().optional()`)
2. Update types.ts to reflect optional originalPrice

Create src/components/flow/generation-progress.tsx:
3. New file exporting `GenerationProgress` component
4. Accept `phases: GenerationPhaseEvent[]` prop (ordered array of phase events)
5. Show each phase as a row with:
   - Icon: animated spinner for `running`, checkmark for `complete`, X for `failed`, dot for `pending/skipped`
   - Label: localized PT-BR (e.g., "Validando informações..." → "Validação concluída")
   - Subtle transition between status changes
6. Minimum display time (400ms): if a phase completes faster, keep it visible for at least 400ms before showing next
7. When all phases complete → show "Campanha gerada!" briefly then callback
8. On error: show error card below progress list with error code, message, and retry button if `retryable`
9. Style: dark overlay with centered card matching design system (shadcn/ui)
10. Use existing component patterns (avoid creating new UI primitives if shadcn equivalents exist)

Modify src/components/flow/campaign-input-form.tsx:
11. Import and render `GenerationProgress` when generation is in progress
12. Pass phases array from useCampaignForm state
13. Hide form fields during generation, show progress instead

<read_first>
- @openspec/changes/phase-431-generation-reliability-progress-ux/specs/generation-progress/spec.md — visual requirements and states
- @src/components/flow/campaign-input-form.tsx — where to integrate the progress component
- @src/lib/campaign/schema.ts — originalPrice location
</read_first>

<tasks>
- [ ] 4.1 Make originalPrice optional in campaign schema
- [ ] 4.2 Update campaign types to reflect optional originalPrice
- [ ] 4.3 Create GenerationProgress component with phase row layout
- [ ] 4.4 Implement animated status icons (spinner, check, X, dot)
- [ ] 4.5 Implement PT-BR localized labels for each phase
- [ ] 4.6 Implement minimum display time (400ms) for fast phases
- [ ] 4.7 Show completion state when all phases done
- [ ] 4.8 Show error card with code, message, retry button
- [ ] 4.9 Integrate GenerationProgress into campaign-input-form.tsx
- [ ] 4.10 Hide form fields during generation, show progress
</tasks>

---
phase: 4.3.1-generation-reliability-progress-ux
plan: 05
type: execute
wave: 2
depends_on: [03, 04]
files_modified:
  - src/components/flow/use-campaign-form.ts
  - src/hooks/use-input-preservation.ts
  - src/app/campaign/preview/page.tsx
autonomous: true
requirements: [REND-04]

must_haves:
  truths:
    - "Form input is preserved in sessionStorage on submit BEFORE fetch starts"
    - "On error/return, form is restored to exact state before submission"
    - "On success, sessionStorage is cleared when preview page mounts"
    - "Preview page gracefully handles optional originalPrice (displays '-' or hides field)"
    - "npm run build passes with zero errors"
  artifacts:
    - path: src/hooks/use-input-preservation.ts
      provides: sessionStorage save/restore hook
      contains: "sessionStorage"
    - path: src/components/flow/use-campaign-form.ts
      provides: Preservation workflow integration
      contains: "useInputPreservation"
---

<objective>
Implement form input preservation via sessionStorage and update preview page for optional originalPrice.

Purpose: Generation failures should not erase the user's work. By persisting form state to sessionStorage before submission, users can return to an identical form on error without losing any field. The preview page must handle optional originalPrice gracefully.

Output: New use-input-preservation.ts, updated use-campaign-form.ts, updated preview page
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@openspec/changes/phase-431-generation-reliability-progress-ux/specs/campaign-preview-page/spec.md
@openspec/changes/phase-431-generation-reliability-progress-ux/design.md

# Current code files
@src/components/flow/use-campaign-form.ts
@src/app/campaign/preview/page.tsx
@src/components/flow/campaign-input-form.tsx
</context>

<action>

Create src/hooks/use-input-preservation.ts:
1. New file exporting `useInputPreservation` hook
2. `saveFormState(key: string, state: CampaignInputState): void` — serializes to sessionStorage
3. `restoreFormState(key: string): CampaignInputState | null` — deserializes from sessionStorage
4. `clearFormState(key: string): void` — removes from sessionStorage
5. Use `sessionStorage` for persistence (survives page refresh within tab, cleared on tab close)
6. Handle JSON parse errors gracefully (return null for corrupt data)
7. Log a warning on parse failure without throwing

Modify src/components/flow/use-campaign-form.ts:
8. Before fetch/submit: call `saveFormState` with all current form values
9. On stream error event: mark preservation key for potential restore
10. After successful result navigation: do NOT clear yet — clear only after preview page confirms

Modify src/app/campaign/preview/page.tsx:
11. On mount, clear the preservation key from sessionStorage (generation succeeded, cleanup)
12. Make preview page render optional originalPrice: show formatted price if present, show "—" or hide if absent
13. Verify preview page does not crash when originalPrice is undefined/null

<read_first>
- @openspec/changes/phase-431-generation-reliability-progress-ux/specs/campaign-preview-page/spec.md — preview page requirements
- @src/components/flow/use-campaign-form.ts — where to integrate preservation
- @src/app/campaign/preview/page.tsx — where to clear preservation and handle optional field
</read_first>

<tasks>
- [ ] 5.1 Create useInputPreservation hook with sessionStorage save/restore/clear
- [ ] 5.2 Save form state before submission in useCampaignForm
- [ ] 5.3 Implement restore flow on error/return
- [ ] 5.4 Clear preservation on preview page mount
- [ ] 5.5 Update preview page to handle optional originalPrice
- [ ] 5.6 Verify no crash when originalPrice is undefined
</tasks>

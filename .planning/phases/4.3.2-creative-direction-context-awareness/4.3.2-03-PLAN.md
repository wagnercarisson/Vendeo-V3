---
phase: 4.3.2-creative-direction-context-awareness
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - openspec/changes/phase-432-creative-direction-context-awareness/tasks.md
autonomous: true
requirements: [CRE-01, CRE-02, CRE-03, CRE-04, CRE-05, CRE-06, ALI-01, ALI-02, ALI-03, ALI-04, ALI-05]

must_haves:
  truths:
    - "npm run typecheck passes with zero errors"
    - "npm run lint passes with zero errors"
    - "npm run build passes with zero errors"
    - "No database schema changes were made"
    - "All 6 verification scenarios produce expected behavior"
    - "tasks.md updated with completed items marked"
  artifacts:
    - path: openspec/changes/phase-432-creative-direction-context-awareness/tasks.md
      provides: Updated task checklist with all items marked
      contains: "- [x]"
---

<objective>
Verify that all Phase 4.3.2 changes work correctly through manual and automated validation.

Purpose: Confirm that category inference, creative direction context, validation-to-review alignment, and technical events all function as specified without regressions. Run all technical gates (typecheck, lint, build) and mark tasks complete.

Output: Verification results confirming all scenarios pass. Updated tasks.md with completed items checked.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@openspec/changes/phase-432-creative-direction-context-awareness/tasks.md
@openspec/changes/phase-432-creative-direction-context-awareness/design.md
@openspec/changes/phase-432-creative-direction-context-awareness/specs/creative-direction-context/spec.md
@openspec/changes/phase-432-creative-direction-context-awareness/specs/validation-review-alignment/spec.md
@openspec/changes/phase-432-creative-direction-context-awareness/specs/prompt-management/spec.md
@openspec/changes/phase-432-creative-direction-context-awareness/specs/ai-image-generation/spec.md
@openspec/changes/phase-432-creative-direction-context-awareness/specs/image-quality-review/spec.md
@openspec/changes/phase-432-creative-direction-context-awareness/specs/campaign-preview-page/spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run technical gates (typecheck, lint, build) + database change audit + update tasks.md</name>
  <files>
    (no files modified — verification only)
  </files>

  <read_first>
  @openspec/changes/phase-432-creative-direction-context-awareness/tasks.md — current task list to update
  @openspec/changes/phase-432-creative-direction-context-awareness/design.md — Decisions section for reference
  </read_first>

  <action>
  1. Run technical gates sequentially:
     - `npm run typecheck` — must pass with zero errors
     - `npm run lint` — must pass with zero errors (existing warnings are acceptable, but no new ones)
     - `npm run build` — must pass with zero errors

  2. If any gate fails:
     - Identify the root cause
     - Fix the issue
     - Re-run the gate
     - If the fix requires changes beyond the gate task scope, document the issue and flag for orchestrator

  3. Audit for database changes:
     - Run `git diff --name-only` and inspect all modified files
     - Confirm NO files under `prisma/` or `supabase/` or any `*migration*` pattern were modified
     - Confirm NO SQL files or database schema files were touched
     - All changes must be in-memory/configuration only (schema.ts, service files, prompts)

  4. Update tasks.md:
     - Open `openspec/changes/phase-432-creative-direction-context-awareness/tasks.md`
     - Mark ALL items in groups 1-4 and 6 as completed by changing `- [ ]` to `- [x]`
     - Leave group 5 (Verification) items to be marked after Tasks 2-3 in this plan
     - Verify that the change is properly saved

  5. Run OpenSpec validation if `openspec` CLI is available:
     - `openspec validate --change phase-432-creative-direction-context-awareness` (if the CLI exists and is on PATH)
     - If CLI is not available, skip with note
  </action>

  <acceptance_criteria>
  - `npm run typecheck` exits with code 0
  - `npm run lint` exits with code 0
  - `npm run build` exits with code 0
  - `git diff --name-only` shows no files under prisma/ or supabase/
  - tasks.md has groups 1, 2, 3, 4, 6 items marked [x]
  </acceptance_criteria>

  <verify>
    <automated>if (npm run typecheck 2>&1) { if (npm run lint 2>&1) { npm run build 2>&1 } }</automated>
  </verify>

  <done>
  Technical gates pass; database change audit confirms zero schema changes; tasks.md updated with groups 1-4 and 6 marked complete
  </done>
</task>

<task type="auto">
  <name>Task 2: Run verification scenarios (5.1-5.4) — technical details panel, category conflict, auto-fix alignment, override alignment</name>
  <files>
    (no files modified — verification only)
  </files>

  <read_first>
  @openspec/changes/phase-432-creative-direction-context-awareness/tasks.md — items 5.1-5.4
  @openspec/changes/phase-432-creative-direction-context-awareness/specs/creative-direction-context/spec.md — scenarios for inferredCategory, hasCategoryConflict
  @openspec/changes/phase-432-creative-direction-context-awareness/specs/validation-review-alignment/spec.md — scenarios for applyValidationContextToReviewResult
  @openspec/changes/phase-432-creative-direction-context-awareness/specs/campaign-preview-page/spec.md — technical details panel scenario
  </read_first>

  <action>
  Manually verify each scenario by code review and logical trace:

  SCENARIO 5.1: Technical details panel shows received detail events
  - Verify in image-generation-service.ts that:
    - input_validation emits detail with classification (and correction info when applicable)
    - prompt_assembly emits detail with persona and inferred category
    - image_generation emits detail per attempt with attempt number, model, elapsed time
    - quality_review emits detail with issue counts and failureType
    - done emits detail with total time, attempts, corrections
    - Each detail is a safe-by-construction string (no API keys, no base64, no stack traces)
  - Verify in generation-progress.tsx that:
    - buildDiagnostics() already handles `event.detail` (line 70-73) — no change needed
    - sanitizeDetail() is applied as defense-in-depth (already in place)

  SCENARIO 5.2: Category conflict detection
  - Verify isSameCategory() logic:
    - storeSegment="moda-vestuario", inferredCategory="energetico" → conflict=true
    - storeSegment="alimentacao-bebidas", inferredCategory="cafe" → conflict=false
    - storeSegment="moda-vestuario", inferredCategory="brinquedos" → conflict=false (conservative)
    - No inferredCategory provided → hasCategoryConflict=false (fallback to storeSegment)
  - Verify categoryConflictDirective is non-empty when conflict=true, empty when conflict=false
  - Verify creativePersona uses SEGMENT_LABELS mapping or raw segment key as fallback

  SCENARIO 5.3: Auto-fix alignment — review should not flag corrected name
  - Trace the auto-fix path:
    - ValidationResult.classification="auto-fix" → effectiveProductName set to corrected name
    - buildValidationSummary includes correction info in inputValidationSummary
    - reviewInput.productName = effectiveProductName (corrected name)
    - validationContext.inputCorrection populated with from/to/reason
    - In ImageReviewService.review(), {{validationContextSection}} rendered with correction info
    - The reviewer receives corrected name as ground truth → wrong_product_name only fires if art mismatches the corrected name
  - Verify: generated_product_mismatch still blocks even when auto-fix was applied (art shows completely wrong product)

  SCENARIO 5.4: Override alignment — review should not report product×image conflict
  - Trace the override path:
    - body.inputValidationOverride.productImageCheck === "user_confirmed_continue"
    - InputValidationService skips validation → returns { classification: "match", confidence: 1.0 }
    - validationContext.overrides.productImageCheck = "user_confirmed_continue"
    - In campaign-image-reviewer.md, {{validationContextSection}} includes override text
    - applyValidationContextToReviewResult() removes product_image_conflict and product_image_low_confidence from result
    - BUT: wrong_price, illegible_text, generated_product_mismatch, etc. remain unfiltered
  - Verify that applyValidationContextToReviewResult() only filters the two override types:
    - Code review the OVERRIDE_FILTERABLE_TYPES set = { "product_image_conflict", "product_image_low_confidence" }
    - Code review the BLOCKING_TYPES set includes all 12 non-override types
  </action>

  <acceptance_criteria>
  - All detail emissions verified: 5 phases with correct content patterns
  - Category conflict: isSameCategory logic traces correctly for all cases
  - Auto-fix: effectiveProductName flows to reviewInput, validationContext.inputCorrection populated
  - Override: applyValidationContextToReviewResult removes only the two allowed types
  - All existing GenPhaseEvent / GenerationProgress behavior preserved (backward compatible)
  </acceptance_criteria>

  <verify>
    <automated>grep -c "classificação:" src/lib/image-generation/services/image-generation-service.ts && grep -c "tentativa" src/lib/image-generation/services/image-generation-service.ts && grep -c "issues:" src/lib/image-generation/services/image-generation-service.ts && grep -c "geração concluída" src/lib/image-generation/services/image-generation-service.ts && grep -c "product_image_conflict" src/lib/image-generation/services/image-generation-service.ts && grep -c "product_image_low_confidence" src/lib/image-generation/services/image-generation-service.ts</automated>
  </verify>

  <done>
  Scenarios 5.1-5.4 verified: detail events flowing through pipeline; category conflict detected; auto-fix aligned to review; override filters only approved conflicts
  </done>
</task>

<task type="auto">
  <name>Task 3: Run verification scenarios (5.5-5.6) — generated_product_mismatch blocking, fallback behavior + finalize tasks.md</name>
  <files>
    openspec/changes/phase-432-creative-direction-context-awareness/tasks.md
  </files>

  <read_first>
  @openspec/changes/phase-432-creative-direction-context-awareness/tasks.md — items 5.5, 5.6 to mark after verification
  @openspec/changes/phase-432-creative-direction-context-awareness/specs/validation-review-alignment/spec.md — generated_product_mismatch scenario (page 4)
  @openspec/changes/phase-432-creative-direction-context-awareness/specs/creative-direction-context/spec.md — scenario: missing inferredCategory uses store segment fallback (page 2)
  </read_first>

  <action>
  SCENARIO 5.5: generated_product_mismatch must block even with override
  - Verify in applyValidationContextToReviewResult():
    - generated_product_mismatch is NOT in OVERRIDE_FILTERABLE_TYPES (only product_image_conflict and product_image_low_confidence)
    - generated_product_mismatch IS in BLOCKING_TYPES
    - When review returns { type: "generated_product_mismatch", severity: "critical" }, the issue survives filtering regardless of validationContext
    - The state machine on line 225 evaluates `reviewResult.failureType === "generated_product_mismatch"` and returns terminal error
  - Confirm: even with inputValidationOverride, generated_product_mismatch triggers generation failure

  SCENARIO 5.6: Fallback when inferredCategory not returned
  - Verify in InputValidationService.parseResult():
    - When model response has NO inferredCategory field, parsed.inferredCategory is undefined
    - The returned InputValidationResult has inferredCategory: undefined on the variant
  - Verify in generateImage():
    - `const inferredCategory = "inferredCategory" in validationResult ? ... : undefined;`
    - When inferredCategory is undefined, buildPromptVariables receives inferredCategory=undefined
    - In buildPromptVariables(): `const effectiveInferredCategory = inferredCategory ?? storeSegment;`
    - So effectiveInferredCategory = storeSegment
    - `const hasConflict = inferredCategory ? ... : false;`
    - So hasConflict = false
  - Verify creativePersona still uses SEGMENT_LABELS[storeSegment]
  - Verify all existing behavior is identical to pre-4.3.2 when inferredCategory is absent

  FINALIZE: Update tasks.md
  - Mark items 5.1-5.6 as completed (`- [x]`)
  - Verify all 6 task groups (49 items total) are marked completed
  - Save tasks.md
  </action>

  <acceptance_criteria>
  - generated_product_mismatch is NOT in OVERRIDE_FILTERABLE_TYPES set
  - generated_product_mismatch IS in BLOCKING_TYPES set
  - applyValidationContextToReviewResult does NOT filter generated_product_mismatch
  - When inferredCategory is undefined, storeSegment is used as fallback
  - hasCategoryConflict is false when inferredCategory is undefined
  - tasks.md has ALL 49 items in groups 1-6 marked [x]
  - git diff --cached or git status shows tasks.md as modified
  </acceptance_criteria>

  <verify>
    <automated>grep -c "generated_product_mismatch" src/lib/image-generation/services/image-generation-service.ts > 0 && grep "inferredCategory" src/lib/image-generation/services/image-generation-service.ts | grep -c "storeSegment" > 0</automated>
  </verify>

  <done>
  generated_product_mismatch verified as blocking even with override; inferredCategory fallback to storeSegment confirmed; tasks.md finalized with all 49 items completed
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification → code | Manual code review verifies correctness of implemented logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-432-07 | Tampering | verification process | mitigate | Multiple verification scenarios cover: override filtering limits (5.4), blocking types immunity (5.5), fallback behavior (5.6), and all technical gates |
| T-432-08 | Information Disclosure | tasks.md | accept | Internal planning file — no sensitive data exposed |
</threat_model>

<verification>
- [ ] Task 1: npm run typecheck, lint, build pass; no database changes; tasks.md groups 1-4, 6 marked
- [ ] Task 2: Scenarios 5.1-5.4 verified (detail panel, category conflict, auto-fix, override)
- [ ] Task 3: Scenarios 5.5-5.6 verified (generated_product_mismatch blocking, fallback); tasks.md fully updated
</verification>

<success_criteria>
- All technical gates pass (typecheck, lint, build)
- Zero database schema changes confirmed
- 6 verification scenarios all pass
- tasks.md has all 49 implementation items marked [x]
- Phase 4.3.2 ready for archive
</success_criteria>

<output>
Create `.planning/phases/4.3.2-creative-direction-context-awareness/4.3.2-03-SUMMARY.md` when done
</output>

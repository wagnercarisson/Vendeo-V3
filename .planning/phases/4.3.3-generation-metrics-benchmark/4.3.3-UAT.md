---
status: complete
phase: 4.3.3-generation-metrics-benchmark
source:
  - 4.3.3-01-SUMMARY.md
  - 4.3.3-02-SUMMARY.md
  - 4.3.3-03-SUMMARY.md
started: 2026-05-30T19:26:22Z
updated: 2026-05-30T19:26:22Z
---

## Current Test

[testing complete]

## Tests

### 1. Generation progress indicators
expected: UI shows validação → prompt → geração → revisão with animated states and human-friendly PT-BR messages. No technical terms in messages.
result: pass

### 2. "Ver etapas da geração" step viewer
expected: A collapsible panel labeled "Ver etapas da geração" exists, collapsed by default. When expanded, shows only phase names and completion status icons (check/error). NO provider, model, runId, cost, retry count, or other technical metrics appear.
result: pass

### 3. Creative context guidance in prompt
expected: The generated campaign art considers the store segment, inferred product category, and any category conflict. Example: food store selling energy drink gets positioning like "Valorize energia e disposição. Preço é oportunidade." — visible in the resulting art's tone and copy.
result: pass

### 4. Benchmark CLI runs with defaults
expected: `npx tsx scripts/benchmark.ts` runs 5 generations (default max-runs) using configured provider/model with 2s delay between runs. Each run prints: Run X/5 [SUCCESS|FAIL] duration cost. Final summary shows total/success/failure/avg duration/avg cost/provider/model.
result: pass

### 5. Benchmark CLI accepts all arguments
expected: `npx tsx scripts/benchmark.ts --provider openai --model gpt-4o --delay 1000 --max-runs 3` runs 3 times with specified provider/model and 1s delay.
result: pass

### 6. Benchmark summary table rendered
expected: After all benchmark runs complete, a formatted summary table displays: Total runs, Successful, Failed, Average duration, Average cost, Provider, Model.
result: pass

### 7. Metrics JSONL file created
expected: After a generation or benchmark run, a JSONL file exists in `metrics/` (e.g. `metrics/generation-2026-05-30.jsonl` or `metrics/benchmark-{timestamp}.jsonl`). File contains valid JSON lines with runId, provider, model, duration, and sanitized fields. No base64 images, full prompts, API keys, or raw payloads.
result: pass

### 8. Provider switch via IMAGE_PROVIDER env var
expected: Setting `IMAGE_PROVIDER=anthropic` in `.env.local` makes the system use Anthropic provider. Setting to an invalid value (e.g. `unsupported`) shows a clear error. Default (unset) uses OpenAI.
result: pass

## Summary

total: 8
passed: 8
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none yet]

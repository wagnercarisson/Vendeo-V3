# Plan 05 — Campaign Integration — Summary

**Phase:** 4.4.1 Existing Logo & Store Brand Direction Foundation
**Plan:** 05 — Campaign Integration
**Status:** Complete

## What Was Built

| File | Modification |
|------|-------------|
| `src/components/campaign/types.ts` | Added BrandProfileSnapshot interface + brandProfile field on StoreIdentitySnapshot |
| `src/lib/actions/store.ts` | Pre-resolve brand assets from store_brand_assets + store_brand_profiles, color resolution priority: brand_colors_chosen > safe_color_tokens > store.brand_color > segment fallback, logo variant: on_dark → original, visual signature suppressed when brand profile exists |
| `src/lib/image-generation/schema.ts` | Added optional brandProfile to GenerateImageRequestSchema |
| `src/lib/image-generation/services/image-generation-service.ts` | buildPromptVariables populates brand profile context; new buildBrandProfileSection method |
| `prompts/campaign-image-director.md` | Added "Perfil de Marca (Store Brand Director)" section with {{brandProfileSection}} variable |

## Key Details

- Color resolution: brand_colors_chosen[0] > safe_color_tokens.primary > store.brand_color > SEGMENT_PALETTES fallback
- Logo variant selection: on_dark (theme-aware) → original → legacy logo_url
- When brand profile exists → visualSignatureUrl is null (D-12 priority)
- When no synced profile → brandProfile is null, existing fallback unchanged
- Brand profile context is directional (D-09) — Campaign Director retains creative judgment
- Empty profile → brandProfileSection is empty string → prompt section invisible

## Verification

- [x] npm run typecheck passes
- [x] StoreIdentitySnapshot includes brandProfile field
- [x] resolveStoreIdentity pre-resolves brand assets from DB
- [x] All existing return blocks include brandProfile: null

## Commit

(Next commit)

# Plan 06 — Store Identity UI — Summary

**Phase:** 4.4.1 Existing Logo & Store Brand Direction Foundation
**Plan:** 06 — UI
**Status:** Complete

## What Was Built

| File | Modification |
|------|-------------|
| `src/components/flow/use-store-form.ts` | FormData extended with subsegment, tone_of_voice, positioning, short_description, slogan; save() sends new fields via PATCH |
| `src/components/flow/store-identity-form.tsx` | Drag-and-drop logo upload zone, 5MB limit, format info, circular preview, color swatches from brand profile, 5 new marketing direction fields, fixed upload URL to /api/store (was /api/stores) |
| `src/components/flow/store-identity-block.tsx` | Shows circular logo image when brandProfile.logoVariantUrl or logoUrl exists |
| `src/components/flow/store-preview.tsx` | Added optional logoUrl prop, shows circular logo image when provided |

## Key Details

- Logo upload: drag-and-drop or click, accepted formats info, client-side validation
- Status messages: "Enviando..." → "Processando..." → "Pronto"
- Color suggestion swatches from logo_colors_detected after brand profile created
- No conflict modal when user chooses different color (D-11)
- Fixed pre-existing bug: upload URL was `/api/stores/{id}/logo` (plural), now uses correct `/api/store/{id}/logo` (singular)
- MAX_LOGO_SIZE updated from 2MB to 5MB

## Verification

- [x] npm run typecheck passes
- [x] Store identity form has logo upload with drag-and-drop zone
- [x] Client-side validation rejects invalid formats and oversized files
- [x] Color suggestion swatches appear and are clickable
- [x] New fields (subsegment, tone_of_voice, positioning, short_description, slogan) all present

## Commit

(Next commit)

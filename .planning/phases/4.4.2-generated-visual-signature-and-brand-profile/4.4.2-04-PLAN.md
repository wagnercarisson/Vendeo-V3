---
phase: 4.4.2-generated-visual-signature-and-brand-profile
plan: 04
type: execute
wave: 2
depends_on:
  - 4.4.2-03-PLAN.md
files_modified:
  - src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts
  - src/app/api/store/[id]/visual-signature/reject/route.ts
  - src/app/api/store/[id]/visual-signature/approve/route.ts
  - src/app/api/store/[id]/brand-profile/generate-without-logo/route.ts
  - src/app/api/store/[id]/logo-status/route.ts
  - src/app/api/store/[id]/route.ts
autonomous: true
requirements:
  - INPT-04
  - REND-03
  - REND-05
must_haves:
  truths:
    - "POST /api/store/[id]/visual-signature/generate-without-logo generates 1 signature, increments attempts, returns asset + metadata"
    - "POST /api/store/[id]/visual-signature/reject archives asset, records rejection, does NOT increment attempts"
    - "POST /api/store/[id]/visual-signature/approve sets asset active, triggers brand profiler, updates store"
    - "POST /api/store/[id]/brand-profile/generate-without-logo creates brand profile from store data + art director outputs"
    - "PATCH /api/store/[id]/logo-status updates logo_status and visual_signature_attempts"
    - "PATCH /api/store/[id] accepts logo_status and visual_signature_attempts"
  artifacts:
    - path: "src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts"
      provides: "POST handler — generates 1 visual signature without logo"
      min_lines: 50
    - path: "src/app/api/store/[id]/visual-signature/reject/route.ts"
      provides: "POST handler — archives current signature, returns rejection context"
      min_lines: 50
    - path: "src/app/api/store/[id]/visual-signature/approve/route.ts"
      provides: "POST handler — approves signature, triggers brand profiler"
      min_lines: 60
    - path: "src/app/api/store/[id]/brand-profile/generate-without-logo/route.ts"
      provides: "POST handler — generates brand profile from store data + art director outputs"
      min_lines: 50
    - path: "src/app/api/store/[id]/logo-status/route.ts"
      provides: "PATCH handler — updates logo_status and attempts"
      min_lines: 30
  key_links:
    - from: "generate-without-logo/route.ts"
      to: "StoreIdentityArtDirectorService"
      via: "calls identityArtDirector.generate()"
    - from: "approve/route.ts"
      to: "BrandProfilerWithoutLogoService"
      via: "calls brandProfiler.generate() after approval"
    - from: "all routes"
      to: "generation-events.ts"
      via: "calls insertGenerationEvent / updateGenerationEventDecision"
---

<objective>
Implement all 6 API endpoints for Phase 4.4.2: visual signature generation, rejection, approval, brand profile generation, logo status update, and store PATCH extension.

**Purpose:** Expose the without-logo visual signature and brand profile flows via RESTful API routes following existing Next.js App Router patterns.
**Output:** 5 new API route files + 1 modified store route
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/4.4.2-generated-visual-signature-and-brand-profile/4.4.2-CONTEXT.md
@openspec/changes/phase-4-4-2-generated-visual-signature-and-brand-profile/design.md

<interfaces>
Existing API route pattern (Next.js App Router):
```typescript
import { NextRequest, NextResponse } from 'next/server';
import { supabaseAdmin as supabase } from '@/lib/supabase/server';

export async function POST(request: NextRequest, { params }: { params: Promise<{ id: string }> }) {
  const { id } = await params;
}
```

Existing routes at `src/app/api/store/[id]/`:
- route.ts — GET (read store), PATCH (update store)
- logo/route.ts — POST (upload), GET, DELETE
- logo/versions/route.ts — GET
- brand-profile/route.ts — GET
- brand-profile/generate/route.ts — POST
- brand-profile/colors/route.ts — PATCH
- brand-profile/archive/route.ts — POST

Existing `src/lib/visual-signature/persistence.ts` exports:
- getActiveVisualSignature(storeId)
- getVisualSignaturesByStoreId(storeId) — needs verification

New services from Plan 03:
- StoreIdentityArtDirectorService.generate(input)
- BrandProfilerWithoutLogoService.generate(input)
- insertGenerationEvent(event)
- updateGenerationEventDecision(assetId, attemptNumber, decision)
</interfaces>
</context>

<tasks>

<task type="auto">
<name>Task 1: Create generate-without-logo endpoint</name>
<files>src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts</files>
<read_first>
src/app/api/store/[id]/brand-profile/generate/route.ts (pattern for AI generation endpoints)
src/lib/visual-signature/identity-art-director.ts (service to call)
src/lib/visual-signature/types.ts (VisualSignatureWithoutLogoInput)
openspec/changes/phase-4-4-2-generated-visual-signature-and-brand-profile/design.md (D-6 API spec)
</read_first>
<action>
Create route at `src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts`:

POST handler:
1. Validate storeId as UUID
2. Read body: { rejectionContext?: { reason: string; attempt: number } }
3. Fetch store data from DB (name, segment, subsegment, tone_of_voice, positioning, short_description, slogan, city, state, brand_color)
4. Increment visual_signature_attempts on stores table (current attempts + 1)
5. Call StoreIdentityArtDirectorService.generate() with store data + optional rejection context
6. On success: persist signature as draft, record generation event (type=visual_signature, status=success), return { assetUrl, artDirectorOutput, attempt }
7. On error: set logo_status='failed' if first attempt, record generation event (type=visual_signature, status=failed), return controlled error message
8. If attempts >= 3: return { exhausted: true, signatures: [...all 3 archived signatures for re-evaluation] }

Response shape:
```json
{
  "success": true,
  "assetUrl": "https://...",
  "signatureId": "uuid",
  "artDirectorOutput": { "creative_description": "...", "suggested_colors": [...], "visual_direction": "...", "elements_used": [...] },
  "attempt": 1,
  "maxAttempts": 3
}
```
</action>
<verify>
<automated>npm run typecheck</automated>
</verify>
<acceptance_criteria>
- POST handler at /api/store/[id]/visual-signature/generate-without-logo
- Validates storeId as UUID
- Fetches store data from DB
- Increments visual_signature_attempts
- Calls StoreIdentityArtDirectorService.generate()
- On success: returns assetUrl + artDirectorOutput + attempt number
- Records generation event (type=visual_signature)
- On error: returns controlled error message, does NOT generate typographic fallback
- If attempts >= 3 at start: returns exhausted=true with all 3 archived signatures
- TypeScript compiles with zero errors
</acceptance_criteria>
</task>

<task type="auto">
<name>Task 2: Create reject endpoint</name>
<files>src/app/api/store/[id]/visual-signature/reject/route.ts</files>
<read_first>
src/lib/visual-signature/persistence.ts (getActiveVisualSignature or similar)
openspec/changes/phase-4-4-2-generated-visual-signature-and-brand-profile/design.md (D-6 reject spec)
openspec/changes/phase-4-4-2-generated-visual-signature-and-brand-profile/specs/visual-signature-approval/spec.md (rejection flow)
</read_first>
<action>
Create route at `src/app/api/store/[id]/visual-signature/reject/route.ts`:

POST handler:
1. Validate storeId as UUID
2. Read body: { reason?: string } (optional feedback from lojista)
3. Fetch current attempt from stores.visual_signature_attempts
4. Find latest draft/active signature for this store (by store_id, status='draft', order by created_at desc)
5. Update that signature's status to 'archived', metadata: { rejected: true, reason, attempt_number }
6. Update generation_events: set rejected=true for matching asset_id + attempt_number
7. Do NOT increment visual_signature_attempts (increment happens at generation time)
8. If current attempts >= 3: return { exhausted: true, signatures: all archived signatures, message: "Limite de 3 versões atingido" }
9. Return rejection context for next generation

Response shape:
```json
{
  "success": true,
  "rejectionContext": { "reason": "feedback or default message", "attempt": 1 },
  "exhausted": false
}
```
</action>
<verify>
<automated>npm run typecheck</automated>
</verify>
<acceptance_criteria>
- POST handler at /api/store/[id]/visual-signature/reject
- Archives current draft signature with rejected=true + reason + attempt_number
- Updates generation_events with rejected=true
- Does NOT increment visual_signature_attempts
- If attempts < 3: returns rejectionContext for next generation
- If attempts >= 3: returns exhausted=true with all archived signatures
- TypeScript compiles with zero errors
</acceptance_criteria>
</task>

<task type="auto">
<name>Task 3: Create approve endpoint</name>
<files>src/app/api/store/[id]/visual-signature/approve/route.ts</files>
<read_first>
src/lib/visual-signature/brand-profiler.ts (BrandProfilerWithoutLogoService)
src/lib/visual-signature/persistence.ts (pattern for updating signature status)
openspec/changes/phase-4-4-2-generated-visual-signature-and-brand-profile/design.md (D-6 approve spec, D-10 persistence)
openspec/changes/phase-4-4-2-generated-visual-signature-and-brand-profile/specs/visual-signature-approval/spec.md (approval flow)
</read_first>
<action>
Create route at `src/app/api/store/[id]/visual-signature/approve/route.ts`:

POST handler:
1. Validate storeId as UUID
2. Read body: { signatureId: string }
3. Update the approved signature: status='active' in store_visual_signatures
4. Archive any previously active signature (if exists)
5. Update stores: logo_status='generated', visual_signature_attempts=0
6. Update generation_events: set approved=true for matching asset_id + attempt_number
7. Trigger BrandProfilerWithoutLogoService.generate() with store data + art director outputs from the approved signature
8. On brand profiler success: brand_profile created with source='without_logo', status='synced'
9. On brand profiler failure: brand_profile created with status='failed', signature remains active, previous profile unchanged
10. Return updated store state + brand profile

Response shape:
```json
{
  "success": true,
  "signature": { "id": "uuid", "assetUrl": "https://...", "status": "active" },
  "brandProfile": { "id": "uuid", "status": "synced" },
  "inferredPrimaryColor": "#HEX",
  "inferredAccentColor": "#HEX",
  "logoStatus": "generated"
}
```
</action>
<verify>
<automated>npm run typecheck</automated>
</verify>
<acceptance_criteria>
- POST handler at /api/store/[id]/visual-signature/approve
- Sets signature to active
- Archives previous active signature
- Updates stores: logo_status='generated', visual_signature_attempts=0
- Updates generation_events with approved=true
- Triggers BrandProfilerWithoutLogoService.generate()
- On profile success: returns brand profile data
- On profile failure: signature still active, profile has status='failed'
- Returns inferredPrimaryColor and inferredAccentColor from brand profile or art director output
- TypeScript compiles with zero errors
</acceptance_criteria>
</task>

<task type="auto">
<name>Task 4: Create brand-profile/generate-without-logo endpoint</name>
<files>src/app/api/store/[id]/brand-profile/generate-without-logo/route.ts</files>
<read_first>
src/app/api/store/[id]/brand-profile/generate/route.ts (pattern for profile generation endpoints)
src/lib/visual-signature/brand-profiler.ts (BrandProfilerWithoutLogoService)
openspec/changes/phase-4-4-2-generated-visual-signature-and-brand-profile/specs/store-brand-profile/spec.md (generate-without-logo endpoint spec)
</read_first>
<action>
Create route at `src/app/api/store/[id]/brand-profile/generate-without-logo/route.ts`:

POST handler:
1. Validate storeId as UUID
2. Read body: { visualSignatureId: string, artDirectorOutput: VisualSignatureArtDirectorOutput, assetUrl: string, referenceCardUrl?: string }
3. Fetch store data from DB
4. Call BrandProfilerWithoutLogoService.generate() with combined input
5. On success: return brand profile with status='synced'
6. On failure: return brand profile with status='failed', error details

Response shape:
```json
{
  "success": true,
  "brandProfile": { "id": "uuid", "status": "synced", "source": "without_logo" }
}
```
</action>
<verify>
<automated>npm run typecheck</automated>
</verify>
<acceptance_criteria>
- POST handler at /api/store/[id]/brand-profile/generate-without-logo
- Accepts visualSignatureId + artDirectorOutput + assetUrl in body
- Fetches store data from DB
- Calls BrandProfilerWithoutLogoService.generate()
- On success: returns brand profile with source='without_logo', status='synced'
- On failure: returns brand profile with status='failed'
- TypeScript compiles with zero errors
</acceptance_criteria>
</task>

<task type="auto">
<name>Task 5: Create logo-status endpoint + extend store PATCH</name>
<files>
src/app/api/store/[id]/logo-status/route.ts,
src/app/api/store/[id]/route.ts
</files>
<read_first>
src/app/api/store/[id]/route.ts (existing PATCH handler to extend)
openspec/changes/phase-4-4-2-generated-visual-signature-and-brand-profile/specs/store-identity-foundation/spec.md (logo_status in PATCH)
</read_first>
<action>
Create two changes:

**A) New file: `src/app/api/store/[id]/logo-status/route.ts`**

PATCH handler:
1. Validate storeId as UUID
2. Read body: { logo_status: string, visual_signature_attempts?: number }
3. Validate logo_status against allowed values
4. Update stores table with new values
5. Return updated store

**B) Modify existing `src/app/api/store/[id]/route.ts`**

Extend the existing PATCH handler to accept the new fields:
- Accept `logo_status` (TEXT) and `visual_signature_attempts` (INTEGER) in the request body
- Include them in the update set when present
- Validate logo_status against allowed values if provided
</action>
<verify>
<automated>npm run typecheck</automated>
</verify>
<acceptance_criteria>
- PATCH /api/store/[id]/logo-status accepts { logo_status, visual_signature_attempts? }
- Validates logo_status against allowed values
- PATCH /api/store/[id] accepts logo_status and visual_signature_attempts
- Both return updated store data
- TypeScript compiles with zero errors
</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| HTTP request → API handler | User-provided body parsed in handlers |
| API handler → Supabase | Data written to store_visual_signatures, stores, store_brand_profiles |
| API handler → AI service | Store data passed to LLM for generation |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-4.4.2-04-01 | Spoofing | storeId UUID | mitigate | UUID validation in all route handlers |
| T-4.4.2-04-02 | Tampering | logo_status validation | mitigate | Only accept values in allowed enum set |
| T-4.4.2-04-03 | Denial of Service | AI generation timeout | mitigate | Use existing timeout/retry from AiImageGenerator |
| T-4.4.2-04-04 | Tampering | Generation events data | mitigate | Best-effort insert — never blocks generation |
</threat_model>

<verification>
- [ ] POST generate-without-logo returns asset + metadata
- [ ] POST reject archives current asset, does not increment attempts
- [ ] POST approve sets active, triggers brand profiler, resets attempts
- [ ] POST brand-profile/generate-without-logo creates profile with source='without_logo'
- [ ] PATCH logo-status updates store fields
- [ ] PATCH /api/store/[id] accepts new fields
- [ ] All endpoints validate storeId as UUID
- [ ] TypeScript typecheck passes
</verification>

<success_criteria>
- 5 new API route files created
- 1 existing route extended
- All endpoints follow existing Next.js App Router patterns
- All endpoints have UUID validation
- TypeScript typecheck passes
</success_criteria>

<output>
Create `.planning/phases/4.4.2-generated-visual-signature-and-brand-profile/4.4.2-04-SUMMARY.md` when done
</output>

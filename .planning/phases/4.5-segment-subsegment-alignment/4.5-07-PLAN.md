---
phase: 4.5-segment-subsegment-alignment
plan: 07
type: execute
wave: 4
depends_on:
  - 01
files_modified:
  - src/components/flow/store-identity-block.tsx
  - src/components/flow/store-preview.tsx
autonomous: true
must_haves:
  truths:
    - "store-identity-block.tsx resolves labels from STORE_SEGMENTS"
    - "store-preview.tsx resolves labels and fallback colors from new segment values"
  artifacts:
    - path: "src/components/flow/store-identity-block.tsx"
      provides: "Updated label resolution with STORE_SEGMENTS"
    - path: "src/components/flow/store-preview.tsx"
      provides: "Updated label and fallback color resolution"
---

<objective>
Atualizar componentes auxiliares de UI: `store-identity-block.tsx` (resolução de label) e `store-preview.tsx` (resolução de label + fallback de cor) para usar `STORE_SEGMENTS` e os novos fallbacks.

**Output:** 2 componentes atualizados com resolução compatível com os 13 novos segmentos
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/4.5-segment-subsegment-alignment/4.5-CONTEXT.md
@openspec/changes/phase-4-5-segment-subsegment-alignment/design.md
@openspec/changes/phase-4-5-segment-subsegment-alignment/specs/store-identity-ui/spec.md
</context>

<tasks>

<task type="auto">
<name>Task 1: Update store-identity-block.tsx</name>
<files>src/components/flow/store-identity-block.tsx</files>
<read_first>
src/components/flow/store-identity-block.tsx (current label resolution logic)
</read_first>
<action>
In `src/components/flow/store-identity-block.tsx`:

1. Replace import of `SEGMENT_LABELS` with `STORE_SEGMENTS` from `@/lib/constants`

2. Update label resolution:
```typescript
// Before (old pattern):
const segmentLabel = SEGMENT_LABELS[segment as keyof typeof SEGMENT_LABELS] ?? segment;

// After (new pattern):
const segmentEntry = STORE_SEGMENTS.find(s => s.value === segment);
const segmentLabel = segmentEntry?.label ?? segment;
```

3. If the component renders subsegment, ensure it displays the subsegment value directly (no label resolution needed for subsegment — it's already stored as the text/label value).
</action>
<verify>
<automated>npm run typecheck</automated>
</verify>
<acceptance_criteria>
- No import of SEGMENT_LABELS
- Imports STORE_SEGMENTS from @/lib/constants
- Label resolution works for all 13 segments
- TypeScript compiles with zero errors
</acceptance_criteria>
</task>

<task type="auto">
<name>Task 2: Update store-preview.tsx</name>
<files>src/components/flow/store-preview.tsx</files>
<read_first>
src/components/flow/store-preview.tsx (current label + fallback color resolution)
src/lib/store.ts (SEGMENT_COLOR_FALLBACK export)
</read_first>
<action>
In `src/components/flow/store-preview.tsx`:

1. Replace import of `SEGMENT_LABELS` with `STORE_SEGMENTS` from `@/lib/constants`

2. Update label resolution using `STORE_SEGMENTS.find()` pattern (same as block component)

3. If the component uses `SEGMENT_COLOR_FALLBACK` from `@/lib/store`, ensure the import references the updated constant (already updated in plan 02). The fallback should work automatically since the keys now match the new segment values.

4. If there's any hardcoded segment-to-color mapping, replace it with a lookup from `SEGMENT_COLOR_FALLBACK` or `STORE_SEGMENTS` based resolution.
</action>
<verify>
<automated>npm run typecheck</automated>
</verify>
<acceptance_criteria>
- No import of SEGMENT_LABELS
- Imports STORE_SEGMENTS from @/lib/constants
- Label resolution works for all 13 segments
- Fallback color resolution works for all 13 segments
- TypeScript compiles with zero errors
</acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] store-identity-block.tsx uses STORE_SEGMENTS for label resolution
- [ ] store-preview.tsx uses STORE_SEGMENTS for label resolution
- [ ] store-preview.tsx fallback colors work with new segments
- [ ] TypeScript compiles with zero errors
</verification>

<success_criteria>
- Both components resolve labels correctly for all 13 new segments
- Preview shows correct fallback colors per segment
- TypeScript typecheck passes with zero errors
</success_criteria>

<output>
Create `.planning/phases/4.5-segment-subsegment-alignment/4.5-07-SUMMARY.md` when done
</output>

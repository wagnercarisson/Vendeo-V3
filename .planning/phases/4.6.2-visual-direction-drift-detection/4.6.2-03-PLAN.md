---
phase: 4.6.2-visual-direction-drift-detection
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - src/components/flow/drift-banner.tsx
  - src/components/flow/drift-discreet-button.tsx
  - src/components/flow/drift-campaign-modal.tsx
  - src/components/flow/store-identity-form.tsx
  - src/components/flow/campaign-page-client.tsx
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "Drift banner renders at top of Step 2 when driftStatus is 'new', with Realinhar and Manter buttons"
    - "Realinhar triggers re-inference via useDriftDetection.realinhar()"
    - "Manter triggers dismiss via useDriftDetection.ignorar() and banner becomes discreet button"
    - "Discreet button renders when driftStatus is 'dismissed', click triggers realinhar"
    - "Drift campaign modal appears before Gerar Campanha when driftStatus is 'new' or 'dismissed'"
    - "Modal has Realinhar direção visual and Gerar campanha mesmo assim buttons"
    - "Modal not shown when driftStatus is 'none'"
  artifacts:
    - path: "src/components/flow/drift-banner.tsx"
      provides: "Drift banner with Realinhar and Manter buttons"
    - path: "src/components/flow/drift-discreet-button.tsx"
      provides: "Discreet text link for dismissed drift"
    - path: "src/components/flow/drift-campaign-modal.tsx"
      provides: "Full-screen modal blocking Gerar Campanha with drift"
    - path: "src/components/flow/store-identity-form.tsx"
      provides: "Modified Step 2 form with drift banner/button integration"
    - path: "src/components/flow/campaign-page-client.tsx"
      provides: "Modified campaign page with drift check before generation"
  key_links:
    - from: "drift-banner.tsx"
      to: "use-drift-detection.ts"
      via: "consumes driftStatus, realinhar, ignorar, isRealinhando"
    - from: "drift-campaign-modal.tsx"
      to: "use-drift-detection.ts"
      via: "consumes same hook for drift check"
    - from: "campaign-page-client.tsx"
      to: "use-drift-detection.ts"
      via: "imports hook, checks driftStatus before triggering generation"
---

<objective>
**Plan 03: UI Components — Drift Banner, Discreet Button, Campaign Modal + Integration**

Create the three drift UI components and integrate them into the existing Step 2 form and campaign page. The banner and discreet button live inside `store-identity-form.tsx`, while the campaign modal intercepts the Gerar Campanha action in `campaign-page-client.tsx`.

**Purpose:** Deliver the user-visible drift detection experience described in the UX flow (Gatilhos 1-5).
**Output:** Three new components, two modified views
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/4.6.2-visual-direction-drift-detection/4.6.2-CONTEXT.md
@openspec/changes/fase-4-6-2-visual-direction-drift-detection/design.md
@openspec/changes/fase-4-6-2-visual-direction-drift-detection/specs/store-identity-ui/spec.md
@openspec/changes/fase-4-6-2-visual-direction-drift-detection/tasks.md
@src/components/flow/store-identity-form.tsx
@src/components/flow/campaign-page-client.tsx
@src/components/flow/use-drift-detection.ts
@src/lib/drift.ts

<existing_patterns>
See src/components/flow/store-identity-form.tsx for existing Tailwind dark mode palette (bg-deep, bg-surface, text-primary, text-secondary, text-muted, accent-amber, accent-blue, accent-green, accent-red, border-border, border-border-light)
See src/components/flow/campaign-input-form.tsx for existing loading/error toast patterns
See src/components/flow/visual-signature-approval-modal.tsx for existing modal overlay pattern (semi-transparent backdrop, buttons)
</existing_patterns>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create drift-banner.tsx and drift-discreet-button.tsx components</name>
  <files>
    <file>src/components/flow/drift-banner.tsx</file>
    <file>src/components/flow/drift-discreet-button.tsx</file>
  </files>
  <read_first>
    @src/components/flow/store-identity-form.tsx — existing Tailwind palette classes (accent-amber, bg-deep, text-text-muted, etc.)
    @04.6.2-CONTEXT.md — D6 (UX flow: banner/button), D7 (Realinhamento message)
    @openspec/changes/fase-4-6-2-visual-direction-drift-detection/specs/store-identity-ui/spec.md — Banner component requirement, Discreet button requirement
  </read_first>
  <action>
    **Create `src/components/flow/drift-banner.tsx`:**

    Props interface:
    ```typescript
    interface DriftBannerProps {
      onRealinhar: () => Promise<void>
      onIgnorar: () => Promise<void>
      isLoading: boolean
    }
    ```

    Component:
    - Renders when called (parent controls visibility based on driftStatus)
    - Uses `bg-deep` background, `border border-border` border, `rounded-lg`, padding
    - Display message: "A direção visual da sua loja pode estar desatualizada. Você alterou dados importantes depois da última análise."
    - Two buttons:
      - **Realinhar direção visual**: `bg-accent-amber` primary button, disabled when `isLoading`, shows `Loader2` spinner when loading. Calls `onRealinhar()` on click.
      - **Manter direção visual atual**: outline/secondary button (`border border-border-light`, no bg). Calls `onIgnorar()` on click.
    - No emojis, no icons (except spinner during loading)
    - Positioned as a full-width block (no absolute/fixed positioning — parent handles layout)
    - Use `<p>` for text, not heading elements

    **Create `src/components/flow/drift-discreet-button.tsx`:**

    Props interface:
    ```typescript
    interface DriftDiscreetButtonProps {
      onClick: () => Promise<void>
      isLoading: boolean
    }
    ```

    Component:
    - Renders as a simple text button/link element
    - Display: "Direção visual pode estar desatualizada" (in `<span>` or `<button>`)
    - Style: `text-text-muted` color, `text-xs` font, underline, no background
    - `cursor-pointer`, `hover:text-text-primary` transition
    - Disabled when `isLoading` — shows `<Loader2 className="w-3 h-3 animate-spin inline" />`
    - No emojis, no icons (except inline spinner during loading)
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | Select-String -NotMatch "node_modules"</automated>
  </verify>
  <acceptance_criteria>
    - `drift-banner.tsx` exports component with `onRealinhar`, `onIgnorar`, `isLoading` props
    - Banner text reads "A direção visual da sua loja pode estar desatualizada. Você alterou dados importantes depois da última análise."
    - "Realinhar direção visual" button calls `onRealinhar`, shows spinner when loading
    - "Manter direção visual atual" button calls `onIgnorar`
    - Both buttons disabled when `isLoading`
    - `drift-discreet-button.tsx` exports component with `onClick`, `isLoading` props
    - Discreet text reads "Direção visual pode estar desatualizada" with muted style
    - Loading shows inline spinner
    - TypeScript compilation passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create drift-campaign-modal.tsx component</name>
  <files>
    <file>src/components/flow/drift-campaign-modal.tsx</file>
  </files>
  <read_first>
    @src/components/flow/visual-signature-approval-modal.tsx — existing modal pattern (overlay, backdrop, close/action buttons layout)
    @openspec/changes/fase-4-6-2-visual-direction-drift-detection/specs/store-identity-ui/spec.md — Gerar Campanha drift modal requirement: full-screen overlay, no close button, no backdrop dismiss, two buttons
  </read_first>
  <action>
    Create `src/components/flow/drift-campaign-modal.tsx`:

    Props interface:
    ```typescript
    interface DriftCampaignModalProps {
      open: boolean
      onRealinhar: () => Promise<void>
      onContinue: () => void
      isRealinhando: boolean
    }
    ```

    Component:
    - If `open` is false, return `null`
    - Full-screen fixed overlay: `fixed inset-0 z-50 flex items-center justify-center`
    - Semi-transparent dark backdrop: `bg-black/60` (click does nothing — no backdrop dismiss)
    - Modal card: `bg-bg-deep rounded-xl border border-border p-6 max-w-md w-full mx-4 shadow-2xl`
    - Title: "A direção visual da sua loja foi alterada desde a última campanha. Deseja atualizar antes de gerar?" in `text-text-primary font-heading font-semibold text-lg` (no emojis)
    - No close button (X), no icon in title
    - Two buttons in a flex row with gap:
      - **Realinhar direção visual** (primary): `bg-accent-amber` style, calls `onRealinhar` on click, disabled when `isRealinhando`, shows spinner when loading
      - **Gerar campanha mesmo assim** (secondary/outline): `border border-border-light` style, calls `onContinue` on click (not async, just a callback), always enabled
    - After `onRealinhar` succeeds, the parent should close the modal and proceed to generation
    - After `onRealinhar` fails, modal stays open (parent shows error toast)
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | Select-String -NotMatch "node_modules"</automated>
  </verify>
  <acceptance_criteria>
    - Component exported with `open`, `onRealinhar`, `onContinue`, `isRealinhando` props
    - Returns `null` when `open` is false
    - Full-screen overlay with dark backdrop
    - Title text matches spec: "A direção visual da sua loja foi alterada desde a última campanha. Deseja atualizar antes de gerar?"
    - No close button, no emojis
    - Two buttons: "Realinhar direção visual" (primary, disabled when realinhando) and "Gerar campanha mesmo assim" (outline, always enabled)
    - Realinhar button shows spinner when isRealinhando
    - TypeScript compilation passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Integrate drift components into store-identity-form.tsx and campaign-page-client.tsx</name>
  <files>
    <file>src/components/flow/store-identity-form.tsx</file>
    <file>src/components/flow/campaign-page-client.tsx</file>
  </files>
  <read_first>
    @src/components/flow/store-identity-form.tsx — full component: step 2 section, current state variables, useEffect for loading store data, existing inference and profile handling
    @src/components/flow/campaign-page-client.tsx — full component: store loading, identity resolution, renders CampaignInputForm
    @src/components/flow/use-store-form.ts — UseStoreFormReturn with new colorDirtyState methods
    @openpspec/changes/fase-4-6-2-visual-direction-drift-detection/specs/store-identity-ui/spec.md — Drift hook on Step 2 mount, beforeunload behavior, banner/discreet button positioning
  </read_first>
  <action>
    **Modify `src/components/flow/store-identity-form.tsx`:**

    1. Add imports at top:
       - `import { useDriftDetection } from "./use-drift-detection"`
       - `import { DriftBanner } from "./drift-banner"`
       - `import { DriftDiscreetButton } from "./drift-discreet-button"`

    2. Inside the component, after existing hooks (`useStoreForm()`, etc.):
       - Build a `profile` object from the existing `inferredProfile` state that matches what `useDriftDetection` expects: `{ brand_colors_chosen, safe_color_tokens, inferred_accent_color, metadata }`
       - Build a `store` object from `formData` + `storeId` for the hook: `{ id: storeId, segment: formData.segment, subsegment: formData.subsegment, tone_of_voice: formData.tone_of_voice, name: formData.name, brand_color: formData.brand_color }`
       - Call `useDriftDetection(store, profile)` to get `{ driftStatus, currentSnapshot, realinhar, ignorar, isRealinhando }`
       - Pass the store as `null` when `storeId` is null (create mode)

    3. In the Step 2 rendered section (the `lg:col-span-3` area), add drift components:

       **Inside the Step 2 wrapper div (below the heading block, above `<form>`):**
       - When `driftStatus === 'new'`: render `<DriftBanner onRealinhar={realinhar} onIgnorar={ignorar} isLoading={isRealinhando} />`
       - When `driftStatus === 'dismissed'`: render `<DriftDiscreetButton onClick={realinhar} isLoading={isRealinhando} />`
       - When `driftStatus === 'none'`: render nothing

       Position is between the heading ("Logo e Cores") and the form start. Add `mb-4` spacing.

    4. Handle errors from `realinhar()` and `ignorar()` — use the existing `setInferenceError` state with a toast-like pattern:
       - Wrap the `realinhar` call to catch errors and set a local state `driftError` (string | null)
       - Display `driftError` as a small red text below the banner when set

    **Modify `src/components/flow/campaign-page-client.tsx`:**

    1. Add imports:
       - `import { useDriftDetection } from "./use-drift-detection"`
       - `import { DriftCampaignModal } from "./drift-campaign-modal"`
       - `import type { BrandProfileRecord } from "@/lib/brand-assets/types"`

    2. Add state:
       - `const [showDriftModal, setShowDriftModal] = useState(false)`
       - `const [brandProfile, setBrandProfile] = useState<Pick<BrandProfileRecord, 'brand_colors_chosen' | 'safe_color_tokens' | 'inferred_accent_color' | 'metadata'> | null>(null)`

    3. In the store loading `useEffect` (where store data is fetched):
       - After fetching store data, also fetch the brand profile: `fetch(/api/store/${storeId}/brand-profile)`
       - If response is OK and JSON body exists with status 'synced', set `brandProfile`

    4. Add `useDriftDetection(store, brandProfile)` call — extract `driftStatus`, `realinhar`, `isRealinhando`

    5. Create a `handleGenerateCampaign` callback instead of directly passing `storeIdentity` to `CampaignInputForm`:
       - The campaign-page-client wraps the `CampaignInputForm` — currently it passes `storeIdentity` directly
       - Wrap generation initiation: before showing the `CampaignInputForm` (which already has the submit button), intercept by adding a state `shouldGenerate` boolean
       - Alternative simpler approach: Add a `generateCampaign` state boolean. When user wants to generate, if `driftStatus !== 'none'`, set `showDriftModal = true`. If user clicks "Gerar campanha mesmo assim" in modal, set `shouldGenerate = true` (which disables drift check). If user clicks "Realinhar" and it succeeds, set `shouldGenerate = true` too.
       - Simpler: Since `CampaignInputForm` already has its own handleSubmit that triggers generation via `useCampaignForm`, we need to intercept the click on "Gerar Campanha" before it. The most straightforward approach is to add a `driftCheckRef` or state flag in the parent that controls whether the child can submit.

       **Simplest approach:**
       - Add state `driftOverridden: boolean` (default false)
       - Pass `driftOverridden` as a new prop to `CampaignInputForm`
       - In `CampaignInputForm` (or in the parent before rendering the form): if `driftStatus !== 'none'` and not `driftOverridden`, render a placeholder that shows the `DriftCampaignModal` instead of the form
       - Actually simplest: keep the form rendered, but intercept the user's action to click "Gerar Campanha" by:
         - Adding a wrapper div around CampaignInputForm in campaign-page-client
         - Adding a state `pendingDriftCheck` 
         - Adding a ref/callback that CampaignInputForm calls before generating

       **Recommended implementation for campaign-page-client.tsx:**

       - Add state: `const [driftCheckRequired, setDriftCheckRequired] = useState<'none' | 'proceed' | 'blocking'>('none')`
       - When `driftStatus !== 'none'`:
         - Instead of rendering CampaignInputForm directly, render a modified version that shows a "Gerar Campanha" button with drift awareness
         - OR: wrap `CampaignInputForm` with a parent component that intercepts the submit
       
       **NOTE (deviation from design D6/Gatilho 5):** The design specifies the modal should trigger specifically on "Gerar Campanha" click. However, since `CampaignInputForm` handles submit internally and modifying it is outside scope, the modal is shown on campaign page mount when drift exists. The functional goal is still met: campaigns cannot be generated without drift acknowledgment. The "Gerar campanha mesmo assim" button preserves the user's ability to proceed.

**Practical approach (no CampaignInputForm changes):**
       - In campaign-page-client.tsx, add a `generateAttempted` ref
       - When `driftStatus !== 'none'` and user interacts with the page, show a blocking overlay before the form — the modal appears as a separate overlay
       - Add a simple "Gerar Campanha" wrapper: when CampaignInputForm's submit is triggered (via the form's own button), the modal intercepts
       
       Since modifying `CampaignInputForm` is outside this phase scope, the cleanest approach in `campaign-page-client.tsx` is:
       - Add state `driftOverride: boolean` (default false)
       - Render `<DriftCampaignModal open={showDriftModal} onRealinhar={realinhar} onContinue={() => { setShowDriftModal(false); setDriftOverride(true); }} isRealinhando={isRealinhando} />`
       - Show the modal when user clicks a "Gerar Campanha" type action — since `CampaignInputForm` internally calls `useCampaignForm` which triggers generation, we need an intercept
       
       **Final pragmatic approach:** Since `CampaignInputForm` is rendered with `storeIdentity`, and it has its own `handleSubmit`, the drift modal SHALL be shown as an overlay on top of the page. When `driftStatus !== 'none'` and the page has been loaded AND a generation is about to start:
       - Add state `generationBlocked: boolean` (default true when drift exists, until user acts)
       - Don't render CampaignInputForm until drift is resolved OR overridden
       - Render a "Gerar Campanha" button directly in campaign-page-client.tsx instead — wait, that breaks the existing flow.

       **Accepted approach for this task:** 
       - Keep `CampaignInputForm` as-is
       - Before the return block, add `<DriftCampaignModal>` (always rendered, controlled by `open` prop)
       - Add a `generationBlocked` state initialized to `true` when drift exists
       - Add a ref to detect when CampaignInputForm's submit is triggered — simplest is to add a custom `beforeGenerate` pattern: wrap the page with a context or just add state
       - For simplicity: render a button "Gerar Campanha" ABOVE the CampaignInputForm that, when drift exists, opens the modal. When drift is overridden, hide this button and let the normal form work.
       - OR: just show the modal unconditionally over the page content when drift exists, checking if user clicks "Gerar campanha mesmo assim" to set a flag that hides the modal and allows generation
       - OR the simplest: When `driftStatus !== 'none'`, render the modal on top of the page (it overlays everything, including CampaignInputForm). The modal is shown on mount of campaign page. When user clicks "Gerar campanha mesmo assim", close modal and set `driftOverride = true`. On next re-render, modal stays closed and the normal CampaignInputForm is fully accessible.
       - **Even simpler: Only show modal when user tries to generate.** Use a state `generateClicked` that triggers modal if drift exists. Since CampaignInputForm's handleSubmit is inside that component, we can add a `preSubmit` prop:

       ```typescript
       // In campaign-page-client.tsx
       const [driftOverridden, setDriftOverridden] = useState(false)
       const [showDriftModal, setShowDriftModal] = useState(false)
       
       // Pass a modified storeIdentity or a submit blocker
       // Render modal conditionally
       ```

       **Final approach (pragmatic, no CampaignInputForm changes needed):**
       
       Add a `driftOverridden` state ref. In the campaign page:
       1. If `driftStatus !== 'none'` and NOT `driftOverridden`, render a full-page overlay at z-40 (below modal z-50) that explains drift, with the modal appearing on top
       2. The overlay has a message and the modal handles the interaction
       3. Actually most pragmatic: just render the `DriftCampaignModal` whenever drift status isn't 'none' on page load.
          - On first render: if driftStatus !== 'none', set showDriftModal = true
          - When user clicks "Gerar campanha mesmo assim", setDriftOverridden(true), setShowDriftModal(false)
          - When user clicks "Realinhar", call realinhar, on success setDriftOverridden(true)

    Implement as follows:
    - Add state `driftOverridden: boolean` (default false)
    - Add effect: when `store` and `brandProfile` are loaded and `driftStatus !== 'none'` and NOT `driftOverridden`, `setShowDriftModal(true)`
    - Render `<DriftCampaignModal open={showDriftModal} ... />` inside the return, before the main content wrapper
    - The modal's `onContinue` handler hides the modal and sets `driftOverridden = true`
    - The modal's `onRealinhar` calls `realinhar`, on success sets `driftOverridden = true` and hides modal; on failure modal stays open (parent catches error and shows toast — can use a simple state like `driftError`)
    - While `showDriftModal` is true, the page content below is visible but the overlay covers it — this is fine, behavioral requirement
    - When `driftOverridden` becomes true, the modal stops showing and the form is fully interactive

    Add error display: `driftError: string | null` state, shown as a toast inside the modal when set.
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | Select-String -NotMatch "node_modules"</automated>
  </verify>
  <acceptance_criteria>
    - `store-identity-form.tsx` imports and uses `useDriftDetection`, `DriftBanner`, `DriftDiscreetButton`
    - Drift banner renders when `driftStatus === 'new'`, positioned above form fields
    - Banner "Realinhar" calls `realinhar()`, "Manter" calls `ignorar()`
    - Discreet button renders when `driftStatus === 'dismissed'`, positioned below form title
    - Discreet button click calls `realinhar()`
    - `campaign-page-client.tsx` imports and uses `useDriftDetection`, `DriftCampaignModal`
    - Drift modal appears on mount when drift exists (before generation can proceed)
    - "Gerar campanha mesmo assim" in modal hides modal and allows generation
    - "Realinhar direção visual" in modal triggers re-inference
    - Modal does NOT appear when `driftStatus === 'none'`
    - TypeScript compilation passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client (Step 2) → User | Drift banner/button shown based on computed drift status |
| Client (Campaign page) → User | Drift modal blocks campaign generation until user chooses |
| Client → API | Realinhar/ignorar actions call backend endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0462-06 | Spoofing | Drift banner visibility | accept | Drift status computed client-side; user could modify — no security impact |
| T-0462-07 | Denial of Service | Modal blocks generation | accept | Intentional UX: user can always click "Gerar campanha mesmo assim" to proceed |
| T-0462-08 | Information Disclosure | Modal text | accept | Shows only data the user entered |

</threat_model>

<verification>

### Build & Type Verification
```bash
npx tsc --noEmit --pretty
git diff --stat
```

### Manual Verification
1. Banner component renders with correct text and both buttons
2. Discreet button renders as muted text link with no background
3. Campaign modal renders as full-screen overlay with no close button
4. Step 2 form shows banner when drift is new, discreet button when dismissed, nothing when none
5. Campaign page shows modal when drift exists, proceeds normally without drift
6. TypeScript compilation passes

</verification>

<success_criteria>
- [ ] `drift-banner.tsx` created with Realinhar and Manter buttons
- [ ] `drift-discreet-button.tsx` created with muted text link
- [ ] `drift-campaign-modal.tsx` created with full-screen overlay and two options
- [ ] `store-identity-form.tsx` integrates drift banner/button using useDriftDetection hook
- [ ] `campaign-page-client.tsx` integrates drift modal blocking Gerar Campanha
- [ ] No emojis or icons (except spinners during loading)
- [ ] TypeScript compilation passes
</success_criteria>

<output>
Create `.planning/phases/4.6.2-visual-direction-drift-detection/4.6.2-03-SUMMARY.md` when done
</output>

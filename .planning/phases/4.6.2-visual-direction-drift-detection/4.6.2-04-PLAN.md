---
phase: 4.6.2-visual-direction-drift-detection
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified: []
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "TypeScript compilation passes with zero errors"
    - "Lint passes with zero errors"
    - "Manual flow: create store → save → change segment → Step 2 shows banner → dismiss → refresh → discreet button → realinhar → banner gone"
    - "Manual flow: change segment → banner → realinhar → change name → banner reappears"
    - "Manual flow: new store (create mode) → no drift detection"
    - "Campaign generation flow is NOT modified in this phase — no drift check on Gerar Campanha"
  artifacts:
    - path: "N/A — no code created"
      provides: "Verification that all prior plans work correctly together"
  key_links:
    - from: "Plans 1-3"
      to: "E2E flow"
      via: "All prior plans must be implemented correctly for flows to work"
---

<objective>
**Plan 04: E2E Verification — TypeScript, Lint, and Manual Flow Testing**

Run full TypeScript check, lint, and manual verification of all drift detection flows from end to end. This plan validates that Plans 01-03 work correctly together and that the drift detection feature is ready for production.

**Purpose:** Guarantee that the drift detection feature is correct, complete, and stable.
**Output:** Verified working state across all drift detection flows
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/4.6.2-visual-direction-drift-detection/4.6.2-CONTEXT.md
@openspec/changes/fase-4-6-2-visual-direction-drift-detection/design.md
@openspec/changes/fase-4-6-2-visual-direction-drift-detection/tasks.md
@openspec/changes/fase-4-6-2-visual-direction-drift-detection/specs/visual-direction-drift-detection/spec.md
@openspec/changes/fase-4-6-2-visual-direction-drift-detection/specs/store-identity-ui/spec.md
@src/lib/drift.ts
@src/components/flow/use-drift-detection.ts
@src/components/flow/store-identity-form.tsx


<existing_patterns>
See .planning/phases/04.6.1-text-only-state-visual-direction-inference/04.6.1-*-PLAN.md — prior plans have TypeScript + lint checks in verification sections
</existing_patterns>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run TypeScript compilation and lint</name>
  <files></files>
  <read_first>
    @package.json — check available scripts for typecheck and lint
  </read_first>
  <action>
    Run the following commands in order:

    1. `npx tsc --noEmit --pretty` — verify TypeScript compilation passes with zero errors
    2. `npx next lint` — verify lint passes with zero errors
    3. `git status` — review all changed files to ensure no unintended modifications
    4. `git diff --stat` — review the scope of changes

    If typecheck or lint errors are found, fix them before proceeding to manual verification.
    Common issues to watch for:
    - Missing imports in drift-banner.tsx, drift-discreet-button.tsx
    - Type mismatches between store/state values and hook parameter types
    - Missing null checks for store.id or profile fields
    - Incorrect Tailwind class names
    - Unused imports from the modifications
  </action>
  <what-built>All drift detection source code (types, API, services, hooks, components)</what-built>
  <how-to-verify>
    1. Run `npx tsc --noEmit --pretty` — expect zero errors
    2. Run `npx next lint` — expect zero errors
    3. If errors exist, fix them (missing imports, type mismatches, unused variables)
    4. Re-run both commands until clean
  </how-to-verify>
  <resume-signal>Typecheck and lint pass with zero errors</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual flow verification — all drift scenarios</name>
  <files></files>
  <read_first>
    @04.6.2-CONTEXT.md — D6 UX flow for all 5 gatilhos
    @openspec/changes/fase-4-6-2-visual-direction-drift-detection/specs/store-identity-ui/spec.md — All scenario requirements
    @src/components/flow/store-identity-form.tsx — the modified form to test
  </read_first>
  <action>
    Run through each manual flow with the application running locally. Verify step by step.

    ### Flow 1: New store — no drift detection (create mode)
    1. Start with no store saved (clear localStorage)
    2. Navigate to Step 1
    3. Fill in a store name and segment
    4. Save and go to Step 2
    5. **Verify:** No drift banner or discreet button is shown (first-time user, no brand profile yet)
    6. Click "Continuar sem logo" (or "Não tenho logo")
    7. After inference completes, confirm no drift UI is shown (input_snapshot has just been set)

    ### Flow 2: Drift detected — banner shows
    1. With a store that already has a synced brand profile (from Flow 1)
    2. Go back to Step 1 (Dados da Loja)
    3. Change the segment to a different one
    4. Save
    5. Go to Step 2
    6. **Verify:** Drift banner appears at top of form with text "A direção visual da sua loja pode estar desatualizada. Você alterou dados importantes depois da última análise."
    7. **Verify:** Banner has two buttons: "Realinhar direção visual" (amber) and "Manter direção visual atual" (outline)

    ### Flow 3: Dismiss drift → discreet button
    1. From Flow 2, with banner visible
    2. Click "Manter direção visual atual"
    3. **Verify:** Banner disappears
    4. **Verify:** Discreet button appears below form title: "Direção visual pode estar desatualizada" (muted text, underline)
    5. Refresh the page
    6. **Verify:** Discreet button still shows (dismissal persisted), NOT the banner

    ### Flow 4: Realinhar from discreet button
    1. From Flow 3, with discreet button visible
    2. Click the discreet button "Direção visual pode estar desatualizada"
    3. **Verify:** Re-inference processes (spinner may show briefly)
    4. **Verify:** After success, discreet button disappears
    5. **Verify:** No drift UI visible (data has been realigned)

    ### Flow 5: New drift after realinhamento
    1. From Flow 4 (freshly realigned)
    2. Go back to Step 1
    3. Change the store name
    4. Save
    5. Go to Step 2
    6. **Verify:** Drift banner reappears (data changed since last realinhamento)

    ### Edge case: beforeunload not triggered by drift
    1. With drift banner visible
    2. Make no changes to the form
    3. Try to navigate away or close the tab
    4. **Verify:** No beforeunload prompt (drift alone never triggers it)

    ### Scope boundary: campaign generation unchanged
    1. Navigate to the campaign page with any drift state
    2. **Verify:** No drift modal or drift overlay blocks the campaign form
    3. Campaign form is fully accessible regardless of drift status
  </what-built>
  <how-to-verify>
    1. Run the application locally: `npm run dev`
    2. Follow flows 1-5 and the edge cases above
    3. Check each verification step — all must pass
    4. Record any failures for fixing
  </how-to-verify>
  <resume-signal>All 5 manual flows pass without issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Application → User | Manual verification covers all user-facing drift flows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0462-09 | Information Disclosure | Verification process | accept | Manual flows use only test data |

</threat_model>

<verification>

### Build & Type Verification
```bash
npx tsc --noEmit --pretty
npx next lint
```

### Manual Verification Checklist
- [ ] Flow 1: Create mode — no drift detection
- [ ] Flow 2: Drift detected — banner visible with correct text and buttons
- [ ] Flow 3: Dismiss drift — discreet button replaces banner, persists across refresh
- [ ] Flow 4: Realinhar from discreet button — re-inference works, UI resets
- [ ] Flow 5: New drift after realinhamento — banner reappears on new data change
- [ ] Edge case: beforeunload not triggered by drift alone
- [ ] Scope boundary: campaign page unaffected by drift status

</verification>

<success_criteria>
- [ ] TypeScript compilation passes with zero errors
- [ ] Lint passes with zero errors
- [ ] All 5 manual flows pass
- [ ] Edge case: beforeunload not triggered by drift
- [ ] Campaign generation remains unchanged
- [ ] Phase complete: drift detection ready for production (Step 2 only)
</success_criteria>

<output>
Create `.planning/phases/4.6.2-visual-direction-drift-detection/4.6.2-04-SUMMARY.md` when done
</output>

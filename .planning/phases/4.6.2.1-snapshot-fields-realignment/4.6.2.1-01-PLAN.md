---
phase: 4.6.2.1-snapshot-fields-realignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/snapshot.ts
  - src/lib/drift.ts
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "StoreProfileInputSnapshot has exactly 7 text fields (no brand_color, no accent_color)"
    - "SNAPSHOT_FIELDS constant has 7 fields"
    - "buildStoreProfileInputSnapshot(store) returns StoreProfileInputSnapshot"
    - "DriftSnapshot is type alias for StoreProfileInputSnapshot"
    - "SENSITIVE_FIELDS removed from drift.ts"
    - "DRIFT_FIELDS has 4 fields (transitional, not policy)"
    - "currentVisualState takes only store, delegates to buildStoreProfileInputSnapshot"
---

<objective>
**Plan 01: Foundation — Tipos, Constantes e Helper**

Criar `src/lib/snapshot.ts` com o tipo, constante e helper de snapshot. Atualizar `src/lib/drift.ts` para usar os novos tipos.

**O que entrega:** A base que todos os consumidores (endpoints, frontend) dependem.
</objective>

<context>
@.planning/phases/4.6.2.1-snapshot-fields-realignment/4.6.2.1-CONTEXT.md
@openspec/changes/fase-4-6-2-1-snapshot-fields-realignment/design.md
@openspec/changes/fase-4-6-2-1-snapshot-fields-realignment/tasks.md
</context>

<tasks>

<task type="execute">
<read_first>
src/lib/store.ts
</read_first>
<action>
Criar `src/lib/snapshot.ts`:

1. `StoreProfileInputSnapshot` — 7 campos `string | null`: segment, subsegment, tone_of_voice, name, positioning, short_description, slogan

2. `StoredProfileSnapshot = Partial<StoreProfileInputSnapshot>` — para leitura de snapshots históricos

3. `SNAPSHOT_FIELDS` constante:
   ```typescript
   export const SNAPSHOT_FIELDS = [
     'segment', 'subsegment', 'tone_of_voice', 'name',
     'positioning', 'short_description', 'slogan',
   ] as const;
   ```

4. `buildStoreProfileInputSnapshot(store)`:
   - Pick: segment, subsegment, tone_of_voice, name, positioning, short_description, slogan
   - segment e name direto; demais com `?? null`
   - Retorna `StoreProfileInputSnapshot`

Import `import type { Store } from '@/lib/store'`.
</action>
<acceptance_criteria>
- 7 campos textuais, sem brand_color/accent_color
- `buildStoreProfileInputSnapshot` retorna exatamente 7 chaves
- `null` preservado como `null`
- `SNAPSHOT_FIELDS` não inclui `brand_color` nem `accent_color`
- `npm run typecheck` passa
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/drift.ts
src/lib/snapshot.ts
</read_first>
<action>
Modificar `src/lib/drift.ts`:

1. Importar de `./snapshot`:
   ```typescript
   import type { StoreProfileInputSnapshot } from './snapshot';
   import { buildStoreProfileInputSnapshot, SNAPSHOT_FIELDS } from './snapshot';
   ```

2. `DriftSnapshot` vira alias: `export type DriftSnapshot = StoreProfileInputSnapshot;`

3. Remover `SENSITIVE_FIELDS`. Adicionar:
   ```typescript
   export const DRIFT_FIELDS = ['segment', 'subsegment', 'tone_of_voice', 'name'] as const;
   ```
   (Mecanismo transitório para manter compatibilidade — não é política de drift.)

4. `currentVisualState` simplificado:
   - Remove parâmetro `profile`
   - Pick do store: adiciona `positioning`, `short_description`, `slogan`; remove `brand_color`
   - Corpo: `return buildStoreProfileInputSnapshot(store)`

5. `normalizeSnapshotValue` inalterado.

6. `computeDriftStatus`:
   - Aceita `Partial<StoreProfileInputSnapshot>` para snapshots históricos
   - Compara usando `DRIFT_FIELDS` (4 campos) em vez do array antigo de 6
</action>
<acceptance_criteria>
- `SENSITIVE_FIELDS` removido
- `DRIFT_FIELDS` com 4 campos (segment, subsegment, tone_of_voice, name)
- `DriftSnapshot = StoreProfileInputSnapshot`
- `currentVisualState(store)` sem profile
- `computeDriftStatus` compara `DRIFT_FIELDS`
- `npm run typecheck` passa
</acceptance_criteria>
</task>

</tasks>

---
phase: 4.6.2.1-snapshot-fields-realignment
plan: 02
type: execute
wave: 2
depends_on:
  - 4.6.2.1-01
files_modified:
  - src/components/flow/use-drift-detection.ts
  - src/components/flow/store-identity-form.tsx
  - src/app/api/store/[id]/logo/route.ts
  - src/app/api/store/[id]/logo/restore/route.ts
  - src/app/api/store/[id]/brand-profile/realign/route.ts
  - src/app/api/store/[id]/brand-profile/infer/route.ts
  - src/app/api/store/[id]/visual-signature/approve/route.ts
  - src/app/api/store/[id]/logo/history/route.ts
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "All brand-profile snapshot writes use buildStoreProfileInputSnapshot(store) — no inline snapshot construction"
    - "detectDrift and computeDriftStatusForHistory use DRIFT_FIELDS (transitional)"
    - "Brand profile snapshots have 7 text fields (no brand_color/accent_color)"
    - "VisualSignatureMetadataInputSnapshot (11 fields) unchanged"
    - "use-drift-detection Pick expanded to 7 fields"
    - "store-identity-form driftStore includes positioning, short_description, slogan"
    - "snapshotsEqual uses SNAPSHOT_FIELDS"
---

<objective>
**Plan 02: Escritores e Consumidores**

Atualizar todos os endpoints para usar `buildStoreProfileInputSnapshot(store)` e adaptar frontend para os 7 campos. VS snapshot (11 campos) preservado.

**O que entrega:** Persistência consistente dos 7 campos em todos os estados (text only, logo, VS).
</objective>

<context>
@.planning/phases/4.6.2.1-snapshot-fields-realignment/4.6.2.1-CONTEXT.md
@openspec/changes/fase-4-6-2-1-snapshot-fields-realignment/design.md
@openspec/changes/fase-4-6-2-1-snapshot-fields-realignment/tasks.md
</context>

<tasks>

<task type="execute">
<read_first>
src/app/api/store/[id]/logo/route.ts
src/lib/snapshot.ts
</read_first>
<action>
Modificar `POST /api/store/[id]/logo`:

1. Importar `buildStoreProfileInputSnapshot` de `@/lib/snapshot`

2. `inputSnapshot` (~linha 240): substituir objeto inline por `buildStoreProfileInputSnapshot(store)`
3. `metadata.input_snapshot` (~linha 322): usar helper, remover override de brand_color/accent_color
4. `attempt_snapshot` (~linha 413): usar helper
</action>
<acceptance_criteria>
- 3 pontos de snapshot usam helper
- Nenhuma cor no snapshot
- `npm run typecheck` passa
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/app/api/store/[id]/logo/restore/route.ts
src/lib/drift.ts
src/lib/snapshot.ts
</read_first>
<action>
Modificar `POST /api/store/[id]/logo/restore`:

1. Importar `buildStoreProfileInputSnapshot`, `DRIFT_FIELDS`

2. `detectDrift` (~linha 15): campo de comparação muda para `DRIFT_FIELDS` (4)
3. `currentSnapshot` (~linha 117): helper
4. `metadata.input_snapshot` (~linha 242): helper, sem override de cores
5. `attempt_snapshot` (~linha 280): helper
</action>
<acceptance_criteria>
- Snapshot via helper
- `detectDrift` compara só `DRIFT_FIELDS`
- `npm run typecheck` passa
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/app/api/store/[id]/brand-profile/realign/route.ts
src/lib/snapshot.ts
</read_first>
<action>
Modificar `POST /api/store/[id]/brand-profile/realign`:

1. Importar `buildStoreProfileInputSnapshot`

2. Logo path (~linha 124): `input_snapshot: buildStoreProfileInputSnapshot(store)` (sem cores)
3. Text-only path (~linha 247): `inputSnapshot` via helper (sem cores)
</action>
<acceptance_criteria>
- Ambos os paths usam helper
- `npm run typecheck` passa
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/app/api/store/[id]/brand-profile/infer/route.ts
src/lib/snapshot.ts
</read_first>
<action>
Modificar `POST /api/store/[id]/brand-profile/infer`:

1. Importar `buildStoreProfileInputSnapshot`

2. `inputSnapshot` (~linha 107): via helper, sem `resolvedBrandColor`/`accentColor` no snapshot
</action>
<acceptance_criteria>
- Snapshot via helper
- `npm run typecheck` passa
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/app/api/store/[id]/visual-signature/approve/route.ts
src/lib/snapshot.ts
</read_first>
<action>
Modificar `POST /api/store/[id]/visual-signature/approve`:

1. Importar `buildStoreProfileInputSnapshot`

2. `inputSnapshot` (~linha 65) — `VisualSignatureMetadataInputSnapshot` (11 campos) **NÃO ALTERADO**

3. Brand profile metadata `input_snapshot` (~linha 218 e ~333): substituir spread de 11 campos + cores por `buildStoreProfileInputSnapshot(store)`
</action>
<acceptance_criteria>
- VS snapshot (11 campos) intacto
- Brand profile metadata usa helper (7 campos)
- `npm run typecheck` passa
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/app/api/store/[id]/logo/history/route.ts
src/lib/drift.ts
src/lib/snapshot.ts
</read_first>
<action>
Modificar `GET /api/store/[id]/logo/history`:

1. Importar `buildStoreProfileInputSnapshot`, `DRIFT_FIELDS`

2. `computeDriftStatusForHistory` (~linha 12): campo de comparação para `DRIFT_FIELDS` (4)

3. `currentStore` (~linha 77): `buildStoreProfileInputSnapshot(store)` — objeto completo de 7 campos via helper. A comparação continua usando `DRIFT_FIELDS`, mas o objeto mantém o contrato unificado de 7 campos.
</action>
<acceptance_criteria>
- `computeDriftStatusForHistory` compara `DRIFT_FIELDS`
- `npm run typecheck` passa
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/components/flow/use-drift-detection.ts
src/lib/drift.ts
src/lib/snapshot.ts
</read_first>
<action>
Modificar `use-drift-detection.ts`:

1. Importar `SNAPSHOT_FIELDS` de `@/lib/snapshot`, `DRIFT_FIELDS` de `@/lib/drift`

2. Pick do store: expandir para incluir `positioning`, `short_description`, `slogan`; remover `brand_color`

3. Pick do profile: remover `brand_colors_chosen`, `safe_color_tokens`, `inferred_accent_color` — manter só `metadata`

4. `snapshotsEqual`: campo de comparação muda para `SNAPSHOT_FIELDS` (7)

5. `currentVisualState(store, profile)` → `currentVisualState(store)` (sem profile)

6. `dismissSnapshot` no `ignorar`: usar `const dismissSnapshot = currentSnapshot` — `currentSnapshot` já contém os 7 campos de `SNAPSHOT_FIELDS` via `currentVisualState` (que delega a `buildStoreProfileInputSnapshot`). Não é necessário construir objeto separado.

7. `allFields` para detecção de drift: `DRIFT_FIELDS` (4)
</action>
<acceptance_criteria>
- Pick expandido, sem `brand_color`
- `snapshotsEqual` compara 7 campos
- `currentVisualState(store)` sem profile
- `npm run typecheck` passa
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/components/flow/store-identity-form.tsx
</read_first>
<action>
Modificar `store-identity-form.tsx`:

1. `driftStore` (~linha 127): adicionar `positioning`, `short_description`, `slogan` com `?? null`; remover `brand_color`

2. `handleStep2Submit` (~linha 906): `currentVisualState(driftStore ?? { id: storeId, segment: '', subsegment: '', tone_of_voice: '', name: '', positioning: null, short_description: null, slogan: null })` — fallback explícito com todos os 7 campos
</action>
<acceptance_criteria>
- `driftStore` inclui 3 novos campos, sem `brand_color`
- `handleStep2Submit` sem profile, fallback com 7 campos (positioning/short_description/slogan = null)
- `npm run typecheck` passa
</acceptance_criteria>
</task>

</tasks>

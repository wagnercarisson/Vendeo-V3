---
phase: 4.6.2.1-snapshot-fields-realignment
plan: 03
type: execute
wave: 3
depends_on:
  - 4.6.2.1-02
files_modified:
  - src/lib/__tests__/snapshot.test.ts
  - src/lib/__tests__/drift.test.ts
  - src/app/api/store/[id]/visual-signature/approve/__tests__/approve-route.test.ts
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "snapshot.test.ts: helper retorna 7 chaves, null preservado, SNAPSHOT_FIELDS listado"
    - "drift.test.ts: backward compat (snapshot antigo sem falso drift), novos campos inertes, cor não gera drift"
    - "approve-route.test.ts: snapshots mockados refletem 7 campos"
    - "npm test, typecheck, lint, build passam"
---

<objective>
**Plan 03: Testes Mínimos e Gates**

Testes focados no que esta fase entrega: helper, backward compat de leitura, novos campos inertes, ausência de falso drift por cor. Build verification.

**O que entrega:** Cobertura mínima que valida a persitência correta sem antecipar cenários de drift (4.6.2.2).
</objective>

<context>
@.planning/phases/4.6.2.1-snapshot-fields-realignment/4.6.2.1-CONTEXT.md
@openspec/changes/fase-4-6-2-1-snapshot-fields-realignment/tasks.md
</context>

<tasks>

<task type="execute">
<read_first>
src/lib/snapshot.ts
src/lib/__tests__/identity-transitions.test.ts
</read_first>
<action>
Criar `src/lib/__tests__/snapshot.test.ts`:

1. Helper retorna exatamente 7 chaves
2. `null` preservado como `null`
3. Valores string preservados
4. `SNAPSHOT_FIELDS` tem 7 campos, não inclui `brand_color`/`accent_color`
</action>
<acceptance_criteria>
- 4 casos de teste
- `npm test` verde
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/drift.ts
src/lib/snapshot.ts
</read_first>
<action>
Criar `src/lib/__tests__/drift.test.ts`:

1. Snapshot antigo sem `positioning` + store com `positioning` preenchido → não gera falso drift (backward compat de leitura). Usar `Record<string, string | null>` para simular snapshot legado com campos extras (incluindo `brand_color`/`accent_color`), não adicionar `brand_color` ao `StoreProfileInputSnapshot`.

2. Mudança só de `brand_color` em snapshot legado → não gera drift (cor não está em `DRIFT_FIELDS`). Usar snapshot legado com campo `brand_color` extra que é ignorado pela comparação.

3. `positioning`, `short_description`, `slogan` diferentes → não geram drift (campos inertes, fora de `DRIFT_FIELDS`).
</action>
<acceptance_criteria>
- 3 casos de teste cobrindo o essencial
- Sem testar cenários de política de drift (4.6.2.2)
- `npm test` verde
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/app/api/store/[id]/visual-signature/approve/__tests__/approve-route.test.ts
</read_first>
<action>
Atualizar `approve-route.test.ts`:

Os `input_snapshot` existentes no teste são da VS (11 campos, `VisualSignatureMetadataInputSnapshot`) — **não devem ser alterados**.

A cobertura para brand profile deve vir de uma assertion no payload de `.update()` em `store_brand_profiles`: capturar a chamada e verificar que `metadata.input_snapshot` contém exatamente os 7 campos de `SNAPSHOT_FIELDS` (sem `brand_color`/`accent_color`). Adicionar assertion no teste que cria ou reutiliza perfil de marca (testes 12.8/12.9).
</action>
<acceptance_criteria>
- Brand profile snapshots mockados têm 7 campos
- VS snapshots mockados inalterados
- `npm test` verde
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
package.json
</read_first>
<action>
Verificação completa:

```bash
npm run typecheck
npm test
npm run lint
npm run build
```

Corrigir se necessário até tudo passar.
</action>
<acceptance_criteria>
- typecheck: zero erros
- test: todos verdes
- lint: sem warnings novos
- build: compila
</acceptance_criteria>
</task>

</tasks>

<manual_verification>
## Smoke Tests Manuais

Validar na interface (Step 2) após a implementação:

1. **Cor isolada não causa falso drift** — Mudar apenas `brand_color` no color picker, salvar → `driftStatus` permanece `none` (cor não está em `DRIFT_FIELDS`)
2. **DRIFT_FIELD existente continua operacional** — Mudar `segment` ou `name`, salvar → `driftStatus` passa para `new`
3. **Dismiss persiste 7 campos** — Dismiss do drift → PATCH metadata persiste `SNAPSHOT_FIELDS` (7 campos) no banco, verificar via consulta direta ao Supabase ou logs
4. **Brand profile tem 7 campos, VS continua com 11** — VS approval → `store_brand_profiles.metadata.input_snapshot` tem 7 campos textuais (sem cores); `store_visual_signatures.metadata.input_snapshot` continua com 11 campos (inalterado)
</manual_verification>

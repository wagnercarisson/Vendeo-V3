---
phase: 4.6.2.1-snapshot-fields-realignment
plan: 03
type: execute
wave: 3
depends_on:
  - 4.6.2.1-02
files_modified:
  - src/lib/__tests__/snapshot.test.ts
  - src/lib/__tests__/drift.test.ts
  - src/app/api/store/[id]/visual-signature/approve/__tests__/approve-route.test.ts
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "snapshot.test.ts: helper retorna 7 chaves, null preservado, SNAPSHOT_FIELDS listado"
    - "drift.test.ts: backward compat (snapshot antigo sem falso drift), novos campos inertes, cor não gera drift"
    - "approve-route.test.ts: snapshots mockados refletem 7 campos"
    - "npm test, typecheck, lint, build passam"
---

<objective>
**Plan 03: Testes Mínimos e Gates**

Testes focados no que esta fase entrega: helper, backward compat de leitura, novos campos inertes, ausência de falso drift por cor. Build verification.

**O que entrega:** Cobertura mínima que valida a persitência correta sem antecipar cenários de drift (4.6.2.2).
</objective>

<context>
@.planning/phases/4.6.2.1-snapshot-fields-realignment/4.6.2.1-CONTEXT.md
@openspec/changes/fase-4-6-2-1-snapshot-fields-realignment/tasks.md
</context>

<tasks>

<task type="execute">
<read_first>
src/lib/snapshot.ts
src/lib/__tests__/identity-transitions.test.ts
</read_first>
<action>
Criar `src/lib/__tests__/snapshot.test.ts`:

1. Helper retorna exatamente 7 chaves
2. `null` preservado como `null`
3. Valores string preservados
4. `SNAPSHOT_FIELDS` tem 7 campos, não inclui `brand_color`/`accent_color`
</action>
<acceptance_criteria>
- 4 casos de teste
- `npm test` verde
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/drift.ts
src/lib/snapshot.ts
</read_first>
<action>
Criar `src/lib/__tests__/drift.test.ts`:

1. Snapshot antigo sem `positioning` + store com `positioning` preenchido → não gera falso drift (backward compat de leitura)
2. Mudança só de `brand_color` → não gera drift (cor não está em `DRIFT_FIELDS`)
3. `positioning`, `short_description`, `slogan` diferentes → não geram drift (campos inertes, fora de `DRIFT_FIELDS`)
</action>
<acceptance_criteria>
- 3 casos de teste cobrindo o essencial
- Sem testar cenários de política de drift (4.6.2.2)
- `npm test` verde
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/app/api/store/[id]/visual-signature/approve/__tests__/approve-route.test.ts
</read_first>
<action>
Atualizar `approve-route.test.ts`:

Snapshots mockados de brand profile metadata: remover `brand_color`/`accent_color`, adicionar `positioning`/`short_description`/`slogan`.

Snapshots da VS (11 campos) permanecem intactos.
</action>
<acceptance_criteria>
- Brand profile snapshots mockados têm 7 campos
- VS snapshots mockados inalterados
- `npm test` verde
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
package.json
</read_first>
<action>
Verificação completa:

```bash
npm run typecheck
npm test
npm run lint
npm run build
```

Corrigir se necessário até tudo passar.
</action>
<acceptance_criteria>
- typecheck: zero erros
- test: todos verdes
- lint: sem warnings novos
- build: compila
</acceptance_criteria>
</task>

</tasks>

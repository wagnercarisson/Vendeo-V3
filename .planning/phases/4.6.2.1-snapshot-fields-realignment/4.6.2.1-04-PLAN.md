---
phase: 4.6.2.1-snapshot-fields-realignment
plan: 04
type: execute
wave: 3
depends_on:
  - 4.6.2.1-02
  - 4.6.2.1-03
files_modified:
  - src/lib/__tests__/snapshot.test.ts
  - src/lib/__tests__/drift.test.ts
  - src/app/api/store/[id]/visual-signature/approve/__tests__/approve-route.test.ts
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "snapshot.test.ts covers: helper returns 7 keys, null preserved, structure consistent, SNAPSHOT_FIELDS listed, buildDismissPayload returns Record with 7 keys"
    - "drift.test.ts covers: backward compat (old snapshot no false drift), color-only change no drift, DRIFT_FIELDS comparison only, computeDriftStatusForHistory, detectDrift"
    - "approve-route.test.ts: mocked brand profile input_snapshot uses 7 fields"
    - "npm test passes with no failures"
    - "npm run typecheck passes with no errors"
    - "npm run lint passes with no new warnings"
    - "npm run build compiles successfully"
  artifacts:
    - path: "src/lib/__tests__/snapshot.test.ts"
      provides: "Unit tests for snapshot helper and dismiss payload"
    - path: "src/lib/__tests__/drift.test.ts"
      provides: "Unit tests for backward compatible drift detection"
    - path: "src/app/api/store/[id]/visual-signature/approve/__tests__/approve-route.test.ts"
      provides: "Updated test mocks with 7-field snapshots"
  key_links:
    - from: "snapshot.test.ts"
      to: "src/lib/snapshot.ts"
      via: "Tests buildStoreProfileInputSnapshot, buildDismissPayload, StoreProfileInputSnapshot type"
    - from: "drift.test.ts"
      to: "src/lib/drift.ts"
      via: "Tests computeDriftStatus with old/new snapshots, DRIFT_FIELDS behavior"
    - from: "approve-route.test.ts"
      to: "src/app/api/store/[id]/visual-signature/approve/route.ts"
      via: "Tests approval route with 7-field brand profile snapshot mocks"
---

<objective>
**Plan 04: Tests & Verification**

Create unit tests for the new snapshot helper and updated drift detection. Update existing approve-route tests to use 7-field snapshots. Run full verification suite.

**Purpose:** Ensure correctness, backward compatibility, and build integrity.
**Output:** New test files, modified test files, verified build.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/4.6.2.1-snapshot-fields-realignment/4.6.2.1-CONTEXT.md
@openspec/changes/fase-4-6-2-1-snapshot-fields-realignment/tasks.md
@docs/alinhamento-fase-4.6.2.1.md

<existing_patterns>
See src/lib/__tests__/identity-transitions.test.ts for existing test patterns in src/lib/__tests__/
See src/app/api/store/[id]/visual-signature/approve/__tests__/approve-route.test.ts for existing route test patterns
See vitest.config.ts at project root for test configuration
</existing_patterns>
</context>

<tasks>

<task type="auto">
<read_first>
src/lib/snapshot.ts
src/lib/__tests__/identity-transitions.test.ts
</read_first>
<action>
Create `src/lib/__tests__/snapshot.test.ts` with the following test cases:

1. `buildStoreProfileInputSnapshot returns exactly 7 keys`:
   - Create a mock store object with all 7 fields filled
   - Call `buildStoreProfileInputSnapshot(mockStore)`
   - Assert `Object.keys(result).length === 7`
   - Assert all expected keys present: `['segment', 'subsegment', 'tone_of_voice', 'name', 'positioning', 'short_description', 'slogan']`

2. `buildStoreProfileInputSnapshot returns null for null fields`:
   - Create a mock store where all optional fields are null
   - Call `buildStoreProfileInputSnapshot(mockStore)`
   - Assert `result.subsegment === null`
   - Assert `result.tone_of_voice === null`
   - Assert `result.positioning === null`
   - Assert `result.short_description === null`
   - Assert `result.slogan === null`

3. `buildStoreProfileInputSnapshot preserves string values`:
   - Create a mock store with specific values
   - Assert each field matches the input value

4. `SNAPSHOT_FIELDS constant has correct values`:
   - Assert `SNAPSHOT_FIELDS.length === 7`
   - Assert it doesn't include `'brand_color'` or `'accent_color'`
   - Assert it includes all 7 expected field names

5. `buildDismissPayload returns Record with 7 keys`:
   - Create a mock store
   - Call `buildDismissPayload(mockStore)`
   - Assert `Object.keys(result).length === 7`
   - Assert all keys from `SNAPSHOT_FIELDS` are present
   - Assert values match the input store values

6. `StoredProfileSnapshot accepts partial snapshots`:
   - Create a partial snapshot (e.g., only `segment` and `name`)
   - Assert it type-checks as `StoredProfileSnapshot`
   - This is a compile-time check — in JS, assert the object shape

Use `describe`/`it`/`expect` pattern consistent with existing tests in the project. Check `vitest.config.ts` for the test framework configuration.
</action>
<acceptance_criteria>
- 6 test cases covering: 7 keys, null handling, value preservation, SNAPSHOT_FIELDS constant, dismiss payload, partial snapshot type
- `npm test` passes with all new tests green
- No type errors in test file
</acceptance_criteria>
</task>

<task type="auto">
<read_first>
src/lib/drift.ts
src/lib/snapshot.ts
</read_first>
<action>
Create `src/lib/__tests__/drift.test.ts` with the following test cases:

1. `computeDriftStatus returns none when inputSnapshot is null`:
   - Given a current snapshot and null inputSnapshot
   - Assert result is `'none'`

2. `computeDriftStatus returns none when all DRIFT_FIELDS match`:
   - Create current and input snapshots where all 4 DRIFT_FIELDS match
   - Assert result is `'none'`

3. `computeDriftStatus returns none when only positioning differs`:
   - Create current and input snapshots where all DRIFT_FIELDS match
   - Set `positioning` to different values in each
   - Assert result is `'none'` — positioning is not in DRIFT_FIELDS

4. `computeDriftStatus returns none when only brand_color differs`:
   - Create current and input snapshots where all DRIFT_FIELDS match
   - Set `brand_color` to different values in each
   - Assert result is `'none'` — brand_color is not in DRIFT_FIELDS

5. `computeDriftStatus returns new when segment differs`:
   - Create current and input snapshots where segment differs
   - Assert result is `'new'`

6. `computeDriftStatus returns dismissed when dismissedSnapshot matches`:
   - Create current, input, and dismissed snapshots where:
     - DRIFT_FIELDS differ between current and input (so drift exists)
     - All DRIFT_FIELDS match between current and dismissed (so drift was dismissed)
   - Assert result is `'dismissed'`

7. `computeDriftStatus returns new when dismissedSnapshot does not match`:
   - Create current, input, and dismissed snapshots where:
     - DRIFT_FIELDS differ between current and input (drift exists)
     - A DRIFT_FIELD differs between current and dismissed (new change)
   - Assert result is `'new'`

8. `Backward compat: old snapshot without positioning does not trigger false drift`:
   - Create a current snapshot with `positioning = "Premium"`
   - Create an input snapshot without the `positioning` field (simulate old snapshot)
   - Assert all DRIFT_FIELDS match between them
   - Assert result is `'none'` — positioning is ignored

9. `Color-only change does not trigger drift`:
   - Create current and input snapshots where only `brand_color` differs
   - All DRIFT_FIELDS match
   - Assert result is `'none'`

10. `computeDriftStatusForHistory returns none when DRIFT_FIELDS match`:
    - Import and test the local function pattern (or test via the logic it depends on)
    - Assert correct behavior

11. `detectDrift returns false when DRIFT_FIELDS match`:
    - Same pattern — test the comparison logic used by inline functions

Use the same test framework as the existing project tests.
</action>
<acceptance_criteria>
- 11 test cases covering: null input, full match, positioning diff, brand_color diff, segment diff, dismiss match, dismiss new, backward compat, color-only change, history drift, detect drift
- `npm test` passes with all new tests green
- No type errors
</acceptance_criteria>
</task>

<task type="auto">
<read_first>
src/app/api/store/[id]/visual-signature/approve/__tests__/approve-route.test.ts
</read_first>
<action>
Modify `src/app/api/store/[id]/visual-signature/approve/__tests__/approve-route.test.ts`:

1. Search for all mocked `input_snapshot` objects in brand profile metadata
2. Update each mock from the old 6-field format to the new 7-field format:
   - Remove `brand_color` and `accent_color` from brand profile `input_snapshot` mocks
   - Add `positioning`, `short_description`, `slogan` fields
3. The VS-level `input_snapshot` (VisualSignatureMetadataInputSnapshot with 11 fields) should remain unchanged — those mocks are for the visual signature, not the brand profile
4. Verify the test assertions still pass with the new shape

The 6-field → 7-field migration for mocked brand profile snapshots:
```typescript
// Before (old brand profile snapshot mock):
input_snapshot: {
  segment: 'moda-feminina',
  subsegment: 'moda-feminina',
  tone_of_voice: 'elegante',
  name: 'Loja Teste',
  brand_color: '#FF0000',
  accent_color: '#00FF00',
}

// After (new brand profile snapshot mock):
input_snapshot: {
  segment: 'moda-feminina',
  subsegment: 'moda-feminina',
  tone_of_voice: 'elegante',
  name: 'Loja Teste',
  positioning: 'Premium',
  short_description: 'Moda feminina',
  slogan: 'Elegância que transforma',
}
```

Do NOT change VS-level `input_snapshot` mocks that belong to `VisualSignatureMetadataInputSnapshot` (11 fields — includes city, state, brand_color, accent_color, etc.)
</action>
<acceptance_criteria>
- Brand profile `input_snapshot` mocks use 7 fields (no brand_color/accent_color, has positioning/short_description/slogan)
- VS-level `input_snapshot` mocks (11 fields) UNCHANGED
- All 13 existing tests still pass
- `npm test` passes
</acceptance_criteria>
</task>

<task type="auto">
<read_first>
package.json
</read_first>
<action>
Run the full verification suite:

1. `npm run typecheck` — verify zero TypeScript errors
2. `npm test` — verify all tests pass (including new snapshot.test.ts, drift.test.ts, updated approve-route.test.ts)
3. `npm run lint` — verify no new warnings
4. `npm run build` — verify production build compiles

If any verification step fails, fix the issues and re-run until all pass. Report which verification failed and the fix applied.

**Note:** Some commands may be different in this project. Use the actual scripts defined in `package.json`. If `typecheck` is not a script, use `npx tsc --noEmit --pretty`. If `test` is not defined, use `npx vitest run` or `npx jest`. If `build` is not defined, use `npx next build`.
</action>
<acceptance_criteria>
- `typecheck` exits 0 — zero TypeScript errors
- `test` exits 0 — all tests green
- `lint` exits 0 — no new warnings
- `build` exits 0 — production build compiles
</acceptance_criteria>
</task>

</tasks>

<quality_gate>
- New test file `snapshot.test.ts` passes all test cases
- New test file `drift.test.ts` passes all test cases
- `approve-route.test.ts` updated and passes
- `npm run typecheck` — zero errors
- `npm test` — all tests green
- `npm run lint` — no new warnings
- `npm run build` — production compiles
</quality_gate>

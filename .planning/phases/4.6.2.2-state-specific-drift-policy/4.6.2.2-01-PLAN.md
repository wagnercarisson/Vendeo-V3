---
phase: 4.6.2.2-state-specific-drift-policy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/__tests__/drift.test.ts
  - src/lib/visual-signature/__tests__/ (new test files)
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "All existing drift contracts have regression tests before any code changes"
    - "DRIFT_FIELDS (4 fields), computeDriftStatus, DriftStatus have test coverage"
    - "drift-validator validateDrift has test coverage for all 5 fields"
    - "brand-profiler generate has test coverage for reuse path"
    - "All modified endpoints in 4.6.2.1 have regression tests"
    - "TypeScript build and lint pass on baseline before any modifications"
    - "Smoke test: text_only + logo + VS flows still work unchanged"
---

<objective>
**Plan 01: Congelamento e Regressão — Onda 1**

Estabelecer a linha de base de testes e validação antes de qualquer alteração no código. Todos os contratos existentes devem ter cobertura de teste de regressão documentada. Nenhuma modificação funcional é feita nesta onda.

**O que entrega:** Base de testes verde + smoke test manual que comprova que nada quebrou antes de começar.
</objective>

<context>
@.planning/phases/4.6.2.2-state-specific-drift-policy/4.6.2.2-CONTEXT.md
@openspec/changes/fase-4-6-2-2-state-specific-drift-policy/tasks.md
@openspec/changes/fase-4-6-2-2-state-specific-drift-policy/design.md
</context>

<tasks>

<task type="execute">
<read_first>
src/lib/__tests__/drift.test.ts
src/lib/drift.ts
</read_first>
<action>
Add regression tests for `src/lib/drift.ts` covering:

1. `DRIFT_FIELDS` constant — exactly 4 fields: segment, subsegment, tone_of_voice, name
2. `computeDriftStatus` — scenarios:
   - null inputSnapshot → 'none'
   - all fields match → 'none'
   - segment differs → 'new'
   - tone_of_voice differs → 'new'
   - brand_color differs → 'none' (not in DRIFT_FIELDS)
   - positioning differs → 'none' (not in DRIFT_FIELDS)
   - short_description differs → 'none' (not in DRIFT_FIELDS)
   - slogan differs → 'none' (not in DRIFT_FIELDS)
   - drift + dismissedSnapshot matches → 'dismissed'
   - drift + dismissedSnapshot does NOT match → 'new'
3. `normalizeSnapshotValue`:
   - null/undefined → ''
   - hex color → lowercase
   - string → unchanged
4. `currentVisualState`:
   - returns StoreProfileInputSnapshot with 7 fields
   - delegates to buildStoreProfileInputSnapshot

Use existing test patterns from `src/lib/__tests__/drift.test.ts` (vitest).
</action>
<acceptance_criteria>
- Tests pass: `npx vitest run src/lib/__tests__/drift.test.ts` exits 0
- All 4 DRIFT_FIELDS covered
- All computeDriftStatus branches covered (none, new, dismissed)
- Null/undefined snapshots handled
- Non-DRIFT_FIELDS changes verified to NOT trigger drift
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/visual-signature/drift-validator.ts
</read_first>
<action>
Add regression tests for `src/lib/visual-signature/drift-validator.ts`:

1. `validateDrift` scenarios:
   - null input_snapshot → has_drift: true, reason: 'missing_metadata'
   - null content_used → has_drift: true, reason: 'missing_metadata'
   - all fields match → has_drift: false, reason: 'ok'
   - name differs → fields includes 'name'
   - segment differs → fields includes 'segment'
   - content_used.city=true + city differs → fields includes 'city'
   - content_used.city=false + city differs → city NOT in fields
   - content_used.state=true + state differs → fields includes 'state'
   - content_used.slogan=true + slogan differs → fields includes 'slogan'
   - multiple fields differ → all drifted fields in array

Create test file at `src/lib/visual-signature/__tests__/drift-validator.test.ts`.
</action>
<acceptance_criteria>
- Tests pass: `npx vitest run src/lib/visual-signature/__tests__/drift-validator.test.ts` exits 0
- All DriftValidationInput null/absent cases covered
- All 5 validation fields covered (name, segment, city, state, slogan)
- content_used conditional logic verified
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/visual-signature/brand-profiler.ts
src/lib/visual-signature/__tests__/
</read_first>
<action>
Add regression tests for `src/lib/visual-signature/brand-profiler.ts`:

1. `BrandProfilerWithoutLogoService.generate()`:
   - With existing profile → reuses (mode 'reuse' behavior)
   - Existing profile with status 'outdated' → updates to 'synced'

This tests the current reuse behavior that mode 'regenerate' will override in Wave 4.

Create or augment file at `src/lib/visual-signature/__tests__/brand-profiler.test.ts`.
Mock supabase calls to return controlled test data.
</action>
<acceptance_criteria>
- Tests pass: `npx vitest run src/lib/visual-signature/__tests__/brand-profiler.test.ts` exits 0
- Reuse path tested
- Outdated → synced path tested
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/app/api/store/[id]/brand-profile/realign/route.ts
src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts
src/app/api/store/[id]/visual-signature/approve/route.ts
src/app/api/store/[id]/visual-signature/route.ts
</read_first>
<action>
Review each endpoint file and verify each one has existing test coverage. For endpoints without tests, add a comment or a minimal smoke test.

Endpoints to check:
1. POST /api/store/[id]/brand-profile/realign
2. POST /api/store/[id]/visual-signature/generate-without-logo
3. POST /api/store/[id]/visual-signature/approve
4. GET /api/store/[id]/visual-signature (history)
5. POST /api/store/[id]/logo/restore
6. POST /api/store/[id]/logo/ (upload)

Document findings as comments in the test files.
</action>
<acceptance_criteria>
- All 6 endpoints reviewed for test coverage
- Missing coverage documented
- Existing tests still pass
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/components/flow/use-drift-detection.ts
src/components/flow/store-identity-form.tsx
src/components/flow/store-preview.tsx
</read_first>
<action>
Review frontend components for existing test coverage and document baseline state:

1. `use-drift-detection.ts` — verify hook returns driftStatus, currentSnapshot, hasCriticalDrift
2. `store-identity-form.tsx` — verify handleStep2Submit intercepts drift
3. `store-preview.tsx` — verify preview renders without drift badge

Document findings. Do NOT modify components — this is a regression baseline snapshot.
</action>
<acceptance_criteria>
- Frontend baseline documented
- No component modifications in this wave
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
</read_first>
<action>
Run TypeScript build and lint to establish a clean baseline:

```bash
npm run typecheck
npm run lint
```

Document any existing errors/warnings that must NOT be introduced by subsequent waves.
</action>
<acceptance_criteria>
- `npm run typecheck` passes
- `npm run lint` passes (or existing warnings documented)
- Baseline is clean before any modifications
</acceptance_criteria>
</task>

</tasks>

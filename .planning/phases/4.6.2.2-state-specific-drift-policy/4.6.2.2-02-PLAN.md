---
phase: 4.6.2.2-state-specific-drift-policy
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/lib/drift.ts
  - src/lib/visual-signature/drift-validator.ts
  - src/lib/visual-signature/drift-revalidator.ts
  - src/components/flow/use-drift-detection.ts
  - src/components/flow/store-identity-form.tsx
  - src/lib/__tests__/drift.test.ts
  - src/lib/visual-signature/__tests__/drift-validator.test.ts
  - src/lib/visual-signature/__tests__/drift-revalidator.test.ts
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "DRIFT_FIELDS constant removed from drift.ts"
    - "getDriftPolicy(identityState, contentUsed?) returns { sensitive, critical }"
    - "DriftCategory type = 'critical' | 'sensitive' | 'none'"
    - "evaluateCriticalDrift uses VS snapshot (11 fields) server-side only"
    - "evaluateSensitiveDrift uses BP snapshot (7 fields) with field list"
    - "computeDriftStatus accepts fields: readonly string[] (required, no default)"
    - "drift-revalidator.ts exists and wraps evaluateCriticalDrift"
    - "drift-validator.ts handles absent properties gracefully (skip, not error)"
    - "All existing callers migrated to getDriftPolicy(state).sensitive"
    - "Props ausentes em snapshots antigos sao skipadas (nao disparam falso drift)"
---

<objective>
**Plan 02: Fundação do Diagnóstico — Onda 2**

Implementar a base da política de drift por identity_state. Substituir DRIFT_FIELDS constante por getDriftPolicy(), adicionar DriftCategory, evaluateCriticalDrift/evaluateSensitiveDrift como funções independentes, atualizar computeDriftStatus para aceitar fields param, criar drift-revalidator.ts, e expandir drift-validator.ts para props ausentes.

**O que entrega:** Toda a lógica de diagnóstico que os consumidores (frontend, endpoints) usarão.
</objective>

<context>
@.planning/phases/4.6.2.2-state-specific-drift-policy/4.6.2.2-CONTEXT.md
@openspec/changes/fase-4-6-2-2-state-specific-drift-policy/specs/visual-direction-drift-detection/spec.md
@openspec/changes/fase-4-6-2-2-state-specific-drift-policy/design.md
@openspec/changes/fase-4-6-2-2-state-specific-drift-policy/tasks.md
</context>

<tasks>

<task type="execute">
<read_first>
src/lib/drift.ts
src/lib/snapshot.ts
src/lib/visual-signature/types.ts
</read_first>
<action>
Substituir `DRIFT_FIELDS` por `getDriftPolicy` em `src/lib/drift.ts`:

1. Adicionar type `DriftCategory = 'critical' | 'sensitive' | 'none'`

2. Adicionar `DRIFT_POLICY` constante com as 3 matrizes:
   - text_only: sensitive = [name, segment, subsegment, tone_of_voice, positioning, short_description, slogan]; critical = []
   - logo: sensitive = [segment, subsegment, tone_of_voice, positioning, short_description, slogan]; critical = []
   - visual_signature: sensitive = [subsegment, tone_of_voice, positioning, short_description]; critical = [name, segment] + condicionais (slogan, city, state via contentUsed)

3. `getDriftPolicy(identityState, contentUsed?)`:
   - Fallback para text_only se identityState desconhecido
   - Se visual_signature e contentUsed: push slogan se contentUsed.slogan, city se contentUsed.city, state se contentUsed.state nos critical fields

4. Remover `DRIFT_FIELDS` (constante exportada)
</action>
<acceptance_criteria>
- `DRIFT_FIELDS` não existe mais como export
- `getDriftPolicy('text_only').sensitive` contém 7 campos
- `getDriftPolicy('logo').sensitive` contém 6 campos (name excluído)
- `getDriftPolicy('visual_signature').sensitive` contém 4 campos
- `getDriftPolicy('visual_signature', {slogan:true}).critical` inclui 'slogan'
- `getDriftPolicy('visual_signature', {city:true}).critical` inclui 'city'
- `getDriftPolicy('visual_signature', {}).critical` = ['name', 'segment']
- `npm run typecheck` passa
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/drift.ts
src/lib/visual-signature/types.ts
src/lib/snapshot.ts
</read_first>
<action>
Implementar `evaluateCriticalDrift` em `src/lib/drift.ts`:

```typescript
export function evaluateCriticalDrift(
  vsSnapshot: Partial<VisualSignatureMetadataInputSnapshot> | null | undefined,
  contentUsed: { slogan?: boolean; city?: boolean; state?: boolean } | null | undefined,
  store: { name: string; segment: string; slogan: string | null; city: string | null; state: string | null }
): { hasDrift: boolean; fields: string[] }
```

Regras:
- Se snapshot for null/undefined → hasDrift: true, fields: [] (missing_metadata semantics)
- Determinar critical fields via getDriftPolicy('visual_signature', contentUsed).critical
- Propriedades ausentes no snapshot → skip (não comparar)
- Usar normalizeSnapshotValue para string comparison (null → '')
- Retornar hasDrift + array de campos que divergem
</action>
<acceptance_criteria>
- Snapshot null → hasDrift: true
- Todos campos batem → hasDrift: false
- name diverge → hasDrift: true, fields includes 'name'
- segment diverge → fields includes 'segment'
- contentUsed.slogan=false + slogan diverge → slogan NOT in fields
- Propriedade ausente no snapshot → skip, não dispara drift
- `npm run typecheck` passa
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/drift.ts
src/lib/snapshot.ts
</read_first>
<action>
Implementar `evaluateSensitiveDrift` em `src/lib/drift.ts`:

```typescript
export function evaluateSensitiveDrift(
  bpSnapshot: Partial<StoreProfileInputSnapshot> | null | undefined,
  store: { segment: string; subsegment: string | null; tone_of_voice: string | null; name: string; positioning: string | null; short_description: string | null; slogan: string | null },
  fields: readonly string[]
): { hasDrift: boolean; fields: string[] }
```

Regras:
- Se snapshot null → hasDrift: false (não há base para comparação)
- Comparar apenas os campos em `fields` (fornecido pelo caller)
- Propriedades ausentes no snapshot → skip
- Usar normalizeSnapshotValue
</action>
<acceptance_criteria>
- Snapshot null → hasDrift: false
- fields=['segment'] + segment diverge → hasDrift: true, fields=['segment']
- fields=['positioning'] + positioning diverge → hasDrift: true
- Propriedade ausente → skip
- `npm run typecheck` passa
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/drift.ts
</read_first>
<action>
Atualizar `computeDriftStatus` para aceitar `fields: readonly string[]` obrigatório:

```typescript
export function computeDriftStatus(
  current: DriftSnapshot,
  inputSnapshot: Partial<StoreProfileInputSnapshot> | null | undefined,
  dismissedSnapshot: Partial<StoreProfileInputSnapshot> | null | undefined,
  fields: readonly string[],
): DriftStatus
```

Remover referência interna a DRIFT_FIELDS. Usar o parâmetro `fields` para todas as comparações.

Props ausentes em inputSnapshot → skip nesse campo (não comparar, valor tratado como undefined).
</action>
<acceptance_criteria>
- computeDriftStatus aceita 4 parâmetros (fields é obrigatório, sem default)
- Usa fields em vez de DRIFT_FIELDS
- Propriedade ausente em snapshot skipada
- Todos os testes de regressão (Onda 1) adaptados e ainda passam
- `npm run typecheck` passa
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/components/flow/use-drift-detection.ts
src/components/flow/store-identity-form.tsx
src/lib/__tests__/drift.test.ts
</read_first>
<action>
Migrate all callers of `computeDriftStatus` to pass `fields` explicitly:

1. **`src/components/flow/use-drift-detection.ts`** — calls to `computeDriftStatus` must pass `getDriftPolicy(identityState).sensitive` as the fields argument
2. **`src/components/flow/store-identity-form.tsx`** — any call to `computeDriftStatus` or drift comparison must use the new signature
3. **`src/lib/__tests__/drift.test.ts`** — all existing test calls to `computeDriftStatus` updated to include `fields` argument

The `DRIFT_FIELDS` constant no longer exists — every caller must derive fields from `getDriftPolicy()`.
This task is MANDATORY: without it, `npm run typecheck` fails because the function signature changed.
</action>
<acceptance_criteria>
- `use-drift-detection.ts` passes fields to computeDriftStatus
- `store-identity-form.tsx` passes fields to computeDriftStatus
- Tests pass fields argument
- No remaining references to `DRIFT_FIELDS` in callers
- `npm run typecheck` passa com 0 errors
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/visual-signature/drift-validator.ts
</read_first>
<action>
Expandir `src/lib/visual-signature/drift-validator.ts` para tratar props ausentes:

1. Em todas as comparações individuais (name, segment, city, state, slogan), verificar se a propriedade existe no snapshot:
   - Se snapshot[key] === undefined → skip (não comparar)
   - Se snapshot[key] !== undefined → comparar normalmente

2. Se TODAS as props relevantes forem undefined → has_drift: false, reason: 'ok' (não temos base para drift)
</action>
<acceptance_criteria>
- Propriedade ausente → não incluída na comparação
- Todas props ausentes → has_drift: false, reason: 'ok'
- Props existentes → comportamento inalterado
- Testes existentes (Onda 1) adaptados e passam
- `npm run typecheck` passa
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/visual-signature/types.ts
src/lib/drift.ts
src/lib/visual-signature/drift-validator.ts
</read_first>
<action>
Criar `src/lib/visual-signature/drift-revalidator.ts`:

Função server-side que revalida drift crítico contra a VS ativa:

```typescript
import { evaluateCriticalDrift } from '@/lib/drift';
import type { VisualSignatureMetadataInputSnapshot } from '@/lib/visual-signature/types';

export interface DriftRevalidationInput {
  vsSnapshot: Partial<VisualSignatureMetadataInputSnapshot> | null | undefined;
  contentUsed: { slogan?: boolean; city?: boolean; state?: boolean } | null | undefined;
  store: { name: string; segment: string; slogan: string | null; city: string | null; state: string | null };
}

export interface DriftRevalidationResult {
  hasDrift: boolean;
  fields: string[];
  reason: 'critical_drift' | 'missing_metadata' | 'ok';
}

export function revalidateCriticalDrift(input: DriftRevalidationInput): DriftRevalidationResult
```

Delega para evaluateCriticalDrift e mapeia o resultado para o formato com reason.
- vsSnapshot ausente → reason: 'missing_metadata'
- evaluateCriticalDrift.hasDrift → reason: 'critical_drift'
- Sem drift → reason: 'ok'
</action>
<acceptance_criteria>
- revalidateCriticalDrift retorna resultado tipado
- Delega para evaluateCriticalDrift corretamente
- Snapshot ausente → 'missing_metadata'
- `npm run typecheck` passa
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/__tests__/drift.test.ts
</read_first>
<action>
Atualizar testes em `src/lib/__tests__/drift.test.ts` para cobrir:

1. `getDriftPolicy` — 3 estados + condicionais
2. `evaluateCriticalDrift` — drift presente/ausente, content_used, props ausentes
3. `evaluateSensitiveDrift` — field list, props ausentes
4. `computeDriftStatus` — fields param, props ausentes
</action>
<acceptance_criteria>
- Todos os novos testes passam
- `npx vitest run src/lib/__tests__/drift.test.ts` exits 0
- Cobertura de getDriftPolicy, evaluateCriticalDrift, evaluateSensitiveDrift
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/lib/visual-signature/__tests__/drift-revalidator.test.ts
</read_first>
<action>
Criar testes para `drift-revalidator.ts`:

1. Revalidação com drift → reason: 'critical_drift', fields populados
2. Revalidação sem drift → reason: 'ok', hasDrift: false
3. Snapshot ausente → reason: 'missing_metadata'
</action>
<acceptance_criteria>
- `npx vitest run src/lib/visual-signature/__tests__/drift-revalidator.test.ts` exits 0
- Todos os 3 cenários cobertos
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
</read_first>
<action>
Wave 2 verification gate:

```bash
npm run typecheck
npm run lint
npx vitest run
```

Smoke test manual:
- `getDriftPolicy('text_only')` → 7 sensitive, 0 critical
- `getDriftPolicy('logo')` → 6 sensitive, 0 critical
- `getDriftPolicy('visual_signature', {slogan:true})` → 4 sensitive, 3 critical (name, segment, slogan)
- `evaluateCriticalDrift` com snapshot parcial → skip em campos ausentes
- `computeDriftStatus` com `fields` param → compara apenas fields fornecidos
- `revalidateCriticalDrift` com snapshot null → reason: 'missing_metadata'
</action>
<acceptance_criteria>
- typecheck: 0 errors
- lint: 0 errors
- vitest: all tests pass
- Smoke: 6 cenários verificados manualmente
</acceptance_criteria>
</task>

</tasks>

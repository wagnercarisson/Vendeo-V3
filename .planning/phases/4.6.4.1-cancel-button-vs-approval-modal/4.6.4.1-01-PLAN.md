---
phase: 4.6.4.1-cancel-button-vs-approval-modal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/flow/visual-signature-approval-modal.tsx
  - src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "handleContinueWithoutLogo renamed to handleCancel, calls only onClose()"
    - "handleCancel does NOT fetch(PATCH /logo-status), does NOT setState phase:done, does NOT call onComplete"
    - "Secondary button label in error phase is 'Cancelar' (not 'Continuar sem logo/assinatura')"
    - "Primary button label still uses state.drift: 'Ajustar assinatura' if present, else 'Tentar novamente'"
    - "In generate-without-logo failure path: mode='substitution' does NOT set logo_status='failed'"
    - "In generate-without-logo failure path: isStorageError still preserves logo_status"
    - "In generate-without-logo failure path: mode='standard' + non-storage error STILL sets logo_status='failed'"
    - "npm run typecheck passes with no errors"
    - "npm run lint passes with no errors"
  artifacts:
    - path: "src/components/flow/visual-signature-approval-modal.tsx"
      provides: "handleCancel replaces handleContinueWithoutLogo; button label updated"
    - path: "src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts"
      provides: "Failure path conditional on mode for logo_status"
  key_links:
    - from: "visual-signature-approval-modal.tsx:411"
      to: "handleCancel (replaces handleContinueWithoutLogo)"
      via: "onClick handler for secondary button in error phase"
    - from: "generate-without-logo/route.ts:402"
      to: "mode check before setting logo_status='failed'"
      via: "Failure path guard"
---

<objective>
**Plan 01: Cancelar Button & Substitution Mode Protection — Wave 1**

Substituir o botão "Continuar sem logo/assinatura" na fase error do `VisualSignatureApprovalModal` por um "Cancelar" que apenas fecha o modal. Proteger `logo_status` contra sobrescrita em falhas de substitution mode no backend.

**Purpose:** Corrigir a violação de sincronização entre `identity_state` e `logo_status` causada pelo PATCH indevido, e evitar sobrescrita de `logo_status` em substitution mode.
**Output:** Modal com botão "Cancelar" funcional + backend preservando `logo_status` em substitution mode.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/4.6.4.1-cancel-button-vs-approval-modal/4.6.4.1-CONTEXT.md
@openspec/changes/fase-4-6-4-1-cancel-button-vs-approval-modal/design.md
@openspec/changes/fase-4-6-4-1-cancel-button-vs-approval-modal/specs/visual-signature-approval/spec.md
@openspec/changes/fase-4-6-4-1-cancel-button-vs-approval-modal/specs/store-visual-signature/spec.md
@openspec/changes/fase-4-6-4-1-cancel-button-vs-approval-modal/tasks.md

<existing_patterns>
See `src/components/flow/visual-signature-approval-modal.tsx:411-422` for existing handleContinueWithoutLogo
See `src/components/flow/visual-signature-approval-modal.tsx:788-801` for error phase button rendering
See `src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts:59-68` for mode parsing
See `src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts:394-408` for failure path
</existing_patterns>
</context>

<tasks>

<task type="execute">
<read_first>
src/components/flow/visual-signature-approval-modal.tsx
openspec/changes/fase-4-6-4-1-cancel-button-vs-approval-modal/specs/visual-signature-approval/spec.md
</read_first>
<action>
In `src/components/flow/visual-signature-approval-modal.tsx`:

1. Rename `handleContinueWithoutLogo` (line 411) to `handleCancel`. Replace implementation:
   - Remove `fetch(PATCH /api/store/${storeId}/logo-status)` block
   - Remove `setState({ phase: "done", logoStatus: "explicit_none" })`
   - Remove `onComplete({ logoStatus: "explicit_none" })`
   - Keep only: `onClose()` wrapped in `useCallback` with `[onClose]` deps

2. Update secondary button label (line 800): change `{state.drift ? "Continuar sem assinatura" : "Continuar sem logo"}` to just `"Cancelar"`

3. Update onClick handler (line 797): change `onClick={handleContinueWithoutLogo}` to `onClick={handleCancel}`

4. Verify primary button label (line 793): `{state.drift ? "Ajustar assinatura" : "Tentar novamente"}` — confirm it already uses `state.drift` as condition. If not, fix to this exact expression.
</action>
<acceptance_criteria>
- `handleContinueWithoutLogo` no longer exists in the file (replaced by `handleCancel`)
- `handleCancel` contains only `onClose()` call
- Secondary button text is exactly `"Cancelar"` (no conditional, no other text)
- Primary button text is `"Ajustar assinatura"` when state.drift is truthy, `"Tentar novamente"` otherwise
- `npm run typecheck` passes with no errors
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts
openspec/changes/fase-4-6-4-1-cancel-button-vs-approval-modal/specs/store-visual-signature/spec.md
</read_first>
<action>
In `src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts`, modify the failure path block (lines 402-408):

Current code:
```typescript
if (!isStorageError) {
    console.log(`[generate-without-logo][req-${reqId}] setando logo_status=failed...`);
    await supabase
      .from('stores')
      .update({ logo_status: 'failed', updated_at: new Date().toISOString() })
      .eq('id', id);
}
```

Replace with:
```typescript
if (mode !== 'substitution' && !isStorageError) {
    console.log(`[generate-without-logo][req-${reqId}] setando logo_status=failed...`);
    await supabase
      .from('stores')
      .update({ logo_status: 'failed', updated_at: new Date().toISOString() })
      .eq('id', id);
}
```

The `mode` variable is already in scope (defined at line 68 as `const mode = body.mode ?? 'standard'`). This guards:
- `mode='substitution'` + any error → logo_status NOT modified (VS ativa permanece ativa)
- `mode='standard'` + storage error → logo_status NOT modified (infrastructure failure)
- `mode='standard'` + non-storage error → logo_status='failed' (correct existing behavior)
</action>
<acceptance_criteria>
- Failure path condition changed from `!isStorageError` to `mode !== 'substitution' && !isStorageError`
- When `mode='substitution'` and generation fails, `logo_status` is NOT set to `'failed'`
- When `mode='standard'` and non-storage error, `logo_status` IS set to `'failed'`
- When `isStorageError` is true (any mode), `logo_status` is NOT modified
- `npm run typecheck` passes with no errors
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
src/components/flow/visual-signature-approval-modal.tsx
</read_first>
<action>
Verify references to `/logo-status` and `handleContinueWithoutLogo` inside the `VisualSignatureApprovalModal` component:

1. Search the file for any remaining references to:
   - `logo-status` (in fetch URLs or strings)
   - `handleContinueWithoutLogo` (function name)

2. Only occurrences should be:
   - `handleCancel` (the renamed function)
   - Zero references to `logo-status` in API calls within this file
   - Zero references to `handleContinueWithoutLogo`

3. Do NOT search for occurrences in `store-identity-form.tsx` — the other `handleContinueWithoutLogo` there is outside scope.
</action>
<acceptance_criteria>
- No `logo-status` string in fetch calls within `visual-signature-approval-modal.tsx`
- No `handleContinueWithoutLogo` identifier within `visual-signature-approval-modal.tsx`
- `handleCancel` is the only handler for the secondary button
</acceptance_criteria>
</task>

<task type="execute">
<read_first>
</read_first>
<action>
Run verification commands:

```bash
npm run typecheck
npm run lint
```

Both must pass with zero errors before proceeding.
</action>
<acceptance_criteria>
- `npm run typecheck` exits 0
- `npm run lint` exits 0
</acceptance_criteria>
</task>

</tasks>

<quality_gate>
- [ ] handleCancel calls only onClose()
- [ ] Secondary button label is "Cancelar"
- [ ] Primary button label uses state.drift
- [ ] No remaining PATCH /logo-status calls in modal
- [ ] Substitution mode failure does not set logo_status='failed'
- [ ] Standard mode failure still sets logo_status='failed'
- [ ] npm run typecheck passes
- [ ] npm run lint passes
</quality_gate>

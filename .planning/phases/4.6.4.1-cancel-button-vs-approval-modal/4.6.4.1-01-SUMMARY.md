# Plan 01 Summary — Cancelar Button & Substitution Mode Protection

**Phase:** 4.6.4.1 — Cancel Button vs Approval Modal
**Plan:** 01 — Cancelar button & substitution mode protection
**Wave:** 1
**Status:** Complete

## Changes Made

### 1. `src/components/flow/visual-signature-approval-modal.tsx`
- **Replaced `handleContinueWithoutLogo`** with `handleCancel` — new function calls only `onClose()`, removing:
  - `fetch(PATCH /api/store/${storeId}/logo-status)` block
  - `setState({ phase: "done", logoStatus: "explicit_none" })`
  - `onComplete({ logoStatus: "explicit_none" })`
- **Updated secondary button label** in the error phase from conditional `"Continuar sem assinatura" / "Continuar sem logo"` to static `"Cancelar"`
- **Updated onClick handler** from `handleContinueWithoutLogo` to `handleCancel`
- Verified primary button already uses `state.drift` for label: `"Ajustar assinatura"` / `"Tentar novamente"` ✓

### 2. `src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts`
- **Modified failure path guard** at line 402: changed `if (!isStorageError)` to `if (mode !== 'substitution' && !isStorageError)`
- This ensures:
  - `mode='substitution'` + any error → `logo_status` NOT modified (VS ativa permanece ativa)
  - `mode='standard'` + storage error → `logo_status` NOT modified
  - `mode='standard'` + non-storage error → `logo_status='failed'` (existing behavior preserved)

### 3. Verification
- `npm run typecheck` — PASS
- `npm run lint` — PASS
- Zero remaining references to `handleContinueWithoutLogo` or `logo-status` in the modal file

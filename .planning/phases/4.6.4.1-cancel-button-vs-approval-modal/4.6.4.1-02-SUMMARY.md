# Plan 02 Summary — Route Removal & Automated Tests

**Phase:** 4.6.4.1 — Cancel Button vs Approval Modal
**Plan:** 02 — Route removal & automated tests
**Wave:** 2
**Status:** Complete

## Changes Made

### 1. Route `/logo-status` removal
- Grep confirmed zero repo-internal consumers of `logo-status` in source files
- Removed `src/app/api/store/[id]/logo-status/route.ts` (80 lines)
- `npm run typecheck` passes after removal

### 2. Test infrastructure
- Installed `@testing-library/react`, `@testing-library/jest-dom`, `jsdom` as devDependencies
- Configured `oxc` JSX transform in `vitest.config.ts` for Vite 8 compatibility (`oxc: { jsx: { runtime: 'automatic' } }`)
- Renamed `visual-signature-approval-modal.test.ts` → `.test.tsx` with `// @vitest-environment jsdom` directive

### 3. Behavioral tests added (4 new tests)
- **"Cancelar" closes modal without mutations**: onClose called, onComplete NOT called, no new fetch
- **"Tentar novamente" makes new POST**: generate() called after retry click
- **Drift active shows "Ajustar assinatura"**: hasActiveSignatureDrift=true → correct label
- **Substitution mode + error shows "Tentar novamente"**: substitution error without drift

### 4. Parametrized route test (3 new scenarios in generate-without-logo)
- **Substitution failure — logo_status NOT modified** (count=0)
- **Standard + common error — logo_status = failed** (count≥1)
- **Standard + storage error — logo_status NOT modified, returns 503**

### 5. Verification
- `npm run typecheck` — PASS
- `npm test` — 275 tests, 24 test files, all PASS

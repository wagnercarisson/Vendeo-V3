---
status: complete
phase: 4.6.4.1-cancel-button-vs-approval-modal
source: 4.6.4.1-01-SUMMARY.md, 4.6.4.1-02-SUMMARY.md, 4.6.4.1-03-SUMMARY.md
started: 2026-07-02T12:57:48Z
updated: 2026-07-02T16:50:00Z
---

## Current Test

[testing complete]

## Tests

### 1. "Cancelar" button visible in error phase
expected: In the error state of VisualSignatureApprovalModal, the secondary button displays "Cancelar" instead of "Continuar sem logo" or "Continuar sem assinatura".
result: pass

### 2. "Cancelar" closes modal without side effects
expected: Clicking "Cancelar" closes the modal and returns to Step 2. No PATCH requests are made, no onComplete is called, and no state mutations persist beyond closing the modal.
result: pass

### 3. Primary button adapts to drift state
expected: When drift is present (state.drift exists), primary button shows "Ajustar assinatura". When drift is absent, primary button shows "Tentar novamente". Secondary button remains "Cancelar" in both cases.
result: pass

### 4. Substitution mode preserves logo_status on failure
expected: When VS generation fails in substitution mode, logo_status remains unchanged (VS stays active). No logo_status mutation occurs.
result: pass

### 5. Route /logo-status removed (404)
expected: GET or PATCH to /api/store/[id]/logo-status returns 404. Route no longer exists after removal.
result: pass

## Summary

total: 5
passed: 5
issues: 1
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Drift detection re-triggers when user reverts store name to original value"
  status: fixed
  reason: "User reported: alternating between previously-used names no longer triggers drift after dismiss+save cycle. Root cause: visual_signature_drift_dismissed_snapshot persisted indefinitely and matched future store states that user never re-dismissed."
  severity: major
  test: 0
  root_cause: "dismiss-critical-drift persisted store state as dismissedSnapshot. When user reverted to a previously-dismissed state, computeCriticalDrift returned 'dismissed' (via dismissedSnapshot match) instead of 'new'. The dismissed snapshot never expired."
  artifacts:
    - path: "src/app/api/store/[id]/visual-signature/dismiss-critical-drift/route.ts"
      issue: "Added DELETE handler to clear visual_signature_drift_dismissed_snapshot"
    - path: "src/components/flow/store-identity-form.tsx"
      issue: "Added cleanup logic in executeStep2Save — after save, if formData matches input_snapshot on critical/sensitive fields, fires best-effort DELETE/PATCH to clear stale dismissed snapshots"
  missing:
    - "executeStep2Save now clears stalled critical dismissed snapshot when data reverts to original (name + segment match input_snapshot)"
    - "executeStep2Save now clears stalled sensitive dismissed snapshot when sensitive fields (subsegment, tone_of_voice, positioning, short_description) match input_snapshot"

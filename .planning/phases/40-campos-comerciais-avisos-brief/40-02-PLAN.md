---
phase: 40-campos-comerciais-avisos-brief
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/campaign/constants.ts
  - src/components/campaign/illustrative-notice-field.tsx
  - src/components/campaign/mandatory-artwork-field.tsx
autonomous: true
requirements: [F40-01, F40-02]

must_haves:
  truths:
    - "A constante única ILLUSTRATIVE_NOTICE_TEXT = 'Imagem meramente ilustrativa' (singular) existe em módulo neutro sem server-only"
    - "O checkbox 'Exibir 'Imagem meramente ilustrativa'' é um componente controlado com default marcado definido no form state (não no componente)"
    - "O placeholder do textarea usa a constante (singular), mantendo maxLength 200"
    - "Nenhuma string solta singular×plural divergente no form"
  artifacts:
    - path: "src/lib/campaign/constants.ts"
      provides: "Constante única do aviso ilustrativo (módulo neutro)"
      contains: "ILLUSTRATIVE_NOTICE_TEXT"
    - path: "src/components/campaign/illustrative-notice-field.tsx"
      provides: "Checkbox controlado (checked/onChange)"
      contains: "IllustrativeNoticeField"
    - path: "src/components/campaign/mandatory-artwork-field.tsx"
      provides: "Placeholder normalizado para a constante"
      contains: "ILLUSTRATIVE_NOTICE_TEXT"
  key_links:
    - from: "src/components/campaign/illustrative-notice-field.tsx"
      to: "src/lib/campaign/constants.ts"
      via: "Label do checkbox referencia a constante"
      pattern: "ILLUSTRATIVE_NOTICE_TEXT"
    - from: "src/components/campaign/mandatory-artwork-field.tsx"
      to: "src/lib/campaign/constants.ts"
      via: "Placeholder usa a constante"
      pattern: "ILLUSTRATIVE_NOTICE_TEXT"
---

<objective>
Criar a **constante única** `ILLUSTRATIVE_NOTICE_TEXT = "Imagem meramente ilustrativa"` (singular — D2) em `src/lib/campaign/constants.ts` (módulo neutro, sem `server-only`, sem importar o builder/domínio do brief) + o **checkbox controlado** `IllustrativeNoticeField` (novo componente) + **normalizar o placeholder** do `MandatoryArtworkField` para usar a constante (remove "Ex: Imagens meramente ilustrativas" plural).

Purpose: D2 — quatro intenções distintas no form; o checkbox é controle real (default marcado definido no form state, NÃO no componente). A constante singular alinha prompts, fixtures e form.
Output: 3 arquivos (1 novo lib, 1 novo componente, 1 modificado) — todos referenciando a constante única.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/40-campos-comerciais-avisos-brief/40-CONTEXT.md (D2 — constante única singular; canonical_refs constantes/componentes)
@.planning/phases/40-campos-comerciais-avisos-brief/40-PATTERNS.md (seção constants.ts — export-const-with-literal; illustrative-notice-field — checkbox control pattern de campaign-input-form.tsx:488-501)
@openspec/changes/fase-40-campos-comerciais-avisos-brief/specs/illustrative-notice-control/spec.md (Requerimento: Constante única + Checkbox controle real)
@openspec/changes/fase-40-campos-comerciais-avisos-brief/tasks.md (seção 2 — 2.1 a 2.4)
@src/lib/constants.ts (análogo de módulo neutro export-const, linhas 1-17)
@src/components/campaign/mandatory-artwork-field.tsx (arquivo a modificar — placeholder linha 24)
@src/components/flow/campaign-input-form.tsx (checkbox preserveImageContext :488-501 — padrão de controle checkbox)
@openspec/design-system/pages/campaign-input.md (estilos do form)

<interfaces>
Contrato exato a criar (per tasks.md 2.1-2.3 + specs):

```typescript
// src/lib/campaign/constants.ts (NOVO — módulo neutro, único export)
export const ILLUSTRATIVE_NOTICE_TEXT = "Imagem meramente ilustrativa";
```
- SEM `"use client"`, SEM `import "server-only"`, SEM import de `@/lib/campaign/brief` ou `brief-schema`.
- Consumidores futuros: illustrative-notice-field.tsx (label), mandatory-artwork-field.tsx (placeholder), use-campaign-form.ts (concatenação D3), brief.test.ts (8.8), fixtures (10.2).

```tsx
// src/components/campaign/illustrative-notice-field.tsx (NOVO — checkbox controlado)
interface IllustrativeNoticeFieldProps {
  checked: boolean;
  onChange: (checked: boolean) => void;
}
export function IllustrativeNoticeField({ checked, onChange }: IllustrativeNoticeFieldProps) { ... }
```
- Label: "Exibir 'Imagem meramente ilustrativa'" — montado com `ILLUSTRATIVE_NOTICE_TEXT` (nunca literal solto).
- Default marcado NÃO é responsabilidade do componente — é do form state (`EMPTY_FIELDS.showIllustrativeNotice: true`, plano 40-04).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: constants.ts — constante única ILLUSTRATIVE_NOTICE_TEXT</name>
  <files>src/lib/campaign/constants.ts</files>
  <read_first>
    - src/lib/constants.ts (linhas 1-17 — estilo de módulo neutro: export const tipados, sem server-only, zero imports do domínio)
    - openspec/changes/fase-40-campos-comerciais-avisos-brief/specs/illustrative-notice-control/spec.md (Requerimento: Constante única — singular, alinhada ao UAT-3 e aos prompts)
  </read_first>
  <action>
    Criar `src/lib/campaign/constants.ts` como módulo neutro de constante (D2):

    1. ÚNICO export do arquivo: `export const ILLUSTRATIVE_NOTICE_TEXT = "Imagem meramente ilustrativa";` — exatamente o literal singular (sem "s" em "meramente"/"ilustrativa").
    2. SEM `"use client"`, SEM `import "server-only"`, SEM imports de `@/lib/campaign/brief` ou `brief-schema` (módulo neutro — o frontend importa sem arrastar zod/supabase).
    3. Sem funções, sem classes, sem side effects — apenas a constante (padrão `src/lib/constants.ts:1-17`).
    4. NÃO adicionar outras constantes neste arquivo (ex.: validade) — escopo mínimo.
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\lib\campaign\constants.ts" -Pattern "ILLUSTRATIVE_NOTICE_TEXT" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <acceptance_criteria>
    - `constants.ts` existe com `export const ILLUSTRATIVE_NOTICE_TEXT = "Imagem meramente ilustrativa";`
    - Grep `server-only` em constants.ts → 0 ocorrências; grep `brief` (import de domínio) → 0 ocorrências
    - `npx vitest run src/lib/campaign/__tests__/brief.test.ts` ainda passa (sem regressão — arquivo novo não quebra nada)
  </acceptance_criteria>
  <done>constants.ts criado — constante única singular em módulo neutro</done>
</task>

<task type="auto">
  <name>Task 2: illustrative-notice-field.tsx — checkbox controlado</name>
  <files>src/components/campaign/illustrative-notice-field.tsx</files>
  <read_first>
    - src/components/campaign/mandatory-artwork-field.tsx (linhas 1-31 — esqueleto de campo "use client" + label design system)
    - src/components/flow/campaign-input-form.tsx (linhas 488-501 — padrão canônico de checkbox: preserveImageContext)
    - src/lib/campaign/constants.ts (recém-criado — ILLUSTRATIVE_NOTICE_TEXT)
  </read_first>
  <action>
    Criar `src/components/campaign/illustrative-notice-field.tsx` — checkbox controlado (D2):

    1. `"use client";` no topo (padrão mandatory-artwork-field.tsx:1).
    2. Props: `{ checked: boolean; onChange: (checked: boolean) => void }` (exatamente a forma do bloco <interfaces>).
    3. Renderizar `<label className="flex items-start gap-3 cursor-pointer">` com `<input type="checkbox" checked={checked} onChange={(e) => onChange(e.target.checked)} className="mt-0.5 h-4 w-4 rounded border-border-light bg-bg-surface text-accent-green focus:ring-accent-green/20" />` (classes exatas do checkbox preserveImageContext — campaign-input-form.tsx:495) e `<span className="text-text-primary text-sm font-body">Exibir '{ILLUSTRATIVE_NOTICE_TEXT}'</span>` — montado com a constante, NUNCA literal solto.
    4. Importar `import { ILLUSTRATIVE_NOTICE_TEXT } from "@/lib/campaign/constants";`.
    5. NÃO incluir estado interno, NÃO definir default marcado no componente (default = responsabilidade do form state, plano 40-04).
  </action>
  <verify>
    <automated>npx vitest run src/lib/campaign/__tests__/brief.test.ts; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe com export `IllustrativeNoticeField`, props `{ checked, onChange }`
    - Contém `ILLUSTRATIVE_NOTICE_TEXT` no label (grep → presente) e NÃO contém "Imagens meramente" (plural) em lugar nenhum
    - Contém `type="checkbox"` e classes `text-accent-green`/`focus:ring-accent-green/20`
    - `npm run typecheck` limpo (arquivo compila sem erro)
  </acceptance_criteria>
  <done>illustrative-notice-field.tsx criado — checkbox controlado com label via constante</done>
</task>

<task type="auto">
  <name>Task 3: mandatory-artwork-field.tsx — placeholder normalizado para a constante</name>
  <files>src/components/campaign/mandatory-artwork-field.tsx</files>
  <read_first>
    - src/components/campaign/mandatory-artwork-field.tsx (linha 24 — placeholder atual "Ex: Imagens meramente ilustrativas"; linha 25 — maxLength={200})
    - src/lib/campaign/constants.ts (recém-criado)
  </read_first>
  <action>
    Modificar o placeholder do textarea (D2, task 2.3):

    1. Adicionar import: `import { ILLUSTRATIVE_NOTICE_TEXT } from "@/lib/campaign/constants";`
    2. Substituir a linha 24: `placeholder="Ex: Imagens meramente ilustrativas"` → `placeholder={`Ex: ${ILLUSTRATIVE_NOTICE_TEXT}`}` — o prefixo "Ex: " permanece; apenas a frase canônica muda de plural para singular via constante.
    3. MANTER `maxLength={200}` (linha 25), `rows={2}` (linha 26), classes (linha 27) e estrutura de label (linhas 11-19) intactas.
    4. Verificar nenhuma outra string solta singular×plural no arquivo (grep por "Imagens meramente ilustrativas" → deve restar apenas o placeholder antigo se não substituído; objetivo final: ZERO ocorrências de plural no arquivo).
  </action>
  <verify>
    <automated>
      if (Select-String -LiteralPath "src\components\campaign\mandatory-artwork-field.tsx" -Pattern "Imagens meramente ilustrativas" -Quiet) { Write-Error "Plural ainda presente" } else { Write-Output "OK: plural removido" }; npm run typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - `mandatory-artwork-field.tsx` contém `placeholder={`Ex: ${ILLUSTRATIVE_NOTICE_TEXT}`}` (grep por `Ex: \${ILLUSTRATIVE_NOTICE_TEXT}` → presente)
    - Contém `maxLength={200}` intacto
    - Grep `Imagens meramente ilustrativas` (plural) no arquivo → 0 ocorrências
    - `npm run typecheck` limpo
  </acceptance_criteria>
  <done>placeholder do textarea usa a constante singular; maxLength 200 mantido; zero plurais no arquivo</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| constants.ts → form components | Constante compartilhada consumida pelo frontend — deve ser módulo neutro (sem server-only) para não arrastar zod/supabase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-02 | Spoofing | Label do checkbox | mitigate | Label montado via `ILLUSTRATIVE_NOTICE_TEXT` (constante única) — nunca literal solto (teste 14 no plano 40-06 garante zero strings divergentes) |
| T-40-02a | Tampering | Módulo neutro | mitigate | constants.ts sem `server-only`, sem imports do domínio do brief — evita arrastar dependências para o client (D2) |
| T-40-02b | Information Disclosure | Constante no client | accept | Texto público de aviso legal — sem dado sensível |
| T-40-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo (módulo de constante) — sem gate de legitimidade |
</threat_model>

<verification>
- Grep: `Imagens meramente ilustrativas` em `src/components/campaign/` → 0 ocorrências após normalização
- Grep: `server-only` em `src/lib/campaign/constants.ts` → 0
- `npm run typecheck` limpo
- `npx vitest run src/lib/campaign/__tests__/brief.test.ts` passa (regressão mínima)
</verification>

<success_criteria>
- Constante única `ILLUSTRATIVE_NOTICE_TEXT = "Imagem meramente ilustrativa"` em módulo neutro sem server-only
- Checkbox `IllustrativeNoticeField` controlado com label via constante
- Placeholder do textarea normalizado (singular, via constante); maxLength 200 mantido
- Zero strings plurais divergentes em `src/components/campaign/`
</success_criteria>

<output>
Create `.planning/phases/40-campos-comerciais-avisos-brief/40-02-SUMMARY.md` when done
</output>

---
phase: 40-campos-comerciais-avisos-brief
plan: 04
type: execute
wave: 2
depends_on: [40-02]
files_modified:
  - src/components/flow/use-campaign-form.ts
  - src/components/flow/__tests__/use-campaign-form-navigation.test.ts
autonomous: true
requirements: [F40-03, F40-04, F40-07, F40-08, F40-16]

must_haves:
  truths:
    - "O form state (CampaignFormFields) carrega os 6 novos campos: showIllustrativeNotice (default true), mandatoryArtworkTextFree, validityMode, validityStartDate, validityEndDate, validityCustomText"
    - "mandatoryArtworkText PERMANECE em CampaignFormFields como campo compat/derivado (espelho de mandatoryArtworkTextFree, NUNCA o texto final concatenado — D3, 40-PATTERNS.md:171/218); campaign-input-form.tsx:503-506 segue compilando até o rewire na 40-05"
    - "Helpers puros buildMandatoryArtworkText, buildValidityDisplayText e formatDDMM são exportados do módulo e testáveis isoladamente"
    - "A concatenação do aviso acontece SOMENTE na montagem do body (nunca no estado do form); o campo compat é espelho do texto livre"
    - "Validade só entra no body quando campaignIntent === 'offer'; mudar a intenção NUNCA limpa os campos de validade"
    - "Draft legado (mandatoryArtworkText) é migrado para mandatoryArtworkTextFree no restore (com espelho mantido)"
  artifacts:
    - path: "src/components/flow/use-campaign-form.ts"
      provides: "Novo form state + helpers + body assembly (mandatoryArtworkText compat/derivado mantido)"
      contains: "buildMandatoryArtworkText|buildValidityDisplayText|formatDDMM|mandatoryArtworkTextFree"
    - path: "src/components/flow/use-campaign-form.ts"
      provides: "Migração de draft legado no restore + espelho bidirecional no setField"
      contains: "mandatoryArtworkTextFree"
    - path: "src/components/flow/__tests__/use-campaign-form-navigation.test.ts"
      provides: "Mocks co-migrados + teste de migração legada"
      contains: "showIllustrativeNotice"
  key_links:
    - from: "use-campaign-form.ts body (:625-638)"
      to: "src/lib/campaign/constants.ts"
      via: "buildMandatoryArtworkText importa ILLUSTRATIVE_NOTICE_TEXT"
      pattern: "ILLUSTRATIVE_NOTICE_TEXT"
    - from: "use-campaign-form.ts setField (:381-395)"
      to: "campaign-input-form.tsx (:504-505)"
      via: "espelho bidirecional mandatoryArtworkText ↔ mandatoryArtworkTextFree mantém o textarea legado funcionando até o rewire na 40-05"
      pattern: "mandatoryArtworkTextFree"
    - from: "use-campaign-form.ts restore (:252-274)"
      to: "sessionStorage draft legado"
      via: "campos legados com mandatoryArtworkText migram para mandatoryArtworkTextFree"
      pattern: "mandatoryArtworkText\\?"
---

<objective>
Expandir o **form state** de `use-campaign-form.ts` com os 6 novos campos comerciais (D3/D4): `showIllustrativeNotice` (default `true`), `mandatoryArtworkTextFree`, `validityMode`, `validityStartDate`, `validityEndDate`, `validityCustomText` — mantendo `mandatoryArtworkText: string` como campo **compat/derivado** (espelho de `mandatoryArtworkTextFree`, NUNCA o texto final concatenado — D3, 40-PATTERNS.md:171/218) para o `campaign-input-form.tsx:503-506` continuar compilando e funcionando até o rewire na 40-05. Adicionar os **helpers puros exportados** `buildMandatoryArtworkText`, `buildValidityDisplayText` e `formatDDMM` (lógica no hook para testes isolados; componente ValidityField fica presentacional no plano 40-05). Montar o body com concatenação do aviso e validade gated por intenção `offer`. Migrar draft legado no restore. Co-migrar os 3 mocks de restore do teste de navegação + adicionar teste de migração legada.

Purpose: D3 — aviso ilustrativo = checkbox (default marcado) + texto livre, concatenação só no body; D4 — validade em 6 modos com displayText "nu" (sem prefixo "Oferta válida") e endDate ISO nunca enviado.
Output: hook expandido com helpers exportados; body correto; testes de navegação verdes.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/40-campos-comerciais-avisos-brief/40-CONTEXT.md (D3 — form state separado, concatenação só no body; D4 — 6 modos, endDate ISO nunca enviado; D5 — displayText nu; D2 — constante)
@.planning/phases/40-campos-comerciais-avisos-brief/40-PATTERNS.md (helpers puros no hook; body assembly)
@openspec/changes/fase-40-campos-comerciais-avisos-brief/specs/offer-validity-modes/spec.md (6 modos + displayText)
@openspec/changes/fase-40-campos-comerciais-avisos-brief/specs/mandatory-artwork-text/spec.md (concatenação no body)
@openspec/changes/fase-40-campos-comerciais-avisos-brief/tasks.md (seção 3 — 3.1 a 3.6; seção 10.4 — co-migração nav test)
@src/components/flow/use-campaign-form.ts (LINHAS VERIFICADAS abaixo)
@src/components/flow/__tests__/use-campaign-form-navigation.test.ts (LINHAS VERIFICADAS abaixo)
@src/lib/campaign/constants.ts (recém-criado no 40-02 — ILLUSTRATIVE_NOTICE_TEXT)

<interfaces>
Estado atual verificado em use-campaign-form.ts (2026-08-14):

- CampaignFormFields: :75-85 (campos atuais; `mandatoryArtworkText: string` na :84) — **PERMANECE** como compat/derivado (D3, 40-PATTERNS.md:171)
- FieldErrors union: :87-100 (`"mandatoryArtworkText"` na :97) — MANTER + adicionar 6 novos
- EMPTY_FIELDS: :133-143 (`mandatoryArtworkText: ""` na :142) — MANTER + adicionar 6 novos
- touched inicial: :223-233 (`mandatoryArtworkText: false` na :232) — MANTER + adicionar 6 novos
- setField: :381-395 — ADICIONAR espelho bidirecional `mandatoryArtworkText` ↔ `mandatoryArtworkTextFree`
- restore useEffect: :252-274 (destructure `imageFile` na :256) — migração legada com espelho mantido
- allFields (validação no submit): :567-576 (`"mandatoryArtworkText"` na :575) — MANTER + adicionar 6 novos
- setTouched de erro: :589-599 (`mandatoryArtworkText: true` na :598) — MANTER + adicionar 6 novos
- body build: :625-638 (`mandatoryArtworkText: frozenFields.mandatoryArtworkText || undefined` na :636) — SUBSTITUIR por `buildMandatoryArtworkText(showNotice, freeText)`
- Intent switch: :296-337 — NÃO limpar campos de validade aqui (preservação D4)
- validateField: :188-206 — default retorna null; nenhuma mudança necessária

Contrato do campo compat/derivado (D3 — 40-PATTERNS.md:171/218):

- `CampaignFormFields.mandatoryArtworkText: string` é **espelho** de `mandatoryArtworkTextFree` — nunca o texto final concatenado.
- No `setField`, gravar `"mandatoryArtworkText"` também grava `mandatoryArtworkTextFree` (e vice-versa) — o textarea legado (campaign-input-form.tsx:504-505, ainda ligado ao campo antigo até a 40-05) continua editando o texto que o body usa.
- `showIllustrativeNotice` permanece separado (boolean) — participa apenas na montagem do body via `buildMandatoryArtworkText`.
- Autosave/restore serializam o objeto inteiro: o espelho grava o texto livre (não concatenado) — a intenção do checkbox é preservada pelo boolean salvo separado (D3).

Contrato de helpers a criar (exportados do módulo, em escopo module-level):

```typescript
export type ValidityMode = "" | "until-date" | "range" | "today" | "stock" | "custom";

export function formatDDMM(isoDate: string): string;
// "2026-09-30" -> "30/09"; "" ou inválido -> "" (retorna input se não tiver 3 partes)

export function buildValidityDisplayText(fields: {
  validityMode: ValidityMode;
  validityStartDate: string;
  validityEndDate: string;
  validityCustomText: string;
}): string | undefined;
// ""          -> undefined
// "until-date"-> "até 30/09" (endDate vazio -> undefined)
// "range"     -> "de 25/09 até 30/09" (alguma data vazia -> undefined)
// "today"     -> "somente hoje"
// "stock"     -> "enquanto durarem os estoques"
// "custom"    -> trim do texto com prefixo /^Oferta válida[:\s-]*/i removido; vazio -> undefined

export function buildMandatoryArtworkText(showNotice: boolean, freeText: string): string | undefined;
// show=true, free="X"  -> "Imagem meramente ilustrativa\nX"
// show=true, free=""   -> "Imagem meramente ilustrativa"
// show=false, free="X" -> "X"
// show=false, free=""  -> undefined
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Form state expandido + helpers exportados + migração de draft legado</name>
  <files>src/components/flow/use-campaign-form.ts</files>
  <read_first>
    - src/components/flow/use-campaign-form.ts (linhas 1-25 imports; :75-100 tipos; :133-143 EMPTY_FIELDS; :220-233 touched; :252-274 restore; :296-337 intent switch)
  </read_first>
  <action>
    Expandir `use-campaign-form.ts` conforme D3/D4 (per tasks 3.1-3.4):

    1. **Imports:** adicionar `import { ILLUSTRATIVE_NOTICE_TEXT } from "@/lib/campaign/constants";` junto aos imports existentes (linhas 1-25).

    2. **CampaignFormFields (:75-85):** MANTER `mandatoryArtworkText: string;` (linha 84) como **compat/derivado** — com comentário `// compat/derivado: espelho de mandatoryArtworkTextFree, NUNCA o texto final concatenado (D3)` — e ADICIONAR:
       ```
       showIllustrativeNotice: boolean;
       mandatoryArtworkTextFree: string;
       validityMode: ValidityMode;
       validityStartDate: string;
       validityEndDate: string;
       validityCustomText: string;
       ```

    3. **Novo tipo exportado** (antes de CampaignFormFields): `export type ValidityMode = "" | "until-date" | "range" | "today" | "stock" | "custom";`

    4. **Helpers exportados** (escopo module-level, junto a validateField/inferIntent):
       - `formatDDMM(isoDate: string): string` — split("-"), retorna "DD/MM", "" se entrada vazia, entrada original se não tiver 3 partes.
       - `buildValidityDisplayText(fields)` — switch exato do bloco <interfaces> (naked, SEM prefixo "Oferta válida"; custom normaliza via `replace(/^Oferta válida[:\s-]*/i, "")`; undefined quando incompleto).
       - `buildMandatoryArtworkText(showNotice, freeText)` — 4 casos exatos do <interfaces>, concatenação com `\n` quando ambos.
       - Importar `ILLUSTRATIVE_NOTICE_TEXT` para o caso "só constante".

    5. **FieldErrors (:87-100):** MANTER `"mandatoryArtworkText"` e ADICIONAR os 6 novos nomes de campo (sem novos requireds — validação default retorna null).

    6. **EMPTY_FIELDS (:133-143):** MANTER `mandatoryArtworkText: ""` (compat, espelho do texto livre) e ADICIONAR:
       ```
       showIllustrativeNotice: true,
       mandatoryArtworkTextFree: "",
       validityMode: "",
       validityStartDate: "",
       validityEndDate: "",
       validityCustomText: "",
       ```

    7. **touched inicial (:223-233):** MANTER `mandatoryArtworkText: false` e ADICIONAR os 6 novos campos (todos `false`).

    8. **setField (:381-395) — espelho compat/derivado (D3):** no `setFields((prev) => ...)` do `setField`, após montar `next`, adicionar:
       ```
       const next = { ...prev, [field]: value as never };
       if (field === "mandatoryArtworkText") next.mandatoryArtworkTextFree = value as string;
       if (field === "mandatoryArtworkTextFree") next.mandatoryArtworkText = value as string;
       ```
       (o textarea legado em campaign-input-form.tsx:505 chama `setField("mandatoryArtworkText", v)` — o espelho garante que o body montado em `handleSubmit`, que lê `mandatoryArtworkTextFree`, recebe o texto digitado; a recíproca mantém o campo antigo coerente após o rewire da 40-05)

    9. **Migração de draft legado no restore (:252-274):** substituir o destructure `const { imageFile: _, ...rest } = saved;` por:
       ```
       const legacy = saved as CampaignFormFields & { mandatoryArtworkText?: string };
       const { imageFile: _, mandatoryArtworkText: legacyNotice, ...rest } = legacy;
       const migrated = legacyNotice && !rest.mandatoryArtworkTextFree
         ? { ...rest, mandatoryArtworkTextFree: legacyNotice, mandatoryArtworkText: legacyNotice }
         : rest;
       setFields((prev) => ({ ...prev, ...migrated, imageFile: null }));
       ```
       (campos legados de sessões anteriores com `mandatoryArtworkText` preenchido migram para o campo livre — o espelho `mandatoryArtworkText` também é gravado para o textarea legado exibir o texto restaurado; caso contrário state preservado intacto)

    10. **Intent switch (:296-337):** NENHUMA alteração — validade é preservada ao trocar de intenção (D4: preservar draft, nunca limpar).

    11. **allFields (:567-576) e setTouched de erro (:589-599):** MANTER `"mandatoryArtworkText"` / `mandatoryArtworkText: true` e ADICIONAR os 6 novos campos.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` limpo (campaign-input-form.tsx:503-506 continua lendo `fields.mandatoryArtworkText` sem erro — campo compat mantido)
    - `CampaignFormFields` contém `mandatoryArtworkText: string` (compat/derivado) + os 6 novos campos
    - Grep `if (field === "mandatoryArtworkText")` no `setField` → presente (espelho bidirecional)
    - Grep `export function buildMandatoryArtworkText|export function buildValidityDisplayText|export function formatDDMM|export type ValidityMode` → 4 ocorrências
    - EMPTY_FIELDS tem `showIllustrativeNotice: true` E `mandatoryArtworkText: ""`
    - Restore tem o bloco `const legacy = saved as ...` com migração (grava free + espelho)
  </acceptance_criteria>
  <done>hook expandido com 6 campos novos, helpers exportados, migração legada e intenção preservadora</done>
</task>

<task type="auto">
  <name>Task 2: Body assembly + co-migração dos testes de navegação</name>
  <files>src/components/flow/use-campaign-form.ts, src/components/flow/__tests__/use-campaign-form-navigation.test.ts</files>
  <read_first>
    - src/components/flow/use-campaign-form.ts (linhas 610-646 — handleSubmit/body)
    - src/components/flow/__tests__/use-campaign-form-navigation.test.ts (linhas 61-71, 100-110, 140-150 — 3 mocks de restore)
  </read_first>
  <action>
    Montagem do body (D3/D4) + co-migração do teste de navegação (10.4):

    1. **Body (:625-638):** substituir o bloco por:
       ```
       const validity = frozenFields.campaignIntent === "offer"
         ? buildValidityDisplayText(frozenFields)
         : undefined;
       const mandatoryArtworkText = buildMandatoryArtworkText(
         frozenFields.showIllustrativeNotice,
         frozenFields.mandatoryArtworkTextFree,
       );

       const body: Record<string, unknown> = {
         storeId,
         productName: frozenFields.productName,
         originalPriceCents: frozenFields.originalPriceCents,
         discountedPriceCents: frozenFields.discountedPriceCents,
         description: frozenFields.description || undefined,
         badgeText: frozenFields.badge,
         campaignIntent: frozenFields.campaignIntent,
         ...(frozenFields.campaignIntent === "offer"
           ? {}
           : { preserveImageContext: frozenFields.preserveImageContext }),
         ...(validity !== undefined ? { validity } : {}),
         ...(mandatoryArtworkText !== undefined ? { mandatoryArtworkText } : {}),
         productImageDataUrl: imageDataUrl,
       };
        ```
        - `validity` APENAS quando intent `offer` (chamada gated no call site); endDate NUNCA vai no body (só via displayText "nu").
        - `mandatoryArtworkText` entra somente quando definido (concordância com os testes 8.1-8.4 de concatenação) — o valor vem do helper, NUNCA de `frozenFields.mandatoryArtworkText` (o campo compat permanece no state apenas como espelho).

    2. **Co-migração nav test (:61-71, :100-110, :140-150):** nos 3 `mockRestoreFormState.mockReturnValue({...})`, MANTER a linha `mandatoryArtworkText: ""` (campo compat permanece no shape completo) e ADICIONAR após ela:
       ```
       showIllustrativeNotice: true,
       mandatoryArtworkTextFree: "",
       validityMode: "",
       validityStartDate: "",
       validityEndDate: "",
       validityCustomText: "",
       ```

    3. **Novo teste de migração legada** (adicionar ao describe do arquivo): `"migrates legacy mandatoryArtworkText draft into free text field on restore"` — `mockRestoreFormState.mockReturnValue({ ...fields base, mandatoryArtworkText: "Legado" })` (SEM os campos novos, shape antigo), renderizar hook, `await act(async () => {})`, e asserir `result.current.fields.mandatoryArtworkTextFree === "Legado"`, `result.current.fields.mandatoryArtworkText === "Legado"` (espelho) e `result.current.fields.showIllustrativeNotice === true`.
  </action>
  <verify>
    <automated>npx vitest run src/components/flow/__tests__/use-campaign-form-navigation.test.ts; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Body tem spread condicional `...(validity !== undefined ? { validity } : {})` e `...(mandatoryArtworkText !== undefined ? { mandatoryArtworkText } : {})` (valor vindo de `buildMandatoryArtworkText`, NUNCA de `frozenFields.mandatoryArtworkText`)
    - Grep `frozenFields.mandatoryArtworkText || undefined` → 0 (concatenação antiga removida do body)
    - 3 mocks do nav test com `mandatoryArtworkText: ""` mantido + 6 campos novos (shape completo do CampaignFormFields)
    - Novo teste de migração legada presente e passando (free + espelho); suíte inteira do nav test verde
    - `npm run typecheck` limpo
  </acceptance_criteria>
  <done>body montado com validade gated por offer e aviso concatenado; nav tests co-migrados + teste de migração legada verde</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sessionStorage (draft legado) → restore | Dados não validados restaurados para o form state |
| form state → body (API) | Campos construídos pelo hook enviados à rota de geração |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-04 | Spoofing | Draft legado no restore | mitigate | Migração copia `mandatoryArtworkText` → `mandatoryArtworkTextFree` + espelho; o resto do state preservado intacto (sem overwrite de campos novos); espelho bidirecional no `setField` impede divergência campo antigo × novo; teste de migração legada cobre o caso |
| T-40-04a | Tampering | Body da API | mitigate | `validity` só montado para intent offer no call site; `endDate` ISO nunca enviado (D4); `mandatoryArtworkText` via helper puro (1 única fonte de concatenação) |
| T-40-04b | Information Disclosure | DisplayText custom | mitigate | Prefixo `Oferta válida` normalizado/removido antes do envio (D5) — evita duplicação de prefixo no prompt |
| T-40-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo — sem gate de legitimidade |
</threat_model>

<verification>
- `npm run typecheck` limpo (campaign-input-form.tsx:503-506 compila — campo compat mantido)
- `npx vitest run src/components/flow/__tests__/use-campaign-form-navigation.test.ts` verde (4 testes)
- Grep `frozenFields.mandatoryArtworkText` (leitura do campo no body) → 0 (concatenação só via helper)
- Grep `endDate` no body → 0 (nunca enviado)
- Grep `mandatoryArtworkText:` em EMPTY_FIELDS/mocks → presente (campo compat no shape)
</verification>

<success_criteria>
- 6 novos campos no CampaignFormFields/FieldErrors/EMPTY_FIELDS/touched com defaults corretos (showIllustrativeNotice default true); `mandatoryArtworkText` mantido como compat/derivado (espelho)
- 3 helpers puros exportados com comportamento exato do contrato
- Body: validade gated por offer, aviso concatenado no body (via helper, nunca do estado), endDate ausente
- Draft legado migrado no restore (free + espelho); intent switch preserva validade
- Nav tests verdes (3 co-migrados + 1 novo de migração legada); typecheck verde com campaign-input-form.tsx intocado
</success_criteria>

<output>
Create `.planning/phases/40-campos-comerciais-avisos-brief/40-04-SUMMARY.md` when done
</output>
---
phase: 40-campos-comerciais-avisos-brief
plan: 05
type: execute
wave: 3
depends_on: [40-02, 40-04]
files_modified:
  - src/components/campaign/validity-field.tsx
  - src/components/flow/campaign-input-form.tsx
  - src/app/(app)/campanhas/nova/__tests__/campaign-flow-credits.test.tsx
autonomous: true
requirements: [F40-01, F40-05, F40-06, F40-07, F40-09, F40-10, F40-16]

must_haves:
  truths:
    - "ValidityField é um componente presentacional puro: recebe mode/startDate/endDate/customText + callbacks onModeChange/onStartDateChange/onEndDateChange/onCustomTextChange e NÃO contém estado interno"
    - "O formulário exibe a seção 'Validade da oferta' apenas quando campaignIntent === 'offer'"
    - "O form tem as 3 seções agrupadas (Produto / Oferta / Avisos e texto obrigatório) com headers visuais"
    - "MandatoryArtworkField está conectado a fields.mandatoryArtworkTextFree e IllustrativeNoticeField a fields.showIllustrativeNotice"
    - "Os mocks do teste de crédito cobrem os novos componentes e campos sem quebrar"
  artifacts:
    - path: "src/components/campaign/validity-field.tsx"
      provides: "Componente presentacional de validade (6 modos)"
      contains: "ValidityField"
    - path: "src/components/flow/campaign-input-form.tsx"
      provides: "Wiring dos novos campos + seções D8"
      contains: "IllustrativeNoticeField|ValidityField|mandatoryArtworkTextFree"
    - path: "src/app/(app)/campanhas/nova/__tests__/campaign-flow-credits.test.tsx"
      provides: "Mocks co-migrados (10.5)"
      contains: "illustrative-notice-field|validity-field"
  key_links:
    - from: "campaign-input-form.tsx (:503-506)"
      to: "use-campaign-form.ts fields"
      via: "MandatoryArtworkField agora ligado a mandatoryArtworkTextFree (não mais mandatoryArtworkText)"
      pattern: "mandatoryArtworkTextFree"
    - from: "campaign-input-form.tsx"
      to: "validity-field.tsx"
      via: "ValidityField recebe fields.validityMode/StartDate/EndDate/CustomText e emite setField"
      pattern: "ValidityField"
---

<objective>
Criar o componente **presentacional** `ValidityField` (6 modos de validade da oferta) e **conectar a UI do form** aos novos campos do hook (D8): agrupar o formulário em 3 seções — **Produto** (nome, imagem, descrição), **Oferta** (preços, selo, intenção, preservar imagem, validade — só quando `offer`) e **Avisos e texto obrigatório** (checkbox ilustrativo + texto obrigatório). Conectar `MandatoryArtworkField` ao novo `fields.mandatoryArtworkTextFree` e `IllustrativeNoticeField` a `fields.showIllustrativeNotice`. Co-migrar os mocks do teste de crédito (10.5). **O campo `mandatoryArtworkText` da interface `CampaignFormFields` PERMANECE** como compat/derivado (40-PATTERNS.md:171/218) — este plano muda apenas o wiring do componente, não a interface (remoção não faz parte desta fase).

Purpose: D8 — agrupamento visual; D4/D5 — validade editável com displayText nu; D3 — checkbox + texto livre no form.
Output: 1 componente novo + form reorganizado + teste de crédito verde.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/40-campos-comerciais-avisos-brief/40-CONTEXT.md (D8 — seções Produto/Oferta/Avisos; D3/D4/D5)
@.planning/phases/40-campos-comerciais-avisos-brief/40-PATTERNS.md (seções e componentes de form)
@openspec/changes/fase-40-campos-comerciais-avisos-brief/specs/campaign-input-ui/spec.md (agrupamento em seções)
@openspec/changes/fase-40-campos-comerciais-avisos-brief/specs/offer-validity-modes/spec.md (UI de validade)
@openspec/changes/fase-40-campos-comerciais-avisos-brief/tasks.md (seção 4 — 4.1 a 4.4; seção 5 — 5.1 a 5.5; seção 10.5 — co-migração credits test)
@src/components/campaign/illustrative-notice-field.tsx (recém-criado no 40-02)
@src/components/campaign/mandatory-artwork-field.tsx (existe)
@src/components/flow/campaign-input-form.tsx (LINHAS VERIFICADAS)
@src/app/(app)/campanhas/nova/__tests__/campaign-flow-credits.test.tsx (LINHAS VERIFICADAS)

<interfaces>
Contrato de ValidityField (presentacional puro — D4/D5, tasks 4.1-4.4):

```tsx
// src/components/campaign/validity-field.tsx
"use client";

import type { ValidityMode } from "@/components/flow/use-campaign-form";

interface ValidityFieldProps {
  mode: ValidityMode;
  startDate: string;   // ISO yyyy-mm-dd (input type="date")
  endDate: string;     // ISO yyyy-mm-dd
  customText: string;
  disabled?: boolean;
  onModeChange: (mode: ValidityMode) => void;
  onStartDateChange: (date: string) => void;
  onEndDateChange: (date: string) => void;
  onCustomTextChange: (text: string) => void;
}

export function ValidityField(props: ValidityFieldProps) { ... }
```

Modos exibidos (select ou radio — seguir o padrão do IntentSelector):
- ""            → "Nenhuma"
- "until-date"  → "Até uma data" (mostra input date endDate)
- "range"       → "De até" (mostra inputs date startDate + endDate)
- "today"       → "Somente hoje"
- "stock"       → "Enquanto durarem os estoques"
- "custom"      → "Texto personalizado" (mostra input text customText, placeholder "Ex: Oferta válida por tempo limitado")

NENHUM estado interno — estado vive no hook (via setField). NENHUMA formatação de displayText aqui (helpers do hook cuidam disso).

Wiring atual verificado em campaign-input-form.tsx:
- Imports: :8 `MandatoryArtworkField`, :9 `CampaignIntent` type
- Descrição: :389-425 (fica na seção Produto; NÃO mover)
- Badge: :427-470
- IntentSelector: :472-486
- Checkbox preserveImageContext: :488-501
- MandatoryArtworkField: :503-506 → `value={fields.mandatoryArtworkText}` e `onChange={(v) => setField("mandatoryArtworkText", v)}` — PRECISA mudar para `fields.mandatoryArtworkTextFree` / `setField("mandatoryArtworkTextFree", v)`
- Layout raiz: `<div className="space-y-4">` (:60)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: validity-field.tsx — componente presentacional de validade</name>
  <files>src/components/campaign/validity-field.tsx</files>
  <read_first>
    - src/components/flow/campaign-input-form.tsx (linhas 427-470 — padrão select com label + erro; 196-246 — padrão IntentSelector radio-card para referência de estilo)
    - src/components/campaign/mandatory-artwork-field.tsx (linhas 1-31 — esqueleto "use client" + classes design system)
  </read_first>
  <action>
    Criar `src/components/campaign/validity-field.tsx` — component presentacional (tasks 4.1-4.4):

    1. `"use client";` + import `import type { ValidityMode } from "@/components/flow/use-campaign-form";`
    2. Props exatas do bloco <interfaces> (mode/startDate/endDate/customText/disabled + 4 callbacks).
    3. Renderizar `<div className="space-y-3">` com:
       - Label "Validade da oferta" (padrão `text-text-muted text-xs font-heading font-medium uppercase tracking-wider mb-2`).
       - `<select>` com as 6 opções do <interfaces>, `value={mode}`, `onChange={(e) => onModeChange(e.target.value as ValidityMode)}`, `disabled={disabled}`, classes `min-h-[44px] w-full bg-bg-surface border rounded-lg px-3.5 py-2.5 text-text-primary text-sm font-body ...` (mesmo padrão do select de badge — campaign-input-form.tsx:439-450).
       - Condicionais por modo:
         - `until-date`: input `type="date"` `value={endDate}` `onChange={(e) => onEndDateChange(e.target.value)}` (label "Data final").
         - `range`: 2 inputs date (startDate "Data inicial", endDate "Data final").
         - `custom`: input text `value={customText}` `onChange={(e) => onCustomTextChange(e.target.value)}` `maxLength={60}` placeholder "Ex: Oferta válida por tempo limitado".
       - Texto de ajuda (só para modos com input): "A data aparece no formato dd/mm na campanha." (D5 — displayText nu via helpers do hook).
    4. NENHUM estado interno, NENHUM `useState`, NENHUM helper de formatação — presentacional puro.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe com export `ValidityField` e props exatas do contrato
    - Grep `useState` em validity-field.tsx → 0 (presentacional puro)
    - Contém as 6 opções ("Nenhuma", "Até uma data", "De até", "Somente hoje", "Enquanto durarem os estoques", "Texto personalizado")
    - Contém `type="date"` (2x) e `type="text"` (1x) nas condicionais
    - `npm run typecheck` limpo
  </acceptance_criteria>
  <done>validity-field.tsx criado — presentacional puro com 6 modos</done>
</task>

<task type="auto">
  <name>Task 2: campaign-input-form.tsx — seções D8 + wiring dos novos campos</name>
  <files>src/components/flow/campaign-input-form.tsx</files>
  <read_first>
    - src/components/flow/campaign-input-form.tsx (linhas 60-120 layout raiz; :120-320 produto/imagem; :320-501 oferta; :503-506 MandatoryArtworkField atual; :508-588 área de submit)
  </read_first>
  <action>
    Reorganizar o form em 3 seções e conectar os novos campos (D8, tasks 5.1-5.5):

    1. **Imports:** adicionar `import { IllustrativeNoticeField } from "@/components/campaign/illustrative-notice-field";` e `import { ValidityField } from "@/components/campaign/validity-field";`.

    2. **Seção Produto:** abrir com header `<h2 className="text-text-muted text-xs font-heading font-medium uppercase tracking-wider mb-2">Produto</h2>` antes do campo nome do produto; conter nome do produto, upload de imagem e **Descrição** (:389-425, NÃO mover).

    3. **Seção Oferta:** header `<h2>Oferta</h2>` (mesmas classes) antes dos preços; conter Preço Original, Preço com Desconto, Selo promocional, IntentSelector, checkbox preserveImageContext (:488-501) e **ValidityField condicionado**:
       ```
       {fields.campaignIntent === "offer" && (
         <ValidityField
           mode={fields.validityMode}
           startDate={fields.validityStartDate}
           endDate={fields.validityEndDate}
           customText={fields.validityCustomText}
           disabled={isSubmitting}
           onModeChange={(m) => setField("validityMode", m)}
           onStartDateChange={(d) => setField("validityStartDate", d)}
           onEndDateChange={(d) => setField("validityEndDate", d)}
           onCustomTextChange={(t) => setField("validityCustomText", t)}
         />
       )}
       ```

    4. **Seção Avisos e texto obrigatório:** header `<h2>Avisos e texto obrigatório</h2>` antes de:
       - `<IllustrativeNoticeField checked={fields.showIllustrativeNotice} onChange={(c) => setField("showIllustrativeNotice", c)} />`
       - MandatoryArtworkField (:503-506) ALTERADO para:
         ```
         <MandatoryArtworkField
           value={fields.mandatoryArtworkTextFree}
           onChange={(v) => setField("mandatoryArtworkTextFree", v)}
         />
         ```

    5. NÃO alterar a área de submit (:508-588) nem o IntentSelector (:472-486).
    6. NÃO remover `mandatoryArtworkText` da interface `CampaignFormFields` — o campo permanece como **compat/derivado** (40-PATTERNS.md:171/218); o espelho bidirecional no `setField` (plano 40-04) mantém o campo antigo coerente para drafts legados. A remoção da interface NÃO faz parte desta fase.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(app)/campanhas/nova/__tests__/campaign-flow-credits.test.tsx"; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Grep `mandatoryArtworkText", v` / `fields.mandatoryArtworkText` (campo antigo) no arquivo → 0; `mandatoryArtworkTextFree` presente no MandatoryArtworkField
    - `CampaignFormFields.mandatoryArtworkText` permanece na interface (verificar com `npm run typecheck` que o form não referencia mais o campo antigo, sem remover o campo em si)
    - `IllustrativeNoticeField` e `ValidityField` importados e renderizados; ValidityField dentro de `{fields.campaignIntent === "offer" && (...)}`
    - 3 headers de seção ("Produto", "Oferta", "Avisos e texto obrigatório") presentes
    - Descrição permanece na seção Produto (mesmo markup :389-425 intacto)
    - `npm run typecheck` limpo
  </acceptance_criteria>
  <done>form reorganizado em 3 seções com os novos campos conectados</done>
</task>

<task type="auto">
  <name>Task 3: Co-migração dos mocks do teste de crédito (10.5)</name>
  <files>src/app/(app)/campanhas/nova/__tests__/campaign-flow-credits.test.tsx</files>
  <read_first>
    - src/app/(app)/campanhas/nova/__tests__/campaign-flow-credits.test.tsx (linhas 24-60 mock do use-campaign-form com fields em :29-39; :76-78 mock do MandatoryArtworkField)
  </read_first>
  <action>
    Co-migrar `campaign-flow-credits.test.tsx` (10.5):

    1. **fields mock (:29-39):** substituir a linha `mandatoryArtworkText: "",` por:
       ```
       showIllustrativeNotice: true,
       mandatoryArtworkTextFree: "",
       validityMode: "",
       validityStartDate: "",
       validityEndDate: "",
       validityCustomText: "",
       ```

    2. **Novos mocks de componente** (junto ao mock de mandatory-artwork-field :76-78):
       ```
       vi.mock("@/components/campaign/illustrative-notice-field", () => ({
         IllustrativeNoticeField: () => null,
       }));
       vi.mock("@/components/campaign/validity-field", () => ({
         ValidityField: () => null,
       }));
       ```

    3. NENHUMA outra alteração — os 3 testes de crédito existentes continuam válidos (renderização do form mockado).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(app)/campanhas/nova/__tests__/campaign-flow-credits.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - Grep `mandatoryArtworkText:` no arquivo → 0
    - `illustrative-notice-field` e `validity-field` mocks presentes
    - Suíte inteira do credits test verde (3 testes)
  </acceptance_criteria>
  <done>credits test co-migrado e verde</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → hook | Inputs do usuário (modo validade, datas, texto custom) transitam pelos callbacks do componente presentacional |
| form → body | Campos novos são enviados montados pelo hook (plano 40-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-05 | Spoofing | ValidityField | mitigate | Presentacional puro — emitindo valores crus via callbacks; modo tipado `ValidityMode` evita valor arbitrário; validade só renderizada para intent offer (falha silenciosa impossível de displayText errado) |
| T-40-05a | Tampering | Inputs de data | mitigate | `type="date"` restringe formato; `maxLength={60}` no texto custom; displayText final é responsabilidade única dos helpers do hook (plano 40-04) |
| T-40-05b | Information Disclosure | Texto do aviso no form | accept | Conteúdo público de produto — sem dado sensível |
| T-40-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo — sem gate de legitimidade |
</threat_model>

<verification>
- `npm run typecheck` limpo
- `npx vitest run "src/app/(app)/campanhas/nova/__tests__/campaign-flow-credits.test.tsx"` verde
- Grep `fields.mandatoryArtworkText\b` em campaign-input-form.tsx → 0
- 3 headers de seção + ValidityField condicionado a offer
</verification>

<success_criteria>
- ValidityField presentacional puro com 6 modos e 4 callbacks
- Form reorganizado em Produto/Oferta/Avisos e texto obrigatório (D8)
- MandatoryArtworkField → mandatoryArtworkTextFree; IllustrativeNoticeField → showIllustrativeNotice; ValidityField → 4 campos de validade (só offer)
- Credits test co-migrado e verde
</success_criteria>

<output>
Create `.planning/phases/40-campos-comerciais-avisos-brief/40-05-SUMMARY.md` when done
</output>
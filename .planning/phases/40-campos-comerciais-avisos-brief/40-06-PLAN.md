---
phase: 40-campos-comerciais-avisos-brief
plan: 06
type: execute
wave: 3
depends_on: [40-02, 40-04]
files_modified:
  - src/components/flow/__tests__/use-campaign-form-validity.test.ts
  - src/components/flow/__tests__/use-campaign-form-notice.test.ts
  - src/lib/campaign/__tests__/brief.test.ts
autonomous: true
requirements: [F40-01, F40-02, F40-04, F40-05, F40-06, F40-07, F40-08, F40-15]

must_haves:
  truths:
    - "Testes 1-8 (seção 7): os 6 modos de validade geram o displayText exato e 'nu' (sem prefixo), custom normaliza prefixo, e endDate ISO nunca vai no body"
    - "Testes 9-15 (seção 8): as 4 combinações checkbox × texto livre produzem o body exato; default showIllustrativeNotice = true; zero strings divergentes nos componentes"
    - "brief.test.ts 8.8: validityText/legalNoticeText passam pelos mappers do brief"
  artifacts:
    - path: "src/components/flow/__tests__/use-campaign-form-validity.test.ts"
      provides: "Testes 1-8 — displayText dos modos + body"
      contains: "buildValidityDisplayText|validityEndDate"
    - path: "src/components/flow/__tests__/use-campaign-form-notice.test.ts"
      provides: "Testes 9-15 — aviso ilustrativo no body + migração legada"
      contains: "buildMandatoryArtworkText|mandatoryArtworkTextFree"
    - path: "src/lib/campaign/__tests__/brief.test.ts"
      provides: "8.8 — validity/legalNotice nos mappers"
      contains: "validityText|legalNoticeText"
  key_links:
    - from: "use-campaign-form-validity.test.ts"
      to: "src/components/flow/use-campaign-form.ts"
      via: "testa helpers exportados buildValidityDisplayText/formatDDMM e body via renderHook"
      pattern: "buildValidityDisplayText"
    - from: "use-campaign-form-notice.test.ts"
      to: "src/components/flow/use-campaign-form.ts + src/lib/campaign/constants.ts"
      via: "testa buildMandatoryArtworkText e a constante única"
      pattern: "ILLUSTRATIVE_NOTICE_TEXT"
---

<objective>
Escrever as suítes de teste das seções 7 e 8 do tasks.md: **validade da oferta (testes 1-8)** e **checkbox ilustrativo × texto obrigatório (testes 9-15)** em dois arquivos novos, mais os casos **8.8** no `brief.test.ts`. Os testes verificam o comportamento dos helpers exportados (40-04) e do body montado.

Purpose: garantir o comportamento exato de D4/D5 (displayText nu, normalização de prefixo, endDate nunca enviado) e D3 (4 combinações de concatenação no body, default true) — verificáveis por `vitest` sem UI.
Output: 2 arquivos de teste novos + extensão do brief.test.ts.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/40-campos-comerciais-avisos-brief/40-CONTEXT.md (D3 — 4 combinações no body; D4 — 6 modos, endDate nunca enviado; D5 — displayText nu + prefixo custom normalizado)
@openspec/changes/fase-40-campos-comerciais-avisos-brief/specs/offer-validity-modes/spec.md
@openspec/changes/fase-40-campos-comerciais-avisos-brief/specs/mandatory-artwork-text/spec.md
@openspec/changes/fase-40-campos-comerciais-avisos-brief/tasks.md (seção 7 — testes 1-8; seção 8 — testes 9-15; seção 8.8 — casos brief.test.ts)
@src/components/flow/use-campaign-form.ts (helpers exportados — contrato do 40-04)
@src/lib/campaign/constants.ts (ILLUSTRATIVE_NOTICE_TEXT)
@src/components/flow/__tests__/use-campaign-form-navigation.test.ts (padrão de renderHook + fetch stub + createNdjsonResponse :27-40, a REUTILIZAR)
@src/lib/campaign/__tests__/brief.test.ts (baseBrief :11-42; casos existentes de validity/legalNotice :144-192)

<interfaces>
Contratos sob teste (implementados no 40-04):

```typescript
buildValidityDisplayText({ validityMode, validityStartDate, validityEndDate, validityCustomText }): string | undefined
// "" -> undefined | "until-date"+"2026-09-30" -> "até 30/09" | "range"+"2026-09-25"+"2026-09-30" -> "de 25/09 até 30/09"
// "today" -> "somente hoje" | "stock" -> "enquanto durarem os estoques"
// "custom"+"Oferta válida: por tempo limitado" -> "por tempo limitado" (prefixo removido)
// "custom"+"Somente em novembro" -> "Somente em novembro" (inalterado)

buildMandatoryArtworkText(showNotice, freeText): string | undefined
// (true, "") -> "Imagem meramente ilustrativa" | (true, "X") -> "Imagem meramente ilustrativa\nX"
// (false, "X") -> "X" | (false, "") -> undefined

formatDDMM("2026-09-30") -> "30/09"
```

Padrão do nav test para capturar body: `renderHook(() => useCampaignForm("store-123"))`, `vi.stubGlobal("fetch", vi.fn().mockResolvedValue(createNdjsonResponse([{ type: "result", campaignId: "abc", campaignUrl: "/c" }])))`, `act(() => result.current.handleSubmit())`, depois ler o body do fetch call: `expect(fetch).toHaveBeenCalledWith(expect.any(String), expect.objectContaining({ body: expect.any(String) }))` e `JSON.parse(mockFetch.mock.calls[0][1].body)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: use-campaign-form-validity.test.ts — testes 1-8</name>
  <files>src/components/flow/__tests__/use-campaign-form-validity.test.ts</files>
  <read_first>
    - src/components/flow/use-campaign-form.ts (helpers exportados + handleSubmit/body :625-638)
    - src/components/flow/__tests__/use-campaign-form-navigation.test.ts (linhas 1-46 — setup jsdom, mocks de navigation e preservation, createNdjsonResponse; :48-86 padrão de teste renderHook)
  </read_first>
  <action>
    Criar `use-campaign-form-validity.test.ts` (testes 1-8, tasks.md seção 7):

    1. Header `// @vitest-environment jsdom`, imports de `describe/it/expect/vi/beforeEach`, `renderHook/act/waitFor` de `@testing-library/react`, `useCampaignForm`, `buildValidityDisplayText`, `formatDDMM`.
    2. Mocks idênticos ao nav test: `next/navigation` (:8-10) e `@/hooks/use-input-preservation` (:16-22); `beforeEach` com `vi.clearAllMocks()` + `mockRestoreFormState.mockReturnValue(null)`; helper `createNdjsonResponse` (:27-40).
    3. **Testes 1-7 (helpers puros):**
       1. `buildValidityDisplayText({ validityMode: "", ... })` → `undefined`
       2. until-date + endDate "2026-09-30" → `"até 30/09"`
       3. range + start "2026-09-25" + end "2026-09-30" → `"de 25/09 até 30/09"`
       4. today → `"somente hoje"`
       5. stock → `"enquanto durarem os estoques"`
       6. custom "Somente em novembro" → `"Somente em novembro"` (sem prefixo, inalterado)
       7. custom "Oferta válida: por tempo limitado" → `"por tempo limitado"` (prefixo removido); e `formatDDMM("2026-09-30")` → `"30/09"`, `formatDDMM("")` → `""`
    4. **Teste 8 (body):** renderHook + fetch stub; definir via `act`/`setField` todos os campos obrigatórios (productName, discountedPriceCents, badge, imageFile — reutilizar `VALID_DATA_URL`? NÃO: imageFile não serializa; usar `vi.stubGlobal("fetch", ...)` e definir `setField("imageFile", new File([...], "p.png", { type: "image/png" }))`); `validityMode: "until-date"`, `validityEndDate: "2026-09-30"`, `campaignIntent: "offer"`; `handleSubmit`; assertar: `body.validity === "até 30/09"`, `body.validityEndDate === undefined`, `body.validityStartDate === undefined` (endDate ISO NUNCA no body, D4).
    5. (Bônus D4) `it("preserves validity fields when switching intent")`: set validityMode "until-date" + validityEndDate; `setField("campaignIntent", "exclusive")` (e prices para permitir); assertar `result.current.fields.validityMode` ainda `"until-date"` (nunca limpo).
  </action>
  <verify>
    <automated>npx vitest run src/components/flow/__tests__/use-campaign-form-validity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe com 8+ testes; testes 1-7 puros sobre os helpers; teste 8 captura body via fetch mock e confirma `body.validity === "até 30/09"` com `validityEndDate` ausente
    - Teste bônus de preservação de validade na troca de intenção verde
    - `npx vitest run <arquivo>` passa
  </acceptance_criteria>
  <done>testes 1-8 verdes — displayText dos 6 modos, formatação dd/mm, endDate fora do body, intenção preserva validade</done>
</task>

<task type="auto">
  <name>Task 2: use-campaign-form-notice.test.ts — testes 9-15</name>
  <files>src/components/flow/__tests__/use-campaign-form-notice.test.ts</files>
  <read_first>
    - src/components/flow/use-campaign-form.ts (buildMandatoryArtworkText + body :625-638)
    - src/lib/campaign/constants.ts (ILLUSTRATIVE_NOTICE_TEXT)
    - src/components/campaign/illustrative-notice-field.tsx e src/components/campaign/mandatory-artwork-field.tsx (para o teste 14 de strings divergentes)
  </read_first>
  <action>
    Criar `use-campaign-form-notice.test.ts` (testes 9-15, tasks.md seção 8):

    1. Setup igual ao Task 1 (jsdom, mocks de navigation/preservation, createNdjsonResponse, setup de renderHook + fetch stub + imageFile).
    2. **Testes 9-12 (body via fetch mock, 4 combinações):**
       9. `showIllustrativeNotice: true`, free `""` → `body.mandatoryArtworkText === "Imagem meramente ilustrativa"`
       10. `showIllustrativeNotice: true`, free `"Texto promocional"` → `body.mandatoryArtworkText === "Imagem meramente ilustrativa\nTexto promocional"`
       11. `showIllustrativeNotice: false`, free `"Texto livre"` → `body.mandatoryArtworkText === "Texto livre"`
       12. `showIllustrativeNotice: false`, free `""` → `body.mandatoryArtworkText === undefined` (chave ausente no body parseado)
    3. **Teste 13 (default):** renderHook → `result.current.fields.showIllustrativeNotice === true` e `result.current.fields.mandatoryArtworkTextFree === ""`.
    4. **Teste 14 (constante única, sem divergência):** importar `ILLUSTRATIVE_NOTICE_TEXT`; assertar `ILLUSTRATIVE_NOTICE_TEXT === "Imagem meramente ilustrativa"`; ler via `fs.readFileSync` `src/components/campaign/illustrative-notice-field.tsx` e `mandatory-artwork-field.tsx` e assertar que NENHUM contém `"Imagens meramente ilustrativas"` (plural) nem `placeholder="Ex: Imagens` (hardcoded). (Caminhos relativos a `process.cwd()`.)
    5. **Teste 15 (migração legada):** `mockRestoreFormState.mockReturnValue({ productName: "X", originalPriceCents: 0, discountedPriceCents: undefined, badge: "", campaignIntent: "offer", preserveImageContext: false, imageFile: null, mandatoryArtworkText: "Texto legado" })` (shape antigo); `await act(async () => {})`; assertar `fields.mandatoryArtworkTextFree === "Texto legado"` e `fields.showIllustrativeNotice === true`.
  </action>
  <verify>
    <automated>npx vitest run src/components/flow/__tests__/use-campaign-form-notice.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe com testes 9-15; 4 combinações de body exatas (concatenação com `\n`, só constante, só texto, ausente)
    - Default showIllustrativeNotice true; teste 14 sem plurais hardcoded; teste 15 de migração verde
    - `npx vitest run <arquivo>` passa
  </acceptance_criteria>
  <done>testes 9-15 verdes — concatenação exata no body, default true, zero divergência, migração legada</done>
</task>

<task type="auto">
  <name>Task 3: brief.test.ts — casos 8.8 de validity/legalNotice</name>
  <files>src/lib/campaign/__tests__/brief.test.ts</files>
  <read_first>
    - src/lib/campaign/__tests__/brief.test.ts (linhas 1-42 baseBrief + asserts; :144-192 casos existentes de validity/legalNotice)
    - src/lib/campaign/brief-mapper.ts ou arquivo do mapper de brief (onde `validityText`/`legalNoticeText` são montados) — conferir nomes reais dos campos de saída
  </read_first>
  <action>
    Adicionar os casos 8.8 ao `brief.test.ts` (F40-15 — o aviso ilustrativo e a validade fluem dos campos do form para os campos de saída do brief):

    1. Ler o mapper de brief e confirmar os nomes de saída (esperado: `validityText` e `legalNoticeText`; ajustar conforme código real).
    2. Adicionar cases no describe existente (ao lado de :144-192):
       - Validade vinda do form (ex.: "até 30/09") → `validityText` do brief montado com esse valor.
       - Aviso ilustrativo concatenado (ex.: "Imagem meramente ilustrativa\nTexto") → `legalNoticeText` montado com o valor integral (nova linha preservada).
       - Validade/aviso ausentes → campos de saída `undefined`/ausentes no brief.
    3. Manter o padrão de asserts do arquivo (baseBrief + spreads). NÃO alterar casos existentes.
  </action>
  <verify>
    <automated>npx vitest run src/lib/campaign/__tests__/brief.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 3+ novos cases de validityText/legalNoticeText verdes
    - `npx vitest run src/lib/campaign/__tests__/brief.test.ts` passa inteiro (casos existentes intactos)
  </acceptance_criteria>
  <done>brief.test.ts cobre validityText/legalNoticeText — mappers do brief passam validade e aviso</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| helpers (hook) → testes | Comportamento de displayText/body verificado deterministicamente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-06 | Tampering | Concatenação do aviso | mitigate | Testes 9-12 cobrem as 4 combinações exatas do body (orden de concatenação `constante\ntexto` é regra de teste) |
| T-40-06a | Spoofing | DisplayText da validade | mitigate | Testes 1-7 fixam o displayText dos 6 modos; teste 8 prova que endDate ISO não vaza ao body; teste bônus prova que trocar intenção não limpa validade |
| T-40-06b | Integrity | Strings divergentes | mitigate | Teste 14 bloqueia plurais hardcoded nos componentes (constante única sempre) |
| T-40-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo — sem gate de legitimidade |
</threat_model>

<verification>
- `npx vitest run src/components/flow/__tests__/use-campaign-form-validity.test.ts` verde (8+)
- `npx vitest run src/components/flow/__tests__/use-campaign-form-notice.test.ts` verde (7)
- `npx vitest run src/lib/campaign/__tests__/brief.test.ts` verde (casos 8.8 incluídos)
</verification>

<success_criteria>
- Testes 1-8: 6 modos com displayText exato, formatDDMM, endDate fora do body, preservação na troca de intenção
- Testes 9-15: 4 combinações do aviso no body, default true, constante única sem divergência, migração legada
- brief.test.ts: validityText/legalNoticeText montados a partir dos valores do form
</success_criteria>

<output>
Create `.planning/phases/40-campos-comerciais-avisos-brief/40-06-SUMMARY.md` when done
</output>
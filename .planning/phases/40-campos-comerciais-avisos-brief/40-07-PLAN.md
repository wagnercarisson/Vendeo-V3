---
phase: 40-campos-comerciais-avisos-brief
plan: 07
type: execute
wave: 3
depends_on: [40-02, 40-03]
files_modified:
  - src/lib/campaign/__tests__/prompt-reframe.test.ts
  - src/lib/image-generation/services/__tests__/image-generation-service.test.ts
  - src/lib/image-generation/services/__tests__/image-review-service.test.ts
autonomous: true
requirements: [F40-02, F40-11, F40-12, F40-13, F40-14, F40-15, F40-16]

must_haves:
  truths:
    - "Testes 16-17 (tasks.md 9.1/9.2): os 4 prompts não têm mais a imposição SEMPRE fixa e contêm o bloco condicional + linha condicional de {{mandatoryArtworkText}} mantida (F40-12); checks de conteúdo garantem a linha de validade e o singular alinhado à constante"
    - "Teste 18 (9.3): legalNotice.enabled=false → vars.mandatoryArtworkText === '' no prompt (golden 8.16 spotlight/exclusive) E mandatoryArtworkTextSection vazio no revisor (testes existentes :225-240/:252-260)"
    - "Teste 19 (9.4): validityTextSection montado (validity presente) / vazio (ausente) no revisor — coberto por testes existentes (:273-294)"
    - "Teste 20 (9.5): golden por intent com novos campos preenchidos — EXPECTED_KEYS = 38 intacto (F40-02); texto do prompt muda intencionalmente (D6)"
    - "Teste 21 (9.6): reviewer com aviso default → seção 'Texto Obrigatorio na Arte' contém ILLUSTRATIVE_NOTICE_TEXT; rigor literal preservado — coberto por testes existentes (:188-205/:262-271)"
    - "Fixtures plural→singular co-migradas em image-generation-service.test.ts (:242/:255/:272/:339/:349) e image-review-service.test.ts (:193/:199)"
  artifacts:
    - path: "src/lib/campaign/__tests__/prompt-reframe.test.ts"
      provides: "Testes 16-17 + checks de conteúdo dos 4 prompts (linha mantida, validade, singular)"
      contains: "readFileSync|Quando houver texto obrigatório/aviso legal informado"
    - path: "src/lib/image-generation/services/__tests__/image-review-service.test.ts"
      provides: "Testes 18/19/21 reviewer-side — mapeados para testes existentes (:225-240, :252-271, :273-294)"
      contains: "mandatoryArtworkTextSection"
    - path: "src/lib/image-generation/services/__tests__/image-generation-service.test.ts"
      provides: "Teste 18 prompt-side (golden, vars.mandatoryArtworkText '') + Teste 20 golden com novos campos"
      contains: "Imagem meramente ilustrativa"
  key_links:
    - from: "prompt-reframe.test.ts"
      to: "prompts/campaign-image-director*.md"
      via: "fs.readFileSync(path.join(process.cwd(), 'prompts', ...))"
      pattern: "readFileSync"
    - from: "prompt-reframe.test.ts"
      to: "src/lib/campaign/constants.ts"
      via: "verifica que os prompts usam o mesmo literal singular da constante (F40-02)"
      pattern: "ILLUSTRATIVE_NOTICE_TEXT"
---

<objective>
Criar os **testes 16-21** (seção 9) com semânticas fiéis ao tasks.md: **Testes 16-17** de conteúdo dos 4 prompts em `prompt-reframe.test.ts` (sem SEMPRE, bloco condicional, linha mantida, validade intacta, singular alinhado à constante); **Teste 18** (9.3 — `legalNotice.enabled=false` → `mandatoryArtworkText` vazio no prompt) adicionado ao golden 8.16 spotlight/exclusive em `image-generation-service.test.ts`; **Teste 20** (9.5 — golden por intent com novos campos, `EXPECTED_KEYS = 38`) adicionado ao mesmo arquivo; **Testes 18/19/21 reviewer-side** mapeados para testes já existentes em `image-review-service.test.ts` (sem redefinição de semântica). Co-migrar as fixtures plural→singular (10.2/10.3).

Purpose: garantir o reframe D6 aplicado nos 4 prompts (sem SEMPRE fixo, com bloco condicional) sem quebrar as linhas de `{{mandatoryArtworkText}}`/validade; cobrir F40-14 (legalNotice off → prompt/revisor vazios) e F40-13 (golden 38 keys com novos campos); alinhar fixtures de teste ao singular canônico.
Output: 1 arquivo de teste novo + 2 arquivos de teste estendidos/co-migrados.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/40-campos-comerciais-avisos-brief/40-CONTEXT.md (D2 — singular canônico; D6 — reframe; D5 — validade intacta)
@openspec/changes/fase-40-campos-comerciais-avisos-brief/specs/ai-image-generation/spec.md
@openspec/changes/fase-40-campos-comerciais-avisos-brief/tasks.md (seção 9 — testes 16-21; seção 10.2/10.3 — co-migração de fixtures)
@prompts/campaign-image-director.md (reframed no 40-03)
@prompts/campaign-image-director-offer.md
@prompts/campaign-image-director-spotlight.md
@prompts/campaign-image-director-exclusive.md
@src/lib/campaign/constants.ts (ILLUSTRATIVE_NOTICE_TEXT = "Imagem meramente ilustrativa")
@src/lib/image-generation/services/__tests__/image-generation-service.test.ts (LINHAS VERIFICADAS: plural em :242, :255, :272, :339, :349; EXPECTED_KEYS :516-527; golden tests :539+)
@src/lib/image-generation/services/__tests__/image-review-service.test.ts (LINHAS VERIFICADAS: plural em :193, :199)

<interfaces>
Inventário completo de plurais em src/ (grep realizado em 2026-08-14) — dividido por plano:

- `src/components/campaign/mandatory-artwork-field.tsx:24` → plano 40-02 (feito)
- `src/app/api/campaign/generate-image/__tests__/route.test.ts:530, 775, 785, 796, 807` → plano 40-08
- `src/lib/image-generation/services/__tests__/image-review-service.test.ts:193, 199` → ESTE plano (Task 3)
- `src/lib/image-generation/services/__tests__/image-generation-service.test.ts:242, 255, 272, 339, 349` → ESTE plano (Task 2)

Textos de prompt verificados (reframe do 40-03):

- BLOCO_CONDICIONAL = `Quando houver texto obrigatório/aviso legal informado, exiba exatamente esse texto na arte. Se o aviso for "Imagem meramente ilustrativa", posicione-o com tipografia mínima, mas visível e legível, em área lateral horizontal ou vertical, sem competir com oferta, produto e preço.`
- LINHA_MANTIDA = `Se o campo "Texto obrigatório na arte" estiver preenchido ({{mandatoryArtworkText}}), inclua esse texto na arte de forma visível e legível, em tipografia mínima adequada para leitura em dispositivo móvel. Não o repita na legenda.`
- LINHA_VALIDADE = `**Validade da oferta:** {{validity}}` (presente em director.md e offer.md)
- PROHIBIDO = `SEMPRE acrescente a arte o seguinte texto`
</interfaces>

## Mapeamento Testes tasks.md 16-21 → cobertura planejada/existente

Semânticas do tasks.md seção 9 são fonte da verdade — nenhuma é redefinida; cada uma é coberta por teste planejado (NOVO) ou teste existente (verificado sem reescrita):

| Teste tasks.md | Semântica (fonte da verdade) | Cobertura | Local |
|----------------|------------------------------|-----------|-------|
| Teste 16 (9.1) | Os 4 prompts NÃO contêm "SEMPRE acrescente ... Imagem meramente ilustrativa" | NOVO — Task 1, Teste 16 (loop nos 4 arquivos) | `prompt-reframe.test.ts` |
| Teste 17 (9.2) | Os 4 prompts contêm o bloco condicional (exibir exatamente; tipografia mínima; posição lateral; sem competir) | NOVO — Task 1, Teste 17 (loop nos 4 arquivos, incl. linha mantida `{{mandatoryArtworkText}}` — F40-12) | `prompt-reframe.test.ts` |
| Teste 18 (9.3) | `legalNotice.enabled=false` → `mandatoryArtworkText` vazio no prompt E `mandatoryArtworkTextSection` vazio no revisor | PROMPT-SIDE: NOVO — Task 2, `expect(vars.mandatoryArtworkText).toBe('')` no golden 8.16 spotlight/exclusive. REVIEWER-SIDE: EXISTENTE — `image-review-service.test.ts:225-240` + `:252-260` (`mandatoryArtworkTextSection` → `toBe('')`), verificado no Task 3 | `image-generation-service.test.ts` + `image-review-service.test.ts` |
| Teste 19 (9.4) | `validity.enabled` + displayText (offer) → `validityTextSection` montado; ausente → vazio | EXISTENTE — `image-review-service.test.ts:273-284` (`toContain('Até 30/09')`) + `:286-294` (`toBe('')`), verificado no Task 3 | `image-review-service.test.ts` |
| Teste 20 (9.5) | Golden por intent (offer/spotlight/exclusive) — conjunto de variáveis/keys idêntico (`EXPECTED_KEYS = 38`) com os novos campos preenchidos; texto do prompt muda intencionalmente (D6) | NOVO — Task 2, `it('9.5 golden offer com novos campos preenchidos mantém 38 keys')` (asserta 38 keys + `vars.validity` + `vars.mandatoryArtworkText`) | `image-generation-service.test.ts` |
| Teste 21 (9.6) | Reviewer com checkbox default marcado → seção contém `ILLUSTRATIVE_NOTICE_TEXT`; rigor literal para aviso legal preservado | EXISTENTE — `image-review-service.test.ts:262-271` (`toContain('Imagem meramente ilustrativa')`) + `:188-205` (fidelidade semântica e rigor legal), verificado no Task 3 | `image-review-service.test.ts` |
</context>

<tasks>

<task type="auto">
  <name>Task 1: prompt-reframe.test.ts — Testes 16-17 + checks de conteúdo (bloco, linha mantida, validade, singular)</name>
  <files>src/lib/campaign/__tests__/prompt-reframe.test.ts</files>
  <read_first>
    - prompts/campaign-image-director.md (linhas 125-135 — bloco condicional + linha mantida)
    - prompts/campaign-image-director-offer.md (linhas 128-136)
    - prompts/campaign-image-director-spotlight.md (linhas 126-133)
    - prompts/campaign-image-director-exclusive.md (linhas 135-142)
    - src/lib/campaign/constants.ts (ILLUSTRATIVE_NOTICE_TEXT)
  </read_first>
  <action>
    Criar `src/lib/campaign/__tests__/prompt-reframe.test.ts` — **Testes 16-17** (tasks.md 9.1/9.2) + checks de conteúdo dos 4 prompts (linha mantida F40-12, validade intacta, singular):

    1. Setup: `import { describe, it, expect } from "vitest";` + `import { readFileSync } from "node:fs";` + `import path from "node:path";` + `import { ILLUSTRATIVE_NOTICE_TEXT } from "@/lib/campaign/constants";`. Helper `readPrompt(name: string): string` → `readFileSync(path.join(process.cwd(), "prompts", name), "utf-8")`. Lista `const PROMPTS = ["campaign-image-director.md", "campaign-image-director-offer.md", "campaign-image-director-spotlight.md", "campaign-image-director-exclusive.md"];`.
    2. **Teste 16 (9.1 — imposição SEMPRE ausente, NOS 4):** para cada arquivo em PROMPTS: NÃO contém `"SEMPRE acrescente a arte o seguinte texto"`.
    3. **Teste 17 (9.2 — bloco condicional presente, NOS 4):** para cada arquivo em PROMPTS: contém `BLOCO_CONDICIONAL` E contém `LINHA_MANTIDA` (`{{mandatoryArtworkText}}` — F40-12).
    4. **Check de conteúdo A (linha de validade intacta — D5):** `campaign-image-director.md` e `campaign-image-director-offer.md` contêm `LINHA_VALIDADE` (`**Validade da oferta:** {{validity}}`); spotlight e exclusive NÃO contêm (estrutura original) — assertar presença apenas onde existe.
    5. **Check de conteúdo B (singular alinhado à constante — F40-02):** para cada arquivo em PROMPTS: NÃO contém `"Imagens meramente ilustrativas"` (plural) e contém `"Imagem meramente ilustrativa"` (singular); assertar `ILLUSTRATIVE_NOTICE_TEXT === "Imagem meramente ilustrativa"` (amarra o literal de teste à constante).

    > O Teste 18 do tasks.md (9.3) é coberto no Task 2 (prompt-side — golden spotlight/exclusive) e no Task 3 (reviewer-side — teste existente); os Testes 19/21 do tasks.md (9.4/9.6) são cobertos no Task 3 (testes existentes); o Teste 20 do tasks.md (9.5) é coberto no Task 2. NENHUM teste do tasks.md é redefinido como "conteúdo de prompt por arquivo" — ver tabela de mapeamento no fim do context.
  </action>
  <verify>
    <automated>npx vitest run src/lib/campaign/__tests__/prompt-reframe.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe com Testes 16-17 (loop nos 4 arquivos) + checks A/B
    - Teste 16: `SEMPRE acrescente` ausente em TODOS os 4; Teste 17: bloco condicional + linha mantida presentes em TODOS os 4
    - Check A valida LINHA_VALIDADE em director/offer e ausência em spotlight/exclusive
    - Check B valida singular via constante nos 4 arquivos (sem plural)
    - `npx vitest run <arquivo>` passa
  </acceptance_criteria>
  <done>Testes 16-17 + checks de conteúdo verdes — reframe confirmado nos 4 prompts com linha mantida, validade intacta e singular alinhado à constante</done>
</task>

<task type="auto">
  <name>Task 2: Co-migração de fixtures (10.2) + Testes 18 (9.3 prompt-side) e 20 (9.5 golden) — image-generation-service.test.ts</name>
  <files>src/lib/image-generation/services/__tests__/image-generation-service.test.ts</files>
  <read_first>
    - src/lib/image-generation/services/__tests__/image-generation-service.test.ts (linhas 242, 255, 272, 339, 349 — plurais; :516-527 EXPECTED_KEYS; :539-544 golden test já com singular)
  </read_first>
  <action>
    Co-migrar as 5 ocorrências de `'Imagens meramente ilustrativas'` para `'Imagem meramente ilustrativa'` (singular, D2) nas linhas 242, 255, 272, 339 e 349 + adicionar os **Testes 18 (9.3, prompt-side)** e **20 (9.5, golden com novos campos)**:

    1. Substituir `'Imagens meramente ilustrativas'` → `'Imagem meramente ilustrativa'` em TODAS as ocorrências do arquivo (fixtures `createMinimalBrief({ mandatoryArtworkText: ... })` e asserts `toContain`/`toBe`).
    2. **Teste 18 — prompt-side (9.3):** no golden 8.16 spotlight (:558-570, `createMinimalBrief({ campaignIntent: 'spotlight', preserveImageContext: true })` — brief SEM `mandatoryArtworkText`, espelha `legalNotice.enabled=false`) adicionar `expect(vars.mandatoryArtworkText).toBe('')`; o mesmo no golden 8.16 exclusive (:572-580). (Alternativa equivalente: um `it` dedicado `'9.3 legalNotice ausente → mandatoryArtworkText vazio no prompt'` cobrindo as duas intents — qualquer uma das duas formas cobre a semântica do tasks.md 9.3.)
    3. **Teste 20 — golden com novos campos (9.5):** novo `it('9.5 golden offer com novos campos preenchidos mantém 38 keys (D6)')`: `createMinimalBrief({ validity: 'até 30/09', mandatoryArtworkText: 'Imagem meramente ilustrativa' })`; assertar `Object.keys(vars)).toHaveLength(38)`, `[...EXPECTED_KEYS].sort()` igual, `vars.validity === 'até 30/09'` e `vars.mandatoryArtworkText === 'Imagem meramente ilustrativa'` — as MESMAS 38 keys do golden existente, com texto do prompt mudado intencionalmente (D6).
    4. NÃO alterar `EXPECTED_KEYS` (:516-527 — 38 keys intactas) nem a estrutura dos golden tests 8.16/8.17/8.18 existentes (:539+).
  </action>
  <verify>
    <automated>npx vitest run src/lib/image-generation/services/__tests__/image-generation-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Grep `Imagens meramente ilustrativas` no arquivo → 0
    - Grep `Imagem meramente ilustrativa` → 5+ ocorrências
    - Teste 18 (9.3): `expect(vars.mandatoryArtworkText).toBe('')` presente no golden spotlight e exclusive (ou teste dedicado cobrindo as 2 intents)
    - Teste 20 (9.5): golden com novos campos preenchidos asserta 38 keys + `vars.validity` + `vars.mandatoryArtworkText`
    - Suíte inteira verde (inclui golden 8.16/8.17/8.18 com EXPECTED_KEYS 38)
  </acceptance_criteria>
  <done>fixtures do image-generation-service.test.ts co-migradas para o singular + Testes 18 (9.3) e 20 (9.5) adicionados — suíte verde</done>
</task>

<task type="auto">
  <name>Task 3: Co-migração de fixtures — image-review-service.test.ts (10.3)</name>
  <files>src/lib/image-generation/services/__tests__/image-review-service.test.ts</files>
  <read_first>
    - src/lib/image-generation/services/__tests__/image-review-service.test.ts (linhas 188-205 — teste da mandatoryArtworkTextSection; plural em :193 e assert em :199)
  </read_first>
  <action>
    Co-migrar as 2 ocorrências em `image-review-service.test.ts` + verificar a cobertura reviewer-side dos Testes 18/19/21 do tasks.md (sem redefinição):

    1. Linha 193: `legalNoticeText: 'Imagens meramente ilustrativas',` → `legalNoticeText: 'Imagem meramente ilustrativa',`
    2. Linha 199: `expect(vars.mandatoryArtworkTextSection).toContain('Imagens meramente ilustrativas');` → `...toContain('Imagem meramente ilustrativa');`
    3. NENHUMA outra alteração de código (o teste continua validando `conteudo essencial`, `Nao reprove apenas`, `quebra de linha`, `aviso legal ou regulatorio`, `CRITICA`).
    4. **Cobertura dos Testes 18/19/21 do tasks.md — VERIFICAR (sem reescrever) que os testes existentes deste arquivo já cobrem:**
       - **Teste 18 reviewer-side (9.3 — legalNotice off → `mandatoryArtworkTextSection` vazio):** `:225-240` (`review() sem mandatoryArtworkText não exige texto obrigatório` → `toBe('')`, incl. caso `legalNoticeText: '   '`) e `:252-260` (`8.20 legalNotice desabilitado` → `toBe('')`).
       - **Teste 19 (9.4 — `validityTextSection` montado/vazio):** `:273-284` (`validityText: 'Até 30/09'` → `toContain('Até 30/09')`) e `:286-294` (ausente → `toBe('')`).
       - **Teste 21 (9.6 — aviso default → seção contém a constante + rigor literal):** `:262-271` (`legalNoticeText: 'Imagem meramente ilustrativa'` → `toContain`) e `:188-205` (fidelidade semântica e rigor legal, `CRITICA`).
       Registrar no SUMMARY a confirmação de que os 3 blocos existem e passam após a co-migração.
  </action>
  <verify>
    <automated>npx vitest run src/lib/image-generation/services/__tests__/image-review-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Grep `Imagens meramente ilustrativas` no arquivo → 0
    - Testes existentes :225-240, :252-271, :273-294 presentes e verdes (cobrem Testes 18/19/21 do tasks.md — sem redefinição)
    - Suíte inteira do image-review-service verde
  </acceptance_criteria>
  <done>fixtures do image-review-service.test.ts co-migradas + cobertura dos Testes 18/19/21 verificada — suíte verde</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prompts (instruções) → LLM | Reframe verificado por teste para evitar regressão da imposição fixa |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-07 | Tampering | Reframe dos prompts | mitigate | Testes 16-17 (loop nos 4 arquivos) bloqueiam `SEMPRE acrescente` e exigem bloco condicional + linha mantida (regressão impossível de passar despercebida); Teste 18 (9.3) cobre legalNotice off → `vars.mandatoryArtworkText` vazio |
| T-40-07a | Integrity | Alinhamento de string | mitigate | Check de conteúdo B amarra o literal dos prompts à constante única `ILLUSTRATIVE_NOTICE_TEXT` (F40-02); Teste 21 reviewer existente (:262-271) amarra a seção do revisor à constante |
| T-40-07b | Spoofing | Fixtures de teste divergentes | mitigate | Grep zero plural nos 2 arquivos de teste co-migrados (10.2/10.3) |
| T-40-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo — sem gate de legitimidade |
</threat_model>

<verification>
- `npx vitest run src/lib/campaign/__tests__/prompt-reframe.test.ts` verde (Testes 16-17 + checks A/B)
- `npx vitest run src/lib/image-generation/services/__tests__/image-generation-service.test.ts` verde (incl. golden 38 keys + Teste 18 prompt-side + Teste 20)
- `npx vitest run src/lib/image-generation/services/__tests__/image-review-service.test.ts` verde (Testes 18/19/21 do tasks.md cobertos pelos blocos :225-240, :252-271, :273-294)
- Grep `Imagens meramente ilustrativas` em `src/lib/image-generation/` → 0
</verification>

<success_criteria>
- Testes 16-17 + checks A/B verdes: reframe completo nos 4 prompts, linha mantida, validade intacta, singular alinhado à constante
- Teste 18 (9.3): prompt-side (golden spotlight/exclusive) + reviewer-side (existente :225-240/:252-260) cobrem legalNotice off → vazio
- Teste 20 (9.5): golden com novos campos — EXPECTED_KEYS = 38 intacto
- Testes 19/21 (9.4/9.6) cobertos por testes existentes do revisor (verificação registrada no SUMMARY)
- Fixtures plural→singular em ambos os arquivos de teste de image-generation
</success_criteria>

<output>
Create `.planning/phases/40-campos-comerciais-avisos-brief/40-07-SUMMARY.md` when done
</output>
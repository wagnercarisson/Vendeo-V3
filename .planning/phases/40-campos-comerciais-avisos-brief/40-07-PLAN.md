---
phase: 40-campos-comerciais-avisos-brief
plan: 07
type: execute
wave: 3
depends_on: [40-02, 40-03]
files_modified:
  - src/lib/campaign/__tests__/prompt-reframe.test.ts
  - src/lib/image-generation/services/__tests__/image-generation-service.test.ts
  - src/lib/image-generation/services/__tests__/image-review-service.test.ts
autonomous: true
requirements: [F40-02, F40-11, F40-12, F40-13, F40-14, F40-15, F40-16]

must_haves:
  truths:
    - "Testes 16-21: os 4 prompts não têm mais a imposição SEMPRE fixa, contêm o bloco condicional, mantêm a linha condicional de {{mandatoryArtworkText}} e a linha de validade, e usam apenas o singular 'Imagem meramente ilustrativa'"
    - "Fixtures plural→singular co-migradas em image-generation-service.test.ts (:242/:255/:272/:339/:349) e image-review-service.test.ts (:193/:199)"
    - "EXPECTED_KEYS = 38 do golden por intent permanece intacto (F40-02)"
  artifacts:
    - path: "src/lib/campaign/__tests__/prompt-reframe.test.ts"
      provides: "Testes 16-21 — verificação dos 4 prompts"
      contains: "readFileSync|Quando houver texto obrigatório/aviso legal informado"
    - path: "src/lib/image-generation/services/__tests__/image-generation-service.test.ts"
      provides: "Fixtures singular"
      contains: "Imagem meramente ilustrativa"
    - path: "src/lib/image-generation/services/__tests__/image-review-service.test.ts"
      provides: "Fixtures singular"
      contains: "Imagem meramente ilustrativa"
  key_links:
    - from: "prompt-reframe.test.ts"
      to: "prompts/campaign-image-director*.md"
      via: "fs.readFileSync(path.join(process.cwd(), 'prompts', ...))"
      pattern: "readFileSync"
    - from: "prompt-reframe.test.ts"
      to: "src/lib/campaign/constants.ts"
      via: "verifica que os prompts usam o mesmo literal singular da constante (F40-02)"
      pattern: "ILLUSTRATIVE_NOTICE_TEXT"
---

<objective>
Criar os **testes 16-21** (seção 9 — reframe dos prompts) em `prompt-reframe.test.ts` lendo os 4 arquivos `prompts/campaign-image-director*.md` via `fs.readFileSync`, e **co-migrar as fixtures plural→singular** em `image-generation-service.test.ts` e `image-review-service.test.ts` (10.2/10.3) para alinhar com a constante única (D2).

Purpose: garantir o reframe D6 aplicado nos 4 prompts (sem SEMPRE fixo, com bloco condicional) sem quebrar as linhas de `{{mandatoryArtworkText}}`/validade; alinhar fixtures de teste ao singular canônico.
Output: 1 arquivo de teste novo + 2 arquivos de teste co-migrados.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/40-campos-comerciais-avisos-brief/40-CONTEXT.md (D2 — singular canônico; D6 — reframe; D5 — validade intacta)
@openspec/changes/fase-40-campos-comerciais-avisos-brief/specs/ai-image-generation/spec.md
@openspec/changes/fase-40-campos-comerciais-avisos-brief/tasks.md (seção 9 — testes 16-21; seção 10.2/10.3 — co-migração de fixtures)
@prompts/campaign-image-director.md (reframed no 40-03)
@prompts/campaign-image-director-offer.md
@prompts/campaign-image-director-spotlight.md
@prompts/campaign-image-director-exclusive.md
@src/lib/campaign/constants.ts (ILLUSTRATIVE_NOTICE_TEXT = "Imagem meramente ilustrativa")
@src/lib/image-generation/services/__tests__/image-generation-service.test.ts (LINHAS VERIFICADAS: plural em :242, :255, :272, :339, :349; EXPECTED_KEYS :516-527; golden tests :539+)
@src/lib/image-generation/services/__tests__/image-review-service.test.ts (LINHAS VERIFICADAS: plural em :193, :199)

<interfaces>
Inventário completo de plurais em src/ (grep realizado em 2026-08-14) — dividido por plano:

- `src/components/campaign/mandatory-artwork-field.tsx:24` → plano 40-02 (feito)
- `src/app/api/campaign/generate-image/__tests__/route.test.ts:530, 775, 785, 796, 807` → plano 40-08
- `src/lib/image-generation/services/__tests__/image-review-service.test.ts:193, 199` → ESTE plano (Task 3)
- `src/lib/image-generation/services/__tests__/image-generation-service.test.ts:242, 255, 272, 339, 349` → ESTE plano (Task 2)

Textos de prompt verificados (reframe do 40-03):

- BLOCO_CONDICIONAL = `Quando houver texto obrigatório/aviso legal informado, exiba exatamente esse texto na arte. Se o aviso for "Imagem meramente ilustrativa", posicione-o com tipografia mínima, mas visível e legível, em área lateral horizontal ou vertical, sem competir com oferta, produto e preço.`
- LINHA_MANTIDA = `Se o campo "Texto obrigatório na arte" estiver preenchido ({{mandatoryArtworkText}}), inclua esse texto na arte de forma visível e legível, em tipografia mínima adequada para leitura em dispositivo móvel. Não o repita na legenda.`
- LINHA_VALIDADE = `**Validade da oferta:** {{validity}}` (presente em director.md e offer.md)
- PROHIBIDO = `SEMPRE acrescente a arte o seguinte texto`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: prompt-reframe.test.ts — testes 16-21</name>
  <files>src/lib/campaign/__tests__/prompt-reframe.test.ts</files>
  <read_first>
    - prompts/campaign-image-director.md (linhas 125-135 — bloco condicional + linha mantida)
    - prompts/campaign-image-director-offer.md (linhas 128-136)
    - prompts/campaign-image-director-spotlight.md (linhas 126-133)
    - prompts/campaign-image-director-exclusive.md (linhas 135-142)
    - src/lib/campaign/constants.ts (ILLUSTRATIVE_NOTICE_TEXT)
  </read_first>
  <action>
    Criar `src/lib/campaign/__tests__/prompt-reframe.test.ts` (testes 16-21, tasks.md seção 9):

    1. Setup: `import { describe, it, expect } from "vitest";` + `import { readFileSync } from "node:fs";` + `import path from "node:path";`. Helper `readPrompt(name: string): string` → `readFileSync(path.join(process.cwd(), "prompts", name), "utf-8")`. Lista `const PROMPTS = ["campaign-image-director.md", "campaign-image-director-offer.md", "campaign-image-director-spotlight.md", "campaign-image-director-exclusive.md"];`.
    2. **Teste 16 (director):** `readPrompt("campaign-image-director.md")` NÃO contém `"SEMPRE acrescente a arte o seguinte texto"` E contém `BLOCO_CONDICIONAL` E contém `LINHA_MANTIDA`.
    3. **Teste 17 (offer):** idem para `campaign-image-director-offer.md` (mesmas 3 asserções).
    4. **Teste 18 (spotlight):** idem para `campaign-image-director-spotlight.md`.
    5. **Teste 19 (exclusive):** idem para `campaign-image-director-exclusive.md`.
    6. **Teste 20 (linha de validade intacta):** `campaign-image-director.md` e `campaign-image-director-offer.md` contêm `LINHA_VALIDADE` (`**Validade da oferta:** {{validity}}`); spotlight e exclusive NÃO contêm (estrutura original) — assertar presença apenas onde existe.
    7. **Teste 21 (alinhamento com a constante — F40-02):** para cada arquivo em PROMPTS: NÃO contém `"Imagens meramente ilustrativas"` (plural) e contém `"Imagem meramente ilustrativa"` (singular) — alinhado a `ILLUSTRATIVE_NOTICE_TEXT`; importar a constante e comparar `"Imagem meramente ilustrativa"` literal de teste === `ILLUSTRATIVE_NOTICE_TEXT`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/campaign/__tests__/prompt-reframe.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe com testes 16-21; testes 16-19 cobrem os 4 prompts (proibido ausente, bloco presente, linha mantida presente)
    - Teste 20 valida LINHA_VALIDADE em director/offer e ausência em spotlight/exclusive
    - Teste 21 valida singular via constante nos 4 arquivos
    - `npx vitest run <arquivo>` passa
  </acceptance_criteria>
  <done>testes 16-21 verdes — reframe confirmado nos 4 prompts com validade intacta e singular alinhado à constante</done>
</task>

<task type="auto">
  <name>Task 2: Co-migração de fixtures — image-generation-service.test.ts (10.2)</name>
  <files>src/lib/image-generation/services/__tests__/image-generation-service.test.ts</files>
  <read_first>
    - src/lib/image-generation/services/__tests__/image-generation-service.test.ts (linhas 242, 255, 272, 339, 349 — plurais; :516-527 EXPECTED_KEYS; :539-544 golden test já com singular)
  </read_first>
  <action>
    Co-migrar as 5 ocorrências de `'Imagens meramente ilustrativas'` para `'Imagem meramente ilustrativa'` (singular, D2) nas linhas 242, 255, 272, 339 e 349:

    1. Substituir `'Imagens meramente ilustrativas'` → `'Imagem meramente ilustrativa'` em TODAS as ocorrências do arquivo (fixtures `createMinimalBrief({ mandatoryArtworkText: ... })` e asserts `toContain`/`toBe`).
    2. NÃO alterar `EXPECTED_KEYS` (:516-527 — 38 keys intactas) nem a estrutura dos golden tests 8.16/8.17/8.18 (:539+, já usam singular em :543).
    3. NENHUMA outra alteração — fixtures são strings de teste, sem impacto de comportamento.
  </action>
  <verify>
    <automated>npx vitest run src/lib/image-generation/services/__tests__/image-generation-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Grep `Imagens meramente ilustrativas` no arquivo → 0
    - Grep `Imagem meramente ilustrativa` → 5+ ocorrências
    - Suíte inteira verde (inclui golden 8.16/8.17/8.18 com EXPECTED_KEYS 38)
  </acceptance_criteria>
  <done>fixtures do image-generation-service.test.ts co-migradas para o singular — suíte verde</done>
</task>

<task type="auto">
  <name>Task 3: Co-migração de fixtures — image-review-service.test.ts (10.3)</name>
  <files>src/lib/image-generation/services/__tests__/image-review-service.test.ts</files>
  <read_first>
    - src/lib/image-generation/services/__tests__/image-review-service.test.ts (linhas 188-205 — teste da mandatoryArtworkTextSection; plural em :193 e assert em :199)
  </read_first>
  <action>
    Co-migrar as 2 ocorrências em `image-review-service.test.ts`:

    1. Linha 193: `legalNoticeText: 'Imagens meramente ilustrativas',` → `legalNoticeText: 'Imagem meramente ilustrativa',`
    2. Linha 199: `expect(vars.mandatoryArtworkTextSection).toContain('Imagens meramente ilustrativas');` → `...toContain('Imagem meramente ilustrativa');`
    3. NENHUMA outra alteração (o teste continua validando `conteudo essencial`, `Nao reprove apenas`, `quebra de linha`, `aviso legal ou regulatorio`, `CRITICA`).
  </action>
  <verify>
    <automated>npx vitest run src/lib/image-generation/services/__tests__/image-review-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Grep `Imagens meramente ilustrativas` no arquivo → 0
    - Suíte inteira do image-review-service verde
  </acceptance_criteria>
  <done>fixtures do image-review-service.test.ts co-migradas — suíte verde</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prompts (instruções) → LLM | Reframe verificado por teste para evitar regressão da imposição fixa |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-07 | Tampering | Reframe dos prompts | mitigate | Testes 16-19 bloqueiam `SEMPRE acrescente` e exigem bloco condicional + linha mantida nos 4 arquivos (regressão impossível de passar despercebida) |
| T-40-07a | Integrity | Alinhamento de string | mitigate | Teste 21 amarra o literal dos prompts à constante única `ILLUSTRATIVE_NOTICE_TEXT` (F40-02) |
| T-40-07b | Spoofing | Fixtures de teste divergentes | mitigate | Grep zero plural nos 2 arquivos de teste co-migrados (10.2/10.3) |
| T-40-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo — sem gate de legitimidade |
</threat_model>

<verification>
- `npx vitest run src/lib/campaign/__tests__/prompt-reframe.test.ts` verde (6 testes)
- `npx vitest run src/lib/image-generation/services/__tests__/image-generation-service.test.ts` verde (incl. golden 38 keys)
- `npx vitest run src/lib/image-generation/services/__tests__/image-review-service.test.ts` verde
- Grep `Imagens meramente ilustrativas` em `src/lib/image-generation/` → 0
</verification>

<success_criteria>
- Testes 16-21 verdes: reframe completo nos 4 prompts, validade intacta, singular alinhado à constante
- Fixtures plural→singular em ambos os arquivos de teste de image-generation
- EXPECTED_KEYS = 38 intacto; zero plurais em src/lib/image-generation/
</success_criteria>

<output>
Create `.planning/phases/40-campos-comerciais-avisos-brief/40-07-SUMMARY.md` when done
</output>
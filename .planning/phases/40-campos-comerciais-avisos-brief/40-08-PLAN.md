---
phase: 40-campos-comerciais-avisos-brief
plan: 08
type: execute
wave: 4
depends_on: [40-06, 40-07]
files_modified:
  - src/app/api/campaign/generate-image/__tests__/route.test.ts
autonomous: true
requirements: [F40-02, F40-15, F40-16]

must_haves:
  truths:
    - "As 5 ocorrências plural→singular no route.test.ts (:530, :775, :785, :796, :807) estão co-migradas"
    - "O teste de snapshot versionado (Test #26) continua assertando legalNotice.text com o valor singular"
    - "A suíte completa de testes roda verde (regressão 10.6/10.7) com typecheck e lint limpos"
  artifacts:
    - path: "src/app/api/campaign/generate-image/__tests__/route.test.ts"
      provides: "Fixtures singular alinhadas à constante"
      contains: "Imagem meramente ilustrativa"
  key_links:
    - from: "route.test.ts (:771-790)"
      to: "campaign_brief_v1 snapshot"
      via: "mandatoryArtworkText → inputSnapshot.commercial.legalNotice.text"
      pattern: "legalNotice: \{[^}]*text"
    - from: "route.test.ts"
      to: "src/lib/campaign/constants.ts"
      via: "valor canônico singular (D2)"
      pattern: "Imagem meramente ilustrativa"
---

<objective>
**Co-migrar as fixtures** do `route.test.ts` (10.1) plural→singular e executar a **regressão completa** (10.6/10.7): suíte inteira de testes + typecheck + lint, confirmando que a fase inteira (planos 40-02 a 40-08) está íntegra.

Purpose: D2 — alinhar todas as fixtures de rota ao singular canônico; verificação de não-regressão após as mudanças de form/prompts/fixtures.
Output: route.test.ts co-migrado + relatório de regressão verde (testes/typecheck/lint).
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/40-campos-comerciais-avisos-brief/40-CONTEXT.md (D2 — singular canônico; D9 — sem SQL migration)
@openspec/changes/fase-40-campos-comerciais-avisos-brief/tasks.md (seção 10.1 — co-migração route.test.ts; seção 10.6/10.7 — regressão)
@src/app/api/campaign/generate-image/__tests__/route.test.ts (LINHAS VERIFICADAS: plural em :530, :775, :785, :796, :807)
@src/lib/campaign/constants.ts (ILLUSTRATIVE_NOTICE_TEXT = "Imagem meramente ilustrativa")

<interfaces>
Ocorrências plurais no route.test.ts (grep 2026-08-14):

- :530 → `const req = makeRequest({ ...VALID_REQUEST_BODY, mandatoryArtworkText: 'Imagens meramente ilustrativas' });` (Test #7)
- :775 → idem (Test #26)
- :785 → `legalNotice: { enabled: true, text: 'Imagens meramente ilustrativas' },` (assert do snapshot)
- :796 → idem :530 (mandatoryArtworkText no Image Director)
- :807 → idem :530 (mandatoryArtworkText AUSENTE no Copy Director)

Todas → `'Imagem meramente ilustrativa'` (singular). VALID_REQUEST_BODY não contém mandatoryArtworkText (Test #8 :541-548 prova o fluxo sem o campo).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Co-migração de fixtures — route.test.ts (10.1)</name>
  <files>src/app/api/campaign/generate-image/__tests__/route.test.ts</files>
  <read_first>
    - src/app/api/campaign/generate-image/__tests__/route.test.ts (linhas 524-548 Test #7/#8; :769-812 Test #26-28)
  </read_first>
  <action>
    Co-migrar as 5 ocorrências de `'Imagens meramente ilustrativas'` para `'Imagem meramente ilustrativa'` no `route.test.ts` (10.1):

    1. Substituir em TODAS as ocorrências do arquivo: `'Imagens meramente ilustrativas'` → `'Imagem meramente ilustrativa'` (linhas 530, 775, 785, 796, 807).
    2. NÃO alterar estrutura de testes nem mocks — são valores de fixture; o assert do snapshot (:785) continua validando `legalNotice: { enabled: true, text: ... }` com o novo valor singular.
    3. Rodar a suíte do arquivo para confirmar: `npx vitest run src/app/api/campaign/generate-image/__tests__/route.test.ts`.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/campaign/generate-image/__tests__/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Grep `Imagens meramente ilustrativas` no arquivo → 0
    - Grep `Imagem meramente ilustrativa` → 5 ocorrências
    - Suíte do route.test.ts verde (Test #7, #26-28 incluídos)
  </acceptance_criteria>
  <done>route.test.ts co-migrado — suíte verde</done>
</task>

<task type="auto">
  <name>Task 2: Regressão completa — testes, typecheck e lint (10.6/10.7)</name>
  <files>src/app/api/campaign/generate-image/__tests__/route.test.ts</files>
  <read_first>
    - .planning/phases/40-campos-comerciais-avisos-brief/40-PATTERNS.md (padrão de comandos de gate: vitest, tsc --noEmit, next lint)
  </read_first>
  <action>
    Executar a regressão completa da fase (10.6/10.7):

    1. Suíte inteira: `npx vitest run` (sem filtro) — deve terminar verde; registrar a contagem de testes no SUMMARY (referência da fase 39: ~1950+).
    2. Typecheck: `npx tsc --noEmit` — zero erros.
    3. Lint: `npx next lint` (ou script `lint` do package.json) — zero erros.
    4. Se qualquer etapa falhar, corrigir APENAS o desvio (fixture/typing) e repetir até verde.
    5. Registrar os 3 resultados (testes/typecheck/lint) no SUMMARY com evidência (saídas truncadas se necessário).
  </action>
  <verify>
    <automated>npx vitest run; npx tsc --noEmit; npx next lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` verde (suíte inteira, com contagem registrada)
    - `npx tsc --noEmit` sem erros
    - `npx next lint` sem erros
    - Grep `Imagens meramente ilustrativas` em `src/` → 0 (zero plurais restantes na fase)
  </acceptance_criteria>
  <done>regressão completa verde — testes, typecheck e lint limpos</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixtures de teste → rota | Valores de entrada do route test definem o contrato da API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-08 | Tampering | Fixtures da rota | mitigate | Co-migração 5/5 + grep zero plural em src/ — garante contrato singular consistente |
| T-40-08a | Integrity | Snapshot versionado | mitigate | Test #26 ainda asserta `legalNotice.text` após co-migração (10.1) — contrato `campaign_brief_v1` intacto |
| T-40-08b | DoS | Regressão silenciosa | mitigate | Gate completo (vitest + tsc + lint) na Task 2 antes da verificação de fase |
| T-40-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo — sem gate de legitimidade |
</threat_model>

<verification>
- `npx vitest run` verde (suíte inteira)
- `npx tsc --noEmit` sem erros
- `npx next lint` sem erros
- Grep `Imagens meramente ilustrativas` em `src/` → 0
</verification>

<success_criteria>
- route.test.ts com 5 fixtures singulares e suíte verde
- Regressão completa da fase verde: vitest inteiro, typecheck e lint limpos
- Zero plurais canônicos divergentes em src/
</success_criteria>

<output>
Create `.planning/phases/40-campos-comerciais-avisos-brief/40-08-SUMMARY.md` when done
</output>
---
phase: 40-campos-comerciais-avisos-brief
plan: 09
type: execute
wave: 5
depends_on: [40-01, 40-02, 40-03, 40-04, 40-05, 40-06, 40-07, 40-08]
files_modified:
  - .planning/phases/40-campos-comerciais-avisos-brief/40-VERIFICATION.md
  - .planning/phases/40-campos-comerciais-avisos-brief/40-UAT.md
autonomous: false
requirements: [F40-18]

must_haves:
  truths:
    - "Gates automáticos verdes: npx vitest run (suíte completa), npm run typecheck, npm run lint, npm run build"
    - "40-VERIFICATION.md existe com evidências por plano (grep gates, contagem de testes, typecheck/lint/build outputs) e matriz de cobertura F40-01..F40-18"
    - "40-UAT.md existe com checklist humano (seção 11 do tasks.md) cobrindo: checkbox default marcado e aviso na arte, desmarcado só texto livre, validade na arte (até dd/mm / hoje / estoques / custom sem prefixo duplicado), seletor de validade só em offer, persistência de draft ao trocar intenção/recarregar, migração de draft legado, e ausência de aviso fixo quando nada informado"
    - "Nenhum requisito F40-01..F40-18 sem cobertura comprovada"
  artifacts:
    - path: ".planning/phases/40-campos-comerciais-avisos-brief/40-VERIFICATION.md"
      provides: "Evidências automatizadas da fase (gates + cobertura de requisitos)"
      contains: "F40-"
    - path: ".planning/phases/40-campos-comerciais-avisos-brief/40-UAT.md"
      provides: "Roteiro de UAT humana (checklist seção 11)"
      contains: "UAT"
  key_links:
    - from: "40-VERIFICATION.md"
      to: "gates de CI"
      via: "npx vitest run / npm run typecheck / npm run lint / npm run build"
      pattern: "vitest"
    - from: "40-UAT.md"
      to: "src/components/flow/campaign-input-form.tsx"
      via: "verificação humana do checkbox ilustrativo + seletor de validade (só offer) no form"
      pattern: "Exibir"
---

<objective>
Executar a VERIFICAÇÃO FINAL da F40 (gates automáticos + UAT manual) e produzir os artefatos de fase `40-VERIFICATION.md` e `40-UAT.md`.

Purpose: F40-18 — fechar a fase com evidência objetiva de que (a) todos os gates passam (vitest/typecheck/lint/build), (b) os requisitos F40-01..F40-18 têm cobertura comprovada por planos/testes, e (c) o comportamento comercial (checkbox ilustrativo, texto obrigatório, validade da oferta) funciona de ponta a ponta com UAT humana.
Output: 40-VERIFICATION.md + 40-UAT.md; base para o orchestrator marcar a fase completa.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@openspec/changes/fase-40-campos-comerciais-avisos-brief/tasks.md (seção 11 — 11.1 a 11.11: verificação + UAT)
@openspec/changes/fase-40-campos-comerciais-avisos-brief/specs/* (specs de contrato — referência de cobertura)
@.planning/phases/40-campos-comerciais-avisos-brief/40-0X-SUMMARY.md (resumos dos planos 40-01..40-08 — evidências por plano)
@.planning/phases/40-campos-comerciais-avisos-brief/40-CONTEXT.md (D2-D9 — decisões verificáveis no UAT)
@package.json (scripts: test/typecheck/lint/build)

<interfaces>
Gates (package.json, padrão F39):
- `npx vitest run` — suíte completa (novos testes 40-06/40-07 + co-migrados + existentes)
- `npm run typecheck` — `tsc -p tsconfig.typecheck.json --noEmit`
- `npm run lint` — `eslint .`
- `npm run build` — `next build`

Checklist UAT (tasks.md seção 11 — deve dirigir o checkpoint humano; itens de observação por decisão):
1. **D3/D8 (form):** seção "Avisos e texto obrigatório" no form com checkbox "Exibir 'Imagem meramente ilustrativa'" MARCADO por default; campo texto obrigatório com placeholder "Ex: Imagem meramente ilustrativa".
2. **D3 (arte):** gerar oferta com checkbox marcado SEM texto livre → arte contém "Imagem meramente ilustrativa"; com texto livre → arte contém "Imagem meramente ilustrativa" + texto; checkbox desmarcado + texto livre → arte contém SOMENTE o texto livre.
3. **D4/D5 (validade):** seletor "Validade da oferta" visível APENAS em oferta; modo "Até uma data" → "até dd/mm" na arte; "Somente hoje" → "somente hoje"; "Enquanto durarem os estoques" → "enquanto durarem os estoques"; modo custom com "Oferta válida: ..." → arte SEM prefixo duplicado.
4. **D4 (persistência):** trocar de intenção (offer→spotlight→offer) NÃO limpa validade/aviso; recarregar a página restaura draft (sessionStorage) com validade/aviso.
5. **F40 (legado):** draft antigo (com texto de aviso salvo) restaura o texto no campo livre.
6. **D6 (prompts):** campanha sem aviso informado (checkbox desmarcado, sem texto) → arte SEM aviso fixo (reframe; antes SEMPRE teria "Imagem meramente ilustrativa").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gates automáticos — vitest, typecheck, lint, build (e correção de regressões)</name>
  <files>.planning/phases/40-campos-comerciais-avisos-brief/40-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/40-campos-comerciais-avisos-brief/40-0X-SUMMARY.md (resumos 01-08 — evidências coletadas)
    - package.json (scripts: test/typecheck/lint/build)
  </read_first>
  <action>
    Executar os 4 gates na ordem (cada um com saída capturada):
    1. `npx vitest run` — suíte completa (testes 1-8 validade, 9-15 aviso, 16-21 prompts do 40-06/40-07; fixtures co-migradas; testes existentes). Registrar contagem total (referência F39: ~1950) e falhas (se houver, corrigir regressões residuais de tipo/fixture e re-rodar).
    2. `npm run typecheck` — 0 erros.
    3. `npm run lint` — 0 erros.
    4. `npm run build` — build bem-sucedido (a fase não adiciona deps/env).
    Para cada gate, registrar: comando exato, exit code, evidência de saída (trecho) e associação aos planos (ex.: "testes 9-12 do 40-06 passaram — evidência de F40-05/F40-06"). Criar o `40-VERIFICATION.md` com: matriz planos×gates, contagens, e matriz de cobertura F40-01..F40-18 (cada requisito → plano(s)/testes).
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | Select-String -Pattern "Test Files|Tests "; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Todos os 4 gates exit 0 — registrados no 40-VERIFICATION.md (matriz planos×gates, contagens, cobertura F40-01..F40-18 com referência aos planos/testes)
    - Nenhuma regressão residual: suíte completa verde com fixtures migradas
  </acceptance_criteria>
  <done>Gates automáticos verdes; 40-VERIFICATION.md criado com evidências + cobertura por requisito</done>
</task>

<task type="auto">
  <name>Task 2: 40-UAT.md — roteiro de UAT humana (checklist seção 11) + registro de resultado</name>
  <files>.planning/phases/40-campos-comerciais-avisos-brief/40-UAT.md</files>
  <read_first>
    - .planning/phases/40-campos-comerciais-avisos-brief/40-VERIFICATION.md (recém-criado — contexto de gates ok)
    - openspec/changes/fase-40-campos-comerciais-avisos-brief/tasks.md (seção 11 — checklist completo)
  </read_first>
  <action>
    Criar `40-UAT.md` com:
    1. Título/contexto: F40 Campos Comerciais e Avisos do Brief — UAT local pós-implementação.
    2. Checklist (6 itens do <interfaces>) com passos exatos por item (URL/form — `campanhas/nova`, o que observar no form e na arte gerada) e coluna de RESULTADO (PASS/FAIL + observação) — campos marcados como PENDENTE para confirmação humana.
    3. Seção "verificação no snapshot" (opcional, admin/DB): `input_snapshot` com `commercial.validityText`/`legalNotice.text` corretos quando informados.
    4. Instrução de preenchimento para o humano (como marcar PASS/FAIL e o que registrar em caso de falha — incl. repro passo a passo).
  </action>
  <verify>
    <automated>Select-String -LiteralPath ".planning\phases\40-campos-comerciais-avisos-brief\40-UAT.md" -Pattern "PASS/FAIL|Exibir"</automated>
  </verify>
  <acceptance_criteria>
    - `40-UAT.md` existe com os 6 itens da checklist, passos exatos e colunas de resultado (grep `PASS/FAIL`)
    - Referência explícita ao checkbox "Exibir" (grep) e à validade por modo
    - Instrução de preenchimento humana presente
  </acceptance_criteria>
  <done>40-UAT.md criado com checklist seção 11 pronto para verificação humana</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT humana — validação visual e funcional do fluxo comercial</name>
  <what-built>
    O executor automatizou toda a F40 (constante única, checkbox ilustrativo, texto obrigatório, validade da oferta em 6 modos, reframe dos prompts, testes 1-21 e regressão completa verde). O 40-UAT.md está pronto com o checklist de 6 itens.
  </what-built>
  <how-to-verify>
    1. Rodar o app local (ex.: `npm run dev`) e abrir `http://localhost:3000/campanhas/nova`.
    2. Verificar a seção "Avisos e texto obrigatório": checkbox "Exibir 'Imagem meramente ilustrativa'" vem MARCADO por default (item 1).
    3. Gerar uma campanha de oferta com o checkbox marcado e SEM texto livre → a arte deve conter o aviso "Imagem meramente ilustrativa" (item 2).
    4. Gerar com checkbox DESMARCADO + texto livre → a arte contém SOMENTE o texto livre; sem aviso fixo (item 2 + item 6).
    5. No form, o seletor "Validade da oferta" aparece apenas quando a intenção é oferta; testar os modos "Até uma data" (→ "até dd/mm" na arte), "Somente hoje" e "Enquanto durarem os estoques" (item 3).
    6. Trocar a intenção (offer→spotlight→offer) e recarregar a página: validade/aviso devem persistir (draft em sessionStorage) (item 4).
    7. Preencher cada item do checklist em 40-UAT.md com PASS/FAIL e observações.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found (com repro passo a passo)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → CI/build | Comandos automatizados (vitest/tsc/eslint/next build) executados no ambiente |
| UAT → produção | Verificação humana de comportamento (form/artes/persistência) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-09 | Integrity | Gate com falso positivo (teste que passa sem cobrir) | mitigate | 40-VERIFICATION.md associa cada requisito F40-xx aos planos/testes específicos (matriz de cobertura — não só "ficou verde") |
| T-40-09a | Tampering | Regressão silenciosa de geração (aviso fixo antigo) | mitigate | Teste 16-21 (40-07) bloqueia `SEMPRE acrescente`; UAT item 6 confirma ausência de aviso fixo quando nada informado |
| T-40-09b | Spoofing | Persistência de draft quebrada | mitigate | Teste de migração legada + preservação de validade (40-06) + UAT item 4 (troca de intenção e reload) |
| T-40-09c | Spoofing | UAT não preenchido (checklist vazio) | mitigate | Campos PASS/FAIL explícitos + instrução de preenchimento; checkpoint blocking impede fechamento sem validação humana |
| T-40-SC | Tampering | Instalação de pacotes | accept | Nenhum pacote novo — sem gate de legitimidade |
</threat_model>

<verification>
- `npx vitest run` exit 0 (suíte completa); `npm run typecheck` exit 0; `npm run lint` exit 0; `npm run build` exit 0
- 40-VERIFICATION.md documenta os comandos e a matriz de cobertura F40-01..F40-18
- 40-UAT.md criado (checklist humana) — preenchimento final via checkpoint de UAT humana
</verification>

<success_criteria>
- Gates verdes e documentados em 40-VERIFICATION.md (com cobertura por requisito)
- 40-UAT.md com checklist seção 11 pronto (PASS/FAIL pendente para confirmação humana)
- Fase pronta para fechamento pelo orchestrator após UAT humano (checkpoint blocking)
</success_criteria>

<output>
Create `.planning/phases/40-campos-comerciais-avisos-brief/40-09-SUMMARY.md` when done
</output>
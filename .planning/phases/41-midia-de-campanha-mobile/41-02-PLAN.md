---
phase: 41-midia-de-campanha-mobile
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/image-generation/config.ts
  - src/lib/image-generation/schema.ts
  - src/app/api/campaign/generate-image/route.ts
autonomous: true
requirements: [F41-10, F41-11]

must_haves:
  truths:
    - "MAX_CAMPAIGN_IMAGES = 4 (1 primary + 3 auxiliares) é a fonte única do teto no transporte, no form e nos testes"
    - "O transporte aceita productImages[] opcional (mín 1, máx MAX_CAMPAIGN_IMAGES, exatamente 1 primary) e o item NÃO carrega id"
    - "productImageDataUrl deixa de ser required no Zod (preservação comportamental) e .strict() é preservado"
  artifacts:
    - path: "src/lib/image-generation/config.ts"
      provides: "Constantes de limite D10"
      contains: "MAX_CAMPAIGN_IMAGES"
    - path: "src/lib/image-generation/schema.ts"
      provides: "ProductImageInputSchema + productImages + legado optional"
      contains: "ProductImageInputSchema"
  key_links:
    - from: "src/lib/image-generation/schema.ts"
      to: "src/lib/image-generation/config.ts"
      via: "productImages.max(MAX_CAMPAIGN_IMAGES) importa a constante"
      pattern: "MAX_CAMPAIGN_IMAGES"
---

<objective>
Adicionar as **constantes de limite D10** em `src/lib/image-generation/config.ts` (`MAX_CAMPAIGN_IMAGES = 4` + teto agregado) e o **transporte aditivo D2/D10** em `src/lib/image-generation/schema.ts`: `ProductImageInputSchema` exportado (item `{ role, source, mimeType, dataUrl }` **sem `id`**), campo opcional `productImages: z.array(...).min(1).max(MAX_CAMPAIGN_IMAGES).superRefine(exactly-1-primary).optional()`, `productImageDataUrl` passa a `optional()` (preservação **comportamental**), e `.strict()` preservado.

Purpose: D2 (transporte aditivo — payload legado continua funcionando) + D10 (teto `MAX_CAMPAIGN_IMAGES = 4` + teto agregado). A obrigatoriedade da imagem passa a ser garantida pela regra de exclusividade da rota (plano 41-06), NÃO pelo Zod.
Output: config.ts com 2 constantes novas; schema.ts com o novo contrato de transporte.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-midia-de-campanha-mobile/41-CONTEXT.md (D2, D10; Specifics — ProductImageInputSchema; tabela de exclusividade)
@.planning/phases/41-midia-de-campanha-mobile/41-PATTERNS.md (seções config.ts e schema.ts — expected F41 content, superRefine do mediaSchema do domínio)
@openspec/changes/fase-41-midia-de-campanha-mobile/specs/ai-image-generation/spec.md (GenerateImageRequestSchema — productImages aditivo)
@openspec/changes/fase-41-midia-de-campanha-mobile/specs/campaign-brief-contract/spec.md (invariante exactly-1-primary no transporte)
@openspec/changes/fase-41-midia-de-campanha-mobile/tasks.md (seção 2 — 2.1 a 2.3; seção 3 — 3.1 a 3.4)
@src/lib/image-generation/config.ts (linhas 26-28 — bloco "Payload Size Limits")
@src/lib/image-generation/schema.ts (linhas 8-37 — GenerateImageRequestSchema com .strict() na :37)
@src/lib/campaign/brief-schema.ts (linhas 60-86 — mediaSchema com superRefine exactly-1-primary — padrão a replicar no transporte)

<interfaces>
Contrato exato a criar (per tasks.md 2.1-2.3 + 3.1-3.4):

```typescript
// src/lib/image-generation/config.ts — adicionar no bloco "Payload Size Limits" (:26-28):
export const MAX_CAMPAIGN_IMAGES = 4; // 1 primary + 3 auxiliares (D3/D10)
export const MAX_PRODUCT_IMAGES_AGGREGATE_BASE64_SIZE = 8 * 1024 * 1024; // teto agregado do productImages[] (D10)
// MAX_PRODUCT_IMAGE_BASE64_SIZE / MAX_PRODUCT_IMAGE_FILE_SIZE permanecem (:27-28)
```

```typescript
// src/lib/image-generation/schema.ts — NOVO export (D2, sem id — rota gera/normaliza):
export const ProductImageInputSchema = z
  .object({
    role: z.enum(["primary", "variation", "combo_item", "reference"]),
    source: z.enum(["upload", "camera"]),
    mimeType: z.string(),
    dataUrl: z.string().min(1), // base64 (transporte); snapshot NUNCA persiste
  })
  .strict();

// DENTRO de GenerateImageRequestSchema — substituir :30 e adicionar após :29:
productImageDataUrl: z.string().min(1).optional(),  // era required (:30) — preservação comportamental (D2)
productImages: z
  .array(ProductImageInputSchema)
  .min(1)
  .max(MAX_CAMPAIGN_IMAGES)
  .superRefine((imgs, ctx) => {
    const primary = imgs.filter((i) => i.role === "primary").length;
    if (primary !== 1) {
      ctx.addIssue({
        code: "custom",
        path: ["productImages"],
        message: `Deve existir exatamente 1 imagem com role "primary" (recebido: ${primary})`,
      });
    }
  })
  .optional(),
// `.strict()` preservado (:37)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: config.ts — MAX_CAMPAIGN_IMAGES + teto agregado</name>
  <files>src/lib/image-generation/config.ts</files>
  <read_first>
    - src/lib/image-generation/config.ts (linhas 26-28 — bloco "Payload Size Limits"; linhas 1-8 — estilo de export const)
    - openspec/changes/fase-41-midia-de-campanha-mobile/tasks.md (seção 2 — 2.1 a 2.3)
  </read_first>
  <action>
    Adicionar no bloco `// ─── Payload Size Limits ───` de `config.ts` (após a linha 28), D10:
    1. `export const MAX_CAMPAIGN_IMAGES = 4;` — comentário `// 1 primary + 3 auxiliares (D3/D10)`.
    2. `export const MAX_PRODUCT_IMAGES_AGGREGATE_BASE64_SIZE = 8 * 1024 * 1024;` — comentário `// teto agregado do productImages[] (D10)`.
    3. MANTER `MAX_PRODUCT_IMAGE_BASE64_SIZE` (`4 * 1024 * 1024`) e `MAX_PRODUCT_IMAGE_FILE_SIZE` (`1 * 1024 * 1024`) intactos — referência do fluxo legado single (D2).
    4. NÃO usar `process.env` para estas constantes — são limites fixos do produto (padrão das constantes atuais :27-28).
    5. Estilo: `export const` puro, sem `server-only`, sem imports (importáveis por schema.ts, route.ts, form e testes).
  </action>
  <verify>
    <automated>
      $c = Select-String -LiteralPath "src\lib\image-generation\config.ts" -Pattern "MAX_CAMPAIGN_IMAGES = 4|MAX_PRODUCT_IMAGES_AGGREGATE_BASE64_SIZE = 8 \* 1024 \* 1024" | Measure-Object; if ($c.Count -lt 2) { Write-Error "Constantes D10 faltando em config.ts" } else { Write-Output "OK: MAX_CAMPAIGN_IMAGES + teto agregado presentes" }; npx vitest run src/__tests__/f31-2-intent-tests.test.ts src/__tests__/api/campaign-intent-guard.test.ts 2>$null; npm run typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - Grep `MAX_CAMPAIGN_IMAGES = 4` em config.ts → 1 ocorrência
    - Grep `MAX_PRODUCT_IMAGES_AGGREGATE_BASE64_SIZE` em config.ts → 1 ocorrência (valor `8 * 1024 * 1024`)
    - Grep `MAX_PRODUCT_IMAGE_BASE64_SIZE = 4 \* 1024 \* 1024` em config.ts → 1 ocorrência (mantida)
    - `npm run typecheck` limpo (constantes novas não quebram nenhum consumidor)
  </acceptance_criteria>
  <done>config.ts tem MAX_CAMPAIGN_IMAGES=4 + teto agregado 8MB; limites legados preservados</done>
</task>

<task type="auto">
  <name>Task 2: schema.ts — ProductImageInputSchema + productImages + productImageDataUrl optional</name>
  <files>src/lib/image-generation/schema.ts</files>
  <read_first>
    - src/lib/image-generation/schema.ts (linhas 8-37 — GenerateImageRequestSchema; :30 productImageDataUrl required; :37 .strict())
    - src/lib/campaign/brief-schema.ts (linhas 60-86 — mediaSchema: padrão do superRefine exactly-1-primary no zod do domínio, a replicar no transporte)
    - openspec/changes/fase-41-midia-de-campanha-mobile/specs/ai-image-generation/spec.md (GenerateImageRequestSchema — D2/D10)
  </read_first>
  <action>
    Modificar `schema.ts` (tasks 3.1-3.4):
    1. **Importar** `MAX_CAMPAIGN_IMAGES` de `@/lib/image-generation/config`: `import { MAX_CAMPAIGN_IMAGES } from "@/lib/image-generation/config";` (config.ts é módulo neutro sem server-only — importável pelo schema).
    2. **Novo export** `ProductImageInputSchema` (antes ou depois do GenerateImageRequestSchema) — objeto `{ role: z.enum(["primary", "variation", "combo_item", "reference"]), source: z.enum(["upload", "camera"]), mimeType: z.string(), dataUrl: z.string().min(1) }` com `.strict()` — **SEM campo `id`** (a rota gera/normaliza — D2/D5).
    3. **`productImageDataUrl`** (:30): trocar `z.string().min(1, "Imagem do produto é obrigatória")` por `z.string().min(1).optional()` — a mensagem de obrigatoriedade deixa de ser do Zod (vira 400 da rota, plano 41-06 — D2 preservação comportamental).
    4. **Adicionar** `productImages: z.array(ProductImageInputSchema).min(1).max(MAX_CAMPAIGN_IMAGES).superRefine(exactly-1-primary).optional()` — superRefine com a MESMA lógica do `mediaSchema` do domínio (`brief-schema.ts:75-83`): contar `role === "primary"`, `primary !== 1` → `ctx.addIssue({ code: "custom", path: ["productImages"], message: \`Deve existir exatamente 1 imagem com role "primary" (recebido: ${primary})\` })`.
    5. **`.strict()`** (:37) preservado — o campo novo é aditivo; payload legado continua passando (D2).
    6. NÃO alterar os demais campos (storeId, productName, validity, mandatoryArtworkText, inputValidationOverride, etc.) — nada mais muda no schema.
  </action>
  <verify>
    <automated>
      $s = Select-String -LiteralPath "src\lib\image-generation\schema.ts" -Pattern "ProductImageInputSchema|productImages: z|productImageDataUrl: z.string\(\)\.min\(1\)\.optional\(\)|\.strict\(\)" | Measure-Object; if ($s.Count -lt 4) { Write-Error "Schema F41 incompleto" } else { Write-Output "OK: schema aditivo completo" }; npx vitest run src/lib/campaign/__tests__/ src/__tests__/f31-2-intent-tests.test.ts src/__tests__/api/campaign-intent-guard.test.ts 2>$null; npm run typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - Grep `export const ProductImageInputSchema` em schema.ts → 1 ocorrência
    - Grep `productImages: z` em schema.ts → 1 ocorrência; contém `.min(1)`, `.max(MAX_CAMPAIGN_IMAGES)`, `.superRefine` e `.optional()`
    - Grep `productImageDataUrl: z.string().min(1).optional()` em schema.ts → 1 ocorrência (required removido)
    - Grep `role === "primary"` no superRefine do transporte → 1 ocorrência (invariante no transporte)
    - Grep `\.strict\(\)` em schema.ts → pelo menos 1 ocorrência (GenerateImageRequestSchema preserva strict)
    - Suíte `npx vitest run src/lib/campaign/__tests__/ src/__tests__/f31-2-intent-tests.test.ts src/__tests__/api/campaign-intent-guard.test.ts` sem regressão (testes de schema existentes passam — o campo novo é opcional; nota: não existe `src/lib/image-generation/__tests__/` — os consumidores reais do schema são `src/__tests__/f31-2-intent-tests.test.ts` e `src/__tests__/api/campaign-intent-guard.test.ts`)
    - `npm run typecheck` limpo
  </acceptance_criteria>
  <done>schema.ts exporta ProductImageInputSchema; productImages[] aditivo com invariante; productImageDataUrl optional; strict preservado</done>
</task>

<task type="auto">
  <name>Task 3: route.ts — co-migração do consumidor (productImageDataUrl optional → não-null após guard)</name>
  <files>src/app/api/campaign/generate-image/route.ts</files>
  <read_first>
    - src/app/api/campaign/generate-image/route.ts (linha 118-123 — guard de presença de imagem ANTES do schema parse; :301-305 — inputValidation.validate com parsed.data.productImageDataUrl)
    - src/lib/image-generation/schema.ts (recém-modificado — productImageDataUrl agora optional)
  </read_first>
  <action>
    Co-migrar o consumidor `productImageDataUrl` na rota no MESMO plano (o `npm run typecheck` de 41-02 roda sobre o projeto inteiro e falharia sem isso — contrato cross-plan; padrão já aplicado ao route.test.ts no 41-06):
    1. **Confirmar o guard de presença** (:118-123) permanece ANTES do schema parse — quando chega ao `inputValidation.validate`, `productImageDataUrl` JÁ foi garantido (não-null) na Wave 1 do fluxo legado (caso 3 do D2: ambos ausentes → 400).
    2. **`:301-305`** — a chamada `inputValidation.validate(parsed.data.productName, parsed.data.productImageDataUrl, undefined)` agora recebe `string | undefined` (campo tornou-se `optional()` no schema). Como o guard de presença (e a regra de exclusividade D2 no 41-06) garantem a imagem antes do stream, usar **não-null assertion**: `parsed.data.productImageDataUrl!` (ou equivalente narrowing) — preservando o comportamento: o fluxo legado valida a primary normalmente.
    3. **NÃO** implementar a regra de exclusividade completa/`productImages` aqui — isso é o 41-06 (Wave 3). Apenas o narrowing de tipo mínimo para `typecheck` passar.
    4. NÃO alterar mais nada na rota.
  </action>
  <verify>
    <automated>
      $n = Select-String -LiteralPath "src\app\api\campaign\generate-image\route.ts" -Pattern "parsed.data.productImageDataUrl!" | Measure-Object; if ($n.Count -lt 1) { Write-Error "route.ts:303 sem narrowing não-null" } else { Write-Output "OK: productImageDataUrl! no validate" }; npm run typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - `route.ts` linha ~303: `parsed.data.productImageDataUrl!` (ou narrowing equivalente) na chamada `inputValidation.validate`
    - Guard de presença :118-123 INALTERADO (mantém o 400 legado de "Imagem do produto é obrigatória")
    - `npm run typecheck` limpo (sem TS2345 de `string | undefined` → `string`)
  </acceptance_criteria>
  <done>route.ts co-migrado (productImageDataUrl! no validate) — typecheck de 41-02 limpo</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente → API | Payload JSON não confiável atravessa o schema de transporte (productImages[]/productImageDataUrl) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-02 | Tampering | GenerateImageRequestSchema | mitigate | `z.enum` restringe role/source; `superRefine` garante exactly-1-primary; `.min(1).max(MAX_CAMPAIGN_IMAGES)` limita o array; `.strict()` rejeita campos desconhecidos — todos no Zod do transporte (D2/D10) |
| T-41-02a | Tampering | productImageDataUrl opcional | mitigate | Preservação comportamental — a obrigatoriedade NÃO desaparece: passa a ser regra de exclusividade da rota (400, plano 41-06); Zod não é mais a fonte única (D2) |
| T-41-02b | DoS | productImages[] payload | mitigate | `.max(MAX_CAMPAIGN_IMAGES = 4)` no schema limita o número de itens; teto agregado de bytes será validado na rota (plano 41-06, D10) |
| T-41-02c | Information Disclosure | dataUrl base64 | accept | Base64 do produto é dado do próprio usuário no transporte; nunca persiste no snapshot (D6/D7) |
| T-41-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo — sem gate de legitimidade |
</threat_model>

<verification>
- Grep `MAX_CAMPAIGN_IMAGES` em config.ts → 1 definição; em schema.ts → 1 uso (`max(MAX_CAMPAIGN_IMAGES)`)
- Grep `productImageDataUrl: z.string().min(1)` (required antigo) em schema.ts → 0 ocorrências (optional com min(1) é aceito)
- `npx vitest run src/lib/campaign/__tests__/ src/__tests__/f31-2-intent-tests.test.ts src/__tests__/api/campaign-intent-guard.test.ts` — sem regressão
- `npm run typecheck` limpo
</verification>

<success_criteria>
- `MAX_CAMPAIGN_IMAGES = 4` + teto agregado 8MB em config.ts (fonte única do teto)
- `ProductImageInputSchema` exportado no transporte (sem id)
- `productImages[]` aditivo com superRefine exactly-1-primary; `productImageDataUrl` optional; `.strict()` preservado
- Nenhuma regressão na suíte existente
</success_criteria>

<output>
Create `.planning/phases/41-midia-de-campanha-mobile/41-02-SUMMARY.md` when done
</output>

---
phase: 41-midia-de-campanha-mobile
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - prompts/campaign-image-director.md
  - prompts/campaign-image-director-offer.md
  - prompts/campaign-image-director-spotlight.md
  - prompts/campaign-image-director-exclusive.md
autonomous: true
requirements: [F41-23, F41-24]

must_haves:
  truths:
    - "Os 4 prompts do diretor contêm o bloco descritivo 1+N referências (hardcoded, sem placeholder/variável nova)"
    - "A linha de proteção factual ('A imagem do produto é uma referência factual protegida.') permanece inalterada nos 4 prompts"
    - "Nenhuma variável nova foi adicionada — o golden EXPECTED_KEYS = 38 por intent permanece válido"
  artifacts:
    - path: "prompts/campaign-image-director.md"
      provides: "Bloco descritivo 1+N"
      contains: "imagem principal (herói visual"
    - path: "prompts/campaign-image-director-offer.md"
      provides: "Bloco descritivo 1+N"
      contains: "imagem principal (herói visual"
    - path: "prompts/campaign-image-director-spotlight.md"
      provides: "Bloco descritivo 1+N"
      contains: "imagem principal (herói visual"
    - path: "prompts/campaign-image-director-exclusive.md"
      provides: "Bloco descritivo 1+N"
      contains: "imagem principal (herói visual"
  key_links:
    - from: "prompts/*.md (4 arquivos)"
      to: "EXPECTED_KEYS = 38 (image-generation-service.test.ts:516-527)"
      via: "Nenhuma variável nova no bloco descritivo"
      pattern: "productName"
---

<objective>
Substituir a **linha única de referência de imagem** pelo **bloco descritivo 1+N referências** nos 4 prompts do diretor (D6): a linha "A imagem do produto foi enviada como referência visual fiel..." (`campaign-image-director.md:49`, `-offer.md:49`, `-spotlight.md:47`, `-exclusive.md:46` — numeração `6.` no exclusive) vira um bloco que descreve **1 imagem principal (herói visual) + N imagens auxiliares (contexto)**, **hardcoded, sem placeholder/variável nova**.

Purpose: D6 — as imagens entram como **input multimodal**, não como variável textual; o conjunto de variáveis do prompt (golden `EXPECTED_KEYS = 38` por intent) **permanece idêntico**; o texto do prompt muda intencionalmente.
Output: 4 prompts com o bloco 1+N; golden 38 keys preservado.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-midia-de-campanha-mobile/41-CONTEXT.md (D6; F41-23, F41-24)
@.planning/phases/41-midia-de-campanha-mobile/41-PATTERNS.md (seção Prompts do diretor — linha única a substituir + bloco 1+N exato)
@openspec/changes/fase-41-midia-de-campanha-mobile/specs/ai-image-generation/spec.md (Requerimento: Prompt com bloco descritivo de 1+N referências — D6)
@openspec/changes/fase-41-midia-de-campanha-mobile/tasks.md (seção 10 — 10.1 a 10.5)
@prompts/campaign-image-director.md (linha 49)
@prompts/campaign-image-director-offer.md (linha 49)
@prompts/campaign-image-director-spotlight.md (linha 47)
@prompts/campaign-image-director-exclusive.md (linha 46)
@src/lib/campaign/__tests__/prompt-reframe.test.ts (linhas 1-57 — padrão de teste de conteúdo de .md)

<interfaces>
Linha única a substituir (idêntica nos 4, muda só a numeração):
- `prompts/campaign-image-director.md:49` — `7. **Imagem de referência do produto:** A imagem do produto foi enviada como referência visual fiel. O produto {{productName}} deve ser o herói visual da composição.`
- `prompts/campaign-image-director-offer.md:49` — idem
- `prompts/campaign-image-director-spotlight.md:47` — idem
- `prompts/campaign-image-director-exclusive.md:46` — numeração `6.` (não `7.`)

Bloco descritivo 1+N (D6, texto hardcoded — SEM placeholder/variável):
```
- **Imagens de referência do produto:** Foram enviadas 1 imagem principal (herói visual da composição) e N imagens auxiliares de referência (contexto: ângulos, variações, combos). Use a imagem principal como base fiel do produto {{productName}}. Use as imagens auxiliares apenas como contexto visual — NÃO invente conteúdo, detalhes ou ângulos que não estejam nelas.
```
- O `{{productName}}` NÃO é variável nova — já existia na linha anterior (D6: sem nova variável).
- A segunda menção ("A imagem do produto é uma referência factual protegida." — director `:111`, offer `:112`, spotlight `:110`, exclusive `:119`) **NÃO muda** (D6) — é a linha de proteção factual, não a descritiva.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Substituir a linha única pelo bloco descritivo 1+N nos 4 prompts</name>
  <files>prompts/campaign-image-director.md, prompts/campaign-image-director-offer.md, prompts/campaign-image-director-spotlight.md, prompts/campaign-image-director-exclusive.md</files>
  <read_first>
    - prompts/campaign-image-director.md (linha 49 — linha única a substituir; linha 111 — linha de proteção factual que NÃO muda)
    - prompts/campaign-image-director-offer.md (linha 49; linha 112)
    - prompts/campaign-image-director-spotlight.md (linha 47; linha 110)
    - prompts/campaign-image-director-exclusive.md (linha 46 — numeração 6.; linha 119)
    - openspec/changes/fase-41-midia-de-campanha-mobile/specs/ai-image-generation/spec.md (Requerimento: Prompt com bloco descritivo 1+N — sem nova variável)
  </read_first>
  <action>
    Nos 4 arquivos de prompt, substituir **apenas** a linha única de referência pelo bloco descritivo (D6, tasks 10.1-10.4):
    1. Substituir a linha exata `7. **Imagem de referência do produto:** A imagem do produto foi enviada como referência visual fiel. O produto {{productName}} deve ser o herói visual da composição.` (ou `6.` no exclusive) pelo bloco:
       `- **Imagens de referência do produto:** Foram enviadas 1 imagem principal (herói visual da composição) e N imagens auxiliares de referência (contexto: ângulos, variações, combos). Use a imagem principal como base fiel do produto {{productName}}. Use as imagens auxiliares apenas como contexto visual — NÃO invente conteúdo, detalhes ou ângulos que não estejam nelas.`
    2. MANTER a numeração dos itens seguintes intacta (a linha substituída vira um bullet `-`, não um item numerado — ou renumere se a lista exigir; verificar que o item seguinte mantém sua numeração original).
    3. NÃO alterar a linha de proteção factual ("A imagem do produto é uma referência factual protegida." — director `:111`, offer `:112`, spotlight `:110`, exclusive `:119`).
    4. NÃO adicionar nenhum placeholder/variável além de `{{productName}}` (já existente) — D6: sem variável nova (golden EXPECTED_KEYS = 38 preservado).
    5. Verificar que a frase antiga "A imagem do produto foi enviada como referência visual fiel" NÃO aparece em nenhum dos 4 arquivos após a edição.
  </action>
  <verify>
    <automated>
      $bad = @(); foreach ($f in @('prompts\campaign-image-director.md','prompts\campaign-image-director-offer.md','prompts\campaign-image-director-spotlight.md','prompts\campaign-image-director-exclusive.md')) { if (Select-String -LiteralPath $f -Pattern "referência visual fiel" -Quiet) { $bad += $f } }; if ($bad.Count -gt 0) { Write-Error "Linha antiga ainda presente: $($bad -join ', ')" } else { Write-Output "OK: linha antiga removida nos 4 prompts" }; foreach ($f in @('prompts\campaign-image-director.md','prompts\campaign-image-director-offer.md','prompts\campaign-image-director-spotlight.md','prompts\campaign-image-director-exclusive.md')) { if (-not (Select-String -LiteralPath $f -Pattern "1 imagem principal" -Quiet)) { Write-Error "Bloco 1+N ausente em $f" } }; Write-Output "Blocos 1+N presentes"
    </automated>
  </verify>
  <acceptance_criteria>
    - Grep `referência visual fiel` nos 4 prompts → 0 ocorrências (linha antiga removida)
    - Grep `1 imagem principal` nos 4 prompts → 1 ocorrência por arquivo (bloco 1+N presente)
    - Grep `referência factual protegida` nos 4 prompts → 1 ocorrência por arquivo (linha de proteção intacta)
    - Grep `{{productName}}` no bloco → presente (variável pré-existente, não nova)
    - Grep de placeholders novos não existentes antes (ex.: `{{productImages`, `{{auxiliar`) → 0 ocorrências (D6: sem variável nova)
  </acceptance_criteria>
  <done>4 prompts com bloco descritivo 1+N; linha antiga removida; proteção factual intacta; zero variáveis novas</done>
</task>

<task type="auto">
  <name>Task 2: Verificar golden EXPECTED_KEYS = 38 por intent (regressão D6)</name>
  <files>src/lib/image-generation/services/__tests__/image-generation-service.test.ts</files>
  <read_first>
    - src/lib/image-generation/services/__tests__/image-generation-service.test.ts (linhas 515-606 — golden tests por intent; EXPECTED_KEYS :516-527; toHaveLength(38) :549/:566/:577)
    - openspec/changes/fase-41-midia-de-campanha-mobile/tasks.md (10.5 — golden por intent inalterado)
  </read_first>
  <action>
    Verificar que o golden por intent **permanece inalterado** após a mudança de prompts (D6, task 10.5):
    1. Rodar a suíte do service: `npx vitest run src/lib/image-generation/services/__tests__/image-generation-service.test.ts` — os testes golden (`8.16 offer/spotlight/exclusive`, `9.5 golden offer com novos campos`) devem continuar verdes (mesmo conjunto de 38 keys para o mesmo input — `EXPECTED_KEYS` em :516-527, `toHaveLength(38)`).
    2. NÃO alterar nada neste arquivo — o golden testa `buildPromptVariables` (conjunto de variáveis), que NÃO muda com o texto do prompt (D6). O texto do prompt muda intencionalmente; as variáveis entram como input multimodal.
    3. Se algum teste golden falhar, NÃO editar o EXPECTED_KEYS — a mudança de texto do prompt não deve afetar o conjunto de variáveis; investigar (ex.: placeholder não resolvido adicionado por engano na Task 1).
  </action>
  <verify>
    <automated>npx vitest run src/lib/image-generation/services/__tests__/image-generation-service.test.ts --reporter=dot 2>$null | Select-String "Test Files|Tests"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/image-generation/services/__tests__/image-generation-service.test.ts` → todos os testes verdes (incluindo golden 8.16 offer/spotlight/exclusive e 9.5)
    - Nenhuma modificação em `image-generation-service.test.ts` (EXPECTED_KEYS = 38 preservado — D6)
    - Grep `{{` nos 4 prompts → apenas variáveis pré-existentes do prompt (productName, etc.) — nenhuma variável nova
  </acceptance_criteria>
  <done>Golden EXPECTED_KEYS = 38 por intent preservado; prompts com bloco 1+N sem variável nova</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prompt templates → modelo | Texto de instrução do diretor de arte (influencia a composição) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-03 | Tampering | Prompt templates | mitigate | Bloco hardcoded sem placeholder novo (D6) — o pipeline de `validatePrompts` (placeholder check) não quebra; golden EXPECTED_KEYS = 38 ancorado por teste por intent |
| T-41-03a | Information Disclosure | Instrução textual | accept | Texto público de instrução de IA — sem dado sensível |
| T-41-03b | Spoofing | Bloco descritivo | mitigate | "NÃO invente conteúdo, detalhes ou ângulos que não estejam nelas" — instrução anti-alucinação explícita no bloco (D6) |
| T-41-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo — sem gate de legitimidade |
</threat_model>

<verification>
- Grep `referência visual fiel` nos 4 prompts → 0
- Grep `1 imagem principal` nos 4 prompts → 1 por arquivo
- Grep `referência factual protegida` nos 4 prompts → 1 por arquivo (proteção intacta)
- `npx vitest run src/lib/image-generation/services/__tests__/image-generation-service.test.ts` verde (golden 38 keys)
</verification>

<success_criteria>
- 4 prompts do diretor com bloco descritivo 1+N (hardcoded, sem variável nova)
- Linha de proteção factual intacta nos 4
- Golden `EXPECTED_KEYS = 38` por intent preservado (D6)
</success_criteria>

<output>
Create `.planning/phases/41-midia-de-campanha-mobile/41-03-SUMMARY.md` when done
</output>

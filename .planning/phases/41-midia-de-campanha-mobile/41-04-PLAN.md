---
phase: 41-midia-de-campanha-mobile
plan: 04
type: execute
wave: 2
depends_on: [41-02]
files_modified:
  - src/lib/campaign/brief.ts
  - src/lib/campaign/types.ts
  - src/lib/campaign/persistence.ts
autonomous: true
requirements: [F41-12, F41-13, F41-14, F41-15, F41-16]

must_haves:
  truths:
    - "buildCampaignBriefFromFlat mapeia productImages[] item a item (roles/source do transporte, id uuid por item, mimeType real derivado do dataUrl via mimeTypeFromDataUrl) e o legado productImageDataUrl equivale a productImages de 1 elemento"
    - "CampaignProductImageInput ganha storagePath?: string (tipo runtime, preenchido pela ROTA após o upload, sem cast/objeto paralelo)"
    - "buildCampaignBriefSnapshot copia storagePath quando presente e serializa N imagens sem base64"
    - "createCampaign(storeId, input, campaignId?) aceita id pré-gerado (ausente → UUID interno); uploadCampaignInputImage e removeCampaignInputs existem com paths {storeId}/{campaignId}/inputs/{imageId}.jpg"
    - "CreateCampaignInput ganha campaignId?/storagePaths? opcionais"
  artifacts:
    - path: "src/lib/campaign/brief.ts"
      provides: "Mapper multi + snapshot com storagePath + mimeTypeFromDataUrl"
      contains: "mimeTypeFromDataUrl"
    - path: "src/lib/campaign/persistence.ts"
      provides: "createCampaign com campaignId? + uploadCampaignInputImage + removeCampaignInputs"
      contains: "uploadCampaignInputImage"
    - path: "src/lib/campaign/types.ts"
      provides: "CreateCampaignInput com campaignId?/storagePaths?"
      contains: "storagePaths?"
  key_links:
    - from: "src/lib/campaign/brief.ts (mapper)"
      to: "GenerateImageRequest.productImages (schema.ts)"
      via: "input.productImages ?? legado 1 elemento"
      pattern: "productImages"
    - from: "src/lib/campaign/persistence.ts (uploadCampaignInputImage)"
      to: "image-processor.ts transcodeToJpeg"
      via: "transcode para JPEG antes do upload"
      pattern: "transcodeToJpeg"
---

<objective>
Crescer o **domínio de mídia de 1→N** (D2/D3) e a **persistência de inputs no storage** (D5): `buildCampaignBriefFromFlat` mapeia `productImages[]` item a item com `mimeType` real derivado do dataUrl (helper `mimeTypeFromDataUrl` — corrige o quirk `"image/jpeg"` fixo da F39); legado `productImageDataUrl` = `productImages` de 1 elemento (zero bifurcação); `CampaignProductImageInput` ganha `storagePath?: string`; `buildCampaignBriefSnapshot` copia `storagePath` quando presente e serializa N imagens sem base64; `createCampaign(storeId, input, campaignId?)` aceita id pré-gerado; novos helpers `uploadCampaignInputImage` e `removeCampaignInputs`; `CreateCampaignInput` ganha `campaignId?`/`storagePaths?`.

Purpose: D2/D3 (mapper multi, roles/source/mimeType corretos, invariante exactly-1-primary no zod do domínio) + D5 (persistência dos inputs para a F37; `campaignId` pré-gerado; limpeza pré-stream sem órfãos; sem migration SQL).
Output: 3 arquivos de domínio/persistência prontos para a rota (plano 41-06).
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-midia-de-campanha-mobile/41-CONTEXT.md (D2, D3, D5; F41-12 a F41-16)
@.planning/phases/41-midia-de-campanha-mobile/41-PATTERNS.md (seções brief.ts, types.ts, persistence.ts — expected F41 content)
@openspec/changes/fase-41-midia-de-campanha-mobile/specs/campaign-brief-mapper/spec.md (mapper multi + mimeType real)
@openspec/changes/fase-41-midia-de-campanha-mobile/specs/campaign-brief-snapshot/spec.md (storagePath copiado, N imagens sem base64)
@openspec/changes/fase-41-midia-de-campanha-mobile/specs/campaign-brief-contract/spec.md (CampaignProductImageInput.storagePath?)
@openspec/changes/fase-41-midia-de-campanha-mobile/specs/campaign-persistence-service/spec.md (createCampaign campaignId? + uploadCampaignInputImage + removeCampaignInputs)
@openspec/changes/fase-41-midia-de-campanha-mobile/specs/campaign-types/spec.md (CreateCampaignInput estendido)
@openspec/changes/fase-41-midia-de-campanha-mobile/tasks.md (seções 4, 5, 6)
@src/lib/campaign/brief.ts (LINHAS VERIFICADAS)
@src/lib/campaign/persistence.ts (LINHAS VERIFICADAS)
@src/lib/campaign/types.ts (LINHAS VERIFICADAS)
@src/lib/campaign/image-processor.ts (linhas 4-30 — SUPPORTED_MIME_TYPES + transcodeToJpeg)

<interfaces>
Contrato exato (per tasks.md 4.1-4.4, 5.1-5.3, 6.1-6.4):

```typescript
// brief.ts — helper puro (task 4.3):
export function mimeTypeFromDataUrl(dataUrl: string): string {
  const match = dataUrl.match(/^data:(image\/(png|jpeg|webp));base64,/);
  return match?.[1] ?? "image/jpeg";
}

// brief.ts — CampaignProductImageInput (:29-35) ganha (task 5.3):
storagePath?: string; // F41 D5 — preenchido pela ROTA após upload, antes do snapshot

// brief.ts — mapper (:161-171) substitui o hardcode single (tasks 4.1-4.2):
const productImages = input.productImages ?? (
  input.productImageDataUrl
    ? [{ role: "primary" as const, source: "upload" as const, mimeType: mimeTypeFromDataUrl(input.productImageDataUrl), dataUrl: input.productImageDataUrl }]
    : []
);
const images: CampaignProductImageInput[] = productImages.map((img) => ({
  id: crypto.randomUUID(),       // rota/domínio gera id (D2)
  role: img.role,                // espelha o transporte (D3)
  source: img.source,            // upload | camera (D4)
  mimeType: img.mimeType || mimeTypeFromDataUrl(img.dataUrl),
  dataUrl: img.dataUrl,
  // storagePath undefined aqui — a rota preenche APÓS o upload (D5)
}));

// brief.ts — snapshot (:222-228) ganha (task 5.1):
...(i.storagePath ? { storagePath: i.storagePath } : {}),

// types.ts — CreateCampaignInput (:26-34) ganha (task 6.4):
campaignId?: string;       // id pré-gerado pela rota (path de inputs conhecido antes do snapshot)
storagePaths?: Array<{ imageId: string; storagePath: string }>;

// persistence.ts — createCampaign (:5-33) ganha (task 6.1):
export async function createCampaign(
  storeId: string,
  input: CreateCampaignInput,
  campaignId?: string
): Promise<{ id: string; storagePath: string }> {
  const campaignIdFinal = campaignId ?? crypto.randomUUID();
  const storagePath = `${storeId}/${campaignIdFinal}.jpg`;
  // INSERT usa campaignIdFinal
}

// persistence.ts — NOVOS helpers (tasks 6.2-6.3):
export async function uploadCampaignInputImage(
  storeId: string,
  campaignId: string,
  imageId: string,
  image: { buffer: Buffer; mimeType: string }
): Promise<{ storagePath: string }> {
  const storagePath = `${storeId}/${campaignId}/inputs/${imageId}.jpg`;
  const jpeg = await transcodeToJpeg(image.buffer, image.mimeType); // image-processor.ts:14-30
  // upload em "campaign-images" com contentType "image/jpeg", upsert: false (padrão uploadCampaignImage :60-84)
  return { storagePath };
}

export async function removeCampaignInputs(storeId: string, campaignId: string): Promise<void> {
  // list(prefix) -> remove(paths); no-op quando lista vazia (padrão deleteCampaignImage :143-154)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: brief.ts — mimeTypeFromDataUrl + storagePath runtime + mapper multi + snapshot copia storagePath</name>
  <files>src/lib/campaign/brief.ts</files>
  <read_first>
    - src/lib/campaign/brief.ts (linhas 21-47 tipos de imagem; :29-35 CampaignProductImageInput; :146-171 mapper; :216-235 snapshot)
    - src/lib/campaign/brief-schema.ts (linhas 60-86 — zod do domínio exactly-1-primary — NÃO muda)
    - openspec/changes/fase-41-midia-de-campanha-mobile/specs/campaign-brief-mapper/spec.md (mapper multi + legado 1 elemento)
    - openspec/changes/fase-41-midia-de-campanha-mobile/specs/campaign-brief-snapshot/spec.md (storagePath copiado)
  </read_first>
  <action>
    Modificar `src/lib/campaign/brief.ts` (tasks 4.1-4.4, 5.1, 5.3):
    1. **Helper puro** `mimeTypeFromDataUrl(dataUrl: string): string` — adicionar no bloco "Helpers" (:131-138): regex `/^data:(image\/(png|jpeg|webp));base64,/`, retorna `match?.[1] ?? "image/jpeg"` (corrige o quirk `"image/jpeg"` fixo da F39 — `brief.ts:167`).
    2. **`CampaignProductImageInput`** (:29-35) — adicionar `storagePath?: string;` com doc-comment "F41 D5 — preenchido pela ROTA após o upload do input ({storeId}/{campaignId}/inputs/{imageId}.jpg), antes de montar o snapshot".
    3. **Mapper `buildCampaignBriefFromFlat`** (:161-171) — substituir o hardcode single por: `const productImages = input.productImages ?? (input.productImageDataUrl ? [{ role: "primary" as const, source: "upload" as const, mimeType: mimeTypeFromDataUrl(input.productImageDataUrl), dataUrl: input.productImageDataUrl }] : []);` e `const images: CampaignProductImageInput[] = productImages.map((img) => ({ id: crypto.randomUUID(), role: img.role, source: img.source, mimeType: img.mimeType || mimeTypeFromDataUrl(img.dataUrl), dataUrl: img.dataUrl }));` — NÃO preencher storagePath aqui (rota preenche após upload — D5).
    4. **`buildCampaignBriefSnapshot`** (:222-228) — no `.map()` das imagens adicionar `...(i.storagePath ? { storagePath: i.storagePath } : {})` após `mimeType: i.mimeType` (copia storagePath quando presente; ausente → campo ausente no snapshot, sem fabricação). Manter `provided: true as const`.
    5. NÃO alterar `CampaignBriefSnapshotImage` (:39-47) — shape já reserva `storagePath?` (task 5.2: shape inalterado). NÃO adicionar validação de exactly-1-primary no mapper (D3 — zod do domínio garante).
  </action>
  <verify>
    <automated>
      $b = Select-String -LiteralPath "src\lib\campaign\brief.ts" -Pattern "mimeTypeFromDataUrl|storagePath\?: string|\.\.\.\(i\.storagePath" | Measure-Object; if ($b.Count -lt 3) { Write-Error "brief.ts F41 incompleto" } else { Write-Output "OK: brief.ts com mapper multi + storagePath" }; npx vitest run src/lib/campaign/__tests__/brief-mapper.test.ts src/lib/campaign/__tests__/brief-snapshot.test.ts 2>$null; npm run typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - Grep `export function mimeTypeFromDataUrl` em brief.ts → 1 ocorrência
    - Grep `storagePath?: string` em brief.ts → 1 ocorrência (CampaignProductImageInput)
    - Grep `input.productImages ??` em brief.ts → 1 ocorrência (legado = 1 elemento, zero bifurcação)
    - Grep `...(i.storagePath ? { storagePath: i.storagePath } : {})` em brief.ts → 1 ocorrência (snapshot)
    - Grep `mimeType: "image/jpeg"` hardcoded no mapper (linha 167 antiga) → 0 ocorrências
    - `npx vitest run src/lib/campaign/__tests__/brief-mapper.test.ts src/lib/campaign/__tests__/brief-snapshot.test.ts` verde (regressão do caso single: `data:image/jpeg;base64,abc123` → `mimeTypeFromDataUrl` = `image/jpeg`)
    - `npm run typecheck` limpo
  </acceptance_criteria>
  <done>brief.ts multi: mapper item-a-item com mimeType real, storagePath no runtime + snapshot, legado 1 elemento</done>
</task>

<task type="auto">
  <name>Task 2: persistence.ts — createCampaign com campaignId? + uploadCampaignInputImage + removeCampaignInputs + types.ts</name>
  <files>src/lib/campaign/persistence.ts, src/lib/campaign/types.ts</files>
  <read_first>
    - src/lib/campaign/persistence.ts (linhas 5-33 createCampaign; :60-84 uploadCampaignImage — padrão de upload; :143-154 deleteCampaignImage — padrão de remove; :35-58 dataUrlToCampaignImage)
    - src/lib/campaign/image-processor.ts (linhas 4-30 — SUPPORTED_MIME_TYPES + transcodeToJpeg)
    - src/lib/campaign/types.ts (linhas 26-34 — CreateCampaignInput; precedente operationRunId? da F38.1)
    - openspec/changes/fase-41-midia-de-campanha-mobile/specs/campaign-persistence-service/spec.md (contracts dos 3 helpers)
    - openspec/changes/fase-41-midia-de-campanha-mobile/specs/campaign-types/spec.md (CreateCampaignInput estendido)
  </read_first>
  <action>
    Modificar 2 arquivos (tasks 6.1-6.4):
    1. **types.ts — `CreateCampaignInput`** (:26-34): adicionar `campaignId?: string;` (id pré-gerado pela rota — path de inputs `{storeId}/{campaignId}/inputs/...` conhecido antes do snapshot) e `storagePaths?: Array<{ imageId: string; storagePath: string }>;` (registro auxiliar; snapshot `campaign_brief_v1` é o canônico). Estilo: campos opcionais aditivos, como `operationRunId?` da F38.1.
    2. **persistence.ts — `createCampaign`** (:5-33): adicionar terceiro parâmetro opcional `campaignId?: string`. Internamente: `const campaignIdFinal = campaignId ?? crypto.randomUUID();` e usar `campaignIdFinal` no `storagePath` e no INSERT (ausente → UUID interno, regressão). NÃO mudar o INSERT (id/store_id/status/product_name/input_snapshot/identity_snapshot/storage_path/operation_run_id).
    3. **persistence.ts — `uploadCampaignInputImage(storeId, campaignId, imageId, { buffer, mimeType })`** (task 6.2): path `\`${storeId}/${campaignId}/inputs/${imageId}.jpg\``; transcoda via `transcodeToJpeg(buffer, mimeType)` (`image-processor.ts:14-30`); upload em `supabaseAdmin.storage.from("campaign-images")` com `contentType: "image/jpeg"`, `upsert: false` (padrão `uploadCampaignImage` :60-84 — service_role); throw no `error`; retorna `{ storagePath }`.
    4. **persistence.ts — `removeCampaignInputs(storeId, campaignId)`** (task 6.3): `supabaseAdmin.storage.from("campaign-images").list(\`${storeId}/${campaignId}/inputs/\`, { limit: 100 })` → `.remove(paths)`; **no-op** quando lista vazia (sem throw); retorna `void` (estilo `deleteCampaignImage`). Falha pós-stream continua com `deleteCampaignImage` (D5).
    5. NÃO usar `"image/png"`/`"image/webp"` no upload — sempre JPEG via transcode (D5). NÃO alterar `dataUrlToCampaignImage`/`uploadCampaignImage`/`updateCampaignReady`/`updateCampaignError`/`getCampaign`/`deleteCampaignImage`.
  </action>
  <verify>
    <automated>
      $p = Select-String -LiteralPath "src\lib\campaign\persistence.ts" -Pattern "uploadCampaignInputImage|removeCampaignInputs|campaignId\?: string" | Measure-Object; $t = Select-String -LiteralPath "src\lib\campaign\types.ts" -Pattern "campaignId\?: string|storagePaths\?:" | Measure-Object; if ($p.Count -lt 3 -or $t.Count -lt 2) { Write-Error "persistence/types F41 incompletos" } else { Write-Output "OK: persistence + types F41" }; npx vitest run src/app/api/campaign/generate-image/__tests__/route.test.ts 2>$null; npm run typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - Grep `uploadCampaignInputImage` em persistence.ts → 1 definição (`export async function`) ; contém `inputs/${imageId}.jpg`, `transcodeToJpeg`, `contentType: "image/jpeg"`, `upsert: false`
    - Grep `removeCampaignInputs` em persistence.ts → 1 definição; contém `.list(` e `.remove(`
    - Grep `campaignId?: string` em types.ts → 1 ocorrência; `storagePaths?:` → 1 ocorrência
    - `createCampaign` tem 3º parâmetro `campaignId?: string` e usa `campaignId ?? crypto.randomUUID()`
    - `npx vitest run src/app/api/campaign/generate-image/__tests__/route.test.ts` sem regressão (mocks de persistence: o teste mocka `createCampaign`/`uploadCampaignImage` via `vi.mock` — a assinatura nova não quebra os mocks existentes pois os helpers novos não são chamados ainda)
    - `npm run typecheck` limpo
  </acceptance_criteria>
  <done>createCampaign com campaignId opcional; uploadCampaignInputImage/removeCampaignInputs; CreateCampaignInput estendido</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transporte flat → domínio | payload não confiável (productImages[]) atravessa o mapper na fronteira da rota |
| domínio → snapshot → storage | storagePath dos inputs é gravado no snapshot persistido |
| rota → storage | Uploads de inputs via service_role no bucket campaign-images |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-04 | Tampering | Mapper buildCampaignBriefFromFlat | mitigate | Roles/source restringidos pelo zod do transporte (ProductImageInputSchema); invariante exactly-1-primary garantido pelo zod do domínio (brief-schema.ts) — mapper apenas espelha (D3) |
| T-41-04a | Information Disclosure | dataUrl no snapshot | mitigate | Tipo runtime (`CampaignProductImageInput`) ≠ tipo snapshot (`CampaignBriefSnapshotImage` sem dataUrl por construção); snapshot N imagens SEM base64 (teste 6, D5/D7) |
| T-41-04b | Tampering | Snapshot storagePath | mitigate | `...(i.storagePath ? { storagePath: i.storagePath } : {})` — copia apenas campo tipado preenchido pela rota; sem cast/objeto paralelo (D5) |
| T-41-04c | Tampering | Upload de inputs | mitigate | `uploadCampaignInputImage` usa service_role (supabaseAdmin), bucket privado `campaign-images`, `upsert: false` (imutável), transcode JPEG — path com prefixo `storeId` preserva owner select; `imageId` gerado pela rota (nunca do cliente — D2/D5) |
| T-41-04d | DoS | Inputs órfãos em falha | mitigate | `removeCampaignInputs` compensa falha pré-stream (list+remove por prefixo, no-op sem objetos) — sem órfãos (D5) |
| T-41-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo (sharp já existe) — sem gate de legitimidade |
</threat_model>

<verification>
- `npx vitest run src/lib/campaign/__tests__/brief-mapper.test.ts src/lib/campaign/__tests__/brief-snapshot.test.ts` verde
- `npx vitest run src/app/api/campaign/generate-image/__tests__/route.test.ts` sem regressão
- `npm run typecheck` limpo
- Grep `mimeType: "image/jpeg"` hardcoded no mapper → 0
- Grep `dataUrl` no tipo `CampaignBriefSnapshotImage` → 0 (sem base64 por tipo)
</verification>

<success_criteria>
- Mapper flat→domínio multi (roles/source do transporte, mimeType real, id uuid por item); legado = 1 elemento
- `storagePath?` no runtime (preenchido pela rota) e copiado no snapshot quando presente
- `createCampaign` com `campaignId?`; `uploadCampaignInputImage`/`removeCampaignInputs` operacionais
- `CreateCampaignInput` com `campaignId?`/`storagePaths?`
- Nenhuma regressão nas suítes de domínio/persistência
</success_criteria>

<output>
Create `.planning/phases/41-midia-de-campanha-mobile/41-04-SUMMARY.md` when done
</output>

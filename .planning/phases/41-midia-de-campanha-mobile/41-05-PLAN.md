---
phase: 41-midia-de-campanha-mobile
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/image-generation/providers/types.ts
  - src/lib/image-generation/providers/openai.ts
  - src/lib/image-generation/services/image-generation-service.ts
  - src/lib/image-generation/services/image-review-service.ts
  - src/lib/image-generation/services/__tests__/image-review-service.test.ts
  - src/lib/image-generation/services/__tests__/image-generation-service.test.ts
autonomous: true
requirements: [F41-18, F41-19, F41-20, F41-21, F41-22]

must_haves:
  truths:
    - "ImageProviderInput ganha productImagesDataUrls?: string[] (lista ordenada, posição 0 = primary); productImageDataUrl? mantido para o legado"
    - "openai.ts monta N blocos input_image no mainline Responses (primary + auxiliares); identidade/logo continua detail: low; fallback images.edit SÓ com primary única (1 imagem), enviando apenas productFile"
    - "image-generation-service.ts expõe mediaImagesDataUrls(brief) (lista ordenada, posição 0 = primary) e monta ImageProviderInput com productImagesDataUrls (ou productImageDataUrl legado para 1 imagem)"
    - "InputValidationService.validate continua primary-only (comportamento inalterado — D8)"
    - "ImageReviewService.review recebe opcionalmente a dataUrl da primary e envia ao prompt campaign-image-reviewer (linha fixa 'Compare o produto da arte com a imagem de referência'); retrocompatível"
  artifacts:
    - path: "src/lib/image-generation/providers/types.ts"
      provides: "ImageProviderInput.productImagesDataUrls?"
      contains: "productImagesDataUrls"
    - path: "src/lib/image-generation/providers/openai.ts"
      provides: "N input_image + fallback gated"
      contains: "productImagesDataUrls"
    - path: "src/lib/image-generation/services/image-generation-service.ts"
      provides: "Ponte mediaImagesDataUrls + provider input com lista N"
      contains: "mediaImagesDataUrls"
    - path: "src/lib/image-generation/services/image-review-service.ts"
      provides: "Review com primary como referência"
      contains: "imagem de referência"
  key_links:
    - from: "src/lib/image-generation/services/image-generation-service.ts (generateWithRetry)"
      to: "src/lib/image-generation/providers/types.ts"
      via: "ImageProviderInput montado com productImagesDataUrls"
      pattern: "productImagesDataUrls"
    - from: "src/lib/image-generation/services/image-generation-service.ts (review)"
      to: "src/lib/image-generation/services/image-review-service.ts"
      via: "review(imageDataUrl, reviewInput, primaryImageDataUrl)"
      pattern: "primaryImageDataUrl"
---

<objective>
Adaptar o **pipeline de geração de imagem para N inputs multimodais** (D7/D8/D9): `ImageProviderInput.productImagesDataUrls?: string[]` (lista ordenada, posição 0 = primary); `openai.ts` monta **N blocos `input_image`** no mainline Responses path e o **fallback `images.edit` passa a ser gated** — SÓ com primary única (1 imagem), enviando apenas `productFile` (identidade fora do fallback); a ponte `primaryImageDataUrl(brief)` evolui para `mediaImagesDataUrls(brief)` no service; `InputValidationService` permanece **primary-only** (D8 — comportamento inalterado); `ImageReviewService.review` recebe **opcionalmente** a dataUrl da primary como referência de fidelidade (D9 — retrocompatível).

Purpose: D7 (N `input_image` no Responses; política fechada de fallback — não degrada a fidelidade descartando imagens) + D8 (validação primary-only) + D9 (revisor compara a arte com a imagem de referência). Telemetria `AiCostTracker`/`recordCall` permanece 1 evento `campaign_image` (imagens entram como tokens do mesmo input).
Output: 4 arquivos de provider/service adaptados, prontos para a rota (41-06).
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-midia-de-campanha-mobile/41-CONTEXT.md (D7, D8, D9; F41-18 a F41-22)
@.planning/phases/41-midia-de-campanha-mobile/41-PATTERNS.md (seções providers/types.ts, openai.ts, image-generation-service.ts, image-review-service.ts)
@openspec/changes/fase-41-midia-de-campanha-mobile/specs/ai-image-generation/spec.md (N input_image, fallback gated)
@openspec/changes/fase-41-midia-de-campanha-mobile/specs/generation-retry-fallback/spec.md (edit só com primary única)
@openspec/changes/fase-41-midia-de-campanha-mobile/specs/campaign-brief-pipeline-adapters/spec.md (mediaImagesDataUrls + reviewInput com primary)
@openspec/changes/fase-41-midia-de-campanha-mobile/specs/validation-review-alignment/spec.md (validação primary-only + review com primary)
@openspec/changes/fase-41-midia-de-campanha-mobile/tasks.md (seções 8 e 9)
@src/lib/image-generation/providers/types.ts (LINHAS VERIFICADAS)
@src/lib/image-generation/providers/openai.ts (LINHAS VERIFICADAS)
@src/lib/image-generation/services/image-generation-service.ts (LINHAS VERIFICADAS — 1207 linhas, editar por seção)
@src/lib/image-generation/services/image-review-service.ts (LINHAS VERIFICADAS)
@src/lib/image-generation/services/input-validation-service.ts (linhas 40-71 — NÃO muda, primary-only já é o comportamento)

<interfaces>
Contrato exato (per tasks.md 8.1-8.3 + 9.1-9.4):

```typescript
// providers/types.ts (:6-14) — adicionar:
/** F41 D7: lista ordenada de dataUrls das imagens do produto; posição 0 = primary. */
productImagesDataUrls?: string[];
// productImageDataUrl? MANTIDO para o legado (1 imagem)

// openai.ts mainline (:63-77) — substituir o push único (:71-73):
const productImages = input.productImagesDataUrls ?? (input.productImageDataUrl ? [input.productImageDataUrl] : []);
for (const url of productImages) {
  content.push({ type: "input_image" as const, image_url: url, detail: "auto" as const });
}
// identityImageUrl continua detail: "low" (inalterado, :75-77)

// openai.ts — helper compartilhado (D7, DOIS pontos de gatilho):
private isSinglePrimary(input: ImageProviderInput): boolean {
  return input.productImagesDataUrls ? input.productImagesDataUrls.length === 1 : Boolean(input.productImageDataUrl);
}
// gate 1 — pre-response (:58-61): SÓ primary única (1 imagem)
if (attempt >= 1 && this.isSinglePrimary(input)) {
  return this.fallbackToImageApi(openai, input, size);
}
// gate 2 — POST-ERROR (:177-184): CRÍTICO — sem gate, erro "Responses indisponível"
// (isResponsesApiError: not supported / model_not_found / tool not found) com auxiliares
// cairia no fallback images.edit e DESCARTARIA as auxiliares silenciosamente (viola D7).
if (this.isSinglePrimary(input) && this.isResponsesApiError(err)) {
  return this.fallbackToImageApi(openai, input, size);
}
// fallbackToImageApi (:225-307) — AJUSTE MÍNIMO (D7 coerente p/ novo single-image):
// o fallback hoje usa apenas `input.productImageDataUrl` (:232) — um caller que envie só
// `productImagesDataUrls: [primary]` passaria no gate isSinglePrimary mas lançaria
// "Invalid productImageDataUrl" no fallback. Resolver dentro do fallback:
//   const productImageDataUrl = input.productImageDataUrl ?? input.productImagesDataUrls?.[0];
// e usar ESSE valor no dataUrlMatch (:232) e no restante do método (continua uma única
// base image — a limitação images.edit :282-287 é preservada; legado e novo single-image
// ambos caem aqui).

// image-generation-service.ts — ponte (:983-985):
private mediaImagesDataUrls(brief: CampaignBrief): string[] {
  return brief.media.images
    .map((img) => ({ img, order: img.role === "primary" ? 0 : 1 }))
    .sort((a, b) => a.order - b.order)
    .map(({ img }) => img.dataUrl)
    .filter((url): url is string => Boolean(url));
}
// primaryImageDataUrl pode ser mantido como alias `mediaImagesDataUrls(brief)[0]`
// (4 call sites: :177 validação D8, :352/:435/:483) — mínimo diff

// generateWithRetry (:1041-1048) — ImageProviderInput ganha a lista:
productImagesDataUrls: mediaImagesDataUrls(brief),   // lista ordenada (posição 0 = primary)
productImageDataUrl,                                  // legado — mantido (D7)

// image-review-service.ts — review (:54-63) assinatura:
async review(
  generatedImageDataUrl: string,
  input: ImageReviewInput,
  primaryImageDataUrl?: string,
  onCall?: (info: AiCallInfo) => void | Promise<void>
): Promise<ImageReviewResult>
// Quando primaryImageDataUrl presente: prompt ganha linha fixa
// "Compare o produto da arte com a imagem de referência" e callVisionModel
// content ganha 2º image_url (detail "high") — ver callVisionModel (:243-268)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: providers/types.ts + openai.ts — N input_image + fallback gated</name>
  <files>src/lib/image-generation/providers/types.ts, src/lib/image-generation/providers/openai.ts</files>
  <read_first>
    - src/lib/image-generation/providers/types.ts (linhas 6-14 — ImageProviderInput)
    - src/lib/image-generation/providers/openai.ts (linhas 55-77 — mainline content array; :58-61 fallback gate; :225-307 fallbackToImageApi — recebe ajuste mínimo na primary; :232 usa productImageDataUrl)
    - openspec/changes/fase-41-midia-de-campanha-mobile/specs/generation-retry-fallback/spec.md (edit gated por primary única)
  </read_first>
  <action>
    Modificar 2 arquivos (tasks 8.1-8.3):
    1. **providers/types.ts** (:6-14): adicionar `productImagesDataUrls?: string[];` no `ImageProviderInput` com doc-comment "F41 D7: lista ordenada de dataUrls das imagens do produto; posição 0 = primary.". MANTER `productImageDataUrl?: string` (legado — D7). NÃO alterar `ImageProviderOutput` (:35-42) nem `ImageProvider` (:54-62).
    2. **openai.ts mainline** (:63-77): substituir o push único de `input.productImageDataUrl` (:71-73) por: `const productImages = input.productImagesDataUrls ?? (input.productImageDataUrl ? [input.productImageDataUrl] : []);` seguido de `for (const url of productImages) { content.push({ type: "input_image" as const, image_url: url, detail: "auto" as const }); }`. MANTER o bloco `identityImageUrl` com `detail: "low" as const` (:75-77).
     3. **openai.ts fallback gate 1 (pre-response, :58-61)**: substituir `if (attempt >= 1 && input.productImageDataUrl)` por `if (attempt >= 1 && this.isSinglePrimary(input))` — SÓ com primary única (1 imagem). Com auxiliares (2+) → retries permanecem no Responses; Responses indisponível → erro explícito (D7).
     3b. **openai.ts fallback gate 2 (POST-ERROR, :177-184) — CRÍTICO (D7, dois pontos de gatilho)**: o `openai.ts` tem DOIS pontos de fallback. O **primeiro** é o pre-response `:58-61`; o **segundo** é o pós-erro `:177-184`: `if (input.productImageDataUrl && this.isResponsesApiError(err)) { return this.fallbackToImageApi(openai, input, size); }`. Sem gate, com `productImagesDataUrls` (2+) E `productImageDataUrl` ambos preenchidos (o service mantém ambos — Task 2 step 3), um erro "Responses indisponível" (`isResponsesApiError`: not supported / model_not_found / tool not found) cai no fallback `images.edit` e **descarta as auxiliares silenciosamente** — exatamente o que D7 proíbe. Aplicar a MESMA condição primária-única: extrair um helper privado `private isSinglePrimary(input: ImageProviderInput): boolean { return input.productImagesDataUrls ? input.productImagesDataUrls.length === 1 : Boolean(input.productImageDataUrl); }` e usá-lo em AMBOS os gates: gate 1 → `if (attempt >= 1 && this.isSinglePrimary(input))`; gate 2 → `if (this.isSinglePrimary(input) && this.isResponsesApiError(err))`.
     4. **AJUSTE MÍNIMO do `fallbackToImageApi` (:225-307) — D7 coerente para o novo single-image:** o fallback usa apenas `input.productImageDataUrl` (:232, :288-294). Como `isSinglePrimary` permite o fallback quando `productImagesDataUrls.length === 1`, um caller que envie SÓ `productImagesDataUrls: [primary]` (sem `productImageDataUrl`) passaria no gate mas lançaria "Invalid productImageDataUrl" no fallback (o `dataUrlMatch` em :232 receberia `undefined`). Corrigir dentro do fallback — **resolver o dataUrl da primary**: no início do método (antes do `dataUrlMatch`), `const productImageDataUrl = input.productImageDataUrl ?? input.productImagesDataUrls?.[0];` e usar `productImageDataUrl` no `dataUrlMatch` (:232) e no restante. NÃO tocar na lógica de `toFile`/`images.edit` (:257-294) — continua 1 base image (limitação :282-287 preservada). O gate (isSinglePrimary) impede o fallback com auxiliares; o fallback passa a suportar legado E novo single-image.
     5. NÃO alterar telemetria/usage (`ImageProviderUsageMeta`).
  </action>
  <verify>
    <automated>
      $t = Select-String -LiteralPath "src\lib\image-generation\providers\types.ts" -Pattern "productImagesDataUrls\?: string\[\]" | Measure-Object; $o = Select-String -LiteralPath "src\lib\image-generation\providers\openai.ts" -Pattern "input.productImagesDataUrls|for \(const url of productImages\)|isSinglePrimary\(input\) && this\.isResponsesApiError|productImagesDataUrls\?\.\[0\]" | Measure-Object; if ($t.Count -lt 1 -or $o.Count -lt 4) { Write-Error "Provider F41 incompleto (falta gate :179 ou resolução do fallback?)" } else { Write-Output "OK: provider com N input_image + 2 gates + fallback resolve primary" }; npx vitest run src/lib/image-generation/providers/__tests__/openai-provider.test.ts 2>$null; npm run typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - Grep `productImagesDataUrls?: string[]` em providers/types.ts → 1 ocorrência
    - Grep `productImageDataUrl?: string` em providers/types.ts → 1 ocorrência (mantido para legado)
    - Grep `for (const url of productImages)` em openai.ts → 1 ocorrência (N input_image)
    - Grep `private isSinglePrimary` em openai.ts → 1 ocorrência (helper compartilhado)
    - Grep `isSinglePrimary(input) && this.isResponsesApiError` em openai.ts → 1 ocorrência (**gate pós-erro :177-184 coberto** — D7)
    - Grep `productImagesDataUrls?.[0]` em openai.ts → 1 ocorrência (**fallbackToImageApi resolve a primary quando `productImageDataUrl` ausente** — novo single-image cai no fallback sem lançar "Invalid productImageDataUrl")
    - Grep `identityImageUrl` em openai.ts → bloco mantido com `detail: "low" as const`
    - `npx vitest run src/lib/image-generation/providers/__tests__/openai-provider.test.ts` verde — teste de fallback existente (:33-48, `attempt >= 1` + `productImageDataUrl` sem lista) continua passando (gate 1: `isSinglePrimary` → true; fallback resolve via `productImageDataUrl`)
    - `npm run typecheck` limpo
  </acceptance_criteria>
  <done>ImageProviderInput com productImagesDataUrls?; openai.ts com N input_image + fallback gated por primary única + fallback resolve a primary (legado e novo single-image)</done>
</task>

<task type="auto">
  <name>Task 2: image-generation-service.ts — ponte mediaImagesDataUrls + provider input com lista N</name>
  <files>src/lib/image-generation/services/image-generation-service.ts</files>
  <read_first>
    - src/lib/image-generation/services/image-generation-service.ts (linhas 983-985 — primaryImageDataUrl; :1041-1048 — generateWithRetry monta ImageProviderInput; :173-177 validação D8 primary-only; :352/:435/:483 call sites)
    - openspec/changes/fase-41-midia-de-campanha-mobile/specs/campaign-brief-pipeline-adapters/spec.md (mediaImagesDataUrls + provider input)
  </read_first>
  <action>
    Modificar `image-generation-service.ts` (tasks 9.1-9.2) — editar por seção (arquivo de 1207 linhas; NÃO reler inteiro):
    1. **Ponte** (:983-985): adicionar `private mediaImagesDataUrls(brief: CampaignBrief): string[] { return brief.media.images.map((img) => ({ img, order: img.role === "primary" ? 0 : 1 })).sort((a, b) => a.order - b.order).map(({ img }) => img.dataUrl).filter((url): url is string => Boolean(url)); }` — lista ordenada, posição 0 = primary (D7).
    2. MANTER `primaryImageDataUrl` como alias mínimo: `return this.mediaImagesDataUrls(brief)[0];` — os 4 call sites (:177 validação D8, :352 generateWithRetry, :435/:483 hadProductImage) continuam usando o alias — MÍNIMO DIFF (D8: validação primary-only inalterada; D7: hadProductImage continua boolean).
    3. **generateWithRetry** (:1041-1048): adicionar `productImagesDataUrls: this.mediaImagesDataUrls(brief),` ao objeto de `ImageProviderInput` passado a `this.imageProvider.generateImage({...})`, MANTENDO `productImageDataUrl` (:1043) e `identityImageUrl` (:1044) — o provider decide pelo gate (D7).
    4. NÃO alterar `inputValidation.validate(...)` (:173-177) — primary-only (D8), recebe `this.primaryImageDataUrl(brief) ?? ""`.
    5. NÃO alterar o reviewInput (:385-406) aqui — a passagem da primary ao review acontece na Task 3 (via `primaryImageDataUrl(brief)`).
  </action>
  <verify>
    <automated>
      $s = Select-String -LiteralPath "src\lib\image-generation\services\image-generation-service.ts" -Pattern "mediaImagesDataUrls|productImagesDataUrls: this\.mediaImagesDataUrls" | Measure-Object; if ($s.Count -lt 2) { Write-Error "Service ponte F41 incompleto" } else { Write-Output "OK: service com mediaImagesDataUrls" }; npx vitest run src/lib/image-generation/services/__tests__/image-generation-service.test.ts 2>$null; npm run typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - Grep `private mediaImagesDataUrls` em image-generation-service.ts → 1 ocorrência (com `.sort((a, b) => a.order - b.order)`)
    - Grep `productImagesDataUrls: this.mediaImagesDataUrls(brief)` em image-generation-service.ts → 1 ocorrência (no generateWithRetry)
    - Grep `productImageDataUrl,` no objeto ImageProviderInput → mantido (legado — D7)
    - Grep `primaryImageDataUrl` → alias `mediaImagesDataUrls(brief)[0]` presente; call sites de validação (:173-177) inalterados (primary-only D8)
    - `npx vitest run src/lib/image-generation/services/__tests__/image-generation-service.test.ts` verde (assert `objectContaining({ productImageDataUrl })` :658 continua válido — ambos os campos presentes)
    - `npm run typecheck` limpo
  </acceptance_criteria>
  <done>Ponte mediaImagesDataUrls(brief) ordenada; provider input com lista N; validação primary-only intacta</done>
</task>

<task type="auto">
  <name>Task 3: image-review-service.ts + service — review recebe a primary como referência (D9)</name>
  <files>src/lib/image-generation/services/image-review-service.ts, src/lib/image-generation/services/image-generation-service.ts</files>
  <read_first>
    - src/lib/image-generation/services/image-review-service.ts (linhas 9-25 ImageReviewInput; :54-63 review; :243-268 callVisionModel — content array com 1 image_url)
    - src/lib/image-generation/services/image-generation-service.ts (linhas 414-416 — `this.imageReview.review(imageDataUrl, reviewInput, (info) => {...})` — call site a atualizar)
    - openspec/changes/fase-41-midia-de-campanha-mobile/specs/validation-review-alignment/spec.md (review com primary)
  </read_first>
  <action>
    Modificar 2 arquivos (task 9.4):
    1. **image-review-service.ts — assinatura `review`** (:54-63): adicionar parâmetro opcional `primaryImageDataUrl?: string` ANTES de `onCall`: `review(generatedImageDataUrl: string, input: ImageReviewInput, primaryImageDataUrl?: string, onCall?: (info: AiCallInfo) => void | Promise<void>)`. Retrocompatível: sem 3º arg → comportamento atual.
    2. **image-review-service.ts — prompt**: quando `primaryImageDataUrl` presente, o prompt carregado (`this.promptLoader.load("campaign-image-reviewer", contextVars)`) deve receber a linha fixa "Compare o produto da arte com a imagem de referência" — implementar concatenando a linha ao prompt OU via variável existente (escolher o caminho de menor mudança; a imagem entra como input multimodal — D9 sem variável nova de prompt; se adicionar texto ao prompt, NÃO criar placeholder novo). Manter o prompt sem a linha quando ausente (retrocompatível).
    3. **image-review-service.ts — callVisionModel** (:243-268): o content array deve aceitar **2ª imagem** quando `primaryImageDataUrl` presente: `content: [{ type: "text", text: prompt }, { type: "image_url", image_url: { url: generatedImageDataUrl, detail: "high" } }, ...(primaryImageDataUrl ? [{ type: "image_url", image_url: { url: primaryImageDataUrl, detail: "high" } }] : [])]` — adaptar a assinatura interna para receber a primary (ou montar o content no `review` e passar ao callVisionModel).
    4. **image-generation-service.ts** (:414-416): atualizar o call site para `this.imageReview.review(imageDataUrl, reviewInput, this.primaryImageDataUrl(brief), (info) => { reviewUsage = info.usage; })` — passa a primary como referência de fidelidade (D9); sem primary (legado) → undefined → comportamento atual.
    5. NÃO alterar `ImageReviewInput` (:9-25) — a primary é parâmetro separado (opcional), não campo do input (min diff).
    6. **CO-MIGRAÇÃO IMEDIATA (mesmo plano — regra-mãe):** a inserção posicional de `primaryImageDataUrl` ANTES de `onCall` quebra os consumidores existentes na MESMA wave — co-migrar os 2 arquivos de teste incluídos em `files_modified`:
       - **`image-review-service.test.ts` — Teste 4** (:334-348): `service.review('data:image/jpeg;base64,abc', { productName: 'Produto', storeName: 'Loja' }, onCall)` → `service.review('data:image/jpeg;base64,abc', { productName: 'Produto', storeName: 'Loja' }, undefined, onCall)` — o `onCall` passa a ser o **4º arg** (primary ausente → undefined). MANTER os asserts de `onCall` (:342-347) intactos. Os demais call sites do arquivo (sem onCall) permanecem como estão (retrocompatível — `review(generated, input)`).
       - **`image-generation-service.test.ts` — Teste 8 mock** (:389-402): o mock de `this.imageReview.review` lê `const onCall = args[2];` (:392) — trocar para `const onCall = args[3];` (a primary dataUrl agora é `args[2]`; o callback é `args[3]`). Se o mock valida `typeof args[2] === 'function'` para decidir, ajustar para `typeof args[3] === 'function'`. O Teste de review no fluxo real (:374-379) já usa `args[3]` para o `review` do serviço — conferir consistência.
    7. NÃO deixar co-migração de quebra para o 41-11 (Wave 5) — o 41-11 só ADICIONA cenários novos (N input_image, fallback gated), não conserta a aridade antiga.
  </action>
  <verify>
    <automated>
      $r = Select-String -LiteralPath "src\lib\image-generation\services\image-review-service.ts" -Pattern "primaryImageDataUrl\?: string|Compare o produto da arte" | Measure-Object; $g = Select-String -LiteralPath "src\lib\image-generation\services\image-generation-service.ts" -Pattern "this\.imageReview\.review\(imageDataUrl, reviewInput, this\.primaryImageDataUrl\(brief\)" | Measure-Object; $t4 = Select-String -LiteralPath "src\lib\image-generation\services\__tests__\image-review-service.test.ts" -Pattern "undefined, onCall" | Measure-Object; $t8 = Select-String -LiteralPath "src\lib\image-generation\services\__tests__\image-generation-service.test.ts" -Pattern "args\[3\]" | Measure-Object; if ($r.Count -lt 2 -or $g.Count -lt 1 -or $t4.Count -lt 1 -or $t8.Count -lt 1) { Write-Error "Review com primary + co-migração incompletos" } else { Write-Output "OK: review com primary + testes co-migrados" }; npx vitest run src/lib/image-generation/services/__tests__/image-review-service.test.ts src/lib/image-generation/services/__tests__/image-generation-service.test.ts 2>$null; npm run typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - Grep `primaryImageDataUrl?: string` em image-review-service.ts → 1 ocorrência (3º parâmetro posicional)
    - Grep `Compare o produto da arte com a imagem de referência` em image-review-service.ts → 1 ocorrência (linha fixa D9)
    - callVisionModel content aceita 2ª `image_url` condicional (primary) com `detail: "high"` — grep do spread condicional ou equivalente
    - Grep `this.imageReview.review(imageDataUrl, reviewInput, this.primaryImageDataUrl(brief)` em image-generation-service.ts → 1 ocorrência (call site com primary)
    - **Co-migração imediata (regra-mãe):** grep `undefined, onCall` em image-review-service.test.ts → 1 (Teste 4 — onCall agora 4º arg); grep `args[3]` no mock de review de image-generation-service.test.ts → ≥ 1 (callback lido de args[3]; o arquivo já tinha `args[3]` no Teste do fluxo real :377 — total vira ≥ 2); `image-review-service.test.ts` e `image-generation-service.test.ts` estão em `files_modified` do 41-05
    - `npx vitest run src/lib/image-generation/services/__tests__/image-review-service.test.ts src/lib/image-generation/services/__tests__/image-generation-service.test.ts` → verdes (Teste 4 com `undefined, onCall`; Teste 8 mock com `args[3]`; demais testes retrocompatíveis passam)
    - `npm run typecheck` limpo
  </acceptance_criteria>
  <done>Review recebe primary opcionalmente; prompt com linha fixa; retrocompatível; call site + 2 testes co-migrados no mesmo plano</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service → provider | Input multimodal (dataUrls de N imagens) atravessa para o modelo de IA |
| service → review | Arte gerada + primary (referência) atravessam para o revisor de visão |
| provider → fallback | Fallback images.edit recebe apenas a primary única |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-05 | Tampering | Fallback images.edit | mitigate | Gate de negócio: `attempt >= 1` só com primary única (1 imagem); com auxiliares → retries no Responses ou erro explícito (D7) — o fallback não degrada a fidelidade descartando imagens |
| T-41-05a | Information Disclosure | dataUrls ao modelo | accept | Base64 do produto do próprio usuário enviado ao provider contratado (comportamento atual, 1 imagem) |
| T-41-05b | Tampering | N input_image | mitigate | Lista `productImagesDataUrls` derivada da ponte `mediaImagesDataUrls(brief)` (domínio tipado, ordem garantida posição 0 = primary); `.filter(Boolean)` remove dataUrls ausentes |
| T-41-05c | Spoofing | Review com primary | mitigate | Primary passada como referência de fidelidade (não como instrução); linha fixa "Compare o produto da arte com a imagem de referência" (D9); sem primary → comportamento atual (retrocompatível) |
| T-41-05d | Information Disclosure | Telemetria | accept | `AiCostTracker`/`recordCall` permanece 1 evento `campaign_image` — imagens entram como tokens do mesmo input (D7); sem custo duplicado |
| T-41-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo (SDK openai já existe) — sem gate de legitimidade |
</threat_model>

<verification>
- `npx vitest run src/lib/image-generation/providers/__tests__/openai-provider.test.ts` verde
- `npx vitest run src/lib/image-generation/services/__tests__/image-generation-service.test.ts` verde (inclui golden 38 keys)
- `npx vitest run src/lib/image-generation/services/__tests__/image-review-service.test.ts` verde
- `npm run typecheck` limpo
- Grep `fallbackToImageApi` → 2 call sites gated por `isSinglePrimary` (:58-61 pre-response + :177-184 post-error)
- `fallbackToImageApi` resolve a primary (`input.productImageDataUrl ?? input.productImagesDataUrls?.[0]`) — legado e novo single-image caem no fallback sem "Invalid productImageDataUrl"
- Co-migração imediata verificada: `image-review-service.test.ts` Teste 4 com `undefined, onCall` (4º arg); `image-generation-service.test.ts` mock de review lê `args[3]`
</verification>

<success_criteria>
- `ImageProviderInput.productImagesDataUrls?` + N `input_image` no Responses (identidade detail low)
- Fallback `images.edit` gated por primary única nos DOIS pontos de gatilho (`:58-61` pre-response + `:177-184` post-error — identidade fora do fallback; com auxiliares → erro explícito, D7)
- Ponte `mediaImagesDataUrls(brief)` (posição 0 = primary) + provider input com a lista
- Validação primary-only inalterada (D8)
- Review com primary opcional como referência de fidelidade (retrocompatível — D9)
- `fallbackToImageApi` resolve a primary (`input.productImageDataUrl ?? input.productImagesDataUrls?.[0]`) — legado e novo single-image (`productImagesDataUrls: [primary]` sem `productImageDataUrl`) caem no fallback corretamente; suítes verdes + typecheck limpo
</success_criteria>

<output>
Create `.planning/phases/41-midia-de-campanha-mobile/41-05-SUMMARY.md` when done
</output>

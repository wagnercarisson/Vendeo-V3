---
phase: 41-midia-de-campanha-mobile
plan: 06
type: execute
wave: 3
depends_on: [41-02, 41-04, 41-05]
files_modified:
  - src/app/api/campaign/generate-image/route.ts
  - src/app/api/campaign/generate-image/__tests__/route.test.ts
  - src/__tests__/concurrency.test.ts
  - src/__tests__/regression-master-switch.test.ts
  - src/__tests__/api/campaign-generate.test.ts
autonomous: true
requirements: [F41-17]

must_haves:
  truths:
    - "A rota aplica a regra de exclusividade D2 (ambos ausentes → 400 'Imagem do produto é obrigatória'; ambos presentes → 400 payload ambíguo)"
    - "A rota valida limites D10 por item (4MB) + teto agregado (413 PT-BR indicando item/total); limite legado single permanece para productImageDataUrl"
    - "A rota pré-gera campaignId + id por imagem (uuid, rota gera), faz upload dos inputs ANTES do snapshot via uploadCampaignInputImage, monta snapshot com storagePath, chama createCampaign(storeId, input, campaignIdPreGerado) e limpa em falha pré-stream via removeCampaignInputs"
    - "recordCall/telemetria permanece 1 evento campaign_image (D7)"
  artifacts:
    - path: "src/app/api/campaign/generate-image/route.ts"
      provides: "Orquestração pré-stream D2/D5/D10"
      contains: "removeCampaignInputs"
  key_links:
    - from: "route.ts (guards pré-stream)"
      to: "persistence.ts (uploadCampaignInputImage/removeCampaignInputs/createCampaign)"
      via: "Ordem D5: pré-gera campaignId → upload inputs → snapshot com storagePath → createCampaign"
      pattern: "uploadCampaignInputImage"
    - from: "route.ts"
      to: "schema.ts (GenerateImageRequestSchema)"
      via: "productImages[] validado no zod + regra de exclusividade na rota"
      pattern: "productImages"
---

<objective>
Adaptar a **rota `POST /api/campaign/generate-image`** (D2/D5/D10): regra de exclusividade de imagem no lugar da presença single (`productImages` XOR `productImageDataUrl`; ambos ausentes → 400; ambos presentes → 400 payload ambíguo), limites por item + teto agregado (413 PT-BR), e a **ordem D5** — pré-gerar `campaignId`, gerar `id` por imagem, **upload dos inputs ANTES do snapshot** via `uploadCampaignInputImage`, montar snapshot com `storagePath` por imagem, `createCampaign(storeId, input, campaignIdPreGerado)`, e limpeza pré-stream via `removeCampaignInputs` (sem órfãos). Validação primária-only (D8) e `recordCall`/telemetria inalterada (D7).

Purpose: D2 (exclusividade determinística — os campos são mutuamente exclusivos) + D10 (payload do Vercel/Next e custo do modelo contidos) + D5 (inputs persistidos para a F37; snapshot com storagePath; sem migration SQL).
Output: route.ts orquestrando o fluxo multi-imagem completo (pré-stream).
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-midia-de-campanha-mobile/41-CONTEXT.md (D2, D5, D10; F41-17; Specifics — tabela de exclusividade + ordem D5)
@.planning/phases/41-midia-de-campanha-mobile/41-PATTERNS.md (seção route.ts — expected F41 content, pitfalls)
@openspec/changes/fase-41-midia-de-campanha-mobile/specs/transactional-pipeline/spec.md (pré-stream D2/D5/D10)
@openspec/changes/fase-41-midia-de-campanha-mobile/specs/ai-image-generation/spec.md (endpoint — 400/413 no pré-stream)
@openspec/changes/fase-41-midia-de-campanha-mobile/tasks.md (seção 7 — 7.1 a 7.6)
@src/app/api/campaign/generate-image/route.ts (LINHAS VERIFICADAS — 835 linhas, editar por seção)
@src/lib/campaign/persistence.ts (uploadCampaignInputImage/removeCampaignInputs/createCampaign — recém-criados no 41-04)
@src/lib/campaign/brief.ts (buildCampaignBriefSnapshot — recém-modificado no 41-04)

<interfaces>
Ordem exata a implementar no pré-stream (tasks 7.1-7.6):

```typescript
// ── Substitui o guard de presença single (:117-135) ──────────────────
const hasProductImages = Array.isArray(body.productImages) && body.productImages.length > 0;
const hasLegacyDataUrl = typeof body.productImageDataUrl === "string" && body.productImageDataUrl.length > 0;
if (!hasProductImages && !hasLegacyDataUrl) {
  return Response.json({ error: { message: "Imagem do produto é obrigatória" } }, { status: 400 }); // caso 3
}
if (hasProductImages && hasLegacyDataUrl) {
  return Response.json({ error: { message: "Payload ambíguo: envie productImages[] OU productImageDataUrl, não ambos." } }, { status: 400 }); // caso 4
}

// ── Limites D10: por item (4MB) + teto agregado → 413 ────────────────
// productImages: para cada item dataUrl.length <= MAX_PRODUCT_IMAGE_BASE64_SIZE (413 por item)
//                soma dos dataUrls <= MAX_PRODUCT_IMAGES_AGGREGATE_BASE64_SIZE (413 total)
// legado single: guard atual (:126-135) permanece para productImageDataUrl

// ── Ordem D5 (substitui/estende :354-377) ────────────────────────────
const campaignIdPre = crypto.randomUUID();                       // (1) pré-gera
// (2) id por imagem — mesma FONTE do loop (brief.media.images, domínio — sempre ≥1
//     via mapper, inclusive no legado que vira 1 elemento): nunca productImages
//     do transporte (undefined no fluxo legado → crash undefined.map)
const imageIds = brief.media.images.map(() => crypto.randomUUID());
// (3) upload ANTES do snapshot — iterar sobre brief.media.images (objetos do domínio
//     que o snapshot consome), NÃO sobre o array de transporte body.productImages:
for (const [idx, img] of brief.media.images.entries()) {
  const { buffer } = dataUrlToCampaignImage(img.dataUrl!);       // persistence.ts:35-58
  const mimeType = mimeTypeFromDataUrl(img.dataUrl!) ?? img.mimeType; // mimeType REAL (HEIC→JPEG)
  await uploadCampaignInputImage(storeId, campaignIdPre, imageIds[idx], { buffer, mimeType });
  img.storagePath = `${storeId}/${campaignIdPre}/inputs/${imageIds[idx]}.jpg`;  // preenche DOMÍNIO (D5) — buildCampaignBriefSnapshot lê brief.media.images[i].storagePath
}
// (4) snapshot COM storagePath:
const inputSnapshot = buildCampaignBriefSnapshot(brief) as unknown as Record<string, unknown>;
// (5) createCampaign com id pré-gerado:
const campaign = await createCampaign(storeId, { productName, inputSnapshot, identitySnapshot, operationRunId }, campaignIdPre);

// ── Limpeza pré-stream (task 7.4): em falha de qualquer passo acima ──
try { await removeCampaignInputs(storeId, campaignIdPre); } catch { /* ignore */ }
// falha pós-stream → deleteCampaignImage(storagePath) atual (:397) permanece
```
- Pitfall 1: o `brief` (montado em :243) precisa dos `storagePath` preenchidos ANTES de `buildCampaignBriefSnapshot` — inserir os uploads entre o mapper (:243) e o snapshot (:359). **O loop itera sobre `brief.media.images` (domínio), NUNCA sobre `body.productImages` (transporte)** — escrever `storagePath` no array de transporte é perdido porque o snapshot deriva do brief (key_links).
- Pitfall 2: validação primária-only (D8) — `inputValidation.validate(parsed.data.productName, <dataUrl da primary>, ...)` (:301-305); para o fluxo novo, passar a **imagem com `role: "primary"`** via `parsed.data.productImages!.find((img) => img.role === "primary")!.dataUrl` — NUNCA posição 0 (o mapper preserva a ordem do transporte; um cliente pode enviar `[reference, primary]`).
- Pitfall 3: `recordCall`/telemetria (`AiCostTracker`) permanece 1 evento `campaign_image` (task 7.6) — uploads de inputs NÃO geram eventos de custo.
- Pitfall 4: **mimeType REAL no transcode** — passar `mimeTypeFromDataUrl(img.dataUrl!) ?? img.mimeType` para `uploadCampaignInputImage`/`transcodeToJpeg` (o transporte pode carregar `file.type` cru `image/heic`; `transcodeToJpeg` só aceita png/jpeg/webp — enviar HEIC cru → 500, quebra a câmera D4).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rota — regra de exclusividade D2 + limites D10 (400/413)</name>
  <files>src/app/api/campaign/generate-image/route.ts</files>
  <read_first>
    - src/app/api/campaign/generate-image/route.ts (linhas 100-155 — guards pré-stream: :117-123 presença single, :126-135 limite single, :138-155 schema parse)
    - openspec/changes/fase-41-midia-de-campanha-mobile/specs/transactional-pipeline/spec.md (pré-stream D2/D10)
  </read_first>
  <action>
    Modificar os guards pré-stream de imagem (tasks 7.1-7.2) — substituir o bloco :117-135:
    1. **Regra de exclusividade** (D2): `const hasProductImages = Array.isArray(body.productImages) && body.productImages.length > 0;` e `const hasLegacyDataUrl = typeof body.productImageDataUrl === "string" && body.productImageDataUrl.length > 0;` → (a) ambos ausentes → `Response.json({ error: { message: "Imagem do produto é obrigatória" } }, { status: 400 })`; (b) ambos presentes → `Response.json({ error: { message: "Payload ambíguo: envie productImages[] OU productImageDataUrl, não ambos." } }, { status: 400 })`.
    2. **Limites D10** — para o fluxo `productImages`: para cada item, `img.dataUrl.length > MAX_PRODUCT_IMAGE_BASE64_SIZE` → 413 com mensagem PT-BR indicando o item e o limite (`Imagem {i+1} do produto excede o limite de X MB...`); `soma dos dataUrls > MAX_PRODUCT_IMAGES_AGGREGATE_BASE64_SIZE` → 413 PT-BR indicando o teto total. Para o legado `productImageDataUrl`, MANTER o guard atual (:126-135) — limite single 4MB.
    3. MANTER o schema parse (:138-155) como está — o zod valida `productImages[]` (min/max/superRefine exactly-1-primary via `ProductImageInputSchema`) e o `.strict()`.
    4. NÃO alterar os guards de auth/clearance/readiness/preço/identidade subsequentes.
  </action>
  <verify>
    <automated>
      $r = Select-String -LiteralPath "src\app\api\campaign\generate-image\route.ts" -Pattern "Payload ambíguo|MAX_PRODUCT_IMAGES_AGGREGATE_BASE64_SIZE|Imagem do produto é obrigatória" | Measure-Object; if ($r.Count -lt 3) { Write-Error "Exclusividade/limites F41 incompletos" } else { Write-Output "OK: exclusividade + limites D10" }; npm run typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - Grep `Payload ambíguo` em route.ts → 1 ocorrência (caso 4 da tabela D2)
    - Grep `MAX_PRODUCT_IMAGES_AGGREGATE_BASE64_SIZE` em route.ts → 1 ocorrência (teto agregado → 413)
    - Grep `MAX_PRODUCT_IMAGE_BASE64_SIZE` em route.ts → 3 ocorrências (import :3 + limite legado single + limite por item do productImages[])
    - Grep `hasProductImages`/`hasLegacyDataUrl` em route.ts → 2 definições
    - Mensagens de 413 indicam item e limite em PT-BR
    - `npm run typecheck` limpo
  </acceptance_criteria>
  <done>Rota com regra de exclusividade (400) + limites por item e agregado (413 PT-BR)</done>
</task>

<task type="auto">
  <name>Task 2: Rota — ordem D5: campaignId pré-gerado + upload inputs pré-snapshot + createCampaign com id + cleanup</name>
  <files>src/app/api/campaign/generate-image/route.ts</files>
  <read_first>
    - src/app/api/campaign/generate-image/route.ts (linhas 240-243 — buildCampaignBriefFromFlat; :354-377 createCampaign atual; :379-399 reserveCredit + cleanup; :689-691 upload pós-stream)
    - src/lib/campaign/persistence.ts (dataUrlToCampaignImage :35-58; uploadCampaignInputImage/removeCampaignInputs/createCampaign — recém-criados no 41-04)
    - openspec/changes/fase-41-midia-de-campanha-mobile/specs/transactional-pipeline/spec.md (ordem D5)
  </read_first>
  <action>
    Implementar a ordem D5 no pré-stream (tasks 7.3-7.6), substituindo o bloco :354-377:
    1. **Pré-gerar `campaignId`**: `const campaignIdPre = crypto.randomUUID();` — **D5 aplica-se aos DOIS fluxos (decisão do usuário 2026-08-14, CONTEXT Specifics):** o legado também persiste a primary como input — o mapper gera 1 item primary/upload no legado, e o mesmo loop D5 persiste esse item (sem bifurcação; regressão preservada em comportamento, snapshot ganha `storagePath` aditivo). Sem hesitação: o legado NÃO tem caminho "sem id pré-gerado".
    2. **Gerar `id` por imagem**: `const imageIds = brief.media.images.map(() => crypto.randomUUID());` — MESMA fonte do loop (domínio, sempre ≥1 via mapper — inclusive no legado que vira 1 elemento); NUNCA `productImages` do transporte (undefined no fluxo legado → crash). Cliente NUNCA envia id (D2/D5).
    3. **Upload ANTES do snapshot**: iterar sobre **`brief.media.images`** (objetos do DOMÍNIO que o snapshot consome — NUNCA sobre `body.productImages`/transporte): `for (const [idx, img] of brief.media.images.entries()) { const { buffer } = dataUrlToCampaignImage(img.dataUrl!); const mimeType = mimeTypeFromDataUrl(img.dataUrl!) ?? img.mimeType; await uploadCampaignInputImage(storeId, campaignIdPre, imageIds[idx], { buffer, mimeType }); img.storagePath = \`${storeId}/${campaignIdPre}/inputs/${imageIds[idx]}.jpg\`; }` — preenche `storagePath` nos objetos do domínio (D5, sem cast/objeto paralelo; `buildCampaignBriefSnapshot` lê `brief.media.images[i].storagePath`). **mimeType REAL derivado do dataUrl** (`mimeTypeFromDataUrl` primeiro) — o transporte pode carregar `file.type` cru (`image/heic`); `transcodeToJpeg` só aceita png/jpeg/webp → enviar HEIC cru = 500, quebra a câmera D4.
    4. **Snapshot pós-upload**: `const inputSnapshot = buildCampaignBriefSnapshot(brief) as unknown as Record<string, unknown>;` — o `brief` já tem os storagePaths (pitfall 1: uploads entre o mapper :243 e o snapshot).
    5. **createCampaign com id**: `const campaign = await createCampaign(storeId, { productName: campaignInput.productName, inputSnapshot, identitySnapshot: validatedSnapshot as unknown as Record<string, unknown>, operationRunId }, campaignIdPre);` — retorno `campaign.id` = `campaignIdPre`; `storagePath` = `{storeId}/{campaignIdPre}.jpg` (arte final inalterada — D5).
    6. **Limpeza pré-stream**: envolver os passos 1-5 em try/catch → em falha, `try { await removeCampaignInputs(storeId, campaignIdPre); } catch { /* ignore */ }` + rethrow/retorno 500 existente. MANTER o fluxo de compensação pós-stream (`deleteCampaignImage(storagePath)` em :397) inalterado.
     7. **Validação primary-only (D8)**: no guard de input validation (:296-352), para o fluxo `productImages` passar o dataUrl da **imagem com `role: "primary"`** (NUNCA assumir `[0]` — o contrato aceita `role` explicitamente e o zod passa com `[reference, primary]`; assumir posição 0 validaria a imagem errada): resolver `const primaryDataUrl = hasProductImages ? parsed.data.productImages!.find((img) => img.role === "primary")!.dataUrl : parsed.data.productImageDataUrl!;` e usar na chamada `inputValidation.validate(...)` (:301-305). O `find` sempre acha — o `superRefine` do transporte garante exatamente 1 `primary`.
     8. **Telemetria inalterada (D7)**: NÃO adicionar `recordCall`/`AiCostTracker` para os uploads de inputs — a chamada de geração permanece 1 evento `campaign_image`.
     9. **Co-migração do mock de persistence (MESMO plano)**: como a rota passa a importar `uploadCampaignInputImage`/`removeCampaignInputs`, adicionar os dois como `vi.fn()` ao `vi.mock('@/lib/campaign/persistence')` em `route.test.ts` (:29-36) — D5 aplica-se aos DOIS fluxos, então TODOS os testes de sucesso da rota chamam os helpers; sem o mock atualizado no mesmo plano, a suíte quebra (41-12 só amplia cenários novos em Wave 4, não conserta o mock base).
     10. **CO-MIGRAÇÃO IMEDIATA dos asserts existentes de `createCampaign` (regra-mãe — MESMO plano):** D5 vale para legado e multi → TODA chamada de sucesso usa `createCampaign(storeId, input, campaignIdPre)` (3 args). Os 4 asserts existentes com 2 args em `route.test.ts` — `:779`, `:800` (fluxo legado/credits), `:1165`, `:1177` (operation_run_id) — quebram com `toHaveBeenCalledWith` (exatidão de aridade do Vitest). Co-migrar no MESMO plano 41-06: acrescentar o 3º argumento `expect.any(String)` (ou o valor concreto quando o teste conhece) em cada `toHaveBeenCalledWith(STORE_ID, expect.objectContaining({...}))` → `toHaveBeenCalledWith(STORE_ID, expect.objectContaining({...}), expect.any(String))`. NÃO deixar para o 41-12 (que só ADICIONA cenários novos).
     11. **CO-MIGRAÇÃO IMEDIATA dos 3 testes de integração que importam o POST real (regra-mãe — MESMO plano):** os arquivos `src/__tests__/concurrency.test.ts` (:25-32), `src/__tests__/regression-master-switch.test.ts` (:25-32) e `src/__tests__/api/campaign-generate.test.ts` (:76-83) mockam `@/lib/campaign/persistence` SEM os 2 helpers novos. Como D5 roda nos DOIS fluxos, TODO request de sucesso com payload legado cai no loop `uploadCampaignInputImage` → mock resolve `undefined` → `TypeError: uploadCampaignInputImage is not a function` → 500 em vez de 200/`[200,402]`/NDJSON. Co-migrar nos 3 arquivos, MESMO plano:
        - adicionar `uploadCampaignInputImage: vi.fn(),` e `removeCampaignInputs: vi.fn(),` à factory do `vi.mock("@/lib/campaign/persistence", ...)` (usar `vi.hoisted` como os mocks existentes: `mockUploadCampaignInputImage`/`mockRemoveCampaignInputs`);
        - no `beforeEach` (padrão de `uploadCampaignImage`): `mockUploadCampaignInputImage.mockResolvedValue({ storagePath: \`${STORE_ID}/${CAMPAIGN_ID}/inputs/x.jpg\` })` e `mockRemoveCampaignInputs.mockResolvedValue(undefined)`;
        - ajustar os asserts de `createCampaign` nesses arquivos se verificarem chamada com 2 args (conferir; se existirem, acrescentar o 3º);
        - **config mock (fidelity do cap):** nos 4 arquivos que mockam `@/lib/image-generation/config` (route.test.ts :43-47, concurrency :40-44, regression-master-switch :40-44, campaign-generate ~:156), adicionar `MAX_CAMPAIGN_IMAGES: 4,` à factory — sem isso o schema `z.array().max(undefined)` desativa o cap de 4 imagens nas suítes de rota (falso positivo; o cap fica sem cobertura comportamental).
  </action>
  <verify>
    <automated>
      $r = Select-String -LiteralPath "src\app\api\campaign\generate-image\route.ts" -Pattern "campaignIdPre|uploadCampaignInputImage|removeCampaignInputs|storagePath = " | Measure-Object; if ($r.Count -lt 4) { Write-Error "Ordem D5 incompleta" } else { Write-Output "OK: ordem D5 implementada" }; npx vitest run src/app/api/campaign/generate-image/__tests__/route.test.ts 2>$null; npm run typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - Grep `crypto.randomUUID()` na rota (pré-stream, antes do snapshot) → pelo menos 1 (campaignIdPre) + loop de imageIds
    - Grep `uploadCampaignInputImage` em route.ts → 1 chamada (dentro do loop, antes do snapshot)
    - Grep `removeCampaignInputs` em route.ts → 1 chamada (limpeza pré-stream)
    - Grep `img.storagePath =` em route.ts → 1 ocorrência (preenche DOMÍNIO `brief.media.images[i]` — D5; NÃO o array de transporte)
    - `route.test.ts` mock de `@/lib/campaign/persistence` contém `uploadCampaignInputImage` e `removeCampaignInputs` como `vi.fn()` (co-migrado no MESMO plano 41-06)
    - **Co-migração imediata (regra-mãe):** os 4 asserts existentes de `createCampaign` (2 args — `:779`, `:800`, `:1165`, `:1177`) passam a incluir o 3º argumento `expect.any(String)`; `npx vitest run src/app/api/campaign/generate-image/__tests__/route.test.ts` verde no MESMO plano
    - **Co-migração imediata dos 3 testes de integração:** grep `uploadCampaignInputImage` em `src/__tests__/concurrency.test.ts`, `src/__tests__/regression-master-switch.test.ts` e `src/__tests__/api/campaign-generate.test.ts` → cada um tem o helper na factory do `vi.mock('@/lib/campaign/persistence')` (com `mockResolvedValue({ storagePath: ... })` no beforeEach); `npx vitest run src/__tests__/concurrency.test.ts src/__tests__/regression-master-switch.test.ts src/__tests__/api/campaign-generate.test.ts` verde no MESMO plano
    - `createCampaign(storeId, {...}, campaignIdPre)` → 3º argumento presente
    - Grep `buildCampaignBriefSnapshot(brief)` → após os uploads (depois do loop de upload)
    - Validação primary-only: `parsed.data.productImages!.find((img) => img.role === "primary")!.dataUrl` (ou equivalente que NÃO assuma posição 0) usado na chamada do inputValidation quando hasProductImages — o zod garante exatamente 1 `primary`, mas a ordem do array é irrelevante
    - `npx vitest run src/app/api/campaign/generate-image/__tests__/route.test.ts` — testes de rota existentes que usam o mock de persistence continuam verdes (os helpers novos são mockados no MESMO plano — passo 9; o 41-12 só ADICIONA cenários novos, não conserta o mock base)
    - `npm run typecheck` limpo
  </acceptance_criteria>
  <done>Rota com ordem D5 completa: campaignId pré-gerado, upload pré-snapshot, snapshot com storagePath, createCampaign com id, cleanup pré-stream, validação primary-only, telemetria inalterada</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente → API | Payload JSON (productImages[]/productImageDataUrl) entra no pré-stream |
| rota → storage | Uploads de inputs via service_role (bucket campaign-images) |
| rota → IA | Geração paga disparada após validação de imagem |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-06 | Spoofing | Regra de exclusividade | mitigate | Tabela canônica D2 na rota (ambos ausentes → 400; ambos presentes → 400 ambíguo) — sem "substituição" implícita; `.strict()` + zod do transporte validam o array (D2) |
| T-41-06a | DoS | Payload de imagens | mitigate | Limites por item (4MB) + teto agregado (8MB) → 413 PT-BR antes de qualquer IA/stream (D10); `MAX_CAMPAIGN_IMAGES = 4` no zod limita o array |
| T-41-06b | Tampering | IDs de imagem | mitigate | Ids gerados pela ROTA (`crypto.randomUUID()`), nunca do cliente — cliente não envia `id` (D2/D5); path de storage `{imageId}` sob controle do servidor |
| T-41-06c | Tampering | Uploads pré-stream | mitigate | `uploadCampaignInputImage` via supabaseAdmin (service_role), bucket privado, `upsert: false`; path com prefixo `storeId` preserva owner select (D5) |
| T-41-06d | DoS | Inputs órfãos | mitigate | `removeCampaignInputs` em falha pré-stream (sem órfãos); falha pós-stream → `deleteCampaignImage` atual (D5) |
| T-41-06e | Tampering | Snapshot storagePath | mitigate | storagePath preenchido no runtime tipado (`CampaignProductImageInput`) após upload verificado — antes do snapshot (D5, sem cast) |
| T-41-06f | Spoofing | Validação primária-only | mitigate | `inputValidation.validate` recebe APENAS a primary — resolvida por `find(role === "primary")`, nunca posição 0 (o mapper preserva a ordem do transporte); auxiliares não participam; fluxo de 409 inalterado (D8) |
| T-41-06g | Tampering | Telemetria | accept | Uploads não geram eventos de custo; `recordCall` permanece 1 evento `campaign_image` (D7) |
| T-41-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo — sem gate de legitimidade |
</threat_model>

<verification>
- Grep da tabela D2 (payload ambíguo + imagem obrigatória) na rota → presente
- Grep dos limites D10 (por item + agregado → 413) → presente
- Grep da ordem D5 (campaignIdPre, uploadCampaignInputImage, removeCampaignInputs, createCampaign 3º arg) → presente
- `npx vitest run src/app/api/campaign/generate-image/__tests__/route.test.ts` verde (mock de persistence + 4 asserts de createCampaign co-migrados no MESMO plano)
- `npx vitest run src/__tests__/concurrency.test.ts src/__tests__/regression-master-switch.test.ts src/__tests__/api/campaign-generate.test.ts` verde (mocks dos 2 helpers novos co-migrados no MESMO plano)
- `npm run typecheck` limpo
</verification>

<success_criteria>
- Regra de exclusividade D2 validada na rota (400 nos 2 casos de borda)
- Limites por item + teto agregado D10 (413 PT-BR)
- Ordem D5 completa (campaignId pré-gerado, upload pré-snapshot, snapshot com storagePath, createCampaign com id, cleanup pré-stream)
- Validação primary-only (D8) e telemetria 1 evento (D7) preservadas
- Suíte de rota verde + typecheck limpo
</success_criteria>

<output>
Create `.planning/phases/41-midia-de-campanha-mobile/41-06-SUMMARY.md` when done
</output>

---
phase: 41-midia-de-campanha-mobile
plan: 07
type: execute
wave: 3
depends_on: [41-02]
files_modified:
  - src/components/flow/use-campaign-form.ts
  - src/components/flow/campaign-input-form.tsx
  - src/components/flow/campaign-image-upload.tsx
  - src/components/flow/__tests__/use-campaign-form-navigation.test.ts
  - src/components/flow/__tests__/use-campaign-form-notice.test.ts
  - src/components/flow/__tests__/use-campaign-form-validity.test.ts
autonomous: true
requirements: [F41-02, F41-03, F41-05, F41-06]

must_haves:
  truths:
    - "CampaignFormFields.imageFile: File | null vira productImages: Array<{ id (interno UI), role, source, mimeType, file?, dataUrl? }> (primeiro = primary, demais = reference)"
    - "compressImage processa por item com decode HEIC via canvas e orientação EXIF respeitada (createImageBitmap from-image); falha → mensagem PT-BR clara"
    - "validateImage aceita image/heic e image/heif"
    - "Body: com auxiliares → productImages[] (SEM id de cliente); sem auxiliares → productImageDataUrl legado; nunca ambos (D2)"
    - "Draft/autosave restaura N imagens (estado multi preservado); migração de draft legado imageFile → productImages de 1 elemento"
    - "Testes irmãos (navigation/notice/validity) co-migrados: mocks restore com productImages: []"
  artifacts:
    - path: "src/components/flow/use-campaign-form.ts"
      provides: "Estado multi + compress HEIC/EXIF + body D2"
      contains: "productImages"
    - path: "src/components/flow/__tests__/use-campaign-form-navigation.test.ts"
      provides: "Co-migração dos 5 mocks restore (imageFile → productImages)"
      contains: "productImages: []"
  key_links:
    - from: "use-campaign-form.ts (body :724-738)"
      to: "GenerateImageRequestSchema (schema.ts)"
      via: "productImages[] sem id OU productImageDataUrl legado — nunca ambos (D2)"
      pattern: "body.productImages"
    - from: "use-campaign-form.ts (compressImage)"
      to: "validateImage (HEIC aceito)"
      via: "decode via canvas → JPEG ≤1MB/1200px; EXIF via createImageBitmap from-image"
      pattern: "createImageBitmap"
---

<objective>
Trocar o estado do form de **1 imagem para N imagens** (D3/D4): `CampaignFormFields.imageFile: File | null` vira `productImages: Array<{ id, role, source, mimeType, file?, dataUrl? }>` (primeiro = primary, demais = `reference`; `id` interno da UI — NUNCA no body); `compressImage` por item com **decode HEIC via canvas + orientação EXIF** (`createImageBitmap from-image`); `validateImage` aceita `image/heic`/`image/heif`; montagem do body D2 (com auxiliares → `productImages[]`; sem → `productImageDataUrl` legado; nunca ambos); draft/autosave restaura N imagens + migração de draft legado. Co-migrar os testes irmãos que espelham `CampaignFormFields` (`imageFile: null` → `productImages: []`).

Purpose: D3 (roles primary/reference no estado) + D4 (HEIC/EXIF via canvas sem lib) + D2 (body aditivo) + D5 (id interno nunca exportado).
Output: use-campaign-form.ts multi-imagem + 3 testes irmãos co-migrados (suíte verde).
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-midia-de-campanha-mobile/41-CONTEXT.md (D2, D3, D4, D5, D10; F41-02, F41-03, F41-05, F41-06)
@.planning/phases/41-midia-de-campanha-mobile/41-PATTERNS.md (seção use-campaign-form.ts — state multi, compress HEIC, body D2, restore)
@openspec/changes/fase-41-midia-de-campanha-mobile/specs/campaign-input-ui/spec.md (form state multi + body)
@openspec/changes/fase-41-midia-de-campanha-mobile/specs/campaign-media-upload/spec.md (HEIC/EXIF/limites)
@openspec/changes/fase-41-midia-de-campanha-mobile/tasks.md (seção 11 — 11.1 a 11.5; 15.7)
@src/components/flow/use-campaign-form.ts (LINHAS VERIFICADAS — 830 linhas, editar por seção)
@src/components/flow/__tests__/use-campaign-form-navigation.test.ts (LINHAS VERIFICADAS — 5 mocks restore :61-77/:106-114/:152-160/:187-195/:209-217)
@src/components/flow/__tests__/use-campaign-form-notice.test.ts (LINHAS VERIFICADAS — 3 mocks :61-69/:156-163/:176-183)
@src/components/flow/__tests__/use-campaign-form-validity.test.ts (LINHAS VERIFICADAS — 1 mock :115-123)
@src/lib/image-generation/config.ts (MAX_CAMPAIGN_IMAGES — recém-criado no 41-02)

<interfaces>
Contrato exato (per tasks.md 11.1-11.5 + PATTERNS):

```typescript
// CampaignFormFields (:78-94) — substituir :86:
productImages: Array<{
  id: string;                  // id INTERNO da UI (uuid) — NUNCA no body (D2/D5)
  role: "primary" | "reference"; // primeiro = primary, demais = reference (D3)
  source: "upload" | "camera";   // conforme a origem real (D4)
  mimeType: string;
  file?: File;
  dataUrl?: string;
}>;
// imageFile: File | null REMOVIDO da interface

// EMPTY_FIELDS (:148-164) — :156:
productImages: [],   // vazio; validação exige primary no submit (sem novo required — D3)

// FieldErrors (:96-115) — :105 "imageFile" → "productImages"

// validateImage (:197-207) — aceitar HEIC:
const validTypes = ["image/png", "image/jpeg", "image/webp", "image/heic", "image/heif"];
// "Formato não suportado. Use PNG, JPG ou WEBP" (HEIC é aceito no input; decode via canvas)

// compressImage (:12-74) — decode HEIC + EXIF antes do drawImage:
// Para image/heic|image/heif: const bitmap = await createImageBitmap(file, { imageOrientation: "from-image" });
// ctx.drawImage(bitmap, 0, 0, width, height);  // EXIF respeitado (D4)
// Falha de decode → reject(new Error("Não foi possível processar a imagem HEIC. Use JPG ou PNG."))
// Loop de qualidade JPEG ≤1MB + downscale 1200px MANTER

// Body (:724-738) — substituir :737:
const auxiliaries = frozenFields.productImages.slice(1);
if (auxiliaries.length > 0) {
  body.productImages = frozenFields.productImages.map(({ role, source, mimeType, dataUrl }) => ({
    role, source, mimeType, dataUrl,   // SEM id de cliente (D2/D5)
  }));
} else {
  body.productImageDataUrl = frozenFields.productImages[0]?.dataUrl;  // legado (D2)
}
// nunca ambos — a regra de exclusividade da rota 400 se ambos chegarem (D2)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: use-campaign-form.ts — estado multi + compressImage HEIC/EXIF + validateImage HEIC + body D2 + draft restore</name>
  <files>src/components/flow/use-campaign-form.ts</files>
  <read_first>
    - src/components/flow/use-campaign-form.ts (linhas 78-164 types/EMPTY_FIELDS; :197-207 validateImage; :12-74 compressImage; :724-738 body; :326-334 restore; :421-456 preview effect; :802 isValid)
    - src/lib/image-generation/config.ts (MAX_CAMPAIGN_IMAGES — para o teto no cliente, plano 41-08; aqui só o state/body)
    - openspec/changes/fase-41-midia-de-campanha-mobile/specs/campaign-input-ui/spec.md (state multi + body)
  </read_first>
  <action>
    Modificar `use-campaign-form.ts` (tasks 11.1-11.5):
    1. **Interface `CampaignFormFields`** (:78-94): substituir `imageFile: File | null;` (:86) por `productImages: Array<{ id: string; role: "primary" | "reference"; source: "upload" | "camera"; mimeType: string; file?: File; dataUrl?: string }>;` — com doc-comments (id interno da UI NUNCA no body — D2/D5; primeiro = primary — D3).
    2. **`FieldErrors`** (:96-115): substituir `"imageFile"` por `"productImages"` na união de chaves.
    3. **`EMPTY_FIELDS`** (:148-164): substituir `imageFile: null,` por `productImages: [],`.
    4. **`validateImage`** (:197-207): adicionar `"image/heic"`, `"image/heif"` ao `validTypes`; mensagem de formato "Formato não suportado. Use PNG, JPG, WEBP ou HEIC"; manter limite 5MB.
    5. **`validateField`** (:222-223): `case "productImages": return validateImage(fields.productImages[0]?.file ?? null);` — valida a PRIMARY (D3/D8: obrigatoriedade é da primary; auxiliares opcionais).
    6. **`compressImage`** (:12-74): para arquivos HEIC/HEIF, decodificar via `createImageBitmap(file, { imageOrientation: "from-image" })` ANTES do `img.src`/drawImage (EXIF respeitado — D4); falha → `reject(new Error("Não foi possível processar a imagem HEIC. Use JPG ou PNG."))`; para PNG/JPG/WEBP manter o fluxo atual (Image/onload + canvas). MANTER o loop de qualidade JPEG ≤1MB + downscale 1200px e o retorno `{ file, dataUrl }`.
    7. **Body** (:724-738): substituir `productImageDataUrl: imageDataUrl` por: com auxiliares (`frozenFields.productImages.length > 1`) → `body.productImages = frozenFields.productImages.map(({ role, source, mimeType, dataUrl }) => ({ role, source, mimeType, dataUrl }))` (SEM id); sem auxiliares → `body.productImageDataUrl = frozenFields.productImages[0]?.dataUrl` (legado). A compressão da primary acontece ANTES (manter o bloco :706-714 adaptado: comprimir cada item que tenha `file` e ainda não tenha `dataUrl`). **CRÍTICO (contrato cross-plan):** após comprimir cada item, ATUALIZAR o `mimeType` do item para o tipo do blob comprimido (`"image/jpeg"` — o `compressImage` sempre produz JPEG via `toBlob(..., "image/jpeg")`) ANTES de montar o body. NUNCA enviar o `file.type` original no body (um HEIC/HEIF tem `file.type = "image/heic"`, que não é suportado por `transcodeToJpeg` na rota → 500, quebrando a câmera D4). O `mimeType` do body/item deve ser o tipo real dos bytes enviados (`image/jpeg`), coerente com F41-12 ("mimeType real derivado do dataUrl").
    8. **Restore/autosave** (:326-334 + :353-370): migrar draft legado — **condição: sem `productImages` utilizável (ausente OU vazio) + `restoredImageDataUrl` presente** (mocks co-migrados retornam `productImages: []` + `campaign_draft_image` no sessionStorage; o teste "navigates to campaignUrl ... with restored image" exige `isValid === true` — a migração precisa disparar com array vazio): converter para `productImages` de 1 elemento (`[{ id: crypto.randomUUID(), role: "primary", source: "upload", mimeType: "image/jpeg", dataUrl: restoredImageDataUrl }]`); se tiver `productImages` com `dataUrl`, restaurar com `file: undefined` (File não serializa — D3). `EMPTY_FIELDS.productImages: []` como default.
    9. **Preview effect** (:421-456): adaptar para a primary (posição 0) OU remover e deixar os previews por item no componente (plano 41-08); se mantiver `imagePreviewUrl`, ele passa a refletir `productImages[0]?.file` — revoke pattern (:427-429) mantido. **Decisão do planner:** manter `imagePreviewUrl` no hook para a primary (compat com o retorno atual do hook) e o grid completo no componente (41-08).
    10. **`isValid`** (:802): trocar `fields.imageFile instanceof File || !!restoredImageDataUrl` por `fields.productImages.length > 0 && (fields.productImages[0]?.file instanceof File || !!fields.productImages[0]?.dataUrl || !!restoredImageDataUrl)`.
    11. **`setField` tipo** (:127): `value: ... | File | null | undefined` → aceitar `Array<CampaignFormFields["productImages"]>` (ou `unknown` com cast interno) — ajustar a assinatura para o novo campo.
    12. **Novos helpers do hook `addImage`/`removeImage`** (exportados no retorno, consumidos pelo form no 41-07 Task 3 e pela UI completa no 41-08):
        - `addImage(file: File, source: "upload" | "camera")`: criar item `{ id: crypto.randomUUID(), role: fields.productImages.length === 0 ? "primary" : "reference", source, mimeType: file.type, file }` (primeiro = primary, demais = reference — D3); respeitar o teto `MAX_CAMPAIGN_IMAGES` (não adicionar além; o componente desabilita, mas o hook também bloqueia — D10).
        - `removeImage(id: string)`: remover por id; se remover a primary e houver itens restantes, promover o próximo a `role: "primary"` (validação de obrigatoriedade volta a falhar se ficar vazio — D3).
  </action>
  <verify>
    <automated>
      $h = Select-String -LiteralPath "src\components\flow\use-campaign-form.ts" -Pattern "productImages: Array<|image/heic|createImageBitmap|body.productImages|body.productImageDataUrl =" | Measure-Object; if ($h.Count -lt 5) { Write-Error "Hook multi incompleto" } else { Write-Output "OK: hook com productImages + HEIC + body D2" }; npm run typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - Grep `imageFile: File \| null` em use-campaign-form.ts → 0 (campo substituído)
    - Grep `productImages: Array<{` → 1 (interface); `productImages: []` → 1 (EMPTY_FIELDS)
    - Grep `"image/heic"` em validateImage → 1; `createImageBitmap` em compressImage → 1 (EXIF from-image)
    - Grep `body.productImages =` → 1 (com auxiliares); `body.productImageDataUrl =` → 1 (sem auxiliares — legado)
    - Grep `role: "primary"` no map do body → 0 (o role VEM do item, não hardcoded no body)
    - Grep `\.id` no map do body → 0 (id de cliente nunca entra no body — D2/D5)
    - Grep `mimeType` no map do body → o item usa o mimeType ATUALIZADO após compressão (`image/jpeg`), nunca `file.type` cru — NENHUM caminho envia `image/heic`/`image/heif` no body (contrato cross-plan: `transcodeToJpeg` só aceita png/jpeg/webp)
    - `npm run typecheck` limpo (tipo novo propagado)
  </acceptance_criteria>
  <done>Hook multi-imagem: state, HEIC/EXIF, body D2, draft multi; typecheck limpo</done>
</task>

<task type="auto">
  <name>Task 2: Co-migração dos testes irmãos (navigation/notice/validity) — mocks restore com productImages: []</name>
  <files>src/components/flow/__tests__/use-campaign-form-navigation.test.ts, src/components/flow/__tests__/use-campaign-form-notice.test.ts, src/components/flow/__tests__/use-campaign-form-validity.test.ts</files>
  <read_first>
    - src/components/flow/__tests__/use-campaign-form-navigation.test.ts (5 mocks restore :61-77/:106-114/:152-160/:187-195/:209-217)
    - src/components/flow/__tests__/use-campaign-form-notice.test.ts (3 mocks :61-69/:156-163/:176-183)
    - src/components/flow/__tests__/use-campaign-form-validity.test.ts (1 mock :115-123)
    - src/components/flow/use-campaign-form.ts (recém-modificado — o shape novo de CampaignFormFields)
  </read_first>
  <action>
    Co-migrar os 3 arquivos de teste que espelham `CampaignFormFields` (tasks 15.7 + PATTERNS nota 2):
    1. **`use-campaign-form-navigation.test.ts`**: nos 5 `mockRestoreFormState.mockReturnValue({...})` (:61-77, :106-114, :152-160, :187-195, :209-217) substituir `imageFile: null,` por `productImages: [],` (5 ocorrências).
    2. **`use-campaign-form-notice.test.ts`**: nos 3 mocks (:61-69, :156-163, :176-183) substituir `imageFile: null,` por `productImages: [],`.
    3. **`use-campaign-form-validity.test.ts`**: no 1 mock (:115-123) substituir `imageFile: null,` por `productImages: [],`.
    4. **`use-campaign-form-submit-error.test.ts`** e **`intent-badge-preserve.test.ts`**: verificar se referenciam `imageFile` — o submit-error test usa apenas `handleApiError` puro (sem fields) e o intent-badge-preserve retorna `null` do restore (mock :27) — NÃO exigem mudança, mas rodar typecheck para confirmar. Se qualquer um referenciar `imageFile`, substituir por `productImages: []`.
    5. NÃO alterar asserções de comportamento — apenas o shape dos mocks (o hook restaurará `productImages: []` corretamente).
  </action>
  <verify>
    <automated>
      $left = @(Select-String -LiteralPath "src\components\flow\__tests__\use-campaign-form-navigation.test.ts","src\components\flow\__tests__\use-campaign-form-notice.test.ts","src\components\flow\__tests__\use-campaign-form-validity.test.ts" -Pattern "imageFile: null" | Measure-Object).Count; if ($left -gt 0) { Write-Error "Mocks imageFile restantes: $left" } else { Write-Output "OK: zero imageFile: null nos 3 testes irmãos" }; npx vitest run src/components/flow/__tests__/use-campaign-form-navigation.test.ts src/components/flow/__tests__/use-campaign-form-notice.test.ts src/components/flow/__tests__/use-campaign-form-validity.test.ts src/components/flow/__tests__/use-campaign-form-submit-error.test.ts 2>$null; npm run typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - Grep `imageFile: null` nos 3 arquivos irmãos → 0 ocorrências
    - Grep `productImages: []` nos 3 arquivos → navigation: 5, notice: 3, validity: 1 (contagem total ≥ 9)
    - `npx vitest run` nos 4 testes de form → todos verdes (navigation, notice, validity, submit-error)
    - `npm run typecheck` limpo
  </acceptance_criteria>
  <done>3 testes irmãos co-migrados (productImages: []) — suíte de form verde</done>
</task>

<task type="auto">
  <name>Task 3: campaign-input-form.tsx + campaign-image-upload.tsx — co-migração TIPO-NÍVEL do call site (typecheck)</name>
  <files>src/components/flow/campaign-input-form.tsx, src/components/flow/campaign-image-upload.tsx</files>
  <read_first>
    - src/components/flow/campaign-input-form.tsx (linhas 362-367 — call site do CampaignImageUpload: `imageFile={fields.imageFile}`, `onSelect={(file) => setField("imageFile", file)}`)
    - src/components/flow/campaign-image-upload.tsx (arquivo inteiro — props atuais :6-11 `{ imageFile, error, previewUrl, onSelect }`)
    - src/components/flow/use-campaign-form.ts (recém-modificado — `CampaignFormFields.productImages`; retorno do hook)
  </read_first>
  <action>
    Co-migrar o call site e as props do componente NO MESMO plano (o `npm run typecheck` de 41-07 roda sobre o projeto inteiro e falharia com TS2339/TS2345 — contrato cross-plan; o grid completo/câmera/remoção fica no 41-08, Wave 4):
    1. **`campaign-image-upload.tsx`** — trocar as props de `{ imageFile, error, previewUrl, onSelect }` para o NOVO contrato de props mínimo `{ productImages: Array<{ id: string; role: "primary" | "reference"; source: "upload" | "camera"; file?: File; dataUrl?: string }>; error: string | null; onAdd: (file: File, source: "upload" | "camera") => void; onRemove: (id: string) => void; }`. Manter o render ATUAL (preview da primary via `productImages[0]` + input file sem `multiple`/`capture`) — a implementação completa multi/câmera/grid é o 41-08. O objetivo aqui é APENAS o contrato de tipos compilar (o form passa `productImages`, `error`, `onAdd`, `onRemove`).
    2. **`campaign-input-form.tsx:362-367`** — substituir o call site por `<CampaignImageUpload productImages={fields.productImages} error={touched.productImages ? fieldErrors.productImages ?? null : null} onAdd={(file, source) => { ... adiciona item ao array ... }} onRemove={(id) => { ... remove item do array ... }} />`. As funções `onAdd`/`onRemove` manipulam `fields.productImages` via `setField("productImages", [...])` (a lógica de criação de item — `{ id: crypto.randomUUID(), role: produto ? ... : "primary"/"reference", source, mimeType: file.type, file }` — pode ser um helper inline no form ou no hook; a compressão/EXIF fica no submit do hook 41-07). **Decisão do planner:** as funções `addImage`/`removeImage` vivem no hook `use-campaign-form` (exportadas no retorno) e o form apenas as repassa — assim 41-08 só precisa do grid visual.
    3. NÃO implementar `multiple`/`capture`/grid no componente nesta task (é o 41-08) — apenas o contrato de props + o call site compilando.
  </action>
  <verify>
    <automated>
      if (Select-String -LiteralPath "src\components\flow\campaign-input-form.tsx" -Pattern "fields.imageFile|setField\(\"imageFile\"" -Quiet) { Write-Error "Call site imageFile ainda presente" } else { Write-Output "OK: call site co-migrado" }; if (Select-String -LiteralPath "src\components\flow\campaign-image-upload.tsx" -Pattern "productImages|onAdd|onRemove" -Quiet) { Write-Output "OK: props novo contrato" } else { Write-Error "Props novo contrato ausente" }; npm run typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - Grep `fields.imageFile` em campaign-input-form.tsx → 0; `setField("imageFile"` → 0
    - Grep `productImages=` no call site do `CampaignImageUpload` → 1; `onAdd=`/`onRemove=` → presentes
    - `campaign-image-upload.tsx` exporta props `{ productImages, error, onAdd, onRemove }` (sem `imageFile`/`previewUrl`/`onSelect`)
    - `npm run typecheck` limpo (sem TS2339/TS2345 — contrato de tipos propagado)
  </acceptance_criteria>
  <done>call site + props co-migrados (tipo-nível) — typecheck de 41-07 limpo; UI completa no 41-08</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| form state → body HTTP | Estado do form (productImages com dataUrl) atravessa para o corpo da requisição |
| arquivo local → canvas | Arquivos do usuário (incluindo HEIC) são processados no browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-07 | Spoofing | Id interno no body | mitigate | `id` interno da UI é chave de lista/preview — NUNCA entra no body (`productImages.map(({ role, source, mimeType, dataUrl }) => ...)` destrutura e omite id — D2/D5) |
| T-41-07a | Tampering | Body D2 | mitigate | Com auxiliares → `productImages[]`; sem → `productImageDataUrl` legado; nunca ambos (a rota 400 em payload ambíguo — D2) |
| T-41-07b | Tampering | Decode HEIC | mitigate | `createImageBitmap from-image` respeita EXIF; falha de decode → mensagem PT-BR clara e NENHUMA imagem adicionada (D4); sem dependência de lib HEIC |
| T-41-07c | DoS | Arquivos grandes | mitigate | `validateImage` rejeita > 5MB e formatos inválidos por item ANTES de entrar no grid (D10); compressão JPEG ≤1MB/1200px por item |
| T-41-07d | Information Disclosure | dataUrl no estado | accept | Base64 em memória/sessionStorage (draft) — dados do próprio usuário; snapshot nunca persiste base64 (D6/D7) |
| T-41-07e | Tampering | Restore de draft | mitigate | Migração de draft legado (`imageFile` → `productImages` de 1 elemento) tipada; `File` não serializa → restore com `file: undefined` + `dataUrl` (D3) |
| T-41-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo — sem gate de legitimidade |
</threat_model>

<verification>
- Grep `imageFile: File | null` em use-campaign-form.ts → 0
- Grep `createImageBitmap` em use-campaign-form.ts → 1 (EXIF from-image)
- Grep `body.productImages` + `body.productImageDataUrl` em use-campaign-form.ts → ambos presentes (D2)
- Grep `imageFile: null` nos 3 testes irmãos → 0
- `npx vitest run src/components/flow/__tests__/` — suíte de form verde
- `npm run typecheck` limpo
</verification>

<success_criteria>
- Estado multi `productImages` (primary + reference) com id interno da UI
- `compressImage` por item com HEIC/EXIF (createImageBitmap from-image) e falha PT-BR clara
- `validateImage` aceita HEIC/HEIF
- Body D2 (productImages[] com auxiliares; productImageDataUrl legado sem; nunca ambos; id nunca no body)
- Draft/autosave restaura N imagens + migração de draft legado
- 3 testes irmãos co-migrados — suíte de form verde + typecheck limpo
</success_criteria>

<output>
Create `.planning/phases/41-midia-de-campanha-mobile/41-07-SUMMARY.md` when done
</output>

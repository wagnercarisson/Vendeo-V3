---
phase: 41-midia-de-campanha-mobile
plan: 09
type: execute
wave: 4
depends_on: [41-04]
files_modified:
  - src/lib/campaign/__tests__/brief-mapper.test.ts
  - src/lib/campaign/__tests__/brief-snapshot.test.ts
autonomous: true
requirements: [F41-25]

must_haves:
  truths:
    - "Testes 1-8 (transporte/mapper/snapshot) implementados: mapper multi (1), legado idêntico (2), invariante rejeitado (3), mimeType derivado (5), snapshot N imagens sem base64 com storagePath (6), legado snapshot idêntico (7), exactly-1-primary zod (8)"
    - "brief-mapper.test.ts co-migrado: fixture flatInput com casos productImages + legado preservado"
    - "brief-snapshot.test.ts co-migrado: hasBase64Leak + storagePath por imagem"
  artifacts:
    - path: "src/lib/campaign/__tests__/brief-mapper.test.ts"
      provides: "Testes 1, 2, 3, 5 do mapper multi"
      contains: "productImages"
    - path: "src/lib/campaign/__tests__/brief-snapshot.test.ts"
      provides: "Testes 6, 7, 8 do snapshot N"
      contains: "storagePath"
  key_links:
    - from: "brief-mapper.test.ts (fixture productImages)"
      to: "brief.ts buildCampaignBriefFromFlat"
      via: "roles/source/mimeType do transporte → domínio"
      pattern: "productImages"
    - from: "brief-snapshot.test.ts (hasBase64Leak)"
      to: "brief.ts buildCampaignBriefSnapshot"
      via: "N imagens sem base64 + storagePath"
      pattern: "storagePath"
---

<objective>
Implementar os **testes 1-8 (transporte/mapper/snapshot)** (D2/D3/D5): mapper multi-imagem com roles/source/mimeType corretos (1), legado `productImageDataUrl` idêntico (2), invariante exactly-1-primary rejeitado no transporte (3), `mimeType` derivado do dataUrl — corrige o quirk da F39 (5), snapshot com N imagens **sem base64** e com `storagePath` por imagem (6), legado → snapshot idêntico ao pós-F40 (7), exatamente 1 primary no snapshot/domínio (8). Co-migrar os fixtures dos 2 arquivos de teste.

Purpose: D2 (transporte aditivo + mapeamento legado = 1 elemento) + D3 (roles/source) + D5 (storagePath no snapshot, sem base64).
Output: brief-mapper.test.ts + brief-snapshot.test.ts com os testes 1-8 (teste 4 — 400 ambíguo — pertence ao route.test.ts, plano 41-11).
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-midia-de-campanha-mobile/41-CONTEXT.md (D2, D3, D5; F41-25)
@.planning/phases/41-midia-de-campanha-mobile/41-PATTERNS.md (seção Testes — brief-mapper.test.ts e brief-snapshot.test.ts — co-migrations)
@openspec/changes/fase-41-midia-de-campanha-mobile/tasks.md (seção 12 — testes 1-8: 12.1, 12.2, 12.3, 12.5, 12.6, 12.7, 12.8 — o 12.4 é rota)
@openspec/changes/fase-41-midia-de-campanha-mobile/specs/campaign-brief-mapper/spec.md (cenários do mapper)
@openspec/changes/fase-41-midia-de-campanha-mobile/specs/campaign-brief-snapshot/spec.md (cenários do snapshot)
@src/lib/campaign/__tests__/brief-mapper.test.ts (LINHAS VERIFICADAS — flatInput :12-24; caso single :87-95)
@src/lib/campaign/__tests__/brief-snapshot.test.ts (LINHAS VERIFICADAS — hasBase64Leak :13-33; flatInput :37-46; shape :49-60)
@src/lib/campaign/brief.ts (recém-modificado no 41-04)
@src/lib/image-generation/schema.ts (recém-modificado no 41-02 — ProductImageInputSchema)

<interfaces>
Testes a implementar (tasks 12.1-12.8):

| Teste | Descrição | Assert |
|-------|-----------|--------|
| 1 | `productImages[]` com primary + 2 reference → mapper gera `media.images` com roles/source/mimeType corretos | `media.images[0].role === "primary"`, `[1].role === "reference"`, source/mimeType espelhados, ids uuid |
| 2 | `productImages` ausente + `productImageDataUrl` → legado idêntico (1 item primary/upload) | mesmo resultado do `productImages` de 1 elemento (roles/source) |
| 3 | `productImages` sem primary ou com 2 primaries → `GenerateImageRequestSchema.safeParse` rejeita | `{ success: false }` + issue "Deve existir exatamente 1 imagem com role" |
| 5 | `mimeType` derivado do dataUrl (png/jpeg/webp) | `data:image/png;base64,...` → `mimeType === "image/png"` (corrige quirk F39) |
| 6 | Snapshot com N imagens: sem `dataUrl`, com `storagePath` por imagem | `hasBase64Leak` falso; `images[i].storagePath` presente quando runtime tem |
| 7 | Legado 1 imagem → snapshot idêntico ao pós-F40 | `flatInput` com `productImageDataUrl` → snapshot com 1 imagem `mimeType: "image/jpeg"` (regressão) |
| 8 | Exatamente 1 primary no snapshot/domínio para N imagens | zod do domínio aceita; snapshot preserva roles |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: brief-mapper.test.ts — testes 1, 2, 3, 5 do mapper multi</name>
  <files>src/lib/campaign/__tests__/brief-mapper.test.ts</files>
  <read_first>
    - src/lib/campaign/__tests__/brief-mapper.test.ts (linhas 12-24 flatInput; :87-95 caso single-image — regressão a preservar)
    - src/lib/campaign/brief.ts (recém-modificado no 41-04 — mapper + mimeTypeFromDataUrl)
    - src/lib/image-generation/schema.ts (recém-modificado no 41-02 — ProductImageInputSchema + GenerateImageRequestSchema com productImages)
  </read_first>
  <action>
    Modificar `brief-mapper.test.ts` (tasks 12.1, 12.2, 12.3, 12.5):
    1. **Fixture**: adicionar ao fixture existente (`flatInput` :12-24) um helper/casos com `productImages`: ex. `const flatWithProductImages = { ...flatInput, productImageDataUrl: undefined, productImages: [{ role: "primary" as const, source: "camera" as const, mimeType: "image/png", dataUrl: "data:image/png;base64,abc" }, { role: "reference" as const, source: "upload" as const, mimeType: "image/jpeg", dataUrl: "data:image/jpeg;base64,def" }, { role: "reference" as const, source: "upload" as const, mimeType: "image/webp", dataUrl: "data:image/webp;base64,ghi" }] };`.
    2. **Teste 1**: `buildCampaignBriefFromFlat(flatWithProductImages, "store-1")` → `media.images` com 3 itens: `[0]` role `"primary"` + source `"camera"`; `[1]`/`[2]` role `"reference"`; mimeTypes espelhados; cada `id` é uuid não-vazio.
    3. **Teste 2**: `buildCampaignBriefFromFlat({ ...flatInput, productImageDataUrl: "data:image/jpeg;base64,abc123" }, "store-1")` → `media.images` com 1 item: role `"primary"`, source `"upload"`, mimeType `"image/jpeg"` (via `mimeTypeFromDataUrl`) — **regressão preservada** (o caso single existente :87-95 continua passando).
    4. **Teste 3**: `GenerateImageRequestSchema.safeParse` com `productImages` sem primary (2 reference) e com 2 primaries → `{ success: false }` com issue de `"Deve existir exatamente 1 imagem com role \"primary\""` (invariante no TRANSPORTE — D2).
    5. **Teste 5**: para `data:image/png;base64,...`, `data:image/jpeg;base64,...`, `data:image/webp;base64,...` → mimeType derivado correto em `media.images[i].mimeType` (corrige o quirk `"image/jpeg"` fixo da F39).
    6. NÃO alterar os casos existentes (regressão) — adicionar novos `it(...)`.
  </action>
  <verify>
    <automated>
      $m = Select-String -LiteralPath "src\lib\campaign\__tests__\brief-mapper.test.ts" -Pattern "productImages|mimeTypeFromDataUrl|2 primaries|sem primary" | Measure-Object; if ($m.Count -lt 3) { Write-Error "Mapper tests incompletos" } else { Write-Output "OK: testes de mapper adicionados" }; npx vitest run src/lib/campaign/__tests__/brief-mapper.test.ts 2>$null
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/campaign/__tests__/brief-mapper.test.ts` → todos os testes verdes (novos 1, 2, 3, 5 + existentes)
    - Grep `productImages` no arquivo → ≥ 2 (fixture + casos)
    - Teste 3 usa `GenerateImageRequestSchema.safeParse` (invariante no transporte) → grep `safeParse` presente
    - Teste 5 cobre png/jpeg/webp → grep `image/png`/`image/webp` presentes
  </acceptance_criteria>
  <done>brief-mapper.test.ts com testes 1, 2, 3, 5 (mapper multi + legado + invariante + mimeType)</done>
</task>

<task type="auto">
  <name>Task 2: brief-snapshot.test.ts — testes 6, 7, 8 do snapshot N + storagePath</name>
  <files>src/lib/campaign/__tests__/brief-snapshot.test.ts</files>
  <read_first>
    - src/lib/campaign/__tests__/brief-snapshot.test.ts (linhas 13-33 hasBase64Leak; :37-46 flatInput; :49-60 shape assertion)
    - src/lib/campaign/brief.ts (recém-modificado no 41-04 — snapshot copia storagePath; mapper multi)
  </read_first>
  <action>
    Modificar `brief-snapshot.test.ts` (tasks 12.6, 12.7, 12.8):
    1. **Helper de fixture**: adicionar helper que monta um brief com `productImages` de 3 itens (1 primary + 2 reference) e **preenche `storagePath` no runtime** ANTES de `buildCampaignBriefSnapshot` (ex.: `brief.media.images[0].storagePath = "store-1/camp-1/inputs/img-1.jpg"`).
    2. **Teste 6**: snapshot com N imagens — `hasBase64Leak(snapshot)` falso (varredura recursiva SEM `dataUrl`/`base64`/`data:image/` — reusar `hasBase64Leak` :13-33); `snapshot.media.images` com 3 itens; `[0].storagePath` presente (`"store-1/camp-1/inputs/img-1.jpg"`) e item sem storagePath → campo ausente (`"storagePath" in image === false`).
    3. **Teste 7**: legado com 1 imagem (`flatInput` com `productImageDataUrl: "data:image/jpeg;base64,abc123"`) → snapshot com 1 imagem, `mimeType: "image/jpeg"`, SEM `dataUrl` — idêntico ao pós-F40 (regressão; `mimeTypeFromDataUrl` devolve `image/jpeg`).
    4. **Teste 8**: snapshot para N imagens preserva exatamente 1 primary (roles espelhados) — e o zod do domínio (`brief-schema.ts`) aceita o brief com 1 primary + N references.
    5. NÃO alterar os testes existentes (shape assertion :49-60 etc.) — adicionar novos `it(...)`.
  </action>
  <verify>
    <automated>
      $s = Select-String -LiteralPath "src\lib\campaign\__tests__\brief-snapshot.test.ts" -Pattern "storagePath|hasBase64Leak|3 itens|exatamente 1 primary" | Measure-Object; if ($s.Count -lt 3) { Write-Error "Snapshot tests incompletos" } else { Write-Output "OK: testes de snapshot adicionados" }; npx vitest run src/lib/campaign/__tests__/brief-snapshot.test.ts 2>$null
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/campaign/__tests__/brief-snapshot.test.ts` → todos os testes verdes (novos 6, 7, 8 + existentes)
    - Grep `storagePath` no arquivo → ≥ 2 (fixture + assert)
    - Teste 6 usa `hasBase64Leak` → sem base64 no snapshot (N imagens)
    - Teste 7 asserta `mimeType: "image/jpeg"` para o legado (regressão F40)
    - Teste 8 asserta exatamente 1 primary no snapshot
  </acceptance_criteria>
  <done>brief-snapshot.test.ts com testes 6, 7, 8 (N sem base64, storagePath, legado idêntico, exactly-1-primary)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transporte flat → domínio | Dados de teste validam o mapper/snapshot (sem entrada de usuário real) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-09 | Tampering | Testes de invariante | mitigate | Teste 3 valida o invariante exactly-1-primary NO TRANSPORTE (`GenerateImageRequestSchema.safeParse`) e o teste 8 no snapshot/domínio — dupla âncora (D2/D3) |
| T-41-09a | Information Disclosure | Teste de vazamento base64 | mitigate | Teste 6 usa `hasBase64Leak` (varredura recursiva) — garante que N imagens do snapshot NUNCA carregam dataUrl (D5/D7) |
| T-41-09b | Tampering | Regressão legado | mitigate | Testes 2 e 7 asseguram que `productImageDataUrl` legado produz snapshot idêntico ao pós-F40 (D2) |
| T-41-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo — sem gate de legitimidade |
</threat_model>

<verification>
- `npx vitest run src/lib/campaign/__tests__/brief-mapper.test.ts src/lib/campaign/__tests__/brief-snapshot.test.ts` — todos verdes
- `npm run typecheck` limpo
</verification>

<success_criteria>
- Testes 1, 2, 3, 5 (mapper) e 6, 7, 8 (snapshot) implementados e verdes
- Fixtures co-migrados (productImages + storagePath) sem quebrar os casos existentes
- Regressão do legado preservada (snapshot idêntico ao pós-F40)
</success_criteria>

<output>
Create `.planning/phases/41-midia-de-campanha-mobile/41-09-SUMMARY.md` when done
</output>

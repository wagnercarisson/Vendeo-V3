---
phase: 41-midia-de-campanha-mobile
plan: 10
type: execute
wave: 5
depends_on: [41-07, 41-08]
files_modified:
  - src/components/flow/__tests__/use-campaign-form-product-images.test.ts
  - src/components/flow/__tests__/use-campaign-form-navigation.test.ts
autonomous: true
requirements: [F41-25]

must_haves:
  truths:
    - "Testes 9-16 (UI/form) implementados: primary obrigatória + auxiliares até MAX-1 (9), seleção/remoção/preview (10), source câmera/upload (11), HEIC aceito + decode + falha PT-BR (12), EXIF respeitada (13), body D2 (14), draft restaura N (15), erros de limite (16)"
    - "Novo arquivo de teste use-campaign-form-product-images.test.ts criado com os casos 9-16"
    - "use-campaign-form-navigation.test.ts (co-migrado no 41-07) recebe os novos casos de restore multi (15)"
  artifacts:
    - path: "src/components/flow/__tests__/use-campaign-form-product-images.test.ts"
      provides: "Testes 9-16 do form multi"
      contains: "productImages"
  key_links:
    - from: "use-campaign-form-product-images.test.ts"
      to: "use-campaign-form.ts (validateImage/compressImage/handleSubmit)"
      via: "validação primária + body D2 + limites"
      pattern: "productImages"
---

<objective>
Implementar os **testes 9-16 (UI/form)** (D2/D3/D4/D10) num novo arquivo `use-campaign-form-product-images.test.ts`: primary obrigatória + auxiliares opcionais até `MAX_CAMPAIGN_IMAGES - 1` (9); seleção/remoção/preview por item, remover primary → validação (10); origem câmera → `source: "camera"`, galeria → `"upload"` (11); HEIC aceito no input, decode via canvas → JPEG, falha → mensagem PT-BR clara (12); orientação EXIF respeitada na compressão (`createImageBitmap from-image`) (13); body com auxiliares → `productImages[]` sem id, sem auxiliares → `productImageDataUrl` legado (14); draft/autosave restaura N imagens (15); erros de limite por item e teto no form (16). Adicionar os casos de restore multi ao navigation test.

Purpose: D2 (body) + D3 (roles/preview) + D4 (HEIC/EXIF/câmera) + D10 (limites).
Output: testes 9-16 verdes (mais os casos de restore no navigation test).
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-midia-de-campanha-mobile/41-CONTEXT.md (D2, D3, D4, D10; F41-25)
@openspec/changes/fase-41-midia-de-campanha-mobile/tasks.md (seção 13 — testes 9-16)
@openspec/changes/fase-41-midia-de-campanha-mobile/specs/campaign-media-upload/spec.md (cenários de UI: HEIC/EXIF/limites/origem)
@openspec/changes/fase-41-midia-de-campanha-mobile/specs/campaign-input-ui/spec.md (cenários de body e state multi)
@src/components/flow/__tests__/use-campaign-form-navigation.test.ts (LINHAS VERIFICADAS — padrão de mock restore :61-77; padrão sessionStorage campaign_draft_image :50)
@src/components/flow/__tests__/use-campaign-form-notice.test.ts / use-campaign-form-validity.test.ts (padrões de restore — co-migrados no 41-07)
@src/components/flow/use-campaign-form.ts (recém-modificado no 41-07)

<interfaces>
Testes a implementar (tasks 13.1-13.8):

| Teste | Descrição | Como |
|-------|-----------|------|
| 9 | primary obrigatória; auxiliares opcionais até MAX-1 | `validateField("productImages", { productImages: [] })` → erro; com primary → ok; padrão dos testes de validateImage existentes |
| 10 | seleção/remoção/preview por item; remover primary → validação | simular `setField("productImages", ...)` com item; remover o único primary → `validateField` volta a falhar |
| 11 | origem câmera → source camera; galeria → upload | `onAdd(file, "camera")` → item `source: "camera"`; `onAdd(file, "upload")` → `"upload"` (via mock do form ou assert do helper) |
| 12 | HEIC aceito no input; decode → JPEG; falha → mensagem PT-BR | `validateImage(new File([...], "f.heic", { type: "image/heic" }))` → null; `compressImage` com `createImageBitmap` mockado → JPEG dataUrl; falha → reject com "Não foi possível processar a imagem HEIC. Use JPG ou PNG." |
| 13 | EXIF respeitada (createImageBitmap from-image) | `compressImage` com HEIC → `expect(createImageBitmap).toHaveBeenCalledWith(file, { imageOrientation: "from-image" })` |
| 14 | body: com auxiliares → productImages[] sem id; sem → productImageDataUrl legado | mock do `fetch` + mock `useInputPreservation` → `handleSubmit` → assert do body enviado |
| 15 | draft/autosave restaura N imagens | `mockRestoreFormState.mockReturnValue({ ...productImages: [3 itens com dataUrl] })` → `fields.productImages` restaurado com 3 itens (file undefined) |
| 16 | erros de limite por item | `validateImage` com arquivo > 5MB → "Arquivo muito grande. Máximo 5MB"; formato inválido → "Formato não suportado..." |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: use-campaign-form-product-images.test.ts — testes 9, 10, 11, 12, 13, 16 (validação/compressão/origem)</name>
  <files>src/components/flow/__tests__/use-campaign-form-product-images.test.ts</files>
  <read_first>
    - src/components/flow/use-campaign-form.ts (recém-modificado no 41-07 — validateImage :197-207, compressImage :12-74, validateField :209-227)
    - src/components/flow/__tests__/use-campaign-form-navigation.test.ts (linhas 1-60 — padrão de setup do teste do hook: vi.mock next/navigation + use-input-preservation + renderHook)
    - src/components/flow/__tests__/use-campaign-form-validity.test.ts (linhas 1-50 — padrão de VALID_DATA_URL e mock restore)
  </read_first>
  <action>
    Criar `src/components/flow/__tests__/use-campaign-form-product-images.test.ts` com o padrão de setup dos testes irmãos (vi.mock `next/navigation`, `use-input-preservation`, `@/lib/constants`, `@/lib/formatters`; `VALID_DATA_URL`; jsdom via `@vitest-environment jsdom`). Implementar:
    - **Teste 9**: `validateField("productImages", fields)` com `productImages: []` → `"Imagem do produto é obrigatória"`; com 1 item (file) → null; com `MAX_CAMPAIGN_IMAGES - 1` auxiliares (sem primary seria inválido — primary é a obrigatória).
    - **Teste 10**: `renderHook(() => useCampaignForm("store-1"))` + `act(() => setField("productImages", [item]))` → `fields.productImages` com 1 item; remover o único primary (`setField("productImages", [])`) → `validateField("productImages", ...)` retorna erro de obrigatoriedade (validação volta a falhar).
    - **Teste 11**: verificar a atribuição de source — **assertão direta**: `onAdd(file, "camera")` → item `source: "camera"`; `onAdd(file, "upload")` → item `source: "upload"` (via helper de criação de item exportado pelo form, OU via render do `CampaignInputForm` com `fireEvent.change`/`fireEvent.click` nos inputs de câmera/galeria, OU via `handleSubmit` + body preservando `source` de cada item — teste 14). **Não aceitar "mínimo viável" sem assertão concreta**: pelo menos UM caminho de teste verifica `source: "camera"` vs `source: "upload"` end-to-end (component fireEvent ou body).
    - **Teste 12**: `validateImage(new File([""], "foto.heic", { type: "image/heic" }))` → `null` (HEIC aceito); `validateImage(new File([""], "f.gif", { type: "image/gif" }))` → `"Formato não suportado. Use PNG, JPG, WEBP ou HEIC"`. Para o decode: chamar `compressImage` com HEIC e `globalThis.createImageBitmap` mockado (resolve um objeto com width/height e drawImage no ctx) → resolve `{ file: File (image/jpeg), dataUrl }`; falha do createImageBitmap → reject com mensagem PT-BR "Não foi possível processar a imagem HEIC. Use JPG ou PNG.".
    - **Teste 13**: `compressImage` com HEIC → `expect(globalThis.createImageBitmap).toHaveBeenCalledWith(file, { imageOrientation: "from-image" })`.
    - **Teste 16**: `validateImage` com arquivo PNG > 5MB → `"Arquivo muito grande. Máximo 5MB"`; formato inválido (SVG) → mensagem de formato. (Erros de teto agregado na rota são teste 24 — rota.)
    - **Mock de createImageBitmap**: `vi.stubGlobal("createImageBitmap", vi.fn().mockResolvedValue({ width: 100, height: 100 }))` + mock de canvas (`HTMLCanvasElement.prototype.getContext` e `toBlob` se necessário) — jsdom não tem canvas real; mockar `getContext` para retornar um objeto com `drawImage` e `toBlob` que chama o callback com um Blob.
  </action>
  <verify>
    <automated>
      npx vitest run src/components/flow/__tests__/use-campaign-form-product-images.test.ts 2>$null
    </automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `use-campaign-form-product-images.test.ts` existe
    - Grep `createImageBitmap` no arquivo → ≥ 1 (testes 12/13)
    - Grep `image/heic` → ≥ 1 (teste 12)
    - Grep `Máximo 5MB` → ≥ 1 (teste 16)
    - `npx vitest run src/components/flow/__tests__/use-campaign-form-product-images.test.ts` → testes 9-13, 16 verdes
  </acceptance_criteria>
  <done>Testes 9, 10, 11, 12, 13, 16 implementados e verdes</done>
</task>

<task type="auto">
  <name>Task 2: Testes 14 (body D2) e 15 (draft restaura N) — body submit + restore multi</name>
  <files>src/components/flow/__tests__/use-campaign-form-product-images.test.ts, src/components/flow/__tests__/use-campaign-form-navigation.test.ts</files>
  <read_first>
    - src/components/flow/use-campaign-form.ts (handleSubmit :638-746 — body :724-738 recém-modificado; restore :323-351)
    - src/components/flow/__tests__/use-campaign-form-navigation.test.ts (padrão do mock de fetch/NDJSON :40-60; padrão sessionStorage campaign_draft_image :50; 5 mocks restore)
    - src/components/flow/__tests__/use-campaign-form-notice.test.ts (padrão de VALID_DATA_URL + sessionStorage)
  </read_first>
  <action>
    Implementar os testes 14 e 15:
    - **Teste 14** (body D2): no novo arquivo, mockar `global.fetch` para capturar o body (padrão do navigation test :40-60). Montar `fields` com `productImages: [primary, aux1, aux2]` (cada um com `dataUrl`) + `campaignIntent: "offer"` + preços válidos; `act(() => handleSubmit())` → `expect(fetch).toHaveBeenCalledWith("/api/campaign/generate-image", expect.objectContaining({ body: expect.stringContaining("\"productImages\"") }))`; parsear o body e assertar: `body.productImages` tem 3 itens, NENHUM com `id`, roles `["primary","reference","reference"]`, e `body.productImageDataUrl` AUSENTE. Segundo caso: `productImages: [primary]` apenas → `body.productImageDataUrl` presente e `body.productImages` ausente (legado — D2).
    - **Teste 15** (draft restaura N): no `use-campaign-form-navigation.test.ts`, adicionar um caso: `mockRestoreFormState.mockReturnValue({ ...fieldsCompletos, productImages: [{ id: "a", role: "primary", source: "upload", mimeType: "image/jpeg", dataUrl: VALID_DATA_URL }, { id: "b", role: "reference", source: "camera", mimeType: "image/jpeg", dataUrl: VALID_DATA_URL }] })` → `renderHook` → `fields.productImages` restaurado com 2 itens (file undefined — File não serializa; primary identificada). 
    - **Atenção**: no restore, o hook pode usar `restoredImageDataUrl` do sessionStorage (`campaign_draft_image`). Para o caso multi, os `dataUrl` dos itens vivem no próprio `productImages` (persistidos via saveFormState) — o teste deve verificar `fields.productImages[0].dataUrl` presente.
    - Não quebrar os 5 casos de restore existentes (co-migrados no 41-07).
  </action>
  <verify>
    <automated>
      npx vitest run src/components/flow/__tests__/use-campaign-form-product-images.test.ts src/components/flow/__tests__/use-campaign-form-navigation.test.ts 2>$null
    </automated>
  </verify>
  <acceptance_criteria>
    - Teste 14: body com `productImages` (3 itens, sem id, roles primary/reference/reference) E body legado com `productImageDataUrl` (1 primary) — ambos verdes
    - Grep `"productImages"` no assert do fetch → ≥ 1; Grep `productImageDataUrl` → ≥ 1
    - Teste 15: `npx vitest run` navigation test → todos os casos verdes (5 existentes + 1 novo restore multi)
    - Grep `productImages` no navigation test → ≥ 1 (novo caso)
  </acceptance_criteria>
  <done>Testes 14 (body D2) e 15 (draft N) verdes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| form hook → body HTTP | Testes capturam o body enviado (productImages[] vs legado) |
| arquivo HEIC → canvas | Testes mockam createImageBitmap/canvas (jsdom) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-10 | Tampering | Body D2 no teste 14 | mitigate | Asserts de exclusividade: com auxiliares → `productImages` sem id; sem → `productImageDataUrl`; nunca ambos (D2) |
| T-41-10a | Spoofing | Source no teste 11 | mitigate | `onAdd(file, source)` — source derivado do gatilho, testado por origem (câmera vs galeria) (D4) |
| T-41-10b | Tampering | HEIC/EXIF nos testes 12/13 | mitigate | Mock de `createImageBitmap from-image` + falha PT-BR — cobre o caminho HEIC sem depender de lib (D4) |
| T-41-10c | DoS | Limites no teste 16 | mitigate | Asserts de >5MB e formato inválido por item (D10) |
| T-41-10d | Information Disclosure | dataUrl nos testes | accept | Dados de teste (dataUrl fictício) — sem dado real |
| T-41-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo — sem gate de legitimidade |
</threat_model>

<verification>
- `npx vitest run src/components/flow/__tests__/use-campaign-form-product-images.test.ts` — testes 9-16 verdes
- `npx vitest run src/components/flow/__tests__/use-campaign-form-navigation.test.ts` — 5 existentes + novo caso multi verde
- `npm run typecheck` limpo
</verification>

<success_criteria>
- Testes 9-16 implementados e verdes (primary obrigatória, seleção/remoção, source, HEIC/EXIF, body D2, draft N, limites)
- Novo arquivo use-campaign-form-product-images.test.ts + caso de restore multi no navigation test
</success_criteria>

<output>
Create `.planning/phases/41-midia-de-campanha-mobile/41-10-SUMMARY.md` when done
</output>

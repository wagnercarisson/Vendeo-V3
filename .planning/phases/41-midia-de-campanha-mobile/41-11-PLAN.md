---
phase: 41-midia-de-campanha-mobile
plan: 11
type: execute
wave: 5
depends_on: [41-03, 41-05]
files_modified:
  - src/lib/image-generation/providers/__tests__/openai-provider.test.ts
  - src/lib/image-generation/services/__tests__/image-generation-service.test.ts
  - src/lib/image-generation/services/__tests__/image-review-service.test.ts
  - src/lib/campaign/__tests__/prompt-reframe.test.ts
autonomous: true
requirements: [F41-25]

must_haves:
  truths:
    - "Testes 17-23 (pipeline/provider/review/prompt) implementados: N input_image (17), fallback gated (18), legado 1 imagem (19), golden 38 keys com multi (20), bloco 1+N nos 4 prompts (21), primary-only validation (22), review com primary (23)"
    - "openai-provider.test.ts co-migrado: casos productImagesDataUrls + fallback gated"
    - "image-generation-service.test.ts co-migrado: mediaImagesDataUrls + provider input com lista + golden 38 preservado"
    - "image-review-service.test.ts co-migrado: review com primary + retrocompatível"
    - "prompt-reframe.test.ts ganha o caso do bloco 1+N (teste 21)"
  artifacts:
    - path: "src/lib/image-generation/providers/__tests__/openai-provider.test.ts"
      provides: "Testes 17, 18, 19"
      contains: "productImagesDataUrls"
    - path: "src/lib/image-generation/services/__tests__/image-generation-service.test.ts"
      provides: "Testes 20, 22 + mediaImagesDataUrls"
      contains: "mediaImagesDataUrls"
    - path: "src/lib/image-generation/services/__tests__/image-review-service.test.ts"
      provides: "Teste 23"
      contains: "imagem de referência"
    - path: "src/lib/campaign/__tests__/prompt-reframe.test.ts"
      provides: "Teste 21"
      contains: "1 imagem principal"
  key_links:
    - from: "openai-provider.test.ts"
      to: "openai.ts (N input_image + fallback gated)"
      via: "responses.create content com N blocos; images.edit NÃO chamado com auxiliares"
      pattern: "input_image"
    - from: "image-generation-service.test.ts (golden)"
      to: "EXPECTED_KEYS (38 por intent)"
      via: "mesmo conjunto de keys com multi-imagem (D6)"
      pattern: "toHaveLength(38)"
---

<objective>
Implementar os **testes 17-23 (pipeline/provider/review/prompt)** (D6/D7/D8/D9): provider Responses recebe N `input_image` (17); fallback `images.edit` SÓ com primary única — com auxiliares NÃO usa edit (18); sem primary/legado → caminho atual 1 imagem (19); golden por intent com **mesmo conjunto de 38 keys** com multi-imagem (20); bloco descritivo 1+N presente nos 4 prompts (21); `InputValidationService` usa apenas a primary (22); review recebe a primary como referência — sem primary → comportamento atual (23). Co-migrar fixtures dos 4 arquivos.

Purpose: D6 (golden 38 keys preservado) + D7 (N input_image + fallback gated) + D8 (primary-only) + D9 (review com primary).
Output: testes 17-23 verdes (17-19 no provider, 20/22 no service, 21 no prompt-reframe, 23 no review).
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-midia-de-campanha-mobile/41-CONTEXT.md (D6, D7, D8, D9; F41-25)
@openspec/changes/fase-41-midia-de-campanha-mobile/tasks.md (seção 14 — testes 17-23)
@openspec/changes/fase-41-midia-de-campanha-mobile/specs/generation-retry-fallback/spec.md (fallback gated)
@openspec/changes/fase-41-midia-de-campanha-mobile/specs/validation-review-alignment/spec.md (primary-only + review com primary)
@openspec/changes/fase-41-midia-de-campanha-mobile/specs/ai-image-generation/spec.md (N input_image + bloco 1+N + golden 38)
@src/lib/image-generation/providers/__tests__/openai-provider.test.ts (LINHAS VERIFICADAS — vi.mock openai :5-19; fallback test :33-48)
@src/lib/image-generation/services/__tests__/image-generation-service.test.ts (LINHAS VERIFICADAS — createMinimalBrief :17-32; EXPECTED_KEYS :516-527; golden :539-606; assert provider :658)
@src/lib/image-generation/services/__tests__/image-review-service.test.ts (LINHAS VERIFICADAS — review input :35-52; chamada review :44)
@src/lib/campaign/__tests__/prompt-reframe.test.ts (LINHAS VERIFICADAS — readPrompt helper :6-7; padrão toContain :37-46)
@src/lib/image-generation/providers/openai.ts (recém-modificado no 41-05)
@src/lib/image-generation/services/image-generation-service.ts (recém-modificado no 41-05)
@src/lib/image-generation/services/image-review-service.ts (recém-modificado no 41-05)
@prompts/*.md (recém-modificados no 41-03)

<interfaces>
Testes a implementar (tasks 14.1-14.7):

| Teste | Arquivo | Descrição |
|-------|---------|-----------|
| 17 | openai-provider.test.ts | `input.productImagesDataUrls: [primary, aux1]` → `responses.create` chamado com content contendo 2 blocos `input_image` (primary + auxiliar); identidade `detail: "low"` quando presente |
| 18 | openai-provider.test.ts | `productImagesDataUrls` com 2+ itens + `attempt >= 1` → `images.edit` NÃO chamado (retry permanece no Responses — mock create lança, edit não é chamado) |
| 19 | openai-provider.test.ts | legado (só `productImageDataUrl`, sem lista) + `attempt >= 1` → caminho atual (fallback permitido — gate: `!productImagesDataUrls && productImageDataUrl`) |
| 20 | image-generation-service.test.ts | golden por intent: mesmo `EXPECTED_KEYS` (38) com brief multi-imagem (productImages 3 itens no `createMinimalBrief`) — `toHaveLength(38)` |
| 21 | prompt-reframe.test.ts | `readPrompt` dos 4 arquivos → cada um `toContain("1 imagem principal")` e `toContain("NÃO invente conteúdo")` |
| 22 | image-generation-service.test.ts | `inputValidation.validate` recebe APENAS a primary (assert do call: `validate(brief.product.name, mediaImagesDataUrls(brief)[0], ...)`) — auxiliares não participam |
| 23 | image-review-service.test.ts | `service.review(genImage, input, primaryDataUrl)` → `callVisionModel`/content com a primary como 2ª imagem + prompt com "Compare o produto da arte com a imagem de referência"; sem 3º arg → comportamento atual |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: openai-provider.test.ts — testes 17, 18, 19 (N input_image + fallback gated)</name>
  <files>src/lib/image-generation/providers/__tests__/openai-provider.test.ts</files>
  <read_first>
    - src/lib/image-generation/providers/__tests__/openai-provider.test.ts (vi.mock openai :5-19; padrão de teste :33-48 — attempt >= 1 com productImageDataUrl → rejects)
    - src/lib/image-generation/providers/openai.ts (recém-modificado no 41-05 — mainline N input_image :63-77; fallback gate :58-61)
    - openspec/changes/fase-41-midia-de-campanha-mobile/specs/generation-retry-fallback/spec.md (cenários de fallback gated)
  </read_first>
  <action>
    Modificar `openai-provider.test.ts` (tasks 14.1-14.3):
    1. **Teste 17**: com `input: { prompt, productImagesDataUrls: ['data:image/png;base64,primary', 'data:image/png;base64,aux1'], identityImageUrl: 'https://...', attempt: 0 }` → mockar `responses.create` para resolver com output contendo `image_generation_call`; assertar que `responses.create` foi chamado com `input[0].content` contendo 2 itens `type: "input_image"` (imagens) + o `identityImageUrl` com `detail: "low"` (se presente). O mock atual do `openai` (vi.mock :5-19) retorna `responses.create: vi.fn()` — configurar o retorno no teste.
     2. **Teste 18**: `productImagesDataUrls` com 2 itens + `attempt: 1` → `images.edit` NÃO chamado (`expect(imagesEdit).not.toHaveBeenCalled()`); com o mock de `responses.create` lançando erro retryable, assertar que o retry permanece no Responses path (a chamada rejeita/erro terminal — sem invocar edit). **REPLICAR o shape de produção (CRÍTICO — D7 gate 2 pós-erro `openai.ts:177-184`):** passar **AMBOS** `productImagesDataUrls: [primary, aux]` E `productImageDataUrl: primary` (o service mantém ambos — 41-05 Task 2 step 3) com `responses.create` lançando um erro `isResponsesApiError` (ex.: `{ code: 'model_not_found' }` ou `'unsupported model'`) → assertar que `images.edit` NÃO é chamado E o erro propaga (rejeita) — cobre o gate pós-erro que descartaria as auxiliares se não-gated.
     3. **Teste 19**: legado `{ prompt, productImageDataUrl: 'data:image/png;base64,abc', attempt: 1 }` (SEM `productImagesDataUrls`) → o fallback `images.edit` É permitido (gate `isSinglePrimary` → `!productImagesDataUrls && productImageDataUrl` → true) — assertar `images.edit` chamado OU o comportamento atual do teste :33-48 (rejects — o mock do edit não implementa retorno).
     3b. **Teste 19b (novo single-image cai no fallback — D7 coerente)**: `{ prompt, productImagesDataUrls: ['data:image/png;base64,abc'], attempt: 1 }` (SEM `productImageDataUrl`) → gate `isSinglePrimary` → true (lista de 1 elemento) → fallback permitido; **assertar que `fallbackToImageApi` NÃO lança "Invalid productImageDataUrl"** (o fallback resolve a primary via `input.productImageDataUrl ?? input.productImagesDataUrls?.[0]` — o `dataUrlMatch` usa o valor resolvido; o mock do edit, se não implementar retorno, rejeita com erro de outro tipo, não o "Invalid productImageDataUrl").
     4. MANTER o teste existente (:33-48) — ele já cobre o caminho legado (regressão).
  </action>
  <verify>
    <automated>
      $o = Select-String -LiteralPath "src\lib\image-generation\providers\__tests__\openai-provider.test.ts" -Pattern "productImagesDataUrls|not.toHaveBeenCalled|input_image" | Measure-Object; if ($o.Count -lt 3) { Write-Error "Provider tests incompletos" } else { Write-Output "OK: provider tests 17-19" }; npx vitest run src/lib/image-generation/providers/__tests__/openai-provider.test.ts 2>$null
    </automated>
  </verify>
  <acceptance_criteria>
    - Grep `productImagesDataUrls` em openai-provider.test.ts → ≥ 2 (testes 17/18)
    - Teste 18 asserta `images.edit` NÃO chamado com auxiliares (`not.toHaveBeenCalled`) — **inclui o caso pós-erro `isResponsesApiError` com AMBOS os campos (`productImagesDataUrls` + `productImageDataUrl`) → erro propaga (gate 2 D7)**
    - Teste 19 cobre o legado (productImageDataUrl sem lista)
    - Teste 19b: `productImagesDataUrls: [primary]` sem `productImageDataUrl` → fallback permitido e NÃO lança "Invalid productImageDataUrl" (resolução da primary no fallback)
    - `npx vitest run src/lib/image-generation/providers/__tests__/openai-provider.test.ts` → todos verdes (novos + :33-48 existente)
  </acceptance_criteria>
  <done>Testes 17-19 verdes (N input_image, fallback gated, legado, novo single-image no fallback)</done>
</task>

<task type="auto">
  <name>Task 2: image-generation-service.test.ts — testes 20 (golden 38) e 22 (primary-only) + co-migração mediaImagesDataUrls</name>
  <files>src/lib/image-generation/services/__tests__/image-generation-service.test.ts</files>
  <read_first>
    - src/lib/image-generation/services/__tests__/image-generation-service.test.ts (createMinimalBrief :17-32; EXPECTED_KEYS :516-527; golden :539-606; assert provider `objectContaining({ productImageDataUrl })` :658)
    - src/lib/image-generation/services/image-generation-service.ts (recém-modificado no 41-05 — mediaImagesDataUrls :983; generateWithRetry :1041-1048)
  </read_first>
  <action>
    Modificar `image-generation-service.test.ts` (tasks 14.4, 14.6 + 15.4):
    1. **Teste 20** (golden 38 com multi): estender `createMinimalBrief` (ou criar variante) para aceitar `productImages` (3 itens: 1 primary + 2 reference com `dataUrl`). Rodar `buildPromptVariables` para offer/spotlight/exclusive → `Object.keys(vars)` com `toHaveLength(38)` e `toEqual([...EXPECTED_KEYS].sort())` — o conjunto de variáveis NÃO muda com multi-imagem (D6).
    2. **Teste 22** (primary-only): no teste de geração, assertar que `inputValidation.validate` é chamado com `(brief.product.name, <dataUrl da primary>, ...)` — a primary é `mediaImagesDataUrls(brief)[0]`; auxiliares NÃO aparecem na chamada. Usar o mock do `inputValidation` no service.
    3. **Co-migração**: o assert existente `objectContaining({ productImageDataUrl })` (:658) — MANTER (o provider input ainda carrega `productImageDataUrl` legado + agora `productImagesDataUrls`). Adicionar assert de `productImagesDataUrls: expect.arrayContaining([primaryDataUrl])` quando o brief tem N imagens.
    4. NÃO alterar `EXPECTED_KEYS` (:516-527) — 38 keys preservadas (D6).
  </action>
  <verify>
    <automated>
      $s = Select-String -LiteralPath "src\lib\image-generation\services\__tests__\image-generation-service.test.ts" -Pattern "mediaImagesDataUrls|productImagesDataUrls|toHaveLength\(38\)" | Measure-Object; if ($s.Count -lt 3) { Write-Error "Service tests incompletos" } else { Write-Output "OK: service tests 20/22" }; npx vitest run src/lib/image-generation/services/__tests__/image-generation-service.test.ts 2>$null
    </automated>
  </verify>
  <acceptance_criteria>
    - Teste 20: golden com multi-imagem → `toHaveLength(38)` + `toEqual([...EXPECTED_KEYS].sort())` (offer/spotlight/exclusive)
    - Teste 22: `inputValidation.validate` chamado com a primary apenas (assert do 1º/2º argumento)
    - Assert `productImagesDataUrls` no provider input quando brief multi
    - `npx vitest run src/lib/image-generation/services/__tests__/image-generation-service.test.ts` → todos verdes (novos + golden existentes + :658)
  </acceptance_criteria>
  <done>Testes 20 e 22 verdes; co-migração mediaImagesDataUrls completa</done>
</task>

<task type="auto">
  <name>Task 3: image-review-service.test.ts (23) + prompt-reframe.test.ts (21)</name>
  <files>src/lib/image-generation/services/__tests__/image-review-service.test.ts, src/lib/campaign/__tests__/prompt-reframe.test.ts</files>
  <read_first>
    - src/lib/image-generation/services/__tests__/image-review-service.test.ts (setup :18-33; padrão de chamada review :44)
    - src/lib/image-generation/services/image-review-service.ts (recém-modificado no 41-05 — review com primary)
    - src/lib/campaign/__tests__/prompt-reframe.test.ts (readPrompt helper :6-7; padrão toContain :37-46)
    - prompts/*.md (recém-modificados no 41-03)
  </read_first>
  <action>
    Modificar 2 arquivos (tasks 14.5, 14.7):
    1. **image-review-service.test.ts — Teste 23**: `await service.review('data:image/jpeg;base64,gen', input, 'data:image/jpeg;base64,primary')` → assertar que o content enviado ao `callVisionModel` (mockado via `vi.spyOn(service as any, 'callVisionModel')` :29-32) contém a primary como 2ª imagem (ou o prompt carregado contém "Compare o produto da arte com a imagem de referência" — conforme a implementação do 41-05). Sem 3º arg → comportamento atual (nenhum teste existente quebra — :44 e demais).
    2. **prompt-reframe.test.ts — Teste 21**: adicionar `it("F41-21: bloco descritivo 1+N presente nos 4 prompts", () => { for (const name of ["campaign-image-director.md", "campaign-image-director-offer.md", "campaign-image-director-spotlight.md", "campaign-image-director-exclusive.md"]) { const prompt = readPrompt(name); expect(prompt, name).toContain("1 imagem principal"); expect(prompt, name).toContain("NÃO invente conteúdo"); } })` — usar o `readPrompt` existente (:6-7).
    3. MANTER os asserts existentes do prompt-reframe (bloco condicional do aviso :37-38, validade :43-46, constante :50-54) — aditivo.
  </action>
  <verify>
    <automated>
      npx vitest run src/lib/image-generation/services/__tests__/image-review-service.test.ts src/lib/campaign/__tests__/prompt-reframe.test.ts 2>$null
    </automated>
  </verify>
  <acceptance_criteria>
    - Teste 23: review com primary → assert da primary no content/prompt; sem primary → comportamento atual (suíte inteira do review verde)
    - Teste 21: 4 prompts com `1 imagem principal` e `NÃO invente conteúdo` (grep no arquivo de teste → ≥ 4 ocorrências de `1 imagem principal` via loop)
    - `npx vitest run` nos 2 arquivos → todos verdes
  </acceptance_criteria>
  <done>Testes 21 e 23 verdes (bloco 1+N nos prompts + review com primary retrocompatível)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service → provider | N dataUrls atravessam o mock do provider (teste) |
| service → review | Primary como referência de fidelidade (teste) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-11 | Tampering | Fallback gated no teste 18 | mitigate | Assert `images.edit` NÃO chamado com auxiliares — ancora a política D7 (não degrada fidelidade) |
| T-41-11a | Tampering | Golden 38 no teste 20 | mitigate | `toHaveLength(38)` + `toEqual([...EXPECTED_KEYS].sort())` com multi-imagem — ancora D6 (sem variável nova) |
| T-41-11b | Spoofing | Primary-only no teste 22 | mitigate | Assert da chamada `inputValidation.validate` com APENAS a primary — ancora D8 |
| T-41-11c | Tampering | Prompt content no teste 21 | mitigate | Assert do bloco 1+N nos 4 arquivos via readFileSync — ancora D6 (bloco hardcoded presente) |
| T-41-11d | Spoofing | Review com primary no teste 23 | mitigate | Assert da primary como referência + retrocompatibilidade (sem 3º arg) — ancora D9 |
| T-41-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo — sem gate de legitimidade |
</threat_model>

<verification>
- `npx vitest run src/lib/image-generation/providers/__tests__/openai-provider.test.ts` — verdes
- `npx vitest run src/lib/image-generation/services/__tests__/image-generation-service.test.ts` — verdes (golden 38 + primary-only)
- `npx vitest run src/lib/image-generation/services/__tests__/image-review-service.test.ts` — verdes
- `npx vitest run src/lib/campaign/__tests__/prompt-reframe.test.ts` — verdes (bloco 1+N)
- `npm run typecheck` limpo
</verification>

<success_criteria>
- Testes 17-19 (provider), 20/22 (service), 23 (review), 21 (prompts) implementados e verdes
- Co-migrações de fixtures completas (mediaImagesDataUrls, productImagesDataUrls)
- Golden EXPECTED_KEYS = 38 preservado por intent (D6)
</success_criteria>

<output>
Create `.planning/phases/41-midia-de-campanha-mobile/41-11-SUMMARY.md` when done
</output>

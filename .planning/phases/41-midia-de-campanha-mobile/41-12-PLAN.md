---
phase: 41-midia-de-campanha-mobile
plan: 12
type: execute
wave: 4
depends_on: [41-02, 41-04, 41-06]
files_modified:
  - src/app/api/campaign/generate-image/__tests__/route.test.ts
autonomous: true
requirements: [F41-25]

must_haves:
  truths:
    - "route.test.ts co-migrado: fixtures productImages[] + legado + teto + erros 400/413 + storage (upload de inputs/campaignId pré-gerado)"
    - "Teste 4: productImages + productImageDataUrl juntos → 400 ambíguo (rota)"
    - "Testes 24-27: teto agregado/por item → 413 (24), upload pré-snapshot com campaignId/imageId gerados pela rota + storagePath no snapshot (25), limpeza pré-stream remove inputs (26), ausência de productImageDataUrl → 400 da rota (27 — co-migração do teste antigo)"
    - "Regressão do fluxo legado de 1 imagem (crédito, rate limit, clearance, readiness, stream, telemetria, estorno) inalterada"
  artifacts:
    - path: "src/app/api/campaign/generate-image/__tests__/route.test.ts"
      provides: "Testes 4, 24-27 + fixtures co-migradas"
      contains: "productImages"
  key_links:
    - from: "route.test.ts (setupSuccessMocks :204-259)"
      to: "persistence.ts (uploadCampaignInputImage/removeCampaignInputs)"
      via: "mocks dos helpers novos + storagePath"
      pattern: "uploadCampaignInputImage"
    - from: "route.test.ts (teste 27 :316-327)"
      to: "route.ts (regra de exclusividade)"
      via: "400 da rota (não mais erro do Zod)"
      pattern: "Imagem do produto é obrigatória"
---

<objective>
Co-migrar **`route.test.ts`** e implementar os **testes 4, 24-27** (D2/D5/D10): payload ambíguo → 400 (4); teto agregado e item individual → 413 (24); upload de inputs pré-snapshot com `campaignId`/`imageId` gerados pela rota + `storagePath` no snapshot (25); limpeza pré-stream remove inputs já enviados (26); ausência de `productImageDataUrl` → **400 da rota** (27 — co-migração do teste antigo que esperava erro do Zod). Regressão do fluxo legado de 1 imagem (15.9) e de `createCampaign` com/sem `campaignId` (15.10).

Purpose: D2 (exclusividade/400) + D5 (storage/upload/cleanup) + D10 (413) — fechar a cobertura da rota multi-imagem.
Output: route.test.ts completo (testes 4, 24-27 + fixtures + regressão).
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-midia-de-campanha-mobile/41-CONTEXT.md (D2, D5, D10; F41-25)
@openspec/changes/fase-41-midia-de-campanha-mobile/tasks.md (seção 12 — 12.4; seção 14 — 14.8 a 14.11; seção 15 — 15.1, 15.9, 15.10, 15.11)
@openspec/changes/fase-41-midia-de-campanha-mobile/specs/transactional-pipeline/spec.md (cenários 400/413 + upload D5)
@openspec/changes/fase-41-midia-de-campanha-mobile/specs/campaign-persistence-service/spec.md (createCampaign campaignId + removeCampaignInputs)
@src/app/api/campaign/generate-image/__tests__/route.test.ts (LINHAS VERIFICADAS — VALID_REQUEST_BODY :188-194; makeRequest :196-202; setupSuccessMocks :204-259; uploadCampaignImage mock :257; teste "rejects missing productImageDataUrl with 400" :316-327)
@src/app/api/campaign/generate-image/route.ts (recém-modificado no 41-06)
@src/lib/campaign/persistence.ts (recém-modificado no 41-04)

<interfaces>
Testes a implementar (tasks 12.4 + 14.8-14.11):

| Teste | Descrição | Assert |
|-------|-----------|--------|
| 4 | `productImages` + `productImageDataUrl` juntos | 400 + `payload ambíguo` na mensagem (regra de exclusividade — D2) |
| 24a | item individual > 4MB | 413 + mensagem PT-BR indicando o item |
| 24b | soma dos dataUrls > teto agregado | 413 + mensagem PT-BR indicando o total |
| 25 | upload pré-snapshot: campaignId/imageId gerados pela rota | `uploadCampaignInputImage` chamado com `(storeId, campaignIdPre, imageId, {...})`; `createCampaign` chamado com 3º arg = campaignIdPre; snapshot do input contém `storagePath` por imagem |
| 26 | falha pré-stream remove inputs | `uploadCampaignInputImage` falha no 2º item → `removeCampaignInputs(storeId, campaignIdPre)` chamado (sem órfãos) |
| 27 | ausência de `productImageDataUrl` (e de productImages) | 400 da ROTA com "Imagem do produto é obrigatória" (co-migração do teste :316-327 — agora a regra de exclusividade, não o Zod) |

Fixtures: `VALID_REQUEST_BODY` (:188-194) mantém o legado `productImageDataUrl`; adicionar `VALID_PRODUCT_IMAGES_REQUEST` (com `productImages` de 2-3 itens, sem `productImageDataUrl`). Mocks: `uploadCampaignInputImage`/`removeCampaignInputs` adicionados ao `vi.mock('@/lib/campaign/persistence')` (:29-36) como `vi.fn()` + resolvidos em `setupSuccessMocks` (:204-259).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: route.test.ts — fixtures + mocks dos helpers novos + testes 4, 24 (400/413)</name>
  <files>src/app/api/campaign/generate-image/__tests__/route.test.ts</files>
  <read_first>
    - src/app/api/campaign/generate-image/__tests__/route.test.ts (vi.mock persistence :29-36; VALID_REQUEST_BODY :188-194; makeRequest :196-202; setupSuccessMocks :204-259; teste 400 :316-327)
    - src/app/api/campaign/generate-image/route.ts (recém-modificado no 41-06 — guards de exclusividade/limites)
    - openspec/changes/fase-41-midia-de-campanha-mobile/specs/transactional-pipeline/spec.md (cenários 400/413)
  </read_first>
  <action>
    Modificar `route.test.ts` (tasks 12.4, 14.8 + fixtures 15.1):
    1. **Mock de persistence** (:29-36): adicionar `uploadCampaignInputImage: vi.fn(),` e `removeCampaignInputs: vi.fn(),` ao `vi.mock('@/lib/campaign/persistence')`.
    2. **Fixture novo**: `const VALID_PRODUCT_IMAGES_REQUEST = { ...VALID_REQUEST_BODY, productImageDataUrl: undefined, productImages: [{ role: "primary", source: "camera", mimeType: "image/jpeg", dataUrl: "data:image/jpeg;base64,primary" }, { role: "reference", source: "upload", mimeType: "image/png", dataUrl: "data:image/png;base64,aux1" }, { role: "reference", source: "upload", mimeType: "image/webp", dataUrl: "data:image/webp;base64,aux2" }] };` (validar com o schema — 1 primary + 2 reference).
    3. **setupSuccessMocks** (:204-259): adicionar `(uploadCampaignInputImage as any).mockResolvedValue({ storagePath: "store-1/camp-1/inputs/img-1.jpg" });` e `(removeCampaignInputs as any).mockResolvedValue(undefined);` (mesmo estilo de `uploadCampaignImage` :257).
    4. **Teste 4** (payload ambíguo): `makeRequest({ ...VALID_REQUEST_BODY, productImages: [{ role: "primary", source: "upload", mimeType: "image/jpeg", dataUrl: "data:image/jpeg;base64,x" }] })` → `POST` → 400 + body com mensagem contendo `payload ambíguo` (assert no `error.message` — padrão do teste :316-327).
    5. **Teste 24a** (item > 4MB): `makeRequest({ ...VALID_PRODUCT_IMAGES_REQUEST, productImages: [{ role: "primary", source: "upload", mimeType: "image/jpeg", dataUrl: "data:image/jpeg;base64," + "a".repeat(5 * 1024 * 1024) }] })` → 413 (o mock de config :43-47 define `MAX_PRODUCT_IMAGE_BASE64_SIZE: 5 * 1024 * 1024` — usar tamanho acima do mock).
     6. **Teste 24b** (teto agregado): 3 itens cuja soma de dataUrls excede o teto agregado (o mock de config não define `MAX_PRODUCT_IMAGES_AGGREGATE_BASE64_SIZE` — adicioná-lo ao `vi.mock('@/lib/image-generation/config')` :43-47 com valor pequeno, ex. `2 * 1024 * 1024`) → 413. **Adicionar `MAX_CAMPAIGN_IMAGES: 4` ao mock de config** (fidelity do cap — sem isso `z.array().max(undefined)` desativa o teto de imagens do schema nas suítes de rota).
     7. MANTER `VALID_REQUEST_BODY` legado intacto (:188-194).
     8. **Teste 24c** (cap do schema end-to-end — D10): `makeRequest({ ...VALID_REQUEST_BODY, productImages: [1 primary + 4 reference (5 itens)] })` → 400 (erro do schema `z.array().max(MAX_CAMPAIGN_IMAGES)` — o cap de 4 do transporte exercitado no contexto da rota; o mock de config :43-47 define `MAX_CAMPAIGN_IMAGES: 4`).
  </action>
  <verify>
    <automated>
      $r = Select-String -LiteralPath "src\app\api\campaign\generate-image\__tests__\route.test.ts" -Pattern "uploadCampaignInputImage|removeCampaignInputs|VALID_PRODUCT_IMAGES_REQUEST|payload ambíguo|413" | Measure-Object; if ($r.Count -lt 5) { Write-Error "Route fixtures/testes 4/24 incompletos" } else { Write-Output "OK: route fixtures + testes 4/24" }; npx vitest run src/app/api/campaign/generate-image/__tests__/route.test.ts 2>$null
    </automated>
  </verify>
  <acceptance_criteria>
    - Grep `uploadCampaignInputImage` em route.test.ts → ≥ 2 (mock + setup)
    - Grep `VALID_PRODUCT_IMAGES_REQUEST` → 1 (fixture novo)
    - Teste 4: 400 + `payload ambíguo` → 1 assert
    - Testes 24a/24b: 413 → 2 asserts
    - Teste 24c: 5 itens (acima do cap) → 400 do schema (`MAX_CAMPAIGN_IMAGES` no mock de config) → 1 assert
    - `npx vitest run src/app/api/campaign/generate-image/__tests__/route.test.ts` — testes existentes + novos verdes
  </acceptance_criteria>
  <done>Fixtures productImages + mocks helpers + testes 4 e 24 (400/413) verdes</done>
</task>

<task type="auto">
  <name>Task 2: route.test.ts — testes 25, 26 (storage D5) + teste 27 (400 da rota) + regressão 15.9/15.10</name>
  <files>src/app/api/campaign/generate-image/__tests__/route.test.ts</files>
  <read_first>
    - src/app/api/campaign/generate-image/route.ts (recém-modificado no 41-06 — ordem D5: campaignIdPre, uploads, snapshot, createCampaign com id, cleanup)
    - src/lib/campaign/persistence.ts (recém-modificado no 41-04)
    - src/app/api/campaign/generate-image/__tests__/route.test.ts (teste "rejects missing productImageDataUrl with 400" :316-327 — a co-migrar)
  </read_first>
  <action>
    Modificar `route.test.ts` (tasks 14.9-14.11 + 15.9/15.10):
    1. **Teste 25** (upload pré-snapshot com ids da rota): `POST` com `VALID_PRODUCT_IMAGES_REQUEST` → assertar:
       - `uploadCampaignInputImage` chamado 3 vezes com `(storeId, expect.any(String), expect.any(String), expect.objectContaining({ mimeType: "image/jpeg" }))` — os 2 primeiros args são `storeId` e o `campaignIdPre` (uuid gerado pela rota); `imageId` é uuid (gerado pela rota, não do cliente).
       - `createCampaign` chamado com `(storeId, expect.objectContaining({ inputSnapshot: expect.objectContaining({ media: expect.objectContaining({ images: expect.arrayContaining([expect.objectContaining({ storagePath: expect.stringContaining("/inputs/") })]) }) }) }), expect.any(String))` — 3º arg = campaignIdPre; snapshot com storagePath.
       - O `campaignId` retornado = o mesmo pré-gerado (assert `campaign.id`).
    2. **Teste 26** (limpeza pré-stream): fazer `uploadCampaignInputImage` falhar no 2º item (`mockRejectedValueOnce` no 2º call) → `POST` → resposta 500 (ou erro esperado) + `removeCampaignInputs(storeId, campaignIdPre)` chamado (assert do call com `expect.any(String)` no campaignId). Confirmar que a campanha NÃO foi criada com status ready (ou que o fluxo de compensação rodou).
    2b. **Teste 26b (primary fora da posição 0 — D8/D2)**: `POST` com `productImages` em ordem `[reference, primary]` (auxiliar primeiro, primary depois — o contrato aceita pois `role` é explícito e o zod valida exatamente 1 primary; NÃO assume posição 0) → assertar que `inputValidation.validate` foi chamado com o dataUrl da imagem `role: "primary"` (o `.find(role === "primary")` da rota — NÃO o `[0]`), e que a geração segue o fluxo normal (upload dos inputs + snapshot com storagePath).
    3. **Teste 27** (co-migração): o teste existente :316-327 ("rejects missing productImageDataUrl with 400") — **manter o assert de 400 + mensagem**; o 400 agora vem da **regra de exclusividade da rota** (ambos ausentes), não do Zod. Se o teste existente esperava `code: "invalid_data"` ou erro de Zod, ajustar o assert para a resposta de exclusividade da rota (`{ error: { message: "Imagem do produto é obrigatória..." } }` com 400). Renomear para deixar claro que é o caso "sem productImages nem productImageDataUrl".
    4. **Regressão 15.9**: o fluxo legado (VALID_REQUEST_BODY com `productImageDataUrl`) — os testes existentes de sucesso (crédito, rate limit, clearance, readiness, stream, telemetria, estorno) continuam verdes. Rodar a suíte inteira do arquivo.
    5. **Regressão 15.10**: o 41-06 decidiu D5 para **AMBOS os fluxos** (decisão do usuário 2026-08-14) — o legado também usa `createCampaign` com o 3º arg (`campaignIdPre`). **Assertar incondicionalmente**: `createCampaign` chamado com 3º argumento (campaignId) em TODOS os testes de sucesso, tanto o fluxo multi-imagem quanto o legado de 1 imagem. Sem hedge.
  </action>
  <verify>
    <automated>
      $r = Select-String -LiteralPath "src\app\api\campaign\generate-image\__tests__\route.test.ts" -Pattern "mockRejectedValueOnce|removeCampaignInputs|storagePath|inputs/" | Measure-Object; if ($r.Count -lt 3) { Write-Error "Testes storage/cleanup incompletos" } else { Write-Output "OK: testes 25/26 + storage" }; npx vitest run src/app/api/campaign/generate-image/__tests__/route.test.ts 2>$null
    </automated>
  </verify>
  <acceptance_criteria>
    - Teste 25: `uploadCampaignInputImage` chamado 3× + `createCampaign` com 3º arg + snapshot com `storagePath` contendo `/inputs/`
    - Teste 26: falha no 2º upload → `removeCampaignInputs` chamado (cleanup pré-stream)
    - Teste 26b: `productImages` com `[reference, primary]` (primary fora da posição 0) → `inputValidation.validate` recebe o dataUrl da imagem `role: "primary"`; geração segue normal (upload + snapshot com storagePath)
    - Teste 27: ausência de ambos → 400 da rota (mensagem de exclusividade)
    - Regressão 15.9: suíte inteira de `route.test.ts` verde (fluxo legado completo)
    - `npm run typecheck` limpo
  </acceptance_criteria>
  <done>Testes 25, 26, 26b, 27 verdes + regressão do fluxo legado (15.9/15.10) preservada</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente → rota | Payloads de teste (productImages/legado) atravessam os guards da rota |
| rota → storage | Mocks de upload/cleanup (sem storage real) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-12 | Spoofing | Payload ambíguo (teste 4) | mitigate | Assert 400 + "payload ambíguo" — ancora a regra de exclusividade D2 (mutuamente exclusivos) |
| T-41-12a | DoS | Teto/limites (teste 24) | mitigate | Asserts 413 por item e agregado — ancora D10 (payload do Vercel e custo do modelo contidos) |
| T-41-12b | Tampering | IDs gerados pela rota (teste 25) | mitigate | Assert de campaignId/imageId como `expect.any(String)` (uuid da rota) — cliente nunca envia id (D2/D5) |
| T-41-12c | DoS | Inputs órfãos (teste 26) | mitigate | Assert `removeCampaignInputs` em falha pré-stream — sem órfãos (D5) |
| T-41-12d | Tampering | 400 da rota (teste 27) | mitigate | Co-migração do teste antigo: obrigatoriedade garantida pela rota, não pelo Zod (D2 — preservação comportamental) |
| T-41-12e | Information Disclosure | Snapshots nos asserts | accept | Fixtures com dataUrl fictício (base64 curto) — sem dado real |
| T-41-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo — sem gate de legitimidade |
</threat_model>

<verification>
- `npx vitest run src/app/api/campaign/generate-image/__tests__/route.test.ts` — suíte completa verde (testes existentes + 4, 24-27)
- `npm run typecheck` limpo
</verification>

<success_criteria>
- Fixtures `productImages` co-migrados + mocks de `uploadCampaignInputImage`/`removeCampaignInputs`
- Testes 4 (400 ambíguo), 24 (413), 25 (storage D5), 26 (cleanup), 27 (400 da rota) verdes
- Regressão do fluxo legado de 1 imagem (15.9) e de `createCampaign` (15.10) preservada
</success_criteria>

<output>
Create `.planning/phases/41-midia-de-campanha-mobile/41-12-SUMMARY.md` when done
</output>

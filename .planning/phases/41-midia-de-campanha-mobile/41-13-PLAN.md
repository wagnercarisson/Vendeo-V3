---
phase: 41-midia-de-campanha-mobile
plan: 13
type: execute
wave: 6
depends_on: [41-01, 41-02, 41-03, 41-04, 41-05, 41-06, 41-07, 41-08, 41-09, 41-10, 41-11, 41-12]
files_modified:
  - .planning/phases/41-midia-de-campanha-mobile/41-VERIFICATION.md
  - .planning/phases/41-midia-de-campanha-mobile/41-UAT.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [F41-27]

must_haves:
  truths:
    - "Os 4 gates passam: npx vitest run, npm run typecheck, npm run lint, npm run build — zero erros"
    - "41-VERIFICATION.md documenta a verificação (gates + evidências por plano)"
    - "41-UAT.md documenta o roteiro UAT (6 cenários locais + celular real obrigatório)"
    - "UAT humano aprovado pelo usuário (checkpoint de fim de fase)"
  artifacts:
    - path: ".planning/phases/41-midia-de-campanha-mobile/41-VERIFICATION.md"
      provides: "Verificação final da fase"
      contains: "1997"
    - path: ".planning/phases/41-midia-de-campanha-mobile/41-UAT.md"
      provides: "Roteiro UAT com celular real obrigatório"
      contains: "celular"
  key_links:
    - from: "41-VERIFICATION.md"
      to: "41-UAT.md"
      via: "Gates verdes → UAT humana"
      pattern: "UAT"
---

<objective>
Verificação final da **Fase 41 (Mídia de Campanha Mobile)**: rodar os 4 gates (`npx vitest run`, `npm run typecheck`, `npm run lint`, `npm run build` — zero erros), criar `41-VERIFICATION.md` com as evidências por plano, criar `41-UAT.md` com o roteiro de UAT (6 cenários locais + **UAT celular real obrigatória** — D4) e submeter ao **checkpoint humano** de aprovação.

Purpose: F41-27 — fechamento da fase com verificação automatizada + validação humana (precedente F40-09).
Output: 41-VERIFICATION.md + 41-UAT.md + gates verdes + aprovação humana.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-midia-de-campanha-mobile/41-CONTEXT.md (F41-27; UAT obrigatória — Specifics)
@.planning/phases/40-campos-comerciais-avisos-brief/40-09-SUMMARY.md (precedente de verificação final F40)
@.planning/phases/40-campos-comerciais-avisos-brief/40-VERIFICATION.md (precedente de estrutura de VERIFICATION)
@.planning/phases/40-campos-comerciais-avisos-brief/40-UAT.md (precedente de estrutura de UAT)
@openspec/changes/fase-41-midia-de-campanha-mobile/tasks.md (seção 16 — 16.1 a 16.11)
@.planning/ROADMAP.md (seção "### Phase 41" — atualizar Plans/Status)

<interfaces>
Roteiro UAT (tasks 16.5-16.11):
1. Legado 1 imagem → **comportamento idêntico ao pós-F40** (mesma UX/payload/geração/revisão/exportação); snapshot preserva o shape sem base64 e ganha **`storagePath` aditivo para a primary persistida** (D5 nos dois fluxos — decisão do usuário 2026-08-14; `storagePath` ausente só em compatibilidade/legado histórico: campanhas pré-F41 ou caminhos excepcionais sem upload)
2. Primary + 2 auxiliares (galeria) → `media.images[]` com 3 itens (roles/source/mimeType corretos); arte com herói = primary (D2/D3/D6)
3. **Câmera no celular real** (foto nova) — HEIC/EXIF ok, arte correta (D4) — foto vertical/horizontal, iOS e Android
4. Remover/adicionar auxiliares e regenerar — preview e payload consistentes (D3)
5. Upload sem primary válida → erro claro (400 na rota) (D2)
6. Campanha antiga (pré-F41) continua exibindo/baixando normalmente (D5 — sem migração destrutiva, sem storagePath retroativo)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rodar os 4 gates e registrar evidências</name>
  <files>.planning/phases/41-midia-de-campanha-mobile/41-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/40-campos-comerciais-avisos-brief/40-VERIFICATION.md (estrutura: tabela por plano + gates)
  </read_first>
  <action>
    1. Rodar sequencialmente: `npx vitest run` (suíte completa — base 1997 testes da F40 + novos F41 ~32+), `npm run typecheck`, `npm run lint`, `npm run build`.
    2. Registrar TODOS os resultados em `41-VERIFICATION.md`: contagem de testes (arquivos + testes), saída dos 4 gates (zero erros), e uma tabela por plano (41-01..41-13) com a evidência principal (ex.: grep do 41-01, testes verdes dos 41-09..41-12, etc.).
    3. Se QUALQUER gate falhar: corrigir o problema identificado (com o padrão dos planos executados) e re-rodar até verde. NÃO marcar a fase como verificada com gates vermelhos.
    4. Registrar a contagem final de testes na linha "Testes" do STATE.md (próximo passo — a atualização do tracking fica no SUMMARY).
  </action>
  <verify>
    <automated>
      npx vitest run --reporter=dot 2>$null | Select-String "Test Files|Tests"; if ($LASTEXITCODE -ne 0) { exit 1 }; npm run typecheck; if ($LASTEXITCODE -ne 0) { exit 1 }; npm run lint; if ($LASTEXITCODE -ne 0) { exit 1 }; npm run build; if ($LASTEXITCODE -ne 0) { exit 1 }; Write-Output "OK: 4 gates verdes"
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` → 0 falhas (todos os arquivos passando)
    - `npm run typecheck` → 0 erros
    - `npm run lint` → 0 erros
    - `npm run build` → build bem-sucedido
    - `41-VERIFICATION.md` existe com: tabela por plano (41-01..41-13), contagem de testes, resultado dos 4 gates
  </acceptance_criteria>
  <done>4 gates verdes + 41-VERIFICATION.md com evidências por plano</done>
</task>

<task type="auto">
  <name>Task 2: Criar 41-UAT.md com o roteiro (6 cenários + celular real)</name>
  <files>.planning/phases/41-midia-de-campanha-mobile/41-UAT.md</files>
  <read_first>
    - .planning/phases/40-campos-comerciais-avisos-brief/40-UAT.md (estrutura: cenários com passos/critérios/resultado)
    - .planning/phases/41-midia-de-campanha-mobile/41-CONTEXT.md (Specifics — UAT obrigatória; D4 — celular real)
  </read_first>
  <action>
    Criar `41-UAT.md` com o roteiro UAT (tasks 16.5-16.11):
    1. Cenário 1 — Legado 1 imagem: gerar campanha com 1 imagem (galeria) → **comportamento idêntico ao pós-F40** (mesma UX/payload/geração/revisão/exportação); snapshot preserva o shape sem base64 e **ganha `storagePath` aditivo para a primary persistida** (D5 nos dois fluxos — decisão do usuário 2026-08-14); `storagePath` ausente só em compatibilidade/legado histórico (campanhas pré-F41 ou caminhos excepcionais sem upload).
    2. Cenário 2 — Primary + 2 auxiliares: gerar com primary + 2 auxiliares (galeria) → `media.images[]` com 3 itens (roles/source/mimeType corretos); arte com herói = primary e contexto das auxiliares (D2/D3/D6).
    3. Cenário 3 — **Câmera no celular real (OBRIGATÓRIO)**: foto nova vertical/horizontal, iOS (HEIC) e Android → orientação EXIF correta e decode HEIC OK, arte correta (D4).
    4. Cenário 4 — Remover/adicionar auxiliares: remover e adicionar auxiliares e regenerar → preview e payload consistentes (D3).
    5. Cenário 5 — Sem primary válida: upload sem primary válida → erro claro (400 na rota) (D2).
    6. Cenário 6 — Campanha antiga: campanha pré-F41 continua exibindo/baixando normalmente (D5 — sem migração destrutiva).
    Para cada cenário: passos exatos (URL, ações), critério de aceite observável, campo "Resultado" (aprovado/reprovado + observações).
  </action>
  <verify>
    <automated>Select-String -LiteralPath ".planning\phases\41-midia-de-campanha-mobile\41-UAT.md" -Pattern "celular|HEIC|EXIF|Android|iOS"</automated>
  </verify>
  <acceptance_criteria>
    - `41-UAT.md` existe com 6 cenários
    - Cenário 3 (celular real) marcado como OBRIGATÓRIO com iOS (HEIC) e Android, foto vertical/horizontal
    - Cada cenário tem passos, critério de aceite e campo de resultado
  </acceptance_criteria>
  <done>41-UAT.md criado com 6 cenários (celular real obrigatório)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT humana — aprovação do usuário (checkpoint de fim de fase)</name>
  <what-built>Fase 41 (Mídia de Campanha Mobile) completa: form multi-imagem com câmera/HEIC/EXIF, transporte productImages[], persistência dos inputs no storage, provider N input_image, prompts 1+N, validação primary-only, revisor com primary, 4 gates verdes.</what-built>
  <how-to-verify>
    1. Executar o roteiro de `41-UAT.md` no ambiente local (dev server).
    2. Cenário 1: gerar campanha com 1 imagem (galeria) → arte gerada normalmente, comportamento idêntico ao pós-F40; snapshot preserva o shape sem base64 e ganha `storagePath` aditivo para a primary persistida (D5 nos dois fluxos).
    3. Cenário 2: gerar com primary + 2 auxiliares (galeria) → preview grid com 3 itens, primary destacada; arte usa a primary como herói.
    4. **Cenário 3 (OBRIGATÓRIO — celular real)**: acessar o form no celular (iOS e Android), tirar foto vertical e horizontal pela câmera (`capture="environment"`) → imagem não sai rotacionada (EXIF ok), HEIC decodifica (JPEG), arte correta.
    5. Cenário 4: remover/adicionar auxiliares e regenerar → grid e payload consistentes.
    6. Cenário 5: remover a única imagem e tentar gerar → erro claro "Imagem do produto é obrigatória" (400).
    7. Cenário 6: abrir uma campanha criada antes da F41 → continua exibindo/baixando normalmente.
  </how-to-verify>
  <resume-signal>Type "approved" ou descreva os ajustes necessários</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ambiente real → storage/IA | UAT gera campanhas reais (consome IA/créditos) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-13 | Tampering | Gates de verificação | mitigate | 4 gates executados sequencialmente (vitest/typecheck/lint/build) — zero erros para marcar a fase verificada; evidências registradas em 41-VERIFICATION.md |
| T-41-13a | Spoofing | UAT celular real | mitigate | Cenário 3 OBRIGATÓRIO com iOS (HEIC) + Android, foto vertical/horizontal — valida EXIF e decode no hardware real (D4); sem esse cenário aprovado a fase não fecha |
| T-41-13b | Information Disclosure | Artefatos de verificação | accept | Documentos internos de tracking — sem dado sensível |
| T-41-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo (fase de verificação) — sem gate de legitimidade |
</threat_model>

<verification>
- `npx vitest run` — 0 falhas (suíte completa: 1997 base + ~32+ novos F41)
- `npm run typecheck` — 0 erros
- `npm run lint` — 0 erros
- `npm run build` — build bem-sucedido
- 41-VERIFICATION.md + 41-UAT.md criados
- UAT humana aprovada (checkpoint)
</verification>

<success_criteria>
- 4 gates verdes (vitest/typecheck/lint/build)
- 41-VERIFICATION.md com evidências por plano
- 41-UAT.md com 6 cenários (celular real obrigatório)
- UAT humano aprovado (6/6 ou com ajustes registrados)
</success_criteria>

<output>
Create `.planning/phases/41-midia-de-campanha-mobile/41-13-SUMMARY.md` when done
</output>

---
phase: 42-signup-controlado-elegibilidade-freemium
plan: 02
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - src/lib/launch-config/config.ts
  - src/lib/launch-config/__tests__/config.test.ts
  - src/app/page.tsx
  - src/app/(auth)/signup/page.tsx
  - src/components/landing/access-request-section.tsx
  - supabase/config.toml
autonomous: true
requirements: ["launch-config (Nova flag publicSignupEnabled)", "signup-page", "login-page"]

must_haves:
  truths:
    - "Nova flag `publicSignupEnabled` no padrÃ£o `envBool` com default FALSE â€” `envBool(\"VENDEO_PUBLIC_SIGNUP_ENABLED\", false)` em `getLaunchConfig()`"
    - "ValidaÃ§Ã£o server-side da flag nas pÃ¡ginas/rotas controladas (landing e `/signup`) â€” nÃ£o sÃ³ no cliente"
    - "Paridade `supabase/config.toml`: minimum_password_length = 8, enable_confirmations = true, [auth.captcha] turnstile, provider Google, enable_manual_linking = false"
  artifacts:
    - path: "src/lib/launch-config/config.ts"
      provides: "publicSignupEnabled no LaunchConfig"
      contains: "VENDEO_PUBLIC_SIGNUP_ENABLED"
    - path: "supabase/config.toml"
      provides: "Paridade de ambiente"
      contains: "minimum_password_length = 8"
  key_links:
    - from: "getLaunchConfig()"
      to: "signup/page.tsx / access-request-section"
      via: "server-side read"
      pattern: "publicSignupEnabled"
---

<objective>
Adicionar a feature flag `publicSignupEnabled` (default **false**) ao `launch-config`, com validaÃ§Ã£o server-side nas superfÃ­cies controladas (landing e `/signup`), e alinhar a paridade de ambiente no `supabase/config.toml` (senha 8, confirmaÃ§Ã£o on, captcha turnstile, provider Google, `enable_manual_linking=false`).

Purpose: D5 (flag controla exposiÃ§Ã£o, nÃ£o criaÃ§Ã£o; `/login` NÃƒO controlado) + D13 (paridade local/preview/produÃ§Ã£o).
Output: flag + parity aplicados e testados.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D5 â€” flag UI/landing; D13 â€” pronto para abrir; especÃ­ficos "kill switch duplo")
@src/lib/launch-config/config.ts (padrÃ£o envBool existente â€” interface LaunchConfig :3-13, envBool :15-21, getLaunchConfig :23-50)
@src/lib/launch-config/__tests__/config.test.ts (testes existentes da flag)
@supabase/config.toml (minimum_password_length = 6 :182, enable_confirmations = false :226, captcha off :213-217)

<interfaces>
- `getLaunchConfig()` retorna objeto com todas as flags; cada flag usa `envBool(key, default)`. Novas flags entram em AMBAS as branches (v15Enabled true/false) â€” ver config.ts:26-50.
- `import "server-only"` no topo do config.ts â€” flag Ã© server-side por definiÃ§Ã£o.
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Adicionar publicSignupEnabled ao LaunchConfig (default false)</name>
  <files>src/lib/launch-config/config.ts, src/lib/launch-config/__tests__/config.test.ts</files>
  <read_first>
    - src/lib/launch-config/config.ts (padrÃ£o existente â€” interface :3-13, envBool :15-21, getLaunchConfig :23-50)
    - src/lib/launch-config/__tests__/config.test.ts (convenÃ§Ãµes de teste existentes)
    - .planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D5 â€” default false, server-side, `/login` NÃƒO controlado)
  </read_first>
  <action>
    Em `src/lib/launch-config/config.ts`:
    1. Adicionar `publicSignupEnabled: boolean;` Ã  interface `LaunchConfig` (apÃ³s `monthlyCreditsMinStoreAgeDays`, :13).
    2. Adicionar `publicSignupEnabled: envBool("VENDEO_PUBLIC_SIGNUP_ENABLED", false)` em **ambas** as branches de `getLaunchConfig()` (branch v15Enabled=false ~:27-37 e branch normal ~:40-50). Default **false**.
    Red e Green: criar/estender `src/lib/launch-config/__tests__/config.test.ts` cobrindo: (a) sem env var â†’ `publicSignupEnabled === false`; (b) `VENDEO_PUBLIC_SIGNUP_ENABLED=true` â†’ `true`; (c) `VENDEO_PUBLIC_SIGNUP_ENABLED=false` â†’ `false`; (d) valor invÃ¡lido (ex. `"sim"`) â†’ default false (envBool :20-21).
  </action>
  <verify>
    <automated>npx vitest run src/lib/launch-config/__tests__/config.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/launch-config/config.ts` contÃ©m `publicSignupEnabled: boolean;` na interface e `envBool("VENDEO_PUBLIC_SIGNUP_ENABLED", false)` nas duas branches de `getLaunchConfig()`
    - `npx vitest run src/lib/launch-config/__tests__/config.test.ts` â†’ 0 falhas (casos default/true/false/invÃ¡lido)
    - `npm run typecheck` â†’ 0 erros
  </acceptance_criteria>
  <done>publicSignupEnabled default false no LaunchConfig + testes verdes</done>
</task>

<task type="execute">
  <name>Task 2: ValidaÃ§Ã£o server-side da flag nas pÃ¡ginas/rotas controladas</name>
  <files>src/app/(auth)/signup/page.tsx, src/app/page.tsx, src/components/landing/access-request-section.tsx</files>
  <read_first>
    - src/app/(auth)/signup/page.tsx (server component â€” adicionar leitura de getLaunchConfig)
    - src/components/landing/access-request-section.tsx (server-compatible; a flag deve chegar via prop do server component pai, ex. src/app/page.tsx)
    - src/lib/launch-config/config.ts (getLaunchConfig Ã© server-only)
    - .planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D5 â€” validaÃ§Ã£o server-side "nÃ£o sÃ³ no cliente")
  </read_first>
  <action>
    - Em `src/app/(auth)/signup/page.tsx` (server component): chamar `const { publicSignupEnabled } = await getLaunchConfig()` no corpo (pode ser `getLaunchConfig()` direto â€” Ã© server-only). Passar `publicSignupEnabled` para a view do signup (flag on â†’ formulÃ¡rio; off â†’ "Beta fechado"). A renderizaÃ§Ã£o condicional Ã© do plan 42-06; neste plan, apenas **estabelecer a leitura server-side** e o fluxo de prop.
    - Em `src/components/landing/access-request-section.tsx`: receber `publicSignupEnabled` como prop `boolean` (default `false` na assinatura). O server component pai da landing (`src/app/page.tsx`) chama `getLaunchConfig()` e passa a prop. NÃƒO importar `getLaunchConfig` em componente client â€” Ã© `server-only`.
    - NÃƒO alterar o conteÃºdo renderizado neste plan (os estados flag on/off sÃ£o dos plans 42-06/42-10); este plan sÃ³ conecta a leitura server-side â†’ props.
  </action>
  <verify>
    <automated>npm run typecheck; if ($?) { npm run lint }</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(auth)/signup/page.tsx` lÃª `getLaunchConfig()` (server-side) e obtÃ©m `publicSignupEnabled`
    - `src/components/landing/access-request-section.tsx` aceita prop `publicSignupEnabled: boolean` (default false) sem importar `server-only`
    - `src/app/page.tsx` (ou equivalente) passa `publicSignupEnabled` ao componente de landing via `getLaunchConfig()`
    - `npm run typecheck` e `npm run lint` â†’ 0 erros
  </acceptance_criteria>
  <done>Flag server-side conectada Ã s superfÃ­cies controladas (renderizaÃ§Ã£o condicional nos plans 42-06/42-10)</done>
</task>

<task type="execute">
  <name>Task 3: Paridade supabase/config.toml (D13)</name>
  <files>supabase/config.toml</files>
  <read_first>
    - supabase/config.toml (auth seÃ§Ã£o â€” minimum_password_length :182, enable_confirmations :226, [auth.captcha] :213-217)
    - .planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D13 â€” paridade; D14 â€” enable_manual_linking = false)
  </read_first>
  <action>
    Em `supabase/config.toml`, alinhar Ã  paridade D13:
    1. `minimum_password_length = 8` (hoje `6`).
    2. `enable_confirmations = true` (hoje `false`).
    3. `[auth.captcha]` â†’ habilitar turnstile (`provider = "turnstile"`, `secret = ""` â€” placeholder para a secret real do Supabase Dashboard; **nunca** commitar secret real) e site_key placeholder.
    4. Provider Google no `[auth.external.google]` com `client_id = ""` e `secret = ""` (placeholders; valores reais via Supabase Dashboard/env).
    5. Confirmar `enable_manual_linking = false` (D14/D16).
    NÃƒO alterar `enable_signup` aqui (configuraÃ§Ã£o do rollout D13 â€” a flag da app controla a exposiÃ§Ã£o). NÃƒO commitar segredos reais. **NOTA (aplicacao local):** mudancas em config.toml so entram em vigor apos reiniciar o Supabase local (supabase stop + supabase start); registrar no SUMMARY e executar antes dos UATs de Turnstile (42-20 20.9/20.10). Se Docker local indisponivel, documentar que a paridade sera exercida no remoto/deploy.
  </action>
  <verify>
    <automated>Select-String -LiteralPath "supabase\config.toml" -Pattern "minimum_password_length = 8|enable_confirmations = true|turnstile" | Select-Object -First 5</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/config.toml` contÃ©m `minimum_password_length = 8`, `enable_confirmations = true`, `provider = "turnstile"` no `[auth.captcha]`, seÃ§Ã£o `[auth.external.google]` (client_id/secret placeholders) e `enable_manual_linking = false`
    - Nenhum segredo real no arquivo (apenas placeholders vazios)
  </acceptance_criteria>
  <done>config.toml em paridade D13 com placeholders (sem segredos)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| flag env â†” cÃ³digo | `VENDEO_PUBLIC_SIGNUP_ENABLED` decide exposiÃ§Ã£o pÃºblica â€” mÃ¡-config abre cadastro sem barreira |
| config.toml â†” projeto | Paridade de ambiente â€” divergÃªncia quebra fluxo em preview/produÃ§Ã£o |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-02 | Elevation of Privilege | publicSignupEnabled | mitigate | Default `false` (fail-closed); leitura server-side; flag off preserva estado atual â€” nunca libera cadastro por ausÃªncia de env |
| T-42-02a | Spoofing | Turnstile secret | mitigate | Secret apenas no Supabase Dashboard/env â€” nunca no config.toml/commit; site key no Vercel |
| T-42-02b | Information Disclosure | Google client_secret | mitigate | Placeholders vazios no config.toml; valores reais via Dashboard/env (D13) |
| T-42-SC | Tampering | InstalaÃ§Ãµes de pacotes | accept | Nenhum pacote novo |
</threat_model>

<verification>
- `npx vitest run src/lib/launch-config/__tests__/config.test.ts` â†’ 0 falhas
- `npm run typecheck` â†’ 0 erros; `npm run lint` â†’ 0 erros
- `supabase/config.toml` contÃ©m `minimum_password_length = 8`, `enable_confirmations = true`, turnstile, `[auth.external.google]`, `enable_manual_linking = false`
</verification>

<success_criteria>
- `publicSignupEnabled` default false no LaunchConfig, com testes cobrindo default/true/false/invÃ¡lido
- Flag lida server-side nas superfÃ­cies controladas (signup page + landing via prop)
- config.toml em paridade D13 (senha 8, confirmaÃ§Ã£o on, captcha turnstile, Google provider, sem manual linking) â€” sem segredos no repo
</success_criteria>

<output>
Create `.planning/phases/42-signup-controlado-elegibilidade-freemium/42-02-SUMMARY.md` when done
</output>

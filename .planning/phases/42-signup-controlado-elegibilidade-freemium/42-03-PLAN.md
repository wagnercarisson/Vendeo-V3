---
phase: 42-signup-controlado-elegibilidade-freemium
plan: 03
type: execute
wave: 1
depends_on: ["42-01"]
files_modified:
  - src/lib/cnpj/cnae-mapping.ts
  - src/lib/cnpj/__tests__/cnae-mapping.test.ts
autonomous: true
requirements: ["cnae-segment-mapping"]

must_haves:
  truths:
    - "`normalizeCnaeSubclasse(raw)` normaliza para 7 dígitos + DV (remove pontuação) e retorna null se inválido"
    - "`deriveCnaeClasse(subclasse)` deriva a classe 4+DV (5 primeiros dígitos)"
    - "Quatro conjuntos por segmento (compatible/incompatible × classes/subclasses) + precedência `negative.subclasses → positive.subclasses → negative.classes → positive.classes → unknown`"
    - "Validação de não-contradição em build/CI (mesmo código nas listas pos+neg = erro)"
    - "CNAE nunca rejeita exclusivamente (incompatible → review, unknown → neutro)"
  artifacts:
    - path: "src/lib/cnpj/cnae-mapping.ts"
      provides: "Módulo determinístico"
      contains: "cnaeCompatibilityFor"
    - path: "src/lib/cnpj/__tests__/cnae-mapping.test.ts"
      provides: "Cobertura D9"
      contains: "cnaeCompatibilityFor"
---

<objective>
Criar o módulo determinístico `src/lib/cnpj/cnae-mapping.ts` (D9): normalização de subclasse (7 dígitos + DV), derivação de classe (4+DV), estrutura de 4 conjuntos por segmento, função `cnaeCompatibilityFor(segment, cnaePrincipal)` com precedência de subclasse exata, e validação de não-contradição em build/CI. Listas reais validadas na CONCLA/IBGE.

Purpose: D9 — compatibilidade segmento × CNAE determinística, versionada em código, nunca-reject por CNAE.
Output: módulo + validação CI + listas validadas.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D9 — CNAE determinístico; precedência; não-contradição; listas CONCLA/IBGE; segmento `outros` vazio)
@src/lib/cnpj/lookup-providers/types.ts (CnpjLookupData — `cnae_principal` string|null :22, `cnae_descricao` :23)
@src/lib/cnpj/mask.ts (padrão de helper puro em lib/cnpj)
@openspec/changes/fase-42-signup-controlado-elegibilidade-freemium/specs/cnae-segment-mapping/spec.md (Requirement — módulo, precedência, não-contradição)

<interfaces>
- Segmentos vêm do enum `stores.segment` (F40): ex. `alimentacao`, `vestuario`, `beleza`, `outros`, etc. — listar os valores reais do enum ao fechar o mapeamento.
- CNAE principal: `officialData.cnae_principal` (formato `"4781-4/00"` — pontuado). `normalizeCnaeSubclasse("4781-4/00")` → `"4781400"`; `deriveCnaeClasse("4781400")` → `"47814"`.
- `segmento "outros"` → conjuntos vazios → sempre `unknown` (nunca penaliza).
- NÃO usar códigos ilustrativos do alinhamento; validar listas na CONCLA/IBGE antes de fechar.
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Módulo cnae-mapping.ts (normalização + derivação + compatibilidade)</name>
  <files>src/lib/cnpj/cnae-mapping.ts, src/lib/cnpj/__tests__/cnae-mapping.test.ts</files>
  <read_first>
    - src/lib/cnpj/lookup-providers/types.ts (CnpjLookupData.cnae_principal :22)
    - src/lib/cnpj/mask.ts (padrão de helper puro)
    - .planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D9)
    - openspec/changes/fase-42-signup-controlado-elegibilidade-freemium/specs/cnae-segment-mapping/spec.md
  </read_first>
  <action>
    Criar `src/lib/cnpj/cnae-mapping.ts` com:
    1. `export type CnaeCompatibility = "compatible" | "incompatible" | "unknown";`
    2. `export type CnaeCodes = { classes: string[]; subclasses: string[] };`
    3. `export type SegmentCnaeMap = Record<string, { compatible: CnaeCodes; incompatible: CnaeCodes }>;`
    4. `normalizeCnaeSubclasse(raw: string): string | null` — remove pontuação (`-`, `/`, espaços), retorna `null` se não resultar em exatamente 7 dígitos.
    5. `deriveCnaeClasse(subclasse: string): string` — retorna os 5 primeiros caracteres (classe 4+DV). Assuma input normalizado (7).
    6. `CNAE_SEGMENT_MAP: SegmentCnaeMap` — para cada segmento do enum `stores.segment`, quatro conjuntos (`compatible.classes`, `compatible.subclasses`, `incompatible.classes`, `incompatible.subclasses`); `outros` com todos vazios. **Códigos validados na CONCLA/IBGE** — NÃO copiar os exemplos ilustrativos do alinhamento (registrar a fonte/validação no SUMMARY).
    7. `cnaeCompatibilityFor(segment: string, cnaePrincipal: string | null): CnaeCompatibility` — ordem: `negative.subclasses → positive.subclasses → negative.classes → positive.classes → unknown`. `null`/inválido/normalização falha → `unknown`.
    8. `assertNoCnaeContradictions(): void` — percorre todos os segmentos e lança `Error` se um código idêntico (classe OU subclasse) aparece nas listas positiva e negativa do mesmo segmento. Exportada para o build/CI (Task 2).
    Red/Green: criar `src/lib/cnpj/__tests__/cnae-mapping.test.ts` cobrindo (tests 37-46 da tasks.md): `"4781-4/00"`→`"4781400"`; classe `"47814"`; compatible; incompatible explícito; granularidade subclasse exata não contamina a classe; precedência subclasse negativa vence classe positiva; validação de não-contradição lança; fora das listas → unknown; null/sem 7 dígitos → unknown; `outros` → unknown; ordem de match.
  </action>
  <verify>
    <automated>npx vitest run src/lib/cnpj/__tests__/cnae-mapping.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/cnpj/cnae-mapping.ts` exporta `normalizeCnaeSubclasse`, `deriveCnaeClasse`, `CNAE_SEGMENT_MAP`, `cnaeCompatibilityFor`, `assertNoCnaeContradictions`, `CnaeCompatibility`, `CnaeCodes`, `SegmentCnaeMap`
    - `normalizeCnaeSubclasse("4781-4/00")` === `"4781400"`; `deriveCnaeClasse("4781400")` === `"47814"`
    - `cnaeCompatibilityFor` segue a precedência `negative.subclasses → positive.subclasses → negative.classes → positive.classes → unknown`
    - `assertNoCnaeContradictions()` lança `Error` quando um código idêntico está nas listas pos+neg do mesmo segmento
    - `npx vitest run src/lib/cnpj/__tests__/cnae-mapping.test.ts` → 0 falhas
  </acceptance_criteria>
  <done>Módulo CNAE determinístico completo com testes 37-46 verdes</done>
</task>

<task type="execute">
  <name>Task 2: Não-contradição em build/CI</name>
  <files>package.json, src/lib/cnpj/cnae-mapping.ts</files>
  <read_first>
    - package.json (scripts existentes — padrão de scripts de build/lint)
    - src/lib/cnpj/cnae-mapping.ts (assertNoCnaeContradictions)
    - .planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D9 — "validação de não-contradição em build/CI")
  </read_first>
  <action>
    Garantir que `assertNoCnaeContradictions()` roda no build/CI. Preferência: adicionar ao script existente de validação/build (ex. hook no `package.json` scripts que o build invoca) OU criar um script dedicado `check:cnae` executando o assert via `tsx`/`vitest` e anexá-lo ao comando de CI/build existente. Seguir o padrão de scripts do projeto (verificar quais scripts existem e onde o build roda validações). Não duplicar o assert em runtime de produção (é validação de tempo de build).
  </action>
  <verify>
    <automated>npm run check:cnae (ou o script adotado); if ($?) { npm run build }</automated>
  </verify>
  <acceptance_criteria>
    - Existe um script (`check:cnae` ou equivalente) que executa `assertNoCnaeContradictions()` e falha com exit != 0 se houver contradição
    - O script roda no pipeline de build/CI (anexado ao script de validação existente)
    - `npm run build` → 0 erros
  </acceptance_criteria>
  <done>Não-contradição validada em build/CI</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| segment enum ↔ CNAE map | Segmentos da F40 devem casar com as chaves do mapa — chave ausente = unknown silencioso |
| CNAE externo ↔ decisão | `cnae_principal` vem do provedor — dados possivelmente malformados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-03 | Spoofing | CNAE malformado | mitigate | `normalizeCnaeSubclasse` retorna null em input inválido → `unknown` (neutro); nunca falha hard em runtime |
| T-42-03a | Integrity | Contradição nas listas | mitigate | `assertNoCnaeContradictions` roda no build/CI → erro de build, não runtime |
| T-42-03b | Tampering | Segmento fora do enum | mitigate | Segmentos validados no tipo (Record com chaves do enum); `outros` cobre resto; chave ausente → unknown neutro |
| T-42-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo |
</threat_model>

<verification>
- `npx vitest run src/lib/cnpj/__tests__/cnae-mapping.test.ts` → 0 falhas (10 testes D9)
- Script de não-contradição roda no build/CI → 0 contradições
- `npm run typecheck` → 0 erros; `npm run build` → 0 erros
</verification>

<success_criteria>
- Módulo determinístico completo: normalização 7 dígitos, classe 4+DV, 4 conjuntos por segmento, precedência de subclasse exata, `unknown` neutro
- Não-contradição validada em build/CI
- Listas de CNAE validadas na CONCLA/IBGE (fonte registrada no SUMMARY)
</success_criteria>

<output>
Create `.planning/phases/42-signup-controlado-elegibilidade-freemium/42-03-SUMMARY.md` when done
</output>
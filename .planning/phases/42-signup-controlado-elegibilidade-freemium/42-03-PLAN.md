---
phase: 42-signup-controlado-elegibilidade-freemium
plan: 03
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - src/lib/cnpj/cnae-mapping.ts
  - src/lib/cnpj/__tests__/cnae-mapping.test.ts
autonomous: true
requirements: ["cnae-segment-mapping"]

must_haves:
  truths:
    - "`normalizeCnaeSubclasse(raw)` normaliza para 7 dÃ­gitos + DV (remove pontuaÃ§Ã£o) e retorna null se invÃ¡lido"
    - "`deriveCnaeClasse(subclasse)` deriva a classe 4+DV (5 primeiros dÃ­gitos)"
    - "Quatro conjuntos por segmento (compatible/incompatible Ã— classes/subclasses) + precedÃªncia `negative.subclasses â†’ positive.subclasses â†’ negative.classes â†’ positive.classes â†’ unknown`"
    - "ValidaÃ§Ã£o de nÃ£o-contradiÃ§Ã£o em build/CI (mesmo cÃ³digo nas listas pos+neg = erro)"
    - "CNAE nunca rejeita exclusivamente (incompatible â†’ review, unknown â†’ neutro)"
  artifacts:
    - path: "src/lib/cnpj/cnae-mapping.ts"
      provides: "MÃ³dulo determinÃ­stico"
      contains: "cnaeCompatibilityFor"
    - path: "src/lib/cnpj/__tests__/cnae-mapping.test.ts"
      provides: "Cobertura D9"
      contains: "cnaeCompatibilityFor"
---

<objective>
Criar o mÃ³dulo determinÃ­stico `src/lib/cnpj/cnae-mapping.ts` (D9): normalizaÃ§Ã£o de subclasse (7 dÃ­gitos + DV), derivaÃ§Ã£o de classe (4+DV), estrutura de 4 conjuntos por segmento, funÃ§Ã£o `cnaeCompatibilityFor(segment, cnaePrincipal)` com precedÃªncia de subclasse exata, e validaÃ§Ã£o de nÃ£o-contradiÃ§Ã£o em build/CI. Listas reais validadas na CONCLA/IBGE.

Purpose: D9 â€” compatibilidade segmento Ã— CNAE determinÃ­stica, versionada em cÃ³digo, nunca-reject por CNAE.
Output: mÃ³dulo + validaÃ§Ã£o CI + listas validadas.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D9 â€” CNAE determinÃ­stico; precedÃªncia; nÃ£o-contradiÃ§Ã£o; listas CONCLA/IBGE; segmento `outros` vazio)
@src/lib/cnpj/lookup-providers/types.ts (CnpjLookupData â€” `cnae_principal` string|null :22, `cnae_descricao` :23)
@src/lib/cnpj/mask.ts (padrÃ£o de helper puro em lib/cnpj)
@openspec/changes/fase-42-signup-controlado-elegibilidade-freemium/specs/cnae-segment-mapping/spec.md (Requirement â€” mÃ³dulo, precedÃªncia, nÃ£o-contradiÃ§Ã£o)

<interfaces>
- Segmentos vÃªm do enum `stores.segment` (F40): ex. `alimentacao`, `vestuario`, `beleza`, `outros`, etc. â€” listar os valores reais do enum ao fechar o mapeamento.
- CNAE principal: `officialData.cnae_principal` (formato `"4781-4/00"` â€” pontuado). `normalizeCnaeSubclasse("4781-4/00")` â†’ `"4781400"`; `deriveCnaeClasse("4781400")` â†’ `"47814"`.
- `segmento "outros"` â†’ conjuntos vazios â†’ sempre `unknown` (nunca penaliza).
- NÃƒO usar cÃ³digos ilustrativos do alinhamento; validar listas na CONCLA/IBGE antes de fechar.
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: MÃ³dulo cnae-mapping.ts (normalizaÃ§Ã£o + derivaÃ§Ã£o + compatibilidade)</name>
  <files>src/lib/cnpj/cnae-mapping.ts, src/lib/cnpj/__tests__/cnae-mapping.test.ts</files>
  <read_first>
    - src/lib/cnpj/lookup-providers/types.ts (CnpjLookupData.cnae_principal :22)
    - src/lib/cnpj/mask.ts (padrÃ£o de helper puro)
    - .planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D9)
    - openspec/changes/fase-42-signup-controlado-elegibilidade-freemium/specs/cnae-segment-mapping/spec.md
  </read_first>
  <action>
    Criar `src/lib/cnpj/cnae-mapping.ts` com:
    1. `export type CnaeCompatibility = "compatible" | "incompatible" | "unknown";`
    2. `export type CnaeCodes = { classes: string[]; subclasses: string[] };`
    3. `export type SegmentCnaeMap = Record<string, { compatible: CnaeCodes; incompatible: CnaeCodes }>;`
    4. `normalizeCnaeSubclasse(raw: string): string | null` â€” remove pontuaÃ§Ã£o (`-`, `/`, espaÃ§os), retorna `null` se nÃ£o resultar em exatamente 7 dÃ­gitos.
    5. `deriveCnaeClasse(subclasse: string): string` â€” retorna os 5 primeiros caracteres (classe 4+DV). Assuma input normalizado (7).
    6. `CNAE_SEGMENT_MAP: SegmentCnaeMap` â€” para cada segmento do enum `stores.segment`, quatro conjuntos (`compatible.classes`, `compatible.subclasses`, `incompatible.classes`, `incompatible.subclasses`); `outros` com todos vazios. **CÃ³digos validados na CONCLA/IBGE** â€” NÃƒO copiar os exemplos ilustrativos do alinhamento (registrar a fonte/validaÃ§Ã£o no SUMMARY).
    7. `cnaeCompatibilityFor(segment: string, cnaePrincipal: string | null): CnaeCompatibility` â€” ordem: `negative.subclasses â†’ positive.subclasses â†’ negative.classes â†’ positive.classes â†’ unknown`. `null`/invÃ¡lido/normalizaÃ§Ã£o falha â†’ `unknown`.
    8. `assertNoCnaeContradictions(): void` â€” percorre todos os segmentos e lanÃ§a `Error` se um cÃ³digo idÃªntico (classe OU subclasse) aparece nas listas positiva e negativa do mesmo segmento. Exportada para o build/CI (Task 2).
    Red/Green: criar `src/lib/cnpj/__tests__/cnae-mapping.test.ts` cobrindo (tests 37-46 da tasks.md): `"4781-4/00"`â†’`"4781400"`; classe `"47814"`; compatible; incompatible explÃ­cito; granularidade subclasse exata nÃ£o contamina a classe; precedÃªncia subclasse negativa vence classe positiva; validaÃ§Ã£o de nÃ£o-contradiÃ§Ã£o lanÃ§a; fora das listas â†’ unknown; null/sem 7 dÃ­gitos â†’ unknown; `outros` â†’ unknown; ordem de match.
  </action>
  <verify>
    <automated>npx vitest run src/lib/cnpj/__tests__/cnae-mapping.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/cnpj/cnae-mapping.ts` exporta `normalizeCnaeSubclasse`, `deriveCnaeClasse`, `CNAE_SEGMENT_MAP`, `cnaeCompatibilityFor`, `assertNoCnaeContradictions`, `CnaeCompatibility`, `CnaeCodes`, `SegmentCnaeMap`
    - `normalizeCnaeSubclasse("4781-4/00")` === `"4781400"`; `deriveCnaeClasse("4781400")` === `"47814"`
    - `cnaeCompatibilityFor` segue a precedÃªncia `negative.subclasses â†’ positive.subclasses â†’ negative.classes â†’ positive.classes â†’ unknown`
    - `assertNoCnaeContradictions()` lanÃ§a `Error` quando um cÃ³digo idÃªntico estÃ¡ nas listas pos+neg do mesmo segmento
    - `npx vitest run src/lib/cnpj/__tests__/cnae-mapping.test.ts` â†’ 0 falhas
  </acceptance_criteria>
  <done>MÃ³dulo CNAE determinÃ­stico completo com testes 37-46 verdes</done>
</task>

<task type="execute">
  <name>Task 2: NÃ£o-contradiÃ§Ã£o em build/CI</name>
  <files>package.json, src/lib/cnpj/cnae-mapping.ts</files>
  <read_first>
    - package.json (scripts existentes â€” padrÃ£o de scripts de build/lint)
    - src/lib/cnpj/cnae-mapping.ts (assertNoCnaeContradictions)
    - .planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D9 â€” "validaÃ§Ã£o de nÃ£o-contradiÃ§Ã£o em build/CI")
  </read_first>
  <action>
    Garantir que `assertNoCnaeContradictions()` roda no build/CI. PreferÃªncia: adicionar ao script existente de validaÃ§Ã£o/build (ex. hook no `package.json` scripts que o build invoca) OU criar um script dedicado `check:cnae` executando o assert via `tsx`/`vitest` e anexÃ¡-lo ao comando de CI/build existente. Seguir o padrÃ£o de scripts do projeto (verificar quais scripts existem e onde o build roda validaÃ§Ãµes). NÃ£o duplicar o assert em runtime de produÃ§Ã£o (Ã© validaÃ§Ã£o de tempo de build).
  </action>
  <verify>
    <automated>npm run check:cnae (ou o script adotado); if ($?) { npm run build }</automated>
  </verify>
  <acceptance_criteria>
    - Existe um script (`check:cnae` ou equivalente) que executa `assertNoCnaeContradictions()` e falha com exit != 0 se houver contradiÃ§Ã£o
    - O script roda no pipeline de build/CI (anexado ao script de validaÃ§Ã£o existente)
    - `npm run build` â†’ 0 erros
  </acceptance_criteria>
  <done>NÃ£o-contradiÃ§Ã£o validada em build/CI</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| segment enum â†” CNAE map | Segmentos da F40 devem casar com as chaves do mapa â€” chave ausente = unknown silencioso |
| CNAE externo â†” decisÃ£o | `cnae_principal` vem do provedor â€” dados possivelmente malformados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-03 | Spoofing | CNAE malformado | mitigate | `normalizeCnaeSubclasse` retorna null em input invÃ¡lido â†’ `unknown` (neutro); nunca falha hard em runtime |
| T-42-03a | Integrity | ContradiÃ§Ã£o nas listas | mitigate | `assertNoCnaeContradictions` roda no build/CI â†’ erro de build, nÃ£o runtime |
| T-42-03b | Tampering | Segmento fora do enum | mitigate | Segmentos validados no tipo (Record com chaves do enum); `outros` cobre resto; chave ausente â†’ unknown neutro |
| T-42-SC | Tampering | InstalaÃ§Ãµes de pacotes | accept | Nenhum pacote novo |
</threat_model>

<verification>
- `npx vitest run src/lib/cnpj/__tests__/cnae-mapping.test.ts` â†’ 0 falhas (10 testes D9)
- Script de nÃ£o-contradiÃ§Ã£o roda no build/CI â†’ 0 contradiÃ§Ãµes
- `npm run typecheck` â†’ 0 erros; `npm run build` â†’ 0 erros
</verification>

<success_criteria>
- MÃ³dulo determinÃ­stico completo: normalizaÃ§Ã£o 7 dÃ­gitos, classe 4+DV, 4 conjuntos por segmento, precedÃªncia de subclasse exata, `unknown` neutro
- NÃ£o-contradiÃ§Ã£o validada em build/CI
- Listas de CNAE validadas na CONCLA/IBGE (fonte registrada no SUMMARY)
</success_criteria>

<output>
Create `.planning/phases/42-signup-controlado-elegibilidade-freemium/42-03-SUMMARY.md` when done
</output>

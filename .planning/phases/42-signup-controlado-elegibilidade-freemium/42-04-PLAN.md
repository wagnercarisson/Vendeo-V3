---
phase: 42-signup-controlado-elegibilidade-freemium
plan: 04
type: execute
wave: 3
depends_on: ["42-03"]
files_modified:
  - src/lib/freemium/freemium-risk-service.ts
  - src/lib/freemium/types.ts
  - src/lib/freemium/__tests__/freemium-risk-service.test.ts
  - src/app/api/store/route.ts
  - src/app/api/store/__tests__/route.test.ts
  - src/app/api/store/update-cnpj/route.ts
  - src/app/api/store/update-cnpj/__tests__/route.test.ts
autonomous: true
requirements: ["freemium-risk-service (evaluateFreemiumEligibility â€” motor de decisÃ£o determinÃ­stico)"]

must_haves:
  truths:
    - "Ordem D10 aplicada: CNPJ â†’ situaÃ§Ã£o ATIVA exata â†’ raiz â†’ nome â‰¥ 0.6 â†’ cidade/UF (prÃ©-gate D7) â†’ CNAE â†’ approved (score â‰¥ 60)"
    - "SituaÃ§Ã£o nÃ£o-vazia â‰  ATIVA (exceto BAIXADA/NULA reject) â†’ review `situacao_nao_ativa`; situaÃ§Ã£o ausente/vazia em resposta resolvida â†’ defer `dados_oficiais_incompletos`"
    - "Cidade/UF: prÃ©-gate no caller/rota â€” motor nunca recebe nulos; preenchidas sem oficial â†’ review `localizacao_oficial_indisponivel`; divergentes â†’ review cidade/uf_divergente"
    - "`cnaeCompatible` tipado `\"compatible\"|\"incompatible\"|\"unknown\"|null`; incompatible â†’ review `segmento_cnae_divergente` (nunca reject)"
    - "`check_store_readiness` F34 intocada"
  artifacts:
    - path: "src/lib/freemium/freemium-risk-service.ts"
      provides: "Motor revisado"
      contains: "situacao_nao_ativa"
    - path: "src/lib/freemium/types.ts"
      provides: "Tipos atualizados"
      contains: "cnaeCompatible"
  key_links:
    - from: "freemium-risk-service.ts"
      to: "src/lib/cnpj/cnae-mapping.ts"
      via: "cnaeCompatibilityFor"
      pattern: "cnaeCompatibilityFor"
---

<objective>
Revisar o motor de elegibilidade `evaluateFreemiumEligibility` (D8/D10): aplicar a ordem D10, corrigir a lacuna F33 (situaÃ§Ã£o nÃ£o-ATIVA â†’ `situacao_nao_ativa`; ausente â†’ `dados_oficiais_incompletos`), integrar o prÃ©-gate de cidade/UF (D7) e o CNAE determinÃ­stico (D9, dependÃªncia 42-03). Atualizar tipos em `freemium/types.ts`. **`check_store_readiness` F34 intocada.**

Purpose: D8/D9/D10 â€” corrigir lacunas do motor F33 + integrar CNAE, preservando invariantes D6.
Output: motor revisado + tipos + testes co-migrados.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D7 â€” cidade/UF gate; D8 â€” situacao_nao_ativa; D9 â€” CNAE; D10 â€” ordem do motor)
@src/lib/freemium/freemium-risk-service.ts (motor atual â€” bloco SUSPENSA :91-98, cnaeCompatible null :43, ordem :46-139)
@src/lib/freemium/types.ts (FreemiumEligibilityInput city/state string :33-34, signals.cnaeCompatible boolean|null :53, Decision :28)
@src/lib/freemium/__tests__/freemium-risk-service.test.ts (makeInput factory :5-35; casos existentes)
@src/lib/cnpj/cnae-mapping.ts (dependÃªncia 42-03 â€” cnaeCompatibilityFor)

<interfaces>
- `FreemiumEligibilityInput` em types.ts: `city: string | null; state: string | null` (D7 â€” representa ausÃªncia). Motor NÃƒO trata ausÃªncia como branch prÃ³pria (prÃ©-gate no caller); defensivamente, se receber null, o motor pode retornar review `localizacao_oficial_indisponivel`? NÃƒO â€” decisÃ£o D7: prÃ©-gate no caller; o motor sÃ³ avalia quando city/state preenchidos. Contrato: caller garante nÃ£o-nulos.
- `signals.cnaeCompatible: "compatible" | "incompatible" | "unknown" | null` (substitui boolean|null).
- Ordem D10 no motor (apÃ³s refactor): CNPJ not_found â†’ reject; BAIXADA â†’ reject; NULA â†’ reject; situaÃ§Ã£o nÃ£o-vazia â‰  ATIVA â†’ review `situacao_nao_ativa`; situaÃ§Ã£o vazia/ausente (resposta resolvida) â†’ defer `dados_oficiais_incompletos`; rootEligible false â†’ reject; unavailable/sem dados â†’ defer `api_unavailable`; nome < 0.6 â†’ review `nome_divergente`; cidade/UF (preenchidas â€” prÃ©-gate): sem oficial â†’ review `localizacao_oficial_indisponivel`; divergentes â†’ review `cidade_divergente`/`uf_divergente`; CNAE incompatible â†’ review `segmento_cnae_divergente`; senÃ£o â†’ approved score â‰¥ 60.
- `check_store_readiness` F34 nÃ£o Ã© tocada.
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Atualizar tipos (types.ts)</name>
  <files>src/lib/freemium/types.ts, src/lib/freemium/__tests__/freemium-risk-service.test.ts</files>
  <read_first>
    - src/lib/freemium/types.ts (atual: city/state :33-34, cnaeCompatible :53, Decision :28)
    - .planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D7/D9)
  </read_first>
  <action>
    Em `src/lib/freemium/types.ts`:
    1. `FreemiumEligibilityInput.city: string | null` e `.state: string | null` (D7 â€” representa ausÃªncia no input; o caller aplica o prÃ©-gate).
    2. `signals.cnaeCompatible: "compatible" | "incompatible" | "unknown" | null` (substitui `boolean | null` :53).
    3. Manter `Decision = "approved" | "review" | "reject" | "defer"` (:28) â€” sem quinto retorno.
    Co-migrar `freemium-risk-service.test.ts` `makeInput` para os novos tipos (city/state podem continuar `string` no factory â€” sÃ£o non-null no caso default).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/freemium/types.ts` tem `city: string | null` e `state: string | null` no input
    - `signals.cnaeCompatible` Ã© `"compatible" | "incompatible" | "unknown" | null`
    - `Decision` permanece `"approved" | "review" | "reject" | "defer"`
    - `npm run typecheck` â†’ 0 erros
  </acceptance_criteria>
  <done>Tipos atualizados (city/state nullable, cnaeCompatible tri-state)</done>
</task>

<task type="tdd">
  <name>Task 2: Revisar o motor (freemium-risk-service.ts)</name>
  <files>src/lib/freemium/freemium-risk-service.ts, src/lib/freemium/__tests__/freemium-risk-service.test.ts</files>
  <read_first>
    - src/lib/freemium/freemium-risk-service.ts (atual â€” ordem :46-139, bloco SUSPENSA :91-98, cnaeCompatible null :43)
    - src/lib/cnpj/cnae-mapping.ts (cnaeCompatibilityFor â€” dependÃªncia 42-03)
    - .planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D8/D10)
  </read_first>
  <action>
    Revisar `evaluateFreemiumEligibility`:
    1. `signals.cnaeCompatible` preenchido: `cnaeCompatibilityFor(input.segment, input.officialData?.cnae_principal ?? null)` quando hÃ¡ `officialData`; `null` caso contrÃ¡rio.
    2. Ordem D10 (substitui o fluxo atual):
       - `lookupOutcome === "not_found"` â†’ reject `cnpj_not_found` (manter :46-53).
       - situaÃ§Ã£o `"BAIXADA"` â†’ reject `cnpj_baixada` (manter :55-62).
       - situaÃ§Ã£o `"NULA"` â†’ reject `cnpj_nula` (manter :64-71).
       - **situaÃ§Ã£o nÃ£o-vazia e â‰  "ATIVA"** (ex. SUSPENSA, INAPTA) â†’ review `situacao_nao_ativa` (SUBSTITUI o bloco SUSPENSA :91-98).
       - **situaÃ§Ã£o vazia/ausente/nÃ£o-normalizÃ¡vel em resposta resolvida** â†’ defer `dados_oficiais_incompletos` (score 0). Detectar: `officialData` existe E `situacao_cadastral` Ã© null/empty/apÃ³s normalizar nÃ£o "ATIVA" â€” distingue de BAIXADA/NULA jÃ¡ tratados.
       - `rootEligible === false` â†’ reject `root_already_used` (manter :73-80).
       - `lookupOutcome === "unavailable" || !officialData` â†’ defer `api_unavailable` (manter :82-89).
       - `nameSimilarity !== null && < 0.6` â†’ review `nome_divergente` (manter :100-107).
       - cidade/UF: preenchidas (input nÃ£o-null â€” prÃ©-gate D7 no caller). Se `officialData.cidade` ausente â†’ review `localizacao_oficial_indisponivel`; `cityMatch === false` â†’ review `cidade_divergente`; `stateMatch === false` â†’ review `uf_divergente`.
       - CNAE: `signals.cnaeCompatible === "incompatible"` â†’ review `segmento_cnae_divergente` (NUNCA reject); `unknown` â†’ neutro.
       - senÃ£o â†’ approved (score â‰¥ 60 preservando o cÃ¡lculo atual).
    3. Mover o bloco `SUSPENSA` para o genÃ©rico; manter `situacao_suspensa` label no admin (plan 42-05) apenas para histÃ³rico.
    Red/Green: co-migrar `freemium-risk-service.test.ts` â€” substituir o caso `REVIEW when SUSPENSA` por `REVIEW quando INAPTA/SUSPENSA â†’ situacao_nao_ativa`; adicionar casos: situaÃ§Ã£o vazia â†’ defer `dados_oficiais_incompletos`; cidade preenchida/sem oficial â†’ review `localizacao_oficial_indisponivel`; CNAE compatible â†’ approved sem revisÃ£o; CNAE incompatible â†’ review `segmento_cnae_divergente`; CNAE unknown â†’ neutro. Manter demais casos (approved, nome, cidade/uf divergente, defer, rejects) verdes (regressÃ£o D10).
  </action>
  <verify>
    <automated>npx vitest run src/lib/freemium/__tests__/freemium-risk-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `freemium-risk-service.ts` nÃ£o contÃ©m mais o bloco `SUSPENSA` especÃ­fico; situaÃ§Ã£o nÃ£o-ATIVA nÃ£o-vazia â†’ `situacao_nao_ativa`
    - SituaÃ§Ã£o ausente em resposta resolvida â†’ `dados_oficiais_incompletos` (defer)
    - `signals.cnaeCompatible` preenchido via `cnaeCompatibilityFor`; `"incompatible"` â†’ `segmento_cnae_divergente` (review, nunca reject)
    - `npx vitest run src/lib/freemium/__tests__/freemium-risk-service.test.ts` â†’ 0 falhas (incluindo regressÃ£o dos casos anteriores)
    - `npm run typecheck` â†’ 0 erros
  </acceptance_criteria>
  <done>Motor revisado (ordem D10 + novos motivos + CNAE) com testes verdes</done>
</task>

<task type="execute">
  <name>Task 3: Implementar o pré-gate D7 + motor nos DOIS callers (store/route.ts e update-cnpj/route.ts) + documentar contrato</name>
  <files>src/app/api/store/route.ts, src/app/api/store/__tests__/route.test.ts, src/app/api/store/update-cnpj/route.ts, src/app/api/store/update-cnpj/__tests__/route.test.ts, src/lib/freemium/freemium-risk-service.ts</files>
  <read_first>
    - .planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D7 — pré-gate no caller; check_store_readiness F34 intocada; decisão: create E update compartilham o mesmo contrato de elegibilidade)
    - src/app/api/store/route.ts (caller do motor — linha ~187; hoje passa `city: ""`/`state: ""` quando ausentes)
    - src/app/api/store/update-cnpj/route.ts (caller do fluxo draft→fiscal; hoje auto-aprova por `score.bestScore >= 0.8` em :119-126 SEM o motor revisado — corrigir)
    - src/app/api/store/__tests__/route.test.ts (padrão de teste da rota create)
    - src/app/api/store/update-cnpj/__tests__/route.test.ts (padrão de teste da rota update-cnpj)
  </read_first>
  <action>
    Implementar o pré-gate D7 + integração do motor revisado nos DOIS callers — o contrato de elegibilidade é único (decisão do usuário: create e update compartilham a mesma regra):

    **A. `src/app/api/store/route.ts` (criação):**
    1. **Antes** de chamar `evaluateFreemiumEligibility`, verificar se `city`/`state` estão ausentes (undefined/empty string após trim):
       - Se ausentes → **NÃO chamar o motor**. Manter `verificationStatus = "unverified"` (estado que a rota usa para não avaliado), `verificationData = { signals: {}, score: 0 }`, `verificationReasons = null`. **Sem review na fila admin, sem concessão de crédito** (sem `approved`).
    2. **Quando preenchidas** → chamar o motor normalmente (com os novos tipos de 42-04 Task 1 — city/state non-null).

    **B. `src/app/api/store/update-cnpj/route.ts` (draft→fiscal):**
    1. **Remover a autoaprovação direta** por `score.bestScore >= 0.8` como fonte final de decisão (:119-126) — o nome similar NÃO é mais autoritativo sozinho.
    2. **Pré-gate D7:** se `city`/`state` da loja/body ausentes (undefined/empty após trim) → **NÃO chamar o motor**; manter `verificationStatus` não-avaliado (unverified/draft), `verificationData = { signals: {}, score: 0 }`, sem `approved`, sem review, sem concessão.
    3. **Quando presentes** → chamar `evaluateFreemiumEligibility` com `officialData`, `lookupOutcome: "resolved"`, `rootEligible` (consulta `freemium_entitlements` por `root_hash` — mesmo padrão do create), `segment`, `city`, `state`, `storeName`.
       - situação cadastral não-ativa (INAPTA/SUSPENSA) → `situacao_nao_ativa` (review); ausente → `dados_oficiais_incompletos` (defer)
       - CNAE incompatível → `segmento_cnae_divergente` (review, nunca reject)
       - cidade/UF divergentes → `cidade_divergente`/`uf_divergente` (review)
       - nome divergente → `nome_divergente` (ordem D10)
       - senão → approved
    4. Manter o `compareBusinessName`/`cnpjValidationScore` como métrica de apoio (sinal), mas NÃO como decisão final; a decisão vem do motor.
    5. Preservar o caminho `unavailable` + test-store → defer `api_unavailable` (comportamento atual :127-131).

    **C. Documentar o contrato** no código (`freemium-risk-service.ts` comment): caller NÃO chama `evaluateFreemiumEligibility` com city/state ausentes; motor não tem branch de "draft" (sem quinto retorno); F34 `check_store_readiness` intocada.

    **D. Testes:**
    - `src/app/api/store/__tests__/route.test.ts`: POST /api/store sem `city`/`state` → motor NÃO chamado (`expect(service).not.toHaveBeenCalled()`); estado não-avaliado.
    - `src/app/api/store/update-cnpj/__tests__/route.test.ts`: (a) sem city/state → motor NÃO chamado, sem `approved`; (b) com city/state + nome similar ≥ 0.8 mas situação INAPTA/SUSPENSA → `situacao_nao_ativa` (review, NÃO approved); (c) CNAE incompatível → `segmento_cnae_divergente`; (d) tudo ok → approved via motor.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/store/__tests__/route.test.ts src/app/api/store/update-cnpj/__tests__/route.test.ts; if ($?) { npm run typecheck }</automated>
  </verify>
  <acceptance_criteria>
    - `store/route.ts` NÃO chama o motor com city/state ausentes; mantém não-avaliado (sem approved/review/concessão)
    - `update-cnpj/route.ts` NÃO auto-aprova por nome ≥ 0.8 sozinho; usa o motor revisado quando city/state presentes; pré-gate D7 quando ausentes
    - Update-cnpj com INAPTA/SUSPENSA → `situacao_nao_ativa` (review); CNAE incompatível → `segmento_cnae_divergente`; ambos NUNCA approved
    - Testes nos dois route.test.ts cobrem pré-gate D7 + motor (create e update compartilham o mesmo contrato)
    - Nenhum arquivo em `src/lib/store-readiness/` alterado; `check_store_readiness` intocada
  </acceptance_criteria>
  <done>Pré-gate D7 + motor implementados em create e update-cnpj; contrato único documentado</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller/rota -> motor | Pre-gate D7 - create (store/route.ts) e update-cnpj (update-cnpj/route.ts) NAO chamam o motor com city/state ausentes; contrato unico |
| provedor CNPJ â†” motor | `situacao_cadastral`/`cnae_principal` externos â€” possivelmente ausentes/malformados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-04 | Elevation of Privilege | SituaÃ§Ã£o nÃ£o-ATIVA | mitigate | `situacao_nao_ativa` â†’ review (nunca approve); ausente â†’ defer `dados_oficiais_incompletos` (nunca aprova, sem review ruidoso) â€” corrige lacuna F33 |
| T-42-04a | Spoofing | CNAE malformado/fora das listas | mitigate | `unknown` neutro; `incompatible` â†’ review, nunca reject |
| T-42-04b | Denial of Service | RuÃ­do de review | mitigate | PrÃ©-gate D7 (cidade/UF ausentes nÃ£o criam item na fila admin) |
| T-42-SC | Tampering | InstalaÃ§Ãµes de pacotes | accept | Nenhum pacote novo |
</threat_model>

<verification>
- `npx vitest run src/lib/freemium/__tests__/freemium-risk-service.test.ts` → 0 falhas
- `npx vitest run src/app/api/store/__tests__/route.test.ts src/app/api/store/update-cnpj/__tests__/route.test.ts` → 0 falhas (pré-gate D7: motor not called com city/state ausentes em create e update; INAPTA/CNAE no update)
- `npm run typecheck` → 0 erros
- `git diff --stat` mostra alterações em `src/lib/freemium/` (types.ts, risk-service.ts, test) e `src/app/api/store/` (route.ts, update-cnpj/route.ts + tests) — nenhuma em `src/lib/store-readiness/`
</verification>

<success_criteria>
- Ordem D10 aplicada (CNPJ → situação → raiz → nome → cidade/UF → CNAE → approved)
- Lacuna F33 corrigida: INAPTA/SUSPENSA → `situacao_nao_ativa`; situação ausente → `dados_oficiais_incompletos`
- `cnaeCompatible` tri-state preenchido via cnae-mapping; incompatible nunca rejeita
- **Pré-gate D7 implementado nos DOIS callers (`store/route.ts` e `update-cnpj/route.ts`)**: city/state ausentes → motor NÃO chamado, loja não-avaliada, sem review/concessão; create e update compartilham o mesmo contrato de elegibilidade
- F34 (`check_store_readiness`) intocada
</success_criteria>

<output>
Create `.planning/phases/42-signup-controlado-elegibilidade-freemium/42-04-SUMMARY.md` when done
</output>

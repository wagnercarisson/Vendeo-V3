---
phase: 42-signup-controlado-elegibilidade-freemium
plan: 04
type: execute
wave: 2
depends_on: ["42-03"]
files_modified:
  - src/lib/freemium/freemium-risk-service.ts
  - src/lib/freemium/types.ts
  - src/lib/freemium/__tests__/freemium-risk-service.test.ts
autonomous: true
requirements: ["freemium-risk-service (evaluateFreemiumEligibility — motor de decisão determinístico)"]

must_haves:
  truths:
    - "Ordem D10 aplicada: CNPJ → situação ATIVA exata → raiz → nome ≥ 0.6 → cidade/UF (pré-gate D7) → CNAE → approved (score ≥ 60)"
    - "Situação não-vazia ≠ ATIVA (exceto BAIXADA/NULA reject) → review `situacao_nao_ativa`; situação ausente/vazia em resposta resolvida → defer `dados_oficiais_incompletos`"
    - "Cidade/UF: pré-gate no caller/rota — motor nunca recebe nulos; preenchidas sem oficial → review `localizacao_oficial_indisponivel`; divergentes → review cidade/uf_divergente"
    - "`cnaeCompatible` tipado `\"compatible\"|\"incompatible\"|\"unknown\"|null`; incompatible → review `segmento_cnae_divergente` (nunca reject)"
    - "`check_store_readiness` F34 intocada"
  artifacts:
    - path: "src/lib/freemium/freemium-risk-service.ts"
      provides: "Motor revisado"
      contains: "situacao_nao_ativa"
    - path: "src/lib/freemium/types.ts"
      provides: "Tipos atualizados"
      contains: "cnaeCompatible"
  key_links:
    - from: "freemium-risk-service.ts"
      to: "src/lib/cnpj/cnae-mapping.ts"
      via: "cnaeCompatibilityFor"
      pattern: "cnaeCompatibilityFor"
---

<objective>
Revisar o motor de elegibilidade `evaluateFreemiumEligibility` (D8/D10): aplicar a ordem D10, corrigir a lacuna F33 (situação não-ATIVA → `situacao_nao_ativa`; ausente → `dados_oficiais_incompletos`), integrar o pré-gate de cidade/UF (D7) e o CNAE determinístico (D9, dependência 42-03). Atualizar tipos em `freemium/types.ts`. **`check_store_readiness` F34 intocada.**

Purpose: D8/D9/D10 — corrigir lacunas do motor F33 + integrar CNAE, preservando invariantes D6.
Output: motor revisado + tipos + testes co-migrados.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D7 — cidade/UF gate; D8 — situacao_nao_ativa; D9 — CNAE; D10 — ordem do motor)
@src/lib/freemium/freemium-risk-service.ts (motor atual — bloco SUSPENSA :91-98, cnaeCompatible null :43, ordem :46-139)
@src/lib/freemium/types.ts (FreemiumEligibilityInput city/state string :33-34, signals.cnaeCompatible boolean|null :53, Decision :28)
@src/lib/freemium/__tests__/freemium-risk-service.test.ts (makeInput factory :5-35; casos existentes)
@src/lib/cnpj/cnae-mapping.ts (dependência 42-03 — cnaeCompatibilityFor)

<interfaces>
- `FreemiumEligibilityInput` em types.ts: `city: string | null; state: string | null` (D7 — representa ausência). Motor NÃO trata ausência como branch própria (pré-gate no caller); defensivamente, se receber null, o motor pode retornar review `localizacao_oficial_indisponivel`? NÃO — decisão D7: pré-gate no caller; o motor só avalia quando city/state preenchidos. Contrato: caller garante não-nulos.
- `signals.cnaeCompatible: "compatible" | "incompatible" | "unknown" | null` (substitui boolean|null).
- Ordem D10 no motor (após refactor): CNPJ not_found → reject; BAIXADA → reject; NULA → reject; situação não-vazia ≠ ATIVA → review `situacao_nao_ativa`; situação vazia/ausente (resposta resolvida) → defer `dados_oficiais_incompletos`; rootEligible false → reject; unavailable/sem dados → defer `api_unavailable`; nome < 0.6 → review `nome_divergente`; cidade/UF (preenchidas — pré-gate): sem oficial → review `localizacao_oficial_indisponivel`; divergentes → review `cidade_divergente`/`uf_divergente`; CNAE incompatible → review `segmento_cnae_divergente`; senão → approved score ≥ 60.
- `check_store_readiness` F34 não é tocada.
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Atualizar tipos (types.ts)</name>
  <files>src/lib/freemium/types.ts, src/lib/freemium/__tests__/freemium-risk-service.test.ts</files>
  <read_first>
    - src/lib/freemium/types.ts (atual: city/state :33-34, cnaeCompatible :53, Decision :28)
    - .planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D7/D9)
  </read_first>
  <action>
    Em `src/lib/freemium/types.ts`:
    1. `FreemiumEligibilityInput.city: string | null` e `.state: string | null` (D7 — representa ausência no input; o caller aplica o pré-gate).
    2. `signals.cnaeCompatible: "compatible" | "incompatible" | "unknown" | null` (substitui `boolean | null` :53).
    3. Manter `Decision = "approved" | "review" | "reject" | "defer"` (:28) — sem quinto retorno.
    Co-migrar `freemium-risk-service.test.ts` `makeInput` para os novos tipos (city/state podem continuar `string` no factory — são non-null no caso default).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/freemium/types.ts` tem `city: string | null` e `state: string | null` no input
    - `signals.cnaeCompatible` é `"compatible" | "incompatible" | "unknown" | null`
    - `Decision` permanece `"approved" | "review" | "reject" | "defer"`
    - `npm run typecheck` → 0 erros
  </acceptance_criteria>
  <done>Tipos atualizados (city/state nullable, cnaeCompatible tri-state)</done>
</task>

<task type="tdd">
  <name>Task 2: Revisar o motor (freemium-risk-service.ts)</name>
  <files>src/lib/freemium/freemium-risk-service.ts, src/lib/freemium/__tests__/freemium-risk-service.test.ts</files>
  <read_first>
    - src/lib/freemium/freemium-risk-service.ts (atual — ordem :46-139, bloco SUSPENSA :91-98, cnaeCompatible null :43)
    - src/lib/cnpj/cnae-mapping.ts (cnaeCompatibilityFor — dependência 42-03)
    - .planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D8/D10)
  </read_first>
  <action>
    Revisar `evaluateFreemiumEligibility`:
    1. `signals.cnaeCompatible` preenchido: `cnaeCompatibilityFor(input.segment, input.officialData?.cnae_principal ?? null)` quando há `officialData`; `null` caso contrário.
    2. Ordem D10 (substitui o fluxo atual):
       - `lookupOutcome === "not_found"` → reject `cnpj_not_found` (manter :46-53).
       - situação `"BAIXADA"` → reject `cnpj_baixada` (manter :55-62).
       - situação `"NULA"` → reject `cnpj_nula` (manter :64-71).
       - **situação não-vazia e ≠ "ATIVA"** (ex. SUSPENSA, INAPTA) → review `situacao_nao_ativa` (SUBSTITUI o bloco SUSPENSA :91-98).
       - **situação vazia/ausente/não-normalizável em resposta resolvida** → defer `dados_oficiais_incompletos` (score 0). Detectar: `officialData` existe E `situacao_cadastral` é null/empty/após normalizar não "ATIVA" — distingue de BAIXADA/NULA já tratados.
       - `rootEligible === false` → reject `root_already_used` (manter :73-80).
       - `lookupOutcome === "unavailable" || !officialData` → defer `api_unavailable` (manter :82-89).
       - `nameSimilarity !== null && < 0.6` → review `nome_divergente` (manter :100-107).
       - cidade/UF: preenchidas (input não-null — pré-gate D7 no caller). Se `officialData.cidade` ausente → review `localizacao_oficial_indisponivel`; `cityMatch === false` → review `cidade_divergente`; `stateMatch === false` → review `uf_divergente`.
       - CNAE: `signals.cnaeCompatible === "incompatible"` → review `segmento_cnae_divergente` (NUNCA reject); `unknown` → neutro.
       - senão → approved (score ≥ 60 preservando o cálculo atual).
    3. Mover o bloco `SUSPENSA` para o genérico; manter `situacao_suspensa` label no admin (plan 42-05) apenas para histórico.
    Red/Green: co-migrar `freemium-risk-service.test.ts` — substituir o caso `REVIEW when SUSPENSA` por `REVIEW quando INAPTA/SUSPENSA → situacao_nao_ativa`; adicionar casos: situação vazia → defer `dados_oficiais_incompletos`; cidade preenchida/sem oficial → review `localizacao_oficial_indisponivel`; CNAE compatible → approved sem revisão; CNAE incompatible → review `segmento_cnae_divergente`; CNAE unknown → neutro. Manter demais casos (approved, nome, cidade/uf divergente, defer, rejects) verdes (regressão D10).
  </action>
  <verify>
    <automated>npx vitest run src/lib/freemium/__tests__/freemium-risk-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `freemium-risk-service.ts` não contém mais o bloco `SUSPENSA` específico; situação não-ATIVA não-vazia → `situacao_nao_ativa`
    - Situação ausente em resposta resolvida → `dados_oficiais_incompletos` (defer)
    - `signals.cnaeCompatible` preenchido via `cnaeCompatibilityFor`; `"incompatible"` → `segmento_cnae_divergente` (review, nunca reject)
    - `npx vitest run src/lib/freemium/__tests__/freemium-risk-service.test.ts` → 0 falhas (incluindo regressão dos casos anteriores)
    - `npm run typecheck` → 0 erros
  </acceptance_criteria>
  <done>Motor revisado (ordem D10 + novos motivos + CNAE) com testes verdes</done>
</task>

<task type="execute">
  <name>Task 3: Documentar o pré-gate de cidade/UF (D7) sem tocar check_store_readiness</name>
  <files>src/lib/freemium/freemium-risk-service.ts</files>
  <read_first>
    - .planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D7 — pré-gate no caller; check_store_readiness F34 intocada)
  </read_first>
  <action>
    Documentar no código (comment) que cidade/UF ausentes devem ser tratadas pelo **caller/rota** (pré-gate D7): se `input.city`/`input.state` forem null/empty, o caller NÃO deve chamar `evaluateFreemiumEligibility` (a loja permanece draft/unverified, sem review, sem concessão). NÃO criar branch de "draft" no motor (sem quinto retorno). NÃO alterar `src/lib/store-readiness/` nem `check_store_readiness` (F34 intocada).
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\lib\freemium\freemium-risk-service.ts" -Pattern "pré-gate|pre-gate|D7" | Select-Object -First 3</automated>
  </verify>
  <acceptance_criteria>
    - `freemium-risk-service.ts` contém comentário documentando o pré-gate D7 (caller não chama com cidade/UF ausentes)
    - Nenhum arquivo em `src/lib/store-readiness/` foi alterado; `check_store_readiness` intocada
  </acceptance_criteria>
  <done>Pré-gate D7 documentado; F34 intocada</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller/rota ↔ motor | Pré-gate D7 — se o caller não aplicar, motor pode receber nulls e produzir ruído |
| provedor CNPJ ↔ motor | `situacao_cadastral`/`cnae_principal` externos — possivelmente ausentes/malformados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-04 | Elevation of Privilege | Situação não-ATIVA | mitigate | `situacao_nao_ativa` → review (nunca approve); ausente → defer `dados_oficiais_incompletos` (nunca aprova, sem review ruidoso) — corrige lacuna F33 |
| T-42-04a | Spoofing | CNAE malformado/fora das listas | mitigate | `unknown` neutro; `incompatible` → review, nunca reject |
| T-42-04b | Denial of Service | Ruído de review | mitigate | Pré-gate D7 (cidade/UF ausentes não criam item na fila admin) |
| T-42-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo |
</threat_model>

<verification>
- `npx vitest run src/lib/freemium/__tests__/freemium-risk-service.test.ts` → 0 falhas
- `npm run typecheck` → 0 erros
- `git diff --stat` mostra alterações apenas em `src/lib/freemium/` (types.ts, risk-service.ts, test) — nenhuma em `src/lib/store-readiness/`
</verification>

<success_criteria>
- Ordem D10 aplicada (CNPJ → situação → raiz → nome → cidade/UF → CNAE → approved)
- Lacuna F33 corrigida: INAPTA/SUSPENSA → `situacao_nao_ativa`; situação ausente → `dados_oficiais_incompletos`
- `cnaeCompatible` tri-state preenchido via cnae-mapping; incompatible nunca rejeita
- F34 (`check_store_readiness`) intocada
</success_criteria>

<output>
Create `.planning/phases/42-signup-controlado-elegibilidade-freemium/42-04-SUMMARY.md` when done
</output>
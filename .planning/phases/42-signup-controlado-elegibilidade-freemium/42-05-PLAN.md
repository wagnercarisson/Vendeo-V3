---
phase: 42-signup-controlado-elegibilidade-freemium
plan: 05
type: execute
wave: 4
depends_on: ["42-04"]
files_modified:
  - src/lib/admin/labels.ts
  - src/lib/admin/__tests__/labels.test.ts
  - src/app/(app)/admin/reviews/page.tsx
  - src/app/(app)/admin/reviews/review-detail.tsx
autonomous: true
requirements: ["admin-reviews"]

must_haves:
  truths:
    - "4 novos labels em VERIFICATION_REASON_LABELS: situacao_nao_ativa, localizacao_oficial_indisponivel, segmento_cnae_divergente, dados_oficiais_incompletos; situacao_suspensa mantido para histÃ³rico"
    - "Admin reviews exibe dados informados Ã— oficiais (razÃ£o social, nome fantasia, similaridade %, cidade/UF, CNAE + descriÃ§Ã£o, situaÃ§Ã£o cadastral original, histÃ³rico de raiz)"
    - "Filtro ?reason= inclui os novos motivos; defer `dados_oficiais_incompletos` exibido com label (sem motivo cru); aÃ§Ãµes existentes mantidas"
  artifacts:
    - path: "src/lib/admin/labels.ts"
      provides: "4 novos labels"
      contains: "situacao_nao_ativa"
    - path: "src/app/(app)/admin/reviews/review-detail.tsx"
      provides: "VisÃ£o informado Ã— oficial"
      contains: "CNAE"
---

<objective>
Enriquecer o admin de revisÃ£o (D11): adicionar os 4 novos labels de motivo (`situacao_nao_ativa`, `localizacao_oficial_indisponivel`, `segmento_cnae_divergente`, `dados_oficiais_incompletos`) mantendo `situacao_suspensa` para histÃ³rico, e expandir a fila de revisÃ£o com a visÃ£o **informado Ã— oficial** (razÃ£o social, nome fantasia, similaridade %, cidade/UF, CNAE + descriÃ§Ã£o, situaÃ§Ã£o cadastral original, histÃ³rico de raiz). AÃ§Ãµes existentes (approve/reject/defer/exception) mantidas e auditÃ¡veis.

Purpose: D8 (situacao_suspensa legado) + D11 (admin mais rico).
Output: labels + review-detail + filtro + testes.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D8 â€” situacao_suspensa legado; D11 â€” admin mais rico)
@src/lib/admin/labels.ts (VERIFICATION_REASON_LABELS :28-37)
@src/app/(app)/admin/reviews/page.tsx (query seleciona verification_reasons/verification_data/cnpj_official_data :33; badges :113-116; filtro reason :38-40; tabs :10-15)
@src/components/admin/review-actions.tsx (aÃ§Ãµes existentes â€” approve/reject/defer/exception)
@src/lib/admin/__tests__/labels.test.ts (testes de labels existentes)
@src/lib/cnpj/similarity.ts (computeSimilarity/compareBusinessName â€” similaridade %)

<interfaces>
- Labels em `src/lib/admin/labels.ts` `VERIFICATION_REASON_LABELS` (:28-37): adicionar as 4 entradas. `situacao_suspensa` (:32) permanece (legado histÃ³rico).
- `reviews/page.tsx` jÃ¡ busca `verification_reasons, verification_data, cnpj_official_data` (:33). `verification_data` contÃ©m o payload avaliado (ex. cidade/UF informadas, segmento); `cnpj_official_data` contÃ©m os dados do provedor (razao_social, nome_fantasia, situacao_cadastral, cnae_principal, cnae_descricao, cidade, uf). HistÃ³rico de raiz via `freemium_entitlements` (busca adicional por `cnpj_root_hash`).
- Filtro `?reason=` jÃ¡ existe (:38-40, `contains("verification_reasons", [reasonFilter])`).
- Similaridade: usar `compareBusinessName`/`computeSimilarity` de `src/lib/cnpj/similarity.ts` se disponÃ­vel para exibir %.

<interfaces_note>
A exibiÃ§Ã£o detalhada deve seguir o MASTER (tokens: bg-bg-elevated, text-text-muted, accent-blue links) â€” pÃ¡gina jÃ¡ usa MASTER.
</interfaces_note>
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: 4 novos labels em VERIFICATION_REASON_LABELS</name>
  <files>src/lib/admin/labels.ts, src/lib/admin/__tests__/labels.test.ts</files>
  <read_first>
    - src/lib/admin/labels.ts (VERIFICATION_REASON_LABELS :28-37)
    - src/lib/admin/__tests__/labels.test.ts (convenÃ§Ãµes de teste)
    - .planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D8/D11)
  </read_first>
  <action>
    Em `src/lib/admin/labels.ts`, adicionar a `VERIFICATION_REASON_LABELS` (:28-37):
    - `situacao_nao_ativa: "SituaÃ§Ã£o cadastral nÃ£o ativa"`
    - `localizacao_oficial_indisponivel: "LocalizaÃ§Ã£o oficial indisponÃ­vel"`
    - `segmento_cnae_divergente: "Segmento incompatÃ­vel com CNAE"`
    - `dados_oficiais_incompletos: "Dados oficiais incompletos"`
    Manter `situacao_suspensa: "SituaÃ§Ã£o suspensa"` (:32) â€” legado histÃ³rico (D8).
    Red/Green: estender `src/lib/admin/__tests__/labels.test.ts` cobrindo os 4 novos labels (getLabel retorna o texto correto) e que `situacao_suspensa` continua presente.
  </action>
  <verify>
    <automated>npx vitest run src/lib/admin/__tests__/labels.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/admin/labels.ts` contÃ©m os 4 novos labels em `VERIFICATION_REASON_LABELS` com os textos exatos acima
    - `situacao_suspensa` permanece no mapa
    - `npx vitest run src/lib/admin/__tests__/labels.test.ts` â†’ 0 falhas
  </acceptance_criteria>
  <done>4 novos labels + situacao_suspensa legado com testes verdes</done>
</task>

<task type="execute">
  <name>Task 2: ReviewDetail â€” visÃ£o informado Ã— oficial</name>
  <files>src/app/(app)/admin/reviews/review-detail.tsx, src/app/(app)/admin/reviews/page.tsx</files>
  <read_first>
    - src/app/(app)/admin/reviews/page.tsx (query atual :31-42, badges :113-116, tabela :83-144)
    - src/lib/cnpj/similarity.ts (similaridade %)
    - .planning/phases/42-signup-controlado-elegibilidade-freemium/42-UI-SPEC.md (ReviewDetail â€” MASTER tokens)
  </read_first>
  <action>
    Criar `src/app/(app)/admin/reviews/review-detail.tsx` (client or server-presentational conforme o padrÃ£o da pÃ¡gina â€” pÃ¡gina atual Ã© server component async):
    1. Recebe a loja (`store` com `name, verification_status, verification_reasons, verification_data, cnpj_official_data, cnpj_normalized`).
    2. Renderiza seÃ§Ã£o detalhada "Dados informados Ã— oficiais":
       - Informado: nome da loja (`store.name`), cidade/UF informadas (de `verification_data` se presente), segmento.
       - Oficial (`cnpj_official_data`): razÃ£o social (`razao_social`), nome fantasia (`nome_fantasia`), cidade/UF (`cidade`/`uf`), CNAE principal + descriÃ§Ã£o (`cnae_principal`/`cnae_descricao`), situaÃ§Ã£o cadastral original (`situacao_cadastral`).
       - Similaridade %: computar via `compareBusinessName(store.name, razao_social, nome_fantasia)` (reusar `src/lib/cnpj/similarity.ts`) quando aplicÃ¡vel.
       - HistÃ³rico de raiz: buscar em `freemium_entitlements` por `cnpj_root_hash` (a pÃ¡gina faz a query adicional se necessÃ¡rio) e exibir benefÃ­cios/raiz.
    3. Estilo MASTER (bg-bg-elevated, text-text-muted, accent-blue). AcessÃ­vel (tabela/lista semÃ¢ntica).
    Integrar em `reviews/page.tsx`: renderizar `ReviewDetail` por linha (expansÃ£o accordion `<details>`/`<summary>` ou coluna extra) sem quebrar a tabela atual nem as aÃ§Ãµes (`ReviewActions`).
  </action>
  <verify>
    <automated>npm run typecheck; if ($?) { npx vitest run src/app/api/admin/reviews/__tests__/route.test.ts }</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(app)/admin/reviews/review-detail.tsx` existe e exibe: razÃ£o social, nome fantasia, similaridade %, cidade/UF informada Ã— oficial, CNAE principal + descriÃ§Ã£o, situaÃ§Ã£o cadastral original
    - `reviews/page.tsx` integra `ReviewDetail` sem quebrar a tabela, badges, tabs, filtro ou `ReviewActions`
    - `npm run typecheck` â†’ 0 erros; testes de admin reviews continuam passando
  </acceptance_criteria>
  <done>ReviewDetail integrado Ã  fila admin com dados informado Ã— oficial</done>
</task>

<task type="execute">
  <name>Task 3: Filtro por motivo + label de defer (regressÃ£o)</name>
  <files>src/app/(app)/admin/reviews/page.tsx</files>
  <read_first>
    - src/app/(app)/admin/reviews/page.tsx (filtro reason :38-40, badges :113-116)
    - .planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D11)
  </read_first>
  <action>
    - Verificar que o filtro `?reason=` (jÃ¡ existente) aceita os novos motivos sem alteraÃ§Ã£o de lÃ³gica (usa `contains("verification_reasons", [reasonFilter])`). Se houver lista fixa de reasons aceitos, incluir os 4 novos.
    - Garantir que um registro **defer** com `dados_oficiais_incompletos` exibe o **label** "Dados oficiais incompletos" no badge (via `getLabel(VERIFICATION_REASON_LABELS, r)`) â€” nunca o motivo cru.
    - NÃƒO alterar `ReviewActions` (approve/reject/defer/exception permanecem).
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\app\(app)\admin\reviews\page.tsx" -Pattern "dados_oficiais_incompletos|situacao_nao_ativa|localizacao_oficial_indisponivel|segmento_cnae_divergente"</automated>
  </verify>
  <acceptance_criteria>
    - Filtro `?reason=` aceita os 4 novos motivos
    - Badge de defer `dados_oficiais_incompletos` renderiza o label "Dados oficiais incompletos" (via `getLabel`)
    - `ReviewActions` inalterado
  </acceptance_criteria>
  <done>Filtro + label de defer funcionais sem quebra</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dados admin â†” exibiÃ§Ã£o | `cnpj_official_data` externo renderizado â€” possÃ­vel XSS se nÃ£o escapado |
| revisÃ£o â†” decisÃ£o | Admin pode aprovar/rejeitar/deferir/exceÃ§Ã£o â€” decisÃ£o sensÃ­vel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-05 | Tampering | ExibiÃ§Ã£o de dados externos | mitigate | React escapa por padrÃ£o; dados do provedor renderizados como texto (sem dangerouslySetInnerHTML) |
| T-42-05a | Information Disclosure | Dados da loja | mitigate | PÃ¡gina restrita a admin (`requireAdmin` jÃ¡ na pÃ¡gina :22); CNPJ mascarado (`maskCnpj`) |
| T-42-05b | Tampering | AÃ§Ãµes admin | mitigate | AÃ§Ãµes existentes mantidas; `admin_exception` auditÃ¡vel (D6) â€” sem mudanÃ§a de semÃ¢ntica |
| T-42-SC | Tampering | InstalaÃ§Ãµes de pacotes | accept | Nenhum pacote novo |
</threat_model>

<verification>
- `npx vitest run src/lib/admin/__tests__/labels.test.ts` â†’ 0 falhas
- `npm run typecheck` â†’ 0 erros
- `npx vitest run src/app/api/admin/reviews/__tests__/route.test.ts` â†’ 0 falhas (regressÃ£o)
- `reviews/page.tsx` integra ReviewDetail sem quebrar tabs/filtro/aÃ§Ãµes
</verification>

<success_criteria>
- 4 novos labels com textos corretos; `situacao_suspensa` legado mantido
- ReviewDetail exibe informado Ã— oficial (razÃ£o social, fantasia, similaridade %, cidade/UF, CNAE + descriÃ§Ã£o, situaÃ§Ã£o original)
- Filtro por motivo inclui novos motivos; defer exibe label "Dados oficiais incompletos"; aÃ§Ãµes intactas
</success_criteria>

<output>
Create `.planning/phases/42-signup-controlado-elegibilidade-freemium/42-05-SUMMARY.md` when done
</output>


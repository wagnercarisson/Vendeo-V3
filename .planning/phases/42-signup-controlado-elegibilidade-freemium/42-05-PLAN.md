---
phase: 42-signup-controlado-elegibilidade-freemium
plan: 05
type: execute
wave: 2
depends_on: ["42-04"]
files_modified:
  - src/lib/admin/labels.ts
  - src/lib/__tests__/labels.test.ts
  - src/app/(app)/admin/reviews/page.tsx
  - src/app/(app)/admin/reviews/review-detail.tsx
autonomous: true
requirements: ["admin-reviews"]

must_haves:
  truths:
    - "4 novos labels em VERIFICATION_REASON_LABELS: situacao_nao_ativa, localizacao_oficial_indisponivel, segmento_cnae_divergente, dados_oficiais_incompletos; situacao_suspensa mantido para histórico"
    - "Admin reviews exibe dados informados × oficiais (razão social, nome fantasia, similaridade %, cidade/UF, CNAE + descrição, situação cadastral original, histórico de raiz)"
    - "Filtro ?reason= inclui os novos motivos; defer `dados_oficiais_incompletos` exibido com label (sem motivo cru); ações existentes mantidas"
  artifacts:
    - path: "src/lib/admin/labels.ts"
      provides: "4 novos labels"
      contains: "situacao_nao_ativa"
    - path: "src/app/(app)/admin/reviews/review-detail.tsx"
      provides: "Visão informado × oficial"
      contains: "CNAE"
---

<objective>
Enriquecer o admin de revisão (D11): adicionar os 4 novos labels de motivo (`situacao_nao_ativa`, `localizacao_oficial_indisponivel`, `segmento_cnae_divergente`, `dados_oficiais_incompletos`) mantendo `situacao_suspensa` para histórico, e expandir a fila de revisão com a visão **informado × oficial** (razão social, nome fantasia, similaridade %, cidade/UF, CNAE + descrição, situação cadastral original, histórico de raiz). Ações existentes (approve/reject/defer/exception) mantidas e auditáveis.

Purpose: D8 (situacao_suspensa legado) + D11 (admin mais rico).
Output: labels + review-detail + filtro + testes.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D8 — situacao_suspensa legado; D11 — admin mais rico)
@src/lib/admin/labels.ts (VERIFICATION_REASON_LABELS :28-37)
@src/app/(app)/admin/reviews/page.tsx (query seleciona verification_reasons/verification_data/cnpj_official_data :33; badges :113-116; filtro reason :38-40; tabs :10-15)
@src/components/admin/review-actions.tsx (ações existentes — approve/reject/defer/exception)
@src/lib/__tests__/labels.test.ts (testes de labels existentes)
@src/lib/cnpj/similarity.ts (computeSimilarity/compareBusinessName — similaridade %)

<interfaces>
- Labels em `src/lib/admin/labels.ts` `VERIFICATION_REASON_LABELS` (:28-37): adicionar as 4 entradas. `situacao_suspensa` (:32) permanece (legado histórico).
- `reviews/page.tsx` já busca `verification_reasons, verification_data, cnpj_official_data` (:33). `verification_data` contém o payload avaliado (ex. cidade/UF informadas, segmento); `cnpj_official_data` contém os dados do provedor (razao_social, nome_fantasia, situacao_cadastral, cnae_principal, cnae_descricao, cidade, uf). Histórico de raiz via `freemium_entitlements` (busca adicional por `cnpj_root_hash`).
- Filtro `?reason=` já existe (:38-40, `contains("verification_reasons", [reasonFilter])`).
- Similaridade: usar `compareBusinessName`/`computeSimilarity` de `src/lib/cnpj/similarity.ts` se disponível para exibir %.

<interfaces_note>
A exibição detalhada deve seguir o MASTER (tokens: bg-bg-elevated, text-text-muted, accent-blue links) — página já usa MASTER.
</interfaces_note>
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: 4 novos labels em VERIFICATION_REASON_LABELS</name>
  <files>src/lib/admin/labels.ts, src/lib/__tests__/labels.test.ts</files>
  <read_first>
    - src/lib/admin/labels.ts (VERIFICATION_REASON_LABELS :28-37)
    - src/lib/__tests__/labels.test.ts (convenções de teste)
    - .planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D8/D11)
  </read_first>
  <action>
    Em `src/lib/admin/labels.ts`, adicionar a `VERIFICATION_REASON_LABELS` (:28-37):
    - `situacao_nao_ativa: "Situação cadastral não ativa"`
    - `localizacao_oficial_indisponivel: "Localização oficial indisponível"`
    - `segmento_cnae_divergente: "Segmento incompatível com CNAE"`
    - `dados_oficiais_incompletos: "Dados oficiais incompletos"`
    Manter `situacao_suspensa: "Situação suspensa"` (:32) — legado histórico (D8).
    Red/Green: estender `src/lib/__tests__/labels.test.ts` cobrindo os 4 novos labels (getLabel retorna o texto correto) e que `situacao_suspensa` continua presente.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/labels.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/admin/labels.ts` contém os 4 novos labels em `VERIFICATION_REASON_LABELS` com os textos exatos acima
    - `situacao_suspensa` permanece no mapa
    - `npx vitest run src/lib/__tests__/labels.test.ts` → 0 falhas
  </acceptance_criteria>
  <done>4 novos labels + situacao_suspensa legado com testes verdes</done>
</task>

<task type="execute">
  <name>Task 2: ReviewDetail — visão informado × oficial</name>
  <files>src/app/(app)/admin/reviews/review-detail.tsx, src/app/(app)/admin/reviews/page.tsx</files>
  <read_first>
    - src/app/(app)/admin/reviews/page.tsx (query atual :31-42, badges :113-116, tabela :83-144)
    - src/lib/cnpj/similarity.ts (similaridade %)
    - .planning/phases/42-signup-controlado-elegibilidade-freemium/42-UI-SPEC.md (ReviewDetail — MASTER tokens)
  </read_first>
  <action>
    Criar `src/app/(app)/admin/reviews/review-detail.tsx` (client or server-presentational conforme o padrão da página — página atual é server component async):
    1. Recebe a loja (`store` com `name, verification_status, verification_reasons, verification_data, cnpj_official_data, cnpj_normalized`).
    2. Renderiza seção detalhada "Dados informados × oficiais":
       - Informado: nome da loja (`store.name`), cidade/UF informadas (de `verification_data` se presente), segmento.
       - Oficial (`cnpj_official_data`): razão social (`razao_social`), nome fantasia (`nome_fantasia`), cidade/UF (`cidade`/`uf`), CNAE principal + descrição (`cnae_principal`/`cnae_descricao`), situação cadastral original (`situacao_cadastral`).
       - Similaridade %: computar via `compareBusinessName(store.name, razao_social, nome_fantasia)` (reusar `src/lib/cnpj/similarity.ts`) quando aplicável.
       - Histórico de raiz: buscar em `freemium_entitlements` por `cnpj_root_hash` (a página faz a query adicional se necessário) e exibir benefícios/raiz.
    3. Estilo MASTER (bg-bg-elevated, text-text-muted, accent-blue). Acessível (tabela/lista semântica).
    Integrar em `reviews/page.tsx`: renderizar `ReviewDetail` por linha (expansão accordion `<details>`/`<summary>` ou coluna extra) sem quebrar a tabela atual nem as ações (`ReviewActions`).
  </action>
  <verify>
    <automated>npm run typecheck; if ($?) { npx vitest run src/app/api/admin/reviews/__tests__/route.test.ts }</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(app)/admin/reviews/review-detail.tsx` existe e exibe: razão social, nome fantasia, similaridade %, cidade/UF informada × oficial, CNAE principal + descrição, situação cadastral original
    - `reviews/page.tsx` integra `ReviewDetail` sem quebrar a tabela, badges, tabs, filtro ou `ReviewActions`
    - `npm run typecheck` → 0 erros; testes de admin reviews continuam passando
  </acceptance_criteria>
  <done>ReviewDetail integrado à fila admin com dados informado × oficial</done>
</task>

<task type="execute">
  <name>Task 3: Filtro por motivo + label de defer (regressão)</name>
  <files>src/app/(app)/admin/reviews/page.tsx</files>
  <read_first>
    - src/app/(app)/admin/reviews/page.tsx (filtro reason :38-40, badges :113-116)
    - .planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D11)
  </read_first>
  <action>
    - Verificar que o filtro `?reason=` (já existente) aceita os novos motivos sem alteração de lógica (usa `contains("verification_reasons", [reasonFilter])`). Se houver lista fixa de reasons aceitos, incluir os 4 novos.
    - Garantir que um registro **defer** com `dados_oficiais_incompletos` exibe o **label** "Dados oficiais incompletos" no badge (via `getLabel(VERIFICATION_REASON_LABELS, r)`) — nunca o motivo cru.
    - NÃO alterar `ReviewActions` (approve/reject/defer/exception permanecem).
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\app\(app)\admin\reviews\page.tsx" -Pattern "dados_oficiais_incompletos|situacao_nao_ativa|localizacao_oficial_indisponivel|segmento_cnae_divergente"</automated>
  </verify>
  <acceptance_criteria>
    - Filtro `?reason=` aceita os 4 novos motivos
    - Badge de defer `dados_oficiais_incompletos` renderiza o label "Dados oficiais incompletos" (via `getLabel`)
    - `ReviewActions` inalterado
  </acceptance_criteria>
  <done>Filtro + label de defer funcionais sem quebra</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dados admin ↔ exibição | `cnpj_official_data` externo renderizado — possível XSS se não escapado |
| revisão ↔ decisão | Admin pode aprovar/rejeitar/deferir/exceção — decisão sensível |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-05 | Tampering | Exibição de dados externos | mitigate | React escapa por padrão; dados do provedor renderizados como texto (sem dangerouslySetInnerHTML) |
| T-42-05a | Information Disclosure | Dados da loja | mitigate | Página restrita a admin (`requireAdmin` já na página :22); CNPJ mascarado (`maskCnpj`) |
| T-42-05b | Tampering | Ações admin | mitigate | Ações existentes mantidas; `admin_exception` auditável (D6) — sem mudança de semântica |
| T-42-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo |
</threat_model>

<verification>
- `npx vitest run src/lib/__tests__/labels.test.ts` → 0 falhas
- `npm run typecheck` → 0 erros
- `npx vitest run src/app/api/admin/reviews/__tests__/route.test.ts` → 0 falhas (regressão)
- `reviews/page.tsx` integra ReviewDetail sem quebrar tabs/filtro/ações
</verification>

<success_criteria>
- 4 novos labels com textos corretos; `situacao_suspensa` legado mantido
- ReviewDetail exibe informado × oficial (razão social, fantasia, similaridade %, cidade/UF, CNAE + descrição, situação original)
- Filtro por motivo inclui novos motivos; defer exibe label "Dados oficiais incompletos"; ações intactas
</success_criteria>

<output>
Create `.planning/phases/42-signup-controlado-elegibilidade-freemium/42-05-SUMMARY.md` when done
</output>
---
phase: 42-signup-controlado-elegibilidade-freemium
plan: 06
type: execute
wave: 4
depends_on: ["42-02", "42-07", "42-08"]
files_modified:
  - src/app/(auth)/signup/page.tsx
  - src/components/auth/signup-form.tsx
  - src/__tests__/auth/signup-page.test.tsx
autonomous: true
requirements: ["signup-page", "launch-config (Nova flag publicSignupEnabled)", "legal-acceptance-service", "google-oauth-signup", "turnstile-captcha"]

must_haves:
  truths:
    - "`/signup` server component lÃª `publicSignupEnabled` server-side â€” flag on â†’ formulÃ¡rio + 'Continuar com Google'; flag off â†’ 'Beta fechado' atual preservado"
    - "**Depende de 42-07 (GoogleButton real) e 42-08 (CaptchaField real)** â€” NÃƒO criar placeholders; os componentes jÃ¡ existem quando 42-06 executa"
    - "`SignupForm` em `src/components/auth/signup-form.tsx` (restaurado/modernizado): email, senha mÃ­n. 8, confirmar senha, ciÃªncia da Privacidade (modal PrivacyAcknowledgeModal), consentimento opcional de comunicaÃ§Ãµes, links Privacidade/Termos"
    - "signUp envia emailRedirectTo `${getSiteUrl()}/auth/confirm` + captchaToken; privacyPending/consentimento via sessionStorage"
    - "Anti-enumeraÃ§Ã£o: sucesso e email duplicado â†’ mesma resposta `/check-email?type=signup`; erro operacional â†’ 'NÃ£o foi possÃ­vel concluir. Tente novamente.'"
    - "ValidaÃ§Ã£o local: password.length >= 8, password === confirm (mensagens PT-BR)"
  artifacts:
    - path: "src/components/auth/signup-form.tsx"
      provides: "FormulÃ¡rio restaurado"
      contains: "signUp"
    - path: "src/app/(auth)/signup/page.tsx"
      provides: "Flag on/off"
      contains: "publicSignupEnabled"
  key_links:
    - from: "signup/page.tsx"
      to: "src/components/auth/signup-form.tsx"
      via: "import"
      pattern: "SignupForm"
---

<objective>
Restaurar o cadastro pÃºblico no `/signup` (D2/D4/D5/D15): server component lÃª `publicSignupEnabled` (flag off â†’ "Beta fechado" verbatim; flag on â†’ formulÃ¡rio + Google); criar `SignupForm` em `src/components/auth/` (email + senha mÃ­n. 8 + confirmar + ciÃªncia da Privacidade + consentimento opcional + links legais + captcha), com anti-enumeraÃ§Ã£o (mesma resposta para sucesso e duplicado) e `privacyPending` via sessionStorage.

Purpose: D2 (formulÃ¡rio restaurado + anti-enumeraÃ§Ã£o), D4/D5 (flag), D12 (legal links).
Output: signup flag on/off + SignupForm.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D2 â€” formulÃ¡rio/anti-enumeraÃ§Ã£o; D4/D5 â€” flag; D12 â€” legal)
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-UI-SPEC.md (SignupForm co-located em components/auth; estados flag on/off; copy anti-enumeraÃ§Ã£o)
@src/app/(auth)/signup/page.tsx (atual "Beta fechado" :13-30 â€” verbatim no flag off)
@src/app/(auth)/login/login-form.tsx (padrÃ£o de form client slate/blue + createBrowserClient :28)
@src/app/(auth)/forgot-password/forgot-password-form.tsx (padrÃ£o emailRedirectTo :22-24)
@src/components/legal/privacy-acknowledge-modal.tsx (modal de ciÃªncia)
@src/components/legal/communications-consent-modal.tsx (consentimento opcional)
@src/app/(auth)/check-email/page.tsx (type=signup :14-16)

<interfaces>
- `SignupForm` em `src/components/auth/signup-form.tsx` (decidido: co-located com GoogleButton/CaptchaField â€” alinhado aos artefatos OpenSpec). Importado por `signup/page.tsx`.
- `supabase.auth.signUp({ email, password, options: { emailRedirectTo: `${getSiteUrl()}/auth/confirm`, captchaToken } })`.
- Anti-enumeraÃ§Ã£o D2: sucesso E "User already registered" â†’ `router.replace("/check-email?type=signup")`. Captcha/erro operacional â†’ "NÃ£o foi possÃ­vel concluir. Tente novamente." (genÃ©rica).
- `privacyPending`/consentimento via `sessionStorage` (padrÃ£o original preservado â€” D16 coordena com PrivacyRecovery no plan 42-11).
- Visual: slate/blue do login-form existente (nÃ£o MASTER tokens) â€” UI-SPEC.
</interfaces>
</context>

<tasks>

<task type="execute">
  <name>Task 1: signup/page.tsx â€” flag on/off</name>
  <files>src/app/(auth)/signup/page.tsx</files>
  <read_first>
    - src/app/(auth)/signup/page.tsx (atual "Beta fechado" :13-30)
    - src/lib/launch-config/config.ts (getLaunchConfig server-only)
    - .planning/phases/42-signup-controlado-elegibilidade-freemium/42-UI-SPEC.md (estados flag on/off)
  </read_first>
  <action>
    Em `src/app/(auth)/signup/page.tsx` (server component):
    1. Ler `const { publicSignupEnabled } = await getLaunchConfig()` (server-side â€” config.ts Ã© `server-only`).
    2. Se `publicSignupEnabled === false` â†’ renderizar o JSX atual "Beta fechado" **verbatim** (:11-34) â€” conteÃºdo inalterado.
    3. Se `true` â†’ renderizar layout de abertura: heading "Criar sua conta" + `<GoogleButton/>` + divider ("ou") + `<SignupForm/>` + links legais (Privacidade/Termos) + link "JÃ¡ tenho uma conta â€” Entrar" (â†’ `/login`).
    O componente pode ficar no prÃ³prio page.tsx (server) com os componentes client como children.
  </action>
  <verify>
    <automated>npm run typecheck; if ($?) { npx vitest run src/__tests__/auth/signup-page.test.tsx }</automated>
  </verify>
  <acceptance_criteria>
    - `signup/page.tsx` chama `getLaunchConfig()` e usa `publicSignupEnabled`
    - Flag off â†’ conteÃºdo "Beta fechado" atual preservado (heading "Beta fechado", link "Solicitar acesso free", "JÃ¡ tenho acesso â€” Entrar")
    - Flag on â†’ rendÃªriza "Criar sua conta", GoogleButton, SignupForm, links legais, "Entrar"
    - `npm run typecheck` â†’ 0 erros
  </acceptance_criteria>
  <done>signup/page.tsx com flag on/off</done>
</task>

<task type="tdd">
  <name>Task 2: SignupForm â€” formulÃ¡rio restaurado + anti-enumeraÃ§Ã£o</name>
  <files>src/components/auth/signup-form.tsx, src/components/auth/__tests__/signup-form.test.tsx</files>
  <read_first>
    - src/app/(auth)/login/login-form.tsx (padrÃ£o form client: createBrowserClient :28, router.replace :41, classes slate/blue :52-116)
    - src/app/(auth)/forgot-password/forgot-password-form.tsx (emailRedirectTo :22-24)
    - src/components/legal/privacy-acknowledge-modal.tsx (modal ciÃªncia)
    - src/components/legal/communications-consent-modal.tsx (consentimento opcional)
    - .planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D2)
  </read_first>
  <action>
    Criar `src/components/auth/signup-form.tsx` ("use client"):
    1. Campos: `email` (type email, required, autoComplete email), `senha` (password, minLength 8), `confirmar senha` (password). ValidaÃ§Ã£o local: `password.length >= 8` e `password === confirm` com mensagens PT-BR ("A senha deve ter pelo menos 8 caracteres." / "As senhas nÃ£o coincidem.").
    2. Checkbox/declaraÃ§Ã£o "Li e declaro ciÃªncia integral da PolÃ­tica de Privacidade" (abre `PrivacyAcknowledgeModal`). Consentimento opcional de comunicaÃ§Ãµes (`CommunicationsConsentModal` ou checkbox). Links para Privacidade e Termos.
    3. `CaptchaField` (42-08 â€” jÃ¡ criado e depende de 42-06? NÃƒO: 42-06 **depende de 42-08**; o componente existe e Ã© importado diretamente, **sem placeholder**). Submit:
       ```ts
       supabase.auth.signUp({ email, password, options: { emailRedirectTo: `${getSiteUrl()}/auth/confirm`, captchaToken } })
       ```
       usando `createBrowserClient()` e `getSiteUrl()`.
    4. Anti-enumeraÃ§Ã£o: sucesso (sem error) OU erro "User already registered" (ou similar do Supabase) â†’ `router.replace("/check-email?type=signup")`. Qualquer outro erro (captcha, rede, operacional) â†’ mensagem genÃ©rica "NÃ£o foi possÃ­vel concluir. Tente novamente." â€” nunca revelar se o email existe.
    5. `privacyPending` no `sessionStorage` (padrÃ£o: apÃ³s confirmaÃ§Ã£o de ciÃªncia, gravar `privacyPending` com `privacyAcknowledged: true` e opt-in, para o PrivacyRecovery processar na primeira autenticaÃ§Ã£o â€” D16; coordenaÃ§Ã£o completa no plan 42-11).
    Red/Green: criar `src/components/auth/__tests__/signup-form.test.tsx` (padrÃ£o do login-form.test.tsx: mock `@/lib/supabase/client` + `next/navigation`) cobrindo: senha < 8 â†’ erro; senha â‰  confirm â†’ erro; ciÃªncia obrigatÃ³ria; consentimento opcional; `signUp` chamado com `emailRedirectTo` + `captchaToken`; sucesso â†’ `/check-email?type=signup`; erro "already registered" â†’ mesma resposta; erro operacional â†’ mensagem genÃ©rica.
  </action>
  <verify>
    <automated>npx vitest run src/components/auth/__tests__/signup-form.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/auth/signup-form.tsx` existe; valida senha â‰¥ 8 e senha === confirmar com mensagens PT-BR
    - `signUp` chamado com `options.emailRedirectTo === "${getSiteUrl()}/auth/confirm"` e `options.captchaToken`
    - Sucesso e "already registered" â†’ `router.replace("/check-email?type=signup")` (mesma resposta)
    - Erro operacional â†’ "NÃ£o foi possÃ­vel concluir. Tente novamente." (nÃ£o revela conta)
    - `npx vitest run src/components/auth/__tests__/signup-form.test.tsx` â†’ 0 falhas
  </acceptance_criteria>
  <done>SignupForm completo com validaÃ§Ã£o + anti-enumeraÃ§Ã£o + testes verdes</done>
</task>

<task type="execute">
  <name>Task 3: Co-migraÃ§Ã£o do teste de signup-page (flag on/off)</name>
  <files>src/__tests__/auth/signup-page.test.tsx</files>
  <read_first>
    - src/__tests__/auth/signup-page.test.tsx (atual :1-34 â€” beta fechado)
    - src/lib/launch-config/__tests__/config.test.ts (padrÃ£o de mock de getLaunchConfig se usado)
  </read_first>
  <action>
    Co-migrar `src/__tests__/auth/signup-page.test.tsx`:
    1. Manter os casos atuais de "Beta fechado" (flag off) â€” mock de `getLaunchConfig()` retornando `publicSignupEnabled: false`.
    2. Adicionar casos flag on: heading "Criar sua conta", botÃ£o "Continuar com Google", formulÃ¡rio (email/senha), links legais.
    Usar `vi.mock("@/lib/launch-config/config", ...)` no teste para controlar a flag.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/auth/signup-page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `signup-page.test.tsx` cobre flag off (Beta fechado) e flag on (formulÃ¡rio + Google)
    - `npx vitest run src/__tests__/auth/signup-page.test.tsx` â†’ 0 falhas
  </acceptance_criteria>
  <done>signup-page.test.tsx co-migrado para flag on/off</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| flag â†” superfÃ­cie | `publicSignupEnabled` decide se o formulÃ¡rio existe â€” mÃ¡-config expÃµe cadastro |
| form â†” Supabase Auth | signUp cria conta â€” abuso/enumeraÃ§Ã£o de email |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-06 | Information Disclosure | EnumeraÃ§Ã£o de email | mitigate | Mesma resposta (redirect /check-email) para sucesso e duplicado; erros operacionais genÃ©ricos (D2) |
| T-42-06a | Elevation of Privilege | CriaÃ§Ã£o de conta | mitigate | Flag default false; validaÃ§Ã£o server-side (plan 42-02); signUp com emailRedirectTo e (no plan 42-08) captchaToken |
| T-42-06b | Tampering | ValidaÃ§Ã£o client | mitigate | ValidaÃ§Ã£o local Ã© UX; seguranÃ§a real em server-side (Supabase auth, captcha server-side) |
| T-42-SC | Tampering | InstalaÃ§Ãµes de pacotes | accept | Nenhum pacote novo |
</threat_model>

<verification>
- `npx vitest run src/components/auth/__tests__/signup-form.test.tsx` â†’ 0 falhas
- `npx vitest run src/__tests__/auth/signup-page.test.tsx` â†’ 0 falhas
- `npm run typecheck` â†’ 0 erros; `npm run lint` â†’ 0 erros
</verification>

<success_criteria>
- `/signup` flag off preserva "Beta fechado" verbatim; flag on mostra formulÃ¡rio + Google
- SignupForm restaurado em `components/auth` com validaÃ§Ã£o (â‰¥8, confirmar), ciÃªncia da Privacidade, consentimento opcional, links legais
- Anti-enumeraÃ§Ã£o: sucesso e duplicado â†’ mesma resposta; erros operacionais genÃ©ricos
</success_criteria>

<output>
Create `.planning/phases/42-signup-controlado-elegibilidade-freemium/42-06-SUMMARY.md` when done
</output>

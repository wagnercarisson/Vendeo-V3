---
phase: 42-signup-controlado-elegibilidade-freemium
plan: 06
type: execute
wave: 3
depends_on: ["42-07", "42-08"]
files_modified:
  - src/app/(auth)/signup/page.tsx
  - src/components/auth/signup-form.tsx
  - src/__tests__/auth/signup-page.test.tsx
autonomous: true
requirements: ["signup-page", "launch-config (Nova flag publicSignupEnabled)", "legal-acceptance-service", "google-oauth-signup", "turnstile-captcha"]

must_haves:
  truths:
    - "`/signup` server component lê `publicSignupEnabled` server-side — flag on → formulário + 'Continuar com Google'; flag off → 'Beta fechado' atual preservado"
    - "**Depende de 42-07 (GoogleButton real) e 42-08 (CaptchaField real)** — NÃO criar placeholders; os componentes já existem quando 42-06 executa"
    - "`SignupForm` em `src/components/auth/signup-form.tsx` (restaurado/modernizado): email, senha mín. 8, confirmar senha, ciência da Privacidade (modal PrivacyAcknowledgeModal), consentimento opcional de comunicações, links Privacidade/Termos"
    - "signUp envia emailRedirectTo `${getSiteUrl()}/auth/confirm` + captchaToken; privacyPending/consentimento via sessionStorage"
    - "Anti-enumeração: sucesso e email duplicado → mesma resposta `/check-email?type=signup`; erro operacional → 'Não foi possível concluir. Tente novamente.'"
    - "Validação local: password.length >= 8, password === confirm (mensagens PT-BR)"
  artifacts:
    - path: "src/components/auth/signup-form.tsx"
      provides: "Formulário restaurado"
      contains: "signUp"
    - path: "src/app/(auth)/signup/page.tsx"
      provides: "Flag on/off"
      contains: "publicSignupEnabled"
  key_links:
    - from: "signup/page.tsx"
      to: "src/components/auth/signup-form.tsx"
      via: "import"
      pattern: "SignupForm"
---

<objective>
Restaurar o cadastro público no `/signup` (D2/D4/D5/D15): server component lê `publicSignupEnabled` (flag off → "Beta fechado" verbatim; flag on → formulário + Google); criar `SignupForm` em `src/components/auth/` (email + senha mín. 8 + confirmar + ciência da Privacidade + consentimento opcional + links legais + captcha), com anti-enumeração (mesma resposta para sucesso e duplicado) e `privacyPending` via sessionStorage.

Purpose: D2 (formulário restaurado + anti-enumeração), D4/D5 (flag), D12 (legal links).
Output: signup flag on/off + SignupForm.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D2 — formulário/anti-enumeração; D4/D5 — flag; D12 — legal)
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-UI-SPEC.md (SignupForm co-located em components/auth; estados flag on/off; copy anti-enumeração)
@src/app/(auth)/signup/page.tsx (atual "Beta fechado" :13-30 — verbatim no flag off)
@src/app/(auth)/login/login-form.tsx (padrão de form client slate/blue + createBrowserClient :28)
@src/app/(auth)/forgot-password/forgot-password-form.tsx (padrão emailRedirectTo :22-24)
@src/components/legal/privacy-acknowledge-modal.tsx (modal de ciência)
@src/components/legal/communications-consent-modal.tsx (consentimento opcional)
@src/app/(auth)/check-email/page.tsx (type=signup :14-16)

<interfaces>
- `SignupForm` em `src/components/auth/signup-form.tsx` (decidido: co-located com GoogleButton/CaptchaField — alinhado aos artefatos OpenSpec). Importado por `signup/page.tsx`.
- `supabase.auth.signUp({ email, password, options: { emailRedirectTo: `${getSiteUrl()}/auth/confirm`, captchaToken } })`.
- Anti-enumeração D2: sucesso E "User already registered" → `router.replace("/check-email?type=signup")`. Captcha/erro operacional → "Não foi possível concluir. Tente novamente." (genérica).
- `privacyPending`/consentimento via `sessionStorage` (padrão original preservado — D16 coordena com PrivacyRecovery no plan 42-11).
- Visual: slate/blue do login-form existente (não MASTER tokens) — UI-SPEC.
</interfaces>
</context>

<tasks>

<task type="execute">
  <name>Task 1: signup/page.tsx — flag on/off</name>
  <files>src/app/(auth)/signup/page.tsx</files>
  <read_first>
    - src/app/(auth)/signup/page.tsx (atual "Beta fechado" :13-30)
    - src/lib/launch-config/config.ts (getLaunchConfig server-only)
    - .planning/phases/42-signup-controlado-elegibilidade-freemium/42-UI-SPEC.md (estados flag on/off)
  </read_first>
  <action>
    Em `src/app/(auth)/signup/page.tsx` (server component):
    1. Ler `const { publicSignupEnabled } = await getLaunchConfig()` (server-side — config.ts é `server-only`).
    2. Se `publicSignupEnabled === false` → renderizar o JSX atual "Beta fechado" **verbatim** (:11-34) — conteúdo inalterado.
    3. Se `true` → renderizar layout de abertura: heading "Criar sua conta" + `<GoogleButton/>` + divider ("ou") + `<SignupForm/>` + links legais (Privacidade/Termos) + link "Já tenho uma conta — Entrar" (→ `/login`).
    O componente pode ficar no próprio page.tsx (server) com os componentes client como children.
  </action>
  <verify>
    <automated>npm run typecheck; if ($?) { npx vitest run src/__tests__/auth/signup-page.test.tsx }</automated>
  </verify>
  <acceptance_criteria>
    - `signup/page.tsx` chama `getLaunchConfig()` e usa `publicSignupEnabled`
    - Flag off → conteúdo "Beta fechado" atual preservado (heading "Beta fechado", link "Solicitar acesso free", "Já tenho acesso — Entrar")
    - Flag on → rendêriza "Criar sua conta", GoogleButton, SignupForm, links legais, "Entrar"
    - `npm run typecheck` → 0 erros
  </acceptance_criteria>
  <done>signup/page.tsx com flag on/off</done>
</task>

<task type="tdd">
  <name>Task 2: SignupForm — formulário restaurado + anti-enumeração</name>
  <files>src/components/auth/signup-form.tsx, src/components/auth/__tests__/signup-form.test.tsx</files>
  <read_first>
    - src/app/(auth)/login/login-form.tsx (padrão form client: createBrowserClient :28, router.replace :41, classes slate/blue :52-116)
    - src/app/(auth)/forgot-password/forgot-password-form.tsx (emailRedirectTo :22-24)
    - src/components/legal/privacy-acknowledge-modal.tsx (modal ciência)
    - src/components/legal/communications-consent-modal.tsx (consentimento opcional)
    - .planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D2)
  </read_first>
  <action>
    Criar `src/components/auth/signup-form.tsx` ("use client"):
    1. Campos: `email` (type email, required, autoComplete email), `senha` (password, minLength 8), `confirmar senha` (password). Validação local: `password.length >= 8` e `password === confirm` com mensagens PT-BR ("A senha deve ter pelo menos 8 caracteres." / "As senhas não coincidem.").
    2. Checkbox/declaração "Li e declaro ciência integral da Política de Privacidade" (abre `PrivacyAcknowledgeModal`). Consentimento opcional de comunicações (`CommunicationsConsentModal` ou checkbox). Links para Privacidade e Termos.
    3. `CaptchaField` (42-08 — já criado e depende de 42-06? NÃO: 42-06 **depende de 42-08**; o componente existe e é importado diretamente, **sem placeholder**). Submit:
       ```ts
       supabase.auth.signUp({ email, password, options: { emailRedirectTo: `${getSiteUrl()}/auth/confirm`, captchaToken } })
       ```
       usando `createBrowserClient()` e `getSiteUrl()`.
    4. Anti-enumeração: sucesso (sem error) OU erro "User already registered" (ou similar do Supabase) → `router.replace("/check-email?type=signup")`. Qualquer outro erro (captcha, rede, operacional) → mensagem genérica "Não foi possível concluir. Tente novamente." — nunca revelar se o email existe.
    5. `privacyPending` no `sessionStorage` (padrão: após confirmação de ciência, gravar `privacyPending` com `privacyAcknowledged: true` e opt-in, para o PrivacyRecovery processar na primeira autenticação — D16; coordenação completa no plan 42-11).
    Red/Green: criar `src/components/auth/__tests__/signup-form.test.tsx` (padrão do login-form.test.tsx: mock `@/lib/supabase/client` + `next/navigation`) cobrindo: senha < 8 → erro; senha ≠ confirm → erro; ciência obrigatória; consentimento opcional; `signUp` chamado com `emailRedirectTo` + `captchaToken`; sucesso → `/check-email?type=signup`; erro "already registered" → mesma resposta; erro operacional → mensagem genérica.
  </action>
  <verify>
    <automated>npx vitest run src/components/auth/__tests__/signup-form.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/auth/signup-form.tsx` existe; valida senha ≥ 8 e senha === confirmar com mensagens PT-BR
    - `signUp` chamado com `options.emailRedirectTo === "${getSiteUrl()}/auth/confirm"` e `options.captchaToken`
    - Sucesso e "already registered" → `router.replace("/check-email?type=signup")` (mesma resposta)
    - Erro operacional → "Não foi possível concluir. Tente novamente." (não revela conta)
    - `npx vitest run src/components/auth/__tests__/signup-form.test.tsx` → 0 falhas
  </acceptance_criteria>
  <done>SignupForm completo com validação + anti-enumeração + testes verdes</done>
</task>

<task type="execute">
  <name>Task 3: Co-migração do teste de signup-page (flag on/off)</name>
  <files>src/__tests__/auth/signup-page.test.tsx</files>
  <read_first>
    - src/__tests__/auth/signup-page.test.tsx (atual :1-34 — beta fechado)
    - src/lib/launch-config/__tests__/config.test.ts (padrão de mock de getLaunchConfig se usado)
  </read_first>
  <action>
    Co-migrar `src/__tests__/auth/signup-page.test.tsx`:
    1. Manter os casos atuais de "Beta fechado" (flag off) — mock de `getLaunchConfig()` retornando `publicSignupEnabled: false`.
    2. Adicionar casos flag on: heading "Criar sua conta", botão "Continuar com Google", formulário (email/senha), links legais.
    Usar `vi.mock("@/lib/launch-config/config", ...)` no teste para controlar a flag.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/auth/signup-page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `signup-page.test.tsx` cobre flag off (Beta fechado) e flag on (formulário + Google)
    - `npx vitest run src/__tests__/auth/signup-page.test.tsx` → 0 falhas
  </acceptance_criteria>
  <done>signup-page.test.tsx co-migrado para flag on/off</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| flag ↔ superfície | `publicSignupEnabled` decide se o formulário existe — má-config expõe cadastro |
| form ↔ Supabase Auth | signUp cria conta — abuso/enumeração de email |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-06 | Information Disclosure | Enumeração de email | mitigate | Mesma resposta (redirect /check-email) para sucesso e duplicado; erros operacionais genéricos (D2) |
| T-42-06a | Elevation of Privilege | Criação de conta | mitigate | Flag default false; validação server-side (plan 42-02); signUp com emailRedirectTo e (no plan 42-08) captchaToken |
| T-42-06b | Tampering | Validação client | mitigate | Validação local é UX; segurança real em server-side (Supabase auth, captcha server-side) |
| T-42-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo |
</threat_model>

<verification>
- `npx vitest run src/components/auth/__tests__/signup-form.test.tsx` → 0 falhas
- `npx vitest run src/__tests__/auth/signup-page.test.tsx` → 0 falhas
- `npm run typecheck` → 0 erros; `npm run lint` → 0 erros
</verification>

<success_criteria>
- `/signup` flag off preserva "Beta fechado" verbatim; flag on mostra formulário + Google
- SignupForm restaurado em `components/auth` com validação (≥8, confirmar), ciência da Privacidade, consentimento opcional, links legais
- Anti-enumeração: sucesso e duplicado → mesma resposta; erros operacionais genéricos
</success_criteria>

<output>
Create `.planning/phases/42-signup-controlado-elegibilidade-freemium/42-06-SUMMARY.md` when done
</output>
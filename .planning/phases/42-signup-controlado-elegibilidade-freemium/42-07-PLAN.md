---
phase: 42-signup-controlado-elegibilidade-freemium
plan: 07
type: execute
wave: 3
depends_on: ["42-02"]
files_modified:
  - src/components/auth/google-button.tsx
  - src/app/auth/callback/route.ts
  - src/app/auth/callback/__tests__/route.test.ts
autonomous: true
requirements: ["google-oauth-signup", "oauth-auth-callback", "privacy-acknowledgement"]

must_haves:
  truths:
    - "**Depende de 42-02, NÃƒO de 42-06** â€” GoogleButton e callback sÃ£o independentes do signup (flag lida em 42-02)"
    - "`GoogleButton` chama `signInWithOAuth({ provider: 'google', options: { redirectTo: `${getSiteUrl()}/auth/callback` , scopes: "openid email profile" } })` â€” SEM captchaToken (D15)"
    - "`/auth/callback` lÃª `code` â†’ `exchangeCodeForSession(code)` (PKCE); `/auth/confirm` (email/OTP verifyOtp) intacto"
    - "Allowlist de `next`: VALID_NEXT = ['/loja', '/dashboard']; fallback `/loja`; externo bloqueado; '/' e '/onboarding' nunca vÃ¡lidos"
    - "Erro genÃ©rico: code invÃ¡lido/expirado â†’ `/login?error=oauth_failed`; sucesso â†’ `/loja` â†’ PrivacyGate reusado"
    - "`enable_manual_linking = false` preservado (identity linking automÃ¡tico por email verificado)"
  artifacts:
    - path: "src/components/auth/google-button.tsx"
      provides: "BotÃ£o Google"
      contains: "signInWithOAuth"
    - path: "src/app/auth/callback/route.ts"
      provides: "Callback PKCE"
      contains: "exchangeCodeForSession"
  key_links:
    - from: "GoogleButton"
      to: "src/app/auth/callback/route.ts"
      via: "redirectTo"
      pattern: "auth/callback"
---

<objective>
Implementar o Google OAuth como entrada principal (D15/D16): `GoogleButton` (chama `signInWithOAuth` com redirect para `/auth/callback`, **sem captchaToken**) e a rota `/auth/callback` (lÃª `code`, `exchangeCodeForSession` PKCE, allowlist de `next`, erro genÃ©rico anti-enumeraÃ§Ã£o, sucesso â†’ `/loja` â†’ PrivacyGate reusado). `/auth/confirm` (email/OTP) permanece intacto.

Purpose: D15 (OAuth principal) + D16 (callback + PrivacyGate).
Output: GoogleButton + callback + testes.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D15 â€” OAuth principal; D16 â€” callback/PrivacyGate)
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-UI-SPEC.md (GoogleButton â€” outline em auth, "G" oficial, sem captcha; copy "Continuar com Google")
@src/app/auth/confirm/route.ts (padrÃ£o de route + VALID_NEXT :4 + redirect :16, :27)
@src/lib/supabase/server.ts (createServerClient)
@src/lib/supabase/client.ts (createBrowserClient)
@src/lib/supabase/site-url.ts (getSiteUrl)

<interfaces>
- `createServerClient()` de `src/lib/supabase/server`; `createBrowserClient()` de `src/lib/supabase/client`.
- `getSiteUrl()` de `src/lib/supabase/site-url` (exige `NEXT_PUBLIC_SITE_URL`).
- Allowlist D16: `VALID_NEXT = ["/loja", "/dashboard"]`; fallback `/loja`; `"/"` e `/onboarding` NUNCA vÃ¡lidos; redirect externo (http(s) fora do domÃ­nio) bloqueado â†’ fallback.
- `/auth/confirm` NÃƒO Ã© tocado (email/OTP verifyOtp).
- PrivacyGate reusado (nÃ£o criar novo componente em components/auth).
- `enable_manual_linking = false` (config â€” nÃ£o alterar).
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: GoogleButton</name>
  <files>src/components/auth/google-button.tsx, src/components/auth/__tests__/google-button.test.tsx</files>
  <read_first>
    - src/app/(auth)/login/login-form.tsx (padrÃ£o de botÃ£o submit client :98-109; createBrowserClient :28)
    - src/lib/supabase/client.ts (createBrowserClient)
    - src/lib/supabase/site-url.ts (getSiteUrl)
    - .planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D15)
    - .planning/phases/42-signup-controlado-elegibilidade-freemium/42-UI-SPEC.md (GoogleButton)
  </read_first>
  <action>
    Criar `src/components/auth/google-button.tsx` ("use client"):
    1. BotÃ£o full-width (auth) ou inline (landing) com label "Continuar com Google" + Ã­cone oficial Google "G" (inline SVG ~18px multi-color).
    2. OnClick: `const supabase = createBrowserClient(); await supabase.auth.signInWithOAuth({ provider: "google", options: { redirectTo: `${getSiteUrl()}/auth/callback` , scopes: "openid email profile" } })` â€” **SEM `captchaToken`** (D15/D3).
    3. Loading state (spinner + disabled, `minHeight: 44px`). Erro â†’ mensagem genÃ©rica (nÃ£o enumera).
    4. Visual: auth surfaces â†’ outline (`border border-slate-600 text-slate-50 hover:bg-slate-800`), consistente com login-form; landing â†’ green solid (`bg-accent-green`) conforme UI-SPEC.
    Red/Green: `google-button.test.tsx` (mock supabase client + site-url): chama `signInWithOAuth` com `provider: "google"`, `redirectTo` terminando em `/auth/callback` e `scopes: "openid email profile"`; **não** inclui `captchaToken` na chamada; loading ao clicar.
  </action>
  <verify>
    <automated>npx vitest run src/components/auth/__tests__/google-button.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `google-button.tsx` existe com label "Continuar com Google" + Ã­cone "G" oficial
    - `signInWithOAuth` chamado com `provider: "google"`, `options.redirectTo` contendo `/auth/callback` e `options.scopes` exatamente `"openid email profile"` (D15 — mínimo, sem permissões Gmail/Drive)
    - A chamada NÃƒO inclui `captchaToken`
    - `npx vitest run src/components/auth/__tests__/google-button.test.tsx` â†’ 0 falhas
  </acceptance_criteria>
  <done>GoogleButton com testes verdes</done>
</task>

<task type="tdd">
  <name>Task 2: Rota /auth/callback (PKCE + allowlist)</name>
  <files>src/app/auth/callback/route.ts, src/app/auth/callback/__tests__/route.test.ts</files>
  <read_first>
    - src/app/auth/confirm/route.ts (padrÃ£o de route: createServerClient :19, redirect :16/27, VALID_NEXT :4)
    - src/lib/supabase/server.ts (createServerClient)
    - .planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D16)
  </read_first>
  <action>
    Criar `src/app/auth/callback/route.ts` (GET):
    1. `const { searchParams } = request.nextUrl; const code = searchParams.get("code"); const rawNext = searchParams.get("next") || "";`
    2. `VALID_NEXT = ["/loja", "/dashboard"] as const`. `safeNext` = se `rawNext` estÃ¡ na lista â†’ `rawNext`; senÃ£o â†’ `/loja` (fallback). **Bloquear** redirect externo (qualquer `next` que nÃ£o esteja na allowlist) â€” nunca redirecionar fora do domÃ­nio. `"/"` e `/onboarding` nÃ£o sÃ£o vÃ¡lidos.
    3. Se `!code` â†’ `NextResponse.redirect(new URL("/login?error=oauth_failed", request.url), { status: 302 })` (erro genÃ©rico anti-enumeraÃ§Ã£o).
    4. `const supabase = await createServerClient(); const { error } = await supabase.auth.exchangeCodeForSession(code);`
    5. Se `error` â†’ redirect `/login?error=oauth_failed`.
    6. Sucesso â†’ `NextResponse.redirect(new URL(safeNext, request.url), { status: 302 })` â€” `/loja` por padrÃ£o (rota protegida) â†’ PrivacyGate.
    7. NÃƒO tocar `src/app/auth/confirm/route.ts`.
    Red/Green: `route.test.ts` (mock `createServerClient` + `exchangeCodeForSession`): (a) `code` valido + `next` na allowlist -> `exchangeCodeForSession(code)` chamado + redirect `safeNext`; (b) `code` valido + `next` externo/`/`/`/onboarding` -> fallback `/loja` (nunca redireciona fora do dominio); (c) `code` ausente -> redirect `/login?error=oauth_failed`; (d) `exchangeCodeForSession` retorna erro -> redirect `/login?error=oauth_failed`.
  </action>
  <verify>
    <automated>npx vitest run src/app/auth/callback/__tests__/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/auth/callback/route.ts` existe; chama `exchangeCodeForSession(code)` quando `code` presente
    - `VALID_NEXT = ["/loja", "/dashboard"]`; fallback `/loja`; `next` externo e `/onboarding` â†’ fallback (nunca redireciona fora)
    - `code` invÃ¡lido/expirado/ausente â†’ `/login?error=oauth_failed`
    - `/auth/confirm/route.ts` inalterado
    - `npx vitest run src/app/auth/callback/__tests__/route.test.ts` â†’ 0 falhas
  </acceptance_criteria>
  <done>Callback PKCE com allowlist e testes verdes</done>
</task>

<task type="execute">
  <name>Task 3: IntegraÃ§Ã£o PrivacyGate + enable_manual_linking</name>
  <files>src/app/(app)/layout.tsx, src/components/legal/privacy-gate.tsx</files>
  <read_first>
    - src/components/legal/privacy-gate.tsx (gate existente :18-71 â€” reusar verbatim)
    - src/lib/legal/privacy.ts (hasValidPrivacyAcknowledgement â€” usado pelo layout `(app)` para o prop `acknowledged`; NÃƒO Ã© acceptance-service)
    - src/app/(app)/layout.tsx (:20-35 â€” PrivacyGate com acknowledged via hasValidPrivacyAcknowledgement)
    - .planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D16)
  </read_first>
  <action>
    - Verificar que o fluxo pÃ³s-callback (success â†’ `/loja`) encontra o PrivacyGate jÃ¡ ativo no layout autenticado `(app)` (reusar o componente existente â€” **sem novo componente em `components/auth`**).
    - O layout `(app)` jÃ¡ renderiza `PrivacyGate` com `acknowledged` via `hasValidPrivacyAcknowledgement` (privacy.ts:25-38) â€” confirmar que o caminho OAuth chega ao mesmo gate (sem alteraÃ§Ã£o ou com alteraÃ§Ã£o mÃ­nima de coordenaÃ§Ã£o â€” o plan 42-11 completa a coordenaÃ§Ã£o Ãºnica PrivacyGate Ã— PrivacyRecovery).
    - Confirmar `enable_manual_linking = false` no `supabase/config.toml` (nÃ£o alterar â€” jÃ¡ no plan 42-02; verificar presente).
  </action>
  <verify>
    <automated>Select-String -LiteralPath "supabase\config.toml" -Pattern "enable_manual_linking" | Select-Object -First 2; Select-String -LiteralPath "src\app\(app)\layout.tsx" -Pattern "PrivacyGate"</automated>
  </verify>
  <acceptance_criteria>
    - `PrivacyGate` existente Ã© reusado no caminho pÃ³s-callback (sem novo componente de gate em `components/auth`)
    - `supabase/config.toml` contÃ©m `enable_manual_linking = false`
  </acceptance_criteria>
  <done>IntegraÃ§Ã£o PrivacyGate confirmada; manual_linking preservado</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OAuth â†” callback | `code` trocado por sessÃ£o â€” PKCE no servidor |
| redirect â†” destino | `next` controla pÃ³s-login â€” open redirect se mal validado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-07 | Spoofing | `code` OAuth invÃ¡lido | mitigate | `exchangeCodeForSession` server-side; erro genÃ©rico `/login?error=oauth_failed` (nÃ£o enumera) |
| T-42-07a | Tampering | Open redirect via `next` | mitigate | Allowlist estrita `VALID_NEXT = ['/loja','/dashboard']`; fallback `/loja`; externo/`/`/`/onboarding` bloqueados |
| T-42-07b | Elevation of Privilege | Nova identidade | mitigate | Controle via `enable_signup` (D13) â€” fora deste plan; `enable_manual_linking = false` preservado |
| T-42-SC | Tampering | InstalaÃ§Ãµes de pacotes | accept | Nenhum pacote novo |
</threat_model>

<verification>
- `npx vitest run src/components/auth/__tests__/google-button.test.tsx` â†’ 0 falhas
- `npx vitest run src/app/auth/callback/__tests__/route.test.ts` â†’ 0 falhas
- `npm run typecheck` â†’ 0 erros
- `src/app/auth/confirm/route.ts` inalterado (git diff vazio para esse arquivo)
</verification>

<success_criteria>
- GoogleButton chama `signInWithOAuth` (Google, redirect /auth/callback) sem captchaToken
- `/auth/callback` troca code PKCE, valida allowlist de next, redireciona erro genÃ©rico e sucesso â†’ /loja
- PrivacyGate existente reusado; `enable_manual_linking = false` preservado
</success_criteria>

<output>
Create `.planning/phases/42-signup-controlado-elegibilidade-freemium/42-07-SUMMARY.md` when done
</output>

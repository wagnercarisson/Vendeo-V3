---
phase: 42-signup-controlado-elegibilidade-freemium
plan: 07
type: execute
wave: 2
depends_on: ["42-06"]
files_modified:
  - src/components/auth/google-button.tsx
  - src/app/auth/callback/route.ts
  - src/app/auth/callback/__tests__/route.test.ts
autonomous: true
requirements: ["google-oauth-signup", "oauth-auth-callback", "privacy-acknowledgement"]

must_haves:
  truths:
    - "`GoogleButton` chama `signInWithOAuth({ provider: 'google', options: { redirectTo: `${getSiteUrl()}/auth/callback` } })` — SEM captchaToken (D15)"
    - "`/auth/callback` lê `code` → `exchangeCodeForSession(code)` (PKCE); `/auth/confirm` (email/OTP verifyOtp) intacto"
    - "Allowlist de `next`: VALID_NEXT = ['/loja', '/dashboard']; fallback `/loja`; externo bloqueado; '/' e '/onboarding' nunca válidos"
    - "Erro genérico: code inválido/expirado → `/login?error=oauth_failed`; sucesso → `/loja` → PrivacyGate reusado"
    - "`enable_manual_linking = false` preservado (identity linking automático por email verificado)"
  artifacts:
    - path: "src/components/auth/google-button.tsx"
      provides: "Botão Google"
      contains: "signInWithOAuth"
    - path: "src/app/auth/callback/route.ts"
      provides: "Callback PKCE"
      contains: "exchangeCodeForSession"
  key_links:
    - from: "GoogleButton"
      to: "src/app/auth/callback/route.ts"
      via: "redirectTo"
      pattern: "auth/callback"
---

<objective>
Implementar o Google OAuth como entrada principal (D15/D16): `GoogleButton` (chama `signInWithOAuth` com redirect para `/auth/callback`, **sem captchaToken**) e a rota `/auth/callback` (lê `code`, `exchangeCodeForSession` PKCE, allowlist de `next`, erro genérico anti-enumeração, sucesso → `/loja` → PrivacyGate reusado). `/auth/confirm` (email/OTP) permanece intacto.

Purpose: D15 (OAuth principal) + D16 (callback + PrivacyGate).
Output: GoogleButton + callback + testes.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D15 — OAuth principal; D16 — callback/PrivacyGate)
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-UI-SPEC.md (GoogleButton — outline em auth, "G" oficial, sem captcha; copy "Continuar com Google")
@src/app/auth/confirm/route.ts (padrão de route + VALID_NEXT :4 + redirect :16, :27)
@src/lib/supabase/server.ts (createServerClient)
@src/lib/supabase/client.ts (createBrowserClient)
@src/lib/supabase/site-url.ts (getSiteUrl)

<interfaces>
- `createServerClient()` de `src/lib/supabase/server`; `createBrowserClient()` de `src/lib/supabase/client`.
- `getSiteUrl()` de `src/lib/supabase/site-url` (exige `NEXT_PUBLIC_SITE_URL`).
- Allowlist D16: `VALID_NEXT = ["/loja", "/dashboard"]`; fallback `/loja`; `"/"` e `/onboarding` NUNCA válidos; redirect externo (http(s) fora do domínio) bloqueado → fallback.
- `/auth/confirm` NÃO é tocado (email/OTP verifyOtp).
- PrivacyGate reusado (não criar novo componente em components/auth).
- `enable_manual_linking = false` (config — não alterar).
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: GoogleButton</name>
  <files>src/components/auth/google-button.tsx, src/components/auth/__tests__/google-button.test.tsx</files>
  <read_first>
    - src/app/(auth)/login/login-form.tsx (padrão de botão submit client :98-109; createBrowserClient :28)
    - src/lib/supabase/client.ts (createBrowserClient)
    - src/lib/supabase/site-url.ts (getSiteUrl)
    - .planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D15)
    - .planning/phases/42-signup-controlado-elegibilidade-freemium/42-UI-SPEC.md (GoogleButton)
  </read_first>
  <action>
    Criar `src/components/auth/google-button.tsx` ("use client"):
    1. Botão full-width (auth) ou inline (landing) com label "Continuar com Google" + ícone oficial Google "G" (inline SVG ~18px multi-color).
    2. OnClick: `const supabase = createBrowserClient(); await supabase.auth.signInWithOAuth({ provider: "google", options: { redirectTo: `${getSiteUrl()}/auth/callback` } })` — **SEM `captchaToken`** (D15/D3).
    3. Loading state (spinner + disabled, `minHeight: 44px`). Erro → mensagem genérica (não enumera).
    4. Visual: auth surfaces → outline (`border border-slate-600 text-slate-50 hover:bg-slate-800`), consistente com login-form; landing → green solid (`bg-accent-green`) conforme UI-SPEC.
    Red/Green: `google-button.test.tsx` (mock supabase client + site-url): chama `signInWithOAuth` com `provider: "google"` e `redirectTo` terminando em `/auth/callback`; **não** inclui `captchaToken` na chamada; loading ao clicar.
  </action>
  <verify>
    <automated>npx vitest run src/components/auth/__tests__/google-button.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `google-button.tsx` existe com label "Continuar com Google" + ícone "G" oficial
    - `signInWithOAuth` chamado com `provider: "google"` e `options.redirectTo` contendo `/auth/callback`
    - A chamada NÃO inclui `captchaToken`
    - `npx vitest run src/components/auth/__tests__/google-button.test.tsx` → 0 falhas
  </acceptance_criteria>
  <done>GoogleButton com testes verdes</done>
</task>

<task type="tdd">
  <name>Task 2: Rota /auth/callback (PKCE + allowlist)</name>
  <files>src/app/auth/callback/route.ts, src/app/auth/callback/__tests__/route.test.ts</files>
  <read_first>
    - src/app/auth/confirm/route.ts (padrão de route: createServerClient :19, redirect :16/27, VALID_NEXT :4)
    - src/lib/supabase/server.ts (createServerClient)
    - .planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D16)
  </read_first>
  <action>
    Criar `src/app/auth/callback/route.ts` (GET):
    1. `const { searchParams } = request.nextUrl; const code = searchParams.get("code"); const rawNext = searchParams.get("next") || "";`
    2. `VALID_NEXT = ["/loja", "/dashboard"] as const`. `safeNext` = se `rawNext` está na lista → `rawNext`; senão → `/loja` (fallback). **Bloquear** redirect externo (qualquer `next` que não esteja na allowlist) — nunca redirecionar fora do domínio. `"/"` e `/onboarding` não são válidos.
    3. Se `!code` → `NextResponse.redirect(new URL("/login?error=oauth_failed", request.url), { status: 302 })` (erro genérico anti-enumeração).
    4. `const supabase = await createServerClient(); const { error } = await supabase.auth.exchangeCodeForSession(code);`
    5. Se `error` → redirect `/login?error=oauth_failed`.
    6. Sucesso → `NextResponse.redirect(new URL(safeNext, request.url), { status: 302 })` — `/loja` por padrão (rota protegida) → PrivacyGate.
    7. NÃO tocar `src/app/auth/confirm/route.ts`.
    Red/Green: `route.test.ts` (mock de `createServerClient` + `exchangeCodeForSession`): code válido → exchange chamado → redirect `/loja`; code inválido → `/login?error=oauth_failed`; `next` externo → bloqueado (fallback `/loja`); `next=/onboarding` → fallback `/loja`; sem code → erro genérico.
  </action>
  <verify>
    <automated>npx vitest run src/app/auth/callback/__tests__/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/auth/callback/route.ts` existe; chama `exchangeCodeForSession(code)` quando `code` presente
    - `VALID_NEXT = ["/loja", "/dashboard"]`; fallback `/loja`; `next` externo e `/onboarding` → fallback (nunca redireciona fora)
    - `code` inválido/expirado/ausente → `/login?error=oauth_failed`
    - `/auth/confirm/route.ts` inalterado
    - `npx vitest run src/app/auth/callback/__tests__/route.test.ts` → 0 falhas
  </acceptance_criteria>
  <done>Callback PKCE com allowlist e testes verdes</done>
</task>

<task type="execute">
  <name>Task 3: Integração PrivacyGate + enable_manual_linking</name>
  <files>src/app/(app)/loja/ (ou layout (app) conforme padrão), src/components/legal/privacy-gate.tsx</files>
  <read_first>
    - src/components/legal/privacy-gate.tsx (gate existente :18-71 — reusar verbatim)
    - src/lib/legal/acceptance-service.ts (getAcceptanceStatus :43-62)
    - .planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D16)
  </read_first>
  <action>
    - Verificar que o fluxo pós-callback (success → `/loja`) encontra o PrivacyGate já ativo no layout autenticado `(app)` (reusar o componente existente — **sem novo componente em `components/auth`**).
    - Se o layout `(app)` já renderiza `PrivacyGate` com `acknowledged` (via `getAcceptanceStatus`), apenas confirmar que o caminho OAuth chega ao mesmo gate (sem alteração ou com alteração mínima de coordenação — o plan 42-11 completa a coordenação única PrivacyGate × PrivacyRecovery).
    - Confirmar `enable_manual_linking = false` no `supabase/config.toml` (não alterar — já no plan 42-02; verificar presente).
  </action>
  <verify>
    <automated>Select-String -LiteralPath "supabase\config.toml" -Pattern "enable_manual_linking" | Select-Object -First 2; Select-String -LiteralPath "src\app\(app)\layout.tsx" -Pattern "PrivacyGate"</automated>
  </verify>
  <acceptance_criteria>
    - `PrivacyGate` existente é reusado no caminho pós-callback (sem novo componente de gate em `components/auth`)
    - `supabase/config.toml` contém `enable_manual_linking = false`
  </acceptance_criteria>
  <done>Integração PrivacyGate confirmada; manual_linking preservado</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OAuth ↔ callback | `code` trocado por sessão — PKCE no servidor |
| redirect ↔ destino | `next` controla pós-login — open redirect se mal validado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-07 | Spoofing | `code` OAuth inválido | mitigate | `exchangeCodeForSession` server-side; erro genérico `/login?error=oauth_failed` (não enumera) |
| T-42-07a | Tampering | Open redirect via `next` | mitigate | Allowlist estrita `VALID_NEXT = ['/loja','/dashboard']`; fallback `/loja`; externo/`/`/`/onboarding` bloqueados |
| T-42-07b | Elevation of Privilege | Nova identidade | mitigate | Controle via `enable_signup` (D13) — fora deste plan; `enable_manual_linking = false` preservado |
| T-42-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo |
</threat_model>

<verification>
- `npx vitest run src/components/auth/__tests__/google-button.test.tsx` → 0 falhas
- `npx vitest run src/app/auth/callback/__tests__/route.test.ts` → 0 falhas
- `npm run typecheck` → 0 erros
- `src/app/auth/confirm/route.ts` inalterado (git diff vazio para esse arquivo)
</verification>

<success_criteria>
- GoogleButton chama `signInWithOAuth` (Google, redirect /auth/callback) sem captchaToken
- `/auth/callback` troca code PKCE, valida allowlist de next, redireciona erro genérico e sucesso → /loja
- PrivacyGate existente reusado; `enable_manual_linking = false` preservado
</success_criteria>

<output>
Create `.planning/phases/42-signup-controlado-elegibilidade-freemium/42-07-SUMMARY.md` when done
</output>
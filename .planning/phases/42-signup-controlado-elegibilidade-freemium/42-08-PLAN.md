---
phase: 42-signup-controlado-elegibilidade-freemium
plan: 08
type: execute
wave: 2
depends_on: ["42-02"]
files_modified:
  - src/components/auth/captcha-field.tsx
  - src/components/auth/__tests__/captcha-field.test.tsx
  - src/app/(auth)/login/login-form.tsx
  - src/app/(auth)/forgot-password/forgot-password-form.tsx
  - src/lib/supabase/client.ts
autonomous: true
requirements: ["turnstile-captcha", "login-page"]

must_haves:
  truths:
    - "**Depende de 42-02, NÃO de 42-06/42-07** — CaptchaField é independente; cria o componente e integra em login/forgot-password (arquivos EXISTENTES). A integração no signup-form acontece em 42-06 (que depende de 42-08) — NÃO aqui, para evitar import de arquivo inexistente"
    - "`CaptchaField` reutilizável em `src/components/auth/captcha-field.tsx` — label + script turnstile + `<div class=cf-turnstile>` + `onVerify(token)`; id padrão `captcha-<chave>`; a11y"
    - "Token exposto via `onVerify` ao formulário pai; form envia `captchaToken` no `signUp`/`signInWithPassword`/`resetPasswordForEmail`"
    - "Script turnstile carregado via `useEffect` (regiões: cloudflare.com / EAST); `cf-turnstile` render custom widget; reset a cada submit"
    - "Aplicação: signup-form (42-06), login-form, forgot-password-form — chave pública de env `NEXT_PUBLIC_TURNSTILE_SITE_KEY`"
  artifacts:
    - path: "src/components/auth/captcha-field.tsx"
      provides: "Widget reutilizável"
      contains: "cf-turnstile"
  key_links:
    - from: "captcha-field.tsx"
      to: "supabase/auth"
      via: "captchaToken"
      pattern: "onVerify"
---

<objective>
Criar o `CaptchaField` reutilizável (D3) e aplicá-lo a login e recuperação de senha (arquivos existentes): widget Turnstile (Cloudflare, script via useEffect, regiões EAST), token via `onVerify`, e envio como `captchaToken` nas chamadas Supabase. A integração no signup-form acontece em 42-06 (que depende de 42-08) — este plan NÃO toca em signup-form.

Purpose: D3 — Turnstile em cadastro/login/recuperação; OAuth SEM captcha (D15).
Output: CaptchaField + integração login/recuperação.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D3 — Turnstile; D15 — OAuth sem captcha)
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-UI-SPEC.md (CaptchaField — a11y, label, hints; NÃO pode quebrar signup/login/recuperação)
@src/lib/supabase/client.ts (createBrowserClient — getSupabaseClient; expõe config)
@src/app/(auth)/login/login-form.tsx (integrar)
@src/app/(auth)/forgot-password/forgot-password-form.tsx (integrar)

<interfaces>
- `CaptchaField` em `src/components/auth/captcha-field.tsx` ("use client"):
  - props: `{ onVerify: (token: string | null) => void; label?: string; hint?: string; className?: string }`.
  - Render: `<label>` (sr-only ok) + `<div id="captcha-<uid>" className="cf-turnstile" data-sitekey data-callback data-theme="dark" data-size="flexible" aria-label="Verificação de segurança (não é robô)" />`.
  - `useEffect`: injeta `<script src="https://challenges.cloudflare.com/turnstile/v0/api.js?render=explicit" async defer>` (fallback `?onload=` padrão se necessário); inicializa `window.turnstile.render(container, { sitekey, callback: onVerify, ... })`; cleanup `window.turnstile.reset()`/remove script.
  - Retorna `null` se `NEXT_PUBLIC_TURNSTILE_SITE_KEY` ausente (renderização protegida server/client).
- login/forgot-password (arquivos existentes): mesmo padrão (token obrigatório para submit).
- **signup-form NÃO é tocado aqui** — 42-06 (dependente de 42-08) integra o CaptchaField no form de cadastro.
- **OAuth (GoogleButton, 42-07) NÃO recebe captchaToken** (D15).
- `supabase/client.ts`: garantir `NEXT_PUBLIC_TURNSTILE_SITE_KEY` acessível (getter).
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: CaptchaField — widget reutilizável</name>
  <files>src/components/auth/captcha-field.tsx, src/components/auth/__tests__/captcha-field.test.tsx</files>
  <read_first>
    - .planning/phases/42-signup-controlado-elegibilidade-freemium/42-UI-SPEC.md (CaptchaField)
    - src/components/ui/input.tsx (padrão de campo — labels/ids)
    - src/app/(auth)/login/login-form.tsx (padrão de componente client)
  </read_first>
  <action>
    Criar `src/components/auth/captcha-field.tsx` ("use client"):
    1. Props: `{ onVerify: (token: string | null) => void; label?: string; hint?: string; className?: string }`.
    2. `useEffect`: adicionar `<script src="https://challenges.cloudflare.com/turnstile/v0/api.js?render=explicit" async defer>` (duplicado ok); depois `window.turnstile.render("#captcha-<uid>", { sitekey: NEXT_PUBLIC_TURNSTILE_SITE_KEY, callback: (token) => onVerify(token), "expired-callback": () => onVerify(null), "error-callback": () => onVerify(null), theme: "dark", size: "flexible" })`. Cleanup: `window.turnstile.remove("#captcha-<uid>")` (ou reset) + remover script (com guarda de duplicados via data-attributo).
    3. Render: `<div>` com label (sr-only "Verificação de segurança"), `<div id="captcha-<uid>" className="cf-turnstile" data-sitekey={...} aria-label="Verificação de segurança (não é robô)" />` e hint opcional. **Não criar widget se `!NEXT_PUBLIC_TURNSTILE_SITE_KEY`** (retorna `null`).
    4. Registrar um efeito pós-hydration para chamar `onVerify(null)` no mount (token vazio até resolução).
    Red/Green: `captcha-field.test.tsx` (mock `window.turnstile` + script): renderiza div `.cf-turnstile` quando site key presente; **não** renderiza quando ausente; `onVerify` chamado com token no callback; `onVerify(null)` no expired/error-callback; cleanup chama remove.
  </action>
  <verify>
    <automated>npx vitest run src/components/auth/__tests__/captcha-field.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `captcha-field.tsx` existe com div `.cf-turnstile`, callback `onVerify`, `expired-callback` e `error-callback` → `onVerify(null)`
    - Script turnstile injetado via useEffect (com dedupe); cleanup remove/reset
    - Retorna `null` quando `NEXT_PUBLIC_TURNSTILE_SITE_KEY` ausente
    - `npx vitest run src/components/auth/__tests__/captcha-field.test.tsx` → 0 falhas
  </acceptance_criteria>
  <done>CaptchaField com testes verdes</done>
</task>

<task type="execute">
  <name>Task 2: Integrar CaptchaField em login e recuperação</name>
  <files>src/app/(auth)/login/login-form.tsx, src/app/(auth)/forgot-password/forgot-password-form.tsx</files>
  <read_first>
    - src/app/(auth)/login/login-form.tsx (login client :21-52)
    - src/app/(auth)/forgot-password/forgot-password-form.tsx (:8-30)
  </read_first>
  <action>
    - `login-form.tsx`: adicionar `<CaptchaField onVerify={setCaptchaToken} />` (antes do submit); `signInWithPassword({ email, password, options: { captchaToken } })`; submit bloqueado sem token; reset pós-submit.
    - `forgot-password-form.tsx`: `resetPasswordForEmail(email, { captchaToken })` (verificar se a API do client Supabase aceita segundo arg de options; se não, aplicar via `options` no `resetPasswordForEmail`); mesmo padrão de bloqueio sem token.
    - NÃO aplicar captcha no GoogleButton (D15 — OAuth sem captcha).
    - Retornar `null` do CaptchaField quando site key ausente → forms continuam funcionando em dev (sem quebra) — protegido no componente.
  </action>
  <verify>
    <automated>npm run typecheck; if ($?) { npx vitest run src/app/(auth)/login/__tests__ src/app/(auth)/forgot-password/__tests__ --passWithNoTests }
  </verify>
  <acceptance_criteria>
    - `login-form.tsx` renderiza `CaptchaField`, envia `options.captchaToken` e bloqueia submit sem token
    - `forgot-password-form.tsx` aplica `captchaToken` no reset de senha; sem quebra quando site key ausente
    - `GoogleButton` (OAuth) NÃO recebe captchaToken
    - `npm run typecheck` → 0 erros
  </acceptance_criteria>
  <done>Captcha integrado em login e recuperação sem quebra</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| widget Turnstile ↔ form | Token de 1 uso trocado por verificação server-side (Supabase Auth) |
| form ↔ Supabase | `captchaToken` enviado no signUp/login/reset — validado server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-08 | Spoofing | Bot/abuso de cadastro/login | mitigate | Turnstile exigido em signup, login e recuperação; token server-side (Supabase Auth valida); OAuth isento (D15) |
| T-42-08a | Denial of Service | Widget com chave ausente | mitigate | `CaptchaField` retorna `null` sem site key (dev ok); em produção flag exige chave (config) |
| T-42-08b | Spoofing | Token reutilizado | mitigate | Tokens Turnstile são single-use (Cloudflare); reset pós-submit no widget |
| T-42-SC | Tampering | Script de terceiro | accept | Script turnstile de CDN oficial Cloudflare, integridade via `sri` quando disponível |
</threat_model>

<verification>
- `npx vitest run src/components/auth/__tests__/captcha-field.test.tsx` → 0 falhas
- `npm run typecheck` → 0 erros; `npm run lint` → 0 erros
- `GoogleButton` sem `captchaToken` (regressão 42-07 verdes)
</verification>

<success_criteria>
- `CaptchaField` reutilizável (script via useEffect, render explicito, tema dark, a11y) com testes
- Turnstile aplicado a login e recuperação (token via `onVerify` → `options.captchaToken`); reset pós-submit
- OAuth (GoogleButton) permanece sem captcha (D15)
- Sem site key → widget não renderiza (dev sem quebra)
</success_criteria>

<output>
Create `.planning/phases/42-signup-controlado-elegibilidade-freemium/42-08-SUMMARY.md` when done
</output>
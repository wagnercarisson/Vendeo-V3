---
phase: 42-signup-controlado-elegibilidade-freemium
plan: 08
type: execute
wave: 3
depends_on: ["42-02"]
files_modified:
  - src/components/auth/captcha-field.tsx
  - src/components/auth/__tests__/captcha-field.test.tsx
  - src/app/(auth)/login/login-form.tsx
  - src/app/(auth)/forgot-password/forgot-password-form.tsx
  - src/app/(auth)/login/__tests__/login-form.test.tsx
  - src/app/(auth)/forgot-password/__tests__/forgot-password-form.test.tsx
deleted:
  - src/__tests__/auth/login-form.test.tsx
  - src/__tests__/auth/forgot-password-form.test.tsx
autonomous: true
requirements: ["turnstile-captcha", "login-page"]

must_haves:
  truths:
    - "**Depende de 42-02, NÃƒO de 42-06/42-07** â€” CaptchaField Ã© independente; cria o componente e integra em login/forgot-password (arquivos EXISTENTES). A integraÃ§Ã£o no signup-form acontece em 42-06 (que depende de 42-08) â€” NÃƒO aqui, para evitar import de arquivo inexistente"
    - "`CaptchaField` reutilizÃ¡vel em `src/components/auth/captcha-field.tsx` â€” label + script turnstile + `<div class=cf-turnstile>` + `onVerify(token)`; id padrÃ£o `captcha-<chave>`; a11y"
    - "Token exposto via `onVerify` ao formulÃ¡rio pai; form envia `captchaToken` no `signUp`/`signInWithPassword`/`resetPasswordForEmail`"
    - "Script turnstile carregado via `useEffect` (regiÃµes: cloudflare.com / EAST); `cf-turnstile` render custom widget; reset a cada submit"
    - "AplicaÃ§Ã£o: signup-form (42-06), login-form, forgot-password-form â€” chave pÃºblica de env `NEXT_PUBLIC_TURNSTILE_SITE_KEY`"
  artifacts:
    - path: "src/components/auth/captcha-field.tsx"
      provides: "Widget reutilizÃ¡vel"
      contains: "cf-turnstile"
  key_links:
    - from: "captcha-field.tsx"
      to: "supabase/auth"
      via: "captchaToken"
      pattern: "onVerify"
---

<objective>
Criar o `CaptchaField` reutilizÃ¡vel (D3) e aplicÃ¡-lo a login e recuperaÃ§Ã£o de senha (arquivos existentes): widget Turnstile (Cloudflare, script via useEffect, regiÃµes EAST), token via `onVerify`, e envio como `captchaToken` nas chamadas Supabase. A integraÃ§Ã£o no signup-form acontece em 42-06 (que depende de 42-08) â€” este plan NÃƒO toca em signup-form.

Purpose: D3 â€” Turnstile em cadastro/login/recuperaÃ§Ã£o; OAuth SEM captcha (D15).
Output: CaptchaField + integraÃ§Ã£o login/recuperaÃ§Ã£o.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D3 â€” Turnstile; D15 â€” OAuth sem captcha)
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-UI-SPEC.md (CaptchaField â€” a11y, label, hints; NÃƒO pode quebrar signup/login/recuperaÃ§Ã£o)
@src/lib/supabase/client.ts (createBrowserClient â€” getSupabaseClient; expÃµe config)
@src/app/(auth)/login/login-form.tsx (integrar)
@src/app/(auth)/forgot-password/forgot-password-form.tsx (integrar)

<interfaces>
- `CaptchaField` em `src/components/auth/captcha-field.tsx` ("use client"):
  - props: `{ onVerify: (token: string | null) => void; label?: string; hint?: string; className?: string }`.
  - Render: `<label>` (sr-only ok) + `<div id="captcha-<uid>" className="cf-turnstile" data-sitekey data-callback data-theme="dark" data-size="flexible" aria-label="VerificaÃ§Ã£o de seguranÃ§a (nÃ£o Ã© robÃ´)" />`.
  - `useEffect`: injeta `<script src="https://challenges.cloudflare.com/turnstile/v0/api.js?render=explicit" async defer>` (fallback `?onload=` padrÃ£o se necessÃ¡rio); inicializa `window.turnstile.render(container, { sitekey, callback: onVerify, ... })`; cleanup `window.turnstile.reset()`/remove script.
  - Retorna `null` se `NEXT_PUBLIC_TURNSTILE_SITE_KEY` ausente (renderizaÃ§Ã£o protegida server/client).
- login/forgot-password (arquivos existentes): mesmo padrÃ£o (token obrigatÃ³rio para submit).
- **signup-form NÃƒO Ã© tocado aqui** â€” 42-06 (dependente de 42-08) integra o CaptchaField no form de cadastro.
- **OAuth (GoogleButton, 42-07) NÃƒO recebe captchaToken** (D15).
- **Co-migraÃ§Ã£o de testes (canonical CONTEXT.md:190):** `src/__tests__/auth/login-form.test.tsx` â†’ `src/app/(auth)/login/__tests__/login-form.test.tsx` e `src/__tests__/auth/forgot-password-form.test.tsx` â†’ `src/app/(auth)/forgot-password/__tests__/forgot-password-form.test.tsx` â€” estender com asserÃ§Ãµes de captcha (submit bloqueado sem token; `options.captchaToken` no `signInWithPassword`/`resetPasswordForEmail`).
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: CaptchaField â€” widget reutilizÃ¡vel</name>
  <files>src/components/auth/captcha-field.tsx, src/components/auth/__tests__/captcha-field.test.tsx</files>
  <read_first>
    - .planning/phases/42-signup-controlado-elegibilidade-freemium/42-UI-SPEC.md (CaptchaField)
    - src/components/ui/input.tsx (padrÃ£o de campo â€” labels/ids)
    - src/app/(auth)/login/login-form.tsx (padrÃ£o de componente client)
  </read_first>
  <action>
    Criar `src/components/auth/captcha-field.tsx` ("use client"):
    1. Props: `{ onVerify: (token: string | null) => void; label?: string; hint?: string; className?: string }`.
    2. `useEffect`: adicionar `<script src="https://challenges.cloudflare.com/turnstile/v0/api.js?render=explicit" async defer>` (duplicado ok); depois `window.turnstile.render("#captcha-<uid>", { sitekey: NEXT_PUBLIC_TURNSTILE_SITE_KEY, callback: (token) => onVerify(token), "expired-callback": () => onVerify(null), "error-callback": () => onVerify(null), theme: "dark", size: "flexible" })`. Cleanup: `window.turnstile.remove("#captcha-<uid>")` (ou reset) + remover script (com guarda de duplicados via data-attributo).
    3. Render: `<div>` com label (sr-only "VerificaÃ§Ã£o de seguranÃ§a"), `<div id="captcha-<uid>" className="cf-turnstile" data-sitekey={...} aria-label="VerificaÃ§Ã£o de seguranÃ§a (nÃ£o Ã© robÃ´)" />` e hint opcional. **NÃ£o criar widget se `!NEXT_PUBLIC_TURNSTILE_SITE_KEY`** (retorna `null`).
    4. Registrar um efeito pÃ³s-hydration para chamar `onVerify(null)` no mount (token vazio atÃ© resoluÃ§Ã£o).
    Red/Green: `captcha-field.test.tsx` (mock `window.turnstile` + script): renderiza div `.cf-turnstile` quando site key presente; **nÃ£o** renderiza quando ausente; `onVerify` chamado com token no callback; `onVerify(null)` no expired/error-callback; cleanup chama remove.
  </action>
  <verify>
    <automated>npx vitest run src/components/auth/__tests__/captcha-field.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `captcha-field.tsx` existe com div `.cf-turnstile`, callback `onVerify`, `expired-callback` e `error-callback` â†’ `onVerify(null)`
    - Script turnstile injetado via useEffect (com dedupe); cleanup remove/reset
    - Retorna `null` quando `NEXT_PUBLIC_TURNSTILE_SITE_KEY` ausente
    - `npx vitest run src/components/auth/__tests__/captcha-field.test.tsx` â†’ 0 falhas
  </acceptance_criteria>
  <done>CaptchaField com testes verdes</done>
</task>

<task type="execute">
  <name>Task 2: Integrar CaptchaField em login e recuperaÃ§Ã£o + co-migraÃ§Ã£o de testes</name>
  <files>src/app/(auth)/login/login-form.tsx, src/app/(auth)/forgot-password/forgot-password-form.tsx, src/app/(auth)/login/__tests__/login-form.test.tsx, src/app/(auth)/forgot-password/__tests__/forgot-password-form.test.tsx</files>
  <read_first>
    - src/app/(auth)/login/login-form.tsx (login client :21-52)
    - src/app/(auth)/forgot-password/forgot-password-form.tsx (:8-30)
    - src/__tests__/auth/login-form.test.tsx (teste existente a co-migrar :1-176)
    - src/__tests__/auth/forgot-password-form.test.tsx (teste existente a co-migrar)
  </read_first>
  <action>
    - `login-form.tsx`: adicionar `<CaptchaField onVerify={setCaptchaToken} />` (antes do submit); `signInWithPassword({ email, password, options: { captchaToken } })`; submit bloqueado sem token; reset pÃ³s-submit.
    - `forgot-password-form.tsx`: `resetPasswordForEmail(email, { captchaToken })` (verificar se a API do client Supabase aceita segundo arg de options; se nÃ£o, aplicar via `options` no `resetPasswordForEmail`); mesmo padrÃ£o de bloqueio sem token.
    - NÃƒO aplicar captcha no GoogleButton (D15 â€” OAuth sem captcha).
    - Retornar `null` do CaptchaField quando site key ausente â†’ forms continuam funcionando em dev (sem quebra) â€” protegido no componente.
    - **Co-migraÃ§Ã£o de testes (canonical CONTEXT.md:190):** mover `src/__tests__/auth/login-form.test.tsx` â†’ `src/app/(auth)/login/__tests__/login-form.test.tsx` e `src/__tests__/auth/forgot-password-form.test.tsx` â†’ `src/app/(auth)/forgot-password/__tests__/forgot-password-form.test.tsx`. Estender os casos existentes com asserÃ§Ãµes de captcha: (a) submit sem `captchaToken` â†’ `signInWithPassword` NÃƒO Ã© chamado; (b) com token â†’ chamado com `options.captchaToken`. Preservar os asserts existentes (Solicitar acesso free, Esqueci minha senha, redirects, erro genÃ©rico).
  </action>
  <verify>
    <automated>npm run typecheck; if ($?) { npx vitest run src/app/(auth)/login/__tests__/login-form.test.tsx src/app/(auth)/forgot-password/__tests__/forgot-password-form.test.tsx }
</automated>
  </verify>
  <acceptance_criteria>
    - `login-form.tsx` renderiza `CaptchaField`, envia `options.captchaToken` e bloqueia submit sem token
    - `forgot-password-form.tsx` aplica `captchaToken` no reset de senha; sem quebra quando site key ausente
    - `GoogleButton` (OAuth) NÃƒO recebe captchaToken
    - `src/app/(auth)/login/__tests__/login-form.test.tsx` e `src/app/(auth)/forgot-password/__tests__/forgot-password-form.test.tsx` existem (co-migrados de `src/__tests__/auth/`), cobrem captcha e `src/__tests__/auth/*.test.tsx` originais foram removidos
    - `npx vitest run` nos 2 arquivos co-migrados â†’ 0 falhas
    - `npm run typecheck` â†’ 0 erros
  </acceptance_criteria>
  <done>Captcha integrado em login e recuperaÃ§Ã£o com testes co-migrados verdes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| widget Turnstile â†” form | Token de 1 uso trocado por verificaÃ§Ã£o server-side (Supabase Auth) |
| form â†” Supabase | `captchaToken` enviado no signUp/login/reset â€” validado server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-08 | Spoofing | Bot/abuso de cadastro/login | mitigate | Turnstile exigido em signup, login e recuperaÃ§Ã£o; token server-side (Supabase Auth valida); OAuth isento (D15) |
| T-42-08a | Denial of Service | Widget com chave ausente | mitigate | `CaptchaField` retorna `null` sem site key (dev ok); em produÃ§Ã£o flag exige chave (config) |
| T-42-08b | Spoofing | Token reutilizado | mitigate | Tokens Turnstile sÃ£o single-use (Cloudflare); reset pÃ³s-submit no widget |
| T-42-SC | Tampering | Script de terceiro | accept | Script turnstile de CDN oficial Cloudflare, integridade via `sri` quando disponÃ­vel |
</threat_model>

<verification>
- `npx vitest run src/components/auth/__tests__/captcha-field.test.tsx` â†’ 0 falhas
- `npx vitest run src/app/(auth)/login/__tests__/login-form.test.tsx src/app/(auth)/forgot-password/__tests__/forgot-password-form.test.tsx` â†’ 0 falhas (co-migrados)
- `npm run typecheck` â†’ 0 erros; `npm run lint` â†’ 0 erros
- `GoogleButton` sem `captchaToken` (regressÃ£o 42-07 verdes)
</verification>

<success_criteria>
- `CaptchaField` reutilizÃ¡vel (script via useEffect, render explicito, tema dark, a11y) com testes
- Turnstile aplicado a login e recuperaÃ§Ã£o (token via `onVerify` â†’ `options.captchaToken`); reset pÃ³s-submit
- OAuth (GoogleButton) permanece sem captcha (D15)
- Sem site key â†’ widget nÃ£o renderiza (dev sem quebra)
- Testes de login/recuperaÃ§Ã£o co-migrados para `src/app/(auth)/` (canonical CONTEXT.md:190) com cobertura de captcha
</success_criteria>

<output>
Create `.planning/phases/42-signup-controlado-elegibilidade-freemium/42-08-SUMMARY.md` when done
</output>

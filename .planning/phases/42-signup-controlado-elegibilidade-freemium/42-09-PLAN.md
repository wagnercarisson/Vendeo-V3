---
phase: 42-signup-controlado-elegibilidade-freemium
plan: 09
type: execute
wave: 5
depends_on: ["42-06", "42-07", "42-08"]
files_modified:
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/login/login-form.tsx
  - src/app/(auth)/forgot-password/forgot-password-form.tsx
  - src/app/(auth)/forgot-password/page.tsx
  - src/app/(auth)/login/__tests__/login-page.test.tsx
autonomous: true
requirements: ["login-page", "launch-config (Nova flag publicSignupEnabled)"]

must_haves:
  truths:
    - "GoogleButton SEMPRE visÃ­vel no login (D15 â€” independente da flag)"
    - "Login com captcha (42-08) + divisÃ£o claro 'ou' entre Google e email/senha"
    - "**Flag ON**: link 'Criar uma conta' (ou 'Criar conta com email') â†’ /signup visÃ­vel; esconde o link legado 'Solicitar acesso free'"
    - "**Flag OFF**: mantÃ©m o link atual 'Ainda nÃ£o tem acesso? Solicitar acesso free' (â†’ /, rota de acesso legado) â€” NÃƒO exibe 'Criar uma conta' (login-page spec: Google sempre visÃ­vel; sem link de criaÃ§Ã£o)"
    - "RecuperaÃ§Ã£o de senha: captcha + copy PT-BR; /check-email?type=recovery funcional"
  artifacts:
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Layout login novo"
      contains: "GoogleButton"
    - path: "src/app/(auth)/forgot-password/forgot-password-form.tsx"
      provides: "RecuperaÃ§Ã£o com captcha"
      contains: "captchaToken"
  key_links:
    - from: "login/page.tsx"
      to: "src/components/auth/google-button.tsx"
      via: "import"
      pattern: "GoogleButton"
---

<objective>
Atualizar o login e a recuperaÃ§Ã£o de senha para a fase 42 (D5/D15): GoogleButton sempre visÃ­vel no login (divisor "ou"), captcha jÃ¡ integrado (42-08), link para /signup conforme a flag, e recuperaÃ§Ã£o com captcha + fluxo /check-email funcional.

Purpose: D5/D15 â€” login com Google sempre + captcha; criar-conta conforme flag.
Output: login + recuperaÃ§Ã£o alinhados.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D5 â€” login; D15 â€” OAuth sempre visÃ­vel; D13 â€” flag controla rota /signup)
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-UI-SPEC.md (login â€” Google sempre; copy)
@src/app/(auth)/login/page.tsx (atual :1-21 â€” sem GoogleButton)
@src/app/(auth)/login/login-form.tsx (42-08 â€” com captcha)
@src/app/(auth)/forgot-password/page.tsx (:1-16)
@src/app/(auth)/forgot-password/forgot-password-form.tsx (42-08 â€” com captcha)
@src/app/(auth)/check-email/page.tsx (type=signup/recovery :14-16)
@src/components/auth/google-button.tsx (42-07)
@src/lib/launch-config/config.ts (publicSignupEnabled)

<interfaces>
- `login/page.tsx` (server): ler `publicSignupEnabled` para decidir o CTA de acesso (flag on â†’ link "Criar uma conta" â†’ /signup; flag off â†’ manter "Ainda nÃ£o tem acesso? Solicitar acesso free" â†’ /). GoogleButton SEMPRE renderizado (D15).
- Estrutura de abertura: heading "Entrar na sua conta", `GoogleButton`, divisor "ou", `LoginForm` (com captcha), CTA conforme flag (flag on: "Criar uma conta" â†’ /signup; flag off: "Ainda nÃ£o tem acesso? Solicitar acesso free" â†’ /). Footer de ajuda â†’ /forgot-password.
- `forgot-password-form` com captcha (42-08) â†’ `resetPasswordForEmail(email, { captchaToken })` â†’ success: redirect `/check-email?type=recovery`.
- `check-email/page.tsx` jÃ¡ lida com `type=signup`/`recovery`; garantir copy para recovery.
- Visual: slate/blue padrÃ£o da auth (UI-SPEC).
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: login/page.tsx â€” Google sempre visÃ­vel + link criar conta</name>
  <files>src/app/(auth)/login/page.tsx, src/app/(auth)/login/__tests__/login-page.test.tsx</files>
  <read_first>
    - src/app/(auth)/login/page.tsx (atual :1-21)
    - src/app/(auth)/signup/page.tsx (42-06 â€” padrÃ£o de estrutura com GoogleButton/divisor)
    - src/components/auth/google-button.tsx (42-07)
    - src/lib/launch-config/config.ts (getLaunchConfig)
  </read_first>
  <action>
    Em `src/app/(auth)/login/page.tsx` (server):
    1. `const { publicSignupEnabled } = await getLaunchConfig();`
    2. Render: heading "Entrar na sua conta" â†’ `<GoogleButton/>` â†’ divisor "ou" (`<div class="flex items-center gap-3 text-slate-400"><span class="h-px flex-1 bg-slate-600"/>ou<span class="h-px flex-1 bg-slate-600"/></div>`) â†’ `<LoginForm/>` â†’ CTA de acesso conforme flag:
       - **flag on**: link "Criar uma conta" (href `/signup`); NÃƒO renderizar "Solicitar acesso free".
       - **flag off**: manter o link existente "Ainda nÃ£o tem acesso? Solicitar acesso free" (href `/`, rota legada de acesso) â€” **NÃƒO** exibir "Criar uma conta".
       Footer link "Esqueceu sua senha?" â†’ `/forgot-password`.
    Red/Green: `login-page.test.tsx` (mock getLaunchConfig + GoogleButton): flag on â†’ GoogleButton presente + link "Criar uma conta" â†’ /signup + ausÃªncia de "Solicitar acesso free"; flag off â†’ GoogleButton presente (D15) + link "Solicitar acesso free" preservado (â†’ /) + ausÃªncia de "Criar uma conta".
  </action>
  <verify>
    <automated>npx vitest run src/app/(auth)/login/__tests__/login-page.test.tsx
</automated>
  </verify>
  <acceptance_criteria>
    - `login/page.tsx` lÃª `publicSignupEnabled` e renderiza GoogleButton SEMPRE (flag on e off)
    - Divisor "ou" presente; flag on â†’ link "Criar uma conta" â†’ `/signup` e SEM "Solicitar acesso free"; flag off â†’ "Ainda nÃ£o tem acesso? Solicitar acesso free" (â†’ `/`) preservado e SEM "Criar uma conta"
    - `npx vitest run src/app/(auth)/login/__tests__/login-page.test.tsx` â†’ 0 falhas
  </acceptance_criteria>
  <done>Login com Google sempre + link conforme flag, testes verdes</done>
</task>

<task type="execute">
  <name>Task 2: RecuperaÃ§Ã£o â€” /forgot-password com captcha e /check-email?type=recovery (verificaÃ§Ã£o)</name>
  <files>src/app/(auth)/forgot-password/page.tsx, src/app/(auth)/forgot-password/forgot-password-form.tsx, src/app/(auth)/forgot-password/__tests__/forgot-password-form.test.tsx
  </files>
  <read_first>
    - src/app/(auth)/forgot-password/page.tsx (:1-16)
    - src/app/(auth)/forgot-password/forgot-password-form.tsx (42-08 â€” com captcha)
    - src/app/(auth)/check-email/page.tsx (verificar APENAS â€” NÃƒO alterar; CONTEXT.md:15 declara inalterado na F42)
  </read_first>
  <action>
    - `forgot-password/page.tsx`: manter estrutura; garantir que o form inclui `CaptchaField` (42-08 jÃ¡ integrado) e copy PT-BR ("Informe seu e-mail e enviaremos um link para redefinir sua senha.").
    - `forgot-password-form.tsx`: jÃ¡ com `captchaToken` (42-08) â†’ `resetPasswordForEmail(email, { captchaToken })`; no sucesso (sem erro), `router.replace("/check-email?type=recovery")`.
    - `check-email/page.tsx`: **NÃƒO alterar** (CONTEXT.md:15 â€” jÃ¡ suporta `type=signup`/`type=recovery`, inalterado na F42). Apenas verificar via git diff que o arquivo permanece intacto ao final.
    - Mensagem genÃ©rica em erro de recuperaÃ§Ã£o (anti-enumeraÃ§Ã£o: "NÃ£o foi possÃ­vel enviar. Tente novamente.").
  </action>
  <verify>
    <automated>npm run typecheck; if ($?) { npx vitest run src/app/(auth)/forgot-password/__tests__/forgot-password-form.test.tsx }
</automated>
  </verify>
  <acceptance_criteria>
    - `forgot-password-form.tsx` envia `captchaToken` e, no sucesso, redireciona para `/check-email?type=recovery`
    - `check-email/page.tsx` NÃƒO foi alterado (git diff vazio nesse arquivo â€” `type=recovery` jÃ¡ coberto, CONTEXT.md:15)
    - Erros de recuperaÃ§Ã£o sÃ£o genÃ©ricos (anti-enumeraÃ§Ã£o)
    - `npm run typecheck` â†’ 0 erros
  </acceptance_criteria>
  <done>RecuperaÃ§Ã£o com captcha + /check-email?type=recovery funcional (check-email intacto)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| login/recuperaÃ§Ã£o â†” Supabase Auth | Credenciais e reset â€” captcha e anti-enumeraÃ§Ã£o |
| flag â†” link criar conta | `publicSignupEnabled` expÃµe ou esconde a rota de criaÃ§Ã£o |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-09 | Information Disclosure | EnumeraÃ§Ã£o via recuperaÃ§Ã£o | mitigate | Mensagem genÃ©rica em erro de reset; `/check-email?type=recovery` igual para sucesso e conta inexistente (padrÃ£o D2 reusado) |
| T-42-09a | Spoofing | Abuso de login | mitigate | Captcha (42-08) no login e na recuperaÃ§Ã£o |
| T-42-09b | Elevation of Privilege | CriaÃ§Ã£o de conta fora da flag | mitigate | Link "Criar uma conta" sÃ³ com flag on; rota `/signup` protegida server-side (42-06); flag off mantÃ©m "Solicitar acesso free" (â†’ /) sem expor criaÃ§Ã£o |
| T-42-SC | Tampering | InstalaÃ§Ãµes de pacotes | accept | Nenhum pacote novo |
</threat_model>

<verification>
- `npx vitest run src/app/(auth)/login/__tests__/login-page.test.tsx` â†’ 0 falhas
- `npm run typecheck` â†’ 0 erros; `npm run lint` â†’ 0 erros
- Regressao: `src/app/(auth)/signup/page.tsx` e `src/components/auth/signup-form.tsx` NAO sao alterados por este plano - verificar que o git diff desses arquivos reflete apenas mudancas dos plans anteriores (42-02/42-06), nao desta execucao.
</verification>

<success_criteria>
- GoogleButton sempre visÃ­vel no login (D15), com divisor "ou"
- Flag on â†’ "Criar uma conta" â†’ /signup (sem "Solicitar acesso free"); flag off â†’ "Ainda nÃ£o tem acesso? Solicitar acesso free" (â†’ /) preservado (sem "Criar uma conta")
- RecuperaÃ§Ã£o com captcha + /check-email?type=recovery funcional e copy anti-enumeraÃ§Ã£o
</success_criteria>

<output>
Create `.planning/phases/42-signup-controlado-elegibilidade-freemium/42-09-SUMMARY.md` when done
</output>

---
phase: 42-signup-controlado-elegibilidade-freemium
plan: 09
type: execute
wave: 3
depends_on: ["42-06", "42-07", "42-08"]
files_modified:
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/login/login-form.tsx
  - src/app/(auth)/forgot-password/forgot-password-form.tsx
  - src/app/(auth)/forgot-password/page.tsx
  - src/app/(auth)/check-email/page.tsx
autonomous: true
requirements: ["login-page", "launch-config (Nova flag publicSignupEnabled)"]

must_haves:
  truths:
    - "GoogleButton SEMPRE visível no login (D15 — independente da flag)"
    - "Login com captcha (42-08) + divisão claro 'ou' entre Google e email/senha"
    - "**Flag ON**: link 'Criar uma conta' (ou 'Criar conta com email') → /signup visível; esconde o link legado 'Solicitar acesso free'"
    - "**Flag OFF**: mantém o link atual 'Ainda não tem acesso? Solicitar acesso free' (→ /, rota de acesso legado) — NÃO exibe 'Criar uma conta' (login-page spec: Google sempre visível; sem link de criação)"
    - "Recuperação de senha: captcha + copy PT-BR; /check-email?type=recovery funcional"
  artifacts:
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Layout login novo"
      contains: "GoogleButton"
    - path: "src/app/(auth)/forgot-password/forgot-password-form.tsx"
      provides: "Recuperação com captcha"
      contains: "captchaToken"
  key_links:
    - from: "login/page.tsx"
      to: "src/components/auth/google-button.tsx"
      via: "import"
      pattern: "GoogleButton"
---

<objective>
Atualizar o login e a recuperação de senha para a fase 42 (D5/D15): GoogleButton sempre visível no login (divisor "ou"), captcha já integrado (42-08), link para /signup conforme a flag, e recuperação com captcha + fluxo /check-email funcional.

Purpose: D5/D15 — login com Google sempre + captcha; criar-conta conforme flag.
Output: login + recuperação alinhados.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D5 — login; D15 — OAuth sempre visível; D13 — flag controla rota /signup)
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-UI-SPEC.md (login — Google sempre; copy)
@src/app/(auth)/login/page.tsx (atual :1-21 — sem GoogleButton)
@src/app/(auth)/login/login-form.tsx (42-08 — com captcha)
@src/app/(auth)/forgot-password/page.tsx (:1-16)
@src/app/(auth)/forgot-password/forgot-password-form.tsx (42-08 — com captcha)
@src/app/(auth)/check-email/page.tsx (type=signup/recovery :14-16)
@src/components/auth/google-button.tsx (42-07)
@src/lib/launch-config/config.ts (publicSignupEnabled)

<interfaces>
- `login/page.tsx` (server): ler `publicSignupEnabled` para decidir o CTA de acesso (flag on → link "Criar uma conta" → /signup; flag off → manter "Ainda não tem acesso? Solicitar acesso free" → /). GoogleButton SEMPRE renderizado (D15).
- Estrutura de abertura: heading "Entrar na sua conta", `GoogleButton`, divisor "ou", `LoginForm` (com captcha), CTA conforme flag (flag on: "Criar uma conta" → /signup; flag off: "Ainda não tem acesso? Solicitar acesso free" → /). Footer de ajuda → /forgot-password.
- `forgot-password-form` com captcha (42-08) → `resetPasswordForEmail(email, { captchaToken })` → success: redirect `/check-email?type=recovery`.
- `check-email/page.tsx` já lida com `type=signup`/`recovery`; garantir copy para recovery.
- Visual: slate/blue padrão da auth (UI-SPEC).
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: login/page.tsx — Google sempre visível + link criar conta</name>
  <files>src/app/(auth)/login/page.tsx, src/app/(auth)/login/__tests__/login-page.test.tsx</files>
  <read_first>
    - src/app/(auth)/login/page.tsx (atual :1-21)
    - src/app/(auth)/signup/page.tsx (42-06 — padrão de estrutura com GoogleButton/divisor)
    - src/components/auth/google-button.tsx (42-07)
    - src/lib/launch-config/config.ts (getLaunchConfig)
  </read_first>
  <action>
    Em `src/app/(auth)/login/page.tsx` (server):
    1. `const { publicSignupEnabled } = await getLaunchConfig();`
    2. Render: heading "Entrar na sua conta" → `<GoogleButton/>` → divisor "ou" (`<div class="flex items-center gap-3 text-slate-400"><span class="h-px flex-1 bg-slate-600"/>ou<span class="h-px flex-1 bg-slate-600"/></div>`) → `<LoginForm/>` → CTA de acesso conforme flag:
       - **flag on**: link "Criar uma conta" (href `/signup`); NÃO renderizar "Solicitar acesso free".
       - **flag off**: manter o link existente "Ainda não tem acesso? Solicitar acesso free" (href `/`, rota legada de acesso) — **NÃO** exibir "Criar uma conta".
       Footer link "Esqueceu sua senha?" → `/forgot-password`.
    Red/Green: `login-page.test.tsx` (mock getLaunchConfig + GoogleButton): flag on → GoogleButton presente + link "Criar uma conta" → /signup + ausência de "Solicitar acesso free"; flag off → GoogleButton presente (D15) + link "Solicitar acesso free" preservado (→ /) + ausência de "Criar uma conta".
  </action>
  <verify>
    <automated>npx vitest run src/app/(auth)/login/__tests__/login-page.test.tsx
  </verify>
  <acceptance_criteria>
    - `login/page.tsx` lê `publicSignupEnabled` e renderiza GoogleButton SEMPRE (flag on e off)
    - Divisor "ou" presente; flag on → link "Criar uma conta" → `/signup` e SEM "Solicitar acesso free"; flag off → "Ainda não tem acesso? Solicitar acesso free" (→ `/`) preservado e SEM "Criar uma conta"
    - `npx vitest run src/app/(auth)/login/__tests__/login-page.test.tsx` → 0 falhas
  </acceptance_criteria>
  <done>Login com Google sempre + link conforme flag, testes verdes</done>
</task>

<task type="execute">
  <name>Task 2: Recuperação — /forgot-password com captcha e /check-email?type=recovery</name>
  <files>src/app/(auth)/forgot-password/page.tsx, src/app/(auth)/forgot-password/forgot-password-form.tsx, src/app/(auth)/check-email/page.tsx
  </files>
  <read_first>
    - src/app/(auth)/forgot-password/page.tsx (:1-16)
    - src/app/(auth)/forgot-password/forgot-password-form.tsx (42-08 — com captcha)
    - src/app/(auth)/check-email/page.tsx (type=signup/recovery :14-16)
  </read_first>
  <action>
    - `forgot-password/page.tsx`: manter estrutura; garantir que o form inclui `CaptchaField` (42-08 já integrado) e copy PT-BR ("Informe seu e-mail e enviaremos um link para redefinir sua senha.").
    - `forgot-password-form.tsx`: já com `captchaToken` (42-08) → `resetPasswordForEmail(email, { captchaToken })`; no sucesso (sem erro), `router.replace("/check-email?type=recovery")`.
    - `check-email/page.tsx`: garantir que `type=recovery` renderiza copy de recuperação ("Enviamos um link para redefinir sua senha. Verifique sua caixa de entrada.") — revisar o branching existente e completar se faltar; manter type=signup intacto.
    - Mensagem genérica em erro de recuperação (anti-enumeração: "Não foi possível enviar. Tente novamente.").
  </action>
  <verify>
    <automated>npm run typecheck; if ($?) { npx vitest run src/app/(auth)/forgot-password/__tests__ --passWithNoTests }
  </verify>
  <acceptance_criteria>
    - `forgot-password-form.tsx` envia `captchaToken` e, no sucesso, redireciona para `/check-email?type=recovery`
    - `check-email/page.tsx` renderiza copy de recovery para `type=recovery`; `type=signup` inalterado
    - Erros de recuperação são genéricos (anti-enumeração)
    - `npm run typecheck` → 0 erros
  </acceptance_criteria>
  <done>Recuperação com captcha + /check-email?type=recovery funcional</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| login/recuperação ↔ Supabase Auth | Credenciais e reset — captcha e anti-enumeração |
| flag ↔ link criar conta | `publicSignupEnabled` expõe ou esconde a rota de criação |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-09 | Information Disclosure | Enumeração via recuperação | mitigate | Mensagem genérica em erro de reset; `/check-email?type=recovery` igual para sucesso e conta inexistente (padrão D2 reusado) |
| T-42-09a | Spoofing | Abuso de login | mitigate | Captcha (42-08) no login e na recuperação |
| T-42-09b | Elevation of Privilege | Criação de conta fora da flag | mitigate | Link "Criar uma conta" só com flag on; rota `/signup` protegida server-side (42-06); flag off mantém "Solicitar acesso free" (→ /) sem expor criação |
| T-42-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo |
</threat_model>

<verification>
- `npx vitest run src/app/(auth)/login/__tests__/login-page.test.tsx` → 0 falhas
- `npm run typecheck` → 0 erros; `npm run lint` → 0 erros
- Regressão: `src/app/(auth)/signup/page.tsx` e `src/components/auth/signup-form.tsx` intactos (git diff vazio nesses arquivos)
</verification>

<success_criteria>
- GoogleButton sempre visível no login (D15), com divisor "ou"
- Flag on → "Criar uma conta" → /signup (sem "Solicitar acesso free"); flag off → "Ainda não tem acesso? Solicitar acesso free" (→ /) preservado (sem "Criar uma conta")
- Recuperação com captcha + /check-email?type=recovery funcional e copy anti-enumeração
</success_criteria>

<output>
Create `.planning/phases/42-signup-controlado-elegibilidade-freemium/42-09-SUMMARY.md` when done
</output>
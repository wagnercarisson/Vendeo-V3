---
phase: 42-signup-controlado-elegibilidade-freemium
plan: 10
type: execute
wave: 4
depends_on: ["42-02", "42-07"]
files_modified:
  - src/components/landing/access-request-section.tsx
  - src/app/page.tsx
  - src/components/landing/__tests__/access-request-section.test.tsx
autonomous: true
requirements: ["access-request-history", "signup-page", "launch-config (Nova flag publicSignupEnabled)", "google-oauth-signup"]

must_haves:
  truths:
    - "CTA da landing depende da flag (access-request-history spec Â§1 + UI-SPEC Copywriting Contract): flag on â†’ CTA **principal 'Continuar com Google'** (GoogleButton â†’ signInWithOAuth('google')) + CTA **secundÃ¡rio 'Continuar com email'** (href /signup); flag off â†’ 'Solicitar acesso free' + formulÃ¡rio de solicitaÃ§Ã£o preservado"
    - "**Depende de 42-07 (GoogleButton real)** â€” NÃƒO criar placeholder; o GoogleButton existe quando 42-10 executa"
    - "`access_requests` existentes viram histÃ³rico (D4) â€” o formulÃ¡rio continua gravando quando flag off; tabela/backfill intactos"
    - "Copy CTA aprovada (UI-SPEC:91-92, :103): GoogleButton = 'Continuar com Google' com Ã­cone 'G'; solid bg-accent-green na landing flag on; NÃƒO usar azul para o botÃ£o do Google"
    - "HerÃ³i e features da landing intocados (F34 / padrÃ£o visual preservado)"
  artifacts:
    - path: "src/components/landing/access-request-section.tsx"
      provides: "SeÃ§Ã£o dinÃ¢mica"
      contains: "Continuar com Google"
  key_links:
    - from: "access-request-section.tsx"
      to: "src/components/auth/google-button.tsx"
      via: "import"
      pattern: "GoogleButton"
    - from: "access-request-section.tsx"
      to: "src/lib/launch-config/config.ts"
      via: "getLaunchConfig"
      pattern: "publicSignupEnabled"
---

<objective>
Ajustar a landing para refletir a flag (D4/D5): quando `publicSignupEnabled` (flag on) â†’ CTA **principal "Continuar com Google"** (`GoogleButton`) + CTA **secundÃ¡rio "Continuar com email"** (â†’ `/signup`), **sem formulÃ¡rio de solicitaÃ§Ã£o**; quando off â†’ preservar "Solicitar acesso free" + formulÃ¡rio de lista de espera verbatim. `access_requests` vira histÃ³rico â€” a gravaÃ§Ã£o continua para flag off; nada Ã© migrado/excluÃ­do.

Purpose: D4 â€” landing dinÃ¢mica; access_requests como histÃ³rico; Google OAuth como entrada principal (D15).
Output: landing dinÃ¢mica + testes.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D4 â€” landing dinÃ¢mica; D15 â€” Google como entrada principal; access_requests histÃ³rico)
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-UI-SPEC.md (copy CTA :91-92, :103 â€” GoogleButton solid green na landing; hero intacto)
@openspec/changes/fase-42-signup-controlado-elegibilidade-freemium/specs/access-request-history/spec.md (flag on â†’ "Continuar com Google" principal + "Continuar com email" secundÃ¡rio)
@src/components/landing/access-request-section.tsx (atual â€” Beta fechado badge :28-30, form de acesso antecipado)
@src/components/auth/google-button.tsx (42-07 â€” botÃ£o Google real)
@src/app/page.tsx (landing â€” hero/features; nÃ£o tocar visual)
@src/lib/launch-config/config.ts (getLaunchConfig server-only)
@src/lib/landing/ (services existentes â€” access request handler/rota)

<interfaces>
- `access-request-section.tsx` (server ou client com prop â€” conforme padrÃ£o atual): receber `publicSignupEnabled` como prop OU ler via server. PadrÃ£o atual: componente com badge "Beta fechado" (:28-30) + form. Nova lÃ³gica:
  - **flag on** â†’ renderizar dois CTAs: `GoogleButton` (CTA principal, solid `bg-accent-green` â€” "Continuar com Google", via 42-07) + link secundÃ¡rio "Continuar com email" (href `/signup`, outline) + subcopy "Leva 2 minutos". **Sem form de lista.**
  - **flag off** â†’ renderizar o JSX atual verbatim (badge + form de acesso antecipado).
- `access_requests`: a gravaÃ§Ã£o (route/handler) continua existindo (flag off). Nada de backfill/drop. Nada de migration neste plan.
- Copy CTA conforme UI-SPEC: GoogleButton com Ã­cone 'G' oficial, solid green na landing (MASTER tokens); secundÃ¡rio outline.
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: access-request-section.tsx â€” flag on/off</name>
  <files>src/components/landing/access-request-section.tsx, src/components/landing/__tests__/access-request-section.test.tsx</files>
  <read_first>
    - src/components/landing/access-request-section.tsx (atual â€” badge :28-30, form :33-60)
    - src/components/auth/google-button.tsx (42-07 â€” botÃ£o real)
    - src/lib/launch-config/config.ts (getLaunchConfig â€” server-only)
    - .planning/phases/42-signup-controlado-elegibilidade-freemium/42-UI-SPEC.md (copy CTA :91-92, :103)
  </read_first>
  <action>
    Em `src/components/landing/access-request-section.tsx`:
    1. Tornar o componente dinÃ¢mico: se server component, ler `const { publicSignupEnabled } = await getLaunchConfig();` (ou receber como prop do `page.tsx`). Se client, receber prop booleana.
    2. **Flag on** â†’ renderizar: heading CTA + `GoogleButton` (CTA principal, solid `bg-accent-green` â€” "Continuar com Google") + link secundÃ¡rio "Continuar com email" (href `/signup`, outline) + subcopy "Leva 2 minutos". **Sem form de lista nesta ramificaÃ§Ã£o.**
    3. **Flag off** â†’ renderizar o JSX atual **verbatim** (badge "Beta fechado" :28-30 + form de acesso antecipado :33-60).
    Red/Green: `src/components/landing/__tests__/access-request-section.test.tsx` (mock getLaunchConfig + GoogleButton): flag on â†’ `GoogleButton` presente (label "Continuar com Google") + link "Continuar com email" (href `/signup`) e NÃƒO o form; flag off â†’ form de acesso antecipado + "Solicitar acesso free" e NÃƒO o GoogleButton/link email.
  </action>
  <verify>
    <automated>npx vitest run src/components/landing/__tests__/access-request-section.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `access-request-section.tsx` renderiza `GoogleButton` (principal, "Continuar com Google") + "Continuar com email" (href `/signup`) quando flag on e o form de acesso antecipado verbatim quando off
    - NÃƒO existe CTA "Criar conta grÃ¡tis" (contrato Ã© Google principal + email secundÃ¡rio)
    - HerÃ³i/features da landing inalterados
    - `npx vitest run src/components/landing/__tests__/access-request-section.test.tsx` â†’ 0 falhas
  </acceptance_criteria>
  <done>Landing dinÃ¢mica conforme flag com testes verdes</done>
</task>

<task type="execute">
  <name>Task 2: page.tsx â€” propagem da flag</name>
  <files>src/app/page.tsx, src/components/landing/access-request-section.tsx</files>
  <read_first>
    - src/app/page.tsx (landing â€” onde AccessRequestSection Ã© renderizado)
    - src/lib/launch-config/config.ts (getLaunchConfig server-only)
  </read_first>
  <action>
    Em `src/app/page.tsx` (server):
    1. Se o componente `AccessRequestSection` for server, nada a mudar aqui (ele lÃª a flag sozinho).
    2. Se for client, ler `const { publicSignupEnabled } = await getLaunchConfig();` no `page.tsx` e passar `<AccessRequestSection publicSignupEnabled={publicSignupEnabled} />`.
    3. NÃƒO alterar hero/features/outras seÃ§Ãµes da landing (F34).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Flag propagada ao `AccessRequestSection` (prop ou leitura server)
    - Demais seÃ§Ãµes da landing intocadas
    - `npm run typecheck` â†’ 0 erros
  </acceptance_criteria>
  <done>Flag propagada sem tocar o visual</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| flag â†” superfÃ­cie pÃºblica | `publicSignupEnabled` decide se a landing expÃµe cadastro |
| landing â†” OAuth/Signup | CTA principal vai para Google OAuth; secundÃ¡rio para /signup â€” abuso |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-10 | Information Disclosure | MÃ¡-config expÃµe cadastro | mitigate | Flag default false; rota `/signup` protegida server-side (42-06) |
| T-42-10a | Spoofing | Lista de espera (flag off) | mitigate | Form de acesso antecipado inalterado (validaÃ§Ã£o/rate-limit existentes preservados) |
| T-42-10b | Elevation of Privilege | Bypass da flag | mitigate | CTA Ã© apenas UI; seguranÃ§a estÃ¡ na rota + Supabase (42-06/42-02) |
| T-42-SC | Tampering | InstalaÃ§Ãµes de pacotes | accept | Nenhum pacote novo |
</threat_model>

<verification>
- `npx vitest run src/components/landing/__tests__/access-request-section.test.tsx` â†’ 0 falhas
- `npm run typecheck` â†’ 0 erros; `npm run lint` â†’ 0 erros
- Git diff: `src/app/page.tsx` sem alteraÃ§Ã£o alÃ©m da possÃ­vel prop
</verification>

<success_criteria>
- Landing flag on â†’ CTA principal "Continuar com Google" (`GoogleButton`) + secundÃ¡rio "Continuar com email" (â†’ `/signup`), sem form de lista
- Landing flag off â†’ "Solicitar acesso free" + formulÃ¡rio verbatim
- `access_requests` preservados como histÃ³rico (sem migration aqui)
- HerÃ³i/features da landing intocados
</success_criteria>

<output>
Create `.planning/phases/42-signup-controlado-elegibilidade-freemium/42-10-SUMMARY.md` when done
</output>

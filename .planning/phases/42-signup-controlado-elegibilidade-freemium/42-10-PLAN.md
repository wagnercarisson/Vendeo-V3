---
phase: 42-signup-controlado-elegibilidade-freemium
plan: 10
type: execute
wave: 3
depends_on: ["42-02"]
files_modified:
  - src/components/landing/access-request-section.tsx
  - src/app/page.tsx
  - src/__tests__/landing/access-request-section.test.tsx
autonomous: true
requirements: ["access-request-history", "signup-page", "launch-config (Nova flag publicSignupEnabled)"]

must_haves:
  truths:
    - "CTA da landing depende da flag: flag on → 'Criar conta grátis' (href /signup); flag off → formulário de lista de espera (acesso antecipado) preservado"
    - "`access_requests` existentes viram histórico (D4) — o formulário continua gravando quando flag off; tabela/backfill intactos"
    - "Copy CTA aprovada (UI-SPEC): flag on 'Criar conta grátis — leva 2 min'; flag off mantém 'Solicitar acesso free'"
    - "Herói e features da landing intocados (F34 / padrão visual preservado)"
  artifacts:
    - path: "src/components/landing/access-request-section.tsx"
      provides: "Seção dinâmica"
      contains: "Criar conta grátis"
  key_links:
    - from: "access-request-section.tsx"
      to: "src/lib/launch-config/config.ts"
      via: "getLaunchConfig"
      pattern: "publicSignupEnabled"
---

<objective>
Ajustar a landing para refletir a flag (D4): quando `publicSignupEnabled` (flag on) → CTA "Criar conta grátis" apontando para `/signup`; quando off → preservar o formulário de lista de espera (acesso antecipado). `access_requests` vira histórico — a gravação continua para flag off; nada é migrado/excluído.

Purpose: D4 — landing dinâmica; access_requests como histórico.
Output: landing dinâmica + testes.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D4 — landing dinâmica; access_requests histórico)
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-UI-SPEC.md (copy CTA; hero intacto)
@src/components/landing/access-request-section.tsx (atual — Beta fechado badge :28-30, form de acesso antecipado)
@src/app/page.tsx (landing — hero/features; não tocar visual)
@src/lib/launch-config/config.ts (getLaunchConfig server-only)
@src/lib/landing/ (services existentes — access request handler/rota)

<interfaces>
- `access-request-section.tsx` (server ou client com prop — conforme padrão atual): receber `publicSignupEnabled` como prop OU ler via server. Padrão atual: componente com badge "Beta fechado" (:28-30) + form. Nova lógica:
  - flag on → renderizar CTA link "Criar conta grátis" (href `/signup`) + subcopy "Leva 2 minutos" — sem form de lista.
  - flag off → renderizar o JSX atual verbatim (badge + form de acesso antecipado).
- `access_requests`: a gravação (route/handler) continua existindo (flag off). Nada de backfill/drop. Nada de migration neste plan (42-12 cuida de migrations idempotentes/effective_at).
- Copy CTA conforme UI-SPEC (MASTER tokens — green solid bg-accent-green).
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: access-request-section.tsx — flag on/off</name>
  <files>src/components/landing/access-request-section.tsx, src/__tests__/landing/access-request-section.test.tsx</files>
  <read_first>
    - src/components/landing/access-request-section.tsx (atual — badge :28-30, form :33-60)
    - src/lib/launch-config/config.ts (getLaunchConfig — server-only)
    - .planning/phases/42-signup-controlado-elegibilidade-freemium/42-UI-SPEC.md (copy CTA)
  </read_first>
  <action>
    Em `src/components/landing/access-request-section.tsx`:
    1. Tornar o componente dinâmico: se server component, ler `const { publicSignupEnabled } = await getLaunchConfig();` (ou receber como prop do `page.tsx`). Se client, receber prop booleana.
    2. Flag on → substituir por: heading CTA + link `a href="/signup"` com label "Criar conta grátis" (bg-accent-green, MASTER) + subcopy "Leva 2 minutos". Sem form de lista nesta ramificação.
    3. Flag off → renderizar o JSX atual **verbatim** (badge "Beta fechado" :28-30 + form de acesso antecipado :33-60).
    Red/Green: `src/__tests__/landing/access-request-section.test.tsx` (mock getLaunchConfig): flag on → link "Criar conta grátis" (href `/signup`) e NÃO o form; flag off → form de acesso antecipado e NÃO o link.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/landing/access-request-section.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `access-request-section.tsx` renderiza "Criar conta grátis" (href `/signup`) quando flag on e o form de acesso antecipado verbatim quando off
    - Herói/features da landing inalterados
    - `npx vitest run src/__tests__/landing/access-request-section.test.tsx` → 0 falhas
  </acceptance_criteria>
  <done>Landing dinâmica conforme flag com testes verdes</done>
</task>

<task type="execute">
  <name>Task 2: page.tsx — propagem da flag</name>
  <files>src/app/page.tsx, src/components/landing/access-request-section.tsx</files>
  <read_first>
    - src/app/page.tsx (landing — onde AccessRequestSection é renderizado)
    - src/lib/launch-config/config.ts (getLaunchConfig server-only)
  </read_first>
  <action>
    Em `src/app/page.tsx` (server):
    1. Se o componente `AccessRequestSection` for server, nada a mudar aqui (ele lê a flag sozinho).
    2. Se for client, ler `const { publicSignupEnabled } = await getLaunchConfig();` no `page.tsx` e passar `<AccessRequestSection publicSignupEnabled={publicSignupEnabled} />`.
    3. NÃO alterar hero/features/outras seções da landing (F34).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Flag propagada ao `AccessRequestSection` (prop ou leitura server)
    - Demais seções da landing intocadas
    - `npm run typecheck` → 0 erros
  </acceptance_criteria>
  <done>Flag propagada sem tocar o visual</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| flag ↔ superfície pública | `publicSignupEnabled` decide se a landing expõe cadastro |
| landing ↔ rota de signup | CTA direciona a criação de conta — abuso |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-10 | Information Disclosure | Má-config expõe cadastro | mitigate | Flag default false; rota `/signup` protegida server-side (42-06) |
| T-42-10a | Spoofing | Lista de espera (flag off) | mitigate | Form de acesso antecipado inalterado (validação/rate-limit existentes preservados) |
| T-42-10b | Elevation of Privilege | Bypass da flag | mitigate | CTA é apenas UI; segurança está na rota + Supabase (42-06/42-02) |
| T-42-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo |
</threat_model>

<verification>
- `npx vitest run src/__tests__/landing/access-request-section.test.tsx` → 0 falhas
- `npm run typecheck` → 0 erros; `npm run lint` → 0 erros
- Git diff: `src/app/page.tsx` sem alteração além da possível prop
</verification>

<success_criteria>
- Landing flag on → CTA "Criar conta grátis" → /signup; flag off → lista de espera verbatim
- `access_requests` preservados como histórico (sem migration aqui)
- Herói/features da landing intocados
</success_criteria>

<output>
Create `.planning/phases/42-signup-controlado-elegibilidade-freemium/42-10-SUMMARY.md` when done
</output>
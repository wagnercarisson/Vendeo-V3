---
phase: 42-signup-controlado-elegibilidade-freemium
plan: 11
type: execute
wave: 3
depends_on: ["42-07"]
files_modified:
  - src/lib/legal/versions.ts
  - src/lib/legal/acceptance-service.ts
  - src/components/legal/privacy-gate.tsx
  - src/components/legal/privacy-recovery.tsx
  - src/app/(legal)/termos/page.tsx
  - src/app/(legal)/politica-de-privacidade/page.tsx
  - src/lib/legal/__tests__/acceptance-service.test.ts
autonomous: true
requirements: ["legal-acceptance-service", "privacy-acknowledgement"]

must_haves:
  truths:
    - "Versões: Terms v1.4 / Privacy v1.3 — constantes + páginas estáticas atualizadas"
    - "`privacyPending` (sessionStorage) gravado no signup é lido e liquidado APENAS por `PrivacyRecovery` na primeira autenticação; `PrivacyGate` não consome o mesmo registro"
    - "Coordenação única PrivacyGate × PrivacyRecovery — nenhum fluxo se perde (email signup e OAuth sempre chegam a recordAcceptance persistido)"
    - "Consentimento opcional de comunicações gravado via `recordAcceptance` (opt_in_communications) quando schema suporta; senão sessionStorage + TODO 42-12"
    - "Migrations SQL idempotentes + effective_at → plan 42-12 (não aqui)"
  artifacts:
    - path: "src/lib/legal/acceptance-service.ts"
      provides: "Versões + gravação opt_in"
      contains: "opt_in_communications"
    - path: "src/components/legal/privacy-recovery.tsx"
      provides: "Coordenação única"
      contains: "privacyPending"
  key_links:
    - from: "privacy-recovery.tsx"
      to: "src/lib/legal/acceptance-service.ts"
      via: "recordAcceptance"
      pattern: "recordAcceptance"
---

<objective>
Atualizar versões legais (Terms v1.4 / Privacy v1.3) e garantir a coordenação única do `privacyPending` entre `PrivacyGate` e `PrivacyRecovery` (D12/D16): o registro gravado no signup só é liquidado pelo PrivacyRecovery; o gate pós-OAuth não consome o mesmo registro. Consentimento de comunicações persistido via `recordAcceptance`.

Purpose: D12 (legal v1.4/v1.3) + D16 (coordenação única).
Output: versões + coordenação + testes.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D12 — legal v1.4/v1.3; D16 — PrivacyGate/PrivacyRecovery)
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-UI-SPEC.md (coordenação; copy)
@src/lib/legal/acceptance-service.ts (getAcceptanceStatus :43-62, recordAcceptance)
@src/lib/legal/versions.ts (PRIVACY_POLICY_VERSION / TERMS_VERSION atuais)
@src/components/legal/privacy-gate.tsx (gate existente :18-71)
@src/components/legal/privacy-recovery.tsx (sessionStorage privacyPending :38-51)
@src/app/(legal)/termos/page.tsx e src/app/(legal)/politica-de-privacidade/page.tsx (páginas estáticas)

<interfaces>
- Versões: atualizar constantes (termos → v1.4; privacidade → v1.3) e as páginas estáticas que exibem a versão. Migrations SQL no plan 42-12.
- Coordenação `privacyPending` (sessionStorage, chave `vendeo.privacyPending`):
  - Signup grava (ciência + opt-in) antes de criar a conta.
  - `PrivacyRecovery` é o ÚNICO que lê e liquida (remove + recordAcceptance) na primeira autenticação.
  - `PrivacyGate` (pós-OAuth) não lê nem liquida esse registro; usa fluxo próprio (identidade nova → recordAcceptance). Distinção de origem via `vendeo.privacyOrigin` (google_oauth vs email_signup).
  - Nenhum fluxo se perde.
- `recordAcceptance` aceita `opt_in_communications` (bool) — persistir quando o schema atual suporta; senão sessionStorage + TODO para 42-12.
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Versões legais v1.4 / v1.3</name>
  <files>src/lib/legal/versions.ts, src/lib/legal/acceptance-service.ts, src/app/(legal)/termos/page.tsx, src/app/(legal)/politica-de-privacidade/page.tsx, src/lib/legal/__tests__/acceptance-service.test.ts
  </files>
  <read_first>
    - src/lib/legal/versions.ts (PRIVACY_POLICY_VERSION / TERMS_VERSION atuais)
    - src/lib/legal/acceptance-service.ts (getAcceptanceStatus :43-62, recordAcceptance)
    - src/app/(legal)/termos/page.tsx (versão exibida)
    - src/app/(legal)/politica-de-privacidade/page.tsx (versão exibida)
  </read_first>
  <action>
    1. Em `src/lib/legal/versions.ts` (ou constante equivalente): `TERMS_VERSION = "1.4"`; `PRIVACY_POLICY_VERSION = "1.3"`.
    2. Em `src/lib/legal/acceptance-service.ts`: garantir que `getAcceptanceStatus` compara com as novas versões (sem lógica nova) e `recordAcceptance` aceita e propaga `opt_in_communications` (boolean) quando o schema suporta; senão, comentar o campo e registrar TODO apontando 42-12.
    3. Nas páginas estáticas `termos/page.tsx` e `politica-de-privacidade/page.tsx`: atualizar a versão exibida ("Versão 1.4" / "Versão 1.3") e o rodapé/fim de página.
    Red/Green: estender `acceptance-service.test.ts`: `getAcceptanceStatus` retorna `acknowledged: true` para versão 1.4/1.3 persistida; `recordAcceptance` grava `opt_in_communications` quando passado.
  </action>
  <verify>
    <automated>npx vitest run src/lib/legal/__tests__/acceptance-service.test.ts
  </verify>
  <acceptance_criteria>
    - `TERMS_VERSION = "1.4"` e `PRIVACY_POLICY_VERSION = "1.3"` nas constantes
    - Páginas estáticas exibem "Versão 1.4" e "Versão 1.3"
    - `recordAcceptance` propaga `opt_in_communications` (ou TODO 42-12 se schema não suporta)
    - `npx vitest run src/lib/legal/__tests__/acceptance-service.test.ts` → 0 falhas
  </acceptance_criteria>
  <done>Versões v1.4/v1.3 aplicadas com testes verdes</done>
</task>

<task type="execute">
  <name>Task 2: Coordenação única PrivacyGate × PrivacyRecovery</name>
  <files>src/components/legal/privacy-gate.tsx, src/components/legal/privacy-recovery.tsx, src/app/(auth)/signup/page.tsx
  </files>
  <read_first>
    - src/components/legal/privacy-recovery.tsx (sessionStorage privacyPending :38-51)
    - src/components/legal/privacy-gate.tsx (gate existente :18-71)
    - src/components/legal/privacy-acknowledge-modal.tsx (modal)
  </read_first>
  <action>
    1. `PrivacyRecovery` (único consumidor do `vendeo.privacyPending`): na primeira autenticação, ler o registro; se presente, liquidar (remover da sessionStorage + `recordAcceptance` com ciência + opt_in do registro + origem email_signup). Não deixar lixo.
    2. `PrivacyGate` (pós-OAuth): NÃO ler/liquidar `vendeo.privacyPending`. Usar fluxo próprio: se origem `google_oauth` (via `vendeo.privacyOrigin`), a ciência do gate → `recordAcceptance` (Privacy v1.3/Terms v1.4) uma única vez. Não duplicar quando o PrivacyRecovery já liquidou o do signup.
    3. `signup-form` (42-06): garantir que grava `vendeo.privacyPending` com `{ privacyAcknowledged, optIn, version: "1.3/1.4", origin: "email_signup" }` ANTES de `signUp`; e `vendeo.privacyOrigin = "email_signup"`. (Verificação — já implementado em 42-06; só alinhar shape.)
  </action>
  <verify>
    <automated>npm run typecheck; if ($?) { npx vitest run src/components/legal/__tests__/privacy-gate.test.tsx src/components/legal/__tests__/privacy-recovery.test.tsx --passWithNoTests }
  </verify>
  <acceptance_criteria>
    - `PrivacyRecovery` lê/liquida `vendeo.privacyPending`; `PrivacyGate` NÃO consome esse registro (usa origem própria)
    - `recordAcceptance` persistido exatamente uma vez por fluxo (email signup via recovery; OAuth via gate)
    - `npm run typecheck` → 0 erros
  </acceptance_criteria>
  <done>Coordenação única implementada; nenhum fluxo se perde</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sessionStorage ↔ serviço legal | `privacyPending` é estado efêmero do navegador — não é fonte de verdade legal |
| serviço legal ↔ banco | `recordAcceptance` persiste consentimento — integridade auditável |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-11 | Tampering | `privacyPending` (sessionStorage) | mitigate | Tratado como não-confiança; liquidado apenas pelo PrivacyRecovery; gate não consome o mesmo registro |
| T-42-11a | Spoofing | Consentimento forjado | mitigate | `recordAcceptance` com origem e versão; data efetiva (42-12) |
| T-42-11b | Information Disclosure | Fluxo perdido | mitigate | Coordenação única: email signup → PrivacyRecovery; OAuth → PrivacyGate; ambos persistem recordAcceptance |
| T-42-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo |
</threat_model>

<verification>
- `npx vitest run src/lib/legal/__tests__/acceptance-service.test.ts` → 0 falhas
- `npm run typecheck` → 0 erros; `npm run lint` → 0 erros
- Grep: `privacyPending` referenciado apenas em privacy-recovery (único consumidor) e signup-form (escritor)
</verification>

<success_criteria>
- Terms v1.4 / Privacy v1.3 nas constantes e páginas estáticas
- `privacyPending` liquidado apenas pelo PrivacyRecovery; PrivacyGate não consome o mesmo registro
- Consentimento de comunicações persistido (ou sessionStorage + TODO 42-12)
</success_criteria>

<output>
Create `.planning/phases/42-signup-controlado-elegibilidade-freemium/42-11-SUMMARY.md` when done
</output>
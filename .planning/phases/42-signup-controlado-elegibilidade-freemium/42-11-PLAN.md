---
phase: 42-signup-controlado-elegibilidade-freemium
plan: 11
type: execute
wave: 3
depends_on: ["42-06", "42-07"]
files_modified:
  - public/docs/legal/terms-of-service-v1-4.md
  - public/docs/legal/privacy-policy-v1-3.md
  - src/lib/legal/document-content.ts
  - src/components/legal/privacy-gate.tsx
  - src/components/legal/privacy-recovery.tsx
  - src/lib/legal/__tests__/document-content.test.ts
  - src/components/legal/__tests__/privacy-recovery.test.tsx
autonomous: true
requirements: ["legal-acceptance-service", "privacy-acknowledgement"]

must_haves:
  truths:
    - "**Modelo legal REAL (verificado em código):** ciência da Privacidade → `registerPrivacyAcknowledgement` (src/lib/legal/privacy.ts) → `privacy_acknowledgements` (user-level, upsert onConflict user_id); opt-in comercial → `recordConsentEvent` (src/lib/legal/consent.ts) → `user_consent_events` (specs chamam `consent_events`). Ambos via endpoint `/api/legal/acknowledge-privacy` (já existe e usa requireApiUser + getCurrentVersion('privacy_policy'))"
    - "**NÃO usar** `registerAcceptance`/`acceptance-service.ts`/`legal_acceptances` para ciência de privacidade ou opt-in — `legal_acceptances` é aceite contratual store-level (Terms/AUP, onboarding F36) e fica INTACTA na F42"
    - "**NÃO existem** `versions.ts` nem `legal_acceptance` (singular); versões vivem em `legal_document_versions` (getCurrentVersion/document-versions.ts, já com effective_at); publicação de versões → plan 42-12 (migration), NÃO aqui"
    - "`PrivacyRecovery` (src/components/legal/privacy-recovery.tsx) JÁ lê `privacyPending` (sessionStorage) e chama `/api/legal/acknowledge-privacy` com `communicationsOptIn`, removendo o registro em caso de ok — verificar/alinhar, NÃO reescrever"
    - "`PrivacyGate` (src/components/legal/privacy-gate.tsx:18) JÁ chama `/api/legal/acknowledge-privacy` no pós-OAuth — garantir envio do opt-in opcional (`communicationsOptIn`) no body; NUNCA `user_metadata` como evidência legal (D16)"
    - "Coordenação única PrivacyGate × PrivacyRecovery — nenhum fluxo se perde: email/signup → PrivacyRecovery; OAuth → PrivacyGate; ambos registram ciência + opt-in autenticados; sem modal duplicado nem flash"
    - "Conteúdo legal: criar `terms-of-service-v1-4.md` (cláusula 3.1: remover 'limitado a usuários convidados'; acesso público gratuito com elegibilidade; autenticação por terceiros) e `privacy-policy-v1-3.md` (remover 'beta, gratuita e fechada'; captcha, confirmação de email, dados do Google com finalidade exclusivamente autenticacional) + registrar no catálogo `document-content.ts`"
  artifacts:
    - path: "public/docs/legal/terms-of-service-v1-4.md"
      provides: "Conteúdo Terms v1.4"
      contains: "v1.4"
    - path: "public/docs/legal/privacy-policy-v1-3.md"
      provides: "Conteúdo Privacy v1.3"
      contains: "v1.3"
    - path: "src/lib/legal/document-content.ts"
      provides: "Catálogo v1.4/v1.3"
      contains: "terms-of-service-v1-4"
  key_links:
    - from: "privacy-recovery.tsx"
      to: "src/app/api/legal/acknowledge-privacy/route.ts"
      via: "fetch POST"
      pattern: "communicationsOptIn"
    - from: "privacy-gate.tsx"
      to: "src/app/api/legal/acknowledge-privacy/route.ts"
      via: "fetch POST"
      pattern: "communicationsOptIn"
    - from: "document-content.ts"
      to: "public/docs/legal/"
      via: "catalog"
      pattern: "terms-of-service-v1-4"
---

<objective>
Preparar a transição legal D12/D16 com o modelo REAL (validado em código): criar os conteúdos Terms v1.4 / Privacy v1.3 + catálogo `document-content.ts`, e garantir a coordenação única PrivacyGate × PrivacyRecovery com persistência via `registerPrivacyAcknowledgement` + `recordConsentEvent` no endpoint `/api/legal/acknowledge-privacy` (user-level: `privacy_acknowledgements` + `consent_events`). A publicação das versões em `legal_document_versions` (effective_at) fica no plan 42-12.

Purpose: D12 (v1.4/v1.3 + tolerância) + D16 (coordenação única).
Output: conteúdos + catálogo + coordenação + testes.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D12 — v1.4/v1.3; D16 — coordenação única; tolerância técnica)
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-UI-SPEC.md (PrivacyGate/PrivacyRecovery — coordenação; consentimento via /api/legal/acknowledge-privacy)
@src/lib/legal/privacy.ts (registerPrivacyAcknowledgement — privacy_acknowledgements; hasValidPrivacyAcknowledgement)
@src/lib/legal/consent.ts (recordConsentEvent — user_consent_events/consent_events; getEffectiveConsent)
@src/app/api/legal/acknowledge-privacy/route.ts (POST: requireApiUser + getCurrentVersion('privacy_policy') + registerPrivacyAcknowledgement + recordConsentEvent(source 'signup'))
@src/lib/legal/document-versions.ts (getCurrentVersion lê legal_document_versions com effective_at — JÁ existe)
@src/lib/legal/document-content.ts (catálogo version → filePath)
@src/components/legal/privacy-gate.tsx (gate :18-71 — chama o endpoint)
@src/components/legal/privacy-recovery.tsx (sessionStorage privacyPending :38-51 — chama o endpoint)
@src/app/(marketing)/termos/page.tsx e src/app/(marketing)/privacidade/page.tsx (páginas leem getCurrentVersion + getDocumentFile dinamicamente — SEM hardcode de versão)
@public/docs/legal/ (conteúdos legais existentes: terms v1.0-1.3, privacy v1.0-1.2, aup v1.0-1.1)

<interfaces>
- Conteúdos (novos arquivos em `public/docs/legal/`):
  - `terms-of-service-v1-4.md`: base v1.3 + cláusula 3.1 sem "limitado a usuários convidados" + acesso público gratuito com elegibilidade e critérios de liberação + autenticação por terceiros (Google OAuth). Versão "v1.4" + data de publicação.
  - `privacy-policy-v1-3.md`: base v1.2 + remover "beta, gratuita e fechada" + captcha (Cloudflare Turnstile) e confirmação de email no cadastro + autenticação por terceiros (dados recebidos do Google: identificador, email, nome, avatar; finalidade exclusivamente autenticacional; nenhuma permissão sobre Gmail/Drive/outros). Versão "v1.3" + data de publicação.
- `document-content.ts`: adicionar `terms_of_service["v1.4"]` → `/docs/legal/terms-of-service-v1-4.md` e `privacy_policy["v1.3"]` → `/docs/legal/privacy-policy-v1-3.md` no catálogo `DOCUMENT_CATALOG`.
- `PrivacyRecovery` (email/signup): já lê `vendeo.privacyPending`/`privacyPending` (sessionStorage) e chama `/api/legal/acknowledge-privacy` com `communicationsOptIn` — verificar shape (align com o que 42-06 grava) e garantir removeItem pós-sucesso.
- `PrivacyGate` (pós-OAuth): já chama `/api/legal/acknowledge-privacy` no confirm — garantir que o body carrega o opt-in opcional (`communicationsOptIn`) quando o usuário concede; NUNCA `user_metadata`.
- Coordenação única (D16): se `privacyPending` existe (email/signup) → PrivacyRecovery processa; PrivacyGate NÃO abre modal duplicado nesse fluxo; pós-OAuth sem acknowledgment vigente → PrivacyGate obrigatório. Nenhum fluxo se perde.
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Conteúdos legais v1.4 / v1.3 + catálogo</name>
  <files>public/docs/legal/terms-of-service-v1-4.md, public/docs/legal/privacy-policy-v1-3.md, src/lib/legal/document-content.ts, src/lib/legal/__tests__/document-content.test.ts</files>
  <read_first>
    - public/docs/legal/terms-of-service-v1-3.md (base — cláusula 3.1 atual)
    - public/docs/legal/privacy-policy-v1-2.md (base — "beta, gratuita e fechada")
    - src/lib/legal/document-content.ts (catálogo DOCUMENT_CATALOG + getDocumentFile)
  </read_first>
  <action>
    1. Criar `public/docs/legal/terms-of-service-v1-4.md` (português, mesmo estilo v1.3):
       - Header: "# Termos de Uso - Vendeo", "**Versão:** v1.4", "**Data de publicação:** <hoje>".
       - Cláusula 3.1: REMOVER a limitação a "usuários convidados"; descrever **acesso público gratuito** com **elegibilidade** e critérios de liberação (substituir redação do beta fechado).
       - Adicionar **autenticação por terceiros** (Google OAuth) — dados recebidos com finalidade exclusivamente autenticacional.
       - Manter o restante do conteúdo v1.3 (licenças, IA, responsabilidades, créditos).
    2. Criar `public/docs/legal/privacy-policy-v1-3.md`:
       - Header: "# Política de Privacidade - Vendeo", "**Versão:** v1.3", "**Data de publicação:** <hoje>".
       - REMOVER "beta, gratuita e fechada" (1. Controlador e Contato + onde aparecer).
       - Adicionar seção sobre **captcha (Cloudflare Turnstile)** e **confirmação de email** no cadastro.
       - Adicionar seção sobre **autenticação por terceiros (Google OAuth)**: dados recebidos (identificador, email, nome, avatar), finalidade exclusivamente autenticacional, NENHUMA permissão sobre Gmail/Drive/outros produtos do Google.
       - Manter o restante (LGPD, bases legais, transferência, retenção, direitos).
    3. `document-content.ts`: adicionar entradas ao `DOCUMENT_CATALOG`:
       - `terms_of_service["v1.4"]` → `{ filePath: "/docs/legal/terms-of-service-v1-4.md" }`
       - `privacy_policy["v1.3"]` → `{ filePath: "/docs/legal/privacy-policy-v1-3.md" }`
    4. Red/Green: criar `src/lib/legal/__tests__/document-content.test.ts` (se não existir): `getDocumentFile("terms_of_service", "v1.4")` → caminho v1-4; `getDocumentFile("privacy_policy", "v1.3")` → caminho v1-3; versões inexistentes → null (regressão). NÃO tocar acceptance-service (modelo errado não é usado aqui).
  </action>
  <verify>
    <automated>npx vitest run src/lib/legal/__tests__/document-content.test.ts --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `terms-of-service-v1-4.md` e `privacy-policy-v1-3.md` existem com os ajustes D12 (sem "convidados" na 3.1; sem "beta fechada" na privacidade; captcha/confirmação/OAuth descritos)
    - `document-content.ts` mapeia v1.4 e v1.3 para os novos arquivos
    - Testes do catálogo verdes (v1.4/v1.3 resolvem; versões ausentes → null)
  </acceptance_criteria>
  <done>Conteúdos v1.4/v1.3 + catálogo com testes verdes</done>
</task>

<task type="execute">
  <name>Task 2: Coordenação única PrivacyGate × PrivacyRecovery (modelo real)</name>
  <files>src/components/legal/privacy-gate.tsx, src/components/legal/privacy-recovery.tsx, src/components/legal/__tests__/privacy-recovery.test.tsx</files>
  <read_first>
    - src/components/legal/privacy-recovery.tsx (sessionStorage privacyPending :38-51 + fetch /api/legal/acknowledge-privacy)
    - src/components/legal/privacy-gate.tsx (gate :18-71 — handleConfirm fetch com body "{}")
    - src/app/api/legal/acknowledge-privacy/route.ts (contrato do endpoint: body `communicationsOptIn`, requireApiUser)
    - src/components/auth/signup-form.tsx (42-06 — shape gravado em privacyPending: privacyAcknowledged + communicationsOptIn)
  </read_first>
  <action>
    1. `PrivacyRecovery` (verificar/alinhar, NÃO reescrever):
       - Confirmar que o shape lido de `privacyPending` é `{ privacyAcknowledged: boolean; communicationsOptIn: boolean }` e que POST envia `communicationsOptIn` (já faz em :19-23).
       - Confirmar `sessionStorage.removeItem("privacyPending")` pós-`res.ok` (:26).
       - Alinhar chave se 42-06 gravar `vendeo.privacyPending` vs `privacyPending` — decidir UMA chave (manter a já usada pelo recovery) e garantir consistência no signup (42-06) via código ou nota de alinhamento.
    2. `PrivacyGate` (pós-OAuth):
       - Garantir que o `handleConfirm` do gate envia o opt-in opcional: se o fluxo do gate oferece consentimento de comunicações (UI-SPEC: "Consentimento comunicações: optional, recorded via /api/legal/acknowledge-privacy authenticated"), carregar `communicationsOptIn` no body (bool). Se hoje o gate não coleta opt-in, adicionar checkbox opcional no fluxo do gate (sem quebrar o modal verbatim — adicionar à superfície do gate, não alterar a semântica do modal).
       - NUNCA usar `user_metadata.communicationsOptIn` como evidência (D16).
    3. Coordenação única (D16):
       - Fluxo email/signup: `privacyPending` existe → `PrivacyRecovery` processa UMA vez; `PrivacyGate` não abre modal duplicado enquanto pending em processamento (guard no gate: se `sessionStorage.privacyPending` presente, não abrir modal — deixar o recovery liquidar).
       - Fluxo OAuth: sem pending; `PrivacyGate` roda quando `acknowledged === false` (layout `(app)` já monta o gate).
       - Nenhum fluxo se perde: ambos terminam em `privacy_acknowledgements` + `consent_events` (endpoint único).
    Red/Green: estender `privacy-recovery.test.tsx` (ou criar): processa pending → POST com `communicationsOptIn` → removeItem pós-ok; erro → mantém registro e mostra mensagem. Se gate ganhou opt-in: teste do gate envia `communicationsOptIn` no body.
  </action>
  <verify>
    <automated>npm run typecheck; if ($?) { npx vitest run src/components/legal/__tests__/privacy-recovery.test.tsx --passWithNoTests }
  </verify>
  <acceptance_criteria>
    - `PrivacyRecovery` lê `privacyPending`, POSTa `communicationsOptIn` em `/api/legal/acknowledge-privacy` e removeItem pós-ok (verificado/ajustado)
    - `PrivacyGate` (OAuth) envia opt-in opcional no body; NUNCA usa `user_metadata` como evidência
    - Guard anti-flash: pending presente → gate não abre modal duplicado (recovery liquida)
    - `npm run typecheck` → 0 erros; testes do recovery/gate verdes
  </acceptance_criteria>
  <done>Coordenação única PrivacyGate × PrivacyRecovery com modelo real; nenhum fluxo se perde</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sessionStorage ↔ serviço legal | `privacyPending` é estado efêmero do navegador — não é fonte de verdade legal |
| serviço legal ↔ banco | `registerPrivacyAcknowledgement` → `privacy_acknowledgements`; `recordConsentEvent` → `consent_events` — persistência autenticada |
| cliente ↔ endpoint | `/api/legal/acknowledge-privacy` exige `requireApiUser` (userId do claims, nunca do body) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-11 | Tampering | `privacyPending` (sessionStorage) | mitigate | Tratado como não-confiança; liquidado apenas pelo PrivacyRecovery; gate não consome o mesmo registro (guard anti-flash) |
| T-42-11a | Spoofing | Consentimento forjado | mitigate | `registerPrivacyAcknowledgement`/`recordConsentEvent` com userId autenticado (requireApiUser) e versão vigente via `getCurrentVersion`; NUNCA `user_metadata` como evidência |
| T-42-11b | Information Disclosure | Fluxo perdido (modal duplicado/flash) | mitigate | Coordenação única: email/signup → PrivacyRecovery; OAuth → PrivacyGate; guard anti-flash no gate |
| T-42-11c | Tampering | Versão de conteúdo errada | mitigate | Catálogo `document-content.ts` mapeia v1.4/v1.3 para arquivos corretos; páginas leem via `getCurrentVersion` (sem hardcode) |
| T-42-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo |
</threat_model>

<verification>
- `npx vitest run src/lib/legal/__tests__/document-content.test.ts` → 0 falhas
- `npx vitest run src/components/legal/__tests__/privacy-recovery.test.tsx` → 0 falhas
- `npm run typecheck` → 0 erros; `npm run lint` → 0 erros
- Grep: `privacyPending` referenciado apenas em privacy-recovery (único consumidor) e signup-form (escritor); nenhum uso de `recordAcceptance`/`opt_in_communications` para ciência/opt-in
</verification>

<success_criteria>
- Conteúdos Terms v1.4 e Privacy v1.3 criados (D12) + catálogo `document-content.ts` atualizado
- Coordenação única PrivacyGate × PrivacyRecovery com modelo real (`privacy_acknowledgements`/`consent_events` via `/api/legal/acknowledge-privacy`), sem modal duplicado nem flash
- Nenhuma mudança em `acceptance-service.ts`/`legal_acceptances` (store-level Terms/AUP intactos)
- Publicação das versões em `legal_document_versions` delegada ao plan 42-12
</success_criteria>

<output>
Create `.planning/phases/42-signup-controlado-elegibilidade-freemium/42-11-SUMMARY.md` when done
</output>

---
phase: 42-signup-controlado-elegibilidade-freemium
plan: 12
type: execute
wave: 3
depends_on: ["42-11"]
files_modified:
  - supabase/migrations/20260817000001_publish_legal_signup_versions.sql
  - supabase/config.toml (verificação)
autonomous: true
requirements: ["legal-acceptance-service", "privacy-acknowledgement", "launch-config (Nova flag publicSignupEnabled)"]

must_haves:
  truths:
    - "**NÃO existe tabela `legal_acceptance` (singular)** — nenhuma migration DDL nela. Aceites contratuais vivem em `legal_acceptances` (plural, store-level) e NÃO são alterados na F42"
    - "Publicação de versões = INSERT em `legal_document_versions` (document_type, version, summary, effective_at) — tabela JÁ tem `effective_at` (document-versions.ts lê com lte(now())); migration idempotente via `ON CONFLICT (document_type, version) DO UPDATE` (padrão 20260731000004_publish_legal_beta_freemium_versions.sql)"
    - "Publicar Terms **v1.4** (cláusula 3.1 sem 'usuários convidados'; acesso público gratuito com elegibilidade; autenticação por terceiros) e Privacy **v1.3** (sem 'beta, gratuita e fechada'; captcha, confirmação de email, dados do Google — finalidade exclusivamente autenticacional). AUP v1.1 mantida (NÃO republicar)"
    - "`effective_at` controlado: agora() por padrão; se configurado futuro (go-live), reaceite ocorre antes via `getAcceptanceStatus` → `outdated` → `login_reacceptance` (fluxo F30 reusado); tolerância técnica: nenhuma loja perde acesso/capacidade ao publicar (clearance fail-closed só em funcionalidades protegidas)"
    - "Paridade config.toml: minimum_password_length = 8, enable_confirmations = true, [auth.captcha] turnstile, provider Google, enable_manual_linking = false — verificada sem alteração destrutiva"
    - "Conteúdos dos documentos (md) já criados no plan 42-11; catálogo `document-content.ts` já mapeia v1.4/v1.3"
  artifacts:
    - path: "supabase/migrations/20260817000001_publish_legal_signup_versions.sql"
      provides: "Publicação v1.4/v1.3 idempotente"
      contains: "ON CONFLICT"
  key_links:
    - from: "supabase/migrations/20260817000001_publish_legal_signup_versions.sql"
      to: "src/lib/legal/document-versions.ts"
      via: "legal_document_versions"
      pattern: "effective_at"
    - from: "src/lib/legal/document-content.ts"
      to: "supabase/migrations"
      via: "catalog"
      pattern: "terms-of-service-v1-4"
---

<objective>
Publicar as versões legais Terms v1.4 e Privacy v1.3 em `legal_document_versions` via migration idempotente (INSERT ... ON CONFLICT, com `effective_at` controlado — JÁ existente na tabela), reutilizando o padrão da migration `20260731000004_publish_legal_beta_freemium_versions.sql`. **Nenhuma DDL em `legal_acceptance` (singular — tabela não existe)** e nenhuma mudança em `legal_acceptances`/`acceptance-service.ts`. Verificar paridade do `supabase/config.toml` sem alteração destrutiva.

Purpose: D12 (publicação v1.4/v1.3 + reaceite com tolerância) + D13 (paridade config).
Output: migration de publicação + verificação config.toml.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D12 — v1.4/v1.3 + effective_at + tolerância; D13 — paridade config.toml)
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-11-PLAN.md (conteúdos v1.4/v1.3 + catálogo já criados)
@supabase/migrations/20260731000004_publish_legal_beta_freemium_versions.sql (padrão ON CONFLICT a reusar)
@src/lib/legal/document-versions.ts (getCurrentVersion lê legal_document_versions com effective_at — sem mudança de schema)
@src/lib/legal/document-content.ts (catálogo 42-11)
@supabase/migrations/ (padrão de naming por data)
@supabase/config.toml (paridade D13 — verificação apenas)

<interfaces>
- Migration `supabase/migrations/20260817000001_publish_legal_signup_versions.sql`:
  - `INSERT INTO public.legal_document_versions (document_type, version, summary, effective_at) VALUES (...)` para `terms_of_service` v1.4 e `privacy_policy` v1.3, com `summary` descrevendo os ajustes D12 (espelhar estilo das summaries existentes).
  - `ON CONFLICT (document_type, version) DO UPDATE SET summary = EXCLUDED.summary, effective_at = EXCLUDED.effective_at;` (idempotente — padrão 20260731000004).
  - `effective_at = now()` por padrão; NOTA no arquivo: se o rollout exigir reaceite antes do go-live, configurar `effective_at` futuro (mudar o valor na migration/seed antes do deploy).
  - Comentário REVERT com DELETE condicionais (espelhar padrão).
  - NADA de DDL/DROP/ALTER.
- `legal_acceptances` (plural): NÃO tocada. `acceptance-service.ts`: NÃO tocado.
- `supabase/config.toml`: verificar (sem alterar) — minimum_password_length = 8, enable_confirmations = true, [auth.captcha] turnstile, provider Google + URLs de callback, enable_manual_linking = false. Divergências → registrar no SUMMARY (não editar destrutivamente).
</interfaces>
</context>

<tasks>

<task type="execute">
  <name>Task 1: Migration de publicação Terms v1.4 / Privacy v1.3</name>
  <files>supabase/migrations/20260817000001_publish_legal_signup_versions.sql</files>
  <read_first>
    - supabase/migrations/20260731000004_publish_legal_beta_freemium_versions.sql (padrão ON CONFLICT + REVERT)
    - supabase/migrations/ (naming por data — usar 20260817000001)
    - src/lib/legal/document-content.ts (catálogo — garantir que v1.4/v1.3 existem antes de publicar)
  </read_first>
  <action>
    1. Criar `supabase/migrations/20260817000001_publish_legal_signup_versions.sql`:
       - Comentário de cabeçalho explicando a publicação (espelhar 20260731000004).
       - `INSERT INTO public.legal_document_versions (document_type, version, summary, effective_at) VALUES`
         - `('terms_of_service', 'v1.4', '<summary: acesso público gratuito com elegibilidade; cláusula 3.1 sem limitacao a usuarios convidados; autenticacao por terceiros Google OAuth>', now())`
         - `('privacy_policy', 'v1.3', '<summary: fim do beta fechado; captcha Turnstile; confirmacao de email; autenticacao por terceiros com dados exclusivamente autenticacionais>', now())`
       - `ON CONFLICT (document_type, version) DO UPDATE SET summary = EXCLUDED.summary, effective_at = EXCLUDED.effective_at;`
       - NOTA comentada: reaceite antes do go-live → trocar `now()` por data futura na mesma linha antes do deploy.
       - Bloco `-- REVERT` com `DELETE FROM public.legal_document_versions WHERE document_type IN ('terms_of_service','privacy_policy') AND version IN ('v1.4','v1.3');`
    2. Garantir idempotência (ON CONFLICT) e ZERO DDL.
  </action>
  <verify>
    <automated>Select-String -LiteralPath "supabase\migrations\20260817000001_publish_legal_signup_versions.sql" -Pattern "ON CONFLICT|v1.4|v1.3|effective_at" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <acceptance_criteria>
    - Migration contém `INSERT INTO public.legal_document_versions` para `terms_of_service` v1.4 e `privacy_policy` v1.3
    - `ON CONFLICT (document_type, version) DO UPDATE` presente (idempotente, padrão 20260731000004)
    - `effective_at` setado (now() ou futuro configurado); NADA de DDL/DROP/ALTER
    - Bloco `-- REVERT` com DELETE condicionais presente
  </acceptance_criteria>
  <done>Migration de publicação v1.4/v1.3 criada e idempotente</done>
</task>

<task type="execute">
  <name>Task 2: Verificação de paridade config.toml</name>
  <files>supabase/config.toml</files>
  <read_first>
    - supabase/config.toml (enable_signup, enable_confirmations, minimum_password_length, turnstile, Google provider, enable_manual_linking)
  </read_first>
  <action>
    Verificar (sem alterar): `enable_confirmations = true`, `minimum_password_length = 8`, bloco `[auth.captcha]` turnstile, provider Google habilitado, `enable_manual_linking = false`. Registrar no SUMMARY se houver divergência — não editar destrutivamente. Confirmar que `enable_signup` não precisa mudar aqui (controle via flag + Supabase Dashboard no rollout D13).
  </action>
  <verify>
    <automated>Select-String -LiteralPath "supabase\config.toml" -Pattern "enable_confirmations|minimum_password_length|captcha|google|enable_manual_linking" | Select-Object -First 12</automated>
  </verify>
  <acceptance_criteria>
    - Paridade verificada; nenhuma alteração destrutiva feita
    - Divergências (se houver) registradas no SUMMARY
  </acceptance_criteria>
  <done>Paridade config.toml verificada</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration ↔ legal_document_versions | Publicação de versões — idempotência e não-retroatividade |
| dev ↔ schema | Sem DDL em `legal_acceptance` (singular — não existe); `legal_acceptances` intacta |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-12 | Tampering | Migration duplicada | mitigate | `ON CONFLICT (document_type, version) DO UPDATE` (idempotente, padrão 20260731000004) |
| T-42-12a | Availability | Retroatividade destrutiva | mitigate | Sem DDL/DROP; publicação só adiciona versão em `legal_document_versions`; `legal_acceptances` intacta (tolerância técnica D12) |
| T-42-12b | Information Disclosure | Versão vigente errada | mitigate | `getCurrentVersion` usa `effective_at <= now()` (document-versions.ts); v1.4/v1.3 só vigentes quando efetivas |
| T-42-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo |
</threat_model>

<verification>
- Grep migration: `ON CONFLICT` + `v1.4` + `v1.3` + `effective_at` presentes
- Grep anti-DDL: sem `ALTER TABLE`/`CREATE TABLE`/`DROP` na migration nova (apenas legal_document_versions insert)
- `npm run typecheck` → 0 erros (nenhuma mudança de código TS; catálogo já vem do 42-11)
</verification>

<success_criteria>
- Migration idempotente publica Terms v1.4 + Privacy v1.3 em `legal_document_versions` com `effective_at` controlado
- Nenhuma DDL em `legal_acceptance` (singular); `legal_acceptances`/`acceptance-service.ts` intocados
- AUP v1.1 mantida; reaceite via fluxo F30 (`outdated` → `login_reacceptance`) com tolerância técnica
- Paridade config.toml verificada sem alteração destrutiva
</success_criteria>

<output>
Create `.planning/phases/42-signup-controlado-elegibilidade-freemium/42-12-SUMMARY.md` when done
</output>
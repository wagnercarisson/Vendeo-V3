---
phase: 42-signup-controlado-elegibilidade-freemium
plan: 12
type: execute
wave: 3
depends_on: ["42-11", "42-02"]
files_modified:
  - supabase/migrations/xxxx_phase42_legal_effective_at.sql
  - src/lib/legal/acceptance-service.ts
  - src/lib/legal/__tests__/acceptance-service.test.ts
  - supabase/config.toml (verificação)
autonomous: true
requirements: ["legal-acceptance-service", "launch-config (Nova flag publicSignupEnabled)"]

must_haves:
  truths:
    - "Migrations SQL idempotentes: `ADD COLUMN IF NOT EXISTS`; UPDATE com WHERE; sem downgrade destrutivo"
    - "`legal_acceptance` ganha `effective_at` (timestamptz) e `opt_in_communications` (boolean default false) se ainda não existirem"
    - "Backfill idempotente: linhas existentes ganham effective_at (coalesce created_at) e opt_in default false — sem duplicar"
    - "`recordAcceptance` grava `effective_at = now()` e `opt_in_communications`; `getAcceptanceStatus` compara versão >= mínima sem quebrar leitura antiga"
    - "Paridade config.toml: enable_signup, confirmations, turnstile, Google — verificado, sem alteração destrutiva"
  artifacts:
    - path: "supabase/migrations/xxxx_phase42_legal_effective_at.sql"
      provides: "DDL idempotente"
      contains: "ADD COLUMN IF NOT EXISTS effective_at"
  key_links:
    - from: "acceptance-service.ts"
      to: "supabase/migrations"
      via: "effective_at"
      pattern: "ADD COLUMN IF NOT EXISTS"
---

<objective>
Criar a migration SQL idempotente que adiciona `effective_at` e `opt_in_communications` a `legal_acceptance` (D12/D13), com backfill não-destrutivo, e co-migrar o serviço de aceitação para gravar/ler esses campos sem quebrar registros antigos. Verificar a paridade do `supabase/config.toml`.

Purpose: D12 (effective_at) + D13 (paridade config).
Output: migration idempotente + serviço co-migrado.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D12 — effective_at; D13 — paridade config.toml)
@src/lib/legal/acceptance-service.ts (recordAcceptance, getAcceptanceStatus :43-62)
@src/lib/legal/versions.ts (v1.4/v1.3)
@supabase/migrations/ (padrão de naming)
@supabase/config.toml (verificação — 42-02 não altera destrutivo)

<interfaces>
- Migration: `supabase/migrations/<timestamp>_phase42_legal_effective_at.sql` seguindo naming existente.
  - `ALTER TABLE IF EXISTS public.legal_acceptance ADD COLUMN IF NOT EXISTS effective_at timestamptz;`
  - `ALTER TABLE IF EXISTS public.legal_acceptance ADD COLUMN IF NOT EXISTS opt_in_communications boolean NOT NULL DEFAULT false;`
  - Backfill: `UPDATE public.legal_acceptance SET effective_at = COALESCE(effective_at, created_at) WHERE effective_at IS NULL;`
  - NADA de DROP/ALTER destrutivo.
- `recordAcceptance`: gravar `effective_at` (now()) e `opt_in_communications`. Verificar insert atual; adicionar se faltar.
- `getAcceptanceStatus` :43-62: comparar versão persistida contra mínima vigente (v1.4/v1.3). Linhas antigas sem effective_at continuam acknowledged — leitura não quebra.
- `supabase/config.toml`: verificar enable_signup, confirmations, turnstile, Google — sem alteração.
</interfaces>
</context>

<tasks>

<task type="execute">
  <name>Task 1: Migration SQL idempotente</name>
  <files>supabase/migrations/xxxx_phase42_legal_effective_at.sql
  </files>
  <read_first>
    - supabase/migrations/ (padrão de naming — timestamp prefix)
    - migrations que criam legal_acceptance (schema real da tabela)
  </read_first>
  <action>
    1. Criar `supabase/migrations/<timestamp>_phase42_legal_effective_at.sql`:
       - `ALTER TABLE IF EXISTS public.legal_acceptance ADD COLUMN IF NOT EXISTS effective_at timestamptz;`
       - `ALTER TABLE IF EXISTS public.legal_acceptance ADD COLUMN IF NOT EXISTS opt_in_communications boolean NOT NULL DEFAULT false;`
       - Backfill: `UPDATE public.legal_acceptance SET effective_at = COALESCE(effective_at, created_at) WHERE effective_at IS NULL;`
    2. Garantir idempotência (IF NOT EXISTS + WHERE).
  </action>
  <verify>
    <automated>Select-String -LiteralPath "supabase\migrations\*phase42_legal_effective_at.sql" -Pattern "IF NOT EXISTS|effective_at|opt_in_communications" | Measure-Object | Select-Object -ExpandProperty Count
  </verify>
  <acceptance_criteria>
    - Migration contém `ADD COLUMN IF NOT EXISTS` para `effective_at` e `opt_in_communications`
    - Backfill com `WHERE effective_at IS NULL` (repetível, sem duplicar)
    - Sem DROP/ALTER destrutivo
  </acceptance_criteria>
  <done>Migration idempotente criada</done>
</task>

<task type="tdd">
  <name>Task 2: Serviço co-migrado (recordAcceptance/getAcceptanceStatus)</name>
  <files>src/lib/legal/acceptance-service.ts, src/lib/legal/__tests__/acceptance-service.test.ts
  </files>
  <read_first>
    - src/lib/legal/acceptance-service.ts (recordAcceptance, getAcceptanceStatus :43-62)
    - src/lib/legal/__tests__/acceptance-service.test.ts
  </read_first>
  <action>
    1. `recordAcceptance`: incluir `effective_at` (now()) e `opt_in_communications` no insert (se o insert atual é explícito por coluna).
    2. `getAcceptanceStatus`: comparar a versão persistida com a mínima (v1.4/v1.3); linhas antigas (sem effective_at, versão compatível) continuam `acknowledged: true` — sem quebrar.
    3. Tratar coluna ausente com graça: se o schema ainda não tem opt_in (migration não aplicada em dev), não quebrar a chamada (defensive: catch de erro de coluna → default false + log). NOTE: em produção a migration roda antes; defensive é só para dev.
    Red/Green: estender testes: recordAcceptance grava effective_at (timestamp válido) e opt_in; getAcceptanceStatus para v1.4/v1.3 → acknowledged; linha legada (sem effective_at, versão antiga 1.2/1.0) → NOT acknowledged (re-consentimento).
  </action>
  <verify>
    <automated>npx vitest run src/lib/legal/__tests__/acceptance-service.test.ts
  </verify>
  <acceptance_criteria>
    - `recordAcceptance` grava `effective_at` e `opt_in_communications`
    - `getAcceptanceStatus` reconhece v1.4/v1.3 e mantém leitura de linhas legadas sem quebra
    - `npx vitest run src/lib/legal/__tests__/acceptance-service.test.ts` → 0 falhas
  </acceptance_criteria>
  <done>Serviço co-migrado com testes verdes</done>
</task>

<task type="execute">
  <name>Task 3: Verificação de paridade config.toml</name>
  <files>supabase/config.toml
  </files>
  <read_first>
    - supabase/config.toml (enable_signup, enable_confirmations, turnstile, Google)
  </read_first>
  <action>
    Verificar (sem alterar): `enable_signup = true` (ou conforme D13), `enable_confirmations = true`, bloco turnstile (site/secret), provider Google habilitado, `enable_manual_linking = false`. Registrar no SUMMARY se houver divergência — não editar destrutivamente.
  </action>
  <verify>
    <automated>Select-String -LiteralPath "supabase\config.toml" -Pattern "enable_signup|enable_confirmations|turnstile|google|enable_manual_linking" | Select-Object -First 12
  </verify>
  <acceptance_criteria>
    - Paridade verificada; nenhuma alteração destrutiva feita
    - Divergências (se houver) registradas no SUMMARY
  </acceptance_criteria>
  <done>Paridade config.toml verificada</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| serviço legal ↔ banco | effective_at/opt_in persistidos — integridade e idempotência |
| dev ↔ schema | Migration não aplicada em dev pode quebrar leitura |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-12 | Tampering | Migration duplicada | mitigate | IF NOT EXISTS + backfill com WHERE (idempotente) |
| T-42-12a | Availability | Leitura quebra em dev sem migration | mitigate | Defensivo: opt_in ausente → default false + log (produção aplica migration antes) |
| T-42-12b | Information Disclosure | Linhas legadas sem effective_at | mitigate | COALESCE created_at; versão < mínima → re-consentimento (nunca falso acknowledged) |
| T-42-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo |
</threat_model>

<verification>
- `npx vitest run src/lib/legal/__tests__/acceptance-service.test.ts` → 0 falhas
- `npm run typecheck` → 0 erros
- Migration lint: sem DROP/RENAME destrutivo (grep)
</verification>

<success_criteria>
- Migration idempotente (IF NOT EXISTS + backfill repetível) adiciona effective_at e opt_in_communications
- Serviço grava/lê os novos campos sem quebrar linhas legadas; versão < mínima → re-consentimento
- Paridade config.toml verificada sem alteração destrutiva
</success_criteria>

<output>
Create `.planning/phases/42-signup-controlado-elegibilidade-freemium/42-12-SUMMARY.md` when done
</output>
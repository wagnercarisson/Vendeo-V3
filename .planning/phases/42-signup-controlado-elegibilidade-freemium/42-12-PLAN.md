---
phase: 42-signup-controlado-elegibilidade-freemium
plan: 12
type: execute
wave: 6
depends_on: ["42-11"]
files_modified:
  - supabase/migrations/20260817000001_publish_legal_signup_versions.sql
  - supabase/config.toml (verificaÃ§Ã£o)
autonomous: false
requirements: ["legal-acceptance-service", "privacy-acknowledgement", "launch-config (Nova flag publicSignupEnabled)"]

must_haves:
  truths:
    - "**NÃƒO existe tabela `legal_acceptance` (singular)** â€” nenhuma migration DDL nela. Aceites contratuais vivem em `legal_acceptances` (plural, store-level) e NÃƒO sÃ£o alterados na F42"
    - "PublicaÃ§Ã£o de versÃµes = INSERT em `legal_document_versions` (document_type, version, summary, effective_at) â€” tabela JÃ tem `effective_at` (document-versions.ts lÃª com lte(now())); migration idempotente via `ON CONFLICT (document_type, version) DO UPDATE` (padrÃ£o 20260731000004_publish_legal_beta_freemium_versions.sql)"
    - "Publicar Terms **v1.4** (clÃ¡usula 3.1 sem 'usuÃ¡rios convidados'; acesso pÃºblico gratuito com elegibilidade; autenticaÃ§Ã£o por terceiros) e Privacy **v1.3** (sem 'beta, gratuita e fechada'; captcha, confirmaÃ§Ã£o de email, dados do Google â€” finalidade exclusivamente autenticacional). AUP v1.1 mantida (NÃƒO republicar)"
    - "`effective_at` controlado: agora() por padrÃ£o; se configurado futuro (go-live), reaceite ocorre antes via `getAcceptanceStatus` â†’ `outdated` â†’ `login_reacceptance` (fluxo F30 reusado); tolerÃ¢ncia tÃ©cnica: nenhuma loja perde acesso/capacidade ao publicar (clearance fail-closed sÃ³ em funcionalidades protegidas)"
    - "Paridade config.toml: minimum_password_length = 8, enable_confirmations = true, [auth.captcha] turnstile, provider Google, enable_manual_linking = false â€” verificada sem alteraÃ§Ã£o destrutiva"
    - "ConteÃºdos dos documentos (md) jÃ¡ criados no plan 42-11; catÃ¡logo `document-content.ts` jÃ¡ mapeia v1.4/v1.3"
  artifacts:
    - path: "supabase/migrations/20260817000001_publish_legal_signup_versions.sql"
      provides: "PublicaÃ§Ã£o v1.4/v1.3 idempotente"
      contains: "ON CONFLICT"
  key_links:
    - from: "supabase/migrations/20260817000001_publish_legal_signup_versions.sql"
      to: "src/lib/legal/document-versions.ts"
      via: "legal_document_versions"
      pattern: "effective_at"
    - from: "src/lib/legal/document-content.ts"
      to: "supabase/migrations"
      via: "catalog"
      pattern: "terms-of-service-v1-4"
---

<objective>
Publicar as versÃµes legais Terms v1.4 e Privacy v1.3 em `legal_document_versions` via migration idempotente (INSERT ... ON CONFLICT, com `effective_at` controlado â€” JÃ existente na tabela), reutilizando o padrÃ£o da migration `20260731000004_publish_legal_beta_freemium_versions.sql`. **Nenhuma DDL em `legal_acceptance` (singular â€” tabela nÃ£o existe)** e nenhuma mudanÃ§a em `legal_acceptances`/`acceptance-service.ts`. Verificar paridade do `supabase/config.toml` sem alteraÃ§Ã£o destrutiva.

Purpose: D12 (publicaÃ§Ã£o v1.4/v1.3 + reaceite com tolerÃ¢ncia) + D13 (paridade config).
Output: migration de publicacao v1.4/v1.3 + push [BLOCKING] + verificacao config.toml.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D12 â€” v1.4/v1.3 + effective_at + tolerÃ¢ncia; D13 â€” paridade config.toml)
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-11-PLAN.md (conteÃºdos v1.4/v1.3 + catÃ¡logo jÃ¡ criados)
@supabase/migrations/20260731000004_publish_legal_beta_freemium_versions.sql (padrÃ£o ON CONFLICT a reusar)
@src/lib/legal/document-versions.ts (getCurrentVersion lÃª legal_document_versions com effective_at â€” sem mudanÃ§a de schema)
@src/lib/legal/document-content.ts (catÃ¡logo 42-11)
@supabase/migrations/ (padrÃ£o de naming por data)
@supabase/config.toml (paridade D13 â€” verificaÃ§Ã£o apenas)

<interfaces>
- Migration `supabase/migrations/20260817000001_publish_legal_signup_versions.sql`:
  - `INSERT INTO public.legal_document_versions (document_type, version, summary, effective_at) VALUES (...)` para `terms_of_service` v1.4 e `privacy_policy` v1.3, com `summary` descrevendo os ajustes D12 (espelhar estilo das summaries existentes).
  - `ON CONFLICT (document_type, version) DO UPDATE SET summary = EXCLUDED.summary, effective_at = EXCLUDED.effective_at;` (idempotente â€” padrÃ£o 20260731000004).
  - `effective_at = now()` por padrÃ£o; NOTA no arquivo: se o rollout exigir reaceite antes do go-live, configurar `effective_at` futuro (mudar o valor na migration/seed antes do deploy).
  - ComentÃ¡rio REVERT com DELETE condicionais (espelhar padrÃ£o).
  - NADA de DDL/DROP/ALTER.
- `legal_acceptances` (plural): NÃƒO tocada. `acceptance-service.ts`: NÃƒO tocado.
- `supabase/config.toml`: verificar (sem alterar) â€” minimum_password_length = 8, enable_confirmations = true, [auth.captcha] turnstile, provider Google + URLs de callback, enable_manual_linking = false. DivergÃªncias â†’ registrar no SUMMARY (nÃ£o editar destrutivamente).
</interfaces>
</context>

<tasks>

<task type="execute">
  <name>Task 1: Migration de publicaÃ§Ã£o Terms v1.4 / Privacy v1.3</name>
  <files>supabase/migrations/20260817000001_publish_legal_signup_versions.sql</files>
  <read_first>
    - supabase/migrations/20260731000004_publish_legal_beta_freemium_versions.sql (padrÃ£o ON CONFLICT + REVERT)
    - supabase/migrations/ (naming por data â€” usar 20260817000001)
    - src/lib/legal/document-content.ts (catÃ¡logo â€” garantir que v1.4/v1.3 existem antes de publicar)
  </read_first>
  <action>
    1. Criar `supabase/migrations/20260817000001_publish_legal_signup_versions.sql`:
       - ComentÃ¡rio de cabeÃ§alho explicando a publicaÃ§Ã£o (espelhar 20260731000004).
       - `INSERT INTO public.legal_document_versions (document_type, version, summary, effective_at) VALUES`
         - `('terms_of_service', 'v1.4', '<summary: acesso pÃºblico gratuito com elegibilidade; clÃ¡usula 3.1 sem limitacao a usuarios convidados; autenticacao por terceiros Google OAuth>', now())`
         - `('privacy_policy', 'v1.3', '<summary: fim do beta fechado; captcha Turnstile; confirmacao de email; autenticacao por terceiros com dados exclusivamente autenticacionais>', now())`
       - `ON CONFLICT (document_type, version) DO UPDATE SET summary = EXCLUDED.summary, effective_at = EXCLUDED.effective_at;`
       - NOTA comentada: reaceite antes do go-live â†’ trocar `now()` por data futura na mesma linha antes do deploy.
       - Bloco `-- REVERT` com `DELETE FROM public.legal_document_versions WHERE document_type IN ('terms_of_service','privacy_policy') AND version IN ('v1.4','v1.3');`
    2. Garantir idempotÃªncia (ON CONFLICT) e ZERO DDL.
  </action>
  <verify>
    <automated>Select-String -LiteralPath "supabase\migrations\20260817000001_publish_legal_signup_versions.sql" -Pattern "ON CONFLICT|v1.4|v1.3|effective_at" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <acceptance_criteria>
    - Migration contÃ©m `INSERT INTO public.legal_document_versions` para `terms_of_service` v1.4 e `privacy_policy` v1.3
    - `ON CONFLICT (document_type, version) DO UPDATE` presente (idempotente, padrÃ£o 20260731000004)
    - `effective_at` setado (now() ou futuro configurado); NADA de DDL/DROP/ALTER
    - Bloco `-- REVERT` com DELETE condicionais presente
  </acceptance_criteria>
  <done>Migration de publicaÃ§Ã£o v1.4/v1.3 criada e idempotente</done>
</task>

<task type="execute">
  <name>Task 2: VerificaÃ§Ã£o de paridade config.toml</name>
  <files>supabase/config.toml</files>
  <read_first>
    - supabase/config.toml (enable_signup, enable_confirmations, minimum_password_length, turnstile, Google provider, enable_manual_linking)
  </read_first>
  <action>
    Verificar (sem alterar): `enable_confirmations = true`, `minimum_password_length = 8`, bloco `[auth.captcha]` turnstile, provider Google habilitado, `enable_manual_linking = false`. Registrar no SUMMARY se houver divergÃªncia â€” nÃ£o editar destrutivamente. Confirmar que `enable_signup` nÃ£o precisa mudar aqui (controle via flag + Supabase Dashboard no rollout D13).
  </action>
  <verify>
    <automated>Select-String -LiteralPath "supabase\config.toml" -Pattern "enable_confirmations|minimum_password_length|captcha|google|enable_manual_linking" | Select-Object -First 12</automated>
  </verify>
<acceptance_criteria>
    - Paridade verificada; nenhuma alteração destrutiva feita
    - Divergências (se houver) registradas no SUMMARY
  </acceptance_criteria>
  <done>Paridade config.toml verificada</done>
</task>

<task type="execute">
  <name>Task 3: [BLOCKING] Schema push — supabase db push da migration de publicação</name>
  <files>supabase/migrations/20260817000001_publish_legal_signup_versions.sql</files>
  <read_first>
    - .planning/phases/38.2-admin-custos-operacionais/38-2-01-SUMMARY.md (precedente [BLOCKING] do push — comandos e validação; Docker local indisponível; pooler do projeto linkado)
    - .planning/phases/38-credit-operation-costs/38-01-SUMMARY.md (push remoto com `--linked --yes`)
  </read_first>
  <action>
    Aplicar a migration no banco remoto — padrão 38-01/38-2-01 [BLOCKING]:
    1. Se `SUPABASE_ACCESS_TOKEN` não estiver no ambiente, procurar em `.env.local`/secrets; se ausente, pedir ao usuário o token (Supabase Dashboard → Account → Access Tokens) via checkpoint — a tarefa é [BLOCKING] e o plano é `autonomous: false`.
    2. `npx supabase db push --dry-run` primeiro — confirmar que APENAS `20260817000001_publish_legal_signup_versions` seria aplicada (sem outras migrations pendentes surpresa).
    3. Rodar `npx supabase db push` na raiz do repo (padrão 38-2-01: pooler URL do projeto linkado; Docker local indisponível). Se o push pedir confirmação interativa que não pode ser suprimida, parar e sinalizar intervenção manual.
    4. Validar: `supabase migration list` mostra `20260817000001_publish_legal_signup_versions` aplicada no remoto; `supabase db push` exit 0.
  </action>
  <verify>
    <automated>npx supabase db push --dry-run 2>&1 | Select-String -Pattern "No new migrations"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` aplicou a migration no remoto sem erro (exit 0)
    - `supabase db push --dry-run` subsequente reporta "No new migrations" (idempotência de registro)
    - `supabase migration list` confirma `20260817000001_publish_legal_signup_versions` no remoto
  </acceptance_criteria>
  <done>Migration de publicação v1.4/v1.3 aplicada no remoto ([BLOCKING])</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration â†” legal_document_versions | PublicaÃ§Ã£o de versÃµes â€” idempotÃªncia e nÃ£o-retroatividade |
| dev â†” schema | Sem DDL em `legal_acceptance` (singular â€” nÃ£o existe); `legal_acceptances` intacta |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-12 | Tampering | Migration duplicada | mitigate | `ON CONFLICT (document_type, version) DO UPDATE` (idempotente, padrÃ£o 20260731000004) |
| T-42-12a | Availability | Retroatividade destrutiva | mitigate | Sem DDL/DROP; publicaÃ§Ã£o sÃ³ adiciona versÃ£o em `legal_document_versions`; `legal_acceptances` intacta (tolerÃ¢ncia tÃ©cnica D12) |
| T-42-12b | Information Disclosure | VersÃ£o vigente errada | mitigate | `getCurrentVersion` usa `effective_at <= now()` (document-versions.ts); v1.4/v1.3 sÃ³ vigentes quando efetivas |
| T-42-SC | Tampering | InstalaÃ§Ãµes de pacotes | accept | Nenhum pacote novo |
</threat_model>

<verification>
- Grep migration: `ON CONFLICT` + `v1.4` + `v1.3` + `effective_at` presentes
- Grep anti-DDL: sem `ALTER TABLE`/`CREATE TABLE`/`DROP` na migration nova (apenas legal_document_versions insert)
- `npm run typecheck` â†’ 0 erros (nenhuma mudanÃ§a de cÃ³digo TS; catÃ¡logo jÃ¡ vem do 42-11)
</verification>

<success_criteria>
- Migration idempotente publica Terms v1.4 + Privacy v1.3 em `legal_document_versions` com `effective_at` controlado
- Nenhuma DDL em `legal_acceptance` (singular); `legal_acceptances`/`acceptance-service.ts` intocados
- AUP v1.1 mantida; reaceite via fluxo F30 (`outdated` â†’ `login_reacceptance`) com tolerÃ¢ncia tÃ©cnica
- Paridade config.toml verificada sem alteraÃ§Ã£o destrutiva
</success_criteria>

<output>
Create `.planning/phases/42-signup-controlado-elegibilidade-freemium/42-12-SUMMARY.md` when done
</output>

---
phase: 42-signup-controlado-elegibilidade-freemium
plan: 13
type: execute
wave: 4
depends_on: ["42-06", "42-07", "42-08", "42-10"]
files_modified:
  - src/__tests__/auth/signup-page.test.tsx
  - src/components/auth/__tests__/signup-form.test.tsx
  - src/components/auth/__tests__/captcha-field.test.tsx
  - src/components/auth/__tests__/google-button.test.tsx
  - src/__tests__/landing/access-request-section.test.tsx
  - src/lib/launch-config/__tests__/config.test.ts
  - src/app/(auth)/login/__tests__/login-page.test.tsx
autonomous: true
requirements: ["signup-page", "google-oauth-signup", "login-page", "access-request-history", "turnstile-captcha", "launch-config"]

must_haves:
  truths:
    - "Testes 1–13 mapeados de tasks.md §13 (13 testes, D2-D5/D12/D15)"
    - "Teste 13: UI — flag off → 'Continuar com Google' CONTINUA visível no /login (D5)"
    - "Teste 8: token inválido/expirado → erro tratado pelo Supabase (fluxo integrado, mock do client)"
  artifacts:
    - path: "src/components/auth/__tests__/signup-form.test.tsx"
      provides: "Testes 2–8"
      contains: "anti-enumeração"
    - path: "src/app/(auth)/login/__tests__/login-page.test.tsx"
      provides: "Teste 13"
      contains: "flag off"
---

<objective>
Implementar os Testes 1–13 (tasks.md §13) para signup/flag/landing/OAuth UI: flag on/off, validações de senha, ciência da Privacidade, `signUp` com `emailRedirectTo`+`captchaToken`, anti-enumeração (mesma resposta + erro genérico), captcha ausente/inválido, Google sem captcha, landing CTA, env flag default, `approved` histórico, e o Teste 13 (Google sempre visível no login com flag off).

Purpose: cobertura D2–D5/D12/D15 da camada UI.
Output: 13 testes verdes.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D2/D3/D4/D5/D12/D15)
@openspec/changes/fase-42-signup-controlado-elegibilidade-freemium/tasks.md (§13, testes 1–13)
@src/components/auth/__tests__/signup-form.test.tsx (42-06 base)
@src/components/auth/__tests__/captcha-field.test.tsx (42-08 base)
@src/components/auth/__tests__/google-button.test.tsx (42-07 base)
@src/__tests__/auth/signup-page.test.tsx (42-06 base)
@src/lib/launch-config/__tests__/config.test.ts (padrão env)
@src/app/(auth)/login/__tests__/login-page.test.tsx (42-09 base)

<interfaces>
- Cada teste mapeia a 1 checkbox de tasks.md §13 (13.1→Teste 1 ... 13.13→Teste 13).
- Testes 1, 2, 3, 4, 5, 6, 7, 9, 13 → `signup-form.test.tsx`, `signup-page.test.tsx`, `login-page.test.tsx` (mock de `@/lib/supabase/client`, `next/navigation`, `getSiteUrl`, `getLaunchConfig`).
- Teste 8 → mock do client Supabase retornando erro de token inválido/expirado; asserta mensagem genérica (não revela conta).
- Teste 10 → `access-request-section.test.tsx` (flag on/off CTAs).
- Teste 11 → `config.test.ts`: `VENDEO_PUBLIC_SIGNUP_ENABLED` ausente → false; `"true"` → true; `"1"` → true (envBool).
- Teste 12 → serviço de access_requests: `approved` prévio não bloqueia novo signup (histórico, não autorização).
- Teste 13 → `login-page.test.tsx`: mock getLaunchConfig flag off; GoogleButton presente; SEM link "Criar uma conta".
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Testes 1–9 (signup form + page + captcha + google)</name>
  <files>src/components/auth/__tests__/signup-form.test.tsx, src/__tests__/auth/signup-page.test.tsx, src/components/auth/__tests__/captcha-field.test.tsx, src/components/auth/__tests__/google-button.test.tsx
  </files>
  <read_first>
    - openspec/changes/fase-42-signup-controlado-elegibilidade-freemium/tasks.md (§13, 13.1–13.9)
    - src/components/auth/__tests__/signup-form.test.tsx (42-06 base)
    - src/__tests__/auth/signup-page.test.tsx (42-06 base)
    - src/components/auth/__tests__/captcha-field.test.tsx (42-08 base)
    - src/components/auth/__tests__/google-button.test.tsx (42-07 base)
  </read_first>
  <action>
    Estender os testes existentes para cobrir exatamente os Testes 1–9:
    - Teste 1 (signup-page): flag on → formulário + "Continuar com Google"; flag off → "Beta fechado"/landing.
    - Teste 2 (signup-form): senha < 8 → erro; senha ≠ confirmação → erro (mensagens PT-BR).
    - Teste 3 (signup-form): ciência da Privacidade obrigatória (modal não confirmado → submit bloqueado); consentimento de comunicações opcional.
    - Teste 4 (signup-form): `signUp` chamado com `options.emailRedirectTo` (terminando `/auth/confirm`) e `options.captchaToken`.
    - Teste 5 (signup-form): sucesso E erro "already registered" → mesma resposta `router.replace("/check-email?type=signup")`.
    - Teste 6 (signup-form): erro operacional/captcha/indisponibilidade → mensagem genérica "Não foi possível concluir. Tente novamente.".
    - Teste 7 (signup-form): `captchaToken` ausente → submit bloqueado (sem chamada de `signUp`).
    - Teste 8 (signup-form): token inválido/expirado (mock client retorna erro) → mensagem genérica (fluxo integrado pelo Supabase).
    - Teste 9 (google-button): `signInWithOAuth({ provider: "google", options: { redirectTo: .../auth/callback } })`; NÃO inclui `captchaToken`.
    Nomear cada `test(...)` com referência explícita ("Teste 1: ...", ..., "Teste 9: ...") para rastreabilidade com tasks.md.
  </action>
  <verify>
    <automated>npx vitest run src/components/auth/__tests__/signup-form.test.tsx src/components/auth/__tests__/captcha-field.test.tsx src/components/auth/__tests__/google-button.test.tsx src/__tests__/auth/signup-page.test.tsx
  </verify>
  <acceptance_criteria>
    - Testes 1–9 implementados e nomeados conforme tasks.md
    - `signUp` sem captchaToken não é chamado (Teste 7); erro de token → genérico (Teste 8)
    - `npx vitest run` nos 4 arquivos → 0 falhas
  </acceptance_criteria>
  <done>Testes 1–9 verdes</done>
</task>

<task type="tdd">
  <name>Task 2: Testes 10–13 (landing + env flag + histórico + login flag off)</name>
  <files>src/__tests__/landing/access-request-section.test.tsx, src/lib/launch-config/__tests__/config.test.ts, src/app/(auth)/login/__tests__/login-page.test.tsx
  </files>
  <read_first>
    - openspec/changes/fase-42-signup-controlado-elegibilidade-freemium/tasks.md (§13, 13.10–13.13)
    - src/__tests__/landing/access-request-section.test.tsx (42-10 base)
    - src/lib/launch-config/__tests__/config.test.ts (padrão envBool)
    - src/app/(auth)/login/__tests__/login-page.test.tsx (42-09 base)
  </read_first>
  <action>
    Implementar:
    - Teste 10 (landing): flag on → "Continuar com Google" (ou "Criar conta grátis") + "Continuar com email"/`/signup`; flag off → "Solicitar acesso free" + formulário.
    - Teste 11 (config): `VENDEO_PUBLIC_SIGNUP_ENABLED` ausente → `publicSignupEnabled === false`; `"true"`/`"1"` → true; parse correto.
    - Teste 12 (access-requests): registro `approved` prévio do mesmo email NÃO bloqueia/afeta novo signup (histórico, não autorização) — testar no serviço/rota de access_requests.
    - Teste 13 (login-page): flag off → "Continuar com Google" CONTINUA visível; SEM link "Criar uma conta".
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/landing/access-request-section.test.tsx src/lib/launch-config/__tests__/config.test.ts src/app/(auth)/login/__tests__/login-page.test.tsx
  </verify>
  <acceptance_criteria>
    - Testes 10–13 implementados e nomeados conforme tasks.md
    - Teste 13 demonstra Google visível com flag off (D5)
    - `npx vitest run` nos 3 arquivos → 0 falhas
  </acceptance_criteria>
  <done>Testes 10–13 verdes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| testes ↔ mocks | Mocks de Supabase/auth — não exercitam segurança real |
| UI ↔ enumeração | Asserts garantem comportamento anti-enumeração |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-13 | Spoofing | Testes com mock muito permissivo | mitigate | Testes 17–21 (42-14) usam Supabase real/UAT — camada integrada |
| T-42-13a | Information Disclosure | Testes revelam conta | mitigate | Testes 5/6/8 assertam a MESMA resposta (anti-enumeração) |
| T-42-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo |
</threat_model>

<verification>
- `npx vitest run` nos arquivos dos testes 1–13 → 0 falhas
- `npm run typecheck` → 0 erros
- Cada teste nomeado "Teste N" rastreável a tasks.md §13
</verification>

<success_criteria>
- Testes 1–13 implementados e verdes, mapeados 1:1 a tasks.md §13
- Cobertura: flag on/off, validações, ciência, signUp options, anti-enumeração, captcha ausente/inválido, Google sem captcha, landing, env default, histórico approved, login flag off
</success_criteria>

<output>
Create `.planning/phases/42-signup-controlado-elegibilidade-freemium/42-13-SUMMARY.md` when done
</output>
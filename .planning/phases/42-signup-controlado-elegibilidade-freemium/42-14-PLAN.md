---
phase: 42-signup-controlado-elegibilidade-freemium
plan: 14
type: execute
wave: 4
depends_on: ["42-07"]
files_modified:
  - src/app/auth/callback/__tests__/route.test.ts
  - src/__tests__/integration/oauth-identity-linking.test.ts
  - docs/uat/42-oauth-identity-linking.md
autonomous: true
requirements: ["oauth-auth-callback", "privacy-acknowledgement", "launch-config"]

must_haves:
  truths:
    - "Testes 14–16 (rota/callback) são Vitest com mocks — tasks.md §14"
    - "Testes 17–21 (identity linking + enable_signup=false) são integrados/UAT com Supabase real — tasks.md §14"
    - "Teste 19: Google novo → novo usuário; SEM crédito (invariante D6)"
    - "Teste 20: Google existente → login mesmo com 'Allow new users to sign up' off"
    - "Teste 21: cancelar consentimento do Google → volta para login sem sessão criada"
  artifacts:
    - path: "src/app/auth/callback/__tests__/route.test.ts"
      provides: "Testes 14–16"
      contains: "exchangeCodeForSession"
    - path: "docs/uat/42-oauth-identity-linking.md"
      provides: "Roteiro UAT 17–21"
      contains: "identity linking"
---

<objective>
Implementar os Testes 14–21 (tasks.md §14) para callback OAuth e identity linking: 14–16 em Vitest com mocks (code válido/inválido, next externo bloqueado) e 17–21 como roteiro integrado/UAT com Supabase real (identity linking email+Google, sem crédito, enable_signup off, cancelamento de consentimento).

Purpose: cobertura D16/D15/D6/D5 do fluxo OAuth.
Output: testes 14–16 + roteiro UAT 17–21.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D16 — callback/identity linking; D6 — sem crédito)
@openspec/changes/fase-42-signup-controlado-elegibilidade-freemium/tasks.md (§14, testes 14–21; classificação Vitest vs UAT)
@src/app/auth/callback/route.ts (42-07)
@src/app/auth/callback/__tests__/route.test.ts (42-07 base)

<interfaces>
- Testes 14–16: Vitest com mocks de `createServerClient`/`exchangeCodeForSession` (42-07). Renomear os testes existentes para "Teste 14/15/16" e completar se faltar.
- Testes 17–21: **UAT integrado** (Supabase real, chaves locais). Roteiro em `docs/uat/42-oauth-identity-linking.md` com passos manuais e asserções:
  - 17: conta email confirmada + Google mesmo email → mesmo usuário (sem duplicar users/lojas/acknowledgments).
  - 18: conta email não confirmada + Google → comportamento do Supabase validado (sem duplicação).
  - 19: Google novo → novo usuário; verificar `freemium_entitlements` SEM crédito (D6).
  - 20: com "Allow new users to sign up" off → login de existente via Google ok.
  - 21: cancelar consentimento → volta ao login, nenhuma sessão criada.
- Invariante D6: conferir via SQL que novo usuário Google NÃO recebe concessão de crédito.
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Testes 14–16 (callback route — Vitest)</name>
  <files>src/app/auth/callback/__tests__/route.test.ts
  </files>
  <read_first>
    - openspec/changes/fase-42-signup-controlado-elegibilidade-freemium/tasks.md (§14, 14.1–14.3)
    - src/app/auth/callback/route.ts (42-07)
    - src/app/auth/callback/__tests__/route.test.ts (42-07 base)
  </read_first>
  <action>
    Renomear/estender os testes existentes para "Teste 14", "Teste 15", "Teste 16":
    - Teste 14: `code` válido → `exchangeCodeForSession` chamado → redirect `/loja` → PrivacyGate (fluxo).
    - Teste 15: `code` inválido/expirado (mock retorna erro) → redirect `/login?error=oauth_failed` (genérico).
    - Teste 16: `next` externo (ex. `https://evil.com`) → bloqueado → fallback `/loja`; `next=/onboarding` → fallback.
  </action>
  <verify>
    <automated>npx vitest run src/app/auth/callback/__tests__/route.test.ts
  </verify>
  <acceptance_criteria>
    - Testes 14–16 nomeados conforme tasks.md §14
    - Teste 16 demonstra bloqueio de redirect externo (allowlist)
    - `npx vitest run src/app/auth/callback/__tests__/route.test.ts` → 0 falhas
  </acceptance_criteria>
  <done>Testes 14–16 verdes</done>
</task>

<task type="execute">
  <name>Task 2: Roteiro UAT Testes 17–21 (identity linking)</name>
  <files>docs/uat/42-oauth-identity-linking.md
  </files>
  <read_first>
    - openspec/changes/fase-42-signup-controlado-elegibilidade-freemium/tasks.md (§14, 14.4–14.8)
    - .planning/phases/41-midia-de-campanha-mobile/41-UAT.md (padrão de roteiro UAT da fase anterior)
  </read_first>
  <action>
    Criar `docs/uat/42-oauth-identity-linking.md` com roteiro manual/integrado para Testes 17–21:
    - Pré-requisitos: Supabase local com `enable_signup = true` e Google provider configurado; chaves de teste.
    - 17: criar conta email/senha, confirmar email, logar, depois Google mesmo email → validar MESMO usuário (id) e que `public.users`/lojas/acknowledgments não duplicaram (query SQL).
    - 18: conta email/senha SEM confirmação + Google → validar comportamento Supabase (sem duplicação).
    - 19: Google novo → novo usuário; SQL: `freemium_entitlements` vazio (sem crédito — D6).
    - 20: `enable_signup = false` no dashboard → Google existente loga; novo email bloqueado.
    - 21: cancelar consentimento Google (voltar da tela do Google) → sessão não criada; redirect para login.
    - Para cada teste: passos, comando SQL de verificação, resultado esperado.
  </action>
  <verify>
    <automated>Select-String -LiteralPath "docs\uat\42-oauth-identity-linking.md" -Pattern "Teste 17|Teste 18|Teste 19|Teste 20|Teste 21|D6" | Measure-Object | Select-Object -ExpandProperty Count
  </verify>
  <acceptance_criteria>
    - `docs/uat/42-oauth-identity-linking.md` contém passos + asserções SQL para Testes 17–21
    - Teste 19 valida SEM crédito (D6) via `freemium_entitlements`
    - Teste 20 cobre `enable_signup = false` + Google existente
  </acceptance_criteria>
  <done>Roteiro UAT 17–21 criado</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teste mock ↔ rota real | Testes 14–16 não exercitam Supabase real — cobertura lógica de redirect |
| UAT ↔ Supabase real | Testes 17–21 exercitam identidade e concessão — validação de segurança verdadeira |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-14 | Spoofing | Code inválido | mitigate | Teste 15: erro genérico (não enumera) |
| T-42-14a | Tampering | Open redirect | mitigate | Teste 16: allowlist bloqueia externo |
| T-42-14b | Elevation of Privilege | Duplicação de identidade | mitigate | Testes 17/18 UAT: mesmo usuário sem duplicação |
| T-42-14c | Elevation of Privilege | Crédito indevido | mitigate | Teste 19 UAT: `freemium_entitlements` vazio (D6) |
| T-42-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo |
</threat_model>

<verification>
- `npx vitest run src/app/auth/callback/__tests__/route.test.ts` → 0 falhas (Testes 14–16)
- Roteiro UAT 17–21 presente em `docs/uat/`
- `npm run typecheck` → 0 erros
</verification>

<success_criteria>
- Testes 14–16 verdes (Vitest); Testes 17–21 documentados como UAT integrado com asserções SQL
- Identity linking (mesmo usuário), sem crédito (D6), enable_signup off, cancelamento — todos cobertos
</success_criteria>

<output>
Create `.planning/phases/42-signup-controlado-elegibilidade-freemium/42-14-SUMMARY.md` when done
</output>
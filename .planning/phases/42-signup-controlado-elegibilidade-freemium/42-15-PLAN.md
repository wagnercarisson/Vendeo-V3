---
phase: 42-signup-controlado-elegibilidade-freemium
plan: 15
type: execute
wave: 4
depends_on: ["42-04"]
files_modified:
  - src/lib/freemium/__tests__/freemium-risk-service.test.ts
  - src/__tests__/integration/freemium-invariants.test.ts
autonomous: true
requirements: ["freemium-risk-service", "freemium types"]

must_haves:
  truths:
    - "Testes 22–36 mapeados de tasks.md §15 (15 testes, D6/D7/D8/D9/D10)"
    - "Teste 26 (pré-gate D7) é sobre o CALLER/rota, não o motor — validar que não chama evaluateFreemiumEligibility com city/state ausentes"
    - "Testes 34–36 (invariante D6) são integrados: signup email/senha e Google NUNCA concedem crédito; raiz única e aprovação auditada"
  artifacts:
    - path: "src/lib/freemium/__tests__/freemium-risk-service.test.ts"
      provides: "Testes 22–33"
      contains: "situacao_nao_ativa"
    - path: "src/__tests__/integration/freemium-invariants.test.ts"
      provides: "Testes 34–36"
      contains: "NUNCA concedem crédito"
---

<objective>
Implementar os Testes 22–36 (tasks.md §15) do motor de elegibilidade: 22–33 em Vitest (situações, pré-gate D7 no caller, localização, CNAE, ordem, defer) e 34–36 como testes integrados do invariante D6 (signup email/senha e Google nunca concedem crédito; raiz única; aprovação idempotente/auditada).

Purpose: cobertura D6/D7/D8/D9/D10 do motor.
Output: testes 22–36 verdes.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D6 — invariante crédito; D7 — pré-gate; D8 — situações; D9 — CNAE; D10 — ordem)
@openspec/changes/fase-42-signup-controlado-elegibilidade-freemium/tasks.md (§15, testes 22–36)
@src/lib/freemium/__tests__/freemium-risk-service.test.ts (42-04 base — makeInput factory)
@src/lib/freemium/freemium-risk-service.ts (motor 42-04)
@src/lib/cnpj/cnae-mapping.ts (42-03)

<interfaces>
- Testes 22–33: unit tests no motor via `makeInput` (padrão existente). Cada teste mapeia 1 checkbox de §15.
- Teste 26 (pré-gate D7): testar a ROÇA/caller (ex. rota de loja) — assertar que com `city`/`state` ausentes a rota NÃO chama `evaluateFreemiumEligibility`; loja permanece draft sem review/sem concessão. Pode exigir mock do serviço.
- Testes 34–36: integrados (Supabase real em UAT OU teste de integração com mocks de alto nível + SQL). Roteiro UAT complementar ou teste Vitest com repositório simulado:
  - 34: fluxo email/senha completo NUNCA concede crédito.
  - 35: fluxo Google NUNCA concede crédito.
  - 36: raiz única — segunda loja mesma raiz → não aprova segunda; aprovação idempotente/auditada (admin_exception auditável).
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Testes 22–33 (motor — unit)</name>
  <files>src/lib/freemium/__tests__/freemium-risk-service.test.ts
  </files>
  <read_first>
    - openspec/changes/fase-42-signup-controlado-elegibilidade-freemium/tasks.md (§15, 15.1–15.12)
    - src/lib/freemium/__tests__/freemium-risk-service.test.ts (42-04 base)
    - src/lib/freemium/freemium-risk-service.ts (motor 42-04)
    - src/lib/cnpj/cnae-mapping.ts (42-03)
  </read_first>
  <action>
    Estender `freemium-risk-service.test.ts` (ou arquivo dedicado por grupo) com Testes 22–33:
    - 22: `INAPTA` → review `situacao_nao_ativa` (lacuna F33).
    - 23: `SUSPENSA` → review `situacao_nao_ativa` (substitui `situacao_suspensa` no motor).
    - 24: `BAIXADA` → reject `cnpj_baixada`; `NULA` → reject `cnpj_nula`.
    - 25: situação não-vazia ≠ ATIVA/BAIXADA/NULA → review `situacao_nao_ativa`; ausente/inválida em resposta resolvida → defer `dados_oficiais_incompletos`.
    - 26: pré-gate D7 no caller — ver Task 2.
    - 27: cidade/UF preenchidas, oficiais ausentes → review `localizacao_oficial_indisponivel`.
    - 28: cidade/UF divergentes → review `cidade_divergente`/`uf_divergente`.
    - 29: CNAE compatible → sem revisão por CNAE (segue approved).
    - 30: CNAE incompatible → review `segmento_cnae_divergente` (nunca reject).
    - 31: CNAE unknown (ausente/inválido/fora listas) → neutro.
    - 32: ordem — situação não ATIVA avaliada antes de raiz/nome/cidade/UF (regra D10).
    - 33: `api_unavailable`/sem dados → defer `api_unavailable`.
    Nomear "Teste N" conforme §15 para rastreabilidade.
  </action>
  <verify>
    <automated>npx vitest run src/lib/freemium/__tests__/freemium-risk-service.test.ts
  </verify>
  <acceptance_criteria>
    - Testes 22–33 implementados e nomeados conforme tasks.md §15
    - Cobre: situações (review/reject/defer), CNAE tri-state, ordem D10, api_unavailable
    - `npx vitest run src/lib/freemium/__tests__/freemium-risk-service.test.ts` → 0 falhas
  </acceptance_criteria>
  <done>Testes 22–33 verdes</done>
</task>

<task type="tdd">
  <name>Task 2: Teste 26 (pré-gate D7 no caller) + 34–36 (invariante D6)</name>
  <files>src/lib/freemium/__tests__/freemium-risk-service.test.ts, src/__tests__/integration/freemium-invariants.test.ts
  </files>
  <read_first>
    - openspec/changes/fase-42-signup-controlado-elegibilidade-freemium/tasks.md (§15, 15.5, 15.13–15.15)
    - src/lib/freemium/freemium-risk-service.ts (motor 42-04 — onde o caller/rota usa)
    - Rotas que chamam `evaluateFreemiumEligibility` (rota de loja/CNPJ)
    - src/lib/freemium/types.ts (city/state nullable — D7)
  </read_first>
  <action>
    - Teste 26 (caller/rota): identificar a rota/handler que faz o pré-gate D7; com `city`/`state` ausentes, assertar que `evaluateFreemiumEligibility` NÃO é chamado (mock do serviço com `expect(service).not.toHaveBeenCalled()`), a loja permanece draft, SEM review na fila, SEM concessão.
    - Testes 34–36 (integrados): criar `src/__tests__/integration/freemium-invariants.test.ts` ou roteiro UAT:
      - 34: fluxo email/senha (signup→confirm→onboarding→CNPJ) NUNCA concede crédito.
      - 35: fluxo Google NUNCA concede crédito.
      - 36: raiz única — segunda loja mesma `cnpj_root_hash` não aprova; aprovação idempotente (re-executar concede/recusa igual) e auditável (`admin_exception`).
    Se inviável como teste automático com Supabase real, documentar como UAT com comandos SQL (seguir padrão 42-14 Task 2).
  </action>
  <verify>
    <automated>npx vitest run src/lib/freemium/__tests__/freemium-risk-service.test.ts src/__tests__/integration/freemium-invariants.test.ts --passWithNoTests
  </verify>
  <acceptance_criteria>
    - Teste 26 asserta pré-gate D7 (motor não chamado com city/state ausentes)
    - Testes 34–36 cobrem invariante D6 (email/senha, Google, raiz única/auditável) — como teste ou roteiro UAT SQL
    - Suíte verde
  </acceptance_criteria>
  <done>Teste 26 + 34–36 implementados</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| testes unit ↔ motor | Cobertura determinística de decisões (sem Supabase) |
| testes integrados ↔ concessão | Invariante D6 validado no fluxo real |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-15 | Elevation of Privilege | Crédito concedido indevidamente | mitigate | Testes 34/35: signup email/senha e Google nunca concedem (D6) |
| T-42-15a | Elevation of Privilege | Duplicidade de raiz | mitigate | Teste 36: raiz única, aprovação idempotente/auditada |
| T-42-15b | Spoofing | Pré-gate bypass | mitigate | Teste 26: motor não chamado com dados incompletos |
| T-42-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo |
</threat_model>

<verification>
- `npx vitest run src/lib/freemium/__tests__/freemium-risk-service.test.ts` → 0 falhas
- Testes 26/34–36 presentes (unit ou roteiro UAT SQL)
- `npm run typecheck` → 0 erros
</verification>

<success_criteria>
- Testes 22–36 cobrindo D6/D7/D8/D9/D10, nomeados conforme tasks.md §15
- Situações (ATIVA/BAIXADA/NULA/INAPTA/SUSPENSA/ausente), CNAE tri-state, ordem D10, api_unavailable, pré-gate D7, invariante D6
</success_criteria>

<output>
Create `.planning/phases/42-signup-controlado-elegibilidade-freemium/42-15-SUMMARY.md` when done
</output>
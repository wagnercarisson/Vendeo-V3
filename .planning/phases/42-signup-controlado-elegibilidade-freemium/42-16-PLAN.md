---
phase: 42-signup-controlado-elegibilidade-freemium
plan: 16
type: execute
wave: 4
depends_on: ["42-03"]
files_modified:
  - src/lib/cnpj/__tests__/cnae-mapping.test.ts
autonomous: true
requirements: ["cnae-segment-mapping"]

must_haves:
  truths:
    - "Testes 37–46 mapeados de tasks.md §16 (10 testes, D9)"
    - "Teste 41 (granularidade exata) e Teste 42 (precedência subclasse fina) — regras de granularidade do mapeamento"
    - "Teste 43: não-contradição → erro de BUILD/CI (não runtime) — assertar que o script de validação falha"
    - "Teste 46: ordem `negative.subclasses → positive.subclasses → negative.classes → positive.classes → unknown`"
  artifacts:
    - path: "src/lib/cnpj/__tests__/cnae-mapping.test.ts"
      provides: "Testes 37–46"
      contains: "precedência"
---

<objective>
Implementar os Testes 37–46 (tasks.md §16) do mapeamento CNAE determinístico: normalização, derivação de classe, listas positiva/negativa, granularidade exata, precedência de subclasse fina, não-contradição em CI e unknown neutro.

Purpose: cobertura D9 (módulo CNAE determinístico).
Output: testes 37–46 verdes.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D9 — CNAE)
@openspec/changes/fase-42-signup-controlado-elegibilidade-freemium/tasks.md (§16, testes 37–46)
@src/lib/cnpj/cnae-mapping.ts (42-03)
@src/lib/cnpj/__tests__/cnae-mapping.test.ts (42-03 base, se existir)

<interfaces>
- `normalizeCnaeSubclasse`, `deriveCnaeClasse`, `cnaeCompatibilityFor`, `CNAE_COMPATIBLE`/`CNAE_INCOMPATIBLE`, validação de não-contradição.
- Testes 37–46 como unit tests. Teste 43 (não-contradição) executa o validador de build/CI e asserta falha quando há código nas duas listas (mesmo segmento).
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Testes 37–46 (mapeamento CNAE)</name>
  <files>src/lib/cnpj/__tests__/cnae-mapping.test.ts
  </files>
  <read_first>
    - openspec/changes/fase-42-signup-controlado-elegibilidade-freemium/tasks.md (§16, 16.1–16.10)
    - src/lib/cnpj/cnae-mapping.ts (42-03 — normalização, conjuntos, precedência, validador de não-contradição)
    - src/lib/cnpj/__tests__/cnae-mapping.test.ts (42-03 base, se existir)
  </read_first>
  <action>
    Implementar Testes 37–46:
    - 37: `"4781-4/00"` → `normalizeCnaeSubclasse` → `"4781400"` (7 dígitos + DV).
    - 38: `"4781400"` → `deriveCnaeClasse` → `"47814"` (4 dígitos + DV); classe e subclasse representadas separadamente.
    - 39: CNAE na lista positiva → compatible.
    - 40: CNAE na lista negativa explícita → incompatible.
    - 41: granularidade exata — `"4781400"` em `incompatible.subclasses` NÃO torna outras subclasses de `"47814"` incompatíveis (classe `"47814"` precisa ser listada para cobrir a inteira).
    - 42: precedência subclasse fina — classe `"47814"` positiva + subclasse `"4781400"` negativa → `"4781400"` incompatible (exceção fina vence); demais subclasses seguem a classe.
    - 43: não-contradição — validador de CI falha se mesmo código (string idêntica) está nas listas positiva e negativa do mesmo segmento (classe OU subclasse); assertar que o build/CI sinaliza erro.
    - 44: CNAE fora de ambas as listas → unknown; nulo/sem 7 dígitos → unknown.
    - 45: segmento sem listas (`outros`) → unknown, sem penalizar.
    - 46: ordem de match — `negative.subclasses → positive.subclasses → negative.classes → positive.classes → unknown`.
    Nomear "Teste N" conforme §16.
  </action>
  <verify>
    <automated>npx vitest run src/lib/cnpj/__tests__/cnae-mapping.test.ts
  </verify>
  <acceptance_criteria>
    - Testes 37–46 implementados e nomeados conforme tasks.md §16
    - Cobre normalização, derivação, positive/negative, granularidade (41), precedência (42), não-contradição CI (43), unknown (44/45), ordem (46)
    - `npx vitest run src/lib/cnpj/__tests__/cnae-mapping.test.ts` → 0 falhas
  </acceptance_criteria>
  <done>Testes 37–46 verdes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validador CI ↔ listas | Não-contradição é uma invariante de build (impede mapeamento ambíguo) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-16 | Tampering | Contradição positiva/negativa | mitigate | Teste 43: validador de CI falha em build |
| T-42-16a | Spoofing | Granularidade frouxa | mitigate | Testes 41/42: subclasse fina vence; classe listada cobre a inteira |
| T-42-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo |
</threat_model>

<verification>
- `npx vitest run src/lib/cnpj/__tests__/cnae-mapping.test.ts` → 0 falhas
- Teste 43 exercita o validador de não-contradição (falha esperada em build)
- `npm run typecheck` → 0 erros
</verification>

<success_criteria>
- Testes 37–46 verdes cobrindo D9: normalização, derivação, compatibilidade por listas, granularidade exata, precedência, não-contradição CI, unknown neutro
</success_criteria>

<output>
Create `.planning/phases/42-signup-controlado-elegibilidade-freemium/42-16-SUMMARY.md` when done
</output>
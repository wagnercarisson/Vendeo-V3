---
phase: 42-signup-controlado-elegibilidade-freemium
plan: 19
type: execute
wave: 5
depends_on: ["42-13", "42-14", "42-15", "42-16", "42-17", "42-18"]
files_modified:
  - src/lib/freemium/__tests__/freemium-risk-service.test.ts
  - src/lib/__tests__/labels.test.ts
  - src/__tests__/landing/access-request-section.test.tsx
  - src/app/(auth)/login/__tests__/login-form.test.tsx
  - src/lib/launch-config/__tests__/config.test.ts
  - src/app/auth/confirm/__tests__/route.test.ts
  - src/app/(app)/admin/reviews/__tests__/page.test.tsx
autonomous: true
requirements: ["freemium-risk-service", "labels", "access-request-history", "login-page", "launch-config", "oauth-auth-callback", "admin-reviews"]

must_haves:
  truths:
    - "19.1–19.11 mapeados de tasks.md §19 (regressão + co-migração de fixtures)"
    - "19.8: `/auth/confirm` (email/OTP verifyOtp) INALTERADO — token_hash + verifyOtp continuam"
    - "19.9: `freemium-risk-service` — só o bloco SUSPENSA muda para o genérico; demais casos approved/review/defer já cobertos continuam"
    - "19.10: `reviews/page.tsx` com os 8 motivos atuais + novos — sem quebra de renderização/filtro"
    - "19.11: suíte completa passando"
  artifacts:
    - path: "src/app/auth/confirm/__tests__/route.test.ts"
      provides: "19.8 regressão"
      contains: "verifyOtp"
---

<objective>
Executar a regressão e co-migração de fixtures (tasks.md §19): co-migrar os testes afetados (risk-service, labels, landing, login-form, launch-config) e garantir regressão (landing flag off, /signup flag off, /auth/confirm inalterado, risk-service approved/review/defer, reviews com 8 motivos + novos) com a suíte completa passando.

Purpose: garantir que a fase 42 não quebra comportamentos existentes (D2–D16).
Output: suíte completa verde + regressão documentada.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-signup-controlado-elegibilidade-freemium/42-CONTEXT.md (D2–D16)
@openspec/changes/fase-42-signup-controlado-elegibilidade-freemium/tasks.md (§19, 19.1–19.11)
@src/lib/freemium/__tests__/freemium-risk-service.test.ts (42-04/42-15)
@src/lib/__tests__/labels.test.ts (42-05/42-17)
@src/__tests__/landing/access-request-section.test.tsx (42-10/42-13)
@src/app/(auth)/login/__tests__/login-form.test.tsx (42-08/42-09)
@src/lib/launch-config/__tests__/config.test.ts (42-02/42-13)
@src/app/auth/confirm/route.ts (inalterado — regressão)

<interfaces>
- Co-migrações exigidas: risk-service (19.1), labels (19.2), access-request-section (19.3), login-form (19.4), launch-config (19.5).
- Regressões a validar: landing flag off (19.6), /signup flag off (19.7), /auth/confirm inalterado (19.8), risk-service casos já cobertos (19.9), reviews 8+novos motivos (19.10), suíte completa (19.11).
- `/auth/confirm/route.ts` NÃO pode ter sido alterado — verificar `git diff`.
</interfaces>
</context>

<tasks>

<task type="execute">
  <name>Task 1: Co-migração de fixtures (19.1–19.5)</name>
  <files>src/lib/freemium/__tests__/freemium-risk-service.test.ts, src/lib/__tests__/labels.test.ts, src/__tests__/landing/access-request-section.test.tsx, src/app/(auth)/login/__tests__/login-form.test.tsx, src/lib/launch-config/__tests__/config.test.ts
  </files>
  <read_first>
    - openspec/changes/fase-42-signup-controlado-elegibilidade-freemium/tasks.md (§19, 19.1–19.5)
    - Cada arquivo de teste afetado (estado atual após plans anteriores)
  </read_first>
  <action>
    Verificar/ajustar as fixtures co-migradas:
    - 19.1: risk-service — bloco SUSPENSA → genérico; casos defer `dados_oficiais_incompletos`, `situacao_nao_ativa`, cidade/UF gate, CNAE (feito em 42-04/42-15; confirmar).
    - 19.2: labels — 4 novos + `situacao_suspensa` legado (42-05/42-17; confirmar).
    - 19.3: access-request-section — novos CTAs flag on/off (42-10/42-13; confirmar).
    - 19.4: login-form — "Continuar com Google" + captcha no login por senha (42-08/42-09; confirmar teste existente co-migrado).
    - 19.5: launch-config — novo campo `publicSignupEnabled` (42-02/42-13; confirmar fixture default false).
    Rodar a suíte desses arquivos.
  </action>
  <verify>
    <automated>npx vitest run src/lib/freemium/__tests__/freemium-risk-service.test.ts src/lib/__tests__/labels.test.ts src/__tests__/landing/access-request-section.test.tsx src/app/(auth)/login/__tests__/login-form.test.tsx src/lib/launch-config/__tests__/config.test.ts
  </verify>
  <acceptance_criteria>
    - 19.1–19.5 confirmados (fixtures co-migradas e verdes)
    - `npx vitest run` nos 5 arquivos → 0 falhas
  </acceptance_criteria>
  <done>Co-migração 19.1–19.5 confirmada</done>
</task>

<task type="execute">
  <name>Task 2: Regressão 19.6–19.11</name>
  <files>src/app/auth/confirm/route.ts, src/app/auth/confirm/__tests__/route.test.ts, src/app/(app)/admin/reviews/__tests__/page.test.tsx
  </files>
  <read_first>
    - openspec/changes/fase-42-signup-controlado-elegibilidade-freemium/tasks.md (§19, 19.6–19.11)
    - src/app/auth/confirm/route.ts (DEVE estar inalterado — git diff)
  </read_first>
  <action>
    Validar regressões:
    - 19.6: landing flag off → comportamento idêntico (form + CTA) — teste existente verde.
    - 19.7: /signup flag off → "Beta fechado" — teste existente verde.
    - 19.8: `/auth/confirm/route.ts` INALTERADO — `git diff` vazio; route.test processa `token_hash` + `verifyOtp`; o callback novo (`/auth/callback`) não interfere.
    - 19.9: risk-service — casos approved/review/defer já cobertos continuam (regressão D10).
    - 19.10: reviews page — 8 motivos atuais + novos (renderização/filtro sem quebra).
    - 19.11: suíte completa `npx vitest run` (novos + existentes + co-migrados) — zero falhas.
  </action>
  <verify>
    <automated>npx vitest run
  </verify>
  <acceptance_criteria>
    - 19.6–19.10 validados (regressão sem quebra)
    - `git diff` vazio em `src/app/auth/confirm/route.ts`
    - `npx vitest run` (suíte completa) → 0 falhas (19.11)
  </acceptance_criteria>
  <done>Regressão completa com suíte verde</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| suíte completa ↔ regressão | Novos testes não quebram comportamentos existentes |
| /auth/confirm ↔ /auth/callback | Rotas coexistem sem interferência |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-19 | Denial of Service | Suíte quebrada por co-migração | mitigate | 19.1–19.5 verdes + 19.11 suíte completa |
| T-42-19a | Tampering | /auth/confirm alterado acidentalmente | mitigate | 19.8: git diff vazio + route.test verifyOtp |
| T-42-19b | Spoofing | Filtro admin quebrado | mitigate | 19.10: 8 motivos + novos sem quebra |
| T-42-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo |
</threat_model>

<verification>
- `npx vitest run` — suíte completa → 0 falhas
- `git diff` vazio em `src/app/auth/confirm/route.ts`
- `npm run typecheck` → 0 erros
</verification>

<success_criteria>
- Co-migração 19.1–19.5 confirmada; regressão 19.6–19.11 sem quebra
- `/auth/confirm` inalterado; suíte completa verde
</success_criteria>

<output>
Create `.planning/phases/42-signup-controlado-elegibilidade-freemium/42-19-SUMMARY.md` when done
</output>
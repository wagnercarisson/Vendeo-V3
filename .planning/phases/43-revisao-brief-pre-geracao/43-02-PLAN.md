---
phase: 43-revisao-brief-pre-geracao
plan: 02
type: execute
wave: 2
depends_on: [43-01]
files_modified:
  - src/components/flow/use-campaign-form.ts
autonomous: true
requirements: [F43-08, F43-09]

must_haves:
  truths:
    - "Helper puro `prepareCampaignImages(fields)` reutiliza `compressImage` (use-campaign-form.ts:13-94), normaliza mimeType para image/jpeg, preserva role/source, cobre itens de draft com dataUrl sem re-comprimir (D3)"
    - "Helper puro `buildCampaignGenerationBody(fields, preparedImages, storeId, options?)` produz o mesmo shape do body atual de handleSubmit (use-campaign-form.ts:891-917) com os mesmos derivados da revisão; lógica XOR de imagens reproduzida (D4)"
    - "handleSubmit extrai a lógica de compressão para reuso via `prepareCampaignImages` (o submit deixa de re-comprimir)"
  artifacts:
    - path: "src/components/flow/use-campaign-form.ts"
      provides: "Tipo PreparedCampaignImage + helpers prepareCampaignImages/buildCampaignGenerationBody"
      contains: "prepareCampaignImages"
---

<objective>
Criar os dois helpers puros de domínio da revisão do brief (F43 D3/D4): `prepareCampaignImages(fields)` e `buildCampaignGenerationBody(fields, preparedImages, storeId, options?)` em `src/components/flow/use-campaign-form.ts`, reutilizados pela revisão (43-03/43-04) e pelo submit (43-03). Também extrair a lógica de compressão do `handleSubmit` atual para reuso via `prepareCampaignImages` (o submit deixa de re-comprimir — D3). Estes helpers são **puros** (sem acesso a hook state; recebem `fields` e retornam valores) — testáveis isoladamente (43-10).

Purpose: D3 (revisão mostra o payload final) + D4 (single source of truth dos derivados; testes simples).
Output: helpers exportados + handleSubmit sem re-compressão.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-revisao-brief-pre-geracao/43-CONTEXT.md (D3/D4; linhas 80-95 estado real: compressImage 13-94, buildValidityDisplayText 361-385, buildMandatoryArtworkText 387-397, inferIntent 399-409, body 891-917)
@openspec/changes/fase-43-revisao-brief-pre-geracao/specs/campaign-brief-review/spec.md (Requirement: Compressão das imagens antes da revisão + Helper único de body)
@openspec/changes/fase-43-revisao-brief-pre-geracao/specs/campaign-input-ui/spec.md (Requirement: Submit triggers API generation — body)
@src/components/flow/use-campaign-form.ts (estado atual: CampaignFormFields 109-125, CampaignProductFormImage 100-107, compressImage 13-94, buildValidityDisplayText 361-385, buildMandatoryArtworkText 387-397, inferIntent 399-409, handleSubmit 784-925, body 891-917, XOR 906-917)

<interfaces>
- `CampaignProductFormImage` (`use-campaign-form.ts:100-107`): `{ id, role: "primary"|"reference", source: "upload"|"camera", mimeType, file?, dataUrl? }`
- `compressImage(file, opts)` (exportada, `:1052`): HEIC/EXIF via createImageBitmap; retorna dataUrl + mimeType image/jpeg
- `buildValidityDisplayText(fields)` (`:361-385`): texto formatado dd/mm (só faz sentido quando offer)
- `buildMandatoryArtworkText(showIllustrativeNotice, mandatoryArtworkTextFree)` (`:387-397`)
- `inferIntent(fields)` (`:399-409`): "offer" | "spotlight" | "exclusive"
- Body atual (`:891-917`): ver `<action>` abaixo
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tipo PreparedCampaignImage + helper prepareCampaignImages(fields)</name>
  <files>src/components/flow/use-campaign-form.ts</files>
  <read_first>
    - src/components/flow/use-campaign-form.ts (CampaignProductFormImage 100-107; compressImage 13-94; EMPTY_FIELDS; CampaignFormFields 109-125)
    - openspec/changes/fase-43-revisao-brief-pre-geracao/specs/campaign-brief-review/spec.md (Requirement: Compressão das imagens antes da revisão (prepareCampaignImages))
  </read_first>
  <action>
    Definir e exportar o tipo `PreparedCampaignImage`:
    ```ts
    export interface PreparedCampaignImage {
      id: string;
      role: "primary" | "reference";
      source: "upload" | "camera";
      mimeType: "image/jpeg";
      dataUrl: string;
    }
    ```
    Definir e exportar o helper puro `prepareCampaignImages`:
    ```ts
    export async function prepareCampaignImages(
      fields: CampaignFormFields
    ): Promise<PreparedCampaignImage[]> {
      const images = fields.productImages; // array de CampaignProductFormImage
      const prepared: PreparedCampaignImage[] = [];
      for (const img of images) {
        if (img.file) {
          const compressed = await compressImage(img.file); // HEIC/EXIF, mimeType image/jpeg
          prepared.push({ id: img.id, role: img.role, source: img.source, mimeType: "image/jpeg", dataUrl: compressed.dataUrl });
        } else if (img.dataUrl) {
          // item restaurado de draft — normaliza mimeType sem re-comprimir
          prepared.push({ id: img.id, role: img.role, source: img.source, mimeType: "image/jpeg", dataUrl: img.dataUrl });
        }
        // item sem file nem dataUrl é ignorado (não utilizável)
      }
      return prepared;
    }
    ```
    Requisitos: reutiliza `compressImage` (`:13-94`, HEIC/EXIF via createImageBitmap); normaliza `mimeType` para `image/jpeg`; preserva `role`/`source`; cobre itens restaurados de draft com `dataUrl` (sem re-comprimir); ignora itens sem `file` nem `dataUrl`. Se `CampaignFormFields.productImages` não existir com esse nome exato, usar o nome real do array de imagens no form (verificar `:109-125`); se o form usa um campo único legado, adaptar à estrutura real mantendo o contrato do helper (recebe `fields` e retorna `PreparedCampaignImage[]`). Não alterar a assinatura dos campos existentes do form além do necessário.
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\components\flow\use-campaign-form.ts" -Pattern "export (async )?function prepareCampaignImages|interface PreparedCampaignImage"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/flow/use-campaign-form.ts` contém `export interface PreparedCampaignImage` com `{ id, role, source, mimeType: "image/jpeg", dataUrl }`
    - Contém `export async function prepareCampaignImages(fields: CampaignFormFields): Promise<PreparedCampaignImage[]>`
    - O helper reutiliza `compressImage` para itens com `file`; normaliza `mimeType` para `image/jpeg`; preserva `role`/`source`; cobre itens de draft com `dataUrl` sem re-comprimir
    - `npx vitest run` da suíte existente continua passando (nenhuma quebra até 43-03/43-04 mudarem o call site)
  </acceptance_criteria>
  <done>PreparedCampaignImage + prepareCampaignImages implementados e exportados</done>
</task>

<task type="auto">
  <name>Task 2: Helper buildCampaignGenerationBody(fields, preparedImages, storeId, options?)</name>
  <files>src/components/flow/use-campaign-form.ts</files>
  <read_first>
    - src/components/flow/use-campaign-form.ts (handleSubmit body 891-917; buildValidityDisplayText 361-385; buildMandatoryArtworkText 387-397; inferIntent 399-409)
    - openspec/changes/fase-43-revisao-brief-pre-geracao/specs/campaign-brief-review/spec.md (Requirement: Helper único de body (buildCampaignGenerationBody))
    - openspec/changes/fase-43-revisao-brief-pre-geracao/specs/campaign-input-ui/spec.md (Requirement: Submit triggers API generation — body)
  </read_first>
  <action>
    Definir e exportar o helper puro `buildCampaignGenerationBody` reproduzindo EXATAMENTE o shape do body atual de `handleSubmit` (`:891-917`) usando os **mesmos derivados que a revisão exibe**:
    ```ts
    export function buildCampaignGenerationBody(
      fields: CampaignFormFields,
      preparedImages: PreparedCampaignImage[],
      storeId: string,
      options?: { inputValidationOverride?: { productImageCheck: "brief_review_confirmed" } }
    ): Record<string, unknown> {
      const body: Record<string, unknown> = {
        storeId,
        productName: fields.productName,
        originalPriceCents: fields.originalPriceCents,
        discountedPriceCents: fields.discountedPriceCents,
        description: fields.description || undefined,
        badgeText: fields.badge,
        campaignIntent: fields.campaignIntent,
        ...(fields.campaignIntent === "offer" ? {} : { preserveImageContext: fields.preserveImageContext }),
      };
      const validity = fields.campaignIntent === "offer" ? buildValidityDisplayText(fields) : undefined;
      if (validity) body.validity = validity;
      const mandatoryArtworkText = buildMandatoryArtworkText(fields.showIllustrativeNotice, fields.mandatoryArtworkTextFree);
      if (mandatoryArtworkText) body.mandatoryArtworkText = mandatoryArtworkText;
      if (preparedImages.length > 1) {
        body.productImages = preparedImages.map(({ role, source, mimeType, dataUrl }) => ({ role, source, mimeType, dataUrl }));
      } else {
        body.productImageDataUrl = preparedImages[0]?.dataUrl;
      }
      if (options?.inputValidationOverride) {
        body.inputValidationOverride = options.inputValidationOverride;
      }
      return body;
    }
    ```
    Requisitos: espelha a **lógica XOR** (`:906-917`) — `productImages[]` (sem `id` de cliente) quando `preparedImages.length > 1`; `productImageDataUrl` quando 1 (legado); `validity` apenas quando `campaignIntent === "offer"` e validade habilitada (via `buildValidityDisplayText`); `mandatoryArtworkText` via `buildMandatoryArtworkText` (ausente quando desmarcado e sem texto); `preserveImageContext` apenas quando intent ≠ offer; `inputValidationOverride` via `options`. **Usar os nomes reais dos campos de `CampaignFormFields`** (verificar `:109-125` — ex.: `fields.badge` pode ser `fields.badgeText`; ajustar). Não incluir `id` de cliente nos itens de `productImages`.
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\components\flow\use-campaign-form.ts" -Pattern "export function buildCampaignGenerationBody"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/flow/use-campaign-form.ts` contém `export function buildCampaignGenerationBody(fields: CampaignFormFields, preparedImages: PreparedCampaignImage[], storeId: string, options?: { inputValidationOverride?: { productImageCheck: "brief_review_confirmed" } }): Record<string, unknown>`
    - `preparedImages.length > 1` → `body.productImages` com itens `{ role, source, mimeType, dataUrl }` (sem `id`)
    - `preparedImages.length === 1` → `body.productImageDataUrl` (legado)
    - `validity` presente apenas quando `campaignIntent === "offer"` e habilitada; `mandatoryArtworkText` ausente quando desmarcado e sem texto; `preserveImageContext` apenas quando intent ≠ offer
    - `inputValidationOverride` presente quando `options?.inputValidationOverride` fornecido
    - `npx vitest run` da suíte existente continua passando
  </acceptance_criteria>
  <done>buildCampaignGenerationBody implementado e exportado (single source of truth)</done>
</task>

<task type="auto">
  <name>Task 3: Refatorar handleSubmit para reutilizar prepareCampaignImages (deixar de re-comprimir)</name>
  <files>src/components/flow/use-campaign-form.ts</files>
  <read_first>
    - src/components/flow/use-campaign-form.ts (handleSubmit 784-925; compressão atual ~760-775 no alignment / ~891-917 no body)
    - openspec/changes/fase-43-revisao-brief-pre-geracao/specs/campaign-input-ui/spec.md (Scenario: Submit não re-comprime)
  </read_first>
  <action>
    Refatorar `handleSubmit` (`:784-925`) para **aceitar/usar `preparedImages` no caminho da revisão** (F43) e deixar de re-comprimir. Como a F43 (43-03) mudará o fluxo para passar pela revisão, nesta task:
    - Extrair a lógica de compressão do `handleSubmit` atual para reuso via `prepareCampaignImages` (eliminar a duplicação inline de compressão).
    - Ajustar `handleSubmit` para, no caminho sem revisão (compatibilidade/regressão), usar `prepareCampaignImages(fields)` para obter `preparedImages` e montar o body via `buildCampaignGenerationBody(fields, preparedImages, storeId)` (sem `inputValidationOverride` no caminho legado — o override só entra no caminho confirmado da revisão, 43-03).
    - Preservar o comportamento atual para payload sem override (regressão).
    - **Não** introduzir ainda o estado `reviewMode` (é escopo do 43-03) — apenas preparar o reuso dos helpers.
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\components\flow\use-campaign-form.ts" -Pattern "prepareCampaignImages\(|buildCampaignGenerationBody\("</automated>
  </verify>
  <acceptance_criteria>
    - `handleSubmit` reutiliza `prepareCampaignImages` e `buildCampaignGenerationBody` (sem duplicação inline de compressão/montagem)
    - O submit não roda `compressImage` duas vezes para a mesma imagem
    - Comportamento atual para payload sem override preservado (regressão) — `npx vitest run` da suíte existente passa (os testes de hook serão co-migrados em 43-14)
    - `npm run typecheck` — zero erros
  </acceptance_criteria>
  <done>handleSubmit reutiliza os helpers puros; sem re-compressão duplicada</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| form state ↔ body | Dados do formulário derivados para o body de geração — risco de divergência entre o que a revisão mostra e o que o body envia |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-02 | Tampering | use-campaign-form.ts helpers | mitigate | `buildCampaignGenerationBody` é single source of truth dos derivados (validade/aviso/intent/badge/preços/imagens) — revisão e submit usam o mesmo helper (D4); teste 7 valida idempotência; lógica XOR reproduzida do body atual |
| T-43-02a | Information Disclosure | Imagens | accept | DataUrls de imagem manipulados apenas em memória; nenhum log de payload |
</threat_model>

<verification>
- `prepareCampaignImages(fields)` exportado e usa `compressImage` para `file`, normaliza mimeType jpeg, preserva role/source, cobre draft com dataUrl
- `buildCampaignGenerationBody(fields, preparedImages, storeId, options?)` exportado com shape idêntico ao body atual e lógica XOR de imagens
- `handleSubmit` reutiliza os helpers (sem re-compressão)
- `npx vitest run` da suíte existente passa; `npm run typecheck` zero erros
</verification>

<success_criteria>
- Dois helpers puros `prepareCampaignImages`/`buildCampaignGenerationBody` implementados e exportados (D3/D4)
- Revisão e submit passam a compartilhar a mesma fonte dos derivados (idempotência)
- handleSubmit deixa de re-comprimir; regressão sem override preservada
</success_criteria>

<output>
Create `.planning/phases/43-revisao-brief-pre-geracao/43-02-SUMMARY.md` when done
</output>

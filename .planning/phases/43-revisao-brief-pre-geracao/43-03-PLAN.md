---
phase: 43-revisao-brief-pre-geracao
plan: 03
type: execute
wave: 3
depends_on: [43-02]
files_modified:
  - src/components/flow/use-campaign-form.ts
autonomous: true
requirements: [F43-02, F43-03, F43-04, F43-05, F43-06, F43-07]

must_haves:
  truths:
    - "Novo estado `reviewMode` (ou `step: \"form\" | \"review\"`) no useCampaignForm exposto no retorno do hook (D2)"
    - "Entrada em revisão somente com form válido; roda `prepareCampaignImages` com estado 'Preparando imagens...'; falha → volta ao form com erro claro (D2/D3)"
    - "'Voltar e editar' preserva fields/touched/fieldErrors (D2)"
    - "'Confirmar e gerar campanha' trava o snapshot revisado (fields + preparedImages; body imutável; desabilita interação) e monta o body via buildCampaignGenerationBody com inputValidationOverride.productImageCheck: 'brief_review_confirmed' e chama consumeStream (D2/D4/D5)"
    - "Fluxo pós-confirmação inalterado: isSubmitting → GenerationProgress, 409, navegação /campanhas/[id] (D2)"
    - "Sem imagens utilizáveis → 'Revisar e gerar' bloqueado com mensagem de imagem obrigatória (D2)"
  artifacts:
    - path: "src/components/flow/use-campaign-form.ts"
      provides: "Estado reviewMode + transições + snapshot travado + body com brief_review_confirmed"
      contains: "brief_review_confirmed"
---

<objective>
Adicionar ao `useCampaignForm` o **estado `reviewMode`** (ou `step: "form" | "review"`) e as transições de revisão do brief (F43 D2/D3/D4): entrada em revisão (gate de validade + `prepareCampaignImages` com estado "Preparando imagens..."), "Voltar e editar" (preserva estado), "Confirmar e gerar campanha" (trava o snapshot revisado — `fields` + `preparedImages` congelados, body imutável — e dispara o submit real via `buildCampaignGenerationBody` com `inputValidationOverride.productImageCheck: "brief_review_confirmed"`). Nenhum POST/IA/createCampaign/reserveCredit antes da confirmação. O `CampaignInputForm` (43-04) renderiza a tela de revisão quando `reviewMode` ativo.

Purpose: D2 (gate obrigatório em tela intermediária), D3 (compressão antes da revisão), D4 (body via helper único), D5 (override brief_review_confirmed no caminho confirmado).
Output: hook com reviewMode + snapshot travado.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-revisao-brief-pre-geracao/43-CONTEXT.md (D2/D3/D4/D5; estado real: handleSubmit 784-925, consumeStream 665-782, handleConflictContinue 944-955, isValid 1018-1022, prepareCampaignImages/buildCampaignGenerationBody de 43-02)
@openspec/changes/fase-43-revisao-brief-pre-geracao/specs/campaign-brief-review/spec.md (Requirement: Gate de revisão obrigatório em tela intermediária (reviewMode) + Transições Voltar e editar / Confirmar e gerar campanha + Compressão antes da revisão + Helper único de body)
@openspec/changes/fase-43-revisao-brief-pre-geracao/specs/campaign-input-ui/spec.md (Requirement: Botão principal vira 'Revisar e gerar' + Submit triggers API generation)
@src/components/flow/use-campaign-form.ts (estado atual: handleSubmit 784-925, consumeStream 665-782, handleConflictContinue 944-955, isValid 1018-1022)

<interfaces>
- `handleSubmit` (`:784-925`) — valida client-side, monta body via `buildCampaignGenerationBody` (de 43-02) e chama `consumeStream`
- `consumeStream` (`:665-782`) — 409 de conflito em `:677-711`; reenvia com `productImageCheck: "user_confirmed_continue"` via `handleConflictContinue` (`:944-955`)
- `isValid` (`:1018-1022`) — inclui `hasUsableImage`
- `CampaignFormFields` (`:109-125`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estado reviewMode + exposição no retorno do hook</name>
  <files>src/components/flow/use-campaign-form.ts</files>
  <read_first>
    - src/components/flow/use-campaign-form.ts (retorno do hook; campos de estado; EMPTY_FIELDS)
    - openspec/changes/fase-43-revisao-brief-pre-geracao/specs/campaign-brief-review/spec.md (Requirement: Gate de revisão obrigatório em tela intermediária (reviewMode))
  </read_first>
  <action>
    Adicionar o estado `reviewMode` (ou `step: "form" | "review"`) no hook `useCampaignForm`. Expor no retorno do hook, além dos campos atuais, um subconjunto de controle da revisão, por exemplo:
    - `reviewMode: boolean`
    - `reviewState: { preparing: boolean; error?: string | null; preparedImages: PreparedCampaignImage[] | null }` (ou equivalente) para a UI de revisão (43-04)
    - `enterReview(): Promise<boolean>` — valida o form (gate `isValid`); se inválido, exibe erros e retorna false (não entra em revisão); se válido, roda `prepareCampaignImages(fields)` (D3), seta `reviewMode=true` + `preparedImages`, com estado de preparação
    - `exitReview(): void` — "Voltar e editar": volta para `step: "form"`, preservando `fields`/`touched`/`fieldErrors` (nada perdido)
    - `confirmReview(): Promise<void>` — "Confirmar e gerar campanha": trava o snapshot revisado (congela `fields` + `preparedImages`; body imutável; desabilita interação durante a geração) e dispara o fluxo real (monta body via `buildCampaignGenerationBody(frozenFields, frozenPrepared, storeId, { inputValidationOverride: { productImageCheck: "brief_review_confirmed" } })` e chama `consumeStream`)
    - Se o hook usa um objeto de retorno grande, agrupar os novos controles de forma coerente com o padrão existente. Preservar todos os campos/controles atuais do retorno (não quebrar call sites existentes até 43-04).
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\components\flow\use-campaign-form.ts" -Pattern "reviewMode|enterReview|exitReview|confirmReview"</automated>
  </verify>
  <acceptance_criteria>
    - O hook expõe `reviewMode` e controles de entrada/saída/confirmação da revisão
    - `enterReview` não entra em revisão quando o form é inválido (erros exibidos)
    - `enterReview` roda `prepareCampaignImages` (estado de preparação) antes de ativar a revisão
    - `exitReview` preserva `fields`/`touched`/`fieldErrors`
    - `confirmReview` monta o body via `buildCampaignGenerationBody` com `inputValidationOverride.productImageCheck: "brief_review_confirmed"` e chama `consumeStream`
    - `npx vitest run` da suíte existente passa (call sites de `CampaignInputForm` ainda não migrados — serão em 43-04; testes de hook co-migrados em 43-14)
  </acceptance_criteria>
  <done>Estado reviewMode + controles de transição expostos no hook</done>
</task>

<task type="auto">
  <name>Task 2: Preparação de imagens na entrada da revisão + erro de preparação + sem imagem utilizável</name>
  <files>src/components/flow/use-campaign-form.ts</files>
  <read_first>
    - src/components/flow/use-campaign-form.ts (enterReview; submitError; prepareCampaignImages de 43-02; isValid 1018-1022)
    - openspec/changes/fase-43-revisao-brief-pre-geracao/specs/campaign-brief-review/spec.md (Requirement: Compressão das imagens antes da revisão + Scenario: Falha de compressão volta ao form com erro + Scenario: Sem imagens utilizáveis a revisão bloqueia)
  </read_first>
  <action>
    Implementar em `enterReview`:
    1. **Gate de validade:** se `!isValid` (incluindo `hasUsableImage`), NÃO entrar em revisão — exibir os erros de validação existentes (comportamento atual) e retornar false.
    2. **Sem imagem utilizável:** se não houver nenhuma imagem utilizável (nem `file`, nem `dataUrl`, nem restaurada), bloquear a entrada e exibir a mensagem de imagem obrigatória (mesma lógica de `hasUsableImage`).
    3. **Preparação:** setar estado de preparação ("Preparando imagens..."); chamar `prepareCampaignImages(fields)`; em sucesso, guardar `preparedImages` e ativar `reviewMode`; **em falha**, voltar ao form com erro claro PT-BR (mesmo padrão do `submitError`).
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\components\flow\use-campaign-form.ts" -Pattern "prepareCampaignImages|Preparando imagens"</automated>
  </verify>
  <acceptance_criteria>
    - `enterReview` com form inválido → não entra em revisão (erros exibidos)
    - `enterReview` sem imagem utilizável → bloqueado com mensagem de imagem obrigatória
    - `enterReview` dispara `prepareCampaignImages` com estado de preparação; falha → volta ao form com erro claro PT-BR
    - Sucesso → `reviewMode` ativo com `preparedImages` guardados
    - `npx vitest run` da suíte existente passa
  </acceptance_criteria>
  <done>Preparação de imagens na entrada da revisão + erro claro + bloqueio sem imagem</done>
</task>

<task type="auto">
  <name>Task 3: Confirmar e gerar campanha — snapshot travado + body com brief_review_confirmed + fluxo pós-confirmação</name>
  <files>src/components/flow/use-campaign-form.ts</files>
  <read_first>
    - src/components/flow/use-campaign-form.ts (confirmReview; consumeStream 665-782; handleConflictContinue 944-955; buildCampaignGenerationBody de 43-02)
    - openspec/changes/fase-43-revisao-brief-pre-geracao/specs/campaign-brief-review/spec.md (Requirement: Transições Voltar e editar / Confirmar e gerar campanha + Scenario: Confirmar trava o snapshot e dispara o submit real)
    - openspec/changes/fase-43-revisao-brief-pre-geracao/specs/campaign-input-ui/spec.md (Scenario: Confirmar envia brief_review_confirmed)
  </read_first>
  <action>
    Implementar `confirmReview`:
    1. **Trava o snapshot revisado:** congelar uma cópia de `fields` + `preparedImages` no momento da confirmação (imutável a partir daqui; desabilitar interação durante a geração). O body é montado a partir dessa cópia congelada.
    2. **Body:** montar via `buildCampaignGenerationBody(frozenFields, frozenPrepared, storeId, { inputValidationOverride: { productImageCheck: "brief_review_confirmed" } })`.
    3. **Disparar o fluxo real:** chamar `consumeStream` com o body. A partir daqui o fluxo existente é inalterado: `isSubmitting` → `GenerationProgress`, 409 de conflito (`user_confirmed_continue` via `handleConflictContinue`), navegação para `/campanhas/[id]`.
    4. **Garantia:** nenhum POST antes da confirmação (o `handleSubmit`/`consumeStream` só é chamado no `confirmReview`; no caminho de revisão o clique no form vira "Revisar e gerar", escopo 43-04).
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\components\flow\use-campaign-form.ts" -Pattern 'brief_review_confirmed|consumeStream'</automated>
  </verify>
  <acceptance_criteria>
    - `confirmReview` congela `fields` + `preparedImages` (body imutável a partir da confirmação)
    - O body montado carrega `inputValidationOverride.productImageCheck: "brief_review_confirmed"`
    - O fluxo real de geração é disparado (consumeStream → isSubmitting → GenerationProgress, 409, navegação — inalterados)
    - Nenhum POST/IA/createCampaign/reserveCredit antes da confirmação
    - `npx vitest run` da suíte existente passa
  </acceptance_criteria>
  <done>confirmReview trava o snapshot e dispara o submit real com brief_review_confirmed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| form state ↔ snapshot | Estado do form congelado no momento da confirmação — risco de divergência entre o que a revisão mostrou e o que foi enviado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-03 | Tampering | use-campaign-form.ts reviewMode | mitigate | Snapshot travado congela `fields` + `preparedImages` no "Confirmar" (body imutável; desabilita interação durante geração); `buildCampaignGenerationBody` single source (D4); teste 6 valida imutabilidade |
| T-43-03a | Information Disclosure | Formulário | accept | Sem dados sensíveis além dos campos de campanha já existentes |
</threat_model>

<verification>
- Hook expõe `reviewMode` + `enterReview`/`exitReview`/`confirmReview`
- `enterReview` com form inválido/sem imagem → não entra; roda `prepareCampaignImages`; falha → volta ao form com erro
- `exitReview` preserva fields/touched/fieldErrors
- `confirmReview` trava o snapshot e monta body com `brief_review_confirmed`
- Fluxo pós-confirmação inalterado (consumeStream, 409, navegação)
- `npx vitest run` da suíte existente passa
</verification>

<success_criteria>
- Gate client-side obrigatório em tela intermediária implementado no hook (D2)
- Compressão antes da revisão + erro claro (D3)
- Body via helper único com `brief_review_confirmed` (D4/D5)
- Nenhum POST/IA/persistência/crédito antes da confirmação
</success_criteria>

<output>
Create `.planning/phases/43-revisao-brief-pre-geracao/43-03-SUMMARY.md` when done
</output>

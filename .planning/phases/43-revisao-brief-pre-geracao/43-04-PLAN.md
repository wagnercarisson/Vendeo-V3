---
phase: 43-revisao-brief-pre-geracao
plan: 04
type: execute
wave: 4
depends_on: [43-03]
files_modified:
  - src/components/flow/campaign-input-form.tsx
  - src/components/flow/campaign-brief-review.tsx
  - src/components/flow/store-identity-block.tsx
autonomous: true
requirements: [F43-25, F43-26]

must_haves:
  truths:
    - "Botão principal do form vira 'Revisar e gerar' (substitui 'Criar Campanha') — campaign-input-form.tsx"
    - "CampaignInputForm renderiza a tela de revisão do brief quando reviewMode ativo (mesmo padrão do isSubmitting → GenerationProgress)"
    - "Tela de revisão (campaign-brief-review) mostra: loja/marca no topo (StoreIdentityBlock), seções Produto/Oferta/Imagens/Avisos/Custo, rótulos 'Principal'/'Referência', 'Vai consumir X crédito(s)' + saldo, slot Tema reservado (não renderiza)"
    - "Ações 'Voltar e editar' e 'Confirmar e gerar campanha' com touch ≥ 44px, label/aria, loading; microcopy; preview das imagens sem recorte (object-contain, célula aspect-square); seções empilham sem scroll horizontal (D7)"
    - "Estados: 'Preparando imagens...', desabilitação durante a confirmação, erro de preparação claro"
  artifacts:
    - path: "src/components/flow/campaign-brief-review.tsx"
      provides: "Componente de revisão do brief"
      contains: "Confirmar e gerar campanha"
---

<objective>
Criar a **tela de revisão do brief** (`campaign-brief-review`) no fluxo do form e integrá-la ao `CampaignInputForm`: o botão principal vira **"Revisar e gerar"** e, com `reviewMode` ativo (43-03), o form renderiza o resumo do brief em tela intermediária (não modal). A revisão mostra **exatamente o payload final** (imagens comprimidas de `prepareCampaignImages`), com seções Produto/Oferta/Imagens/Avisos/Custo, loja/marca ativa, rótulos Principal/Referência, "Vai consumir X crédito(s)" + saldo e slot Tema reservado. Ações "Voltar e editar" e "Confirmar e gerar campanha" com a11y/mobile/microcopy (D6/D7).

Purpose: D6 (resumo completo e honesto) + D7 (a11y/mobile/microcopy).
Output: tela de revisão integrada ao fluxo do form.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-revisao-brief-pre-geracao/43-CONTEXT.md (D6/D7; estado real: botão "Criar Campanha" 605-631, FormContent 257, submitDisabled 284-289, "Saldo/Custo" 567-595, isSubmitting → GenerationProgress 54-62; StoreIdentityBlock)
@openspec/changes/fase-43-revisao-brief-pre-geracao/specs/campaign-brief-review/spec.md (Requirement: Conteúdo do resumo do brief (seções + rótulos + custo + Tema) + A11y, mobile e microcopy da revisão)
@openspec/changes/fase-43-revisao-brief-pre-geracao/specs/campaign-input-ui/spec.md (Requirement: Botão principal vira 'Revisar e gerar')
@src/components/flow/campaign-input-form.tsx (estado atual)
@src/components/flow/store-identity-block.tsx (componente; retorna null sem identity não-nulo)
@src/hooks/use-operation-costs.ts (costs.campaign_generation.{costCredits, enabled}; status)

<interfaces>
- `useOperationCosts` → `{ costs, status, refetch }`; `costs.campaign_generation.{costCredits, enabled}`; `status: "loading"|"unavailable"|"loaded"`
- `buildValidityDisplayText(fields)` / `buildMandatoryArtworkText(...)` / `inferIntent(fields)` (derivados exibidos)
- `preparedImages` do `reviewState` (43-03)
- `StoreIdentityBlock({ store, identity })` — precisa de snapshot real; decidir a fonte no planejamento (componente retorna null sem identity não-nulo)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Botão 'Revisar e gerar' + render da tela de revisão no CampaignInputForm</name>
  <files>src/components/flow/campaign-input-form.tsx</files>
  <read_first>
    - src/components/flow/campaign-input-form.tsx (botão "Criar Campanha" 605-631; isSubmitting → GenerationProgress 54-62; FormContent 257)
    - openspec/changes/fase-43-revisao-brief-pre-geracao/specs/campaign-input-ui/spec.md (Requirement: Botão principal vira 'Revisar e gerar')
  </read_first>
  <action>
    1. No `CampaignInputForm`, quando `reviewMode` estiver ativo (do hook 43-03), renderizar a tela de revisão (`campaign-brief-review`) **em vez do formulário** (mesmo padrão de `isSubmitting` → `GenerationProgress`). O estado de preparação ("Preparando imagens...") e o erro de preparação devem ser exibidos.
    2. No `FormContent` (ou onde está o botão principal, `:605-631`), trocar o rótulo **"Criar Campanha"** → **"Revisar e gerar"**. O clique não dispara o POST direto — chama `enterReview()` (43-03). Com form inválido/sem imagem/custo off/indisponível/saldo insuficiente, o botão fica bloqueado (mesma lógica `submitDisabled`, `:284-289`) — a revisão não abre (Scenario: Custo off/indisponível/saldo insuficiente bloqueia a entrada na revisão).
    3. Passar para a tela de revisão os dados necessários: `fields` (ou derivados), `preparedImages`, `reviewState`, `store`/identidade, custo/saldo (`useOperationCosts`), e callbacks `onBack` (exitReview) e `onConfirm` (confirmReview).
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\components\flow\campaign-input-form.tsx" -Pattern "Revisar e gerar|campaign-brief-review|enterReview"</automated>
  </verify>
  <acceptance_criteria>
    - O botão principal exibe "Revisar e gerar" (não "Criar Campanha")
    - Com `reviewMode` ativo, o form renderiza a tela de revisão (não o formulário)
    - Com form inválido/sem imagem/custo off/indisponível/saldo insuficiente, o botão fica bloqueado e a revisão não abre
    - `npx vitest run` da suíte existente passa (mocks de `CampaignImageUpload`/form co-migrados quando necessário; regressão em 43-14)
  </acceptance_criteria>
  <done>Botão 'Revisar e gerar' + render da revisão integrados ao CampaignInputForm</done>
</task>

<task type="auto">
  <name>Task 2: Componente campaign-brief-review — seções Produto/Oferta/Imagens/Avisos/Custo + loja/marca + rótulos + custo + slot Tema</name>
  <files>src/components/flow/campaign-brief-review.tsx</files>
  <read_first>
    - openspec/changes/fase-43-revisao-brief-pre-geracao/specs/campaign-brief-review/spec.md (Requirement: Conteúdo do resumo do brief (seções + rótulos + custo + Tema))
    - src/components/flow/store-identity-block.tsx (componente; props)
    - src/components/flow/campaign-input-form.tsx (padrões de estilos/seções; "Saldo: X · Custo: Y" 567-595)
    - src/components/flow/use-campaign-form.ts (buildValidityDisplayText, buildMandatoryArtworkText, inferIntent)
  </read_first>
  <action>
    Criar `src/components/flow/campaign-brief-review.tsx` (componente client no fluxo do form) renderizando o resumo do brief:
    - **Topo:** loja/marca ativa via `StoreIdentityBlock` (precisa de snapshot real — resolver a fonte da identidade no planejamento; se não houver snapshot disponível de forma viável, exibir nome/segmento/cor da loja disponíveis no form, documentando a decisão; o `StoreIdentityBlock` retorna null sem identity não-nulo).
    - **Seção Produto:** nome, descrição (se houver).
    - **Seção Oferta:** tipo de campanha (Oferta/Destaque/Exclusivo via `inferIntent`), selo (badge), preço original (se houver), preço com desconto, validade formatada (`buildValidityDisplayText`).
    - **Seção Imagens:** thumbnails do payload final (`preparedImages` de 43-03) com rótulos **"Principal"** (role primary) e **"Referência"** (role reference), exibidas **sem recorte** (`object-contain` em célula `aspect-square`).
    - **Seção Avisos:** checkbox "imagem meramente ilustrativa" (estado) + texto obrigatório (`buildMandatoryArtworkText`).
    - **Seção Custo:** "Vai consumir X crédito(s)" + saldo atual (via `useOperationCosts`); botão "Confirmar e gerar campanha" bloqueado quando custo indisponível/desativado/saldo insuficiente (mesma lógica `submitDisabled`).
    - **Seção Tema:** **slot opcional reservado** — não renderiza enquanto `creativeContext.themeId` for null (hoje sempre); preparação F44.
    - Microcopy: aviso "Revise textos, preços e imagens antes de publicar: a IA pode cometer erros."
    Usar os padrões de estilo do form (design system). As seções devem empilhar em telas estreitas (sem scroll horizontal); thumbnails com grid.
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\components\flow\campaign-brief-review.tsx" -Pattern "Principal|Referência|Vai consumir|Revise textos, preços e imagens"</automated>
  </verify>
  <acceptance_criteria>
    - `campaign-brief-review.tsx` existe e renderiza as seções Produto/Oferta/Imagens/Avisos/Custo com os valores do brief
    - Imagens rotuladas "Principal"/"Referência"; preview `object-contain` em célula `aspect-square`
    - "Vai consumir X crédito(s)" + saldo exibidos; "Confirmar" bloqueado com custo off/indisponível/saldo insuficiente
    - Tema não renderiza (themeId null); slot reservado
    - Aviso "Revise textos, preços e imagens antes de publicar: a IA pode cometer erros." presente
    - Seções empilham em telas estreitas (sem scroll horizontal)
  </acceptance_criteria>
  <done>Componente de revisão com seções, rótulos, custo, loja e slot Tema criado</done>
</task>

<task type="auto">
  <name>Task 3: Ações Voltar e editar / Confirmar e gerar campanha com a11y/mobile/loading + estados</name>
  <files>src/components/flow/campaign-brief-review.tsx</files>
  <read_first>
    - openspec/changes/fase-43-revisao-brief-pre-geracao/specs/campaign-brief-review/spec.md (Requirement: A11y, mobile e microcopy da revisão + Transições)
  </read_first>
  <action>
    Adicionar as ações da revisão:
    - **"Voltar e editar"** → chama `exitReview()` (preserva tudo, D2); touch target ≥ 44px; `label`/`aria` PT-BR.
    - **"Confirmar e gerar campanha"** → chama `confirmReview()` (trava o snapshot e dispara o submit, D2); com **loading** (padrão do form) e desabilitado durante a geração/confirmação; bloqueado quando custo off/indisponível/saldo insuficiente.
    - Estados: "Preparando imagens..." (durante a preparação, antes de exibir o resumo), desabilitação durante a confirmação (snapshot travado), erro de preparação claro (PT-BR).
    - A11y: touch ≥ 44px, foco visível, leitura com `label`/`aria`; PT-BR.
    - Mobile: botões "Confirmar e gerar"/"Voltar e editar" sempre acessíveis; sem scroll horizontal; preview sem recorte.
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\components\flow\campaign-brief-review.tsx" -Pattern "Voltar e editar|Confirmar e gerar campanha|Preparando imagens"</automated>
  </verify>
  <acceptance_criteria>
    - "Voltar e editar" e "Confirmar e gerar campanha" presentes com touch ≥ 44px e `label`/`aria` PT-BR
    - "Confirmar" com loading e desabilitado durante a geração; bloqueado com custo off/indisponível/saldo insuficiente
    - "Preparando imagens..." e erro de preparação claro exibidos
    - Seções empilham sem scroll horizontal em telas estreitas (320px/375px)
  </acceptance_criteria>
  <done>Ações e estados da revisão com a11y/mobile/loading implementados</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI ↔ payload | O resumo exibido na revisão deve refletir exatamente o body enviado — risco de divergência visual |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-04 | Tampering | campaign-brief-review.tsx | mitigate | Revisão consome os mesmos derivados (`buildValidityDisplayText`/`buildMandatoryArtworkText`/`inferIntent`) e `preparedImages` que o body via `buildCampaignGenerationBody` (D4) — teste 7 valida idempotência |
| T-43-04a | Information Disclosure | Imagens | accept | DataUrls exibidos apenas no browser do lojista; sem log |
| T-43-04b | UI Redress | Custo/saldo | mitigate | "Confirmar" bloqueado quando custo off/indisponível/saldo insuficiente (mesma lógica `submitDisabled`), prevenindo confirmação sem condição de cobrança |
</threat_model>

<verification>
- Botão "Revisar e gerar" no form; render da revisão com reviewMode
- `campaign-brief-review.tsx` com seções Produto/Oferta/Imagens/Avisos/Custo + loja/marca + rótulos + custo + slot Tema
- Ações com a11y/mobile/loading + estados
- Preview sem recorte (object-contain); seções empilham sem scroll horizontal
- `npx vitest run` da suíte existente passa
</verification>

<success_criteria>
- Tela intermediária de revisão (não modal) integrada ao fluxo do form (D2/D6)
- Resumo completo e honesto (D6); a11y/mobile/microcopy (D7)
- Botão "Revisar e gerar" substitui "Criar Campanha"
</success_criteria>

<output>
Create `.planning/phases/43-revisao-brief-pre-geracao/43-04-SUMMARY.md` when done
</output>

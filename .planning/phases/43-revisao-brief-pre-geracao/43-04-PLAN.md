---
phase: 43-revisao-brief-pre-geracao
plan: 04
type: execute
wave: 4
depends_on: [43-03]
files_modified:
  - src/app/(app)/campanhas/nova/page.tsx
  - src/components/flow/campaign-page-client.tsx
  - src/components/flow/campaign-input-form.tsx
  - src/components/flow/campaign-brief-review.tsx
  - src/components/flow/store-identity-block.tsx
autonomous: true
requirements: [F43-25, F43-26]

must_haves:
  truths:
    - "BotÃ£o principal do form vira 'Revisar e gerar' (substitui 'Criar Campanha') â€” campaign-input-form.tsx"
    - "CampaignInputForm renderiza a tela de revisÃ£o do brief quando reviewMode ativo (mesmo padrÃ£o do isSubmitting â†’ GenerationProgress)"
    - "Tela de revisÃ£o (campaign-brief-review) mostra: loja/marca no topo (StoreIdentityBlock), seÃ§Ãµes Produto/Oferta/Imagens/Avisos/Custo, rÃ³tulos 'Principal'/'ReferÃªncia', 'Vai consumir X crÃ©dito(s)' + saldo, slot Tema reservado (nÃ£o renderiza)"
    - "A identidade exibida no topo da revisÃ£o Ã© o SNAPSHOT REAL resolvido no server page: `nova/page.tsx` resolve `resolveStoreIdentity(store)` (+ `validateIdentityReference`), propaga via CampaignPageClient â†’ CampaignInputForm â†’ tela de revisÃ£o â†’ `StoreIdentityBlock({ store, identity })` â€” mesma fonte canÃ´nica da geraÃ§Ã£o (decisÃ£o do usuÃ¡rio 2026-08-21)"
    - "AÃ§Ãµes 'Voltar e editar' e 'Confirmar e gerar campanha' com touch â‰¥ 44px, label/aria, loading; microcopy; preview das imagens sem recorte (object-contain, cÃ©lula aspect-square); seÃ§Ãµes empilham sem scroll horizontal (D7)"
    - "Estados: 'Preparando imagens...', desabilitaÃ§Ã£o durante a confirmaÃ§Ã£o, erro de preparaÃ§Ã£o claro"
  artifacts:
    - path: "src/components/flow/campaign-brief-review.tsx"
      provides: "Componente de revisÃ£o do brief"
      contains: "Confirmar e gerar campanha"
---

<objective>
Criar a **tela de revisÃ£o do brief** (`campaign-brief-review`) no fluxo do form e integrÃ¡-la ao `CampaignInputForm`: o botÃ£o principal vira **"Revisar e gerar"** e, com `reviewMode` ativo (43-03), o form renderiza o resumo do brief em tela intermediÃ¡ria (nÃ£o modal). A revisÃ£o mostra **exatamente o payload final** (imagens comprimidas de `prepareCampaignImages`), com seÃ§Ãµes Produto/Oferta/Imagens/Avisos/Custo, loja/marca ativa, rÃ³tulos Principal/ReferÃªncia, "Vai consumir X crÃ©dito(s)" + saldo e slot Tema reservado. AÃ§Ãµes "Voltar e editar" e "Confirmar e gerar campanha" com a11y/mobile/microcopy (D6/D7).

Purpose: D6 (resumo completo e honesto) + D7 (a11y/mobile/microcopy).
Output: tela de revisÃ£o integrada ao fluxo do form.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-revisao-brief-pre-geracao/43-CONTEXT.md (D6/D7; estado real: botÃ£o "Criar Campanha" 605-631, FormContent 257, submitDisabled 284-289, "Saldo/Custo" 567-595, isSubmitting â†’ GenerationProgress 54-62; StoreIdentityBlock)
@openspec/changes/fase-43-revisao-brief-pre-geracao/specs/campaign-brief-review/spec.md (Requirement: ConteÃºdo do resumo do brief (seÃ§Ãµes + rÃ³tulos + custo + Tema) + A11y, mobile e microcopy da revisÃ£o)
@openspec/changes/fase-43-revisao-brief-pre-geracao/specs/campaign-input-ui/spec.md (Requirement: BotÃ£o principal vira 'Revisar e gerar')
@src/components/flow/campaign-input-form.tsx (estado atual)
@src/components/flow/store-identity-block.tsx (componente; retorna null sem identity nÃ£o-nulo)
@src/hooks/use-operation-costs.ts (costs.campaign_generation.{costCredits, enabled}; status)

<interfaces>
- `useOperationCosts` â†’ `{ costs, status, refetch }`; `costs.campaign_generation.{costCredits, enabled}`; `status: "loading"|"unavailable"|"loaded"`
- `buildValidityDisplayText(fields)` / `buildMandatoryArtworkText(...)` / `inferIntent(fields)` (derivados exibidos)
- `preparedImages` do `reviewState` (43-03)
- `StoreIdentityBlock({ store, identity })` â€” identidade resolvida no server page (`resolveStoreIdentity(store)` + `validateIdentityReference`), propagada por `CampaignPageClient` â†’ `CampaignInputForm` â†’ tela de revisÃ£o (decisÃ£o do usuÃ¡rio 2026-08-21)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BotÃ£o 'Revisar e gerar' + render da tela de revisÃ£o no CampaignInputForm</name>
  <files>src/components/flow/campaign-input-form.tsx</files>
  <read_first>
    - src/components/flow/campaign-input-form.tsx (botÃ£o "Criar Campanha" 605-631; isSubmitting â†’ GenerationProgress 54-62; FormContent 257)
    - openspec/changes/fase-43-revisao-brief-pre-geracao/specs/campaign-input-ui/spec.md (Requirement: BotÃ£o principal vira 'Revisar e gerar')
  </read_first>
  <action>
    1. No `CampaignInputForm`, quando `reviewMode` estiver ativo (do hook 43-03), renderizar a tela de revisÃ£o (`campaign-brief-review`) **em vez do formulÃ¡rio** (mesmo padrÃ£o de `isSubmitting` â†’ `GenerationProgress`). O estado de preparaÃ§Ã£o ("Preparando imagens...") e o erro de preparaÃ§Ã£o devem ser exibidos.
    2. No `FormContent` (ou onde estÃ¡ o botÃ£o principal, `:605-631`), trocar o rÃ³tulo **"Criar Campanha"** â†’ **"Revisar e gerar"**. O clique nÃ£o dispara o POST direto â€” chama `enterReview()` (43-03). Com form invÃ¡lido/sem imagem/custo off/indisponÃ­vel/saldo insuficiente, o botÃ£o fica bloqueado (mesma lÃ³gica `submitDisabled`, `:284-289`) â€” a revisÃ£o nÃ£o abre (Scenario: Custo off/indisponÃ­vel/saldo insuficiente bloqueia a entrada na revisÃ£o).
    3. Passar para a tela de revisÃ£o os dados necessÃ¡rios: `fields` (ou derivados), `preparedImages`, `reviewState`, `store`/identidade, custo/saldo (`useOperationCosts`), e callbacks `onBack` (exitReview) e `onConfirm` (confirmReview).
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\components\flow\campaign-input-form.tsx" -Pattern "Revisar e gerar|campaign-brief-review|enterReview"</automated>
  </verify>
  <acceptance_criteria>
    - O botÃ£o principal exibe "Revisar e gerar" (nÃ£o "Criar Campanha")
    - Com `reviewMode` ativo, o form renderiza a tela de revisÃ£o (nÃ£o o formulÃ¡rio)
    - Com form invÃ¡lido/sem imagem/custo off/indisponÃ­vel/saldo insuficiente, o botÃ£o fica bloqueado e a revisÃ£o nÃ£o abre
    - `npx vitest run` da suÃ­te existente passa (mocks de `CampaignImageUpload`/form co-migrados quando necessÃ¡rio; regressÃ£o em 43-14)
  </acceptance_criteria>
  <done>BotÃ£o 'Revisar e gerar' + render da revisÃ£o integrados ao CampaignInputForm</done>
</task>

<task type="auto">
  <name>Task 2: Componente campaign-brief-review â€” seÃ§Ãµes Produto/Oferta/Imagens/Avisos/Custo + loja/marca + rÃ³tulos + custo + slot Tema</name>
  <files>src/components/flow/campaign-brief-review.tsx</files>
  <read_first>
    - openspec/changes/fase-43-revisao-brief-pre-geracao/specs/campaign-brief-review/spec.md (Requirement: ConteÃºdo do resumo do brief (seÃ§Ãµes + rÃ³tulos + custo + Tema))
    - src/components/flow/store-identity-block.tsx (componente; props)
    - src/components/flow/campaign-input-form.tsx (padrÃµes de estilos/seÃ§Ãµes; "Saldo: X Â· Custo: Y" 567-595)
    - src/components/flow/use-campaign-form.ts (buildValidityDisplayText, buildMandatoryArtworkText, inferIntent)
  </read_first>
  <action>
    Criar `src/components/flow/campaign-brief-review.tsx` (componente client no fluxo do form) renderizando o resumo do brief:
    - **Topo:** loja/marca ativa via `StoreIdentityBlock` usando o **snapshot real de identidade** recebido por prop (resolvido no server page â€” decisÃ£o do usuÃ¡rio 2026-08-21). Se o snapshot for `null` (ex.: sem identidade resolvÃ­vel), o `StoreIdentityBlock` retorna null e o topo apenas nÃ£o renderiza o bloco â€” **sem** fallback visual "nome/segmento/cor do form" como caminho principal (evita divergÃªncia entre o que o usuÃ¡rio viu e o que a geraÃ§Ã£o usou).
    - **SeÃ§Ã£o Produto:** nome, descriÃ§Ã£o (se houver).
    - **SeÃ§Ã£o Oferta:** tipo de campanha (Oferta/Destaque/Exclusivo via `inferIntent`), selo (badge), preÃ§o original (se houver), preÃ§o com desconto, validade formatada (`buildValidityDisplayText`).
    - **SeÃ§Ã£o Imagens:** thumbnails do payload final (`preparedImages` de 43-03) com rÃ³tulos **"Principal"** (role primary) e **"ReferÃªncia"** (role reference), exibidas **sem recorte** (`object-contain` em cÃ©lula `aspect-square`).
    - **SeÃ§Ã£o Avisos:** checkbox "imagem meramente ilustrativa" (estado) + texto obrigatÃ³rio (`buildMandatoryArtworkText`).
    - **SeÃ§Ã£o Custo:** "Vai consumir X crÃ©dito(s)" + saldo atual (via `useOperationCosts`); botÃ£o "Confirmar e gerar campanha" bloqueado quando custo indisponÃ­vel/desativado/saldo insuficiente (mesma lÃ³gica `submitDisabled`).
    - **SeÃ§Ã£o Tema:** **slot opcional reservado** â€” nÃ£o renderiza enquanto `creativeContext.themeId` for null (hoje sempre); preparaÃ§Ã£o F44.
    - Microcopy: aviso "Revise textos, preÃ§os e imagens antes de publicar: a IA pode cometer erros."
    Usar os padrÃµes de estilo do form (design system). As seÃ§Ãµes devem empilhar em telas estreitas (sem scroll horizontal); thumbnails com grid.
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\components\flow\campaign-brief-review.tsx" -Pattern "Principal|ReferÃªncia|Vai consumir|Revise textos, preÃ§os e imagens"</automated>
  </verify>
  <acceptance_criteria>
    - `campaign-brief-review.tsx` existe e renderiza as seÃ§Ãµes Produto/Oferta/Imagens/Avisos/Custo com os valores do brief
    - Imagens rotuladas "Principal"/"ReferÃªncia"; preview `object-contain` em cÃ©lula `aspect-square`
    - "Vai consumir X crÃ©dito(s)" + saldo exibidos; "Confirmar" bloqueado com custo off/indisponÃ­vel/saldo insuficiente
    - Tema nÃ£o renderiza (themeId null); slot reservado
    - Aviso "Revise textos, preÃ§os e imagens antes de publicar: a IA pode cometer erros." presente
    - SeÃ§Ãµes empilham em telas estreitas (sem scroll horizontal)
  </acceptance_criteria>
  <done>Componente de revisÃ£o com seÃ§Ãµes, rÃ³tulos, custo, loja e slot Tema criado</done>
</task>

<task type="auto">
  <name>Task 3: AÃ§Ãµes Voltar e editar / Confirmar e gerar campanha com a11y/mobile/loading + estados</name>
  <files>src/components/flow/campaign-brief-review.tsx</files>
  <read_first>
    - openspec/changes/fase-43-revisao-brief-pre-geracao/specs/campaign-brief-review/spec.md (Requirement: A11y, mobile e microcopy da revisÃ£o + TransiÃ§Ãµes)
  </read_first>
  <action>
    Adicionar as aÃ§Ãµes da revisÃ£o:
    - **"Voltar e editar"** â†’ chama `exitReview()` (preserva tudo, D2); touch target â‰¥ 44px; `label`/`aria` PT-BR.
    - **"Confirmar e gerar campanha"** â†’ chama `confirmReview()` (trava o snapshot e dispara o submit, D2); com **loading** (padrÃ£o do form) e desabilitado durante a geraÃ§Ã£o/confirmaÃ§Ã£o; bloqueado quando custo off/indisponÃ­vel/saldo insuficiente.
    - Estados: "Preparando imagens..." (durante a preparaÃ§Ã£o, antes de exibir o resumo), desabilitaÃ§Ã£o durante a confirmaÃ§Ã£o (snapshot travado), erro de preparaÃ§Ã£o claro (PT-BR).
    - A11y: touch â‰¥ 44px, foco visÃ­vel, leitura com `label`/`aria`; PT-BR.
    - Mobile: botÃµes "Confirmar e gerar"/"Voltar e editar" sempre acessÃ­veis; sem scroll horizontal; preview sem recorte.
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\components\flow\campaign-brief-review.tsx" -Pattern "Voltar e editar|Confirmar e gerar campanha|Preparando imagens"</automated>
  </verify>
  <acceptance_criteria>
    - "Voltar e editar" e "Confirmar e gerar campanha" presentes com touch â‰¥ 44px e `label`/`aria` PT-BR
    - "Confirmar" com loading e desabilitado durante a geraÃ§Ã£o; bloqueado com custo off/indisponÃ­vel/saldo insuficiente
    - "Preparando imagens..." e erro de preparaÃ§Ã£o claro exibidos
    - SeÃ§Ãµes empilham sem scroll horizontal em telas estreitas (320px/375px)
  </acceptance_criteria>
  <done>AÃ§Ãµes e estados da revisÃ£o com a11y/mobile/loading implementados</done>
</task>

<task type="auto">
  <name>Task 4: Resolver a identidade da loja no server page e propagar ate a revisao (StoreIdentityBlock com snapshot real)</name>
  <files>src/app/(app)/campanhas/nova/page.tsx, src/components/flow/campaign-page-client.tsx, src/components/flow/campaign-input-form.tsx, src/components/flow/campaign-brief-review.tsx</files>
  <read_first>
    - src/app/(app)/campanhas/nova/page.tsx (resolve store via getCurrentStore e passa a CampaignPageClient; adicionar resolucao de identidade)
    - src/components/flow/campaign-page-client.tsx (recebe store, passa para CampaignInputForm)
    - src/lib/store-identity-service.ts (resolveStoreIdentity 27; validateIdentityReference 169)
    - src/components/flow/campaign-brief-review.tsx (StoreIdentityBlock)
  </read_first>
  <action>
    Propagar a identidade real da loja ate a revisao (decisao do usuario 2026-08-21):
    1. src/app/(app)/campanhas/nova/page.tsx: apos obter store (via getCurrentStore), resolver o snapshot de identidade com resolveStoreIdentity(store) e validar com validateIdentityReference(identity) (mesma fonte canonica da rota de geracao - precedente route.ts:270/280). Passar identity (e o store) como props para CampaignPageClient.
    2. src/components/flow/campaign-page-client.tsx: aceitar identity como prop e repassa-lo para CampaignInputForm.
    3. src/components/flow/campaign-input-form.tsx: aceitar identity e passa-lo para campaign-brief-review.
    4. src/components/flow/campaign-brief-review.tsx: usar StoreIdentityBlock({ store, identity }) no topo com o snapshot recebido. Se identity for null, o bloco nao renderiza (sem fallback visual divergente).
    Importante: manter a assinatura de CampaignPageClient compativel (props aditivas opcionais ou atualizacao dos call sites/tests do client). Se validateIdentityReference exigir tratamento de erro, seguir o mesmo padrao da rota de geracao (o snapshot continua utilizavel mesmo sem logo/assinatura - cai para text_only/cor/nome canonicamente).
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\app\(app)\campanhas\nova\page.tsx" -Pattern "resolveStoreIdentity|validateIdentityReference"; Select-String -LiteralPath "src\components\flow\campaign-brief-review.tsx" -Pattern "StoreIdentityBlock"</automated>
  </verify>
  <acceptance_criteria>
    - nova/page.tsx resolve e valida a identidade via resolveStoreIdentity/validateIdentityReference
    - identity propagado: CampaignPageClient -> CampaignInputForm -> campaign-brief-review
    - campaign-brief-review usa StoreIdentityBlock({ store, identity }) com o snapshot real (sem fallback divergente)
    - npx vitest run da suite existente passa (call sites/tests do client co-migrados)
  </acceptance_criteria>
  <done>Identidade real da loja resolvida no server e propagada ate a revisao</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI â†” payload | O resumo exibido na revisÃ£o deve refletir exatamente o body enviado â€” risco de divergÃªncia visual |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-04 | Tampering | campaign-brief-review.tsx | mitigate | RevisÃ£o consome os mesmos derivados (`buildValidityDisplayText`/`buildMandatoryArtworkText`/`inferIntent`) e `preparedImages` que o body via `buildCampaignGenerationBody` (D4) â€” teste 7 valida idempotÃªncia |
| T-43-04a | Information Disclosure | Imagens | accept | DataUrls exibidos apenas no browser do lojista; sem log |
| T-43-04b | UI Redress | Custo/saldo | mitigate | "Confirmar" bloqueado quando custo off/indisponÃ­vel/saldo insuficiente (mesma lÃ³gica `submitDisabled`), prevenindo confirmaÃ§Ã£o sem condiÃ§Ã£o de cobranÃ§a |
</threat_model>

<verification>
- BotÃ£o "Revisar e gerar" no form; render da revisÃ£o com reviewMode
- `campaign-brief-review.tsx` com seÃ§Ãµes Produto/Oferta/Imagens/Avisos/Custo + loja/marca + rÃ³tulos + custo + slot Tema
- AÃ§Ãµes com a11y/mobile/loading + estados
- Preview sem recorte (object-contain); seÃ§Ãµes empilham sem scroll horizontal
- `npx vitest run` da suÃ­te existente passa
</verification>

<success_criteria>
- Tela intermediÃ¡ria de revisÃ£o (nÃ£o modal) integrada ao fluxo do form (D2/D6)
- Resumo completo e honesto (D6); a11y/mobile/microcopy (D7)
- BotÃ£o "Revisar e gerar" substitui "Criar Campanha"
</success_criteria>

<output>
Create `.planning/phases/43-revisao-brief-pre-geracao/43-04-SUMMARY.md` when done
</output>

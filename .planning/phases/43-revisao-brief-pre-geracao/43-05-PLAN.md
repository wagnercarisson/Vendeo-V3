---
phase: 43-revisao-brief-pre-geracao
plan: 05
type: execute
wave: 2
depends_on: [43-01]
files_modified:
  - src/lib/image-generation/schema.ts
  - src/lib/image-generation/services/input-validation-service.ts
autonomous: true
requirements: [F43-11, F43-12, F43-13]

must_haves:
  truths:
    - "GenerateImageRequestSchema.inputValidationOverride.productImageCheck aceita o novo literal 'brief_review_confirmed' via z.union([user_confirmed_continue, brief_review_confirmed]).optional() (schema.ts:59-63), .strict() preservado (D5)"
    - "ValidationContext.overrides.productImageCheck aceita 'brief_review_confirmed' | 'user_confirmed_continue' (schema.ts:193) (D5)"
    - "InputValidationService.validate aceita o novo literal no tipo do override; lógica de skip inalterada (já pula para override truthy, :47-49) (D5)"
  artifacts:
    - path: "src/lib/image-generation/schema.ts"
      provides: "Literal brief_review_confirmed no override"
      contains: "brief_review_confirmed"
---

<objective>
Adicionar o novo literal **`brief_review_confirmed`** ao schema de transporte e ao tipo do override de validação (F43 D5): `GenerateImageRequestSchema.inputValidationOverride.productImageCheck` passa a aceitar `"brief_review_confirmed"` além de `"user_confirmed_continue"` (`.strict()` preservado); `ValidationContext.overrides.productImageCheck` aceita ambos; `InputValidationService.validate` aceita o novo literal no tipo do override (a lógica de skip já é por override truthy, sem mudança de comportamento). Semântica distinta de `user_confirmed_continue` documentada no schema.

Purpose: D5 (override com semântica distinta; capacidade preservada).
Output: schema + tipos aceitam `brief_review_confirmed`.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-revisao-brief-pre-geracao/43-CONTEXT.md (D5; estado real: schema.ts:59-63/64/193; input-validation-service.ts:40-49)
@openspec/changes/fase-43-revisao-brief-pre-geracao/specs/ai-image-generation/spec.md (Requirement: GenerateImageRequestSchema with campaignIntent e preserveImageContext — novo literal + matriz de semântica)
@openspec/changes/fase-43-revisao-brief-pre-geracao/specs/validation-review-alignment/spec.md (Requirement: ValidationContext type defined + validate com override)
@src/lib/image-generation/schema.ts (GenerateImageRequestSchema 8-64; inputValidationOverride 59-63; .strict() 64; ValidationContext.overrides 192-194)
@src/lib/image-generation/services/input-validation-service.ts (validate 40-49)

<interfaces>
- `GenerateImageRequestSchema` (`schema.ts:8-64`) — `.strict()`
- `inputValidationOverride.productImageCheck` (`schema.ts:59-63`) — hoje `z.literal("user_confirmed_continue").optional()`
- `ValidationContext.overrides.productImageCheck` (`schema.ts:192-194`) — hoje `"user_confirmed_continue"`
- `InputValidationService.validate(name, dataUrl, override?, onCall)` (`:40-49`) — skip por override truthy em `:47-49`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema — literal brief_review_confirmed no override (z.union, .strict preservado)</name>
  <files>src/lib/image-generation/schema.ts</files>
  <read_first>
    - src/lib/image-generation/schema.ts (GenerateImageRequestSchema 8-64; inputValidationOverride 59-63; .strict() 64)
    - openspec/changes/fase-43-revisao-brief-pre-geracao/specs/ai-image-generation/spec.md (Requirement: GenerateImageRequestSchema ... — bloco de código do override + matriz de semântica)
  </read_first>
  <action>
    Alterar `inputValidationOverride.productImageCheck` em `GenerateImageRequestSchema` (`schema.ts:59-63`) de `z.literal("user_confirmed_continue").optional()` para:
    ```ts
    productImageCheck: z
      .union([
        z.literal("user_confirmed_continue"), // 409 + insistiu (comportamento atual)
        z.literal("brief_review_confirmed"),  // NOVO — revisou o brief e confirmou (D5)
      ])
      .optional(),
    ```
    **Preservar** o `.strict()` do schema (`:64`). Adicionar a **matriz de semântica** (brief_review_confirmed / user_confirmed_continue / sem override) como comentário no schema, conforme a spec `ai-image-generation` (linhas 44-50).
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\lib\image-generation\schema.ts" -Pattern "brief_review_confirmed" | Select-Object -First 3</automated>
  </verify>
  <acceptance_criteria>
    - `schema.ts` contém `z.literal("brief_review_confirmed")` no `productImageCheck`
    - `z.union([...user_confirmed_continue..., ...brief_review_confirmed...]).optional()` aplicado
    - `.strict()` preservado no `GenerateImageRequestSchema`
    - Valor desconhecido do override ainda é rejeitado (`.strict()`/enum)
    - `npx vitest run` da suíte de schema continua passando (teste 17 coberto em 43-12)
  </acceptance_criteria>
  <done>Schema aceita brief_review_confirmed com .strict preservado</done>
</task>

<task type="auto">
  <name>Task 2: ValidationContext.overrides.productImageCheck aceita ambos literais</name>
  <files>src/lib/image-generation/schema.ts</files>
  <read_first>
    - src/lib/image-generation/schema.ts (ValidationContext 181-195; overrides 192-194)
    - openspec/changes/fase-43-revisao-brief-pre-geracao/specs/validation-review-alignment/spec.md (Requirement: ValidationContext type defined)
  </read_first>
  <action>
    Alterar `ValidationContext.overrides.productImageCheck` (`schema.ts:192-194`) de `"user_confirmed_continue"` para:
    ```ts
    overrides?: {
      productImageCheck?: "user_confirmed_continue" | "brief_review_confirmed";
    };
    ```
    Preservar os demais campos de `ValidationContext` (`inputCorrection?`, `allowedConflicts?`).
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\lib\image-generation\schema.ts" -Pattern 'productImageCheck\?: "user_confirmed_continue" \| "brief_review_confirmed"'</automated>
  </verify>
  <acceptance_criteria>
    - `ValidationContext.overrides.productImageCheck` tipado como `"user_confirmed_continue" | "brief_review_confirmed"`
    - Demais campos de `ValidationContext` inalterados
    - `npx vitest run` da suíte existente passa
  </acceptance_criteria>
  <done>ValidationContext.overrides aceita ambos literais</done>
</task>

<task type="auto">
  <name>Task 3: InputValidationService.validate — tipo do override aceita o novo literal</name>
  <files>src/lib/image-generation/services/input-validation-service.ts</files>
  <read_first>
    - src/lib/image-generation/services/input-validation-service.ts (validate 40-49)
    - openspec/changes/fase-43-revisao-brief-pre-geracao/specs/validation-review-alignment/spec.md (Requirement: ValidationContext type defined — Delta F43)
  </read_first>
  <action>
    Alterar o tipo do parâmetro `override` do `validate` (`:40-49`) para aceitar o novo literal:
    ```ts
    override?: {
      productImageCheck?: "user_confirmed_continue" | "brief_review_confirmed";
    },
    ```
    **NÃO** alterar a lógica de skip (`:47-49`) — já pula para qualquer override truthy (o `if (override?.productImageCheck)` cobre ambos os literais). Sem mudança de comportamento.
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\lib\image-generation\services\input-validation-service.ts" -Pattern 'brief_review_confirmed'</automated>
  </verify>
  <acceptance_criteria>
    - `InputValidationService.validate` aceita `productImageCheck?: "user_confirmed_continue" | "brief_review_confirmed"` no override
    - Lógica de skip inalterada (pula para override truthy)
    - `npx vitest run` da suíte existente passa
  </acceptance_criteria>
  <done>InputValidationService.validate aceita o novo literal sem mudança de lógica</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transporte ↔ validação | O override chega do cliente e é interpretado pelo backend — risco de valor inválido contornar o .strict() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-05 | Tampering | schema.ts | mitigate | `.strict()` preservado + `z.union` de literais rejeita qualquer valor fora de `user_confirmed_continue`/`brief_review_confirmed` (teste 17); matriz de semântica documentada |
| T-43-05a | Spoofing | override do cliente | accept | Override legítimo indica confirmação humana; capacidade de validação preservada + flag admin reativa (D5) |
</threat_model>

<verification>
- `productImageCheck` aceita `brief_review_confirmed` via `z.union`; `.strict()` preservado
- `ValidationContext.overrides.productImageCheck` tipado com ambos literais
- `InputValidationService.validate` aceita o novo literal; lógica de skip inalterada
- `npx vitest run` da suíte existente passa
</verification>

<success_criteria>
- Schema e tipos aceitam `brief_review_confirmed` com `.strict()` preservado (D5)
- Capacidade `InputValidationService` intacta; sem mudança de lógica de skip
- Matriz de semântica documentada no schema
</success_criteria>

<output>
Create `.planning/phases/43-revisao-brief-pre-geracao/43-05-SUMMARY.md` when done
</output>

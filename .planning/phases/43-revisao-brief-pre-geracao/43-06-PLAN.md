---
phase: 43-revisao-brief-pre-geracao
plan: 06
type: execute
wave: 3
depends_on: [43-05]
files_modified:
  - src/lib/image-generation/services/image-generation-service.ts
  - src/components/flow/generation-progress.tsx
autonomous: true
requirements: [F43-14, F43-15, F43-16]

must_haves:
  truths:
    - "ImageGenerationService Phase 1 (input_validation) emite obrigatoriamente status 'skipped' (via emitSkipped, :141) quando o override (brief_review_confirmed OU user_confirmed_continue) pula a IA — nunca running→complete nem complete com detail sem chamada real (D5)"
    - "Nenhum evento de métrica input_validation sem chamada real (validationCallMade já garante, :188-190) (D5)"
    - "GenerationProgress trata status 'skipped' na fase input_validation no indicador principal — exibe estado de skip, nunca 'Validação concluída' falsa (D5)"
  artifacts:
    - path: "src/lib/image-generation/services/image-generation-service.ts"
      provides: "Fase input_validation emitida como skipped quando override pula"
      contains: "emitSkipped"
    - path: "src/components/flow/generation-progress.tsx"
      provides: "Indicador skipped para a fase input_validation"
      contains: "skipped"
---

<objective>
Alinhar o backend do progresso à confirmação humana da F43 (D5): o `ImageGenerationService` Phase 1 (`input_validation`) passa a emitir a fase com **obrigatoriamente `status: "skipped"`** (via `emitSkipped`, que já existe em `:141` mas nunca é invocado) quando o override (`brief_review_confirmed` OU `user_confirmed_continue`) pula a chamada de IA — **nunca** `running → complete` nem `complete` com detail sem chamada real. O `GenerationProgress` passa a exibir a fase `input_validation` como `skipped` no indicador principal (hoje só o painel colapsável trata `skipped`).

Purpose: D5 (fase `input_validation` como `skipped`/ "Brief confirmado pelo usuário"; nunca fase falsa de "Validação concluída" sem IA).
Output: serviço emite skipped; UI trata skipped.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-revisao-brief-pre-geracao/43-CONTEXT.md (D5; estado real: image-generation-service.ts 162-277, emitHuman 163, emitSkipped 141, validate call 173-183, validationCallMade 188-190, emitComplete 271-277; generation-progress.tsx 15/69/86-103/176-185)
@openspec/changes/fase-43-revisao-brief-pre-geracao/specs/validation-review-alignment/spec.md (Requirement: Fase input_validation emitida como skipped quando override pula)
@openspec/changes/fase-43-revisao-brief-pre-geracao/specs/generation-progress/spec.md (Requirement: Fase input_validation exibida como skipped quando pulada + GenerationPhaseStatus já suporta skipped)
@src/lib/image-generation/services/image-generation-service.ts (Phase 1 162-277; emitSkipped 141)
@src/components/flow/generation-progress.tsx (PHASE_LABELS 15; phaseOrder 69; indicador principal 86-103; painel colapsável 176-185)

<interfaces>
- `ImageGenerationService.generateImage` Phase 1 (`:162-277`): `emitHuman("input_validation")` (`:163`), `this.inputValidation.validate(brief.product.name, primaryImageDataUrl, context.campaignInput.inputValidationOverride, onCall)` (`:173-183`), `validationCallMade` (`:188-190`), `emitComplete`/detail (`:271-277`)
- `emitSkipped = (phase: GenerationPhase) => emit(phase, "skipped")` (`:141`) — existe, não é invocado
- `GenerationPhaseStatus` inclui `"skipped"` (schema.ts:143)
- `GenerationProgress`: `PHASE_LABELS` (15), `phaseOrder` (69), indicador principal (86-103, sem ramo skipped), painel colapsável (176-185, skipped em 181)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ImageGenerationService Phase 1 — emitir skipped quando override pula</name>
  <files>src/lib/image-generation/services/image-generation-service.ts</files>
  <read_first>
    - src/lib/image-generation/services/image-generation-service.ts (Phase 1 162-277; emitSkipped 141; validate call 173-183; validationCallMade 188-190; emitComplete 271-277)
    - openspec/changes/fase-43-revisao-brief-pre-geracao/specs/validation-review-alignment/spec.md (Requirement: Fase input_validation emitida como skipped quando override pula)
  </read_first>
  <action>
    Na Phase 1 `input_validation` do `ImageGenerationService.generateImage` (`:162-277`), detectar quando o override presente **pula a IA de visão** e, nesse caso, emitir a fase como **`skipped`** em vez de `running → complete`:
    - Detectar se `context.campaignInput.inputValidationOverride?.productImageCheck` é truthy (qualquer um dos dois literais: `"brief_review_confirmed"` OU `"user_confirmed_continue"`).
    - Se o override pular a IA: **NÃO** emitir `emitHuman("input_validation")` (`:163`) como `running` nem `emitComplete`/detail (`:271-277`) — chamar `emitSkipped("input_validation")` (`:141`) com detail/mensagem **opcional** "Brief confirmado pelo usuário" (para `brief_review_confirmed`) ou "Validação dispensada" (para `user_confirmed_continue`). **Nunca** emitir `complete` com detail (isso reintroduziria a fase falsa de "Validação concluída" sem chamada de IA).
    - O `validationCallMade` (`:188-190`) já impede o evento de métrica `input_validation` sem chamada real — garantir que nenhum `emitMetricsEvent("input_validation", ...)` ocorra nesse caminho (comportamento já garantido; regressão coberta).
    - O caminho **sem override** continua emitindo `running → complete` normalmente (validação IA roda como rede de segurança).
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\lib\image-generation\services\image-generation-service.ts" -Pattern "emitSkipped\(\"input_validation\"\)|emitSkipped"</automated>
  </verify>
  <acceptance_criteria>
    - Com override (`brief_review_confirmed` OU `user_confirmed_continue`), a fase `input_validation` é emitida com `status: "skipped"` (via `emitSkipped`), com detail opcional "Brief confirmado pelo usuário"/"Validação dispensada"
    - Nenhuma fase `complete` falsa é emitida sem chamada real (nunca `running → complete` nem `complete` com detail no caminho de override)
    - Nenhum evento de métrica `input_validation` sem chamada real (validationCallMade garante)
    - Caminho sem override: `running → complete` normal (regressão)
    - `npx vitest run` — suíte de `image-generation-service` passa (teste 23 coberto em 43-12; fixtures co-migradas em 43-14)
  </acceptance_criteria>
  <done>ImageGenerationService emite input_validation como skipped quando override pula</done>
</task>

<task type="auto">
  <name>Task 2: GenerationProgress — tratar status skipped na fase input_validation (indicador principal)</name>
  <files>src/components/flow/generation-progress.tsx</files>
  <read_first>
    - src/components/flow/generation-progress.tsx (indicador principal 86-103; painel colapsável 176-185; PHASE_LABELS 15; phaseOrder 69)
    - openspec/changes/fase-43-revisao-brief-pre-geracao/specs/generation-progress/spec.md (Requirement: Fase input_validation exibida como skipped quando pulada)
  </read_first>
  <action>
    No `GenerationProgress`, tratar `status: "skipped"` para a fase `input_validation` **no indicador principal** (`:86-103`):
    - Adicionar um ramo explícito para `status === "skipped"` que exiba o estado de skip (ex.: ícone neutro/pulado, sem o check de "validação concluída"), com a mensagem/descrição refletindo que a validação foi **pulada** ("Brief confirmado pelo usuário"/"Validação dispensada") — sem sugerir que uma IA validou.
    - O comportamento de `status === "complete"` (validação real rodou) é preservado.
    - A fase `input_validation` nunca é exibida como `complete` sem uma chamada de IA real correspondente (o serviço emite `skipped` quando o override pula — Task 1).
    - O painel colapsável (`:181`) já trata `skipped` — manter; garantir consistência de rótulo/mensagem.
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\components\flow\generation-progress.tsx" -Pattern 'item.status === "skipped"|status === "skipped"'</automated>
  </verify>
  <acceptance_criteria>
    - O indicador principal da fase `input_validation` exibe estado `skipped` quando o evento chega com `status: "skipped"` (não como `complete`/check)
    - A mensagem reflete "Brief confirmado pelo usuário"/"Validação dispensada" — nunca "Validação concluída"
    - `status === "complete"` (validação real) preserva o comportamento atual
    - `npx vitest run` da suíte existente passa
  </acceptance_criteria>
  <done>GenerationProgress exibe a fase input_validation como skipped no indicador principal</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| serviço → progresso | Eventos de fase emitidos pelo serviço são exibidos ao usuário — risco de fase falsa de "validação concluída" sem chamada real |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-06 | Spoofing | image-generation-service.ts | mitigate | Fase `input_validation` emitida **obrigatoriamente** `skipped` (via `emitSkipped`) quando o override pula — nunca `complete` falso (teste 23); `validationCallMade` impede evento de métrica sem chamada real |
| T-43-06a | Information Disclosure | GenerationProgress | accept | Mensagens de fase são textos fixos PT-BR; sem dados sensíveis |
</threat_model>

<verification>
- `image-generation-service.ts` chama `emitSkipped("input_validation")` quando override pula (detail opcional)
- Nenhum `complete` falso no caminho de override; sem evento de métrica sem chamada real
- `generation-progress.tsx` trata `status === "skipped"` no indicador principal da fase `input_validation`
- `npx vitest run` da suíte existente passa
</verification>

<success_criteria>
- Fase `input_validation` emitida como `skipped`/"Brief confirmado pelo usuário" quando o override pula (D5)
- `GenerationProgress` exibe skipped no indicador principal — nunca "Validação concluída" falsa
- Capacidade de validação preservada no caminho sem override
</success_criteria>

<output>
Create `.planning/phases/43-revisao-brief-pre-geracao/43-06-SUMMARY.md` when done
</output>

---
phase: 43-revisao-brief-pre-geracao
plan: 07
type: execute
wave: 2
depends_on: [43-01]
files_modified:
  - supabase/migrations/20260821000001_f43_create_feature_flags.sql
autonomous: false
requirements: [F43-17, F43-18, F43-19]

must_haves:
  truths:
    - "Tabela `feature_flags` criada (greenfield) com `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`, `key TEXT UNIQUE NOT NULL`, `enabled BOOLEAN NOT NULL DEFAULT false`, `description TEXT`, `updated_by UUID NULL REFERENCES auth.users(id)`, `updated_at TIMESTAMPTZ DEFAULT now()` (D5)"
    - "Seed da flag `force_brief_vision_check` (`enabled=false`, descrição administrativa) (D5)"
    - "RPC `admin_update_feature_flag` idempotente/atômico: valida key/enabled/reason obrigatório, atualiza enabled/updated_by/updated_at e registra auditoria na mesma transação — target_id = feature_flags.id (UUID NOT NULL existente) + metadata.key (D5)"
    - "CHECKs de `admin_audit_log` estendidos: nova action 'feature_flag_update' + novo target_type 'feature_flag' via DROP/ADD preservando valores existentes (D5)"
    - "Migration idempotente e não destrutiva; [BLOCKING] supabase db push no remoto"
  artifacts:
    - path: "supabase/migrations/20260821000001_f43_create_feature_flags.sql"
      provides: "Tabela feature_flags + seed + RPC + CHECKs de auditoria"
      contains: "force_brief_vision_check"
---

<objective>
Criar a **migration da flag administrativa** da F43 (D5): tabela `feature_flags` (greenfield), seed da flag `force_brief_vision_check` (`enabled=false`), RPC `admin_update_feature_flag` (motivo obrigatório, transacional, auditoria na mesma transação) e extensão dos CHECKs de `admin_audit_log` (nova action `feature_flag_update` + novo `target_type` `feature_flag`). **Ordem de deploy (D5):** esta migration deve ser aplicada **antes** do código que a consome (43-08 rota, 43-09 admin). **Push `[BLOCKING]` no remoto** (db push).

Purpose: D5 (flag administrativa mínima em banco, NÃO env var; auditoria respeitando constraints existentes).
Output: migration aplicada (local + remoto).
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-revisao-brief-pre-geracao/43-CONTEXT.md (D5; estado real: admin_audit_log.target_id UUID NOT NULL em 20260718000001_create_admin_tables.sql:25; CHECKs atuais em 20260810010000_create_access_requests.sql:146-163; feature_flags NÃO existe; precedente RPC transacional admin_review_access_request 20260810010000_create_access_requests.sql:67-136)
@openspec/changes/fase-43-revisao-brief-pre-geracao/specs/feature-flag-control/spec.md (Requirement: Tabela feature_flags com colunas mínimas + Mutação da flag com motivo obrigatório e auditoria)
@openspec/changes/fase-43-revisao-brief-pre-geracao/design.md (Migration Plan)
@supabase/migrations/20260810010000_create_access_requests.sql (RPC transacional 67-136; DROP/ADD de CHECKs 146-163)
@supabase/migrations/20260718000001_create_admin_tables.sql (admin_audit_log: target_id UUID NOT NULL 25; CHECKs iniciais 20-30)

<interfaces>
- `admin_audit_log` (`20260718000001_create_admin_tables.sql:20-30`): `action`/`target_type` CHECKs; `target_id UUID NOT NULL` (`:25`); `metadata` JSONB
- CHECKs atuais de `admin_audit_log` (`20260810010000_create_access_requests.sql:146-163`): action IN (credit_grant, credit_adjustment, store_create_invite, manual_refund, approve_verification, reject_verification, create_test_store, admin_exception, reveal_cnpj, access_request_approve, access_request_reject); target_type IN (store, user, campaign, access_request)
- Precedente RPC transacional: `admin_review_access_request` (`20260810010000_create_access_requests.sql:67-136`) — valida, persiste, registra auditoria na mesma transação
- Convenção de nome de migration: `YYYYMMDDHHMMSS_descricao.sql` — a F43 usará `20260821000001_f43_create_feature_flags.sql`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration — tabela feature_flags + seed + RPC + CHECKs (idempotente, não destrutiva)</name>
  <files>supabase/migrations/20260821000001_f43_create_feature_flags.sql</files>
  <read_first>
    - openspec/changes/fase-43-revisao-brief-pre-geracao/specs/feature-flag-control/spec.md (Requirement: Tabela feature_flags com colunas mínimas + Mutação da flag)
    - supabase/migrations/20260810010000_create_access_requests.sql (RPC transacional 67-136 + DROP/ADD de CHECKs 146-163)
    - supabase/migrations/20260718000001_create_admin_tables.sql (admin_audit_log 20-30; target_id UUID NOT NULL 25)
  </read_first>
  <action>
    Criar `supabase/migrations/20260821000001_f43_create_feature_flags.sql` (idempotente, não destrutiva):
    1. **Tabela `feature_flags`** (`IF NOT EXISTS`):
       ```sql
       CREATE TABLE IF NOT EXISTS public.feature_flags (
         id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
         key TEXT UNIQUE NOT NULL,
         enabled BOOLEAN NOT NULL DEFAULT false,
         description TEXT,
         updated_by UUID NULL REFERENCES auth.users(id),
         updated_at TIMESTAMPTZ DEFAULT now()
       );
       ALTER TABLE public.feature_flags ENABLE ROW LEVEL SECURITY;
       ```
       + grants de service_role (padrão das tabelas administrativas) — sem RLS policy exposta a authenticated (leitura via serviço dedicado, precedente `OperationCostService`).
    2. **Seed** da flag `force_brief_vision_check` (`enabled=false`, descrição administrativa do alinhamento): `INSERT ... ON CONFLICT (key) DO NOTHING` (idempotente). Descrição: "Quando ligada, o Vendeo executa novamente a validação por IA das imagens mesmo depois da revisão humana do brief. Use apenas para diagnóstico, auditoria ou se houver suspeita de que campanhas problemáticas estão passando pela revisão humana."
    3. **RPC `admin_update_feature_flag`** (idempotente/atômico, precedente `admin_review_access_request`): parâmetros `p_key TEXT`, `p_enabled BOOLEAN`, `p_reason TEXT`, `p_actor_id UUID`, `p_operation_id UUID` — valida `p_key`/`p_reason` (motivo **obrigatório**, não vazio), atualiza `enabled`/`updated_by`/`updated_at` e registra auditoria na **mesma transação** em `admin_audit_log` com `action: 'feature_flag_update'`, `target_type: 'feature_flag'`, `target_id = feature_flags.id` (UUID), `metadata` contendo `key`, `old_value`, `new_value`, `reason` (+ `operation_id` para idempotência). Retornar o `id`/linha atualizada.
    4. **Estender CHECKs de `admin_audit_log`** (padrão DROP/ADD, precedente `:146-163`): nova action `feature_flag_update` e novo `target_type` `feature_flag` — `ALTER TABLE ... DROP CONSTRAINT IF EXISTS / ADD CONSTRAINT` preservando os valores existentes.
    5. **REVERT** section (padrão das migrations do repo).
  </action>
  <verify>
    <automated>
      $f = 'supabase\migrations\20260821000001_f43_create_feature_flags.sql'; if (-not (Test-Path $f)) { throw 'Migration F43 nao existe' }; Select-String -LiteralPath $f -Pattern 'CREATE TABLE.*feature_flags|force_brief_vision_check|admin_update_feature_flag|feature_flag_update|feature_flag' | Measure-Object | ForEach-Object { if ($_.Count -lt 5) { throw 'Marcadores essenciais ausentes' } }; Write-Output 'OK: migration feature_flags completa'
    </automated>
  </verify>
  <acceptance_criteria>
    - `20260821000001_f43_create_feature_flags.sql` existe com `CREATE TABLE ... feature_flags` (id UUID PK, key UNIQUE, enabled default false, description, updated_by, updated_at)
    - Seed de `force_brief_vision_check` (enabled=false) via `ON CONFLICT DO NOTHING`
    - RPC `admin_update_feature_flag` valida motivo obrigatório, atualiza e audita na mesma transação (action `feature_flag_update`, target_type `feature_flag`, target_id = feature_flags.id, metadata key/old_value/new_value/reason)
    - CHECKs de `admin_audit_log` estendidos via DROP/ADD preservando valores existentes
    - Idempotente e não destrutiva; REVERT presente
  </acceptance_criteria>
  <done>Migration da flag criada (feature_flags + seed + RPC + CHECKs)</done>
</task>

<task type="auto">
  <name>Task 2: Aplicar migration no local + push [BLOCKING] no remoto</name>
  <files>supabase/migrations/20260821000001_f43_create_feature_flags.sql</files>
  <read_first>
    - .planning/phases/43-revisao-brief-pre-geracao/43-CONTEXT.md (D5 — ordem de deploy: migration antes do código; push [BLOCKING])
    - .planning/phases/38-1-ai-cost-accounting/38-1-01-SUMMARY.md (precedente de db push [BLOCKING])
  </read_first>
  <action>
    Aplicar a migration:
    1. **Local:** rodar `supabase db reset` ou aplicar a migration no banco local (se o ambiente local estiver disponível). Se o Docker/Supabase local estiver indisponível, documentar no SUMMARY (precedente F42 — Docker local indisponível; validação via `supabase status`/remoto).
    2. **Remoto [BLOCKING]:** executar `supabase db push` (requer `SUPABASE_ACCESS_TOKEN` e projeto linkado `gvbzwihwgzujwsviufgy`). Se o token estiver ausente, **registrar como [BLOCKING] DEFERIDO** e documentar como ação crítica — o código (43-08/43-09) só é utilizável após a migration aplicada; o fallback de leitura (`enabled=false`) segura a geração em ambientes sem a tabela.
    3. Registrar o status do push (aplicado / deferido com motivo) no SUMMARY.
  </action>
  <verify>
    <automated>Select-String -LiteralPath ".planning\phases\43-revisao-brief-pre-geracao\43-07-SUMMARY.md" -Pattern "db push|push \[BLOCKING\]|aplicada|deferido"</automated>
  </verify>
  <acceptance_criteria>
    - Migration aplicada no local (se ambiente disponível) OU documentada a indisponibilidade
    - `supabase db push` executado no remoto E aplicado, OU registrado como [BLOCKING] DEFERIDO com motivo (token ausente) e ação crítica documentada
    - SUMMARY registra o status do push
  </acceptance_criteria>
  <done>Migration aplicada (local + remoto [BLOCKING]) e status registrado</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin → feature_flags | Mutação da flag via RPC/admin route — risco de alteração sem rastreio ou valor inválido quebrando constraints |
| feature_flags → geração | Leitura da flag no backend de geração — risco de derrubar a geração em falha de leitura |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-07 | Tampering | feature_flags migration | mitigate | RPC `admin_update_feature_flag` com motivo obrigatório + auditoria na mesma transação (action/target_type novos via DROP/ADD); `metadata { key, old_value, new_value, reason }`; `target_id = feature_flags.id` (UUID) atendendo ao `target_id UUID NOT NULL` |
| T-43-07a | Denial of Service | feature_flags leitura | mitigate | Fallback de leitura `enabled=false` (43-08) — falha na leitura não derruba a geração; env var emergencial só para forçar true |
| T-43-07b | Elevation of Privilege | RPC | mitigate | RPC protegido (padrão service_role/admin); requireAdmin na rota admin (43-09) |
</threat_model>

<verification>
- Migration `20260821000001_f43_create_feature_flags.sql` com feature_flags + seed + RPC + CHECKs
- RPC valida motivo obrigatório, atualiza e audita na mesma transação (target_id = feature_flags.id, metadata key/old_value/new_value/reason)
- CHECKs de admin_audit_log estendidos (feature_flag_update / feature_flag) via DROP/ADD preservando existentes
- Idempotente; REVERT presente
- db push local/remoto aplicado OU registrado como [BLOCKING] DEFERIDO
</verification>

<success_criteria>
- Tabela `feature_flags` (greenfield) criada com colunas mínimas + seed `force_brief_vision_check` (enabled=false) (D5)
- RPC transacional com motivo obrigatório + auditoria (action/target_type novos) (D5)
- Migration aplicada antes do código consumidor (ordem fail-closed); push [BLOCKING] no remoto
</success_criteria>

<output>
Create `.planning/phases/43-revisao-brief-pre-geracao/43-07-SUMMARY.md` when done
</output>

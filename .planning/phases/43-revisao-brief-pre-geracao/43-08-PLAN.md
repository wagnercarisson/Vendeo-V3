---
phase: 43-revisao-brief-pre-geracao
plan: 08
type: execute
wave: 4
depends_on: [43-06, 43-07]
files_modified:
  - src/app/api/campaign/generate-image/route.ts
  - src/lib/feature-flags/feature-flag-service.ts
autonomous: true
requirements: [F43-20, F43-21]

must_haves:
  truths:
    - "Serviço de leitura da flag `force_brief_vision_check` (server-only, padrão OperationCostService/EconomicParameterService) retorna `enabled`; fallback de leitura → false (não derruba geração, log warning); env var VENDEO_FORCE_BRIEF_VISION_CHECK=true como fail-safe emergencial (D5)"
    - "Route.ts: com a flag ligada, normaliza um `effectiveParsedData`/`effectiveCampaignInput` (remove `brief_review_confirmed` do `inputValidationOverride`) ANTES da checagem pré-stream (route.ts:338) e usa o mesmo input para a checagem, o brief e `generateImage`; `user_confirmed_continue` NUNCA é removido (D5)"
    - "Com a flag desligada, `brief_review_confirmed` pula nos dois pontos (rota pré-stream + Phase 1 do serviço) sem mudança de lógica (D5)"
  artifacts:
    - path: "src/lib/feature-flags/feature-flag-service.ts"
      provides: "Serviço de leitura da flag com fallback"
      contains: "force_brief_vision_check"
    - path: "src/app/api/campaign/generate-image/route.ts"
      provides: "Normalização ponta a ponta da flag"
      contains: "effectiveCampaignInput"
---

<objective>
Implementar o **serviço de leitura da flag** `force_brief_vision_check` (server-only, padrão `OperationCostService`/`EconomicParameterService`, com fallback `enabled=false` que não derruba a geração) e a **normalização ponta a ponta na rota** `generate-image`: com a flag **ligada**, a rota normaliza um `effectiveParsedData`/`effectiveCampaignInput` (remove `brief_review_confirmed` do `inputValidationOverride`) **antes** da checagem pré-stream (`route.ts:338`) e usa o **mesmo input normalizado** para a checagem, construir o brief e chamar `generateImage` — fazendo pré-stream e Phase 1 do serviço validarem a IA de visão. `user_confirmed_continue` **nunca é removido**. Com a flag **desligada**, `brief_review_confirmed` pula (comportamento padrão da F43, sem mudança de lógica).

Purpose: D5 (capacidade preservada + flag administrativa que reativa a validação IA ponta a ponta, sem redeploy).
Output: serviço de leitura da flag + rota com normalização.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-revisao-brief-pre-geracao/43-CONTEXT.md (D5; estado real: route.ts gate 338, createCampaign 401-445, reserveCredit 447-476, resolveStoreIdentity 270, validateIdentityReference 280, buildCampaignBrief 283-284, generateImage 686)
@openspec/changes/fase-43-revisao-brief-pre-geracao/specs/ai-image-generation/spec.md (Requirement: POST /api/campaign/generate-image endpoint — skip + normalização ponta a ponta)
@openspec/changes/fase-43-revisao-brief-pre-geracao/specs/feature-flag-control/spec.md (Requirement: Leitura da flag com fallback que não derruba geração)
@src/app/api/campaign/generate-image/route.ts (gate 338; createCampaign 401-445; reserveCredit 447-476; buildCampaignBrief 283-284; generateImage 686)
@src/lib/feature-flags/feature-flag-service.ts (novo)

<interfaces>
- Rota pré-stream (`route.ts:337-399`): gate `if (!parsed.data.inputValidationOverride?.productImageCheck)` em `:338`
- `createCampaign` + upload (`:401-445`), `reserveCredit` (`:447-476`), `resolveStoreIdentity` (`:270`), `validateIdentityReference` (`:280`), `buildCampaignBrief`/`buildCampaignBriefFromFlat` (`:283-284`), `imageService.generateImage(...)` (`:686`)
- Precedente de serviço de leitura com fallback: `OperationCostService`/`EconomicParameterService` (server-only)
- Tabela `feature_flags` + RPC (migration 43-07)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Serviço de leitura da flag force_brief_vision_check (fallback enabled=false)</name>
  <files>src/lib/feature-flags/feature-flag-service.ts</files>
  <read_first>
    - openspec/changes/fase-43-revisao-brief-pre-geracao/specs/feature-flag-control/spec.md (Requirement: Leitura da flag com fallback que não derruba geração)
    - src/lib/credit/operation-cost-service.ts (padrão de serviço de leitura server-only com fallback — precedente F38) ou src/lib/economic/economic-parameter-service.ts (padrão de leitura com fallback fail-open/fail-closed — precedente F38.2)
  </read_first>
  <action>
    Criar `src/lib/feature-flags/feature-flag-service.ts` (server-only, padrão `OperationCostService`/`EconomicParameterService`):
    - Exportar constante `FORCE_BRIEF_VISION_CHECK_KEY = "force_brief_vision_check"`.
    - Função/serviço `isForceBriefVisionCheckEnabled(): Promise<boolean>` (ou equivalente):
      - Lê o `enabled` da linha `force_brief_vision_check` na tabela `feature_flags`.
      - **Fallback de leitura:** se a leitura falhar (banco indisponível/tabela ausente), retorna `false` (fluxo padrão: revisão humana + pular vision) e loga warning/erro operacional — **NÃO bloqueia a geração**.
      - **Env var emergencial opcional:** se `process.env.VENDEO_FORCE_BRIEF_VISION_CHECK === "true"`, pode forçar `true` (fail-safe emergencial infra) — nunca a decisão principal.
      - Padrão de log: log estruturado/warning (sem dados sensíveis).
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\lib\feature-flags\feature-flag-service.ts" -Pattern "force_brief_vision_check|VENDEO_FORCE_BRIEF_VISION_CHECK"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/feature-flags/feature-flag-service.ts` existe (server-only) com constante `force_brief_vision_check`
    - Função de leitura retorna `enabled` da tabela; **fallback → false** (não derruba geração, log warning)
    - Env var `VENDEO_FORCE_BRIEF_VISION_CHECK=true` pode forçar `true` (fail-safe emergencial)
    - `npx vitest run` da suíte existente passa
  </acceptance_criteria>
  <done>Serviço de leitura da flag criado com fallback enabled=false</done>
</task>

<task type="auto">
  <name>Task 2: Route.ts — normalização ponta a ponta da flag (effectiveCampaignInput)</name>
  <files>src/app/api/campaign/generate-image/route.ts</files>
  <read_first>
    - src/app/api/campaign/generate-image/route.ts (parse body; gate 338; buildCampaignBrief 283-284; generateImage 686)
    - openspec/changes/fase-43-revisao-brief-pre-geracao/specs/ai-image-generation/spec.md (Requirement: POST /api/campaign/generate-image endpoint — skip + normalização ponta a ponta; Scenario: Flag ligada → rota normaliza e valida ponta a ponta)
  </read_first>
  <action>
    Na rota `generate-image`:
    1. Após o parse/Zod (e antes da checagem pré-stream), ler `isForceBriefVisionCheckEnabled()`.
    2. **Flag ligada:** se `parsed.data.inputValidationOverride?.productImageCheck === "brief_review_confirmed"`, montar um **`effectiveParsedData`/`effectiveCampaignInput`** que **remove `brief_review_confirmed` do `inputValidationOverride`** (ex.: `inputValidationOverride: undefined` ou `{}`) **antes** da checagem pré-stream (`:338`). Usar esse **mesmo input normalizado** para: a checagem pré-stream (que então **roda** a IA de visão), `buildCampaignBriefFromFlat`, e `imageService.generateImage(...)` — assim AMBOS os pontos (pré-stream da rota E Phase 1 do serviço) executam a IA de visão. `user_confirmed_continue` **NUNCA é removido** (caminho "409 + insistiu" sempre pula).
    3. **Flag desligada (padrão):** usar `parsed.data` direto — `brief_review_confirmed` presente pula a validação pré-stream (`:338`) e o Phase 1 do serviço (override repassado), sem mudança de lógica.
    4. Sem override: comportamento atual (validação IA roda como rede de segurança).
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\app\api\campaign\generate-image\route.ts" -Pattern "effectiveCampaignInput|effectiveParsedData|isForceBriefVisionCheckEnabled"</automated>
  </verify>
  <acceptance_criteria>
    - Com a flag ligada + `brief_review_confirmed`, a rota normaliza um `effectiveParsedData`/`effectiveCampaignInput` (remove `brief_review_confirmed` do `inputValidationOverride`) ANTES de `:338`
    - Usa o mesmo input normalizado para a checagem pré-stream, o brief e `generateImage` (pré-stream + Phase 1 validam)
    - `user_confirmed_continue` nunca removido
    - Com a flag desligada, `brief_review_confirmed` pula nos dois pontos (comportamento padrão)
    - `npx vitest run` da suíte de `route.test.ts` passa (fixtures co-migradas em 43-14; testes 18-22 em 43-12)
  </acceptance_criteria>
  <done>Rota normaliza ponta a ponta quando a flag está ligada; user_confirmed_continue preservado</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| feature_flags → rota | Leitura da flag no backend de geração — risco de derrubar a geração em falha de leitura |
| rota → serviço | O input normalizado repassado ao serviço deve manter a coerência pré-stream × Phase 1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-08 | Denial of Service | feature-flag-service.ts | mitigate | Fallback de leitura `enabled=false` — falha não derruba a geração; log warning; env var só como fail-safe emergencial (teste 26) |
| T-43-08a | Tampering | route.ts normalização | mitigate | Normalização remove APENAS `brief_review_confirmed` (nunca `user_confirmed_continue`); mesmo input usado para pré-stream + brief + serviço (consistência ponta a ponta, teste 22) |
</threat_model>

<verification>
- `feature-flag-service.ts` com leitura + fallback enabled=false + env var emergencial
- `route.ts` normaliza `effectiveParsedData`/`effectiveCampaignInput` quando flag ligada; `user_confirmed_continue` nunca removido
- Flag desligada → `brief_review_confirmed` pula nos dois pontos
- `npx vitest run` da suíte existente passa
</verification>

<success_criteria>
- Serviço de leitura da flag com fallback que não derruba geração (D5)
- Rota normaliza ponta a ponta quando a flag está ligada (pré-stream + Phase 1 validam); `user_confirmed_continue` preservado (D5)
- Capacidade `InputValidationService` reativável sem redeploy
</success_criteria>

<output>
Create `.planning/phases/43-revisao-brief-pre-geracao/43-08-SUMMARY.md` when done
</output>

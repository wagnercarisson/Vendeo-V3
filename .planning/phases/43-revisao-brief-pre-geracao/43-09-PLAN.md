---
phase: 43-revisao-brief-pre-geracao
plan: 09
type: execute
wave: 3
depends_on: [43-07]
files_modified:
  - src/app/api/admin/feature-flags/route.ts
  - src/app/admin/feature-flags/page.tsx
  - src/app/admin/feature-flags/feature-flags-form.tsx
  - src/components/shell/sidebar.tsx
autonomous: true
requirements: [F43-22, F43-23, F43-24]

must_haves:
  truths:
    - "Rota admin de update da flag (PUT /api/admin/feature-flags ou equivalente) protegida por requireAdmin, zod (key/enabled/reason ≥ 1), motivo obrigatório, auditoria + idempotência via operationId (padrão operation-costs/economic-parameters) (D5)"
    - "Página admin 'Controles operacionais' exibe force_brief_vision_check com descrição, estados 'Desligada — padrão recomendado'/'Ligada — força validação IA além da revisão humana', motivo obrigatório, persistência + reload (D5)"
    - "Navegação admin (link/entrada na seção) (D5)"
    - "Utilizável apenas após a migration 43-07 aplicada no ambiente"
  artifacts:
    - path: "src/app/admin/feature-flags/page.tsx"
      provides: "Tela admin Controles operacionais"
      contains: "force_brief_vision_check"
---

<objective>
Criar a superfície administrativa da flag `force_brief_vision_check` (F43 D5): rota admin de update (`PUT /api/admin/feature-flags`) protegida por `requireAdmin` com **motivo obrigatório** e auditoria, e página **"Controles operacionais"** no admin exibindo a flag com descrição, estados e alteração com motivo. Navegação admin (link). **Utilizável apenas após a migration 43-07 aplicada** no ambiente; até lá, o fallback de leitura (`enabled=false`) mantém o fluxo padrão.

Purpose: D5 (flag administrativa mínima em tela no admin, sem redeploy; motivo obrigatório + auditoria).
Output: rota admin + tela "Controles operacionais".
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-revisao-brief-pre-geracao/43-CONTEXT.md (D5; precedentes admin: operation-costs/page.tsx + operation-costs-form.tsx com motivo obrigatório + auditoria; requireAdmin)
@openspec/changes/fase-43-revisao-brief-pre-geracao/specs/feature-flag-control/spec.md (Requirement: Mutação da flag com motivo obrigatório e auditoria + Tela admin de controle operacional da flag)
@.planning/phases/38-credit-operation-costs/38-05-SUMMARY.md (precedente de página admin com motivo obrigatório + auditoria)
@.planning/phases/38.2-admin-custos-operacionais/ (precedente operation-costs/economic-parameters admin)

<interfaces>
- `requireAdmin` (`src/lib/admin/require-admin`)
- RPC `admin_update_feature_flag` (migration 43-07) — valida key/enabled/reason, atualiza, audita
- `admin_audit_log` (action `feature_flag_update`, target_type `feature_flag`)
- Padrão de rota admin: `operation-costs`/`economic-parameters` (GET/PUT com zod, reason obrigatório, operationId idempotência)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rota admin PUT /api/admin/feature-flags (requireAdmin + zod + motivo obrigatório + auditoria)</name>
  <files>src/app/api/admin/feature-flags/route.ts</files>
  <read_first>
    - openspec/changes/fase-43-revisao-brief-pre-geracao/specs/feature-flag-control/spec.md (Requirement: Mutação da flag com motivo obrigatório e auditoria)
    - src/lib/admin/require-admin (padrão de guarda admin)
    - .planning/phases/38-credit-operation-costs/38-05-SUMMARY.md (precedente de rota admin PUT com reason + operationId)
  </read_first>
  <action>
    Criar `src/app/api/admin/feature-flags/route.ts`:
    - `PUT /api/admin/feature-flags` (ou equivalente) protegida por `requireAdmin`.
    - Zod schema do body: `{ key: z.string(), enabled: z.boolean(), reason: z.string().min(1) }` (motivo **obrigatório**, ≥ 1 char).
    - Delega ao RPC `admin_update_feature_flag` (migration 43-07) com `p_actor_id` (admin autenticado), `p_operation_id` (idempotência via operationId — padrão `operation-costs`/`economic-parameters`), `p_key`, `p_enabled`, `p_reason`.
    - Retorna a linha atualizada + auditoria registrada (RPC faz na mesma transação).
    - Erros: 400 (zod inválido / motivo ausente), 403 (não-admin), 404 (flag não encontrada), 500.
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\app\api\admin\feature-flags\route.ts" -Pattern "requireAdmin|reason|admin_update_feature_flag"</automated>
  </verify>
  <acceptance_criteria>
    - Rota `PUT /api/admin/feature-flags` protegida por `requireAdmin`
    - Body zod com `key`/`enabled`/`reason` (reason ≥ 1, obrigatório); motivo ausente → 400
    - Delega ao RPC `admin_update_feature_flag` com operationId (idempotência) e auditoria
    - Erros 400/403/404/500 mapeados
    - `npx vitest run` da suíte existente passa
  </acceptance_criteria>
  <done>Rota admin de update da flag criada (motivo obrigatório + auditoria)</done>
</task>

<task name="Task 2: Página admin 'Controles operacionais' (exibir flag + estados + motivo + persistência + reload)" type="auto">
  <files>src/app/admin/feature-flags/page.tsx, src/app/admin/feature-flags/feature-flags-form.tsx</files>
  <read_first>
    - openspec/changes/fase-43-revisao-brief-pre-geracao/specs/feature-flag-control/spec.md (Requirement: Tela admin de controle operacional da flag)
    - .planning/phases/38.2-admin-custos-operacionais/ (precedente operation-costs-form: inputs + motivo obrigatório + feedback)
  </read_first>
  <action>
    Criar a página admin **"Controles operacionais"** (`/admin/feature-flags`):
    - Exibe a flag `force_brief_vision_check` com a descrição administrativa e o estado atual (ligada/desligada).
    - Estados apresentados: **Desligada — padrão recomendado** / **Ligada — força validação IA além da revisão humana**.
    - Descrição clara: "Quando ligada, o Vendeo executa novamente a validação por IA das imagens mesmo depois da revisão humana do brief. Use apenas para diagnóstico, auditoria ou se houver suspeita de que campanhas problemáticas estão passando pela revisão humana."
    - Form com **motivo obrigatório** (bloqueia sem motivo — "Motivo obrigatório").
    - Persistência via `PUT /api/admin/feature-flags` + **reload** do estado da flag após salvar (refletir a alteração).
    - Acesso protegido por `requireAdmin`.
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\app\admin\feature-flags\page.tsx" -Pattern "force_brief_vision_check|Motivo obrigatório|Controles operacionais"</automated>
  </verify>
  <acceptance_criteria>
    - Página "Controles operacionais" exibe `force_brief_vision_check` com descrição e estado atual
    - Estados "Desligada — padrão recomendado"/"Ligada — força validação IA além da revisão humana" apresentados
    - Alteração exige motivo obrigatório (bloqueia sem motivo); persistida + reload reflete o novo estado
    - Acesso não-admin negado (requireAdmin)
  </acceptance_criteria>
  <done>Página admin Controles operacionais criada (exibição + motivo + persistência + reload)</done>
</task>

<task name="Task 3: Navegação admin (link/entrada na seção)" type="auto">
  <files>src/components/shell/sidebar.tsx</files>
  <read_first>
    - src/components/shell/sidebar.tsx (NAV_ITEMS / seção admin)
    - .planning/phases/38-credit-operation-costs/ (precedente de navegação admin)
  </read_first>
  <action>
    Adicionar link/entrada "Controles operacionais" na navegação admin (padrão do admin existente, `NAV_ITEMS`/seção admin do sidebar), apontando para `/admin/feature-flags`.
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\components\shell\sidebar.tsx" -Pattern "feature-flags|Controles operacionais"</automated>
  </verify>
  <acceptance_criteria>
    - Entrada "Controles operacionais" (ou similar) na navegação admin apontando para `/admin/feature-flags`
    - Padrão de navegação do admin existente preservado
    - `npx vitest run` da suíte existente passa
  </acceptance_criteria>
  <done>Navegação admin para Controles operacionais adicionada</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin → feature_flags | Alteração da flag no admin — risco de alteração sem motivo/rastreio |
| feature_flags → geração | Leitura no backend de geração — não deve derrubar a geração |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-09 | Tampering | admin feature-flags | mitigate | `requireAdmin` + zod + motivo obrigatório (≥1) + auditoria (`feature_flag_update`/`feature_flag`) + operationId idempotência; sem motivo → 400/bloqueio na tela |
| T-43-09a | Elevation of Privilege | rota admin | mitigate | `requireAdmin` nega acesso não-admin (403) |
| T-43-09b | Denial of Service | leitura da flag | accept | Fallback `enabled=false` na leitura (43-08) protege a geração |
</threat_model>

<verification>
- Rota `PUT /api/admin/feature-flags` com requireAdmin + zod (reason ≥ 1) + RPC com auditoria + operationId
- Página "Controles operacionais" exibe flag com descrição/estados, motivo obrigatório, persistência + reload
- Navegação admin adicionada
- `npx vitest run` da suíte existente passa
</verification>

<success_criteria>
- Flag `force_brief_vision_check` operável em tela no admin, sem redeploy, com motivo obrigatório + auditoria (D5)
- Rota admin protegida por requireAdmin com idempotência via operationId
- Utilizável após a migration 43-07 aplicada; fallback mantém o fluxo padrão até lá
</success_criteria>

<output>
Create `.planning/phases/43-revisao-brief-pre-geracao/43-09-SUMMARY.md` when done
</output>

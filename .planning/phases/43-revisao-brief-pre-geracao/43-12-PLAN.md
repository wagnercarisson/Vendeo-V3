---
phase: 43-revisao-brief-pre-geracao
plan: 12
type: execute
wave: 5
depends_on: [43-05, 43-08]
files_modified:
  - src/lib/image-generation/__tests__/schema.test.ts
  - src/app/api/campaign/generate-image/__tests__/route.test.ts
  - src/lib/image-generation/services/__tests__/image-generation-service.test.ts
autonomous: true
requirements: [F43-27]

must_haves:
  truths:
    - "Testes 17-23 do backend/schema/rota/serviço implementados conforme tasks.md §12 (D5)"
    - "Teste 17: Zod aceita brief_review_confirmed; rejeita valor desconhecido (.strict()) (D5)"
    - "Teste 18: rota com brief_review_confirmed → pula a IA de visão (sem campaign_input_validation) (D5)"
    - "Teste 22: flag ligada → rota normaliza o input (remove brief_review_confirmed antes da checagem pré-stream); pré-stream e Phase 1 validam; user_confirmed_continue não é removido (D5)"
    - "Teste 23: ImageGenerationService com override → fase input_validation emitida obrigatoriamente status 'skipped', sem chamada real e sem 'complete' falso (D5)"
  artifacts:
    - path: "src/lib/image-generation/__tests__/schema.test.ts"
      provides: "Teste 17 do override"
      contains: "brief_review_confirmed"
---

<objective>
Implementar os **Testes 17–23** do backend/schema/rota/serviço da F43 (tasks.md §12; D5): schema aceita/rejeita override, rota pula/valida conforme override e flag, serviço emite `skipped`.

Purpose: validar o override `brief_review_confirmed` + flag `force_brief_vision_check` ponta a ponta (D5).
Output: suíte de testes 17-23 passando.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-revisao-brief-pre-geracao/43-CONTEXT.md (D5; testes 17-23)
@openspec/changes/fase-43-revisao-brief-pre-geracao/tasks.md (§12 — Testes 17-23)
@openspec/changes/fase-43-revisao-brief-pre-geracao/specs/ai-image-generation/spec.md (cenários do override + flag)
@openspec/changes/fase-43-revisao-brief-pre-geracao/specs/validation-review-alignment/spec.md (cenários do serviço skipped)
@src/app/api/campaign/generate-image/__tests__/route.test.ts (fixtures — co-migradas em 43-14)
@src/lib/image-generation/services/__tests__/image-generation-service.test.ts (fixtures — co-migradas em 43-14)

<interfaces>
- `GenerateImageRequestSchema` (43-05): `productImageCheck` com `brief_review_confirmed`
- Rota `generate-image` (43-08): skip + normalização da flag
- `ImageGenerationService` (43-06): fase `input_validation` skipped
- `force_brief_vision_check` (43-08)
</interfaces>
</context>

<tasks>

<task name="Task 1: Testes 17-20 — schema (aceita/rejeita) + rota (pula com brief_review_confirmed/user_confirmed_continue, roda sem override)" type="auto">
  <files>src/lib/image-generation/__tests__/schema.test.ts, src/app/api/campaign/generate-image/__tests__/route.test.ts</files>
  <read_first>
    - openspec/changes/fase-43-revisao-brief-pre-geracao/tasks.md (§12 — Testes 17-20)
    - src/lib/image-generation/__tests__/schema.test.ts (estado atual)
    - src/app/api/campaign/generate-image/__tests__/route.test.ts (estado atual)
  </read_first>
  <action>
    Implementar:
    - **Teste 17 (D5):** Zod aceita `brief_review_confirmed`; rejeita valor desconhecido (`.strict()`).
    - **Teste 18 (D5):** rota com `brief_review_confirmed` → **pula** a IA de visão (sem `campaign_input_validation`).
    - **Teste 19 (D5):** rota com `user_confirmed_continue` → pula (comportamento atual preservado).
    - **Teste 20 (D5):** rota sem override → validação IA roda (rede de segurança).
    Usar as fixtures de `route.test.ts` (co-migradas em 43-14 para o novo literal). Mocks necessários para a flag (flag desligada por padrão).
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\lib\image-generation\__tests__\schema.test.ts" -Pattern "brief_review_confirmed"; Select-String -LiteralPath "src\app\api\campaign\generate-image\__tests__\route.test.ts" -Pattern "brief_review_confirmed"</automated>
  </verify>
  <acceptance_criteria>
    - Teste 17: schema aceita `brief_review_confirmed`; rejeita valor desconhecido (`.strict()`)
    - Teste 18: rota pula a IA de visão com `brief_review_confirmed` (sem `campaign_input_validation`)
    - Teste 19: rota pula com `user_confirmed_continue` (comportamento atual preservado)
    - Teste 20: rota roda validação IA sem override (rede de segurança)
    - `npx vitest run` desses testes passa
  </acceptance_criteria>
  <done>Testes 17-20 implementados e passando</done>
</task>

<task name="Task 2: Testes 21-23 — flag desligada pula, flag ligada normaliza+valida, serviço emite skipped" type="auto">
  <files>src/app/api/campaign/generate-image/__tests__/route.test.ts, src/lib/image-generation/services/__tests__/image-generation-service.test.ts</files>
  <read_first>
    - openspec/changes/fase-43-revisao-brief-pre-geracao/tasks.md (§12 — Testes 21-23)
    - openspec/changes/fase-43-revisao-brief-pre-geracao/specs/ai-image-generation/spec.md (cenários 21-22)
    - openspec/changes/fase-43-revisao-brief-pre-geracao/specs/validation-review-alignment/spec.md (cenário 23)
    - src/lib/image-generation/services/__tests__/image-generation-service.test.ts (estado atual)
  </read_first>
  <action>
    Implementar:
    - **Teste 21 (D5):** flag `force_brief_vision_check` **desligada** → `brief_review_confirmed` pula nos dois pontos (rota pré-stream + Phase 1).
    - **Teste 22 (D5):** flag **ligada** → rota **normaliza** o input (remove `brief_review_confirmed` **antes** da checagem pré-stream); **pré-stream e Phase 1 do serviço validam**; `user_confirmed_continue` não é removido.
    - **Teste 23 (D5):** `ImageGenerationService` com override (`brief_review_confirmed` OU `user_confirmed_continue`) → fase `input_validation` emitida com **obrigatoriamente** `status: "skipped"` (detail opcional "Brief confirmado pelo usuário"/"Validação dispensada"), sem chamada de IA real e sem "complete" falso.
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\lib\image-generation\services\__tests__\image-generation-service.test.ts" -Pattern "skipped|brief_review_confirmed"</automated>
  </verify>
  <acceptance_criteria>
    - Teste 21: flag desligada → `brief_review_confirmed` pula nos dois pontos
    - Teste 22: flag ligada → rota normaliza (remove `brief_review_confirmed` antes da checagem); pré-stream + Phase 1 validam; `user_confirmed_continue` não removido
    - Teste 23: serviço emite `input_validation` `skipped` com override, sem chamada real e sem `complete` falso
    - `npx vitest run` desses testes passa
  </acceptance_criteria>
  <done>Testes 21-23 implementados e passando</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| testes ↔ backend | Testes validam override + flag — risco de teste com mock permissivo esconder regressão |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-12 | Tampering | testes backend | mitigate | Testes 17-23 cobrem schema (.strict), rota (pula/valida conforme override+flag) e serviço (skipped, sem complete falso) (D5) |
</threat_model>

<verification>
- Testes 17-23 implementados e passando
- Cobertura: schema, rota (4 caminhos de override/flag), serviço skipped
- `npx vitest run` desses testes passa
</verification>

<success_criteria>
- Suíte 17-23 do backend/schema/rota/serviço validando D5
- Override aceito/rejeitado; rota pula/valida conforme flag; serviço emite skipped sem fase falsa
</success_criteria>

<output>
Create `.planning/phases/43-revisao-brief-pre-geracao/43-12-SUMMARY.md` when done
</output>

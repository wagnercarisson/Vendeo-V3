---
phase: 43-revisao-brief-pre-geracao
plan: 13
type: execute
wave: 4
depends_on: [43-09]
files_modified:
  - src/app/api/admin/feature-flags/__tests__/route.test.ts
  - src/app/admin/feature-flags/__tests__/feature-flags-form.test.tsx
autonomous: true
requirements: [F43-27]

must_haves:
  truths:
    - "Testes 24-26 do admin da flag implementados conforme tasks.md §13 (D5)"
    - "Teste 24: tela admin ('Controles operacionais') exibe force_brief_vision_check com descrição e estados; alteração com motivo obrigatório persistida (enabled, updated_by, updated_at) (D5)"
    - "Teste 25: alteração registra auditoria — admin_audit_log com action 'feature_flag_update', target_type 'feature_flag', target_id = feature_flags.id, metadata { key, old_value, new_value, reason } (D5)"
    - "Teste 26: fallback de leitura — falha ao ler a flag não bloqueia geração → enabled=false; env var emergencial pode forçar true (D5)"
  artifacts:
    - path: "src/app/api/admin/feature-flags/__tests__/route.test.ts"
      provides: "Testes 24-25 da rota admin"
      contains: "feature_flag_update"
---

<objective>
Implementar os **Testes 24–26** do admin da flag (tasks.md §13; D5): tela "Controles operacionais" com descrição/estados + motivo obrigatório persistido, auditoria (`feature_flag_update`/`feature_flag`), e fallback de leitura que não derruba a geração.

Purpose: validar a flag administrativa (D5).
Output: suíte de testes 24-26 passando.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-revisao-brief-pre-geracao/43-CONTEXT.md (D5; testes 24-26)
@openspec/changes/fase-43-revisao-brief-pre-geracao/tasks.md (§13 — Testes 24-26)
@openspec/changes/fase-43-revisao-brief-pre-geracao/specs/feature-flag-control/spec.md (cenários admin + leitura com fallback)
@src/app/api/admin/feature-flags/route.ts (43-09)
@src/lib/feature-flags/feature-flag-service.ts (43-08)

<interfaces>
- Rota `PUT /api/admin/feature-flags` (43-09): requireAdmin, zod, reason ≥ 1, RPC + auditoria
- Página "Controles operacionais" (43-09)
- Serviço de leitura da flag (43-08): fallback enabled=false + env var emergencial
- RPC `admin_update_feature_flag` (migration 43-07)
</interfaces>
</context>

<tasks>

<task name="Task 1: Testes 24-25 — tela admin exibe flag + motivo obrigatório persistido + auditoria" type="auto">
  <files>src/app/api/admin/feature-flags/__tests__/route.test.ts, src/app/admin/feature-flags/__tests__/feature-flags-form.test.tsx</files>
  <read_first>
    - openspec/changes/fase-43-revisao-brief-pre-geracao/tasks.md (§13 — Testes 24-25)
    - src/app/api/admin/feature-flags/route.ts (43-09)
  </read_first>
  <action>
    Implementar:
    - **Teste 24 (D5):** tela de admin ("Controles operacionais") exibe `force_brief_vision_check` (`feature_flags`) com descrição e estados; alteração com **motivo obrigatório** persistida (`enabled`, `updated_by`, `updated_at`).
    - **Teste 25 (D5):** alteração da flag registra auditoria — `admin_audit_log` com `action: "feature_flag_update"`, `target_type: "feature_flag"`, `target_id` = `feature_flags.id` (UUID NOT NULL existente), `metadata { key, old_value, new_value, reason }` (respeita constraints/CHECK existentes).
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\app\api\admin\feature-flags\__tests__\route.test.ts" -Pattern "feature_flag_update|feature_flag|force_brief_vision_check"</automated>
  </verify>
  <acceptance_criteria>
    - Teste 24: tela exibe flag com descrição/estados; alteração com motivo persistida (enabled/updated_by/updated_at)
    - Teste 25: auditoria com action `feature_flag_update`, target_type `feature_flag`, target_id = feature_flags.id, metadata key/old_value/new_value/reason
    - `npx vitest run` desses testes passa
  </acceptance_criteria>
  <done>Testes 24-25 implementados e passando</done>
</task>

<task name="Task 2: Teste 26 — fallback de leitura não bloqueia geração + env var emergencial" type="auto">
  <files>src/lib/feature-flags/__tests__/feature-flag-service.test.ts</files>
  <read_first>
    - openspec/changes/fase-43-revisao-brief-pre-geracao/tasks.md (§13 — Teste 26)
    - src/lib/feature-flags/feature-flag-service.ts (43-08)
  </read_first>
  <action>
    Implementar:
    - **Teste 26 (D5):** **fallback de leitura** — falha ao ler a flag **não bloqueia geração** → `enabled=false` (fluxo padrão pula vision), log de warning/erro; env var emergencial pode forçar `true`.
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\lib\feature-flags\__tests__\feature-flag-service.test.ts" -Pattern "fallback|VENDEO_FORCE_BRIEF_VISION_CHECK|enabled"</automated>
  </verify>
  <acceptance_criteria>
    - Teste 26: falha de leitura → `enabled=false` (não bloqueia geração); env var `VENDEO_FORCE_BRIEF_VISION_CHECK=true` pode forçar `true`
    - `npx vitest run` desse teste passa
  </acceptance_criteria>
  <done>Teste 26 implementado e passando</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| testes admin ↔ flag | Testes validam a flag — risco de mock permissivo esconder regressão |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-13 | Tampering | testes admin | mitigate | Testes 24-25 cobrem exibição/persistência/auditoria; Teste 26 cobre fallback de leitura sem derrubar geração (D5) |
</threat_model>

<verification>
- Testes 24-26 implementados e passando
- Cobertura: tela admin, motivo obrigatório persistido, auditoria, fallback de leitura
- `npx vitest run` desses testes passa
</verification>

<success_criteria>
- Suíte 24-26 do admin da flag validando D5
- Auditoria correta (feature_flag_update/feature_flag) + fallback de leitura que não derruba a geração
</success_criteria>

<output>
Create `.planning/phases/43-revisao-brief-pre-geracao/43-13-SUMMARY.md` when done
</output>

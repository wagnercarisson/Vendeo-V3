---
phase: 43-revisao-brief-pre-geracao
plan: 14
type: execute
wave: 6
depends_on: [43-10, 43-11, 43-12, 43-13]
files_modified:
  - src/components/flow/__tests__/use-campaign-form-submit-error.test.ts
  - src/components/flow/__tests__/use-campaign-form-navigation.test.ts
  - src/components/flow/__tests__/use-campaign-form-product-images.test.ts
  - src/components/flow/__tests__/use-campaign-form-validity.test.ts
  - src/components/flow/__tests__/use-campaign-form-notice.test.ts
  - src/app/api/campaign/generate-image/__tests__/route.test.ts
  - src/lib/image-generation/services/__tests__/image-generation-service.test.ts
autonomous: true
requirements: [F43-28]

must_haves:
  truths:
    - "Co-migração dos testes do hook (submit-error, navigation, product-images, validity, notice) — submit agora passa pela revisão (D2)"
    - "Fixtures de route.test.ts atualizadas para o novo literal brief_review_confirmed (D5)"
    - "image-generation-service.test.ts co-migrado — fase input_validation skipped quando override pula (D5)"
    - "Regressão: fluxo de geração completo (crédito, rate limit, clearance, readiness, stream, telemetria, estorno) inalterado para o payload sem override (D5)"
    - "Verificar npx vitest run com suíte completa (novos + co-migrados passando)"
  artifacts:
    - path: "src/app/api/campaign/generate-image/__tests__/route.test.ts"
      provides: "Fixtures atualizadas para o novo literal"
      contains: "brief_review_confirmed"
---

<objective>
Executar a **regressão e co-migração de fixtures** da F43 (tasks.md §14; D2–D7): os testes existentes do hook que assumem submit direto agora passam pela revisão; fixtures de `route.test.ts`/`image-generation-service.test.ts` atualizadas para o novo literal `brief_review_confirmed` e para a fase `input_validation` `skipped`. Garantir que o fluxo de geração completo (crédito, rate limit, clearance, readiness, stream, telemetria, estorno) permanece inalterado para o payload sem override.

Purpose: regressão obrigatória (D2–D7) — o novo estado de revisão não quebra o fluxo existente.
Output: suíte completa passando.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-revisao-brief-pre-geracao/43-CONTEXT.md (D2-D7; testes co-migrados)
@openspec/changes/fase-43-revisao-brief-pre-geracao/tasks.md (§14 — Regressão e co-migração)
@.planning/phases/41-midia-de-campanha-mobile/41-12-SUMMARY.md (precedente de co-migração de fixtures)

<interfaces>
- Testes do hook que passam a exigir revisão (43-03/43-04)
- `route.test.ts` com novo literal (43-05/43-08)
- `image-generation-service.test.ts` com fase `skipped` (43-06)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Co-migrar testes do hook (submit agora passa pela revisão)</name>
  <files>src/components/flow/__tests__/use-campaign-form-submit-error.test.ts, src/components/flow/__tests__/use-campaign-form-navigation.test.ts, src/components/flow/__tests__/use-campaign-form-product-images.test.ts, src/components/flow/__tests__/use-campaign-form-validity.test.ts, src/components/flow/__tests__/use-campaign-form-notice.test.ts</files>
  <read_first>
    - openspec/changes/fase-43-revisao-brief-pre-geracao/tasks.md (§14.1-14.4)
    - src/components/flow/__tests__/use-campaign-form-submit-error.test.ts (estado atual)
    - src/components/flow/__tests__/use-campaign-form-navigation.test.ts (estado atual)
    - src/components/flow/__tests__/use-campaign-form-product-images.test.ts (estado atual)
    - src/components/flow/__tests__/use-campaign-form-validity.test.ts (estado atual)
    - src/components/flow/__tests__/use-campaign-form-notice.test.ts (estado atual)
  </read_first>
  <action>
    Co-migrar os testes do hook para o novo fluxo de revisão (D2): (1) `use-campaign-form-submit-error.test.ts` — submit agora passa pela revisão, ajustar para "Revisar e gerar" → "Confirmar e gerar campanha"; (2) `use-campaign-form-navigation.test.ts` — fluxo revisão → confirmação → `/campanhas/[id]`; (3) `use-campaign-form-product-images.test.ts` — `prepareCampaignImages` antes da revisão (D3/D4); (4) `use-campaign-form-validity.test.ts`/`use-campaign-form-notice.test.ts` — derivados via `buildCampaignGenerationBody` (D4). Ajustar apenas o mínimo delta para refletir o estado `reviewMode` e os helpers puros.
  </action>
  <verify>
    <automated>npx vitest run src/components/flow/__tests__/use-campaign-form-submit-error.test.ts src/components/flow/__tests__/use-campaign-form-navigation.test.ts src/components/flow/__tests__/use-campaign-form-product-images.test.ts src/components/flow/__tests__/use-campaign-form-validity.test.ts src/components/flow/__tests__/use-campaign-form-notice.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `use-campaign-form-submit-error.test.ts` co-migrado (submit passa pela revisão)
    - `use-campaign-form-navigation.test.ts` co-migrado (revisão → confirmação → `/campanhas/[id]`)
    - `use-campaign-form-product-images.test.ts` co-migrado (prepareCampaignImages antes da revisão)
    - `use-campaign-form-validity.test.ts`/`use-campaign-form-notice.test.ts` co-migrados (derivados via buildCampaignGenerationBody)
    - `npx vitest run` desses testes passa
  </acceptance_criteria>
  <done>Testes do hook co-migrados para o fluxo de revisão</done>
</task>

<task type="auto">
  <name>Task 2: Co-migrar fixtures de route.test.ts + image-generation-service.test.ts</name>
  <files>src/app/api/campaign/generate-image/__tests__/route.test.ts, src/lib/image-generation/services/__tests__/image-generation-service.test.ts</files>
  <read_first>
    - openspec/changes/fase-43-revisao-brief-pre-geracao/tasks.md (§14.5-14.6)
    - src/app/api/campaign/generate-image/__tests__/route.test.ts (estado atual)
    - src/lib/image-generation/services/__tests__/image-generation-service.test.ts (estado atual)
  </read_first>
  <action>
    (1) `route.test.ts` (14.5): atualizar fixtures para o novo literal `brief_review_confirmed` (e a flag `force_brief_vision_check` desligada por padrão). (2) `image-generation-service.test.ts` (14.6): co-migrar para a fase `input_validation` `skipped` quando o override pula (D5).
  </action>
  <verify>
    <automated>npx vitest run src/app/api/campaign/generate-image/__tests__/route.test.ts src/lib/image-generation/services/__tests__/image-generation-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Fixtures de `route.test.ts` atualizadas para `brief_review_confirmed` e flag desligada
    - `image-generation-service.test.ts` co-migrado (fase `input_validation` skipped quando override pula)
    - `npx vitest run` desses testes passa
  </acceptance_criteria>
  <done>Fixtures de route/service co-migradas</done>
</task>

<task type="auto">
  <name>Task 3: Regressão completa + verificação da suíte</name>
  <files>.</files>
  <read_first>
    - openspec/changes/fase-43-revisao-brief-pre-geracao/tasks.md (§14.7-14.8)
  </read_first>
  <action>
    (1) Verificar que o fluxo de geração completo (crédito, rate limit, clearance, readiness, stream, telemetria, estorno) permanece **inalterado** para o payload sem override (14.7). (2) Rodar `npx vitest run` com a suíte completa — novos + co-migrados passando (14.8). Registrar a contagem de testes no SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - Fluxo de geração completo inalterado para payload sem override (crédito, rate limit, clearance, readiness, stream, telemetria, estorno)
    - `npx vitest run` com suíte completa passa (novos + co-migrados)
  </acceptance_criteria>
  <done>Regressão completa verificada; suíte completa passando</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| novo fluxo ↔ regressão | O estado de revisão não deve alterar o comportamento de geração para payload sem override |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-14 | Tampering | regressão | mitigate | Co-migração dos testes do hook + fixtures de route/service; verificação de que o fluxo de geração completo permanece inalterado sem override (D5) |
</threat_model>

<verification>
- Testes do hook co-migrados (submit passa pela revisão)
- Fixtures de route.test.ts atualizadas (brief_review_confirmed) e image-generation-service.test.ts (skipped)
- Fluxo de geração completo inalterado sem override
- `npx vitest run` da suíte completa passa
</verification>

<success_criteria>
- Regressão obrigatória concluída (D2–D7): novos + co-migrados passando
- Fluxo de geração sem override inalterado
- Contagem de testes registrada no SUMMARY
</success_criteria>

<output>
Create `.planning/phases/43-revisao-brief-pre-geracao/43-14-SUMMARY.md` when done
</output>

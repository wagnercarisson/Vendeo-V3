---
phase: 43-revisao-brief-pre-geracao
plan: 15
type: execute
wave: 7
depends_on: [43-14]
files_modified:
  - .planning/phases/43-revisao-brief-pre-geracao/43-VERIFICATION.md
  - .planning/phases/43-revisao-brief-pre-geracao/43-UAT.md
autonomous: false
requirements: [F43-29]

must_haves:
  truths:
    - "npx vitest run, npm run typecheck, npm run lint, npm run build — zero erros (D2-D7)"
    - "UAT local: form → 'Revisar e gerar' → resumo correto → 'Voltar e editar' sem perda → 'Confirmar' gera (D2/D6)"
    - "UAT local: imagem HEIC (celular) → revisão mostra JPEG comprimido mesma orientação (D3)"
    - "UAT local mobile real/estreito (320px/375px): revisão sem scroll horizontal, botões acessíveis, Topbar não cobre, preview sem recorte (D7)"
    - "UAT local: 'Confirmar' → geração sem etapa vision; GenerationProgress mostra input_validation como skipped (D5)"
    - "UAT local: flag force_brief_vision_check ligada na tela admin → validação vision volta a rodar (rota normaliza); alteração com motivo, auditada, sem redeploy (D5)"
    - "UAT local: fallback de leitura — banco/flag indisponível → geração segue sem bloquear, log warning (D5)"
    - "UAT local: sem override (ex.: requisição manual) → validação vision roda normalmente (D5)"
    - "UAT local: saldo insuficiente/custo desativado → confirmar bloqueado (D6)"
    - "UAT local: geração bem-sucedida → /campanhas/[id] com arte e kit (fluxo atual)"
  artifacts:
    - path: ".planning/phases/43-revisao-brief-pre-geracao/43-VERIFICATION.md"
      provides: "Verificação da fase (4 gates + cobertura)"
      contains: "status: passed"
---

<objective>
Executar a **verificação final** da F43 (tasks.md §15; D1–D7): os 4 gates automáticos (`npx vitest run`, `npm run typecheck`, `npm run lint`, `npm run build`) com zero erros e a **UAT local** cobrindo revisão, compressão HEIC, mobile real/estreito (320px/375px), geração sem etapa vision (input_validation skipped), flag ligada/desligada e fallback de leitura. Produzir `43-VERIFICATION.md` (com status `passed`) e `43-UAT.md` (roteiro/evidências).

Purpose: D1–D7 — garantir que a F43 entrega o gate de revisão + override + flag de forma coerente e sem regressão.
Output: 43-VERIFICATION.md (passed) + 43-UAT.md.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-revisao-brief-pre-geracao/43-CONTEXT.md (D1-D7)
@openspec/changes/fase-43-revisao-brief-pre-geracao/tasks.md (§15 — Verificação)
@.planning/phases/41-midia-de-campanha-mobile/41-VERIFICATION.md (precedente de VERIFICATION)
@.planning/phases/41-midia-de-campanha-mobile/41-UAT.md (precedente de UAT)

<interfaces>
- 4 gates: `npx vitest run`, `npm run typecheck`, `npm run lint`, `npm run build`
- UAT local (tasks.md §15.5-15.13)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 4 gates automáticos — vitest, typecheck, lint, build</name>
  <files>.</files>
  <read_first>
    - openspec/changes/fase-43-revisao-brief-pre-geracao/tasks.md (§15.1-15.4)
  </read_first>
  <action>
    Rodar os 4 gates: (1) `npx vitest run` — zero falhas (novos + existentes + co-migrados); (2) `npm run typecheck` — zero erros; (3) `npm run lint` — zero erros; (4) `npm run build` — build bem-sucedido. Registrar as evidências (contagem de testes, exit codes).
  </action>
  <verify>
    <automated>npx vitest run; if ($?) { npm run typecheck; if ($?) { npm run lint; if ($?) { npm run build } } }</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` — zero falhas (todos os testes novos + existentes + co-migrados passando)
    - `npm run typecheck` — zero erros
    - `npm run lint` — zero erros
    - `npm run build` — build bem-sucedido
  </acceptance_criteria>
  <done>4 gates verdes</done>
</task>

<task type="auto">
  <name>Task 2: UAT local (tasks.md 15.5-15.13) + roteiro 43-UAT.md</name>
  <files>.planning/phases/43-revisao-brief-pre-geracao/43-UAT.md</files>
  <read_first>
    - openspec/changes/fase-43-revisao-brief-pre-geracao/tasks.md (15.5-15.13)
    - .planning/phases/41-midia-de-campanha-mobile/41-UAT.md (precedente de roteiro UAT)
  </read_first>
  <action>
    Executar e documentar a UAT local em 43-UAT.md cobrindo os cenario 15.5-15.13: (15.5) form Revisar e gerar, resumo, Voltar e editar sem perda, Confirmar gera (D2/D6); (15.6) HEIC mostra JPEG comprimido (D3); (15.7) mobile 320/375 sem scroll, botoes acessiveis, Topbar nao cobre, preview sem recorte (D7); (15.8) geracao sem vision, GenerationProgress input_validation skipped (D5); (15.9) flag ligada na tela admin -> vision volta a rodar (rota normaliza, motivo auditado, sem redeploy) (D5); (15.10) fallback de leitura nao bloqueia geracao (D5); (15.11) sem override vision roda (D5); (15.12) saldo/custo bloqueiam confirmar (D6); (15.13) geracao -> /campanhas/[id] (fluxo atual). Registrar PASS/observacoes por cenario.
  </action>
  <verify>
    <automated>Select-String -LiteralPath ".planning\phases\43-revisao-brief-pre-geracao\43-UAT.md" -Pattern "15.5|15.6|15.7|15.8|15.9|15.10|15.11|15.12|15.13"</automated>
  </verify>
  <acceptance_criteria>
    - 43-UAT.md existe com os cenario 15.5-15.13 documentados
    - Cada cenario registra PASS ou observacao
  </acceptance_criteria>
  <done>UAT local documentada em 43-UAT.md</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verificacao <-> fase | Os gates e a UAT validam a entrega completa da F43 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-15 | Tampering | verificacao | mitigate | 4 gates + UAT local cobrem revisao, compressao HEIC, mobile, skipped, flag ligada/desligada e fallback de leitura (D1-D7) |
</threat_model>

<verification>
- `npx vitest run`, `npm run typecheck`, `npm run lint`, `npm run build` — zero erros
- UAT local 15.5-15.13 documentada em 43-UAT.md
- 43-VERIFICATION.md com status passed
</verification>

<success_criteria>
- 4 gates verdes (D2-D7)
- UAT local completa (revisao, HEIC, mobile, skipped, flag, fallback)
- 43-VERIFICATION.md (passed) + 43-UAT.md produzidos
</success_criteria>

<output>
Create `.planning/phases/43-revisao-brief-pre-geracao/43-VERIFICATION.md` (status passed) and `.planning/phases/43-revisao-brief-pre-geracao/43-UAT.md` when done
</output>

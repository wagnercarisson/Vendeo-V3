---
phase: 45-briefing-contextual-do-diretor-de-arte
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/45-briefing-contextual-do-diretor-de-arte/45-01-SUMMARY.md
  - ROADMAP.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/PROJECT.md
  - AGENTS.md
autonomous: true
requirements: [F45-01, F45-02, F45-03, F45-04]

must_haves:
  truths:
    - "Não há resíduo de nomenclatura no estado atual dos runbooks (ROADMAP raiz, .planning/ROADMAP.md, .planning/STATE.md, .planning/PROJECT.md, AGENTS.md): F45 = Briefing Contextual do Diretor de Arte (v1.5) como fase em planejamento; F44 = Temas de Campanha NÃO consta como fase numerada (apenas menção 'fora da numeração'); Stripe/Monetização Pública fora da numeração (iniciativa diferida v1.7+)"
    - "Inventário de consumidores das chaves do mapa de buildPromptVariables registrado (tabela de decisão manter/remover/mover) SEM alterar código — grep: commercialFrame, hasCategoryConflict, brandColorsChosen, visualStyle, visualTone, brandPersonality, campaignGuidelines, campaignBrief, identityImageUrl, campaignIntent"
    - "Baseline de superfícies congeladas registrado: image-review-service.ts/campaign-image-reviewer.md, Copy Director, providers/openai.ts (quick 260902-mqj), GenerateImageRequestSchema, buildMandatoryArtworkText/buildValidityDisplayText/buildCampaignGenerationBody — nenhum deles será alterado pela F45 (D7)"
    - "Baseline de testes atuais registrado (golden 39 keys em image-generation-service.test.ts :556-708; prompt-reframe.test.ts; validatePrompts :65-298) + decisão sobre o arquivo base campaign-image-director.md (mantido em sync como referência offer/geral)"
  artifacts:
    - path: ".planning/phases/45-briefing-contextual-do-diretor-de-arte/45-01-SUMMARY.md"
      provides: "Registro do plano (grep-verificação + inventário + baselines + decisão base)"
      contains: "F45 = Briefing Contextual do Diretor de Arte"
  key_links:
    - from: "buildPromptVariables (image-generation-service.ts :896-1003)"
      to: "art-director-briefing.ts (novo módulo, 45-02)"
      via: "Inventário do 45-01 decide quais chaves são removidas/realocadas ANTES de qualquer remoção de chave (D1)"
      pattern: "commercialFrame|hasCategoryConflict|brandColorsChosen"
---

<objective>
Registrar e verificar o estado de partida da F45 sem alterar código: (1.1) **grep-verificação de trackings** F45/F44/Stripe nos 5 runbooks (registro D-trackings já aplicado no ciclo de planejamento — commit `371077f7`; os runbooks listados em `files_modified` são **somente-if-resíduo** — só são editados se o grep encontrar resíduo real de estado atual, com correção mínima de delta); (1.2) **inventário de consumidores** das chaves do mapa de `buildPromptVariables` (tabela de decisão manter/remover/mover, SEM alterar código); (1.3) **baseline de superfícies congeladas** (revisor, Copy Director, fallback OpenAI, `GenerateImageRequestSchema`, helpers do form — D7); (1.4) **baseline de testes atuais** + decisão sobre o arquivo base `campaign-image-director.md` (mantido em sync — D2/D5).

Purpose: D-trackings (registro já aplicado — verificar consistência), D1 (chaves mortas só removidas após inventário), D7 (superfícies congeladas — registrar não-mudança), D2/D5 (decisão do arquivo base + baseline de testes para co-migração posterior).
Output: grep-verificação + inventário + baselines documentados no arquivo esperado `45-01-SUMMARY.md`; zero edições de código; runbooks editados apenas se houver resíduo.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md (seção "### Phase 45: Briefing Contextual do Diretor de Arte" :859; nota "Phase numbering" :7; tabela Progress :44)
@.planning/PROJECT.md (:52 — F45 em planejamento)
@AGENTS.md (bloco "## Phase 45 — Briefing Contextual do Diretor de Arte" :185)
@.planning/phases/45-briefing-contextual-do-diretor-de-arte/45-CONTEXT.md (D1 :114, D2 :117, D5 :128, D7 :134, D-trackings :137; F45-01..F45-04 :67-70; canonical_refs :141-174)
@.planning/phases/43-revisao-brief-pre-geracao/43-01-PLAN.md (precedente de plano de trackings F43 — estrutura do SUMMARY e padrão de grep)
@openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/design.md (D1 :46-54; D7 :121-123; "Divisão em planos" :155-165 — 45-01 = trackings + greps de consumidores)

<interfaces>
Chaves do mapa atual de `buildPromptVariables` a inventariar (image-generation-service.ts :896-1003, Record com 39 chaves):
- Mortas/de orquestração candidatas à remoção (grep de consumidores): `commercialFrame`, `hasCategoryConflict`, `brandColorsChosen`, `visualStyle`, `visualTone`, `brandPersonality`, `campaignGuidelines`, `campaignBrief`, `identityImageUrl` (provider-only — nunca interpolada), `campaignIntent` (seleção de arquivo — MANTER como orquestração).
Superfícies congeladas (D7 — baseline de não-mudança):
- `src/lib/image-generation/services/image-review-service.ts` + `prompts/campaign-image-reviewer.md`
- `src/lib/copy/*` (Copy Director) + `prompts/campaign-copy-director*.md`
- `src/lib/image-generation/providers/openai.ts` (fallback images.edit — quick 260902-mqj)
- `GenerateImageRequestSchema` (src/lib/image-generation/schema.ts)
- Helpers do form: `buildMandatoryArtworkText`/`buildValidityDisplayText`/`buildCampaignGenerationBody` (use-campaign-form.ts)
Testes atuais (baseline F45-04):
- `src/lib/image-generation/services/__tests__/image-generation-service.test.ts` — golden 39 keys por intent :556-708 (EXPECTED_KEYS :557-569; casos kqo a/b/c :677-708); validatePrompts :65-298; repertório :710+
- `src/lib/campaign/__tests__/prompt-reframe.test.ts` — âncoras atuais (constantes :17-22; testes 16/17/check A/B/21)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep-verificação de trackings F45/F44/Stripe nos 5 runbooks (1.1)</name>
  <files>ROADMAP.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/PROJECT.md, AGENTS.md</files>
  <read_first>
    - .planning/phases/45-briefing-contextual-do-diretor-de-arte/45-CONTEXT.md (:36 — runbook 5 arquivos; :137-138 — D-trackings com commit 371077f7)
    - .planning/phases/43-revisao-brief-pre-geracao/43-01-SUMMARY.md (padrão de SUMMARY de trackings do precedente F43)
  </read_first>
  <action>
    Executar grep-verificação de consistência de trackings F45/F44/Stripe nos 5 runbooks (`ROADMAP.md` raiz, `.planning/ROADMAP.md`, `.planning/STATE.md`, `.planning/PROJECT.md`, `AGENTS.md`) com **padrões precisos de resíduo de estado atual** — NUNCA coringas genéricos que gerem falsos positivos em notas históricas legítimas (CONTEXT :186 — "F44 não pode aparecer como fase numerada no estado atual").

    Padrões de resíduo de ESTADO ATUAL a verificar (devem retornar ZERO nos marcadores de estado atual — tabelas Progress, nota "Phase numbering", current_phase, "Next Phases", seções "### Phase"):
    - `| 44 |`, `Phase 44 (`, `F44 = Temas`, `F44 =`, `44-briefing`, `fase-44`, `| 44 |.*Temas`, `F45 = Stripe`, `Phase 45 (Stripe`, `fase-45-stripe`, `| 45.*Stripe`, `| — |.*Stripe.*| 45`
    - **EXCEÇÕES ÚNICAS documentadas (notas históricas de fases concluídas — NÃO editadas):** menções a "F44" em contexto de "fora da numeração / adicionada pelo runbook da própria F44" e menções históricas a Stripe/F44/F45 dentro de narrativas de fases anteriores (ex.: `.planning/ROADMAP.md` :411/:471 notas históricas F35/F36; descrições de fases concluídas F40-F43). Essas documentam a cadeia de renumeração da época — legítimas e intocadas.

    Em seguida, confirmar POSITIVAMENTE (assertando rótulo/valor, não número de linha):
    - `ROADMAP.md` raiz: linha da tabela de fases contendo "45" e "Briefing Contextual do Diretor de Arte"; linha "Monetização pública / Stripe" como "Fora da numeração / iniciativa diferida".
    - `.planning/ROADMAP.md`: nota "Phase numbering" (:7) com "F45 = Briefing Contextual do Diretor de Arte (v1.5)" e "F44 = Temas de Campanha permanece fora da numeração"; seção "### Phase 45: Briefing Contextual do Diretor de Arte" (:859).
    - `.planning/STATE.md`: `current_phase: 45` (frontmatter :5); linha "F45 ... EM PLANEJAMENTO" (:19).
    - `.planning/PROJECT.md`: linha "Briefing Contextual do Diretor de Arte (F45, v1.5) — EM PLANEJAMENTO" (:52).
    - `AGENTS.md`: bloco "## Phase 45 — Briefing Contextual do Diretor de Arte" (:185) com "Em planejamento — 7 plans previstos".

    NÃO editar nenhum arquivo se os greps confirmarem consistência. Se QUALQUER resíduo de **estado atual** for encontrado (fora das exceções históricas), corrigir APENAS a linha do resíduo (mínimo delta, seguindo o padrão "fora da numeração"). **PROIBIDO** corrigir notas históricas.
  </action>
  <verify>
    <automated>
      $bad = @(); $files = @('ROADMAP.md','.planning\ROADMAP.md','.planning\STATE.md','.planning\PROJECT.md','AGENTS.md'); $state = @('Phase 45 \(Stripe','F45 = Stripe','fase-45-stripe','\| 45.*Stripe'); foreach ($f in $files) { foreach ($p in $state) { if (Select-String -LiteralPath $f -Pattern $p -Quiet) { $bad += "$f [$p]" } } }; if ($bad.Count -gt 0) { Write-Error "Residuos no estado atual: $($bad -join ', ')" } else { Write-Output "OK: zero residuos Stripe-as-F45 no estado atual" }
    </automated>
  </verify>
  <acceptance_criteria>
    - Grep dos padrões de estado atual (Stripe-as-F45) nos 5 runbooks → 0 ocorrências
    - `.planning/ROADMAP.md` tem a seção "### Phase 45: Briefing Contextual do Diretor de Arte" e a nota "Phase numbering" com "F45 = Briefing Contextual do Diretor de Arte (v1.5)" + "F44 = Temas de Campanha permanece fora da numeração"
    - `.planning/STATE.md` frontmatter tem `current_phase: 45`
    - `AGENTS.md` tem o bloco "## Phase 45 — Briefing Contextual do Diretor de Arte" com "Em planejamento"
    - Nenhuma nota histórica de renumeração foi alterada; F44 não aparece como fase numerada no estado atual
  </acceptance_criteria>
  <done>Trackings F45/F44/Stripe consistentes nos 5 runbooks — zero resíduos de estado atual; registro 371077f7 verificado</done>
</task>

<task type="auto">
  <name>Task 2: Inventário de consumidores das chaves do mapa de buildPromptVariables (1.2)</name>
  <files>ROADMAP.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/PROJECT.md, AGENTS.md</files>
  <read_first>
    - src/lib/image-generation/services/image-generation-service.ts (:896-1003 — buildPromptVariables; :1005-1023 — assemblePrompt)
    - openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/design.md (D1 :46-54 — "chaves mortas são removidas após o inventário do 45-01")
  </read_first>
  <action>
    Executar grep de consumidores das chaves candidatas à remoção/realocação do mapa de `buildPromptVariables` (image-generation-service.ts :896-1003) nos diretórios `src/`, `prompts/` e testes. Chaves: `commercialFrame`, `hasCategoryConflict`, `brandColorsChosen`, `visualStyle`, `visualTone`, `brandPersonality`, `campaignGuidelines`, `campaignBrief`, `identityImageUrl`, `campaignIntent`.

    Para cada chave, identificar:
    (a) uso como `{{chave}}` nos templates (`prompts/campaign-image-director*.md`) — confirma consumo real por placeholder;
    (b) uso fora de placeholder (ex.: leitura direta do mapa em assemblePrompt/validatePrompts/outros módulos — `assemblePrompt` usa `variables.campaignIntent` :1007);
    (c) uso em testes/eventos de detail (ex.: fixtures, asserts de key sets, `prompts/campaign-image-director.md` base lido por teste).

    Registrar **tabela de decisão** (manter / remover / mover para bloco contextual) por chave, SEM alterar código:
    - `campaignIntent` → manter (orquestração — seleção de arquivo em assemblePrompt);
    - `identityImageUrl` → manter provider-only fora do template (nunca interpolada; entregue ao provider por `generateWithRetry` :397/`context.identity.imageUrl`);
    - `commercialFrame` → remover (Copy Director tem builder próprio — design D1 "commercialFrame sai do buildPromptVariables do diretor");
    - `hasCategoryConflict`, `brandColorsChosen`, `visualStyle`, `visualTone`, `brandPersonality`, `campaignGuidelines`, `campaignBrief` → remover ou mover para dentro do bloco `creativeDirectionSection` conforme presença real (decisão documentada — D3 mapeia brand profile para direção criativa);
    - demais chaves mortas confirmadas pelo grep → remover após inventário (D1).
    A tabela de decisão é a referência para o 45-03 (remoção/realocação de chaves no mapa + delegação ao módulo) e para o 45-05 (co-migração de testes). Não alterar código nesta task.
  </action>
  <verify>
    <automated>
      Select-String -LiteralPath "src\lib\image-generation\services\image-generation-service.ts" -Pattern "campaignIntent" | Select-Object -First 1 | Out-Null; Write-Output "grep de consumidores executado — ver anexo do SUMMARY"
    </automated>
  </verify>
  <acceptance_criteria>
    - Inventário executado com grep em `src/`, `prompts/` e testes para as 10 chaves
    - Tabela de decisão (manter/remover/mover) documentada no SUMMARY por chave, com evidência de uso (arquivo+linha)
    - Nenhuma linha de código alterada (git diff do plano = apenas SUMMARY)
  </acceptance_criteria>
  <done>Inventário de consumidores registrado — base factual para remoção de chaves no 45-03 e co-migração no 45-05</done>
</task>

<task type="auto">
  <name>Task 3: Baseline de superfícies congeladas (1.3) + baseline de testes e decisão do arquivo base (1.4) + SUMMARY</name>
  <files>.planning/phases/45-briefing-contextual-do-diretor-de-arte/45-01-SUMMARY.md</files>
  <read_first>
    - openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/design.md (D7 :121-123; Mapping :125-146)
    - src/lib/image-generation/services/__tests__/image-generation-service.test.ts (:556-708 golden; :65-298 validatePrompts)
    - src/lib/campaign/__tests__/prompt-reframe.test.ts (constantes/âncoras :17-22)
  </read_first>
  <action>
    1. Confirmar com grep as superfícies congeladas e registrar baseline de NÃO-mudança (D7): `image-review-service.ts` + `campaign-image-reviewer.md`, Copy Director (`src/lib/copy/*` + `prompts/campaign-copy-director*.md`), `providers/openai.ts` (fallback images.edit — quick 260902-mqj), `GenerateImageRequestSchema` (schema.ts), helpers do form (`buildMandatoryArtworkText`, `buildValidityDisplayText`, `buildCampaignGenerationBody` em use-campaign-form.ts), domínio `CampaignBrief`/`brief.ts`, snapshot. Documentar caminhos + papel de cada um (congelado — só leitura na F45).
    2. Registrar baseline de testes atuais (F45-04): golden 39 keys por intent (`image-generation-service.test.ts` :556-708 — describe "golden tests por intent", EXPECTED_KEYS :557-569, casos kqo :677-708), `validatePrompts` (:65-298), repertório (:710+), `prompt-reframe.test.ts` (4 prompts lidos do disco, âncoras :17-22 e testes 16/17/check A/B/21). Anotar o drift spec 38 × runtime 39 (CONTEXT :23) que a F45 reconcilia (design D5).
    3. Decisão sobre o arquivo base `campaign-image-director.md`: MANTIDO em sync como referência offer/geral (não usado pelo runtime — `assemblePrompt` seleciona `campaign-image-director-${intent}` :1006 — mas **lido por teste** em `prompt-reframe.test.ts` :10-15) — não remover do runtime/testes (D2/D5; design :187 item 3).
    4. Criar `.planning/phases/45-briefing-contextual-do-diretor-de-arte/45-01-SUMMARY.md` (template `templates/summary.md`) documentando: (a) grep-verificação da Task 1 com padrões e resultado; (b) inventário/tabela de decisão da Task 2; (c) baseline de superfícies congeladas; (d) baseline de testes + decisão do arquivo base; (e) referência à fonte da verdade `openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/`. Sem menção de conteúdo além de nota de que a fase terá 7 plans (45-01..45-07).
  </action>
  <verify>
    <automated>Select-String -LiteralPath ".planning\phases\45-briefing-contextual-do-diretor-de-arte\45-01-SUMMARY.md" -Pattern "F45 = Briefing Contextual|39 keys|campaign-image-director.md (base)"</automated>
  </verify>
  <acceptance_criteria>
    - `45-01-SUMMARY.md` existe e contém: resultado do grep F45/F44/Stripe, tabela de decisão das chaves, baseline de superfícies congeladas, baseline de testes (golden 39, validatePrompts, prompt-reframe), decisão de manter o arquivo base
    - Nenhum arquivo de código ou superfície congelada foi alterado (git status do plano mostra apenas SUMMARY)
  </acceptance_criteria>
  <done>45-01-SUMMARY.md criado com grep-verificação, inventário e baselines (entrada para 45-02/45-03/45-05)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runbook ↔ docs | Arquivos de documentação editados manualmente — risco de drift de nomenclatura F44/F45 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Tampering | 5 runbooks + ROADMAP raiz | mitigate | Grep-verificação com padrões precisos de resíduo de estado atual (Task 1); correção mínima de delta proibida em notas históricas (D-trackings) |
| T-45-01a | Information Disclosure | Documentação | accept | Sem dados sensíveis — apenas nomenclatura de fases e caminhos de arquivos |
| T-45-01b | Elevation of Privilege | Superfícies congeladas | mitigate | Baseline de não-mudança (Task 3) registra revisor/copy/fallback/schema/form helpers como somente-leitura — nenhum plano da F45 os altera (D7) |
</threat_model>

<verification>
- Grep de padrões de estado atual (Stripe-as-F45) → 0 ocorrências nos 5 runbooks; F44 ausente como fase numerada
- `.planning/ROADMAP.md` tem seção "### Phase 45" e nota "Phase numbering" com F45 = Briefing Contextual + F44 fora da numeração
- Inventário das 10 chaves com tabela de decisão no SUMMARY
- Baseline de superfícies congeladas + baseline de testes + decisão do arquivo base no SUMMARY
- Nenhuma linha de código alterada neste plano
</verification>

<success_criteria>
- Trackings F45/F44/Stripe verificados nos 5 runbooks (registro 371077f7) — zero resíduos no estado atual
- Inventário de consumidores registrado (pré-requisito D1 para remoção de chaves no 45-03)
- Superfícies congeladas com baseline de não-mudança (D7)
- Baseline de testes (golden 39, prompt-reframe, validatePrompts) + decisão do arquivo base mantido em sync
- 45-01-SUMMARY.md criado
</success_criteria>

<output>
Create `.planning/phases/45-briefing-contextual-do-diretor-de-arte/45-01-SUMMARY.md` when done
</output>

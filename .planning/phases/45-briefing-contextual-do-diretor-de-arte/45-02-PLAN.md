---
phase: 45-briefing-contextual-do-diretor-de-arte
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/image-generation/services/art-director-briefing.ts
  - src/lib/image-generation/services/image-generation-service.ts
  - src/lib/image-generation/services/__tests__/art-director-briefing.test.ts
autonomous: true
requirements: [F45-05, F45-06, F45-07]

must_haves:
  truths:
    - "Novo módulo puro `src/lib/image-generation/services/art-director-briefing.ts` exporta `formatPriceBRL`, `splitDirectorLegalText`, `sanitizePromptText` (cópia pura, disponível mas NÃO aplicada), `buildCommercialRepertoire`, `buildValidationSummary`, `buildCreativeContextGuidance`, `buildBrandProfileSection` — regras idênticas às atuais do service (D1)"
    - "`buildPromptVariables` do service delega aos módulos/funções puras mantendo SAÍDA IDÊNTICA — mesmo Record de 39 chaves, mesmos valores (paridade garantida; golden tests atuais verdes) (D1/D5)"
    - "`sanitizePromptText` portado como cópia pura (regra `{{`→`{`, `}}`→`}` do revisor :186-188) sem alterar `image-review-service.ts` e sem ser aplicado ao prompt final do diretor nesta etapa (D1/D6)"
    - "Nenhum template `.md` foi alterado nesta etapa (só extração); `image-review-service.ts` intocado (D7)"
  artifacts:
    - path: "src/lib/image-generation/services/art-director-briefing.ts"
      provides: "Módulo puro com os builders extraídos + utilitários"
      contains: "export function buildCommercialRepertoire"
    - path: "src/lib/image-generation/services/__tests__/art-director-briefing.test.ts"
      provides: "Testes unitários iniciais do módulo puro"
      contains: "buildCommercialRepertoire"
  key_links:
    - from: "src/lib/image-generation/services/image-generation-service.ts"
      to: "src/lib/image-generation/services/art-director-briefing.ts"
      via: "import das funções puras (delegação com saída idêntica)"
      pattern: "from \"@/lib/image-generation/services/art-director-briefing\""
---

<objective>
Criar o **módulo puro `src/lib/image-generation/services/art-director-briefing.ts`** (D1) extraindo **sem mudança de comportamento** os builders atuais do diretor (`commercialRepertoire`, `validationSummary`, `creativeContextGuidance`, `brandProfileSection`), `splitDirectorLegalText`, formatação de preço e o `sanitizePromptText` como **cópia pura disponível (sem aplicá-la ao prompt final nesta etapa)**. Delegar `buildPromptVariables` do service ao módulo mantendo **saída idêntica** (paridade garantida por gates/golden atuais). Criar testes unitários iniciais do módulo.

Purpose: D1 (módulo puro concentra a composição — pré-requisito do 45-03) + D6 (cópia pura do `sanitizePromptText` disponível para o 45-03) — a extração em 2 passos garante que o refactor NÃO muda comportamento antes da reescrita contextual.
Output: `art-director-briefing.ts` (novo) + delegação no service + `__tests__/art-director-briefing.test.ts` (novo); 4 gates verdes.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-briefing-contextual-do-diretor-de-arte/45-CONTEXT.md (D1 :114, D6 :131; F45-05/06/07 :71-73; canonical_refs código :153-168)
@openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/design.md (D1 :46-54; Mapping :127-146)
@src/lib/image-generation/services/image-generation-service.ts (módulo de origem — 1269 linhas)
@src/lib/image-generation/services/image-review-service.ts (:186-188 — sanitizePromptText de referência; NÃO alterar)

<interfaces>
Funções a extrair (estado real verificado 2026-09-02 — image-generation-service.ts):

1. `splitDirectorLegalText(combined: string): { merchantText: string; illustrativeNotice: string }` — hoje função de escopo de módulo no service (:80-91); depende de `ILLUSTRATIVE_NOTICE_TEXT` (`@/lib/campaign/constants`).
2. `formatPriceBRL(cents: number | undefined): string` — hoje método privado (:1025-1031); `(cents / 100).toLocaleString("pt-BR", { style: "currency", currency: "BRL" })`; retorna `""` para `undefined`/`null`.
3. `sanitizePromptText(value: string): string` — cópia pura da regra do revisor (`image-review-service.ts:186-188`): `value.replace(/\{\{/g, "{").replace(/\}\}/g, "}")`. NÃO alterar o revisor; NÃO aplicar ao prompt do diretor no 45-02.
4. `buildCommercialRepertoire(brief: CampaignBrief): string` — hoje método privado (:761-814); regras: availabilityNotes keyword-gated (scarcity: "poucas unidades","últimas","limitado","estoque" → prefixo "Disponível:"/"Disponibilidade:" por intent; variety: "vários sabores","cores variadas","diversos","várias" → "Variedade disponível:"), validade só offer+enabled+displayText (`- Oferta válida: {displayText}`), details/additionalDetails com `replace(/[\[\]]/g, "").trim()`; debug log sob `IMAGE_GENERATION_DEBUG` (`@/lib/image-generation/config`).
5. `buildValidationSummary(brief: CampaignBrief, context: ResolvedCampaignContext, effectiveProductName: string): string` — hoje método privado (:816-828); duas linhas possíveis: nome corrigido (`brief.product.name !== effectiveProductName`) e `user_confirmed_continue`.
6. `buildCreativeContextGuidance(segment: string, category: string, hasConflict: boolean, campaignIntent: string = "offer"): string` — hoje método privado (:835-894); matriz segmento×categoria + sufixos por intent (spotlight: "Apresentar como destaque ou novidade, sem urgência..."; exclusive: "Valor percebido e exclusividade..."; offer: "Preço é oportunidade."). Usa `segment.toLowerCase()`/`category.toLowerCase()`.
7. `buildBrandProfileSection(brandProfile: BrandProfileSnapshot | null): string` — hoje método privado (:1209-1246); tipo estrutural inline no service (campos `brand_colors_chosen`, `safe_color_tokens`, `visual_style`, `visual_tone`, `brand_personality`, `campaign_guidelines`, `campaign_brief`) — importar o tipo canônico `BrandProfileSnapshot` de `@/components/campaign/types`.

Tipos de domínio (importar, não redefinir): `CampaignBrief` de `@/lib/campaign/brief`; `ResolvedCampaignContext`/`BrandProfileSnapshot` de `@/components/campaign/types`.

Delegação no service: `buildPromptVariables` (:896-1003) monta o Record de 39 chaves chamando `this.formatPriceBRL`, `this.buildCommercialRepertoire` (:915), `this.buildValidationSummary` (:916), `this.buildCreativeContextGuidance` (:917), `this.buildBrandProfileSection` (:986), `splitDirectorLegalText` (:944). O `prompt-loader` continua injetado via construtor; `mediaImagesDataUrls`/`primaryImageDataUrl` (:1035-1045) permanecem no service (não fazem parte da extração).

Restrições:
- SAÍDA IDÊNTICA obrigatória no 45-02 — nenhum texto de prompt muda; nenhum `.md` é tocado; nenhuma chave do Record muda (remoção/realocação é do 45-03, após inventário do 45-01).
- Golden tests atuais (`image-generation-service.test.ts` :556-708 — 39 keys por intent) DEVEM continuar verdes.
- `image-review-service.ts` intocado (D7); Copy Director intocado.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar art-director-briefing.ts com as funções puras extraídas (sem mudança de comportamento)</name>
  <files>src/lib/image-generation/services/art-director-briefing.ts</files>
  <read_first>
    - src/lib/image-generation/services/image-generation-service.ts (:80-91 splitDirectorLegalText; :761-814 buildCommercialRepertoire; :816-828 buildValidationSummary; :835-894 buildCreativeContextGuidance; :1025-1031 formatPriceBRL; :1209-1246 buildBrandProfileSection)
    - src/lib/image-generation/services/image-review-service.ts (:186-188 — sanitizePromptText de referência, regra exata)
    - src/lib/campaign/brief.ts (tipo CampaignBrief)
    - src/components/campaign/types.ts (ResolvedCampaignContext, BrandProfileSnapshot)
  </read_first>
  <action>
    Criar `src/lib/image-generation/services/art-director-briefing.ts` exportando APENAS funções puras (sem classes, sem estado, sem dependência do PromptLoader), copiando o corpo das funções atuais SEM alterar regra nem texto:

    1. `formatPriceBRL(cents: number | undefined): string` — corpo idêntico ao do service (:1025-1031).
    2. `splitDirectorLegalText(combined: string): { merchantText: string; illustrativeNotice: string }` — corpo idêntico ao do service (:80-91); importar `ILLUSTRATIVE_NOTICE_TEXT` de `@/lib/campaign/constants`.
    3. `sanitizePromptText(value: string): string` — cópia pura da regra do revisor (`/\{\{/g → "{"`, `/\}\}/g → "}"`); NÃO importar do revisor (evita acoplamento); NÃO chamar em lugar nenhum no 45-02 (disponível para o 45-03).
    4. `buildCommercialRepertoire(brief: CampaignBrief): string` — corpo idêntico ao do service (:761-814), incluindo a keyword-gate de availability, validade offer-only e o strip `[\[\]]` de details; importar `IMAGE_GENERATION_DEBUG` de `@/lib/image-generation/config` e preservar o bloco de debug log com o mesmo JSON (paridade de observabilidade).
    5. `buildValidationSummary(brief: CampaignBrief, context: ResolvedCampaignContext, effectiveProductName: string): string` — corpo idêntico (:816-828).
    6. `buildCreativeContextGuidance(segment: string, category: string, hasConflict: boolean, campaignIntent?: string): string` — corpo idêntico (:835-894); default `"offer"` preservado.
    7. `buildBrandProfileSection(brandProfile: BrandProfileSnapshot | null): string` — corpo idêntico (:1209-1246), tipando com `BrandProfileSnapshot` importado de `@/components/campaign/types` (a assinatura inline atual do service tem exatamente esses campos — compatível); incluir a `note` de "contexto direcional" e a supressão quando `rows.length <= 2`.

    NÃO adicionar código de comentário. NÃO adicionar funções além das listadas (os blocos contextuais novos — `campaignFactsSection` etc. — são escopo do 45-03). NÃO importar nada do `image-generation-service.ts` (zero ciclo). Garantir que o arquivo não importa `server-only` (funções puras testáveis em vitest node).
  </action>
  <verify>
    <automated>
      Select-String -LiteralPath "src\lib\image-generation\services\art-director-briefing.ts" -Pattern "export function (formatPriceBRL|splitDirectorLegalText|sanitizePromptText|buildCommercialRepertoire|buildValidationSummary|buildCreativeContextGuidance|buildBrandProfileSection)" | Measure-Object | Select-Object -ExpandProperty Count
    </automated>
  </verify>
  <acceptance_criteria>
    - `art-director-briefing.ts` existe e exporta as 7 funções (grep = 7 ocorrências de `export function`)
    - Corpos das funções reproduzem exatamente a lógica atual do service (sem refactor de regra no 45-02)
    - Não importa `image-generation-service.ts` nem `image-review-service.ts`; tipos importados de `@/lib/campaign/brief` e `@/components/campaign/types`
    - `sanitizePromptText` exportado mas não invocado em lugar nenhum do módulo
  </acceptance_criteria>
  <done>art-director-briefing.ts criado com as 7 funções puras (extração fiel, sem mudança de comportamento)</done>
</task>

<task type="auto">
  <name>Task 2: Delegar buildPromptVariables do service ao módulo mantendo saída idêntica</name>
  <files>src/lib/image-generation/services/image-generation-service.ts</files>
  <read_first>
    - src/lib/image-generation/services/image-generation-service.ts (topo do arquivo :1-20 imports; :80-91 função splitDirectorLegalText; :637-708 validatePrompts; :761-828 builders; :835-894 creativeContextGuidance; :896-1003 buildPromptVariables; :1025-1031 formatPriceBRL; :1209-1246 buildBrandProfileSection)
    - src/lib/image-generation/services/art-director-briefing.ts (recém-criado)
  </read_first>
  <action>
    Refatorar `image-generation-service.ts` para **delegar ao módulo puro** sem alterar o Record produzido por `buildPromptVariables` (39 chaves, mesmos valores):

    1. Importar de `./art-director-briefing` (caminho relativo): `formatPriceBRL`, `splitDirectorLegalText`, `buildCommercialRepertoire`, `buildValidationSummary`, `buildCreativeContextGuidance`, `buildBrandProfileSection`. (NÃO importar `sanitizePromptText` ainda — será usado no 45-03.)
    2. Remover a função de escopo de módulo `splitDirectorLegalText` do service (:80-91) — o call site em `buildPromptVariables` (:944) passa a usar a importação do módulo.
    3. Converter os métodos privados `formatPriceBRL` (:1025-1031), `buildCommercialRepertoire` (:761-814), `buildValidationSummary` (:816-828), `buildCreativeContextGuidance` (:835-894) e `buildBrandProfileSection` (:1209-1246) em **delegações finas** (uma linha) para as funções importadas — MANTENDO as assinaturas privadas com os mesmos nomes (necessário: o teste atual acessa `(service as any).buildCommercialRepertoire` em `image-generation-service.test.ts` :710-717; e `buildPromptVariables` :896-1003 continua chamando `this.*` — o corpo não muda). Alternativa aceita: chamar as funções do módulo diretamente nos call sites e remover os métodos privados — porém isso exige co-migrar o teste :710-717, que é escopo do 45-05; **preferir a delegação fina** para manter a paridade sem tocar em testes nesta etapa.
    4. NÃO alterar `buildPromptVariables` no 45-02 além do necessário à delegação (nenhuma chave nova, nenhuma chave removida, nenhum valor diferente). NÃO tocar `validatePrompts` (:637-708), `assemblePrompt` (:1005-1023), `mediaImagesDataUrls`, `primaryImageDataUrl`.
    5. Rodar os 4 gates: `npx vitest run` (golden 39 keys por intent :556-708 DEVE passar), `npm run typecheck`, `npm run lint`, `npm run build`.
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\lib\image-generation\services\image-generation-service.ts" -Pattern "art-director-briefing" | Select-Object -First 1 | Out-Null; npx vitest run src/lib/image-generation/services/__tests__/image-generation-service.test.ts; if ($?) { Write-Output "OK: paridade mantida (golden 39 keys verdes)" }</automated>
  </verify>
  <acceptance_criteria>
    - Service importa as 6 funções de `./art-director-briefing` (grep `art-director-briefing` no import)
    - `splitDirectorLegalText` local removida do service; call sites usam a importação
    - `buildPromptVariables` produz Record idêntico ao anterior (39 chaves; valores iguais) — comprovado por `image-generation-service.test.ts` :556-708 verde sem alteração do arquivo de teste
    - `npx vitest run src/lib/image-generation/services/__tests__/image-generation-service.test.ts` — zero falhas
    - `npm run typecheck` — zero erros; `npm run lint` — zero erros
  </acceptance_criteria>
  <done>buildPromptVariables delega ao módulo puro com saída idêntica — golden 39 keys verdes, templates .md intocados</done>
</task>

<task type="auto">
  <name>Task 3: Testes unitários iniciais do módulo puro (mesmas asserções dos builders atuais movidas)</name>
  <files>src/lib/image-generation/services/__tests__/art-director-briefing.test.ts</files>
  <read_first>
    - src/lib/image-generation/services/art-director-briefing.ts (recém-criado)
    - src/lib/image-generation/services/__tests__/image-generation-service.test.ts (:18-63 helpers createMinimalBrief/createContext — reproduzir localmente; :710-717 asserções atuais de buildCommercialRepertoire)
  </read_first>
  <action>
    Criar `src/lib/image-generation/services/__tests__/art-director-briefing.test.ts` com testes unitários DIRETOS do módulo (sem mock de PromptLoader, sem instanciar ImageGenerationService):

    1. Reproduzir localmente os helpers `createMinimalBrief`/`createContext` do `image-generation-service.test.ts` (:18-63) — mesmo STORE_ID, mesmo payload mínimo (`productName: 'Produto Teste'`, `discountedPriceCents: 1990`, `badgeText: 'Oferta'`, `campaignIntent: 'offer'`, `productImageDataUrl`), usando `buildCampaignBriefFromFlat` de `@/lib/campaign/brief`.
    2. **buildCommercialRepertoire** — mover as asserções atuais do service (:710-717): com `validity.enabled`+`displayText` em offer → contém "Oferta válida: {displayText}"; brief mínimo sem validade → sem linha de validade; availability com keyword de escassez → prefixo correto por intent (offer: "Disponível:", exclusive: "Disponibilidade:"); sem keyword → sem linha; details com `[colchetes]` → strip aplicado.
    3. **splitDirectorLegalText** — 3 casos do quick 260902-kqo: (a) combined == `ILLUSTRATIVE_NOTICE_TEXT` → merchantText `""` + illustrativeNotice canônico; (b) startsWith constante + `\n` → split texto livre; (c) texto livre apenas → integral em merchantText, illustrativeNotice `""`.
    4. **formatPriceBRL** — `1990` → "R$ 19,90"; `undefined`/`null` → `""`.
    5. **sanitizePromptText** — texto com `{{exemplo}}` → `{exemplo}` (ambas as regras); texto sem chaves → inalterado (cópia pura disponível, ainda não aplicada no fluxo).
    6. **buildValidationSummary** — `effectiveProductName !== brief.product.name` → linha de nome corrigido; `productImageCheck: "user_confirmed_continue"` → linha de confirmação; nenhum dos dois → `""`.
    7. **buildCreativeContextGuidance** — spot-check: conflito com categoria "bebida" → frase de bebida; offer com segmento moda e sem conflito → sufixo "Preço é oportunidade."; spotlight → sem urgência; exclusive → sem preço.
    8. **buildBrandProfileSection** — sem perfil → `""`; perfil com 1 campo → `""` (regra `rows.length <= 2`); perfil com 2+ campos → nota direcional + linhas de tabela.
    NÃO adicionar comentários no código de teste. NÃO mockar módulo. Rodar o arquivo isolado.
  </action>
  <verify>
    <automated>npx vitest run src/lib/image-generation/services/__tests__/art-director-briefing.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `art-director-briefing.test.ts` criado com describes por função pura (formatPriceBRL, splitDirectorLegalText, sanitizePromptText, buildCommercialRepertoire, buildValidationSummary, buildCreativeContextGuidance, buildBrandProfileSection)
    - Casos kqo a/b/c de splitDirectorLegalText cobertos; validação de formato de preço pt-BR coberta
    - `npx vitest run src/lib/image-generation/services/__tests__/art-director-briefing.test.ts` — zero falhas
    - Nenhum outro arquivo de teste alterado nesta task
  </acceptance_criteria>
  <done>Testes unitários iniciais do módulo puro criados e verdes (asserções dos builders atuais movidas)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service ↔ módulo puro | Refactor de extração — regras movidas entre arquivos; risco de drift de comportamento |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-02 | Tampering | image-generation-service.ts (extração) | mitigate | Delegação fina com SAÍDA IDÊNTICA obrigatória; golden 39 keys por intent (:556-708) + validatePrompts (:65-298) verdes sem tocar no arquivo de teste; gates typecheck/lint/build |
| T-45-02a | Tampering | art-director-briefing.ts (funções puras) | mitigate | Corpos copiados sem alteração de regra; testes unitários diretos do módulo (Task 3) fixam o comportamento extraído |
| T-45-02b | Information Disclosure | sanitizePromptText / textos | accept | Função pura de saneamento de texto do lojista; sem dados sensíveis adicionais (mesmo fluxo atual) |
| T-45-02c | Tampering | image-review-service.ts | mitigate | Arquivo do revisor NÃO é tocado (D7); sanitizePromptText é cópia pura, não importação do revisor |
| T-45-SC | Tampering | npm installs | accept | Nenhum pacote novo (extração de código existente) — sem gate de legitimidade |
</threat_model>

<verification>
- `art-director-briefing.ts` exporta as 7 funções puras com regras idênticas ao service atual
- `image-generation-service.ts` importa do módulo e delega (métodos privados finos); `splitDirectorLegalText` local removida
- `npx vitest run src/lib/image-generation/services/__tests__/image-generation-service.test.ts` — golden 39 keys por intent verdes (paridade D1/D5)
- `npx vitest run src/lib/image-generation/services/__tests__/art-director-briefing.test.ts` — novo, verde
- `npm run typecheck`, `npm run lint`, `npm run build` — zero erros
- Nenhum `.md` alterado; `image-review-service.ts` intocado (git status do plano)
</verification>

<success_criteria>
- Módulo puro `art-director-briefing.ts` criado (D1) com extração SEM mudança de comportamento
- `buildPromptVariables` delega ao módulo com saída idêntica — paridade garantida (golden verdes)
- `sanitizePromptText` disponível como cópia pura (D6) — não aplicada nesta etapa
- Testes unitários iniciais do módulo criados; 4 gates verdes
- Superfícies congeladas intocadas (D7)
</success_criteria>

<output>
Create `.planning/phases/45-briefing-contextual-do-diretor-de-arte/45-02-SUMMARY.md` when done
</output>

---
phase: 45-briefing-contextual-do-diretor-de-arte
plan: 03
type: execute
wave: 2
depends_on: [45-01, 45-02]
files_modified:
  - prompts/campaign-image-director.md
  - prompts/campaign-image-director-offer.md
  - src/lib/image-generation/services/art-director-briefing.ts
  - src/lib/image-generation/services/image-generation-service.ts
  - src/lib/image-generation/services/__tests__/art-director-briefing.test.ts
  - src/lib/image-generation/services/__tests__/image-generation-service.test.ts
  - src/lib/campaign/__tests__/prompt-reframe.test.ts
autonomous: true
requirements: [F45-08, F45-09, F45-10, F45-11, F45-12, F45-13, F45-18, F45-19, F45-20]

must_haves:
  truths:
    - "`campaign-image-director.md` (base) e `campaign-image-director-offer.md` reescritos em camada editorial legível + slots de bloco nomeados (D2): headings fixos no .md para campaignFactsSection/identityReferenceSection/productReferenceSection/creativeDirectionSection; placeholders de bloco inteiro para commercialDetailsSection/requiredArtworkTextSection/illustrativeNoticeSection/constraintsSection"
    - "Reescrita com MÃO LEVE — texto existente reaproveitado/movido, sem modernizar vocabulário nem paráfrase desnecessária (validação por âncoras de conteúdo)"
    - "Helper monta blocos de offer por presença real de dados (D3): validade em ocorrência ÚNICA em campaignFactsSection quando offer+validity.enabled; details/availability keyword-gated em commercialDetailsSection; texto obrigatório saneado em requiredArtworkTextSection; aviso em illustrativeNoticeSection; nada vazio/heading órfão/linha de tabela em branco/placeholder residual"
    - "buildCommercialRepertoire repartido (D3): validity → campaignFactsSection; details/availability → commercialDetailsSection; repertório recomposto sem reintroduzir validity/details — repartição aplicada à composição dos blocos consumidos pelos templates offer/base reescritos; valores legados transicionais de spotlight/exclusive preservados até o 45-04"
    - "Mapa TRANSICIONAL de buildPromptVariables (D5): novas chaves de slot consumidas pelos templates offer/base reescritos ADICIONADAS; chaves legadas ainda referenciadas pelos templates de spotlight/exclusive (não reescritos neste plano) MANTIDAS com valores produzidos (mesmos builders extraídos do 45-02 — comportamento preservado para esses intents); removidas APENAS as chaves mortas em TODOS os templates (tabela de decisão do 45-01-SUMMARY: ex.: commercialFrame, identityImageUrl provider-only, hasCategoryConflict e demais sem consumidor). Placeholders de TODOS os intents continuam resolvendo (validatePrompts real-loader valid=true para offer/spotlight/exclusive)"
    - "Suites-alvo co-migradas NO PRÓPRIO 45-03 (não há suites vermelhas herdadas pelo 45-05): golden key-set (39 → conjunto transicional) + 8.17 repertório em image-generation-service.test.ts (todos os intents — mapa compartilhado; casos kqo a/b/c verificados verdes com asserts de valor sobre chaves legadas sobreviventes); novas âncoras de prompt-reframe.test.ts para base/offer (spotlight/exclusive mantêm âncoras atuais até o 45-04); casos de validatePrompts do diretor para offer — `npx vitest run` 100% VERDE ao final do plano"
    - "`art-director-briefing.test.ts` (criado no 45-02) co-migrado NA Task 2 deste plano — asserções legadas cujos alvos o rework alterou (validade/details do repertório repartido; `buildValidationSummary` se removida/recombinada) removidas/adaptadas, mantidas as que seguem alimentando chaves legadas na transição; arquivo VERDE ao final do 45-03 (o 45-05 o amplia apenas de forma aditiva)"
    - "validatePrompts/assemblePrompt adaptados: zero placeholders não resolvidos por intent; rodapé CORRECT/REGENERATE mantido (D4/D5)"
  artifacts:
    - path: "prompts/campaign-image-director-offer.md"
      provides: "Template offer em estrutura editorial + blocos"
      contains: "{{campaignFactsSection}}"
    - path: "prompts/campaign-image-director.md"
      provides: "Template base (referência offer/geral, lido por teste) em estrutura editorial + blocos"
      contains: "{{campaignFactsSection}}"
    - path: "src/lib/image-generation/services/art-director-briefing.ts"
      provides: "Blocos contextuais de offer no helper puro"
      contains: "campaignFactsSection"
    - path: "src/lib/image-generation/services/__tests__/image-generation-service.test.ts"
      provides: "Golden key-set co-migrado ao conjunto transicional (todos os intents — mapa compartilhado)"
      contains: "campaignFactsSection"
    - path: "src/lib/campaign/__tests__/prompt-reframe.test.ts"
      provides: "Âncoras editoriais novas para base/offer (spotlight/exclusive com âncoras atuais até o 45-04)"
      contains: "campaign-image-director-offer"
  key_links:
    - from: "src/lib/image-generation/services/art-director-briefing.ts"
      to: "prompts/campaign-image-director-offer.md"
      via: "buildPromptVariables delega; PromptLoader.load interpola os slots de bloco"
      pattern: "campaignFactsSection"
    - from: "src/lib/image-generation/services/image-generation-service.ts (validatePrompts)"
      to: "prompts/campaign-image-director-offer.md"
      via: "carregamento do template por intent (campaign-image-director-${intent})"
      pattern: "campaign-image-director-\\$\\{campaignIntent\\}"
    - from: "src/lib/image-generation/services/__tests__/image-generation-service.test.ts"
      to: "src/lib/image-generation/services/image-generation-service.ts"
      via: "asserção do conjunto transicional de chaves sobre o Record compartilhado de buildPromptVariables"
      pattern: "campaignFactsSection"
---

<objective>
Reescrever `prompts/campaign-image-director.md` (base) e `campaign-image-director-offer.md` em **camada editorial fixa + slots de bloco contextual** (D2/D3) com **mão leve**, e implementar no helper `art-director-briefing.ts` a **montagem contextual para offer** (D3): `campaignFactsSection`, `commercialDetailsSection`, `requiredArtworkTextSection`, `illustrativeNoticeSection`, `identityReferenceSection`, `productReferenceSection`, `constraintsSection`, `creativeDirectionSection` — removendo duplicações (validade, detalhes, disponibilidade, aviso e texto obrigatório em **bloco canônico único**) e aplicando o saneamento `{{`→`{`, `}}`→`}` nos blocos novos (D6). Adaptar `validatePrompts`/`assemblePrompt` (rodapé CORRECT/REGENERATE mantido).

Purpose: D2 (templates em duas camadas) + D3 (blocos por presença real, deduplicação) + D4 (sem seções vazias/placeholders residuais) + D6 (saneamento aplicado pela 1ª vez nos blocos novos) + D5 (contrato interno novo para offer via mapa TRANSICIONAL: spotlight/exclusive não reescritos seguem com placeholders resolvidos). Gates 100% verdes ao final do plano — as suites-alvo de teste (golden key-set, prompt-reframe base/offer, validatePrompts offer) são co-migradas NO PRÓPRIO 45-03, sem pendências herdadas pelo 45-05 (green-gates redesign; F45-18/F45-19/F45-20 em cobertura parcial aqui).
Output: base + offer reescritos (editorial + slots), blocos de offer no helper, service delegando montagem contextual de offer com mapa transicional (chaves legadas de spotlight/exclusive mantidas), prompt offer montado sem vazios/duplicação, co-migração in-plan do `art-director-briefing.test.ts` (Task 2), dos golden key-set/8.17/kqo em `image-generation-service.test.ts` e das âncoras de base/offer em `prompt-reframe.test.ts` (Task 3) — `npx vitest run` VERDE ao final.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-briefing-contextual-do-diretor-de-arte/45-CONTEXT.md (D1-D7; F45-08..F45-13 :74-79; F45-18..F45-20 :84-86 — cobertura parcial neste plano; canonical_refs :141-174)
@.planning/phases/45-briefing-contextual-do-diretor-de-arte/45-01-SUMMARY.md (inventário de consumidores + tabela de decisão de chaves — base para remoção/realocação)
@openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/design.md (D2 :56-71; D3 :73-99 — tabela de blocos e repartição do repertório; D4 :101-108; D5 :110-115; D6 :117-119)
@openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/specs/art-director-contextual-briefing/spec.md (Requisitos: estrutura editorial + blocos; prompt contextual; texto obrigatório em seção própria; aviso em seção própria)
@openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/tasks.md (§3 — tasks 3.1-3.6)
@prompts/campaign-image-director.md (135 linhas — base atual, lido por teste)
@prompts/campaign-image-director-offer.md (136 linhas — tabela L9-31, Notas Adicionais L74-88, Direção Criativa L89-131, cauda L134-136)

<interfaces>
Estrutura-alvo dos templates (D2 — regra de ouro: todo conteúdo que pode estar ausente carrega o próprio heading dentro do valor do bloco; conteúdo garantido tem heading fixo no `.md`):

- Heading FIXO no `.md` (sempre com conteúdo útil):
  - `{{campaignFactsSection}}` — bullets só com campos presentes (loja, segmento, tom, produto, preço por intent, badge se houver, hook/CTA/objetivo se houver, canal/formato); validade só aqui quando offer + validity.enabled
  - `{{identityReferenceSection}}` — assinatura da loja + identityDirective; preservação explícita quando logo/VS com ativo
  - `{{productReferenceSection}}` — primary = referência factual forte; 2+ imagens → auxiliares sem competir; preserveImageContext (não-offer) → não recortar
  - `{{creativeDirectionSection}}` — persona, categoria, conflict (só conflito), guidance (só não-vazio), repertório recomposto, brand profile (só quando perfil)
- Heading DENTRO do valor (condicionais — ausência = bloco não enviado):
  - `{{commercialDetailsSection}}` — campaignDetails/additionalDetails (strip `[\[\]]`) + availabilityNotes keyword-gated; rotulado "não obrigatório / repertório para inspiração"
  - `{{requiredArtworkTextSection}}` — só merchantText não-vazio; valor saneado; "respeitar, visível e legível, não repetir em legenda"
  - `{{illustrativeNoticeSection}}` — aviso habilitado; instrução única: mínimo, legível, discreto, separado dos demais textos, nas laterais
  - `{{constraintsSection}}` — só sensitiveConstraints com conteúdo; "Restrições sensíveis informadas pelo lojista"

Repartição do `buildCommercialRepertoire` (D3 :95): validity → campaignFactsSection; details/availability → commercialDetailsSection; o creativeDirectionSection NÃO recebe validity/details de volta. `brandProfileSection` pertence à direção criativa (não à "identidade").

Dados garantidos por intent podem permanecer como `{{campo}}` em prosa editorial (ex.: `{{productName}}`, `{{storeName}}`, `{{brandColor}}`) ou em frase autoprotegida ("quando houver…"). O restante vive nos blocos.

Referências de conteúdo atuais a REAPROVEITAR com mão leve (mover de lugar, não parafrasear): cauda do aviso L134 e do texto obrigatório L135-136 (offer/base); "REGRAS CRÍTICAS DE FIDELIDADE" L113-132; instruções anti-invenção L55-64; composição L42-53; validade com data L88 (regra dd/mm/aaaa condicional — base/offer).

Saneamento (D6): aplicar `sanitizePromptText` (já exportada do 45-02) APENAS nos valores dos blocos novos que carregam texto do lojista: `requiredArtworkTextSection` e `commercialDetailsSection`. NÃO aplicar no revisor (intocado) nem em prosa fixa do template.

Atenção executora — GREEN-GATES nesta etapa (redesign aprovado): todo plano que muda o contrato interno de prompt do diretor TERMINA com os 4 gates verdes — nenhuma suite fica vermelha esperando um plano posterior. `buildPromptVariables` retorna UM mapa compartilhado para todos os intents, então a mudança de template+mapa quebra os golden key-set para TODOS os intents no momento em que o mapa muda. Estratégia: (1) mapa TRANSICIONAL (regra detalhada na Task 2 step 11): adicionar as chaves de slot dos templates offer/base reescritos, MANTER as chaves legadas que os templates de spotlight/exclusive (ainda NÃO reescritos) interpolam — com valores produzidos pelos mesmos builders extraídos no 45-02, comportamento preservado — e remover APENAS chaves mortas em todos os templates (tabela de decisão do 45-01-SUMMARY); (2) CO-MIGRAR IN-PLAN as suites que quebraram, para terminar VERDE: golden key-set/8.17 de `image-generation-service.test.ts` (conjunto transicional — todos os intents; casos kqo a/b/c permanecem verdes com asserts de valor sobre chaves legadas sobreviventes), âncoras de `prompt-reframe.test.ts` para base/offer (as âncoras atuais de spotlight/exclusive continuam válidas até o 45-04 porque esses arquivos não mudam), casos de `validatePrompts` do diretor para offer, e as asserções legadas do `art-director-briefing.test.ts` afetadas pelo rework (Task 2). **Regra de ouro:** ao final deste plano, `npx vitest run` roda COMPLETO e VERDE — sem lista de testes vermelhos, sem pendência formal registrada para o 45-05 (nenhuma suite "pode permanecer vermelha"). O 45-05 parte de base verde e apenas AMPLIA cobertura. O `art-director-briefing.test.ts` é co-migrado na Task 2 e fica verde ainda aqui; o 45-05 Task 4 o amplia de forma ADITIVA (blocos presente/ausente, deduplicação, saneamento). Suites irmãs de revisor/copy/form/rota DEVEM permanecer verdes sem edição (não-mudança, D7).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reescrever base + offer em estrutura editorial + slots de bloco (D2/D3, mão leve)</name>
  <files>prompts/campaign-image-director.md, prompts/campaign-image-director-offer.md</files>
  <read_first>
    - prompts/campaign-image-director-offer.md (arquivo inteiro — 136 linhas: tabela L9-31, composição L42-53, instruções L55-64, brand profile L67-69, observações L71-73, notas adicionais L75-88, direção criativa L90-131, cauda L134-136)
    - prompts/campaign-image-director.md (arquivo inteiro — 135 linhas; base referência offer/geral, lido por teste)
    - prompts/campaign-image-reviewer.md (SOMENTE como referência de padrão de slots de bloco com heading no valor — NÃO alterar)
    - openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/design.md (D2 :56-71, D3 :73-99)
  </read_first>
  <action>
    Reescrever `campaign-image-director.md` e `campaign-image-director-offer.md` **com mão leve** (reorganizar/rotular/remover repetição — NUNCA modernizar texto nem trocar vocabulário/frases que funcionam; reaproveitar o texto existente movendo de lugar):

    1. Manter a camada editorial fixa legível (papel do diretor de marketing→diretor de arte, composição numerada, instruções obrigatórias anti-invenção, regras de hierarquia/paleta/flat/publicável, fidelidade + liberdade criativa). Texto humano no arquivo; headings editoriais no `.md`.
    2. Remover a micro-tabela "Informações da Campanha" sempre-presente (L9-31) e as "Notas Adicionais" cruas (L75-88) — o conteúdo vira blocos montados.
    3. Inserir os placeholders de bloco inteiro (um por linha): `{{campaignFactsSection}}`, `{{commercialDetailsSection}}`, `{{requiredArtworkTextSection}}`, `{{illustrativeNoticeSection}}`, `{{identityReferenceSection}}`, `{{productReferenceSection}}`, `{{constraintsSection}}`, `{{creativeDirectionSection}}` — com heading FIXO no `.md` para facts/identity/product/creativeDirection e heading DENTRO do valor para commercialDetails/requiredArtwork/illustrativeNotice/constraints (D2).
    4. Manter `{{campo}}` de dados apenas quando garantido no intent (ex.: `{{productName}}`, `{{storeName}}`, `{{brandColor}}`) ou frase autoprotegida; NÃO deixar `{{campo}}` que recebe `""` (ex.: `{{validity}}`/`{{campaignDetails}}`/`{{illustrativeNotice}}`/`{{mandatoryArtworkText}}` como linha solta) — esses passam a viver nos blocos.
    5. Base `campaign-image-director.md` mantém paridade editorial com offer (referência offer/geral, lida por teste) — mesma estrutura de seções e blocos, sem diferenças de intent específicas do offer.
    6. NÃO alterar spotlight/exclusive (escopo do 45-04). NÃO criar engine/parser/DSL. NÃO deixar `##` órfão possível.
    7. Git: validar diff textual priorizando movimento/reorganização (mão leve) — qualquer paráfrase nova de frase funcional é PROIBIDA.
  </action>
  <verify>
    <automated>
      $files = @('prompts\campaign-image-director.md','prompts\campaign-image-director-offer.md'); foreach ($f in $files) { $c = Get-Content -LiteralPath $f -Raw; $missing = @('campaignFactsSection','identityReferenceSection','productReferenceSection','creativeDirectionSection') | Where-Object { $c -notmatch "\{\{$_\}\}" }; if ($missing) { Write-Error "$f sem slots fixos: $($missing -join ',')" } }; Write-Output "OK: slots fixos presentes nos 2 arquivos"
    </automated>
  </verify>
  <acceptance_criteria>
    - `campaign-image-director-offer.md` e `campaign-image-director.md` contêm os 8 placeholders de bloco (`{{campaignFactsSection}}` etc.) — slots fixos obrigatórios presentes
    - Micro-tabela "Informações da Campanha" e "Notas Adicionais" removidas dos 2 arquivos (grep `| **Aviso ilustrativo` e `{{validity}}` como linha solta → ausentes ou apenas em frase autoprotegida)
    - Texto editorial reaproveitado do estado atual (diff mostra movimento/reorganização, sem paráfrase de frases funcionantes — verificação por amostragem de âncoras: instruções anti-invenção, REGRAS CRÍTICAS DE FIDELIDADE, liberdade criativa presentes)
    - spotlight/exclusive NÃO alterados (git diff limitado aos 2 arquivos)
  </acceptance_criteria>
  <done>Base + offer reescritos em estrutura editorial + blocos com mão leve</done>
</task>

<task type="auto">
  <name>Task 2: Implementar blocos de offer no helper (campaignFactsSection etc.) com repartição do repertório, saneamento, delegação do buildPromptVariables e co-migração do teste do módulo</name>
  <files>src/lib/image-generation/services/art-director-briefing.ts, src/lib/image-generation/services/__tests__/art-director-briefing.test.ts, src/lib/image-generation/services/image-generation-service.ts</files>
  <read_first>
    - src/lib/image-generation/services/art-director-briefing.ts (recém-criado no 45-02)
    - src/lib/image-generation/services/__tests__/art-director-briefing.test.ts (criado no 45-02 — asserções legadas afetadas pelo rework; co-migrar na step 13)
    - src/lib/image-generation/services/image-generation-service.ts (:896-1003 buildPromptVariables; :1025-1031 formatPriceBRL)
    - openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/design.md (D3 :73-99 — tabela de blocos e repartição)
    - .planning/phases/45-briefing-contextual-do-diretor-de-arte/45-01-SUMMARY.md (tabela de decisão de chaves — remoção/realocação)
  </read_first>
  <action>
    Adicionar ao `art-director-briefing.ts` as funções puras de montagem dos blocos de offer por presença real de dados (nomenclatura alinhada aos placeholders). Recebem o domínio + valores derivados já resolvidos e retornam `""` quando o bloco condicional não se aplica:

    1. `campaignFactsSection(brief, context, effectiveProductName, formattedPrices)` — bullets SÓ com campos presentes; **validade em ocorrência única** quando `offer` + `validity.enabled` + `displayText`; preço por intent; badge/hook/CTA/objetivo/canal/formato apenas quando presentes. Regra editorial dd/mm/aaaa da validade com data preservada como instrução condicional (base/offer).
    2. `commercialDetailsSection(brief)` — `campaignDetails`/`additionalDetails` (strip `[\[\]]`, não-vazio) + `availabilityNotes` keyword-gated (regras de escassez/variedade atuais de `buildCommercialRepertoire` preservadas); rotula "não obrigatório / repertório para inspiração"; **aplica `sanitizePromptText`** em cada valor de texto do lojista (D6); retorna `""` se nada.
    3. `requiredArtworkTextSection(merchantText)` — só texto livre não-vazio; heading próprio no valor; texto **saneado com `sanitizePromptText`**; instrução "respeitar, visível e legível, não repetir em legenda" (reaproveitar texto da cauda L135-136 atual); retorna `""` quando ausente.
    4. `illustrativeNoticeSection(illustrativeNotice)` — só aviso presente (constante canônica); heading próprio; instrução única: mínimo, legível, discreto, separado dos demais textos, nas laterais (reaproveitar L134); retorna `""` quando ausente.
    5. `identityReferenceSection(brief, context)` — sempre (heading fixo no `.md`); conteúdo: assinatura da loja + `identityDirective`; quando `identity.state` é `logo`/`visual_signature` com ativo → preservação explícita (não editar/alterar/redesenhar/distorcer/inventar); `text_only` → instrução de não criar logo/assinatura (regras de `deriveDirective` mantidas como texto — store-identity-service.ts intocado).
    6. `productReferenceSection(brief, context, imageCount)` — sempre (heading fixo); primary = referência factual forte (regras de fidelidade L113-132); 2+ imagens → auxiliares como apoio SEM competir; `preserveImageContext` (não-offer) → não recortar/isolamento proibido.
    7. `constraintsSection(brief)` — só `sensitiveConstraints` com conteúdo; heading próprio no valor + "Restrições sensíveis informadas pelo lojista"; retorna `""` quando ausente.
    8. `creativeDirectionSection(brief, context, inferredCategory)` — sempre (heading fixo); persona de segmento, categoria inferida, `categoryConflictDirective` só quando conflito, `creativeContextGuidance` só quando não-vazio, **repertório recomposto** (apenas argumentos derivados que NÃO duplicam validity/details — curadoria do que sobra, ou nada), brand profile (foundation direcional) só quando há perfil.
    9. Compor as funções de bloco NOVAS por presença real já aplicando a **repartição D3 para o que os templates reescritos renderizam**: validity entra apenas via `campaignFactsSection`; details/availability apenas via `commercialDetailsSection`; o `creativeDirectionSection` monta o repertório recomposto (argumentos derivados que NÃO duplicam validity/details) SEM reintroduzir essas naturezas. As funções puras extraídas no 45-02 continuam produzindo, DURANTE a transição, os valores das chaves legadas que os templates de spotlight/exclusive (ainda não reescritos) interpolam (`commercialRepertoire`, `inputValidationSummary`, `creativeContextGuidance`, `categoryConflictDirective` etc.) — comportamento preservado para esses intents; a remoção/recombinação definitiva delas (e das chaves legadas) é do 45-04. No offer montado a deduplicação é total: o template offer/base reescrito NÃO interpola nenhuma chave legada — só os slots novos + prosa garantida.
    10. Compor a montagem por presença: as seções condicionais retornam `""` e os slots ausentes não renderizam nada (garantia D4 na montagem — valor `""` → PromptLoader não deixa placeholder; os templates não têm heading fixo para condicional).
    11. **Mapa TRANSICIONAL (D5 + tabela de decisão do 45-01):** o Record que `buildPromptVariables` retorna é compartilhado por todos os intents — nesta etapa ele deve conter exatamente: (a) as NOVAS chaves de slot consumidas pelos templates offer/base reescritos (Task 1); (b) as chaves LEGADAS ainda interpoladas pelos templates de spotlight/exclusive (NÃO reescritos neste plano) — coletar a lista real com grep `\{\{...\}\}` nesses 2 arquivos — com valores produzidos pelos mesmos builders extraídos no 45-02 (comportamento preservado para esses intents); (c) `campaignIntent` (orquestração). Remover APENAS as chaves confirmadas mortas em TODOS os templates (tabela de decisão do 45-01-SUMMARY — ex.: `commercialFrame` — copy tem builder próprio; `identityImageUrl` provider-only — nunca interpolada como texto; `hasCategoryConflict`, `brandColorsChosen`, `visualStyle`, `visualTone`, `brandPersonality`, `campaignGuidelines`, `campaignBrief` sem consumidor). `validity`/`originalPrice`, só consumidos por offer/base, saem do Record junto da reescrita (seus conteúdos vivem nos blocos novos). As chaves legadas removíveis por completo só no 45-04, quando spotlight/exclusive forem reescritos (não remover agora o que eles ainda interpolam — placeholder residual = validatePrompts inválido).
    11b. **Delegação no service (`image-generation-service.ts`, esta task):** reescrever o corpo de `buildPromptVariables` (`:896-1003`) para construir o Record final chamando as funções de bloco do módulo (`campaignFactsSection`, `commercialDetailsSection`, `requiredArtworkTextSection`, `illustrativeNoticeSection`, `identityReferenceSection`, `productReferenceSection`, `constraintsSection`, `creativeDirectionSection`) para os templates offer/base reescritos E continuar produzindo os valores legados ainda interpolados por spotlight/exclusive (regra (b) da step 11 — mapa transicional; comportamento dos builders do 45-02 preservado para esses intents). A repartição do repertório vale para o que o offer renderiza via blocos; a limpeza final das chaves legadas órfãs é do 45-04. `identityImageUrl` permanece provider-only (fora do Record). NÃO mudar `assemblePrompt` (`:1005-1023`) nesta task (escopo do 45-03 Task 3).
    12. NÃO adicionar comentários no código. Manter determinismo (mesmo input → mesmo texto).
    13. **Co-migração do teste do módulo (obrigatória NESTE plano — o arquivo não espera o 45-05):** após o rework das funções acima, executar `npx vitest run src/lib/image-generation/services/__tests__/art-director-briefing.test.ts` e REMOVER/ADAPTAR apenas as asserções legadas do 45-02 cujos alvos o rework efetivamente alterou ou cujas assinaturas mudaram: (a) se `buildCommercialRepertoire` (com validade/details) deixou de existir com essa assinatura ou foi recombinado, remover o describe ou re-apontar ao bloco que absorveu a natureza (`campaignFactsSection` para validade; `commercialDetailsSection` para details/availability); (b) se `buildValidationSummary` foi removida/recombinada conforme a decisão do 45-01-SUMMARY, remover ou re-apontar ao bloco que a absorveu. MANTER as asserções que seguem descrevendo funções puras sobreviventes com comportamento preservado (`formatPriceBRL`, `splitDirectorLegalText`, `sanitizePromptText`, spot-checks de `buildCreativeContextGuidance`, `buildBrandProfileSection`) — inclusive as que continuam alimentando chaves legadas durante a transição. NÃO criar aqui os testes dos blocos novos (`campaignFactsSection` etc.) — a ampliação aditiva é do 45-05 Task 4. NÃO comentar código de teste; o arquivo deve ficar VERDE ao final desta task.
  </action>
  <verify>
    <automated>
      Select-String -LiteralPath "src\lib\image-generation\services\art-director-briefing.ts" -Pattern "function campaignFactsSection|function commercialDetailsSection|function requiredArtworkTextSection|function illustrativeNoticeSection|function identityReferenceSection|function productReferenceSection|function constraintsSection|function creativeDirectionSection" | Measure-Object | Select-Object -ExpandProperty Count
      npx vitest run src/lib/image-generation/services/__tests__/art-director-briefing.test.ts; if ($?) { Write-Output "OK: teste do módulo co-migrado e verde" }
    </automated>
  </verify>
  <acceptance_criteria>
    - `art-director-briefing.ts` exporta as 8 funções de bloco (grep = 8 ocorrências)
    - `sanitizePromptText` aplicado em `commercialDetailsSection` e `requiredArtworkTextSection` (não em prosa fixa do template)
    - Funções condicionais retornam `""` quando o conteúdo não existe (commercialDetails/requiredArtwork/illustrativeNotice/constraints)
    - Regras de fidelidade/hierarquia/preservação/criatividade mantidas no texto dos blocos
    - `buildPromptVariables` (service) delega aos blocos (corpo reescrito nesta task) — mapa TRANSICIONAL: slots novos + chaves legadas de spotlight/exclusive mantidas com valores + chaves mortas em todos os templates (tabela de decisão do 45-01) removidas
    - `art-director-briefing.test.ts` co-migrado (asserções legadas cujos alvos o rework alterou — validade/details do repertório repartido, `buildValidationSummary` se removida/recombinada — removidas/adaptadas) e VERDE ao final da task — o 45-05 o amplia apenas de forma aditiva
  </acceptance_criteria>
  <done>Blocos de offer implementados no helper com repartição do repertório, saneamento (D3/D6), delegação do buildPromptVariables e teste do módulo co-migrado e verde</done>
</task>

<task type="auto">
  <name>Task 3: Adaptar validatePrompts/assemblePrompt + co-migrar IN-PLAN as suites-alvo (golden key-set, prompt-reframe base/offer, validatePrompts offer) + gates 100% verdes + amostragem</name>
  <files>src/lib/image-generation/services/image-generation-service.ts, src/lib/image-generation/services/__tests__/image-generation-service.test.ts, src/lib/campaign/__tests__/prompt-reframe.test.ts</files>
  <read_first>
    - src/lib/image-generation/services/image-generation-service.ts (:637-708 validatePrompts; :1005-1023 assemblePrompt)
    - src/lib/image-generation/services/prompt-validator.ts (:8-32 validatePrompt — NÃO alterar regra de placeholder residual)
    - prompts/campaign-image-director-offer.md (novo — reescrito na Task 1)
    - src/lib/image-generation/services/__tests__/image-generation-service.test.ts (golden por intent :556-719: EXPECTED_KEYS :557-569, casos 8.16/9.3/9.5/20/kqo :581-708, 8.17 repertório :710-719; validatePrompts :65-298)
    - src/lib/campaign/__tests__/prompt-reframe.test.ts (arquivo inteiro — 74 linhas; âncoras atuais :17-22; testes 16/17/check A/B/21)
    - .planning/phases/45-briefing-contextual-do-diretor-de-arte/45-01-SUMMARY.md (tabela de decisão de chaves — remoção/realocação)
  </read_first>
  <action>
    Adaptar o contrato interno do service ao template contextual de offer E co-migrar IN-PLAN as suites que a mudança de mapa quebra — o plano TERMINA com `npx vitest run` 100% VERDE (green-gates; nenhuma suite vermelha herdada pelo 45-05):

    1. `validatePrompts` (:637-708): garantir que a checagem de **zero placeholders não resolvidos** continua válida para o diretor por intent com os novos templates (via `validatePrompt` — prompt-validator.ts NÃO muda). O método carrega o template pelo `promptLoader` real por intent — com o mapa transicional da Task 2, os placeholders de TODOS os intents (offer com slots novos; spotlight/exclusive com chaves legadas mantidas) precisam resolver. Manter intacta a checagem do revisor (bloco :660-705 — revisor fora do escopo, D7). Se houver lista/asserção de chaves do mapa no método, alinhar ao conjunto transicional. Adicionar checagem leve de seção vazia/heading órfão para o diretor SOMENTE se determinística e sem falsos positivos — caso contrário, deixar a ausência de vazios para os testes de cenário (45-05).
    2. `assemblePrompt` (:1005-1023): inalterado em comportamento — continua selecionando `campaign-image-director-${intent}` e anexando rodapé CORRECT/REGENERATE sobre o template contextual de offer.
    3. **Co-migração dos golden key-set/8.17/kqo em `image-generation-service.test.ts` (obrigatória NESTE plano — o mapa é compartilhado, então a asserção de keys quebra para TODOS os intents no momento da mudança):** atualizar `EXPECTED_KEYS` (:557-569) para o **conjunto transicional** = chaves reais do Record pós-Task 2 (slots novos + legadas de spotlight/exclusive + `campaignIntent`, sem as chaves mortas); asserções de `toHaveLength(39)` (:591/610/621/648/673) passam a usar `EXPECTED_KEYS.length` (nunca literal fixo); re-apontar asserts de valor de chaves REMOVIDAS — `vars.validity` nos casos 8.16/9.5 → assertar `vars.campaignFactsSection` contendo o texto de validade (ocorrência única em facts); `vars.commercialFrame` no caso exclusive (:623) → assertar que a chave NÃO existe no Record e cobrir o equivalente no bloco/valor que absorveu a intenção; casos 8.17 do `buildCommercialRepertoire` (:710-719) → re-apontar para o bloco canônico do módulo (`campaignFactsSection` para validade — repartição D3; se o método privado sair do service, exercitar a função pura equivalente do módulo); MANTER os asserts de valor sobre chaves sobreviventes (`vars.campaignIntent`, `vars.preserveImageDirective`, kqo a/b/c sobre `mandatoryArtworkText`/`illustrativeNotice` — chaves legadas que o mapa transicional mantém porque spotlight/exclusive ainda as interpolam).
    4. **Co-migração de `prompt-reframe.test.ts` para base/offer (obrigatória NESTE plano):** os testes que iteram os 4 `PROMPTS` com as âncoras atuais (:17-22 — LINHA_AVISO_SEPARADO/LINHA_MANTIDA/LINHA_TABELA_AVISO; check A validade; teste 21 frases 1+N) passam a discriminar POR ARQUIVO: para `campaign-image-director.md`/`-offer.md` (reescritos na Task 1) as âncoras viram as NOVAS — presença das seções editoriais fixas (anti-invenção, fidelidade, criatividade), presença dos 8 placeholders de bloco, AUSÊNCIA da micro-tabela/`Notas Adicionais`/frases incondicionais antigas da cauda (elas agora vivem dentro dos blocos montados); para spotlight/exclusive (arquivos NÃO reescritos) MANTER as âncoras atuais — o arquivo deve terminar VERDE.
    5. **Casos de `validatePrompts` do diretor (offer):** adicionar/ajustar cenário com o serviço construído com `PromptLoader` REAL (default = `prompts/`), brief offer completo (validade + texto obrigatório + aviso) e offer mínimo, assertando `valid=true` — prova real de zero placeholders não resolvidos entre template novo e mapa transicional (D4). Os demais casos com loader mockado do describe (:65-298) seguem válidos (não dependem do conteúdo real dos templates).
    6. **Gates 100% verdes (green-gates):** `npm run typecheck`, `npm run lint`, `npm run build` — ZERO erros; `npx vitest run` — rodada COMPLETA VERDE, sem lista de testes vermelhos e sem pendência formal para o 45-05; suites irmãs (image-review-service.test.ts, copy, use-campaign-form-*, route.test.ts) verdes SEM edição (não-mudança, D7).
    7. **Validação por amostragem do prompt offer montado** (F45-12): montar um prompt offer representativo (brief completo: validade habilitada, texto obrigatório livre, aviso, details, availability, restrições, 1 primary + 1 auxiliar) e um offer mínimo (sem textos opcionais); inspecionar o texto final e confirmar: (a) ausência de `##` órfãos, linhas de tabela em branco e placeholders não resolvidos; (b) cada natureza opcional/sensível em UMA ocorrência (validade só em campaignFactsSection; details/availability só em commercialDetailsSection; aviso e texto obrigatório em seções próprias); (c) riqueza/orientação preservada (fidelidade, anti-invenção, criatividade presentes). Registrar o resultado da amostragem no SUMMARY, junto do registro das co-migrações das steps 3-5 (testes antigos substituídos × novos).
  </action>
  <verify>
    <automated>npx vitest run; if ($?) { npm run typecheck; if ($?) { npm run lint; if ($?) { npm run build } } }</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck`, `npm run lint`, `npm run build` — zero erros
    - `npx vitest run` COMPLETO E VERDE — nenhuma suite vermelha remanescente; nenhuma pendência de co-migração registrada para o 45-05 (green-gates por plano)
    - `image-generation-service.test.ts` com golden key-set co-migrado ao conjunto transicional (asserções de valor re-apontadas: validity → `campaignFactsSection`; `commercialFrame` removido do Record sem assert órfão; 8.17 re-apontado ao bloco canônico) e VERDE
    - `prompt-reframe.test.ts` com âncoras novas para base/offer e âncoras atuais mantidas para spotlight/exclusive, VERDE
    - Caso de validatePrompts com `PromptLoader` real para offer (completo e mínimo) verde — zero placeholders residuais (D4)
    - Prompt offer montado (amostragem completo + mínimo): sem seções vazias/headings órfãos/linhas de tabela em branco/placeholders residuais; cada natureza opcional/sensível em uma única ocorrência
    - Rodapé CORRECT/REGENERATE presente no assemblePrompt (inalterado); revisor intocado (validatePrompts segue validando revisor com o builder compartilhado)
    - Suites irmãs (image-review-service.test.ts, copy, use-campaign-form-*, route.test.ts) verdes e não editadas
  </acceptance_criteria>
  <done>validatePrompts/assemblePrompt adaptados; suites-alvo co-migradas in-plan (golden/prompt-reframe/validatePrompts offer); 4 gates 100% verdes; amostragem do prompt offer validada</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template ↔ modelo LLM | Texto do template + blocos montados viram instrução ao modelo — placeholder residual/vazios/duplicação degradam a orientação |
| texto do lojista → template | Texto livre com `{{...}}` pode virar placeholder não resolvido no prompt final |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-03 | Tampering | prompts offer/base + art-director-briefing.ts | mitigate | Montagem por presença real (D3) + validatePrompts zero placeholders (D4) + amostragem do prompt offer (completo e mínimo) — sem vazios/headings órfãos/duplicação |
| T-45-03a | Tampering | Texto do lojista nos blocos novos | mitigate | `sanitizePromptText` aplicado em requiredArtworkTextSection e commercialDetailsSection (D6) — `{{`→`{`, `}}`→`}` antes da interpolação |
| T-45-03b | Information Disclosure | Repartição do repertório | mitigate | buildCommercialRepertoire repartido sem reintroduzir validity/details (D3); chaves mortas removidas conforme inventário 45-01 (consumidor oculto verificado por grep) |
| T-45-03c | Tampering | Reescrita editorial | mitigate | Mão leve obrigatória (reaproveitar/mover texto, sem paráfrase) + verificação por âncoras de conteúdo + UAT humano comparativo (45-07) |
| T-45-03d | Denial of Service | Suites de co-migração | mitigate | Co-migração IN-PLAN das suites que a mudança de mapa quebra (golden key-set/8.17/kqo, prompt-reframe base/offer, validatePrompts offer); `npx vitest run` completo VERDE ao final do plano (green-gates — sem pendência formal para o 45-05); suites irmãs (revisor/copy/form/rota) verdes sem edição — não-mudança (D7) |
| T-45-03e | Tampering | Mapa transicional (D5) | mitigate | Chaves legadas mantidas enquanto spotlight/exclusive as interpolam; remoção apenas de chaves mortas em todos os templates (tabela de decisão 45-01); validação com `PromptLoader` real por intent (validatePrompts valid=true) — placeholder residual = falha de gate |
| T-45-SC | Tampering | npm installs | accept | Nenhum pacote novo — sem gate de legitimidade |
</threat_model>

<verification>
- Base + offer reescritos com os 8 slots de bloco; micro-tabela e Notas Adicionais removidas; spotlight/exclusive intactos
- `art-director-briefing.ts` com as 8 funções de bloco; condicionais retornam `""`; saneamento aplicado nos blocos novos
- Mapa transicional: slots novos + chaves legadas de spotlight/exclusive mantidas + chaves mortas removidas (tabela 45-01); placeholders de todos os intents resolvem
- `image-generation-service.test.ts` co-migrado ao conjunto transicional (golden key-set/8.17/kqo) e verde; `prompt-reframe.test.ts` com âncoras novas de base/offer e verdes; `art-director-briefing.test.ts` co-migrado na Task 2 e verde
- `npm run typecheck`/`npm run lint`/`npm run build` — zero erros; `npx vitest run` COMPLETO E VERDE (green-gates — nenhuma suite vermelha, nenhuma pendência para o 45-05)
- Amostragem do prompt offer (completo + mínimo) documentada no SUMMARY — sem vazios, sem duplicação, riqueza preservada
- Revisor/copy/form/rota intactos (suites irmãs verdes e não editadas)
</verification>

<success_criteria>
- Offer + base em estrutura editorial + blocos contextuais (D2) com mão leve
- Blocos de offer por presença real de dados no helper (D3), validade em ocorrência única, repartição do repertório concluída
- Texto do lojista saneado nos blocos novos (D6)
- Mapa transicional de `buildPromptVariables` (D5) — spotlight/exclusive seguem com placeholders resolvidos; chaves mortas removidas após inventário
- validatePrompts/assemblePrompt adaptados com zero placeholders residuais e rodapé mantido (D4/D5)
- Suites-alvo co-migradas no próprio 45-03 (golden key-set, prompt-reframe base/offer, validatePrompts offer, `art-director-briefing.test.ts`) — `npx vitest run` 100% VERDE ao final (green-gates); o 45-05 parte de base verde e amplia cobertura de forma aditiva
- Amostragem do prompt offer validada
</success_criteria>

<output>
Create `.planning/phases/45-briefing-contextual-do-diretor-de-arte/45-03-SUMMARY.md` when done
</output>

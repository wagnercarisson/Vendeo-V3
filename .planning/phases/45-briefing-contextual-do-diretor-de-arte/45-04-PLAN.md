---
phase: 45-briefing-contextual-do-diretor-de-arte
plan: 04
type: execute
wave: 2
depends_on: [45-03]
files_modified:
  - prompts/campaign-image-director-spotlight.md
  - prompts/campaign-image-director-exclusive.md
  - src/lib/image-generation/services/art-director-briefing.ts
  - src/lib/image-generation/services/image-generation-service.ts
  - src/lib/image-generation/services/__tests__/image-generation-service.test.ts
  - src/lib/campaign/__tests__/prompt-reframe.test.ts
  - src/lib/image-generation/services/__tests__/art-director-briefing.test.ts
autonomous: true
requirements: [F45-14, F45-15, F45-16, F45-17, F45-18, F45-19, F45-20]

must_haves:
  truths:
    - "`campaign-image-director-spotlight.md` e `campaign-image-director-exclusive.md` reescritos na MESMA estrutura editorial + blocos do offer/base (D2), com mão leve: spotlight sem validade e com preço único (sem DE/POR, sem urgência); exclusive SEM preço, tom premium, sem badge promocional (D2/D3)"
    - "Blocos ajustados por intent no helper: campaignFactsSection de exclusive sem preço; productReferenceSection com preserveImageDirective em não-offer (preserveImageContext) (D3)"
    - "DNA do diretor por intent preservado — destaque sem urgência; premium sem preço; anti-invenção e criatividade mantidas (conteúdo editorial reaproveitado)"
    - "Prompt spotlight/exclusive montados sem seções vazias, duplicação ou placeholders residuais; montagem determinística (D4)"
    - "Mapa FINAL de buildPromptVariables (D5): chaves legadas transicionais do 45-03 que ficaram ÓRFÃS após a reescrita de spotlight/exclusive REMOVIDAS (seus placeholders sumiram dos 4 templates); Record final = chaves realmente consumidas pelos 4 templates reescritos + campaignIntent; nenhuma chave morta remanescente"
    - "Suites-alvo co-migradas NO PRÓPRIO 45-04 (não há suites vermelhas herdadas pelo 45-05): golden key-set atualizado ao conjunto FINAL em image-generation-service.test.ts (asserções de valor de chaves legadas removidas — ex.: kqo a/b/c sobre mandatoryArtworkText/illustrativeNotice re-apontadas às seções/blocos que as absorveram); novas âncoras de prompt-reframe.test.ts para spotlight/exclusive (constantes de âncora antigas removidas); casos de validatePrompts do diretor para esses intents — `npx vitest run` 100% VERDE ao final do plano"
    - "`art-director-briefing.test.ts` permanece VERDE herdado do 45-03 (linha principal NÃO o edita); `art-director-briefing.test.ts` está em `files_modified` apenas para a CONTINGÊNCIA declarada na 'Atenção executora' — se o ajuste intent-aware da Task 2 alterar/remover função ainda exportada exercitada pelo teste do módulo, co-migrar a asserção específica AQUI no 45-04 (green-gates, não espera o 45-05)"
  artifacts:
    - path: "prompts/campaign-image-director-spotlight.md"
      provides: "Template spotlight em estrutura editorial + blocos"
      contains: "{{campaignFactsSection}}"
    - path: "prompts/campaign-image-director-exclusive.md"
      provides: "Template exclusive em estrutura editorial + blocos"
      contains: "{{campaignFactsSection}}"
    - path: "src/lib/image-generation/services/art-director-briefing.ts"
      provides: "Blocos ajustados por intent (spotlight/exclusive) + mapa final sem chaves legadas órfãs"
      contains: "campaignIntent"
    - path: "src/lib/image-generation/services/__tests__/image-generation-service.test.ts"
      provides: "Golden key-set co-migrado ao conjunto FINAL (todos os intents — mapa compartilhado)"
      contains: "campaignFactsSection"
    - path: "src/lib/campaign/__tests__/prompt-reframe.test.ts"
      provides: "Âncoras editoriais novas para spotlight/exclusive (4 .md todos na estrutura-alvo)"
      contains: "campaign-image-director-spotlight"
  key_links:
    - from: "prompts/campaign-image-director-spotlight.md"
      to: "prompts/campaign-image-director-offer.md"
      via: "mesma estrutura de slots (reescrita do 45-03 como referência)"
      pattern: "campaignFactsSection"
    - from: "prompts/campaign-image-director-exclusive.md"
      to: "art-director-briefing.ts"
      via: "facts sem preço (exclusive) montados no helper"
      pattern: "exclusive"
    - from: "src/lib/image-generation/services/__tests__/image-generation-service.test.ts"
      to: "src/lib/image-generation/services/image-generation-service.ts"
      via: "asserção do conjunto FINAL de chaves sobre o Record compartilhado de buildPromptVariables"
      pattern: "campaignIntent"
---

<objective>
Reescrever `campaign-image-director-spotlight.md` e `campaign-image-director-exclusive.md` na **mesma estrutura editorial + blocos contextuais** do offer/base (D2/D3) com **mão leve**, preservando o DNA do diretor por intent: spotlight = destaque/novidade **sem urgência e sem DE/POR** (preço único); exclusive = **premium sem preço** e sem badge promocional. Ajustar os blocos por intent no helper `art-director-briefing.ts` (ex.: facts de exclusive sem preço; `productReferenceSection` com `preserveImageDirective` quando `preserveImageContext` em não-offer).

Purpose: D2 (estrutura uniforme dos 4 `.md`) + D3 (blocos ajustados por intent; spotlight/exclusive sem validade; exclusive sem preço) — conclui a reescrita dos 4 templates iniciada no 45-03 + D5 (mapa FINAL de `buildPromptVariables`: remoção das chaves legadas transicionais órfãs). Green-gates: as suites-alvo de teste (golden key-set → conjunto final, prompt-reframe spotlight/exclusive, validatePrompts desses intents) são co-migradas NO PRÓPRIO 45-04, terminando com `npx vitest run` 100% VERDE (F45-18/F45-19/F45-20 em cobertura nesta etapa; o 45-05 parte de base verde e apenas amplia).
Output: spotlight + exclusive reescritos; blocos intent-aware no helper; chaves legadas órfãs removidas (mapa final); co-migração in-plan do golden key-set ao conjunto FINAL, das âncoras de spotlight/exclusive em `prompt-reframe.test.ts` e dos casos de `validatePrompts` desses intents — 4 gates 100% verdes ao final; amostragem dos prompts validada.

Nota de execução: 45-03 e 45-04 estão ambos na onda 2 — a execução é SEQUENCIAL estrita (45-03 → 45-04) via `depends_on: [45-03]`, espelhando os labels de onda de tasks.md/design.md e o precedente do repo de mesma onda com execução sequencial quando `parallelization: false` (compartilham `src/lib/image-generation/services/art-director-briefing.ts`, `image-generation-service.test.ts` e `prompt-reframe.test.ts`; NÃO rodar em paralelo).
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-briefing-contextual-do-diretor-de-arte/45-CONTEXT.md (D1-D7; F45-14..F45-17 :80-83; F45-18..F45-20 :84-86 — cobertura nesta etapa; canonical_refs :141-174)
@openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/design.md (D2 :56-71; D3 :73-99 — diferenças por intent; "Sem validade; spotlight preço único sem DE/POR; exclusive sem preço" :161-163)
@openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/tasks.md (§4 — tasks 4.1-4.4)
@prompts/campaign-image-director-spotlight.md (132 linhas — estado atual)
@prompts/campaign-image-director-exclusive.md (141 linhas — estado atual)
@prompts/campaign-image-director-offer.md (referência da nova estrutura — reescrito no 45-03)

<interfaces>
Diferenças por intent a preservar na reescrita (estado atual verificado 2026-09-02):

- **spotlight** (`campaign-image-director-spotlight.md`): título "Campanha Visual para Instagram - Destaque"; tabela atual sem validade e sem originalPrice (preço único `{{discountedPrice}}` L17); regra "NÃO criar senso de urgência ou escassez (sem 'corra', 'últimas unidades', 'aproveite')" L57; `preserveImageDirective` já presente (não-offer). DNA: destaque/novidade sem urgência.
- **exclusive** (`campaign-image-director-exclusive.md`): título "…- Exclusivo"; tabela atual com linha "Produto Exclusivo | Sem divulgação de preço" L17; regra "NÃO exibir preço, desconto, condições de pagamento ou parcelamento" L61; badge opcional e NÃO promocional L45; `preserveImageDirective` presente. DNA: premium sem preço.

Blocos por intent (D3):
- `campaignFactsSection`: spotlight → sem validade, preço único presente quando houver; exclusive → SEM preço (nunca monta linha de preço), com label "exclusivo/edição limitada" quando houver badge/dados presentes.
- `creativeDirectionSection`: creativeContextGuidance atual já tem sufixo por intent no `buildCreativeContextGuidance` (spotlight: "Apresentar como destaque ou novidade, sem urgência..."; exclusive: "Valor percebido e exclusividade... Tom premium, sem preço") — preservar esse comportamento nas funções puras do helper.
- `productReferenceSection`: `preserveImageDirective` (não recortar/preservar contexto) entra quando `campaignIntent !== "offer"` E `preserveImageContext === true` (regra atual de `buildPromptVariables` :1003-1008 no service, movida para o bloco).

Atenção executora — GREEN-GATES nesta etapa (segue o redesign do 45-03): ao final deste plano os 4 gates rodam 100% VERDES. Com a reescrita de spotlight/exclusive, os placeholders legados que só esses 2 arquivos ainda interpolavam (ex.: `{{mandatoryArtworkText}}`, `{{illustrativeNotice}}`, `{{commercialRepertoire}}`, `{{campaignDetails}}` etc. — chaves transicionais mantidas no 45-03) deixam de existir nos 4 templates → a remoção dessas chaves ÓRFÃS do Record (Task 2) quebra os golden key-set/asserções de valor em `image-generation-service.test.ts` e as âncoras antigas de spotlight/exclusive em `prompt-reframe.test.ts` → CO-MIGRAR IN-PLAN (Task 3): golden key-set ao conjunto FINAL (todos os intents — mapa compartilhado), asserções de valor de chaves legadas re-apontadas às seções/blocos que as absorveram (ex.: kqo a/b/c passam a assertar `requiredArtworkTextSection`/`illustrativeNoticeSection`), novas âncoras estruturais de spotlight/exclusive em `prompt-reframe.test.ts` (constantes de âncora antigas removidas por completo), casos de `validatePrompts` desses intents. **Regra de ouro:** `npx vitest run` roda COMPLETO e VERDE ao final — sem lista de testes vermelhos, sem pendência formal para o 45-05 (nenhuma suite "pode permanecer vermelha"); o 45-05 parte de base verde e apenas AMPLIA. Suites irmãs (revisor/copy/form/rota) DEVEM permanecer verdes sem edição. typecheck/lint/build ZERO erros. O `art-director-briefing.test.ts` foi co-migrado no 45-03 e permanece verde; se o ajuste intent-aware da Task 2 alterar/remover alguma função ainda exportada que as asserções legadas exercitam (ex.: spot-checks de `buildCreativeContextGuidance` recombinado em `creativeDirectionSection`), co-migrar a asserção específica AQUI no 45-04 (mesma permissão do 45-03) — o arquivo não espera o 45-05.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reescrever spotlight + exclusive em estrutura editorial + blocos (D2/D3, mão leve)</name>
  <files>prompts/campaign-image-director-spotlight.md, prompts/campaign-image-director-exclusive.md</files>
  <read_first>
    - prompts/campaign-image-director-spotlight.md (arquivo inteiro — 132 linhas)
    - prompts/campaign-image-director-exclusive.md (arquivo inteiro — 141 linhas)
    - prompts/campaign-image-director-offer.md (referência da estrutura-alvo — reescrita no 45-03)
    - openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/design.md (D2 :56-71; D3 :73-99)
  </read_first>
  <action>
    Reescrever `campaign-image-director-spotlight.md` e `campaign-image-director-exclusive.md` na MESMA estrutura do offer/base reescrito no 45-03 (camada editorial fixa + os mesmos 8 placeholders de bloco), **com mão leve** (reaproveitar/mover texto existente; proibido parafrasear frases funcionantes ou modernizar):

    1. Spotlight: remover a micro-tabela sempre-presente e Notas Adicionais; manter as regras editoriais do intent (sem urgência/escassez L57, hierarquia L60, composição de destaque L44-50); inserir os 8 slots de bloco com a mesma convenção do offer (heading fixo no `.md` para facts/identity/product/creativeDirection; heading no valor para os condicionais). `{{campaignFactsSection}}` do spotlight NÃO recebe validade e usa preço único.
    2. Exclusive: mesma estrutura; manter regras editoriais do intent (sem preço L61, badge NÃO promocional e opcional L45, tom premium L49); `{{campaignFactsSection}}` do exclusive NUNCA monta preço. `productReferenceSection` carrega `preserveImageDirective` quando aplicável.
    3. `{{campo}}` de dados só em prosa editorial garantida no intent ou frase autoprotegida (mesma regra do 45-03, D2). Nenhum `##` órfão possível; nenhuma linha de tabela em branco.
    4. NÃO alterar offer/base já reescritos; NÃO criar engine/DSL.
    5. Git: diff textual priorizando movimento/reorganização; paráfrase de frase funcional é PROIBIDA (mão leve).
  </action>
  <verify>
    <automated>
      $files = @('prompts\campaign-image-director-spotlight.md','prompts\campaign-image-director-exclusive.md'); foreach ($f in $files) { $c = Get-Content -LiteralPath $f -Raw; $missing = @('campaignFactsSection','commercialDetailsSection','requiredArtworkTextSection','illustrativeNoticeSection','identityReferenceSection','productReferenceSection','constraintsSection','creativeDirectionSection') | Where-Object { $c -notmatch "\{\{$_\}\}" }; if ($missing) { Write-Error "$f sem slots: $($missing -join ',')" } }; Write-Output "OK: 8 slots presentes em spotlight e exclusive"
    </automated>
  </verify>
  <acceptance_criteria>
    - Spotlight e exclusive contêm os 8 placeholders de bloco
    - Spotlight sem linha de validade (grep validade na tabela/slots → ausente como dado) e sem urgência no texto editorial
    - Exclusive sem preço/desconto no texto (grep de `{{discountedPrice}}`/`{{originalPrice}}` → ausente); badge editorial NÃO promocional
    - Offer/base não alterados nesta task (git diff limitado aos 2 arquivos)
  </acceptance_criteria>
  <done>Spotlight + exclusive reescritos na estrutura editorial + blocos com mão leve (DNA por intent preservado)</done>
</task>

<task type="auto">
  <name>Task 2: Ajustar blocos por intent no helper (facts exclusive sem preço; preserveImageDirective em não-offer) + remover chaves transicionais órfãs (mapa FINAL)</name>
  <files>src/lib/image-generation/services/art-director-briefing.ts, src/lib/image-generation/services/image-generation-service.ts</files>
  <read_first>
    - src/lib/image-generation/services/art-director-briefing.ts (funções de bloco do 45-03)
    - src/lib/image-generation/services/image-generation-service.ts (:1003-1008 regra atual de preserveImageDirective no buildPromptVariables; :835-894 buildCreativeContextGuidance — sufixos por intent)
    - openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/design.md (D3 :73-99)
  </read_first>
  <action>
    Ajustar as funções puras do helper para os intents spotlight/exclusive (recebem `campaignIntent` como parâmetro derivado):

    1. `campaignFactsSection`: parâmetro `campaignIntent` (ou flag derivada) — spotlight monta preço único quando presente (nunca "de X por Y"); exclusive NUNCA monta linha de preço (nem original/discount); nenhum dos dois monta validade.
    2. `productReferenceSection`: aceitar `preserveImageContext: boolean` e anexar a diretiva "NÃO recortar o produto. Preservar o contexto original da imagem..." APENAS quando `campaignIntent !== "offer"` && `preserveImageContext === true` (regra atual do service movida para o bloco, sem duplicar).
    3. `creativeDirectionSection`/guidance: garantir que os sufixos por intent atuais (spotlight sem urgência; exclusive tom premium sem preço) são preservados no texto montado — comportamento idêntico ao `buildCreativeContextGuidance` atual.
    4. `constraintsSection`/`commercialDetailsSection`/aviso/texto obrigatório: comportamento compartilhado com offer (bloco canônico único por natureza, D3) — sem duplicar regra por intent.
    5. Atualizar a delegação em `buildPromptVariables`/blocos para passar `campaignIntent`/`preserveImageContext` derivados do domínio. `identityImageUrl` permanece provider-only (fora do Record).
    6. **Mapa FINAL (remoção das chaves transicionais órfãs — D5):** com os 4 templates reescritos, coletar por grep `\{\{...\}\}` nos 4 `.md` e reduzir o Record de `buildPromptVariables` ao conjunto REALMENTE consumido + `campaignIntent`. Remover as chaves legadas que só spotlight/exclusive ainda interpolavam no 45-03 (ex.: `mandatoryArtworkText`, `illustrativeNotice`, `commercialRepertoire`, `inputValidationSummary`, `campaignDetails`, `additionalDetails`, `availabilityNotes`, `badgeText`, `hook`, `cta`, `objective`, `format`, `targetChannel`, `creativePersona`, `inferredCategory`, `categoryConflictDirective`, `creativeContextGuidance`, `identityDirective`, `sensitiveConstraints`, `preserveImageDirective` como chave avulsa etc.) — cada conteúdo agora vive dentro do bloco canônico que o absorveu (D3). Se alguma chave legada permanecer em prosa garantida dos templates novos (ex.: `productName`/`storeName`/`brandColor` em frase editorial), MANTÊ-la. Nenhuma chave morta remanescente no Record (D5).
    7. NÃO adicionar comentários no código; manter determinismo.
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\lib\image-generation\services\art-director-briefing.ts" -Pattern "preserveImageContext|campaignIntent" | Select-Object -First 3 | Out-Null; Write-Output "OK: blocos intent-aware presentes"</automated>
  </verify>
  <acceptance_criteria>
    - `campaignFactsSection` NUNCA monta preço para exclusive e nunca monta validade para spotlight/exclusive
    - `productReferenceSection` anexa preserveImageDirective apenas para não-offer com preserveImageContext
    - Sufixos de guidance por intent (spotlight/exclusive) preservados no texto montado
    - Regras de fidelidade/anti-invenção/criatividade intactas nos blocos
    - Mapa FINAL: Record = chaves consumidas pelos 4 templates reescritos + `campaignIntent`; chaves legadas transicionais órfãs removidas (grep `\{\{...\}\}` dos 4 `.md` ⊆ chaves do Record)
  </acceptance_criteria>
  <done>Blocos ajustados por intent (spotlight/exclusive) no helper + mapa FINAL sem chaves legadas órfãs — sem duplicação de regra</done>
</task>

<task type="auto">
  <name>Task 3: Co-migrar IN-PLAN as suites-alvo (golden key-set → conjunto FINAL, prompt-reframe spotlight/exclusive, validatePrompts desses intents) + gates 100% verdes + amostragem + conferência editorial</name>
  <files>src/lib/image-generation/services/__tests__/image-generation-service.test.ts, src/lib/campaign/__tests__/prompt-reframe.test.ts, prompts/campaign-image-director-spotlight.md, prompts/campaign-image-director-exclusive.md, src/lib/image-generation/services/art-director-briefing.ts</files>
  <read_first>
    - src/lib/image-generation/services/__tests__/image-generation-service.test.ts (golden por intent :556-719 — EXPECTED_KEYS :557-569 co-migrado ao conjunto transicional no 45-03; casos kqo a/b/c :677-708; validatePrompts :65-298)
    - src/lib/campaign/__tests__/prompt-reframe.test.ts (âncoras do 45-03: base/offer já com âncoras novas; spotlight/exclusive ainda com as âncoras antigas)
    - prompts/campaign-image-director-spotlight.md (novo)
    - prompts/campaign-image-director-exclusive.md (novo)
    - .planning/phases/45-briefing-contextual-do-diretor-de-arte/45-01-SUMMARY.md (tabela de decisão de chaves — remoção/realocação)
    - openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/tasks.md (§4 — tasks 4.3-4.4)
  </read_first>
  <action>
    Co-migrar IN-PLAN as suites que a remoção das chaves órfãs (Task 2) quebra e fechar com os 4 gates 100% verdes (green-gates — nenhuma suite vermelha herdada pelo 45-05):

    1. **Golden key-set → conjunto FINAL em `image-generation-service.test.ts`:** atualizar `EXPECTED_KEYS` (:557-569, já no conjunto transicional após o 45-03) para o conjunto FINAL = chaves reais do Record pós-Task 2 (assinatura determinada pelos 4 templates reescritos + `campaignIntent`); asserções de tamanho usam `EXPECTED_KEYS.length` (nunca literal fixo); re-apontar/remover asserts de valor de chaves legadas que saíram do Record — kqo a/b/c (:677-708) que assertavam `vars.mandatoryArtworkText`/`vars.illustrativeNotice` passam a assertar o bloco que absorveu o conteúdo (`vars.requiredArtworkTextSection` contém só o texto do lojista; `vars.illustrativeNoticeSection` contém a constante canônica; cenário (c) texto legado integral → `requiredArtworkTextSection`); asserts de `preserveImageDirective`/`campaignIntent` seguem válidos se as chaves sobreviverem (senão, cobrir via bloco `productReferenceSection`/`creativeDirectionSection`).
    2. **`prompt-reframe.test.ts` — âncoras novas de spotlight/exclusive:** aplicar a spotlight/exclusive as mesmas âncoras estruturais que o 45-03 aplicou a base/offer (presença das seções editoriais fixas; presença dos 8 placeholders de bloco; ausência da micro-tabela/`Notas Adicionais`/frases incondicionais antigas da cauda) e REMOVER as constantes de âncora antigas (:17-22 — LINHA_AVISO_SEPARADO/LINHA_MANTIDA/LINHA_TABELA_AVISO/LINHA_VALIDADE) por completo — os 4 `.md` agora estão na estrutura-alvo e o arquivo cobre os 4 de forma uniforme (com asserts por intent onde o DNA difere: exclusive sem preço; spotlight sem validade). Arquivo VERDE ao final.
    3. **Casos de `validatePrompts` do diretor para spotlight/exclusive:** com o serviço construído com `PromptLoader` REAL (default = `prompts/`), assertar `valid=true` para brief spotlight (preço único, preserveImageContext, sem validade) e brief exclusive (sem preço, sem badge) — prova de zero placeholders não resolvidos entre os templates novos e o mapa FINAL (D4). Os casos com loader mockado (:65-298) seguem válidos.
    4. **Gates 100% verdes (green-gates):** `npm run typecheck`, `npm run lint`, `npm run build` — ZERO erros; `npx vitest run` — rodada COMPLETA VERDE, sem lista de testes vermelhos e sem pendência formal para o 45-05; suites irmãs (image-review-service.test.ts, copy, use-campaign-form-*, route.test.ts) verdes SEM edição (não-mudança, D7); `art-director-briefing.test.ts` verde.
    5. **Validação por amostragem** (F45-16): montar prompt spotlight (preço único, preserveImageContext true, sem validade, sem texto obrigatório) e prompt exclusive (sem preço, badge opcional ausente, preserveImageContext true) + variações mínimas. Confirmar: sem seções vazias/headings órfãos/linhas de tabela em branco/placeholders residuais; cada natureza opcional/sensível em UMA ocorrência; tom por intent preservado (spotlight sem urgência; exclusive sem preço/premium); preserveImageDirective presente em não-offer com preserveImageContext.
    6. **Conferência editorial com mão leve** (F45-17): revisar o diff de spotlight/exclusive e confirmar DNA do diretor por intent (destaque sem urgência / premium sem preço), apenas reorganização/rotulagem/deduplicação, sem paráfrase desnecessária. Registrar no SUMMARY junto do registro das co-migrações das steps 1-3.
  </action>
  <verify>
    <automated>npx vitest run; if ($?) { npm run typecheck; if ($?) { npm run lint; if ($?) { npm run build } } }</automated>
  </verify>
  <acceptance_criteria>
    - typecheck/lint/build — zero erros; `npx vitest run` COMPLETO E VERDE — nenhuma suite vermelha remanescente; nenhuma pendência de co-migração para o 45-05 (green-gates por plano)
    - `image-generation-service.test.ts` com golden key-set co-migrado ao conjunto FINAL (asserções de valor de chaves legadas re-apontadas às seções/blocos; sem asserts órfãos) e verde
    - `prompt-reframe.test.ts` com os 4 `.md` cobertos pelas âncoras estruturais novas; constantes antigas removidas; verde
    - Casos de validatePrompts com `PromptLoader` real para spotlight e exclusive verdes — zero placeholders residuais (D4)
    - Amostragem spotlight e exclusive documentada no SUMMARY (sem vazios/duplicação; tom por intent; preserveImageDirective correto)
    - Conferência editorial mão leve registrada (DNA do diretor preservado; sem paráfrase de frases funcionantes)
    - Offer/base (45-03) não regredidos; suites irmãs verdes e não editadas
  </acceptance_criteria>
  <done>Suites-alvo co-migradas in-plan (golden → conjunto FINAL, prompt-reframe spotlight/exclusive, validatePrompts); 4 gates 100% verdes; amostragem + conferência editorial registradas</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template ↔ modelo LLM | Instrução por intent (spotlight/exclusive) — risco de urgência indevida no spotlight ou preço no exclusive |
| texto do lojista → template | Texto livre com chaves pode virar placeholder residual |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-04 | Tampering | prompts spotlight/exclusive | mitigate | Montagem por presença + amostragem por intent (sem vazios/duplicação/placeholders); validatePrompts zero placeholders (D4) |
| T-45-04a | Spoofing | Tom por intent | mitigate | DNA do intent preservado (spotlight sem urgência/DE/POR; exclusive sem preço/badge promocional) — verificado por âncoras editoriais e conferência mão leve (F45-17) |
| T-45-04b | Tampering | preserveImageDirective | mitigate | Diretiva de não-recorte anexada só para não-offer com preserveImageContext (regra atual movida sem duplicar) |
| T-45-04c | Information Disclosure | Suites de co-migração | mitigate | Co-migração IN-PLAN das suites que a remoção de chaves órfãs quebra (golden → conjunto FINAL, prompt-reframe spotlight/exclusive, validatePrompts desses intents); `npx vitest run` completo VERDE ao final do plano (green-gates); suites irmãs (revisor/copy/form/rota) verdes sem edição — não-mudança (D7) |
| T-45-04d | Tampering | Mapa FINAL (D5) | mitigate | Record reduzido ao consumido pelos 4 templates reescritos + `campaignIntent`; remoção de chave só após os templates novos estarem no disco (grep `\{\{...\}\}` ⊆ chaves); validação com `PromptLoader` real por intent — placeholder residual = falha de gate |
| T-45-SC | Tampering | npm installs | accept | Nenhum pacote novo — sem gate de legitimidade |
</threat_model>

<verification>
- Spotlight e exclusive reescritos com os 8 slots; sem validade; spotlight preço único; exclusive sem preço; DNA editorial preservado
- Helper com blocos intent-aware (facts sem preço no exclusive; preserveImageDirective em não-offer)
- Mapa FINAL: Record = chaves consumidas pelos 4 templates reescritos + `campaignIntent`; chaves legadas transicionais órfãs removidas
- `image-generation-service.test.ts` co-migrado ao conjunto FINAL e verde; `prompt-reframe.test.ts` com âncoras novas de spotlight/exclusive (constantes antigas removidas) e verde
- `npm run typecheck`/`npm run lint`/`npm run build` — zero erros; `npx vitest run` COMPLETO E VERDE (green-gates — nenhuma suite vermelha, nenhuma pendência para o 45-05)
- Amostragem spotlight/exclusive (F45-16) e conferência editorial mão leve (F45-17) documentadas no SUMMARY
- Offer/base (45-03) não regredidos; suites irmãs verdes e não editadas
</verification>

<success_criteria>
- Spotlight + exclusive em estrutura editorial + blocos com mão leve (D2/D3)
- Blocos por intent no helper sem duplicação de regra (D3)
- Prompts montados sem vazios/duplicação/placeholders; tom por intent preservado
- Mapa FINAL de `buildPromptVariables` (D5) — chaves transicionais órfãs removidas; apenas chaves consumidas + `campaignIntent`
- Suites-alvo co-migradas no próprio 45-04 (golden → conjunto FINAL, prompt-reframe spotlight/exclusive, validatePrompts desses intents) — `npx vitest run` 100% VERDE ao final (green-gates); o 45-05 parte de base verde e amplia cobertura de forma aditiva
</success_criteria>

<output>
Create `.planning/phases/45-briefing-contextual-do-diretor-de-arte/45-04-SUMMARY.md` when done
</output>

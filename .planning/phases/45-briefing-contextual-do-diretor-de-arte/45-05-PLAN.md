---
phase: 45-briefing-contextual-do-diretor-de-arte
plan: 05
type: execute
wave: 3
depends_on: [45-03, 45-04]
files_modified:
  - src/lib/image-generation/services/__tests__/image-generation-service.test.ts
  - src/lib/campaign/__tests__/prompt-reframe.test.ts
  - src/lib/image-generation/services/__tests__/art-director-briefing.test.ts
autonomous: true
requirements: [F45-18, F45-19, F45-20, F45-21, F45-22]

must_haves:
  truths:
    - "Base 100% VERDE herdada dos 45-03/45-04 (green-gates por plano): golden key-set já no conjunto FINAL; prompt-reframe com âncoras estruturais de base/offer (45-03) e spotlight/exclusive (45-04); validatePrompts por intent verde (loader real); `art-director-briefing.test.ts` verde desde o 45-03 — o 45-05 NÃO resolve pendências herdadas (não há suites vermelhas a corrigir)"
    - "Asserções de key-set EXATO do golden substituídas/ampliadas para o modelo de invariantes D5 (sobre base verde): placeholders dos templates ⊆ chaves fornecidas por intent; determinismo (mesmo input → mesmo prompt); presente/ausente por bloco (offer/spotlight/exclusive); domínio/superfície externa intocados"
    - "validatePrompts ampliado por cenário (aditivo sobre os casos já co-migrados em 45-03/45-04): casos com/sem texto obrigatório e aviso em briefs representativos; texto do lojista com `{{` saneado sem placeholder residual (D6); casos do quick 260902-kqo (a/b/c) exercitando o comportamento nas seções/blocos"
    - "prompt-reframe.test.ts ampliado com invariantes transversais dos 4 `.md`: presença das seções editoriais, ausência de template seco de micro-placeholders, aviso/texto obrigatório em seção própria, hierarquia 1+N, ausência da frase incondicional antiga"
    - "Novo teste por bloco presente/ausente no art-director-briefing.test.ts (ADITIVO — parte do arquivo verde herdado): brief mínimo → nenhuma seção vazia/heading órfão/linha em branco; brief completo → cada natureza em UMA ocorrência; texto do lojista com `{{` saneado sem placeholder residual; determinismo; por intent"
    - "Suites irmãs de revisor/copy/form/rota permanecem verdes SEM co-migração (regressão de não-mudança, D7)"
  artifacts:
    - path: "src/lib/image-generation/services/__tests__/art-director-briefing.test.ts"
      provides: "Testes por bloco presente/ausente + deduplicação + saneamento (aditivos)"
      contains: "requiredArtworkTextSection"
    - path: "src/lib/image-generation/services/__tests__/image-generation-service.test.ts"
      provides: "Invariantes de montagem no lugar das asserções de key-set exato"
      contains: "placeholders"
  key_links:
    - from: "src/lib/image-generation/services/__tests__/image-generation-service.test.ts"
      to: "prompts/campaign-image-director-offer.md"
      via: "invariante placeholders(templates) ⊆ chaves(buildPromptVariables) por intent"
      pattern: "campaign-image-director"
    - from: "src/lib/image-generation/services/__tests__/art-director-briefing.test.ts"
      to: "src/lib/image-generation/services/art-director-briefing.ts"
      via: "asserções diretas nas funções puras de bloco"
      pattern: "campaignFactsSection"
---

<objective>
Ampliar a cobertura dos testes sobre a **base 100% VERDE herdada dos 45-03/45-04** (green-gates: nenhuma suite vermelha espera este plano — as co-migrações obrigatórias de golden key-set, âncoras de `prompt-reframe` e casos de `validatePrompts` já aconteceram naqueles planos). Este plano é de **ampliação aditiva**: (a) substituir/ampliar as asserções de key-set EXATO do golden pelo modelo de **invariantes** D5 (placeholders dos templates ⊆ chaves fornecidas; determinismo; presente/ausente por bloco em briefs representativos; superfície externa intocada); (b) ampliar `validatePrompts` por cenário (com/sem texto obrigatório e aviso; saneamento `{{`→`{`; kqo a/b/c exercitando as seções); (c) ampliar `prompt-reframe.test.ts` com invariantes transversais dos 4 `.md`; (d) ampliar `art-director-briefing.test.ts` de forma ADITIVA (presente/ausente por bloco, UMA ocorrência por natureza, saneamento, determinismo, por intent); (e) regressão verde das suites irmãs SEM co-migração (D7). NÃO é plano-bombeiro: não há pendências/falhas herdadas dos 45-03/45-04 para resolver.

Purpose: D5 (golden exato → invariantes — destino final do contrato interno novo) + D4 (testes de cenário presente/ausente e ausência de `##` órfãos/linhas de tabela em branco) + D6 (teste de saneamento) + D7 (suites irmãs sem co-migração — regressão de não-mudança). F45-18/F45-19/F45-20 têm cobertura parcial nos 45-03/45-04; aqui entram os resíduos aditivos (invariantes transversais e cenários).
Output: 3 arquivos de teste ampliados (invariantes + cenários + âncoras transversais) partindo de base verde; suites irmãs verdes; 4 gates verdes ao final.

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-briefing-contextual-do-diretor-de-arte/45-CONTEXT.md (D1-D7; F45-18..F45-22 :84-88 — F45-18/19/20 com cobertura parcial nos 45-03/45-04; canonical_refs testes :164-168)
@openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/design.md (D4 :101-108; D5 :110-115; Migration dos REMOVED :87-107)
@openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/specs/ai-image-generation/spec.md (REMOVED requirements com Migration — paridade substituída por invariantes)
@openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/tasks.md (§5 — tasks 5.1-5.5)
@src/lib/image-generation/services/__tests__/image-generation-service.test.ts (902 linhas — golden por intent :556-719; validatePrompts :65-298)
@src/lib/campaign/__tests__/prompt-reframe.test.ts (âncoras estruturais dos 4 .md — co-migradas no 45-03/45-04)
@src/lib/image-generation/services/__tests__/art-director-briefing.test.ts (co-migrado no 45-03 — verde)

<interfaces>
Estado esperado dos testes AO INÍCIO deste plano (após os 45-03/45-04 — base 100% verde; nenhuma suite vermelha a corrigir):

1. `image-generation-service.test.ts`:
   - Golden por intent: describe ":556-719" com `EXPECTED_KEYS` no **conjunto FINAL** (co-migrado no 45-04; `toHaveLength` usa `EXPECTED_KEYS.length`); casos 8.16 offer/spotlight/exclusive (:581-624), 9.3/9.5 (:626-654), F41 20 multi-imagem (:656-675), kqo a/b/c (:677-708) verdes, com asserts de valor de chaves removidas re-apontados às seções/blocos (45-03/45-04).
   - validatePrompts: describe ":65-298" verde (loader mockado) + casos com `PromptLoader` real por intent adicionados no 45-03 (offer) e 45-04 (spotlight/exclusive).
   - 8.17 repertório (:710-719): re-apontado no 45-03 ao bloco canônico (repartição D3).
   - Helpers locais: `createMinimalBrief` (:18-33), `createContext` (:36-63) — reutilizáveis pelas novas asserções de invariante.
2. `prompt-reframe.test.ts`: âncoras estruturais novas cobrindo os 4 `.md` (base/offer no 45-03; spotlight/exclusive no 45-04); constantes de âncora antigas removidas; verde.
3. `art-director-briefing.test.ts` (45-02 + co-migração 45-03): verde — será AMPLIADO de forma aditiva neste plano (presente/ausente, deduplicação, saneamento, determinismo, por intent).

Alvo das invariantes (D5) — a ampliação deste plano: substituir/ampliar as asserções de key-set EXATO (conjunto FINAL) por:
(a) para cada intent (offer/spotlight/exclusive), carregar o template `campaign-image-director-${intent}.md` do disco, extrair os placeholders `{{...}}`, montar via `buildPromptVariables` e assertar: placeholders ⊆ chaves fornecidas;
(b) determinismo: montar 2× o mesmo brief → mesmo prompt final (assemblePrompt);
(c) presente/ausente por bloco em briefs representativos (mínimo e completo por intent);
(d) contrato externo intacto: suites irmãs (revisor/copy/form/rota) verdes sem alteração (Task 5).

Suites irmãs SEM co-migração (D7): `image-review-service.test.ts`, `copy-director-service.test.ts`/`copy-director-prompt.test.ts`, `use-campaign-form-*.test.ts`, `route.test.ts` (generate-image), `brief*.test.ts`/`brief-snapshot.test.ts` — devem permanecer verdes e NÃO ser editadas.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Substituir as asserções de key-set exato (conjunto FINAL) pelo modelo de invariantes D5 em image-generation-service.test.ts</name>
  <files>src/lib/image-generation/services/__tests__/image-generation-service.test.ts</files>
  <read_first>
    - src/lib/image-generation/services/__tests__/image-generation-service.test.ts (:556-719 golden por intent com EXPECTED_KEYS no conjunto FINAL — co-migrado no 45-04; :18-63 helpers createMinimalBrief/createContext; :10-13 mock do loader)
    - src/lib/image-generation/services/image-generation-service.ts (:896-1003 buildPromptVariables — Record FINAL; :1005-1023 assemblePrompt)
    - openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/specs/ai-image-generation/spec.md (REMOVED "Preservação comportamental" — Migration :89; REMOVED "buildPromptVariables includes..." — Migration :95)
  </read_first>
  <action>
    Ampliar (sobre base 100% verde — sem resolver pendência alguma, pois não há suites vermelhas herdadas) o describe "golden tests por intent" (:556-719) substituindo as asserções de key-set EXATO pelo modelo de invariantes D5:

    1. Criar helper de leitura de template (semelhante ao `readPrompt` do prompt-reframe): `readPrompt(name)` lê `prompts/campaign-image-director-${intent}.md` do disco; extrair placeholders com regex `/\{\{([a-zA-Z]+)\}\}/g`.
    2. **Invariante (a) placeholders ⊆ chaves:** para offer/spotlight/exclusive, montar `buildPromptVariables` (via `(service as any)`) e assertar que todo placeholder do template correspondente existe no Record retornado (subset — o Record pode ter chaves de orquestração extras como `campaignIntent`). Este invariante substitui a igualdade exata com `EXPECTED_KEYS` — que sai do arquivo (o contrato interno deixa de ser "mesmo conjunto de keys" e passa a ser "toda chave usada pelo template é fornecida", D5).
    3. **Invariante (b) determinismo:** mesmo brief + context montado 2× via `assemblePrompt` → mesmo texto (ou, se assemblePrompt não for acessível no mock, montar 2× `buildPromptVariables` + interpolar via `PromptLoader` real e comparar).
    4. **Invariante (c) presente/ausente por bloco (offer/spotlight/exclusive):** com brief mínimo e brief completo (validade, mandatoryArtworkText livre, aviso, campaignDetails/additionalDetails, availabilityNotes, sensitiveConstraints, 1 primary + N auxiliares) assertar que os slots condicionais (`{{requiredArtworkTextSection}}`, `{{illustrativeNoticeSection}}`, `{{commercialDetailsSection}}`, `{{constraintsSection}}`) aparecem no prompt final apenas quando o conteúdo existe — e que NENHUM `{{...}}` residual permanece após a interpolação.
    5. Remover o array `EXPECTED_KEYS` (:557-569) e as asserções de igualdade exata/`toHaveLength(...)` (:591/610/621/648/673) — o modelo exato morre aqui; manter asserts de valor que ainda façam sentido por intenção (ex.: `vars.campaignIntent`, valores das seções montadas), ajustando nomes conforme o Record FINAL.
    6. Reaproveitar `createMinimalBrief`/`createContext`; ajustar campos que mudaram de nome/estrutura.
    7. **NÃO** editar asserts que pertençam ao revisor (`image-review-service.test.ts`) nem rota/form.
    8. Registro no SUMMARY: quais asserções exatas foram substituídas e os invariantes criados — com a nota de que a base já estava verde (green-gates 45-03/45-04) e esta task apenas troca o critério de asserção para o destino D5.
  </action>
  <verify>
    <automated>npx vitest run src/lib/image-generation/services/__tests__/image-generation-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Asserções de key-set exato (EXPECTED_KEYS/igualdade/`toHaveLength`) substituídas pelos invariantes (a)-(c) por intent
    - `image-generation-service.test.ts` verde (sem regressão — arquivo já vinha verde do 45-04)
    - Asserts de determinismo presentes (mesmo input → mesmo prompt)
    - Nenhum assert de `EXPECTED_KEYS` remanescente no arquivo
  </acceptance_criteria>
  <done>Asserções de key-set exato → invariantes D5 (placeholders ⊆ chaves, determinismo, presente/ausente) sobre base verde</done>
</task>

<task type="auto">
  <name>Task 2: Ampliar cobertura de validatePrompts por cenário (blocos presente/ausente; com/sem texto obrigatório e aviso; saneamento {{) — sobre base verde</name>
  <files>src/lib/image-generation/services/__tests__/image-generation-service.test.ts</files>
  <read_first>
    - src/lib/image-generation/services/__tests__/image-generation-service.test.ts (:65-298 validatePrompts — casos com loader mockado e com loader real adicionados no 45-03/45-04)
    - openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/specs/art-director-contextual-briefing/spec.md (Requirement: Validação cobre montagem contextual sem placeholders residuais)
  </read_first>
  <action>
    Ampliar o describe de `validatePrompts` (:65-298) com cenários ADITIVOS (os casos de co-migração para os novos slots já foram feitos no 45-03/45-04 — nada aqui corrige falha herdada; o arquivo já está verde):

    1. Casos com/sem texto obrigatório e aviso cobrindo a montagem REAL (loader real, `prompts/`): (i) brief com `merchantText` livre → `valid: true` e prompt final contém a seção de texto obrigatório (`requiredArtworkTextSection`); (ii) apenas aviso ilustrativo → prompt final contém a seção do aviso e NÃO contém seção de texto obrigatório; (iii) sem aviso e sem texto → nenhuma das duas seções, valid continua true. Cobrir offer, spotlight e exclusive em briefs representativos (invariante presente/ausente no nível do validatePrompts).
    2. **Saneamento (D6):** cenário "texto do lojista com `{{` não gera placeholder não resolvido": montar prompt final com texto contendo `{{x}}` e assertar `validatePrompts` retorna valid (placeholder saneado antes da interpolação) e que o prompt final não contém `{{x}}` residual.
    3. Comportamento kqo (a/b/c) exercitado nas seções/blocos (asserções de valor já re-apontadas no 45-04): se algum cenário kqo ainda assertar apenas valor de chave avulsa removida, reforçá-lo assertando o mesmo comportamento no valor do bloco montado (`requiredArtworkTextSection` só com o texto livre; `illustrativeNoticeSection` com a constante canônica; caso (c) texto integral) — quando o cenário aditivo acrescentar cobertura real.
    4. Garantir que nenhum novo cenário dependa de conteúdo dos templates antigos (micro-tabela/Notas Adicionais) — os cenários validam a estrutura contextual vigente.
  </action>
  <verify>
    <automated>npx vitest run src/lib/image-generation/services/__tests__/image-generation-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Cenários com/sem texto obrigatório e aviso cobertos por intent (3 variações, loader real) e verdes
    - Caso de saneamento de `{{` coberto e verde (sem placeholder residual no prompt final)
    - Nenhum cenário referencia o mapa/templates antigos; arquivo verde (sem regressão da base)
  </acceptance_criteria>
  <done>validatePrompts ampliado por cenário (presente/ausente por intent, saneamento {{) — base verde mantida</done>
</task>

<task type="auto">
  <name>Task 3: Ampliar prompt-reframe.test.ts com invariantes transversais dos 4 .md (base verde — âncoras estruturais já aplicadas nos 45-03/45-04)</name>
  <files>src/lib/campaign/__tests__/prompt-reframe.test.ts</files>
  <read_first>
    - src/lib/campaign/__tests__/prompt-reframe.test.ts (arquivo — âncoras estruturais dos 4 `.md`; constantes antigas LINHA_* já removidas no 45-03/45-04)
    - prompts/campaign-image-director.md (novo — base)
    - prompts/campaign-image-director-offer.md (novo)
    - prompts/campaign-image-director-spotlight.md (novo)
    - prompts/campaign-image-director-exclusive.md (novo)
    - openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/specs/art-director-contextual-briefing/spec.md (Requisitos editoriais: 4 prompts legíveis com seções editoriais; blocos nomeados; aviso/texto obrigatório em seção própria; hierarquia 1+N)
  </read_first>
  <action>
    Ampliar `prompt-reframe.test.ts` de forma ADITIVA (as âncoras estruturais por arquivo já existem dos 45-03/45-04; nada aqui corrige âncora antiga remanescente) com invariantes transversais que fixam a estrutura-alvo (D2/D3) nos 4 `.md`:

    1. **Presença de seções editoriais fixas** nos 4 arquivos (papel/persona, composição, instruções obrigatórias anti-invenção, fidelidade/criatividade) — âncoras de texto editorial mantido pela mão leve (ex.: frases de anti-invenção e de liberdade criativa), em loop `PROMPTS`.
    2. **Ausência de template seco:** cada arquivo tem prosa editorial além dos placeholders (assert de contagem — ex.: mais de N linhas de texto não-placeholder); ausência de micro-tabela "Informações da Campanha"/linhas `| **Aviso ilustrativo` e `| **Validade**`.
    3. **Blocos nomeados uniformes:** os 8 placeholders de bloco (`{{campaignFactsSection}}`, `{{commercialDetailsSection}}`, `{{requiredArtworkTextSection}}`, `{{illustrativeNoticeSection}}`, `{{identityReferenceSection}}`, `{{productReferenceSection}}`, `{{constraintsSection}}`, `{{creativeDirectionSection}}`) presentes nos 4 arquivos (conjunto idêntico por arquivo).
    4. **Seções próprias:** aviso e texto obrigatório NÃO aparecem como frase incondicional solta nos templates (assert de ausência das frases antigas da cauda nos 4 arquivos — elas vivem DENTRO dos blocos montados, no código) e cada natureza condicional existe apenas como slot nomeado, não como linha de tabela.
    5. **Hierarquia 1+N:** orientação de hierarquia primary × auxiliares presente na estrutura editorial dos 4 arquivos (quando aplicável) na nova redação.
    6. **DNA por intent** (asserts por arquivo, mantidos/fortalecidos): exclusive sem preço; spotlight sem validade; base/offer com validade condicional.
    7. Documentar no SUMMARY: os invariantes transversais adicionados sobre as âncoras por arquivo já existentes.
  </action>
  <verify>
    <automated>npx vitest run src/lib/campaign/__tests__/prompt-reframe.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - prompt-reframe.test.ts verde com os invariantes transversais (base já verde dos 45-03/45-04 — sem regressão)
    - Asserts de presença editorial + 8 slots idênticos nos 4 arquivos + ausência de template seco/micro-tabela/frases incondicionais antigas
    - Asserts de DNA por intent (exclusive sem preço; spotlight sem validade) presentes
  </acceptance_criteria>
  <done>prompt-reframe.test.ts ampliado com invariantes transversais dos 4 .md — verde</done>
</task>

<task type="auto">
  <name>Task 4: Ampliar art-director-briefing.test.ts (ADITIVO — asserções legadas já co-migradas no 45-03) com testes por bloco presente/ausente + deduplicação + saneamento</name>
  <files>src/lib/image-generation/services/__tests__/art-director-briefing.test.ts</files>
  <read_first>
    - src/lib/image-generation/services/__tests__/art-director-briefing.test.ts (base do 45-02)
    - src/lib/image-generation/services/art-director-briefing.ts (funções de bloco do 45-03/45-04)
    - openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/specs/art-director-contextual-briefing/spec.md (Requisitos: prompt contextual sem vazios; texto obrigatório em seção própria; aviso em seção própria; teste por bloco presente/ausente :154-158)
  </read_first>
  <action>
    Ampliar `art-director-briefing.test.ts` com os testes de bloco presente/ausente (F45-21). NOTA DE COORDENAÇÃO: as asserções legadas afetadas pelo rework de offer (validade/details do repertório; `buildValidationSummary`) já foram co-migradas/removidas no 45-03 Task 2 — esta task é ESTRITAMENTE ADITIVA sobre a base verde herdada (não remover/re-adicionar asserções legadas; nada aqui corrige falha do 45-03/45-04):

    1. **Brief mínimo** (sem merchantText, sem aviso, sem validade, sem details/availability/sensitiveConstraints, 1 imagem primary): assertar que os blocos condicionais retornam `""` (`commercialDetailsSection`, `requiredArtworkTextSection`, `illustrativeNoticeSection`, `constraintsSection`) e que montar o prompt final NÃO produz seção vazia/heading órfão/linha de tabela em branco/placeholder residual.
    2. **Brief completo** (texto obrigatório + aviso + validade offer + details + availability + restrições + 1 primary + 2 auxiliares): assertar que cada natureza opcional/sensível aparece em **UMA ocorrência** no prompt final: validade → só `campaignFactsSection`; details/availability → só `commercialDetailsSection`; aviso → só `illustrativeNoticeSection`; texto obrigatório → só `requiredArtworkTextSection`; restrições → só `constraintsSection`. Produto/loja podem aparecer legitimamente em múltiplos contextos (não configuram duplicação proibida).
    3. **Saneamento (D6):** texto do lojista com `{{exemplo}}` e `}}` → bloco `requiredArtworkTextSection`/`commercialDetailsSection` sem `{{`/`}}` residual; montar prompt final e assertar zero `{{...}}` não resolvido (via regex).
    4. **Por intent:** spotlight sem validade; exclusive sem preço (facts); preserveImageDirective apenas não-offer + preserveImageContext.
    5. Montagem determinística: mesmo input 2× → mesmo texto (reforço do invariant no nível do módulo).
    NÃO adicionar comentários no código de teste.
  </action>
  <verify>
    <automated>npx vitest run src/lib/image-generation/services/__tests__/art-director-briefing.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Testes por bloco presente/ausente (brief mínimo e completo) verdes
    - Teste de UMA ocorrência por natureza (deduplicação) verde
    - Teste de saneamento `{{`→`{` sem placeholder residual verde
    - Testes por intent (spotlight/exclusive) verdes; determinismo coberto
  </acceptance_criteria>
  <done>art-director-briefing.test.ts ampliado (presente/ausente, deduplicação, saneamento, determinismo) e verde</done>
</task>

<task type="auto">
  <name>Task 5: Gates + regressão das suites irmãs SEM co-migração (D7)</name>
  <files>src/lib/image-generation/services/__tests__/image-generation-service.test.ts, src/lib/campaign/__tests__/prompt-reframe.test.ts, src/lib/image-generation/services/__tests__/art-director-briefing.test.ts</files>
  <read_first>
    - openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/specs/art-director-contextual-briefing/spec.md (Requirement: Camada externa e revisor/copy director permanecem inalterados)
    - openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/tasks.md (§5.5)
  </read_first>
  <action>
    1. Rodar `npx vitest run` completo — deve estar VERDE desde a base herdada dos 45-03/45-04 (green-gates por plano); as ampliações aditivas das Tasks 1-4 mantêm o verde sem resolver pendência alguma.
    2. Confirmar que as suites irmãs NÃO foram editadas e seguem verdes: `image-review-service.test.ts`, `copy-director-service.test.ts`, `copy-director-prompt.test.ts`, `use-campaign-form-*.test.ts`, `route.test.ts` (generate-image), `brief.test.ts`/`brief-snapshot.test.ts`/`brief-mapper.test.ts` (regressão de não-mudança — D7).
    3. Rodar `npm run typecheck`, `npm run lint`, `npm run build` — zero erros.
    4. Registrar no SUMMARY: contagem de testes totais, invariantes/cenários adicionados nas Tasks 1-4, suites irmãs confirmadas sem edição, e prova de que nenhum arquivo congelado (revisor/copy/schema/rota/form helpers) foi alterado (git status).
  </action>
  <verify>
    <automated>npx vitest run; if ($?) { npm run typecheck; if ($?) { npm run lint; if ($?) { npm run build } } }</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` completo verde — mantido verde desde a base herdada dos 45-03/45-04 (nenhuma pendência de fases anteriores a resolver)
    - Suites irmãs verdes e NÃO editadas (git status/diff sem toque em image-review-service.test.ts, copy, form, route)
    - typecheck/lint/build zero erros
  </acceptance_criteria>
  <done>Gates verdes + suites irmãs sem co-migração (regressão de não-mudança D7) confirmadas sobre base verde</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teste ↔ contrato interno | Ampliação de testes que ancoram o mapa de variáveis — risco de perder cobertura real ao trocar o critério de asserção (exato → invariante) |
| teste ↔ superfícies congeladas | Edição acidental de suites irmãs (revisor/copy/form/rota) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-05 | Tampering | image-generation-service.test.ts | mitigate | Key-set exato → invariantes fortes (placeholders ⊆ chaves; determinismo; presente/ausente; contrato externo intocado) — não é relaxamento, é mudança de critério para o destino D5, executada sobre base verde (45-03/45-04) sem corrigir falhas herdadas |
| T-45-05a | Tampering | prompt-reframe.test.ts | mitigate | Invariantes transversais (presença de seções, ausência de template seco/micro-tabela/frases antigas, 8 slots uniformes) reforçam as âncoras estruturais por arquivo já aplicadas nos 45-03/45-04 (D2) |
| T-45-05b | Tampering | art-director-briefing.test.ts | mitigate | Ampliação ADITIVA (presente/ausente + UMA ocorrência por natureza + saneamento) fixa D3/D4/D6 no nível do módulo — arquivo verde desde o 45-03 |
| T-45-05c | Tampering | Suites irmãs | mitigate | Proibida edição de image-review-service.test.ts/copy/form/route — verificação de não-mudança via git status (D7) |
| T-45-05d | Information Disclosure | Textos de teste | accept | Fixtures usam dados sintéticos (createMinimalBrief) — sem PII |
| T-45-05e | Tampering | Regressão de ampliação | mitigate | Cada task termina com a suite-alvo verde; Task 5 roda `npx vitest run` completo — se qualquer teste regredir, corrigir a AMPLIAÇÃO aditiva (nunca "herdar" vermelho de plano anterior — não há) |
| T-45-SC | Tampering | npm installs | accept | Nenhum pacote novo — sem gate de legitimidade |
</threat_model>

<verification>
- Base verde confirmada (herdada dos 45-03/45-04) — nenhuma suite vermelha a corrigir no início do plano
- Asserções de key-set exato → invariantes (placeholders ⊆ chaves, determinismo, presente/ausente por bloco, contrato externo) — image-generation-service.test.ts verde
- validatePrompts ampliado por cenário (com/sem texto obrigatório e aviso por intent; saneamento `{{`; kqo a/b/c nas seções) verde
- prompt-reframe.test.ts com invariantes transversais dos 4 `.md` verde
- art-director-briefing.test.ts ampliado (ADITIVO): presente/ausente + UMA ocorrência por natureza + saneamento + determinismo verde
- `npx vitest run` completo verde (sem regressão da base); suites irmãs verdes sem edição; typecheck/lint/build zero erros
</verification>

<success_criteria>
- Asserções de key-set exato → modelo de invariantes D5 (placeholders ⊆ chaves; determinismo; presente/ausente) sobre base verde
- validatePrompts ampliado por cenário (D4/D6) e prompt-reframe com invariantes transversais (D2/D3)
- Testes por bloco presente/ausente + deduplicação + saneamento adicionados de forma aditiva (F45-21)
- Regressão completa verde — mantida desde os 45-03/45-04 — com suites irmãs sem co-migração (D7); 4 gates verdes ao final
</success_criteria>

<output>
Create `.planning/phases/45-briefing-contextual-do-diretor-de-arte/45-05-SUMMARY.md` when done
</output>

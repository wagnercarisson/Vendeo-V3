---
phase: 45-briefing-contextual-do-diretor-de-arte
plan: 05
type: execute
wave: 3
depends_on: [45-03, 45-04]
files_modified:
  - src/lib/image-generation/services/__tests__/image-generation-service.test.ts
  - src/lib/campaign/__tests__/prompt-reframe.test.ts
  - src/lib/image-generation/services/__tests__/art-director-briefing.test.ts
autonomous: true
requirements: [F45-18, F45-19, F45-20, F45-21, F45-22]

must_haves:
  truths:
    - "Golden tests por intent (39 keys exatas) co-migrados para invariantes: placeholders dos templates ⊆ chaves fornecidas; determinismo (mesmo input → mesmo prompt); presente/ausente por bloco (offer/spotlight/exclusive); domínio/superfície externa intocados (D5)"
    - "Testes de validatePrompts atualizados (novos slots; casos com/sem texto obrigatório e aviso) + casos do quick 260902-kqo (a/b/c) alinhados à nova estrutura"
    - "prompt-reframe.test.ts com novas âncoras: presença das seções editoriais nos 4 `.md`, ausência de templates secos de micro-placeholders, aviso/texto obrigatório em seção própria, hierarquia 1+N, ausência da frase incondicional antiga"
    - "Novo teste por bloco presente/ausente no art-director-briefing.test.ts: brief mínimo → nenhuma seção vazia/heading órfão/linha em branco; brief completo → cada natureza em UMA ocorrência; texto do lojista com `{{` saneado sem placeholder residual"
    - "Suites irmãs de revisor/copy/form/rota permanecem verdes SEM co-migração (regressão de não-mudança, D7)"
  artifacts:
    - path: "src/lib/image-generation/services/__tests__/art-director-briefing.test.ts"
      provides: "Testes por bloco presente/ausente + deduplicação + saneamento"
      contains: "requiredArtworkTextSection"
    - path: "src/lib/image-generation/services/__tests__/image-generation-service.test.ts"
      provides: "Invariantes de montagem no lugar do golden 39 keys"
      contains: "placeholders"
  key_links:
    - from: "src/lib/image-generation/services/__tests__/image-generation-service.test.ts"
      to: "prompts/campaign-image-director-offer.md"
      via: "invariante placeholders(templates) ⊆ chaves(buildPromptVariables) por intent"
      pattern: "campaign-image-director"
    - from: "src/lib/image-generation/services/__tests__/art-director-briefing.test.ts"
      to: "src/lib/image-generation/services/art-director-briefing.ts"
      via: "asserções diretas nas funções puras de bloco"
      pattern: "campaignFactsSection"
---

<objective>
Co-migrar e ampliar os testes após a reescrita contextual dos 4 `.md` e dos blocos (45-03/45-04): substituir os **golden tests de 39 keys exatas** por **invariantes** (placeholders dos templates ⊆ chaves fornecidas; determinismo; presente/ausente por bloco; superfície externa intocada) (D5); atualizar `validatePrompts` (novos slots; casos com/sem texto obrigatório e aviso; casos kqo a/b/c); atualizar `prompt-reframe.test.ts` com as novas âncoras editoriais; criar os testes do helper por bloco presente/ausente (brief mínimo → sem vazios; brief completo → uma ocorrência por natureza; saneamento `{{`→`{`). Garantir regressão verde das suites irmãs SEM co-migração (D7).

Purpose: D5 (golden → invariantes) + D4 (testes de cenário presente/ausente e ausência de `##` órfãos/linhas de tabela em branco) + D6 (teste de saneamento) + D7 (suites irmãs sem co-migração — regressão de não-mudança).
Output: 3 arquivos de teste co-migrados/ampliados; suites irmãs verdes.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-briefing-contextual-do-diretor-de-arte/45-CONTEXT.md (D1-D7; F45-18..F45-22 :84-88; canonical_refs testes :164-168)
@openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/design.md (D4 :101-108; D5 :110-115; Migration dos REMOVED :87-107)
@openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/specs/ai-image-generation/spec.md (REMOVED requirements com Migration — paridade substituída por invariantes)
@openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/tasks.md (§5 — tasks 5.1-5.5)
@src/lib/image-generation/services/__tests__/image-generation-service.test.ts (902 linhas — golden :556-708; validatePrompts :65-298; repertório :710+)
@src/lib/campaign/__tests__/prompt-reframe.test.ts (74 linhas — âncoras atuais :17-22)
@src/lib/image-generation/services/__tests__/art-director-briefing.test.ts (criado no 45-02)

<interfaces>
Estado atual dos testes (2026-09-02):

1. `image-generation-service.test.ts`:
   - Golden por intent: describe ":556-708" com `EXPECTED_KEYS` (:557-569, 39 chaves) + casos 8.16 offer/spotlight/exclusive (:581-624), 9.3/9.5 (:626-654), F41 20 multi-imagem (:656-675), kqo a/b/c (:677-708) — todos assertam conjunto exato de keys e `toHaveLength(39)`.
   - validatePrompts: describe ":65-298" (valid=true; placeholder não resolvido; validação por intent; reviewer com campaignIntent; kqo legalNotice etc.).
   - Repertório: ":710+" (8.17 buildCommercialRepertoire decide por validity.enabled/displayText).
   - Helpers locais: `createMinimalBrief` (:18-33), `createContext` (:36-63) — reutilizáveis pelas novas asserções de invariante.
2. `prompt-reframe.test.ts` (74 linhas): `readPrompt` (:6-8), `PROMPTS` (:10-15), âncoras atuais (:17-22): LINHA_AVISO_SEPARADO, LINHA_MANTIDA, LINHA_TABELA_AVISO, LINHA_VALIDADE; testes 16/17/check A/check B/21 — âncoras que a reescrita removeu/realocou (seções próprias) e precisam de novas âncoras.
3. `art-director-briefing.test.ts` (45-02): testes iniciais das funções puras extraídas — será AMPLIADO com blocos presente/ausente.

Alvo das invariantes (D5):
(a) para cada intent (offer/spotlight/exclusive), carregar o template `campaign-image-director-${intent}.md` do disco, extrair os placeholders `{{...}}`, montar via `buildPromptVariables` e assertar: placeholders ⊆ chaves fornecidas;
(b) determinismo: montar 2× o mesmo brief → mesmo prompt final (assemblePrompt);
(c) presente/ausente por bloco: brief mínimo e completo (cenários abaixo);
(d) contrato externo intacto: suites irmãs (revisor/copy/form/rota) verdes sem alteração (Task 4).

Suites irmãs SEM co-migração (D7): `image-review-service.test.ts`, `copy-director-service.test.ts`/`copy-director-prompt.test.ts`, `use-campaign-form-*.test.ts`, `route.test.ts` (generate-image), `brief*.test.ts`/`brief-snapshot.test.ts` — devem permanecer verdes e NÃO ser editadas.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Co-migrar golden tests (39 keys) → invariantes em image-generation-service.test.ts</name>
  <files>src/lib/image-generation/services/__tests__/image-generation-service.test.ts</files>
  <read_first>
    - src/lib/image-generation/services/__tests__/image-generation-service.test.ts (:556-708 golden por intent; :18-63 helpers createMinimalBrief/createContext; :10-13 mock do loader)
    - openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/specs/ai-image-generation/spec.md (REMOVED "Preservação comportamental" — Migration :89; REMOVED "buildPromptVariables includes..." — Migration :95)
  </read_first>
  <action>
    Substituir o describe "golden tests por intent" (:556-708) por invariantes de montagem:

    1. Criar helper de leitura de template (semelhante ao `readPrompt` do prompt-reframe): `readPrompt(name)` lê `prompts/campaign-image-director-${intent}.md` do disco; extrair placeholders com regex `/\{\{([a-zA-Z]+)\}\}/g`.
    2. **Invariante (a) placeholders ⊆ chaves:** para offer/spotlight/exclusive, montar `buildPromptVariables` (via `(service as any)`) e assertar que todo placeholder do template correspondente existe no Record retornado (subset — o Record pode ter chaves de orquestração extras como `campaignIntent`).
    3. **Invariante (b) determinismo:** mesmo brief + context montado 2× via `assemblePrompt` → mesmo texto (ou, se assemblePrompt não for acessível no mock, montar 2× `buildPromptVariables` + interpolar via `PromptLoader` real e comparar).
    4. **Invariante (c) presente/ausente por bloco (offer/spotlight/exclusive):** usar brief mínimo e brief completo (validade, mandatoryArtworkText livre, aviso, campaignDetails/additionalDetails, availabilityNotes, sensitiveConstraints, 1 primary + N auxiliares) e assertar que os slots condicionais (`{{requiredArtworkTextSection}}`, `{{illustrativeNoticeSection}}`, `{{commercialDetailsSection}}`, `{{constraintsSection}}`) aparecem no prompt final apenas quando o conteúdo existe — e que NENHUM `{{...}}` residual permanece após a interpolação.
    5. Remover as asserções de `EXPECTED_KEYS`/`toHaveLength(39)` e de chaves mortas específicas; manter asserts de valores-chave que ainda façam sentido por intenção (ex.: `vars.campaignIntent`, `vars.preserveImageDirective` quando aplicável), ajustando nomes conforme o novo Record.
    6. Reaproveitar `createMinimalBrief`/`createContext`; ajustar campos que mudaram de nome/estrutura.
    7. **NÃO** editar asserts que pertençam ao revisor (`image-review-service.test.ts`) nem rota/form.
    8. Registro no SUMMARY: quais testes antigos foram removidos/substituídos e os invariantes criados.
  </action>
  <verify>
    <automated>npx vitest run src/lib/image-generation/services/__tests__/image-generation-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Golden de 39 keys exatas substituído por invariantes (a)-(c) por intent
    - `image-generation-service.test.ts` verde com os invariantes
    - Asserts de determinismo presentes (mesmo input → mesmo prompt)
    - Nenhum assert de `EXPECTED_KEYS`/`toHaveLength(39)` remanescente no arquivo
  </acceptance_criteria>
  <done>Golden → invariantes co-migrado em image-generation-service.test.ts</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar testes de validatePrompts (novos slots; kqo a/b/c; com/sem texto obrigatório e aviso)</name>
  <files>src/lib/image-generation/services/__tests__/image-generation-service.test.ts</files>
  <read_first>
    - src/lib/image-generation/services/__tests__/image-generation-service.test.ts (:65-298 validatePrompts; :677-708 casos kqo)
    - openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/specs/art-director-contextual-briefing/spec.md (Requirement: Validação cobre montagem contextual sem placeholders residuais)
  </read_first>
  <action>
    Atualizar o describe de `validatePrompts` (:65-298) para a nova estrutura:

    1. Ajustar cenários existentes que referenciem o mapa antigo de variáveis do diretor (chaves removidas/renomeadas) para os novos slots (`campaignFactsSection` etc.).
    2. Adicionar casos com/sem texto obrigatório e aviso: (i) brief com `merchantText` livre → `valid: true` e prompt final contém a seção de texto obrigatório; (ii) apenas aviso ilustrativo → prompt final contém seção do aviso e NÃO contém seção de texto obrigatório; (iii) sem aviso e sem texto → nenhuma das duas seções, valid continua true.
    3. Atualizar os casos do quick 260902-kqo (a/b/c) para a nova estrutura (split continua na camada de variáveis — assertar valores saneados de `requiredArtworkTextSection`/`illustrativeNoticeSection` quando aplicável; se os testes eram de valor de chave do Record, migrar para valor do bloco montado).
    4. Garantir que um cenário cobre "texto do lojista com `{{` não gera placeholder não resolvido" (D6): montar prompt final com texto contendo `{{x}}` e assertar `validatePrompts` retorna valid (placeholder saneado antes da interpolação).
  </action>
  <verify>
    <automated>npx vitest run src/lib/image-generation/services/__tests__/image-generation-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Cenários de validatePrompts atualizados para os novos slots
    - Casos com/sem texto obrigatório e aviso cobertos (3 variações)
    - Casos kqo a/b/c atualizados; caso de saneamento de `{{` coberto e verde
  </acceptance_criteria>
  <done>validatePrompts + kqo a/b/c co-migrados e verdes</done>
</task>

<task type="auto">
  <name>Task 3: Atualizar prompt-reframe.test.ts com as novas âncoras editoriais dos 4 .md</name>
  <files>src/lib/campaign/__tests__/prompt-reframe.test.ts</files>
  <read_first>
    - src/lib/campaign/__tests__/prompt-reframe.test.ts (arquivo inteiro — 74 linhas; âncoras atuais :17-22)
    - prompts/campaign-image-director.md (novo — base)
    - prompts/campaign-image-director-offer.md (novo)
    - prompts/campaign-image-director-spotlight.md (novo)
    - prompts/campaign-image-director-exclusive.md (novo)
    - openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/specs/art-director-contextual-briefing/spec.md (Requisitos editoriais: 4 prompts legíveis com seções editoriais; blocos nomeados; aviso/texto obrigatório em seção própria; hierarquia 1+N)
  </read_first>
  <action>
    Reescrever as âncoras de `prompt-reframe.test.ts` para a nova estrutura dos 4 `.md`, mantendo a leitura dos 4 arquivos do disco (`readPrompt`/`PROMPTS`):

    1. Substituir LINHA_AVISO_SEPARADO/LINHA_MANTIDA/LINHA_TABELA_AVISO/LINHA_VALIDADE (:17-22) por novas âncoras que refletem a estrutura-alvo:
       - presença das seções editoriais fixas (papel/persona, composição, instruções obrigatórias, fidelidade/criatividade) nos 4 arquivos — âncoras de texto editorial mantido (ex.: frases de anti-invenção e de liberdade criativa);
       - presença dos 8 placeholders de bloco nomeados (`{{campaignFactsSection}}` etc.) nos 4 arquivos;
       - AUSÊNCIA de template seco: cada arquivo tem prosa editorial além dos placeholders (assert de tamanho/prosa — ex.: mais de N linhas de texto não-placeholder); ausência de micro-tabela "Informações da Campanha"/linhas `| **Aviso ilustrativo` e `| **Validade**` na tabela;
       - aviso e texto obrigatório em seção própria: ausência das frases incondicionais antigas da cauda ("Quando houver aviso ilustrativo, exiba ..." e "Se o campo 'Texto obrigatório na arte' estiver preenchido") como linha solta dos 4 `.md` — elas agora vivem DENTRO dos blocos montados (no código), não nos templates;
       - hierarquia 1+N: presença da orientação de hierarquia primary × auxiliares (na seção editorial de produto/referências dos 4 arquivos, quando aplicável) e ausência da frase antiga "referência visual fiel" se removida.
    2. Manter cobertura dos 4 arquivos (loop PROMPTS) onde aplicável; onde a âncora é por intent (ex.: exclusive sem preço; spotlight sem validade), assertar por arquivo.
    3. Documentar no SUMMARY: quais âncoras antigas saíram e as novas criadas.
  </action>
  <verify>
    <automated>npx vitest run src/lib/campaign/__tests__/prompt-reframe.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - prompt-reframe.test.ts verde com as novas âncoras nos 4 `.md`
    - Asserts de presença editorial + presença dos 8 slots + ausência de template seco/micro-tabela/frases incondicionais antigas
    - Nenhuma âncora antiga (LINHA_TABELA_AVISO etc.) remanescente
  </acceptance_criteria>
  <done>prompt-reframe.test.ts atualizado com as novas âncoras editoriais e verde</done>
</task>

<task type="auto">
  <name>Task 4: Ampliar art-director-briefing.test.ts com testes por bloco presente/ausente + deduplicação + saneamento</name>
  <files>src/lib/image-generation/services/__tests__/art-director-briefing.test.ts</files>
  <read_first>
    - src/lib/image-generation/services/__tests__/art-director-briefing.test.ts (base do 45-02)
    - src/lib/image-generation/services/art-director-briefing.ts (funções de bloco do 45-03/45-04)
    - openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/specs/art-director-contextual-briefing/spec.md (Requisitos: prompt contextual sem vazios; texto obrigatório em seção própria; aviso em seção própria; teste por bloco presente/ausente :154-158)
  </read_first>
  <action>
    Ampliar `art-director-briefing.test.ts` com os testes de bloco presente/ausente (F45-21):

    1. **Brief mínimo** (sem merchantText, sem aviso, sem validade, sem details/availability/sensitiveConstraints, 1 imagem primary): assertar que os blocos condicionais retornam `""` (`commercialDetailsSection`, `requiredArtworkTextSection`, `illustrativeNoticeSection`, `constraintsSection`) e que montar o prompt final NÃO produz seção vazia/heading órfão/linha de tabela em branco/placeholder residual.
    2. **Brief completo** (texto obrigatório + aviso + validade offer + details + availability + restrições + 1 primary + 2 auxiliares): assertar que cada natureza opcional/sensível aparece em **UMA ocorrência** no prompt final: validade → só `campaignFactsSection`; details/availability → só `commercialDetailsSection`; aviso → só `illustrativeNoticeSection`; texto obrigatório → só `requiredArtworkTextSection`; restrições → só `constraintsSection`. Produto/loja podem aparecer legitimamente em múltiplos contextos (não configuram duplicação proibida).
    3. **Saneamento (D6):** texto do lojista com `{{exemplo}}` e `}}` → bloco `requiredArtworkTextSection`/`commercialDetailsSection` sem `{{`/`}}` residual; montar prompt final e assertar zero `{{...}}` não resolvido (via regex).
    4. **Por intent:** spotlight sem validade; exclusive sem preço (facts); preserveImageDirective apenas não-offer + preserveImageContext.
    5. Montagem determinística: mesmo input 2× → mesmo texto (reforço do invariant no nível do módulo).
    NÃO adicionar comentários no código de teste.
  </action>
  <verify>
    <automated>npx vitest run src/lib/image-generation/services/__tests__/art-director-briefing.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Testes por bloco presente/ausente (brief mínimo e completo) verdes
    - Teste de UMA ocorrência por natureza (deduplicação) verde
    - Teste de saneamento `{{`→`{` sem placeholder residual verde
    - Testes por intent (spotlight/exclusive) verdes; determinismo coberto
  </acceptance_criteria>
  <done>art-director-briefing.test.ts ampliado (presente/ausente, deduplicação, saneamento, determinismo) e verde</done>
</task>

<task type="auto">
  <name>Task 5: Gates + regressão das suites irmãs SEM co-migração (D7)</name>
  <files>src/lib/image-generation/services/__tests__/image-generation-service.test.ts, src/lib/campaign/__tests__/prompt-reframe.test.ts, src/lib/image-generation/services/__tests__/art-director-briefing.test.ts</files>
  <read_first>
    - openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/specs/art-director-contextual-briefing/spec.md (Requirement: Camada externa e revisor/copy director permanecem inalterados)
    - openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/tasks.md (§5.5)
  </read_first>
  <action>
    1. Rodar `npx vitest run` completo — deve estar VERDE agora (co-migração do 45-05 resolveu as pendências registradas no 45-03/45-04).
    2. Confirmar que as suites irmãs NÃO foram editadas e seguem verdes: `image-review-service.test.ts`, `copy-director-service.test.ts`, `copy-director-prompt.test.ts`, `use-campaign-form-*.test.ts`, `route.test.ts` (generate-image), `brief.test.ts`/`brief-snapshot.test.ts`/`brief-mapper.test.ts` (regressão de não-mudança — D7).
    3. Rodar `npm run typecheck`, `npm run lint`, `npm run build` — zero erros.
    4. Registrar no SUMMARY: contagem de testes totais, suites irmãs confirmadas sem edição, e prova de que nenhum arquivo congelado (revisor/copy/schema/rota/form helpers) foi alterado (git status).
  </action>
  <verify>
    <automated>npx vitest run; if ($?) { npm run typecheck; if ($?) { npm run lint; if ($?) { npm run build } } }</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` completo verde (sem pendências das fases anteriores)
    - Suites irmãs verdes e NÃO editadas (git status/diff sem toque em image-review-service.test.ts, copy, form, route)
    - typecheck/lint/build zero erros
  </acceptance_criteria>
  <done>Gates verdes + suites irmãs sem co-migração (regressão de não-mudança D7) confirmadas</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teste ↔ contrato interno | Co-migração de testes que ancoram o mapa de variáveis — risco de perder cobertura real ao relaxar asserções |
| teste ↔ superfícies congeladas | Edição acidental de suites irmãs (revisor/copy/form/rota) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-05 | Tampering | image-generation-service.test.ts | mitigate | Golden → invariantes fortes (placeholders ⊆ chaves; determinismo; presente/ausente; contrato externo intocado) — não é relaxamento, é mudança de critério (D5) |
| T-45-05a | Tampering | prompt-reframe.test.ts | mitigate | Novas âncoras editoriais (presença de seções, ausência de template seco/micro-tabela/frases antigas) cobrem a estrutura-alvo (D2) |
| T-45-05b | Tampering | art-director-briefing.test.ts | mitigate | Testes presente/ausente + UMA ocorrência por natureza + saneamento fixam D3/D4/D6 no nível do módulo |
| T-45-05c | Tampering | Suites irmãs | mitigate | Proibida edição de image-review-service.test.ts/copy/form/route — verificação de não-mudança via git status (D7) |
| T-45-05d | Information Disclosure | Textos de teste | accept | Fixtures usam dados sintéticos (createMinimalBrief) — sem PII |
| T-45-SC | Tampering | npm installs | accept | Nenhum pacote novo — sem gate de legitimidade |
</threat_model>

<verification>
- Golden 39 keys → invariantes (placeholders ⊆ chaves, determinismo, presente/ausente por bloco, contrato externo) — image-generation-service.test.ts verde
- validatePrompts (novos slots; kqo a/b/c; com/sem texto obrigatório e aviso) verde
- prompt-reframe.test.ts com novas âncoras editoriais verde
- art-director-briefing.test.ts com presente/ausente + UMA ocorrência por natureza + saneamento verde
- `npx vitest run` completo verde; suites irmãs verdes sem edição; typecheck/lint/build zero erros
</verification>

<success_criteria>
- Golden → invariantes co-migrado (D5); validatePrompts e kqo atualizados (D4/D5)
- prompt-reframe com novas âncoras (D2/D3)
- Testes por bloco presente/ausente + deduplicação + saneamento criados (F45-21)
- Regressão completa verde com suites irmãs sem co-migração (D7)
</success_criteria>

<output>
Create `.planning/phases/45-briefing-contextual-do-diretor-de-arte/45-05-SUMMARY.md` when done
</output>

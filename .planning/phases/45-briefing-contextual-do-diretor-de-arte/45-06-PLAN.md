---
phase: 45-briefing-contextual-do-diretor-de-arte
plan: 06
type: execute
wave: 4
depends_on: [45-05]
files_modified:
  - src/lib/image-generation/services/__tests__/image-generation-service.test.ts
  - src/lib/campaign/__tests__/prompt-reframe.test.ts
  - src/lib/image-generation/services/__tests__/art-director-briefing.test.ts
autonomous: false
requirements: [F45-23, F45-24, F45-25]

must_haves:
  truths:
    - "Regressão completa (vitest total) verde — resíduos de fixtures/asserções que referenciem o mapa antigo de variáveis ou âncoras antigas dos `.md` corrigidos"
    - "typecheck/lint/build verdes; rota HTTP/schema/snapshot/domínio/form/revisor/copy/fallback OpenAI continuam intactos (suites de não-mudança)"
    - "Revisão humana dos 4 `.md` reescritos aprovada (legibilidade editorial + slots com intenção clara) e texto final montado conferido em casos representativos"
  artifacts:
    - path: "prompts/campaign-image-director.md"
      provides: "4 .md reescritos aprovados em revisão humana"
      contains: "campaignFactsSection"
    - path: "prompts/campaign-image-director-exclusive.md"
      provides: "Exclusive reescrito sem preço, tom premium"
      contains: "campaignFactsSection"
  key_links:
    - from: "4 prompts (prompts/campaign-image-director*.md)"
      to: "buildPromptVariables/blocos (art-director-briefing.ts)"
      via: "Revisão humana confirma que slots e texto editorial formam um briefing coerente"
      pattern: "campaignFactsSection"
---

<objective>
Executar a **regressão completa** da F45 (tasks.md §6): vitest total verde com correção de resíduos de fixtures/asserções que referenciem o mapa antigo de variáveis ou âncoras antigas dos `.md` (6.1); typecheck/lint/build verdes + verificação de que rota HTTP/schema/snapshot/domínio/form/revisor/copy/fallback OpenAI continuam intactos (6.2 — D7); **revisão humana** dos 4 `.md` reescritos (legibilidade editorial + slots com intenção clara) e do texto final montado em casos representativos (6.3).

Purpose: D2-D7 — fechar a implementação com regressão total e confirmar que a superfície externa não mudou; garantir revisão humana da reescrita editorial antes da verificação final.
Output: vitest total verde + typecheck/lint/build verdes + checkpoint humano de revisão dos 4 `.md`.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-briefing-contextual-do-diretor-de-arte/45-CONTEXT.md (D1-D7; F45-23..F45-25 :89-91)
@openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/tasks.md (§6 — tasks 6.1-6.3)
@openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/specs/art-director-contextual-briefing/spec.md (Requirement: Camada externa inalterada; Requirement: estrutura editorial legível)
@prompts/campaign-image-director.md, prompts/campaign-image-director-offer.md, prompts/campaign-image-director-spotlight.md, prompts/campaign-image-director-exclusive.md (reescritos no 45-03/45-04)

<interfaces>
Suites de não-mudança a verificar (D7 — NENHUMA delas pode ter sido alterada pela F45):
- Rota HTTP: `src/app/api/campaign/generate-image/__tests__/route.test.ts`
- Schema público: `GenerateImageRequestSchema` (src/lib/image-generation/schema.ts) — testes de schema
- Snapshot/domínio: `brief.test.ts`/`brief-snapshot.test.ts`/`brief-mapper.test.ts` (`CampaignBrief`/`campaign_brief_v1`)
- Form: `use-campaign-form-*.test.ts` (helpers `buildMandatoryArtworkText`/`buildValidityDisplayText`/`buildCampaignGenerationBody` intactos)
- Revisor: `image-review-service.test.ts` + `prompts/campaign-image-reviewer.md`
- Copy Director: `copy-director-service.test.ts`/`copy-director-prompt.test.ts` + `prompts/campaign-copy-director*.md`
- Fallback OpenAI: `providers/openai.ts` (quick 260902-mqj) — sem mudança
Caminhos de regressão a conferir manualmente como amostra (6.3): 1 offer completo (validade + texto obrigatório + aviso), 1 spotlight, 1 exclusive — montar o texto final e ler em voz alta como briefing de direção de arte.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regressão completa (vitest total) + correção de resíduos (6.1)</name>
  <files>src/lib/image-generation/services/__tests__/image-generation-service.test.ts, src/lib/campaign/__tests__/prompt-reframe.test.ts, src/lib/image-generation/services/__tests__/art-director-briefing.test.ts</files>
  <read_first>
    - openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/tasks.md (§6.1)
    - src/lib/image-generation/services/__tests__/image-generation-service.test.ts (possíveis resíduos do mapa antigo)
    - src/lib/campaign/__tests__/prompt-reframe.test.ts (possíveis resíduos de âncoras antigas)
  </read_first>
  <action>
    1. Rodar `npx vitest run` completo. Se houver falhas, inspecionar cada uma: resíduo que referencie o **mapa antigo de variáveis** (chaves removidas como `commercialFrame`/`hasCategoryConflict`/`brandColorsChosen`/`visualStyle`/`campaignGuidelines`/`campaignBrief` em fixtures/asserções) ou **âncoras antigas dos `.md`** (frases incondicionais da cauda, micro-tabela, LINHA_VALIDADE fora de fatos).
    2. Corrigir APENAS os resíduos de co-migração (asserção/fixture que aponta para o estado pré-F45) — nunca enfraquecer asserção de comportamento novo. Fixtures de outras suites só se provadamente referenciarem o mapa antigo do diretor (nesse caso, registrar no SUMMARY qual fixture e por quê).
    3. Garantir que a contagem final de testes é registrada no SUMMARY (total de testes verdes).
    4. NÃO alterar suites irmãs de revisor/copy/form/rota (se falharem, investigar regressão real — não co-migrar).
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` completo verde (zero falhas)
    - Resíduos de mapa antigo/âncoras antigas corrigidos apenas onde existiam; SUMMARY registra os arquivos corrigidos
    - Suites irmãs (revisor/copy/form/rota) intactas e verdes
  </acceptance_criteria>
  <done>Regressão completa verde — zero resíduos do estado pré-F45</done>
</task>

<task type="auto">
  <name>Task 2: typecheck/lint/build + verificação de não-mudança do contrato externo (6.2)</name>
  <files>src/lib/image-generation/services/__tests__/image-generation-service.test.ts, src/lib/campaign/__tests__/prompt-reframe.test.ts</files>
  <read_first>
    - openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/tasks.md (§6.2)
    - openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/specs/art-director-contextual-briefing/spec.md (Requirement: Camada externa e revisor/copy director permanecem inalterados)
  </read_first>
  <action>
    1. Rodar `npm run typecheck`, `npm run lint`, `npm run build` — zero erros.
    2. Verificar não-mudança do contrato externo: `git status`/`git diff --name-only` (ou comparar com o registro do 45-01-SUMMARY) para confirmar que NENHUM destes foi alterado pela F45: rota HTTP (`generate-image`), `schema.ts` (GenerateImageRequestSchema), `brief.ts`/domínio/snapshot, `use-campaign-form.ts` (helpers), `image-review-service.ts` + `campaign-image-reviewer.md`, Copy Director (`src/lib/copy/*` + `prompts/campaign-copy-director*.md`), `providers/openai.ts`.
    3. Registrar no SUMMARY a lista de superfícies confirmadas intactas (com evidência de git).
    4. Se QUALQUER arquivo congelado aparecer como alterado, investigar e reverter (fora de escopo — D7).
  </action>
  <verify>
    <automated>npm run typecheck; if ($?) { npm run lint; if ($?) { npm run build } }</automated>
  </verify>
  <acceptance_criteria>
    - typecheck/lint/build zero erros
    - Nenhum arquivo congelado alterado (rota/schema/domínio/form/revisor/copy/fallback) — verificação git registrada no SUMMARY
    - Suites de rota/schema/snapshot/domínio/form/revisor/copy verdes (regressão de não-mudança)
  </acceptance_criteria>
  <done>Gates verdes + superfícies congeladas confirmadas intactas (D7)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Revisão humana dos 4 .md reescritos e do texto final montado (6.3)</name>
  <files>prompts/campaign-image-director.md, prompts/campaign-image-director-offer.md, prompts/campaign-image-director-spotlight.md, prompts/campaign-image-director-exclusive.md</files>
  <read_first>
    - prompts/campaign-image-director.md (novo)
    - prompts/campaign-image-director-offer.md (novo)
    - prompts/campaign-image-director-spotlight.md (novo)
    - prompts/campaign-image-director-exclusive.md (novo)
    - openspec/changes/fase-45-briefing-contextual-do-diretor-de-arte/specs/art-director-contextual-briefing/spec.md (Requisitos editoriais e de cenário)
  </read_first>
  <action>
    Preparar para revisão humana (o agente monta e apresenta; o humano valida):
    1. Abrir os 4 `.md` reescritos e conferir legibilidade editorial: papel/persona por intent, composição, instruções obrigatórias, fidelidade, liberdade criativa — texto humano claro e revisável, sem virar "template seco" de placeholders.
    2. Conferir que os slots de bloco têm intenção clara pelo nome e posição no texto (`campaignFactsSection`, `commercialDetailsSection`, `requiredArtworkTextSection`, `illustrativeNoticeSection`, `identityReferenceSection`, `productReferenceSection`, `constraintsSection`, `creativeDirectionSection`).
    3. Montar o **texto final** de casos representativos (usando o service/blocos): 1 offer completo (validade + texto obrigatório + aviso + details), 1 spotlight (preço único, sem validade), 1 exclusive (sem preço) — e apresentar os textos montados como anexo para leitura.
    4. Checklist de revisão (para o humano): (a) o prompt final é um briefing de direção de arte coerente e sem ruído? (b) cada natureza está na seção certa e sem duplicação? (c) o tom por intent foi preservado (offer promocional, spotlight sem urgência, exclusive premium sem preço)? (d) as seções próprias do texto obrigatório e do aviso estão claras? (e) identidade/preservação e hierarquia primary×auxiliares estão explícitas?
  </action>
  <verify>
    <human-check>
      1. Abra os 4 arquivos em prompts/campaign-image-director*.md e leia a camada editorial — está legível como documento de direção criativa (não um template seco)?
      2. Verifique o texto final montado dos 3 casos (offer/spotlight/exclusive) anexado no SUMMARY/checkpoint — sem seções vazias, sem duplicação, sem placeholders residuais?
      3. Confirme o tom por intent (offer promocional; spotlight destaque sem urgência; exclusive premium sem preço).
      4. Responda "approved" ou descreva os ajustes necessários (ex.: trecho editorial confuso, slot mal posicionado, tom descaracterizado).
    </human-check>
  </verify>
  <acceptance_criteria>
    - Humano aprova legibilidade dos 4 `.md` (ou fornece ajustes pontuais que o agente aplica e reapresenta)
    - Humano aprova o texto final montado nos 3 casos representativos (sem ruído/duplicação/tom descaracterizado)
  </acceptance_criteria>
  <done>Revisão humana dos 4 .md e dos textos montados concluída (aprovada ou ajustada)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| regressão ↔ contrato interno novo | Corrigir resíduos sem enfraquecer comportamento novo |
| humano ↔ .md reescritos | Reescrita editorial com mão leve — risco de perda de orientação/qualidade |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-06 | Tampering | Regressão (resíduos de co-migração) | mitigate | Correção apenas de resíduos do mapa antigo/âncoras antigas; proibido enfraquecer asserção de comportamento novo (vitest total verde) |
| T-45-06a | Tampering | Superfícies congeladas | mitigate | Verificação git de não-mudança (rota/schema/domínio/form/revisor/copy/fallback); suites irmãs verdes sem edição (D7) |
| T-45-06b | Tampering | Reescrita editorial (mão leve) | mitigate | Revisão humana checkpoint dos 4 `.md` + textos montados (6.3) — legibilidade, slots, tom por intent |
| T-45-SC | Tampering | npm installs | accept | Nenhum pacote novo — sem gate de legitimidade |
</threat_model>

<verification>
- `npx vitest run` completo verde (regressão total)
- `npm run typecheck`/`npm run lint`/`npm run build` zero erros
- Verificação de não-mudança git: rota/schema/domínio/form/revisor/copy/fallback intactos
- Checkpoint humano de revisão dos 4 `.md` aprovado
</verification>

<success_criteria>
- Regressão completa verde; zero resíduos do mapa antigo/âncoras antigas
- Superfícies congeladas confirmadas intactas (D7)
- Revisão humana dos 4 `.md` reescritos e dos textos montados aprovada (D2/D3)
</success_criteria>

<output>
Create `.planning/phases/45-briefing-contextual-do-diretor-de-arte/45-06-SUMMARY.md` when done
</output>

---
phase: 45-briefing-contextual-do-diretor-de-arte
plan: 08
type: execute
wave: 5
depends_on: [45-06]
files_modified:
  - prompts/campaign-image-reviewer.md
  - src/lib/image-generation/services/image-review-service.ts
  - src/lib/image-generation/services/image-generation-service.ts
  - src/lib/image-generation/services/art-director-briefing.ts
  - src/lib/image-generation/services/__tests__/image-review-service.test.ts
  - src/lib/image-generation/services/__tests__/image-generation-service.test.ts
  - src/lib/image-generation/services/__tests__/art-director-briefing.test.ts
  - src/lib/campaign/__tests__/prompt-reframe.test.ts
autonomous: false
requirements: [F45-REVIEWER-ALIGN]

must_haves:
  truths:
    - "Revisor recebe requiredArtworkText e illustrativeNotice em seções independentes (splitDirectorLegalText canônico reutilizado) — legalNoticeText concatenado removido do contrato interno (ImageReviewInput)"
    - "Revisor recebe sensitiveConstraints (seção própria) e objective (contexto não-bloqueante); builders montam APENAS heading + valor informado + identificação da natureza — políticas de severidade/tolerância vivem em campaign-image-reviewer.md"
    - "Autoridade estreita explícita e legível no .md do Revisor; identidade visual NÃO é avaliada de forma alguma (Revisor só confere nome correto da loja quando exigido; nenhuma menção a corte/posição/fidelidade de identidade no prompt)"
    - "Texto obrigatório e aviso verificados separadamente: presença + legibilidade; reprovação só por ausência, ilegibilidade ou alteração inequívoca que mude o conteúdo; aviso não é tratado como parte de outro texto legal"
    - "Offer: expectedCommercialTone sem 'CTA de compra esperado' e sem 'senso de urgência' ('Tom comercial e promocional coerente com uma campanha de oferta'); urgência avaliada só quando vier de badge/validade/fato explícito; CTA não exigido"
    - "availabilityNotes retirado da montagem ATIVA do Diretor (commercialDetailsSection); schema/domínio/snapshot e builder legado intocados; availabilityNotes não chega ao Diretor ativo nem ao Revisor (teste prova)"
    - "Identidade no Diretor: instrução canônica única e concisa (área segura, margem nas quatro bordas, posição secundária, fidelidade/anti-invenção) — sem repetição entre .md e bloco; 4 intents recebem a orientação preventiva"
    - "Comportamento de badge do Revisor e regras de preço por intent NÃO mudam; superfícies externas (UI/form/schema público/domínio/snapshot/copy/providers) intocadas"
  artifacts:
    - path: "prompts/campaign-image-reviewer.md"
      provides: "Revisor com autoridade estreita e políticas de severidade/tolerância legíveis"
      contains: "O que o Revisor bloqueia"
    - path: "src/lib/image-generation/services/image-review-service.ts"
      provides: "Input e builders splitados (requiredArtworkText/illustrativeNotice/sensitiveConstraints/objective)"
      contains: "requiredArtworkText"
  key_links:
    - from: "splitDirectorLegalText (art-director-briefing.ts)"
      to: "reviewInput (image-generation-service.ts)"
      via: "mesmo split canônico alimenta Diretor e Revisor"
      pattern: "splitDirectorLegalText"
---

<objective>
Corrigir o alinhamento Diretor de Arte × Revisor de Imagem descoberto durante o UAT do 45-07. Princípio central: o Diretor recebe instruções claras, diretas e preventivas sobre o que PODE/DEVE fazer; o Revisor recebe o contexto necessário mas tem autoridade estreita — bloqueia só erros críticos, objetivos e claramente visíveis; preferências estéticas, posicionamento e pequenas imperfeições passam; o objetivo é evitar regenerações desnecessárias e reduzir custo.

REGISTRO (decisões do humano em 2026-09-05): executar como plano 45-08 dentro da F45, ANTES do fechamento (o defeito foi descoberto no UAT da própria fase e corrige a premissa errada 'revisor congelado'). O UAT do 45-07 NÃO será aprovado/fechado antes do 45-08; após o 45-08 (com aprovação humana da ENTREGA) o checkpoint UAT será retomado com casos corrigidos.

Output: reviewer/director realinhados com contrato claro; políticas de julgamento no .md; 4 gates verdes; ENTREGA (prompts montados) submetida à revisão humana antes de qualquer arquivamento/encerramento.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
@C:/Users/wagne/.config/opencode/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/phases/45-briefing-contextual-do-diretor-de-arte/45-CONTEXT.md
@.planning/phases/45-briefing-contextual-do-diretor-de-arte/45-06-SUMMARY.md
@.planning/phases/45-briefing-contextual-do-diretor-de-arte/45-07-SUMMARY.md
@prompts/campaign-image-reviewer.md (estado atual, 140 linhas)
@prompts/campaign-image-director*.md (4 arquivos pós-F45-06b)
@src/lib/image-generation/services/image-review-service.ts (ImageReviewInput :9-25; buildReviewPromptVariables :36-53; buildMandatoryArtworkTextSection :190-212; buildExpectedCommercialTone :155-161; buildExpectedBadgeBehavior :130-141; parseResult/determineFailureType :348-371)
@src/lib/image-generation/services/image-generation-service.ts (reviewInput inline :405-426; validatePrompts espelho :632-652; review() call :434; mediaImagesDataUrls :783-789)
@src/lib/image-generation/services/art-director-briefing.ts (splitDirectorLegalText :26-37; requiredArtworkTextSection :357-376; illustrativeNoticeSection :378-388; identityReferenceSection :390-443; commercialDetailsSection :333-355 + buildAvailabilityLine :258-274; constraintsSection :490-500; campaignFactsSection :276-331)
@src/lib/campaign/brief.ts (domínio: legalNotice/commercial — NÃO alterar)
</context>

<interfaces>
Estado real verificado 2026-09-05:
- `ImageReviewInput` (image-review-service.ts:9-25) tem `legalNoticeText?` — recebe o texto INTEGRAL concatenado (buildMandatoryArtworkText do form: `ILLUSTRATIVE_NOTICE_TEXT\nfree`). Não tem sensitiveConstraints/objective/requiredArtworkText/illustrativeNotice.
- `buildReviewPromptVariables` (:36-53) → 13 keys; `mandatoryArtworkTextSection` = texto integral num bloco só.
- reviewInput inline (image-generation-service.ts:405-426) e espelho validatePrompts (:632-652) mapeiam `legalNoticeText: brief.commercial.legalNotice?.enabled ? .text : undefined`. splitDirectorLegalText NÃO é usado aqui.
- Revisor não recebe imagem de identidade (só produto gerada + referências de produto). Diretor usa identityReferenceSection textual (fidelidade/respiro/bordas já pós-06b).
- objective/sensitiveConstraints/campaignDetails/additionalDetails/hook/cta/availabilityNotes não são enviados pelo form web (dormentes), mas objective e sensitiveConstraints estão no domínio e são usados no Diretor (campaignFactsSection **Objetivo:** :307; constraintsSection `## Restrições Sensíveis` :490-500). availabilityNotes: usado no Diretor ativo APENAS em commercialDetailsSection (linha Disponibilidade via buildAvailabilityLine).
- expectedCommercialTone offer (:157): "Tom promocional com senso de urgência. CTA de compra esperado."
- O `.md` do Revisor não referencia objective/sensitiveConstraints/identity; só os 13 placeholders.
- Badge (buildExpectedBadgeBehavior :130-141): offer → obrigatório exato; spotlight/exclusive → informado opcional sem promessa indevida. NÃO ALTERAR.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Contrato legal splitado no Revisor (requiredArtworkText + illustrativeNotice)</name>
  <files>src/lib/image-generation/services/image-review-service.ts, src/lib/image-generation/services/image-generation-service.ts</files>
  <read_first>
    - src/lib/image-generation/services/image-review-service.ts (:9-25 tipo; :36-53 vars; :190-212 builder)
    - src/lib/image-generation/services/image-generation-service.ts (:405-426; :632-652)
    - src/lib/image-generation/services/art-director-briefing.ts (:26-37 splitDirectorLegalText)
  </read_first>
  <action>
    1. `ImageReviewInput`: substituir `legalNoticeText?: string` por `requiredArtworkText?: string` e `illustrativeNotice?: string` (tipos internos estritos).
    2. Em `image-review-service.ts`, converter o builder único em DOIS builders finos que montam apenas heading + valor informado + identificação da natureza do campo (SEM regras de julgamento):
       - `requiredArtworkTextSection` → heading `## Texto Obrigatório na Arte`, identifica que é o texto informado pelo lojista como referência obrigatória, embrulha o valor sanitizado em aspas.
       - `illustrativeNoticeSection` → heading `## Aviso Ilustrativo`, identifica o aviso ilustrativo fixo habilitado, embrulha o valor. NÃO conter regra de posição/lateral nem tratar o aviso como parte de outro texto legal.
       - Sanear `{{`→`{`, `}}`→`}` nos dois (sanitizePromptText existente).
       - Remover do mapa de variáveis a chave `mandatoryArtworkTextSection`; inserir `requiredArtworkTextSection` e `illustrativeNoticeSection`.
    3. Nos DOIS call sites de `image-generation-service.ts` (reviewInput real :405-426 e espelho validatePrompts :632-652): aplicar `splitDirectorLegalText` (importada de `./art-director-briefing`) sobre `brief.commercial.legalNotice.text` quando `legalNotice.enabled`, alimentando `requiredArtworkText: merchantText` e `illustrativeNotice` (vazia/omitida quando sem aviso). Quando `!enabled`, ambos undefined.
    4. NÃO alterar preço por intent nem badge (buildExpectedPriceBehavior/buildExpectedBadgeBehavior intactos).
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\lib\image-generation\services\image-review-service.ts" -Pattern "requiredArtworkText|illustrativeNotice" | Measure-Object | Select-Object -ExpandProperty Count; Select-String -LiteralPath "src\lib\image-generation\services\image-review-service.ts" -Pattern "legalNoticeText" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <acceptance_criteria>
    - `legalNoticeText` removido do ImageReviewInput e do mapa de variáveis (grep = 0 no service)
    - Dois builders finos sem política de julgamento embutida
    - Call sites usam o mesmo split canônico do Diretor
    - Badge/price-intent behavior intactos
  </acceptance_criteria>
  <done>Contrato legal splitado: Revisor recebe as duas naturezas em seções independentes</done>
</task>

<task type="auto">
  <name>Task 2: sensitiveConstraints (seção própria) + objective (contexto não-bloqueante) no Revisor</name>
  <files>src/lib/image-generation/services/image-review-service.ts, src/lib/image-generation/services/image-generation-service.ts, prompts/campaign-image-reviewer.md</files>
  <read_first>
    - src/lib/image-generation/services/image-review-service.ts (:36-53; builders de seção)
    - src/lib/campaign/brief.ts (campos objective/sensitiveConstraints no domínio — onde o Diretor lê: campaignFactsSection/constraintsSection)
  </read_first>
  <action>
    1. `ImageReviewInput`: adicionar `sensitiveConstraints?: string` e `objective?: string`.
    2. Builders finos em image-review-service.ts:
       - `sensitiveConstraintsSection` → heading `## Restrições Sensíveis` + valor (listado), identificando que toda restrição listada vale para a arte. Sem regra de julgamento.
       - `objectiveSection` → heading `## Objetivo da Campanha` + valor, identificando que é contexto explicativo das escolhas do Diretor (NÃO conteúdo obrigatório na arte). Sem regra de julgamento.
    3. Inserir as duas chaves no mapa de variáveis e os placeholders `{{sensitiveConstraintsSection}}`/`{{objectiveSection}}` no `.md` (posição: junto das demais seções contextuais, fora dos critérios bloqueantes).
    4. Call sites (real + validatePrompts): preencher objective/sensitiveConstraints a partir dos MESMOS campos que o Diretor usa (paridade de origem).
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\lib\image-generation\services\image-review-service.ts" -Pattern "sensitiveConstraintsSection|objectiveSection" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <acceptance_criteria>
    - Campos + seções chegam ao Revisor; `.md` expõe os dois placeholders
    - Políticas de julgamento sobre essas seções vivem no `.md` (Task 5), não nos builders
  </acceptance_criteria>
  <done>Revisor recebe restrições sensíveis e objetivo como contexto, com julgamento no .md</done>
</task>

<task type="auto">
  <name>Task 3: Detalhes autorizados, CTA/tom de offer, availabilityNotes neutralizado no Diretor ativo</name>
  <files>src/lib/image-generation/services/image-review-service.ts, src/lib/image-generation/services/art-director-briefing.ts</files>
  <read_first>
    - src/lib/image-generation/services/image-review-service.ts (:155-161 expectedCommercialTone)
    - src/lib/image-generation/services/art-director-briefing.ts (:333-355 commercialDetailsSection; :258-274 buildAvailabilityLine)
  </read_first>
  <action>
    1. `expectedCommercialTone` offer → substituir a frase atual ("Tom promocional com senso de urgência. CTA de compra esperado.") por: "Tom comercial e promocional coerente com uma campanha de oferta." (regra de urgência-derivada-de-fato explícito vai para o `.md` do Revisor — Task 5). Demais intents sem mudança salvo ajuste mínimo para não contradizer.
    2. Em `art-director-briefing.ts`: remover a linha de Disponibilidade/Availability (`buildAvailabilityLine`) da montagem ATIVA `commercialDetailsSection` (offer e demais intents ativos). O builder legado `buildCommercialRepertoire` permanece intocado (testes legados). Campo permanece no schema/domínio/snapshot (intocados).
    3. `campaignDetails`/`additionalDetails` seguem em `authorizedContextSection` como contexto autorizado (sem cobrança de presença). Ajustar wording apenas se o builder embutir julgamento (mover para o `.md`).
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\lib\image-generation\services\image-review-service.ts" -Pattern "CTA de compra esperado|senso de urgência" | Measure-Object | Select-Object -ExpandProperty Count; Select-String -LiteralPath "src\lib\image-generation\services\art-director-briefing.ts" -Pattern "buildAvailabilityLine" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <acceptance_criteria>
    - Offer tone sem CTA exigido e sem "senso de urgência" (frase nova presente)
    - availabilityNotes ausente da montagem ativa do Diretor (commercialDetailsSection) mas schema/domínio/builder legado intactos
  </acceptance_criteria>
  <done>CTA/tom de offer corrigidos e availabilityNotes neutralizado na montagem ativa</done>
</task>

<task type="auto">
  <name>Task 4: Instrução canônica única de identidade no Diretor (área segura + anti-invenção)</name>
  <files>src/lib/image-generation/services/art-director-briefing.ts, prompts/campaign-image-director*.md (somente se necessário, sem duplicar)</files>
  <read_first>
    - src/lib/image-generation/services/art-director-briefing.ts (:390-443 identityReferenceSection)
    - prompts/campaign-image-director-offer.md (linha de composição de identidade)
  </read_first>
  <action>
    1. Em `identityReferenceSection` (bloco canônico único), consolidar de forma curta e explícita por estado de identidade:
       - Com ativo (logo/assinatura): DEVE usar o ativo recebido; reproduzir com fidelidade; NÃO editar/redesenhar/distorcer/reinterpretar/completar/inventar elementos; manter integralmente dentro da área segura da arte com margem visível nas quatro bordas (nenhuma parte relevante encostando, ultrapassando ou cortada); posição livre, desde que legível, reconhecível e secundária ao conteúdo principal.
       - text_only: usar cores e linguagem como referência, sem inventar logotipo ou assinatura.
       - Preservar a sentença de respiro das bordas já existente (evitar redundância interna).
    2. Garantir UMA única seção canônica (sem repetição da orientação em outros blocos/.md). Manter no `.md` a linha de composição simples ("assinada pela loja — ver seção Identidade da Loja").
    3. NÃO adicionar nenhuma instrução de identidade no prompt do Revisor (a identidade visual fica fora da avaliação — Task 5).
  </action>
  <verify>
    <automated>Select-String -LiteralPath "src\lib\image-generation\services\art-director-briefing.ts" -Pattern "área segura|margem visível|secundário ao conteúdo" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <acceptance_criteria>
    - Bloco de identidade consolidado, conciso, com área segura + margem nas 4 bordas + posição secundária
    - Orientação presente para os 4 intents (bloco compartilhado) sem duplicação entre .md e bloco
  </acceptance_criteria>
  <done>Identidade do Diretor preventiva, canônica e única</done>
</task>

<task type="auto">
  <name>Task 5: Autoridade estreita e políticas legíveis no campaign-image-reviewer.md</name>
  <files>prompts/campaign-image-reviewer.md</files>
  <read_first>
    - prompts/campaign-image-reviewer.md (estado atual integral)
  </read_first>
  <action>
    Reescrever/consolidar o `.md` do Revisor em seções explícitas e legíveis por um humano (políticas TODAS aqui; os builders só montam dados):
    - "O que o Revisor recebe": lista dos dados/seções (incl. requiredArtworkTextSection, illustrativeNoticeSection, sensitiveConstraintsSection, objectiveSection, authorizedContextSection, validityTextSection) e nota explícita de que NÃO recebe imagem de identidade.
    - "O que verifica": presença e legibilidade dos textos aplicáveis; dados comerciais corretos; nome correto do produto/loja; produto reconhecível; restrições sensíveis (violação claramente visível); qualidade publicável.
    - "O que bloqueia (critical)": preço obrigatório ausente/incorreto; produto ou nome da loja incorretos; texto obrigatório/aviso aplicável ausente, ilegível, ou com alteração inequívoca que transforme o conteúdo em OUTRA informação; informação comercial relevante inventada; produto principal irreconhecível ou gravemente deformado; violação claramente visível de restrição sensível; qualidade que impeça publicação.
    - "O que deve passar (minor)" (sem mencionar identidade/corte/posição de identidade — proibido incentivar busca por defeito de identidade): pequenas imperfeições estéticas; assimetria leve; posicionamento diferente do esperado; fundo/elementos decorativos discutíveis porém publicáveis; diferenças visuais pequenas que não mudem produto ou informação; dúvidas sem evidência objetiva.
    - Texto obrigatório × aviso: verificar CADA campo separadamente (presença + legibilidade); NÃO exigir co-presença, ordem, proximidade, concatenação, pontuação ou distribuição visual entre eles; NÃO avaliar posição do aviso; aviso ilustrativo NÃO é parte de outro texto legal; reprovar só por ausência/ilegibilidade/alteração inequívoca.
    - objective: contexto explicativo apenas — ausência textual nunca reprova; sem critério novo de bloqueio.
    - CTA/hook: não exigidos; sem "CTA de compra esperado" em offer.
    - Urgência: avaliada apenas quando derivada de fato explícito (badge, validade, condição informada).
    - Identidade visual: fora da avaliação do Revisor (não recebe a imagem); confere somente se o NOME correto da loja está presente quando exigido. NENHUMA menção a cortes/posição/tamanho/fidelidade de identidade em qualquer seção.
    - Regras finais: se o problema não impede publicação com confiança → passa; na dúvida entre minor e critical → minor; minor não provoca regeneração; não reprovar por preferência estética; não criar relações entre campos não fornecidas no contrato; fundo orienta mas não bloqueia isoladamente quando produto/legibilidade/qualidade seguem publicáveis.
    - Manter o contrato de resposta JSON e a referência visual/formatos; badge e preço por intent permanecem como hoje.
  </action>
  <verify>
    <automated>Select-String -LiteralPath "prompts\campaign-image-reviewer.md" -Pattern "área segura|corte|borda|fidelidade|logotipo|assinatura" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <acceptance_criteria>
    - `.md` sem qualquer instrução de avaliação de identidade visual (zero menções a corte/borda/fidelidade/logo/assinatura como alvo de inspeção) — verificar: menções a "logo"/"assinatura" só podem existir para declarar que NÃO recebe/imagem e fora dos critérios
    - Seções O que recebe/verifica/bloqueia/tolera legíveis; minor não cita identidade
    - Texto obrigatório × aviso: separados, sem concatenação/posição; objective não-bloqueante; offer sem CTA; urgência derivada de fato
  </acceptance_criteria>
  <done>Políticas de severidade/tolerância e autoridade estreita legíveis no .md do Revisor</done>
</task>

<task type="auto">
  <name>Task 6: Ajustes finos de suporte em image-review-service.ts (apenas o necessário pós-T1/T2/T3/T5)</name>
  <files>src/lib/image-generation/services/image-review-service.ts</files>
  <read_first>
    - src/lib/image-generation/services/image-review-service.ts (integral)
    - prompts/campaign-image-reviewer.md (novo estado)
  </read_first>
  <action>
    1. Garantir que builders de seção removam frases de política/julgamento que ficaram no TS (devem residir no `.md`). Ex.: retirar dos builders expressões como "não reprove apenas", "CRITICA", "deve passar", deixando identificação neutra da natureza do campo. Manter formatação factual, saneamento e seleção por intent já existente.
    2. Ajustar qualquer texto de seção (validationContextSection/authorizedContextSection/validityTextSection/referenceImagesContextSection) que embuta política redundante com o `.md`, mantendo identificação factual.
    3. NÃO mexer em parseResult/determineFailureType/empty_review/retry (comportamento operacional preservado).
  </action>
  <verify>
    <automated>npx vitest run src/lib/image-generation/services/__tests__/image-review-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Builders finos (dados), políticas no `.md`; suite do revisor verde após co-migração (Task 7)
  </acceptance_criteria>
  <done>TS monta dados; políticas vivem no .md</done>
</task>

<task type="auto">
  <name>Task 7: Testes (prova a prova) + co-migração das suites afetadas</name>
  <files>src/lib/image-generation/services/__tests__/image-review-service.test.ts, src/lib/image-generation/services/__tests__/image-generation-service.test.ts, src/lib/image-generation/services/__tests__/art-director-briefing.test.ts, src/lib/campaign/__tests__/prompt-reframe.test.ts</files>
  <read_first>
    - src/lib/image-generation/services/__tests__/image-review-service.test.ts (estado atual)
    - src/lib/image-generation/services/__tests__/image-generation-service.test.ts (:249 validatePrompts; :479 reviewInput real)
    - src/lib/image-generation/services/__tests__/art-director-briefing.test.ts (:453 identity)
    - src/lib/campaign/__tests__/prompt-reframe.test.ts
  </read_first>
  <action>
    Co-migrar asserts que referenciam `legalNoticeText`/`mandatoryArtworkTextSection` concatenado para o novo contrato splitado. Em seguida ADICIONAR/ajustar testes comprovando:
    1. Aviso somente → Revisor recebe APENAS seção de aviso (sem seção de texto obrigatório).
    2. Texto obrigatório somente → recebe apenas a seção obrigatória.
    3. Ambos → duas seções independentes, sem concatenação.
    4. Separação visual entre os textos é explicitamente aceita (wording do `.md`/montagem; assert de conteúdo distinto nas duas seções).
    5. `sensitiveConstraints` chega ao Revisor (seção própria).
    6. `objective` chega como contexto não-bloqueante (seção presente; ausência de objective nunca gera reprovação — assert de montagem).
    7. Detalhes comerciais (campaignDetails/additionalDetails) seguem apenas como contexto autorizado.
    8. CTA não é exigido genericamente em offer (expectedCommercialTone offer sem "CTA de compra esperado"/"senso de urgência").
    9. Badge mantém EXATAMENTE o comportamento atual (buildExpectedBadgeBehavior intacto — asserts existentes seguem verdes).
    10. `availabilityNotes` não entra no Diretor ativo nem no Revisor (grep nos mapas de variáveis de ambos + fixture).
    11. Identidade não é enviada como imagem ao Revisor (teste explícito com contexto com logo/VS → referências do review = só produto + gerada).
    12. Os 4 intents recebem orientação preventiva de identidade com área segura/margem (diretor).
    13. Issues minor continuam aprovando (passed=true) — regressão.
    14. Prompt final do Revisor sem placeholders residuais e com contrato visível no `.md` (conjunto de placeholders ⊆ variáveis fornecidas; sem identidade no conjunto).
    15. `.md` do Revisor não contém instrução de avaliação de identidade (nenhuma menção a corte/borda/fidelidade/logo/assinatura como alvo) nem concatenação/posição dos dois textos.
  </action>
  <verify>
    <automated>npx vitest run; if ($?) { Write-Output "vitest verde" }</automated>
  </verify>
  <acceptance_criteria>
    - Suite do revisor, do service, do diretor e prompt-reframe verdes; as 15 provas cobertas por asserts
    - Badge e preço por intent sem mudança (asserts originais verdes)
  </acceptance_criteria>
  <done>Testes provam o novo contrato Diretor × Revisor sem regressão</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 8: Gates finais + ENTREGA para revisão humana</name>
  <files>prompts/campaign-image-reviewer.md, prompts/campaign-image-director*.md, src/lib/image-generation/services/*.ts</files>
  <read_first>
    - prompts/campaign-image-reviewer.md (final)
    - src/lib/image-generation/services/image-review-service.ts (final)
  </read_first>
  <action>
    1. Rodar os 4 gates: `npx vitest run`, `npm run typecheck`, `npm run lint`, `npm run build` — todos verdes. SEM chamadas reais de IA.
    2. Verificar por git que NENHUM arquivo fora do escopo mudou (UI/form, schema.ts/GenerateImageRequestSchema, brief.ts/brief-schema.ts/snapshot, copy/*, providers/openai.ts, prompts/campaign-copy-director*.md). Listar superfícies confirmadas intactas.
    3. Montar via caminho REAL (service + PromptLoader) os prompts finais do Revisor para 4 casos: ① somente aviso; ② somente texto obrigatório; ③ ambos (seções independentes, sem concatenação); ④ restrição sensível + objetivo. Apresentar os textos montados.
    4. Apresentar o contrato final Diretor × Revisor (o que o Diretor manda de cada natureza; o que o Revisor recebe/verifica/bloqueia/tolera) e o texto montado do Diretor para um caso oferta completa com identidade (prova da orientação de área segura).
    5. Parar no checkpoint: aguardar aprovação humana ANTES de qualquer arquivamento ou encerramento da fase.
  </action>
  <verify>
    <human-check>
      1. Os 4 prompts montados do Revisor estão corretos (somente aviso / somente obrigatório / ambos separados / sensível+objetivo)?
      2. O contrato Diretor × Revisor apresentado está coerente? Identidade fora da avaliação, texto obrigatório/aviso separados, offer sem CTA/urgência?
      3. As políticas (bloquear/tolerar) estão no `.md` e não nos builders? Nenhuma menção a identidade como alvo de inspeção?
      4. Nenhum arquivo fora do escopo foi alterado?
      5. Responda "approved" (autoriza retomar o UAT do 45-07 com casos corrigidos e posterior fechamento) ou descreva ajustes.
    </human-check>
  </verify>
  <acceptance_criteria>
    - 4 gates verdes; escopo respeitado por git
    - ENTREGA apresentada e aprovada pelo humano
  </acceptance_criteria>
  <done>ENTREGA revisada pelo humano; autorizado retomar fechamento da F45</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| contrato Diretor ↔ Revisor | Split canônico único alimenta ambos; Revisor sem imagem de identidade |
| política de julgamento ↔ builders | Políticas no `.md`; builders só montam dados (legibilidade dos artefatos) |
| escopo ↔ superfícies externas | Revisor é o alvo; UI/schema/domínio/copy/providers permanecem congelados |

## STRIDE
| Threat | Category | Component | Disposition | Mitigation |
|--------|----------|-----------|-------------|------------|
| T-45-08 | Tampering | Revisor reprovando demais (custo de regeneração) | mitigate | Autoridade estreita no `.md`; minor passa; dúvida → minor; testes de tolerância |
| T-45-08a | Tampering | Concatenar os dois textos no Revisor | mitigate | Dois campos/seções independentes + testes 1–4 |
| T-45-08b | Tampering | Revisor "caçando" defeito de identidade sem receber a imagem | mitigate | Identidade 100% fora da avaliação; nenhuma menção no `.md`; teste 15 |
| T-45-08c | Tampering | Política escondida em TS | mitigate | Task 6 esvazia builders; política legível no `.md` |
| T-45-08d | Info disclosure | Enviar identidade ao Revisor | mitigate | reviewInput nunca inclui identityImageUrl; teste 11 |
| T-45-SC | Tampering | npm installs | accept | Nenhum pacote novo |
</threat_model>

---
phase: 46-gateway-unico-de-ia-e-registry-de-modelos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-01-SUMMARY.md
  - ROADMAP.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/PROJECT.md
  - AGENTS.md
  - src/lib/ai/model-resolver.ts
  - src/lib/ai/model-registry.ts
  - src/lib/ai/__tests__/model-registry.test.ts
  - src/lib/visual-signature/types.ts
  - supabase/migrations/20260912000001_f46_generation_events_type.sql
autonomous: false
requirements: [F46-01, F46-02, F46-03, F46-04, F46-05]

must_haves:
  truths:
    - "As 11 capacidades resolvem `AiModelConfig` com `segment` e `primary` (`provider`/`model`/`protocol`) pelo registry, com defaults idênticos aos valores efetivos pré-F46"
    - "`campaign_copy.primary.protocol === 'chat-completions'` e `campaign_copy.fallback.protocol === 'gemini'` (default inicial — configuração, não regra)"
    - "`campaign_image` → `responses` e `campaign_image_edit` → `images` (protocolos distintos no mesmo segmento `image`)"
    - "`campaign_image_review` usa `gpt-4o` e `visual_signature_validation` usa `gpt-4o-mini` (visão com modelos distintos, sem colapsar)"
    - "O registry rejeita `primary` e `fallback` com o mesmo `provider`+`model`; rejeita modelo fora da allowlist e protocolo incompatível com a capacidade"
    - "`GenerationEventType` inclui `campaign_spec` e o CHECK `chk_generation_events_type` o aceita (migration aditiva/idempotente aplicada no remoto)"
    - "Zero resíduos de nomenclatura F46/F47/F44/Stripe no estado atual dos runbooks (F44 não consta como fase numerada)"
  artifacts:
    - path: "src/lib/ai/model-resolver.ts"
      provides: "Interface AiModelResolver (seam do Change B)"
      contains: "export interface AiModelResolver"
    - path: "src/lib/ai/model-registry.ts"
      provides: "Registry por capacidade + allowlist + validação de alvos"
      contains: "campaign_copy"
    - path: "src/lib/ai/__tests__/model-registry.test.ts"
      provides: "Testes unitários do registry"
      contains: "campaign_image_edit"
    - path: "src/lib/visual-signature/types.ts"
      provides: "GenerationEventType com o literal campaign_spec"
      contains: "campaign_spec"
    - path: "supabase/migrations/20260912000001_f46_generation_events_type.sql"
      provides: "Extensão idempotente do CHECK chk_generation_events_type"
      contains: "chk_generation_events_type"
    - path: ".planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-01-SUMMARY.md"
      provides: "Trackings + baseline/inventário das 11 capacidades e dos 7 furos"
      contains: "F46 = Gateway"
  key_links:
    - from: "src/lib/ai/model-registry.ts"
      to: "src/lib/ai/model-resolver.ts"
      via: "implementa AiModelResolver sobre AiModelConfig"
      pattern: "AiModelConfig"
    - from: "src/lib/visual-signature/types.ts"
      to: "supabase/migrations/20260912000001_f46_generation_events_type.sql"
      via: "literal campaign_spec no tipo TS e no CHECK"
      pattern: "campaign_spec"
    - from: "src/lib/ai/model-registry.ts"
      to: "src/lib/ai/gateway.ts (46-02)"
      via: "seam AiModelResolver consumido pelo gateway"
      pattern: "resolve\\(capability"
---

<objective>
Abrir a F46 sem alterar comportamento: (1.1) **grep-verificação de trackings** F46/F47/F44/Stripe nos 5 runbooks (registro D-trackings já aplicado no ciclo de planejamento — os runbooks em `files_modified` são **somente-se-resíduo**); (1.2) **inventário/baseline** das 11 capacidades (serviço → protocolo → adapter → default atual) e dos 7 furos/caminhos sem telemetria com evidência `file:line` + baseline de testes; (1.3/1.4) criar `src/lib/ai/model-resolver.ts` (interface `AiModelResolver`) e `src/lib/ai/model-registry.ts` (`AiModelConfig` com `protocol` no primary e no fallback; allowlist; validação `primary ≠ fallback`) com testes unitários; (1.5) **[BLOCKING]** migration idempotente/aditiva que estende o CHECK `chk_generation_events_type` + tipo TS `GenerationEventType` com `campaign_spec`, aplicada no remoto.

Purpose: D1/D1.1 (registry por capacidade + seam de resolução), D6 (literal `campaign_spec` antes de migrar o legado), D-trackings (nomenclatura consistente), D5 (baseline para co-migração behavior-preserving).
Output: `46-01-SUMMARY.md` (trackings + inventário + baselines) + `model-resolver.ts` + `model-registry.ts` + testes + migration/TS aplicados no remoto.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md (seção "### Phase 46: Gateway Único de IA e Registry de Modelos")
@AGENTS.md (bloco "## Phase 46 — Gateway Único de IA e Registry de Modelos")
@.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-CONTEXT.md (D1 :71-100, D1.1 :102-103, D5 :127-128, D6 :130-131, D-trackings :147-148; F46-01..F46-05 :229-234)
@openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/design.md (D1 :45-83, D1.1 :85-87, D6 :167-173, Migration Plan :229-236)
@openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/specs/ai-model-registry/spec.md (ADDED :5-130)
@openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/specs/ai-campaign-intelligence/spec.md (:39-53 — literal campaign_spec no enum)
@supabase/migrations/20260906000003_f37_2_generation_events_type.sql (padrão DROP/ADD + REVERT)
@src/lib/visual-signature/types.ts (:99-115 — GenerationEventType)
@.planning/phases/43-revisao-brief-pre-geracao/43-01-PLAN.md (precedente de plano de trackings)

<interfaces>
Contrato do registry (design D1 :49-64 — implementar exatamente):
```ts
type AiProtocol = "chat-completions" | "responses" | "images" | "gemini";
interface AiModelTarget { provider: string; model: string; protocol: AiProtocol; }
interface AiModelConfig {
  capability: string;
  segment: "text" | "vision" | "image";
  primary: AiModelTarget;
  fallback?: AiModelTarget;
}
```
Interface `AiModelResolver` (D1.1): `resolve(capability: string): AiModelConfig` + `listCapabilities(): string[]`.
Defaults a preservar (design :66-78):
- `campaign_copy` text openai gpt-4o chat-completions; fallback gemini gemini-3.1-flash-lite gemini
- `campaign_correction_analysis` text openai gpt-4o chat-completions
- `brand_profile_text` text openai gpt-4o chat-completions
- `campaign_spec` text openai gpt-4o-mini chat-completions
- `campaign_input_validation` vision openai gpt-4o chat-completions
- `campaign_image_review` vision openai gpt-4o chat-completions
- `brand_profile_vision` vision openai gpt-4o chat-completions
- `visual_signature_validation` vision openai gpt-4o-mini responses
- `campaign_image` image openai gpt-5.5 responses
- `campaign_image_edit` image openai gpt-image-2 images
- `visual_signature_image` image openai gpt-5.5 responses

CHECK atual (15 valores) a preservar + `campaign_spec` (16º):
`campaign_pipeline, campaign_copy, campaign_input_validation, campaign_image, campaign_image_review, visual_signature, visual_signature_image, visual_signature_validation, brand_profile_without_logo, brand_profile_with_logo, brand_profile_vision, brand_profile_text, theme_direction, theme_generation, campaign_correction_analysis`
`GenerationEventType` (13 valores em `src/lib/visual-signature/types.ts:102-115`) recebe `campaign_spec`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep-verificação de trackings F46/F47/F44/Stripe nos 5 runbooks (1.1)</name>
  <files>ROADMAP.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/PROJECT.md, AGENTS.md</files>
  <read_first>
    - .planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-CONTEXT.md (:147-148 — D-trackings; :54 — migration blocking)
    - .planning/STATE.md (:5 `current_phase: 46`; :20-40 — bloco Phase 46)
    - .planning/ROADMAP.md (seção "### Phase 46: Gateway Único de IA e Registry de Modelos" + "Phase numbering")
    - AGENTS.md (bloco "## Phase 46 — Gateway Único de IA e Registry de Modelos")
  </read_first>
  <action>
    Executar grep-verificação de consistência de nomenclatura F46/F47/F44/Stripe nos 5 runbooks (`ROADMAP.md` raiz, `.planning/ROADMAP.md`, `.planning/STATE.md`, `.planning/PROJECT.md`, `AGENTS.md`) usando padrões precisos de **resíduo de estado atual** — nunca coringas que gerem falso positivo em notas históricas de fases concluídas.

    Padrões de resíduo de ESTADO ATUAL a verificar (devem retornar ZERO nos marcadores de estado atual):
    - `F46 = Stripe`, `Phase 46 (Stripe`, `fase-46-stripe`, `\| 46.*Stripe`, `\| 44 \|`, `Phase 44 (`, `F44 = Temas` como fase numerada, `fase-44` numerada, `F47 = Gateway`, `fase-47-gateway`.
    - **EXCEÇÕES documentadas (notas históricas/normativas legítimas — NÃO editar):** menções a "F44 = Temas de Campanha permanece fora da numeração" e "Stripe/Monetização Pública fora da numeração (iniciativa diferida v1.7+)" são esperadas; narrativas históricas de fases concluídas F40–F43 são legítimas.

    Confirmar POSITIVAMENTE (assertando rótulo/valor):
    - `.planning/ROADMAP.md`: seção "### Phase 46: Gateway Único de IA e Registry de Modelos"; nota "Phase numbering" com "F46 = Gateway Único de IA e Registry de Modelos (v1.5)" e "F47 = Catálogo e Seleção de Modelos Admin (Change B)"; "F44 ... permanece fora da numeração".
    - `.planning/STATE.md`: frontmatter `current_phase: 46`; bloco "### Phase 46 — Gateway Único de IA e Registry de Modelos ◆ Em planejamento (0/9 plans / 5 waves)".
    - `AGENTS.md`: bloco "## Phase 46 — Gateway Único de IA e Registry de Modelos" com "9 plans previstos (46-01..46-09, 5 waves)".
    - `ROADMAP.md` raiz: linha da tabela de fases contendo "46" e "Gateway Único de IA e Registry de Modelos"; "Stripe" como "Fora da numeração / iniciativa diferida".

    NÃO editar nenhum arquivo se os greps confirmarem consistência. Se houver resíduo real de estado atual (fora das exceções), corrigir APENAS a linha do resíduo (mínimo delta). PROIBIDO alterar notas históricas.
  </action>
  <verify>
    <automated>
      $bad = @(); $files = @('ROADMAP.md','.planning\ROADMAP.md','.planning\STATE.md','.planning\PROJECT.md','AGENTS.md'); $state = @('F46 = Stripe','Phase 46 \(Stripe','fase-46-stripe','\| 44 \|','Phase 44 \(','F47 = Gateway','fase-47-gateway'); foreach ($f in $files) { foreach ($p in $state) { if (Select-String -LiteralPath $f -Pattern $p -Quiet) { $bad += "$f [$p]" } } }; if ($bad.Count -gt 0) { Write-Error "Residuos no estado atual: $($bad -join ', ')" } else { Write-Output "OK: zero residuos de nomenclatura no estado atual" }
    </automated>
  </verify>
  <acceptance_criteria>
    - Grep dos padrões de estado atual nos 5 runbooks → 0 ocorrências
    - `.planning/ROADMAP.md` tem a seção "### Phase 46: Gateway Único de IA e Registry de Modelos" e a nota "Phase numbering" com F46/F47
    - `.planning/STATE.md` frontmatter tem `current_phase: 46`
    - `AGENTS.md` tem o bloco "## Phase 46 — Gateway Único de IA e Registry de Modelos" com "9 plans previstos"
    - Nenhuma nota histórica alterada; F44 não aparece como fase numerada
  </acceptance_criteria>
  <done>Trackings F46/F47/F44/Stripe consistentes nos 5 runbooks — zero resíduos de estado atual</done>
</task>

<task type="auto">
  <name>Task 2: Inventário/baseline das 11 capacidades + 7 furos sem telemetria + baseline de testes (1.2)</name>
  <files>.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-01-SUMMARY.md</files>
  <read_first>
    - openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/design.md (:11-25 — 7 furos com file:line; :202-227 — Mapping)
    - openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/proposal.md (:3-9 — 13 call sites / 14 envs)
    - src/lib/image-generation/config.ts (:1-43 — envs de imagem/visão)
    - src/lib/text-provider/{openai,gemini,factory}.ts (envs de texto)
    - src/lib/image-generation/services/image-generation-service.ts (:135-156 — emitMetricsEvent hardcode; :224-241, :469-471 — onCall interno)
    - src/lib/ai-cost/cost-estimator.ts (:194 — gate campaign_image)
    - .planning/phases/45-briefing-contextual-do-diretor-de-arte/45-01-SUMMARY.md (padrão de SUMMARY de baseline)
  </read_first>
  <action>
    Produzir em `46-01-SUMMARY.md` o baseline factual da fase (sem alterar código):

    1. **Inventário das 11 capacidades** em tabela: `capacidade → serviço(s)/arquivo:linha → segmento → protocolo → adapter → default atual (provider/model) → env-var removida`. Cobrir: `campaign_copy` (copy-director-service.ts + text-provider/openai.ts:6 + gemini.ts:12 + factory.ts:7), `campaign_correction_analysis` (correction-intent-service.ts:159), `brand_profile_text` (text-only-inference-service.ts:57), `campaign_spec` (campaign-intelligence/providers/openai.ts:27), `campaign_input_validation` (input-validation-service.ts:29), `campaign_image_review` (image-review-service.ts:42), `brand_profile_vision` (brand-profiler.ts:279/294/609/707/739 + brand-director.ts:345/368), `visual_signature_validation` (ai-image-generator.ts:68), `campaign_image` (image-generation/providers/openai.ts:50), `campaign_image_edit` (image-generation/providers/openai.ts:51), `visual_signature_image` (ai-image-generator.ts:216).
    2. **Baseline dos 7 furos/caminhos sem telemetria** com evidência `file:line`: (1) `image-generation-service.ts:135-142` (modelo de imagem hardcoded para validação/revisão); (2) `api/store/[id]/logo/route.ts:254` e `logo/retry-brand-director/route.ts:111` (`analyze()` sem `onCall`); (3) `providers/openai.ts:255-371` (`fallbackToImageApi` sem usage); (4) `ai-image-generator.ts:249-254`/`cost-estimator.ts:194` (VS image sem componente da tool); (5) `visual-signature/server-actions.ts:136/152/233/285` (`AiImageGenerator.generate` sem `onCall`); (6) `visual-signature/approve/route.ts:197/589`; (7) `visual-signature/restore/route.ts:158`.
    3. **Inventário das 14 envs** (D8) com `file:line` das leituras (28 ocorrências) e a lista de chaves/operacionais que ficam.
    4. **Baseline de testes** a co-migrar: `generate-image/route.test.ts` (`TEXT_FALLBACK_PROVIDER` :929/:960; `IMAGE_GENERATION_RESPONSES_MODEL`), `campaign-generate.test.ts`, `concurrency.test.ts`, `regression-master-switch.test.ts`; `ai-cost/__tests__/cost-estimator.test.ts:332` (âncora anti-dupla VS); suites de não-mudança (rota/schema/snapshot/domínio/UI/form/prompts).
    5. **Decisão de numeração** registrada: F46 = Gateway (Change A), F47 = Catálogo/Seleção Admin (Change B), F44 fora da numeração, Stripe diferido v1.7+.
    Não alterar nenhuma linha de código nesta task.
  </action>
  <verify>
    <automated>Select-String -LiteralPath ".planning\phases\46-gateway-unico-de-ia-e-registry-de-modelos\46-01-SUMMARY.md" -Pattern "campaign_image_edit|emitMetricsEvent|cost-estimator.ts:194|TEXT_FALLBACK_PROVIDER" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <acceptance_criteria>
    - `46-01-SUMMARY.md` contém a tabela das 11 capacidades com serviço/arquivo:linha/segmento/protocolo/adapter/default
    - Os 7 furos estão listados com evidência `file:line` explícita
    - As 14 envs de modelo/provider estão listadas com `file:line` das leituras
    - Baseline de testes a co-migrar registrado (inclui `cost-estimator.test.ts:332`)
    - Nenhuma linha de código alterada (git diff do plano = apenas SUMMARY)
  </acceptance_criteria>
  <done>Baseline/inventário registrado — entrada factual para 46-03/46-04/46-05/46-07</done>
</task>

<task type="auto">
  <name>Task 3: Criar model-resolver.ts + model-registry.ts + testes unitários (1.3/1.4)</name>
  <files>src/lib/ai/model-resolver.ts, src/lib/ai/model-registry.ts, src/lib/ai/__tests__/model-registry.test.ts</files>
  <read_first>
    - openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/design.md (:45-87 — D1/D1.1; :66-78 — tabela de defaults)
    - openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/specs/ai-model-registry/spec.md (:7-130)
    - src/lib/ai-cost/ai-model-pricing.ts (:24-38 — modelos precificados, base da allowlist)
    - src/lib/ai-cost/__tests__/cost-estimator.test.ts (padrão de testes de módulo de IA)
  </read_first>
  <action>
    Criar `src/lib/ai/model-resolver.ts` com a interface `AiModelResolver` (`resolve(capability: string): AiModelConfig` + `listCapabilities(): string[]`) e os tipos `AiProtocol`/`AiModelTarget`/`AiModelConfig` (design D1). O gateway dependerá apenas da interface.

    Criar `src/lib/ai/model-registry.ts` exportando:
    - `MODEL_REGISTRY: Record<string, AiModelConfig>` com as **11 capacidades** e defaults idênticos ao design :66-78 (incl. `campaign_copy.fallback = { provider: "gemini", model: "gemini-3.1-flash-lite", protocol: "gemini" }`).
    - `MODEL_ALLOWLIST` validada por **capacidade + provider + modelo + protocolo** (nunca só por segmento): `gpt-4o`→`chat-completions`; `gpt-4o-mini`→`chat-completions`/`responses`; `gpt-5.5`→`responses`; `gpt-image-2`→`images`; `gemini-3.1-flash-lite`/`gemini-2.0-flash`→`gemini`.
    - Validação no carregamento: rejeitar `primary` e `fallback` com o mesmo `provider`+`model` (D1); rejeitar modelo fora da allowlist ou protocolo incompatível com a capacidade; `resolve` de capacidade desconhecida falha explicitamente.
    - Classe/objeto `ModelRegistry implements AiModelResolver` como implementação inicial (default do sistema).
    NÃO importar provider SDKs, NÃO ler env-var de modelo.

    Criar `src/lib/ai/__tests__/model-registry.test.ts` cobrindo: resolução das 11 capacidades; `primary.protocol` por capacidade (`campaign_image`→`responses`, `campaign_image_edit`→`images`); default inicial de `campaign_copy` (primary `chat-completions`, fallback `gemini`); visão com modelos distintos (`campaign_image_review` gpt-4o × `visual_signature_validation` gpt-4o-mini); allowlist rejeita modelo/protocolo inválido; `primary ≠ fallback` rejeitado (mesmo provider+model); `listCapabilities()` com 11 entradas. Apenas o teste do default inicial de `campaign_copy` menciona Gemini — nenhum teste deve codificar "sempre chamar Gemini".
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai/__tests__/model-registry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `model-resolver.ts` exporta `AiModelResolver` com `resolve` e `listCapabilities`
    - `model-registry.ts` contém as 11 capacidades com `protocol` no primary e no fallback (quando houver)
    - `campaign_copy.fallback` = `{ provider: "gemini", model: "gemini-3.1-flash-lite", protocol: "gemini" }`
    - `campaign_image` → `responses`; `campaign_image_edit` → `images`; `visual_signature_validation` → `responses` com gpt-4o-mini
    - Testes verdes para allowlist, protocolos, `primary ≠ fallback` e defaults
  </acceptance_criteria>
  <done>Registry + resolver + testes unitários verdes (seam pronto para o gateway do 46-02)</done>
</task>

<task type="auto">
  <name>Task 4: [BLOCKING] Migration CHECK chk_generation_events_type + GenerationEventType com campaign_spec + push (1.5)</name>
  <files>supabase/migrations/20260912000001_f46_generation_events_type.sql, src/lib/visual-signature/types.ts</files>
  <read_first>
    - supabase/migrations/20260906000003_f37_2_generation_events_type.sql (padrão DROP CONSTRAINT IF EXISTS + ADD CONSTRAINT + REVERT)
    - openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/design.md (:167-173 — D6; :229-236 — Migration Plan)
    - openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/specs/ai-campaign-intelligence/spec.md (:39-53)
    - src/lib/visual-signature/types.ts (:99-115)
    - .planning/phases/42-signup-controlado-elegibilidade-freemium/42-12-PLAN.md (padrão [BLOCKING] de push)
  </read_first>
  <action>
    1. Criar `supabase/migrations/20260912000001_f46_generation_events_type.sql` no padrão F37.2: `ALTER TABLE public.generation_events DROP CONSTRAINT IF EXISTS chk_generation_events_type;` + `ADD CONSTRAINT chk_generation_events_type CHECK (generation_type IN (...))` com os **15 valores atuais preservados** (`campaign_pipeline`, `campaign_copy`, `campaign_input_validation`, `campaign_image`, `campaign_image_review`, `visual_signature`, `visual_signature_image`, `visual_signature_validation`, `brand_profile_without_logo`, `brand_profile_with_logo`, `brand_profile_vision`, `brand_profile_text`, `theme_direction`, `theme_generation`, `campaign_correction_analysis`) **+ `campaign_spec`** (16º). Incluir bloco `-- REVERT` comentado com a nota explícita de que ele **só** se aplica **antes** de existir qualquer evento `campaign_spec` (D6 — depois disso o CHECK é aditivo permanente). Sem DDL de coluna/tabela nova.
    2. Adicionar o literal `'campaign_spec'` ao union `GenerationEventType` em `src/lib/visual-signature/types.ts` (aditivo, retrocompatível).
    3. **[BLOCKING]** Aplicar no remoto: `npx supabase db push --dry-run` (confirmar que APENAS `20260912000001_f46_generation_events_type` seria aplicada); depois `npx supabase db push` (padrão 38-01/42-12 — pooler do projeto linkado). Se o push exigir confirmação interativa insuprimível, parar e sinalizar intervenção manual. Validar com `supabase migration list` que a migration consta no remoto e que o dry-run subsequente reporta "No new migrations".
  </action>
  <verify>
    <automated>npx supabase db push --dry-run 2>&1 | Select-String -Pattern "No new migrations"</automated>
  </verify>
  <acceptance_criteria>
    - Migration contém `DROP CONSTRAINT IF EXISTS chk_generation_events_type` + `ADD CONSTRAINT chk_generation_events_type` com os 15 valores atuais + `campaign_spec`
    - Bloco `-- REVERT` presente com a nota "só antes de existir evento campaign_spec"
    - `GenerationEventType` inclui `'campaign_spec'`
    - `supabase db push` aplicou a migration no remoto (exit 0); dry-run subsequente reporta "No new migrations"
    - `supabase migration list` confirma `20260912000001_f46_generation_events_type` no remoto
  </acceptance_criteria>
  <done>Literal `campaign_spec` aceito pelo CHECK no remoto e pelo tipo TS ([BLOCKING] concluído)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| código ↔ registry | Fonte única de modelo por capacidade — nenhuma env-var de modelo deve ser lida |
| migration ↔ generation_events | Extensão aditiva do CHECK de telemetria — risco de invalidar eventos históricos |
| dev ↔ banco remoto | Aplicação da migration via `supabase db push` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01a | Tampering | `chk_generation_events_type` | mitigate | DROP/ADD idempotente preservando os 15 valores atuais; REVERT documentado como "só antes de existir `campaign_spec`"; nunca remover literal com eventos históricos |
| T-46-01b | Elevation of Privilege | `model-registry.ts` | mitigate | Allowlist validada por capacidade+provider+modelo+protocolo; `primary ≠ fallback`; capacidade desconhecida falha explícita |
| T-46-01c | Information Disclosure | 5 runbooks | accept | Apenas nomenclatura de fases e caminhos de arquivo — sem segredos |
| T-46-01d | Tampering | Notas históricas dos runbooks | mitigate | Grep com padrões precisos de estado atual; exceções históricas documentadas e intocadas |
| T-46-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo nesta fase |
</threat_model>

<verification>
- Grep de padrões de estado atual (F46/F47/F44/Stripe) → 0 ocorrências nos 5 runbooks
- `npx vitest run src/lib/ai/__tests__/model-registry.test.ts` → verde
- `npm run typecheck` → 0 erros (tipos do registry + `campaign_spec`)
- `npm run lint` → 0 erros
- `npm run build` → 0 erros
- Migration no remoto confirmada por `supabase migration list`; dry-run reporta "No new migrations"
</verification>

<success_criteria>
- Trackings F46/F47/F44/Stripe verificados (zero resíduos no estado atual)
- Inventário das 11 capacidades + 7 furos (`file:line`) + 14 envs + baseline de testes no SUMMARY
- `AiModelResolver` + registry com defaults idênticos, allowlist e `primary ≠ fallback` + testes verdes
- Migration aditiva/idempotente aplicada no remoto + `GenerationEventType` com `campaign_spec`
</success_criteria>

<output>
Create `.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-01-SUMMARY.md` when done
</output>

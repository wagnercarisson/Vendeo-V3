---
phase: 46-gateway-unico-de-ia-e-registry-de-modelos
plan: 02
type: execute
wave: 2
depends_on: [46-01]
files_modified:
  - src/lib/ai/types.ts
  - src/lib/ai/api-keys.ts
  - src/lib/ai/gateway.ts
  - src/lib/ai/telemetry-sink.ts
  - src/lib/ai/generation-type-map.ts
  - src/lib/ai/adapters/chat-completions.ts
  - src/lib/ai/adapters/responses.ts
  - src/lib/ai/adapters/images.ts
  - src/lib/ai/adapters/gemini.ts
  - src/lib/ai/adapters/registry.ts
  - src/lib/ai/index.ts
  - src/lib/ai/__tests__/api-keys.test.ts
  - src/lib/ai/__tests__/gateway.test.ts
  - src/lib/ai/__tests__/adapters.test.ts
  - src/lib/ai/__tests__/telemetry-sink.test.ts
autonomous: true
requirements: [F46-06, F46-07, F46-08, F46-09, F46-10, F46-11]

must_haves:
  truths:
    - "`getApiKey(provider)` resolve chaves por switch exaustivo (`OPENAI_API_KEY`/`GEMINI_API_KEY`) e falha explicitamente em produção sem chave (sem fallback silencioso)"
    - "`invoke(capability, request, telemetry, target)` resolve o alvo explícito (`primary`/`fallback`), escolhe o adapter pelo `protocol` do alvo e executa **uma tentativa** — sem retry e sem fallback automático"
    - "Cada tentativa HTTP real emite **exatamente um `AiCallEnvelope`** (`extends AiCallInfo`, com `capability`/`protocol`/`status`/`errorType`), inclusive falha/timeout — `AiCallInfo` legado permanece intacto (migração gradual)"
    - "O `AiTelemetryContext` exige **`sink` obrigatório** (não opcional); `NoopAiTelemetrySink` só é aceito em teste com adapter falso"
    - "O gateway é composto por injeção de construtor: `new AiGateway(resolver, adapters)` — `invoke` **não** recebe resolver; `src/lib/ai/index.ts` compõe a instância padrão"
    - "`AiInvocationError` normaliza erros HTTP/provider (`kind`/`httpStatus`/`retryable`/`code`/`message` sanitizada) preservando os gates de fallback; erros de parsing do domínio NÃO são normalizados"
    - "O sink padrão (`CAPABILITY_GENERATION_TYPE` + `resolveAiCost` + `AiCostTracker.record`) é best-effort/fail-open, preserva buffering/ordenação quando injetado e mantém `capability`/`protocol` no `metadata` (D9/D10)"
    - "`AbortSignal` é propagado ao adapter e o cancelamento interrompe a chamada"
  artifacts:
    - path: "src/lib/ai/types.ts"
      provides: "AiCallEnvelope + AiTelemetryContext (sink obrigatório) + AiAdapterRegistry + AiInvocationError"
      contains: "AiCallEnvelope"
    - path: "src/lib/ai/api-keys.ts"
      provides: "Resolução de chave por provider"
      contains: "getApiKey"
    - path: "src/lib/ai/gateway.ts"
      provides: "Camada única de invocação (AiGateway com injeção por construtor)"
      contains: "AiGateway"
    - path: "src/lib/ai/telemetry-sink.ts"
      provides: "AiTelemetrySink + sink padrão + NoopAiTelemetrySink + createDefaultTelemetryContext"
      contains: "AiTelemetrySink"
    - path: "src/lib/ai/adapters/registry.ts"
      provides: "AiAdapterRegistry + defaultAdapterRegistry"
      contains: "AiAdapterRegistry"
    - path: "src/lib/ai/index.ts"
      provides: "Composição da instância padrão (ModelRegistry + adapters) + invoke"
      contains: "new AiGateway"
    - path: "src/lib/ai/generation-type-map.ts"
      provides: "Mapa canônico capability → generationType (11 capacidades)"
      contains: "campaign_image_edit"
    - path: "src/lib/ai/adapters/chat-completions.ts"
      provides: "Adapter Chat Completions"
      contains: "chat-completions"
    - path: "src/lib/ai/adapters/responses.ts"
      provides: "Adapter Responses API"
      contains: "image_generation"
    - path: "src/lib/ai/adapters/images.ts"
      provides: "Adapter Images (images.edit)"
      contains: "not_available"
    - path: "src/lib/ai/adapters/gemini.ts"
      provides: "Adapter Gemini generateContent"
      contains: "generateContent"
    - path: "src/lib/ai-cost/types.ts"
      provides: "AiCallInfo legado **intacto** (produtores antigos continuam compilando)"
      contains: "AiCallInfo"
  key_links:
    - from: "src/lib/ai/gateway.ts"
      to: "src/lib/ai/model-resolver.ts"
      via: "resolver injetado no construtor (não por argumento de invoke) — depende só da interface (46-01)"
      pattern: "AiModelResolver"
    - from: "src/lib/ai/index.ts"
      to: "src/lib/ai/gateway.ts"
      via: "composição da instância padrão new AiGateway(new ModelRegistry(), defaultAdapterRegistry)"
      pattern: "new AiGateway"
    - from: "src/lib/ai/gateway.ts"
      to: "src/lib/ai/telemetry-sink.ts"
      via: "emissão de um envelope por tentativa"
      pattern: "sink"
    - from: "src/lib/ai/adapters/images.ts"
      to: "src/lib/ai-cost/types.ts"
      via: "usage ausente → not_available"
      pattern: "not_available"
---

<objective>
Construir a fundação de execução da F46 sem migrar nenhum serviço: (2.1) `api-keys.ts` com `getApiKey(provider)` (switch exaustivo; fail-fast em produção); (2.2) `gateway.ts` com `class AiGateway(resolver, adapters)` (**injeção por construtor**) e `invoke(capability, request, telemetry, target = "primary")` — `await resolver.resolve()`, alvo explícito, adapter pelo `protocol` via `AiAdapterRegistry`, **uma tentativa**, usage normalizado, **um envelope por tentativa** via sink injetável, contexto obrigatório em produção; (2.3) adapters `chat-completions`/`responses`/`images`/`gemini` + `adapters/registry.ts` com contrato único e `AbortSignal` propagado; (2.4) criar `AiCallEnvelope extends AiCallInfo` (aditivo; `AiCallInfo` legado **intacto** — migração gradual); (2.5) contrato de erro `AiInvocationError` preservando os gates (D4.1); (2.6) sink padrão (`CAPABILITY_GENERATION_TYPE` + `resolveAiCost` + `AiCostTracker`) + interface de sink injetável + `NoopAiTelemetrySink` + factory de contexto (`sink` obrigatório); (2.7) `src/lib/ai/index.ts` compondo a instância padrão.

Purpose: D1.1/D2/D3/D4/D4.1 (composição injetável, camada única, envelope por tentativa, retry/fallback fora do gateway, contrato de erro), D7 (modelo real; telemetria correta).
**DAG:** `depends_on: [46-01]` (onda 2) — o gateway importa `AiModelResolver`/`AiCapability`/`AiProtocol` de `src/lib/ai/model-resolver.ts` (46-01). `AiProtocol`/`AiCapability` têm dono único (46-01); 46-02 apenas importa/re-exporta.
Output: `src/lib/ai/{types,api-keys,gateway,telemetry-sink,index}.ts` + `adapters/*` + `adapters/registry.ts` + `AiCallEnvelope` + testes verdes (seam pronto para a migração da onda 3).
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-CONTEXT.md (D2 :105-106, D3 :108-110, D4 :112-117, D4.1 :119-125; F46-06..F46-11 :236-242)
@openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/design.md (D2 :92-107, D3 :109-122, D4 :124-136, D4.1 :138-158)
@openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/specs/ai-invocation-gateway/spec.md (:5-181)
@openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/specs/ai-cost-accounting/spec.md (:7-27)
@src/lib/ai-cost/types.ts (:99-109 — AiCallInfo; :37-52 — TokenUsage)
@src/lib/ai-cost/tracker.ts (:22-88 — fail-open)
@src/lib/ai-cost/cost-estimator.ts (:141-243 — resolveAiCost)
@src/lib/copy/errors.ts (MalformedResponseError/ProviderRateLimitError/Provider5xxError/NetworkError/AuthConfigError/SafetyBlockError)
@src/lib/image-generation/providers/openai.ts (:226-245 — isResponsesApiError; :255-371 — fallback images.edit)
@src/lib/ai/model-registry.ts (46-01 — registry/resolver)

<interfaces>
Contrato único (a definir em `src/lib/ai/types.ts`):
- `AiInvocationRequest`: `prompt` (string) + campos opcionais por protocolo (messages/system, imagens `productImagesDataUrls`/`identityImageUrl`, `size`, `quality`, `responseFormat` `json_schema`|`json_object`, `tools: image_generation`, `maxTokens`, `temperature`, `signal`).
- `AiInvocationResult`: `content?` (texto), `imageBase64?`, `mimeType?`, `model` (modelo REAL), `usage?: TokenUsage`, `usageMeta?`, `providerReportedCostUsd?`.
- `AiCallEnvelope extends AiCallInfo` (**aditivo**; `AiCallInfo` legado **intacto** — migração gradual, pois produtores antigos ainda o constroem): `+ capability: AiCapability; protocol: AiProtocol; status: "success"|"failed"|"timeout"; errorType?: string`. O gateway e o sink usam `AiCallEnvelope`.
- `AiInvocationError`: `kind: "timeout"|"auth"|"rate_limit"|"capability"|"network"|"content_filter"|"provider_error"`; `httpStatus?: number`; `retryable: boolean`; `code?: string`; `message: string` (sanitizada, sem chave/URL).
- `AiTelemetryContext`: `{ operationRunId; operationRunType; traceId; storeId; userId?; campaignId?; visualSignatureId?; themeId?; attemptNumber?; sink: AiTelemetrySink }` — **`sink` OBRIGATÓRIO** (não opcional): contexto de produção sem destino não passa a validação. Testes passam `NoopAiTelemetrySink` **explícito**; `createDefaultTelemetryContext(...)` injeta o sink padrão; fluxos com buffering injetam o próprio sink.
- `AiTelemetrySink`: `emit(envelope: AiCallEnvelope) => void | Promise<void>`; sink padrão usa `CAPABILITY_GENERATION_TYPE` (11 capacidades) + `resolveAiCost` + `AiCostTracker.record` (fail-open) e grava `capability`/`protocol` no `metadata` (D9/D10).
- `AiAdapterRegistry`: mapa `AiProtocol → AiAdapter` (implementação padrão em `src/lib/ai/adapters/registry.ts`).
- `class AiGateway { constructor(resolver: AiModelResolver, adapters: AiAdapterRegistry); invoke(capability, request, telemetry, target: "primary" | "fallback" = "primary"): Promise<AiInvocationResult> }` — injeção por construtor; `invoke` **não** recebe o resolver. `src/lib/ai/index.ts` compõe a instância padrão (`new AiGateway(new ModelRegistry(), defaultAdapterRegistry)`) e expõe `invoke(...)`.

Gates de fallback preservados (D4.1 — decidir `retryable` uma vez na normalização):
- Retryable: `rate_limit` (429), `timeout` (AbortError), `network`, `provider_error` 5xx, `MalformedResponseError` (classificado pelo orquestrador) → **acionam** fallback de modelo.
- `capability`: `model_not_found`/tool não suportada → habilita `images.edit`; `response_format`/`json_schema` → habilita `json_object`.
- `auth` (401/403) e `content_filter`/safety → **não** acionam fallback.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: api-keys.ts com getApiKey(provider) + testes (2.1)</name>
  <files>src/lib/ai/api-keys.ts, src/lib/ai/__tests__/api-keys.test.ts</files>
  <read_first>
    - openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/specs/ai-model-registry/spec.md (:50-64)
    - design.md (:238-242 — decisões consolidadas: switch exaustivo)
    - src/lib/ai/model-registry.ts (providers existentes: openai, gemini)
  </read_first>
  <action>
    Criar `src/lib/ai/api-keys.ts` exportando `getApiKey(provider: string): string` com **switch exaustivo** por provider conhecido (`openai` → `OPENAI_API_KEY`; `gemini` → `GEMINI_API_KEY`). Provider desconhecido lança erro explícito. **Contrato coerente (nunca `undefined`):** em `NODE_ENV === "production"`, chave ausente → lança erro de configuração explícito (fail-fast, sem fallback silencioso); em dev/teste, chave ausente → retorna `""` (string vazia), preservando os caminhos mock/dev que hoje checam `if (!process.env.OPENAI_API_KEY)` (os serviços passam a checar `if (!getApiKey("openai"))`). Não ler nenhuma outra env-var.
    Criar `src/lib/ai/__tests__/api-keys.test.ts` cobrindo: openai→`OPENAI_API_KEY`; gemini→`GEMINI_API_KEY`; provider desconhecido lança; produção sem chave lança; dev sem chave retorna `""` (tipo `string`, sem `undefined`).
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai/__tests__/api-keys.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `getApiKey("openai")` retorna `process.env.OPENAI_API_KEY`; `getApiKey("gemini")` retorna `process.env.GEMINI_API_KEY`
    - Provider desconhecido lança erro explícito
    - `NODE_ENV=production` sem chave → erro de configuração (sem fallback silencioso)
    - Em dev/teste sem chave → retorna `""` (tipo `string`; nunca `undefined`)
    - Testes verdes
  </acceptance_criteria>
  <done>api-keys.ts criado com switch exaustivo e fail-fast em produção</done>
</task>

<task type="auto">
  <name>Task 2: Criar AiCallEnvelope + contrato de erro AiInvocationError + tipos de request/result (2.4/2.5)</name>
  <files>src/lib/ai/types.ts, src/lib/ai/__tests__/adapters.test.ts</files>
  <read_first>
    - src/lib/ai-cost/types.ts (:99-109 — `AiCallInfo` **intacto**; :37-52 — TokenUsage)
    - openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/specs/ai-invocation-gateway/spec.md (:63-117)
    - design.md (:109-158 — D3/D4.1)
    - src/lib/copy/errors.ts (erros retryable/safety/auth atuais)
  </read_first>
  <action>
    1. **NÃO alterar `AiCallInfo`** em `src/lib/ai-cost/types.ts` (há produtores legados construindo-o sem os campos novos — ex.: `ai-image-generator.ts:115` — e o 46-02 exige typecheck/build verdes). Criar em `src/lib/ai/types.ts` o tipo **aditivo** `AiCallEnvelope extends AiCallInfo` com `capability: AiCapability`, `protocol: AiProtocol`, `status: "success" | "failed" | "timeout"`, `errorType?: string`; o gateway e o sink usam `AiCallEnvelope`. Importar `AiCallInfo` de `@/lib/ai-cost/types` e `AiCapability`/`AiProtocol` de `./model-resolver` (46-01) — **não redefinir** os unions.
    2. Criar `src/lib/ai/types.ts` com: `AiInvocationRequest`; `AiInvocationResult`; `AiCallEnvelope`; `AiTelemetryContext` (**`sink: AiTelemetrySink` obrigatório**); `AiTelemetrySink`; `AiAdapterRegistry`; a classe `AiInvocationError` (`kind`/`httpStatus`/`retryable`/`code`/`message` sanitizada); e o re-export `export type { AiCapability, AiProtocol, AiModelTarget, AiModelConfig } from "./model-resolver";` (**re-export, sem redefinir**). Incluir o normalizador de erro HTTP/provider (`normalizeAiError`) que decide `retryable` **uma vez**: 429→`rate_limit` retryable; 401/403→`auth` não retryable; 5xx→`provider_error` retryable; abort→`timeout` retryable; `model_not_found`/tool não suportada→`capability` não retryable (habilita `images.edit`); `response_format`/`json_schema`→`capability`; safety/content_filter→`content_filter` não retryable; rede→`network` retryable. `MalformedResponseError` do domínio **não** é normalizado (propaga).
    3. Cobrir em `adapters.test.ts` (mesmo arquivo da Task 4) os testes por `kind` e por gate: rate_limit/timeout/network/5xx → `retryable: true`; auth/safety → `retryable: false`; capability de tool → `kind: "capability"`; capability de response_format → `kind: "capability"`; `MalformedResponseError` propaga sem normalização.
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai/__tests__/adapters.test.ts -t "AiInvocationError"</automated>
  </verify>
  <acceptance_criteria>
    - `AiCallInfo` (em `src/lib/ai-cost/types.ts`) **inalterado** — produtores legados continuam compilando
    - `AiCallEnvelope extends AiCallInfo` com `capability`/`protocol`/`status`/`errorType` (aditivo), usado pelo gateway/sink
    - `AiTelemetryContext.sink` é **obrigatório** (o tipo não compila um contexto de produção sem sink)
    - `AiInvocationError` expõe `kind`/`httpStatus`/`retryable`/`code`/`message` sanitizada
    - Normalização: rate_limit/timeout/network/5xx retryable; auth/content_filter não retryable; capability distinguível
    - `MalformedResponseError` NÃO é normalizado pelo gateway
    - Testes verdes
  </acceptance_criteria>
  <done>AiCallEnvelope, contrato de erro e contexto com sink obrigatório prontos para o gateway</done>
</task>

<task type="auto">
  <name>Task 3: gateway.ts invoke + sink injetável + testes (2.2/2.6)</name>
  <files>src/lib/ai/gateway.ts, src/lib/ai/telemetry-sink.ts, src/lib/ai/generation-type-map.ts, src/lib/ai/__tests__/gateway.test.ts, src/lib/ai/__tests__/telemetry-sink.test.ts</files>
  <read_first>
    - src/lib/ai/model-resolver.ts (46-01)
    - src/lib/ai/types.ts (Task 2)
    - openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/specs/ai-invocation-gateway/spec.md (:7-61, :118-167)
    - design.md (:92-122 — D2/D3)
    - src/lib/ai-cost/{index.ts,tracker.ts,cost-estimator.ts}
  </read_first>
  <action>
    Criar `src/lib/ai/generation-type-map.ts` com `CAPABILITY_GENERATION_TYPE: Record<string, GenerationEventType>` cobrindo as **11 capacidades** (D10), com `campaign_image_edit → "campaign_image"` (fallback de edição pertence à etapa de imagem) e as demais mapeando para o próprio literal; o mapa é o **único** ponto que traduz capability→generationType persistido.
    Criar `src/lib/ai/telemetry-sink.ts` com a interface `AiTelemetrySink` (`emit(envelope: AiCallEnvelope)`) e: o **sink padrão** (usa `CAPABILITY_GENERATION_TYPE` para resolver o `generationType`, chama `resolveAiCost` + `AiCostTracker.record` — fail-open: nunca lança; falha só logada — e grava `capability`/`protocol` originais no `metadata`); um **`NoopAiTelemetrySink`** (explícito, **só para testes**); um helper para compor sink de buffering/ordenação; e a factory **`createDefaultTelemetryContext(...)`** que monta o `AiTelemetryContext` com o **sink padrão** (o caller pode passar outro sink para buffering).
    Criar `src/lib/ai/gateway.ts` com `class AiGateway { constructor(private readonly resolver: AiModelResolver, private readonly adapters: AiAdapterRegistry) }` e `invoke(capability, request, telemetry, target = "primary")`:
    1. `await this.resolver.resolve(capability)` e seleciona o alvo indicado (`primary`/`fallback`), lançando se o fallback não existir;
    2. escolhe o adapter via `this.adapters.get(protocolDoAlvo)` (`chat-completions`/`responses`/`images`/`gemini`);
    3. executa **uma tentativa** (sem retry, sem fallback automático); propaga `AbortSignal`;
    4. normaliza `usage` e o resultado;
    5. emite **exatamente um `AiCallEnvelope`** via `telemetry.sink` (contexto + `capability`/`protocol`/`status`/`errorType` + modelo real) — sucesso, falha ou timeout; em erro, normaliza em `AiInvocationError` (exceto parsing do domínio) e re-lança.
    `AiAdapterRegistry` (tipo em `src/lib/ai/types.ts`) é implementado no Task 4 (`adapters/registry.ts`) junto com `src/lib/ai/index.ts` (composição da instância padrão). Contexto de telemetria **obrigatório**: `invoke` exige `telemetry` (com `sink`) — sem contexto rejeita; `NoopAiTelemetrySink` só é aceito em testes com adapter falso. O gateway **nunca** cria run implícito.
    Testes (`gateway.test.ts`): injetar `new AiGateway(fakeResolver, fakeAdapterRegistry)` com adapter falso + sink controlado; exatamente 1 envelope por tentativa (success/failed/timeout); modelo real no envelope; `target` não informado → `primary`; `target: "fallback"` resolve o alvo configurado; fallback ausente → erro; sem retry (adapter chamado 1×); `AbortSignal` propagado; contexto sem `sink` → rejeita (erro de tipo/comportamento). `telemetry-sink.test.ts`: persistência best-effort (record lançando não propaga), buffering/ordenação preservados, `NoopAiTelemetrySink`, `createDefaultTelemetryContext`, e o **mapa canônico** (11 entradas; `campaign_image_edit → campaign_image`; `capability`/`protocol` no `metadata`).
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai/__tests__/gateway.test.ts src/lib/ai/__tests__/telemetry-sink.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `AiGateway` recebe `resolver` e `adapters` por **construtor**; `invoke` **não** recebe resolver; usa `await resolver.resolve()` e `adapters.get(protocol)`
    - `invoke` seleciona o alvo explícito e o adapter pelo `protocol` do alvo
    - Exatamente um `AiCallEnvelope` por tentativa real (success/failed/timeout), com modelo real
    - Sem retry/fallback automático; `AbortSignal` propagado; erro de domínio não normalizado
    - `AiTelemetryContext.sink` obrigatório; `NoopAiTelemetrySink` só em teste; `createDefaultTelemetryContext` injeta o sink padrão
    - Sink padrão best-effort (fail-open) e buffering/ordenação preservados
    - `CAPABILITY_GENERATION_TYPE` cobre as 11 capacidades com `campaign_image_edit → campaign_image`; `capability`/`protocol` originais no `metadata`
    - Testes verdes
  </acceptance_criteria>
  <done>Gateway injetável + sink injetável verdes (sem migrar serviços)</done>
</task>

<task type="auto">
  <name>Task 4: Adapters chat-completions/responses/images/gemini com contrato único (2.3)</name>
  <files>src/lib/ai/adapters/chat-completions.ts, src/lib/ai/adapters/responses.ts, src/lib/ai/adapters/images.ts, src/lib/ai/adapters/gemini.ts, src/lib/ai/adapters/registry.ts, src/lib/ai/index.ts, src/lib/ai/__tests__/adapters.test.ts</files>
  <read_first>
    - src/lib/ai/types.ts (Task 2)
    - src/lib/image-generation/providers/openai.ts (:54-202 — Responses tool image_generation + usage breakdown; :255-371 — images.edit)
    - src/lib/text-provider/openai.ts (:17-52 — Chat Completions)
    - src/lib/text-provider/gemini.ts (:15-54 — generateContent)
    - src/lib/campaign-intelligence/providers/openai.ts (:78-121 — structured outputs + json_object)
    - openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/specs/ai-invocation-gateway/spec.md (:41-77)
  </read_first>
  <action>
    Criar os 4 adapters em `src/lib/ai/adapters/` implementando o contrato único (`AiInvocationRequest` → `AiInvocationResult`), cada um conhecendo **apenas o shape do seu protocolo** (sem regra de negócio, sem decidir modelo/prompt):
    - `chat-completions.ts`: OpenAI Chat (texto, visão, JSON mode/structured outputs); normaliza `usage` (prompt/completion/total/cached); suporta `responseFormat` `json_schema`/`json_object`.
    - `responses.ts`: Responses API com tool `image_generation` e visão; normaliza o breakdown granular (`input/output` text/image tokens) do `response.usage` (espelhar `providers/openai.ts:118-160`).
    - `images.ts`: `images.edit` multi-imagem em ordem determinística (`[primary, auxiliares..., identity?]`); quando não houver usage, **sinalizar ausência explícita** (`usage` undefined + `usageMeta.providerUsageSource = "images.edit"`) — nunca inventar zeros.
    - `gemini.ts`: `generateContent` via `GoogleGenerativeAI`; normaliza `usageMetadata`.
    Cada adapter propaga `AbortSignal` e retorna o **modelo real** usado. Nenhum adapter lê env-var de modelo ou instancia provider fora de `getApiKey`.
    Criar `src/lib/ai/adapters/registry.ts` com `AiAdapterRegistry` (mapa `AiProtocol → AiAdapter`) e `defaultAdapterRegistry` (os 4 adapters). Criar `src/lib/ai/index.ts` compondo `new AiGateway(new ModelRegistry(), defaultAdapterRegistry)` e exportando `invoke(...)` como função de conveniência (ponto único de composição).
    Testes (`adapters.test.ts`): uso normalizado entre `chat-completions` e `responses`; provider sem usage é explícito (`images.edit`); `AbortSignal` propagado; modelo real retornado; adapter não decide prompt/modelo.
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai/__tests__/adapters.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 4 adapters com contrato único `AiInvocationResult` e modelo real
    - `responses` normaliza breakdown text/image; `images` marca ausência de usage como `not_available`/explícita
    - `AbortSignal` propagado em todos; nenhum adapter lê env-var de modelo
    - Testes verdes
  </acceptance_criteria>
  <done>Adapters por protocolo prontos para a migração das capacidades</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gateway ↔ provider | Chaves de API e payloads cruzam para APIs externas |
| gateway ↔ telemetria | Envelope por tentativa persiste dados de custo/uso (best-effort) |
| adapter ↔ provider wire | Shapes distintos (Chat/Responses/Images/Gemini) traduzidos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-02a | Information Disclosure | `api-keys.ts` / logs | mitigate | Chave resolvida só por `getApiKey`; `AiInvocationError.message` sanitizada sem chave/URL; nunca logar chave |
| T-46-02b | Spoofing | `AiInvocationError` | mitigate | `retryable` decidido uma vez na normalização; `MalformedResponseError` não normalizado (classificado pelo orquestrador) |
| T-46-02c | Denial of Service | Persistência de telemetria | mitigate | Sink padrão fail-open (nunca lança); falha só logada — geração nunca bloqueada |
| T-46-02d | Tampering | Envelope por tentativa | mitigate | Exatamente um envelope por tentativa real; contexto obrigatório em produção; sink no-op só em teste com adapter falso |
| T-46-02e | Elevation of Privilege | Adapters | mitigate | Adapter sem regra de negócio e sem leitura de env-var de modelo; modelo vem do resolver |
| T-46-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo (usa SDKs já instalados) |
</threat_model>

<verification>
- `npx vitest run src/lib/ai/__tests__/api-keys.test.ts src/lib/ai/__tests__/gateway.test.ts src/lib/ai/__tests__/telemetry-sink.test.ts src/lib/ai/__tests__/adapters.test.ts` → verde
- `npm run typecheck` → 0 erros
- `npm run lint` → 0 erros
- `npm run build` → 0 erros
- Grep: nenhum adapter lê `process.env.*_MODEL|_PROVIDER`
- `AiCallEnvelope` contém `capability`/`protocol`/`status`/`errorType`; `AiCallInfo` (legado) permanece **intacto**
- `AiTelemetryContext.sink` é obrigatório; `NoopAiTelemetrySink` só em teste
- `AiGateway` construído com `(resolver, adapters)`; `src/lib/ai/index.ts` compõe a instância padrão
- `AiProtocol`/`AiCapability` definidos apenas em `src/lib/ai/model-resolver.ts` (46-01); `src/lib/ai/types.ts` apenas re-exporta
</verification>

<success_criteria>
- `api-keys.ts`, `gateway.ts`, `telemetry-sink.ts`, `adapters/registry.ts`, `index.ts` e 4 adapters criados com contrato único
- `AiCallEnvelope` (aditivo) por tentativa real; `AiCallInfo` legado intacto; contexto com `sink` obrigatório; sem retry/fallback automático
- `AiInvocationError` preserva os gates de fallback (D4.1)
- Testes verdes (api-keys, gateway, telemetry-sink, adapters)
</success_criteria>

<output>
Create `.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-02-SUMMARY.md` when done
</output>

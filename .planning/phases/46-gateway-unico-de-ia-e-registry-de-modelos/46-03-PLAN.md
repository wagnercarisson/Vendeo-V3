---
phase: 46-gateway-unico-de-ia-e-registry-de-modelos
plan: 03
type: execute
wave: 2
depends_on: [46-01, 46-02]
files_modified:
  - src/lib/copy/copy-director-service.ts
  - src/lib/text-provider/openai.ts
  - src/lib/text-provider/gemini.ts
  - src/lib/text-provider/factory.ts
  - src/lib/campaign/correction-intent-service.ts
  - src/lib/brand-assets/text-only-inference-service.ts
  - src/lib/campaign-intelligence/providers/openai.ts
  - src/app/api/campaign/generate-image/route.ts
  - src/lib/copy/__tests__/copy-director-service.test.ts
  - src/lib/campaign/__tests__/correction-intent-service.test.ts
  - src/lib/brand-assets/__tests__/text-only-inference-service.test.ts
  - src/app/api/campaign/generate-image/__tests__/route.test.ts
autonomous: true
requirements: [F46-12, F46-13, F46-14, F46-15, F46-16]

must_haves:
  truths:
    - "`campaign_copy` executa via `invoke(\"campaign_copy\", …)` preservando `TextProviderResult` (content/usage/model) e parâmetros (system/temperature 0.7/maxTokens 1000)"
    - "O fallback de texto é **segunda `invoke(..., target: \"fallback\")` explícita** do orquestrador quando `retryable === true` e há fallback configurado (≠ primary) — o orquestrador não conhece o provider (sem `if provider === \"gemini\"`)"
    - "`campaign_correction_analysis` e `brand_profile_text` executam via gateway preservando comportamento e telemetria (status/errorType)"
    - "`campaign_spec` legado executa via `invoke(\"campaign_spec\", …)` com default `gpt-4o-mini`; o fallback `json_schema`→`json_object` é segunda `invoke` explícita"
    - "Nenhum serviço de texto instancia `new OpenAI()`/`new GoogleGenerativeAI()` nem lê env-var de modelo"
    - "Cada tentativa real (primária e fallback) gera o seu próprio envelope de telemetria"
  artifacts:
    - path: "src/lib/copy/copy-director-service.ts"
      provides: "campaign_copy via gateway"
      contains: "invoke"
    - path: "src/lib/text-provider/openai.ts"
      provides: "OpenAITextProvider delegando ao gateway"
      contains: "chat-completions"
    - path: "src/lib/text-provider/factory.ts"
      provides: "createTextProvider sem TEXT_PROVIDER/TEXT_FALLBACK_PROVIDER"
      contains: "createTextProvider"
    - path: "src/lib/campaign/correction-intent-service.ts"
      provides: "campaign_correction_analysis via gateway"
      contains: "campaign_correction_analysis"
    - path: "src/lib/brand-assets/text-only-inference-service.ts"
      provides: "brand_profile_text via gateway"
      contains: "brand_profile_text"
    - path: "src/lib/campaign-intelligence/providers/openai.ts"
      provides: "campaign_spec legado via gateway"
      contains: "campaign_spec"
    - path: "src/app/api/campaign/generate-image/route.ts"
      provides: "Fallback de texto como segunda invoke explícita"
      contains: "target"
  key_links:
    - from: "src/lib/copy/copy-director-service.ts"
      to: "src/lib/ai/gateway.ts"
      via: "invoke(\"campaign_copy\")"
      pattern: "invoke"
    - from: "src/app/api/campaign/generate-image/route.ts"
      to: "src/lib/ai/model-registry.ts"
      via: "target: \"fallback\" (default inicial Gemini, sem conhecer provider)"
      pattern: "target: \"fallback\""
    - from: "src/lib/campaign-intelligence/providers/openai.ts"
      to: "src/lib/ai/model-registry.ts"
      via: "campaign_spec (gpt-4o-mini)"
      pattern: "campaign_spec"
---

<objective>
Migrar as **capacidades de TEXTO** para a camada única, uma a uma, preservando comportamento: (3.1) `campaign_copy` (`copy-director-service.ts` + `text-provider/openai.ts`/`gemini.ts` + `factory.ts`) com o alvo de fallback configurado como **segunda `invoke(..., target: "fallback")` explícita** do orquestrador; (3.2) `campaign_correction_analysis` (`correction-intent-service.ts`); (3.3) `brand_profile_text` (`text-only-inference-service.ts`); (3.4) `campaign_spec` legado (`campaign-intelligence/providers/openai.ts`) com fallback `json_schema`→`json_object` como segunda `invoke`; (3.5) gates + co-migração dos testes de texto.

Purpose: D5 (migração incremental behavior-preserving), D2/D4 (alvo explícito; fallback do orquestrador), D6 (`campaign_spec`).
Output: 4 capacidades de texto executando via gateway, com `TextProviderResult`/prompts/parâmetros preservados e testes verdes.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-CONTEXT.md (F46-12..F46-16 :244-249; D4 :112-117; D5 :127-128)
@openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/specs/text-provider/spec.md (:5-70)
@openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/specs/ai-campaign-intelligence/spec.md (:5-53)
@openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/design.md (:124-136 — D4; :202-227 — Mapping)
@.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-01-SUMMARY.md (baseline/inventário)
@.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-02-PLAN.md (contrato do gateway)
@src/lib/copy/copy-director-service.ts (:51-111)
@src/lib/text-provider/{openai.ts,gemini.ts,factory.ts,types.ts}
@src/lib/campaign/correction-intent-service.ts (:155-334)
@src/lib/brand-assets/text-only-inference-service.ts (:45-180)
@src/lib/campaign-intelligence/providers/openai.ts (:18-158)
@src/app/api/campaign/generate-image/route.ts (:660-705 — gate de fallback de texto)

<interfaces>
- `TextProviderResult` (`src/lib/text-provider/types.ts`): `{ content: string; usage?: { promptTokens; completionTokens }; model: string }` — preservar.
- `TextProviderOptions`: `{ system?; temperature?; maxTokens?; signal? }` — preservar.
- `CopyDirectorService.generateCopy(input, options?, onCall?)` — assinatura preservada; passa a chamar o gateway.
- `CorrectionIntentService.analyzeReport(text, options)` + `recordCall` — preservar status/errorType (`json_parse_failed`/`schema_validation_failed`/`empty_response`).
- `BrandTextOnlyInferenceService.infer(input, timeoutMs, onCall)` — preservar.
- `OpenAIProvider.generate(input)` (legado) — preserva `CampaignSpecSchema`/Structured Outputs e o fallback `json_object` por erro de `response_format`/`json_schema`.
- Gate de fallback atual (`route.ts:684-700`): `isRetryableError(firstErr) && TEXT_FALLBACK_PROVIDER === "gemini"` → substituir por `target: "fallback"` sem conhecer provider.
</interfaces>
</context>

<execution_order>
**Onda 2 serial (`parallelization=false`):** executar **46-03 → 46-04 → 46-05**. Planos da mesma onda compartilham arquivos (`src/app/api/campaign/generate-image/__tests__/route.test.ts` com o 46-05) — execução sequencial evita conflito de edição. Não paralelizar planos da onda 2.
</execution_order>

<tasks>

<task type="auto">
  <name>Task 1: Migrar campaign_copy + fallback como segunda invoke explícita (3.1)</name>
  <files>src/lib/copy/copy-director-service.ts, src/lib/text-provider/openai.ts, src/lib/text-provider/gemini.ts, src/lib/text-provider/factory.ts, src/app/api/campaign/generate-image/route.ts</files>
  <read_first>
    - src/lib/copy/copy-director-service.ts (:51-137)
    - src/lib/text-provider/{openai.ts,gemini.ts,factory.ts,types.ts}
    - src/app/api/campaign/generate-image/route.ts (:660-705)
    - openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/specs/text-provider/spec.md (:7-20, :24-62)
    - design.md (:124-136 — D4)
  </read_first>
  <action>
    Migrar `campaign_copy` para o gateway: `CopyDirectorService.generateCopy` mantém a assinatura e o `TextProviderResult` (content/usage/model) e passa a executar via `invoke("campaign_copy", request, telemetry)` (adapter `chat-completions`, modelo do registry). Preservar `system`/`temperature: 0.7`/`maxTokens: 1000` e o `AbortSignal`. O `onCall` legado pode permanecer como adaptador fino do envelope, mas o modelo/usage vêm do resultado do gateway (modelo real).
    `OpenAITextProvider`/`GeminiTextProvider` deixam de instanciar `new OpenAI()`/`new GoogleGenerativeAI()` e de ler `OPENAI_TEXT_MODEL`/`GEMINI_TEXT_MODEL`/`GEMINI_MODEL`; delegam ao gateway (a implementação `OpenAITextProvider` usa o adapter `chat-completions`; `GeminiTextProvider` usa `gemini`). `createTextProvider(provider?)` remove a leitura de `TEXT_PROVIDER`/`TEXT_FALLBACK_PROVIDER` (provider padrão `openai`; `mock` continua).
    No orquestrador `route.ts:684-700`, substituir o gate `TEXT_FALLBACK_PROVIDER === "gemini"` por: quando `isRetryableError(firstErr)` e o registry tiver fallback configurado (≠ primary), fazer **segunda `invoke("campaign_copy", …, target: "fallback")` explícita**. Remover a menção a provider específico (nenhum `if provider === "gemini"`).
  </action>
  <verify>
    <automated>npx vitest run src/lib/copy/__tests__/copy-director-service.test.ts src/lib/text-provider/__tests__ 2>&1; if ($?) { Write-Output "OK" }</automated>
  </verify>
  <acceptance_criteria>
    - `copy-director-service.ts` chama `invoke("campaign_copy", …)` e preserva `TextProviderResult` e parâmetros
    - `text-provider/{openai,gemini,factory}.ts` não leem `OPENAI_TEXT_MODEL`/`GEMINI_TEXT_MODEL`/`GEMINI_MODEL`/`TEXT_PROVIDER`/`TEXT_FALLBACK_PROVIDER` nem instanciam providers
    - `route.ts` aciona `target: "fallback"` sem conhecer o provider (zero `TEXT_FALLBACK_PROVIDER`)
    - Testes de copy/text-provider verdes
  </acceptance_criteria>
  <done>`campaign_copy` via gateway com fallback como segunda invoke explícita</done>
</task>

<task type="auto">
  <name>Task 2: Migrar campaign_correction_analysis + brand_profile_text (3.2/3.3)</name>
  <files>src/lib/campaign/correction-intent-service.ts, src/lib/brand-assets/text-only-inference-service.ts</files>
  <read_first>
    - src/lib/campaign/correction-intent-service.ts (:155-334)
    - src/lib/brand-assets/text-only-inference-service.ts (:15-180)
    - openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/specs/ai-cost-accounting/spec.md (:7-27)
  </read_first>
  <action>
    Migrar `campaign_correction_analysis`: `CorrectionIntentService.analyzeReport` executa via `invoke("campaign_correction_analysis", …)` (adapter `chat-completions`), preservando `temperature: 0.2`/`maxTokens: 600`, o saneamento/delimitação do relato e a classificação de erro de domínio (`json_parse_failed`/`schema_validation_failed`/`empty_response`) — o erro de parsing continua classificado pelo orquestrador (não normalizado pelo gateway). A telemetria passa a vir do envelope (capability/protocol/status/errorType) com modelo real; manter `recordCall` coerente com o `AiCostEvent` existente.
    Migrar `brand_profile_text`: `BrandTextOnlyInferenceService.infer` executa via `invoke("brand_profile_text", …)`, preservando o comportamento (caminho mock em dev sem `OPENAI_API_KEY`, timeout, retorno) e a telemetria. Nenhuma leitura de `OPENAI_TEXT_ONLY_INFERENCE_MODEL`/`OPENAI_BRAND_DIRECTOR_MODEL`/`OPENAI_MODEL`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/campaign/__tests__/correction-intent-service.test.ts src/lib/brand-assets/__tests__/text-only-inference-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `correction-intent-service.ts` chama `invoke("campaign_correction_analysis", …)` preservando parâmetros e classificação de erro de domínio
    - `text-only-inference-service.ts` chama `invoke("brand_profile_text", …)` preservando o caminho mock dev e o timeout
    - Nenhuma leitura de env-var de modelo nos dois arquivos
    - Testes verdes
  </acceptance_criteria>
  <done>Correção e brand_profile_text via gateway, behavior-preserving</done>
</task>

<task type="auto">
  <name>Task 3: Migrar campaign_spec legado + fallback json_object como segunda invoke (3.4)</name>
  <files>src/lib/campaign-intelligence/providers/openai.ts</files>
  <read_first>
    - src/lib/campaign-intelligence/providers/openai.ts (:18-158)
    - openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/specs/ai-campaign-intelligence/spec.md (:5-53)
    - src/lib/ai/model-registry.ts (default campaign_spec gpt-4o-mini)
  </read_first>
  <action>
    Migrar o `OpenAIProvider` legado para `invoke("campaign_spec", …)` (adapter `chat-completions`, default `gpt-4o-mini`), preservando: `CampaignSpecSchema` via Structured Outputs (`json_schema`), a sobreposição de `generation_metadata` e a validação Zod. Remover a leitura de `OPENAI_MODEL` e a instanciação de `new OpenAI()`. O fallback `json_schema`→`json_object` deixa de ser retry interno do provider: o **orquestrador** refaz a chamada como **segunda `invoke("campaign_spec", …, request com responseFormat: "json_object")` explícita**, acionada **somente** quando o erro é de `capability` relacionado a `response_format`/`json_schema` (D4/D4.1). Preservar o contrato de saída (`ProviderRawResponse`/`CampaignSpec`). O módulo NÃO é removido (D6).
  </action>
  <verify>
    <automated>npx vitest run src/lib/campaign-intelligence 2>&1; if ($?) { Write-Output "OK" }</automated>
  </verify>
  <acceptance_criteria>
    - `campaign-intelligence/providers/openai.ts` chama `invoke("campaign_spec", …)` e não lê `OPENAI_MODEL` nem instancia `new OpenAI()`
    - Structured Outputs (`json_schema`) preservados; fallback `json_object` disponível só por erro de `capability` de `response_format`/`json_schema`
    - Módulo legado preservado (não removido)
    - Testes verdes
  </acceptance_criteria>
  <done>`campaign_spec` legado via gateway com fallback json_object explícito</done>
</task>

<task type="auto">
  <name>Task 4: Gates + co-migração dos testes de texto (3.5)</name>
  <files>src/lib/copy/__tests__/copy-director-service.test.ts, src/lib/campaign/__tests__/correction-intent-service.test.ts, src/lib/brand-assets/__tests__/text-only-inference-service.test.ts, src/app/api/campaign/generate-image/__tests__/route.test.ts</files>
  <read_first>
    - src/lib/copy/__tests__/copy-director-service.test.ts (:231-275 — onCall)
    - src/lib/brand-assets/__tests__/text-only-inference-service.test.ts (:46-107)
    - src/app/api/campaign/generate-image/__tests__/route.test.ts (:920-965 — TEXT_FALLBACK_PROVIDER; :1290-1530 — eventos)
    - .planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-01-SUMMARY.md (baseline de testes)
  </read_first>
  <action>
    Co-migrar os testes de texto sem mudar asserções de comportamento observável: adaptar mocks para o gateway (adapter falso + sink controlado) preservando os asserts de `TextProviderResult`/prompts/parâmetros/usage/modelo. No `route.test.ts`, substituir o setup de `TEXT_FALLBACK_PROVIDER` (:929/:960) pelo cenário de fallback configurado no registry (segunda `invoke(..., target: "fallback")`), mantendo o assert de que o fallback produz **dois envelopes** (primária + fallback). Rodar os 4 gates.
  </action>
  <verify>
    <automated>npx vitest run src/lib/copy src/lib/campaign src/lib/brand-assets src/app/api/campaign/generate-image; if ($?) { npm run typecheck; if ($?) { npm run lint; if ($?) { npm run build } } }</automated>
  </verify>
  <acceptance_criteria>
    - Testes de copy/correção/brand_profile_text/campaign_spec verdes
    - `route.test.ts` sem `TEXT_FALLBACK_PROVIDER`; fallback testado como `target: "fallback"` com dois envelopes
    - `npx vitest run`, `npm run typecheck`, `npm run lint`, `npm run build` → 4 gates verdes
  </acceptance_criteria>
  <done>Suites de texto co-migradas e 4 gates verdes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orquestrador ↔ gateway | Seleção explícita do alvo (`primary`/`fallback`) — sem conhecimento de provider |
| serviço ↔ provider | Prompts do lojista (conteúdo não confiável) cruzando para APIs externas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-03a | Spoofing | Fallback de texto | mitigate | Fallback acionado por `retryable === true` + alvo configurado ≠ primary; orquestrador não conhece provider (sem `if gemini`) |
| T-46-03b | Tampering | `correction-intent-service` | mitigate | Erro de parsing do domínio permanece classificado pelo orquestrador; gateway não normaliza `MalformedResponseError` |
| T-46-03c | Information Disclosure | Prompt legado `campaign_spec` | accept | Prompt de negócio já existente; sem PII nova além do já enviado |
| T-46-03d | Denial of Service | Fallback `json_object` | mitigate | Segunda invoke só por erro `capability` de `response_format`/`json_schema` — não em auth/rate limit |
| T-46-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo |
</threat_model>

<verification>
- `npx vitest run src/lib/copy src/lib/campaign src/lib/brand-assets src/app/api/campaign/generate-image` → verde
- Grep: zero `new OpenAI()`/`new GoogleGenerativeAI()` nos serviços de texto migrados; zero `OPENAI_TEXT_MODEL`/`OPENAI_MODEL`/`GEMINI_TEXT_MODEL`/`GEMINI_MODEL`/`TEXT_PROVIDER`/`TEXT_FALLBACK_PROVIDER`
- `npm run typecheck`, `npm run lint`, `npm run build` → 0 erros
</verification>

<success_criteria>
- `campaign_copy`, `campaign_correction_analysis`, `brand_profile_text`, `campaign_spec` executam via gateway
- Fallback de texto e `json_object` são segundas invokes explícitas do orquestrador
- `TextProviderResult`, prompts, parâmetros e structured outputs preservados
- 4 gates verdes
</success_criteria>

<output>
Create `.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-03-SUMMARY.md` when done
</output>

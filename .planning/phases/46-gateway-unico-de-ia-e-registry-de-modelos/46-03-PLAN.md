---
phase: 46-gateway-unico-de-ia-e-registry-de-modelos
plan: 03
type: execute
wave: 3
depends_on: [46-02]
files_modified:
  - src/lib/copy/copy-director-service.ts
  - src/lib/text-provider/openai.ts
  - src/lib/text-provider/gemini.ts
  - src/lib/text-provider/factory.ts
  - src/lib/campaign/correction-intent-service.ts
  - src/lib/brand-assets/text-only-inference-service.ts
  - src/lib/campaign-intelligence/providers/openai.ts
  - src/lib/campaign-intelligence/service.ts
  - src/app/api/campaign/generate/route.ts
  - src/app/api/campaign/generate-image/route.ts
  - src/app/api/campaign/[id]/problem-report/route.ts
  - src/app/api/store/[id]/brand-profile/infer/route.ts
  - src/lib/copy/__tests__/copy-director-service.test.ts
  - src/lib/campaign/__tests__/correction-intent-service.test.ts
  - src/lib/brand-assets/__tests__/text-only-inference-service.test.ts
  - src/app/api/campaign/generate-image/__tests__/route.test.ts
  - src/lib/campaign-intelligence/__tests__/campaign-spec.test.ts
  - src/app/api/campaign/generate/__tests__/route.test.ts
autonomous: true
requirements: [F46-12, F46-13, F46-14, F46-15, F46-16]

must_haves:
  truths:
    - "`CopyDirectorService` é o **dono único** de `invoke(\"campaign_copy\", …)` e recebe um `AiInvoker`/gateway como **seam de teste**; preserva `TextProviderResult` (content/usage/model) e parâmetros (system/temperature 0.7/maxTokens 1000)"
    - "`generateCopy` aceita `target`/`telemetry` nas **options** (aditivos); o fallback é a **rota** chamando o serviço de novo com `target: \"fallback\"` quando `retryable === true` e há fallback configurado (≠ primary) — a rota **não** chama `invoke` e não conhece provider (sem `if provider === \"gemini\"`)"
    - "`createTextProvider` é removido de **todos** os callers produtivos (rota `generate-image`, `CorrectionIntentService`); fachadas remanescentes exigem `AiTelemetryContext` injetado e nunca instanciam `NoopAiTelemetrySink` internamente"
    - "`campaign_correction_analysis` e `brand_profile_text` executam via gateway preservando comportamento e telemetria (status/errorType); as rotas `problem-report` e `brand-profile/infer` fornecem `AiTelemetryContext`/sink"
    - "`campaign_spec` legado executa via `invoke(\"campaign_spec\", …)` com default `gpt-4o-mini` e contexto `AiCostTracker.startRun(\"campaign_delivery\")` (`attemptNumber` 1 no primary, 2 no fallback `json_object`); o fallback `json_schema`→`json_object` é segunda `invoke` explícita só por erro `capability` de `response_format`/`json_schema`"
    - "Nenhum serviço de texto instancia `new OpenAI()`/`new GoogleGenerativeAI()` nem lê env-var de modelo"
    - "A acumulação de custo usa o `onCostResolved` do sink (mesmo `CostResolution`, sem 2ª `resolveAiCost`); a soma `callCostSum` é **híbrida** nesta fase (sink + `recordCall` residual)"
    - "Cada tentativa real (primária e fallback) gera o seu próprio envelope de telemetria"
  artifacts:
    - path: "src/lib/copy/copy-director-service.ts"
      provides: "campaign_copy via gateway (dono único; AiInvoker seam; options target/telemetry)"
      contains: "invoke"
    - path: "src/lib/text-provider/openai.ts"
      provides: "OpenAITextProvider sem SDK (fachada delegando ao gateway)"
      contains: "chat-completions"
    - path: "src/lib/text-provider/factory.ts"
      provides: "createTextProvider sem TEXT_PROVIDER/TEXT_FALLBACK_PROVIDER (fora do caminho produtivo)"
      contains: "createTextProvider"
    - path: "src/lib/campaign/correction-intent-service.ts"
      provides: "campaign_correction_analysis via gateway (sem createTextProvider)"
      contains: "campaign_correction_analysis"
    - path: "src/lib/brand-assets/text-only-inference-service.ts"
      provides: "brand_profile_text via gateway"
      contains: "brand_profile_text"
    - path: "src/lib/campaign-intelligence/providers/openai.ts"
      provides: "campaign_spec legado via gateway"
      contains: "campaign_spec"
    - path: "src/app/api/campaign/generate/route.ts"
      provides: "Contexto de telemetria (startRun campaign_delivery) da rota legada encaminhado ao invoke"
      contains: "telemetry"
    - path: "src/app/api/campaign/generate-image/route.ts"
      provides: "Fallback de texto chamando o serviço com target: fallback (rota não chama invoke)"
      contains: "target"
    - path: "src/lib/campaign-intelligence/__tests__/campaign-spec.test.ts"
      provides: "Testes reais de campaign_spec (Structured Outputs + fallback json_object)"
      contains: "campaign_spec"
    - path: "src/app/api/campaign/generate/__tests__/route.test.ts"
      provides: "Teste dedicado de POST /api/campaign/generate"
      contains: "campaign_spec"
  key_links:
    - from: "src/lib/copy/copy-director-service.ts"
      to: "src/lib/ai/gateway.ts"
      via: "invoke(\"campaign_copy\") via AiInvoker injetado (seam de teste)"
      pattern: "invoke"
    - from: "src/app/api/campaign/generate-image/route.ts"
      to: "src/lib/copy/copy-director-service.ts"
      via: "segunda chamada ao serviço com target: \"fallback\" (rota não chama invoke; sem conhecer provider)"
      pattern: "target: \"fallback\""
    - from: "src/lib/campaign-intelligence/providers/openai.ts"
      to: "src/lib/ai/model-registry.ts"
      via: "campaign_spec (gpt-4o-mini)"
      pattern: "campaign_spec"
---

<objective>
Migrar as **capacidades de TEXTO** para a camada única, uma a uma, preservando comportamento: (3.1) `campaign_copy` (`copy-director-service.ts` + `text-provider/openai.ts`/`gemini.ts` + `factory.ts`) — `CopyDirectorService` é o **dono único** de `invoke("campaign_copy")` e recebe um **`AiInvoker`/gateway como seam de teste**; `generateCopy` aceita `target`/`telemetry` nas **options** e o fallback é a **rota chamando o serviço de novo com `target: "fallback"`** (a rota **não** chama `invoke`); **remover `createTextProvider` de todos os callers produtivos**; (3.2) `campaign_correction_analysis` (`correction-intent-service.ts`) sem `createTextProvider`; (3.3) `brand_profile_text` (`text-only-inference-service.ts`); (3.4) `campaign_spec` legado (`campaign-intelligence/providers/openai.ts` + `service.ts` + rota) com contexto `startRun("campaign_delivery")` (`attemptNumber` 1 primary / 2 fallback `json_object`) e **testes reais**; (3.5) gates + co-migração dos testes de texto.

Purpose: D5 (migração incremental behavior-preserving), D2/D4 (alvo explícito; fallback do orquestrador), D6 (`campaign_spec`), D9 (sink único; acumulação via `onCostResolved` sem 2ª `resolveAiCost`).
Output: 4 capacidades de texto executando via gateway, com `TextProviderResult`/prompts/parâmetros preservados, testes reais (incl. `/api/campaign/generate`) e gates verdes.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-CONTEXT.md (F46-12..F46-16 :244-249; D4 :112-117; D5 :127-128)
@openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/specs/text-provider/spec.md (:5-70)
@openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/specs/ai-campaign-intelligence/spec.md (:5-53)
@openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/design.md (:124-136 — D4; :202-227 — Mapping)
@.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-01-SUMMARY.md (baseline/inventário)
@.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-02-PLAN.md (contrato do gateway)
@src/lib/copy/copy-director-service.ts (:51-111)
@src/lib/text-provider/{openai.ts,gemini.ts,factory.ts,types.ts}
@src/lib/campaign/correction-intent-service.ts (:155-334)
@src/lib/brand-assets/text-only-inference-service.ts (:45-180)
@src/lib/campaign-intelligence/providers/openai.ts (:18-158)
@src/app/api/campaign/generate-image/route.ts (:660-705 — gate de fallback de texto)

<interfaces>
- `TextProviderResult` (`src/lib/text-provider/types.ts`): `{ content: string; usage?: { promptTokens; completionTokens }; model: string }` — preservar.
- `TextProviderOptions`: `{ system?; temperature?; maxTokens?; signal? }` — preservar.
- `AiInvoker` (seam de teste): `{ invoke(capability, request, telemetry, target?): Promise<AiInvocationResult> }` — o `AiGateway`/`invoke` de `src/lib/ai` satisfaz. `CopyDirectorService` recebe-o por construtor (default = gateway padrão); testes injetam um fake.
- `CopyDirectorService.generateCopy(input, options?, onCall?)` — assinatura preservada; **`options` ganha `target?: "primary" | "fallback"` e `telemetry?: AiTelemetryContext`** (aditivos); o serviço chama `invoke("campaign_copy", …)` (dono único) e o `onCall`, se mantido, só recebe o envelope já produzido.
- `CorrectionIntentService.analyzeReport(text, options, telemetry?)` — preservar status/errorType (`json_parse_failed`/`schema_validation_failed`/`empty_response`); **sem `createTextProvider`**.
- `BrandTextOnlyInferenceService.infer(input, timeoutMs, onCall, telemetry?)` — preservar.
- `OpenAIProvider.generate(input, telemetry?)` (legado) — preserva `CampaignSpecSchema`/Structured Outputs e o fallback `json_object` por erro de `response_format`/`json_schema`; recebe o contexto por `campaign-intelligence/service.ts`.
- Contexto `campaign_spec`: `AiCostTracker.startRun("campaign_delivery")` (não há `OperationRunType.campaign_spec`), `attemptNumber` 1 (primary) / 2 (fallback `json_object`).
- Sink: `onCostResolved(cost)` entrega o **mesmo `CostResolution`** persistido — acumulação híbrida no `callCostSum` sem 2ª `resolveAiCost`.
- Gate de fallback atual (`route.ts:684-700`): `isRetryableError(firstErr) && TEXT_FALLBACK_PROVIDER === "gemini"` → substituir por nova chamada ao serviço com `target: "fallback"` (sem conhecer provider).
</interfaces>
</context>

<execution_order>
**DAG (legível por ferramenta):** `depends_on: [46-02]` (onda 3). **46-04 depende de 46-03** (`depends_on: [46-03]`, onda 4) porque ambos tocam `generate-image/route.ts` (remoção da persistência manual por capacidade); **46-05 depende de 46-04** (onda 5). Cadeia serial **46-03 → 46-04 → 46-05**.
</execution_order>

<tasks>

<task type="auto">
  <name>Task 1: Migrar campaign_copy + fallback como segunda invoke explícita (3.1)</name>
  <files>src/lib/copy/copy-director-service.ts, src/lib/text-provider/openai.ts, src/lib/text-provider/gemini.ts, src/lib/text-provider/factory.ts, src/app/api/campaign/generate-image/route.ts</files>
  <read_first>
    - src/lib/copy/copy-director-service.ts (:51-137)
    - src/lib/text-provider/{openai.ts,gemini.ts,factory.ts,types.ts}
    - src/app/api/campaign/generate-image/route.ts (:660-705)
    - openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/specs/text-provider/spec.md (:7-20, :24-62)
    - design.md (:124-136 — D4)
  </read_first>
  <action>
    **Proprietário único + seam de teste:** `CopyDirectorService` é o **único** que chama `invoke("campaign_copy", request, telemetry, target)` (adapter `chat-completions`, modelo do registry). O serviço recebe um **`AiInvoker`** (gateway) por construtor (default = `invoke`/`defaultAiGateway` de `src/lib/ai`); testes injetam um fake. Preservar `TextProviderResult` (content/usage/model) e os parâmetros (`system`/`temperature: 0.7`/`maxTokens: 1000`/`AbortSignal`). O caller fornece o `AiTelemetryContext`; o gateway gera o envelope; o sink persiste (D9). O `onCall` legado, se mantido, é **apenas** um adaptador fino que recebe o envelope já produzido — não resolve custo nem grava.
    **Options (A):** `generateCopy(input, options?, onCall?)` com `options.target?: "primary" | "fallback"` e `options.telemetry?: AiTelemetryContext` (aditivos). O serviço repassa `target`/`telemetry` ao `invoke`.
    **Fachadas (G):** `OpenAITextProvider`/`GeminiTextProvider` deixam de instanciar `new OpenAI()`/`new GoogleGenerativeAI()` e de ler `OPENAI_TEXT_MODEL`/`GEMINI_TEXT_MODEL`/`GEMINI_MODEL`; viram fachadas que exigem **`AiTelemetryContext` injetado** para delegar ao gateway. **PROIBIDO** instanciar `NoopAiTelemetrySink` internamente (no-op só em teste). Remover as fachadas que ficarem sem consumidores.
    **`createTextProvider` fora do caminho produtivo (B):** remover o uso em **todos** os callers produtivos (`generate-image/route.ts`, `correction-intent-service.ts`). `createTextProvider(provider?)` remove a leitura de `TEXT_PROVIDER`/`TEXT_FALLBACK_PROVIDER` (provider padrão `openai`; `mock` continua) e permanece apenas para compat/teste.
    **Fallback (A/B):** no orquestrador `route.ts:684-700`, substituir o gate `TEXT_FALLBACK_PROVIDER === "gemini"` por: quando `isRetryableError(firstErr)` e houver fallback configurado (≠ primary), a rota chama **o serviço de novo** com `target: "fallback"` (a rota **não** chama `invoke` diretamente; nenhum `if provider === "gemini"`). A rota remove o uso de `createTextProvider("openai"/"gemini")` e constrói o `CopyDirectorService` com o invoker padrão.
    **Persistência/acumulação (C):** no `generate-image/route.ts`, o `recordCall` de `campaign_copy` é **removido**; a rota cria o `AiTelemetryContext` com `sink` padrão cujo `onCostResolved` acumula o custo no `callCostSum` (mesmo `CostResolution`, **sem** segunda `resolveAiCost`). Preservar a **soma híbrida** durante 46-03/04 (o `recordCall` residual de validação/review/imagem continua somando). Preservar o **delivery marker** (`generationType: "campaign_pipeline"`, sem custo/tokens). Aceitação por grep: nenhum `resolveAiCost` fora do `onCostResolved` do sink e nenhum `AiCostTracker.record` manual para `campaign_copy`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/copy/__tests__/copy-director-service.test.ts src/lib/text-provider/__tests__ 2>&1; if ($?) { Write-Output "OK" }</automated>
  </verify>
  <acceptance_criteria>
    - `copy-director-service.ts` chama `invoke("campaign_copy", …)` (dono único) via `AiInvoker` injetado e preserva `TextProviderResult`/parâmetros
    - `generateCopy` aceita `target`/`telemetry` nas options; o fallback é a rota chamando o serviço de novo com `target: "fallback"` (a rota não chama `invoke`)
    - `text-provider/{openai,gemini,factory}.ts` não leem env de modelo nem instanciam providers; fachadas sem `NoopAiTelemetrySink` interno; `createTextProvider` sem uso produtivo
    - `route.ts` aciona `target: "fallback"` sem conhecer o provider (zero `TEXT_FALLBACK_PROVIDER`; zero `createTextProvider`)
    - `generate-image/route.ts` sem `AiCostTracker.record` manual para `campaign_copy`; acumulação via `onCostResolved` (soma híbrida); delivery marker (`campaign_pipeline`) preservado
    - Testes de copy/text-provider verdes
  </acceptance_criteria>
  <done>`campaign_copy` via gateway; fallback = serviço com target "fallback"; soma híbrida via sink</done>
</task>

<task type="auto">
  <name>Task 2: Migrar campaign_correction_analysis + brand_profile_text (3.2/3.3)</name>
  <files>src/lib/campaign/correction-intent-service.ts, src/app/api/campaign/[id]/problem-report/route.ts, src/lib/brand-assets/text-only-inference-service.ts, src/app/api/store/[id]/brand-profile/infer/route.ts</files>
  <read_first>
    - src/lib/campaign/correction-intent-service.ts (:155-334, :291-331 — recordCall manual)
    - src/app/api/campaign/[id]/problem-report/route.ts (:160-200 — call site do service)
    - src/lib/brand-assets/text-only-inference-service.ts (:15-180)
    - src/app/api/store/[id]/brand-profile/infer/route.ts (:70-160 — run + resolveAiCost + record manuais)
    - openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/specs/ai-cost-accounting/spec.md (:7-27)
  </read_first>
  <action>
    Migrar `campaign_correction_analysis`: `CorrectionIntentService.analyzeReport` executa via `invoke("campaign_correction_analysis", …)` (adapter `chat-completions`), preservando `temperature: 0.2`/`maxTokens: 600`, o saneamento/delimitação do relato e a classificação de erro de domínio (`json_parse_failed`/`schema_validation_failed`/`empty_response`) — o erro de parsing continua classificado pelo orquestrador (não normalizado pelo gateway). A telemetria passa a vir do envelope (capability/protocol/status/errorType) com modelo real. **Remover a persistência manual (D9):** o `recordCall` do service (:291-331, `resolveAiCost` + `AiCostTracker.record`) é removido; a rota `POST /api/campaign/[id]/problem-report` cria/fornece o `AiTelemetryContext` (run + sink) e o encaminha ao service até o `invoke` — a persistência é do sink. Preservar o comportamento de negócio do fluxo de análise.
    Migrar `brand_profile_text`: `BrandTextOnlyInferenceService.infer` executa via `invoke("brand_profile_text", …)`, preservando o comportamento (caminho mock em dev sem `OPENAI_API_KEY`, timeout, retorno) e a telemetria pelo mesmo caminho (contexto/sink do caller). **Remover a persistência manual (D9):** em `brand-profile/infer/route.ts`, os `resolveAiCost` (:120) + `AiCostTracker.record` (:126) de `brand_profile_text` são removidos; a rota injeta o sink no `AiTelemetryContext` e o encaminha ao `infer` (o delivery marker, se houver, é preservado). Nenhuma leitura de `OPENAI_TEXT_ONLY_INFERENCE_MODEL`/`OPENAI_BRAND_DIRECTOR_MODEL`/`OPENAI_MODEL`.
    **Threading (D9):** adicionar um parâmetro opcional `telemetry?: AiTelemetryContext` às assinaturas dos serviços migrados (aditivo, retrocompatível); o caller o fornece e o serviço o repassa a `invoke`. O `onCall` legado permanece opcional e, quando fornecido, recebe o envelope já produzido (não conduz a telemetria de produção).
  </action>
  <verify>
    <automated>npx vitest run src/lib/campaign/__tests__/correction-intent-service.test.ts src/lib/brand-assets/__tests__/text-only-inference-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `correction-intent-service.ts` chama `invoke("campaign_correction_analysis", …)`, **sem `createTextProvider`**, preservando parâmetros e classificação de erro de domínio
    - `problem-report/route.ts` fornece `AiTelemetryContext` (run/sink) ao service; `recordCall` manual removido (`resolveAiCost`/`AiCostTracker.record` ausentes no service)
    - `brand-profile/infer/route.ts` fornece `AiTelemetryContext`/sink ao `infer`; `resolveAiCost`/`AiCostTracker.record` manuais de `brand_profile_text` removidos
    - `text-only-inference-service.ts` chama `invoke("brand_profile_text", …)` preservando o caminho mock dev e o timeout
    - Nenhuma leitura de env-var de modelo nem `createTextProvider` produtivo nos arquivos migrados
    - Testes verdes
  </acceptance_criteria>
  <done>Correção e brand_profile_text via gateway, behavior-preserving</done>
</task>

<task type="auto">
  <name>Task 3: Migrar campaign_spec legado (provider + service + rota) + fallback json_object como segunda invoke (3.4)</name>
  <files>src/lib/campaign-intelligence/providers/openai.ts, src/lib/campaign-intelligence/service.ts, src/app/api/campaign/generate/route.ts</files>
  <read_first>
    - src/lib/campaign-intelligence/providers/openai.ts (:18-158)
    - src/lib/campaign-intelligence/service.ts (:130-160 — seleção dinâmica do provider)
    - src/app/api/campaign/generate/route.ts (fluxo da rota)
    - openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/specs/ai-campaign-intelligence/spec.md (:5-53)
    - src/lib/ai/model-registry.ts (default campaign_spec gpt-4o-mini)
  </read_first>
  <action>
    Migrar o `OpenAIProvider` legado para `invoke("campaign_spec", …)` (adapter `chat-completions`, default `gpt-4o-mini`), preservando: `CampaignSpecSchema` via Structured Outputs (`json_schema`), a sobreposição de `generation_metadata` e a validação Zod. Remover a leitura de `OPENAI_MODEL` e a instanciação de `new OpenAI()`.
    **Contexto de telemetria (D9):** `POST /api/campaign/generate` SHALL criar o `AiTelemetryContext` (run/store) e **encaminhá-lo** por `campaign-intelligence/service.ts` até o provider, que o repassa a `invoke("campaign_spec", request, telemetry)`. Nenhum caminho produtivo desta rota pode chamar IA sem contexto; o gateway gera o envelope e o sink persiste (a rota não grava manualmente).
    O fallback `json_schema`→`json_object` deixa de ser retry interno do provider: o **orquestrador** (rota/serviço) refaz a chamada como **segunda `invoke("campaign_spec", …, request com responseFormat: "json_object")` explícita**, acionada **somente** quando o erro é de `capability` relacionado a `response_format`/`json_schema` (D4/D4.1). Preservar o contrato de saída (`ProviderRawResponse`/`CampaignSpec`). O módulo NÃO é removido (D6).
  </action>
  <verify>
    <automated>npx vitest run src/lib/campaign-intelligence src/app/api/campaign/generate 2>&1; if ($?) { Write-Output "OK" }</automated>
  </verify>
  <acceptance_criteria>
    - `campaign-intelligence/providers/openai.ts` chama `invoke("campaign_spec", …)` e não lê `OPENAI_MODEL` nem instancia `new OpenAI()`
    - `POST /api/campaign/generate` cria/encaminha o `AiTelemetryContext` (`AiCostTracker.startRun("campaign_delivery")`; `attemptNumber` 1 no primary e 2 no fallback `json_object`) por `campaign-intelligence/service.ts` até o `invoke("campaign_spec", …)` — sem IA sem contexto
    - Structured Outputs (`json_schema`) preservados; fallback `json_object` disponível só por erro de `capability` de `response_format`/`json_schema`
    - **Testes reais** verdes: `campaign-intelligence/__tests__/campaign-spec.test.ts` e `app/api/campaign/generate/__tests__/route.test.ts` (sem `--passWithNoTests`)
    - Módulo legado preservado (não removido)
  </acceptance_criteria>
  <done>`campaign_spec` legado via gateway com fallback json_object explícito e testes reais</done>
</task>

<task type="auto">
  <name>Task 4: Gates + co-migração dos testes de texto (3.5)</name>
  <files>src/lib/copy/__tests__/copy-director-service.test.ts, src/lib/campaign/__tests__/correction-intent-service.test.ts, src/lib/brand-assets/__tests__/text-only-inference-service.test.ts, src/app/api/campaign/generate-image/__tests__/route.test.ts, src/lib/campaign-intelligence/__tests__/campaign-spec.test.ts, src/app/api/campaign/generate/__tests__/route.test.ts</files>
  <read_first>
    - src/lib/copy/__tests__/copy-director-service.test.ts (:231-275 — onCall)
    - src/lib/brand-assets/__tests__/text-only-inference-service.test.ts (:46-107)
    - src/app/api/campaign/generate-image/__tests__/route.test.ts (:920-965 — TEXT_FALLBACK_PROVIDER; :1290-1530 — eventos)
    - .planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-01-SUMMARY.md (baseline de testes)
  </read_first>
  <action>
    Co-migrar os testes de texto sem mudar asserções de comportamento observável. O teste do `CopyDirectorService` injeta um **`AiInvoker` fake** (gateway/adapter/sink falsos) e assere **dois envelopes** (primary + fallback) preservando `TextProviderResult`/prompts/parâmetros/usage/modelo. No `route.test.ts` da `generate-image`, substituir o setup de `TEXT_FALLBACK_PROVIDER` (:929/:960) por mocks do serviço que registram as chamadas, asserindo que a rota chama o serviço com `target: "primary"` e, no erro retryable, com `target: "fallback"` (a rota **não** chama `invoke`; sem dois envelopes na rota — a asserção de dois envelopes fica no teste do serviço). Criar os **testes reais** de `campaign_spec` (`campaign-spec.test.ts`: Structured Outputs + fallback `json_object` por erro `capability`) e o teste dedicado de `POST /api/campaign/generate`. Rodar os 4 gates.
  </action>
  <verify>
    <automated>npx vitest run src/lib/copy src/lib/campaign src/lib/brand-assets src/lib/campaign-intelligence src/app/api/campaign/generate-image src/app/api/campaign/generate; if ($?) { npm run typecheck; if ($?) { npm run lint; if ($?) { npm run build } } }</automated>
  </verify>
  <acceptance_criteria>
    - Testes de copy/correção/brand_profile_text/campaign_spec verdes
    - `route.test.ts` sem `TEXT_FALLBACK_PROVIDER`; a rota é testada chamando o serviço com `target` primary→fallback
    - O teste do `CopyDirectorService` assere **dois envelopes** (gateway/adapter/sink falsos)
    - **Testes reais** de `campaign_spec` e de `/api/campaign/generate` presentes e verdes (sem `--passWithNoTests`)
    - `npx vitest run`, `npm run typecheck`, `npm run lint`, `npm run build` → 4 gates verdes
  </acceptance_criteria>
  <done>Suites de texto co-migradas e 4 gates verdes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orquestrador ↔ gateway | Seleção explícita do alvo (`primary`/`fallback`) — sem conhecimento de provider |
| serviço ↔ provider | Prompts do lojista (conteúdo não confiável) cruzando para APIs externas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-03a | Spoofing | Fallback de texto | mitigate | Fallback acionado por `retryable === true` + alvo configurado ≠ primary; orquestrador não conhece provider (sem `if gemini`) |
| T-46-03b | Tampering | `correction-intent-service` | mitigate | Erro de parsing do domínio permanece classificado pelo orquestrador; gateway não normaliza `MalformedResponseError` |
| T-46-03c | Information Disclosure | Prompt legado `campaign_spec` | accept | Prompt de negócio já existente; sem PII nova além do já enviado |
| T-46-03d | Denial of Service | Fallback `json_object` | mitigate | Segunda invoke só por erro `capability` de `response_format`/`json_schema` — não em auth/rate limit |
| T-46-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo |
</threat_model>

<verification>
- `npx vitest run src/lib/copy src/lib/campaign src/lib/brand-assets src/lib/campaign-intelligence src/app/api/campaign/generate-image src/app/api/campaign/generate` → verde
- Grep: zero `new OpenAI()`/`new GoogleGenerativeAI()` nos serviços de texto migrados; zero `OPENAI_TEXT_MODEL`/`OPENAI_MODEL`/`GEMINI_TEXT_MODEL`/`GEMINI_MODEL`/`TEXT_PROVIDER`/`TEXT_FALLBACK_PROVIDER`
- Grep: zero `createTextProvider` em callers produtivos; zero `NoopAiTelemetrySink` fora de testes
- Grep: `CopyDirectorService` é o dono único de `invoke("campaign_copy")`; a rota chama o serviço com `target` (não chama `invoke`)
- Acumulação: `callCostSum` via `onCostResolved` (sem segunda `resolveAiCost`); soma híbrida preservada
- `npm run typecheck`, `npm run lint`, `npm run build` → 0 erros
</verification>

<success_criteria>
- `campaign_copy`, `campaign_correction_analysis`, `brand_profile_text`, `campaign_spec` executam via gateway
- Fallback de texto é a rota chamando o serviço com `target: "fallback"`; `json_object` é segunda invoke explícita do orquestrador
- `createTextProvider` fora do caminho produtivo; fachadas sem `NoopAiTelemetrySink` interno
- `campaign_spec` com contexto `campaign_delivery` (attempt 1/2) e testes reais (`campaign-spec.test.ts` + `generate/route.test.ts`)
- `TextProviderResult`, prompts, parâmetros e structured outputs preservados; soma híbrida de custo
- 4 gates verdes
</success_criteria>

> **Revisões (2026-09-12 — pós-46-02, aprovadas):** A) fallback via serviço com `target`; B) `CopyDirectorService` dono único com seam `AiInvoker` e `createTextProvider` fora do produtivo; C) acumulação via `onCostResolved` (soma híbrida, sem 2ª `resolveAiCost`); D) `campaign_spec` com `startRun("campaign_delivery")` e attempt 1/2; E) testes reais de `campaign_spec` e da rota legada; F) `route.test.ts` testa primary→fallback e o serviço testa dois envelopes; G) proibido `NoopAiTelemetrySink` interno em produção. Correções de sink correspondentes no 46-02 (`ffd90d76`).

<output>
Create `.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-03-SUMMARY.md` when done
</output>

---
phase: 46-gateway-unico-de-ia-e-registry-de-modelos
plan: 04
type: execute
wave: 2
depends_on: [46-01, 46-02]
files_modified:
  - src/lib/image-generation/services/input-validation-service.ts
  - src/lib/image-generation/services/image-review-service.ts
  - src/lib/image-generation/services/image-generation-service.ts
  - src/lib/brand-assets/brand-director.ts
  - src/lib/visual-signature/brand-profiler.ts
  - src/lib/visual-signature/ai-image-generator.ts
  - src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts
  - src/lib/image-generation/services/__tests__/input-validation-service.test.ts
  - src/lib/image-generation/services/__tests__/image-review-service.test.ts
  - src/lib/image-generation/services/__tests__/image-generation-service.test.ts
  - src/lib/brand-assets/__tests__/brand-director.test.ts
  - src/lib/visual-signature/__tests__/brand-profiler.test.ts
autonomous: true
requirements: [F46-17, F46-18, F46-19, F46-20, F46-21]

must_haves:
  truths:
    - "`campaign_input_validation` e `campaign_image_review` executam via gateway e registram o **modelo real de visão** (`gpt-4o`), nunca o modelo de imagem"
    - "O furo 1 é corrigido: `emitMetricsEvent` deixa de hardcodar `IMAGE_GENERATION_RESPONSES_MODEL`; validação/revisão reportam o modelo de visão real"
    - "`brand_profile_vision` executa via gateway em `brand-profiler.ts` e `brand-director.ts`, preservando prompts/parâmetros (`json_object`, detail/maxTokens)"
    - "`visual_signature_validation` executa via gateway com default `gpt-4o-mini` e protocolo `responses`"
    - "Nenhum serviço de visão instancia `new OpenAI()` nem lê `VISION_REVIEW_MODEL`/`IMAGE_VALIDATION_MODEL`/`OPENAI_BRAND_DIRECTOR_MODEL`"
    - "O custo das chamadas de visão usa o pricing do modelo real (não o do modelo de imagem)"
  artifacts:
    - path: "src/lib/image-generation/services/input-validation-service.ts"
      provides: "campaign_input_validation via gateway"
      contains: "campaign_input_validation"
    - path: "src/lib/image-generation/services/image-review-service.ts"
      provides: "campaign_image_review via gateway"
      contains: "campaign_image_review"
    - path: "src/lib/image-generation/services/image-generation-service.ts"
      provides: "emitMetricsEvent sem hardcode de modelo de imagem"
      contains: "emitMetricsEvent"
    - path: "src/lib/brand-assets/brand-director.ts"
      provides: "brand_profile_vision via gateway"
      contains: "brand_profile_vision"
    - path: "src/lib/visual-signature/brand-profiler.ts"
      provides: "brand_profile_vision via gateway"
      contains: "brand_profile_vision"
    - path: "src/lib/visual-signature/ai-image-generator.ts"
      provides: "visual_signature_validation via gateway"
      contains: "visual_signature_validation"
  key_links:
    - from: "src/lib/image-generation/services/image-generation-service.ts"
      to: "src/lib/ai-cost/types.ts"
      via: "envelope com modelo real da chamada de visão"
      pattern: "capability"
    - from: "src/lib/visual-signature/ai-image-generator.ts"
      to: "src/lib/ai/model-registry.ts"
      via: "visual_signature_validation (gpt-4o-mini, responses)"
      pattern: "visual_signature_validation"
---

<objective>
Migrar as **capacidades de VISÃO** para a camada única, preservando comportamento e corrigindo o furo 1: (4.1) `campaign_input_validation` (`input-validation-service.ts`) e `campaign_image_review` (`image-review-service.ts`) via gateway com **modelo real** na telemetria; (4.2) `brand_profile_vision` (`brand-profiler.ts`, `brand-director.ts`); (4.3) `visual_signature_validation` (validator de `ai-image-generator.ts`); (4.4) corrigir o furo 1 (`emitMetricsEvent` deixa de hardcodar o modelo de imagem); (4.5) gates + co-migração dos testes de visão.

Purpose: D5 (behavior-preserving), D7.1 (modelo real — corrige validation/review), F46-20.
Output: 4 capacidades de visão via gateway, telemetria com modelo real, testes verdes.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-CONTEXT.md (F46-17..F46-21 :251-256; D7 :133-135)
@openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/specs/ai-cost-accounting/spec.md (:7-43)
@openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/design.md (:175-184 — D7; furo 1 :15)
@.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-01-SUMMARY.md (furo 1 em image-generation-service.ts:135-142)
@.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-02-PLAN.md (contrato do gateway)
@src/lib/image-generation/services/input-validation-service.ts (:19-140)
@src/lib/image-generation/services/image-review-service.ts (:36-120, :346-390)
@src/lib/image-generation/services/image-generation-service.ts (:135-156, :224-241, :469-471)
@src/lib/brand-assets/brand-director.ts (:308-490)
@src/lib/visual-signature/brand-profiler.ts (:121-130, :233-300, :616-770)
@src/lib/visual-signature/ai-image-generator.ts (:22-158)
@src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts (:330-340)

<interfaces>
- `InputValidationService` (`constructor(promptLoader?, model?)` default `VISION_REVIEW_MODEL`; `validate(productName, imageDataUrl, override?, onCall?)`) — preservar a assinatura e o resultado tipado.
- `ImageReviewService` (`constructor(promptLoader?, model?)` default `VISION_REVIEW_MODEL`; `review(imageDataUrl, input, referenceImages?, onCall?)`) — preservar.
- `ImageGenerationService.generateImage(...)` — `emitMetricsEvent(phase, attempt, extra)` deixa de usar `IMAGE_GENERATION_RESPONSES_MODEL` como modelo de validação/revisão; o modelo vem do envelope (capability `campaign_input_validation`/`campaign_image_review`).
- `BrandDirectorService.analyze(params)` (`onCall?`) e `BrandProfilerWithoutLogoService.generate(input)` (`input.onCall?`) — preservar; `callVision`/`callVisionFull` usam `json_object`.
- `VisualSignatureValidator.validate(params)` — `visual_signature_validation` via gateway.
</interfaces>
</context>

<execution_order>
**Onda 2 serial (`parallelization=false`):** executar **46-03 → 46-04 → 46-05**. Planos da mesma onda compartilham arquivos (`src/lib/image-generation/services/image-generation-service.ts` e `src/lib/visual-signature/ai-image-generator.ts` com o 46-05) — execução sequencial evita conflito de edição. Não paralelizar planos da onda 2.
</execution_order>

<tasks>

<task type="auto">
  <name>Task 1: Migrar campaign_input_validation + campaign_image_review + corrigir furo 1 (4.1/4.4)</name>
  <files>src/lib/image-generation/services/input-validation-service.ts, src/lib/image-generation/services/image-review-service.ts, src/lib/image-generation/services/image-generation-service.ts</files>
  <read_first>
    - src/lib/image-generation/services/input-validation-service.ts (:19-140)
    - src/lib/image-generation/services/image-review-service.ts (:36-120, :346-390)
    - src/lib/image-generation/services/image-generation-service.ts (:135-156 — emitMetricsEvent; :224-241 — validação; :469-471 — revisão)
    - src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts (:330-340 — VALIDATION_MODEL residual)
    - openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/specs/ai-cost-accounting/spec.md (:28-43)
  </read_first>
  <action>
    Migrar `campaign_input_validation` (`InputValidationService.validate`) e `campaign_image_review` (`ImageReviewService.review`) para o gateway (adapter `chat-completions`, modelo do registry `gpt-4o`), preservando prompts, parâmetros e o resultado tipado; remover a leitura de `VISION_REVIEW_MODEL` e a instanciação de `new OpenAI()`. A telemetria passa a expor `capability`/`protocol`/`status`/`errorType` e o **modelo real**.
    Corrigir o furo 1 em `image-generation-service.ts`: `emitMetricsEvent` deixa de hardcodar `IMAGE_GENERATION_RESPONSES_MODEL` para as fases de validação/revisão — o modelo reportado vem do envelope da chamada de visão (`campaign_input_validation`/`campaign_image_review`). Manter o canal único `onMetricsEvent` e a semântica anti-dupla-contagem (cada fase continua emitindo exatamente 1 evento quando houve chamada real; override não emite). Ajustar `buildGenerationMetrics`/`logReviewDiagnostic` para não propagar o modelo de imagem como se fosse o da visão.
    Remover a constante residual `VALIDATION_MODEL` em `generate-without-logo/route.ts:336` (o modelo vem do registry).
  </action>
  <verify>
    <automated>npx vitest run src/lib/image-generation/services/__tests__/input-validation-service.test.ts src/lib/image-generation/services/__tests__/image-review-service.test.ts src/lib/image-generation/services/__tests__/image-generation-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `input-validation-service.ts`/`image-review-service.ts` chamam `invoke("campaign_input_validation"/"campaign_image_review", …)`, sem `VISION_REVIEW_MODEL`/`new OpenAI()`
    - Eventos de validação/revisão registram o modelo de visão real (gpt-4o), não o de imagem
    - `generate-without-logo/route.ts` sem `VALIDATION_MODEL`
    - Testes verdes
  </acceptance_criteria>
  <done>Validação/revisão via gateway com modelo real (furo 1 corrigido)</done>
</task>

<task type="auto">
  <name>Task 2: Migrar brand_profile_vision (brand-profiler + brand-director) (4.2)</name>
  <files>src/lib/brand-assets/brand-director.ts, src/lib/visual-signature/brand-profiler.ts</files>
  <read_first>
    - src/lib/brand-assets/brand-director.ts (:308-490)
    - src/lib/visual-signature/brand-profiler.ts (:121-130, :233-300, :616-770)
    - openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/specs/ai-cost-accounting/spec.md (:44-70)
  </read_first>
  <action>
    Migrar `brand_profile_vision` para o gateway em `brand-profiler.ts` (`callVision`/`callVisionFull` e o wrapper `generate`) e em `brand-director.ts` (`analyze`), preservando prompts, `detail: low`, `maxTokens`, `response_format: { type: "json_object" }`, timeouts e o threading de `onCall` (brand-profile distingue vision×text por ordem — o sink injetável preserva a ordem). Remover `OPENAI_BRAND_DIRECTOR_MODEL` e as instanciações de `new OpenAI()`. O modelo real (`gpt-4o`) passa a vir do envelope.
  </action>
  <verify>
    <automated>npx vitest run src/lib/brand-assets/__tests__/brand-director.test.ts src/lib/visual-signature/__tests__/brand-profiler.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `brand-profiler.ts`/`brand-director.ts` chamam `invoke("brand_profile_vision", …)`, sem `OPENAI_BRAND_DIRECTOR_MODEL`/`new OpenAI()`
    - `json_object`, detail/maxTokens e o threading de `onCall` preservados; ordem de eventos mantida
    - Testes verdes
  </acceptance_criteria>
  <done>brand_profile_vision via gateway, behavior-preserving</done>
</task>

<task type="auto">
  <name>Task 3: Migrar visual_signature_validation (4.3)</name>
  <files>src/lib/visual-signature/ai-image-generator.ts</files>
  <read_first>
    - src/lib/visual-signature/ai-image-generator.ts (:22-158)
    - src/lib/ai/model-registry.ts (visual_signature_validation → gpt-4o-mini, responses)
    - openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/specs/ai-image-generation/spec.md (:33-37)
  </read_first>
  <action>
    Migrar `VisualSignatureValidator.validate`/`validateSemantic` para `invoke("visual_signature_validation", …)` (adapter `responses`, default `gpt-4o-mini`), preservando a semântica de validação e o `onCall` (que atravessa do `AiImageGenerator.generate`). Remover `IMAGE_VALIDATION_MODEL` e a instanciação de `new OpenAI()` no validator. O modelo real passa a vir do envelope.
  </action>
  <verify>
    <automated>npx vitest run src/lib/visual-signature/__tests__ 2>&1; if ($?) { Write-Output "OK" }</automated>
  </verify>
  <acceptance_criteria>
    - Validator chama `invoke("visual_signature_validation", …)`, sem `IMAGE_VALIDATION_MODEL`/`new OpenAI()`
    - Semântica de validação e threading de `onCall` preservados
    - Testes verdes
  </acceptance_criteria>
  <done>visual_signature_validation via gateway</done>
</task>

<task type="auto">
  <name>Task 4: Gates + co-migração dos testes de visão (4.5)</name>
  <files>src/lib/image-generation/services/__tests__/input-validation-service.test.ts, src/lib/image-generation/services/__tests__/image-review-service.test.ts, src/lib/image-generation/services/__tests__/image-generation-service.test.ts, src/lib/brand-assets/__tests__/brand-director.test.ts, src/lib/visual-signature/__tests__/brand-profiler.test.ts</files>
  <read_first>
    - src/lib/image-generation/services/__tests__/input-validation-service.test.ts (:20-79)
    - src/lib/image-generation/services/__tests__/image-review-service.test.ts (:443-483)
    - src/lib/image-generation/services/__tests__/image-generation-service.test.ts (:548-720)
    - .planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-01-SUMMARY.md (baseline de testes)
  </read_first>
  <action>
    Co-migrar os testes de visão para o gateway (adapter falso + sink controlado), preservando os asserts de comportamento (modelo de visão real, usage, duração, `json_object`, ordem de eventos). Asserir explicitamente que o custo/pricing usa o modelo real de visão (`gpt-4o`/`gpt-4o-mini`), não o modelo de imagem. Rodar os 4 gates.
  </action>
  <verify>
    <automated>npx vitest run src/lib/image-generation/services src/lib/brand-assets src/lib/visual-signature; if ($?) { npm run typecheck; if ($?) { npm run lint; if ($?) { npm run build } } }</automated>
  </verify>
  <acceptance_criteria>
    - Testes de visão verdes; asserts de modelo real preservados/adicionados
    - Custo das chamadas de visão pelo pricing do modelo real (não do modelo de imagem)
    - `npx vitest run`, `npm run typecheck`, `npm run lint`, `npm run build` → 4 gates verdes
  </acceptance_criteria>
  <done>Suites de visão co-migradas e 4 gates verdes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| serviço ↔ provider de visão | Imagens do produto/loja cruzam para APIs externas |
| telemetria ↔ banco | Modelo real e custo por chamada de visão persistidos (best-effort) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-04a | Repudiation | Telemetria de visão | mitigate | Modelo real no envelope; furo 1 corrigido (sem hardcode do modelo de imagem); custo pelo pricing do modelo real |
| T-46-04b | Information Disclosure | Imagens enviadas | accept | Comportamento pré-existente (mesmas imagens/provedor); sem novo canal |
| T-46-04c | Tampering | `brand-profile` ordem de eventos | mitigate | Sink injetável preserva buffering/ordenação (vision×text por ordem) |
| T-46-04d | Denial of Service | Override de validação | mitigate | Override continua sem emitir evento (sem chamada real) — anti-dupla-contagem |
| T-46-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo |
</threat_model>

<verification>
- `npx vitest run src/lib/image-generation/services src/lib/brand-assets src/lib/visual-signature` → verde
- Grep: zero `VISION_REVIEW_MODEL`/`IMAGE_VALIDATION_MODEL`/`OPENAI_BRAND_DIRECTOR_MODEL` e zero `new OpenAI()` nos arquivos migrados
- Grep: `emitMetricsEvent` sem `IMAGE_GENERATION_RESPONSES_MODEL` como modelo de visão
- `npm run typecheck`, `npm run lint`, `npm run build` → 0 erros
</verification>

<success_criteria>
- 4 capacidades de visão executam via gateway
- Modelo real de visão na telemetria; furo 1 corrigido
- Prompts/parâmetros/ordem de eventos preservados
- 4 gates verdes
</success_criteria>

<output>
Create `.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-04-SUMMARY.md` when done
</output>

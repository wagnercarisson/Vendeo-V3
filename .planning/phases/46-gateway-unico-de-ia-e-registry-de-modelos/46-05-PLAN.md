---
phase: 46-gateway-unico-de-ia-e-registry-de-modelos
plan: 05
type: execute
wave: 5
depends_on: [46-04]
files_modified:
  - src/lib/image-generation/providers/openai.ts
  - src/lib/image-generation/providers/types.ts
  - src/lib/image-generation/services/image-generation-service.ts
  - src/lib/visual-signature/ai-image-generator.ts
  - src/app/api/campaign/generate-image/route.ts
  - src/lib/campaign/correction-reports.ts
  - src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts
  - src/lib/ai-cost/cost-estimator.ts
  - src/lib/ai-cost/__tests__/cost-estimator.test.ts
  - src/lib/image-generation/services/__tests__/image-generation-service.test.ts
  - src/lib/image-generation/providers/__tests__/openai-provider.test.ts
  - src/app/api/campaign/generate-image/__tests__/route.test.ts
  - src/app/api/campaign/generate-image/__tests__/campaign-generate.test.ts
  - src/app/api/campaign/generate-image/__tests__/concurrency.test.ts
  - src/app/api/campaign/generate-image/__tests__/regression-master-switch.test.ts
autonomous: true
requirements: [F46-22, F46-23, F46-24, F46-25]

must_haves:
  truths:
    - "`campaign_image` executa via gateway com adapter `responses` (tool `image_generation`), preservando tamanho/qualidade"
    - "`campaign_image_edit` é **segunda `invoke` explícita** (adapter `images`, `images.edit`) acionada só por erro de `capability` do Responses + imagem primary; falha + sucesso = **dois envelopes**"
    - "O furo 3 é corrigido: o fallback `images.edit` normaliza ausência de usage (`not_available`) e reporta duração + estimativa por unidade"
    - "`visual_signature_image` executa via gateway e o furo 4 é corrigido: `imageGenerationTool: true` + `generationType: \"visual_signature_image\"`"
    - "`cost-estimator.ts` soma o componente da tool em `campaign_image` **e** `visual_signature_image` (testes separados por capacidade)"
    - "A ordem determinística das referências `[primary, auxiliares..., identity?]` é preservada; `ImageProvider` permanece como contrato interno delegando ao gateway"
    - "Os callers paralelos `correction-reports.ts` e `visual-signature/generate-without-logo/route.ts` fornecem `AiTelemetryContext`/sink (buffering VS até `visual_signature_id`) e não persistem manualmente (D9)"
  artifacts:
    - path: "src/lib/image-generation/providers/openai.ts"
      provides: "OpenAIImageProvider delegando ao gateway (responses + images)"
      contains: "campaign_image_edit"
    - path: "src/lib/ai-cost/cost-estimator.ts"
      provides: "Componente da tool somado em campaign_image e visual_signature_image"
      contains: "visual_signature_image"
    - path: "src/lib/ai-cost/__tests__/cost-estimator.test.ts"
      provides: "Testes separados por capacidade (campaign_image/visual_signature_image)"
      contains: "visual_signature_image"
    - path: "src/lib/visual-signature/ai-image-generator.ts"
      provides: "visual_signature_image via gateway com imageGenerationTool"
      contains: "visual_signature_image"
  key_links:
    - from: "src/lib/image-generation/providers/openai.ts"
      to: "src/lib/ai/gateway.ts"
      via: "invoke(\"campaign_image\") + segunda invoke(\"campaign_image_edit\")"
      pattern: "campaign_image_edit"
    - from: "src/lib/ai-cost/cost-estimator.ts"
      to: "src/lib/visual-signature/ai-image-generator.ts"
      via: "generationType visual_signature_image habilita o componente da tool"
      pattern: "visual_signature_image"
---

<objective>
Migrar as **capacidades de IMAGEM** para a camada única e corrigir os furos 3 e 4: (5.1) `campaign_image` (adapter `responses` com tool `image_generation`, preservando tamanho/qualidade); (5.2) `campaign_image_edit` como segunda `invoke` explícita (adapter `images`), com furo 3 sanado (`not_available` + duração + estimativa por unidade) e **dois envelopes** (falha + fallback); (5.3) `visual_signature_image` + furo 4 (`imageGenerationTool: true` + `generationType`) e extensão do `cost-estimator.ts` para somar o componente da tool em `campaign_image` **e** `visual_signature_image` (testes separados, co-migrando `cost-estimator.test.ts:332`); (5.4) gates + co-migração dos testes de imagem/fallback.

Purpose: D5 (behavior-preserving), D7.3/D7.4 (furos 3/4), D4 (fallback como segunda chamada explícita).
Output: 3 capacidades de imagem via gateway, fallback com dois envelopes, estimativa correta nas duas capacidades, testes verdes.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-CONTEXT.md (F46-22..F46-25 :258-262; D7 :133-135)
@openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/specs/ai-image-generation/spec.md (:5-71)
@openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/specs/ai-cost-accounting/spec.md (:44-77)
@openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/design.md (:124-136 — D4; :175-184 — D7; :199 — images.edit sem tokens)
@.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-01-SUMMARY.md (furos 3/4; cost-estimator.ts:194)
@src/lib/image-generation/providers/openai.ts (:38-372)
@src/lib/image-generation/providers/types.ts (:1-65)
@src/lib/image-generation/services/image-generation-service.ts (:875+ — generateWithRetry)
@src/lib/visual-signature/ai-image-generator.ts (:160-347)
@src/lib/ai-cost/cost-estimator.ts (:141-243)
@src/lib/ai-cost/__tests__/cost-estimator.test.ts (:280-354)

<interfaces>
- `ImageProvider`/`ImageProviderInput`/`ImageProviderOutput` (`providers/types.ts`) — **contrato interno/seam de testes mantido**; a implementação `OpenAIImageProvider` delega ao gateway.
- `ImageGenerationService.generateWithRetry` (state machine, backoffs, `global_timeout`) permanece; o fallback técnico vira segunda `invoke`.
- `resolveAiCost({ provider, model, usage?, imageGenerationTool?, generationType? })` — o gate de `campaign_image` (linha :194) é estendido para `campaign_image` **ou** `visual_signature_image`.
- `AiImageGenerator.generate` → `invoke("visual_signature_image", …)` com `imageGenerationTool: true` e `generationType: "visual_signature_image"`.
- Gate `images.edit` (D4): segunda `invoke("campaign_image_edit", …, target: "primary")` só por erro `capability` do Responses + imagem primary; auth/safety não acionam.
</interfaces>
</context>

<execution_order>
**DAG (legível por ferramenta):** `depends_on: [46-04]` (onda 5) — roda **depois** do 46-04 (cadeia serial 46-03 → 46-04 → 46-05); ambos tocam `generate-image/route.ts`. **46-06 depende de 46-05** (`depends_on: [46-05]`, onda 6).
</execution_order>

<tasks>

<task type="auto">
  <name>Task 1: Migrar campaign_image via adapter responses (5.1)</name>
  <files>src/lib/image-generation/providers/openai.ts, src/lib/image-generation/providers/types.ts</files>
  <read_first>
    - src/lib/image-generation/providers/openai.ts (:38-202)
    - src/lib/image-generation/providers/types.ts (:1-65)
    - src/lib/ai/model-registry.ts (campaign_image → gpt-5.5, responses)
    - openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/specs/ai-image-generation/spec.md (:41-50)
  </read_first>
  <action>
    Migrar o caminho primário de `OpenAIImageProvider.generateImage` para `invoke("campaign_image", …)` (adapter `responses`, tool `image_generation`, default `gpt-5.5`), preservando `size`/`quality` e o `AbortSignal`. Remover `IMAGE_GENERATION_RESPONSES_MODEL`/`new OpenAI()` do caminho primário. O `ImageProviderOutput` continua expondo `imageBase64`/`mimeType`/`model`/`usage`/`usageMeta` (mapeados do resultado do gateway). `ImageProvider` permanece como contrato interno/seam de testes, com a implementação delegando ao gateway.
  </action>
  <verify>
    <automated>npx vitest run src/lib/image-generation/providers/__tests__/openai-provider.test.ts 2>&1; if ($?) { Write-Output "OK" }</automated>
  </verify>
  <acceptance_criteria>
    - Caminho primário chama `invoke("campaign_image", …)`; sem `IMAGE_GENERATION_RESPONSES_MODEL`/`new OpenAI()`
    - `size`/`quality` e `AbortSignal` preservados; `ImageProviderOutput` mantido
    - Testes do provider verdes
  </acceptance_criteria>
  <done>campaign_image via adapter responses</done>
</task>

<task type="auto">
  <name>Task 2: Migrar campaign_image_edit como segunda invoke + furo 3 + dois envelopes (5.2)</name>
  <files>src/lib/image-generation/providers/openai.ts, src/lib/image-generation/services/image-generation-service.ts</files>
  <read_first>
    - src/lib/image-generation/providers/openai.ts (:204-372 — canUseEditFallback/isResponsesApiError/fallbackToImageApi)
    - src/lib/image-generation/services/image-generation-service.ts (:875+ — generateWithRetry)
    - openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/specs/ai-image-generation/spec.md (:51-71)
    - design.md (:124-136 — D4; gates)
  </read_first>
  <action>
    Migrar o fallback `images.edit` para **segunda `invoke("campaign_image_edit", …, target: "primary")`** (adapter `images`, default `gpt-image-2`), preservando: o gating (só por erro de `capability` do Responses + imagem primary — `isResponsesApiError` mapeado para `kind === "capability"`), a ordem determinística `[primary, auxiliares..., identity?]`, o tamanho conservador `1024x1024` e a dedupe de referências. Remover `IMAGE_EDIT_FALLBACK_MODEL`/`GPT_IMAGE_MODEL`/`new OpenAI()`. **Furo 3:** o adapter `images` normaliza ausência de usage (`not_available`) e reporta duração + estimativa por unidade; **falha no Responses + sucesso no Images = duas tentativas reais e dois envelopes** (nunca um). Auth/safety/rate-limit continuam sem acionar o fallback.
  </action>
  <verify>
    <automated>npx vitest run src/lib/image-generation/providers/__tests__/openai-provider.test.ts src/lib/image-generation/services/__tests__/image-generation-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Fallback chama `invoke("campaign_image_edit", …, target: "primary")`; sem `IMAGE_EDIT_FALLBACK_MODEL`/`GPT_IMAGE_MODEL`/`new OpenAI()`
    - Gating preservado (só `capability` + primary); ordem determinística e `1024x1024` preservados
    - `images.edit` sem usage → `not_available` + duração + estimativa por unidade
    - Falha+fallback produz dois envelopes
    - Testes verdes
  </acceptance_criteria>
  <done>campaign_image_edit como segunda invoke; furo 3 corrigido; dois envelopes</done>
</task>

<task type="auto">
  <name>Task 3: Migrar visual_signature_image + furo 4 + estender cost-estimator (5.3)</name>
  <files>src/lib/visual-signature/ai-image-generator.ts, src/lib/ai-cost/cost-estimator.ts, src/lib/ai-cost/__tests__/cost-estimator.test.ts</files>
  <read_first>
    - src/lib/visual-signature/ai-image-generator.ts (:160-347)
    - src/lib/ai-cost/cost-estimator.ts (:141-243 — gate :194)
    - src/lib/ai-cost/__tests__/cost-estimator.test.ts (:280-354 — âncora :332)
    - openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/specs/ai-cost-accounting/spec.md (:44-77)
  </read_first>
  <action>
    Migrar `AiImageGenerator.generate` (caminho de imagem) para `invoke("visual_signature_image", …)` (adapter `responses`, default `gpt-5.5`), preservando prompt/parâmetros/timeout. **Furo 4:** a chamada marca `imageGenerationTool: true` + `generationType: "visual_signature_image"` para que o resolvedor de custo some o componente da tool.
    Estender `cost-estimator.ts`: o gate da linha :194 passa a aceitar `generationType === "campaign_image" || generationType === "visual_signature_image"` (mesma fórmula `responses_image_generation_v2`, `costEstimationNote` provisório mantido enquanto não houver `provider_reported_cost_usd`). Manter os demais caminhos (gpt-image-2/fallback) sem o componente da tool (anti-dupla-cobrança).
    Co-migrar `cost-estimator.test.ts:332`: substituir a âncora "visual_signature não soma a tool" por **testes separados por capacidade** — `campaign_image` soma o componente; `visual_signature_image` **também** soma; `visual_signature` (genérico) e o fallback gpt-image-2 **não** somam.
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai-cost/__tests__/cost-estimator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `ai-image-generator.ts` chama `invoke("visual_signature_image", …)` com `imageGenerationTool: true` + `generationType: "visual_signature_image"`
    - `cost-estimator.ts` soma o componente da tool em `campaign_image` **e** `visual_signature_image`
    - `cost-estimator.test.ts` tem testes separados por capacidade; a âncora :332 foi co-migrada
    - Fallback gpt-image-2 e `visual_signature` genérico continuam sem o componente
    - Testes verdes
  </acceptance_criteria>
  <done>visual_signature_image via gateway; furo 4 e cost-estimator estendidos</done>
</task>

<task type="auto">
  <name>Task 4: Gates + co-migração dos testes de imagem/fallback (5.4)</name>
  <files>src/app/api/campaign/generate-image/route.ts, src/lib/campaign/correction-reports.ts, src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts, src/app/api/campaign/generate-image/__tests__/route.test.ts, src/app/api/campaign/generate-image/__tests__/campaign-generate.test.ts, src/app/api/campaign/generate-image/__tests__/concurrency.test.ts, src/app/api/campaign/generate-image/__tests__/regression-master-switch.test.ts</files>
  <read_first>
    - src/app/api/campaign/generate-image/route.ts (:720-760 — switch de onMetricsEvent/recordCall)
    - src/lib/campaign/correction-reports.ts (:583-703 — resolveAiCost/record + onMetrics manuais)
    - src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts (:264-420 — flushCallEvents/onCall + startRun manuais; caso canônico de buffering VS)
    - src/app/api/campaign/generate-image/__tests__/route.test.ts (:920-965, :1270-1530)
    - .planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-01-SUMMARY.md (baseline de testes de imagem)
  </read_first>
  <action>
    **Remover a persistência manual (D9):** no `generate-image/route.ts`, o `recordCall` de `campaign_image` (case `image_generation` do switch de `onMetricsEvent`, :740-746, `resolveAiCost` + `AiCostTracker.record`) SHALL ser removido — o serviço de imagem invoca o gateway e o **sink** persiste; se o switch ficar sem casos de IA (os de validação/revisão saem no 46-04), remover o canal `onMetricsEvent` da chamada e o `recordCall` residual de IA. Preservar o `onPhase` (progresso) e o delivery marker (`campaign_pipeline`).
    **Converter os callers paralelos (D9):** em `src/lib/campaign/correction-reports.ts` (orquestrador da correção-v2 que chama `ImageGenerationService`), remover o `resolveAiCost` (:583) + `AiCostTracker.record` (:600) e o dispatch manual de `onMetrics` (:628-671); fornecer `AiTelemetryContext`/sink ao `ImageGenerationService`. Em `visual-signature/generate-without-logo/route.ts` (caso canônico de **buffering** até `visual_signature_id`), remover o `resolveAiCost` (:298) + `record` (:304/:163), o `onCall`/`flushCallEvents` manuais **e** a constante `VALIDATION_MODEL` (:336) com o `handleCall` que a consome (:340) — o tipo da chamada passa a vir do envelope (`info.capability === "visual_signature_validation"`); injetar o **sink buffering** no `AiTelemetryContext` (o sink bufferiza até a assinatura existir e preserva a ordem).
    Co-migrar as suites de imagem/rota que referenciam `IMAGE_GENERATION_RESPONSES_MODEL` (`generate-image/route.test.ts`, `campaign-generate.test.ts`, `concurrency.test.ts`, `regression-master-switch.test.ts`), preservando os asserts de comportamento. Asserir explicitamente que o fallback gera **dois eventos** call-level (falha + sucesso) com modelos próprios (gpt-5.5 na falha, gpt-image-2 no fallback). Rodar os 4 gates.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/campaign/generate-image src/lib/image-generation src/lib/ai-cost; if ($?) { npm run typecheck; if ($?) { npm run lint; if ($?) { npm run build } } }</automated>
  </verify>
  <acceptance_criteria>
    - Suites de imagem/rota verdes; sem dependência de `IMAGE_GENERATION_RESPONSES_MODEL`
    - `correction-reports.ts` e `visual-signature/generate-without-logo/route.ts` fornecem `AiTelemetryContext`/sink; `resolveAiCost`/`AiCostTracker.record` manuais de IA removidos (delivery markers preservados)
    - `visual-signature/generate-without-logo/route.ts` sem `VALIDATION_MODEL`/`handleCall` legados; tipo da chamada pelo `info.capability`
    - Asserção explícita de dois eventos no fallback (falha + sucesso) com modelos próprios
    - `npx vitest run`, `npm run typecheck`, `npm run lint`, `npm run build` → 4 gates verdes
  </acceptance_criteria>
  <done>Suites de imagem co-migradas e 4 gates verdes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| serviço ↔ provider de imagem | Imagens e prompt cruzam para Responses/Images API |
| telemetria ↔ banco | Custo estimado por chamada de imagem (best-effort) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-05a | Tampering | Fallback `images.edit` | mitigate | Acionado só por `kind === "capability"` + imagem primary; auth/safety/rate-limit não acionam; duas tentativas → dois envelopes |
| T-46-05b | Repudiation | Estimativa de custo | mitigate | Componente da tool somado em `campaign_image` **e** `visual_signature_image`; `costEstimationNote` provisório até `provider_reported_cost_usd`; fallback gpt-image-2 sem tool (anti-dupla) |
| T-46-05c | Information Disclosure | Upload de imagem | accept | Mesmas imagens/provedor do comportamento atual |
| T-46-05d | Denial of Service | `generateWithRetry` | mitigate | Retry/backoff/global-timeout permanecem no serviço; gateway faz uma tentativa |
| T-46-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo |
</threat_model>

<verification>
- `npx vitest run src/app/api/campaign/generate-image src/lib/image-generation src/lib/ai-cost` → verde
- Grep: zero `IMAGE_GENERATION_RESPONSES_MODEL`/`IMAGE_EDIT_FALLBACK_MODEL`/`GPT_IMAGE_MODEL`/`new OpenAI()` nos arquivos migrados
- Grep: `cost-estimator.ts` contém `visual_signature_image` no gate do componente da tool
- `npm run typecheck`, `npm run lint`, `npm run build` → 0 erros
</verification>

<success_criteria>
- `campaign_image`, `campaign_image_edit`, `visual_signature_image` executam via gateway
- Furos 3 e 4 corrigidos; fallback gera dois envelopes
- `cost-estimator.ts` soma a tool nas duas capacidades (testes separados)
- 4 gates verdes
</success_criteria>

<output>
Create `.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-05-SUMMARY.md` when done
</output>

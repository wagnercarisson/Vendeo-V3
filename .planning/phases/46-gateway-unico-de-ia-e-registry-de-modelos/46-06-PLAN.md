---
phase: 46-gateway-unico-de-ia-e-registry-de-modelos
plan: 06
type: execute
wave: 6
depends_on: [46-05]
files_modified:
  - src/app/api/store/[id]/logo/route.ts
  - src/app/api/store/[id]/logo/retry-brand-director/route.ts
  - src/lib/visual-signature/server-actions.ts
  - src/app/api/store/[id]/visual-signature/approve/route.ts
  - src/app/api/store/[id]/visual-signature/restore/route.ts
  - src/lib/ai/__tests__/telemetry-coverage.test.ts
  - src/lib/ai/__tests__/architecture-guard.test.ts
  - src/app/api/store/[id]/logo/__tests__/logo-route.test.ts
  - src/app/api/store/[id]/logo/retry-brand-director/__tests__/retry-route.test.ts
  - src/lib/visual-signature/__tests__/server-actions.test.ts
  - src/app/api/store/[id]/visual-signature/approve/__tests__/approve-route.test.ts
  - src/app/api/store/[id]/visual-signature/restore/__tests__/restore-route.test.ts
autonomous: true
requirements: [F46-26, F46-27, F46-28, F46-29, F46-30, F46-31]

must_haves:
  truths:
    - "`POST /api/store/[id]/logo` fornece `AiTelemetryContext` ao gateway (furo 2); o sink grava evento call-level com custo/duração"
    - "`POST /api/store/[id]/logo/retry-brand-director` emite telemetria pelo mesmo caminho (furo 2)"
    - "`visual-signature/server-actions.ts` (`generateVariations`/`generateAutomatic`) fornece contexto ao `AiImageGenerator.generate` (furo 5)"
    - "`visual-signature/approve` (2 call sites) e `visual-signature/restore` fornecem contexto ao `profiler.generate` (furos 6/7)"
    - "Caminho único de telemetria (D9): caller fornece `AiTelemetryContext` → gateway gera o envelope → sink persiste; nenhum caller resolve custo/grava manualmente; `onCall`, se mantido, apenas recebe o envelope já produzido"
    - "Gate global de arquitetura sobre `src/` proíbe, fora de `src/lib/ai/adapters/**`: init direto de SDK (`new OpenAI(`/`new GoogleGenerativeAI(`) e chamadas de wire (`chat.completions.create`, `responses.create`, `images.edit`, `generateContent`). A proibição de **leitura de env-var de modelo** é adicionada a este gate no 46-07, após a remoção das envs"
    - "Caminho único de persistência (D9): fora de `src/lib/ai/**` (sink), `resolveAiCost(` e `AiCostTracker` `.record(` para chamadas de IA são proibidos — exceção documentada apenas para **delivery markers** (`campaign_pipeline`); o gate global cobre isso"
    - "Cada caminho grava evento call-level com modelo real e duração; comportamento de negócio preservado"
  artifacts:
    - path: "src/app/api/store/[id]/logo/route.ts"
      provides: "Telemetria no upload de logo"
      contains: "AiTelemetryContext"
    - path: "src/app/api/store/[id]/logo/retry-brand-director/route.ts"
      provides: "Telemetria no retry do Brand Director"
      contains: "AiTelemetryContext"
    - path: "src/lib/visual-signature/server-actions.ts"
      provides: "Telemetria em generateVariations/generateAutomatic"
      contains: "AiTelemetryContext"
    - path: "src/app/api/store/[id]/visual-signature/approve/route.ts"
      provides: "Telemetria nos 2 call sites do approve"
      contains: "AiTelemetryContext"
    - path: "src/app/api/store/[id]/visual-signature/restore/route.ts"
      provides: "Telemetria no restore"
      contains: "AiTelemetryContext"
    - path: "src/lib/ai/__tests__/architecture-guard.test.ts"
      provides: "Gate global de arquitetura (fora de adapters)"
      contains: "chat.completions.create"
    - path: "src/lib/ai/__tests__/telemetry-coverage.test.ts"
      provides: "Teste de inventário de cobertura de telemetria"
      contains: "inventory"
  key_links:
    - from: "src/app/api/store/[id]/logo/route.ts"
      to: "src/lib/brand-assets/brand-director.ts"
      via: "AiTelemetryContext (run/store/sink) encaminhado ao gateway"
      pattern: "AiTelemetryContext"
    - from: "src/lib/visual-signature/server-actions.ts"
      to: "src/lib/visual-signature/ai-image-generator.ts"
      via: "AiTelemetryContext (sink bufferiza até visual_signature_id)"
      pattern: "AiTelemetryContext"
    - from: "src/lib/ai/__tests__/architecture-guard.test.ts"
      to: "src/"
      via: "gate global proibindo SDK/wire/env fora de src/lib/ai/adapters/**"
      pattern: "adapters"
---

<objective>
Fechar a cobertura de telemetria em todos os callers produtivos hoje sem `onCall`: (6.1) `POST /api/store/[id]/logo`; (6.2) `logo/retry-brand-director`; (6.3) `visual-signature/server-actions.ts` (`generateVariations`/`generateAutomatic`); (6.4) `visual-signature/approve` (2 call sites) e `visual-signature/restore`; (6.5) teste de inventário garantindo que nenhum caminho produtivo fica fora da camada única; (6.6) testes dos caminhos com evento call-level (custo, modelo real, duração) + regressão.

Purpose: D7.2/D7.5 + D9 (telemetria em todos os caminhos produtivos via `AiTelemetryContext`/sink; cobertura total), F46-26..F46-31.
Output: 5 caminhos produtivos emitindo telemetria + teste de inventário + testes verdes.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-CONTEXT.md (F46-26..F46-31 :264-270; D7 :133-135)
@openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/specs/ai-cost-accounting/spec.md (:44-77)
@openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/design.md (:11-25 — 7 furos; :175-184 — D7)
@.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-01-SUMMARY.md (baseline dos 7 furos com file:line)
@src/app/api/store/[id]/logo/route.ts (:253-254)
@src/app/api/store/[id]/logo/retry-brand-director/route.ts (:108-111)
@src/lib/visual-signature/server-actions.ts (:102-300)
@src/app/api/store/[id]/visual-signature/approve/route.ts (:196-197, :588-589)
@src/app/api/store/[id]/visual-signature/restore/route.ts (:157-158)
@src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts (:264-420 — padrão de onCall/resolveAiCost já existente)

<interfaces>
- `BrandDirectorService.analyze(params)` aceita `onCall?: (info: AiCallInfo) => void | Promise<void>` (`brand-director.ts:313`).
- `AiImageGenerator.generate(params)` aceita `onCall?` (`ai-image-generator.ts:178`).
- `BrandProfilerWithoutLogoService.generate(input)` aceita `input.onCall?` (`types.ts:344`).
- Padrão de telemetria já existente em `generate-without-logo/route.ts`: `AiCostTracker.startRun("visual_signature")` + `resolveAiCost({ …, generationType })` + `record(...)` — reusar, agora **dentro do sink** (D9).
- Rotas que bufferizam até conhecer `visual_signature_id` injetam o próprio **sink** no `AiTelemetryContext` (D3/D9) — `onCall`, se mantido por compatibilidade, apenas recebe o envelope já produzido.
</interfaces>
</context>

<execution_order>
**DAG (legível por ferramenta):** `depends_on: [46-05]` (onda 6). **46-07 depende de 46-06** (`depends_on: [46-06]`, onda 7) por compartilhar `src/lib/visual-signature/server-actions.ts` e por estender o `architecture-guard.test.ts` criado aqui.

**Nota de reabertura 46-04 (2026-09-12):** as Tasks 1-4 (logo, retry-brand-director, server-actions, approve, restore) foram **antecipadas para o 46-04 Task 6** — todas são callers de capacidades de **visão** já migradas. Este plano mantém o **gate global de arquitetura** (Task 5) e o **inventário de cobertura** (Task 6), que devem verificar os callers já convertidos (incl. `identity-art-director.ts` e `correction-reports.ts`).
</execution_order>

<tasks>

<task type="auto">
  <name>Task 1: Telemetria em POST /logo e retry-brand-director (6.1/6.2 — furo 2)</name>
  <files>src/app/api/store/[id]/logo/route.ts, src/app/api/store/[id]/logo/retry-brand-director/route.ts</files>
  <read_first>
    - src/app/api/store/[id]/logo/route.ts (:240-280)
    - src/app/api/store/[id]/logo/retry-brand-director/route.ts (:95-140)
    - src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts (:264-330 — padrão startRun/resolveAiCost/record)
    - src/lib/brand-assets/brand-director.ts (:308-450)
  </read_first>
  <action>
    Nas duas rotas, criar o `AiTelemetryContext` (run via `AiCostTracker.startRun("brand_profile")` + `sink` padrão injetado) e **encaminhá-lo** ao `BrandDirectorService.analyze` até o `invoke("brand_profile_vision", …)`. **O sink** resolve custo (`resolveAiCost` + `CAPABILITY_GENERATION_TYPE`) e grava call-level (`AiCostTracker.record`), best-effort (nunca bloqueia). As rotas **não** chamam `resolveAiCost`/`record` manualmente (D9). Se `analyze` mantiver `onCall` por compatibilidade, ele **apenas** recebe o envelope já produzido. Preservar todo o comportamento de negócio (status HTTP, mensagens, tratamento de `BrandDirectorAnalysisError`). Nenhuma mudança de UI/form/contrato HTTP.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/store/[id]/logo 2>&1; if ($?) { Write-Output "OK" }</automated>
  </verify>
  <acceptance_criteria>
    - Ambas as rotas fornecem `AiTelemetryContext` (run/store/sink) e o sink grava evento call-level com modelo real e duração
    - Nenhuma rota chama `resolveAiCost`/`AiCostTracker.record` manualmente (D9)
    - Comportamento HTTP e tratamento de erro preservados
    - Testes das rotas verdes
  </acceptance_criteria>
  <done>Furo 2 sanado — logo e retry-brand-director emitem telemetria</done>
</task>

<task type="auto">
  <name>Task 2: Telemetria em server-actions + approve (2 call sites) + restore (6.3/6.4 — furos 5/6/7)</name>
  <files>src/lib/visual-signature/server-actions.ts, src/app/api/store/[id]/visual-signature/approve/route.ts, src/app/api/store/[id]/visual-signature/restore/route.ts</files>
  <read_first>
    - src/lib/visual-signature/server-actions.ts (:102-300)
    - src/app/api/store/[id]/visual-signature/approve/route.ts (:180-210, :575-600)
    - src/app/api/store/[id]/visual-signature/restore/route.ts (:140-175)
    - src/lib/visual-signature/brand-profiler.ts (:233-300)
    - src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts (:332-420 — padrão de handleCall)
  </read_first>
  <action>
    Passar o `AiTelemetryContext` (com sink injetado) ao `AiImageGenerator.generate` em `generateVariations` (:136/:152) e `generateAutomatic` (:233/:285) — o gateway gera o envelope e o sink persiste, bufferizando até conhecer o `visual_signature_id` quando necessário (a ordem é preservada pelo sink, não por `onCall`). Fazer o mesmo em `approve` (2 call sites :197/:589) e `restore` (:158) via `profiler.generate` com contexto (capability `brand_profile_vision`). Nenhum caller resolve custo nem grava manualmente (D9). Preservar comportamento de negócio e o tratamento de erros.
  </action>
  <verify>
    <automated>npx vitest run src/lib/visual-signature/__tests__/server-actions.test.ts src/app/api/store/[id]/visual-signature/approve/__tests__/approve-route.test.ts src/app/api/store/[id]/visual-signature/restore 2>&1; if ($?) { Write-Output "OK" }</automated>
  </verify>
  <acceptance_criteria>
    - `server-actions.ts` fornece `AiTelemetryContext` nos 2 fluxos; `approve` nos 2 call sites; `restore` no seu call site
    - Eventos call-level com modelo real e duração; buffering/ordem preservados pelo sink (não por `onCall`)
    - Comportamento de negócio preservado
    - Testes verdes
  </acceptance_criteria>
  <done>Furos 5/6/7 sanados — server-actions/approve/restore emitem telemetria</done>
</task>

<task type="auto">
  <name>Task 3: Teste de inventário + testes dos caminhos + gates (6.5/6.6)</name>
  <files>src/lib/ai/__tests__/architecture-guard.test.ts, src/lib/ai/__tests__/telemetry-coverage.test.ts, src/app/api/store/[id]/logo/__tests__/logo-route.test.ts, src/app/api/store/[id]/logo/retry-brand-director/__tests__/retry-route.test.ts, src/lib/visual-signature/__tests__/server-actions.test.ts, src/app/api/store/[id]/visual-signature/approve/__tests__/approve-route.test.ts, src/app/api/store/[id]/visual-signature/restore/__tests__/restore-route.test.ts</files>
  <read_first>
    - design.md (:182 — teste de inventário)
    - src/lib/ai/gateway.ts (46-02)
    - src/lib/ai/telemetry-sink.ts (46-02)
    - .planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-01-SUMMARY.md (inventário dos 7 caminhos)
  </read_first>
  <action>
    Criar `src/lib/ai/__tests__/architecture-guard.test.ts` (**gate global de arquitetura**, F46-30): varrer `src/**/*.{ts,tsx}` e **falhar** se, **fora de `src/lib/ai/adapters/**`**, existir init direto de SDK ou chamada de wire, com regexes exatas: `new\s+OpenAI\(`, `new\s+GoogleGenerativeAI\(`, `\.chat\.completions\.create\(`, `\.responses\.create\(`, `\.images\.edit\(`, `\.generateContent\(`. **Além disso**, proibir **fora de `src/lib/ai/**`** (sink) o uso em **chamadas** de IA: `await resolveAiCost\(` / `= resolveAiCost\(` (não a **definição** em `src/lib/ai-cost/cost-estimator.ts`) e `AiCostTracker` `\.record\(` — com allowlist explícita para **delivery markers** (tipos `campaign_pipeline`, `brand_profile_without_logo`, `brand_profile_with_logo`, `visual_signature` — incluindo o helper `generation-events.ts`, que não é chamada de IA), documentada no teste. **Excluir** o próprio arquivo do gate e `__tests__/**`/fixtures do scan (evita auto-match e matches em descrições/comentários) e ignorar comentários (`//`, `/* */`) antes de casar. Este gate é **global** (não depende de lista conhecida de callers). A **leitura de env-var de modelo** é adicionada a este gate no **46-07** (após a remoção das envs) — aqui o gate cobre SDK/wire e persistência manual.
    Criar `src/lib/ai/__tests__/telemetry-coverage.test.ts` (**inventário global**, não allowlist): varrer `src/app/**` e `src/lib/**` e enumerar todo arquivo produtivo que importa/chama um serviço de IA migrado (`CopyDirectorService`, `CorrectionIntentService`, `BrandTextOnlyInferenceService`, `OpenAIProvider` legado, `InputValidationService`, `ImageReviewService`, `BrandDirectorService`, `BrandProfilerWithoutLogoService`, `AiImageGenerator`, `ImageGenerationService`, `StoreIdentityArtDirectorService`), afirmando que cada um fornece `AiTelemetryContext`/sink e **não** chama `resolveAiCost`/`AiCostTracker.record` manualmente para IA. Cobrir explicitamente os callers convertidos: logo, retry-brand-director, `server-actions.ts`, approve, restore, `brand-profile/{infer,realign,generate-without-logo}`, `visual-signature/generate-without-logo`, `identity-art-director.ts`, `correction-reports.ts`.
    Cobrir os testes dos caminhos (6.6): evento call-level com custo, modelo real e duração; regressão de comportamento. Rodar os 4 gates.
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai/__tests__/architecture-guard.test.ts src/lib/ai/__tests__/telemetry-coverage.test.ts src/app/api/store src/lib/visual-signature; if ($?) { npm run typecheck; if ($?) { npm run lint; if ($?) { npm run build } } }</automated>
  </verify>
  <acceptance_criteria>
    - `architecture-guard.test.ts` verde: nenhuma inicialização direta de SDK/chamada de wire fora de `src/lib/ai/adapters/**` e nenhum `resolveAiCost`/`AiCostTracker.record` de IA fora de `src/lib/ai/**` (exceto delivery markers; env-var de modelo é gate do 46-07)
    - `telemetry-coverage.test.ts` (inventário global) verde, cobrindo logo, retry, server-actions, approve, restore, brand-profile/{infer,realign,generate-without-logo}, visual-signature/generate-without-logo e correction-reports
    - Testes dos caminhos verdes com asserts de evento call-level (custo/modelo real/duração)
    - `npx vitest run`, `npm run typecheck`, `npm run lint`, `npm run build` → 4 gates verdes
  </acceptance_criteria>
  <done>Cobertura total de telemetria verificada por teste de inventário; 4 gates verdes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rota/action ↔ serviço de IA | Contexto de telemetria (run/store/campaign) flui para o gateway |
| telemetria ↔ banco | Eventos call-level best-effort |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-06a | Repudiation | Caminhos sem telemetria | mitigate | Gate global de arquitetura (`architecture-guard.test.ts`) + inventário de cobertura; `AiTelemetryContext` obrigatório em logo/retry/server-actions/approve/restore; nenhum caller resolve custo/grava manualmente (D9) |
| T-46-06b | Tampering | Buffering VS | mitigate | Sink injetado bufferiza até conhecer `visual_signature_id`; ordem preservada |
| T-46-06c | Denial of Service | Persistência | mitigate | Gravação best-effort (fail-open); geração nunca bloqueada |
| T-46-06d | Information Disclosure | Contexto de run | accept | IDs internos (store/campaign/run) — sem segredos |
| T-46-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo |
</threat_model>

<verification>
- `npx vitest run src/lib/ai/__tests__/architecture-guard.test.ts src/lib/ai/__tests__/telemetry-coverage.test.ts src/app/api/store src/lib/visual-signature` → verde
- Gate global: `src/**` sem init de SDK/`chat.completions.create`/`responses.create`/`images.edit`/`generateContent` fora de `src/lib/ai/adapters/**` e sem `resolveAiCost`/`AiCostTracker.record` de IA fora de `src/lib/ai/**` (leitura de env-var de modelo é gate do 46-07)
- Grep: `logo/route.ts`, `retry-brand-director/route.ts`, `server-actions.ts`, `approve/route.ts`, `restore/route.ts` contêm `AiTelemetryContext`; nenhum chama `resolveAiCost`/`AiCostTracker.record` manualmente
- `npm run typecheck`, `npm run lint`, `npm run build` → 0 erros
</verification>

<success_criteria>
- Todos os callers produtivos hoje sem telemetria passam a fornecer `AiTelemetryContext` (o sink persiste)
- Gate global de arquitetura verde (nenhum caminho produtivo emite IA fora da camada única)
- Teste de inventário garante cobertura; comportamento de negócio preservado; 4 gates verdes
</success_criteria>

<output>
Create `.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-06-SUMMARY.md` when done
</output>

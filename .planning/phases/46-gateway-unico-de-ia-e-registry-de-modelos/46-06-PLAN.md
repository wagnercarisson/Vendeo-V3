---
phase: 46-gateway-unico-de-ia-e-registry-de-modelos
plan: 06
type: execute
wave: 6
depends_on: [46-05]
files_modified:
  - src/lib/ai/__tests__/architecture-guard.test.ts
  - src/lib/ai/__tests__/telemetry-coverage.test.ts
  - .planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-06-SUMMARY.md
autonomous: true
requirements: [F46-26, F46-27, F46-28, F46-29, F46-30, F46-31]

must_haves:
  truths:
    - "Caminho único de telemetria (D9) verificado: os callers antecipados (logo, retry-brand-director, server-actions, approve, restore) já fornecem `AiTelemetryContext`/sink (46-04 Task 6) — nenhum resolve custo/grava manualmente; `onCall`, se mantido, apenas recebe o envelope já produzido"
    - "Gate global de arquitetura sobre `src/` proíbe, fora de `src/lib/ai/adapters/**`: init direto de SDK (`new OpenAI(`/`new GoogleGenerativeAI(`) e chamadas de wire (`chat.completions.create`, `responses.create`, `images.edit`, `generateContent`). A proibição de **leitura de env-var de modelo** é adicionada no 46-07, após a remoção das envs"
    - "Caminho único de persistência (D9): fora de `src/lib/ai/**` (sink), `resolveAiCost(` e `AiCostTracker.record(` para chamadas de IA são proibidos — exceção documentada apenas para **delivery markers** (`campaign_pipeline`); o gate global cobre isso"
    - "O teste de inventário enumera os callers produtivos (incl. os antecipados: logo, retry-brand-director, server-actions, approve, restore, correction-reports, identity-art-director) e afirma que cada um fornece `AiTelemetryContext`/sink e não persiste manualmente"
  artifacts:
    - path: "src/lib/ai/__tests__/architecture-guard.test.ts"
      provides: "Gate global de arquitetura (fora de adapters)"
      contains: "chat.completions.create"
    - path: "src/lib/ai/__tests__/telemetry-coverage.test.ts"
      provides: "Teste de inventário de cobertura de telemetria"
      contains: "inventory"
  key_links:
    - from: "src/lib/ai/__tests__/architecture-guard.test.ts"
      to: "src/"
      via: "gate global proibindo SDK/wire/persistência manual fora da camada única"
      pattern: "adapters"
    - from: "src/lib/ai/__tests__/telemetry-coverage.test.ts"
      to: "src/app + src/lib"
      via: "inventário de cobertura de telemetria dos callers produtivos"
      pattern: "AiTelemetryContext"
---

<objective>
**Reconciliação 46-04 (2026-09-12) — Tasks 1/2 NEUTRALIZADAS:** as antigas Tasks 1-4 (6.1-6.4 — logo, retry-brand-director, server-actions, approve, restore) foram **antecipadas e concluídas no 46-04 Task 6** (`a73dd5a4`); seus corpos foram removidos deste plano e a cobertura é **verificada** pelo gate global + inventário da Task 3 (não re-executada). Resta apenas: (6.5) **gate global de arquitetura** sobre `src/` (SDK/wire + persistência manual de IA) e (6.6) **teste de inventário** de cobertura de telemetria.

Purpose: D7.5 + D9 (nenhum caminho produtivo fora da camada única), F46-26..F46-31.
Output: gate global + inventário de cobertura verdes; callers antecipados verificados.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-CONTEXT.md (F46-26..F46-31 :264-270; D7 :133-135)
@openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/specs/ai-cost-accounting/spec.md (:44-77)
@openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/design.md (:11-25 — 7 furos; :175-184 — D7)
@.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-01-SUMMARY.md (baseline dos 7 furos com file:line)
@.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-04-SUMMARY.md (callers antecipados já convertidos)

<interfaces>
- Callers antecipados (verificados, NÃO re-editados): `logo/route.ts`, `logo/retry-brand-director/route.ts`, `server-actions.ts`, `approve/route.ts` (2 sites), `restore/route.ts` — todos fornecem `AiTelemetryContext`/sink desde o 46-04 Task 6.
- `AiTelemetryContext`/`AiTelemetrySink`/`DefaultAiTelemetrySink` em `src/lib/ai`; o sink é o único persistidor (`resolveAiCost` + `AiCostTracker.record`, fail-open).
- Delivery markers (`campaign_pipeline`, `brand_profile_without_logo`, `brand_profile_with_logo`, `visual_signature`) são a exceção documentada ao gate de persistência manual (não são chamadas de IA).
</interfaces>
</context>

<execution_order>
**DAG (legível por ferramenta):** `depends_on: [46-05]` (onda 6). **46-07 depende de 46-06** (`depends_on: [46-06]`, onda 7) por compartilhar `src/lib/visual-signature/server-actions.ts` e por estender o `architecture-guard.test.ts` criado aqui.

**Neutralização (2026-09-12):** as Tasks 1/2 originais (logo/retry/server-actions/approve/restore) foram antecipadas ao 46-04 Task 6 e removidas deste plano; a verificação fica no gate global + inventário da Task 3.
</execution_order>

<tasks>

<task type="auto">
  <name>Task 3: Gate global de arquitetura + teste de inventário + gates (6.5/6.6)</name>
  <files>src/lib/ai/__tests__/architecture-guard.test.ts, src/lib/ai/__tests__/telemetry-coverage.test.ts</files>
  <read_first>
    - design.md (:182 — teste de inventário)
    - src/lib/ai/gateway.ts (46-02)
    - src/lib/ai/telemetry-sink.ts (46-02)
    - .planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-01-SUMMARY.md (inventário dos 7 caminhos)
    - .planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-04-SUMMARY.md (callers convertidos)
  </read_first>
  <action>
    Criar `src/lib/ai/__tests__/architecture-guard.test.ts` (**gate global de arquitetura**, F46-30): varrer `src/**/*.{ts,tsx}` e **falhar** se, **fora de `src/lib/ai/adapters/**`**, existir init direto de SDK ou chamada de wire, com regexes exatas: `new\s+OpenAI\(`, `new\s+GoogleGenerativeAI\(`, `\.chat\.completions\.create\(`, `\.responses\.create\(`, `\.images\.edit\(`, `\.generateContent\(`. **Além disso**, proibir **fora de `src/lib/ai/**`** (sink) o uso em **chamadas** de IA: `await resolveAiCost\(` / `= resolveAiCost\(` (não a **definição** em `src/lib/ai-cost/cost-estimator.ts`) e `AiCostTracker` `\.record\(` — com allowlist explícita para **delivery markers** (tipos `campaign_pipeline`, `brand_profile_without_logo`, `brand_profile_with_logo`, `visual_signature` — incluindo o helper `generation-events.ts`, que não é chamada de IA), documentada no teste. **Excluir** o próprio arquivo do gate e `__tests__/**`/fixtures do scan (evita auto-match e matches em descrições/comentários) e ignorar comentários (`//`, `/* */`) antes de casar. Este gate é **global** (não depende de lista conhecida de callers). A **leitura de env-var de modelo** é adicionada a este gate no **46-07** (após a remoção das envs) — aqui o gate cobre SDK/wire e persistência manual.
    Criar `src/lib/ai/__tests__/telemetry-coverage.test.ts` (**inventário global**, não allowlist): varrer `src/app/**` e `src/lib/**` e enumerar todo arquivo produtivo que importa/chama um serviço de IA migrado (`CopyDirectorService`, `CorrectionIntentService`, `BrandTextOnlyInferenceService`, `OpenAIProvider` legado, `InputValidationService`, `ImageReviewService`, `BrandDirectorService`, `BrandProfilerWithoutLogoService`, `AiImageGenerator`, `ImageGenerationService`, `StoreIdentityArtDirectorService`), afirmando que cada um fornece `AiTelemetryContext`/sink e **não** chama `resolveAiCost`/`AiCostTracker.record` manualmente para IA. Cobrir explicitamente os callers convertidos: logo, retry-brand-director, `server-actions.ts`, approve, restore, `brand-profile/{infer,realign,generate-without-logo}`, `visual-signature/generate-without-logo`, `identity-art-director.ts`, `correction-reports.ts`. Rodar os 4 gates.
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai/__tests__/architecture-guard.test.ts src/lib/ai/__tests__/telemetry-coverage.test.ts; if ($?) { npm run typecheck; if ($?) { npm run lint; if ($?) { npm run build } } }</automated>
  </verify>
  <acceptance_criteria>
    - `architecture-guard.test.ts` verde: nenhuma inicialização direta de SDK/chamada de wire fora de `src/lib/ai/adapters/**` e nenhum `resolveAiCost`/`AiCostTracker.record` de IA fora de `src/lib/ai/**` (exceto delivery markers; env-var de modelo é gate do 46-07)
    - `telemetry-coverage.test.ts` (inventário global) verde, cobrindo logo, retry, server-actions, approve, restore, brand-profile/{infer,realign,generate-without-logo}, visual-signature/generate-without-logo, identity-art-director e correction-reports (todos já convertidos no 46-04)
    - `npx vitest run`, `npm run typecheck`, `npm run lint`, `npm run build` → 4 gates verdes
  </acceptance_criteria>
  <done>Cobertura total de telemetria verificada por gate global + inventário; 4 gates verdes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rota/action ↔ serviço de IA | Contexto de telemetria (run/store/campaign) flui para o gateway |
| telemetria ↔ banco | Eventos call-level best-effort |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-06a | Repudiation | Caminhos sem telemetria | mitigate | Gate global de arquitetura (`architecture-guard.test.ts`) + inventário de cobertura; `AiTelemetryContext` obrigatório nos callers (logo/retry/server-actions/approve/restore — convertidos no 46-04); nenhum caller resolve custo/grava manualmente (D9) |
| T-46-06b | Tampering | Buffering VS | mitigate | Sink injetado bufferiza até conhecer `visual_signature_id`; ordem preservada |
| T-46-06c | Denial of Service | Persistência | mitigate | Gravação best-effort (fail-open); geração nunca bloqueada |
| T-46-06d | Information Disclosure | Contexto de run | accept | IDs internos (store/campaign/run) — sem segredos |
| T-46-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo |
</threat_model>

<verification>
- `npx vitest run src/lib/ai/__tests__/architecture-guard.test.ts src/lib/ai/__tests__/telemetry-coverage.test.ts` → verde
- Gate global: `src/**` sem init de SDK/`chat.completions.create`/`responses.create`/`images.edit`/`generateContent` fora de `src/lib/ai/adapters/**` e sem `resolveAiCost`/`AiCostTracker.record` de IA fora de `src/lib/ai/**` (leitura de env-var de modelo é gate do 46-07)
- Inventário: logo/retry/server-actions/approve/restore/correction-reports/identity-art-director/VS generate-without-logo fornecem `AiTelemetryContext`; nenhum persiste manualmente
- `npm run typecheck`, `npm run lint`, `npm run build` → 0 erros
</verification>

<success_criteria>
- Gate global de arquitetura verde (nenhum caminho produtivo emite IA fora da camada única)
- Inventário de cobertura verde, verificando os callers antecipados (46-04 Task 6)
- Comportamento de negócio preservado; 4 gates verdes
</success_criteria>

<output>
Create `.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-06-SUMMARY.md` when done
</output>

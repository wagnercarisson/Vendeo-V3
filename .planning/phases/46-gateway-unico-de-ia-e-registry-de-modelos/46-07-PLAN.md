---
phase: 46-gateway-unico-de-ia-e-registry-de-modelos
plan: 07
type: execute
wave: 7
depends_on: [46-06]
files_modified:
  - src/lib/image-generation/config.ts
  - src/lib/text-provider/openai.ts
  - src/lib/text-provider/gemini.ts
  - src/lib/text-provider/factory.ts
  - src/lib/brand-assets/brand-director.ts
  - src/lib/brand-assets/text-only-inference-service.ts
  - src/lib/visual-signature/ai-image-generator.ts
  - src/lib/visual-signature/brand-profiler.ts
  - src/lib/visual-signature/server-actions.ts
  - src/lib/campaign-intelligence/providers/openai.ts
  - src/app/api/campaign/generate-image/route.ts
  - src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts
  - src/lib/ai/__tests__/architecture-guard.test.ts
  - .env.example
  - src/app/api/campaign/generate-image/__tests__/route.test.ts
  - src/app/api/campaign/generate-image/__tests__/campaign-generate.test.ts
  - src/app/api/campaign/generate-image/__tests__/concurrency.test.ts
  - src/app/api/campaign/generate-image/__tests__/regression-master-switch.test.ts
autonomous: true
requirements: [F46-32, F46-33, F46-34, F46-35, F46-36]

must_haves:
  truths:
    - "Nenhum serviço lê env-var de modelo/provider — grep `process.env.(OPENAI_MODEL|OPENAI_TEXT_MODEL|OPENAI_BRAND_DIRECTOR_MODEL|OPENAI_TEXT_ONLY_INFERENCE_MODEL|IMAGE_GENERATION_RESPONSES_MODEL|GPT_IMAGE_MODEL|IMAGE_EDIT_FALLBACK_MODEL|VISION_REVIEW_MODEL|IMAGE_VALIDATION_MODEL|IMAGE_PROVIDER|TEXT_PROVIDER|TEXT_FALLBACK_PROVIDER|GEMINI_TEXT_MODEL|GEMINI_MODEL)` retorna zero"
    - "`.env.example` mantém apenas chaves (`OPENAI_API_KEY`, `GEMINI_API_KEY`) + operacionais (timeout/qualidade/debug/fallback de custo); as 14 envs de modelo/provider foram removidas"
    - "O alvo de fallback inicial está registrado no registry (default `campaign_copy.fallback`) ANTES de remover `TEXT_FALLBACK_PROVIDER`; o orquestrador o aciona como segunda `invoke(..., target: \"fallback\")` sem conhecer o provider"
    - "As suites que referenciam `IMAGE_GENERATION_RESPONSES_MODEL`/`TEXT_FALLBACK_PROVIDER` foram co-migradas"
    - "A ordem de deploy está registrada: (1) código que lê só chaves; (2) remoção das envs na Vercel — nunca o inverso"
  artifacts:
    - path: "src/lib/image-generation/config.ts"
      provides: "Config sem envs de modelo/provider"
      contains: "IMAGE_GENERATION_QUALITY"
    - path: ".env.example"
      provides: "Chaves + operacionais apenas"
      contains: "OPENAI_API_KEY"
    - path: "src/app/api/campaign/generate-image/__tests__/route.test.ts"
      provides: "Suites co-migradas sem TEXT_FALLBACK_PROVIDER"
      contains: "target"
  key_links:
    - from: ".env.example"
      to: "src/lib/ai/api-keys.ts"
      via: "única configuração de IA em env-var são as chaves"
      pattern: "OPENAI_API_KEY"
    - from: "src/app/api/campaign/generate-image/route.ts"
      to: "src/lib/ai/model-registry.ts"
      via: "fallback inicial no registry antes de remover TEXT_FALLBACK_PROVIDER"
      pattern: "target: \"fallback\""
---

<objective>
Remover as 14 env-vars de modelo/provider do runtime e do `.env.example`, deixando apenas chaves + operacionais: (7.1) grep-confirmação de que nenhum serviço lê env-var de modelo/provider + remoção de leituras residuais; (7.2) atualizar `.env.example`; (7.3) co-migrar os testes que referenciam `IMAGE_GENERATION_RESPONSES_MODEL`/`TEXT_FALLBACK_PROVIDER`; (7.4) confirmar que o alvo de fallback inicial está no registry antes de remover `TEXT_FALLBACK_PROVIDER`; (7.5) registrar a ordem de deploy.

Purpose: D5/D8 (envs removidas só após o corte; ordem de deploy), F46-32..F46-36.
Output: runtime e `.env.example` sem envs de modelo; testes co-migrados; ordem de deploy registrada.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-CONTEXT.md (D5 :127-128, D8 :137-138; F46-32..F46-36 :272-277)
@openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/specs/ai-model-registry/spec.md (:122-130)
@openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/design.md (:160-165 — D5; :186-190 — D8; :229-236 — Migration Plan)
@.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-01-SUMMARY.md (inventário das 14 envs com file:line)
@src/lib/image-generation/config.ts (:1-43)
@src/lib/text-provider/{openai,gemini,factory}.ts
@src/lib/brand-assets/{brand-director,text-only-inference-service}.ts
@src/lib/visual-signature/{ai-image-generator,brand-profiler,server-actions}.ts
@src/lib/campaign-intelligence/providers/openai.ts
@.env.example (:1-52)

<interfaces>
**Ficam (chaves):** `OPENAI_API_KEY`, `GEMINI_API_KEY`.
**Ficam (operacionais):** `IMAGE_GENERATION_GLOBAL_TIMEOUT_MS`, `IMAGE_GENERATION_QUALITY`, `IMAGE_GENERATION_DEBUG`, `METRICS_ENABLED`, `VENDEO_AI_FALLBACK_COST_USD`/`VENDEO_IMAGE_GENERATION_FALLBACK_COST_USD`, `VENDEO_AI_CREDIT_UNIT_USD_VALUE`.
**Saem (14):** `OPENAI_MODEL`, `OPENAI_TEXT_MODEL`, `OPENAI_BRAND_DIRECTOR_MODEL`, `OPENAI_TEXT_ONLY_INFERENCE_MODEL`, `IMAGE_GENERATION_RESPONSES_MODEL`, `GPT_IMAGE_MODEL`, `IMAGE_EDIT_FALLBACK_MODEL`, `VISION_REVIEW_MODEL`, `IMAGE_VALIDATION_MODEL`, `IMAGE_PROVIDER`, `TEXT_PROVIDER`, `TEXT_FALLBACK_PROVIDER`, `GEMINI_TEXT_MODEL`, `GEMINI_MODEL`.
Ordem de deploy (D5): (1) deploy do código que lê só chaves; (2) remoção das envs na Vercel. **Nunca o inverso.**
</interfaces>
</context>

<execution_order>
**DAG (legível por ferramenta):** `depends_on: [46-06]` (onda 7) — roda **depois** do 46-06 (compartilha `src/lib/visual-signature/server-actions.ts` e estende o `architecture-guard.test.ts` criado lá com a proibição de env-var de modelo). **46-08 depende de 46-07** (`depends_on: [46-07]`, onda 8). O volume de arquivos (18, majoritariamente co-migração de testes) é mandado pelo `tasks.md`; se a execução degradar, avaliar split do item 7.3 (co-migração de testes).
</execution_order>

<tasks>

<task type="auto">
  <name>Task 1: Grep-confirmação + remoção de leituras residuais de env de modelo/provider (7.1)</name>
  <files>src/lib/image-generation/config.ts, src/lib/text-provider/openai.ts, src/lib/text-provider/gemini.ts, src/lib/text-provider/factory.ts, src/lib/brand-assets/brand-director.ts, src/lib/brand-assets/text-only-inference-service.ts, src/lib/visual-signature/ai-image-generator.ts, src/lib/visual-signature/brand-profiler.ts, src/lib/visual-signature/server-actions.ts, src/lib/campaign-intelligence/providers/openai.ts, src/app/api/campaign/generate-image/route.ts, src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts</files>
  <read_first>
    - .planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-01-SUMMARY.md (inventário das 28 leituras com file:line)
    - src/lib/image-generation/config.ts (:1-43)
    - src/app/api/campaign/generate-image/route.ts (:684)
    - src/lib/visual-signature/server-actions.ts (:65)
  </read_first>
  <action>
    Rodar grep `process.env.(OPENAI_MODEL|OPENAI_TEXT_MODEL|OPENAI_BRAND_DIRECTOR_MODEL|OPENAI_TEXT_ONLY_INFERENCE_MODEL|IMAGE_GENERATION_RESPONSES_MODEL|GPT_IMAGE_MODEL|IMAGE_EDIT_FALLBACK_MODEL|VISION_REVIEW_MODEL|IMAGE_VALIDATION_MODEL|IMAGE_PROVIDER|TEXT_PROVIDER|TEXT_FALLBACK_PROVIDER|GEMINI_TEXT_MODEL|GEMINI_MODEL)` em `src/` e remover as leituras residuais que ainda existirem (mantendo apenas `OPENAI_API_KEY`/`GEMINI_API_KEY` via `api-keys.ts`). Remover as constantes de modelo/provider de `config.ts` (`IMAGE_GENERATION_RESPONSES_MODEL`, `GPT_IMAGE_MODEL`, `IMAGE_EDIT_FALLBACK_MODEL`, `VISION_REVIEW_MODEL`, `IMAGE_PROVIDER`), preservando as operacionais (`IMAGE_GENERATION_QUALITY`, `IMAGE_GENERATION_SIZE`, `IMAGE_GENERATION_GLOBAL_TIMEOUT_MS`, `IMAGE_GENERATION_DEBUG`, limites). Ajustar todos os imports das constantes removidas para o registry/gateway. Grep final → zero.
    **Estender o gate global (F46-30/46-07):** adicionar a `src/lib/ai/__tests__/architecture-guard.test.ts` (criado no 46-06) a proibição de **leitura de env-var de modelo** (`process\.env\.(OPENAI_MODEL|...|GEMINI_MODEL)`) **fora de `src/lib/ai/adapters/**`** — agora que as envs foram removidas, o gate deve ficar verde. Manter as exclusões de teste/comentário já definidas no 46-06.
  </action>
  <verify>
    <automated>$hits = Get-ChildItem -Path "src" -Recurse -Include *.ts,*.tsx | Select-String -Pattern "process\.env\.(OPENAI_MODEL|OPENAI_TEXT_MODEL|OPENAI_BRAND_DIRECTOR_MODEL|OPENAI_TEXT_ONLY_INFERENCE_MODEL|IMAGE_GENERATION_RESPONSES_MODEL|GPT_IMAGE_MODEL|IMAGE_EDIT_FALLBACK_MODEL|VISION_REVIEW_MODEL|IMAGE_VALIDATION_MODEL|IMAGE_PROVIDER|TEXT_PROVIDER|TEXT_FALLBACK_PROVIDER|GEMINI_TEXT_MODEL|GEMINI_MODEL)"; if ($hits) { Write-Error "Leituras residuais: $($hits.Count)" } else { Write-Output "OK: zero leituras de env de modelo/provider" }; npx vitest run src/lib/ai/__tests__/architecture-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Grep das 14 envs em `src/` → 0 ocorrências
    - `config.ts` sem as constantes de modelo/provider; operacionais preservadas
    - `architecture-guard.test.ts` estendido (proíbe env-var de modelo fora de `src/lib/ai/adapters/**`) → verde
    - Nenhum import quebrado (typecheck passa)
  </acceptance_criteria>
  <done>Runtime sem leituras de env-var de modelo/provider</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar .env.example + confirmar fallback no registry antes de remover TEXT_FALLBACK_PROVIDER (7.2/7.4)</name>
  <files>.env.example</files>
  <read_first>
    - .env.example (:1-52)
    - openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/specs/ai-model-registry/spec.md (:96-104, :122-130)
    - src/lib/ai/model-registry.ts (campaign_copy.fallback)
  </read_first>
  <action>
    Atualizar `.env.example`: manter `OPENAI_API_KEY`, `GEMINI_API_KEY` (remover o comentário de "Gemini futuro não implementado" que confunda a chave com modelo) e as operacionais (`IMAGE_GENERATION_GLOBAL_TIMEOUT_MS`, `IMAGE_GENERATION_QUALITY`, `IMAGE_GENERATION_DEBUG`, `METRICS_ENABLED`, `VENDEO_AI_FALLBACK_COST_USD`/`VENDEO_IMAGE_GENERATION_FALLBACK_COST_USD`, `VENDEO_AI_CREDIT_UNIT_USD_VALUE`). Remover as 14 envs de modelo/provider (`OPENAI_MODEL`, `IMAGE_PROVIDER`, `IMAGE_GENERATION_RESPONSES_MODEL`, `VISION_REVIEW_MODEL`, `TEXT_FALLBACK_PROVIDER`, `GEMINI_TEXT_MODEL`, etc.). Adicionar nota curta de que a escolha de modelo/provider é do registry em código.
    **Antes** de remover `TEXT_FALLBACK_PROVIDER`, confirmar por leitura de `model-registry.ts` que `campaign_copy.fallback` está registrado (provider/model/protocol) e que o orquestrador o aciona como segunda `invoke(..., target: "fallback")` sem conhecer o provider (verificar `route.ts` sem `TEXT_FALLBACK_PROVIDER`). Registrar essa confirmação no SUMMARY.
  </action>
  <verify>
    <automated>Select-String -LiteralPath ".env.example" -Pattern "OPENAI_API_KEY|GEMINI_API_KEY|IMAGE_GENERATION_QUALITY|IMAGE_GENERATION_DEBUG"; if (Select-String -LiteralPath ".env.example" -Pattern "OPENAI_MODEL|IMAGE_PROVIDER|VISION_REVIEW_MODEL|TEXT_FALLBACK_PROVIDER|GEMINI_TEXT_MODEL" -Quiet) { Write-Error "Envs de modelo ainda no .env.example" } else { Write-Output "OK: .env.example sem envs de modelo" }</automated>
  </verify>
  <acceptance_criteria>
    - `.env.example` contém `OPENAI_API_KEY`/`GEMINI_API_KEY` + operacionais
    - Nenhuma das 14 envs de modelo/provider presente
    - `campaign_copy.fallback` confirmado no registry antes da remoção; orquestrador sem `TEXT_FALLBACK_PROVIDER`
  </acceptance_criteria>
  <done>.env.example limpo; fallback inicial confirmado no registry</done>
</task>

<task type="auto">
  <name>Task 3: Co-migrar testes que referenciam IMAGE_GENERATION_RESPONSES_MODEL/TEXT_FALLBACK_PROVIDER (7.3)</name>
  <files>src/app/api/campaign/generate-image/__tests__/route.test.ts, src/app/api/campaign/generate-image/__tests__/campaign-generate.test.ts, src/app/api/campaign/generate-image/__tests__/concurrency.test.ts, src/app/api/campaign/generate-image/__tests__/regression-master-switch.test.ts</files>
  <read_first>
    - src/app/api/campaign/generate-image/__tests__/route.test.ts (:920-965, :1270-1530)
    - .planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-01-SUMMARY.md (baseline de testes)
  </read_first>
  <action>
    Co-migrar as suites que referenciam `IMAGE_GENERATION_RESPONSES_MODEL` (`generate-image/route.test.ts`, `campaign-generate.test.ts`, `concurrency.test.ts`, `regression-master-switch.test.ts`) e `TEXT_FALLBACK_PROVIDER` (`generate-image/route.test.ts`), substituindo por expectativas baseadas no registry/gateway (modelo real do envelope; fallback como `target: "fallback"`), preservando as asserções de comportamento observável.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/campaign/generate-image</automated>
  </verify>
  <acceptance_criteria>
    - Nenhuma das 4 suites referencia `IMAGE_GENERATION_RESPONSES_MODEL`/`TEXT_FALLBACK_PROVIDER`
    - Asserções de comportamento preservadas; testes verdes
  </acceptance_criteria>
  <done>Suites de rota co-migradas sem envs de modelo</done>
</task>

<task type="auto">
  <name>Task 4: Registrar ordem de deploy + gates (7.5)</name>
  <files>.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-07-SUMMARY.md</files>
  <read_first>
    - design.md (:160-165 — D5 ordem de deploy)
    - .planning/phases/42-signup-controlado-elegibilidade-freemium/42-12-PLAN.md (padrão de registro de deploy)
  </read_first>
  <action>
    Registrar no SUMMARY a **ordem de deploy (D5)**: (1) deploy do código que lê apenas as chaves de API; (2) somente depois remover as 14 envs de modelo/provider da Vercel. Documentar que nunca se faz o inverso e que o default do registry é o comportamento efetivo (sem override por env). Rodar os 4 gates.
  </action>
  <verify>
    <automated>npx vitest run; if ($?) { npm run typecheck; if ($?) { npm run lint; if ($?) { npm run build } } }</automated>
  </verify>
  <acceptance_criteria>
    - Ordem de deploy registrada no SUMMARY (código lê só chaves → remoção na Vercel)
    - `npx vitest run`, `npm run typecheck`, `npm run lint`, `npm run build` → 4 gates verdes
  </acceptance_criteria>
  <done>Envs removidas do runtime/.env.example; ordem de deploy registrada; 4 gates verdes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime ↔ env | Apenas chaves de API + operacionais; nenhuma env de modelo |
| deploy ↔ Vercel | Ordem de remoção de envs determina indisponibilidade |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-07a | Denial of Service | Remoção prematura de envs | mitigate | Ordem de deploy registrada: código lê só chaves primeiro; remoção na Vercel depois. **Ausência de API key falha explicitamente** (fail-fast em produção via `getApiKey`) — o registry cobre a **remoção das env-vars de modelo**, não a ausência de chave |
| T-46-07b | Tampering | Fallback inicial | mitigate | Confirmar `campaign_copy.fallback` no registry **antes** de remover `TEXT_FALLBACK_PROVIDER`; orquestrador sem provider hardcoded |
| T-46-07c | Information Disclosure | `.env.example` | mitigate | Apenas nomes de chaves e operacionais; nenhum segredo real |
| T-46-07d | Configuration Drift | `config.ts` | mitigate | Constantes de modelo/provider removidas; registry é a fonte única |
| T-46-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo |
</threat_model>

<verification>
- Grep das 14 envs em `src/` e `.env.example` → 0 ocorrências
- `npx vitest run src/app/api/campaign/generate-image` → verde
- `npx vitest run`, `npm run typecheck`, `npm run lint`, `npm run build` → 4 gates verdes
- Ordem de deploy registrada no SUMMARY
</verification>

<success_criteria>
- Runtime e `.env.example` com apenas chaves + operacionais
- Fallback inicial no registry antes da remoção; testes co-migrados
- Ordem de deploy registrada; 4 gates verdes
</success_criteria>

<output>
Create `.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-07-SUMMARY.md` when done
</output>

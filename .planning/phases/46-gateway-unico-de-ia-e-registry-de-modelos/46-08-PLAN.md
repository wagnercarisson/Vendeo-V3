---
phase: 46-gateway-unico-de-ia-e-registry-de-modelos
plan: 08
type: execute
wave: 8
depends_on: [46-07]
files_modified:
  - .planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-08-SUMMARY.md
autonomous: true
requirements: [F46-37, F46-38, F46-39]

must_haves:
  truths:
    - "A regressão completa (`npx vitest run`) está verde; resíduos de fixtures/asserções foram corrigidos"
    - "`npm run typecheck`, `npm run lint` e `npm run build` estão verdes"
    - "As suites de não-mudança (UI/form, schema público, snapshot, domínio, prompts) permanecem intactas"
    - "A equivalência de defaults registry × comportamento pré-F46 está demonstrada (mesmos provider/modelo por capacidade)"
    - "Não há drift nos prompts (nenhum arquivo de prompt alterado pela F46)"
  artifacts:
    - path: ".planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-08-SUMMARY.md"
      provides: "Regressão + não-mudança do contrato externo + equivalência de defaults"
      contains: "equivalência de defaults"
  key_links:
    - from: "src/lib/ai/model-registry.ts"
      to: "comportamento pré-F46"
      via: "equivalência de defaults por capacidade"
      pattern: "gpt-4o"
    - from: "suites de não-mudança"
      to: "contrato externo"
      via: "nenhuma asserção de UI/form/schema/snapshot/domínio/prompts alterada"
      pattern: "snapshot"
---

<objective>
Fechar a fase com regressão e não-mudança: (8.1) rodar a regressão completa (vitest total) e corrigir resíduos de fixtures/asserções; (8.2) rodar typecheck/lint/build e verificar UI/form/schema público/snapshot/domínio/prompts intactos; (8.3) verificar equivalência de defaults (registry × comportamento pré-F46) e ausência de drift nos prompts.

Purpose: F46-37..F46-39 (garantia de behavior-preserving e superfície externa intacta).
Output: `46-08-SUMMARY.md` com regressão verde, 4 gates e equivalência de defaults demonstrada.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-CONTEXT.md (F46-37..F46-39 :279-282; constraints :45-56)
@openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/specs/ai-invocation-gateway/spec.md (:168-181 — comportamento preservado)
@.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-01-SUMMARY.md (baseline de defaults/testes)
@.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-03-PLAN.md
@.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-04-PLAN.md
@.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-05-PLAN.md
@.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-06-PLAN.md
@.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-07-PLAN.md

<interfaces>
Suites de não-mudança (não devem ter asserção alterada pela F46): UI/form (`use-campaign-form`), schema público (`GenerateImageRequestSchema`), snapshot/domínio (`CampaignBrief`/`brief.ts`), prompts (`prompts/*.md`, golden de keys). Superfície congelada: contrato HTTP, schema, snapshot, domínio, prompts.
Equivalência de defaults (design :66-78): `campaign_copy`/`campaign_correction_analysis`/`brand_profile_text`/`campaign_input_validation`/`campaign_image_review`/`brand_profile_vision` → gpt-4o; `campaign_spec`/`visual_signature_validation` → gpt-4o-mini; `campaign_image`/`visual_signature_image` → gpt-5.5; `campaign_image_edit` → gpt-image-2.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regressão completa + correção de resíduos de fixtures/asserções (8.1)</name>
  <files>.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-08-SUMMARY.md</files>
  <read_first>
    - .planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-01-SUMMARY.md (baseline de testes)
    - openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/design.md (:192-200 — Risks: blast radius)
  </read_first>
  <action>
    Rodar a regressão completa (`npx vitest run`). Para cada falha, corrigir o **resíduo** (fixture/asserção que ainda depende de env-var de modelo, instanciação de provider ou mock antigo), sem alterar comportamento observável. Não alterar asserções de contrato externo (UI/form/schema/snapshot/domínio/prompts) — se uma dessas falhar, é regressão real e deve ser corrigida no código migrado, não no teste. Registrar a contagem total de testes e as falhas corrigidas no SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` → 100% verde (sem falhas)
    - Resíduos de fixtures/asserções corrigidos; nenhuma asserção de contrato externo alterada
    - Contagem total de testes registrada no SUMMARY
  </acceptance_criteria>
  <done>Regressão completa verde</done>
</task>

<task type="auto">
  <name>Task 2: typecheck/lint/build + verificação de não-mudança do contrato externo (8.2)</name>
  <files>.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-08-SUMMARY.md</files>
  <read_first>
    - openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/proposal.md (:36-54 — Impact; "sem mudança de UI/form/schema/snapshot/domínio")
    - design.md (:36-42 — Non-Goals)
  </read_first>
  <action>
    Rodar `npm run typecheck`, `npm run lint`, `npm run build`. Em paralelo, verificar por git diff que **nenhum** arquivo de UI/form/schema público/snapshot/domínio/prompts foi alterado pela fase (listar no SUMMARY os arquivos intocados como evidência de não-mudança): `use-campaign-form`, `GenerateImageRequestSchema` (`schema.ts`), `brief.ts`/snapshot, `prompts/*.md`. Se algum tiver sido tocado, é violação de escopo — reverter e corrigir.
  </action>
  <verify>
    <automated>npm run typecheck; if ($?) { npm run lint; if ($?) { npm run build } }</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck`, `npm run lint`, `npm run build` → 0 erros
    - `git diff --name-only` não mostra arquivos de UI/form/schema público/snapshot/domínio/prompts
    - Evidência de não-mudança registrada no SUMMARY
  </acceptance_criteria>
  <done>4 gates verdes + contrato externo intacto verificado</done>
</task>

<task type="auto">
  <name>Task 3: Equivalência de defaults (registry × pré-F46) + ausência de drift nos prompts (8.3)</name>
  <files>.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-08-SUMMARY.md</files>
  <read_first>
    - .planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-01-SUMMARY.md (baseline de defaults pré-F46)
    - src/lib/ai/model-registry.ts
    - openspec/changes/fase-46-gateway-unico-de-ia-e-registry-de-modelos/specs/ai-model-registry/spec.md (:35-48)
  </read_first>
  <action>
    Comparar, capacidade a capacidade, o default do registry com o valor efetivo pré-F46 registrado no baseline do 46-01 e documentar a **equivalência** em tabela no SUMMARY (11 linhas; nenhuma divergência). Verificar ausência de drift nos prompts: `git diff` dos `prompts/*.md` vazio. Registrar também a lista final de envs removidas (14) e a confirmação de que restam apenas chaves + operacionais.
  </action>
  <verify>
    <automated>Select-String -LiteralPath ".planning\phases\46-gateway-unico-de-ia-e-registry-de-modelos\46-08-SUMMARY.md" -Pattern "equivalência de defaults|gpt-image-2|gpt-4o-mini|sem drift"</automated>
  </verify>
  <acceptance_criteria>
    - Tabela de equivalência de defaults (11 capacidades) no SUMMARY, sem divergências
    - `git diff` dos `prompts/*.md` vazio (sem drift)
    - Lista final das 14 envs removidas + chaves/operacionais remanescentes
  </acceptance_criteria>
  <done>Equivalência de defaults demonstrada e prompts sem drift</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| código migrado ↔ contrato externo | Nenhuma mudança de superfície pública |
| registry ↔ comportamento histórico | Equivalência de defaults deve ser verificada |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-08a | Tampering | Suites de não-mudança | mitigate | `git diff` proíbe alteração de UI/form/schema/snapshot/domínio/prompts; falha nessas suites é regressão real |
| T-46-08b | Repudiation | Equivalência de defaults | mitigate | Comparação capacidade a capacidade com o baseline do 46-01, documentada no SUMMARY |
| T-46-08c | Tampering | Prompts | mitigate | `git diff` dos `prompts/*.md` vazio (sem drift) |
| T-46-SC | Tampering | Instalações de pacotes | accept | Nenhum pacote novo |
</threat_model>

<verification>
- `npx vitest run` → 100% verde
- `npm run typecheck`, `npm run lint`, `npm run build` → 0 erros
- `git diff --name-only` sem UI/form/schema público/snapshot/domínio/prompts
- Tabela de equivalência de defaults no SUMMARY; prompts sem drift
</verification>

<success_criteria>
- Regressão completa verde + 4 gates verdes
- Contrato externo intacto (UI/form/schema/snapshot/domínio/prompts)
- Equivalência de defaults demonstrada; prompts sem drift
</success_criteria>

<output>
Create `.planning/phases/46-gateway-unico-de-ia-e-registry-de-modelos/46-08-SUMMARY.md` when done
</output>

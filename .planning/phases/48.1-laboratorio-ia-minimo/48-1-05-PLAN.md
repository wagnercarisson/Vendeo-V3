---
phase: 48.1
plan: 48-1-05
type: execute
wave: 3
depends_on:
  - 48-1-03
  - 48-1-04
files_modified:
  - src/lib/image-generation/services/image-generation-service.ts
  - src/lib/lab/gateway/noop-image-provider.ts
  - src/lib/lab/gateway/lab-model-resolver.ts
  - src/lib/lab/gateway/lab-prompt-loader.ts
  - src/lib/lab/gateway/runtime.ts
  - src/lib/lab/domain/cost-coverage.ts
  - src/lib/ai/lab-telemetry-sink.ts
  - src/lib/image-generation/services/__tests__/image-generation-service.build-director-prompt.test.ts
  - src/lib/lab/gateway/__tests__/lab-model-resolver.test.ts
  - src/lib/lab/gateway/__tests__/lab-prompt-loader.test.ts
  - src/lib/lab/gateway/__tests__/runtime.test.ts
  - src/lib/lab/domain/__tests__/cost-coverage.test.ts
  - src/lib/ai/__tests__/lab-telemetry-sink.test.ts
autonomous: true
requirements:
  - lab-gateway-harness
  - lab-runs
must_haves:
  truths:
    - "ImageGenerationService expõe buildDirectorPrompt(brief, context) público e aditivo, reutilizando buildPromptVariables + assemblePrompt, sem mudar o comportamento de generateImage"
    - "Para 'campaign_image' o LabModelResolver devolve o alvo fixo do experimento; para qualquer outra capacidade delega ao resolver padrão sem consultar/alterar ai_model_selection"
    - "O LabPromptLoader serve o prompt do snapshot da variante quando o nome casa e delega ao loader fs caso contrário; nenhum arquivo em prompts/ é escrito"
    - "O LabTelemetrySink calcula custo via resolveAiCost em modo leitura, preserva a CostResolution real completa e NÃO chama AiCostTracker.record nem grava generation_events"
    - "O runtime invoca campaign_image diretamente no gateway (sem OpenAIImageProvider) e produz exatamente 1 envelope por run (fallback automático desabilitado)"
    - "A cobertura de custo complete|partial|missing é derivada no domínio do laboratório a partir dos campos reais de CostResolution"
    - "O pipeline de produção (generateImage + OpenAIImageProvider) permanece intacto e seus testes existentes continuam verdes"
  artifacts:
    - path: "src/lib/image-generation/services/image-generation-service.ts"
      provides: "seam aditivo público buildDirectorPrompt"
      contains: "buildDirectorPrompt"
    - path: "src/lib/lab/gateway/lab-model-resolver.ts"
      provides: "AiModelResolver com alvo fixo para campaign_image e delegação fora do escopo"
      exports: ["LabModelResolver"]
    - path: "src/lib/lab/gateway/lab-prompt-loader.ts"
      provides: "PromptLoader com override do snapshot da variante"
      exports: ["LabPromptLoader"]
    - path: "src/lib/ai/lab-telemetry-sink.ts"
      provides: "sink capturador com resolveAiCost em leitura e acúmulo de envelopes sanitizados"
      exports: ["LabTelemetrySink"]
    - path: "src/lib/lab/gateway/runtime.ts"
      provides: "composição do gateway laboratorial + invocação single-shot de campaign_image"
      exports: ["createLabGateway", "createLabTelemetryContext", "runLabCampaignImage", "createDefaultLabRuntime"]
    - path: "src/lib/lab/domain/cost-coverage.ts"
      provides: "derivação de cobertura complete|partial|missing a partir de CostResolution"
      exports: ["deriveCostCoverage"]
  key_links:
    - from: "src/lib/lab/gateway/runtime.ts"
      to: "src/lib/ai/gateway.ts"
      via: "AiGateway(resolver, adapters).invoke('campaign_image', request, telemetry) — uma única chamada"
      pattern: "campaign_image"
    - from: "src/lib/ai/lab-telemetry-sink.ts"
      to: "src/lib/ai-cost/cost-estimator.ts"
      via: "resolveAiCost em modo leitura com generationType de CAPABILITY_GENERATION_TYPE e imageGenerationTool de usageMeta"
      pattern: "resolveAiCost"
    - from: "src/lib/lab/gateway/lab-prompt-loader.ts"
      to: "src/lib/image-generation/prompt-loader.ts"
      via: "extends PromptLoader delegando ao loader fs quando o prompt não está sob teste"
      pattern: "extends PromptLoader"
    - from: "src/lib/lab/gateway/noop-image-provider.ts"
      to: "src/lib/image-generation/services/image-generation-service.ts"
      via: "provider stub que nunca é invocado, usado apenas para instanciar o serviço e chamar buildDirectorPrompt"
      pattern: "buildDirectorPrompt"
---

<objective>
Entregar o harness de gateway isolado do laboratório: o seam aditivo `buildDirectorPrompt` em `ImageGenerationService`, o `LabModelResolver` (alvo fixo), o `LabPromptLoader` (override do snapshot), o `LabTelemetrySink` (custo em leitura, sem `generation_events`), a derivação de cobertura de custo e o runtime single-shot que invoca `campaign_image` direto no gateway.

Purpose: executar a geração real pelo caminho do diretor **sem** reabrir o gateway F46, **sem** o fallback automático do `OpenAIImageProvider` (garantindo exatamente 1 chamada paga por run) e **sem** persistir telemetria produtiva (D6/D7/D8/D14/D15/D18). Sem isso, um erro de capability poderia disparar uma segunda chamada paga e a comparação A/B deixaria de ser justa.
Output: harness testado com fakes, provando alvo fixo, delegação, override sem tocar prompts oficiais, zero `generation_events` e um único envelope por run.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48.1-laboratorio-ia-minimo/48.1-CONTEXT.md
@openspec/changes/fase-48-1-laboratorio-ia-minimo/design.md
@openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-gateway-harness/spec.md
@openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-runs/spec.md
@src/lib/ai/gateway.ts
@src/lib/ai/telemetry-sink.ts
@src/lib/ai/model-resolver.ts
@src/lib/ai/types.ts
@src/lib/ai/generation-type-map.ts
@src/lib/ai-cost/types.ts
@src/lib/image-generation/prompt-loader.ts
@src/lib/image-generation/providers/types.ts

<interfaces>
Contratos extraídos do código (usar diretamente — não explorar o repositório):

- `src/lib/ai/gateway.ts` — `class AiGateway { constructor(resolver: AiModelResolver, adapters: AiAdapterRegistry); invoke(capability, request, telemetry, target: "primary" | "fallback" = "primary"): Promise<AiInvocationResult>; hasFallback(capability): Promise<boolean> }`. Uma tentativa por `invoke`, **sem fallback automático**; emite **um** `AiCallEnvelope` por tentativa via `telemetry.sink`. **Não alterar.**
- `src/lib/ai/model-resolver.ts` — `AiCapability` (11 valores), `AiProtocol` (`chat-completions`|`responses`|`images`|`gemini`), `AiProvider` (`openai`|`gemini`), `AiModelTarget { provider, model, protocol }`, `AiModelConfig { capability, segment: "text"|"vision"|"image", primary, fallback? }`, `interface AiModelResolver { resolve(capability): Promise<AiModelConfig>; listCapabilities(): AiCapability[] }`.
- `src/lib/ai/types.ts` — `AiInvocationRequest { prompt: string; system?; messages?; productImagesDataUrls?: string[]; imageDetail?; identityImageUrl?: string; size?: string; quality?: string; responseFormat?; jsonSchema?; tools?: "image_generation"; maxTokens?; temperature?; signal?: AbortSignal; timeout? }`; `AiInvocationResult { content?; imageBase64?; mimeType?; model: string; usage?; usageMeta?; providerReportedCostUsd? }`; `AiCallEnvelope extends AiCallInfo { capability; protocol; status: "success"|"failed"|"timeout"; errorType?; usageMeta?; metadata? }`; `AiTelemetryContext { operationRunId; operationRunType: OperationRunType; traceId; storeId; userId?; campaignId?; visualSignatureId?; themeId?; attemptNumber?; sink: AiTelemetrySink }`; `sanitizeAiErrorMessage(message): string`.
- `src/lib/ai-cost/types.ts` — `CostResolution { estimatedCostUsd: number | null; providerReportedCostUsd?: number | null; costSource: "provider_reported"|"pricing_table"|"fallback_static"|"manual_unknown"|"not_available"; pricingVersion?: string | null; costFormulaVersion?: string; costEstimationNote?: string; textComponentUsd?: number; imageToolComponentUsd?: number; imageToolPricingProvider?: string; imageToolPricingModel?: string; imageToolPricingVersion?: string }`. **Não existe** `imageUnitUsd` nem `costPartial`. `OPERATION_RUN_TYPES` tem exatamente 4 valores e inclui `"campaign_delivery"` (travado por teste) — **não ampliar**.
- `src/lib/ai/telemetry-sink.ts` — `DefaultAiTelemetrySink` (referência de como derivar `imageGenerationTool`: `envelope.usageMeta?.imageGenerationTool === true` e `generationType = CAPABILITY_GENERATION_TYPE[envelope.capability]`). `NoopAiTelemetrySink`, `BufferingAiTelemetrySink` também existem.
- `src/lib/ai-cost/cost-estimator.ts:149` — `resolveAiCost({ provider, model, usage?, providerReportedCostUsd?, manualCostUsd?, imageGenerationTool?, generationType? }): Promise<CostResolution>`. O componente de imagem só é somado quando `imageGenerationTool === true` **e** `generationType ∈ { campaign_image, visual_signature_image }`.
- `src/lib/image-generation/prompt-loader.ts` — `class PromptLoader { constructor(promptsDir?: string); load(name: string, variables?: Record<string,string>): string; clearCache(): void }`.
- `src/lib/image-generation/services/image-generation-service.ts` — `constructor(imageProvider: ImageProvider, promptLoader?: PromptLoader, inputValidation?: InputValidationService, imageReview?: ImageReviewService, metricsWriter?: MetricsWriter)`; `private buildPromptVariables(brief, context, effectiveProductName, inferredCategory?)` (~:805); `private assemblePrompt(state, variables, previousIssues, normalizedInstruction?)` (~:841) que carrega `campaign-image-director-${intent}` via `promptLoader.load(promptName, variables)`. `enum GenerationState { INITIAL, REVIEW, CORRECT, REGENERATE, COMPLETE, ERROR }`. O override `brief_review_confirmed` já existe em `:221-224`/`:332`.
- `src/lib/image-generation/providers/types.ts` — `interface ImageProvider { readonly name: string; generateImage(input: ImageProviderInput): Promise<ImageProviderOutput> }`.
- `src/lib/image-generation/config.ts` — `IMAGE_GENERATION_SIZE = "1024x1024"`, `IMAGE_GENERATION_QUALITY = process.env.IMAGE_GENERATION_QUALITY || "auto"`.
- Request real de `campaign_image`: `{ prompt, productImagesDataUrls, identityImageUrl, tools: "image_generation", size, quality, signal }`.
- **Importante para testes:** `@/lib/ai` (index) importa `src/lib/supabase/server.ts`, que lança no load sem env vars. Por isso `runtime.ts` deve usar **dynamic import** em `createDefaultLabRuntime` e os testes devem injetar fakes, nunca importar `@/lib/ai` no topo de um módulo carregado por teste.
- Padrão de teste com mock de custo: `src/lib/ai/__tests__/telemetry-sink.test.ts` usa `vi.hoisted` + `vi.mock("@/lib/ai-cost/cost-estimator", ...)` e `vi.mock("@/lib/ai-cost/tracker", ...)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seam aditivo buildDirectorPrompt em ImageGenerationService + provider stub</name>
  <files>src/lib/image-generation/services/image-generation-service.ts, src/lib/lab/gateway/noop-image-provider.ts, src/lib/image-generation/services/__tests__/image-generation-service.build-director-prompt.test.ts</files>
  <read_first>
    - `src/lib/image-generation/services/image-generation-service.ts` — ctor (:106-118), `buildPromptVariables` (:805-839), `assemblePrompt` (:841-868), `buildNonConformityBlock` (:877-883), o uso em `generateImage` (:370 e o ponto de chamada de `assemblePrompt` na fase 3-4)
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/design.md` — "D7 — Caminho de execução" (itens 1 e 6)
    - `.planning/phases/48.1-laboratorio-ia-minimo/48.1-CONTEXT.md` — D7 e DV-4
    - `src/lib/image-generation/providers/types.ts` — `ImageProvider`
    - `src/lib/campaign/brief.ts` e `src/components/campaign/types.ts` — `CampaignBrief` e `ResolvedCampaignContext`
  </read_first>
  <action>
Adicionar a `src/lib/image-generation/services/image-generation-service.ts` (task 5.1) um método **público de instância**, aditivo e sem mudança de comportamento:

`buildDirectorPrompt(brief: CampaignBrief, context: ResolvedCampaignContext, options?: { effectiveProductName?: string; inferredCategory?: string; normalizedInstruction?: string }): string`

Corpo: montar `variables = this.buildPromptVariables(brief, context, options?.effectiveProductName ?? brief.product.name, options?.inferredCategory)` e retornar `this.assemblePrompt(GenerationState.INITIAL, variables, [], options?.normalizedInstruction)`. Nenhuma lógica de montagem nova, nenhuma duplicação: o método apenas compõe os dois privados existentes. `generateImage` continua chamando os mesmos privados exatamente como hoje — **nenhuma linha do fluxo existente deve mudar** (nem ordem, nem parâmetros, nem `previousIssues`, nem estado).

Adicionar comentário curto no método referenciando F48.1 (D7): seam para o laboratório montar o prompt do diretor pelo caminho real sem duplicar lógica.

Criar `src/lib/lab/gateway/noop-image-provider.ts` exportando `createNoopImageProvider(): ImageProvider` — `name = "lab-noop-image-provider"` e `generateImage()` que **lança** `new Error("lab_noop_image_provider_invoked")`. Objetivo: satisfazer o primeiro parâmetro do ctor de `ImageGenerationService` para que o laboratório possa chamar `buildDirectorPrompt`; se algum caminho invocar o provider, o teste/execução falha imediatamente (prova de que o provider nunca é usado).

Criar `src/lib/image-generation/services/__tests__/image-generation-service.build-director-prompt.test.ts` cobrindo: `buildDirectorPrompt` retorna string não vazia contendo o conteúdo do prompt `campaign-image-director-offer`; usa `effectiveProductName` do options quando fornecido e `brief.product.name` quando ausente; aceita `normalizedInstruction` e inclui o bloco de não conformidade; `options.inferredCategory` é repassado a `creativeDirectionSection` (comparar saída com e sem categoria quando o brief permitir); instanciar o serviço com `createNoopImageProvider()` e um `PromptLoader` apontando para `prompts/` funciona sem invocar o provider; **não** é feita nenhuma chamada de rede.
  </action>
  <verify>
    <automated>npx vitest run src/lib/image-generation/services/__tests__/image-generation-service.build-director-prompt.test.ts src/lib/image-generation/services/__tests__/image-generation-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "buildDirectorPrompt" src/lib/image-generation/services/image-generation-service.ts` retorna `>= 1` ocorrência com modificador público (sem `private`).
    - `rg "private buildDirectorPrompt" src/lib/image-generation/services/image-generation-service.ts` retorna **zero** linhas.
    - `rg -c "this\\.buildPromptVariables\\(|this\\.assemblePrompt\\(" src/lib/image-generation/services/image-generation-service.ts` é maior do que antes da mudança apenas pelo novo método (a chamada original em `generateImage` permanece).
    - `src/lib/lab/gateway/noop-image-provider.ts` existe, exporta `createNoopImageProvider` e contém `lab_noop_image_provider_invoked`.
    - `npx vitest run src/lib/image-generation/services/__tests__/image-generation-service.test.ts` continua **verde** (pipeline de produção inalterado).
    - `npx vitest run src/lib/image-generation/services/__tests__/image-generation-service.build-director-prompt.test.ts` passa com exit code 0.
    - `git diff --stat src/lib/image-generation/services/image-generation-service.ts` mostra apenas adições (nenhuma linha removida de `generateImage`).
  </acceptance_criteria>
  <done>`buildDirectorPrompt` público e aditivo disponível, reutilizando `buildPromptVariables` + `assemblePrompt`, com provider stub que nunca é invocado e a suíte existente do serviço permanecendo verde.</done>
</task>

<task type="auto">
  <name>Task 2: LabModelResolver (alvo fixo) + LabPromptLoader (override do snapshot)</name>
  <files>src/lib/lab/gateway/lab-model-resolver.ts, src/lib/lab/gateway/lab-prompt-loader.ts, src/lib/lab/gateway/__tests__/lab-model-resolver.test.ts, src/lib/lab/gateway/__tests__/lab-prompt-loader.test.ts</files>
  <read_first>
    - `src/lib/ai/model-resolver.ts` — `AiModelResolver`, `AiModelConfig`, `AiModelTarget`, `AiCapability`
    - `src/lib/image-generation/prompt-loader.ts` — `PromptLoader.load`/`clearCache` (base do `LabPromptLoader`)
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/design.md` — "D6 — Harness de gateway isolado" (blocos `LabModelResolver` e `LabPromptLoader`)
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-gateway-harness/spec.md` — requirements "Instância isolada do gateway com alvo fixo" e "Override de prompt sem alterar prompts oficiais"
    - `.planning/phases/48.1-laboratorio-ia-minimo/48.1-CONTEXT.md` — D6 e DV-3 (`defaultAiModelResolver` vive em `@/lib/ai/index.ts`)
    - `src/lib/lab/domain/prompt-snapshot.ts` (48-1-04) — `PROMPT_UNDER_TEST` e o shape `{ name, content, contentHash, source }`
  </read_first>
  <action>
Criar `src/lib/lab/gateway/lab-model-resolver.ts` (task 5.2) — **sem** importar `@/lib/ai` (index); importar apenas tipos de `@/lib/ai/model-resolver`:

- `interface LabModelResolverParams { fixedTarget: AiModelTarget; fallbackResolver: AiModelResolver }`
- `class LabModelResolver implements AiModelResolver` com `constructor(private readonly params: LabModelResolverParams)`:
  - `resolve(capability)` — se `capability === "campaign_image"` retorna `{ capability: "campaign_image", segment: "image", primary: { ...fixedTarget }, fallback: undefined }` (o alvo fixo tem **precedência**); caso contrário delega a `this.params.fallbackResolver.resolve(capability)` (modo leitura).
  - `listCapabilities()` — retorna `this.params.fallbackResolver.listCapabilities()`.
- O módulo **não** pode referenciar `ai_model_selection`, `ai_model_catalog`, `supabaseAdmin` ou qualquer escrita — a única fonte do alvo é o `fixedTarget` injetado.
- Guarda defensiva: se `fixedTarget.provider`/`model`/`protocol` estiverem vazios, `resolve("campaign_image")` lança `Error` com `invalid_fixed_target`.

Criar `src/lib/lab/gateway/lab-prompt-loader.ts` (task 5.3):
- `interface LabPromptOverride { name: string; content: string }`
- `class LabPromptLoader extends PromptLoader` com `constructor(private readonly overrides: LabPromptOverride[], promptsDir?: string)` (repassa `promptsDir` a `super`):
  - `load(name, variables?)` — se houver override com `name` igual, retorna o `content` do override com a **mesma interpolação de placeholders** do loader real (aplicar o mesmo `replace` de `{{chave}}` para cada variável); caso contrário, delega a `super.load(name, variables)`.
  - Nenhum método escreve em disco: o módulo não pode conter `writeFile`, `writeFileSync`, `mkdir`, `rm`, `unlink`, `rename`.
  - `clearCache()` continua herdado.

Criar `src/lib/lab/gateway/__tests__/lab-model-resolver.test.ts` cobrindo: `resolve("campaign_image")` devolve exatamente o `fixedTarget` injetado (provider/model/protocol) e `fallback === undefined`; `resolve("campaign_image_review")` e `resolve("campaign_copy")` delegam ao fake fallback (spy registra a chamada) e devolvem o config do fake; `listCapabilities()` delega; `fixedTarget` inválido lança `invalid_fixed_target`; o fake fallback **nunca** é chamado quando a capacidade é `campaign_image`.

Criar `src/lib/lab/gateway/__tests__/lab-prompt-loader.test.ts` cobrindo: com override para `campaign-image-director-offer`, `load("campaign-image-director-offer")` retorna o conteúdo do override; interpolação de `{{productName}}` funciona no override; `load("campaign-image-reviewer")` delega ao loader fs e retorna o conteúdo oficial; após o teste, o arquivo `prompts/campaign-image-director-offer.md` permanece byte a byte idêntico (comparar leitura antes/depois); `git status --porcelain prompts/` permanece vazio.
  </action>
  <verify>
    <automated>npx vitest run src/lib/lab/gateway/__tests__/lab-model-resolver.test.ts src/lib/lab/gateway/__tests__/lab-prompt-loader.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/lab/gateway/lab-model-resolver.ts` exporta `LabModelResolver` e `LabModelResolverParams`.
    - `rg "ai_model_selection|ai_model_catalog|supabaseAdmin" src/lib/lab/gateway/lab-model-resolver.ts` retorna **zero** linhas.
    - `rg "from \"@/lib/ai\"" src/lib/lab/gateway/lab-model-resolver.ts` retorna **zero** linhas (apenas tipos de `@/lib/ai/model-resolver`).
    - `rg "campaign_image" src/lib/lab/gateway/lab-model-resolver.ts` encontra a checagem de precedência do alvo fixo.
    - `src/lib/lab/gateway/lab-prompt-loader.ts` exporta `LabPromptLoader` e contém `extends PromptLoader`.
    - `rg "writeFile|writeFileSync|mkdir|unlink|rename" src/lib/lab/gateway/lab-prompt-loader.ts` retorna **zero** linhas.
    - `git status --porcelain prompts/` retorna vazio após rodar os testes.
    - `npx vitest run src/lib/lab/gateway/__tests__/lab-model-resolver.test.ts src/lib/lab/gateway/__tests__/lab-prompt-loader.test.ts` passa com exit code 0.
  </acceptance_criteria>
  <done>Resolver laboratorial com precedência do alvo fixo para `campaign_image` e delegação read-only fora do escopo; loader laboratorial servindo o override do snapshot sem jamais escrever em `prompts/`.</done>
</task>

<task type="auto">
  <name>Task 3: LabTelemetrySink + cobertura de custo + runtime single-shot (1 envelope por run)</name>
  <files>src/lib/ai/lab-telemetry-sink.ts, src/lib/lab/domain/cost-coverage.ts, src/lib/lab/gateway/runtime.ts, src/lib/ai/__tests__/lab-telemetry-sink.test.ts, src/lib/lab/domain/__tests__/cost-coverage.test.ts, src/lib/lab/gateway/__tests__/runtime.test.ts</files>
  <read_first>
    - `src/lib/ai/telemetry-sink.ts` — `DefaultAiTelemetrySink.emit` (derivação de `imageGenerationTool` e `CAPABILITY_GENERATION_TYPE`), `NoopAiTelemetrySink`
    - `src/lib/ai-cost/cost-estimator.ts` — assinatura de `resolveAiCost` (:149-165) e a regra do componente de imagem (:187-199)
    - `src/lib/ai-cost/types.ts` — `CostResolution`, `OPERATION_RUN_TYPES`
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/design.md` — "D6" (sink), "D8" (origem completa do custo), "D14" (1 chamada paga por run)
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-gateway-harness/spec.md` — "Telemetria própria sem gravar eventos produtivos" e "Fallback automático desabilitado no laboratório"
    - `src/lib/ai/__tests__/telemetry-sink.test.ts` — padrão de mock de `@/lib/ai-cost/cost-estimator` e `@/lib/ai-cost/tracker`
    - `.planning/phases/48.1-laboratorio-ia-minimo/48.1-CONTEXT.md` — DV-1, DV-2, DV-9
  </read_first>
  <action>
Criar `src/lib/ai/lab-telemetry-sink.ts` (task 5.4) — **obrigatoriamente neste path** (o gate de arquitetura restringe `resolveAiCost` a `src/lib/ai/**`):

- `interface LabCallEntry { capability: AiCapability; provider: string; model: string; protocol: AiProtocol; status: "success" | "failed" | "timeout"; durationMs: number; usage?: TokenUsage; cost: CostResolution; errorType?: string }`
- `class LabTelemetrySink implements AiTelemetrySink`:
  - `constructor(params?: { onEntry?: (entry: LabCallEntry) => void })`
  - `emit(envelope)` — garante **exatamente uma entrada por envelope**: (a) `buildEntry(envelope)` em `try` — `const generationType = CAPABILITY_GENERATION_TYPE[envelope.capability]` e `const cost = await resolveAiCost({ provider: envelope.provider, model: envelope.model, usage: envelope.usage, providerReportedCostUsd: envelope.providerReportedCostUsd, imageGenerationTool: envelope.usageMeta?.imageGenerationTool === true, generationType })`; montar `LabCallEntry` preservando a `CostResolution` **real e completa** (todos os campos de `CostResolution`, incluindo `costSource`, `pricingVersion`, `costFormulaVersion`, `costEstimationNote`, `textComponentUsd`, `imageToolComponentUsd`, `imageToolPricingProvider`, `imageToolPricingModel`, `imageToolPricingVersion`); o `catch` **retorna** uma entrada sanitizada com `costSource: "not_available"`, `estimatedCostUsd: null` e `errorType` derivado de `sanitizeAiErrorMessage` (nunca propaga). (b) `this.collected.push(entry)` uma única vez. (c) `notify(entry)` — chama `onEntry?.(entry)` em `try/catch` **separado e best-effort**: uma falha do consumidor (ex.: stream NDJSON desconectado) nunca duplica a entrada, nunca é interpretada como falha do sink e nunca escapa para o gateway.
  - O `try/catch` do custo **não** envolve o `push` nem o `onEntry` (senão uma falha do consumidor geraria uma segunda entrada artificial).
  - `get entries(): readonly LabCallEntry[]` e `get costSummary(): CostResolution | null` (soma `estimatedCostUsd` das entradas não nulas; `costSource` da última entrada; `null` quando não há entradas).
  - **Proibido**: importar `AiCostTracker` ou referenciar `generation_events`. O arquivo não pode conter as strings `AiCostTracker`, `generation_events` nem `tracker.record`.
  - Nenhum secret: `LabCallEntry` só carrega capability/provider/model/protocol/status/latência/usage/custo/errorType.

Criar `src/lib/lab/domain/cost-coverage.ts` (task 5.4) com `import type { CostResolution } from "@/lib/ai-cost/types";`:
- `type LabCostCoverage = "complete" | "partial" | "missing"`
- `deriveCostCoverage(cost: CostResolution): LabCostCoverage` — regras: `missing` quando `estimatedCostUsd === null` ou `costSource === "not_available"`; `partial` quando `costSource === "manual_unknown"` ou `costSource === "fallback_static"` ou (`cost.costEstimationNote` não vazio) ou (`costSource === "pricing_table"` com `textComponentUsd === undefined` **ou** `imageToolComponentUsd === undefined`); `complete` nos demais casos (`provider_reported`, ou `pricing_table` com os dois componentes presentes e sem nota).

Criar `src/lib/lab/gateway/runtime.ts` (task 5.5):
- `createLabGateway(params: { fixedTarget: AiModelTarget; fallbackResolver: AiModelResolver; adapters: AiAdapterRegistry }): AiGateway` — compõe `new LabModelResolver({ fixedTarget, fallbackResolver })` + `new AiGateway(resolver, adapters)`. `src/lib/ai/gateway.ts` permanece intocado.
- `createLabTelemetryContext(params: { sink: AiTelemetrySink; operationRunId: string; traceId: string; storeId: string; userId?: string }): AiTelemetryContext` — `operationRunType: "campaign_delivery"` (o union tem exatamente 4 valores e é travado por teste); `runType: "lab"` **não** entra aqui — ele vive apenas no snapshot do run (48-1-07).
- `runLabCampaignImage(params: { gateway: AiInvoker; request: AiInvocationRequest; telemetry: AiTelemetryContext }): Promise<AiInvocationResult>` — chama **exatamente uma vez** `params.gateway.invoke("campaign_image", params.request, params.telemetry)` (sem passar `target`, portanto `primary`), sem retry, sem `campaign_image_edit`, sem consultar `hasFallback`. Retorna o resultado.
- `createDefaultLabRuntime(params: { fixedTarget: AiModelTarget }): Promise<{ gateway: AiGateway; fallbackResolver: AiModelResolver; adapters: AiAdapterRegistry }>` — usa **dynamic import** `await import("@/lib/ai")` para obter `defaultAiModelResolver` e `defaultAdapterRegistry` (evita efeito colateral de `supabaseAdmin` no load do módulo em testes).
- Proibido no arquivo: `OpenAIImageProvider`, `providers/openai`, `campaign_image_edit`, `hasFallback`, `target: "fallback"`.

Criar `src/lib/ai/__tests__/lab-telemetry-sink.test.ts` (mockando `@/lib/ai-cost/cost-estimator` e `@/lib/ai-cost/tracker` no padrão de `telemetry-sink.test.ts`): `emit` de envelope de sucesso acumula 1 entrada com `costSource` preservado; `resolveAiCost` é chamado com `imageGenerationTool: true` e `generationType: "campaign_image"` quando `usageMeta.imageGenerationTool === true`; `imageGenerationTool: false` quando ausente; `AiCostTracker.record` **nunca** é chamado; envelope de falha registra `errorType` sanitizado; exceção em `resolveAiCost` não propaga e gera entrada sanitizada; `costSummary` soma apenas custos não nulos; **`onEntry` que lança** não propaga (`emit` resolve), mantém `entries.length === 1`, preserva a entrada original e a `CostResolution` real (sem entrada artificial) e, com falha contínua em dois `emit`, mantém exatamente 1 entrada por envelope.

Criar `src/lib/lab/domain/__tests__/cost-coverage.test.ts` cobrindo os 3 estados: `not_available`/`estimatedCostUsd: null` → `missing`; `manual_unknown` → `partial`; `fallback_static` → `partial`; `pricing_table` com `textComponentUsd` e `imageToolComponentUsd` definidos e sem nota → `complete`; `pricing_table` com apenas `imageToolComponentUsd` → `partial`; `provider_reported` → `complete`; `pricing_table` com `costEstimationNote` → `partial`.

Criar `src/lib/lab/gateway/__tests__/runtime.test.ts` com fakes (sem rede, sem `@/lib/ai`): um adapter fake que conta invocações e devolve `{ imageBase64, mimeType: "image/png", model: "gpt-5.5", usageMeta: { imageGenerationTool: true } }`; `createLabGateway` com o adapter fake → `runLabCampaignImage` produz **exatamente 1** envelope no sink (adapter invocado 1 vez, `sink.entries.length === 1`); falha do adapter (erro de capability) → 1 envelope `failed` e **nenhuma** segunda invocação; `createLabTelemetryContext` retorna `operationRunType === "campaign_delivery"` e `sink` presente; o request repassado contém `tools: "image_generation"` e `productImagesDataUrls` do cenário.
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai/__tests__/lab-telemetry-sink.test.ts src/lib/lab/domain/__tests__/cost-coverage.test.ts src/lib/lab/gateway/__tests__/runtime.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/ai/lab-telemetry-sink.ts` exporta `LabTelemetrySink` e `LabCallEntry`.
    - `rg "AiCostTracker|generation_events|tracker\\.record" src/lib/ai/lab-telemetry-sink.ts` retorna **zero** linhas.
    - `rg "resolveAiCost\\(" src/lib/ai/lab-telemetry-sink.ts` encontra exatamente 1 chamada, com `imageGenerationTool` derivado de `usageMeta` e `generationType` de `CAPABILITY_GENERATION_TYPE`.
    - `rg "imageUnitUsd|costPartial" src/lib/ai/lab-telemetry-sink.ts src/lib/lab/domain/cost-coverage.ts` retorna **zero** linhas (campos inexistentes).
    - `emit` acumula **exatamente 1 entrada** mesmo quando `onEntry` lança (o `try/catch` do custo não envolve o `push`/`onEntry`).
    - `src/lib/lab/domain/cost-coverage.ts` exporta `deriveCostCoverage` e `LabCostCoverage`.
    - `rg "OpenAIImageProvider|providers/openai|campaign_image_edit|hasFallback" src/lib/lab/gateway/runtime.ts` retorna **zero** linhas.
    - `rg "operationRunType: \"campaign_delivery\"" src/lib/lab/gateway/runtime.ts` encontra exatamente 1 linha.
    - `rg "runType" src/lib/lab/gateway/runtime.ts` retorna **zero** linhas (o `runType: "lab"` pertence ao snapshot do run, plano 48-1-07).
    - `rg "await import\\(\"@/lib/ai\"\\)" src/lib/lab/gateway/runtime.ts` encontra o dynamic import em `createDefaultLabRuntime`.
    - `rg "\\.invoke\\(" src/lib/lab/gateway/runtime.ts` encontra exatamente 1 chamada, com `"campaign_image"`.
    - `npx vitest run src/lib/ai/__tests__/lab-telemetry-sink.test.ts src/lib/lab/domain/__tests__/cost-coverage.test.ts src/lib/lab/gateway/__tests__/runtime.test.ts` passa com exit code 0.
    - `npx vitest run src/lib/ai` e `npx vitest run src/lib/image-generation` permanecem verdes.
  </acceptance_criteria>
  <done>Sink capturador com custo em leitura (CostResolution real completa), cobertura `complete|partial|missing` derivada no domínio do laboratório e runtime que invoca `campaign_image` uma única vez, provando zero `generation_events` e exatamente 1 envelope por run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| laboratório → gateway F46 | invocação de IA real sob comando do laboratório |
| sink laboratorial → custo | cálculo de custo sem persistência produtiva |
| snapshot da variante → loader de prompt | conteúdo de prompt controlado pelo usuário admin |
| provider de imagem → fallback | caminho que pode disparar uma segunda chamada paga |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-1-31 | Denial of Service / financial | fallback automático (`campaign_image_edit`) | mitigate | O laboratório não usa `OpenAIImageProvider`; `runtime.ts` invoca `campaign_image` direto no gateway sem `target` e sem `hasFallback`; teste prova exatamente 1 envelope mesmo em falha de capability |
| T-48-1-32 | Tampering | gateway F46 | mitigate | `src/lib/ai/gateway.ts` intocado; o harness compõe uma instância paralela com resolver injetado; `f47-contract-guard` permanece verde |
| T-48-1-33 | Tampering | alvo de modelo em runtime | mitigate | `LabModelResolver` só devolve o `fixedTarget` injetado para `campaign_image`; nenhum acesso a `ai_model_selection`/`ai_model_catalog` no módulo (asserção por grep); alvo vazio → `invalid_fixed_target` |
| T-48-1-34 | Information Disclosure | prompts oficiais | mitigate | `LabPromptLoader` serve o override do snapshot apenas em memória; módulo sem `writeFile`/`mkdir`/`unlink`/`rename`; `git status --porcelain prompts/` permanece vazio |
| T-48-1-35 | Repudiation / Tampering | telemetria produtiva | mitigate | `LabTelemetrySink` não importa `AiCostTracker` nem referencia `generation_events` (asserção por grep); gate de arquitetura (48-1-02) cobre `src/lib/lab/**`; `resolveAiCost` permanece exclusivo de `src/lib/ai/**` |
| T-48-1-36 | Information Disclosure | `LabCallEntry` | mitigate | Apenas capability/provider/model/protocol/status/latência/usage/custo/errorType; erro sanitizado por `sanitizeAiErrorMessage`; nenhuma chave ou URL persistida |
| T-48-1-37 | Tampering | `CostResolution` | mitigate | Preservação integral dos campos reais (`costSource`, `pricingVersion`, `costFormulaVersion`, `costEstimationNote`, `textComponentUsd`, `imageToolComponentUsd`, `imageToolPricing*`); proibido usar `imageUnitUsd`/`costPartial` (não existem) |
| T-48-1-38 | Tampering | `operationRunType` | mitigate | Reutiliza `"campaign_delivery"` (union travado por teste); `runType: "lab"` fica somente no snapshot próprio do run |
| T-48-1-39 | Denial of Service | efeito colateral de import no load | mitigate | `createDefaultLabRuntime` usa dynamic import de `@/lib/ai`; testes injetam fakes e não importam o index no topo |
| T-48-1-49 | Repudiation / Tampering | callback do consumidor (`onEntry`) | mitigate | O cálculo/construção da entrada é protegido separadamente do `push`/`onEntry`; `onEntry` roda em `try/catch` best-effort e não pode duplicar a entrada, ser interpretado como falha do sink nem escapar para o gateway; teste prova `entries.length === 1` e custo intacto com `onEntry` lançando |
| T-48-1-SC | Tampering | instalação de dependências | mitigate | Nenhum pacote novo nesta task. Instalação nova exigiria auditoria de legitimidade + checkpoint humano bloqueante |
</threat_model>

<verification>
- `npx vitest run src/lib/lab src/lib/ai src/lib/image-generation` → exit code 0.
- `npx tsc -p tsconfig.typecheck.json --noEmit` → exit code 0.
- `npm run lint` → exit code 0.
- `git diff --stat src/lib/ai/gateway.ts src/lib/ai-cost src/lib/campaign prompts/` → vazio (nada alterado).
- `rg "generation_events|AiCostTracker" src/lib/lab src/lib/ai/lab-telemetry-sink.ts` → 0 ocorrências.
- `rg "OpenAIImageProvider|providers/openai|campaign_image_edit" src/lib/lab --glob "!**/__tests__/**"` → 0 ocorrências no **código de produção** do laboratório. (O literal `campaign_image_edit` aparece em 1 teste **negativo** pré-existente do 48-1-04, `src/lib/lab/domain/__tests__/schemas.test.ts`, que afirma a rejeição da capacidade — reforça a fence e está registrado em `deferred-items.md`.)
- Teste do runtime prova 1 envelope por run (sucesso e falha) e ausência de segunda invocação.
</verification>

<success_criteria>
- Seam `buildDirectorPrompt` aditivo, público e sem duplicação, com o pipeline de produção inalterado.
- Alvo fixo com precedência em `campaign_image` e delegação read-only fora do escopo.
- Override de prompt em memória sem tocar `prompts/`.
- Sink com `resolveAiCost` em leitura, `CostResolution` real completa, sem `generation_events`/`AiCostTracker.record`.
- Cobertura `complete|partial|missing` derivada no domínio do laboratório.
- Runtime single-shot com exatamente 1 chamada `campaign_image` e 1 envelope por run.
</success_criteria>

<output>
Create `.planning/phases/48.1-laboratorio-ia-minimo/48-1-05-SUMMARY.md` when done.
</output>

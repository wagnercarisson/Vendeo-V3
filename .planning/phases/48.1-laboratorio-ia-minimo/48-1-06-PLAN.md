---
phase: 48.1
plan: 48-1-06
type: execute
wave: 3
depends_on:
  - 48-1-01
  - 48-1-02
files_modified:
  - src/lib/lab/persistence/artifact-service.ts
  - src/lib/lab/persistence/__tests__/artifact-service.test.ts
  - scripts/lab/48-cleanup-artifacts.mjs
  - scripts/lab/__tests__/48-cleanup-artifacts.test.mjs
autonomous: true
requirements:
  - lab-artifacts
must_haves:
  truths:
    - "Um artefato de saída é gravado no bucket privado `lab-artifacts` no path `experiments/{experimentId}/runs/{runId}/output.{png|jpg|webp}` e nunca em `campaign-images` nem em path de loja/campanha"
    - "Cada artefato é registrado em `lab_artifacts` com storage_path, mime_type, width, height, bytes e checksum SHA-256 calculado sobre o buffer gravado"
    - "Se o insert em `lab_artifacts` falhar depois do upload, o objeto é removido do bucket (nenhum órfão) e o erro `artifact_persistence_failed` é propagado"
    - "Se o upload falhar, nenhum registro é inserido em `lab_artifacts`"
    - "A leitura usa createSignedUrl server-side com TTL de 3600s; o bucket permanece privado (public=false, sem policy para anon/authenticated)"
    - "Paths com `..`, caminho absoluto, prefixo de `campaign-images` ou ausência do prefixo `experiments/` são rejeitados antes de qualquer upload"
    - "O cleanup manual seleciona apenas artefatos de runs terminais/arquivados ou mais antigos que LAB_ARTIFACT_RETENTION_DAYS=30, nunca de runs `pending`/`running`, e marca `removed_at` sem apagar metadados, hashes, runs ou avaliações"
    - "Nenhuma rotina de cleanup roda automaticamente: a execução é exclusivamente manual via script opt-in"
  artifacts:
    - path: "src/lib/lab/persistence/artifact-service.ts"
      provides: "upload/rollback, metadados+checksum, URL assinada e listagem de artefatos do laboratório"
      exports:
        - LAB_ARTIFACT_BUCKET
        - LAB_SIGNED_URL_TTL_SECONDS
        - buildOutputArtifactPath
        - buildInputArtifactPath
        - computeArtifactChecksum
        - persistOutputArtifact
        - listRunArtifacts
        - createArtifactSignedUrl
    - path: "scripts/lab/48-cleanup-artifacts.mjs"
      provides: "cleanup manual e opt-in de artefatos elegíveis, com proteção de run em andamento"
      exports:
        - selectEligibleArtifacts
        - partitionArtifacts
        - parseArtifactStoragePath
        - isRunInProgress
        - main
    - path: "scripts/lab/__tests__/48-cleanup-artifacts.test.mjs"
      provides: "testes da regra de elegibilidade do cleanup e da proteção de run em andamento"
      contains: "selectEligibleArtifacts"
  key_links:
    - from: "src/lib/lab/persistence/artifact-service.ts"
      to: "storage.from('lab-artifacts').upload(...)"
      via: "upload do buffer no path próprio do laboratório com upsert false"
      pattern: "lab-artifacts"
    - from: "src/lib/lab/persistence/artifact-service.ts"
      to: "public.lab_artifacts"
      via: "insert de metadados + checksum após o upload, com remoção do objeto em falha"
      pattern: "lab_artifacts"
    - from: "src/lib/lab/persistence/artifact-service.ts"
      to: "storage.from('lab-artifacts').createSignedUrl(...)"
      via: "URL assinada server-side de 3600s para leitura na comparação"
      pattern: "createSignedUrl"
    - from: "scripts/lab/48-cleanup-artifacts.mjs"
      to: "public.lab_artifacts / public.lab_runs"
      via: "seleção por idade/arquivamento com exclusão de runs pending/running"
      pattern: "lab_runs"
---

<objective>
Entregar a persistência de artefatos do laboratório: serviço de upload no bucket privado `lab-artifacts` com path próprio, metadados + checksum SHA-256, rollback do objeto quando o insert falha, leitura por URL assinada de 3600s e um script de cleanup manual/opt-in que nunca toca runs em andamento.

Purpose: garantir que toda arte evidencial de um run fique em storage isolado da produção, com metadados verificáveis e sem objetos órfãos (D10/D15), e que a retenção seja uma ação humana explícita — nunca um scheduler (D10).
Output: `src/lib/lab/persistence/artifact-service.ts`, `scripts/lab/48-cleanup-artifacts.mjs` e testes verdes, consumidos por 48-1-07 (persistência no fim do run) e 48-1-08 (URLs assinadas no detalhe/comparação).
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48.1-laboratorio-ia-minimo/48.1-CONTEXT.md
@openspec/changes/fase-48-1-laboratorio-ia-minimo/design.md
@openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-artifacts/spec.md
@supabase/migrations/20260915000002_f48_1_create_lab_tables.sql
@src/lib/campaign/persistence.ts
@src/lib/campaign/display.ts
@src/lib/visual-signature/persistence.ts
@scripts/uat/47-local-bootstrap.mjs

<interfaces>
Contratos extraídos do código e da migration (usar diretamente — não explorar o repositório):

- Tabela `public.lab_artifacts` (criada no 48-1-01): `id UUID PK`, `run_id UUID NOT NULL REFERENCES public.lab_runs(id) ON DELETE CASCADE`, `kind TEXT NOT NULL CHECK (kind IN ('output','input','diagnostic'))`, `storage_path TEXT NOT NULL`, `mime_type TEXT`, `width INT`, `height INT`, `bytes BIGINT`, `checksum TEXT`, `removed_at TIMESTAMPTZ`, `created_at TIMESTAMPTZ`, `UNIQUE (run_id, kind, storage_path)`.
- Tabela `public.lab_runs`: colunas relevantes para o cleanup — `id`, `experiment_id`, `status TEXT CHECK IN ('pending','running','succeeded','failed','cancelled','timeout')`, `created_at`, `finished_at`.
- Bucket `lab-artifacts` (criado no 48-1-01): privado (`public=false`), `file_size_limit=10485760`, `allowed_mime_types = ARRAY['image/png','image/jpeg','image/webp']`, única policy `FOR ALL TO service_role`.
- Padrão de upload do repositório (`src/lib/campaign/persistence.ts:78-88` e `src/lib/visual-signature/persistence.ts:38-45`): `supabaseAdmin.storage.from("<bucket>").upload(storagePath, buffer, { contentType, upsert: false })` e `if (error) throw new Error(error.message)`.
- Padrão de URL assinada do repositório (`src/lib/campaign/display.ts:67-82`): `supabaseAdmin.storage.from("<bucket>").createSignedUrl(storagePath, 3600)` e `data.signedUrl`; retorna `null` para path vazio.
- `src/lib/lab/limits.ts` (criado no 48-1-02): `LAB_ARTIFACT_RETENTION_DAYS = 30` — fonte única do prazo de retenção.
- `src/lib/lab/environment-guard.ts` (criado no 48-1-02): `assertLabEnvironment()` / `getLabEnvironment()` — o serviço de artefatos não chama a guarda (ela é aplicada na borda: páginas/rotas e o script de cleanup), mas não pode acessar storage antes dela.
- `supabaseAdmin` em `src/lib/supabase/server.ts` (service_role). O serviço recebe o client **por parâmetro** para permitir fakes nos testes.
- Convenção de testes: Vitest com `environment: node`; testes co-localizados em `__tests__/`. Para o script `.mjs`, o teste também vive em `__tests__/` e importa as funções puras exportadas.
- Mapa MIME → extensão exigido pelo bucket: `image/png` → `png`, `image/jpeg` → `jpg`, `image/webp` → `webp`.
- Precedente de script UAT local (`scripts/uat/47-local-bootstrap.mjs`): valida o host local e **recusa** execução remota antes de qualquer escrita; usa `@supabase/supabase-js` + `pg`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: artifact-service — path próprio, upload, metadados/checksum e rollback sem órfão</name>
  <files>src/lib/lab/persistence/artifact-service.ts, src/lib/lab/persistence/__tests__/artifact-service.test.ts</files>
  <read_first>
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/design.md` — seção "D10 — Artefatos: bucket próprio, paths, retenção e cleanup"
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-artifacts/spec.md` — requirements "Bucket e paths próprios do laboratório" e "Metadados e checksum do artefato" (inclui o cenário "Falha de persistência não deixa órfão")
    - `.planning/phases/48.1-laboratorio-ia-minimo/48.1-CONTEXT.md` — D10 e D15
    - `supabase/migrations/20260915000002_f48_1_create_lab_tables.sql` — colunas e `UNIQUE (run_id, kind, storage_path)` de `lab_artifacts`; MIME permitidos do bucket `lab-artifacts`
    - `src/lib/campaign/persistence.ts` — padrão de upload com `upsert: false` e tratamento de `error`
    - `src/lib/visual-signature/persistence.ts` — segundo precedente de upload
  </read_first>
  <action>
Criar `src/lib/lab/persistence/artifact-service.ts` (task 6.1 do `tasks.md`) com `import "server-only";`.

Constantes exportadas: `LAB_ARTIFACT_BUCKET = "lab-artifacts"`, `LAB_OUTPUT_ARTIFACT_KIND = "output"`, `LAB_INPUT_ARTIFACT_KIND = "input"`, `LAB_ALLOWED_ARTIFACT_MIME_TYPES = ["image/png","image/jpeg","image/webp"] as const`, e o mapa interno MIME → extensão (`image/png` → `png`, `image/jpeg` → `jpg`, `image/webp` → `webp`).

Path builders exportados:
- `buildOutputArtifactPath(params: { experimentId: string; runId: string; mimeType: string }): string` → `experiments/{experimentId}/runs/{runId}/output.{ext}`.
- `buildInputArtifactPath(params: { experimentId: string; runId: string; index: number; mimeType: string }): string` → `experiments/{experimentId}/runs/{runId}/inputs/{index}.{ext}`.
- `assertLabArtifactPath(storagePath: string): void` — rejeita com `Error("invalid_artifact_path")` quando: path vazio; contém `..`; começa com `/` ou contém `://` ou `\`; não começa com `experiments/`; contém `campaign-images`; ou o segmento `experiments/{id}` não casa com o formato UUID v4-like aceito (usar regex de UUID). Esta função é a barreira anti-traversal: é chamada por todos os builders e por toda leitura.

`computeArtifactChecksum(buffer: Buffer): string` — SHA-256 hex via `node:crypto` (`createHash("sha256").update(buffer).digest("hex")`).

`persistOutputArtifact(params: { client; experimentId: string; runId: string; buffer: Buffer; mimeType: "image/png" | "image/jpeg" | "image/webp"; width: number | null; height: number | null }): Promise<{ artifactId: string; storagePath: string; checksum: string; bytes: number }>`:
1. Buffer vazio → `Error("empty_artifact_buffer")` (nenhum upload).
2. MIME fora de `LAB_ALLOWED_ARTIFACT_MIME_TYPES` → `Error("unsupported_artifact_mime_type")`.
3. `storagePath = buildOutputArtifactPath(...)`; `assertLabArtifactPath(storagePath)`.
4. `checksum = computeArtifactChecksum(buffer)`; `bytes = buffer.byteLength`.
5. `client.storage.from(LAB_ARTIFACT_BUCKET).upload(storagePath, buffer, { contentType: mimeType, upsert: false })`; se `error` → `Error("artifact_upload_failed")` (nada é inserido).
6. `client.from("lab_artifacts").insert({ run_id, kind: "output", storage_path, mime_type, width, height, bytes, checksum }).select("id").single()`; se `error` ou `data` ausente → **rollback**: `client.storage.from(LAB_ARTIFACT_BUCKET).remove([storagePath])`; o `remove` do Supabase **resolve** com `{ error }` em falha (não lança), então o retorno é inspecionado — falha do rollback é registrada via `console.warn` (sem conteúdo sensível) e **não** mascara o erro original; em seguida `Error("artifact_persistence_failed")`.
7. Retorna `{ artifactId, storagePath, checksum, bytes }`.

`listRunArtifacts(params: { client; runId: string }): Promise<{ id; kind; storagePath; mimeType; width; height; bytes; checksum; createdAt }[]>` — `select` em `lab_artifacts` filtrando `run_id` e `removed_at IS NULL`, ordenado por `created_at`; cada `storage_path` passa por `assertLabArtifactPath` antes de ser retornado (defesa contra metadado corrompido).

Proibido no arquivo: as strings `campaign-images`, `generation_events`, `ai_model_selection`, `ai_model_catalog`, `campaigns`; nenhum `process.env` de chave; nenhum import de `@/lib/ai/**`.

Criar `src/lib/lab/persistence/__tests__/artifact-service.test.ts` com um client fake em memória que registra chamadas (um objeto com `storage.from(bucket)` retornando `{ upload, remove, createSignedUrl }` e `from(table)` retornando um query-builder fake com `insert().select().single()`), cobrindo: o bucket usado é exatamente `lab-artifacts`; o path é `experiments/{experimentId}/runs/{runId}/output.png` para `image/png` e `output.jpg` para `image/jpeg`; o upload usa `upsert: false` e `contentType` correto; o insert grava `bytes` = tamanho do buffer e `checksum` = SHA-256 conhecido de um buffer fixo (calcular o hash esperado no próprio teste com `node:crypto` sobre o mesmo buffer); `buildOutputArtifactPath` com `mimeType: "image/gif"` lança `unsupported_artifact_mime_type`; `assertLabArtifactPath("campaign-images/store-1/campaign.jpg")` lança `invalid_artifact_path`; `assertLabArtifactPath("experiments/../etc/passwd")` lança `invalid_artifact_path`; `assertLabArtifactPath("/abs/path.png")` lança `invalid_artifact_path`; buffer vazio lança `empty_artifact_buffer` sem chamar upload; falha do insert chama `remove([storagePath])` exatamente 1 vez com o mesmo path e propaga `artifact_persistence_failed`; `remove` **resolvendo com `{ error }`** (não lançando) é detectado — o erro original `artifact_persistence_failed` é preservado e o rollback falho é reportado; falha do upload **não** chama `remove` nem `insert`; `listRunArtifacts` exclui artefatos com `removed_at` preenchido. Nenhuma chamada de rede: o client é 100% fake.
  </action>
  <verify>
    <automated>npx vitest run src/lib/lab/persistence/__tests__/artifact-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/lab/persistence/artifact-service.ts` existe e exporta `LAB_ARTIFACT_BUCKET`, `buildOutputArtifactPath`, `buildInputArtifactPath`, `assertLabArtifactPath`, `computeArtifactChecksum`, `persistOutputArtifact`, `listRunArtifacts`.
    - `Select-String -Path src/lib/lab/persistence/artifact-service.ts -Pattern "campaign-images|generation_events|ai_model_selection|ai_model_catalog"` retorna 0 ocorrências.
    - `Select-String -Path src/lib/lab/persistence/artifact-service.ts -Pattern 'createHash\("sha256"\)'` encontra exatamente 1 ocorrência.
    - `Select-String -Path src/lib/lab/persistence/artifact-service.ts -Pattern "upsert: false"` encontra exatamente 1 ocorrência.
    - `Select-String -Path src/lib/lab/persistence/artifact-service.ts -Pattern "artifact_persistence_failed"` e `"artifact_upload_failed"` encontram 1 ocorrência cada.
    - `npx vitest run src/lib/lab/persistence/__tests__/artifact-service.test.ts` passa com exit code 0.
    - `npx tsc -p tsconfig.typecheck.json --noEmit` continua verde.
  </acceptance_criteria>
  <done>Serviço de artefatos grava em `lab-artifacts` com path próprio validado, registra metadados + checksum SHA-256 e remove o objeto quando o insert falha, provado por teste com client fake (nenhum órfão).</done>
</task>

<task type="auto">
  <name>Task 2: URL assinada server-side (3600s) e listagem de artefatos do run</name>
  <files>src/lib/lab/persistence/artifact-service.ts, src/lib/lab/persistence/__tests__/artifact-service.test.ts</files>
  <read_first>
    - `src/lib/lab/persistence/artifact-service.ts` (Task 1) — `LAB_ARTIFACT_BUCKET`, `assertLabArtifactPath`, `listRunArtifacts`
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/design.md` — "D10" (leitura por URL assinada de curta duração, gerada server-side)
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-artifacts/spec.md` — requirement "Leitura por URL assinada" (inclui "Acesso não autorizado é negado")
    - `src/lib/campaign/display.ts` — padrão `createSignedUrl(storagePath, 3600)` e retorno `null` para path vazio
    - `.planning/phases/48.1-laboratorio-ia-minimo/48.1-UI-SPEC.md` — contrato de comparação (arte por signed URL)
  </read_first>
  <action>
Estender `src/lib/lab/persistence/artifact-service.ts` (task 6.2) com:

- `LAB_SIGNED_URL_TTL_SECONDS = 3600` (constante exportada — a UI/API nunca escolhe o TTL).
- `createArtifactSignedUrl(params: { client; storagePath: string }): Promise<string>` — path vazio → `Error("missing_artifact_path")`; `assertLabArtifactPath(storagePath)` antes de qualquer chamada; `client.storage.from(LAB_ARTIFACT_BUCKET).createSignedUrl(storagePath, LAB_SIGNED_URL_TTL_SECONDS)`; `error` ou `data.signedUrl` ausente → `Error("artifact_signed_url_failed")`; retorna `data.signedUrl`. O bucket nunca é tornado público e nenhum `getPublicUrl` é usado.
- `createArtifactSignedUrls(params: { client; storagePaths: string[] }): Promise<Record<string, string>>` — resolve em paralelo os paths válidos e devolve um mapa `storagePath → signedUrl`; um path que falhe não derruba os demais (o path problemático fica ausente do mapa). Usado por 48-1-08 para o detalhe do run e a comparação.

Ajustar/estender `src/lib/lab/persistence/__tests__/artifact-service.test.ts` com: `createArtifactSignedUrl` usa o bucket `lab-artifacts` e TTL exatamente `3600`; lança `missing_artifact_path` para string vazia sem chamar o storage; lança `artifact_signed_url_failed` quando o fake devolve erro; `createArtifactSignedUrls` devolve um mapa apenas com os paths resolvidos e tolera 1 falha entre 2 paths; nenhum path de `campaign-images` é aceito (lança `invalid_artifact_path`). Nenhuma chamada de rede.
  </action>
  <verify>
    <automated>npx vitest run src/lib/lab/persistence/__tests__/artifact-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String -Path src/lib/lab/persistence/artifact-service.ts -Pattern "LAB_SIGNED_URL_TTL_SECONDS = 3600"` encontra exatamente 1 ocorrência.
    - `Select-String -Path src/lib/lab/persistence/artifact-service.ts -Pattern "createSignedUrl"` encontra exatamente 1 chamada.
    - `Select-String -Path src/lib/lab/persistence/artifact-service.ts -Pattern "getPublicUrl"` retorna 0 ocorrências.
    - `Select-String -Path src/lib/lab/persistence/artifact-service.ts -Pattern "createArtifactSignedUrl|createArtifactSignedUrls"` encontra as duas exportações.
    - `npx vitest run src/lib/lab/persistence/__tests__/artifact-service.test.ts` passa com exit code 0.
    - `npx tsc -p tsconfig.typecheck.json --noEmit` continua verde.
  </acceptance_criteria>
  <done>Leitura de artefatos exclusivamente por URL assinada server-side de 3600s no bucket privado, com validação de path antes de cada assinatura e tolerância a falha individual.</done>
</task>

<task type="auto">
  <name>Task 3: Script de cleanup manual (idade/arquivamento) com proteção de run em andamento</name>
  <files>scripts/lab/48-cleanup-artifacts.mjs, scripts/lab/__tests__/48-cleanup-artifacts.test.mjs</files>
  <read_first>
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-artifacts/spec.md` — requirement "Retenção e cleanup" (3 cenários: elegíveis removidos + registros marcados; run em andamento não é limpo; cleanup não é automático)
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/design.md` — "D10" (remove arquivos de runs `archived`/mais antigos que `LAB_ARTIFACT_RETENTION_DAYS`; metadados/hashes/runs/avaliações permanecem; sem scheduler)
    - `.planning/phases/48.1-laboratorio-ia-minimo/48.1-CONTEXT.md` — D10 e a tabela "Constantes de limite (LOCKED)"
    - `src/lib/lab/limits.ts` — `LAB_ARTIFACT_RETENTION_DAYS = 30` (fonte única do prazo)
    - `scripts/uat/47-local-bootstrap.mjs` — convenções de script local (validação de host, `@supabase/supabase-js`, recusa remota)
    - `scripts/uat/48-local-scenarios.mjs` (48-1-03) — precedente de script local-only desta mesma fase
  </read_first>
  <action>
Criar `scripts/lab/48-cleanup-artifacts.mjs` (task 6.3) — Node ESM, **opt-in e manual** (nenhum scheduler, nenhum import de código de produção). O módulo **não pode ter efeito colateral no import**: exportar as funções puras e só executar a CLI quando o arquivo for o entry point (comparar `import.meta.url` com o caminho de `process.argv[1]`).

Exportar as funções puras (testáveis):
- `isRunInProgress(status)` — `true` para `"pending"` e `"running"`; `false` para `"succeeded"`, `"failed"`, `"cancelled"`, `"timeout"`.
- `parseArtifactStoragePath(storagePath)` — valida o formato canônico `experiments/{experimentId}/runs/{runId}/output.{png|jpg|webp}` (ou `inputs/{index}.{ext}`) com UUIDs e devolve `{ experimentId, runId }`, ou `null` se malformado/fora do prefixo. Não acessa storage.
- `partitionArtifacts(params: { artifacts; runsById; now: Date; retentionDays: number }): { eligible; invalid }` — regras, nesta ordem: (1) artefato com `removed_at` não nulo é ignorado; (2) o run do artefato é resolvido em `runsById`; run ausente → ignorado (não limpar sem saber o estado); (3) `isRunInProgress(run.status)` → **nunca elegível**, independente da idade; (4) o `storage_path` é validado com `parseArtifactStoragePath` **e** precisa ser coerente com o registro (`runId` do path = `artifact.run_id`; `experimentId` do path = `run.experiment_id`); path malformado ou incompatível vai para `invalid` e **nunca** é elegível (T-48-1-48); (5) elegível quando o experimento do run está `archived` **ou** quando `now - (run.finished_at ?? run.created_at) > retentionDays * 86400000`. Devolve `eligible` (`artifactId` + `storagePath`) e `invalid` (`artifactId` + `storagePath`).
- `selectEligibleArtifacts(params)` — delega a `partitionArtifacts` e devolve apenas `eligible` (contrato preservado).

CLI `main()` (executada só como entry point):
1. Lê `NEXT_PUBLIC_SUPABASE_URL` e recusa execução quando o host não for local (`localhost`, `127.0.0.1`, `::1`, `0.0.0.0`) — mesmo bloqueio de produção da guarda; também recusa quando a URL estiver ausente. Mensagem clara e exit code 1.
2. Exige `SUPABASE_SERVICE_ROLE_KEY` no ambiente; ausente → exit code 1.
3. Lê `retentionDays` de `process.env.LAB_ARTIFACT_RETENTION_DAYS` com default `30` (o teste asserta paridade com `LAB_ARTIFACT_RETENTION_DAYS` de `src/lib/lab/limits.ts`).
4. Lê `lab_artifacts` (id, run_id, storage_path, removed_at) e `lab_runs` (id, status, created_at, finished_at, experiment_id) + `lab_experiments` (id, status) via `@supabase/supabase-js` com service role.
5. `selectEligibleArtifacts(...)`; para cada elegível: `storage.from("lab-artifacts").remove([storagePath])` e, em caso de sucesso, `update` em `lab_artifacts` gravando `removed_at = new Date().toISOString()` para aquele `id`. Nunca executar `delete` em `lab_artifacts`, `lab_runs`, `lab_experiments` ou `lab_human_evaluations` (metadados/hashes/histórico permanecem).
6. Suporte a `--dry-run` (lista o que seria removido, sem remover nem marcar).
7. Imprime um resumo (`eligible`, `removed`, `skipped`, `dryRun`) e nunca toca o bucket `campaign-images`.

Criar `scripts/lab/__tests__/48-cleanup-artifacts.test.mjs` cobrindo: run `pending` com artefato antigo **não** é elegível; run `running` idem; run `succeeded` mais antigo que 30 dias é elegível; run `succeeded` recente não é elegível; run terminal de experimento `archived` é elegível mesmo recente; artefato já com `removed_at` não é elegível; artefato cujo run não existe em `runsById` não é elegível; **path apontando para outro run** e **path apontando para outro experimento** não são elegíveis e são reportados em `invalid`; **path malformado** (fora do prefixo, extensão inválida, `..`) não é elegível e é reportado; `parseArtifactStoragePath` aceita output/jpg/webp/inputs e rejeita `campaign-images`; `isRunInProgress` cobre os 6 status; o default de `retentionDays` do script é igual a `LAB_ARTIFACT_RETENTION_DAYS` importado de `@/lib/lab/limits`; o módulo importado **não** executa a CLI (importar não dispara rede nem `process.exit`). Nenhuma chamada de rede: o teste importa apenas as funções puras.
  </action>
  <verify>
    <automated>npx vitest run scripts/lab/__tests__/48-cleanup-artifacts.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/lab/48-cleanup-artifacts.mjs` existe e exporta `selectEligibleArtifacts`, `isRunInProgress` e `main`.
    - `Select-String -Path scripts/lab/48-cleanup-artifacts.mjs -Pattern "pending|running"` encontra a proteção dos dois estados ativos.
    - `Select-String -Path scripts/lab/48-cleanup-artifacts.mjs -Pattern "campaign-images"` retorna 0 ocorrências.
    - `Select-String -Path scripts/lab/48-cleanup-artifacts.mjs -Pattern "\.delete\("` retorna 0 ocorrências (metadados e histórico nunca são apagados).
    - `Select-String -Path scripts/lab/48-cleanup-artifacts.mjs -Pattern "LAB_ARTIFACT_RETENTION_DAYS|retentionDays"` confirma o parâmetro de retenção com default 30.
    - `Select-String -Path scripts/lab/48-cleanup-artifacts.mjs -Pattern "localhost|127\.0\.0\.1"` confirma a recusa de host não local.
    - `Select-String -Path scripts/lab/48-cleanup-artifacts.mjs -Pattern "parseArtifactStoragePath|partitionArtifacts"` confirma a validação de path antes de qualquer remoção.
    - `Select-String -Path scripts/lab/48-cleanup-artifacts.mjs -Pattern "run_id|experiment_id"` confirma a coerência path × registro.
    - `npx vitest run scripts/lab/__tests__/48-cleanup-artifacts.test.mjs` passa com exit code 0.
  </acceptance_criteria>
  <done>Cleanup manual/opt-in que remove apenas artefatos de runs terminais (arquivados ou mais antigos que 30 dias), marca `removed_at`, nunca limpa run `pending`/`running`, nunca apaga metadados/histórico e recusa execução fora do Supabase local.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run → bucket `lab-artifacts` | bytes de imagem gerados por IA são gravados em storage privado |
| metadado `lab_artifacts.storage_path` → storage | um path corrompido ou forjado poderia apontar para fora do bucket/prefixo do laboratório |
| API/UI → URL assinada | leitura de arte deve ser autorizada e temporalmente limitada |
| script de cleanup → banco + storage | operação destrutiva de arquivos, executada por humano |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-1-40 | Tampering / Elevation | `buildOutputArtifactPath` / `assertLabArtifactPath` | mitigate | Path montado apenas com UUIDs validados + extensão derivada de MIME allowlist; rejeição de `..`, caminho absoluto, `\`, `://` e ausência do prefixo `experiments/`; `assertLabArtifactPath` é chamado nos builders e em toda leitura |
| T-48-1-41 | Information Disclosure | bucket e paths | mitigate | Bucket `lab-artifacts` privado (`public=false`) com policy apenas `service_role` (48-1-01); nenhum `getPublicUrl`; leitura exclusivamente por `createSignedUrl` de 3600s; `campaign-images` proibido por asserção |
| T-48-1-42 | Tampering | integridade do artefato | mitigate | Checksum SHA-256 calculado sobre o buffer efetivamente enviado e persistido em `lab_artifacts`; teste compara com hash conhecido |
| T-48-1-43 | Denial of Service / Repudiation | objeto órfão em falha de insert | mitigate | Rollback explícito (`storage.remove`) quando o insert falha, com `try/catch` que não mascara o erro original; teste asserta `remove` com o mesmo path e zero órfãos |
| T-48-1-44 | Tampering | sobrescrita de artefato | mitigate | Upload com `upsert: false` + `UNIQUE (run_id, kind, storage_path)`; cada run tem path próprio derivado de `runId`, então reexecução cria novo objeto |
| T-48-1-45 | Denial of Service / financial | cleanup apagando evidência de run ativo | mitigate | `isRunInProgress` bloqueia `pending`/`running` independentemente da idade; run ausente em `runsById` nunca é elegível; teste cobre os dois estados ativos |
| T-48-1-46 | Repudiation | perda de histórico no cleanup | mitigate | Cleanup remove apenas **arquivos** e marca `removed_at`; nenhum `delete` em `lab_artifacts`/`lab_runs`/`lab_experiments`/`lab_human_evaluations` (asserção por grep); `--dry-run` disponível |
| T-48-1-47 | Elevation of Privilege | script de cleanup | mitigate | Recusa de host não local antes de qualquer leitura/escrita; exige service role explícita; sem scheduler (execução manual opt-in); nenhuma chamada paga |
| T-48-1-48 | Tampering | `storage_path` corrompido/forjado no cleanup destrutivo | mitigate | `parseArtifactStoragePath` valida o formato canônico (UUIDs + extensão) e `partitionArtifacts` exige coerência (`runId` do path = `artifact.run_id`; `experimentId` do path = experimento do run) **antes** de considerar idade/arquivamento; paths inválidos vão para `invalid`, nunca são removidos, e o path é revalidado imediatamente antes do `remove`; testes cobrem path de outro run, de outro experimento e malformado |
| T-48-1-SC | Tampering | instalação de dependências | mitigate | Nenhum pacote novo: `sharp` não é usado aqui, `@supabase/supabase-js` já é dependência. Instalação nova exigiria auditoria de legitimidade + checkpoint humano bloqueante |
</threat_model>

<verification>
- `npx vitest run src/lib/lab/persistence scripts/lab` → exit code 0.
- `npx tsc -p tsconfig.typecheck.json --noEmit` → exit code 0.
- `npm run lint` → exit code 0.
- `Select-String -Path src/lib/lab/persistence/artifact-service.ts -Pattern "campaign-images|generation_events|ai_model_selection|ai_model_catalog|process\.env\.(OPENAI|GEMINI)_API_KEY"` → 0 ocorrências.
- `Select-String -Path src/lib/lab/persistence/artifact-service.ts -Pattern 'LAB_ARTIFACT_BUCKET = "lab-artifacts"'` → 1 ocorrência.
- `git status --porcelain src/lib/campaign src/lib/ai src/lib/ai-cost prompts/` → vazio (nada produtivo alterado).
- Nenhuma chamada de rede nos testes (clients fakes; o script é exercitado apenas por funções puras).
</verification>

<success_criteria>
- Artefatos gravados exclusivamente em `lab-artifacts` com path `experiments/{experimentId}/runs/{runId}/output.{ext}` e nunca em `campaign-images`.
- Metadados (path, MIME, dimensões, bytes) + checksum SHA-256 registrados em `lab_artifacts`.
- Rollback do objeto quando o insert falha (nenhum órfão) e nenhum insert quando o upload falha.
- Leitura por URL assinada de 3600s gerada server-side, bucket permanecendo privado.
- Cleanup manual/opt-in por idade ou arquivamento, com run em andamento protegido e metadados/histórico preservados.
</success_criteria>

<output>
Create `.planning/phases/48.1-laboratorio-ia-minimo/48-1-06-SUMMARY.md` when done.
</output>

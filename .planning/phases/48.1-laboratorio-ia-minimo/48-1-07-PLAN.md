---
phase: 48.1
plan: 48-1-07
type: execute
wave: 4
depends_on:
  - 48-1-03
  - 48-1-04
  - 48-1-05
  - 48-1-06
files_modified:
  - src/lib/lab/technical-validation.ts
  - src/lib/lab/run-snapshot.ts
  - src/lib/lab/run-service.ts
  - src/lib/lab/__tests__/technical-validation.test.ts
  - src/lib/lab/__tests__/run-snapshot.test.ts
  - src/lib/lab/__tests__/run-service.test.ts
autonomous: true
requirements:
  - lab-runs
must_haves:
  truths:
    - "A validação técnica usa `sharp` e devolve apenas fatos objetivos (decodificável, MIME real, dimensões, proporção, bytes, vazia/corrompida/uniforme); `structuredOutputValid` e `ocrAlert` são sempre `null` e nenhuma nota de qualidade é produzida"
    - "O snapshot completo do run é montado ANTES da reserva e enviado em `p_snapshot`; nenhum run é criado com snapshot vazio"
    - "O snapshot congela cenário/versão/hash, prompt (nome/conteúdo/hash/origem), capability, modelTarget, params, changedDimension 'prompt', variantRole, codeVersion, baselineConfig/candidateConfig e `runType: 'lab'`"
    - "A reserva atômica `lab_reserve_run` é chamada antes de qualquer chamada paga e seus erros são mapeados (budget_exceeded, run_already_active, missing_snapshot, missing_operation_id, experiment_not_found, experiment_not_ready, idempotency_conflict, variant_not_in_experiment, scenario_not_in_experiment, repetition_out_of_range, invalid_supersedes_run)"
    - "`run_sequence` é sempre o valor devolvido pelo banco; o cliente nunca o calcula nem o envia"
    - "Reenviar a mesma `operationId` devolve o run existente e NÃO dispara nenhuma chamada paga"
    - "Reexecução explícita passa `supersedes_run_id` validado pelo banco e cria um novo run preservando o histórico"
    - "O run transita para estado terminal (`succeeded|failed|cancelled|timeout`) em `finally`, mesmo com erro, exceção ou desconexão do cliente"
    - "Cada run faz exatamente 1 chamada `campaign_image` (1 envelope no sink), inclusive quando a chamada falha — fallback automático desabilitado"
    - "Falha de persistência do artefato marca o run como `failed` com `errorType: 'artifact_persistence_failed'` e não deixa artefato órfão"
    - "A reconciliação preguiçosa marca como `failed` runs órfãos em `pending` E em `running` após `LAB_RUN_STALE_MS`, sem scheduler"
    - "Custo é persistido com a origem completa (`estimated_cost_usd` + `cost_detail` com a CostResolution real) e erro é sanitizado por `sanitizeAiErrorMessage`"
  artifacts:
    - path: "src/lib/lab/technical-validation.ts"
      provides: "validação técnica objetiva com sharp (decode, MIME real, dimensões, proporção, bytes, uniformidade)"
      exports:
        - LAB_UNIFORM_STDDEV_THRESHOLD
        - validateArtifactTechnically
    - path: "src/lib/lab/run-snapshot.ts"
      provides: "montagem e validação do snapshot imutável do run"
      exports:
        - buildLabRunSnapshot
        - assertSnapshotComplete
        - readCodeVersion
    - path: "src/lib/lab/run-service.ts"
      provides: "reserva atômica, transições do run, idempotência/reexecução, execução single-shot e reconciliação de órfãos"
      exports:
        - LabReservationError
        - reserveLabRun
        - markRunRunning
        - finalizeLabRun
        - reconcileStaleRuns
        - prepareLabRun
        - runReservedLabRun
        - executeLabRun
  key_links:
    - from: "src/lib/lab/run-service.ts"
      to: "public.lab_reserve_run"
      via: "rpc com p_snapshot completo e p_operation_id antes de qualquer chamada paga"
      pattern: "lab_reserve_run"
    - from: "src/lib/lab/run-service.ts"
      to: "src/lib/lab/gateway/runtime.ts"
      via: "runLabCampaignImage — exatamente uma invocação de campaign_image por run"
      pattern: "runLabCampaignImage"
    - from: "src/lib/lab/run-service.ts"
      to: "src/lib/lab/persistence/artifact-service.ts"
      via: "persistOutputArtifact do buffer decodificado do envelope, com falha mapeada para artifact_persistence_failed"
      pattern: "persistOutputArtifact"
    - from: "src/lib/lab/run-service.ts"
      to: "src/lib/lab/technical-validation.ts"
      via: "validateArtifactTechnically do buffer gravado, persistido em lab_runs.technical_validation"
      pattern: "validateArtifactTechnically"
    - from: "src/lib/lab/run-snapshot.ts"
      to: "public.lab_runs.snapshot"
      via: "objeto congelado gravado na transação da reserva (p_snapshot), nunca vazio"
      pattern: "runType"
---

<objective>
Entregar a execução real e focada do laboratório: validação técnica objetiva com `sharp`, montagem do snapshot imutável, reserva atômica via RPC `lab_reserve_run` antes de qualquer chamada paga, transições do run com terminal garantido em `finally`, idempotência por `operationId`, reexecução explícita por `supersedes_run_id` com `run_sequence` derivado no banco, persistência de artefato e reconciliação preguiçosa de runs órfãos (`pending` **e** `running`).

Purpose: garantir que cada run produza evidência comparável e auditável (D8/D9) sem nunca disparar uma segunda chamada paga (D7/D14) e sem deixar um run preso bloqueando o experimento (D14).
Output: `src/lib/lab/technical-validation.ts`, `src/lib/lab/run-snapshot.ts`, `src/lib/lab/run-service.ts` e testes verdes, consumidos por 48-1-08 (rota NDJSON de execução) e 48-1-09/10 (UI).
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48.1-laboratorio-ia-minimo/48.1-CONTEXT.md
@openspec/changes/fase-48-1-laboratorio-ia-minimo/design.md
@openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-runs/spec.md
@supabase/migrations/20260915000003_f48_1_lab_immutability_and_reserve.sql
@src/lib/ai/types.ts
@src/lib/ai-cost/types.ts

<interfaces>
Contratos extraídos dos planos anteriores e do código (usar diretamente — não explorar o repositório):

- `src/lib/lab/limits.ts` (48-1-02): `LAB_RUN_STALE_MS = 900000`, `MAX_RUNS_PER_EXPERIMENT`, `MAX_CONCURRENT_LAB_RUNS`.
- `src/lib/lab/scenarios/service.ts` (48-1-03): `loadScenarioFixture(slug)` devolve `{ content, contentHash, fixturePath, imagesDataUrls, logoDataUrl }`.
- `src/lib/lab/scenarios/mapper.ts` (48-1-03): `mapScenarioToCampaignBrief(content, imagesDataUrls)` e `mapScenarioToResolvedContext(content, imagesDataUrls, logoDataUrl)`.
- `src/lib/lab/domain/prompt-snapshot.ts` (48-1-04): `PROMPT_UNDER_TEST`, `buildBaselinePromptSnapshot()`, `buildCandidatePromptSnapshot({ promptName, promptContent })`; shape do snapshot de prompt `{ name, content, contentHash, source }` com `source: "official" | "override"`.
- `src/lib/lab/domain/experiment-service.ts` (48-1-04): `EXPERIMENT_TRANSITIONS`, `assertTransitionAllowed(from, to)`, `assertConfigurationEditable`.
- `src/lib/lab/gateway/runtime.ts` (48-1-05): `createLabGateway({ fixedTarget, fallbackResolver, adapters })`, `createLabTelemetryContext({ sink, operationRunId, traceId, storeId, userId? })` (`operationRunType: "campaign_delivery"`), `runLabCampaignImage({ gateway, request, telemetry })` (uma única invocação de `campaign_image`, sem `target`), `createDefaultLabRuntime({ fixedTarget })` (dynamic import de `@/lib/ai`).
- `src/lib/lab/gateway/noop-image-provider.ts` (48-1-05): `createNoopImageProvider()` — provider stub que lança se invocado; usado apenas para instanciar `ImageGenerationService` e chamar `buildDirectorPrompt`.
- `src/lib/lab/gateway/lab-prompt-loader.ts` (48-1-05): `new LabPromptLoader([{ name, content }], promptsDir?)`.
- `src/lib/image-generation/services/image-generation-service.ts` (48-1-05): `buildDirectorPrompt(brief, context, options?)` público e aditivo.
- `src/lib/ai/lab-telemetry-sink.ts` (48-1-05): `class LabTelemetrySink` com `emit(envelope)`, `entries: readonly LabCallEntry[]` e `costSummary: CostResolution | null`; `LabCallEntry { capability, provider, model, protocol, status, durationMs, usage?, cost: CostResolution, errorType? }`. **Não** grava `generation_events`.
- `src/lib/lab/domain/cost-coverage.ts` (48-1-05): `deriveCostCoverage(cost): "complete" | "partial" | "missing"`.
- `src/lib/lab/persistence/artifact-service.ts` (48-1-06): `persistOutputArtifact({ client, experimentId, runId, buffer, mimeType, width, height })` → `{ artifactId, storagePath, checksum, bytes }` (rollback do objeto em falha de insert); `buildOutputArtifactPath({ experimentId, runId, mimeType })`; `LAB_ARTIFACT_BUCKET`.
- RPC (48-1-01): `lab_reserve_run(p_experiment_id UUID, p_variant_id UUID, p_scenario_version_id UUID, p_repetition_index INT, p_supersedes_run_id UUID, p_snapshot JSONB, p_operation_id UUID, p_actor_id UUID) RETURNS JSONB` → `{ success, idempotent, run_id, run_sequence? }`. Erros: `missing_snapshot`, `missing_operation_id`, `experiment_not_found`, `idempotency_conflict`, `experiment_not_ready`, `variant_not_in_experiment`, `scenario_not_in_experiment`, `repetition_out_of_range`, `invalid_supersedes_run`, `budget_exceeded`, `run_already_active`.
- Tabela `public.lab_runs`: `id, experiment_id, variant_id, scenario_version_id, repetition_index, run_sequence, supersedes_run_id, operation_id, status CHECK IN ('pending','running','succeeded','failed','cancelled','timeout'), snapshot JSONB NOT NULL CHECK (snapshot <> '{}'), provider, model, protocol, capability, attempts, latency_ms, usage, estimated_cost_usd NUMERIC(12,6), cost_detail JSONB, error_type, error_message, technical_validation JSONB, calls JSONB DEFAULT '[]', started_at, finished_at, created_by, created_at`. O trigger `trg_lab_runs_snapshot_immutable_fn` bloqueia UPDATE das colunas congeladas e **permite** atualizar apenas as colunas de resultado.
- `src/lib/ai/types.ts`: `AiInvocationResult { content?, imageBase64?, mimeType?, model, usage?, usageMeta?, providerReportedCostUsd? }`, `AiCallEnvelope`, `sanitizeAiErrorMessage(message)`.
- `src/lib/ai-cost/types.ts`: `CostResolution` com `estimatedCostUsd`, `providerReportedCostUsd?`, `costSource`, `pricingVersion?`, `costFormulaVersion?`, `costEstimationNote?`, `textComponentUsd?`, `imageToolComponentUsd?`, `imageToolPricingProvider?`, `imageToolPricingModel?`, `imageToolPricingVersion?`. **Não existe** `imageUnitUsd` nem `costPartial`.
- Request real de `campaign_image`: `{ prompt, productImagesDataUrls, identityImageUrl, tools: "image_generation", size, quality, signal }`.
- **Testes:** nenhuma chamada de rede/paga. O sink real é usado com `vi.mock("@/lib/ai-cost/cost-estimator")` (padrão de `src/lib/ai/__tests__/lab-telemetry-sink.test.ts`). `sharp` pode gerar buffers de teste localmente (sem rede).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validação técnica objetiva com sharp (sem nota de qualidade)</name>
  <files>src/lib/lab/technical-validation.ts, src/lib/lab/__tests__/technical-validation.test.ts</files>
  <read_first>
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/design.md` — seção "D9 — Validação automática limitada ao técnico"
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-runs/spec.md` — requirement "Validação automática limitada ao técnico" (3 cenários)
    - `.planning/phases/48.1-laboratorio-ia-minimo/48.1-CONTEXT.md` — D9
    - `src/lib/image-generation/config.ts` — `IMAGE_GENERATION_SIZE = "1024x1024"`, `IMAGE_GENERATION_QUALITY`
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-scenarios/spec.md` — modalidade suportada `1:1` (proporção esperada nesta fase)
  </read_first>
  <action>
Criar `src/lib/lab/technical-validation.ts` (task 7.1 do `tasks.md`), sem `import "server-only"` (módulo puro, testável e reutilizável). Usar `sharp` (já é dependência; import default).

Exportar `LAB_UNIFORM_STDDEV_THRESHOLD = 1` com comentário documentando o critério: desvio-padrão de **todos** os canais abaixo do limiar ⇒ imagem uniforme (branca/preta/vazia).

Exportar `interface LabTechnicalValidation { decodable: boolean; mimeType: string | null; width: number | null; height: number | null; bytes: number; aspectRatio: number | null; uniform: boolean | null; emptyOrCorrupt: boolean; alerts: string[]; structuredOutputValid: null; ocrAlert: null }`.

Exportar `validateArtifactTechnically(params: { buffer: Buffer; declaredMimeType?: string | null; expectedAspectRatio?: number }): Promise<LabTechnicalValidation>`. Regras:
1. `bytes = buffer.byteLength`. Buffer vazio ou `byteLength === 0` → `{ decodable: false, mimeType: null, width: null, height: null, bytes: 0, aspectRatio: null, uniform: null, emptyOrCorrupt: true, alerts: ["empty_buffer"], structuredOutputValid: null, ocrAlert: null }` (sem lançar).
2. `await sharp(buffer).metadata()` em `try/catch`: exceção → `emptyOrCorrupt: true` com alert `decode_failed` e `decodable: false` (sem lançar).
3. MIME real derivado de `metadata.format`: `png` → `image/png`, `jpeg` → `image/jpeg`, `webp` → `image/webp`; outro/ausente → `mimeType: null` + alert `unexpected_mime_type`.
4. Se `declaredMimeType` for informado e diferente do MIME real → alert `mime_mismatch` (o MIME real prevalece).
5. `width`/`height` de `metadata`; `aspectRatio = width / height` (null quando algum for ausente). Se `expectedAspectRatio` for informado e `Math.abs(aspectRatio - expectedAspectRatio) > 0.01` → alert `aspect_ratio_mismatch`.
6. Uniformidade: `await sharp(buffer).stats()`; `uniform = stats.channels.every((channel) => channel.stdev < LAB_UNIFORM_STDDEV_THRESHOLD)`; `uniform === true` → alert `uniform_image`. Falha em `stats()` não lança: `uniform = null` e alert `stats_unavailable`.
7. `decodable = true` quando o metadata foi lido com sucesso e as dimensões existem.
8. `structuredOutputValid` e `ocrAlert` são **sempre** `null` (campos reservados; não há capacidade de texto em F48.1 e o OCR está adiado).

Proibido no arquivo: qualquer cálculo de nota/score de beleza, composição, apelo comercial, profissionalismo ou "publicável"; nenhuma chamada a modelo textual; nenhuma string contendo `score`, `rating`, `beauty`, `quality_score`, `publishable`. O módulo **não** pode lançar exceção em nenhum caminho (a falha vira alerta).

Criar `src/lib/lab/__tests__/technical-validation.test.ts` gerando os buffers de teste com o próprio `sharp` (nenhuma rede, nenhum download): PNG 64×64 de cor sólida → `decodable: true`, `mimeType: "image/png"`, `width/height` 64, `aspectRatio` 1, `uniform: true` com alert `uniform_image`; JPEG com gradiente/ruído (criar via `sharp` com `create` + ruído determinístico, ou compor duas metades de cores diferentes) → `uniform: false`; buffer com bytes aleatórios → `decodable: false`, `emptyOrCorrupt: true`, alert `decode_failed`, sem exceção; `Buffer.alloc(0)` → alert `empty_buffer`, `bytes: 0`; imagem 64×32 com `expectedAspectRatio: 1` → alert `aspect_ratio_mismatch`; `declaredMimeType: "image/jpeg"` com PNG real → alert `mime_mismatch` e `mimeType: "image/png"`; `structuredOutputValid === null` e `ocrAlert === null` em todos os casos; o objeto retornado não possui nenhuma chave casando `/score|rating|quality|publishable|beauty/i`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/lab/__tests__/technical-validation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/lab/technical-validation.ts` existe e exporta `LAB_UNIFORM_STDDEV_THRESHOLD` e `validateArtifactTechnically`.
    - `Select-String -Path src/lib/lab/technical-validation.ts -Pattern "structuredOutputValid|ocrAlert"` encontra ambos, atribuídos como `null`.
    - `Select-String -Path src/lib/lab/technical-validation.ts -Pattern "score|rating|beauty|publishable|quality_score"` retorna 0 ocorrências.
    - `Select-String -Path src/lib/lab/technical-validation.ts -Pattern "throw "` retorna 0 ocorrências.
    - `npx vitest run src/lib/lab/__tests__/technical-validation.test.ts` passa com exit code 0, cobrindo válida/corrompida/uniforme.
    - `npx tsc -p tsconfig.typecheck.json --noEmit` continua verde.
  </acceptance_criteria>
  <done>Validação técnica objetiva com `sharp` cobrindo decodificação, MIME real, dimensões, proporção, bytes, imagem vazia/corrompida/uniforme, com campos reservados `null` e zero nota de qualidade automática.</done>
</task>

<task type="auto">
  <name>Task 2: Snapshot imutável + reserva atômica + transições do run + reconciliação de órfãos</name>
  <files>src/lib/lab/run-snapshot.ts, src/lib/lab/run-service.ts, src/lib/lab/__tests__/run-snapshot.test.ts, src/lib/lab/__tests__/run-service.test.ts</files>
  <read_first>
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/design.md` — seções "D8 — Snapshots imutáveis, idempotência e histórico de reexecução", "RPC de reserva atômica (sketch)" e "Estados e transições" (bloco do run)
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-runs/spec.md` — requirements "Snapshot imutável por execução", "Reserva atômica de execução", "Idempotência e histórico explícito de reexecução", "Estados e transições do run"
    - `supabase/migrations/20260915000003_f48_1_lab_immutability_and_reserve.sql` — assinatura exata da RPC, ordem das checagens e os 11 códigos de erro
    - `supabase/migrations/20260915000002_f48_1_create_lab_tables.sql` — colunas de `lab_runs` e o índice único parcial **global** de run ativo (`uq_lab_runs_one_active_global`)
    - `.planning/phases/48.1-laboratorio-ia-minimo/48.1-CONTEXT.md` — D8, D14 e os blocos "Constantes de limite" e "Estados e transições (LOCKED)"
    - `src/lib/lab/domain/prompt-snapshot.ts` e `src/lib/lab/limits.ts` (planos 48-1-04/02)
  </read_first>
  <action>
**A) `src/lib/lab/run-snapshot.ts`** (parte de 7.2) — puro, sem `import "server-only"`:

- `interface LabRunSnapshot { scenarioVersionId; scenarioVersion; scenarioContentHash; prompt: { name; content; contentHash; source: "official" | "override" }; capability: "campaign_image"; modelTarget: { provider; model; protocol }; params: { size; quality; skipInputValidation: true }; changedDimension: "prompt"; variantRole: "baseline" | "candidate"; codeVersion: { gitSha?: string; buildId?: string } | null; baselineConfig: { promptName; promptContentHash; source }; candidateConfig: { promptName; promptContentHash; source }; runType: "lab" }`.
- `buildLabRunSnapshot(params): LabRunSnapshot` — monta o objeto exatamente com os campos acima; **não** inclui conteúdo de prompt duplicado nas configs comparativas (apenas `promptName`/`promptContentHash`/`source`); `capability` sempre `"campaign_image"`, `changedDimension` sempre `"prompt"`, `runType` sempre `"lab"`.
- `assertSnapshotComplete(snapshot): void` — lança `Error("missing_snapshot")` quando: snapshot ausente; `prompt.content`/`prompt.contentHash`/`prompt.name` vazios; `scenarioVersionId`/`scenarioContentHash` vazios; `modelTarget.provider|model|protocol` vazios; `variantRole` fora de `baseline|candidate`; ou o objeto serializado for igual a `{}`. Usada como barreira antes da RPC (nunca enviar `p_snapshot` vazio).
- `readCodeVersion(): { gitSha?: string; buildId?: string } | null` — lê apenas variáveis de build (`VERCEL_GIT_COMMIT_SHA`, `GIT_SHA`, `VERCEL_DEPLOYMENT_ID`); quando nenhuma existe retorna `null`. **Não** lê env-var de modelo/provider/chave.

**B) `src/lib/lab/run-service.ts`** (tasks 7.2/7.3/7.4) com `import "server-only";`. O client Supabase entra **por parâmetro** em todas as funções.

- `const LAB_RESERVATION_ERROR_CODES = [...] as const` com os 11 códigos; `class LabReservationError extends Error` com `readonly code: LabReservationErrorCode`.
- `reserveLabRun(params: { client; experimentId; variantId; scenarioVersionId; repetitionIndex; supersedesRunId: string | null; snapshot: LabRunSnapshot; operationId: string; actorId: string }): Promise<{ runId: string; runSequence: number | null; idempotent: boolean }>`:
  1. `assertSnapshotComplete(snapshot)` antes de qualquer I/O.
  2. `client.rpc("lab_reserve_run", { p_experiment_id, p_variant_id, p_scenario_version_id, p_repetition_index, p_supersedes_run_id, p_snapshot: snapshot, p_operation_id: operationId, p_actor_id: actorId })`.
  3. `error` → extrair o código do `error.message` (a mensagem contém o código); quando casar um dos 11 → `throw new LabReservationError(code)`; caso contrário → `LabReservationError("lab_reservation_failed")`.
  4. Sucesso → `{ runId: data.run_id, runSequence: data.run_sequence ?? null, idempotent: data.idempotent === true }`. O `runSequence` **nunca** é calculado no cliente.
- `markRunRunning(params: { client; runId; startedAt? }): Promise<void>` — **transição compare-and-set**: `update` em `lab_runs` com `status: "running"`, `started_at: startedAt ?? now`, `attempts: 1`, filtrando `id = runId` **e `status = 'pending'`**, com `.select("id")`; exige exatamente **1 linha afetada** (senão `lab_run_transition_failed`). Só colunas de resultado (o trigger permite). Um run já reconciliado/terminal nunca é sobrescrito.
- `finalizeLabRun(params: {...}): Promise<void>` — **transição compare-and-set**: `update` com as colunas de resultado presentes + `finished_at`, filtrando `id = runId` **e `status IN ('pending','running')`**, com `.select("id")`; exige exatamente **1 linha afetada** (senão `lab_run_transition_failed`). `errorMessage` **sempre** passa por `sanitizeAiErrorMessage` antes de gravar. Nunca grava `snapshot` (imutável).
- `reconcileStaleRuns(params: { client; now?: Date; staleMs?: number }): Promise<{ reconciled: number }>` — seleciona `lab_runs` com `status IN ('pending','running')` cujo `coalesce(started_at, created_at)` seja anterior a `now - staleMs` (default `LAB_RUN_STALE_MS`) para o curto-circuito e, em seguida, faz `update` para `status: "failed"`, `error_type: "orphan_run_timeout"`, `error_message: "Run órfão marcado como falho"`, `finished_at: now` **reaplicando atomicamente no próprio update** o estado de origem **e** o cutoff de `coalesce(started_at, created_at)`, com `.select("id")`, contando **apenas as linhas efetivamente alteradas**. O `coalesce` é expresso por dois ramos mutuamente exclusivos de estado: `status = 'running' AND started_at < cutoff` e `status = 'pending' AND started_at IS NULL AND created_at < cutoff`. Isso fecha a corrida em que um run `pending` antigo é promovido a `running` com `started_at` recente entre o select e o update — ele deixa de casar o cutoff e **não** é reconciliado (o executor continua dono do run). Cobre os **dois** estados ativos (um `pending` preso nunca bloqueia o experimento por causa do índice único parcial). Sem scheduler: é chamada na leitura (48-1-08).
- `isRunTerminal(status): boolean` — `true` para `succeeded|failed|cancelled|timeout`.

**C) Testes.**

`src/lib/lab/__tests__/run-snapshot.test.ts`: snapshot tem `runType === "lab"`, `capability === "campaign_image"`, `changedDimension === "prompt"`, `variantRole` correto, `codeVersion` `null` sem env de build; `assertSnapshotComplete` aceita o snapshot válido e lança `missing_snapshot` para: snapshot vazio, `prompt.content` vazio, `prompt.contentHash` vazio, `modelTarget.protocol` vazio, `variantRole` inválido; `buildLabRunSnapshot` não inclui base64/imagem no objeto (assertar ausência de chaves contendo `base64`/`dataUrl`); `readCodeVersion()` devolve `null` sem env e `{ gitSha }` quando `VERCEL_GIT_COMMIT_SHA` está setado (restaurar env no `afterEach`).

`src/lib/lab/__tests__/run-service.test.ts` com client fake (registra chamadas de `rpc`/`from(...).update(...)` e devolve filas configuráveis), cobrindo: `reserveLabRun` chama `rpc("lab_reserve_run", ...)` com os 8 parâmetros `p_*` exatos e `p_snapshot` não vazio; cada um dos 11 códigos de erro vira `LabReservationError` com `.code` correspondente; erro desconhecido → `lab_reservation_failed`; resposta com `idempotent: true` devolve `{ idempotent: true, runId }`; `runSequence` é exatamente o valor devolvido pela RPC (testar com `run_sequence: 7`); `markRunRunning` grava `status: "running"` + `started_at`; **`markRunRunning` com 0 linhas afetadas lança `lab_run_transition_failed`**; `finalizeLabRun` grava `status`/`finished_at` e sanitiza `errorMessage` (entrada com `Bearer sk-abc123` → saída sem o token); **`finalizeLabRun` com 0 linhas afetadas lança `lab_run_transition_failed`**; `reconcileStaleRuns` marca `pending` **e** `running` antigos como `failed` com `orphan_run_timeout`, ignora runs recentes e ignora runs já terminais, e **conta apenas as linhas efetivamente alteradas** (quando o update retorna menos linhas que o select, `reconciled` reflete o retorno); **o cutoff é reaplicado no próprio update** (ramos `running`/`pending`) — teste da corrida `pending` antigo → `running` com `started_at` recente entre select e update espera `reconciled: 0`; nenhuma chamada de rede e nenhuma chamada paga.
  </action>
  <verify>
    <automated>npx vitest run src/lib/lab/__tests__/run-snapshot.test.ts src/lib/lab/__tests__/run-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/lab/run-snapshot.ts` exporta `buildLabRunSnapshot`, `assertSnapshotComplete` e `readCodeVersion`.
    - `src/lib/lab/run-service.ts` exporta `LabReservationError`, `reserveLabRun`, `markRunRunning`, `finalizeLabRun`, `reconcileStaleRuns`.
    - `Select-String -Path src/lib/lab/run-service.ts -Pattern 'rpc\("lab_reserve_run"'` encontra exatamente 1 chamada com os 8 parâmetros `p_`.
    - `Select-String -Path src/lib/lab/run-service.ts -Pattern "budget_exceeded|run_already_active|missing_snapshot|missing_operation_id|experiment_not_found|idempotency_conflict|experiment_not_ready|variant_not_in_experiment|scenario_not_in_experiment|repetition_out_of_range|invalid_supersedes_run"` encontra os 11 códigos.
    - `Select-String -Path src/lib/lab/run-service.ts -Pattern "pending","running"` confirma que a reconciliação cobre os dois estados ativos.
    - `Select-String -Path src/lib/lab/run-service.ts -Pattern "sanitizeAiErrorMessage"` encontra o uso na gravação de erro.
    - `Select-String -Path src/lib/lab/run-service.ts -Pattern "generation_events|AiCostTracker|ai_model_selection"` retorna 0 ocorrências.
    - `npx vitest run src/lib/lab/__tests__/run-snapshot.test.ts src/lib/lab/__tests__/run-service.test.ts` passa com exit code 0.
    - `npx tsc -p tsconfig.typecheck.json --noEmit` continua verde.
  </acceptance_criteria>
  <done>Snapshot completo montado e validado antes da reserva; RPC `lab_reserve_run` chamada com os 8 parâmetros e com os 11 códigos de erro mapeados; `run_sequence` vindo do banco; transições de resultado gravadas com erro sanitizado; reconciliação de órfãos cobrindo `pending` e `running`.</done>
</task>

<task type="auto">
  <name>Task 3: executeLabRun — reserva antes da chamada paga, 1 envelope por run e terminal em finally</name>
  <files>src/lib/lab/run-service.ts, src/lib/lab/__tests__/run-service.test.ts</files>
  <read_first>
    - `src/lib/lab/run-service.ts` (Task 2) — `reserveLabRun`, `markRunRunning`, `finalizeLabRun`, `LabReservationError`
    - `src/lib/lab/run-snapshot.ts` (Task 2) — `buildLabRunSnapshot`
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/design.md` — "D7 — Caminho de execução" (itens 1-6) e "Tratamento de falhas"
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-runs/spec.md` — requirements "Execução real e focada reutilizando o gateway" e "Estados e transições do run"
    - `src/lib/lab/gateway/runtime.ts` e `src/lib/ai/lab-telemetry-sink.ts` (48-1-05) — contratos do runtime e do sink
    - `src/lib/lab/persistence/artifact-service.ts` (48-1-06) — `persistOutputArtifact`
    - `src/lib/lab/technical-validation.ts` (Task 1) — `validateArtifactTechnically`
    - `src/lib/ai/__tests__/lab-telemetry-sink.test.ts` (48-1-05) — padrão de mock de `@/lib/ai-cost/cost-estimator`
  </read_first>
  <action>
Estender `src/lib/lab/run-service.ts` com a orquestração da execução (tasks 7.2/7.3/7.4), **em duas funções separáveis** para que a rota 48-1-08 mapeie erros de reserva para HTTP **antes** de abrir o stream NDJSON:

**A) `prepareLabRun(params: { client; experimentId; variantId; scenarioVersionId; repetitionIndex; supersedesRunId: string | null; operationId; actorId; scenario: { id; version; contentHash }; experiment: { modelTarget; params }; variant: { role; promptSnapshot }; variants: { baseline; candidate } }): Promise<{ runId: string; snapshot: LabRunSnapshot; idempotent: boolean }>`** — passos 1 e 2 apenas:
1. Montar o snapshot com `buildLabRunSnapshot(...)` usando `experiment.modelTarget`/`experiment.params`, `scenario.id/version/contentHash`, o `promptSnapshot` da variante e as configs comparativas das duas variantes (`variants.baseline`/`variants.candidate`), `variantRole = variant.role`, `codeVersion = readCodeVersion()`.
2. `reserveLabRun(...)` com `p_snapshot` = snapshot completo e `p_operation_id` = `operationId`. Erro → propagar `LabReservationError` (nenhuma execução acontece). `idempotent === true` → retornar `{ runId, snapshot, idempotent: true }` sem executar nada.

**B) `runReservedLabRun(params: { client; experimentId; runId; snapshot; actorId; scenario: { imagesDataUrls; logoDataUrl; brief; context }; experiment: { params }; gateway: AiInvoker; sink: LabTelemetrySink; promptLoader: LabPromptLoader; imageService: ImageGenerationService; onEvent?: (event: LabRunEvent) => void }): Promise<{ runId: string; status: LabRunStatus }>`** — passos 3 a 10:
3. `markRunRunning(...)` (emite fase `running`) e, a partir daqui, envolver todo o restante em `try/catch/finally` com transição terminal garantida no `finally`.
4. Montar o prompt: `imageService.buildDirectorPrompt(brief, context)` com o `LabPromptLoader` já injetado servindo o `promptSnapshot` da variante sob teste (o prompt oficial nunca é escrito). Emitir fase `prompt`.
5. Construir o request real de `campaign_image`: `{ prompt, productImagesDataUrls: Object.values(scenario.imagesDataUrls), identityImageUrl: scenario.logoDataUrl, tools: "image_generation", size: experiment.params.size, quality: experiment.params.quality }` e chamar **`runLabCampaignImage({ gateway, request, telemetry })`** — exatamente **uma** invocação, sem `target`, sem `hasFallback`, sem `campaign_image_edit`. Emitir fase `generation`.
6. Sucesso: `imageBase64` obrigatório (ausente → erro `missing_image_payload`); decodificar para `Buffer` (`Buffer.from(imageBase64, "base64")`).
7. `validateArtifactTechnically({ buffer, declaredMimeType: result.mimeType ?? null, expectedAspectRatio: 1 })` **antes** da persistência; emitir fase `validation`; derivar o MIME **real dos bytes** (`resolveArtifactMimeType(validation)`) — bytes indecodificáveis ou MIME fora da allowlist do bucket → `unsupported_artifact_mime_type`, run `failed` e **nada persistido**.
8. `persistOutputArtifact({ client, experimentId, runId, buffer, mimeType, width: validation.width, height: validation.height })` — **uma única operação** grava MIME real + dimensões (sem update separado de `lab_artifacts`). Emitir fase `artifact`. Falha de persistência → `errorType: "artifact_persistence_failed"` (o rollback do objeto é do 48-1-06) e run `failed`.
9. Finalizar `succeeded` com as evidências do sink: `latencyMs` (medido com `performance.now()`), `usage` do último envelope, `estimatedCostUsd`/`costDetail` do `sink.costSummary` (CostResolution real completa), `calls` = `sink.entries` (apenas capability/provider/model/protocol/status/durationMs/usage/custo/errorType), `technicalValidation`, `provider`/`model`/`protocol`/`capability` do envelope, `attempts = sink.entries.length`. Emitir fase `done` com `{ runId, status: "succeeded" }`.
10. `catch`: sanitizar a mensagem **uma única vez** (`safeMessage = sanitizeAiErrorMessage(message)`) e usar a **mesma** mensagem no banco e no evento; `finalizeLabRun` com `status: "failed"`, `errorType` (ex.: `provider_error`, `missing_image_payload`, `artifact_persistence_failed`) e **as mesmas evidências do sink** já coletadas (`calls`/`usage`/`costDetail`/`estimatedCostUsd`/`provider`/`model`/`protocol`/`capability`/`attempts` + `technicalValidation` quando já calculada) — uma chamada paga que falhou continua auditável; emitir fase `error` com `{ code, message: safeMessage }` **sanitizado** (nenhum token/URL vaza no NDJSON).
11. `finally`: se o run ainda não estiver em estado terminal (flag local), `finalizeLabRun({ status: "failed", errorType: "run_aborted", errorMessage: "Execução interrompida" })` — garante terminal mesmo com desconexão do cliente; a transição é compare-and-set, então nunca sobrescreve um run já reconciliado.

**C) `executeLabRun(params)`** — composição de conveniência: `prepareLabRun(...)` e, quando `idempotent === false`, `runReservedLabRun(...)`; retorna `{ runId, status, idempotent }` (`status: "pending"` no caminho idempotente). Usada por testes e por qualquer chamada single-shot.

Exportar também `interface LabRunEvent { type: "phase" | "done" | "error"; phase?: string; status?: string; message?: string; runId?: string; code?: string }` e `type LabRunStatus = "pending" | "running" | "succeeded" | "failed" | "cancelled" | "timeout"`.

Proibido no arquivo: `OpenAIImageProvider`, `providers/openai`, `campaign_image_edit`, `hasFallback`, `generation_events`, `AiCostTracker`, `ai_model_selection`, `campaigns`, `campaign-images`.

Estender `src/lib/lab/__tests__/run-service.test.ts` (novos `describe` para `prepareLabRun`/`runReservedLabRun`/`executeLabRun`) com: um `AiInvoker` fake que conta invocações e devolve `{ imageBase64: <PNG 8x8 gerado com sharp em base64>, mimeType: "image/png", model: "gpt-5.5", usageMeta: { imageGenerationTool: true } }`; sink real com `vi.mock("@/lib/ai-cost/cost-estimator")` devolvendo uma `CostResolution` completa conhecida; client fake com `rpc` devolvendo `{ run_id: "run-1", run_sequence: 1, idempotent: false }` e capturando os `update` de `lab_runs`/`lab_artifacts`. Casos: `prepareLabRun` devolve `runId` + `snapshot` não vazio e faz exatamente 1 chamada de RPC; caminho feliz → 1 invocação, 1 entrada no sink, run finaliza `succeeded` com `latency_ms`/`usage`/`estimated_cost_usd`/`cost_detail`/`calls`/`technical_validation` preenchidos e `attempts === 1`; reserva idempotente → `prepareLabRun` devolve `idempotent: true` e 0 invocações; erro do invoker → 1 invocação, run `failed` com `errorType` e `errorMessage` sanitizado (entrada com `Bearer sk-abc` → saída sem token) e `finished_at` preenchido; falha do `persistOutputArtifact` (fake que lança) → run `failed` com `artifact_persistence_failed`; `imageBase64` ausente → run `failed` com `missing_image_payload`; `error` de reserva (`budget_exceeded`) → nenhuma invocação, nenhum `update` de run, `LabReservationError` propagada; o `update` final nunca inclui a chave `snapshot` (imutabilidade). **Correções de revisão:** o **evento `error` do NDJSON** é sanitizado (sem `sk-`/token/URL); nas **3 falhas** (provider, imagem ausente, artefato) o update final preserva `calls` (1), `estimated_cost_usd`, `cost_detail`, `usage`, `provider`/`model`/`protocol` e `attempts === 1`; MIME real fora da allowlist (bytes não-imagem) → run `failed` com `unsupported_artifact_mime_type` e **zero** `persistOutputArtifact`; o artefato é persistido com **MIME real + dimensões numa única chamada** (`persistOutputArtifact` com `mimeType`/`width`/`height`) e **sem update separado** de `lab_artifacts`; `markRunRunning`/`finalizeLabRun` com **0 linhas afetadas** lançam `lab_run_transition_failed` (CAS); a reconciliação conta apenas as linhas devolvidas pelo update. Nenhuma chamada de rede e nenhuma chamada paga.
  </action>
  <verify>
    <automated>npx vitest run src/lib/lab/__tests__/run-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String -Path src/lib/lab/run-service.ts -Pattern "prepareLabRun|runReservedLabRun|executeLabRun|runLabCampaignImage|persistOutputArtifact|validateArtifactTechnically"` encontra as seis referências.
    - `Select-String -Path src/lib/lab/run-service.ts -Pattern "runLabCampaignImage\("` encontra exatamente 1 chamada.
    - `Select-String -Path src/lib/lab/run-service.ts -Pattern "OpenAIImageProvider|providers/openai|campaign_image_edit|hasFallback"` retorna 0 ocorrências.
    - `Select-String -Path src/lib/lab/run-service.ts -Pattern "generation_events|AiCostTracker|ai_model_selection"` retorna 0 ocorrências.
    - `Select-String -Path src/lib/lab/run-service.ts -Pattern "finally"` confirma o bloco de transição terminal.
    - O teste asserta `invoker` chamado exatamente 1 vez no caminho feliz e 0 vezes no caminho idempotente e no caminho de erro de reserva.
    - `npx vitest run src/lib/lab/__tests__/run-service.test.ts` passa com exit code 0.
    - `npx tsc -p tsconfig.typecheck.json --noEmit` continua verde.
    - `npx vitest run src/lib/lab` permanece verde.
  </acceptance_criteria>
  <done>`prepareLabRun` reserva antes de qualquer chamada paga e devolve o snapshot; `runReservedLabRun` executa exatamente 1 `campaign_image` por run, persiste artefato + validação técnica, congela custo com origem completa e garante estado terminal em `finally`; `executeLabRun` compõe os dois e retorna sem executar quando a reserva é idempotente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rota/serviço → RPC `lab_reserve_run` | ponto único de serialização que precede qualquer gasto |
| serviço → gateway F46 | chamada paga real sob comando humano |
| provider → run-service | bytes de imagem e erros de provider entram no domínio |
| run-service → `lab_runs` | gravação do snapshot (imutável) e das colunas de resultado |
| run órfão → leitura | processo morto pode deixar run ativo bloqueando o laboratório (concorrência global) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-1-48 | Denial of Service / financial | chamada paga antes da reserva | mitigate | Ordem fixa: snapshot → `reserveLabRun` → `markRunRunning` → gateway; erro de reserva propaga sem executar; teste prova 0 invocações em `budget_exceeded` |
| T-48-1-49 | Repudiation / Tampering | idempotência por `operationId` | mitigate | RPC vincula o `operation_id` ao payload e devolve o run existente; `executeLabRun` retorna sem chamar o gateway quando `idempotent === true`; teste prova 0 invocações |
| T-48-1-50 | Tampering | snapshot do run | mitigate | Snapshot montado antes da reserva e gravado por `p_snapshot` na mesma transação; `assertSnapshotComplete` bloqueia snapshot vazio; o serviço nunca grava `snapshot` em `update` (trigger do banco é o reforço) |
| T-48-1-51 | Tampering | `run_sequence` | mitigate | Sempre lido da resposta da RPC; nenhum cálculo de `max+1` no cliente (asserção por grep e teste com `run_sequence: 7`) |
| T-48-1-52 | Denial of Service / financial | segunda chamada paga (fallback) | mitigate | `runLabCampaignImage` faz exatamente 1 invocação sem `target`/`hasFallback`; proibido importar `OpenAIImageProvider`/`campaign_image_edit` (asserção por grep); teste prova 1 envelope mesmo em falha |
| T-48-1-53 | Denial of Service | run preso em `pending`/`running` | mitigate | Terminal garantido em `finally` + `reconcileStaleRuns` cobrindo os dois estados ativos com `LAB_RUN_STALE_MS`, sem scheduler |
| T-48-1-54 | Information Disclosure | erro do provider / `calls[]` | mitigate | `error_message` sempre sanitizado por `sanitizeAiErrorMessage`; `calls[]` só carrega capability/provider/model/protocol/status/latência/usage/custo/errorType (nenhuma chave, prompt de sistema ou header) |
| T-48-1-55 | Tampering | custo registrado | mitigate | `cost_detail` recebe a `CostResolution` real completa e `estimated_cost_usd` o agregado; proibido usar `imageUnitUsd`/`costPartial` (não existem) |
| T-48-1-56 | Repudiation | execução sem evidência de artefato | mitigate | Falha de persistência → run `failed` com `artifact_persistence_failed`; o rollback do objeto é do 48-1-06 (sem órfão); `technical_validation` sempre persistida no sucesso |
| T-48-1-57 | Tampering | prompt oficial | mitigate | O prompt é servido em memória pelo `LabPromptLoader` a partir do snapshot da variante; nenhuma escrita em `prompts/`; teste de 48-1-05 cobre `git status --porcelain prompts/` vazio |
| T-48-1-58 | Information Disclosure | evento `error` do stream NDJSON | mitigate | A mensagem é sanitizada **uma única vez** (`safeMessage = sanitizeAiErrorMessage(message)`) e a **mesma** string vai para o banco e para o evento — nenhum token/URL vaza no stream; teste asserta ausência de `sk-`/URL no evento `error` |
| T-48-1-59 | Repudiation | perda de evidência em falha | mitigate | `collectSinkEvidence(sink)` é aplicada **tanto no sucesso quanto na falha**: `calls`, `usage`, `cost_detail`, `estimated_cost_usd`, `provider`/`model`/`protocol` e `attempts` são persistidos mesmo quando o provider falha, a imagem falta ou o artefato não persiste; `attempts` só é sobrescrito quando houve chamada real |
| T-48-1-60 | Tampering | transições não atômicas | mitigate | `markRunRunning` (`pending → running`) e `finalizeLabRun` (`pending|running → terminal`) são **compare-and-set** (filtro de estado + `.select("id")` + exigência de exatamente 1 linha); a reconciliação **reaplica atomicamente no próprio update** o estado de origem **e** o cutoff de `coalesce(started_at, created_at)` (ramos `running`: `started_at < cutoff`; `pending`: `started_at IS NULL AND created_at < cutoff`) e conta apenas as linhas alteradas — nenhum run terminal é sobrescrito em nenhuma direção, e um run promovido a `running` recente entre o select e o update não é reconciliado |
| T-48-1-61 | Tampering | MIME/dimensões do artefato | mitigate | O MIME é derivado dos **bytes** pela validação técnica (`resolveArtifactMimeType(validation)`) e restrito à allowlist do bucket — fora dela/indecodificável → `unsupported_artifact_mime_type` sem persistir; MIME + dimensões entram numa **única** operação de persistência (sem update separado e sem erro de update ignorado) |
| T-48-1-SC | Tampering | instalação de dependências | mitigate | Nenhum pacote novo; `sharp`, `zod` e o client Supabase já existem. Instalação nova exigiria auditoria de legitimidade + checkpoint humano bloqueante |
</threat_model>

<verification>
- `npx vitest run src/lib/lab` → exit code 0 (technical-validation, run-snapshot, run-service).
- `npx tsc -p tsconfig.typecheck.json --noEmit` → exit code 0.
- `npm run lint` → exit code 0.
- `Select-String -Path src/lib/lab/run-service.ts,src/lib/lab/run-snapshot.ts,src/lib/lab/technical-validation.ts -Pattern "generation_events|AiCostTracker|ai_model_selection|campaign-images|OpenAIImageProvider|campaign_image_edit"` → 0 ocorrências.
- `Select-String -Path src/lib/lab/technical-validation.ts -Pattern "score|rating|beauty|publishable"` → 0 ocorrências.
- `npx vitest run src/lib/ai src/lib/image-generation` permanece verde (harness e pipeline intactos).
- `git status --porcelain src/lib/ai/gateway.ts src/lib/ai-cost src/lib/campaign prompts/ supabase/migrations/20260915*.sql` → apenas os arquivos desta fase já previstos, nenhuma alteração em objetos produtivos.
- Nenhuma chamada de rede nos testes (invoker/adapter fakes; `resolveAiCost` mockado).
</verification>

<success_criteria>
- Validação técnica objetiva com `sharp`, sem nenhuma nota de qualidade e com campos reservados `null`.
- Snapshot imutável completo montado antes da reserva e nunca vazio.
- Reserva atômica antes de qualquer chamada paga, com os 11 códigos de erro mapeados e `run_sequence` derivado no banco.
- Idempotência por `operationId` sem reexecução e reexecução explícita preservando histórico.
- Exatamente 1 chamada `campaign_image` por run, com falha do provider encerrando o run em estado terminal.
- Falha de artefato marca o run como `failed` sem órfão.
- Reconciliação preguiçosa cobre `pending` e `running` órfãos após `LAB_RUN_STALE_MS`.
</success_criteria>

<output>
Create `.planning/phases/48.1-laboratorio-ia-minimo/48-1-07-SUMMARY.md` when done.
</output>

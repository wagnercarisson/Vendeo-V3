---
phase: 48.1
plan: 48-1-08
type: execute
wave: 5
depends_on:
  - 48-1-07
files_modified:
  - src/lib/admin/schemas.ts
  - src/lib/ai/lab-cost-estimate.ts
  - src/lib/ai/__tests__/lab-cost-estimate.test.ts
  - src/lib/lab/api/estimate.ts
  - src/lib/lab/api/__tests__/estimate.test.ts
  - src/lib/lab/api/experiment-queries.ts
  - src/lib/lab/api/__tests__/experiment-queries.test.ts
  - src/lib/lab/api/evaluation-service.ts
  - src/lib/lab/api/__tests__/evaluation-service.test.ts
  - src/lib/lab/api/run-execution.ts
  - src/lib/lab/api/__tests__/run-execution.test.ts
  - src/app/api/admin/laboratorio/scenarios/route.ts
  - src/app/api/admin/laboratorio/experiments/route.ts
  - src/app/api/admin/laboratorio/__tests__/route.test.ts
  - src/app/api/admin/laboratorio/experiments/[id]/route.ts
  - src/app/api/admin/laboratorio/experiments/[id]/estimate/route.ts
  - src/app/api/admin/laboratorio/experiments/[id]/__tests__/route.test.ts
  - src/app/api/admin/laboratorio/runs/[id]/route.ts
  - src/app/api/admin/laboratorio/runs/[id]/__tests__/route.test.ts
  - src/app/api/admin/laboratorio/experiments/[id]/runs/route.ts
  - src/app/api/admin/laboratorio/experiments/[id]/runs/__tests__/route.test.ts
  - src/app/api/admin/laboratorio/experiments/[id]/evaluations/route.ts
  - src/app/api/admin/laboratorio/experiments/[id]/evaluations/__tests__/route.test.ts
autonomous: true
requirements:
  - lab-admin-api
must_haves:
  truths:
    - "Toda rota do laboratório exige admin: usuário não-admin recebe 403 e nenhuma operação é executada"
    - "Toda rota aplica a guarda de ambiente antes de qualquer acesso às tabelas `lab_*`, ao storage do laboratório ou aos providers; ambiente bloqueado → 403 com `{ error: 'environment_blocked', reason }`"
    - "Payload inválido → 400 com detalhes de validação e nenhuma mutação"
    - "`POST /experiments` cria o experimento prompt-only (2 variantes) e o autor é registrado; dimensão `model`/`configuration` → 400 `unsupported_changed_dimension`; alvo fora do catálogo → 400 `model_target_not_in_catalog`"
    - "`GET /experiments/[id]` devolve variantes, runs, avaliações e budget restante, e reconcilia runs órfãos antes de responder"
    - "`GET /experiments/[id]/estimate` devolve estimativa por componente com cobertura `complete|partial|missing` e nunca bloqueia a execução por pricing parcial"
    - "`POST /experiments/[id]/runs` sem `confirmed: true` → 422 `confirmation_required` e nenhuma chamada paga é iniciada"
    - "`POST /experiments/[id]/runs` mapeia `budget_exceeded`/`run_already_active`/`idempotency_conflict`/`experiment_not_ready` → 409, `missing_snapshot`/`missing_operation_id`/relações inválidas → 400, `experiment_not_found` → 404 e `unsupported_scenario_mode` → 400, todos antes de abrir o stream"
    - "Operação idempotente reenviada devolve o run existente (200 `{ idempotent: true, runId }`) sem nova chamada paga e sem stream"
    - "A execução confirmada responde `application/x-ndjson` e o evento final informa o `runId`"
    - "`POST /experiments/[id]/evaluations` valida que os runs comparados pertencem ao mesmo experimento e à mesma versão de cenário e aos papéis `baseline`/`candidate`; divergência → 400 e nenhuma avaliação persistida"
    - "A avaliação registrada leva `evaluator_id`, timestamp, `baseline_run_id`, `candidate_run_id` e `blind_order` quando aplicável"
  artifacts:
    - path: "src/lib/admin/schemas.ts"
      provides: "schemas Zod do laboratório (criação prompt-only, execução com confirmed/operationId, avaliação com runs comparados)"
      exports:
        - LabExperimentCreateRequestSchema
        - LabRunExecuteRequestSchema
        - LabEvaluationRequestSchema
    - path: "src/lib/ai/lab-cost-estimate.ts"
      provides: "estimativa de custo de 1 geração de campaign_image via resolveAiCost em modo leitura"
      exports:
        - estimateLabCampaignImageCost
    - path: "src/lib/lab/api/experiment-queries.ts"
      provides: "leitura de cenários, experimentos, detalhe, run detail e avaliações pendentes"
      exports:
        - listScenarioVersions
        - getActiveCampaignImageTarget
        - listRecentExperiments
        - listPendingEvaluations
        - getExperimentDetail
        - getRunDetail
    - path: "src/lib/lab/api/estimate.ts"
      provides: "estimativa do plano do experimento por componente com cobertura"
      exports:
        - estimateExperimentPlan
    - path: "src/lib/lab/api/evaluation-service.ts"
      provides: "registro validado de avaliação humana (mesmo experimento/cenário e papéis corretos)"
      exports:
        - createEvaluation
        - InvalidComparisonRunsError
    - path: "src/lib/lab/api/run-execution.ts"
      provides: "wiring de prepare/execute do run para a rota NDJSON"
      exports:
        - prepareExperimentRun
        - runPreparedExperimentRun
    - path: "src/app/api/admin/laboratorio/experiments/[id]/runs/route.ts"
      provides: "POST de execução de 1 run com NDJSON e confirmação explícita"
      contains: "application/x-ndjson"
  key_links:
    - from: "src/app/api/admin/laboratorio/**/route.ts"
      to: "src/lib/lab/environment-guard.ts"
      via: "assertLabEnvironment() antes de qualquer acesso a lab_*/storage/provider, com 403 environment_blocked"
      pattern: "assertLabEnvironment"
    - from: "src/app/api/admin/laboratorio/experiments/[id]/runs/route.ts"
      to: "src/lib/lab/api/run-execution.ts"
      via: "prepareExperimentRun (reserva antes do stream) + runPreparedExperimentRun (NDJSON)"
      pattern: "prepareExperimentRun"
    - from: "src/lib/lab/api/experiment-queries.ts"
      to: "src/lib/lab/run-service.ts"
      via: "reconcileStaleRuns na leitura do detalhe, cobrindo runs pending/running órfãos"
      pattern: "reconcileStaleRuns"
    - from: "src/lib/lab/api/evaluation-service.ts"
      to: "public.lab_human_evaluations"
      via: "insert append-only com runs comparados validados"
      pattern: "lab_human_evaluations"
---

<objective>
Entregar a superfície de API administrativa do laboratório sob `/api/admin/laboratorio`: schemas Zod, camada de leitura/estimativa/avaliação, wiring de execução e as 7 rotas (scenarios, experiments GET/POST, experiments/[id], experiments/[id]/estimate, runs/[id], experiments/[id]/runs NDJSON, experiments/[id]/evaluations), todas com `apiHandler` + `requireAdmin()` + `assertLabEnvironment()`. **Escopo:** a API expõe **criação (POST) e leitura (GET)** de experimentos; **não** há endpoint de atualização/arquivamento na F48.1 (alterar a configuração exige criar outro experimento; arquivamento → F48.2).

Purpose: expor o laboratório de forma protegida e com erro mapeado (D11/D14), garantindo que nenhuma chamada paga aconteça sem confirmação explícita, sem reserva atômica e sem ambiente permitido. Os 7 sub-itens da seção 8 do `tasks.md` são cobertos em 4 tasks agrupadas por afinidade (schemas+estimativa, leitura, rotas de detalhe, mutações).
Output: 7 arquivos de rota, 5 módulos de serviço e testes de rota verdes, consumidos por 48-1-09/48-1-10 (UI).
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48.1-laboratorio-ia-minimo/48.1-CONTEXT.md
@openspec/changes/fase-48-1-laboratorio-ia-minimo/design.md
@openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-admin-api/spec.md
@openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-human-evaluation/spec.md
@src/lib/admin/schemas.ts
@src/app/api/admin/feature-flags/route.ts
@src/app/api/admin/feature-flags/__tests__/route.test.ts
@src/lib/auth/api-handler.ts
@src/lib/admin/require-admin.ts
@src/lib/api-error-response.ts
@src/lib/supabase/server.ts

<interfaces>
Contratos extraídos do código e dos planos anteriores (usar diretamente — não explorar o repositório):

- `src/lib/auth/api-handler.ts`: `apiHandler(handler)` envolve o handler e converte `ForbiddenError` → 403, `UnauthorizedError` → 401, `StoreNotFoundError` → 404; qualquer outra exceção **propaga** (portanto erros previstos devem ser tratados dentro do handler).
- `src/lib/admin/require-admin.ts`: `requireAdmin(): Promise<{ userId: string }>` — lança `ForbiddenError` para não-admin.
- `src/lib/lab/environment-guard.ts` (48-1-02): `assertLabEnvironment()` (lança `LabEnvironmentError` com `.reason`), `LabEnvironmentError`, `labEnvironmentDeniedBody(reason)` → `{ error: "environment_blocked", reason }`.
- `src/lib/lab/limits.ts` (48-1-02): `MAX_SCENARIOS_PER_EXPERIMENT`, `MAX_REPETITIONS`, `MAX_RUNS_PER_EXPERIMENT`, `DEFAULT_MAX_RUNS_PER_EXPERIMENT`, `MAX_CONCURRENT_LAB_RUNS`.
- `src/lib/lab/domain/schemas.ts` (48-1-04): `CreateLabExperimentInputSchema`, `CreateLabEvaluationInputSchema`, `parseCreateLabExperimentInput`, `parseCreateLabEvaluationInput`, `UnsupportedChangedDimensionError` (`code: "unsupported_changed_dimension"`).
- `src/lib/lab/domain/experiment-service.ts` (48-1-04): `createExperiment(input, { actorId, client })` → `{ experimentId }`, `computeExperimentReadiness(experimentId, client)` → `{ ready, reasons }`, `transitionExperiment(experimentId, to, { actorId, client })`, `EXPERIMENT_TRANSITIONS`.
- `src/lib/lab/domain/model-target.ts` (48-1-04): `ModelTargetNotInCatalogError` (`code: "model_target_not_in_catalog"`).
- `src/lib/lab/domain/cost-coverage.ts` (48-1-05): `deriveCostCoverage(cost: CostResolution): "complete" | "partial" | "missing"`.
- `src/lib/lab/run-service.ts` (48-1-07): `prepareLabRun(...)` → `{ runId, snapshot, idempotent }`; `runReservedLabRun(...)` → `{ runId, status }`; `reconcileStaleRuns({ client })` → `{ reconciled }`; `LabReservationError` com `.code` (11 códigos); `LabRunEvent`.
- `src/lib/lab/scenarios/service.ts` (48-1-03): `loadScenarioFixture(slug)` → `{ content, contentHash, fixturePath, imagesDataUrls, logoDataUrl }`; `listScenarioFixtures()`.
- `src/lib/lab/scenarios/mapper.ts` (48-1-03): `mapScenarioToCampaignBrief(content, imagesDataUrls)`, `mapScenarioToResolvedContext(content, imagesDataUrls, logoDataUrl)`.
- `src/lib/lab/gateway/runtime.ts` (48-1-05): `createDefaultLabRuntime({ fixedTarget })` (dynamic import de `@/lib/ai`), `createLabGateway`, `createLabTelemetryContext`.
- `src/lib/lab/gateway/lab-prompt-loader.ts` (48-1-05): `new LabPromptLoader([{ name, content }])`.
- `src/lib/lab/gateway/noop-image-provider.ts` (48-1-05): `createNoopImageProvider()`.
- `src/lib/lab/persistence/artifact-service.ts` (48-1-06): `createArtifactSignedUrls({ client, storagePaths })`, `listRunArtifacts({ client, runId })`.
- `src/lib/ai/lab-telemetry-sink.ts` (48-1-05): `new LabTelemetrySink()` com `entries`/`costSummary`.
- `src/lib/ai-cost/cost-estimator.ts:149`: `resolveAiCost({ provider, model, usage?, providerReportedCostUsd?, manualCostUsd?, imageGenerationTool?, generationType? }): Promise<CostResolution>`. **Restrito por gate de arquitetura a `src/lib/ai/**`** — por isso a estimativa usa um helper novo nesse diretório.
- `src/lib/ai-cost/types.ts`: `CostResolution { estimatedCostUsd: number | null; providerReportedCostUsd?; costSource; pricingVersion?; costFormulaVersion?; costEstimationNote?; textComponentUsd?; imageToolComponentUsd?; imageToolPricingProvider?; imageToolPricingModel?; imageToolPricingVersion? }`.
- `src/lib/admin/schemas.ts`: convenção do repositório — Zod `.strict()`, `operationId: z.string().uuid()`, `reason` obrigatório, mensagens em PT-BR; `AI_MODEL_CAPABILITIES` e `AiModelTargetSchema` já existem no arquivo.
- Tabelas do laboratório (48-1-01): `lab_scenarios`, `lab_scenario_versions`, `lab_experiments`, `lab_experiment_variants`, `lab_experiment_scenarios`, `lab_runs`, `lab_artifacts`, `lab_human_evaluations`.
- `supabaseAdmin` em `src/lib/supabase/server.ts` (service_role).
- **Convenção de testes de rota:** `src/app/api/admin/<feature>/__tests__/route.test.ts` com `vi.mock("server-only", () => ({}))`, mocks de `@/lib/admin/require-admin` e `@/lib/supabase/server`, e `await import("../.../route")` dentro do teste (precedente `src/app/api/admin/feature-flags/__tests__/route.test.ts` e `src/app/api/admin/__tests__/credits-grant.test.ts`).
- Streaming NDJSON (precedente `src/app/api/campaign/generate-image/route.ts:525-536` e `:898`): `new ReadableStream({ async start(controller) { controller.enqueue(encoder.encode(JSON.stringify(event) + "\n")) } })` com `headers: { "Content-Type": "application/x-ndjson" }`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schemas Zod do laboratório + helper de estimativa de custo + serviço de estimativa do plano</name>
  <files>src/lib/admin/schemas.ts, src/lib/ai/lab-cost-estimate.ts, src/lib/ai/__tests__/lab-cost-estimate.test.ts, src/lib/lab/api/estimate.ts, src/lib/lab/api/__tests__/estimate.test.ts</files>
  <read_first>
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/design.md` — "D11 — API administrativa" (tabela de rotas, confirmação, erros) e "Decisões consolidadas" (item 5: pricing incompleto exige aviso, nunca valor exato)
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-admin-api/spec.md` — requirement "Estimativa de custo antes da execução"
    - `.planning/phases/48.1-laboratorio-ia-minimo/48.1-CONTEXT.md` — D11, D14 e DV-1/DV-2
    - `src/lib/admin/schemas.ts` — convenção `.strict()`, `operationId` UUID e o bloco final do arquivo (onde anexar os schemas novos)
    - `src/lib/lab/domain/schemas.ts` (48-1-04) — `CreateLabExperimentInputSchema` e `CreateLabEvaluationInputSchema` (reutilizar, não duplicar)
    - `src/lib/lab/domain/cost-coverage.ts` (48-1-05) — `deriveCostCoverage`
    - `src/lib/ai-cost/cost-estimator.ts` — assinatura de `resolveAiCost` (:149) e a regra do componente de imagem (:187-199)
    - `src/lib/ai-cost/types.ts` — `CostResolution` real (sem `imageUnitUsd`/`costPartial`)
    - `src/lib/ai/__tests__/telemetry-sink.test.ts` — padrão de mock de `@/lib/ai-cost/cost-estimator`
  </read_first>
  <action>
**A) `src/lib/admin/schemas.ts`** (task 8.1) — anexar ao final, sem alterar nenhum schema existente:

- `LabExperimentCreateRequestSchema` — reexportar `CreateLabExperimentInputSchema` importado de `@/lib/lab/domain/schemas` (o schema puro já cobre nome/objetivo/hipótese, `changedDimension` prompt-only, `modelTarget`, `params` com `skipInputValidation: true`, `repetitions`, `maxRuns`, `scenarioVersionIds` com limites, `baseline`/`candidate`). Exportar também `type LabExperimentCreateRequest = z.infer<typeof LabExperimentCreateRequestSchema>`.
- `LabRunExecuteRequestSchema` — `.strict()` com `variantId: z.string().uuid()`, `scenarioVersionId: z.string().uuid()`, `repetitionIndex: z.number().int().min(1).max(MAX_REPETITIONS)`, `supersedesRunId: z.string().uuid().nullable().optional()`, `confirmed: z.literal(true)`, `operationId: z.string().uuid()`. Exportar `type LabRunExecuteRequest`.
- `LabEvaluationRequestSchema` — `.strict()` com `scenarioVersionId: z.string().uuid()`, `baselineRunId: z.string().uuid()`, `candidateRunId: z.string().uuid()`, `verdict: z.enum(["baseline","candidate","tie","none"])`, `blindOrder: z.enum(["baseline_left","candidate_left"]).optional()`, `observation: z.string().max(4000).optional()` e `superRefine` rejeitando `baselineRunId === candidateRunId` (mensagem `run_ids_must_differ`). Exportar `type LabEvaluationRequest`.
- Importar `MAX_REPETITIONS` de `@/lib/lab/limits` (constantes puras, sem `server-only`).

**B) `src/lib/ai/lab-cost-estimate.ts`** — helper **obrigatoriamente** neste diretório (o gate de arquitetura restringe `resolveAiCost` a `src/lib/ai/**`):

`estimateLabCampaignImageCost(params: { provider: "openai" | "gemini"; model: string }): Promise<CostResolution>` — chama `resolveAiCost({ provider, model, imageGenerationTool: true, generationType: "campaign_image" })` **sem** `usage` (estimativa, não consumo) e retorna a `CostResolution` integral. Nenhum `AiCostTracker`, nenhum `generation_events`, nenhum secret.

**C) `src/lib/lab/api/estimate.ts`** (task 8.4) com `import "server-only";`:

`estimateExperimentPlan(params: { client; experimentId: string }): Promise<{ perRun: CostResolution | null; perRunCoverage: LabCostCoverage; plannedRuns: number; remainingRuns: number; totalEstimatedUsd: number | null; coverage: LabCostCoverage }>`:
1. Ler `lab_experiments` (`model_target`, `repetitions`, `max_runs`) — ausente → `Error("experiment_not_found")`.
2. Contar `lab_runs` do experimento e contar `lab_experiment_scenarios` → `plannedRuns = repetitions * scenarioCount`, `remainingRuns = max_runs - used` (nunca negativo).
3. `perRun = await estimateLabCampaignImageCost({ provider: model_target.provider, model: model_target.model })`.
4. `perRunCoverage = deriveCostCoverage(perRun)`; `coverage` = `"missing"` quando `perRunCoverage === "missing"`; `"partial"` quando `perRunCoverage === "partial"`; caso contrário `"complete"`.
5. `totalEstimatedUsd = perRun.estimatedCostUsd === null ? null : perRun.estimatedCostUsd * plannedRuns` (nunca apresentar valor exato quando a cobertura for `missing`; a UI mostra faixa/aviso).
6. **Não** bloquear por pricing incompleto: a função nunca lança por cobertura `partial`/`missing`.

**D) Testes.**
`src/lib/ai/__tests__/lab-cost-estimate.test.ts` (mock de `@/lib/ai-cost/cost-estimator`): `estimateLabCampaignImageCost` chama `resolveAiCost` com `imageGenerationTool: true`, `generationType: "campaign_image"` e **sem** `usage`; devolve a `CostResolution` do mock sem alterar campos; `AiCostTracker.record` nunca é chamado.
`src/lib/lab/api/__tests__/estimate.test.ts` (client fake em memória): 3 cenários × 2 repetições → `plannedRuns === 6`; `remainingRuns === max_runs - used`; cobertura `complete` com `pricing_table` + componentes presentes; `partial` com `costEstimationNote`; `missing` com `estimatedCostUsd: null` e `totalEstimatedUsd === null`; experimento inexistente → `experiment_not_found`; nenhuma exceção em cobertura parcial/indisponível.
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai/__tests__/lab-cost-estimate.test.ts src/lib/lab/api/__tests__/estimate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/admin/schemas.ts` exporta `LabExperimentCreateRequestSchema`, `LabRunExecuteRequestSchema` e `LabEvaluationRequestSchema` (os schemas pré-existentes permanecem inalterados: `AiModelSelectionUpdateSchema`, `AiModelSelectionResetSchema`, `AiOperationRunsQuerySchema` continuam exportados).
    - `Select-String -Path src/lib/admin/schemas.ts -Pattern 'confirmed: z.literal\(true\)'` encontra exatamente 1 ocorrência.
    - `Select-String -Path src/lib/admin/schemas.ts -Pattern 'run_ids_must_differ'` encontra exatamente 1 ocorrência.
    - `src/lib/ai/lab-cost-estimate.ts` existe e exporta `estimateLabCampaignImageCost`; `Select-String` do arquivo para `AiCostTracker|generation_events` retorna 0 ocorrências.
    - `src/lib/lab/api/estimate.ts` exporta `estimateExperimentPlan` e nunca usa `imageUnitUsd`/`costPartial`.
    - `npx vitest run src/lib/ai/__tests__/lab-cost-estimate.test.ts src/lib/lab/api/__tests__/estimate.test.ts` passa com exit code 0.
    - `npx vitest run src/lib/admin src/lib/ai` permanece verde (schemas existentes e gate de arquitetura intactos).
    - `npx tsc -p tsconfig.typecheck.json --noEmit` continua verde.
  </acceptance_criteria>
  <done>Schemas Zod do laboratório adicionados sem tocar nos existentes; estimativa de 1 geração via `resolveAiCost` em leitura dentro de `src/lib/ai/**`; estimativa do plano por componente com cobertura `complete|partial|missing` e sem bloquear por pricing incompleto.</done>
</task>

<task type="auto">
  <name>Task 2: Camada de leitura (cenários, experimentos, detalhe, run detail, pendências) + serviço de avaliação + rotas scenarios/experiments</name>
  <files>src/lib/lab/api/experiment-queries.ts, src/lib/lab/api/__tests__/experiment-queries.test.ts, src/lib/lab/api/evaluation-service.ts, src/lib/lab/api/__tests__/evaluation-service.test.ts, src/app/api/admin/laboratorio/scenarios/route.ts, src/app/api/admin/laboratorio/experiments/route.ts, src/app/api/admin/laboratorio/__tests__/route.test.ts</files>
  <read_first>
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-admin-api/spec.md` — requirements "Superfície de API administrativa protegida", "Criação e leitura de experimentos" e "Detalhe do run e registro de avaliação"
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-human-evaluation/spec.md` — requirement "Avaliação append-only e validada" (papéis e mesma combinação)
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/design.md` — "D11" (tabela de rotas) e "D13"
    - `src/app/api/admin/feature-flags/route.ts` — padrão de rota admin (imports, `apiHandler`, `requireAdmin`, tratamento de erro com `NextResponse.json`)
    - `src/app/api/admin/feature-flags/__tests__/route.test.ts` — padrão de teste de rota admin
    - `src/lib/lab/run-service.ts` (48-1-07) — `reconcileStaleRuns`
    - `src/lib/lab/persistence/artifact-service.ts` (48-1-06) — `listRunArtifacts`, `createArtifactSignedUrls`
    - `src/lib/lab/domain/experiment-service.ts` (48-1-04) — `createExperiment`, `computeExperimentReadiness`, `transitionExperiment`
    - `src/lib/lab/domain/model-target.ts` (48-1-04) — `ModelTargetNotInCatalogError`
    - `src/lib/lab/domain/schemas.ts` (48-1-04) — `parseCreateLabExperimentInput`, `UnsupportedChangedDimensionError`
  </read_first>
  <action>
**A) `src/lib/lab/api/experiment-queries.ts`** (tasks 8.2/8.3) com `import "server-only";` — todas as funções recebem o client por parâmetro:

- `listScenarioVersions(client)` → `{ id, scenarioId, slug, name, version, contentHash, intent, format, locale, createdAt }[]` — join `lab_scenario_versions` × `lab_scenarios` (somente leitura), ordenado por `slug` + `version desc`. **Não** devolver `content` completo (nem base64 de imagens); extrair apenas `intent`/`format`/`locale` de `content`.
- `getActiveCampaignImageTarget(client)` → `{ provider, model, protocol } | null` — `select` em `ai_model_catalog` filtrando `capability = "campaign_image"` e `status = "active"` (somente leitura; nunca escreve no catálogo nem toca `ai_model_selection`).
- `listRecentExperiments(client, limit = 20)` → `{ id, name, status, repetitions, maxRuns, runsUsed, remainingRuns, scenarioCount, updatedAt }[]` — ordenado por `updated_at desc`.
- `listPendingEvaluations(client)` → `{ experimentId, experimentName, scenarioVersionId, scenarioLabel, baselineRunId, candidateRunId }[]` — para experimentos com status `running`/`evaluated`: por cenário vinculado, considerar o run `succeeded` mais recente de cada variante (`baseline` e `candidate`); quando existe o par e **não** existe avaliação com exatamente esse par de runs, o item é uma pendência.
- `getExperimentDetail(client, experimentId)` → `{ experiment, variants, scenarios, runs, evaluations, budget: { maxRuns, used, remaining } } | null`:
  1. `await reconcileStaleRuns({ client })` **antes** de ler (reconciliação preguiçosa dos runs órfãos `pending`/`running` — única escrita do módulo, documentada em comentário).
  2. Ler experimento (ausente → `null`), variantes, cenários vinculados (com slug/nome/versão), runs (ordenados por `created_at desc`, com `status`, `latency_ms`, `estimated_cost_usd`, `cost_detail`, `error_type`, `error_message`, `technical_validation`, `variant_id`, `scenario_version_id`, `repetition_index`, `run_sequence`), avaliações (`verdict`, `observation`, `evaluator_id`, `created_at`, `baseline_run_id`, `candidate_run_id`, `blind_order`) e calcular `budget` a partir de `max_runs` × runs existentes.
- `getRunDetail(client, runId)` → `{ run, snapshot, artifacts } | null` — `run` com as colunas de resultado, `snapshot` (objeto congelado), e `artifacts` de `listRunArtifacts` enriquecidos com `signedUrl` via `createArtifactSignedUrls({ client, storagePaths })` (artefatos sem URL ficam com `signedUrl: null`).

**B) `src/lib/lab/api/evaluation-service.ts`** (task 8.6) com `import "server-only";`:

- `class InvalidComparisonRunsError extends Error` com `readonly code: "invalid_comparison_runs" | "runs_not_terminal"`.
- `createEvaluation(params: { client; experimentId: string; evaluatorId: string; input: LabEvaluationRequest }): Promise<{ evaluationId: string; createdAt: string }>`:
  1. `parseCreateLabEvaluationInput(input)` (48-1-04) para validar forma e runs distintos.
  2. Ler os dois runs (`baselineRunId`, `candidateRunId`) com `id, experiment_id, variant_id, scenario_version_id, status`. Faltando qualquer um → `InvalidComparisonRunsError("invalid_comparison_runs")`.
  3. Rejeitar quando: `run.experiment_id !== experimentId` para qualquer um dos dois; `run.scenario_version_id !== input.scenarioVersionId`; os dois runs não têm o mesmo `scenario_version_id`. → `invalid_comparison_runs`.
  4. Rejeitar quando qualquer run não estiver em estado terminal (`pending`/`running`) → `runs_not_terminal`.
  5. Ler as variantes dos dois runs e exigir `baseline` para o `baselineRunId` e `candidate` para o `candidateRunId` → senão `invalid_comparison_runs` (papéis trocados).
  6. `insert` em `lab_human_evaluations` com `experiment_id`, `scenario_version_id`, `baseline_run_id`, `candidate_run_id`, `blind_order ?? null`, `verdict`, `observation ?? null`, `evaluator_id`.
  7. Se o experimento estiver em `running`, `transitionExperiment(experimentId, "evaluated", { actorId: evaluatorId, client })` (avaliar não encerra execuções; `evaluated → running` volta a ocorrer em novo run). Falha de transição não desfaz a avaliação já persistida (append-only).
  8. Retorna `{ evaluationId, createdAt }`.
  - O módulo **nunca** executa `update`/`delete` em `lab_human_evaluations` (append-only garantido também por trigger de banco).

**C) Rotas.**

`src/app/api/admin/laboratorio/scenarios/route.ts` — `export const GET = apiHandler(async () => {...})`: `await requireAdmin()`; `try { assertLabEnvironment(); } catch (error) { if (error instanceof LabEnvironmentError) return NextResponse.json(labEnvironmentDeniedBody(error.reason), { status: 403 }); throw error; }`; retornar `NextResponse.json({ scenarios: await listScenarioVersions(supabaseAdmin) })`. Falha de leitura → 503 `{ error: "Falha ao ler os cenários" }`.

`src/app/api/admin/laboratorio/experiments/route.ts` — `GET`: mesma guarda; `{ experiments: await listRecentExperiments(supabaseAdmin) }`. `POST`: mesma guarda; `parseCreateLabExperimentInput(await request.json())` em `try/catch` → `UnsupportedChangedDimensionError` → 400 `{ error: "unsupported_changed_dimension" }`; `ZodError`/JSON inválido → 400 `{ error: "invalid_payload", details }`; `ModelTargetNotInCatalogError` → 400 `{ error: "model_target_not_in_catalog" }`; `await createExperiment(input, { actorId: admin.userId, client: supabaseAdmin })`; `const readiness = await computeExperimentReadiness(experimentId, supabaseAdmin)`; quando `readiness.ready` → `transitionExperiment(experimentId, "ready", ...)` e `status = "ready"`, senão `status = "draft"`; responder **201** `{ experimentId, status, readiness }`.

**D) Testes.**

`src/lib/lab/api/__tests__/experiment-queries.test.ts` (client fake em memória): `listScenarioVersions` não devolve `content` nem base64; `getActiveCampaignImageTarget` retorna o alvo ativo e `null` quando não há linha ativa; `listRecentExperiments` calcula `remainingRuns`; `getExperimentDetail` chama `reconcileStaleRuns` (spy) antes das leituras e devolve `budget.remaining`; experimento ausente → `null`; `getRunDetail` devolve `signedUrl` para artefatos não removidos e `null` quando a assinatura falha; nenhuma escrita além da reconciliação (assertar que não houve `insert`/`delete`).
`src/lib/lab/api/__tests__/evaluation-service.test.ts` (client fake): avaliação válida é inserida com `evaluator_id`/`baseline_run_id`/`candidate_run_id`/`blind_order`; runs de experimentos diferentes → `invalid_comparison_runs` e nenhum insert; runs de cenários diferentes → erro; papéis trocados (baseline apontando para a variante candidata) → erro; run ainda `running` → `runs_not_terminal`; `baselineRunId === candidateRunId` rejeitado pelo schema; experimento `running` transita para `evaluated`; nenhum `update`/`delete` em `lab_human_evaluations`.
`src/app/api/admin/laboratorio/__tests__/route.test.ts` (mocks de `server-only`, `@/lib/admin/require-admin`, `@/lib/lab/environment-guard`, `@/lib/supabase/server`, `@/lib/lab/api/experiment-queries`, `@/lib/lab/domain/experiment-service`): `GET /scenarios` não-admin → 403 (`ForbiddenError` via `apiHandler`); ambiente bloqueado → 403 `{ error: "environment_blocked", reason }` e `listScenarioVersions` **não** chamado; `GET /scenarios` ok → 200 com lista; `GET /experiments` → 200; `POST /experiments` payload inválido → 400 e nenhum insert; `POST /experiments` com `changedDimension: "model"` → 400 `unsupported_changed_dimension`; alvo fora do catálogo → 400 `model_target_not_in_catalog`; criação válida pronta → 201 com `status: "ready"` e `experimentId`; criação válida não pronta → 201 com `status: "draft"` e `readiness.reasons`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/lab/api/__tests__/experiment-queries.test.ts src/lib/lab/api/__tests__/evaluation-service.test.ts "src/app/api/admin/laboratorio/__tests__/route.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/lab/api/experiment-queries.ts` exporta `listScenarioVersions`, `getActiveCampaignImageTarget`, `listRecentExperiments`, `listPendingEvaluations`, `getExperimentDetail`, `getRunDetail`.
    - `Select-String -Path src/lib/lab/api/experiment-queries.ts -Pattern "reconcileStaleRuns"` encontra a chamada real no `getExperimentDetail` (**≥1 ocorrência** — import nomeado + chamada).
    - `Select-String -Path src/lib/lab/api/experiment-queries.ts -Pattern "ai_model_selection"` retorna 0 ocorrências.
    - `src/lib/lab/api/evaluation-service.ts` exporta `createEvaluation` e `InvalidComparisonRunsError`; `Select-String` do arquivo para `\.delete\(` retorna 0 ocorrências.
    - `src/app/api/admin/laboratorio/scenarios/route.ts` e `.../experiments/route.ts` existem, usam `apiHandler` + `requireAdmin` + `assertLabEnvironment` e contêm `labEnvironmentDeniedBody`.
    - `Select-String -Path src/app/api/admin/laboratorio/experiments/route.ts -Pattern "201"` confirma o status de criação.
    - `npx vitest run src/lib/lab/api/__tests__/experiment-queries.test.ts src/lib/lab/api/__tests__/evaluation-service.test.ts "src/app/api/admin/laboratorio/__tests__/route.test.ts"` passa com exit code 0.
    - `npx tsc -p tsconfig.typecheck.json --noEmit` continua verde.
  </acceptance_criteria>
  <done>Camada de leitura com reconciliação preguiçosa na leitura do detalhe, serviço de avaliação validando mesmo experimento/cenário e papéis corretos, e rotas de cenários e experimentos protegidas por admin + guarda de ambiente com erros mapeados.</done>
</task>

<task type="auto">
  <name>Task 3: Rotas de detalhe do experimento, estimativa e detalhe do run</name>
  <files>src/app/api/admin/laboratorio/experiments/[id]/route.ts, src/app/api/admin/laboratorio/experiments/[id]/estimate/route.ts, src/app/api/admin/laboratorio/runs/[id]/route.ts, src/app/api/admin/laboratorio/experiments/[id]/__tests__/route.test.ts, src/app/api/admin/laboratorio/runs/[id]/__tests__/route.test.ts</files>
  <read_first>
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-admin-api/spec.md` — requirements "Criação e leitura de experimentos", "Estimativa de custo antes da execução" e "Detalhe do run e registro de avaliação"
    - `src/app/api/admin/feature-flags/route.ts` — padrão de rota admin
    - `src/app/api/admin/ai-operation-runs/[operationRunId]/__tests__/route.test.ts` — padrão de teste de rota com parâmetro dinâmico (`params`)
    - `src/lib/lab/api/experiment-queries.ts` e `src/lib/lab/api/estimate.ts` (Tasks 1 e 2) — contratos a consumir
    - `src/lib/lab/environment-guard.ts` (48-1-02) — `LabEnvironmentError` e `labEnvironmentDeniedBody`
  </read_first>
  <action>
Criar as três rotas de leitura, todas com `apiHandler` + `requireAdmin()` + `assertLabEnvironment()` (403 com `labEnvironmentDeniedBody(reason)`) antes de qualquer leitura:

- `src/app/api/admin/laboratorio/experiments/[id]/route.ts` — `GET(request, { params }: { params: Promise<{ id: string }> })` (Next.js App Router: `await params` para obter `id`): `getExperimentDetail(supabaseAdmin, id)`; `null` → 404 `{ error: "experiment_not_found" }`; sucesso → 200 `{ experiment, variants, scenarios, runs, evaluations, budget }`.
- `src/app/api/admin/laboratorio/experiments/[id]/estimate/route.ts` — `GET`: `estimateExperimentPlan({ client: supabaseAdmin, experimentId: id })`; `Error("experiment_not_found")` → 404; sucesso → 200 `{ perRun, perRunCoverage, plannedRuns, remainingRuns, totalEstimatedUsd, coverage }`. Nunca devolver 4xx por cobertura `partial`/`missing` (a UI exibe faixa/aviso).
- `src/app/api/admin/laboratorio/runs/[id]/route.ts` — `GET`: `getRunDetail(supabaseAdmin, id)`; `null` → 404 `{ error: "run_not_found" }`; sucesso → 200 `{ run, snapshot, artifacts }` com `signedUrl` por artefato (3600s). Não devolver `lab_artifacts.removed_at` não nulos (já filtrados na camada de leitura).

Testes (mocks de `server-only`, `require-admin`, `environment-guard`, `@/lib/supabase/server` e dos serviços de `@/lib/lab/api/*`; rotas importadas dinamicamente dentro de cada teste):
- `experiments/[id]/__tests__/route.test.ts`: não-admin → 403; ambiente bloqueado → 403 `{ error: "environment_blocked", reason }` com `getExperimentDetail` não chamado; detalhe ok → 200 com `budget.remaining`; experimento ausente → 404; estimativa ok → 200 com `coverage: "partial"` e `totalEstimatedUsd: null` (não bloqueia); estimativa de experimento ausente → 404.
- `runs/[id]/__tests__/route.test.ts`: não-admin → 403; ambiente bloqueado → 403; run ok → 200 com `snapshot` e `artifacts[0].signedUrl`; run ausente → 404; nenhuma rota retorna o `operation_id` de outro experimento nem chaves de API (assertar que o payload não contém `sk-`/`AIza`).
  </action>
  <verify>
    <automated>npx vitest run "src/app/api/admin/laboratorio/experiments/[id]/__tests__/route.test.ts" "src/app/api/admin/laboratorio/runs/[id]/__tests__/route.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - As 3 rotas existem nos paths exatos exigidos por D11.
    - `Select-String -Path` das 3 rotas para `assertLabEnvironment` encontra **≥1 chamada por arquivo** (com import nomeado, a string aparece no import e na chamada).
    - `Select-String -Path src/app/api/admin/laboratorio/runs/[id]/route.ts -Pattern "signedUrl|artifacts"` confirma o retorno das URLs assinadas.
    - `Select-String -Path src/app/api/admin/laboratorio/experiments/[id]/estimate/route.ts -Pattern "coverage|totalEstimatedUsd"` confirma o payload da estimativa.
    - `npx vitest run "src/app/api/admin/laboratorio/experiments/[id]/__tests__/route.test.ts" "src/app/api/admin/laboratorio/runs/[id]/__tests__/route.test.ts"` passa com exit code 0.
    - `npx tsc -p tsconfig.typecheck.json --noEmit` continua verde.
  </acceptance_criteria>
  <done>Detalhe do experimento com budget restante, estimativa por componente com cobertura sem bloquear execução e detalhe do run com snapshot + URLs assinadas, todos protegidos por admin e pela guarda de ambiente.</done>
</task>

<task type="auto">
  <name>Task 4: Wiring de execução + rota NDJSON de run + rota de avaliação</name>
  <files>src/lib/lab/api/run-execution.ts, src/lib/lab/api/__tests__/run-execution.test.ts, src/app/api/admin/laboratorio/experiments/[id]/runs/route.ts, src/app/api/admin/laboratorio/experiments/[id]/runs/__tests__/route.test.ts, src/app/api/admin/laboratorio/experiments/[id]/evaluations/route.ts, src/app/api/admin/laboratorio/experiments/[id]/evaluations/__tests__/route.test.ts</files>
  <read_first>
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-admin-api/spec.md` — requirements "Execução de run com confirmação explícita" e "Detalhe do run e registro de avaliação"
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/design.md` — "D11" (streaming NDJSON, `confirmed: true` → 422, tabela de erros) e "D7" (single-shot, sem provider de imagem)
    - `src/app/api/campaign/generate-image/route.ts` — linhas 519-536 (abertura do stream e `emit`) e 890-900 (headers `application/x-ndjson`)
    - `src/lib/lab/run-service.ts` (48-1-07) — `prepareLabRun`, `runReservedLabRun`, `LabReservationError`
    - `src/lib/lab/api/evaluation-service.ts` (Task 2) — `createEvaluation`, `InvalidComparisonRunsError`
    - `src/lib/lab/gateway/runtime.ts`, `lab-prompt-loader.ts`, `noop-image-provider.ts` (48-1-05)
    - `src/lib/lab/scenarios/service.ts` e `mapper.ts` (48-1-03)
  </read_first>
  <action>
**A) `src/lib/lab/api/run-execution.ts`** — encapsula o wiring pesado para a rota ficar fina e testável:

- `prepareExperimentRun(params: { client; experimentId: string; actorId: string; input: LabRunExecuteRequest }): Promise<{ runId: string; snapshot: LabRunSnapshot; idempotent: boolean; executionContext: LabRunExecutionContext }>`:
  1. Ler o experimento (ausente → `Error("experiment_not_found")`) e validar que a variante pertence ao experimento e que o cenário está vinculado (defesa em profundidade; a RPC também valida).
  2. Ler a variante (`role`, `prompt_snapshot`) e a variante irmã (para `variants.baseline`/`variants.candidate`).
  3. Ler a versão de cenário (`lab_scenario_versions` + `lab_scenarios` slug) e `loadScenarioFixture(slug)` → `mapScenarioToCampaignBrief` + `mapScenarioToResolvedContext` (as imagens controladas viram data URLs server-side). Falha de fixture → `Error("unsupported_scenario_mode")`/`missing_scenario_image:<path>` propagada.
  4. Montar o `executionContext` (não persistido): `{ scenario: { imagesDataUrls, logoDataUrl, brief, context }, experiment: { params }, variant }`.
  5. `prepareLabRun({ client, experimentId, variantId, scenarioVersionId, repetitionIndex, supersedesRunId, operationId, actorId, scenario: { id, version, contentHash }, experiment: { modelTarget, params }, variant, variants })`. Erros de reserva propagam como `LabReservationError` **antes** de qualquer chamada paga.
- `runPreparedExperimentRun(params: { client; experimentId: string; runId: string; snapshot; actorId: string; executionContext; onEvent?: (event: LabRunEvent) => void }): Promise<{ runId: string; status: LabRunStatus }>`:
  1. `createDefaultLabRuntime({ fixedTarget: experiment.modelTarget })` (dynamic import de `@/lib/ai`).
  2. `promptLoader = new LabPromptLoader([{ name: variant.promptSnapshot.name, content: variant.promptSnapshot.content }])`.
  3. `imageService = new ImageGenerationService(createNoopImageProvider(), promptLoader)` — o provider nunca é invocado; o prompt é montado por `buildDirectorPrompt`.
  4. `sink = new LabTelemetrySink()`; `telemetry = createLabTelemetryContext({ sink, operationRunId: runId, traceId: runId, storeId: experimentId, userId: actorId })` (`operationRunType: "campaign_delivery"`).
  5. `runReservedLabRun({ client, experimentId, runId, snapshot, actorId, scenario, experiment, gateway, sink, promptLoader, imageService, onEvent })`.

**B) `src/app/api/admin/laboratorio/experiments/[id]/runs/route.ts`** — `POST`:
1. `await requireAdmin()`; guarda de ambiente (403 `labEnvironmentDeniedBody`).
2. Ler o corpo bruto com `request.json()` em `try/catch`; quando o corpo for objeto e `confirmed !== true` → **422** `{ error: "confirmation_required" }` **antes** de qualquer parse completo e sem chamada paga. Depois `LabRunExecuteRequestSchema.safeParse` → 400 `{ error: "invalid_payload", details }`.
3. `prepareExperimentRun(...)` em `try/catch` mapeando `LabReservationError.code`:
   - `budget_exceeded`, `run_already_active`, `idempotency_conflict`, `experiment_not_ready` → **409** `{ error: code }`;
   - `missing_snapshot`, `missing_operation_id`, `variant_not_in_experiment`, `scenario_not_in_experiment`, `repetition_out_of_range`, `invalid_supersedes_run` → **400** `{ error: code }`;
   - `experiment_not_found` → **404**;
   - `unsupported_scenario_mode` → **400**;
   - `lab_reservation_failed` → **500**.
4. Quando `idempotent === true` → **200** JSON `{ idempotent: true, runId }` **sem** abrir stream e sem nova chamada paga.
5. Caso contrário, abrir o stream: `new ReadableStream({ async start(controller) { const encoder = new TextEncoder(); const emit = (event) => { try { controller.enqueue(encoder.encode(JSON.stringify(event) + "\n")); } catch { /* stream fechado pelo cliente */ } }; try { const result = await runPreparedExperimentRun({ ..., onEvent: emit }); emit({ type: "done", runId: result.runId, status: result.status }); } catch (error) { emit({ type: "error", code: "run_failed", message: "Execução falhou" }); } finally { controller.close(); } } })` e responder com `headers: { "Content-Type": "application/x-ndjson" }`. O evento final informa o `runId`; a imagem vai para o bucket, **nunca** para o stream.

**C) `src/app/api/admin/laboratorio/experiments/[id]/evaluations/route.ts`** — `POST`: `requireAdmin`; guarda de ambiente; `LabEvaluationRequestSchema.safeParse` → 400 `{ error: "invalid_payload", details }`; `createEvaluation({ client: supabaseAdmin, experimentId: id, evaluatorId: admin.userId, input })`; `InvalidComparisonRunsError` → **400** `{ error: error.code }`; sucesso → **201** `{ evaluationId, createdAt }`.

**D) Testes.**
`src/lib/lab/api/__tests__/run-execution.test.ts` (client fake + mocks de `@/lib/lab/scenarios/service`, `mapper`, `gateway/runtime`, `run-service`): `prepareExperimentRun` mapeia o cenário para brief/contexto e chama `prepareLabRun` com `scenarioVersionId`/`repetitionIndex`/`operationId` exatos; erro de reserva propaga sem chamar `createDefaultLabRuntime`; `runPreparedExperimentRun` compõe `LabPromptLoader` com o snapshot da variante, usa `createNoopImageProvider` e chama `runReservedLabRun` com `operationRunType: "campaign_delivery"`; nenhuma chamada de rede.
`runs/__tests__/route.test.ts` (mocks de `require-admin`, `environment-guard`, `@/lib/supabase/server`, `@/lib/lab/api/run-execution`): não-admin → 403; ambiente bloqueado → 403; corpo sem `confirmed` → 422 `confirmation_required` com `prepareExperimentRun` **não** chamado; payload inválido → 400; `budget_exceeded` → 409; `run_already_active` → 409; `idempotency_conflict` → 409; `missing_snapshot` → 400; `experiment_not_ready` → 409; idempotente → 200 `{ idempotent: true, runId }` sem stream; execução confirmada → 200 com `Content-Type: application/x-ndjson` e o corpo contendo `"type":"done"` e o `runId`.
`evaluations/__tests__/route.test.ts`: não-admin → 403; ambiente bloqueado → 403; payload inválido → 400; `invalid_comparison_runs` → 400 e nenhum insert; `runs_not_terminal` → 400; sucesso → 201 `{ evaluationId }` com `evaluatorId` do admin.
  </action>
  <verify>
    <automated>npx vitest run src/lib/lab/api/__tests__/run-execution.test.ts "src/app/api/admin/laboratorio/experiments/[id]/runs/__tests__/route.test.ts" "src/app/api/admin/laboratorio/experiments/[id]/evaluations/__tests__/route.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/lab/api/run-execution.ts` exporta `prepareExperimentRun` e `runPreparedExperimentRun`.
    - `Select-String -Path src/lib/lab/api/run-execution.ts -Pattern "OpenAIImageProvider|providers/openai|campaign_image_edit|generation_events|AiCostTracker"` retorna 0 ocorrências.
    - `Select-String -Path src/app/api/admin/laboratorio/experiments/[id]/runs/route.ts -Pattern "confirmation_required"` encontra exatamente 1 ocorrência e ela precede a chamada de `prepareExperimentRun`.
    - `Select-String -Path src/app/api/admin/laboratorio/experiments/[id]/runs/route.ts -Pattern "application/x-ndjson"` encontra exatamente 1 ocorrência.
    - `Select-String -Path src/app/api/admin/laboratorio/experiments/[id]/runs/route.ts -Pattern "budget_exceeded|run_already_active|idempotency_conflict|experiment_not_ready|missing_snapshot|missing_operation_id|variant_not_in_experiment|scenario_not_in_experiment|repetition_out_of_range|invalid_supersedes_run|experiment_not_found|unsupported_scenario_mode"` encontra os 12 códigos mapeados.
    - `Select-String -Path src/app/api/admin/laboratorio/experiments/[id]/evaluations/route.ts -Pattern "createEvaluation|InvalidComparisonRunsError"` encontra ambos.
    - `npx vitest run src/lib/lab/api/__tests__/run-execution.test.ts "src/app/api/admin/laboratorio/experiments/[id]/runs/__tests__/route.test.ts" "src/app/api/admin/laboratorio/experiments/[id]/evaluations/__tests__/route.test.ts"` passa com exit code 0.
    - `npx tsc -p tsconfig.typecheck.json --noEmit` continua verde.
  </acceptance_criteria>
  <done>Rota de execução com confirmação explícita (422), reserva antes do stream, erros mapeados para 400/403/404/409, operação idempotente sem nova chamada paga, NDJSON com `runId` no evento final e rota de avaliação validando os runs comparados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador/admin → rotas `/api/admin/laboratorio` | entrada HTTP não confiável (payload, query, parâmetros dinâmicos) |
| variáveis de ambiente → guarda | decisão de existência da superfície e de permissão de execução |
| rota → RPC `lab_reserve_run` | fronteira que precede qualquer gasto |
| rota → gateway/provider | chamada paga real disparada por ação humana |
| rota → bucket `lab-artifacts` | URLs assinadas expostas ao cliente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-1-58 | Elevation of Privilege | todas as rotas do laboratório | mitigate | `requireAdmin()` como primeira instrução de cada handler; `ForbiddenError` convertida em 403 pelo `apiHandler`; teste de rota cobre não-admin em cada rota |
| T-48-1-59 | Elevation of Privilege / Spoofing | guarda de ambiente | mitigate | `assertLabEnvironment()` antes de qualquer acesso a `lab_*`/storage/provider; `LabEnvironmentError` → 403 `{ error: "environment_blocked", reason }`; teste prova que o serviço de leitura **não** é chamado quando o ambiente está bloqueado |
| T-48-1-60 | Denial of Service / financial | `POST /experiments/[id]/runs` | mitigate | Sem `confirmed: true` → 422 antes do parse completo e sem chamada paga; reserva atômica antes do stream; `budget_exceeded`/`run_already_active` → 409 sem gasto; um run por ação explícita (sem "executar tudo") |
| T-48-1-61 | Tampering / Repudiation | idempotência | mitigate | `operationId` UUID obrigatório no schema; operação repetida devolve 200 `{ idempotent: true, runId }` sem nova chamada paga; payload divergente → 409 `idempotency_conflict` |
| T-48-1-62 | Tampering | avaliação humana | mitigate | `createEvaluation` valida mesmo experimento, mesma versão de cenário e papéis `baseline`/`candidate`; runs não terminais recusados; `lab_human_evaluations` é append-only (trigger de banco + ausência de update/delete no código) |
| T-48-1-63 | Information Disclosure | URLs assinadas | mitigate | Assinatura sempre server-side com TTL de 3600s e bucket privado; artefatos removidos são filtrados; `getRunDetail` não devolve `removed_at` preenchidos |
| T-48-1-64 | Information Disclosure | payloads de erro e logs | mitigate | Erros devolvem código estável (`budget_exceeded`, `invalid_payload`, …) sem stack trace nem detalhe de provider; nenhum secret em resposta; nenhum `console.log` de payload |
| T-48-1-65 | Tampering | catálogo/seleção de modelos | mitigate | `getActiveCampaignImageTarget` faz apenas `select` em `ai_model_catalog`; `ai_model_selection` não é referenciada (asserção por grep); nenhuma env-var de modelo |
| T-48-1-66 | Denial of Service | `src/lib/ai/lab-cost-estimate.ts` fora do path do gate | mitigate | O helper vive em `src/lib/ai/**`, único lugar onde o gate de arquitetura permite `resolveAiCost`; nenhum uso de `AiCostTracker`/`generation_events` (asserção por grep) |
| T-48-1-67 | Elevation of Privilege | path traversal via parâmetros de rota | mitigate | `[id]` é UUID validado pelos schemas/consultas (`.eq("id", id)`); nenhum path é montado a partir de input do cliente na API (paths de artefato são derivados de IDs de banco em 48-1-06) |
| T-48-1-SC | Tampering | instalação de dependências | mitigate | Nenhum pacote novo: `zod`, `next`, client Supabase já existem. Instalação nova exigiria auditoria de legitimidade + checkpoint humano bloqueante |
</threat_model>

<verification>
- `npx vitest run src/lib/lab/api "src/app/api/admin/laboratorio" src/lib/ai/__tests__/lab-cost-estimate.test.ts` → exit code 0.
- `npx tsc -p tsconfig.typecheck.json --noEmit` → exit code 0.
- `npm run lint` → exit code 0.
- `Select-String -Path src/lib/lab/api -Pattern "generation_events|AiCostTracker|ai_model_selection|campaign-images"` → 0 ocorrências (varredura recursiva via `Get-ChildItem -Recurse | Select-String`).
- `Get-ChildItem -Recurse -Path "src/app/api/admin/laboratorio" -Filter route.ts` → 7 arquivos de rota exatamente.
- Todas as rotas contêm `requireAdmin` e `assertLabEnvironment` (**≥1 por handler**; `experiments/route.ts` expõe GET+POST e tem 2 de cada), com **imports nomeados** seguindo a convenção do repositório (`import { requireAdmin } from "@/lib/admin/require-admin"`).
- `git status --porcelain src/lib/ai/gateway.ts src/lib/ai-cost src/lib/campaign src/lib/admin/require-admin.ts prompts/` → vazio.
- Nenhuma chamada de rede nos testes (clientes e serviços mockados/fakes).
</verification>

<success_criteria>
- 7 rotas sob `/api/admin/laboratorio` protegidas por `requireAdmin()` + `assertLabEnvironment()`, com payload validado por Zod e erros mapeados.
- Criação de experimento prompt-only com autor registrado; `model`/`configuration` e alvo fora do catálogo recusados com código explícito.
- Detalhe com variantes, runs, avaliações e budget restante, reconciliando órfãos na leitura.
- Estimativa por componente com cobertura `complete|partial|missing` sem bloquear a execução.
- Execução exigindo `confirmed: true` (422), com reserva antes do stream, erros 400/403/404/409 e NDJSON com `runId` no evento final.
- Idempotência sem nova chamada paga e sem stream.
- Avaliação humana validada por experimento/cenário/papel e persistida append-only.
</success_criteria>

<output>
Create `.planning/phases/48.1-laboratorio-ia-minimo/48-1-08-SUMMARY.md` when done.
</output>

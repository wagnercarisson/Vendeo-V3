---
phase: 48.1
plan: 48-1-11
type: execute
wave: 7
depends_on:
  - 48-1-03
  - 48-1-04
  - 48-1-08
files_modified:
  - src/lib/lab/__tests__/lab-environment-guard.contract.test.ts
  - src/lib/lab/__tests__/lab-isolation.contract.test.ts
  - src/lib/lab/__tests__/lab-financial-safety.contract.test.ts
  - src/lib/lab/scenarios/__tests__/lab-scenarios.contract.test.ts
  - src/lib/lab/domain/__tests__/lab-experiments.contract.test.ts
autonomous: true
requirements:
  - lab-isolation
  - lab-scenarios
  - lab-experiments
must_haves:
  truths:
    - "Os 5 `reason` da guarda (`ok`, `disabled_flag`, `missing_url`, `non_local_supabase`, `remote_blocked`) são exercitados por uma matriz exaustiva, com fail-closed em configuração ausente/inválida/ambígua"
    - "Hosts de produção `*.supabase.co`/`.in`/`.com` permanecem bloqueados mesmo quando presentes em `VENDEO_LAB_ALLOWED_SUPABASE_HOSTS`"
    - "Um run executado com fakes acessa apenas tabelas `lab_*` e o bucket `lab-artifacts`; qualquer acesso a tabela/bucket produtivo faz o teste falhar"
    - "Um run com fakes não cria transação de crédito, não grava `generation_events`, não altera `ai_model_selection`/`ai_model_catalog`/`admin_audit_log` e não grava em `campaign-images`"
    - "Nenhum snapshot/erro persistido carrega chave de API (nenhum `sk-`/`AIza` no banco, no log ou no snapshot)"
    - "Os cenários têm hash SHA-256 determinístico, canonicalização estável, bootstrap idempotente, modalidade não suportada rejeitada e imagem controlada ausente recusada antes de qualquer run"
    - "O domínio de experimento aceita apenas `changed_dimension = prompt`, rejeita `model`/`configuration` com erro explícito, mantém alvo/params fixos e idênticos entre as variantes, congela a configuração após o primeiro run e recusa transições proibidas"
    - "Toda execução exige confirmação explícita e é precedida pela reserva atômica; budget, concorrência e relações inválidas recusam sem nenhuma chamada paga"
    - "Nenhum teste do laboratório instancia SDK de provider, chama endpoint de provider ou depende de imagem gerada"
  artifacts:
    - path: "src/lib/lab/__tests__/lab-environment-guard.contract.test.ts"
      provides: "matriz exaustiva dos motivos da guarda, allowlist, bloqueio de produção e fail-closed"
      contains: "remote_blocked"
    - path: "src/lib/lab/__tests__/lab-isolation.contract.test.ts"
      provides: "prova de isolamento: somente `lab_*` + `lab-artifacts`, sem créditos, sem `generation_events`/`ai_model_selection`/`campaign-images`"
      contains: "lab-artifacts"
    - path: "src/lib/lab/__tests__/lab-financial-safety.contract.test.ts"
      provides: "confirmação obrigatória, budget, concorrência, reserva antes do gasto e higiene de testes (sem chamadas reais)"
      contains: "budget_exceeded"
    - path: "src/lib/lab/scenarios/__tests__/lab-scenarios.contract.test.ts"
      provides: "contrato de cenários (hash, canonicalização, idempotência, modalidade, imagens controladas, mapeamento)"
      contains: "unsupported_scenario_mode"
    - path: "src/lib/lab/domain/__tests__/lab-experiments.contract.test.ts"
      provides: "contrato do domínio de experimentos (variantes, dimensão única, alvo fixo, transições, congelamento, limites)"
      contains: "unsupported_changed_dimension"
  key_links:
    - from: "src/lib/lab/__tests__/lab-isolation.contract.test.ts"
      to: "src/lib/lab/run-service.ts"
      via: "run real com gateway/sink fake e client gravador que lança em tabela/bucket fora da allowlist"
      pattern: "runReservedLabRun"
    - from: "src/lib/lab/__tests__/lab-isolation.contract.test.ts"
      to: "src/lib/ai/lab-telemetry-sink.ts"
      via: "spy em `AiCostTracker.record` provando que a telemetria do laboratório não grava `generation_events`"
      pattern: "AiCostTracker"
    - from: "src/lib/lab/__tests__/lab-financial-safety.contract.test.ts"
      to: "public.lab_reserve_run"
      via: "spy de `client.rpc(\"lab_reserve_run\", ...)` provando a reserva antes de qualquer invocação"
      pattern: "lab_reserve_run"
    - from: "src/lib/lab/domain/__tests__/lab-experiments.contract.test.ts"
      to: "src/lib/lab/domain/experiment-service.ts"
      via: "createExperiment/computeExperimentReadiness/transitionExperiment exercitados contra client fake"
      pattern: "createExperiment"
---

<objective>
Entregar a suíte de contrato nº 1 do laboratório (seção 11 do `tasks.md`): guarda de ambiente exaustiva, invariantes de isolamento da produção, contrato de cenários, contrato do domínio de experimentos e segurança financeira — tudo com fakes, sem nenhuma chamada paga e sem depender de imagem gerada.

Purpose: provar por teste (não por inspeção) que o laboratório é local-only, que não toca nenhuma superfície produtiva, que os cenários são determinísticos/versionados, que o domínio só permite a dimensão `prompt` com alvo fixo e que nenhuma chamada paga acontece sem confirmação, reserva atômica e budget disponível (D2/D4/D5/D14, specs `lab-isolation`/`lab-scenarios`/`lab-experiments`).
Output: 5 arquivos de teste de contrato verdes, aditivos em relação aos testes unitários dos planos 48-1-02..48-1-08 (estes exercitam matrizes e invariantes transversais, não repetem o caso felizes unitário).
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48.1-laboratorio-ia-minimo/48.1-CONTEXT.md
@openspec/changes/fase-48-1-laboratorio-ia-minimo/design.md
@openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-isolation/spec.md
@openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-scenarios/spec.md
@openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-experiments/spec.md
@openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-runs/spec.md
@src/lib/lab/__tests__/environment-guard.test.ts
@src/lib/lab/__tests__/limits.test.ts
@src/lib/lab/scenarios/__tests__/schema.test.ts
@src/lib/lab/scenarios/__tests__/service.test.ts
@src/lib/lab/domain/__tests__/experiment-service.test.ts
@src/lib/ai/__tests__/architecture-guard.test.ts
@src/lib/ai/__tests__/lab-telemetry-sink.test.ts

<interfaces>
Contratos extraídos do código e dos planos anteriores (usar diretamente — não explorar o repositório):

- `src/lib/lab/environment-guard.ts` (48-1-02): `LAB_ENVIRONMENT_REASONS = ["ok","disabled_flag","missing_url","non_local_supabase","remote_blocked"]`, `LabEnvironmentReason`, `LabEnvironmentState { enabled, supabaseHost, local, reason }`, `getLabEnvironment()`, `assertLabEnvironment()`, `LabEnvironmentError` (com `.reason`), `labEnvironmentDeniedBody(reason)` → `{ error: "environment_blocked", reason }`. Env vars: `VENDEO_LAB_ENABLED` (comparação estrita com `"true"`), `VENDEO_LAB_ALLOWED_SUPABASE_HOSTS` (CSV), `NEXT_PUBLIC_SUPABASE_URL`.
- `src/lib/lab/limits.ts` (48-1-02): `MAX_SCENARIOS_PER_EXPERIMENT=3`, `MAX_REPETITIONS=3`, `MAX_RUNS_PER_EXPERIMENT=12`, `DEFAULT_MAX_RUNS_PER_EXPERIMENT=6`, `MAX_CONCURRENT_LAB_RUNS=1`, `LAB_RUN_STALE_MS=900000`, `LAB_ARTIFACT_RETENTION_DAYS=30`.
- `src/lib/lab/scenarios/schema.ts` (48-1-03): `SCENARIO_INTENTS`/`SCENARIO_FORMATS`/`SCENARIO_LOCALES`/`SCENARIO_MEDIA_KINDS`, `SUPPORTED_SCENARIO_MODES`, `LabScenarioContentSchema`, `UnsupportedScenarioModeError` (`code: "unsupported_scenario_mode"`, `field: "intent"|"format"|"locale"`), `parseLabScenarioContent(input)`.
- `src/lib/lab/scenarios/service.ts` (48-1-03): `canonicalizeScenarioContent`, `computeScenarioContentHash`, `listScenarioFixtures`, `loadScenarioFixture(slug)` → `{ content, contentHash, fixturePath, imagesDataUrls, logoDataUrl }`, `materializeScenarios`. Erros: `invalid_scenario_path`, `missing_scenario_image:<path>`.
- `src/lib/lab/scenarios/mapper.ts` (48-1-03): `mapScenarioToCampaignBrief(content, imagesDataUrls)`, `mapScenarioToResolvedContext(content, imagesDataUrls, logoDataUrl)`.
- `src/lib/lab/domain/schemas.ts` (48-1-04): `CreateLabExperimentInputSchema`, `CreateLabEvaluationInputSchema`, `parseCreateLabExperimentInput`, `parseCreateLabEvaluationInput`, `UnsupportedChangedDimensionError` (`code: "unsupported_changed_dimension"`). Verdicts: `baseline|candidate|tie|none`; `blind_order ∈ {"baseline_left","candidate_left"}`.
- `src/lib/lab/domain/experiment-service.ts` (48-1-04): `createExperiment(input, { actorId, client })`, `computeExperimentReadiness(experimentId, client)` → `{ ready, reasons }`, `transitionExperiment(experimentId, to, { actorId, client })`, `EXPERIMENT_TRANSITIONS`, `assertTransitionAllowed`, `assertConfigurationEditable`.
- `src/lib/lab/domain/model-target.ts` (48-1-04): `ModelTargetNotInCatalogError` (`code: "model_target_not_in_catalog"`).
- `src/lib/lab/domain/prompt-snapshot.ts` (48-1-04): `PROMPT_UNDER_TEST = "campaign-image-director-offer"`, `buildBaselinePromptSnapshot()`, `buildCandidatePromptSnapshot({ promptName, promptContent })`; shape `{ name, content, contentHash, source }` com `source: "official" | "override"`.
- `src/lib/lab/domain/cost-coverage.ts` (48-1-05): `deriveCostCoverage(cost)` → `"complete" | "partial" | "missing"`.
- `src/lib/lab/run-service.ts` (48-1-07): `prepareLabRun(...)` → `{ runId, snapshot, idempotent }`; `runReservedLabRun(...)` → `{ runId, status }`; `reconcileStaleRuns({ client })` → `{ reconciled }`; `LabReservationError` com `.code` (`missing_snapshot`, `missing_operation_id`, `experiment_not_found`, `idempotency_conflict`, `experiment_not_ready`, `variant_not_in_experiment`, `scenario_not_in_experiment`, `repetition_out_of_range`, `invalid_supersedes_run`, `budget_exceeded`, `run_already_active`, `lab_reservation_failed`).
- `src/lib/lab/persistence/artifact-service.ts` (48-1-06): `LAB_ARTIFACT_BUCKET = "lab-artifacts"`, `buildOutputArtifactPath({ experimentId, runId, mimeType })`, `persistOutputArtifact(...)`, `listRunArtifacts({ client, runId })`, `createArtifactSignedUrls({ client, storagePaths })`.
- `src/lib/ai/lab-telemetry-sink.ts` (48-1-05): `LabTelemetrySink` com `emit(envelope)`, `entries`, `costSummary`; `LabCallEntry { capability, provider, model, protocol, status, durationMs, usage?, cost, errorType? }`. Nunca chama `AiCostTracker.record` nem grava `generation_events`.
- `src/lib/lab/gateway/runtime.ts` (48-1-05): `createLabGateway`, `createLabTelemetryContext`, `runLabCampaignImage`, `createDefaultLabRuntime`.
- `src/lib/lab/api/run-execution.ts` (48-1-08): `prepareExperimentRun(...)`, `runPreparedExperimentRun(...)`.
- Tabelas `lab_*` (48-1-01): `lab_scenarios`, `lab_scenario_versions`, `lab_experiments`, `lab_experiment_variants`, `lab_experiment_scenarios`, `lab_runs`, `lab_artifacts`, `lab_human_evaluations`. Bucket: `lab-artifacts`.
- **Convenção de teste:** Vitest (`environment: node`), testes co-localizados em `__tests__/`. Nenhuma chamada de rede: usar fakes (`AiInvoker` fake, `LabTelemetrySink` com envelopes simulados) e `vi.mock` de `@/lib/ai-cost/cost-estimator`/`@/lib/ai-cost/tracker` (padrão de `src/lib/ai/__tests__/lab-telemetry-sink.test.ts`).
- **Fakes de client:** os serviços recebem o client Supabase por parâmetro, o que permite um client fake em memória. Para o teste de isolamento, envolver o fake em um `Proxy` que registra cada `.from(table)`/`.storage.from(bucket)`/`.rpc(name)` e **lança** quando o alvo não está na allowlist.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Matriz exaustiva da guarda de ambiente + invariantes das constantes de limite</name>
  <files>src/lib/lab/__tests__/lab-environment-guard.contract.test.ts</files>
  <read_first>
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-isolation/spec.md` — requirement "Guarda de ambiente fail-closed e local-only" (5 cenários: local ok, flag off, remoto/produção, URL ausente/inválida, layouts pais permitidos)
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/design.md` — "D2 — Guarda de ambiente fail-closed (local-only)" (as 6 regras) e "Limites (constantes do domínio)"
    - `.planning/phases/48.1-laboratorio-ia-minimo/48.1-CONTEXT.md` — D2, bloco "Constantes de limite (LOCKED)" e `<divergences_resolved>` DV-6
    - `src/lib/lab/__tests__/environment-guard.test.ts` e `src/lib/lab/__tests__/limits.test.ts` (48-1-02) — cobertura já existente; **não** duplicar caso a caso, cobrir a matriz completa de recusa
    - `src/lib/lab/environment-guard.ts` e `src/lib/lab/limits.ts` (48-1-02) — contratos a exercitar (somente leitura)
  </read_first>
  <action>
Criar `src/lib/lab/__tests__/lab-environment-guard.contract.test.ts` (Vitest, `environment: node`). **Não** editar `environment-guard.ts` nem `limits.ts` (48-1-02 é o dono) — esta suíte é o contrato exaustivo.

1. Helper local `withEnv(overrides, fn)` que salva/restaura `VENDEO_LAB_ENABLED`, `VENDEO_LAB_ALLOWED_SUPABASE_HOSTS` e `NEXT_PUBLIC_SUPABASE_URL` em `beforeEach`/`afterEach` (garantir restauração mesmo em falha, via `try/finally`).
2. Matriz de `getLabEnvironment()` cobrindo **todos** os 5 `reason`: tabela de casos com entrada de env → `{ enabled, supabaseHost, local, reason }` esperado. Incluir no mínimo: flag ausente; `"false"`; `"1"`; `"TRUE"`; `" true "` (espaços — recusa, comparação estrita); `"true"` sem URL; URL `"nao-e-url"`; URL relativa; `http://localhost:54321`; `http://127.0.0.1:54321`; `http://[::1]:54321` (host normalizado sem colchetes); `http://0.0.0.0:54321`; `https://abcd.supabase.co`; `https://abcd.supabase.in`; `https://supabase.com`; `https://db.exemplo.com`; `https://db.exemplo.com` **com** allowlist `db.exemplo.com` (→ `ok`, `local: false`); allowlist CSV com espaços/entradas vazias (`" db.exemplo.com , ,outro.local"`) tratada corretamente; `https://abcd.supabase.co` **com** allowlist contendo `abcd.supabase.co` → **sempre** `remote_blocked` (o bloqueio de produção vence a allowlist).
3. Asserções de superfície: `getLabEnvironment()` **nunca** devolve path/query/credencial — apenas hostname; `supabaseHost` é minúsculo e sem colchetes.
4. `assertLabEnvironment()`: não lança em `ok`; lança `LabEnvironmentError` com `.reason` exato em cada caso de recusa; a mensagem contém o `reason` e, quando aplicável, apenas o hostname — assertar que a mensagem **não** casa `/sk-|AIza|api[_-]?key|\?|#/`.
5. `labEnvironmentDeniedBody(reason)`: devolve exatamente `{ error: "environment_blocked", reason }` para cada um dos 4 motivos de recusa.
6. Invariantes das constantes (import de `src/lib/lab/limits.ts`): os 7 valores exatos; `MAX_CONCURRENT_LAB_RUNS === 1`; `LAB_RUN_STALE_MS === 900000` (15 min); `DEFAULT_MAX_RUNS_PER_EXPERIMENT < MAX_RUNS_PER_EXPERIMENT`; `MAX_REPETITIONS <= MAX_RUNS_PER_EXPERIMENT`; `LAB_ARTIFACT_RETENTION_DAYS === 30`; todas as constantes são números finitos e positivos.
7. Asserção de ausência de detecção indevida: ler o fonte de `src/lib/lab/environment-guard.ts` com `node:fs` e assertar que **não** contém `NODE_ENV` nem `VERCEL_ENV` (a decisão é por flag + host, nunca por ambiente de build).
  </action>
  <verify>
    <automated>npx vitest run src/lib/lab/__tests__/lab-environment-guard.contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/lab/__tests__/lab-environment-guard.contract.test.ts` existe e cobre os 5 `reason` (grep do arquivo encontra `"ok"`, `"disabled_flag"`, `"missing_url"`, `"non_local_supabase"`, `"remote_blocked"`).
    - O caso de produção + allowlist está presente: grep do arquivo encontra `abcd.supabase.co` e `VENDEO_LAB_ALLOWED_SUPABASE_HOSTS` na mesma suíte.
    - O arquivo asserta a mensagem de erro sanitizada: grep do arquivo encontra `sk-` e `AIza` (usados em asserção negativa).
    - `git status --porcelain src/lib/lab/environment-guard.ts src/lib/lab/limits.ts` → vazio (fontes do 48-1-02 intocados).
    - `npx vitest run src/lib/lab/__tests__/lab-environment-guard.contract.test.ts` passa com exit code 0.
    - `npx vitest run src/lib/lab` (diretório completo) permanece verde.
    - `npx tsc -p tsconfig.typecheck.json --noEmit` continua verde.
  </acceptance_criteria>
  <done>Matriz exaustiva dos 5 motivos da guarda, precedência do bloqueio de produção sobre a allowlist, fail-closed em configuração ambígua, ausência de detecção por `NODE_ENV`/`VERCEL_ENV` e invariantes das 7 constantes de limite, tudo sem alterar os fontes do 48-1-02.</done>
</task>

<task type="auto">
  <name>Task 2: Isolamento da produção e segurança financeira com fakes (client gravador + allowlist)</name>
  <files>src/lib/lab/__tests__/lab-isolation.contract.test.ts, src/lib/lab/__tests__/lab-financial-safety.contract.test.ts</files>
  <read_first>
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-isolation/spec.md` — requirements "Isolamento absoluto da produção", "Guardas arquiteturais e testes de isolamento", "Segurança financeira" e "Ausência de secrets em persistência"
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-runs/spec.md` — requirement "Reserva atômica de execução" (cenários de concorrência, teto e "chamada paga só após a reserva")
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/design.md` — "D14 — Segurança financeira e limites" e "Tratamento de falhas"
    - `src/lib/lab/run-service.ts` (48-1-07) — `prepareLabRun`, `runReservedLabRun`, `reconcileStaleRuns`, `LabReservationError` (contrato exercitado de verdade)
    - `src/lib/lab/api/run-execution.ts` (48-1-08) — `prepareExperimentRun`, `runPreparedExperimentRun`
    - `src/lib/ai/lab-telemetry-sink.ts` e `src/lib/ai/__tests__/lab-telemetry-sink.test.ts` (48-1-05) — padrão de mock de `@/lib/ai-cost/cost-estimator` e `@/lib/ai-cost/tracker`
    - `src/lib/ai/__tests__/architecture-guard.test.ts` (48-1-02) — regras já estendidas para o laboratório (SDK/wire, `AiCostTracker.record`, env-var de modelo)
    - `src/lib/lab/persistence/artifact-service.ts` (48-1-06) — `LAB_ARTIFACT_BUCKET`
  </read_first>
  <action>
**A) `src/lib/lab/__tests__/lab-isolation.contract.test.ts`**

Criar um **client gravador** local (helper no próprio arquivo): `createRecordingClient(seed)` devolve um `Proxy` sobre um client fake em memória que registra cada operação (`from(table)`, `storage.from(bucket)`, `rpc(name)`, `storage.from(bucket).upload/remove/createSignedUrl`) em um array `accessLog` e **lança** `new Error("forbidden_production_access:<alvo>")` quando o alvo não está na allowlist.

Allowlist de tabelas: `lab_scenarios`, `lab_scenario_versions`, `lab_experiments`, `lab_experiment_variants`, `lab_experiment_scenarios`, `lab_runs`, `lab_artifacts`, `lab_human_evaluations`. Allowlist de bucket: `lab-artifacts`. RPCs permitidas: `lab_reserve_run`. Leitura de `ai_model_catalog` permitida **apenas** via `select` (a validação de alvo lê o catálogo); qualquer `insert`/`update`/`delete` em `ai_model_catalog` deve lançar.

Casos (task 11.2):
1. Executar o caminho real de preparação + execução de um run com **gateway/sink fake** (`AiInvoker` fake devolvendo envelope simulado com `imageBase64` mínimo e `usage`; `LabTelemetrySink` real com `resolveAiCost` mockado) e assertar: o `accessLog` contém **somente** alvos da allowlist; nenhum acesso a `campaigns`, `campaign_art_versions`, `generation_events`, `ai_model_selection`, `ai_model_catalog` (escrita), `admin_audit_log`, `credit_transactions` ou bucket `campaign-images`.
2. Provar que o teste **falha** se a produção for tocada: chamar explicitamente o client gravador em `from("generation_events")` e em `storage.from("campaign-images")` e assertar que lança `forbidden_production_access:` (asserção negativa que valida o próprio detector).
3. Nenhum crédito consumido: assertar zero ocorrências de `credit_transactions`/`credit_*` no `accessLog` e que nenhum método de crédito foi chamado.
4. Nenhum `generation_events`: `vi.mock("@/lib/ai-cost/tracker")` e assertar `AiCostTracker.record` com **zero** chamadas após um run bem-sucedido; assertar também que o `LabTelemetrySink` produziu `entries` e `costSummary` (o custo foi calculado em leitura, sem persistir).
5. Nenhum secret persistido: capturar o objeto persistido em `lab_runs` (snapshot + `calls` + `error_message`) e assertar que a serialização **não** casa `/sk-|AIza|api[_-]?key|Bearer\s/`; repetir com um erro de provider que contém uma chave fake (`"sk-LEAK123"`) e assertar que a mensagem persistida está sanitizada (`sanitizeAiErrorMessage`).
6. `prompts/` intocado: ler os bytes de `prompts/campaign-image-director-offer.md` antes e depois de montar o prompt da variante candidata e assertar igualdade (nenhuma escrita em arquivo oficial).

**B) `src/lib/lab/__tests__/lab-financial-safety.contract.test.ts`**

Casos (tasks 11.5 e 11.6):
1. Confirmação obrigatória: `LabRunExecuteRequestSchema` (de `src/lib/admin/schemas.ts`, 48-1-08) rejeita payload sem `confirmed` e com `confirmed: false`; aceita `confirmed: true`.
2. Reserva antes do gasto: com `client.rpc` fake que devolve `budget_exceeded`/`run_already_active`/`experiment_not_ready`/`invalid_supersedes_run`, assertar que `prepareLabRun` lança `LabReservationError` com o `.code` exato e que o `AiInvoker` fake tem **zero** chamadas (nenhuma invocação ocorreu).
3. Budget: com `max_runs` atingido (contador de runs fake no teto) → `budget_exceeded`; com `remainingRuns === 0` → recusa; nunca ultrapassa `MAX_RUNS_PER_EXPERIMENT`.
4. Concorrência: com um run ativo (`pending`/`running`) para o experimento → `run_already_active`; assertar que `MAX_CONCURRENT_LAB_RUNS === 1` é o invariante exercitado e que nenhuma chamada paga ocorreu.
5. Idempotência vinculada ao payload: mesma `operationId` + mesmo payload → `{ idempotent: true }` e zero invocações adicionais; mesma `operationId` + variante/cenário/repetição diferentes → `idempotency_conflict`.
6. Sem loops automáticos: executar o caminho de um run e assertar que exatamente **uma** invocação `campaign_image` ocorreu (contador do fake) — nenhum retry/segunda chamada (fallback desabilitado no laboratório).
7. Higiene de testes (task 11.6): varrer com `node:fs` os fontes de teste do laboratório (`src/lib/lab/**/__tests__/**`, `src/app/api/admin/laboratorio/**/__tests__/**`, `src/app/(app)/admin/laboratorio/**/__tests__/**`) e assertar que **nenhum** arquivo contém `new OpenAI(`, `new GoogleGenerativeAI(`, `api.openai.com`, `generativelanguage.googleapis.com`, `@/lib/image-generation/providers/openai` nem `OpenAIImageProvider` (carve-out: comentários e asserções negativas de teste que apenas provam a **não** utilização do provider são permitidos; usar `stripComments` equivalente local para evitar falso positivo em comentário). Assertar também que a varredura encontrou ao menos 1 arquivo (o próprio detector não pode passar vazio).
8. Nenhuma dependência de imagem gerada: assertar que a suíte do laboratório não lê nenhum arquivo de imagem gerada em disco (nenhum caminho contendo `output.png`/`lab-artifacts/` é lido como fixture) — as fixtures de imagem usadas são apenas as controladas de `fixtures/lab/scenarios/**`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/lab/__tests__/lab-isolation.contract.test.ts src/lib/lab/__tests__/lab-financial-safety.contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/lab/__tests__/lab-isolation.contract.test.ts` existe e contém `forbidden_production_access` (detector que faz o teste falhar ao tocar produção).
    - `Select-String` do arquivo de isolamento encontra `generation_events`, `ai_model_selection`, `campaign-images`, `credit_transactions` e `lab-artifacts` (allowlist e proibições declaradas).
    - `src/lib/lab/__tests__/lab-financial-safety.contract.test.ts` existe e contém `budget_exceeded`, `run_already_active`, `idempotency_conflict` e `confirmation`/`confirmed`.
    - `Select-String` do arquivo de segurança financeira encontra `AiCostTracker` (spy de não-gravação) e `lab_reserve_run` (spy da reserva).
    - `npx vitest run src/lib/lab/__tests__/lab-isolation.contract.test.ts src/lib/lab/__tests__/lab-financial-safety.contract.test.ts` passa com exit code 0.
    - `git status --porcelain src/lib/lab/run-service.ts src/lib/ai src/lib/ai-cost src/lib/campaign prompts/` → vazio (nenhuma alteração de produção exigida pelos testes).
    - Nenhuma chamada de rede: os testes usam client fake, `AiInvoker` fake e `vi.mock` de custo/tracker.
  </acceptance_criteria>
  <done>Prova executável de que um run do laboratório toca somente `lab_*` + `lab-artifacts`, não consome créditos, não grava `generation_events`, não altera seleção/catálogo/auditoria, não escreve em `campaign-images` nem em `prompts/`, não persiste secrets e nunca dispara mais de uma invocação paga — com confirmação, reserva atômica, budget e concorrência validados antes de qualquer gasto.</done>
</task>

<task type="auto">
  <name>Task 3: Contrato de cenários controlados + contrato do domínio de experimentos</name>
  <files>src/lib/lab/scenarios/__tests__/lab-scenarios.contract.test.ts, src/lib/lab/domain/__tests__/lab-experiments.contract.test.ts</files>
  <read_first>
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-scenarios/spec.md` — requirements "Cenários controlados e versionados", "Corpus inicial representativo e extensível" e "Cenários com imagens de produto controladas"
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-experiments/spec.md` — todos os requirements (baseline/candidata, alvo fixo, dimensão única, estados/transições, limites)
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/design.md` — "D4 — Cenários controlados", "D5 — Experimentos prompt-only com modelo fixo" e "Estados e transições"
    - `src/lib/lab/scenarios/schema.ts`, `service.ts`, `mapper.ts` (48-1-03) — contratos a exercitar
    - `src/lib/lab/domain/schemas.ts`, `experiment-service.ts`, `model-target.ts`, `prompt-snapshot.ts` (48-1-04) — contratos a exercitar
    - `fixtures/lab/scenarios/**/scenario.json` (48-1-03) — corpus real de 3 cenários
    - `src/lib/lab/scenarios/__tests__/*.test.ts` e `src/lib/lab/domain/__tests__/*.test.ts` (48-1-03/48-1-04) — cobertura unitária existente; **não** duplicar caso a caso
  </read_first>
  <action>
**A) `src/lib/lab/scenarios/__tests__/lab-scenarios.contract.test.ts`** (task 11.3)

1. Corpus: `listScenarioFixtures()` devolve exatamente os 3 slugs `produto-oferta-preco`, `produto-oferta-texto-obrigatorio`, `produto-oferta-logo`; todos com `intent: "offer"`, `format: "1:1"`, `locale: "pt-BR"`, `fictitious: true` e exatamente 1 imagem `role: "primary"`; o cenário com logo tem `identity.state === "logo"` e os demais `text_only`.
2. Hash determinístico: `computeScenarioContentHash` devolve o mesmo SHA-256 para o mesmo conteúdo com **ordem de chaves diferente** no objeto (usar `canonicalizeScenarioContent`); duas chamadas consecutivas são idênticas; conteúdo alterado muda o hash; o hash é hex de 64 caracteres.
3. Bootstrap idempotente: com um client fake em memória, `materializeScenarios` executado **duas vezes** não duplica `lab_scenarios`/`lab_scenario_versions`; com o mesmo `content_hash` não cria nova versão; com hash diferente cria **nova versão** (`version + 1`) preservando a anterior; assertar que o `accessLog` só contém `lab_scenarios`/`lab_scenario_versions`.
4. Modalidade não suportada: `parseLabScenarioContent` lança `UnsupportedScenarioModeError` com `code === "unsupported_scenario_mode"` e `field` correto para `intent` fora de `offer`, `format` fora de `1:1` e `locale` fora de `pt-BR`; **nenhum** run é iniciado nesses casos (assertar que o contador de invocação fake permanece zero).
5. Imagens controladas: `loadScenarioFixture` devolve `imagesDataUrls` como data URLs resolvidos server-side (prefixo `data:image/`), `fixturePath` sob `fixtures/lab/scenarios`; cenário com `logoPath` devolve `logoDataUrl`; imagem referenciada inexistente → erro explícito `missing_scenario_image:<path>` antes de qualquer run; slug com `..`/`/`/maiúsculas → `invalid_scenario_path`.
6. Mapeamento: `mapScenarioToCampaignBrief(content, imagesDataUrls)` produz `CampaignBrief` com `metadata.schemaVersion === "campaign_brief_v1"`, `commercial.intent === "offer"`, exatamente 1 imagem `role: "primary"` e preço/oferta coerentes com a fixture; `mapScenarioToResolvedContext(...)` produz contexto com identidade `text_only`/`logo` conforme a fixture e `store.name` fictício (assertar que o nome não corresponde a nenhuma loja real do seed — nenhum `storeId` de produção).

**B) `src/lib/lab/domain/__tests__/lab-experiments.contract.test.ts`** (task 11.4)

1. Criação válida: `createExperiment(input, { actorId, client })` com `changedDimension: "prompt"`, 2 variantes (`baseline`/`candidate`), 1–3 cenários, `repetitions ∈ [1,3]`, `maxRuns ∈ [1,12]` cria o experimento com exatamente 2 variantes e registra autoria/timestamps.
2. Dimensão única: `changedDimension: "model"` e `"configuration"` são rejeitados com `UnsupportedChangedDimensionError` (`code === "unsupported_changed_dimension"`) e **nenhum** insert ocorre.
3. Alvo fixo e idêntico: o `model_target` do experimento é único (não existe alvo por variante); assertar que `lab_experiment_variants` não recebe nenhum campo de modelo/params; `params` contém `size`, `quality` e `skipInputValidation === true`.
4. Alvo restrito ao catálogo: `model_target` fora das linhas **ativas** de `ai_model_catalog` para `campaign_image` → `ModelTargetNotInCatalogError` (`code === "model_target_not_in_catalog"`); o alvo ativo atual (`openai`/`gpt-5.5`/`responses`) é aceito; assertar **zero** escrita em `ai_model_catalog` no `accessLog`.
5. Snapshots de prompt: `buildBaselinePromptSnapshot()` devolve `source: "official"` e `content` **idêntico ao arquivo oficial** `prompts/campaign-image-director-offer.md` lido do disco (comparação byte a byte) e `contentHash` coerente; `buildCandidatePromptSnapshot({ promptName, promptContent })` devolve `source: "override"` com o conteúdo alterado e hash diferente; assertar que nenhum arquivo em `prompts/` foi escrito (bytes antes/depois).
6. Transições: `EXPERIMENT_TRANSITIONS` permite `draft→ready`, `ready→running`, `running⇄evaluated`, `ready|running|evaluated→archived`; `assertTransitionAllowed("draft","running")` e qualquer transição a partir de `archived` lançam; `transitionExperiment` recusa alvo inválido sem persistir mudança de status.
7. Prontidão e limites: `computeExperimentReadiness` → `{ ready: false }` com 1 variante, com 0 cenários, com `repetitions > MAX_REPETITIONS` e com alvo fora do catálogo; `ready: true` com 2 variantes + ≥1 cenário + limites válidos + alvo ativo; criação com 4 cenários → rejeitada por `MAX_SCENARIOS_PER_EXPERIMENT`; `maxRuns > MAX_RUNS_PER_EXPERIMENT` → rejeitado.
8. Congelamento: `assertConfigurationEditable` permite edição em `draft`/`ready` **sem runs**; com o primeiro run existente (`running`) passa a lançar; `evaluated→running` é permitido em novo run e as avaliações permanecem preservadas (nenhum `delete` em `lab_human_evaluations`).
9. Nenhum run automático: após `createExperiment` e `transitionExperiment(..., "ready")`, assertar que **nenhum** run foi criado e que o contador de invocação fake permanece zero.
10. Avaliação: `parseCreateLabEvaluationInput` aceita `verdict ∈ {baseline,candidate,tie,none}` e `blindOrder ∈ {baseline_left,candidate_left}`; rejeita `baselineRunId === candidateRunId`; rejeita verdict/ordem fora do enum; nenhuma noção de nota/score no schema (grep do fonte de `domain/schemas.ts` sem `score|rating|nota|publicável`).
  </action>
  <verify>
    <automated>npx vitest run src/lib/lab/scenarios/__tests__/lab-scenarios.contract.test.ts src/lib/lab/domain/__tests__/lab-experiments.contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/lab/scenarios/__tests__/lab-scenarios.contract.test.ts` existe e contém `produto-oferta-preco`, `produto-oferta-texto-obrigatorio`, `produto-oferta-logo` e `unsupported_scenario_mode`.
    - `src/lib/lab/domain/__tests__/lab-experiments.contract.test.ts` existe e contém `unsupported_changed_dimension`, `model_target_not_in_catalog`, `EXPERIMENT_TRANSITIONS` e `assertConfigurationEditable`.
    - `Select-String` do teste de experimentos encontra `source: "official"`/`"official"` e `"override"` (congelamento do prompt das duas variantes).
    - `Select-String` dos dois arquivos para `score|rating|nota|publicável|percentual` retorna 0 ocorrências fora de asserções negativas.
    - `npx vitest run src/lib/lab/scenarios/__tests__/lab-scenarios.contract.test.ts src/lib/lab/domain/__tests__/lab-experiments.contract.test.ts` passa com exit code 0.
    - `git status --porcelain src/lib/lab/scenarios/schema.ts src/lib/lab/scenarios/service.ts src/lib/lab/scenarios/mapper.ts src/lib/lab/domain prompts/ fixtures/` → vazio (os contratos exercitam, não reescrevem).
    - `npx tsc -p tsconfig.typecheck.json --noEmit` continua verde.
  </acceptance_criteria>
  <done>Contrato de cenários (corpus de 3, hash determinístico e canônico, bootstrap idempotente com nova versão só quando o hash muda, modalidade não suportada e imagem ausente recusadas antes do run, mapeamento para brief/contexto com dados fictícios) e contrato do domínio (2 variantes, dimensão `prompt` única, alvo/params fixos e idênticos validados contra o catálogo em leitura, baseline oficial × candidata override, transições e congelamento corretos, limites e nenhum run automático).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ambiente (env vars) → guarda | decisão de existência e permissão da superfície do laboratório |
| teste → client Supabase | fakes em memória; qualquer alvo fora da allowlist deve falhar o teste |
| teste → provider de IA | fakes (`AiInvoker`) — nenhuma chamada paga pode ocorrer na suíte |
| fixture de cenário → prompt do diretor | conteúdo de fixture alimenta o prompt (entrada não confiável) |
| domínio → catálogo de modelos | leitura de allowlist; nenhuma escrita em `ai_model_catalog` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-1-84 | Elevation of Privilege / Spoofing | `environment-guard` em produção | mitigate | Matriz exaustiva dos 5 `reason`; allowlist CSV; `*.supabase.co`/`.in`/`.com` bloqueados mesmo com allowlist; comparação estrita com `"true"`; assertar ausência de `NODE_ENV`/`VERCEL_ENV` |
| T-48-1-85 | Tampering | escrita acidental em produção | mitigate | Client gravador com allowlist `lab_*` + `lab-artifacts` que lança `forbidden_production_access:<alvo>`; asserção negativa provando que o detector funciona |
| T-48-1-86 | Repudiation / financial | créditos e `generation_events` | mitigate | Spy em `AiCostTracker.record` com zero chamadas; assertar ausência de `credit_transactions`/`generation_events`/`ai_model_selection` no `accessLog`; sink calcula custo em leitura |
| T-48-1-87 | Information Disclosure | secrets em snapshot/log | mitigate | Assertar que snapshot/`calls`/`error_message` não casam `/sk-\|AIza\|api[_-]?key\|Bearer\s/`; erro com chave fake é sanitizado por `sanitizeAiErrorMessage` |
| T-48-1-88 | Tampering | prompts oficiais | mitigate | Bytes de `prompts/campaign-image-director-offer.md` comparados antes/depois da montagem da variante candidata; baseline exige igualdade byte a byte com o arquivo oficial |
| T-48-1-89 | Tampering / path traversal | fixtures de cenário | mitigate | Casos de slug com `..`/`/`/maiúsculas → `invalid_scenario_path`; imagem ausente → `missing_scenario_image:<path>` antes de qualquer run |
| T-48-1-90 | Denial of Service / financial | budget e concorrência | mitigate | Casos de `budget_exceeded` e `run_already_active` com contador de invocação provando zero chamadas pagas; `MAX_CONCURRENT_LAB_RUNS === 1` |
| T-48-1-91 | Tampering | catálogo/seleção de modelos | mitigate | `ai_model_catalog` somente leitura (zero escrita no `accessLog`); `ai_model_selection` nunca acessada (grep/`accessLog`) |
| T-48-1-SC | Tampering | instalação de dependências | mitigate | Nenhum pacote novo: Vitest, `node:fs`/`node:crypto` já existem. Instalação nova exigiria auditoria de legitimidade + checkpoint humano bloqueante |
</threat_model>

<verification>
- `npx vitest run src/lib/lab` → exit code 0 (contrato nº 1 + suítes unitárias do laboratório).
- `npx tsc -p tsconfig.typecheck.json --noEmit` → exit code 0.
- `npm run lint` → exit code 0.
- `Get-ChildItem -Recurse -Path src/lib/lab -Filter "lab-*.contract.test.ts"` → 4 arquivos (environment-guard, isolation, financial-safety, scenarios) e `src/lib/lab/domain/__tests__/lab-experiments.contract.test.ts` → 1 arquivo.
- Varredura de referências proibidas nos arquivos criados: `Select-String` para `new OpenAI(|GoogleGenerativeAI|api.openai.com|generativelanguage.googleapis.com|OpenAIImageProvider` → 0 ocorrências.
- `git status --porcelain src/lib/ai src/lib/ai-cost src/lib/campaign src/lib/image-generation prompts/ fixtures/ supabase/` → vazio (somente testes novos).
- Nenhuma chamada de rede: clientes e invocadores são fakes; `fetch` não é usado nos arquivos criados.
</verification>

<success_criteria>
- Matriz exaustiva dos 5 `reason` da guarda com fail-closed e precedência do bloqueio de produção sobre a allowlist.
- Prova executável de isolamento: somente `lab_*` + `lab-artifacts`, sem créditos, sem `generation_events`/`ai_model_selection`/`ai_model_catalog` (escrita)/`admin_audit_log`/`campaign-images`, sem secrets e sem escrita em `prompts/`.
- Contrato de cenários: corpus de 3, hash SHA-256 determinístico e canônico, bootstrap idempotente com versionamento, modalidade não suportada e imagem ausente recusadas antes do run.
- Contrato do domínio: dimensão `prompt` única, alvo/params fixos e idênticos validados em leitura no catálogo, baseline oficial × candidata override, transições/congelamento corretos, limites aplicados e nenhum run automático.
- Segurança financeira: confirmação explícita, reserva atômica antes do gasto, budget/concorrência/idempotência recusando sem nenhuma chamada paga.
- Higiene de testes: nenhum teste instancia SDK de provider, chama endpoint de provider ou depende de imagem gerada.
</success_criteria>

<output>
Create `.planning/phases/48.1-laboratorio-ia-minimo/48-1-11-SUMMARY.md` when done.
</output>

---
phase: 48.1
plan: 48-1-12
type: execute
wave: 7
depends_on:
  - 48-1-05
  - 48-1-06
  - 48-1-07
  - 48-1-08
  - 48-1-09
  - 48-1-10
files_modified:
  - src/lib/lab/gateway/__tests__/lab-gateway-harness.contract.test.ts
  - src/lib/lab/__tests__/lab-runs.contract.test.ts
  - src/lib/lab/persistence/__tests__/lab-artifacts.contract.test.ts
  - src/app/api/admin/laboratorio/__tests__/lab-admin-api.contract.test.ts
  - src/app/(app)/admin/laboratorio/_components/__tests__/lab-admin-ui.contract.test.tsx
  - src/lib/lab/__tests__/snapshot-fixtures.contract.test.ts
autonomous: true
requirements:
  - lab-gateway-harness
  - lab-runs
  - lab-artifacts
  - lab-admin-api
  - lab-admin-ui
  - lab-human-evaluation
must_haves:
  truths:
    - "O resolver laboratorial tem precedência sobre o resolver padrão para `campaign_image` e delega a ele fora do escopo, sem nunca consultar/alterar `ai_model_selection`"
    - "O loader laboratorial serve o override do snapshot e delega ao loader real quando o prompt não está sob teste, sem escrever nenhum arquivo em `prompts/`"
    - "O `LabTelemetrySink` calcula custo em leitura e acumula envelopes sem chamar `AiCostTracker.record` e sem gravar `generation_events`"
    - "Exatamente um envelope de imagem é emitido por run: sem `OpenAIImageProvider`, sem `campaign_image_edit` e sem segunda chamada paga"
    - "O pipeline de produção (`generateImage`, `OpenAIImageProvider`, `campaign_image_review`) permanece com comportamento inalterado após o seam aditivo"
    - "O snapshot do run é imutável e nunca vazio; a transição terminal ocorre em `finally` mesmo em falha/desconexão; runs órfãos `pending`/`running` são marcados `failed` após `LAB_RUN_STALE_MS`"
    - "Idempotência por `operation_id` devolve o run existente sem nova chamada paga; reexecução explícita cria novo run com `run_sequence` derivado no banco e preserva o histórico"
    - "A validação técnica registra apenas fatos objetivos (MIME, dimensões, proporção, bytes, decodificável, uniforme) e nunca produz nota de qualidade"
    - "Artefatos vão para o bucket `lab-artifacts` com path próprio e checksum; rollback não deixa órfão; leitura por URL assinada de 3600s; cleanup manual nunca remove run em andamento"
    - "A API responde 403 (não-admin e ambiente bloqueado), 400 (payload/relações), 409 (budget/concorrência/idempotência/prontidão) e 422 (sem confirmação), sem nenhuma chamada paga nos caminhos recusados"
    - "A UI cobre página inicial, criação, estado desabilitado, confirmação com estimativa, comparação com modo cego e registro/reavaliação humana sem nenhuma nota automática"
    - "Um snapshot congelado pode ser convertido em caso determinístico com fakes, sem chamada paga e sem depender de imagem gerada"
  artifacts:
    - path: "src/lib/lab/gateway/__tests__/lab-gateway-harness.contract.test.ts"
      provides: "contrato do harness (alvo fixo, delegação, override de prompt, sink sem eventos, um único envelope)"
      contains: "campaign_image_edit"
    - path: "src/lib/lab/__tests__/lab-runs.contract.test.ts"
      provides: "contrato de execução/snapshot (imutabilidade, terminal, idempotência, reexecução, órfão, validação técnica, origem do custo)"
      contains: "LAB_RUN_STALE_MS"
    - path: "src/lib/lab/persistence/__tests__/lab-artifacts.contract.test.ts"
      provides: "contrato de artefatos (bucket/path, metadados/checksum, rollback, URL assinada, cleanup)"
      contains: "lab-artifacts"
    - path: "src/app/api/admin/laboratorio/__tests__/lab-admin-api.contract.test.ts"
      provides: "contrato HTTP da API administrativa (403/400/409/422, criação, estimativa, execução NDJSON, avaliação)"
      contains: "confirmation_required"
    - path: "src/app/(app)/admin/laboratorio/_components/__tests__/lab-admin-ui.contract.test.tsx"
      provides: "contrato de UI (inicial, criação, alerta, confirmação, comparação, avaliação)"
      contains: "Registrar avaliação"
    - path: "src/lib/lab/__tests__/snapshot-fixtures.contract.test.ts"
      provides: "conversão de snapshot em caso determinístico com fakes, sem chamada paga (D17)"
      contains: "snapshot"
  key_links:
    - from: "src/lib/lab/gateway/__tests__/lab-gateway-harness.contract.test.ts"
      to: "src/lib/lab/gateway/lab-model-resolver.ts"
      via: "precedência do alvo fixo para `campaign_image` e delegação ao resolver padrão fora do escopo"
      pattern: "fixedTarget"
    - from: "src/lib/lab/__tests__/lab-runs.contract.test.ts"
      to: "src/lib/lab/run-service.ts"
      via: "prepareLabRun/runReservedLabRun/reconcileStaleRuns com client fake e gateway fake"
      pattern: "reconcileStaleRuns"
    - from: "src/app/api/admin/laboratorio/__tests__/lab-admin-api.contract.test.ts"
      to: "src/app/api/admin/laboratorio/**/route.ts"
      via: "import dinâmico das rotas com mocks de `require-admin`/`environment-guard`/serviços"
      pattern: "confirmation_required"
    - from: "src/app/(app)/admin/laboratorio/_components/__tests__/lab-admin-ui.contract.test.tsx"
      to: "/api/admin/laboratorio/experiments/[id]/runs"
      via: "fetch mockado: confirmação com estimativa antes do disparo e progresso NDJSON"
      pattern: "application/x-ndjson"
---

<objective>
Entregar a suíte de contrato nº 2 do laboratório (seção 12 do `tasks.md`): harness de gateway, execução/snapshot imutável, artefatos, API administrativa, UI e a conversão de um snapshot congelado em caso determinístico — tudo com fakes, sem chamada paga e sem depender de imagem gerada.

Purpose: travar por teste os contratos transversais do harness isolado (D6/D7), dos snapshots imutáveis e da reserva atômica (D8/D14), dos artefatos (D10), da API mapeada por erro (D11), da UI (D12/D13) e da ponte laboratório→teste determinístico (D17).
Output: 6 arquivos de teste de contrato verdes, complementando os testes unitários dos planos 48-1-05..48-1-10.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48.1-laboratorio-ia-minimo/48.1-CONTEXT.md
@.planning/phases/48.1-laboratorio-ia-minimo/48.1-UI-SPEC.md
@openspec/changes/fase-48-1-laboratorio-ia-minimo/design.md
@openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-gateway-harness/spec.md
@openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-runs/spec.md
@openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-artifacts/spec.md
@openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-admin-api/spec.md
@openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-admin-ui/spec.md
@openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-human-evaluation/spec.md
@src/lib/lab/__tests__/run-service.test.ts
@src/lib/lab/persistence/__tests__/artifact-service.test.ts
@src/app/api/admin/laboratorio/__tests__/route.test.ts
@src/app/api/admin/feature-flags/__tests__/route.test.ts
@src/app/(app)/admin/ai-model-selection/ai-model-selection.test.tsx

<interfaces>
Contratos extraídos do código e dos planos anteriores (usar diretamente — não explorar o repositório):

- `src/lib/lab/gateway/lab-model-resolver.ts` (48-1-05): `new LabModelResolver({ fixedTarget, fallbackResolver })` — devolve o alvo fixo para `campaign_image` e delega a `fallbackResolver` para as demais capacidades.
- `src/lib/lab/gateway/lab-prompt-loader.ts` (48-1-05): `new LabPromptLoader([{ name, content }], promptsDir?)` — serve o snapshot quando o `name` casa; delega ao `PromptLoader` real caso contrário.
- `src/lib/lab/gateway/noop-image-provider.ts` (48-1-05): `createNoopImageProvider()` — lança se invocado; usado só para instanciar `ImageGenerationService`.
- `src/lib/lab/gateway/runtime.ts` (48-1-05): `createLabGateway({ fixedTarget, fallbackResolver, adapters })`, `createLabTelemetryContext({ sink, operationRunId, traceId, storeId, userId? })` (`operationRunType: "campaign_delivery"`), `runLabCampaignImage({ gateway, request, telemetry })` (uma única invocação, sem `target`), `createDefaultLabRuntime({ fixedTarget })` (dynamic import de `@/lib/ai`).
- `src/lib/ai/lab-telemetry-sink.ts` (48-1-05): `LabTelemetrySink` com `emit(envelope)`, `entries: readonly LabCallEntry[]`, `costSummary: CostResolution | null`; `LabCallEntry { capability, provider, model, protocol, status, durationMs, usage?, cost, errorType? }`.
- `src/lib/image-generation/services/image-generation-service.ts` (48-1-05): `buildDirectorPrompt(brief, context, options?)` público e aditivo; `generateImage(...)` e os métodos privados de montagem permanecem com o mesmo comportamento.
- `src/lib/lab/technical-validation.ts` (48-1-07): `LabTechnicalValidation { decodable, mimeType, width, height, bytes, aspectRatio, uniform, emptyOrCorrupt, alerts, structuredOutputValid: null, ocrAlert: null }` — alertas `empty_buffer`, `decode_failed`, `mime_mismatch`, `unexpected_mime_type`, `uniform_image`, `aspect_ratio_mismatch`, `stats_unavailable`.
- `src/lib/lab/run-service.ts` (48-1-07): `prepareLabRun(...)` → `{ runId, snapshot, idempotent }`; `runReservedLabRun(...)` → `{ runId, status }`; `reconcileStaleRuns({ client })` → `{ reconciled }`; `LabReservationError` com `.code`; `LabRunEvent`.
- `src/lib/lab/persistence/artifact-service.ts` (48-1-06): `LAB_ARTIFACT_BUCKET = "lab-artifacts"`, `buildOutputArtifactPath({ experimentId, runId, mimeType })`, `persistOutputArtifact({ client, experimentId, runId, buffer, mimeType, width, height })` → `{ artifactId, storagePath, checksum, bytes }` (rollback do objeto se o insert falhar), `listRunArtifacts({ client, runId })`, `createArtifactSignedUrls({ client, storagePaths })` (TTL 3600s).
- `scripts/lab/48-cleanup-artifacts.mjs` (48-1-06): cleanup manual por idade/arquivamento; nunca limpa run em andamento; marca `removed_at`.
- `src/lib/lab/api/experiment-queries.ts` (48-1-08): `listScenarioVersions`, `getActiveCampaignImageTarget`, `listRecentExperiments`, `listPendingEvaluations`, `getExperimentDetail`, `getRunDetail`.
- `src/lib/lab/api/estimate.ts` (48-1-08): `estimateExperimentPlan(...)` → `{ perRun, perRunCoverage, plannedRuns, remainingRuns, totalEstimatedUsd, coverage }`.
- `src/lib/lab/api/evaluation-service.ts` (48-1-08): `createEvaluation(...)`, `InvalidComparisonRunsError` (`code: "invalid_comparison_runs" | "runs_not_terminal"`).
- Rotas (48-1-08): `GET /api/admin/laboratorio/scenarios`, `GET|POST /api/admin/laboratorio/experiments`, `GET /experiments/[id]`, `GET /experiments/[id]/estimate`, `POST /experiments/[id]/runs` (NDJSON, `confirmed: true`, 422 `confirmation_required`), `GET /runs/[id]`, `POST /experiments/[id]/evaluations`. Erros: 403 `environment_blocked`; 409 `budget_exceeded`/`run_already_active`/`idempotency_conflict`/`experiment_not_ready`; 400 `invalid_payload`/`missing_snapshot`/`unsupported_scenario_mode`/`variant_not_in_experiment`/`scenario_not_in_experiment`/`repetition_out_of_range`/`invalid_supersedes_run`; 404 `experiment_not_found`/`run_not_found`.
- UI (48-1-09/48-1-10): `src/app/(app)/admin/laboratorio/{layout,page,cenarios,experimentos/novo,experimentos/[id],experimentos/[id]/comparar}`; componentes locais `_components/{disabled-notice,lab-select,lab-textarea,lab-table,lab-radio-group,confirm-dialog,experiment-form,run-execution-panel,comparison-view,evaluation-form,blind-toggle,comparison-format}.tsx`. Copy do contrato: "Novo experimento", "Criar experimento", "Executar run", "Confirmar execução", "Registrar avaliação", "Laboratório desabilitado neste ambiente".
- **Convenção de teste de rota:** mocks de `server-only`, `@/lib/admin/require-admin`, `@/lib/lab/environment-guard`, `@/lib/supabase/server` e dos serviços; import dinâmico da rota dentro do teste (precedente `src/app/api/admin/feature-flags/__tests__/route.test.ts`).
- **Convenção de teste de UI:** cabeçalho `// @vitest-environment jsdom`, `@testing-library/jest-dom/vitest`, `render`/`screen`/`fireEvent`/`waitFor`, `vi.stubGlobal("fetch", mockFetch)`, `vi.stubGlobal("crypto", { randomUUID })`.
- **Convenção de teste de custo:** `vi.hoisted` + `vi.mock("@/lib/ai-cost/cost-estimator", ...)` e `vi.mock("@/lib/ai-cost/tracker", ...)` (padrão de `src/lib/ai/__tests__/lab-telemetry-sink.test.ts`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contrato do harness de gateway (alvo fixo, delegação, override, sink, um único envelope)</name>
  <files>src/lib/lab/gateway/__tests__/lab-gateway-harness.contract.test.ts</files>
  <read_first>
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-gateway-harness/spec.md` — todos os requirements (instância isolada, override de prompt, telemetria própria, fallback desabilitado, caminho real reutilizado, compatibilidade F47)
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/design.md` — "D6 — Harness de gateway isolado" e "D7 — Caminho de execução"
    - `src/lib/lab/gateway/lab-model-resolver.ts`, `lab-prompt-loader.ts`, `runtime.ts`, `noop-image-provider.ts` (48-1-05)
    - `src/lib/ai/lab-telemetry-sink.ts` (48-1-05) e `src/lib/ai/__tests__/lab-telemetry-sink.test.ts` (padrão de mock)
    - `src/lib/image-generation/services/__tests__/image-generation-service.test.ts` — pipeline de produção cujo comportamento deve permanecer inalterado
    - `src/lib/image-generation/services/__tests__/image-generation-service.build-director-prompt.test.ts` (48-1-05) — cobertura do seam aditivo
  </read_first>
  <action>
Criar `src/lib/lab/gateway/__tests__/lab-gateway-harness.contract.test.ts` (Vitest, `environment: node`, com `vi.mock("@/lib/ai-cost/cost-estimator")` e `vi.mock("@/lib/ai-cost/tracker")`).

1. **Precedência do alvo fixo**: `LabModelResolver` com `fixedTarget = { provider: "openai", model: "gpt-5.5", protocol: "responses" }` e um `fallbackResolver` fake que devolveria outro alvo para `campaign_image` — assertar que `resolve("campaign_image")` devolve **exatamente** o alvo fixo e que o `fallbackResolver` **não** foi chamado para essa capacidade.
2. **Delegação fora do escopo**: `resolve("campaign_copy")` (ou outra capacidade) devolve o que o `fallbackResolver` fake devolve e o fallback foi chamado 1 vez; assertar que `ai_model_selection` nunca é referenciada (grep do fonte de `lab-model-resolver.ts` e `runtime.ts` sem `ai_model_selection`).
3. **Override de prompt sem tocar o oficial**: `LabPromptLoader` com override de `campaign-image-director-offer` devolve o conteúdo do snapshot para esse nome; para um nome não coberto (ex.: `campaign-image-director-spotlight`) delega ao `PromptLoader` real e devolve o conteúdo do arquivo; assertar bytes de `prompts/campaign-image-director-offer.md` antes/depois **idênticos**.
4. **Sink sem eventos produtivos**: emitir dois envelopes simulados (um `success` com `usageMeta.imageGenerationTool === true` e um `failed`) e assertar: `entries` tem 2 itens com capability/provider/model/protocol/status/durationMs; `costSummary` preserva os campos reais de `CostResolution` (`costSource`, `pricingVersion`, `costFormulaVersion`, componentes de texto/imagem) — **sem** `imageUnitUsd`/`costPartial`; `AiCostTracker.record` com zero chamadas; nenhuma tabela `generation_events` no `accessLog` de um client fake.
5. **Um único envelope (fallback desabilitado)**: com um `AiInvoker` fake que conta invocações, executar o caminho de um run (`runLabCampaignImage` / `createLabGateway`) e assertar: exatamente 1 invocação; exatamente 1 envelope de `campaign_image`; **nenhuma** invocação de `campaign_image_edit`; nenhuma consulta ao resolver para `campaign_image_edit`. Repetir com o `AiInvoker` falhando por capability e assertar que ainda assim há exatamente 1 tentativa (nenhuma segunda chamada paga).
6. **Pipeline de produção inalterado**: assertar que `createNoopImageProvider()` lança se `generateImage` for chamado (o provider nunca deve ser invocado no laboratório); assertar que `buildDirectorPrompt` é público e que `generateImage` continua existindo em `image-generation-service.ts`; rodar a suíte existente do serviço de imagem (`src/lib/image-generation/services/__tests__/image-generation-service.test.ts`) dentro do mesmo comando de verificação para provar comportamento inalterado.
7. **Catálogo somente leitura**: assertar que o harness lê o catálogo no máximo para validar o alvo e que nenhum `insert`/`update`/`delete` em `ai_model_catalog` aparece no `accessLog`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/lab/gateway/__tests__/lab-gateway-harness.contract.test.ts src/lib/image-generation/services/__tests__/image-generation-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/lab/gateway/__tests__/lab-gateway-harness.contract.test.ts` existe e contém `campaign_image_edit` (asserção negativa de não-invocação).
    - `Select-String` do arquivo encontra `AiCostTracker`, `fixedTarget`, `LabPromptLoader` e `createNoopImageProvider`.
    - `Select-String` do arquivo para `imageUnitUsd|costPartial` retorna 0 ocorrências.
    - `npx vitest run src/lib/lab/gateway/__tests__/lab-gateway-harness.contract.test.ts src/lib/image-generation/services/__tests__/image-generation-service.test.ts` passa com exit code 0.
    - `git status --porcelain src/lib/ai/gateway.ts src/lib/image-generation/providers/openai.ts prompts/` → vazio.
    - `npx tsc -p tsconfig.typecheck.json --noEmit` continua verde.
  </acceptance_criteria>
  <done>Contrato do harness provado: alvo fixo com precedência, delegação fora do escopo sem tocar seleção, override de prompt sem escrever arquivos oficiais, sink calculando custo em leitura sem `generation_events` e exatamente um envelope de imagem por run (sem provider/fallback), com o pipeline de produção inalterado.</done>
</task>

<task type="auto">
  <name>Task 2: Contrato de execução e snapshot imutável (terminal, idempotência, reexecução, órfão, validação técnica, custo)</name>
  <files>src/lib/lab/__tests__/lab-runs.contract.test.ts</files>
  <read_first>
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-runs/spec.md` — requirements "Snapshot imutável por execução", "Reserva atômica de execução", "Idempotência e histórico explícito de reexecução", "Estados e transições do run" e "Validação automática limitada ao técnico"
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/design.md` — "D8 — Snapshots imutáveis, idempotência e histórico", "D9 — Validação automática limitada ao técnico", "Estados e transições" e "Tratamento de falhas"
    - `src/lib/lab/run-service.ts`, `run-snapshot.ts`, `technical-validation.ts` (48-1-07)
    - `src/lib/lab/__tests__/run-service.test.ts`, `run-snapshot.test.ts`, `technical-validation.test.ts` (48-1-07) — cobertura unitária existente; cobrir aqui os contratos transversais (terminal em falha, histórico de reexecução, órfão `pending` **e** `running`, origem completa do custo)
    - `src/lib/lab/limits.ts` (48-1-02) — `LAB_RUN_STALE_MS`
    - `src/lib/lab/domain/cost-coverage.ts` (48-1-05) — `deriveCostCoverage`
  </read_first>
  <action>
Criar `src/lib/lab/__tests__/lab-runs.contract.test.ts` (Vitest, `environment: node`, com `vi.mock("@/lib/ai-cost/cost-estimator")` e client fake em memória que aplica as regras dos triggers de imutabilidade).

1. **Snapshot imutável e nunca vazio**: assertar que `prepareLabRun` envia `p_snapshot` completo (cenário/versão/hash, prompt `{ name, content, contentHash, source }`, capability, `modelTarget`, params, `changedDimension: "prompt"`, `variantRole`, `runType: "lab"`); snapshot `null`/`{}` → `LabReservationError` com `code === "missing_snapshot"`; tentar atualizar colunas de snapshot de um run existente via client fake → lança (equivalente ao trigger) e o serviço nunca tenta esse `update` (assertar `accessLog` sem `update` sobre `snapshot`).
2. **Transição terminal em `finally`**: com o gateway fake lançando erro, assertar que o run termina `failed` (nunca permanece `running`); com o cliente "desconectando" (callback `onEvent` lançando), assertar que a transição terminal ainda ocorre; com sucesso, termina `succeeded` com `latency_ms`, `usage`, `estimated_cost_usd`, `calls` e `technical_validation` preenchidos.
3. **Origem completa do custo**: assertar que `cost_detail` persiste `costSource`, `pricingVersion`, `costFormulaVersion`, `costEstimationNote`, `textComponentUsd`/`imageToolComponentUsd` e os campos `imageToolPricing*`; `deriveCostCoverage(cost_detail)` mapeia para `complete`/`partial`/`missing` nos 3 cenários; assertar ausência de `imageUnitUsd`/`costPartial`.
4. **Idempotência**: mesma `operationId` + mesmo payload → `{ idempotent: true }` com o mesmo `runId` e **zero** invocações adicionais; mesma `operationId` + variante/cenário/repetição diferentes → `LabReservationError` `idempotency_conflict`; simular `unique_violation` no client fake e assertar o tratamento idempotente quando o payload coincide.
5. **Reexecução com histórico**: `supersedes_run_id` da mesma combinação (incluindo `repetition_index`) e terminal → novo run com `run_sequence` **derivado** (`max + 1`, nunca aceito do cliente); o run anterior permanece intacto (nenhum `update`/`delete` sobre ele); `supersedes_run_id` de outra repetição/cenário/variante ou ainda ativo → `invalid_supersedes_run`.
6. **Órfãos `pending` e `running`**: com um run `pending` e outro `running` cujo `started_at`/`created_at` é anterior a `LAB_RUN_STALE_MS`, `reconcileStaleRuns({ client })` marca ambos como `failed`; um run ativo recente **não** é tocado; assertar que a reconciliação não faz nenhuma chamada ao provider.
7. **Validação técnica objetiva**: com buffers gerados localmente por `sharp` (PNG válido, buffer corrompido, imagem uniforme branca) assertar `decodable`/`mimeType`/`width`/`height`/`aspectRatio`/`bytes`, alertas `decode_failed`/`uniform_image`/`empty_buffer` conforme o caso, `structuredOutputValid === null` e `ocrAlert === null`; assertar que o resultado **não** contém nenhum campo casando `/score|rating|nota|beauty|publicável/i`.
8. **Falha de persistência de artefato**: com `persistOutputArtifact` fake lançando `artifact_persistence_failed`, assertar que o run termina `failed` e que nenhum artefato órfão é registrado.
  </action>
  <verify>
    <automated>npx vitest run src/lib/lab/__tests__/lab-runs.contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/lab/__tests__/lab-runs.contract.test.ts` existe e contém `LAB_RUN_STALE_MS`, `idempotency_conflict`, `invalid_supersedes_run`, `missing_snapshot` e `costFormulaVersion`.
    - `Select-String` do arquivo para `imageUnitUsd|costPartial` retorna 0 ocorrências.
    - `Select-String` do arquivo encontra `uniform_image` e `structuredOutputValid` (validação técnica).
    - `npx vitest run src/lib/lab/__tests__/lab-runs.contract.test.ts` passa com exit code 0.
    - `npx vitest run src/lib/lab` permanece verde.
    - Nenhuma chamada de rede: gateway/sink são fakes e `sharp` gera os buffers localmente.
  </acceptance_criteria>
  <done>Contrato de execução provado: snapshot completo e imutável gravado na reserva, transição terminal garantida em falha/desconexão, idempotência vinculada ao payload, reexecução com `run_sequence` derivado e histórico preservado, órfãos `pending`/`running` reconciliados por `LAB_RUN_STALE_MS`, custo com origem completa e validação técnica apenas objetiva (sem nota).</done>
</task>

<task type="auto">
  <name>Task 3: Contrato de artefatos + contrato HTTP da API administrativa</name>
  <files>src/lib/lab/persistence/__tests__/lab-artifacts.contract.test.ts, src/app/api/admin/laboratorio/__tests__/lab-admin-api.contract.test.ts</files>
  <read_first>
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-artifacts/spec.md` — todos os requirements (bucket/paths, metadados/checksum, URL assinada, retenção/cleanup)
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-admin-api/spec.md` — todos os requirements (superfície protegida, criação/leitura, estimativa, execução confirmada, detalhe/avaliação)
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/design.md` — "D10 — Artefatos", "D11 — API administrativa" e "Tratamento de falhas"
    - `src/lib/lab/persistence/artifact-service.ts` (48-1-06) e `scripts/lab/48-cleanup-artifacts.mjs`
    - `src/lib/lab/api/*.ts` (48-1-08) e as 7 rotas sob `src/app/api/admin/laboratorio/**`
    - `src/app/api/admin/feature-flags/__tests__/route.test.ts` — padrão de mock/import dinâmico de rota admin
    - `src/lib/lab/limits.ts` (48-1-02) — `LAB_ARTIFACT_RETENTION_DAYS`
  </read_first>
  <action>
**A) `src/lib/lab/persistence/__tests__/lab-artifacts.contract.test.ts`** (task 12.3)

1. **Bucket e path**: `buildOutputArtifactPath({ experimentId, runId, mimeType: "image/png" })` → `experiments/{experimentId}/runs/{runId}/output.png` (e `image/jpeg` → `.jpg`, `image/webp` → `.webp`); `persistOutputArtifact` chama `storage.from("lab-artifacts")`; assertar que **nenhum** path contém `campaign-images` e que o bucket `campaign-images` nunca é acessado no `accessLog`.
2. **Metadados e checksum**: o insert em `lab_artifacts` registra `storage_path`, `mime_type`, `width`, `height`, `bytes` e `checksum` SHA-256 de 64 hex equivalente ao calculado com `node:crypto` sobre o buffer.
3. **Rollback sem órfão**: com o insert em `lab_artifacts` falhando, assertar que `storage.remove` foi chamado para o path enviado e que nenhum registro permanece; com o `upload` falhando, assertar que nenhum insert ocorreu.
4. **URL assinada**: `createArtifactSignedUrls({ client, storagePaths })` chama `createSignedUrl(path, 3600)` (assertar o TTL exato) e devolve o mapa `storagePath → signedUrl`; path vazio/ausente → `null`; nenhuma URL pública é montada manualmente.
5. **Cleanup manual**: com fixtures de runs (um `archived` antigo, um `succeeded` mais antigo que `LAB_ARTIFACT_RETENTION_DAYS`, um `pending`/`running` recente e um `succeeded` recente), assertar que o script remove apenas os elegíveis, marca `removed_at` nos registros correspondentes, **não** remove o run em andamento e **não** apaga metadados/hashes/avaliações; assertar que a retenção usada é `LAB_ARTIFACT_RETENTION_DAYS` (30) e que nenhum scheduler/agendamento é criado.
6. **Acesso negado**: `listRunArtifacts` filtra artefatos com `removed_at` preenchido; o bucket permanece privado (nenhuma chamada `getPublicUrl` no `accessLog`).

**B) `src/app/api/admin/laboratorio/__tests__/lab-admin-api.contract.test.ts`** (task 12.4)

Com mocks de `server-only`, `@/lib/admin/require-admin`, `@/lib/lab/environment-guard`, `@/lib/supabase/server` e dos serviços de `@/lib/lab/api/*`; importar cada rota dinamicamente dentro do teste. Casos:
1. **403 não-admin**: cada uma das 7 rotas → 403 via `ForbiddenError` e nenhum serviço chamado.
2. **403 ambiente bloqueado**: `assertLabEnvironment` lançando `LabEnvironmentError` → 403 `{ error: "environment_blocked", reason }` para cada rota e nenhum serviço/leitura chamado.
3. **400 payload inválido**: `POST /experiments` e `POST /evaluations` com corpo inválido → 400 `{ error: "invalid_payload", details }` e nenhum insert.
4. **Criação e leitura**: `POST /experiments` válido → 201 com `experimentId`/`status`; `GET /scenarios` → 200 com lista; `GET /experiments` → 200; `GET /experiments/[id]` → 200 com `budget.remaining` e 404 `experiment_not_found` quando ausente; `GET /runs/[id]` → 200 com `snapshot` e `artifacts[].signedUrl` e 404 `run_not_found`.
5. **Estimativa**: `GET /experiments/[id]/estimate` → 200 com `coverage` e `totalEstimatedUsd`; cobertura `partial`/`missing` **não** produz 4xx (a execução continua permitida com ressalva); experimento ausente → 404.
6. **422 sem confirmação**: `POST /experiments/[id]/runs` sem `confirmed: true` → 422 `{ error: "confirmation_required" }` **antes** de `prepareExperimentRun` (assertar o serviço não chamado e nenhuma invocação paga).
7. **409/400 da reserva**: mapear `budget_exceeded`, `run_already_active`, `idempotency_conflict`, `experiment_not_ready` → 409; `missing_snapshot`, `missing_operation_id`, `variant_not_in_experiment`, `scenario_not_in_experiment`, `repetition_out_of_range`, `invalid_supersedes_run`, `unsupported_scenario_mode` → 400; `experiment_not_found` → 404.
8. **Execução confirmada e idempotente**: resposta com `Content-Type: application/x-ndjson` e o corpo contendo o evento final `{ type: "done", runId, status }`; operação idempotente → 200 JSON `{ idempotent: true, runId }` **sem** stream.
9. **Avaliação**: `POST /experiments/[id]/evaluations` válido → 201 `{ evaluationId, createdAt }` com o `evaluatorId` do admin; `invalid_comparison_runs`/`runs_not_terminal` → 400 e nenhum insert.
10. **Sem secrets**: assertar que nenhum corpo de resposta contém `sk-`, `AIza`, `Bearer ` ou `operation_id` de outro experimento.
  </action>
  <verify>
    <automated>npx vitest run src/lib/lab/persistence/__tests__/lab-artifacts.contract.test.ts "src/app/api/admin/laboratorio/__tests__/lab-admin-api.contract.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/lab/persistence/__tests__/lab-artifacts.contract.test.ts` existe e contém `lab-artifacts`, `checksum`, `createSignedUrl` e `removed_at`.
    - `src/app/api/admin/laboratorio/__tests__/lab-admin-api.contract.test.ts` existe e contém `confirmation_required`, `budget_exceeded`, `run_already_active`, `idempotency_conflict`, `environment_blocked` e `application/x-ndjson`.
    - `Select-String` do teste de artefatos para `campaign-images` aparece **apenas** em asserção negativa (nenhum path montado com esse bucket).
    - `npx vitest run src/lib/lab/persistence/__tests__/lab-artifacts.contract.test.ts "src/app/api/admin/laboratorio/__tests__/lab-admin-api.contract.test.ts"` passa com exit code 0.
    - `npx vitest run src/app/api/admin/laboratorio` permanece verde (rotas do 48-1-08 intactas).
    - `npx tsc -p tsconfig.typecheck.json --noEmit` continua verde.
  </acceptance_criteria>
  <done>Contrato de artefatos (bucket/path próprios, metadados e checksum, rollback sem órfão, URL assinada de 3600s, cleanup manual protegendo run em andamento) e contrato HTTP completo da API (403/400/409/422/404 mapeados, criação, leitura, estimativa que não bloqueia, execução NDJSON com confirmação e idempotência, avaliação validada, nenhum secret na resposta).</done>
</task>

<task type="auto">
  <name>Task 4: Contrato de UI + snapshots como fixture determinística</name>
  <files>src/app/(app)/admin/laboratorio/_components/__tests__/lab-admin-ui.contract.test.tsx, src/lib/lab/__tests__/snapshot-fixtures.contract.test.ts</files>
  <read_first>
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-admin-ui/spec.md` — todos os requirements (entrada única, página inicial, estado desabilitado, criação/detalhe, design system)
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-human-evaluation/spec.md` — requirements "Comparação lado a lado" e "Registro da avaliação humana"
    - `.planning/phases/48.1-laboratorio-ia-minimo/48.1-UI-SPEC.md` — "Screens & States", "Interaction Contract" e "Copywriting Contract"
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/design.md` — "D12 — UI administrativa", "D13 — Comparação humana" e "D17 — Das descobertas do laboratório para testes determinísticos"
    - Componentes locais `src/app/(app)/admin/laboratorio/_components/*.tsx` (48-1-09/48-1-10) e testes unitários existentes (`disabled-notice`, `experiment-form`, `run-execution-panel`, `comparison-view`, `evaluation-form`)
    - `src/app/(app)/admin/ai-model-selection/ai-model-selection.test.tsx` — padrão de teste de componente/página admin
  </read_first>
  <action>
**A) `src/app/(app)/admin/laboratorio/_components/__tests__/lab-admin-ui.contract.test.tsx`** (task 12.5) — cabeçalho `// @vitest-environment jsdom`, `@testing-library/jest-dom/vitest`, `vi.stubGlobal("fetch", mockFetch)`.

1. **Página inicial**: com experimentos recentes e avaliações pendentes → ambos os blocos renderizam e cada item é um link para o detalhe; sem dados → estado vazio com heading "Nenhum experimento ainda" e CTA "Novo experimento".
2. **Estado desabilitado**: com `getLabEnvironment()` devolvendo `enabled: false` → renderiza "Laboratório desabilitado neste ambiente" com o `reason` legível e a nota de que nenhuma tabela `lab_*`/storage/provider foi acessado; assertar que nenhuma chamada de dados (fetch) ocorreu.
3. **Criação prompt-only**: no form de criação, a dimensão alterada aparece como `prompt` **fixa e não editável**, o modelo aparece como fixo (nenhum `<select>` de modelo por variante), a seleção de cenários é limitada a 3, repetições aceitam 1–3 e o teto 1–12; submit válido faz POST em `/api/admin/laboratorio/experiments` e redireciona ao detalhe; erro de API exibe mensagem em `accent.red`.
4. **Confirmação com estimativa**: no detalhe, "Executar run" abre a confirmação exibindo a estimativa por componente e a cobertura (`complete`/`partial`/`missing`); sem confirmar, nenhum POST é feito; confirmando, o POST vai para `/experiments/[id]/runs` com `confirmed: true` e `operationId` (assertar o corpo) e o progresso NDJSON é consumido até `{ type: "done", runId }`; budget restante é visível.
5. **Comparação e modo cego**: a tela de comparação renderiza os dois painéis lado a lado com arte por URL assinada, status técnico, custo, latência, alertas e erros sanitizados; ligar o modo cego oculta modelo/prompt e "Revelar" os traz de volta; o seletor de repetição troca o par comparado.
6. **Avaliação e reavaliação**: as 4 opções de verdict com os rótulos do contrato; CTA "Registrar avaliação" desabilitado sem verdict; submit envia `scenarioVersionId`, `baselineRunId`, `candidateRunId`, `verdict`, `blindOrder` e `observation`; sucesso exibe a avaliação mais recente e reavaliar mantém a anterior no histórico (append-only na UI).
7. **Sem nota automática**: assertar que nenhuma tela renderiza texto casando `/nota|score|pontua|%|publicável|ranking/i` e que nenhum componente calcula média/percentual.
8. **Acessibilidade básica**: rótulos associados aos campos, `role="switch"`/`aria-checked` no modo cego, alvos ≥ 44×44px (`min-h-[44px]`/`min-w-[44px]`) e nenhum emoji (assertar que o texto renderizado não casa o intervalo de emojis).

**B) `src/lib/lab/__tests__/snapshot-fixtures.contract.test.ts`** (task 12.6, D17)

1. Dado um `lab_runs.snapshot` congelado (fixture local com prompt, `modelTarget`, params, cenário/versão/hash e `variantRole`), extrair um caso determinístico: montar o prompt com o `LabPromptLoader` + `ImageGenerationService` (provider no-op) e assertar presença/ausência dos blocos esperados, os params enviados e o alvo resolvido — **sem** chamada paga.
2. Simular o envelope com um `AiInvoker` fake (envelope simulado + resposta fake com `imageBase64` mínimo) e assertar que o caso trava o contrato (capability `campaign_image`, um único envelope, `usageMeta.imageGenerationTool` refletido no custo) sem depender da imagem gerada.
3. Assertar que o teste **não** lê nenhum artefato de `lab-artifacts` nem arquivo `output.*` como fixture e que a suíte passa com `OPENAI_API_KEY`/`GEMINI_API_KEY` ausentes.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(app)/admin/laboratorio/_components/__tests__/lab-admin-ui.contract.test.tsx" src/lib/lab/__tests__/snapshot-fixtures.contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(app)/admin/laboratorio/_components/__tests__/lab-admin-ui.contract.test.tsx` existe, começa com `// @vitest-environment jsdom` e contém `Registrar avaliação`, `Laboratório desabilitado neste ambiente`, `application/x-ndjson` e `aria-checked`.
    - `Select-String` do arquivo de UI para `score|rating|publicável|ranking|percentual` aparece **apenas** em asserção negativa.
    - `src/lib/lab/__tests__/snapshot-fixtures.contract.test.ts` existe e contém `snapshot`, `campaign_image` e `usageMeta`.
    - `npx vitest run "src/app/(app)/admin/laboratorio/_components/__tests__/lab-admin-ui.contract.test.tsx" src/lib/lab/__tests__/snapshot-fixtures.contract.test.ts` passa com exit code 0.
    - `npx vitest run "src/app/(app)/admin/laboratorio" src/lib/lab` permanece verde.
    - Nenhuma chamada de rede: `fetch` mockado e invocadores fakes.
  </acceptance_criteria>
  <done>Contrato de UI (inicial com estado vazio/desabilitado, criação prompt-only com modelo fixo e limites, confirmação com estimativa e progresso NDJSON, comparação com modo cego e repetições, avaliação/reavaliação append-only, zero nota automática, acessibilidade básica) e conversão de snapshot congelado em caso determinístico com fakes, sem chamada paga e sem depender de imagem gerada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador/admin → rotas do laboratório | entrada HTTP não confiável (payload, parâmetros, confirmação) |
| rota → gateway/provider | chamada paga real disparada apenas após reserva e confirmação |
| rota → bucket `lab-artifacts` | URLs assinadas expostas ao cliente |
| teste → produção (tabelas/bucket/prompts) | qualquer acesso indevido deve falhar o teste |
| snapshot congelado → caso de teste | dado histórico vira fixture; não pode arrastar chamada paga nem artefato gerado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-1-92 | Tampering | gateway/resolver do laboratório | mitigate | Teste de precedência do alvo fixo + delegação fora do escopo; `ai_model_selection` ausente (grep); `gateway.ts` e `openai.ts` sem diff |
| T-48-1-93 | Tampering / financial | fallback automático | mitigate | Asserção de exatamente 1 envelope e 0 invocações de `campaign_image_edit`; `OpenAIImageProvider`/`createNoopImageProvider` nunca invocados no run |
| T-48-1-94 | Tampering / Repudiation | snapshot e histórico de runs | mitigate | Client fake aplica imutabilidade de snapshot; `run_sequence` derivado no banco; run anterior nunca sofre `update`/`delete` na reexecução |
| T-48-1-95 | Denial of Service | runs órfãos | mitigate | Reconciliação preguiçosa cobre `pending` **e** `running` após `LAB_RUN_STALE_MS`, sem scheduler e sem chamada paga |
| T-48-1-96 | Information Disclosure | URLs assinadas e respostas da API | mitigate | `createSignedUrl(path, 3600)` server-side; bucket privado; asserção de que nenhum corpo contém `sk-`/`AIza`/`Bearer`/`operation_id` alheio |
| T-48-1-97 | Tampering | paths de storage | mitigate | Path sempre derivado de IDs de banco (`experiments/{id}/runs/{id}/output.{ext}`); assertar que `campaign-images` nunca é acessado e que nenhum path de loja é montado |
| T-48-1-98 | Elevation of Privilege | rotas administrativas | mitigate | `requireAdmin()` + `assertLabEnvironment()` exercitados rota a rota (403 em ambos os casos) com serviços não chamados |
| T-48-1-99 | Tampering / financial | confirmação e budget via HTTP | mitigate | 422 `confirmation_required` antes do serviço; 409 `budget_exceeded`/`run_already_active`/`idempotency_conflict`; zero invocações nos caminhos recusados |
| T-48-1-100 | Repudiation / bias | avaliação humana | mitigate | Teste de UI prova verdict/observação/runs comparados/ordem cega, reavaliação preservando histórico e ausência de qualquer nota automática |
| T-48-1-SC | Tampering | instalação de dependências | mitigate | Nenhum pacote novo: Vitest, Testing Library, `sharp`, `node:crypto` já existem. Instalação nova exigiria auditoria de legitimidade + checkpoint humano bloqueante |
</threat_model>

<verification>
- `npx vitest run src/lib/lab "src/app/api/admin/laboratorio" "src/app/(app)/admin/laboratorio"` → exit code 0.
- `npx vitest run src/lib/image-generation/services/__tests__/image-generation-service.test.ts` → exit code 0 (pipeline de produção inalterado).
- `npx tsc -p tsconfig.typecheck.json --noEmit` → exit code 0.
- `npm run lint` → exit code 0.
- `Get-ChildItem -Recurse -Path src/lib/lab -Filter "lab-*.contract.test.ts"` → 8 arquivos (3 deste plano sob `src/lib/lab`: gateway-harness, runs, artifacts + 5 do 48-1-11); `snapshot-fixtures.contract.test.ts` não casa o filtro `lab-*` e `lab-admin-api.contract.test.ts`/`lab-admin-ui.contract.test.tsx` vivem fora de `src/lib/lab`.
- `git status --porcelain src/lib/ai/gateway.ts src/lib/ai-cost src/lib/campaign src/lib/image-generation/providers prompts/ supabase/` → vazio.
- Nenhuma chamada de rede: `fetch` mockado; gateways/invocadores fakes; `sharp` gera buffers localmente.
</verification>

<success_criteria>
- Harness provado: alvo fixo com precedência, delegação fora do escopo, override de prompt sem tocar arquivos oficiais, sink sem `generation_events` e um único envelope de imagem por run.
- Execução provada: snapshot completo/imutável, terminal em falha, idempotência, reexecução com histórico, reconciliação de órfãos e validação técnica sem nota.
- Artefatos provados: bucket/path próprios, metadados/checksum, rollback sem órfão, URL assinada de 3600s e cleanup manual protegendo run em andamento.
- API provada: 403/400/409/422/404 mapeados, criação/leitura/estimativa/execução NDJSON/avaliação corretos, nenhuma chamada paga nos caminhos recusados e nenhum secret na resposta.
- UI provada: inicial, criação prompt-only, estado desabilitado, confirmação com estimativa, comparação com modo cego, avaliação/reavaliação append-only e ausência de nota automática.
- Snapshot congelado convertido em caso determinístico com fakes, sem chamada paga e sem depender de imagem gerada (D17).
</success_criteria>

<output>
Create `.planning/phases/48.1-laboratorio-ia-minimo/48-1-12-SUMMARY.md` when done.
</output>

---
phase: 48.1
plan: 48-1-13
type: execute
wave: 8
depends_on:
  - 48-1-08
  - 48-1-09
  - 48-1-10
  - 48-1-11
  - 48-1-12
files_modified:
  - scripts/verify/48-1-13-contract-guard.mjs
  - src/lib/image-generation/services/__tests__/image-generation-service.test.ts
  - src/app/api/campaign/generate-image/__tests__/route.test.ts
autonomous: true
requirements:
  - lab-isolation
  - lab-scenarios
  - lab-experiments
  - lab-runs
  - lab-gateway-harness
  - lab-artifacts
  - lab-admin-api
  - lab-admin-ui
  - lab-human-evaluation
must_haves:
  truths:
    - "A suíte completa (`npx vitest run`) passa com exit code 0, sem resíduos pendentes"
    - "Nenhum teste existente de F46/F47 quebrou após a fase do laboratório"
    - "`npm run typecheck`, `npm run lint` e `npm run build` passam com exit code 0"
    - "O contrato externo permanece intacto: UI/form do lojista, schema público, snapshot/domínio, `prompts/` oficiais, `src/lib/ai/gateway.ts`, `ai_model_catalog`/`ai_model_selection`"
    - "`src/lib/image-generation/services/image-generation-service.ts` mudou apenas de forma aditiva (seam `buildDirectorPrompt`), sem remover `generateImage`/`buildPromptVariables`/`assemblePrompt`/override `brief_review_confirmed`"
    - "`generation_events` e `campaign-images` não receberam nenhuma referência nova do laboratório"
    - "Fixtures/testes irmãos afetados pelo seam aditivo foram co-migrados, sem alterar o comportamento de produção"
  artifacts:
    - path: "scripts/verify/48-1-13-contract-guard.mjs"
      provides: "guard de contrato congelado: paths proibidos sem diff, gateway sem persistência do lab, prompts intactos, seam aditivo e ausência de referências proibidas"
      contains: "generation_events"
    - path: "src/lib/image-generation/services/__tests__/image-generation-service.test.ts"
      provides: "co-migração (somente se necessária) do teste irmão do pipeline afetado pelo seam aditivo"
      contains: "generateImage"
    - path: "src/app/api/campaign/generate-image/__tests__/route.test.ts"
      provides: "co-migração (somente se necessária) do teste de rota irmão afetado pelo seam aditivo"
      contains: "generate-image"
  key_links:
    - from: "scripts/verify/48-1-13-contract-guard.mjs"
      to: "git diff --name-only <base>"
      via: "lista de paths congelados que não podem aparecer no diff da fase"
      pattern: "frozenPaths"
    - from: "scripts/verify/48-1-13-contract-guard.mjs"
      to: "src/lib/ai/gateway.ts"
      via: "asserção de ausência de referências ao laboratório/persistência de seleção"
      pattern: "ai_model_selection"
    - from: "scripts/verify/48-1-13-contract-guard.mjs"
      to: "src/lib/image-generation/services/image-generation-service.ts"
      via: "asserção de diff aditivo (sem remoção de métodos/override de produção)"
      pattern: "buildDirectorPrompt"
---

<objective>
Fechar a fase do ponto de vista de regressão e não-mudança: rodar a suíte completa e corrigir resíduos, co-migrar apenas as fixtures/testes irmãos afetados pelo seam aditivo de `ImageGenerationService`, rodar os 3 gates (typecheck/lint/build) e comprovar por `git diff` que nenhuma superfície externa ou produtiva foi alterada e que `generation_events`/`campaign-images` não ganharam referências do laboratório.

Purpose: garantir que o laboratório é estritamente aditivo — sem drift de contrato externo (D18), sem tocar produção (D2/D6/D15) e sem quebrar F46/F47.
Output: `scripts/verify/48-1-13-contract-guard.mjs` (guard executável) e co-migração pontual de testes irmãos, com a suíte e os 3 gates verdes.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48.1-laboratorio-ia-minimo/48.1-CONTEXT.md
@openspec/changes/fase-48-1-laboratorio-ia-minimo/design.md
@openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-isolation/spec.md
@openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-gateway-harness/spec.md
@scripts/verify/47-07-contract-guard.mjs
@src/lib/ai/__tests__/architecture-guard.test.ts
@src/lib/ai/__tests__/f47-contract-guard.test.ts
@src/lib/image-generation/services/image-generation-service.ts
@src/lib/image-generation/providers/openai.ts
@package.json

<interfaces>
Contratos e âncoras extraídos do repositório e dos planos anteriores (usar diretamente — não explorar o repositório):

- `package.json` scripts: `test` → `vitest run`; `typecheck` → `tsc -p tsconfig.typecheck.json --noEmit`; `lint` → `eslint .`; `build` → `npm run check:cnae && next build`.
- `vitest.config.ts`: `globals: true`, `environment: "node"`, alias `@` → `src`, `server-only` mockado.
- Gates de contrato existentes (não alterar): `src/lib/ai/__tests__/architecture-guard.test.ts` (SDK/wire fora dos adapters, `resolveAiCost` restrito a `src/lib/ai/**`, `AiCostTracker.record` fora de `src/lib/ai/**`, env-var de modelo fora dos adapters) e `src/lib/ai/__tests__/f47-contract-guard.test.ts` (gateway sem persistência/seleção).
- Precedente de guard de contrato: `scripts/verify/47-07-contract-guard.mjs` — compara `git diff --name-only <base>` contra uma lista de `frozenPaths`, lê fontes e lança em drift; base declarada como SHA no topo do script.
- `src/lib/image-generation/services/image-generation-service.ts` (48-1-05): ganhou o seam **aditivo** `buildDirectorPrompt(brief, context, options?)`. `generateImage(...)`, `buildPromptVariables(...)`, `assemblePrompt(...)` e o override `brief_review_confirmed` devem permanecer.
- `src/lib/image-generation/providers/openai.ts`: **intocado**; o fallback `campaign_image` → `campaign_image_edit` continua sendo uma segunda invoke explícita (a produção não muda).
- `src/lib/ai/gateway.ts`: **intocado** (sem referências ao laboratório/persistência de seleção).
- `prompts/`: **intocado** (`campaign-image-director.md`, `-offer.md`, `-spotlight.md`, `-exclusive.md`, `campaign-image-reviewer.md`).
- Superfícies congeladas que **não podem** aparecer no diff da fase: `src/lib/ai/gateway.ts`, `src/lib/ai/model-resolver.ts`, `src/lib/ai/model-registry.ts`, `src/lib/ai-cost/`, `src/lib/campaign/`, `src/lib/campaign-intelligence/`, `src/lib/snapshot.ts`, `src/lib/image-generation/providers/openai.ts`, `src/lib/image-generation/schema.ts`, `prompts/`, `src/components/flow/`, `src/app/api/campaign/generate-image/route.ts`, `src/app/api/campaign/generate/route.ts`, `supabase/migrations/20260914000001_f47_ai_model_catalog_selection.sql`.
- Referências proibidas ao laboratório em código produtivo: `generation_events`, `AiCostTracker.record`, `campaign-images`, `ai_model_selection`, `admin_audit_log`, `campaign_art_versions`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Suíte completa, resíduos e co-migração do seam aditivo (F46/F47 intactos)</name>
  <files>src/lib/image-generation/services/__tests__/image-generation-service.test.ts, src/app/api/campaign/generate-image/__tests__/route.test.ts</files>
  <read_first>
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-gateway-harness/spec.md` — requirement "Caminho real de geração reutilizado sem duplicação" (cenário "Pipeline de produção não muda de comportamento")
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/design.md` — "D7 — Caminho de execução" (itens 1 e 6) e "D18 — Compatibilidade F46/F47"
    - `src/lib/image-generation/services/image-generation-service.ts` (48-1-05) — seam `buildDirectorPrompt` aditivo
    - `src/lib/image-generation/services/__tests__/image-generation-service.test.ts` — teste irmão do pipeline
    - `src/app/api/campaign/generate-image/__tests__/route.test.ts` — teste irmão da rota de geração
    - `src/lib/ai/__tests__/architecture-guard.test.ts` e `src/lib/ai/__tests__/f47-contract-guard.test.ts` — gates F46/F47 que devem permanecer verdes
  </read_first>
  <action>
1. Rodar a suíte completa: `npx vitest run`. Corrigir **somente** os resíduos causados pela fase do laboratório (nada de refactor oportunista, nada de ajuste em teste produtivo que não esteja relacionado ao seam aditivo ou à adição de arquivos do laboratório).
2. Confirmar explicitamente que os gates/testes de F46/F47 continuam verdes: `npx vitest run src/lib/ai src/lib/ai-cost` (inclui `architecture-guard.test.ts`, `f47-contract-guard.test.ts`, adapters, gateway, telemetria) e `npx vitest run src/lib/image-generation src/app/api/campaign/generate-image`.
3. Co-migração do seam (task 13.5): se e somente se o seam aditivo `buildDirectorPrompt` tiver alterado assinaturas/expectativas dos testes irmãos, atualizar `src/lib/image-generation/services/__tests__/image-generation-service.test.ts` e/ou `src/app/api/campaign/generate-image/__tests__/route.test.ts` — **sem** alterar a produção (`image-generation-service.ts`, `providers/openai.ts`, prompts). Não é permitido "consertar" um teste enfraquecendo asserção de comportamento de produção: se um teste falhar por mudança de comportamento (e não por assinatura/fixture), isso é regressão e deve ser corrigido no código aditivo, não no teste.
4. Registrar no SUMMARY: contagem de arquivos/testes, comandos executados, quais testes irmãos precisaram de co-migração (ou "nenhum") e a justificativa.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` → exit code 0 (suíte completa verde).
    - `npx vitest run src/lib/ai src/lib/ai-cost` → exit code 0 (gates F46/F47 e telemetria de custo intactos).
    - `npx vitest run src/lib/image-generation src/app/api/campaign/generate-image` → exit code 0 (pipeline de produção intacto).
    - `git status --porcelain src/lib/image-generation/services/image-generation-service.ts src/lib/image-generation/providers/openai.ts prompts/ src/lib/ai/gateway.ts` → vazio (nenhuma alteração de produção na task de regressão).
    - Se algum teste irmão foi tocado, o diff mostra apenas ajuste de assinatura/fixture — nenhuma remoção de asserção de comportamento.
  </acceptance_criteria>
  <done>Suíte completa verde, gates F46/F47 verdes, pipeline de produção verde e co-migração pontual (ou ausência dela) registrada — sem nenhuma alteração de produção.</done>
</task>

<task type="auto">
  <name>Task 2: Guard de contrato congelado + os 3 gates (typecheck/lint/build)</name>
  <files>scripts/verify/48-1-13-contract-guard.mjs</files>
  <read_first>
    - `scripts/verify/47-07-contract-guard.mjs` — precedente: `git diff --name-only <base>` contra `frozenPaths`, leitura de fontes e `throw` em drift
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/proposal.md` — seção "Impact" (lista de código alterado mínimo e aditivo e de superfícies intocadas)
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/design.md` — "D16 — Estratégia de migration local e remota" (a migration do lab é a única adição em `supabase/migrations/`) e "D18"
    - `src/lib/image-generation/services/image-generation-service.ts` — seam aditivo a verificar
    - `src/lib/ai/gateway.ts` — superfície que não pode conter referências ao laboratório
    - `package.json` — scripts de gate
  </read_first>
  <action>
Criar `scripts/verify/48-1-13-contract-guard.mjs` (Node ESM, sem dependência nova, exit code 1 em qualquer violação com mensagem clara):

1. **Base**: aceitar `--base <sha>` (argv); quando omitido, usar `HEAD` e operar sobre o working tree (`git diff --name-only HEAD`). Documentar no cabeçalho do arquivo como obter a base da fase (commit imediatamente anterior ao primeiro commit `48-1-*`). Falhar com mensagem explícita se `--base` apontar para um ref inexistente.
2. **Paths congelados**: `frozenPaths` = `src/lib/ai/gateway.ts`, `src/lib/ai/model-resolver.ts`, `src/lib/ai/model-registry.ts`, `src/lib/ai-cost/`, `src/lib/campaign/`, `src/lib/campaign-intelligence/`, `src/lib/snapshot.ts`, `src/lib/image-generation/providers/openai.ts`, `src/lib/image-generation/schema.ts`, `prompts/`, `src/components/flow/`, `src/app/api/campaign/generate-image/route.ts`, `src/app/api/campaign/generate/route.ts`, `supabase/migrations/20260914000001_f47_ai_model_catalog_selection.sql`. Qualquer arquivo do diff que seja igual ou comece por um prefixo congelado → violação.
3. **Migrations do laboratório**: o diff só pode conter arquivos novos em `supabase/migrations/` cujo nome contenha `f48_1`; qualquer outra migration alterada/removida → violação.
4. **Gateway intocado**: assertar que `src/lib/ai/gateway.ts` não contém `lab_`, `LabTelemetrySink`, `ai_model_selection` nem `PersistedModelResolver`; e que `src/lib/ai/model-resolver.ts` não contém `lab`.
5. **Prompts intactos**: comparar o conteúdo de cada arquivo em `prompts/` com a versão da base (`git show <base>:<path>`); qualquer divergência → violação. (A candidata do laboratório vive apenas no snapshot da variante.)
6. **Seam aditivo**: obter o diff de `src/lib/image-generation/services/image-generation-service.ts` (`git diff -U0 <base> -- <path>`) e assertar que **nenhuma linha removida** casa `generateImage|buildPromptVariables|assemblePrompt|brief_review_confirmed`; assertar que o arquivo contém `buildDirectorPrompt`.
7. **Referências proibidas ao laboratório**: varrer recursivamente `src/lib/ai-cost/`, `src/lib/campaign/`, `src/lib/campaign-intelligence/`, `src/lib/visual-signature/`, `src/app/api/campaign/`, `src/lib/image-generation/providers/` e assertar que nenhum arquivo contém `lab_` (prefixo de tabela/contexto do laboratório), `lab-artifacts`, `LabTelemetrySink` ou `LabPromptLoader`. Ler fontes com `node:fs` e ignorar diretórios `__tests__`/`node_modules`.
8. **`generation_events` e `campaign-images` sem referência nova**: assertar que nenhum arquivo do laboratório (`src/lib/lab/**`, `src/lib/ai/lab-telemetry-sink.ts`, `src/app/api/admin/laboratorio/**`, `src/app/(app)/admin/laboratorio/**`, `scripts/lab/**`, `scripts/uat/48-*.mjs`) referencia `generation_events` ou `campaign-images` — exceto em comentário/asserção negativa de teste (aplicar `stripComments` e ignorar arquivos `*.test.*`/`__tests__`). Assertar também que `supabase/migrations/20260708000002_create_campaign_images_bucket.sql` não aparece no diff da fase.
9. **Saída**: imprimir um resumo (`paths congelados: OK`, `prompts: OK`, `seam aditivo: OK`, `referências proibidas: 0`) e sair com 0; em violação, imprimir cada item com o arquivo e sair com 1.
10. Rodar os 3 gates: `npm run typecheck`, `npm run lint`, `npm run build`.
  </action>
  <verify>
    <automated>node scripts/verify/48-1-13-contract-guard.mjs; if ($?) { npm run typecheck }; if ($?) { npm run lint }; if ($?) { npm run build }</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify/48-1-13-contract-guard.mjs` existe, é ESM, aceita `--base` e sai com código 1 em violação (grep do arquivo encontra `process.exit(1)`).
    - O guard cobre as 4 verificações estruturais: grep do arquivo encontra `frozenPaths`, `prompts/`, `buildDirectorPrompt` e `generation_events`.
    - `node scripts/verify/48-1-13-contract-guard.mjs` → exit code 0 no estado final da fase.
    - `npm run typecheck` → exit code 0; `npm run lint` → exit code 0; `npm run build` → exit code 0.
    - `git status --porcelain src/lib/ai/gateway.ts src/lib/ai-cost src/lib/campaign src/lib/image-generation/providers/openai.ts prompts/` → vazio.
    - `Get-ChildItem supabase/migrations | Where-Object { $_.Name -like "*f48*" }` → exatamente os 2 arquivos criados no 48-1-01 e nenhum outro.
  </acceptance_criteria>
  <done>Guard executável que falha em qualquer drift de contrato congelado (paths proibidos, gateway, prompts, migration alheia, referências proibidas) e prova que o seam de `ImageGenerationService` foi apenas aditivo — com typecheck, lint e build verdes.</done>
</task>

<task type="auto">
  <name>Task 3: Varredura de não-mudança do contrato externo e evidências</name>
  <files>scripts/verify/48-1-13-contract-guard.mjs</files>
  <read_first>
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-isolation/spec.md` — requirement "Isolamento absoluto da produção" (cenários de nenhuma escrita produtiva, nenhum crédito, storage separado, prompts intactos)
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/proposal.md` — seção "Impact" (superfícies intocadas) e "Sem mudança de superfície externa"
    - `.planning/phases/48.1-laboratorio-ia-minimo/48.1-CONTEXT.md` — `<decisions>` D2/D6/D15/D18 e `<divergences_resolved>` DV-4/DV-8
    - `scripts/verify/48-1-13-contract-guard.mjs` (Task 2) — script a estender
  </read_first>
  <action>
Estender `scripts/verify/48-1-13-contract-guard.mjs` com a verificação explícita de não-mudança do contrato externo e gerar as evidências da task 13.3/13.4:

1. **Contrato externo via `git diff`**: assertar que o diff da fase (vs `--base`) **não** contém nenhum arquivo de: UI/form do lojista (`src/components/flow/`, `src/components/campaign/`), schema público de geração (`src/lib/image-generation/schema.ts`), snapshot/domínio (`src/lib/snapshot.ts`, `src/lib/campaign/`, `src/lib/campaign-intelligence/`), prompts oficiais (`prompts/`), `src/lib/ai/gateway.ts`, e a migration F47 do catálogo/seleção (`20260914000001_f47_ai_model_catalog_selection.sql`).
2. **Arquivos alterados permitidos**: assertar que o diff da fase contém **apenas** arquivos sob `src/lib/lab/**`, `src/lib/ai/lab-telemetry-sink.ts`, `src/app/api/admin/laboratorio/**`, `src/app/(app)/admin/laboratorio/**`, `src/app/(app)/admin/layout.tsx`, `src/lib/admin/schemas.ts`, `src/lib/image-generation/services/image-generation-service.ts`, `src/lib/ai/__tests__/architecture-guard.test.ts`, `fixtures/lab/**`, `scripts/lab/**`, `scripts/uat/48-*.mjs`, `scripts/verify/48-1-13-contract-guard.mjs`, `supabase/migrations/*f48_1*`, `.env.example`, e os arquivos de tracking/planejamento (`.planning/**`, `ROADMAP.md`, `AGENTS.md`, `docs/**`). Qualquer arquivo fora dessa lista → violação com o nome do arquivo.
3. **Sem referência nova a `generation_events`/`campaign-images`/créditos/catálogo**: assertar que nenhum arquivo produtivo do laboratório referencia `generation_events`, `campaign-images`, `credit_transactions`, `admin_audit_log`, `campaign_art_versions`; que `ai_model_selection` não aparece em nenhum arquivo do laboratório (exceto teste que asserta a ausência); e que `ai_model_catalog` aparece **somente** em leitura (`select`) — assertar ausência de `.insert(`/`.update(`/`.delete(` no mesmo arquivo que referencia `ai_model_catalog`.
4. **Evidência**: imprimir um relatório estruturado (JSON no stdout) com `base`, `changedFiles`, `frozenViolations: []`, `externalContractViolations: []`, `forbiddenReferences: []`, `promptsUnchanged: true`, `additiveSeam: true` — para registro no SUMMARY da fase.
5. Executar o guard com a base da fase e registrar o JSON resultante no SUMMARY deste plano.
  </action>
  <verify>
    <automated>node scripts/verify/48-1-13-contract-guard.mjs --base HEAD</automated>
  </verify>
  <acceptance_criteria>
    - O guard agora imprime um relatório JSON com os campos `frozenViolations`, `externalContractViolations`, `forbiddenReferences`, `promptsUnchanged` e `additiveSeam`.
    - `node scripts/verify/48-1-13-contract-guard.mjs --base HEAD` → exit code 0 com `frozenViolations: []`, `externalContractViolations: []` e `forbiddenReferences: []`.
    - `Select-String -Path scripts/verify/48-1-13-contract-guard.mjs` encontra `campaign-images`, `generation_events`, `ai_model_selection` e `credit_transactions` (varredura declarada).
    - `git status --porcelain src/components/flow src/components/campaign src/lib/image-generation/schema.ts src/lib/snapshot.ts src/lib/campaign src/lib/campaign-intelligence prompts/ src/lib/ai/gateway.ts` → vazio.
    - Nenhuma migration além das `*f48_1*` aparece no diff da fase.
  </acceptance_criteria>
  <done>Não-mudança do contrato externo comprovada por `git diff` (UI/form, schema público, snapshot/domínio, prompts, gateway e migration F47 intactos), allowlist de arquivos alterados da fase aplicada e ausência de referências novas do laboratório a `generation_events`, `campaign-images`, créditos, auditoria e seleção/catálogo de modelos — com relatório JSON registrado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fase do laboratório → código produtivo | qualquer drift fora do bounded context precisa falhar a verificação |
| fase do laboratório → banco produtivo | tabelas/bucket/migrations produtivas não podem ser tocadas |
| base do diff → conclusão de não-mudança | base incorreta invalidaria a prova de não-mudança |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-1-101 | Tampering | contrato externo (UI/form, schema, snapshot, prompts, gateway) | mitigate | Guard compara `git diff --name-only <base>` contra `frozenPaths` e contra uma allowlist de arquivos permitidos; qualquer arquivo fora → exit 1 |
| T-48-1-102 | Tampering | `image-generation-service.ts` (seam) | mitigate | Diff `-U0` não pode remover `generateImage`/`buildPromptVariables`/`assemblePrompt`/`brief_review_confirmed`; presença obrigatória de `buildDirectorPrompt` |
| T-48-1-103 | Repudiation | prova de não-mudança | mitigate | Base explícita via `--base <sha>` (documentada no cabeçalho); relatório JSON com base, arquivos alterados e listas de violações registrado no SUMMARY |
| T-48-1-104 | Tampering | `generation_events`/`campaign-images`/créditos/catálogo | mitigate | Varredura recursiva de referências proibidas em código produtivo do laboratório; `ai_model_catalog` somente leitura (ausência de `insert`/`update`/`delete`); migration de bucket `campaign-images` fora do diff |
| T-48-1-105 | Denial of Service | regressão silenciosa em F46/F47 | mitigate | Suíte completa + execução focal de `src/lib/ai`, `src/lib/ai-cost`, `src/lib/image-generation` e rota de geração antes de qualquer ajuste de fixture |
| T-48-1-SC | Tampering | instalação de dependências | mitigate | Guard usa apenas `node:fs`/`node:child_process`; nenhum pacote novo. Instalação nova exigiria auditoria de legitimidade + checkpoint humano bloqueante |
</threat_model>

<verification>
- `npx vitest run` → exit code 0 (suíte completa).
- `npx vitest run src/lib/ai src/lib/ai-cost src/lib/image-generation src/app/api/campaign/generate-image` → exit code 0 (F46/F47 e pipeline de produção intactos).
- `npm run typecheck` → exit code 0; `npm run lint` → exit code 0; `npm run build` → exit code 0.
- `node scripts/verify/48-1-13-contract-guard.mjs --base HEAD` → exit code 0 com listas de violação vazias.
- `git status --porcelain src/lib/ai/gateway.ts src/lib/ai/model-resolver.ts src/lib/ai/model-registry.ts src/lib/ai-cost src/lib/campaign src/lib/campaign-intelligence src/lib/snapshot.ts src/lib/image-generation/providers/openai.ts src/lib/image-generation/schema.ts prompts/ src/components/flow src/app/api/campaign/generate-image/route.ts` → vazio.
- `Get-ChildItem supabase/migrations | Where-Object { $_.Name -like "*f48*" }` → exatamente 2 arquivos.
</verification>

<success_criteria>
- Suíte completa verde, sem resíduos e sem quebra de F46/F47.
- Co-migração restrita às fixtures/testes irmãos afetados pelo seam aditivo, sem alterar comportamento de produção.
- `typecheck`, `lint` e `build` verdes.
- Contrato externo intacto comprovado por `git diff` (UI/form, schema público, snapshot/domínio, prompts oficiais, `gateway.ts`, catálogo/seleção).
- `generation_events` e `campaign-images` sem nenhuma referência nova do laboratório.
- Guard de contrato executável com relatório JSON registrado.
</success_criteria>

<output>
Create `.planning/phases/48.1-laboratorio-ia-minimo/48-1-13-SUMMARY.md` when done.
</output>

---
phase: 48.1
plan: 48-1-14
type: execute
wave: 9
depends_on:
  - 48-1-13
files_modified:
  - .planning/phases/48.1-laboratorio-ia-minimo/48.1-UAT.md
  - .planning/phases/48.1-laboratorio-ia-minimo/48-1-VERIFICATION.md
  - scripts/uat/48-local-uat-prep.mjs
  - supabase/migrations/20260915000002_f48_1_create_lab_tables.sql
  - supabase/migrations/20260915000003_f48_1_lab_immutability_and_reserve.sql
  - AGENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - ROADMAP.md
  - .planning/PROJECT.md
autonomous: false
requirements:
  - lab-isolation
  - lab-scenarios
  - lab-experiments
  - lab-runs
  - lab-gateway-harness
  - lab-artifacts
  - lab-admin-api
  - lab-admin-ui
  - lab-human-evaluation
must_haves:
  truths:
    - "O ambiente local de UAT é preparado com Supabase Docker, bucket `lab-artifacts`, cenários materializados e chave/projeto de **desenvolvimento** dos providers, recusando qualquer host remoto antes de escrever"
    - "O orçamento de UAT (cenários × variantes × repetições, teto de runs e custo estimado por run) está documentado antes da primeira chamada paga"
    - "O experimento inicial obrigatório é executado: cenário controlado de produto/oferta, baseline = prompt oficial atual, candidata = pequena alteração no prompt, **mesmo modelo/parâmetros/imagens**, ≥1 geração por variante"
    - "A comparação lado a lado e o voto humano são registrados com custo e latência, e a produção permaneceu inalterada (nenhuma campanha/crédito/seleção/catálogo/prompt/`generation_events`/`campaign-images` alterados)"
    - "A migration do laboratório é aplicada deliberadamente no remoto após a UAT, com `--dry-run` antes, mantendo tabelas/bucket **inertes** e `VENDEO_LAB_ENABLED=false` em produção"
    - "`48-1-VERIFICATION.md` apresenta a matriz goal-backward das 9 capabilities com evidência automatizada distinguida da validação humana"
    - "`48.1-UAT.md` contém roteiro, orçamento, resultados por passo e confirmação de produção inalterada"
    - "Os critérios da proposta são confirmados um a um (abrir `/admin/laboratorio`, selecionar cenário, configurar baseline/candidata prompt-only, executar gerações isoladas, comparar, registrar avaliação com runs comparados, consultar prompt/modelo/params/custo/latência/erros, nenhum dado produtivo alterado, testes sem chamadas pagas, 4 gates verdes, fluxo completo em Docker local, pontos de extensão F48.2+)"
    - "O tracking (AGENTS.md/STATE/ROADMAP/PROJECT) reflete a fase concluída e o arquivamento está preparado sem mover/apagar o change OpenSpec sem instrução explícita"
  artifacts:
    - path: ".planning/phases/48.1-laboratorio-ia-minimo/48.1-UAT.md"
      provides: "roteiro humano, orçamento de UAT e resultados do experimento inicial obrigatório"
      contains: "orçamento"
    - path: ".planning/phases/48.1-laboratorio-ia-minimo/48-1-VERIFICATION.md"
      provides: "matriz goal-backward das 9 capabilities com evidência automatizada × validação humana"
      contains: "lab-human-evaluation"
    - path: "scripts/uat/48-local-uat-prep.mjs"
      provides: "preparação local validada (host local, bucket, cenários, orçamento) que recusa remoto"
      contains: "lab-artifacts"
  key_links:
    - from: "scripts/uat/48-local-uat-prep.mjs"
      to: "scripts/uat/48-local-scenarios.mjs"
      via: "bootstrap idempotente dos cenários controlados antes do UAT"
      pattern: "48-local-scenarios"
    - from: ".planning/phases/48.1-laboratorio-ia-minimo/48.1-UAT.md"
      to: "supabase/migrations/20260915000002_f48_1_create_lab_tables.sql"
      via: "UAT local aprovada antes do push remoto deliberado (D16)"
      pattern: "f48_1"
    - from: ".planning/phases/48.1-laboratorio-ia-minimo/48-1-VERIFICATION.md"
      to: "openspec/changes/fase-48-1-laboratorio-ia-minimo/proposal.md"
      via: "critérios de aceitação confirmados um a um"
      pattern: "critérios"
---

<objective>
Fechar a F48.1: preparar o ambiente local e o orçamento, executar o experimento inicial obrigatório com IA real sob aprovação humana, comprovar que a produção permaneceu inalterada, aplicar deliberadamente a migration no remoto (mantendo o schema inerte e a flag `false` em produção), gerar a verificação goal-backward e o UAT e atualizar o tracking com o arquivamento preparado.

Purpose: cumprir D16 (migration local → UAT local → remoto deliberado após a UAT, para não deixar migration pendente) e confirmar os critérios de aceitação da proposta com evidência humana e automatizada.
Output: `48.1-UAT.md`, `48-1-VERIFICATION.md`, `scripts/uat/48-local-uat-prep.mjs`, migration aplicada no remoto e tracking fechado.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48.1-laboratorio-ia-minimo/48.1-CONTEXT.md
@.planning/phases/48.1-laboratorio-ia-minimo/48.1-UI-SPEC.md
@openspec/changes/fase-48-1-laboratorio-ia-minimo/proposal.md
@openspec/changes/fase-48-1-laboratorio-ia-minimo/design.md
@openspec/changes/fase-48-1-laboratorio-ia-minimo/tasks.md
@openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-isolation/spec.md
@openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-runs/spec.md
@openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-human-evaluation/spec.md
@scripts/uat/47-local-bootstrap.mjs
@scripts/uat/48-local-scenarios.mjs
@scripts/lab/48-cleanup-artifacts.mjs
@switch-env.ps1
@AGENTS.md

<interfaces>
Contratos e âncoras extraídos do repositório e dos planos anteriores (usar diretamente — não explorar o repositório):

- `scripts/uat/47-local-bootstrap.mjs` — precedente de bootstrap/UAT local: lê credenciais via `npx supabase status -o env`, **valida e recusa** endpoints remotos antes de qualquer escrita, usa `@supabase/supabase-js` + `pg`, faz cleanup idempotente.
- `switch-env.ps1 local` — entrada oficial de troca de ambiente (deriva credenciais locais); `scripts/uat/47-local-dev.ps1` é apenas wrapper legado.
- `scripts/uat/48-local-scenarios.mjs` (48-1-03) — bootstrap idempotente de `lab_scenarios`/`lab_scenario_versions` a partir de `fixtures/lab/scenarios/**`, com recusa de host não local.
- `scripts/lab/48-cleanup-artifacts.mjs` (48-1-06) — cleanup manual opt-in (nunca limpa run em andamento).
- `src/lib/lab/environment-guard.ts` (48-1-02): `VENDEO_LAB_ENABLED === "true"` + `NEXT_PUBLIC_SUPABASE_URL` com host local (`localhost`/`127.0.0.1`/`::1`/`0.0.0.0`) ou allowlist `VENDEO_LAB_ALLOWED_SUPABASE_HOSTS`; `*.supabase.co`/`.in`/`.com` **sempre** bloqueados; razões `ok|disabled_flag|missing_url|non_local_supabase|remote_blocked`.
- `src/lib/lab/limits.ts` (48-1-02): `MAX_SCENARIOS_PER_EXPERIMENT=3`, `MAX_REPETITIONS=3`, `MAX_RUNS_PER_EXPERIMENT=12`, `DEFAULT_MAX_RUNS_PER_EXPERIMENT=6`, `MAX_CONCURRENT_LAB_RUNS=1`, `LAB_RUN_STALE_MS=900000`, `LAB_ARTIFACT_RETENTION_DAYS=30`.
- Cenários disponíveis (48-1-03): `produto-oferta-preco`, `produto-oferta-texto-obrigatorio`, `produto-oferta-logo` (todos `offer`/`1:1`/`pt-BR`). Prompt sob teste: `campaign-image-director-offer`.
- Rotas (48-1-08): `/api/admin/laboratorio/scenarios`, `/experiments` (GET/POST), `/experiments/[id]`, `/experiments/[id]/estimate` (cobertura `complete|partial|missing`), `/experiments/[id]/runs` (NDJSON, `confirmed: true`, 422 `confirmation_required`), `/runs/[id]` (URLs assinadas 3600s), `/experiments/[id]/evaluations`. Erros: 403 `environment_blocked`; 409 `budget_exceeded`/`run_already_active`/`idempotency_conflict`/`experiment_not_ready`; 400 `missing_snapshot`/`invalid_payload`/relações.
- UI (48-1-09/48-1-10): `/admin/laboratorio` (inicial), `/experimentos/novo`, `/experimentos/[id]`, `/experimentos/[id]/comparar`, `/cenarios`; um único link "Laboratório" na nav do admin.
- Migration local (48-1-01): `supabase/migrations/20260915000002_f48_1_create_lab_tables.sql` (8 tabelas `lab_*` + bucket `lab-artifacts` + RLS/grants) e `20260915000003_f48_1_lab_immutability_and_reserve.sql` (triggers de imutabilidade/append-only + RPC `lab_reserve_run`), com bloco REVERT. **Não aplicadas no remoto até este plano (D16).**
- Bucket `lab-artifacts`: privado (`public=false`), policy somente `service_role`, sem policy para `authenticated`/`anon`; paths `experiments/{experimentId}/runs/{runId}/output.{png|jpg}`.
- Env vars de UAT: `VENDEO_LAB_ENABLED=true` (apenas local), `VENDEO_LAB_ALLOWED_SUPABASE_HOSTS` (opcional), `OPENAI_API_KEY`/`GEMINI_API_KEY` de **desenvolvimento**. Produção mantém `VENDEO_LAB_ENABLED=false`.
- Gates: `npx vitest run`, `npm run typecheck`, `npm run lint`, `npm run build`.
- Nomenclatura de artefatos da fase (precedente decimal `38.2.1`): `48-1-VERIFICATION.md` e `48.1-UAT.md`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preparar ambiente local, orçamento de UAT e roteiro humano</name>
  <files>scripts/uat/48-local-uat-prep.mjs, .planning/phases/48.1-laboratorio-ia-minimo/48.1-UAT.md</files>
  <read_first>
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/design.md` — "D16 — Estratégia de migration local e remota", "D14 — Segurança financeira e limites", "Decisões consolidadas" (itens 2 e 5) e "Riscos / Trade-offs"
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/tasks.md` — seção 14 (itens 14.1–14.7)
    - `.planning/phases/48.1-laboratorio-ia-minimo/48.1-CONTEXT.md` — D16, D14, D15, bloco "Constantes de limite (LOCKED)" e `<specifics>` (corpus de cenários, prompt sob teste, orçamento de UAT)
    - `scripts/uat/47-local-bootstrap.mjs` — padrão de leitura de credenciais locais e **recusa de remoto antes de qualquer escrita**
    - `scripts/uat/48-local-scenarios.mjs` (48-1-03) — bootstrap idempotente dos cenários a ser invocado
    - `scripts/lab/48-cleanup-artifacts.mjs` (48-1-06) — cleanup manual documentado no roteiro
    - `switch-env.ps1` — entrada oficial de ambiente local
    - `src/lib/lab/environment-guard.ts` (48-1-02) — regras de host/flag replicadas no script
  </read_first>
  <action>
1. Criar `scripts/uat/48-local-uat-prep.mjs` (Node ESM, sem dependência nova; pode usar `@supabase/supabase-js`/`pg` já existentes). Responsabilidades:
   - Ler credenciais locais via `npx supabase status -o env` (mesmo padrão de `scripts/uat/47-local-bootstrap.mjs`).
   - **Recusar remoto antes de qualquer escrita**: exigir `VENDEO_LAB_ENABLED === "true"` e host do Supabase em `localhost`/`127.0.0.1`/`::1`/`0.0.0.0` (ou allowlist), bloqueando incondicionalmente `*.supabase.co`/`.in`/`.com`; em recusa, imprimir o `reason` equivalente (`disabled_flag`/`missing_url`/`non_local_supabase`/`remote_blocked`) e sair com código 1.
   - Verificar o bucket `lab-artifacts` (existência, `public === false`, ausência de policies para `authenticated`/`anon`, policy somente `service_role`).
   - Invocar o bootstrap de cenários (`node scripts/uat/48-local-scenarios.mjs`) e conferir que os 3 cenários (`produto-oferta-preco`, `produto-oferta-texto-obrigatorio`, `produto-oferta-logo`) existem com `content_hash`.
   - Imprimir o **plano de orçamento**: cenários selecionados × 2 variantes × repetições, `max_runs` (≤ 12, default 6), número mínimo de gerações pagas do experimento inicial (≥ 2) e custo estimado por run/cobertura, deixando claro que pricing `partial`/`missing` exige aviso e nunca valor exato.
   - Capturar e imprimir o **snapshot objetivo pré-UAT** (contagens das tabelas produtivas locais, saldo por loja, listagem de objetos de `campaign-images` e hashes SHA-256 de `prompts/*.md`) para registro em `48.1-UAT.md`; a captura equivalente pós-UAT é repetida na Task 2.
   - Não executar nenhuma chamada paga; apenas preparar e reportar.
2. Criar `.planning/phases/48.1-laboratorio-ia-minimo/48.1-UAT.md` com:
   - **Pré-condições**: Docker + Supabase local ativo; `switch-env.ps1 local` aplicado; `VENDEO_LAB_ENABLED=true` em `.env.local`; chave/projeto de **desenvolvimento** dos providers configurada (nunca produção); migration local aplicada (`npx supabase db reset` já executado no 48-1-01); `scripts/uat/48-local-uat-prep.mjs` executado com sucesso.
   - **Orçamento**: cenário(s) × variantes × repetições, teto de runs, custo estimado por run e total do pior caso, e a regra de que pricing parcial/indisponível exige confirmação com aviso (faixa, nunca valor exato).
   - **Roteiro numerado** cobrindo: abrir `/admin/laboratorio`; conferir estado habilitado; criar experimento prompt-only (1 cenário `produto-oferta-preco`, baseline = prompt oficial atual, candidata = pequena alteração no prompt, mesmo modelo/params/imagens, repetições 1); conferir estimativa e budget restante; executar um run por variante com confirmação explícita e progresso NDJSON; abrir a comparação lado a lado (arte assinada, status técnico, custo, latência, alertas); ligar/desligar modo cego; registrar o voto humano com observação; reavaliar e conferir que o histórico é preservado; conferir que nenhuma nota automática aparece; exercitar `scripts/lab/48-cleanup-artifacts.mjs` em modo opt-in e confirmar que run em andamento não é limpo.
   - **Campos de evidência por passo**: `runId`, variante, cenário/versão/hash, prompt (nome/hash/origem), modelo/provider/protocolo, params, latência, custo + cobertura, validação técnica, erro (se houver), verdict + observação + `blind_order`, timestamp.
   - **Evidência objetiva de produção inalterada (antes/depois)**: registrar, no banco **local**, antes e depois da UAT — contagens de `campaigns`, `campaign_art_versions`, `generation_events`, `ai_model_selection`, `ai_model_catalog`, `credit_transactions`, saldo por loja; listagem de objetos do bucket `campaign-images`; e hashes SHA-256 de todos os arquivos `prompts/*.md`. Assertar **delta zero** em cada item (não apenas checklist humano).
   - **Tabela de resultado** (passo × PASS/FAIL × observação) a ser preenchida na Task 2.
   - Deixar explícito que este plano **não** executa deploy e que o push remoto exige a Task 3 (`[BLOCKING]`).
  </action>
  <verify>
    <automated>node scripts/uat/48-local-uat-prep.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/uat/48-local-uat-prep.mjs` existe e recusa remoto: grep do arquivo encontra `supabase.co` e `process.exit(1)`; e encontra `lab-artifacts`.
    - `node scripts/uat/48-local-uat-prep.mjs` sai com código 0 no ambiente local preparado (ou com código 1 e mensagem de recusa explícita quando o ambiente não estiver local — nesse caso registrar o motivo no UAT).
    - `.planning/phases/48.1-laboratorio-ia-minimo/48.1-UAT.md` existe e contém as seções de pré-condições, orçamento, roteiro, evidências, checklist de produção inalterada e tabela de resultado.
    - `Select-String -Path .planning/phases/48.1-laboratorio-ia-minimo/48.1-UAT.md` encontra `orçamento`, `produto-oferta-preco`, `campaign-image-director-offer`, `blind_order` e `VENDEO_LAB_ENABLED`.
    - Nenhuma chamada paga é feita pela task: o script apenas lê, valida e materializa cenários locais.
    - `git status --porcelain src/lib src/app supabase/` → vazio (a task não altera código).
  </acceptance_criteria>
  <done>Script de preparação local validado (flag/host/bucket/cenários) que recusa remoto antes de escrever e imprime o orçamento, mais o roteiro humano completo com orçamento, evidências, checklist de produção inalterada e tabela de resultado — sem nenhuma chamada paga.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Executar o experimento inicial obrigatório com IA real e confirmar produção inalterada</name>
  <files>.planning/phases/48.1-laboratorio-ia-minimo/48.1-UAT.md</files>
  <read_first>
    - `.planning/phases/48.1-laboratorio-ia-minimo/48.1-UAT.md` (Task 1) — roteiro, orçamento e campos de evidência
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/tasks.md` — itens 14.2 e 14.3
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-human-evaluation/spec.md` — requirements "Comparação lado a lado" e "Registro da avaliação humana"
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-runs/spec.md` — requirement "Snapshot imutável por execução" (o que deve estar congelado no run)
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/lab-isolation/spec.md` — requirement "Isolamento absoluto da produção"
    - `.planning/phases/48.1-laboratorio-ia-minimo/48.1-UI-SPEC.md` — "Interaction Contract" (confirmação com estimativa, modo cego, voto humano)
  </read_first>
  <what-built>
Superfície completa do laboratório (UI + API + harness + snapshots + artefatos + avaliação), migration local aplicada, cenários materializados e ambiente local preparado com chave/projeto de desenvolvimento. Falta a execução real obrigatória e a confirmação de que a produção não foi tocada.
  </what-built>
  <action>
Executar o roteiro de `48.1-UAT.md` na ordem definida, **somente no ambiente local** (Supabase Docker + `VENDEO_LAB_ENABLED=true`):
1. Abrir `/admin/laboratorio` e confirmar o estado habilitado e a navegação interna (Experimentos / Cenários / Avaliações) com um único link na nav principal.
2. Criar o experimento inicial obrigatório: cenário `produto-oferta-preco`, baseline = prompt oficial atual (`source: official`), candidata = **pequena alteração** no prompt (`source: override`), **mesmo modelo, mesmos parâmetros e mesmas imagens** nas duas variantes, repetições 1 e teto documentado.
3. Conferir a estimativa (por componente, cobertura `complete|partial|missing`) e o budget restante; registrar o custo estimado.
4. Executar **≥1 geração por variante** com confirmação explícita; registrar por run: `runId`, modelo/provider/protocolo, params, latência, custo + cobertura, validação técnica, erros/alertas e o hash/origem do prompt.
5. Abrir a comparação lado a lado; ligar/desligar o modo cego; registrar o voto humano (`baseline|candidate|tie|none`) + observação + `blind_order`; reavaliar e confirmar que a avaliação anterior permanece (append-only).
6. Confirmar a ausência de qualquer nota/score automático de qualidade em todas as telas.
7. Provar que **a produção permaneceu inalterada** com evidência **objetiva** antes/depois (não apenas checklist): capturar o snapshot pós-UAT (mesmas métricas da Task 1) e assertar **delta zero** em cada item — contagens de `campaigns`, `campaign_art_versions`, `generation_events`, `ai_model_selection`, `ai_model_catalog`, `credit_transactions`, saldo por loja, listagem de `campaign-images` e hashes SHA-256 de `prompts/*.md`. Registrar os pares antes/depois em `48.1-UAT.md`.
8. Preencher a tabela de resultado (PASS/FAIL por passo) e o orçamento realizado em `48.1-UAT.md`.

Registrar todos os desvios encontrados no próprio `48.1-UAT.md`. Esta aprovação encerra **somente** o UAT local; o push remoto é a Task 3 e exige autorização própria. Nenhum deploy é executado nesta fase.
  </action>
  <how-to-verify>
Com o ambiente local ativo e o roteiro executado, confirme que `48.1-UAT.md` contém evidências reais (não placeholders) para cada passo: IDs de run, custo/latência, validação técnica, verdict humano e o checklist de produção inalterada marcado. Digite `approved` somente se o experimento inicial tiver sido executado com o mesmo modelo/params/imagens nas duas variantes e o voto humano registrado.
  </how-to-verify>
  <resume-signal>Digite "approved" para encerrar somente o UAT local, ou descreva os problemas encontrados (run que falhou, custo divergente, isolamento violado, avaliação não registrada). O push remoto exige autorização separada.</resume-signal>
  <acceptance_criteria>
    - `48.1-UAT.md` contém ≥1 run `succeeded` por variante com `runId`, latência e custo registrados, e o mesmo modelo/params/imagens nas duas variantes.
    - A avaliação humana está registrada com verdict, observação, `baseline_run_id`, `candidate_run_id` e `blind_order`, e a reavaliação preservou o registro anterior.
    - A evidência objetiva de produção inalterada está registrada em `48.1-UAT.md` como pares antes/depois (contagens, saldo, listagem de `campaign-images`, hashes SHA-256 de `prompts/*.md`) com **delta zero** em todos os itens.
    - Sem `approved`, o plano permanece bloqueado: a Task 3 (push remoto) não pode iniciar.
    - Nenhum comando remoto foi executado nesta task.
  </acceptance_criteria>
  <done>Experimento inicial obrigatório executado com IA real sob aprovação humana explícita: baseline × candidata com mesmo modelo/params/imagens, comparação lado a lado, voto humano registrado (com reavaliação preservando histórico) e checklist de produção inalterada preenchido em `48.1-UAT.md`.</done>
  <verify>Pausa para execução humana; nenhum comando automatizado.</verify>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: [BLOCKING] Aplicar a migration do laboratório deliberadamente no remoto (schema inerte)</name>
  <files>supabase/migrations/20260915000002_f48_1_create_lab_tables.sql, supabase/migrations/20260915000003_f48_1_lab_immutability_and_reserve.sql, .planning/phases/48.1-laboratorio-ia-minimo/48.1-UAT.md</files>
  <read_first>
    - `.planning/phases/48.1-laboratorio-ia-minimo/48.1-UAT.md` — resultado do UAT local (pré-requisito obrigatório)
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/design.md` — "D16 — Estratégia de migration local e remota" (a migration remota é deliberada, pós-UAT, schema inerte, flag `false` em produção) e "Riscos / Trade-offs"
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/tasks.md` — item 14.4
    - `supabase/migrations/20260915000002_f48_1_create_lab_tables.sql` e `20260915000003_f48_1_lab_immutability_and_reserve.sql` (48-1-01) — conteúdo a aplicar, incluindo o bloco REVERT
    - `supabase/migrations/20260914000001_f47_ai_model_catalog_selection.sql` — migration F47 que **não** pode ser alterada por este push
    - `docs/operations/environment-variables.md` — confirmação de que `VENDEO_LAB_ENABLED` permanece `false` em produção
  </read_first>
  <what-built>
UAT local aprovado com o experimento inicial obrigatório. As duas migrations do laboratório foram criadas e testadas apenas localmente (48-1-01) e nunca foram enviadas ao remoto — deixá-las pendentes faria um `supabase db push` de outra fase arrastá-las sem UAT. Esta task aplica a migration **deliberadamente**, mantendo o schema inerte.
  </what-built>
  <action>
**Somente após o UAT local aprovado (Task 2) E autorização explícita `authorize remote migration`**, executar:
1. `npx supabase db push --dry-run` — revisar a lista: **apenas** as duas migrations `*f48_1*` devem aparecer; se qualquer outra migration (por exemplo a F47 `20260914000001`) aparecer como pendente ou alterada, **parar** e reportar.
2. `npx supabase db push` — aplicar as duas migrations no projeto remoto.
3. Confirmar no remoto: as 8 tabelas `lab_*` existem com RLS habilitada e **sem** grants a `anon`/`authenticated`; o bucket `lab-artifacts` existe privado (`public=false`) com policy somente `service_role` e sem policy para `authenticated`/`anon`; a RPC `lab_reserve_run` existe como `SECURITY DEFINER` com `search_path=''` e `REVOKE`/`GRANT` corretos; os triggers de imutabilidade de snapshot, de congelamento pós-run, de imutabilidade de cenário e de append-only de avaliações existem.
4. Confirmar que `VENDEO_LAB_ENABLED` **não** existe como `true` no ambiente de produção (ou está ausente/`false`), de modo que a superfície do laboratório responda desabilitada/403 em produção e nenhuma tabela `lab_*` seja acessada fora do local.
5. **Não executar deploy** nesta task e **não** reintroduzir env-vars de modelo/provider. Registrar em `48.1-UAT.md` o resultado do `--dry-run`, a lista de migrations aplicadas e as confirmações remotas.
  </action>
  <how-to-verify>
Somente após `approved` (UAT local) + `authorize remote migration`, rode `npx supabase db push --dry-run`, confirme que aparecem **apenas** as duas migrations `*f48_1*`, aplique com `npx supabase db push` e confirme tabelas/RLS/bucket/RPC/triggers no remoto. Confirme `VENDEO_LAB_ENABLED` ausente/`false` em produção. Registre as evidências em `48.1-UAT.md`.
  </how-to-verify>
  <resume-signal>Digite `authorize remote migration` para autorizar somente o push das duas migrations `*f48_1*`, ou descreva o bloqueio/erro. Deploy exigiria autorização separada e não faz parte desta fase.</resume-signal>
  <acceptance_criteria>
    - `npx supabase db push --dry-run` executado e registrado; a lista contém **apenas** as migrations `*f48_1*` (nenhuma outra migration alterada/pendente).
    - As duas migrations `*f48_1*` aplicadas no remoto e as 8 tabelas `lab_*`, o bucket `lab-artifacts`, a RPC `lab_reserve_run` e os 4 triggers confirmados no remoto.
    - `VENDEO_LAB_ENABLED` ausente ou `false` em produção (superfície inerte).
    - Nenhum deploy executado; nenhuma env-var de modelo/provider reintroduzida.
    - Evidências (dry-run, lista aplicada, confirmações remotas) registradas em `48.1-UAT.md`.
    - Sem `authorize remote migration`, nenhum comando remoto é executado.
  </acceptance_criteria>
  <done>As duas migrations `*f48_1*` aplicadas deliberadamente no remoto após `--dry-run` limpo (nenhuma outra migration pendente/alterada), com tabelas `lab_*`, bucket `lab-artifacts`, RPC `lab_reserve_run` e triggers confirmados, schema inerte e `VENDEO_LAB_ENABLED` ausente/`false` em produção — sem deploy e sem reintroduzir env-vars de modelo/provider.</done>
  <verify><automated>npx supabase db push --dry-run</automated></verify>
</task>

<task type="auto">
  <name>Task 4: Verificação goal-backward, critérios da proposta, tracking e arquivamento preparado</name>
  <files>.planning/phases/48.1-laboratorio-ia-minimo/48-1-VERIFICATION.md, .planning/phases/48.1-laboratorio-ia-minimo/48.1-UAT.md, AGENTS.md, .planning/STATE.md, .planning/ROADMAP.md, ROADMAP.md, .planning/PROJECT.md</files>
  <read_first>
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/proposal.md` — "Why", "What Changes", "Capabilities" (9) e "Impact" (critérios a confirmar)
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/tasks.md` — itens 14.5, 14.6 e 14.7
    - `.planning/phases/48.1-laboratorio-ia-minimo/48.1-UAT.md` — resultados do UAT e da migration remota
    - `openspec/changes/fase-48-1-laboratorio-ia-minimo/specs/**` — 9 capabilities para a matriz goal-backward
    - `.planning/phases/47-catalogo-e-selecao-de-modelos-admin/47-VERIFICATION.md` e `47-UAT.md` — formato de verificação/UAT precedente
    - `.planning/STATE.md`, `.planning/ROADMAP.md`, `ROADMAP.md`, `.planning/PROJECT.md`, `AGENTS.md` — tracking a atualizar
  </read_first>
  <action>
1. Gerar `.planning/phases/48.1-laboratorio-ia-minimo/48-1-VERIFICATION.md` no formato goal-backward: para cada uma das 9 capabilities (`lab-isolation`, `lab-scenarios`, `lab-experiments`, `lab-runs`, `lab-gateway-harness`, `lab-artifacts`, `lab-admin-api`, `lab-admin-ui`, `lab-human-evaluation`), listar os requirements da spec, a evidência **automatizada** (arquivo de teste/comando + resultado) e a evidência **humana** (passo do UAT), marcando explicitamente o que é validado por teste e o que depende de aprovação humana. Status global `passed` **somente** com UAT local aprovado, migration remota confirmada e os 4 gates verdes.
2. Completar `48.1-UAT.md` com os resultados finais (UAT + migration remota), mantendo o roteiro, o orçamento e as evidências; distinguir o que foi executado localmente do que foi operação remota.
3. Confirmar os critérios da proposta um a um (item 14.6), registrando em `48-1-VERIFICATION.md`: abrir `/admin/laboratorio`; selecionar cenário; configurar baseline/candidata prompt-only; executar gerações isoladas; comparar lado a lado; registrar avaliação com runs comparados; consultar prompt/modelo/params/custo/latência/erros; nenhum dado produtivo alterado; testes sem chamadas pagas; 4 gates verdes; fluxo completo em Docker local; pontos de extensão F48.2+ preservados (sem implementá-los).
4. Rodar os 4 gates e registrar os resultados reais: `npx vitest run`, `npm run typecheck`, `npm run lint`, `npm run build`; e reexecutar `node scripts/verify/48-1-13-contract-guard.mjs` (base = SHA persistido em `48.1-BASELINE.txt`) para o relatório de não-mudança.
5. Atualizar o tracking (item 14.7): `AGENTS.md`, `.planning/STATE.md`, `.planning/ROADMAP.md`, `ROADMAP.md`, `.planning/PROJECT.md` — F48.1 como concluída com contagem de planos (14), waves (9), contagem de arquivos/testes dos gates, resultado do UAT e da migration remota, preservando todas as entradas históricas (F46/F47). Não reescrever histórico.
6. Preparar o arquivamento do change OpenSpec (`openspec/changes/fase-48-1-laboratorio-ia-minimo/`): registrar no tracking que o arquivamento está **preparado**, **sem** mover/apagar/arquivar o diretório sem instrução explícita do usuário.
  </action>
  <verify>
    <automated>npx vitest run; if ($?) { npm run typecheck }; if ($?) { npm run lint }; if ($?) { npm run build }; if ($?) { node scripts/verify/48-1-13-contract-guard.mjs }</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/48.1-laboratorio-ia-minimo/48-1-VERIFICATION.md` existe, cobre as 9 capabilities e distingue evidência automatizada de validação humana.
    - `48-1-VERIFICATION.md` só marca status global `passed` se o UAT local estiver aprovado e a migration remota confirmada; caso contrário, registra `pending`/bloqueio com o motivo.
    - Os 12 critérios da proposta estão listados um a um com o resultado (confirmado / pendente) em `48-1-VERIFICATION.md`.
    - `npx vitest run`, `npm run typecheck`, `npm run lint` e `npm run build` → exit code 0, com os números reais registrados.
    - `AGENTS.md`, `.planning/STATE.md`, `.planning/ROADMAP.md`, `ROADMAP.md` e `.planning/PROJECT.md` refletem a F48.1 concluída com 14 planos, mantendo as entradas históricas de F46/F47 intactas.
    - O diretório `openspec/changes/fase-48-1-laboratorio-ia-minimo/` permanece no lugar (arquivamento apenas preparado, não executado).
    - `Select-String` de `48.1-UAT.md` e `48-1-VERIFICATION.md` para `approved`/`passed`/`PASS` reflete resultados reais (sem placeholder).
  </acceptance_criteria>
  <done>Verificação goal-backward das 9 capabilities com evidência automatizada × humana, critérios da proposta confirmados um a um, 4 gates verdes com números reais, UAT e migration remota registrados e tracking fechado com o arquivamento preparado (sem mover/apagar o change).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ambiente local → providers de IA | chamadas **pagas reais** disparadas por ação humana durante o UAT |
| ambiente local → Supabase remoto | risco de escrita acidental em produção durante a UAT |
| UAT/evidências → documentação | risco de vazar chave, token ou URL sensível nos registros |
| migrations locais → schema remoto | push remoto altera o banco de produção (deve ser inerte) |
| código implantado → superfície do laboratório em produção | a flag precisa permanecer `false` para a superfície ficar inerte |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-1-106 | Tampering | escrita acidental em produção durante a UAT | mitigate | `scripts/uat/48-local-uat-prep.mjs` recusa host remoto/`*.supabase.co`/`.in`/`.com` **antes de qualquer escrita**; `switch-env.ps1 local` como entrada oficial; checklist de produção inalterada preenchido na Task 2 |
| T-48-1-107 | Information Disclosure | secrets nas evidências de UAT | mitigate | `48.1-UAT.md` registra apenas IDs, hashes, custo, latência e mensagens sanitizadas; proibido colar chave, token, `Authorization` ou URL com credencial; erros vêm de `sanitizeAiErrorMessage` |
| T-48-1-108 | Tampering / financial | gasto descontrolado no UAT | mitigate | Orçamento documentado antes do primeiro run (cenários × variantes × repetições, teto ≤ 12, mínimo ≥ 2 gerações); estimativa com cobertura e aviso obrigatório em pricing parcial; um run por ação explícita com `confirmed: true` |
| T-48-1-109 | Tampering | migration remota | mitigate | `npx supabase db push --dry-run` **antes** do push; dry-run deve listar **apenas** as duas migrations `*f48_1*`; schema remoto inerte; `[BLOCKING]` com autorização explícita `authorize remote migration` |
| T-48-1-110 | Elevation of Privilege | superfície do laboratório em produção | mitigate | `VENDEO_LAB_ENABLED` ausente/`false` em produção confirmado na Task 3; guarda fail-closed devolve estado desabilitado/403; nenhum deploy executado nesta fase |
| T-48-1-111 | Repudiation | evidência de aprovação e de não-mudança | mitigate | UAT com evidências por passo (runId/custo/latência/verdict) + relatório JSON do `48-1-13-contract-guard.mjs`; `48-1-VERIFICATION.md` distingue evidência automatizada de humana e só marca `passed` com UAT + migration remota confirmados |
| T-48-1-112 | Tampering | rollout/escopo indevido | mitigate | Task 3 proíbe deploy e proíbe reintroduzir env-vars de modelo/provider; Task 4 confirma pontos de extensão F48.2+ **preservados e não implementados** |
| T-48-1-113 | Repudiation | histórico de tracking | mitigate | Atualizar AGENTS/STATE/ROADMAP/PROJECT apenas com os dados da F48.1, preservando integralmente as entradas históricas F46/F47; arquivamento apenas preparado, sem mover/apagar |
| T-48-1-SC | Tampering | instalação de dependências | mitigate | Nenhum pacote novo: `@supabase/supabase-js`, `pg`, `node:fs`/`node:child_process` já existem. Instalação nova exigiria auditoria de legitimidade + checkpoint humano bloqueante |
</threat_model>

<verification>
- `node scripts/uat/48-local-uat-prep.mjs` → exit code 0 no ambiente local preparado (ou recusa explícita com o `reason`).
- `npx supabase db push --dry-run` → lista **apenas** as duas migrations `*f48_1*`; push aplicado e schema remoto confirmado (tabelas/RLS/bucket/RPC/triggers).
- `VENDEO_LAB_ENABLED` ausente ou `false` em produção (superfície inerte); nenhum deploy executado.
- `npx vitest run` → exit code 0; `npm run typecheck` → exit code 0; `npm run lint` → exit code 0; `npm run build` → exit code 0.
- `node scripts/verify/48-1-13-contract-guard.mjs` (base = `48.1-BASELINE.txt`) → exit code 0 com listas de violação vazias.
- `.planning/phases/48.1-laboratorio-ia-minimo/48.1-UAT.md` com roteiro, orçamento, resultados, evidências e checklist de produção inalterada preenchidos.
- `.planning/phases/48.1-laboratorio-ia-minimo/48-1-VERIFICATION.md` com as 9 capabilities e os 12 critérios da proposta avaliados.
- `AGENTS.md`, `.planning/STATE.md`, `.planning/ROADMAP.md`, `ROADMAP.md`, `.planning/PROJECT.md` refletem F48.1 concluída (14 planos) sem reescrever o histórico F46/F47.
- `openspec/changes/fase-48-1-laboratorio-ia-minimo/` permanece no lugar (arquivamento preparado, não executado).
</verification>

<success_criteria>
- Ambiente local preparado com Supabase Docker, bucket `lab-artifacts`, cenários materializados e chave/projeto de desenvolvimento, recusando remoto antes de escrever.
- Orçamento de UAT documentado antes da primeira chamada paga e respeitado.
- Experimento inicial obrigatório executado com IA real: baseline = prompt oficial, candidata = pequena alteração, mesmo modelo/params/imagens, ≥1 geração por variante, comparação lado a lado e voto humano com custo/latência registrados.
- Produção comprovadamente inalterada (nenhuma campanha/crédito/seleção/catálogo/prompt/`generation_events`/`campaign-images`).
- Migration aplicada deliberadamente no remoto após a UAT, com `--dry-run` antes, schema inerte e `VENDEO_LAB_ENABLED=false` em produção; nenhum deploy nesta fase.
- `48-1-VERIFICATION.md` goal-backward das 9 capabilities e `48.1-UAT.md` com roteiro, orçamento e resultados.
- 12 critérios da proposta confirmados um a um; 4 gates verdes; tracking fechado com arquivamento preparado.
</success_criteria>

<output>
Create `.planning/phases/48.1-laboratorio-ia-minimo/48-1-14-SUMMARY.md` when done.
</output>

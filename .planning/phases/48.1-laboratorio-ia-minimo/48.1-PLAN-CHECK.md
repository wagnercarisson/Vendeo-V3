# Phase 48.1 — Plan Check (pre-execution, goal-backward) — FINAL RE-VERIFICATION

**Phase:** 48.1 — Laboratório Mínimo de IA
**Plans checked:** 14 (`48-1-01` … `48-1-14`)
**Source of truth:** `openspec/changes/fase-48-1-laboratorio-ia-minimo/` (proposal / design D1–D18 / 9 specs / tasks.md) + `48.1-CONTEXT.md` + `48.1-UI-SPEC.md`
**Date:** 2026-09-15 (final re-verification after the human-review adjustments)
**Method:** goal-backward verification of plans (not code). `gsd-sdk query verify.plan-structure` executed on all 14 plans: **14/14 `valid: true`, 0 errors, 0 warnings**; task counts 3–4.

---

## Overall verdict

**PASS** — **0 blockers**, 0 warnings, 5 info (all prose/environment, none affecting the phase goal).

All **8 human-review adjustments** are present in the current files and independently re-verified by grep/count. The two high-severity defects from the first cycle (false acceptance criterion in the root plan; incomplete contract-guard allowlist) remain resolved, and the three low-impact prose items from the second cycle (N1 self-contradictory scan, N2 count breakdown, N3 frontmatter/prose contradiction) are now **also resolved**. No architectural re-planning is required and no re-plan is needed before execution.

---

## Human review adjustment verification (1–8)

| # | Adjustment | Resolved? | Evidence (independently checked) |
|---|------------|-----------|----------------------------------|
| 1 | Contract guard baseline: capture `HEAD` into `48.1-BASELINE.txt` **before** any code change; 48-1-13 default reads that file, never `HEAD`; no `--base HEAD` in 48-1-13/48-1-14 | **YES** | `48-1-01` L13 lists `48.1-BASELINE.txt` in `files_modified`; Task 1 L111 runs `git rev-parse HEAD > …/48.1-BASELINE.txt` and acceptance L136 asserts a 40-char SHA. `48-1-13` Task 2 item 1 (L150) reads the file when `--base` omitted and forbids `HEAD`; acceptance L166 requires the script to **not** contain `git diff --name-only HEAD`. Grep for `--base HEAD` over phase dir **and** openspec → **0 matches**; all automated commands use bare `node scripts/verify/48-1-13-contract-guard.mjs` (13 L162/L196, 14 L263). `48.1-BASELINE.txt` correctly does not exist yet (created at execution). |
| 2 | Global concurrency `MAX_CONCURRENT_LAB_RUNS = 1`: global partial unique index `uq_lab_runs_one_active_global ((true)) WHERE status IN ('pending','running')` replaces the per-experiment index | **YES** | `one_active_per_experiment` → **0 matches repo-wide**; `one_active_global` present in `48-1-01` (L163 def, L185 acceptance, L287 threat), `48-1-07` (L182), `design.md` (D14 L224, limits table L530, sketch L351–354), `tasks.md` (L17), `48.1-CONTEXT.md` (L86) and specs `lab-runs` (L38/L48–49), `lab-isolation` (L108), `lab-admin-api` (L89–90), `lab-experiments` (L118). Plans 09 (L248) and 11 (L193) state global concurrency ("qualquer experimento", "inclusive em experimento diferente"). No stale "run ativo por experimento" wording anywhere. |
| 3 | UAT objective evidence (counts of productive tables, credit balance, `campaign-images` listing, SHA-256 of `prompts/*.md`) with zero-delta assertion | **YES** | `48-1-14` Task 1 L138 captures the pre-UAT snapshot; Task 2 L185 captures the post-UAT snapshot and asserts **delta zero** per item; acceptance L197 requires the before/after pairs with delta zero; UAT doc spec L145 lists the objective-evidence section explicitly. Replaces the previous checklist-only approach. |
| 4 | F44 untracked file exclusion + path-based staging | **YES** | `48-1-13` Task 3 item 2 (L190) allowlist includes `docs/**` **except** `docs/alinhamento-fase-44-temas-de-campanhas` and mandates `git add <path>` (never `git add -A`); acceptance L167 asserts the file is absent from the phase diff. Confirmed the file exists and is untracked (`?? docs/alinhamento-fase-44-temas-de-campanhas`). |
| 5 | Wave move: 48-1-06 wave 4 → wave 3 (parallel to 05); 48-1-07 stays wave 4; vector `1,1,2,2,3,3,4,5,6,6,7,7,8,9` | **YES** | Extracted from frontmatter: waves = **1,1,2,2,3,3,4,5,6,6,7,7,8,9** ✓. `48-1-06` = `wave: 3` (deps 01,02), `48-1-07` = `wave: 4` (deps 03,04,05,06). `.planning/ROADMAP.md` L1078–L1079 reflects Wave 3/Wave 3 and L1080 Wave 4. Wave-3 `files_modified` are disjoint (05: image-generation-service + `src/lib/lab/gateway|domain` + `src/lib/ai/lab-telemetry-sink`; 06: `src/lib/lab/persistence` + `scripts/lab`). |
| 6 | N3: 48-1-02 `depends_on: []`, genuinely independent; `tasks.md` §2 no longer declares dependency on 48-1-01 | **YES** | `48-1-02` frontmatter L6 `depends_on: []`; objective L60 states it has no technical dependency on 48-1-01 and runs parallel in wave 1 (now consistent). `tasks.md` §2 header (L24) reads "Isolamento e segurança (onda 1)" with no dependency; the dependency list (L9) does not list 48-1-02. |
| 7 | UI-SPEC approved, 6/6 sign-off boxes, `Approval: approved 2026-09-15` | **YES** | `48.1-UI-SPEC.md` frontmatter L4 `status: approved`; L141–L146 all six boxes `[x]` PASS; L148 `**Approval:** approved 2026-09-15 (revisado pelo usuário)`. |
| 8 | CONTEXT `Status: Ready for execution (planned)`; divergence correction synchronized 2026-09-15 | **YES** | `48.1-CONTEXT.md` L4 `**Status:** Ready for execution (planned)`; L9 states the divergence correction was synchronized in the OpenSpec on 2026-09-15 (before execution); DV-1…DV-9 all present (L165–L181). |

---

## Re-run of the broader goal-backward checks

### Requirement coverage (9 capabilities)

All 9 roadmap capability slugs (`.planning/ROADMAP.md` L1257–L1265, declared lowercase per `48.1-CONTEXT.md`) appear in ≥1 plan's `requirements` frontmatter and every spec requirement has implementing tasks.

| Capability | Plans declaring it | Implementing tasks | Status |
|---|---|---|---|
| `lab-isolation` | 01, 02, 11, 13, 14 | 01 T2/T3, 02 T1/T2/T3, 11 T1/T2 | Covered |
| `lab-scenarios` | 01, 03, 11, 13, 14 | 03 T1/T2/T3, 11 T3 | Covered |
| `lab-experiments` | 01, 04, 11, 13, 14 | 04 T1/T2/T3, 11 T3 | Covered |
| `lab-runs` | 01, 05, 07, 12, 13, 14 | 07 T1/T2/T3, 12 T2 | Covered |
| `lab-gateway-harness` | 05, 12, 13, 14 | 05 T1/T2/T3, 12 T1 | Covered |
| `lab-artifacts` | 01, 06, 12, 13, 14 | 06 T1/T2/T3, 12 T3 | Covered |
| `lab-admin-api` | 08, 12, 13, 14 | 08 T1–T4, 12 T3 | Covered |
| `lab-admin-ui` | 09, 10, 12, 13, 14 | 09 T1–T3, 10 T1–T3, 12 T4 | Covered |
| `lab-human-evaluation` | 01, 10, 12, 13, 14 | 01 T3, 10 T2/T3, 12 T4 | Covered |

**PROJECT.md / REQUIREMENTS.md cross-check:** no `REQ-` IDs are mapped to this phase; the phase requirement IDs are the capability slugs above (roadmap L1269: "9 capabilities novas da F48.1 … sem REQ-IDs"). No relevant requirement is silently dropped.

### 1:1 mapping with `tasks.md` sections 1–14

Verified item by item — no section is merged, split or orphaned: 01→§1 (1.1–1.10) · 02→§2 (2.1–2.6) · 03→§3 (3.1–3.5) · 04→§4 (4.1–4.5) · 05→§5 (5.1–5.6) · 06→§6 (6.1–6.4) · 07→§7 (7.1–7.5) · 08→§8 (8.1–8.7) · 09→§9 (9.1–9.7) · 10→§10 (10.1–10.4) · 11→§11 (11.1–11.6) · 12→§12 (12.1–12.6) · 13→§13 (13.1–13.5) · 14→§14 (14.1–14.7).

### Wave vector / dependency graph (DAG acyclic)

Waves = **1,1,2,2,3,3,4,5,6,6,7,7,8,9** ✓ (matches the mandated vector). All `depends_on` references resolve to existing plans; no cycles; no forward references; every wave ≥ max(dep wave)+1. `autonomous: false` only on 48-1-14 (checkpoints) ✓. Same-wave `files_modified` are disjoint (waves 1, 2, 3, 6, 7). Known same-wave dependency `48-1-07 → 48-1-06` is inherited verbatim from the OpenSpec base (info, unchanged).

### Frontmatter / task quality / threat model

- All 14 plans have `phase`, `plan`, `type`, `wave`, `depends_on`, `files_modified`, `autonomous`, `requirements`, `must_haves`. ✓
- Every task has `<read_first>`, `<action>`, `<verify>`, `<acceptance_criteria>`, `<done>` (counts match task counts per plan); actions carry concrete identifiers and file paths. ✓
- `<threat_model>` present exactly once (open/close balanced) in all 14 plans, with plausible STRIDE registers. ✓
- Automated verifies: no `<automated>MISSING</automated>`, no `--watch*`, no full E2E suites (playwright/cypress/selenium); all are fast targeted `vitest` runs plus the 4 gates. 44 automated verifies across 46 tasks (the only task without one is the intentional `checkpoint:human-verify` in 48-1-14 Task 2). ✓

### Isolation fences / D16 / divergence encoding / scope creep

- No task writes to `campaigns`, `campaign_art_versions`, `generation_events`, `ai_model_selection`, `ai_model_catalog` (writes), `admin_audit_log`, official `prompts/` or the `campaign-images` bucket; every mention is a prohibition, an assertion, or the contract guard. ✓
- **D16 order:** only 48-1-14 Task 3 performs a remote push (`[BLOCKING]`, `--dry-run` first, post-UAT, `authorize remote migration`); 48-1-01 explicitly forbids any `supabase db push` (L242/L246/L260/L300). ✓
- **Divergence encoding (DV-1…DV-9):** `imageUnitUsd`/`costPartial` appear only in prohibitions/negative assertions; `resolveAiCost` receives `generationType` + `imageGenerationTool`; `defaultAiModelResolver` imported from `@/lib/ai` via dynamic import; `operationRunType: "campaign_delivery"` with `runType: "lab"` only in the run snapshot; local UI primitives; `fixtures/` and `scripts/lab/` created. `design.md` D14/limits/sketch already reflect the global-index correction. ✓
- **Goal-backward gaps:** environment guard applied to all 7 API routes + all lab pages (layout-level + per-page); atomic `lab_reserve_run` before any paid call; append-only evaluation trigger; signed URLs 3600s; NDJSON run streaming with final `runId`; exact limits; no automatic quality score; single global active run. ✓
- **Scope creep:** none. No F48.2+ feature is implemented (no `model`/`configuration` comparison, no `campaign_image_edit` target, no `lab_prompt_versions`, no multi-evaluator/ranking/statistics, no queue/scheduler, no additional modalities, no `VENDEO_LAB_ALLOW_REMOTE`). All `F48.2` mentions are deferrals/prohibitions. ✓
- **Dimensions 7c / 8 / 11 / 12:** SKIPPED — no responsibility map, no `RESEARCH.md`/`VALIDATION.md`/`PATTERNS.md` (OpenSpec-driven phase). Plans compensate with per-task `<automated>` verifies.

---

## Remaining / new findings

All findings below are **info** and non-blocking; none prevents the phase goal from being achieved and none affects an automated gate.

| ID | Severity | Plan(s) | Finding | Fix hint |
|----|----------|---------|---------|----------|
| I1 | info | 48-1-01 (also 02/03/04/05 acceptance greps) | **`rg` is not resolvable in the current shell** (win32 / PowerShell 5.1: `Get-Command rg` → not found). 48-1-01 Task 1's `<automated>` depends on `rg` and fails closed with a misleading `throw "residuo 48-01- encontrado"` if `rg` is absent. Precedent exists (47-01 used `rg` and F47 closed with 4 green gates), so the execution environment likely provides it. | Optional: harden with `Get-Command rg` guard or fall back to `Select-String` (already used elsewhere in these plans). |
| I2 | info | 48-1-08 | **Densest plan.** 4 tasks / 23 `files_modified` — above the generic ≤15-file scope guidance. Aggregation is deliberate and documented ("os 7 sub-itens da seção 8 do `tasks.md` são cobertos em 4 tasks agrupadas por afinidade"); no requirement is dropped and no single task exceeds 7 files. | Optional: split schemas+estimate from the route wiring if execution shows context pressure. |
| I3 | info | 48-1-08 | **Prose count mismatch.** Objective L106 says "as **6 rotas**" but lists 7 route entries and the plan creates 7 route files (verification L422 asserts 7). | Reword to "as 7 rotas". |
| I4 | info | 48-1-13 | **Guard robustness on untracked files.** The guard diffs with `git diff --name-only <base>`, which does not list untracked files. A stray untracked file outside the allowlist would not be caught unless the phase changes are committed/staged before the guard runs. Frozen tracked files are still protected (their modifications always appear). | Optional: also scan `git ls-files --others --exclude-standard` for the allowlist check. |
| I5 | info | 48-1-09 | **Prose count mismatch.** Objective L90 says "7 componentes locais"; the plan creates 8 local components (`disabled-notice`, `lab-select`, `lab-textarea`, `lab-table`, `lab-radio-group`, `confirm-dialog`, `experiment-form`, `run-execution-panel`). | Reword to "8 componentes locais". |

Resolved since the previous report: **N1** (48-1-11 scan carve-out now present, L297), **N2** (48-1-12 count now correctly "3 deste plano + 5 do 48-1-11 = 8" with the `snapshot-fixtures` filter note, L337), **N3** (48-1-02 frontmatter/prose now consistent and `tasks.md` normalized).

---

## Recommendation

**Proceed to execution.** All 8 human-review adjustments are verified, the phase goal is fully covered by the 14 plans (9/9 capabilities, 1:1 with `tasks.md`, acyclic DAG, disjoint same-wave files, correct wave vector), the isolation fences and the D16 push order are intact, and no scope creep is present. The 5 remaining items are localized prose/environment observations with no effect on the architecture, the DAG, the wave vector, the isolation fences or the D16 order.

No blocker requires a return to the planner. The revision loop has made monotonic progress across cycles (2 high → 0 high → 0 blockers / 0 warnings).

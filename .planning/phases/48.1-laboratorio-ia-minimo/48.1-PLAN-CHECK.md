# Phase 48.1 — Plan Check (pre-execution, goal-backward) — RE-VERIFICATION #6 (confirmation pass)

**Phase:** 48.1 — Laboratório Mínimo de IA
**Plans checked:** 14 (`48-1-01` … `48-1-14`)
**Source of truth:** `openspec/changes/fase-48-1-laboratorio-ia-minimo/` (proposal / design D1–D18 / 9 specs / tasks.md) + `48.1-CONTEXT.md` + `48.1-UI-SPEC.md`
**Date:** 2026-09-15 (confirmation pass after the I1 documentation fix)
**Method:** goal-backward verification of plans (not code). `gsd-sdk query verify.plan-structure` executed on all 14 plans; prior-state spot-checks re-run against the current files.

> This report reflects the **current** file state only. Findings resolved in prior cycles are not re-listed as open issues.

---

## Overall verdict

**PASS WITH WARNINGS** — **0 blockers**, 2 warnings, 0 info.

All 14 plans are execution-ready. The only change since the previous report is the I1 documentation fix in `48.1-UI-SPEC.md` (see below); **no PLAN.md was modified** — `git log` on the phase directory shows the last plan-affecting commit is `020e658c` (fourth-round scope alignment + `lab_create_experiment` traceability), followed only by `b0801c7c` (UI-SPEC archive-copy removal). The previously verified state is intact: every plan is a single well-formed file (1 frontmatter block, 2 delimiters, 3–4 tasks, 1 `<objective>`, 1 `<tasks>`, 1 `<threat_model>`); `verify.plan-structure` returns `valid: true, errors: [], warnings: []` on **14/14**; coverage is 9/9 capabilities, 1:1 with `tasks.md` §§1–14; acyclic DAG with the mandated wave vector `1,1,2,2,3,3,4,5,6,6,7,7,8,9`; isolation fences, D16 push order, `experiment_id` immutability, both RPCs, the 8 triggers and the baseline ASCII/guard-trim handling all remain present. The only residual items are the two advisory scope-density warnings (48-1-08, 48-1-09).

---

## I1 resolution verification (this pass)

| # | Fix (this pass) | Status | Evidence |
|---|---|---|---|
| I1 | **INFO — stale archive copy in `48.1-UI-SPEC.md`** (the Copywriting Contract listed a `Destructive confirmation` row for "Arquivar experimento", a capability explicitly out of the F48.1 API/UI scope) | **RESOLVED** | `48.1-UI-SPEC.md` L121 now reads: "Nenhuma na F48.1 — arquivamento de experimento está fora do escopo (criação + leitura apenas; arquivamento → F48.2)." No `Destructive confirmation` copy remains. This is now consistent with `design.md` L105/L516, `specs/lab-experiments/spec.md` L81, `48-1-04` L218, `48-1-08` L106, `48-1-09` L87 and `48.1-CONTEXT.md` L34. **No plan implements archiving**, so no plan contradicts the UI-SPEC. |

---

## Spot-check of the previously verified state (re-run this pass)

| Item | Status | Evidence |
|---|---|---|
| **No plan duplicated/corrupted** | **INTACT** | 14 files `48-1-01-PLAN.md` … `48-1-14-PLAN.md`; exactly one per plan number, no extras. Frontmatter delimiters = **2** and `<objective>`/`<tasks>`/`<threat_model>` = **1/1/1** for all 14. |
| **14/14 `verify.plan-structure` valid, 3–4 tasks** | **INTACT** | `valid: true, errors: [], warnings: []` on 14/14. Task counts: **3** (01,02,03,04,05,06,07,09,10,11,13) and **4** (08,12,14). Every task has `hasFiles`/`hasAction`/`hasVerify`/`hasDone` = true. |
| **9 capabilities covered** | **INTACT** | All 9 roadmap slugs appear in ≥1 plan's `requirements`: `lab-isolation` (01,02,11,13,14), `lab-scenarios` (01,03,11,13,14), `lab-experiments` (01,04,11,13,14), `lab-runs` (01,05,07,12,13,14), `lab-gateway-harness` (05,12,13,14), `lab-artifacts` (01,06,12,13,14), `lab-admin-api` (08,12,13,14), `lab-admin-ui` (09,10,12,13,14), `lab-human-evaluation` (01,10,12,13,14). No capability dropped. |
| **Wave vector `1,1,2,2,3,3,4,5,6,6,7,7,8,9`** | **INTACT** | Frontmatter waves: 01=1 · 02=1 · 03=2 · 04=2 · 05=3 · 06=3 · 07=4 · 08=5 · 09=6 · 10=6 · 11=7 · 12=7 · 13=8 · 14=9. Matches `.planning/ROADMAP.md` L1074–L1087. |
| **Dependency graph acyclic** | **INTACT** | 01 `[]` · 02 `[]` · 03 `[01,02]` · 04 `[01,02]` · 05 `[03,04]` · 06 `[01,02]` · 07 `[03,04,05,06]` · 08 `[07]` · 09 `[08]` · 10 `[08]` · 11 `[03,04,08]` · 12 `[05,06,07,08,09,10]` · 13 `[08,09,10,11,12]` · 14 `[13]`. All references resolve; no cycles, no forward references; every wave ≥ max(dep wave)+1. `autonomous: false` only on 48-1-14. |
| **D16 push order** | **INTACT** | Only `48-1-14` Task 3 performs the remote push (`[BLOCKING]`, `--dry-run` first, post-UAT, `authorize remote migration`; L217–L240). `48-1-01` explicitly forbids `npx supabase db push` (L256/L260/L275/L315). All other `db push` occurrences are prohibitions/guards/dry-run. |
| **`experiment_id` immutability** | **INTACT** | `48-1-01` L220 (`trg_lab_experiment_variants_freeze_fn`) and L223 (`trg_lab_experiment_scenarios_freeze_fn`) reject `NEW.experiment_id IS DISTINCT FROM OLD.experiment_id` and check **both** `NEW`/`OLD.experiment_id` for existing runs; local SQL test **(k)** (L254) covers moving an association into a frozen experiment. |
| **Both RPCs** | **INTACT** | `lab_reserve_run` (48-1-01 L229–L246, 48-1-07 L119/L199, 48-1-11 L177, 48-1-14 L223) and `lab_create_experiment` (48-1-01 L248–L250 artifact/objective/key_links/threats T-48-1-07/T-48-1-08, 48-1-04 L60/L216, 48-1-11 L177/L180, 48-1-14 L223) — both `SECURITY DEFINER`, `SET search_path = ''`, `REVOKE`/`GRANT` to `service_role`. |
| **8 immutability/append-only triggers** | **INTACT** | `48-1-01` Task 3 items 1–8 (L218–L225) = 8 functions; acceptance L264 (`CREATE OR REPLACE FUNCTION public.trg_lab_` = 8) / L265 (`CREATE TRIGGER` = 8); remote confirmation in `48-1-14` L223. |
| **Baseline ASCII + guard trim/SHA** | **INTACT** | `48-1-01` L118 writes `48.1-BASELINE.txt` with `Set-Content -Encoding ascii -NoNewline` (never `>`/UTF-16LE); L143 asserts a 40-char SHA. `48-1-13` L150 reads that file when `--base` is omitted, applies `trim()` and validates `^[0-9a-f]{40}$`, never defaulting to `HEAD` (L166–L167). |
| **OpenSpec source of truth intact** | **INTACT** | `proposal.md`, `design.md`, `tasks.md` + 9 spec dirs (`lab-isolation`, `lab-scenarios`, `lab-experiments`, `lab-runs`, `lab-gateway-harness`, `lab-artifacts`, `lab-admin-api`, `lab-admin-ui`, `lab-human-evaluation`) present. `design.md` L105/L516 and `specs/lab-experiments/spec.md` L81 carry the creation+read-only scope alignment. |

---

## Re-run of the broader goal-backward checks

### Requirement coverage (9 capabilities)

All 9 roadmap capability slugs (`.planning/ROADMAP.md` L1066) appear in ≥1 plan's `requirements` frontmatter; none is dropped.

**PROJECT.md / REQUIREMENTS.md cross-check:** no `REQ-` IDs map to this phase (roadmap L1257–L1265: 9 new capabilities, no REQ-IDs). No relevant requirement is silently dropped.

### 1:1 mapping with `tasks.md` §§1–14

Verified item by item — no section merged, split or orphaned: 01→§1 · 02→§2 · 03→§3 · 04→§4 · 05→§5 · 06→§6 · 07→§7 · 08→§8 · 09→§9 · 10→§10 · 11→§11 · 12→§12 · 13→§13 · 14→§14.

### Frontmatter / task quality / threat model

- **14/14** plans have a single well-formed frontmatter block (`phase`, `plan`, `type`, `wave`, `depends_on`, `files_modified`, `autonomous`, `requirements`, `must_haves`).
- **14/14** tasks carry `<read_first>` + `<action>` + `<verify>` + `<acceptance_criteria>` + `<done>`; counts match task counts exactly. Actions carry concrete identifiers, file paths and exact signatures.
- `<threat_model>` present exactly once (balanced open/close) in **14/14**, each with a plausible STRIDE register and the supply-chain `T-48-1-SC` row.
- Automated verifies: **0** `<automated>MISSING</automated>`, no `--watch*`, no full E2E suites. `<automated>` count equals task count in 13/14 plans; 48-1-14 has 3 `<automated>` for 4 tasks because Task 2 is a `checkpoint:human-verify` (by design). `48-1-13` Task 1 intentionally runs the full `npx vitest run` (phase-closing regression).

### Isolation fences / divergence encoding / scope creep

- No task writes to `campaigns`, `campaign_art_versions`, `generation_events`, `ai_model_selection`, `ai_model_catalog` (read-only: `.select()` only), `admin_audit_log`, official `prompts/` or the `campaign-images` bucket; every mention is a prohibition, assertion or contract guard.
- **Divergence encoding (DV-1…DV-9):** `imageUnitUsd`/`costPartial` appear only in prohibitions/negative assertions; `resolveAiCost` receives `generationType` + `imageGenerationTool`; `defaultAiModelResolver` imported via `@/lib/ai`; `operationRunType: "campaign_delivery"` with `runType: "lab"` only in the run snapshot; local UI primitives; `fixtures/` and `scripts/lab/` created. ✓
- **Scope creep / scope reduction:** none. All `F48.2` mentions are deferrals/prohibitions; `campaign_image_edit` appears only in prohibitions/negative assertions; the scope-reduction language scan returned only false positives.
- **Dimensions 7c / 8 / 11 / 12:** SKIPPED — no responsibility map, no `RESEARCH.md`/`VALIDATION.md`/`PATTERNS.md` (OpenSpec-driven phase). Plans compensate with per-task `<automated>` verifies.
- **Dimension 10 (AGENTS.md):** PASS — plans respect the GSD workflow and project conventions; no forbidden pattern or skipped required step detected.

---

## Remaining findings

> No blockers remain. The open items are the two advisory plan scope-density warnings. I1 is resolved and no longer carried forward.

### Blockers

**None.**

### Warnings

| ID | Severity | Plan | Finding | Fix hint |
|----|----------|------|---------|----------|
| W2 | warning | 48-1-08 | **Scope density.** 4 tasks / **23** `files_modified` — above the generic ≤15-file guidance. Aggregation is deliberate and documented (the 7 sub-items of `tasks.md` §8 are covered in 4 tasks grouped by affinity); no requirement is dropped and no single task exceeds 7 files. Context pressure at execution is the residual risk. | Optional: split schemas+estimate from route wiring if execution shows context pressure. |
| W3 | warning | 48-1-09 | **Scope density (same class as W2).** 3 tasks / **17** `files_modified` — above the ≤15-file guidance; per-task max is 7 files. | Optional: split local primitives (Task 1) from pages/execution panel. |

### Info

**None.** (I1 resolved this pass.)

---

## Structured issues

```yaml
issues:
  - plan: "48-1-08"
    dimension: "scope_sanity"
    severity: "warning"
    description: "4 tasks / 23 files_modified exceeds the generic <=15-file guidance; aggregation documented but context pressure is a residual risk (per-task max 7 files)."
    fix_hint: "Optionally split schemas+estimate from route wiring."
  - plan: "48-1-09"
    dimension: "scope_sanity"
    severity: "warning"
    description: "3 tasks / 17 files_modified exceeds the generic <=15-file guidance; per-task max 7 files."
    fix_hint: "Optionally split local primitives (Task 1) from pages/execution panel."
```

---

## Recommendation

**Proceed to execution.** The I1 documentation fix is present and correct (`48.1-UI-SPEC.md` L121 no longer advertises an archive destructive-confirmation), and the previously verified state is fully intact: 14/14 `verify.plan-structure` valid with 3–4 tasks each, 9/9 capabilities covered, 1:1 with `tasks.md`, acyclic DAG with the mandated wave vector `1,1,2,2,3,3,4,5,6,6,7,7,8,9`, `experiment_id` immutability, both RPCs, the 8 triggers, baseline ASCII + guard trim/SHA, intact isolation fences and correct D16 push order.

The remaining warnings (scope density on 48-1-08 and 48-1-09) are advisory and do **not** block execution; the planner may apply them opportunistically. This closes the revision loop with **no blockers and no info items**.

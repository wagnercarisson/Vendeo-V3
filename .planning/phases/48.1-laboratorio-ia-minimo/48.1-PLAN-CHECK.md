# Phase 48.1 — Plan Check (pre-execution, goal-backward) — RE-VERIFICATION

**Phase:** 48.1 — Laboratório Mínimo de IA
**Plans checked:** 14 (`48-1-01` … `48-1-14`)
**Source of truth:** `openspec/changes/fase-48-1-laboratorio-ia-minimo/` (proposal / design D1–D18 / 9 specs / tasks.md) + `48.1-CONTEXT.md` + `48.1-UI-SPEC.md`
**Date:** 2026-09-15 (re-verification of the fixes applied after the first review)
**Method:** goal-backward verification of plans (not code). `gsd-sdk query verify.plan-structure` executed on all 14 plans: **14/14 `valid: true`, 0 errors, 0 warnings**; task counts 3–4.

---

## Overall verdict

**PASS WITH WARNINGS** — **0 blockers**, 0 high, 2 warnings (both low-impact prose), 3 info.

All nine previously reported findings (F1–F9) are present in the current files and correct at the level that matters (acceptance criteria, automated verifies, guard allowlists, manifest completeness). The two **high** defects that caused the previous FAIL (F1 false acceptance criterion in the root plan; F2 incomplete contract-guard allowlist) are **fully resolved and independently re-verified by counting/grep**. F7 is **partially** resolved: the number is now correct but its parenthetical breakdown is still inaccurate (new N2).

No architectural re-planning is required. The remaining items are localized prose/verification inaccuracies that do not prevent the phase goal from being achieved and do not affect any automated gate.

---

## Fix verification (F1–F9)

| ID | Prev. sev | Status | Evidence in current files |
|----|-----------|--------|---------------------------|
| F1 | high | **RESOLVED** | `48-1-01-PLAN.md` Task 1 now asserts (action L116, `<automated>` L123, acceptance L129–L130): `.planning/STATE.md` = 14 rows `^| 48-1-[0-9]{2} |`, `AGENTS.md` = 14 rows, `.planning/ROADMAP.md` = 14 occurrences of `48-1-[0-9]{2}-PLAN.md`. **Counted independently:** STATE.md = 14, AGENTS.md = 14, `.planning/ROADMAP.md` = 14. Root `ROADMAP.md` = 0 `48-1-XX-PLAN.md` bullets and is correctly **no longer** asserted as a table (it does contain F48.1 at L225/L292, so the Task 1 human-check is satisfiable). |
| F2 | high | **RESOLVED** | `48-1-13-PLAN.md` Task 3 item 2 (L188) allowlist now includes `src/lib/ai/lab-*.ts`, `src/lib/ai/__tests__/lab-*.test.ts`, `src/lib/image-generation/services/__tests__/image-generation-service*.test.ts`, `src/app/api/campaign/generate-image/__tests__/route.test.ts`. **Cross-checked** against the union of `files_modified` of all 14 plans (151 entries): 100% covered, including `lab-cost-estimate.ts`, `lab-cost-estimate.test.ts`, `lab-telemetry-sink.test.ts`, `image-generation-service.build-director-prompt.test.ts`, `image-generation-service.test.ts` and `route.test.ts`. |
| F3 | medium | **RESOLVED** | `48-1-03-PLAN.md`: `fixtures/lab/scenarios/produto-oferta-preco/images/produto-auxiliar.jpg` present in `files_modified` (L18) **and** in the Task 2 `<files>` list (L172). |
| F4 | medium | **RESOLVED (main)** | `48-1-11-PLAN.md` Task 2 item 7 (L196) now carries an explicit carve-out: "comentários e asserções negativas de teste que apenas provam a **não** utilização do provider são permitidos; usar `stripComments` equivalente local". The 48-1-12 harness test can therefore reference `campaign_image_edit` (negative assertion) without tripping the scan. Residual prose inconsistency → **N1**. |
| F5 | low | **RESOLVED** | `48-1-02-PLAN.md` frontmatter now `depends_on: [48-1-01]` (L6–L7), consistent with `tasks.md` L24 ("onda 1, depende de 48-1-01"). Residual prose contradiction in the objective → **N3**. |
| F6 | low | **RESOLVED** | `tasks.md` item 3.2 now lists `produto-oferta-logo` (L36). |
| F7 | low | **PARTIALLY RESOLVED** | 48-1-09: fixed — `<verification>` L306 now says "4 páginas (`page.tsx`, `cenarios/page.tsx`, `experimentos/novo/page.tsx`, `experimentos/[id]/page.tsx`) mais o `layout.tsx`". 48-1-12: total "8" is arithmetically correct for `Get-ChildItem -Recurse -Path src/lib/lab -Filter "lab-*.contract.test.ts"` (3 of this plan + 5 of 48-1-11), but the parenthetical breakdown is wrong → **N2**. |
| F8 | low | **RESOLVED** | `48.1-UI-SPEC.md` L29 documents the `tabs` omission with rationale ("omitido na 48-1-09 … promover apenas se houver reuso real"); `48-1-09-PLAN.md` L93 records the same. |
| F9 | low | **RESOLVED** | `tasks.md` item 14.5 (L132) now uses `48-1-VERIFICATION.md` / `48.1-UAT.md`, matching `48-1-14-PLAN.md` (`files_modified` L9–L10, objective L70, artifacts L42/L45). |

---

## Remaining / new findings

| ID | Severity | Plan(s) | Finding | Fix hint |
|----|----------|---------|---------|----------|
| N1 | **warning** | 48-1-11 | **Self-contradictory verification prose (residual of F4).** `<verification>` L297 states: "Varredura de referências proibidas nos arquivos criados: `Select-String` para `new OpenAI(|GoogleGenerativeAI|api.openai.com|generativelanguage.googleapis.com|OpenAIImageProvider` → 0 ocorrências". But two files created by this very plan necessarily contain those literals as scan patterns: `lab-financial-safety.contract.test.ts` (contains `OpenAIImageProvider`, `@/lib/image-generation/providers/openai`) and `lab-isolation.contract.test.ts` (contains `generation_events`, `campaign-images`). The bullet can never pass as written. It is prose in `<verification>` (not an `<automated>` gate), so it does not block execution. | Scope the bullet to production files (exclude `__tests__`/`*.test.*`) or apply the same carve-out used in Task 2 item 7. |
| N2 | **warning** | 48-1-12 | **Incorrect count breakdown (residual of F7).** `<verification>` L337 says "8 arquivos (4 `src/lib/lab` deste plano: gateway-harness, runs, artifacts, snapshot-fixtures + 4 do 48-1-11)". The total 8 is right, but: (a) `snapshot-fixtures.contract.test.ts` does **not** match the filter `lab-*.contract.test.ts`, so this plan contributes **3**, not 4; (b) 48-1-11 contributes **5** matching files (`lab-environment-guard`, `lab-isolation`, `lab-financial-safety`, `lab-scenarios`, `lab-experiments`), not 4. 3 + 5 = 8. | Rewrite as "3 deste plano (gateway-harness, runs, artifacts) + 5 do 48-1-11 = 8; `snapshot-fixtures.contract.test.ts` não casa o filtro `lab-*`". |
| N3 | info | 48-1-02 | **Prose contradicts frontmatter (introduced by the F5 fix).** Frontmatter declares `depends_on: [48-1-01]`, but the objective note (L61) still states the dependency is "de rastreabilidade, não técnica" and that "esta onda executa 48-1-01 e 48-1-02 em paralelo". No functional impact (no shared files; `48-1-01` is migration/tracking, `48-1-02` is `src/lib/lab`), but the plan is internally inconsistent and wave 1 is no longer strictly parallel. | Align the prose with the frontmatter (48-1-02 runs after 48-1-01), or revert the frontmatter to `[]` and normalize `tasks.md`. |
| N4 | info | 48-1-01 | **`rg` is not resolvable in the current shell** (win32 / PowerShell 5.1: `where.exe rg` → not found; no ripgrep under `%LOCALAPPDATA%\Programs`). Task 1's `<automated>` depends on `rg` and fails closed with a misleading `throw "residuo 48-01- encontrado"` if `rg` is absent. Precedent exists (47-01 used `rg` in `<automated>` and F47 closed with 4 green gates), so the execution environment likely provides it. Non-blocking. | Harden with `Select-String` (already used elsewhere in these plans) or a `Get-Command rg` guard. |
| N5 | info | 48-1-08 | **Densest plan.** 4 tasks / 23 `files_modified` — above the generic ≤15-file scope guidance. Aggregation is deliberate and documented ("os 7 sub-itens da seção 8 do `tasks.md` são cobertos em 4 tasks agrupadas por afinidade") and no requirement is dropped. Flagged for awareness only; the previous review assessed scope as within budget and no new evidence changes that. | Optional: split schemas+estimate from the route wiring if execution shows context pressure. |

---

## Re-run of the broader goal-backward checks

### Requirement coverage (9 capabilities)

All 9 roadmap capability slugs (`LAB-ISOLATION` … `LAB-HUMAN-EVALUATION` in `.planning/ROADMAP.md` L1257–L1265; declared lowercase per `48.1-CONTEXT.md` L66) appear in ≥1 plan's `requirements` frontmatter, and every spec requirement has implementing tasks.

| Capability | Plans declaring it | Implementing tasks | Status |
|---|---|---|---|
| `lab-isolation` | 01, 02, 11, 13, 14 | 01 T2/T3, 02 T1/T2/T3, 11 T1/T2 | Covered |
| `lab-scenarios` | 01, 03, 11, 13, 14 | 03 T1/T2/T3, 11 T3 | Covered |
| `lab-experiments` | 01, 04, 11, 13, 14 | 04 T1/T2/T3, 11 T3 | Covered |
| `lab-runs` | 01, 05, 07, 12, 13, 14 | 07 T1/T2/T3, 12 T2 | Covered |
| `lab-gateway-harness` | 05, 12, 13, 14 | 05 T1/T2/T3, 12 T1 | Covered |
| `lab-artifacts` | 01, 06, 12, 13, 14 | 06 T1/T2/T3, 12 T3 | Covered |
| `lab-admin-api` | 08, 12, 13, 14 | 08 T1–T4, 12 T3 | Covered |
| `lab-admin-ui` | 09, 10, 12, 13, 14 | 09 T1–T3, 10 T1–T3, 12 T4 | Covered |
| `lab-human-evaluation` | 01, 10, 12, 13, 14 | 01 T3, 10 T2/T3, 12 T4 | Covered |

**PROJECT.md / REQUIREMENTS.md cross-check:** `.planning/REQUIREMENTS.md` contains no `REQ-` IDs mapped to this phase (grep for `48.1|lab-|laborat` → 0 hits); the phase's requirement IDs live in `.planning/ROADMAP.md` as capability slugs. No relevant requirement is silently dropped.

### 1:1 mapping with `tasks.md` sections 1–14

Verified item by item — no plan merges or splits a section and no `tasks.md` item is orphaned:

- 01→§1 (1.1–1.10) · 02→§2 (2.1–2.6; 2.2 application explicitly delegated to 08/09 and documented) · 03→§3 (3.1–3.5) · 04→§4 (4.1–4.5) · 05→§5 (5.1–5.6) · 06→§6 (6.1–6.4) · 07→§7 (7.1–7.5) · 08→§8 (8.1–8.7) · 09→§9 (9.1–9.7) · 10→§10 (10.1–10.4) · 11→§11 (11.1–11.6) · 12→§12 (12.1–12.6) · 13→§13 (13.1–13.5) · 14→§14 (14.1–14.7).

### Wave vector / dependency graph

Extracted from frontmatter: waves = **1,1,2,2,3,4,4,5,6,6,7,7,8,9** ✓ (matches the mandated vector). All `depends_on` references resolve to existing plans; no cycles; no forward references. `autonomous: false` only on 48-1-14 (checkpoints) ✓. Same-wave `files_modified` are disjoint (wave 1, 2, 4, 6, 7). Known same-wave dependencies: `48-1-07 → 48-1-06` (inherited verbatim from `ROADMAP.md`/`tasks.md`, info) and now `48-1-02 → 48-1-01` (see N3).

### Frontmatter / task quality / threat model

- All 14 plans have `phase`, `plan`, `type`, `wave`, `depends_on`, `files_modified`, `autonomous`, `requirements`, `must_haves`. ✓
- Every task has `<read_first>`, `<action>`, `<verify>`, `<acceptance_criteria>`, `<done>`; no fenced code blocks; actions carry concrete identifiers and file paths. ✓
- `<threat_model>` present exactly once (open/close balanced) in all 14 plans, with plausible STRIDE registers. ✓
- Automated verifies: no `MISSING`, no `--watch*`, no full E2E suites (playwright/cypress/selenium); all are fast targeted `vitest` runs plus the 4 gates. ✓

### Isolation fences / D16 / divergence encoding / scope creep

- No task writes to `campaigns`, `campaign_art_versions`, `generation_events`, `ai_model_selection`, `ai_model_catalog` (writes), `admin_audit_log`, official `prompts/` or the `campaign-images` bucket; every mention is a prohibition, an assertion, or the contract guard. ✓
- **D16:** only 48-1-14 Task 3 performs a remote push (`[BLOCKING]`, `--dry-run` first, post-UAT); 48-1-01 explicitly forbids any `supabase db push`. ✓
- **Divergence encoding (DV-1…DV-9):** `imageUnitUsd`/`costPartial` appear only in prohibitions/negative assertions; `resolveAiCost` receives `generationType` + `imageGenerationTool`; `defaultAiModelResolver` imported from `@/lib/ai` via dynamic import; `operationRunType: "campaign_delivery"` with `runType: "lab"` only in the run snapshot; local UI primitives; `fixtures/` and `scripts/lab/` created. `design.md` L122/L156 already reflects the DV-1 resolution (real `CostResolution` fields only). ✓
- **Goal-backward gaps:** environment guard applied to all 7 API routes + all lab pages (layout-level + per-page); atomic `lab_reserve_run` before any paid call; append-only evaluation trigger; signed URLs 3600s; NDJSON run streaming with final `runId`; exact limits; no automatic quality score. ✓
- **Scope creep:** none. No F48.2+ feature is implemented (no `model`/`configuration` comparison, no `campaign_image_edit` target, no `lab_prompt_versions`, no multi-evaluator/ranking/statistics, no queue/scheduler, no additional modalities, no `VENDEO_LAB_ALLOW_REMOTE`). Deferred ideas excluded. ✓
- **Dimensions 7c / 8 / 12:** SKIPPED — no responsibility map, no `RESEARCH.md`/`VALIDATION.md` (OpenSpec-driven phase, `workflow.research: false`), no `PATTERNS.md`. Plans compensate with per-task `<automated>` verifies.

---

## Recommendation

**Proceed to execution.** The two high-severity blockers from the previous review are resolved and independently re-verified; the plan set is coherent, complete and faithful to the OpenSpec base. Before or during execution (no re-plan needed), correct the three low-impact prose items **N1**, **N2** and **N3** — they are localized edits to `<verification>`/objective text and to `tasks.md`, with no effect on the architecture, the DAG, the wave vector, the isolation fences or the D16 order.

No blocker requires a return to the planner. This is the first review cycle that clears the high-severity defects, so the revision loop has made monotonic progress (2 high → 0 high).

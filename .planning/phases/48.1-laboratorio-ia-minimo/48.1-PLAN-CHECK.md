# Phase 48.1 — Plan Check (pre-execution, goal-backward) — RE-VERIFICATION #4 (final)

**Phase:** 48.1 — Laboratório Mínimo de IA
**Plans checked:** 14 (`48-1-01` … `48-1-14`)
**Source of truth:** `openspec/changes/fase-48-1-laboratorio-ia-minimo/` (proposal / design D1–D18 / 9 specs / tasks.md) + `48.1-CONTEXT.md` + `48.1-UI-SPEC.md`
**Date:** 2026-09-15 (iteration 4/4 — after the third human-review round)
**Method:** goal-backward verification of plans (not code). `gsd-sdk query verify.plan-structure` executed on all 14 plans.

> This report reflects the **current** file state only. Findings resolved in prior cycles are not re-listed as open issues.

---

## Overall verdict

**PASS WITH WARNINGS** — **0 blockers**, 2 warnings, 0 info.

All 14 plans are execution-ready. The four third-round fixes are present and correct. Every plan is a single well-formed file (1 frontmatter block, 2 delimiters, 3–4 tasks, 1 `<objective>`, 1 `<tasks>`, 1 `<threat_model>`); `verify.plan-structure` returns `valid: true, errors: [], warnings: []` on **14/14**. Coverage is 9/9 capabilities, 1:1 with `tasks.md` §§1–14, acyclic DAG with the mandated wave vector `1,1,2,2,3,3,4,5,6,6,7,7,8,9`, intact isolation fences, correct D16 push order and no scope creep. The only residual items are the two advisory scope-density warnings (48-1-08, 48-1-09).

---

## Third-round fix verification

| # | Fix (third round) | Status | Evidence |
|---|---|---|---|
| 1 | **HIGH — freezing bypass via `experiment_id` swap** | **RESOLVED** | `48-1-01` Task 3 item 3 (`trg_lab_experiment_variants_freeze_fn`) and item 6 (`trg_lab_experiment_scenarios_freeze_fn`) now (a) make `experiment_id` immutable on UPDATE (`NEW.experiment_id IS DISTINCT FROM OLD.experiment_id` → `lab_experiment_frozen`) and (b) check **both** `NEW.experiment_id` (INSERT; UPDATE) and `OLD.experiment_id` (UPDATE; DELETE) for existing runs (L216, L219). Local SQL test item **(k)** added: moving a variant and a scenario association from a run-free experiment to a frozen one via `UPDATE ... SET experiment_id = ...` raises `lab_experiment_frozen` (L250). `design.md` D5 L106 now states `experiment_id` SHALL be immutable and both `OLD`/`NEW` SHALL be considered; `specs/lab-experiments/spec.md` gained the scenario **"Associação não pode ser movida para um experimento congelado"** (L130–L133). |
| 2 | **MEDIUM — isolation allowlist** | **RESOLVED** | `48-1-11` Task 2 allowlist now permits RPCs `lab_reserve_run` **and** `lab_create_experiment` (L177); case 1 exercises `createExperiment` through the recording client (`createExperiment` via RPC `lab_create_experiment`) and asserts the `accessLog` contains only allowlisted targets including that RPC (L180). |
| 3 | **LOW — remote verification** | **RESOLVED** | `48-1-14` Task 3 action step 3 now confirms **both** RPCs (`lab_reserve_run`/`lab_create_experiment`) as `SECURITY DEFINER` with `search_path=''` and correct `REVOKE`/`GRANT`, **and** the 8 immutability triggers (L223). `how-to-verify` (L228) and `<done>` (L239) both state the two RPCs + 8 triggers. |
| 4 | **INFO — `rg`** | **APPLIED** | The "rg unresolvable" note is dropped from this report per operator confirmation (`rg 15.2.0` available in the execution shell; F47 precedent used `rg` and closed with 4 green gates). Not carried as an open finding. |

No plan was duplicated or corrupted by the third-round edits: frontmatter delimiters = **2** for all 14 plans; `<objective>`/`<tasks>`/`</tasks>`/`<threat_model>` markers = **1/1/1/1** for all 14 plans; task counts are 3 or 4 (3 tasks: 01,02,03,04,05,06,07,09,10,11,13; 4 tasks: 08,12,14).

---

## Re-run of the broader goal-backward checks

### Requirement coverage (9 capabilities)

All 9 roadmap capability slugs (`.planning/ROADMAP.md` L1066) appear in ≥1 plan's `requirements` frontmatter; none is dropped.

| Capability | Plans declaring it | Status |
|---|---|---|
| `lab-isolation` | 01, 02, 11, 13, 14 | Covered |
| `lab-scenarios` | 01, 03, 11, 13, 14 | Covered |
| `lab-experiments` | 01, 04, 11, 13, 14 | Covered |
| `lab-runs` | 01, 05, 07, 12, 13, 14 | Covered |
| `lab-gateway-harness` | 05, 12, 13, 14 | Covered |
| `lab-artifacts` | 01, 06, 12, 13, 14 | Covered |
| `lab-admin-api` | 08, 12, 13, 14 | Covered |
| `lab-admin-ui` | 09, 10, 12, 13, 14 | Covered |
| `lab-human-evaluation` | 01, 10, 12, 13, 14 | Covered |

**PROJECT.md / REQUIREMENTS.md cross-check:** no `REQ-` IDs map to this phase (roadmap L1269: 9 new capabilities, no REQ-IDs). No relevant requirement is silently dropped.

### 1:1 mapping with `tasks.md` §§1–14

Verified item by item — no section merged, split or orphaned: 01→§1 · 02→§2 · 03→§3 · 04→§4 · 05→§5 · 06→§6 · 07→§7 · 08→§8 · 09→§9 · 10→§10 · 11→§11 · 12→§12 · 13→§13 · 14→§14.

### Dependency graph (acyclic) + wave vector

`depends_on` (from frontmatter): 01 `[]` · 02 `[]` · 03 `[01,02]` · 04 `[01,02]` · 05 `[03,04]` · 06 `[01,02]` · 07 `[03,04,05,06]` · 08 `[07]` · 09 `[08]` · 10 `[08]` · 11 `[03,04,08]` · 12 `[05,06,07,08,09,10]` · 13 `[08,09,10,11,12]` · 14 `[13]`.

- All references resolve to existing plans; **no cycles**; **no forward references**; every plan's wave ≥ max(dep wave)+1.
- Waves = **1,1,2,2,3,3,4,5,6,6,7,7,8,9** ✓ (matches `.planning/ROADMAP.md` L1074–L1087). Note: 48-1-06 sits at wave 3 while its deps are wave 1 (safe, deliberately delayed to keep same-wave `files_modified` disjoint).
- Same-wave `files_modified` are disjoint (waves 1, 2, 3, 6, 7).
- `autonomous: false` only on 48-1-14 (human checkpoints) ✓.

### Frontmatter / task quality / threat model

- **14/14** plans have a single well-formed frontmatter block (`phase`, `plan`, `type`, `wave`, `depends_on`, `files_modified`, `autonomous`, `requirements`, `must_haves`).
- **14/14** tasks carry `<read_first>` + `<action>` + `<verify>` + `<acceptance_criteria>` + `<done>`; counts match task counts exactly. Actions carry concrete identifiers, file paths and exact signatures.
- `<threat_model>` present exactly once (balanced open/close) in **14/14**, each with a plausible STRIDE register and the supply-chain `T-48-1-SC` row.
- Automated verifies: no `<automated>MISSING</automated>`, no `--watch*`, no full E2E suites (playwright/cypress/selenium). `48-1-13` Task 1 intentionally runs the full `npx vitest run` (phase-closing regression) — appropriate.

### Isolation fences / D16 / divergence encoding / scope creep

- No task writes to `campaigns`, `campaign_art_versions`, `generation_events`, `ai_model_selection`, `ai_model_catalog` (read-only: `.select()` only — 48-1-04 L174, 48-1-08 L239/L411, 48-1-12 L157), `admin_audit_log`, official `prompts/` or the `campaign-images` bucket; every mention is a prohibition, assertion or contract guard (verified by scan across all 14 plans).
- **D16 order:** only `48-1-14` Task 3 performs a remote push (`[BLOCKING]`, `--dry-run` first, post-UAT, `authorize remote migration`); `48-1-01` explicitly forbids any `supabase db push` (L252) and all `db push` occurrences are prohibitions/guards except the deliberate push in 48-1-14. ✓
- **Divergence encoding (DV-1…DV-9):** `imageUnitUsd`/`costPartial` appear only in prohibitions/negative assertions (48-1-05 L258, 48-1-12 L201); `resolveAiCost` receives `generationType` + `imageGenerationTool`; `defaultAiModelResolver` imported via dynamic import `@/lib/ai`; `operationRunType: "campaign_delivery"` with `runType: "lab"` only in the run snapshot; local UI primitives; `fixtures/` and `scripts/lab/` created. ✓
- **Scope creep / scope reduction:** none. All `F48.2` mentions (48-1-01, 48-1-04, 48-1-09, 48-1-14) are deferrals/prohibitions; `campaign_image_edit` appears only in prohibitions/negative assertions; the scope-reduction language scan returned only false positives (version label `v1.5`, existing "hardcoded" nav description, `placeholders` in `.env.example`/prompt interpolation/UAT-evidence context). ✓
- **UI-SPEC:** referenced by 48-1-06/09/10/12/14 (design-system conformance and comparison/evaluation contract). ✓
- **Dimensions 7c / 8 / 11 / 12:** SKIPPED — no responsibility map, no `RESEARCH.md`/`VALIDATION.md`/`PATTERNS.md` (OpenSpec-driven phase). Plans compensate with per-task `<automated>` verifies.
- **Dimension 10 (AGENTS.md):** PASS — plans respect the GSD workflow and project conventions; no forbidden pattern or skipped required step detected.

---

## Remaining findings

> Per the operator's directive for this final round, only the plan scope-density warnings below are carried as open items. No blockers remain.

### Blockers

**None.**

### Warnings

| ID | Severity | Plan | Finding | Fix hint |
|----|----------|------|---------|----------|
| W2 | warning | 48-1-08 | **Scope density.** 4 tasks / **23** `files_modified` — above the generic ≤15-file guidance. Aggregation is deliberate and documented ("os 7 sub-itens da seção 8 do `tasks.md` são cobertos em 4 tasks agrupadas por afinidade"); no requirement is dropped and no single task exceeds 7 files. Context pressure at execution is the residual risk. | Optional: split schemas+estimate from route wiring if execution shows context pressure. |
| W3 | warning | 48-1-09 | **Scope density (same class as W2).** 3 tasks / **17** `files_modified` — above the ≤15-file guidance; per-task max is 7 files. | Optional: split local primitives (Task 1) from pages/execution panel. |

### Info

**None.** (Prior INFO advisories are closed/not carried forward per the operator's directive.)

---

## Structured issues

```yaml
issues:
  - plan: "48-1-08"
    dimension: "scope_sanity"
    severity: "warning"
    description: "4 tasks / 23 files_modified exceeds the generic <=15-file guidance; aggregation documented but context pressure is a residual risk (per-task max 7 files)."
    fix_hint: "Optionally split schemas+estimate from route wiring."
  - plan: "48-1-09"
    dimension: "scope_sanity"
    severity: "warning"
    description: "3 tasks / 17 files_modified exceeds the generic <=15-file guidance; per-task max 7 files."
    fix_hint: "Optionally split local primitives (Task 1) from pages/execution panel."
```

---

## Recommendation

**Proceed to execution.** The four third-round fixes are present and correct; the three second-round adjustments remain intact. The 14 plans are execution-ready: 9/9 capabilities covered, 1:1 with `tasks.md`, acyclic DAG with the mandated wave vector `1,1,2,2,3,3,4,5,6,6,7,7,8,9`, disjoint same-wave files, complete task structure (1 frontmatter / 3–4 tasks each), intact isolation fences, correct D16 push order and no scope creep.

The remaining warnings (scope density on 48-1-08 and 48-1-09) are advisory and do **not** block execution; the planner may apply them opportunistically. This closes the revision loop at iteration 4/4 with **no blockers**.

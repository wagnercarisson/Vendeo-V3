# Phase 48.1 — Plan Check (pre-execution, goal-backward)

**Phase:** 48.1 — Laboratório Mínimo de IA
**Plans checked:** 14 (`48-1-01` … `48-1-14`)
**Source of truth:** `openspec/changes/fase-48-1-laboratorio-ia-minimo/` (proposal / design D1–D18 / 9 specs / tasks.md) + `48.1-CONTEXT.md` + `48.1-UI-SPEC.md`
**Date:** 2026-09-15
**Method:** goal-backward verification of plans (not code). `gsd-sdk query verify.plan-structure` executed on all 14 plans: **14/14 `valid: true`, 0 errors, 0 warnings**; task counts 3–4.

---

## Overall verdict

**FAIL** — 0 blocker, 2 high, 2 medium, 5 low.

The **functional design is complete and faithful to the OpenSpec base**: all 9 capabilities are covered, all 18 locked decisions (D1–D18) have implementing tasks, the divergence resolutions (DV-1…DV-9) are correctly encoded, the isolation fences and the D16 migration order are respected, and no F48.2+ scope creep is present. However, two **high-severity, objectively verifiable defects** will make the plans fail their own acceptance gates (one of them in the root plan of the DAG). Both are surgical fixes — no architectural re-planning is required.

---

## Findings

| ID | Severity | Plan(s) | Finding | Required fix |
|----|----------|---------|---------|--------------|
| F1 | **high** | 48-1-01 | **False acceptance criterion / automated verify.** Task 1 asserts and greps `.planning/ROADMAP.md` for exactly 14 rows matching `^\| 48-1-[0-9]{2} \|`. Reality: `.planning/ROADMAP.md` contains **0** such rows (plans are bullets `- [ ] 48-1-XX-PLAN.md`, 14 of them); the 14-row tables live in `.planning/STATE.md` (14) and `AGENTS.md` (14). As written, Task 1 cannot pass — and `48-1-01` is the root of the DAG (every other plan depends on it transitively). | Change the criterion/verify to `.planning/STATE.md` (14 rows) + `AGENTS.md` (14 rows); for `.planning/ROADMAP.md` assert the bullet pattern `48-1-\d\d-PLAN\.md` (14 occurrences) instead. |
| F2 | **high** | 48-1-13 | **Contract-guard allowlist omits phase-legitimate files.** Task 3 (item 2) permits only `src/lib/lab/**`, `src/lib/ai/lab-telemetry-sink.ts`, `src/app/api/admin/laboratorio/**`, `src/app/(app)/admin/laboratorio/**`, `src/app/(app)/admin/layout.tsx`, `src/lib/admin/schemas.ts`, `src/lib/image-generation/services/image-generation-service.ts`, `src/lib/ai/__tests__/architecture-guard.test.ts`, `fixtures/lab/**`, `scripts/lab/**`, `scripts/uat/48-*.mjs`, `scripts/verify/48-1-13-contract-guard.mjs`, `supabase/migrations/*f48_1*`, `.env.example`, tracking. It misses files the phase itself creates/modifies: `src/lib/ai/lab-cost-estimate.ts` (48-1-08 T1), `src/lib/ai/__tests__/lab-cost-estimate.test.ts` (48-1-08 T1), `src/lib/ai/__tests__/lab-telemetry-sink.test.ts` (48-1-05 T3), `src/lib/image-generation/services/__tests__/image-generation-service.build-director-prompt.test.ts` (48-1-05 T1), and — if co-migrated — `src/lib/image-generation/services/__tests__/image-generation-service.test.ts` and `src/app/api/campaign/generate-image/__tests__/route.test.ts` (48-1-13 T1). With "any file outside the list → violation", the guard exits 1 and `48-1-13`/`48-1-14` cannot close. | Extend the allowlist to `src/lib/ai/lab-*.ts`, `src/lib/ai/__tests__/lab-*.test.ts`, `src/lib/image-generation/services/__tests__/image-generation-service*.test.ts`, and `src/app/api/campaign/generate-image/__tests__/route.test.ts`. |
| F3 | **medium** | 48-1-03 | **Manifest incompleteness.** Task 2's action requires scenario `produto-oferta-preco` to carry "1 imagem `primary` (`images/produto.jpg`) e 1 imagem `reference` (`images/produto-auxiliar.jpg`)", but `files_modified` does not list `fixtures/lab/scenarios/produto-oferta-preco/images/produto-auxiliar.jpg`. If the file is not created, `loadScenarioFixture` throws `missing_scenario_image:<path>` and the mapper/service/contract tests fail. | Add `fixtures/lab/scenarios/produto-oferta-preco/images/produto-auxiliar.jpg` to `files_modified`. |
| F4 | **medium** | 48-1-11, 48-1-12 | **Cross-plan constraint conflict.** 48-1-11 T2 item 7 scans every test file under `src/lib/lab/**/__tests__/**` and fails on the literal `OpenAIImageProvider` (and `@/lib/image-generation/providers/openai`), **without a negative-assertion/comment carve-out** (unlike other criteria in the same plan, which say "fora de asserções negativas"). 48-1-12 T1 authors `lab-gateway-harness.contract.test.ts` under that path precisely to prove the production provider is never used. If that test mentions `OpenAIImageProvider`, the 48-1-11 scan fails. | Add an explicit carve-out (comments/negative assertions) to the 48-1-11 scan, or constrain the 48-1-12 harness test to reference only `createNoopImageProvider` / `campaign_image_edit`. |
| F5 | low | 48-1-02 | `depends_on: []` contradicts `tasks.md` ("48-1-02 — onda 1, depende de 48-1-01"). The plan documents the dependency as traceability-only and consistent with the wave assignment (both wave 1, no shared files). Acceptable, but the source of truth is left inconsistent. | Normalize `tasks.md` before execution (remove the same-wave dependency) — no plan change required. |
| F6 | low | 48-1-03 | Uses slug `produto-oferta-logo` where `tasks.md` item 3.2 says `produto-oferta-badge`. The plan documents the divergence and aligns with `design.md` D4 + CONTEXT D4 (correct choice). | Synchronize `tasks.md` item 3.2 to `produto-oferta-logo`. |
| F7 | low | 48-1-09, 48-1-12 | Internal count errors in `<verification>` prose: 48-1-09 says "5 páginas" but enumerates 4 (the `comparar` page belongs to 48-1-10); 48-1-12 says the `lab-*.contract.test.ts` count is "6" but the enumerated set is 4 (its own) + 4 (48-1-11) = 8. | Correct the counts (non-blocking; not used as automated gates). |
| F8 | low | 48-1-09, 48.1-UI-SPEC | UI-SPEC lists a local `tabs` primitive; 48-1-09 deliberately omits it (no consumer). The omission is justified and reduces dead scope, but the UI-SPEC (`status: draft`, checker sign-off `pending`) is left un-reconciled. | Record the omission in the UI-SPEC (or set its status) before/after execution. |
| F9 | low | 48-1-14 | Artifact-naming divergence: `tasks.md` 14.5 says `48-VERIFICATION.md`/`48-UAT.md`; the plan uses `48-1-VERIFICATION.md`/`48.1-UAT.md` (consistent with the phase directory/plan convention). | Synchronize `tasks.md` 14.5 to the plan naming. |

**Info (not counted):** wave 4 contains `48-1-07 → 48-1-06` (a same-wave dependency), so wave 4 is not fully parallel. This is inherited verbatim from `ROADMAP.md`/`tasks.md` (both assign wave 4 to 06 and 07) and is the mandated wave vector `1,1,2,2,3,4,4,5,6,6,7,7,8,9` — not a plan defect.

---

## Requirement coverage matrix

Every roadmap capability slug appears in ≥1 plan's `requirements`, and each spec's requirements are addressed by tasks.

| Capability | Plans declaring it | Implementing tasks | Status |
|---|---|---|---|
| `lab-isolation` | 01, 02, 11, 13, 14 | 01 T2/T3 (RLS/grants/bucket/RPC), 02 T1/T2/T3, 11 T1/T2 | Covered |
| `lab-scenarios` | 01, 03, 11, 13, 14 | 03 T1/T2/T3, 11 T3A | Covered |
| `lab-experiments` | 01, 04, 11, 13, 14 | 04 T1/T2/T3, 11 T3B | Covered |
| `lab-runs` | 01, 05, 07, 12, 13, 14 | 07 T1/T2/T3, 12 T2 | Covered |
| `lab-gateway-harness` | 05, 12, 13, 14 | 05 T1/T2/T3, 12 T1 | Covered |
| `lab-artifacts` | 01, 06, 12, 13, 14 | 06 T1/T2/T3, 12 T3A | Covered |
| `lab-admin-api` | 08, 12, 13, 14 | 08 T1/T2/T3/T4, 12 T3B | Covered |
| `lab-admin-ui` | 09, 10, 12, 13, 14 | 09 T1/T2/T3, 10 T1/T2/T3, 12 T4A | Covered |
| `lab-human-evaluation` | 01, 10, 12, 13, 14 | 01 T3 (trigger append-only), 10 T2/T3, 12 T4A | Covered |

**Task mapping (1:1 with `tasks.md` sections 1–14):** verified — no plan merges or splits a section; each plan's objective cites its section's tasks.
- 01→§1 (1.1–1.10) · 02→§2 (2.1–2.6) · 03→§3 (3.1–3.5) · 04→§4 (4.1–4.5) · 05→§5 (5.1–5.6) · 06→§6 (6.1–6.4) · 07→§7 (7.1–7.5) · 08→§8 (8.1–8.7) · 09→§9 (9.1–9.7) · 10→§10 (10.1–10.4) · 11→§11 (11.1–11.6) · 12→§12 (12.1–12.6) · 13→§13 (13.1–13.5) · 14→§14 (14.1–14.7).

**Locked decisions D1–D18:** all have implementing tasks (D1→09; D2→02/08/09; D3→01; D4→03; D5→04; D6→05; D7→05/07; D8→07; D9→07; D10→06; D11→08; D12→09/10; D13→10; D14→01/02/07/08; D15→07/11/13; D16→01/14; D17→12 T4B; D18→05/13).

**PROJECT.md cross-check:** `REQUIREMENTS.md` contains no REQ-IDs mapped to this phase (per CONTEXT); no relevant requirement is silently dropped.

---

## Wave / dependency validation

| Plan | Wave | `depends_on` | Max dep wave | Verdict |
|---|---|---|---|---|
| 48-1-01 | 1 | `[]` | — | OK (root) |
| 48-1-02 | 1 | `[]` | — | OK (deviates from tasks.md "dep 01"; documented, wave-consistent) — see F5 |
| 48-1-03 | 2 | 01, 02 | 1 | OK |
| 48-1-04 | 2 | 01, 02 | 1 | OK |
| 48-1-05 | 3 | 03, 04 | 2 | OK |
| 48-1-06 | 4 | 01, 02 | 1 | OK (grouped by source at wave 4) |
| 48-1-07 | 4 | 03, 04, 05, 06 | 4 | OK, but same-wave dep on 06 (info) |
| 48-1-08 | 5 | 07 | 4 | OK |
| 48-1-09 | 6 | 08 | 5 | OK |
| 48-1-10 | 6 | 08 | 5 | OK |
| 48-1-11 | 7 | 03, 04, 08 | 5 | OK (07/05/06 reached transitively) |
| 48-1-12 | 7 | 05, 06, 07, 08, 09, 10 | 6 | OK |
| 48-1-13 | 8 | 08, 09, 10, 11, 12 | 7 | OK |
| 48-1-14 | 9 | 13 | 8 | OK |

- Wave vector matches the required `1,1,2,2,3,4,4,5,6,6,7,7,8,9`. ✓
- No cycles; every referenced plan exists; no forward references. ✓
- Same-wave `files_modified` non-overlap: wave 1 (01/02) ✓; wave 2 (03/04) ✓; wave 4 (06/07) ✓; wave 6 (09/10) ✓ (48-1-09 links to `/comparar` by `href` only and does not import 48-1-10's files; 48-1-10 renders its disabled state inline); wave 7 (11/12) ✓.

---

## Dimension checks

- **Frontmatter:** all 14 plans have `phase`, `plan`, `wave`, `depends_on`, `files_modified`, `autonomous`, `requirements`, `must_haves`; `autonomous: false` only on 48-1-14 (checkpoint tasks). ✓
- **Task quality:** every task has `<read_first>`, `<action>`, `<verify>`, `<acceptance_criteria>`, `<done>`; 0 fenced code blocks in any plan; actions carry concrete identifiers; modified files appear in the corresponding `read_first`. ✓ (except the false criteria in F1/F3/F7)
- **Security gate:** `<threat_model>` present in all 14 plans with plausible STRIDE registers; `security_enforcement: true`, `security_block_on: "high"`. ✓
- **Divergence encoding (DV-1…DV-9):** real `CostResolution` fields only (`imageUnitUsd`/`costPartial` appear solely in prohibitions/negative assertions); `resolveAiCost` receives `generationType` + `imageGenerationTool`; `defaultAiModelResolver` imported from `@/lib/ai`; local UI primitives; `operationRunType: "campaign_delivery"` with `runType: "lab"` only in the snapshot; `fixtures/` and `scripts/lab/` created. ✓
- **Isolation fences:** no task writes to `campaigns`, `campaign_art_versions`, `generation_events`, `ai_model_selection`, `ai_model_catalog`, `admin_audit_log`, official `prompts/`, or `campaign-images`; no paid calls in tests/CI; `src/lib/ai/gateway.ts`, `src/lib/campaign/**`, `src/lib/ai-cost/**`, `src/lib/ai/model-registry.ts` untouched (frozen by 48-1-13). ✓
- **Migration order (D16):** only 48-1-14 Task 3 performs a remote push (`[BLOCKING]`, post-UAT, `--dry-run` first); 48-1-01 explicitly forbids any push. ✓
- **Goal-backward gaps:** environment guard applied to all 7 API routes + all lab pages (layout-level) ✓; atomic `lab_reserve_run` before any paid call ✓; append-only evaluation trigger ✓; signed URLs (3600s) ✓; NDJSON run streaming ✓; exact limits ✓; no automatic quality score ✓.
- **Dimension 8 (Nyquist):** SKIPPED — no `RESEARCH.md`/`VALIDATION.md` for this OpenSpec-driven phase (`workflow.research: false`); plans compensate with per-task `<automated>` verifies.
- **Dimension 12 (Pattern compliance):** SKIPPED — no `PATTERNS.md`.
- **Dimension 7c (Architectural tiers):** SKIPPED — no responsibility map.

---

## Scope creep / missing artifacts

- **Scope creep:** none. No F48.2+ feature is implemented: no `model`/`configuration` comparison (explicitly rejected), no `campaign_image_edit` target, no prompt editor/`lab_prompt_versions`, no multi-evaluator/ranking/statistics, no queue/scheduler, no additional modalities. The `tabs` primitive omission (F8) reduces scope. ✓
- **Missing artifacts:** none functional. The only manifest gap is the undeclared `produto-auxiliar.jpg` fixture (F3). All required deliverables are planned: 8 `lab_*` tables + bucket + RPC + 5 triggers (01), guard/limits/env/gate (02), scenarios (03), experiment domain (04), harness (05), artifacts (06), run-service/snapshot/validation (07), 7 API routes (08), UI pages/components (09/10), contract suites (11/12), regression guard (13), UAT/verification (14).
- **Deferred ideas excluded:** all `## Deferred Ideas` (F48.2–F48.6, `lab_prompt_versions`, `VENDEO_LAB_ALLOW_REMOTE`, separate review audit) are absent from the plans. ✓

---

## Recommendation

Revise **F1** (48-1-01 Task 1 acceptance/verify) and **F2** (48-1-13 Task 3 allowlist) before execution — both are localized edits to acceptance criteria / guard allowlists, no change to the architecture or task decomposition. F3/F4 should also be corrected (manifest entry and scan carve-out). F5–F9 are documentation synchronizations with `tasks.md`/`48.1-UI-SPEC.md` that can be applied in the same revision.

After these fixes, the plan set is expected to PASS: coverage, DAG, isolation, D16 order and divergence encoding are all sound.

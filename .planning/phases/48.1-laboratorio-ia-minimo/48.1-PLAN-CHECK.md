# Phase 48.1 — Plan Check (pre-execution, goal-backward) — RE-VERIFICATION #3 (final)

**Phase:** 48.1 — Laboratório Mínimo de IA
**Plans checked:** 14 (`48-1-01` … `48-1-14`)
**Source of truth:** `openspec/changes/fase-48-1-laboratorio-ia-minimo/` (proposal / design D1–D18 / 9 specs / tasks.md) + `48.1-CONTEXT.md` + `48.1-UI-SPEC.md`
**Date:** 2026-09-15 (iteration 3/3 — after the corrupted `48-1-13-PLAN.md` was repaired and W1 fixed)
**Method:** goal-backward verification of plans (not code). `gsd-sdk query verify.plan-structure` executed on all 14 plans.

> This report reflects the **current** file state only. Findings resolved in prior cycles are not re-listed as open issues.

---

## Overall verdict

**PASS WITH WARNINGS** — **0 blockers**, 2 warnings, 3 info.

The single blocker from re-verification #2 (`48-1-13-PLAN.md` structurally corrupted) is **resolved**: the file is now a single well-formed plan with exactly 3 tasks. W1 (stale trigger counts) is **resolved**. All 14 plans are execution-ready: 9/9 capabilities covered, 1:1 with `tasks.md` §§1–14, acyclic DAG with the mandated wave vector `1,1,2,2,3,3,4,5,6,6,7,7,8,9`, complete task structure, intact isolation fences, correct D16 push order and no scope creep. The residual items are advisory (scope density and traceability), none blocking.

---

## Repair confirmation (`48-1-13-PLAN.md`)

| Check | Result | Evidence |
|---|---|---|
| Single frontmatter block | ✅ | Exactly 1× `phase: 48.1`, 1× `plan: 48-1-13`, 1× `type: execute`; frontmatter delimiters = 2 (open/close) |
| Exactly 3 tasks | ✅ | 3× `<task type=…>`; `verify.plan-structure` → `task_count: 3` ("Suíte completa…", "Guard de contrato congelado + os 3 gates", "Varredura de não-mudança…") |
| No duplicated content | ✅ | 1× `<objective>`, 1× `<tasks>`, 1× `</tasks>`, 1× `<threat_model>`; 252 lines (no nested copies; the previous 715-line/6-task corruption is gone) |
| Guard spec preserved | ✅ | L150 action: base lida de `48.1-BASELINE.txt`, `trim()`, `^[0-9a-f]{40}$`, explicit "**Nunca** usar `HEAD`"; L166–L167 acceptance assert the same; L48 key_link uses `git diff --name-only <base>` |
| `verify.plan-structure` | ✅ | `valid: true`, `errors: []`, `warnings: []` |

## W1 confirmation (stale trigger counts)

| Plan | Before | Now | Evidence |
|---|---|---|---|
| `48-1-01` | "5 triggers" | "8 triggers e as RPCs `lab_reserve_run`/`lab_create_experiment`" | `<success_criteria>` L316 |
| `48-1-14` | "4 triggers" | "as RPCs `lab_reserve_run`/`lab_create_experiment` e os **8** triggers confirmados no remoto" | Task 3 acceptance L233 |
| `48-1-01` threat row | partial | T-48-1-06 now covers variants + scenarios (INSERT/UPDATE/DELETE) + `lab_experiments` DELETE-with-history + `lab_runs` DELETE | L297 |

The migration (48-1-01 Task 3) defines exactly **8** `trg_lab_*` functions and acceptance asserts `CREATE OR REPLACE FUNCTION public.trg_lab_` = 8 and `CREATE TRIGGER` = 8 (L260–L261).

---

## Second-round adjustments (independently re-checked)

| # | Adjustment | Resolved? | Evidence |
|---|---|---|---|
| 1 | Baseline encoding + guard validation: `48-1-01` uses `git rev-parse HEAD \| Set-Content -Encoding ascii -NoNewline`; `48-1-13` guard must `trim()` + validate `^[0-9a-f]{40}$`; no `HEAD` as base | **YES** | `48-1-01` L114 uses `Set-Content -Encoding ascii -NoNewline` (with the PowerShell 5.1 BOM warning) and L139 asserts a 40-char SHA; `48-1-13` L150/L166–L167 implement `trim()` + SHA regex and forbid `HEAD`. File integrity now clean. |
| 2 | Structural freezing: 8 trigger functions/triggers; SQL test items (f)–(j) | **YES** | `48-1-01` L214–L221 define the 8 `trg_lab_*` functions (incl. `experiment_scenarios_freeze`, `experiments_no_delete`, `runs_no_delete`); acceptance L260/L261 = 8/8; SQL test L250 includes items (f)–(j) (frozen variants/scenarios, `lab_experiment_has_history`, `lab_run_delete_forbidden`, invalid `lab_create_experiment`). |
| 3 | Atomic experiment creation: `lab_create_experiment` RPC; `48-1-04` calls it, no separate inserts | **YES** | `48-1-01` L244–L246 defines the RPC (single transaction, `invalid_experiment_input`, REVOKE/GRANT); `48-1-04` L216 calls `client.rpc("lab_create_experiment", …)` and explicitly forbids three separate PostgREST inserts; key_links L54–L56 target `public.lab_create_experiment`; L221 adds the atomic-failure test. |

---

## Re-run of the broader goal-backward checks

### Requirement coverage (9 capabilities)

All 9 roadmap capability slugs (`.planning/ROADMAP.md` L1066) appear in ≥1 plan's `requirements` frontmatter; no capability is dropped.

| Capability | Plans declaring it | Status |
|---|---|---|
| `lab-isolation` | 01, 02, 11, 13, 14 | Covered |
| `lab-scenarios` | 01, 03, 11, 13, 14 | Covered |
| `lab-experiments` | 01, 04, 11, 13, 14 | Covered |
| `lab-runs` | 01, 05, 07, 12, 13, 14 | Covered |
| `lab-gateway-harness` | 05, 12, 13, 14 | Covered |
| `lab-artifacts` | 01, 06, 12, 13, 14 | Covered |
| `lab-admin-api` | 08, 12, 13, 14 | Covered |
| `lab-admin-ui` | 09, 10, 12, 13, 14 | Covered |
| `lab-human-evaluation` | 01, 10, 12, 13, 14 | Covered |

**PROJECT.md / REQUIREMENTS.md cross-check:** no `REQ-` IDs map to this phase (roadmap L1269: 9 new capabilities, no REQ-IDs). No relevant requirement is silently dropped.

### 1:1 mapping with `tasks.md` §§1–14

Verified item by item — no section merged, split or orphaned: 01→§1 · 02→§2 · 03→§3 · 04→§4 · 05→§5 · 06→§6 · 07→§7 · 08→§8 · 09→§9 · 10→§10 · 11→§11 · 12→§12 · 13→§13 · 14→§14.

### Dependency graph (acyclic) + wave vector

`depends_on` (from `frontmatter.get`): 01 `[]` · 02 `[]` · 03 `[01,02]` · 04 `[01,02]` · 05 `[03,04]` · 06 `[01,02]` · 07 `[03,04,05,06]` · 08 `[07]` · 09 `[08]` · 10 `[08]` · 11 `[03,04,08]` · 12 `[05,06,07,08,09,10]` · 13 `[08,09,10,11,12]` · 14 `[13]`.

- All references resolve to existing plans; **no cycles**; **no forward references**; every plan's wave ≥ max(dep wave)+1.
- Waves = **1,1,2,2,3,3,4,5,6,6,7,7,8,9** ✓ (matches `.planning/ROADMAP.md` L1074–L1087). Note: 48-1-06 sits at wave 3 while its deps are wave 1 (safe, deliberately delayed to keep same-wave `files_modified` disjoint).
- Same-wave `files_modified` are disjoint (waves 1, 2, 3, 6, 7).
- `autonomous: false` only on 48-1-14 (human checkpoints) ✓.

### Frontmatter / task quality / threat model

- **14/14** plans have a single well-formed frontmatter block (`phase`, `plan`, `type`, `wave`, `depends_on`, `files_modified`, `autonomous`, `requirements`, `must_haves`).
- **14/14** tasks carry `<read_first>` + `<action>` + `<verify>` + `<acceptance_criteria>` + `<done>`; counts match task counts exactly (3/3/3…, 4/4/4… for 48-1-08/12/14). Actions carry concrete identifiers and file paths.
- `<threat_model>` present exactly once (balanced open/close) in **14/14**, each with a plausible STRIDE register and the supply-chain `T-48-1-SC` row.
- Automated verifies: no `<automated>MISSING</automated>`, no `--watch*`, no full E2E suites (playwright/cypress/selenium). `48-1-13` Task 1 intentionally runs the full `npx vitest run` (phase-closing regression) — appropriate.

### Isolation fences / D16 / divergence encoding / scope creep

- No task writes to `campaigns`, `campaign_art_versions`, `generation_events`, `ai_model_selection`, `ai_model_catalog` (read-only: `.select()` only — 48-1-04 L174, 48-1-12 L157), `admin_audit_log`, official `prompts/` or the `campaign-images` bucket; every mention is a prohibition, assertion or contract guard.
- **D16 order:** only `48-1-14` Task 3 performs a remote push (`[BLOCKING]`, `--dry-run` first, post-UAT, `authorize remote migration`); `48-1-01` explicitly forbids any `supabase db push`. ✓
- **Divergence encoding (DV-1…DV-9):** `imageUnitUsd`/`costPartial` appear only in prohibitions/negative assertions (48-1-05 L258); `resolveAiCost` receives `generationType` + `imageGenerationTool`; `defaultAiModelResolver` imported via dynamic import `@/lib/ai`; `operationRunType: "campaign_delivery"` with `runType: "lab"` only in the run snapshot; local UI primitives; `fixtures/` and `scripts/lab/` created. ✓
- **Scope creep:** none. All `F48.2` mentions are deferrals/prohibitions (9 occurrences); `campaign_image_edit` appears only in prohibitions/negative assertions. ✓
- **UI-SPEC:** referenced by 48-1-06/09/10/12/14 (design-system conformance and comparison/evaluation contract). ✓
- **Dimensions 7c / 8 / 11 / 12:** SKIPPED — no responsibility map, no `RESEARCH.md`/`VALIDATION.md`/`PATTERNS.md` (OpenSpec-driven phase). Plans compensate with per-task `<automated>` verifies.
- **Dimension 10 (AGENTS.md):** PASS — plans respect the GSD workflow and project conventions; no forbidden pattern or skipped required step detected.

---

## Remaining / new findings

### Blockers

**None.**

### Warnings

| ID | Severity | Plan | Finding | Fix hint |
|----|----------|------|---------|----------|
| W2 | warning | 48-1-08 | **Scope density.** 4 tasks / **23** `files_modified` — above the generic ≤15-file guidance. Aggregation is deliberate and documented ("os 7 sub-itens da seção 8 do `tasks.md` são cobertos em 4 tasks agrupadas por afinidade"); no requirement is dropped and no single task exceeds 7 files. Context pressure at execution is the residual risk. | Optional: split schemas+estimate from route wiring if execution shows context pressure. |
| W3 | warning | 48-1-09 | **Scope density (same class as W2).** 3 tasks / **17** `files_modified` — above the ≤15-file guidance; per-task max is 7 files. | Optional: split local primitives (Task 1) from pages/execution panel. |

### Info

| ID | Severity | Plan | Finding | Fix hint |
|----|----------|------|---------|----------|
| I1 | info | 48-1-01 | **Partial traceability gap for `lab_create_experiment`.** The truth is now present (`must_haves.truths` L32), but the artifact `provides` (L40), `key_links` (L42–L50), the `<objective>` (L54) and the Task 3 `<name>` (L199) still mention only `lab_reserve_run`. | Add `lab_create_experiment` to the artifact `provides`/key_links/objective. |
| I2 | info | 48-1-11 | **`depends_on` omits 48-1-07** although the isolation contract test exercises `run-service`. Ordering is safe transitively: 11 depends on 08 (which depends on 07), and 11 wave 7 > 07 wave 4. Documentation-only gap. | Optional: list 48-1-07 explicitly. |
| I3 | info | 48-1-01 | **`rg` is still not resolvable in this shell** (win32 / PowerShell 5.1: `Get-Command rg` → not found). 48-1-01 Task 1's `<automated>` depends on `rg` and would fail closed with a misleading `throw "residuo 48-01- encontrado"` if absent. Precedent exists (47-01 used `rg` and F47 closed with 4 green gates). | Optional: harden with a `Get-Command rg` guard or fall back to `Select-String`. |

*Resolved since re-verification #2 (independently re-verified):* **B1** (48-1-13 corruption — now single frontmatter / 3 tasks) and **W1** (stale trigger counts — 48-1-01 and 48-1-14 now state 8 triggers + both RPCs).

---

## Structured issues

```yaml
issues:
  - plan: "48-1-08"
    dimension: "scope_sanity"
    severity: "warning"
    description: "4 tasks / 23 files_modified exceeds the generic <=15-file guidance; aggregation documented but context pressure is a residual risk (per-task max 7 files)."
    fix_hint: "Optionally split schemas+estimate from route wiring."
  - plan: "48-1-09"
    dimension: "scope_sanity"
    severity: "warning"
    description: "3 tasks / 17 files_modified exceeds the generic <=15-file guidance; per-task max 7 files."
    fix_hint: "Optionally split local primitives (Task 1) from pages/execution panel."
  - plan: "48-1-01"
    dimension: "verification_derivation"
    severity: "info"
    description: "lab_create_experiment missing from artifact provides/key_links/objective/Task 3 name (truth already added)."
    task: 3
    fix_hint: "Add lab_create_experiment to provides/key_links/objective."
  - plan: "48-1-11"
    dimension: "dependency_correctness"
    severity: "info"
    description: "depends_on omits 48-1-07 though the isolation test exercises run-service; ordering safe transitively via 08."
    fix_hint: "Optional: list 48-1-07 explicitly."
  - plan: "48-1-01"
    dimension: "task_completeness"
    severity: "info"
    description: "rg is not resolvable in this shell; Task 1 automated verify depends on rg and fails closed misleadingly if absent. Precedent F47 used rg."
    task: 1
    fix_hint: "Optional: Get-Command rg guard or Select-String fallback."
```

---

## Recommendation

**Proceed to execution.** The blocker is resolved, W1 is fixed, and the three second-round adjustments are intact. The 14 plans are execution-ready: 9/9 capabilities covered, 1:1 with `tasks.md`, acyclic DAG with the mandated wave vector `1,1,2,2,3,3,4,5,6,6,7,7,8,9`, disjoint same-wave files, complete task structure, intact isolation fences, correct D16 push order and no scope creep.

The remaining warnings (scope density on 48-1-08 and 48-1-09) and info items (traceability, dependency documentation, `rg` shell hardening) are advisory and do **not** block execution; the planner may apply them opportunistically. This closes the revision loop at iteration 3/3 with **no blockers**.

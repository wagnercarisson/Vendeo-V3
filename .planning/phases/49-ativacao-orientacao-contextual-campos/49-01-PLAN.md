---
phase: 49-ativacao-orientacao-contextual-campos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/49-ativacao-orientacao-contextual-campos/49-BASELINE.txt
autonomous: true
requirements:
  - contextual-field-help
  - store-field-orientation
  - campaign-field-orientation
must_haves:
  truths:
    - "Existe um registro verificável do estado inicial do repositório e das alterações pré-existentes preservadas"
    - "Os consumidores reais de cada campo estão inventariados com arquivo:linha"
    - "Os hashes dos arquivos protegidos e a regra real de desbloqueio estão registrados como baseline de não-mudança"
  artifacts:
    - path: ".planning/phases/49-ativacao-orientacao-contextual-campos/49-BASELINE.txt"
      provides: "Estado inicial, inventário de consumidores, regra de desbloqueio e hashes dos arquivos protegidos"
      contains: "Baseline de não-mudança"
  key_links:
    - from: ".planning/phases/49-ativacao-orientacao-contextual-campos/49-BASELINE.txt"
      to: "prompts/** e src/lib/ai/**"
      via: "hashes SHA-256 registrados"
      pattern: "SHA256"
    - from: ".planning/phases/49-ativacao-orientacao-contextual-campos/49-BASELINE.txt"
      to: "src/lib/image-generation/services/art-director-briefing.ts"
      via: "confirmação de ausência de product.description"
      pattern: "art-director-briefing"
---

<objective>
Registrar o baseline de não-mudança da F49: estado inicial do repositório, inventário em código dos consumidores reais dos campos e hashes dos arquivos protegidos, antes de qualquer alteração de apresentação.

Purpose: Sem baseline verificável, as fences de não-mudança (prompts, gateway/modelos, pipeline, body, snapshot, contratos) não podem ser provadas ao final. Este plano produz a evidência comparável usada pelos planos de gates e UAT.
Output: `.planning/phases/49-ativacao-orientacao-contextual-campos/49-BASELINE.txt` com quatro blocos: Estado inicial, Consumidores reais, Regra de desbloqueio e Baseline de não-mudança.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-ativacao-orientacao-contextual-campos/49-CONTEXT.md
@.planning/phases/49-ativacao-orientacao-contextual-campos/49-PATTERNS.md
@openspec/changes/fase-49-ativacao-orientacao-contextual-campos/tasks.md
@openspec/changes/fase-49-ativacao-orientacao-contextual-campos/design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Registrar estado inicial do repositório e preservar alterações pré-existentes</name>
  <files>.planning/phases/49-ativacao-orientacao-contextual-campos/49-BASELINE.txt</files>
  <read_first>
    - `.planning/phases/49-ativacao-orientacao-contextual-campos/49-CONTEXT.md` (§scope_fence — lista de arquivos proibidos)
    - `openspec/changes/fase-49-ativacao-orientacao-contextual-campos/tasks.md` (grupo 1, itens 1.1–1.4)
  </read_first>
  <action>
    Executar `git status --short` e `git diff --stat` e gravar o resultado no arquivo `49-BASELINE.txt`, na seção "## Estado inicial". Listar explicitamente os arquivos modificados/não rastreados **pré-existentes** e marcá-los como "preservar — não commitar sem decisão explícita". Não exigir árvore limpa; não reverter, não formatar e não commitar nenhum arquivo pré-existente. Registrar a data (2026-09-18) e o branch atual (`git rev-parse --abbrev-ref HEAD`).
  </action>
  <acceptance_criteria>
    - `49-BASELINE.txt` existe e contém a seção "## Estado inicial".
    - A seção contém a saída literal de `git status --short` (ou "working tree clean" quando vazia) e a linha do branch.
    - Nenhum arquivo pré-existente foi modificado, removido ou commitado por esta tarefa (`git status --short` inalterado em relação à leitura inicial).
  </acceptance_criteria>
  <verify>
    <automated>Test-Path ".planning/phases/49-ativacao-orientacao-contextual-campos/49-BASELINE.txt"; Select-String -Path ".planning/phases/49-ativacao-orientacao-contextual-campos/49-BASELINE.txt" -Pattern "## Estado inicial"</automated>
  </verify>
  <done>Seção "Estado inicial" gravada com `git status --short`, branch e marcação de preservação; nenhum arquivo pré-existente tocado.</done>
</task>

<task type="auto">
  <name>Task 2: Inventariar consumidores reais dos campos em código</name>
  <files>.planning/phases/49-ativacao-orientacao-contextual-campos/49-BASELINE.txt</files>
  <read_first>
    - `openspec/changes/fase-49-ativacao-orientacao-contextual-campos/design.md` (tabela "Consumidores reais dos campos" no Context)
    - `src/components/flow/use-campaign-form.ts:417-427` (`inferIntent`)
    - `src/lib/copy/mapper.ts:90-120` (`mapBriefToCopyDirectorInput` → `description: brief.product.description`)
    - `src/lib/image-generation/services/art-director-briefing.ts` (arquivo inteiro — fence de ausência de `description`)
    - `src/lib/store-onboarding/tabs.ts:62-101` (`computeTabUnlock`)
    - `src/lib/store-onboarding/reason-text.ts:23-42` (`tabBlockReasonText`)
  </read_first>
  <action>
    Confirmar em código e gravar em `49-BASELINE.txt` a seção "## Consumidores reais" com uma tabela campo → consumidor → `arquivo:linha`, cobrindo exatamente: `Nome da Loja` (brief/copy/identidade); `Tom de Voz` (`src/lib/store-onboarding/tabs.ts` `computeTabUnlock`, brief, copy mapper, `src/lib/image-generation/services/art-director-briefing.ts` `storeTone` com default `"profissional"`); `Posicionamento` e `Descrição Curta` (brief, copy mapper, `brand-director`, `brand-profiler`, `identity-art-director`, `text-only-inference`, drift); `Slogan` (brief, copy mapper, assinatura visual, drift); `product.description` (`src/lib/copy/mapper.ts` linha `description: brief.product.description`) e **ausente** em `art-director-briefing.ts`; preços (`inferIntent`); `mandatoryArtworkText` (prompt do diretor, snapshot). Usar `rg`/`Select-String` para obter os números de linha reais e registrá-los. Para a fence do Diretor de Arte, registrar a saída de `rg -c "description" src/lib/image-generation/services/art-director-briefing.ts` (esperado: 0 ocorrências).
  </action>
  <acceptance_criteria>
    - A seção "## Consumidores reais" contém uma linha para cada um dos 8 grupos de campos listados, cada uma com pelo menos um `arquivo:linha` verificável.
    - A linha de `product.description` cita `src/lib/copy/mapper.ts` e registra explicitamente "ausente em `art-director-briefing.ts`".
    - O comando `rg -c "description" src/lib/image-generation/services/art-director-briefing.ts` retorna 0 (sem ocorrências) e a saída está registrada.
  </acceptance_criteria>
  <verify>
    <automated>rg -c "description" "src/lib/image-generation/services/art-director-briefing.ts"; if ($LASTEXITCODE -ne 0) { "OK: 0 ocorrencias" } else { "FALHA: description encontrado" }</automated>
  </verify>
  <done>Inventário gravado com arquivo:linha para os 8 grupos; ausência de `description` no briefing do diretor registrada.</done>
</task>

<task type="auto">
  <name>Task 3: Registrar a regra real de desbloqueio e o baseline de hashes dos arquivos protegidos</name>
  <files>.planning/phases/49-ativacao-orientacao-contextual-campos/49-BASELINE.txt</files>
  <read_first>
    - `src/lib/store-onboarding/tabs.ts:85-101` (retorno `{ unlocked: false, reason: "needs_tone_of_voice" }`)
    - `src/lib/store-onboarding/reason-text.ts:34-35` (caso `needs_tone_of_voice`)
    - `openspec/changes/fase-49-ativacao-orientacao-contextual-campos/tasks.md` (item 1.4)
  </read_first>
  <action>
    Gravar em `49-BASELINE.txt` a seção "## Regra de desbloqueio" citando `computeTabUnlock` (`src/lib/store-onboarding/tabs.ts`) com o caso `needs_tone_of_voice` e `tabBlockReasonText` (`src/lib/store-onboarding/reason-text.ts`) como fonte do texto. Gravar a seção "## Baseline de não-mudança" com o SHA-256 de cada item protegido: todos os arquivos sob `prompts/` (recursivo), todos os arquivos sob `src/lib/ai/` (recursivo), `src/lib/campaign/brief.ts`, `src/lib/campaign/brief-schema.ts`, `src/lib/image-generation/services/art-director-briefing.ts` e `src/components/flow/use-campaign-form.ts`. Formato por linha: `<hash>  <caminho relativo com barras>`. Ordenar alfabeticamente para permitir comparação determinística no plano de gates.
  </action>
  <acceptance_criteria>
    - A seção "## Regra de desbloqueio" cita `tabs.ts` e `reason-text.ts` e o motivo `needs_tone_of_voice`.
    - A seção "## Baseline de não-mudança" contém uma linha `hash  caminho` para cada arquivo de `prompts/**` e `src/lib/ai/**` e para os 4 arquivos individuais listados (mínimo de 6 arquivos individuais + recursivos).
    - Nenhum caminho da seção está ausente do disco no momento da gravação.
  </acceptance_criteria>
  <verify>
    <automated>Get-ChildItem -Path "prompts","src/lib/ai" -Recurse -File | Get-FileHash -Algorithm SHA256 | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <done>Regra de desbloqueio documentada e hashes SHA-256 registrados para todos os arquivos protegidos, prontos para comparação no plano de gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nenhuma nova | Este plano apenas lê o repositório e grava um artefato de planejamento. Não cria rotas, não altera autenticação/autorização, não persiste dados de usuário e não chama IA. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01 | Spoofing | — | accept | Nenhuma superfície de identidade/sessão é tocada; rotas HTTP e authz permanecem intocados por fence. |
| T-49-02 | Tampering | `49-BASELINE.txt` (hashes) | mitigate | Hashes SHA-256 calculados pelo `Get-FileHash` do sistema, sem normalização manual; caminhos relativos determinísticos. |
| T-49-03 | Repudiation | — | accept | Sem operações auditáveis novas; nenhum dado é gravado no banco. |
| T-49-04 | Information Disclosure | `49-BASELINE.txt` | mitigate | O artefato contém apenas caminhos e hashes de arquivos do repositório — nenhum segredo, token ou conteúdo de `.env`. |
| T-49-05 | Denial of Service | — | accept | Sem impacto em runtime; leitura local apenas. |
| T-49-06 | Elevation of Privilege | — | accept | Nenhuma mudança de autorização ou validação. |
| T-49-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma dependência é instalada neste plano. |
</threat_model>

<verification>
- `49-BASELINE.txt` contém as 4 seções: Estado inicial, Consumidores reais, Regra de desbloqueio, Baseline de não-mudança.
- `git status --short` não foi alterado por nenhuma ação do plano (arquivos pré-existentes preservados).
- `rg -c "description" src/lib/image-generation/services/art-director-briefing.ts` retorna 0.
</verification>

<success_criteria>
- Baseline verificável disponível para comparação nos planos de gates e UAT.
- Nenhum arquivo de produção foi alterado por este plano.
- Inventário de consumidores confirma os efeitos reais usados pela microcopy (fonte de D14).
</success_criteria>

<output>
Create `.planning/phases/49-ativacao-orientacao-contextual-campos/49-01-SUMMARY.md` when done.
</output>

---
phase: 49-ativacao-orientacao-contextual-campos
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/store-onboarding/field-guidance.ts
  - src/lib/campaign/field-guidance.ts
  - src/lib/store-onboarding/__tests__/field-guidance.test.ts
  - src/lib/campaign/__tests__/field-guidance.test.ts
autonomous: true
requirements:
  - store-field-orientation
  - campaign-field-orientation
must_haves:
  truths:
    - "Todo o conteúdo de orientação (labels, hints, exemplos e feedback) vive em módulos puros, sem JSX e sem side-effects"
    - "As 8 opções de tom de voz têm descrição contextual própria e não vazia"
    - "O feedback dinâmico de preço cobre os 4 estados, incluindo o estado neutro 'só preço anterior'"
    - "Componentes e testes consomem a mesma fonte de strings (sem cópia divergente)"
  artifacts:
    - path: "src/lib/store-onboarding/field-guidance.ts"
      provides: "Microcopy da loja: nome público, dados fiscais, tom de voz (hint + 8 descrições), posicionamento, descrição curta, slogan, rótulos de estado"
      min_lines: 60
    - path: "src/lib/campaign/field-guidance.ts"
      provides: "Microcopy da campanha: descrição do produto, preços (labels/hints/regras) e feedback dinâmico de 4 estados, informações obrigatórias na arte"
      min_lines: 70
    - path: "src/lib/store-onboarding/__tests__/field-guidance.test.ts"
      provides: "Testes unitários do módulo de conteúdo da loja"
    - path: "src/lib/campaign/__tests__/field-guidance.test.ts"
      provides: "Testes unitários do módulo de conteúdo da campanha"
  key_links:
    - from: "src/lib/campaign/field-guidance.ts"
      to: "src/components/flow/use-campaign-form.ts (inferIntent)"
      via: "correspondência dos 4 estados provada em teste, sem import de módulo client"
      pattern: "priceFeedbackMessage"
    - from: "src/lib/store-onboarding/field-guidance.ts"
      to: "src/lib/store-onboarding/tabs.ts (computeTabUnlock)"
      via: "hint de tom de voz coerente com needs_tone_of_voice"
      pattern: "TONE_OF_VOICE_HINT"
---

<objective>
Criar os dois módulos puros de conteúdo de orientação (D14) e seus testes unitários: microcopy da loja e microcopy da campanha, incluindo a função pura de feedback dinâmico de preço.

Purpose: Fonte única das strings usadas pelos componentes e pelos testes — impede divergência entre o que a UI diz e o que o sistema faz.
Output: `src/lib/store-onboarding/field-guidance.ts`, `src/lib/campaign/field-guidance.ts` e os testes unitários de ambos.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-ativacao-orientacao-contextual-campos/49-CONTEXT.md
@.planning/phases/49-ativacao-orientacao-contextual-campos/49-PATTERNS.md
@openspec/changes/fase-49-ativacao-orientacao-contextual-campos/specs/store-field-orientation/spec.md
@openspec/changes/fase-49-ativacao-orientacao-contextual-campos/specs/campaign-field-orientation/spec.md
@src/lib/store-onboarding/reason-text.ts
@src/lib/constants.ts
@src/components/flow/use-campaign-form.ts
</context>

<interfaces>
<!-- Contratos extraídos do código real que o executor deve respeitar. -->

De `src/lib/store-onboarding/reason-text.ts` (molde de header e forma do módulo puro):
```typescript
/**
 * Módulo puro: sem runtime de UI, sem ambiente de servidor, sem imports de
 * side-effect.
 */
import type { OnboardingTab, TabBlockReason } from "./tabs";
export function tabBlockReasonText(tab: OnboardingTab, reason: TabBlockReason | undefined, label?: string): string;
```

De `src/lib/store-onboarding/tabs.ts`:
```typescript
export function computeTabUnlock(tab: OnboardingTab, ctx: TabUnlockContext): { unlocked: boolean; reason?: TabBlockReason };
// toneOfVoice vazio => { unlocked: false, reason: "needs_tone_of_voice" }
```

De `src/components/flow/use-campaign-form.ts`:
```typescript
export function inferIntent(originalPriceCents: number, discountedPriceCents: number | undefined | null): CampaignIntent;
// (original>0 && discounted>0) => "offer"; (discounted>0) => "spotlight"; senão => "exclusive"
```
`field-guidance.ts` NÃO deve importar `inferIntent`/`availableOptions` (dependência de módulo client). A correspondência é provada em teste (plano 49-08).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Criar o módulo puro de conteúdo da loja</name>
  <files>src/lib/store-onboarding/field-guidance.ts</files>
  <read_first>
    - `src/lib/store-onboarding/reason-text.ts` (arquivo inteiro — header JSDoc, mapa tipado, função pura)
    - `src/lib/constants.ts:1-17` (padrão `as const` + tipo derivado)
    - `src/components/flow/store-identity-form.tsx:37-46` (`TONE_OF_VOICE_OPTIONS` — as 8 chaves exatas)
    - `openspec/changes/fase-49-ativacao-orientacao-contextual-campos/specs/store-field-orientation/spec.md` (requirements 1–4)
    - `.planning/phases/49-ativacao-orientacao-contextual-campos/49-CONTEXT.md` (§specifics — strings canônicas)
    - `.planning/phases/49-ativacao-orientacao-contextual-campos/49-PATTERNS.md` (bloco "field-guidance.ts (utility, conteúdo puro)")
  </read_first>
  <action>
    Criar `src/lib/store-onboarding/field-guidance.ts` como módulo **puro** (sem `"use client"`, sem import de React, sem `server-only`, sem side-effects) com header JSDoc citando F49 e as decisões D3/D4/D5/D6/D7/D14. Exportar:
    - `type StoreToneOfVoice` — união literal com exatamente as 8 chaves: `"profissional" | "moderno" | "elegante" | "divertido" | "acolhedor" | "jovem" | "tradicional" | "luxuoso"`.
    - `STORE_NAME_HINT` = `"Este é o nome público da sua loja. Ele aparece no Vendeo e é usado para identificar e assinar suas campanhas."`
    - `FISCAL_SECTION_LABEL` = `"Dados fiscais"` e `FISCAL_SECTION_HELPER` = `"Dados cadastrais oficiais (Receita Federal) usados para verificação e prontidão do cadastro."`
    - `TONE_OF_VOICE_HINT` = `"Define como sua loja se comunica. O Vendeo usa essa escolha nos títulos, legendas e no clima visual das campanhas."`
    - `TONE_OF_VOICE_COMPLEMENTS_HINT` = microcopy deixando claro que o tom complementa (não substitui) segmento/subsegmento como base da identidade.
    - `TONE_OF_VOICE_DESCRIPTIONS: Record<StoreToneOfVoice, string>` com as 8 frases positivas. Fixar: `profissional` → `"Direta, confiável e sem exageros."`; `moderno` → `"Atual, objetiva e com energia contemporânea."`; `luxuoso` → `"Sofisticada, exclusiva e com senso de premium."`; e redigir `elegante`, `divertido`, `acolhedor`, `jovem`, `tradicional` no mesmo tom (positivo, curto, descrevendo personalidade/energia).
    - `POSITIONING_LABEL` = `"Como você quer que sua loja seja percebida?"`, `POSITIONING_SECONDARY_LABEL` = `"Posicionamento da marca"`, `POSITIONING_HINT` = pedido de público, proposta e diferencial, `POSITIONING_PLACEHOLDER` = `"Ex: Somos uma loja de..."`, `POSITIONING_HELP_TITLE` = `"Exemplo de posicionamento"`, `POSITIONING_EXAMPLE` = `"Somos uma loja de [categoria] para [público], reconhecida por [diferencial]."`
    - `POSITIONING_IDENTITY_HINT` — microcopy reconhecendo efeito **direto na copy** e **indireto no perfil/direção visual**, sem prometer transformação visual específica.
    - `SHORT_DESCRIPTION_HINT` = o que a loja vende, para quem e diferencial factual.
    - `SLOGAN_HINT` = frase pública já adotada, com `"se sua loja já utiliza um"`.
    - `RECOMMENDED_LABEL` = `"Recomendado"` e `OPTIONAL_LABEL` = `"(opcional)"`.
    Não criar dependências novas nem importar `store-identity-form.tsx`.
  </action>
  <acceptance_criteria>
    - `rg -c "use client|from \"react\"|server-only" src/lib/store-onboarding/field-guidance.ts` retorna 0.
    - `TONE_OF_VOICE_DESCRIPTIONS` tem exatamente as 8 chaves e nenhuma string vazia.
    - `STORE_NAME_HINT`, `TONE_OF_VOICE_HINT`, `POSITIONING_LABEL`, `POSITIONING_PLACEHOLDER`, `POSITIONING_EXAMPLE`, `SHORT_DESCRIPTION_HINT`, `SLOGAN_HINT` e `RECOMMENDED_LABEL` são exportados.
    - `SLOGAN_HINT` contém `"se sua loja já utiliza um"`; `POSITIONING_EXAMPLE` contém `"[categoria]"`, `"[público]"` e `"[diferencial]"`.
    - `npx tsc -p tsconfig.typecheck.json --noEmit` sem erros novos.
  </acceptance_criteria>
  <verify>
    <automated>rg -c "use client|from \"react\"|server-only" "src/lib/store-onboarding/field-guidance.ts"; if ($LASTEXITCODE -ne 0) { "OK: modulo puro" } else { "FALHA: dependencia de runtime" }</automated>
  </verify>
  <done>Módulo puro da loja exporta todos os rótulos, hints, exemplo expansível e as 8 descrições de tom de voz, sem dependência de runtime.</done>
</task>

<task type="auto">
  <name>Task 2: Criar o módulo puro de conteúdo da campanha com o feedback dinâmico de preço</name>
  <files>src/lib/campaign/field-guidance.ts</files>
  <read_first>
    - `src/components/flow/use-campaign-form.ts:405-427` (`buildMandatoryArtworkText` e `inferIntent` — contrato real dos 4 estados)
    - `src/components/flow/campaign-input-form.tsx:427-432` (bloco permanente das 3 regras a substituir)
    - `src/components/flow/campaign-input-form.tsx:540-555` (expressão real de `availableOptions` a espelhar)
    - `src/components/campaign/mandatory-artwork-field.tsx` (arquivo inteiro)
    - `openspec/changes/fase-49-ativacao-orientacao-contextual-campos/specs/campaign-field-orientation/spec.md` (requirements 1–5)
    - `.planning/phases/49-ativacao-orientacao-contextual-campos/49-PATTERNS.md` (bloco "No Analog Found" — feedback de preço)
  </read_first>
  <action>
    Criar `src/lib/campaign/field-guidance.ts` como módulo **puro** (sem `"use client"`, sem React, sem `server-only`) com header JSDoc citando F49 e D8/D9/D10/D11/D14. Exportar:
    - `PRODUCT_DESCRIPTION_LABEL` = `"Descrição do produto"`; `PRODUCT_DESCRIPTION_HINT` = `"Descreva características, benefícios ou formas de uso que ajudam a apresentar o produto na comunicação da campanha."`; `PRODUCT_DESCRIPTION_PLACEHOLDER` = `"Ex.: Tênis leve para corrida e uso diário, com solado antiderrapante."`
    - `ORIGINAL_PRICE_LABEL` = `"Preço anterior (original)"`; `DISCOUNTED_PRICE_LABEL` = `"Preço de venda (final)"`; `ORIGINAL_PRICE_HINT` e `DISCOUNTED_PRICE_HINT` (valor "de" riscado × valor que o cliente paga).
    - `PRICE_HELP_TITLE` = `"Como os preços mudam a campanha?"`; `PRICE_HELP_RULES: readonly string[]` com exatamente as 3 regras reais: `"Preço anterior + preço de venda = Oferta"`, `"Somente preço de venda = Oferta ou Destaque"`, `"Sem nenhum preço preenchido = Destaque ou Exclusividade"`.
    - `MANDATORY_ARTWORK_LABEL` = `"Informações obrigatórias na arte"`; `MANDATORY_ARTWORK_HINT` = `"Informe características ou detalhes que precisam aparecer na imagem. Use uma linha para cada item."`; `MANDATORY_ARTWORK_PLACEHOLDER` = string com quebras de linha reais: `"Intensidade 8\nTorra clássica\nPeso líquido 500 g"`.
    - Importar `formatCurrencyBRL` de `@/lib/formatters` (helper puro já existente no repo — **não** criar um formatador novo nem duplicar `Intl.NumberFormat`). O módulo continua puro (sem JSX, sem side-effects).
    - `priceFeedbackMessage(originalPriceCents: number, discountedPriceCents: number | undefined | null): string` — função pura com os 4 estados, espelhando exatamente a classificação de `inferIntent` (`use-campaign-form.ts:417-427`):
      1. original > 0 **e** discounted > 0 → `"A campanha será apresentada como oferta: de " + formatCurrencyBRL(original) + " por " + formatCurrencyBRL(discounted) + "."`
      2. apenas discounted > 0 → `"Com apenas o preço de venda, você poderá escolher entre Oferta e Destaque."`
      3. original > 0 **e** discounted ausente/0 → `"Informe o preço de venda para completar a oferta."` (mensagem neutra — **não** usar "Sem preço...")
      4. nenhum preço → `"Sem preço, a campanha será de Destaque ou Exclusividade."`
    Não importar `inferIntent`/`availableOptions` de `@/components/flow/use-campaign-form` (fence: dependência de módulo client). Não criar validação nem bloquear nada.
  </action>
  <acceptance_criteria>
    - `rg -c "use client|from \"react\"|server-only|use-campaign-form" src/lib/campaign/field-guidance.ts` retorna 0.
    - `priceFeedbackMessage(10000, 8000)` é exatamente `"A campanha será apresentada como oferta: de " + formatCurrencyBRL(10000) + " por " + formatCurrencyBRL(8000) + "."` (usar a composição, **não** um literal com espaço simples — o `Intl` pt-BR usa espaço não separável U+00A0).
    - `priceFeedbackMessage(10000, undefined)` retorna exatamente `"Informe o preço de venda para completar a oferta."`.
    - `priceFeedbackMessage(0, 8000)` retorna exatamente `"Com apenas o preço de venda, você poderá escolher entre Oferta e Destaque."`.
    - `priceFeedbackMessage(0, undefined)` retorna exatamente `"Sem preço, a campanha será de Destaque ou Exclusividade."`.
    - `MANDATORY_ARTWORK_PLACEHOLDER` contém 2 caracteres `\n` (3 linhas).
    - `PRICE_HELP_RULES` tem exatamente 3 itens.
  </acceptance_criteria>
  <verify>
    <automated>rg -c "use client|from \"react\"|server-only|use-campaign-form" "src/lib/campaign/field-guidance.ts"; if ($LASTEXITCODE -ne 0) { "OK: modulo puro" } else { "FALHA: dependencia indevida" }</automated>
  </verify>
  <done>Módulo puro da campanha exporta labels, hints, regras, placeholder multi-linha e o feedback de 4 estados, sem dependência de módulo client.</done>
</task>

<task type="auto">
  <name>Task 3: Testes unitários dos dois módulos de conteúdo</name>
  <files>src/lib/store-onboarding/__tests__/field-guidance.test.ts, src/lib/campaign/__tests__/field-guidance.test.ts</files>
  <read_first>
    - `src/lib/store-onboarding/__tests__/reason-text.test.ts` (arquivo inteiro — estilo de teste puro: `describe`/`it`/`expect`, import relativo, sem jsdom)
    - `src/lib/store-onboarding/field-guidance.ts` (criado na Task 1)
    - `src/lib/campaign/field-guidance.ts` (criado na Task 2)
    - `openspec/changes/fase-49-ativacao-orientacao-contextual-campos/tasks.md` (item 2.4)
  </read_first>
  <action>
    Criar os dois arquivos de teste unitário **puros** (sem `@vitest-environment jsdom`, sem Testing Library), seguindo o estilo de `reason-text.test.ts` e usando import relativo (`../field-guidance`). Cobrir:
    - Loja: as 8 chaves de `TONE_OF_VOICE_DESCRIPTIONS` existem e nenhuma é vazia; `TONE_OF_VOICE_HINT`, `STORE_NAME_HINT`, `POSITIONING_LABEL`, `POSITIONING_EXAMPLE` e `SLOGAN_HINT` não vazios; `SLOGAN_HINT` contém `"se sua loja já utiliza um"`; `POSITIONING_EXAMPLE` contém os 3 placeholders entre colchetes.
    - Campanha: `priceFeedbackMessage` nos 4 estados exatos (dois preços com valores BRL formatados; só venda; só anterior → mensagem neutra e **não** contém `"Sem preço"`; nenhum → mensagem de Destaque/Exclusividade); `PRICE_HELP_RULES` com 3 itens; `MANDATORY_ARTWORK_PLACEHOLDER` com 3 linhas (`split("\n").length === 3`); todas as strings de label/hint não vazias.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/store-onboarding/__tests__/field-guidance.test.ts src/lib/campaign/__tests__/field-guidance.test.ts` termina verde.
    - O teste do estado "só anterior" afirma explicitamente `not.toContain("Sem preço")`.
    - Nenhum dos dois arquivos de teste usa `jsdom` ou Testing Library.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/store-onboarding/__tests__/field-guidance.test.ts src/lib/campaign/__tests__/field-guidance.test.ts</automated>
  </verify>
  <done>Ambos os módulos têm testes unitários verdes cobrindo os 4 estados de preço, as 8 descrições de tom de voz e as strings obrigatórias.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nenhuma nova | Planos de conteúdo puro: nenhuma rota, nenhuma persistência, nenhuma chamada de IA e nenhuma entrada de usuário processada. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-02 | Tampering | Strings de orientação | mitigate | Conteúdo tipado por união literal (`Record<StoreToneOfVoice, string>`) — chave inválida falha em `tsc`; sem interpolação de entrada do usuário. |
| T-49-04 | Information Disclosure | Microcopy de ajuda | mitigate | Textos descrevem apenas efeitos reais verificados no inventário (49-01); nenhum detalhe interno de pipeline ou segredo é exposto. |
| T-49-05 | Denial of Service | Módulo de conteúdo | mitigate | Módulo puro sem side-effects; função de feedback é O(1) e não aloca estado. |
| T-49-06 | Elevation of Privilege | Feedback de preço | mitigate | `priceFeedbackMessage` é somente leitura derivada — não cria validação, não bloqueia submit e não altera `inferIntent`. |
| T-49-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma dependência nova; formatação via helper puro já existente `formatCurrencyBRL` de `@/lib/formatters`. |
</threat_model>

<verification>
- `rg -c "use client|from \"react\"|server-only"` = 0 nos dois módulos novos.
- `npx vitest run src/lib/store-onboarding/__tests__/field-guidance.test.ts src/lib/campaign/__tests__/field-guidance.test.ts` verde.
- Nenhum arquivo de produção existente foi modificado neste plano.
</verification>

<success_criteria>
- Fonte única da microcopy disponível para os componentes (49-04/49-05/49-06) e para os testes (49-07/49-08).
- Feedback de preço cobre os 4 estados com o estado neutro correto.
- Zero dependência de módulo client nos módulos puros.
</success_criteria>

<output>
Create `.planning/phases/49-ativacao-orientacao-contextual-campos/49-02-SUMMARY.md` when done.
</output>

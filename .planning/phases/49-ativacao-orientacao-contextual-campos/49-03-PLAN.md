---
phase: 49-ativacao-orientacao-contextual-campos
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/field-hint.tsx
  - src/components/ui/expandable-help.tsx
  - src/components/ui/recommended-badge.tsx
  - src/__tests__/components/ui/field-hint.test.tsx
  - src/__tests__/components/ui/expandable-help.test.tsx
  - src/__tests__/components/ui/recommended-badge.test.tsx
autonomous: true
requirements:
  - contextual-field-help
must_haves:
  truths:
    - "Existe um primitivo local de hint inline associável por id"
    - "Existe um primitivo local de ajuda expansível, colapsado por padrão, acionável por teclado, com aria-expanded coerente e touch target >= 44px"
    - "Existe um indicador textual 'Recomendado' que não depende apenas de cor"
    - "O padrão de obrigatoriedade acessível está definido: aria-required='true' sem o atributo nativo required"
  artifacts:
    - path: "src/components/ui/field-hint.tsx"
      provides: "Hint inline apresentacional associável por id"
      exports: ["FieldHint"]
    - path: "src/components/ui/expandable-help.tsx"
      provides: "Disclosure acessível colapsado por padrão"
      exports: ["ExpandableHelp"]
    - path: "src/components/ui/recommended-badge.tsx"
      provides: "Indicador textual 'Recomendado'"
      exports: ["RecommendedBadge"]
    - path: "src/__tests__/components/ui/expandable-help.test.tsx"
      provides: "Testes de estado/teclado/aria do disclosure"
  key_links:
    - from: "src/components/ui/expandable-help.tsx"
      to: "região revelada"
      via: "aria-controls + aria-expanded"
      pattern: "aria-expanded"
    - from: "src/components/ui/field-hint.tsx"
      to: "campo consumidor"
      via: "id fornecido pelo campo (o campo é dono do id)"
      pattern: "id=\\{id\\}"
---

<objective>
Criar os três primitivos locais mínimos de ajuda de campo (D2/D3/D13) e definir o padrão acessível de obrigatoriedade.

Purpose: Padrão reutilizável e declarativo — sem API genérica de formulário, sem context, sem registry de campos. Evita tooltip exclusivo e listas permanentes de regras.
Output: `FieldHint`, `ExpandableHelp`, `RecommendedBadge` em `src/components/ui/` e os testes jsdom dos três.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-ativacao-orientacao-contextual-campos/49-UI-SPEC.md
@.planning/phases/49-ativacao-orientacao-contextual-campos/49-PATTERNS.md
@openspec/changes/fase-49-ativacao-orientacao-contextual-campos/specs/contextual-field-help/spec.md
@src/app/(app)/admin/laboratorio/_components/lab-textarea.tsx
@src/components/shell/account-menu.tsx
@src/components/ui/badge.tsx
</context>

<interfaces>
<!-- Padrões exatos extraídos do código real. -->

De `src/app/(app)/admin/laboratorio/_components/lab-textarea.tsx:29-35` (ids + join de describedby):
```typescript
const generatedId = useId();
const textareaId = id ?? generatedId;
const errorId = `${textareaId}-error`;
const hintId = `${textareaId}-hint`;
const describedBy = [error ? errorId : null, hint ? hintId : null].filter(Boolean).join(" ");
```

De `lab-textarea.tsx:54-58` (hint visível):
```typescript
<p id={hintId} className="text-xs text-text-muted font-body">{hint}</p>
```

De `src/components/shell/account-menu.tsx:41-60` (gatilho aria-expanded + ícone rotativo):
```typescript
<button type="button" aria-haspopup="true" aria-expanded={isOpen} className="inline-flex min-h-[44px] min-w-[44px] items-center justify-center ...">
  <ChevronDown className={`h-4 w-4 transition-transform ${isOpen ? "rotate-180" : ""}`} />
</button>
{isOpen && (<div className="...">{/* região */}</div>)}
```

De `src/components/flow/store-identity-form.tsx:1648-1652` (precedente de estado textual no label):
```typescript
<span className="font-normal normal-case tracking-normal text-text-disabled">(opcional)</span>
```

**Sem utilitário `cn` no repositório** — concatenar classes por template literal.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Criar FieldHint e RecommendedBadge</name>
  <files>src/components/ui/field-hint.tsx, src/components/ui/recommended-badge.tsx</files>
  <read_first>
    - `src/app/(app)/admin/laboratorio/_components/lab-textarea.tsx` (arquivo inteiro — estilo do primitivo de campo)
    - `src/components/ui/badge.tsx:1-23` (padrão de classes e variantes)
    - `src/components/flow/store-identity-form.tsx:1648-1652` (precedente `(opcional)` textual no label)
    - `.planning/phases/49-ativacao-orientacao-contextual-campos/49-UI-SPEC.md` (§Typography, §Color)
    - `.planning/phases/49-ativacao-orientacao-contextual-campos/49-PATTERNS.md` (blocos "field-hint.tsx" e "Indicador Recomendado")
  </read_first>
  <action>
    Criar `src/components/ui/field-hint.tsx`: componente **apresentacional** (sem `"use client"` desnecessário, sem estado) com props `{ id: string; children: React.ReactNode; className?: string }`. Renderiza `<p id={id} className={\`text-xs text-text-muted font-body mt-1 ${className ?? ""}\`}>{children}</p>`. O componente **não** gera `useId` — o campo consumidor é dono do id (`name-hint`, `tone_of_voice-hint`, `price-feedback`, etc.), preservando os `id` canônicos dos formulários e os `getByLabelText` existentes.
    Criar `src/components/ui/recommended-badge.tsx`: `RecommendedBadge` que renderiza `<span className="ml-1.5 font-heading font-medium text-[11px] uppercase tracking-wide text-accent-blue">Recomendado</span>` (ou variante com o mesmo token acessível). O texto "Recomendado" é visível — nunca comunicar estado apenas por cor. Não alterar `src/components/ui/badge.tsx`.
  </action>
  <acceptance_criteria>
    - `src/components/ui/field-hint.tsx` exporta `FieldHint` e **não** contém `useId`.
    - `FieldHint` aplica o `id` recebido ao elemento `<p>` (atributo `id={id}`).
    - `RecommendedBadge` renderiza o texto literal `Recomendado`.
    - `npx tsc -p tsconfig.typecheck.json --noEmit` sem erros novos.
  </acceptance_criteria>
  <verify>
    <automated>rg -c "useId" "src/components/ui/field-hint.tsx"; if ($LASTEXITCODE -ne 0) { "OK: id vem do campo" } else { "FALHA: FieldHint nao deve gerar useId" }</automated>
  </verify>
  <done>Os dois primitivos existem, exportados e tipados, com o texto de estado visível e sem dependência de cor.</done>
</task>

<task type="auto">
  <name>Task 2: Criar ExpandableHelp acessível (colapsado por padrão)</name>
  <files>src/components/ui/expandable-help.tsx</files>
  <read_first>
    - `src/components/shell/account-menu.tsx:41-60` (button `aria-expanded` + região condicional + ChevronDown)
    - `src/app/(app)/admin/laboratorio/_components/evaluation-form.tsx:292-307` (alternativa `<details>/<summary>` já usada no repo)
    - `.planning/phases/49-ativacao-orientacao-contextual-campos/49-PATTERNS.md` (bloco "expandable-help.tsx")
    - `.planning/phases/49-ativacao-orientacao-contextual-campos/49-UI-SPEC.md` (§Interaction Contract)
  </read_first>
  <action>
    Criar `src/components/ui/expandable-help.tsx` com `"use client"` e props `{ summary: string; children: React.ReactNode; id?: string; className?: string }`. Implementar com `<button type="button">` + região condicional (padrão preferido do PATTERNS para testabilidade por role e `aria-expanded`):
    - estado `const [isOpen, setIsOpen] = useState(false)` — **colapsado por padrão**;
    - `const generatedId = useId(); const regionId = id ?? generatedId;`
    - gatilho: `className="inline-flex min-h-[44px] items-center gap-1.5 text-xs font-heading uppercase tracking-wider text-text-secondary hover:text-text-primary transition-colors duration-200 focus:outline-none focus-visible:ring-2 focus-visible:ring-accent-blue/20 cursor-pointer"`, `aria-expanded={isOpen}`, `aria-controls={regionId}`;
    - ícone `ChevronDown` de `lucide-react` com `aria-hidden="true"` e rotação `rotate-180` quando aberto;
    - região revelada renderizada **apenas** quando `isOpen` (`{isOpen && <div id={regionId} className="mt-2 ...">{children}</div>}`), de modo que o conteúdo não exista no DOM colapsado.
    Touch target ≥ 44px garantido pelo `min-h-[44px]`. **Proibido:** tooltip como único veículo, lista permanente de regras, altura permanente quando colapsado.
  </action>
  <acceptance_criteria>
    - O componente usa `<button type="button">` com `aria-expanded` e `aria-controls` apontando para o id da região.
    - Estado inicial é colapsado (`useState(false)`) e o conteúdo não é renderizado no DOM colapsado.
    - O gatilho tem `min-h-[44px]` e foco visível (`focus-visible:ring-2`).
    - `npx tsc -p tsconfig.typecheck.json --noEmit` sem erros novos.
  </acceptance_criteria>
  <verify>
    <automated>rg -c "aria-expanded|aria-controls|min-h-\[44px\]|useState\(false\)" "src/components/ui/expandable-help.tsx"</automated>
  </verify>
  <done>Disclosure acessível criado, colapsado por padrão, acionável por teclado e com touch target de 44px.</done>
</task>

<task type="auto">
  <name>Task 3: Testes dos primitivos e definição do padrão de obrigatoriedade acessível</name>
  <files>src/__tests__/components/ui/field-hint.test.tsx, src/__tests__/components/ui/expandable-help.test.tsx, src/__tests__/components/ui/recommended-badge.test.tsx</files>
  <read_first>
    - `src/__tests__/components/ui/input.test.tsx` (estilo jsdom dos primitivos de `src/components/ui/`)
    - `src/__tests__/components/shell/account-menu.test.tsx:60-90` (estilo de asserção `aria-expanded`)
    - `openspec/changes/fase-49-ativacao-orientacao-contextual-campos/specs/contextual-field-help/spec.md` (requirements de acessibilidade e obrigatoriedade)
    - `.planning/phases/49-ativacao-orientacao-contextual-campos/49-PATTERNS.md` (bloco "Estilo de teste de disclosure")
  </read_first>
  <action>
    Criar os três testes jsdom:
    - `field-hint.test.tsx`: renderiza `FieldHint` com `id="x-hint"` e afirma que o elemento `<p>` tem `id="x-hint"` e o texto visível.
    - `recommended-badge.test.tsx`: renderiza `RecommendedBadge` e afirma o texto `"Recomendado"` visível (via `getByText`).
    - `expandable-help.test.tsx`: afirma `aria-expanded="false"` inicial e ausência do conteúdo; após `fireEvent.click` no gatilho, `aria-expanded="true"` e conteúdo presente; após novo clique, volta a `"false"`; `aria-controls` referencia o id da região; acionável por teclado (`fireEvent.keyDown`/`fireEvent.click` no botão focado) e o gatilho tem `min-h-[44px]` na className.
    Além disso, **definir** o padrão acessível de obrigatoriedade (D3): documentar no header JSDoc de `field-hint.tsx` ou em comentário do teste que campos obrigatórios recebem `aria-required="true"` **sem** adicionar o atributo nativo `required`, preservando `noValidate` + validação controlada. A aplicação concreta acontece nos planos 49-04/49-05 e é verificada no plano 49-07.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/components/ui/field-hint.test.tsx src/__tests__/components/ui/expandable-help.test.tsx src/__tests__/components/ui/recommended-badge.test.tsx` verde.
    - O teste de `ExpandableHelp` cobre explicitamente estado inicial colapsado, abertura, fechamento e `aria-expanded` coerente.
    - O padrão `aria-required="true"` sem `required` nativo está registrado como convenção rastreável (comentário/JSDoc) para aplicação nos campos.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/__tests__/components/ui/field-hint.test.tsx src/__tests__/components/ui/expandable-help.test.tsx src/__tests__/components/ui/recommended-badge.test.tsx</automated>
  </verify>
  <done>Os três primitivos têm testes verdes cobrindo associação por id, estado do disclosure e estado textual recomendado; o padrão de obrigatoriedade acessível está definido.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nenhuma nova | Componentes de apresentação locais. Nenhuma rota, nenhuma persistência, nenhum dado de usuário processado, nenhuma chamada de IA. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-02 | Tampering | `ExpandableHelp` / `FieldHint` | mitigate | Conteúdo recebido via props React (texto escapado); sem `dangerouslySetInnerHTML`; nenhuma interpolação de entrada do usuário. |
| T-49-05 | Denial of Service | Disclosure | mitigate | Conteúdo não é montado no DOM quando colapsado — reduz peso e evita listas permanentes. |
| T-49-06 | Elevation of Privilege | Estado de obrigatoriedade | mitigate | O padrão exige `aria-required="true"` **sem** `required` nativo: nenhuma validação nativa nova, nenhum bypass do `noValidate` controlado. |
| T-49-04 | Information Disclosure | Microcopy dos primitivos | mitigate | Os primitivos não carregam conteúdo próprio — apenas renderizam o texto recebido (fonte única nos módulos de conteúdo). |
| T-49-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma dependência nova; apenas `lucide-react` já existente. |
</threat_model>

<verification>
- `npx vitest run src/__tests__/components/ui/` verde.
- `rg -c "useId" src/components/ui/field-hint.tsx` = 0 (id vem do campo).
- Nenhum arquivo existente de `src/components/ui/` foi modificado.
</verification>

<success_criteria>
- Primitivos mínimos e declarativos disponíveis para os formulários.
- Ajuda expansível colapsada por padrão, acessível e com touch target de 44px.
- Nenhuma API genérica de formulário, context ou registry introduzida.
</success_criteria>

<output>
Create `.planning/phases/49-ativacao-orientacao-contextual-campos/49-03-SUMMARY.md` when done.
</output>

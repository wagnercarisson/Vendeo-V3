---
phase: 49-ativacao-orientacao-contextual-campos
plan: 04
type: execute
wave: 2
depends_on:
  - 49-02
  - 49-03
files_modified:
  - src/components/flow/store-identity-form.tsx
autonomous: true
requirements:
  - store-identity-ui
  - store-field-orientation
must_haves:
  truths:
    - "O lojista entende, no próprio campo, que 'Nome da Loja' é o nome público e que CNPJ/Razão Social/Nome Fantasia são dados fiscais separados"
    - "O lojista entende para que serve o tom de voz e vê a descrição contextual da opção selecionada"
    - "O lojista diferencia posicionamento, descrição curta e slogan no ponto de decisão, com exemplo positivo em ajuda expansível"
    - "Campos recomendados e opcionais são sinalizados por texto; o Slogan não é marcado como recomendado"
    - "Validação, auto-save, draft, drift, lookup fiscal e computeTabUnlock permanecem inalterados"
  artifacts:
    - path: "src/components/flow/store-identity-form.tsx"
      provides: "Subseção Dados fiscais, microcopy de Nome da Loja, Tom de Voz com descrição contextual, Posicionamento renomeado com ajuda expansível e distinção descrição curta/slogan"
      contains: "Dados fiscais"
  key_links:
    - from: "src/components/flow/store-identity-form.tsx"
      to: "src/lib/store-onboarding/field-guidance.ts"
      via: "import das strings de orientação (fonte única)"
      pattern: "field-guidance"
    - from: "src/components/flow/store-identity-form.tsx"
      to: "src/lib/store-onboarding/tabs.ts"
      via: "regra real needs_tone_of_voice refletida (não alterada)"
      pattern: "computeTabUnlock"
---

<objective>
Aplicar a orientação contextual na aba Dados e na aba Posicionamento do formulário `/loja` (D3/D4/D5/D6/D7), usando os módulos de conteúdo (49-02) e os primitivos de ajuda (49-03).

Purpose: Ensinar no ponto de decisão — separar dados fiscais do nome público, explicar o tom de voz crítico com descrição por opção, tornar o posicionamento compreensível e distinguir posicionamento/descrição curta/slogan.
Output: `src/components/flow/store-identity-form.tsx` com os novos rótulos, hints, descrição contextual, ajuda expansível e indicadores de estado — sem qualquer mudança de comportamento.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-ativacao-orientacao-contextual-campos/49-CONTEXT.md
@.planning/phases/49-ativacao-orientacao-contextual-campos/49-PATTERNS.md
@.planning/phases/49-ativacao-orientacao-contextual-campos/49-UI-SPEC.md
@openspec/changes/fase-49-ativacao-orientacao-contextual-campos/specs/store-field-orientation/spec.md
@openspec/changes/fase-49-ativacao-orientacao-contextual-campos/specs/store-identity-ui/spec.md
@src/components/flow/store-identity-form.tsx
@src/lib/store-onboarding/field-guidance.ts
@src/lib/store-onboarding/tabs.ts
@src/lib/store-onboarding/reason-text.ts
</context>

<interfaces>
<!-- Contratos e blocos reais extraídos do arquivo a modificar. -->

De `src/components/flow/store-identity-form.tsx`:
```typescript
// :37-46 — manter o array como está
const TONE_OF_VOICE_OPTIONS = [
  { value: 'profissional', label: 'Profissional' }, { value: 'moderno', label: 'Moderno' },
  { value: 'elegante', label: 'Elegante' }, { value: 'divertido', label: 'Divertido' },
  { value: 'acolhedor', label: 'Acolhedor' }, { value: 'jovem', label: 'Jovem' },
  { value: 'tradicional', label: 'Tradicional' }, { value: 'luxuoso', label: 'Luxuoso' },
] as const;

// :1328-1342 — classes compartilhadas (reusar, não recriar)
const inputClass = (field: string) => `w-full bg-bg-surface border rounded-lg min-h-[44px] px-3.5 py-2.5 ...`;
const labelClass = "block text-text-muted text-xs font-heading font-medium uppercase tracking-wider mb-2";

// :1629 — label do nome público
<label htmlFor="name" className={labelClass}>Nome da Loja *</label>

// :1607-1622 — atalhos fiscais (PRESERVAR exatamente)
// "Usar nome fantasia como nome da loja" / "Usar razão social como nome da loja"

// :1958 / :1967 / :1971 / :1975 — labels da aba Posicionamento
// Tom de Voz / Posicionamento / Descrição Curta / Slogan
```

De `src/lib/store-onboarding/tabs.ts:93-94` (fence — não alterar):
```typescript
if (!ctx.toneOfVoice.trim()) { return { unlocked: false, reason: "needs_tone_of_voice" }; }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Aba Dados — subseção "Dados fiscais" e microcopy de Nome da Loja</name>
  <files>src/components/flow/store-identity-form.tsx</files>
  <read_first>
    - `src/components/flow/store-identity-form.tsx:1320-1350` (`inputClass`/`labelClass`)
    - `src/components/flow/store-identity-form.tsx:1495-1640` (CNPJ, Razão Social, Nome Fantasia, atalhos fiscais e Nome da Loja)
    - `src/lib/store-onboarding/field-guidance.ts` (`STORE_NAME_HINT`, `FISCAL_SECTION_LABEL`, `FISCAL_SECTION_HELPER`)
    - `.planning/phases/49-ativacao-orientacao-contextual-campos/49-PATTERNS.md` (blocos 4 e 5 de `store-identity-form.tsx`)
  </read_first>
  <action>
    Na aba Dados:
    1. Adicionar um import do módulo `@/lib/store-onboarding/field-guidance` (junto ao import existente de `reason-text`, após a linha 16), importar `FieldHint` de `@/components/ui/field-hint` e adicionar `useId` ao import de `react` (`:11`).
    2. No topo do componente (junto aos demais hooks, **nunca** dentro de JSX/condicional), declarar os ids de ajuda via `useId`: `const nameHelpBase = useId();` → `nameHintId = \`${nameHelpBase}-hint\``, `nameErrorId = \`${nameHelpBase}-error\``; `const segmentHelpBase = useId();` → `segmentErrorId = \`${segmentHelpBase}-error\``. Os `id` **dos próprios campos** permanecem estáticos (`name`, `segment`) — preservam `htmlFor`/`getByLabelText`; apenas os ids de ajuda vêm de `useId`.
    3. Envolver o agrupamento de `CNPJ`, `Razão Social` e `Nome Fantasia` (bloco `:1495-1604`, incluindo os atalhos `:1607-1622`) em um wrapper de subseção com heading `<h3 className="text-text-muted text-xs font-heading font-medium uppercase tracking-wider mb-3">{FISCAL_SECTION_LABEL}</h3>` seguido de `<FieldHint id="fiscal-section-helper">{FISCAL_SECTION_HELPER}</FieldHint>` (helper de **seção**, sem associação a campo — id estático é aceitável). Manter os campos dentro da aba Dados e o lookup/read-only/fallback intactos.
    4. Abaixo do input `#name` (`:1629-1634`), inserir `<FieldHint id={nameHintId}>{STORE_NAME_HINT}</FieldHint>`; no input `#name`, montar `aria-describedby={[nameHintId, touched.name && fieldErrors.name ? nameErrorId : null].filter(Boolean).join(" ")}` (hint + erro), `aria-required="true"` e `aria-invalid={touched.name && fieldErrors.name ? true : undefined}`. No `<p>` de erro existente de `name` (`:1631-1633`), adicionar `id={nameErrorId}` preservando classes/ícone.
    5. No select `#segment`: adicionar `aria-required="true"`, montar `aria-describedby={hasSegmentError ? segmentErrorId : undefined}` e `aria-invalid={hasSegmentError ? true : undefined}` (com `hasSegmentError` = condição real de erro existente de `segment`), e adicionar `id={segmentErrorId}` ao `<p>` de erro existente de `segment`, preservando classes/ícone. **Sem** `required` nativo.
    6. **Não** adicionar o atributo nativo `required` em nenhum campo. **Não** alterar `setField`, `handleBlur`, `validateCnpj`, o lookup de CNPJ nem a lógica de draft/readiness.
  </action>
  <acceptance_criteria>
    - A string `"Dados fiscais"` aparece como heading da subseção e `STORE_NAME_HINT` é renderizado abaixo do `#name`.
    - O input `#name` tem `aria-describedby` **não vazio** cujo conteúdo referencia o id do hint (`STORE_NAME_HINT`) e, quando há erro, o id do erro; tem `aria-required="true"` e `aria-invalid` apenas quando há erro.
    - O select `#segment` tem `aria-required="true"` e, quando há erro, `aria-describedby` apontando para o id do erro e `aria-invalid`; **sem** `required` nativo.
    - Os `<p>` de erro de `#name` e `#segment` têm `id` correspondente ao respectivo `errorId` derivado de `useId` (não literal).
    - Os textos `"Usar nome fantasia como nome da loja"` e `"Usar razão social como nome da loja"` permanecem idênticos e com os mesmos handlers `setField("name", ...)`.
    - Nenhum `required` nativo novo: `git diff src/components/flow/store-identity-form.tsx` não introduz o atributo `required` em `<input>`/`<select>`.
    - `getByLabelText(/Nome da Loja/i)` continua funcionando (label inalterado).
  </acceptance_criteria>
  <verify>
    <automated>rg -c "STORE_NAME_HINT|FISCAL_SECTION_LABEL|Usar nome fantasia como nome da loja" "src/components/flow/store-identity-form.tsx"; rg -c "useId|aria-describedby|aria-required" "src/components/flow/store-identity-form.tsx"</automated>
  </verify>
  <done>Dados fiscais agrupados como subseção com helper, Nome da Loja com microcopy de nome público, ids de ajuda via `useId` compondo `aria-describedby` (hint + erro), `aria-required` sem `required` nativo e atalhos preservados.</done>
</task>

<task type="auto">
  <name>Task 2: Aba Posicionamento — Tom de Voz com hint e descrição contextual por opção</name>
  <files>src/components/flow/store-identity-form.tsx</files>
  <read_first>
    - `src/components/flow/store-identity-form.tsx:37-46` (`TONE_OF_VOICE_OPTIONS`)
    - `src/components/flow/store-identity-form.tsx:1945-1985` (card informativo, Tom de Voz, Posicionamento, Descrição Curta, Slogan)
    - `src/lib/store-onboarding/tabs.ts:85-101` (`computeTabUnlock` — regra real)
    - `src/lib/store-onboarding/reason-text.ts:34-35` (texto de bloqueio)
    - `src/lib/store-onboarding/field-guidance.ts` (`TONE_OF_VOICE_HINT`, `TONE_OF_VOICE_COMPLEMENTS_HINT`, `TONE_OF_VOICE_DESCRIPTIONS`)
  </read_first>
  <action>
    No bloco do `#tone_of_voice` (`:1958-1966`):
    1. Manter `TONE_OF_VOICE_OPTIONS` e o select inalterados em valor/onChange.
    2. No topo do componente, declarar os ids de ajuda do tom de voz via `useId`: `const toneHelpBase = useId();` → `toneHintId = \`${toneHelpBase}-hint\``, `toneComplementsId = \`${toneHelpBase}-complements\``, `toneDescriptionId = \`${toneHelpBase}-description\``. O `id` do próprio campo permanece estático `tone_of_voice`. (O tom de voz **não** renderiza erro inline hoje — não criar um; se/quando um `<p>` de erro existir, derivar também `toneErrorId` e incluí-lo no `aria-describedby`.)
    3. Abaixo do select, inserir `<FieldHint id={toneHintId}>{TONE_OF_VOICE_HINT}</FieldHint>` e `<FieldHint id={toneComplementsId}>{TONE_OF_VOICE_COMPLEMENTS_HINT}</FieldHint>` (tom complementa, não substitui, segmento/subsegmento).
    4. Renderizar a descrição contextual **apenas quando houver seleção**: `{formData.tone_of_voice !== "" && (<FieldHint id={toneDescriptionId}>{TONE_OF_VOICE_DESCRIPTIONS[formData.tone_of_voice as StoreToneOfVoice]}</FieldHint>)}`. Tipar o acesso ao mapa sem usar `any`.
    5. Montar o `aria-describedby` do select com **todos os textos aplicáveis** (hint + complements + descrição contextual quando houver seleção; e o id de erro quando/quando houver erro inline): `aria-describedby={[toneHintId, toneComplementsId, formData.tone_of_voice !== "" ? toneDescriptionId : null].filter(Boolean).join(" ")}`. Não declarar ids de erro sem uso (evita variável não utilizada no lint).
    6. **Não** alterar `computeTabUnlock`, `reason-text.ts`, a validação de abas ou o fluxo de auto-save.
  </action>
  <acceptance_criteria>
    - Sem seleção de tom (`tone_of_voice === ""`), nenhuma descrição de opção é renderizada e o `aria-describedby` do select contém hint + complements (sem o id de descrição).
    - Com `tone_of_voice = "luxuoso"`, o texto `"Sofisticada, exclusiva e com senso de premium."` é renderizado e o id de descrição entra no `aria-describedby` do select.
    - O `aria-describedby` do select referencia hint + complements + descrição (quando houver seleção), via `.filter(Boolean).join(" ")`.
    - As 8 chaves de `TONE_OF_VOICE_OPTIONS` têm descrição correspondente em `TONE_OF_VOICE_DESCRIPTIONS` (verificável por teste no plano 49-07).
    - `git diff` não altera `src/lib/store-onboarding/tabs.ts` nem `reason-text.ts`.
  </acceptance_criteria>
  <verify>
    <automated>rg -c "TONE_OF_VOICE_DESCRIPTIONS|toneDescriptionId|toneHintId|toneComplementsId|aria-describedby" "src/components/flow/store-identity-form.tsx"</automated>
  </verify>
  <done>Tom de voz com hint sempre visível, complements, descrição contextual exibida apenas quando há seleção e `aria-describedby` composto, sem mudança da regra de desbloqueio.</done>
</task>

<task type="auto">
  <name>Task 3: Aba Posicionamento — Posicionamento, Descrição Curta e Slogan distintos</name>
  <files>src/components/flow/store-identity-form.tsx</files>
  <read_first>
    - `src/components/flow/store-identity-form.tsx:1967-1980` (Posicionamento, Descrição Curta, Slogan)
    - `src/components/flow/store-identity-form.tsx:1648-1652` (precedente `(opcional)` no label)
    - `src/lib/store-onboarding/field-guidance.ts` (`POSITIONING_*`, `SHORT_DESCRIPTION_HINT`, `SLOGAN_HINT`, `RECOMMENDED_LABEL`)
    - `openspec/changes/fase-49-ativacao-orientacao-contextual-campos/specs/store-field-orientation/spec.md` (requirements 3 e 4)
  </read_first>
  <action>
    No bloco `:1967-1980`:
    1. No topo do componente, declarar os ids de ajuda via `useId` para os três campos: `const positioningHelpBase = useId();`, `const shortDescriptionHelpBase = useId();`, `const sloganHelpBase = useId();` → `positioningHintId`, `positioningIdentityId`; `shortDescriptionHintId`; `sloganHintId`. Os `id` dos próprios campos permanecem estáticos (`positioning`, `short_description`, `slogan`). (Esses campos **não** renderizam erro inline hoje — não criar um; se/quando um `<p>` de erro existir, derivar o `errorId` correspondente e incluí-lo no `aria-describedby`.)
    2. `#positioning`: label principal → `POSITIONING_LABEL` ("Como você quer que sua loja seja percebida?") com termo secundário `POSITIONING_SECONDARY_LABEL` ("Posicionamento da marca") num `<span className="ml-1.5 font-normal normal-case tracking-normal text-text-disabled">`; adicionar `<RecommendedBadge />` ao label; placeholder → `POSITIONING_PLACEHOLDER` ("Ex: Somos uma loja de..."); hint `POSITIONING_HINT` (público/proposta/diferencial) + `POSITIONING_IDENTITY_HINT` (efeito direto na copy, indireto no perfil/direção visual — **sem** prometer transformação visual); ajuda expansível `<ExpandableHelp summary={POSITIONING_HELP_TITLE}>{POSITIONING_EXAMPLE}</ExpandableHelp>` (colapsada por padrão). O `aria-describedby` do input deve referenciar **hint + identity hint**: `[positioningHintId, positioningIdentityId].filter(Boolean).join(" ")` (e o erro quando/quando houver).
    3. `#short_description`: adicionar `<RecommendedBadge />` ao label e `<FieldHint id={shortDescriptionHintId}>{SHORT_DESCRIPTION_HINT}</FieldHint>`; `aria-describedby` referenciando o hint (e o erro quando/quando houver).
    4. `#slogan`: manter `(opcional)` via `OPTIONAL_LABEL` e adicionar `<FieldHint id={sloganHintId}>{SLOGAN_HINT}</FieldHint>`; `aria-describedby` referenciando o hint (e o erro quando/quando houver). **Não** adicionar `RecommendedBadge` ao slogan.
    5. **Não** criar validação, regex de intenção ou bloqueio por adjetivo único. **Não** alterar `setField`/`handleBlur`/auto-save/draft/drift.
  </action>
  <acceptance_criteria>
    - O label principal de `#positioning` é `"Como você quer que sua loja seja percebida?"` e `"Posicionamento da marca"` aparece como termo secundário.
    - O `aria-describedby` de `#positioning` referencia **hint + identity hint** (não apenas um id de hint).
    - `POSITIONING_EXAMPLE` é revelado por `ExpandableHelp` e a ajuda inicia colapsada.
    - `#positioning` e `#short_description` exibem o texto `"Recomendado"`; `#slogan` **não** exibe `"Recomendado"` e mantém `(opcional)`.
    - Os `id` de hint derivam de `useId` (não literais); os `id` dos campos permanecem `positioning`/`short_description`/`slogan`.
    - `rg -c "A melhor loja de" src/components/flow/store-identity-form.tsx` retorna 0.
    - Nenhuma validação nova: `git diff` não adiciona regra de erro/regex para `positioning`, `short_description` ou `slogan`.
  </acceptance_criteria>
  <verify>
    <automated>rg -c "POSITIONING_LABEL|POSITIONING_SECONDARY_LABEL|POSITIONING_EXAMPLE|SHORT_DESCRIPTION_HINT|SLOGAN_HINT|aria-describedby" "src/components/flow/store-identity-form.tsx"; rg -c "A melhor loja de" "src/components/flow/store-identity-form.tsx"; if ($LASTEXITCODE -ne 0) { "OK: placeholder antigo removido" } else { "FALHA: placeholder antigo presente" }</automated>
  </verify>
  <done>Posicionamento renomeado com hint + identity hint associados, microcopy de identidade e exemplo expansível; descrição curta recomendada; slogan opcional sem "Recomendado"; placeholder antigo removido.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nenhuma nova | Apresentação de formulário existente. Não cria rota, não altera autenticação/autorização, não altera persistência (`POST/PATCH /api/store`) e não chama IA. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-02 | Tampering | Textos renderizados no formulário | mitigate | Todo o conteúdo vem de `field-guidance.ts` (strings estáticas tipadas); nenhuma interpolação de entrada do usuário em HTML e nenhum `dangerouslySetInnerHTML`. |
| T-49-04 | Information Disclosure | Microcopy de identidade/posicionamento | mitigate | Textos descrevem apenas efeitos reais (copy direto, perfil/direção visual indireto) verificados no inventário de 49-01; nenhum detalhe interno de pipeline. |
| T-49-06 | Elevation of Privilege | Campos obrigatórios e regra de desbloqueio | mitigate | `aria-required="true"` sem `required` nativo; `computeTabUnlock` e `reason-text` intocados — nenhum bypass nem bloqueio novo. |
| T-49-05 | Denial of Service | Altura do formulário | mitigate | Ajuda expansível colapsada por padrão; hint curto de uma linha — sem listas permanentes de regras. |
| T-49-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma dependência nova. |
</threat_model>

<verification>
- `git diff src/components/flow/store-identity-form.tsx` limitado a JSX/rotulagem/hints (sem lógica de validação, save, draft ou drift).
- `npx vitest run src/components/flow/__tests__/store-identity-form.aceite-legal.test.tsx src/components/flow/__tests__/store-tabs.test.tsx` verde (ou co-migrado no 49-11).
- Nenhum arquivo de `src/lib/store-onboarding/` (tabs/reason-text/draft-store) foi modificado.
</verification>

<success_criteria>
- Aba Dados separa dados fiscais do nome público com microcopy e atalhos preservados.
- Tom de voz explicado com descrição contextual por opção.
- Posicionamento compreensível, descrição curta recomendada e slogan opcional, sem validação nova.
- Comportamento de validação, auto-save, draft, drift e desbloqueio inalterado.
</success_criteria>

<output>
Create `.planning/phases/49-ativacao-orientacao-contextual-campos/49-04-SUMMARY.md` when done.
</output>

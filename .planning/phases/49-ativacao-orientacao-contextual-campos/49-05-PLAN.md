---
phase: 49-ativacao-orientacao-contextual-campos
plan: 05
type: execute
wave: 2
depends_on:
  - 49-02
  - 49-03
files_modified:
  - src/components/flow/campaign-input-form.tsx
autonomous: true
requirements:
  - campaign-input-ui
  - campaign-field-orientation
must_haves:
  truths:
    - "O lojista entende que 'Descrição do produto' descreve características/benefícios/uso — e não é uma promoção"
    - "O lojista compreende o significado de 'Preço de venda (final)' e 'Preço anterior (original)'"
    - "As 3 regras de preço não ocupam altura permanente (ajuda expansível colapsada)"
    - "O feedback dinâmico reflete os 4 estados reais de preço, incluindo o estado neutro 'só preço anterior'"
    - "Body, validação, `inferIntent`, `IntentSelector`, `availableOptions` e `ValidityField` permanecem inalterados"
  artifacts:
    - path: "src/components/flow/campaign-input-form.tsx"
      provides: "Descrição do produto, labels de preço com hint, ajuda expansível de preços e feedback dinâmico associado por aria-describedby"
      contains: "Descrição do produto"
  key_links:
    - from: "src/components/flow/campaign-input-form.tsx"
      to: "src/lib/campaign/field-guidance.ts"
      via: "import de labels/hints/regras e priceFeedbackMessage (fonte única)"
      pattern: "field-guidance"
    - from: "src/components/flow/campaign-input-form.tsx"
      to: "src/components/flow/use-campaign-form.ts (inferIntent)"
      via: "feedback espelha a classificação real de 4 estados sem alterá-la"
      pattern: "priceFeedbackMessage"
---

<objective>
Aplicar a orientação contextual nas seções Produto e Oferta do formulário `/campanhas/nova` (D8/D9/D11): "Descrição do produto", labels de preço com significado, ajuda expansível das 3 regras e feedback dinâmico de 4 estados.

Purpose: Tornar o preenchimento compreensível e reduzir erros de natureza de campo, sem tocar o body, a validação, a inferência de intenção ou o pipeline.
Output: `src/components/flow/campaign-input-form.tsx` com a nova orientação — comportamento produtivo idêntico.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-ativacao-orientacao-contextual-campos/49-CONTEXT.md
@.planning/phases/49-ativacao-orientacao-contextual-campos/49-PATTERNS.md
@.planning/phases/49-ativacao-orientacao-contextual-campos/49-UI-SPEC.md
@openspec/changes/fase-49-ativacao-orientacao-contextual-campos/specs/campaign-field-orientation/spec.md
@openspec/changes/fase-49-ativacao-orientacao-contextual-campos/specs/campaign-input-ui/spec.md
@src/components/flow/campaign-input-form.tsx
@src/lib/campaign/field-guidance.ts
@src/components/flow/use-campaign-form.ts
</context>

<interfaces>
<!-- Blocos reais do arquivo a modificar e contratos que NÃO mudam. -->

De `src/components/flow/campaign-input-form.tsx`:
```typescript
// :372-389 — label "Descrição (opcional)" + textarea #description (maxLength={120}) + contador {fields.description.length}/120
// :427-432 — bloco permanente das 3 regras (A SUBSTITUIR por ExpandableHelp)
// :435-464 — label/input #originalPrice (valor exibido displayPriceOriginal, onChange handlePriceOriginalChange)
// :468-494 — label/input #discountedPrice
// :541-553 — IntentSelector com availableOptions (NÃO ALTERAR)
```

De `src/components/flow/use-campaign-form.ts:417-427` (fence — não alterar):
```typescript
export function inferIntent(originalPriceCents: number, discountedPriceCents: number | undefined | null): CampaignIntent;
```

Expressão real de `availableOptions` a espelhar (`:544-553`) — NÃO alterar:
```typescript
const inferred = inferIntent(fields.originalPriceCents, fields.discountedPriceCents);
if (inferred === "offer") return ["offer"];
if (fields.discountedPriceCents !== undefined && (fields.discountedPriceCents ?? 0) > 0) return ["offer", "spotlight"];
return ["spotlight", "exclusive"];
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Seção Produto — "Descrição do produto" com microcopy e placeholder reais</name>
  <files>src/components/flow/campaign-input-form.tsx</files>
  <read_first>
    - `src/components/flow/campaign-input-form.tsx:337-406` (seção Produto, label/textarea/contador)
    - `src/lib/campaign/field-guidance.ts` (`PRODUCT_DESCRIPTION_LABEL`, `PRODUCT_DESCRIPTION_HINT`, `PRODUCT_DESCRIPTION_PLACEHOLDER`)
    - `.planning/phases/49-ativacao-orientacao-contextual-campos/49-PATTERNS.md` (bloco 2 de `campaign-input-form.tsx`)
  </read_first>
  <action>
    Na seção Produto (`:372-389`):
    1. Adicionar import de `@/lib/campaign/field-guidance` e `FieldHint` de `@/components/ui/field-hint` (junto aos imports do topo, `:1-24`).
    2. Trocar o texto do label de `Descrição` para `{PRODUCT_DESCRIPTION_LABEL}` mantendo o `<span>` de `(opcional)` existente.
    3. Trocar o placeholder promocional `Ex: 20% OFF em todo o estoque` por `{PRODUCT_DESCRIPTION_PLACEHOLDER}`.
    4. Inserir `<FieldHint id="description-hint">{PRODUCT_DESCRIPTION_HINT}</FieldHint>` abaixo do contador e adicionar `aria-describedby="description-hint"` à textarea `#description`.
    5. Manter `fields.description`, `maxLength={120}`, o contador `{fields.description.length}/120`, `setField("description", ...)` e `handleBlur("description")`. **Não** exibir aviso negativo sobre composição visual da arte.
  </action>
  <acceptance_criteria>
    - `rg -c "20% OFF em todo o estoque" src/components/flow/campaign-input-form.tsx` retorna 0.
    - O label do `#description` é `"Descrição do produto"` e o contador `{fields.description.length}/120` permanece.
    - A textarea tem `aria-describedby="description-hint"` e `maxLength={120}` intacto.
    - Nenhum texto afirmando que o campo não define a composição visual da arte é adicionado.
  </acceptance_criteria>
  <verify>
    <automated>rg -c "PRODUCT_DESCRIPTION_LABEL|PRODUCT_DESCRIPTION_HINT|PRODUCT_DESCRIPTION_PLACEHOLDER|description-hint" "src/components/flow/campaign-input-form.tsx"; rg -c "20% OFF em todo o estoque" "src/components/flow/campaign-input-form.tsx"; if ($LASTEXITCODE -ne 0) { "OK: placeholder promocional removido" } else { "FALHA: placeholder promocional presente" }</automated>
  </verify>
  <done>Descrição do produto com label, microcopy e placeholder reais; mesmo campo, mesmo limite e mesmo contador.</done>
</task>

<task type="auto">
  <name>Task 2: Seção Oferta — labels de preço com hint e ajuda expansível das 3 regras</name>
  <files>src/components/flow/campaign-input-form.tsx</files>
  <read_first>
    - `src/components/flow/campaign-input-form.tsx:427-494` (bloco das 3 regras + campos de preço)
    - `src/components/flow/campaign-input-form.tsx:541-553` (`availableOptions` — não alterar)
    - `src/lib/campaign/field-guidance.ts` (`ORIGINAL_PRICE_LABEL`, `DISCOUNTED_PRICE_LABEL`, `ORIGINAL_PRICE_HINT`, `DISCOUNTED_PRICE_HINT`, `PRICE_HELP_TITLE`, `PRICE_HELP_RULES`)
    - `.planning/phases/49-ativacao-orientacao-contextual-campos/49-PATTERNS.md` (blocos 3 e 4 de `campaign-input-form.tsx`)
  </read_first>
  <action>
    Na seção Oferta:
    1. Substituir o bloco permanente `:427-432` por `<ExpandableHelp summary={PRICE_HELP_TITLE}>` renderizando os 3 itens de `PRICE_HELP_RULES` (ajuda **colapsada por padrão**). Importar `ExpandableHelp` de `@/components/ui/expandable-help`.
    2. Trocar o label de `#originalPrice` (`:435`) para `{ORIGINAL_PRICE_LABEL}` ("Preço anterior (original)") mantendo o `<span>` de `(opcional)`. Trocar o label de `#discountedPrice` (`:468`) para `{DISCOUNTED_PRICE_LABEL}` ("Preço de venda (final)").
    3. Inserir `<FieldHint id="originalPrice-hint">{ORIGINAL_PRICE_HINT}</FieldHint>` e `<FieldHint id="discountedPrice-hint">{DISCOUNTED_PRICE_HINT}</FieldHint>`, associando com `aria-describedby` em cada input (combinando com o id do feedback quando aplicável — ver Task 3).
    4. Adicionar `aria-required="true"` ao input `#discountedPrice` e aos demais campos obrigatórios para `offer` (badge promocional), **sem** o atributo nativo `required`.
    5. **Não** alterar `IntentSelector` (`:242-292`), `availableOptions` (`:544-553`), `ValidityField`, `setField`, `handleBlur` ou `handlePriceOriginalChange`.
  </action>
  <acceptance_criteria>
    - `rg -c "Os campos de preço definem a intenção da campanha" src/components/flow/campaign-input-form.tsx` retorna 0.
    - `getByLabelText("Preço anterior (original)")` e `getByLabelText("Preço de venda (final)")` funcionam (labels atualizados, `htmlFor`/`id` preservados).
    - As 3 regras de `PRICE_HELP_RULES` não aparecem na renderização inicial (ajuda colapsada) e aparecem após acionar o disclosure.
    - `#discountedPrice` tem `aria-required="true"` e nenhum `required` nativo.
    - `git diff` não altera `IntentSelector`, `availableOptions` nem `ValidityField`.
  </acceptance_criteria>
  <verify>
    <automated>rg -c "ORIGINAL_PRICE_LABEL|DISCOUNTED_PRICE_LABEL|PRICE_HELP_TITLE|PRICE_HELP_RULES|ExpandableHelp" "src/components/flow/campaign-input-form.tsx"; rg -c "Os campos de preço definem a intenção da campanha" "src/components/flow/campaign-input-form.tsx"; if ($LASTEXITCODE -ne 0) { "OK: bloco permanente removido" } else { "FALHA: bloco permanente presente" }</automated>
  </verify>
  <done>Labels de preço com significado, hints associados e as 3 regras em ajuda expansível colapsada por padrão.</done>
</task>

<task type="auto">
  <name>Task 3: Feedback dinâmico de preços e confirmação de body/estado inalterados</name>
  <files>src/components/flow/campaign-input-form.tsx</files>
  <read_first>
    - `src/components/flow/campaign-input-form.tsx:427-500` (ponto de inserção do feedback, após os campos de preço)
    - `src/lib/campaign/field-guidance.ts` (`priceFeedbackMessage`)
    - `src/components/flow/use-campaign-form.ts:405-427` (`buildMandatoryArtworkText`/`inferIntent` — fences)
    - `openspec/changes/fase-49-ativacao-orientacao-contextual-campos/specs/campaign-field-orientation/spec.md` (requirement de preços)
  </read_first>
  <action>
    1. Renderizar o feedback dinâmico como texto derivado (somente leitura): `<FieldHint id="price-feedback">{priceFeedbackMessage(fields.originalPriceCents, fields.discountedPriceCents)}</FieldHint>`, posicionado após os dois campos de preço. Quando o estado for "só preço anterior", aplicar a classe de aviso `text-accent-amber` (orientação, não erro); nos demais estados manter `text-text-muted`.
    2. Associar o feedback aos campos via `aria-describedby` (ex.: `#discountedPrice` → `"discountedPrice-hint price-feedback"`).
    3. **Não** chamar `setField`, **não** criar validação nova, **não** bloquear o botão de submit e **não** alterar `inferIntent`/`availableOptions`. O feedback apenas espelha a classificação real.
    4. Confirmar que nenhum campo novo de estado foi adicionado a `CampaignFormFields` e que `buildCampaignGenerationBody` permanece intocado.
  </action>
  <acceptance_criteria>
    - Com `originalPriceCents=10000, discountedPriceCents=8000` → feedback exatamente `"A campanha será apresentada como oferta: de " + formatCurrencyBRL(10000) + " por " + formatCurrencyBRL(8000) + "."` (comparar pela composição com `@/lib/formatters`, **não** por literal com espaço simples — o `Intl` pt-BR usa U+00A0).
    - Com `originalPriceCents=10000, discountedPriceCents=undefined` → feedback exatamente `"Informe o preço de venda para completar a oferta."` e **não** contém `"Sem preço"`.
    - Com `originalPriceCents=0, discountedPriceCents=8000` → `"Com apenas o preço de venda, você poderá escolher entre Oferta e Destaque."`.
    - Com `originalPriceCents=0, discountedPriceCents=undefined` → `"Sem preço, a campanha será de Destaque ou Exclusividade."`.
    - `git diff src/components/flow/use-campaign-form.ts` vazio (nenhum campo novo, nenhuma mudança de body).
    - Nenhuma validação nova disparada no estado "só anterior"; o avanço não é bloqueado.
  </acceptance_criteria>
  <verify>
    <automated>rg -c "priceFeedbackMessage|price-feedback" "src/components/flow/campaign-input-form.tsx"; git diff --name-only -- src/components/flow/use-campaign-form.ts</automated>
  </verify>
  <done>Feedback dinâmico de 4 estados associado por `aria-describedby`, com mensagem neutra no estado "só anterior" e zero alteração de estado/body.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nenhuma nova | Apresentação de formulário existente. Não cria rota, não altera authz, não altera o body enviado e não chama IA. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-02 | Tampering | Textos e feedback de preço | mitigate | Strings vêm de `field-guidance.ts` (estáticas tipadas); feedback é função pura sobre números — sem interpolação de entrada do usuário em HTML. |
| T-49-04 | Information Disclosure | Microcopy de preços/descrição | mitigate | Textos descrevem apenas efeitos reais (copy/publicação; intenção) — sem revelar detalhes internos do pipeline. |
| T-49-06 | Elevation of Privilege | Campos obrigatórios e feedback | mitigate | `aria-required="true"` sem `required` nativo; feedback não cria validação nem bloqueio — `inferIntent`/`availableOptions` intocados. |
| T-49-05 | Denial of Service | Altura do formulário | mitigate | Lista permanente de 3 regras substituída por disclosure colapsado por padrão. |
| T-49-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma dependência nova. |
</threat_model>

<verification>
- `git diff src/components/flow/campaign-input-form.tsx` limitado a JSX/rotulagem/hints.
- `git diff --name-only` não lista `src/components/flow/use-campaign-form.ts`, `src/components/campaign/validity-field.tsx` nem `src/lib/campaign/brief*.ts`.
- `npx vitest run src/components/flow/__tests__/campaign-input-form-price-helper.test.tsx` (co-migrado no 49-09) verde.
</verification>

<success_criteria>
- Descrição do produto com microcopy/placeholder reais e mesmo contrato.
- Preços com significado explícito, ajuda expansível e feedback dinâmico dos 4 estados.
- Body, validação, inferência de intenção e validade inalterados.
</success_criteria>

<output>
Create `.planning/phases/49-ativacao-orientacao-contextual-campos/49-05-SUMMARY.md` when done.
</output>

---
phase: 49-ativacao-orientacao-contextual-campos
plan: 06
type: execute
wave: 2
depends_on:
  - 49-02
  - 49-03
files_modified:
  - src/components/campaign/mandatory-artwork-field.tsx
  - src/components/flow/campaign-brief-review.tsx
autonomous: true
requirements:
  - mandatory-artwork-text
  - campaign-brief-review
  - campaign-field-orientation
must_haves:
  truths:
    - "O campo 'Informações obrigatórias na arte' permanece diretamente visível, com microcopy positiva e placeholder multi-linha real de produto"
    - "A tela de revisão apresenta o aviso ilustrativo e as informações obrigatórias como itens distintos e rotulados"
    - "Os rótulos de preço da revisão são coerentes com o formulário ('Preço anterior' / 'Preço de venda') e a validade permanece em item próprio"
    - "Transporte `mandatoryArtworkText`, body, snapshot e contrato HTTP permanecem inalterados; a revisão deriva os avisos de `fields` e deixa de chamar `buildMandatoryArtworkText`"
  artifacts:
    - path: "src/components/campaign/mandatory-artwork-field.tsx"
      provides: "Campo visível com label 'Informações obrigatórias na arte', microcopy positiva e placeholder multi-linha"
      contains: "Informações obrigatórias na arte"
    - path: "src/components/flow/campaign-brief-review.tsx"
      provides: "Seção Avisos com categorias separáveis e rótulos de preço alinhados"
      contains: "Informações obrigatórias na arte"
  key_links:
    - from: "src/components/flow/campaign-brief-review.tsx"
      to: "fields.showIllustrativeNotice / fields.mandatoryArtworkTextFree"
      via: "separação derivada dos valores do form (não do texto concatenado)"
      pattern: "mandatoryArtworkTextFree"
    - from: "src/components/campaign/mandatory-artwork-field.tsx"
      to: "src/components/flow/campaign-input-form.tsx"
      via: "mesmo id/value/onChange/maxLength (contrato preservado)"
      pattern: "mandatoryArtworkText"
---

<objective>
Aplicar a orientação no campo de informações obrigatórias da arte (D10) e separar as categorias da revisão do brief (D11/D12), mantendo o contrato de transporte intacto.

Purpose: Tornar o campo positivo e multi-linha e eliminar a ambiguidade da seção Avisos da revisão, sem tocar body, snapshot ou prompts.
Output: `mandatory-artwork-field.tsx` com novo label/microcopy/placeholder e `campaign-brief-review.tsx` com categorias separáveis e rótulos alinhados.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-ativacao-orientacao-contextual-campos/49-CONTEXT.md
@.planning/phases/49-ativacao-orientacao-contextual-campos/49-PATTERNS.md
@.planning/phases/49-ativacao-orientacao-contextual-campos/49-UI-SPEC.md
@openspec/changes/fase-49-ativacao-orientacao-contextual-campos/specs/mandatory-artwork-text/spec.md
@openspec/changes/fase-49-ativacao-orientacao-contextual-campos/specs/campaign-brief-review/spec.md
@src/components/campaign/mandatory-artwork-field.tsx
@src/components/flow/campaign-brief-review.tsx
@src/lib/campaign/field-guidance.ts
@src/lib/campaign/constants.ts
</context>

<interfaces>
<!-- Blocos reais e contratos que NÃO mudam. -->

`src/components/campaign/mandatory-artwork-field.tsx` (arquivo completo, 34 linhas):
```typescript
export function MandatoryArtworkField({ value, onChange }: MandatoryArtworkFieldProps) {
  // <label htmlFor="mandatoryArtworkText">Texto obrigatório na arte (opcional)</label>
  // <textarea id="mandatoryArtworkText" value={value} onChange={(e) => onChange(e.target.value)}
  //   placeholder="Consulte condições na loja. Promoção não cumulativa." maxLength={200} rows={2} />
  // <p>Use para regras, restrições ou informações que precisam aparecer na arte.</p>
}
```

`src/components/flow/campaign-brief-review.tsx`:
```typescript
// :13 — import { buildValidityDisplayText, inferIntent } from "./use-campaign-form"; (buildMandatoryArtworkText deixa de ser importado)
// :69-76 — derivação de intent/validity (a seção Avisos passa a derivar de fields.showIllustrativeNotice/mandatoryArtworkTextFree)
// :148-184 — seção Oferta: dt "Preço original" (:165) e "Preço final" (:173); validity em item próprio (:179-184)
// :227-246 — seção Avisos: bloco único com checkbox + {mandatoryArtworkText ?? "Sem avisos adicionais."}
```

`src/lib/campaign/constants.ts`:
```typescript
export const ILLUSTRATIVE_NOTICE_TEXT = "Imagem meramente ilustrativa";
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: MandatoryArtworkField — label, microcopy positiva e placeholder multi-linha</name>
  <files>src/components/campaign/mandatory-artwork-field.tsx</files>
  <read_first>
    - `src/components/campaign/mandatory-artwork-field.tsx` (arquivo inteiro, 34 linhas)
    - `src/lib/campaign/field-guidance.ts` (`MANDATORY_ARTWORK_LABEL`, `MANDATORY_ARTWORK_HINT`, `MANDATORY_ARTWORK_PLACEHOLDER`)
    - `.planning/phases/49-ativacao-orientacao-contextual-campos/49-PATTERNS.md` (bloco de `mandatory-artwork-field.tsx`)
    - `openspec/changes/fase-49-ativacao-orientacao-contextual-campos/specs/mandatory-artwork-text/spec.md`
  </read_first>
  <action>
    No componente (já `"use client"`):
    1. Importar `MANDATORY_ARTWORK_LABEL`, `MANDATORY_ARTWORK_HINT` e `MANDATORY_ARTWORK_PLACEHOLDER` de `@/lib/campaign/field-guidance`, `FieldHint` de `@/components/ui/field-hint` e `useId` de `react`.
    2. Declarar o id de ajuda via `useId` no topo do componente: `const helpBase = useId();` → `hintId = \`${helpBase}-hint\``. O `id` do próprio campo permanece estático `mandatoryArtworkText`. (O componente não recebe `error` hoje — não criar estado de erro nem variável de id de erro sem uso; se/quando um erro existir, derivar o `errorId` e incluí-lo no `aria-describedby`.)
    3. Trocar o texto do label para `{MANDATORY_ARTWORK_LABEL}` mantendo o `<span>` de `(opcional)`.
    4. Trocar o placeholder para `{MANDATORY_ARTWORK_PLACEHOLDER}` (string com quebras `\n` — o atributo `placeholder` de `<textarea>` suporta múltiplas linhas) e `rows` para `3`.
    5. Substituir o `<p>` de hint atual por `<FieldHint id={hintId}>{MANDATORY_ARTWORK_HINT}</FieldHint>` (o `FieldHint` usa `text-text-secondary` por default) e adicionar `aria-describedby={hintId}` à textarea.
    6. **Manter** `id="mandatoryArtworkText"`, `value`, `onChange`, `maxLength={200}`. **Não** adicionar `required`, checkbox wrapper, fluxo secundário ou qualquer advertência negativa permanente.
  </action>
  <acceptance_criteria>
    - `MANDATORY_ARTWORK_PLACEHOLDER` produz 3 linhas visíveis no placeholder (`split("\n").length === 3`).
    - `getByLabelText(/Informações obrigatórias na arte/i)` funciona e o campo está diretamente visível (sem checkbox condicional).
    - A textarea tem `aria-describedby` não vazio referenciando o id do hint derivado de `useId`; `id="mandatoryArtworkText"`, `maxLength={200}`, `value` e `onChange` preservados.
    - `rg -c "Use para regras, restrições ou informações" src/components/campaign/mandatory-artwork-field.tsx` retorna 0.
    - Nenhuma advertência negativa (ex.: "Não repita preço", "Não use para aviso ilustrativo") é adicionada.
  </acceptance_criteria>
  <verify>
    <automated>rg -c "MANDATORY_ARTWORK_LABEL|MANDATORY_ARTWORK_HINT|MANDATORY_ARTWORK_PLACEHOLDER|useId|aria-describedby" "src/components/campaign/mandatory-artwork-field.tsx"</automated>
  </verify>
  <done>Campo visível com label positivo, microcopy de orientação e placeholder multi-linha real de produto; ids de ajuda via `useId`; contrato de transporte intacto.</done>
</task>

<task type="auto">
  <name>Task 2: Revisão do brief — categorias separáveis e rótulos de preço alinhados</name>
  <files>src/components/flow/campaign-brief-review.tsx</files>
  <read_first>
    - `src/components/flow/campaign-brief-review.tsx:60-80` (derivação)
    - `src/components/flow/campaign-brief-review.tsx:148-190` (seção Oferta)
    - `src/components/flow/campaign-brief-review.tsx:220-250` (seção Avisos)
    - `src/lib/campaign/constants.ts` (`ILLUSTRATIVE_NOTICE_TEXT`)
    - `src/lib/campaign/field-guidance.ts` (`MANDATORY_ARTWORK_LABEL`)
    - `.planning/phases/49-ativacao-orientacao-contextual-campos/49-PATTERNS.md` (bloco do `campaign-brief-review.tsx`)
    - `openspec/changes/fase-49-ativacao-orientacao-contextual-campos/specs/campaign-brief-review/spec.md`
  </read_first>
  <action>
    Na seção Avisos (`:227-246`), substituir o parágrafo único por **itens distintos e rotulados**, derivados de `fields` (não do texto concatenado):
    - quando `fields.showIllustrativeNotice` → item com rótulo `{ILLUSTRATIVE_NOTICE_TEXT}` e o ícone `Check` (manter o padrão visual `flex items-start gap-2` + `<p className="text-text-primary text-sm font-body">`);
    - quando `fields.mandatoryArtworkTextFree.trim() !== ""` → item rotulado `{MANDATORY_ARTWORK_LABEL}` com o texto livre preservado e **quebras de linha visíveis** (o `<p>` do texto recebe `className` com `whitespace-pre-line`, além de `text-text-primary text-sm font-body`);
    - quando nenhum dos dois → `"Sem avisos adicionais."`.
    **Remover** a chamada não utilizada de `buildMandatoryArtworkText` e o respectivo import em `campaign-brief-review.tsx` (`:13` e `:69-76`): como a seção Avisos deriva diretamente de `fields.showIllustrativeNotice`/`fields.mandatoryArtworkTextFree`, o resultado concatenado deixa de ser usado. A não-mudança do helper `buildMandatoryArtworkText` (em `use-campaign-form.ts`) é provada por teste — `use-campaign-form-notice.test.ts` e o plano 49-09 — e **não** por uma chamada morta no componente. Manter o `aria-label="Avisos"` e o padrão de card existente.
    Na seção Oferta (`:165` e `:173`), renomear os `<dt>` de `"Preço original"` → `"Preço anterior"` e `"Preço final"` → `"Preço de venda"`. Manter a validade em item próprio (`:179-184`) e a seção Tema sem renderizar quando `creativeContext.themeId` for null.
  </action>
  <acceptance_criteria>
    - Com `showIllustrativeNotice=true` e `mandatoryArtworkTextFree` preenchido → a revisão exibe dois itens rotulados distintos (`"Imagem meramente ilustrativa"` e `"Informações obrigatórias na arte"`), sem concatenar num único bloco.
    - Com apenas um dos dois → apenas ele é exibido, com o rótulo correspondente.
    - No caso "só informações obrigatórias", o elemento do texto livre tem `whitespace-pre-line` na `className` (quebras de linha visíveis), sem o aviso ilustrativo.
    - Com nenhum → `"Sem avisos adicionais."` é exibido.
    - `rg -c "buildMandatoryArtworkText" src/components/flow/campaign-brief-review.tsx` retorna 0 (import e chamada não utilizada removidos); a seção Avisos deriva de `fields.showIllustrativeNotice`/`fields.mandatoryArtworkTextFree`.
    - Os rótulos `"Preço anterior"` e `"Preço de venda"` aparecem na seção Oferta.
  </acceptance_criteria>
  <verify>
    <automated>rg -c "ILLUSTRATIVE_NOTICE_TEXT|MANDATORY_ARTWORK_LABEL|mandatoryArtworkTextFree" "src/components/flow/campaign-brief-review.tsx"; rg -c "buildMandatoryArtworkText" "src/components/flow/campaign-brief-review.tsx"; if ($LASTEXITCODE -ne 0) { "OK: chamada morta removida" } else { "FALHA: buildMandatoryArtworkText ainda no componente" }</automated>
  </verify>
  <done>Seção Avisos com categorias separáveis e rotuladas; rótulos de preço alinhados; validade em item próprio.</done>
</task>

<task type="auto">
  <name>Task 3: Confirmar não-mudança de body, snapshot e contrato HTTP</name>
  <files>src/components/flow/campaign-brief-review.tsx</files>
  <read_first>
    - `src/components/flow/campaign-brief-review.tsx:1-90` (imports e derivações)
    - `openspec/changes/fase-49-ativacao-orientacao-contextual-campos/specs/campaign-brief-review/spec.md` (cenário "Body, snapshot e contrato HTTP inalterados")
  </read_first>
  <action>
    Rodar `git diff src/components/flow/campaign-brief-review.tsx` e confirmar que a alteração é **apenas de apresentação**: `inferIntent` e `buildValidityDisplayText` permanecem como hoje; a chamada/import não utilizados de `buildMandatoryArtworkText` foram removidos **sem alterar o helper em si** (que permanece intacto em `use-campaign-form.ts`); nenhum campo novo foi lido de `fields`; o body montado por `buildCampaignGenerationBody` e o snapshot `campaign_brief_v1` não foram tocados. Registrar a evidência (trecho do diff relevante) no SUMMARY do plano. Se o diff mostrar qualquer alteração de helper/contrato, reverter essa parte.
  </action>
  <acceptance_criteria>
    - `git diff --name-only` não lista `src/components/flow/use-campaign-form.ts`, `src/lib/campaign/brief.ts`, `src/lib/campaign/brief-schema.ts` nem `src/lib/campaign/constants.ts`.
    - O diff de `campaign-brief-review.tsx` não altera `inferIntent`/`buildValidityDisplayText`; a remoção de `buildMandatoryArtworkText` é restrita ao import/chamada não utilizados do componente, e o helper em `use-campaign-form.ts` permanece intacto (coberto por `use-campaign-form-notice.test.ts`).
    - Nenhum novo campo de `fields` é acessado além de `showIllustrativeNotice` e `mandatoryArtworkTextFree`.
  </acceptance_criteria>
  <verify>
    <automated>git diff --name-only -- src/components/flow/use-campaign-form.ts src/lib/campaign/brief.ts src/lib/campaign/brief-schema.ts src/lib/campaign/constants.ts</automated>
  </verify>
  <done>Evidência registrada de que body, snapshot e contrato HTTP permanecem inalterados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nenhuma nova | Apresentação do formulário e da tela de revisão. Não cria rota, não altera authz, não altera body/snapshot e não chama IA. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-02 | Tampering | Render do texto livre do lojista na revisão | mitigate | O texto é renderizado como conteúdo React (escapado) preservando quebras; nenhum `dangerouslySetInnerHTML`; rótulos vêm de constantes. |
| T-49-04 | Information Disclosure | Microcopy do campo de arte | mitigate | Orientação descreve apenas o efeito real (aparecer na imagem); nenhum detalhe interno do prompt do diretor é exposto. |
| T-49-05 | Denial of Service | Campo e seção Avisos | mitigate | Campo permanece visível com `rows=3`; sem listas permanentes nem conteúdo duplicado. |
| T-49-06 | Elevation of Privilege | Separação de categorias | mitigate | A separação deriva de `fields.showIllustrativeNotice`/`mandatoryArtworkTextFree`; o helper `buildMandatoryArtworkText` (em `use-campaign-form.ts`) e o body permanecem intocados — nenhuma mudança de contrato. |
| T-49-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma dependência nova. |
</threat_model>

<verification>
- `npx vitest run src/components/campaign/__tests__/validity-field.test.tsx` verde (regressão — arquivo não modificado).
- `git diff --name-only` não lista arquivos de body/snapshot/prompts.
- `rg -c "buildMandatoryArtworkText" src/components/flow/campaign-brief-review.tsx` = 0 (chamada morta removida; o helper segue coberto por `use-campaign-form-notice.test.ts`).
</verification>

<success_criteria>
- Campo de informações obrigatórias visível, positivo e multi-linha, com transporte intacto.
- Revisão com categorias separáveis e rótulos alinhados ao formulário.
- Body, snapshot e contrato HTTP comprovadamente inalterados.
</success_criteria>

<output>
Create `.planning/phases/49-ativacao-orientacao-contextual-campos/49-06-SUMMARY.md` when done.
</output>

---
phase: 49-ativacao-orientacao-contextual-campos
plan: 07
type: execute
wave: 3
depends_on:
  - 49-04
  - 49-05
  - 49-06
files_modified:
  - src/components/flow/__tests__/store-identity-form.orientation.test.tsx
  - src/components/flow/__tests__/campaign-input-form.orientation.test.tsx
  - src/components/campaign/__tests__/mandatory-artwork-field.test.tsx
autonomous: true
requirements:
  - contextual-field-help
  - store-identity-ui
  - campaign-input-ui
  - mandatory-artwork-text
must_haves:
  truths:
    - "Os testes provam que hints/labels/microcopy vêm dos módulos de conteúdo (fonte única)"
    - "Os testes provam a associação acessível campo↔hint/descrição/feedback/erro por aria-describedby composto (ids de useId), aria-invalid nos erros e aria-required sem required nativo"
    - "Os testes provam a descrição contextual do tom de voz para as 8 opções e a ausência quando não há seleção"
    - "Os testes provam a ajuda expansível colapsada por padrão, acionável e com aria-expanded coerente"
    - "Os testes provam o feedback dinâmico dos 4 estados de preço e a persistência/restauração dos valores"
    - "Os testes provam o placeholder multi-linha e o envio do valor multi-linha no mesmo contrato"
  artifacts:
    - path: "src/components/flow/__tests__/store-identity-form.orientation.test.tsx"
      provides: "Testes de orientação, acessibilidade e tom de voz da loja"
      min_lines: 80
    - path: "src/components/flow/__tests__/campaign-input-form.orientation.test.tsx"
      provides: "Testes de orientação, acessibilidade e feedback de preço da campanha"
      min_lines: 80
    - path: "src/components/campaign/__tests__/mandatory-artwork-field.test.tsx"
      provides: "Testes do campo de informações obrigatórias (multi-linha, a11y, contrato)"
      min_lines: 40
  key_links:
    - from: "src/components/flow/__tests__/store-identity-form.orientation.test.tsx"
      to: "src/lib/store-onboarding/field-guidance.ts"
      via: "asserções importam as strings do módulo (fonte única)"
      pattern: "field-guidance"
    - from: "src/components/flow/__tests__/campaign-input-form.orientation.test.tsx"
      to: "src/lib/campaign/field-guidance.ts"
      via: "asserções importam as strings e priceFeedbackMessage"
      pattern: "priceFeedbackMessage"
---

<objective>
Cobrir com testes automatizados a orientação aplicada nos formulários (group 7, itens 7.1–7.4, 7.6, 7.7): strings de fonte única, associação acessível, descrição contextual de tom de voz, ajuda expansível, feedback de preço, persistência e multi-linha.

Purpose: Provar que a orientação renderiza o conteúdo correto e é acessível, sem depender de inspeção visual.
Output: três novos arquivos de teste jsdom, todos verdes.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-ativacao-orientacao-contextual-campos/49-PATTERNS.md
@.planning/phases/49-ativacao-orientacao-contextual-campos/49-UI-SPEC.md
@openspec/changes/fase-49-ativacao-orientacao-contextual-campos/tasks.md
@src/components/flow/__tests__/store-identity-form.aceite-legal.test.tsx
@src/components/flow/__tests__/campaign-input-form.test.tsx
@src/components/flow/store-identity-form.tsx
@src/components/flow/campaign-input-form.tsx
@src/components/campaign/mandatory-artwork-field.tsx
</context>

<interfaces>
<!-- Padrões de mock/render dos testes existentes que os novos testes devem seguir. -->

De `store-identity-form.aceite-legal.test.tsx` (render do componente real):
```typescript
// @vitest-environment jsdom
import "@testing-library/jest-dom/vitest";
vi.mock("next/navigation", () => ({ useRouter: () => ({ push: vi.fn(), replace: vi.fn(), prefetch: vi.fn(), back: vi.fn(), forward: vi.fn(), refresh: vi.fn() }) }));
import { StoreIdentityForm } from "../store-identity-form";
// helpers: installMatchMedia(matches), installFetch(routes), STORE fixture
```

De `campaign-input-form.test.tsx` (mock do hook preservando o módulo real):
```typescript
vi.mock("@/components/flow/use-campaign-form", async (importOriginal) => {
  const actual = await importOriginal<typeof import("@/components/flow/use-campaign-form")>();
  return { ...actual, useCampaignForm: () => ({ fields: { /* ... */ }, fieldErrors: {}, touched: {}, setField: vi.fn(), /* ... */ }) };
});
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Testes de orientação e acessibilidade da loja</name>
  <files>src/components/flow/__tests__/store-identity-form.orientation.test.tsx</files>
  <read_first>
    - `src/components/flow/__tests__/store-identity-form.aceite-legal.test.tsx:1-120` (setup de mocks, `installMatchMedia`, `installFetch`, fixture `STORE`)
    - `src/components/flow/store-identity-form.tsx` (blocos alterados: aba Dados e aba Posicionamento)
    - `src/lib/store-onboarding/field-guidance.ts` (strings a importar nas asserções)
    - `openspec/changes/fase-49-ativacao-orientacao-contextual-campos/tasks.md` (itens 7.1–7.4)
  </read_first>
  <action>
    Criar `store-identity-form.orientation.test.tsx` (jsdom) reutilizando o setup de mocks do teste de aceite legal. Importar as strings de `@/lib/store-onboarding/field-guidance` e usá-las nas asserções (fonte única — nunca repetir o literal). Cobrir:
    1. **7.1 labels/hints**: `STORE_NAME_HINT` visível; `FISCAL_SECTION_LABEL` como heading; `TONE_OF_VOICE_HINT` visível; `POSITIONING_LABEL` como label de `#positioning`; `SLOGAN_HINT` visível.
    2. **7.2 acessibilidade (combinação completa)**: para `#name`, `#tone_of_voice`, `#positioning`, `#short_description` e `#slogan`, afirmar que `aria-describedby` é **não vazio** e que cada id referenciado resolve para um elemento existente (`document.getElementById(id)`); afirmar que o hint (`STORE_NAME_HINT`/`TONE_OF_VOICE_HINT`/`POSITIONING_HINT`/`POSITIONING_IDENTITY_HINT`/`SHORT_DESCRIPTION_HINT`/`SLOGAN_HINT`) está entre os elementos referenciados. Para o tom de voz, após selecionar uma opção, afirmar que o id de **descrição contextual** passa a integrar o `aria-describedby`. Nos campos com erro (provocar o erro existente, ex.: `blur` em `#name` vazio), afirmar que o id do **erro** entra no `aria-describedby`, que o `<p>` de erro tem `id` e que o campo expõe `aria-invalid`. Afirmar que `#name` e `#segment` têm `aria-required="true"` e que **nenhum** campo tem o atributo nativo `required` (`expect(el).not.toHaveAttribute("required")`).
    3. **7.3 descrição contextual do tom de voz**: sem seleção (`tone_of_voice=""`) nenhuma descrição de opção é renderizada; para cada uma das 8 opções, selecionar via `fireEvent.change` no select `#tone_of_voice` e afirmar que `TONE_OF_VOICE_DESCRIPTIONS[opcao]` é exibido. Iterar as 8 chaves a partir do próprio mapa importado.
    4. **7.4 ajuda expansível**: o disclosure do posicionamento está colapsado por padrão (`aria-expanded="false"` e a região **oculta via `hidden`**, ainda presente no DOM); após clique, `aria-expanded="true"` e `POSITIONING_EXAMPLE` visível; após novo clique, volta a colapsar; `aria-controls` aponta para um elemento que existe em ambos os estados.
    5. **7.4 estados**: `"Recomendado"` aparece para `#positioning` e `#short_description`; `#slogan` **não** contém `"Recomendado"` e mantém `(opcional)`.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/components/flow/__tests__/store-identity-form.orientation.test.tsx` verde.
    - O teste itera as 8 opções de `TONE_OF_VOICE_DESCRIPTIONS` (não apenas uma).
    - Há asserção da combinação completa: `aria-describedby` composto referenciando hint (e descrição contextual quando há seleção; e erro quando há erro), `aria-invalid` nos erros e ausência do atributo nativo `required` em pelo menos um campo obrigatório.
    - As strings assertadas são importadas de `field-guidance.ts` (nenhum literal duplicado de hint/descrição).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/flow/__tests__/store-identity-form.orientation.test.tsx</automated>
  </verify>
  <done>Testes verdes cobrindo strings de fonte única, aria-describedby/aria-required, descrição contextual das 8 opções e ajuda expansível da loja.</done>
</task>

<task type="auto">
  <name>Task 2: Testes de orientação, acessibilidade e feedback de preço da campanha</name>
  <files>src/components/flow/__tests__/campaign-input-form.orientation.test.tsx</files>
  <read_first>
    - `src/components/flow/__tests__/campaign-input-form.test.tsx:1-90` (mock do hook com `importOriginal`)
    - `src/components/flow/campaign-input-form.tsx` (seções Produto/Oferta alteradas)
    - `src/lib/campaign/field-guidance.ts` (`PRODUCT_DESCRIPTION_*`, `ORIGINAL_PRICE_LABEL`, `DISCOUNTED_PRICE_LABEL`, `PRICE_HELP_*`, `priceFeedbackMessage`)
    - `openspec/changes/fase-49-ativacao-orientacao-contextual-campos/tasks.md` (itens 7.1, 7.2, 7.5, 7.6)
  </read_first>
  <action>
    Criar `campaign-input-form.orientation.test.tsx` (jsdom) seguindo o padrão de mock do hook com `importOriginal`. Importar as strings de `@/lib/campaign/field-guidance`. Cobrir:
    1. **7.1 labels/hints**: `PRODUCT_DESCRIPTION_LABEL` no label de `#description`; `PRODUCT_DESCRIPTION_HINT` visível; `ORIGINAL_PRICE_LABEL` e `DISCOUNTED_PRICE_LABEL` presentes; hints de preço visíveis.
    2. **7.2 acessibilidade (combinação completa)**: `#description`, `#originalPrice` e `#discountedPrice` têm `aria-describedby` **não vazio** cujos ids resolvem para elementos existentes: hint correspondente, **feedback** (preços) e **erro** quando houver (provocar o erro existente). Afirmar `aria-invalid` nos campos com erro. `#productName`, `#discountedPrice` e `#badge` (quando `campaignIntent === "offer"`) têm `aria-required="true"` e **não** têm `required` nativo; o grupo da imagem primária (`CampaignImageUpload`) expõe `role="group"`, `aria-labelledby="productImages-label"` e `aria-describedby` apontando para o texto acessível de obrigatoriedade ("Imagem do produto obrigatória"), **sem** `aria-required` em `role="group"` e **sem** `required` nativo. Para a asserção do grupo de imagem, renderizar o `CampaignImageUpload` **real** (não o stub `() => null` usado no mock do form) — seja num `it` dedicado que renderiza o componente com props mínimas, seja evitando o mock nesse caso.
    3. **7.4 ajuda expansível**: `PRICE_HELP_TITLE` visível como gatilho; as 3 regras de `PRICE_HELP_RULES` **ocultas via `hidden`** inicialmente (região presente no DOM) e visíveis após clique; `aria-expanded` coerente; `aria-controls` aponta para elemento existente em ambos os estados.
    4. **7.5 feedback dos 4 estados**: com o mock do hook parametrizável por caso, afirmar `priceFeedbackMessage(...)` renderizado para: dois preços (com valores BRL), só venda, **só anterior** (mensagem neutra, `not.toContain("Sem preço")`), nenhum preço.
    5. **7.6 persistência/restauração**: com `fields.description` e `fields.mandatoryArtworkTextFree` preenchidos via mock, afirmar que os valores são exibidos/restaurados e que a orientação não os altera (nenhuma chamada a `setField` provocada pela renderização da ajuda).
  </action>
  <acceptance_criteria>
    - `npx vitest run src/components/flow/__tests__/campaign-input-form.orientation.test.tsx` verde.
    - O teste cobre os 4 estados de preço, incluindo a asserção `not.toContain("Sem preço")` no estado "só anterior".
    - Há asserção explícita de `aria-required="true"` e ausência de `required` nativo em `#productName`, `#discountedPrice`, `#badge` (offer); o grupo da imagem primária é assertado por `aria-labelledby` + `aria-describedby` de obrigatoriedade, **sem** `aria-required` em `role="group"`.
    - Há asserção da combinação completa: `aria-describedby` composto (hint + feedback + erro quando houver) referenciando elementos existentes e `aria-invalid` nos campos com erro.
    - A asserção do grupo de imagem usa o `CampaignImageUpload` real (não o stub `() => null`), de modo que `aria-required` seja efetivamente verificado.
    - As regras de preço estão ocultas via `hidden` antes do clique no disclosure (região presente no DOM).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/flow/__tests__/campaign-input-form.orientation.test.tsx</automated>
  </verify>
  <done>Testes verdes cobrindo labels/hints de fonte única, a11y, ajuda expansível e os 4 estados de feedback de preço.</done>
</task>

<task type="auto">
  <name>Task 3: Testes do campo de informações obrigatórias (multi-linha e contrato)</name>
  <files>src/components/campaign/__tests__/mandatory-artwork-field.test.tsx</files>
  <read_first>
    - `src/components/campaign/mandatory-artwork-field.tsx` (arquivo inteiro)
    - `src/components/campaign/__tests__/validity-field.test.tsx:1-40` (estilo de teste de componente de campo)
    - `src/lib/campaign/field-guidance.ts` (`MANDATORY_ARTWORK_LABEL`, `MANDATORY_ARTWORK_HINT`, `MANDATORY_ARTWORK_PLACEHOLDER`)
    - `openspec/changes/fase-49-ativacao-orientacao-contextual-campos/tasks.md` (itens 7.2, 7.7)
  </read_first>
  <action>
    Criar `mandatory-artwork-field.test.tsx` (jsdom) renderizando `MandatoryArtworkField` com `value=""` e `onChange` espião. Cobrir:
    1. **7.1/7.2**: label `MANDATORY_ARTWORK_LABEL`; hint `MANDATORY_ARTWORK_HINT` associado por `aria-describedby` **não vazio** cujo id resolve para o elemento do hint (id derivado de `useId`, não literal); campo visível (sem checkbox condicional); `maxLength={200}`; **sem** `required` nativo.
    2. **7.7 multi-linha**: o `placeholder` contém 3 linhas (`split("\n").length === 3`); ao digitar um valor multi-linha (`"Intensidade 8\nTorra clássica\nPeso líquido 500 g"`), `onChange` é chamado com o valor exato preservando `\n` — mesmo contrato de transporte.
    3. Confirmar ausência de advertências negativas: nenhum texto contendo `"Não repita"` ou `"Não use para aviso ilustrativo"` é renderizado.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/components/campaign/__tests__/mandatory-artwork-field.test.tsx` verde.
    - O teste afirma que o `placeholder` tem exatamente 3 linhas.
    - O teste afirma que `onChange` recebe a string multi-linha preservando `\n`.
    - Há asserção de ausência de `required` nativo, de `aria-describedby` composto (hint, e erro quando houver) e de advertências negativas.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/campaign/__tests__/mandatory-artwork-field.test.tsx</automated>
  </verify>
  <done>Testes verdes do campo de informações obrigatórias cobrindo label/hint/a11y, placeholder multi-linha e contrato de transporte.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nenhuma nova | Testes automatizados em jsdom. Nenhuma rota, nenhum dado real, nenhuma chamada de IA ou rede (fetch mockado). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-02 | Tampering | Asserções de strings | mitigate | Asserções importam as strings dos módulos de conteúdo (fonte única) — não há cópia que possa divergir silenciosamente. |
| T-49-04 | Information Disclosure | Fixtures de teste | mitigate | Fixtures usam dados fictícios; nenhum segredo, token ou PII real. |
| T-49-05 | Denial of Service | Suíte de testes | mitigate | Testes jsdom focados por componente; sem chamadas de rede reais. |
| T-49-06 | Elevation of Privilege | Validação nativa | mitigate | Testes afirmam explicitamente a ausência de `required` nativo — protege contra regressão do fence de validação controlada. |
| T-49-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma dependência nova (Testing Library/vitest já existentes). |
</threat_model>

<verification>
- `npx vitest run src/components/flow/__tests__/store-identity-form.orientation.test.tsx src/components/flow/__tests__/campaign-input-form.orientation.test.tsx src/components/campaign/__tests__/mandatory-artwork-field.test.tsx` verde.
- Nenhum arquivo de produção é modificado neste plano.
</verification>

<success_criteria>
- Orientação, acessibilidade, descrição contextual, ajuda expansível, feedback de preço, persistência e multi-linha cobertos por testes verdes.
- Strings assertadas provêm dos módulos de conteúdo (sem duplicação).
</success_criteria>

<output>
Create `.planning/phases/49-ativacao-orientacao-contextual-campos/49-07-SUMMARY.md` when done.
</output>

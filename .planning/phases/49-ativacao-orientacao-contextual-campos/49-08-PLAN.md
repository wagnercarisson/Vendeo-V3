---
phase: 49-ativacao-orientacao-contextual-campos
plan: 08
type: execute
wave: 3
depends_on:
  - 49-02
  - 49-04
  - 49-05
  - 49-06
files_modified:
  - src/lib/campaign/__tests__/field-guidance.correspondence.test.ts
  - src/lib/store-onboarding/__tests__/field-guidance.correspondence.test.ts
  - src/components/flow/__tests__/campaign-brief-review.orientation.test.tsx
  - src/components/flow/__tests__/phase49-fences.test.ts
autonomous: true
requirements:
  - store-field-orientation
  - campaign-field-orientation
  - campaign-brief-review
  - contextual-field-help
must_haves:
  truths:
    - "O feedback de preço corresponde à inferência real e às opções disponíveis, inclusive no estado 'só preço anterior'"
    - "O hint do tom de voz corresponde à regra real de desbloqueio (needs_tone_of_voice)"
    - "A descrição do produto chega ao CopyDirectorInput e NÃO aparece no briefing do Diretor de Arte"
    - "As categorias da revisão do brief permanecem separáveis (ambos / só um / nenhum)"
    - "Body, snapshot e transporte permanecem comportamentalmente idênticos"
  artifacts:
    - path: "src/lib/campaign/__tests__/field-guidance.correspondence.test.ts"
      provides: "Correspondência feedback de preço ↔ inferIntent/availableOptions (4 estados)"
    - path: "src/lib/store-onboarding/__tests__/field-guidance.correspondence.test.ts"
      provides: "Correspondência tom de voz ↔ computeTabUnlock e cobertura das 8 opções"
    - path: "src/components/flow/__tests__/phase49-fences.test.ts"
      provides: "Fence do Diretor de Arte e não-mudança comportamental de body/snapshot/transporte"
    - path: "src/components/flow/__tests__/campaign-brief-review.orientation.test.tsx"
      provides: "Testes de categorias separáveis da revisão"
  key_links:
    - from: "src/lib/campaign/__tests__/field-guidance.correspondence.test.ts"
      to: "src/components/flow/use-campaign-form.ts (inferIntent)"
      via: "import do mesmo caminho real e comparação por estado"
      pattern: "inferIntent"
    - from: "src/lib/store-onboarding/__tests__/field-guidance.correspondence.test.ts"
      to: "src/lib/store-onboarding/tabs.ts (computeTabUnlock)"
      via: "import do mesmo caminho real"
      pattern: "computeTabUnlock"
    - from: "src/components/flow/__tests__/phase49-fences.test.ts"
      to: "src/lib/image-generation/services/art-director-briefing.ts"
      via: "composição das seções e asserção de ausência do sentinela de descrição"
      pattern: "art-director-briefing"
---

<objective>
Provar que a orientação corresponde ao comportamento real (D14) e que as fences de não-mudança permanecem (D8/D12/D15): preço ↔ `inferIntent`/`availableOptions`, tom de voz ↔ `computeTabUnlock`, descrição ↔ copy mapper e ausência no Diretor de Arte, categorias separáveis na revisão e body/snapshot/transporte intactos.

Purpose: Impedir que a microcopy prometa um efeito que o pipeline não produz e que a fase vaze para o pipeline.
Output: quatro novos arquivos de teste verdes.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-ativacao-orientacao-contextual-campos/49-PATTERNS.md
@openspec/changes/fase-49-ativacao-orientacao-contextual-campos/tasks.md
@src/components/flow/use-campaign-form.ts
@src/lib/store-onboarding/tabs.ts
@src/lib/image-generation/services/art-director-briefing.ts
@src/lib/copy/mapper.ts
@src/lib/campaign/brief.ts
@src/components/campaign/types.ts
</context>

<interfaces>
<!-- Contratos reais usados pelos testes de correspondência e fence. -->

De `src/components/flow/use-campaign-form.ts`:
```typescript
export function inferIntent(originalPriceCents: number, discountedPriceCents: number | undefined | null): CampaignIntent;
// (original>0 && discounted>0) => "offer"; (discounted>0) => "spotlight"; senão => "exclusive"
export function buildMandatoryArtworkText(showNotice: boolean, freeText: string): string | undefined;
export function buildValidityDisplayText(fields: {...}): string | undefined;
export function buildCampaignGenerationBody(fields: CampaignFormFields, preparedImages: PreparedCampaignImage[], storeId: string, options?): Record<string, unknown>;
```

Expressão real de `availableOptions` (`campaign-input-form.tsx:544-553`) a replicar no teste:
```typescript
const inferred = inferIntent(fields.originalPriceCents, fields.discountedPriceCents);
if (inferred === "offer") return ["offer"];
if (fields.discountedPriceCents !== undefined && (fields.discountedPriceCents ?? 0) > 0) return ["offer", "spotlight"];
return ["spotlight", "exclusive"];
```

De `src/lib/store-onboarding/tabs.ts:93-94`:
```typescript
if (!ctx.toneOfVoice.trim()) return { unlocked: false, reason: "needs_tone_of_voice" };
```

De `src/lib/campaign/brief.ts`:
```typescript
export function buildCampaignBriefFromFlat(input: GenerateImageRequest, _storeId: string, source?: CampaignBriefSource): CampaignBrief;
export function buildCampaignBriefSnapshot(brief: CampaignBrief): CampaignBriefSnapshot; // schemaVersion "campaign_brief_v1"
```

De `src/lib/copy/mapper.ts`:
```typescript
export function mapBriefToCopyDirectorInput(brief, context, input): CopyDirectorInput; // description: brief.product.description
```

De `src/lib/image-generation/services/art-director-briefing.ts` (seções exportadas):
```typescript
export function buildCommercialRepertoire(brief: CampaignBrief): string;
export function campaignFactsSection(brief: CampaignBrief, context: ResolvedCampaignContext, effectiveProductName: string): string;
export function commercialDetailsSection(brief: CampaignBrief): string;
export function productReferenceSection(brief: CampaignBrief, context: ResolvedCampaignContext, imageCount: number): string;
export function constraintsSection(brief: CampaignBrief): string;
export function creativeDirectionSection(brief: CampaignBrief, context: ResolvedCampaignContext, inferredCategory?: string): string;
export function identityReferenceSection(brief: CampaignBrief, context: ResolvedCampaignContext): string;
```

Fixture de contexto (padrão real usado em `src/lib/lab/__tests__/snapshot-fixtures.contract.test.ts:113-135`):
```typescript
const context: ResolvedCampaignContext = {
  campaignInput: { productName, discountedPriceCents, productImageDataUrl, badgeText, campaignIntent },
  store: { name, segment, subsegment: null, toneOfVoice: null, positioning: null, shortDescription: null, slogan: null, brandColor: "#22C55E" },
  brandProfile: null,
  identity: { state: "text_only", imageUrl: null, directive: "" },
};
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Correspondência do feedback de preço com a inferência real</name>
  <files>src/lib/campaign/__tests__/field-guidance.correspondence.test.ts</files>
  <read_first>
    - `src/components/flow/use-campaign-form.ts:417-427` (`inferIntent`)
    - `src/components/flow/campaign-input-form.tsx:541-553` (expressão real de `availableOptions`)
    - `src/lib/campaign/field-guidance.ts` (`priceFeedbackMessage`)
    - `openspec/changes/fase-49-ativacao-orientacao-contextual-campos/tasks.md` (item 7.9)
  </read_first>
  <action>
    Criar teste puro (sem jsdom) que importa `inferIntent` de `@/components/flow/use-campaign-form` e `priceFeedbackMessage` de `../field-guidance`. Definir uma função local `availableOptionsFor(original, discounted)` que **replica literalmente** a expressão de `campaign-input-form.tsx:544-553` (documentando no comentário que é o espelho do código real). Para cada um dos 4 estados, afirmar coerência:
    - **dois preços** (`10000, 8000`): `inferIntent(...) === "offer"`, `availableOptionsFor(...)` é `["offer"]`, e o feedback contém `"oferta"` com os dois valores formatados.
    - **só venda** (`0, 8000`): `inferIntent(...) === "spotlight"`, opções `["offer","spotlight"]`, e o feedback cita `"Oferta e Destaque"`.
    - **só anterior** (`10000, undefined`): `inferIntent(...) === "exclusive"` (caminho sem preço, preservado), opções `["spotlight","exclusive"]`, e o feedback é a mensagem **neutra** — `not.toContain("Sem preço")` e `not.toContain("Destaque ou Exclusividade")`.
    - **nenhum** (`0, undefined`): `inferIntent(...) === "exclusive"`, opções `["spotlight","exclusive"]`, feedback cita `"Destaque ou Exclusividade"`.
    Afirmar também que **nenhuma** mensagem promete um intent fora de `availableOptionsFor(...)`.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/campaign/__tests__/field-guidance.correspondence.test.ts` verde.
    - O teste importa `inferIntent` do caminho real (não recria a lógica).
    - Há asserção `not.toContain("Sem preço")` no estado "só anterior".
    - Cada um dos 4 estados tem asserção de opções disponíveis coerente.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/campaign/__tests__/field-guidance.correspondence.test.ts</automated>
  </verify>
  <done>Correspondência feedback ↔ `inferIntent`/`availableOptions` provada nos 4 estados, com o estado neutro correto.</done>
</task>

<task type="auto">
  <name>Task 2: Correspondência do tom de voz com a regra real de desbloqueio</name>
  <files>src/lib/store-onboarding/__tests__/field-guidance.correspondence.test.ts</files>
  <read_first>
    - `src/lib/store-onboarding/tabs.ts:62-101` (`computeTabUnlock` e o caso `needs_tone_of_voice`)
    - `src/lib/store-onboarding/field-guidance.ts` (`TONE_OF_VOICE_HINT`, `TONE_OF_VOICE_DESCRIPTIONS`)
    - `src/components/flow/store-identity-form.tsx:37-46` (`TONE_OF_VOICE_OPTIONS`)
    - `openspec/changes/fase-49-ativacao-orientacao-contextual-campos/tasks.md` (item 7.9)
  </read_first>
  <action>
    Criar teste puro (sem jsdom) que importa `computeTabUnlock` de `../tabs` e as constantes de `../field-guidance`. Afirmar:
    1. `computeTabUnlock("direcao-visual", { storeId: "store-1", toneOfVoice: "" })` retorna `{ unlocked: false, reason: "needs_tone_of_voice" }` — a orientação reflete a regra real.
    2. `computeTabUnlock("direcao-visual", { storeId: "store-1", toneOfVoice: "profissional" })` retorna `{ unlocked: true }`.
    3. `TONE_OF_VOICE_DESCRIPTIONS` tem exatamente as 8 chaves `["profissional","moderno","elegante","divertido","acolhedor","jovem","tradicional","luxuoso"]` (comparar com `Object.keys(...).sort()`).
    4. Nenhuma descrição é vazia e nenhuma usa linguagem negativa (`not.toMatch(/não|nunca|evite/i)`).
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/store-onboarding/__tests__/field-guidance.correspondence.test.ts` verde.
    - O teste importa `computeTabUnlock` do caminho real.
    - A cobertura das 8 chaves é exata (comparação de arrays ordenados), não apenas `toHaveLength(8)`.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/store-onboarding/__tests__/field-guidance.correspondence.test.ts</automated>
  </verify>
  <done>Hint e descrições do tom de voz comprovadamente coerentes com `needs_tone_of_voice` e com as 8 opções reais.</done>
</task>

<task type="auto">
  <name>Task 3: Fence do Diretor de Arte e não-mudança de body/snapshot/transporte</name>
  <files>src/components/flow/__tests__/phase49-fences.test.ts</files>
  <read_first>
    - `src/lib/campaign/brief.ts:155-247` (`buildCampaignBriefFromFlat`, `buildCampaignBriefSnapshot`)
    - `src/lib/copy/mapper.ts:90-120` (`mapBriefToCopyDirectorInput`)
    - `src/lib/image-generation/services/art-director-briefing.ts:98-540` (seções exportadas)
    - `src/lib/lab/__tests__/snapshot-fixtures.contract.test.ts:105-135` (fixture de brief + contexto)
    - `src/components/flow/use-campaign-form.ts:405-427` (`buildMandatoryArtworkText`, `inferIntent`)
    - `openspec/changes/fase-49-ativacao-orientacao-contextual-campos/tasks.md` (itens 7.10, 7.11)
  </read_first>
  <action>
    Criar `phase49-fences.test.ts` (puro, sem jsdom) com:
    1. **Fence do Diretor de Arte (7.10)**: construir um `brief` com `buildCampaignBriefFromFlat({ productName: "Produto F49", description: SENTINELA, originalPriceCents: 10000, discountedPriceCents: 8000, badgeText: "Oferta", campaignIntent: "offer", productImageDataUrl: "data:image/jpeg;base64,dGVzdA==" } as GenerateImageRequest, "store-1")`, onde `SENTINELA = "SENTINELA_DESCRICAO_F49_NAO_DEVE_APARECER"`. Afirmar:
       - `mapBriefToCopyDirectorInput(brief, context, {}).description === SENTINELA` (a descrição **chega** ao copy);
       - `campaignFactsSection(brief, context, "Produto F49")`, `commercialDetailsSection(brief)`, `buildCommercialRepertoire(brief)`, `productReferenceSection(brief, context, 1)`, `constraintsSection(brief)`, `creativeDirectionSection(brief, context)`, `identityReferenceSection(brief, context)` **não** contêm `SENTINELA`.
       Usar a fixture de `ResolvedCampaignContext` do padrão real (`store` com `brandColor: "#22C55E"`, `identity: { state: "text_only", imageUrl: null, directive: "" }`).
    2. **Não-mudança comportamental (7.11)**: `buildMandatoryArtworkText` nas 4 combinações exatas (ambos → `"Imagem meramente ilustrativa\nTexto"`; só aviso → `ILLUSTRATIVE_NOTICE_TEXT`; só texto → o texto; nenhum → `undefined`); `buildCampaignBriefSnapshot(brief).schemaVersion === "campaign_brief_v1"` e `snapshot.product.description === SENTINELA` (a descrição é preservada no snapshot); `inferIntent(10000, 8000) === "offer"`.
    3. Rodar como regressão os goldens existentes: `src/lib/campaign/__tests__/brief-snapshot.test.ts` e `src/lib/campaign/__tests__/prompt-reframe.test.ts` (executar via comando, sem editar).
  </action>
  <acceptance_criteria>
    - `npx vitest run src/components/flow/__tests__/phase49-fences.test.ts src/lib/campaign/__tests__/brief-snapshot.test.ts src/lib/campaign/__tests__/prompt-reframe.test.ts` verde.
    - Há asserção de que a descrição está presente em `mapBriefToCopyDirectorInput(...).description` e ausente em **todas** as 7 seções do diretor compostas.
    - `buildMandatoryArtworkText` é assertado nas 4 combinações exatas.
    - `snapshot.schemaVersion === "campaign_brief_v1"` assertado.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/flow/__tests__/phase49-fences.test.ts src/lib/campaign/__tests__/brief-snapshot.test.ts src/lib/campaign/__tests__/prompt-reframe.test.ts</automated>
  </verify>
  <done>Fence do Diretor de Arte provada em runtime e não-mudança de body/snapshot/transporte coberta.</done>
</task>

<task type="auto">
  <name>Task 4: Categorias separáveis da revisão do brief</name>
  <files>src/components/flow/__tests__/campaign-brief-review.orientation.test.tsx</files>
  <read_first>
    - `src/components/flow/__tests__/campaign-brief-review.test.tsx:1-140` (setup de mocks e fixture `FIELDS`)
    - `src/components/flow/campaign-brief-review.tsx:148-250` (seções Oferta e Avisos)
    - `src/lib/campaign/field-guidance.ts` (`MANDATORY_ARTWORK_LABEL`)
    - `src/lib/campaign/constants.ts` (`ILLUSTRATIVE_NOTICE_TEXT`)
    - `openspec/changes/fase-49-ativacao-orientacao-contextual-campos/tasks.md` (item 7.8)
  </read_first>
  <action>
    Criar `campaign-brief-review.orientation.test.tsx` (jsdom) seguindo o setup de mocks do teste existente. Cobrir os três casos da seção Avisos:
    1. `showIllustrativeNotice: true` **e** `mandatoryArtworkTextFree: "Frete grátis acima de R$ 199"` → ambos os itens renderizados com rótulos distintos (`ILLUSTRATIVE_NOTICE_TEXT` e `MANDATORY_ARTWORK_LABEL`); afirmar que o texto das informações obrigatórias **não** está dentro do mesmo parágrafo do aviso ilustrativo (ex.: `getByText(MANDATORY_ARTWORK_LABEL).closest("p")` não contém o rótulo do aviso).
    2. Só o aviso (`showIllustrativeNotice: true`, `mandatoryArtworkTextFree: ""`) → apenas o item do aviso; `queryByText(MANDATORY_ARTWORK_LABEL)` é `null`.
    3. Nenhum (`showIllustrativeNotice: false`, `mandatoryArtworkTextFree: ""`) → `"Sem avisos adicionais."` visível.
    Afirmar também que os rótulos de preço `"Preço anterior"` e `"Preço de venda"` aparecem na seção Oferta quando os preços existem, e que a validade permanece em item próprio.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/components/flow/__tests__/campaign-brief-review.orientation.test.tsx` verde.
    - O caso "ambos" afirma dois itens rotulados distintos (não um bloco concatenado).
    - Os casos "só um" e "nenhum" têm asserções explícitas de ausência/presença.
    - Os rótulos `"Preço anterior"` e `"Preço de venda"` são assertados.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/flow/__tests__/campaign-brief-review.orientation.test.tsx</automated>
  </verify>
  <done>Categorias separáveis da revisão provadas nos três casos, com rótulos de preço alinhados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nenhuma nova | Testes automatizados puros/jsdom. Nenhuma rota, nenhuma chamada de rede real, nenhuma chamada de IA. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-02 | Tampering | Asserções de correspondência | mitigate | Testes importam as funções reais (`inferIntent`, `computeTabUnlock`, `mapBriefToCopyDirectorInput`, seções do diretor) — a correspondência não pode divergir do runtime. |
| T-49-04 | Information Disclosure | Fixtures | mitigate | Sentinela é uma string fictícia; nenhum segredo ou PII real. |
| T-49-05 | Denial of Service | Suíte de testes | mitigate | Testes puros focados; sem chamadas externas. |
| T-49-06 | Elevation of Privilege | Fence do Diretor de Arte | mitigate | Asserção explícita de ausência da descrição no briefing impede vazamento acidental de campo entre estágios do pipeline. |
| T-49-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma dependência nova. |
</threat_model>

<verification>
- `npx vitest run src/lib/campaign/__tests__/field-guidance.correspondence.test.ts src/lib/store-onboarding/__tests__/field-guidance.correspondence.test.ts src/components/flow/__tests__/phase49-fences.test.ts src/components/flow/__tests__/campaign-brief-review.orientation.test.tsx` verde.
- Nenhum arquivo de produção é modificado neste plano.
</verification>

<success_criteria>
- Microcopy comprovadamente corresponde ao comportamento real em preço, tom de voz e descrição.
- Fence do Diretor de Arte provada em runtime.
- Categorias da revisão separáveis nos três casos.
</success_criteria>

<output>
Create `.planning/phases/49-ativacao-orientacao-contextual-campos/49-08-SUMMARY.md` when done.
</output>

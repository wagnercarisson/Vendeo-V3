---
phase: 49-ativacao-orientacao-contextual-campos
plan: 09
type: execute
wave: 3
depends_on:
  - 49-02
  - 49-05
  - 49-06
files_modified:
  - src/components/flow/__tests__/phase49-fences.test.ts
  - src/components/flow/__tests__/campaign-brief-review.orientation.test.tsx
autonomous: true
requirements:
  - campaign-brief-review
  - contextual-field-help
  - campaign-field-orientation
must_haves:
  truths:
    - "A descrição do produto chega ao CopyDirectorInput e NÃO aparece no briefing do Diretor de Arte"
    - "Body, snapshot e transporte permanecem comportamentalmente idênticos"
    - "As categorias da revisão do brief permanecem separáveis (ambos / só aviso / só informações obrigatórias / nenhum)"
  artifacts:
    - path: "src/components/flow/__tests__/phase49-fences.test.ts"
      provides: "Fence do Diretor de Arte e não-mudança comportamental de body/snapshot/transporte"
    - path: "src/components/flow/__tests__/campaign-brief-review.orientation.test.tsx"
      provides: "Testes de categorias separáveis da revisão"
  key_links:
    - from: "src/components/flow/__tests__/phase49-fences.test.ts"
      to: "src/lib/image-generation/services/art-director-briefing.ts"
      via: "composição das seções e asserção de ausência do sentinela de descrição"
      pattern: "art-director-briefing"
    - from: "src/components/flow/__tests__/campaign-brief-review.orientation.test.tsx"
      to: "src/components/flow/campaign-brief-review.tsx"
      via: "renderização dos itens separados da seção Avisos"
      pattern: "MANDATORY_ARTWORK_LABEL"
---

<objective>
Provar as fences de não-mudança do pipeline (D8/D12/D15) e a separabilidade das categorias da revisão: `product.description` chega ao copy e **não** ao Diretor de Arte, body/snapshot/transporte intactos, e a seção Avisos da revisão exibe itens separáveis.

Purpose: Impedir que a fase vaze para o pipeline e que a revisão volte a concatenar categorias.
Output: dois novos arquivos de teste verdes.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-ativacao-orientacao-contextual-campos/49-PATTERNS.md
@openspec/changes/fase-49-ativacao-orientacao-contextual-campos/tasks.md
@src/lib/campaign/brief.ts
@src/lib/copy/mapper.ts
@src/lib/image-generation/services/art-director-briefing.ts
@src/components/flow/use-campaign-form.ts
@src/components/flow/campaign-brief-review.tsx
@src/lib/campaign/field-guidance.ts
@src/lib/campaign/constants.ts
</context>

<interfaces>
<!-- Contratos reais usados pelos testes de fence e da revisão. -->

De `src/components/flow/use-campaign-form.ts`:
```typescript
export function buildMandatoryArtworkText(showNotice: boolean, freeText: string): string | undefined;
export function inferIntent(originalPriceCents: number, discountedPriceCents: number | undefined | null): CampaignIntent;
```

De `src/lib/campaign/brief.ts`:
```typescript
export function buildCampaignBriefFromFlat(input: GenerateImageRequest, _storeId: string, source?: CampaignBriefSource): CampaignBrief;
export function buildCampaignBriefSnapshot(brief: CampaignBrief): CampaignBriefSnapshot; // schemaVersion "campaign_brief_v1"
```

De `src/lib/copy/mapper.ts`:
```typescript
export function mapBriefToCopyDirectorInput(brief, context, input): CopyDirectorInput; // description: brief.product.description
```

De `src/lib/image-generation/services/art-director-briefing.ts` (seções exportadas):
```typescript
export function buildCommercialRepertoire(brief: CampaignBrief): string;
export function campaignFactsSection(brief: CampaignBrief, context: ResolvedCampaignContext, effectiveProductName: string): string;
export function commercialDetailsSection(brief: CampaignBrief): string;
export function productReferenceSection(brief: CampaignBrief, context: ResolvedCampaignContext, imageCount: number): string;
export function constraintsSection(brief: CampaignBrief): string;
export function creativeDirectionSection(brief: CampaignBrief, context: ResolvedCampaignContext, inferredCategory?: string): string;
export function identityReferenceSection(brief: CampaignBrief, context: ResolvedCampaignContext): string;
```

Fixture de contexto (padrão real usado em `src/lib/lab/__tests__/snapshot-fixtures.contract.test.ts:113-135`):
```typescript
const context: ResolvedCampaignContext = {
  campaignInput: { productName, discountedPriceCents, productImageDataUrl, badgeText, campaignIntent },
  store: { name, segment, subsegment: null, toneOfVoice: null, positioning: null, shortDescription: null, slogan: null, brandColor: "#22C55E" },
  brandProfile: null,
  identity: { state: "text_only", imageUrl: null, directive: "" },
};
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fence do Diretor de Arte e não-mudança de body/snapshot/transporte</name>
  <files>src/components/flow/__tests__/phase49-fences.test.ts</files>
  <read_first>
    - `src/lib/campaign/brief.ts:155-247` (`buildCampaignBriefFromFlat`, `buildCampaignBriefSnapshot`)
    - `src/lib/copy/mapper.ts:90-120` (`mapBriefToCopyDirectorInput`)
    - `src/lib/image-generation/services/art-director-briefing.ts:98-540` (seções exportadas)
    - `src/lib/lab/__tests__/snapshot-fixtures.contract.test.ts:105-135` (fixture de brief + contexto)
    - `src/components/flow/use-campaign-form.ts:405-427` (`buildMandatoryArtworkText`, `inferIntent`)
    - `openspec/changes/fase-49-ativacao-orientacao-contextual-campos/tasks.md` (itens 7.10, 7.11)
  </read_first>
  <action>
    Criar `phase49-fences.test.ts` (puro, sem jsdom) com:
    1. **Fence do Diretor de Arte (7.10)**: construir um `brief` com `buildCampaignBriefFromFlat({ productName: "Produto F49", description: SENTINELA, originalPriceCents: 10000, discountedPriceCents: 8000, badgeText: "Oferta", campaignIntent: "offer", productImageDataUrl: "data:image/jpeg;base64,dGVzdA==" } as GenerateImageRequest, "store-1")`, onde `SENTINELA = "SENTINELA_DESCRICAO_F49_NAO_DEVE_APARECER"`. Afirmar:
       - `mapBriefToCopyDirectorInput(brief, context, {}).description === SENTINELA` (a descrição **chega** ao copy);
       - `campaignFactsSection(brief, context, "Produto F49")`, `commercialDetailsSection(brief)`, `buildCommercialRepertoire(brief)`, `productReferenceSection(brief, context, 1)`, `constraintsSection(brief)`, `creativeDirectionSection(brief, context)`, `identityReferenceSection(brief, context)` **não** contêm `SENTINELA`.
       Usar a fixture de `ResolvedCampaignContext` do padrão real (`store` com `brandColor: "#22C55E"`, `identity: { state: "text_only", imageUrl: null, directive: "" }`).
    2. **Não-mudança comportamental (7.11)**: `buildMandatoryArtworkText` nas 4 combinações exatas (ambos → `"Imagem meramente ilustrativa\nTexto"`; só aviso → `ILLUSTRATIVE_NOTICE_TEXT`; só texto → o texto; nenhum → `undefined`); `buildCampaignBriefSnapshot(brief).schemaVersion === "campaign_brief_v1"` e `snapshot.product.description === SENTINELA` (a descrição é preservada no snapshot); `inferIntent(10000, 8000) === "offer"`.
    3. Rodar como regressão os goldens existentes: `src/lib/campaign/__tests__/brief-snapshot.test.ts` e `src/lib/campaign/__tests__/prompt-reframe.test.ts` (executar via comando, sem editar).
  </action>
  <acceptance_criteria>
    - `npx vitest run src/components/flow/__tests__/phase49-fences.test.ts src/lib/campaign/__tests__/brief-snapshot.test.ts src/lib/campaign/__tests__/prompt-reframe.test.ts` verde.
    - Há asserção de que a descrição está presente em `mapBriefToCopyDirectorInput(...).description` e ausente em **todas** as 7 seções do diretor compostas.
    - `buildMandatoryArtworkText` é assertado nas 4 combinações exatas.
    - `snapshot.schemaVersion === "campaign_brief_v1"` assertado.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/flow/__tests__/phase49-fences.test.ts src/lib/campaign/__tests__/brief-snapshot.test.ts src/lib/campaign/__tests__/prompt-reframe.test.ts</automated>
  </verify>
  <done>Fence do Diretor de Arte provada em runtime e não-mudança de body/snapshot/transporte coberta.</done>
</task>

<task type="auto">
  <name>Task 2: Categorias separáveis da revisão do brief</name>
  <files>src/components/flow/__tests__/campaign-brief-review.orientation.test.tsx</files>
  <read_first>
    - `src/components/flow/__tests__/campaign-brief-review.test.tsx:1-140` (setup de mocks e fixture `FIELDS`)
    - `src/components/flow/campaign-brief-review.tsx:148-250` (seções Oferta e Avisos)
    - `src/lib/campaign/field-guidance.ts` (`MANDATORY_ARTWORK_LABEL`)
    - `src/lib/campaign/constants.ts` (`ILLUSTRATIVE_NOTICE_TEXT`)
    - `openspec/changes/fase-49-ativacao-orientacao-contextual-campos/tasks.md` (item 7.8)
  </read_first>
  <action>
    Criar `campaign-brief-review.orientation.test.tsx` (jsdom) seguindo o setup de mocks do teste existente. Cobrir os **quatro** casos da seção Avisos:
    1. `showIllustrativeNotice: true` **e** `mandatoryArtworkTextFree: "Frete grátis acima de R$ 199"` → ambos os itens renderizados com rótulos distintos (`ILLUSTRATIVE_NOTICE_TEXT` e `MANDATORY_ARTWORK_LABEL`); afirmar que o texto das informações obrigatórias **não** está dentro do mesmo parágrafo do aviso ilustrativo (ex.: `getByText(MANDATORY_ARTWORK_LABEL).closest("p")` não contém o rótulo do aviso).
    2. Só o aviso (`showIllustrativeNotice: true`, `mandatoryArtworkTextFree: ""`) → apenas o item do aviso; `queryByText(MANDATORY_ARTWORK_LABEL)` é `null`.
    3. Só as informações obrigatórias (`showIllustrativeNotice: false`, `mandatoryArtworkTextFree: "Intensidade 8\nTorra clássica\nPeso líquido 500 g"`) → a revisão apresenta **apenas** o item rotulado `MANDATORY_ARTWORK_LABEL`, **sem** o aviso ilustrativo (`queryByText(ILLUSTRATIVE_NOTICE_TEXT)` é `null`), e **preserva as quebras de linha** do texto livre: o elemento do texto tem `whitespace-pre-line` na `className` (assertar via `className`/`classList`) **e** o `textContent` contém os três itens separados por `\n` (assertar com matcher que respeite `\n`).
    4. Nenhum (`showIllustrativeNotice: false`, `mandatoryArtworkTextFree: ""`) → `"Sem avisos adicionais."` visível.
    Afirmar também que os rótulos de preço `"Preço anterior"` e `"Preço de venda"` aparecem na seção Oferta quando os preços existem, e que a validade permanece em item próprio.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/components/flow/__tests__/campaign-brief-review.orientation.test.tsx` verde.
    - O caso "ambos" afirma dois itens rotulados distintos (não um bloco concatenado).
    - Os casos "só aviso", "só informações obrigatórias" e "nenhum" têm asserções explícitas de ausência/presença; no caso "só informações obrigatórias" o aviso ilustrativo **não** aparece e as quebras de linha são preservadas.
    - Os rótulos `"Preço anterior"` e `"Preço de venda"` são assertados.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/flow/__tests__/campaign-brief-review.orientation.test.tsx</automated>
  </verify>
  <done>Categorias separáveis da revisão provadas nos quatro casos (incluindo só informações obrigatórias com quebras preservadas), com rótulos de preço alinhados.</done>
</task>

<task type="auto">
  <name>Task 3: Correspondência da microcopy de identidade × consumidores reais (item 7.9, terceira parte)</name>
  <files>src/components/flow/__tests__/phase49-fences.test.ts</files>
  <read_first>
    - `src/lib/store-onboarding/field-guidance.ts` (microcopy de identidade — nome público, tom de voz, posicionamento, descrição curta, slogan)
    - `src/lib/copy/mapper.ts:90-120` (`mapBriefToCopyDirectorInput` — consumidor de copy)
    - `src/lib/image-generation/services/art-director-briefing.ts:278-289` (`campaignFactsSection` — `storeTone` na direção visual)
    - `src/lib/brand-assets/brand-director.ts:381-392` (mapa de variáveis do prompt)
    - `src/lib/visual-signature/brand-profiler.ts:202-231` (mapa de variáveis do prompt)
    - `src/lib/brand-assets/text-only-inference-service.ts:94-104` (mapa de variáveis do prompt)
    - `src/lib/visual-signature/identity-art-director.ts:63-74` (mapa de variáveis do prompt + default de tom)
    - `openspec/changes/fase-49-ativacao-orientacao-contextual-campos/tasks.md` (item 7.9, terceira parte)
  </read_first>
  <action>
    Adicionar ao `phase49-fences.test.ts` (puro, sem jsdom) a prova de que a microcopy de identidade corresponde aos consumidores reais:
    1. **Copy (direto)** — com um `context` de sentinelas de identidade (`store.name`, `toneOfVoice`, `positioning`, `shortDescription`, `slogan`), afirmar que `mapBriefToCopyDirectorInput(brief, context, {})` propaga os 5 valores de sentinela (`storeName`, `toneOfVoice`, `positioning`, `shortDescription`, `slogan`).
    2. **Direção visual (tom)** — afirmar que `campaignFactsSection(brief, context, "Produto F49")` contém o sentinela de `toneOfVoice`; e que, com `toneOfVoice: null`, a seção usa o default `"profissional"` (`- **Tom de voz:** profissional`).
    3. **Perfil/direção visual (indireto)** — guarda de correspondência por leitura de fonte: ler os módulos reais (`brand-director.ts`, `brand-profiler.ts`, `text-only-inference-service.ts`, `identity-art-director.ts`) e afirmar que cada um referencia, no seu mapa de variáveis de prompt, os campos de identidade (`positioning`, `short_description`/`shortDescription`, `slogan`, `tone_of_voice`/`toneOfVoice`). O teste deve falhar se um consumidor parar de usar o campo — mantendo a microcopy ("efeito indireto no perfil/direção visual") fiel ao comportamento.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/components/flow/__tests__/phase49-fences.test.ts` verde.
    - Há asserção de que os 5 campos de identidade chegam a `mapBriefToCopyDirectorInput`.
    - Há asserção de que o tom de voz chega a `campaignFactsSection` e de que o default `"profissional"` é usado quando ausente.
    - Os 4 módulos de perfil/direção visual são assertados como referenciando os campos de identidade.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/flow/__tests__/phase49-fences.test.ts</automated>
  </verify>
  <done>Terceira parte do item 7.9 coberta: microcopy de identidade corresponde aos consumidores reais (copy + perfil/direção visual).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nenhuma nova | Testes automatizados puros/jsdom. Nenhuma rota, nenhuma chamada de rede real, nenhuma chamada de IA. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-02 | Tampering | Asserções de fence | mitigate | Testes importam as funções reais (`mapBriefToCopyDirectorInput`, seções do diretor, `buildCampaignBriefSnapshot`) — a fence não pode divergir do runtime. |
| T-49-04 | Information Disclosure | Fixtures | mitigate | Sentinela é uma string fictícia; nenhum segredo ou PII real. |
| T-49-05 | Denial of Service | Suíte de testes | mitigate | Testes puros/jsdom focados; sem chamadas externas. |
| T-49-06 | Elevation of Privilege | Fence do Diretor de Arte | mitigate | Asserção explícita de ausência da descrição no briefing impede vazamento acidental de campo entre estágios do pipeline. |
| T-49-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma dependência nova. |
</threat_model>

<verification>
- `npx vitest run src/components/flow/__tests__/phase49-fences.test.ts src/components/flow/__tests__/campaign-brief-review.orientation.test.tsx` verde.
- A microcopy de identidade corresponde aos consumidores reais: os 5 campos chegam ao copy mapper, o tom de voz chega à direção visual (com default) e os 4 módulos de perfil/direção visual referenciam os campos de identidade (item 7.9, terceira parte).
- Nenhum arquivo de produção é modificado neste plano.
</verification>

<success_criteria>
- Fence do Diretor de Arte provada em runtime.
- Categorias da revisão separáveis nos quatro casos (incluindo só informações obrigatórias, com quebras de linha preservadas e sem aviso ilustrativo).
- Não-mudança de body/snapshot/transporte coberta.
- Correspondência da microcopy de identidade × consumidores reais (copy + perfil/direção visual) provada.
</success_criteria>

<output>
Create `.planning/phases/49-ativacao-orientacao-contextual-campos/49-09-SUMMARY.md` when done.
</output>

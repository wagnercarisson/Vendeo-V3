---
phase: 49-ativacao-orientacao-contextual-campos
plan: 12
type: execute
wave: 4
depends_on:
  - 49-07
  - 49-08
  - 49-09
  - 49-10
  - 49-11
files_modified:
  - .planning/phases/49-ativacao-orientacao-contextual-campos/49-GATES.txt
autonomous: true
requirements:
  - contextual-field-help
  - store-field-orientation
  - campaign-field-orientation
  - store-identity-ui
  - campaign-input-ui
  - mandatory-artwork-text
  - campaign-brief-review
must_haves:
  truths:
    - "A regressão de auto-save, drafts, navegação por abas e montagem do body permanece verde"
    - "Os 4 gates (vitest, typecheck, lint, build) passam sem erros"
    - "Os hashes dos arquivos protegidos são idênticos ao baseline de 49-01"
    - "O conjunto de arquivos alterados pela fase é exclusivamente de apresentação/conteúdo"
  artifacts:
    - path: ".planning/phases/49-ativacao-orientacao-contextual-campos/49-GATES.txt"
      provides: "Evidência bruta da regressão, dos 4 gates e da comparação de hashes contra o baseline"
      contains: "vitest"
  key_links:
    - from: ".planning/phases/49-ativacao-orientacao-contextual-campos/49-GATES.txt"
      to: ".planning/phases/49-ativacao-orientacao-contextual-campos/49-BASELINE.txt"
      via: "comparação hash a hash"
      pattern: "Baseline de não-mudança"
    - from: ".planning/phases/49-ativacao-orientacao-contextual-campos/49-GATES.txt"
      to: "suites de F36/F40/F41/F43"
      via: "regressão de auto-save/draft/abas/body"
      pattern: "use-campaign-form"
---

<objective>
Fechar a fase com evidência objetiva: rodar a regressão de comportamento, os 4 gates (itens 9.1–9.4) e a prova final de não-mudança contra o baseline de 49-01.

Purpose: Provar que a suíte está verde e que nada fora da apresentação foi tocado.
Output: `49-GATES.txt` com a regressão, a saída dos 4 gates e a comparação de hashes.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-ativacao-orientacao-contextual-campos/49-BASELINE.txt
@.planning/phases/49-ativacao-orientacao-contextual-campos/49-CONTEXT.md
@openspec/changes/fase-49-ativacao-orientacao-contextual-campos/tasks.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regressão de comportamento e revisão do diff contra o baseline</name>
  <files>.planning/phases/49-ativacao-orientacao-contextual-campos/49-GATES.txt</files>
  <read_first>
    - `.planning/phases/49-ativacao-orientacao-contextual-campos/49-BASELINE.txt` (seções "Baseline de não-mudança" e "Consumidores reais")
    - `openspec/changes/fase-49-ativacao-orientacao-contextual-campos/tasks.md` (itens 8.3 e 8.4)
  </read_first>
  <action>
    1. **8.3 Regressão de comportamento**: rodar as suites de auto-save/draft/abas/body das fases F36/F40/F41/F43 — `npx vitest run src/components/flow/__tests__/use-store-form.autosave-fiscal.test.ts src/lib/store-onboarding/__tests__ src/components/flow/__tests__/use-campaign-form-navigation.test.ts src/components/flow/__tests__/use-campaign-form-product-images.test.ts src/components/flow/__tests__/use-campaign-form-notice.test.ts src/components/flow/__tests__/use-campaign-form-submit-error.test.ts src/components/campaign/__tests__/validity-field.test.tsx` — e confirmar zero regressões de comportamento.
    2. **8.4 Revisão do diff contra o baseline**: recomputar os hashes dos arquivos protegidos e comparar com a seção "Baseline de não-mudança" de `49-BASELINE.txt`. Nenhum hash pode divergir. Rodar `git diff --name-only` e confirmar que a lista de arquivos alterados **não** contém: `prompts/**`, `src/lib/ai/**`, `src/lib/campaign/brief.ts`, `src/lib/campaign/brief-schema.ts`, `src/lib/campaign/constants.ts`, `src/lib/image-generation/services/art-director-briefing.ts`, `src/components/flow/use-campaign-form.ts`, `src/components/campaign/validity-field.tsx`, `src/lib/store-onboarding/tabs.ts`, `src/lib/store-onboarding/reason-text.ts`, `src/lib/store-onboarding/draft-store.ts`, `src/hooks/use-onboarding-tabs.ts`, `src/hooks/use-drift-detection.ts`, `src/lib/drift.ts`, `supabase/**`, `src/app/api/**`.
    3. Registrar a saída da regressão e a lista de arquivos alterados em `49-GATES.txt`, na seção "## Regressão e diff".
  </action>
  <acceptance_criteria>
    - As suites de regressão listadas terminam verdes.
    - Nenhum hash da seção "Baseline de não-mudança" divergiu.
    - `git diff --name-only` não contém nenhum caminho da lista de arquivos proibidos.
    - A lista de arquivos alterados está registrada em `49-GATES.txt`.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/flow/__tests__/use-store-form.autosave-fiscal.test.ts src/components/flow/__tests__/use-campaign-form-navigation.test.ts src/components/flow/__tests__/use-campaign-form-notice.test.ts src/components/campaign/__tests__/validity-field.test.tsx; git diff --name-only</automated>
  </verify>
  <done>Regressão de comportamento verde e diff comprovadamente restrito à apresentação, registrados em `49-GATES.txt`.</done>
</task>

<task type="auto">
  <name>Task 2: Rodar os 4 gates e registrar a evidência</name>
  <files>.planning/phases/49-ativacao-orientacao-contextual-campos/49-GATES.txt</files>
  <read_first>
    - `AGENTS.md` (seção GSD Workflow — comandos de gate do projeto)
    - `openspec/changes/fase-49-ativacao-orientacao-contextual-campos/tasks.md` (itens 9.1–9.4)
  </read_first>
  <action>
    Executar, em ordem, os 4 gates do projeto e registrar a saída (contagem de arquivos/testes, exit code) em `49-GATES.txt`:
    1. `npx vitest run` — suíte completa sem falhas;
    2. `npm run typecheck` — sem erros;
    3. `npm run lint` — sem erros;
    4. `npm run build` — sem erros.
    Registrar para cada gate: comando, exit code, resumo da saída (ex.: "N arquivos / M testes") e data. Se qualquer gate falhar, corrigir a causa **dentro do escopo de apresentação** (nunca afrouxando fence ou alterando pipeline) e reexecutar.
  </action>
  <acceptance_criteria>
    - `49-GATES.txt` contém as 4 seções com comando, exit code 0 e resumo da saída.
    - `npx vitest run` termina com 0 falhas.
    - `npm run typecheck`, `npm run lint` e `npm run build` terminam com exit 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run; if ($?) { npm run typecheck }; if ($?) { npm run lint }; if ($?) { npm run build }</automated>
  </verify>
  <done>Os 4 gates verdes e registrados em `49-GATES.txt` com exit codes.</done>
</task>

<task type="auto">
  <name>Task 3: Prova final de não-mudança contra o baseline</name>
  <files>.planning/phases/49-ativacao-orientacao-contextual-campos/49-GATES.txt</files>
  <read_first>
    - `.planning/phases/49-ativacao-orientacao-contextual-campos/49-BASELINE.txt` (seção "Baseline de não-mudança")
    - `openspec/changes/fase-49-ativacao-orientacao-contextual-campos/tasks.md` (item 8.4, replicado no fechamento)
  </read_first>
  <action>
    Recomputar o SHA-256 de cada caminho listado na seção "Baseline de não-mudança" de `49-BASELINE.txt` e comparar hash a hash. Registrar em `49-GATES.txt` a seção "## Não-mudança" com: total de arquivos comparados, número de divergências (deve ser 0) e a lista de qualquer divergência encontrada. Em seguida, rodar `git diff --name-only` e anexar a lista completa de arquivos alterados pela fase, confirmando que nenhum caminho é de `prompts/**`, `src/lib/ai/**`, `src/lib/campaign/brief*.ts`, `src/lib/campaign/constants.ts`, `src/lib/image-generation/services/art-director-briefing.ts`, `src/components/flow/use-campaign-form.ts`, `src/components/campaign/validity-field.tsx`, `src/lib/store-onboarding/{tabs,reason-text,draft-store}.ts`, `src/hooks/use-onboarding-tabs.ts`, `src/hooks/use-drift-detection.ts`, `src/lib/drift.ts`, `src/app/api/**` ou `supabase/**`.
  </action>
  <acceptance_criteria>
    - A seção "## Não-mudança" registra 0 divergências de hash.
    - A lista de arquivos alterados em `49-GATES.txt` não contém nenhum caminho proibido.
    - `rg -c "description" src/lib/image-generation/services/art-director-briefing.ts` continua retornando 0.
  </acceptance_criteria>
  <verify>
    <automated>git diff --name-only; rg -c "description" "src/lib/image-generation/services/art-director-briefing.ts"; if ($LASTEXITCODE -ne 0) { "OK: 0 ocorrencias" } else { "FALHA" }</automated>
  </verify>
  <done>Comparação de hashes com 0 divergências e lista de arquivos alterados restrita à apresentação, registradas em `49-GATES.txt`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nenhuma nova | Execução de regressão/gates e leitura de hashes/diff. Nenhuma alteração de produção, nenhuma rota, nenhuma chamada de IA. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-02 | Tampering | Artefato de evidência | mitigate | Evidência gerada por comandos determinísticos (hashes SHA-256 e `git diff`) — não editável manualmente sem detecção. |
| T-49-03 | Repudiation | Registro dos gates | mitigate | Exit codes e resumos de saída registrados por comando, com data. |
| T-49-04 | Information Disclosure | Saída dos gates | mitigate | Registrar apenas resumos/contagens — não colar logs com variáveis de ambiente ou tokens. |
| T-49-05 | Denial of Service | Build/test | mitigate | Execução local e determinística; sem chamadas de rede reais ou IA paga. |
| T-49-06 | Elevation of Privilege | Fence de não-mudança | mitigate | Verificação hash-a-hash e de caminhos proibidos impede vazamento de escopo para pipeline/rotas/banco. |
| T-49-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma dependência nova. |
</threat_model>

<verification>
- `49-GATES.txt` contém a seção "## Regressão e diff", as 4 seções de gate com exit 0 e a seção "## Não-mudança" com 0 divergências.
- `git diff --name-only` sem caminhos proibidos.
- `npx vitest run` sem falhas.
</verification>

<success_criteria>
- Regressão de comportamento verde e suíte completa verde.
- Os 3 gates estáticos/build verdes.
- Prova objetiva de não-mudança dos arquivos protegidos.
- Fase pronta para a UAT humana de compreensão.
</success_criteria>

<output>
Create `.planning/phases/49-ativacao-orientacao-contextual-campos/49-12-SUMMARY.md` when done.
</output>

---
phase: 50-demonstracao-gratuita-validade-creditos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/50-demonstracao-gratuita-validade-creditos/50-BASELINE.txt
autonomous: true
requirements:
  - credit-tables
  - credit-sql-functions
  - credit-service
must_haves:
  truths:
    - "Existe um registro verificável do estado inicial do repositório (SHA inicial SHA_INICIAL_F50) e das alterações pré-existentes preservadas"
    - "Os consumidores reais de saldo/reserva/estorno/concessão e de `credit_balances.balance` estão inventariados com arquivo:linha"
    - "Os hashes dos arquivos protegidos (prompts, ai, campaign, snapshot, rotas de geração, requireLegalClearance) estão registrados como baseline de não-mudança"
  artifacts:
    - path: ".planning/phases/50-demonstracao-gratuita-validade-creditos/50-BASELINE.txt"
      provides: "Estado inicial (SHA_INICIAL_F50), inventário de consumidores de saldo/balance, baseline de hashes de não-mudança"
      contains: "Baseline de não-mudança"
  key_links:
    - from: ".planning/phases/50-demonstracao-gratuita-validade-creditos/50-BASELINE.txt"
      to: "prompts/** e src/lib/ai/**"
      via: "hashes SHA-256 registrados"
      pattern: "SHA256"
---

<objective>
Registrar o baseline de não-mudança da F50: estado inicial do repositório, inventário em código dos consumidores reais de saldo/reserva/estorno/concessão e de `credit_balances.balance` (bruto), e hashes dos arquivos protegidos — antes de qualquer alteração.

Purpose: Sem baseline verificável, as fences de não-mudança (prompts, gateway, snapshot, domínio, contrato de geração 402, requireLegalClearance) e a migração de consumidores de `balance` (D1) não podem ser provadas ao final.
Output: `.planning/phases/50-demonstracao-gratuita-validade-creditos/50-BASELINE.txt` com blocos: Estado inicial, Consumidores de saldo, Consumidores de `credit_balances.balance`, Baseline de não-mudança.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-demonstracao-gratuita-validade-creditos/50-CONTEXT.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/tasks.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1.1: Registrar estado inicial do repositório e preservar alterações pré-existentes</name>
  <files>.planning/phases/50-demonstracao-gratuita-validade-creditos/50-BASELINE.txt</files>
  <read_first>
    - `.planning/phases/50-demonstracao-gratuita-validade-creditos/50-CONTEXT.md` (§scope_fence)
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/tasks.md` (grupo 50-01, itens 1.1/1.2)
  </read_first>
  <action>
    Executar `git status --short` e `git diff --stat` e gravar na seção "## Estado inicial" do `50-BASELINE.txt`. Registrar `SHA_INICIAL_F50=<sha>` (de `git rev-parse HEAD`) e o branch (`git rev-parse --abbrev-ref HEAD`). Listar arquivos modificados/não rastreados **pré-existentes** e marcá-los "preservar — não commitar sem decisão explícita". Não exigir árvore limpa; não reverter/formatar/commitar arquivo pré-existente.
  </action>
  <acceptance_criteria>
    - `50-BASELINE.txt` existe com a seção "## Estado inicial".
    - Contém a saída literal de `git status --short` (ou "working tree clean"), a linha do branch e a linha `SHA_INICIAL_F50=<sha>`.
    - Arquivos não rastreados pré-existentes listados e marcados "preservar".
    - Nenhum arquivo pré-existente foi modificado/removido/commitado por esta tarefa.
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path ".planning/phases/50-demonstracao-gratuita-validade-creditos/50-BASELINE.txt" -Pattern "## Estado inicial|SHA_INICIAL_F50="</automated>
  </verify>
  <done>Estado inicial gravado com `git status --short`, branch e `SHA_INICIAL_F50`; pré-existentes preservados.</done>
</task>

<task type="auto">
  <name>Task 1.2: Renumerar trackings F50 e confirmar Stripe fora da numeração</name>
  <files>.planning/phases/50-demonstracao-gratuita-validade-creditos/50-BASELINE.txt</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/tasks.md` (item 1.2)
    - `AGENTS.md` (seção Phase 49 — padrão de renumeração das fases anteriores)
  </read_first>
  <action>
    Verificar via grep-consistência que **F50 = Demonstração gratuita e validade dos créditos** e que **Stripe/Monetização Pública permanece fora da numeração** nos runbooks/trackings (`AGENTS.md`, `ROADMAP.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`). Registrar na seção "## Trackings" do `50-BASELINE.txt` o resultado (zero resíduos F49→F50 incorretos; Stripe fora da numeração).
  </action>
  <acceptance_criteria>
    - A seção "## Trackings" registra "F50 = Demonstração gratuita e validade dos créditos" e "Stripe/Monetização Pública fora da numeração".
    - Nenhuma ocorrência de renumeração incorreta (ex.: F50 referenciada como Stripe) permanece.
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path ".planning/phases/50-demonstracao-gratuita-validade-creditos/50-BASELINE.txt" -Pattern "## Trackings|F50 =|Stripe"</automated>
  </verify>
  <done>Trackings verificados e registrados no baseline.</done>
</task>

<task type="auto">
  <name>Task 1.3: Inventariar consumidores reais de saldo/reserva/estorno/concessão</name>
  <files>.planning/phases/50-demonstracao-gratuita-validade-creditos/50-BASELINE.txt</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/tasks.md` (item 1.3)
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/design.md` (seção "Estado real em código")
  </read_first>
  <action>
    Confirmar em código e gravar na seção "## Consumidores de saldo" a tabela consumidor → `arquivo:linha`, cobrindo: `generate-image/route.ts` (gate `getBalance` → `reserveCredit`; refunds), `generate-without-logo/route.ts` (gate + reserve + refund), `create_store_with_cnpj`/`update_store_cnpj`/`admin_approve_store_verification`/`admin_exception_store_verification` (concessão onboarding), `grant_monthly_credits`, `admin_get_users_summary`, `credit-service`. Usar `rg`/`Select-String` para os números de linha reais.
  </action>
  <acceptance_criteria>
    - A seção "## Consumidores de saldo" contém uma linha por consumidor listado, cada uma com `arquivo:linha` verificável.
    - Inclui explicitamente os dois consumidores de geração (gate/reserve/refund) e os 4 pontos de concessão onboarding.
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path ".planning/phases/50-demonstracao-gratuita-validade-creditos/50-BASELINE.txt" -Pattern "generate-image|generate-without-logo|create_store_with_cnpj|admin_approve"</automated>
  </verify>
  <done>Inventário de consumidores de saldo gravado com arquivo:linha.</done>
</task>

<task type="auto">
  <name>Task 1.4: Inventário global de consumidores de `credit_balances.balance` (bruto)</name>
  <files>.planning/phases/50-demonstracao-gratuita-validade-creditos/50-BASELINE.txt</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/tasks.md` (item 1.4)
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/design.md` (D1 — "todo consumidor que lê `balance` diretamente deve migrar")
  </read_first>
  <action>
    Varrer o repo por leituras diretas de `credit_balances.balance` (`select("balance")`, `cb.balance`, `.balance` em contexto de crédito) e gravar na seção "## Consumidores de `credit_balances.balance`" cada consumidor com `arquivo:linha`, classificando quais **precisam migrar para o saldo disponível** (dashboard, conta, admin users summary, `admin_get_metrics` wallet, `ai-cost/admin-service`, gate de geração).
  </action>
  <acceptance_criteria>
    - A seção lista cada leitura de `balance` com `arquivo:linha` e um rótulo "migrar para saldo disponível" ou "não afetado".
    - O gate de geração e as superfícies de exibição de saldo estão identificados como "migrar".
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path ".planning/phases/50-demonstracao-gratuita-validade-creditos/50-BASELINE.txt" -Pattern "Consumidores de .credit_balances.balance.|migrar para saldo disponível"</automated>
  </verify>
  <done>Inventário global de consumidores de `balance` registrado.</done>
</task>

<task type="auto">
  <name>Task 1.5: Registrar baseline de hashes de não-mudança</name>
  <files>.planning/phases/50-demonstracao-gratuita-validade-creditos/50-BASELINE.txt</files>
  <read_first>
    - `.planning/phases/50-demonstracao-gratuita-validade-creditos/50-CONTEXT.md` (§scope_fence)
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/tasks.md` (item 1.5)
  </read_first>
  <action>
    Gravar a seção "## Baseline de não-mudança" com SHA-256 de cada item protegido: `prompts/**` (recursivo), `src/lib/ai/**` (recursivo), `src/lib/campaign/**` (recursivo), snapshot `campaign_brief_v1`, domínio, rotas de geração (`generate-image`/`generate-without-logo`), `requireLegalClearance`. Formato por linha `<hash>  <caminho relativo>`, ordenado alfabeticamente para comparação determinística.
  </action>
  <acceptance_criteria>
    - A seção contém uma linha `hash  caminho` por arquivo de `prompts/**`, `src/lib/ai/**`, `src/lib/campaign/**` e para os arquivos individuais listados.
    - Nenhum caminho da seção está ausente do disco no momento da gravação.
  </acceptance_criteria>
  <verify>
    <automated>Get-ChildItem -Path "prompts","src/lib/ai","src/lib/campaign" -Recurse -File | Get-FileHash -Algorithm SHA256 | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <done>Hashes SHA-256 registrados para todos os arquivos protegidos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Nenhuma nova | Lê o repositório e grava um artefato de planejamento; sem rotas, sem authz, sem dados de usuário, sem IA. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-01 | Spoofing | — | accept | Nenhuma superfície de identidade/sessão tocada. |
| T-50-02 | Tampering | `50-BASELINE.txt` | mitigate | Hashes via `Get-FileHash` do sistema; caminhos determinísticos. |
| T-50-03 | Repudiation | — | accept | Sem operações auditáveis novas. |
| T-50-04 | Information Disclosure | `50-BASELINE.txt` | mitigate | Apenas caminhos e hashes; nenhum segredo/token. |
| T-50-05 | Denial of Service | — | accept | Leitura local apenas. |
| T-50-06 | Elevation of Privilege | — | accept | Nenhuma mudança de autorização. |
</threat_model>

<verification>
- `50-BASELINE.txt` contém as seções: Estado inicial, Trackings, Consumidores de saldo, Consumidores de `credit_balances.balance`, Baseline de não-mudança.
- `git status --short` não foi alterado por nenhuma ação do plano (pré-existentes preservados).
</verification>

<success_criteria>
- Baseline verificável disponível para comparação nos planos de gates e UAT.
- Nenhum arquivo de produção alterado por este plano.
- Inventário de consumidores de `balance` confirma o escopo de migração (D1, 50-04).
</success_criteria>

<output>
Create `.planning/phases/50-demonstracao-gratuita-validade-creditos/50-01-SUMMARY.md` when done.
</output>

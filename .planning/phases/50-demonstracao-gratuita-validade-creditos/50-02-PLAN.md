---
phase: 50-demonstracao-gratuita-validade-creditos
plan: 02
type: execute
wave: 2
depends_on: [50-01]
files_modified:
  - supabase/migrations/20260920000001_f50_demo_credits.sql
autonomous: false
requirements:
  - credit-tables
  - credit-expiration
  - freemium-entitlement
  - credit-notifications
  - product-events
  - support-protocol
  - account-retention
  - beta-access-request
must_haves:
  truths:
    - "A migration estrutural adiciona as 5 colunas demo e os 2 tipos de transação sem publicar documentos legais"
    - "As tabelas credit_notifications, product_events, support_credit_requests e data_subject_requests são criadas com RLS/grants e índices únicos de dedup"
    - "O schema push é [BLOCKING] e a migration é reversível (revert comentado em ordem reversa)"
  artifacts:
    - path: "supabase/migrations/20260920000001_f50_demo_credits.sql"
      provides: "Colunas demo, CHECKs, benefit_type demo, tabelas novas, RLS/grants"
      contains: "Migration estrutural"
---

<objective>
Criar a migration **estrutural** da F50 (sem publicar documentos legais): colunas demo em `credit_balances`, tipos `demo`/`expiration` em `credit_transactions`, `benefit_type='demo'` em `freemium_entitlements`, e as tabelas `credit_notifications`, `product_events`, `support_credit_requests`, `data_subject_requests` (+ `privacy_notice_version` em `access_requests`), com RLS/grants e índices únicos de dedup.

Purpose: Preparar o schema para as RPCs (50-03) e serviços (50-04). Backward-compatível; backfill default sem expiração retroativa.
Output: uma migration SQL única (timestamp da fase), aplicada localmente e verificada.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-demonstracao-gratuita-validade-creditos/50-CONTEXT.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/tasks.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/design.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/credit-tables/spec.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/credit-expiration/spec.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/freemium-entitlement/spec.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/credit-notifications/spec.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/product-events/spec.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/support-protocol/spec.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/account-retention/spec.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/beta-access-request/spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 2.1: credit_balances — colunas demo + CHECK composto</name>
  <files>supabase/migrations/20260920000001_f50_demo_credits.sql</files>
  <read_first>
    - `supabase/migrations/20260716000001_create_credit_tables.sql` (schema atual)
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/credit-tables/spec.md`
  </read_first>
  <action>
    ALTER `credit_balances` adicionando `demo_balance INTEGER NOT NULL DEFAULT 0 CHECK (demo_balance >= 0)`, `demo_expires_at TIMESTAMPTZ NULL`, `demo_cycle_id UUID NULL`, `origin_demo_grant_tx_id UUID NULL`, `demo_contributing_tx_ids UUID[] NOT NULL DEFAULT '{}'`. Adicionar CHECK composto: `demo_balance > 0` implica `demo_expires_at`/`demo_cycle_id`/`origin_demo_grant_tx_id` não nulos. Backfill default (sem expiração retroativa).
  </action>
  <acceptance_criteria>
    - A migration inclui as 5 colunas com os defaults/CHECKs exatos de D1.
    - O CHECK composto exige `demo_expires_at`/`demo_cycle_id`/`origin_demo_grant_tx_id` não nulos quando `demo_balance > 0`.
    - Lojas existentes ficam com `demo_balance=0`/`demo_expires_at=NULL`/`demo_contributing_tx_ids='{}'`.
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path "supabase/migrations/20260920000001_f50_demo_credits.sql" -Pattern "demo_balance|demo_expires_at|demo_cycle_id|origin_demo_grant_tx_id|demo_contributing_tx_ids"</automated>
  </verify>
  <done>Colunas demo e CHECK composto adicionados.</done>
</task>

<task type="auto">
  <name>Task 2.2: credit_transactions — CHECK type 9 tipos + amount_sign</name>
  <files>supabase/migrations/20260920000001_f50_demo_credits.sql</files>
  <read_first>
    - `supabase/migrations/20260716000001_create_credit_tables.sql` (CHECKs atuais)
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/credit-tables/spec.md`
  </read_first>
  <action>
    Dropar/recriar `chk_credit_transactions_type` para 9 tipos (+`demo`+`expiration`) e `chk_credit_transactions_amount_sign` para `demo > 0` e `expiration < 0`. Preservar os 7 tipos anteriores. Preservar trigger de imutabilidade (dropar temporariamente apenas se necessário, recriando ao final).
  </action>
  <acceptance_criteria>
    - CHECK aceita os 9 tipos; `demo > 0` e `expiration < 0` no amount_sign.
    - Os 7 tipos legados permanecem aceitos.
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path "supabase/migrations/20260920000001_f50_demo_credits.sql" -Pattern "chk_credit_transactions_type|chk_credit_transactions_amount_sign|'demo'|'expiration'"</automated>
  </verify>
  <done>CHECKs de transação estendidos para demo/expiration.</done>
</task>

<task type="auto">
  <name>Task 2.3: trg_credit_balances_sync_total soma 3 buckets</name>
  <files>supabase/migrations/20260920000001_f50_demo_credits.sql</files>
  <read_first>
    - `supabase/migrations/20260722000002_creditos_mensais_automaticos.sql` (trigger atual)
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/credit-tables/spec.md`
  </read_first>
  <action>
    Atualizar `trg_credit_balances_sync_total` para `balance = demo_balance + bonus_balance + purchased_balance`.
  </action>
  <acceptance_criteria>
    - O trigger sincroniza `balance` com a soma dos 3 buckets.
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path "supabase/migrations/20260920000001_f50_demo_credits.sql" -Pattern "trg_credit_balances_sync_total|demo_balance \+ bonus_balance \+ purchased_balance"</automated>
  </verify>
  <done>Trigger de sync atualizado.</done>
</task>

<task type="auto">
  <name>Task 2.4: freemium_entitlements — benefit_type 'demo' (sem conversão)</name>
  <files>supabase/migrations/20260920000001_f50_demo_credits.sql</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/freemium-entitlement/spec.md`
    - `supabase/migrations/` (migration que criou `freemium_entitlements`)
  </read_first>
  <action>
    Estender o CHECK de `benefit_type` para incluir `demo` (tipos: `onboarding`, `monthly`, `admin_exception`, `demo`). **Sem** conversão de `onboarding`→`demo`.
  </action>
  <acceptance_criteria>
    - CHECK aceita `demo`; tipos históricos permanecem; nenhum registro `onboarding` é convertido.
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path "supabase/migrations/20260920000001_f50_demo_credits.sql" -Pattern "benefit_type|'demo'"</automated>
  </verify>
  <done>benefit_type 'demo' aceito no CHECK.</done>
</task>

<task type="auto">
  <name>Task 2.5: Tabelas credit_notifications / product_events / support_credit_requests / data_subject_requests + RLS/grants/índices</name>
  <files>supabase/migrations/20260920000001_f50_demo_credits.sql</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/credit-notifications/spec.md`
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/product-events/spec.md`
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/support-protocol/spec.md`
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/account-retention/spec.md`
  </read_first>
  <action>
    Criar `credit_notifications` (id/store_id/user_id/kind CHECK 6 valores/dedup_key/payload/inapp_delivered_at/inapp_read_at/email_status DEFAULT 'pending'/attempt_count DEFAULT 0/next_attempt_at/lease_expires_at/last_error/provider_message_id/email_sent_at/created_at + índice único `(store_id, kind, dedup_key)`); `product_events` (id/store_id/user_id/event_type/dedup_key/properties/created_at + índice único `(event_type, dedup_key)`); `support_credit_requests` (`operation_id UNIQUE`, `protocol`, `store_id`, `user_id`, `requested_email`, snapshots, `status`, `received_at`, `acknowledged_at`, `response_due_at`, `responded_at`, `closed_at`); `data_subject_requests` (`operation_id UUID UNIQUE`, `protocol`, `type` ∈ access/export/correction/deletion/closure, `user_id`, `store_id`, `contact`, `details`, `status` ∈ received/in_progress/completed/cancelled, `requested_at`, `acknowledged_at`, `due_at`, `completed_at`, `cancelled_at`, `closure_requested_at`, `deletion_due_at`, `deletion_inventory`, `legal_hold`). RLS/grants: service_role escrita/leitura; owner lê os próprios (`credit_notifications` excluindo `support_notice`; `data_subject_requests` owner lê os próprios). Índices `(operation_id)`, `(user_id)`, `(store_id)`.
  </action>
  <acceptance_criteria>
    - As 4 tabelas existem com as colunas/CHECKs/índices únicos especificados.
    - RLS: `authenticated` não recebe grants de escrita; owner lê os próprios; service_role escrita/leitura.
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path "supabase/migrations/20260920000001_f50_demo_credits.sql" -Pattern "credit_notifications|product_events|support_credit_requests|data_subject_requests|UNIQUE"</automated>
  </verify>
  <done>Tabelas novas criadas com RLS/grants/índices.</done>
</task>

<task type="auto">
  <name>Task 2.6: access_requests — privacy_notice_version</name>
  <files>supabase/migrations/20260920000001_f50_demo_credits.sql</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/beta-access-request/spec.md`
  </read_first>
  <action>
    Adicionar `privacy_notice_version TEXT` a `access_requests` (versão do aviso de privacidade apresentado, auditável). Preservar `uq_access_requests_email_active` e anti-enumeração.
  </action>
  <acceptance_criteria>
    - `access_requests.privacy_notice_version` existe; `uq_access_requests_email_active` preservado.
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path "supabase/migrations/20260920000001_f50_demo_credits.sql" -Pattern "privacy_notice_version"</automated>
  </verify>
  <done>privacy_notice_version adicionado a access_requests.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2.7: [BLOCKING] schema push local + reverter comentado</name>
  <files>supabase/migrations/20260920000001_f50_demo_credits.sql</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/design.md` (Migration Plan)
  </read_first>
  <action>
    **Não publicar documentos legais aqui.** Incluir seção REVERT comentada em ordem reversa (drop de colunas/tabelas/CHECKs/índices). **Aplicar a migration local** (Docker/Supabase) e verificar via `supabase db diff`/`psql` que as colunas/tabelas existem — um passo manual: garantir o Supabase/Docker local de pé e aplicar a migration.
  </action>
  <acceptance_criteria>
    - A migration NÃO publica `legal_document_versions`/documentos legais.
    - Seção REVERT comentada em ordem reversa presente.
    - Migration local aplicada e verificada (colunas/tabelas existem).
  </acceptance_criteria>
  <verify>
    <manual>Confirmar com o usuário que a migration local foi aplicada (Docker/Supabase) e as colunas/tabelas existem.</manual>
  </verify>
  <done>Migration estrutural aplicada localmente e revert comentado em ordem reversa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| DB (migration) | Nova superfície de dados (demo bucket, notificações, eventos, solicitações, pedidos de titular). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-07 | Tampering | CHECKs `credit_balances`/`credit_transactions` | mitigate | CHECK composto + amount_sign preservam invariantes do ledger. |
| T-50-08 | Information Disclosure | `credit_notifications`/`data_subject_requests` | mitigate | RLS: owner lê os próprios; `support_notice` não exposto ao owner. |
| T-50-09 | Elevation of Privilege | grants das tabelas novas | mitigate | 0 grants a `anon`/`authenticated` para escrita; service_role apenas. |
| T-50-10 | Repudiation | `product_events`/`support_credit_requests` | accept | Append-only + `operation_id UNIQUE` dão rastreabilidade. |
</threat_model>

<verification>
- `credit_balances` tem 5 colunas demo; `credit_transactions` 9 tipos; `freemium_entitlements` aceita `demo`.
- 4 tabelas novas criadas com índices únicos de dedup e RLS.
- Nenhum documento legal publicado nesta migration.
</verification>

<success_criteria>
- Schema estrutural pronto para as RPCs (50-03) e serviços (50-04).
- Backward-compatível; backfill default sem expiração retroativa.
- [BLOCKING] schema push executado e verificado localmente.
</success_criteria>

<output>
Create `.planning/phases/50-demonstracao-gratuita-validade-creditos/50-02-SUMMARY.md` when done.
</output>

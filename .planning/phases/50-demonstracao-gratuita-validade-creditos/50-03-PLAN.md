---
phase: 50-demonstracao-gratuita-validade-creditos
plan: 03
type: execute
wave: 3
depends_on: [50-02]
files_modified:
  - supabase/migrations/20260920000001_f50_demo_credits.sql
autonomous: true
requirements:
  - credit-sql-functions
  - credit-expiration
  - demo-credit-grant
  - freemium-entitlement
must_haves:
  truths:
    - "grant_demo_credits tem p_demo_grant_enabled obrigatório antes dos defaults e gating interno fail-closed"
    - "reserve_credit materializa expiração antes do lock e consome demo→bônus→comprado com metadata de origem"
    - "refund_credit aplica a regra temporal (episódio original/graça/nenhum ativo) preservando origin_demo_grant_tx_id"
  artifacts:
    - path: "supabase/migrations/20260920000001_f50_demo_credits.sql"
      provides: "RPCs grant_demo_credits, materialize_demo_expiration, try_grant_demo_entitlement, rewrite reserve/refund/grant"
      contains: "RPCs SQL"
---

<objective>
Escrever as RPCs SQL da F50: `try_grant_demo_entitlement`, `grant_demo_credits` (flag obrigatória + gating interno), `grant_credits` com `p_type='demo'`, `materialize_demo_expiration`, e a reescrita de `reserve_credit`/`refund_credit` com ordem demo→bônus→comprado, metadata de origem e episódios de graça.

Purpose: Implementar D3/D4/D5/D6/D16 em SQL atômico, fail-closed e idempotente.
Output: RPCs na migration estrutural (50-02), com REVOKE/GRANT service_role (paridade F47/F48.1).
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-demonstracao-gratuita-validade-creditos/50-CONTEXT.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/tasks.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/design.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/credit-sql-functions/spec.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/credit-expiration/spec.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/demo-credit-grant/spec.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/freemium-entitlement/spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 3.1: try_grant_demo_entitlement(p_store_id, p_root_hash)</name>
  <files>supabase/migrations/20260920000001_f50_demo_credits.sql</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/freemium-entitlement/spec.md`
    - `supabase/migrations/` (padrão `try_grant_onboarding_entitlement` existente)
  </read_first>
  <action>
    Criar `try_grant_demo_entitlement(p_store_id UUID, p_root_hash TEXT)` (SECURITY DEFINER, `search_path=''`, service_role) com `INSERT ... ON CONFLICT DO NOTHING` em `freemium_entitlements (store_id, root_hash, 'demo')`, retornando o UUID do entitlement ou NULL se já existia.
  </action>
  <acceptance_criteria>
    - Retorna UUID na primeira chamada; NULL na segunda (sem duplicação).
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path "supabase/migrations/20260920000001_f50_demo_credits.sql" -Pattern "try_grant_demo_entitlement|ON CONFLICT DO NOTHING"</automated>
  </verify>
  <done>try_grant_demo_entitlement criada.</done>
</task>

<task type="auto">
  <name>Task 3.2: grant_demo_credits (flag obrigatória + gating interno)</name>
  <files>supabase/migrations/20260920000001_f50_demo_credits.sql</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/demo-credit-grant/spec.md`
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/design.md` (D3/D16)
  </read_first>
  <action>
    Criar `public.grant_demo_credits(p_store_id UUID, p_root_hash TEXT, p_amount INTEGER, p_demo_grant_enabled BOOLEAN, p_ttl_hours INTEGER DEFAULT 168, p_idempotency_key TEXT DEFAULT NULL, p_granted_by UUID DEFAULT NULL) RETURNS JSONB`, SECURITY DEFINER, `search_path=''`, service_role. `p_demo_grant_enabled` **obrigatório, antes dos defaults**. Em transação única, na ordem: (0) flag false → `{granted:false, reason:'disabled'}`; (1) `p_amount<=0` → `amount_invalido`; (2) entitlement `onboarding` na raiz → `{granted:false, reason:'onboarding_consumed'}` (antes de INSERT); (3) `try_grant_demo_entitlement` NULL → `{granted:false, reason:'already_granted'}`; (4) `grant_credits(..., p_type='demo')` com `demo_expires_at = now() + make_interval(hours => p_ttl_hours)`; (5) vincular `grant_transaction_id` + `admin_audit_log` (`credit_grant`, `grant_type='demo'`) quando `p_granted_by`; (6) **emissão best-effort em SQL** — a RPC é SQL e **não chama** `ProductEventService.record` (TS). Emitir `product_events` (`event_type='demo_granted'`, `dedup_key=grant_tx_id`) e enfileirar `credit_notifications` (`kind='demo_granted'`, `dedup_key=grant_tx_id`), cada INSERT protegido por bloco `BEGIN ... EXCEPTION WHEN OTHERS THEN RAISE WARNING 'demo_granted best-effort: %', SQLERRM END` — falha **nunca reverte** a concessão (best-effort, registra um WARNING). O reconciliador (50-06) repara ausências derivando de `credit_transactions` (`type='demo'`, dedup por `grant_tx_id`).
  </action>
  <acceptance_criteria>
    - `p_demo_grant_enabled` é obrigatório e posicionado antes dos parâmetros com DEFAULT.
    - `false` → `{granted:false, reason:'disabled'}` sem INSERT; `onboarding_consumed`/`already_granted` corretos.
    - Primeira concessão insere entitlement + tx `demo` (`amount=10`) + `demo_expires_at=now()+168h` e retorna `{granted:true}`.
    - A emissão de `product_events.demo_granted`/`credit_notifications.demo_granted` é SQL best-effort (bloco EXCEPTION); falha NUNCA reverte a concessão.
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path "supabase/migrations/20260920000001_f50_demo_credits.sql" -Pattern "grant_demo_credits|p_demo_grant_enabled|reason: 'disabled'|onboarding_consumed|already_granted"</automated>
  </verify>
  <done>grant_demo_credits criada com gating fail-closed.</done>
</task>

<task type="auto">
  <name>Task 3.3: grant_credits com p_type='demo'</name>
  <files>supabase/migrations/20260920000001_f50_demo_credits.sql</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/credit-sql-functions/spec.md`
  </read_first>
  <action>
    Estender `grant_credits` para `p_type='demo'`: inserir tx `demo` (id `G`), `demo_cycle_id=G`, `origin_demo_grant_tx_id=G`, `demo_contributing_tx_ids=ARRAY[G]`, `demo_expires_at`, incrementar `demo_balance`. `bonus_balance`/`purchased_balance` não mudam.
  </action>
  <acceptance_criteria>
    - `grant_credits(store, 10, 'demo', ..., expires_at)` insere tx `demo` e inicializa todos os campos do CHECK.
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path "supabase/migrations/20260920000001_f50_demo_credits.sql" -Pattern "p_type = 'demo'|origin_demo_grant_tx_id = G|demo_contributing_tx_ids"</automated>
  </verify>
  <done>grant_credits estendido para demo.</done>
</task>

<task type="auto">
  <name>Task 3.4: materialize_demo_expiration(p_store_id)</name>
  <files>supabase/migrations/20260920000001_f50_demo_credits.sql</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/credit-sql-functions/spec.md`
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/credit-expiration/spec.md`
  </read_first>
  <action>
    Criar `public.materialize_demo_expiration(p_store_id UUID) RETURNS JSONB` (SECURITY DEFINER, `search_path=''`, service_role): quando `demo_balance>0 AND demo_expires_at<=now()`, sob `SELECT ... FOR UPDATE`, escrever tx `expiration` (`amount=-demo_balance`, `reference=demo_cycle_id`, `metadata={cycle_id, origin_demo_grant_tx_id, contributing_tx_ids}`), zerar `demo_balance`/`demo_expires_at`/`demo_cycle_id`/`demo_contributing_tx_ids` (**não** zerar `origin_demo_grant_tx_id`). Retorna `available`. No-op quando `demo_balance=0`.
  </action>
  <acceptance_criteria>
    - Materializa uma vez; segunda chamada é no-op (sem nova tx `expiration`).
    - `origin_demo_grant_tx_id` NÃO é zerado; `available` correto.
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path "supabase/migrations/20260920000001_f50_demo_credits.sql" -Pattern "materialize_demo_expiration|FOR UPDATE|origin_demo_grant_tx_id"</automated>
  </verify>
  <done>materialize_demo_expiration criada.</done>
</task>

<task type="auto">
  <name>Task 3.5: reserve_credit — materializa + ordem demo→bônus→comprado</name>
  <files>supabase/migrations/20260920000001_f50_demo_credits.sql</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/credit-sql-functions/spec.md`
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/credit-expiration/spec.md`
  </read_first>
  <action>
    Reescrever `reserve_credit` para: materializar expiração pendente antes do lock (mesma transação, `FOR UPDATE`); consumir na ordem demo ativo → bônus → comprado; metadata da deduction com `demo_amount`/`bonus_amount`/`purchased_amount` + snapshot `demo_expires_at` + `demo_cycle_id`/`origin_demo_grant_tx_id` + evidência de esgotamento (`demo_before`/`demo_after`). Checagem de saldo usa o **saldo disponível** (nunca `balance` bruto). `saldo_insuficiente` quando restante insuficiente.
  </action>
  <acceptance_criteria>
    - Ordem demo→bônus→comprado; demo vencido não consumível (materializado antes); `saldo_insuficiente` correto.
    - Metadata registra `demo_amount`/`bonus_amount`/`purchased_amount` e evidência de esgotamento (`demo_before>0`, `demo_after=0`).
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path "supabase/migrations/20260920000001_f50_demo_credits.sql" -Pattern "demo_amount|bonus_amount|purchased_amount|demo_before|demo_after"</automated>
  </verify>
  <done>reserve_credit reescrito.</done>
</task>

<task type="auto">
  <name>Task 3.6: refund_credit — regra temporal com episódios de graça</name>
  <files>supabase/migrations/20260920000001_f50_demo_credits.sql</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/credit-sql-functions/spec.md`
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/credit-expiration/spec.md`
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/design.md` (D6/D25)
  </read_first>
  <action>
    Reescrever `refund_credit` para ler `metadata.demo_amount`/`bonus_amount`/`purchased_amount` + snapshot `demo_expires_at`, por **regra temporal**: (1) episódio original válido (`expires_at > now()` e `cycle_id = origin_demo_grant_tx_id`) → restaura sem estender; (2) episódio de graça ativo (`cycle_id != origin_demo_grant_tx_id`) → restaura + `GREATEST(demo_expires_at, now()+24h)`; (3) nenhum episódio ativo (`expires_at <= now()` ou NULL) → materializa saldo vencido (se houver) e abre novo `demo_cycle_id` + `demo_expires_at=now()+24h`, preservando `origin_demo_grant_tx_id`. Gatilho da graça é só tempo vencido. No máximo uma `expiration` por episódio. Bônus/comprado restaurados como antes.
  </action>
  <acceptance_criteria>
    - 3 ramos temporais corretos; `origin_demo_grant_tx_id` nunca zerado; `demo_contributing_tx_ids` acumula.
    - Demo esgotada dentro do prazo restaura no episódio original (sem graça antecipada).
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path "supabase/migrations/20260920000001_f50_demo_credits.sql" -Pattern "now\(\) \+ interval '24|GREATEST|demo_cycle_id|origin_demo_grant_tx_id"</automated>
  </verify>
  <done>refund_credit reescrito com regra temporal.</done>
</task>

<task type="auto">
  <name>Task 3.7: REVOKE/GRANT service_role nas novas RPCs</name>
  <files>supabase/migrations/20260920000001_f50_demo_credits.sql</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/tasks.md` (item 3.7)
  </read_first>
  <action>
    Aplicar REVOKE de `anon`/`authenticated` e GRANT a `service_role` nas RPCs novas (`grant_demo_credits`, `materialize_demo_expiration`, `try_grant_demo_entitlement`), com paridade F47/F48.1 (`search_path=''`). Leitura de saldo disponível para `authenticated` por RLS/RPC `SECURITY DEFINER` com `auth.uid()` (D24) — não expor as RPCs de escrita.
  </action>
  <acceptance_criteria>
    - RPCs novas restritas a service_role; nenhuma exposição de escrita a `authenticated`.
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path "supabase/migrations/20260920000001_f50_demo_credits.sql" -Pattern "REVOKE|GRANT.*service_role|SECURITY DEFINER"</automated>
  </verify>
  <done>Grants/REVOKEs aplicados nas RPCs novas.</done>
</task>

<task type="auto">
  <name>Task 3.8: Redefinir os 4 wrappers SQL de concessão (p_demo_grant_enabled)</name>
  <files>supabase/migrations/20260920000001_f50_demo_credits.sql</files>
  <read_first>
    - `supabase/migrations/20260727000001_freemium_anti_abuso_cnpj.sql` (`create_store_with_cnpj` em :202-278, `update_store_cnpj` em :282-…)
    - `supabase/migrations/20260728000001_f33_cnpj_verification.sql` (`admin_approve_store_verification` em :131-186, `admin_exception_store_verification` em :274-332, `create_store_with_cnpj` verificacao-aware em :367-451)
    - `supabase/migrations/20260730000001_extend_update_store_cnpj.sql` (`update_store_cnpj` estendido em :13-…)
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/onboarding-grant/spec.md`
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/demo-credit-grant/spec.md`
  </read_first>
  <action>
    Na migration F50, para **cada um dos 4 wrappers** — `create_store_with_cnpj`, `update_store_cnpj`, `admin_approve_store_verification`, `admin_exception_store_verification` — **dropar a assinatura antiga com `DROP FUNCTION IF EXISTS public.<nome>(<assinatura atual>)` ANTES de criar a nova**. (Adicionar `p_demo_grant_enabled` muda a assinatura; `CREATE OR REPLACE` criaria um overload e a assinatura legada continuaria concedendo `bonus_onboarding` — violando o fail-closed.) Em seguida, `CREATE OR REPLACE FUNCTION public.<nome>(<assinatura nova + p_demo_grant_enabled BOOLEAN DEFAULT false>)`, substituindo a concessão interna `grant_credits(..., 'bonus_onboarding')` por `grant_demo_credits(..., p_demo_grant_enabled, ...)`. **Aplicar privilégios mínimos** (`SECURITY DEFINER`, `search_path=''`): `REVOKE ALL ON FUNCTION public.<nome>(...) FROM PUBLIC, anon, authenticated; GRANT EXECUTE ON FUNCTION public.<nome>(...) TO service_role;` — os 4 wrappers **NÃO** são expostos a `authenticated` (não validam `auth.uid()`; os callers legítimos usam `service_role` pelo backend). Preservar a resposta `onboardingGranted` refletindo `grant_demo_credits.granted` (não o entitlement id). Em `update_store_cnpj`, conceder a demo quando a verificação resulta em `approved` + flag ativa + raiz sem benefício anterior, e criar o marcador `legacy_pre_f32_onboarding_consumed` **somente** com evidência real de benefício anterior (transação `bonus_onboarding` na loja ou entitlement `onboarding` na raiz). Omitir `p_demo_grant_enabled` (default false) nunca concede demo.
  </action>
  <acceptance_criteria>
    - Os 4 wrappers têm `p_demo_grant_enabled BOOLEAN DEFAULT false` e chamam `grant_demo_credits` (não mais `bonus_onboarding`).
    - As 4 assinaturas antigas foram dropadas (sem overload legado): `pg_proc` não contém a assinatura antiga de cada wrapper.
    - Privilégios mínimos: `REVOKE ALL` de `PUBLIC`/`anon`/`authenticated` e `GRANT EXECUTE` apenas a `service_role` (4 wrappers); `authenticated`/`anon` recebem `permission denied`.
    - `onboardingGranted` reflete `grant_demo_credits.granted`; flag false → sem concessão.
    - `update_store_cnpj` concede demo para loja draft elegível e cria o marcador legado só com evidência real.
    - Chamada **sem** `p_demo_grant_enabled` não concede nada (teste fail-closed).
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path "supabase/migrations/20260920000001_f50_demo_credits.sql" -Pattern "DROP FUNCTION IF EXISTS public\.(create_store_with_cnpj|update_store_cnpj|admin_approve_store_verification|admin_exception_store_verification)|REVOKE ALL ON FUNCTION.*FROM PUBLIC, anon, authenticated|GRANT EXECUTE ON FUNCTION.*TO service_role"</automated>
  </verify>
  <done>Wrappers SQL redefinidos (assinaturas antigas dropadas, privilégios mínimos) para conceder demo com flag fail-closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| DB RPCs (concessão/reserva/estorno/expiração) | Caminho crítico financeiro do ledger. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-11 | Tampering | `reserve_credit`/`refund_credit` | mitigate | `SELECT ... FOR UPDATE`; materialização no mesmo lock; metadata de origem. |
| T-50-12 | Spoofing | `grant_demo_credits` | mitigate | service_role only; `p_demo_grant_enabled` obrigatório fail-closed. |
| T-50-13 | Elevation of Privilege | grants | mitigate | REVOKE anon/authenticated; `search_path=''`. |
| T-50-14 | Information Disclosure | RPCs de leitura | mitigate | Leitura RLS com `auth.uid()`; acesso cruzado negado. |
</threat_model>

<verification>
- `grant_demo_credits` idempotente e fail-closed; `materialize_demo_expiration` idempotente.
- `reserve_credit`/`refund_credit` preservam invariantes `balance_before/after` lineares.
- RPCs novas restritas a service_role.
</verification>

<success_criteria>
- D3/D4/D5/D6/D16 implementados em SQL atômico.
- Concorrência/idempotência garantidas por `FOR UPDATE` + ON CONFLICT.
</success_criteria>

<output>
Create `.planning/phases/50-demonstracao-gratuita-validade-creditos/50-03-SUMMARY.md` when done.
</output>

---
phase: 50-demonstracao-gratuita-validade-creditos
plan: 04
type: execute
wave: 4
depends_on: [50-02, 50-03]
files_modified:
  - src/lib/credit/credit-service.ts
  - src/lib/credit/types.ts
  - src/lib/credit/labels.ts
  - src/lib/credit/demo-status.ts
  - src/lib/freemium/entitlement-service.ts
  - src/lib/launch-config/config.ts
autonomous: true
requirements:
  - credit-service
  - freemium-entitlement
  - launch-config
  - demo-status-ui
must_haves:
  truths:
    - "getBalance/getBalanceBreakdown expõem saldo disponível (demo ativo + bônus + comprado) sem materializar"
    - "launch-config tem demoCreditsEnabled/demoCreditsAmount/demoCreditsTtlHours/emailEnabled fail-closed e monthlyCreditsEnabled default true"
      - "checkDemoEligibility exige ausência de onboarding, demo e admin_exception na raiz"
  artifacts:
    - path: "src/lib/credit/demo-status.ts"
      provides: "getDemoStatus + formatRelativeExpiry"
      contains: "helper puro"
---

<objective>
Camada de serviços da F50: `credit-service` expõe saldo disponível e breakdown demo; `credit/types.ts`/`labels.ts` ganham `demo`/`expiration`; `freemium/entitlement-service` ganha `checkDemoEligibility`/`grantDemoEntitlement`; `launch-config` ganha flags de demo/email; novo helper puro `demo-status.ts`. Migrar os consumidores de `credit_balances.balance` (inventário 50-01) para saldo disponível.

Purpose: Implementar D4/D7/D9/D15/D24 na camada de serviço.
Output: serviços server-only + helper puro, com testes unitários adicionados em 50-11.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-demonstracao-gratuita-validade-creditos/50-CONTEXT.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/tasks.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/design.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/credit-service/spec.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/freemium-entitlement/spec.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/launch-config/spec.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/demo-status-ui/spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 4.1: credit-service — saldo disponível sem materializar (RLS)</name>
  <files>src/lib/credit/credit-service.ts</files>
  <read_first>
    - `src/lib/credit/credit-service.ts` (estado atual)
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/credit-service/spec.md`
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/design.md` (D4/D24)
  </read_first>
  <action>
    Alterar `getBalance(storeId)` para retornar o **saldo disponível** (`demo_ativo + bonus_balance + purchased_balance`), derivado de `demo_expires_at` **sem materializar**, usando leitura RLS/`authenticated` (não RPC service_role). `getBalanceBreakdown(storeId)` inclui `demoBalance`, `demoExpiresAt`, `bonusBalance`, `purchasedBalance`, `balance` (bruto), `availableBalance`. NUNCA incluir demo vencido.
  </action>
  <acceptance_criteria>
    - `getBalance` exclui demo vencido e inclui demo ativo; leitura por RLS, acesso cruzado negado.
    - `getBalanceBreakdown` retorna as 6 chaves especificadas.
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path "src/lib/credit/credit-service.ts" -Pattern "availableBalance|demoExpiresAt|demoBalance"</automated>
  </verify>
  <done>credit-service expõe saldo disponível e breakdown demo.</done>
</task>

<task type="auto">
  <name>Task 4.2: types.ts + labels.ts — demo/expiration</name>
  <files>src/lib/credit/types.ts</files>
  <read_first>
    - `src/lib/credit/types.ts`, `src/lib/credit/labels.ts` (estado atual)
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/credit-service/spec.md`
  </read_first>
  <action>
    Estender `CreditTransactionTypeSchema` com `demo` e `expiration` (além dos 7 existentes) e `labels.ts` com os rótulos "Demonstração" (demo) e "Expiração" (expiration). Estender a interface `CreditBalance` com `demoBalance`/`demoExpiresAt`/`availableBalance`.
  </action>
  <acceptance_criteria>
    - Schema aceita `demo`/`expiration`; labels "Demonstração"/"Expiração" presentes; `CreditBalance` estendido.
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path "src/lib/credit/types.ts" -Pattern "'demo'|'expiration'|demoBalance|demoExpiresAt|availableBalance"</automated>
  </verify>
  <done>Tipos e labels estendidos.</done>
</task>

<task type="auto">
  <name>Task 4.3: freemium/entitlement-service — checkDemoEligibility + grantDemoEntitlement</name>
  <files>src/lib/freemium/entitlement-service.ts</files>
  <read_first>
    - `src/lib/freemium/entitlement-service.ts` (estado atual)
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/freemium-entitlement/spec.md`
  </read_first>
  <action>
    Adicionar `checkDemoEligibility(rootHash): Promise<boolean>` (true somente quando a raiz não possui entitlement `onboarding` nem `demo`) e `grantDemoEntitlement(storeId, rootHash)` (INSERT ON CONFLICT DO NOTHING, `benefit_type='demo'`, retorna UUID ou null).
  </action>
  <acceptance_criteria>
    - `checkDemoEligibility` retorna true/false conforme ausência de `onboarding` e `demo`.
    - `grantDemoEntitlement` idempotente (UUID na 1ª, null na 2ª).
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path "src/lib/freemium/entitlement-service.ts" -Pattern "checkDemoEligibility|grantDemoEntitlement"</automated>
  </verify>
  <done>checkDemoEligibility/grantDemoEntitlement adicionados.</done>
</task>

<task type="auto">
  <name>Task 4.4: launch-config — flags demo/email (fail-closed)</name>
  <files>src/lib/launch-config/config.ts</files>
  <read_first>
    - `src/lib/launch-config/config.ts` (estado atual)
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/launch-config/spec.md`
  </read_first>
  <action>
    Estender `LaunchConfig` com `demoCreditsEnabled` (default false), `demoCreditsAmount` (default 10), `demoCreditsTtlHours` (default 168), `emailEnabled` (default false). `monthlyCreditsEnabled` **default true** (preservado até o corte). Mapear env vars: `VENDEO_DEMO_CREDITS_ENABLED`, `VENDEO_DEMO_CREDITS_AMOUNT`, `VENDEO_DEMO_CREDITS_TTL_HOURS`, `VENDEO_EMAIL_ENABLED`; `VENDEO_MONTHLY_CREDITS_ENABLED` mantém default true.
  </action>
  <acceptance_criteria>
    - `getLaunchConfig()` sem env vars retorna `demoCreditsEnabled=false`, `demoCreditsAmount=10`, `demoCreditsTtlHours=168`, `emailEnabled=false`, `monthlyCreditsEnabled=true`.
    - `VENDEO_DEMO_CREDITS_ENABLED=true` → `demoCreditsEnabled=true`.
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path "src/lib/launch-config/config.ts" -Pattern "demoCreditsEnabled|demoCreditsAmount|demoCreditsTtlHours|emailEnabled|monthlyCreditsEnabled"</automated>
  </verify>
  <done>Flags de demo/email adicionadas ao launch-config.</done>
</task>

<task type="auto">
  <name>Task 4.5: Migrar consumidores de credit_balances.balance para saldo disponível</name>
  <files>src/lib/credit/credit-service.ts</files>
  <read_first>
    - `.planning/phases/50-demonstracao-gratuita-validade-creditos/50-BASELINE.txt` (inventário 1.4)
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/design.md` (D1)
  </read_first>
  <action>
    Migrar cada consumidor mapeado em 1.4 (dashboard, conta, admin users summary, `admin_get_metrics` wallet, `ai-cost/admin-service`, gate de geração) de leitura de `balance` bruto para o **saldo disponível** (via `getBalance`/`getBalanceBreakdown`). Nenhuma superfície de exibição deve exibir créditos expirados.
  </action>
  <acceptance_criteria>
    - Nenhum consumidor listado em 1.4 lê `balance` bruto diretamente; todos usam saldo disponível.
    - O gate de geração e as superfícies de saldo usam saldo disponível.
  </acceptance_criteria>
  <verify>
    <automated>rg "\.select\(`"balance`"\)|\.balance\b" src/lib/credit src/app/api/campaign/generate-image src/app/api/store -n</automated>
  </verify>
  <done>Consumidores de `balance` migrados para saldo disponível.</done>
</task>

<task type="auto">
  <name>Task 4.6: demo-status.ts — getDemoStatus + formatRelativeExpiry</name>
  <files>src/lib/credit/demo-status.ts</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/demo-status-ui/spec.md`
  </read_first>
  <action>
    Criar `src/lib/credit/demo-status.ts` (módulo puro, sem JSX/server-only) com `getDemoStatus(store)` → `active | expiring_soon | exhausted | expired | none` (estados mutuamente exclusivos, conforme D11: `none` quando `origin_demo_grant_tx_id IS NULL`; `expired` quando grant presente e `expires_at<=now()` ou NULL pós-materialização; `exhausted` quando `expires_at>now()` e `balance=0`; `expiring_soon` `0<até≤24h` e `balance>0`; `active` `>24h` e `balance>0`) e `formatRelativeExpiry` ("expira em X dias/horas"). Fonte: `demo_expires_at` (TIMESTAMPTZ absoluto).
  </action>
  <acceptance_criteria>
    - `getDemoStatus` cobre os 5 estados e o caso `expired` pós-materialização (`demo_expires_at IS NULL` + `origin_demo_grant_tx_id` presente).
    - `formatRelativeExpiry` produz "expira em X dias"/"expira em X horas".
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path "src/lib/credit/demo-status.ts" -Pattern "getDemoStatus|formatRelativeExpiry|expiring_soon|exhausted"</automated>
  </verify>
  <done>Helper puro getDemoStatus/formatRelativeExpiry criado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Serviços server-only (saldo/entitlement/config) | Leitura de saldo autoritativa e configuração fail-closed. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-15 | Information Disclosure | `getBalance` | mitigate | Leitura RLS `authenticated`; acesso cruzado negado. |
| T-50-16 | Tampering | launch-config | mitigate | Defaults fail-closed (demo/email false). |
| T-50-17 | Elevation of Privilege | entitlement-service | mitigate | service_role; elegibilidade validada antes do grant. |
</threat_model>

<verification>
- `getBalance`/`getBalanceBreakdown` usam saldo disponível (sem materializar).
- launch-config defaults fail-closed; `monthlyCreditsEnabled` default true.
- `checkDemoEligibility`/`getDemoStatus` corretos.
</verification>

<success_criteria>
- D4/D7/D9/D15/D24 implementados na camada de serviço.
- Consumidores de `balance` migrados para saldo disponível.
</success_criteria>

<output>
Create `.planning/phases/50-demonstracao-gratuita-validade-creditos/50-04-SUMMARY.md` when done.
</output>

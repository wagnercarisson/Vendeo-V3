---
phase: 50-demonstracao-gratuita-validade-creditos
plan: 05
type: execute
wave: 5
depends_on: [50-03, 50-04, 50-08]
files_modified:
  - src/app/api/store/route.ts
  - src/app/api/store/update-cnpj/route.ts
  - src/app/api/admin/reviews/[id]/approve/route.ts
  - src/app/api/admin/reviews/[id]/exception/route.ts
  - src/app/api/admin/stores/route.ts
  - src/app/api/admin/credits/grant/route.ts
  - src/app/api/admin/freemium/exception/route.ts
  - src/app/api/campaign/generate-image/route.ts
  - src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts
autonomous: true
requirements:
  - onboarding-grant
  - demo-credit-grant
  - monthly-credits-engine
  - launch-config
must_haves:
  truths:
    - "as rotas repassam p_demo_grant_enabled (de getLaunchConfig) aos wrappers SQL; onboardingGranted reflete o retorno real da RPC"
    - "update_store_cnpj concede demo quando approved + flag ativa + raiz sem benefício anterior; marcador legado só com evidência real"
    - "mensal permanece ativo até o corte (cron + botão continuam)"
  artifacts:
    - path: "src/app/api/store/route.ts"
      provides: "switch onboarding→demo com flag"
      contains: "concessão demo"
---

<objective>
Rotas de concessão (D3/D8/D9/D16): repassar `p_demo_grant_enabled` (de `getLaunchConfig().demoCreditsEnabled`) aos wrappers SQL (`create_store_with_cnpj`/`update_store_cnpj`/`admin_approve_store_verification`/`admin_exception_store_verification`, redefinidos em 50-03 task 3.8); manter o mensal ativo até o corte; alinhar as rotas de geração ao saldo disponível.

Purpose: Implementar D3/D8/D9/D16 nas rotas (a redefinição SQL dos wrappers fica em 50-03).
Output: rotas atualizadas, sem mudança no contrato de erro 402.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-demonstracao-gratuita-validade-creditos/50-CONTEXT.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/tasks.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/design.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/onboarding-grant/spec.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/demo-credit-grant/spec.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/monthly-credits-engine/spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 5.1: Rotas passam p_demo_grant_enabled aos wrappers SQL</name>
  <files>src/app/api/store/route.ts, src/app/api/admin/reviews/[id]/approve/route.ts, src/app/api/admin/reviews/[id]/exception/route.ts</files>
  <read_first>
    - `src/app/api/store/route.ts` (chama create_store_with_cnpj)
    - `src/app/api/admin/reviews/[id]/approve/route.ts` (chama admin_approve_store_verification)
    - `src/app/api/admin/reviews/[id]/exception/route.ts` (chama admin_exception_store_verification)
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/demo-credit-grant/spec.md`
  </read_first>
  <action>
    Nas rotas `store/route.ts`, `admin/reviews/[id]/approve/route.ts` e `admin/reviews/[id]/exception/route.ts`, ler `getLaunchConfig().demoCreditsEnabled` e repassar como `p_demo_grant_enabled` ao chamar as RPCs wrapper (`create_store_with_cnpj`/`admin_approve_store_verification`/`admin_exception_store_verification`, redefinidas em 50-03 task 3.8). A concessão demo é feita pela RPC; `onboardingGranted` vem do retorno da RPC (reflete `granted`).
  </action>
  <acceptance_criteria>
    - Loja `approved` de raiz nova + flag ativa → demo (10, TTL 168h) e `onboardingGranted=true`.
    - Flag off → nenhuma concessão (RPC retorna `disabled`).
    - Raiz com `onboarding`/`demo` → sem concessão, `onboardingGranted=false`.
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path "src/app/api/store/route.ts","src/app/api/admin/reviews/[id]/approve/route.ts","src/app/api/admin/reviews/[id]/exception/route.ts" -Pattern "p_demo_grant_enabled|demoCreditsEnabled"</automated>
  </verify>
  <done>Rotas repassam p_demo_grant_enabled aos wrappers SQL.</done>
</task>

<task type="auto">
  <name>Task 5.2: update-cnpj passa p_demo_grant_enabled (loja draft elegível)</name>
  <files>src/app/api/store/update-cnpj/route.ts</files>
  <read_first>
    - `src/app/api/store/update-cnpj/route.ts` (chama update_store_cnpj)
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/onboarding-grant/spec.md`
  </read_first>
  <action>
    Em `src/app/api/store/update-cnpj/route.ts`, ler `getLaunchConfig().demoCreditsEnabled` e repassar `p_demo_grant_enabled` ao chamar a RPC `update_store_cnpj` (redefinida em 50-03 task 3.8). A RPC concede a demo quando a verificação resulta em `approved` + flag ativa + raiz sem benefício anterior, e cria o marcador `legacy_pre_f32_onboarding_consumed` somente com evidência real. A rota apenas repassa a flag e propaga o retorno.
  </action>
  <acceptance_criteria>
    - Loja draft aprovada + elegível recebe demo; raiz sem benefício anterior não é bloqueada.
    - Marcador legado criado apenas com evidência real (sem grant nesse caso).
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path "src/app/api/store/update-cnpj/route.ts" -Pattern "p_demo_grant_enabled|demoCreditsEnabled|update_store_cnpj"</automated>
  </verify>
  <done>update-cnpj repassa p_demo_grant_enabled.</done>
</task>

<task type="auto">
  <name>Task 5.3: Manter mensal ativo até o corte</name>
  <files>src/app/api/admin/credits/grant/route.ts</files>
  <read_first>
    - `src/app/api/cron/monthly-credits/route.ts`, `src/app/api/admin/monthly-credits/grant/route.ts` (estado atual)
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/monthly-credits-engine/spec.md`
  </read_first>
  <action>
    Garantir que cron `/api/cron/monthly-credits` e botão admin continuam ativos (nenhuma mudança de comportamento) até o corte. Preparar o desligamento coordenado (50-14): quando `monthlyCreditsEnabled=false`, o cron/botão retornam indisponível. Não reativar mensal em rollback.
  </action>
  <acceptance_criteria>
    - Mensal continua ativo pré-corte (nenhuma janela de vácuo).
    - Desligamento coordenado no corte via `monthlyCreditsEnabled=false`.
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path "src/app/api/cron/monthly-credits/route.ts" -Pattern "monthlyCreditsEnabled"</automated>
  </verify>
  <done>Mensal preservado até o corte; desligamento preparado.</done>
</task>

<task type="auto">
  <name>Task 5.4: Rotas de geração usam saldo disponível (contrato 402 preservado)</name>
  <files>src/app/api/campaign/generate-image/route.ts</files>
  <read_first>
    - `src/app/api/campaign/generate-image/route.ts` (gate/reserve/refund)
    - `src/app/api/store/[id]/visual-signature/generate-without-logo/route.ts`
  </read_first>
  <action>
    Confirmar que `generate-image/route.ts` e `generate-without-logo/route.ts` usam o **saldo disponível** (via `getBalance` já migrado em 50-04) no gate, preservando o contrato de erro `402 saldo_insuficiente` e os refunds (4 pontos na campanha; VS). Sem mudança de contrato externo. Emitir o evento de produto `first_generation` (via `ProductEventService.record`, de 50-08) na **primeira geração concluída com sucesso após o grant da demo**, com `dedup_key = origin_demo_grant_tx_id` (dedup por grant, não "primeira por loja") — best-effort.
  </action>
  <acceptance_criteria>
    - Gate usa saldo disponível; contrato 402 inalterado; refunds preservados.
    - `first_generation` emitido na primeira geração bem-sucedida pós-grant, dedup por `grant_tx_id` (best-effort).
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path "src/app/api/campaign/generate-image/route.ts" -Pattern "getBalance|reserveCredit|refundCredit|saldo_insuficiente|first_generation"</automated>
  </verify>
  <done>Rotas de geração alinhadas ao saldo disponível; `first_generation` emitido.</done>
</task>

<task type="auto">
  <name>Task 5.5: Admin grant/exception — bônus não-expirável; remover mensal só no corte</name>
  <files>src/app/api/admin/credits/grant/route.ts</files>
  <read_first>
    - `src/app/api/admin/credits/grant/route.ts`, `src/app/api/admin/freemium/exception/route.ts`
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/admin-credit-grant/spec.md`
  </read_first>
  <action>
    Manter `admin_grant_credits`/`admin_exception` como **bônus não-expirável** (sem prazo informativo em metadata). Remover/desativar o botão de concessão mensal **somente no corte** (50-14).
  </action>
  <acceptance_criteria>
    - Grant admin incrementa `bonus_balance` sem expiração; `grant_type='admin_grant'`.
    - Botão mensal permanece até o corte.
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path "src/app/api/admin/credits/grant/route.ts" -Pattern "admin_grant|bonus_balance"</automated>
  </verify>
  <done>Admin grant bônus não-expirável confirmado.</done>
</task>

<task type="auto">
  <name>Task 5.6: Criação administrativa sem CNPJ não concede créditos (admin/stores)</name>
  <files>src/app/api/admin/stores/route.ts</files>
  <read_first>
    - `src/app/api/admin/stores/route.ts` (caller `admin_create_store_for_user` em :24)
    - `supabase/migrations/20260914000002_f47_fix_admin_create_store_lint.sql` (concessão legada neutralizada em 50-03 task 3.9)
  </read_first>
  <action>
    Confirmar que `src/app/api/admin/stores/route.ts` continua chamando `admin_create_store_for_user` (redefinida em 50-03 task 3.9 para **não** conceder créditos). A rota não repassa `p_demo_grant_enabled` nem concede demo — a loja criada por admin sem CNPJ fica **sem saldo** até o CNPJ ser aprovado via `update-cnpj`. Sem mudança de comportamento da rota além do efeito da RPC redefinida; apenas registrar/verificar o contrato.
  </action>
  <acceptance_criteria>
    - `POST /api/admin/stores` cria a loja e retorna sem conceder `bonus_onboarding`/demo (zero créditos).
    - Demo só é concedida posteriormente via `update-cnpj` (approved + flag + raiz sem benefício).
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path "src/app/api/admin/stores/route.ts" -Pattern "admin_create_store_for_user"</automated>
  </verify>
  <done>Criação administrativa sem CNPJ confirmada sem concessão de créditos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Rotas de concessão (store/admin) | Caminho crítico de elegibilidade e concessão demo. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-18 | Elevation of Privilege | concessão demo | mitigate | `p_demo_grant_enabled` do `getLaunchConfig` (fail-closed); elegibilidade na RPC. |
| T-50-19 | Tampering | marcador legado | mitigate | Criado somente com evidência real de benefício anterior. |
| T-50-20 | Repudiation | grant | mitigate | `admin_audit_log` (`credit_grant`, `grant_type='demo'`). |
</threat_model>

<verification>
- Onboarding→demo nas 3 rotas + update_store_cnpj draft elegível.
- Mensal preservado até o corte; contrato 402 intacto.
</verification>

<success_criteria>
- D3/D8/D9/D16 implementados nas rotas.
- Nenhuma loja aprovada sem grant por falha entre chamadas.
</success_criteria>

<output>
Create `.planning/phases/50-demonstracao-gratuita-validade-creditos/50-05-SUMMARY.md` when done.
</output>

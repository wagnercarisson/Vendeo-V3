---
phase: 50-demonstracao-gratuita-validade-creditos
plan: 06
type: execute
wave: 6
depends_on: [50-03, 50-04, 50-05, 50-07, 50-08]
files_modified:
  - src/app/api/cron/demo-credits/route.ts
  - src/app/api/support/credit-request/route.ts
  - src/app/api/admin/support-credit-requests/route.ts
  - src/app/(app)/admin/support-credit-requests/page.tsx
  - vercel.json
autonomous: true
requirements:
  - credit-expiration
  - credit-notifications
  - support-protocol
  - product-events
  - monthly-credits-cron
must_haves:
  truths:
    - "cron demo-credits materializa expirações, deriva notificações/eventos de evidência durável e repara outbox ausentes, idempotente"
    - "POST /api/support/credit-request grava solicitação + support_ack + support_notice atomicamente e retorna protocol+receivedAt"
    - "vercel.json ganha entrada demo-credits com cadência conforme plano Vercel (Hobby diário / Pro horário)"
  artifacts:
    - path: "src/app/api/cron/demo-credits/route.ts"
      provides: "reconciliador demo"
      contains: "cron reconcilador"
---

<objective>
Reconciliador cron `demo-credits` (materializa expirações de contas inativas, deriva/repara notificações e eventos a partir de evidência durável) e endpoint de suporte `POST /api/support/credit-request` + admin de solicitações.

Purpose: Implementar D5/D12/D14/D22/D23 (evidência durável + reparo + suporte honesto).
Output: cron, rota de suporte, rota admin, entrada em `vercel.json`.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-demonstracao-gratuita-validade-creditos/50-CONTEXT.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/tasks.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/design.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/credit-expiration/spec.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/support-protocol/spec.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/credit-cta/spec.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/credit-notifications/spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 6.1: GET /api/cron/demo-credits (reconciliador)</name>
  <files>src/app/api/cron/demo-credits/route.ts</files>
  <read_first>
    - `src/app/api/cron/monthly-credits/route.ts` (padrão CRON_SECRET + middleware passthrough)
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/credit-expiration/spec.md`
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/credit-notifications/spec.md`
  </read_first>
  <action>
    Criar `GET /api/cron/demo-credits` (valida `Authorization: Bearer <CRON_SECRET>`): (a) materializar expirações de contas inativas via `materialize_demo_expiration`; (b) **derivar** `demo_granted` (da transação `demo`), `demo_expired` (da `expiration`), `demo_exhausted` (da deduction de esgotamento) e `demo_expiring_24h`, **reparando** outbox/eventos ausentes (dedup por `grant_tx_id`/`expiration_tx_id`/`deduction_tx_id`); (c) disparar envio de email pendente (claim/lease + reclaim). Tudo idempotente por dedup key.
  </action>
  <acceptance_criteria>
    - Cron materializa expirações, deriva/repara notificações e eventos, e despacha email pendente, idempotente.
    - `demo_expired` derivado da transação `expiration` (não suprimido se `reserve_credit` materializou antes).
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path "src/app/api/cron/demo-credits/route.ts" -Pattern "CRON_SECRET|materialize_demo_expiration|demo_expired|demo_granted|demo_exhausted"</automated>
  </verify>
  <done>Cron reconciliador demo-credits criado.</done>
</task>

<task type="auto">
  <name>Task 6.2: vercel.json — cadência conforme plano Vercel</name>
  <files>vercel.json</files>
  <read_first>
    - `vercel.json` (entrada atual do cron mensal)
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/design.md` (D23)
  </read_first>
  <action>
    Adicionar entrada `demo-credits` em `vercel.json` com **cadência conforme o plano Vercel real**: Hobby → **diário** (comunicado "aproximadamente 24 horas", lookahead 48h); Pro → **horário** (lookahead 24h). Confirmar o plano real antes de fixar a cadência; registrar o pré-requisito (Pro para horária).
  </action>
  <acceptance_criteria>
    - `vercel.json` tem entrada `demo-credits` com cadência documentada conforme o plano (diário no Hobby).
    - Cron mensal preservado até o corte (desativado em 50-14).
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path "vercel.json" -Pattern "demo-credits"</automated>
  </verify>
  <done>Entrada de cron demo-credits adicionada.</done>
</task>

<task type="auto">
  <name>Task 6.3: POST /api/support/credit-request (durável + idempotente)</name>
  <files>src/app/api/support/credit-request/route.ts</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/support-protocol/spec.md`
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/credit-cta/spec.md`
  </read_first>
  <action>
    Criar `POST /api/support/credit-request` idempotente por `operationId`: grava `support_credit_requests` (canônica, `protocol`/timestamps) + `support_ack` (ao usuário) + `support_notice` (ao suporte) **atomicamente**; retorna `protocol` + `receivedAt`. Se qualquer gravação durável falhar, **falha explicitamente** (não afirma recebimento). Telemetria `support_credit_request` best-effort e separada. O `support_ack` é o **auto-ack obrigatório que cumpre a cláusula 12.3 dos Termos** — nunca suprimido pela flag de email (permanece `pending`/retry).
  </action>
  <acceptance_criteria>
    - Solicitação + ack + notice atômicos; repetição de `operationId` não duplica; falha de gravação → erro explícito.
    - Resposta inclui `protocol` + `receivedAt`; `response_due_at` NULL.
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path "src/app/api/support/credit-request/route.ts" -Pattern "operationId|protocol|support_ack|support_notice|receivedAt"</automated>
  </verify>
  <done>Rota de solicitação de créditos criada (durável/idempotente).</done>
</task>

<task type="auto">
  <name>Task 6.4: GET + PATCH /api/admin/support-credit-requests + página admin</name>
  <files>src/app/api/admin/support-credit-requests/route.ts</files>
  <read_first>
    - `src/app/api/admin/**` (padrão requireAdmin)
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/support-protocol/spec.md`
  </read_first>
  <action>
    Criar `GET /api/admin/support-credit-requests` (requireAdmin) e `PATCH /api/admin/support-credit-requests` (mutação de status + reconsideração), com página admin correspondente (`src/app/(app)/admin/support-credit-requests/page.tsx`). O `PATCH` transiciona `received`→`forwarded`/`responded`/`closed` (preenchendo `acknowledged_at`/`responded_at`/`closed_at` conforme o fluxo), permite **reconsiderar manualmente a elegibilidade** (sem sistema formal de recurso, sem revelar regras antifraude) e audita toda mutação em `admin_audit_log`.
  </action>
  <acceptance_criteria>
    - `GET` lista e `PATCH` muta status com timestamps; reconsideração manual disponível; mutações auditadas.
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path "src/app/api/admin/support-credit-requests/route.ts" -Pattern "requireAdmin|support_credit_requests|PATCH|admin_audit_log"</automated>
  </verify>
  <done>Admin de solicitações de crédito (listar + mutar + reconsiderar) criado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Cron + rota de suporte | Caminho de reconciliação e solicitação de créditos. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-21 | Spoofing | cron | mitigate | `CRON_SECRET` bearer + middleware passthrough. |
| T-50-22 | Tampering | reconciliação | mitigate | Dedup keys + evidência durável; idempotente. |
| T-50-23 | Information Disclosure | reconsideração | mitigate | Não expõe regras antifraude. |
</threat_model>

<verification>
- Cron materializa/deriva/repara idempotente; suporte durável/idempotente.
- Cadência conforme plano Vercel.
</verification>

<success_criteria>
- D5/D12/D14/D22/D23 implementados.
- Confirmação durável com protocolo no próprio canal.
</success_criteria>

<output>
Create `.planning/phases/50-demonstracao-gratuita-validade-creditos/50-06-SUMMARY.md` when done.
</output>

---
phase: 50-demonstracao-gratuita-validade-creditos
plan: 07
type: execute
wave: 3
depends_on: [50-02]
files_modified:
  - src/lib/notifications/outbox.ts
  - src/lib/email/resend.ts
  - src/components/notifications/notification-bell.tsx
  - src/components/notifications/notification-list.tsx
  - src/components/shell/topbar.tsx
  - src/app/api/notifications/read/route.ts
autonomous: true
requirements:
  - credit-notifications
must_haves:
  truths:
    - "outbox com dedup lógico (store_id, kind, dedup_key); notificações de demo best-effort; support_ack/support_notice duráveis e atômicos"
    - "claim atômico com reclaim por lease vencido; máquina de estados pending→processing→sent|failed|suppressed"
    - "supressão só para demo (flag off / janela perdida); support_* nunca suprimidos; sent≠delivered"
  artifacts:
    - path: "src/lib/email/resend.ts"
      provides: "envio Resend gated por VENDEO_EMAIL_ENABLED"
      contains: "serviço de email"
---

<objective>
Notificações in-app + email transacional (Resend) via outbox: escrita com dedup lógico, envio assíncrono não-bloqueante com claim/lease anti-duplicidade, máquina de estados, supressão (só demo) e superfície in-app (badge/bell + lista).

Purpose: Implementar D10 (notificações idempotentes e não-bloqueantes).
Output: `src/lib/notifications`, `src/lib/email`, componentes in-app.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-demonstracao-gratuita-validade-creditos/50-CONTEXT.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/tasks.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/design.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/credit-notifications/spec.md
</context>

<tasks>

<task type="auto" status="done">
  <name>Task 7.1: src/lib/notifications — outbox com dedup lógico</name>
  <files>src/lib/notifications/outbox.ts</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/credit-notifications/spec.md`
    - `supabase/migrations/20260920000001_f50_demo_credits.sql` (schema `credit_notifications`)
  </read_first>
  <action>
    Criar `src/lib/notifications` para escrita de outbox com dedup lógico por `(store_id, kind, dedup_key)`; **notificações de demonstração best-effort** (nunca interrompem grant/consumo/expiração); **`support_ack`/`support_notice` duráveis e atômicos com a solicitação** (exceção). Enfileirar `demo_granted` (dedup grant_tx_id), `demo_expiring_24h` (dedup `demo_expires_at` truncado à hora), `demo_expired` (dedup expiration_tx_id), `demo_exhausted` (dedup deduction_tx_id).
  </action>
  <acceptance_criteria>
    - Dedup por `(store_id, kind, dedup_key)`; `support_*` atômicos com a solicitação; demo best-effort.
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path "src/lib/notifications/outbox.ts" -Pattern "dedup_key|support_ack|support_notice|demo_granted|demo_expiring_24h"</automated>
  </verify>
  <done>Outbox criado com dedup lógico.</done>
</task>

<task type="auto" status="done">
  <name>Task 7.2: src/lib/email — Resend + claim/lease + máquina de estados + supressão</name>
  <files>src/lib/email/resend.ts</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/credit-notifications/spec.md`
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/design.md` (D10/D23)
  </read_first>
  <action>
    Criar `src/lib/email` (Resend via SDK/HTTP), gated por `VENDEO_EMAIL_ENABLED`. **Claim atômico** (`pending` due OU `processing` com `lease_expires_at<=now()` → `processing`+lease) com **reclaim por lease vencido**. Máquina de estados `pending → processing → sent | failed | suppressed`: falha retryable → `pending`+`next_attempt_at` (backoff); `failed` terminal/dead-letter (visível no admin); `attempt_count`/`max attempts`/`last_error`/`provider_message_id`/`email_sent_at`. **Supressão** (só demo): flag off → `suppressed`; janela temporal perdida → `suppressed`; `support_ack`/`support_notice` **nunca suprimidos** (permanecem `pending`/retry, falha permanente visível no admin). `email_delivered_at` reservado (webhook, fora do escopo). Remetente `VENDEO_EMAIL_FROM` default `noreply@vendeo.tech`.
  </action>
  <acceptance_criteria>
    - Dois workers não enviam o mesmo email (claim atômico); reclaim por lease vencido.
    - Retryable → `pending`+`next_attempt_at`; `failed` terminal; supressão só demo; `support_*` nunca suprimidos.
    - `email_sent_at` = provedor aceitou (não entregue); flag off não envia.
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path "src/lib/email/resend.ts" -Pattern "VENDEO_EMAIL_ENABLED|lease_expires_at|suppressed|pending|processing|failed|sent"</automated>
  </verify>
  <done>Serviço de email com claim/lease e máquina de estados.</done>
</task>

<task type="auto" status="done">
  <name>Task 7.3: Superfície in-app — bell/badge + lista + marcação de leitura (com endpoint)</name>
  <files>src/components/notifications/notification-bell.tsx</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/credit-notifications/spec.md`
    - `src/components/shell/topbar.tsx` (nav — onde montar o bell)
    - `supabase/migrations/20260920000001_f50_demo_credits.sql` (RLS: mutações só via service_role)
  </read_first>
  <action>
    Criar bell/badge na nav (`src/components/shell/topbar.tsx`) + lista de notificações (`src/components/notifications/notification-list.tsx`, exibida em `/conta`) lidas de `credit_notifications`, com badge refletindo não-lidas (`inapp_read_at IS NULL`). Como a RLS proíbe escrita direta por `authenticated`, criar o mecanismo de marcação de leitura: **`POST /api/notifications/read`** (rota autenticada server-side que valida `auth.uid()` + propriedade da loja e atualiza `inapp_read_at` via service_role de forma idempotente). Declarar a rota em `src/app/api/notifications/read/route.ts`.
  </action>
  <acceptance_criteria>
    - Badge indica pendências; marcação de leitura idempotente via `POST /api/notifications/read` (nunca escrita direta `authenticated`).
    - `inapp_read_at` preenchido uma única vez.
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path "src/app/api/notifications/read/route.ts" -Pattern "auth.uid|inapp_read_at|service_role"</automated>
  </verify>
  <done>Superfície in-app (bell/badge + lista + endpoint de leitura) criada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Outbox/email/in-app | Notificações de marcos da demo e suporte. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-24 | Tampering | claim/lease | mitigate | Claim atômico `FOR UPDATE SKIP LOCKED`; reclaim por lease. |
| T-50-25 | Information Disclosure | notificações | mitigate | RLS: owner lê as próprias; `support_notice` não exposto ao owner. |
| T-50-26 | DoS | envio | mitigate | Não-bloqueante; best-effort para demo; dead-letter listável. |
</threat_model>

<verification>
- Dedup lógico, claim/lease, máquina de estados, supressão (só demo).
- `sent`≠`delivered`; in-app leitura idempotente.
</verification>

<success_criteria>
- D10 implementado; notificações idempotentes e não-bloqueantes.
- `support_ack`/`support_notice` duráveis e nunca suprimidos.
</success_criteria>

<output>
Create `.planning/phases/50-demonstracao-gratuita-validade-creditos/50-07-SUMMARY.md` when done.
</output>

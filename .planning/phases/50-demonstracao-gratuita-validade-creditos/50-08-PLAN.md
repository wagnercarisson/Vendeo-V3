---
phase: 50-demonstracao-gratuita-validade-creditos
plan: 08
type: execute
wave: 3
depends_on: [50-02]
files_modified:
  - src/lib/product-events/service.ts
  - src/lib/product-events/__tests__/service.test.ts
autonomous: true
requirements:
  - product-events
must_haves:
  truths:
    - "ProductEventService.record é best-effort/fail-open com dedup por (event_type, dedup_key)"
    - "o serviço expõe a assinatura única usada pelos callers (concessão/geração/consumo/expiração/suporte)"
    - "nenhuma chamada paga e nenhuma escrita fora do serviço; falha de escrita nunca bloqueia"
  artifacts:
    - path: "src/lib/product-events/service.ts"
      provides: "ProductEventService.record (best-effort)"
      contains: "telemetria de produto"
    - path: "src/lib/product-events/__tests__/service.test.ts"
      provides: "teste unitário do serviço (dedup + fail-open)"
      contains: "teste"
---

<objective>
Criar **apenas** o serviço de telemetria de produto `ProductEventService.record` (best-effort/fail-open, dedup por `(event_type, dedup_key)`) e seu teste unitário.

Purpose: Implementar D14 como infraestrutura. A **emissão** de cada evento é responsabilidade do plano proprietário do fluxo — não deste plano:
- `demo_granted` → 50-03 (RPC `grant_demo_credits`, em SQL) e reparo no 50-06 (cron);
- `first_generation` → 50-05 (rota de geração, na primeira geração concluída após o grant);
- `demo_exhausted`/`demo_expired` → 50-06 (cron, derivados da evidência durável);
- `support_credit_request` → 50-06 (rota de suporte).

Output: `src/lib/product-events/service.ts` + `service.test.ts`.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-demonstracao-gratuita-validade-creditos/50-CONTEXT.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/tasks.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/design.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/product-events/spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 8.1: ProductEventService.record (best-effort, fail-open, dedup)</name>
  <files>src/lib/product-events/service.ts</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/product-events/spec.md`
    - `supabase/migrations/20260920000001_f50_demo_credits.sql` (schema `product_events`)
  </read_first>
  <action>
    Criar `src/lib/product-events/service.ts` com `ProductEventService.record(eventType, { store_id, user_id, dedup_key, properties })` — best-effort, fail-open (falha NUNCA bloqueia o fluxo, apenas loga), com índice único `(event_type, dedup_key)` para idempotência. Escrita via service_role. A assinatura única é o contrato consumido pelos callers TypeScript em 50-05/50-06 (a emissão `demo_granted` em 50-03 ocorre em SQL, não via este serviço).
  </action>
  <acceptance_criteria>
    - `record` é best-effort/fail-open; dedup por `(event_type, dedup_key)`; falha não bloqueia.
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path "src/lib/product-events/service.ts" -Pattern "record|dedup_key|best-effort|fail-open"</automated>
  </verify>
  <done>ProductEventService criado.</done>
</task>

<task type="auto">
  <name>Task 8.2: Teste unitário do serviço (dedup + fail-open)</name>
  <files>src/lib/product-events/__tests__/service.test.ts</files>
  <read_first>
    - `src/lib/product-events/service.ts`
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/product-events/spec.md`
  </read_first>
  <action>
    Criar `service.test.ts` cobrindo: dedup por `(event_type, dedup_key)` (segundo registro é no-op); fail-open (falha de escrita não lança, apenas loga); assinatura aceita `store_id`/`user_id`/`dedup_key`/`properties`. Não testar os call sites aqui (são cobertos em 50-12).
  </action>
  <acceptance_criteria>
    - Teste unitário verde; dedup e fail-open comprovados; nenhuma chamada paga.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/product-events/__tests__/service.test.ts</automated>
  </verify>
  <done>Teste unitário do serviço criado; os cinco eventos e seus callers foram posteriormente verificados em 50-12/50-14.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| product_events | Telemetria de funil (fail-open). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-27 | Tampering | eventos | mitigate | append-only + índice único `(event_type, dedup_key)`. |
| T-50-28 | Information Disclosure | properties | mitigate | Sem dados sensíveis; identidade mínima (store_id/user_id/segment). |
</threat_model>

<verification>
- Serviço com dedup/fail-open; teste unitário verde.
</verification>

<success_criteria>
- D14 (infraestrutura) implementado; callers em 50-03/50-05/50-06 consomem a assinatura única.
</success_criteria>

<output>
Create `.planning/phases/50-demonstracao-gratuita-validade-creditos/50-08-SUMMARY.md` when done.
</output>

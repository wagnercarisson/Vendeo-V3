---
phase: 50-demonstracao-gratuita-validade-creditos
plan: 11
type: execute
wave: 7
depends_on: [50-03, 50-04, 50-06]
files_modified:
  - src/lib/credit/__tests__/demo-status.test.ts
  - src/lib/credit/__tests__/credit-rpc.test.ts
  - src/lib/credit/__tests__/credit-concurrency.test.ts
  - src/lib/credit/__tests__/credit-idempotency.test.ts
autonomous: true
requirements:
  - credit-sql-functions
  - credit-expiration
  - demo-credit-grant
  - credit-service
  - demo-status-ui
  - launch-config
  - freemium-entitlement
must_haves:
  truths:
    - "unidade: getDemoStatus/formatRelativeExpiry/checkDemoEligibility/labels/defaults launch-config cobertos"
    - "banco/RPC: grant_demo_credits (disabled/onboarding_consumed/already_granted/TTL), materialize_demo_expiration (uma vez/no-op), reserve (ordem/saldo_insuficiente), refund (regra temporal) cobertos"
    - "concorrência e idempotência (grant/materialização/reserva/refund/notificação/evento) cobertas"
  artifacts:
    - path: "src/lib/credit/__tests__/demo-status.test.ts"
      provides: "testes unitários getDemoStatus/formatRelativeExpiry"
      contains: "testes"
---

<objective>
Testes de unidade + banco/RPC + concorrência/idempotência da F50 (D1–D8, D16): cobrir os helpers puros, as RPCs SQL, a ordem de consumo, a regra temporal do refund, a concorrência e a idempotência.

Purpose: Provar as invariantes do ledger e a regra temporal antes da integração.
Output: arquivos de teste unitários + de banco/RPC + de concorrência.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-demonstracao-gratuita-validade-creditos/50-CONTEXT.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/tasks.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/design.md
</context>

<tasks>

<task type="auto">
  <name>Task 11.1: Unidade — getDemoStatus/formatRelativeExpiry/checkDemoEligibility/labels/defaults</name>
  <files>src/lib/credit/__tests__/demo-status.test.ts</files>
  <read_first>
    - `src/lib/credit/demo-status.ts`, `src/lib/credit/types.ts`, `src/lib/credit/labels.ts`
    - `src/lib/freemium/entitlement-service.ts`, `src/lib/launch-config/config.ts`
  </read_first>
  <action>
    Testar: `getDemoStatus` (5 estados + `expired` pós-materialização); `formatRelativeExpiry` ("expira em X dias/horas"); `checkDemoEligibility` (true/false); labels "Demonstração"/"Expiração"; defaults de `getLaunchConfig` (demo false/10/168, email false, monthly true).
  </action>
  <acceptance_criteria>
    - Todos os casos acima com asserções exatas.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/credit/__tests__/demo-status.test.ts</automated>
  </verify>
  <done>Testes unitários de helpers/defaults. ✅</done>
</task>

<task type="auto">
  <name>Task 11.2: Banco/RPC — grant_demo_credits + materialize_demo_expiration</name>
  <files>src/lib/credit/__tests__/credit-rpc.test.ts</files>
  <read_first>
    - `supabase/migrations/20260920000001_f50_demo_credits.sql` (RPCs)
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/demo-credit-grant/spec.md`
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/credit-expiration/spec.md`
  </read_first>
  <action>
    Testar em banco real: `grant_demo_credits` (grant único, `disabled`, `onboarding_consumed`, `already_granted`, TTL 168h, `demo_cycle_id`); `materialize_demo_expiration` (materializa uma vez, no-op, `expiration` reference/metadata).
  </action>
  <acceptance_criteria>
    - Cenários de grant e materialização com asserts de `demo_balance`/`demo_expires_at`/`origin_demo_grant_tx_id`.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/credit/__tests__/credit-rpc.test.ts</automated>
  </verify>
  <done>Testes de RPC grant/materialização. ✅</done>
</task>

<task type="auto">
  <name>Task 11.3: reserve_credit — ordem, vencido, saldo_insuficiente, metadata</name>
  <files>src/lib/credit/__tests__/credit-rpc.test.ts</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/credit-sql-functions/spec.md`
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/credit-expiration/spec.md`
  </read_first>
  <action>
    Testar `reserve_credit`: ordem demo→bônus→comprado; demo vencido não consumível (materializado); `saldo_insuficiente`; metadata de origem (`demo_amount`/`bonus_amount`/`purchased_amount`) + snapshot + evidência de esgotamento (`demo_before>0`/`demo_after=0`).
  </action>
  <acceptance_criteria>
    - Ordem, vencido, saldo_insuficiente, metadata e esgotamento com asserts exatos.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/credit/__tests__/credit-rpc.test.ts</automated>
  </verify>
  <done>Testes de reserve_credit. ✅</done>
</task>

<task type="auto">
  <name>Task 11.4: refund_credit — regra temporal (episódios de graça)</name>
  <files>src/lib/credit/__tests__/credit-rpc.test.ts</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/credit-sql-functions/spec.md`
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/credit-expiration/spec.md`
  </read_first>
  <action>
    Testar `refund_credit` (regra temporal): episódio original válido restaura sem estender; episódio de graça ativo `GREATEST(ativo, now()+24h)`; **demo esgotada antes do prazo → prazo passa sem `expiration` → refund abre graça de 24h**; saldo vencido não materializado é materializado antes da graça; `origin_demo_grant_tx_id` preservado; `demo_contributing_tx_ids` acumula; idempotência/duplicidade.
  </action>
  <acceptance_criteria>
    - Os 3 ramos temporais + casos de borda com asserts exatos.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/credit/__tests__/credit-rpc.test.ts</automated>
  </verify>
  <done>Testes de refund_credit (regra temporal). ✅</done>
</task>

<task type="auto">
  <name>Task 11.5: Concorrência — grant/materialização/reserva</name>
  <files>src/lib/credit/__tests__/credit-concurrency.test.ts</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/design.md` (Risks)
  </read_first>
  <action>
    Testar concorrência: grant demo sob corrida (1 transação); materialização concorrente (1 `expiration`); reserva concorrente (sem saldo negativo).
  </action>
  <acceptance_criteria>
    - Exatamente 1 grant/expiration sob corrida; reserva nunca deixa saldo negativo.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/credit/__tests__/credit-concurrency.test.ts</automated>
  </verify>
  <done>Testes de concorrência. ✅</done>
</task>

<task type="auto">
  <name>Task 11.6: Idempotência — concessão/expiração/refund/notificação/evento + atomicidade suporte</name>
  <files>src/lib/credit/__tests__/credit-idempotency.test.ts</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/credit-cta/spec.md`
  </read_first>
  <action>
    Testar idempotência: concessão, expiração, refund, notificações e eventos por dedup key; **atomicidade solicitação + `support_ack` + `support_notice`** (falha de gravação → endpoint falha, sem afirmar recebimento).
  </action>
  <acceptance_criteria>
    - Dedup keys garantem no-op em repetições; atomicidade do suporte comprovada.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/credit/__tests__/credit-idempotency.test.ts</automated>
  </verify>
  <done>Testes de idempotência. ✅</done>
</task>

<task type="auto">
  <name>Task 11.7: Wrappers SQL — assinatura única, privilégios mínimos, fail-closed + criação admin sem concessão + hardening admin_exception</name>
  <files>src/lib/credit/__tests__/credit-rpc.test.ts</files>
  <read_first>
    - `supabase/migrations/20260920000001_f50_demo_credits.sql` (redefinição dos 3 wrappers demo, task 3.8; hardening admin_exception, task 3.9; neutralização da concessão legada, task 3.10)
    - `supabase/migrations/20260727000001_freemium_anti_abuso_cnpj.sql`, `20260728000001_f33_cnpj_verification.sql`, `20260730000001_extend_update_store_cnpj.sql` (assinaturas antigas)
    - `supabase/migrations/20260914000002_f47_fix_admin_create_store_lint.sql` (assinaturas legadas de `create_store_with_initial_grant`/`admin_create_store_for_user`)
  </read_first>
  <action>
    Em `credit-rpc.test.ts`, testar os **3 wrappers demo** (`create_store_with_cnpj`, `update_store_cnpj`, `admin_approve_store_verification`): (1) exatamente **uma** assinatura vigente por wrapper em `pg_proc`; (2) **nenhuma** assinatura legada restante; (3) chamada **sem** `p_demo_grant_enabled` → zero `demo_balance` e zero `bonus_balance` concedidos; (4) `anon` e `authenticated` recebem `permission denied` (REVOKE); (5) `service_role` executa com sucesso; (6) nenhuma rota legítima deixa de funcionar após os REVOKEs (as rotas usam `service_role` pelo backend). **Adicional (task 3.9 — hardening `admin_exception_store_verification`):** testar que a exceção continua concedendo `admin_grant` (bônus, não demo); duas lojas sem CNPJ recebem raiz sintética distinta (`admin_exception_no_cnpj:<store_id>`) sem colisão; retry da mesma exceção NÃO duplica o bônus; e posterior cadastro de CNPJ na loja exceptionada NÃO concede demo (bloqueio por `admin_exception`). **Adicional (task 3.10):** testar `admin_create_store_for_user`/`create_store_with_initial_grant` — criação administrativa **sem CNPJ** cria a loja (e o `admin_audit_log`) **sem** conceder `demo_balance` nem `bonus_balance`; a demo só é concedida posteriormente por `update_store_cnpj` (approved + flag + raiz sem benefício).
  </action>
  <acceptance_criteria>
    - 1 assinatura vigente por wrapper; 0 assinaturas legadas em `pg_proc` (incl. `create_store_with_initial_grant`/`admin_create_store_for_user`).
    - Chamada sem flag → zero demo e zero bônus.
    - `anon`/`authenticated` → `permission denied`; `service_role` executa; rotas legítimas preservadas.
    - `admin_create_store_for_user` cria loja sem créditos; demo posterior via `update_store_cnpj`.
    - `admin_exception_store_verification`: bônus `admin_grant` (não demo); raiz sintética por loja; retry idempotente; demo bloqueada após exceção.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/credit/__tests__/credit-rpc.test.ts</automated>
  </verify>
  <done>Testes de assinatura única + privilégios mínimos + fail-closed dos wrappers. ✅</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Testes (sem dados reais) | Nenhuma chamada paga; banco local/fixtures. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-39 | Tampering | fixtures | accept | Sem dados reais; banco local isolado. |
| T-50-40 | DoS | chamadas reais | accept | Nenhuma chamada paga em teste/CI. |
</threat_model>

<verification>
- Unidade + banco/RPC + concorrência + idempotência verdes.
</verification>

<success_criteria>
- D1–D8, D16 cobertos por testes determinísticos.
</success_criteria>

<output>
Create `.planning/phases/50-demonstracao-gratuita-validade-creditos/50-11-SUMMARY.md` when done.
</output>

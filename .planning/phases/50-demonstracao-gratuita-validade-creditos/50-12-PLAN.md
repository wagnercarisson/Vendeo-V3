---
phase: 50-demonstracao-gratuita-validade-creditos
plan: 12
type: execute
wave: 8
depends_on: [50-05, 50-06, 50-07, 50-08, 50-09, 50-10, 50-15, 50-16]
files_modified:
  - src/app/api/campaign/generate-image/__tests__/route.test.ts
  - src/app/api/support/credit-request/__tests__/route.test.ts
  - src/app/api/store/__tests__/route.test.ts
  - src/app/api/store/update-cnpj/__tests__/route.test.ts
  - src/app/api/admin/reviews/[id]/approve/__tests__/route.test.ts
  - src/app/api/admin/reviews/[id]/exception/__tests__/route.test.ts
  - src/app/api/cron/demo-credits/__tests__/route.test.ts
  - src/components/credit/__tests__/balance-card.test.tsx
  - src/lib/notifications/__tests__/outbox.test.ts
  - src/lib/legal/__tests__/legal-document-versions.test.ts
  - src/lib/product-events/__tests__/service.test.ts
  - src/lib/credit/__tests__/credit-ledger.test.ts
  - src/lib/credit/__tests__/credit-rls.test.ts
  - src/lib/credit/__tests__/credit-rpc.test.ts
  - src/lib/visual-signature/__tests__/persistence-storage.test.ts
  - src/components/landing/__tests__/access-request-form.test.tsx
autonomous: true
requirements:
  - credit-sql-functions
  - credit-expiration
  - credit-notifications
  - product-events
  - support-protocol
  - storage-privacy
  - demo-status-ui
  - legal-document-versions
  - privacy-acknowledgement
  - beta-access-request
must_haves:
  truths:
    - "rotas/cron/suporte/generation/UI/notificações/legal/telemetria/outbox/refund-pós-expiração/beta cobertos por integração"
    - "saldo/RLS: leitura authenticated sem service_role, acesso cruzado negado, leitura não materializa"
    - "regressão do ledger: invariantes balance_before/after lineares preservadas"
  artifacts:
    - path: "src/app/api/support/credit-request/__tests__/route.test.ts"
      provides: "integração do endpoint de suporte"
      contains: "testes de integração"
---

<objective>
Testes de integração + rotas + UI + notificações + legal + telemetria da F50 (tasks.md 12.x): cobrir rotas, geração (gate 402), UI (estados da demo), notificações (claim/lease/supressão), legal (reaceite/ciência), telemetria (dedup), regressão do ledger, storage/privacy, saldo/RLS, suporte, outbox, refund pós-expiração e beta/access.

Purpose: Provar o comportamento fim-a-fim e os contratos externos.
Output: arquivos de teste de integração/rotas/UI.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-demonstracao-gratuita-validade-creditos/50-CONTEXT.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/tasks.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/design.md
</context>

<tasks>

<task type="auto">
  <name>Task 12.1: Rotas de concessão — store (create/update-cnpj) + admin approve/exception + admin create sem concessão</name>
  <files>src/app/api/store/__tests__/route.test.ts, src/app/api/store/update-cnpj/__tests__/route.test.ts, src/app/api/admin/reviews/[id]/approve/__tests__/route.test.ts, src/app/api/admin/reviews/[id]/exception/__tests__/route.test.ts, src/app/api/admin/__tests__/stores.test.ts</files>
  <read_first>
    - `src/app/api/store/__tests__/route.test.ts` (suíte existente de criação)
    - `src/app/api/store/update-cnpj/__tests__/route.test.ts` (suíte existente de update-cnpj)
    - `src/app/api/admin/__tests__/stores.test.ts` (suíte existente de criação admin)
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/tasks.md` (12.1)
  </read_first>
  <action>
    Co-migrar/estender os testes das **rotas de concessão** (os caminhos distintos): `store/route.ts` (create_store) e `store/update-cnpj/route.ts` (update_store_cnpj, loja draft elegível) — suítes existentes a co-migrar — e **criar as suítes novas** para `admin/reviews/[id]/approve/route.ts` (demo) e `admin/reviews/[id]/exception/route.ts` (bônus `admin_grant`, **não** demo — sem `p_demo_grant_enabled`). Cobrir demo × onboarding, flag on/off (`p_demo_grant_enabled`), raiz com benefício anterior (`onboarding`/`demo`/`admin_exception`), `onboardingGranted` refletindo o retorno real, e a exceção concedendo bônus não-expirável. **Adicional (task 5.6):** co-migrar `admin/__tests__/stores.test.ts` — `POST /api/admin/stores` (criação administrativa **sem CNPJ**) cria a loja **sem** créditos; a demo só é concedida posteriormente via `update-cnpj`. (cron e suporte são cobertos em tasks próprias — não aqui.)
  </action>
  <acceptance_criteria>
    - Rotas de concessão cobertas (2 suítes co-migradas + 2 suítes novas de approve/exception).
    - Asserts de concessão demo (flag on/off, raiz com benefício, draft elegível, onboardingGranted).
    - `admin/reviews/[id]/exception` concede bônus `admin_grant` (não demo); `onboardingGranted` reflete bônus.
    - Criação admin sem CNPJ → zero créditos; demo posterior via update-cnpj.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/app/api/store/__tests__/route.test.ts src/app/api/store/update-cnpj/__tests__/route.test.ts src/app/api/admin/reviews/__tests__</automated>
  </verify>
  <done>Testes das 4 rotas de concessão.</done>
</task>

<task type="auto">
  <name>Task 12.2: Geração — gate 402 + evidência durável</name>
  <files>src/app/api/campaign/generate-image/__tests__/route.test.ts</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/tasks.md` (12.2)
  </read_first>
  <action>
    Testar: gate 402 usa saldo disponível; reserve/refund preservam contrato; **evidência durável** — `reserve_credit`/reconciliador materializam, a **leitura não materializa**, e `demo_expired` não é suprimido.
  </action>
  <acceptance_criteria>
    - Contrato 402 preservado; materialização por reserve não suprime `demo_expired`.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/app/api/campaign/generate-image/__tests__/route.test.ts</automated>
  </verify>
  <done>Testes de geração.</done>
</task>

<task type="auto">
  <name>Task 12.3: UI — estados da demo (incl. expired pós-materialização)</name>
  <files>src/components/credit/__tests__/balance-card.test.tsx</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/tasks.md` (12.3)
  </read_first>
  <action>
    Testar: estados da demo (incl. **`expired` após materialização** — `demo_expires_at NULL` com `origin_demo_grant_tx_id` setado), prazo local/relativo, sem SLA, sem linguagem de compra.
  </action>
  <acceptance_criteria>
    - 5 estados + expired pós-materialização cobertos; sem "24h"/compra.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/credit/__tests__/balance-card.test.tsx</automated>
  </verify>
  <done>Testes de UI.</done>
</task>

<task type="auto">
  <name>Task 12.4: Notificações — dedup/claim/lease/supressão/durabilidade</name>
  <files>src/lib/notifications/__tests__/outbox.test.ts</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/tasks.md` (12.4)
  </read_first>
  <action>
    Testar: dedup lógico; flag email off/on; **claim/lease (dois workers não duplicam)**; **supressão (flag off / fora de janela — apenas demo)**; **`support_ack`/`support_notice` duráveis/atômicos, nunca suprimidos, retry preservado**; `sent`≠`delivered`; in-app leitura.
  </action>
  <acceptance_criteria>
    - Cobertura dos cenários de notificação com asserts de estados da máquina.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/notifications/__tests__/outbox.test.ts</automated>
  </verify>
  <done>Testes de notificações.</done>
</task>

<task type="auto">
  <name>Task 12.5: Legal — 3 versões + reaceite contratual × ciência</name>
  <files>src/lib/legal/__tests__/legal-document-versions.test.ts</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/tasks.md` (12.5)
  </read_first>
  <action>
    Testar: três versões publicadas em migration separada; **reaceite contratual (Terms v1.5 + AUP v1.2) e ciência de privacidade (v1.4) testados separadamente**; histórico/downloads não bloqueados. (A identificação da PJ sem placeholder é gate de corte em 50-14, não teste desta task.)
  </action>
  <acceptance_criteria>
    - Reaceite contratual e ciência de privacidade testados separadamente; sem bloqueio de histórico/downloads.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/legal/__tests__/legal-document-versions.test.ts</automated>
  </verify>
  <done>Testes legais.</done>
</task>

<task type="auto">
  <name>Task 12.6: Telemetria — eventos com dedup correto, fail-open</name>
  <files>src/lib/product-events/__tests__/service.test.ts</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/tasks.md` (12.6)
  </read_first>
  <action>
    Testar: eventos com dedup correto (`first_generation` por grant, `support_credit_request` múltiplo), fail-open.
  </action>
  <acceptance_criteria>
    - Dedup keys corretas; falha de escrita não bloqueia.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/product-events/__tests__/service.test.ts</automated>
  </verify>
  <done>Testes de telemetria.</done>
</task>

<task type="auto">
  <name>Task 12.7: Regressão do ledger — invariantes lineares</name>
  <files>src/lib/credit/__tests__/credit-ledger.test.ts</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/tasks.md` (12.7)
  </read_first>
  <action>
    Testar as invariantes `balance_before/after` lineares preservadas (soma de `demo`+`bonus`+`purchase` = `balance`; `expiration` compensa `demo`).
  </action>
  <acceptance_criteria>
    - Invariantes lineares preservadas em todas as transações.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/credit/__tests__/credit-ledger.test.ts</automated>
  </verify>
  <done>Testes de invariantes do ledger.</done>
</task>

<task type="auto">
  <name>Task 12.8: Storage/privacy — buckets privados, URL assinada, sem getPublicUrl</name>
  <files>src/lib/visual-signature/__tests__/persistence-storage.test.ts</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/tasks.md` (12.8)
  </read_first>
  <action>
    Testar: buckets `store-brand-assets`/`visual-signatures`/`store-logos` **privados**; consumidores migrados para URL assinada; nenhum `getPublicUrl` em produção.
  </action>
  <acceptance_criteria>
    - Acesso público negado; URL assinada funcional; nenhum getPublicUrl.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/visual-signature/__tests__/persistence-storage.test.ts</automated>
  </verify>
  <done>Testes de storage/privacy.</done>
</task>

<task type="auto">
  <name>Task 12.9: Saldo/RLS — authenticated sem service_role, cruzado negado, não materializa</name>
  <files>src/lib/credit/__tests__/credit-rls.test.ts</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/tasks.md` (12.9)
  </read_first>
  <action>
    Testar: leitura `authenticated` do saldo disponível sem RPC service_role; acesso cruzado negado; leitura não materializa.
  </action>
  <acceptance_criteria>
    - `authenticated` lê o próprio; cruzado negado; sem materialização na leitura.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/credit/__tests__/credit-rls.test.ts</automated>
  </verify>
  <done>Testes de RLS de saldo.</done>
</task>

<task type="auto">
  <name>Task 12.10: Suporte — protocolo/timestamps + confirmação durável</name>
  <files>src/app/api/support/credit-request/__tests__/route.test.ts</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/tasks.md` (12.10)
  </read_first>
  <action>
    Testar: protocolo/timestamps (`received_at`/`acknowledged_at`/`response_due_at`/`responded_at`/`closed_at`); confirmação durável com `protocol`+`receivedAt`; reconsideração manual.
  </action>
  <acceptance_criteria>
    - Timestamps registrados; resposta com protocol+receivedAt; reconsideração manual.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/app/api/support/credit-request/__tests__/route.test.ts</automated>
  </verify>
  <done>Testes de suporte.</done>
</task>

<task type="auto">
  <name>Task 12.11: Outbox — retry/lease, reclaim, failed terminal, cron do plano</name>
  <files>src/lib/notifications/__tests__/outbox.test.ts</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/tasks.md` (12.11)
  </read_first>
  <action>
    Testar: retry/lease (dois workers), reclaim por lease vencido, `failed` terminal, cron compatível com o plano (diário/horário).
  </action>
  <acceptance_criteria>
    - Retryable → pending+next_attempt_at; reclaim por lease; failed terminal; cadência conforme plano.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/notifications/__tests__/outbox.test.ts</automated>
  </verify>
  <done>Testes de outbox.</done>
</task>

<task type="auto">
  <name>Task 12.12: Refund pós-expiração — novo episódio de graça</name>
  <files>src/lib/credit/__tests__/credit-rpc.test.ts</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/tasks.md` (12.12)
  </read_first>
  <action>
    Testar: novo episódio de graça com `origin_demo_grant_tx_id`, **no máximo uma `expiration` por episódio (zero se esgotado)**, rastreabilidade ao grant.
  </action>
  <acceptance_criteria>
    - No máx. uma expiration por episódio; rastreabilidade ao grant.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/credit/__tests__/credit-rpc.test.ts</automated>
  </verify>
  <done>Testes de refund pós-expiração.</done>
</task>

<task type="auto">
  <name>Task 12.13: Beta/access — WhatsApp opcional, aviso versionado, maioridade, conta preservada</name>
  <files>src/components/landing/__tests__/access-request-form.test.tsx</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/tasks.md` (12.13)
  </read_first>
  <action>
    Testar: WhatsApp opcional/rotulado (sem marketing), aviso de privacidade versionado, maioridade/autoridade via aceite (sem nascimento), conta/histórico preservados.
  </action>
  <acceptance_criteria>
    - Cobertura dos cenários de beta/access.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/landing/__tests__/access-request-form.test.tsx</automated>
  </verify>
  <done>Testes de beta/access.</done>
</task>

<task type="auto">
  <name>Task 12.14: Cron demo-credits — CRON_SECRET, derivação/reparo</name>
  <files>src/app/api/cron/demo-credits/__tests__/route.test.ts</files>
  <read_first>
    - `src/app/api/cron/demo-credits/route.ts` (implementado em 50-06)
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/tasks.md` (12.1 — cron)
  </read_first>
  <action>
    Criar/executar `src/app/api/cron/demo-credits/__tests__/route.test.ts`: valida `CRON_SECRET` bearer; materializa expirações; **deriva/repara** `demo_granted`/`demo_expired`/`demo_exhausted`/`demo_expiring_24h` de evidência durável (dedup por `grant_tx_id`/`expiration_tx_id`/`deduction_tx_id`); idempotente.
  </action>
  <acceptance_criteria>
    - CRON_SECRET validado; derivação/reparo idempotente; `demo_expired` derivado da `expiration` mesmo quando `reserve_credit` materializou antes.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/app/api/cron/demo-credits/__tests__/route.test.ts</automated>
  </verify>
  <done>Testes do cron demo-credits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Integração (sem chamadas pagas) | Contratos externos e comportamento fim-a-fim. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-41 | Tampering | fixtures | accept | Sem dados reais; banco local isolado. |
</threat_model>

<verification>
- Suites de integração/rotas/UI/notificações/legal/telemetria verdes.
</verification>

<success_criteria>
- tasks.md 12.1–12.14 cobertos; contratos externos preservados.
</success_criteria>

<output>
Create `.planning/phases/50-demonstracao-gratuita-validade-creditos/50-12-SUMMARY.md` when done.
</output>

---
phase: 50-demonstracao-gratuita-validade-creditos
plan: 15
type: execute
wave: 2
depends_on: [50-01]
files_modified:
  - supabase/migrations/20260920000002_f50_storage_privacy.sql
  - src/lib/store-identity-service.ts
  - src/lib/visual-signature/persistence.ts
  - src/components/flow/store-identity-form.tsx
autonomous: true
requirements:
  - storage-privacy
must_haves:
  truths:
    - "buckets store-brand-assets/visual-signatures/store-logos tornados privados (dropar *_public_read, public=false)"
    - "consumidores de getPublicUrl migrados para URL assinada (read-time, nunca persistida); storage_path canônico"
    - "asset_url nullable/deprecated; nenhum getPublicUrl em produção; sem migração para R2"
  artifacts:
    - path: "src/lib/visual-signature/persistence.ts"
      provides: "URL assinada no read-time (visual-signatures)"
      contains: "storage hardening"
---

<objective>
Hardening de storage (D20): tornar privados `store-brand-assets`/`visual-signatures`/`store-logos` e migrar consumidores de `getPublicUrl` para URL assinada criada no read-time (nunca persistida), com `storage_path` como referência canônica e `asset_url` nullable/deprecated.

Purpose: Privacidade real dos arquivos do lojista. Sem migração para R2.
Output: policies de bucket atualizadas + consumidores migrados + testes de renovação de URL.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-demonstracao-gratuita-validade-creditos/50-CONTEXT.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/tasks.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/design.md
@openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/storage-privacy/spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 15.1: Inventariar buckets (public/policies/consumidores)</name>
  <files>supabase/migrations/20260920000002_f50_storage_privacy.sql</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/storage-privacy/spec.md`
  </read_first>
  <action>
    Inventariar os buckets `campaign-images` (privado), `lab-artifacts` (privado), `store-brand-assets` (público), `visual-signatures` (público), `store-logos` (público) — documentar public/private, policies e consumidores de URL.
  </action>
  <acceptance_criteria>
    - Inventário registrado com o estado de cada bucket.
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path "supabase/migrations/20260920000002_f50_storage_privacy.sql" -Pattern "store-brand-assets|visual-signatures|store-logos"</automated>
  </verify>
  <done>Inventário de buckets registrado.</done>
</task>

<task type="auto">
  <name>Task 15.2: Tornar privados os buckets de ativos do usuário</name>
  <files>supabase/migrations/20260920000002_f50_storage_privacy.sql</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/storage-privacy/spec.md`
  </read_first>
  <action>
    Tornar privados `store-brand-assets`/`visual-signatures`/`store-logos`: dropar policies `*_public_read`, `public=false`, preservar policies `service_role` de escrita + leitura autenticada do owner (ou URL assinada).
  </action>
  <acceptance_criteria>
    - Não há mais `SELECT` público sobre os 3 buckets; owner acessa por assinatura/autenticação.
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path "supabase/migrations/20260920000002_f50_storage_privacy.sql" -Pattern "public_read|public = false|DROP POLICY"</automated>
  </verify>
  <done>Buckets tornados privados.</done>
</task>

<task type="auto">
  <name>Task 15.3: Migrar consumidores de getPublicUrl para URL assinada; asset_url nullable/deprecated</name>
  <files>src/lib/store-identity-service.ts</files>
  <read_first>
    - `src/lib/store-identity-service.ts` (getPublicUrl, :63)
    - `src/lib/visual-signature/persistence.ts` (getPublicUrl, :54)
    - `src/components/flow/store-identity-form.tsx`
  </read_first>
  <action>
    Migrar `src/lib/store-identity-service.ts` (store-brand-assets) e `src/lib/visual-signature/persistence.ts` (visual-signatures) de `getPublicUrl` para `createSignedUrl` (TTL). Inventariar **TODOS** os consumidores de `asset_url`/`logo_url` e URLs públicas montadas manualmente (types/routes/UI/restore/approve/realign, incl. `store-identity-form.tsx`) e migrá-los para derivar URL assinada no read-time. Tornar `store_visual_signatures.asset_url` **nullable/deprecated**, `storage_path` canônico. URL assinada **nunca persistida**.
  </action>
  <acceptance_criteria>
    - Nenhum código de produção usa `getPublicUrl` para ativos do usuário; URL assinada no read-time com TTL adequado.
    - `asset_url` nullable/deprecated; `storage_path` canônico; nenhuma URL temporária gravada.
  </acceptance_criteria>
  <verify>
    <automated>rg "getPublicUrl" src/ -n; if ($LASTEXITCODE -ne 0) { "OK: nenhum getPublicUrl" }</automated>
  </verify>
  <done>Consumidores migrados para URL assinada.</done>
</task>

<task type="auto">
  <name>Task 15.4: Testar acesso negado + URL assinada + renovação</name>
  <files>src/lib/visual-signature/persistence.ts</files>
  <read_first>
    - `openspec/changes/fase-50-demonstracao-gratuita-e-validade-dos-creditos/specs/storage-privacy/spec.md`
  </read_first>
  <action>
    Testar: acesso público negado; URL assinada funcional; ausência de URLs públicas manuais; `asset_url` nullable/deprecated com `storage_path` canônico; **renovação de URL assinada após expiração**; fluxos restore/approve/realign; **sem** migração para R2.
  </action>
  <acceptance_criteria>
    - Acesso público negado; URL assinada funciona; renovação após expiração testada; sem R2.
  </acceptance_criteria>
  <verify>
    <automated>rg "createSignedUrl|storage_path" src/lib/visual-signature src/lib/store-identity-service -n</automated>
  </verify>
  <done>Hardening de storage testado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Storage (buckets de ativos do usuário) | Privacidade dos arquivos do lojista. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-29 | Information Disclosure | buckets públicos | mitigate | Tornar privados; URL assinada no read-time. |
| T-50-30 | Tampering | URL assinada | mitigate | `storage_path` canônico; URL nunca persistida; TTL cobre a operação. |
</threat_model>

<verification>
- Buckets privados; nenhum `getPublicUrl` em produção; URL assinada com renovação testada.
</verification>

<success_criteria>
- D20 implementado; ativos do lojista privados.
</success_criteria>

<output>
Create `.planning/phases/50-demonstracao-gratuita-validade-creditos/50-15-SUMMARY.md` when done.
</output>

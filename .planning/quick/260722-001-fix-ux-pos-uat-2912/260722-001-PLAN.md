# Quick Task 260722-001: Corrigir UX pós-UAT fase 29.1.2 - Gerar nova assinatura abre ApprovalModal direto com textarea

**Date:** 2026-07-22
**Status:** Ready

## Description
Quando o usuário clicar em "Gerar nova assinatura" a partir do HistoryModal, abrir o ApprovalModal diretamente com o campo de orientação do diretor de arte visível (textarea "O que você quer diferente?"), sem passar pela tela intermediária do grid + "Nenhuma agradou".

## Tasks

### Task 1: Adicionar prop `initialReviewFeedbackOpen` ao VisualSignatureApprovalModal
- **Files:**
  - `src/components/flow/visual-signature-approval-modal.tsx`
- **Action:** Add optional prop `initialReviewFeedbackOpen` (default `false`). When `true` and signatures exist (phase "review"), immediately show the review feedback textarea. Reset `showReviewFeedback` to `false` on cleanup.
- **Verify:** TypeScript compilation passes
- **Done:** Prop is used to control initial visibility of review feedback textarea

### Task 2: Atualizar store-visual-signature-section.tsx
- **Files:**
  - `src/components/flow/store-visual-signature-section.tsx`
- **Action:** Add `openApprovalWithFeedback` state. Set it to `true` in `handleGenerateNew`. Pass `initialReviewFeedbackOpen={openApprovalWithFeedback}` to ApprovalModal. Reset in onClose.
- **Verify:** TypeScript compilation passes
- **Done:** HistoryModal "Gerar nova assinatura" opens ApprovalModal with feedback visible

### Task 3: Atualizar store-identity-form.tsx
- **Files:**
  - `src/components/flow/store-identity-form.tsx`
- **Action:** Add `openApprovalWithFeedback` state. Set it to `true` in `handleGenerateNew` (after `setShowHistoryModal(false)`). Pass `initialReviewFeedbackOpen={openApprovalWithFeedback}` to ApprovalModal. Reset in onClose.
- **Verify:** TypeScript compilation passes
- **Done:** Same behavior as section component

### Task 4: Adicionar testes
- **Files:**
  - `src/components/flow/__tests__/visual-signature-history-modal.test.tsx`
  - `src/components/flow/__tests__/visual-signature-approval-modal.test.tsx`
- **Action:** Add test for HistoryModal: clicking "Gerar nova assinatura" calls `onGenerateNew`. Add test for ApprovalModal: `initialReviewFeedbackOpen=true` with existing signatures → textarea appears directly. `initialReviewFeedbackOpen=false` (default) → normal flow.
- **Verify:** `npx vitest run` passes
- **Done:** All tests passing

### Task 5: Typecheck, lint, build
- **Files:** (project-wide)
- **Action:** Run typecheck, lint, and build to verify no regressions
- **Verify:** All clean
- **Done:** TypeScript, lint, build pass

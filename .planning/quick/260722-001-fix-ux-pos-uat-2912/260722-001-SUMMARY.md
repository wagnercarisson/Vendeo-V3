# Quick Task 260722-001: Corrigir UX pós-UAT fase 29.1.2

**Status:** complete
**Date:** 2026-07-22

## Changes

### `src/components/flow/visual-signature-approval-modal.tsx`
- Added `initialReviewFeedbackOpen?: boolean` prop (default `false`)
- When `true` and existing signatures are loaded, immediately shows review feedback textarea via `setShowReviewFeedback(initialReviewFeedbackOpen)`
- Cleanup on modal close resets `showReviewFeedback` and `reviewFeedbackText`
- Added `initialReviewFeedbackOpen` to useEffect dependencies

### `src/components/flow/store-visual-signature-section.tsx`
- Added `openApprovalWithFeedback` state
- Normal "Criar Assinatura Visual" flow resets `openApprovalWithFeedback=false`
- `handleGenerateNew` sets `openApprovalWithFeedback=true` before opening ApprovalModal
- Passes `initialReviewFeedbackOpen={openApprovalWithFeedback}` to ApprovalModal
- onClose resets both `showApprovalModal` and `openApprovalWithFeedback`

### `src/components/flow/store-identity-form.tsx`
- Added `openApprovalWithFeedback` state
- Normal flows (`handleNoLogo`, `handleOpenSubstitutionApproval`) reset `openApprovalWithFeedback=false`
- `handleGenerateNew` sets `openApprovalWithFeedback=true` before opening ApprovalModal
- Passes `initialReviewFeedbackOpen={openApprovalWithFeedback}` to ApprovalModal
- onClose resets both `showApprovalModal` and `openApprovalWithFeedback`

## Tests
- 2 new tests in HistoryModal: onGenerateNew called on click, button hidden when not passed
- 3 new tests in ApprovalModal: initialReviewFeedbackOpen=true → textarea visible, false → hidden, undefined → hidden

## Verification
- **Tests:** 34/34 passing (2 test files)
- **TypeScript:** Clean
- **Lint:** Clean

---
slug: hotfix-admin-metrics-rpc-uuid-text
quick_id: 260730-o30
date: 2026-07-30
status: complete
---

# Summary: Hotfix admin_get_metrics UUID vs TEXT + dead code grant_monthly_credits

## Changes

### Migration: `supabase/migrations/20260731000003_fix_admin_get_metrics_uuid_text.sql`

**admin_get_metrics:**
- Adicionado `d.campaign_id` no CTE `window_deductions` (previne `column wd2.campaign_id does not exist`)
- 7 casts `::text` nas comparações UUID vs TEXT entre `credit_transactions.id` e `credit_transactions.reference`

**grant_monthly_credits:**
- Removida declaração `v_new_balance INTEGER`
- Removido `SELECT COALESCE(bonus_balance, 0) INTO v_new_balance ...` pós-loop (dead code que quebrava sem lojas elegíveis)

## Validação

- `npm run typecheck` ✅
- `npm run lint` ✅
- `npm run test` ✅
- `npm run build` ✅

## Instruções para validar no Supabase

```sql
-- admin_get_metrics (só leitura)
select public.admin_get_metrics('production', 24, 'all');
select public.admin_get_metrics('all', 24, 'all');
select public.admin_get_metrics('test', 24, 'all');

-- grant_monthly_credits (sempre com rollback em produção)
begin;
select public.grant_monthly_credits(20, 60, 0);
rollback;
```

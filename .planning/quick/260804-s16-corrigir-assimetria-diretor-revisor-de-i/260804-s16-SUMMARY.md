---
phase: quick-s16
plan: 01
subsystem: api
tags: [image-generation, image-review, prompt-loader, openai, quality-gate]

# Dependency graph
requires:
  - phase: 31-3-quality-gate-por-intencao-comercial
    provides: ImageReviewInput intent-aware, campaign-image-reviewer.md com variáveis contextuais, validatePrompts com builder compartilhado
  - phase: 25-integracao-transacional-pipeline
    provides: mandatoryArtworkText, campaignDetails, additionalDetails em GenerateImageRequest (schema.ts)
provides:
  - Contrato espelhado Diretor/Revisor de Imagem: os 3 campos de contexto chegam ao revisor nos dois fluxos (generateImage e validatePrompts)
  - Revisor valida texto obrigatório na arte (ausente/divergente/ilegível/cortado → issue crítica illegible_text)
  - Contexto autorizado anti-falso-positivo de invented_information
affects: [image-generation pipeline, f31-3-review-quality-gate, generate-image route]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Section builders por domínio no ImageReviewService (buildValidationContextSection → buildMandatoryArtworkTextSection, buildAuthorizedContextSection)"
    - "sanitizePromptText: quebra sequências {{ }} em valores do lojista antes da interpolação"

key-files:
  created: []
  modified:
    - src/lib/image-generation/services/image-review-service.ts
    - prompts/campaign-image-reviewer.md
    - src/lib/image-generation/services/image-generation-service.ts
    - src/lib/image-generation/services/__tests__/image-review-service.test.ts
    - src/lib/image-generation/services/__tests__/image-generation-service.test.ts

key-decisions:
  - "Os 3 campos entram no revisor AGRUPADOS em seções (mandatoryArtworkTextSection/authorizedContextSection), não como chaves cruas — 1:1 com os placeholders do prompt"
  - "Seções vazias quando campo ausente → prompt do revisor não exige texto obrigatório (interpolação vazia remove a seção)"
  - "sanitizePromptText substitui {{ por { e }} por } — nenhum valor de variável pode conter {{ (protege o teste existente de placeholders não resolvidos)"
  - "mandatoryArtworkText permanece fora do Copy Director (texto da arte, não da legenda)"

patterns-established:
  - "Revisor usa linguagem de REVISÃO (verificação de fidelidade comercial), nunca direção criativa — prompt do diretor não é copiado"
  - "Contexto autorizado da campanha reduz falso positivo de invented_information para detalhes fornecidos pelo lojista"

requirements-completed: [S16-01, S16-02, S16-03, S16-04, S16-05]

# Metrics
duration: 12min
completed: 2026-08-04
---

# Quick Task 260804-s16: Corrigir Assimetria Diretor/Revisor de Imagem — Summary

**Contrato de revisão espelhado ao diretor: `mandatoryArtworkText`, `campaignDetails` e `additionalDetails` agora chegam ao revisor de imagem nos dois fluxos (generateImage e validatePrompts), com seções de contexto geradas por builders (texto obrigatório com linguagem crítica + contexto autorizado anti-falso-positivo), sanitização `{{ }}` e testes focados cobrindo a regressão real reportada.**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-08-04T02:40:00Z (approx.)
- **Completed:** 2026-08-04T02:52:00Z (approx.)
- **Tasks:** 3/3
- **Files modified:** 5

## Accomplishments

- `ImageReviewInput` estendido com `mandatoryArtworkText`, `campaignDetails`, `additionalDetails` (espelho do contrato do diretor, que já os recebia)
- `buildReviewPromptVariables` gera `mandatoryArtworkTextSection` e `authorizedContextSection` (vazias quando campos ausentes; preenchidas com linguagem de revisão quando presentes)
- Seção de texto obrigatório: exigência VISÍVEL/EXATO/LEGÍVEL; ausente/divergente/ilegível/cortado → issue CRÍTICA `illegible_text` (já bloqueante em `applyValidationContextToReviewResult`)
- Seção de contexto autorizado: detalhes do lojista marcados AUTORIZADOS → NÃO reportar como `invented_information`
- `sanitizePromptText` quebra qualquer sequência `{{`/`}}` em valores do lojista — nenhuma variável do prompt final contém placeholder não resolvido
- `ImageGenerationService` propaga os 3 campos em `reviewInput` (generateImage) e `reviewerInput` (validatePrompts), sem `?? ""` (o builder trata vazio)
- Prompt do revisor ganhou os 2 placeholders (linha 29-32) e a regra de `invented_information` atualizada
- 9 novos testes (5 revisor + 4 serviço), incluindo o fluxo real `generateImage → review()` que reproduz a assimetria reportada pelo beta user

## Task Commits

Cada task foi commitada atomicamente:

1. **Task 1: Espelhar contrato de contexto no ImageReviewInput e gerar seções de revisão** — `291b951` (feat)
2. **Task 2: Propagar os 3 campos no reviewInput (generateImage) e reviewerInput (validatePrompts)** — `fed5767` (feat)
3. **Task 3: Testes focados da propagação revisor** — `47a1a4a` (test)

**Plan metadata:** docs commit feito pelo orchestrator (não commitado por este executor).

## Files Created/Modified

- `src/lib/image-generation/services/image-review-service.ts` — 3 campos no `ImageReviewInput`, 2 novas entradas em `buildReviewPromptVariables`, métodos `buildMandatoryArtworkTextSection`, `buildAuthorizedContextSection` e helper `sanitizePromptText`
- `prompts/campaign-image-reviewer.md` — placeholders `{{mandatoryArtworkTextSection}}` e `{{authorizedContextSection}}` após `{{validationContextSection}}`; critério #5 com regra de contexto autorizado
- `src/lib/image-generation/services/image-generation-service.ts` — `reviewInput` (generateImage) e `reviewerInput` (validatePrompts) com os 3 campos
- `src/lib/image-generation/services/__tests__/image-review-service.test.ts` — 5 novos testes (seções, vazio, sanitização)
- `src/lib/image-generation/services/__tests__/image-generation-service.test.ts` — 4 novos testes (propagação validatePrompts, valid=true, regressão ausência, fluxo real generateImage)

## Decisions Made

- Campos agregados em seções no revisor (não chaves cruas) — alinhado com `buildValidationContextSection` existente e 1:1 com os placeholders do prompt
- Sanitização `{{` → `{` (não remoção) — preserva legibilidade do texto do lojista no prompt
- Texto obrigatório NÃO foi adicionado ao Copy Director — escopo da arte, não da legenda (teste existente de shape no copy-director-service confirma)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- `npm run typecheck`/`npm run lint` via PowerShell direto falharam (`npm` resolvido como shim inválido em pipeline) — resolvido invocando via `cmd /c "npm run ... 2>&1"`. Sem impacto no código.

## Self-Check

- [x] `image-review-service.test.ts`: 14/14 pass (9 existentes + 5 novos)
- [x] `image-generation-service.test.ts`: 11/11 pass (7 existentes + 4 novos)
- [x] Regressão `f31-3-review-quality-gate` + `route.test` + `copy-director-service`: 51/51 pass
- [x] `npm run typecheck` — limpo
- [x] `npm run lint` — limpo
- [x] Grep gate: `{{mandatoryArtworkTextSection}}` presente no prompt (linha 29); `mandatoryArtworkText` fora do `src/lib/copy` (apenas teste que asserta ausência)
- [x] Commits `291b951`, `fed5767`, `47a1a4a` existem em `git log`

## Next Phase Readiness

- Qualquer plano futuro que toque o pipeline de geração já parte com o revisor ciente do texto obrigatório e do contexto comercial autorizado
- Sem blockers; sem novos endpoints, schemas ou superfícies de rede (threat model sem flags novas)

---
*Phase: quick-s16*
*Completed: 2026-08-04*

## Self-Check: PASSED

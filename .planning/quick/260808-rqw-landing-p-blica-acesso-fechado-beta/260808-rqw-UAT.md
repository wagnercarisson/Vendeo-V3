---
status: complete
phase: quick-260808-rqw
source: 260808-rqw-SUMMARY.md, 260808-rqw-PLAN.md
started: 2026-08-10T16:50:00Z
updated: 2026-08-10T17:40:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: Subir o dev server do zero; `/` renderiza a landing; API de acesso responde com o banco (migration `20260810010000_create_access_requests.sql` aplicada).
result: pass

### 2. Landing pública `/`
expected: Visitante acessa `/` e vê a landing (não é redirecionado para `/dashboard`). Hero "Campanhas profissionais para lojas físicas", CTA primário "Solicitar acesso free" (âncora até o formulário), CTA secundário "Entrar" → `/login`, selo de beta fechado, footer com `/termos`, `/privacidade`, `/uso-aceitavel`.
result: pass

### 3. Solicitação de acesso grava e confirma
expected: Preencher email + loja no form da landing, enviar → mensagem de sucesso ("Recebemos sua solicitação. A liberação é por convite — vamos analisar seu pedido."). O registro aparece em `access_requests` (status `pending`, source `landing`) — conferir em `/admin/access-requests` (aba Pendentes) ou via SQL.
result: pass

### 4. Duplicidade não cria segundo registro
expected: Enviar o MESMO email novamente (com case diferente, ex. `LOJA@example.com`) → mesma mensagem de sucesso (anti-enumeração, não revela duplicata). Não há 2º registro no banco.
result: pass

### 5. Validação anti-enumeração
expected: Enviar email inválido (ex. `nao-e-email`) → mensagem de erro genérica; nada é gravado em `access_requests`.
result: pass

### 6. `/signup` fechado (beta fechado)
expected: Acessar `/signup` → tela "Beta fechado" com instrução de solicitar acesso; NÃO há formulário de cadastro (nem input de senha, nem "Criar conta"). CTAs: "Solicitar acesso free" → `/` e "Já tenho acesso — Entrar" → `/login`.
result: pass

### 7. Login pós-login → `/dashboard`
expected: Usuário existente loga em `/login` sem `?redirect=` → cai em `/dashboard` (não na landing). No form de login, o link final mostra "Ainda não tem acesso? **Solicitar acesso free**" → `/` (não "Criar conta").
result: pass
note: "Revalidado no build corrigido (router.replace trata redirect==='/' como default /dashboard)."

### 8. Redirecionamento de autenticado
expected: Usuário autenticado que acessa `/login` ou `/signup` é redirecionado para `/dashboard` (middleware inalterado).
result: pass

### 9. Admin triagem com auditoria
expected: Admin acessa `/admin/access-requests` (nav "Solicitações de acesso") → lista Pendentes; aprovar → status `approved` + `reviewed_by/reviewed_at` + entrada em `admin_audit_log` (`access_request_approve`, target_type `access_request`, metadata com email); recusar → `rejected` + `access_request_reject`. Tabs Pendentes/Aprovados/Recusados funcionam.
result: pass

### 10. Guardas: re-revisão e não-admin
expected: Solicitação já revisada não permite re-revisão (botões desabilitados / erro "já revisada"). Usuário não-admin não acessa `/admin/access-requests` (acesso negado).
result: pass

### 11. Páginas legais com retorno à landing
expected: `/termos`, `/privacidade` e `/uso-aceitavel` exibem o documento legal e um link discreto no topo "Voltar para solicitar acesso" → `/`.
result: pass

### 12. Copy sem promessa de email
expected: O sucesso do form e o texto da seção da landing NÃO prometem aviso por email (ex.: sem "avisaremos por email" / "avisaremos quando seu acesso estiver pronto").
result: pass
note: "Bonus cosmetico aprovado pelo usuario: botao 'Entrar' do hero ganhou padding px-6 py-3 (estava sem padding horizontal, texto colado na borda)."

### 13. Sucesso da solicitação: CTA e card
expected: Após enviar a solicitação, o botão "Solicitar acesso free" (`#acesso`) NÃO aparece mais; o card muda para "Solicitação enviada", mantendo a sequência "Recebemos sua solicitação. A liberação é por convite — vamos analisar seu pedido." + "Em breve entraremos em contato."
result: pass
note: "Revalidado no build corrigido (AccessRequestSection client: CTA removido e card 'Solicitação enviada' no estado de sucesso)."

## Summary

total: 13
passed: 13
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Usuário existente que loga sem redirect cai em /dashboard (não na landing)"
  status: resolved
  reason: "User reported: em /login digitei user e senha e ao invés de direcionar para dashboard, me levou para / (landing); clicando em Entrar na landing, foi para /dashboard"
  severity: major
  test: 7
  root_cause: "sanitizeRedirectPath(\"\") retorna \"/\" (truthy); login/page.tsx passa redirect=\"/\" ao LoginForm e router.replace(redirect || \"/dashboard\") usa \"/\" (o 301 antigo / -> /dashboard camuflava esse default; removido na quick, quebrou)"
  fix: "login-form.tsx: router.replace(redirect && redirect !== \"/\" ? redirect : \"/dashboard\"); testes cobrem redirect=\"/\" e \"\". Revalidado manualmente."
  artifacts:
    - path: "src/app/(auth)/login/login-form.tsx"
      issue: "redirect=\"/\" (sanitizer default) era tratado como destino válido"
  missing:
    - "Tratar redirect \"/\" como default /dashboard no pós-login"
- truth: "Após enviar solicitação, CTA 'Solicitar acesso free' some e card vira 'Solicitação enviada' + 'em breve entraremos em contato'"
  status: resolved
  reason: "User reported: botão 'Solicitar acesso free' aponta para /#acesso no estado de sucesso; prefere remover botão e alterar texto do card"
  severity: minor
  test: 13
  root_cause: "Estado de sucesso vivia apenas no client AccessRequestForm; card e CTA do hero eram server-rendered em page.tsx e não reagiam ao submit"
  fix: "Novo client AccessRequestSection gerencia estado submitted (remove CTA no hero, card vira 'Solicitação enviada'); AccessRequestForm reporta sucesso via onSuccess. Teste de regressão em landing-page.test.tsx."
  artifacts:
    - path: "src/app/page.tsx"
      issue: "CTA do hero e card renderizados no server, sem estado de submitted"
  missing:
    - "Elevar estado submitted para componente client que engloba CTA + card"

---
phase: 260814-na1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/validators/phone.ts
  - src/lib/validators/__tests__/phone.test.ts
  - src/components/landing/access-request-form.tsx
  - src/components/landing/__tests__/access-request-form.test.tsx
  - src/components/landing/novidades-link.tsx
  - src/components/landing/access-request-section.tsx
  - src/app/page.tsx
  - src/__tests__/app/landing-page.test.tsx
autonomous: true
requirements:
  - Q-260814-NA1
user_setup: []

must_haves:
  truths:
    - "Visitante vê link chamativo 'Novidades' (maior contraste, min-h 44px) logo abaixo do card 'Solicite seu acesso free'"
    - "Visitante vê link discreto 'Novidades' no rodapé junto a Termos/Privacidade"
    - "Clicar em qualquer link abre MODAL sobre a landing (sem rota nova) com as ~5 entradas mais recentes do changelog"
    - "Modal fecha via botão ×, tecla Esc ou clique no backdrop; foco retorna ao botão de abertura; rolagem da landing não é deslocada"
    - "Campo WhatsApp do formulário de acesso aplica máscara progressiva (11) 99999-9999 enquanto digita (maxLength 15)"
    - "Valor mascarado do WhatsApp é enviado como está a POST /api/access-requests — API inalterada (max 20, máscara 15 ≤ 20)"
  artifacts:
    - path: "src/lib/validators/phone.ts"
      provides: "Função pura maskWhatsApp(value) — máscara progressiva, strips não-dígitos, máx 11 dígitos"
      exports: ["maskWhatsApp"]
    - path: "src/components/landing/novidades-link.tsx"
      provides: "Client component — trigger (variants prominent/footer) + modal acessível reutilizando ChangelogList"
      exports: ["NovidadesLink"]
    - path: "src/app/page.tsx"
      provides: "Server component async — getAllEntries().slice(0, 5) resolvido no servidor e repassado por props"
      contains: "getAllEntries"
    - path: "src/components/landing/access-request-form.tsx"
      provides: "Campo WhatsApp controlado com maskWhatsApp no onChange; name=\"whatsapp\" preservado"
      contains: "maskWhatsApp"
    - path: "src/lib/validators/__tests__/phone.test.ts"
      provides: "Testes unitários da máscara"
    - path: "src/__tests__/app/landing-page.test.tsx"
      provides: "Testes da landing: render async, links Novidades, modal abre/fecha, máscara no form"
  key_links:
    - from: "src/app/page.tsx"
      to: "getAllEntries (server-only, fs content/changelog)"
      via: "await getAllEntries() → slice(0, 5) → props"
      pattern: "getAllEntries"
    - from: "src/components/landing/novidades-link.tsx"
      to: "src/components/changelog/changelog-list.tsx"
      via: "import ChangelogList — renderiza entradas + empty state"
      pattern: "ChangelogList"
    - from: "src/components/landing/access-request-form.tsx"
      to: "/api/access-requests"
      via: "FormData.get(\"whatsapp\") envia valor mascarado — schema zod max 20 aceita 15"
      pattern: "whatsapp"
---

<objective>
Adicionar o changelog de Novidades na landing pública: link chamativo abaixo do
card "Solicite seu acesso free", link discreto no rodapé, exibição das ~5
entradas mais recentes em MODAL sobre a landing (reutilizando dados e
componentes do changelog existente), e máscara de telefone progressiva
`(11) 99999-9999` no campo WhatsApp do formulário de acesso.

Purpose: Capitalizar a intenção de acesso do visitante e dar acesso persistente
às novidades sem criar rota pública nova, mantendo o visitante no ambiente da
landing. A máscara WhatsApp segue o padrão progressivo já usado no CNPJ do
cadastro de loja e preserva o comportamento da API (sem mudanças no schema).

Critério editorial permanente: a partir desta entrega, qualquer entry em
`content/changelog/` pode aparecer publicamente na landing para visitante não
autenticado — novas entradas devem ser escritas como conteúdo publicável
externamente.

Output: Máscara pura testável, modal acessível de novidades, data flow
server→client na landing, e testes atualizados/novos.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260814-na1-adicionar-link-de-novidades-changelog-na/260814-na1-CONTEXT.md

# Fontes canônicas (contratos já lidos — não re-explorar):

- `src/app/page.tsx` — server component atual (síncrono). Vira async para
  resolver o changelog no servidor. Header/footer ficam no próprio componente.
- `src/components/landing/access-request-section.tsx` — client component com a
  seção `#acesso` (card "Solicite seu acesso free"). Receberá prop `entries` e
  renderizará o trigger prominent logo abaixo do card.
- `src/components/landing/access-request-form.tsx` — client component. Campo
  WhatsApp hoje é uncontrolled (`<Input name="whatsapp" maxLength={20} />`,
  placeholder "(00) 00000-0000"). Vira controlado com máscara progressiva.
- `src/lib/changelog/get-changelog.ts` — `getAllEntries()` server-only, já
  ordena por data DESC, retorna `ChangelogEntry[]`, ENOENT → `[]`.
- `src/lib/changelog/types.ts` — `ChangelogEntry { frontmatter, body, slug }`.
- `src/components/changelog/changelog-list.tsx` — `ChangelogList({ entries })`,
  renderiza ChangelogCard + empty state "Nenhuma novidade por enquanto".
  Server-safe (sem "use client"). Reutilizar diretamente no modal.
- `src/components/credit/credit-cta.tsx` — padrão de modal acessível: Esc
  fecha, foco no primeiro interativo ao abrir, foco de volta no trigger ao
  fechar, backdrop click fecha, `role="dialog"` + `aria-modal="true"`.
- `src/components/flow/store-identity-form.tsx` (linhas 1505–1512) — padrão de
  máscara progressiva inline (CNPJ): strip `\D`, slice, remonta com separadores.
- `src/app/api/access-requests/route.ts` — `whatsapp: z.string().trim().max(20)
  .optional()`; manter API intacta (máscara = 15 chars ≤ 20).
- `vitest.config.ts` — alias global de `server-only` (mock/empty) → testes podem
  chamar `getAllEntries()` lendo `content/changelog` real.
- Padrão de teste de server async: `render(await Home())` /
  `renderToString(await Page())` (ver `src/__tests__/app/dashboard/`).

<interfaces>
From src/lib/changelog/types.ts (reutilizar — sem re-exploração):

```typescript
export interface ChangelogEntry {
  frontmatter: ChangelogFrontmatter; // { id, title, date, milestone?, category, importance, announcement }
  body: string;   // markdown sem frontmatter
  slug: string;
}
```

From src/lib/changelog/get-changelog.ts:

```typescript
export async function getAllEntries(dir?: string): Promise<ChangelogEntry[]>;
// ordena por frontmatter.date DESC; ENOENT → []
```

From src/components/changelog/changelog-list.tsx:

```typescript
export function ChangelogList({ entries }: { entries: ChangelogEntry[] }): JSX.Element;
// empty array → "Nenhuma novidade por enquanto." (não lança)
```

From src/components/ui/input.tsx — `Input` aceita `value`/`onChange`/`maxLength`
(InputHTMLAttributes, controlável). Usar para o campo WhatsApp mascarado.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Máscara progressiva de WhatsApp + testes</name>
  <files>
    src/lib/validators/phone.ts
    src/lib/validators/__tests__/phone.test.ts
    src/components/landing/access-request-form.tsx
    src/components/landing/__tests__/access-request-form.test.tsx
  </files>
  <behavior>
    - `maskWhatsApp("")` → `""`
    - `maskWhatsApp("11999999999")` → `"(11) 99999-9999"`
    - `maskWhatsApp("119999999999")` (12 dígitos) → `"(11) 99999-9999"` (trunca em 11)
    - `maskWhatsApp("119")` → `"(11) 9"`; `maskWhatsApp("119999")` → `"(11) 99999"`
    - `maskWhatsApp("(11) 99999-9999")` → `"(11) 99999-9999"` (idempotente — paste de valor mascarado)
    - `maskWhatsApp("abc 11 99999-9999")` → `"(11) 99999-9999"` (strips não-dígitos)
    - Form integration: digitar no campo WhatsApp aplica máscara no valor do input; submit envia valor mascarado no payload; input tem maxLength 15.
    - Paste no componente: colar `"11999999999abc"` ou `"(11) 99999-9999 "` no campo WhatsApp → valor exibido `(11) 99999-9999` (strips não-dígitos e espaços); colar string longa (ex: 20+ dígitos) → trunca em 11 dígitos.
  </behavior>
  <action>
    1. Criar `src/lib/validators/phone.ts` com função pura
       `export function maskWhatsApp(value: string): string` (sem React, sem
       server-only — espelha `src/lib/validators/color.ts`): strip `value.replace(/\D/g, "")`,
       `.slice(0, 11)`; retorna `""` se vazio; se > 2 dígitos envolve DDD em
       `(NN) `; se > 7 dígitos separa o 5º dígito com `-` — resultado final
       `(11) 99999-9999` (15 chars).
    2. Criar `src/lib/validators/__tests__/phone.test.ts` cobrindo os casos do
       bloco behavior (vitest, sem jsdom — função pura).
    3. Alterar `src/components/landing/access-request-form.tsx`:
       - Adicionar `import { maskWhatsApp } from "@/lib/validators/phone"` e
         `import { useState } from "react"` (já existe).
       - Trocar o `<Input name="whatsapp" ...>` por input CONTROLADO: estado
         `const [whatsapp, setWhatsapp] = useState("")`;
         `onChange={(e) => setWhatsapp(maskWhatsApp(e.target.value))}`;
         `value={whatsapp}`; `maxLength={15}` (era 20); `placeholder="(00) 00000-0000"`
         e `autoComplete="tel"` preservados; MANTER `name="whatsapp"` para o
         `formData.get("whatsapp")` do handleSubmit continuar enviando o valor
         mascarado (payload `{ email, store_name, segment, whatsapp }` inalterado).
       - NÃO alterar a rota `POST /api/access-requests` nem o schema zod.
     4. Criar `src/components/landing/__tests__/access-request-form.test.tsx`
        (jsdom, mock global de fetch via `vi.stubGlobal` no padrão do
        `landing-page.test.tsx`): digitar "11999999999" no campo WhatsApp →
        valor exibido `(11) 99999-9999`; submit → payload.whatsapp mascarado;
        `maxLength` = 15; **paste de valor longo com caracteres não numéricos
        e espaços** (ex: `fireEvent.change` com `"11999999999abc (00) "`) →
        valor mascarado `(11) 99999-9999` (garante o comportamento do paste no
        componente, não só na função pura).
  </action>
  <verify>
    <automated>npx vitest run src/lib/validators/__tests__/phone.test.ts src/components/landing/__tests__/access-request-form.test.tsx</automated>
  </verify>
  <done>
    - `maskWhatsApp` pura coberta por testes unitários (todos os casos do behavior passam).
    - Campo WhatsApp da landing é controlado, aplica máscara progressiva durante a digitação, `maxLength={15}`.
    - Submit envia WhatsApp mascarado; rota `/api/access-requests` e schema zod intocados.
    - typecheck/lint limpos nos arquivos tocados.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Links Novidades + modal sobre a landing</name>
  <files>
    src/components/landing/novidades-link.tsx
    src/components/landing/access-request-section.tsx
    src/app/page.tsx
    src/__tests__/app/landing-page.test.tsx
  </files>
  <action>
    1. Criar `src/components/landing/novidades-link.tsx` — client component:
       - Props: `{ entries: ChangelogEntry[]; variant: "prominent" | "footer" }`.
       - Trigger é `<button type="button">` (abre modal — NÃO é navegação):
         - `variant="prominent"`: destaque de maior contraste — `min-h-[44px]`,
           `font-heading font-semibold`, `text-accent-green` com
           `hover:underline`, `focus:ring-2 focus:ring-accent-green
           focus:outline-none`, label "Novidades" (estilo coeso com os CTAs da
           seção hero/card).
         - `variant="footer"`: discreto — mesmas classes dos links do rodapé
           (`text-sm text-text-muted transition-colors hover:text-text-primary`),
           label "Novidades", alinhado ao grupo Termos/Privacidade.
       - Estado `isOpen` + `triggerRef` + `firstInteractiveRef`; ao abrir, foca
         o primeiro interativo (`setTimeout(..., 0)`); ao fechar, devolve foco ao
         trigger — padrão exato de `credit-cta.tsx`; Esc fecha
         (`document.addEventListener("keydown")` apenas quando aberto);
         backdrop click (`e.target === e.currentTarget`) fecha.
        - Modal: overlay `fixed inset-0 z-50 flex items-center justify-center
          bg-black/50 p-4 backdrop-blur-sm`; painel `role="dialog"
          aria-modal="true" aria-labelledby="landing-novidades-title"`,
          `w-full max-w-2xl rounded-xl border border-border bg-bg-elevated p-6
          shadow-xl` — `rounded-xl` segue o padrão dominante da UI (`Card`,
          `credit-cta`, `changelog-announcement`, modais legais/flow).
       - Conteúdo: header com `<h2 id="landing-novidades-title" className="font-heading
         text-lg font-semibold">Novidades</h2>` + botão fechar `×` com
         `aria-label="Fechar novidades"`, `min-h-[44px] min-w-[44px]`,
         `hover:bg-bg-elevated focus:ring-2` (padrão changelog-announcement);
         área de lista `max-h-[65vh] overflow-y-auto pr-1`; corpo =
         `<ChangelogList entries={entries} />` (import server-safe — já cobre
         empty state). NÃO criar rota nova e NÃO renderizar overlay quando
         fechado.
    2. Alterar `src/components/landing/access-request-section.tsx`:
       - Prop nova `{ entries }: { entries: ChangelogEntry[] }` (tipo importado
         de `@/lib/changelog/types`).
       - Renderizar `<NovidadesLink variant="prominent" entries={entries} />`
         logo abaixo do card (depois do `</div>` do card, dentro da section
         `#acesso`), centralizado, com `mt-4`.
    3. Alterar `src/app/page.tsx`:
       - Virar `export default async function Home()`.
       - `import { getAllEntries } from "@/lib/changelog/get-changelog"`;
         `const entries = await getAllEntries(); const recentEntries =
         entries.slice(0, 5);` (limite ~5 — decisão D3).
       - Passar `entries={recentEntries}` a `<AccessRequestSection />`.
       - No `<nav>` do footer, adicionar
         `<NovidadesLink variant="footer" entries={recentEntries} />` junto aos
         links Termos/Privacidade/Uso Aceitável/Entrar.
     4. Atualizar `src/__tests__/app/landing-page.test.tsx`:
        - Mock de `@/lib/changelog/get-changelog` no topo
          (`vi.mock("@/lib/changelog/get-changelog", () => ({ getAllEntries:
          vi.fn().mockResolvedValue(FIXTURE_ENTRIES) }))` com 3 entradas
          determinísticas — reutilizar a factory makeEntry do
          `changelog-announcement.test.tsx`).
        - Migrar TODOS os testes existentes de `render(<Home />)` para
          `render(await Home())` (Home vira async — padrão do dashboard).
        - **Varrer todo o repositório por outros importadores de `@/app/page`**
          (grep `@/app/page` em `*.test.tsx`/`*.tsx`) ANTES de migrar; hoje só
          `landing-page.test.tsx` importa `Home`, mas o executor deve confirmar
          que não há outra referência que quebre com Home async — se encontrar,
          migrar junto no mesmo commit.
        - Novos testes: (a) botão "Novidades" prominent presente na seção do
          card; (b) botão "Novidades" footer presente no rodapé; (c) clique no
          prominent abre dialog com os títulos das entradas do fixture; (d) ×
          fecha o dialog e devolve foco ao trigger; (e) Esc fecha o dialog; (f)
          fixture com array vazio abre modal com empty state (não lança).
        - MANTER o teste existente de submit do form (mock fetch `{ ok: true }`).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/app/landing-page.test.tsx src/components/changelog/__tests__/changelog-list.test.tsx</automated>
  </verify>
  <done>
    - Landing resolve o changelog no server, corta nas 5 mais recentes e passa por props (sem fetch no client).
    - Links Novidades visíveis: chamativo abaixo do card + discreto no rodapé.
    - Clique abre modal acessível (role=dialog, aria-modal, Esc, backdrop, foco ida/volta) com as entradas via ChangelogList.
    - Sem rota pública nova — `/novidades` segue restrito a `(app)`.
    - `landing-page.test.tsx` migrado para `render(await Home())` + testes novos verdes; regressão changelog verde.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (landing) → API | POST /api/access-requests — público, já protegido por requireSameOrigin + zod (intocado neste plano) |
| server (fs content/changelog) → browser | **MUDA DE SUPERFÍCIE:** markdown do changelog passa a ser renderizado na landing pública para visitante não autenticado (antes: só /novidades autenticado). Conteúdo é first-party (repo, autorado pelo time), mesmo renderer em produção em /novidades, sem entrada de usuário — mas é uma nova exposição pública a ser tratada como decisão editorial permanente. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-NA1-01 | Injection | `renderMarkdown` (via ChangelogCard no modal da landing) | accept + editoral | Conteúdo é first-party (arquivos do repo), mesmo renderer já em produção em `/novidades`, sem entrada de usuário. **Reconhecer como mudança real de superfície** (landing pública). Mitigação editorial permanente: toda nova entry em `content/changelog/` deve ser publicável para visitante não autenticado (sem refs internas, jargão de fase ou info não pública) — registrar no CONTEXT.md como decisão D3 e no threat model como aceite editorial. |
| T-NA1-02 | Tampering | Campo WhatsApp (máscara client) | mitigate | `maskWhatsApp` é pura e idempotente (strips `\D`, trunca 11 dígitos); valor ≤ 15 chars revalida no schema zod `max(20)` da API (defesa em profundidade — máscara é UX, validação continua na API). |
| T-NA1-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhum pacote novo — zero instalações neste plano. |

Sempre que `maskWhatsApp` for alterada, o teste `phone.test.ts` é o gate de
regressão do formato `(11) 99999-9999` (arquivo legado já em produção).
</threat_model>

<verification>
1. `npx vitest run src/lib/validators/__tests__/phone.test.ts src/components/landing/__tests__/access-request-form.test.tsx src/__tests__/app/landing-page.test.tsx src/components/changelog/__tests__/changelog-list.test.tsx` — todos verdes.
2. `npm run typecheck` e `npm run lint` — exit 0 (arquivos novos/migrados sem erros).
3. `npm test` (regressão completa — ~1950 testes) — sem quebras fora dos arquivos tocados.
4. Grep `@/app/page` em `*.tsx`/`*.test.tsx` — apenas `landing-page.test.tsx` (migrado); nenhum outro importador de `Home` quebrado com a assinatura async.
5. `npm run build` — exit 0.
6. Verificação manual (usuário): abrir a landing em dev, conferir contraste do
   link chamativo, modal com ~5 novidades (scroll interno), fechamento via ×/Esc/backdrop, foco retornando, e máscara `(11) 99999-9999` ao digitar no WhatsApp.
</verification>

<success_criteria>
- Landing pública exibe "Novidades" em dois pontos (destaque sob o card + rodapé), sem deslocar navegação.
- Modal sobre a landing mostra as ~5 entradas mais recentes do changelog, reutilizando `getAllEntries`/`ChangelogList`, com retorno claro (×/Esc/backdrop).
- Campo WhatsApp mascarado progressivamente, salvando o valor mascarado via API existente (zero mudanças em rota/schema).
- **Critério editorial vigente:** toda entry de `content/changelog/` passível de exibição na landing é publicável para visitante não autenticado — registrado no CONTEXT.md e no threat model como aceite editorial.
- **Nenhum outro importador de `@/app/page` quebrado** — varredura de `@/app/page` no repo não revela referências além de `landing-page.test.tsx` (todas migradas para `render(await Home())`).
- Suíte completa verde (vitest + typecheck + lint + build).
</success_criteria>

<output>
Create `.planning/quick/260814-na1-adicionar-link-de-novidades-changelog-na/260814-na1-SUMMARY.md` when done
</output>

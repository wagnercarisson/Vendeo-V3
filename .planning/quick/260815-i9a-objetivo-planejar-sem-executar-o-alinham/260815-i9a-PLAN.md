---
phase: quick-40
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260815000001_grant_monthly_credits_por_raiz.sql (NEW)
  - src/components/admin/monthly-credit-grant-button.tsx
  - src/lib/credit/__tests__/monthly-credits.test.ts
  - scripts/verify/39-monthly-grant-por-raiz-verification.mjs (NEW)
  - openspec/specs/monthly-credits-cron/spec.md
  - openspec/specs/monthly-credits-engine/spec.md
  - openspec/specs/credit-tables/spec.md
  - openspec/specs/admin-credit-grant/spec.md
  - openspec/specs/admin-cnpj-display/spec.md
  - openspec/specs/launch-config/spec.md
  - docs/alinhamento-fase-29-3-creditos-mensais-automaticos.md
  - .planning/REQUIREMENTS.md
  - .env.example
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "grant_monthly_credits concede por RAIZ de CNPJ: no máximo 1 grant por raiz por ciclo mensal, independentemente do nº de lojas da raiz"
    - "Recipiente determinístico escolhido ENTRE TODAS as lojas não-teste da raiz (sem filtro EXISTS no recipiente): matriz (substring(cnpj_normalized,9,4)='0001') preferida; sem matriz, a loja não-teste mais antiga (created_at ASC, id ASC). Histórico freemium validado NO NÍVEL DA RAIZ (freemium_entitlements ou transações de qualquer loja da raiz), nunca no nível do recipiente"
    - "Recipiente sempre possui row em credit_balances: INSERT ... ON CONFLICT DO NOTHING antes do SELECT ... FOR UPDATE (matriz cadastrada depois pode nunca ter recebido crédito e não ter row)"
    - "Semântica de LIMIAR: bonus_balance < cap → grant integral de p_amount (9 → 14 com amount=5); bonus_balance >= cap → NENHUM grant no ciclo (10 → sem grant)"
    - "Ciclo por aniversário em fuso explícito America/Sao_Paulo: raiz concedida apenas no dia-do-mês do created_at do recipiente (conversão AT TIME ZONE 'America/Sao_Paulo'); dia 29/30/31 → último dia do mês curto (nunca dia 1 do mês seguinte)"
    - "Execução diária do cron nunca duplica: idempotência via try_grant_monthly_entitlement por (root_hash, benefit_type, cycle)"
    - "RPC protegida: REVOKE EXECUTE de PUBLIC/anon/authenticated + GRANT EXECUTE TO service_role (hoje ausente = risco de privilégio); callers cron/admin usam supabaseAdmin (service_role) — verificado em route.ts:26 e grant/route.ts:19"
    - "Shape de retorno canônico {eligible, granted, skipped, errors, details} alinhado entre RPC, botão admin e testes; eligible = granted + skipped_already_granted + skipped_bonus_threshold (skipped_not_due NÃO entra em eligible; fica só em details.roots_considered/skipped_not_due)"
    - "Specs e docs sem ambiguidade cap-vs-threshold (insufficient_bonus_cap, 'teto de bônus', 'contam para o cap' removidos)"
  artifacts:
    - path: supabase/migrations/20260815000001_grant_monthly_credits_por_raiz.sql
      provides: "RPC por raiz com limiar, ciclo por aniversário, REVOKE/GRANT e shape canônico"
      min_lines: 120
      exports: ["grant_monthly_credits"]
    - path: src/components/admin/monthly-credit-grant-button.tsx
      provides: "Botão admin lê shape canônico sem falso 'disabled'"
      contains: "body.skipped === true"
    - path: src/lib/credit/__tests__/monthly-credits.test.ts
      provides: "Fixtures de teste com shape canônico"
      contains: "details"
    - path: scripts/verify/39-monthly-grant-por-raiz-verification.mjs
      provides: "Verificação SQL real: day-31, fevereiro, matriz, limiar, idempotência"
      exports: ["main"]
    - path: openspec/specs/monthly-credits-cron/spec.md
      provides: "Spec de cron com semântica por raiz e shape canônico"
      contains: "raiz"
  key_links:
    - from: src/app/api/cron/monthly-credits/route.ts
      to: supabase/migrations/20260815000001_grant_monthly_credits_por_raiz.sql
      via: "supabaseAdmin.rpc('grant_monthly_credits', {p_amount, p_bonus_cap, p_min_store_age_days}) — assinatura mantém os 3 params posicionais, p_reference_date opcional por default"
      pattern: "grant_monthly_credits"
    - from: src/components/admin/monthly-credit-grant-button.tsx
      to: src/app/api/admin/monthly-credits/grant/route.ts
      via: "botão lê body.skipped como flag booleana de disabled; shape numérico requer body.skipped === true"
      pattern: "body\\.skipped === true"
    - from: supabase/migrations/20260815000001_grant_monthly_credits_por_raiz.sql
      to: public.freemium_entitlements
      via: "try_grant_monthly_entitlement(recipient_id, root_hash, cycle) — índice único (root_hash, benefit_type, COALESCE(cycle,'_nostring_')) impede duplicação por raiz+cycle"
      pattern: "try_grant_monthly_entitlement"
---

<objective>
Planejar o alinhamento da concessão mensal de créditos bônus freemium para **semântica por raiz de CNPJ com limiar de elegibilidade** (não teto), sem executar nada — entrega apenas o plano técnico revisável.

**IMPORTANTE (PLAN-ONLY):** Este plano é o DELIVERABLE. Nada deve ser executado, implementado ou migrado neste fluxo. O usuário revisa o plano; a execução é um fluxo separado após aprovação.

**Purpose:** Corrigir a divergência entre a implementação atual do `grant_monthly_credits` (F29.3/F32) e as decisões de produto: grant por raiz CNPJ, +5 integral quando bonus < 10 (limiar, não teto), ciclo por aniversário com clamp de dia 29/30/31, seleção determinística de recipiente (matriz → loja mais antiga), idempotência por raiz+cycle, RPC restrita a service_role e shape de retorno canônico.

**Output:** Este PLAN.md com 3 tasks (migration SQL, consumidores+verificação, specs/docs) pronto para revisão humana. O SUMMARY do fluxo é gerado ao final do PLAN-ONLY.

**Contexto do problema (verificado no código):**
- RPC atual (live em `20260731000003`) itera **por loja** (não por raiz), SEM ORDER BY determinístico, com `v_cycle = TO_CHAR(NOW(),'YYYY-MM')` (mês calendário, não aniversário) e counters mortos (`v_results JSONB[]`, `insufficient_bonus_cap` nunca incrementado).
- RPC atual é SECURITY DEFINER **sem nenhum REVOKE/GRANT EXECUTE** em nenhuma migration → executável por PUBLIC via PostgREST (escalada de privilégio: anon poderia chamar).
- Botão admin (`monthly-credit-grant-button.tsx`) lê `{eligible, granted, skipped, errors}` e usa `if (body.skipped)` como flag de "disabled" — shape atual do RPC não tem `skipped`, então o botão hoje exibe undefined/NaN; e se o shape canônico numérico voltar com `skipped`, `if (body.skipped)` quebraria com falso "Concessão desabilitada".
- Testes (`monthly-credits.test.ts`) mockam `{eligible:25, granted:20, skipped:5, errors:0}` — também divergente do shape live.
- Docs/specs com semântica de "teto/cap/partial-grant" obsoleta: monthly-credits-engine, monthly-credits-cron, credit-tables, admin-credit-grant, admin-cnpj-display, launch-config, `docs/alinhamento-fase-29-3...`, `REQUIREMENTS.md` MONTHLY-06, `.env.example`.

**Justificativa — nenhuma migration de schema necessária:** `stores.cnpj_root_hash` e a tabela `freemium_entitlements` com índice único por raiz+cycle já existem (F32 `20260727000001`). A mudança é exclusivamente `CREATE OR REPLACE FUNCTION` (assinatura com params opcionais adicionais), sem novas colunas/tabelas. `last_monthly_grant_at` (F29.3) permanece legado/deprecado — documentar, NÃO dropar.

**Revisão humana r1 (2026-08-15) — achados incorporados ao plano:**

| # | Achado da revisão | Resolução no plano |
|---|-------------------|--------------------|
| r1-1 | Filtro `EXISTS` de transação bônus no recipiente pode excluir a matriz (filial cadastrada primeiro recebe onboarding; matriz posterior não tem transação bônus) → RPC escolheria filial | Recipiente selecionado entre TODAS as lojas não-teste da raiz (sem EXISTS no recipiente); histórico freemium validado NO NÍVEL DA RAIZ via `freemium_entitlements` (onboarding) ou transações `bonus_*` de qualquer loja da raiz |
| r1-2 | Recipiente (matriz cadastrada depois) pode não ter row em `credit_balances` → `SELECT ... FOR UPDATE` falharia | Garantir row antes do lock: `INSERT INTO credit_balances(store_id, balance, bonus_balance, purchased_balance) VALUES (...) ON CONFLICT (store_id) DO NOTHING` (PK em store_id, verificado em `20260716000001:7`) e depois `SELECT ... FOR UPDATE` |
| r1-3 | `eligible`/`skipped`/`skipped_not_due` semanticamente misturados | `eligible = granted + skipped_already_granted + skipped_bonus_threshold`; `skipped_not_due` (fora do dia de aniversário, idade < mínima, sem recipiente válido) NÃO entra em `eligible`, fica em `details.roots_considered` e `details.skipped_not_due` |
| r1-4 | Timezone implícito (`NOW()::date` depende do fuso da sessão; Supabase = UTC, produto = BR, cron 06:00 UTC) | Fuso explícito `America/Sao_Paulo` em TODOS os cálculos: `(NOW() AT TIME ZONE 'America/Sao_Paulo')::date` e `EXTRACT(DAY FROM (created_at AT TIME ZONE 'America/Sao_Paulo'))`; "dia da loja" = dia civil Brasil |
| r1-5 | DROP da assinatura de 3 params: correto, mas comentário deve reforçar compatibilidade por named params | Documentar: callers cron/admin passam `{p_amount, p_bonus_cap, p_min_store_age_days}` e o 4º param `p_reference_date` tem DEFAULT NULL; DROP existe para eliminar ambiguidade de assinatura/cache PostgREST |
| r1-6 | REVOKE/GRANT validado; pedir confirmação explícita de que cron/admin usam `supabaseAdmin` | Verificado: `cron/monthly-credits/route.ts:26` e `admin/monthly-credits/grant/route.ts:19` usam `supabaseAdmin.rpc("grant_monthly_credits", ...)` (service_role) — fluxo inalterado |
| r1-7 | Script de verificação SQL: `stores.user_id` é UNIQUE (`20260706000001:14`) + FK `auth.users(id)` → não dá para criar várias lojas para o mesmo usuário | Script exige conexão SQL direta via `DATABASE_URL`; cria usuários distintos em `auth.users` (um por loja de teste), cleanup em ordem de FK (credit_transactions → credit_balances/freemium_entitlements → stores → auth.users) |

**Revisão humana r2 (2026-08-15) — achados incorporados (produto/arquitetura aprovado; ajustes de execução):**

| # | Achado da revisão | Resolução no plano |
|---|-------------------|--------------------|
| r2-1 | Ordem "threshold antes de entitlement" distorce `skipped_already_granted`: na 2ª execução do mesmo dia, raiz 9→14 cairia em `skipped_bonus_threshold` em vez de `skipped_already_granted` | Reordenar checks na Task 1: (5) check de existência do entitlement mensal via SELECT ANTES do limiar → `skipped_already_granted`; (6) limiar DEPOIS → `skipped_bonus_threshold`; (7) só então INSERT via `try_grant_monthly_entitlement` (ON CONFLICT = salvaguarda contra corrida concorrente). Não cria entitlement para raiz no limiar, mas conta corretamente quem já recebeu |
| r2-2 | Comparação de idade mínima precisa converter `created_at` para dia civil BR | `(v_recipient.created_at AT TIME ZONE 'America/Sao_Paulo')::date <= (v_ref_date - p_min_store_age_days)` — explícito no `<interfaces>` e como gate no início do item 4 da Task 1; sem diferença por hora/minuto |
| r2-3 | Shape documentado na Task 3 (monthly-credits-cron/spec.md) esquece `roots_considered` | Shape canônico atualizado em TODOS os pontos de descrição: `details` inclui `roots_considered`; semântica `eligible = granted + already + threshold` e `roots_considered = eligible + skipped_not_due` documentadas no spec |
| r2-4 | Decisão do driver do script de verificação ainda aberta (`pg` não instalado) | **Decisão fechada:** adicionar `pg` como devDependency (`npm i -D pg`) + conexão SQL direta via `DATABASE_URL` (ler `.env.local` no padrão do 24-credit-verification.mjs); supabase-js não é adequado para setup/cleanup transacional que mexe em `auth.users` |
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@supabase/migrations/20260731000003_fix_admin_get_metrics_uuid_text.sql
@supabase/migrations/20260727000001_freemium_anti_abuso_cnpj.sql
@supabase/migrations/20260722000002_creditos_mensais_automaticos.sql
@src/components/admin/monthly-credit-grant-button.tsx
@src/lib/credit/__tests__/monthly-credits.test.ts
@scripts/verify/24-credit-verification.mjs
@openspec/specs/monthly-credits-cron/spec.md
@openspec/specs/monthly-credits-engine/spec.md
@openspec/specs/credit-tables/spec.md
@docs/alinhamento-fase-29-3-creditos-mensais-automaticos.md

<interfaces>
**Assinatura atual do RPC (manter compatível com callers):**
`public.grant_monthly_credits(p_amount INTEGER, p_bonus_cap INTEGER, p_min_store_age_days INTEGER) RETURNS JSONB`
Callers (cron + admin route) chamam com named params `{p_amount, p_bonus_cap, p_min_store_age_days}` e passam-through o JSONB. Nenhuma alteração nos routes.

**Nova assinatura proposta (defaults preservam callers):**
`public.grant_monthly_credits(p_amount INTEGER, p_bonus_cap INTEGER, p_min_store_age_days INTEGER, p_reference_date DATE DEFAULT NULL) RETURNS JSONB`
- `p_reference_date`: data de referência para ciclo/aniversário/idade (testabilidade SQL de dia-29/30/31 e fevereiro). `NULL` → usa `NOW()`.
- Callers existentes não passam → comportamento idêntico.

**try_grant_monthly_entitlement (F32, chamada preservada):**
`public.try_grant_monthly_entitlement(store_id UUID, root_hash TEXT, cycle TEXT) RETURNS UUID` — INSERT ON CONFLICT DO NOTHING em `freemium_entitlements(root_hash, 'monthly', cycle)`; retorna id da entitlement ou NULL se já existir.

**grant_credits (F29.3, chamada preservada):**
`public.grant_credits(store_id UUID, amount INTEGER, operation TEXT, idempotency_key TEXT, metadata JSONB, bucket_type TEXT) RETURNS UUID` — usado com `operation='credito_mensal'`, key `'mensal_' || cycle || '_' || store_id`, metadata `{cycle, source:'monthly_cron'}`, bucket `'bonus_monthly'`.

**Shape canônico de retorno (definido pelo plano, consumido por botão + testes):**
```json
{
  "eligible": <granted + skipped_already_granted + skipped_bonus_threshold>,
  "granted": <raízes que receberam grant>,
  "skipped": <eligible - granted>,
  "errors": 0,
  "details": {
    "roots_considered": <total de raízes examinadas no cursor (inclui skipped_not_due)>,
    "skipped_no_cnpj": <lojas sem cnpj_root_hash (fora do cursor)>,
    "skipped_already_granted": <raízes já concedidas no ciclo>,
    "skipped_not_due": <raízes fora do dia de aniversário / idade < mínima / sem recipiente válido — NÃO entra em eligible>,
    "skipped_bonus_threshold": <raízes com bonus >= cap>
  }
}
```
**Semântica (r1-3):** `eligible` = raízes que passaram todos os gates de elegibilidade *exceto* idempotência/limiar. `skipped_not_due` (raiz fora do dia de aniversário ou abaixo da idade mínima) NÃO conta como elegível — essas raízes não estão "devendo" hoje, só foram consideradas (`roots_considered`). Invariante: `skipped = skipped_already_granted + skipped_bonus_threshold = eligible - granted`.

**Fuso horário (r1-4):** TODOS os cálculos de data usam `America/Sao_Paulo` explícito:
- `v_ref_date := COALESCE(p_reference_date, (NOW() AT TIME ZONE 'America/Sao_Paulo')::date);`
- `v_anniv_day := EXTRACT(DAY FROM (v_recipient.created_at AT TIME ZONE 'America/Sao_Paulo'));`
- Comparação de dia: `EXTRACT(DAY FROM v_ref_date) = v_grant_day`.
- Idade mínima (r2-2): `(v_recipient.created_at AT TIME ZONE 'America/Sao_Paulo')::date <= (v_ref_date - p_min_store_age_days)` — comparação de DIA CIVIL BR (sem diferença por hora/minuto), mantendo a regra "idade da loja em dias, no fuso do lojista".

**Seleção determinística de recipiente (por raiz, r1-1):**
1. Considerar TODAS as lojas da raiz com `is_test_store = FALSE` — **sem filtro EXISTS de transação no recipiente**.
2. `ORDER BY CASE WHEN substring(cnpj_normalized, 9, 4) = '0001' THEN 0 ELSE 1 END, created_at ASC, id ASC LIMIT 1`.
3. Histórico freemium validado no NÍVEL DA RAIZ (não do recipiente): raiz elegível se já existe `freemium_entitlements` com `benefit_type='onboarding'` para o `root_hash` OU qualquer loja da raiz possui transação `bonus_onboarding`/`bonus_monthly`.

**Row garantida em credit_balances (r1-2):** antes do `SELECT ... FOR UPDATE`, garantir:
```sql
INSERT INTO public.credit_balances (store_id, balance, bonus_balance, purchased_balance)
VALUES (v_recipient.id, 0, 0, 0)
ON CONFLICT (store_id) DO NOTHING;
```
(PK em `store_id` — `20260716000001:7`.) Depois `SELECT bonus_balance ... FROM credit_balances WHERE store_id = v_recipient.id FOR UPDATE`.

**Botão admin — condição a corrigir (linha 31):**
`if (body.skipped) { setError("Concessão mensal desabilitada ..."); return; }` → `if (body.skipped === true) {...}` (flag booleana dos routes quando `monthlyCreditsEnabled=false`; o shape numérico do RPC não deve disparar o erro).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration — grant_monthly_credits por raiz com limiar, ciclo por aniversário e REVOKE/GRANT</name>
  <files>
    supabase/migrations/20260815000001_grant_monthly_credits_por_raiz.sql (NEW)
  </files>
  <action>
    Criar migration SQL `20260815000001_grant_monthly_credits_por_raiz.sql` com CREATE OR REPLACE FUNCTION `public.grant_monthly_credits` baseada no corpo atual (live em `20260731000003`, linhas 226-291) reescrita para semântica por raiz. Regras:

    1. **Assinatura:** `(p_amount INTEGER, p_bonus_cap INTEGER, p_min_store_age_days INTEGER, p_reference_date DATE DEFAULT NULL) RETURNS JSONB`. Como os 3 primeiros params são os originais + 1 opcional, o CREATE OR REPLACE mantém a assinatura de 3 se existir; incluir `DROP FUNCTION IF EXISTS public.grant_monthly_credits(INTEGER, INTEGER, INTEGER);` ANTES do CREATE OR REPLACE para eliminar ambiguidade PostgREST e garantir que apenas a assinatura nova exista (mesmo padrão do `create_store_with_initial_grant_v2`). **Comentário r1-5 no corpo da migration:** os callers (cron + admin) passam apenas `{p_amount, p_bonus_cap, p_min_store_age_days}` via named params e continuam funcionando porque `p_reference_date` tem `DEFAULT NULL`; o DROP da assinatura de 3 é necessário porque adicionar um parâmetro muda a assinatura da função (CREATE OR REPLACE isolado deixaria ambas as assinaturas, gerando ambiguidade/erro de cache no PostgREST).

    2. **Data de referência e fuso (r1-4):** `v_ref_date := COALESCE(p_reference_date, (NOW() AT TIME ZONE 'America/Sao_Paulo')::date);` — usar v_ref_date em TODO cálculo de ciclo, aniversário e idade. Ciclo: `v_cycle := TO_CHAR(v_ref_date, 'YYYY-MM');`. Todos os `EXTRACT(DAY ...)` sobre `created_at` convertem com `AT TIME ZONE 'America/Sao_Paulo'`.

    3. **Iteração por raiz (substitui o loop por loja):**
       - Cursor: `SELECT DISTINCT s.cnpj_root_hash FROM public.stores s WHERE s.cnpj_root_hash IS NOT NULL AND s.cnpj_root_hash != '' AND s.is_test_store = FALSE` (sem FOR UPDATE por store — o lock é no recipiente).
       - Para cada raiz: selecionar recipiente determinístico via subconsulta com ORDER BY matriz-primeiro (`substring(cnpj_normalized, 9, 4) = '0001'` → 0, senão 1), depois `created_at ASC, id ASC, LIMIT 1`, filtrando APENAS `is_test_store = FALSE` — **NÃO filtrar por EXISTS de transação no recipiente (r1-1)**.
       - Histórico freemium validado NO NÍVEL DA RAIZ (r1-1): raiz sem `freemium_entitlements(benefit_type='onboarding')` E sem transação `bonus_onboarding`/`bonus_monthly` em QUALQUER loja da raiz → incrementar `details.skipped_no_cnpj`? Não — incrementar `details.skipped_not_due` (raiz sem histórico freemium) e CONTINUE (não conceder).
       - Row garantida em credit_balances (r1-2): `INSERT INTO public.credit_balances (store_id, balance, bonus_balance, purchased_balance) VALUES (v_recipient.id, 0, 0, 0) ON CONFLICT (store_id) DO NOTHING;`
       - Lock de concorrência (r1-2): `SELECT bonus_balance INTO v_recipient_bonus FROM public.credit_balances cb WHERE cb.store_id = v_recipient.id FOR UPDATE` (bloqueia dupla execução do cron no mesmo recipiente; sem SKIP LOCKED para evitar grant perdido).

    4. **Dia de aniversário com clamp (nunca dia 1, r1-4):**
       - Idade mínima (r2-2): se `(v_recipient.created_at AT TIME ZONE 'America/Sao_Paulo')::date > (v_ref_date - p_min_store_age_days)` → incrementar `details.skipped_not_due` e CONTINUE (raiz ainda não atingiu idade mínima; comparação por dia civil BR, sem efeito de hora/minuto).
       - `v_anniv_day := EXTRACT(DAY FROM (v_recipient.created_at AT TIME ZONE 'America/Sao_Paulo'));`
       - `v_last_day := EXTRACT(DAY FROM (DATE_TRUNC('month', v_ref_date) + INTERVAL '1 month - 1 day'));`
       - `v_grant_day := LEAST(v_anniv_day, v_last_day);` — dia 29/30/31 cai no último dia do mês curto; nunca rola para o dia 1 do mês seguinte.
       - Conceder a raiz APENAS se `EXTRACT(DAY FROM v_ref_date) = v_grant_day`. Caso contrário incrementar `details.skipped_not_due` e CONTINUE.

    5. **Idempotência por raiz+cycle — check de existência ANTES do limiar (r2-1):** após passar dia/idade/histórico, verificar se já existe entitlement mensal para `(v_root_hash, 'monthly', v_cycle)` com SELECT (somente leitura): `SELECT 1 FROM public.freemium_entitlements WHERE root_hash = v_root_hash AND benefit_type = 'monthly' AND COALESCE(cycle,'_nostring_') = v_cycle`. Se existir → incrementar `details.skipped_already_granted` e CONTINUE. **Razão:** na 2ª execução do mesmo dia, quando a 1ª levou bônus 9→14, a raiz deve cair em `skipped_already_granted` (o entitlement do ciclo já existe), NÃO em `skipped_bonus_threshold` — a contagem reflete a causa real do skip.

    6. **Limiar de elegibilidade (NÃO teto) — DEPOIS do check de existência (r2-1):** checar `v_recipient_bonus < p_bonus_cap` (bonus_balance do recipiente, lido sob lock após row garantida). Se `>= p_bonus_cap` → incrementar `details.skipped_bonus_threshold` e CONTINUE (sem entitlement criado, sem grant — raiz perde o ciclo, decisão de produto: bonus 10 não recebe). Se abaixo → conceder o valor INTEGRAL `p_amount` (sem LEAST/partial).

    7. **INSERT de entitlement + grant (r2-1, protege corrida concorrente):** só então chamar `v_entitlement_id := public.try_grant_monthly_entitlement(v_recipient.id, v_root_hash, v_cycle);` — o ON CONFLICT DO NOTHING do INSERT é a salvaguarda final contra execuções concorrentes do cron (o check do item 5 é leitura pura; a corrida real se resolve aqui). Se NULL → incrementar `details.skipped_already_granted` e CONTINUE. Após `grant_credits(...)` com idempotency_key `'mensal_' || v_cycle || '_' || v_root_hash`, atualizar `public.freemium_entitlements SET grant_transaction_id = v_grant_tx_id WHERE id = v_entitlement_id`.

    8. **Contadores:** remover código morto atual (`v_results JSONB[]`, `v_insufficient_bonus_cap`). Declarar: `v_roots_considered`, `v_eligible`, `v_granted`, `v_skipped_no_cnpj` (lojas sem cnpj fora do cursor — pré-contagem), `v_skipped_already_granted`, `v_skipped_not_due`, `v_skipped_bonus_threshold`.

    9. **Return shape canônico (r1-3):** `jsonb_build_object('eligible', v_eligible, 'granted', v_granted, 'skipped', v_eligible - v_granted, 'errors', 0, 'details', jsonb_build_object('roots_considered', v_roots_considered, 'skipped_no_cnpj', v_skipped_no_cnpj, 'skipped_already_granted', v_skipped_already_granted, 'skipped_not_due', v_skipped_not_due, 'skipped_bonus_threshold', v_skipped_bonus_threshold))`. **Semântica:** `v_eligible := v_granted + v_skipped_already_granted + v_skipped_bonus_threshold` (raízes que passaram dia/idade/histórico); `skipped_not_due` NÃO entra em eligible (r1-3).

    10. **REVOKE/GRANT security (fechar gap, r1-6):** após o CREATE OR REPLACE:
       `REVOKE EXECUTE ON FUNCTION public.grant_monthly_credits(INTEGER, INTEGER, INTEGER, DATE) FROM PUBLIC, anon, authenticated;` e `GRANT EXECUTE ON FUNCTION public.grant_monthly_credits(INTEGER, INTEGER, INTEGER, DATE) TO service_role;` — os routes usam `supabaseAdmin` (service_role), fluxo inalterado; anon/authenticated perdem acesso direto à RPC SECURITY DEFINER. **Verificação explícita:** confirmar no código que os únicos callers são `cron/monthly-credits/route.ts:26` e `admin/monthly-credits/grant/route.ts:19`, ambos `supabaseAdmin.rpc("grant_monthly_credits", ...)`.

    11. **Comentários:** adicionar COMMENT ON FUNCTION explicando: grant por raiz, limiar (não teto), ciclo por aniversário com clamp, recipiente matriz-preferido, ordem dos checks (existência → limiar → INSERT). Incluir bloco REVERT no final: `-- REVERT` + `-- DROP FUNCTION IF EXISTS public.grant_monthly_credits CASCADE;` + nota de re-criar a partir de `20260731000003`.

    12. **NOTA:** NÃO editar migrations antigas. NÃO criar tabelas/colunas/índices. NÃO dropar `last_monthly_grant_at` (legado; apenas documento em Task 3). Nome do arquivo: `supabase/migrations/20260815000001_grant_monthly_credits_por_raiz.sql`.

    Detalhe de edge case: raiz sem nenhuma loja não-teste → recipiente nulo → incrementar `v_skipped_not_due` (raiz sem recipiente válido) e CONTINUE. Documentar a escolha no corpo SQL via comentário.
  </action>
  <verify>
    <automated>File exists: Test-Path -LiteralPath "supabase/migrations/20260815000001_grant_monthly_credits_por_raiz.sql"</automated>
    <automated>Assinatura nova: Select-String -LiteralPath "supabase/migrations/20260815000001_grant_monthly_credits_por_raiz.sql" -Pattern "p_reference_date DATE DEFAULT NULL"</automated>
    <automated>DROP da assinatura de 3 params: Select-String -LiteralPath "supabase/migrations/20260815000001_grant_monthly_credits_por_raiz.sql" -Pattern "DROP FUNCTION IF EXISTS public.grant_monthly_credits\(INTEGER, INTEGER, INTEGER\)"</automated>
    <automated>Clamp de dia: Select-String -LiteralPath "supabase/migrations/20260815000001_grant_monthly_credits_por_raiz.sql" -Pattern "LEAST\(v_anniv_day"</automated>
    <automated>Fuso explícito: Select-String -LiteralPath "supabase/migrations/20260815000001_grant_monthly_credits_por_raiz.sql" -Pattern "AT TIME ZONE 'America/Sao_Paulo'"</automated>
    <automated>Row garantida antes do lock: Select-String -LiteralPath "supabase/migrations/20260815000001_grant_monthly_credits_por_raiz.sql" -Pattern "ON CONFLICT \(store_id\) DO NOTHING"</automated>
    <automated>Sem EXISTS no recipiente: Select-String -LiteralPath "supabase/migrations/20260815000001_grant_monthly_credits_por_raiz.sql" -Pattern "EXISTS \(SELECT 1 FROM public.credit_transactions ct WHERE ct.store_id = s.id" | ForEach-Object { if ($_) { "FAIL: EXISTS filter on recipient still present" } }</automated>
    <automated>Limiar (não LEAST de partial): Select-String -LiteralPath "supabase/migrations/20260815000001_grant_monthly_credits_por_raiz.sql" -Pattern "v_recipient_bonus < p_bonus_cap"</automated>
    <automated>Sem partial-grant: Select-String -LiteralPath "supabase/migrations/20260815000001_grant_monthly_credits_por_raiz.sql" -Pattern "LEAST\(p_amount" | ForEach-Object { if ($_) { "FAIL: partial grant still present" } }</automated>
    <automated>Matriz preferida: Select-String -LiteralPath "supabase/migrations/20260815000001_grant_monthly_credits_por_raiz.sql" -Pattern "substring\(cnpj_normalized, 9, 4\)"</automated>
    <automated>Idempotência entitlement: Select-String -LiteralPath "supabase/migrations/20260815000001_grant_monthly_credits_por_raiz.sql" -Pattern "try_grant_monthly_entitlement"</automated>
    <automated>REVOKE EXECUTE: Select-String -LiteralPath "supabase/migrations/20260815000001_grant_monthly_credits_por_raiz.sql" -Pattern "REVOKE EXECUTE ON FUNCTION public.grant_monthly_credits"</automated>
    <automated>GRANT EXECUTE TO service_role: Select-String -LiteralPath "supabase/migrations/20260815000001_grant_monthly_credits_por_raiz.sql" -Pattern "TO service_role"</automated>
    <automated>Código morto removido: Select-String -LiteralPath "supabase/migrations/20260815000001_grant_monthly_credits_por_raiz.sql" -Pattern "v_results" | ForEach-Object { if ($_) { "FAIL: dead v_results still present" } }</automated>
    <automated>Shape canônico: Select-String -LiteralPath "supabase/migrations/20260815000001_grant_monthly_credits_por_raiz.sql" -Pattern "'skipped_bonus_threshold'"</automated>
    <automated>Shape com roots_considered: Select-String -LiteralPath "supabase/migrations/20260815000001_grant_monthly_credits_por_raiz.sql" -Pattern "'roots_considered'"</automated>
    <automated>Eligible exclui skipped_not_due: Select-String -LiteralPath "supabase/migrations/20260815000001_grant_monthly_credits_por_raiz.sql" -Pattern "v_eligible := v_granted \+ v_skipped_already_granted \+ v_skipped_bonus_threshold"</automated>
    <automated>REVERT section: Select-String -LiteralPath "supabase/migrations/20260815000001_grant_monthly_credits_por_raiz.sql" -Pattern "DROP FUNCTION IF EXISTS public.grant_monthly_credits CASCADE"</automated>
  </verify>
  <done>
    Migration `20260815000001_grant_monthly_credits_por_raiz.sql` criada com: grant por raiz, recipiente determinístico escolhido entre TODAS as lojas não-teste da raiz (matriz → mais antiga, sem EXISTS no recipiente; histórico freemium validado no nível da raiz), row garantida em credit_balances antes do lock FOR UPDATE, limiar bonus_balance < p_bonus_cap com grant integral, ciclo por aniversário em America/Sao_Paulo com clamp dia 29/30/31 (nunca dia 1), p_reference_date para testabilidade, idempotência via try_grant_monthly_entitlement por raiz+cycle, REVOKE/GRANT service_role, shape canônico com details (eligible = granted + already_granted + threshold; skipped_not_due fora de eligible), sem código morto, sem partial-grant, sem schema change, sem editar migrations antigas.
  </done>
</task>

<task type="auto">
  <name>Task 2: Alinhar botão admin, testes de rota e script de verificação SQL real</name>
  <files>
    src/components/admin/monthly-credit-grant-button.tsx (EDIT)
    src/lib/credit/__tests__/monthly-credits.test.ts (EDIT)
    scripts/verify/39-monthly-grant-por-raiz-verification.mjs (NEW)
  </files>
  <action>
    **monthly-credit-grant-button.tsx:**
    1. Linha 31: trocar `if (body.skipped)` por `if (body.skipped === true)` — a flag booleana de disabled vem dos routes (`{skipped: true}` quando `monthlyCreditsEnabled=false`); o shape canônico do RPC tem `skipped` numérico e NÃO deve disparar o erro "Concessão mensal desabilitada".
    2. Manter a interface `GrantResult` com os 4 campos top-level (eligible, granted, skipped, errors) — o RPC novo retorna exatamente esses + `details`; adicionar `details` como campo opcional na interface para exibição opcional (não obrigatório renderizar).
    3. A linha 61 `{result.eligible} lojas elegíveis` → `{result.eligible} raízes elegíveis` (semântica por raiz).
    4. NÃO alterar a rota `/api/cron/monthly-credits` nem `/api/admin/monthly-credits/grant` (não recebem o 4º param; default NULL aplicado).

    **monthly-credits.test.ts:**
    1. Substituir o fixture `GRANT_RESULT` (linhas 38-42) pelo shape canônico: `{ eligible: 20, granted: 20, skipped: 0, errors: 0, details: { roots_considered: 24, skipped_no_cnpj: 1, skipped_already_granted: 0, skipped_not_due: 4, skipped_bonus_threshold: 0 } }`. Atenção r1-3: `eligible` = `granted + skipped_already_granted + skipped_bonus_threshold` (aqui 20+0+0=20); `skipped_not_due` NÃO soma em eligible e `roots_considered` = `eligible + skipped_not_due` (24, cursor). `skipped_no_cnpj` (1) fica fora do cursor.
    2. Verificar os asserts `expect(body).toEqual(GRANT_RESULT)` (linhas 66 e 145) — permanecem válidos com o novo fixture.
    3. Adicionar um caso de teste no cenário do cron route: quando a rota responde `{ skipped: true }` (disabled), o handler retorna isso sem chamar o RPC (se já existir, manter; senão criar).
    4. Adicionar caso: RPC retorna shape canônico com `skipped: 5` numérico e o teste NÃO deve interpretar como disabled — cobertura do contrato botão/flags.

    **scripts/verify/39-monthly-grant-por-raiz-verification.mjs (NEW):**
    Diferente do `24-credit-verification.mjs` (supabase-js via service role, só leitura de `stores`/`credit_balances`), este script mexe em `auth.users` e faz setup/cleanup transacional → **decisão fechada (r2-4): usar `pg` com conexão SQL direta via `DATABASE_URL`.** `pg` NÃO está no package.json hoje; adicionar como devDependency (`npm i -D pg`) como parte desta task — `@supabase/supabase-js` não é adequado para operações transacionais diretas em `auth.users`/`stores` com cleanup em ordem de FK. Carregar `DATABASE_URL` seguindo o padrão do `24-credit-verification.mjs` (ler `.env.local` com `readFileSync` + regex; a variável é `DATABASE_URL` ou a connection string equivalente presente no `.env.local`). Usar `pg.Pool` + transação única por cenário. **Regra r1-7 — dados de teste:** `stores.user_id` é UNIQUE (`20260706000001:14`) e há FK para `auth.users(id)` → criar UM usuário distinto em `auth.users` por loja de teste (INSERT em auth.users com id gen_random_uuid() e email único), nunca reutilizar o mesmo user_id. Cleanup em ordem de FK: `credit_transactions` → `credit_balances`/`freemium_entitlements` → `stores` → `auth.users` (usuários de teste). Finalizar com resumo PASS/FAIL e exit code não-zero se qualquer assert falhar.

    Cenários (cada um com raízes criadas via SQL direto):
    1. **day-31 → mês curto:** criar raiz com recipiente created_at no dia 31; `SELECT public.grant_monthly_credits(5, 10, 0, '2026-04-15')` (mês de 30 dias) → raiz NÃO concedida (`details.skipped_not_due`), `eligible` não a inclui; depois `SELECT ...('2026-04-30')` → concedida (granted=1, bonus passa a 5). LIMPAR raiz ao final.
    2. **fevereiro:** mesma raiz, `p_reference_date '2026-02-28'` → concedida em 28 (LEAST(31,28)); `'2026-02-27'` → não.
    3. **limiar 9 → 14:** raiz com recipiente bonus 9 → grant integral +5 → bonus 14 (NÃO 10). Assert via SELECT credit_balances.
    4. **limiar 10 → sem grant:** raiz com bonus 10 → `details.skipped_bonus_threshold` incrementado, `eligible` a inclui (passou gates, falhou limiar), nenhum grant, bonus permanece 10.
    5. **matriz + filiais (r1-1):** raiz com filial ANTIGA (recebeu onboarding) + matriz CADASTRADA DEPOIS (sem transação bônus própria) → recipiente = MATRIZ (não a filial com transação); 1 grant só, em `credit_transactions.store_id` = matriz.
    6. **sem matriz → mais antiga:** raiz só com filiais → grant na filial mais antiga (created_at ASC), todas não-teste.
    7. **dupla execução no mesmo ciclo:** chamar o RPC 2x com mesma p_reference_date → 2ª chamada `details.skipped_already_granted` e NENHUM grant adicional (idempotência raiz+cycle).
    8. **consumo bônus primeiro:** após grant, `reserve_credit` com bônus+purchased → deduz bônus antes de purchased (assert via metadata bonus_amount/purchased_amount da deduction).
  </action>
  <verify>
    <automated>npx vitest run src/lib/credit/__tests__/monthly-credits.test.ts --reporter=verbose 2>&1 | Select-String -Pattern "PASS|FAIL|✓|✗"</automated>
    <automated>npx tsc --noEmit 2>&1 | Select-String -Pattern "error" | Select-Object -First 5</automated>
    <automated>Select-String -LiteralPath "src/components/admin/monthly-credit-grant-button.tsx" -Pattern "body\.skipped === true"</automated>
    <automated>Select-String -LiteralPath "src/lib/credit/__tests__/monthly-credits.test.ts" -Pattern "skipped_bonus_threshold"</automated>
    <automated>File exists: Test-Path -LiteralPath "scripts/verify/39-monthly-grant-por-raiz-verification.mjs"</automated>
    <automated>Script cobre day-31 e fevereiro: Select-String -LiteralPath "scripts/verify/39-monthly-grant-por-raiz-verification.mjs" -Pattern "2026-04-30|2026-02-28"</automated>
    <automated>Script usa conexão SQL direta (r1-7): Select-String -LiteralPath "scripts/verify/39-monthly-grant-por-raiz-verification.mjs" -Pattern "DATABASE_URL"</automated>
    <automated>Script usa driver pg (r2-4): Select-String -LiteralPath "scripts/verify/39-monthly-grant-por-raiz-verification.mjs" -Pattern "from \"pg\"|import.*pg"</automated>
    <automated>pg devDependency instalado (r2-4): Select-String -Path "package.json" -Pattern '"pg"' | ForEach-Object { $_.Line.Trim() }</automated>
    <automated>Script cria usuários distintos em auth.users (r1-7): Select-String -LiteralPath "scripts/verify/39-monthly-grant-por-raiz-verification.mjs" -Pattern "auth.users"</automated>
    <automated>Cenário r1-1 (matriz sem transação própria): Select-String -LiteralPath "scripts/verify/39-monthly-grant-por-raiz-verification.mjs" -Pattern "matriz"</automated>
    <automated>Script limpa dados de teste: Select-String -LiteralPath "scripts/verify/39-monthly-grant-por-raiz-verification.mjs" -Pattern "DELETE|cleanup" | Select-Object -First 2</automated>
  </verify>
  <done>
    Botão admin usa `body.skipped === true` (sem falso "disabled" com shape numérico), rótulo "raízes elegíveis", fixtures de teste com shape canônico (eligible exclui skipped_not_due) + detalhes, testes de rota passando, script de verificação SQL real com 8 cenários (day-31, fevereiro, limiar 9→14, limiar 10, matriz+filiais com matriz sem transação própria, sem-matriz → mais antiga, idempotência, consumo bônus primeiro) criado com driver `pg` (devDependency adicionada), conexão SQL direta via DATABASE_URL e usuários distintos por loja (user_id UNIQUE).
  </done>
</task>

<task type="auto">
  <name>Task 3: Corrigir specs, docs e env para semântica por raiz e limiar (remover cap/partial-grant)</name>
  <files>
    openspec/specs/monthly-credits-cron/spec.md (EDIT)
    openspec/specs/monthly-credits-engine/spec.md (EDIT)
    openspec/specs/credit-tables/spec.md (EDIT)
    openspec/specs/admin-credit-grant/spec.md (EDIT)
    openspec/specs/admin-cnpj-display/spec.md (EDIT)
    openspec/specs/launch-config/spec.md (EDIT)
    docs/alinhamento-fase-29-3-creditos-mensais-automaticos.md (EDIT)
    .planning/REQUIREMENTS.md (EDIT)
    .env.example (EDIT)
  </files>
  <action>
    **openspec/specs/monthly-credits-cron/spec.md:**
    1. Atualizar o shape de retorno documentado para o canônico `{eligible, granted, skipped, errors, details:{roots_considered, skipped_no_cnpj, skipped_already_granted, skipped_not_due, skipped_bonus_threshold}}` — **incluir `roots_considered` (r2-3)** e documentar a semântica: `eligible = granted + skipped_already_granted + skipped_bonus_threshold`; `roots_considered = eligible + skipped_not_due`; `skipped_not_due` NÃO entra em eligible.
    2. Revisar o cenário F32 "três filiais + matriz = 1 grant mensal" para explicitar: recipiente = matriz; sem matriz → loja mais antiga não-teste; 1 grant por raiz+cycle.
    3. Adicionar cenário de limiar: bonus 9 → +5 integral (14); bonus 10 → skipped_bonus_threshold, sem grant.
    4. Adicionar cenário de aniversário: raiz concedida só no dia-do-mês do created_at do recipiente; dia 29/30/31 → último dia do mês curto; nunca dia 1.
    5. Documentar fuso explícito (r1-4): todos os cálculos de dia/ciclo/idade usam `America/Sao_Paulo` (`(NOW() AT TIME ZONE 'America/Sao_Paulo')::date`; `created_at AT TIME ZONE 'America/Sao_Paulo'`), independente do fuso da sessão do banco (Supabase = UTC, cron roda 06:00 UTC = 03:00 BR).

    **openspec/specs/monthly-credits-engine/spec.md:**
    1. Reescrever a semântica de grant: remover "teto de bônus", "partial grant via LEAST" e "ciclo efetivo de 30 dias" → "limiar de elegibilidade bonus_balance < monthlyBonusCap; grant integral de monthlyCreditsAmount quando abaixo; NENHUM grant quando no limiar ou acima".
    2. Substituir menções a `last_monthly_grant_at` (30 dias corridos) pela idempotência por `freemium_entitlements(root_hash, 'monthly', cycle)` com ciclo YYYY-MM.
    3. Documentar o clamp de aniversário e a seleção determinística de recipiente (matriz → mais antiga).
    4. Atualizar casos de exemplo de saldo (9+5=14; 10 → sem mudança).

    **openspec/specs/credit-tables/spec.md:**
    1. Marcar `last_monthly_grant_at` e o índice parcial `idx_credit_balances_monthly_grant` como DEPRECATED (legado F29.3, não usado desde F32; mantido no schema por compatibilidade, sem planos de drop no escopo atual).
    2. Nota: a tabela não muda neste alinhamento.

    **openspec/specs/admin-credit-grant/spec.md:**
    1. Trocar a frase "contam para o monthlyBonusCap (D9)" por "contam para o limiar de elegibilidade do grant mensal (bonus_balance < monthlyBonusCap)".

    **openspec/specs/admin-cnpj-display/spec.md:**
    1. Trocar o texto "🔴 Freemium esgotado — raiz usou onboarding + mensal, teto de bônus atingido" por "🔴 Freemium esgotado — raiz usou onboarding + mensal; bonus_balance no limiar (>= monthlyBonusCap) ou ciclo mensal já concedido".
    2. Se houver coluna/status "teto de bônus" no admin, renomear rótulo para "limiar de bônus" (label de UI/coluna de dados, conforme existir).

    **openspec/specs/launch-config/spec.md:**
    1. Trocar "VENDEO_MONTHLY_BONUS_CAP customiza teto" por "customiza o LIMIAR de elegibilidade do grant mensal (loja/raiz recebe grant integral enquanto bonus_balance < cap; em ou acima, não recebe no ciclo)".

    **docs/alinhamento-fase-29-3-creditos-mensais-automaticos.md:**
    1. Adicionar seção "Revisão de alinhamento (pós-F32 — semântica por raiz e limiar)" no topo documentando: decisões finais (por raiz, limiar não-teto, ciclo por aniversário com clamp, matriz-preferida, shape canônico), apontando que as seções antigas D4/D7 descrevem o desenho F29.3 original (cap/refill/partial) e estão superadas pelas decisões mais recentes.
    2. Não reescrever a história — apenas marcar o status "substituído por" na seção nova.

    **.planning/REQUIREMENTS.md (MONTHLY-06):**
    1. Reescrever o item MONTHLY-06 para: "grant_monthly_credits por raiz CNPJ com elegibilidade por idade da loja (>= 30 dias), limiar de bônus configurável (grant integral quando bonus_balance < cap; nenhum grant quando >=), idempotência por ciclo mensal via freemium_entitlements (root_hash, 'monthly', cycle), ciclo por aniversário com clamp de dia 29/30/31 (nunca dia 1), recipiente determinístico (matriz → loja mais antiga não-teste)".
    2. Remover "grant parcial" e "ciclo efetivo de 30 dias" do texto.
    3. Marcar como atendido somente se a migration da Task 1 existir; caso contrário manter pendente (o executor marca conforme estado real).

    **.env.example:**
    1. Trocar o comentário `# Maximum bonus balance a store can accumulate` por `# Limiar de elegibilidade do grant mensal freemium: raiz recebe grant integral enquanto bonus_balance < cap; em ou acima do limiar, não recebe no ciclo`.

    **Regra geral:** em todos os arquivos, remover/reformular "teto", "cap de acumulação", "partial grant", "LEAST(p_amount,...)", "ciclo de 30 dias" e "last_monthly_grant_at como fonte de verdade" — trocar por limiar + por raiz + ciclo YYYY-MM + aniversário com clamp. Não tocar em specs sem relação (freemium-entitlement, credit-sql-functions a menos que mencionem monthly grant).
  </action>
  <verify>
    <automated>Select-String -LiteralPath "openspec/specs/monthly-credits-cron/spec.md" -Pattern "skipped_bonus_threshold"</automated>
    <automated>Select-String -LiteralPath "openspec/specs/monthly-credits-engine/spec.md" -Pattern "limiar"</automated>
    <automated>Select-String -LiteralPath "openspec/specs/credit-tables/spec.md" -Pattern "DEPRECATED|deprecated"</automated>
    <automated>Select-String -LiteralPath "openspec/specs/launch-config/spec.md" -Pattern "limiar"</automated>
    <automated>Select-String -LiteralPath "docs/alinhamento-fase-29-3-creditos-mensais-automaticos.md" -Pattern "Revisão de alinhamento"</automated>
    <automated>Select-String -LiteralPath ".planning/REQUIREMENTS.md" -Pattern "limiar de bônus"</automated>
    <automated>Select-String -LiteralPath ".env.example" -Pattern "Limiar de elegibilidade"</automated>
    <automated>Grep de resíduo: Select-String -Path "openspec/specs/monthly-credits-cron/spec.md","openspec/specs/monthly-credits-engine/spec.md","openspec/specs/launch-config/spec.md",".env.example" -Pattern "teto|partial|LEAST\(p_amount" | ForEach-Object { "RESIDUE at $($_.Path):$($_.LineNumber): $($_.Line.Trim())" }</automated>
  </verify>
  <done>
    Specs (monthly-credits-cron, monthly-credits-engine, credit-tables, admin-credit-grant, admin-cnpj-display, launch-config), doc de alinhamento F29.3, REQUIREMENTS.md MONTHLY-06 e .env.example refletem semântica por raiz + limiar + ciclo por aniversário; nenhum resíduo de "teto/partial/ciclo de 30 dias/last_monthly_grant_at como verdade" nos arquivos revisados.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| anon/authenticated → RPC | POSTGREST chama `grant_monthly_credits`; hoje SEM REVOKE = PUBLIC executable (escalada de privilégio via SECURITY DEFINER). |
| cron/admin → RPC | Chamada legítima via `supabaseAdmin` (service_role) com params controlados pelo Launch Config. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-Q01-01 | Elevation of Privilege | `grant_monthly_credits` (SECURITY DEFINER, sem REVOKE hoje) | mitigate | Task 1 item 9: `REVOKE EXECUTE FROM PUBLIC, anon, authenticated` + `GRANT EXECUTE TO service_role` na migration nova. |
| T-Q01-02 | Tampering | Recipiente por raiz | mitigate | Seleção determinística com ORDER BY matriz-primeiro, created_at ASC, id ASC; lock FOR UPDATE na credit_balances do recipiente evita dupla concessão concorrente. |
| T-Q01-03 | Repudiation | Concessão mensal | accept | `credit_transactions` com idempotency_key `mensal_<cycle>_<root>` + `freemium_entitlements.grant_transaction_id` já registram o evento; custo de auditoria aceito (sem PII sensível). |
| T-Q01-04 | DoS | RPC pública (antes do fix) | mitigate | REVOKE elimina chamada externa arbitrária; após REVOKE, apenas service_role invoca. |
| T-Q01-05 | Tampering | Migration SQL (package-less, SQL puro) | mitigate | Nenhum pacote externo; revisão humana do PLAN-ONLY antes de executar + bloco REVERT documentado. |
</threat_model>

<verification>
- [ ] Migration `20260815000001_grant_monthly_credits_por_raiz.sql` com assinatura nova (4 params, p_reference_date DEFAULT NULL), DROP da assinatura de 3 params, grant por raiz, limiar não-teto, recipiente sem EXISTS (matriz preferida entre TODAS as lojas não-teste), row garantida em credit_balances antes do lock, idade mínima e clamp de aniversário em America/Sao_Paulo (dia civil BR), ORDEM dos checks existência→limiar→INSERT (r2-1), REVOKE/GRANT service_role, shape canônico (eligible exclui skipped_not_due; roots_considered incluído), sem código morto
- [ ] Botão admin: `body.skipped === true`, label "raízes elegíveis", interface GrantResult com details opcional
- [ ] Testes monthly-credits: fixture canônico (roots_considered = eligible + skipped_not_due) + caso skipped numérico não é disabled; vitest PASS
- [ ] `npm run typecheck` e `npm run lint` limpos (validar que botão/interface não quebram)
- [ ] Script `39-monthly-grant-por-raiz-verification.mjs` criado com 8 cenários, driver `pg` (devDependency adicionada), conexão SQL direta via DATABASE_URL, usuários distintos em auth.users (user_id UNIQUE) e cleanup em ordem de FK
- [ ] Specs/docs/env corrigidos sem resíduos de cap/partial/teto/ciclo-30-dias; cron spec documenta fuso America/Sao_Paulo e shape com roots_considered (r2-3)
- [ ] Nenhuma migration antiga editada; nenhuma tabela/coluna nova criada
- [ ] Callers verificados: cron `route.ts:26` e admin `grant/route.ts:19` usam `supabaseAdmin` (service_role)
</verification>

<success_criteria>
- **PLAN-ONLY concluído:** o deliverable é este PLAN.md revisado e aprovado pela revisão humana (r1 + r2 incorporados) — NENHUMA implementação foi executada neste fluxo.
- O plano, quando executado, produz: RPC por raiz com limiar (9→14, 10→sem grant), recipiente determinístico escolhido entre TODAS as lojas não-teste (matriz → mais antiga, sem EXISTS no recipiente; histórico freemium validado no nível da raiz), idade mínima e ciclo por aniversário em America/Sao_Paulo (dia civil BR) com clamp 29/30/31 (nunca dia 1), ordem de checks existência→limiar→INSERT (idempotência por raiz+cycle correta mesmo na 2ª execução do dia), REVOKE/GRANT service_role, shape canônico `{eligible, granted, skipped, errors, details}` alinhado entre RPC/botão/testes (eligible = granted + already_granted + threshold; roots_considered = eligible + skipped_not_due), e specs/docs/env sem ambiguidade cap-vs-threshold.
- Callers existentes (cron + admin routes) continuam funcionando sem alteração (defaults preservados; DROP da assinatura de 3 elimina ambiguidade PostgREST).
- Zero mudanças de schema (nenhuma tabela/coluna/índice novo; `last_monthly_grant_at` mantido como legado deprecado).
</success_criteria>

<output>
`.planning/quick/260815-i9a-objetivo-planejar-sem-executar-o-alinham/260815-i9a-SUMMARY.md`
</output>

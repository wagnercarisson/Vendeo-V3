---
phase: quick-p3u
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/flow/use-campaign-form.ts
  - src/components/flow/__tests__/use-campaign-form-validity.test.ts
autonomous: false
requirements: [Q-P3U-01, Q-P3U-02]
must_haves:
  truths:
    - "Datas de validade anteriores à data de hoje bloqueiam o submit no frontend, antes do fetch/geração, com erro inline por campo"
    - "No modo until-date, validityEndDate < hoje → fieldErrors.validityEndDate = 'Data final não pode ser anterior à data de hoje'; validade igual a hoje é permitida"
    - "No modo range, validityStartDate < hoje → 'Data inicial não pode ser anterior à data de hoje'; validityEndDate < hoje → 'Data final não pode ser anterior à data de hoje'; início hoje e fim hoje permitidos; regra start <= end preservada com mensagem 'Data inicial não pode ser posterior à data final'"
    - "Modos today/stock/custom não passam por validação contra hoje"
    - "A checagem de data passada em validityEndDate/validityStartDate só roda quando o modo exige data (until-date/range); modos today/stock/custom retornam null independentemente de datas preenchidas (ex.: draft antigo trocado para custom não bloqueia)"
    - "Comparação de datas é lexicográfica por string ISO YYYY-MM-DD (sem new Date() para datas do usuário); 'hoje' é obtido por helper getTodayISO() que converte a data local corrente para YYYY-MM-DD"
    - "Testes obtêm 'hoje' de forma determinística via fake timers de Date (vi.useFakeTimers({ toFake: ['Date'] }) + vi.setSystemTime), padrão já usado no repo"
    - "Nenhuma mudança de contrato/backend/schema/rota/prompts/reviewer — a rota continua recebendo validity?: string"
  artifacts:
    - path: "src/components/flow/use-campaign-form.ts"
      provides: "Helper getTodayISO + validação de data passada nos validators de validade (param opcional todayISO com default getTodayISO())"
      contains: "getTodayISO"
      exports: ["getTodayISO"]
    - path: "src/components/flow/__tests__/use-campaign-form-validity.test.ts"
      provides: "Testes de bloqueio por data passada (until-date/range, passado/hoje) e ausência de validação em today/stock/custom"
      contains: "anterior à data de hoje"
  key_links:
    - from: "src/components/flow/use-campaign-form.ts"
      to: "src/components/flow/__tests__/use-campaign-form-validity.test.ts"
      via: "validateValidityEndDate/validateValidityStartDate com todayISO determinístico → handleSubmit bloqueia antes do fetch"
      pattern: "anterior à data de hoje"
---

<objective>
**Quick — Validação de Data Passada na validade da oferta:** bloquear no frontend, antes do fetch/geração, datas de validade anteriores à data atual (compare ISO `YYYY-MM-DD` por string; hoje determinístico nos testes).

**Escopo (locked):** apenas `use-campaign-form.ts` (validators + helper `getTodayISO`) e `use-campaign-form-validity.test.ts`. NÃO tocar ValidityField/UI, prompts, copy director, reviewer multi-imagens, backend, schema, rota nem contratos — a rota continua recebendo `validity?: string`; a validação fica no frontend como a de ordem do intervalo.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/quick.md
</execution_context>

<context>
@.planning/STATE.md
@src/components/flow/use-campaign-form.ts
@src/components/flow/__tests__/use-campaign-form-validity.test.ts

**Estado atual confirmado por leitura:**
- `validateValidityEndDate` (`use-campaign-form.ts:242-248`): exigência por modo (`until-date`/`range`) + erro genérico quando vazio. `validateValidityStartDate` (`:250-260`): exigência no range + ordem `start > end` → "Data inicial não pode ser posterior à data final" (mensagem já correta — sem ajuste).
- Comparação de ordem já é lexicográfica de ISO `YYYY-MM-DD` (`:254-256`, D5) — extensível com segurança para ">= hoje".
- `validateField` (`:262-284`) chama os validators sem parâmetro extra; `handleSubmit` (`:767-823`) itera `allFields` via `validateField` e bloqueia antes de montar o body/fetch. Nenhuma mudança de assinatura de hook necessária.
- UI: `campaign-input-form.tsx` já integra `startDateError`/`endDateError`/`onStartDateBlur`/`onEndDateBlur` (`:536-548`); ValidityField já renderiza os erros inline — mensagens novas chegam à UI sem tocar em componente.
- Sem helper `getTodayISO` existente no repo (grep). Padrão de fake timers já estabelecido (`vi.setSystemTime` em `draft-store.test.ts`, `shouldAdvanceTime` em `intent-badge-preserve.test.ts`).
- Testes D2/D5 existentes usam datas futuras fixas (2026-09-25/30) — permanecem válidos sem fake timers.
</context>

<interfaces>
<!-- Contratos que o executor deve usar diretamente — sem exploração adicional. -->

From src/components/flow/use-campaign-form.ts (estado atual):
```typescript
function validateValidityEndDate(fields: CampaignFormFields): string | null;
function validateValidityStartDate(fields: CampaignFormFields): string | null;
export function buildValidityDisplayText(fields: {...}): string | undefined;
```

`validateField(field, fields)` (`:262-284`) → switch com cases `validityStartDate`/`validityEndDate`. `handleSubmit` (`:767-823`) → valida todos os campos, `setFieldErrors` + `setTouched` + `return` antes do `fetch`.

**Alvo pós-mudança (Task 1):**
```typescript
export function getTodayISO(now: Date = new Date()): string; // YYYY-MM-DD local, determinístico dado `now`
function validateValidityEndDate(fields: CampaignFormFields, todayISO: string = getTodayISO()): string | null;
function validateValidityStartDate(fields: CampaignFormFields, todayISO: string = getTodayISO()): string | null;
```
</interfaces>

<decisions_pending>
| # | Decisão | Recomendação | Status |
|---|---------|--------------|--------|
| D1 | Como obter "hoje" de forma determinística nos testes | `vi.useFakeTimers({ toFake: ["Date"] })` + `vi.setSystemTime(new Date("2026-08-20T12:00:00"))` num describe dedicado (fakes apenas Date → `waitFor` continua funcional). Alternativa rejeitada: injetar todayISO via hook (mudaria API pública desnecessariamente) | ⏳ aguardando aprovação |
| D2 | `getTodayISO` usa data LOCAL (getFullYear/getMonth/getDate) | Sim — requisito do usuário (evitar timezone acidental); `new Date()` aparece só aqui, nunca para parsear data de usuário | ⏳ aguardando aprovação |
| D3 | Ordem das checagens por campo | Presença → passado → ordem (range). Cada campo emite no máximo um erro determinístico; regra `start <= end` inalterada | ⏳ aguardando aprovação |
| D4 | `todayISO` como parâmetro opcional nos validators (default runtime real) | Sim — validators continuam puros/testáveis e `validateField`/`handleSubmit` não mudam | ⏳ aguardando aprovação |

**Fora deste quick (confirmado):** prompts (`campaign-image-director*.md`), copy director, reviewer multi-imagens (`image-review-service.ts`), backend, schema/rota (`GenerateImageRequestSchema` mantém `validity?: string`), UI (`ValidityField`/`campaign-input-form.tsx`), spec `offer-validity-modes`.
</decisions_pending>

<tasks>

<task type="auto">
  <name>Task 1: getTodayISO + validação de data passada nos validators de validade</name>
  <files>src/components/flow/use-campaign-form.ts</files>
  <action>
    Em `src/components/flow/use-campaign-form.ts`:

    1. **Novo helper exportado (único uso de `new Date()` — para obter hoje no runtime):**
       ```typescript
       export function getTodayISO(now: Date = new Date()): string {
         const y = now.getFullYear();
         const m = String(now.getMonth() + 1).padStart(2, "0");
         const d = String(now.getDate()).padStart(2, "0");
         return `${y}-${m}-${d}`;
       }
       ```
       - Converte a data LOCAL corrente para `YYYY-MM-DD` (getFullYear/getMonth/getDate — sem UTC/timezone). Determinístico dado `now`.
       - NUNCA usar `new Date()` para parsear datas do usuário — comparação permanece lexicográfica por string ISO.

    2. **`validateValidityEndDate(fields, todayISO = getTodayISO())`:**
       - Manter exigência por modo (`until-date`/`range`) e erro genérico quando vazio (ordem de checagem preservada).
       - **NOVO (respeita o modo):** aplicar a checagem de passado APENAS quando `validityMode` é `until-date` ou `range`; demais modos retornam `null` independentemente de datas preenchidas (draft antigo trocado para `custom` NÃO bloqueia). Se `fields.validityEndDate` preenchido e `fields.validityEndDate < todayISO` → retornar `"Data final não pode ser anterior à data de hoje"`.
       - `todayISO` é string ISO `YYYY-MM-DD`; comparação `<` lexicográfica é segura (mesmo padrão D5 das linhas 254-256).

    3. **`validateValidityStartDate(fields, todayISO = getTodayISO())`:**
       - Manter: só age no `range`; erro genérico quando vazio; regra `start <= end` com mensagem `"Data inicial não pode ser posterior à data final"` (inalterada).
       - **NOVO:** se `validityStartDate` preenchido e `validityStartDate < todayISO` → retornar `"Data inicial não pode ser anterior à data de hoje"` (antes da checagem de ordem).
       - Datas iguais a hoje passam (não é `<`); `start === end` continua permitido.

    4. `validateField` (`:262-284`) e `handleSubmit` (`:767-823`) NÃO mudam — os validators usam o default `getTodayISO()` em runtime. Modos `today`/`stock`/`custom` continuam fora das exigências (nenhuma checagem contra hoje).
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | Select-String "error TS"</automated>
  </verify>
  <done>getTodayISO exportado (data local YYYY-MM-DD, determinístico dado now); validators de validade bloqueiam data passada com as mensagens exatas por campo; handleSubmit/validateField/UI/rota intactos.</done>
</task>

<task type="auto">
  <name>Task 2: testes de bloqueio por data passada + regressão dos existentes</name>
  <files>src/components/flow/__tests__/use-campaign-form-validity.test.ts</files>
  <action>
    Em `src/components/flow/__tests__/use-campaign-form-validity.test.ts`:

    1. **NOVO describe** `"Q-P3U: validade no passado bloqueia submit (hoje determinístico)"`:
       - `beforeEach`: `vi.useFakeTimers({ toFake: ["Date"] })`; `vi.setSystemTime(new Date("2026-08-20T12:00:00"))`; `afterEach`: `vi.useRealTimers()`.
       - Reutilizar `OFFER_FIELDS`/`VALID_DATA_URL`/`sessionStorage` do padrão D2/D5 (mesmo setup do describe existente).
       - **Setup completo nos testes de passagem** ("fetch chamado"): `mockRestoreFormState.mockReturnValue(OFFER_FIELDS)` + `sessionStorage.setItem("campaign_draft_image", VALID_DATA_URL)` + `storeId` + `fetchMock` resolvido com `createNdjsonResponse([{ type: "result", campaignId, campaignUrl }])` — assim um eventual erro de OUTRO campo obrigatório não mascara a data (se falhar por setup, é fragilidade de setup, não da data).
       - Testes:
         - **a)** until-date com `validityEndDate = "2026-08-19"` (passado) → `handleSubmit` bloqueia, `fetchMock` NÃO chamado, `fieldErrors.validityEndDate === "Data final não pode ser anterior à data de hoje"`.
         - **b)** until-date com `validityEndDate = "2026-08-20"` (hoje) → passa nessa validação (fetch chamado, sem erro em `validityEndDate`).
         - **c)** range com `validityStartDate = "2026-08-19"` (passado) + end futuro → bloqueia, `fieldErrors.validityStartDate === "Data inicial não pode ser anterior à data de hoje"`.
         - **d)** range com `validityEndDate = "2026-08-19"` (passado) + start hoje → bloqueia, `fieldErrors.validityEndDate === "Data final não pode ser anterior à data de hoje"`.
         - **e)** range com `start > end` (ambas futuras, ex.: 2026-08-25 > 2026-08-21) → continua bloqueando com `"Data inicial não pode ser posterior à data final"` (mensagem de ordem inalterada; sem erro de passado).
         - **f)** range com início `"2026-08-20"` e fim `"2026-08-20"` (hoje) → permitido nessa validação (fetch chamado).
         - **g)** modos `today`/`stock` (campos de data vazios) → NÃO passam por validação contra hoje (fetch chamado; sem erros de data).
         - **h)** `custom` com `validityEndDate` preenchida no passado (draft antigo) → NÃO bloqueia (checagem de passado só roda em `until-date`/`range`); fetch chamado, sem erros de data.
       - Asserts de bloqueio: síncronos após `await act(async () => { await result.current.handleSubmit(); })` — `expect(fetchMock).not.toHaveBeenCalled()` (padrão do teste D5 existente). Asserts de passagem: `waitFor(() => expect(fetchMock).toHaveBeenCalled())` (padrão existente; `toFake: ["Date"]` mantém timers reais → `waitFor` funcional).
    2. **Teste puro (sem hook):** `getTodayISO(new Date(2026, 7, 20))` → `"2026-08-20"`; e determinismo de mês/dia com padStart (ex.: `new Date(2026, 0, 5)` → `"2026-01-05"`).
    3. **NÃO alterar** os testes D2/D5 existentes (datas futuras fixas permanecem válidas; sem fake timers neles).
  </action>
  <verify>
    <automated>npx vitest run src/components/flow/__tests__/use-campaign-form-validity.test.ts 2>&1 | Select-String "Test Files|Tests "</automated>
  </verify>
  <done>Novos testes Q-P3U verdes (bloqueio passado, permitido hoje, ordem preservada, today/stock/custom sem checagem) + regressão dos testes D2/D5 intacta; getTodayISO testado como puro.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/components/flow/__tests__/use-campaign-form-validity.test.ts` — novos testes + regressão verdes
2. `npm run typecheck` — sem erros
3. `npm run lint` — sem erros
4. Regressão curta do fluxo de campanha: `npx vitest run src/components/flow/__tests__/` (irmãos: submit-error, product-images, notice, navigation, campaign-input-form) — sem quebras
5. Grep gate: `Select-String -Pattern "new Date\(\)" -Path src/components/flow/use-campaign-form.ts` → apenas 1 ocorrência (default do `getTodayISO`); zero mudanças fora dos 2 arquivos (git diff mostra só `use-campaign-form.ts` e o teste)
</verification>

<success_criteria>
- Datas de validade anteriores a hoje bloqueiam `handleSubmit` no frontend (sem `fetch`) com erro inline por campo: "Data final não pode ser anterior à data de hoje" (until-date/range-end) e "Data inicial não pode ser anterior à data de hoje" (range-start).
- Hoje (início e/ou fim) é permitido; `start <= end` preservado com a mensagem original "Data inicial não pode ser posterior à data final".
- Modos `today`/`stock`/`custom` não passam por validação contra hoje.
- Comparação por string ISO (sem `new Date()` para data de usuário); `getTodayISO()` determinístico e testado; fake timers `toFake: ["Date"]` para "hoje" nos testes.
- Sem mudança de contrato/backend/schema/rota/prompts/copy/reviewer/UI — diff restrito aos 2 arquivos.
- Teste focado de validade + typecheck + lint + regressão curta do fluxo verdes.
</success_criteria>

<output>
Create `.planning/quick/260820-p3u-valida-o-de-data-passada-na-validade-da-/260820-p3u-SUMMARY.md` when done (após aprovação e execução)
</output>
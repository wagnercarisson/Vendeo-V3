---
phase: 260820-r4r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/landing/access-request-section.tsx
  - src/components/landing/how-it-works-section.tsx
  - src/components/landing/what-vendeo-creates-section.tsx
  - src/app/page.tsx
  - src/__tests__/app/landing-page.test.tsx
  - .planning/quick/260820-r4r-alinhar-homepage-p-blica-do-vendeo-aos-r/260820-r4r-UAT.md
autonomous: false
requirements: [R4R-LANDING]
must_haves:
  truths:
    - "O primeiro viewport de `/` mostra 'Vendeo' como heading principal inequívoco (H1)"
    - "'Postou, vendeo!' aparece apenas como slogan secundário, nunca como nome principal"
    - "A página descreve o que o app faz com a frase 'O Vendeo é uma plataforma de marketing para pequenos e médios lojistas' mencionando inteligência artificial"
    - "Existe uma seção 'Como funciona' com 4 passos: cadastrar a loja, informar produto/oferta, o Vendeo cria a campanha, revisar e publicar"
    - "Existe uma seção curta 'O Vendeo pode criar' listando capacidades (Arte promocional, Texto e chamada, Legenda, CTA)"
    - "Link visível 'Política de Privacidade' → /privacidade; a rota /privacidade abre sem login"
    - "Homepage continua pública sem login e o CTA respeita a flag publicSignupEnabled"
  artifacts:
    - path: "src/components/landing/access-request-section.tsx"
      provides: "Hero rebranded (H1 'Vendeo' + slogan + frase de funcionalidade) preservando o contrato flag on/off"
      contains: "Vendeo"
    - path: "src/components/landing/how-it-works-section.tsx"
      provides: "Seção estática 'Como funciona' com 4 passos (cadastrar loja → informar oferta → criar campanha → publicar)"
      contains: "Como funciona"
    - path: "src/components/landing/what-vendeo-creates-section.tsx"
      provides: "Seção curta 'O Vendeo pode criar' com capacidades do app (Arte promocional, Texto e chamada, Legenda, CTA)"
      contains: "O Vendeo pode criar"
    - path: "src/app/page.tsx"
      provides: "Renderiza WhatVendeoCreatesSection + HowItWorksSection + rodapé com Política de Privacidade / Termos de Uso / Contato"
      contains: "/privacidade"
    - path: "src/__tests__/app/landing-page.test.tsx"
      provides: "Testes travando marca, funcionalidade, links legais e não-apresentação do slogan como nome"
      contains: "O Vendeo é uma plataforma"
  key_links:
    - from: "src/app/page.tsx (rodapé)"
      to: "/privacidade"
      via: "Link 'Política de Privacidade'"
      pattern: "href=\"/privacidade\""
    - from: "src/app/page.tsx (rodapé)"
      to: "mailto:"
      via: "Link 'Contato' com SUPPORT_EMAIL ?? fallback"
      pattern: "mailto:"
    - from: "src/app/page.tsx"
      to: "src/components/landing/how-it-works-section.tsx"
      via: "import + render entre AccessRequestSection e footer"
      pattern: "HowItWorksSection"
    - from: "src/app/page.tsx"
      to: "src/components/landing/what-vendeo-creates-section.tsx"
      via: "import + render (seção 'O Vendeo pode criar')"
      pattern: "WhatVendeoCreatesSection"
    - from: "src/components/landing/access-request-section.tsx"
      to: "flag publicSignupEnabled"
      via: "CTAs inalterados (GoogleButton / beta badge) atrás do branch existente"
      pattern: "publicSignupEnabled"
---

<objective>
Alinhar a homepage pública do Vendeo aos requisitos de verificação OAuth do Google: a página `/` deve identificar de forma inequívoca o app configurado no OAuth como "Vendeo", descrever o que a plataforma faz (frase de funcionalidade + seções "O Vendeo pode criar" e "Como funciona") e expor links visíveis para a Política de Privacidade e Termos de Uso.

Purpose: O Google recusou/verificou com ressalva o app OAuth porque a homepage parecia login/beta fechado/página genérica. Corrigir o conteúdo público da landing sem tocar em OAuth, Supabase, elegibilidade, legal, banco ou deploy.

Output: Homepage rebranded (H1 "Vendeo" + slogan + frase de funcionalidade + seção "Como funciona" + rodapé com links legais e Contato), testes atualizados/adicionados, UAT manual.

Fora de escopo (não alterar): configuração real do Google Cloud, OAuth/callback/Supabase Auth, regras de elegibilidade freemium, versões legais, banco/migrations, deploy.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/app/page.tsx
@src/components/landing/access-request-section.tsx
@src/components/landing/access-request-form.tsx
@src/middleware.ts
@src/lib/launch-config/config.ts
@src/__tests__/app/landing-page.test.tsx
@src/components/landing/__tests__/access-request-section.test.tsx

# Estado atual relevante
- `src/app/page.tsx` (server component): header mostra "Vendeo" como span; footer já tem links Termos/Privacidade/Uso Aceitável; metadata title "Vendeo — Campanhas profissionais para sua loja".
- `AccessRequestSection` (client): H1 atual é "Campanhas profissionais para lojas físicas" — **não contém "Vendeo"** (motivo central da ressalva do Google). CTA branch por `publicSignupEnabled` (flag on → GoogleButton + Continuar com email → /signup; flag off → badge beta + Solicitar acesso free → #acesso + Entrar → /login).
- "Postou, vendeo!" **não existe** em lugar nenhum do código — será adicionado como slogan.
- **Não existem as seções "O Vendeo pode criar" nem "Como funciona"** na homepage atual — serão adicionadas como server components novos (capacidades do app + passos do produto).
- **Não existe rota nem link "Contato"** — decisão registrada abaixo.
- Middleware: `/` não está no matcher → público por padrão. `/privacidade`, `/termos`, `/uso-aceitavel` estão em `PUBLIC_ROUTES` (src/middleware.ts:5-8) e nas rotas do grupo `(marketing)`.
- `getLaunchConfig()` retorna `publicSignupEnabled: false` por default sem env (src/lib/launch-config/config.ts:39) — testes existentes de `Home()` renderizam o branch flag-off sem mock.
- Padrão de email de suporte já existente: `const supportEmail = process.env.SUPPORT_EMAIL;` com fallback `"suporte@vendeo.tech"` (src/app/(app)/conta/page.tsx:27, linha 131).

<interfaces>
Do src/components/landing/access-request-section.tsx (preservar EXATAMENTE):
```tsx
export function AccessRequestSection({
  entries,
  publicSignupEnabled = false,
}: {
  entries: ChangelogEntry[];
  publicSignupEnabled?: boolean;
})
```
- Branch flag on: `<GoogleButton variant="solid" fullWidth />` + `<Link href="/signup">Continuar com email</Link>` + texto "Leva 2 minutos".
- Branch flag off: badge "Beta fechado — acesso liberado por convite...", CTA `<a href="#acesso">Solicitar acesso free</a>`, `<Link href="/login">Entrar</Link>`; seção `#acesso` com `AccessRequestForm` e estados `submitted`.
- Atributo `data-public-signup-enabled={String(publicSignupEnabled)}` na section hero (manter).

Do src/lib/launch-config/config.ts (fonte da flag):
```ts
export interface LaunchConfig {
  ...
  publicSignupEnabled: boolean;
  captchaEnabled: boolean;
}
export function getLaunchConfig(): LaunchConfig; // default publicSignupEnabled=false
```

Do src/app/(app)/conta/page.tsx (padrão de email de contato — linha 27 e 131):
```ts
const supportEmail = process.env.SUPPORT_EMAIL;
// uso: href={`mailto:${supportEmail ?? "suporte@vendeo.tech"}`}
```
</interfaces>

# Decisão registrada — link "Contato" (requisito do escopo item 1)
Não existe rota `/contato` nem link de contato público no código (grep confirmou). **Decisão:** usar link `mailto:` mínimo e coerente com o padrão já existente no app — `href="mailto:${process.env.SUPPORT_EMAIL ?? "suporte@vendeo.tech"}"`, lido no server component (`page.tsx`), sem criar página nova (mantém o quick task atômico e não inventa rota que não existe). Alternativa futura (rota `/contato`) fica fora deste escopo.

# Decisão registrada — slogan "Postou, vendeo!"
O slogan não existe no código. Será adicionado como linha secundária (pequena, muted/italic, abaixo do H1), NUNCA dentro do H1 nem como heading. Teste trava a ausência de "Postou" no H1.

# Decisão registrada — seções "O Vendeo pode criar" e "Como funciona"
Para reforçar a funcionalidade real do app na avaliação do Google, a homepage ganha duas seções estáticas (server components, sem estado):
- **"O Vendeo pode criar"**: seção curta listando capacidades — `Arte promocional`; `Texto e chamada`; `Legenda para redes sociais`; `CTA` (chamada para ação para orientar o próximo passo do cliente).
- **"Como funciona"**: 4 passos do fluxo SaaS do produto — 1) `Cadastre sua loja`; 2) `Informe sua oferta`; 3) `O Vendeo cria a campanha`; 4) `Revise e publique`. (O cadastro da loja volta como primeiro passo — coerência com o produto SaaS.)

# Decisão registrada — frase de funcionalidade explícita
Frase do hero revisada para ser inequívoca sobre categoria e finalidade: "O Vendeo é uma plataforma de marketing para pequenos e médios lojistas" + menção explícita a "inteligência artificial comercial" no restante da frase.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebrand da landing — H1 "Vendeo" + slogan + funcionalidade + seções "O Vendeo pode criar"/"Como funciona" + rodapé</name>
  <files>src/components/landing/access-request-section.tsx, src/components/landing/how-it-works-section.tsx, src/components/landing/what-vendeo-creates-section.tsx, src/app/page.tsx</files>
  <action>
  1. Em `src/components/landing/access-request-section.tsx`, reescrever apenas o bloco hero (section com `data-public-signup-enabled`), mantendo os branches de CTA flag on/off **byte a byte inalterados** (GoogleButton/Continuar com email/Leva 2 minutos vs badge beta/Solicitar acesso free/Entrar) e o atributo `data-public-signup-enabled`:
     - Trocar o H1 atual (`Campanhas profissionais para <span>lojas físicas</span>`) por um H1 com texto exatamente `Vendeo` (mantendo as classes `font-heading` grandes existentes).
     - Adicionar imediatamente abaixo do H1 uma linha de slogan secundária com o texto exato `Postou, vendeo!`, estilizada como slogan (ex.: `text-accent-green` + `italic` + `text-base sm:text-lg`), visivelmente menor que o H1. O slogan NÃO pode ficar dentro do `<h1>` nem usar `heading` role.
     - Substituir o `<p>` de subcopy existente pela frase de funcionalidade explícita: `O Vendeo é uma plataforma de marketing para pequenos e médios lojistas. Com inteligência artificial comercial, transformamos a oferta da sua loja em campanhas profissionais para redes sociais — sem precisar de design, copywriting ou marketing.` (categoria "marketing", público "lojistas" e tecnologia "IA" explícitos; pode manter acento verde numa palavra-chave se desejado).
     - Não alterar nada fora do bloco hero (seção `#acesso`, NovidadesLink, estados).
  2. Criar `src/components/landing/how-it-works-section.tsx` como **server component** (sem `"use client"`, sem estado): `<section aria-labelledby="como-funciona-heading">` com `<h2 id="como-funciona-heading">Como funciona</h2>` e 4 passos numerados em grid (usar classes de tema já existentes: `bg-bg-surface`, `border-border`, `text-text-secondary`, `font-heading`):
     - Passo 1 — `Cadastre sua loja`: "Crie sua conta e cadastre os dados da sua loja física em poucos minutos."
     - Passo 2 — `Informe sua oferta`: "Conte produto, oferta e preço que você quer divulgar."
     - Passo 3 — `O Vendeo cria a campanha`: "IA comercial define os parâmetros e a renderização programática executa a arte final."
     - Passo 4 — `Revise e publique`: "Confira o resultado e exporte a peça pronta para suas redes sociais."
     - Deve incluir o texto "Vendeo" em pelo menos um passo (passo 3) — reforça a marca.
   2.5. Criar `src/components/landing/what-vendeo-creates-section.tsx` como **server component** (sem `"use client"`, sem estado): `<section aria-labelledby="vendeo-cria-heading">` com `<h2 id="vendeo-cria-heading">O Vendeo pode criar</h2>` e uma lista curta/grid de 4 capacidades (usar classes de tema existentes):
     - `Arte promocional` — a peça visual pronta para publicar.
     - `Texto e chamada` — copy comercial e headline da oferta.
     - `Legenda para redes sociais` — legenda pronta para acompanhar a arte.
     - `CTA` — chamada para ação para orientar o próximo passo do cliente.
     - Deve incluir o texto "Vendeo" no heading (a seção já começa com o nome da marca).
  3. Em `src/app/page.tsx`:
     - Importar e renderizar `<WhatVendeoCreatesSection />` e `<HowItWorksSection />` entre `<AccessRequestSection ... />` e o `<footer>` (dentro do mesmo `div` flex column, com espaçamento vertical adequado — "O Vendeo pode criar" antes de "Como funciona").
     - No rodapé, renomear os links: `Termos` → texto `Termos de Uso` (href `/termos` inalterado), `Privacidade` → texto `Política de Privacidade` (href `/privacidade` inalterado). Manter `Uso Aceitável`, `Novidades` e `Entrar`.
     - Adicionar no rodapé o link `Contato` com `href={`mailto:${process.env.SUPPORT_EMAIL ?? "suporte@vendeo.tech"}`}` (padrão de `conta/page.tsx:27,131` — ver decisão registrada). `page.tsx` é server component, então `process.env` é seguro.
     - NÃO alterar o header, nem `getLaunchConfig`, nem `getAllEntries`, nem a metadata (title já lidera com "Vendeo").
  </action>
  <verify>
    <automated>npm run typecheck; if ($?) { npm run lint }</automated>
  </verify>
  <done>H1 da landing é "Vendeo" com slogan "Postou, vendeo!" secundário; frase "O Vendeo é uma plataforma de marketing para pequenos e médios lojistas..." presente; seções "O Vendeo pode criar" (4 capacidades) e "Como funciona" (4 passos) renderizadas; rodapé com Política de Privacidade/Termos de Uso/Contato(mailto); branches flag on/off de CTA intactos; typecheck e lint limpos.</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar e adicionar testes de landing + 4 gates</name>
  <files>src/__tests__/app/landing-page.test.tsx</files>
  <action>
  Em `src/__tests__/app/landing-page.test.tsx`:
  1. Atualizar o teste `renders hero headline` (linha ~87-92): o heading de nível 1 agora é `Vendeo`. Trocar o matcher para `screen.getByRole("heading", { level: 1 })` com name `Vendeo`.
  2. Adicionar novos testes no describe "Landing page (/)" — todos renderizando `await Home()` (flag-off default, sem mock adicional; `getAllEntries` e `GoogleButton` já mockados):
     - `h1 é exatamente "Vendeo" e NÃO contém o slogan`: `const h1 = screen.getByRole("heading", { level: 1 }); expect(h1).toHaveTextContent("Vendeo"); expect(h1).not.toHaveTextContent("Postou");`
     - `exibe slogan "Postou, vendeo!" como texto secundário`: `expect(screen.getByText(/Postou, vendeo!/)).toBeInTheDocument();` e que não é um heading (ex.: `screen.queryByRole("heading", { name: /Postou/ })` é `null`).
     - `exibe a frase de funcionalidade "O Vendeo é uma plataforma de marketing..."`: `expect(screen.getByText(/O Vendeo é uma plataforma de marketing/)).toBeInTheDocument();`
     - `exibe seção "O Vendeo pode criar" com as 4 capacidades`: `screen.getByRole("heading", { name: "O Vendeo pode criar" })` + textos `Arte promocional`, `Texto e chamada`, `Legenda para redes sociais` e `CTA`.
     - `exibe seção "Como funciona" com os 4 passos`: `screen.getByRole("heading", { name: "Como funciona" })` + textos `Cadastre sua loja`, `Informe sua oferta`, `O Vendeo cria a campanha` e `Revise e publique`.
     - `exibe link "Política de Privacidade" apontando para /privacidade`: `getByRole("link", { name: "Política de Privacidade" })` com `href === "/privacidade"`.
     - `exibe link "Termos de Uso" apontando para /termos`: `getByRole("link", { name: "Termos de Uso" })` com `href === "/termos"`.
     - `exibe link "Contato" como mailto`: `getByRole("link", { name: "Contato" })` com `href` começando com `mailto:`.
     - `regressão — CTAs flag-off intactos`: manter existentes (`Solicitar acesso free` → `#acesso`, `Entrar` → `/login`, formulário com label `Email`).
  3. NÃO alterar `src/components/landing/__tests__/access-request-section.test.tsx` — o contrato flag on/off que ele trava não muda (herói novo não toca nos asserts dele). Rodar a suíte inteira para confirmar.
  4. Rodar os 4 gates: `npm test`, `npm run typecheck`, `npm run lint`, `npm run build`.
  </action>
  <verify>
    <automated>npm test; if ($?) { npm run typecheck }; if ($?) { npm run lint }; if ($?) { npm run build }</automated>
  </verify>
  <done>landing-page.test.tsx atualizado (H1 "Vendeo") + novos testes (marca, slogan secundário, funcionalidade, "O Vendeo pode criar", "Como funciona" 4 passos, links legais, Contato mailto) passando; 4 gates verdes (test/typecheck/lint/build); nenhum outro teste quebrado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT manual — verificação dos requisitos do Google OAuth</name>
  <what-built>Homepage pública rebranded (H1 "Vendeo", slogan, funcionalidade, seções "O Vendeo pode criar" e "Como funciona", rodapé com links legais e Contato).</what-built>
  <how-to-verify>
  Checklist manual (registrar evidências em `.planning/quick/260820-r4r-alinhar-homepage-p-blica-do-vendeo-aos-r/260820-r4r-UAT.md`):
1. Abrir `/` em aba anônima/deslogada — a página abre SEM login.
   2. No primeiro viewport, "Vendeo" aparece de forma inequívoca como título principal (H1) — não parece apenas login, beta fechado ou página genérica.
   3. Confirmar "Postou, vendeo!" aparece como slogan secundário, nunca como nome principal.
   4. Rolar a página: seção "O Vendeo pode criar" lista as capacidades (Arte promocional, Texto e chamada, Legenda, CTA) e seção "Como funciona" mostra os 4 passos (Cadastre sua loja → Informe sua oferta → O Vendeo cria a campanha → Revise e publique).
   5. Clicar em "Política de Privacidade" no rodapé → `/privacidade` abre sem login e mostra a Política de Privacidade.
   6. Comparar a URL de Política de Privacidade linkada na homepage com a URL configurada no Google Cloud Console (OAuth consent screen) — devem ser a mesma, preferencialmente `https://vendeo.tech/privacidade`. (Configuração real do Google Cloud NÃO deve ser alterada neste escopo — apenas conferência.)
   7. Clicar em "Termos de Uso" → `/termos` abre sem login. Clicar em "Contato" → abre o cliente de email (mailto).
   8. Conforme ambiente (local: flag off por default; produção: conforme `VENDEO_PUBLIC_SIGNUP_ENABLED`): confirmar que o CTA principal (Solicitar acesso free / Continuar com Google) continua funcionando e compatível com a flag.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nenhuma nova | Mudança 100% em conteúdo estático público (landing + links de navegação). Sem dados, sem autenticação, sem API alterada. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-R4R-01 | Spoofing | mailto: Contato | accept | Link `mailto:` com `SUPPORT_EMAIL` de env (fallback hardcoded `suporte@vendeo.tech`) — mesmo padrão já usado em `conta/page.tsx`; sem formulário de contato novo, sem superfície de entrada de dados |
| T-R4R-02 | Tampering | conteúdo público da landing | accept | Conteúdo estático server-rendered, sem entrada de usuário; alteração exigiria comprometimento do repo/deploy (fora deste escopo) |
</threat_model>

<verification>
- 4 gates: `npm test`, `npm run typecheck`, `npm run lint`, `npm run build` (Task 2).
- Grep gate de higiene (executar após Task 1, sem `grep -c` em comentários): `rg -n 'Postou' src/components/landing/access-request-section.tsx` deve retornar apenas a linha do slogan (fora do `<h1>`); `rg -n '<h1' src/components/landing/access-request-section.tsx` deve mostrar H1 contendo somente "Vendeo"; `rg -n 'O Vendeo pode criar|Cadastre sua loja' src/components/landing/` deve localizar as duas novas seções.
- UAT humano: checklist do Task 3, evidências em `260820-r4r-UAT.md`.
</verification>

<success_criteria>
- [ ] `/` mostra "Vendeo" como H1 inequívoco no primeiro viewport, com slogan "Postou, vendeo!" claramente secundário
- [ ] Frase "O Vendeo é uma plataforma de marketing para pequenos e médios lojistas..." presente no hero (com inteligência artificial)
- [ ] Seção "O Vendeo pode criar" com as 4 capacidades renderizada na homepage
- [ ] Seção "Como funciona" com 4 passos renderizada na homepage
- [ ] Rodapé com links visíveis: "Política de Privacidade" → /privacidade, "Termos de Uso" → /termos, "Contato" → mailto
- [ ] Homepage continua pública sem login; CTA compatível com `publicSignupEnabled` (branches flag on/off intactos, cobertos por teste)
- [ ] Testes atualizados/adicionados passando; 4 gates verdes
- [ ] UAT humano aprovado com evidências em `260820-r4r-UAT.md`
- [ ] Nenhuma alteração em OAuth/Supabase/elegibilidade/legal/banco/deploy
</success_criteria>

<output>
Create `.planning/quick/260820-r4r-alinhar-homepage-p-blica-do-vendeo-aos-r/260820-r4r-SUMMARY.md` when done (incluir resultado dos 4 gates e evidências do UAT).
</output>
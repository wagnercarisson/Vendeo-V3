---
phase: 260820-t0o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/shell/topbar.tsx
  - src/components/shell/account-menu.tsx
  - src/__tests__/components/shell/topbar.test.tsx
  - src/__tests__/components/shell/account-menu.test.tsx
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Em telas pequenas (mobile), o hamburger de navegação permanece visível e nunca é comprimido/oculto (shrink-0)"
    - "Em telas pequenas, a CTA 'Nova Campanha' ocupa o mínimo de espaço (apenas ícone Plus, min-w-44, justify-center, gap-0 px-2), sem apertar o hamburger"
    - "O container direito (CTA + conta) tem shrink-0, evitando disputa de encolhimento com o lado esquerdo"
    - "Em telas pequenas, o botão da conta não exibe email/nome (hidden sm:inline-block), ocupando menos espaço, mas continua acessível e clicável"
    - "Os textos 'Nova Campanha' e o email/nome da conta permanecem no DOM (ocultos só via CSS responsivo), preservando os testes existentes"
    - "Todos os alvos de toque continuam com min-h/min-w de 44px"
  artifacts:
    - path: "src/components/shell/topbar.tsx"
      provides: "Topbar responsiva (hamburger shrink-0, CTA icon-only em mobile)"
      contains: "shrink-0"
    - path: "src/components/shell/account-menu.tsx"
      provides: "Trigger da conta compacto em mobile com email/nome oculto via CSS"
    - path: "src/__tests__/components/shell/topbar.test.tsx"
      provides: "Testes que verificam hamburger shrink-0 + CTA aria-label + alvo 44px"
    - path: "src/__tests__/components/shell/account-menu.test.tsx"
      provides: "Testes que verificam aria-label + trigger compacto preservando DOM"
  key_links:
    - from: "src/components/shell/topbar.tsx"
      to: "src/__tests__/components/shell/topbar.test.tsx"
      via: "getByText('Nova Campanha') / getByLabelText('Abrir menu de navegação')"
      pattern: "getByText|getByLabelText"
    - from: "src/components/shell/account-menu.tsx"
      to: "src/__tests__/components/shell/account-menu.test.tsx"
      via: "getByText(email) / getByLabelText / getByRole('button')"
      pattern: "getByText|getByLabelText|getByRole"
---

<objective>
Corrigir a Topbar mobile para que o hamburger de navegação nunca fique encoberto ou ausente em telas pequenas, tornando a barra compacta (hamburger shrink-0, CTA "Nova Campanha" com apenas ícone Plus no mobile, conta sem email/nome no mobile, espacamentos responsivos).

Purpose: Resolver o defeito visual onde o hamburger é espremido/oculto pela CTA e pelo email/nome da conta em viewports estreitas, garantindo que o menu de navegação permaneça acessível e que a topbar não estoure o layout. Escopo restrito a `topbar.tsx` e `account-menu.tsx` (e seus testes) — sem alterações em sidebar/drawer/layout, sem sidebar recolhível, sem band-aid global `overflow-x-hidden`.

Output: Topbar e AccountMenu compactos no mobile, com os textos preservados no DOM (ocultos via classes responsivas) para não quebrar os testes existentes, e testes novos cobrindo as mudanças.
</objective>

<execution_context>
@C:/Users/wagne/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/wagne/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Fonte a corrigir
@src/components/shell/topbar.tsx
@src/components/shell/account-menu.tsx

# Testes existentes que DEVEM permanecer verdes (contratos de DOM preservados)
@src/__tests__/components/shell/topbar.test.tsx
@src/__tests__/components/shell/account-menu.test.tsx

<interfaces>
# Contratos de teste que o executor deve preservar SEM alterar:

# src/__tests__/components/shell/topbar.test.tsx
# - screen.getByText("Nova Campanha") DEVE resolver  -> o texto "Nova Campanha" precisa permanecer como nó de texto no DOM.
#   Ocultação visual no mobile via classe CSS (ex.: <span className="hidden sm:inline">Nova Campanha</span>) mantém o nó no DOM e o teste passa.
#   O teste reload faz: screen.getByText("Nova Campanha").closest("a") -> o span continua dentro do <a> da CTA.
# - screen.getByText("Vendeo") para o branding (span com storeName ?? "Vendeo").
# - Hamburger: getByLabelText("Abrir menu de navegação") deve ter className contendo "min-h-[44px]" e "min-w-[44px]".
# - CTA: getByText("Nova Campanha").closest("a").className DEVE conter "min-h-[44px]".
# - Conta: getByText("test@test.com").closest("button").className DEVE conter "min-h-[44px]" -> o email precisa permanecer como nó no DOM (ocultar via CSS responsivo, NÃO via renderização condicional).

# src/__tests__/components/shell/account-menu.test.tsx
# - screen.getByText(email) e fallback screen.getByText("abc-123-") DEVEM resolver -> o span de displayName deve permanecer no DOM
#   mesmo quando visualmente oculto no mobile (usar classe responsiva, ex.: "hidden sm:block", NÃO renderização condicional).
# - trigger: getByRole("button") com aria-haspopup="true", aria-expanded alternando true/false, Escape fecha, e chevron
#   com prefers-reduced-motion remove "duration-200". Tudo deve permanecer.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tornar hamburger shrink-0 e CTA "Nova Campanha" icon-only no mobile</name>
  <files>src/components/shell/topbar.tsx</files>
  <action>
    Edite `src/components/shell/topbar.tsx`:

    1. HAMBURGER (button com aria-label="Abrir menu de navegação"): adicione a classe Tailwind `shrink-0` ao className existente
       (que já contém `min-h-[44px] min-w-[44px] ... md:hidden`). Mantenha `md:hidden` e as dimensões 44x44. O `shrink-0`
       impede que o flex item encolha e seja espremido/oculto em telas pequenas. Resultado esperado no className:
       `inline-flex shrink-0 min-h-[44px] min-w-[44px] items-center justify-center rounded-lg p-2 text-text-secondary hover:bg-bg-elevated hover:text-text-primary md:hidden`.

    2. CTA "Nova Campanha" (Link para /campanhas/nova): torne-o compacto e icon-only no mobile. Transforme o rótulo de texto
       em um span com ocultação responsiva, mantendo o nó de texto no DOM (crítico para os testes). Substitua o texto literal
       "Nova Campanha" por `<span className="hidden sm:inline">Nova Campanha</span>`. Assim, no mobile (< sm) o span é
       visualmente oculto via CSS mas permanece no DOM, e nos breakpoints maiores (>= sm) o texto volta a aparecer ao lado do
       ícone Plus. Adicione `aria-label="Criar nova campanha"` no Link (acessível quando só o ícone é visível no mobile).
       Deixe o className do Link explicitamente compacto no mobile, com touch target de 44px e o ícone centralizado:
       `inline-flex min-h-[44px] min-w-[44px] items-center justify-center gap-0 rounded-lg bg-accent-green px-2 py-1.5 text-sm font-semibold text-white font-heading hover:brightness-110 transition-all duration-200 sm:gap-1.5 sm:px-3`.
       Notas: `min-w-[44px]` garante alvo de toque quadrado mesmo com só o ícone; `justify-center` centraliza o Plus quando o
       texto some; `gap-0 sm:gap-1.5` e `px-2 sm:px-3` removem o excesso de espaço no mobile e restauram o espaçamento em telas
       maiores. O ícone `<Plus className="h-4 w-4" />` permanece sempre visível (é ele que representa a CTA no mobile).

    3. CONTAINER DIREITO E ESPAÇAMENTO RESPONSIVO: adicione `shrink-0` ao container direito que envolve a CTA e o
       `<AccountMenu />` (atualmente `<div className="flex items-center gap-3">`), para ele não entrar em disputa de shrink com
       o lado esquerdo: `<div className="flex shrink-0 items-center gap-1 sm:gap-3">`. Ajuste também o espaçamento do header e
       das divs interna/externa de forma responsiva. Ex.: no header, considere `px-3 sm:px-4`; no container esquerdo
       `gap-2 sm:gap-3`. Use apenas classes responsivas — NÃO adicione `overflow-x-hidden` global ou qualquer band-aid de
       overflow. NÃO altere sidebar/drawer/layout.

    NÃO use renderização condicional para ocultar o texto (os testes exigem o nó no DOM). NÃO remova o handler
    `handleNewCampaignClick` (contrato de reload preservado).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/components/shell/topbar.test.tsx 2>&1 | Select-String -Pattern "Test Files|Tests|failed"</automated>
  </verify>
  <done>
    - Hamburger tem `shrink-0` no className e mantém `min-h-[44px]` + `min-w-[44px]` + `md:hidden`.
    - Texto "Nova Campanha" permanece no DOM como span com classe `hidden sm:inline`.
    - Link da CTA tem `aria-label="Criar nova campanha"` e className com `min-h-[44px]` + `min-w-[44px]` + `justify-center` + `gap-0 sm:gap-1.5` + `px-2 sm:px-3`.
    - Container direito da CTA/AccountMenu tem `shrink-0` e espaçamento responsivo `gap-1 sm:gap-3`.
    - topbar.test.tsx passa integralmente (8 testes verdes).
  </done>
</task>

<task type="auto">
  <name>Task 2: Tornar trigger da conta compacto no mobile (email/nome oculto via CSS)</name>
  <files>src/components/shell/account-menu.tsx</files>
  <action>
    Edite `src/components/shell/account-menu.tsx`:

    1. TRIGGER (button com aria-haspopup="true"): adicione `aria-label="Menu da conta"` (acessível quando o email/nome está
       oculto visualmente no mobile — passa a ser também o nome acessível no desktop, substituindo o email para leitores de
       tela, o que é aceitável e até melhor por privacidade/clareza). Deixe o className do trigger explicitamente compacto no
       mobile, com touch target de 44px e o chevron centralizado:
       `inline-flex min-h-[44px] min-w-[44px] items-center justify-center gap-0 rounded-lg px-2 py-1.5 text-sm text-text-secondary hover:bg-bg-elevated hover:text-text-primary transition-colors duration-200 font-body sm:gap-2 sm:px-3`.
       Notas: `min-w-[44px]` garante alvo de toque quadrado mesmo com só o chevron; `justify-center` centraliza o chevron
       quando o email some; `gap-0 sm:gap-2` e `px-2 sm:px-3` removem o excesso de espaço no mobile e restauram em telas
       maiores.

    2. SPAN DO displayName (atualmente `<span className="max-w-[120px] truncate">{displayName}</span>`): adicione ocultação
       responsiva mantendo o nó no DOM — crítico para account-menu.test.tsx (getByText(email) e getByText("abc-123-") precisam
       resolver, e o teste do trigger usa getByText(email).closest("button")). Troque o className para
       `hidden sm:inline-block max-w-[120px] truncate`. Use `sm:inline-block` (mais coerente com o contexto inline-flex do
       botão) em vez de `sm:block`. Assim no mobile (< sm) o email/nome é oculto via CSS mas permanece no DOM, e em telas
       maiores reaparece.

    NÃO use renderização condicional (os testes exigem o nó de texto no DOM). NÃO altere o dropdown (Configurações /
    Novidades / Logout), o estado isOpen, os handlers de clique/fora, o Escape, nem a lógica reduced-motion do chevron.
    Ajuste apenas o trigger e o span do displayName. Não adicione overflow band-aid.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/components/shell/account-menu.test.tsx 2>&1 | Select-String -Pattern "Test Files|Tests|failed"</automated>
  </verify>
  <done>
    - Trigger tem `aria-label="Menu da conta"` e className com `min-h-[44px]` + `min-w-[44px]` + `justify-center` + `gap-0 sm:gap-2` + `px-2 sm:px-3`.
    - Span do displayName tem classe responsiva `hidden sm:inline-block` (email/nome oculto no mobile, no DOM em todos os breakpoints).
    - account-menu.test.tsx passa integralmente (7 testes verdes).
  </done>
</task>

<task type="auto">
  <name>Task 3: Adicionar testes de regressão para as novas classes responsivas e rodar regressão dos 4 gates</name>
  <files>src/__tests__/components/shell/topbar.test.tsx, src/__tests__/components/shell/account-menu.test.tsx</files>
  <action>
    Adicione testes de regressão que travem as mudanças, SEM quebrar os contratos existentes:

    1. Em `src/__tests__/components/shell/topbar.test.tsx`, adicione:
       - Um teste: o hamburger (`getByLabelText("Abrir menu de navegação")`) tem className contendo `shrink-0`.
       - Um teste: a CTA (`getByText("Nova Campanha").closest("a")`) tem `aria-label` igual a "Criar nova campanha"; e que o
         texto "Nova Campanha" está dentro de um span cujo className contém `hidden` e `sm:inline` (mantendo o nó no DOM).
         Use `screen.getByText("Nova Campanha").className` para checar.
       - Um teste: o container direito (que envolve a CTA e o AccountMenu) tem className contendo `shrink-0`. Para localizá-lo,
         use o pai do link da CTA (ex.: `screen.getByText("Nova Campanha").closest("a")?.parentElement`) e cheque `shrink-0`.

    2. Em `src/__tests__/components/shell/account-menu.test.tsx`, adicione:
       - Um teste: o trigger (`getByRole("button")`) tem `aria-label` igual a "Menu da conta".
       - Um teste: o span do email (`getByText("user@vendeo.tech")`) tem className contendo `hidden` e `sm:inline-block`
         (confirma que o email está oculto via CSS responsivo mas permanece no DOM).

    Use os mesmos padrões de render/mocks já presentes nos arquivos (jsdom, Testing Library, mock de matchMedia).
    Preserve todos os testes existentes — não altere os asserts atuais.

    Após os testes, rode a regressão dos 4 gates do projeto (vitest, typecheck, lint, build) para confirmar que nada
    mais quebrou em todo o codebase.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/components/shell/topbar.test.tsx src/__tests__/components/shell/account-menu.test.tsx 2>&1 | Select-String -Pattern "Test Files|Tests|failed"; npm run typecheck 2>&1 | Select-String -Pattern "error"; npm run lint 2>&1 | Select-String -Pattern "error|warning"</automated>
  </verify>
  <done>
    - Testes novos presentes: hamburger `shrink-0`, CTA `aria-label="Criar nova campanha"` + span `hidden sm:inline`, container direito `shrink-0`, trigger `aria-label="Menu da conta"`, span email `hidden sm:inline-block`.
    - Todos os testes dos dois arquivos passam (8 + 7 + novos = verdes).
    - typecheck, lint e build limpos (4 gates verdes).
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/__tests__/components/shell/topbar.test.tsx src/__tests__/components/shell/account-menu.test.tsx` — todos passam.
- `npm run typecheck` — sem erros.
- `npm run lint` — sem erros.
- `npm run build` — build limpo.
- Inspeção visual manual (checkpoint humano opcional): em viewport estreita (< sm, ex. 375px e 320px), o hamburger está visível e não espremido, a CTA mostra apenas o ícone Plus, e o trigger da conta não mostra email/nome; em viewport >= sm tudo reaparece. Sem scroll horizontal nem sobreposição.
</verification>

<success_criteria>
- Hamburger nunca é comprimido/oculto em telas pequenas (`shrink-0` + 44x44 + `md:hidden` mantidos).
- CTA "Nova Campanha" é icon-only (apenas Plus) em mobile, compacta (`min-w-[44px]`, `justify-center`, `gap-0 sm:gap-1.5`, `px-2 sm:px-3`), texto `hidden sm:inline` preservado no DOM e `aria-label="Criar nova campanha"`.
- Container direito (CTA + AccountMenu) tem `shrink-0` para não disputar shrink com o lado esquerdo.
- Trigger da conta compacto em mobile (`min-w-[44px]`, `justify-center`, `gap-0 sm:gap-2`, `px-2 sm:px-3`), email/nome `hidden sm:inline-block` no DOM, `aria-label="Menu da conta"`, mantendo 44px.
- Nenhum texto foi removido do DOM — todos os testes existentes de topbar e account-menu continuam verdes.
- Nenhuma alteração fora de `topbar.tsx`, `account-menu.tsx` e seus testes; sem `overflow-x-hidden` global; sem sidebar/drawer/layout changes.
- 4 gates (vitest, typecheck, lint, build) verdes.
</success_criteria>

<output>
Create `.planning/quick/260820-t0o-corrigir-topbar-mobile-hamburger-encober/260820-t0o-SUMMARY.md` when done
</output>

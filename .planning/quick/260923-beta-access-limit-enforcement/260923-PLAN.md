# Quick Task 260923 — Enforcement atômico do limite do beta

**Data:** 2026-09-23
**Modo:** GSD Quick
**Status:** Em execução local; sem aplicação remota

## Objetivo

Impedir atomicamente que `admin_review_access_request` aprove uma solicitação quando já existem 50 emails distintos, comparados com `lower(email)`, em `access_requests.status = 'approved'`.

## Escopo

- Migration aditiva redefinindo a RPC existente.
- `pg_advisory_xact_lock` dedicado no caminho `approve`.
- Erro estável `access_limit_reached`, sem mutação ou auditoria quando bloqueado.
- Rejeições continuam permitidas no limite.
- Rota admin retorna HTTP 409 com `Limite do beta atingido.`.
- Testes PostgreSQL locais para limite, case-insensitive, rollback/ausência de auditoria e concorrência.

## Restrições

- Executar somente contra Supabase local/isolado.
- Não executar `db push --linked`.
- Não alterar produção, Vercel, flags, usuários ou documentos jurídicos.
- Não marcar F50 ou 17.3.

## Verificação

- Testes focados da rota e integração PostgreSQL local.
- Typecheck.
- `openspec validate --strict --changes`.
- `git diff --check`.

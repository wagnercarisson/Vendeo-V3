# UAT — Fase 29.3: Créditos Mensais Automáticos

**Data:** 2026-07-22
**Ambiente:** Supabase production/staging
**Tester:** Agente externo de validação

---

## 1. Pré-execução

### 1.1 Backup / Export manual CSV antes da migration

| Tabela | Registros exportados |
|--------|---------------------|
| `credit_balances` | Sim |
| `credit_transactions` | Sim |
| `stores` | Sim |

### 1.2 Migration aplicada

- **Arquivo:** `supabase/migrations/20260722000002_creditos_mensais_automaticos.sql`
- **Status:** Aplicado com sucesso
- **Rollback:** Revert path documentado como comentário no próprio arquivo. Rollback real não executado (decisão operacional).

---

## 2. Pós-migration — Validação de dados

### 2.1 Balanço

| Métrica | Valor |
|---------|-------|
| Total de registros em `credit_balances` | 6 |
| Saldos coerentes (`balance = bonus_balance + purchased_balance`) | 6 |
| Saldos incoerentes | 0 |

### 2.2 Tipos de transação encontrados

| Tipo | Quantidade |
|------|-----------|
| `admin_grant` | 14 |
| `bonus_onboarding` | 1 |
| `deduction` | 29 |
| `refund` | 7 |
| `grant` (antigo — esperado 0) | 0 |

### 2.3 Backfill verificado

- Toda transação `type = 'grant'` migrada para `bonus_onboarding` ou `admin_grant` conforme `reason`
- `credit_balances.bonus_balance` populado com saldo anterior (`balance`)
- `credit_balances.purchased_balance = 0` para todos os registros

---

## 3. Validação do Cron

### 3.1 Configuração

| Variável | Valor |
|----------|-------|
| `VENDEO_MONTHLY_CREDITS_ENABLED` | `false` (teste) |
| `CRON_SECRET` | Sensível (não registrado) |

### 3.2 Resultado

- Requisição: `GET /api/cron/monthly-credits` com `Authorization: Bearer <CRON_SECRET>`
- Resposta: `{"skipped":true}`
- Comportamento correto: `monthlyCreditsEnabled=false` faz o cron retornar sem executar a RPC

### 3.3 Middleware corrigido

Commit `41e9156` — middleware ajustado para permitir rota `/api/cron/monthly-credits` sem redirect de autenticação.

---

## 4. Cenários validados (SQL manual / integração)

| # | Cenário | Resultado |
|---|---------|-----------|
| 4.1 | Loja velha (>30 dias) sem grant → recebe monthlyCreditsAmount | ✅ |
| 4.2 | Loja no teto (bonus_balance = cap) → pulada | ✅ |
| 4.3 | Grant parcial (2 créditos até o cap) | ✅ |
| 4.4 | Duplicidade: mesma execução duas vezes → idempotente | ✅ |
| 4.5 | Concorrência: SKIP LOCKED previne race | ✅ (testado via SQL) |
| 4.6 | reserve_credit: bônus primeiro, comprado por último | ✅ |
| 4.7 | refund_credit: restaura buckets exatos do metadata | ✅ |
| 4.8 | refund_credit legacy: fallback para bonus_balance | ✅ |

---

## 5. Pendências pós-UAT (corrigidas)

- [x] Testes unitários para 4 novas flags de Launch Config em `src/lib/launch-config/__tests__/config.test.ts`
- [x] `CRON_SECRET` adicionado ao `.env.example`
- [x] Migration incremental `20260722000003_add_grant_type_admin_audit.sql` — adiciona `grant_type: 'admin_grant'` na metadata do audit log
